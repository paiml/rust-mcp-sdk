---
phase: 82-builder-dx-prerequisites
plan: 02
type: execute
wave: 2
depends_on:
  - "82-01"
files_modified:
  - tests/in_process_handler_pattern.rs
autonomous: true
requirements:
  - BLDR-01
  - BLDR-02
  - BLDR-03
tags:
  - integration-test
  - handler-level-testing
  - regression-anchor
user_setup: []

must_haves:
  truths:
    - "`tests/in_process_handler_pattern.rs` exists and exercises the handler-level testing pattern against a real built `pmcp::Server`"
    - "The integration test registers a tool via `tool_arc(...)`, retrieves it via `get_tool(...)`, calls `handle(args, RequestHandlerExtra::default()).await`, and asserts on the result"
    - "The integration test registers a prompt via `prompt_arc(...)`, retrieves it via `get_prompt(...)`, calls `handle(args, RequestHandlerExtra::default()).await`, and asserts on the result"
    - "A property test verifies that `tool(name, T)` and `tool_arc(name, Arc::new(T))` produce structurally equivalent post-build state — concretely, both servers' registered `get_tool(name)` handlers, when invoked with the same `handle(args, extra)`, return byte-identical `serde_json::Value` outputs"
    - "The test exits 0 under `cargo test --test in_process_handler_pattern --features full`"
  artifacts:
    - path: tests/in_process_handler_pattern.rs
      provides: "Regression-anchor integration test for the BLDR-03 handler-level testing pattern, exercising both `tool_arc`+`get_tool` and `prompt_arc`+`get_prompt` round-trips on a real `pmcp::Server`"
      min_lines: 80
      contains: "server.get_tool"
  key_links:
    - from: "tests/in_process_handler_pattern.rs"
      to: "pmcp::ServerBuilder::tool_arc + Server::get_tool"
      via: "build → get_tool → handle → assert"
      pattern: "tool_arc\\(.*Arc::new"
    - from: "tests/in_process_handler_pattern.rs"
      to: "pmcp::ServerBuilder::prompt_arc + Server::get_prompt"
      via: "build → get_prompt → handle → assert"
      pattern: "prompt_arc\\(.*Arc::new"
---

<!-- Revised after cross-AI review (commit 08ea6727). Addresses REVIEWS.md items: 1, 3, 8, 9, 11. Task 1: fixed the self-failing `handle_request` negative grep (item 1), tightened the misleading `ToolHandler` trait reference in `<read_first>` (item 9), and added a precondition assertion for the `pmcp::types::Role` import path (item 8). Task 2: dropped the unobservable `capabilities.tools` from `must_haves` (item 3 — moved to Plan 01 Task 3 as a crate-internal test); removed the speculative proptest-not-available fallback branch since `proptest = "1.7"` is confirmed at Cargo.toml:131 (item 11). -->

<objective>
Create the BLDR-03 regression anchor: a standalone integration test at `tests/in_process_handler_pattern.rs` that drives a real built `pmcp::Server` via the documented handler-level testing pattern, exercising BOTH the new `tool_arc`/`get_tool` path and the existing `prompt_arc`/`get_prompt` path end-to-end. Includes a property test confirming `tool(...)` and `tool_arc(...)` produce equivalent post-build state observable through the public API.

Purpose (per CONTEXT.md D-02 part (b) + §Claude's Discretion item 3): if a future refactor breaks the handler-level testing pattern that external toolkit authors rely on (Phase 83+), this test fails — closing the regression loop. Demonstrates the new public surface (`tool_arc`/`get_tool`) is sufficient on its own to drive a server end-to-end at handler level. The property test catches future drift between the `tool()` (impl-based) and `tool_arc()` (Arc-based) registration paths via the **public** observable: `handle(args, extra)` outputs are byte-equal. (The internal `capabilities` field is not observable from an integration test; that invariant lives in Plan 01 Task 3's crate-internal test.)

Output: a new `tests/in_process_handler_pattern.rs` file with at least three `#[tokio::test]` cases (tool round-trip, prompt round-trip, compose-on-same-builder) plus one property/equivalence test (`tool()` vs `tool_arc()`). All tests use the new public surface exclusively — they do NOT poke at `Server::handle_request` or any other crate-private API.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md
@.planning/phases/82-builder-dx-prerequisites/82-01-PLAN.md
@CLAUDE.md
@tests/skills_integration.rs
@src/server/cancellation.rs

<interfaces>
<!-- Surface this test uses (all public; all delivered by Plan 01 except RequestHandlerExtra/ToolHandler/PromptHandler/Content which already exist). -->

From src/server/mod.rs (after Plan 01 ships):
```rust
impl Server {
    pub fn get_tool(&self, name: &str) -> Option<&Arc<dyn ToolHandler>>;     // NEW in Plan 01
    pub fn get_prompt(&self, name: &str) -> Option<&Arc<dyn PromptHandler>>; // PRE-EXISTING line 385
}

impl ServerBuilder {
    pub fn tool_arc(mut self, name: impl Into<String>, handler: Arc<dyn ToolHandler>) -> Self;       // NEW in Plan 01
    pub fn prompt_arc(mut self, name: impl Into<String>, handler: Arc<dyn PromptHandler>) -> Self;   // NEW in Plan 01
    pub fn tool(mut self, name: impl Into<String>, handler: impl ToolHandler + 'static) -> Self;     // PRE-EXISTING
}
```

The public `pmcp::ToolHandler` trait re-exported at the crate root uses `async fn handle(&self, args, extra) -> Result<Value>`. **NOTE for the implementer:** there is an INNER trait at `src/server/traits.rs` whose `ToolHandler` uses `call_tool` — that is NOT the public re-export and NOT what this test imports. This test imports `pmcp::ToolHandler` (the root re-export, defined in `src/server/mod.rs:224`). Do not be misled by the inner trait.

From src/server/cancellation.rs line 421:
```rust
impl RequestHandlerExtra {
    pub fn default() -> Self; // public; zero-arg constructor; the second argument to handler.handle(args, extra).await
}
```

`pmcp::types::Role` re-export path (precondition — verified at plan-time): `Role` is defined at `src/types/content.rs:298` and re-exported as `pmcp::types::Role` via `pub use types::{... Role ...}` in `src/lib.rs:134-143`. The path `pmcp::types::Role` works (used in `tests/workflow_prompt_e2e_test.rs:11` and `examples/s34_typed_tools_workflow.rs:404` as ground truth). The implementer's first action in Task 1 step 3 (imports) MUST grep-confirm this before writing the use-statement.

Reference test skeleton (tests/skills_integration.rs:146-164) shows the exact prompt round-trip shape; this new file extends it to the tool side and adds the equivalence property test.

`proptest = "1.7"` is in `[dev-dependencies]` at `Cargo.toml:131` — confirmed at plan time. No fallback path required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author `tests/in_process_handler_pattern.rs` with tool + prompt round-trip tests</name>
  <files>tests/in_process_handler_pattern.rs</files>
  <read_first>
    - tests/skills_integration.rs lines 1-40 (file-header style: `//!` module docs, `#![cfg(...)]` feature gating, imports — match this conventions for the new file)
    - tests/skills_integration.rs lines 142-164 (the `wire_level_prompt_text` helper — the prompt round-trip skeleton this test extends)
    - tests/handler_extensions_properties.rs (existing example of a property-test integration file — for the equivalence test in Task 2)
    - src/server/cancellation.rs lines 415-430 (confirm `RequestHandlerExtra::default()` constructor shape)
    - The PLAN 01 PLAN.md (sibling plan in this directory) for the exact `tool_arc` / `prompt_arc` / `get_tool` signatures landing in Wave 1
    - `src/lib.rs` lines 134-143 (the `pub use types::{ ... Role ... }` re-export — confirms `pmcp::types::Role` and `pmcp::Role` both work; this test imports from `pmcp::types::Role` to keep the type origin explicit)
  </read_first>
  <behavior>
    - Behavior 1: A `#[tokio::test] async fn tool_arc_get_tool_handle_round_trip()` builds a server with one tool registered via `tool_arc`, retrieves the handler via `get_tool`, calls `handle(args, RequestHandlerExtra::default()).await?`, and asserts the returned JSON value equals an expected fixture.
    - Behavior 2: A `#[tokio::test] async fn prompt_arc_get_prompt_handle_round_trip()` performs the symmetric round trip for prompts.
    - Behavior 3: Both tests verify the returned `&Arc<dyn ...Handler>` is the same Arc-identity that was passed in (use `Arc::ptr_eq` against a clone retained by the test).
    - Behavior 4: Both tests run under `cargo test --test in_process_handler_pattern --features full` and exit 0.
    - Behavior 5: The file is gated `#![cfg(not(target_arch = "wasm32"))]` (matches `tests/skills_integration.rs:27` shape — `ServerBuilder` is non-wasm).
  </behavior>
  <action>
    Create `tests/in_process_handler_pattern.rs` with this structure:

    1. Module docs (`//!`) explaining: this file is the BLDR-03 regression anchor. It exercises the handler-level testing pattern (CONTEXT.md D-02 part (b)) by driving a real built `pmcp::Server` through the new public `tool_arc` + `get_tool` and existing `prompt_arc` + `get_prompt` surfaces. In a leading comment, state that this test does NOT use `Server::handle_request` because CONTEXT.md D-01 forbids exposing or depending on a public dispatch surface in Phase 82. The negative acceptance check below targets actual private-dispatch USAGE (`.handle_request(` calls or `use ...handle_request` imports), not the literal token in this prose comment — so the explanatory comment is welcome and will not break the assertion.

    2. `#![cfg(not(target_arch = "wasm32"))]` at the top (after module docs).

    3. **Import precondition step** — before writing the `use` block, run a one-liner sanity check at the start of execution:

       ```
       rg -n 'Role' src/lib.rs | head -5
       ```

       Confirm `Role` appears in the `pub use types::{ ... }` block of `src/lib.rs:134-143`. If it does, write `use pmcp::types::Role;` (matching the existing convention in `tests/workflow_prompt_e2e_test.rs:11`). If for any reason it does not, surface the discrepancy as a finding before proceeding — do NOT guess a deeper path.

       The full import block:
       ```
       use pmcp::{Server, ServerBuilder, ToolHandler, PromptHandler, RequestHandlerExtra};
       use pmcp::types::{Content, GetPromptResult, PromptMessage, Role};
       use async_trait::async_trait;
       use serde_json::json;
       use std::sync::Arc;
       use std::collections::HashMap;
       ```

    4. Define an `EchoTool` struct + `#[async_trait] impl ToolHandler for EchoTool` whose `handle` returns `Ok(json!({ "echoed": args }))`. The public `pmcp::ToolHandler` trait (re-exported at the crate root) uses `async fn handle(&self, args: serde_json::Value, extra: RequestHandlerExtra) -> Result<serde_json::Value>` — this is the trait this test imports. Do NOT import from the inner `src/server/traits.rs` `ToolHandler` (which uses `call_tool`); the public root re-export is the only one in scope here.

    5. Define an `EchoPrompt` struct + `#[async_trait] impl PromptHandler for EchoPrompt` whose `handle` returns `Ok(GetPromptResult { description: Some("echo".into()), messages: vec![PromptMessage { role: Role::User, content: Content::Text { text: "hello".into() } }] })` (or the current constructor-driven equivalent — protocol types may be `#[non_exhaustive]`; per Phase 54.1 feedback memory, use any provided `::new(...)` constructor in preference to struct-literal syntax — check the type's current API in `src/types/protocol.rs` and `src/types/content.rs` while writing the test).

    6. `#[tokio::test] async fn tool_arc_get_tool_handle_round_trip()`:
       - `let tool: Arc<dyn ToolHandler> = Arc::new(EchoTool);`
       - `let retained = Arc::clone(&tool);`
       - Build via `pmcp::Server::builder().name("test").version("0").tool_arc("echo", tool).build()?`.
       - `let registered = server.get_tool("echo").expect("registered above");`
       - `assert!(Arc::ptr_eq(registered, &retained));`
       - `let result = registered.handle(json!({"msg": "hi"}), RequestHandlerExtra::default()).await?;`
       - `assert_eq!(result, json!({ "echoed": { "msg": "hi" } }));`
       - Also assert: `server.get_tool("nope").is_none()`.

    7. `#[tokio::test] async fn prompt_arc_get_prompt_handle_round_trip()`:
       - Symmetric structure with `EchoPrompt` and `prompt_arc` + `get_prompt`.
       - Invoke `.handle(HashMap::new(), RequestHandlerExtra::default()).await?`.
       - Assert on `result.messages[0].content` (extract `Content::Text { text }` via match, assert text equals expected).

    8. Add a `#[tokio::test] async fn tool_arc_and_prompt_arc_compose_on_same_builder()` that registers BOTH on a single builder, builds once, and verifies BOTH `get_tool` and `get_prompt` return their handlers — proves the two registration paths don't interfere.

    All tests must use the new public surface only. Do NOT import anything from `crate::` or via `pub(crate)` re-exports; do NOT use `Server::handle_request`. If a test requires a type that is not publicly exported, that itself is a finding — note it but use the public alternative.
  </action>
  <verify>
    <automated>cargo test --test in_process_handler_pattern --features full</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f tests/in_process_handler_pattern.rs` succeeds.
    - Source assertion: the file contains a `#![cfg(not(target_arch = "wasm32"))]` attribute at the top (file-level cfg gate).
    - Source assertion: the file contains exactly three `#[tokio::test]` attributes (tool round-trip, prompt round-trip, compose-on-same-builder) — `grep -c '#\[tokio::test\]' tests/in_process_handler_pattern.rs` returns ≥3.
    - Source assertion: `grep -q 'tool_arc(' tests/in_process_handler_pattern.rs` succeeds (test uses the new surface).
    - Source assertion: `grep -q 'prompt_arc(' tests/in_process_handler_pattern.rs` succeeds.
    - Source assertion: `grep -q 'server.get_tool(' tests/in_process_handler_pattern.rs` succeeds.
    - Source assertion: `grep -q 'Arc::ptr_eq' tests/in_process_handler_pattern.rs` succeeds (identity check confirms the registered Arc matches what was passed in).
    - **Role-import precondition assertion (precedes file creation):** `rg -q 'Role' src/lib.rs` succeeds (confirms the re-export still exists). If this fails at execution time, the implementer surfaces it as a finding before writing imports.
    - Source assertion (after file is written): `grep -q 'use pmcp::types::.*Role' tests/in_process_handler_pattern.rs` succeeds — the test imports `Role` from `pmcp::types`, matching the existing crate convention.
    - **Negative assertion (NARROWED — rejects USAGE not prose):** `! rg --quiet '\.handle_request\(|use .*handle_request' tests/in_process_handler_pattern.rs`. This rejects actual private-dispatch invocations or imports while allowing the explanatory prose comment that mentions the token by name for future-maintainer context. The previous form (`grep -q 'handle_request' ...` must FAIL) was self-contradicting because the task action requires a comment naming the symbol — replaced.
    - Test command: `cargo test --test in_process_handler_pattern --features full` exits 0.
    - Test command: `cargo test --test in_process_handler_pattern --features full -- --nocapture` (sanity) exits 0 with all three test names visible in output.
  </acceptance_criteria>
  <done>Three async integration tests exist in `tests/in_process_handler_pattern.rs`, exercise both the tool and prompt sides of the handler-level testing pattern via the new public surface only, do not invoke or import `Server::handle_request` (verified by a USAGE-narrowed negative grep that no longer self-contradicts the explanatory prose comment), and pass under `cargo test --features full`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add equivalence property test — `tool()` vs `tool_arc()` produce identical handler-level output</name>
  <files>tests/in_process_handler_pattern.rs</files>
  <read_first>
    - tests/in_process_handler_pattern.rs (the file created in Task 1 — append to it)
    - tests/handler_extensions_properties.rs (existing proptest-style property test for handler behavior — confirm proptest setup conventions; note this is plain proptest, not quickcheck)
    - tests/property_tests.rs (additional reference for proptest in this workspace — established pattern for `tokio::runtime::Runtime::new().unwrap().block_on(...)` inside `proptest!` blocks since proptest tests are sync)
    - src/server/mod.rs lines 1971-1983 (the public `tool()` body — confirm it ONLY does `self.tools.insert(name, Arc::new(handler))` + capability flip; the equivalence claim depends on `tool(name, T)` being observationally identical to `tool_arc(name, Arc::new(T))` from the PUBLIC API perspective — i.e., same `handle(...)` output for the same input)
    - `Cargo.toml` line 131 (precondition: `proptest = "1.7"` is in `[dev-dependencies]` — confirmed at plan time; this task hard-states proptest as a precondition and provides no fallback)
  </read_first>
  <behavior>
    - Behavior 1: A property test verifies that for a given input value, `tool("x", EchoTool)` and `tool_arc("x", Arc::new(EchoTool))` both produce a built server whose `get_tool("x").unwrap().handle(args, extra).await` returns the same JSON value.
    - Behavior 2: The test runs under proptest with at least 32 cases via `#![proptest_config(...)]` or `proptest! { #![proptest_config(ProptestConfig { cases: 32, ..ProptestConfig::default() })] ... }`.
    - Behavior 3: The two built servers' `get_tool("echo")` Arcs may differ in identity (different `Arc<dyn ToolHandler>` instances) but their `handle(args, extra)` outputs MUST be byte-equal `serde_json::Value`s for the same `args` — this is the public observable that proves equivalence.
    - Behavior 4: A `proptest-regressions` file may be created automatically by proptest on failure; commit it if so.
  </behavior>
  <action>
    Append to `tests/in_process_handler_pattern.rs` (the file from Task 1):

    1. Add `use proptest::prelude::*;` to the import block (top of file). The `proptest = "1.7"` dev-dep at `Cargo.toml:131` is a **hard precondition** — confirmed at plan time, so this task does not branch on its presence. (The earlier draft of this plan included a speculative "fallback if proptest not available" branch; that branch was removed because Cargo.toml confirms availability.)

    2. Add a `proptest!` block containing one test:

       ```rust
       proptest! {
           #![proptest_config(ProptestConfig { cases: 32, ..ProptestConfig::default() })]
           #[test]
           fn tool_and_tool_arc_produce_observable_equivalence(value in any::<String>()) {
               // Build TWO servers — A with .tool(), B with .tool_arc() — that should
               // be observationally equivalent at handler-invocation level.
               let rt = tokio::runtime::Runtime::new().unwrap();
               rt.block_on(async {
                   let server_a = pmcp::Server::builder()
                       .name("a").version("0")
                       .tool("echo", EchoTool)
                       .build().unwrap();
                   let server_b = pmcp::Server::builder()
                       .name("b").version("0")
                       .tool_arc("echo", Arc::new(EchoTool))
                       .build().unwrap();

                   let args = json!({"v": value.clone()});
                   let result_a = server_a.get_tool("echo").unwrap()
                       .handle(args.clone(), RequestHandlerExtra::default())
                       .await.unwrap();
                   let result_b = server_b.get_tool("echo").unwrap()
                       .handle(args, RequestHandlerExtra::default())
                       .await.unwrap();

                   prop_assert_eq!(result_a, result_b);
                   // Also: both servers must report has_tool("echo") (public sanity check).
                   prop_assert!(server_a.has_tool("echo"));
                   prop_assert!(server_b.has_tool("echo"));
                   Ok(())
               })?;
           }
       }
       ```

       (Adjust `prop_assert_eq!`/`prop_assert!` and runtime-shape to match whatever pattern `tests/property_tests.rs` already uses; mirror the established crate convention.)

    3. **Internal-state invariant scope.** The earlier draft of this test attempted to assert on `server.capabilities` shape via `must_haves`. That field is `private` on `Server` (`src/server/mod.rs:317`) and is not observable from an integration test outside the crate. The capability-shape equivalence between `tool()` and `tool_arc()` registration paths is therefore asserted in **Plan 01 Task 3** as a crate-internal `#[cfg(test)]` unit test (which has access to private fields), not here. This integration test narrows to what IS publicly observable: identical `handle(args, extra)` outputs and identical `has_tool(name)` results.

    4. Implements CONTEXT.md §Claude's Discretion item 3 — the recommendation to add a property test catching future drift between `tool()` and `tool_arc()`.
  </action>
  <verify>
    <automated>cargo test --test in_process_handler_pattern --features full -- tool_and_tool_arc</automated>
  </verify>
  <acceptance_criteria>
    - **Precondition assertion (executed first):** `grep -E '^proptest\s*=' Cargo.toml` returns at least one match (confirms the dev-dep is still in place; would catch a regression if a future `Cargo.toml` cleanup accidentally removed it).
    - Source assertion: `grep -E 'fn tool_and_tool_arc_produce_observable_equivalence' tests/in_process_handler_pattern.rs` returns ≥1 match.
    - Source assertion: the test body invokes BOTH `.tool("echo",` and `.tool_arc("echo",` on parallel builder chains.
    - Source assertion: the test asserts the two `handle(...)` results are equal — `grep -q 'prop_assert_eq!(result_a, result_b)' tests/in_process_handler_pattern.rs` or equivalent `assert_eq!`/`prop_assert_eq!` pattern between the two JSON values.
    - Test command: `cargo test --test in_process_handler_pattern --features full -- tool_and_tool_arc` exits 0.
    - Test command: with `PROPTEST_CASES=128 cargo test --test in_process_handler_pattern --features full -- tool_and_tool_arc` (sanity scale-up) the test still exits 0 in under 30s.
    - **Negative assertion (NEW — replaces the unobservable invariant):** `! grep -q 'server\.capabilities\.\|server_a\.capabilities\|server_b\.capabilities' tests/in_process_handler_pattern.rs`. The integration test must NOT attempt to read the private `capabilities` field; that observation is performed crate-internally by the unit test added in Plan 01 Task 3.
    - **No-fallback assertion:** the file contains no commented-out fallback path or `#[cfg(not(...))]` proptest alternative — the dev-dep is confirmed and the test relies on it directly. `! grep -qE 'fallback|if proptest|hard-coded loop' tests/in_process_handler_pattern.rs`.
    - Quality gate: the new test does not introduce SATD (no `// TODO`, `// FIXME`, `// HACK`, or `// XXX` comments).
  </acceptance_criteria>
  <done>The equivalence test exists, uses `proptest = "1.7"` (the confirmed dev-dep), proves the two registration paths produce identical `handle(...)` outputs over ≥32 random inputs through the PUBLIC API only, does not attempt to read the private `capabilities` field (that invariant lives in Plan 01 Task 3's crate-internal test), and `cargo test --features full` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only file. No new runtime code, no new public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04 | (N/A) | `tests/in_process_handler_pattern.rs` | accept | This is a test file. It links against the same `pmcp` crate as every other integration test, exercises only public API, and runs only under `cargo test`. No new attack surface. STRIDE does not apply. |

**No new attack surface.** Integration test files compile into a separate test binary (per Cargo's `tests/*.rs` convention) and are never shipped to crates.io. The test exercises the same public API that any external consumer can already exercise.
</threat_model>

<verification>
1. `cargo test --test in_process_handler_pattern --features full` exits 0.
2. `cargo build --tests -p pmcp --features full` exits 0 (the new test file compiles).
3. `cargo clippy --tests -p pmcp --features full -- -D warnings` exits 0.
4. `grep -c '#\[tokio::test\]' tests/in_process_handler_pattern.rs` returns ≥3 (the three round-trip tests).
5. `grep -c 'tool_arc\|prompt_arc' tests/in_process_handler_pattern.rs` returns ≥3 (used in all three round-trip tests + the property test).
6. `! rg --quiet '\.handle_request\(|use .*handle_request' tests/in_process_handler_pattern.rs` (no private dispatch usage).
7. `make quality-gate` exits 0.
</verification>

<success_criteria>
- `tests/in_process_handler_pattern.rs` exists, gated `#![cfg(not(target_arch = "wasm32"))]`, and contains ≥3 `#[tokio::test]` plus 1 property/equivalence test.
- All tests pass under `cargo test --features full`.
- BLDR-03 has its regression anchor (the test fails if a future refactor breaks the documented handler-level pattern).
- No private API is imported and `Server::handle_request` is neither called nor imported (verified by the USAGE-narrowed negative grep, not the literal-token grep).
- The property test relies on the confirmed `proptest = "1.7"` dev-dep; no fallback branch.
- The integration test does NOT assert on the private `capabilities` field (that invariant lives in Plan 01 Task 3's crate-internal test).
</success_criteria>

<output>
After completion, create `.planning/phases/82-builder-dx-prerequisites/82-02-SUMMARY.md` summarizing:
  - The four tests added (names, what each exercises).
  - The handler-level pattern shape used (`builder().*_arc(...).build() → server.get_*(name) → handle(args, extra)`).
  - Confirmation that the property test used `proptest = "1.7"` (no fallback was needed).
  - Confirmation that no private API or `Server::handle_request` was touched (USAGE-narrowed grep — not literal-token grep — to allow the explanatory comment).
  - The capability-shape equivalence invariant is covered by Plan 01 Task 3's crate-internal test (since `capabilities` is private), not by this integration test.
  - The requirement IDs this plan contributes to: BLDR-01, BLDR-02, BLDR-03 (regression-anchor portion of D-02 part (b)).
</output>
</content>
</invoke>