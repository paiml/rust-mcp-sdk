---
phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [CONF-04, CONF-05, CONF-06, CONF-07, CONF-08]

must_haves:
  truths:
    - "CONF-04..CONF-08 exist in REQUIREMENTS.md with wire-behaviour text a FIXED/REFUTED/DEFERRED verdict can be scored against"
    - "The requirement coverage table maps CONF-04..CONF-08 to Phase 118.1"
    - "CONF-01..CONF-03 and UNAS-01 are byte-unchanged"
    - "The ROADMAP Phase 118.1 entry carries a real goal, the five requirement IDs, and the 14-plan list"
    - "Phase 118's work is on main and the 118.1 branch is cut from merged main (D-14)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-04..CONF-08 requirement text plus coverage rows"
      contains: "CONF-08"
    - path: ".planning/ROADMAP.md"
      provides: "Filled Phase 118.1 entry with goal, requirements, plan list"
      contains: "CONF-04, CONF-05, CONF-06, CONF-07, CONF-08"
  key_links:
    - from: ".planning/REQUIREMENTS.md section Conformance (CONF)"
      to: ".planning/REQUIREMENTS.md coverage table"
      via: "one row per new ID mapped to Phase 118.1"
      pattern: "CONF-0[45678].*Phase 118.1"
---

<objective>
Mint the five requirement IDs this phase is measured against (D-12), fill the ROADMAP Phase 118.1
entry that currently reads `[Urgent work - to be planned]` / `TBD`, and satisfy D-14's branch
precondition so every subsequent plan lands on a tree whose conformance CI gates are on `main`.

Purpose: every downstream plan declares `requirements: [CONF-0X]` and those IDs do not exist yet.
D-10 scores each gap's disposition against the requirement text written here, so vague text makes
the phase unscoreable.

Output: amended `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`; a merged Phase 118 PR; a
118.1 branch cut from merged `main`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the tree is releasable and open the Phase 118 PR (D-14, first half)</name>
  <files>(no repository files edited — this task produces a PR)</files>
  <read_first>
    - CLAUDE.md — section "Release and Publish Workflow": the `make quality-gate` rule (bare
      `cargo clippy` is weaker than CI) and the `gh pr create --repo paiml/rust-mcp-sdk --head FORK:BRANCH --base main` form
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md — decision D-14
    - .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md — section "The measurement"
  </read_first>
  <action>
    Run `git remote -v` and `git branch --show-current` first and record both verbatim in the
    SUMMARY. The PR head spec depends on whether a fork remote exists.

    Then on the existing `phase-118-conformance` branch run the quality gate through a pipefail
    shell that captures the log and asserts the exit code separately, per D-19 — never `| tail`,
    never a masked pipe. Use `bash -o pipefail -c 'make quality-gate 2>&1 | tee target/118.1-pre-pr-gate.log'`
    and then assert `$?` is 0 in a following statement.

    If the gate exits 0, push the branch and open a PR against `paiml/rust-mcp-sdk` base `main`,
    titled `feat(118): official MCP conformance suite in CI + declared non-conformance`. The PR
    body must quote the measured baseline verbatim from `118-CONFORMANCE-GAPS.md` section "The
    measurement" — v1 51 passed / 15 failed, exit 1, 11 scored scenarios red, 66 checks; v2 124
    passed / 54 failed, exit 1, 7 scored red, 178 checks — and must state explicitly that neither
    leg exits 0 and that the gate is scoped to the MRTR surface plus the era matrix with no
    `--expected-failures` and no allowlist.

    If the gate does not exit 0, STOP, report the failure verbatim, do not open the PR and do not
    proceed to the checkpoint.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'gh pr list --repo paiml/rust-mcp-sdk --head "$(git branch --show-current)" --json number --jq ".[0].number"' | grep -E '^[0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    - `target/118.1-pre-pr-gate.log` exists and the recorded `make quality-gate` exit code is `0`
    - `gh pr list --repo paiml/rust-mcp-sdk --head BRANCH` returns exactly one open PR number
    - The PR body contains the literals `51`, `15`, `124`, `54`, `--expected-failures` and the
      sentence stating neither leg exits 0
    - The SUMMARY records the PR number and the `git remote -v` output verbatim
  </acceptance_criteria>
  <done>An open PR against `paiml/rust-mcp-sdk:main` carrying the Phase 118 conformance work, with a green local quality gate recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-14 branch precondition sign-off</name>
  <read_first>
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md — decision D-14 and its squash-divergence rationale
    - The Task 1 PR body and the CI check list on the PR itself
  </read_first>
  <files>(no repository files edited — this task pauses for human verification)</files>
  <action>PAUSE and hand control to the developer. Present the what-built summary and every
    numbered step in the how-to-verify block below, then WAIT for the resume signal. Do NOT
    proceed to the next task and do NOT self-approve: this gate is blocking, and
    `workflow.auto_advance` does not apply to it. Record the developer's verbatim response in
    the SUMMARY.</action>
  <what-built>
    A PR carrying all of Phase 118 — the conformance drivers, the dual-version target, the era
    matrix, the two blocking CI jobs and `118-CONFORMANCE-GAPS.md` — against
    `paiml/rust-mcp-sdk:main`, with `make quality-gate` green locally.
  </what-built>
  <how-to-verify>
    D-14 requires Phase 118 to be ON `main` before 118.1 fixes are measured, so the re-measurement
    baseline comes from a tree CI actually gates, and requires the 118.1 branch to be cut from
    merged `main` — NOT from `phase-118-conformance`, which is the PR #319 squash-divergence trap.

    1. Open the PR reported in Task 1. Confirm every required check is green, including `gate`,
       `quality-gate`, `Official MCP Conformance Suite` and `Era Matrix`.
    2. Merge it.
    3. Locally run `git checkout main`, `git pull upstream main`, then
       `git checkout -b gsd/phase-118.1-conformance-gaps` FROM that merged `main`.
    4. Confirm `git rev-list --count main..HEAD` prints `0` at cut time and that
       `git log --oneline -1` is the merge commit.

    If the PR was already merged before this plan ran, verify steps 3 and 4 only and say so.
  </how-to-verify>
  <resume-signal>Type "approved" once the PR is merged and the 118.1 branch is cut from merged `main`, or describe what blocked it</resume-signal>
  <verify>
    <human-check>The developer completed the numbered steps above and returned the resume signal.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer returned the resume signal and it is recorded verbatim in the SUMMARY
    - Every numbered step in the how-to-verify block above has a recorded yes/no answer
    - No step was self-approved by the agent; the gate is blocking and `workflow.auto_advance` does not apply
  </acceptance_criteria>
  <done>The developer responded with the resume signal, and that response is recorded verbatim in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Mint CONF-04..CONF-08 in REQUIREMENTS.md (D-12)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — section "Conformance (CONF)" near line 922 (CONF-01..03, all `[x]`);
      section "Unassigned — Awaiting Phase Assignment (UNAS)" near line 934; the coverage table near
      line 1039 that holds the `| CONF-01 | Phase 118 | Complete |` rows and the `**Coverage:**` counts
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md — D-12 and D-13
    - .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md — the G-1..G-9 table
    - schema/vendored/core-2026-07-28/schema.ts — lines 678-696, 1514-1553, 1734-1748, 2644-2663
  </read_first>
  <action>
    Append five unchecked requirements to section "Conformance (CONF)", after CONF-03 and before
    section "Docs in Three Shapes (DOCS …)". Do NOT edit CONF-01, CONF-02 or CONF-03 — D-12 forbids
    reopening them. Each new requirement is stated as WIRE BEHAVIOUR, not as a code change, so a
    FIXED/REFUTED/DEFERRED verdict can be scored against it.

    CONF-04 (G-1, G-2, D-06 annotations): an embedded resource in a tool result or a prompt message
    serializes as the spec `EmbeddedResource` shape — `type: "resource"` with contents nested under
    `resource`, schema.ts:1734-1748 — on BOTH eras; binary content carries `blob` in both the nested
    position and the flat `ReadResourceResult.contents` position, schema.ts:1514-1553; content-level
    `annotations` is carried; and pmcp parses BOTH the nested spec shape and the legacy flat shape
    on input while emitting only the nested one.

    CONF-05 (G-4, G-5): `completion/complete` is served by a registered handler seam on both native
    dispatchers and returns `{completion: {values: [...]}}`, schema.ts:2644-2663; and all five
    methods absent from the 2026-07-28 core schema — `initialize`, `ping`, `logging/setLevel`,
    `resources/subscribe`, `resources/unsubscribe` — answer HTTP 404 with JSON-RPC `-32601` on v2
    EVEN WITH WELL-FORMED PARAMS, while all five still answer normally on v1.

    CONF-06 (G-6, G-7, G-8): a v2 request whose `params._meta` is absent, or which omits
    `io.modelcontextprotocol/protocolVersion` or `io.modelcontextprotocol/clientCapabilities`, is
    rejected with `-32602` and HTTP 400, while a request omitting only
    `io.modelcontextprotocol/clientInfo` is SERVED with HTTP 200; a header/`_meta` protocol-version
    disagreement answers `-32020` and an agreed-but-unsupported version answers `-32022` with
    `data.supported` and `data.requested`; and `server/discover` emits `supportedVersions`,
    schema.ts:678-696, from the same accept list, so every element of `data.supported` appears in it.

    CONF-07 (G-3, D-06 `set_result_meta`, D-07 `PeerHandle::elicit`): the server-to-client
    back-channel works over StreamableHTTP — a tool handler's `peer.sample()`, `peer.list_roots()`
    and `peer.elicit()` complete over v1 stateful HTTP without blocking concurrent requests;
    progress notifications reach the client on both eras (v1 via the session SSE stream, v2 via an
    SSE-framed POST response carrying only notification and result frames, never an independent
    server-to-client request); and `RequestHandlerExtra::set_result_meta` survives the
    `ToolOutput::Result` verbatim path on both dispatchers.

    CONF-08 (G-9): `RequestHandlerExtra::client_capabilities()` returns the capabilities a v1 client
    advertised in its `initialize` handshake at every handler-dispatch construction site, so a
    server-side capability gate reads the same value under v1 as under v2.

    Then add five rows to the coverage table in the existing `| ID | Phase | Status |` format, each
    mapping to `Phase 118.1` with status `Pending`, placed immediately after the `CONF-03` row.
    Update the `**Coverage:**` counts below the table to include the five new IDs.

    Do NOT change UNAS-01 — D-13 defers it to the plan-13 re-pin trigger and that decision is
    recorded there, not here.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'for id in CONF-04 CONF-05 CONF-06 CONF-07 CONF-08; do grep -q "\*\*$id\*\*" .planning/REQUIREMENTS.md || { echo "MISSING $id text"; exit 1; }; grep -qE "\| *$id *\| *Phase 118\.1 *\|" .planning/REQUIREMENTS.md || { echo "MISSING $id coverage row"; exit 1; }; done; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*CONF-0[123]\*\*' .planning/REQUIREMENTS.md` prints `3` — CONF-01..03 still checked
    - Each of CONF-04..CONF-08 appears exactly once as `- [ ] **CONF-0X**:` in section Conformance
    - Each of CONF-04..CONF-08 has exactly one coverage-table row mapping it to `Phase 118.1`
    - CONF-04's text contains the literal `1734-1748`; CONF-05's contains `-32601`; CONF-06's
      contains both `-32020` and `-32022`; CONF-07's contains `set_result_meta`; CONF-08's contains
      `client_capabilities()`
    - `git diff .planning/REQUIREMENTS.md` shows zero changed lines inside the UNAS section
  </acceptance_criteria>
  <done>CONF-04..CONF-08 exist with scoreable wire-behaviour text and coverage rows; CONF-01..03 and UNAS-01 are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Fill the ROADMAP Phase 118.1 entry</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — the Phase 118.1 entry at ~line 2770 with `**Goal:** [Urgent work - to be planned]`
      and `**Requirements**: TBD`; the Phase 119 entry immediately below it, which is the house
      format to copy (Goal / Depends on / Requirements / Success Criteria / Plans); and the v2.5
      milestone progress table at ~line 2800
    - .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md — section "The measurement"
  </read_first>
  <action>
    Replace `**Goal:** [Urgent work - to be planned]` with: both official-suite legs measure
    strictly better than the Phase-118 baseline (v1 51 passed / 15 failed, exit 1, 11 scored
    scenarios red, 66 checks; v2 124 passed / 54 failed, exit 1, 7 scored red, 178 checks), each of
    G-1..G-9 carries an explicit FIXED / REFUTED / DEFERRED disposition backed by a named RED-to-GREEN
    artifact, and the blocking CI gate is widened to exactly the surfaces that then pass — with no
    `--expected-failures`, no allowlist and no known-failure baseline.

    Replace `**Requirements**: TBD` with `**Requirements**: CONF-04, CONF-05, CONF-06, CONF-07, CONF-08`.
    Keep `**Depends on:** Phase 118`. Replace `**Plans:** 0 plans` with `**Plans:** 14 plans`.

    Add a `**Success Criteria** (what must be TRUE):` block in the Phase 119 entry's exact numbered
    format, one criterion per requirement, each ending with its requirement ID in parentheses.

    Replace the `- [ ] TBD (run /gsd:plan-phase 118.1 to break down)` line with the 14-item plan
    list grouped by wave, each item shaped `- [ ] 118.1-NN-PLAN.md — Wave N. OBJECTIVE (REQ IDs)`:
    01 bookkeeping plus the D-14 branch precondition (CONF-04..08); 02 CONF-04 RED fences (spec-derived
    goldens plus the tolerant-reader fuzz target); 03 CONF-04 fix (nested emitter, tolerant reader,
    blob, annotations, `#[non_exhaustive]` plus constructors, CHANGELOG); 04 CONF-05 G-4 completion
    seam on both dispatchers; 05 CONF-05 G-5 method-string retirement at the v2 ingress; 06 CONF-06
    G-6/G-8 three-way `_meta` classifier and check ordering; 07 CONF-06 G-7 `supportedVersions`;
    08 CONF-08 G-9 v1 capability plumbing; 09 CONF-07 `PeerHandle::elicit` plus the `set_result_meta`
    verbatim drain; 10 CONF-07 G-3 v1 channel plus inbound routing before the server mutex; 11 CONF-07 G-3 v1
    session-bound peer injection, progress, and the era-matrix tripwire flips; 12 CONF-07 G-3 v2 multi-frame
    SSE progress (D-16); 13 re-measurement number one at the held pin plus the D-10 dispositions;
    14 gate widening plus the D-08 re-pin and re-measurement number two.

    Finally add a `| 118.1 Close the Nine Conformance Gaps | 0/14 | Not started | - |` row to the
    v2.5 milestone progress table immediately after the `118. Conformance Against the Official
    Suite` row.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'grep -q "Urgent work - to be planned" .planning/ROADMAP.md && { echo "placeholder still present"; exit 1; }; grep -q "CONF-04, CONF-05, CONF-06, CONF-07, CONF-08" .planning/ROADMAP.md || exit 1; n=$(grep -c "118.1-[0-9][0-9]-PLAN.md" .planning/ROADMAP.md); test "$n" -eq 14 || { echo "expected 14 plan lines, got $n"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Urgent work - to be planned" .planning/ROADMAP.md` prints `0`
    - The Phase 118.1 entry contains `**Requirements**: CONF-04, CONF-05, CONF-06, CONF-07, CONF-08`
      and `**Plans:** 14 plans`
    - `grep -c "118.1-[0-9][0-9]-PLAN.md" .planning/ROADMAP.md` prints `14`
    - The Success Criteria block has 5 numbered items, each ending in a `(CONF-0X)` reference
    - The milestone progress table has a `118.1` row with `0/14`
  </acceptance_criteria>
  <done>The Phase 118.1 roadmap entry is fully specified: real goal, five requirement IDs, 14 plans listed by wave, progress row present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo to GitHub (`gh` push / PR) | Task 1 publishes the tree; nothing untrusted flows inward |
| planning docs to downstream executors | The requirement text written here becomes the contract every later plan is scored against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.1-01-01 | Repudiation | Requirement text so vague that a gap can be silently declared FIXED | mitigate | Each requirement states a wire behaviour with a literal code or schema line, and the acceptance criteria grep for those literals |
| T-118.1-01-02 | Tampering | Editing CONF-01..03 or UNAS-01 would misattribute the Phase-118 defect | mitigate | Acceptance criterion asserts CONF-01..03 remain `[x]` and `git diff` shows zero UNAS changes |
| T-118.1-01-03 | Denial of Service | Merging Phase 118 with a red gate would put an unenforceable claim on `main` | mitigate | Task 1 blocks on `make quality-gate` exit 0; the blocking checkpoint requires all required PR checks green before merge |
| T-118.1-SC | Tampering | npm/pip/cargo installs | accept | This plan runs no package-manager install. The single install point in the phase is the D-08 re-pin in plan 13, which carries its own blocking legitimacy checkpoint |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` both parse as before (no broken tables)
- The 118.1 branch's merge-base with `main` is `main` itself
- No source file was modified by this plan: `git diff --name-only` lists only the two `.planning/` files
</verification>

<success_criteria>
- CONF-04..CONF-08 exist, are scoreable, and are mapped to Phase 118.1 in the coverage table
- The ROADMAP Phase 118.1 entry has no `TBD` or `[Urgent work` placeholder left
- Phase 118 is merged to `main` and the 118.1 branch is cut from it
</success_criteria>

<output>
Create `.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-01-SUMMARY.md` when done
</output>
