---
phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
verified: 2026-08-12T02:33:34Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "gap_attributable_failures == 0 on BOTH legs (118.1-13 / 118.1-14 must_haves)"
    reason: "Measured value is 1, not 0 (tools-call-with-logging: no handler-facing notifications/message emitter exists). Escalated to the developer at plan 118.1-13's Task 4 blocking gate (D-10) and ruled on the merits, verbatim, 2026-08-11: 'CONF-07 — BOOK IT. The logging scenario exercises a capability no requirement in this phase ever asked for, so it does not block CONF-07.' Not reclassified or hidden — stays booked as gap-attributable, independently re-confirmed by this verifier via a live re-run of the official suite (tools-call-with-logging: 1 passed, 1 failed), and owned by name in ROADMAP.md Phase 118.2 with matching success criteria."
    accepted_by: "project developer (D-10 sign-off, recorded verbatim in 118.1-13-SUMMARY.md)"
    accepted_at: "2026-08-11"
gaps: []
deferred:
  - truth: "G-3 sub-item (c): pmcp's own StreamableHttpTransport CLIENT can hold a live GET SSE stream and consume server-initiated messages over v1 HTTP"
    addressed_in: "Phase 118.2"
    evidence: "ROADMAP.md Phase 118.2 Success Criterion 1: 'pmcp's own StreamableHttpTransport client can hold a live GET SSE stream and consume server-initiated messages over v1 HTTP.' Measured root cause matches exactly: collect_body_within_cap whole-body read in src/shared/streamable_http.rs."
  - truth: "G-3 sub-item (d): a tool handler can emit MCP notifications/message log records and they reach the client on both eras"
    addressed_in: "Phase 118.2"
    evidence: "ROADMAP.md Phase 118.2 Success Criterion 2: 'A tool handler can emit MCP notifications/message log records during a call, and they reach the client on both eras.' Matches the one remaining GAP_ATTRIBUTABLE_FAILURES=1 finding (tools-call-with-logging) exactly."
---

# Phase 118.1: Close the nine conformance gaps G-1..G-9 found by the official suite Verification Report

**Phase Goal:** Close the nine conformance gaps (G-1..G-9) found by the official MCP conformance suite.
**Verified:** 2026-08-12T02:33:34Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

This phase's ROADMAP entry states its own falsifiable goal (not a generic phase-goal restatement):
"Both official-suite legs measure strictly better than the Phase-118 baseline... each of G-1..G-9
carries an explicit FIXED / REFUTED / DEFERRED disposition backed by a named RED-to-GREEN
artifact, and the blocking CI gate is widened to exactly the surfaces that then pass." That is
what was verified below, independently, not by trusting SUMMARY.md.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Both official-suite legs measure strictly better than the Phase-118 baseline | VERIFIED | **Independently re-ran `./scripts/run-conformance-suite.sh` live against the merged tree** (installed GNU `coreutils` for `timeout(1)` and used Node 22.22.2, both absent by default in this environment). Observed output matches the claimed numbers exactly: v1 `2025-11-25` **72 passed, 2 failed, 74 checks, exit 1** (baseline 51/15/66/1); v2 `2026-07-28` **142 passed, 36 failed, 178 checks, exit 0** (baseline 124/54/178/1). Script's own gate section printed `CONF-01 gates PASSED.` |
| 2 | G-1 (flat EmbeddedResource) is FIXED | VERIFIED | `src/types/content.rs` implements a hand-written `Serialize`/tolerant `Deserialize` for `Content::Resource` emitting `{"type":"resource","resource":{...}}`. `cargo nextest run --features full -E 'binary(embedded_resource_golden)'` → **10 tests run, 10 passed** (independently executed, not quoted). Live suite corroborates: `tools-call-embedded-resource` and `prompts-get-embedded-resource` scenarios pass. |
| 3 | G-2 (no blob-bearing resource-contents variant) is FIXED | VERIFIED | `Content::resource_with_blob` constructor exists in `src/types/content.rs`; same 10/10 golden run covers `resources-read-binary` (flat `{uri, mimeType, blob}`). Live suite: `resources-read-binary` in the re-run's not-scored-vs-scored breakdown is green. |
| 4 | G-4 (`completion/complete` catch-all) is FIXED | VERIFIED | `src/server/core.rs::complete_completion` + registration slots in `src/server/builder.rs` and `src/server/mod.rs`. `cargo nextest run --features full -E 'binary(completion_complete)'` → 6/6 passed (run as part of a combined 60-test batch below). |
| 5 | G-5 (v2 method retirement narrower than claimed) is FIXED | VERIFIED | `cargo nextest run --features full -E 'binary(v2_retired_methods)'` → 4/4 passed. Live suite `server-stateless` scenario (which hosts these checks) is part of the 37/37 fully-green v2 scored set. |
| 6 | G-6/G-8 (`_meta` classifier + `-32020`/`-32022` ordering) FIXED | VERIFIED | `cargo nextest run --features full -E 'binary(v2_meta_validation_codes)'` → 8/8 passed, including the `referee_matrix` proptest-style six-row rule. |
| 7 | G-7 (`server/discover` missing `supportedVersions`) FIXED | VERIFIED | `cargo nextest run --features full -E 'binary(v2_discover_supported_versions)'` → 3/3 passed. `src/types/protocol/mod.rs` carries `supported_versions` on `ServerDiscoverResult`, `#[non_exhaustive]`. |
| 8 | G-9 (v1 handshake capabilities never reach handlers) FIXED | VERIFIED | `cargo nextest run --features full -E 'binary(v1_handler_client_capabilities)'` → 5/5 passed. `src/server/core.rs` folds `with_client_capabilities` at every `ProtocolContext` construction site. |
| 9 | G-3 (server-to-client back-channel over StreamableHTTP) — server half + v2 SSE FIXED | VERIFIED | `binary(http_peer_roundtrip)` 12/12, `binary(in_tool_peer_roundtrip)` 7/7, `binary(v2_sse_progress)` 10/10 — all independently executed, all passed. `src/server/streamable_http_server/peer_channel.rs` implements ownership-keyed correlation (`owner_of`) and session-scoped routing; `src/shared/peer.rs` carries `async fn elicit` as a defaulted trait method. Live suite corroborates: `tools-call-sampling`, `tools-call-elicitation`, `tools-call-with-progress` (both eras) all green in the re-run. |
| 10 | G-3 — two named sub-items (pmcp's own v1 client SSE transport; a handler-facing log-notification emitter) remain OPEN | DEFERRED, not a gap | See Deferred Items below — explicitly owned by Phase 118.2, entered in ROADMAP.md with matching success criteria, root-caused by file/line in both the phase's own docs and independently confirmed by this verifier (`grep -rn LogMessage src/` shows construction only inside `#[cfg(test)]` modules; no production emission path). |
| 11 | Every one of G-1..G-9 ends in an explicit, non-silent disposition | VERIFIED (with a documented interpretive nuance) | `118-CONFORMANCE-GAPS.md` "Dispositions — Phase 118.1" table gives all nine gaps + 2 adjacent items (D-06, D-07) a named verdict with an executed test count. G-3 does not reduce to one of the roadmap goal's literal three words (FIXED/REFUTED/DEFERRED) — it is recorded as a developer-approved four-part SPLIT (2 FIXED, 2 OPEN owned by name). This is a conscious, escalated, dated developer ruling (D-10, 2026-08-11, quoted verbatim in `118.1-13-SUMMARY.md`), not an executor self-grade, and every sub-item individually resolves unambiguously. Treated as satisfying the goal's intent rather than its literal wording; flagged here for visibility rather than silently accepted. |
| 12 | Blocking CI gate widened to exactly what passes, no allowlist/known-failure baseline | VERIFIED | `scripts/run-conformance-suite.sh` structurally forbids `--expected-failures`; `tests/ci_conformance_gate_wiring.rs` runs **18/18 tests passed** (independently executed), including `no_known_fail_allowlist_reaches_a_conformance_command` and `the_era_script_enforces_a_nonzero_test_count`. `.github/workflows/ci.yml` `gate` job's `needs:` array includes both `conformance-suite` and `era-matrix`. |

**Score:** 12/12 truths verified (1 via a documented, developer-approved override — see frontmatter).

### Deferred Items

Both items below are the OPEN halves of G-3, explicitly not closed by this phase and explicitly
carried forward by name, not silently dropped.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | pmcp's own `StreamableHttpTransport` client cannot hold a live GET SSE stream over v1 | Phase 118.2 | ROADMAP.md Phase 118.2 Success Criterion 1, root-caused to `src/shared/streamable_http.rs`'s `collect_body_within_cap` whole-body read, verified present in `deferred-items.md` (118.1-11 entry) and cross-checked against the live suite re-run (no scenario regression observed on the server-proven half). |
| 2 | No handler-facing emitter for `notifications/message` (log records) | Phase 118.2 | ROADMAP.md Phase 118.2 Success Criterion 2. Independently confirmed: `grep -rn LogMessage src/` shows `ServerNotification::LogMessage` constructed only inside `#[cfg(test)]` modules (`src/types/notifications.rs:278`, `src/types/subscriptions.rs:920`, `src/server/subscriptions.rs:1459` — the last verified in this session to sit inside `mod listen_registry` under `#[cfg(test)]`). Live suite re-run confirms `tools-call-with-logging: 1 passed, 1 failed` is the SOLE scored failure on either leg. |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `.planning/REQUIREMENTS.md` | CONF-04..CONF-08 text + coverage rows mapped to Phase 118.1 | VERIFIED | Lines 927-931 (requirement text), 1100-1104 (coverage table, all "Complete"). CONF-01..03 evidence AMENDED not overwritten (CONF-03 amendment dated 2026-08-11, additive). UNAS-01 present, carried to v2.6, not silently dropped. |
| `.planning/ROADMAP.md` | Filled Phase 118.1 entry + Phase 118.2 follow-up entry | VERIFIED | Phase 118.1 entry (lines 2771-2836) carries goal, 5 requirement IDs, 14/14 plan checklist, 5 success criteria. Phase 118.2 entry (lines 2838-2860) exists with 4 success criteria matching the two OPEN G-3 sub-items exactly. |
| `src/types/content.rs` | Nested `EmbeddedResource` emitter, tolerant reader, `blob`, `annotations`, `#[non_exhaustive]` | VERIFIED | 944 lines added on this branch; `resource_with_blob`/`resource_with_text` constructors present, `#[non_exhaustive]` on `Content::Resource`, hand-written wire-format-aware `Serialize`. |
| `CHANGELOG.md` | Prominent wire-change callout | VERIFIED | `### ⚠ WIRE CHANGE — embedded resources now serialize as the spec's EmbeddedResource` at line 10, with a "why there is no opt-out" section and migration guidance. |
| `tests/embedded_resource_golden.rs` | Byte-literal spec-derived goldens | VERIFIED | 1059 lines; gated `#![cfg(all(feature="streamable-http", feature="http-client", not(wasm32)))]`; 10/10 passing under `--features full`. |
| `src/server/builder.rs`, `src/server/mod.rs`, `src/server/core.rs` | `completion/complete` handler seam on both dispatchers | VERIFIED | `complete_completion` in `core.rs`; registration slots + dispatch arms in both builder families; `completion_complete` binary 6/6. |
| `src/server/streamable_http_server.rs`, `.../peer_channel.rs`, `src/server/server_request_dispatcher.rs` | v1 server-to-client channel, ownership-keyed correlation, v2 multi-frame SSE | VERIFIED | `owner_of`/`dispatch_owned` present; `http_peer_roundtrip` 12/12 (including session-isolation and deadlock-guard tests); `v2_sse_progress` 10/10. |
| `src/shared/peer.rs` | `PeerHandle::elicit` default-bodied trait method | VERIFIED | `async fn elicit(&self, params: ElicitRequestParams) -> Result<ElicitResult>` at line 172; 186 net lines added; `in_tool_peer_roundtrip` 7/7. |
| `src/server/core.rs` (v1 capability fold) | `with_client_capabilities` at every construction site | VERIFIED | `v1_handler_client_capabilities` binary 5/5. |
| `scripts/run-conformance-suite.sh` | Widened blocking surface, raised floors, no allowlist | VERIFIED | `MIN_CHECKS_V1=74`, `MIN_CHECKS_V2=178`; structurally forbids `--expected-failures`; independently RAN and passed live in this session. |
| `tests/ci_conformance_gate_wiring.rs` | Structural pins in lockstep with the script | VERIFIED | `ZERO_CHECK_DECLARATIONS` const present; 18/18 tests passed. |
| `conformance/package.json` / `package-lock.json` | Suite version pin | VERIFIED | Pinned to `0.2.0-alpha.11`, matches the held pin the whole phase measured against; confirmed newest published (per orchestrator's independent npm registry check). |
| `.planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md` | Appended disposition section, all 9 gaps + 2 adjacent items | VERIFIED | "Dispositions — Phase 118.1 (amendment)" section present, amended not rewritten (explicit note that nothing above the amendment line changed), every gap has a named artifact + executed count. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `src/types/content.rs Content::Resource Serialize` | `tests/embedded_resource_golden.rs` | nested `EmbeddedResource` byte literal | WIRED | Golden test asserts `"type":"resource","resource":{` substring; 10/10 pass. |
| `Server::supported_protocol_versions()` | `ServerDiscoverResult::supported_versions` | threaded through `build_discover_response` | WIRED | `v2_discover_supported_versions` 3/3, including the subset-correlation test against `data.supported`. |
| `PeerHandle::elicit` | `ServerRequestDispatcher::dispatch` | `ServerRequest::ElicitationCreate`, single correlation authority | WIRED | `in_tool_peer_roundtrip` and `http_peer_roundtrip` both exercise elicit end-to-end and pass. |
| `handle_post_request` entry | `ServerRequestDispatcher::handle_response` | response-envelope classification before `state.server.lock()` | WIRED | `http_peer_roundtrip`'s deadlock-guard test (`a_parked_handler_does_not_block_a_concurrent_post`-class assertion) passes — this is the load-bearing regression test for that ordering. |
| `scripts/run-conformance-suite.sh` constants | `tests/ci_conformance_gate_wiring.rs ZERO_CHECK_DECLARATIONS` | bijective name mapping | WIRED | `the_zero_check_gate_and_the_check_floors_are_declared` test passes. |
| `.github/workflows/ci.yml gate.needs` | `conformance-suite`, `era-matrix` jobs | required-check membership | WIRED | Both present verbatim in the `needs:` array feeding the org-required `gate` status check. |

### Data-Flow Trace (Level 4)

Not applicable in the UI-rendering sense — this phase is protocol/wire-format and CI-gate work,
not a data-rendering surface. The equivalent trace performed here is "does the measured suite
number actually come from a live run, not a cached/static claim" — answered by independently
re-executing `scripts/run-conformance-suite.sh` end-to-end against a freshly spawned server
process rather than reading `target/conformance-results/` artifacts left by a prior run.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| G-1/G-2 fix holds | `cargo nextest run --features full -E 'binary(embedded_resource_golden)'` | 10 tests run, 10 passed | PASS |
| G-4/G-5/G-6/G-7/G-8/G-3(partial)/G-9/D-06/D-07 fixes hold, combined | `cargo nextest run --features full -E "binary(completion_complete) or binary(v2_retired_methods) or binary(v2_meta_validation_codes) or binary(v2_discover_supported_versions) or binary(v1_handler_client_capabilities) or binary(tool_output_result_http) or binary(in_tool_peer_roundtrip) or binary(http_peer_roundtrip) or binary(v2_sse_progress) or binary(embedded_resource_example_run)"` | 60 tests run, 60 passed (sum of the ten binaries' documented counts: 6+4+8+3+5+4+7+12+10+1=60) | PASS |
| era-matrix G-3 tripwires flipped | `cd crates/pmcp-team-servers && cargo nextest run --features http -E 'binary(era_matrix)'` | 4 tests run, 4 passed (1 leaky, non-fatal) | PASS |
| CI gate wiring structurally sound | `cargo nextest run --features full -E 'binary(ci_conformance_gate_wiring)'` | 18 tests run, 18 passed | PASS |
| The gate script itself runs and gates correctly | `./scripts/run-conformance-suite.sh` (live, full end-to-end, both legs) | v1 72/2/74/exit1, v2 142/36/178/exit0, `CONF-01 gates PASSED.` | PASS |

### Probe Execution

`scripts/run-conformance-suite.sh` was treated as this phase's probe (the phase's own README calls
it the gate driver) and was run directly per Step 7c's contract, not narrated from SUMMARY.md.

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| `scripts/run-conformance-suite.sh` | `bash scripts/run-conformance-suite.sh` (Node 22.22.2, GNU coreutils `timeout` installed for this session — both absent from the default shell PATH and required by the script's own preflight checks) | exit 0, `CONF-01 gates PASSED.`, v1 72/2 exit 1 (expected — one gap-attributable failure remains and is reported, not masked), v2 142/36 exit 0 | PASS |

Note on environment: this sandbox's default `PATH` lacks GNU `timeout(1)` and ships Node 20.8.1
(the suite requires Node >= 22 because it imports `globSync` from `node:fs` at module scope). Both
were preflight-checked and refused to run by the script itself with clear, actionable error
messages rather than a confusing failure deep in the run — installing `coreutils` via Homebrew and
switching to the pre-existing `nvm` Node 22.22.2 install resolved both without modifying the repo.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| CONF-04 | 118.1-01, 02, 03, 13, 14 | Embedded resource nested shape, blob, annotations, tolerant reader | SATISFIED | `embedded_resource_golden` 10/10, `embedded_resource_example_run` 1/1, live suite corroboration (see truths 2-3 above) |
| CONF-05 | 118.1-01, 04, 05, 13, 14 | `completion/complete` handler seam + v2 method retirement | SATISFIED | `completion_complete` 6/6, `v2_retired_methods` 4/4 |
| CONF-06 | 118.1-01, 06, 07, 13, 14 | `_meta` classifier, `-32020`/`-32022` ordering, `supportedVersions` | SATISFIED | `v2_meta_validation_codes` 8/8, `v2_discover_supported_versions` 3/3 |
| CONF-07 | 118.1-01, 09, 10, 11, 12, 13, 14 | Server-to-client back-channel over StreamableHTTP | SATISFIED (with a stated, developer-approved carve-out — see override) | `http_peer_roundtrip` 12/12, `in_tool_peer_roundtrip` 7/7, `v2_sse_progress` 10/10, `tool_output_result_http` 4/4; one gap-attributable suite failure (`tools-call-with-logging`) explicitly ruled out-of-scope for CONF-07's own text by the developer at the D-10 gate, and owned by name in Phase 118.2 |
| CONF-08 | 118.1-01, 08, 13, 14 | v1 handshake capabilities reach handler-dispatch sites | SATISFIED | `v1_handler_client_capabilities` 5/5 |

No orphaned requirements: `.planning/REQUIREMENTS.md`'s coverage table maps all five IDs to
Phase 118.1, and every plan's `requirements:` frontmatter field is a subset of {CONF-04..CONF-08}.
UNAS-01 is explicitly out of scope for this phase (carried to v2.6, not folded in) and is not
listed against Phase 118.1 anywhere in REQUIREMENTS.md.

### Anti-Patterns Found

None. Scanned every file this phase's plans declared as `files_modified` under `src/` and
`scripts/`/`tests/` (content.rs, mcp_client.rs, simple_resources.rs, conversion.rs, sampling.rs,
builder.rs, mod.rs, core.rs, streamable_http_server.rs + peer_channel.rs,
server_request_dispatcher.rs, protocol/context.rs, protocol/mod.rs, cancellation.rs, peer.rs,
peer_impl.rs, run-conformance-suite.sh, ci_conformance_gate_wiring.rs) for `TBD|FIXME|XXX`,
`TODO|HACK|PLACEHOLDER`, and "not yet implemented"/"coming soon" phrasing. Zero hits.

### Human Verification Required

None. Every truth resolved programmatically: this phase is protocol wire-format, dispatch, and CI
gate work with no user-facing UI, no visual rendering, and no external-service dependency beyond
the already-pinned official conformance suite, which was run live in this session rather than
deferred to a human.

### Gaps Summary

No unresolved gaps. Eight of nine numbered gaps (G-1, G-2, G-4 through G-9) are FIXED with
independently re-executed, non-zero, passing test counts, cross-corroborated by a live re-run of
the official MCP conformance suite that reproduced the phase's claimed numbers byte-for-byte
(v1 72/2/74/exit1, v2 142/36/178/exit0). G-3 does not reduce to a single verdict word and is
recorded, by an explicit and dated developer ruling, as a four-part split: two halves FIXED
(v1 server seam, v2 SSE) and two sub-items OPEN, both of which are named, root-caused, and entered
as Phase 118.2 in ROADMAP.md with success criteria that match the open sub-items exactly. The one
plan-level must-have that literally FAILED as worded — `GAP_ATTRIBUTABLE_FAILURES == 0` — was
escalated to the developer at a blocking gate rather than silently softened or reclassified, ruled
on the merits (the one remaining failure exercises a capability CONF-07's own requirement text
never asked for), and is recorded here as an accepted, evidenced override rather than a hidden
gap. The phase's actual ROADMAP-level goal — both legs measure strictly better, every gap gets a
named disposition, the CI gate widens to exactly what passes with no allowlist — is verified true
in the codebase, not merely claimed in SUMMARY.md.

---

_Verified: 2026-08-12T02:33:34Z_
_Verifier: Claude (gsd-verifier)_
