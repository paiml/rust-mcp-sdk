---
phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
plan: 03
type: execute
wave: 3
depends_on: [118.1-02]
files_modified:
  - src/types/content.rs
  - src/composition/mcp_client.rs
  - src/server/simple_resources.rs
  - src/server/workflow/conversion.rs
  - src/client/host/sampling.rs
  - benches/transport_performance.rs
  - cargo-pmcp/src/templates/mcp_app.rs
  - crates/mcp-tester/src/tester.rs
  - crates/pmcp-server/src/resources/docs.rs
  - crates/pmcp-server-toolkit/src/resources.rs
  - examples/s54_v2_dual_conformance.rs
  - examples/c03_client_resources.rs
  - examples/c10_client_skills.rs
  - examples/26-server-tester/src/tester.rs
  - examples/mcp-apps-chess/src/main.rs
  - examples/mcp-apps-dataviz/src/main.rs
  - examples/mcp-apps-map/src/main.rs
  - tests/protocol_invariants.rs
  - tests/toolresult_property_tests.rs
  - tests/embedded_resource_example_run.rs
  - CHANGELOG.md
autonomous: true
requirements: [CONF-04]

must_haves:
  truths:
    - "A tool result carrying an embedded resource serializes as {\"type\":\"resource\",\"resource\":{...}} on both eras"
    - "A prompt message carrying an embedded resource serializes the same way"
    - "resources/read on a binary resource emits flat {uri, mimeType, blob}"
    - "The dual-conformance example is RUN, not merely built: a live resources/read on test://static-binary answers with a blob on both eras"
    - "pmcp parses BOTH the nested spec shape and the legacy flat shape on input, and emits only nested"
    - "Content::Resource carries blob and annotations and is #[non_exhaustive] with public constructors"
    - "tests/v1_lists_golden.rs RESOURCES_READ is still green — the flat ReadResourceResult projection is unchanged"
    - "The cargo semver-checks major-lint delta is recorded explicitly as a one-time documented conformance exception"
    - "A Content::Resource carrying NEITHER text NOR blob still emits a spec-valid EmbeddedResource — never an object with neither key"
    - "Content::resource(uri) is deprecated and every in-crate caller is migrated, so -D warnings stays green"
  artifacts:
    - path: "src/types/content.rs"
      provides: "strict nested emitter, tolerant reader, blob, annotations, non_exhaustive, constructors"
      contains: "resource_with_blob"
    - path: "CHANGELOG.md"
      provides: "D-02 prominent wire-change callout plus migration note"
      contains: "EmbeddedResource"
    - path: "examples/s54_v2_dual_conformance.rs"
      provides: "test://static-binary served as a blob resource so resources-read-binary can pass"
      contains: "resource_with_blob"
    - path: "tests/embedded_resource_example_run.rs"
      provides: "live-run proof that the built example answers resources/read on test://static-binary with a blob, on both eras"
      min_lines: 60
      contains: "test://static-binary"
  key_links:
    - from: "src/types/content.rs Content::Resource Serialize"
      to: "tests/embedded_resource_golden.rs"
      via: "nested EmbeddedResource byte literal"
      pattern: "\"type\":\"resource\",\"resource\":"
    - from: "src/types/content.rs resource_contents_serde"
      to: "tests/v1_lists_golden.rs RESOURCES_READ"
      via: "unchanged flat projection"
      pattern: "\\{\"uri\":.*\"mimeType\":.*\"text\":"
    - from: "tests/embedded_resource_example_run.rs"
      to: "target/debug/examples/s54_v2_dual_conformance"
      via: "spawned child process answering a live resources/read, recorded at target/118.1-03-blob-response.json"
      pattern: "target/debug/examples/s54_v2_dual_conformance"
---

<objective>
Close G-1 and G-2 and land D-06's `annotations`: make `Content::Resource` emit the spec
`EmbeddedResource` shape in the tool-result and prompt-message positions, gain a `blob` carrier in
BOTH emitters, gain content-level `annotations`, become `#[non_exhaustive]` with mandatory
constructors, and accept both the nested and the legacy flat shape on input.

Purpose: G-1 is the largest single source of red scenarios — 4 of 11 v1 scored failures and 4 of 7
v2. It is also the one change the automated semver guards are structurally blind to, which is why
plan 02's fences exist and why this plan's semver delta is recorded, not silently accepted.

Output: a spec-correct emitter, a tolerant reader, a green `tests/embedded_resource_golden.rs`, a
still-green `tests/v1_lists_golden.rs`, a compiling workspace, and a CHANGELOG wire callout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-PATTERNS.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-02-SUMMARY.md
@src/types/content.rs
@schema/vendored/core-2026-07-28/schema.ts
</context>

<interfaces>
<!-- Contracts the executor needs. No exploration required. -->

Today's public shape — `src/types/content.rs:78-91`, variant `Content::Resource` with fields
`uri: String`, `text: Option<String>`, `mime_type: Option<String>`,
`meta: Option<serde_json::Map<String, Value>>`, all four under `#[serde(rename_all = "camelCase")]`
inside `#[serde(tag = "type", rename_all = "camelCase")] pub enum Content`. NO `#[non_exhaustive]`.

Target spec shape — `schema/vendored/core-2026-07-28/schema.ts:1734-1748`:
`EmbeddedResource { type: "resource"; resource: TextResourceContents | BlobResourceContents; annotations?: Annotations; _meta?: MetaObject }`
where `ResourceContents` (`:1514`) is `{ uri, mimeType?, _meta? }`, `TextResourceContents` (`:1535`)
adds required `text`, `BlobResourceContents` (`:1548`) adds required `blob`.

Existing precedents to copy, all in `src/types/content.rs`:
- `resource_contents_serde` at `:325` — the borrowed shadow struct `Rc<'a>` with `&'a str` /
  `&'a Option<String>` reference fields (allocation-free), plus its tolerant `Vec<Value>` pre-pass
  deserializer at `:378-412`
- `ResourceLinkContent` at `:213-293` — the file's own `#[non_exhaustive]` type with `new()` plus a
  `with_*` builder chain, and `Annotations` at `:21` which is already `#[non_exhaustive]`
- `Content::resource_with_text` at `:159` — the constructor shape `resource_with_blob` mirrors
- The container-attribute reader precedent: `crates/pmcp-package/src/digest/canonical.rs:46`
  `#[serde(try_from = "String", into = "String")]`

`serde_json` is built with `preserve_order` (`Cargo.toml:55`), so shadow-struct field declaration
order IS wire key order and is pinned by plan 02's literals.

Sites the compiler will name (from plan 02 Task 1's inventory), pre-measured: 18 field-bearing
`Content::Resource { … }` sites, 6 in `src/` (`composition/mcp_client.rs:275,362`,
`server/simple_resources.rs:38,58,350`, `types/content.rs:398,435`), 12 outside; 46 further sites
already use a `..` rest pattern and are unaffected.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict nested emitter, tolerant reader, blob, annotations, and the neither-branch rule</name>
  <files>src/types/content.rs, src/server/workflow/conversion.rs, src/client/host/sampling.rs</files>
  <behavior>
    - `serde_json::to_string(&Content::resource_with_text("u", "t", Some("text/plain")))` produces
      `{"type":"resource","resource":{"uri":"u","mimeType":"text/plain","text":"t"}}` — key order
      exactly as declared, `resource` nested, no top-level `uri`
    - `serde_json::to_string(&Content::resource_with_blob("u", "AAA=", "image/png"))` produces the
      same envelope with `"blob":"AAA="` in place of `text`
    - `.with_annotations(...)` places `annotations` at content level, a sibling of `resource`, never inside it
    - `serde_json::from_str::<Content>` accepts the nested spec shape AND the legacy flat shape
      `{"type":"resource","uri":...,"text":...}`, and re-serializing either yields the nested shape
    - An object carrying BOTH `text` and `blob` inside `resource` is rejected or resolved by one
      documented deterministic rule — the spec type is an XOR
    - `serde_json::to_string(&Content::Resource { uri: "u".into(), text: None, blob: None, .. })`
      produces `{"type":"resource","resource":{"uri":"u","text":""}}` — a valid
      `TextResourceContents`, NEVER `{"type":"resource","resource":{"uri":"u"}}`
    - `Content::resource("u")` still returns `Content::Resource` and still compiles, but emits a
      `#[deprecated]` warning at every call site outside this crate
    - `ReadResourceResult.contents` still serializes FLAT and now carries `blob`:
      `{"uri":...,"mimeType":...,"blob":...}` with no `type` tag
    - `Content::Text`, `Image`, `Audio` and `ResourceLink` emit byte-identical output to today
  </behavior>
  <read_first>
    - src/types/content.rs — READ IN FULL (649 lines): the `Content` enum at 60-113, the `impl Content`
      constructors at 114-212, `ResourceLinkContent` at 213-293, `resource_contents_serde` at 318-413,
      and the unit tests at 420-500 including `test_content_resource_meta_serialization` at 429
    - schema/vendored/core-2026-07-28/schema.ts — lines 1514-1553 and 1734-1748
    - crates/pmcp-package/src/digest/canonical.rs — lines 40-60 (the `try_from` container-attribute precedent)
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md — D-01, D-03, D-15
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-RESEARCH.md — Pattern 1, Pitfall 2, Pitfall 6
  </read_first>
  <action>
    Batch every breaking change into ONE edit, per D-15. Do not split it across commits — the point
    of batching is that `#[non_exhaustive]` lands in the same commit as the fields it protects, so
    every FUTURE spec field on this variant is minor.

    1. Add two fields to `Content::Resource`: `blob: Option<String>` and
       `annotations: Option<Annotations>`, both `#[serde(skip_serializing_if = "Option::is_none")]`.
       Mark the variant `#[non_exhaustive]`.

    2. Replace `Content`'s derived `Serialize` with a hand-written `impl Serialize for Content` that
       serializes a BORROWED shadow enum. Only the `Resource` arm is hand-shaped; the other four
       arms delegate to a `#[derive(Serialize)]` shadow so their bytes cannot drift. The
       `Resource` arm's wire struct declares fields in schema order: `type`, `resource`,
       `annotations`, `_meta`; the inner `resource` object declares `uri`, `mimeType`, then `text`
       or `blob`. Use reference fields (`&'a str`, `&'a Option<String>`) exactly as `Rc<'a>` does at
       `:343-359` so a base64 blob is never deep-cloned per tool result. Do NOT use
       `#[serde(into = "…")]` unless the borrow proves impossible — it forces a full clone on every
       serialization; if you fall back to it, say so and justify it in the SUMMARY.

    3. Replace `Content`'s derived `Deserialize` with `#[serde(try_from = "WireContentIn")]` where
       `WireContentIn`'s `Resource` arm accepts EITHER a nested payload (has `resource`) or a flat
       payload (has top-level `uri`), e.g. via an inner `#[serde(untagged)]` payload enum whose two
       arms are disjoint on the required key. Verify empirically that the untagged disambiguation is
       correct — RESEARCH assumption A1 is unproven and a silent wrong-arm pick is the failure mode.
       Emit only nested; the flat shape is accept-only.

    4. Decide and DOCUMENT the `text` + `blob` XOR rule. `TextResourceContents | BlobResourceContents`
       is an XOR in the spec, so an object carrying both is out of contract. Pick one of reject-with-error
       or deterministic-prefer, state which in the field rustdoc, and test it.

    4b. THE NEITHER-BRANCH, decided here and not left open. `Content::resource(uri)` at
       `src/types/content.rs:142-149` builds `Self::Resource { uri, text: None, mime_type: None,
       meta: None }` — neither `text` nor `blob`. Under the nested shape that value has NO valid
       `resource` payload, because `TextResourceContents` (`schema.ts:1535`) requires `text` and
       `BlobResourceContents` (`schema.ts:1548`) requires `blob`, and the union admits nothing else.
       Emitting `{"type":"resource","resource":{"uri":"u"}}` would ship a NEW spec violation inside
       the fix for the old one. THE RULE IS: when both `text` and `blob` are `None`, the emitter
       writes `text` as the empty string — `{"type":"resource","resource":{"uri":"u","text":""}}` —
       so `text` is the DEFAULT arm of the union. Write that sentence into the `text` field's
       rustdoc and add a unit test asserting those exact bytes.
       Why not a serialization error: D-03's tolerant reader accepts the legacy flat shape, which
       may legitimately carry no `text`; a relay such as `src/composition/mcp_client.rs` or
       `crates/mcp-tester` that deserializes then re-serializes would then fail at RESPONSE time,
       turning a compatibility affordance into a runtime fault on the serving path. Record this
       rationale in the SUMMARY.

    4c. DEPRECATE the URI-only embedded-resource constructor and add its replacement. A URI-only
       reference is `ResourceLink` in the spec, not `EmbeddedResource`; `Content::resource`'s own
       rustdoc already calls it "a minimal resource reference". Do three things:
       - Add `#[deprecated(since = "2.19.0", note = "a URI-only value cannot be a spec-valid
         EmbeddedResource; use Content::resource_with_text / Content::resource_with_blob for
         embedded content, or Content::resource_link for a reference")]` to `Content::resource`.
         Leave the BODY and the returned variant unchanged — this phase does not silently switch
         which variant a caller gets.
       - Add `pub fn resource_link(name: impl Into<String>, uri: impl Into<String>) -> Self`, a thin
         constructor over `ResourceLinkContent::new(name, uri)` at `:241`, boxed into
         `Content::ResourceLink`, with a working rustdoc example.
       - SEMVER CONSEQUENCE, to be stated verbatim in the SUMMARY and in the CHANGELOG next to the
         D-02 callout: `#[deprecated]` on an existing public fn is MINOR and adding a public fn is
         MINOR, so this adds nothing to the one-time D-15 major delta. Removal is scheduled for the
         next major, not performed here.
       - `make lint` runs with `-D warnings`, so every IN-CRATE caller must migrate in this same
         commit or the gate fails. There are exactly four (`git grep -n 'Content::resource('`):
         `src/server/workflow/conversion.rs:56` (`PromptContent::ResourceUri`) and `:89`
         (`PromptContent::ResourceHandle`) become `Content::resource_link(uri, uri)` — both arms'
         own comments say "Return as resource reference (LLM will fetch)", which is exactly
         `ResourceLink` semantics; `src/client/host/sampling.rs:232` and
         `src/types/content.rs:577` are tests and move to an explicit construction that states
         what they are testing. Update `conversion.rs`'s own unit tests to expect
         `Content::ResourceLink`. `git grep -n 'ResourceUri'` shows no test outside
         `conversion.rs` pins the old wire shape, and the dual-conformance example contains zero
         `workflow` references, so this touches no golden — CONFIRM both greps before landing and
         record their output in the SUMMARY.

    5. Add `blob` to `resource_contents_serde`'s private shadow struct `Rc<'a>` and teach its
       `deserialize` half to read one. This is the SECOND emitter and the one that is easy to forget —
       without it `resources-read-binary` stays red even though the nested fix is perfect. Field order
       must match plan 02's RESOURCES_READ_BINARY literal.

    6. Add the mandatory constructors — `#[non_exhaustive]` makes external struct-literal construction
       impossible FOREVER, so these are not optional: `Content::resource_with_blob(uri, blob, mime_type)`
       mirroring `resource_with_text` at `:159`, and a `with_annotations` builder in the shape of
       `ResourceLinkContent::with_annotations` at `:213-293`. Both need working rustdoc examples —
       `make test-doc` gates them.

    7. Flip `test_content_resource_meta_serialization` at `:429`: `json["uri"]` becomes
       `json["resource"]["uri"]`. Add per-arm unit tests for nested emission, blob emission,
       annotations placement, flat acceptance, nested acceptance, and the XOR rule.

    Watch two named hazards. First, a `try_from` container attribute changes deserialization ERROR
    MESSAGES from serde's "unknown variant"/"missing field" text to the `TryFrom::Error` `Display` —
    grep `src/` and `tests/` for assertions on `Content` deserialization error text before landing,
    and fix any that flip. Second, confirm `Content` is not used inside a `#[serde(untagged)]` enum
    or behind `#[serde(flatten)]` anywhere, where `try_from` still works but buffers the whole value.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo nextest run --features full -E "binary(pmcp)" -- content 2>&1 | tee target/118.1-03-unit.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-03-unit.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$rc" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'non_exhaustive' src/types/content.rs` shows the attribute on the `Resource` variant
    - `git grep -c 'blob' src/types/content.rs` is at least 6 (variant field, wire struct, `Rc<'a>`,
      deserializer, constructor, tests)
    - `Content::resource_with_blob` and a `with_annotations` builder are public with rustdoc examples
    - The `Content` unit tests run with a NON-ZERO count and exit 0
    - `cargo nextest run --features full -E 'binary(v1_lists_golden)'` still exits 0 with a non-zero
      count — if `RESOURCES_READ` goes red, the flat projection was broken and the change is wrong
    - The SUMMARY names the chosen `text` + `blob` XOR rule and quotes the rustdoc sentence stating it
    - A unit test asserts the NEITHER branch emits exactly
      `{"type":"resource","resource":{"uri":"u","text":""}}`; `git grep -n 'text: \"\"' src/types/content.rs`
      finds the rule's implementation and the field rustdoc states it
    - `git grep -n 'deprecated' src/types/content.rs` shows the attribute on `Content::resource`,
      and `Content::resource_link` exists with a rustdoc example
    - `git grep -n 'Content::resource(' -- '*.rs'` returns ZERO hits inside `src/` after migration
    - `bash -o pipefail -c 'cargo build --features full 2>&1 | tee target/118.1-03-deprecation.log'`
      exits 0, and a following `! grep -q 'use of deprecated' target/118.1-03-deprecation.log`
      confirms the pmcp crate emits no deprecation warnings
    - The SUMMARY quotes the semver sentence: deprecation and the new constructor are both MINOR and
      add nothing to the D-15 major delta
  </acceptance_criteria>
  <done>`Content::Resource` emits the spec nested shape with blob and annotations, accepts both shapes on input, and the flat `ReadResourceResult` projection is unchanged except for gaining `blob`.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the in-crate construction and pattern sites inside src/</name>
  <files>src/composition/mcp_client.rs, src/server/simple_resources.rs</files>
  <read_first>
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-02-SUMMARY.md — the classified recorded-bytes inventory, specifically the COMPILE-ONLY rows
    - src/server/simple_resources.rs — lines 30-70 and 340-360
    - src/composition/mcp_client.rs — lines 270-380 (both the construction at 275 and the exhaustive
      match arm at 362-367 that maps into `PromptContent::Resource`)
  </read_first>
  <action>
    Fix ONLY the sites inside the `pmcp` library crate, and stop at the crate boundary.
    `#[non_exhaustive]` does not restrict the defining crate, so here the two new fields break
    exactly two things: exhaustive struct literals (which now miss `blob` and `annotations`) and
    exhaustive patterns (which now miss the same two bindings). Add the fields to the literals, and
    add `..` to every pattern that does not need them.

    Plan 02 Task 1's inventory pre-measured this surface: 6 field-bearing sites in `src/` —
    `composition/mcp_client.rs:275,362`, `server/simple_resources.rs:38,58,350`,
    `types/content.rs:398,435` — of which the two in `types/content.rs` are already handled by
    Task 1. 46 further sites already carry a `..` rest pattern and are unaffected; do not touch them.

    Be precise about `composition/mcp_client.rs:362-367`. That is a MATCH ARM mapping `Content` into
    `PromptContent::Resource`, not a construction. If `blob` or `annotations` carries meaning on the
    far side of that map, propagate it rather than discarding it behind a bare `..`, and say in the
    SUMMARY which choice you made and why. Silently dropping `blob` there would make an embedded
    binary resource in a prompt message lose its payload — a data-loss defect that still compiles
    and that no golden in this phase would catch.

    Everything outside `src/` belongs to Task 3 — INCLUDING this package's own `tests/` and
    `benches/` targets, which are separate crates for `#[non_exhaustive]` purposes and are therefore
    blocked from struct-literal construction exactly like a downstream consumer.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo build -p pmcp --lib --features full 2>&1 | tee target/118.1-03-lib.log; rc=${PIPESTATUS[0]}; test "$rc" -eq 0 || { echo "pmcp LIB DOES NOT COMPILE"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p pmcp --lib --features full` exits 0
    - Every site the compiler named inside `src/` is listed in the SUMMARY with the fix applied
    - The SUMMARY states whether `composition/mcp_client.rs`'s `PromptContent::Resource` map
      propagates or discards `blob` and `annotations`, and why
  </acceptance_criteria>
  <done>The pmcp library compiles against the new variant, with the prompt-message map's field handling stated rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Fix every consumer OUTSIDE the pmcp library crate, workspace-wide</name>
  <files>benches/transport_performance.rs, cargo-pmcp/src/templates/mcp_app.rs, crates/mcp-tester/src/tester.rs, crates/pmcp-server/src/resources/docs.rs, crates/pmcp-server-toolkit/src/resources.rs, examples/s54_v2_dual_conformance.rs, examples/c03_client_resources.rs, examples/c10_client_skills.rs, examples/26-server-tester/src/tester.rs, examples/mcp-apps-chess/src/main.rs, examples/mcp-apps-dataviz/src/main.rs, examples/mcp-apps-map/src/main.rs, tests/protocol_invariants.rs, tests/toolresult_property_tests.rs, tests/embedded_resource_example_run.rs</files>
  <read_first>
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-02-SUMMARY.md — the classified recorded-bytes inventory, specifically the COMPILE-ONLY rows
    - cargo-pmcp/src/templates/mcp_app.rs — lines 240-270 and 455-475: these are TEMPLATE STRINGS
      (note the doubled `{{` braces) that emit `Content::Resource { … }` into freshly scaffolded USER
      crates, which are external to `pmcp` and therefore blocked by `#[non_exhaustive]`
    - examples/s54_v2_dual_conformance.rs — line 332, the `test://static-binary` handler
    - examples/s54_v2_dual_conformance.rs — lines 1389-1400 (the argv[1] bind-address override) and
      1443-1460, where `start()` returns AFTER the socket is bound; that is the readiness guarantee a
      driver polls against, and line 1450 names 8155 as the fallback hint so 8155 is NOT free
    - tests/common/v2.rs — lines 565-600 (`v2_body` and the `params._meta` era signal),
      672-760 (`v1_body`, `v2_headers_for`, `header`), and 775 plus 844-880 (the `Response { raw }`
      shape and `post`)
  </read_first>
  <action>
    Task 2 left the library compiling; this task makes the rest of the workspace compile. Run a full
    workspace build and fix every site the compiler names. Outside the `pmcp` crate —
    `cargo-pmcp`, `crates/mcp-tester`, `crates/pmcp-server`, `crates/pmcp-server-toolkit`,
    `examples/26-server-tester`, `benches/`, `examples/`, `tests/` — `#[non_exhaustive]` blocks
    struct-literal construction entirely, so adding the two fields is not a fix there. Replace every
    such construction with `Content::resource_with_text(...)` or `Content::resource_with_blob(...)`
    plus `.with_annotations(...)` where needed, and add `..` to every exhaustive pattern.

    Treat `cargo-pmcp/src/templates/mcp_app.rs` as the highest-risk site: it is a scaffold template,
    so a stale `Content::Resource { … }` literal there produces user crates that DO NOT COMPILE
    against the new pmcp. Rewrite both occurrences to use the constructor form. This is the same
    class of defect as the structured-output scaffold-pin tripwire.

    Then grow `examples/s54_v2_dual_conformance.rs`'s surface so the closed gaps are actually
    exercised — this is the CLAUDE.md ALWAYS-example requirement and it is load-bearing, not
    cosmetic. `test://static-binary` currently returns `Content::image(TINY_PNG_BASE64, "image/png")`
    at line 332, which emits `{"type":"image","data":…}`; the suite's `resources-read-binary`
    requires `contents[0]` carrying `uri`, `mimeType` and `blob`. Change it to
    `Content::resource_with_blob("test://static-binary", TINY_PNG_BASE64, "image/png")`. Leave
    `test://embedded-resource` and `test://mixed-content-resource` on `resource_with_text` — they
    now nest automatically. Consider adding an annotations-bearing embedded resource so the
    `annotations` placement is exercised end to end; if you add one, say which URI carries it.

    Note that `examples/s54_v2_dual_conformance.rs` is also touched by plan 04 (the completion
    provider) and plan 12 (the SSE progress work) — NOT plan 11, which never opens this file. Keep
    this edit confined to the resource handler so those cannot collide.

    Finally, PROVE the example is RUN and not merely built. The ALWAYS-example requirement is only
    real if a machine checks it, so this leg is part of THIS task rather than a SUMMARY claim.
    Create `tests/embedded_resource_example_run.rs` with the standard file gate (`streamable-http`,
    `http-client`, not wasm32) and `mod common;` plus the `common::v2` imports. The leg spawns the
    ALREADY-BUILT binary at `target/debug/examples/s54_v2_dual_conformance` with `127.0.0.1:8157` as
    argv[1], POLLS for readiness rather than sleeping, and issues `resources/read` on
    `test://static-binary` on BOTH eras — `common::v2::v1_body` and `common::v2::v2_body` for the two
    request framings, `v2_headers_for` for the headers, and `common::v2::post` to send. Do NOT
    hand-roll a curl probe: the header contract (`Mcp-Method`, `MCP-Protocol-Version`, the
    `params._meta` era signal) is exactly what a shell probe gets wrong, and a probe that mis-frames
    the request produces a red that says nothing about the gap.

    Write BOTH raw response bodies to `target/118.1-03-blob-response.json` as one object keyed `v1`
    and `v2`, resolving the path through `env!("CARGO_MANIFEST_DIR")` rather than the process CWD.
    Pretty-print it — the SUMMARY quotes this artifact verbatim, and the era-count assertion counts
    `blob` OCCURRENCES rather than lines, so it is legibility that is at stake here, not the
    assertion. If the binary is ABSENT the leg must FAIL, never skip: a skip would restore exactly
    the unenforced criterion this leg exists to close. Tear the child down on every exit path and
    prove the port was released, the way `scripts/run-conformance-suite.sh`'s cleanup does; a
    teardown that did not tear down leaves a stale listener answering the next run.

    8157 is chosen deliberately: 8147 (`s47_v2_stateless_mrtr`), 8149 (this example's own default),
    8150 (`s50`/`s51`) and 8151 (`scripts/run-conformance-suite.sh`) are held by in-repo examples and
    scripts, 8155 is named as the fallback hint in this example's bind-failure message at line 1450,
    and 8153 is taken by plan 04 Task 3's sibling leg. Once plan 04 lands, both test binaries run
    concurrently under nextest, so the two ports must not collide. Name the port in the SUMMARY.

    This leg lands FIRST — plan 04 Task 3's `completion/complete` leg mirrors it one wave later — so
    write it self-contained rather than deferring to a sibling that does not exist yet.

    If plan 02's inventory classified any `contracts/team-servers/fixtures/` fixture as WILL FLIP,
    regenerate exactly those fixtures using the driver's existing regeneration entry point and list
    each regenerated file with its before/after diff summary in the SUMMARY. Do NOT regenerate
    fixtures that were classified MUST NOT FLIP or COMPILE-ONLY.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo build --workspace --all-targets --features full 2>&1 | tee target/118.1-03-build.log; rc=${PIPESTATUS[0]}; test "$rc" -eq 0 || { echo "WORKSPACE BUILD FAILED"; exit 1; }; grep -q "Content::Resource {" cargo-pmcp/src/templates/mcp_app.rs && { echo "scaffold template still uses a struct literal"; exit 1; }; test -x target/debug/examples/s54_v2_dual_conformance || { echo "EXAMPLE BINARY MISSING"; exit 1; }; rm -f target/118.1-03-blob-response.json; cargo nextest run --features full -E "binary(embedded_resource_example_run)" 2>&1 | tee target/118.1-03-examplerun.log; trc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-03-examplerun.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$trc" -eq 0 || { echo "EXAMPLE-RUN LEG FAILED"; exit 1; }; test -s target/118.1-03-blob-response.json || { echo "EXAMPLE WAS NOT RUN: no response artifact"; exit 1; }; blobs=$(grep -o "\"blob\"" target/118.1-03-blob-response.json | wc -l | tr -d " "); test "$blobs" -ge 2 || { echo "ARTIFACT CARRIES $blobs blob KEYS, EXPECTED ONE PER ERA"; exit 1; }; grep -q "test://static-binary" target/118.1-03-blob-response.json || { echo "ARTIFACT IS NOT FOR test://static-binary"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --all-targets --features full` exits 0
    - `git grep -n 'Content::Resource {' -- cargo-pmcp crates examples benches tests` returns no
      struct-literal CONSTRUCTION sites (pattern matches carrying `..` are acceptable and must be
      listed in the SUMMARY if any remain)
    - `examples/s54_v2_dual_conformance.rs` maps `test://static-binary` through `resource_with_blob`
      and no longer through `Content::image`
    - The example is RUN, not merely built, and it is machine-checked in THIS task:
      `binary(embedded_resource_example_run)` selects a NON-ZERO count, exits 0, and leaves
      `target/118.1-03-blob-response.json` non-empty, naming `test://static-binary` and carrying a
      `"blob"` key on BOTH era legs — quoted VERBATIM in the SUMMARY
    - That leg FAILS rather than skipping when `target/debug/examples/s54_v2_dual_conformance` is
      absent, and releases its port on every exit path; the SUMMARY names the port it took (8157)
    - The SUMMARY lists every file the compiler named and the fix applied to each
  </acceptance_criteria>
  <done>The whole workspace compiles against the new variant, the scaffold template emits constructor calls, and the dual-conformance example is PROVED by a live run to serve a real binary resource with a `blob`.</done>
</task>

<task type="auto">
  <name>Task 4: Turn plan 02's fences GREEN and run the fuzz campaign</name>
  <files>(no new files — this task proves plan 02's artifacts flip)</files>
  <read_first>
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-02-SUMMARY.md — the captured RED output the GREEN run is compared against
    - tests/embedded_resource_golden.rs — the four literals and the leak guard
    - fuzz/fuzz_targets/content_tolerant_reader.rs — the three invariants
  </read_first>
  <action>
    Run `tests/embedded_resource_golden.rs` and prove all four literals now match, on both eras.
    Capture the GREEN output and place it beside plan 02's captured RED output in the SUMMARY so the
    flip is legible as one before/after pair per literal — that is D-09's per-gap named artifact for
    G-1 and G-2.

    If any literal still fails, do NOT edit the literal. The literal is derived from `schema.ts` and
    is the oracle; a mismatch means the emitter is wrong. The one exception is a demonstrable
    transcription error in the literal itself, which must be proven by re-reading the cited
    `schema.ts` line and quoting it in the SUMMARY.

    Run the fuzz campaign at the CLAUDE.md minimum:
    `cargo +nightly fuzz run content_tolerant_reader -- -max_total_time=300`. All three invariants
    (no-panic, nested fixed point, flat acceptance producing nested) must now hold. Record the
    execution count and confirm the artifacts directory is EMPTY — a non-empty `fuzz/artifacts/`
    directory is a finding, not noise.

    Re-run `tests/v1_lists_golden.rs` and confirm `RESOURCES_READ` is still green. Its flat
    `{uri, mimeType, text}` shape is the D-01 boundary; if it went red the change reached past its
    scope.

    Add a property test covering the round-trip pair: nested to `Content` to nested is a fixed point,
    and flat to `Content` to nested produces the spec shape. Put it wherever the crate's other
    `proptest` cases live so `make test-property` picks it up, and name the file in the SUMMARY.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo nextest run --features full -E "binary(embedded_resource_golden)" 2>&1 | tee target/118.1-03-green.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-03-green.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$rc" -eq 0 || exit 1; cargo nextest run --features full -E "binary(v1_lists_golden)" 2>&1 | tee target/118.1-03-v1golden.log; rc2=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-03-v1golden.log || exit 1; test "$rc2" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - `binary(embedded_resource_golden)` runs a NON-ZERO count and exits 0
    - `binary(v1_lists_golden)` runs a NON-ZERO count and exits 0
    - `cargo +nightly fuzz run content_tolerant_reader -- -max_total_time=300` completes with an
      EMPTY `fuzz/artifacts/content_tolerant_reader/` directory, and the execution count is recorded
    - The SUMMARY contains, per literal, the plan-02 RED text and this plan's GREEN text side by side
    - A property test covering both round-trip directions exists and is named in the SUMMARY
  </acceptance_criteria>
  <done>G-1 and G-2 each have a named artifact demonstrably flipped RED to GREEN, with the fuzz campaign clean and the D-01 boundary intact.</done>
</task>

<task type="auto">
  <name>Task 5: Record the semver delta and write the D-02 CHANGELOG wire callout</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md — the most recent released section, for heading level and entry style
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md — D-02 and D-15
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-RESEARCH.md — Pitfall 1, including the measured lint severities
    - docs/v1-sunset-policy.md — the nearest tonal model in-repo for a policy-bearing doc
  </read_first>
  <action>
    Run `cargo semver-checks` against the phase base revision and capture the FULL output.

    D-15 makes the resulting major-lint delta a documented one-time exception, NOT a regression.
    Expect `enum_struct_variant_field_added` (blob, annotations) and
    `enum_variant_marked_non_exhaustive` (the variant attribute). Record the exact lint names and
    counts in the SUMMARY under an explicitly labelled heading such as "Accepted semver delta
    (D-15, one-time)". An acceptance criterion of the form "cargo semver-checks reports no update
    required" is UNSATISFIABLE under D-15 and its presence would mean the decision was ignored —
    do not write one.

    Contrast it in the same record with `ServerDiscoverResult`, which plan 07 extends for free
    because that struct IS `#[non_exhaustive]` (`src/types/protocol/mod.rs:626-628`, whose own
    rustdoc carries the argument). Stating both in one place is what makes the asymmetry read as
    measured rather than sloppy.

    Then write the CHANGELOG entry under the unreleased section. It must be prominent, not a bullet
    buried in a list, and must cover four things:
    1. WHAT changed on the wire: an embedded resource in a tool result or prompt message now
       serializes as `{"type":"resource","resource":{...}}` instead of the previous flat
       `{"type":"resource","uri":...}`, on BOTH 2025-11-25 and 2026-07-28.
    2. WHY: the flat shape was a defect that made pmcp incompatible with every other MCP
       implementation; reliance on it was reliance on a bug. There is NO opt-out escape hatch.
    3. WHAT DID NOT change: `ReadResourceResult.contents` stays flat, because `ResourceContents` IS
       flat in the spec (schema.ts:1514-1560). It gains `blob`, nothing else.
    4. The MIGRATION note: readers of pmcp tool results must read `content.resource.uri` instead of
       `content.uri`; pmcp's own reader accepts both shapes so a mixed-version fleet keeps working
       in the client direction; and `Content::Resource` is now `#[non_exhaustive]`, so downstream
       crates construct it through `Content::resource_with_text` / `Content::resource_with_blob`
       and match it with a `..` rest pattern.

    Note in the entry that Phase 119's migration guide depends on this text.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'grep -q "EmbeddedResource\|\"resource\":" CHANGELOG.md || { echo "no wire callout in CHANGELOG"; exit 1; }; grep -q "resource_with_blob" CHANGELOG.md || { echo "no migration guidance for the constructor"; exit 1; }; grep -q "non_exhaustive" CHANGELOG.md || { echo "no non_exhaustive migration note"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY has a heading naming the accepted semver delta as a D-15 one-time exception, with
      the exact `cargo semver-checks` lint names and counts quoted from the captured run
    - The SUMMARY contains NO claim that `cargo semver-checks` reports no update required
    - The SUMMARY states the `ServerDiscoverResult` contrast and cites `src/types/protocol/mod.rs:626-628`
    - CHANGELOG.md contains all four elements: the wire change, the no-opt-out rationale, the
      explicit statement that `ReadResourceResult.contents` stays flat, and the migration note naming
      `content.resource.uri`, `resource_with_blob` and `#[non_exhaustive]`
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>The semver delta is recorded as a deliberate documented exception and the wire change is announced with a migration path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote MCP server to pmcp client (`Content::deserialize`) | The D-03 tolerant reader parses untrusted server output; this is a NEW parse surface |
| pmcp server to remote MCP client (`Content::serialize`) | Emitted bytes now carry base64 blobs; content must not be reinterpreted across the text/blob boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.1-03-01 | Tampering | Blob content treated as text — a hostile payload carrying BOTH `text` and `blob` inside `resource`, where the spec type is an XOR | mitigate | Task 1 step 4 requires a documented deterministic XOR rule (reject or prefer), stated in the field rustdoc and covered by a unit test |
| T-118.1-03-02 | Denial of Service | Memory amplification through the tolerant reader — a hybrid flat/nested payload causing repeated buffering | mitigate | `try_from` buffers the value once; the existing body bounds guarded by `v2_bounded_reads_tripwire` are unchanged; the fuzz campaign runs at `-max_total_time=300` with an empty artifacts directory required |
| T-118.1-03-03 | Information Disclosure | A wrong untagged-arm pick silently routing a nested payload into the flat arm, dropping or misattributing fields | mitigate | RESEARCH assumption A1 is explicitly unproven; Task 1 requires empirical verification of the disambiguation plus fuzz invariants 2 and 3 that would catch a wrong-arm pick |
| T-118.1-03-04 | Repudiation | The semver guards are structurally blind to a wire reshape, so a green gate could be read as "the wire is unchanged" | mitigate | Task 5 records the accepted delta explicitly and forbids the unsatisfiable no-update-required criterion; plan 02's byte literals are the actual wire fence |
| T-118.1-03-05 | Denial of Service | Scaffolded user crates failing to compile against `#[non_exhaustive]` | mitigate | Task 3 rewrites both `cargo-pmcp/src/templates/mcp_app.rs` template occurrences to constructor form and asserts the struct literal is gone |
| T-118.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan; all dependencies come from the committed `Cargo.lock` |
</threat_model>

<verification>
- `binary(embedded_resource_golden)` non-zero count, exit 0
- `binary(v1_lists_golden)` non-zero count, exit 0 — the D-01 flat boundary held
- `cargo build --workspace --all-targets --features full` exit 0
- `binary(embedded_resource_example_run)` non-zero count, exit 0 — it spawns the built
  `s54_v2_dual_conformance` binary and records its `resources/read` answers for `test://static-binary`
  at `target/118.1-03-blob-response.json`, carrying a `blob` on both eras
- `cargo +nightly fuzz run content_tolerant_reader -- -max_total_time=300` clean, artifacts empty
- `make quality-gate` exit 0 (includes `make test-doc` for the new constructor rustdoc examples)
- `pmat analyze complexity --max-cognitive 25` reports no new violation in `src/types/content.rs`
</verification>

<success_criteria>
- G-1 and G-2 each carry a named RED-to-GREEN artifact with both texts captured
- The nested emitter, the tolerant reader, `blob` in BOTH emitters, `annotations`,
  `#[non_exhaustive]` and the constructors all land in one batched edit
- `ReadResourceResult.contents` is still flat and now carries `blob`
- The accepted semver delta is recorded as a D-15 one-time exception with lint names and counts
- CHANGELOG carries the wire callout and the migration note
</success_criteria>

<output>
Create `.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-03-SUMMARY.md` when done
</output>
