---
phase: 73
plan: 03
type: execute
wave: 3
revision: 2
depends_on:
  - 73-01
  - 73-02
files_modified:
  - examples/c02_client_tools.rs
  - examples/c09_client_list_all.rs
  - examples/README.md
  - Cargo.toml
  - crates/mcp-tester/Cargo.toml
  - cargo-pmcp/Cargo.toml
  - examples/25-oauth-basic/Cargo.toml
  - examples/test-basic/Cargo.toml
  - crates/pmcp-tasks/Cargo.toml
  - crates/pmcp-server/Cargo.toml
  - crates/pmcp-server/pmcp-server-lambda/Cargo.toml
  - CHANGELOG.md
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PARITY-CLIENT-01
tags:
  - rust
  - examples
  - docs
  - release
  - changelog
  - version-bump

must_haves:
  truths:
    - "`examples/c02_client_tools.rs` demonstrates `call_tool_typed` with a `#[derive(Serialize)]` struct, replacing the existing manual `json!({…})` pattern."
    - "`examples/c09_client_list_all.rs` exists, compiles, is registered in root `Cargo.toml` `[[example]]` with `required-features = [\"full\"]`, and is registered in `examples/README.md` under Client examples; filename avoids the c08 collision with Phase 74's `c08_oauth_dcr.rs`."
    - "`examples/c09_client_list_all.rs` exercises ALL FOUR `list_all_*` helpers, including `list_all_resource_templates`; if a helper is intentionally skipped, the example contains a rustdoc/top-of-file comment stating why AND the CHANGELOG mirrors that caveat."
    - "`examples/c09_client_list_all.rs` has a header comment block that documents exactly how to run it (what MCP server to pair with via stdio) OR marks itself explicitly as compile-only/demo-only so users don't expect `cargo run --example c09_client_list_all` to produce meaningful output without a paired server."
    - "Root `Cargo.toml` pmcp version is bumped from `2.5.0` to `2.6.0`."
    - "All 7 workspace Cargo.toml files that pin `pmcp = \"2.5.0\"` are bumped to `\"2.6.0\"` (per PATTERNS §9 — verified via grep, not just the 2 mentioned in CONTEXT.md)."
    - "`CHANGELOG.md` has a new `## [2.6.0]` entry above the existing `## [2.5.0]` block, listing the four typed helpers (via ClientOptions path), the four `list_all_*` helpers (explicitly including `list_all_resource_templates`), `ClientOptions`, `with_client_options`, the new example, and the REQUIREMENTS.md doc-fix."
    - "`.planning/REQUIREMENTS.md:55` has `call_prompt_typed` replaced with `get_prompt_typed` (D-15 doc-fix)."
    - "`README.md` Key Features list includes a 'Typed Client Helpers' bullet and the Latest Version line reflects 2.6."
    - "`examples/README.md` client-example count and/or table is updated to reflect the addition of c09 (not stale at the pre-Phase-73 count)."
    - "`make quality-gate` passes end-to-end against the full workspace with all Phase 73 changes staged."
  artifacts:
    - path: "examples/c09_client_list_all.rs"
      provides: "end-to-end demo of typed helpers + ALL FOUR list_all_* (or explicitly documented omission)"
      min_lines: 70
    - path: "examples/c02_client_tools.rs"
      provides: "updated example showcasing call_tool_typed"
      contains: "call_tool_typed"
    - path: "examples/README.md"
      provides: "c09 registration + updated client-example count"
      contains: "c09_client_list_all"
    - path: "CHANGELOG.md"
      provides: "## [2.6.0] entry naming list_all_resource_templates explicitly"
      contains: "[2.6.0]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "get_prompt_typed correction on line 55"
      contains: "get_prompt_typed"
    - path: "Cargo.toml"
      provides: "pmcp version bumped to 2.6.0 + [[example]] c09 with required-features"
      contains: "version = \"2.6.0\""
  key_links:
    - from: "examples/c09_client_list_all.rs"
      to: "root Cargo.toml"
      via: "[[example]] stanza name = \"c09_client_list_all\" with required-features = [\"full\"]"
      pattern: "c09_client_list_all"
    - from: "examples/c09_client_list_all.rs"
      to: "examples/README.md"
      via: "Client section entry with copy-paste cargo run command + updated count"
      pattern: "c09_client_list_all"
    - from: "CHANGELOG.md"
      to: "Cargo.toml version"
      via: "## [2.6.0] header matches `version = \"2.6.0\"`"
      pattern: "2\\.6\\.0"
---

## Revision Notes

Revised in response to `73-REVIEWS.md` findings. Addresses:

- **MEDIUM (both reviewers) — c09 doesn't cover `list_all_resource_templates`.** The example body now exercises the fourth helper explicitly, matching the shipped surface. If the executor determines that `list_all_resource_templates` legitimately cannot be demonstrated in the chosen paired server (e.g. the reference server doesn't advertise `resources/templates/list`), they MUST add (a) a top-of-file comment in `c09` explaining the omission AND (b) a one-line caveat in the CHANGELOG `### Added` block — silent omission is rejected.
- **MEDIUM (Codex) — c09 isn't self-contained.** The example header comment block now explicitly documents either:
    1. The exact stdio MCP server to pair with (binary path + flags, preferably one of the repo's own example servers such as `examples/01_server_basic.rs`), OR
    2. A clear "compile-only / demo-only" banner stating that the binary compiles to demonstrate the API surface but requires a paired server to produce meaningful output (no silent stdio-hang on `cargo run`).
  This prevents README framing from overstating runnability.
- **MEDIUM (Gemini / Codex) — `examples/README.md` client-example count.** The existing Client-examples count/table (currently reading 7 client examples) must be bumped to reflect c09's addition. Task 1 step 4 now explicitly updates the count/table, not just inserts a new c09 block.
- **LOW (Gemini) — `required-features` on the `[[example]]` stanza.** Now explicitly required (not optional) because c09 uses `StdioTransport` + `Client::with_client_options` + the full client surface, which depends on the `full` feature set. Missing `required-features` would break CI's minimal-feature build.

Consensus items preserved: non-breaking 8-pin workspace bump; CHANGELOG v2.6.0 entry; REQUIREMENTS.md §55 D-15 doc-fix; README bullet; `make quality-gate` as the final gate.

<objective>
Finalize Phase 73 with the release + documentation surface: update the existing `c02` client example to showcase `call_tool_typed`, ship a new dedicated pagination example `c09_client_list_all.rs` that exercises ALL four `list_all_*` helpers (including `list_all_resource_templates`), register it in `Cargo.toml` + `examples/README.md` (bumping the client-example count), bump pmcp from 2.5.0 to 2.6.0 across all 8 pin lines in 7 workspace Cargo.toml files, write the CHANGELOG v2.6.0 entry, apply the REQUIREMENTS.md §55 doc-fix (`call_prompt_typed` → `get_prompt_typed`, D-15), and refresh the README feature bullet list.

Purpose: Ship PARITY-CLIENT-01 as a releasable v2.6.0 artifact, with discoverable and accurate examples. Closes the DX loop: a user can read the c09 header comment, pair it with the documented stdio server, `cargo run --example c09_client_list_all --features full`, and see the new helpers in action.

Output: Two example files (one updated, one new); eight Cargo.toml version-pin bumps; CHANGELOG entry; REQUIREMENTS.md doc-fix; README bullet; updated `examples/README.md` client-example count; `make quality-gate` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-CONTEXT.md
@.planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-RESEARCH.md
@.planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-PATTERNS.md
@.planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-REVIEWS.md
@.planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-01-SUMMARY.md
@.planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- All eight pin lines that must be bumped (verified via PATTERNS §9 grep): -->

```
examples/25-oauth-basic/Cargo.toml:24:       pmcp = { version = "2.5.0", path = "../../", features = ["streamable-http"] }
cargo-pmcp/Cargo.toml:38:                    pmcp = { version = "2.5.0", path = "..", features = ["streamable-http", "oauth"] }
examples/test-basic/Cargo.toml:13:           pmcp = { version = "2.5.0", path = "../../", features = ["http"] }
crates/pmcp-tasks/Cargo.toml:10:             pmcp = { version = "2.5.0", path = "../..", default-features = false }
crates/pmcp-tasks/Cargo.toml:32:             pmcp = { version = "2.5.0", path = "../..", features = ["full"] }
crates/pmcp-server/Cargo.toml:30:            pmcp = { version = "2.5.0", path = "../../", features = ["streamable-http"] }
crates/pmcp-server/pmcp-server-lambda/Cargo.toml:17: pmcp = { version = "2.5.0", path = "../../..", features = ["streamable-http"] }
crates/mcp-tester/Cargo.toml:21:             pmcp = { version = "2.5.0", path = "../../", features = ["streamable-http", "oauth"] }
```

CHANGELOG.md existing format (lines 8-32 — `## [2.5.0]` entry — mirror structure):

```markdown
## [2.5.0] - 2026-04-21

### Added
- ...

### Changed
- ...

### Fixed
- ...
```

REQUIREMENTS.md §55 current text:

```markdown
- [ ] **PARITY-CLIENT-01**: Ship typed-input `call_tool_typed` / `call_prompt_typed` helpers and auto-paginating `list_all_tools` / `list_all_prompts` / `list_all_resources` convenience methods on `Client`, reducing client boilerplate to one call per operation.
```

Target text (per D-15):

```markdown
- [ ] **PARITY-CLIENT-01**: Ship typed-input `call_tool_typed` / `get_prompt_typed` helpers and auto-paginating `list_all_tools` / `list_all_prompts` / `list_all_resources` convenience methods on `Client`, reducing client boilerplate to one call per operation.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `examples/c09_client_list_all.rs` covering all four `list_all_*` + self-contained-ness docs; register with `required-features`; update `examples/c02_client_tools.rs` and `examples/README.md` client-example count</name>
  <read_first>
    - examples/c02_client_tools.rs (full — to see the current `json!({…})` pattern being superseded and the overall example structure)
    - examples/c04_client_prompts.rs (full — to see the `HashMap<String, String>` pattern that `get_prompt_typed` supersedes)
    - examples/README.md (full — to locate (a) the current client-example count/table header text AND (b) the Client section where c09 goes)
    - Cargo.toml (root — full — search for existing `[[example]]` stanzas to confirm the current style and whether neighbouring stanzas use `required-features`)
    - examples/01_server_basic.rs or similar (ls examples/ to pick a stdio MCP server to cite in the c09 header as the "how to run" companion)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-PATTERNS.md (§7 — example body template + registration rules)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-RESEARCH.md (§"Landmines" item 2 — c08 collision forces c09 filename)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-REVIEWS.md (MEDIUM #4 — templates coverage, runnability docs, README count, required-features)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-CONTEXT.md (D-14 — example footprint intent)
  </read_first>
  <behavior>
    - `examples/c09_client_list_all.rs` compiles with `--features full` and exercises: `Client::with_client_options(transport, opts)`, `call_tool_typed` with a `#[derive(Serialize)]` struct, `get_prompt_typed` with a `#[derive(Serialize)]` struct, `list_all_tools`, `list_all_prompts`, `list_all_resources`, AND `list_all_resource_templates`.
    - IF `list_all_resource_templates` is omitted because no readily-pairable stdio server advertises the templates capability in the repo, the example MUST:
        1. Contain a clearly-labeled top-of-file comment `// NOTE: list_all_resource_templates is intentionally omitted — <reason>. See Phase 73 73-REVIEWS.md MEDIUM finding #4.`
        2. The corresponding CHANGELOG `### Added` line for the example MUST mirror the caveat (Task 3).
    - The file header comment block must explicitly document how to run the example: either (a) citing a specific stdio server binary from the repo with exact invocation, or (b) banner-tagging the file as compile-only/demo-only so users don't assume `cargo run --example c09_client_list_all` produces output.
    - `examples/c02_client_tools.rs` has at least one `call_tool` call replaced with `call_tool_typed` using a `#[derive(Serialize)]` struct. A top-of-file comment references Phase 73.
    - Root `Cargo.toml` has a new `[[example]]` stanza with `name = "c09_client_list_all"`, `path = "examples/c09_client_list_all.rs"`, AND `required-features = ["full"]` (Gemini LOW finding #10; required so CI's minimal-feature build does not fail).
    - `examples/README.md` gains:
        1. A c09 entry under the Client section with copy-paste `cargo run --example c09_client_list_all --features full` block AND the same runnability caveat as the source-file header.
        2. An updated client-example count/table — whatever the file currently lists (e.g., "7 client examples") must be incremented (e.g., "8 client examples") and any tabular lists must include c09.
    - `cargo check --example c09_client_list_all --features full` succeeds.
    - `cargo check --example c02_client_tools --features full` succeeds.
  </behavior>
  <action>
**Step 1 — Create `examples/c09_client_list_all.rs`.** Use this body as a starting point; the header-comment block and the explicit templates call are load-bearing.

```rust
//! Example: Typed call helpers and auto-paginating list helpers (Phase 73).
//!
//! Demonstrates the Phase 73 PARITY-CLIENT-01 additions:
//!   - `Client::with_client_options` (custom ClientOptions + max_iterations knob)
//!   - `Client::call_tool_typed` (struct → JSON args)
//!   - `Client::get_prompt_typed` (struct → HashMap<String, String>)
//!   - `Client::list_all_tools`, `list_all_prompts`, `list_all_resources`,
//!     AND `list_all_resource_templates` (the last uses the distinct
//!     `resources/templates/list` capability).
//!
//! # How to run
//!
//! This example drives an MCP server over **stdio**. It is NOT self-contained —
//! you must pair it with a compatible stdio MCP server. The simplest option is
//! one of the repo's own example servers:
//!
//! ```bash
//! # Terminal A — build the server example you want to probe:
//! cargo build --example 01_server_basic --features full
//!
//! # Terminal B — run c09, piping the server binary as its child:
//! # (adjust the path to the server binary your platform produced)
//! cargo run --example c09_client_list_all --features full
//! ```
//!
//! Running `cargo run --example c09_client_list_all --features full` **without**
//! a paired server will hang reading from stdio — this is expected. The binary
//! nonetheless compiles and demonstrates the PARITY-CLIENT-01 API surface for
//! readers; pair it with any stdio MCP server to see real output.

use pmcp::{Client, ClientCapabilities, ClientOptions, StdioTransport};
use serde::Serialize;

#[derive(Serialize)]
struct SearchArgs {
    query: String,
    limit: u32,
}

#[derive(Serialize)]
struct SummaryArgs {
    topic: String,
    length: u32,
}

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    tracing_subscriber::fmt()
        .with_env_filter("pmcp=info")
        .try_init()
        .ok();

    // Custom ClientOptions — lower the safety cap to illustrate the knob.
    let opts = ClientOptions {
        max_iterations: 50,
        ..Default::default()
    };

    let transport = StdioTransport::new();
    let mut client = Client::with_client_options(transport, opts);
    client.initialize(ClientCapabilities::minimal()).await?;

    // Typed call_tool — no hand-rolled json!({...}).
    let _call = client
        .call_tool_typed(
            "search",
            &SearchArgs {
                query: "rust mcp".into(),
                limit: 10,
            },
        )
        .await?;
    println!("called tool: search");

    // Typed get_prompt — numeric leaves auto-stringified per D-06.
    let _prompt = client
        .get_prompt_typed(
            "summarize",
            &SummaryArgs {
                topic: "rust async".into(),
                length: 200,
            },
        )
        .await?;
    println!("fetched prompt: summarize");

    // Auto-pagination across ALL FOUR families (MEDIUM finding #4 — templates
    // is the one with a distinct capability path `resources/templates/list`).
    let tools = client.list_all_tools().await?;
    println!("discovered {} tools across all pages", tools.len());

    let prompts = client.list_all_prompts().await?;
    println!("discovered {} prompts across all pages", prompts.len());

    let resources = client.list_all_resources().await?;
    println!("discovered {} resources across all pages", resources.len());

    let templates = client.list_all_resource_templates().await?;
    println!(
        "discovered {} resource templates across all pages",
        templates.len()
    );

    Ok(())
}
```

**If `StdioTransport` feature gating differs**, copy the exact `use` path from `examples/c02_client_tools.rs`.

**If the paired server does not expose templates**, the executor MAY remove the `list_all_resource_templates` call BUT MUST (a) add a top-of-file comment `// NOTE: list_all_resource_templates intentionally omitted — <reason>. See Phase 73 REVIEWS #4.`, and (b) reflect the omission in the CHANGELOG entry (Task 3). Silent omission fails the acceptance criteria.

**Step 2 — Update `examples/c02_client_tools.rs`.** Locate the existing `client.call_tool(...)` call (current line ~35 per CONTEXT.md), add a `#[derive(Serialize)] struct` above `main`, and replace one `call_tool` with `call_tool_typed`. Keep the rest of the example intact. Add a comment at the top referencing Phase 73 (`//! Phase 73 update: now demonstrates `Client::call_tool_typed` with a `#[derive(Serialize)]` struct.`).

**Step 3 — Register `c09` in root `Cargo.toml` with `required-features`.** Append after all existing `c*` stanzas:

```toml
[[example]]
name = "c09_client_list_all"
path = "examples/c09_client_list_all.rs"
required-features = ["full"]
```

The `required-features = ["full"]` line is mandatory (Gemini LOW finding #10) — neighbouring c-series stanzas may not have it, but c09 specifically uses `StdioTransport` + `Client::with_client_options` + `tracing_subscriber`, which require the `full` feature set. Without this line, `cargo build --workspace` with minimal features will fail on c09.

**Step 4 — Update `examples/README.md`.** Two edits:

1. **Locate the Client-examples count** (search for phrasing like `7 client examples`, `Client Examples (7)`, `| 7 |`, or similar). Increment it by 1. If the file uses a table with a row-count header, update both the narrative count AND the header. If no count exists, this step is a no-op and should be noted in SUMMARY.md.

2. **Insert the c09 entry** under the Client section, matching the existing style (headed section + description + code-fence invocation). Sample:

```markdown
### c09_client_list_all — Typed helpers + auto-paginating list_all_*

Phase 73 PARITY-CLIENT-01 demo. Shows `call_tool_typed`, `get_prompt_typed`, and all
four `list_all_*` helpers (including `list_all_resource_templates`) driven by a
custom `ClientOptions { max_iterations: 50, .. }`.

This example drives an MCP server over **stdio** and is not self-contained — see
the file header for pairing instructions.

```bash
cargo run --example c09_client_list_all --features full
```
```

If the intentional-omission path was taken in Step 1, mirror the caveat text here too.
  </action>
  <verify>
    <automated>cargo check --example c09_client_list_all --features full && cargo check --example c02_client_tools --features full</automated>
  </verify>
  <acceptance_criteria>
    - `test -f examples/c09_client_list_all.rs` succeeds.
    - `test ! -f examples/c08_client_list_all.rs` succeeds (NOT the collision filename).
    - `grep -nE "call_tool_typed" examples/c09_client_list_all.rs` returns at least 1 match.
    - `grep -nE "get_prompt_typed" examples/c09_client_list_all.rs` returns at least 1 match.
    - `grep -cE "list_all_tools|list_all_prompts|list_all_resources|list_all_resource_templates" examples/c09_client_list_all.rs` returns at least 4 — **OR** the file contains an explicit `NOTE: list_all_resource_templates intentionally omitted` comment matching `grep -nE "list_all_resource_templates intentionally omitted" examples/c09_client_list_all.rs`.
    - `grep -nE "list_all_resource_templates" examples/c09_client_list_all.rs` returns at least 1 match (either as a function call or as the omission-note text — reviews-driven coverage requirement).
    - `grep -nE "# How to run|not self-contained|compile-only|demo-only" examples/c09_client_list_all.rs` returns at least 1 match (runnability docs — MEDIUM finding #4).
    - `grep -nE "with_client_options" examples/c09_client_list_all.rs` returns at least 1 match.
    - `grep -nE "call_tool_typed" examples/c02_client_tools.rs` returns at least 1 match.
    - `grep -nE "^name = \"c09_client_list_all\"" Cargo.toml` returns 1 match.
    - `grep -nE "required-features = \[\"full\"\]" Cargo.toml | head -20` contains at least one match within the c09 stanza (use awk-scoping if necessary: `awk '/^name = "c09_client_list_all"/,/^\[\[/' Cargo.toml | grep -c "required-features"` returns at least 1).
    - `grep -nE "c09_client_list_all" examples/README.md` returns at least 1 match.
    - **`examples/README.md` client-example count updated** — executor records the before/after count in SUMMARY.md; a grep-checkable proxy is: `git diff examples/README.md` (to be inspected during review) must show either a count bump OR a table row addition beyond the c09 block itself.
    - `cargo check --example c09_client_list_all --features full` exits 0.
    - `cargo check --example c02_client_tools --features full` exits 0.
  </acceptance_criteria>
  <done>c09 exercises all four `list_all_*` helpers (or documents the templates omission), has runnability docs, is registered in Cargo.toml with `required-features = ["full"]`, and is added to examples/README.md with an updated client-example count. c02 uses `call_tool_typed`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bump pmcp 2.5.0 → 2.6.0 across all 8 pin lines in 7 Cargo.toml files</name>
  <read_first>
    - Cargo.toml (root, lines 1-10 — confirm current `version = "2.5.0"` and general `[package]` layout)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-PATTERNS.md (§9 — verbatim grep result listing ALL 8 pin lines in 7 files)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-RESEARCH.md (§"Standard Stack — Installation" — confirms bump scope)
    - CLAUDE.md §"Version Bump Rules" — confirms downstream pin-bumps are mandatory when pmcp bumps
    - Cargo.lock (do not edit by hand; cargo regenerates on next build)
  </read_first>
  <behavior>
    - Root `Cargo.toml` line 3: `version = "2.5.0"` → `version = "2.6.0"`.
    - All 8 downstream pin lines listed in the Interfaces block swap `"2.5.0"` → `"2.6.0"` (one edit per line; do not touch `path`, `features`, or `default-features` attributes).
    - `cargo build --workspace --all-features` succeeds, regenerating `Cargo.lock` with the new version.
    - After the bump, `grep -rE 'pmcp = \{ version = "2\.5\.0"' --include=Cargo.toml` across the workspace returns zero matches.
  </behavior>
  <action>
**Step 1 — Bump root version.** In `Cargo.toml` (root, line 3):

```toml
# BEFORE
version = "2.5.0"
# AFTER
version = "2.6.0"
```

**Step 2 — Bump all 8 downstream pins.** Apply the substitution `"2.5.0"` → `"2.6.0"` to each of these lines (re-read the file first; line numbers may drift):

1. `examples/25-oauth-basic/Cargo.toml:24`
2. `cargo-pmcp/Cargo.toml:38`
3. `examples/test-basic/Cargo.toml:13`
4. `crates/pmcp-tasks/Cargo.toml:10`
5. `crates/pmcp-tasks/Cargo.toml:32`
6. `crates/pmcp-server/Cargo.toml:30`
7. `crates/pmcp-server/pmcp-server-lambda/Cargo.toml:17`
8. `crates/mcp-tester/Cargo.toml:21`

Only change the quoted version string. Preserve every other attribute, feature flag, and the `path = "..."` part verbatim.

**Step 3 — Let cargo regenerate `Cargo.lock`.** Do NOT hand-edit the lockfile.

**Step 4 — Verify.** After all edits, run:

```bash
grep -rnE 'pmcp = \{ version = "2\.5\.0"' --include=Cargo.toml .
```

It must return zero matches (outside of `.planning/`, `target/`, `.git/`).

If a pin line mentioning `2.5.0` resurfaces that was NOT in the 8-line list above, investigate — it may be a new pin added by Phase 74; bump it and note in SUMMARY.md.
  </action>
  <verify>
    <automated>cargo build --workspace --all-features 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^version = "2\.6\.0"' Cargo.toml` returns 1 match at line 3.
    - `grep -rnE 'pmcp = \{ version = "2\.5\.0"' --include=Cargo.toml .` returns zero matches across the workspace (excluding `.git/`, `target/`, `.planning/`).
    - `grep -rnE 'pmcp = \{ version = "2\.6\.0"' --include=Cargo.toml . | wc -l` returns 8 (the 8 pin lines now updated).
    - `cargo build --workspace --all-features` exits 0.
    - `cargo tree -p pmcp | head -1` reports `pmcp v2.6.0`.
  </acceptance_criteria>
  <done>All 8 pin lines in 7 files bumped; workspace builds; no `"2.5.0"` pmcp pin remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship docs — CHANGELOG v2.6.0 (with explicit templates mention or omission caveat), REQUIREMENTS.md D-15 doc-fix, README bullet — and run `make quality-gate`</name>
  <read_first>
    - CHANGELOG.md (full — to see existing `## [2.5.0]` entry format, section headings, date convention)
    - README.md (lines 205-230 — Key Features list and Latest Version line to update)
    - .planning/REQUIREMENTS.md (lines 50-60 — to confirm current text of line 55 before doing the replace)
    - examples/c09_client_list_all.rs (from Task 1 — to confirm whether the templates helper is invoked or documented as omitted; CHANGELOG wording must match)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-PATTERNS.md (§10 CHANGELOG analog; §11 README analog)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-REVIEWS.md (MEDIUM #4 — CHANGELOG wording must not overstate what c09 demonstrates)
    - .planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-CONTEXT.md (D-15 — exact rename)
    - CLAUDE.md §"Release & Publish Workflow" — confirms `make quality-gate` is the CI-exact gate
  </read_first>
  <behavior>
    - `CHANGELOG.md` gains a new `## [2.6.0] - YYYY-MM-DD` block above the existing `## [2.5.0]` block, with sections listing all Phase 73 additions + the REQUIREMENTS.md doc-fix under `### Fixed`. The CHANGELOG explicitly mentions `list_all_resource_templates` (either as a shipped helper demonstrated by c09 OR as a shipped helper that c09 intentionally does not demonstrate — wording must match what c09 actually does).
    - `.planning/REQUIREMENTS.md:55` has `call_prompt_typed` replaced with `get_prompt_typed` (single, precise one-token substitution).
    - `README.md` has a new "Typed Client Helpers" bullet in the Key Features list.
    - `README.md` Latest Version line reads `pmcp = "2.6"` (or `"2.6.0"` — match neighbouring format).
    - `make quality-gate` passes end-to-end.
  </behavior>
  <action>
**Step 1 — CHANGELOG entry.** Prepend the following block above the existing `## [2.5.0] - 2026-04-21` section in `CHANGELOG.md` (use today's date from the environment — 2026-04-21 or later). Note the explicit `list_all_resource_templates` line; if c09 omits the templates call, add an `(c09 example does not demonstrate this helper — pair with a server that advertises resources/templates/list to exercise it.)` clause to keep the CHANGELOG honest.

```markdown
## [2.6.0] - 2026-04-21

### Added

- **pmcp 2.6.0 — Typed client helpers** (Phase 73, PARITY-CLIENT-01):
  `Client::call_tool_typed<A: Serialize>`, `Client::call_tool_typed_with_task<A: Serialize>`,
  `Client::call_tool_typed_and_poll<A: Serialize>`, and `Client::get_prompt_typed<A: Serialize>`.
  Each serializes caller-provided `&A` via `serde_json::to_value` and delegates to the existing
  untyped sibling method. Serialization failures return `Error::validation` naming the argument
  source. Signatures match the live siblings exactly — `call_tool_typed_with_task` is two-arg;
  `call_tool_typed_and_poll` is three-arg with `max_polls: usize`.
- **pmcp 2.6.0 — Auto-paginating list helpers** (Phase 73, PARITY-CLIENT-01): `Client::list_all_tools`,
  `Client::list_all_prompts`, `Client::list_all_resources`, and
  **`Client::list_all_resource_templates`** (the last uses the distinct `resources/templates/list`
  capability). Each loops on `next_cursor` and returns the full concatenated item list. A bounded
  `max_iterations` safety cap (configured via `ClientOptions::max_iterations`, default 100) returns
  `Error::validation` rather than looping indefinitely on a buggy server. Empty-string cursors
  (`Some("")`) continue the loop; only `None` terminates. Each helper's rustdoc documents the
  memory-amplification caveat.
- **`pmcp::ClientOptions`** — new `#[non_exhaustive]` configuration struct. Constructed via
  `ClientOptions::default()` + field-update syntax. Future client-level knobs can land
  non-breakingly. `max_iterations = 0` is a legal but degenerate value (documented in rustdoc;
  produces immediate `Error::Validation` from every `list_all_*` helper).
- **`Client::with_client_options(transport, options)`** — new constructor for wiring a custom
  `ClientOptions`. Does not collide with the pre-existing
  `Client::with_options(transport, info, ProtocolOptions)`. `ClientBuilder` intentionally does not
  expose a `.client_options()` setter in this release — builder-level parity is tracked for a future
  phase.
- **`examples/c09_client_list_all.rs`** — end-to-end demo exercising `Client::with_client_options`,
  `call_tool_typed`, `get_prompt_typed`, and all four `list_all_*` helpers (including
  `list_all_resource_templates`). Drives an MCP server over stdio — see the file header for pairing
  instructions.
- **`examples/c02_client_tools.rs`** updated to showcase `call_tool_typed` with a
  `#[derive(Serialize)]` struct instead of the prior hand-rolled `json!({...})` pattern.

### Fixed

- **REQUIREMENTS.md §55** — renamed `call_prompt_typed` to `get_prompt_typed` to match the
  MCP method name (`prompts/get`) and the shipped helper name (Phase 73 D-15).
```

If c09 omits the templates helper (see Task 1 intentional-omission path), the `examples/c09_client_list_all.rs` bullet above should read: `...and three of the four list_all_* helpers; list_all_resource_templates is intentionally omitted because <reason — copied verbatim from the c09 header comment>.`

**Step 2 — REQUIREMENTS.md doc-fix (D-15).** In `.planning/REQUIREMENTS.md:55`, perform a single-token replacement: `call_prompt_typed` → `get_prompt_typed`. Preserve everything else on the line.

**Step 3 — README.md bullet + version line.** In the Key Features list in `README.md` (around line 211-222), add:

```markdown
- **Typed Client Helpers**: `call_tool_typed`, `get_prompt_typed`, and auto-paginating `list_all_*` with bounded safety cap
```

Place it between `- **Tower Middleware** ...` and `- **Performance** ...` (preserve topical order).

Update the Latest Version line from `**Latest Version:** \`pmcp = "2.0"\`` to `**Latest Version:** \`pmcp = "2.6"\``. If the file does not currently read `"2.0"`, update whatever is there to `"2.6"`.

**Step 4 — Run `make quality-gate`.** This is the CI-equivalent gate per CLAUDE.md §"Release & Publish Workflow". It MUST pass before commit. Common failure modes:

- clippy pedantic lints on the new helpers → tweak implementation (may require going back to Plan 01 Task 3 style decisions).
- doctest compilation failures → check `#[derive(Serialize)]` import paths.
- workspace test failure due to a downstream crate's dep-pin issue → verify all 8 pins were bumped correctly.
- cargo-audit advisory on Cargo.lock — only a blocker if it's a NEW advisory.
  </action>
  <verify>
    <automated>make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^## \[2\.6\.0\]" CHANGELOG.md` returns 1 match, above any `## [2.5.0]` line.
    - `grep -nE "list_all_resource_templates" CHANGELOG.md` returns at least 1 match (MEDIUM finding #4 — CHANGELOG must explicitly name the templates helper, whether as demonstrated or as intentionally omitted).
    - `awk '/^## \[2\.6\.0\]/,/^## \[2\.5\.0\]/' CHANGELOG.md | grep -cE "call_tool_typed|get_prompt_typed|list_all_tools|list_all_resource_templates|ClientOptions|with_client_options|c09_client_list_all"` returns at least 6 (all the new named items appear in the 2.6.0 section).
    - `grep -nE "get_prompt_typed" .planning/REQUIREMENTS.md | head -5` returns at least 1 match on or near line 55.
    - `grep -nE "call_prompt_typed" .planning/REQUIREMENTS.md` returns zero matches (D-15 fully applied).
    - `grep -nE "Typed Client Helpers" README.md` returns 1 match.
    - `grep -nE 'pmcp = "2\.6' README.md` returns 1 match.
    - `make quality-gate` exits 0 (all of: fmt check, clippy pedantic+nursery, workspace build, workspace tests, cargo-audit, example builds).
    - `cargo test --doc -p pmcp --features full` passes.
    - `cargo run --example c09_client_list_all --features full` compiles and runs to the point of attempting stdio I/O (acceptable: blocks / exits with stdio read behaviour — we just want to confirm the binary is runnable; document in SUMMARY.md).
  </acceptance_criteria>
  <done>CHANGELOG + REQUIREMENTS + README updated with explicit templates mention; `make quality-gate` green; release-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced by Plan 03. This plan is docs + version-bump + example ergonomics — all additive.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-P3-01 | Tampering | Downstream pin-bump miss (one of 8 files not updated) → mixed-version workspace, heisenbugs at runtime | mitigate | Grep-verified task acceptance criterion `grep -rE 'pmcp = \{ version = "2\.5\.0"'` returns zero across the workspace after bump. `cargo build --workspace` must succeed; any miss surfaces immediately. |
| T-73-P3-02 | Repudiation | CHANGELOG drift (feature shipped but not documented, or documented but not shipped) | mitigate | CHANGELOG `### Added` block explicitly names `list_all_resource_templates` — matching the shipped surface. If c09 omits the helper, CHANGELOG matches that omission verbatim. Acceptance criterion greps for the explicit templates mention. |
| T-73-P3-03 | Denial of Service | `make quality-gate` skipped → post-merge CI red | mitigate | Task 3 verify step runs `make quality-gate` explicitly. CLAUDE.md pre-commit hook enforces it on commit. Cannot be bypassed without `--no-verify`, which is prohibited. |
| T-73-P3-04 | Information Disclosure | Example c09 appears runnable (README wording) but actually requires a paired stdio server | mitigate | File-header `# How to run` block explicitly states pairing requirements. `examples/README.md` entry mirrors the caveat. Users cannot be misled into thinking `cargo run --example c09_client_list_all` will produce meaningful output unpaired. |

No new runtime threat surface. T-73-01 (malicious cursor chain DoS) is mitigated entirely in Plan 02.
</threat_model>

<verification>
- `cargo check --example c09_client_list_all --features full` — green.
- `cargo check --example c02_client_tools --features full` — green.
- `cargo build --workspace --all-features` — green.
- `grep` acceptance criteria in each task all hold.
- `make quality-gate` — green end-to-end, matching CI exactly.
- CHANGELOG v2.6.0 entry is above v2.5.0 entry, with a date, AND explicitly names `list_all_resource_templates`.
- REQUIREMENTS.md has no remaining `call_prompt_typed` mentions.
- README.md reflects 2.6 Latest Version + Typed Client Helpers bullet.
- `examples/README.md` client-example count reflects c09's addition.
- `examples/c09_client_list_all.rs` has a header `# How to run` block documenting stdio pairing requirements.
</verification>

<success_criteria>
- New example `c09_client_list_all.rs` compiles, is registered in `Cargo.toml` with `required-features = ["full"]` AND in `examples/README.md` with updated count, exercises all four `list_all_*` helpers (or documents the templates omission in both the source-file header and the CHANGELOG), and has a `# How to run` header block documenting the stdio-pairing requirement.
- Existing `c02_client_tools.rs` demonstrates `call_tool_typed`.
- pmcp version 2.6.0 is live across all 8 pin lines in 7 Cargo.toml files; workspace builds.
- CHANGELOG has a complete v2.6.0 entry with an explicit `list_all_resource_templates` mention.
- REQUIREMENTS.md §55 reads `get_prompt_typed` (D-15 doc-fix applied; old mention is gone).
- README Key Features reflects the new capability; Latest Version line is `"2.6"`.
- `make quality-gate` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/73-typed-client-helpers-list-all-pagination-parity-client-01/73-03-SUMMARY.md` summarizing:
- Example files created/modified + line counts + `cargo check` result.
- Explicit mention of whether c09 invokes `list_all_resource_templates` OR omits it with a documented reason.
- All 8 pin-bump lines before/after + lockfile regeneration confirmed.
- CHANGELOG entry excerpt (particularly the `list_all_resource_templates` line).
- REQUIREMENTS.md diff (the one-token rename).
- README diff (the one bullet + the version line).
- `examples/README.md` client-example count before/after.
- `make quality-gate` full output tail (last ~30 lines) to confirm green.
- Any deviations (e.g., an additional pin line discovered that was not in the original 8-line list).
- Release-ready state: note whether pmcp 2.6.0 tag is ready to be cut by the operator (`git tag -a v2.6.0`).
</output>
