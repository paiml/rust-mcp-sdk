---
phase: 116-auth-hardening-seps
plan: 15
type: execute
wave: 10
depends_on: ["116-13", "116-14"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/116-auth-hardening-seps/deferred-items.md
  - contracts/binding.yaml
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Every phase gate has been run at HEAD and classified as REQUIRED-GREEN or ACCEPTED BASELINE DELTA before anything is booked"
    - "AUTH-01, AUTH-02 and AUTH-03 are booked against their AMENDED requirement text, each citing an artifact plus a named test binary and a parsed non-zero count"
    - "The eight contract bindings authored ahead of implementation in 116-01 are resolved against real source and flipped from planned to implemented"
    - "Every item this phase did not do has a written home with a named owner"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUTH-01/02/03 booked with cited evidence against the amended text, and the requirement-index rows updated"
      contains: "AUTH-01"
    - path: ".planning/phases/116-auth-hardening-seps/deferred-items.md"
      provides: "the two-class gate results, the contract-first closure, and every deferral, refinement and named-owner item this phase produced or inherited"
      min_lines: 110
    - path: "contracts/binding.yaml"
      provides: "the Phase 116 bindings flipped from planned to implemented, each resolved against a real fn"
      contains: "status: implemented"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/116-auth-hardening-seps/116-BASELINES.md"
      via: "every booked number is a delta against a recorded baseline"
      pattern: "b2bf9157"
    - from: "contracts/binding.yaml"
      to: "src/shared/oauth_validation.rs"
      via: "each binding's function: resolved against a real fn before the status flip"
      pattern: "validate_authorization_response"
---

<objective>
Run every phase gate at HEAD, classify each result under an explicit two-class acceptance policy,
close the contract-first loop by resolving the bindings 116-01 authored, and only then book
AUTH-01/02/03 — each citing an artifact, a named test binary and a parsed count (D-20).

Purpose: `D-115-G` (booking ahead of evidence) recurred TWICE on a single requirement in Phase 115,
and this project's roadmap warns in as many words against inheriting `[~]` by habit.

**One policy change from the previous revision, forced by cross-AI review (Codex HIGH #9).** The
earlier plan said "if ANY gate is red, STOP" while also asserting that `make doc-check` stays red at
28 pre-existing errors this phase neither caused nor will clear. Those two statements cannot both
hold, and a plan containing both is not executable — the executor either stops forever or ignores the
rule. This revision states the acceptance policy as two explicitly named classes so a reader can tell,
per gate, what "acceptable" means and why.

**A second change:** AUTH-01 and AUTH-03 were AMENDED in commit `0aebf7f6`. The bookings are now
against the amended text, so they no longer have to narrate a gap between wording and implementation
— that gap was closed in the requirement itself.
Output: three bookings a future reader can re-derive, eight resolved contract bindings, and a
deferred-items register with no orphans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-auth-hardening-seps/116-CONTEXT.md
@.planning/phases/116-auth-hardening-seps/116-RESEARCH.md
@.planning/phases/116-auth-hardening-seps/116-BASELINES.md
@.planning/phases/116-auth-hardening-seps/116-REVIEWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run every phase gate at HEAD and classify each under the two-class acceptance policy</name>
  <files>.planning/phases/116-auth-hardening-seps/deferred-items.md</files>
  <read_first>
    - .planning/phases/116-auth-hardening-seps/116-BASELINES.md (READ IN FULL — every number this task produces a delta against, the `### doc-check ACCEPTED BASELINE DELTA ANCHOR` table, the standard verification snippet in item 7, and the RESEARCH assumption A2 item 116-01 left open)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § Pitfalls 3, 4, 5, 7, 8, 9 (why each obvious gate command is insufficient, and which two `make` targets are no-op stubs)
    - .planning/phases/116-auth-hardening-seps/116-REVIEWS.md § Codex "HIGH — The final booking gate cannot complete as written"
    - CLAUDE.md § "Quality Gate Enforcement" and § "ALWAYS Requirements for New Features"
    - Makefile lines 400-430 (`doc-check`) and lines 673-695 (`quality-gate` — confirm for yourself that it does NOT chain `doc-check`; that is what makes the two classes independent rather than contradictory)
  </read_first>
  <action>
Write a `## Phase-End Gate Results` section into `deferred-items.md` (creating the file). Open it with
the acceptance policy, stated BEFORE any result, so the classification cannot be chosen after seeing
the numbers.

**Class A — REQUIRED-GREEN.** These must exit 0. Any red here STOPS the phase; do not proceed to
Task 2 or Task 3. Booking a requirement on a red Class-A gate is precisely the failure `D-115-G`
records twice.
  A1 `make quality-gate`
  A2 `cargo nextest run --features full,oauth`, plus a non-zero PARSED count per binary
  A3 `cargo clippy --features full,oauth --lib --tests` with `make lint`'s full flag set
  A4 `pmat quality-gate --fail-on-violation --checks complexity`
  A5 `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157`
  A6 `make wasm-build` after `rustup target add wasm32-unknown-unknown`
  A7 `make check-todos`
  A8 `make test-examples` plus the two `cargo run --example c11_oauth_iss_state_validation` invocations
  A9 the two `cargo fuzz` campaigns
  A10 the refined dependency fence, including the `Cargo.lock` diff assertion
  A11 `make comply` plus the contract-binding resolution check (Task 2)

**Class B — ACCEPTED BASELINE DELTA.** Exactly ONE gate is in this class: `make doc-check`. It was
ALREADY red at the phase base with 28 `^error` lines, none in files this phase touches
(`116-BASELINES.md` § doc-check ACCEPTED BASELINE DELTA ANCHOR); it is recorded as `D-113-W` /
`D-114-V` with owner UNASSIGNED; and clearing it is not this phase's scope. `make quality-gate` does
NOT chain it (Makefile:673-694), so Class A can be green while Class B is red without contradiction.

Its acceptance condition is a DELTA, not zero, and has two parts, BOTH of which must hold:
  B1 the total `^error` count at HEAD is LESS THAN OR EQUAL TO the recorded anchor; and
  B2 ZERO `^error` lines occur in any file this phase created or modified.
If either fails, `make doc-check` is treated as Class A red and the phase STOPS. Write this rule
verbatim into the record before running it, so "acceptable" cannot be decided retroactively.

Nothing else may be added to Class B. A gate that turns red for a new reason is Class A red.

Then record each gate as a numbered entry naming the command, the exit code, the number, and the
delta against `116-BASELINES.md`. Use `&&` between commands and `set -o pipefail` before any pipeline
— a `;` separator or a bare `| tail` reports the wrong status, which cross-AI review flagged as a
failure-masking pattern in the previous revision's own verify blocks.

1. **A1 `make quality-gate`** — exit code and the passed/failed/ignored counts. This closes RESEARCH
   assumption A2, which 116-01 carried rather than measured. If it fails on the known macOS-keychain
   live-HTTP flake (`D-115-AL`: `streamable_http.rs:458` native-roots `.expect` with
   `Os { code: -36 }`), re-run the WHOLE gate and record BOTH runs — do not normalize a failure away
   silently.
2. **A2 per-binary counts.** For every binary this phase created or extended, run the standard
   snippet from `116-BASELINES.md` item 7 and record the PARSED count:
   `oauth_iss_validation`, `oauth_discovery_urls`, `oauth_application_type`, `oauth_credential_store`,
   `oauth_credential_file`, `oauth_discovery_validation`, `oauth_provider_discovery`,
   `oauth_state_csrf`, `oauth_iss_integration`, `oauth_dcr_integration`, `oauth_store_wiring`,
   `oauth_refresh`, `v2_bounded_reads_tripwire`, plus cargo-pmcp's `binary(auth_integration)` (and
   `binary(cargo_pmcp) and test(auth_cmd)` if 116-13 added inline unit tests). Every count must be
   NON-ZERO and PARSED from the `Summary [...] N tests run` line — never inferred from a `tail`. A
   mis-typed selector exits 0 having run nothing, and that defect hit Phase 114 seven times. Use
   `binary(...)` for file selection, never a bare `test(...)`: measured in this repo, `test(auth)`
   skips 4 of `auth_integration.rs`'s 7 tests — including the load-bearing
   `logout_no_args_errors_via_cli` — while sweeping in two unrelated files, so it can report a
   healthy non-zero count having run none of the tests AUTH-03's booking depends on. `test(...)` is
   permitted only inside the compound `binary(X) and test(Y)` form.
3. **A3 clippy** with `-D clippy::all -W clippy::pedantic -W clippy::nursery -W clippy::cargo`.
   `make lint` alone uses `--features "full"` and compiles ZERO lines of `src/client/oauth.rs`; this
   run is what actually covers the phase's code.
4. **A4 `pmat quality-gate --fail-on-violation --checks complexity`** — must be 0 violations. Also
   record whether any `#[allow(clippy::cognitive_complexity)]` was added anywhere in the phase; the
   answer should be none.
5. **A5 `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157`** — must exit 0 now that
   116-13 bumped the minor. Name the baseline in the recorded command. State separately that the
   crates.io 2.17.0 comparison carries a PRE-EXISTING `#[deprecated]` failure on
   `OptimizedSseTransport` that is not this phase's.
6. **B1/B2 `make doc-check`** — total `^error` count and per-file distribution, evaluated against BOTH
   acceptance conditions and recorded as a table alongside the anchor's table so the delta is visible
   per file, with a stated PASS or FAIL for each of B1 and B2. State the sentence: the phase neither
   caused nor cleared the pre-existing errors. `make doc-check` is the ONLY gate whose feature list
   includes `oauth`, so it is the only place this phase's new rustdoc is compiled at all — which is
   why B2 is a REQUIRED half of the condition and not a nicety.
7. **A6 `make wasm-build`** after `rustup target add wasm32-unknown-unknown` — exit code and warning
   count, plus confirmation that the CI job 116-05 added is present in `gate`'s `needs:` array.
8. **A7 `make check-todos`** — must pass; `grep -rn 'TODO\|FIXME\|HACK\|XXX' src/ cargo-pmcp/src/`
   must return nothing.
9. **A8 examples** — `make test-examples` plus `cargo run --example c11_oauth_iss_state_validation`
   both with and without `--features full,oauth`.
10. **A9 fuzz** — the campaign result for BOTH targets (`oauth_authorization_response` and
    `oauth_credential_and_dcr`): run count and artifacts-dir state, carried from 116-08. Note that
    `fuzz` is in the workspace `exclude` array (`D-115-AB`), so `make quality-gate` covers nothing
    under `fuzz/` — the campaign result is separate evidence.
11. **A10 the refined dependency fence** from 116-13, with its output, INCLUDING the `Cargo.lock`
    diff assertion.

Do NOT cite `make check-unwraps` or `make unused-deps` as evidence for anything: both are no-op stubs
(`Makefile:768-772` and `:202-206`) that print success unconditionally. If either appears in the gate
transcript, note in the record that it proves nothing.
  </action>
  <verify>
    <automated>mkdir -p target/116-verify &amp;&amp; set -o pipefail &amp;&amp; make quality-gate &amp;&amp; cargo nextest run --features full,oauth 2>&amp;1 | tee target/116-verify/full-sweep.log &amp;&amp; grep -qE 'Summary \[.*\] [1-9][0-9]* tests? run' target/116-verify/full-sweep.log &amp;&amp; pmat quality-gate --fail-on-violation --checks complexity &amp;&amp; cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157 &amp;&amp; grep -q 'Class B — ACCEPTED BASELINE DELTA' .planning/phases/116-auth-hardening-seps/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` opens `## Phase-End Gate Results` with the two-class acceptance policy, naming Class A's eleven members and Class B's single member with its two-part condition B1 and B2, BEFORE any measured number appears in the file
    - All eleven numbered result entries are present, each naming its exact command and exit code
    - The per-binary table lists all fourteen named binaries with a NON-ZERO count each, obtained with `binary(...)` selectors (bare `test(...)` forbidden; the compound `binary(X) and test(Y)` form is allowed) and PARSED from the `Summary` line — the record shows the parsed number, not a `tail` transcript
    - The cargo-pmcp row names `binary(auth_integration)`, NOT `test(auth)`, and its count is >= 7
    - No recorded command uses a `;` separator, and every pipeline is preceded by `set -o pipefail`
    - `make quality-gate` exit 0 is recorded, closing RESEARCH assumption A2 explicitly by name
    - `pmat quality-gate --checks complexity` records 0 violations and states whether any cognitive-complexity allow attribute was added (expected: none)
    - `cargo semver-checks ... --baseline-rev b2bf9157` records exit 0, and the crates.io pre-existing failure is named separately as not this phase's
    - `make doc-check` is recorded with BOTH B1 and B2 evaluated explicitly, each with a stated PASS or FAIL, and with a per-file table set beside the anchor's
    - `check-unwraps` and `unused-deps` are explicitly noted as no-op stubs that prove nothing
    - No requirement in `.planning/REQUIREMENTS.md` was modified by this task
  </acceptance_criteria>
  <done>Every claim Task 3 will make has a command, an exit code, a parsed number and a stated acceptance class behind it.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the contract bindings authored in Wave 1 and flip them to implemented</name>
  <files>contracts/binding.yaml, .planning/phases/116-auth-hardening-seps/deferred-items.md</files>
  <read_first>
    - contracts/binding.yaml (the `# === OAuth Client Hardening (Phase 116) ===` section 116-01 added, with its eight `status: planned` entries and their DESIGNED `signature:` values)
    - contracts/mcp-protocol-sdk-v1.yaml (the three equations 116-01 authored — `oauth_authorization_response_validation`, `oauth_discovery_anchor`, `oauth_credential_binding` — and their `invariants:` lists)
    - .planning/phases/116-auth-hardening-seps/116-BASELINES.md § Contract-First Finding (which contract directories exist, and the `make comply` invocation with its Makefile line number)
    - Makefile lines 813-850 (`comply-bindings-check`, which resolves `function:` values in `contracts/team-servers/binding.yaml` only, and the graceful `comply` target)
    - src/shared/oauth_validation.rs, src/shared/credential_store.rs, src/server/auth/provider.rs (as the phase left them — the real signatures the bindings must now match)
  </read_first>
  <action>
Close the contract-first loop CLAUDE.md opens. 116-01 authored the equations and the bindings BEFORE
implementation; this task verifies the implementation actually satisfies them and records the result.
This is Class A gate A11.

1. **Resolve every `function:`.** For each of the eight Phase-116 entries, confirm the named function
   exists in the crate at the declared `module_path`, with a signature matching the `signature:` value
   authored in Wave 1. Record any DIVERGENCE explicitly: if the shipped signature differs from the one
   the contract committed to, say so, say why, and update the `signature:` value — a contract that
   silently absorbs whatever shipped is not a contract, and the divergence is itself the interesting
   finding. Mirror the team-servers gate's resolution technique, adapted to `src/`: extract each
   `function:` value from the Phase-116 section and assert `grep -rqE "fn <name>\b" src/`. Write the
   exact command you used into the record so a reader can re-run it.
2. **Map every invariant to a fence.** For each invariant listed in the three equations, name the
   test that pins it — test binary plus test name. An invariant with no test named against it is a
   real finding, not a formatting problem. If ANY invariant is unfenced, STOP and escalate rather
   than flipping its binding.
3. **Flip the status.** Change `status: planned` to `status: implemented` for every entry whose
   function resolved AND whose equation's invariants are all fenced. Leave any entry that did not
   resolve at `planned` and record why. Update the section comment with the flip date and this plan's
   id.
4. **Re-run `make comply`** and record its exit code, using the same invocation `116-BASELINES.md`
   named.

Record all four steps under `## Contract-First Closure` in `deferred-items.md`, including the
invariant-to-test mapping table.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('contracts/binding.yaml')); print('yaml ok')" &amp;&amp; test "$(grep -c 'status: planned' contracts/binding.yaml)" = "0" &amp;&amp; make comply &amp;&amp; grep -q '^## Contract-First Closure' .planning/phases/116-auth-hardening-seps/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/binding.yaml` contains ZERO `status: planned` entries; all eight Phase-116 entries read `status: implemented`
    - Every Phase-116 `function:` value resolves to a real `fn` under `src/`, proven by the recorded command and its output
    - Any signature divergence between the Wave-1 designed value and the shipped code is recorded with its reason, and the `signature:` value updated
    - `## Contract-First Closure` contains an invariant-to-test mapping table covering EVERY invariant in all three equations, each naming a test binary and a test name
    - `contracts/binding.yaml` parses as YAML and `make comply` exits 0
    - `git diff contracts/binding.yaml` shows only `status:`, `signature:` and comment changes within the Phase-116 section — no pre-existing binding was touched
  </acceptance_criteria>
  <done>The contract written before the code has been checked against the code, and every equation invariant has a named fence.</done>
</task>

<task type="auto">
  <name>Task 3: Book AUTH-01, AUTH-02 and AUTH-03 on the measured evidence</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 655-700 (READ THIS RANGE — the `### Auth Hardening (AUTH)` section with the three unchecked entries AS AMENDED on 2026-08-03 in commit `0aebf7f6`: AUTH-01's flag-keyed rule, AUTH-03's `(issuer, account, server)` key, its TWO adopted clarifications, and its explicit SEP-2350 out-of-scope sentence. The amended text is AUTHORITATIVE — do not re-derive or re-argue it. Also read the Phase-115 booking immediately above and copy its evidence discipline: cited artifact, named test binary, count, and named limitations)
    - .planning/REQUIREMENTS.md lines 800-850 (the requirement-index rows `AUTH-01 | Phase 116 | Pending`, and the phase-count line — all must be updated in lockstep)
    - .planning/phases/116-auth-hardening-seps/deferred-items.md § "Phase-End Gate Results" and § "Contract-First Closure" (as Tasks 1 and 2 left them)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "Pitfall 6" (the exact scoping AUTH-03's no-oauth2 claim requires)
    - .planning/phases/116-auth-hardening-seps/116-REVIEWS.md § Consensus Summary (the findings this replan answered — cited in the bookings as provenance)
    - .planning/ROADMAP.md § "Phase 116" (the three success criteria the bookings answer)
  </read_first>
  <action>
Book the three requirements `[x]`, each with an evidence block a future reader can re-derive rather
than trust. Run this task ONLY after Task 1 recorded every Class-A gate at exit 0 and Class B passing
BOTH B1 and B2, and after Task 2 resolved every binding.

**AUTH-01.** The requirement now reads: validation is strict whenever the authorization server
advertises `authorization_response_iss_parameter_supported` or emits `iss`; a present-but-mismatched
`iss` is rejected on EVERY era; the v1 leniency tolerates only an ABSENT `iss` from a v1 server that
never emitted one. That is exactly what shipped, so the booking DEMONSTRATES conformance rather than
narrating a gap — the previous revision's "state the realization and why" framing is obsolete, because
commit `0aebf7f6` moved the requirement to meet the code on measured grounds rather than the reverse.

Cite each clause against evidence:
- "strict whenever the AS advertises the flag" → the `IssPresence::Required` rows in
  `binary(oauth_iss_validation)` and the advertised-flag rejection case in
  `binary(oauth_iss_integration)`.
- "or emits `iss`" → the `IssPresence::Optional` + present-and-different row, which returns `Err`.
  Name this one explicitly: it is the clause that makes v1 STRICTLY SAFER than before, since a v1
  deployment whose AS emits `iss` now gets it validated where nothing was checked at all.
- "tolerates only an ABSENT `iss`" → the `Optional` + absent row, which returns `Ok`.
Cite the artifacts `src/shared/oauth_validation.rs`, `src/client/oauth.rs`, `src/error/mod.rs`, and
the parsed counts for `binary(oauth_iss_validation)`, `binary(oauth_state_csrf)`,
`binary(oauth_iss_integration)` and `binary(oauth_discovery_validation)`.

Name two things the requirement did not ask for and got anyway: the RFC 8414 §3.3 anchor validation
(RESEARCH Pitfall 1 — without it the `iss` check would have been decorative, since the anchor itself
came from an unvalidated document), and the validate-before-respond ordering in the loopback listener,
which means the page the user sees can never claim success for a callback that was rejected.

**AUTH-02.** Cite `src/server/auth/provider.rs`, `src/shared/oauth_validation.rs`,
`src/client/oauth.rs`, and the parsed counts for `binary(oauth_application_type)` and
`binary(oauth_dcr_integration)` (the latter as a before/after: 5 at the phase base). State that
`application_type` is asserted on the WIRE BODY, not on the in-memory struct, and that the semver gate
stayed additive because the value rides the existing `#[serde(flatten)] extra` carrier. Name the one
obligation deliberately NOT adopted: SEP-837's optional retry with an adjusted `application_type` (a
MAY), and why an automatic retry would defeat the "surface a meaningful error" obligation it sits
beside.

**AUTH-03.** The requirement now reads: credential storage keyed by `(issuer, account, server)`, plus
the TWO adopted clarifications SEP-2351 and SEP-2207, without breaking existing v1 OAuth deployments,
and with no `oauth2`/`openidconnect` crates added to the core SDK — with SEP-2350 EXPLICITLY OUT OF
SCOPE. Book against exactly those clauses:
- the three-part key → `binary(oauth_credential_store)`'s cross-issuer test AND its
  two-servers-one-authorization-server collision test, plus `binary(oauth_store_wiring)`'s
  end-to-end collision test and `cargo-pmcp`'s logout-isolation test in
  `binary(auth_integration)`. State plainly WHY the key was
  widened from CONTEXT's original `(issuer, account)`: two MCP servers sharing one authorization
  server and account would otherwise collide, and RFC 8707's audience binding — which would have
  mitigated it — is deferred by owner decision `b2bf9157`. Cite the review provenance
  (`116-REVIEWS.md`, Codex HIGH #1) so a reader can see the change was measured, not stylistic.
- SEP-2351 → `binary(oauth_discovery_urls)`, `binary(oauth_discovery_validation)`,
  `binary(oauth_provider_discovery)`, including the terminal-not-fallback outcome-matrix tests.
- SEP-2207 → `binary(oauth_refresh)`, including the three D-14 defect tests, the granted-scope
  containment property, and the `offline_access`-at-authorization tests in
  `binary(oauth_dcr_integration)`.
- no v1 breakage → the D-01 floor test (absent flag + absent `iss` proceeds) and the untouched
  `stateless()` + `AllowedOrigins::any()` proxy exception.
- no new crate → scope the claim EXACTLY as RESEARCH Pitfall 6 requires: "no new
  `oauth2`/`openidconnect` dependency; `pmcp` core remains oauth2-free; cargo-pmcp's PRE-EXISTING
  direct `oauth2 = 5.0` is confined to `src/deployment/` and is untouched, with zero `oauth2::`
  references under `src/commands/`" — and cite the fence commands. A reviewer WILL grep and find that
  line; an unscoped claim reopens the booking.
Also cite `binary(oauth_credential_file)` for the at-rest safety properties and
`binary(v2_bounded_reads_tripwire)` for D-15's closure.

**SEP-2350 is no longer a limitation of AUTH-03** — the amended text puts it explicitly out of scope,
so record it as a DEFERRAL in Task 4's register and do NOT list it as an AUTH-03 limitation. Listing
an out-of-scope item as a limitation of the requirement it is outside of is what made the previous
booking unbookable.

Record, inside AUTH-03's booking, the three limitations that ARE in scope:
(1) AS-change detection does not use the spec's stated mechanism because RFC 9728 Protected Resource
Metadata is deferred by owner decision; (2) schema-1 cargo-pmcp entries that record no issuer are
dropped rather than guessed, and the migration reports the count; (3) an already-installed cargo-pmcp
0.18.0 hard-errors on `schema_version: 2` and nothing in this repo can change it.

Update the requirement-index rows from `Pending` to the booked state, and reconcile the phase-count
line. All three must move together — a booked requirement with a `Pending` index row is the kind of
inconsistency that reopens a phase.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[x\] \*\*AUTH-0' .planning/REQUIREMENTS.md)" = "3" &amp;&amp; test "$(grep -c 'AUTH-0[123] | Phase 116 | Pending' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; grep -q 'issuer, account, server' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All three AUTH entries read `- [x]` in `.planning/REQUIREMENTS.md`
    - No `AUTH-0N | Phase 116 | Pending` row remains in the requirement index
    - Each booking cites at least one source artifact path AND at least one `binary(...)` name AND a numeric count taken from Task 1's parsed table
    - AUTH-01's booking cites all three clauses of the amended text separately, and names the "or emits `iss`" clause as the one that makes v1 strictly safer
    - AUTH-01's booking names the anchor validation and the validate-before-respond ordering as work the requirement did not ask for
    - AUTH-02's booking states the wire-body assertion and names the non-adopted SEP-837 retry MAY with its reason
    - AUTH-03's booking cites the two-servers-one-authorization-server collision test BY NAME, states why the key was widened, and cites the review provenance
    - AUTH-03's booking scopes the no-oauth2 claim naming `cargo-pmcp/Cargo.toml`, `src/deployment/` and the zero-hits grep under `src/commands/`
    - AUTH-03's booking enumerates exactly the THREE in-scope limitations and does NOT list SEP-2350 among them
    - The amended requirement wording is not altered by this task: `git diff .planning/REQUIREMENTS.md` shows the AUTH bullets changed only from `- [ ]` to `- [x]` plus an ADDED evidence block, with the amended prose byte-identical
    - `git diff` shows `.planning/REQUIREMENTS.md` changed only in the AUTH section, the three index rows and the count line
  </acceptance_criteria>
  <done>A reader who distrusts the bookings can re-derive all three from the cited commands without asking anyone.</done>
</task>

<task type="auto">
  <name>Task 4: Give every deferral and refinement a written home with a named owner</name>
  <files>.planning/phases/116-auth-hardening-seps/deferred-items.md</files>
  <read_first>
    - .planning/phases/116-auth-hardening-seps/deferred-items.md (as Tasks 1 and 2 left it)
    - .planning/phases/116-auth-hardening-seps/116-CONTEXT.md § Deferred Ideas (all nine entries, including the two the owner added on 2026-08-02: RFC 9728 Protected Resource Metadata and the RFC 8707 `resource` parameter, both owner: Guy)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "Decisions Requiring Amendment" (A1-A4), § "Open Questions" (1-5), § "State of the Art" (the DCR deprecation in favor of Client ID Metadata Documents)
    - .planning/phases/116-auth-hardening-seps/116-REVIEWS.md (the full Consensus Summary — every HIGH and MEDIUM finding, so the register can state which were adopted, which were declined and why)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md (the house format for a deferred item: identifier, statement, evidence, owner, status)
  </read_first>
  <action>
Write the phase's deferred-items register. Follow the house format: a stable identifier, a statement
of what was NOT done, the evidence or reasoning, a named owner, and a status. Every entry below must
appear; anything with no owner must say UNASSIGNED rather than being left implicit.

**Owner-deferred, from CONTEXT (2026-08-02):**
- RFC 9728 Protected Resource Metadata discovery — an MCP-spec client MUST that pmcp does not
  implement; it derives the AS from the MCP base URL directly (`src/client/oauth.rs:366-390`). Record
  the NAMED DEPENDENCY: SEP-2352 specifies AS-change detection as "detected via updated protected
  resource metadata", so D-18's detection (116-11) uses the provenance signal available today instead
  of the spec's stated mechanism. Owner: Guy. Needs a roadmap slot.
- RFC 8707 `resource` parameter on authorization and token requests — the other MCP-spec client MUST;
  pmcp sends none (`src/client/oauth.rs:664-672`). Record its SECOND named consequence, discovered by
  cross-AI review: deferring it removed the audience binding that would have prevented two MCP servers
  sharing one authorization server from colliding, which is why the credential key was widened to
  `(issuer, account, server)` instead (D-116-R1). Ships together with the RFC 9728 item. Owner: Guy.
- SEP-2350 step-up scope accumulation — deferred WHOLE, both halves together, and now EXPLICITLY OUT
  OF SCOPE for AUTH-03 per the amended requirement text (`0aebf7f6`). Record RESEARCH's correction to
  the rationale: the client half CAN be triggered by a non-pmcp server, so the deferral stands as a
  coherence decision (ship both halves as one feature) rather than as "nothing would trigger it".
- `UpstreamAuthDecorator` + `HEADER_UPSTREAM_AUTH` SDK extraction; the outbound-OAuth vending core
  (`OutboundOAuthCore`, ~987 lines, and note that `docs/design/agents-teams-sdk-extraction-plan.md`
  Phases A-F contain NO phase for it, so it needs a roadmap slot rather than an assumption that Phase
  117 absorbs it); the Cognito internal/external fallback chain; token-at-rest encryption in core;
  typed accessors for the other RFC 7591 fields `DcrResponse` drops into `extra`.

**Owner decisions taken in response to cross-AI review (record as ADOPTED, with provenance):**
- D-116-R1 — the credential key widened from `(issuer, account)` to `(issuer, account, server)`,
  superseding CONTEXT's D-07 and amended into AUTH-03 by `0aebf7f6`. Provenance: `116-REVIEWS.md`
  Codex HIGH #1.
- D-116-R2 — `CredentialStoreAdmin` added alongside `CredentialStore` because the five originally
  declared methods could not support `auth status`, `auth logout --all`, accurate deletion counts or
  migration reporting. Provenance: Codex HIGH #2.
- D-116-R3 — AUTH-01 and AUTH-03 requirement text amended (`0aebf7f6`). Provenance: Codex HIGH #4
  and #5.
- Callback validation moved INSIDE the listener, before the response is committed (Codex HIGH #3).
- `offline_access` moved to the authorization request, recorded only if granted, never introduced at
  refresh (Codex HIGH #6).
- The discovery outcome matrix, with anchor mismatch, over-cap body and malformed security metadata
  TERMINAL, plus the per-issuer candidate-index cache folding in Gemini's latency finding
  (Codex HIGH #7 + Gemini risk 1).
- The OAuth contract equations authored in Wave 1 and resolved in Wave 10 (Codex HIGH #8).
- The two-class gate acceptance policy replacing the self-contradictory "any gate red" rule
  (Codex HIGH #9).
- The tripwire anti-vacuity control run in the meaningful direction, with full relative paths
  (Codex MEDIUM).

**Review findings DECLINED, with reasons — record these too, so a future reader does not re-raise them:**
- Codex LOW: "the phase is very broad, ten serial waves". Declined by owner: the wave structure is
  dependency-derived, and collapsing it would put unrelated file sets in one wave.
- Codex suggestion: "split credential-store and cargo-pmcp migration into a separate phase". Declined
  by owner: the key was widened and the store stays in this phase. The context cost of D-116-R1/R2 was
  absorbed by adding plan 116-16 within the existing wave structure instead.

**Amendments this phase adopted from RESEARCH (record the fact, not a deferral):**
- A1 — the RFC 9207 flag did NOT go on `OidcDiscoveryMetadata`; it rides a new
  `AuthorizationServerExtras` sibling type returned by `discover_with_extras`, because a field would
  be a MAJOR semver break.
- A2 — the markers ride `Error::Protocol`, not `Error::Authentication`, which has no `data` member.
- A3 — SEP-2351 landed as an ORDERED PROBE (not an append-to-insert swap, which was measured to 404
  Microsoft Entra ID), and SEP-2207's actual content (`grant_types` + `offline_access`) landed IN
  ADDITION to D-14's three defects.
- A4 — D-18 branches on credential provenance: warn-and-re-register for DCR credentials, a typed
  `reauth_required` error for pre-registered ones.

**Refinements and known limitations:**
- D-17 refinement: schema-1 cargo-pmcp entries that record NO issuer cannot be re-keyed without
  guessing (which SEP-2352 forbids) and are dropped with a named report entry and a reported count.
  State this against D-17's "every existing login is preserved" wording explicitly. Note the good
  news the widened key brought: the v1 map key WAS the normalized server URL, so the third key
  component is populated losslessly from data v1 already recorded.
- Forward-compat trap: an already-installed `cargo-pmcp` 0.18.0 hard-errors on `schema_version: 2`
  (`cache.rs:74-80`) with an "upgrade cargo-pmcp" message. Already actionable; nothing in this repo
  can change an installed binary. Released-behavior note. Owner: cargo-pmcp release notes.
- The file store's advisory lock is COOPERATIVE and its staleness window is 30 seconds — a different
  program writing `oauth-cache.json` directly still clobbers it. Acceptable for a per-user credential
  file; the trait is the answer for a real multi-writer runtime. Owner: UNASSIGNED, informational.
- SEP-837's optional retry with an adjusted `application_type` (a MAY) was deliberately not adopted.
- Client ID Metadata Documents (PR #2858) — the spec now DEPRECATES DCR in favor of CIMD while
  retaining DCR for backcompat. This phase hardens DCR, which remains correct; CIMD support is out of
  scope and needs its own slot. Owner: UNASSIGNED.
- `make doc-check` is RED at HEAD with its pre-existing errors, none in this phase's files, and it
  blocks the org-required `gate`. Recorded as `D-113-W` / `D-114-V`, owner UNASSIGNED. This phase
  NEITHER CAUSED NOR CLEARED IT — repeat the measured B1/B2 delta from Task 1 and cross-reference the
  existing identifiers rather than minting a new one.
- `make check-unwraps` and `make unused-deps` are no-op stubs; the two
  `.duration_since(UNIX_EPOCH).unwrap()` calls in `src/client/oauth.rs` production paths are named as
  a real, uncovered item. Owner: UNASSIGNED.

Cross-reference D-113-V as CLOSED by 116-14, citing the tripwire's zero report and the two controls
that fired, so a reader of the Phase 113 deferred-items file can follow the thread forward.
  </action>
  <verify>
    <automated>test -f .planning/phases/116-auth-hardening-seps/deferred-items.md &amp;&amp; grep -q 'RFC 9728' .planning/phases/116-auth-hardening-seps/deferred-items.md &amp;&amp; grep -q 'RFC 8707' .planning/phases/116-auth-hardening-seps/deferred-items.md &amp;&amp; grep -q 'D-113-V' .planning/phases/116-auth-hardening-seps/deferred-items.md &amp;&amp; grep -q 'D-116-R1' .planning/phases/116-auth-hardening-seps/deferred-items.md &amp;&amp; grep -q 'DECLINED' .planning/phases/116-auth-hardening-seps/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` contains an entry for every item enumerated in the action, each with an identifier, a statement, evidence, an owner and a status
    - The RFC 9728 entry states the NAMED DEPENDENCY on SEP-2352's AS-change detection mechanism and names the owner as Guy
    - The RFC 8707 entry names owner Guy, states it ships with the RFC 9728 item, AND states its second consequence — that its deferral is why the credential key was widened
    - D-116-R1, D-116-R2 and D-116-R3 each appear as ADOPTED with their review provenance cited
    - The two DECLINED review findings are recorded with their reasons, so they are not re-raised
    - The four amendments A1-A4 are each recorded as ADOPTED with a one-line statement of the corrected shape
    - The D-17 refinement is stated explicitly against D-17's "every existing login is preserved" wording, including the note that the v1 map key populated the new server component losslessly
    - The `make doc-check` entry cross-references `D-113-W` / `D-114-V`, mints no new identifier, and repeats the measured B1/B2 delta
    - D-113-V is recorded as CLOSED by 116-14 with the tripwire's zero report and both firing controls cited
    - Every item with no owner says UNASSIGNED explicitly
    - `grep -rn 'TODO\|FIXME\|HACK\|XXX' src/ cargo-pmcp/src/` returns nothing — deferred work lives in this file, never in a source comment
  </acceptance_criteria>
  <done>No item this phase chose not to do, and no review finding it declined, is discoverable only by re-reading the plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor claims → `.planning/REQUIREMENTS.md` `[x]` | A booking is the artifact downstream phases and reviewers trust without re-deriving |
| authored contract → shipped implementation | A contract that silently absorbs whatever shipped constrains nothing |
| this phase's scope → the milestone's remaining spec obligations | Unrecorded gaps become false parity claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-59 | Repudiation | booking a requirement ahead of measured evidence (`D-115-G`, twice in Phase 115) | mitigate | Task 3 runs ONLY after Task 1 records every Class-A gate at exit 0 and Class B passing both B1 and B2, and after Task 2 resolves every binding; each booking cites an artifact, a named `binary(...)` and a parsed count |
| T-116-59a | Repudiation | an acceptance policy that cannot be satisfied, so it is silently ignored | mitigate | The two classes are stated BEFORE any number is recorded, Class B has exactly one member with a two-part condition, and a gate red for a NEW reason is Class A red by rule |
| T-116-60 | Repudiation | a verify block that "passes" having selected zero tests (Phase 114, seven times) | mitigate | Every per-binary count is PARSED from the `Summary` line with a `binary(...)` selector and must be NON-ZERO; `test(/.../)` is forbidden for file selection; no recorded command uses `;` or an unguarded pipeline |
| T-116-60a | Repudiation | a contract equation with no fence, read as though it were enforced | mitigate | Task 2's invariant-to-test mapping table names a test per invariant, and an unfenced invariant blocks the status flip rather than being noted |
| T-116-61 | Repudiation | AUTH-03's "no oauth2 crate" claim falsified by a reviewer's grep | mitigate | The claim is scoped naming `cargo-pmcp/Cargo.toml`, `src/deployment/` and the zero-hit grep under `src/commands/`, with the fence commands cited |
| T-116-62 | Repudiation | booking AUTH-01 or AUTH-03 against superseded wording | mitigate | Both are booked against the text AMENDED in `0aebf7f6`, and an acceptance criterion asserts the amended prose is byte-identical after the booking — the booking adds evidence, it does not re-edit the requirement |
| T-116-63 | Information Disclosure | a deferred spec MUST (RFC 9728 / RFC 8707) read as implemented | mitigate | Both recorded with named owner Guy, with RFC 9728's dependency on SEP-2352's detection mechanism and RFC 8707's dependency on the widened credential key stated in the register AND in 116-11's rustdoc |
| T-116-63a | Repudiation | a declined review finding silently re-raised in a later phase | mitigate | Both DECLINED findings are recorded with their owner reasons in the register |
| T-116-SC | Tampering | cargo installs | mitigate | Phase installed zero packages; the refined dependency fence including `Cargo.lock` is recorded in Task 1 item 11 |
</threat_model>

<verification>
- Every Class-A gate recorded with command, exit code and delta; all green
- Class B (`make doc-check`) recorded with B1 and B2 each explicitly PASS
- All eight Phase-116 contract bindings resolved and flipped to `implemented`, with every equation invariant mapped to a named test
- All three AUTH requirements booked `[x]` with citations against the amended text, and no `Pending` index row remains
- `deferred-items.md` exists with every enumerated entry, both declined findings, and no implicit owner
- `make quality-gate` exits 0 at the final commit
</verification>

<success_criteria>
- The acceptance policy is executable: two named classes, one member in Class B, a two-part condition, stated before the numbers
- AUTH-01, AUTH-02 and AUTH-03 are booked on evidence a stranger can re-derive, against the amended text
- The contract written in Wave 1 has been checked against the code that shipped, with divergences recorded rather than absorbed
- AUTH-03's no-new-crate claim is scoped so a grep confirms rather than contradicts it
- Every deferral, amendment, limitation and declined review finding has a written home with a named owner
</success_criteria>

<output>
Create `.planning/phases/116-auth-hardening-seps/116-15-SUMMARY.md` when done
</output>
</content>
