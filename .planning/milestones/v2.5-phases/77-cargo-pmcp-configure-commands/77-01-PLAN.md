---
phase: 77
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - cargo-pmcp/Cargo.toml
  - cargo-pmcp/CHANGELOG.md
autonomous: true
requirements: [REQ-77-01, REQ-77-02, REQ-77-03, REQ-77-04, REQ-77-05, REQ-77-06, REQ-77-07, REQ-77-08, REQ-77-09, REQ-77-10, REQ-77-11]
must_haves:
  truths:
    - "REQUIREMENTS.md contains REQ-77-01..REQ-77-11 entries with text + traceability rows"
    - "cargo-pmcp Cargo.toml version is 0.11.0"
    - "CHANGELOG.md has [0.11.0] section stub for Phase 77"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      contains: "REQ-77-01"
    - path: "cargo-pmcp/Cargo.toml"
      contains: "version = \"0.11.0\""
    - path: "cargo-pmcp/CHANGELOG.md"
      contains: "## [0.11.0]"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 77 plans"
      via: "REQ-77-XX traceability table"
      pattern: "REQ-77-0[1-9]\\s*\\|\\s*Phase 77"
---

<objective>
Mint REQ-77-01..REQ-77-11 in REQUIREMENTS.md and bump `cargo-pmcp` from 0.10.0 → 0.11.0 with a Phase 77 CHANGELOG stub. This is foundation work — every downstream plan references these REQ-IDs and the version bump must land before any feature commits so the version-check on first task is consistent.

Purpose: Traceability + release-readiness scaffolding. No feature code lands here.
Output: Three modified files (REQUIREMENTS.md, Cargo.toml, CHANGELOG.md). Single commit.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@cargo-pmcp/Cargo.toml
@cargo-pmcp/CHANGELOG.md
@.planning/phases/77-cargo-pmcp-configure-commands/77-CONTEXT.md
@.planning/phases/77-cargo-pmcp-configure-commands/77-RESEARCH.md
@.planning/phases/77-cargo-pmcp-configure-commands/77-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint REQ-77-01..REQ-77-11 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current sections + traceability table; understand existing format used by SDK-DCR-01, CLI-AUTH-01)
    - .planning/phases/77-cargo-pmcp-configure-commands/77-VALIDATION.md (Per-Task Verification Map — copy behavior text VERBATIM into REQ entries)
    - .planning/phases/77-cargo-pmcp-configure-commands/77-CONTEXT.md (D-01..D-13 — REQ entries paraphrase these decisions)
  </read_first>
  <action>
    Add a new section to `.planning/REQUIREMENTS.md` after `### CLI auth subcommand + SDK DCR (Phase 74)` (line 74) and before `## Future Requirements`. Insert a `### CLI configure subcommand (Phase 77)` heading with these 10 entries (use exact text):

    ```markdown
    ### CLI configure subcommand (Phase 77)

    Seeded by Phase 77 cargo-pmcp configure commands research (`.planning/phases/77-cargo-pmcp-configure-commands/77-RESEARCH.md` + `77-VALIDATION.md`). One REQ-ID per Phase 77 testable behavior.

    - [ ] **REQ-77-01**: `cargo pmcp configure {add,use,list,show}` subcommand group ships under a new `configure/` module — each subcommand persists or reads target state and emits stable text/JSON output.
    - [ ] **REQ-77-02**: `~/.pmcp/config.toml` schema is a `#[serde(tag = "type")]` enum with variants `pmcp-run`, `aws-lambda`, `google-cloud-run`, `cloudflare-workers`; per-variant structs use `#[serde(deny_unknown_fields)]` so typos are rejected at parse time.
    - [ ] **REQ-77-03**: `.pmcp/active-target` workspace marker is a single-line plain-text file containing only the active target name; permissive on read (trim+UTF-8 normalize), strict on write.
    - [ ] **REQ-77-04**: `PMCP_TARGET=<name>` env var is the highest-priority target selector and emits a stderr override note when it overrides the workspace marker; the note fires even when `--quiet` is set. `--target <name>` is a new global flag on the top-level `Cli`.
    - [ ] **REQ-77-05**: A header banner is emitted to stderr by every target-consuming command before any AWS API / CDK / upload call; field ordering is fixed (api_url / aws_profile / region / source); banner is suppressible by `--quiet` (except the D-03 PMCP_TARGET override note).
    - [ ] **REQ-77-06**: Field-level precedence at command-execution time is `ENV > explicit --flag > active target > .pmcp/deploy.toml`; verified by property test.
    - [ ] **REQ-77-07**: `configure add` rejects raw-credential patterns (AKIA[0-9A-Z]{16}, ASIA[0-9A-Z]{16}, ghp_*, github_pat_*, sk_live_*, AIza*) with an actionable error pointing the user at AWS profile names / env-var refs / Secrets Manager ARNs.
    - [ ] **REQ-77-08**: `~/.pmcp/config.toml` writes are atomic via `tempfile::NamedTempFile::persist`; on Unix the file is `0o600`, the parent dir `0o700`; concurrent writers are last-writer-wins (no partial file).
    - [ ] **REQ-77-09**: When `~/.pmcp/config.toml` does not exist, `cargo pmcp deploy` and `cargo pmcp pmcp.run upload` behave byte-identically to Phase 76 — no banner about targets, no migration nag, zero touch.
    - [ ] **REQ-77-10**: ALWAYS gates pass: `cargo fuzz run pmcp_config_toml_parser -- -max_total_time=60`, `cargo test -p cargo-pmcp configure::config::proptests`, `cargo test -p cargo-pmcp configure::resolver::proptests::precedence_holds`, `cargo run --example multi_target_monorepo -p cargo-pmcp` all exit 0.
    - [ ] **REQ-77-11**: Banner emission integrates with ALL target-consuming entry points enumerated in 77-RESEARCH §7 (HIGH-2 per 77-REVIEWS.md): `commands/deploy/mod.rs` (8+ AWS-touching sites), `commands/test/upload.rs` (top of `execute` before `auth::get_credentials()`), `commands/loadtest/upload.rs` (same pattern), and `commands/landing/deploy.rs` (lines 69, 215, 334). The OnceLock-guarded `emit_resolved_banner_once` makes duplicate calls within a single process invocation safe.
    ```

    Then append 11 rows to the `## Traceability` table (after the `CLI-AUTH-01 | Phase 74 | Complete` row at line 176) with status `Pending`:
    ```
    | REQ-77-01 | Phase 77 | Pending |
    | REQ-77-02 | Phase 77 | Pending |
    ... through REQ-77-11
    ```

    Update the `**Coverage:**` block at line 178-181: bump v2.1 total from 31 → 42 (add 11 Phase-77 IDs incl. REQ-77-11 from HIGH-2 fix); update last-updated footer at line 189: append `*Last updated: 2026-04-26 — added 11 REQ-77-* IDs seeded by Phase 77 cargo pmcp configure commands research.*`.
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*REQ-77-" .planning/REQUIREMENTS.md | grep -q "^11$" && grep -c "^| REQ-77-" .planning/REQUIREMENTS.md | grep -q "^11$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*REQ-77-" .planning/REQUIREMENTS.md` returns 11
    - `grep -c "^| REQ-77-" .planning/REQUIREMENTS.md` returns 11
    - `grep "REQ-77-01.*Phase 77.*Pending" .planning/REQUIREMENTS.md` returns 1 line
    - `grep -q "### CLI configure subcommand (Phase 77)" .planning/REQUIREMENTS.md` exits 0
    - `grep -q "v2.1 requirements: 42 total" .planning/REQUIREMENTS.md` exits 0
  </acceptance_criteria>
  <done>11 REQ-77-XX entries exist in REQUIREMENTS.md with traceability rows; coverage count incremented; footer dated 2026-04-26.</done>
</task>

<task type="auto">
  <name>Task 2: Bump cargo-pmcp to 0.11.0 + CHANGELOG stub</name>
  <files>cargo-pmcp/Cargo.toml, cargo-pmcp/CHANGELOG.md</files>
  <read_first>
    - cargo-pmcp/Cargo.toml (line 3 — current version "0.10.0")
    - cargo-pmcp/CHANGELOG.md (most recent entry's heading format — copy structure verbatim)
    - .planning/phases/77-cargo-pmcp-configure-commands/77-CONTEXT.md (D-12 deprecation note for `--target` rename)
  </read_first>
  <action>
    1. In `cargo-pmcp/Cargo.toml`, change line 3 EXACTLY:
       - BEFORE: `version = "0.10.0"`
       - AFTER:  `version = "0.11.0"`
       Touch nothing else in the manifest. No new dependencies (regex, dirs, tempfile, toml, proptest, serde all already present per 77-RESEARCH Environment Availability table).

    2. In `cargo-pmcp/CHANGELOG.md`, prepend a new section IMMEDIATELY below the file's title heading and above any existing `## [0.10.0]` (or current most-recent entry):

    ```markdown
    ## [0.11.0] - 2026-04-XX

    ### Added
    - `cargo pmcp configure {add,use,list,show}` command group for managing named deployment targets (dev / prod / staging / …) — modeled on `aws configure`. Targets are defined in `~/.pmcp/config.toml` (typed-per-variant TOML schema with `pmcp-run`, `aws-lambda`, `google-cloud-run`, `cloudflare-workers` variants). A workspace selects a target via `.pmcp/active-target` (single-line marker file). Resolution precedence: `PMCP_TARGET` env > `--target` flag > `.pmcp/active-target` > none.
    - New global `--target <name>` flag on the top-level `Cli` for one-off named-target overrides.
    - Header banner emitted to stderr before each AWS API / CDK / upload call: `→ Using target: <name> (<type>)` + fixed-order field block (api_url / aws_profile / region / source). Suppressible with `--quiet` (except the `PMCP_TARGET` override note, which always fires as a safety signal).
    - `~/.pmcp/config.toml` parser fuzz target (`pmcp_config_toml_parser`) and property tests for atomic-write round-trip and field-precedence resolution.
    - Working example: `cargo run --example multi_target_monorepo -p cargo-pmcp` — demonstrates a monorepo with two sibling servers (one `pmcp-run`, one `aws-lambda`) each carrying their own `.pmcp/active-target`.

    ### Changed
    - **DEPRECATED**: `cargo pmcp deploy --target <type>` (where `<type>` is `aws-lambda`, `cloudflare-workers`, `pmcp-run`, `google-cloud-run`) — renamed to `--target-type <type>`. The old spelling continues to work via `#[arg(alias = "target")]` for one release cycle and will be removed in 0.12.0. The bare `--target <name>` flag now refers to a NAMED target from `~/.pmcp/config.toml`.

    ### Security
    - `configure add` rejects raw-credential patterns (AWS access keys `AKIA[0-9A-Z]{16}`, AWS session keys `ASIA[0-9A-Z]{16}`, GitHub tokens `ghp_*` / `github_pat_*`, Stripe live keys `sk_live_*`, Google API keys `AIza*`) at insertion time with actionable error messages pointing at AWS profile names, env-var references, or Secrets Manager ARNs. `~/.pmcp/config.toml` itself never stores raw secrets — only references.
    - Config writes are atomic (`tempfile::NamedTempFile::persist`); on Unix, file mode is `0o600` and parent directory is `0o700`.
    ```

    Use `2026-04-XX` literally — the actual release date is filled in later by Plan 09.
  </action>
  <verify>
    <automated>grep -q '^version = "0.11.0"$' cargo-pmcp/Cargo.toml && grep -q '^## \[0.11.0\]' cargo-pmcp/CHANGELOG.md && cd cargo-pmcp && cargo build --quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0.11.0"$' cargo-pmcp/Cargo.toml` returns 1
    - `grep -c '^version = "0.10.0"$' cargo-pmcp/Cargo.toml` returns 0
    - `grep -c '^## \[0.11.0\]' cargo-pmcp/CHANGELOG.md` returns 1
    - `grep -q "DEPRECATED.*--target" cargo-pmcp/CHANGELOG.md` exits 0
    - `cd cargo-pmcp && cargo build --quiet` exits 0 (no Cargo.toml syntax errors; project still builds at 0.11.0)
    - `git diff cargo-pmcp/Cargo.toml | grep -c '^[-+]version'` returns 2 (one removed, one added)
  </acceptance_criteria>
  <done>cargo-pmcp version is 0.11.0; CHANGELOG has a stub `[0.11.0] - 2026-04-XX` entry covering Added/Changed/Security; `cargo build` still succeeds.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ↔ git | Doc/version edits committed to repo; no runtime behavior change |
| repo ↔ crates.io | Version bump on Cargo.toml is a release-readiness signal, not a release itself (the actual publish happens via the release workflow on a `v*` tag) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-12-A | Tampering | Cargo.toml accidental dep injection during version bump | accept | task action explicitly says "Touch nothing else in the manifest"; verification grep counts only `^version` lines changed |
| T-77-12-B | Information Disclosure | CHANGELOG leaks unreleased detail before public release | accept | repo is public; CHANGELOG entries are intentional public artifacts; using literal `2026-04-XX` placeholder until release |

</threat_model>

<verification>
- `make quality-gate` passes (no new code; just metadata changes that the build system parses).
- `grep` counts above all match.
- `cargo build` continues to succeed with the new version.
</verification>

<success_criteria>
- 11 REQ-77-XX entries with traceability rows exist in REQUIREMENTS.md.
- cargo-pmcp version is 0.11.0 in Cargo.toml.
- CHANGELOG has a 0.11.0 stub covering Added/Changed/Security.
- `cargo build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/77-cargo-pmcp-configure-commands/77-01-SUMMARY.md` recording: REQ-77-XX IDs minted, version bump confirmation, list of files modified.
</output>
