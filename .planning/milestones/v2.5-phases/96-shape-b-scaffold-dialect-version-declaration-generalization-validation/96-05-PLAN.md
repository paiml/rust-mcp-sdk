---
phase: 96-shape-b-scaffold-dialect-version-declaration-generalization-validation
plan: 05
type: execute
wave: 4
depends_on: [96-03, 96-04]
files_modified:
  - crates/pmcp-workbook-compiler/tests/fixtures/quirks/
  - crates/pmcp-workbook-compiler/src/quirks_reconcile.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
  - crates/pmcp-workbook-runtime/src/scalar_eval.rs
autonomous: true
requirements: [WBEX-02]
must_haves:
  truths:
    - "Each of ~7-9 Excel quirks is asserted as a fast scalar_eval unit test (runtime layer)"
    - "Each named roadmap quirk has >= 1 mini reconcile fixture run through the real penny path (within_tol, TOL=0.01) UNLESS Plan 03 marks it impossible"
    - "Each quirk is specified as {formula, cached Excel oracle, runtime expected, reconcile cell key}"
    - "The half-rounding quirk asserts against excel_round (NOT a naive round) — 1594.925 -> 1594.93"
    - "Production (Enforce) REFUSES the quirk fixture class; only the trusted #[cfg(test)] override accepts it"
    - "The 1900-leap quirk is handled per the Plan-03 spike disposition (with WBEX-02 traceability) and no date functions added"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/quirks_reconcile.rs"
      provides: "mini reconcile fixtures through reconcile::within_tol + a quirk->WBEX-02 traceability map"
      contains: "within_tol"
    - path: "crates/pmcp-workbook-runtime/src/scalar_eval.rs"
      provides: "per-quirk scalar_eval unit assertions"
      contains: "excel_round"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/quirks_reconcile.rs"
      to: "crates/pmcp-workbook-compiler/src/reconcile"
      via: "within_tol penny grade against the cached oracle"
      pattern: "within_tol"
    - from: "crates/pmcp-workbook-compiler/src/quirks_reconcile.rs"
      to: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      via: "authors quirk .xlsx via the shared author"
      pattern: "fixture_author"
---

<objective>
Deliver the WBEX-02 Excel-quirk fixture corpus: ~7-9 quirks (D-09) each encoded in BOTH layers (D-08) —
a fast `scalar_eval` unit test (runtime crate) AND, where numerically expressible, a mini reconcile
fixture (compiler crate) run through the REAL penny-reconcile path (`reconcile::within_tol`, TOL=0.01,
the same mechanism the golden gate uses). This verifies reconcile determinism beyond the single golden
case. The corpus covers the four roadmap-named quirks (1900 leap-year — per the Plan-03 spike
disposition; empty-cell coercion; error propagation; half-rounding boundaries) plus a curated set
(text->number coercion, explicit `#DIV/0!` propagation, a float boundary like 0.1+0.2, negative-value
rounding sign). Each quirk is specified precisely as {formula, cached Excel oracle, runtime expected,
reconcile cell key} so the corpus cannot accidentally test compile-success only.

Purpose: Prove determinism across a corpus of Excel edge cases, not just the single golden — closing
the milestone's correctness story.
Output: ~7-9 quirk .xlsx fixtures + markers, a `quirks_reconcile.rs` `#[cfg(test)]` reconcile harness
(with a quirk->WBEX-02 traceability map), and per-quirk scalar_eval unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-CONTEXT.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-RESEARCH.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-PATTERNS.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-03-SUMMARY.md

<interfaces>
<!-- The reconcile penny path + the scalar_eval test layer. Sources verified in-repo. -->
- crates/pmcp-workbook-compiler/src/reconcile/mod.rs:35-90 — `pub const TOL: f64 = 0.01;`
  `pub fn within_tol(computed: &CellValue, target: &CellValue) -> bool` (NUMBERS ±0.01 penny;
  non-numeric STRUCTURALLY); the `ComparisonMap` builder `.with(cell_key, target)` / `.with_value(...)`.
  Exact-float `==` on money is FORBIDDEN repo-wide — always go through within_tol.
- crates/pmcp-workbook-runtime/src/scalar_eval.rs:262-332 — the existing `mod tests` to extend; the
  quirk-adjacent precedents `div_by_zero_matches_kernel_nan_clamped_to_zero` (:285-300) and
  `error_leaf_short_circuits_above_arithmetic` (:315-332); the `num(n)` / `CellEnv::new().with_value(...)` drivers.
- crates/pmcp-workbook-runtime/src/sheet_ir/rounding.rs:30-60 — `excel_round`/`excel_roundup`/`excel_ceiling`
  the half-rounding quirk asserts against (excel_round(1594.925, 2) == 1594.93, NOT 1594.92).
- crates/pmcp-workbook-compiler/src/fixture_author.rs (Plan 03 — the shared author for each quirk .xlsx,
  via its env-gated generator path so committed quirk fixtures are produced non-mutatingly)
- crates/pmcp-workbook-compiler/SPIKE-1900-leap.md (Plan 03 — the DECIDED disposition for the leap quirk +
  the WBEX-02 traceability note: DAG reconcile fixture / scalar_eval-only / documented limitation)
- crates/pmcp-workbook-compiler/src/reemit_golden.rs:62-72 + :239-252 — the
  compile_workbook_with_fixture_override recipe per quirk AND the production-refusal counter-test model.
- crates/pmcp-workbook-compiler/src/lib.rs — where to declare `#[cfg(test)] mod quirks_reconcile;` (avoid a duplicate mod decl).
<!-- VERIFIED (reviews replan): Codex MEDIUM — quirk semantics that need formula-context definition.
     "Empty-cell coercion" and "text->number coercion" differ by operator/function context, so each
     quirk's <formula> column must pin the exact context (e.g. empty cell + arithmetic `+` coerces to 0;
     empty cell as IF condition coerces to FALSE; text "5" in arithmetic coerces to 5 but in SUM is
     ignored). Specify the context in the per-quirk table, do NOT leave it ambiguous. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scalar_eval unit-test layer for the quirk corpus (runtime crate)</name>
  <files>crates/pmcp-workbook-runtime/src/scalar_eval.rs</files>
  <read_first>
    - crates/pmcp-workbook-runtime/src/scalar_eval.rs (the existing `mod tests` at :262-332 to extend; the
      precedent tests div_by_zero...:285-300 and error_leaf...:315-332; the num()/CellEnv drivers)
    - crates/pmcp-workbook-runtime/src/sheet_ir/rounding.rs:30-60 (excel_round/excel_roundup/excel_ceiling —
      the half-rounding source of truth to assert against, NOT a naive round)
    - crates/pmcp-workbook-compiler/SPIKE-1900-leap.md (the leap-quirk disposition — if scalar_eval-only,
      the numeric assertion goes HERE)
    - 96-CONTEXT.md D-08 (two-layer) + D-09 (the ~7-9 quirks)
    - 96-PATTERNS.md "crates/pmcp-workbook-runtime/src/scalar_eval.rs"
  </read_first>
  <behavior>
    <!-- Codex MEDIUM (verified): each quirk must be a precise tuple, not a vague label. -->
    QUIRK SPEC — define each quirk as {formula (with explicit operator/function CONTEXT), cached Excel
    oracle, runtime expected value, reconcile cell key}. Operator/function context is MANDATORY for the
    coercion quirks:
      - empty-cell coercion: pin the context (e.g. empty + number via `+` -> 0; empty as IF condition -> FALSE).
      - text->number coercion: pin the context (e.g. "5" + 1 via `+` -> 6; numeric-text inside SUM ignored).
    - Test: half-rounding boundary — `assert_eq!(excel_round(1594.925, 2), 1594.93)` AND a negative-value
      sign case via excel_round/excel_ceiling (NOT a naive round).
    - Test: empty-cell coercion (per the pinned context).
    - Test: error propagation (an error leaf short-circuits — mirror error_leaf...).
    - Test: explicit `#DIV/0!` propagation (mirror div_by_zero...).
    - Test: text->number coercion (per the pinned context).
    - Test: float boundary (0.1+0.2 within tol, not exact `==`).
    - Test: negative-value rounding sign.
    - Test: the 1900-leap quirk PER the Plan-03 disposition (if scalar_eval-only: a numeric assertion of
      the serial offset; if DAG/limitation: a doc-comment pointer to SPIKE-1900-leap.md + the reconcile/
      limitation handled in Task 2).
  </behavior>
  <action>
    Extend crates/pmcp-workbook-runtime/src/scalar_eval.rs's `#[cfg(test)] mod tests` with one `#[test]`
    per quirk (D-08 layer 1), each driven by `num(n)` / `CellEnv::new().with_value(...)`, implementing the
    QUIRK SPEC above. Each test's doc-comment MUST state the quirk's {formula+context, oracle, expected}
    tuple so the assertion intent is unambiguous. Aim for ~7-9 quirk assertions total, reusing the existing
    assertion style; the half-rounding test MUST use `excel_round` (NOT a naive round). Handle the 1900-leap
    quirk exactly per SPIKE-1900-leap.md. Do NOT add date functions.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-runtime scalar_eval</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-runtime/src/scalar_eval.rs contains >= 7 new quirk `#[test]` functions
    - each quirk test documents its {formula+context, oracle, expected} tuple; the coercion quirks pin the operator/function context explicitly
    - the half-rounding test asserts excel_round(1594.925, 2) == 1594.93 (uses excel_round, NOT a naive round)
    - the leap-year quirk is handled exactly per SPIKE-1900-leap.md disposition (no DATE/DATEVALUE added anywhere)
    - `cargo test -p pmcp-workbook-runtime scalar_eval` exits 0
  </acceptance_criteria>
  <done>Each quirk has a fast, pinpoint scalar_eval unit assertion with a precise {formula+context, oracle, expected} tuple, asserting against the runtime's rounding/coercion source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: quirk .xlsx fixtures + quirks_reconcile.rs penny-reconcile harness + WBEX-02 traceability map (compiler crate)</name>
  <files>crates/pmcp-workbook-compiler/tests/fixtures/quirks/, crates/pmcp-workbook-compiler/src/quirks_reconcile.rs, crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/fixture_author.rs (Plan 03 — author each quirk .xlsx + its provenance marker via the env-gated generator path)
    - crates/pmcp-workbook-compiler/src/reemit_golden.rs:62-72 (the compile_workbook_with_fixture_override recipe per fixture) + :239-252 (the production-refusal counter-test to mirror for the quirk class)
    - crates/pmcp-workbook-compiler/src/reconcile/mod.rs:35-90 (TOL, within_tol, ComparisonMap builder — the penny path; read how computed values + cached oracle values are RETRIEVED so the harness compares real computed-vs-oracle, not compile-success)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.provenance-override.json (the per-fixture marker shape)
    - crates/pmcp-workbook-compiler/SPIKE-1900-leap.md (the leap-quirk disposition + WBEX-02 traceability — include a reconcile fixture ONLY if disposition (A); otherwise skip with the documented reason)
    - crates/pmcp-workbook-compiler/src/lib.rs (where to declare `#[cfg(test)] mod quirks_reconcile;`)
    - 96-PATTERNS.md "tests/fixtures/quirks/*.xlsx" + "crates/pmcp-workbook-compiler/src/quirks_reconcile.rs" +
      "Penny reconcile (±0.01), never exact-float on money"
  </read_first>
  <action>
    Author the numerically-expressible quirks as tiny `.xlsx` fixtures under
    crates/pmcp-workbook-compiler/tests/fixtures/quirks/ (one per quirk: half-rounding, empty-coercion,
    error-propagation, div0, text-coercion, float-boundary, negative-rounding, and the leap quirk ONLY if
    Plan-03 disposition (A)), each authored via the Plan-03 fixture author (env-gated generator path) with
    genuine Excel identity, each with cached cell values as the oracle, and each paired with a
    `*.provenance-override.json` marker. Create crates/pmcp-workbook-compiler/src/quirks_reconcile.rs as a
    `#[cfg(test)]` module that, for each quirk fixture: copies it into a TempDir, compiles via
    `compile_workbook_with_fixture_override`, then RETRIEVES the executor's computed value AND the cached
    oracle value (specify exactly how each is obtained — e.g. the computed value from the run result at the
    reconcile cell key, the oracle from the cached `<v>`) and asserts they reconcile within `TOL` through
    the REAL `within_tol` penny path (use the ComparisonMap builder; NEVER exact-float `==` on money) — so
    the test cannot pass on compile-success alone (Codex MEDIUM).
    REQUIRE >= 1 reconcile assertion per NAMED roadmap quirk (1900-leap, empty-cell coercion, error
    propagation, half-rounding) UNLESS Plan-03 explicitly marks that quirk impossible (then the
    scalar_eval assertion from Task 1 + the SPIKE traceability note stands in). Add a PRODUCTION-REFUSAL
    SPOT CHECK (Codex suggestion): one shared test proving a quirk fixture is REFUSED by bare
    `compile_workbook` (Enforce) and only passes via the `#[cfg(test)]` trusted override. Add a
    QUIRK->WBEX-02 TRACEABILITY MAP (Codex suggestion): a doc-comment or table at the top of
    quirks_reconcile.rs mapping each of the ~7-9 quirks back to WBEX-02 (which layer(s) cover it, and for
    the leap quirk, the SPIKE disposition). Declare `#[cfg(test)] mod quirks_reconcile;` in lib.rs. If the
    leap quirk is disposition (B)/(C), add a clear comment pointing to SPIKE-1900-leap.md + the scalar_eval
    assertion (Task 1) instead of a reconcile fixture — do NOT add date code. Do NOT edit existing fixtures.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler quirks</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-compiler/tests/fixtures/quirks/ contains the numerically-expressible quirk .xlsx (>= 6) each with a provenance-override.json marker
    - crates/pmcp-workbook-compiler/src/quirks_reconcile.rs exists, gated `#[cfg(test)]`, declared in lib.rs, and grades each by retrieving the real computed value + oracle and comparing via within_tol (no exact-float == on money; cannot pass on compile-success alone)
    - each NAMED roadmap quirk has >= 1 reconcile assertion UNLESS Plan-03 marks it impossible (then the scalar_eval assertion + SPIKE note covers it)
    - a production-refusal spot check proves a quirk fixture is refused by bare compile_workbook (Enforce)
    - a quirk->WBEX-02 traceability map maps each quirk back to WBEX-02 (and the leap quirk to its SPIKE disposition)
    - the corpus total across both layers is ~7-9 quirks (D-09 cap)
    - the leap quirk follows SPIKE-1900-leap.md exactly (reconcile fixture only if disposition A; no date functions)
    - `cargo test -p pmcp-workbook-compiler quirks` exits 0
  </acceptance_criteria>
  <done>Each numerically-expressible quirk is a mini reconcile fixture graded through the real penny path (computed-vs-oracle, not compile-success), each named quirk has >=1 reconcile (or a documented stand-in), a production-refusal spot check guards the quirk class, and a traceability map ties the corpus to WBEX-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quirk fixture .xlsx -> compiler freshness/provenance gate | Test-authored quirk fixtures ride the trusted-fixture override; production must still refuse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-13 | Tampering | a quirk fixture weakening the freshness/reconcile gate | mitigate | Quirk fixtures use the `#[cfg(test)]`-only override (CR-01); a production-refusal spot check proves bare `compile_workbook` (Enforce) refuses the class; reconcile grades every value through within_tol (the unmodified penny path); the gate code is not touched. |
| T-96-14 | Tampering | exact-float `==` smuggled into a money assertion | mitigate | All reconcile assertions go through within_tol (±0.01); exact-float `==` on money is forbidden repo-wide and absent from the harness. |
| T-96-14b | Tampering | a quirk test silently degrading to compile-success only | mitigate | The harness RETRIEVES the real computed value + cached oracle at the reconcile cell key and compares via within_tol — a compile that produced a wrong value FAILS the assertion. |
| T-96-15 | Elevation | the leap quirk forcing date functions into the whitelist | accept-by-design | Disposition fixed in Plan 03 (with WBEX-02 traceability) bounds the quirk to whitelisted ops or a documented limitation; no DATE/DATEVALUE added (the WBDL-01 doc<->const guard would fail otherwise). |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages — no install task -> no legitimacy checkpoint. |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-runtime scalar_eval` green (layer 1).
- `cargo test -p pmcp-workbook-compiler quirks` green (layer 2 penny reconcile + production-refusal spot check).
- `git diff` shows no edits to existing fixtures and no WHITELIST change.
- Corpus count is ~7-9 quirks across both layers (D-09 cap honored); each quirk maps back to WBEX-02 via the traceability map.
</verification>

<success_criteria>
- WBEX-02 satisfied: an Excel-quirk fixture corpus (1900 leap-year, empty-cell coercion, error propagation, half-rounding boundaries + curated additions) verifies reconcile determinism beyond the single golden case, in both the scalar_eval and penny-reconcile layers, with each quirk precisely specified ({formula+context, oracle, expected, reconcile key}), >=1 reconcile per named quirk (or a documented stand-in), and explicit WBEX-02 traceability.
</success_criteria>

<output>
Create `.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-05-SUMMARY.md` when done
</output>
