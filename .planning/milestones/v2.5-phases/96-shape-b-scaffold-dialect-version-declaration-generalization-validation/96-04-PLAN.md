---
phase: 96-shape-b-scaffold-dialect-version-declaration-generalization-validation
plan: 04
type: execute
wave: 3
depends_on: [96-03]
files_modified:
  - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx
  - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.provenance-override.json
  - crates/pmcp-workbook-compiler/src/reemit_loan.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
autonomous: true
requirements: [WBEX-01]
must_haves:
  truths:
    - "A second, non-lighthouse loan workbook compiles end-to-end through the generic compile_workbook driver"
    - "The loan bundle's SERVED get_manifest / tools/list schema reflects the loan's OWN inputs/outputs (NOT tax-calc's), behind the SAME five generic tool names"
    - "The five generic tool names (calculate/explain/get_manifest/diff_version/render_workbook) are UNCHANGED vs tax-calc; only the manifest/schema payload behind them differs"
    - "Loan-specific input/output fields are PRESENT in the served schema AND tax-specific fields are ABSENT"
    - "The loan workbook uses VLOOKUP/INDEX-MATCH/IFERROR/nested-IF/ROUND/CEILING and NO PMT/POWER/exponentiation"
    - "The loan manifest has multiple named outputs with no privileged single headline"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx"
      provides: "the second-workbook generalization fixture (loan/mortgage rate-tier calculator)"
    - path: "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
      provides: "in-crate compile-and-serve proof asserting the loan's OWN served schema (get_manifest/tools/list)"
      contains: "input_schema_for_manifest"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
      to: "crates/pmcp-server-toolkit::workbook::schema"
      via: "input_schema_for_manifest/output_schema_for_manifest over the loaded loan bundle"
      pattern: "input_schema_for_manifest"
    - from: "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
      to: "crates/pmcp-server-toolkit::workbook::GetManifestHandler"
      via: "served get_manifest projection reflects the loan's own inputs/outputs"
      pattern: "GetManifestHandler"
    - from: "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
      to: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      via: "authors loan-calc.xlsx via the shared author"
      pattern: "fixture_author"
---

<objective>
Deliver the WBEX-01 generalization gate: a second, non-lighthouse loan/mortgage example workbook
(D-01) that is deliberately divergent from `tax-calc` — a fixed-cell DAG whose divergence comes from
whitelist-legal lookup families (VLOOKUP / INDEX-MATCH against a rate-tier table, IFERROR guards,
nested-IF tiering, ROUND/CEILING to currency; NO PMT/POWER/exponentiation per D-02). It compiles and
serves end-to-end through the generic SDK path with ZERO per-workbook served/runtime Rust, and its
SERVED `get_manifest` / `tools/list` schema reflects ITS OWN inputs with multiple named outputs and no
privileged single output — proving the manifest-driven §5 fix generalized. The five generic tool NAMES
stay identical to tax-calc; only the manifest/schema payload behind them differs. That difference IS
the generalization proof.

Purpose: This is the generalization proof — the gate that demonstrates a second workbook serves its
OWN schema behind the same five generic tool names with no shared served Rust.
Output: The loan-calc.xlsx fixture + provenance marker, and an in-crate `#[cfg(test)]`
compile-and-serve proof (`reemit_loan.rs`) asserting the loan's OWN SERVED schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-CONTEXT.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-RESEARCH.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-PATTERNS.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-03-SUMMARY.md

<interfaces>
<!-- The compile-and-serve proof + the loan workbook content rules. Sources verified in-repo. -->
- crates/pmcp-workbook-compiler/src/reemit_golden.rs (FULL — clone the structure: golden_dir/place_fixture/
  compile_fixture at :42-72; the seven-member-present check at :124-142; BUNDLE.lock recompute via
  build_bundle_lock at :144-175; the toolkit-load check `structural_eq_check4_loads_via_toolkit` at
  :178-192 which already uses `pmcp_server_toolkit::workbook::{load_bundle, LocalDirSource}`; the
  production-refusal counter-test at :239-252). For loan: assert INTRINSIC structure + ITS OWN SERVED
  schema, NOT equality to a golden (no loan golden exists).
- crates/pmcp-workbook-compiler/src/fixture_author.rs (Plan 03 — the shared author used to produce loan-calc.xlsx)
- `compile_workbook_with_fixture_override(&xlsx, &out_root, "loan-calc", "1.0.0", "proof-approver")`
  (reemit_golden.rs:68) — the trusted-fixture compile.
- crates/pmcp-workbook-compiler/src/lib.rs — `promote_named_outputs` consumes `out_*` named ranges
  (lib.rs:264-269 doc) — the loan workbook must use the `out_*` convention for its named outputs.
- crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs:29-42 — confirms VLOOKUP/INDEX/MATCH/IFERROR/
  ROUND/CEILING/IF are already dispatched (no new runtime code needed).
- crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx — the CONTRAST case (3 inputs / 4 outputs);
  the loan must differ visibly.
<!-- VERIFIED (reviews replan): the SERVED-schema surface the WBEX-01 proof must assert, all reachable
     under the compiler crate's EXISTING dev-dep `pmcp-server-toolkit` with the `workbook` feature
     (Cargo.toml:97). No new dev-dep (no `pmcp` core) is required for this path. -->
- crates/pmcp-server-toolkit/src/workbook/mod.rs — `pub use pmcp_workbook_runtime::{..., WorkbookBundle}`;
  `WorkbookBundle` has PUBLIC `manifest: Manifest` and `cell_map: CellMap` (bundle_loader.rs:90,92).
  `load_bundle(&LocalDirSource::new(&bundle)) -> Result<WorkbookBundle, _>` (already used in reemit_golden).
- crates/pmcp-server-toolkit/src/workbook/schema.rs — `pub mod schema;` (mod.rs:46) exposes:
  `pub fn input_schema_for_manifest(manifest: &Manifest, cell_map: &CellMap) -> Value` (the EXACT served
  per-tool input schema the calculate/explain ToolInfo carry — built from `cell_map.inputs`) and
  `pub fn output_schema_for_manifest(manifest: &Manifest, cell_map: &CellMap) -> Value` (built from
  `cell_map.outputs`). These are the served-schema functions the handlers feed to ToolInfo; calling them
  on the loaded loan bundle asserts the SERVED schema directly without a full server boot.
- crates/pmcp-server-toolkit/src/workbook/handler.rs — the five handler structs each expose a public
  `const NAME: &str` (e.g. `GetManifestHandler::NAME == "get_manifest"`,
  `CalculateHandler::NAME == "calculate"`, etc.) — the generic tool NAMES to assert are UNCHANGED.
  `GetManifestHandler::new(Arc<WorkbookBundle>)` + its `curated_manifest` projection serve the manifest
  reflecting the bundle's own inputs/outputs (handler.rs:327-440).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: author the loan-calc.xlsx fixture (rate-tier DAG, whitelist-legal) + provenance marker</name>
  <files>crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx, crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.provenance-override.json</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/fixture_author.rs (Plan 03 — the author API + provenance-marker helper + the env-gated generator path that writes committed fixtures)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx + tax-calc.provenance-override.json (the
      contrast fixture + the marker shape to clone, retargeting `fixture: "loan-calc.xlsx"`)
    - crates/pmcp-workbook-dialect/src/lib.rs:35 (the 13-fn WHITELIST — the loan workbook MUST stay within
      it; NO PMT/POWER/`^`) and :43-45 (colour-role palette)
    - crates/pmcp-workbook-compiler/src/lib.rs:264-269 (promote_named_outputs / the `out_*` named-output convention)
    - crates/pmcp-workbook-runtime/src/sheet_ir/rounding.rs:30-60 (excel_round/excel_ceiling — the
      currency-rounding source of truth the loan's cached values must match)
    - 96-CONTEXT.md D-01, D-02 + 96-PATTERNS.md "tests/fixtures/loan-calc.xlsx" + RESEARCH Anti-Pattern
      "A privileged single output in the loan manifest"
  </read_first>
  <action>
    Using the Plan-03 fixture author (via its env-gated generator path so the committed fixture is produced
    non-mutatingly), author crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx: a fixed-cell
    formula DAG modeling a CREDIBLE loan/mortgage rate-tier calculation (Codex MEDIUM: not a toy lookup —
    a believable model). Inputs (blue-font `FF0000FF`, e.g. loan_amount, term_months, credit_score or
    ltv_band) feed a rate-tier lookup TABLE (constant cells `FFE2EFDA`) resolved via VLOOKUP and/or
    INDEX-MATCH (credit/LTV band -> applied rate), guarded by IFERROR, with nested-IF tiering, and
    ROUND/CEILING to currency. NO PMT, NO POWER, NO exponentiation (arbitrary-term `(1+r)^n` is NOT
    expressible — deferred). Emit MULTIPLE named outputs via the `out_*` convention (e.g.
    `out_applied_rate`, `out_monthly_interest`, `out_total_interest`) — NO privileged single headline
    output (Anti-Pattern). Give at least one output a CUSTOM UNIT (Gemini suggestion: e.g. `percent` for
    the applied rate, `USD` for an amount) so the served schema's unit projection is exercised for a
    non-golden workbook. DOCUMENT the formula DAG + the exact named inputs/outputs (with units) in the
    fixture generation metadata (Codex suggestion). Set each formula's cached `<v>` (via the author's
    Formula::set_result path) to the Excel-correct value computed against excel_round/excel_ceiling so the
    reconcile oracle is right. Add an explicit `pmcp_dialect_version` cell (e.g. "1.0") to exercise the
    WBDL-02 present-path (Plan 01) — keep it compatible so the loan compiles. Write the paired
    loan-calc.provenance-override.json (clone tax-calc's, retarget `fixture`). Do NOT edit any existing fixture.
  </action>
  <verify>
    <automated>test -f crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx && test -f crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.provenance-override.json && cargo test -p pmcp-workbook-compiler fixture_author</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx and loan-calc.provenance-override.json exist
    - the workbook's formulas use only whitelisted functions (no PMT/POWER/`^`) — asserted by a successful lint during compile (no dialect-refuse finding)
    - the workbook declares at least 3 distinct `out_*` named outputs (no single privileged output), at least one carrying a custom unit
    - the workbook contains a `pmcp_dialect_version` cell with a compatible value
    - the fixture generation metadata documents the formula DAG + named inputs/outputs + units
  </acceptance_criteria>
  <done>A whitelist-legal, credibly-divergent loan/mortgage fixture authored with genuine Excel identity and a correct reconcile oracle, with multiple named (unit-carrying) outputs. (Verify asserts the authored fixtures exist and the proven Plan-03 fixture author still passes; the full served-schema proof is exercised by Task 2's `reemit_loan` test.)</done>
</task>

<task type="auto">
  <name>Task 2: reemit_loan.rs compile-and-serve proof asserting the loan's OWN SERVED schema (get_manifest/tools/list)</name>
  <files>crates/pmcp-workbook-compiler/src/reemit_loan.rs, crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/reemit_golden.rs (FULL — the clone source: compile_fixture recipe
      :62-72, seven-member-present :124-142, BUNDLE.lock recompute :144-175,
      structural_eq_check4_loads_via_toolkit :178-192 (it imports `load_bundle, LocalDirSource`),
      production-refusal counter-test :239-252)
    - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx (Task 1 — the fixture under proof)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (the PUBLIC `input_schema_for_manifest` +
      `output_schema_for_manifest` — the served per-tool schema fns to call over the loaded loan bundle)
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (the five `NAME` consts + GetManifestHandler::new
      + curated_manifest — the served get_manifest projection; assert the loan's own inputs/outputs)
    - crates/pmcp-server-toolkit/src/workbook/mod.rs (the `pub use` surface: load_bundle, LocalDirSource,
      schema, the five handlers, WorkbookBundle — all reachable under the `workbook` dev-dep feature)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx (the contrast — the proof must assert the
      loan's served inputs/outputs are NOT tax-calc's 3-in/4-out and tax-specific keys are ABSENT)
    - crates/pmcp-workbook-compiler/src/lib.rs (where to declare `#[cfg(test)] mod reemit_loan;`; avoid a duplicate mod decl)
    - 96-PATTERNS.md "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
  </read_first>
  <action>
    Create crates/pmcp-workbook-compiler/src/reemit_loan.rs as a `#[cfg(test)]` module cloning
    reemit_golden.rs's structure but asserting the loan's OWN SERVED schema (no loan golden exists).
    Implement: (a) a compile_fixture helper that copies loan-calc.xlsx into a TempDir and compiles via
    `compile_workbook_with_fixture_override(&xlsx, &out_root, "loan-calc", "1.0.0", "proof-approver")`;
    (b) the seven-member-present check; (c) BUNDLE.lock recompute via build_bundle_lock; (d) THE SERVED-
    SCHEMA GENERALIZATION ASSERTION (Codex HIGH, verified — the single most important upgrade):
      - `let bundle = load_bundle(&LocalDirSource::new(&bundle_dir))?;` then assert
        `bundle.stamp.bundle_id == "loan-calc"`.
      - GENERIC TOOL NAMES UNCHANGED: assert the five served tool names are exactly
        {CalculateHandler::NAME, ExplainHandler::NAME, GetManifestHandler::NAME, DiffVersionHandler::NAME,
        RenderWorkbookHandler::NAME} == {calculate, explain, get_manifest, diff_version, render_workbook}
        — identical to tax-calc (the names are generic; only the payload differs).
      - SERVED INPUT SCHEMA reflects the LOAN's own inputs: call
        `schema::input_schema_for_manifest(&bundle.manifest, &bundle.cell_map)` and assert the returned
        JSON's `properties.inputs.properties` CONTAINS the loan input keys (e.g. loan_amount, term_months,
        credit_score/ltv_band) and does NOT contain any tax-calc-specific input key.
      - SERVED OUTPUT SCHEMA reflects the LOAN's own outputs: call
        `schema::output_schema_for_manifest(&bundle.manifest, &bundle.cell_map)` and assert it carries the
        loan `out_*` keys (incl. the custom-unit output) and NOT tax-calc's output keys.
      - SERVED get_manifest payload: build `GetManifestHandler::new(Arc::new(bundle.clone()))`, invoke its
        handler (or assert over `curated_manifest`), and assert the manifest's `inputs`/`outputs` arrays
        reflect the loan cells (loan-specific PRESENT, tax-specific ABSENT), with MULTIPLE outputs and no
        privileged single headline.
      - CONTRAST: load tax-calc the same way (or use known tax keys) and assert the loan's served input/
        output key SETS are DISJOINT from tax-calc's tax-specific keys — this disjointness IS the proof.
    (e) the production-refusal counter-test asserting bare `compile_workbook` (Enforce) refuses the same
    bytes. Declare `#[cfg(test)] mod reemit_loan;` in lib.rs. Use ONLY the `pmcp_server_toolkit::workbook`
    surface already available under the `workbook` dev-dep feature (no new dev-dep). Name the concrete
    assertions in test fn names (e.g. `served_input_schema_reflects_loan_inputs`,
    `tax_specific_fields_absent_from_loan_schema`, `five_generic_tool_names_unchanged`).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler reemit_loan</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-compiler/src/reemit_loan.rs exists, gated `#[cfg(test)]`, declared in lib.rs (no duplicate mod decl)
    - a test asserts the five served tool names == {calculate, explain, get_manifest, diff_version, render_workbook} (UNCHANGED vs tax-calc) via the handler `NAME` consts
    - a test calls `schema::input_schema_for_manifest(&bundle.manifest, &bundle.cell_map)` and asserts loan input keys PRESENT and tax-specific input keys ABSENT
    - a test calls `schema::output_schema_for_manifest(...)` and asserts loan `out_*` keys (incl. the custom-unit output) PRESENT and tax-specific output keys ABSENT
    - a test asserts the served get_manifest projection (GetManifestHandler/curated_manifest) reflects the loan's own inputs/outputs with multiple outputs and no privileged headline
    - a test asserts the loan served input/output key sets are DISJOINT from tax-calc's tax-specific keys (the generalization proof)
    - a test asserts bare compile_workbook (Enforce) refuses the loan bytes (production-refusal counter-test)
    - `cargo test -p pmcp-workbook-compiler reemit_loan` exits 0
  </acceptance_criteria>
  <done>An in-crate compile-and-serve proof that the loan workbook serves its OWN get_manifest/tools/list schema (loan-specific in/out present, tax-specific absent) behind the SAME five generic tool names with zero per-workbook served Rust — the literal WBEX-01 generalization gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loan fixture .xlsx -> compiler freshness/provenance gate | The loan fixture is test-authored and rides the trusted-fixture override; production must still refuse it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-10 | Elevation | the loan fixture passing the production freshness gate | mitigate | reemit_loan.rs includes the production-refusal counter-test: bare `compile_workbook` (Enforce) refuses the same bytes; only the `#[cfg(test)]` override accepts it (CR-01). |
| T-96-11 | Tampering | a per-workbook Rust shortcut faking generalization | mitigate | The proof reads the SERVED schema produced by the GENERIC toolkit fns (input_schema_for_manifest/output_schema_for_manifest/GetManifestHandler) over the loaded bundle — no loan-specific Rust builds the manifest; the assertion reads the generic-driver output, not a hand-built schema. |
| T-96-12 | Information disclosure | customer/TowelRads material leaking into the fixture | mitigate | The loan fixture is synthetic (rate-tier toy data); SUMMARY confirms zero customer/TowelRads content (hard constraint, 96-CONTEXT canonical_refs). |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages — no install task -> no legitimacy checkpoint. |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-compiler reemit_loan` green (served-schema generalization + production-refusal).
- SUMMARY confirms the loan fixture is synthetic with zero customer/TowelRads material.
- `git diff` shows no edits to existing fixtures.
</verification>

<success_criteria>
- WBEX-01 satisfied: a second, non-lighthouse workbook compiles and serves end-to-end through the SDK path; its SERVED get_manifest/tools/list schema reflects its OWN inputs/outputs (loan-specific present, tax-specific absent) behind the SAME five generic tool names, with multiple named outputs and no privileged single output — proving the manifest-driven §5 fix generalized.
</success_criteria>

<output>
Create `.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-04-SUMMARY.md` when done
</output>
