---
phase: 110-cargo-pmcp-agent-team-verbs
plan: 06
type: execute
wave: 3
depends_on: [110-02, 110-03, 110-04, 110-05]
files_modified:
  - cargo-pmcp/src/lib.rs
  - cargo-pmcp/examples/agent_scaffold_and_run.rs
  - cargo-pmcp/examples/team_dev_transcript.rs
  - cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs
  - cargo-pmcp/fuzz/Cargo.toml
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04]

must_haves:
  truths:
    - "cargo run -p cargo-pmcp --example agent_scaffold_and_run scaffolds an agent project AND drives the PRODUCTION fixed-source runner (cargo_pmcp::agent_run::run_fixed_source) to completion offline — exercising cargo-pmcp's own code, not a re-implemented loop"
    - "cargo run -p cargo-pmcp --example team_dev_transcript prints the offline doc-review team transcript (the ALWAYS EXAMPLE deliverable for team dev)"
    - "cargo +nightly fuzz run fuzz_package_kind feeds arbitrary bytes to the untrusted manifest-parse boundary (artifact_type_from_manifest_json → detect_kind) and never panics (the ALWAYS FUZZ deliverable for the package parse path)"
    - "All deliverables reach production code through narrow #[doc(hidden)] public lib seams, never the bin-only command layer"
  artifacts:
    - path: "cargo-pmcp/examples/agent_scaffold_and_run.rs"
      provides: "Runnable agent new + agent dev --source fixed demo over the production seams"
      contains: "run_fixed_source"
    - path: "cargo-pmcp/examples/team_dev_transcript.rs"
      provides: "Runnable offline doc-review team transcript demo (ALWAYS EXAMPLE for team dev)"
      contains: "TeamRuntimeBuilder"
    - path: "cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs"
      provides: "libfuzzer target over the untrusted manifest-parse boundary (bytes → artifact_type → kind)"
      contains: "artifact_type_from_manifest_json"
    - path: "cargo-pmcp/src/lib.rs"
      provides: "templates_agent + package_kind + agent_run #[doc(hidden)] public #[path] seams"
      contains: "agent_run"
  key_links:
    - from: "cargo-pmcp/examples/agent_scaffold_and_run.rs"
      to: "cargo_pmcp::agent_run::run_fixed_source"
      via: "lib #[path] seam onto commands/agent/run.rs (the production fixed-source runner)"
      pattern: "run_fixed_source"
    - from: "cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs"
      to: "cargo_pmcp::package_kind::artifact_type_from_manifest_json"
      via: "lib #[path] seam onto commands/package/kind.rs"
      pattern: "artifact_type_from_manifest_json"
---

<objective>
Ship the CLAUDE.md "ALWAYS Requirements for New Features" deliverables the Phase 110
verbs still lack: (1) a runnable `cargo run --example` demo for the agent verbs
(`agent new` scaffold + `agent dev --source fixed` offline loop) that drives the PRODUCTION
fixed-source runner (`cargo_pmcp::agent_run::run_fixed_source` from plan 110-03) rather than a
re-implemented loop (Codex 110-06 HIGH), (2) a runnable offline team-dev transcript example (the
ALWAYS EXAMPLE for team dev), and (3) a fuzz target over the UNTRUSTED manifest-parse boundary —
`artifact_type_from_manifest_json` (arbitrary bytes → artifactType) → `detect_kind` — the real
untrusted seam of `package show` (Codex 110-06 MEDIUM), not just an 8-constant string match. All
reach production code through narrow `#[doc(hidden)]` public lib `#[path]` seams (Codex 110-06
MEDIUM), never the bin-only command layer.

Purpose: Close the ALWAYS-deliverable gap (FUZZ + EXAMPLE for the new verbs) while exercising the
PRODUCTION CLI code paths, not duplicates.
Output: An agent example (over the production runner seam) + a team-dev example + a fuzz target over
the manifest-parse boundary + the three `#[doc(hidden)]` public seams.

## Phase-level ALWAYS interpretation (Codex HIGH #7 — recorded decision)
The CLAUDE.md ALWAYS matrix (fuzz/property/unit/example) is satisfied per-verb as follows:
- `agent new`: example (this plan) + unit drift-guard + scaffold-compile integration (110-02).
- `agent dev`: example (this plan, via the production `run_fixed_source` seam) + fixed/sampling integration (110-03).
- `team dev`: example (this plan) + offline-transcript + --serve + --llm integration (110-04).
- `package show`: fuzz (this plan, manifest-parse boundary) + `detect_kind` proptest + offline-render integration (110-05).
- `package capture`: capture is an HTTP client whose only UNTRUSTED input is the package it parses (covered by
  the `package show` parse fuzz + pmcp-package's own digest verification) and whose network path is mock-tested
  (110-05 configured-success test). A dedicated capture fuzz/example beyond that is not a meaningful untrusted
  boundary — this is the explicitly-approved phase-level interpretation of the ALWAYS rule for the capture verb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-CONTEXT.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-RESEARCH.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-PATTERNS.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-REVIEWS.md
@cargo-pmcp/examples/workbook_server_scaffold.rs
@cargo-pmcp/fuzz/fuzz_targets/fuzz_iam_config.rs
@crates/pmcp-team-servers/examples/doc_review_team.rs

<interfaces>
<!-- Everything the executor needs; no exploration. -->
The three seams to add mirror the VERIFIED existing convention in cargo-pmcp/src/lib.rs
  (`#[path = "templates/workbook_server.rs"] pub mod templates_workbook_server;`), each marked
  `#[doc(hidden)]` (Codex 110-06 MEDIUM — internal support seams, not stable API):
  - `#[doc(hidden)] #[path = "templates/agent.rs"] pub mod templates_agent;` (leaf from 110-02 —
    std::fs + format! + pmcp_package serialization; no clap/GlobalFlags).
  - `#[doc(hidden)] #[path = "commands/package/kind.rs"] pub mod package_kind;` (leaf from 110-05 —
    pure `detect_kind` + `artifact_type_from_manifest_json`; no clap/GlobalFlags/OciLayout).
  - `#[doc(hidden)] #[path = "commands/agent/run.rs"] pub mod agent_run;` (leaf from 110-03 — lib-safe
    `pub async fn run_fixed_source(config: ResolvedAgentConfig) -> RunOutcome`; pmcp_agent + std only).

Production seam the agent example calls (from plan 110-03, run.rs):
  - `cargo_pmcp::agent_run::run_fixed_source(config: ResolvedAgentConfig) -> RunOutcome` — the SAME
    fixed-source path the `agent dev --source fixed` CLI arm uses. The example builds a
    `ResolvedAgentConfig::new(...)` (s50 args) and calls it (Codex 110-06 HIGH — no re-implemented loop).

Agent scaffold seam the example calls (from plan 110-02, templates/agent.rs):
  - `cargo_pmcp::templates_agent::generate(&dir, "demo_agent")?` — emits the scaffold file tree.

Team transcript the team example drives (public pmcp-team-servers API, mirroring doc_review_team.rs):
  - `pmcp_team_servers::compose::wiring::TeamRuntimeBuilder` + `fixed_override()` + the 7-step flow.

Manifest-parse boundary the fuzz target drives (from plan 110-05, kind.rs):
  - `cargo_pmcp::package_kind::artifact_type_from_manifest_json(bytes: &[u8]) -> Option<String>` then
    `cargo_pmcp::package_kind::detect_kind(&s) -> Option<PackageKind>`.

fuzz/Cargo.toml `[[bin]]` block shape (verified — mirror an existing entry exactly):
  `[[bin]]\nname = "fuzz_package_kind"\npath = "fuzz_targets/fuzz_package_kind.rs"\ndoc = false\ntest = false\nbench = false`
  The fuzz crate already depends on `cargo-pmcp = { path = ".." }`; the seam re-exports make the fns reachable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three #[doc(hidden)] lib seams and the agent example (over the production runner)</name>
  <files>cargo-pmcp/src/lib.rs, cargo-pmcp/examples/agent_scaffold_and_run.rs</files>
  <read_first>
    - cargo-pmcp/src/lib.rs (the `templates_workbook_server` `#[path]` seam block to mirror, incl. doc comments)
    - cargo-pmcp/examples/workbook_server_scaffold.rs (the scaffold-via-public-seam example shape)
    - cargo-pmcp/src/commands/agent/run.rs (the `run_fixed_source` signature from plan 110-03)
    - cargo-pmcp/src/templates/agent.rs (the `generate(dir, name)` signature from plan 110-02)
  </read_first>
  <action>
    In lib.rs, add three `#[doc(hidden)]` `#[path]` seams mirroring the existing `templates_workbook_server`
    block (leaf modules, dependency-light, reached by an example/fuzz target NOT the bin-only tree):
    - `#[doc(hidden)] #[path = "templates/agent.rs"] pub mod templates_agent;`
    - `#[doc(hidden)] #[path = "commands/package/kind.rs"] pub mod package_kind;`
    - `#[doc(hidden)] #[path = "commands/agent/run.rs"] pub mod agent_run;`
    Create examples/agent_scaffold_and_run.rs mirroring workbook_server_scaffold.rs: in an auto-cleaned
    `tempfile::tempdir()`, `fs::create_dir_all(dir.join("src"))`, then
    `cargo_pmcp::templates_agent::generate(&dir, "demo_agent")?` and print the emitted file tree. THEN
    demonstrate `agent dev --source fixed` fully offline by calling the PRODUCTION runner seam (Codex 110-06
    HIGH — do NOT re-implement the loop with a bare `AgentEngine`): build `ResolvedAgentConfig::new(...)`
    (s50 args) and `let outcome = cargo_pmcp::agent_run::run_fixed_source(config).await;`, print the terminal
    `RunOutcome`. Header doc-comment MUST state it satisfies the CLAUDE.md ALWAYS EXAMPLE requirement for
    CLI-01/CLI-02 and give the run command `cargo run -p cargo-pmcp --example agent_scaffold_and_run`.
    `#[tokio::main]` main. Do NOT reference the bin-only `templates::*`/`commands::*` paths — only the public
    seams + pmcp_agent.
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp --example agent_scaffold_and_run && cargo run -p cargo-pmcp --example agent_scaffold_and_run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs contains all three `#[doc(hidden)] #[path = ...] pub mod` seams (templates_agent, package_kind, agent_run)
    - examples/agent_scaffold_and_run.rs contains `templates_agent::generate` and `agent_run::run_fixed_source` (production seam, no inline `AgentEngine::new`)
    - the example performs NO network I/O
    - `cargo run -p cargo-pmcp --example agent_scaffold_and_run` exits 0 and prints a terminal RunOutcome
  </acceptance_criteria>
  <done>The agent verbs have a runnable end-to-end example (scaffold + offline fixed-source loop) reached through the production runner seam; ALWAYS EXAMPLE requirement satisfied without duplicating the loop.</done>
</task>

<task type="auto">
  <name>Task 2: Add fuzz_package_kind over the untrusted manifest-parse boundary</name>
  <files>cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs, cargo-pmcp/fuzz/Cargo.toml</files>
  <read_first>
    - cargo-pmcp/fuzz/fuzz_targets/fuzz_iam_config.rs (the `#![no_main]` + never-panic target shape)
    - cargo-pmcp/fuzz/Cargo.toml (the `[[bin]]` block form to append)
    - cargo-pmcp/src/commands/package/kind.rs (the `artifact_type_from_manifest_json` + `detect_kind` signatures from plan 110-05)
  </read_first>
  <action>
    Create fuzz_targets/fuzz_package_kind.rs mirroring fuzz_iam_config.rs: `#![no_main]`,
    `use libfuzzer_sys::fuzz_target;`, then a `fuzz_target!(|data: &[u8]| { ... })` that feeds the RAW bytes
    (no utf8 pre-filter — the manifest parser must tolerate arbitrary bytes) to
    `cargo_pmcp::package_kind::artifact_type_from_manifest_json(data)`, and, if it returns `Some(s)`, chains
    `let _ = cargo_pmcp::package_kind::detect_kind(&s);` — exercising the full untrusted extraction path
    (Codex 110-06 MEDIUM — the real boundary, not an 8-constant string match). The invariant: neither fn
    panics on arbitrary/adversarial manifest bytes drawn from an untrusted `.pmcp` package (threat
    T-110-05-03 / T-110-06-01). Header doc-comment names the threat, gives the run command
    `cargo +nightly fuzz run fuzz_package_kind` + the `-max_total_time=60` smoke form, and states it satisfies
    the CLAUDE.md ALWAYS FUZZ requirement for the package parse path.
    In fuzz/Cargo.toml, append a `[[bin]]` block for `fuzz_package_kind` mirroring the existing entries. No new
    dependency — the `cargo-pmcp` path dep + the `package_kind` seam expose both fns.
  </action>
  <verify>
    <automated>grep -q 'name = "fuzz_package_kind"' cargo-pmcp/fuzz/Cargo.toml && grep -q 'artifact_type_from_manifest_json' cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs && cargo build --manifest-path cargo-pmcp/fuzz/Cargo.toml --bin fuzz_package_kind 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - fuzz_targets/fuzz_package_kind.rs contains `#![no_main]` and drives `artifact_type_from_manifest_json` on raw `&[u8]` then chains `detect_kind`
    - fuzz/Cargo.toml contains a `[[bin]]` block named `fuzz_package_kind`
    - `cargo build --manifest-path cargo-pmcp/fuzz/Cargo.toml --bin fuzz_package_kind` compiles (libfuzzer target links)
  </acceptance_criteria>
  <done>The untrusted manifest-parse boundary (arbitrary bytes → artifactType → kind) has a libfuzzer target that never panics; ALWAYS FUZZ requirement satisfied at a meaningful boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Add the offline team-dev transcript example (ALWAYS EXAMPLE for team dev)</name>
  <files>cargo-pmcp/examples/team_dev_transcript.rs</files>
  <read_first>
    - crates/pmcp-team-servers/examples/doc_review_team.rs (the reference transcript + fixtures + body()/step() helpers)
    - cargo-pmcp/examples/workbook_server_scaffold.rs (the cargo-pmcp example header/run-command convention)
  </read_first>
  <action>
    Create examples/team_dev_transcript.rs mirroring the offline default path of `team dev` (plan 110-04):
    build the doc-review `TeamPackage` fixture into an auto-cleaned `tempfile::tempdir()`, a
    `LocalDirPackageResolver`, a stub slot resolver, and a `fixed_override()`; `TeamRuntimeBuilder::new(...)
    .with_completion_override(fixed_override()).with_data_root(dir).build(&pkg).await?`; obtain the four
    clients; run the 7-step doc-review flow printing a labeled/colored transcript line per step (copy the
    `body()` + `step(n,msg)` helpers); finish with `rt.shutdown().await`. Fully offline, deterministic, no
    network. Header doc-comment MUST state it satisfies the CLAUDE.md ALWAYS EXAMPLE requirement for CLI-03
    and give the run command `cargo run -p cargo-pmcp --example team_dev_transcript`. `#[tokio::main]` main.
    Drive composition entirely through `TeamRuntime` (D-02 — do NOT hand-roll server spin-up).
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp --example team_dev_transcript && cargo run -p cargo-pmcp --example team_dev_transcript 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - examples/team_dev_transcript.rs contains `TeamRuntimeBuilder` and `with_completion_override(fixed_override())`
    - the example prints the 7-step labeled transcript and calls `rt.shutdown()`
    - the example performs NO network I/O (FixedSource over a tempdir data root)
    - `cargo run -p cargo-pmcp --example team_dev_transcript` exits 0 and prints the transcript
  </acceptance_criteria>
  <done>The team dev verb has a runnable offline transcript example; ALWAYS EXAMPLE requirement for CLI-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `.pmcp` manifest bytes → artifact_type_from_manifest_json/detect_kind | Adversarial manifest JSON bytes from a foreign package cross into parse + kind detection |
| example scaffold/team runtime → filesystem | Emitters write into auto-cleaned tempdirs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-06-01 | Denial of Service | `artifact_type_from_manifest_json` + `detect_kind` on adversarial bytes | mitigate | libfuzzer `fuzz_package_kind` proves the pure parse+dispatch never panics/hangs on arbitrary manifest bytes, covering package show's untrusted-parse boundary (the real seam, Codex 110-06 MEDIUM) |
| T-110-06-02 | Tampering | example filesystem writes | accept | Examples write only into `tempfile::tempdir()` dirs that auto-clean; no user path input, no network |
| T-110-06-SC | Tampering | cargo dependency additions | accept | No new registry packages — the seams re-mount in-repo leaves; the fuzz crate reuses its existing `cargo-pmcp` path dep. No slopcheck applies. |
</threat_model>

<verification>
- `cargo run -p cargo-pmcp --example agent_scaffold_and_run` — scaffold + offline production-runner loop green.
- `cargo run -p cargo-pmcp --example team_dev_transcript` — offline team transcript green.
- `cargo build --manifest-path cargo-pmcp/fuzz/Cargo.toml --bin fuzz_package_kind` — fuzz target links.
- `cargo +nightly fuzz run fuzz_package_kind -- -max_total_time=60` — bounded smoke fuzz finds no panic (native/nightly, optional in CI, Codex 110-06 LOW).
- `make quality-gate` before commit (fmt/clippy/build/test).
</verification>

<success_criteria>
- The agent verbs have a runnable example (scaffold + fixed-source loop) that calls the PRODUCTION `run_fixed_source` seam.
- The team dev verb has a runnable offline transcript example.
- `package show`'s untrusted manifest-parse boundary has a libfuzzer target (bytes → artifactType → kind).
- All deliverables reach production code through `#[doc(hidden)]` public lib `#[path]` seams, not the bin-only command tree.
- ALWAYS EXAMPLE + ALWAYS FUZZ deliverables for Phase 110 are satisfied, with an explicit phase-level interpretation recorded for package capture.
</success_criteria>

<output>
Create `.planning/phases/110-cargo-pmcp-agent-team-verbs/110-06-SUMMARY.md` when done
</output>
</content>
