---
phase: 110-cargo-pmcp-agent-team-verbs
plan: 02
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - cargo-pmcp/src/commands/agent/new.rs
  - cargo-pmcp/src/templates/agent.rs
  - cargo-pmcp/src/templates/mod.rs
  - cargo-pmcp/src/commands/new.rs
  - cargo-pmcp/tests/scaffold_agent.rs
autonomous: true
requirements: [CLI-01]

must_haves:
  truths:
    - "cargo pmcp agent new <name> scaffolds an agent project with an AgentPackage JSON manifest, a standalone runner, and a tripwire test"
    - "The emitted project actually COMPILES (cargo check passes) and its generated tests/pin.rs passes — proven against the real binary output"
    - "The emitted runner loads agent.package.json (the manifest is the source of truth), not a hardcoded config"
    - "The scaffold's pmcp-agent pin cannot silently drift from the workspace crate version"
    - "A non-empty destination is refused unless --force"
  artifacts:
    - path: "cargo-pmcp/src/templates/agent.rs"
      provides: "Agent scaffold emitter + PMCP_AGENT_VERSION drift-guard"
      contains: "PMCP_AGENT_VERSION"
    - path: "cargo-pmcp/src/commands/agent/new.rs"
      provides: "agent new handler: validate name + destination policy then emit scaffold"
      contains: "validate_crate_name"
    - path: "cargo-pmcp/tests/scaffold_agent.rs"
      provides: "CLI-01 scaffold integration test that compiles the emitted project"
      exports: []
  key_links:
    - from: "cargo-pmcp/src/commands/agent/new.rs"
      to: "templates::agent::generate"
      via: "scaffolder delegation after validate_crate_name"
      pattern: "templates::agent::generate"
    - from: "cargo-pmcp/src/templates/agent.rs"
      to: "crates/pmcp-agent/Cargo.toml"
      via: "include_str! drift-guard test"
      pattern: "include_str!.*pmcp-agent/Cargo.toml"
---

<objective>
Implement `cargo pmcp agent new <name>` (CLI-01): a scaffolder that emits an agent
project — an `AgentPackage` JSON manifest, a standalone runner that LOADS that manifest
and drives `AgentEngine`, a `Cargo.toml` pinning `pmcp-agent` with the full dependency
set, and a `tests/pin.rs` tripwire in the generated project — plus an internal template
drift-guard so the emitted pin cannot drift from the released `pmcp-agent` version (D-05).
The integration test PROVES the emitted project compiles (`cargo check`) and its pin test
passes. Reuses the existing scaffolding engine in `cargo-pmcp/src/templates/` and
`validate_crate_name` from `commands/new.rs` (D-01a). Fills the plan-110-01 stub in agent/new.rs.

Purpose: cargo-pmcp becomes the agent on-ramp, matching its server-scaffold story — and the
scaffold is a *compilable* deliverable, not just an emitted file tree (Codex 110-02 HIGH).
Output: A dedicated `templates/agent.rs` emitter + wired `agent new` handler + a scaffold
test that compiles the emitted crate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-CONTEXT.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-RESEARCH.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-PATTERNS.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-REVIEWS.md
@cargo-pmcp/src/templates/workbook_server.rs
@cargo-pmcp/src/commands/new.rs
@crates/pmcp-agent/examples/s50_standalone_vs_sampled.rs

<interfaces>
<!-- Contracts the executor uses directly — no exploration needed. -->
From cargo-pmcp/src/commands/new.rs (verified):
  - `fn validate_crate_name(name: &str) -> Result<()>` at line 119 — currently PRIVATE.
    Promote to `pub(crate) fn validate_crate_name`. Hardened path-traversal + Cargo-name guard
    (rejects empty, leading digit, `/`, `\`, `..`, illegal chars). REUSE — do not re-implement.
  - `execute_sql_server(workspace_dir, name, global_flags)` (line ~149) is the scaffolder shape:
    validate_crate_name(name)? → fs::create_dir_all(dir.join("src")) → templates::*::generate(dir, name)
    → gated `println!` next-steps behind `global_flags.should_output()`.

From cargo-pmcp/tests/scaffold_sql_server.rs (VERIFIED — the compile-proof precedent):
  - Invokes the REAL binary via `env!("CARGO_BIN_EXE_cargo-pmcp")` in a `tempfile::tempdir()`,
    then calls `append_crates_io_patch(&crate_dir)` (from `tests/support/scaffold_patch.rs`) to
    write a `[patch.crates-io]` block resolving the unpublished workspace deps, then SPAWNS a REAL
    `cargo` subprocess in the scaffold dir to compile it. Every child is wrapped in `ChildGuard`
    (Drop-kill). Belongs to the `--test-threads=1` group (heavy build).

From cargo-pmcp/src/templates/workbook_server.rs (the emitter + drift-guard to mirror):
  - `pub fn generate(dir: &Path, name: &str) -> Result<()>` orchestrates per-file `generate_*` fns,
    each a raw `fs::write(dir.join(...), format!(...)).context(...)` (NO template engine; escape braces `{{`/`}}`).
  - Drift-guard: `const PMCP_VERSION: &str = "2.17.0";` + `#[test] fn emitted_pmcp_version_matches_workspace_pin()`
    that `include_str!("../../../Cargo.toml")`, parses `["package"]["version"]`, and `assert_eq!`.

From crates/pmcp-package (AgentPackage struct the emitted manifest MUST round-trip through):
  - `use pmcp_package::{AgentPackage, ConfigSlot, SlotType};`
  - Fields (exact literal in s50 `agent_package()`): name:String, version:semver::Version,
    instructions:String, llm: ConfigSlot { slot: SlotType::LlmProvider { name, tested_value } },
    max_tokens:i64, max_iterations:i64, connectors:vec![], tool_selection:None, input_schema:None,
    output_schema:None, importance:None, finalizer_role:None, budget_defaults:vec![].

From crates/pmcp-agent (runner target — the manifest-driven runner, Codex 110-02 MEDIUM):
  - `pmcp_agent::resolve_agent(&agent_pkg, &slot_resolver).await -> Result<ResolvedAgentConfig>`
    (config/resolver.rs:309) — resolve an AgentPackage into a runnable config using a SlotResolver
    (`pmcp_agent::EnvVarResolver::new()` for the env-var convention). This is how the shipped
    `team-mcp` binary turns a package into a config; the emitted runner MUST do the same.
  - `AgentEngine::new(source, invoker, InMemoryStore::new(), config)` + `.run("...").await -> RunOutcome`.
  - `OpenAiCompatSource::new(endpoint, model, SecretString::new(key)) -> Result<_, CompletionError>`
    (feature "openai-compat"; endpoint scheme validated at construction).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold integration test that COMPILES the emitted project (RED)</name>
  <files>cargo-pmcp/tests/scaffold_agent.rs</files>
  <behavior>
    - Running the real cargo-pmcp binary with `agent new <name>` in a tempdir creates
      `<name>/Cargo.toml`, `<name>/src/main.rs`, `<name>/agent.package.json`, and `<name>/tests/pin.rs`.
    - The emitted manifest file parses as `pmcp_package::AgentPackage` (round-trips).
    - After `append_crates_io_patch`, a real `cargo check` on the emitted crate SUCCEEDS (the scaffold
      compiles — Codex 110-02 HIGH), and the generated `cargo test --test pin` PASSES.
    - Running `agent new <name>` a SECOND time into the same non-empty dir FAILS unless `--force`
      (destination-overwrite policy — Codex 110-02 MEDIUM).
    - Test fails RED now (agent new is still the 110-01 bail! stub).
  </behavior>
  <read_first>
    - cargo-pmcp/tests/scaffold_sql_server.rs (the compile-the-emitted-crate shape: real binary → append_crates_io_patch → spawn `cargo` → ChildGuard)
    - cargo-pmcp/tests/support/scaffold_patch.rs (append_crates_io_patch, ChildGuard)
  </read_first>
  <action>
    Mirror scaffold_sql_server.rs: invoke the REAL built binary via `env!("CARGO_BIN_EXE_cargo-pmcp")`
    (do NOT weaken to an in-process call) inside a `tempfile::tempdir()`, running `agent new <name>`.
    Assert the four emitted files exist and that `agent.package.json` deserializes via
    `serde_json::from_str::<pmcp_package::AgentPackage>`. Then call
    `append_crates_io_patch(&crate_dir)` and SPAWN a real `cargo check` subprocess in the scaffold dir
    (wrap the child in `ChildGuard`); assert exit success. Also spawn `cargo test --test pin` in the
    scaffold dir and assert it passes (the emitted in-scaffold tripwire runs GREEN against the patched
    deps). Add a negative case: run `agent new <name>` a second time into the now-non-empty dir and
    assert it exits non-zero with a message naming `--force`; then run with `--force` and assert
    success. Mark the module `--test-threads=1` (heavy tempdir build) consistent with scaffold_sql_server.rs.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test scaffold_agent -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - cargo-pmcp/tests/scaffold_agent.rs contains `env!("CARGO_BIN_EXE_cargo-pmcp")` and `append_crates_io_patch`
    - The test spawns a real `cargo check` on the emitted crate and asserts success (compile proof)
    - The test runs the emitted `cargo test --test pin` and asserts it passes
    - The test covers the `--force` destination policy (second run without `--force` fails; with `--force` succeeds)
    - Before Task 2/3, `cargo test -p cargo-pmcp --test scaffold_agent` fails (RED)
  </acceptance_criteria>
  <done>A real-binary scaffold test exists that compiles the emitted crate + runs its pin test + checks the force policy, and fails because agent new is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: templates/agent.rs emitter — manifest-driven runner, full deps, two-level pin tripwire</name>
  <files>cargo-pmcp/src/templates/agent.rs, cargo-pmcp/src/templates/mod.rs</files>
  <behavior>
    - `generate(dir, name)` writes Cargo.toml (pins pmcp-agent + the FULL dependency set the runner and
      tests/pin.rs need), src/main.rs (a runner that LOADS agent.package.json and drives AgentEngine),
      agent.package.json (AgentPackage), and tests/pin.rs into the scaffold.
    - Unit test `emitted_agent_version_matches_workspace_pin` fails if PMCP_AGENT_VERSION drifts from
      crates/pmcp-agent/Cargo.toml `[package] version`.
    - Unit test asserts the emitted JSON manifest deserializes as `pmcp_package::AgentPackage`.
  </behavior>
  <read_first>
    - cargo-pmcp/src/templates/workbook_server.rs (generate orchestrator + const + include_str! drift-test to mirror exactly)
    - cargo-pmcp/src/templates/mod.rs (barrel — add `pub mod agent;` next to `pub mod workbook_server;`)
    - crates/pmcp-agent/examples/s50_standalone_vs_sampled.rs (the AgentPackage literal + the run_standalone runner body)
    - crates/pmcp-agent/src/bin patterns for `resolve_agent` usage (config from a loaded package)
    - crates/pmcp-agent/Cargo.toml (the version the drift-guard targets)
  </read_first>
  <action>
    Add `pub mod agent;` to templates/mod.rs. Create templates/agent.rs mirroring workbook_server.rs:
    a `pub fn generate(dir: &Path, name: &str) -> Result<()>` calling per-file `generate_cargo_toml`,
    `generate_main_rs`, `generate_manifest`, `generate_pin_test`, each a raw `fs::write(...).context(...)`.
    - generate_cargo_toml: emit `[package]` + a COMPLETE `[dependencies]` set the runner and pin test
      compile against (Codex 110-02 MEDIUM — enumerate everything): `pmcp-agent = { version =
      "{PMCP_AGENT_VERSION}", features = ["openai-compat"] }`, `pmcp-package = "0.1"`, `tokio = { version
      = "1", features = ["full"] }`, `serde_json = "1"`, `anyhow = "1"`; and a `[dev-dependencies]` `toml
      = "0.8"` for tests/pin.rs. Escape literal braces `{{`/`}}`.
    - generate_manifest: emit an `AgentPackage` serialized to pretty JSON matching the s50
      `agent_package()` literal (name = the scaffold name, version "1.0.0", a demo instructions string,
      llm ConfigSlot LlmProvider, max_tokens 100_000, max_iterations 5, all other fields empty/None).
      Write to `agent.package.json`.
    - generate_main_rs: emit a runner that reads `agent.package.json` from the crate root, deserializes it
      as `pmcp_package::AgentPackage`, resolves it via `pmcp_agent::resolve_agent(&pkg,
      &pmcp_agent::EnvVarResolver::new()).await?` into a `ResolvedAgentConfig` (the manifest is the source
      of truth — Codex 110-02 MEDIUM), builds `OpenAiCompatSource::new("http://localhost:11434/v1",
      <model>, SecretString::new("ollama"))?`, `AgentEngine::new(...)`, and `engine.run("agent-run").await`,
      printing the terminal `RunOutcome`. `#[tokio::main]` main.
    - generate_pin_test: emit `tests/pin.rs` into the scaffold that parses the scaffold's OWN Cargo.toml
      via the `toml` dev-dep and asserts the `[dependencies] pmcp-agent` version req contains the pinned
      major.minor (satisfies the literal "generated tripwire test" reading of CLI-01/D-05, Research
      Open-Q1 → ship both levels).
    Add `const PMCP_AGENT_VERSION: &str = "0.1.0";` and, mirroring workbook_server.rs, a
    `#[test] fn emitted_agent_version_matches_workspace_pin()` that `include_str!("../../../crates/pmcp-agent/Cargo.toml")`,
    parses `["package"]["version"]`, and `assert_eq!(PMCP_AGENT_VERSION, v, "agent scaffold pin drifted — bump PMCP_AGENT_VERSION")`.
    Add a second unit test that serializes the manifest and asserts `serde_json::from_str::<pmcp_package::AgentPackage>`
    succeeds (round-trip lock). Keep each `generate_*` and the handler under cognitive complexity 25.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --lib emitted_agent_version_matches_workspace_pin && cargo test -p cargo-pmcp --lib agent -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - templates/mod.rs contains `pub mod agent;`
    - templates/agent.rs contains `const PMCP_AGENT_VERSION: &str = "0.1.0";`
    - templates/agent.rs contains `include_str!("../../../crates/pmcp-agent/Cargo.toml")`
    - generate_cargo_toml emits pmcp-agent, pmcp-package, tokio(full), serde_json, anyhow, and dev-dep toml
    - generate_main_rs reads `agent.package.json` and calls `resolve_agent` (manifest-driven runner, not hardcoded config)
    - `cargo test -p cargo-pmcp --lib emitted_agent_version_matches_workspace_pin` passes
    - The manifest round-trip unit test passes (`serde_json::from_str::<pmcp_package::AgentPackage>` on the emitted JSON is Ok)
  </acceptance_criteria>
  <done>The emitter produces a compilable AgentPackage manifest + a manifest-driven runner + in-scaffold pin test + full deps, and the internal drift-guard passes against pmcp-agent 0.1.0.</done>
</task>

<task type="auto">
  <name>Task 3: Wire agent new handler — validate name/path/destination, promote validate_crate_name (GREEN)</name>
  <files>cargo-pmcp/src/commands/agent/new.rs, cargo-pmcp/src/commands/new.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/agent/new.rs (the 110-01 stub with NewArgs { name, path, force } to replace)
    - cargo-pmcp/src/commands/new.rs (validate_crate_name at line 119 to promote to pub(crate); execute_sql_server scaffolder shape)
  </read_first>
  <action>
    In commands/new.rs, change `fn validate_crate_name` to `pub(crate) fn validate_crate_name`
    (promote, do not copy). In agent/new.rs, replace the stub `execute` body:
    - Call `crate::commands::new::validate_crate_name(&args.name)?` BEFORE any fs write (path-traversal
      guard, T-86-03-02 precedent).
    - Resolve the target dir from `args.path` (default `./<name>`). Validate the path (Codex 110-02
      MEDIUM): reject a path whose final component differs from a safe crate dir, and refuse to follow a
      symlinked destination — if `dir` exists and `dir.symlink_metadata()?.file_type().is_symlink()`,
      `bail!`. Destination policy: if `dir` exists and is NON-EMPTY and `!args.force`, `bail!("destination
      <dir> is not empty — pass --force to overwrite")`; with `--force`, proceed.
    - `fs::create_dir_all(dir.join("src"))`, then delegate to `crate::templates::agent::generate(&dir, &args.name)?`.
    - Gate a colored next-steps `println!` (cd, `cargo run`, run the pin test) behind `global_flags.should_output()`.
    Return Ok(()). Keep the handler under cognitive complexity 25 (split the destination-policy check into a small helper).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test scaffold_agent -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - commands/new.rs declares `pub(crate) fn validate_crate_name`
    - agent/new.rs calls `validate_crate_name` before any `fs::` write and calls `templates::agent::generate`
    - agent/new.rs refuses a non-empty destination unless `args.force`, and rejects a symlinked destination
    - agent/new.rs no longer contains the `bail!("agent new: implemented in plan 110-02")` stub
    - `cargo test -p cargo-pmcp --test scaffold_agent` passes (GREEN — including the compile + pin + force cases)
  </acceptance_criteria>
  <done>`cargo pmcp agent new <name>` scaffolds a COMPILABLE agent project with a manifest-driven runner; the scaffold integration test (compile + pin + force policy) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → agent new | User-supplied crate name + output path (untrusted; may attempt traversal or target a symlink) |
| emitter → filesystem | Scaffolded files written under the resolved project dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-02-01 | Tampering | scaffold name / output path | mitigate | Reuse hardened `validate_crate_name` (path-traversal + Cargo-name guard) BEFORE any fs write; reject symlinked destination; acceptance-gated (Research V5, Codex 110-02 MEDIUM) |
| T-110-02-02 | Tampering | emitted `pmcp-agent` pin drift | mitigate | `emitted_agent_version_matches_workspace_pin` drift-guard test + in-scaffold `tests/pin.rs` (D-05); high severity → covered by Task 2 |
| T-110-02-03 | Integrity | emitted manifest shape drift | mitigate | Round-trip unit test asserts the emitted JSON deserializes as `pmcp_package::AgentPackage`; the emitted runner LOADS that manifest, so runner and manifest cannot diverge (Codex 110-02 MEDIUM) |
| T-110-02-04 | Tampering | partial/overwriting write into an existing destination | mitigate | Refuse a non-empty destination unless `--force` (destination-overwrite policy); acceptance-gated |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --lib emitted_agent_version_matches_workspace_pin` — pin drift-guard green.
- `cargo test -p cargo-pmcp --test scaffold_agent -- --test-threads=1` — end-to-end scaffold green, INCLUDING a real `cargo check` + `cargo test --test pin` on the emitted crate + the `--force` policy.
- `make quality-gate` before commit (fmt/clippy/build/test; PMAT cog ≤25 on new fns in CI).
</verification>

<success_criteria>
- `cargo pmcp agent new <name>` emits an AgentPackage manifest + a manifest-driven runner + in-scaffold pin test.
- The emitted project COMPILES (`cargo check`) and its pin test passes — proven by the integration test.
- Two-level pin tripwire (internal drift-guard + emitted tests/pin.rs) present per D-05.
- Crate name validated for path traversal, symlink destination rejected, and a non-empty destination refused unless `--force`.
</success_criteria>

<output>
Create `.planning/phases/110-cargo-pmcp-agent-team-verbs/110-02-SUMMARY.md` when done
</output>
</content>
