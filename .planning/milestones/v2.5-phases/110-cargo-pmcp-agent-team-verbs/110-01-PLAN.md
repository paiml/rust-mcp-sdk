---
phase: 110-cargo-pmcp-agent-team-verbs
plan: 01
type: execute
wave: 1
depends_on: []
# NOTE (scope rationale): this plan lists 12 files_modified (above the usual ~10
# threshold) BY DESIGN — it is the shared-foundation plan and 8 of the 11 source
# files are tiny NEW stub files (three group mod.rs + five one-line `bail!` handler
# stubs). The real work is 3 files (Cargo.toml deps, commands/mod.rs barrel,
# main.rs arms) plus one help-assertion test. The stub surface exists solely so the
# four Wave-2 verb plans own DISJOINT files and run in parallel with zero conflicts.
files_modified:
  - cargo-pmcp/Cargo.toml
  - cargo-pmcp/src/commands/mod.rs
  - cargo-pmcp/src/commands/agent/mod.rs
  - cargo-pmcp/src/commands/agent/new.rs
  - cargo-pmcp/src/commands/agent/dev.rs
  - cargo-pmcp/src/commands/team/mod.rs
  - cargo-pmcp/src/commands/team/dev.rs
  - cargo-pmcp/src/commands/package/mod.rs
  - cargo-pmcp/src/commands/package/show.rs
  - cargo-pmcp/src/commands/package/capture.rs
  - cargo-pmcp/src/main.rs
  - cargo-pmcp/tests/verb_help.rs
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04]

must_haves:
  truths:
    - "cargo-pmcp compiles with pmcp-agent, pmcp-team-servers, pmcp-package as dependencies AND passes make quality-gate (zero clippy warnings — no unused-arg warnings from stubs)"
    - "cargo pmcp agent --help, team --help, package --help all list their subcommands (asserted by an automated test, not manual)"
    - "Each new verb currently exits with an actionable not-yet-implemented message (stub), never a panic"
  artifacts:
    - path: "cargo-pmcp/Cargo.toml"
      provides: "Three workspace-crate dependencies with load-bearing feature flags"
      contains: "pmcp-agent"
    - path: "cargo-pmcp/src/commands/agent/mod.rs"
      provides: "AgentCommand group enum with async execute dispatch"
      contains: "enum AgentCommand"
    - path: "cargo-pmcp/src/commands/team/mod.rs"
      provides: "TeamCommand group enum with async execute dispatch"
      contains: "enum TeamCommand"
    - path: "cargo-pmcp/src/commands/package/mod.rs"
      provides: "PackageCommand group enum with async execute dispatch"
      contains: "enum PackageCommand"
    - path: "cargo-pmcp/tests/verb_help.rs"
      provides: "Automated --help surface assertions for the three groups"
      contains: "agent"
    - path: "cargo-pmcp/src/main.rs"
      provides: "Three Commands arms + three async dispatch wrappers"
      contains: "Commands::Agent"
  key_links:
    - from: "cargo-pmcp/src/main.rs"
      to: "commands::agent::AgentCommand"
      via: "Commands::Agent { command } dispatch arm"
      pattern: "Commands::Agent"
    - from: "cargo-pmcp/src/commands/mod.rs"
      to: "agent/team/package modules"
      via: "pub mod barrel declarations"
      pattern: "pub mod agent"
---

<objective>
Wire the shared foundation every Phase 110 verb needs: add the three
already-shipped workspace crates as cargo-pmcp dependencies (with the load-bearing
feature flags), create the `agent`/`team`/`package` nested subcommand groups
(mirroring the existing `workbook` group, D-01), and register three `enum Commands`
arms with async dispatch in `main.rs`. Handler bodies are stubbed (actionable
`bail!`, unused params underscore-prefixed to satisfy the zero-warning gate) so the
whole binary compiles and every verb is reachable — the four Wave-2 verb plans fill
the stubs in parallel with zero file conflicts.

Purpose: Establish the CLI surface + dependency wiring once, so the four verb
implementations own disjoint files and run in parallel.
Output: A compiling cargo-pmcp binary exposing `agent`, `team`, `package` groups,
green under `make quality-gate`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-CONTEXT.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-RESEARCH.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-PATTERNS.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-REVIEWS.md

<interfaces>
<!-- The command-group pattern to mirror three times. Read the analog once. -->
From cargo-pmcp/src/commands/workbook/mod.rs (the exact structure to copy):
  - `pub mod <sub>;` per subcommand handler file
  - `#[derive(Debug, Subcommand)] pub enum WorkbookCommand { Compile(compile::CompileArgs), Lint(lint::LintArgs) }`
  - `impl WorkbookCommand { pub fn execute(self, global_flags: &GlobalFlags) -> Result<()> { match self { ... } } }`
  - NOTE: workbook's execute is SYNC. The three new groups are ASYNC — signature is
    `pub async fn execute(self, global_flags: &GlobalFlags) -> Result<()>`.

From cargo-pmcp/src/main.rs (verified):
  - `dispatch_trait_based(command: Commands, global_flags: &GlobalFlags) -> Option<Result<()>>` is SYNC.
  - Async command groups are wrapped by a helper that builds a runtime: see
    `execute_landing` — `let runtime = tokio::runtime::Runtime::new()?; runtime.block_on(command.execute(...))`.
  - Existing arms to mirror: `Commands::Workbook { command } => command.execute(global_flags)` (sync),
    `Commands::Landing { command } => execute_landing(command, global_flags)` (async wrapper).
  - `is_target_consuming(command)` (main.rs:374-382) decides whether a command clobbers
    `PMCP_TARGET`/AWS env from the active configure target.

From cargo-pmcp/Cargo.toml (current, verified): version = "0.17.4"; already depends on
  pmcp (path ".."), reqwest, colored, tokio (full), url, secrecy; dev-dep assert_cmd = "2",
  predicates = "3".

REVIEW DECISION (Codex MEDIUM — global `--target`): `package capture` reaches its platform
  target via a CAPTURE-LOCAL `--target <name>` flag on `CaptureArgs` (passed as
  `resolve_target(explicit_name, ..)`), NOT a new top-level `GlobalFlags.--target`. This keeps
  the change local to `package/capture.rs` (owned by plan 110-05) and out of every other
  command. Therefore this plan does NOT modify `GlobalFlags`, and `Package` stays OUT of
  `is_target_consuming` (capture resolves its own target explicitly). The stub `CaptureArgs`
  carries the `--target` flag so the surface is stable for 110-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three workspace-crate dependencies and bump cargo-pmcp version</name>
  <files>cargo-pmcp/Cargo.toml</files>
  <read_first>
    - cargo-pmcp/Cargo.toml (current deps, version 0.17.4, existing pmcp path-dep line)
    - crates/pmcp-agent/Cargo.toml (confirm feature name `openai-compat` and package version 0.1.0)
    - crates/pmcp-team-servers/Cargo.toml (confirm feature names `runtime`, `http`; note `http` transitively enables `member-llm`)
    - crates/pmcp-package/Cargo.toml (confirm package version 0.1.0)
  </read_first>
  <action>
    Under `[dependencies]` add three path+version deps (path wins locally, version applies at
    publish per design §5 publish-order — cargo-pmcp now sits after all three):
    - `pmcp-agent = { version = "0.1", path = "../crates/pmcp-agent", features = ["openai-compat"] }`
    - `pmcp-team-servers = { version = "0.1", path = "../crates/pmcp-team-servers", features = ["runtime", "http"] }`
    - `pmcp-package = { version = "0.1", path = "../crates/pmcp-package" }`
    Feature-flag notes (Research Pitfall 1; Codex 110-01 LOW): `openai-compat` enables the default
    `agent dev` source; `runtime` enables the in-process team-dev server set; `http` enables the
    HTTP-first team-mcp serve path (`team dev --serve`) AND transitively enables `member-llm`
    (verified: `http = ["pmcp/streamable-http", "pmcp/http", "member-llm", "dep:url"]`), so
    `team dev --llm` works with `http` alone — do NOT also list `member-llm` (redundant, obscures
    the minimal feature graph). Add a one-line comment noting `http` pulls in `member-llm`.
    The `pmcp-package` version req MUST be the caret string `"0.1"` exactly (CLI-04 / D-04b) — a
    tripwire test in plan 110-05 asserts this literal string; do NOT use `=0.1.0`.
    Bump `[package] version` from `0.17.4` to `0.18.0` (minor — new verbs, D discretion).
  </action>
  <verify>
    <automated>cargo tree -p cargo-pmcp -i pmcp-agent >/dev/null && cargo tree -p cargo-pmcp -i pmcp-team-servers >/dev/null && cargo tree -p cargo-pmcp -i pmcp-package >/dev/null && cargo tree -p cargo-pmcp -e features -i pmcp-team-servers 2>/dev/null | grep -qi 'member-llm'</automated>
  </verify>
  <acceptance_criteria>
    - cargo-pmcp/Cargo.toml contains `pmcp-agent = { version = "0.1"` with `features = ["openai-compat"]`
    - cargo-pmcp/Cargo.toml contains `pmcp-team-servers` with `features = ["runtime", "http"]` (NOT an explicit `member-llm`)
    - cargo-pmcp/Cargo.toml contains the exact substring `pmcp-package = { version = "0.1"` (caret, not `=0.1.0`)
    - cargo-pmcp/Cargo.toml `[package] version` equals `0.18.0`
    - `cargo tree -p cargo-pmcp -i pmcp-agent` exits 0 and the team-servers feature tree still shows `member-llm` (via `http`)
  </acceptance_criteria>
  <done>All three crates resolve with correct features; `member-llm` is present transitively via `http`; version bumped to 0.18.0.</done>
</task>

<task type="auto">
  <name>Task 2: Create agent/team/package command-group modules with zero-warning stubbed handlers</name>
  <files>cargo-pmcp/src/commands/mod.rs, cargo-pmcp/src/commands/agent/mod.rs, cargo-pmcp/src/commands/agent/new.rs, cargo-pmcp/src/commands/agent/dev.rs, cargo-pmcp/src/commands/team/mod.rs, cargo-pmcp/src/commands/team/dev.rs, cargo-pmcp/src/commands/package/mod.rs, cargo-pmcp/src/commands/package/show.rs, cargo-pmcp/src/commands/package/capture.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/workbook/mod.rs (the group-enum + dispatch shape to mirror)
    - cargo-pmcp/src/commands/mod.rs (existing `pub mod workbook;` barrel line to mirror)
    - cargo-pmcp/src/commands/new.rs (GlobalFlags import path and `should_output()` usage)
  </read_first>
  <action>
    Add barrel lines `pub mod agent;`, `pub mod team;`, `pub mod package;` to commands/mod.rs
    (mirror the existing `pub mod workbook;`).
    Create three group modules mirroring workbook/mod.rs but with ASYNC execute:
    - agent/mod.rs: `pub mod new; pub mod dev;` + `#[derive(Debug, Subcommand)] pub enum AgentCommand { New(new::NewArgs), Dev(dev::DevArgs) }` + `impl AgentCommand { pub async fn execute(self, global_flags: &GlobalFlags) -> anyhow::Result<()> }` matching each arm to `new::execute(args, global_flags)` / `dev::execute(args, global_flags).await`.
    - team/mod.rs: `pub mod dev;` + `pub enum TeamCommand { Dev(dev::DevArgs) }` + async execute dispatching to `dev::execute(args, global_flags).await`.
    - package/mod.rs: `pub mod show; pub mod capture;` + `pub enum PackageCommand { Show(show::ShowArgs), Capture(capture::CaptureArgs) }` + async execute (`show::execute` is sync file-I/O; `capture::execute` is async `.await`).
    Create the five stub handler files. Each defines its `#[derive(Debug, clap::Args)]` Args struct
    with the pivotal flags named in CONTEXT (so downstream plans extend them) — the stub bodies do
    NOT use their params, so name the params `_args`/`_global_flags` to satisfy the zero-warning
    quality gate (Codex 110-01 HIGH — downstream plans rename to `args`/`global_flags` when they
    implement the body):
    - new.rs `NewArgs { name: String, #[arg(long)] path: Option<PathBuf>, #[arg(long)] force: bool }`, `pub fn execute(_args: NewArgs, _global_flags: &GlobalFlags) -> Result<()> { anyhow::bail!("agent new: implemented in plan 110-02") }`.
    - dev.rs `DevArgs { #[arg(long, default_value = "openai-compat")] source: String, #[arg(long)] endpoint: Option<String>, #[arg(long)] package: Option<PathBuf>, #[arg(long)] api_key_env: Option<String>, #[arg(long)] allow_insecure_http: bool }`, `pub async fn execute(_args: DevArgs, _global_flags: &GlobalFlags) -> Result<()> { anyhow::bail!("agent dev: implemented in plan 110-03") }`. (Plan 110-03 refines `source` to a clap `ValueEnum`.)
    - team/dev.rs `DevArgs { #[arg(long)] serve: bool, #[arg(long, default_value_t = 8080)] port: u16, #[arg(long)] llm: Option<String>, #[arg(long)] package: Option<PathBuf>, #[arg(long)] data_dir: Option<PathBuf>, #[arg(long)] allow_insecure_http: bool }`, async stub bail "team dev: implemented in plan 110-04".
    - package/show.rs `ShowArgs { path: PathBuf }`, sync stub bail "package show: implemented in plan 110-05".
    - package/capture.rs `CaptureArgs { path: PathBuf, #[arg(long)] target: Option<String> }`, async stub bail "package capture: implemented in plan 110-05". (The `--target` flag is the capture-local target selector — Codex MEDIUM; consumed by 110-05.)
    Import `GlobalFlags` via `use super::GlobalFlags;` in each group mod (workbook precedent). Do NOT
    reference `templates::agent` yet (plan 110-02 creates it). Keep every stub body a single `bail!`,
    never `unimplemented!`/`panic!`/`todo!` (actionable error, never a panic — D-03a/D-04a posture).
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp 2>&1 | tail -5; cargo clippy -p cargo-pmcp --all-targets 2>&1 | grep -i "warning: unused" && echo "UNUSED-WARN-FAIL" || echo "no unused-arg warnings"</automated>
  </verify>
  <acceptance_criteria>
    - cargo-pmcp/src/commands/mod.rs contains `pub mod agent;`, `pub mod team;`, `pub mod package;`
    - agent/mod.rs contains `enum AgentCommand` with `New` and `Dev` variants
    - team/mod.rs contains `enum TeamCommand` with a `Dev` variant
    - package/mod.rs contains `enum PackageCommand` with `Show` and `Capture` variants
    - package/capture.rs stub `CaptureArgs` carries `#[arg(long)] target: Option<String>`
    - Each stub handler body uses `anyhow::bail!` and names unused params `_args`/`_global_flags`
    - `grep -L 'unimplemented!\|todo!\|panic!' cargo-pmcp/src/commands/{agent,team,package}/*.rs` returns all stub files (none contain a panic macro)
    - `cargo build -p cargo-pmcp` exits 0 and `cargo clippy -p cargo-pmcp --all-targets` emits NO unused-argument warnings
  </acceptance_criteria>
  <done>All three command groups compile with zero clippy warnings; each subcommand parses and returns an actionable bail! stub, no panics.</done>
</task>

<task type="auto">
  <name>Task 3: Register Commands arms + async dispatch wrappers and assert the --help surfaces</name>
  <files>cargo-pmcp/src/main.rs, cargo-pmcp/tests/verb_help.rs</files>
  <read_first>
    - cargo-pmcp/src/main.rs (the `enum Commands` definition near the Workbook arm; `dispatch_trait_based`; the `execute_landing` async-wrapper helper; `is_target_consuming`)
    - cargo-pmcp/src/commands/agent/mod.rs (the AgentCommand type created in Task 2)
    - cargo-pmcp/tests/cli_acceptance.rs (assert_cmd/predicates shape for `--help` assertions)
  </read_first>
  <action>
    Add three variants to `enum Commands` mirroring the `Workbook` arm shape, each with a doc comment:
    - `Agent { #[command(subcommand)] command: commands::agent::AgentCommand }`
    - `Team { #[command(subcommand)] command: commands::team::TeamCommand }`
    - `Package { #[command(subcommand)] command: commands::package::PackageCommand }`
    In `dispatch_trait_based`, add three match arms wrapped via a helper mirroring `execute_landing`
    (build `tokio::runtime::Runtime::new()?` then `runtime.block_on(command.execute(global_flags))`):
    - `Commands::Agent { command } => execute_agent(command, global_flags)`
    - `Commands::Team { command } => execute_team(command, global_flags)`
    - `Commands::Package { command } => execute_package(command, global_flags)`
    Define `execute_agent`/`execute_team`/`execute_package` as sync fns that each own a
    `tokio::runtime::Runtime` and `block_on` the group's async `execute` (copy `execute_landing`'s body shape).
    Do NOT add these to `is_target_consuming` (capture resolves its platform target explicitly inside
    capture.rs via `resolve_target` using the capture-local `--target` flag, so it must NOT clobber
    `PMCP_TARGET`/AWS env — REVIEW DECISION, Codex MEDIUM).
    Create cargo-pmcp/tests/verb_help.rs (assert_cmd/predicates): assert `cargo pmcp agent --help`
    exit 0 and stdout lists `new` and `dev`; `team --help` lists `dev`; `package --help` lists `show`
    and `capture` (Codex 110-01 LOW — the `--help` truth must be automated, not manual).
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp && cargo test -p cargo-pmcp --test verb_help && cargo test -p cargo-pmcp --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - main.rs `enum Commands` contains `Agent {`, `Team {`, `Package {` variants
    - `dispatch_trait_based` contains arms `Commands::Agent`, `Commands::Team`, `Commands::Package`
    - main.rs defines `fn execute_agent`, `fn execute_team`, `fn execute_package` each calling `block_on`
    - main.rs `is_target_consuming` does NOT list `Commands::Package`
    - cargo-pmcp/tests/verb_help.rs asserts each group's `--help` lists its subcommands
    - `cargo test -p cargo-pmcp --test verb_help` passes and `cargo test -p cargo-pmcp --lib` passes (no regression)
  </acceptance_criteria>
  <done>`cargo pmcp agent|team|package --help` list their subcommands (test-asserted); running any verb reaches its stub bail!.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → cargo-pmcp CLI | User-supplied argv (subcommand names, paths, flags) crosses here |
| cargo-pmcp → workspace crates | In-repo first-party crates; no external/network trust added by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Denial of Service | stub handlers | mitigate | Every stub is a single `anyhow::bail!` — no `panic!`/`unimplemented!`; verb never aborts the process (acceptance criteria grep-gated) |
| T-110-02 | Elevation of Privilege | main.rs `is_target_consuming` | mitigate | `Package` is NOT target-consuming (capture uses a capture-local `--target`), so it cannot clobber `PMCP_TARGET`/`AWS_PROFILE` env for unrelated commands |
| T-110-SC | Tampering | cargo dependency additions | accept | No new external/registry packages — all three additions are first-party in-repo workspace crates already reviewed in Phases 107–109 (Research Package Legitimacy Audit: zero new registry packages). No slopcheck applies. |
</threat_model>

<verification>
- `cargo build -p cargo-pmcp` compiles with the three new deps and command groups.
- `cargo clippy -p cargo-pmcp --all-targets` — zero unused-arg warnings from stubs (Codex HIGH zero-warning fix).
- `cargo test -p cargo-pmcp --test verb_help` — the three `--help` surfaces list their subcommands.
- `cargo test -p cargo-pmcp --lib` — existing unit tests still pass (no regression).
- `make quality-gate` before commit (fmt/clippy/build/test — the real CI gate, not bare `cargo build`).

## Repository workflow notes (Codex HIGH #7)
- Contract-first: Phase 110 introduces NO new protocol types or tool contracts — every wire
  contract it touches (AgentPackage/TeamPackage, team tools) is owned + YAML-contracted by Phases
  107–109. No new provable-contracts YAML is required for these CLI verbs; `pmat comply check --path .`
  runs as the informational report per the 109-08 D-07 precedent.
- Publish readiness (Codex MEDIUM): cargo-pmcp now acquires three published-dependency requirements
  (pmcp-agent, pmcp-team-servers, pmcp-package) that publish BEFORE it (design §5). At release time run
  `cargo publish --dry-run -p cargo-pmcp` after those crates are on crates.io; the release workflow skips
  already-published crates gracefully (Research Pitfall 2). This is a release-checkpoint note, not a Phase-110 task.
</verification>

<success_criteria>
- cargo-pmcp depends on pmcp-agent, pmcp-team-servers, pmcp-package with correct features (member-llm via http).
- `agent`, `team`, `package` subcommand groups exist, are reachable from `main.rs`, and their `--help` surfaces are test-asserted.
- Every verb stub returns an actionable `bail!`, never a panic; zero clippy warnings from unused stub args.
- Version bumped to 0.18.0; `pmcp-package` pinned caret `"0.1"`; `package capture` carries a capture-local `--target`.
</success_criteria>

<output>
Create `.planning/phases/110-cargo-pmcp-agent-team-verbs/110-01-SUMMARY.md` when done
</output>
</content>
</invoke>
