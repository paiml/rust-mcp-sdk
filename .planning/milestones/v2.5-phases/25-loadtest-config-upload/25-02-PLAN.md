---
phase: 25-loadtest-config-upload
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - cargo-pmcp/src/commands/loadtest/upload.rs
  - cargo-pmcp/src/commands/loadtest/mod.rs
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - UPLD-01
  - UPLD-02
  - UPLD-03
  - VALD-01
  - VALD-02

must_haves:
  truths:
    - "cargo check succeeds for the cargo-pmcp crate with no errors"
    - "cargo clippy produces zero warnings for modified files"
    - "cargo fmt --check passes with no formatting issues"
    - "Existing loadtest tests still pass (cargo test -p cargo-pmcp loadtest)"
    - "All 9 requirement IDs are satisfied by the implementation"
  artifacts:
    - path: "cargo-pmcp/src/commands/loadtest/upload.rs"
      provides: "Compiling upload command implementation"
      contains: "upload_loadtest_config"
    - path: "cargo-pmcp/src/commands/loadtest/mod.rs"
      provides: "Compiling loadtest command with Upload variant"
      contains: "Upload"
---

<objective>
Verify the `cargo pmcp loadtest upload` implementation from Plan 25-01 compiles, passes quality gates, and satisfies all 9 requirements. Fix any compilation errors, clippy warnings, or formatting issues.

Purpose: Ensure the new upload command integrates correctly with the existing codebase before shipping. This is the quality gate plan that catches integration issues.

Output: Clean build with zero warnings, all existing tests passing, and all requirements verified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Files from Plan 25-01 to verify:**
- `cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs` — new upload_loadtest_config() function
- `cargo-pmcp/src/commands/loadtest/upload.rs` — new upload command module
- `cargo-pmcp/src/commands/loadtest/mod.rs` — updated with Upload variant

**Quality gates to pass:**
1. `cd cargo-pmcp && cargo check` — compilation
2. `cd cargo-pmcp && cargo clippy -- -D warnings` — zero warnings
3. `cd cargo-pmcp && cargo fmt --check` — formatting
4. `cd cargo-pmcp && cargo test` — existing tests still pass
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile check and fix any build errors</name>
  <files>
    cargo-pmcp/src/commands/loadtest/upload.rs
    cargo-pmcp/src/commands/loadtest/mod.rs
    cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs
  </files>
  <action>
Run `cd cargo-pmcp && cargo check 2>&1` and analyze the output.

Common issues to look for and fix:
1. **Import path for LoadTestConfig** — the upload.rs module uses `cargo_pmcp::loadtest::config::LoadTestConfig`. Verify this path is correct by checking how `run.rs` imports it. If it uses `crate::` prefix instead, adjust accordingly. Check both `use crate::` and `use cargo_pmcp::` patterns in the loadtest command modules.
2. **Auth credentials struct** — verify `credentials.access_token` is the correct field name. Read `auth.rs` to check the `Credentials` struct fields.
3. **Colored trait import** — verify `colored::Colorize` is available. Check the cargo-pmcp Cargo.toml for the `colored` dependency.
4. **Missing `use std::path::PathBuf`** in mod.rs if not already imported (check the existing import).

Fix ANY compilation error by reading the relevant source file and correcting the code. Do NOT leave compilation errors.

After fixes, run `cd cargo-pmcp && cargo check` again to confirm clean compilation.
  </action>
  <verify>
    <automated>cd cargo-pmcp && cargo check 2>&1 | tail -1 | grep -q "Finished"</automated>
    Run: cd cargo-pmcp && cargo check 2>&1
    Last line should contain "Finished" with no errors.
  </verify>
  <done>
    cargo check passes with zero errors. All import paths are correct, struct field access is valid, and the Upload variant dispatches correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run clippy, fmt, and existing tests</name>
  <files>
    cargo-pmcp/src/commands/loadtest/upload.rs
    cargo-pmcp/src/commands/loadtest/mod.rs
    cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs
  </files>
  <action>
Run these quality checks in sequence:

1. **Formatting:** `cd cargo-pmcp && cargo fmt --check 2>&1`
   - If formatting issues found, run `cd cargo-pmcp && cargo fmt` to fix them
   - Then re-check with `cargo fmt --check`

2. **Clippy:** `cd cargo-pmcp && cargo clippy -- -D warnings 2>&1`
   - Fix any warnings (unused imports, unnecessary clones, missing docs, etc.)
   - Common clippy issues:
     - `needless_borrow` on string references
     - `unused_imports` if colored or other traits aren't used
     - `missing_docs` if the crate requires doc comments
   - Re-run clippy after fixes until zero warnings

3. **Tests:** `cd cargo-pmcp && cargo test 2>&1`
   - All existing tests must pass
   - Pay special attention to loadtest config tests
   - If any test fails, investigate and fix the root cause

4. **Final verification:** Run all three again to confirm clean state:
   ```
   cd cargo-pmcp && cargo fmt --check && cargo clippy -- -D warnings && cargo test
   ```

Fix ANY issue found. The output must be clean on all three checks.
  </action>
  <verify>
    <automated>cd cargo-pmcp && cargo fmt --check 2>&1 | grep -qE "^$|All files" && cd cargo-pmcp && cargo clippy -- -D warnings 2>&1 | grep -q "Finished"</automated>
    Run: cd cargo-pmcp && cargo fmt --check && cargo clippy -- -D warnings && cargo test 2>&1 | tail -5
    All three commands should succeed with clean output.
  </verify>
  <done>
    All quality gates pass: cargo fmt (clean), cargo clippy (zero warnings), cargo test (all tests pass including existing loadtest config tests). The implementation integrates cleanly with the existing codebase.
  </done>
</task>

</tasks>

<verification>
1. `cd cargo-pmcp && cargo check` exits 0 (no compilation errors)
2. `cd cargo-pmcp && cargo fmt --check` exits 0 (no formatting issues)
3. `cd cargo-pmcp && cargo clippy -- -D warnings` exits 0 (no clippy warnings)
4. `cd cargo-pmcp && cargo test` exits 0 (all tests pass)
5. Requirement traceability confirmed:
   - CLI-01: Upload variant exists in LoadtestCommand with --server-id and path args
   - CLI-02: Validation error path with actionable message exists in upload.rs
   - CLI-03: Success path displays config_id and version from GraphQL response
   - CLI-04: Next-steps section with dashboard link and guidance exists
   - UPLD-01: upload_loadtest_config() GraphQL mutation function exists
   - UPLD-02: auth::get_credentials() call exists in upload.rs
   - UPLD-03: GraphQL variables include content, format, name, serverId
   - VALD-01: LoadTestConfig::from_toml() call exists before upload
   - VALD-02: Error message includes specific reason and fix guidance
</verification>

<success_criteria>
- Zero compilation errors
- Zero clippy warnings
- Zero formatting issues
- All existing tests pass
- No regressions in existing loadtest functionality
- All 9 requirements verified present in the code
</success_criteria>

<output>
After completion, create `.planning/phases/25-loadtest-config-upload/25-02-SUMMARY.md`
</output>
