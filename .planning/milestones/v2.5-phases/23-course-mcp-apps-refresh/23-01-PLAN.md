---
phase: 23-course-mcp-apps-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-course/src/part8-advanced/ch20-mcp-apps.md
  - pmcp-course/src/part8-advanced/ch20-01-ui-resources.md
  - pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md
  - pmcp-course/src/SUMMARY.md
autonomous: true
requirements:
  - CRAP-01
  - CRAP-02

must_haves:
  truths:
    - "Learner can scaffold a new MCP Apps project using cargo pmcp app new and run it with cargo pmcp preview"
    - "Learner understands WidgetDir file-based convention: .html files in widgets/ map to ui://app/{name} URIs"
    - "Learner can write a widget using window.mcpBridge.callTool() as the universal bridge API"
    - "Learner understands bridge communication flow: widget calls mcpBridge -> bridge script -> host -> MCP server -> response"
    - "Learner knows the three adapters (ChatGptAdapter, McpAppsAdapter, McpUiAdapter) and when to use each"
    - "Learner can implement the ResourceHandler pattern connecting WidgetDir to ChatGptAdapter"
    - "Ch 20 parent chapter introduces MCP Apps with WidgetDir paradigm, not the old UIResourceBuilder paradigm"
  artifacts:
    - path: "pmcp-course/src/part8-advanced/ch20-mcp-apps.md"
      provides: "Rewritten parent chapter introducing MCP Apps with WidgetDir, quick-start, and chapter overview"
      min_lines: 150
      contains: "WidgetDir"
    - path: "pmcp-course/src/part8-advanced/ch20-01-ui-resources.md"
      provides: "Hands-on tutorial for WidgetDir authoring and cargo pmcp app workflow"
      min_lines: 350
      contains: "cargo pmcp app new"
    - path: "pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md"
      provides: "Hands-on tutorial for bridge communication, mcpBridge API, and adapter pattern"
      min_lines: 350
      contains: "window.mcpBridge"
    - path: "pmcp-course/src/SUMMARY.md"
      provides: "Updated chapter titles and sub-chapter references for Ch 20"
      contains: "MCP Apps"
  key_links:
    - from: "pmcp-course/src/part8-advanced/ch20-mcp-apps.md"
      to: "pmcp-book/src/ch12-5-mcp-apps.md"
      via: "Cross-reference to book chapter for full reference documentation"
      pattern: "ch12-5-mcp-apps"
---

<objective>
Rewrite Ch 20 parent chapter and sub-chapters 20-01 and 20-02 for pmcp-course, replacing the old UIResourceBuilder/postMessage paradigm with the current WidgetDir/mcpBridge/adapter paradigm.

Purpose: Course learners need hands-on, teaching-oriented tutorials that walk them through building MCP Apps widgets using the current SDK. The existing Ch 20 sub-chapters use the old UIResourceBuilder and raw postMessage pattern -- they need to be replaced with content teaching WidgetDir file-based authoring, the cargo pmcp app workflow, and the mcpBridge bridge API.

Output: Three files rewritten in course teaching style:
- `ch20-mcp-apps.md` — Parent chapter with introduction, quick-start, and chapter overview
- `ch20-01-ui-resources.md` — Renamed to "Widget Authoring and Developer Workflow" (WidgetDir, cargo pmcp app new/build/preview)
- `ch20-02-tool-ui-association.md` — Renamed to "Bridge Communication and Adapters" (mcpBridge API, adapter pattern, MultiPlatformResource)

Also update SUMMARY.md to reflect the new sub-chapter titles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**CRITICAL STYLE GUIDANCE:** This is a COURSE chapter, not a book chapter. Read these files to match the course's teaching style:
- `pmcp-course/src/part1-foundations/ch02-03-calculator.md` — exemplar hands-on tutorial style
- `pmcp-course/src/part8-advanced/ch19-01-foundations.md` — Part 8 advanced chapter style
- `pmcp-course/src/part4-testing/ch12-01-remote.md` — sub-chapter detail level

The course style features:
- "Learning Objectives" at the top (bullet list of what learner will be able to do)
- Step-by-step instructions with numbered sequences the learner follows
- ASCII diagrams for architecture/flow visualization
- Tables for quick reference (key-value summaries)
- "Try this:" informal exercises woven throughout
- Teaching voice: "Let's...", "You'll...", "Try this...", second person throughout
- Code blocks with inline comments explaining each line
- Emphasis on DOING, not just reading

**Source material:** The book's Ch 12.5 (`pmcp-book/src/ch12-5-mcp-apps.md`, 1294 lines) contains all the reference content with the CURRENT APIs. Read it during execution to extract accurate details about:
- WidgetDir API (construction, discover(), read_widget(), inject_bridge_script())
- Bridge API (window.mcpBridge methods: callTool, readResource, getPrompt, notify, setState, etc.)
- Adapter pattern (UIAdapter trait, ChatGptAdapter, McpAppsAdapter, McpUiAdapter)
- Developer workflow (cargo pmcp app new/build/preview flags and behavior)
- ResourceHandler pattern (the 3-step read pattern all examples share)

The course chapter should TEACH the same material hands-on, not duplicate the reference prose.

**Key paradigm shift:** The old sub-chapters used UIResourceBuilder + raw postMessage. The new content must use:
- WidgetDir for file-based widget authoring (widgets/*.html -> ui://app/{name})
- window.mcpBridge.callTool() as the universal bridge API (NOT raw postMessage)
- ChatGptAdapter/McpAppsAdapter/McpUiAdapter for multi-platform support
- cargo pmcp app new/build/preview for the developer workflow

**SUMMARY.md update:** The current SUMMARY.md has Ch 20 as:
```
- [MCP Applications](./part8-advanced/ch20-applications.md)
  - [Building UIs for MCP](./part8-advanced/ch20-01-ui.md)
  - [High Availability](./part8-advanced/ch20-02-ha.md)
  - [Migration Strategies](./part8-advanced/ch20-03-migration.md)
  - [Chapter 20 Exercises](./part8-advanced/ch20-exercises.md)
```

This needs to change to reference the MCP Apps sub-chapters:
```
- [MCP Apps: Interactive Widgets](./part8-advanced/ch20-mcp-apps.md)
  - [Widget Authoring and Developer Workflow](./part8-advanced/ch20-01-ui-resources.md)
  - [Bridge Communication and Adapters](./part8-advanced/ch20-02-tool-ui-association.md)
  - [Example Walkthroughs](./part8-advanced/ch20-03-postmessage.md)
  - [Chapter 20 Exercises](./part8-advanced/ch20-exercises.md)
```

Note: We keep the existing filenames (ch20-01-ui-resources.md, ch20-02-tool-ui-association.md, ch20-03-postmessage.md) but with NEW content and NEW titles in SUMMARY.md. The old files (ch20-01-ui.md, ch20-02-ha.md, ch20-03-migration.md, ch20-applications.md) remain but are no longer linked from SUMMARY.md. The quiz embedding stays in ch20-mcp-apps.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Ch 20 parent chapter (ch20-mcp-apps.md) and update SUMMARY.md</name>
  <files>pmcp-course/src/part8-advanced/ch20-mcp-apps.md, pmcp-course/src/SUMMARY.md</files>
  <action>
Read the book's Ch 12.5 (`pmcp-book/src/ch12-5-mcp-apps.md`) Quick Start and introduction sections for reference content accuracy.

Read these course chapters for style reference:
- `pmcp-course/src/part1-foundations/ch02-first-server.md` (parent chapter style)
- `pmcp-course/src/part8-advanced/ch19-composition.md` (Part 8 parent chapter style)

**Rewrite `pmcp-course/src/part8-advanced/ch20-mcp-apps.md`** with this structure:

**1. Title: # MCP Apps: Interactive Widgets**

**2. Experimental Status Warning** (keep the existing warning box from the current file — it's still accurate)

**3. Introduction paragraph** — Teaching framing: "In this chapter, you'll learn to build rich, interactive widgets — charts, maps, games, dashboards — that run alongside your MCP tools."

**4. ## Learning Objectives** (bullet list):
- Scaffold an MCP Apps project with `cargo pmcp app new` and preview it
- Author widgets as HTML files using the `WidgetDir` file-based convention
- Communicate between widgets and your server using the `window.mcpBridge` bridge API
- Use adapters to deploy the same widget to ChatGPT, MCP Apps, and MCP-UI hosts
- Follow the chess, map, and dataviz examples hands-on

**5. ## Why Widgets?** — Brief teaching section: text vs visual information, when widgets add value. Include the Text vs Visual table from the current ch20-mcp-apps.md. Keep concise.

**6. ## Architecture at a Glance** — Simple ASCII diagram showing the four-layer stack:
```
Widget (HTML)  -->  Bridge (mcpBridge)  -->  Host (ChatGPT/MCP/MCP-UI)  -->  Server (tools)
```
Brief explanation: "Widgets are plain HTML files. The bridge script handles communication. The host renders the widget. The server processes tool calls."

**7. ## Quick Start: Your First Widget** — Condensed 3-step quick start adapted from book Ch 12.5:
- Step 1: `cargo pmcp app new my-widget-app && cd my-widget-app`
- Step 2: Show the generated project structure (widgets/hello.html, src/main.rs)
- Step 3: `cargo run &` then `cargo pmcp preview --url http://localhost:3000 --open`
- Brief note: "Edit widgets/hello.html, refresh browser — changes appear instantly (no server restart)"

**8. ## Feature Flag** — Show `pmcp = { version = "1.10", features = ["mcp-apps"] }` with brief explanation.

**9. ## Chapter Contents** — Updated list linking to the new sub-chapter content:
1. **[Widget Authoring and Developer Workflow](./ch20-01-ui-resources.md)** — WidgetDir, cargo pmcp app new/build/preview, hot-reload development
2. **[Bridge Communication and Adapters](./ch20-02-tool-ui-association.md)** — mcpBridge API, adapter pattern, multi-platform support
3. **[Example Walkthroughs](./ch20-03-postmessage.md)** — Chess, map, and dataviz hands-on

**10. ## Knowledge Check** — Keep the quiz embed: `{{#quiz ../quizzes/ch20-mcp-apps.toml}}`

**11. Navigation** — `*Continue to [Widget Authoring and Developer Workflow](./ch20-01-ui-resources.md) →*`

**Update `pmcp-course/src/SUMMARY.md`** — Replace the Ch 20 section:

FROM:
```
- [MCP Applications](./part8-advanced/ch20-applications.md)
  - [Building UIs for MCP](./part8-advanced/ch20-01-ui.md)
  - [High Availability](./part8-advanced/ch20-02-ha.md)
  - [Migration Strategies](./part8-advanced/ch20-03-migration.md)
  - [Chapter 20 Exercises](./part8-advanced/ch20-exercises.md)
```

TO:
```
- [MCP Apps: Interactive Widgets](./part8-advanced/ch20-mcp-apps.md)
  - [Widget Authoring and Developer Workflow](./part8-advanced/ch20-01-ui-resources.md)
  - [Bridge Communication and Adapters](./part8-advanced/ch20-02-tool-ui-association.md)
  - [Example Walkthroughs](./part8-advanced/ch20-03-postmessage.md)
  - [Chapter 20 Exercises](./part8-advanced/ch20-exercises.md)
```

IMPORTANT: The parent chapter should be engaging and give learners a taste of what they'll build. The quick start should feel achievable in 5 minutes. Use teaching voice throughout.
  </action>
  <verify>
    <automated>grep -c "WidgetDir\|cargo pmcp app\|mcpBridge\|Learning Objectives" pmcp-course/src/part8-advanced/ch20-mcp-apps.md | xargs test 3 -le</automated>
    Verify ch20-mcp-apps.md contains: Learning Objectives, WidgetDir mention, cargo pmcp app quick start, chapter contents linking to all 3 sub-chapters, quiz embed, feature flag.
    Verify SUMMARY.md Ch 20 section references ch20-mcp-apps.md, ch20-01-ui-resources.md, ch20-02-tool-ui-association.md, ch20-03-postmessage.md.
    Run: grep -c "ch20-mcp-apps\|ch20-01-ui-resources\|ch20-02-tool-ui-association\|ch20-03-postmessage" pmcp-course/src/SUMMARY.md
    Should return 4.
  </verify>
  <done>
    Ch 20 parent chapter rewritten with WidgetDir/mcpBridge paradigm, quick-start, learning objectives, and updated chapter contents. SUMMARY.md updated with new sub-chapter titles and file references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch 20-01 — Widget Authoring and Developer Workflow</name>
  <files>pmcp-course/src/part8-advanced/ch20-01-ui-resources.md</files>
  <action>
Read the book's Ch 12.5 (`pmcp-book/src/ch12-5-mcp-apps.md`) sections on WidgetDir API, Hot-Reload Development, ResourceHandler Pattern, and Developer Workflow for reference content accuracy.

Read these course chapters for style reference:
- `pmcp-course/src/part1-foundations/ch02-03-calculator.md` (hands-on tutorial exemplar)
- `pmcp-course/src/part1-foundations/ch02-04-code-walkthrough.md` (code walkthrough style)

**Rewrite `pmcp-course/src/part8-advanced/ch20-01-ui-resources.md`** — completely replace the old UIResourceBuilder content with WidgetDir-based content:

**1. Title: # Widget Authoring and Developer Workflow**

**2. Introduction** — "In this section, you'll build widgets using the WidgetDir file-based convention and learn the full cargo pmcp development cycle."

**3. ## Learning Objectives** (for this sub-chapter):
- Create a new MCP Apps project with `cargo pmcp app new`
- Author widgets as standalone HTML files in the `widgets/` directory
- Use the hot-reload development workflow (edit HTML, refresh browser)
- Implement the `ResourceHandler` pattern that connects widgets to the MCP protocol
- Build distribution artifacts with `cargo pmcp app build`

**4. ## The WidgetDir Convention** (## The WidgetDir Convention)
- Teaching framing: "Widgets are just HTML files. Drop them in a folder. The server does the rest."
- ### File-to-URI Mapping
  - Table: `widgets/board.html` -> `ui://app/board`, `widgets/map.html` -> `ui://app/map`
  - "Each .html file in widgets/ becomes an MCP resource automatically"
- ### WidgetDir API Walkthrough
  - Construction: `WidgetDir::new("widgets")`
  - Discovery: `widget_dir.discover()` — show `WidgetEntry` fields (filename, uri, path)
  - Reading: `widget_dir.read_widget("board")` — "Reads from disk every time. No cache. This is intentional."
  - Bridge injection: `WidgetDir::inject_bridge_script()` — "The bridge script is added automatically by the adapter"
  - "Try this:" exercise — create a second widget file and check that discover() finds it

**5. ## Hands-On: Scaffold Your First MCP App** (## Hands-On: Scaffold Your First MCP App)
- ### Step 1: Create the Project
  - `cargo pmcp app new my-widget-app && cd my-widget-app`
  - Show the generated directory structure
  - Explain each file's purpose
- ### Step 2: Explore the Generated Code
  - Walk through `src/main.rs` with annotated code (the ServerBuilder + WidgetDir + ChatGptAdapter pattern)
  - Explain the three-step ResourceHandler pattern: extract name -> read from disk -> transform with adapter
  - Walk through `widgets/hello.html` — "Notice: no bridge script tag. The adapter injects it for you."
- ### Step 3: Run and Preview
  - `cargo run &` then `cargo pmcp preview --url http://localhost:3000 --open`
  - "Type a name, click the button — the widget calls your hello tool and displays the response"
  - "Try this:" — edit the CSS in hello.html, refresh the browser, see the change instantly
- ### Step 4: Add a Second Widget
  - Create `widgets/counter.html` with a simple counter that calls a tool
  - Add a `counter` tool handler to main.rs
  - "Refresh the preview — your new widget appears in the widget selector"

**6. ## The ResourceHandler Pattern** (## The ResourceHandler Pattern)
- Teaching framing: "Every MCP Apps server needs this pattern. Learn it once, use it everywhere."
- Show the complete ResourceHandler implementation with teaching annotations for each section:
  1. Extract widget name from `ui://app/{name}` URI
  2. Read HTML from disk via `widget_dir.read_widget(name)`
  3. Transform via `adapter.transform()` to inject the bridge script
- Show the `list()` method using `widget_dir.discover()`
- "This exact pattern appears in the chess, map, and dataviz examples. Once you understand it, you can build any widget-based MCP server."

**7. ## Hot-Reload Development** (## Hot-Reload Development)
- Teaching framing: "MCP Apps development feels like frontend development."
- The hot-reload workflow:
  1. Start server: `cargo run`
  2. Open preview: `cargo pmcp preview --url http://localhost:3000 --open`
  3. Edit HTML in `widgets/`
  4. Refresh browser — changes appear instantly
- "Why does this work? WidgetDir reads from disk on every request. There's no cache."
- "When do you need to restart the server? Only when you change Rust code (tool handlers, main.rs). Widget changes are always instant."

**8. ## Building for Distribution** (## Building for Distribution)
- Teaching framing: "Ready to share your widget? Build produces two deployment artifacts."
- ### cargo pmcp app build
  - `cargo pmcp app build --url https://my-server.example.com`
  - Show the output: `dist/manifest.json` and `dist/landing.html`
  - Explain each artifact briefly:
    - manifest.json: ChatGPT-compatible app directory listing
    - landing.html: Standalone demo page with mock bridge
- ### Individual Commands
  - `cargo pmcp app manifest --url ...` for just the manifest
  - `cargo pmcp app landing` for just the landing page
- CLI flags table: `--url`, `--logo`, `--widget`, `--output`
- "Try this:" — run `cargo pmcp app build` and open the landing.html in your browser

**9. ## Preview Command Deep Dive** (## Preview Command Deep Dive)
- Flags table: `--url`, `--port`, `--open`, `--tool`, `--theme`, `--locale`, `--widgets-dir`
- "Try this:" — test with `--theme dark` and `--locale ja-JP`

**10. ## Summary and Next Steps**
- Key concepts recap as bullet list
- "In the next section, you'll learn how widgets communicate with your server through the bridge API, and how adapters make your widgets work across different hosts."
- Navigation: `*Continue to [Bridge Communication and Adapters](./ch20-02-tool-ui-association.md) →*`

IMPORTANT: This sub-chapter replaces the OLD UIResourceBuilder content entirely. Do NOT mention UIResourceBuilder anywhere. Use WidgetDir throughout. Teaching voice. Every section is hands-on — the learner DOES something, then the explanation follows.
  </action>
  <verify>
    <automated>wc -l pmcp-course/src/part8-advanced/ch20-01-ui-resources.md | awk '{if ($1 >= 350) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines, need 350+"; exit ($1 < 350)}'</automated>
    Verify key content:
    Run: grep -cE "(WidgetDir|cargo pmcp app new|hot-reload|ResourceHandler|cargo pmcp preview|manifest.json|landing.html|Try this)" pmcp-course/src/part8-advanced/ch20-01-ui-resources.md
    Should return 10+ matches.
    Verify NO old API references:
    Run: grep -c "UIResourceBuilder" pmcp-course/src/part8-advanced/ch20-01-ui-resources.md
    Should return 0.
  </verify>
  <done>
    Ch 20-01 rewritten as "Widget Authoring and Developer Workflow" covering WidgetDir convention, scaffolding walkthrough, ResourceHandler pattern, hot-reload development, cargo pmcp app build, and preview deep dive. All in course teaching style with hands-on steps. Old UIResourceBuilder content completely replaced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Ch 20-02 — Bridge Communication and Adapters</name>
  <files>pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md</files>
  <action>
Read the book's Ch 12.5 (`pmcp-book/src/ch12-5-mcp-apps.md`) sections on Bridge Communication (window.mcpBridge API), Communication Flow, and Multi-Platform Adapter Pattern for reference content accuracy.

Read these course chapters for style reference:
- `pmcp-course/src/part1-foundations/ch02-03-calculator.md` (hands-on tutorial exemplar)
- `pmcp-course/src/part2-design/ch06-01-resources-vs-tools.md` (comparison teaching style)

**Rewrite `pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md`** — completely replace the old .with_ui()/ToolUIMetadata content with bridge/adapter content:

**1. Title: # Bridge Communication and Adapters**

**2. Introduction** — "In this section, you'll learn how widgets talk to your server through the bridge API, and how adapters let you deploy the same widget to ChatGPT, standard MCP hosts, and MCP-UI hosts."

**3. ## Learning Objectives** (for this sub-chapter):
- Use `window.mcpBridge.callTool()` to call server tools from widget JavaScript
- Handle bridge initialization, errors, and async responses
- Understand how different hosts (ChatGPT, MCP Apps, MCP-UI) use different bridge mechanisms
- Choose the right adapter for your target host
- Use `MultiPlatformResource` to serve all hosts from a single server

**4. ## The Bridge API** (## The Bridge API)
- Teaching framing: "Your widget needs to talk to your MCP server. The bridge makes this simple — one API that works everywhere."
- ### window.mcpBridge
  - Core operations table: `callTool(name, args)`, `readResource(uri)`, `getPrompt(name, args)`, `notify(method, params)`
  - "These four methods work on ALL hosts. Write them once, they work everywhere."
- ### Hands-On: Making Your First Bridge Call
  - Show a minimal widget that calls a tool:
    ```javascript
    const result = await window.mcpBridge.callTool('hello', { name: 'World' });
    document.getElementById('result').textContent = result.greeting;
    ```
  - "Try this:" — modify the hello widget from Ch 20-01 to add a second tool call
- ### Error Handling
  - Always wrap in try/catch pattern
  - Show the error handling code from the book
- ### Bridge Initialization
  - The `mcpBridgeReady` event pattern
  - "In most cases the bridge is available immediately, but use the event for widgets that load asynchronously"
- ### ChatGPT-Only Extras
  - State management table: `setState()`, `getState()`
  - Communication table: `sendMessage()`, `openExternal()`
  - Display modes table: `requestDisplayMode()`, `requestClose()`, `notifyIntrinsicHeight()`
  - Environment properties: `theme`, `locale`, `displayMode`, `toolInput`, `toolOutput`
  - "These extras are available when running inside ChatGPT. On other hosts, stick to the four core methods."

**5. ## Communication Flow** (## Communication Flow)
- ASCII diagram showing the full flow: Widget -> bridge script -> host -> MCP server -> result -> widget
  (adapt from book Ch 12.5 "Communication Flow" section)
- Explain each step in teaching voice
- Platform differences: "ChatGPT uses window.openai under the hood. MCP Apps uses postMessage with JSON-RPC. You don't need to know this — the bridge handles it."

**6. ## The Adapter Pattern** (## The Adapter Pattern)
- Teaching framing: "One widget. Three hosts. Zero code changes. Here's how."
- ### Why Adapters?
  - The problem: different hosts use different communication mechanisms
  - ChatGPT: window.openai + text/html+skybridge
  - MCP Apps: postMessage + text/html+mcp
  - MCP-UI: postMessage + text/html
  - "The adapter injects the right bridge script at serve time"
- ### The UIAdapter Trait
  - Show the trait with 5 methods, briefly explain each
  - TransformedResource struct explained
- ### ChatGptAdapter
  - Construction with WidgetMeta
  - MIME type: text/html+skybridge
  - "This is what all three shipped examples use — ChatGPT is the most mature host"
  - WidgetMeta fields table: prefers_border, domain, description, csp
- ### McpAppsAdapter
  - Construction with optional CSP
  - MIME type: text/html+mcp
  - "Minimal bridge — core MCP operations only"
- ### McpUiAdapter
  - Construction with format selection (Html, Url, RemoteDom)
  - Three output formats table
  - "MCP-UI hosts have extra features: sendIntent, openLink"
- ### Choosing an Adapter
  - Comparison table: Adapter | Host | MIME Type | Bridge API | Best For
  - "Start with ChatGptAdapter. Switch to MultiPlatformResource when you need to support multiple hosts."

**7. ## Hands-On: Multi-Platform Support** (## Hands-On: Multi-Platform Support)
- Teaching framing: "Let's serve the same widget to multiple hosts."
- Show MultiPlatformResource construction:
  ```rust
  let multi = MultiPlatformResource::new(uri, name, html)
      .with_all_adapters();
  ```
- Show for_host() usage for specific hosts
- "Try this:" — modify the hello server from Ch 20-01 to use McpAppsAdapter instead of ChatGptAdapter and observe the different MIME type

**8. ## Summary and Next Steps**
- Key concepts recap
- "In the next section, you'll walk through the chess, map, and dataviz examples hands-on to see these patterns in real-world applications."
- Navigation: `*Continue to [Example Walkthroughs](./ch20-03-postmessage.md) →*`

IMPORTANT: This sub-chapter replaces the OLD Tool-UI Association content entirely. Do NOT mention `.with_ui()`, `ToolUIMetadata`, or `ui/resourceUri` metadata — those are the old paradigm. The bridge API is window.mcpBridge, NOT raw postMessage. Teaching voice throughout. Accurate to book Ch 12.5 API details.
  </action>
  <verify>
    <automated>wc -l pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md | awk '{if ($1 >= 350) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines, need 350+"; exit ($1 < 350)}'</automated>
    Verify key content:
    Run: grep -cE "(mcpBridge|callTool|ChatGptAdapter|McpAppsAdapter|McpUiAdapter|UIAdapter|MultiPlatformResource|postMessage|window.openai|Try this)" pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md
    Should return 12+ matches.
    Verify NO old API references:
    Run: grep -c "with_ui\|ToolUIMetadata\|ui/resourceUri" pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md
    Should return 0.
  </verify>
  <done>
    Ch 20-02 rewritten as "Bridge Communication and Adapters" covering mcpBridge API (core + ChatGPT extras), communication flow, UIAdapter trait, three concrete adapters, and MultiPlatformResource. All in course teaching style with hands-on exercises. Old .with_ui()/ToolUIMetadata content completely replaced.
  </done>
</task>

</tasks>

<verification>
1. `pmcp-course/src/part8-advanced/ch20-mcp-apps.md` rewritten with WidgetDir/mcpBridge paradigm, > 150 lines
2. `pmcp-course/src/part8-advanced/ch20-01-ui-resources.md` rewritten as widget authoring tutorial, > 350 lines
3. `pmcp-course/src/part8-advanced/ch20-02-tool-ui-association.md` rewritten as bridge/adapter tutorial, > 350 lines
4. `pmcp-course/src/SUMMARY.md` updated with new Ch 20 structure
5. NO references to old APIs: UIResourceBuilder, .with_ui(), ToolUIMetadata, ui/resourceUri
6. All content uses current APIs: WidgetDir, window.mcpBridge, ChatGptAdapter, McpAppsAdapter, McpUiAdapter
7. Teaching style matches existing course chapters (learning objectives, step-by-step, ASCII diagrams, "Try this" exercises)
8. Cross-references book Ch 12.5 for full reference documentation
9. Covers requirements CRAP-01 (WidgetDir, cargo pmcp app workflow, adapter pattern) and CRAP-02 (bridge communication patterns)
</verification>

<success_criteria>
- A course learner can follow the tutorials start-to-finish and build a working MCP Apps widget
- The chapters teach through doing — every concept is introduced via a hands-on step the learner performs
- Writing style matches the established course voice (second person, teaching-oriented, "Let's...", "Try this...")
- All API details (method names, types, flags, MIME types) are accurate to the book Ch 12.5 reference
- The three adapters and their differences are clearly explained with a decision table
</success_criteria>

<output>
After completion, create `.planning/phases/23-course-mcp-apps-refresh/23-01-SUMMARY.md`
</output>
