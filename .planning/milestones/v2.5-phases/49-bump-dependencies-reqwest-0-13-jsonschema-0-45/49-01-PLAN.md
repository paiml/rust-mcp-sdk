---
phase: 49-bump-dependencies-reqwest-0-13-jsonschema-0-45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/mcp-tester/Cargo.toml
  - crates/mcp-preview/Cargo.toml
  - cargo-pmcp/Cargo.toml
  - crates/mcp-tester/src/tester.rs
  - cargo-pmcp/src/commands/deploy/init.rs
  - cargo-pmcp/src/templates/oauth/proxy.rs
  - examples/26-server-tester/src/tester.rs
autonomous: true
requirements: [DEP-01]

must_haves:
  truths:
    - "cargo build --features full compiles successfully with reqwest 0.13 and jsonschema 0.45"
    - "make quality-gate passes with zero warnings"
    - "All .form() call sites compile (form feature enabled where needed)"
    - "Template strings generate correct reqwest 0.13 dependency lines for new projects"
    - "MSRV is 1.83.0 to satisfy jsonschema 0.45 requirement"
  artifacts:
    - path: "Cargo.toml"
      provides: "Root crate reqwest 0.13, jsonschema 0.45, MSRV 1.83"
      contains: 'reqwest.*0.13'
    - path: "crates/mcp-tester/Cargo.toml"
      provides: "mcp-tester reqwest 0.13"
      contains: 'reqwest.*0.13'
    - path: "crates/mcp-preview/Cargo.toml"
      provides: "mcp-preview reqwest 0.13"
      contains: 'reqwest.*0.13'
    - path: "cargo-pmcp/Cargo.toml"
      provides: "cargo-pmcp reqwest 0.13"
      contains: 'reqwest.*0.13'
  key_links:
    - from: "Cargo.toml"
      to: "src/client/oauth.rs"
      via: "form feature enables .form() method"
      pattern: 'features.*form'
    - from: "cargo-pmcp/Cargo.toml"
      to: "cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs"
      via: "form feature enables .form() method"
      pattern: 'features.*form'
---

<objective>
Upgrade reqwest from 0.12 to 0.13 and jsonschema from 0.38 to 0.45 across the entire workspace.

Purpose: Align with latest dependency versions. jsonschema 0.45 depends on reqwest 0.13, so upgrading both eliminates duplicate reqwest versions in the dependency tree (except oauth2 which pins 0.12). Also picks up reqwest's default rustls backend and security improvements.

Output: All four workspace Cargo.toml files updated, feature flags renamed, template strings corrected, deprecated methods proactively renamed, quality gate passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-bump-dependencies-reqwest-0-13-jsonschema-0-45/49-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all Cargo.toml files and MSRV</name>
  <files>Cargo.toml, crates/mcp-tester/Cargo.toml, crates/mcp-preview/Cargo.toml, cargo-pmcp/Cargo.toml</files>
  <action>
Update all four Cargo.toml files for the reqwest 0.12 -> 0.13 upgrade and jsonschema 0.38 -> 0.45 upgrade. Apply these exact changes:

**Root Cargo.toml:**
- Change `rust-version = "1.82.0"` to `rust-version = "1.83.0"` (jsonschema 0.45 requires MSRV 1.83)
- Change `reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }` to `reqwest = { version = "0.13", default-features = false, features = ["json", "rustls", "form"] }`
  - `rustls-tls` renamed to `rustls` in reqwest 0.13
  - `form` now opt-in (required for OAuth .form() calls in src/client/oauth.rs and src/client/auth.rs)
- Change `jsonschema = { version = "0.38", optional = true }` to `jsonschema = { version = "0.45", optional = true }`

**crates/mcp-tester/Cargo.toml:**
- Change `reqwest = { version = "0.12", features = ["json", "stream", "rustls-tls"], default-features = false }` to `reqwest = { version = "0.13", features = ["json", "stream", "rustls"], default-features = false }`
  - No `form` needed -- mcp-tester does not use .form() on RequestBuilder

**crates/mcp-preview/Cargo.toml:**
- Change `reqwest = { version = "0.12", features = ["json"] }` to `reqwest = { version = "0.13", features = ["json"] }`
  - Uses default TLS (not default-features = false), so gets rustls by default in 0.13

**cargo-pmcp/Cargo.toml:**
- Change `reqwest = { version = "0.12", features = ["json", "multipart", "rustls-tls"], default-features = false }` to `reqwest = { version = "0.13", features = ["json", "multipart", "rustls", "form"], default-features = false }`
  - `form` needed for OAuth flows in cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs

After editing, run `cargo check --features full` to verify the workspace compiles. Accept that oauth2 5.0.0 will keep reqwest 0.12 in the lockfile as a transitive dependency -- this is expected and documented.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo check --features full 2>&1 | tail -5</automated>
  </verify>
  <done>All four Cargo.toml files reference reqwest 0.13 with correct feature names. MSRV is 1.83.0. jsonschema is 0.45. Workspace compiles successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Update source code (deprecated methods + template strings) and run quality gate</name>
  <files>crates/mcp-tester/src/tester.rs, cargo-pmcp/src/commands/deploy/init.rs, cargo-pmcp/src/templates/oauth/proxy.rs, examples/26-server-tester/src/tester.rs</files>
  <action>
**Deprecated method rename (mcp-tester):**
In `crates/mcp-tester/src/tester.rs`, rename all 3 occurrences of `danger_accept_invalid_certs` to `tls_danger_accept_invalid_certs`. These are on lines ~126, ~147, and ~1106. The method is soft-deprecated in reqwest 0.13 but should be proactively renamed to avoid future breakage and because `make quality-gate` runs pedantic clippy lints.

**Deprecated method rename (example, outside workspace):**
In `examples/26-server-tester/src/tester.rs`, apply the same rename on lines ~114, ~135, and ~1071. This is excluded from the workspace build but should stay consistent.

**Template string updates (cargo-pmcp):**
In `cargo-pmcp/src/commands/deploy/init.rs`:
- Line ~880: Change `reqwest = {{ version = "0.12", default-features = false, features = ["json", "rustls-tls"] }}` to `reqwest = {{ version = "0.13", default-features = false, features = ["json", "rustls"] }}`
- Line ~918: Same change as line 880.

In `cargo-pmcp/src/templates/oauth/proxy.rs`:
- Line ~474: Change `reqwest = {{ version = "0.12", default-features = false, features = ["json", "rustls-tls"] }}` to `reqwest = {{ version = "0.13", default-features = false, features = ["json", "rustls", "form"] }}` (form needed because the OAuth proxy template generates code that uses .form())

**Final verification:**
Run `make quality-gate` to verify everything passes (formatting, clippy with pedantic+nursery, build, tests, doctests).
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && make quality-gate 2>&1 | tail -20</automated>
  </verify>
  <done>All deprecated methods renamed. Template strings generate correct reqwest 0.13 lines. `make quality-gate` passes with zero warnings.</done>
</task>

</tasks>

<verification>
1. `cargo check --features full` compiles without errors
2. `make quality-gate` passes (fmt, clippy, build, test, doctest)
3. `grep -r "rustls-tls" Cargo.toml crates/*/Cargo.toml cargo-pmcp/Cargo.toml` returns no results
4. `grep -r 'reqwest.*0\.12' Cargo.toml crates/*/Cargo.toml cargo-pmcp/Cargo.toml` returns no results
5. `grep -r 'danger_accept_invalid_certs' crates/mcp-tester/src/` returns no results (only tls_ prefixed version)
6. `cargo tree -d 2>/dev/null | grep reqwest` shows expected dual versions (0.12 via oauth2, 0.13 direct)
</verification>

<success_criteria>
- Workspace builds and all tests pass with reqwest 0.13 and jsonschema 0.45
- Zero clippy warnings under pedantic+nursery lint configuration
- MSRV correctly declared as 1.83.0
- Template strings produce correct dependencies for newly scaffolded projects
- No references to old feature name `rustls-tls` in any Cargo.toml
</success_criteria>

<output>
After completion, create `.planning/phases/49-bump-dependencies-reqwest-0-13-jsonschema-0-45/49-01-SUMMARY.md`
</output>
