---
phase: 65-examples-cleanup-protocol-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - README.md
  - examples/08_server_resources.rs
  - examples/11_progress_countdown.rs
  - examples/12_prompt_workflow_progress.rs
  - examples/32_simd_parsing_performance.rs
  - examples/40_middleware_demo.rs
  - examples/47_multiple_clients_parallel.rs
  - examples/48_structured_output_schema.rs
  - examples/54_hybrid_workflow_execution.rs
  - examples/58_oauth_transport_to_tools.rs
  - examples/59_dynamic_resource_workflow.rs
  - examples/60_resource_only_steps.rs
  - examples/61_observability_middleware.rs
  - examples/client.rs
  - examples/currency_server.rs
  - examples/refactored_server_example.rs
  - examples/server.rs
  - examples/test_currency_server.rs
  - examples/21_macro_tools.rs.disabled
autonomous: true
requirements:
  - EXMP-02
  - PROT-01

must_haves:
  truths:
    - "Every .rs file in examples/ either has a [[example]] entry in Cargo.toml or has been deleted"
    - "No .disabled files exist in examples/"
    - "The README.md MCP badge shows 2025-11-25, not 2025-03-26"
    - "The README.md compatibility table shows 2025-11-25"
    - "The README.md feature list shows 2025-11-25"
  artifacts:
    - path: "Cargo.toml"
      provides: "[[example]] entries for all surviving orphan files"
      contains: "[[example]]"
    - path: "README.md"
      provides: "Corrected protocol version references"
      contains: "2025-11-25"
  key_links:
    - from: "Cargo.toml [[example]] entries"
      to: "examples/*.rs files"
      via: "path field matching actual file on disk"
      pattern: 'path = "examples/.*\.rs"'
---

<objective>
Audit all 24 orphan and unnumbered example files (register viable ones in Cargo.toml, attempt minor fixes for broken ones before deleting truly unrecoverable ones), delete the .disabled file, and fix the README protocol version badge in 3 locations.

Purpose: Resolve EXMP-02 (orphan registration) and PROT-01 (protocol accuracy) so that every example file is runnable and the README accurately reflects the SDK's protocol support.
Output: Updated Cargo.toml with new [[example]] entries, cleaned examples/ directory, and corrected README.md protocol version.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-examples-cleanup-protocol-accuracy/65-CONTEXT.md
@Cargo.toml
@README.md
</context>

<threat_model>
This is a documentation and configuration phase. No new runtime code, no new dependencies, no authentication changes, no user data handling. Threat surface is effectively zero. The only risk is accidentally removing a public example that users reference by name -- mitigated by git history and the fact that Plan 02 will renumber everything anyway.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-audit discovery -- identify helper modules and classify all example files</name>
  <read_first>
    - Cargo.toml (current [[example]] entries, starting at line ~220)
    - All .rs files in examples/ (at minimum the first 20 lines of each to check for mod/path attributes)
  </read_first>
  <files>
    (no files modified -- this is a discovery step that informs Task 2)
  </files>
  <action>
    Before auditing individual orphan files, perform three discovery passes:

    **Pass 1: Identify helper/module files that should NOT be treated as standalone examples.**
    Search for non-runnable helper scripts that other examples import:
    ```bash
    # Check for #[path = "..."] attributes referencing other .rs files in examples/
    grep -rn '#\[path' examples/*.rs

    # Check for mod declarations that reference sibling .rs files (not inline mod tests {})
    grep -rn '^mod ' examples/*.rs | grep -v 'mod tests' | grep -v 'mod wasm_example'

    # Check for include! macros referencing .rs files
    grep -rn 'include!' examples/*.rs

    # List any .rs files that do NOT have a fn main() -- these are likely helper modules
    for f in examples/*.rs; do
      if ! grep -q 'fn main' "$f" && ! grep -q '#\[tokio::main\]' "$f" && ! grep -q 'async fn main' "$f"; then
        echo "NO MAIN: $f"
      fi
    done
    ```
    Any file identified as a helper module (no main function, imported by other examples via `mod` or `#[path]`) should be flagged as "helper -- do not register as [[example]], do not rename with role prefix in Plan 02."

    **Pass 2: Systematic import analysis for feature flag discovery.**
    For each orphan/unnumbered .rs file, extract feature requirements by analyzing imports:
    ```bash
    for f in examples/{08_server_resources,11_progress_countdown,12_prompt_workflow_progress,32_simd_parsing_performance,40_middleware_demo,47_multiple_clients_parallel,48_structured_output_schema,54_hybrid_workflow_execution,58_oauth_transport_to_tools,59_dynamic_resource_workflow,60_resource_only_steps,61_observability_middleware,client,currency_server,refactored_server_example,server,test_currency_server}.rs; do
      echo "=== $(basename $f) ==="
      grep -E 'use pmcp::|use pmcp_' "$f" | head -20
    done
    ```
    Map imports to features using these rules:
    - `pmcp::server::workflow` or `WorkflowEngine` -> required-features = ["full"] (workflow is behind full)
    - `pmcp::server::streamable_http` or `StreamableHttpService` -> required-features = ["streamable-http"]
    - `pmcp::server::composition` -> required-features = ["composition"]
    - `pmcp::server::mcp_apps` or `McpApp` -> required-features = ["mcp-apps"]
    - `pmcp::server::auth` or `OAuthProvider` -> required-features = ["full"]
    - `pmcp::server::middleware` -> required-features = ["full"]
    - `TypedTool` or `JsonSchema` (from schemars) -> required-features = ["schema-generation"]
    - `pmcp::client` imports only -> no special features (client is always available)
    - `pmcp::server` basic imports -> no special features (server is always available)
    - Multiple features needed -> use required-features = ["full"]

    **Pass 3: Quick viability check.**
    Run a single workspace-level compilation check to identify immediately broken files:
    ```bash
    cargo check --examples --features full 2>&1 | grep "^error" | head -30
    ```
    This gives a quick signal about which files have compilation errors before doing per-file audits.

    Record all findings (helper files, feature mappings, compilation status) in a scratch note to use in Task 2.
  </action>
  <verify>
    <automated>bash -c 'echo "Discovery complete -- verify helper scan ran"; grep -rn "^mod " examples/*.rs | grep -v "mod tests" | grep -v "mod wasm_example" | wc -l | tr -d " "'</automated>
  </verify>
  <acceptance_criteria>
    - The helper module scan has been executed (grep for `#[path]`, `^mod`, `include!`, and missing `fn main`)
    - The import analysis has been executed for all 17 orphan + 7 unnumbered files
    - The `cargo check --examples --features full` viability check has been run
    - Findings are recorded (which files are helpers, which features each file needs, which files fail compilation)
  </acceptance_criteria>
  <done>All example files have been classified as: standalone-viable, standalone-fixable, standalone-broken, or helper-module. Feature requirements have been determined by import analysis. Compilation status is known.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve all 24 orphan/unnumbered example files using discovery findings</name>
  <read_first>
    - Cargo.toml (current [[example]] entries, starting at line ~220)
    - examples/08_server_resources.rs
    - examples/11_progress_countdown.rs
    - examples/12_prompt_workflow_progress.rs
    - examples/32_simd_parsing_performance.rs
    - examples/40_middleware_demo.rs
    - examples/47_multiple_clients_parallel.rs
    - examples/48_structured_output_schema.rs
    - examples/54_hybrid_workflow_execution.rs
    - examples/58_oauth_transport_to_tools.rs
    - examples/59_dynamic_resource_workflow.rs
    - examples/60_resource_only_steps.rs
    - examples/61_observability_middleware.rs
    - examples/client.rs
    - examples/currency_server.rs
    - examples/refactored_server_example.rs
    - examples/server.rs
    - examples/test_currency_server.rs
    - examples/21_macro_tools.rs.disabled
  </read_first>
  <files>
    Cargo.toml,
    examples/08_server_resources.rs,
    examples/11_progress_countdown.rs,
    examples/12_prompt_workflow_progress.rs,
    examples/32_simd_parsing_performance.rs,
    examples/40_middleware_demo.rs,
    examples/47_multiple_clients_parallel.rs,
    examples/48_structured_output_schema.rs,
    examples/54_hybrid_workflow_execution.rs,
    examples/58_oauth_transport_to_tools.rs,
    examples/59_dynamic_resource_workflow.rs,
    examples/60_resource_only_steps.rs,
    examples/61_observability_middleware.rs,
    examples/client.rs,
    examples/currency_server.rs,
    examples/refactored_server_example.rs,
    examples/server.rs,
    examples/test_currency_server.rs,
    examples/21_macro_tools.rs.disabled
  </files>
  <action>
    Per D-03 and D-04: Using the classification from Task 1, resolve all 24 files. For each file, follow this decision tree:

    **If classified as helper-module (no fn main, imported by others):**
    - Do NOT register as [[example]] in Cargo.toml
    - Do NOT delete -- other examples depend on it
    - Note in SUMMARY that this file is a helper and should be excluded from renaming in Plan 02

    **If classified as standalone-viable (compiles with correct features):**
    1. Add a `[[example]]` entry to Cargo.toml using the feature flags determined by import analysis in Task 1
    2. Use the import-derived feature flags, NOT ad-hoc guessing

    **If classified as standalone-fixable (fails compilation but fixable with minor changes):**
    Before deleting, attempt these common fixes in order:
    1. **Import path updates**: If the error is `use pmcp::old_module::Thing` and the type moved to `pmcp::new_module::Thing`, update the import path
    2. **Deprecated API updates**: If the error is a removed method like `Server::builder()`, replace with the current API (e.g., `ServerCoreBuilder::new()`)
    3. **Missing trait imports**: Add missing `use` statements for traits that were re-exported differently
    4. **Type signature changes**: Update function signatures to match current trait definitions
    5. After each fix attempt, run `cargo check --example {name} --features full` to verify
    6. If the fix takes more than 3-4 line changes, the example is too stale -- proceed to deletion

    **If classified as standalone-broken (does not compile, not trivially fixable):**
    - Delete the file with `rm`
    - Note in SUMMARY what the example did and why it was deleted (for potential future recreation)

    **If classified as near-duplicate of an already-registered example:**
    - Delete the file
    - Note which registered example it duplicates

    The 17 orphan files to audit (NOT in Cargo.toml):
    - 08_server_resources.rs (duplicate prefix with registered 08_logging.rs)
    - 11_progress_countdown.rs (duplicate prefix with registered 11_request_cancellation.rs)
    - 12_prompt_workflow_progress.rs (duplicate prefix with registered 12_error_handling.rs)
    - 32_simd_parsing_performance.rs (duplicate prefix with registered 32_typed_tools.rs)
    - 40_middleware_demo.rs
    - 47_multiple_clients_parallel.rs
    - 48_structured_output_schema.rs
    - 54_hybrid_workflow_execution.rs
    - 58_oauth_transport_to_tools.rs
    - 59_dynamic_resource_workflow.rs
    - 60_resource_only_steps.rs
    - 61_observability_middleware.rs
    - client.rs
    - currency_server.rs
    - refactored_server_example.rs
    - server.rs
    - test_currency_server.rs

    The 7 unnumbered files (per D-04, treated same as orphans):
    - client.rs, server.rs, currency_server.rs, hotel_gallery.rs, conference_venue_map.rs, refactored_server_example.rs, test_currency_server.rs

    Also delete examples/21_macro_tools.rs.disabled (dead disabled file per Pitfall 12).

    Add new [[example]] entries in Cargo.toml maintaining alphabetical order by name within the existing [[example]] section. Each entry format:
    ```toml
    [[example]]
    name = "example_name"
    path = "examples/example_name.rs"
    required-features = ["feature-name"]  # only if needed, based on import analysis
    ```

    Also remove the commented-out [[example]] block for 21_macro_tools (around line 306-309 of Cargo.toml) since the .rs.disabled file is being deleted.

    **SUMMARY output requirement:** The SUMMARY must include:
    - Full list of files registered (with their feature flags)
    - Full list of files deleted (with reason: broken, duplicate, or stale)
    - Full list of files identified as helpers (excluded from [[example]] registration)
    - Full list of minor fixes applied to fixable examples (what was changed and why)
  </action>
  <verify>
    <automated>
      bash -c 'ORPHANS=0; for f in examples/*.rs; do name=$(basename "$f" .rs); if ! grep -q "name = \"$name\"" Cargo.toml; then echo "ORPHAN: $name"; ORPHANS=$((ORPHANS+1)); fi; done; echo "Orphan count: $ORPHANS"; test $ORPHANS -eq 0 && echo "PASS" || echo "FAIL"'
    </automated>
  </verify>
  <acceptance_criteria>
    - Running `for f in examples/*.rs; do name=$(basename "$f" .rs); grep -q "name = \"$name\"" Cargo.toml || echo "MISSING: $name"; done` produces zero output
    - The file examples/21_macro_tools.rs.disabled does NOT exist (deleted)
    - The commented-out [[example]] block for 21_macro_tools (lines ~306-309 in Cargo.toml) is removed
    - No .disabled files exist: `ls examples/*.disabled 2>/dev/null` returns empty
    - `cargo check --features full 2>&1 | grep -c "error"` returns 0 (workspace still compiles)
    - For any example that was fixed (not deleted), `cargo check --example {name} --features full` passes individually
  </acceptance_criteria>
  <done>Every .rs file in examples/ has a corresponding [[example]] entry in Cargo.toml with correct required-features (determined by import analysis). No .disabled files remain. Any fixable examples were fixed before resorting to deletion. The workspace compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Fix README protocol version from 2025-03-26 to 2025-11-25</name>
  <read_first>
    - README.md (lines 15-20 for badge, lines 375-385 for feature list, lines 635-645 for compatibility table)
    - src/types/protocol/version.rs (to confirm LATEST_PROTOCOL_VERSION = "2025-11-25")
  </read_first>
  <files>README.md</files>
  <action>
    Per D-07: Update exactly 3 locations in README.md where "2025-03-26" appears, changing each to "2025-11-25":

    1. **Line 17 (badge URL)**: Change
       `[![MCP Compatible](https://img.shields.io/badge/MCP-v2025--03--26-blue.svg)](https://modelcontextprotocol.io)`
       to
       `[![MCP Compatible](https://img.shields.io/badge/MCP-v2025--11--25-blue.svg)](https://modelcontextprotocol.io)`

    2. **Line 381 (feature list bullet)**: Change
       `- **Protocol v2025-03-26**: Full alignment with the latest MCP specification, backward compatible with `2024-11-05``
       to
       `- **Protocol v2025-11-25**: Full alignment with the latest MCP specification, backward compatible with `2024-11-05``

    3. **Line 641 (compatibility table)**: Change
       `| Protocol Version | 2025-03-26 | 2025-03-26 (+ 2024-11-05 compat) |`
       to
       `| Protocol Version | 2025-11-25 | 2025-11-25 (+ 2024-11-05 compat) |`

    After editing, verify no occurrences of "2025-03-26" remain in README.md.
  </action>
  <verify>
    <automated>bash -c 'COUNT=$(grep -c "2025-03-26" README.md); echo "Remaining 2025-03-26 count: $COUNT"; test $COUNT -eq 0 && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "2025-03-26" README.md` returns 0
    - `grep -c "2025-11-25" README.md` returns at least 3
    - `grep "MCP-v2025--11--25" README.md` matches the badge line
    - `grep "Protocol v2025-11-25" README.md` matches the feature list line
    - `grep "Protocol Version | 2025-11-25" README.md` matches the compatibility table line
  </acceptance_criteria>
  <done>All 3 README.md references to the protocol version show 2025-11-25, matching LATEST_PROTOCOL_VERSION in src/types/protocol/version.rs.</done>
</task>

</tasks>

<verification>
1. Zero orphan example files: every .rs in examples/ has a [[example]] entry in Cargo.toml
2. Zero .disabled files in examples/
3. Zero occurrences of "2025-03-26" in README.md
4. At least 3 occurrences of "2025-11-25" in README.md
5. `cargo check --features full` succeeds (no compilation errors introduced)
6. Helper/module files (if any) were identified and excluded from [[example]] registration
7. Fixable examples were attempted before deletion
</verification>

<success_criteria>
- `for f in examples/*.rs; do name=$(basename "$f" .rs); grep -q "name = \"$name\"" Cargo.toml || echo "MISSING: $name"; done` produces empty output
- `grep -c "2025-03-26" README.md` returns 0
- `ls examples/*.disabled 2>/dev/null` returns empty
- `cargo check --features full` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/65-examples-cleanup-protocol-accuracy/65-01-SUMMARY.md`

The SUMMARY must include:
- List of files registered with feature flags (from import analysis)
- List of files deleted with reasons
- List of helper/module files excluded from [[example]] registration (if any)
- List of minor fixes applied to salvaged examples
- Any files that Plan 02 should exclude from renaming (helpers)
</output>
