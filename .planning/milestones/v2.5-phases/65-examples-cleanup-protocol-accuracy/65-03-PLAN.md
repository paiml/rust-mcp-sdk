---
phase: 65-examples-cleanup-protocol-accuracy
plan: 03
type: execute
wave: 3
depends_on: ["65-02"]
files_modified:
  - examples/README.md
autonomous: true
requirements:
  - EXMP-01

must_haves:
  truths:
    - "examples/README.md describes PMCP examples, not Spin framework"
    - "Every .rs file in examples/ is listed in examples/README.md with a description and a runnable cargo run command"
    - "Examples are organized by Role (Server, Client, Transport, Middleware) then by MCP capability then by complexity"
    - "Each example entry has a copy-paste cargo run --example command with required features"
    - "A migration reference section maps old example names to new role-prefixed names"
  artifacts:
    - path: "examples/README.md"
      provides: "Complete PMCP example index organized by category"
      contains: "cargo run --example"
  key_links:
    - from: "examples/README.md"
      to: "examples/*.rs files"
      via: "example names in cargo run commands match actual filenames (without .rs)"
      pattern: "cargo run --example [scmt][0-9]"
    - from: "examples/README.md"
      to: "Cargo.toml [[example]] entries"
      via: "required-features in README match required-features in Cargo.toml"
      pattern: "--features"
---

<objective>
Replace the current examples/README.md (which contains the Spin framework README) with an accurate PMCP example index organized by Role -> Capability -> Complexity, including a migration reference section mapping old names to new names.

Purpose: Resolve EXMP-01 so that developers browsing the examples/ directory see a categorized guide to all PMCP examples with runnable commands. The migration section helps existing users find examples that were renumbered. This is the most visible credibility fix in Phase 65.
Output: A complete examples/README.md that serves as the definitive example reference for the PMCP SDK.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-examples-cleanup-protocol-accuracy/65-CONTEXT.md
@.planning/phases/65-examples-cleanup-protocol-accuracy/65-02-SUMMARY.md
@Cargo.toml
</context>

<threat_model>
This is a pure documentation task -- replacing one markdown file. No code changes, no dependencies, no runtime impact. Zero threat surface. The only risk is listing examples with wrong feature flags, which would cause copy-paste commands to fail. Mitigated by reading Cargo.toml required-features directly.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the complete examples/README.md index with migration reference</name>
  <read_first>
    - examples/README.md (current content -- the Spin framework README to be fully replaced)
    - .planning/phases/65-examples-cleanup-protocol-accuracy/65-02-SUMMARY.md (the rename mapping table showing all final filenames AND old->new name mappings)
    - Cargo.toml (all [[example]] entries with their required-features)
    - At least the first 5-10 lines of each example .rs file (to extract the purpose/description from doc comments or initial code)
  </read_first>
  <files>examples/README.md</files>
  <action>
    Per D-01 and D-02: Replace the entire contents of examples/README.md with a PMCP example index.

    **Structure (per D-01 -- Three-level hierarchy: Role -> Capability -> Complexity):**

    ```markdown
    # PMCP SDK Examples

    Examples demonstrating the [PMCP](https://crates.io/crates/pmcp) Model Context Protocol SDK.

    ## Server Examples

    ### Tools
    [examples ordered basic -> advanced]

    ### Resources
    [examples ordered basic -> advanced]

    ### Prompts
    [examples ordered basic -> advanced]

    ### Sampling
    ...

    ### Typed Tools
    ...

    ### Macros
    ...

    ### Workflow
    ...

    ### Apps
    ...

    ### Auth
    ...

    ## Client Examples

    ### Initialization
    ...

    ### Tools
    ...

    ### Resources
    ...

    ### Prompts
    ...

    ## Transport Examples

    ### WebSocket
    ...

    ### SSE
    ...

    ### Streamable HTTP
    ...

    ## Middleware Examples

    ### Basic Middleware
    ...

    ### Auth Middleware
    ...

    ### Error Recovery
    ...

    ### Observability
    ...

    ## Standalone Example Projects

    [Brief section for subdirectory projects like mcp-apps-chess, mcp-apps-map, etc.]

    ## Migration Reference

    [Old name -> New name mapping table for users upgrading from previous versions]
    ```

    **Entry format (per D-02 -- 2-3 lines each):**
    ```markdown
    **s01_basic_server** - Minimal MCP server with a single tool handler
    ```bash
    cargo run --example s01_basic_server
    ```
    ```

    For examples with required-features:
    ```markdown
    **t01_websocket_transport** - WebSocket transport for MCP client-server communication
    ```bash
    cargo run --example t01_websocket_transport --features websocket
    ```
    ```

    **Content rules:**
    1. Read the first doc comment or function signature of each example to write a one-line description
    2. For required-features, read directly from the [[example]] entry in Cargo.toml -- do NOT guess
    3. Every .rs file in examples/ MUST appear in the README -- verify by comparing the file list with the README entries
    4. Include a header note about prerequisites (Rust toolchain, feature flags concept)
    5. Do NOT include subdirectory examples (mcp-apps-map, mcp-apps-chess, etc.) in the main sections -- those have their own Cargo.toml and are standalone projects. Mention them in a "Standalone Example Projects" section at the bottom with a brief note.
    6. For examples that need external services (DynamoDB, Redis, OAuth), add a note like "(requires running Redis)" after the description

    **Migration Reference section:**
    At the bottom of the README, add a "Migration Reference" section with a table mapping old example names to new role-prefixed names. Read the old->new mapping from the 65-02-SUMMARY.md. Format:

    ```markdown
    ## Migration Reference

    If you were using examples from a previous version, this table maps old names to new ones:

    | Old Name | New Name |
    |----------|----------|
    | 01_client_initialize | c01_client_initialize |
    | 02_server_basic | s01_basic_server |
    | ... | ... |
    ```

    Include ALL renamed examples (not just the ones that changed roles). This helps users who have bookmarks, scripts, or documentation referencing the old numbered names.

    **After writing, verify completeness:**
    ```bash
    # Count .rs files in examples/
    ls examples/*.rs | wc -l
    # Count cargo run --example lines in README
    grep -c "cargo run --example" examples/README.md
    # These two numbers should match
    ```
  </action>
  <verify>
    <automated>
      bash -c 'RS_COUNT=$(ls examples/*.rs 2>/dev/null | wc -l | tr -d " "); README_COUNT=$(grep -c "cargo run --example" examples/README.md 2>/dev/null); echo "Files: $RS_COUNT, README entries: $README_COUNT"; test "$RS_COUNT" = "$README_COUNT" && echo "PASS: counts match" || echo "FAIL: counts differ"'
    </automated>
  </verify>
  <acceptance_criteria>
    - `head -1 examples/README.md` contains "PMCP" (not "Spin")
    - `grep -c "cargo run --example" examples/README.md` equals the number of .rs files in examples/
    - `grep "## Server Examples" examples/README.md` matches (role section exists)
    - `grep "## Client Examples" examples/README.md` matches (role section exists)
    - `grep "## Transport Examples" examples/README.md` matches (role section exists)
    - `grep "## Middleware Examples" examples/README.md` matches (role section exists)
    - `grep "## Migration Reference" examples/README.md` matches (migration section exists)
    - Every example name in Cargo.toml appears in examples/README.md: `grep -oP 'name = "\K[^"]+' Cargo.toml | while read n; do grep -q "$n" examples/README.md || echo "MISSING: $n"; done` produces no output
    - No occurrence of "Spin" in examples/README.md: `grep -ci "spin" examples/README.md` returns 0
    - Required features in README match Cargo.toml: for any example with `--features X` in README, the Cargo.toml entry for that example has `required-features = ["X"]`
    - Migration table contains "Old Name" and "New Name" columns: `grep "Old Name.*New Name" examples/README.md` matches
  </acceptance_criteria>
  <done>examples/README.md is a complete PMCP example index organized by Role -> Capability -> Complexity, with a runnable cargo run command for every example, a migration reference mapping old names to new names, matching the actual Cargo.toml entries. No trace of Spin framework content remains.</done>
</task>

</tasks>

<verification>
1. `head -1 examples/README.md` mentions PMCP, not Spin
2. Number of `cargo run --example` lines in README equals number of .rs files in examples/
3. All four role sections exist: Server, Client, Transport, Middleware
4. Every [[example]] name from Cargo.toml appears in the README
5. `grep -ci "spin" examples/README.md` returns 0
6. Migration Reference section exists with old->new name mappings
</verification>

<success_criteria>
- examples/README.md is a PMCP example index (not Spin)
- Every .rs file in examples/ is listed with a description and runnable command
- Organization follows Role -> Capability -> Complexity hierarchy
- Feature flags in README commands match Cargo.toml required-features
- Migration Reference section maps all old names to new role-prefixed names
</success_criteria>

<output>
After completion, create `.planning/phases/65-examples-cleanup-protocol-accuracy/65-03-SUMMARY.md`
</output>
