---
phase: 67.2-code-mode-derive-hardening
plan: 05
subsystem: validation
tags: [graphql, refactoring, performance, code-mode, validation-pipeline]

# Dependency graph
requires:
  - phase: 67.2-code-mode-derive-hardening (plans 01-03)
    provides: Arc<dyn PolicyEvaluator>, Result-returning constructors, async validation path
provides:
  - check_config_authorization helper method for DRY config authorization
  - Eliminated double GraphQL parse in async validation fallback
affects: [67.2-06, pmcp-code-mode validation pipeline consumers]

# Tech tracking
tech-stack:
  added: []
  patterns: [extracted-helper-method, box-large-result-err]

key-files:
  created: []
  modified:
    - crates/pmcp-code-mode/src/validation.rs

key-decisions:
  - "Used Box<ValidationResult> in check_config_authorization return type to satisfy clippy::result_large_err lint"

patterns-established:
  - "check_config_authorization: shared config authorization helper for sync and async validation paths"

requirements-completed: [CMSUP-10]

# Metrics
duration: 6min
completed: 2026-04-12
---

# Phase 67.2 Plan 05: Eliminate Double GraphQL Parse Summary

**Extracted check_config_authorization helper to eliminate redundant GraphQL re-parse in async validation fallback path**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-12T16:53:14Z
- **Completed:** 2026-04-12T16:59:38Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Extracted mutation/query authorization checks (blocklist, allowlist) into `check_config_authorization` private helper method
- Refactored `validate_graphql_query` (sync) to delegate to helper instead of inline checks
- Fixed `validate_graphql_query_async` fallback to reuse already-parsed `query_info` via helper instead of calling `validate_graphql_query` (which re-parsed the same query)
- Zero behavioral change: same authorization logic, same error messages, all 65 tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract config authorization checks and eliminate double parse** - `fcb2a032` (refactor)

## Files Created/Modified
- `crates/pmcp-code-mode/src/validation.rs` - Added `check_config_authorization` helper, refactored sync and async paths to use it, eliminated double parse in async fallback

## Decisions Made
- Used `Result<(), Box<ValidationResult>>` return type for `check_config_authorization` to satisfy clippy's `result_large_err` lint (ValidationResult is 184+ bytes). Callers dereference with `*failure`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Clippy result_large_err lint on check_config_authorization return type**
- **Found during:** Task 1 (after implementing the helper method)
- **Issue:** `Result<(), ValidationResult>` triggers clippy::result_large_err because ValidationResult is 184+ bytes
- **Fix:** Changed return type to `Result<(), Box<ValidationResult>>` and wrapped Err values with `Box::new()`, callers dereference with `*failure`
- **Files modified:** crates/pmcp-code-mode/src/validation.rs
- **Verification:** `cargo clippy -p pmcp-code-mode -- -D warnings` passes cleanly
- **Committed in:** fcb2a032 (part of task commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix for clippy lint)
**Impact on plan:** Minor type wrapper change to satisfy linter. No behavioral impact.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- validation.rs refactoring complete, ready for Plan 06 (quality + cleanup wave)
- All validation tests passing (65/65)
- No clippy warnings

---
*Phase: 67.2-code-mode-derive-hardening*
*Completed: 2026-04-12*
