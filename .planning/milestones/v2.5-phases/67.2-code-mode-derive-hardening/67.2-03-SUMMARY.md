---
phase: 67.2-code-mode-derive-hardening
plan: 03
subsystem: auth
tags: [token-error, result-propagation, compile-fail, trybuild, breaking-change, migration]

# Dependency graph
requires:
  - phase: 67.2-02
    provides: darling context_from and language attributes, dual code gen paths
provides:
  - HmacTokenGenerator::new returns Result<Self, TokenError> (non-panicking)
  - ValidationPipeline constructors return Result<Self, TokenError>
  - register_code_mode_tools returns Result<ServerBuilder, TokenError>
  - Compile-fail trybuild tests for missing token_secret and code_executor
  - CHANGELOG.md with breaking change migration guide
affects: [67.2-04 trybuild tests, 67.2-05 scope-chain performance]

# Tech tracking
tech-stack:
  added: []
  patterns: [Result propagation from leaf constructor through pipeline to generated code, per-field error messages with derive attribution]

key-files:
  created:
    - crates/pmcp-code-mode-derive/tests/ui/missing_token_secret.rs
    - crates/pmcp-code-mode-derive/tests/ui/missing_token_secret.stderr
    - crates/pmcp-code-mode-derive/tests/ui/missing_code_executor.rs
    - crates/pmcp-code-mode-derive/tests/ui/missing_code_executor.stderr
    - crates/pmcp-code-mode/CHANGELOG.md
    - crates/pmcp-code-mode-derive/CHANGELOG.md
  modified:
    - crates/pmcp-code-mode/src/token.rs
    - crates/pmcp-code-mode/src/types.rs
    - crates/pmcp-code-mode/src/lib.rs
    - crates/pmcp-code-mode/src/validation.rs
    - crates/pmcp-code-mode/tests/property_tests.rs
    - crates/pmcp-code-mode-derive/src/lib.rs
    - crates/pmcp-code-mode-derive/tests/code_mode_tests.rs
    - crates/pmcp-code-mode-derive/tests/ui/valid_code_mode.rs
    - crates/pmcp-code-mode-derive/tests/ui/valid_code_mode_context.rs
    - crates/pmcp-code-mode-derive/tests/ui/valid_code_mode_language.rs
    - crates/pmcp-code-mode-derive/tests/ui/missing_config.stderr
    - crates/pmcp-code-mode-derive/tests/ui/missing_policy_evaluator.stderr

key-decisions:
  - "HmacTokenGenerator::new returns Result instead of panicking -- prevents runtime panics on misconfigured secrets"
  - "register_code_mode_tools returns Result -- breaking change acceptable at v0.1.0 pre-release"
  - "Error messages per-field with type hints and #[derive(CodeMode)] attribution for clarity"

patterns-established:
  - "Result propagation chain: HmacTokenGenerator::new -> ValidationPipeline::new -> register_code_mode_tools"
  - "Compile-fail test pattern: one .rs + one .stderr per missing required field"

requirements-completed: [CMSUP-09]

# Metrics
duration: 10min
completed: 2026-04-12
---

# Phase 67.2 Plan 03: HmacTokenGenerator Result + Compile-Fail Tests Summary

**Make HmacTokenGenerator::new non-panicking (Result), propagate through pipeline and derive macro, add compile-fail tests for missing fields, document breaking API change**

## Performance

- **Duration:** 10 min
- **Started:** 2026-04-12T16:29:14Z
- **Completed:** 2026-04-12T16:39:38Z
- **Tasks:** 2
- **Files created:** 6
- **Files modified:** 12

## Accomplishments

- Changed `HmacTokenGenerator::new` from `assert!`-panicking to returning `Result<Self, TokenError>` with descriptive `SecretTooShort` variant
- Added `TokenError` enum to `types.rs` with `thiserror::Error` derive, re-exported from crate root
- Changed all 4 `ValidationPipeline` constructors (`new`, `from_token_secret`, `with_policy_evaluator`, `from_token_secret_with_policy`) to return `Result<Self, TokenError>`
- Updated derive macro generated `register_code_mode_tools` to return `Result<ServerBuilder, TokenError>` in both code paths (with and without `context_from`)
- Improved derive macro error messages: missing fields now show `#[derive(CodeMode)] requires field 'X' (type: Y)` with type hints
- Created 2 new compile-fail trybuild tests (missing_token_secret, missing_code_executor) with correct stderr snapshots
- Updated all 3 existing compile-pass trybuild tests for Result return type
- Updated 2 existing compile-fail stderr snapshots for new error message format
- Updated 5 property tests for Result-returning constructors
- Created CHANGELOG.md for both crates with breaking change migration guide

## Task Commits

Each task was committed atomically:

1. **Task 1: Make HmacTokenGenerator::new return Result and add TokenError** - `2b3f929b` (feat)
2. **Task 2: Propagate Result through pipeline+derive, compile-fail tests, migration docs** - `51d1b327` (feat)

## Files Created/Modified

- `crates/pmcp-code-mode/src/types.rs` - Added `TokenError::SecretTooShort` enum
- `crates/pmcp-code-mode/src/token.rs` - `new`/`new_from_bytes`/`from_env` return Result, tests use assertion pattern
- `crates/pmcp-code-mode/src/lib.rs` - Re-export `TokenError`
- `crates/pmcp-code-mode/src/validation.rs` - All 4 constructors return `Result<Self, TokenError>`, test helpers unwrap
- `crates/pmcp-code-mode/tests/property_tests.rs` - All `HmacTokenGenerator::new` and `with_policy_evaluator` calls unwrap
- `crates/pmcp-code-mode/CHANGELOG.md` - Breaking changes and additions documented
- `crates/pmcp-code-mode-derive/src/lib.rs` - `register_code_mode_tools` returns Result in both paths, improved error messages
- `crates/pmcp-code-mode-derive/tests/code_mode_tests.rs` - Added 2 new compile-fail tests (9 total)
- `crates/pmcp-code-mode-derive/tests/ui/missing_token_secret.rs` + `.stderr` - New compile-fail test
- `crates/pmcp-code-mode-derive/tests/ui/missing_code_executor.rs` + `.stderr` - New compile-fail test
- `crates/pmcp-code-mode-derive/tests/ui/valid_code_mode*.rs` - Updated for Result return type
- `crates/pmcp-code-mode-derive/tests/ui/missing_config.stderr` - Updated for new error format
- `crates/pmcp-code-mode-derive/tests/ui/missing_policy_evaluator.stderr` - Updated for new error format
- `crates/pmcp-code-mode-derive/CHANGELOG.md` - Breaking changes and additions documented

## Decisions Made

- `HmacTokenGenerator::new` returns `Result` instead of panicking -- prevents runtime panics on misconfigured secrets (T-67.2-07 mitigation)
- `register_code_mode_tools` returns `Result` -- breaking change acceptable at v0.1.0 pre-release, documented with migration guide
- Per-field error messages with type hints instead of generic "missing required fields" -- explicitly attributes requirement to `#[derive(CodeMode)]` for developer clarity

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated property tests for Result API change**
- **Found during:** Task 1 (HmacTokenGenerator::new now returns Result)
- **Issue:** 5 property tests (`hmac_roundtrip_any_code`, `hmac_detects_code_modification`, `hmac_rejects_bitflipped_tokens`, `hmac_rejects_wrong_secret`, `default_deny_without_noop` + `noop_evaluator_allows_query`) called `HmacTokenGenerator::new` or `ValidationPipeline::with_policy_evaluator` which now return Result
- **Fix:** Added `.unwrap()` to all affected constructor calls
- **Files modified:** `crates/pmcp-code-mode/tests/property_tests.rs`
- **Verification:** All 14 property tests pass
- **Committed in:** 51d1b327 (Task 2 commit -- grouped with other Result propagation)

**2. [Rule 1 - Bug] Updated existing compile-fail stderr snapshots for new error format**
- **Found during:** Task 2 (error message format change)
- **Issue:** `missing_config.stderr` and `missing_policy_evaluator.stderr` had the old error format ("is missing required field(s): ...") but the new format is per-field with type hints
- **Fix:** Ran `TRYBUILD=overwrite` to regenerate all stderr snapshots
- **Files modified:** `missing_config.stderr`, `missing_policy_evaluator.stderr`
- **Verification:** All 9 trybuild tests pass
- **Committed in:** 51d1b327 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both necessary consequences of planned API changes)
**Impact on plan:** Both auto-fixes were within the expected ripple of Result propagation. No scope creep.

## Issues Encountered

- Pre-existing doctest failure in `pmcp-code-mode/src/policy/mod.rs` (`NoopPolicyEvaluator` line 95) -- unrelated to this plan, not fixed (out of scope, same as Plans 01 and 02)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Result propagation complete end-to-end: HmacTokenGenerator -> ValidationPipeline -> register_code_mode_tools
- All 9 trybuild tests pass (6 compile-fail, 3 compile-pass)
- Plan 04 can add trybuild tests for `context_from`/`language` edge cases
- Plan 05 can build on the TokenError pattern for other error types

## Self-Check: PASSED

All files exist, all commits found, all key content verified.

---
*Phase: 67.2-code-mode-derive-hardening*
*Completed: 2026-04-12*
