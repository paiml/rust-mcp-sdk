---
phase: 67.2-code-mode-derive-hardening
plan: 04
subsystem: runtime
tags: [eval, performance, hashmap, scope-chain, js-runtime]

# Dependency graph
requires:
  - phase: 67.1
    provides: eval.rs array method infrastructure
provides:
  - Scope-chain push/pop optimization for all 7 array callback methods
affects: [67.2-code-mode-derive-hardening]

# Tech tracking
tech-stack:
  added: []
  patterns: ["scope-chain push/pop: insert loop var, evaluate, restore old value"]

key-files:
  created: []
  modified:
    - crates/pmcp-code-mode/src/eval.rs

key-decisions:
  - "Sort comparator clone left as-is per plan scope -- only callback-style array methods (Map/Filter/Find/Some/Every/FlatMap/Reduce) optimized"
  - "Caller clones once before &mut handoff -- 1 clone per array method call instead of N per element"

patterns-established:
  - "Push/pop scope pattern: save old = local_vars.insert(key, new); evaluate; match old { Some(prev) => restore, None => remove }"

requirements-completed: [CMSUP-10]

# Metrics
duration: 5min
completed: 2026-04-12
---

# Phase 67.2 Plan 04: Array Method Scope-Chain Push/Pop Optimization Summary

**Replaced per-element HashMap clone with scope-chain push/pop in all 7 array callback methods (Map, Filter, Find, Some, Every, FlatMap, Reduce) -- 1 clone per method call instead of N per element**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-12T16:44:37Z
- **Completed:** 2026-04-12T16:49:31Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Changed `evaluate_array_method_with_scope` to take `&mut HashMap<String, JsonValue>` instead of `&HashMap`
- Replaced clone-per-element pattern with insert/remove push/pop in all 7 array callback methods
- Caller in `evaluate_with_scope` now clones scope once before passing `&mut` to the array method
- Added 3 new tests: large-array map performance (1000 elements, 20 scope vars), scope preservation after filter, large-array reduce (1000-element sum)

## Task Commits

Each task was committed atomically:

1. **Task 1: Change evaluate_array_method_with_scope to use mutable scope push/pop** - `cfe2a25b` (feat)

## Files Created/Modified
- `crates/pmcp-code-mode/src/eval.rs` - Scope-chain push/pop optimization for array methods, 3 new tests

## Decisions Made
- Sort comparator clone left unchanged per plan scope -- Sort uses `sort_by` which has different borrowing constraints and was explicitly out of scope
- Reduce uses two push/pop pairs (one for acc_var, one for item_var) to properly restore both bindings

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed reduce test assertion for floating-point result**
- **Found during:** Task 1 (test verification)
- **Issue:** The evaluator's Add operation produces f64 results, so sum of integers yields 499500.0 (float), not 499500 (integer)
- **Fix:** Changed assertion from `JsonValue::Number(499_500.into())` to `serde_json::json!(499_500.0)`
- **Files modified:** crates/pmcp-code-mode/src/eval.rs
- **Verification:** All 205 lib tests pass
- **Committed in:** cfe2a25b (part of Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Test-only fix, no scope creep.

## Issues Encountered
- Pre-existing doctest failure in policy module (NoopPolicyEvaluator doctest missing import) -- unrelated to this plan, not fixed per scope boundary rule

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- eval.rs array methods now use efficient scope-chain pattern
- All 205 lib tests pass (48 eval-specific, 10 array-specific)
- Ready for remaining Wave 4/5 plans

---
## Self-Check: PASSED

- [x] crates/pmcp-code-mode/src/eval.rs exists
- [x] Commit cfe2a25b exists
- [x] SUMMARY.md exists

*Phase: 67.2-code-mode-derive-hardening*
*Completed: 2026-04-12*
