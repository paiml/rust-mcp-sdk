---
phase: 67.2-code-mode-derive-hardening
type: validation
created: 2026-04-12
---

# Phase 67.2: Validation Architecture

## Automated Verification

| SC | Verification Method | Command |
|----|-------------------|---------|
| 1 | Integration test: derive-generated handler calls policy evaluator | `cargo test -p pmcp-code-mode-derive --test code_mode_tests` |
| 2 | Trybuild compile-pass: struct with `context_from` attribute compiles | `cargo test -p pmcp-code-mode-derive --test code_mode_tests` |
| 3 | Trybuild compile-pass: struct with `language = "sql"` produces correct tool metadata | `cargo test -p pmcp-code-mode-derive --test code_mode_tests` |
| 4 | Unit test: `HmacTokenGenerator::new` returns `Err` on short secret | `cargo test -p pmcp-code-mode --lib test_short_secret` |
| 5 | Trybuild compile-fail: missing `token_secret` and `code_executor` fields | `cargo test -p pmcp-code-mode-derive --test code_mode_tests` |
| 6 | Grep: single `Arc<ValidationPipeline>` in generated code, not two constructions | `grep -c "ValidationPipeline" crates/pmcp-code-mode-derive/src/lib.rs` |
| 7 | Unit test: array method eval does not clone HashMap | `cargo test -p pmcp-code-mode --lib --features js-runtime` |
| 8 | Unit test: async validation fallback does not re-parse | `cargo test -p pmcp-code-mode --lib test_async_no_reparse` |
| 9 | Grep: single `json_to_string` function, no `value_to_string` | `grep -rn "value_to_string" crates/pmcp-code-mode/src/eval.rs` (expect 0) |
| 10 | Grep: no `LoopContinue`/`LoopBreak` in public `ExecutionError` | `grep -n "LoopContinue\|LoopBreak" crates/pmcp-code-mode/src/types.rs` (expect 0 in pub enum) |
| 11 | Grep: `ValidationResponse` contains `pub result: ValidationResult` | `grep "ValidationResult" crates/pmcp-code-mode/src/handler.rs` |
| 12 | Full test suite | `cargo test -p pmcp-code-mode -p pmcp-code-mode-derive` |
| 13 | Clippy check | `cargo clippy -p pmcp-code-mode -- -D warnings` |

## Quality Gate

Final verification: `make quality-gate` must pass after all plans complete.

## Security Validation

- SC-1 (policy wiring) is the primary security fix — verify that a restrictive `PolicyEvaluator` actually denies operations in the derive-generated handler
- SC-2 (context_from) ensures tokens are bound to real user/session — verify token `user_id` field reflects the method's return value, not "anonymous"
