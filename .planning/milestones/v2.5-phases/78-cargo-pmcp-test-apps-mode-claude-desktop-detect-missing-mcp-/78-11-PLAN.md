---
phase: 78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-
plan: 11
type: execute
wave: 3
depends_on: [09, 10]
files_modified:
  - crates/mcp-tester/examples/validate_widget_pair.rs
  - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md
autonomous: false
gap_closure: true
task_count: 3
requirements:
  - PHASE-78-AC-1
  - PHASE-78-AC-2
  - PHASE-78-AC-3
  - PHASE-78-AC-4
  - PHASE-78-AC-5
  - PHASE-78-ALWAYS-EXAMPLE
tags: [mcp-apps, validator, claude-desktop, gap-closure, cycle-2, always-coverage, human-uat, checkpoint, cost-coach-prod]

must_haves:
  truths:
    - "The working example `validate_widget_pair.rs` is extended to validate the 6 real-prod fixtures captured in Plan 09 (in addition to the 2 cycle-1 sample fixtures it already runs); each real-prod run reports zero Failed rows after Plan 78-10's regex generalization"
    - "`cargo run -p mcp-tester --example validate_widget_pair` exits 0 end-to-end and prints per-fixture summaries that confirm the cycle-2 GREEN state"
    - "`78-HUMAN-UAT.md` is rewritten with the cycle-2 expectations: the 6 numbered tests are re-binable post-cycle-2, Test 6 (cost-coach prod re-verify) has the explicit expected outcome '0 Failed rows on 8 cost-coach prod widgets', `gap_closure_validated` flips from `false` to `true` only when Test 6 passes"
    - "The Test 6 re-verification recipe is documented verbatim with the exact command, expected exit code, and expected output shape — no ambiguity for the operator"
    - "After Test 6 passes (operator-confirmed): frontmatter `gap_closure_validated: false` → `true`; phase 78 status routes to `/gsd-verify-work` for final phase verification rather than another `/gsd-plan-phase --gaps` cycle"
  artifacts:
    - path: "crates/mcp-tester/examples/validate_widget_pair.rs"
      provides: "Working example: 3 widget categories — broken (sample), corrected (sample), real-prod (6 widgets) — demonstrating the cycle-2 fix end-to-end"
      contains: "real-prod"
    - path: ".planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md"
      provides: "Re-verification HUMAN-UAT with explicit Test 6 expected outcome bound to cost-coach prod 8-widget zero-Failed result"
  key_links:
    - from: "crates/mcp-tester/examples/validate_widget_pair.rs"
      to: "crates/mcp-tester/tests/fixtures/widgets/bundled/real-prod/*.html"
      via: "include_str!"
      pattern: "include_str!.*real-prod"
    - from: "78-HUMAN-UAT.md Test 6"
      to: "cargo pmcp test apps --mode claude-desktop https://cost-coach.us-west.pmcp.run/mcp"
      via: "binary-boundary command + 0-Failed-rows acceptance"
      pattern: "cost-coach.us-west.pmcp.run"
---

<objective>
Close gap-closure cycle 2 by (a) extending the ALWAYS-requirement working example to demonstrate the real-prod GREEN state, (b) rewriting the HUMAN-UAT with cycle-2-explicit expected outcomes, and (c) checkpointing for operator re-verification of Test 6 against cost-coach prod.

Plan 10 fixed the validator at the library level; the cycle-2 RED tests turn GREEN. This plan closes the binary-boundary verification path:
- Working example proves end-to-end (cargo run output shows zero Failed rows on real-prod fixtures)
- HUMAN-UAT re-binds the 6 tests with the cycle-2 acceptance bar (zero Failed rows on 8 cost-coach prod widgets, contrasted with the v1 → cycle-1 → cycle-2 regression history)
- Operator runs Test 6 against `https://cost-coach.us-west.pmcp.run/mcp` and confirms

After Test 6 passes, the `gap_closure_validated: false` → `true` flip routes the phase out of gap-closure mode and into final `/gsd-verify-work`.

Purpose: Make the cycle-2 fix observably correct from the operator's perspective; produce the binding evidence that the false-positive class is gone in prod.
Output: Updated `validate_widget_pair.rs` (3 commits ago covered cycle 1 — this is the cycle-2 update), rewritten `78-HUMAN-UAT.md`, and an operator-verified Test 6 result.

**Why `autonomous: false`:** Task 3 is the human-verify checkpoint — operator runs Test 6 against prod and reports the result. Tasks 1 and 2 are autonomous given Plans 09 and 10 are landed.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-VERIFICATION.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/uat-evidence/2026-05-02-cost-coach-prod-rerun.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-08-SUMMARY.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-09-PLAN.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-10-PLAN.md

@CLAUDE.md
@crates/mcp-tester/examples/validate_widget_pair.rs
@crates/mcp-tester/tests/fixtures/widgets/bundled/real-prod/README.md

<interfaces>
<!-- Existing example structure (Plan 78-08 final). The cycle-2 update extends but does not replace. -->

```rust
const BROKEN: &str = include_str!("../tests/fixtures/widgets/broken_no_sdk.html");
const CORRECTED: &str = include_str!("../tests/fixtures/widgets/corrected_minimal.html");
const COST_SUMMARY_MINIFIED: &str =
    include_str!("../tests/fixtures/widgets/bundled/cost_summary_minified.html");

fn run_one(label: &str, html: &str) {
    /* prints a per-widget report, returns nothing */
}

fn main() {
    println!("MCP Apps widget validator — broken/corrected/bundled-prod demo");
    println!("================================================================");
    run_one("broken_no_sdk", BROKEN);
    run_one("corrected_minimal", CORRECTED);
    run_one("cost_summary_minified (cost-coach prod shape)", COST_SUMMARY_MINIFIED);
    println!("Done. broken: many Failed; corrected: zero Failed; cost_summary_minified: zero Failed (post-Plan-06 fix).");
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `validate_widget_pair.rs` example to cover all 6 real-prod fixtures</name>
  <files>crates/mcp-tester/examples/validate_widget_pair.rs</files>

  <read_first>
    crates/mcp-tester/examples/validate_widget_pair.rs (existing — 3 widget runs: broken, corrected, cost_summary_minified)
    crates/mcp-tester/tests/fixtures/widgets/bundled/real-prod/README.md (Plan 09 — per-fixture mode-emission table; informs which tool name to use per fixture in the example)
    crates/mcp-tester/tests/app_validator_widgets_real_prod.rs (Plan 09 — tool-name → fixture binding the example mirrors)
  </read_first>

  <behavior>
    The example demonstrates the cycle-2 GREEN state end-to-end. Output structure:

    ```
    MCP Apps widget validator — broken/corrected/cycle-2-real-prod demo
    ====================================================================

    === broken_no_sdk (mode = claude-desktop) === [reports many Failed rows]
    === corrected_minimal (mode = claude-desktop) === [reports zero Failed rows]
    === cost_summary_minified (cost-coach prod shape, cycle-1 synthetic) === [zero Failed rows]
    === real-prod cost-summary.html === [zero Failed rows]
    === real-prod cost-over-time.html === [zero Failed rows]
    === real-prod savings-summary.html === [zero Failed rows]
    === real-prod tag-coverage.html === [zero Failed rows]
    === real-prod connect-account.html === [zero Failed rows]
    === real-prod service-sankey.html === [zero Failed rows]

    Done. broken: many Failed; corrected: zero Failed; cycle-1 synthetic: zero Failed; cycle-2 real-prod: zero Failed across 6 widgets.
    ```

    The example exits 0. If any real-prod widget reports >0 Failed rows, the example STILL exits 0 (it's not a test runner) but the summary line surfaces the Failed count so an observer notices something is wrong. The integration tests in Plan 09's `app_validator_widgets_real_prod.rs` are the load-bearing assertion; the example is observability proof.
  </behavior>

  <action>
    Edit `crates/mcp-tester/examples/validate_widget_pair.rs` as follows:

    **Step 1: Add 6 new `include_str!` constants** for the real-prod fixtures, after the existing `COST_SUMMARY_MINIFIED` constant:

    ```rust
    /// Cycle-2 real-prod fixtures (Plan 78-09 Task 1 capture from cost-coach prod).
    /// Under the post-Plan-78-10 validator (G1+G2 generalization), all 6 must
    /// produce zero Failed rows — that's the binding proof that the cost-coach
    /// v2 false-positive class (33 Failed rows in 2026-05-02 prod re-run) is
    /// closed.
    const REAL_PROD_COST_SUMMARY: &str =
        include_str!("../tests/fixtures/widgets/bundled/real-prod/cost-summary.html");
    const REAL_PROD_COST_OVER_TIME: &str =
        include_str!("../tests/fixtures/widgets/bundled/real-prod/cost-over-time.html");
    const REAL_PROD_SAVINGS_SUMMARY: &str =
        include_str!("../tests/fixtures/widgets/bundled/real-prod/savings-summary.html");
    const REAL_PROD_TAG_COVERAGE: &str =
        include_str!("../tests/fixtures/widgets/bundled/real-prod/tag-coverage.html");
    const REAL_PROD_CONNECT_ACCOUNT: &str =
        include_str!("../tests/fixtures/widgets/bundled/real-prod/connect-account.html");
    const REAL_PROD_SERVICE_SANKEY: &str =
        include_str!("../tests/fixtures/widgets/bundled/real-prod/service-sankey.html");
    ```

    **Step 2: Update `main()`** to add 6 new `run_one` calls AFTER the existing 3, plus a tally + updated summary line. Replace the existing `main()` body:

    ```rust
    fn main() {
        println!("MCP Apps widget validator — broken/corrected/cycle-2-real-prod demo");
        println!("====================================================================");

        // Sample fixtures (cycle-1 baseline)
        run_one("broken_no_sdk", BROKEN);
        run_one("corrected_minimal", CORRECTED);
        run_one(
            "cost_summary_minified (cost-coach prod shape, cycle-1 synthetic)",
            COST_SUMMARY_MINIFIED,
        );

        // Cycle-2 real-prod fixtures (Plan 78-09 captures, Plan 78-10 GREEN)
        let real_prod = [
            ("real-prod cost-summary.html", REAL_PROD_COST_SUMMARY),
            ("real-prod cost-over-time.html", REAL_PROD_COST_OVER_TIME),
            ("real-prod savings-summary.html", REAL_PROD_SAVINGS_SUMMARY),
            ("real-prod tag-coverage.html", REAL_PROD_TAG_COVERAGE),
            ("real-prod connect-account.html", REAL_PROD_CONNECT_ACCOUNT),
            ("real-prod service-sankey.html", REAL_PROD_SERVICE_SANKEY),
        ];
        let mut real_prod_failed_total: usize = 0;
        for (label, html) in real_prod {
            real_prod_failed_total += run_one_and_count_failed(label, html);
        }

        println!(
            "\nDone. broken: many Failed; corrected: zero Failed; cycle-1 synthetic: zero Failed; cycle-2 real-prod: {} Failed total across 6 widgets.",
            real_prod_failed_total
        );
        if real_prod_failed_total > 0 {
            eprintln!(
                "WARNING: cycle-2 real-prod widgets produced {} Failed rows. The cost-coach prod false-positive class is NOT yet closed. Run `cargo test -p mcp-tester --test app_validator_widgets_real_prod` to see which assertions fail.",
                real_prod_failed_total
            );
        } else {
            println!("All 6 cycle-2 real-prod widgets produced zero Failed rows. Cost-coach v2 false-positive class CLOSED.");
        }
    }
    ```

    **Step 3: Add the helper `run_one_and_count_failed`** alongside the existing `run_one`:

    ```rust
    fn run_one_and_count_failed(label: &str, html: &str) -> usize {
        println!("\n=== {label} (mode = claude-desktop) ===\n");
        let validator = AppValidator::new(AppValidationMode::ClaudeDesktop, None);
        let results = validator.validate_widgets(&[(
            format!("example-{label}"),
            format!("ui://example-{label}"),
            html.to_string(),
        )]);
        let failed = results
            .iter()
            .filter(|r| r.status == TestStatus::Failed)
            .count();
        let warnings = results
            .iter()
            .filter(|r| r.status == TestStatus::Warning)
            .count();
        let passed = results
            .iter()
            .filter(|r| r.status == TestStatus::Passed)
            .count();

        let mut report = TestReport::new();
        for r in results {
            report.add_test(r);
        }
        report.print(OutputFormat::Pretty);
        println!("\nSummary for {label}: {passed} passed, {warnings} warnings, {failed} failed.\n");
        failed
    }
    ```

    The existing `run_one` helper is preserved unchanged for the 3 cycle-1 widget calls (it doesn't return the failed count, which is fine for the cycle-1 baseline; the cycle-2 calls use the count-returning variant for the tally).

    **Step 4: Update the file-level docstring** to mention cycle-2 coverage:

    ```rust
    //! Example: Validate broken / corrected / cycle-2 real-prod widgets.
    //!
    //! ALWAYS-requirement working example for Phase 78 (extended in cycle 2).
    //! Demonstrates the silent-fail bug Cost Coach hit, the cycle-1 fix, AND
    //! the cycle-2 fix that handles real cost-coach prod minified output.
    //!
    //! Usage:
    //!   cargo run -p mcp-tester --example validate_widget_pair
    //!
    //! Expected output: 9 widget runs (3 cycle-1 + 6 cycle-2 real-prod).
    //! `broken_no_sdk` reports many Failed rows; all 8 others report zero
    //! Failed rows. The summary line tallies the cycle-2 real-prod failed
    //! count — must read 0 for cycle-2 to be considered closed.
    ```

    **Step 5: Run end-to-end** and confirm:

    ```sh
    cargo run -p mcp-tester --example validate_widget_pair 2>&1 | tail -20
    ```

    Expected output last 2 lines (post-Plan-10):
    ```
    Done. broken: many Failed; corrected: zero Failed; cycle-1 synthetic: zero Failed; cycle-2 real-prod: 0 Failed total across 6 widgets.
    All 6 cycle-2 real-prod widgets produced zero Failed rows. Cost-coach v2 false-positive class CLOSED.
    ```

    Exit code: 0.

    If `real_prod_failed_total > 0`, the example will print to stderr instead of stdout and the overall run still exits 0 (examples are observability tools, not test runners). But this only happens if Plan 10 didn't fully close the cycle-2 gap — it's an observability signal that something needs another iteration.

    **Step 6: Quality gate:**
    ```sh
    cargo build -p mcp-tester --examples
    cargo clippy -p mcp-tester --examples -- -D warnings
    cargo fmt --all -- --check
    make quality-gate
    ```

    All exit 0.

    **Step 7: Commit message:**
    ```
    test(78-11): extend validate_widget_pair example with 6 cycle-2 real-prod widgets

    The ALWAYS-requirement working example now demonstrates the cycle-2
    fix end-to-end: 6 real cost-coach prod widget bundles (captured in
    Plan 78-09) are validated and the summary line tallies their Failed
    count. Post-Plan-78-10 expected output: "cycle-2 real-prod: 0 Failed
    total across 6 widgets" + "Cost-coach v2 false-positive class CLOSED".

    `cargo run -p mcp-tester --example validate_widget_pair` exits 0 with
    9 widget reports (3 cycle-1 baseline + 6 cycle-2 real-prod).
    ```
  </action>

  <verify>
    <automated>
test -f crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'REAL_PROD_COST_SUMMARY' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'REAL_PROD_COST_OVER_TIME' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'REAL_PROD_SAVINGS_SUMMARY' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'REAL_PROD_TAG_COVERAGE' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'REAL_PROD_CONNECT_ACCOUNT' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'REAL_PROD_SERVICE_SANKEY' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'fn run_one_and_count_failed' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'real_prod_failed_total' crates/mcp-tester/examples/validate_widget_pair.rs && grep -q 'Cost-coach v2 false-positive class CLOSED' crates/mcp-tester/examples/validate_widget_pair.rs && cargo build -p mcp-tester --examples 2>&1 | tail -5 && cargo clippy -p mcp-tester --examples -- -D warnings && cargo fmt --all -- --check && cargo run -p mcp-tester --example validate_widget_pair 2>&1 | tail -5 | grep -q 'cycle-2 real-prod: 0 Failed' && cargo run -p mcp-tester --example validate_widget_pair 2>&1 | tail -5 | grep -q 'CLOSED'
    </automated>
  </verify>

  <acceptance_criteria>
    - File `crates/mcp-tester/examples/validate_widget_pair.rs` contains 6 new `include_str!` constants for `real-prod/*.html` (grep all 6 names exit 0).
    - File contains `fn run_one_and_count_failed` helper.
    - File contains the variable `real_prod_failed_total`.
    - File contains the success-line literal `Cost-coach v2 false-positive class CLOSED`.
    - File-level docstring mentions `cycle 2`.
    - `cargo build -p mcp-tester --examples` exits 0.
    - `cargo clippy -p mcp-tester --examples -- -D warnings` exits 0.
    - `cargo fmt --all -- --check` exits 0.
    - `cargo run -p mcp-tester --example validate_widget_pair` exits 0.
    - Last 5 lines of stdout from the example contain BOTH `cycle-2 real-prod: 0 Failed` AND `CLOSED` (the success-path branch fired — Plan 10's fix is working at the cycle-2 widget set).
    - `make quality-gate` exits 0.
  </acceptance_criteria>

  <done>
    Working example extended with 6 real-prod widget runs; the success-path summary line confirms the cycle-2 GREEN state observably. Operator can run `cargo run -p mcp-tester --example validate_widget_pair` to see the fix demonstrated end-to-end without hitting cost-coach prod.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite `78-HUMAN-UAT.md` with cycle-2 expected outcomes + Test 6 explicit acceptance</name>
  <files>.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md</files>

  <read_first>
    .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md (current state — 6 numbered tests, status `re-verify`, gap_closure_validated `false`, Test 6 result `FAILED — false-positive class still present`)
    .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/uat-evidence/2026-05-02-cost-coach-prod-rerun.md (current cycle-1 evidence — informs the v1 → cycle-1 → cycle-2 progression narrative)
    .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-09-PLAN.md (cycle-2 RED phase context)
    .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-10-PLAN.md (cycle-2 GREEN phase context)
  </read_first>

  <action>
    Rewrite `78-HUMAN-UAT.md` in full. The new content:

    ```markdown
    ---
    status: re-verify
    phase: 78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-
    source: [78-VERIFICATION.md, 78-05-PLAN.md, 78-06-PLAN.md, 78-07-PLAN.md, 78-09-PLAN.md, 78-10-PLAN.md]
    started: 2026-05-02T00:00:00Z
    updated: <today's date in ISO 8601>
    gap_closure_landed: 2026-05-02
    cycle_2_landed: <today's date in ISO 8601>
    gap_closure_validated: false
    ---

    ## Current Test

    [awaiting human re-verification post Plan 78-09/10/11 cycle-2 gap closure]

    ## Tests

    ### 1. AC-78-1 at CLI binary boundary — broken widget fails claude-desktop mode
    expected: Run `cargo pmcp test apps --mode claude-desktop --widgets-dir <path>` against a directory containing a deliberately-broken widget (no SDK presence signals: no `@modelcontextprotocol/ext-apps`, no `[ext-apps]` log prefix, no `ui/initialize`, no `ui/notifications/tool-result`, no cycle-2 added signals; no `new <id>({name:"...",version:"..."})` constructor in any form; no protocol handlers; no `app.connect()`). The simplest reproducer is to copy `crates/mcp-tester/tests/fixtures/widgets/broken_no_sdk.html` into a tempdir and point `--widgets-dir` at it. Process exits non-zero AND stdout/stderr names at least one missing handler (e.g. `onteardown`).

    Why this is testable without a fixture binary: Plan 78-07 added `--widgets-dir`, which bypasses the `cli_acceptance.rs` fixture-binary skip-gate.

    Cycle-2 note: this test is unchanged from cycle 1 — Plan 10's regex generalization adds signals OR'd into `has_sdk` but does NOT make broken widgets pass. The synthetic broken fixture remains broken.

    result: [pending]

    ### 2. AC-78-2 at CLI binary boundary — corrected widget passes claude-desktop mode
    expected: Run the same `--widgets-dir` command against a directory containing `crates/mcp-tester/tests/fixtures/widgets/corrected_minimal.html`. Process exits zero.

    Cycle-2 note: cycle-1 corrected fixture continues to pass. Plan 10's broader regex doesn't reject the unminified `App` constructor + import literal that the corrected fixture uses.

    result: [pending]

    ### 3. AC-78-3 at CLI binary boundary — Standard mode is permissive
    expected: Run `cargo pmcp test apps --widgets-dir <path>` (no `--mode` flag) against BOTH the broken and corrected widgets. Both invocations exit zero — no regression for the permissive default.

    result: [pending]

    ### 4. AC-78-4 at CLI binary boundary — chatgpt mode is unchanged
    expected: Run `cargo pmcp test apps --mode chatgpt --widgets-dir <path>` against both fixtures. Both exit zero AND stderr/stdout MUST NOT contain any of the four protocol handler names — chatgpt mode is a no-op for widget validation.

    result: [pending]

    ### 5. AC-78-5 — UX review of READMEs and `--help` text
    expected: Visual review of `cargo pmcp test apps --help` and both READMEs (`cargo-pmcp/README.md`, `crates/mcp-tester/README.md`). Reader can answer:
    - "What does `--mode claude-desktop` do, when should I use it, and what does it check?"
    - "What does `--widgets-dir` do, when should I prefer source scan over bundle scan?"

    Both questions answerable from the docs alone.

    result: [pending]

    ### 6. Re-verify against cost-coach prod (the cycle-2 binding proof)
    expected: Run

    ```sh
    cargo run -p cargo-pmcp -- test apps --mode claude-desktop https://cost-coach.us-west.pmcp.run/mcp
    ```

    Expected outcome (cycle-2 acceptance bar):
    - Process exits **zero**.
    - Output reports **zero Failed rows** on the 8 production widgets.
    - Compare to cycle-1 result captured in `uat-evidence/2026-05-02-cost-coach-prod-rerun.md` (97 tests, 60 passed, **33 failed**, 4 warnings — all 33 confirmed false positives). Post-cycle-2 expected: 97 tests, 97 passed, 0 failed, 0 unwarranted warnings (the 4 warnings from cycle 1 may persist if they're genuinely WARN-tier signals like missing `ontoolresult` — those are soft and do NOT block acceptance).

    Optional supplemental run (offline-fallback / drift-check):

    ```sh
    cargo run -p mcp-tester --example validate_widget_pair
    ```

    This runs the validator against the 6 real-prod fixtures committed in Plan 78-09 (captured from the same prod URL). Expected last line: `All 6 cycle-2 real-prod widgets produced zero Failed rows. Cost-coach v2 false-positive class CLOSED.` — proves the fixtures match the live prod via Plan 78-09's CAPTURE.md SHA-256s, and that the same bytes pass under the live binary.

    Source feedback driving this re-verification: `/Users/guy/projects/mcp/cost-coach/drafts/feedback-pmcp-test-apps-v1-false-positives.md` (8.4 KB, dated 2026-05-02). The original feedback documents the v1 false-positive class. The cycle-1 re-verify (`uat-evidence/2026-05-02-cost-coach-prod-rerun.md`) showed cycle-1 didn't generalize. This cycle-2 re-verify confirms cycle-2 does.

    **Acceptance bar (load-bearing):** Test 6 is the single load-bearing acceptance gate for closing Phase 78 cycle 2. If Test 6 reports >0 Failed rows on cost-coach prod, the gap closure is INCOMPLETE and this UAT routes back to `/gsd-plan-phase 78 --gaps` for cycle 3. If Test 6 reports 0 Failed rows, the gap closure is COMPLETE and this UAT routes to `/gsd-verify-work` for final phase verification.

    On pass:
    - Update this file's frontmatter `gap_closure_validated: false` → `true`.
    - Update Test 6 result from `[pending]` to `[pass]` with the prod re-run output captured in a new `uat-evidence/<date>-cost-coach-prod-cycle2-rerun.md` file.
    - Tests 1-5 results updated to `[pass]` if the operator confirmed each via local `--widgets-dir` runs.

    On fail:
    - Update Test 6 result with the failed-row count + diagnosis.
    - Capture the prod re-run output in `uat-evidence/<date>-cost-coach-prod-cycle3-rerun.md`.
    - Add a Gap entry below documenting the new failure mode.
    - DO NOT flip `gap_closure_validated` — it stays `false` until cycle 3 closes the residual gap.

    result: [pending]

    ## Summary

    total: 6
    passed: 0
    issues: 0
    pending: 6
    skipped: 0
    blocked: 0

    ## Re-verification context

    Cycle 1 (Plans 78-05/06/07/08, completed 2026-05-02):
    - **Plan 05** (Wave 1) captured 3 synthetic bundled fixtures + RED-phase integration tests encoding the v1 false-positive class.
    - **Plan 06** (Wave 2) replaced fragile regexes with cycle-1 minification-resistant patterns + eliminated the SDK→handler/connect cascade. The 5 RED-phase tests turned GREEN.
    - **Plan 07** (Wave 3) added `--widgets-dir <path>` source-scan mode + 3 CLI-boundary integration tests via `assert_cmd`.
    - **Plan 08** (Wave 4) extended the property test corpus + bundled-fixture example + READMEs and rewrote this UAT.
    - **Cycle-1 outcome:** library-level GREEN against synthetic fixtures, but the 2026-05-02 cost-coach prod re-run reported 33 Failed rows on 8 prod widgets — same count as v1. The synthetic fixtures didn't match real Vite-singlefile prod output.

    Cycle 2 (Plans 78-09/10/11, this iteration):
    - **Plan 09** (Wave 1) captured 6 REAL cost-coach prod widget bundles into `tests/fixtures/widgets/bundled/real-prod/` + added 6 RED-phase integration tests bound to those bytes + 1 cycle-1 no-regression test.
    - **Plan 10** (Wave 2) generalized G1 SDK-presence signals (1-3 added signals OR'd into `has_sdk`) + replaced the cycle-1 G2 constructor regex with a generalized form (wider id cap, quoted-key tolerance, both orderings) — derived from grep evidence in `real-prod/CAPTURE.md`. Cycle-1 + cycle-2 RED tests all GREEN; cycle-1 unit tests + property tests all preserved.
    - **Plan 11** (Wave 3 — this plan) extended `validate_widget_pair.rs` example with 6 cycle-2 real-prod widget runs + rewrote this UAT for cycle-2 expectations + checkpointed for operator Test 6 re-verification.

    Pre-cycle-2 evidence: 8 cost-coach prod widgets produced 33 false-positive Failed rows (cycle-1 outcome).
    Post-cycle-2 expected: 8 cost-coach prod widgets produce zero Failed rows.

    ## Gaps

    (No new gaps unless Test 6 fails. The cycle-1 G6 entry below is preserved for history.)

    ### G6 — Plan 05/06 fix did not generalize to real cost-coach prod bundles (CLOSED by cycle 2)
    - **Date:** 2026-05-02
    - **Source:** Test 6 operator re-verification against `https://cost-coach.us-west.pmcp.run/mcp`
    - **Evidence:** `uat-evidence/2026-05-02-cost-coach-prod-rerun.md` (97 tests, 60 passed, 33 failed, 4 warnings)
    - **Cycle-2 closure:** Plans 78-09/10/11. Plan 09 captured the actual prod bytes; Plan 10 derived the generalized regex from those bytes; Plan 11 wires the binding proof. Validation: this UAT's Test 6 re-run.
    - **Status:** closing — pending Test 6 confirmation by operator.
    ```

    Replace `<today's date in ISO 8601>` with the actual current date when writing.

    Run after writing:

    ```sh
    grep -q '^status: re-verify' .planning/phases/78-*/78-HUMAN-UAT.md
    grep -q 'cycle_2_landed:' .planning/phases/78-*/78-HUMAN-UAT.md
    grep -q '0 Failed rows on the 8 production widgets' .planning/phases/78-*/78-HUMAN-UAT.md
    grep -q 'Cost-coach v2 false-positive class CLOSED' .planning/phases/78-*/78-HUMAN-UAT.md
    grep -q 'cycle 2 (Plans 78-09/10/11' .planning/phases/78-*/78-HUMAN-UAT.md
    grep -q 'gap_closure_validated: false' .planning/phases/78-*/78-HUMAN-UAT.md
    ```

    All grep should exit 0.

    **Commit message:**
    ```
    docs(78-11): rewrite HUMAN-UAT for cycle-2 expected outcomes + Test 6 acceptance bar

    Updates 78-HUMAN-UAT.md to reflect the cycle-2 gap-closure context:
    - Frontmatter adds cycle_2_landed timestamp
    - source list extended with 78-09-PLAN.md + 78-10-PLAN.md
    - Test 6 expected outcome: zero Failed rows on 8 cost-coach prod widgets
    - "Re-verification context" section narrates v1 → cycle-1 → cycle-2 progression
    - Cycle-1 G6 gap entry preserved with `closing — pending Test 6` status
    - gap_closure_validated stays `false` until operator confirms Test 6

    No code changes; documentation only.
    ```
  </action>

  <verify>
    <automated>
test -f .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q '^status: re-verify' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q 'cycle_2_landed:' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q 'gap_closure_validated: false' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q '0 Failed rows on the 8 production widgets\|zero Failed rows on the 8 production widgets' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q 'Cost-coach v2 false-positive class CLOSED' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q 'Plans 78-09/10/11' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q '78-09-PLAN.md' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q '78-10-PLAN.md' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q 'total: 6' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md && grep -q 'pending: 6' .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-HUMAN-UAT.md
    </automated>
  </verify>

  <acceptance_criteria>
    - File `78-HUMAN-UAT.md` exists with `status: re-verify` in frontmatter (grep exits 0).
    - Frontmatter contains `cycle_2_landed:` field (grep exits 0).
    - Frontmatter contains `gap_closure_validated: false` (grep exits 0; this stays false until Task 3's checkpoint).
    - Frontmatter `source` list contains both `78-09-PLAN.md` and `78-10-PLAN.md` (each grep exits 0).
    - Body contains the Test 6 acceptance bar literal `zero Failed rows on the 8 production widgets` OR `0 Failed rows on the 8 production widgets` (grep matches one of these).
    - Body contains the success-line literal `Cost-coach v2 false-positive class CLOSED` (mirroring the example output).
    - Body contains the cycle reference `Plans 78-09/10/11`.
    - Body summary block reads `total: 6, passed: 0, issues: 0, pending: 6, skipped: 0, blocked: 0`.
    - Cycle-1 G6 gap entry preserved in the `## Gaps` section (grep `G6 — Plan 05/06 fix did not generalize` exits 0).
    - 6 numbered tests present (`### 1.`, `### 2.`, `### 3.`, `### 4.`, `### 5.`, `### 6.` — each grep exits 0).
  </acceptance_criteria>

  <done>
    `78-HUMAN-UAT.md` is rewritten to make the cycle-2 acceptance bar explicit (zero Failed rows on 8 cost-coach prod widgets is the load-bearing gate). Tests 1-5 are unchanged in spirit (cycle 2 doesn't break their preconditions); Test 6 is upgraded with explicit cycle-1 vs cycle-2 contrast and the route-on-pass / route-on-fail decision tree. Frontmatter is teed up for Task 3's checkpoint flip.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runs Test 6 against cost-coach prod and confirms cycle-2 GREEN</name>

  <what-built>
    Plans 09 + 10 + 11 are committed:
    - Plan 09: 6 real-prod fixtures + CAPTURE.md + RED-phase integration tests
    - Plan 10: generalized G1 + G2 regexes; cycle-2 RED tests turn GREEN; PMAT cog ≤ 25 preserved
    - Plan 11 Tasks 1-2: extended example demonstrates cycle-2 GREEN + HUMAN-UAT rewritten

    Library-level evidence:
    - `cargo test -p mcp-tester --test app_validator_widgets_real_prod` — 7/7 pass
    - `cargo test -p mcp-tester --test app_validator_widgets_bundled` — 5/5 pass (no regression)
    - `cargo test -p mcp-tester --test app_validator_widgets` — 7/7 pass (no regression)
    - `cargo test -p mcp-tester --test property_tests` — 4/4 pass
    - `cargo run -p mcp-tester --example validate_widget_pair` — exits 0; last 2 lines confirm zero real-prod Failed rows

    The CLI binary boundary against real cost-coach prod is the one verification path automation can't take — the prod endpoint is external and may have updated since Plan 09's capture date. Operator runs Test 6 to confirm the binding proof.
  </what-built>

  <how-to-verify>
    **Pre-flight:**

    ```sh
    cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk
    git status                                         # Plans 09/10/11 should all be committed
    cargo build -p cargo-pmcp                          # cargo-pmcp binary builds clean
    make quality-gate                                  # exits 0
    ```

    **Optional pre-flight: confirm captured fixtures haven't drifted** (tells you whether the live prod widgets still match Plan 09's captures):

    ```sh
    cd crates/mcp-tester/tests/fixtures/widgets/bundled/real-prod
    shasum -a 256 *.html
    # Compare against the SHA-256 column in CAPTURE.md.
    # If a fixture has drifted, the prod widget rebuild changed the bytes —
    # the `cargo run --example validate_widget_pair` proof still holds for
    # the captured snapshot, but Test 6 against live prod tests the LIVE
    # bytes (which may differ).
    ```

    **Test 6 — bundle-scan against cost-coach prod (load-bearing):**

    ```sh
    cargo run -p cargo-pmcp -- test apps \
      --mode claude-desktop \
      https://cost-coach.us-west.pmcp.run/mcp \
      2>&1 | tee /tmp/test6-cycle2-output.txt
    ```

    Expected outcome:
    - Process exits **zero**.
    - Output reports **zero Failed rows** on the 8 production widgets (8 App-capable tools).
    - Output may show some Warnings (typically the soft `ontoolresult` warnings if widgets don't register that handler — that's WARN-tier, not blocking).
    - Compare to cycle-1 result in `uat-evidence/2026-05-02-cost-coach-prod-rerun.md` (97 tests, 60 passed, 33 failed) — cycle-2 expected: 97 tests, ≥97-Warning_count passed, 0 failed.

    **Branching:**

    **CASE A — Test 6 PASSES** (zero Failed rows):

    1. Capture the prod re-run output to a new evidence file:
       ```sh
       DATE=$(date +%Y-%m-%d)
       cp /tmp/test6-cycle2-output.txt \
          .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/uat-evidence/${DATE}-cost-coach-prod-cycle2-rerun.md
       ```
       Wrap the file in markdown structure (header + command + result + per-widget breakdown — same shape as the cycle-1 evidence file at `uat-evidence/2026-05-02-cost-coach-prod-rerun.md`).

    2. Update `78-HUMAN-UAT.md`:
       - Frontmatter: `gap_closure_validated: false` → `true`
       - Frontmatter: add `validated: <ISO 8601 today>` field
       - Test 6 `result:` from `[pending]` to `[pass]` with link to the new evidence file
       - Tests 1-5 `result:` updates to reflect operator's actual local re-runs (operator's decision per their re-run pace)
       - In `## Gaps` section, mark the G6 entry as `**Status:** closed (cycle-2 Test 6 passed YYYY-MM-DD)`

    3. Commit:
       ```sh
       git add .planning/phases/78-*/78-HUMAN-UAT.md \
               .planning/phases/78-*/uat-evidence/${DATE}-cost-coach-prod-cycle2-rerun.md
       git commit -m "docs(78-11): record cost-coach prod cycle-2 GREEN — Test 6 PASSED

    Test 6 re-verification against https://cost-coach.us-west.pmcp.run/mcp
    on $DATE: zero Failed rows across 8 prod widgets. Cycle-2 gap closure
    (Plans 78-09/10/11) eliminates the v1/cycle-1 false-positive class.

    Frontmatter gap_closure_validated false → true; Phase 78 ready for
    /gsd-verify-work final phase verification."
       ```

    4. Operator types `approved` (orchestrator routes phase 78 to `/gsd-verify-work`).

    **CASE B — Test 6 FAILS** (≥1 Failed row, indicating cycle-2 didn't fully close the gap):

    1. Capture the prod re-run output to a new evidence file (same shape as Case A):
       ```sh
       DATE=$(date +%Y-%m-%d)
       cp /tmp/test6-cycle2-output.txt \
          .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/uat-evidence/${DATE}-cost-coach-prod-cycle3-rerun.md
       ```

    2. Add a per-widget diagnosis to the evidence file: which widgets failed, what kind of failure (G1 SDK-not-detected? G2 constructor-not-detected? G3 regression? new failure mode?), and what specific bytes the existing regex still misses.

    3. Update `78-HUMAN-UAT.md`:
       - Frontmatter stays `gap_closure_validated: false`
       - Test 6 `result:` from `[pending]` to `[failed]: <N> Failed rows; see <evidence-file>; routes to /gsd-plan-phase 78 --gaps for cycle 3`
       - Add a new Gap entry `### G7 — cycle-2 fix did not fully generalize against <prod state>` documenting the residual failure mode

    4. Commit:
       ```sh
       git add .planning/phases/78-*/78-HUMAN-UAT.md \
               .planning/phases/78-*/uat-evidence/${DATE}-cost-coach-prod-cycle3-rerun.md
       git commit -m "docs(78-11): record cost-coach prod cycle-2 re-verify — Test 6 FAILED

    Cycle-2 fix did not fully eliminate the false-positive class on
    cost-coach prod. <N> Failed rows on the 8 production widgets after
    Plans 78-09/10/11. Diagnosis in
    uat-evidence/$DATE-cost-coach-prod-cycle3-rerun.md.

    Routes to /gsd-plan-phase 78 --gaps for cycle 3."
       ```

    5. Operator types `failed: <reason>` (orchestrator routes phase 78 to a third gap-closure cycle).

    **Other resume signals:**
    - `blocked: <reason>` — if Test 6 cannot run (prod down, network issue, etc.); the orchestrator pauses without flipping `gap_closure_validated` and records the block.
    - `defer: <reason>` — if the operator wants to pause cycle-2 closure for now (e.g. waiting for cost-coach team to confirm a separate issue); orchestrator records the defer reason and exits without state change.
  </how-to-verify>

  <resume-signal>approved | failed: &lt;reason&gt; | blocked: &lt;reason&gt; | defer: &lt;reason&gt;</resume-signal>
</task>

</tasks>

<verification>
After all 3 tasks complete:

```sh
# Library acceptance unchanged (Plan 11 Task 1 doesn't touch validator)
cargo test -p mcp-tester --test app_validator_widgets_real_prod    # 7/7
cargo test -p mcp-tester --test app_validator_widgets_bundled      # 5/5
cargo test -p mcp-tester --test app_validator_widgets              # 7/7
cargo test -p mcp-tester --lib app_validator                        # all pass
cargo test -p mcp-tester --test property_tests                     # 4/4

# Working example demonstrates cycle-2 GREEN
cargo run -p mcp-tester --example validate_widget_pair 2>&1 | tail -5
# Last 2 lines: "...cycle-2 real-prod: 0 Failed total..." + "...CLOSED"

# HUMAN-UAT shape
grep -E '^(status|gap_closure_validated|cycle_2_landed):' .planning/phases/78-*/78-HUMAN-UAT.md

# Quality gate
make quality-gate    # exits 0

# Operator-facing checkpoint state (post Task 3)
# CASE A passed:
#   git log --oneline -1   # latest commit: docs(78-11): record cost-coach prod cycle-2 GREEN
#   grep gap_closure_validated .planning/phases/78-*/78-HUMAN-UAT.md  # true
# CASE B failed:
#   git log --oneline -1   # latest commit: docs(78-11): record cost-coach prod cycle-2 re-verify — Test 6 FAILED
#   grep gap_closure_validated .planning/phases/78-*/78-HUMAN-UAT.md  # false
#   ls .planning/phases/78-*/uat-evidence/  # new cycle3-rerun.md present
```
</verification>

<success_criteria>
- `validate_widget_pair.rs` extended with 6 cycle-2 real-prod widget runs + tally + success-path summary literal `Cost-coach v2 false-positive class CLOSED`.
- `cargo run -p mcp-tester --example validate_widget_pair` exits 0 and prints the success-path summary.
- `78-HUMAN-UAT.md` rewritten with cycle-2 frontmatter (cycle_2_landed, source list extended) + Test 6 acceptance bar (zero Failed rows on 8 prod widgets) + cycle-1 vs cycle-2 narrative + branching pass/fail recipe.
- Operator runs Test 6 and resumes with `approved` / `failed: <reason>` / `blocked: <reason>` / `defer: <reason>`.
- On `approved`: `gap_closure_validated: false` → `true`, new `<date>-cost-coach-prod-cycle2-rerun.md` evidence committed, phase 78 ready for `/gsd-verify-work`.
- On `failed`: `gap_closure_validated` stays `false`, new `<date>-cost-coach-prod-cycle3-rerun.md` evidence committed, new Gap entry `G7` added, phase 78 routes to `/gsd-plan-phase 78 --gaps` for cycle 3.
- 2 atomic commits before checkpoint: `test(78-11): extend validate_widget_pair example ...` + `docs(78-11): rewrite HUMAN-UAT for cycle-2 ...`.
- 1 additional commit after checkpoint resume: `docs(78-11): record cost-coach prod cycle-2 ...` (PASSED or FAILED variant).
</success_criteria>

<output>
After Tasks 1 + 2 complete and Task 3 checkpoint resumes, create `.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-11-SUMMARY.md`.
</output>
