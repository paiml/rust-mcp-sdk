---
phase: 78
plan: 04
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - cargo-pmcp/README.md
  - crates/mcp-tester/README.md
  - cargo-pmcp/src/commands/test/mod.rs
  - src/server/mcp_apps/GUIDE.md
  - crates/mcp-tester/src/report.rs
autonomous: true
requirements:
  - PHASE-78-AC-5
tags:
  - mcp-apps
  - docs
  - guide-anchors
  - cli-help
must_haves:
  truths:
    - "`cargo pmcp test apps --help` long-help text spells out what `--mode claude-desktop` actually checks (strict static inspection of widget HTML for ext-apps SDK wiring) — beyond the bare 'standard, chatgpt, or claude-desktop' line that exists today"
    - "`cargo-pmcp/README.md` has a new `## App Validation` section with a `### Modes` subheading describing the three modes; section is grep-findable by the literal `claude-desktop`"
    - "`cargo-pmcp/README.md` includes a `--tool` filter note explaining that `--mode claude-desktop` honors the `--tool` filter (REVISION HIGH-4 documentation)"
    - "`cargo-pmcp/README.md` includes a single-line note about Vite singlefile minification empirical verification (REVISION optional Gemini doc-only) and a section emphasizing the `;profile=mcp-app` MIME parameter as a common Claude Desktop failure point (REVISION optional Gemini doc-only)"
    - "`crates/mcp-tester/README.md` has the new mode mentioned via a new `## App Validation` section that mirrors the `## Protocol Conformance` block style"
    - "`src/server/mcp_apps/GUIDE.md` has explicit HTML anchor markers (`<a id=\"handlers-before-connect\"></a>` etc.) inserted just before the relevant `###` headings"
    - "Anchor expander in `crates/mcp-tester/src/report.rs` replaces `[guide:slug]` tokens in `details` strings at print time"
    - "An integration test (`error_messages_anchored`) asserts running the validator on a broken fixture produces details strings whose `[guide:handlers-before-connect]` token expands to the canonical GitHub URL"
    - "A printer-level test (`pretty_output_includes_expanded_url`) captures pretty output and asserts the expanded URL appears in the printed bytes (REVISION partial — addresses Codex MEDIUM concern about indirect verification)"
  artifacts:
    - path: "cargo-pmcp/README.md"
      provides: "## App Validation section with claude-desktop mode documented + --tool filter note + Vite/MIME doc-only notes"
      contains: "claude-desktop"
    - path: "crates/mcp-tester/README.md"
      provides: "claude-desktop mode mention with link to mcp_apps/GUIDE.md"
      contains: "claude-desktop"
    - path: "cargo-pmcp/src/commands/test/mod.rs"
      provides: "enriched ///-doc on Apps variant explaining --mode claude-desktop semantics"
      contains: "claude-desktop"
    - path: "src/server/mcp_apps/GUIDE.md"
      provides: "explicit HTML anchor markers (<a id=\"...\">) at 5 link target sites"
      contains: "handlers-before-connect"
    - path: "crates/mcp-tester/src/report.rs"
      provides: "expand_guide_anchor helper + integration into TestReport printer"
      contains: "expand_guide_anchor"
  key_links:
    - from: "cargo-pmcp/src/commands/test/mod.rs::TestCommand::Apps"
      to: "the user-visible --help text"
      via: "clap derive ///-doc harvest"
      pattern: "claude-desktop"
    - from: "crates/mcp-tester/src/report.rs::expand_guide_anchor"
      to: "src/server/mcp_apps/GUIDE.md#handlers-before-connect"
      via: "string replace of [guide:handlers-before-connect] -> URL"
      pattern: "handlers-before-connect"
    - from: "src/server/mcp_apps/GUIDE.md anchor markers"
      to: "the URLs produced by expand_guide_anchor"
      via: "explicit HTML anchor stable regardless of heading rename"
      pattern: "id=\"handlers-before-connect\""
---

<objective>
Polish the user-facing surface for Phase 78: CLI `--help` long-text, both READMEs, GUIDE.md HTML anchor markers, and an anchor-expander in the report printer that turns `[guide:slug]` tokens into absolute GitHub URLs.

Purpose: deliver PHASE-78-AC-5 ("README + `--help` document the new mode").

REVISION additions:
- HIGH-4 doc note: `--mode claude-desktop` honors the `--tool` filter (already implemented in Plan 02; this plan documents it).
- Optional Gemini doc-only items: Vite singlefile minification empirical-verification note + MIME profile (`;profile=mcp-app`) emphasis as common Claude Desktop failure point.
- Codex MEDIUM concern (printer-level verification): add a printer test capturing pretty output and asserting the expanded URL appears there.

This plan can run in parallel with Plan 02 in Wave 2 (both depend only on Plan 01).

Output:
- 1 new section in `cargo-pmcp/README.md` (~30 lines)
- 1 new section in `crates/mcp-tester/README.md` (~15 lines)
- 1 enriched `///-doc` on `TestCommand::Apps` variant
- 5 HTML anchor markers in `src/server/mcp_apps/GUIDE.md`
- 1 new helper (`expand_guide_anchor`) in `crates/mcp-tester/src/report.rs`, wired into the printer
- 2 new tests asserting anchor expansion works (one library-level, one printer-level)
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-RESEARCH.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-PATTERNS.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-VALIDATION.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-REVIEWS.md
@.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-01-PLAN.md
@CLAUDE.md
@cargo-pmcp/README.md
@crates/mcp-tester/README.md
@cargo-pmcp/src/commands/test/mod.rs
@src/server/mcp_apps/GUIDE.md
@crates/mcp-tester/src/report.rs

Anchor token contract (defined in Plan 01, consumed here):

The full set of slugs Phase 78 uses:

- `[guide:handlers-before-connect]` — required protocol handlers (GUIDE.md:185)
- `[guide:do-not-pass-tools]` — capabilities declaration warning (GUIDE.md:205)
- `[guide:csp-external-resources]` — CSP for external domains (GUIDE.md:135)
- `[guide:vite-singlefile]` — bundling widgets with Vite (GUIDE.md:328)
- `[guide:common-failures-claude]` — "Widget shows briefly then connection drops" (GUIDE.md:426)

Each token expands to: `https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#SLUG`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `expand_guide_anchor` helper in report.rs + wire into the pretty printer + add 5 unit tests + 1 printer-level test (Codex MEDIUM)</name>

  <read_first>
    - crates/mcp-tester/src/report.rs (full file — find the existing `print_pretty` (or `print` with `Pretty` arm) function)
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-PATTERNS.md (PATTERNS.md critical finding #3)
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-01-PLAN.md (verify Plan 01 emits `[guide:handlers-before-connect]` and `[guide:common-failures-claude]`)
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-REVIEWS.md (Codex MEDIUM: printer-level test should assert the URL appears in pretty output, not just verify the helper function)
  </read_first>

  <files>crates/mcp-tester/src/report.rs</files>

  <behavior>
    - Test 1 (expand_guide_anchor_handlers_before_connect): `expand_guide_anchor("Missing handler [guide:handlers-before-connect]")` returns the input string with the token replaced by `https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#handlers-before-connect`.
    - Test 2 (expand_guide_anchor_no_token): `expand_guide_anchor("plain text")` returns `"plain text"` unchanged.
    - Test 3 (expand_guide_anchor_unknown_slug): unknown tokens left in place.
    - Test 4 (expand_guide_anchor_multiple_tokens): both known tokens fully expanded.
    - Test 5 (expand_guide_anchor_common_failures): `[guide:common-failures-claude]` expands to a URL ending with `#common-failures-claude`.
    - Test 6 (pretty_output_includes_expanded_url) — REVISION (Codex MEDIUM): build a `TestReport` containing one `TestResult` whose `details` is `Some("[guide:handlers-before-connect]")`, render it via the pretty printer to a `Vec<u8>` (or capture stdout), and assert the captured bytes contain the literal substring `https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#handlers-before-connect`. This proves the printer wiring is correct, not just the helper.
  </behavior>

  <action>
First, read `crates/mcp-tester/src/report.rs` in full. Locate where `details` strings are emitted to stdout in the `Pretty` print path.

Step 1: Add the `expand_guide_anchor` function. Place it near the top of the file or just before the `TestReport` impl block:

```rust
/// Expand `[guide:SLUG]` tokens in `details` strings to absolute URLs into
/// `src/server/mcp_apps/GUIDE.md`. Unknown slugs are left in place.
pub fn expand_guide_anchor(details: &str) -> String {
    const KNOWN_SLUGS: &[&str] = &[
        "handlers-before-connect",
        "do-not-pass-tools",
        "csp-external-resources",
        "vite-singlefile",
        "common-failures-claude",
    ];
    const URL_PREFIX: &str =
        "https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#";
    let mut out = details.to_string();
    for slug in KNOWN_SLUGS {
        let token = format!("[guide:{slug}]");
        let url = format!("{URL_PREFIX}{slug}");
        out = out.replace(&token, &url);
    }
    out
}
```

Step 2: Wire into the printer. Locate the line in `print_pretty` (or equivalent) that prints `details`. Replace the printed string with `expand_guide_anchor(d)`.

If the existing print path does NOT print `details` at all today, ADD a print line for it inside the existing pretty-print loop, immediately after the `error` line.

REVISION (Codex MEDIUM): the printer must support a writer-injection seam for the printer-level test. If the existing `print` function writes to `println!` directly, refactor minimally to extract a `print_to_writer<W: Write>(&self, format: OutputFormat, w: &mut W)` helper that accepts any `std::io::Write` sink. The existing `print(OutputFormat)` becomes a thin wrapper calling `print_to_writer(format, &mut std::io::stdout())`. This lets the test capture output into `Vec<u8>`.

If a writer-seam refactor is too invasive, a fallback: have the test use `gag` or `os_pipe` to capture stdout. The writer-seam refactor is preferred — cleaner, no new deps.

Step 3: Add 5 unit tests for `expand_guide_anchor` plus the printer-level test:

```rust
#[test]
fn pretty_output_includes_expanded_url() {
    let mut report = TestReport::new();
    report.add_test(TestResult {
        name: "[example] handler: onteardown".to_string(),
        category: TestCategory::Apps,
        status: TestStatus::Failed,
        duration: std::time::Duration::from_secs(0),
        error: None,
        details: Some(
            "Widget does not register onteardown. [guide:handlers-before-connect]"
                .to_string(),
        ),
    });
    let mut buf: Vec<u8> = Vec::new();
    report.print_to_writer(OutputFormat::Pretty, &mut buf);
    let captured = String::from_utf8_lossy(&buf);
    assert!(
        captured.contains(
            "https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#handlers-before-connect"
        ),
        "pretty output must contain the expanded URL; got:\n{}",
        captured
    );
    assert!(
        !captured.contains("[guide:handlers-before-connect]"),
        "pretty output must not contain the unexpanded token; got:\n{}",
        captured
    );
}
```

Step 4: Verify visibility. `expand_guide_anchor` is `pub` so the integration test in Task 4 can call it. Confirm `pub use report::expand_guide_anchor;` is added to `crates/mcp-tester/src/lib.rs` if needed.

Run:
- `cargo test -p mcp-tester report::tests::expand_guide_anchor`
- `cargo test -p mcp-tester report::tests::pretty_output_includes_expanded_url`
- `cargo test -p mcp-tester` (regression)
- `cargo clippy -p mcp-tester --all-targets -- -D warnings`
- `cargo fmt --all -- --check`
  </action>

  <verify>
    <automated>cargo test -p mcp-tester report -- --nocapture && cargo clippy -p mcp-tester --all-targets -- -D warnings</automated>
  </verify>

  <acceptance_criteria>
    - File `crates/mcp-tester/src/report.rs` contains `pub fn expand_guide_anchor(details: &str) -> String`
    - File contains all 5 known slugs as string literals
    - File contains the URL prefix `https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#`
    - The pretty printer calls `expand_guide_anchor` on `details`
    - File has `print_to_writer` (or equivalent writer-seam) — verifies Codex MEDIUM remediation
    - `cargo test -p mcp-tester report::tests::expand_guide_anchor_handlers_before_connect` exits 0
    - `cargo test -p mcp-tester report::tests::expand_guide_anchor_unknown_slug` exits 0
    - `cargo test -p mcp-tester report::tests::pretty_output_includes_expanded_url` exits 0 (Codex MEDIUM)
    - `cargo test -p mcp-tester` exits 0 (regression)
    - `cargo clippy -p mcp-tester --all-targets -- -D warnings` exits 0
  </acceptance_criteria>

  <done>5 anchor-expander unit tests + 1 printer-level test pass; printer wired with writer-seam (Codex MEDIUM); pub re-exported as needed.</done>
</task>

<task type="auto">
  <name>Task 2: Insert explicit HTML anchor markers in src/server/mcp_apps/GUIDE.md (5 anchors)</name>

  <read_first>
    - src/server/mcp_apps/GUIDE.md (full file — confirm headings at lines 135, 183, 196, 207, 328, 424)
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-PATTERNS.md (CRITICAL FINDING #3)
  </read_first>

  <files>src/server/mcp_apps/GUIDE.md</files>

  <action>
This is a docs-only edit. The 5 anchors map to 5 sections. For each, insert a one-line HTML anchor element IMMEDIATELY before the existing `###` heading.

NOTE: locate each by heading text, not by line number.

Insertions (one HTML `a` element per slug, immediately before the matching heading):

1. Before `### 5. Declare CSP for external domains`: `<a id="csp-external-resources"></a>`
2. Before `### Required protocol handlers`: `<a id="handlers-before-connect"></a>`
3. Before `### Capabilities declaration`: `<a id="do-not-pass-tools"></a>`
4. Before the Vite singlefile section (`### Setup` under "Bundling widgets with Vite"): `<a id="vite-singlefile"></a>`
5. Before `### Common failures`: `<a id="common-failures-claude"></a>`

After insertion, verify each anchor by grepping for the exact attribute value.
  </action>

  <verify>
    <automated>for slug in handlers-before-connect do-not-pass-tools csp-external-resources vite-singlefile common-failures-claude; do grep -q "id=\"$slug\"" src/server/mcp_apps/GUIDE.md || exit 1; done</automated>
  </verify>

  <acceptance_criteria>
    - File `src/server/mcp_apps/GUIDE.md` contains `id="handlers-before-connect"`
    - File contains `id="do-not-pass-tools"`
    - File contains `id="csp-external-resources"`
    - File contains `id="vite-singlefile"`
    - File contains `id="common-failures-claude"`
    - The 5 anchors are placed IMMEDIATELY BEFORE the matching `###` headings
  </acceptance_criteria>

  <done>5 HTML id anchors present; placement verified.</done>
</task>

<task type="auto">
  <name>Task 3: Update READMEs (cargo-pmcp + mcp-tester) and enrich `cargo pmcp test apps --help` long-text — REVISION HIGH-4 doc note + Gemini optional Vite/MIME notes</name>

  <read_first>
    - cargo-pmcp/README.md (full file — 294 lines)
    - crates/mcp-tester/README.md (full file — 196 lines)
    - cargo-pmcp/src/commands/test/mod.rs (lines 33-59 — current `///-doc` on the `Apps` variant)
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-PATTERNS.md (§"cargo-pmcp/README.md and crates/mcp-tester/README.md")
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-RESEARCH.md (§"Pitfall 1: All existing in-repo widget examples FAIL the new check")
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-REVIEWS.md (HIGH-4 doc note + Gemini optional Vite + MIME profile notes)
  </read_first>

  <files>cargo-pmcp/README.md, crates/mcp-tester/README.md, cargo-pmcp/src/commands/test/mod.rs</files>

  <action>

Step 1: Edit `cargo-pmcp/src/commands/test/mod.rs`. Replace the existing `///-doc` block above the `Apps` variant with the enriched version:

```rust
    /// Validate MCP App metadata compliance.
    ///
    /// Checks tools for App-capable metadata (ui.resourceUri), validates MIME
    /// types, cross-references with resources, and optionally validates
    /// host-specific keys.
    ///
    /// MODES:
    ///   * `standard` (default) - permissive: emits ONE summary Warning per
    ///     widget (MCP Apps is optional in the spec).
    ///   * `chatgpt` - also checks for openai/* _meta keys ChatGPT requires.
    ///     For widget validation specifically, `chatgpt` mode is a no-op
    ///     (no widget rows emitted) — preserves the previous behavior.
    ///   * `claude-desktop` - STRICT: statically inspects each widget HTML body
    ///     fetched via resources/read for the @modelcontextprotocol/ext-apps
    ///     import, the new App({...}) constructor, the four required protocol
    ///     handlers (onteardown, ontoolinput, ontoolcancelled, onerror),
    ///     and the app.connect() call. Missing signals are emitted as ERROR
    ///     (one row per missing handler). Honors `--tool` to restrict the
    ///     check to a single tool's widget. Recommended pre-deploy check
    ///     for servers shipping to Claude clients.
```

Step 2: Edit `cargo-pmcp/README.md`. Add a brand-new section between the existing `## Commands` and `## Global Flags` sections.

Section content:

- Section heading: `## App Validation`
- One-paragraph intro: `cargo pmcp test apps URL` validates MCP App metadata on a running server.
- Subsection heading: `### Modes`
- Mode table with three rows:
  - `standard` (default) | Warning | Permissive — ONE summary Warning row per widget.
  - `chatgpt` | Error | Strict for ChatGPT compatibility. **For widget validation specifically, this mode is a no-op** (no widget-related rows emitted; preserves prior behavior).
  - `claude-desktop` | Error | Strict for Claude Desktop / Claude.ai. Statically inspects each widget HTML body fetched via resources/read for the `@modelcontextprotocol/ext-apps` import, the `new App({...})` constructor, the four required protocol handlers (`onteardown`, `ontoolinput`, `ontoolcancelled`, `onerror`), and the `app.connect()` call.
- Code fence (bash):
  - `cargo pmcp test apps http://localhost:3000` (default permissive)
  - `cargo pmcp test apps http://localhost:3000 --mode chatgpt`
  - `cargo pmcp test apps http://localhost:3000 --mode claude-desktop`
  - `cargo pmcp test apps http://localhost:3000 --mode claude-desktop --tool open_dashboard` (REVISION HIGH-4 example: `--tool` filter)
- Subsection heading: `### Why --mode claude-desktop`
- Body paragraph (silent-fail explanation).
- Markdown link: `[src/server/mcp_apps/GUIDE.md#handlers-before-connect](https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#handlers-before-connect).`
- Subsection heading: `### MIME profile (`;profile=mcp-app`)` — REVISION optional Gemini doc-only:
  - Body: A common Claude Desktop failure point is omitting the `;profile=mcp-app` MIME parameter on widget resources. Claude Desktop uses this MIME parameter to identify resources that should be rendered as MCP App widgets vs plain HTML resources. Verify your server registers widget resources with `mime_type: "text/html;profile=mcp-app"` (or equivalent).
- Subsection heading: `### Vite singlefile minification` — REVISION optional Gemini doc-only:
  - Single-line note: If your widget bundle is produced by Vite singlefile in production, the validator's regex strategy is designed to survive that minifier; if you encounter false-negatives, file an issue with a bundled HTML for empirical analysis.
- Blockquote (Pitfall 1 disclosure): `> Note: The legacy widget examples under examples/mcp-apps-chess/, examples/mcp-apps-dataviz/, and examples/mcp-apps-map/ use the older postMessage channel and will fail --mode claude-desktop. For a Claude-Desktop-ready widget, see crates/mcp-tester/examples/validate_widget_pair.rs and the corrected fixture at crates/mcp-tester/tests/fixtures/widgets/corrected_minimal.html.`

Step 3: Edit `crates/mcp-tester/README.md`. Add a new short prose section after the existing `## Protocol Conformance` block:

- Section heading: `## App Validation`
- Body intro.
- Code fence (bash) with three commands using `mcp-tester apps http://localhost:3000` (with `--mode chatgpt` and `--mode claude-desktop` variants).
- Body paragraph: `--mode claude-desktop` statically inspects each widget HTML body for the @modelcontextprotocol/ext-apps import and the four required protocol handlers (onteardown, ontoolinput, ontoolcancelled, onerror) before connect(). Followed by a link to GUIDE.md#handlers-before-connect.

Step 4: Verify the `--help` change is visible:

```bash
cargo build -p cargo-pmcp
cargo run -p cargo-pmcp -- test apps --help | grep -q claude-desktop
cargo run -p cargo-pmcp -- test apps --help | grep -qi static
```

Run quality gate:
- `cargo fmt --all -- --check`
- `cargo clippy -p cargo-pmcp --all-targets -- -D warnings`
  </action>

  <verify>
    <automated>cargo build -p cargo-pmcp && cargo run -p cargo-pmcp -- test apps --help | grep -q claude-desktop && cargo run -p cargo-pmcp -- test apps --help | grep -qi static && grep -q "## App Validation" cargo-pmcp/README.md && grep -q claude-desktop cargo-pmcp/README.md && grep -q "## App Validation" crates/mcp-tester/README.md && grep -q "MIME profile" cargo-pmcp/README.md && grep -q "Vite singlefile" cargo-pmcp/README.md && grep -q "\-\-tool" cargo-pmcp/README.md && cargo clippy -p cargo-pmcp --all-targets -- -D warnings</automated>
  </verify>

  <acceptance_criteria>
    - `cargo-pmcp/src/commands/test/mod.rs` contains `claude-desktop` and `statically inspects` in the `Apps` variant doc comment
    - `cargo-pmcp/src/commands/test/mod.rs` contains the literal `Honors `--tool`` (verifies REVISION HIGH-4 doc note in clap help)
    - `cargo run -p cargo-pmcp -- test apps --help` stdout contains `claude-desktop` and `statically` (case-insensitive)
    - `cargo-pmcp/README.md` contains `## App Validation`
    - `cargo-pmcp/README.md` contains `claude-desktop`
    - `cargo-pmcp/README.md` contains `--tool` reference (REVISION HIGH-4 doc)
    - `cargo-pmcp/README.md` contains `MIME profile` (Gemini optional doc)
    - `cargo-pmcp/README.md` contains `Vite singlefile` (Gemini optional doc)
    - `cargo-pmcp/README.md` contains a reference to `mcp-apps-chess` (Pitfall 1 disclosure)
    - `crates/mcp-tester/README.md` contains `## App Validation` and `claude-desktop`
    - `cargo build -p cargo-pmcp` exits 0
    - `cargo clippy -p cargo-pmcp --all-targets -- -D warnings` exits 0
    - `cargo fmt --all -- --check` exits 0
  </acceptance_criteria>

  <done>--help long-text shows new mode semantics including `--tool` honoring (REVISION HIGH-4); both READMEs document the mode + MIME profile + Vite note (Gemini optional); Pitfall 1 disclosure landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add `error_messages_anchored` integration test asserting end-to-end anchor expansion + tool-name presence (REVISION HIGH-4)</name>

  <read_first>
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-VALIDATION.md (last row: "Error messages link to GUIDE anchor")
    - .planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-03-PLAN.md (the broken_no_sdk.html fixture from Plan 03)
    - crates/mcp-tester/src/report.rs (the `expand_guide_anchor` helper from Task 1)
    - crates/mcp-tester/src/app_validator.rs (Plan 01's `validate_widgets` — emits `[guide:handlers-before-connect]` tokens, three-element tuple per REVISION HIGH-4)
  </read_first>

  <files>crates/mcp-tester/tests/error_messages_anchored.rs</files>

  <behavior>
    - Test 1 (error_messages_anchored): run `validate_widgets` under ClaudeDesktop on a broken widget; for each result with `Some(details)`, expand via `expand_guide_anchor`; assert at least one expanded details string contains the canonical URL.
    - Test 2 (no_orphaned_guide_tokens): every `details` string round-trips cleanly through `expand_guide_anchor` with no `[guide:` substring remaining.
    - Test 3 (error_messages_include_tool_name) — REVISION HIGH-4: assert each Failed result's `name` field contains the tool name passed in the tuple (`open_dashboard`).
  </behavior>

  <action>
Create `crates/mcp-tester/tests/error_messages_anchored.rs`. REVISION HIGH-4: three-element tuple `(tool_name, uri, html)`:

```rust
//! End-to-end test: validator emits `[guide:SLUG]` tokens, the report printer
//! expander turns them into absolute GitHub URLs.

use mcp_tester::{expand_guide_anchor, AppValidationMode, AppValidator, TestStatus};

const BROKEN_INLINE: &str = r#"<!doctype html>
<html><head><title>broken</title></head><body>
<div id="app">Loading</div>
<script type="module">
  const root = document.getElementById("app");
  window.openai = window.openai || {};
</script>
</body></html>"#;

#[test]
fn error_messages_anchored() {
    let validator = AppValidator::new(AppValidationMode::ClaudeDesktop, None);
    let results = validator.validate_widgets(&[(
        "open_dashboard".to_string(),
        "ui://test".to_string(),
        BROKEN_INLINE.to_string(),
    )]);
    let any_expanded_to_url = results.iter().filter_map(|r| r.details.as_ref()).any(|d| {
        let expanded = expand_guide_anchor(d);
        expanded.contains(
            "https://github.com/paiml/rust-mcp-sdk/blob/main/src/server/mcp_apps/GUIDE.md#handlers-before-connect",
        )
    });
    assert!(
        any_expanded_to_url,
        "At least one validation result must emit an [guide:handlers-before-connect] token \
         that expand_guide_anchor turns into the canonical GUIDE.md URL."
    );
}

#[test]
fn no_orphaned_guide_tokens() {
    let validator = AppValidator::new(AppValidationMode::ClaudeDesktop, None);
    let results = validator.validate_widgets(&[(
        "open_dashboard".to_string(),
        "ui://test".to_string(),
        BROKEN_INLINE.to_string(),
    )]);
    for r in &results {
        if let Some(d) = &r.details {
            let expanded = expand_guide_anchor(d);
            assert!(
                !expanded.contains("[guide:"),
                "Result {:?} contains an unexpanded [guide:...] token after expander pass; \
                 every slug emitted by validator must appear in expand_guide_anchor's \
                 KNOWN_SLUGS list. Token block: {}",
                r.name,
                expanded
            );
        }
    }
}

/// REVISION HIGH-4: every Failed row's `name` field must include the tool
/// name (`open_dashboard`), so error reports identify which tool the widget
/// belongs to.
#[test]
fn error_messages_include_tool_name() {
    let validator = AppValidator::new(AppValidationMode::ClaudeDesktop, None);
    let results = validator.validate_widgets(&[(
        "open_dashboard".to_string(),
        "ui://test".to_string(),
        BROKEN_INLINE.to_string(),
    )]);
    let failed: Vec<_> = results.iter().filter(|r| r.status == TestStatus::Failed).collect();
    assert!(failed.len() >= 1, "must produce >=1 Failed row");
    for r in &failed {
        assert!(
            r.name.contains("open_dashboard"),
            "REVISION HIGH-4: Failed row name must include tool name; got: {}",
            r.name
        );
    }
}
```

Run:
- `cargo test -p mcp-tester --test error_messages_anchored`
- `cargo clippy -p mcp-tester --all-targets -- -D warnings`
  </action>

  <verify>
    <automated>cargo test -p mcp-tester --test error_messages_anchored && cargo clippy -p mcp-tester --all-targets -- -D warnings</automated>
  </verify>

  <acceptance_criteria>
    - File `crates/mcp-tester/tests/error_messages_anchored.rs` exists
    - File contains `mcp_tester::expand_guide_anchor` (or equivalent import)
    - File contains `#[test] fn error_messages_anchored`
    - File contains `#[test] fn no_orphaned_guide_tokens`
    - File contains `#[test] fn error_messages_include_tool_name` (REVISION HIGH-4)
    - `cargo test -p mcp-tester --test error_messages_anchored` exits 0 (3 tests pass)
    - `cargo clippy -p mcp-tester --all-targets -- -D warnings` exits 0
  </acceptance_criteria>

  <done>End-to-end anchor expansion verified by 3 integration tests; tool name in error report verified per REVISION HIGH-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validator emission to printer | The `details` strings on `TestResult` originate from `AppValidator::validate_widgets` (Plan 01). Plan 04's `expand_guide_anchor` rewrites them at print time. The boundary is in-process and from a trusted code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04-01 | Information Disclosure | `expand_guide_anchor` rewrites a string into a clickable URL pointing to a public GitHub repo | accept | Both the slug list and the URL prefix are static literals. The expanded URL points to public documentation only. |
| T-78-04-02 | Tampering | A future Plan 01 modification adds a new `[guide:NEW-SLUG]` token without updating `KNOWN_SLUGS` | mitigate | Task 4's integration test `no_orphaned_guide_tokens` asserts every `details` string round-trips cleanly. CI gate runs this on every PR. |
| T-78-04-03 | Denial of Service | Maliciously huge `details` string causes per-slug `String::replace` loop to run on KB-MB inputs | accept | `details` strings are produced only by `AppValidator::validate_widgets`, fixed-shape ~100-300 chars. |
</threat_model>

<verification>
Final pass after all four tasks:

- `cargo test -p mcp-tester report` (Task 1: 5 expander tests + 1 printer-level test pass)
- Verify all 5 GUIDE.md anchors via the for-loop grep (Task 2)
- `cargo run -p cargo-pmcp -- test apps --help | grep -q claude-desktop` (Task 3)
- `cargo run -p cargo-pmcp -- test apps --help | grep -qi static` (Task 3)
- `grep -q "## App Validation" cargo-pmcp/README.md` (Task 3)
- `grep -q "MIME profile" cargo-pmcp/README.md` (Task 3 — Gemini optional doc)
- `grep -q "Vite singlefile" cargo-pmcp/README.md` (Task 3 — Gemini optional doc)
- `grep -q "## App Validation" crates/mcp-tester/README.md` (Task 3)
- `cargo test -p mcp-tester --test error_messages_anchored` (Task 4: 3 tests pass)
- `cargo test -p mcp-tester` (regression)
- `cargo clippy -p mcp-tester --all-targets -- -D warnings` and `cargo clippy -p cargo-pmcp --all-targets -- -D warnings`
- `cargo fmt --all -- --check`

All commands must exit 0.
</verification>

<success_criteria>
- `crates/mcp-tester/src/report.rs` has `pub fn expand_guide_anchor(details: &str) -> String` and the printer calls it; new `print_to_writer` writer-seam helper added (Codex MEDIUM)
- 5 unit tests for expand_guide_anchor pass + 1 printer-level test passes
- `crates/mcp-tester/src/lib.rs` re-exports `expand_guide_anchor` if needed
- `src/server/mcp_apps/GUIDE.md` has 5 explicit HTML id anchors placed immediately before the matching headings
- `cargo-pmcp/src/commands/test/mod.rs::TestCommand::Apps` has an enriched `///-doc` block mentioning `claude-desktop`, `statically inspects`, and `Honors --tool` (REVISION HIGH-4)
- `cargo-pmcp/README.md` has a new `## App Validation` section with the 3-mode table, examples (incl. `--tool` filter example), `--tool` mention, MIME profile note (Gemini), Vite singlefile note (Gemini), and Pitfall 1 disclosure
- `crates/mcp-tester/README.md` has a new `## App Validation` section
- `crates/mcp-tester/tests/error_messages_anchored.rs` exists with 3 integration tests, all passing — including `error_messages_include_tool_name` (REVISION HIGH-4)
- `cargo run -p cargo-pmcp -- test apps --help | grep -q claude-desktop` exits 0
- `cargo test -p mcp-tester` exits 0 (regression)
- `cargo clippy -p mcp-tester --all-targets -- -D warnings` and `cargo clippy -p cargo-pmcp --all-targets -- -D warnings` both exit 0
- `cargo fmt --all -- --check` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/78-cargo-pmcp-test-apps-mode-claude-desktop-detect-missing-mcp-/78-04-SUMMARY.md` summarizing: (1) anchor-expander helper + writer-seam refactor, (2) 5 explicit HTML id anchors, (3) README sections added with REVISION HIGH-4 (`--tool` filter doc) + Gemini optional Vite/MIME notes, (4) enriched clap doc text, (5) 3 integration tests, (6) any deviations.
</output>
