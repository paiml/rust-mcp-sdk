---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 11
type: execute
wave: 6
depends_on: [118.2-10]
files_modified:
  - scripts/run-conformance-suite.sh
  - .planning/REQUIREMENTS.md
  - .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md
autonomous: true
requirements: [CONF-09, CONF-10]

estimate:
  tokens: 108000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The official suite is re-measured at the HELD 0.2.0-alpha.11 pin and the delta is attributable to this phase's fixes alone"
    - "tools-call-with-logging is green on the 2025-11-25 leg and GAP_ATTRIBUTABLE_FAILURES is 0"
    - "The 2025-11-25 leg is gated on its own EXIT CODE, not only on a check-count floor"
    - "No allowlist, no --expected-failures, no known-failure baseline is introduced"
    - "CONF-09 and CONF-10 are booked with named binary(...) selectors and parsed non-zero counts"
  artifacts:
    - path: "scripts/run-conformance-suite.sh"
      provides: "2025-11-25 in FULLY_SCORED_GREEN_REVISIONS with a PER-REVISION scored-scenario floor and a widened blocking list"
      contains: "2025-11-25"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-09 and CONF-10 booked with evidence"
  key_links:
    - from: "the held-pin re-measurement"
      to: "MIN_CHECKS_V1 and the per-revision scored floor"
      via: "floors raised to the measured numbers in the same edit as the widening"
      pattern: "MIN_CHECKS_V1"
---

<objective>
Re-measure the official suite at the **held** `0.2.0-alpha.11` pin, harden the CI gate to require the
`2025-11-25` leg to **exit 0** (D-16), and book CONF-09 / CONF-10.

Purpose: D-14 repeats 118.1's D-08 verbatim — fix against the referee that produced 118.1's closing
numbers so the delta is attributable to these two changes alone. 118.1's own bump delta was recorded as
nil, so its closing numbers ARE `0.2.0-alpha.11` numbers and the comparison is like-for-like.

The baseline to beat (118.1's closing measurement, same pin):

| Requirement set | Result | Exit | Scored red | Total checks |
|---|---|---|---|---|
| `2025-11-25` | 72 passed, 2 failed | 1 | 1 | 74 |
| `2026-07-28` | 142 passed, 36 failed | 0 | 0 | 178 |

The one remaining scored red on v1 is `tools-call-with-logging` (`GAP_ATTRIBUTABLE_FAILURES = 1`). The
other v1 failure is `ServerAcceptsWhitespaceHeaderValue`, already **REFUTED** as an SDK defect (the
server trims OWS correctly in 14/14 fresh processes) and a suite-side check-design issue.

**D-16 is the headline of the phase's gate work:** the first time an era leg is gated on its EXIT CODE
rather than a check-count floor. A floor that only counts checks can be satisfied by a run that fails.
**D-21 carries forward verbatim: no `--expected-failures`, no allowlist, no known-failure baseline.**

Output: two measured numbers, a hardened gate, and two booked requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-CONTEXT.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-10-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. Line numbers grep-confirmed in the working tree. -->

`scripts/run-conformance-suite.sh` gate constants:

| Constant | Line | Current value | Disposition |
|---|---|---|---|
| `MIN_CHECKS_V1` | `:213` | `74` | RAISE to the re-measured number |
| `MIN_CHECKS_V2` | `:214` | `178` | unchanged unless the measurement moves it |
| `MRTR_SCENARIO_PREFIX` / `MIN_MRTR_SCENARIOS` | `:232` / `:233` | `input-required-result-` / `14` | unchanged |
| `FULLY_SCORED_GREEN_REVISIONS` | `:259` | `(2026-07-28)` | ADD `2025-11-25` |
| `MIN_SCORED_SCENARIOS_PER_FULLY_GREEN_REVISION` | `:265` | `37` | **must become PER-REVISION** |
| `BLOCKING_GREEN_SCENARIOS` | `:301-331` | 29 `2025-11-25:` entries | see below |
| `MIN_BLOCKING_GREEN_SCENARIOS` | `:337` | `29` | see below |

The `FULLY_SCORED_GREEN_REVISIONS` clause is ALREADY the strongest shape and already includes an
exit-code assertion. Read `:857-899`: for every revision named there it asserts (i) `scored_scenarios >=`
the floor, (ii) `scored_failing == 0`, and (iii) `suite_status_for_rev "$rev" == 0` — with the message
"this assertion and the scored-failure count above are two INDEPENDENT authorities on the same fact". So
D-16's "hard-require v1 exit 0" is delivered by ADDING the revision to that list, not by writing new
exit-code logic.

**Pitfall 10, the concrete trap.** `MIN_SCORED_SCENARIOS_PER_FULLY_GREEN_REVISION` is a SINGLE constant
applied per-revision at `:859`. `37` is the v2 measurement; the v1 leg has 30 scored scenarios (29 in
`BLOCKING_GREEN_SCENARIOS` + `tools-call-with-logging`). Adding `2025-11-25` without splitting the floor
fails with *"the '2025-11-25' run had only 30 scored scenario(s); the floor is 37."* Make the floor
per-revision (a lookup keyed by revision, mirroring `min_checks_for_rev` at `:487-490`, or a second
constant `MIN_SCORED_SCENARIOS_V1`) **in the same edit**. Lowering the shared constant to 30 would
silently weaken the v2 guard — do not do that. The script's own comment at `:224`/`:293` anticipates the
widening but does NOT mention this floor.

**A SECOND floor trap this plan must not walk into, not named in RESEARCH.** The script's `:259` comment
says "when it lands, MOVE 2025-11-25 here and delete its entries from BLOCKING_GREEN_SCENARIOS below —
that is a widening, not a rewrite." But `MIN_BLOCKING_GREEN_SCENARIOS = 29` (`:337`) is a floor on the
LIST SIZE, checked at `:993`, and its own comment says **NEVER LOWERED**. Deleting the 29 entries makes
`blocking_listed` 0 and fails that floor. **Resolution: do NOT delete the entries.** Keep all 29, ADD
`2025-11-25:tools-call-with-logging` as a 30th, and RAISE `MIN_BLOCKING_GREEN_SCENARIOS` to 30. That is
strictly stricter in both directions — the whole-scored-set clause covers the leg universally AND the
named list still cannot be quietly shortened — and it avoids contradicting a constant marked NEVER
LOWERED. Record this deviation from the script's own comment, and update that comment to say why keeping
the entries is the stronger choice.

`GAP_ATTRIBUTABLE_FAILURES` and the gaps document: `GAPS_DOC=".planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md"` (near `:344`).

The suite command: `bash scripts/run-conformance-suite.sh`. It builds the examples at `:625` and runs
`npm ci --ignore-scripts` in `conformance/` itself. The pin is
`conformance/package.json` `"@modelcontextprotocol/conformance": "0.2.0-alpha.11"` — **do not touch it in
this plan.** Plan 12 owns the re-pin.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure both legs at the HELD pin and record the attributable delta</name>
  <files>.planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md</files>
  <read_first>
    - scripts/run-conformance-suite.sh — read `:1-120` (the header explaining what the script asserts and why), the constants block `:205-345`, `min_checks_for_rev` (`:487-490`), the per-revision loop and the fully-scored-green clause (`:750-900`), and the blocking-list check (`:975-1020`).
    - conformance/package.json — confirm the pin is still `0.2.0-alpha.11` BEFORE running. If it is not, STOP: the measurement would not be like-for-like with 118.1's closing numbers.
    - .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md — read the amendment separating SDK defects from missing fixtures, and the G-1..G-9 disposition table, so the new entry is appended in the document's own idiom.
    - .planning/phases/118.2-.../118.2-09-SUMMARY.md — confirm the example binary was rebuilt; a stale `target/debug/examples/s54_v2_dual_conformance` produces a false measurement.
  </read_first>
  <action>
Rebuild the conformance example, then run the suite at the HELD pin and record BOTH legs.

Run `cargo build --example s54_v2_dual_conformance --features "streamable-http,testing"`, then
`bash scripts/run-conformance-suite.sh 2>&1 | tee target/118.2-11-conf-heldpin.log`.

The gate will still be at its pre-hardening settings for this run — that is deliberate. This measurement
is the INPUT to task 2's floor values, so it must be taken before the floors move.

Extract and record, for each leg, from the log and from `target/conformance-results/<rev>/`:
- passed / failed counts, total checks, and the suite's own EXIT status per revision;
- the scored-scenario count per revision;
- the per-scenario status of `2025-11-25:tools-call-with-logging` specifically, with its checks.json
  content — this is the scenario the whole phase exists to flip;
- `GAP_ATTRIBUTABLE_FAILURES`;
- whether `ServerAcceptsWhitespaceHeaderValue` fired in this run (it is MEASURED FLAKY at ~3 per 14 fresh
  processes and is already REFUTED as an SDK defect; note it, do not chase it).

Compute the delta against 118.1's closing numbers (v1: 72 passed / 2 failed / 74 checks / exit 1;
v2: 142 passed / 36 failed / 178 checks / exit 0). Present it as a table with a per-scenario breakdown of
every status change, so the delta is attributable to this phase's fixes rather than asserted.

**If `tools-call-with-logging` is still red**, STOP and report before touching the gate. Read the
scenario's checks.json and diagnose against Pitfall 11's measured requirements (≥1 notification, ≥3 log
messages, within the 200 ms window after `setLoggingLevel('debug')`) and RESEARCH assumption A2 (the
timing interaction was NOT measured). Do not harden a gate against a red leg.

**If the v1 leg does not exit 0** — for example because `ServerAcceptsWhitespaceHeaderValue` scored on
this run — record that exposure explicitly and STOP before task 2. D-16 anticipates exactly this: the
refuted flake is a live risk, and the instruction is to record the exposure in the phase summary rather
than pre-emptively softening the gate. Bring it to the developer rather than deciding alone.

AMEND `118-CONFORMANCE-GAPS.md` with a Phase 118.2 entry recording the disposition of the two OPEN G-3
sub-items that plan `118.1-13`'s D-10 gate signed off: the client transport residual and the log-emitter
residual. Write it in that document's existing idiom (FIXED / REFUTED / DEFERRED with cited evidence),
and do not restructure the document.
  </action>
  <verify>
    <automated>grep -E 'requirements 2025-11-25|requirements 2026-07-28|scored scenario|suite exit' target/118.2-11-conf-heldpin.log | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `conformance/package.json` still pins `0.2.0-alpha.11` — verified BEFORE the run and unchanged after
    - `target/118.2-11-conf-heldpin.log` exists and contains both legs' pass/fail/check counts and both legs' suite exit statuses
    - `target/conformance-results/2025-11-25/` contains a `tools-call-with-logging` scenario directory whose `checks.json` shows zero FAILURE checks and at least one SUCCESS check
    - `GAP_ATTRIBUTABLE_FAILURES` is 0, recorded from the log
    - The delta table against 118.1's closing numbers is written into the SUMMARY with a per-scenario breakdown
    - `118-CONFORMANCE-GAPS.md` carries a Phase 118.2 amendment dispositioning both G-3 sub-items
    - No gate constant was changed in this task
  </acceptance_criteria>
  <done>Both legs are measured at the held pin, the delta is attributable and written down, and `tools-call-with-logging` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the gate — v1 joins the fully-scored-green set, with the floor split in the same edit</name>
  <files>scripts/run-conformance-suite.sh</files>
  <read_first>
    - scripts/run-conformance-suite.sh — read the ENTIRE constants block `:205-345` including every comment, `min_checks_for_rev` (`:487-490`) as the per-revision-lookup shape to copy, `rev_is_fully_scored_green` and the clause it guards (`:857-899`), and the blocking-list check with its NEVER-LOWERED comment (`:975-1020`).
    - target/118.2-11-conf-heldpin.log — the measurement from task 1. Every floor value in this task comes from it, not from this plan's prose.
  </read_first>
  <action>
Four coupled edits, all in one commit, because each of them alone breaks the run.

**(a) Add `2025-11-25` to `FULLY_SCORED_GREEN_REVISIONS`** (`:259`). This is what delivers D-16: the
clause at `:857-899` already asserts the revision's OWN suite exit status is 0, alongside the
scored-failure count, calling them "two INDEPENDENT authorities on the same fact." No new exit-code logic
is needed or wanted.

**(b) Make the scored-scenario floor PER-REVISION** (`:265`). Mirror `min_checks_for_rev`'s shape
(`:487-490`): a small function keyed by revision, or a second constant `MIN_SCORED_SCENARIOS_V1` selected
by revision. Set each revision's floor to the number MEASURED in task 1 (expected: v1 = 30, v2 = 37).
**Do NOT lower the shared constant to 30** — that silently weakens the v2 guard, which is the one thing
the constant's own comment forbids. Carry the "NEVER LOWERED" rationale into whichever form the split
takes, per revision.

**(c) Widen `BLOCKING_GREEN_SCENARIOS`, do not shorten it.** ADD `2025-11-25:tools-call-with-logging` as
a 30th entry and RAISE `MIN_BLOCKING_GREEN_SCENARIOS` to 30. Then UPDATE the `:259` comment that
currently instructs deleting the v1 entries: explain that deleting them would drive `blocking_listed` to
0 and violate `MIN_BLOCKING_GREEN_SCENARIOS`'s NEVER-LOWERED floor, and that keeping them is strictly
stronger — the whole-scored-set clause covers the leg universally AND the named inclusion list still
cannot be quietly shortened. Also update the `BLOCKING_GREEN_SCENARIOS` provenance comment (`:282-288`),
which currently says the 30th scored scenario `tools-call-with-logging` "FAILS and is therefore simply
not claimed": it is now claimed, and the comment must say when and why it changed.

**(d) Raise `MIN_CHECKS_V1`** (`:213`) to the number measured in task 1, and raise `MIN_CHECKS_V2`
(`:214`) only if the v2 total moved. These are floors that are raised, never lowered. Update the
provenance comment above them with this phase's measurement, in the same idiom as the existing
118.1-14 entry.

**D-21 carries forward verbatim.** Do NOT add `--expected-failures`, an allowlist, or a known-failure
baseline in any form. `conformance/README.md` § 9 admits none. If any assertion cannot be satisfied, that
is a finding to report, not a constant to soften.

Then re-run the suite WITH the hardened gate and confirm it passes:

`bash scripts/run-conformance-suite.sh 2>&1 | tee target/118.2-11-conf-hardened.log`.

The script's summary output should now name `2025-11-25` and `2026-07-28` in its whole-scored-set line.
  </action>
  <verify>
    <automated>bash scripts/run-conformance-suite.sh 2>&1 | tee target/118.2-11-conf-hardened.log | tail -25 &amp;&amp; grep -E 'whole-scored-set green' target/118.2-11-conf-hardened.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FULLY_SCORED_GREEN_REVISIONS=' scripts/run-conformance-suite.sh` shows BOTH `2025-11-25` and `2026-07-28`
    - The scored-scenario floor is per-revision — `grep -c 'MIN_SCORED_SCENARIOS' scripts/run-conformance-suite.sh` returns ≥ 2, or a revision-keyed function exists; the v2 floor is still 37
    - `grep -c '2025-11-25:' scripts/run-conformance-suite.sh` shows 30 blocking entries, and `MIN_BLOCKING_GREEN_SCENARIOS` is 30
    - `MIN_CHECKS_V1` equals the value measured in task 1 and is ≥ 74
    - `grep -cE '\-\-expected-failures|allowlist|known.failure' scripts/run-conformance-suite.sh` returns 0 new occurrences (existing prose forbidding them may match; verify each hit is a prohibition, not an introduction)
    - `bash scripts/run-conformance-suite.sh` exits 0 with the hardened gate, and its summary line names both revisions as whole-scored-set green
    - The `:259` and `:282-288` comments are updated to explain the deviation from "delete its entries" and the change in `tools-call-with-logging`'s claimed status
  </acceptance_criteria>
  <done>The `2025-11-25` leg is gated on its own exit code with a per-revision floor, the blocking list is wider rather than shorter, and no exemption mechanism was introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Book CONF-09 and CONF-10 with named selectors and parsed counts</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read the CONF-09 / CONF-10 checklist entries (minted at planning time, currently `- [ ]`) and their traceability rows (currently `Pending`), plus the CONF-04..08 rows immediately above them as the booking idiom: named `binary(...)` selectors, parsed non-zero counts, suite-corroborated scenario transitions, and explicitly stated limitations.
    - All plan SUMMARYs `118.2-01` through `118.2-10` — the booking must cite evidence that was actually produced, with the real counts.
    - target/118.2-11-conf-heldpin.log and target/118.2-11-conf-hardened.log — the suite corroboration.
  </read_first>
  <action>
Flip CONF-09 and CONF-10 to `[x]` in the checklist and replace their `Pending` traceability rows with
bookings, in the CONF-04..08 idiom.

Each booking must carry:
- the plan that books it (`118.2-11`);
- named `binary(...)` selectors with PARSED counts, taken from the actual runs, not from the plans:
  `binary(client_sse_stream)`, `binary(log_emitter)`, `binary(pmcp_both_ends_logging)`,
  `binary(era_matrix)`, `binary(http_peer_roundtrip)`, `binary(v2_bounded_reads_tripwire)`,
  `binary(v1_severability_tripwire)`, `binary(v2_retired_methods)`. State each count and that it is
  non-zero — a `test(/…/)` selector silently selects zero tests and exits 0, which is why the selector
  form is recorded alongside the count;
- OFFICIAL-SUITE corroboration, not only in-tree evidence: `2025-11-25:tools-call-with-logging` moving
  from failing to `N/0`, `GAP_ATTRIBUTABLE_FAILURES` 1 → 0, and the v1 leg's exit status 1 → 0;
- the `cargo semver-checks` verdict recorded by plan 04, discharging ROADMAP success criterion 3;
- any STATED LIMITATION, written plainly rather than omitted. At minimum:
  - the v2 log vehicle is `new_v2_progress_queue` (`mpsc::channel(64)`, DROP-NEWEST `try_send`), so a v2
    handler emitting more than 64 notifications in one call loses the excess — the progress queue's
    policy, not the emitter's;
  - a v2 client that requests only JSON (no `text/event-stream` in `Accept`, or `enable_json_response`
    on) receives no log records at all, by design;
  - `extra.log` with no sink is silently `Ok(())` (D-08), a production-diagnostics hole caught by the
    conformance fence rather than by the runtime;
  - the D-15.3 joint fence asserts at the TRANSPORT layer because `pmcp::Client` has no notification
    observation API; that API is deferred with its measurement.

Also update CONF-07's traceability row: it currently carries **two named preconditions, both OPEN
sub-items of G-3 and both owned by Phase 118.2** — the client transport and the log emitter. Both are now
closed. Amend the row to say so, citing `118.2-11` and the dates, WITHOUT rewriting CONF-07's original
booking evidence. Amend, do not replace: 118.1's evidence remains valid for what it booked.

Update the Coverage block arithmetic if the booking changes any tally, and update the `Last updated:`
footer line in the document's existing idiom.
  </action>
  <verify>
    <automated>grep -c 'CONF-09\|CONF-10' .planning/REQUIREMENTS.md &amp;&amp; grep -E '^\- \[x\] \*\*CONF-09|^\- \[x\] \*\*CONF-10' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - CONF-09 and CONF-10 are `- [x]` in the checklist body
    - Their traceability rows name plan `118.2-11`, list at least four `binary(...)` selectors with parsed non-zero counts, and cite the suite transition for `tools-call-with-logging`
    - CONF-07's row is AMENDED (not replaced) to record that both of its named preconditions are closed
    - The stated limitations listed above all appear in the bookings
    - `.planning/REQUIREMENTS.md` still has exactly one traceability row per requirement — no duplicate CONF-09/CONF-10 rows from the planning-time mint
    - The Coverage block arithmetic is consistent with the document's contents
  </acceptance_criteria>
  <done>Both requirements are booked on measured evidence with their limitations stated, and CONF-07's open preconditions are recorded as closed.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and rows this plan CREATES (excluded from drift verification):

**New / changed shell items** (in `scripts/run-conformance-suite.sh`)
- `2025-11-25` added to `FULLY_SCORED_GREEN_REVISIONS`
- the shared `MIN_SCORED_SCENARIOS_PER_FULLY_GREEN_REVISION` constant SPLIT into a per-revision lookup
  (v1 and v2 floors set independently, v2's never lowered)
- `BLOCKING_GREEN_SCENARIOS` WIDENED (never shortened) and `MIN_BLOCKING_GREEN_SCENARIOS` raised to match
- `MIN_CHECKS_V1` / `MIN_CHECKS_V2` raised to the measured numbers

**New requirement rows**
- `CONF-09` and `CONF-10` booked in `.planning/REQUIREMENTS.md` with their evidence, in BOTH the
  checklist and the traceability table (D-17)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external conformance suite → the CI gate | A third-party referee whose verdict blocks merges; its pin is a supply-chain input |
| gate constants → the claim the repository makes | Every floor is a claim; lowering one weakens the claim silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118.2-11-01 | Repudiation | a check-count floor satisfied by a failing run | high | mitigate | D-16: the `2025-11-25` leg joins `FULLY_SCORED_GREEN_REVISIONS`, whose clause asserts the revision's OWN suite exit status is 0 alongside an independently derived scored-failure count |
| T-118.2-11-02 | Tampering | weakening the v2 guard while splitting the floor | high | mitigate | The floor becomes per-revision rather than being lowered; the v2 floor stays 37, asserted explicitly in the acceptance criteria |
| T-118.2-11-03 | Tampering | quietly shortening the blocking inclusion list | high | mitigate | The list is WIDENED to 30 and `MIN_BLOCKING_GREEN_SCENARIOS` raised to 30, deliberately deviating from the script's own "delete its entries" comment, which would have driven the count to 0 and violated a NEVER-LOWERED floor. The deviation is written into the comment |
| T-118.2-11-04 | Repudiation | an exemption mechanism smuggled in to make the gate pass | high | mitigate | D-21 verbatim: no `--expected-failures`, no allowlist, no known-failure baseline. If an assertion cannot be met, the plan STOPS and reports rather than softening |
| T-118.2-11-05 | Repudiation | a false measurement from a stale example binary | high | mitigate | The example is rebuilt before the run, and `assert_binary_is_not_stale` guards every example-spawning fence. A stale binary produced a false RED in 118.1 |
| T-118.2-11-06 | Denial of Service | the refuted `ServerAcceptsWhitespaceHeaderValue` flake redding a hardened gate | medium | accept | MEASURED flaky at ~3 per 14 fresh processes and REFUTED as an SDK defect. If it scores on this run the plan STOPS and reports the exposure to the developer rather than pre-emptively softening the gate, per D-16 |
| T-118.2-11-SC | Tampering | conformance suite supply chain | high | mitigate | The pin is HELD at `0.2.0-alpha.11` for this entire plan; the committed lockfile is unchanged. Plan 12 owns the re-pin and records its lockfile diff |
</threat_model>

<verification>
- `bash scripts/run-conformance-suite.sh` — exits 0 with the hardened gate; summary names both revisions as whole-scored-set green
- `target/118.2-11-conf-heldpin.log` and `target/118.2-11-conf-hardened.log` both retained
- `conformance/package.json` unchanged (`0.2.0-alpha.11`)
- `make quality-gate`
- All phase fences green: `binary(client_sse_stream)`, `binary(log_emitter)`, `binary(pmcp_both_ends_logging)`, `binary(era_matrix)`, `binary(http_peer_roundtrip)`, `binary(v2_bounded_reads_tripwire)`, `binary(v1_severability_tripwire)` — each non-zero count, 0 failed
</verification>

<success_criteria>
- The v1 leg exits 0 and is gated on that exit code
- `GAP_ATTRIBUTABLE_FAILURES` is 0 across both legs
- The delta is attributable to this phase's fixes alone, at the held pin, with a per-scenario breakdown
- CONF-09 and CONF-10 are booked with named selectors, parsed counts, suite corroboration and stated limitations
- No exemption mechanism of any shape was introduced
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-11-SUMMARY.md` when done.
Record: both legs' measured numbers and exit statuses, the per-scenario delta table, the exact gate constants before and after, the `MIN_BLOCKING_GREEN_SCENARIOS` deviation and its rationale, and whether `ServerAcceptsWhitespaceHeaderValue` fired.
</output>
