---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 06
type: execute
wave: 2
depends_on: [118.2-05]
files_modified:
  - src/types/protocol/context.rs
  - src/server/core.rs
  - src/server/mod.rs
  - tests/log_emitter.rs
autonomous: true
requirements: [CONF-10]

estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One shared unit builds the log sink AND applies the resolved log level, and BOTH native dispatch roots call it"
    - "A request-scoped TransportBackchannel sink wins over the root's global fallback"
    - "ServerCore, which has no notification channel at all, still gets the request-scoped sink"
    - "The log sink is UNGATED by the progress token, while the ProgressReporter stays gated"
    - "The sink is attached after the tool_authorizer check, never before"
    - "ProtocolContext carries a crate-private resolved log level that the shared unit reads — the carrier the HTTP ingress writes in plan 07"
  artifacts:
    - path: "src/types/protocol/context.rs"
      provides: "ProtocolContext.resolved_log_level, a crate-private field with paired with_resolved_log_level / resolved_log_level accessors, modelled on the existing crate-private MRTR fields"
      contains: "resolved_log_level"
    - path: "src/server/core.rs"
      provides: "attach_request_log_sink — the ONE unit both roots call, twin of attach_request_peer"
      contains: "attach_request_log_sink"
    - path: "src/server/mod.rs"
      provides: "the Server root delegating to the shared unit with its notification_tx-derived fallback"
      contains: "attach_request_log_sink"
  key_links:
    - from: "ProtocolContext.transport_backchannel.notification_sink"
      to: "RequestHandlerExtra.log_sink"
      via: "attach_request_log_sink, called at the same site as attach_peer"
      pattern: "attach_request_log_sink"
    - from: "ProtocolContext.resolved_log_level"
      to: "RequestHandlerExtra.log_level"
      via: "the SAME shared unit, so the level and the sink cannot be attached at different sites or by different roots"
      pattern: "with_log_level"
    - from: "Server::notification_tx"
      to: "RequestHandlerExtra.log_sink"
      via: "the root's fallback closure, used only when no request-scoped sink exists"
      pattern: "notification_tx"
---

<objective>
Hoist the notification sink to a shared per-request unit (D-07) so a handler's `extra.log(..)` reaches
a client on both native dispatch roots.

Purpose: `ServerProgressReporter::new` takes `Arc<dyn Fn(Notification) + Send + Sync>` — a sink that
already carries **any** `Notification` — and it is already plumbed into both roots. But
`progress_reporter_for` (`src/server/mod.rs:1314`) only builds it when `req._meta` carries a progress
token, so today the sink is `None` for every call that did not ask for progress. One function builds
the sink for a request; the progress-token gate then applies **only** to the `ProgressReporter`, and the
log sink is unconditional.

**Both roots or neither.** CONTEXT.md names changing one and not the other as "the phase's most likely
silent defect", and 118.1 spent real effort collapsing exactly this class of divergence
(`completion/complete` had two dispatchers giving two *different* wrong answers). The precedent to copy
is `attach_request_peer` (`src/server/core.rs:4514`), created by 118.1 for precisely this reason.

**The asymmetry to size for:** `ServerCore` has **no `notification_tx` of any kind** — that field exists
only on `Server` — and all four of `ServerCore`'s `RequestHandlerExtra::new` sites pass
`progress_reporter: None` unconditionally. So `ServerCore`'s fallback argument to the shared unit is a
literal `None`, and its only possible sink is the request-scoped `TransportBackchannel`.

**This plan also owns the LOG-LEVEL CARRIER** (cross-AI review finding 3, resolved here rather than in
plan 07). The reviewed draft had plan 07 declare a `with_log_level` write from
`src/server/streamable_http_server.rs`, but that file NEVER constructs a `RequestHandlerExtra` —
verified: zero matches for `RequestHandlerExtra::new` anywhere under `src/server/streamable_http_server*`;
the constructors live in `src/server/core.rs` and `src/server/mod.rs`. The HTTP ingress can only RESOLVE
a level; something must CARRY it to the dispatch root that builds the `extra`.

The carrier is `ProtocolContext`, and it belongs here because this plan already owns the one unit that
reads `extra.protocol_context` at both roots. Task 1 therefore adds a crate-private
`resolved_log_level: Option<LoggingLevel>` field to `ProtocolContext` — the same shape as its existing
crate-private `mrtr` / `mrtr_verified` fields — and `attach_request_log_sink` attaches BOTH the sink and
the level. Until plan 07 writes it, the field is always `None` and `DEFAULT_LOG_LEVEL` applies, which is
the correct behaviour on its own, so this plan lands green in isolation.

*Rejected route, recorded:* threading the level through
`peer_channel.rs::attach_session_backchannel`. That function returns EARLY on `!site.sessions_on ||
site.is_init_request` (`src/server/streamable_http_server/peer_channel.rs:610`), and v2 has no sessions,
so it structurally cannot carry the v2 `_meta` level. A carrier that works for one era and not the other
is the drift this phase exists to remove.

Output: the carrier, one unit, two call sites, and fences proving both roots and the precedence rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-CONTEXT.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-05-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. -->

The pattern to twin, `src/server/core.rs:4485-4532` — `attach_request_peer(extra, global)`. Its rustdoc
states the rule verbatim: "the precedence rule is the kind of thing two roots must never disagree about,
and a rule spelled twice is kept in step by a comment instead of by the compiler. Both roots previously
carried this body verbatim." It also carries an `# Ordering` section: "Every dispatch site calls this
AFTER its `tool_authorizer` check, so an unauthorized caller returns before a handler body ever runs."
Its body clones the request-scoped value out of the borrow BEFORE `with_peer` consumes `extra`:

```
#[cfg(not(target_arch = "wasm32"))]
pub(crate) fn attach_request_peer(extra, global: Option<&Arc<dyn PeerHandle>>) -> RequestHandlerExtra {
    let request_scoped = extra.protocol_context.as_ref()
        .and_then(ProtocolContext::transport_backchannel)
        .and_then(TransportBackchannel::peer)
        .cloned();
    if let Some(peer) = request_scoped { return extra.with_peer(peer); }
    if let Some(peer) = global { return extra.with_peer(peer.clone()); }
    extra
}
```

Root-B delegation with its wasm arm, `src/server/core.rs:747-756`:

```
#[inline]
fn attach_peer(&self, extra: RequestHandlerExtra) -> RequestHandlerExtra {
    #[cfg(not(target_arch = "wasm32"))] { attach_request_peer(extra, self.peer_handle.as_ref()) }
    #[cfg(target_arch = "wasm32")] { extra }
}
```

Root-A's sink resolver, ALREADY correct and already preferring the request-scoped backchannel —
`src/server/mod.rs:1282-1300`, `fn progress_notification_sink(&self, protocol_context) -> Option<Arc<dyn Fn(Notification) + Send + Sync>>`:
it returns the `TransportBackchannel::notification_sink` when present, else derives a closure from
`self.notification_tx` with `try_send`. Root A's `attach_peer` is at `src/server/mod.rs:1248`.

The gate D-07 removes for the LOG path only, `src/server/mod.rs:1313-1322`:

```
fn progress_reporter_for(&self, meta, protocol_context) -> Option<Arc<dyn ProgressReporter>> {
    let token = meta.and_then(|meta| meta.progress_token.as_ref())?;   // <- gate STAYS here
    let sink = self.progress_notification_sink(protocol_context)?;
    ...
}
```
Its three call sites are `src/server/mod.rs:2199`, `:2526`, `:2644`. **The token gate must remain on the
`ProgressReporter`.** Only the log sink becomes unconditional.

The v1 sink that already exists on every non-`initialize` v1 session request —
`src/server/streamable_http_server/peer_channel.rs:506-517` builds `session_notification_sink` routing
through `v1::route_to_session_stream`, and `attach_session_backchannel` (`:605`, applied at `:637-646`)
hangs it on `ProtocolContext.transport_backchannel` together with the `SessionPeerHandle` whenever
`sessions_on && session_id.is_some() && !is_init_request`. Read it; do not rebuild it.

Pitfall 12 — the move that makes ordering matter: `ServerCore::handle_request`'s tool arm moves
`protocol_context` into `RequestHandlerExtra::with_protocol_context(protocol_context)` at
`src/server/core.rs:867`, and several values (`create_trigger`, `validation_era`) are deliberately
captured BEFORE that move. Read the sink back off `extra.protocol_context` AFTER construction — which is
exactly what `attach_request_peer` does — rather than capturing it before the move.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The ProtocolContext log-level carrier, plus attach_request_log_sink — one unit, called by both dispatch roots</name>
  <files>src/types/protocol/context.rs, src/server/core.rs, src/server/mod.rs</files>
  <read_first>
    - src/types/protocol/context.rs — read `TransportBackchannel` (`:107-198`) in full including its `UnwindSafe` / `RefUnwindSafe` block and WHY it exists; `ProtocolContext` (`:200-340`) including its `#[non_exhaustive]` declaration, its `pub era` / `pub negotiated_version` / `pub client_info` / `pub client_capabilities` fields, the CRATE-PRIVATE `mrtr` (`:239`) and `mrtr_verified` (`:245`) fields, the hand-written redacting `Debug` (`:262-280`), `ProtocolContext::new` (`:294`), `transport_backchannel` (`:314`) and `with_transport_backchannel` (`:325`). The crate-private-field-plus-`pub(crate)`-accessor shape is the pattern this task copies.
    - src/server/core.rs — read `attach_request_peer` (`:4485-4532`) in full including its rustdoc and its `# Ordering` section; `attach_peer` (`:747-756`); all four `RequestHandlerExtra::new` sites (`:856`, `:1143`, `:1172`, `:1223`) and the `with_protocol_context` move at `:867`.
    - src/server/mod.rs — read `attach_peer` (`:1248`), `progress_notification_sink` (`:1282-1300`), `progress_reporter_for` (`:1313-1322`) and its three call sites (`:2199`, `:2526`, `:2644`).
    - src/server/streamable_http_server/peer_channel.rs — read `session_notification_sink` (`:506-517`), `BackchannelSite` (`:521`) and `attach_session_backchannel` (`:605-650`) INCLUDING its `if !site.sessions_on || site.is_init_request { return context; }` early return at `:610`, so the precedence rule is written against the sink that actually exists and the rejected carrier route is understood rather than re-proposed.
    - src/server/cancellation.rs — the `log_sink` and `log_level` fields, the `with_log_sink` / `with_log_level` seams and `DEFAULT_LOG_LEVEL` plan 05 added.
  </read_first>
  <action>
**(a) The log-level carrier on `ProtocolContext`.** In `src/types/protocol/context.rs`, add

`resolved_log_level: Option<crate::types::notifications::LoggingLevel>`

as a CRATE-PRIVATE field on `ProtocolContext`, modelled exactly on the existing crate-private `mrtr` /
`mrtr_verified` fields, with:
- initialisation to `None` in `ProtocolContext::new`;
- `pub(crate) fn with_resolved_log_level(mut self, level: LoggingLevel) -> Self` — a `#[must_use]`
  builder matching `with_transport_backchannel`'s shape;
- `pub(crate) fn resolved_log_level(&self) -> Option<LoggingLevel>` — returns an OWNED `Option`, not a
  borrow, since `LoggingLevel` is `Copy`;
- rustdoc naming the problem it solves: the HTTP ingress knows the level (a v1 session's stored value or
  a v2 request `_meta` key) but never constructs a `RequestHandlerExtra`; the dispatch roots construct
  the `extra` but do not know the session. `ProtocolContext` is the value already threaded between them.
  Name the rejected alternative — `peer_channel::attach_session_backchannel` — and why: it returns early
  when sessions are off, and v2 has no sessions, so it cannot carry a v2 level.

Semver notes to check rather than assume: `ProtocolContext` is `#[non_exhaustive]`, so a new private
field is additive; `Option<LoggingLevel>` is a plain `Copy` enum so it cannot remove an auto-trait impl
(the reason the file carries explicit `UnwindSafe` / `RefUnwindSafe` impls for
`TransportBackchannel` — read that comment block at `:130-157` before adding anything to this type).
Do NOT add the field to the hand-written `Debug`'s redaction list unless it prints a value; a level is
not sensitive, so printing it is fine, but stay consistent with whatever the neighbouring fields do.

**(b) The shared unit.** Add `attach_request_log_sink` to `src/server/core.rs`, immediately beside
`attach_request_peer`, as its twin. Signature and shape mirror the peer unit exactly:

Signature: `pub(crate) fn attach_request_log_sink(extra: RequestHandlerExtra, fallback: Option<Arc<dyn Fn(Notification) + Send + Sync>>) -> RequestHandlerExtra`,
carrying the same `#[cfg(not(target_arch = "wasm32"))]` gate the peer unit carries.

Precedence, identical in shape to the peer rule: request-scoped
`ProtocolContext::transport_backchannel` → `TransportBackchannel::notification_sink` FIRST, cloned out
of the borrow before `with_log_sink` consumes `extra`; then the root's `fallback`; then `extra`
unchanged. Its rustdoc must carry, in the peer unit's own words, the reason it is one unit rather than
two copies, plus an `# Ordering` section stating that every dispatch site calls it AFTER its
`tool_authorizer` check so an unauthorized caller returns before a handler body ever runs.

**The SAME unit also applies the resolved level.** In the same read of `extra.protocol_context` — before
`with_log_sink` consumes `extra` — clone out `ProtocolContext::resolved_log_level()` and, when it is
`Some`, apply `with_log_level(..)`. When it is `None`, leave `extra.log_level` alone so plan 05's
`DEFAULT_LOG_LEVEL` applies at emit time. Doing both in one unit is the point: the level and the sink
are two halves of one per-request decision, and attaching them at different sites is how one root ends
up filtering differently from the other. State that in the rustdoc.

Reading the level here rather than at the ingress also makes the filter read O(1) at emit time — a plain
`Option<LoggingLevel>` on the `extra` — instead of a lock per record (RESEARCH Open Question 1).

Wire BOTH roots at the SAME site as `attach_peer`:
- **`ServerCore`** — extend `ServerCore::attach_peer` (`:747`) to also call the log-sink unit, or add a
  sibling `attach_log_sink` beside it with the identical `#[cfg]` wasm arm. `ServerCore`'s `fallback`
  argument is a literal `None`: it has NO `notification_tx` of any kind, so its only possible sink is
  the request-scoped `TransportBackchannel`. Say that in a comment at the call site — it is the
  asymmetry a future reader will otherwise try to "fix".
- **`Server`** — call the same unit from `Server::attach_peer`'s site (`src/server/mod.rs:1248`), passing
  the `notification_tx`-derived fallback. Reuse `progress_notification_sink`'s existing derivation rather
  than writing a second `try_send` closure: extract the `notification_tx` fallback half into a small
  helper if that is cleaner, but there must be exactly ONE place that turns `notification_tx` into a
  sink.

**Do not touch the progress-token gate.** `progress_reporter_for` keeps
`let token = meta.and_then(|meta| meta.progress_token.as_ref())?;` as its first line. The gate applies
only to the `ProgressReporter`. The log sink is unconditional. Add a one-line comment at the gate saying
so, so a reader does not later "unify" the two and make progress unconditional as well.

Keep both roots' wasm arms intact — on `wasm32` these functions return `extra` unchanged.

Keep every touched function under PMAT cog 25.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full" 2>&1 | tee target/118.2-06-t2-native.log' &amp;&amp; bash -o pipefail -c 'RUSTFLAGS="-D warnings" cargo build -p pmcp --target wasm32-unknown-unknown --no-default-features --features "logging" 2>&1 | tee target/118.2-06-t2-wasm.log'</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: with_log_level -->
    - `grep -v '^\s*//\|^\s*///' src/server/core.rs | grep -c 'attach_request_log_sink'` returns ≥ 2 (definition plus the `ServerCore` call)
    - `grep -v '^\s*//\|^\s*///' src/server/mod.rs | grep -c 'attach_request_log_sink'` returns ≥ 1 (the `Server` call)
    - `grep -v '^\s*//\|^\s*///' src/types/protocol/context.rs | grep -c 'resolved_log_level'` returns ≥ 3 (the field, the builder, the reader)
    - `grep -v '^\s*//\|^\s*///' src/server/core.rs | grep -c 'with_log_level'` returns ≥ 1 — the shared unit applies the level, and it is the ONLY place that does
    - `grep -rv '^\s*//\|^\s*///' src/server/streamable_http_server.rs | grep -c 'with_log_level'` returns 0 — the ingress writes `with_resolved_log_level` on the context (plan 07), never `with_log_level` on an `extra` it does not construct
    - `grep -n 'progress_token.as_ref()?' src/server/mod.rs` still shows the gate on the FIRST line of `progress_reporter_for` — unchanged
    - The number of places converting `self.notification_tx` into an `Arc<dyn Fn(Notification)>` closure is exactly 1 — verify by reading and record the location in the SUMMARY
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` exits 0
    - The wasm32 build still compiles (`RequestHandlerExtra` is a zero-field stub there; the `#[cfg(target_arch = "wasm32")]` arms must return `extra` unchanged)
    - No touched function exceeds PMAT cognitive complexity 25
  </acceptance_criteria>
  <done>One unit resolves the log sink; both native roots call it at the post-authorization site; the progress token gate is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fences proving both roots, the precedence rule, and the ordering constraint</name>
  <files>tests/log_emitter.rs</files>
  <read_first>
    - tests/log_emitter.rs — the file and fences plan 05 created; extend, do not restructure.
    - src/server/core.rs — the unit task 1 just wrote, plus the existing `attach_peer_is_a_no_op_when_neither_source_is_configured` test at `:9480`, which is the in-file precedent for testing this family of units.
    - src/server/mod.rs — the twin test at `:7323` with the same name. Both roots already have a paired unit test for the peer unit; the log-sink unit needs the same pairing.
  </read_first>
  <action>
Add fences to `tests/log_emitter.rs` for the four claims that distinguish this plan from "it compiles":

1. `a_request_scoped_sink_wins_over_the_root_fallback` — construct a `RequestHandlerExtra` whose
   `protocol_context` carries a `TransportBackchannel` with a distinguishable capturing sink, pass a
   DIFFERENT capturing sink as the `fallback`, run it through the unit, emit one record, and assert the
   REQUEST-SCOPED capture received it and the fallback capture is empty.

2. `the_root_fallback_is_used_when_no_request_scoped_sink_exists` — the mirror: no backchannel, a
   fallback sink, assert the fallback receives the record.

3. `both_dispatch_roots_attach_the_log_sink` — drive a real tool handler that calls `extra.log(..)`
   through EACH root and assert the record is observable in both. If a wire-level drive is too heavy
   here, assert structurally instead: both roots call `attach_request_log_sink`, proven by a source
   assertion over `src/server/core.rs` and `src/server/mod.rs` in the style the severability tripwire
   uses. Prefer the behavioural version; if the structural version is taken, say why in the SUMMARY, and
   note that plan 07's wire fences cover the behavioural half.

4. `the_progress_token_gate_still_applies_to_progress_only` — a request with NO progress token in
   `_meta` gets a live `log_sink` and a `None` `progress_reporter`. This is the D-07 claim stated as a
   test: the gate moved off the sink and stayed on the reporter.

5. `a_resolved_log_level_on_the_context_reaches_the_extra` — build a `ProtocolContext` carrying
   `with_resolved_log_level(LoggingLevel::Debug)`, run it through the unit, and assert the resulting
   `extra` filters at `debug` (a `log(Debug, ..)` record is captured). Then run a context with NO
   resolved level through the unit and assert the D-12 `info` default applies (a `log(Debug, ..)` record
   is NOT captured). This is the carrier fence: it proves the seam plan 07 writes into actually reaches
   the emitter, BEFORE plan 07 exists, so a broken carrier cannot hide behind a missing writer.

Add a note in the file's module doc pointing at ASVS V4 / Pitfall 12: the sink is attached at the same
site as `attach_peer`, which is already AFTER the `tool_authorizer` check, so an unauthorized caller
never reaches a handler that could emit. If an authorization-ordering fence is cheap to write here,
write it (`an_unauthorized_call_never_reaches_a_handler_with_a_live_sink`); otherwise record it as
covered-by-construction with the source location that makes it true.

Demonstrate RED before task 1 lands where the fence is behavioural (fences 1, 2, 4 will fail against a
tree without the unit). Capture to `target/118.2-06-red.log`, then GREEN to `target/118.2-06-green.log`.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(log_emitter)' --features "full" 2>&1 | tee target/118.2-06-green.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run -E 'binary(log_emitter)' --features "full"` reports a NON-ZERO test count with 0 failed, and the count is strictly greater than plan 05's recorded count
    - Fences 1, 2, 4 and 5 exist by name in `tests/log_emitter.rs`
    - Fence 3 exists in either its behavioural or its structural form, and the SUMMARY says which and why
    - `target/118.2-06-red.log` and `target/118.2-06-green.log` form the RED→GREEN pair
    - `cargo nextest run -E 'binary(http_peer_roundtrip)' --features "full"` reports 12 run, 12 passed
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>The precedence rule, the both-roots claim and the untouched progress gate are each pinned by a fence rather than by a comment.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols this plan CREATES (excluded from drift verification):

**New source items**
- `src/types/protocol/context.rs` — `ProtocolContext::resolved_log_level` (crate-private field),
  `ProtocolContext::with_resolved_log_level` (`pub(crate)` builder),
  `ProtocolContext::resolved_log_level()` (`pub(crate)` reader)
- `src/server/core.rs` — `attach_request_log_sink` (the shared unit), plus either an extension of
  `ServerCore::attach_peer` or a sibling `attach_log_sink` with the identical wasm `#[cfg]` arm
- `src/server/mod.rs` — the `Server` call site, and possibly a small extracted helper turning
  `notification_tx` into an `Arc<dyn Fn(Notification)>` (there must be exactly ONE such conversion site)

**New test functions** (appended to `tests/log_emitter.rs`)
- `a_request_scoped_sink_wins_over_the_root_fallback`
- `the_root_fallback_is_used_when_no_request_scoped_sink_exists`
- `both_dispatch_roots_attach_the_log_sink` (behavioural or structural form)
- `the_progress_token_gate_still_applies_to_progress_only`
- `a_resolved_log_level_on_the_context_reaches_the_extra`
- optionally `an_unauthorized_call_never_reaches_a_handler_with_a_live_sink`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated caller → tool handler | The `tool_authorizer` check is the boundary; anything attached before it is reachable without authorization |
| one session's request → another session's sink | The request-scoped backchannel is per-request; the root fallback is server-wide |
| HTTP ingress → dispatch root (the resolved level) | A per-request value crosses two layers; the carrier must not be able to leak between requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118.2-06-01 | Elevation of Privilege | log-sink attachment point | high | mitigate | Attach at the SAME site as `attach_peer`, which every dispatch site calls AFTER its `tool_authorizer` check, so an unauthorized caller returns before a handler body ever runs (ASVS V4). Stated in the unit's `# Ordering` rustdoc, mirroring `attach_request_peer` |
| T-118.2-06-02 | Information Disclosure | root fallback preferred over the request-scoped sink | high | mitigate | Precedence is request-scoped FIRST, fallback second — the same order as the peer unit. Inverting it would route one session's records to the server-wide channel. Fenced by `a_request_scoped_sink_wins_over_the_root_fallback` |
| T-118.2-06-03 | Tampering | the two roots drifting | high | mitigate | ONE unit, called by both, attaching BOTH the sink and the level, with the precedence rule spelled once. This is the class of defect 118.1 collapsed for `completion/complete`, where two dispatchers gave two DIFFERENT wrong answers |
| T-118.2-06-04 | Denial of Service | making progress unconditional by accident | medium | mitigate | The progress-token gate stays on `progress_reporter_for`'s first line with a comment saying it is deliberate. Fenced by `the_progress_token_gate_still_applies_to_progress_only` |
| T-118.2-06-05 | Information Disclosure | the log-level carrier leaking between requests | medium | mitigate | `resolved_log_level` lives on `ProtocolContext`, which is constructed PER REQUEST at ingress and moved into that request's `RequestHandlerExtra`; it is never stored on the shared `Arc<Mutex<Server>>`. That is the same isolation argument D-11 makes for the v1 session store, applied to the carrier. Fenced by `a_resolved_log_level_on_the_context_reaches_the_extra` here and by plan 07's cross-session fence |
| T-118.2-06-06 | Tampering | a semver break smuggled in through `ProtocolContext` | medium | mitigate | The new field is crate-private on a `#[non_exhaustive]` public struct and its type is a `Copy` enum, so no auto-trait impl can be removed — the failure mode the file's explicit `UnwindSafe`/`RefUnwindSafe` block at `:130-157` exists to prevent. Plan 04's `cargo semver-checks` run is the measurement |
| T-118.2-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no packages installed |
</threat_model>

<verification>
- `cargo nextest run -E 'binary(log_emitter)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(http_peer_roundtrip)' --features "full"` — 12 run, 12 passed
- `cargo nextest run -E 'binary(in_tool_peer_roundtrip)' --features "full"` — non-zero count, 0 failed
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` and the wasm32 build
- `make quality-gate`
</verification>

<success_criteria>
- Exactly one function resolves the log sink AND applies the resolved level, and both native roots call it
- `ProtocolContext` carries the resolved level as a crate-private field with paired `pub(crate)` accessors, and a fence proves it reaches the emitter before any writer exists
- The progress-token gate is provably still on the reporter and provably off the sink
- `ServerCore`'s literal-`None` fallback is explained at the call site rather than left as a puzzle
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-06-SUMMARY.md` when done.
Record: the unit's final signature; both call sites with line numbers; the `ProtocolContext` carrier's
field declaration and both accessor signatures verbatim; the single `notification_tx`-to-sink conversion
site; and which form fence 3 took.
</output>
