---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 24
type: execute
wave: 18
depends_on: [118.2-22, 118.2-25, 118.2-23]
files_modified:
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
  - pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md
  - tests/client_sse_stream.rs
autonomous: true
gap_closure: true
requirements: [CONF-09, CONF-10]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`WINDOWS.md` no longer asserts that per-id routing removed the mismatch ceiling. The false clause is GONE from BOTH the markdown table and the JSON mirror, replaced by a statement of what actually bounds the client at this HEAD, named by symbols that resolve"
    - "Every symbol any ledger entry or requirement row instructs a reader to LOCATE BY resolves in the tree at this HEAD. The five deleted identifiers appear nowhere in `.planning/WINDOWS.md`, `.planning/REQUIREMENTS.md` or `pmcp-book/` except where explicitly marked as superseded history"
    - "The THIRD gap-closure round — four commits, a 378-line rewrite of `dispatch_request` into a per-id router, the largest consumer-observable change in the phase — has a ledger entry of record naming what it deleted, what it added, what it left behind, and a semver verdict"
    - "Plans 22 and 23 each have a ledger entry disclosing what changed for a consumer, including the two residuals plan 23 accepts by name — concurrent POSTs on one transport, and outbound frame ordering"
    - "The consumer-facing migration chapter describes the client that exists: frames belonging to a live caller are ROUTED, not discarded; a frame nobody awaits is dropped and, if it is the late answer of an abandoned call, absorbed without charging anyone; and the send path no longer serialises the client"
    - "The `2025-11-25` and `2026-07-28` official-suite legs and the CONF-10 emitter backstop are re-measured at THIS HEAD and the deltas reported against Phase 118.1's closing numbers, so no measurement of record predates the tree it describes"
    - "The five live fence-calibration comments in `tests/client_sse_stream.rs` that reason about deleted constants are corrected to name the constants that actually set the bounds they are calibrated against"
    - statement: "binary(windows_disclosure_tripwire) stays green, and the plan records explicitly that green from that tripwire proves MIRRORING and not TRUTH — it cannot see a stale mirrored paragraph, which is how the record reached this state"
      verification: backstop
    - statement: "binary(client_sse_stream) stays green after the comment corrections, proving the edits were to prose and not to calibration"
      verification: backstop
  prohibitions:
    - "Do NOT append a correction beneath a false claim while leaving the false claim standing. Every correction in this plan removes the false clause and is verified by a NEGATIVE grep proving it is gone plus a POSITIVE grep proving the replacement is present. This is the house rule plan 21 set for exactly this file and it carries forward."
    - "Do NOT edit `.planning/WINDOWS.md`'s markdown table without applying the identical edit to its JSON mirror in the fenced block at the end of the same file. The two are one record; a divergence between them is a worse defect than the one being corrected."
    - "Do NOT hand-write NEW ledger entries into the table. New entries go through the `windows append` subcommand so the frontmatter counters stay consistent; only corrections to EXISTING entries' text are hand edits, and those must touch both representations."
    - "Do NOT flip CONF-09's or CONF-10's checkbox, and do NOT re-open their `Complete` bookings. Both requirements' literal text was satisfied and remains satisfied; what is corrected is EVIDENCE that points at deleted symbols and asserts a removal that did not happen."
    - "Do NOT mint a new requirement ID. D-17 already warns that ten REQ-IDs appear in bodies without table rows; a new ID would widen that warning for work belonging to CONF-09's surface."
    - "Do NOT mark CR-03 fixed, closed, waived, or scheduled. `WINDOWS.md` entry 23 is OPEN AND ACCEPTED by explicit developer decision; this plan may cross-reference it and must not restate, re-measure or re-litigate it."
    - "Do NOT weaken the D-16 conformance gate. If the re-measurement reds a leg, that is a reportable finding under D-16's own tension clause, not a reason to add an allowlist, an --expected-failures flag or a known-failure baseline. D-21 carries forward verbatim."
    - "Do NOT touch `tests/v2_subscriptions.rs`. Its local `receive_bounded` helper shares a name with a deleted client method and is UNRELATED; renaming or editing it would be a change made by grep rather than by reading."
    - "Do NOT edit any existing `*-SUMMARY.md`, and do NOT edit any `*-PLAN.md` numbered 01 through 21. A superseded summary is named as superseded from the corrected ledger entry, not rewritten."
    - "Do NOT use a nextest `-E 'test(/.../)'` selector anywhere. Every test command uses `binary(...)` and asserts an EXACT count line or an exit code, never a bare run count."
    - "Do NOT leave a mention of a deleted identifier on a line that lacks the literal marker `SUPERSEDED BY`. History may cite a dead symbol; current guidance may not, and the verify checks exactly that distinction — a bare absence grep would forbid the history too."
    - "Do NOT repeat `118.2-VERIFICATION.md`'s claim that the five deleted identifiers have ZERO hits in `tests/`. That claim is FALSE: they survive at five comment sites in `tests/client_sse_stream.rs`, which task 3 corrects. The verification is right about `src/` and wrong about `tests/`; carry the corrected form into every document this plan touches."
  artifacts:
    - path: ".planning/WINDOWS.md"
      provides: "entries 13 and 20 corrected in both representations, plus entries of record for the third round and for plans 22 and 23"
      contains: "CONSUMER-OBSERVABLE"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-09's evidence re-pointed at symbols that exist, limitation (vi) closed out, and the fourth-round amendment"
      contains: "CONF-09"
    - path: "pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md"
      provides: "the consumer-facing description of the client that actually ships"
      contains: "WINDOWS.md entry"
    - path: "tests/client_sse_stream.rs"
      provides: "fence-calibration comments that name constants which exist"
      contains: "PUMP_RECEIVE_SLICE"
  key_links:
    - from: "a reader of the ledger or the requirement row"
      to: "the code that implements what it describes"
      via: "symbols that resolve at HEAD, under headings that instruct locating by symbol"
      pattern: "LOCATE BY SYMBOL"
    - from: "a downstream consumer upgrading across this phase"
      to: "the failure model of the client they will actually run"
      via: "the migration chapter, kept in sync by the disclosure tripwire for MIRRORING and by this plan for TRUTH"
      pattern: "ch12-17"
---

<objective>
Make the documentation of record true again, and give the phase's largest behaviour change a record
at all.

Purpose. `118.2-VERIFICATION.md` judged the state of the record separately and failed it on four
counts, every one of them verifiable by `grep`. Entry 20 asserts that per-id routing removed the
mismatch ceiling; the source's own rustdoc says it came back under new names. Entries 13 and 20 and
the CONF-09 row send a reader to five identifiers that return zero hits in `src/` and `tests/`, under
headings that instruct locating BY SYMBOL. The consumer-facing migration chapter still describes a
client that discards mis-addressed frames in a loop — behaviour the router removed. And the third
gap-closure round that caused all of it — four commits, the largest consumer-observable change in the
phase — has no plan, no summary, no ledger entry, no requirement amendment and no semver verdict.

The disclosure tripwire is green throughout and cannot see any of it, because it verifies that marked
ids are MIRRORED, not that mirrored prose is still TRUE. That limitation is not a defect in the
tripwire; it is the reason this plan's corrections are verified by paired greps instead.

**What the failure mode becomes.** Before: a reader who trusts the ledger believes a live ceiling was
removed, looks for five symbols that do not exist, and learns the wrong failure model from the one
document written for downstream consumers. After: every locate-by-symbol instruction resolves, the
removal claim is replaced by a statement of what actually bounds the client, the round that changed it
is auditable from the planning record alone, and the measurements of record are taken at the tree they
describe. Nothing is softened and nothing accepted is quietly promoted to fixed — CR-03 stays exactly
where the developer put it.

Output: two corrected ledger entries in both representations, three new ledger entries, a corrected
requirement row, a corrected migration chapter, five corrected fence comments, and re-measurements
taken at this HEAD.

**Locked decisions this plan preserves rather than revisits.** D-17 — no new requirement ID, and the
ten-orphan warning is not widened. D-16 and D-21 — the conformance gate is re-run, never relaxed, and
no allowlist or expected-failures baseline is introduced. D-14's two-clean-numbers rule governs how
the re-measurement is reported: the delta attributable to this round is reported separately from any
suite-pin effect. D-15's evidence discipline carries forward: every number quoted comes from a log
this plan produced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-VERIFICATION.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-22-SUMMARY.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-23-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. Line numbers read at HEAD 5caeed05; expect drift,
     locate by the quoted strings and by symbol. -->

**`.planning/WINDOWS.md` is TWO records in one file.** A markdown table whose rows begin at `:18`,
one row per line, and a JSON mirror in a fenced block at the end whose objects begin at `:45`, `:57`,
… `:309` for entries 1..23. Frontmatter carries `open_count`, `waived_count`, `fixed_count`,
`total_count` and `last_updated`. The CLI — `node <gsd-tools> windows` — offers `status`, `append`,
`waive` and `fixed`; there is NO edit subcommand, so a correction to an existing entry's TEXT is a
hand edit applied to both occurrences, while NEW entries go through `windows append` so the counters
stay right. Plan 21 established exactly this rule for exactly this file.

**The two entries to correct, and precisely what is wrong with each.**

- Entry 13 (markdown row at `:30`, JSON object at `:189`). Its subject is the discard-and-loop
  behaviour `118.2-15` shipped. Everything it says about the ORIGINAL defect and about `118.2-20`'s
  bounding is history and stays. What is now false is that it describes current behaviour: the per-id
  router removed the discarding of frames belonging to a LIVE caller, and plan 22 removed the
  charging of frames that are the late answer of an ABANDONED caller. Its RESOLUTION clause also
  locates the fix by three constants and one method that no longer exist.
- Entry 20 (markdown row at `:37`, JSON object at `:273`). Two false things, not one. Its item (1)
  ends by asserting that per-id routing removes the ceiling entirely — the clause the verification
  singles out, and the one the source's own rustdoc contradicts. Its item (2) asserts that another
  operation on the same `Client` waits at most one slice instead of indefinitely — true for the
  receive path and, until plan 23, false for the send path.

**The consumer-facing mirror.** `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md` carries one
bulleted paragraph per marked ledger entry. The entry-13 paragraph is at `:350-358` and the entry-20
paragraph at `:372-386`; the entry-19 paragraph sits between them at `:361-371` and is ACCURATE —
do not touch it. The entry-23 paragraph follows at `:387` onward and is the accepted deferral —
do not touch it either.

**The tripwire's exact contract, so this plan does not over- or under-serve it.**
`tests/windows_disclosure_tripwire.rs` scans `.planning/WINDOWS.md` for the sentinel string
`[CONSUMER-OBSERVABLE]`, derives the set of marked ids, and requires each to be mirrored in the
chapter. Its header says a future entry carrying the sentinel is in scope automatically with no edit
there. So: any NEW entry this plan marks consumer-observable MUST get a chapter paragraph in the same
commit, or the tripwire reds. It cannot tell whether an existing paragraph is still true.

**`REQUIREMENTS.md`'s CONF-09 row is ONE markdown table line** (around `:1126`). It already carries
the D-17 minting note, eight `binary(...)` selectors with parsed counts, two dated `AMENDED …` blocks
and six numbered stated limitations. Two things in it are stale: the second amendment books BLOCKER 2
as closed by three constants and one method that no longer exist, together with a fence name that no
longer exists; and limitation (vi) still names per-id routing as a FUTURE redesign, which shipped in
`d01b87e2`. The house idiom is a bolded dated `AMENDED …` clause that states nothing above it is
rewritten — this plan needs that idiom AND, for those two spots specifically, in-place correction,
because an amendment beneath a false claim leaves the false claim readable as current.

**The third round's four commits, for the entry of record:** `e104dea6`, `d01b87e2` ("route JSON-RPC
responses by id instead of discarding them"), `2d385d60`, `26447f94`. What it DELETED: `Client::
receive_bounded`, `MismatchBudget`, `MISMATCH_DISCARD_TIMEOUT`, `MAX_ID_MISMATCH_DISCARDS`,
`MISMATCH_RECEIVE_SLICE`, and the fence
`a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever`. What it ADDED:
`Pending`, `Client::active_requests`'s answer channel, `Client::pump_once`, `PumpStep`,
`PUMP_RECEIVE_SLICE`, `UnmatchedBudget`, `UNMATCHED_RESPONSE_TIMEOUT`, `MAX_UNMATCHED_RESPONSES`,
`echoed_request_id`, `MAX_ECHOED_REQUEST_ID`, and fences 25 and 26. What it LEFT BEHIND: the
self-sustaining chain plan 22 closed, and the send-path wedge plan 23 closed.

**The five stale fence-calibration comments**, all in `tests/client_sse_stream.rs`, all live comments
above or inside passing fences: `:244`, `:248`, `:2400`, `:3146`, `:3163`. They reason about the
deleted `MISMATCH_DISCARD_TIMEOUT` and `MISMATCH_RECEIVE_SLICE` as if those still set the bounds the
fences are calibrated against. The constants that DO set them at this HEAD are `PUMP_RECEIVE_SLICE`
and `UNMATCHED_RESPONSE_TIMEOUT` / `MAX_UNMATCHED_RESPONSES`.

**A NAME COLLISION that must not be swept up by grep.** `tests/v2_subscriptions.rs:1661` defines a
local helper called `receive_bounded` over a duplex transport. It is UNRELATED to the deleted client
method of the same name and is out of scope; a grep-driven edit there would be a change made without
reading.

**The measurement obligations, from the verification's truths 3 and 4.** The semver verdict of record
(`223 pass, 30 skip`, booked by `118.2-21`) predates all four third-round commits. The official-suite
measurement of record likewise predates them, and the suite exercises pmcp only as a SERVER — CONF-09
limitation (i) — so the client rewrite is outside its scope while the server-side edits in those
commits are not. Both re-measurements belong to this plan, reported per D-14's two-clean-numbers rule.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: The ledger — correct what is false in both representations, and give the third round and this one entries of record</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - `.planning/WINDOWS.md` `:1-17` (frontmatter and the CLI usage note), the markdown rows for entries 12, 13, 19, 20, 21 and 23, and the JSON objects mirroring 13 and 20.
    - `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-22-SUMMARY.md` and `118.2-23-SUMMARY.md` — specifically each summary's closing paragraph stating what the failure mode IS after that plan. Those paragraphs are the source text for the two new entries; do not re-derive them from the plans.
  </read_first>
  <action>
Three corrections and three additions, every one applied to BOTH the markdown table and the JSON
mirror.

Correction one, entry 20. Delete the clause at the end of item (1) that promises per-id routing
removes the ceiling. Replace it with what is true at this HEAD: the ceiling exists under two renamed
private constants; it fires only once a frame addressed to an id this client never minted has
actually been seen; and the self-sustaining chain — where the debris of a call that already died
armed the NEXT call's deadline on frame one — was closed by plan 22, with the mechanism named by
symbol. Then correct item (2): its "waits at most one slice" claim was true for the receive path and
false for the send path until plan 23, and the entry must say so rather than leave a reader with a
claim that was half wrong for two rounds. Keep item (1)'s blast-radius paragraph and its REJECTED
ALTERNATIVE paragraph — both are still accurate and both are the kind of honest disclosure this
ledger exists for.

Every surviving mention of a deleted identifier — in this entry or any other — must sit on a line
that also carries the literal marker `SUPERSEDED BY`, followed by the symbol that replaced it. That
marker is what lets a reader, and the verify check, tell history from current guidance; an entry that
cites a dead symbol without it is indistinguishable from the defect being repaired.

Correction two, entry 13. Keep its history intact. Add a dated RESOLUTION-superseded clause stating
that the behaviour it describes is no longer current: a mis-addressed frame belonging to a LIVE
caller is routed to that caller, not discarded; only a frame nobody awaits is dropped; and of those,
one that is the late answer of an abandoned call is absorbed without charging anyone. Re-point its
locate-by-symbol instruction at symbols that exist. Name the superseded summaries rather than editing
them.

Correction three, the frontmatter counters. After the three appends below, `open_count`,
`total_count` and `last_updated` must be consistent with the table; let the CLI maintain them rather
than hand-editing, and verify afterwards with the status subcommand.

Addition one — the THIRD ROUND, entry of record. Append an entry, marked consumer-observable, naming
its four commits, what it deleted, what it added and what it left behind, using the inventory in this
plan's interfaces section. State plainly that it shipped with no plan and no summary, that this entry
is retrospective, and that its semver verdict is recorded by task 3 of this plan rather than by the
round itself. Because it is marked consumer-observable, it needs a chapter paragraph in task 2.

Addition two — plan 22, entry of record. Marked consumer-observable. What changed for a consumer: a
response frame that is the late answer of a request this client already stopped waiting for no longer
spends the next request's budget, so a peer whose honest round trip exceeds the ceiling no longer
fails every later call on that `Client` after one stray frame. State the bound that remains and the
one that never existed. State the memory the fix adds and why it cannot be grown by a peer.

Addition three — plan 25, entry of record. Marked consumer-observable. Two transport-wide sequences
were serialised only by the client's outer guard and are now serialised in their own right: the 401
token recovery (single-flight from the purge THROUGH the retry request's `get_access_token`, so two
concurrent 401s produce ONE `on_unauthorized` and ONE vend) and the session-stream restart (atomic, so
two concurrent restarts cannot strand a reader `close()` can never reach). State that this landed
BEFORE the guard was removed precisely so no commit in this round carried the hazard.

That entry carries a NAMED RESIDUAL WITH AN OWNER, given the same treatment as `PooledTransport` in
plan 23's entry, because this phase's whole failure history is undisclosed residuals. THE RESIDUAL:
"exactly one vend" is preconditioned on the `AuthProvider` implementation CACHING what
`get_access_token` vends before returning, and the trait imposes no such contract —
`on_unauthorized`'s default is a no-op and `get_access_token` is unconstrained. Against a NON-CACHING
provider the generation check is inert: the loser skips the purge but vends anyway, so the lock
converts two concurrent vends into two SERIALISED vends, which a rotating refresh token still rejects.
State the reachability honestly: the shape is a durable agent holding one transport across concurrent
tasks. State why it is UNFENCEABLE IN-REPO rather than merely unfenced: there is NO in-tree
client-side `AuthProvider` implementation at all — `grep` finds only the unrelated server-side trait
and three test doubles — so every real implementation is downstream and no in-tree implementation
demonstrates the behaviour either way. State why the assumption is nonetheless well-founded: the
trait's own rustdoc only makes sense if `get_access_token` normally serves from cache. OWNER: the
client-transport hardening plan named in this phase's deferred-items.md Disposition section, the same
owner as entry 23 — and the durable fix, recorded for whoever picks it up, is a caching contract
stated on the trait, which is a decision with an external-implementor cost rather than a gap-closure
edit.

Addition four — plan 23, entry of record. Marked consumer-observable. Four things a consumer can
observe, and all four must be there rather than only the flattering one: a stalled peer no longer
serialises the whole client; two POSTs can now be in flight on one transport at once where the client
previously imposed a total order; outbound frame ordering across separate POSTs is no longer imposed
by the client; and `PooledTransport` inherits the accessor's default, so a pooled
`StreamableHttpTransport` KEEPS the wedge — a named, owned residual, not an omission. Name the new
public trait method and its default, and state that transports without a shared-send path are
byte-for-byte unchanged.

Write every new entry to the standard the good entries in this file already set — entry 23 is the
model: the defect or change, the trigger measured precisely, the consequence at the severity actually
found, the reasoning, and a LOCATE BY SYMBOL instruction whose symbols resolve.
  </action>
  <verify>
    <!-- `grep -c` EXITS 1 when the count is 0, so a BARE `grep -c` for a string the
         fix removes passes only while the defect is still there. Every absence check
         below is either `| grep -qx 0` or a filtered-line check that can actually fail. -->
    <automated>grep -c "Per-id response routing removes discards entirely and therefore removes this too" .planning/WINDOWS.md | grep -qx 0</automated>
    <!-- A deleted identifier MAY survive as explicitly-marked history. This asserts
         the real rule: no line mentions one WITHOUT the superseded marker. `grep -v`
         prints and exits 0 when such a line exists, so the leading `!` fails then. -->
    <automated>! grep -n "MISMATCH_DISCARD_TIMEOUT\|MAX_ID_MISMATCH_DISCARDS\|MISMATCH_RECEIVE_SLICE\|a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever\|Client::receive_bounded" .planning/WINDOWS.md | grep -v "SUPERSEDED BY"</automated>
    <automated>grep -c "UNMATCHED_RESPONSE_TIMEOUT" .planning/WINDOWS.md | grep -qvx 0</automated>
    <!-- Parity over ALL entries, not just phase-118 ones: keying the table count on
         `| 118` false-fails the day a non-118 entry is appended. -->
    <automated>test "$(grep -c '"id": [0-9]*' .planning/WINDOWS.md)" = "$(grep -cE '^\| [0-9]+ \|' .planning/WINDOWS.md)"</automated>
  </verify>
  <done>The false removal claim returns zero hits across the whole file, markdown and JSON alike. No line mentions a deleted identifier without the `SUPERSEDED BY` marker. The count of JSON objects equals the count of ALL table rows — counted without keying on a phase number — so the two representations cannot have drifted. Four new entries exist (the third round, plan 22, plan 25, plan 23), each carrying the consumer-observable sentinel, each appended through the CLI so the frontmatter counters are consistent.</done>
</task>

<task type="auto">
  <name>Task 2: The two documents a reader outside this repo actually reads — the requirement row and the migration chapter</name>
  <files>.planning/REQUIREMENTS.md, pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the CONF-09 row around `:1126`, in full, including both existing `AMENDED …` blocks and all six numbered limitations; and the CONF-10 row for the backstop wording.
    - `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md` `:340-400` — the entry-13, entry-19, entry-20 and entry-23 paragraphs, so the two that are accurate are recognised and left alone.
    - `tests/windows_disclosure_tripwire.rs` `:1-60` — the sentinel and the derived-never-enumerated rule, so the new paragraphs satisfy it by construction.
  </read_first>
  <action>
Correct CONF-09's evidence, then bring the consumer-facing chapter into agreement with the code.

The requirement row. Two in-place corrections and one dated amendment, on the single table line.
In place: the second amendment's booking of BLOCKER 2 must stop naming three constants, one method
and one fence that do not exist — re-point it at the constructs that do, and state that the mechanism
was subsequently replaced by the per-id router rather than merely renamed. Also in place: limitation
(vi) must stop describing per-id routing as future work; it shipped, and the limitation it stated is
either closed or replaced by a narrower one, whichever the code says. Then append a dated amendment,
in the file's own idiom that states nothing above it is rewritten, carrying: the third round's
identity and what it changed; plans 22 and 23 with the defect each closed and the fence that proves
it, by NAME and with parsed counts from the logs those plans produced; the semver verdict this plan
records; the re-measured official-suite figures; and the residuals plan 23 accepts. Do not flip the
checkbox and do not re-open the booking.

The chapter. Rewrite the entry-13 paragraph so it describes the client that ships: a response frame
is delivered to whichever request registered its id, so a mis-addressed frame belonging to a live
caller is no longer discarded and no longer costs that caller its answer; a frame nobody awaits is
dropped, and if it is the late answer of a call that already stopped waiting it is absorbed silently
rather than counted against the next call. Rewrite the entry-20 paragraph the same way: keep the
honest blast-radius warning about a slow-and-lenient peer, because the ceiling still exists and still
has that property, and delete the sentence promising that another operation waits at most one slice —
which was only ever true of the receive path — replacing it with the send-path statement plan 23 made
true. Leave the entry-19 and entry-23 paragraphs untouched; they are accurate.

Then add one paragraph per new ledger entry from task 1, in the same bulleted form, so the tripwire's
derived set is satisfied in this same commit. Each paragraph is written for someone upgrading pmcp who
has never read this repo's planning directory: what they will observe, when, and what to do about it.
For plan 23's paragraph that means saying out loud that two POSTs can now be in flight on one
transport and that the client no longer orders outbound frames across separate POSTs — the two
residuals, not just the fix.

Finally, state once in the chapter — where its own preamble explains why it exists — that the
tripwire guarantees these paragraphs EXIST for every marked entry and cannot guarantee they are still
TRUE, and that a behaviour change is therefore not complete until its paragraph is re-read. That
sentence is the only defence against the exact rot this plan is repairing.
  </action>
  <verify>
    <automated>grep -c "keeps looping on a mismatch, discarding the mismatched frame" pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md | grep -qx 0</automated>
    <automated>grep -c "waits at most one slice instead of indefinitely" pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md | grep -qx 0</automated>
    <automated>! grep -n "MISMATCH_DISCARD_TIMEOUT\|MAX_ID_MISMATCH_DISCARDS\|MISMATCH_RECEIVE_SLICE\|Client::receive_bounded\|a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever" .planning/REQUIREMENTS.md | grep -v "SUPERSEDED BY"</automated>
    <automated>cargo nextest run --features "full" -E 'binary(windows_disclosure_tripwire)' > target/118.2-24-tripwire.log 2>&1; rc=$?; grep -qE "1 test run: 1 passed( \\([0-9]+ leaky\\))?, 0 skipped" target/118.2-24-tripwire.log && test $rc -eq 0</automated>
  </verify>
  <done>The chapter no longer tells a consumer the client discards mis-addressed frames in a loop, nor that another operation waits at most one slice. CONF-09's row cites only identifiers that resolve at HEAD, or frames them explicitly as superseded history. Every marked ledger entry, including the three new ones, has a chapter paragraph, and `binary(windows_disclosure_tripwire)` is green with a parsed run count of one.</done>
</task>

<task type="auto">
  <name>Task 3: Measurements taken at the tree they describe — fence comments, semver verdict, suite delta, and the gate</name>
  <files>tests/client_sse_stream.rs</files>
  <read_first>
    - `tests/client_sse_stream.rs` `:229-260` (the two constants whose doc comments cite deleted symbols), `:2390-2410`, `:3140-3170` (the three remaining stale comment sites). NOTE: `118.2-VERIFICATION.md` says these five identifiers have zero hits in `tests/`; that is FALSE and these five sites are the counter-example. Correct the claim wherever this plan restates it.
    - `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-21-SUMMARY.md` — for the conformance runner's invocation, its two fail-closed guards, and the closing figures this round's delta is reported against.
  </read_first>
  <action>
Repair the calibration comments, then produce the round's numbers from the tree they describe.

The comments. At each of the five sites, the comment reasons about a bound set by a constant that no
longer exists. Replace each with the constant that actually sets the bound the fence is calibrated
against at this HEAD, keeping every comment's ARGUMENT intact — the point each one makes about why
its bound sits above or below the mechanism under test is still the right point; only the symbol is
wrong. Where a comment's argument is itself now wrong because the mechanism changed, say what the
bound relates to now rather than translating the old sentence word for word. Change no constant
VALUE and no assertion: this task edits prose inside a test file and the fence set must be green
before and after with the same counts.

The verdicts. Run the semver check against the phase's standing baseline for the `pmcp` package at
this HEAD and record it verbatim, with the caveat this phase has recorded three times: the tool is a
breaking-change linter rather than an API-diff inventory, so for plan 23's additive trait method
"nothing fired" is the whole of the claim, and the addition itself is evidenced by the trait
definition rather than by a tool line.

The suite. Re-run the conformance gate at this HEAD on both era legs and report, per D-14's
two-clean-numbers rule: the delta attributable to this round separately from any suite-pin effect,
against Phase 118.1's closing figures. State, as CONF-09 limitation (i) already does, that the suite
exercises pmcp only as a SERVER, so it cannot attribute either of this round's client fixes — and
that it CAN see the server-side edits the third round made, which is why it is being re-run at all
rather than skipped as out of scope. If a leg reds, report it as a finding; do not touch the gate.

The gate. Run the repo's own quality gate and classify any step that does not exit zero by provenance
with a file-list citation, rather than sweeping it in.

Every number that reaches the summary is quoted from a log this task produced. Nothing is carried
over from an earlier plan's log — that carry-over is how the record reached the state this plan is
repairing.
  </action>
  <verify>
    <automated>grep -c "MISMATCH_DISCARD_TIMEOUT\|MISMATCH_RECEIVE_SLICE\|MAX_ID_MISMATCH_DISCARDS" tests/client_sse_stream.rs | grep -qx 0</automated>
    <automated>cargo nextest run --features "full" -E 'binary(client_sse_stream)' > target/118.2-24-fences.log 2>&1; rc=$?; grep -qE "30 tests run: 30 passed( \\([0-9]+ leaky\\))?, 0 skipped" target/118.2-24-fences.log && test $rc -eq 0</automated>
    <automated>cargo nextest run --features "full" -E 'binary(log_emitter) + binary(pmcp_both_ends_logging) + binary(windows_disclosure_tripwire)' > target/118.2-24-backstop.log 2>&1; rc=$?; N=$(grep -oE '[0-9]+ tests run' target/118.2-24-backstop.log | tail -1 | grep -oE '^[0-9]+'); test $rc -eq 0 && test "${N:-0}" -ge 28</automated>
    <automated>cargo semver-checks --baseline-rev cb5d1365 -p pmcp > target/118.2-24-semver.log 2>&1; rc=$?; tail -20 target/118.2-24-semver.log; test $rc -eq 0</automated>
    <automated>make quality-gate > target/118.2-24-gate.log 2>&1; rc=$?; tail -30 target/118.2-24-gate.log; test $rc -eq 0</automated>
  </verify>
  <done>No live comment in `tests/client_sse_stream.rs` names a deleted constant. The named binaries are green with parsed non-zero counts and the same fence count plans 22 and 23 left. The semver verdict and both era legs are recorded verbatim at this HEAD, with the round's delta reported separately from any pin effect. `make quality-gate` is green, or every non-zero step is classified by provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the planning record → a future maintainer | A ledger entry is read as fact; a false entry is a defect with a person-shaped blast radius |
| the migration chapter → a downstream consumer | The only place outside this repo where an unsemvered behaviour change is disclosed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118.2-24-01 | Repudiation | `.planning/WINDOWS.md` entries 13 and 20 | high | mitigate | The record asserts a removal that did not happen and locates code by five deleted symbols; a reader trusting it believes a live defect is gone. Closed by in-place correction in both representations, verified by paired negative and positive greps |
| T-118.2-24-02 | Repudiation | the unrecorded third gap-closure round | high | mitigate | The phase's largest consumer-observable change is unauditable from the planning record. Closed by a retrospective entry naming its commits, its deletions, its additions and its residuals, plus a semver verdict taken at this HEAD |
| T-118.2-24-03 | Information Disclosure | `pmcp-book` migration chapter | medium | mitigate | Downstream consumers are told the wrong failure model for a client they run in production. Closed by rewriting the two stale paragraphs and adding one per new marked entry, with the tripwire enforcing existence in the same commit |
| T-118.2-24-04 | Repudiation | measurements of record predating HEAD | medium | mitigate | The semver verdict and the suite figures both predate four commits that touched client and server code. Closed by re-measuring at this HEAD and reporting the delta per D-14 |
| T-118.2-24-05 | Tampering | a grep-driven edit reaching an unrelated symbol | low | mitigate | `tests/v2_subscriptions.rs` has a local helper sharing a name with a deleted client method; a prohibition names it explicitly and no verify command reads that file |
| T-118.2-24-06 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; no dependency is added. The legitimacy gate is not reached |
</threat_model>

<verification>
- Negative greps prove the false removal claim, the stale chapter sentences, and the deleted-symbol citations are GONE from the files that carried them; positive greps prove the replacements are present.
- The count of JSON objects in `.planning/WINDOWS.md` equals the count of table rows, so the two representations agree.
- `binary(windows_disclosure_tripwire)` is green with a parsed run count of one, and the summary records explicitly that this proves mirroring rather than truth.
- `binary(client_sse_stream)`, `binary(log_emitter)` and `binary(pmcp_both_ends_logging)` green with parsed non-zero counts, unchanged from what plans 22 and 23 left.
- The semver verdict and both official-suite legs are recorded verbatim from logs this plan produced, with the round's delta reported separately from any suite-pin effect.
- `make quality-gate` green, or every non-zero step classified by provenance with a file-list citation.
</verification>

<success_criteria>
Every locate-by-symbol instruction in the ledger and in the CONF-09 row resolves at HEAD. No document
of record asserts a removal that did not happen. The third gap-closure round, plan 22 and plan 23 each
have an entry naming what a consumer will observe, including the two residuals plan 23 accepts. The
migration chapter describes the client that ships, and says once that the tripwire cannot vouch for
its truth. CR-03 remains exactly as the developer left it: open, accepted, disclosed, unscheduled.
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-24-SUMMARY.md` when done.

The summary MUST carry, verbatim from logs this plan produced: each paired grep result proving a
correction landed; the tripwire result with its stated limitation; the parsed counts for the named
binaries; the semver verdict with its caveat; both era legs' figures with the delta reported per
D-14's two-clean-numbers rule; and a short closing statement of what remains OPEN in this phase —
`WINDOWS.md` entries 5, 6, 7, 9, 19, 21 and 23 — so the next reader inherits a list rather than a
search.
</output>
