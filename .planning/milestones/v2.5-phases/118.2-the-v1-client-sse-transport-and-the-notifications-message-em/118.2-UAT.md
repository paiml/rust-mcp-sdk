---
status: complete
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
source: 118.2-01..25-SUMMARY.md (24 plans; numbering gap at 02 is deliberate)
started: 2026-08-18T22:20:00Z
updated: 2026-08-20T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. pmcp's own client holds a live v1 GET SSE stream
expected: Client opens and holds a GET SSE stream over v1; server-initiated messages arrive and complete. `era_matrix`'s deprecated-capabilities assertion reads `completed`, not `no-live-stream`.
result: pass
evidence: "client_sse_stream 26/26, http_peer_roundtrip 12/12, in_tool_peer_roundtrip 7/7 at HEAD 26447f94"

### 2. A tool handler can emit MCP log notifications
expected: `extra.log(..)` / `extra.log_with_data(..)` inside a tool handler produce `notifications/message` frames that reach the client on both eras; no sink means `Ok(())`, not an error.
result: pass
evidence: "conformance tools-call-with-logging 2/0; log_emitter 25/0 + 2 property arms; example s55_handler_logging exit 0"

### 3. Per-era logging control behaves correctly
expected: `logging/setLevel` returns literal `{}` on v1 and `-32601` on v2. On v2, a request that did not authorize logging via `_meta["io.modelcontextprotocol/logLevel"]` receives NO notifications at all (SEP-2575).
result: pass
evidence: "7 log_emitter wire fences + 17 unit fences green. NOTE: the SEP-2575 v2 gate was added post-phase in 26447f94; three phase-authored fences had encoded the defect as expected and were rewritten."

### 4. The official conformance suite passes on both revisions
expected: `make test-conformance` exits 0 with both scored sets entirely green and GAP_ATTRIBUTABLE_FAILURES = 0.
result: pass
evidence: "two independent runs at HEAD, CONF-01 gates PASSED both times, both legs exit 0, GAP_ATTRIBUTABLE_FAILURES 1 -> 0"

### 5. The change is additive to public API, with a semver verdict
expected: `cargo semver-checks` reports no breaking changes at HEAD, so this ships as a 2.x minor.
result: pass
evidence: "cargo semver-checks --baseline-rev cb5d1365 -p pmcp at HEAD: 223 checks, 223 pass, no semver update required - identical to plan 21. NOTE: against the crates.io baseline the milestone carries 118.1 accepted D-15 major+minor."

### 6. The client is safe to rely on end-to-end
expected: A peer cannot drive the client into unbounded resource consumption; a terminal transport error cannot be paired with or displace an unrelated request's response; a single bad frame cannot wedge the client — including under concurrency.
result: issue
reported: "Since we are moving away from SSE, and more toward using stateless calls with MCP Tasks using task storage for progress, I think that we can show it as a gap and defer its fix (if any) to the future."
severity: minor
evidence: "CR-01 and CR-02 closed structurally at HEAD (receive_bounded, MismatchBudget, MISMATCH_DISCARD_TIMEOUT have zero references; responses route by id). CR-03 remains open in drain_or_latch and is DEFERRED - WINDOWS.md entry 23."

<!-- Tests 7-33 added 2026-08-20. Plans 22-25 landed AFTER the 2026-08-19
     session and every deliverable in them classifies `all_auto_covered: true`
     (27 of 27), so per the coverage-aware path they are recorded as automated
     passes rather than re-presented as human checkpoints. User confirmed the
     set on 2026-08-20.

     TWO CAVEATS, recorded rather than smoothed over:
     - Plan 25's "the guarded region reaches the retry's TOKEN FETCH" is true
       of the RETRY vend only. The ORDINARY vend on every request build was
       UNGUARDED -- that was T-118.2-25-01, found by the security audit and
       closed by `ColdVendGate` + fence 31. The covering tests pass; the
       deliverable's wording is narrower than it reads.
     - Plan 25's "two concurrent start_sse calls leave exactly ONE live
       reader" holds restart-vs-restart. Restart-vs-`close()` is a different
       pairing, closed by the shutdown watch rather than by the abort.
     NEITHER `ColdVendGate` nor fence 31 has a coverage entry of its own: they
     came from the security audit, not from a plan. See 118.2-SECURITY.md. -->

### 7. Plan 22 — A response frame that is the late answer of a request this client already stopped waiting for is absorbed as ordinary progress and spends none of the next request's UnmatchedBudget
expected: A response frame that is the late answer of a request this client already stopped waiting for is absorbed as ordinary progress and spends none of the next request's UnmatchedBudget
result: pass
source: automated
covered_by: integration: cargo test --features full --test client_sse_stream debris_from_a_dead_call_does_not_charge_the_next_calls_budget
covered_by: unit: src/client/mod.rs#client::tests::an_abandoned_id_is_absorbed

### 8. Plan 22 — A response frame addressed to an id this client never minted still spends budget and still fails the awaiting call with the named, counted error, at the same two bounds
expected: A response frame addressed to an id this client never minted still spends budget and still fails the awaiting call with the named, counted error, at the same two bounds
result: pass
source: automated
covered_by: unit: src/client/mod.rs#client::tests::an_id_this_client_never_minted_is_not_absorbed
covered_by: integration: tests/client_sse_stream.rs#debris_from_a_dead_call_does_not_charge_the_next_calls_budget (call one must FAIL naming 32)

### 9. Plan 22 — The added memory is bounded by construction, holds only locally-minted ids, and each entry is consumed on first use
expected: The added memory is bounded by construction, holds only locally-minted ids, and each entry is consumed on first use
result: pass
source: automated
covered_by: unit: src/client/mod.rs#client::tests::the_abandoned_ledger_evicts_the_oldest_at_its_cap
covered_by: unit: src/client/mod.rs#client::tests::an_abandoned_id_is_absorbed_only_once

### 10. Plan 22 — A live registration is never shadowed by debris from a previous life of the same id
expected: A live registration is never shadowed by debris from a previous life of the same id
result: pass
source: automated
covered_by: unit: src/client/mod.rs#client::tests::registering_a_request_clears_stale_debris_for_its_id

### 11. Plan 22 — The UNMATCHED_RESPONSE_TIMEOUT and Pending rustdocs describe the code at this HEAD, with the superseded clause removed rather than annotated
expected: The UNMATCHED_RESPONSE_TIMEOUT and Pending rustdocs describe the code at this HEAD, with the superseded clause removed rather than annotated
result: pass
source: automated
covered_by: other: /usr/bin/grep -c 'wearing the router' src/client/mod.rs | /usr/bin/grep -qx 0

### 12. Plan 22 — Nothing else moved: all 26 pre-existing client_sse_stream fences and both CONF-10 backstop binaries stay green
expected: Nothing else moved: all 26 pre-existing client_sse_stream fences and both CONF-10 backstop binaries stay green
result: pass
source: automated
covered_by: integration: cargo nextest run --features full -E 'binary(client_sse_stream) + binary(log_emitter) + binary(pmcp_both_ends_logging)' -> 54 tests run: 54 passed

### 13. Plan 23 — A peer that accepts a POST and never writes its response HEAD blocks only ITS OWN call; a notification send AND a full request/response round trip both complete on the same client while that POST is still parked
expected: A peer that accepts a POST and never writes its response HEAD blocks only ITS OWN call; a notification send AND a full request/response round trip both complete on the same client while that POST is still parked
result: pass
source: automated
covered_by: integration: cargo test --features full --test client_sse_stream a_peer_that_never_writes_response_headers_does_not_serialise_the_client -> target/118.2-23-green.log
covered_by: other: RED at commit 42c089f5 -> target/118.2-23-red.log (probe never completed; the cancel notification never reached the wire at all)

### 14. Plan 23 — The public API change is ADDITIVE: a new trait plus one defaulted trait method, with a recorded semver verdict at this HEAD
expected: The public API change is ADDITIVE: a new trait plus one defaulted trait method, with a recorded semver verdict at this HEAD
result: pass
source: automated
covered_by: other: cargo semver-checks --baseline-rev cb5d1365 -p pmcp -> 223 checks: 223 pass, 30 skip; 'Summary no semver update required'; exit 0 -> target/118.2-23-semver.log
covered_by: unit: src/shared/transport.rs#shared::transport::tests::a_transport_that_does_not_opt_in_answers_none

### 15. Plan 23 — A transport WITHOUT a shared-send path still sends through the exclusive `&mut` path — the fallback is exercised, not assumed
expected: A transport WITHOUT a shared-send path still sends through the exclusive `&mut` path — the fallback is exercised, not assumed
result: pass
source: automated
covered_by: unit: src/shared/streamable_http.rs#shared::streamable_http::tests::stdio_offers_no_shared_send_path
covered_by: unit: every `Client` unit test in src/client/mod.rs runs over a MockTransport that answers None, so `send_frame`'s fallback branch is the branch those tests take (cargo test --lib, green in the gate)

### 16. Plan 23 — The handle is not a second wire path: a frame sent through it produces a byte-identical request to one sent through `&mut send`
expected: The handle is not a second wire path: a frame sent through it produces a byte-identical request to one sent through `&mut send`
result: pass
source: automated
covered_by: unit: src/shared/streamable_http.rs#shared::streamable_http::tests::the_shared_handle_writes_the_same_request_as_the_exclusive_path (method, path, full sorted header block and body compared against a recording listener)

### 17. Plan 23 — The two trait definitions did not drift: native, `v1-compat`-severed and wasm32 all compile
expected: The two trait definitions did not drift: native, `v1-compat`-severed and wasm32 all compile
result: pass
source: automated
covered_by: other: cargo build --features full (rc 0); cargo build --no-default-features --features v1-compat (rc 0); cargo check --target wasm32-unknown-unknown --no-default-features (rc 0)

### 18. Plan 23 — Nothing else moved: all 29 pre-existing client_sse_stream fences plus the five backstop binaries stay green, and the repo gate is green with no new #[allow]
expected: Nothing else moved: all 29 pre-existing client_sse_stream fences plus the five backstop binaries stay green, and the repo gate is green with no new #[allow]
result: pass
source: automated
covered_by: integration: cargo nextest run --features full -E 'binary(client_sse_stream)' -> 30 tests run: 30 passed, 0 skipped
covered_by: integration: binary(http_peer_roundtrip) + binary(v1_severability_tripwire) + binary(v2_bounded_reads_tripwire) + binary(log_emitter) + binary(pmcp_both_ends_logging) -> 70 tests run: 70 passed, 2 skipped
covered_by: other: make quality-gate -> exit 0 AND the literal banner ALL TOYOTA WAY QUALITY CHECKS PASSED (count 1)

### 19. Plan 23 — Fence 26's (and fence 23's) superseded assertion sentence is GONE
expected: Fence 26's (and fence 23's) superseded assertion sentence is GONE
result: pass
source: automated
covered_by: other: grep -c 'held across an unbounded `receive().await`' tests/client_sse_stream.rs -> 0

### 20. Plan 24 — WINDOWS.md no longer asserts that per-id routing removed the mismatch ceiling, in EITHER representation, and states what actually bounds the client named by symbols that resolve
expected: WINDOWS.md no longer asserts that per-id routing removed the mismatch ceiling, in EITHER representation, and states what actually bounds the client named by symbols that resolve
result: pass
source: automated
covered_by: other: /usr/bin/grep -c 'Per-id response routing removes discards entirely and therefore removes this too' .planning/WINDOWS.md -> 0 (was 2: table + mirror)
covered_by: other: /usr/bin/grep -c UNMATCHED_RESPONSE_TIMEOUT .planning/WINDOWS.md -> 10 (was 0)

### 21. Plan 24 — Every locate-by-symbol instruction resolves at HEAD; the five deleted identifiers appear nowhere in WINDOWS.md or REQUIREMENTS.md except on a line explicitly marked SUPERSEDED BY
expected: Every locate-by-symbol instruction resolves at HEAD; the five deleted identifiers appear nowhere in WINDOWS.md or REQUIREMENTS.md except on a line explicitly marked SUPERSEDED BY
result: pass
source: automated
covered_by: other: ! /usr/bin/grep -n '<five ids>' .planning/WINDOWS.md | /usr/bin/grep -v 'SUPERSEDED BY' -> no output, exit 1 on the inner grep -> PASS
covered_by: other: same filtered check over .planning/REQUIREMENTS.md -> PASS

### 22. Plan 24 — The THIRD gap-closure round has a ledger entry of record naming its four commits, what it deleted, what it added, what it left behind, and a semver verdict
expected: The THIRD gap-closure round has a ledger entry of record naming its four commits, what it deleted, what it added, what it left behind, and a semver verdict
result: pass
source: automated
covered_by: other: .planning/WINDOWS.md entry 24, [CONSUMER-OBSERVABLE], appended via `gsd-tools windows append`; semver verdict at target/118.2-24-semver.log

### 23. Plan 24 — Plans 22, 25 and 23 each have a ledger entry disclosing what changed for a consumer, and all THREE residuals are booked with owners
expected: Plans 22, 25 and 23 each have a ledger entry disclosing what changed for a consumer, and all THREE residuals are booked with owners
result: pass
source: automated
covered_by: other: entries 25, 26 (non-caching AuthProvider residual + owner), 27 (PooledTransport residual + owner; open_event_stream residual + owner)

### 24. Plan 24 — The migration chapter describes the client that ships, and says once that the tripwire cannot vouch for its truth
expected: The migration chapter describes the client that ships, and says once that the tripwire cannot vouch for its truth
result: pass
source: automated
covered_by: other: /usr/bin/grep -c 'keeps looping on a mismatch, discarding the mismatched frame' -> 0; /usr/bin/grep -c 'waits at most one slice instead of indefinitely' -> 0; the EXIST-not-TRUE sentence present (count 1)
covered_by: integration: cargo nextest run --features full -E 'binary(windows_disclosure_tripwire)' -> 1 test run: 1 passed, 0 skipped, exit 0 -> target/118.2-24-tripwire.log

### 25. Plan 24 — Both era legs and the CONF-10 emitter backstop are re-measured at THIS HEAD with the delta reported per D-14
expected: Both era legs and the CONF-10 emitter backstop are re-measured at THIS HEAD with the delta reported per D-14
result: pass
source: automated
covered_by: other: ./scripts/run-conformance-suite.sh -> exit 0 run 1; 2025-11-25 73/1, 2026-07-28 142/36, both legs exit 0 -> target/118.2-24-conf.log
covered_by: integration: binary(log_emitter) + binary(pmcp_both_ends_logging) + binary(windows_disclosure_tripwire) -> 28 tests run: 28 passed, 2 skipped -> target/118.2-24-backstop.log

### 26. Plan 24 — The five live fence-calibration comments name constants that actually set the bounds they are calibrated against, and the fence set is unchanged
expected: The five live fence-calibration comments name constants that actually set the bounds they are calibrated against, and the fence set is unchanged
result: pass
source: automated
covered_by: other: /usr/bin/grep -c 'MISMATCH_DISCARD_TIMEOUT|MISMATCH_RECEIVE_SLICE|MAX_ID_MISMATCH_DISCARDS' tests/client_sse_stream.rs -> 0 (was 5)
covered_by: integration: cargo nextest run --features full -E 'binary(client_sse_stream)' -> 30 tests run: 30 passed, 0 skipped, exit 0, before AND after -> target/118.2-24-fences-pre.log, target/118.2-24-fences.log

### 27. Plan 25 — Two concurrent POSTs on one transport that both receive a 401 produce exactly ONE on_unauthorized call and exactly ONE token VEND; the loser is served from the cache the winner warmed and both sends succeed
expected: Two concurrent POSTs on one transport that both receive a 401 produce exactly ONE on_unauthorized call and exactly ONE token VEND; the loser is served from the cache the winner warmed and both sends succeed
result: pass
source: automated
covered_by: integration: cargo test --features full --test client_sse_stream two_concurrent_401s_refresh_the_token_once
covered_by: unit: src/shared/streamable_http.rs#shared::streamable_http::tests::two_concurrent_401s_take_one_purge_and_one_vend

### 28. Plan 25 — The guarded region reaches the retry's TOKEN FETCH, not merely the purge — a purge-only single-flight still fails the fence
expected: The guarded region reaches the retry's TOKEN FETCH, not merely the purge — a purge-only single-flight still fails the fence
result: pass
source: automated
covered_by: other: counterfactual probe: `drop(_refresh)` inserted after the generation bump -> target/118.2-25-counterfactual-purge-only.log, still FAILED on vends_peak == 2

### 29. Plan 25 — A caller whose token generation is still CURRENT still refreshes; only a superseded caller skips
expected: A caller whose token generation is still CURRENT still refreshes; only a superseded caller skips
result: pass
source: automated
covered_by: unit: src/shared/streamable_http.rs#shared::streamable_http::tests::a_401_on_a_current_generation_still_refreshes

### 30. Plan 25 — The retry POST is NOT sent under the refresh lock, so the transport does not grow the whole-transport bottleneck plan 23 is removing
expected: The retry POST is NOT sent under the refresh lock, so the transport does not grow the whole-transport bottleneck plan 23 is removing
result: pass
source: automated
covered_by: unit: src/shared/streamable_http.rs#shared::streamable_http::tests::the_retry_post_is_sent_outside_the_refresh_lock (the listener probes try_lock from the SERVER side while serving the retry)

### 31. Plan 25 — A solo caller is unchanged — one 401, one purge, one retry, a second 401 returned unchanged with no further purge — and a 401 with no provider takes no lock at all
expected: A solo caller is unchanged — one 401, one purge, one retry, a second 401 returned unchanged with no further purge — and a 401 with no provider takes no lock at all
result: pass
source: automated
covered_by: unit: src/shared/streamable_http.rs#shared::streamable_http::tests::a_solo_401_recovery_purges_once_and_bumps_the_generation_once
covered_by: unit: src/shared/streamable_http.rs#shared::streamable_http::tests::a_401_with_no_provider_is_returned_unchanged_and_moves_no_generation

### 32. Plan 25 — Two concurrent start_sse calls leave exactly ONE live session-stream reader, and close() reaches it
expected: Two concurrent start_sse calls leave exactly ONE live session-stream reader, and close() reaches it
result: pass
source: automated
covered_by: integration: cargo test --features full --test client_sse_stream two_concurrent_session_stream_restarts_leave_one_reader

### 33. Plan 25 — Nothing else moved: all 27 pre-existing client_sse_stream fences and the three backstop binaries stay green, and the repo gate is green with no new #[allow]
expected: Nothing else moved: all 27 pre-existing client_sse_stream fences and the three backstop binaries stay green, and the repo gate is green with no new #[allow]
result: pass
source: automated
covered_by: integration: cargo nextest run --features full -E 'binary(client_sse_stream)' -> 29 tests run: 29 passed, 0 skipped
covered_by: integration: cargo nextest run --features full -E 'binary(log_emitter) + binary(pmcp_both_ends_logging) + binary(http_peer_roundtrip)' -> 39 tests run: 39 passed
covered_by: other: make quality-gate -> exit 0 AND the literal banner ALL TOYOTA WAY QUALITY CHECKS PASSED
## Summary

total: 33
passed: 32
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The v1 client SSE transport is safe to rely on end-to-end - a peer cannot drive it into unbounded resource consumption, a terminal transport error cannot be paired with or displace an unrelated request's response, and a single bad frame cannot wedge the client"
  status: deferred
  reason: "Developer decision 2026-08-18: CR-01 and CR-02 are closed structurally at HEAD; CR-03 survives but lives exclusively in the POST-response SSE reader path, i.e. on the v1 surface being deprecated in favour of stateless calls plus MCP Tasks with task-store polling for progress. Recorded as a gap, no fix scheduled."
  severity: minor
  test: 6
  root_cause: "drain_or_latch gates the terminal latch on open_post_readers, a TRANSPORT-WIDE count, answering a per-caller question; PostReaderGuard::drop wakes only on the 1->0 transition. Reader ERRORS travel on the shared ReaderDelivery latch and are not per-caller routed, even though successful RESPONSES are routed by id since d01b87e2."
  artifacts:
    - path: "src/shared/streamable_http.rs"
      issue: "CR-03: the open_post_readers gate in drain_or_latch. Trigger is concurrency >= 2 SSE-answered POST responses AND a terminal failure on one of them - NOT load-dependent. Consequence at HEAD is a DELAYED error bounded by the other reader's lifetime, not the process-lifetime hang 118.2-VERIFICATION.md described against 31a80a75."
    - path: "src/shared/streamable_http.rs"
      issue: "The unit test a_post_reader_in_flight_gates_a_reason_from_either_stream asserts the defective shape is correct and pins the single-reader case only. No two-concurrent-reader fence exists anywhere in the crate."
  missing:
    - "DEFERRED, not scheduled. The durable fix: scope the gate to the stream that owns the question - let a StreamKind::PostResponse reason surface immediately regardless of open_post_readers, reserving the transport-wide gate for StreamKind::Session reasons. Fully durable form is a reason per reader delivered to its owning caller."
    - "When fixed: amend the misleading unit test and add a two-concurrent-reader fence."
  owner: "the client-transport hardening plan named in deferred-items.md Disposition - same owner as closure-review WR-01/WR-02/WR-03 (WINDOWS.md entry 21), which touch the same loop"
  window: 23
