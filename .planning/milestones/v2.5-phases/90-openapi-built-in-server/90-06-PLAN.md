---
phase: 90-openapi-built-in-server
plan: 06
type: execute
wave: 5
depends_on: [90-03, 90-04, 90-05]
files_modified:
  - crates/pmcp-openapi-server/Cargo.toml
  - crates/pmcp-openapi-server/src/main.rs
  - crates/pmcp-openapi-server/src/lib.rs
  - crates/pmcp-openapi-server/src/cli.rs
  - crates/pmcp-openapi-server/src/dispatch.rs
  - crates/pmcp-openapi-server/src/assemble.rs
  - Cargo.toml
autonomous: true
requirements: [OAPI-06]
must_haves:
  truths:
    - "The pmcp-openapi-server binary serves a config-driven OpenAPI MCP server over streamable HTTP"
    - "--spec is optional at runtime; a curated-only config boots without a spec (D-03)"
    - "dispatch builds the HttpConnector + HttpCodeExecutor pair lazily (no backend contact at startup, CF-2)"
    - "build_server wires single-call tools, script tools, and Code Mode via the toolkit"
    - "The inbound TokenCaptureAuthProvider captures the MCP client token into AuthContext, threaded into outgoing oauth_passthrough auth (H1)"
    - "[code_mode] enabled=true with NO --spec has a defined behavior (runs without api_schema, with a warn)"
  artifacts:
    - path: "crates/pmcp-openapi-server/src/lib.rs"
      provides: "run/run_serving/load_config_and_spec/serve pipeline"
      contains: "load_config_and_spec"
    - path: "crates/pmcp-openapi-server/src/dispatch.rs"
      provides: "dispatch → (Arc<dyn HttpConnector>, HttpCodeExecutor)"
      contains: "HttpCodeExecutor"
    - path: "crates/pmcp-openapi-server/src/assemble.rs"
      provides: "build_server (toolkit assemble pattern, NOT reference pmcp_server.rs) + inbound token capture"
      contains: "build_server"
  key_links:
    - from: "crates/pmcp-openapi-server/src/lib.rs"
      to: "pmcp::StreamableHttpServer"
      via: "serve over streamable HTTP (CF-1)"
      pattern: "StreamableHttpServer"
    - from: "crates/pmcp-openapi-server/src/assemble.rs"
      to: "pmcp-server-toolkit"
      via: "ServerBuilderExt + synthesize_from_config_with_http_connector + code_mode_tools_from_executor"
      pattern: "ServerBuilderExt"
---

<objective>
Build the `pmcp-openapi-server` Shape A binary (OAPI-06) — a structural copy of
`pmcp-sql-server`: `run → load_config_and_spec → dispatch → build_server → serve`
over streamable HTTP (CF-1), with `--spec` OPTIONAL at runtime (D-03) and lazy
startup (CF-2). Do NOT lift the reference `pmcp_server.rs` (Pitfall 6) — assemble
via the toolkit's `assemble.rs` pattern + `ServerBuilderExt`.

Purpose: The pure-config binary — a non-developer points it at config.toml (+ optional
spec) and gets a live MCP server with zero Rust written.
Output: `crates/pmcp-openapi-server` (5-file crate) added to the workspace.

NOTE (H1 — passthrough capture side): this binary owns the INBOUND side of the
`oauth_passthrough` bridge. It installs an inbound `TokenCaptureAuthProvider` (ref
`pmcp_server.rs:37-64`) that stores the MCP client token in `AuthContext`; the
synthesized tool handlers / ScriptToolHandler / Code-Mode path receive
`RequestHandlerExtra` at `ToolHandler::handle(args, extra)` and thread the captured
token into the outgoing `HttpCodeExecutor.with_inbound_token(..)` so the outbound
passthrough provider (Plan 01/04) forwards it. See Task 3.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
@.planning/phases/90-openapi-built-in-server/90-03-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-04-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the crate — Cargo.toml + cli.rs + main.rs (workspace member)</name>
  <files>crates/pmcp-openapi-server/Cargo.toml, crates/pmcp-openapi-server/src/cli.rs, crates/pmcp-openapi-server/src/main.rs, Cargo.toml</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/{Cargo.toml, src/cli.rs, src/main.rs}; MODIFY root Cargo.toml `[workspace.members]`.
    - ANALOG (exact): crates/pmcp-sql-server/Cargo.toml (deps + features + `exclude`), crates/pmcp-sql-server/src/cli.rs (`Args` struct + tests — `config: PathBuf` required :44, `schema: PathBuf` required :46-47, `http: String` default `"127.0.0.1:8080"` :50-51), crates/pmcp-sql-server/src/main.rs (492-byte `#[tokio::main]` shim → `run(Args::parse())`).
    - RESEARCH "Installation" deps: `pmcp` w/ `streamable-http`, `pmcp-server-toolkit` w/ `["openapi-code-mode"]` (the umbrella feature that composes to `http` + `code-mode` + `pmcp-code-mode/js-runtime` — Plan 90-01 Task 1; the bare `code-mode` feature does NOT forward `pmcp-code-mode/js-runtime`, so the script-tool/code-mode engine path would NOT compile under `["http","code-mode"]` alone), `clap` (`derive`,`env`), `tokio` (`macros`,`rt-multi-thread`), `thiserror`, `tracing`+`tracing-subscriber`; dev: `mcp-tester` 0.7.0, `wiremock` 0.6.
    - DECISION: D-03 (`--spec` OPTIONAL — `Option<PathBuf>`, drop requiredness), CF-1 (streamable HTTP), CLAUDE.md publish slot (after toolkit + connectors).
  </read_first>
  <action>
    Create `crates/pmcp-openapi-server/Cargo.toml` mirroring `pmcp-sql-server/Cargo.toml`: enable `pmcp-server-toolkit` features `["openapi-code-mode"]` (the umbrella that composes `http` + `code-mode` + `pmcp-code-mode/js-runtime` — Plan 90-01 Task 1; drop `sqlite`/connector features). Do NOT use `["code-mode", "http"]` — that omits `pmcp-code-mode/js-runtime` and the `ScriptToolHandler` + code-mode engine path (gated `#[cfg(feature = "openapi-code-mode")]` in Plans 90-04/90-05) would compile out, breaking the binary's script-tool/code-mode wiring (RESEARCH Pitfall 4). The binary serves the full surface, so it needs the umbrella on. Add `exclude = [tests/, .planning/, .pmat/, fuzz/]` to keep fixtures out of the published crate (mirror sql-server). Add `crates/pmcp-openapi-server` to root `Cargo.toml` `[workspace.members]`.
    Create `src/cli.rs` copying the sql-server `Args` struct + its tests, changing ONLY: `pub schema: PathBuf` (required) → `pub spec: Option<PathBuf>` (OPTIONAL, D-03 — doc "required at scaffold time, optional at runtime"); `name`/`about` strings → openapi-server. Keep `config: PathBuf` (required) and `http: String` default `"127.0.0.1:8080"` unchanged.
    Create `src/main.rs` as the thin `#[tokio::main]` shim calling `pmcp_openapi_server::run(Args::parse())` (copy sql-server main verbatim, rename crate).
    Coverage: keep the cli arg-parse tests (mirror sql-server's).
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/Cargo.toml` lists `pmcp-server-toolkit` with `["openapi-code-mode"]` (NOT `["code-mode","http"]`) and `crates/pmcp-openapi-server` is in root `[workspace.members]`: `grep -q 'openapi-code-mode' crates/pmcp-openapi-server/Cargo.toml`.
    - `grep -n "pub spec: Option<PathBuf>" crates/pmcp-openapi-server/src/cli.rs` matches (OPTIONAL, D-03).
    - `cargo build -p pmcp-openapi-server` compiles (after Tasks 2-3 land the lib) — with `openapi-code-mode` on, `PlanCompiler`/`ScriptToolHandler` are in scope.
    - `cargo test -p pmcp-openapi-server cli -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server cli -- --test-threads=1</automated>
  </verify>
  <done>Crate scaffolded as a workspace member; --spec optional; toolkit dep uses the openapi-code-mode umbrella so the script-tool/code-mode engine compiles in.</done>
</task>

<task type="auto">
  <name>Task 2: dispatch.rs — build the (HttpConnector, HttpCodeExecutor) pair lazily (CF-2)</name>
  <files>crates/pmcp-openapi-server/src/dispatch.rs</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/src/dispatch.rs.
    - ANALOG (shape + redaction): crates/pmcp-sql-server/src/dispatch.rs (read fully) — the `DispatchError` `#[non_exhaustive]` enum + Security doc-comment (dispatch.rs:42-103, Display names backend/field ONLY), and the lazy-startup discipline (no `schema_text()`/`execute()` at dispatch time, dispatch.rs:113-115). The SQL version returns a single `Arc<dyn SqlConnector>`; the OpenAPI version returns BOTH (partial-analog, the one "No Analog Found" item).
    - SOURCE (the seam shape): 90-RESEARCH.md "Dispatch seam" code block (lines ~660-674) + 90-PATTERNS.md dispatch.rs section.
    - DECISION: CF-2 (lazy — reqwest client built without contacting the backend), D-05 (six auth modes via `create_auth_provider`), Pitfall 5 (redaction).
  </read_first>
  <action>
    Implement `pub async fn dispatch(cfg: &ServerConfig) -> Result<(Arc<dyn HttpConnector>, HttpCodeExecutor), DispatchError>`: read `cfg.backend` (error `DispatchError::MissingBackend` if absent), build `auth = toolkit::http::auth::create_auth_provider(&backend.auth)?` (the static-construction path — `oauth_passthrough` is constructed with no token here and gets its per-request token threaded in Task 3, H1), build a shared `reqwest::Client` (lazy — no network, CF-2), construct `HttpClient::new(client.clone(), backend.base_url.clone(), auth.clone())?` and `HttpCodeExecutor::new(client, backend.base_url.clone(), auth)`, return the pair.
    Copy the SQL `DispatchError` `#[non_exhaustive]` enum + its Security doc-comment (V7 / Pitfall 5 — Display names backend/field ONLY, never URL/credential, including the backend base_url — Codex LOW: add the base-url redaction case to the test). Add a `display_no_secret` test for `DispatchError` that includes the backend base_url among the strings asserted absent.
    Coverage: a unit test asserting `dispatch` on a london-tube-shaped `[backend]` config returns the pair WITHOUT any network call (offline / fast — CF-2), and `MissingBackend` when `[backend]` is absent.
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/src/dispatch.rs` returns `(Arc<dyn HttpConnector>, HttpCodeExecutor)`.
    - `grep -c "schema_text\|\.execute(" crates/pmcp-openapi-server/src/dispatch.rs` returns 0 (lazy, CF-2).
    - `cargo test -p pmcp-openapi-server dispatch -- --test-threads=1` exits 0 (offline + MissingBackend + display_no_secret incl. base_url).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server dispatch -- --test-threads=1</automated>
  </verify>
  <done>dispatch builds the connector+executor pair lazily; DispatchError redacts (incl. base_url); MissingBackend handled.</done>
</task>

<task type="auto">
  <name>Task 3: assemble.rs + lib.rs — build_server + token capture + run pipeline over streamable HTTP (D-03/CF-1/H1)</name>
  <files>crates/pmcp-openapi-server/src/assemble.rs, crates/pmcp-openapi-server/src/lib.rs, crates/pmcp-openapi-server/examples/openapi_server_min.rs</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/src/{assemble.rs, lib.rs}.
    - ANALOG (assemble): crates/pmcp-sql-server/src/assemble.rs (read first 200 lines) — `AssembleError` (:71-83, `Toolkit(#[from])` + `Build(#[from] pmcp::Error)`), the `ServerBuilderExt` single crate-root import (:42-44), `merge_schema_resource` (:98-152, the `/schema`-suffix override at :111), the synthesized `code-mode://instructions`/`code-mode://policies` resources (:155-200). PITFALL 6: do NOT lift the reference `pmcp_server.rs`.
    - PASSTHROUGH CAPTURE (H1 — read_first the bridge): `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/crates/mcp-openapi-server-core/src/pmcp_server.rs:37-64` — the inbound `TokenCaptureAuthProvider` stores the MCP client token in `AuthContext`. Lift the CAPTURE concept (NOT the whole `pmcp_server.rs`, Pitfall 6): install an inbound auth/middleware that captures the client token, and thread it from the handler's `RequestHandlerExtra` (`ToolHandler::handle(args, extra)`) into `HttpCodeExecutor.with_inbound_token(token)` per request so the outbound passthrough provider (Plan 01/04) forwards it.
    - ANALOG (lib): crates/pmcp-sql-server/src/lib.rs (read fully) — `RunError` (:52-97; rename `schema`→`spec`), `load_config_and_schema` (:108-119) → `load_config_and_spec` returning `(ServerConfig, Option<OpenApiSchema>)` (D-03 — `args.spec.as_ref().map(parse)` not an unconditional read), `serve` (:156-164, copy VERBATIM — `StreamableHttpServer::with_config(default())`, CF-1), `run_serving` (:200-211), `run` (:245-265, copy VERBATIM incl. `handle.await.map_err(RunError::Serving)`, threat T-85-10-02).
    - DEPENDS: Plan 03 (`synthesize_from_config_with_http_connector` + single-call), Plan 04 (`HttpCodeExecutor` + `with_inbound_token` + generalized `code_mode_tools_from_executor` + `JsCodeExecutor`), Plan 05 (`ScriptToolHandler` branch — gated `#[cfg(feature = "openapi-code-mode")]`; compiled in because Task 1 enabled the umbrella feature on the toolkit dep).
    - DECISION: D-03 (spec optional + NO-spec/code-mode behavior, see action), CF-1 (streamable HTTP only), D-02 (one engine), H1 (token capture).
  </read_first>
  <action>
    Implement `assemble.rs::build_server(cfg, connector, http_exec, spec: Option<OpenApiSchema>) -> Result<pmcp::Server, AssembleError>`: via `ServerBuilderExt`, register single-call + script tools through `synthesize_from_config_with_http_connector(cfg, connector, http_exec.clone(), exec_config)` (Plan 03/05 — the synthesizer routes `is_script_tool()` to `ScriptToolHandler` over the shared `http_exec`), wire Code Mode via the generalized `code_mode_tools_from_executor(builder, cfg, Arc::new(JsCodeExecutor::new(http_exec.clone(), exec_config)), ValidationFlavor::OpenApi)` (Plan 04 — the SAME `http_exec` instance, D-02), preserve configured resources/prompts (mirror sql-server `merge_schema_resource` + `StaticPromptHandler::from_configs`).
    H1 — TOKEN CAPTURE: install the inbound capture so the MCP client token lands in `AuthContext`, and ensure the synthesized handlers / `ScriptToolHandler` / Code-Mode path derive a per-request `HttpCodeExecutor` via `http_exec.with_inbound_token(token_from_extra)` (read the captured token from `RequestHandlerExtra`/`AuthContext` in `handle(args, extra)`). This is what makes `oauth_passthrough` actually forward the client token. If the toolkit synthesizer does not yet thread `extra` into the per-request executor, wire the capture at the binary's handler boundary (document the seam; the outbound `apply(.., inbound_token)` from Plan 01 is the receiving end). Add a `read_first` reference to `pmcp_server.rs:37-64`.
    NO-SPEC + CODE-MODE behavior (Codex MEDIUM — DEFINE it): when `[code_mode] enabled = true` and `spec.is_none()`, Code Mode RUNS but WITHOUT the `api_schema` resource — emit a `tracing::warn!` "code_mode enabled without --spec: the api_schema resource is unavailable; the LLM will generate long-tail scripts without the OpenAPI contract" and proceed (do NOT fail, do NOT silently drop Code Mode). When `spec.is_some()` merge it as the `api_schema`/`/schema` resource (D-03 surface (a)). Add a test asserting this warn-and-proceed behavior (server still builds + Code Mode tools register with no spec).
    Implement `lib.rs`: `RunError` (rename schema→spec), `load_config_and_spec(args)` → `(ServerConfig::from_toml_strict_validated, Option<OpenApiSchema>)` with the spec parsed only when `args.spec` is `Some` (D-03), `serve` copied VERBATIM from sql-server (`StreamableHttpServer`, CF-1), `run_serving` (load → dispatch → build_server → parse addr → serve), `run` copied VERBATIM. Expose `run_serving` as a testable seam returning `(bound_addr, handle)` (mirror sql-server's Codex-HIGH-#5 pattern) so tests drive the real binary path with a BOUNDED shutdown guard (drop/abort the handle at test end — no leaked spawned server).
    Per ALWAYS coverage: an `http_smoke` integration test (mirror SQL SC-1) that drives `run_serving` against a curated-only config (NO spec — D-03 proof) over a wiremock backend, confirms the server binds + serves over streamable HTTP and that a tools/list reflects the configured tools, then ABORTS the server handle (bounded shutdown — Codex: no leaked servers across tests). Add a runnable example `examples/openapi_server_min.rs` (≤15-line wiring shape, CF-5) — make it BUILD-ONLY safe for CI: verify it via `cargo build --example openapi_server_min` (Codex HIGH: a running-server example would hang `cargo run --example`; either gate the actual `serve()` behind reading args and document "run manually", or have `main` build the server and return without blocking when an env flag like `OPENAPI_EXAMPLE_BUILD_ONLY` is set). The acceptance check uses `cargo build --example`, NOT `cargo run --example`.
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/src/lib.rs` contains `load_config_and_spec` returning an `Option<OpenApiSchema>` and `serve` using `StreamableHttpServer`.
    - `grep -c "mcp_server_common::\|OpenApiPmcpBuilder" crates/pmcp-openapi-server/src/assemble.rs` returns 0 (Pitfall 6 — toolkit assemble, not reference builder).
    - assemble.rs threads the captured inbound token into `with_inbound_token` (H1): `grep -c "with_inbound_token" crates/pmcp-openapi-server/src/assemble.rs` ≥ 1.
    - No-spec + code-mode warn-and-proceed test present and green.
    - `cargo test -p pmcp-openapi-server http_smoke -- --test-threads=1` exits 0 (curated-only / no-spec boot + serve + bounded shutdown).
    - `cargo build -p pmcp-openapi-server --example openapi_server_min` builds (ALWAYS example, build-only — does NOT hang).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server http_smoke -- --test-threads=1 && cargo build -p pmcp-openapi-server --example openapi_server_min</automated>
  </verify>
  <done>The binary serves a config-driven OpenAPI MCP server over streamable HTTP; spec optional with defined no-spec+code-mode behavior; inbound token capture feeds oauth_passthrough (H1); one engine feeds tools+code-mode; build-only example does not hang; bounded test shutdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → binary transport | streamable-HTTP requests (CORS/DNS-rebinding handled by the SDK) |
| binary → REST backend | outgoing requests via the dispatched connector/executor |
| inbound MCP token → AuthContext → outbound passthrough | captured client token threaded per request (H1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-06-01 | Information Disclosure | `DispatchError`/`RunError`/`AssembleError` Display | mitigate | Copy SQL redaction discipline; `display_no_secret` for DispatchError (incl. base_url); names backend/field only |
| T-90-06-02 | Spoofing/DoS | inbound MCP transport (CORS, DNS rebinding) | mitigate | Reuse `pmcp::StreamableHttpServer` security layers (Phase 56) — not re-implemented |
| T-90-06-03 | Availability | serve-task panic exits silently | mitigate | `handle.await.map_err(RunError::Serving)` propagates a serve panic non-zero (threat T-85-10-02, copied verbatim); tests bound the handle (no leak) |
| T-90-06-04 | Information Disclosure | spec/config as deploy assets leaking secrets | accept | spec/config are admin-authored read-only assets; secret refs resolved via toolkit secrets (CF-8); deploy posture handled in Plan 08 |
| T-90-06-05 | Spoofing/EoP | captured inbound token misrouted / leaked | mitigate | token flows only to `oauth_passthrough` via `with_inbound_token`+`apply(.., inbound_token)`; static providers ignore it (Plan 01); errors redact it (Pitfall 5) |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server -- --test-threads=1` green (cli + dispatch + http_smoke).
- Curated-only (no-spec) boot proven (D-03); defined no-spec+code-mode warn-and-proceed; streamable HTTP only (CF-1); lazy startup (CF-2).
- Inbound token capture threads to `with_inbound_token` (H1); example is build-only (no hang); test server handles are bounded.
- Toolkit dep uses `openapi-code-mode` umbrella; no `mcp_server_common::`/reference `pmcp_server.rs` survivors (Pitfall 6).
</verification>

<success_criteria>
- `pmcp-openapi-server` serves a config-driven OpenAPI MCP server over streamable HTTP (OAPI-06).
- `--spec` optional at runtime with defined no-spec+code-mode behavior; lazy startup; one engine feeds single-call + script tools + Code Mode.
- Inbound TokenCaptureAuthProvider feeds the oauth_passthrough outbound forward (H1).
- Build-only runnable example (does not hang) + bounded-shutdown smoke test present (ALWAYS coverage).
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-06-SUMMARY.md`
</output>
</content>
