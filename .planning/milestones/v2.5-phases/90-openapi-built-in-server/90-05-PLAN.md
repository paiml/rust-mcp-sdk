---
phase: 90-openapi-built-in-server
plan: 05
type: execute
wave: 4
depends_on: [90-03, 90-04]
files_modified:
  - crates/pmcp-server-toolkit/src/tools.rs
  - crates/pmcp-server-toolkit/tests/script_tool_engine_parity.rs
autonomous: true
requirements: [OAPI-02b, OAPI-10]
must_haves:
  truths:
    - "A script [[tools]] entry runs its embedded JS through the SAME engine as Code Mode"
    - "Script-tool args are bound to the `args` variable and schema-validated first"
    - "A multi-call chain with filter/map across steps executes and returns the chained result"
    - "Script tools run with NO validate/token cycle (admin-authored, ExecutionConfig-bounded)"
    - "The SAME script produces IDENTICAL output via a script tool and via execute_code (D-02 proof)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/tools.rs"
      provides: "ScriptToolHandler + the is_script_tool() synthesis branch"
      contains: "struct ScriptToolHandler"
    - path: "crates/pmcp-server-toolkit/tests/script_tool_engine_parity.rs"
      provides: "wiremock D-02 byte-equality proof (script tool vs execute_code)"
      contains: "engine_parity"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/tools.rs ScriptToolHandler"
      to: "pmcp_code_mode::PlanCompiler / PlanExecutor"
      via: "compile_code + PlanExecutor over HttpCodeExecutor"
      pattern: "PlanExecutor::new"
    - from: "ScriptToolHandler"
      to: "HttpCodeExecutor"
      via: "shared seam (same instance feeds Code Mode)"
      pattern: "http_exec"
---

<objective>
Implement script tools (OAPI-02b / D-01): a `[[tools]]` entry with a `script` field
runs admin-authored embedded JS through the EXACT SAME `PlanCompiler` + `PlanExecutor`
+ `HttpCodeExecutor` seam that Code Mode uses (D-02), with NO validate/token cycle.
Then prove D-02 with a first-class engine-parity test: the same script yields
identical output via a script tool and via `execute_code` (OAPI-10's parity proof).

Purpose: The headline decision — one engine, two surfaces. A script promoted from
Code Mode to a curated tool behaves identically.
Output: `ScriptToolHandler` + the wired `is_script_tool()` branch + the parity test.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
@.planning/phases/90-openapi-built-in-server/90-03-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScriptToolHandler + the is_script_tool() synthesis branch (OAPI-02b / D-01 / D-02)</name>
  <files>crates/pmcp-server-toolkit/src/tools.rs</files>
  <read_first>
    - MODIFY crates/pmcp-server-toolkit/src/tools.rs (add `ScriptToolHandler` + fill the `is_script_tool()` branch left by Plan 03).
    - DESIGN ANCHOR (no verbatim body to lift — Status: Proposed in the reference): `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/OPENAPI_SCRIPT_TOOLS.md` (Steps 1-3: `script`/`[[tools.parameters]]`/`args` config, detection, ScriptToolHandler over PlanCompiler/PlanExecutor/HttpExecutor). The reference `tools/mod.rs` does NOT implement this — IMPLEMENT it here.
    - IN-TREE analog (ToolHandler shape): the toolkit's `SynthesizedToolHandler` in tools.rs (`#[async_trait] impl ToolHandler { handle; metadata -> Some(ToolInfo) }`) + the `ValidateCodeHandler`/`ExecuteCodeHandler` in code_mode.rs:243-381 (how a handler wires a `pmcp_code_mode::*` type). `build_input_schema` builds the `tool_info` from `[[tools.parameters]]` (same path as single-call — `args` schema-validated before the script runs).
    - SDK engine: `pmcp_code_mode::PlanCompiler::with_config` + `compile_code`; `PlanExecutor::new(http_exec, exec_config)` + `set_variable("args", args)` + `execute(&plan)` (RESEARCH Pattern 4b / lines executor.rs:701, 2737).
    - FEATURE GATE: `ScriptToolHandler` (and the engine path it needs — `PlanCompiler`/`PlanExecutor`) is gated under the toolkit umbrella feature `openapi-code-mode` (Plan 90-01 Task 1: `openapi-code-mode = ["http", "code-mode", "pmcp-code-mode/js-runtime"]`). The bare `code-mode` feature does NOT forward `pmcp-code-mode/js-runtime`, so `PlanCompiler::with_config` is NOT in scope under `["http","code-mode"]` alone — the gate MUST be `#[cfg(feature = "openapi-code-mode")]`, not `#[cfg(all(feature="http", feature="code-mode"))]` (RESEARCH Pitfall 4 — the light/curated-only build must keep compiling without this).
    - DECISION: D-02 (SAME engine — the SAME `HttpCodeExecutor` instance feeds both surfaces), D-01 (`script.is_some()` ⇒ ScriptToolHandler; `[[tools.parameters]]` → `args`), Pitfall 7 (NO token cycle — admin-authored, bounded only by `ExecutionConfig`: `max_api_calls=50`, `max_loop_iterations=100`, `timeout_seconds=30`).
  </read_first>
  <action>
    Add `#[cfg(feature = "openapi-code-mode")] pub struct ScriptToolHandler { script: String, http_exec: HttpCodeExecutor, exec_config: pmcp_code_mode::ExecutionConfig, tool_info: pmcp::types::ToolInfo }` with `pub fn new(tool_cfg: &ToolDecl, http_exec: HttpCodeExecutor, exec_config: ExecutionConfig) -> Result<Self, ToolkitError>` (builds `tool_info` from `[[tools.parameters]]` via `build_input_schema`). Gate the impl block with the SAME `#[cfg(feature = "openapi-code-mode")]` — `PlanCompiler::with_config` is only in scope under that umbrella (`code-mode` alone does not forward `pmcp-code-mode/js-runtime`; Plan 90-01 Task 1, RESEARCH Pitfall 4). Implement `#[pmcp_code_mode::async_trait] impl pmcp::ToolHandler for ScriptToolHandler::handle`: compile the script with `PlanCompiler::with_config(&self.exec_config).compile_code(&self.script)` (compile error → `pmcp::Error::Internal("script compile failed: ...")`), build `PlanExecutor::new(self.http_exec.clone(), self.exec_config.clone())`, `set_variable("args", args)`, `execute(&plan).await` (exec error → `Internal`), return `result.value`. Implement `fn metadata(&self) -> Some(self.tool_info.clone())`. NO `validate_code`/token machinery (Pitfall 7). Keep `handle` cog ≤25.
    Fill the `is_script_tool()` branch in `synthesize_from_config_with_http_connector` (the seam Plan 03 left): `if tool_cfg.is_script_tool() { builder.tool_arc(name, Arc::new(ScriptToolHandler::new(tool_cfg, http_exec.clone(), exec_config.clone())?)) } else { /* single-call, Plan 03 */ }`. Gate the `ScriptToolHandler`-constructing branch under `#[cfg(feature = "openapi-code-mode")]` so the synthesizer still compiles in the light build (single-call only); thread `http_exec: HttpCodeExecutor` and `exec_config` into the synthesizer signature (add params — the binary's assemble.rs in Plan 06 supplies them; update the Plan 03 single-call signature accordingly).
    Per ALWAYS coverage: `script_tool` integration tests (wiremock) — (a) a single-step script `return await api.get('/Line/Mode/tube/Status')` returns the mock JSON; (b) the london-tube multi-call chain from RESEARCH "Code Examples" (`filter` disrupted lines → `slice(0,args.maxLines)` → per-line `api.get('/Line/'+id+'/Disruption')`) returns the chained result; (c) `args.maxLines` binding from `[[tools.parameters]]` is honored; (d) an `ExecutionConfig` `max_api_calls` cap aborts an over-budget script (DoS bound). Run these tests under `--features openapi-code-mode`.
  </action>
  <behavior>
    - Test (wiremock): a single-call script returns the mocked status JSON.
    - Test (wiremock): a multi-call chain with filter/map/slice across steps returns the chained result, using `args.maxLines`.
    - Test: a script with no `script` field is never routed here; a `script` tool is never given a token requirement.
    - Test: exceeding `max_api_calls` aborts with a bounded error (no infinite loop).
  </behavior>
  <acceptance_criteria>
    - `grep -n "struct ScriptToolHandler" crates/pmcp-server-toolkit/src/tools.rs` matches; `grep -c "validate_code\|approval token" crates/pmcp-server-toolkit/src/tools.rs` returns 0 in the ScriptToolHandler region (Pitfall 7).
    - `ScriptToolHandler` is gated `#[cfg(feature = "openapi-code-mode")]` (NOT `all(feature="http", feature="code-mode")`): `grep -n 'cfg(feature = "openapi-code-mode")' crates/pmcp-server-toolkit/src/tools.rs` matches the handler; `grep -c 'cfg(all(feature = "http", feature = "code-mode"))' crates/pmcp-server-toolkit/src/tools.rs` returns 0.
    - `cargo build -p pmcp-server-toolkit --features "http code-mode"` (light build, NO `openapi-code-mode`) still compiles — the script-tool path is cfg'd out, single-call only (RESEARCH Pitfall 4).
    - `cargo test -p pmcp-server-toolkit --features openapi-code-mode script_tool -- --test-threads=1` exits 0 (single + chain + args + bound).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features openapi-code-mode script_tool -- --test-threads=1</automated>
  </verify>
  <done>Script tools compile+run admin-authored JS over the shared engine with args binding + multi-call chain + ExecutionConfig bounds; no token cycle; gated under openapi-code-mode (light build still compiles).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine-parity proof — script tool vs execute_code byte-equality (D-02 / OAPI-10)</name>
  <files>crates/pmcp-server-toolkit/tests/script_tool_engine_parity.rs</files>
  <read_first>
    - CREATE crates/pmcp-server-toolkit/tests/script_tool_engine_parity.rs (Wave-0 file from 90-VALIDATION.md).
    - SOURCE-OF-TRUTH (the D-02 invariant): 90-VALIDATION.md OAPI-10 row — "script tool + Code Mode produce IDENTICAL output for the SAME script"; 90-RESEARCH.md Pattern 4 + State-of-the-Art "Reconciliation verdict (D-02)" (JsCodeExecutor wraps the same PlanCompiler/PlanExecutor).
    - SHAPE analog (parity-as-assertion harness): crates/pmcp-sql-server `tests/parity_chinook.rs` (the per-step success-gating + spawn pattern) — adapt to wiremock + in-process executor comparison.
    - FEATURE GATE: this test exercises `ScriptToolHandler` + the JsCodeExecutor path, both gated under `openapi-code-mode`. Run the test under `--features openapi-code-mode` (the umbrella that brings `pmcp-code-mode/js-runtime` into scope; `["http","code-mode"]` alone is insufficient — RESEARCH Pitfall 4).
    - DEPENDS: Task 1 (ScriptToolHandler), Plan 04 (HttpCodeExecutor + generalized code_mode wiring + JsCodeExecutor path).
    - DECISION: D-02 (hard requirement — identical semantics), Pitfall 0 (one engine, no Boa).
  </read_first>
  <action>
    Write an integration test `engine_parity` against a single `wiremock::MockServer` configured with the london-tube-shaped responses (a `/Line/Mode/tube/Status` list + per-line `/Line/{id}/Disruption`). Build ONE `HttpCodeExecutor` over the mock base_url + a `NoAuth`/`ApiKeyAuth` provider. Define ONE script string `S` (the disrupted-lines chain). Path A: construct a `ScriptToolHandler` with `S` and call `handle(args)`. Path B: wrap the SAME `HttpCodeExecutor` in `JsCodeExecutor` and execute `S` via the generalized `execute_code` surface (or directly via the `CodeExecutor::execute` the toolkit wires), passing the same `args` (bound to `args`). Assert `path_a_output == path_b_output` (serde_json::Value equality) — the D-02 byte-equality proof.
    Add a second assertion that the mock received the SAME sequence of requests from both paths (request-count / path equality) to prove identical engine behavior, not just identical final value.
    Keep the test deterministic and offline (wiremock only, no network — D-04). Run under `--test-threads=1`. Gate the whole test file `#![cfg(feature = "openapi-code-mode")]` so it is skipped in the light build.
  </action>
  <behavior>
    - Test: `engine_parity` — output of the script via ScriptToolHandler equals output via execute_code for the SAME script + args (serde_json::Value equality).
    - Test: both paths issue the same backend request sequence against the mock.
  </behavior>
  <acceptance_criteria>
    - `crates/pmcp-server-toolkit/tests/script_tool_engine_parity.rs` exists and contains the `engine_parity` test with a `Value`-equality assertion.
    - `cargo test -p pmcp-server-toolkit --features openapi-code-mode engine_parity -- --test-threads=1` exits 0.
    - The test uses wiremock only (no live network): `grep -c "tfl.gov.uk\|reqwest::get" crates/pmcp-server-toolkit/tests/script_tool_engine_parity.rs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features openapi-code-mode engine_parity -- --test-threads=1</automated>
  </verify>
  <done>The D-02 invariant is proven by a first-class offline test: one script, two surfaces, identical output + identical backend request sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-authored script → engine | trusted embedded JS compiled + executed without approval gate |
| client tool args → `args` variable | client args bound into the script's `args` after schema validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-05-01 | EoP / Malicious Code | script tool skips the validate/token cycle | accept | BY DESIGN — admin-authored + trusted, like a `sql=` curated query (Pitfall 7, OPENAPI_SCRIPT_TOOLS.md §Security); bounded by `ExecutionConfig` + the PlanCompiler-accepted JS subset (no eval/FFI) |
| T-90-05-02 | DoS | unbounded multi-call / loop in a script | mitigate | `ExecutionConfig` caps (`max_api_calls`, `max_loop_iterations`, `timeout_seconds`) enforced by PlanExecutor; tested (over-budget abort) |
| T-90-05-03 | Tampering | client args injecting beyond declared params | mitigate | `args` schema-validated against `[[tools.parameters]]` (object envelope, `additionalProperties:false`) before the script runs |
| T-90-05-04 | correctness/divergence | script tool diverging from Code Mode | mitigate | engine_parity test proves byte-equality + identical request sequence across both surfaces (D-02) |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features openapi-code-mode script_tool engine_parity -- --test-threads=1` green.
- Light build (`--features "http code-mode"`, no `openapi-code-mode`) still compiles — script-tool path cfg'd out (RESEARCH Pitfall 4).
- Engine-parity test is offline (wiremock); no token cycle in ScriptToolHandler.
</verification>

<success_criteria>
- Script tools run admin-authored JS over the shared engine with args binding + multi-call chain (OAPI-02b / D-01).
- The D-02 one-engine invariant is proven by a first-class byte-equality test (OAPI-10).
- ExecutionConfig bounds enforced; no validate/token cycle; gated under `openapi-code-mode`.
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-05-SUMMARY.md`
</output>
