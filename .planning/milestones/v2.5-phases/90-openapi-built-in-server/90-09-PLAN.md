---
phase: 90-openapi-built-in-server
plan: 09
type: execute
wave: 7
depends_on: [90-06, 90-07, 90-08]
files_modified:
  - crates/pmcp-openapi-server/README.md
  - pmcp-book/src/SUMMARY.md
  - pmcp-book/src/openapi-built-in-server.md
  - pmcp-course/src/SUMMARY.md
  - pmcp-course/src/openapi-built-in-server.md
autonomous: true
requirements: [OAPI-09]
must_haves:
  truths:
    - "The crate README documents the Shape A binary, two-kind tools, 5 auth variants, and --spec optionality"
    - "A pmcp-book chapter covers the OpenAPI built-in server (cargo pmcp on-ramp first)"
    - "A pmcp-course chapter covers the OpenAPI built-in server"
    - "All doc code fences are doctest-safe and grounded in real source"
  artifacts:
    - path: "crates/pmcp-openapi-server/README.md"
      provides: "crate README (Shape A binary, config-driven, two-kind tools, auth, CLI)"
      contains: "pmcp-openapi-server"
    - path: "pmcp-book/src/openapi-built-in-server.md"
      provides: "book chapter"
      contains: "openapi-server"
    - path: "pmcp-course/src/openapi-built-in-server.md"
      provides: "course chapter"
      contains: "openapi-server"
  key_links:
    - from: "pmcp-book/src/SUMMARY.md"
      to: "openapi-built-in-server.md"
      via: "SUMMARY toc entry"
      pattern: "openapi-built-in-server"
---

<objective>
Document the OpenAPI built-in server in three shapes (OAPI-09): the crate README, a
`pmcp-book` chapter, and a `pmcp-course` chapter — each leading with the `cargo pmcp`
CLI workflow (the on-ramp), grounded in real source, doctest-safe. Mirrors the SQL
docs work and the project's "docs in three shapes" convention.

Purpose: Make the new built-in discoverable and usable by non-developers and
developers alike.
Output: README + book chapter + course chapter + SUMMARY toc entries.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-06-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-07-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crate README — pmcp-openapi-server (Shape A binary)</name>
  <files>crates/pmcp-openapi-server/README.md</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/README.md.
    - ANALOG (the model README): crates/pmcp-sql-server/README.md (quick task 260528-oam — "the improvement" framing: two inputs config.toml + spec vs hand-written Rust, no recompile; Pareto ~20% curated tools / ~80% Code Mode; backends table traced verbatim to source; quickstart with build/install + minimal config + `--config`/`--spec`/`--http` CLI). Read it fully as the template.
    - SOURCE-OF-TRUTH (ground claims in real source): `crates/pmcp-openapi-server/src/cli.rs` (the actual CLI flags), `examples/openapi_server_min.rs` (the minimal config), Plan 07 london-tube fixture (the worked example), 90-CONTEXT.md D-01..D-06 (the feature set).
    - DECISION: docs lead with `cargo pmcp` on-ramp (memory feedback_docs_three_shapes_cargo_pmcp); D-01 (two-kind tools), D-03 (--spec optional), D-05 (5 auth variants).
  </read_first>
  <action>
    Write `crates/pmcp-openapi-server/README.md` mirroring the sql-server README structure but for OpenAPI: the "improvement" framing (two inputs `config.toml` + optional OpenAPI spec, no recompile; ~20% curated `[[tools]]` / ~80% Code Mode); the TWO-KIND tool model (single-call `path`/`method` AND script `script="""..."""`, D-01) with the london-tube example; the FIVE auth variants table (`none`/`api_key`/`bearer`/`basic`/`oauth2_client_credentials`/`oauth_passthrough`, D-05) traced to `http::auth`; `--spec` optionality (D-03 — curated-only boots without a spec); quickstart (build/install, the minimal config from the example, `--config`/`--spec`/`--http` CLI traced to `cli.rs`). Keep all fences doctest-safe (no fenced rust that would run as a doctest, or mark `text`/`toml`/`bash`). Lead with the `cargo pmcp new --kind openapi-server` on-ramp.
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/README.md` exists; contains the two-kind tool model, the 5-auth table, and `--config`/`--spec`/`--http`.
    - CLI flags in the README match `cli.rs` (no invented flags) — spot-checked against the actual `Args`.
    - `cargo build -p pmcp-openapi-server` still green (README adds no compile surface; doctest-safe fences).
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/pmcp-openapi-server/README.md && grep -q "openapi-server" crates/pmcp-openapi-server/README.md && echo OK</automated>
  </verify>
  <done>Crate README documents the Shape A binary, two-kind tools, 5 auth variants, --spec optionality, grounded in real source.</done>
</task>

<task type="auto">
  <name>Task 2: pmcp-book + pmcp-course chapters + SUMMARY toc</name>
  <files>pmcp-book/src/openapi-built-in-server.md, pmcp-book/src/SUMMARY.md, pmcp-course/src/openapi-built-in-server.md, pmcp-course/src/SUMMARY.md</files>
  <read_first>
    - CREATE pmcp-book/src/openapi-built-in-server.md, pmcp-course/src/openapi-built-in-server.md; MODIFY pmcp-book/src/SUMMARY.md, pmcp-course/src/SUMMARY.md.
    - ANALOG (the SQL chapters + toc placement): the existing SQL built-in / pmcp-sql-server chapters in pmcp-book and pmcp-course (Phase 85/86 docs) and their `SUMMARY.md` entries. Read the SQL chapter to mirror structure + the on-ramp-first framing.
    - SOURCE: Plan 06 (binary), Plan 07 (london-tube worked example), Plan 08 (scaffold), 90-CONTEXT.md.
    - DECISION: docs lead with `cargo pmcp` on-ramp; Shape A (binary) vs Shape B (`cargo pmcp new --kind openapi-server` scaffold) are siblings (memory feedback).
  </read_first>
  <action>
    Write `pmcp-book/src/openapi-built-in-server.md` and `pmcp-course/src/openapi-built-in-server.md` mirroring the SQL built-in chapters: the config-driven model, the `cargo pmcp new --kind openapi-server` on-ramp first, the two-kind tools + Code Mode long-tail (D-01/D-02), the 5 auth variants (D-05), `--spec` optionality (D-03), and the london-tube worked example (from Plan 07). Differentiate audiences: book = reference depth; course = guided walkthrough. Add the chapter to both `SUMMARY.md` files in the appropriate section (next to the SQL built-in chapter). Keep fences doctest-safe / `toml`/`bash`/`text` tagged.
    Run the mdbook build for both books to confirm the new chapters render and the toc resolves (no broken links).
  </action>
  <acceptance_criteria>
    - Both chapter files exist and are referenced in their respective `SUMMARY.md`.
    - `grep -c "openapi-built-in-server" pmcp-book/src/SUMMARY.md pmcp-course/src/SUMMARY.md` ≥ 1 each.
    - `mdbook build pmcp-book` and `mdbook build pmcp-course` succeed (no broken-link/toc errors).
  </acceptance_criteria>
  <verify>
    <automated>mdbook build pmcp-book && mdbook build pmcp-course</automated>
  </verify>
  <done>Book + course chapters cover the OpenAPI built-in server (cargo pmcp on-ramp first); both mdbooks build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | docs guide users to configure secrets + deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-09-01 | Information Disclosure | docs showing a real secret as a copy-paste example | mitigate | examples use `${ENV}` references + a loud DEV-only note; never a real token/app_key in any fence |
| T-90-09-02 | correctness | docs drifting from actual CLI/config | mitigate | flags/config traced to `cli.rs`/the example; doc fences doctest-safe; mdbook build gate |
</threat_model>

<verification>
- README + both chapters exist and are toc-linked; both mdbooks build clean.
- CLI/config claims match real source; no real secrets in examples.
</verification>

<success_criteria>
- Docs in three shapes: crate README + pmcp-book chapter + pmcp-course chapter (OAPI-09).
- Each leads with the `cargo pmcp` on-ramp; grounded in real source; doctest-safe.
- The scoped cargo-pmcp README `--kind openapi-server` addition (Plan 08) completes the docs surface.
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-09-SUMMARY.md`
</output>
