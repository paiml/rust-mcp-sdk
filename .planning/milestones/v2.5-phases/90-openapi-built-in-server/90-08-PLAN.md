---
phase: 90-openapi-built-in-server
plan: 08
type: execute
wave: 6
depends_on: [90-06]
files_modified:
  - cargo-pmcp/src/commands/new.rs
  - cargo-pmcp/src/templates/openapi_server.rs
  - cargo-pmcp/src/templates/mod.rs
  - cargo-pmcp/tests/scaffold_openapi_server.rs
autonomous: true
requirements: [OAPI-07]
must_haves:
  truths:
    - "`cargo pmcp new --kind openapi-server` emits a single runnable crate"
    - "The generated config ships [code_mode] enabled=true + an inline DEV-ONLY token_secret with a replace-for-production note"
    - "The generated main.rs is the ≤15-line wiring shape (load config[+spec] → connector → ServerBuilderExt → serve HTTP)"
    - "Deploy parity holds — scaffolded deploy.toml uses target_type=pmcp-run; the Phase 77 target enum is unchanged"
  artifacts:
    - path: "cargo-pmcp/src/templates/openapi_server.rs"
      provides: "openapi-server scaffold emitter (Cargo.toml/main.rs/config.toml/api.yaml/deploy.toml)"
      contains: "openapi-server"
    - path: "cargo-pmcp/src/commands/new.rs"
      provides: "--kind openapi-server arm + execute/print helpers"
      contains: "openapi-server"
  key_links:
    - from: "cargo-pmcp/src/commands/new.rs"
      to: "cargo-pmcp/src/templates/openapi_server.rs"
      via: "execute_openapi_server → templates::openapi_server::generate"
      pattern: "openapi_server::generate"
---

<objective>
Add the `cargo pmcp new --kind openapi-server` scaffold (OAPI-07 / CF-3), mirroring
`--kind sql-server`: emit a single runnable crate with the ≤15-line wiring shape
(CF-5), a generated `config.toml` with `[code_mode] enabled=true` + inline DEV-ONLY
`token_secret` (CF-4), and deploy parity via a scaffolded `deploy.toml`
(`target_type=pmcp-run`) with the Phase 77 target enum UNCHANGED (CF-6). Also
document the new `--kind` surface in the cargo-pmcp README/help (scoped fold).

Purpose: Shape B/C/D for OpenAPI — the no-Rust on-ramp. A non-developer scaffolds,
edits config, runs, deploys.
Output: the scaffold arm + template + a `--no-run` scaffold compile test.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
@.planning/phases/90-openapi-built-in-server/90-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: openapi_server template emitter (CF-3/4/5/6)</name>
  <files>cargo-pmcp/src/templates/openapi_server.rs, cargo-pmcp/src/templates/mod.rs</files>
  <read_first>
    - CREATE cargo-pmcp/src/templates/openapi_server.rs; MODIFY cargo-pmcp/src/templates/mod.rs (declare the module).
    - ANALOG (exact — read first 60 lines): cargo-pmcp/src/templates/sql_server.rs — the `generate` orchestrator (sql_server.rs:33-44), one `generate_<file>` per file, raw-string emission (no template engine; `format!` escapes literal braces as `{{`/`}}`), the Cargo.toml features line (sql_server.rs:58), and `generate_deploy_toml` (CF-6).
    - ANALOG (≤15-line wiring): Phase 86 Plan 02 Shape C example — the ≤15-STATEMENT-line `main.rs` shape (hoisted asset-dir + HTTP boilerplate out of main; STATE.md Plan 86-02 decisions). The OpenAPI `main.rs` mirrors the `pmcp-openapi-server` example from Plan 06 (Plan 06 Task 3 `examples/openapi_server_min.rs`).
    - ANALOG (in-bundle secret posture for deploy): Phase 86 Plan 05 (`bundle_assets_if_configured` rewrites token_secret to `${CODE_MODE_SECRET}` for deploy; local keeps inline DEV secret; deploy.toml emitted to root + `.pmcp/`).
    - DECISION: CF-3 (single runnable crate), CF-4 (`[code_mode] enabled=true` + inline DEV token_secret + loud note + `allow_inline_token_secret_for_dev=true`), CF-5 (≤15-line wiring), CF-6 (deploy enum unchanged, detection-based, `target_type=pmcp-run`), D-03 (ship a minimal `api.yaml` for the scaffold-discovery story; spec optional at runtime).
  </read_first>
  <action>
    Create `templates/openapi_server.rs` mirroring `templates/sql_server.rs::generate` + per-file emitters. Emit a SINGLE runnable crate:
    - `Cargo.toml` — depend on `pmcp-server-toolkit` with the umbrella feature `["openapi-code-mode"]` (mirror sql_server.rs:58; drop `sqlite`; the umbrella composes `http` + `code-mode` + `pmcp-code-mode/js-runtime`, matching the Plan 90-06 binary dep so the scaffolded crate's script-tool/code-mode path compiles — RESEARCH Pitfall 4); `pmcp` with `streamable-http`; `tokio`, `tracing*`.
    - `src/main.rs` — the ≤15-line wiring (CF-5): load config[+optional spec] → `dispatch` → `ServerBuilderExt`/`build_server` → serve HTTP. Inline the `StreamableHttpServer` boilerplate via a private `serve()` helper (NOT `pmcp_openapi_server::serve`) so main stays ≤15 statement lines (mirror Plan 86-02 Pitfall §2).
    - `config.toml` — `[backend]` + a single-call AND a script `[[tools]]` (RESEARCH london-tube "Code Examples"), `[code_mode] enabled=true` + inline DEV `token_secret` + `allow_inline_token_secret_for_dev=true` + a LOUD "replace for production" comment (CF-4).
    - `api.yaml` — a minimal OpenAPI spec (scaffold-discovery story, D-03).
    - `deploy.toml` + `.pmcp/deploy.toml` — mirror `generate_deploy_toml` with `target_type = "pmcp-run"` (CF-6; no shape inference — `get_target_id` selects pmcp.run only by this).
    Declare `pub mod openapi_server;` in `templates/mod.rs`.
  </action>
  <acceptance_criteria>
    - `cargo-pmcp/src/templates/openapi_server.rs` exists with a `generate` orchestrator + per-file emitters; declared in `templates/mod.rs`.
    - The emitted `config.toml` string contains `enabled = true`, `allow_inline_token_secret_for_dev = true`, and a "replace for production" note (grep the template source).
    - The emitted `deploy.toml` string contains `target_type = "pmcp-run"`.
    - The emitted `Cargo.toml` string enables `pmcp-server-toolkit` with `openapi-code-mode` (grep the template source).
    - `cargo build -p cargo-pmcp` compiles.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cargo-pmcp && grep -q 'enabled = true' cargo-pmcp/src/templates/openapi_server.rs && grep -q 'allow_inline_token_secret_for_dev' cargo-pmcp/src/templates/openapi_server.rs && grep -qi 'replace.*production' cargo-pmcp/src/templates/openapi_server.rs && grep -q 'target_type.*pmcp-run' cargo-pmcp/src/templates/openapi_server.rs && grep -q 'openapi-code-mode' cargo-pmcp/src/templates/openapi_server.rs && echo PASS</automated>
  </verify>
  <done>The openapi-server template emits a single runnable crate with code_mode-enabled config (DEV-secret note), ≤15-line wiring, openapi-code-mode toolkit dep, and pmcp-run deploy.toml — asserted by the automated grep gate.</done>
</task>

<task type="auto">
  <name>Task 2: --kind openapi-server command arm + scaffold compile test (OAPI-07 / folded README)</name>
  <files>cargo-pmcp/src/commands/new.rs, cargo-pmcp/tests/scaffold_openapi_server.rs</files>
  <read_first>
    - MODIFY cargo-pmcp/src/commands/new.rs; CREATE cargo-pmcp/tests/scaffold_openapi_server.rs.
    - ANALOG (exact): the existing sql-server arm — `match kind.as_deref()` block at new.rs:69-73 (`Some("sql-server") => return execute_sql_server(...)`, the `Some(k) => bail!("unknown --kind '{}'; supported: sql-server", k)` error), `execute_sql_server` (new.rs:139-160), `print_sql_server_next_steps` (new.rs:162-191), and `validate_crate_name` (new.rs:109-133, blocks path-traversal).
    - ANALOG (scaffold test): cargo-pmcp/tests/scaffold_sql_server.rs (TEST-05) — compiles via `--no-run` + `[patch.crates-io]` writer for transitive unpublished deps (`scaffold_patch.rs` covers pmcp-code-mode, pmcp-widget-utils, pmcp-server-toolkit). STATE.md Phase 86 decisions: full cold execution deferred to the orchestrator; the test asserts the scaffold compiles.
    - DECISION: CF-3 (verbatim `--kind`), CONTEXT Folded Todo (document the new `--kind openapi-server` surface in cargo-pmcp README/help — SCOPED; broad rewrite stays Phase 89).
  </read_first>
  <action>
    Add an `Some("openapi-server") => return execute_openapi_server(&workspace_dir, &name, global_flags),` arm to the `match` at new.rs:69-73 and WIDEN the error message to `"supported: sql-server, openapi-server"`. Copy `execute_sql_server` → `execute_openapi_server` (validate name via `validate_crate_name`, `fs::create_dir_all(src)`, call `templates::openapi_server::generate`) and `print_sql_server_next_steps` → `print_openapi_server_next_steps` (adjust the run/deploy hints for the openapi flow). Update the cargo-pmcp README/`--help` text to list `openapi-server` as a supported `--kind` (scoped fold — do NOT do the broad rewrite, that's Phase 89).
    Create `tests/scaffold_openapi_server.rs` mirroring `scaffold_sql_server.rs`: scaffold into a tempdir, write the `[patch.crates-io]` block (reuse the existing `scaffold_patch.rs` / `append_crates_io_patch` helper covering the unpublished toolkit deps), and assert the scaffold compiles via `cargo build --no-run` (or `cargo check`). Gate full cold execution behind the orchestrator (mirror TEST-05). Add a golden-drift assertion that the emitted `main.rs` matches the ≤15-line Shape C wiring (mirror Plan 86-03 golden drift test).
  </action>
  <acceptance_criteria>
    - `grep -n "openapi-server" cargo-pmcp/src/commands/new.rs` shows the new arm + widened error message.
    - `cargo test -p cargo-pmcp scaffold_openapi -- --test-threads=1` exits 0 (scaffold compiles via --no-run / check).
    - The cargo-pmcp README/help lists `openapi-server` (grep the README).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cargo-pmcp scaffold_openapi -- --test-threads=1</automated>
  </verify>
  <done>`cargo pmcp new --kind openapi-server` emits + compiles a runnable crate; deploy parity (target enum unchanged); README documents the new surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scaffold input (crate name) → filesystem | user-supplied name becomes a directory path |
| generated config secret → deploy bundle | inline DEV secret must be rewritten for deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-08-01 | Tampering / path traversal | `--name` writing outside the workspace | mitigate | `validate_crate_name` blocks path-traversal before `fs::write` (copied from sql-server) |
| T-90-08-02 | Information Disclosure | inline DEV token_secret shipped to production | mitigate | generated config carries a LOUD replace-for-production note + `allow_inline_token_secret_for_dev`; deploy rewrites to a secrets ref (CF-4, Phase 86 Plan 05 posture) |
| T-90-08-03 | correctness | deploy shape inference picking wrong target | mitigate | `target_type = "pmcp-run"` is explicit in the scaffolded deploy.toml; Phase 77 target enum unchanged, detection-based (CF-6) |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp scaffold_openapi -- --test-threads=1` green (scaffold compiles).
- `cargo build -p cargo-pmcp` green; README lists the new `--kind`.
- Generated config is code_mode-enabled with a DEV secret note; deploy.toml is pmcp-run; toolkit dep is openapi-code-mode.
</verification>

<success_criteria>
- `cargo pmcp new --kind openapi-server` emits a single runnable, compiling crate (OAPI-07 / CF-3).
- Generated config: `[code_mode] enabled=true` + inline DEV token_secret + note (CF-4); ≤15-line wiring (CF-5).
- Deploy parity: scaffolded `target_type=pmcp-run`, Phase 77 target enum unchanged (CF-6); README documents the new `--kind` (scoped fold).
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-08-SUMMARY.md`
</output>
