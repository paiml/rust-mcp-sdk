---
phase: 90-openapi-built-in-server
plan: 07
type: execute
wave: 6
depends_on: [90-06]
files_modified:
  - crates/pmcp-openapi-server/tests/parity_replay.rs
  - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml
  - crates/pmcp-openapi-server/tests/fixtures/london-tube-api.yaml
  - crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml
autonomous: true
requirements: [OAPI-08]
must_haves:
  truths:
    - "The london-tube instance reproduces the reference server's tools + behavior, replayed offline"
    - "The api_key query-parameter outgoing-auth path is exercised end-to-end"
    - "Outbound auth receives the RESOLVED ${TFL_APP_KEY} value (app_key=dummy in the request), not the literal ${...}"
    - "The parity replay uses wiremock (pure-Rust, no Docker, no live network in default CI)"
    - "A live api.tfl.gov.uk replay exists but is env-gated (#[ignore])"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/parity_replay.rs"
      provides: "wiremock-backed london-tube parity assertion + secret-expansion assertion + env-gated live test"
      contains: "parity"
    - path: "crates/pmcp-openapi-server/tests/fixtures/london-tube.toml"
      provides: "vendored london-tube config (api_key query-param auth) with reference-provenance comments"
      contains: "api_key"
  key_links:
    - from: "crates/pmcp-openapi-server/tests/parity_replay.rs"
      to: "pmcp_openapi_server::run_serving"
      via: "drives the real binary pipeline against wiremock"
      pattern: "run_serving"
---

<objective>
Reproduce the london-tube (TfL) reference instance and assert parity (OAPI-08 / D-04):
the binary serves the same tools + behavior as the pmcp-run reference, replayed
OFFLINE via wiremock (pure-Rust, no Docker, no live network in default CI). This is
the single most valuable parity target — it exercises the new `api_key`
query-parameter outgoing-auth path. A live api.tfl.gov.uk replay is env-gated.

Purpose: Proves the lift behaves identically to the reference for the canonical demo.
Output: vendored london-tube fixtures + the wiremock parity test + the gated live test.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
@.planning/phases/90-openapi-built-in-server/90-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the london-tube fixtures (config + spec + scenarios) with provenance</name>
  <files>crates/pmcp-openapi-server/tests/fixtures/london-tube.toml, crates/pmcp-openapi-server/tests/fixtures/london-tube-api.yaml, crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/tests/fixtures/{london-tube.toml, london-tube-api.yaml, london-tube-scenarios.yaml}.
    - SOURCE-OF-TRUTH (vendor from): `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/instances/london-tube.toml` and `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/servers/london-tube/` (the reference tools + expected output). RESEARCH "Code Examples" shows the london-tube config shape (`[backend] base_url=https://api.tfl.gov.uk`, `[backend.auth] type="api_key" query_params={app_key="${TFL_APP_KEY}"}`, a single-call `get-tube-status` + a script `disrupted-lines-with-detail`).
    - ANALOG (vendoring discipline): Phase 85 Plan 03 vendored self-contained parity fixtures into the SDK repo under `tests/fixtures/` with `exclude = [tests/]` keeping them out of the published crate (STATE.md Plan 85-03 decisions).
    - DECISION: D-04 (london-tube ONLY; api_key query-param auth; offline wiremock), CF-8 (two-input model — config + spec).
  </read_first>
  <action>
    Vendor a self-contained `london-tube.toml` adapted to the toolkit `[backend]` config shape (NOT the reference's `shared/mcp-server-common` shape): `[backend]`, `[backend.auth] type="api_key"` with `query_params = { app_key = "${TFL_APP_KEY}" }` and `required = false`, `[backend.http]`, at least one single-call `[[tools]]` (`get-tube-status`) AND one script `[[tools]]` (`disrupted-lines-with-detail` with `[[tools.parameters]] maxLines`), plus `[code_mode] enabled = true` with an inline DEV-ONLY `token_secret` + `allow_inline_token_secret_for_dev = true` (CF-4). Vendor a minimal `london-tube-api.yaml` OpenAPI spec covering the operations the tools call. Vendor `london-tube-scenarios.yaml` (the mcp-tester `TestScenario` set) capturing the reference's expected tool list + tool outputs.
    PROVENANCE (Codex MEDIUM — capture it to prevent drift): add a header comment to EACH vendored fixture recording the reference source path AND the pmcp-run commit/date it was vendored from (e.g. `# vendored from ~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/instances/london-tube.toml @ <commit-or-date>`), so a future reviewer can diff against the reference.
    Ensure `exclude` in the crate Cargo.toml keeps `tests/` out of the published crate (mirror sql-server). Keep fixtures small + offline-deterministic (no real secrets — the `${TFL_APP_KEY}` is a placeholder resolved to a dummy in tests).
    Coverage: a fixture-validity test (`london_tube_fixture`) that parses `london-tube.toml` via `ServerConfig::from_toml_strict_validated` and parses `london-tube-api.yaml` via `OpenApiSchema::parse`, asserting both succeed and the expected tool names are present.
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/tests/fixtures/london-tube.toml` contains `type = "api_key"`, a `script = """..."""` tool, and a `# vendored from ...` provenance comment.
    - `cargo test -p pmcp-openapi-server london_tube_fixture -- --test-threads=1` exits 0 (config + spec parse, tool names present).
    - `cargo package -p pmcp-openapi-server --list 2>/dev/null` does NOT list `tests/fixtures/*` (exclude works).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server london_tube_fixture -- --test-threads=1</automated>
  </verify>
  <done>Self-contained london-tube fixtures vendored (config + spec + scenarios) with reference provenance; parse-valid; publish-excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Wiremock parity replay + secret-expansion assertion + env-gated live replay (OAPI-08 / D-04)</name>
  <files>crates/pmcp-openapi-server/tests/parity_replay.rs</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/tests/parity_replay.rs (Wave-0 file from 90-VALIDATION.md).
    - ANALOG (parity harness): crates/pmcp-sql-server `tests/parity_chinook.rs` — drives the REAL binary path (`run_serving` testable seam, Codex HIGH #5), per-step `StepResult.success` gating, `ChildGuard`/readiness-poll spawn pattern, mcp-tester `TestScenario` replay. Adapt: spawn against a wiremock backend instead of a vendored DB. Use a BOUNDED shutdown guard (abort the handle at test end — no leaked server, per Plan 06).
    - ANALOG (env-gating a live test): Phase 84 Athena `#[ignore]` + env-var gate; Phase 85 dispatch offline-safety (explicit region stops IMDS). Here: gate live replay behind `PMCP_OPENAPI_LIVE_TEST=1` + `TFL_APP_KEY` (90-VALIDATION.md Manual-Only row).
    - SECRET EXPANSION (Codex MEDIUM — the assertion to add): the toolkit resolves `${TFL_APP_KEY}` via Phase-83 secrets/`${ENV}` expansion BEFORE outbound auth applies it. The test sets `TFL_APP_KEY=dummy` in the process env and asserts the wiremock backend RECEIVES `app_key=dummy` in the query string — NOT the literal `${TFL_APP_KEY}`. This proves the expansion path end-to-end (Plan 01 applies the resolved value; Plan 02/Phase 83 resolves it).
    - DECISION: D-04 (wiremock default, live env-gated), Pitfall — api_key in QUERY param is the path being proven; required=false behavior (Codex LOW — omitted when unset, present when set).
  </read_first>
  <action>
    Write `parity_replay.rs`: stand up a `wiremock::MockServer` mounting the london-tube backend responses (a `/Line/Mode/tube/Status` list + per-line `/Line/{id}/Disruption`). Mount the matchers to REQUIRE the `app_key` QUERY param with value `dummy` (proves api_key query-param outgoing auth AND secret expansion, D-04). Set `TFL_APP_KEY=dummy` in the test process env, point `london-tube.toml`'s `[backend] base_url` at the mock (override at test time). Drive the REAL binary pipeline via `pmcp_openapi_server::run_serving`, then replay `london-tube-scenarios.yaml` through `mcp-tester` against the bound address, gating on per-step `StepResult.success`. Assert the tool list + tool outputs match the reference scenarios (parity). Add an EXPLICIT assertion that the recorded wiremock request query contained `app_key=dummy` and NOT the substring `${TFL_APP_KEY}` (secret-expansion proof).
    Add a `required=false` behavior assertion (Codex LOW): with `required=false` and the api_key SET (`dummy`), it is present in the request; document/assert the unset case is handled (omitted when the env var resolves empty) per the chosen `required=false` semantics from Plan 01.
    Add an `#[ignore]`-d live test `parity_live_tfl` gated on `PMCP_OPENAPI_LIVE_TEST=1` + a real `TFL_APP_KEY` that runs the same scenarios against `https://api.tfl.gov.uk` (early-return if the env gate is unset, mirror Phase 84/86 double-gate).
    Run under `--test-threads=1` (spawn+replay harness convention); abort the server handle at the end of each test (bounded shutdown).
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/tests/parity_replay.rs` contains the wiremock `parity` test asserting the `app_key=dummy` query param (and asserting the literal `${TFL_APP_KEY}` is NOT present) and an `#[ignore]` `parity_live_tfl`.
    - `grep -c "app_key=dummy\|\"app_key\"" crates/pmcp-openapi-server/tests/parity_replay.rs` ≥ 1 (secret-expansion assertion present).
    - `cargo test -p pmcp-openapi-server parity -- --test-threads=1` exits 0 (offline wiremock replay; live test skipped).
    - `grep -c "PMCP_OPENAPI_LIVE_TEST" crates/pmcp-openapi-server/tests/parity_replay.rs` ≥ 1 (live gate present).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server parity -- --test-threads=1</automated>
  </verify>
  <done>London-tube parity proven offline via wiremock through the real binary path; api_key query-param auth + ${TFL_APP_KEY} resolution to app_key=dummy proven; required=false behavior asserted; live replay env-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mock backend | replay traffic carries the api_key query param |
| live gate → real TfL API | only when explicitly env-enabled with a real key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-07-01 | Information Disclosure | a real TFL_APP_KEY committed in fixtures | mitigate | fixtures use `${TFL_APP_KEY}` placeholder + a dummy in tests; live key only from env at runtime; never vendored |
| T-90-07-02 | reliability | live network flake in CI | mitigate | live replay double-gated (`#[ignore]` + `PMCP_OPENAPI_LIVE_TEST`); default CI is wiremock-only (D-04, no Docker) |
| T-90-07-03 | correctness | parity drift vs reference | mitigate | scenario replay gates per-step `StepResult.success`; tool list + outputs asserted against vendored reference scenarios; fixtures carry reference provenance |
| T-90-07-04 | correctness | unresolved `${ENV}` secret reaching the backend literally | mitigate | secret-expansion assertion: backend receives `app_key=dummy`, never `${TFL_APP_KEY}` |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server parity london_tube_fixture -- --test-threads=1` green (offline).
- No real secrets in fixtures; live replay env-gated; api_key query-param path + secret expansion (`app_key=dummy`) exercised; required=false behavior asserted; fixtures carry provenance.
</verification>

<success_criteria>
- The london-tube instance reproduces the reference tools + behavior, replayed offline via wiremock (OAPI-08 / D-04).
- The api_key query-parameter outgoing-auth path is proven end-to-end, with `${TFL_APP_KEY}` RESOLVED to `app_key=dummy` in the request (not the literal `${...}`).
- A live api.tfl.gov.uk replay exists but is env-gated.
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-07-SUMMARY.md`
</output>
</content>
