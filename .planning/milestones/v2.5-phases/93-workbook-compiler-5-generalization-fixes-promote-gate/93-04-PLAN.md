---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 04
type: execute
wave: 3
depends_on: ["93-02", "93-03"]
files_modified:
  - crates/pmcp-workbook-compiler/src/manifest/mod.rs
  - crates/pmcp-workbook-compiler/src/manifest/synth.rs
  - crates/pmcp-workbook-compiler/src/manifest/ratify.rs
  - crates/pmcp-workbook-compiler/src/manifest/projections.rs
  - crates/pmcp-workbook-compiler/src/manifest/model.rs
  - crates/pmcp-workbook-compiler/src/reconcile/mod.rs
  - crates/pmcp-workbook-compiler/src/reconcile/classifier.rs
  - crates/pmcp-workbook-compiler/src/reconcile/drift.rs
  - crates/pmcp-workbook-compiler/src/sheet_ir/mod.rs
  - crates/pmcp-workbook-compiler/src/sheet_ir/eval_bridge.rs
autonomous: true
requirements: [WBCO-02, WBCO-04, WBCO-06]

# WIRING NOTE: 93-04 is the first plan that consumes BOTH 93-02 (WorkbookMap + cached oracle) and
# 93-03 (CellSource-based linter/parser/DAG). This is where WorkbookMap implements the CellSource
# trait introduced in 93-03 (the seam that kept 93-02 and 93-03 parallel). The manifest synth and
# reconcile both read the real WorkbookMap here.

must_haves:
  truths:
    - "The semantic manifest is synthesized fully workbook-driven from colour/Guide/headers (no per-workbook Rust, no build_reference_manifest)"
    - "WorkbookMap implements the 93-03 CellSource trait so the linter/parser run on the real workbook"
    - "BA-authored cell metadata (meaning/unit/enum labels) is length-capped + sanitized to documented limits before entering the manifest"
    - "Inline DV literal lists (≤10) become closed JSON-Schema enums; range/named-range DV sources fall back to a dynamic input with a precise reason-code WARNING (never a block), with schema-valid defaults that do not conflict with WR-01"
    - "Unclassifiable cells stay in the computation but are NOT exposed; a warning fires only when a cell looks like it should be exposed"
    - "Penny-reconcile grades compiled IR vs the cached oracle under operand-anchored rounding — never a naïve delta.abs() tolerance; cached text/bool/blank/Excel-error cells are handled"
    - "A named-output mismatch is an ERROR (blocks emit); a helper-cell mismatch is a located WARNING; O-1 pure-Rust reconcile parity is a NAMED test suite"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/manifest/synth.rs"
      provides: "Colour/Guide/header → roles synthesis with metadata length-caps (WBCO-02/06)"
      contains: "synthesize"
    - path: "crates/pmcp-workbook-compiler/src/reconcile/classifier.rs"
      provides: "Operand-anchored reconcile (no delta.abs())"
      contains: "BOUNDARY_EPSILON"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/manifest/synth.rs"
      to: "pmcp_workbook_runtime::Manifest"
      via: "emits the re-exported Manifest with annotations field"
      pattern: "Manifest"
    - from: "crates/pmcp-workbook-compiler/src/reconcile/classifier.rs"
      to: "pmcp_workbook_runtime::sheet_ir::rounding"
      via: "operand-anchored rounding helpers"
      pattern: "excel_round|excel_roundup|excel_ceiling"
---

<objective>
Lift manifest synthesis (colour/Guide/header → roles + ratification, WBCO-02/06) and
penny-reconcile (operand-anchored, WBCO-04), wiring them to the runtime's re-exported
`Manifest`/`rounding` types and to the real `WorkbookMap` (which implements the 93-03
`CellSource` trait here). Reconcile the `Manifest.annotations` struct delta (the in-repo
runtime has a field the lighthouse lacks). Honor the D-02/D-03/D-05/D-06 tolerance rules
and apply explicit metadata length-caps on BA strings.

Purpose: This is the heart of the "stay-in-Excel" generalization. The manifest must be
synth-driven (no per-workbook Rust). Every `Manifest { … }` literal must add
`annotations: vec![]` or the byte-identical re-emit fails. BA cell strings reach the agent
LLM through the manifest, so they must be length-capped + sanitized. The reconcile
classifier must be grep-gate-clean of `delta.abs()` — a naïve abs-delta tolerance is the
milestone's named trap — and must handle non-numeric cached cells. O-1 pure-Rust reconcile
parity (no SWC) becomes a named test suite, not a summary note.
Output: manifest/, reconcile/, sheet_ir/ (compiler-side eval bridge only) modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/manifest/{synth,ratify,projections,model,mod}.rs (1006 LOC synth)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/reconcile/{classifier,drift,mod}.rs
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/sheet_ir/{eval_bridge,executor,mod}.rs (NOT loop_exec.rs — deferred)

These modules already exist as compilable stubs (Plan 01 Task 3) — fill the stub bodies.

In-repo re-export TARGET struct (read in full — every Manifest literal must match it):
- crates/pmcp-workbook-runtime/src/manifest_model.rs (lines ~290-327): Manifest fields are
  schema_version, workflow, workbook_hash, ratified, ratified_by, ratified_at, cells (Vec<CellRole>),
  loop_block, governed_data, changelog, capability_calls, and annotations: Vec<AnnotationDecl>
  (serde-default; NOT in the lighthouse Manifest — every synth literal MUST supply annotations: vec![]).
  Also re-export Role (Input/Constant/Output/Formula), Dtype (Number/Text/Bool), InputTier — do NOT redefine.
- crates/pmcp-workbook-runtime/src/sheet_ir/rounding.rs — excel_round/excel_roundup/excel_ceiling (anchors).
- crates/pmcp-workbook-runtime/src/sheet_ir/ executor — reuse; do not re-declare the executor types.
- crates/pmcp-workbook-compiler/src/ingest/ (Plan 02) WorkbookMap + cached oracle; src/formula + src/dag (Plan 03) over the CellSource seam.

## BA-string metadata limits (pre-decided — Codex MEDIUM; sanitize BEFORE the string enters the Manifest)
- MAX_MEANING_LEN = 280 chars; MAX_UNIT_LEN = 32; MAX_ENUM_LABEL_LEN = 64; MAX_INPUT_COUNT = 64;
  MAX_OUTPUT_COUNT = 64; MAX_SHEET_NAME_LEN = 64.
- Sanitization: strip control characters and collapse whitespace; truncate at the cap with an ellipsis;
  a length overflow emits a WARNING (never a hard block — keep the BA in Excel). These caps are the
  info-flow boundary for strings reaching the agent LLM.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift manifest synth + ratify; wire WorkbookMap→CellSource; reconcile annotations + cap BA strings (WBCO-02/06)</name>
  <files>crates/pmcp-workbook-compiler/src/manifest/mod.rs, crates/pmcp-workbook-compiler/src/manifest/synth.rs, crates/pmcp-workbook-compiler/src/manifest/ratify.rs, crates/pmcp-workbook-compiler/src/manifest/projections.rs, crates/pmcp-workbook-compiler/src/manifest/model.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/manifest/{synth,ratify,projections,model,mod}.rs (colour/Guide/header heuristics + DV resolution + ratify sign-off)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (lines 290-327 the TARGET Manifest struct with annotations; Role/Dtype/InputTier/CellRole/AnnotationDecl to re-export)
    - crates/pmcp-workbook-compiler/src/dialect/ + src/formula/ (Plan 03 — the CellSource trait WorkbookMap must implement here)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/manifest/ — the annotations reconciliation site, lines 100-122)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md (D-04 stay-in-Excel + recorded ratification; D-05 unclassifiable→internal + selective warning; D-06 inline-DV-only enums)
    - This plan's <interfaces> (BA-string metadata limits — implement exactly)
  </read_first>
  <behavior>
    - Test workbookmap_implements_cellsource: WorkbookMap (Plan 02) implements the 93-03 CellSource trait so the linter/parser run on the real workbook (the wiring seam).
    - Test synth_inline_dv_becomes_enum: an inline DV list (formula1 quoted literals, ≤10) becomes a closed JSON-Schema enum.
    - Test synth_range_dv_falls_back: a DV sourced from a cell range / named range becomes a DYNAMIC input with a precise reason-code WARNING (never a block); its default value is schema-valid and does NOT later conflict with WR-01 enum-tiering — D-06.
    - Test synth_unclassifiable_internal: a cell the compiler can't confidently classify stays in the computation but is NOT exposed as input/output; a warning fires ONLY when it looks exposable (e.g. a bare hardcoded number) — D-05.
    - Test manifest_has_annotations_field: every synthesized Manifest carries the annotations field (vec![] when no Guide annotations) and the ratified/ratified_by/ratified_at sign-off fields — Pitfall 2 / D-04.
    - Test ba_string_metadata_capped: an over-long meaning/unit/enum label is truncated to the documented cap (MAX_MEANING_LEN/MAX_UNIT_LEN/MAX_ENUM_LABEL_LEN), control chars stripped, and a WARNING emitted — never a hard block.
  </behavior>
  <action>
    Fill the manifest/ stub (Plan 01). Lift manifest/{synth,ratify,projections,model,mod}.rs. Re-export
    the runtime Manifest/CellRole/Role/Dtype/InputTier/AnnotationDecl — do NOT re-declare a local Manifest.
    At EVERY hand-built Manifest { … } literal site, supply annotations: vec![] (or synthesize from a
    Guide-annotation convention) plus ratified/ratified_by/ratified_at — the lighthouse struct lacks
    annotations and will not compile against the in-repo struct otherwise. Implement WorkbookMap's
    CellSource impl here (the seam from 93-03). DV resolution (WBCO-06): inline literal lists ≤10 →
    closed enum; range/named-range source → dynamic input + precise reason-code WARNING with a
    schema-valid default that does not conflict with WR-01 (D-06). Unclassifiable cells (D-05): keep
    internal, warn only when exposable. Apply the BA-string metadata limits from <interfaces>
    (length-cap + sanitize meaning/unit/enum labels + input/output/sheet-name counts) BEFORE the string
    enters the Manifest. Ratify = recorded sign-off (approver + date), D-04. Scrub all customer
    identifiers; lift the synth tests, scrubbed, plus the six behavior tests.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler manifest:: 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler manifest::` passes including the six behavior tests.
    - No local Manifest re-declaration: `! grep -rnE 'pub (struct) Manifest\b' crates/pmcp-workbook-compiler/src/manifest/`
    - WorkbookMap implements CellSource: `grep -rn 'impl.*CellSource.*for.*WorkbookMap\|impl CellSource' crates/pmcp-workbook-compiler/src/` returns a match.
    - Every Manifest literal supplies annotations (a synth test serializes a manifest and asserts the annotations key handling).
    - BA-string caps applied: `grep -qE 'MAX_MEANING_LEN|MAX_UNIT_LEN|MAX_ENUM_LABEL_LEN' crates/pmcp-workbook-compiler/src/manifest/` and ba_string_metadata_capped passes.
    - build_reference_manifest absent on non-test paths: `! grep -rn 'build_reference_manifest' crates/pmcp-workbook-compiler/src/ --include='*.rs' | grep -v test`
    - Zero customer identifiers in manifest/.
  </acceptance_criteria>
  <done>manifest synth+ratify lifted + scrubbed; WorkbookMap→CellSource wired; annotations/ratification reconciled; BA-string caps applied; inline-DV enums + range-DV warning (WR-01-safe defaults) + unclassifiable-internal honored; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift penny-reconcile — operand-anchored, no delta.abs(), non-numeric cells, named-output ERROR / helper WARNING, O-1 parity suite (WBCO-04)</name>
  <files>crates/pmcp-workbook-compiler/src/reconcile/mod.rs, crates/pmcp-workbook-compiler/src/reconcile/classifier.rs, crates/pmcp-workbook-compiler/src/reconcile/drift.rs, crates/pmcp-workbook-compiler/src/sheet_ir/mod.rs, crates/pmcp-workbook-compiler/src/sheet_ir/eval_bridge.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/reconcile/{classifier,drift,mod}.rs (lines 14-24 the operand-anchored invariant doc; imports of runtime rounding)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/sheet_ir/{eval_bridge,executor,mod}.rs (compiler-side eval bridge; DO NOT lift loop_exec.rs)
    - crates/pmcp-workbook-runtime/src/sheet_ir/rounding.rs (excel_round/excel_roundup/excel_ceiling anchors to repoint)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pattern 2 operand-anchored; O-1 SWC drop — verify pure-Rust reconcile parity)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md (D-03 named-output ERROR vs helper WARNING)
  </read_first>
  <behavior>
    - Test reconcile_rounding_boundary_ok: a computed-vs-cached divergence within one rounding step of a ROUND/ROUNDUP/CEILING boundary (operand within BOUNDARY_EPSILON) is ACCEPTED.
    - Test reconcile_named_output_mismatch_errors: a non-rounding divergence on a NAMED OUTPUT is an ERROR that blocks emit (D-03).
    - Test reconcile_helper_mismatch_warns: the same divergence on an intermediate/helper cell is a located WARNING (not a block) (D-03).
    - Test no_blanket_tolerance: a large divergence NOT anchored to a rounding op is rejected (never forgiven by a blanket epsilon).
    - Test reconcile_non_numeric_cached: cached cells holding TEXT, BOOL, BLANK, and Excel-error values (#REF!/#DIV/0!) are compared correctly (exact for text/bool/blank; error-cell handling documented) — never a panic on a non-numeric cached value (Codex MEDIUM).
    - Test o1_parity_suite (NAMED suite): a representative set of formulas reconciles via pure-Rust scalar_eval + sheet_ir executor with NO SWC/js oracle — this suite IS the O-1 parity proof (not just a summary note).
  </behavior>
  <action>
    Fill the reconcile/ + sheet_ir/ stubs (Plan 01). Lift reconcile/{classifier,drift,mod}.rs and the
    compiler-side sheet_ir/{eval_bridge,mod}.rs (the executor + rounding TYPES come from the runtime —
    re-export, do NOT re-declare; do NOT lift loop_exec.rs / RoomAggregator — deferred). Repoint the
    rounding imports workbook_runtime::sheet_ir::rounding::{…} → pmcp_workbook_runtime::sheet_ir::rounding::{…}.
    The classifier stays operand-anchored: a divergence is acceptable ONLY when the deciding cell is a
    ROUND/ROUNDUP/CEILING op, the operand is within BOUNDARY_EPSILON (1e-6) of the boundary, and the
    divergence is ≤ one rounding step — a `delta.abs() < X` branch is FORBIDDEN. Handle non-numeric
    cached cells (text/bool/blank/Excel-error) without panicking. D-03 split: named-output mismatch =
    ERROR (blocks emit), helper-cell mismatch = located WARNING. Implement O-1 as a NAMED test suite
    (o1_parity_suite): confirm pure-Rust scalar_eval + sheet_ir executor reconciles without the SWC/js
    oracle (do NOT add pmcp-code-mode); document the parity result in the SUMMARY referencing the named
    suite. Scrub all customer identifiers; lift reconcile tests, scrubbed, plus the six behavior tests;
    add the delta.abs() grep-gate test.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler reconcile:: o1_parity 2>&1 | tail -6 && ! grep -n 'delta.abs()' crates/pmcp-workbook-compiler/src/reconcile/classifier.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler reconcile::` passes including the six behavior tests (incl. reconcile_non_numeric_cached and the named o1_parity_suite).
    - GREP GATE: `! grep -n 'delta.abs()' crates/pmcp-workbook-compiler/src/reconcile/classifier.rs` returns nothing.
    - Classifier imports the runtime rounding anchors: `grep -q 'pmcp_workbook_runtime::sheet_ir::rounding' crates/pmcp-workbook-compiler/src/reconcile/classifier.rs`
    - No pmcp-code-mode/SWC dep added (O-1 parity confirmed by the named suite): `! grep -q 'pmcp-code-mode' crates/pmcp-workbook-compiler/Cargo.toml`
    - loop_exec.rs NOT lifted: `! test -f crates/pmcp-workbook-compiler/src/sheet_ir/loop_exec.rs`
    - Zero customer identifiers in reconcile/ and sheet_ir/.
  </acceptance_criteria>
  <done>operand-anchored reconcile lifted + scrubbed; delta.abs() grep-gate clean; non-numeric cached cells handled; named-output ERROR vs helper WARNING honored; SWC dropped with parity confirmed by the named o1_parity_suite; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA-authored cell strings (meaning/unit/enum) → synthesized manifest → served schema → agent LLM | prompt-injection info-flow |
| compiled IR vs cached oracle | a wrong published answer must block, not be forgiven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04-INJ | Tampering (info-flow) | BA cell strings reaching the agent LLM through the manifest | mitigate | Length-cap (MAX_MEANING_LEN/MAX_UNIT_LEN/MAX_ENUM_LABEL_LEN) + control-char strip + sanitize cell meaning/unit/enum metadata before it enters the manifest (Task 1); document that BA strings reach the agent |
| T-93-04-TOL | Tampering | a naïve blanket abs-delta tolerance forgiving a genuinely wrong output | mitigate | Operand-anchored classifier; delta.abs() grep gate; named-output mismatch = ERROR (Task 2, WBCO-04/D-03) |
| T-93-04-ENUM | Input Validation | a range/named-range DV silently treated as a closed enum (over-promising the schema) or a default conflicting with WR-01 | mitigate | Inline-DV-only enums; range-DV → dynamic input + reason-code warning + WR-01-safe default (Task 1, WBCO-06/D-06) |
| T-93-04-PANIC | Denial of Service | a non-numeric cached cell (text/bool/blank/error) crashing the reconcile classifier | mitigate | Non-numeric cached cells handled without panic; reconcile_non_numeric_cached test (Task 2) |
</threat_model>

<verification>
- manifest + reconcile tests green; WorkbookMap→CellSource wired; annotations reconciled; BA-string caps applied; delta.abs() grep gate clean; non-numeric cached cells handled.
- O-1 parity (pure-Rust reconcile, no SWC) confirmed by the named o1_parity_suite and documented.
- Zero customer identifiers; no Manifest/Expr re-declaration; loop_exec not lifted.
</verification>

<success_criteria>
- WBCO-02: fully synth-driven manifest (no per-workbook Rust), annotations + ratification reconciled, BA-string caps applied.
- WBCO-06: inline-DV enums; range-DV → dynamic-input warning with reason code + WR-01-safe default.
- WBCO-04: operand-anchored reconcile, delta.abs() grep-gate clean, non-numeric cells handled, named-output ERROR / helper WARNING, O-1 parity as a named suite.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-04-SUMMARY.md` when done
</output>
</content>
