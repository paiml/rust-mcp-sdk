---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 03
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - crates/pmcp-workbook-compiler/src/dialect/mod.rs
  - crates/pmcp-workbook-compiler/src/dialect/linter.rs
  - crates/pmcp-workbook-compiler/src/formula/mod.rs
  - crates/pmcp-workbook-compiler/src/formula/token.rs
  - crates/pmcp-workbook-compiler/src/formula/parser.rs
  - crates/pmcp-workbook-compiler/src/formula/rebase.rs
  - crates/pmcp-workbook-compiler/src/dag/mod.rs
  - crates/pmcp-workbook-compiler/src/dag/graph.rs
  - crates/pmcp-workbook-compiler/src/dag/resolve.rs
  - crates/pmcp-workbook-compiler/src/dag/topo.rs
  - crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_formula_parser.rs
autonomous: true
requirements: [WBDL-03, WBCO-03]

# DEPENDENCY NOTE (resolves Codex HIGH hidden 93-03 → 93-02 dependency): 93-03 depends ONLY on
# 93-01. It does NOT consume 93-02's WorkbookMap. The dialect linter, formula parser, and DAG are
# tested against SYNTHETIC in-memory inputs (hand-built cell maps / formula strings / a minimal
# CellSource trait satisfied by a test double), NOT 93-02's umya-produced WorkbookMap. This keeps
# 93-02 and 93-03 genuinely parallel in Wave 2. The wiring of the real WorkbookMap into the
# linter/parser happens in Plan 04 (manifest/reconcile, which depends on BOTH 93-02 and 93-03) and
# Plan 07 (driver). If a synthetic test double proves impossible for any sub-component, fall back to
# declaring depends_on: ["93-01","93-02"] and moving this plan to Wave 3 — but the parser/DAG/linter
# are pure functions over cell maps, so the synthetic-input path is expected to hold.

must_haves:
  truths:
    - "The dialect linter runs against pmcp-workbook-dialect::WHITELIST + DialectRules, collect-all, located (cell-addressed), with repair guidance, over a SYNTHETIC cell-source test double (no 93-02 dependency)"
    - "An out-of-whitelist function is rejected AT PARSE TIME (not a post-parse filter)"
    - "The formula parser builds runtime Expr and the DAG topo-sorts via Kahn (no petgraph)"
    - "The formula parser never panics on attacker-controlled formula bytes and enforces a recursion-depth limit BEFORE fuzzing"
    - "A documented supported-formula matrix states which Excel formula constructs are supported vs rejected, tied to the dialect WHITELIST"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/dialect/linter.rs"
      provides: "Running WBDL-03 linter over a cell source against the dialect contract"
      contains: "WHITELIST"
    - path: "crates/pmcp-workbook-compiler/src/formula/parser.rs"
      provides: "Pratt parser with whitelist-at-parse-time rejection + recursion-depth limit"
      contains: "Expr"
    - path: "crates/pmcp-workbook-compiler/src/dag/topo.rs"
      provides: "Kahn toposort over the formula DAG"
    - path: "crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_formula_parser.rs"
      provides: "Fuzz target over the untrusted formula parser"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/dialect/linter.rs"
      to: "pmcp_workbook_dialect::WHITELIST"
      via: "re-exported dialect contract"
      pattern: "WHITELIST|DialectRules"
    - from: "crates/pmcp-workbook-compiler/src/formula/parser.rs"
      to: "pmcp_workbook_runtime::Expr"
      via: "re-exported IR type"
      pattern: "Expr"
---

<objective>
Lift the dialect-linter execution (WBDL-03 — re-mapped here from Phase 91), the
formula parser (Pratt, whitelist-AT-PARSE-time — the dialect security primitive), and
the DAG compile (Kahn toposort, no petgraph) — WBCO-03. The linter runs against the
already-shipped `pmcp-workbook-dialect` WHITELIST/DialectRules contract; the parser
builds the runtime's `Expr` IR. All three are tested against SYNTHETIC in-memory inputs
(not 93-02's WorkbookMap) to keep this plan parallel with 93-02. Fuzz the untrusted
formula parser behind a recursion-depth limit.

Purpose: This is the parse-and-analyze middle of the pipeline. The whitelist-at-parse-time
gate is THE core security property (an out-of-whitelist function must be a parse-time
REJECTION, never a silent accept). The linter must consume the dialect crate's contract
directly — re-declaring WHITELIST would defeat the dialect crate's drift-protection test.
Output: dialect/, formula/, dag/ modules + a supported-formula matrix + the formula-parser
fuzz target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dialect/{linter,finding,rules,mod}.rs (596 LOC linter)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/formula/{token,parser,rebase,mod}.rs (Pratt, whitelist-at-parse)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dag/{graph,resolve,topo,mod}.rs (Kahn)

These modules already exist as compilable stubs (Plan 01 Task 3) — fill the stub bodies.

In-repo contract + re-export source:
- crates/pmcp-workbook-dialect/src/lib.rs — WHITELIST (13-fn slice), DialectRules, CandidateRole
  (the lighthouse dialect/rules.rs re-declares these — DELETE the local copies, re-export from
  pmcp_workbook_dialect; the dialect crate's rules.rs:248-336 spec-binding test asserts the
  WHITELIST cannot drift, a second copy would defeat it).
- crates/pmcp-workbook-runtime/src/finding.rs — LintFinding/LintReport/Severity (re-export; the
  linter emits these collect-all located findings — D-01).
- crates/pmcp-workbook-runtime/src/formula.rs — Expr/BinOp/UnOp/CellValue (the parser BUILDS these — re-export, do not re-declare).
- crates/pmcp-workbook-runtime/src/dag.rs + resolve.rs — Dag/toposort (re-use).

## Synthetic cell source (decouples 93-03 from 93-02's WorkbookMap)
Define a minimal `trait CellSource { fn formula_at(&self, addr) -> Option<&str>; fn iter_cells(&self) -> ... }`
(or reuse the lighthouse's narrowest cell-iteration interface). The linter/parser/DAG consume `&impl CellSource`.
93-02's WorkbookMap will implement CellSource in Plan 04 (the wiring plan); HERE the tests use a hand-built
`TestCells` double. This is the seam that keeps 93-02 and 93-03 parallel.

## Supported-formula matrix (document explicitly — Codex MEDIUM)
List, tied to pmcp-workbook-dialect::WHITELIST, which constructs the parser SUPPORTS vs REJECTS:
sheet-qualified refs, quoted sheet names, A1 ranges, absolute refs ($A$1), unary +/-, string literals,
percent (%), and Excel error values (#REF!/#DIV/0!). Each row states support/reject + the finding emitted
on reject. Record the matrix in code comments at the parser head AND in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the WBDL-03 dialect linter — runs against the dialect contract over a synthetic cell source, collect-all located findings</name>
  <files>crates/pmcp-workbook-compiler/src/dialect/mod.rs, crates/pmcp-workbook-compiler/src/dialect/linter.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dialect/{linter,rules,mod}.rs (the running linter + its local WHITELIST copy to DELETE)
    - crates/pmcp-workbook-dialect/src/lib.rs (WHITELIST/DialectRules/CandidateRole — the contract to re-export and run against; the spec-binding drift test)
    - crates/pmcp-workbook-runtime/src/finding.rs (LintFinding/LintReport/Severity to emit)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/dialect/ — consume the contract, delete local copies)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md (D-01 collect-all never fail-fast; D-02 dialect violation = ERROR)
  </read_first>
  <action>
    Fill the dialect/ stub (Plan 01). Lift dialect/{linter,mod}.rs. DELETE the lighthouse's local
    WHITELIST/DialectRules/CandidateRole copies (dialect/rules.rs) and re-export from
    pmcp_workbook_dialect instead — the linter runs against the shipped contract. Use the re-exported
    runtime finding types (LintFinding/LintReport/Severity) for collect-all, cell-addressed findings
    with repair guidance (D-01 — report the whole pass at once, never fail-fast). A non-whitelist
    function / dialect violation is an ERROR (D-02, blocks emit). The linter consumes a `&impl CellSource`
    (the synthetic seam in <interfaces>), NOT 93-02's WorkbookMap directly — tests use a hand-built
    TestCells double, so this plan stays parallel with 93-02. Scrub all customer identifiers; lift the
    linter tests, scrubbed, re-pointed at TestCells.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler dialect:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler dialect::` passes (against the synthetic TestCells double — no 93-02 dependency).
    - No re-declared WHITELIST in the compiler: `! grep -rn 'WHITELIST.*=.*&\[' crates/pmcp-workbook-compiler/src/dialect/` (it is re-exported from the dialect crate).
    - The linter consumes a CellSource abstraction, not WorkbookMap: `! grep -rn 'WorkbookMap' crates/pmcp-workbook-compiler/src/dialect/`
    - Linter emits collect-all located findings — a test asserts multiple violations report in one pass (not fail-fast).
    - Zero customer identifiers in dialect/.
  </acceptance_criteria>
  <done>WBDL-03 linter runs against the dialect contract over a synthetic cell source; collect-all located findings; tests green; no WHITELIST duplication; no 93-02 dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift formula parser (whitelist-at-parse, depth-limited) + DAG Kahn toposort (WBCO-03) + supported-formula matrix</name>
  <files>crates/pmcp-workbook-compiler/src/formula/mod.rs, crates/pmcp-workbook-compiler/src/formula/token.rs, crates/pmcp-workbook-compiler/src/formula/parser.rs, crates/pmcp-workbook-compiler/src/formula/rebase.rs, crates/pmcp-workbook-compiler/src/dag/mod.rs, crates/pmcp-workbook-compiler/src/dag/graph.rs, crates/pmcp-workbook-compiler/src/dag/resolve.rs, crates/pmcp-workbook-compiler/src/dag/topo.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/formula/{token,parser,rebase,mod}.rs (Pratt parser; whitelist-at-parse rejection point)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dag/{graph,resolve,topo,mod}.rs (Kahn toposort)
    - crates/pmcp-workbook-runtime/src/formula.rs (Expr/BinOp/UnOp/CellValue the parser builds — re-export)
    - crates/pmcp-workbook-runtime/src/dag.rs + resolve.rs (Dag/toposort to reuse)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Don't Hand-Roll — whitelist-AT-PARSE-time is the security primitive; petgraph rejected)
    - This plan's <interfaces> (supported-formula matrix to document; CellSource seam)
  </read_first>
  <behavior>
    - Test whitelist_at_parse_rejects: a formula calling a function NOT in WHITELIST is rejected
      at parse time — the parser returns a typed parse error (NOT a lint finding; the linter, not the
      parser, emits dialect findings — keep the API boundary crisp per Codex MEDIUM), not parsed-then-filtered.
    - Test whitelisted_function_parses: a whitelisted function (e.g. ROUND) parses into runtime Expr.
    - Test depth_limit_rejects: a formula nested beyond MAX_PARSE_DEPTH is rejected with a typed
      depth-exceeded error (the limit is enforced in code, not discovered by the fuzzer).
    - Test supported_construct_matrix: a table-driven test exercises each row of the supported-formula
      matrix (sheet-qualified ref, quoted sheet, range, absolute ref, unary, string literal, percent,
      error value) and asserts support/reject per the documented matrix.
    - Test kahn_toposort_orders_deps: a 3-cell dependency chain (built from a synthetic cell source) topo-sorts in dependency order.
    - Test cycle_detected: a circular reference is detected and surfaced as an error (not an infinite loop).
  </behavior>
  <action>
    Fill the formula/ and dag/ stubs (Plan 01). Lift formula/{token,parser,rebase,mod}.rs and
    dag/{graph,resolve,topo,mod}.rs. The parser BUILDS the re-exported runtime Expr (do NOT re-declare
    Expr/BinOp/UnOp). Whitelist enforcement happens AT PARSE TIME — an out-of-whitelist function is a
    parse-time REJECTION returning a typed parse ERROR (the parser does NOT push dialect findings — that
    is the linter's job; keep parser-error vs lint-finding boundaries crisp). Add a MAX_PARSE_DEPTH
    recursion-depth limit ENFORCED IN CODE before fuzzing (not as a reaction to fuzz crashes). Document
    the supported-formula matrix from <interfaces> at the parser head. The DAG uses the lifted Kahn
    toposort (no petgraph); reuse the runtime Dag/toposort where the runtime already provides them.
    Detect cycles → error (no infinite loop). All tests use SYNTHETIC cell sources / formula strings —
    no 93-02 WorkbookMap. Scrub all customer identifiers; lift the existing parser/dag tests, scrubbed,
    plus the six behavior tests.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler formula:: dag:: 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler formula:: dag::` passes, including whitelist_at_parse_rejects, depth_limit_rejects, supported_construct_matrix, and cycle_detected.
    - Parser builds the re-exported runtime Expr (no local Expr): `! grep -rnE 'pub (enum|struct) Expr\b' crates/pmcp-workbook-compiler/src/formula/`
    - Recursion-depth limit present in code: `grep -qE 'MAX_PARSE_DEPTH|depth' crates/pmcp-workbook-compiler/src/formula/parser.rs`
    - Supported-formula matrix documented in code: a matrix comment block exists at the parser head.
    - No petgraph dependency: `! grep -q petgraph crates/pmcp-workbook-compiler/Cargo.toml`
    - Zero customer identifiers in formula/ and dag/.
  </acceptance_criteria>
  <done>formula parser (whitelist-at-parse, depth-limited, crisp parser-error vs lint boundary) + Kahn DAG lifted + scrubbed; supported-formula matrix documented; cycles detected; tests green on synthetic inputs; runtime Expr reused.</done>
</task>

<task type="auto">
  <name>Task 3: Fuzz target over the formula parser (ALWAYS: FUZZ)</name>
  <files>crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_formula_parser.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/formula/parser.rs (the parse entry the fuzz target drives — MAX_PARSE_DEPTH already enforced in Task 2)
    - crates/pmcp-workbook-compiler/fuzz/Cargo.toml (created in Plan 02 Task 3 — add the second target)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-VALIDATION.md (## ALWAYS — FUZZ the formula parser)
  </read_first>
  <action>
    Add a cargo-fuzz target fuzz_formula_parser feeding arbitrary UTF-8/byte strings as formula
    text into the parser. Invariant: any input yields Ok(Expr) or a typed parse error — NEVER a
    panic, hang, or unbounded recursion/allocation (the MAX_PARSE_DEPTH guard from Task 2 bounds
    recursion). Reuse the fuzz Cargo.toml from Plan 02.
  </action>
  <verify>
    <automated>cargo fuzz build fuzz_formula_parser 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The fuzz target builds (automated gate): `cargo fuzz build fuzz_formula_parser` exits 0.
    - Smoke run (environment-gated — nightly + cargo-fuzz required): `cargo fuzz run fuzz_formula_parser -- -runs=10000` finds no panic/crash. Run it as acceptance where nightly+cargo-fuzz are present; otherwise record the skip in the SUMMARY and rely on the build gate.
  </acceptance_criteria>
  <done>Formula-parser fuzz target builds (automated); smoke run clean where nightly+cargo-fuzz are available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA-authored formula text → parser | untrusted formula strings cross into the IR builder |
| synthetic/real cell source → linter/parser | workbook-sourced function names + cell refs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-INJ | Tampering / Elevation | a non-whitelisted/dangerous function in a cell | mitigate | Whitelist-AT-PARSE-time rejection — out-of-whitelist = parse-time REJECT (typed error), never silent accept (Task 2) |
| T-93-03-DOS | Denial of Service | malicious formula text (deep nesting / cycle) crashing or hanging the parser/DAG | mitigate | MAX_PARSE_DEPTH recursion limit enforced in code + cycle detection in the DAG + fuzz target (Tasks 2, 3); #![deny(panic)] |
| T-93-03-DRIFT | Tampering | a re-declared WHITELIST diverging from the shipped dialect contract | mitigate | re-export from pmcp_workbook_dialect; grep gate forbids a local WHITELIST (Task 1) |
</threat_model>

<verification>
- dialect/formula/dag tests green on synthetic inputs; whitelist-at-parse rejection + depth limit + cycle detection verified.
- Both fuzz targets build (smoke run where nightly+cargo-fuzz available).
- Zero customer identifiers; no WHITELIST/Expr duplication; no petgraph; no 93-02 dependency.
</verification>

<success_criteria>
- WBDL-03: running linter over a synthetic cell source against the dialect contract, collect-all located findings.
- WBCO-03: whitelist-at-parse parser (depth-limited, crisp error boundary) building runtime Expr + Kahn DAG with cycle detection + documented supported-formula matrix.
- The untrusted-formula surface is fuzzed and panic-free; 93-03 stays parallel with 93-02.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-03-SUMMARY.md` when done
</output>
</content>
