---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 07
type: execute
wave: 6
depends_on: ["93-06"]
files_modified:
  - crates/pmcp-workbook-compiler/src/stage1.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
  - crates/pmcp-workbook-compiler/examples/compile_a_workbook.rs
  - crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs
  - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx
  - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.provenance-override.json
autonomous: true
requirements: [WBCO-02, WBCO-05]

# O-2 PROOF-SHAPE PRE-DECISION (resolves both reviewers' "move O-2 earlier"): the producer/consumer
# proof DEFAULTS to STRUCTURAL EQUIVALENCE (not byte-identical). This was previously a blocking
# checkpoint:decision in Wave 6, which both reviewers flagged as too late (it changes artifact-emit
# /serialization design that hardens in Waves 4-5). Pre-deciding it here makes this plan autonomous
# and lets Plan 05's deterministic serializer be designed against the structural-equivalence target.
# Byte-identical is retained ONLY as a stretch goal, attempted in Task 3 IF (and only if) the
# deterministic serializer (Plan 05) plus a faithfully-authored fixture happen to reproduce the golden
# bytes exactly — never a blocking requirement.

must_haves:
  truths:
    - "stage1 composes the collect-all analysis pass (lint + synth + freshness + drift) in a single ingest"
    - "The generic compile_workbook driver routes ingest → stage1 → synth → ratify → parse+DAG → reconcile → gate → emit with the manifest coming SOLELY from synthesize→ratify (no build_reference_manifest)"
    - "A neutral tax-calc.xlsx (authored NOT via umya, carrying cached formula values as the reconcile oracle) compiles through the pipeline and reproduces the committed tax-calc@1.1.0 golden by STRUCTURAL EQUIVALENCE (byte-identical is a stretch goal only)"
    - "The fixture carries a committed trusted-fixture provenance override so the WBCO-07 gate accepts it WITHOUT the override weakening the production refuse path"
    - "cargo run --example compile_a_workbook demonstrates ingest→emit on the neutral fixture"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/lib.rs"
      provides: "Wired generic compile_workbook driver (no build_reference_manifest)"
      contains: "compile_workbook"
    - path: "crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs"
      provides: "Producer/consumer proof: re-emit reproduces the golden by structural equivalence"
      contains: "tax-calc"
    - path: "crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx"
      provides: "Neutral source workbook authored without umya, with cached formula values"
    - path: "crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.provenance-override.json"
      provides: "Trusted-fixture provenance override (test-path only)"
    - path: "crates/pmcp-workbook-compiler/examples/compile_a_workbook.rs"
      provides: "Library-level ingest→emit demonstration"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/lib.rs compile_workbook"
      to: "manifest::synthesize + ratify"
      via: "synth-driven manifest (no per-workbook Rust)"
      pattern: "synthesize|ratify"
    - from: "crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs"
      to: "crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/"
      via: "structural-equivalence diff against the committed golden"
      pattern: "tax-calc@1.1.0"
---

<objective>
Wire the whole pipeline together: lift `stage1.rs` (the composed collect-all pass), wire the
generic `compile_workbook` driver in `lib.rs` (replacing the deleted `build_reference_manifest`
with `ingest → stage1 → synthesize → ratify → parse+DAG → reconcile → gate → emit`), author a
neutral `tax-calc.xlsx` (NOT via umya, WITH cached formula values) plus its trusted-fixture
provenance override, prove the producer/consumer loop (re-emit reproduces the committed
`tax-calc@1.1.0` golden by STRUCTURAL EQUIVALENCE — the pre-decided default), and ship the
library-level example.

Purpose: This is the capstone that proves the §5 manifest-driven generalization. The driver MUST
route the manifest solely through synth→ratify — proving a second workbook would be served ITS OWN
schema (the whole point of deleting build_reference_manifest). The fixture must NOT be umya-authored
(the provenance gate would refuse it — Plan 02) and MUST carry cached formula values (the reconcile
oracle in Plan 04); since rust_xlsxwriter authoring may not carry Excel provenance, the fixture ships
a trusted-fixture provenance override (Plan 02) that is honored ONLY on the test path. O-2 is
pre-decided as structural equivalence (defined precisely below); byte-identical is a stretch goal only.
Output: wired driver, neutral fixture + override, producer/consumer proof test, example; full pipeline green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/stage1.rs (composed collect-all pass)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/lib.rs (lines 443 build_candidate_model — the driver shape WITHOUT the hand-built manifest; lines 769-858 build_reference_manifest to DELETE)

stage1.rs already exists as a compilable stub (Plan 01 Task 3); lib.rs has the typed-NotImplemented
compile_workbook stub from Plan 01 — replace it with the wired driver.

In-repo target (the producer/consumer agreement):
- crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ — the committed seven-member golden to reproduce (manifest.json, executable.ir.json, cell_map.json, layout.json, BUNDLE.lock, evidence/). The golden was synthesized from runtime types via tests/support/fixture_gen.rs (no source .xlsx exists — Pitfall 3).
- crates/pmcp-workbook-runtime render/rust_xlsxwriter — available for authoring the neutral .xlsx fixture (NOT umya). NOTE: rust_xlsxwriter may not write cached formula <v> values; if so, set the cached values explicitly via the writer's cached-result API, or author the fixture once in real Excel and commit the .xlsx. The fixture MUST carry cached formula values (the Plan 04 reconcile oracle).
- crates/pmcp-workbook-compiler/src/provenance/ (Plan 02) — the ProvenanceClass classifier + the trusted-fixture override mechanism. The fixture is authored by a non-Excel tool, so it ships a committed `tax-calc.provenance-override.json` honored ONLY on the test path; production classify still refuses fabricated provenance.

## STRUCTURAL EQUIVALENCE — precise definition (O-2 default; Plan 05's deterministic serializer is the basis)
The re-emit reproduces the committed tax-calc@1.1.0 golden when ALL of these hold (per emitted member):
1. Normalized-JSON equality — parse both sides to serde_json::Value and compare for deep equality
   (key order / whitespace ignored; this is why Plan 05 pins deterministic emit). Applies to
   manifest.json, executable.ir.json, cell_map.json, layout.json.
2. Required-member presence — all seven members exist (manifest/IR/cell_map/layout/BUNDLE.lock/evidence/).
3. Lock-hash recomputation — BUNDLE.lock's combined hash recomputes (via the runtime build_bundle_lock/
   fold_evidence_hash) to the same value over the emitted members; bundle_id present (D-17).
4. Toolkit-loader acceptance — the emitted bundle loads via pmcp-server-toolkit::workbook without error.
5. Named-output behavior — the emitted manifest's named outputs (names + dtypes + roles) match the golden's.
Note: no golden re-commit is required for structural equivalence (the
committed golden stays the frozen target). Byte-identical (a stricter superset) is attempted only as a
stretch goal in Task 2 (the proof test) and never blocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift stage1 + wire the generic compile_workbook driver (WBCO-02)</name>
  <files>crates/pmcp-workbook-compiler/src/stage1.rs, crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/stage1.rs (the composed lint+synth+freshness+drift pass)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/lib.rs (lines 443 build_candidate_model driver; lines 769-858 build_reference_manifest — DELETE)
    - crates/pmcp-workbook-compiler/src/lib.rs (the compile_workbook typed-NotImplemented stub + module declarations from Plan 01)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (## Code Examples — the generic compile_workbook driver shape; Anti-Pattern: never copy build_reference_manifest onto the emit path)
  </read_first>
  <action>
    Lift stage1.rs (the composed collect-all analysis pass — lint + synth + freshness + drift in one
    ingest, emitting collect-all located findings, D-01). Wire the generic compile_workbook driver in
    lib.rs replacing the Plan-01 typed-NotImplemented stub: read ORIGINAL bytes (provenance classify) →
    ingest::ingest (umya) → stage1::run_stage1 → manifest::synthesize → manifest::ratify →
    formula::parse_all + dag::build_dag (whitelist-at-parse) → reconcile::reconcile (named-out ERROR,
    helper WARNING) → change_class gate vs prior baseline → emit on a clean gate (first version: gate
    no-op, D-12). The manifest comes SOLELY from synthesize→ratify — build_reference_manifest must NOT
    appear on this path (it is the one surviving §5 gap; keep ONLY a scrubbed #[cfg(test)] anti-drift
    fixture if genuinely needed). Errors block emit; warnings are advisory (D-01/D-02). Scrub all
    customer identifiers; the driver takes a workbook_path argument (NO REFERENCE_WORKBOOK_PATH/
    WORKFLOW_NAME consts).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler stage1:: 2>&1 | tail -5 && ! grep -rn 'build_reference_manifest' crates/pmcp-workbook-compiler/src/ --include='*.rs' | grep -v test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler stage1::` passes; the crate builds with the wired driver.
    - GREP GATE: `! grep -rn 'build_reference_manifest' crates/pmcp-workbook-compiler/src/ --include='*.rs' | grep -v test` returns nothing.
    - compile_workbook routes through synthesize+ratify: `grep -qE 'synthesize|ratify' crates/pmcp-workbook-compiler/src/lib.rs`
    - The driver no longer returns CompileError::NotImplemented (the stub is replaced): a test drives compile_workbook end-to-end.
    - No REFERENCE_WORKBOOK_PATH/WORKFLOW_NAME consts: `! grep -rn 'REFERENCE_WORKBOOK_PATH\|WORKFLOW_NAME' crates/pmcp-workbook-compiler/src/`
    - Zero customer identifiers.
  </acceptance_criteria>
  <done>stage1 lifted; generic compile_workbook wired (synth-driven, no build_reference_manifest, no reference-path consts, no NotImplemented stub); pipeline builds; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Author neutral tax-calc.xlsx (with cached values + provenance override), write the structural-equivalence proof test + the example (WBCO-05)</name>
  <files>crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx, crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.provenance-override.json, crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs, crates/pmcp-workbook-compiler/examples/compile_a_workbook.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ (the full seven-member golden to reproduce — manifest.json shows the inputs/enum/outputs the fixture .xlsx must contain)
    - crates/pmcp-workbook-runtime render/ rust_xlsxwriter (the writer to author the .xlsx — NOT umya; the cached-result API to set cached formula values)
    - crates/pmcp-workbook-compiler/src/provenance/ (Plan 02 — the trusted-fixture override format the fixture ships; production refuse path it must NOT weaken)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pitfall 3 — fixture must NOT be umya-authored; the provenance gate refuses umya identity)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-VALIDATION.md (## ALWAYS — EXAMPLE compile_a_workbook on the neutral fixture)
    - This plan's <interfaces> (STRUCTURAL EQUIVALENCE precise definition — implement the 5-point check exactly)
  </read_first>
  <action>
    Author a neutral tax-calc.xlsx (3 inputs incl. an inline-DV enum, a governed bracket table,
    multiple named outputs, real formulas WITH CACHED VALUES — the Plan 04 reconcile oracle) — author it
    with real Excel or rust_xlsxwriter, NEVER via umya (the provenance gate would refuse a umya-authored
    fixture). If rust_xlsxwriter does not write cached `<v>` values, set them explicitly via its
    cached-result API or author once in real Excel; the fixture MUST carry cached values. Commit a
    `tax-calc.provenance-override.json` trusted-fixture override (Plan 02) so the WBCO-07 gate accepts the
    non-Excel-authored fixture on the TEST path only. Use only neutral domain names (tax-calc; zero
    customer identifiers).

    Write tests/reemit_tax_calc_golden.rs implementing STRUCTURAL EQUIVALENCE per <interfaces> (the
    pre-decided O-2 default): compile the fixture through compile_workbook (honoring the trusted-fixture
    override on the test path), then assert all five structural-equivalence checks against the committed
    tax-calc@1.1.0 golden — (1) normalized-JSON deep equality per member, (2) all seven members present,
    (3) BUNDLE.lock combined-hash recomputes + bundle_id present, (4) the emitted bundle loads via
    pmcp-server-toolkit::workbook, (5) named-output names/dtypes/roles match. Do NOT regenerate or
    re-commit the golden (structural equivalence keeps it frozen). OPTIONALLY also assert byte-identical
    as a non-blocking stretch check (skip/log if it does not hold). Add a test asserting the SAME fixture
    bytes WITHOUT the override are REFUSED by the production provenance path (the override does not weaken
    production refusal).

    Write examples/compile_a_workbook.rs demonstrating ingest→emit on the neutral fixture (library-level;
    the CLI is Phase 94).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --test reemit_tax_calc_golden 2>&1 | tail -6 && cargo run -p pmcp-workbook-compiler --example compile_a_workbook 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler --test reemit_tax_calc_golden` passes (re-emit reproduces the golden by STRUCTURAL EQUIVALENCE — all five checks).
    - `cargo run -p pmcp-workbook-compiler --example compile_a_workbook` runs and emits a bundle on the neutral fixture.
    - The fixture is NOT umya-authored AND carries cached values: a test compiles it (via the override) without an oracle/non-excel-app refusal, and reconcile finds the cached oracle.
    - The trusted-fixture override does NOT weaken production refusal: a test asserts the same bytes WITHOUT the override are refused on the production path.
    - Zero customer identifiers in the fixture/test/example: `! grep -rEi 'ufh|towelrad|coil|heat_source|heat_pump|underfloor|radiator|plot3' crates/pmcp-workbook-compiler/tests/ crates/pmcp-workbook-compiler/examples/`
  </acceptance_criteria>
  <done>Neutral tax-calc.xlsx authored (not via umya, with cached values) + trusted-fixture override; structural-equivalence producer/consumer proof green (golden stays frozen); override cannot weaken production refusal; example runs.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate — quality-gate + purity-check + all grep gates green (reserve scope for mechanical clippy refactors)</name>
  <files>crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-VALIDATION.md (## Sampling Rate — phase gate: full suite + structural-equivalence re-emit + umya-fabrication-refused + grep gates)
    - CLAUDE.md (make quality-gate is the CI-equivalent gate; the 6 refactor techniques P1-P6 + the `// Why:`-annotated allow template for irreducible complexity; cog ≤25, hard cap 50)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Architecture Patterns — the P1-P6 refactor techniques for cognitive-complexity violations)
  </read_first>
  <action>
    Run the phase-gate suite and fix any residual issues: `make quality-gate` (fmt + clippy
    pedantic/nursery + build + test + audit) then `make purity-check`. Confirm the full per-requirement
    test map is green and all four grep gates pass: no delta.abs() in reconcile/classifier.rs; no
    build_reference_manifest on non-test paths; zero customer identifiers in non-fixture paths; the
    umya-fabrication-refused regression. Confirm both fuzz targets build.

    RESERVE SCOPE FOR MECHANICAL CLIPPY REFACTORS (Codex MEDIUM): a ~13K-LOC lift will surface clippy
    pedantic/nursery findings and cognitive-complexity violations. Budget this task to resolve them
    mechanically: apply the P1-P6 refactor techniques (extract-method, guard clauses, table dispatch,
    etc.) to bring functions to cog ≤25; for irreducibly-complex parser/AST/protocol-dispatch functions,
    add a `// Why:`-annotated `#[allow(clippy::cognitive_complexity)]` per the project template (hard cap
    cog 50). This is expected, in-scope work — do NOT under-scope the gate task or treat findings as a
    surprise.
  </action>
  <verify>
    <automated>make quality-gate && make purity-check && ! grep -rEi 'ufh|towelrad|coil|heat_source|heat_pump|underfloor|radiator|plot3' crates/pmcp-workbook-compiler/src/</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0 (fmt/clippy/build/test/audit) — including any mechanical refactors / `// Why:`-annotated allows needed to clear the ~13K-LOC lift's clippy findings.
    - `make purity-check` exits 0 (compiler positive `-i umya-spreadsheet` assertion + served-crate negatives + single-version guard).
    - All four grep gates pass (delta.abs / build_reference_manifest / customer-identifier / umya-fabrication-refused).
    - Both fuzz targets build (`cargo fuzz build fuzz_formula_parser` and `cargo fuzz build fuzz_provenance_reader`).
    - Any cognitive-complexity allow carries a `// Why:` annotation and stays ≤ cog 50.
  </acceptance_criteria>
  <done>Full quality-gate + purity-check green (incl. mechanical clippy refactors / annotated allows); all grep gates pass; fuzz targets build; phase ready for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| neutral fixture .xlsx → provenance gate | a umya-authored fixture would be refused; the proof requires a non-umya-authored workbook with cached values + a test-only provenance override |
| trusted-fixture override → production refuse path | the override must never admit fabricated provenance on the production compile path |
| compiler output → Phase 92 consumer | the emitted bytes must satisfy the frozen seven-member boot contract (structural equivalence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-07-FIXTURE | Spoofing | a umya-authored fixture (fabricated provenance) used as the proof source | mitigate | Author tax-calc.xlsx via real Excel / rust_xlsxwriter (NEVER umya) with cached values; ship a test-only trusted-fixture provenance override; a test asserts the same bytes WITHOUT the override are refused on production (Task 2) |
| T-93-07-DRIFT | Tampering | build_reference_manifest sneaking back onto the wired emit path (a second workbook served the wrong schema) | mitigate | Grep gate on non-test paths in the wired driver (Task 1) + the phase-gate suite (Task 3) |
| T-93-07-SCRUB | Information Disclosure | a surviving customer identifier in scrubbed code/comments/fixtures/docs | mitigate | Customer-identifier grep gate across src/ tests/ examples/ (Tasks 1, 2, 3) |
| T-93-07-SC | Tampering | npm/pip/cargo installs (none new here; reader deps gated in Plan 01) | accept | No new package-manager installs in this plan; reader-dep legitimacy already gated in Plan 01 Task 1 |
</threat_model>

<verification>
- stage1 + wired driver build; structural-equivalence producer/consumer proof green; example runs; Phase 92 consumer still loads the golden.
- make quality-gate + make purity-check green (incl. mechanical clippy refactors); all four grep gates pass; both fuzz targets build.
- Zero customer identifiers anywhere in the new crate; trusted-fixture override does not weaken production refusal.
</verification>

<success_criteria>
- WBCO-02: the wired generic compile_workbook driver is synth-driven (no build_reference_manifest), proving the §5 generalization.
- WBCO-05: the producer/consumer proof reproduces the committed tax-calc@1.1.0 golden by STRUCTURAL EQUIVALENCE (byte-identical a non-blocking stretch goal), golden stays frozen.
- The full phase gate (quality-gate + purity-check + grep gates + fuzz builds + reserved clippy-refactor scope) is green.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-07-SUMMARY.md` when done
</output>
</content>
