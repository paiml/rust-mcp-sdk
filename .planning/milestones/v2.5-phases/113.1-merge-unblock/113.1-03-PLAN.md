---
phase: 113.1-merge-unblock
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/http_constants.rs
  - src/shared/http.rs
  - src/shared/sse_optimized.rs
  - src/shared/mod.rs
  - tests/v2_bounded_reads_tripwire.rs
  - examples/t03_sse_optimized.rs
  - examples/README.md
autonomous: true
requirements: [HTTP-09]
roadmap_success_criteria: [SC-3]
closes_decisions: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "OptimizedSseTransport::connect_sse refuses a response body over 16 MiB without ever allocating it whole, whether or not the peer declares a Content-Length"
    - "A body of exactly the cap is admitted and parses normally, so the comparison is provably > and not >="
    - "An under-cap body still reaches recv_tx through connect_sse itself — the cap bounds the read without breaking the wiring"
    - "The refusal names the limit and echoes no body content"
    - "The bounded-read tripwire's whole-body allowlist is EMPTY, and its accumulation allowlist names the mechanism bounding the new chunk() loop"
    - "OptimizedSseTransport is marked deprecated pointing at StreamableHttpTransport, with zero public items removed"
    - "The shipped example still builds under --features sse, and rustdoc still has zero warnings after the constant moves"
  artifacts:
    - path: "src/shared/sse_optimized.rs"
      provides: "collect_sse_text_within_cap + the #[deprecated] transport"
      contains: "collect_sse_text_within_cap"
    - path: "tests/v2_bounded_reads_tripwire.rs"
      provides: "an empty WHOLE_BODY_ALLOWLIST and a reviewed accumulation entry for the new loop"
      contains: "WHOLE_BODY_ALLOWLIST.len(),\n        0"
  key_links:
    - from: "connect_sse"
      to: "collect_sse_text_within_cap"
      via: "call passing DEFAULT_HTTP_SSE_BUFFERED_BYTES as the cap"
      pattern: "collect_sse_text_within_cap\\("
    - from: "collect_sse_text_within_cap"
      to: "reqwest::Response::chunk"
      via: "running-total loop, not Response::text"
      pattern: "\\.chunk\\(\\)\\.await"
---

<objective>
Close D-113-Q: `OptimizedSseTransport::connect_sse` reads an entire SSE response with
`reqwest::Response::text()` (`src/shared/sse_optimized.rs:266`), which accepts no limit argument, so
a remote peer chooses the allocation. It is currently the bounded-read tripwire's single
`NOT BOUNDED` allowlist entry — a gap that is enumerated rather than fixed.

Purpose: HTTP-09 clauses 1 and 2. This is the last unbounded whole-body read inside the requirement's
scope fence; closing it drives `WHOLE_BODY_ALLOWLIST` to length 0, which is the ratchet's floor.

Output: a `chunk()`-based capped read reusing the crate's existing 16 MiB SSE ceiling, over/at-cap
tests plus an under-cap wiring test, both tripwire lists moved to their new state, a `#[deprecated]`
marker on the transport, and the shipped example kept compiling.

Serves ROADMAP Phase-113.1 Success Criterion 3, plus HTTP-09 clauses 1 and 2.

## Planner correction to RESEARCH and PATTERNS — read this before Task 1

D-03 says to reuse `pub const DEFAULT_HTTP_SSE_BUFFERED_BYTES` at `src/shared/http.rs:116`. That
constant is **not reachable from `sse_optimized.rs` as the tree stands**, and neither RESEARCH nor
PATTERNS caught it:

- `src/shared/mod.rs:54` gates `pub mod http;` on `#[cfg(all(feature = "http", not(target_arch = "wasm32")))]`
- `src/shared/mod.rs:31` gates `pub mod sse_optimized;` on `#[cfg(feature = "sse")]`
- `Cargo.toml:194` defines `sse = ["http-client", "dep:bytes"]` and `Cargo.toml:191` defines
  `http-client = ["dep:reqwest"]` — **neither enables the `http` feature**
- Verified: `cargo check --no-default-features --features sse` builds today, and `src/shared/http.rs`
  is not compiled in that configuration

So a bare reference to `crate::shared::http::DEFAULT_HTTP_SSE_BUFFERED_BYTES` from `sse_optimized.rs`
fails to compile under `--features sse`, which is exactly the configuration the D-04 example build
uses. Task 1 relocates the constant's DEFINITION into the ungated `src/shared/http_constants.rs` and
re-exports it from `src/shared/http.rs`, preserving the existing public path byte-for-byte. This
honors D-03's intent — one number for every SSE read in the crate — rather than minting a second
constant or cfg-gating the cap.

**Second-order hazard the relocation creates.** The constant's rustdoc ends with an intra-doc link
`[`HttpTransport::with_sse_buffered_bytes`]`. That link resolves today because `HttpTransport` is in
scope in `http.rs`; it is **not** in scope in `http_constants.rs`, so moving the doc comment verbatim
produces an unresolved-link warning. `make doc-check` runs
`RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --features composition,http,...,sse,...`, so that
becomes an error there. Task 1 fixes the link when it moves the doc — see the action.

## Wave-1 concurrency note

`.planning/config.json` sets `workflow.use_worktrees: false`: all four Wave-1 plans share one
worktree. Every git assertion in this plan is therefore scoped to files this plan owns exclusively
(`src/shared/sse_optimized.rs`, `tests/v2_bounded_reads_tripwire.rs`, `src/shared/http_constants.rs`,
`src/shared/http.rs`, `src/shared/mod.rs`, `examples/*`) or to the commit the task produced. Do not
add any assertion of the form "the worktree is globally clean" — plans 01/02/04 are running in it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md
@.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md
@.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md

<interfaces>
<!-- Verified at HEAD e8cbd998. -->

The read being replaced — src/shared/sse_optimized.rs:264-284:
  `response` is bound at :246-252 as an OWNED reqwest::Response; `match response.text().await { .. }`
  at :266 reads the whole body, then iterates `text.lines()` looking for a `data: ` prefix and
  `serde_json::from_str::<TransportMessage>` on each.
  `chunk(&mut self)` needs a MUTABLE binding — put the `mut` on the HELPER's parameter
  (`mut response: reqwest::Response`), NOT on the caller's `let response`. The helper takes the
  response BY VALUE, so a `mut` at the call site is never read and trips `unused_mut` under
  `-D warnings`.

The private fn under test, reachable from the in-file `#[cfg(test)] mod tests` via `Self::` :
  async fn connect_sse(config: &OptimizedSseConfig, client: &reqwest::Client,
      state: &Arc<RwLock<ConnectionState>>, recv_tx: &mpsc::Sender<TransportMessage>,
      last_event_id: &Arc<RwLock<Option<String>>>) -> Result<()>                    :225-231

reqwest 0.13.2 API surface (read from the vendored registry source):
  pub fn content_length(&self) -> Option<u64>                                   :90    no cfg
  pub async fn text(self) -> crate::Result<String>                              :163   no cfg
  pub async fn chunk(&mut self) -> crate::Result<Option<Bytes>>                 :315   NO cfg  <-- D-02
  #[cfg(feature = "stream")] pub fn bytes_stream(self) -> impl Stream<..>       :354   GATED, unavailable

Crate error family used throughout connect_sse (:251, :255, :274, :282):
  crate::error::Error::internal(<String or &str>)   —  `use crate::error::{Error, Result};` at :10

The ceiling, and its current home:
  pub const DEFAULT_HTTP_SSE_BUFFERED_BYTES: usize = 16 * 1024 * 1024;    src/shared/http.rs:116
  its rustdoc ends with the intra-doc link [`HttpTransport::with_sse_buffered_bytes`]  :114-115
  eight in-crate references, all inside src/shared/http.rs (:62, :116, :130, :171, :207, :223,
  :710, :715, :719) plus one rustdoc link from src/shared/streamable_http.rs:309

The shape to mirror (doctrine, not mechanism):
  collect_body_within_cap            src/shared/http.rs:283-319, src/shared/streamable_http.rs:528-553
  collected_body_over_cap            src/shared/streamable_http.rs:327-347   (the error-builder)
  assert_over_cap_refusal + the over/at pair   src/shared/streamable_http.rs:2369-2436
  const CAP: usize = 512;            src/shared/streamable_http.rs:2331  ("cost bytes, not megabytes")

Tripwire anchors:
  WHOLE_BODY_NEEDLES                        tests/v2_bounded_reads_tripwire.rs:562
  WHOLE_BODY_ALLOWLIST (single entry)       :588-600
  the length pin (currently `1`)            :720-727, inside
                                            every_whole_body_exemption_carries_a_substantive_justification
  MIN_JUSTIFICATION_CHARS = 40              :731
  ACCUMULATION_NEEDLES                      :741  = ["extend_from_slice(", "push_str(", ".extend(", ".append("]
  the src/shared/http.rs entry (closest twin)   :796-806
  the src/shared/sse_optimized.rs entry          :819-828  (push_str, count 1)
  scope fence — DO NOT WIDEN                     :64-69

Rustdoc gate (NOT part of make quality-gate — run it explicitly):
  make doc-check  ->  RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --features composition,http,
                      http-client,jwt-auth,macros,mcp-apps,oauth,rayon,resource-watcher,
                      schema-generation,simd,sse,streamable-http,validation,websocket   Makefile:418-421

dev-dependency already present: mockito = "1.5.0"  (Cargo.toml:169) — serves a real HTTP endpoint,
so it works against reqwest as well as hyper.
An existing `#[cfg(test)]` module already sits at src/shared/sse_optimized.rs:460.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the 16 MiB ceiling reachable, bound connect_sse with a chunk() loop, and update the accumulation allowlist atomically</name>

  <files>src/shared/http_constants.rs, src/shared/http.rs, src/shared/sse_optimized.rs, tests/v2_bounded_reads_tripwire.rs</files>

  <behavior>
    - `collect_sse_text_within_cap(response, max_bytes)` returns the full body as a `String` when the delivered bytes are at or under `max_bytes`.
    - It returns `Err` when the peer declares a `Content-Length` over `max_bytes`, WITHOUT reading a body byte.
    - It returns `Err` when the peer omits or understates `Content-Length` but delivers more than `max_bytes`, stopping mid-flight so the over-cap body is never held whole.
    - The over-cap check cannot itself overflow: the running total is compared against the remaining headroom before appending, or added with `checked_add`.
    - The error message names the limit and contains no body content.
    - `connect_sse`'s observable behavior for an under-cap body is unchanged: the same `data: ` lines are parsed into the same `TransportMessage`s in the same order and delivered on `recv_tx`.
  </behavior>

  <read_first>
    - `src/shared/sse_optimized.rs` `:225-300` — `connect_sse` from its signature through the request build, the `.text().await` match, and out. Read the whole function; the replacement must preserve its error family and its post-read state transitions.
    - `src/shared/sse_optimized.rs` `:1-20` — the module header and the `use crate::error::{Error, Result};` import.
    - `src/shared/http.rs` `:104-126` — `DEFAULT_HTTP_SSE_BUFFERED_BYTES` with its full rustdoc INCLUDING the trailing `[`HttpTransport::with_sse_buffered_bytes`]` intra-doc link, which is also the model for stating what breaks at the boundary.
    - `src/shared/http.rs` `:283-319` and `src/shared/streamable_http.rs` `:514-553` — `collect_body_within_cap` and the **two-refusal doctrine** rustdoc to reproduce (Content-Length as advisory early refusal; the delivered bytes as the authority).
    - `src/shared/streamable_http.rs` `:327-347` — `collected_body_over_cap`, the error-builder convention: names the limit and the observed size, names the escape hatch, deliberately echoes no body content.
    - `src/shared/http.rs` `:359-378` — the nearest in-repo structural cousin of the loop (`while let Some(chunk) = body.frame().await`, append, drain in the same iteration).
    - `src/shared/http_constants.rs` — the whole file (43 lines); note its module doc currently says "headers and content types".
    - `Makefile` `:414-421` — the `doc-check` recipe and the exact feature list it builds docs under.
    - `tests/v2_bounded_reads_tripwire.rs` `:731-860` — `MIN_JUSTIFICATION_CHARS`, `ACCUMULATION_NEEDLES`, the `Accumulation` struct and its "keyed by file+needle+count, never a line number" rationale, and every existing entry.
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Pitfall 3" — why this task must touch the accumulation list, and why a plan that only does D-05 fails.
    - `.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md` §§ 6 and 9.
  </read_first>

  <action>
Four edits, one commit.

**Edit 1 — relocate the ceiling so `sse_optimized` can reach it** (see the objective's planner
correction for why this is necessary). Move the `pub const DEFAULT_HTTP_SSE_BUFFERED_BYTES: usize = 16 * 1024 * 1024;`
definition, with its existing rustdoc, from `src/shared/http.rs:104-116` into
`src/shared/http_constants.rs` (which `src/shared/mod.rs:56` declares with no `cfg` at all). At the
old site in `src/shared/http.rs`, put `pub use crate::shared::http_constants::DEFAULT_HTTP_SSE_BUFFERED_BYTES;`
so the existing public path `pmcp::shared::http::DEFAULT_HTTP_SSE_BUFFERED_BYTES` is preserved
byte-for-byte and all eight in-file references keep resolving unqualified. Widen
`http_constants.rs`'s module doc from "Constants for HTTP headers and content types used in MCP" to
also cover transport byte ceilings, and add a line to the constant's rustdoc naming the two readers
that now share it: `HttpTransport`'s SSE reader and `OptimizedSseTransport::connect_sse`.

**Fix the intra-doc link while you move it.** The final paragraph currently reads
``[`HttpTransport::with_sse_buffered_bytes`] is the escape hatch``. `HttpTransport` is not in scope in
`http_constants.rs`, so that link becomes unresolvable and `make doc-check` (which runs with
`RUSTDOCFLAGS="-D warnings"`) turns it into an error. Rewrite it fully qualified as
``[`crate::shared::http::HttpTransport::with_sse_buffered_bytes`]``. Then run `make doc-check` — it
builds with the `http` feature enabled, so the qualified path resolves. If `doc-check` still warns on
that link in any configuration it builds, degrade it to plain inline code (backticks, no link
brackets) and add a one-line comment saying the type lives behind the `http` feature while this
constant does not. Do NOT leave a warning behind and do NOT silence it with an `allow`.

This edit is purely additive to the public API — one new public path, zero removed — which the Task 3
`cargo semver-checks` run confirms.

**Edit 2 — the capped read.** Add a private
`async fn collect_sse_text_within_cap(mut response: reqwest::Response, max_bytes: usize) -> Result<String>`
to `src/shared/sse_optimized.rs`, placed immediately before `connect_sse`. Note the `mut` is on the
**parameter binding**, because the helper takes the response by value and `chunk(&mut self)` needs a
mutable place. The caller's `let response = ...` at `:246` stays IMMUTABLE — adding `mut` there would
never be read and would trip clippy's `unused_mut` under `make lint`'s `-D warnings`.

The `max_bytes` parameter exists so the tests can drive a small cap; production has exactly one call
site and it passes `crate::shared::http_constants::DEFAULT_HTTP_SSE_BUFFERED_BYTES`. Do **not** add a
field to `OptimizedSseConfig` — its fields are all `pub`, so that is
`constructible_struct_adds_field` and `cargo semver-checks` runs in this milestone's gate (D-03).

Implement the two-refusal doctrine, and say so in the rustdoc in the words of
`streamable_http.rs:514-527`:
1. If `response.content_length()` is `Some(n)` and `n > max_bytes as u64`, return `Err` before
   reading a single body byte. The header is a peer-controlled OPTIMISATION, never the authority
   (the 113-20 rule).
2. Loop `while let Some(chunk) = response.chunk().await`, maintain a running total, and check
   against `max_bytes` **before each append** so an over-cap body is never held whole. Do the check
   overflow-safely — either `if chunk.len() > max_bytes - accumulated.len() { return Err(..) }`
   (safe because `accumulated.len() <= max_bytes` is the loop invariant) or
   `accumulated.len().checked_add(chunk.len())` compared against `max_bytes`. Never compute
   `accumulated.len() + chunk.len()` unguarded. Append the chunk bytes into a `Vec<u8>` accumulator
   and decode once at the end with `String::from_utf8_lossy` — matching the lossy tolerance
   `.text()` already had, so the change is a bound and not also a strictness change.

A body of exactly `max_bytes` is ADMITTED; one byte over is refused. State that boundary in the
rustdoc. Because `max_bytes` is a parameter, phrase the rustdoc in terms of "the configured cap" and
name `DEFAULT_HTTP_SSE_BUFFERED_BYTES` as what the single production call site passes — do not write
as though every invocation used the default.

Build the error in the shape of `collected_body_over_cap` (`streamable_http.rs:327-347`) but with
this file's own error family — `Error::internal(format!(...))`, matching `connect_sse`'s four
existing sites at `:251`, `:255`, `:274`, `:282`; do not introduce `Error::Transport`. The message
must name `max_bytes` and the constant `DEFAULT_HTTP_SSE_BUFFERED_BYTES`, distinguish the declared
case from the mid-flight case ("delivered more than the cap (Content-Length absent or understated)"),
and **echo no body content** — the refusal must not become a channel for the very bytes it refused.

Use `reqwest::Response::chunk()`, not `bytes_stream()`: `chunk()` at
`reqwest-0.13.2/src/async_impl/response.rs:315` carries no `cfg`, while `bytes_stream()` at `:354-356`
is behind `#[cfg(feature = "stream")]`, which this crate does not enable (`Cargo.toml:146` pins
`default-features = false, features = ["json", "rustls", "form"]`). D-02.

**Edit 3 — wire it.** Replace `match response.text().await { .. }` at `:266` with a call to
`collect_sse_text_within_cap(response, crate::shared::http_constants::DEFAULT_HTTP_SSE_BUFFERED_BYTES).await`,
keeping the surrounding `Ok`/`Err` arms' behavior identical: the `Ok` arm still iterates
`text.lines()`, strips the `data: ` prefix, deserializes `TransportMessage`, and sends on `recv_tx`
with the same "Receiver channel closed" error; the `Err` arm still logs and returns. **Leave
`let response = ...` at `:246` immutable** — see Edit 2.

**Edit 4 — the accumulation allowlist, in this same commit.** RESEARCH § Pitfall 3 measured that the
new loop trips `every_peer_byte_accumulation_is_reviewed`: `ACCUMULATION_NEEDLES` includes
`extend_from_slice(` and `push_str(`, `src/shared/` is in scope, and `src/shared/sse_optimized.rs`
already has a pinned `push_str(` population of 1. Update `tests/v2_bounded_reads_tripwire.rs`:

- Add (or extend) the `Accumulation` entry for `src/shared/sse_optimized.rs` matching the needle your
  loop actually uses, with the count you actually measure. Do not guess the count — run the tripwire
  and read the reported number.
- Its `why` must NAME THE MECHANISM, not assert a label: the running total checked against
  `DEFAULT_HTTP_SSE_BUFFERED_BYTES` **before each append**, plus the `Content-Length` early refusal.
  Minimum 40 characters (`MIN_JUSTIFICATION_CHARS` at `:731`). Model it on the `src/shared/http.rs`
  entry at `:796-806`, which is the closest semantic twin.
- The EXISTING `sse_optimized.rs` / `push_str(` entry at `:819-828` must also be corrected: its `why`
  currently ends *"the LIVE path in this file is connect_sse's reqwest whole-body read, which is
  enumerated separately in WHOLE_BODY_ALLOWLIST"* — a sentence this task falsifies.

**Do NOT** touch the scope fence at `tests/v2_bounded_reads_tripwire.rs:64-69`. D-18 forbids widening
it; the 18 auth-surface reads sit outside it by design and are recorded by plan 04.
**Do NOT** touch `WHOLE_BODY_ALLOWLIST` in this task — that is Task 2, so the two ratchets move as
separately reviewable changes.
  </action>

  <verify>
    <automated>cargo test --test v2_bounded_reads_tripwire --features full &amp;&amp; cargo check --no-default-features --features sse &amp;&amp; make doc-check &amp;&amp; cargo build --example t03_sse_optimized --features sse</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'DEFAULT_HTTP_SSE_BUFFERED_BYTES: usize = 16 \* 1024 \* 1024;' src/shared/http_constants.rs` returns 1 and the same grep against `src/shared/http.rs` returns 0
    - `grep -c 'pub use crate::shared::http_constants::DEFAULT_HTTP_SSE_BUFFERED_BYTES;' src/shared/http.rs` returns 1 — the old public path survives
    - `grep -c 'HttpTransport::with_sse_buffered_bytes' src/shared/http_constants.rs` returns at least 1, and every occurrence inside `[...]` link brackets is fully qualified with a `crate::shared::http::` prefix
    - `make doc-check` exits 0 with zero rustdoc warnings — this is the check the relocation can break, and it is NOT covered by `make quality-gate`
    - `grep -c 'async fn collect_sse_text_within_cap' src/shared/sse_optimized.rs` returns 1, and its signature binds the parameter as `mut response`
    - `grep -c 'let mut response' src/shared/sse_optimized.rs` returns 0 — the caller binding stayed immutable, so `unused_mut` cannot fire
    - `grep -v '^\s*//' src/shared/sse_optimized.rs | grep -v '^\s*///' | grep -c '\.text()\.await'` returns 0 — the unbounded read is gone from CODE, not merely wrapped. Doc/comment lines are filtered because the new rustdoc names `.text().await` when explaining what it replaced.
    - `grep -c '\.chunk()\.await' src/shared/sse_optimized.rs` returns at least 1
    - `grep -v '^\s*//' src/shared/sse_optimized.rs | grep -v '^\s*///' | grep -c 'bytes_stream'` returns 0 — no code path uses it (comments filtered: the rustdoc records D-02's reason for rejecting it)
    - `grep -c 'content_length()' src/shared/sse_optimized.rs` returns at least 1 — the early refusal exists
    - The over-cap comparison is overflow-safe: `awk '/async fn collect_sse_text_within_cap/,/^}/' src/shared/sse_optimized.rs | grep -cE 'checked_add|max_bytes - '` returns at least 1
    - `grep -c 'pub struct OptimizedSseConfig' src/shared/sse_optimized.rs` returns 1 and `git diff -- src/shared/sse_optimized.rs | grep -c '^+.*pub .*:.*,$'` shows no new field added to that struct
    - `cargo check --no-default-features --features sse` exits 0 — proves the relocated constant is reachable in the sse-only configuration
    - `cargo test --test v2_bounded_reads_tripwire --features full` exits 0, including `every_peer_byte_accumulation_is_reviewed`
    - `git diff -- tests/v2_bounded_reads_tripwire.rs` shows changes only inside the `Accumulation` list — the scope fence at `:64-69` and `WHOLE_BODY_ALLOWLIST` are untouched in this task
    - `cargo nextest run --features full --test sse_optimization_tests` exits 0
    - `make lint` exits 0 with zero warnings
  </acceptance_criteria>

  <done>`connect_sse` reads through a capped, overflow-safe `chunk()` loop against the crate's single 16 MiB SSE ceiling; that ceiling is reachable in every configuration that compiles the transport and its rustdoc link still resolves; the accumulation allowlist names the mechanism that bounds the new loop.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the cap is falsifiable with an over/at/under test set, then drive WHOLE_BODY_ALLOWLIST to zero</name>

  <files>src/shared/sse_optimized.rs, tests/v2_bounded_reads_tripwire.rs</files>

  <read_first>
    - `src/shared/sse_optimized.rs` `:460` onward — the existing `#[cfg(test)]` module, for its imports and conventions.
    - `src/shared/sse_optimized.rs` — `collect_sse_text_within_cap` and `connect_sse` as Task 1 left them, including `connect_sse`'s five-argument signature (it is a private associated fn, reachable from this module's tests).
    - `src/shared/streamable_http.rs` `:2331` and `:2369-2436` — `const CAP: usize = 512;` with its "cost bytes, not megabytes" note, the `assert_over_cap_refusal` helper, and the over/at pair. Copy all three pieces; the PAIR is what pins the comparison as `>` and not `>=`.
    - `tests/v2_bounded_reads_tripwire.rs` `:580-600` (the entry to delete) and `:705-728` (`every_whole_body_exemption_carries_a_substantive_justification`, which holds the length pin).
    - `.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md` § D-05 — the exact new pin semantics and what was rejected.
  </read_first>

  <action>
**Part 1 — the test set.** Add four `#[tokio::test]`s plus one assertion helper to the existing
`#[cfg(test)]` module in `src/shared/sse_optimized.rs`, using `mockito` (already a dev-dependency at
`Cargo.toml:169`; it serves a real HTTP endpoint, so it works against `reqwest`). Use a small
`const CAP: usize = 512;` and call `collect_sse_text_within_cap` directly with it where the test is
about the helper — this is exactly why Task 1 made the cap a parameter, and it keeps the tests
costing bytes rather than megabytes.

- `assert_over_cap_refusal(error, cap)`: asserts the message CONTAINS the cap as a decimal string and
  does NOT contain a distinctive filler token from the body. Copy `streamable_http.rs:2369`.
- `connect_sse_one_byte_over_the_cap_is_refused`: serve `CAP + 1` bytes with `.with_chunked_body(..)`
  so there is **no `Content-Length` at all** — that way the refusal can only come from the
  authoritative running total, not from the advisory header (T-113-93's point).
- `connect_sse_at_exactly_the_cap_is_admitted`: serve exactly `CAP` bytes containing a real
  `data: {json}` line, assert `Ok`, and assert the returned text still contains that line. This pins
  the comparison as `>` and also prevents a "fast because it read less" regression.
- `declared_content_length_over_the_cap_is_refused_without_reading_the_body` — **MANDATORY, not
  optional.** The early-refusal branch is a stated truth of this plan, so it gets a test. It is cheap:
  serve a body of `CAP + 1` = **513 bytes** with `mockito`'s default `.with_body(..)`, which sets a
  real `Content-Length: 513`. No 16 MiB transfer is needed — the branch is about the header value
  versus `max_bytes`, and 513 > 512 exercises it exactly. Assert `Err` and run
  `assert_over_cap_refusal`.
- `connect_sse_under_the_cap_still_delivers_a_message_to_recv_tx` — the wiring test. The three above
  exercise the collector; none proves that an ADMITTED body still flows through `connect_sse` into the
  channel. Build an `mpsc::channel`, an `OptimizedSseConfig` pointed at a `mockito` endpoint serving a
  small under-cap body with one `data: {json-rpc}` line, call
  `OptimizedSseTransport::connect_sse(&config, &client, &state, &tx, &last_event_id).await`, and
  assert a `TransportMessage` arrives on the receiver. This is the test that would catch the cap being
  wired in a way that swallows the body.

**Part 2 — the recorded negative control.** The phase's discipline is that a guard which cannot go
RED is not a guard. Temporarily delete the running-total check from `collect_sse_text_within_cap`
(leaving the loop and the `Content-Length` pre-check in place), run
`cargo test --lib --features full connect_sse_one_byte_over_the_cap_is_refused`, record the failing
output verbatim, and revert. Then confirm `git status --porcelain src/shared/sse_optimized.rs` is
clean of the control (this file is in no other plan's `files_modified`, so the check is
concurrency-safe).

**If the control comes back GREEN, that is a finding, not a formality to skip.** Record it in the
SUMMARY under an explicit `UNTESTED RESIDUAL RISK` heading naming the property the test set fails to
constrain, and do NOT cite the over-cap test as evidence the running total is load-bearing.

**Part 3 — the whole-body ratchet (D-05).** In `tests/v2_bounded_reads_tripwire.rs`:
- Delete the entire `WHOLE_BODY_ALLOWLIST` entry at `:588-600` **by hand**. The tripwire's
  dead-entry rule does not cover this list, so a stale entry would sit there silently.
- Rewrite the length pin at `:720-727` from `assert_eq!(WHOLE_BODY_ALLOWLIST.len(), 1, ...)` to
  `assert_eq!(WHOLE_BODY_ALLOWLIST.len(), 0, ...)`. The new message must keep the existing doctrine —
  "the list should SHRINK, never grow" and "a decision a human has to make on the record" — and state
  the new fact: the list is now EMPTY, the last entry was the `reqwest` whole-body read in
  `OptimizedSseTransport` that this tripwire itself found, closed in plan 113.1-03, and **any** entry
  at all now means a new unbounded read was exempted rather than fixed.
- `every_whole_body_exemption_carries_a_substantive_justification`'s loop stays as-is; it is
  vacuously correct at length 0 and remains the guard if the list ever regrows.

Rejected alternatives, recorded so they are not re-litigated: relaxing the pin to `<= 1`; deleting
the mechanism.

**Do NOT** widen the scope fence at `:64-69`.
  </action>

  <verify>
    <automated>cargo test --test v2_bounded_reads_tripwire --features full &amp;&amp; cargo test --lib --features full connect_sse &amp;&amp; cargo test --lib --features full declared_content_length_over_the_cap</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'connect_sse_one_byte_over_the_cap_is_refused' src/shared/sse_optimized.rs` returns 1
    - `grep -c 'connect_sse_at_exactly_the_cap_is_admitted' src/shared/sse_optimized.rs` returns 1
    - `grep -c 'declared_content_length_over_the_cap_is_refused_without_reading_the_body' src/shared/sse_optimized.rs` returns 1 — the early-refusal branch is tested, not merely stated
    - `grep -c 'connect_sse_under_the_cap_still_delivers_a_message_to_recv_tx' src/shared/sse_optimized.rs` returns 1, and that test calls `connect_sse(` rather than only the collector
    - `grep -c 'with_chunked_body' src/shared/sse_optimized.rs` returns at least 1 — the over-cap case carries no `Content-Length`
    - `grep -c 'NOT BOUNDED' tests/v2_bounded_reads_tripwire.rs` returns 0
    - `grep -A3 'WHOLE_BODY_ALLOWLIST.len(),' tests/v2_bounded_reads_tripwire.rs | grep -c '^\s*0,'` returns 1 — the pin is at zero
    - `grep -c 'const WHOLE_BODY_ALLOWLIST: &\[Allowed\] = &\[\];' tests/v2_bounded_reads_tripwire.rs` returns 1
    - `sed -n '64,69p' tests/v2_bounded_reads_tripwire.rs` is byte-identical to its pre-plan content — the scope fence was not widened
    - `cargo test --test v2_bounded_reads_tripwire --features full` exits 0 with all 13 tests passing
    - `cargo test --lib --features full connect_sse` exits 0
    - `git status --porcelain src/shared/sse_optimized.rs` shows no leftover from the negative control, and the SUMMARY carries the control's verbatim failing output (or an `UNTESTED RESIDUAL RISK` entry if it stayed green)
  </acceptance_criteria>

  <done>The cap is proven falsifiable by a recorded negative control; the over/at pair pins the boundary as `>`; the declared-`Content-Length` branch and the under-cap `connect_sse`→channel wiring both have their own tests; and the whole-body exemption list is empty with its ratchet driven to the floor.</done>
</task>

<task type="auto">
  <name>Task 3: Deprecate OptimizedSseTransport, keep the example compiling, and confirm additivity</name>

  <files>src/shared/sse_optimized.rs, src/shared/mod.rs, examples/t03_sse_optimized.rs, examples/README.md</files>

  <read_first>
    - `src/shared/sse_optimized.rs` `:1-20` — the module header, which is where the inner `#![allow(deprecated)]` goes.
    - `src/shared/mod.rs` `:88-116` — the re-export barrel, showing the cfg-attribute-then-`pub use` line convention and the exact line to annotate (`:105-106`).
    - `src/types/ui.rs` `:93-103` and `src/server/mod.rs` `:4474-4477` — the crate's two existing `#[deprecated]` instances (the `since` + `note` form naming the replacement, and the `#[allow(deprecated)]` doctest convention).
    - `examples/t03_sse_optimized.rs` `:1-9` — the header block, including its STALE run line which says `--example 28_sse_optimized` while the file is `t03_sse_optimized`.
    - `examples/README.md` `:376-380` — the row's two-line shape.
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Pitfall 2" — the empirically verified rustc behavior that makes both `allow` sites mandatory, and the measured reason `make test-examples` cannot catch a break here.
  </read_first>

  <action>
**Part 1 — the attribute.** Add to `OptimizedSseTransport` (the struct only):
`#[deprecated(since = "2.18.0", note = "Use StreamableHttpTransport, which bounds every peer-controlled read; OptimizedSseTransport is retained for 2.x compatibility only")]`.
The `since` value is derived, not guessed: root `Cargo.toml` reads `version = "2.17.0"`, so the next
published version is `2.18.0`. Match the `since` + `note` form used at `src/types/ui.rs:100-103`.

**Deprecate ONLY `OptimizedSseTransport`, never `OptimizedSseConfig`.** RESEARCH verified that
`tests/sse_optimization_tests.rs:7` imports `OptimizedSseConfig` and nothing else, and `make lint`
lints `--tests` — so deprecating the config would force a `#![allow(deprecated)]` onto that test file
for no benefit. This also resolves the Claude's-Discretion item on deprecation scope.

Optionally add a one-line note to the module-level rustdoc pointing at `StreamableHttpTransport`
(also Claude's Discretion) — if you do, keep it to the existing header block rather than restructuring it.

**Part 2 — the two mandatory `allow` sites.** RESEARCH verified empirically with rustc that the
`deprecated` lint fires on uses WITHIN the defining crate, including `pub use` re-exports and every
`impl` block on the type (a minimal repro produced 1 error for the re-export and 4 for one impl
block). `make lint` runs `cargo clippy --features "full" --lib --tests` with `RUSTFLAGS = -D warnings`
(`Makefile:11,152`), so both must be silenced:

1. `#![allow(deprecated)]` as an **inner attribute at the top of `src/shared/sse_optimized.rs`**,
   placed with the existing module header — one line covering all ~15 in-file references, rather than
   annotating each `impl`.
2. `#[allow(deprecated)]` on the re-export at `src/shared/mod.rs:105-106`, above the existing
   `#[cfg(feature = "sse")] pub use sse_optimized::{OptimizedSseConfig, OptimizedSseTransport};`.

Every `allow` needs a `// Why:` comment per CLAUDE.md's zero-tolerance rule: state that the item is
deprecated on purpose and that the crate must still compile its own retained transport.

**Part 3 — the example (D-04).** `examples/t03_sse_optimized.rs` STAYS — it is the only compile-check
consumer, and the transport still ships in 2.x. Add `#![allow(deprecated)]` at the top and extend the
header doc-comment to name `StreamableHttpTransport` as the replacement for new code. While in the
header, fix the stale run line: it says `cargo run --example 28_sse_optimized --features sse`; the
file is `t03_sse_optimized` and `examples/README.md:378-380` already has it right.

Add a short deprecation note to the `examples/README.md:376-380` row, keeping its existing two-line
bold-name + em-dash shape and its fenced run command.

**Part 4 — verification that actually verifies.** RESEARCH measured that `make test-examples`
(reached via `make quality-gate` → `test-all`) **cannot fail here**: the Makefile does not export
`RUSTFLAGS` for that recipe, so `-D warnings` is not in effect, and the recipe swallows every failure
into a yellow "requires specific features (skipped)" message. So run the build explicitly:
`cargo build --example t03_sse_optimized --features sse`.

Run `make doc-check` again here as well — a `#[deprecated]` on a re-exported type is a second way to
disturb rustdoc, and `make quality-gate` does not cover rustdoc warnings.

Also run `cargo semver-checks check-release` (or the repo's pinned invocation) now rather than at the
phase gate. RESEARCH assumption A5 — that an added `#[deprecated]` is non-breaking — was not verified
in that session, and D-01's whole justification is the milestone's "zero removed public items"
additivity claim. If semver-checks flags it, stop and record it rather than working around it.

**Do NOT** retire the transport or remove either re-exported item. That is a 3.0 action (recorded as
a deferred idea); this `#[deprecated]` is the signal that starts the clock.
**Do NOT** rewrite the example onto `StreamableHttpTransport` — that is a Phase 119 docs item.
  </action>

  <verify>
    <automated>cargo build --example t03_sse_optimized --features sse &amp;&amp; make lint &amp;&amp; make doc-check</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'since = "2.18.0"' src/shared/sse_optimized.rs` returns 1
    - The `#[deprecated]` attribute sits directly above `pub struct OptimizedSseTransport`, and `awk '/pub struct OptimizedSseConfig/{print prev} {prev=$0}' src/shared/sse_optimized.rs` shows no `#[deprecated]` above the config struct
    - `head -3 src/shared/sse_optimized.rs | grep -c '#!\[allow(deprecated)\]'` returns 1
    - `grep -B2 'pub use sse_optimized::{OptimizedSseConfig, OptimizedSseTransport};' src/shared/mod.rs | grep -c 'allow(deprecated)'` returns 1
    - `grep -c 'pub use sse_optimized::{OptimizedSseConfig, OptimizedSseTransport};' src/shared/mod.rs` returns 1 — no public item was removed
    - `head -3 examples/t03_sse_optimized.rs | grep -c '#!\[allow(deprecated)\]'` returns 1
    - `grep -c '28_sse_optimized' examples/t03_sse_optimized.rs` returns 0 — the stale run line is corrected
    - `grep -c 'StreamableHttpTransport' examples/README.md` returns at least 1 in the t03 row's vicinity
    - `grep -c '// Why:' src/shared/mod.rs` increased by 1 relative to its pre-plan value — the new `allow` carries its justification
    - `cargo build --example t03_sse_optimized --features sse` exits 0
    - `make lint` exits 0 with zero warnings
    - `make doc-check` exits 0 with zero rustdoc warnings
    - `cargo semver-checks` reports no update required (record the literal pass count in the SUMMARY); if it flags the deprecation, that is recorded as a finding and NOT worked around
  </acceptance_criteria>

  <done>`OptimizedSseTransport` is deprecated toward `StreamableHttpTransport` with both mandatory `allow` sites in place, no public item removed, the shipped example builds explicitly under `--features sse`, rustdoc is warning-free, and semver additivity is confirmed by a real run rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote SSE server → `OptimizedSseTransport::connect_sse` | The response body is entirely peer-sized. This transport is OFF the v2 streamable-HTTP path (v2 collects through `StreamableHttpTransport::collect_body_within_cap`) but is `pub use`'d at `src/shared/mod.rs:106`, so it is reachable in a shipped build. |
| peer-supplied `Content-Length` header → the early-refusal branch | Advisory only. A peer may omit or understate it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113.1-20 | Denial of Service | `connect_sse`'s `response.text().await` | mitigate | Replaced by `collect_sse_text_within_cap`, a `chunk()` loop with an overflow-safe running total checked against `DEFAULT_HTTP_SSE_BUFFERED_BYTES` (16 MiB) before each append (Task 1), pinned by an over/at-cap pair, a declared-`Content-Length` test, an under-cap wiring test, and a recorded negative control (Task 2). |
| T-113.1-21 | Spoofing | a peer omitting or understating `Content-Length` to bypass the early refusal | mitigate | The header is an optimisation only; the running total over delivered bytes is the authority. The over-cap test deliberately uses `with_chunked_body` so no `Content-Length` exists at all, making the header path incapable of producing the pass. |
| T-113.1-22 | Information Disclosure | the refusal message echoing refused body content into logs | mitigate | `assert_over_cap_refusal` asserts the message names the limit and contains no body filler token — the same doctrine as `collected_body_over_cap` (`streamable_http.rs:327-347`). |
| T-113.1-23 | Repudiation | a future unbounded read exempted rather than fixed | mitigate | `assert_eq!(WHOLE_BODY_ALLOWLIST.len(), 0)` (Task 2) makes any exemption a human decision on the record; `every_peer_byte_accumulation_is_reviewed` keeps the accumulation population pinned by file+needle+count. |
| T-113.1-24 | Tampering | widening the tripwire's scope fence to make a number look complete | accept | Explicitly forbidden by D-18. The 18 auth-surface reads sit outside the fence by design (semi-trusted IdP, not arbitrary peer) and are recorded with Phase 116 as owner by plan 04. |
| T-113.1-25 | Denial of Service | the 16 MiB ceiling itself being too low for legitimate media | accept | Documented in the constant's own rustdoc (base64 expands by ~4/3, so a 12 MiB binary is already 16 MiB encoded). `HttpTransport::with_sse_buffered_bytes` is the escape hatch for the sibling reader — linked fully qualified after the relocation so the pointer survives; `OptimizedSseTransport` is deprecated toward `StreamableHttpTransport`, which has its own configurable cap. |
| T-113.1-26 | Denial of Service | an integer overflow in the cap comparison silently admitting an over-cap body | mitigate | The comparison is `chunk.len() > max_bytes - accumulated.len()` (safe under the loop invariant) or `checked_add`; never an unguarded sum. Asserted by grep in Task 1. |
| T-113.1-SC | Tampering | cargo installs | accept | No package is installed. `reqwest` 0.13.2 and `mockito` 1.5.0 are pre-existing; `chunk()` needs no new feature (verified against the vendored source). RESEARCH § "Package Legitimacy Audit" records the phase as N/A. |
</threat_model>

<verification>
1. `cargo test --test v2_bounded_reads_tripwire --features full` — 13 tests, zero failures, with both
   `no_unbounded_whole_body_read_over_peer_supplied_bytes` and
   `every_peer_byte_accumulation_is_reviewed` green.
2. `cargo check --no-default-features --features sse` — exits 0 (proves the relocated ceiling is
   reachable in the sse-only configuration).
3. `make doc-check` — zero rustdoc warnings. This is what the constant relocation and the
   `#[deprecated]` can break, and `make quality-gate` does NOT cover it.
4. `cargo build --example t03_sse_optimized --features sse` — explicit; `make test-examples` cannot
   fail here and is not accepted as evidence.
5. `cargo nextest run --features full` — full suite green.
6. `make lint` — zero warnings (this is what a missing `allow(deprecated)` site or a stray
   `let mut response` would red).
7. `make quality-gate` — exits 0.
8. `cargo semver-checks` — no update required, zero removed public items.

**Concurrency caveat.** Wave 1 shares one worktree (`use_worktrees: false`). Every git assertion here
is scoped to a file this plan owns exclusively. Do not add any assertion of the form "the worktree is
globally clean" — plans 01/02/04 are running in it.
</verification>

<success_criteria>
- No `.text().await` remains in `src/shared/sse_optimized.rs` code; the read goes through an overflow-safe `chunk()` loop bounded by the crate's single SSE ceiling
- The caller's response binding is immutable and the helper's parameter is `mut` — no `unused_mut`
- The relocated constant's rustdoc link is fully qualified and `make doc-check` is green
- `WHOLE_BODY_ALLOWLIST` is empty and its pin asserts length 0
- The accumulation allowlist has a reviewed entry naming the running-total mechanism, and the stale sentence in the old entry is corrected
- The tripwire scope fence is byte-unchanged
- Four tests cover over-cap (no `Content-Length`), exactly-at-cap, declared-over-cap, and under-cap delivery through `connect_sse` to `recv_tx`
- `OptimizedSseTransport` is deprecated, `OptimizedSseConfig` is not, and both remain exported
- The example builds under `--features sse` by explicit command
- `cargo semver-checks` confirms additivity by a real run
</success_criteria>

<output>
Create `.planning/phases/113.1-merge-unblock/113.1-03-SUMMARY.md` when done.

It MUST record: the verbatim negative-control output from Task 2 Part 2 (or an `UNTESTED RESIDUAL
RISK` entry); the measured accumulation counts the tripwire reported (not the planned ones); the
`make doc-check` result; and the literal `cargo semver-checks` result.
</output>
