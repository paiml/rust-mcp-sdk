---
phase: 113.1-merge-unblock
plan: 06
type: execute
wave: 3
depends_on: ["113.1-02", "113.1-03", "113.1-04", "113.1-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md
autonomous: true
requirements: [HTTP-09]
roadmap_success_criteria: [SC-1, SC-2, SC-3, SC-4, SC-5]   # SC-1 recorded as SC-1a discharged / SC-1b human handoff
closes_decisions: [D-17, D-20]

must_haves:
  truths:
    - "HTTP-09 reads [x] in .planning/REQUIREMENTS.md, and its requirement TEXT is byte-unchanged — it closed on the merits, not by narrowing"
    - "The [~] publication-hold note no longer contradicts itself by naming HTTP-09 as the [ ] exception"
    - "The ROADMAP's Phase-113 warning block no longer asserts two claims this phase falsifies"
    - "The ROADMAP is consistent in ALL THREE places it describes Phase 113.1: the top-level v2.5 bullet, the six-plan list, and the progress table row"
    - "SC-1 is recorded as split — SC-1a (local exact gate green) discharged and asserted; SC-1b (org-required `gate` status green on the branch) recorded as a NAMED HUMAN HANDOFF this phase deliberately cannot discharge"
    - "D-113-Q, D-113-R and D-113-U are marked RESOLVED with real commit shas"
    - "The tree is MERGEABLE: pmat quality-gate and make quality-gate are both green, with the PMAT invocation run explicitly, and the fuzz campaign is recorded rather than waived"
    - "The [~] publication hold on HTTP-01..08 / CLNT-01/02/05 is untouched"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "HTTP-09 closed at all three sites"
      contains: "[x] **HTTP-09**"
    - path: ".planning/ROADMAP.md"
      provides: "a Phase-113 warning block that no longer states falsified claims, plus a consistent Phase-113.1 record in all three locations"
  key_links:
    - from: ".planning/REQUIREMENTS.md HTTP-09 traceability row"
      to: "Phase 113.1"
      via: "the owning-phase column"
      pattern: "HTTP-09 \\| Phase 113.1"
---

<objective>
Close HTTP-09 on the merits and leave a MERGEABLE tree.

Purpose: HTTP-09 has reopened three times, each round capping the sites that round's review named
while the next review found another. Its closure condition is deliberately **enumerable, not
narrative** — a tripwire test asserting the invariant, not a claim that the work was done. Plans 02
and 03 supplied the two missing pieces (clause 3's falsifiable O(n) guards and clause 1's last
unbounded read), and plan 05 cleared the PR-blocking complexity gate locally. This plan records the
closure, corrects every planning document that now asserts a falsified claim, and runs the phase gate.

**HTTP-09 is the explicit exception to the `[~]` publication hold.** Its own text in
`.planning/REQUIREMENTS.md` says so: *"a genuine open gap, not a publication-gated one, and it does
not clear on 2026-07-28."* So it flips to `[x]`, not `[~]` (D-17) — while the hold on HTTP-01..08 and
CLNT-01/02/05 stands untouched, as a recorded decision made by Guy Ernest on 2026-07-27.

## SC-1 is SPLIT. Read this before writing a word of the closure record.

ROADMAP Success Criterion 1 (`.planning/ROADMAP.md:2393`) reads:

> `pmat quality-gate --fail-on-violation --checks complexity` passes with no `src/` function over
> cog 25, **and the org-required `gate` status check is green on the branch** (D-113-U)

D-20 says the phase ends at a MERGEABLE tree and that *"pushing, opening the PR to
`paiml/rust-mcp-sdk`, and merging stay separate human actions."* The org `gate` check runs on the
pushed head of PR #299. **These two cannot both be satisfied by this phase.** D-20 is the locked user
decision, it is the later and more specific one, and it explicitly reserves the push. So D-20 governs,
and SC-1 is recorded as two halves:

- **SC-1a — locally provable, DISCHARGED here.** `pmat quality-gate --fail-on-violation --checks complexity`
  passes with no `src/` function over cog 25, run explicitly from the repo root, literal output
  recorded. Plans 01 and 05 did the work; this plan re-runs and records it.
- **SC-1b — remote, NOT discharged, a NAMED HUMAN HANDOFF.** The org-required `gate` status check
  turning green on the branch requires a push to `paiml/rust-mcp-sdk`. That is outside this phase by
  D-20. It is recorded in the SUMMARY as an explicit outstanding handoff with the exact steps, not
  quietly claimed.

**Do NOT weaken SC-1's text in the ROADMAP** — the criterion is right; what changes is the honest
accounting of which half this phase closes. **Do NOT write "SC-1 met" anywhere.** Writing SC-1a met
and SC-1b pending-human is the truthful record, and this plan's entire job is the truthfulness of the
project record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md
@.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md
@.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md
@.planning/phases/113.1-merge-unblock/113.1-02-SUMMARY.md
@.planning/phases/113.1-merge-unblock/113.1-03-SUMMARY.md
@.planning/phases/113.1-merge-unblock/113.1-04-SUMMARY.md
@.planning/phases/113.1-merge-unblock/113.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the evidence, flip HTTP-09 to [x], correct the ROADMAP warning block, and mark the three defects RESOLVED</name>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` `:22-33` — the `[~]` status-marker note, whose third sentence currently reads *"HTTP-09 is the exception: it is `[ ]`, not `[~]`"*. Flipping the bullet without this sentence leaves the file self-contradicting.
    - `.planning/REQUIREMENTS.md` `:54` — the HTTP-09 bullet, ending `— *NOT met; see below*`.
    - `.planning/REQUIREMENTS.md` `:55-70` — the "Why HTTP-09 exists" block explaining the three reopenings. It stays; it is the reason the closure condition is enumerable.
    - `.planning/REQUIREMENTS.md` `:211` — the traceability row `| HTTP-09 | Phase 113 | **NOT met** — needs the bounded-read tripwire test |`, and the neighbouring rows for their format.
    - `.planning/REQUIREMENTS.md` `:20` — a completed sibling (VERS-09) showing the `[x]` form: no trailing status clause.
    - `.planning/ROADMAP.md` `:2500-2520` — the Phase-113 warning block. (CONTEXT cites `:2473-2491`; RESEARCH § Corrections measured the true range as `:2500-2520`. Both quoted sentences are present verbatim.)
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` `:1-20` — `## D-113-A`'s `**Status:**` line, the RESOLVED convention: `✅ **RESOLVED** in plan NN, commit \`sha\``.
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` `:858` (`## D-113-Q`), `:890` (`## D-113-R`), `:1117` (`## D-113-U`) — the three `**Status:**` lines to update.
    - `.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md` § D-20 — the exact two sentences this phase falsifies.
    - All four sibling SUMMARYs (02, 03, 04, 05) — the evidence being relied on.
  </read_first>

  <action>
### Part 1 — verify the evidence rather than assuming the upstream plans delivered

HTTP-09 decomposes into three enumerable clauses; confirm each mechanically before touching a
checkbox:

- Clause 1 — no unbounded whole-body read in scope outside a reviewed allowlist, **and the allowlist
  is now empty**: `cargo test --test v2_bounded_reads_tripwire --features full no_unbounded_whole_body_read_over_peer_supplied_bytes`
  passes, and `WHOLE_BODY_ALLOWLIST` has length 0.
- Clause 2 — no unreviewed accumulation over peer bytes: `every_peer_byte_accumulation_is_reviewed`
  passes with the new `chunk()` loop's population reviewed.
- Clause 3 — no scan over peer-chosen input worse than O(n): both
  `sse_parser_feed_1b_chunks_stays_within_its_linear_time_budget` and
  `sse_parser_feed_cost_grows_linearly_not_quadratically` pass, AND plan 02's SUMMARY records BOTH
  their pre-fix RED values and their post-fix negative-control RED values. **A guard that was never
  demonstrated RED does not close this clause** — that is the exact failure the existing 4 KiB budget
  test represents.

If any clause is not demonstrably closed, STOP and report rather than flipping. HTTP-09's whole
history is of being closed prematurely.

### Part 2 — REQUIREMENTS.md: three sites, and only these three

1. `:54` — change `- [ ] **HTTP-09**:` to `- [x] **HTTP-09**:` and delete the trailing
   `— *NOT met; see below*` clause, matching the completed-sibling form at `:20`.
2. `:22-33` — rewrite the sentence naming HTTP-09 as the `[ ]` exception. It must now say that
   HTTP-09 was the exception to the hold **and has been closed on the merits by Phase 113.1**, so the
   note no longer contradicts the bullet. Everything else in that block — the `[~]` hold, the
   `## Verdict: PENDING` reference, the "re-run the checkpoint" instruction — stays untouched.
3. `:211` — the traceability row becomes `| HTTP-09 | Phase 113.1 | ... |` with a status naming the
   enforcing mechanism rather than a narrative: the bounded-read tripwire with an EMPTY whole-body
   allowlist, plus the two falsifiable O(n) guards on `SseParser::feed`. Note the owning-phase column
   moves from `Phase 113` to `Phase 113.1`.

**Do NOT edit HTTP-09's requirement TEXT.** The phase's own success criterion 4 forbids closing it by
narrowing the requirement, and the text is what the tripwire's scope fence mirrors verbatim.
**Do NOT touch any `[~]` requirement.** HTTP-01..08 and CLNT-01/02/05 stay `[~]` — that is a recorded
decision (`113-SPEC-RECHECK.md` § Third Outcome Policy, `hold`, Guy Ernest, 2026-07-27), not a
default, and the branch merges with it in place.
**Do NOT touch `## Verdict`** in `113-SPEC-RECHECK.md`; it stays `PENDING`.

### Part 3 — the ROADMAP's Phase-113 warning block (`:2500-2520`)

Two sentences in it are now false:

- *"**HTTP-09 remains `[ ]` on the merits** — D-113-R (a quadratic scan over peer-chosen input)
  violates its explicit O(n) clause and is unowned."* — D-113-R is closed by plan 113.1-02 and
  HTTP-09 is `[x]`.
- *"Two violations remain and still block merge: `handle_post_fast_path` (cog 30) and
  `handle_post_with_middleware` (cog 31)..."* — both are closed by plans 113.1-01 and 113.1-05.

Replace them with the measured post-phase facts: HTTP-09 is closed on the merits by Phase 113.1 with
the enforcing mechanism named; both handlers now measure at the values plan 05's SUMMARY records, and
`pmat quality-gate --fail-on-violation --checks complexity` passes **locally** — say "locally", and
add the SC-1b sentence: the org-required `gate` status on PR #299 still requires a human push, which
D-20 keeps outside this phase.

**Do not delete the block** — its history is why the `[~]` hold is legible. Keep the `[~]`
publication-hold paragraph in the same block entirely untouched, including the `## Verdict`
still-`PENDING` statement, the "both arms must be run" sentence, and the condition-not-date trigger.
If plan 04 recorded arm 2 as run with no drift, add one sentence noting arm 2 has been executed and
where its record lives — but arm 1 stays open and the obligation stays undischarged. If plan 04 took
its Branch B (real upstream drift), instead note the drift, its Phase-118 owner, and that
`v2_conformance_pin` is expected-red pending that phase.

### Part 4 — the deferred items

Update three `**Status:**` lines to the file's own RESOLVED convention, each naming the plan AND a
real commit sha (take the shas from `git log`, do not invent them; verify each with
`git cat-file -t <sha>` returning `commit`):

- `## D-113-Q` (`:858`) — resolved in plan 113.1-03.
- `## D-113-R` (`:890`) — resolved in plan 113.1-02. Its `**Status:**` currently reads *"OPEN —
  HTTP-09's O(n) clause is not fully dischargeable until this is fixed"*; the replacement should note
  the clause is now discharged and name the two guards that discharge it.
- `## D-113-U` (`:1117`) — resolved in plans 113.1-01 and 113.1-05, naming both handlers' final
  measured complexity. Note that its third violation, `write_canonical`, was already closed separately
  in `58f82368`. Note also that the resolution is the LOCAL gate (SC-1a); the remote `gate` status is
  a separate human action.

Leave every other deferred item's status alone — in particular D-113-S and D-113-T remain OPEN and
unowned, plan 04's new auth-surface entry stays OPEN with Phase 116 as owner, and the amended
`D-113-J` stays OPEN with its documentation owner.

**Do NOT** mark Phase 113 itself as closed or flip any `[~]` requirement. Phase 113 remains HELD on
its publication gate; this phase closes a merge blocker, not that hold.
**Do NOT** touch the Phase-113.1 plan checkboxes, the progress table, or the top-level v2.5 bullet in
this task — those are Task 3's, deliberately, because plan 06's own SUMMARY does not exist yet and
ticking its box now would leave the list permanently at 5/6.
  </action>

  <verify>
    <automated>cargo test --test v2_bounded_reads_tripwire --features full &amp;&amp; cargo test --lib --features full sse_parser &amp;&amp; grep -q '^- \[x\] \*\*HTTP-09\*\*' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'NOT met' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'HTTP-09 remains `\[ \]` on the merits' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*HTTP-09\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c 'NOT met' .planning/REQUIREMENTS.md` returns 0 (both occurrences were HTTP-09's; verified against the pre-plan file)
    - `grep -c 'HTTP-09 | Phase 113.1' .planning/REQUIREMENTS.md` returns 1, and `grep -c 'HTTP-09 | Phase 113 ' .planning/REQUIREMENTS.md` returns 0
    - The HTTP-09 requirement text between `**HTTP-09**:` and the end of the sentence is byte-identical to its pre-plan content apart from the removed trailing status clause — verify with `git diff .planning/REQUIREMENTS.md` and confirm no word of the requirement itself changed
    - ``grep -c 'is the exception: it is `\[ \]`' .planning/REQUIREMENTS.md`` returns 0 — the self-contradicting sentence was updated
    - `grep -c '^- \[~\] \*\*HTTP-0' .planning/REQUIREMENTS.md` returns 8 — HTTP-01..08 are untouched
    - `grep -c '^- \[~\] \*\*CLNT-0' .planning/REQUIREMENTS.md` is unchanged from its pre-plan value
    - `grep -c 'HTTP-09 remains' .planning/ROADMAP.md` returns 0 and `grep -c 'Two violations remain and still block merge' .planning/ROADMAP.md` returns 0
    - The replacement warning-block text contains the word `locally` in the sentence about the PMAT gate, and a sentence naming the org-required `gate` status as still requiring a human push
    - The ROADMAP warning block still contains the `[~]` publication-hold paragraph: `grep -c 'Third Outcome Policy' .planning/ROADMAP.md` is unchanged from its pre-plan value, and ``grep -c 'still `PENDING`' .planning/ROADMAP.md`` is unchanged
    - The `## D-113-Q`, `## D-113-R` and `## D-113-U` entries each have a `**Status:**` line containing `RESOLVED` and a git sha that `git cat-file -t <sha>` resolves to `commit`
    - `grep -c 'Status:\*\* OPEN' .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` is greater than 0 — D-113-S, D-113-T, plan 04's new entry and D-113-J were not swept
    - `cargo test --test v2_bounded_reads_tripwire --features full` exits 0 with 13 tests passing
    - Plan 02's SUMMARY is confirmed to contain BOTH the pre-fix RED values and the post-fix negative-control RED values for the two perf guards; the fact of that confirmation is recorded in this plan's SUMMARY
    - `git diff .planning/ROADMAP.md` shows no change to any requirement checkbox and no change to the Phase 114-119 sections
  </acceptance_criteria>

  <done>HTTP-09 reads `[x]` at the bullet and in the traceability table with Phase 113.1 as owner, the marker note is internally consistent, the requirement text is unnarrowed, the ROADMAP's two falsified sentences are replaced with measured facts that say "locally" and name the remote handoff, the publication hold is intact, and the three closed defects carry RESOLVED statuses with verifiable shas.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase gate — including the MANDATORY fuzz campaign — and write the closure SUMMARY</name>

  <files>.planning/phases/113.1-merge-unblock/113.1-06-SUMMARY.md</files>

  <read_first>
    - `CLAUDE.md` § "CI Quality Gates" — the exact PR-blocking invocation, the PMAT 3.15.0 pin, the org-required `gate` aggregate job, and the prohibition on weakening the gate.
    - `CLAUDE.md` § "ALWAYS Requirements for New Features" — fuzz, property, unit, example. Fuzz is mandatory, not best-effort.
    - `AGENTS.md` — the PMAT quality-proxy / PDMT-todo / contract-first sequence, so the SUMMARY can record accurately how this phase related to it (see Part 3).
    - `.planning/phases/113.1-merge-unblock/113.1-VALIDATION.md` § "Sampling Rate" and § "Manual-Only Verifications".
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Validation Architecture" — the verified composition of `make quality-gate` (PMAT appears nowhere) and § "Pitfall 6" (PR #299 is already OPEN and behind; the remaining human action is pushing).
    - All four sibling SUMMARYs, for the measurements this task aggregates — including plan 04's, in case it took Branch B and flagged `v2_conformance_pin` as expected-red.
  </read_first>

  <action>
### Part 1 — the gate, in order, every result recorded verbatim

This is the evidence that the tree is mergeable; D-20's bar is a mergeable tree, not a merged one.

1. `pmat quality-gate --fail-on-violation --checks complexity` — **must PASS**. Run it EXPLICITLY:
   RESEARCH verified at `Makefile:673-690` that `make quality-gate` does not include PMAT (Phase 75
   D-07, deliberate), which is precisely how D-113-U reached CI unnoticed. It costs 0.5 s. This
   discharges **SC-1a**.
2. `pmat analyze complexity -p . --format json` — both handlers at cognitive 20 or below. The gate
   alone only proves 25.
3. `make quality-gate` — exits 0. It runs fmt-check, lint, build, test-all, pmcp-package-gate, audit,
   unused-deps, check-todos, check-unwraps, validate-always, purity-check and comply.
4. `cargo nextest run --features full` — full suite green; record the passed/failed counts, as this
   phase's sibling SUMMARYs do. If plan 04 recorded `v2_conformance_pin` as expected-red under its
   Branch B, that single test is the one documented exception — name it explicitly with its
   Phase-118 owner rather than letting it read as an unexplained failure.
5. `cargo test --test v2_bounded_reads_tripwire --features full` — 13 tests green.
6. `make doc-check` — zero rustdoc warnings. Plan 03 relocated a constant carrying an intra-doc link
   and added a `#[deprecated]`; `make quality-gate` does NOT cover rustdoc.
7. `cargo semver-checks` — no update required, zero removed public items. Record the literal pass
   count; the milestone's additivity claim depends on it and plan 03 added a `#[deprecated]`.
8. `cargo build --example t03_sse_optimized --features sse` — explicit. `make test-examples` swallows
   failures into a yellow "skipped" message and carries no `-D warnings`, so it is not evidence.
9. `RUSTUP_TOOLCHAIN=nightly cargo fuzz run subscription_listen_frames -- -runs=20000` —
   **MANDATORY.** Inspect the OUTPUT, not only the exit code. `make test-fuzz` reports success even
   when no target builds (D-113-G), so it is never a substitute. The toolchain was verified present
   during replanning (`nightly-aarch64-apple-darwin` installed; `cargo-fuzz 0.13.1` on `PATH`), so
   **"nightly unavailable" is not an accepted outcome**: if the campaign cannot run, this phase is
   INCOMPLETE and closure is blocked — record the literal error and STOP rather than closing with a
   note. (Plan 02 already ran this campaign; re-running it here at the integrated head is the phase
   gate's own evidence. If plan 02's campaign covers the identical head sha, cite it rather than
   duplicating the run, and say which.)
10. `cargo check --no-default-features --features sse` — exits 0 (the configuration plan 03's constant
    relocation exists to serve).

**CLAUDE.md's ALWAYS requirements, checked explicitly.** Confirm and record where each is satisfied
for this phase's changes: **fuzz** — the `subscription_listen_frames` campaign; **property** —
`property_feed_chunking_invariance`; **unit** — the two perf guards, the newline-free invariant test,
and the over/at/declared/under-cap set; **example** — `t03_sse_optimized` builds under `--features sse`.
If any is not genuinely satisfied, say so instead of asserting it.

### Part 2 — write the closure record into the SUMMARY

It must contain:

- **The five ROADMAP success criteria, each with the specific evidence that discharges it — and SC-1
  split.** Write SC-1 as two rows:
  - `SC-1a — DISCHARGED`: the literal passing `pmat quality-gate --fail-on-violation --checks complexity`
    output plus the per-function numbers.
  - `SC-1b — NOT DISCHARGED, HUMAN HANDOFF`: the org-required `gate` status check cannot turn green
    without pushing this branch to `paiml/rust-mcp-sdk` and updating PR #299, which D-20 reserves as a
    separate human action. Spell out the handoff steps (push the branch; the existing PR #299 picks up
    the new head; the `gate` aggregate job runs; merge only on green). Do NOT mark SC-1 met.
- the measured before/after complexity for both handlers (30/31 → final);
- the measured RED and GREEN values for both new O(n) guards — pre-fix RED, post-fix negative-control
  RED, and GREEN — so HTTP-09's clause 3 closure is falsifiable on the record rather than asserted;
- the `WHOLE_BODY_ALLOWLIST` length (0) and the reviewed accumulation entry;
- the arm-2 verdict from plan 04, including which branch it took;
- the fuzz campaign's seed, run count, exit code and artifacts-dir state;
- **and, explicitly, what remains open**: the `[~]` publication hold on HTTP-01..08 / CLNT-01/02/05
  with arm 1 still blocked on the condition "a versioned schema directory exists"; D-113-S and
  D-113-T; plan 04's new auth-surface entry (Phase 116) and the amended D-113-J; any Phase-118 drift
  item; and SC-1b — **pushing, opening/updating PR #299 against `paiml/rust-mcp-sdk`, and merging are
  separate human actions outside this phase.**

### Part 3 — record the AGENTS.md process divergence, do not retrofit process

`AGENTS.md` prescribes a PMAT quality-proxy for all file writes, PDMT-generated todos, and an explicit
contract-first pre/post `pmat comply` sequence. Phase 113's own shipped plans (113-29 … 113-32) do not
invoke any of the three — verified by grep — and `pmat comply` runs inside `make quality-gate` as its
final step, which every plan in this phase runs. So this phase followed the established house style.

Record that as a one-paragraph **divergence note** in the SUMMARY: what AGENTS.md prescribes, what the
house style actually does, that `comply` is covered via `make quality-gate`, and that reconciling the
two is repo-wide process work with no home in a three-blocker merge unblock. **Do not add
quality-proxy or PDMT steps to this phase** — a merge unblock is the wrong place to relitigate
repo-wide process, and adding unexercised process would be its own kind of false record.

**Do NOT** push, open or update any PR. **Do NOT** weaken, disable or skip any gate to reach green —
if something fails, record the failure and stop.
  </action>

  <verify>
    <automated>pmat quality-gate --fail-on-violation --checks complexity &amp;&amp; make quality-gate &amp;&amp; make doc-check &amp;&amp; cargo test --test v2_bounded_reads_tripwire --features full &amp;&amp; cargo build --example t03_sse_optimized --features sse &amp;&amp; cargo check --no-default-features --features sse</automated>
  </verify>

  <acceptance_criteria>
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0 and its literal output is in the SUMMARY
    - `make quality-gate` exits 0
    - `make doc-check` exits 0 with zero rustdoc warnings
    - `cargo nextest run --features full` exits 0, with the passed/failed counts recorded — or, if plan 04 took Branch B, with `v2_conformance_pin` named as the single documented expected-red test with its Phase-118 owner
    - `cargo test --test v2_bounded_reads_tripwire --features full` exits 0 with 13 passing
    - `cargo semver-checks` reports no update required, with the literal pass count recorded
    - `cargo build --example t03_sse_optimized --features sse` exits 0
    - `cargo check --no-default-features --features sse` exits 0
    - The SUMMARY records the fuzz campaign result (seed, runs, exit code, artifacts state), or cites plan 02's campaign at the identical head sha. **A "MISSING EVIDENCE" note is NOT an acceptable outcome** — the toolchain is present, so an unrunnable campaign is a blocking failure to report.
    - The SUMMARY maps the five ROADMAP success criteria to specific evidence, with SC-1 written as SC-1a DISCHARGED and SC-1b NOT DISCHARGED / HUMAN HANDOFF, and the string `SC-1 met` appears nowhere
    - The SUMMARY contains an explicit "what remains open" section naming the `[~]` hold, arm 1, D-113-S, D-113-T, plan 04's new entry, D-113-J, and the human-only push/PR/merge actions
    - The SUMMARY contains the AGENTS.md divergence note
    - **No push or PR mutation occurred.** Record the statement explicitly: no `git push`, `gh pr create`, `gh pr edit`, `gh pr merge` or equivalent was invoked during this phase, and the branch's upstream tracking ref is unchanged. Do NOT try to prove this with `git log` — push state is not represented in commit history, so a `git log` assertion would be unverifiable theatre. The recorded statement plus the absence of those commands in the session transcript is the evidence.
  </acceptance_criteria>

  <done>The full phase gate — including the PMAT invocation `make quality-gate` does not cover, the rustdoc check it also does not cover, and the mandatory fuzz campaign — is green and recorded; the SUMMARY exists on disk with the five criteria mapped to evidence, SC-1 split honestly, what remains open stated plainly, and the process divergence recorded; the tree is mergeable and no human-only action was taken.</done>
</task>

<task type="auto">
  <name>Task 3: Final bookkeeping — bring all three ROADMAP records of Phase 113.1 to 6/6 and consistent</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
    - `.planning/ROADMAP.md` `:2220` (approximately) — the top-level v2.5 phase-list bullet, currently `- [ ] **Phase 113.1: Merge Unblock** — the branch cannot merge and nothing downstream can ship until three things clear...` describing all three blockers as open. Locate by the `**Phase 113.1: Merge Unblock**` string, not by line number.
    - `.planning/ROADMAP.md` `:2376-2400` — the `### Phase 113.1: Merge Unblock (INSERTED)` section, its five success criteria, and its six-plan / three-wave list with unchecked boxes.
    - `.planning/ROADMAP.md` `:2502` (approximately) — the progress table row `| 113.1 Merge Unblock | 0/TBD | Not started | - |`. Locate by the `113.1 Merge Unblock` string. Note the neighbouring rows' format for the Completed column (`2026-07-23`, `2026-07-27`).
    - `.planning/phases/113.1-merge-unblock/` — confirm which SUMMARYs exist on disk right now. Plan 06's own SUMMARY was written in Task 2, so all six should be present.
    - This plan's own `113.1-06-SUMMARY.md` as Task 2 left it — Task 3's outcome gets appended to it.
  </read_first>

  <action>
This task exists because the previous revision of this plan ticked plan checkboxes in a task that ran
BEFORE plan 06's own SUMMARY was written, which made 6/6 unreachable — the list could only ever read
5/6. Task 2 wrote the SUMMARY; this task does the bookkeeping afterwards, so every record can be
correct simultaneously. The ROADMAP describes Phase 113.1 in **three** places and all three must
agree.

**Site 1 — the six-plan list (`:2376-2400` section).** Tick each `- [ ]` box whose plan has a SUMMARY
on disk. With Task 2 complete, that is all six including `113.1-06-PLAN.md`. Verify each SUMMARY
actually exists before ticking its box; leave unticked any that does not and say so in the SUMMARY.
If a plan's actual scope diverged from its one-line description, correct the description to match what
shipped rather than leaving the roadmap describing an intention. Two descriptions are known to need
review: plan 02's (it now lands the guards and the fix as ONE commit with two recorded RED
measurements) and plan 04's (it UPDATES `D-113-J` rather than creating a new PMAT-recipe entry).

**Site 2 — the progress table row (`:2502`).** Change
`| 113.1 Merge Unblock | 0/TBD | Not started | - |` to `| 113.1 Merge Unblock | 6/6 | Complete | <YYYY-MM-DD> |`
using the actual completion date, matching the neighbouring rows' format. Note what that column means
here, consistent with the existing warning above the table: **"Complete" counts PLANS, not the remote
CI status.** SC-1b remains a human handoff, and the warning block Task 1 rewrote already says so.

**Site 3 — the top-level v2.5 phase bullet (`:2220`).** It currently reads `- [ ] **Phase 113.1: Merge
Unblock** — the branch cannot merge...` and describes D-113-U, D-113-R and D-113-Q as all open, with
`sse_optimized.rs:266`'s read "currently allowlisted `NOT BOUNDED`". Every one of those clauses is now
false. Flip the box to `- [x]` and rewrite the body to the shipped facts, keeping the bullet's existing
density and shape (the sibling Phase-112 and Phase-113 bullets are the format model):

- D-113-U closed — both handlers extracted to their measured final cognitive values;
  `pmat quality-gate --fail-on-violation --checks complexity` passes **locally**, with the org-required
  `gate` status on PR #299 still pending a human push (D-20).
- D-113-R closed — the scan-window cursor plus the `debug_assert` removal, guarded by two falsifiable
  O(n) tests plus a chunking-invariance property.
- D-113-Q closed — `connect_sse` bounded by a `chunk()` running total against the crate's 16 MiB SSE
  ceiling; `WHOLE_BODY_ALLOWLIST` is now EMPTY.
- HTTP-09 `[x]` on the merits.
- The auth-surface reads recorded and assigned to Phase 116 (four files, not three — correct that
  claim while you are in the sentence; the bullet currently says three).

**Then append a short bookkeeping section to `113.1-06-SUMMARY.md`** recording: which of the six
SUMMARYs were confirmed on disk; the three ROADMAP sites edited; and a final restatement that SC-1b is
outstanding as a human handoff.

**Do NOT** touch any requirement checkbox (Task 1 owns those), the Phase 114-119 sections, or the
Phase-113 row and its warning block beyond what Task 1 already rewrote. **Do NOT** mark Phase 113
itself complete — it stays HELD on its publication gate.
  </action>

  <verify>
    <automated>grep -q '| 113.1 Merge Unblock | 6/6 | Complete' .planning/ROADMAP.md &amp;&amp; grep -q '^- \[x\] \*\*Phase 113.1: Merge Unblock\*\*' .planning/ROADMAP.md &amp;&amp; test "$(grep -c '^- \[ \] 113.1-0' .planning/ROADMAP.md)" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^- \[ \] 113.1-0' .planning/ROADMAP.md` returns 0 and `grep -c '^- \[x\] 113.1-0' .planning/ROADMAP.md` returns 6 — every plan box including 113.1-06 is ticked, and each corresponds to a SUMMARY that exists on disk
    - `grep -c '| 113.1 Merge Unblock | 6/6 | Complete |' .planning/ROADMAP.md` returns 1, with a real date in the Completed column matching the neighbouring rows' `YYYY-MM-DD` format
    - `grep -c '| 113.1 Merge Unblock | 0/TBD' .planning/ROADMAP.md` returns 0
    - `grep -c '^- \[x\] \*\*Phase 113.1: Merge Unblock\*\*' .planning/ROADMAP.md` returns 1 — the top-level v2.5 bullet is checked
    - The rewritten top-level bullet contains the word `locally` next to the PMAT claim, does not describe any of the three blockers as open, does not still say `currently allowlisted \`NOT BOUNDED\``, and says four files rather than three for the auth-surface reads
    - `grep -c 'TBD (run /gsd:plan-phase 113.1' .planning/ROADMAP.md` returns 0
    - `grep -c 'SC-1 met' .planning/ROADMAP.md` returns 0 and the same grep against `.planning/phases/113.1-merge-unblock/113.1-06-SUMMARY.md` returns 0
    - `git diff .planning/ROADMAP.md` shows no change to any requirement checkbox, to the Phase 114-119 sections, or to the Phase-113 progress-table row
    - `113.1-06-SUMMARY.md` gained a bookkeeping section listing the six confirmed SUMMARYs, the three ROADMAP sites edited, and the SC-1b restatement
  </acceptance_criteria>

  <done>All three ROADMAP descriptions of Phase 113.1 agree and are true: six ticked plans, a `6/6 | Complete` progress row with a date, and a checked top-level bullet whose body states the shipped facts including that the remote gate is still a human handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future readers and future phases | `REQUIREMENTS.md` and `ROADMAP.md` are what Phases 114-119 read to decide what is already true. A checkbox flipped without evidence is a false statement to every downstream phase. |
| local branch → `paiml/rust-mcp-sdk` PR #299 | The org-required `gate` check runs on the pushed head. Pushing is a human action outside this phase, and SC-1b is recorded as outstanding rather than claimed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113.1-50 | Repudiation | HTTP-09 flipped to `[x]` without its three clauses actually closed | mitigate | Task 1 verifies each clause mechanically before editing, including that plan 02's SUMMARY carries BOTH the pre-fix RED values and the post-fix negative-control RED values. A guard never demonstrated RED does not close clause 3 — that is the precise failure the existing 4 KiB budget test represents. |
| T-113.1-51 | Tampering | closing HTTP-09 by narrowing its text or widening the tripwire fence | mitigate | The requirement text is asserted byte-unchanged; the scope fence was asserted unchanged by plan 03; D-18's deferral of the auth reads is recorded rather than absorbed. |
| T-113.1-52 | Tampering | the `[~]` publication hold being swept along with HTTP-09's flip | mitigate | Task 1's criteria pin the `[~]` counts and keep the hold paragraph and the `PENDING` verdict intact. The hold is a recorded decision, not a default. |
| T-113.1-53 | Repudiation | a green `make quality-gate` mistaken for a green complexity gate, or for a green rustdoc build | mitigate | PMAT is run EXPLICITLY as gate step 1 and `make doc-check` as step 6, both with literal output recorded. RESEARCH verified `make quality-gate` contains neither — the exact mechanism by which D-113-U reached CI unnoticed. |
| T-113.1-54 | Repudiation | `make test-fuzz` or `make test-examples` accepted as evidence, or fuzz evidence waived entirely | mitigate | Both are measured-incapable of failing here (D-113-G; the swallowed-failure recipe with no `-D warnings`). The gate runs the fuzz target and the example build explicitly, and the fuzz campaign is a HARD completion condition — nightly and `cargo-fuzz 0.13.1` were verified present, so an unrunnable campaign is a blocking failure, not a note. |
| T-113.1-55 | Elevation of Privilege | taking a human-only action (push / PR / merge) | mitigate | D-20 scopes the phase to a mergeable tree. Task 2 forbids push and PR operations and records a positive statement that none was invoked — rather than a `git log` assertion, which cannot represent push state. |
| T-113.1-56 | Repudiation | claiming SC-1 met when only its local half was proven, or leaving the ROADMAP describing solved blockers as open | mitigate | SC-1 is recorded as SC-1a DISCHARGED / SC-1b HUMAN HANDOFF, and `SC-1 met` is asserted absent from both the ROADMAP and the SUMMARY. Task 3 brings all three ROADMAP descriptions of the phase into agreement, so no site is left stale. |
| T-113.1-SC | Tampering | cargo installs | accept | Nothing is installed. `pmat` stays pinned at 3.15.0 and must not be upgraded inside this phase. |
</threat_model>

<verification>
The phase gate is Task 2 in full. In summary, from the repo root:

1. `pmat quality-gate --fail-on-violation --checks complexity` — PASS (not covered by `make quality-gate`); discharges SC-1a only
2. `pmat analyze complexity -p . --format json` — both handlers at 20 or below
3. `make quality-gate` — exits 0
4. `cargo nextest run --features full` — full suite green
5. `cargo test --test v2_bounded_reads_tripwire --features full` — 13 green
6. `make doc-check` — zero rustdoc warnings (also not covered by `make quality-gate`)
7. `cargo semver-checks` — no update required
8. `cargo build --example t03_sse_optimized --features sse` — exits 0
9. `cargo check --no-default-features --features sse` — exits 0
10. `RUSTUP_TOOLCHAIN=nightly cargo fuzz run subscription_listen_frames -- -runs=20000` — MANDATORY, output inspected

SC-1b is verified by a human after pushing. It is not verifiable from this tree and is not asserted here.
</verification>

<success_criteria>
- HTTP-09 reads `[x]` at the bullet and in the traceability table, owned by Phase 113.1, with its requirement text unnarrowed
- The `[~]` marker note no longer contradicts itself, and every `[~]` requirement is untouched
- The ROADMAP's two falsified sentences are replaced with measured facts that say "locally"; the publication-hold paragraph is intact
- All three ROADMAP records of Phase 113.1 agree: six ticked plans, `6/6 | Complete` with a date, and a checked top-level bullet stating the shipped facts
- D-113-Q, D-113-R and D-113-U carry RESOLVED statuses with verifiable commit shas; D-113-S, D-113-T, D-113-J and plan 04's entry stay OPEN
- The full phase gate is green, including the explicit PMAT invocation, `make doc-check`, and a fuzz campaign that actually ran
- The SUMMARY maps the five success criteria to evidence with SC-1 split into SC-1a (discharged) and SC-1b (human handoff), states plainly what remains open, and records the AGENTS.md process divergence
- No push, PR or merge was performed, and that is recorded as a statement rather than inferred from commit history
</success_criteria>

<output>
Create `.planning/phases/113.1-merge-unblock/113.1-06-SUMMARY.md` in **Task 2** — it is the phase's
closure record, and it must exist on disk before Task 3 can honestly tick plan 06's own box.

It MUST contain: the five success criteria each mapped to specific evidence, with SC-1 split into
SC-1a (DISCHARGED, with the literal passing gate output) and SC-1b (NOT DISCHARGED — human handoff,
with the steps spelled out); the measured complexity trajectory; the measured pre-fix RED, post-fix
negative-control RED and GREEN values for both O(n) guards; the `cargo semver-checks` result; the
`make doc-check` result; the fuzz campaign result; the AGENTS.md divergence note; and an explicit
"what remains open" section.

Task 3 appends the bookkeeping section: the six confirmed SUMMARYs, the three ROADMAP sites edited,
and the SC-1b restatement.
</output>
