---
phase: 113.1-merge-unblock
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md
  - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md
autonomous: true
requirements: [HTTP-09]
roadmap_success_criteria: [SC-5]
closes_decisions: [D-18, D-19]

must_haves:
  truths:
    - "All unbounded auth-surface reads are enumerated by file, with BOTH the raw needle-match count and the reviewed-unbounded subset recorded, the threat-model distinction stated, and Phase 116 named as owner"
    - "The already-existing D-113-J entry gains an owner and a status — no duplicate PMAT-recipe entry is created"
    - "Arm 2 of Phase 113's rolled-forward re-verification obligation is EXECUTED against upstream HEAD, not against the pin, and its result is recorded"
    - "The recorded arm-2 result distinguishes 'we checked and found no drift' from 'nobody checked'"
    - "Nothing in this plan blocks the merge: an arm-2 drift finding becomes a new deferred item owned by Phase 118, and a v2_conformance_pin failure caused by real upstream drift is recorded and transferred, not treated as a phase failure"
  artifacts:
    - path: ".planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md"
      provides: "the auth-read deferral with Phase 116 as owner, plus an UPDATED D-113-J owner/status line"
      contains: "Phase 116"
    - path: ".planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md"
      provides: "the arm-2 run record appended under section B.6"
      contains: "B.6.5"
  key_links:
    - from: "the new deferred item"
      to: "Phase 116 (Auth Hardening SEPs)"
      via: "an explicit Owner line, not UNASSIGNED"
      pattern: "Owner:.*Phase 116"
    - from: "the existing D-113-J entry"
      to: "an actual owning phase"
      via: "its Owner line changing away from 'unassigned'"
      pattern: "D-113-J"
---

<objective>
Discharge the two record-only obligations of this phase, neither of which depends on any code change.

1. **D-18** — the unbounded whole-body reads on the auth surface are the same defect class as
   D-113-Q but sit at a different trust boundary: a semi-trusted IdP the deployment configured, not an
   arbitrary remote peer. They are outside HTTP-09's scope fence by design. They get ENUMERATED and
   ASSIGNED to Phase 116, whose whole subject (RFC 9207 `iss` validation, DCR `application_type`) is
   re-examining exactly the trust assumption they rest on. They do NOT get swept into HTTP-09 to make
   a number look complete, and the tripwire's fence does NOT get widened.

2. **D-19** — arm 2 of Phase 113's rolled-forward re-verification obligation. Arm 1 is blocked on
   `schema/2026-07-28` not existing upstream; arm 2 (the conformance predicate behind HTTP-08) is not
   publication-gated and is runnable today. Running it means the branch does not merge with a runnable
   obligation undischarged. It is RECORD-ONLY and explicitly non-blocking: if it shows drift, that
   becomes a new deferred item owned by **Phase 118 (Conformance Against the Official Suite)** —
   the phase whose subject is exactly the official conformance suite — because HTTP-08 is already
   held at `[~]` on unrelated grounds.

Serves ROADMAP Phase-113.1 Success Criterion 5. Wave 1, independent of all three code plans — it
touches no source file.

## Wave-1 concurrency note — read before running any `git` assertion

`.planning/config.json` sets `workflow.use_worktrees: false`, so all four Wave-1 plans share **one
worktree on one branch**. Plans 01, 02 and 03 are concurrently editing `src/` and
`tests/v2_bounded_reads_tripwire.rs`. A global `git status --porcelain src/ tests/` will therefore
show THEIR uncommitted work and would false-fail this plan's "record-only" assertion.

The correct mechanism is **commit-scoped**: after each task commits, capture the sha it produced and
assert that the commit's file list contains only `.planning/` paths. That proves this plan changed no
source file regardless of what the other three are doing beside it. The intent is unchanged; only the
mechanism is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md
@.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md
@.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the auth-surface unbounded reads as a deferred item owned by Phase 116, and give the EXISTING D-113-J entry an owner</name>

  <files>.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md</files>

  <read_first>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` `:890-960` — the `## D-113-R` entry. This is the FULLER entry shape (a `**Found during:** / **Severity:** / **Owner:** / **Status:**` block, then a measured table, then "Why the phase's existing bounds do not cover it", "Why not fixed here", "Fix shape"). `## D-113-Q` at `:858` lacks that block — do not copy the thinner one.
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` `:509-560` — **`## D-113-J` in full.** This entry ALREADY records the broken CLAUDE.md PMAT recipe: the exact command, the drifted JSON shape (`.violations` is null at the top level; the array lives at `.summary.violations`; each violation names its file under `.file`, not `.path`), the working substitute query, and `**Owner:** unassigned`. Read it before writing anything about the PMAT recipe.
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` `:1-20` — the file header and `## D-113-A`'s `**Status:**` line, the RESOLVED convention.
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` — scan all `## D-113-*` headings to find the next unused identifier. The file currently runs through `D-113-U`; verify rather than assume.
    - `tests/v2_bounded_reads_tripwire.rs` `:55-70` — the scope fence, so the entry can state accurately why these four files sit outside it.
    - `tests/v2_bounded_reads_tripwire.rs` `:562` — `WHOLE_BODY_NEEDLES`, the needle set to re-measure with.
    - `.planning/REQUIREMENTS.md` — HTTP-09's requirement text, which scopes to a *"peer-controlled read on the v2 transport path"*.
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Security Domain" — the threat-model note the entry must carry: *"the distinction is not severity, it is who chooses the bytes."*
  </read_first>

  <action>
### Deliverable 1 — the auth-surface deferral (NEW entry)

Append a new entry to `deferred-items.md` following `## D-113-R`'s full shape, under the next unused
identifier in the file's own sequence.

**First, re-measure the counts — and record TWO numbers per file, not one.** CONTEXT counted 18
during the discussion and RESEARCH reproduced the total, but this entry becomes the permanent record,
so derive the numbers rather than transcribing them. A raw needle match is a SYNTACTIC count; it is
not automatically an unbounded-read count, because a match may already sit behind a local cap. So for
each of the four files record:

- **raw matches** — occurrences of the tripwire's own `WHOLE_BODY_NEEDLES`
  (`tests/v2_bounded_reads_tripwire.rs:562`); and
- **reviewed-unbounded subset** — of those, the ones you actually inspected and confirmed carry no
  local bound.

State the method in one sentence so the next reader knows the second column is a review result, not a
grep result. If a match turns out to be already bounded, say so and exclude it from the subset rather
than quietly keeping the round number.

The expected result, to be confirmed or corrected:

| File | Expected unbounded whole-body reads |
|---|---|
| `src/server/auth/providers/generic_oidc.rs` | 5 |
| `src/client/auth.rs` | 5 |
| `src/server/auth/providers/cognito.rs` | 4 |
| `src/client/oauth.rs` | 4 |
| **total** | **18** |

If your reviewed subset differs from 18, record the measured number and say so plainly — the
roadmap's "18" and CONTEXT's four-file breakdown are both claims this entry supersedes. Note that the
roadmap text says three files; CONTEXT corrected it to four.

The entry must contain, in this order:

- **The metadata block.** `**Found during:**` plan 113.1-04 (D-18, phase 113.1). `**Severity:**`
  stated honestly against the trust boundary, not inflated to match D-113-Q. `**Owner:** Phase 116
  (Auth Hardening SEPs)` — an actual owner, not `UNASSIGNED`. `**Status:** OPEN`.
- **The measured file × (raw matches / reviewed-unbounded) table**, with the method sentence.
- **The threat-model distinction, stated as the reason for the deferral rather than as an excuse.**
  `drain_complete_lines` and `connect_sse` read from an arbitrary remote peer. These read from an
  IdP the deployment configured — semi-trusted, not arbitrary. Same defect class, different trust
  boundary. That is a statement about who chooses the bytes, not about severity.
- **Why Phase 116 is the right owner.** Its subject is re-examining exactly that trust assumption
  (RFC 9207 `iss` validation, DCR `application_type`, issuer-keyed credential storage). A bound landed
  before that re-examination would be defending a trust model that is about to change.
- **Why the tripwire fence was NOT widened.** The fence at `tests/v2_bounded_reads_tripwire.rs:64-69`
  mirrors HTTP-09's requirement text verbatim, which scopes to a peer-controlled read on the v2
  transport path. Widening it needs a HOME REQUIREMENT first — either editing HTTP-09, which this
  phase's own success criterion forbids, or minting a new one, which is milestone-scoping work inside
  a merge unblock. Cross-reference the existing "Widening the bounded-read tripwire's scope fence"
  deferred idea rather than duplicating it.
- **Fix shape.** The same running-total / `collect_body_within_cap` doctrine plan 113.1-03 applies to
  `connect_sse`, pointing at that plan's `collect_sse_text_within_cap` as the worked `reqwest`
  example.

### Deliverable 2 — UPDATE `D-113-J`; do NOT create a second PMAT-recipe entry

The CLAUDE.md PMAT-recipe defect is **already on the record** as `## D-113-J` at `deferred-items.md`
`:509`. It already carries: the exact broken command, the measured JSON-shape drift (`.violations` is
`null` at the top level; the array is at `.summary.violations`; violations name `.file`, not `.path`),
the working substitute query, and the reason it matters. Minting a second entry would duplicate the
ledger and split one defect's record across two identifiers.

So the deliverable here is an **amendment in place**, not a new heading:

- Change its `**Owner:** unassigned — a CLAUDE.md documentation fix, owned by no Phase-113 plan.`
  line to name an actual owner, and add an explicit `**Status:**` line (the entry currently has none,
  unlike `D-113-R`'s block). The fix is a two-line documentation change to a BINDING file, and
  editing `CLAUDE.md` is outside D-01…D-20, so this phase cannot make it. Assign it to the next phase
  that touches developer documentation — **Phase 119 (Documentation — Three Shapes + v2 Migration)**
  is the natural home — and state explicitly that it is **awaiting developer approval** because
  CLAUDE.md is binding. Do not invent authority this phase does not have.
- Add a short `**Re-confirmed:**` line: plan 113.1-04 (phase 113.1) re-observed the same behavior on
  pmat 3.15.0 during this round, so the entry is current rather than stale. While there, add the
  second measured trap RESEARCH found which D-113-J does not yet record:
  `pmat analyze complexity --file <path>` reports **14** and **18** on the identical unmodified file
  and is NOT the gate. Include the measured top-level JSON keys (`summary`, `files`,
  `top_files_limit` — and no `violations`) if they are not already spelled out.
- Leave the rest of the entry byte-unchanged. A correction is recorded, never erased.

**Do NOT** fix any of the auth reads here. **Do NOT** widen the tripwire scope fence. **Do NOT** edit
CLAUDE.md. **Do NOT** touch any file under `src/` or `tests/` in this plan. **Do NOT** create a new
`## D-113-*` heading for the PMAT recipe.
  </action>

  <verify>
    <automated>grep -q 'Phase 116' .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md &amp;&amp; grep -q 'generic_oidc.rs' .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md &amp;&amp; test "$(grep -c '^## D-113-J' .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md)" -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `deferred-items.md` gains exactly ONE new `## D-113-<next>` heading, whose identifier did not already exist in the file
    - `grep -c '^## D-113-J' .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` returns 1 — the PMAT-recipe defect was amended in place, not duplicated under a second identifier
    - The `## D-113-J` section no longer contains the string `Owner:** unassigned`, and now contains both an `Owner:` naming a phase and a `Status:` line
    - The `## D-113-J` section contains the strings `14` and `18` in the context of `pmat analyze complexity --file` — the second measured trap is recorded alongside the original one
    - The new entry contains a line matching `Owner:` and `Phase 116` on the same line — not `UNASSIGNED`
    - The new entry names all four files: `generic_oidc.rs`, `client/auth.rs`, `cognito.rs`, `client/oauth.rs`, each with BOTH a raw-match count and a reviewed-unbounded count, plus a stated total and a one-sentence description of how the second column was derived
    - The new entry contains the phrase distinguishing an arbitrary remote peer from a configured IdP, and an explicit statement that the tripwire scope fence was NOT widened with the reason (no home requirement)
    - **Commit-scoped, concurrency-safe:** capture the sha this task's commit produced and assert `git show --pretty=format: --name-only <sha> | grep -cv '^\.planning/'` returns 0 — this plan's commit touched only planning documents. Do NOT use a global `git status --porcelain src/ tests/`; Wave 1 shares one worktree and plans 01/02/03 have uncommitted work in both trees.
  </acceptance_criteria>

  <done>The auth-surface reads are enumerated with measured raw and reviewed counts, an explicit Phase-116 owner, and a written reason they sit outside HTTP-09's fence; the pre-existing D-113-J entry gained an owner, a status and the second measured trap without being duplicated; this plan's commit contains only `.planning/` paths.</done>
</task>

<task type="auto">
  <name>Task 2: Execute arm 2 of the rolled-forward re-verification and record the result</name>

  <files>.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md</files>

  <read_first>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` `:412-440` — "Arm 2 — Conformance predicate (§ B.6)", the four-step procedure this task executes. Follow it literally; this task does not redesign it.
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` `:676-816` — § B.6 in full: B.6.1 Provenance (the table shape and the literal `gh api` fetch and sha-confirmation commands), B.6.2 the byte-for-byte predicate quotation, B.6.3 the disjuncts table, B.6.4 what drift means.
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` § `## Verdict` and § `## Third Outcome Policy` — the `hold` decision recorded by Guy Ernest on 2026-07-27, which this task must not disturb.
    - `tests/v2_conformance_pin.rs` — how it parses § B.6.3 at runtime, so you can see exactly which table properties are load-bearing.
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Code Examples → D-19 arm 2 is runnable today, and shows NO drift" — the risk sizing, including the measured line-range move.
    - `.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md` § 11, and in particular its warning that **B.6.3's table shape is load-bearing**: `tests/v2_conformance_pin.rs` parses it at runtime.
  </read_first>

  <action>
Execute the four-step arm-2 procedure exactly as `113-SPEC-RECHECK.md:412-440` writes it, then record
the run.

**Step 1.** Fetch `src/scenarios/server/stateless.ts` from
`github.com/modelcontextprotocol/conformance` at **`main`/HEAD**, not at § B.1's pinned sha — fetching
the pin back would compare the pin against itself and can never detect drift. Also capture upstream's
current HEAD sha with `gh api repos/modelcontextprotocol/conformance/commits/main --jq '.sha'`.

**Step 2.** Locate `advertisesSubscriptions` and diff it against § B.6.2's verbatim quotation. Record
the HEAD sha and the line range actually found. A changed line number is NOT by itself drift — § B.6.2
step 2 says so explicitly, and RESEARCH already measured that the predicate moved from the pinned
`:983-1016` to roughly `:997-1002` with no content change.

**Step 3.** Any difference in the DISJUNCT SET is drift, whether a disjunct is gained, lost, or its
capability path renamed. Comment and formatting changes are not drift; record them and move on. The
pinned disjunct set is `tools.listChanged`, `prompts.listChanged`, `resources.listChanged`,
`resources.subscribe`.

**Step 4 — and the two branches it forks into.** RESEARCH measured no drift at upstream HEAD
`89c2c045`, so the expected path is the first one. Both are defined here so the executor is never in
the position of improvising at the moment of discovery.

- **Branch A — NO DRIFT (expected).** Do not edit § B.6.3 at all. Run
  `cargo nextest run --features full --test v2_conformance_pin` anyway — it is cheap and it is
  worthwhile evidence either way. **It must exit 0.** A failure on this branch is a real problem in
  this plan and blocks the task.

- **Branch B — REAL DRIFT.** Update § B.6.3's table to match upstream, then re-run
  `cargo nextest run --test v2_conformance_pin`. **On this branch the pin test may legitimately
  FAIL**, because a genuinely new or renamed disjunct means the code no longer satisfies the
  predicate the table now records — that is the test doing its job, not a defect introduced here.
  A failure on Branch B is therefore CONDITIONALLY ACCEPTED, and only under all of these:
  1. the failure is recorded verbatim in § B.6.5 and in the SUMMARY, with the specific disjunct that
     changed;
  2. a new deferred item is opened in `deferred-items.md` naming **Phase 118 (Conformance Against the
     Official Suite)** as owner — that phase's subject is running the official suite in CI, so
     conformance-predicate drift is its work, not this merge unblock's;
  3. the SUMMARY states explicitly that the merge is unaffected, because HTTP-08 is already `[~]` on
     unrelated grounds and D-19 explicitly rejected letting an upstream repository veto this merge;
  4. plan 06 is told, via the SUMMARY, that `v2_conformance_pin` is expected-red pending Phase 118 —
     so the phase gate does not read it as an unexplained failure.

  **Never** resolve a Branch-B failure by editing the table back to the pin. That would be recording
  a fact you know to be false.

**Record the run as a NEW subsection `#### B.6.5 Arm-2 re-verification runs`, appended after B.6.4.**
Do not restructure or renumber B.6.1–B.6.4, and above all **do not reshape B.6.3's table**:
`tests/v2_conformance_pin.rs` parses it at runtime, so its shape (one disjunct per row, four columns,
no merged cells, capability path in column 2) is load-bearing rather than cosmetic.

B.6.5 should carry a run-log row per execution, in B.6.1's provenance style: run date (UTC), the
executing plan (113.1-04), upstream HEAD sha, the line range where the predicate was found, whether
the disjunct set matched the pin, the `v2_conformance_pin` result, and the verdict — `NO DRIFT` or
`DRIFT`. Include the literal commands run and their literal output, as B.6.1 does.

**Also update § "Arm 2" itself** to note that a run has now occurred and where its record lives, so a
future re-verifier reads "arm 2 was run on <date>, no drift" rather than re-deriving it.

**Do NOT** touch `## Verdict` — it stays `PENDING`. **Do NOT** flip any checkbox in
`.planning/REQUIREMENTS.md`; the `[~]` publication hold on HTTP-01..08 / CLNT-01/02/05 stands, and
HTTP-09's flip belongs to plan 06. **Do NOT** treat arm 2 as discharging arm 1: the obligation as a
whole lands only when BOTH arms have been run, and arm 1 is still blocked on the condition "a
versioned schema directory exists".
  </action>

  <verify>
    <automated>grep -q 'B.6.5' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md &amp;&amp; grep -qE 'NO DRIFT|DRIFT' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md</automated>
  </verify>

  <acceptance_criteria>
    - `113-SPEC-RECHECK.md` contains a `#### B.6.5` heading with a run-log entry naming plan 113.1-04, a UTC run date, the upstream HEAD sha fetched, the line range where `advertisesSubscriptions` was found, and a verdict of `NO DRIFT` or `DRIFT`
    - The entry contains the literal `gh api` command used and its literal output for the sha fetch
    - `grep -c 'advertisesSubscriptions' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` is greater than its pre-plan value — the run added evidence rather than replacing it
    - `sed -n '/#### B.6.3/,/#### B.6.4/p' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` is byte-identical to its pre-plan content on **Branch A**. On **Branch B** it changed, and all four Branch-B conditions in the action are satisfied — the recorded failure, the Phase-118 deferred item, the merge-unaffected statement, and the expected-red note for plan 06.
    - On Branch A: `cargo nextest run --features full --test v2_conformance_pin` exits 0. On Branch B: its failure output is recorded verbatim in both § B.6.5 and the SUMMARY, and a `deferred-items.md` entry names Phase 118 as owner.
    - `grep -c '## Verdict' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` is unchanged, and the Verdict still reads `PENDING`
    - **Commit-scoped, concurrency-safe:** capture the sha this task's commit produced and assert `git show --pretty=format: --name-only <sha> | grep -cv '^\.planning/'` returns 0 — no source file changed. Do NOT assert global worktree cleanliness for `src/`, `tests/` or `.planning/REQUIREMENTS.md`; Wave 1 shares one worktree.
  </acceptance_criteria>

  <done>Arm 2 has been executed against upstream HEAD rather than against its own pin, its result is recorded in a new B.6.5 run log without disturbing the machine-parsed B.6.3 table, the no-drift and real-drift branches are each resolved by their defined rule, and the Verdict and every requirement checkbox are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream `modelcontextprotocol/conformance` repository → this project's HTTP-08 obligation | An external repository whose content defines a conformance predicate pmcp binds itself to. It moves on its own cadence and is not under this project's control. |
| configured IdP → the four auth-surface files | Semi-trusted: the deployment chose the issuer. Distinct from the arbitrary-peer boundary HTTP-09 governs. This plan records the distinction; it does not change any code at that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113.1-30 | Denial of Service | unbounded whole-body reads on the auth surface | transfer | Transferred to Phase 116 (Auth Hardening SEPs) with a written owner, measured raw and reviewed-unbounded counts, and a fix shape. Transfer is recorded in `deferred-items.md`, so the gap is enumerated rather than unnamed — the failure mode HTTP-09 exists to prevent. |
| T-113.1-31 | Repudiation | an obligation that was runnable but never run | mitigate | Arm 2 is executed and its result written to `113-SPEC-RECHECK.md` § B.6.5, so "we checked and found no drift" stays distinguishable from "nobody checked". |
| T-113.1-32 | Tampering | reshaping B.6.3's table while recording the run, or editing it back to hide real drift | mitigate | B.6.3 is parsed at runtime by `tests/v2_conformance_pin.rs`; the run is appended as a NEW subsection and the table is asserted byte-identical on the no-drift branch. On real drift the table is updated to match upstream and the resulting pin failure is recorded and transferred — never reverted to make the test green. |
| T-113.1-33 | Elevation of Privilege | letting an upstream repository gate this merge | accept | Explicitly rejected by D-19. A drift finding becomes a deferred item owned by Phase 118; HTTP-08 is already `[~]` on unrelated grounds. |
| T-113.1-34 | Repudiation | a misleading debugging recipe in a BINDING file, or the same defect split across two ledger entries | mitigate | The defect stays as the single pre-existing `D-113-J`, amended in place with an owner, a status, a re-confirmation and the second measured trap — rather than duplicated or edited unilaterally into CLAUDE.md, which is outside D-01…D-20. |
| T-113.1-35 | Repudiation | a raw grep count presented as a security finding | mitigate | The entry records raw needle matches and the reviewed-unbounded subset separately, with the review method stated, so nobody later mistakes a syntactic count for an audited one. |
| T-113.1-SC | Tampering | cargo installs | accept | This plan installs nothing and changes no source. `gh` and `cargo nextest` are pre-existing. |
</threat_model>

<verification>
1. `cargo nextest run --features full --test v2_conformance_pin` — exits 0 on the no-drift branch;
   on the real-drift branch its failure is recorded and transferred to Phase 118 per the Branch-B
   rule, and is NOT treated as a failure of this plan.
2. **Commit-scoped record-only proof.** For each sha this plan produced:
   `git show --pretty=format: --name-only <sha> | grep -cv '^\.planning/'` returns 0.
   Do NOT run a global `git status --porcelain src/ tests/ CLAUDE.md` — Wave 1 shares one worktree
   (`use_worktrees: false`) and plans 01/02/03 have uncommitted work in `src/` and `tests/`.
3. Read back the new `deferred-items.md` entry, the amended `## D-113-J`, and the new
   `113-SPEC-RECHECK.md` § B.6.5 to confirm they are enumerable records (measured numbers, literal
   commands, literal output), not narrative.
</verification>

<success_criteria>
- A new deferred item enumerates the auth-surface reads with MEASURED raw and reviewed-unbounded per-file counts, names Phase 116 as owner, states the trust-boundary distinction, and explains why the tripwire fence was not widened
- The pre-existing `D-113-J` is amended in place with an owner, a status, a re-confirmation and the `--file` 14/18 trap — and there is still exactly one `## D-113-J` heading in the file
- Arm 2 was executed against upstream HEAD and recorded in a new § B.6.5 run log
- B.6.3's machine-parsed table is unchanged on the no-drift branch; on real drift it matches upstream and the pin failure is recorded and owned by Phase 118
- `## Verdict` still reads `PENDING` and no requirement checkbox moved
- Every commit this plan produced contains only `.planning/` paths
</success_criteria>

<output>
Create `.planning/phases/113.1-merge-unblock/113.1-04-SUMMARY.md` when done.

It MUST record: the MEASURED per-file read counts in both columns (raw matches and reviewed-unbounded
— not the planned 5/5/4/4, unless that is what was measured) with the review method; the upstream
conformance HEAD sha and the line range where the predicate was found; an explicit NO DRIFT / DRIFT
verdict with what follows from it; and, if Branch B was taken, the expected-red note plan 06 needs so
it does not read `v2_conformance_pin` as an unexplained gate failure.
</output>
