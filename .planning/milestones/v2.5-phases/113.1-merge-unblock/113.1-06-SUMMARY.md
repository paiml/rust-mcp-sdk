---
phase: 113.1-merge-unblock
plan: 06
status: complete
wave: 3
completed: 2026-07-27
requirements: [HTTP-09]
closes_decisions: [D-17, D-20]
---

# Phase 113.1 — Closure Record

**HTTP-09 is closed on the merits and reads `[x]`.** The three defects that kept
`fix/mcp-publisher-oidc-audience` from merging are fixed, and the local gates are green.

**The tree is NOT yet gate-green in CI**, for two pre-existing reasons this phase neither caused nor
owns. That is stated up front rather than buried — see § 3.

---

## 1. The five ROADMAP success criteria

### SC-1 — split, and only half is dischargeable here

**SC-1a — DISCHARGED.** `pmat quality-gate --fail-on-violation --checks complexity`, run explicitly
from the repo root (it appears nowhere in `make quality-gate` — Phase 75 D-07, verified at
`Makefile:673-690`, which is exactly how D-113-U reached CI unnoticed):

```
Quality Gate: PASSED
Total violations: 0

✅ Quality gate PASSED
```

Per-function, `pmat analyze complexity -p . --format json --top-files 0`:

| Function | baseline | final | nesting_max |
|---|---|---|---|
| `handle_post_fast_path` | **30** | **15** | 10 → 5 |
| `handle_post_with_middleware` | **31** | **15** | 10 → 5 |

Five points of margin under the phase's own ≤ 20 target, ten under the gate's 25.

**SC-1b — NOT DISCHARGED. NAMED HUMAN HANDOFF.** The org-required `gate` status check cannot turn
green without a push to `paiml/rust-mcp-sdk`, which D-20 reserves as a human action. **SC-1 is
therefore never claimed as met — not in this document, and not in the ROADMAP.** Only SC-1a is
discharged.

Handoff steps:

1. `git push` the branch to `paiml/rust-mcp-sdk`.
2. PR **#299** picks up the new head automatically. Its current head is `fb99bca1` from
   **2026-07-10 — 532 commits behind** this branch, so its CI results describe a tree that no longer
   exists.
3. The `gate` aggregate job runs (`needs: [test, quality-gate, purity-check, pmcp-agent-targets]`).
4. **It will still be RED** until the two blockers in § 3 are cleared. Both are pre-existing.
5. Merge only on green.

### SC-2 — the O(n) guards (D-113-R)

Closed by plan 113.1-02. Both guards are **falsifiable, demonstrated twice**:

| Guard | pre-fix RED | **GREEN** | post-fix control RED |
|---|---|---|---|
| `sse_parser_feed_1b_chunks_stays_within_its_linear_time_budget` (512 KiB @ 1 byte/call, ceiling 1 s) | **6.805 s** | **63.63 ms** | **4.357 s** |
| `sse_parser_feed_cost_grows_linearly_not_quadratically` (64→256 KiB, ceiling 8.0×) | **15.06×** | **4.39×** | **14.85×** |

107× faster at the adversarial shape; growth ratio moved from the quadratic signature to the linear
one. In the control run the pre-existing 4 KiB budget test **passed at 5.13 ms** against the
identical defect — D-14 re-confirmed by measurement, not argument.

### SC-3 — the unbounded `reqwest` read (D-113-Q)

Closed by plan 113.1-03. `connect_sse` reads through `collect_sse_text_within_cap`: a
`Content-Length` early refusal (advisory) plus a `Response::chunk()` loop whose overflow-safe running
total is checked **before each append**. `WHOLE_BODY_ALLOWLIST` is **EMPTY**, its pin asserts length
0 — the ratchet at its floor. Negative control RED (the 513-byte body admitted whole with the check
removed).

### SC-4 — HTTP-09 closed on the merits

Every clause verified mechanically **before** the checkbox moved:

| Clause | Evidence |
|---|---|
| 1 — no unbounded whole-body read in scope | `no_unbounded_whole_body_read_over_peer_supplied_bytes` green; allowlist length **0** |
| 2 — no unreviewed accumulation | `every_peer_byte_accumulation_is_reviewed` green with the `chunk()` loop's population reviewed |
| 3 — no scan worse than O(n) | both guards green **and** both RED measurements on record |

**The requirement TEXT is byte-identical** — proven by diffing the bullet with only the trailing
`— *NOT met; see below*` clause removed. It closed on the merits, not by narrowing, and the
tripwire's scope fence (`:64-69`) is byte-unchanged.

### SC-5 — the auth reads recorded and assigned

Closed by plan 113.1-04 as **`D-113-V`**, owner **Phase 116**. The counts were re-measured, and the
roadmap's "18" is an **undercount**: the true reviewed-unbounded figure is **31** across four files.
The gap is the tripwire's **scope fence**, not its needles — its scanner strips whitespace and
already resolves rustfmt-split chains, so widening the fence alone would surface all of them
(⚠️ corrected after review; the first statement of this blamed the needles). Two matches excluded
and said so:
`oauth.rs:282` is already bounded (post-hoc, noted), `oauth.rs:953` is a local-disk read.

---

## 2. The phase gate — every step, recorded

| # | Check | Result |
|---|---|---|
| 1 | `pmat quality-gate --fail-on-violation --checks complexity` | ✅ **PASSED, 0 violations** |
| 2 | `pmat analyze complexity -p . --top-files 0` | ✅ both handlers **15** |
| 3 | `make quality-gate` | ✅ **exit 0** — `ALL TOYOTA WAY QUALITY CHECKS PASSED` |
| 4 | `cargo nextest run --features full` | ✅ **2312 passed, 2 skipped, 0 failed** |
| 5 | `cargo test --test v2_bounded_reads_tripwire` | ✅ **13 passed** |
| 6 | `make doc-check` | ❌ **exit 2 — 26 errors, ALL PRE-EXISTING** (§ 3) |
| 7 | `cargo semver-checks` | ⚠️ **222 pass / 1 fail / 30 skip — 0 major, 1 minor** (§ 3) |
| 8 | `cargo build --example t03_sse_optimized --features sse` | ✅ exit 0 |
| 9 | `RUSTUP_TOOLCHAIN=nightly cargo fuzz run subscription_listen_frames -- -runs=20000` | ✅ **ran** (below) |
| 10 | `cargo check --no-default-features --features sse` | ✅ exit 0 |

**Step 9, the mandatory fuzz campaign, re-run at the integrated head** rather than cited from
plan 02, because the head differs:

| Field | Value |
|---|---|
| Commit under test | `1df8522944bd50d3be29c6c834baad7e8291a7b7` |
| libFuzzer seed | **4219536610** |
| Runs | **20 000** — reached `#20000 DONE` |
| Exit code | **0** |
| Artifacts directory | **exists, EMPTY** (`0` files) |
| Coverage | `cov: 237`, `ft: 975`, `corp: 185/7340b`, RSS 155 MB |

**`make test-fuzz` was NOT used and is never accepted here** — D-113-G: it sets `CARGO = cargo`
(stable) while `cargo fuzz` needs nightly for `-Zsanitizer=address`, so all 17 targets fail to build
and each failure is swallowed by `|| echo`. A green `make test-fuzz` proves nothing.

### CLAUDE.md's ALWAYS requirements, checked explicitly

| Requirement | Where satisfied |
|---|---|
| **FUZZ** | `subscription_listen_frames`, 20 000 runs, seed 4219536610, zero artifacts |
| **PROPERTY** | `property_feed_chunking_invariance` (1000 cases) |
| **UNIT** | the two perf guards, `drain_leaves_no_newline_in_the_retained_buffer`, and the over/at/declared/under-cap set |
| **EXAMPLE** | `t03_sse_optimized` builds under `--features sse` by explicit command |

No new feature surface was added, so no new example was invented to satisfy a checklist.

---

## 3. ⚠️ What is NOT green, and why it is not this phase's doing

### Blocker A — `make doc-check`: 26 pre-existing rustdoc errors (`D-113-W`)

`make doc-check` runs in CI's `quality-gate` job (`ci.yml:231`), and that job is in the
org-required **`gate`** aggregate's `needs:` list (`ci.yml:386`). **While it is red, the branch
cannot merge regardless of the complexity gate.**

Proven not to be this phase's doing, by stashing plan 03's three files and re-running:

| Tree | `doc-check` | errors |
|---|---|---|
| `HEAD` before any phase-113.1 edit | exit 2 | **26** |
| after plan 03 Task 1 | exit 2 | 26 |
| after plan 03 Task 3 | exit 2 | 26 |
| **final integrated head** | exit 2 | **26** |

`diff` of the sorted error lists is **EMPTY against the pre-phase baseline at every step**. Phase
113.1 introduced **zero** rustdoc errors.

Recorded as **`D-113-W`** with the full error list, the measurement method, a stated hypothesis
about cause (532 commits of drift and/or a newer local `rustc 1.97.1` tightening
`private_intra_doc_links`), and a fix shape. **Recorded and assigned rather than fixed** — fixing 26
errors across `feed`, `mrtr`, `ProtocolContext`, `server_discover`, `TraceContext` and others is
scope expansion into modules this phase does not touch, and silencing them with `#[allow]` is
forbidden by CLAUDE.md's zero-tolerance rule and would be exactly the "make the number look
complete" move HTTP-09 exists to prevent. **Developer decision, 2026-07-27: record and defer.**

### Blocker B — the Purity Gate

Also failing at PR #299's head, at its own `Run purity gate` step. Separately known
tooling/lockfile drift, not phase 113.1's doing, and also in front of the org-required `gate`.
Cross-referenced from `D-113-W`. Anyone driving SC-1b to green needs both cleared.

### Finding — `cargo semver-checks` requires a MINOR bump

```
Checked 223 checks: 222 pass, 1 fail, 0 warn, 30 skip
--- failure type_marked_deprecated: #[deprecated] added on type ---
  Struct OptimizedSseTransport
Summary semver requires new minor version: 0 major and 1 minor checks failed
```

**Recorded, not worked around.** RESEARCH's assumption A5 (an added `#[deprecated]` is
version-neutral) is wrong. But the milestone's actual claim is intact: **0 major checks failed and
zero public items were removed**, which is what D-01's additivity justification rests on. The
attribute's `since = "2.18.0"` already names the minor bump this implies from 2.17.0, so the
deprecation is self-consistent with the next release. The result is byte-identical before and after
plan 05, confirming every helper plan 05 added is private.

---

## 4. Process divergence from AGENTS.md — recorded, not retrofitted

`AGENTS.md` prescribes a PMAT quality-proxy for all file writes, PDMT-generated todos, and an
explicit contract-first pre/post `pmat comply` sequence. Phase 113's own shipped plans
(113-29 … 113-32) invoke none of the three, and `pmat comply` runs inside `make quality-gate` as its
final step — which every plan in this phase ran and passed. So this phase followed the established
house style rather than the document.

Reconciling the two is repo-wide process work with no home in a three-blocker merge unblock, and
adding unexercised process steps here would be its own kind of false record. **No quality-proxy or
PDMT steps were added.**

---

## 5. What remains open

- **The `[~]` publication hold** on HTTP-01…HTTP-08 and CLNT-01/02/05 — a recorded decision
  (`113-SPEC-RECHECK.md` § Third Outcome Policy, `hold`, Guy Ernest, 2026-07-27), untouched. All
  eight `[~]` HTTP rows and the CLNT rows are byte-unchanged, and `## Verdict` still reads
  **`PENDING`**.
- **Arm 1** of the rolled-forward re-verification — still blocked on the *condition* "a versioned
  schema directory exists", not on the 2026-07-28 date. **Arm 2 HAS been run** (plan 113.1-04,
  upstream HEAD `5cc567c3`, **NO DRIFT**, recorded in § B.6.5), but the obligation lands only when
  both arms have run, so it remains undischarged.
- **`D-113-W`** — the 26 rustdoc errors. OPEN, unassigned, **blocks the org gate**.
- **The Purity Gate drift** — OPEN, pre-existing, also blocks the org gate.
- **`D-113-V`** — the 31 auth-surface unbounded reads. OPEN, owner **Phase 116**.
- **`D-113-J`** — CLAUDE.md's broken PMAT recipe. OPEN, owner **Phase 119**, awaiting developer
  approval because CLAUDE.md is binding.
- **`D-113-S`** and **`D-113-T`** — untouched, still OPEN and unowned.
- **`v2_conformance_pin`** is **normally green** (5/5). Plan 04 took Branch A; it is *not*
  expected-red, and no Phase-118 drift item was needed.
- **Push, PR and merge** — `git push`, updating PR #299, and merging are **human actions outside
  this phase** (D-20).

### Two untested residual risks, carried forward from plan 01

Recorded because a green negative control proves nothing and must not be cited as evidence:

1. **No test constrains that a v2 gate `Reject` on the middleware path fires
   `report_middleware_error`.** The behavior is preserved in the tree, but deleting the hook left all
   42 middleware tests green.
2. **No check constrains the per-arm `Box::pin` inside `dispatch_message_fast`.** After the outer
   boxing added in plan 01 Task 2, removing an inner one is invisible to `make lint` and to the
   subscription suites.

---

## 6. No human-only action was taken

**No `git push`, `gh pr create`, `gh pr edit`, `gh pr merge` or equivalent was invoked at any point
in this phase.** The branch's upstream tracking ref is unchanged.

This is recorded as a positive statement rather than inferred from `git log` — push state is not
represented in commit history, so a `git log` assertion would be unverifiable theatre. The only
`gh` calls made were **read-only**: `gh api .../commits/main`, `.../contents/...`,
`.../check-runs`, `gh pr list` — used to execute arm 2 against upstream and to read PR #299's
existing CI status.

---

## Decisions closed

- **D-17** — HTTP-09 flipped to `[x]`, not `[~]`, on the merits, with its text unnarrowed.
- **D-20** — the phase ends at a tree whose *local* gates are green, the three defects closed, and
  the planning record corrected. Pushing, opening the PR and merging remain separate human actions.

---

## 7. Task 3 bookkeeping

### The six SUMMARYs, confirmed on disk before any box was ticked

```
113.1-01-SUMMARY.md   113.1-02-SUMMARY.md   113.1-03-SUMMARY.md
113.1-04-SUMMARY.md   113.1-05-SUMMARY.md   113.1-06-SUMMARY.md
```

All six present. This task deliberately runs AFTER § 1–6 were written, so plan 06's own box could be
ticked honestly rather than leaving the list permanently at 5/6.

### ROADMAP sites edited

| Site | Before | After |
|---|---|---|
| 1 — the six-plan list | `- [ ] 113.1-0*` ×6 | `- [x]` ×6 |
| 2 — the progress table row | `| 113.1 Merge Unblock | 0/TBD | Not started | - |` | `| 113.1 Merge Unblock | 6/6 | Complete | 2026-07-27 |` |
| 3 — the top-level v2.5 bullet | `- [ ]`, all three blockers described as open, `sse_optimized.rs:266` "currently allowlisted `NOT BOUNDED`", "18 unbounded reads … three files" | `- [x]`, the shipped facts, the PMAT claim qualified **locally**, and the outstanding human push plus both pre-existing CI blockers named |

Three plan descriptions were corrected to match what shipped rather than what was intended: **02**
(the recorded RED/GREEN/control measurements), **04** (D-113-J was AMENDED IN PLACE, and arm 2
returned NO DRIFT so Branch A was taken and no Phase-118 item was needed), and **05** (landed at
**15 / 15**, beating the 16/16 target row).

**"Complete" in the progress table counts PLANS, not remote CI status** — consistent with the
existing warning above that table, and with the Phase-113 row's own meaning.

### A fourth site, added rather than edited

The `### Phase 113.1: Merge Unblock (INSERTED)` detail section's **Goal / Scope / Success Criteria**
are the phase's CHARTER, and its Scope block says so explicitly ("verbatim from the milestone phase
list"). Editing them would falsify that claim and, for SC-1, would weaken a criterion plan 06
forbids weakening. They were left byte-unchanged.

But Scope item 3 reads *"currently allowlisted `NOT BOUNDED`"*, which a reader could take as a
present-tense claim. Rather than edit charter text, an **Outcome (2026-07-27)** block was appended
after the Success Criteria, stating that the text above it is the charter, mapping each criterion to
what shipped, and carrying the two charter figures this phase corrected: the `NOT BOUNDED` entry no
longer exists, and the auth population is **31 across four files**, not 18 across three.

### Untouched, verified

- No requirement checkbox moved in the ROADMAP.
- The Phase 114–119 sections are unchanged.
- The Phase-113 progress row is unchanged, and Phase 113 stays **HELD**, not closed.
- The `[~]` publication-hold paragraph and the `## Verdict … PENDING` statement are intact.

### SC-1b, restated one final time

**Pushing the branch, updating PR #299 against `paiml/rust-mcp-sdk`, and merging are human actions
outside this phase.** The org-required `gate` will remain red until `D-113-W` (26 pre-existing
rustdoc errors) and the Purity Gate drift are cleared — neither caused by, nor owned by, Phase 113.1.
