---
phase: 113.1-merge-unblock
verified: 2026-07-28T00:56:26Z
verified_at_commit: c9944a65
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  re_verified: 2026-08-08
  re_verified_at_commit: 7697bd33
  gaps_closed:
    - "SC-1b (the org-required `gate` check green on the branch): CLOSED. The branch has since
       been pushed — `git ls-remote origin fix/mcp-publisher-oidc-audience` -> `57367dc3`
       (2026-08-07), not the `fb99bca1` this report measured. Re-read the checks at that head
       with `gh api repos/paiml/rust-mcp-sdk/commits/57367dc3.../check-runs`: **`gate: success`,
       `Purity Gate: success`, `Quality Gate: success`**, 30 check-runs total with 0 failures
       (5 skipped: the two fuzzing-schedule jobs, `deploy`, `upload-to-release`). Both of the
       two blockers this report listed under `missing` are therefore no longer failing in CI:
       D-113-W's 26 rustdoc errors (the `Quality Gate` job runs `make doc-check`) and the
       Purity Gate lockfile/tooling drift both pass at that head. Neither was closed BY this
       phase — they were closed by later work — which is why this is recorded as a re-verified
       gap rather than a retroactive credit to 113.1."
    - "The documentation-freshness WARNING: CLOSED. Both rustdoc sites now read
       `**Complexity budget: cognitive 4** here plus **0** in [...]_inner`
       (`src/server/streamable_http_server.rs:3493` and `:4168`), with an added note explaining
       that pmat does not list a cognitive-0 function at all. Independently confirmed by direct
       read this session, not accepted from any summary. The two planning records are clean
       too: a grep for `cognitive 15` over `.planning/ROADMAP.md` and
       `.planning/phases/113-*/deferred-items.md` returns no claim about these two handlers."
  regressions: []
  caveat: >-
    The green CI above is at `57367dc3`, and local `HEAD` is **96 commits ahead** of it — the
    whole of Phase 117's v1-severance work postdates the last run CI has seen. SC-1b as
    CHARTERED ("the gate is green on the branch") is met; it is NOT evidence about the current
    working tree. Phase 118 depends on the union of 112-117 and should not treat this as a
    green light for the tree it will run against.
gaps:
  - truth: "`pmat quality-gate --fail-on-violation --checks complexity` passes with no `src/` function over cog 25, and the org-required `gate` status check is green on the branch (D-113-U)"
    status: partial
    reason: >-
      The conjunction's first half is INDEPENDENTLY VERIFIED (my own pmat 3.15.0 run at this
      commit: `Total violations: 0`; no function in any `*/src/` tree exceeds cognitive 25, max
      is 23). The second half is objectively NOT met: the branch has never been pushed
      (origin/fix/mcp-publisher-oidc-audience is at `fb99bca1`, local is 537 commits ahead), and
      the `gate` check-run at that stale head is `failure`. The phase did NOT claim SC-1 as met
      — it split it and marked SC-1b undischarged — so this is an OPEN HANDOFF, not a
      misreported success. Closure requires a human `git push` (reserved by the phase's own
      D-20) plus clearing two pre-existing CI failures that this phase neither caused nor owns.
    artifacts:
      - path: "(remote) refs/heads/fix/mcp-publisher-oidc-audience"
        issue: "Head is `fb99bca1` (2026-07-10); local HEAD `c9944a65` is 537 commits ahead. CI has never seen the phase's work."
      - path: "(CI) paiml/rust-mcp-sdk check-runs @ fb99bca1"
        issue: "`gate` = failure; `Purity Gate` = failure; `Quality Gate` = failure (the job that runs `make doc-check`)."
    missing:
      - "A human `git push` of `fix/mcp-publisher-oidc-audience` to `paiml/rust-mcp-sdk` so PR #299 picks up the real tree (D-20 reserves this)."
      - "D-113-W — 26 pre-existing rustdoc errors failing `make doc-check` inside CI's `quality-gate` job. OPEN, unassigned. Out of a merge-unblock's scope per the phase charter."
      - "Purity Gate tooling/lockfile drift. OPEN, pre-existing. Out of a merge-unblock's scope."
  - truth: "Records in the tree match the tree (documentation freshness)"
    status: partial
    reason: >-
      WARNING, not a blocker. Three records still state that both POST handlers "measure
      cognitive 15". Commit `dafc77c5` (landed after plan 05's SUMMARY and after plan 06's
      closure record) moved each pipeline into a `*_inner` fn behind a `?`-collapsing wrapper,
      so at HEAD the two named functions measure cognitive **1** and their pipelines measure
      **5**. The direction is conservative — the tree is better than the record claims — and
      `dafc77c5`'s own commit message documents the change honestly. But two of the three stale
      sites are rustdoc INSIDE the file this phase modified, and the phase's own final commit
      was `docs(113.1): correct a false claim about the bounded-read tripwire`, so the standard
      being applied is "records must match the tree".
    artifacts:
      - path: "src/server/streamable_http_server.rs:3287-3289"
        issue: "`**Complexity budget: cognitive 15** (pmat 3.15.0), down from 30` — `handle_post_fast_path` measures 1 at HEAD."
      - path: "src/server/streamable_http_server.rs:3908-3910"
        issue: "`**Complexity budget: cognitive 15** (pmat 3.15.0), down from 31` — `handle_post_with_middleware` measures 1 at HEAD."
      - path: ".planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md:1180-1182"
        issue: "D-113-U status: \"Both handlers now measure **cognitive 15**\" — stale by one commit."
      - path: ".planning/ROADMAP.md:2551-2553"
        issue: "Warning block: \"`handle_post_fast_path` went 30 → **15** and `handle_post_with_middleware` 31 → **15**\" — stale by one commit."
    missing:
      - "Update the two rustdoc complexity-budget lines to the post-`dafc77c5` measurement (wrapper 1 / inner 5), or restate them as a budget for the `*_inner` pipeline."
      - "Optionally refresh the two planning records to match, or add a one-line note that `dafc77c5` reduced them further."
deferred:
  - truth: "The 26 rustdoc errors blocking `make doc-check` (D-113-W)"
    addressed_in: "Unassigned — natural home Phase 119 (Documentation — Three Shapes + v2 Migration)"
    evidence: "deferred-items.md D-113-W: Owner UNASSIGNED, Phase 119 named as the natural home; proven pre-existing at pre-phase HEAD by a stash-and-remeasure method with an empty sorted diff across three trees."
  - truth: "The 31 auth-surface unbounded reads"
    addressed_in: "Phase 116 (Auth Hardening SEPs)"
    evidence: "deferred-items.md D-113-V: Owner **Phase 116**, Status OPEN. Roadmap Phase 116 goal covers 'RFC 9207 `iss` validation, DCR `application_type`, issuer-keyed credential storage' — the same trust boundary these reads sit on."
---

# Phase 113.1: Merge Unblock — Verification Report

**Phase Goal:** Clear the three things that keep the `fix/mcp-publisher-oidc-audience` branch from
merging, so Phases 114-119 can start. Two of them (D-113-R, D-113-Q) are what hold **HTTP-09** at
`[ ]` on the merits; the third (D-113-U) is the org-required CI `gate` check itself.
**Verified:** 2026-07-28T00:56:26Z at commit `c9944a65` (branch `fix/mcp-publisher-oidc-audience`)
**Status:** `passed` — 5/5 chartered Success Criteria met (re-verified 2026-08-08)
**Re-verification:** Yes — second pass, 2026-08-08 at `7697bd33`. The initial pass (below,
unaltered) scored 4/5 with SC-1 half-open on a human `git push` that D-20 reserved to the owner.
That push has since happened: origin head is `57367dc3` (2026-08-07) and `gate`, `Purity Gate`
and `Quality Gate` are all `success` there. The documentation-freshness warning is closed too.
See the `re_verification:` block in the frontmatter for the evidence and for the one CAVEAT
that survives — CI's green head is **96 commits behind** local `HEAD`.

**Must-haves source:** `.planning/ROADMAP.md` § `### Phase 113.1: Merge Unblock (INSERTED)`
**Success Criteria** (ROADMAP:2391-2397), read as the contract. PLAN-level `must_haves` were not
used to narrow them. The ROADMAP's `**Outcome (2026-07-27).**` block (ROADMAP:2399-2417) was
treated as a **claim under test**, not as evidence.

---

## Goal Achievement

### Observable Truths (the five chartered Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `pmat quality-gate --fail-on-violation --checks complexity` passes with no `src/` function over cog 25, **and** the org-required `gate` status check is green on the branch (D-113-U) | ✗ **FAILED (partial)** — 1a VERIFIED, 1b NOT MET | **1a:** my own `pmat 3.15.0` run at `c9944a65` → `Quality Gate: PASSED / Total violations: 0`, exit 0. Independent per-function sweep of the whole repo: **no function in any `*/src/` tree exceeds cognitive 25** (max = 23, `src/server/schema_utils.rs::add_constraints`). **1b:** `git ls-remote origin` → remote head `fb99bca1`; local is **537 commits ahead** — never pushed. `gh api .../commits/fb99bca1/check-runs` → `gate` **failure**, `Purity Gate` **failure**, `Quality Gate` **failure**. |
| 2 | No scan over peer-chosen input on the v2 transport path is worse than O(n), proven by a falsifiable linear-time budget that fails on the current `drain_complete_lines` shape (D-113-R) | ✓ VERIFIED | Source confirmed at `src/shared/sse_parser.rs:502` — `let scan_start = self.buffer.len();` before the append, with the per-call `debug_assert` full-buffer scan removed and its invariant re-pinned by a named test (`:483-521`). Two falsifiable guards present (`:1179`, `:1252`); the 1 s ceiling sits between the committed 63.6 ms and the quadratic shape's 6.81 s, and the budget test also asserts `buffered_bytes() == CHUNKS` so "fast by retaining less" cannot pass. Timing **relied on** the independent re-measurement at this same commit (113-VERIFICATION.md): committed **29.22 ms vs 4.26 s** pre-fix shape (146x), growth **3.98x vs 15.26x** over a 4x step. |
| 3 | The bounded-read tripwire's allowlist no longer carries `sse_optimized.rs:266` as `NOT BOUNDED` (D-113-Q) | ✓ VERIFIED | `tests/v2_bounded_reads_tripwire.rs:591` is literally `const WHOLE_BODY_ALLOWLIST: &[Allowed] = &[];`. The removed entry was the `sse_optimized.rs` / `.text().await` / `"NOT BOUNDED — …"` record (confirmed by diffing the const across `97cbbd08..HEAD`). The read is now `collect_sse_text_within_cap` (`src/shared/sse_optimized.rs:280`), a `Response::chunk()` loop with a running total checked before each append. `:712` pins `WHOLE_BODY_ALLOWLIST.len()`. |
| 4 | **HTTP-09 flips from `[ ]` to met on the merits** in `.planning/REQUIREMENTS.md` — not by narrowing the requirement | ✓ VERIFIED | `REQUIREMENTS.md:56` reads `- [x] **HTTP-09**`. **Byte-identity proven programmatically**, not accepted from the SUMMARY: the pre-flip line body is 489 bytes, the post-flip body 466; removing *only* the 23-byte trailing clause `" — *NOT met; see below*"` yields a string **byte-identical** to the current text. All three sites landed (see § Key Link Verification). **Anti-narrowing check passed independently** — see § Criterion-Weakening Check. |
| 5 | The 18 auth-path unbounded reads are recorded as a named deferral with an owning phase | ✓ VERIFIED | `deferred-items.md:1280` — `## D-113-V`, **Owner: Phase 116 (Auth Hardening SEPs)**, **Status: OPEN**, with a four-file measured table, a stated counting method, two named exclusions with exact line numbers, a fix shape, and a stated reason the tripwire fence was not widened. **Counts independently reproduced exactly** (see § Behavioral Spot-Checks). Charter figure corrected upward, 18→31 across 4 files, not down. |

**Score:** 4/5 truths verified. SC-1 is half-open (SC-1a verified, SC-1b not met).

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/server/streamable_http_server.rs` | Both POST handlers under the cog-25 gate | ✓ EXISTS + SUBSTANTIVE + WIRED | 105 functions analysed. `handle_post_fast_path` cog **1** (wrapper) → `_inner` cog **5**; `handle_post_with_middleware` cog **1** → `_inner` cog **5**. Extracted siblings all present and called: `resolve_v2_gate` (3), `resolve_v2_gate_with_error_hook` (1), `dispatch_message_fast` (1), `read_and_classify_fast` (5), `guard_legacy_version_fast` (4), and their middleware twins. Highest in the file is `extract_auth_from_proxy_headers` at 19 — untouched by this phase. |
| `src/shared/sse_parser.rs` | Scan-window cursor + falsifiable O(n) guards | ✓ EXISTS + SUBSTANTIVE | +385/-31 lines this phase. `scan_start` cursor at `:502`; four named tests present: the 1-byte-chunk budget (`:1179`), the growth-ratio guard (`:1252`), the retained-buffer invariant (`:1321`), and `property_feed_chunking_invariance` (`:1831`). |
| `src/shared/sse_optimized.rs` | `connect_sse` bounded | ✓ EXISTS + SUBSTANTIVE + WIRED | `collect_sse_text_within_cap` at `:280` with the `chunk()` loop at `:294`; called from the production path at `:394` and exercised by three tests (`:680`, `:710`, `:745`). The old `response.text().await` is named in the rustdoc as what it replaced. |
| `tests/v2_bounded_reads_tripwire.rs` | Allowlist empty, fence/needles unchanged | ✓ EXISTS + SUBSTANTIVE | `WHOLE_BODY_ALLOWLIST = &[]` at `:591`; `SHARED_DIR`, `EXTRA_SCOPE` and `WHOLE_BODY_NEEDLES` all **byte-identical** to their pre-phase form. |
| `.planning/REQUIREMENTS.md` | HTTP-09 `[x]` at three sites, text unnarrowed | ✓ EXISTS + SUBSTANTIVE | The flip commit `1df85229` touched this file only: 1 file, **6 insertions, 4 deletions**, three hunks, exactly the three planned sites. No `[~]` requirement moved. |
| `.../113-.../deferred-items.md` | D-113-V recorded with an owning phase | ✓ EXISTS + SUBSTANTIVE | D-113-V at `:1280` (owner Phase 116); D-113-W at `:1352`; D-113-Q/R/U statuses flipped to `✅ RESOLVED` naming plans and shas. |

**Artifacts:** 6/6 verified.

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| SC-4 site 1 | `REQUIREMENTS.md:56` | `- [ ]` → `- [x]` + trailing clause deleted | ✓ WIRED | Verified at HEAD; text byte-identical modulo the clause. |
| SC-4 site 2 | `REQUIREMENTS.md:22-30` | the `[~]`-hold exception note rewritten | ✓ WIRED | Now reads "HTTP-09 **was** the exception … **closed on the merits by Phase 113.1** and now reads `[x]`; the hold below applies to the remaining `[~]` requirements only." No longer contradicts the bullet. |
| SC-4 site 3 | `REQUIREMENTS.md:213` | traceability row | ✓ WIRED | `\| HTTP-09 \| Phase 113.1 \| **Met** — bounded-read tripwire green with an EMPTY `WHOLE_BODY_ALLOWLIST`, plus two falsifiable O(n) guards on `SseParser::feed` \|`. Owning-phase column moved 113 → 113.1 as planned. |
| Plan 06 Part 3 | `ROADMAP.md:2530-2560` | falsified warning-block sentences corrected | ✓ WIRED | Both named-false sentences replaced; the `[~]` publication-hold paragraph and the `## Verdict … PENDING` reference are intact. |
| Plan 06 Part 4 | `deferred-items.md` D-113-Q/R/U | `**Status:** ✅ RESOLVED` + shas | ✓ WIRED | All six cited shas (`8d010dba`, `dca1702c`, `647d2f4b`, `1ca4b52c`, `7ee92523`, `58f82368`) resolve — `git cat-file -t` returns `commit` for each. No invented shas. |
| SC-5 record | Phase 116 | `**Owner:** Phase 116 (Auth Hardening SEPs)` | ✓ WIRED | Phase 116's ROADMAP goal is the same trust boundary these reads sit on, so the assignment is substantive, not nominal. |
| SC-1b | org `gate` on PR #299 | human `git push` | ✗ **NOT WIRED** | Remote head `fb99bca1`, local 537 ahead. CI has never evaluated this tree. |

**Wiring:** 6/7 connections verified.

---

### Criterion-Weakening Check (the anti-narrowing test)

SC-4 forbids closing HTTP-09 by narrowing it. There are three ways it *could* have been narrowed;
all three were checked mechanically at HEAD against `97cbbd08` (the pre-phase base):

| Narrowing vector | Result | Evidence |
|---|---|---|
| Edit the requirement sentence | ✓ **NOT NARROWED** | Line body byte-identical after removing only the 23-byte `— *NOT met; see below*` clause (489 → 466 bytes; remainder compares equal). |
| Shrink the tripwire's **scope fence** (which files get scanned) | ✓ **NOT NARROWED** | `const SHARED_DIR` and `const EXTRA_SCOPE` are **byte-identical** across the phase. |
| Weaken the tripwire's **needle set** (what counts as a read) | ✓ **NOT NARROWED** | `const WHOLE_BODY_NEEDLES` (8 needles) is **byte-identical** across the phase. |
| Net change to the tripwire | — | The **only** const that moved is `WHOLE_BODY_ALLOWLIST`, from one `NOT BOUNDED` entry to `&[]`. That is the direction that makes the requirement **stricter**, not weaker. |

This is the decisive evidence that HTTP-09 closed on the merits: the scanner still looks at exactly
the same files for exactly the same patterns, and now finds nothing to exempt.

---

### Data-Flow Trace (Level 4)

Not applicable in the usual sense — this phase ships no data-rendering surface. The analogous check
is whether the enforcing mechanisms are non-vacuous rather than merely present:

| Artifact | "Data" | Source | Real? | Status |
|---|---|---|---|---|
| `WHOLE_BODY_ALLOWLIST` emptiness | scanned file set | runtime `read_dir` of `src/shared/` + 2 named files, with an anti-vacuity assertion that discovery is non-empty (`:75`) | Yes — an empty scan cannot make the check pass | ✓ FLOWING |
| The 1 s linear-time budget | measured elapsed | real `SseParser::feed` under 512 Ki single-byte calls, `black_box`'d, min of 3 | Yes — ceiling sits 15.7x above linear and 6.8x below quadratic | ✓ FLOWING |
| `collect_sse_text_within_cap` bound | running total | `Response::chunk()`, checked **before** each append (not post-hoc) | Yes — over-cap body never held whole | ✓ FLOWING |
| D-113-V's "31" | per-file counts | reproducible whitespace-stripped scan | Yes — I reproduced 19/33 exactly | ✓ FLOWING |

---

### Behavioral Spot-Checks (this round's own measurements)

| Behavior | Command | Result | Status |
|---|---|---|---|
| SC-1a, the exact PR-blocking invocation | `pmat quality-gate --fail-on-violation --checks complexity` (pmat **3.15.0**, per CLAUDE.md's pin) | `Quality Gate: PASSED` / `Total violations: 0` / `✅ Quality gate PASSED`, **exit 0** | ✓ PASS |
| SC-1a, per-function, repo-wide | `pmat analyze complexity -p . --format json --top-files 0` (3.5 MB JSON, 887 files / 4688 functions) | **Zero** functions over cognitive 25 in any `*/src/` tree. Max = **23** (`src/server/schema_utils.rs::add_constraints`). Both handlers: **1** (wrappers), their `*_inner` pipelines: **5** each | ✓ PASS |
| SC-1b, has the branch been pushed? | `git ls-remote origin fix/mcp-publisher-oidc-audience` + `git status -sb` | Remote head `fb99bca15e5c…` (2026-07-10); local `c9944a65`, **ahead 537**. Confirms the SUMMARY's positive claim that no push occurred | ✓ CONFIRMS the claim; ✗ SC-1b unmet |
| SC-1b, is `gate` green? | `gh api repos/paiml/rust-mcp-sdk/commits/fb99bca1…/check-runs` (read-only) | `gate` **failure**; `Purity Gate` **failure**; `Quality Gate` **failure**; 10 others success | ✗ FAIL — SC-1b objectively not met |
| SC-4, is the requirement text unnarrowed? | Programmatic compare of the HTTP-09 line at `1df85229^` vs `1df85229` | 489 → 466 bytes; after removing only the 23-byte NOT-met clause the remainder is **byte-identical** | ✓ PASS |
| SC-4, did the flip touch anything else? | `git show --stat 1df85229 -- .planning/REQUIREMENTS.md` | 1 file, **+6 / -4**, three hunks — exactly the three planned sites. No `[~]` row moved | ✓ PASS |
| SC-5, is "31" reproducible? | Own Python scan of the four auth files for the tripwire's 8 needles, both raw-line and whitespace-stripped | **raw = 19** (5 / 5 / 4 / 5) and **whitespace-stripped = 33** (11 / 5 / 9 / 8) — reproduces D-113-V's table exactly. 33 − 2 named exclusions = **31** | ✓ PASS |
| SC-5, are they all production code? | Same scan vs each file's `mod tests` line | `generic_oidc.rs` max match 747 < `mod tests` 770; `auth.rs` 421 < 431; `cognito.rs` 513 < 536; `oauth.rs` has no `mod tests`. All above | ✓ PASS |
| Cited-sha integrity | `git cat-file -t` on all 6 shas in the resolved D-113-Q/R/U statuses | All return `commit` | ✓ PASS |
| Debt-marker gate (CLAUDE.md zero-SATD) | `grep -nE "TODO\|FIXME\|XXX\|HACK\|PLACEHOLDER\|\bTBD\b"` across the 8 files this phase changed | **Zero hits** | ✓ PASS |
| Suppression check | `git diff 97cbbd08..HEAD -- src/ \| grep '^+.*#\[allow('` | **Zero** new `#[allow(...)]` added anywhere in the phase's source diff — the complexity gate was passed by refactoring, not by suppression | ✓ PASS |
| Stub scan | `git diff 97cbbd08..HEAD -- src/ tests/ examples/ \| grep '^+.*(unimplemented!\|todo!\|placeholder\|not yet implemented)'` | **Zero hits** | ✓ PASS |
| `make quality-gate` | **NOT RE-RUN THIS ROUND** | Cited, not claimed: measured **exit 0** at this same commit `c9944a65` by the Phase 113 re-verification (`113-VERIFICATION.md` § Behavioral Spot-Checks), which also verified `rtk` exit-code fidelity. The tree has not changed since except `113-VERIFICATION.md` itself | ℹ️ CITED, not measured here |
| SC-2 timing harness | **NOT RE-RUN THIS ROUND** | Cited, not claimed: `drain_complete_lines` committed **29.22 ms** vs pre-fix shape **4.26 s**; growth **3.98x** vs **15.26x**. Measured minutes earlier at this same commit by the Phase 113 re-verification | ℹ️ CITED, not measured here |

### Probe Execution

Step 7c: **SKIPPED.** `find scripts -path '*/tests/probe-*.sh'` returns nothing, and no PLAN or
SUMMARY in this phase declares a probe. The project's runnable gate is `make quality-gate`, handled
above.

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| HTTP-09 | 113.1-01…06 (phase `requirements: [HTTP-09]`) | Every peer-controlled read on the v2 transport path is memory-bounded; enumerable via a tripwire with an explicit reviewed allowlist, and no scan over peer-chosen input worse than O(n) | ✓ **SATISFIED** | All three of its clauses have an enforcing mechanism in the tree: clause 1 — allowlist empty and pinned at `len() == 0`; clause 2 — the accumulation review test with the `chunk()` loop reviewed; clause 3 — two falsifiable guards, independently re-measured at 29.22 ms / 3.98x against a 4.26 s / 15.26x pre-fix shape. Fence and needles byte-unchanged. |

**Coverage:** 1/1 requirement satisfied. No orphaned requirements — `REQUIREMENTS.md`'s traceability
table maps only HTTP-09 to Phase 113.1.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/server/streamable_http_server.rs` | 3287-3289 | `**Complexity budget: cognitive 15** (pmat 3.15.0), down from 30` — the function measures **1** at HEAD | ⚠️ Warning | Stale rustdoc. Conservative direction (tree is better than claimed), but inaccurate in a file this phase owns. |
| `src/server/streamable_http_server.rs` | 3908-3910 | Same, "down from 31" — measures **1** at HEAD | ⚠️ Warning | Same. |
| `.planning/.../deferred-items.md` | 1180-1182 | D-113-U: "Both handlers now measure **cognitive 15**" | ⚠️ Warning | Stale by one commit (`dafc77c5`). |
| `.planning/ROADMAP.md` | 2551-2553 | "went 30 → **15** … 31 → **15**" | ⚠️ Warning | Stale by one commit. |

**Anti-patterns:** 4 found (**0 blockers**, 4 warnings — all the same root cause: `dafc77c5` landed
after the records were written and reduced the numbers further without refreshing them). Notably
**zero** debt markers, **zero** new `#[allow]`, **zero** stubs across the phase's 8 changed files.

---

## Human Verification Required

### 1. Push the branch and drive the org `gate` (SC-1b)

**Test:** `git push` `fix/mcp-publisher-oidc-audience` to `paiml/rust-mcp-sdk`; PR #299 picks up the
new head automatically. Watch the `gate` aggregate job
(`needs: [test, quality-gate, purity-check, pmcp-agent-targets]`).
**Expected:** `gate` green — which requires **first** clearing D-113-W (26 pre-existing rustdoc
errors failing `make doc-check` inside the `quality-gate` job) and the Purity Gate's tooling drift.
Both are pre-existing and outside a merge-unblock's charter.
**Why human:** GSD reserves push/PR/merge as human actions, and the phase's own D-20 records that
reservation explicitly. A verifier cannot discharge it, and must not.

### 2. Decide whether SC-1b's non-closure should be accepted as an override

**Test:** Read the SC-1 assessment below and decide whether the split is acceptable as scope
recording (my assessment: it is).
**Expected:** If accepted, add the override block in § Suggested Override to this file's frontmatter
and re-run verification — the phase then scores 5/5.
**Why human:** Overrides require a named accepter and timestamp. A verifier may suggest but may
never self-accept.

---

## Assessment: is the SC-1 split honest, or a requirement narrowed to fit what shipped?

**Verdict: honest scope-recording, not a narrowing.** Five independent tests, all passed:

1. **The charter text was not touched.** ROADMAP:2393 at HEAD still reads the full conjunctive
   sentence — "…passes with no `src/` function over cog 25, **and** the org-required `gate` status
   check is green on the branch". The `**Outcome (2026-07-27).**` block was **appended after** the
   Success Criteria and opens by declaring that the text above it is the charter, quoted as written
   and not a current-state claim. A narrowing would have edited the criterion; this did not.
2. **SC-1 is never claimed as met, anywhere.** The Outcome block says "SC-1 **split** … SC-1b **NOT**
   discharged". `113.1-06-SUMMARY.md` says it in the strongest available form: "**SC-1 is therefore
   never claimed as met — not in this document, and not in the ROADMAP.**" The milestone bullet
   qualifies the PMAT result as passing "**locally**" and names the outstanding push plus both
   pre-existing CI blockers in the same bullet. Half a conjunction was reported as half, not as
   whole.
3. **The un-discharged half is not code-addressable.** It requires a `git push` to a remote. I
   confirmed independently that no push occurred (`origin` head `fb99bca1`; local ahead 537) — which
   corroborates the SUMMARY's § 6 positive claim rather than contradicting it. No refactor,
   no test, no additional plan could have turned SC-1b green from inside the phase.
4. **The residual blockers are provably pre-existing, by a falsifiable method.** D-113-W was
   measured by stashing the phase's three source files and re-running `make doc-check`: 26 errors
   before the phase, 26 after each step, sorted diff **empty** at every point. I corroborated the
   two failing checks independently at the remote head (`Quality Gate` failure, `Purity Gate`
   failure). The phase did not cause them and the charter does not own them.
5. **The anti-narrowing tests on the criterion it *could* have quietly weakened all pass.** HTTP-09's
   requirement sentence is byte-identical; and the tripwire's scope fence and needle set — the real
   mechanism through which HTTP-09 could have been narrowed invisibly — are **byte-identical** too.
   The only thing that shrank is the allowlist, which makes the requirement stricter. A phase that
   was in the business of narrowing requirements to fit what shipped had an easy, low-visibility
   opportunity here and did not take it.

**The one place the record leans optimistic** (recorded as a warning, not a blocker): the phase
carries `- [x]` in the milestone list and the bullet opens *"the three blockers that kept this
branch from merging are CLOSED"* — when one of the three is closed only at its local half. The same
bullet's closing sentences correct this, and the ROADMAP states the convention that "Complete"
counts **plans**, not remote CI status. So it is disclosed, not concealed. But a reader who stops
at the first clause takes away more than the evidence supports, and the merge is *not* unblocked in
the sense the phase title promises. That is the gap this report records — as an open handoff with a
named owner and a known cost, not as a misreported success.

---

## Suggested Override

SC-1b's non-closure is a deliberate, documented handoff rather than incomplete work. If you accept
that framing, add to this file's frontmatter and re-run verification:

```yaml
overrides:
  - must_have: "pmat quality-gate complexity passes and the org-required gate status check is green on the branch"
    reason: >-
      SC-1a is discharged and independently verified (0 violations, no src/ function over cog 25).
      SC-1b requires a git push to paiml/rust-mcp-sdk, which D-20 reserves as a human action, plus
      clearing two pre-existing CI failures (D-113-W's 26 rustdoc errors, Purity Gate drift) that
      Phase 113.1 neither caused nor owns. Accepted as a named handoff; tracked in D-113-W and in
      the ROADMAP Outcome block.
    accepted_by: "{your name}"
    accepted_at: "{ISO timestamp}"
```

---

## Gaps Summary

### Critical Gaps (block the phase's stated goal, not its code)

1. **SC-1b — the org-required `gate` is not green, and CI has never seen this tree.**
   - Missing: a human `git push`; then D-113-W (26 pre-existing rustdoc errors) and the Purity Gate
     drift cleared.
   - Impact: the branch does **not** merge yet. The phase's goal — "clear the three things that keep
     the branch from merging" — is achieved for two of three blockers on the merits, and for the
     third only at its local half.
   - Fix: human handoff (see § Human Verification Required #1), or accept the override.

### Non-Critical Gaps (defer or fix cheaply)

1. **Stale complexity records, four sites.**
   - Issue: two rustdoc "Complexity budget: cognitive 15" lines in
     `src/server/streamable_http_server.rs`, plus D-113-U's status line and the ROADMAP warning
     block, all state a figure that commit `dafc77c5` superseded (wrapper 1 / inner 5).
   - Impact: limited — the direction is conservative and `dafc77c5`'s commit message records the
     change honestly. But two of the four sites are in production source this phase owns, and the
     phase's own last commit was a false-claim correction, so the applied standard is exacting.
   - Recommendation: fix the two rustdoc lines cheaply; the planning records can be refreshed with
     a one-line note or left with a pointer to `dafc77c5`.

### Known and explicitly out of scope (not counted as phase gaps)

- **D-113-W** — 26 pre-existing rustdoc errors failing `make doc-check` in CI's `quality-gate` job.
  Proven present at pre-phase HEAD. OPEN, unassigned; Phase 119 named as the natural home.
- **Purity Gate tooling/lockfile drift.** OPEN, pre-existing, unowned.
- **`cargo semver-checks`** — 1 minor-level failure (`#[deprecated]` added on `OptimizedSseTransport`).
  0 major, 0 public items removed, and the attribute's `since = "2.18.0"` already names the implied
  bump. Recorded by the phase rather than worked around; not a gap.

---

## Recommended Fix Plans

No fix plan is warranted. The single critical gap is a human action, not a planning gap, and the
non-critical gap is a four-line documentation refresh that does not justify a plan of its own —
fold it into whichever phase next touches `streamable_http_server.rs`.

---

## Verification Metadata

**Verification approach:** Goal-backward from ROADMAP Success Criteria (the contract), with
PLAN-level `must_haves` explicitly not permitted to reduce scope.
**Must-haves source:** `.planning/ROADMAP.md:2391-2397` (five chartered Success Criteria).
**Adversarial stance:** SUMMARY and ROADMAP Outcome claims treated as hypotheses under test. Six
claims were re-measured independently rather than accepted: the pmat gate result, both handlers'
complexity, the push state, the `gate` CI status, HTTP-09's text byte-identity, and D-113-V's counts.
**Measured this round:** pmat quality-gate (exit 0), pmat per-function sweep (4688 functions),
remote-head/push state, PR #299 check-runs, HTTP-09 byte-identity, tripwire fence/needle/allowlist
diffs, auth-read counts, sha integrity, debt/suppression/stub scans.
**Cited, not re-measured (stated explicitly):** `make quality-gate` exit 0 and the SC-2 timing
harness — both measured at this same commit `c9944a65` by the Phase 113 re-verification.
**Automated checks:** 14 passed, 1 failed (SC-1b).
**Human checks required:** 2.

---
*Verified: 2026-07-28T00:56:26Z*
*Verifier: Claude (gsd-verifier)*
