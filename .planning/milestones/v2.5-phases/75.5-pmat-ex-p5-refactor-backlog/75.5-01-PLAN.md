---
phase: 75.5-pmat-ex-p5-refactor-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/elicitation.rs
  - src/server/notification_debouncer.rs
  - src/server/resource_watcher.rs
  - src/server/mod.rs
  - src/server/transport/websocket_enhanced.rs
  - src/shared/sse_optimized.rs
  - src/shared/connection_pool.rs
  - src/shared/logging.rs
  - src/client/mod.rs
  - src/client/http_logging_middleware.rs
autonomous: true
requirements: []
tags:
  - pmat
  - cognitive-complexity
  - refactor
  - quality-debt

must_haves:
  truths:
    - "Zero `#[allow(clippy::cognitive_complexity)]` attributes remain in src/ for the 12 Category-A sites listed in 75.5-CONTEXT.md."
    - "`make quality-gate` exits 0 on the final commit of this plan."
    - "`pmat quality-gate --fail-on-violation --checks complexity` (CI command per CLAUDE.md) exits 0 with zero src/ violations."
    - "`cargo test --workspace --all-features` exits 0 — no behavior regression introduced by removal/refactor."
    - "For any site whose underlying function still measured cog > 25, the function was refactored to ≤ 25 (or, if D-03 ≤50 ceiling rationale applies, refactored to ≤ 50 with measurement documented in commit message). No function above 50 remains."
  artifacts:
    - path: "src/server/elicitation.rs"
      provides: "elicit_input — bare allow removed"
      contains: "fn elicit_input"
    - path: "src/server/notification_debouncer.rs"
      provides: "flush_pending — bare allow removed"
      contains: "fn flush_pending"
    - path: "src/server/resource_watcher.rs"
      provides: "process_events — bare allow removed"
      contains: "fn process_events"
    - path: "src/server/transport/websocket_enhanced.rs"
      provides: "handle_client_send + handle_connection — bare allows removed"
      contains: "fn handle_client_send"
    - path: "src/server/mod.rs"
      provides: "handle_call_tool — bare allow removed"
      contains: "fn handle_call_tool"
    - path: "src/shared/sse_optimized.rs"
      provides: "connect_sse — bare allow removed"
      contains: "fn connect_sse"
    - path: "src/shared/connection_pool.rs"
      provides: "start — bare allow removed"
      contains: "fn start"
    - path: "src/shared/logging.rs"
      provides: "log — bare allow removed"
      contains: "fn log"
    - path: "src/client/mod.rs"
      provides: "send_request — bare allow removed"
      contains: "fn send_request"
    - path: "src/client/http_logging_middleware.rs"
      provides: "log_request + log_response — bare allows removed"
      contains: "fn log_request"
  key_links:
    - from: "src/ source tree (12 Category-A sites)"
      to: ".github/workflows/ci.yml quality-gate job"
      via: "pmat quality-gate --fail-on-violation --checks complexity"
      pattern: "pmat quality-gate.*--checks complexity"
    - from: "Phase 75 Wave 0 D-10-B spike (75-ADDENDUM-D10B.md)"
      to: "this plan's removal action"
      via: "Empirical proof PMAT 3.15.0 ignores #[allow(clippy::cognitive_complexity)] — therefore bare allows are dead code; removing has zero gate-effect when underlying cog ≤25, and is mandatory when cog >25 because the allow does not suppress."
      pattern: "ignores .*cognitive_complexity"
---

<objective>
Remove the 12 ineffective bare `#[allow(clippy::cognitive_complexity)]` attributes from `src/` (Category A migrated wholesale from Phase 75 Plan 75-01 Task 1a-C). For each site, either (a) delete the allow because the underlying function is already at cog ≤25 (the expected outcome — Phase 75 Wave 5 closure messages confirm `make quality-gate` is currently green on main with these 12 allows in place), or (b) refactor the function to cog ≤25 first and then delete the allow if measurement reveals it is genuinely above the threshold.

Purpose: Phase 75 Wave 0 D-10-B empirically proved that **PMAT 3.15.0 ignores `#[allow(clippy::cognitive_complexity)]`** (see `pmat-allow-behavior.md` and `75-W0-SPIKE-RESULTS.md`). Every bare allow in the codebase is therefore dead code — it does not suppress the PMAT gate, and clippy itself does not warn unless cog actually exceeds 25 (in which case the allow is hiding real debt). Removing them improves clarity and prevents the misleading hint that the function is intentionally complex.

Output: 10 modified `src/` files (12 allows removed across them), final `make quality-gate` exit 0, final `cargo test --workspace --all-features` exit 0, zero PMAT complexity violations against the 12 sites.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-CONTEXT.md
@.planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-ESCAPEES.md
@.planning/phases/75-fix-pmat-issues/75-ADDENDUM-D10B.md
@.planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md
@.planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md
@.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
@.planning/phases/75-fix-pmat-issues/75-PATTERNS.md
@CLAUDE.md

<empirical_reality>
Verified by orchestrator on 2026-04-25 via `grep -rn '#\[allow(clippy::cognitive_complexity)\]' src/`:

| # | File | Line | Function |
|---|------|------|----------|
| 1 | src/server/elicitation.rs | 66 | elicit_input |
| 2 | src/server/notification_debouncer.rs | 240 | flush_pending |
| 3 | src/server/resource_watcher.rs | 111 | process_events |
| 4 | src/server/transport/websocket_enhanced.rs | 232 | handle_client_send |
| 5 | src/server/transport/websocket_enhanced.rs | 271 | handle_connection |
| 6 | src/server/mod.rs | 1061 | handle_call_tool |
| 7 | src/shared/sse_optimized.rs | 225 | connect_sse |
| 8 | src/shared/connection_pool.rs | 159 | start |
| 9 | src/shared/logging.rs | 433 | log |
| 10 | src/client/mod.rs | 1913 | send_request |
| 11 | src/client/http_logging_middleware.rs | 332 | log_request |
| 12 | src/client/http_logging_middleware.rs | 401 | log_response |

Note: PATTERNS.md said "13" — empirical count is 12. The 13th was Wave 1a's `streamable_http_server.rs:1004 handle_post_with_middleware` which Phase 75 already refactored.

Note: ESCAPEES.md is empty (Category B = 0). Scope is Category A only.

Note: pmat AST parsing is sparse — `pmat analyze complexity --include <FILE> --format json | jq '.files[0].functions | length'` on these 10 files returns:
  - notification_debouncer.rs: 3, resource_watcher.rs: 1, logging.rs: 1
  - elicitation.rs: 0, websocket_enhanced.rs: 0, server/mod.rs: 0, sse_optimized.rs: 0, connection_pool.rs: 0, client/mod.rs: 0, http_logging_middleware.rs: 0

For files where pmat returns 0 functions, executor MUST fall back to manual cognitive-complexity counting (see 75-RESEARCH.md "Pattern 1" / "Pattern 2" sections — count +1 per branch decision, +nesting per nested branch). Alternatively, since `make quality-gate` currently passes on `main` with these 12 allows in place, the empirical inference is that all 12 underlying functions are either (a) below cog 25 already, or (b) below pmat's AST visibility. Either way, removing the bare allow is safe and correct: clippy will warn at cog > 25, and since CI is currently green, no warning is expected.
</empirical_reality>

<refactor_techniques_if_needed>
If a site requires refactor (cog > 25), apply these patterns from 75-RESEARCH.md:
- **P1 (Extract per-arm helpers):** for top-level `match cmd { A => ..., B => ... }` shapes — pull each arm body into a named `fn handle_a(...)` helper. Used successfully in Phase 75 Waves 2-4.
- **P2 (Early-return guards):** convert `if a { if b { if c { ... } } }` into `if !a { return Err(...) } if !b { return Err(...) } ...`.
- **P3 (Extract validation closures):** long validation pipelines → named helpers per concern.
- **P4 (State enum + dispatch table):** for protocol-handler hot paths.

Do NOT add a fresh `#[allow(clippy::cognitive_complexity)]` — Rule 1 of 75-ADDENDUM-D10B.md prohibits it (it is ineffective).
</refactor_techniques_if_needed>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove bare allows from server/ + server/transport/ (6 sites)</name>
  <files>
    src/server/elicitation.rs,
    src/server/notification_debouncer.rs,
    src/server/resource_watcher.rs,
    src/server/mod.rs,
    src/server/transport/websocket_enhanced.rs
  </files>
  <read_first>
    @.planning/phases/75-fix-pmat-issues/75-ADDENDUM-D10B.md
    @.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
    @src/server/elicitation.rs
    @src/server/notification_debouncer.rs
    @src/server/resource_watcher.rs
    @src/server/mod.rs
    @src/server/transport/websocket_enhanced.rs
  </read_first>
  <action>
For each of the 6 sites in this group, follow this exact procedure (do not batch — handle one site at a time so failures localize):

**Sites to process (in order):**
1. `src/server/elicitation.rs:66` — `elicit_input`
2. `src/server/notification_debouncer.rs:240` — `flush_pending`
3. `src/server/resource_watcher.rs:111` — `process_events`
4. `src/server/mod.rs:1061` — `handle_call_tool`
5. `src/server/transport/websocket_enhanced.rs:232` — `handle_client_send`
6. `src/server/transport/websocket_enhanced.rs:271` — `handle_connection`

**Per-site procedure:**

1. **Measure cognitive complexity.** Run:
   ```bash
   pmat analyze complexity --include "<FILE>" --format json \
     | jq -r '.files[0].functions[]? | select(.name == "<FUNC>" or (.name | endswith("::<FUNC>"))) | "\(.line_start) \(.name) cog=\(.metrics.cognitive)"'
   ```
   If output is empty (pmat AST limit on this file — expected for 4 of these 6 sites per the empirical-reality table in plan context), measure manually using 75-RESEARCH.md cognitive-complexity counting rules: +1 per branch decision (if/match arm/loop/&&/||), +nesting per nested branch. Read the function body and count. Note the measured value.

2. **Decide action based on measurement:**
   - **If cog ≤ 25 (expected outcome):** Delete the `#[allow(clippy::cognitive_complexity)]` line at the recorded site (use `Edit` tool — match the indentation: lines 232 and 271 of `websocket_enhanced.rs` and line 1061 of `server/mod.rs` are 4-space indented inside an impl/method block; the rest are at module top-level). Skip step 3.
   - **If cog > 25:** Apply P1/P2/P3/P4 from 75-RESEARCH.md to refactor the function to ≤ 25. Common pattern for handler-style functions like `handle_call_tool`, `handle_client_send`, `handle_connection`: P1 extract per-message-type or per-WS-frame helpers. Common pattern for `flush_pending`, `process_events`: P2 early-return + P3 extract per-event-kind handlers. Re-measure after refactor. Then delete the allow.
   - **If cog > 50 after a P1-P4 pass:** Apply more aggressive extraction to reach ≤50 (D-03 ceiling, mandatory). If still residual >25 after exhausting techniques, document the measurement in the commit message and proceed (the bare allow MUST come off either way — D-10-B says it is dead code).

3. **Verify per-site behavior preservation (cheap check, run after each site):**
   ```bash
   cargo build --features full --all-targets 2>&1 | tail -5
   ```
   Must exit 0. If it fails (e.g., a deleted allow exposes an `unused_attributes` lint elsewhere or a refactor introduced a borrow error), fix before moving to the next site.

4. **Note results in a scratch tally** (kept in plan executor's working memory, surfaced in SUMMARY.md):
   - `<file>:<line> <func> — cog measured = N — action = (removed | refactored to N) — build OK`

**Concrete `Edit` semantics:** Each site is a single-line deletion. Example for `src/server/elicitation.rs:66`:
- Before: line 66 contains `    #[allow(clippy::cognitive_complexity)]` (4-space indent because it sits inside an `impl` block above `fn elicit_input`).
- After: the line is removed. The function attribute chain (e.g., `pub async fn elicit_input` on line 67) shifts up by one.
- Use `Edit` with `old_string` containing 1-2 lines of surrounding context to disambiguate (the attribute line + the function signature line below it), and `new_string` containing only the function signature line.

**Order rationale:** elicitation.rs first (smallest file, simplest attribute), then notification_debouncer/resource_watcher (mid-size, top-level fns), then server/mod.rs (large file, attribute is method-level — careful with Edit context), then websocket_enhanced.rs both lines together (same file, both method-level attributes — read once, edit twice with distinct context strings).

Do NOT run `make quality-gate` per site — that's the final task's job. Per-site `cargo build --features full --all-targets` is sufficient incremental verification.
  </action>
  <verify>
    <automated>
! grep -n "#\[allow(clippy::cognitive_complexity)\]" src/server/elicitation.rs src/server/notification_debouncer.rs src/server/resource_watcher.rs src/server/mod.rs src/server/transport/websocket_enhanced.rs && cargo build --features full --all-targets
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "#\[allow(clippy::cognitive_complexity)\]" src/server/elicitation.rs src/server/notification_debouncer.rs src/server/resource_watcher.rs src/server/mod.rs src/server/transport/websocket_enhanced.rs` returns NO matches (empty output, exit 1).
    - `cargo build --features full --all-targets` exits 0.
    - For any site that required refactor, the commit message records the measured cog before/after.
    - No fresh `#[allow(clippy::cognitive_complexity)]` introduced anywhere in the touched files (forbidden by 75-ADDENDUM-D10B Rule 1; an allow is dead code and obscures real debt).
  </acceptance_criteria>
  <done>
    All 6 sites in server/ and server/transport/ have their bare `#[allow(clippy::cognitive_complexity)]` removed, the workspace still builds with `--features full --all-targets`, and any site that required refactoring has been refactored to cog ≤ 25 (or ≤ 50 with documented measurement if D-03 ceiling rationale applies). Tally noted for inclusion in plan SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove bare allows from shared/ + client/ (6 sites)</name>
  <files>
    src/shared/sse_optimized.rs,
    src/shared/connection_pool.rs,
    src/shared/logging.rs,
    src/client/mod.rs,
    src/client/http_logging_middleware.rs
  </files>
  <read_first>
    @.planning/phases/75-fix-pmat-issues/75-ADDENDUM-D10B.md
    @.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
    @src/shared/sse_optimized.rs
    @src/shared/connection_pool.rs
    @src/shared/logging.rs
    @src/client/mod.rs
    @src/client/http_logging_middleware.rs
  </read_first>
  <action>
For each of the 6 sites in this group, follow the same procedure as Task 1.

**Sites to process (in order):**
1. `src/shared/sse_optimized.rs:225` — `connect_sse` (75-RESEARCH.md Pattern 5 has a worked example for this exact site — read the format prescription block)
2. `src/shared/connection_pool.rs:159` — `start`
3. `src/shared/logging.rs:433` — `log`
4. `src/client/http_logging_middleware.rs:332` — `log_request`
5. `src/client/http_logging_middleware.rs:401` — `log_response`
6. `src/client/mod.rs:1913` — `send_request`

**Per-site procedure** (identical to Task 1):

1. **Measure cog** with the same `pmat analyze complexity --include "<FILE>" ...` query. Per the empirical-reality table, only `logging.rs` reports a function (1) — the rest fall back to manual counting.

2. **Decide action:**
   - **cog ≤ 25:** delete the allow line.
   - **cog > 25:** apply P1/P2/P3/P4 from 75-RESEARCH.md, re-measure, delete the allow.
     - For `connect_sse`: 75-RESEARCH.md Pattern 5 explicitly mentions this site as a candidate that "would have been retro-justified" — but per D-10-B the retro-justification doesn't help; if it's >25 after recovery-loop extraction, log to ESCAPEES.md and refactor harder.
     - For `log_request` / `log_response` / `log`: typical pattern is P3 (extract per-log-level or per-field-kind closures into helpers).
     - For `send_request`: typical pattern is P1 + P2 (extract per-message-type handlers + early-return on transport error).
     - For `start` (connection_pool): typical pattern is P1 (extract per-connection-lifecycle helpers).
   - **cog > 50:** mandatory refactor to ≤ 50 (D-03), document in commit message.

3. **Verify per-site:** `cargo build --features full --all-targets` after each site. Note: `src/client/mod.rs` and `src/shared/sse_optimized.rs` are large files — be careful with `Edit` context strings to avoid ambiguity (include the function signature line as part of `old_string`).

4. **Note results in scratch tally**, same format as Task 1.

**Concrete attribute indentation note:** `connect_sse` (sse_optimized.rs:225), `start` (connection_pool.rs:159), `log` (logging.rs:433), `log_request` and `log_response` (http_logging_middleware.rs:332, 401), and `send_request` (client/mod.rs:1913) are all 4-space indented inside `impl` blocks (verified by orchestrator grep output showing leading 4-space indent on each match). The `Edit` `old_string` should preserve that indent so the surrounding function signature line provides disambiguation.

Do NOT run `make quality-gate` per site — final task's job.

**Why this task is parallelizable with Task 1 in principle but is run sequentially here:** Same wave, same plan, same executor. No file overlap with Task 1, but running sequentially keeps the tally coherent and the build state clean.
  </action>
  <verify>
    <automated>
! grep -n "#\[allow(clippy::cognitive_complexity)\]" src/shared/sse_optimized.rs src/shared/connection_pool.rs src/shared/logging.rs src/client/mod.rs src/client/http_logging_middleware.rs && cargo build --features full --all-targets
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "#\[allow(clippy::cognitive_complexity)\]" src/shared/sse_optimized.rs src/shared/connection_pool.rs src/shared/logging.rs src/client/mod.rs src/client/http_logging_middleware.rs` returns NO matches.
    - `cargo build --features full --all-targets` exits 0.
    - Any refactored site has measured cog before/after recorded in commit message.
    - No fresh `#[allow(clippy::cognitive_complexity)]` introduced.
    - Combined with Task 1: `grep -rn "#\[allow(clippy::cognitive_complexity)\]" src/` returns zero matches across the entire `src/` tree.
  </acceptance_criteria>
  <done>
    All 6 sites in shared/ and client/ have their bare `#[allow(clippy::cognitive_complexity)]` removed, the workspace still builds with `--features full --all-targets`, and the entire `src/` tree contains zero remaining bare allows for `clippy::cognitive_complexity`. Tally noted for SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final phase verification — make quality-gate + cargo test --workspace + pmat CI gate command + commit</name>
  <files>
    .planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-01-SUMMARY.md
  </files>
  <read_first>
    @CLAUDE.md
    @.planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-CONTEXT.md
    @.planning/phases/75-fix-pmat-issues/75-ADDENDUM-D10B.md
    @.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
Run the full Toyota-Way quality gate, the CI complexity gate command, and the workspace test suite. Then write the plan SUMMARY.md.

1. **Full Toyota Way quality gate (matches CI exactly per CLAUDE.md "Release Steps"):**
   ```bash
   make quality-gate
   ```
   Must exit 0. This runs `cargo fmt --all -- --check`, `cargo clippy` with pedantic+nursery on `--features full`, `cargo build`, `cargo test`, and `cargo audit`. If clippy now warns about `cognitive_complexity` on any of the 12 just-uncovered functions (i.e., the bare allow was masking a real >25 cog), revisit that site and refactor (do NOT re-add the allow — see 75-ADDENDUM-D10B Rule 1). If the function genuinely cannot reach ≤25 after a P1-P4 pass, escalate to operator (the assumption that all 12 are at cog ≤25 was wrong, in which case Phase 75 Wave 5's claim of `make quality-gate` exit 0 was incorrect — flag this as a separate bug to investigate).

2. **PMAT complexity gate (matches `.github/workflows/ci.yml` `quality-gate` job per Phase 75 Wave 5):**
   ```bash
   pmat quality-gate --fail-on-violation --checks complexity
   ```
   Must exit 0. If pmat is not installed locally, log a note in SUMMARY.md and rely on CI to verify post-merge (per Phase 75 Wave 5 SUMMARY's local-pmat evidence pattern).

3. **Full workspace test suite (no behavior regression):**
   ```bash
   cargo test --workspace --all-features
   ```
   Must exit 0.

4. **Write plan SUMMARY.md** at `.planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-01-SUMMARY.md` per the summary template, including:
   - The per-site tally accumulated from Tasks 1 + 2 (file:line, function name, measured cog, action taken — removed vs refactored).
   - Confirmation that `grep -rn "#\[allow(clippy::cognitive_complexity)\]" src/` returns 0 matches.
   - `make quality-gate` and `cargo test --workspace --all-features` and `pmat quality-gate --fail-on-violation --checks complexity` exit codes.
   - Note that ESCAPEES.md remains empty (Category B = 0 across Phase 75).
   - Aggregate Phase 75 + 75.5 status: "All Category A bare allows in src/ removed; complexity gate clean; no behavior regression."

5. **Commit** all changes (Task 1 + Task 2 file edits + this SUMMARY.md) using the project commit style:
   ```
   refactor(75.5-01): remove 12 ineffective bare clippy::cognitive_complexity allows from src/

   Per Phase 75 Wave 0 D-10-B empirical finding: PMAT 3.15.0 ignores
   #[allow(clippy::cognitive_complexity)]. The 12 bare allows in src/ were
   therefore dead code — they did not suppress the PMAT gate, and clippy did
   not warn because the underlying functions are at cog <=25 (verified: pmat
   complexity gate still clean after removal). Removing them improves clarity
   and prevents the misleading hint that the function is intentionally complex.

   Sites cleared: <list-from-tally>

   Verification:
   - grep -rn '#[allow(clippy::cognitive_complexity)]' src/  -> 0 matches
   - make quality-gate -> exit 0
   - pmat quality-gate --fail-on-violation --checks complexity -> exit 0
   - cargo test --workspace --all-features -> exit 0

   Closes Phase 75.5 Category A (12/12 sites). Category B remained empty across
   Phase 75 Waves 1-4 (75.5-ESCAPEES.md unchanged).
   ```

   Stage only the touched src/ files + the SUMMARY.md. Do NOT use `git add -A` (per CLAUDE.md). Do NOT skip hooks.
  </action>
  <verify>
    <automated>
make quality-gate && cargo test --workspace --all-features && pmat quality-gate --fail-on-violation --checks complexity && test -f .planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-01-SUMMARY.md && ! grep -rqn "#\[allow(clippy::cognitive_complexity)\]" src/
    </automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0 (matches CI exactly per CLAUDE.md).
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0 (matches `.github/workflows/ci.yml` `quality-gate` job command per Phase 75 Wave 5).
    - `cargo test --workspace --all-features` exits 0 (no behavior regression from removals/refactors).
    - `grep -rn "#\[allow(clippy::cognitive_complexity)\]" src/` returns 0 matches.
    - SUMMARY.md exists at `.planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-01-SUMMARY.md` and includes the per-site tally + verification command outputs.
    - A single git commit captures the src/ edits + SUMMARY.md (no unrelated files staged; pre-commit hook ran successfully — not bypassed with --no-verify).
    - `git log --oneline -1` shows the new commit subject `refactor(75.5-01): ...`.
  </acceptance_criteria>
  <done>
    Phase 75.5 Plan 01 is complete: all 12 Category-A bare allows are gone from src/, all three quality verifications (make quality-gate, pmat complexity gate, cargo test --workspace --all-features) exit 0, the SUMMARY.md records the work, and the changes are committed via the standard pre-commit-enforced path. ROADMAP.md Phase 75.5 Plans entry can be flipped to checked.
  </done>
</task>

</tasks>

<threat_model>
**N/A: pure refactor; no behavior change, no new endpoints, no new data flows, no new dependencies, no auth/transport additions.**

The 12 sites are all internal helper functions or instance methods. Removing a `#[allow(...)]` attribute or applying P1-P4 extract-method refactors does not change runtime semantics, network exposure, parsing surface, or trust boundaries. Existing test suites (`cargo test --workspace --all-features`) cover behavior preservation; the Phase 75 Wave 5 CI gate covers complexity preservation. No new STRIDE register required.
</threat_model>

<verification>
**Plan-level verification (final task gates these):**

1. `grep -rn "#\[allow(clippy::cognitive_complexity)\]" src/` → 0 matches (all 12 Category A sites cleared).
2. `make quality-gate` → exit 0 (full Toyota Way gate per CLAUDE.md).
3. `pmat quality-gate --fail-on-violation --checks complexity` → exit 0 (matches CI command per Phase 75 Wave 5 D-07 enforcement).
4. `cargo test --workspace --all-features` → exit 0 (behavior preservation).
5. SUMMARY.md exists with per-site tally + verification outputs.
6. Single coherent commit on the working branch (no force pushes, no hook bypasses, pre-commit gate green).

**Why these are sufficient:** The phase goal (75.5-CONTEXT.md) is "all Category A bare allows resolved" + "PMAT complexity gate count drops to 0 (in conjunction with Phase 75's work)". Check 1 satisfies the first; checks 2-3 satisfy the second; check 4 ensures nothing regressed in the process. Check 5-6 satisfy GSD plan-execution hygiene.
</verification>

<success_criteria>
- All 12 listed bare `#[allow(clippy::cognitive_complexity)]` attributes removed from src/.
- For any site whose underlying function still has cog > 25 after work, the function has been refactored to cog ≤ 25 (or ≤ 50 with explicit D-03 documentation in commit message — but this is unexpected since `make quality-gate` is currently passing on main with these 12 allows in place).
- `make quality-gate` exits 0 on the final commit.
- `pmat quality-gate --fail-on-violation --checks complexity` (CI command) reports zero src/ violations.
- No regressions: `cargo test --workspace --all-features` exits 0.
- ESCAPEES.md unchanged (Category B was empty across Phase 75 Waves 1-4 and remains so).
- SUMMARY.md committed alongside src/ edits in a single coherent commit.
</success_criteria>

<output>
After completion, create `.planning/phases/75.5-pmat-ex-p5-refactor-backlog/75.5-01-SUMMARY.md` per the gsd summary template, including:
- Tasks completed (1, 2, 3) with per-site tally from Tasks 1+2.
- Verification command exit codes (make quality-gate, pmat complexity gate, cargo test --workspace --all-features).
- Confirmation that grep returns 0 matches in src/.
- Notes on any sites that required refactor (expected: zero, since gate is currently passing on main).
- Aggregate Phase 75 + 75.5 closure status: "All Category A bare allows in src/ removed; ESCAPEES.md (Category B) was empty across Phase 75 Waves 1-4; complexity gate clean."
</output>
