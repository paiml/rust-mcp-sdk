---
phase: 71
plan: 04
type: execute
wave: 4
depends_on: [71-03]
autonomous: true
requirements: [PARITY-MACRO-01]
requirements_addressed: [PARITY-MACRO-01]
tags: [release, version-bump, requirements, quality-gate, ripple-audit]
files_modified:
  - pmcp-macros/Cargo.toml
  - crates/pmcp-macros-support/Cargo.toml
  - Cargo.toml
  - cargo-pmcp/Cargo.toml
  - crates/mcp-tester/Cargo.toml
  - CHANGELOG.md
  - .planning/REQUIREMENTS.md
must_haves:
  truths:
    - "pmcp-macros version bumped 0.5.0 → 0.6.0."
    - "pmcp-macros-support pinned at 0.1.0 (new crate, no prior version to bump)."
    - "Root Cargo.toml pmcp-macros dep pins (both optional prod dep and dev-dep for examples) bumped to 0.6.0."
    - "pmcp root version bumped 2.3.0 → 2.4.0 (MINOR) — new feature surface (`#[mcp_tool]` accepts rustdoc-only fns) warrants a minor bump per Codex review MEDIUM-4 and semver conventions for additive features."
    - "Workspace pmcp-dependency ripple audited explicitly: `grep -rn '^pmcp = ' Cargo.toml cargo-pmcp/Cargo.toml crates/*/Cargo.toml fuzz/Cargo.toml` run and each pin's shape recorded. Pins of form `\"2.2.0\"` are caret (accept 2.4.0) — no bump required. No non-caret pmcp pins (`= 2.3.0`, `~2.3.0`) are found."
    - "CHANGELOG.md has a new entry for pmcp 2.4.0 / pmcp-macros 0.6.0 citing PARITY-MACRO-01 + the new pmcp-macros-support 0.1.0 support crate."
    - "REQUIREMENTS.md row 145 reflects PARITY-MACRO-01 as covered by Phase 71; status updated from Pending to Complete."
    - "All 25 `#[mcp_tool]` call sites compile unchanged under `cargo check --workspace --examples --features full`."
    - "cargo-pmcp version bumped 0.6.0 → 0.6.1 (concurrent patch bump per CLAUDE.md §\"Version Bump Rules\" discipline — downstream crates that pin a bumped dependency must also be bumped)."
    - "mcp-tester version bumped 0.5.0 → 0.5.1 (same discipline)."
    - "`make quality-gate` passes end-to-end on the final tree."
  artifacts:
    - path: "pmcp-macros/Cargo.toml"
      provides: "version = \"0.6.0\""
      contains: "version = \"0.6.0\""
    - path: "Cargo.toml"
      provides: "Root crate 2.4.0 + pmcp-macros 0.6.0 pins"
      contains: "pmcp-macros = { version = \"0.6.0\""
    - path: "CHANGELOG.md"
      provides: "Release note for pmcp 2.4.0 / pmcp-macros 0.6.0 / pmcp-macros-support 0.1.0"
      contains: "rustdoc fallback"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARITY-MACRO-01 → Phase 71 → Complete"
      contains: "| PARITY-MACRO-01 | Phase 71 | Complete |"
  key_links:
    - from: "Cargo.toml (root)"
      to: "pmcp-macros/Cargo.toml + crates/pmcp-macros-support/Cargo.toml"
      via: "path dep pins"
      pattern: "pmcp-macros = \\{ version = \"0\\.6\\.0\""
    - from: "CHANGELOG.md"
      to: "71-RESEARCH.md + 69-PROPOSALS Proposal 3"
      via: "release note text citing PARITY-MACRO-01"
      pattern: "PARITY-MACRO-01"
---

<objective>
Release mechanics for Phase 71 with explicit workspace ripple audit (Codex HIGH-2):

(a) bump `pmcp-macros` 0.5.0 → 0.6.0
(b) bump BOTH `pmcp-macros = { version = "0.5.0", ... }` pins in root `Cargo.toml` (optional prod dep + dev-dep for examples) to `"0.6.0"`
(c) bump pmcp root `2.3.0 → 2.4.0` — **minor bump** per Codex MEDIUM-4 because the macro now accepts a newly-valid source form (rustdoc-only functions); that is additive feature surface, conventionally a minor bump
(d) **explicit workspace `pmcp`-dependency ripple audit** (HIGH-2): grep every `^pmcp = ` line across the workspace, record each pin's shape, and either bump or justify skipping for every downstream pin
(e) pmcp-macros-support 0.1.0 is already pinned (new crate from Plan 01) — no bump needed
(f) CHANGELOG.md entry for pmcp 2.4.0 + pmcp-macros 0.6.0 + pmcp-macros-support 0.1.0 citing PARITY-MACRO-01
(g) update `.planning/REQUIREMENTS.md` row 145 (`PARITY-MACRO-01 | TBD | Pending` → `PARITY-MACRO-01 | Phase 71 | Complete`)
(h) run `make quality-gate` + `cargo check --workspace --examples --features full` + `cargo test --doc -p pmcp-macros --features full` as the 3-gate sign-off

Purpose: Finalize the phase per proposal Success Criterion 5. Release pipeline can publish `pmcp-macros-support 0.1.0` + `pmcp-macros 0.6.0` + `pmcp 2.4.0` via the existing workspace publish order (pmcp-widget-utils → **pmcp-macros-support (new)** → pmcp-macros → pmcp → mcp-tester → mcp-preview → cargo-pmcp) documented in CLAUDE.md §"Release & Publish Workflow" — updated to include the new support crate.

**Strategy note — MEDIUM-4 resolution (semver posture):**
The prior iteration bumped pmcp 2.3.0 → 2.3.1 (patch). Codex correctly observed that the macro surface ACCEPTS a newly valid source form: a function previously rejected at compile time (`#[mcp_tool]` with no description attribute) now compiles successfully. That is additive feature surface. Rust semver convention for additive features is a minor bump. This plan uses `2.3.0 → 2.4.0`.

Because the existing downstream pins in `cargo-pmcp/Cargo.toml:38` and `crates/mcp-tester/Cargo.toml:21` are `pmcp = { version = "2.2.0", ... }` (caret, accepts all 2.x), no downstream ripple is forced by either 2.3.1 or 2.4.0 — the minor vs. patch choice is semver-posture, not resolution-mechanics. HIGH-2's ripple audit confirms this; the explicit grep is still required so the reasoning is recorded.

**Strategy note — HIGH-2 resolution (workspace ripple audit):**
The prior iteration audited only `pmcp-macros =` pins. This plan adds an explicit, grep-verifiable audit of `^pmcp = ` across ALL workspace Cargo.toml files. Expected findings (from the 2026-04-17 state of the tree):
- `cargo-pmcp/Cargo.toml:38` — `pmcp = { version = "2.2.0", ... }` — caret, accepts 2.4.0 — NO BUMP needed.
- `crates/mcp-tester/Cargo.toml:21` — `pmcp = { version = "2.2.0", ... }` — caret, accepts 2.4.0 — NO BUMP needed.
- `crates/mcp-preview/Cargo.toml` — no `pmcp` dep — SKIP.
- No exact-version pins (`= 2.3.0`) or tilde pins (`~2.3.0`) found.

If the executor's actual grep finds non-caret pins that do NOT accept 2.4.0, the executor MUST concurrently bump those downstream crates per CLAUDE.md's "Downstream crates that pin a bumped dependency must also be bumped" rule and add their bumps to this plan's edits. The acceptance criteria below include the grep as evidence.

**Strategy note — W2 resolution (caret-pin discipline, CLAUDE.md verbatim):**
CLAUDE.md §"Version Bump Rules" states: *"Downstream crates that pin a bumped dependency must also be bumped (e.g., if `pmcp` bumps, update the `pmcp = { version = "..." }` line in `mcp-tester/Cargo.toml` and `cargo-pmcp/Cargo.toml`, and bump their versions)"*. The example names both downstream crates explicitly and does not qualify the rule with pin-shape mechanics. Although the current `pmcp = { version = "2.2.0", ... }` caret pins in `cargo-pmcp/Cargo.toml:38` and `crates/mcp-tester/Cargo.toml:21` would accept 2.4.0 without edits, this plan chooses **Option A (discipline)** — concurrently patch-bumps both downstream crates to honor the rule literally:
- `cargo-pmcp` 0.6.0 → 0.6.1
- `mcp-tester` 0.5.0 → 0.5.1

Rationale: avoids creating semantic debt where a release captain reading CLAUDE.md would reasonably expect concurrent bumps. Two-line Cargo.toml edits + two CHANGELOG bullets is cheaper than the ambiguity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-RESEARCH.md
@.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-PATTERNS.md
@.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-VALIDATION.md
@.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-REVIEWS.md
@.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-01-SUMMARY.md
@.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-02-SUMMARY.md
@.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-03-SUMMARY.md
@.planning/REQUIREMENTS.md
@CHANGELOG.md

<interfaces>
<!-- Exact version strings this plan mutates. -->

Current pmcp root crate version (`Cargo.toml:3`):
```toml
version = "2.3.0"
```

Current pmcp-macros version (`pmcp-macros/Cargo.toml:3`):
```toml
version = "0.5.0"
```

Current pmcp-macros-support version (`crates/pmcp-macros-support/Cargo.toml:3` — landed in Plan 01):
```toml
version = "0.1.0"
```

Current root dep pins for pmcp-macros (`Cargo.toml`):
```toml
# Line 53 — optional prod dep:
pmcp-macros = { version = "0.5.0", path = "pmcp-macros", optional = true }
# Line 147 — dev-dep for examples:
pmcp-macros = { version = "0.5.0", path = "pmcp-macros" }  # For macro examples (s23_mcp_tool_macro)
```

Current downstream pmcp pins (from 2026-04-17 tree state — confirm with grep):
```toml
# cargo-pmcp/Cargo.toml:38 — caret, accepts 2.4.0:
pmcp = { version = "2.2.0", path = "..", features = ["streamable-http", "oauth"] }
# crates/mcp-tester/Cargo.toml:21 — caret, accepts 2.4.0:
pmcp = { version = "2.2.0", path = "../../", features = ["streamable-http", "oauth"] }
# crates/mcp-preview/Cargo.toml — no pmcp dep
```

REQUIREMENTS.md row 145:
```markdown
| PARITY-MACRO-01 | TBD | Pending |
```
</interfaces>

<read_first>
1. `71-REVIEWS.md` §"HIGH-2" — full ripple-audit directive + expected pin shapes.
2. `71-REVIEWS.md` §"MEDIUM-4" — semver-posture decision (2.4.0, not 2.3.1).
3. `71-PATTERNS.md` §"pmcp-macros/Cargo.toml + root Cargo.toml (version bump)" (lines 576-625) — before/after diff shape.
4. Current `pmcp-macros/Cargo.toml` line 3 — verify `version = "0.5.0"`.
5. Current root `Cargo.toml` line 3 — verify `version = "2.3.0"`.
6. Current root `Cargo.toml` lines 53, 147 — verify both `pmcp-macros = { version = "0.5.0", ... }` pins.
7. Current `cargo-pmcp/Cargo.toml` line 38 — record exact `pmcp = ...` pin shape.
8. Current `crates/mcp-tester/Cargo.toml` line 21 — record exact `pmcp = ...` pin shape.
9. Current `crates/mcp-preview/Cargo.toml` — confirm absence of `pmcp` dep.
10. `CHANGELOG.md` top of file — find the most recent version header to anchor the new `## [2.4.0]` section above it.
11. `.planning/REQUIREMENTS.md` lines 140-150 — confirm row 145 text. Line 56 — confirm checkbox text.
12. `CLAUDE.md` §"Release & Publish Workflow" — §"Version Bump Rules" + §"Release Steps" + publish order (NOTE: publish order now includes pmcp-macros-support; plan 04 Task 3 documents this).
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace pmcp-dependency ripple audit + version bumps (pmcp-macros 0.5.0→0.6.0, root 2.3.0→2.4.0, 2 root pins 0.5.0→0.6.0, concurrent downstream bumps cargo-pmcp 0.6.0→0.6.1 + mcp-tester 0.5.0→0.5.1 per CLAUDE.md §"Version Bump Rules")</name>
  <files>
    pmcp-macros/Cargo.toml,
    Cargo.toml,
    cargo-pmcp/Cargo.toml,
    crates/mcp-tester/Cargo.toml
  </files>
  <read_first>
    - 71-REVIEWS.md §"HIGH-2" — explicit audit procedure.
    - 71-REVIEWS.md §"MEDIUM-4" — 2.4.0 (minor bump) rationale.
    - Current `pmcp-macros/Cargo.toml:3`, root `Cargo.toml:3`, lines 53 and 147.
    - Current `cargo-pmcp/Cargo.toml:38`, `crates/mcp-tester/Cargo.toml:21`, `crates/mcp-preview/Cargo.toml` (no pmcp dep).
    - CLAUDE.md §"Version Bump Rules" — "Downstream crates that pin a bumped dependency must also be bumped" — applies ONLY to non-caret pins.
  </read_first>
  <behavior>
    - Ripple audit executed: `grep -rn '^pmcp = ' Cargo.toml cargo-pmcp/Cargo.toml crates/*/Cargo.toml fuzz/Cargo.toml` run; each pin's shape recorded.
    - For each pin found: caret `^2.2.0` or `"2.2.0"` accepts 2.4.0 — NO bump needed; exact `= 2.3.0` or tilde `~2.3.0` does NOT accept 2.4.0 — concurrent bump required.
    - In the expected 2026-04-17 state: no non-caret pins exist → no downstream bumps.
    - pmcp-macros/Cargo.toml line 3: `version = "0.5.0"` → `version = "0.6.0"`.
    - Cargo.toml line 3: `version = "2.3.0"` → `version = "2.4.0"`.
    - Cargo.toml line 53: `pmcp-macros = { version = "0.5.0", path = "pmcp-macros", optional = true }` → `pmcp-macros = { version = "0.6.0", ... }`.
    - Cargo.toml line 147: `pmcp-macros = { version = "0.5.0", path = "pmcp-macros" }  # ...` → `pmcp-macros = { version = "0.6.0", path = "pmcp-macros" }  # ...`.
    - cargo-pmcp/Cargo.toml version 0.6.0 → 0.6.1 (concurrent patch bump per CLAUDE.md §"Version Bump Rules" discipline, even though caret pin accepts 2.4.0).
    - crates/mcp-tester/Cargo.toml version 0.5.0 → 0.5.1 (same discipline).
    - `cargo check --workspace --features full` exits 0.
    - `cargo check --workspace --examples --features full` exits 0 (all 25 call sites compile).
  </behavior>
  <action>
    **Step 1 — Ripple audit (HIGH-2):** Run the following grep and record EACH matching line in a commit-message-local note (will be summarized in the plan SUMMARY):

    ```bash
    grep -rn '^pmcp = ' Cargo.toml cargo-pmcp/Cargo.toml crates/*/Cargo.toml fuzz/Cargo.toml 2>/dev/null
    ```

    Also audit any inline-table form:

    ```bash
    grep -rn 'pmcp *= *{' Cargo.toml cargo-pmcp/Cargo.toml crates/*/Cargo.toml fuzz/Cargo.toml 2>/dev/null | grep -v 'pmcp-macros' | grep -v 'pmcp-widget-utils' | grep -v 'pmcp-code-mode' | grep -v 'pmcp-tasks' | grep -v 'pmcp-server' | grep -v 'pmcp-run'
    ```

    Expected output (2026-04-17 state):
    - `cargo-pmcp/Cargo.toml:38: pmcp = { version = "2.2.0", path = "..", features = ["streamable-http", "oauth"] }`
    - `crates/mcp-tester/Cargo.toml:21: pmcp = { version = "2.2.0", path = "../../", features = ["streamable-http", "oauth"] }`

    For EACH pin found:
    - `pmcp = "2.2.0"` (caret semantics by default in `Cargo.toml`) accepts 2.4.0 → **NO BUMP NEEDED**. Record as "skipped (caret accepts 2.4.0)".
    - `pmcp = "= 2.3.0"` (exact) does NOT accept 2.4.0 → bump the crate's `pmcp` pin to `"= 2.4.0"` AND bump that crate's own version per CLAUDE.md — THIS PLAN's files_modified list would need expansion; STOP and flag for the user.
    - `pmcp = "~2.3.0"` (tilde) does NOT accept 2.4.0 → same flag as exact.

    If the audit reveals ANY non-caret pin, halt, document the finding in the plan SUMMARY as a blocker, and request explicit user approval before proceeding with the bump.

    **Step 2** — Edit `pmcp-macros/Cargo.toml`. At line 3:

    ```toml
    version = "0.5.0"
    ```
    →
    ```toml
    version = "0.6.0"
    ```

    **Step 3** — Edit root `Cargo.toml`. Make THREE changes:

    (a) Line 3:
    ```toml
    version = "2.3.0"
    ```
    →
    ```toml
    version = "2.4.0"
    ```

    (b) Line 53 (optional prod dep):
    ```toml
    pmcp-macros = { version = "0.5.0", path = "pmcp-macros", optional = true }
    ```
    →
    ```toml
    pmcp-macros = { version = "0.6.0", path = "pmcp-macros", optional = true }
    ```

    (c) Line 147 (dev-dep — preserve trailing comment):
    ```toml
    pmcp-macros = { version = "0.5.0", path = "pmcp-macros" }  # For macro examples (s23_mcp_tool_macro)
    ```
    →
    ```toml
    pmcp-macros = { version = "0.6.0", path = "pmcp-macros" }  # For macro examples (s23_mcp_tool_macro)
    ```

    **Step 4** — Concurrent downstream patch bumps per CLAUDE.md §"Version Bump Rules" (W2 Option A — honor the rule literally regardless of caret-pin mechanics).

    (a) Edit `cargo-pmcp/Cargo.toml`. Find the crate's own `version = "0.6.0"` line (near the top, in `[package]`):
    ```toml
    version = "0.6.0"
    ```
    →
    ```toml
    version = "0.6.1"
    ```
    Leave the `pmcp = { version = "2.2.0", ... }` pin at line 38 UNCHANGED (caret accepts 2.4.0).

    (b) Edit `crates/mcp-tester/Cargo.toml`. Find the crate's own `version = "0.5.0"` line (near the top, in `[package]`):
    ```toml
    version = "0.5.0"
    ```
    →
    ```toml
    version = "0.5.1"
    ```
    Leave the `pmcp = { version = "2.2.0", ... }` pin at line 21 UNCHANGED (caret accepts 2.4.0).

    (c) If Step 1's audit found ANY non-caret `pmcp = "= X.Y.Z"` or `~X.Y.Z` pin in any workspace crate (unexpected per 2026-04-17 state), that crate's `pmcp` pin MUST ALSO be updated to accept 2.4.0; add that as an additional concurrent bump here.

    **Step 5** — Verify no stray `0.5.0` references for pmcp-macros remain:

    ```bash
    grep -rn 'pmcp-macros.*"0\.5\.0"' Cargo.toml pmcp-macros/ crates/ cargo-pmcp/ fuzz/ 2>/dev/null
    ```

    Must return zero lines.

    **Step 6** — Run `cargo check --workspace --features full`. Must exit 0.

    **Step 7** — Run `cargo check --workspace --examples --features full`. Must exit 0 — proves all 25 `#[mcp_tool]` call sites still compile after the bumps.
  </action>
  <verify>
    <automated>cargo check --workspace --examples --features full</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^version = "0.6.0"' pmcp-macros/Cargo.toml` returns exactly 1 line (line 3).
    - `grep -n '^version = "2.4.0"' Cargo.toml` returns exactly 1 line (line 3).
    - `grep -c 'pmcp-macros = { version = "0.6.0"' Cargo.toml` returns exactly 2 (lines 53 and 147).
    - `grep -c 'pmcp-macros = { version = "0.5.0"' Cargo.toml` returns 0.
    - `grep -c '^version = "0.5.0"' pmcp-macros/Cargo.toml` returns 0.
    - `grep -c '^version = "2.3.0"' Cargo.toml` returns 0.
    - `grep -rn 'pmcp-macros.*"0\.5\.0"' crates/ cargo-pmcp/ fuzz/ 2>/dev/null` returns 0 lines.
    - `grep -rn '^pmcp = ' Cargo.toml cargo-pmcp/Cargo.toml crates/*/Cargo.toml fuzz/Cargo.toml 2>/dev/null` executed — output recorded in plan SUMMARY.
    - No non-caret pmcp pin (`= 2.3.0`, `~2.3.0`, `= 2.4.0`, etc.) exists anywhere other than this plan's edits (verified by `grep -rn 'pmcp = "= ' Cargo.toml cargo-pmcp/ crates/ fuzz/ 2>/dev/null` returning empty, and `grep -rn 'pmcp = "~' Cargo.toml cargo-pmcp/ crates/ fuzz/ 2>/dev/null` returning empty).
    - `grep -c '^version = "0.6.1"' cargo-pmcp/Cargo.toml` returns exactly 1 (concurrent downstream patch bump per CLAUDE.md §"Version Bump Rules").
    - `grep -c '^version = "0.5.1"' crates/mcp-tester/Cargo.toml` returns exactly 1 (same discipline).
    - `grep -c '^version = "0.6.0"' cargo-pmcp/Cargo.toml` returns 0.
    - `grep -c '^version = "0.5.0"' crates/mcp-tester/Cargo.toml` returns 0.
    - `cargo check --workspace --features full` exit 0.
    - `cargo check --workspace --examples --features full` exit 0 (25 call sites compile).
  </acceptance_criteria>
  <done>Ripple audit completed + recorded; five version strings updated (pmcp-macros 0.6.0, root 2.4.0, 2 root pins 0.6.0, cargo-pmcp 0.6.1, mcp-tester 0.5.1); no stray 0.5.0 for pmcp-macros; CLAUDE.md §"Version Bump Rules" discipline honored via concurrent downstream patch bumps; `cargo check --workspace --examples` green on all 25 sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CHANGELOG.md entry for pmcp 2.4.0 / pmcp-macros 0.6.0 / pmcp-macros-support 0.1.0 + update REQUIREMENTS.md row 145 traceability + tick line 56 checkbox + update footer</name>
  <files>
    CHANGELOG.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - Existing `CHANGELOG.md` first ~60 lines — find the most recent version header to anchor the new entry.
    - `.planning/REQUIREMENTS.md` line 56 — checkbox to tick.
    - `.planning/REQUIREMENTS.md` line 145 — row to update.
    - `.planning/REQUIREMENTS.md` footer — Last-updated line to append.
    - 71-RESEARCH.md §"Proposed Change" — one-sentence summary.
  </read_first>
  <behavior>
    - CHANGELOG.md gains a new top-of-file entry for `## [2.4.0] - 2026-04-17` with subsections `### Added` (pmcp-macros 0.6.0 rustdoc fallback + pmcp-macros-support 0.1.0 new support crate), `### Changed` (error message wording + semver-posture bump), `### Internal` (trybuild snapshots + property tests + fuzz target).
    - REQUIREMENTS.md line 56 checkbox: `- [ ]` → `- [x]`.
    - REQUIREMENTS.md row 145: `| PARITY-MACRO-01 | TBD | Pending |` → `| PARITY-MACRO-01 | Phase 71 | Complete |`.
    - REQUIREMENTS.md footer appended with today's date + note about the closure.
  </behavior>
  <action>
    **Step 1** — Read the top of `CHANGELOG.md` to find the most recent version header. Insert a NEW entry ABOVE it (or replace `## [Unreleased]` with `## [2.4.0] - 2026-04-17` if that convention is in use — match the file's existing style exactly).

    Add this entry:

    ```markdown
    ## [2.4.0] - 2026-04-17

    ### Added
    - **pmcp-macros 0.6.0**: `#[mcp_tool]` now harvests the annotated function's rustdoc comment as the tool description when the `description = "..."` attribute is omitted (PARITY-MACRO-01). Explicit attributes always win over rustdoc; when neither is present, the macro fails with a clear error naming both options. Backwards-compatible — all existing call sites continue to work unchanged.
    - **pmcp-macros-support 0.1.0** (new workspace crate): pure non-proc-macro helpers for `pmcp-macros`, extracted so property tests and fuzz targets can consume the rustdoc-harvest normalizer without running into the proc-macro crate's public-API restrictions. Workspace-internal — external users should depend on `pmcp` (with the `macros` feature) or `pmcp-macros` directly, not on this crate.
    - **pmcp-macros README**: New "Rustdoc-derived descriptions (pmcp-macros 0.6.0+)" migration section with a compiling `rust,no_run` doctest, plus a "Limitations" subsection enumerating unsupported rustdoc forms (`#[doc = include_str!(...)]`, `#[cfg_attr(..., doc = "...")]`, indented code fences, explicit empty-string descriptions).

    ### Changed
    - **pmcp-macros**: Error message for `#[mcp_tool]` without a description updated from `mcp_tool requires at least \`description = "..."\` attribute` to `mcp_tool requires either a \`description = "..."\` attribute or a rustdoc comment on the function` — names both fallback sources.
    - **pmcp**: Minor version bump 2.3.0 → 2.4.0. The macro surface accepts a newly valid source form (rustdoc-only tool functions); this is additive feature surface, conventionally a minor bump.
    - Bumped `pmcp-macros` 0.5.0 → 0.6.0 (additive, backwards-compatible minor bump).
    - **cargo-pmcp**: Patch bump 0.6.0 → 0.6.1 (concurrent downstream bump per CLAUDE.md §"Version Bump Rules" — even though the `pmcp = "2.2.0"` caret pin accepts 2.4.0 without edit, the rule is honored literally to avoid release-captain ambiguity).
    - **mcp-tester**: Patch bump 0.5.0 → 0.5.1 (same rationale).

    ### Internal
    - New workspace crate `crates/pmcp-macros-support/` scaffolded with the pure normalization helper, unit tests for 10 normalization vectors + 3 unsupported-form cases, and 4 proptest invariants at 1000 cases each (reference-equivalence, determinism, no-panic on arbitrary UTF-8, mixed-attr-shape robustness).
    - New trybuild compile-fail snapshots `mcp_tool_missing_description_and_rustdoc.rs` (empty-args) and `mcp_tool_nonempty_args_missing_description_and_rustdoc.rs` (non-empty-args) lock the new error wording against regression.
    - New fuzz target `fuzz/fuzz_targets/rustdoc_normalize.rs` exercises the normalizer via `pmcp-macros-support` with mixed attribute shapes (plain doc + `#[doc(hidden)]` + `#[doc(alias = ...)]` + non-doc attrs).
    - Shared resolver `pmcp-macros/src/mcp_common.rs::resolve_tool_args` is the single entry point consumed by both `#[mcp_tool]` parse sites (standalone fn in `mcp_tool.rs`, impl-block method in `mcp_server.rs::parse_mcp_tool_attr`) — eliminates the drift risk of duplicated call sequences.
    ```

    If the CHANGELOG uses different category names, map the content onto them. Do NOT remove or reword existing 2.3.0 content.

    **Step 2** — Edit `.planning/REQUIREMENTS.md` line 56. Current:

    ```markdown
    - [ ] **PARITY-MACRO-01**: Support rustdoc as a fallback source for `#[mcp_tool]` descriptions, so well-documented tool functions do not have to repeat themselves in the macro attribute.
    ```

    Change to:

    ```markdown
    - [x] **PARITY-MACRO-01**: Support rustdoc as a fallback source for `#[mcp_tool]` descriptions, so well-documented tool functions do not have to repeat themselves in the macro attribute.
    ```

    **Step 3** — Edit `.planning/REQUIREMENTS.md` line 145. Current:

    ```markdown
    | PARITY-MACRO-01 | TBD | Pending |
    ```

    Change to:

    ```markdown
    | PARITY-MACRO-01 | Phase 71 | Complete |
    ```

    **Step 4** — Update the footer at the bottom of REQUIREMENTS.md. Current footer shape (line ~154):

    ```markdown
    *Last updated: 2026-04-16 — added 3 PARITY-* IDs seeded by Phase 69 rmcp parity research*
    ```

    Append a new line (preserve history):

    ```markdown
    *Last updated: 2026-04-17 — PARITY-MACRO-01 closed by Phase 71 (pmcp 2.4.0 / pmcp-macros 0.6.0 / pmcp-macros-support 0.1.0 — rustdoc fallback)*
    ```

    Or replace the single line if the file uses a single-line convention — match existing style.
  </action>
  <verify>
    <automated>grep -c "PARITY-MACRO-01 | Phase 71 | Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## \[2.4.0\]' CHANGELOG.md` returns exactly 1.
    - `grep -c 'PARITY-MACRO-01' CHANGELOG.md` returns ≥ 1.
    - `grep -c 'rustdoc fallback' CHANGELOG.md` returns ≥ 1.
    - `grep -c 'pmcp-macros-support' CHANGELOG.md` returns ≥ 1.
    - `grep -c 'cargo-pmcp.*0\.6\.1' CHANGELOG.md` returns ≥ 1.
    - `grep -c 'mcp-tester.*0\.5\.1' CHANGELOG.md` returns ≥ 1.
    - `grep -c '- \[x\] \*\*PARITY-MACRO-01\*\*' .planning/REQUIREMENTS.md` returns exactly 1.
    - `grep -c '- \[ \] \*\*PARITY-MACRO-01\*\*' .planning/REQUIREMENTS.md` returns 0.
    - `grep -c 'PARITY-MACRO-01 | Phase 71 | Complete' .planning/REQUIREMENTS.md` returns exactly 1.
    - `grep -c 'PARITY-MACRO-01 | TBD | Pending' .planning/REQUIREMENTS.md` returns 0.
    - `grep -c '2026-04-17 — PARITY-MACRO-01' .planning/REQUIREMENTS.md` returns exactly 1.
  </acceptance_criteria>
  <done>CHANGELOG.md entry added for 2.4.0 release naming all three crate versions; REQUIREMENTS.md line 56 ticked, row 145 shows Phase 71 Complete, footer reflects closure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final CI sweep — `cargo check --workspace --examples --features full` + `cargo test --doc -p pmcp-macros --features full` + `make quality-gate` as the phase sign-off</name>
  <files>
    (none — this task runs CI commands; no file edits unless failures require remediation)
  </files>
  <read_first>
    - CLAUDE.md §"Pre-Commit Quality Gates" + §"Release Steps Step 4" — `make quality-gate` is the single-command final gate.
    - CLAUDE.md §"Release & Publish Workflow" publish order — note that `pmcp-macros-support` needs to be added to the publish list BEFORE `pmcp-macros`. Record this in the SUMMARY for the release cycle that publishes these crates.
    - 71-RESEARCH.md §"Validation Architecture / Quality-gate hook summary" — the 9 checks under `make quality-gate`.
    - 71-VALIDATION.md — sampling contract.
    - Plan 01-03 must_haves for full sign-off inventory.
  </read_first>
  <behavior>
    - `cargo check --workspace --examples --features full` exits 0 (all 25 `#[mcp_tool]` call sites compile).
    - `cargo test --doc -p pmcp-macros --features full` exits 0 (README doctest green).
    - `make quality-gate` exits 0 (all 9 sub-gates green).
    - Property tests (`cargo test -p pmcp-macros-support --test property_tests`) continue to pass.
    - Trybuild snapshots (`cargo test -p pmcp-macros --features full -- compile_fail_tests`) continue to pass (4 fixtures).
    - Fuzz target compiles (`cd fuzz && cargo build --bin rustdoc_normalize`).
    - If any gate fails, the failure surfaces immediately and this task flags a blocker.
  </behavior>
  <action>
    **Step 1** — Run the three gates in sequence (do NOT parallelize):

    ```bash
    cargo check --workspace --examples --features full
    ```

    If this fails:
    - Call-site regression → investigate the specific file reported.
    - Dependency resolution → check `cargo tree -p pmcp-macros` for conflicts.

    ```bash
    cargo test --doc -p pmcp-macros --features full
    ```

    If this fails:
    - The README doctest is broken. Inspect the error; most likely cause is the `use` line not matching the existing `### Example` block. Plan 03 Task 2 Step 2 mandated byte-identical `use` lines; if a later edit diverged, restore symmetry.

    ```bash
    make quality-gate
    ```

    If this fails at `lint`:
    - Pedantic/nursery clippy on newly-added helpers. Apply minimal fixes (often `#[must_use]`, removing an unnecessary `.clone()`). Do NOT add `#[allow(clippy::...)]` unless the lint is a genuine false-positive and the allow is scoped tightly.

    If it fails at `check-unwraps`:
    - Propagate `syn::Result<_>` or use `?` — the helpers from Plans 01+02 should already avoid `.unwrap()` in non-test code.

    If it fails at `check-todos`:
    - No `TODO` / `FIXME` / `XXX` / `SATD` comments in any new file.

    **Step 2** — Confirm the three Nyquist coverage sampling rules from 71-VALIDATION.md:
    - Per-task commit: every `cargo test -p pmcp-macros`/`pmcp-macros-support` already satisfied.
    - Per-wave merge: `cargo check --workspace --examples && cargo test -p pmcp-macros` satisfied by Task 1 + this task.
    - Phase gate: `make quality-gate` passes.

    **Step 3** — If `cargo fuzz` is available, optionally re-run the fuzz smoke test:

    ```bash
    cd fuzz && timeout 30s cargo fuzz run rustdoc_normalize -- -max_total_time=20 || true
    ```

    Timeout exit is SUCCESS; assertion panic is FAILURE. If `cargo fuzz` not installed, Plan 03's `cargo build --bin rustdoc_normalize` is sufficient.

    **Step 4** — Confirm publish-order note for the release cycle that ships these crates. Per CLAUDE.md §"Release & Publish Workflow", the current workspace publish order is:
    1. `pmcp-widget-utils`
    2. `pmcp` (core SDK)
    3. `mcp-tester`
    4. `mcp-preview`
    5. `cargo-pmcp`

    With Phase 71 landing `pmcp-macros-support`, the updated publish order for the 2.4.0 release cycle is:
    1. `pmcp-widget-utils` (leaf, no internal deps)
    2. **`pmcp-macros-support`** (new, leaf — no internal deps beyond syn/proc-macro2)
    3. `pmcp-macros` (depends on pmcp-macros-support; publishes transitively with pmcp via the `macros` feature)
    4. `pmcp` (core SDK; depends on pmcp-macros via optional `macros` feature)
    5. `mcp-tester`
    6. `mcp-preview`
    7. `cargo-pmcp`

    Record this updated order in the plan SUMMARY. Updating CLAUDE.md itself is OUT OF SCOPE for Phase 71 — flag as a follow-up note in the SUMMARY so the next release-plan PR updates the doc.

    **Step 5** — Write a one-line summary of which gates passed into the plan SUMMARY for cross-reference by the Phase 71 verifier.
  </action>
  <verify>
    <automated>make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --workspace --examples --features full` exit 0.
    - `cargo test --doc -p pmcp-macros --features full` exit 0.
    - `make quality-gate` exit 0.
    - `cargo test -p pmcp-macros-support` exit 0 (Plan 01's unit + property tests still green).
    - `cargo test -p pmcp-macros --features full -- compile_fail_tests` exit 0 (Plan 03's 4 trybuild snapshots green).
    - `cd fuzz && cargo build --bin rustdoc_normalize` exit 0.
    - `grep -rn 'pmcp-macros.*"0\.5\.0"' .` returns 0 lines.
    - `grep -c '^version = "2.4.0"' Cargo.toml` returns exactly 1.
    - `grep -c '^version = "0.6.0"' pmcp-macros/Cargo.toml` returns exactly 1.
    - `grep -c '^version = "0.1.0"' crates/pmcp-macros-support/Cargo.toml` returns exactly 1.
  </acceptance_criteria>
  <done>`make quality-gate` green; all CI sweeps green; all 25 `#[mcp_tool]` call sites compile unchanged; property + trybuild + fuzz + doctest surfaces green; updated publish order recorded in SUMMARY; PARITY-MACRO-01 ready for release sign-off.</done>
</task>

</tasks>

<verification>
Phase-level verification (all 10 must_haves from planning context + review findings):

1. Rustdoc-only fn compiles with normalized description → Plan 02 `test_rustdoc_only_description` + Plan 03 README doctest.
2. Attribute wins byte-for-byte on 3 s23 sites → Plan 02 `test_attribute_wins_over_rustdoc` + `cargo check --workspace --examples` in this plan.
3. Neither-present compile-fail locked by THREE trybuild snapshots (MEDIUM-2) → Plan 03 + `cargo test -- compile_fail_tests`.
4. `description = ""` semantic locked (MEDIUM-3) → Plan 02 Task 1 unit tests + Plan 03 Limitations subsection.
5. Unsupported rustdoc forms documented (MEDIUM-3) → Plan 01 Task 2 unit tests + Plan 03 Task 2 Limitations subsection.
6. All 25 call sites compile unchanged → `cargo check --workspace --examples --features full` this Task 1 + Task 3.
7. `make quality-gate` passes → this Task 3.
8. ALWAYS requirements met → FUZZ (Plan 03 fuzz target, mixed-shape per LOW-3), PROPERTY (Plan 01 property tests, 4 invariants), UNIT (Plan 01 lib tests + Plan 02 resolver tests), EXAMPLE (Plan 03 README doctest).
9. Both parse sites byte-symmetric via SHARED RESOLVER (MEDIUM-1) → Plan 02 Task 2 + symmetric integration tests.
10. pmcp-macros 0.6.0 + pmcp 2.4.0 (MEDIUM-4) + pmcp-macros-support 0.1.0 (HIGH-1 Option A) → this Task 1 + Plan 01.
11. Workspace pmcp ripple audit (HIGH-2) → this Task 1 Step 1 + SUMMARY recording of grep output.
12. PARITY-MACRO-01 ticked in REQUIREMENTS.md → this Task 2.
</verification>

<success_criteria>
- Five version strings bumped: pmcp-macros 0.5.0 → 0.6.0, root pmcp 2.3.0 → 2.4.0 (minor, per MEDIUM-4), both root dep pins 0.5.0 → 0.6.0, cargo-pmcp 0.6.0 → 0.6.1 (concurrent per CLAUDE.md §"Version Bump Rules" W2 Option A), mcp-tester 0.5.0 → 0.5.1 (same discipline).
- pmcp-macros-support 0.1.0 landed in Plan 01, no bump needed here.
- Workspace `pmcp`-dependency ripple audit executed with output recorded in SUMMARY (HIGH-2).
- No non-caret pmcp pins found, or if found, additional concurrent downstream bumps applied beyond the Option-A baseline.
- CHANGELOG.md has `## [2.4.0]` entry citing PARITY-MACRO-01 + all three crate versions.
- REQUIREMENTS.md line 56 ticked; row 145 = `| PARITY-MACRO-01 | Phase 71 | Complete |`; footer updated.
- `make quality-gate` exit 0.
- `cargo check --workspace --examples --features full` exit 0.
- `cargo test --doc -p pmcp-macros --features full` exit 0.
- Updated publish order (including `pmcp-macros-support`) recorded in SUMMARY.
- All 5 proposal Success Criteria from 69-PROPOSALS.md Proposal 3 satisfied.
- All 2 HIGH + 4 MEDIUM + 3 LOW findings from 71-REVIEWS.md addressed or explicitly justified.
</success_criteria>

<output>
After completion, create `.planning/phases/71-rustdoc-fallback-for-mcp-tool-tool-descriptions-parity-macro/71-04-SUMMARY.md` documenting: ripple-audit grep output (HIGH-2), three version bumps applied (MEDIUM-4 rationale), CHANGELOG entry, REQUIREMENTS.md traceability update, `make quality-gate` pass confirmation, `cargo check --workspace --examples` pass, updated publish order for the 2.4.0 release cycle (HIGH-1 Option A ripple), and explicit sign-off on all 10 phase must_haves + all 9 review findings. Include `git diff --stat` output for Cargo.toml + pmcp-macros/Cargo.toml + crates/pmcp-macros-support/Cargo.toml + CHANGELOG.md + .planning/REQUIREMENTS.md as evidence.
</output>
</content>
</invoke>