---
phase: 72
plan: 03
type: execute
wave: 3
depends_on:
  - 72-01-PLAN
  - 72-02-PLAN
files_modified:
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
autonomous: true
requirements:
  - RMCP-EVAL-05
tags:
  - research
  - decision
  - rmcp
  - foundations
  - recommendation
  - synthesis

must_haves:
  truths:
    - "72-RECOMMENDATION.md starts with `**Recommendation:** <A|B|C1|C2|D|DEFER>` in the first 5 lines (NEW option set per reviews-mode; E is NOT a valid recommendation — it is contingency-only per 72-STRATEGY-MATRIX.md footnote)"
    - "72-RECOMMENDATION.md contains at least 5 `### Criterion` subsections, one per strategy-matrix criterion"
    - "Each `### Criterion` subsection cites at least one threshold (T1..T9) from 72-DECISION-RUBRIC.md AND at least one row number from 72-INVENTORY.md OR 72-STRATEGY-MATRIX.md (semantic audit gate — new per reviews MEDIUM finding)"
    - "Recommendation is derived by walking the Decision Tree in 72-DECISION-RUBRIC.md — NOT by defaulting to B"
    - "If net-resolved T-count < 3: Recommendation = DEFER; If 3..4: Recommendation = D; If ≥5: highest-scoring non-disqualified option"
  artifacts:
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md"
      provides: "Final go/no-go recommendation with per-criterion justification — closes RMCP-EVAL-05"
      min_lines: 100
  key_links:
    - from: "72-RECOMMENDATION.md"
      to: "72-DECISION-RUBRIC.md thresholds T1..T9"
      via: "per-criterion subsections cite threshold IDs"
      pattern: "T[1-9]"
    - from: "72-RECOMMENDATION.md"
      to: "72-STRATEGY-MATRIX.md option rows"
      via: "recommendation cites the winning option's matrix cells for each criterion"
      pattern: "A\\. Full adopt|B\\. Hybrid|C1\\. Selective|C2\\. Selective|D\\. Status quo|DEFER"
    - from: "72-RECOMMENDATION.md"
      to: "72-INVENTORY.md rows"
      via: "per-criterion justifications cite inventory rows by number"
      pattern: "72-INVENTORY|row [0-9]"
    - from: "72-RECOMMENDATION.md"
      to: "72-POC-RESULTS.md"
      via: "T4 justification cites the real measured compile-error count from the Slice 1 spike"
      pattern: "T4_compile_errors|72-POC-RESULTS"
---

<objective>
Synthesize the six prior deliverables (72-CONTEXT.md, 72-INVENTORY.md, 72-STRATEGY-MATRIX.md, 72-POC-PROPOSAL.md, 72-POC-RESULTS.md, 72-DECISION-RUBRIC.md) into Phase 72's single final output: the go/no-go recommendation.

**Revision scope (reviews mode):**
- **Removes the default-to-B rule entirely** per HIGH-1 (both reviewers consensus). The decision tree now treats B as just another scorable option, never as a fallback.
- **Adds DEFER as an explicit valid outcome** when net-resolved thresholds < 3; if 3..4, falls back to D (most conservative), NEVER B.
- **Uses the revised option set** {A, B, C1, C2, D, DEFER}. E is NOT a valid recommendation letter (contingency footnote only).
- **NEW Task 1b: semantic audit task** verifies every `### Criterion` subsection cites a T-ID from the rubric AND a row number from inventory or matrix (addresses MEDIUM consensus finding). Audit failure → recommendation auto-downgrades to DEFER.
- **Uses real T4 data** from 72-POC-RESULTS.md (Slice 1 spike output from Plan 02 Task 1b). T4 is RESOLVED in this plan — not UNRESOLVED.

Purpose: close RMCP-EVAL-05. Convert measured evidence into a single answer: should pmcp refactor its protocol layer onto rmcp 1.5.0? The recommendation must be traceable (every justification cites a T-ID AND an inventory/matrix row — enforced by the semantic audit), falsifiable (UNRESOLVED thresholds are stated, not hidden), and contained.

Output:
  - `72-RECOMMENDATION.md` — opens with `**Recommendation:** <A|B|C1|C2|D|DEFER>`, followed by 5 `### Criterion` justification subsections, an UNRESOLVED-thresholds section, decision-tree traversal log, and a "Next Steps" pointer matched to the chosen letter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-REVIEWS.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-RESULTS.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-02-SUMMARY.md

<interfaces>
Recommendation file shape (mandated by 72-VALIDATION.md row 5, UPDATED per reviews-mode):
- First 5 lines MUST contain `^\*\*Recommendation:\*\* (A|B|C1|C2|D|DEFER)` (revised option set; E prohibited).
- At least 5 `### Criterion` H3 subsections, one per strategy-matrix criterion.

Semantic audit (NEW per reviews MEDIUM finding):
Every `### Criterion` subsection MUST contain:
  - At least one T-ID reference matching regex `T[1-9]`
  - At least one row-number reference matching regex `(row|Row) [0-9]+` OR a matrix-option reference matching `A\.|B\.|C1\.|C2\.|D\.|Full adopt|Hybrid|Selective|Status quo`

If the audit fails, the recommendation is AUTO-DOWNGRADED to DEFER by Task 1b before commit.

Decision-tree consumption (from 72-DECISION-RUBRIC.md §"Decision Tree"):
Net-resolved count N := number of T1..T9 with concrete measured value.
  - N < 3 → DEFER
  - N = 3..4 → D
  - N ≥ 5 → highest-scoring non-disqualified option (ties → lower migration cost; order D > C2 > C1 > B > A)

NEVER:
  - Default to B when evidence is unresolved (removed per HIGH-1).
  - Select E as primary recommendation (contingency-only).

T4 is RESOLVED via 72-POC-RESULTS.md (Slice 1 spike executed in Plan 02 Task 1b).
T6 and T7 are RESOLVED if 72-CONTEXT.md has concrete values; UNRESOLVED if UNKNOWN.
T3 is UNRESOLVED unless a future phase executes Slice 2.
T8 is RESOLVED via git log query (run at synthesis time).
T9 is RESOLVED via matrix lookup.
T1, T2, T5 are RESOLVED at synthesis time via deliverable content.
</interfaces>

<read_first>
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md (Plan 01 Task 0 — T6 and T7 values)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (Plan 01 Task 2 — row citations)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md (Plan 01 Task 3 — winning option's cells)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md (Plan 02 Task 1)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-RESULTS.md (Plan 02 Task 1b — REAL T4 data)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md (Plan 02 Task 2 — T1..T9, decision tree)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-REVIEWS.md (reviews-mode driver)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-02-SUMMARY.md
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md
</read_first>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (markdown only) | Plan 03 produces the final recommendation document; no runtime code paths introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05 | Repudiation (recommendation without cited evidence could be later disowned) | 72-RECOMMENDATION.md per-criterion subsections | mitigate | Every criterion subsection MUST cite a threshold ID AND an inventory/matrix row; semantic audit (Task 1b) enforces this, auto-downgrading to DEFER if any subsection fails |
| T-72-06 | Tampering (aesthetic adoption of rmcp despite rubric failure — Pitfall 3 from 72-RESEARCH.md) | recommendation pick | mitigate | Executor MUST follow the revised Decision Tree in 72-DECISION-RUBRIC.md (default-to-B rule REMOVED); if thresholds disqualify A/B, recommendation MUST pick C1/C2/D/DEFER |
| T-72-10 | Information Disclosure (Plan 03 might paper over UNRESOLVED thresholds) | UNRESOLVED Thresholds section | mitigate | Every T-ID with UNRESOLVED state MUST appear in the `## UNRESOLVED Thresholds` section with the named next measurement; absence of a UNRESOLVED section when any T is unresolved fails acceptance |

This plan modifies markdown only. The recommendation is a decision document; acting on it is a separate future phase.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Derive the recommendation by walking the Decision Tree (no default-to-B; DEFER is valid)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md</files>
  <read_first>
    - 72-DECISION-RUBRIC.md §"Decision Tree" (revised — walk it using current resolved/unresolved state)
    - 72-02-SUMMARY.md (authoritative UNRESOLVED list)
    - 72-POC-RESULTS.md (T4 resolved data)
    - 72-CONTEXT.md (T6, T7 values)
    - 72-STRATEGY-MATRIX.md (per-option per-criterion cells)
    - 72-INVENTORY.md (row citations)
  </read_first>
  <action>
**Step 1 — Compute net-resolved threshold count (N).**

Walk T1..T9 and classify each as RESOLVED or UNRESOLVED/UNKNOWN:

| T-ID | Source | RESOLVED if... |
|------|--------|----------------|
| T1 | 72-INVENTORY.md Totals | always RESOLVED (Plan 01 output) |
| T2 | gh CLI / WebFetch fallback | RESOLVED if executor runs the measurement now; UNRESOLVED if environment cannot authenticate and WebFetch fallback is not run |
| T3 | 72-POC-RESULTS.md Slice 2 data | UNRESOLVED (Slice 2 is proposal-only; not executed in Phase 72) |
| T4 | 72-POC-RESULTS.md `T4_compile_errors` | RESOLVED for subcount (a); UNRESOLVED for subcounts (b) and (c) — count the row as HALF-RESOLVED, treat as RESOLVED for decision-tree purposes because subcount (a) is the hard gate |
| T5 | 72-POC-PROPOSAL.md Time-box declarations | always RESOLVED |
| T6 | 72-CONTEXT.md `breaking_change_window:` | RESOLVED if value is `open` / `closed` / `closing_YYYY-MM-DD`; UNRESOLVED if `UNKNOWN` |
| T7 | 72-CONTEXT.md `production_user_tolerance:` | RESOLVED if value is a range (0, 1-2, 3-5, 6-20, 21+); UNRESOLVED if `UNKNOWN` |
| T8 | `git log --since=180.days.ago --oneline -- src/types/ src/shared/ \| wc -l` | RESOLVED at synthesis (executor runs the command) |
| T9 | 72-STRATEGY-MATRIX.md Enterprise-feature-preservation column for each option | RESOLVED at synthesis (executor reads the matrix) |

**Executor action:** run the commands below and record the actual outcomes in a scratch buffer for use in the recommendation body:

```bash
# T2 — governance responsiveness
if command -v gh > /dev/null 2>&1; then
  gh issue list -R modelcontextprotocol/rust-sdk --state closed --limit 50 --json closedAt,createdAt > /tmp/72-t2-issues.json 2>&1 && T2_ISSUES_RESOLVED=1 || T2_ISSUES_RESOLVED=0
  gh pr list --repo modelcontextprotocol/rust-sdk --state merged --limit 50 --json mergedAt,createdAt > /tmp/72-t2-prs.json 2>&1 && T2_PRS_RESOLVED=1 || T2_PRS_RESOLVED=0
else
  T2_ISSUES_RESOLVED=0; T2_PRS_RESOLVED=0
fi
# If 0, executor MAY still WebFetch https://api.github.com/repos/modelcontextprotocol/rust-sdk/issues?state=closed&per_page=100
# If still unresolvable in current environment, T2 counts as UNRESOLVED and the Next-Steps checklist
# names the WebFetch URL for a future executor.

# T8 — historical churn
T8_CHURN=$(git log --since=180.days.ago --oneline -- src/types/ src/shared/ 2>/dev/null | wc -l | tr -d ' ')
echo "T8 churn (last 180d): $T8_CHURN commits"

# T4 — read from 72-POC-RESULTS.md
T4_ERRORS=$(grep -E '^T4_compile_errors: ' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-RESULTS.md | awk '{print $2}')
echo "T4 compile errors (spike): $T4_ERRORS"

# T6, T7 — read from 72-CONTEXT.md
T6_WINDOW=$(grep -E '^breaking_change_window: ' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md | awk '{print $2}')
T7_TOLERANCE=$(grep -E '^production_user_tolerance: ' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md | awk '{print $2}')
```

**Step 2 — Count N.**

Treat each T-ID as: `RESOLVED` (concrete value) OR `UNRESOLVED` (UNKNOWN / not run / data missing). N = count of RESOLVED.

Minimum baseline (commands run cleanly):
  - T1 ✓ (always)
  - T5 ✓ (always)
  - T8 ✓ (if git log works)
  - T9 ✓ (always — matrix lookup)
  - T4 ✓ (spike output exists)
Additional if data present:
  - T6 ✓ if CONTEXT.md value ≠ UNKNOWN
  - T7 ✓ if CONTEXT.md value ≠ UNKNOWN
  - T2 ✓ if gh or WebFetch succeeded in Step 1

T3 is expected UNRESOLVED (Slice 2 is proposal-only).

Typical N range in the baseline: 5–7. If CONTEXT.md has UNKNOWN for both T6 and T7 AND T2 cannot be measured, N could drop to 4.

**Step 3 — Apply Decision Tree.**

- N < 3 → `**Recommendation:** DEFER`. Produce next-measurements checklist.
- N = 3..4 → `**Recommendation:** D`. List 2 measurements that would flip the recommendation.
- N ≥ 5 → Score each of {A, B, C1, C2, D} using the weights in 72-DECISION-RUBRIC.md §"Decision Tree" Step 4. Pick highest-scoring non-disqualified option.

Disqualifications (any one → option scored -∞):
- T4 compile errors > 200 OR broken examples > 10 OR broken downstream crates > 3 → disqualify A.
- T6 = `closed` → disqualify A, B, C1.
- T7 = 0 or "1-2 with no v3.0" → disqualify A, B, C1.
- T2 median > 14 days (issues) or > 7 days (PRs) → disqualify A, B, C1, C2.
- T9 any feature BROKEN for an option → disqualify that option.

**Step 4 — Write 72-RECOMMENDATION.md.**

Use this exact structure. Fill all `<...>` placeholders with actual values derived in Steps 1–3. The recommendation letter in line 1 comes from Step 3.

```markdown
# Phase 72 — Final Recommendation (RMCP-EVAL-05, reviews-mode revised)

**Recommendation:** <A | B | C1 | C2 | D | DEFER>
**Baseline pin:** rmcp 1.5.0 / pmcp current main.
**Synthesis date:** 2026-04-19.
**Option-set note (per 72-REVIEWS.md replan action #7):** valid recommendations are {A, B, C1, C2, D, DEFER}. E (Fork) is contingency-only per 72-STRATEGY-MATRIX.md footnote and is NOT a valid recommendation.

## Decision Tree Traversal

- **Net-resolved threshold count (N):** <fill>
  - RESOLVED: <list T-IDs>
  - UNRESOLVED: <list T-IDs with reason>
- **Tree branch taken:** <"N < 3 → DEFER" | "N = 3..4 → D" | "N ≥ 5 → scored">
- **If scored (N ≥ 5), option scores:**
  - A: <score> (disqualifications: <list or "none">)
  - B: <score> (disqualifications: <list or "none">)
  - C1: <score> (disqualifications: <list or "none">)
  - C2: <score> (disqualifications: <list or "none">)
  - D: <score>
- **Winner:** <letter>
- **Tiebreaker applied:** <"none" | "migration cost (D > C2 > C1 > B > A)">

## Summary

<One-paragraph statement naming the chosen option, the precedent it follows (tonic/prost for B; reqwest/hyper for A; serde for C1; sqlx for C2; none for D), the headline consequence (v3.0 required / stays on 2.x / etc.), and if DEFER was chosen, the specific measurements that would unblock a real recommendation. Must NOT introduce new evidence — only synthesize prior deliverables.>

## Per-Criterion Justification

Each subsection cites ≥1 threshold ID (T1..T9) from 72-DECISION-RUBRIC.md AND ≥1 row number from 72-INVENTORY.md OR matrix option from 72-STRATEGY-MATRIX.md. These citations are enforced by Task 1b's semantic audit.

### Criterion: Maintenance reduction

Justification: <2–4 sentences citing T1 (LOC delta) AND T8 (historical churn = <N> commits last 180d); cite specific 72-INVENTORY.md rows (e.g., row 1 jsonrpc, row 4 tools) AND the matrix cell for the chosen option>.

### Criterion: Migration cost

Justification: <2–4 sentences citing T5 (time-box) AND T4 (resolved via 72-POC-RESULTS.md: compile errors = <N>); cite 72-POC-PROPOSAL.md §"Workspace Ripple Caveat" AND the matrix "Migration cost" cell for the chosen option; reference at least one inventory row e.g., row 18 streamable-http>.

### Criterion: Breaking-change surface

Justification: <2–4 sentences citing T4 (Slice 1 compile errors from 72-POC-RESULTS.md) AND T6 (v2.x window state from 72-CONTEXT.md); cite the matrix "Breaking-change surface" cell for the chosen option AND state whether v3.0 is required>.

### Criterion: Enterprise feature preservation

Justification: <2–4 sentences citing T3 (coverage percent — currently UNRESOLVED) AND T9 (per-feature checklist); enumerate all 7 enterprise features (TypedTool, workflow, mcp_apps, auth, middleware, mcp-preview, cargo-pmcp) with PRESERVED/BROKEN status; cite 72-INVENTORY.md rows 14 (mcp_apps), 16 (websocket), 22 (middleware), 23 (simd_parsing)>.

### Criterion: Spec-upgrade agility

Justification: <2–4 sentences citing T2 (governance responsiveness — state resolved/unresolved with actual median values if measured) AND the matrix "Spec-upgrade agility" cell for the chosen option; if Option A or B chosen, note the pin + patch-fork strategy from 72-RESEARCH.md §"Rmcp Trajectory Risk">.

## UNRESOLVED Thresholds

<For each threshold Plan 02 tagged UNRESOLVED or Task 1 found UNRESOLVED, list the T-ID and the exact next measurement that would close it. Empty subsection is ONLY valid if all 9 T-IDs are RESOLVED.>

- **T3 — Enterprise extension-point coverage percent:** Execute PoC Slice 2 (see 72-POC-PROPOSAL.md) in a follow-on migration-scoping phase. Run `mcp-tester conformance run --target-example examples/z01_rmcp_foundation_poc.rs` and record coverage %.
- **T4 subcounts (b) and (c):** Execute `for ex in examples/*.rs; do cargo check --example "$(basename "$ex" .rs)" --features full; done` on a migration branch; count broken downstream workspace crates.
- <Add entries for T2, T6, T7 if UNRESOLVED>

## Next Steps

<Exactly one of the following, matching the Recommendation letter:>

- **If A or B:** "Open a v3.0 migration-scoping phase. Kickoff deliverable: a PoC execution report running PoC Slice 1 (already done in Phase 72 — see 72-POC-RESULTS.md) + Slice 2 + optionally Slice 3. Do NOT migrate production code until the PoC report closes T3 and T4 subcounts (b)+(c)." Cite 72-POC-PROPOSAL.md.
- **If C1:** "Open a types-only borrow phase limited to 72-INVENTORY.md rows 1–10. Scope ≤4,500 LOC touched; transports stay first-party."
- **If C2:** "Open a transports-only borrow phase limited to 72-INVENTORY.md row 15 (stdio) and optionally row 18 client wrapper. Scope ≤1,500 LOC touched."
- **If D:** "Open an upstream-contribution phase. Identify top 3 rmcp ergonomic gaps pmcp can upstream (seed from 72-INVENTORY.md row 9 ElicitationSchemaBuilder and from 69-PROPOSALS.md). Do NOT migrate; keep pmcp's protocol layer first-party."
- **If DEFER:** "Phase 72.1: close the named UNRESOLVED thresholds before revisiting adoption. Required measurements: <list from the UNRESOLVED section>. Time-box the Phase 72.1 to match the cost of the measurements (typically ≤1 week)."

## Provenance

- 72-CONTEXT.md (T6, T7 locks — Plan 01 Task 0)
- 72-INVENTORY.md (RMCP-EVAL-01 — Plan 01 Task 2)
- 72-STRATEGY-MATRIX.md (RMCP-EVAL-02 — Plan 01 Task 3)
- 72-POC-PROPOSAL.md (RMCP-EVAL-03 — Plan 02 Task 1)
- 72-POC-RESULTS.md (T4 data — Plan 02 Task 1b, NEW in reviews-mode)
- 72-DECISION-RUBRIC.md (RMCP-EVAL-04 — Plan 02 Task 2)
- 72-RESEARCH.md (phase brief)
- 72-REVIEWS.md (reviews-mode driver)

## Closes

- RMCP-EVAL-05 (this document IS the deliverable).
```

Fill every `<...>` placeholder with actual values. DO NOT leave any placeholder in the final file. If the recommendation is DEFER, the criterion subsections still MUST be filled (they justify the DEFER decision by enumerating unresolved gates) — they are not optional.

After writing:
```bash
REC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
head -5 "$REC" | grep -E '^\*\*Recommendation:\*\* (A|B|C1|C2|D|DEFER)'
grep -c '^### Criterion' "$REC"    # expect 5
! grep -qE '<[a-z]' "$REC" || echo "WARN: unfilled placeholder remains"
```
  </action>
  <verify>
    <automated>REC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md; head -5 $REC | grep -qE '^\*\*Recommendation:\*\* (A|B|C1|C2|D|DEFER)' && test $(grep -c '^### Criterion' $REC) -ge 5 && grep -qE 'T[1-9]' $REC && grep -q '72-INVENTORY.md' $REC && grep -q '72-DECISION-RUBRIC.md' $REC && grep -q '72-POC-RESULTS.md' $REC && grep -q '72-STRATEGY-MATRIX.md' $REC && ! grep -qE '^\*\*Recommendation:\*\* E' $REC && ! grep -qE '<[a-z][a-z]+>' $REC</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-.../72-RECOMMENDATION.md`
    - `wc -l` ≥ 100
    - `head -5 72-RECOMMENDATION.md | grep -E '^\*\*Recommendation:\*\* (A|B|C1|C2|D|DEFER)'` matches exactly one line
    - `grep -q '^\*\*Recommendation:\*\* E'` returns NO match (E is not a valid recommendation)
    - `grep -c '^### Criterion' 72-RECOMMENDATION.md` returns exactly 5
    - `! grep -qE '<[a-z][a-z]+>' 72-RECOMMENDATION.md` (no unfilled placeholders)
    - `grep -q '^## Decision Tree Traversal'` succeeds (new reviews-mode section)
    - `grep -q '^## UNRESOLVED Thresholds'` succeeds
    - `grep -q '^## Next Steps'` succeeds
    - `grep -q 'RMCP-EVAL-05'` succeeds
    - `grep -c '72-POC-RESULTS.md\|72-CONTEXT.md\|72-INVENTORY.md\|72-STRATEGY-MATRIX.md\|72-DECISION-RUBRIC.md\|72-POC-PROPOSAL.md' 72-RECOMMENDATION.md` ≥ 5 (all prior deliverables cited)
    - `! grep -iE '\bTBD\b|\bFIXME\b' 72-RECOMMENDATION.md`
    - The chosen option letter in the header appears in the Next Steps branch taken (consistency)
  </acceptance_criteria>
  <done>72-RECOMMENDATION.md exists with header, decision-tree traversal log, 5 Criterion subsections, UNRESOLVED section, and Next Steps matching the chosen letter.</done>
</task>

<task type="auto">
  <name>Task 1b: Run the semantic audit — every Criterion subsection cites ≥1 T-ID AND ≥1 inventory/matrix reference; failure auto-downgrades to DEFER</name>
  <files>
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
  </files>
  <read_first>
    - 72-RECOMMENDATION.md (Task 1 output)
    - 72-REVIEWS.md §"MEDIUM consensus finding — Shallow automated validation" (justification for this task)
  </read_first>
  <action>
**Purpose (per 72-REVIEWS.md MEDIUM finding):** grep-based row counts verify document shape, not semantic consistency. This task performs cross-doc consistency checks beyond grep. Every `### Criterion <name>` subsection in 72-RECOMMENDATION.md MUST cite at least one T-ID (T1..T9) AND at least one row-number reference to 72-INVENTORY.md OR a matrix-option reference to 72-STRATEGY-MATRIX.md. Failure of the audit → the recommendation is AUTO-DOWNGRADED to DEFER.

**Step 1 — Run the semantic audit with awk.**

Run this exact audit script. It walks each `### Criterion ...` block and checks for T-ID + inventory/matrix references within that block.

```bash
REC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md

awk '
  /^### Criterion/ {
    if (current_block != "") {
      if (has_tid == 0 || has_ref == 0) {
        print "FAIL: " current_block " — T-ID=" has_tid ", inventory/matrix ref=" has_ref
        fail_count++
      } else {
        print "PASS: " current_block
      }
    }
    current_block = $0
    has_tid = 0
    has_ref = 0
    next
  }
  /^## / && current_block != "" {
    if (has_tid == 0 || has_ref == 0) {
      print "FAIL: " current_block " — T-ID=" has_tid ", inventory/matrix ref=" has_ref
      fail_count++
    } else {
      print "PASS: " current_block
    }
    current_block = ""
    has_tid = 0
    has_ref = 0
  }
  current_block != "" {
    if (match($0, /T[1-9]/)) has_tid = 1
    if (match($0, /(row|Row) [0-9]+/) || match($0, /(A\.|B\.|C1\.|C2\.|D\.)/) || match($0, /(Full adopt|Hybrid|Selective|Status quo)/)) has_ref = 1
  }
  END {
    if (current_block != "") {
      if (has_tid == 0 || has_ref == 0) {
        print "FAIL: " current_block " — T-ID=" has_tid ", inventory/matrix ref=" has_ref
        fail_count++
      } else {
        print "PASS: " current_block
      }
    }
    if (fail_count > 0) {
      print "AUDIT FAIL: " fail_count " subsection(s) missing required citations"
      exit 1
    } else {
      print "AUDIT PASS: all subsections cite T-IDs and inventory/matrix references"
      exit 0
    }
  }
' "$REC"
```

**Step 2 — Handle audit result.**

- **If audit exits 0 (PASS):** proceed to Task 2 (commit).
- **If audit exits 1 (FAIL):** the recommendation is auto-downgraded to DEFER.
  1. Use the Edit tool (NOT Write, NOT heredoc) to change the `**Recommendation:**` line from whatever letter is there to `DEFER`.
  2. Prepend a new section between the header and `## Decision Tree Traversal`:

```markdown
## Semantic Audit Failure (auto-downgrade to DEFER per 72-REVIEWS.md MEDIUM finding)

The semantic audit (Plan 03 Task 1b) found that the following criterion subsection(s) did NOT cite at least one T-ID from T1..T9 AND at least one row-number from 72-INVENTORY.md or option from 72-STRATEGY-MATRIX.md:

<paste the FAIL lines from Step 1>

Per 72-REVIEWS.md MEDIUM consensus finding (shallow automated validation), a recommendation that cannot semantically engage its rubric is not a decision — it is a formatted opinion. The recommendation is therefore auto-downgraded to DEFER. Next Steps: reopen this plan, fix the missing citations in the failing subsection(s), re-run the audit, and re-synthesize the recommendation.
```

  3. Update the `## Next Steps` section to the DEFER branch.

**Step 3 — Re-verify after any auto-downgrade.**

```bash
# After auto-downgrade (if applied), re-run the audit. The DEFER version must itself pass.
# The failing subsection still needs citations — the DEFER note acknowledges the failure
# but does not skip the citation requirement for the DEFER rationale itself.
awk '...' "$REC"  # same script as Step 1; must now exit 0
```

If the auto-downgrade leaves any subsection still failing, Task 2 (commit) MUST NOT proceed. Surface the failure as a blocking error and abort the plan so the executor fixes the citations by hand.
  </action>
  <verify>
    <automated>REC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md; awk '/^### Criterion/ { if (cb != "") { if (t == 0 || r == 0) { f++ } } cb = $0; t = 0; r = 0; next } /^## / && cb != "" { if (t == 0 || r == 0) { f++ } cb = ""; t = 0; r = 0 } cb != "" { if (match($0, /T[1-9]/)) t = 1; if (match($0, /(row|Row) [0-9]+/) || match($0, /(A\.|B\.|C1\.|C2\.|D\.)/) || match($0, /(Full adopt|Hybrid|Selective|Status quo)/)) r = 1 } END { if (cb != "") { if (t == 0 || r == 0) { f++ } } exit (f > 0 ? 1 : 0) }' $REC</automated>
  </verify>
  <acceptance_criteria>
    - Semantic audit awk script exits 0 on the final file
    - Every `### Criterion` subsection contains at least one `T[1-9]` reference
    - Every `### Criterion` subsection contains at least one of: `(row|Row) [0-9]+` OR `A.` / `B.` / `C1.` / `C2.` / `D.` OR "Full adopt" / "Hybrid" / "Selective" / "Status quo"
    - If audit failed and DEFER downgrade applied: the `**Recommendation:**` line is `DEFER` AND a `## Semantic Audit Failure` section is present
    - After any downgrade, the file is re-audited and passes
  </acceptance_criteria>
  <done>Semantic audit passes. 72-RECOMMENDATION.md is either (a) the original recommendation with full citation quality, or (b) an auto-downgraded-to-DEFER recommendation with the audit-failure section prepended. Task 2 (commit) can proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Commit Plan 03 deliverable and close Phase 72 (reviews-mode)</name>
  <files>
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
  </files>
  <read_first>
    - CLAUDE.md §"Quality Gate Enforcement" (markdown-only commit)
  </read_first>
  <action>
**Pre-commit check:** confirm the semantic audit passed.

```bash
REC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
awk '...' "$REC" || { echo "ABORT: semantic audit still failing; do not commit"; exit 1; }
```

Commit:

```bash
gsd-sdk query commit "docs(72): final recommendation on rmcp foundation adoption — closes RMCP-EVAL-05 [reviews-mode]" \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
```

Fallback:

```bash
git add .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
git commit -m "docs(72): final recommendation on rmcp foundation adoption — closes RMCP-EVAL-05 [reviews-mode]"
```

Do NOT run `make quality-gate` (markdown-only). Do NOT use `--no-verify`.

Do NOT in this task:
- Mark any RMCP-EVAL-## row as `[x]` complete in REQUIREMENTS.md (that is `/gsd-verify-work`'s job).
- Update ROADMAP.md counters (automated elsewhere).
- Edit STATE.md.
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -q '72-RECOMMENDATION.md' && git log -1 --pretty=%s | grep -qE 'docs\(72\):.*recommendation' && git log -1 --pretty=%s | grep -q 'RMCP-EVAL-05' && git log -1 --pretty=%s | grep -q 'reviews-mode'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` contains `docs(72):` AND `recommendation` AND `RMCP-EVAL-05` AND `reviews-mode`
    - `git log -1 --name-only` lists `72-RECOMMENDATION.md`
    - `git status --porcelain` is empty for the recommendation file
    - Exactly 1 file changed in the commit (`git log -1 --stat` shows only 72-RECOMMENDATION.md)
  </acceptance_criteria>
  <done>Phase 72 final deliverable committed; recommendation closes RMCP-EVAL-05; phase ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<verification>
Plan 03 (reviews-mode revised) is complete (AND Phase 72 is ready for verification) when:
1. 72-RECOMMENDATION.md opens with `**Recommendation:** <A|B|C1|C2|D|DEFER>` in the first 5 lines (E is NOT valid).
2. It contains exactly 5 `### Criterion` subsections.
3. Semantic audit (Task 1b) passes: every criterion cites ≥1 T-ID AND ≥1 inventory/matrix reference.
4. UNRESOLVED Thresholds section enumerates every UNRESOLVED T-ID with a named next-measurement.
5. Decision-tree traversal log shows the N value and the tree branch taken.
6. Next Steps section matches the chosen letter.
7. Plan 03's commit is on `HEAD` with `reviews-mode` in the subject.
8. All six Phase 72 deliverables exist:
   - 72-CONTEXT.md
   - 72-INVENTORY.md
   - 72-STRATEGY-MATRIX.md
   - 72-POC-PROPOSAL.md
   - 72-POC-RESULTS.md
   - 72-DECISION-RUBRIC.md
   - 72-RECOMMENDATION.md

Cross-document consistency check (for plan-checker):
- Every strategy-matrix criterion (5 columns) appears as a `### Criterion` subsection.
- Every T-ID referenced in the recommendation exists in 72-DECISION-RUBRIC.md.
- Every slice cited in the recommendation exists in 72-POC-PROPOSAL.md OR 72-POC-RESULTS.md.
- T4 citation in the recommendation references the actual numeric value from 72-POC-RESULTS.md.
- The recommendation letter is NEVER `E`.

Per 72-VALIDATION.md Falsifiability Checklist: reviewer fills the phase-gate checklist; this plan's SUMMARY must report PASS/FAIL on each item (updated to include the semantic audit as a new check).
</verification>

<success_criteria>
- 72-RECOMMENDATION.md exists with header, 5 Criterion subsections each passing the semantic audit, decision-tree traversal log, UNRESOLVED section, and Next Steps matching the chosen letter.
- Recommendation letter ∈ {A, B, C1, C2, D, DEFER} — NEVER E.
- One git commit on HEAD with message containing `docs(72):` AND `recommendation` AND `RMCP-EVAL-05` AND `reviews-mode`.
- Phase 72 now has 7 deliverables present (CONTEXT + INVENTORY + STRATEGY-MATRIX + POC-PROPOSAL + POC-RESULTS + DECISION-RUBRIC + RECOMMENDATION).
</success_criteria>

<output>
After completion, create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-03-SUMMARY.md` documenting:
- Reviews-mode revision summary (which findings addressed: HIGH-1 default-to-B removed, HIGH-2 T4 resolved via 72-POC-RESULTS.md, replan #6 semantic audit added)
- Chosen recommendation letter (A/B/C1/C2/D/DEFER) and one-sentence rationale
- Net-resolved threshold count (N) and which T-IDs were UNRESOLVED
- Decision-tree traversal log: which branch was taken (N<3 / 3..4 / ≥5)
- If scored (N≥5): per-option scores and the winning option's score breakdown
- Whether 72-CONTEXT.md locked T6 and T7 to concrete values or UNKNOWN
- Semantic audit result: PASS or FAIL+DOWNGRADE
- Falsifiability Checklist status (each phase-gate item PASS/FAIL, updated to include semantic audit)
- Next-Steps handoff: whether the successor is v3.0 migration-scoping, C1 types-borrow, C2 transports-borrow, upstream-contribution, or Phase 72.1 DEFER follow-up
</output>
