---
phase: 72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
autonomous: true
requirements:
  - RMCP-EVAL-01
  - RMCP-EVAL-02
tags:
  - research
  - decision
  - rmcp
  - foundations
  - inventory
  - strategy-matrix
  - context

must_haves:
  truths:
    - "72-CONTEXT.md exists and records explicit (or explicitly UNKNOWN) values for T6 (breaking-change window) and T7 (production-user tolerance)"
    - "REQUIREMENTS.md contains rows for RMCP-EVAL-01 through RMCP-EVAL-05"
    - "72-INVENTORY.md lists at least 15 pmcp module families with a 9-column evidence schema (family, pmcp defining file:line, rmcp evidence, exact symbols, public API surface, owned impls/macros, serde compat risk, feature flag, downstream crates)"
    - "72-STRATEGY-MATRIX.md scores exactly 5 option rows (A, B, C1, C2, D) × 5 criteria = 25 cells with no TBD placeholders; E is present only as a footnote (contingency escalation only)"
    - "Every row in INVENTORY maps cleanly onto one of {A, B, C1, C2, D} (no orphan modules)"
  artifacts:
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md"
      provides: "User/project locks for T6 (breaking-change window) and T7 (production-user tolerance); resolves these from STATE.md + MEMORY.md + PROJECT.md where possible, otherwise marks UNKNOWN explicitly"
      min_lines: 20
    - path: ".planning/REQUIREMENTS.md"
      provides: "RMCP-EVAL-01..05 requirement rows + traceability rows"
      contains: "RMCP-EVAL-01"
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md"
      provides: "Inversion inventory with 9-column row schema: pmcp→rmcp module mapping with exact symbols, public-API surface impact, owned impls/macros, serde-compat risk, feature flags, downstream crates touched"
      min_lines: 60
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md"
      provides: "5×5 scored strategy matrix (A / B / C1 / C2 / D) with per-cell rationale; E demoted to footnote"
      min_lines: 80
  key_links:
    - from: "72-CONTEXT.md"
      to: "72-DECISION-RUBRIC.md T6, T7"
      via: "explicit YAML-like fields breaking_change_window and production_user_tolerance"
      pattern: "breaking_change_window|production_user_tolerance"
    - from: "72-INVENTORY.md"
      to: "72-RESEARCH.md §'Inversion Inventory'"
      via: "row-by-row correspondence with overlap ratings preserved; evidence UPGRADED to 9-column schema"
      pattern: "EXACT|UNVERIFIED|NO RMCP EQUIVALENT|Partial"
    - from: "72-STRATEGY-MATRIX.md"
      to: "72-INVENTORY.md totals row"
      via: "LOC-savings columns cite inventory per-row impact"
      pattern: "LOC"
    - from: ".planning/REQUIREMENTS.md"
      to: "72-RESEARCH.md §'Proposed REQ-IDs'"
      via: "one row per RMCP-EVAL-## with description copied from research"
      pattern: "RMCP-EVAL-0[1-5]"
---

<objective>
Seed Phase 72's anchor deliverables — the CONTEXT locks, the inversion inventory, and the strategy matrix — and land the five RMCP-EVAL REQ-IDs in `.planning/REQUIREMENTS.md`.

**Revision scope (reviews mode):** This plan has been revised in response to 72-REVIEWS.md:
- **NEW Task 0** creates `72-CONTEXT.md` to lock T6 (breaking-change window) and T7 (production-user tolerance) from known project state — addresses Gemini replan action #3.
- **Task 2 (Inventory)** row schema is upgraded from 5 columns to **9 columns** to meet the Codex HIGH-3 evidence-standard bar: exact symbols, public API surface, owned impls/macros, serde compat risk, feature flags, downstream crates touched.
- **Task 3 (Strategy Matrix)** option set is changed from `{A, B, C, D, E}` to `{A, B, C1, C2, D}` with E demoted to a footnote — addresses Codex replan action #7.

Purpose: give downstream plans (02, 03) the canonical module-overlap evidence, the context-locked unknowns, and the scored option framework they need to build a PoC proposal, a decision rubric, a spike execution, and a final recommendation. These documents are the backbone of the decision; every later deliverable cites them.

Output:
  - `72-CONTEXT.md` — explicit locks or explicit UNKNOWN markers for T6 and T7.
  - Five RMCP-EVAL-01..05 rows in REQUIREMENTS.md with Traceability entries.
  - `72-INVENTORY.md` — inversion inventory covering ≥15 pmcp module families, each row carrying a 9-column evidence schema.
  - `72-STRATEGY-MATRIX.md` — scored 5-options × 5-criteria matrix (A, B, C1, C2, D) with rationale per cell, no TBD placeholders; E documented as footnote only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-REVIEWS.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md

<interfaces>
<!-- RMCP-EVAL REQ-IDs (authoritative text from 72-RESEARCH.md §"Proposed REQ-IDs") -->
- RMCP-EVAL-01: Produce a source-citation-backed inversion inventory covering every module in src/types/ and src/shared/ (and src/server/cancellation.rs), with nearest rmcp 1.5.0 equivalent and overlap classification (EXACT / Partial / pmcp-superset / NO RMCP EQUIVALENT / UNVERIFIED).
- RMCP-EVAL-02: Score each of five architectural options (A. Full adopt / B. Hybrid wrapper / C1. Selective borrow — types only / C2. Selective borrow — transports only / D. Status quo + upstream PRs) against five criteria (maintenance reduction, migration cost, breaking-change surface, enterprise feature preservation, upgrade agility). E (Fork) demoted to contingency footnote.
- RMCP-EVAL-03: Propose 2-3 candidate PoC slices ≤500 LOC each with falsifiable pass/fail criteria; Slice 1 is EXECUTED in Plan 02 as a time-boxed throwaway spike.
- RMCP-EVAL-04: Decision rubric with ≥5 falsifiable thresholds each citing a named data source; updated post-reviews to add historical-churn, PR merge latency, enterprise-feature preservation, and expanded break-count thresholds (T8, T9 added).
- RMCP-EVAL-05: Final recommendation (A|B|C1|C2|D|DEFER) with per-criterion justification citing RMCP-EVAL-04 thresholds and RMCP-EVAL-01 rows. DEFER is an explicit, valid outcome; E is NOT a valid recommendation (contingency only).

<!-- Row structure mandated by 72-VALIDATION.md row 1, TIGHTENED per reviews HIGH-3 -->
INVENTORY row grep gate: `grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' 72-INVENTORY.md` >= 15
(every row must contain a pmcp file:line cell shaped `| path/to/file.rs:NNN |` pointing at a DEFINING item — NOT `:1`).

<!-- 9-column schema — per Codex HIGH-3 -->
INVENTORY column order (left→right):
  1. pmcp module family
  2. pmcp evidence (file:line of a DEFINING item: struct/enum/trait/impl/macro — not `:1`)
  3. rmcp evidence (docs.rs anchor OR GitHub blob URL with line anchor; if absent: "no equivalent — <reason>")
  4. Exact symbols touched (type/trait/macro names)
  5. Public API surface impacted (crate-root re-export? pmcp::prelude? pub vs pub(crate)?)
  6. Owned impls/macros affected (serde derives, custom impls, proc-macro expansions)
  7. Serde compatibility risk (EXACT | compatible-via-adapter | INCOMPATIBLE | N/A)
  8. Feature flag(s) gating the code
  9. Downstream crates touched (cargo-pmcp, mcp-tester, mcp-preview, pmcp-macros, pmcp-widget-utils, pmcp-macros-support)
Plus: overlap rating (kept), gap-from-rmcp (kept), migration-LOC impact (kept) in trailing columns — so the final table is 9 mandated columns + 3 retained columns = 12 columns.

<!-- Strategy matrix — REVISED option set per reviews replan action #7 -->
STRATEGY-MATRIX option rows (exactly 5): `grep -E '^\| (A\. Full adopt|B\. Hybrid|C1\. Selective — types only|C2\. Selective — transports only|D\. Status quo)' 72-STRATEGY-MATRIX.md` must match exactly 5 lines; `! grep -i 'TBD' 72-STRATEGY-MATRIX.md`.
E (Fork) appears once as a footnote block (H2 or H3 heading "## Contingency (not scored): E. Fork") but does NOT appear as a table row.

<!-- Five strategy matrix criteria columns (unchanged from 72-RESEARCH.md §"Strategy Matrix") -->
Columns: Maintenance reduction | Migration cost | Breaking-change surface | Enterprise feature preservation | Spec-upgrade agility

<!-- CONTEXT.md shape (new — per reviews replan action #3) -->
72-CONTEXT.md MUST contain at minimum these two explicit fields (YAML-frontmatter or inline markdown, either is fine):
  - breaking_change_window: open | closed | closing_YYYY-MM-DD | UNKNOWN
  - production_user_tolerance: "0" | "1-2" | "3-5" | "6-20" | "21+" | UNKNOWN
If UNKNOWN, the value line MUST be followed by a "Resolution path:" line naming what input would close it (e.g., "ask user", "survey pmcp-run + Lambda users").
</interfaces>

<read_first>
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md (AUTHORITATIVE — copy tables and verbatim text from §"Inversion Inventory", §"Strategy Matrix", §"Architectural Responsibility Map", §"Proposed REQ-IDs")
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md (mandates per-deliverable grep gates — tightened post-reviews)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-REVIEWS.md (reviews mode driver — all revisions in this plan trace back to specific findings)
- .planning/REQUIREMENTS.md (add rows here; match existing section style used by PARITY-* rows)
- .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md (rmcp 1.5.0 baseline pin rationale)
- .planning/STATE.md (source for 72-CONTEXT.md T6/T7 locks where available)
- `.planning/projects/PROJECT.md` if present (source for T6/T7)
- CLAUDE.md (`gsd-sdk query commit` final-task convention)
</read_first>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (markdown only) | Phase 72 produces documentation artifacts only; no runtime code paths, no data flows, no user input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Information Disclosure | `.planning/REQUIREMENTS.md` and deliverable markdown | accept | Public planning docs contain no secrets, no PII, no credentials; reviewed by planner-audit per 72-VALIDATION.md |
| T-72-02 | Tampering (supply-chain risk of future rmcp dependency) | strategy-matrix scoring | mitigate | Strategy matrix explicitly scores "Breaking-change surface"; E (Fork) is documented as a contingency footnote — this plan does NOT add rmcp as a dependency |
| T-72-07 | Repudiation (CONTEXT.md UNKNOWN values later re-litigated without paper trail) | 72-CONTEXT.md | mitigate | Every UNKNOWN value MUST be followed by a "Resolution path:" line naming what input would close it; plan-checker verifies |

Rationale (applies to all three Phase 72 plans): This phase modifies markdown only. Plan 02 additionally runs a throwaway compile spike on a scratch branch that is deleted before any commit — see Plan 02 threat_model.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Create 72-CONTEXT.md to lock T6 and T7 (or mark UNKNOWN explicitly)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md</files>
  <read_first>
    - .planning/STATE.md (search for "breaking change", "v2.x", "v3.0", "SemVer window")
    - .planning/projects/PROJECT.md if it exists; otherwise search `git ls-files .planning | head -50` for a project charter
    - `.claude/projects/-Users-guy-Development-mcp-sdk-rust-mcp-sdk/memory/MEMORY.md` (system-reminder already surfaced — reference `v2.0 cleanup philosophy` feedback entry: "During breaking-change window, consolidate aggressively")
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-REVIEWS.md §"Recommended Replan Actions" #3
  </read_first>
  <action>
Create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md` with EXACTLY this structure. The point is to make unknowns EXPLICIT, not to pretend the rubric can auto-default. If a field is genuinely unknowable from repo state, mark it `UNKNOWN` and provide a "Resolution path" — this is strictly better than leaving T6/T7 permanently UNRESOLVED in Plan 03.

```markdown
# Phase 72 — Context Locks (T6, T7)

**Purpose:** lock the two rubric thresholds that cannot be derived from code analysis or documentation: T6 (pmcp v2.x breaking-change window) and T7 (production-user tolerance for v3.0). This file is the source of truth for those two inputs into `72-DECISION-RUBRIC.md`.

**Why this file exists (per 72-REVIEWS.md replan action #3):** without explicit locks here, T6 and T7 become permanently UNRESOLVED in the decision rubric, which forces the recommendation toward DEFER for reasons unrelated to the evidence the phase actually gathered.

**Rule:** every field below is either a concrete value or `UNKNOWN`. If `UNKNOWN`, a "Resolution path" MUST follow. Leaving fields blank is forbidden.

---

## T6 — pmcp v2.x breaking-change window

breaking_change_window: <open | closed | closing_YYYY-MM-DD | UNKNOWN>

**Evidence cited:**
- <quote from STATE.md / PROJECT.md / MEMORY.md, e.g., MEMORY.md "v2.0 cleanup philosophy" feedback: "During breaking-change window, consolidate aggressively — don't defer as 'not worth the churn'" — this indicates the window was open at least during the v2.0 cleanup work>
- <current pmcp version from root Cargo.toml (executor MUST run `grep '^version' Cargo.toml | head -1` and paste the result)>
- <any SemVer policy text from PROJECT.md or CLAUDE.md>

**Resolution path (if UNKNOWN):** ask user directly — "Is the pmcp v2.x breaking-change window still open? If closed, provide the close date." This is a binary question and does not require code analysis.

**Implication for Plan 03 recommendation:**
- If `open`: Option A, B, C1, C2 all remain eligible (subject to other thresholds).
- If `closed`: Options A and B are disqualified; Plan 03 must pick among C1, C2, D, or DEFER.
- If `UNKNOWN`: Plan 03 lists T6 as UNRESOLVED and counts it against the decision-tree resolved-threshold total.

---

## T7 — Production-user tolerance for v3.0

production_user_tolerance: <0 | 1-2 | 3-5 | 6-20 | 21+ | UNKNOWN>

**Evidence cited:**
- <list known production users from MEMORY.md / STATE.md / feedback files (e.g., "pmcp-run is a confirmed major consumer"; "Lambda DNS rebinding feedback implies ≥1 Lambda deployment in production")>
- <count the evidence-supported minimum>

**Resolution path (if UNKNOWN):** either (a) ask user for a count and a go/no-go on v3.0 breaking changes, or (b) run a lightweight pmcp-user survey (post in pmcp discussion forum / crates.io dependents listing).

**Tolerance interpretation:**
- If production_user_tolerance = 0 OR 1-2 with explicit "no v3.0": Option A/B disqualified → prefer D.
- If 3-5 with ≥2 tolerating: Option A or B eligible.
- If 6-20 or 21+: Plan 03 MUST add a user-communication subtask to Next Steps regardless of option.
- If UNKNOWN: Plan 03 lists T7 as UNRESOLVED.

---

## Other Context (informational, not rubric-gating)

- **Baseline pin:** rmcp 1.5.0 (inherited from Phase 69 D-02) / pmcp current main (executor confirms version).
- **Phase 72 is research-only.** No rmcp dependency is added to pmcp in this phase. Plan 02 executes a throwaway spike on a scratch branch that is deleted before any commit.
- **Contingency option E (Fork rmcp):** NOT a primary scored strategy per 72-REVIEWS.md replan action #7. Documented in 72-STRATEGY-MATRIX.md as a footnote only.

---

## Closes

T6 lock (or explicit UNKNOWN + resolution path) and T7 lock (or explicit UNKNOWN + resolution path) for use by 72-DECISION-RUBRIC.md.
```

Fill the `<...>` placeholders with actual values gathered from the read_first sources. If no evidence supports a concrete value, write `UNKNOWN` and fill the resolution-path line. DO NOT leave any `<...>` placeholder in the final file.
  </action>
  <verify>
    <automated>CTX=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md; test -f $CTX && grep -qE '^breaking_change_window: (open|closed|closing_[0-9]{4}-[0-9]{2}-[0-9]{2}|UNKNOWN)' $CTX && grep -qE '^production_user_tolerance: (0|1-2|3-5|6-20|21\+|UNKNOWN)' $CTX && ! grep -qE '<[a-z_]+>' $CTX && (! grep -q 'UNKNOWN' $CTX || grep -q 'Resolution path' $CTX)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md`
    - `wc -l` ≥ 20
    - Contains exactly one `breaking_change_window:` line matching `(open|closed|closing_YYYY-MM-DD|UNKNOWN)`
    - Contains exactly one `production_user_tolerance:` line matching `(0|1-2|3-5|6-20|21+|UNKNOWN)`
    - If ANY value is `UNKNOWN`, a `Resolution path:` line appears within the same subsection
    - No unfilled `<...>` placeholders remain
    - `! grep -iE '\bTBD\b|\bFIXME\b' 72-CONTEXT.md`
  </acceptance_criteria>
  <done>72-CONTEXT.md exists with explicit T6 + T7 values (or UNKNOWN + resolution paths). Plan 03 has a deterministic source to consult.</done>
</task>

<task type="auto">
  <name>Task 1: Land RMCP-EVAL-01..05 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — match section style of PARITY-HANDLER-01 block)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 42-55 (§"Phase Requirements" — authoritative REQ-ID descriptions, copy verbatim with light wrap)
  </read_first>
  <action>
Append a new H3 section `### rmcp Foundation Evaluation (Phase 72 research)` to the v2.1 Requirements area, immediately below the existing `### rmcp Parity (Phase 69 research — seeds follow-on phases)` block (i.e., insert before `## Previous Requirements`).

The section MUST contain exactly these five checkbox rows, in order:

- [ ] **RMCP-EVAL-01**: Produce a source-citation-backed inversion inventory covering every module family in `src/types/` and `src/shared/` (and `src/server/cancellation.rs`), identifying the nearest rmcp 1.5.0 equivalent and an overlap rating (EXACT / Partial / pmcp-superset / pmcp-exclusive / UNVERIFIED). Each row MUST carry a 9-column evidence schema: (1) pmcp module family, (2) pmcp defining `file:line`, (3) rmcp docs.rs anchor or GitHub blob URL, (4) exact symbols touched, (5) public API surface impacted, (6) owned impls/macros affected, (7) serde compatibility risk, (8) feature flag(s), (9) downstream crates touched.
- [ ] **RMCP-EVAL-02**: Score the five architectural options (A. Full adopt / B. Hybrid wrapper / C1. Selective borrow — types only / C2. Selective borrow — transports only / D. Status quo + upstream PRs) against five criteria (maintenance reduction, migration cost, breaking-change surface, enterprise feature preservation, upgrade agility). All 25 cells scored with rationale; no `TBD`. E (Fork) documented as a contingency footnote only, not a scored row.
- [ ] **RMCP-EVAL-03**: Propose 2-3 candidate PoC slices, each `≤500` LOC touched, each with explicit files list, hypothesis tested, pass criterion, and disqualifying outcome. One slice must be executable in `≤3` days. Plan 02 additionally EXECUTES Slice 1 as a throwaway time-boxed spike to resolve T3/T4 with real data.
- [ ] **RMCP-EVAL-04**: Publish a decision rubric with `≥5` falsifiable thresholds (numeric or boolean), each citing a named data source (git log query, gh CLI query, mcp-tester run, PoC branch output, or CONTEXT.md entry). Post-reviews rubric adds T8 (historical churn on `src/types/` + `src/shared/`) and T9 (enterprise-feature preservation checklist) and updates T2 (PR merge latency) and T4 (broken-APIs + broken-examples + broken-downstream-crates subcounts).
- [ ] **RMCP-EVAL-05**: Publish a final recommendation picking exactly one of {A, B, C1, C2, D, DEFER}, with a per-criterion subsection that engages every rubric criterion from RMCP-EVAL-04 and cites the inventory row(s) and matrix cell(s) supporting its conclusion. DEFER is an explicit, valid outcome when net-resolved thresholds < 3; E (Fork) is NOT a valid recommendation.

Also extend the `## Traceability` table (after the existing `| PARITY-MACRO-01 | Phase 71 | Complete |` row) with five new rows:

| RMCP-EVAL-01 | Phase 72 | Pending |
| RMCP-EVAL-02 | Phase 72 | Pending |
| RMCP-EVAL-03 | Phase 72 | Pending |
| RMCP-EVAL-04 | Phase 72 | Pending |
| RMCP-EVAL-05 | Phase 72 | Pending |

Update the Coverage block footer: bump "v2.1 requirements: 23 total" to "28 total (20 pre-seed + 3 seeded by Phase 69 + 5 seeded by Phase 72)" and "Mapped to phases: 23" to "28". Add a `*Last updated: 2026-04-19 — added 5 RMCP-EVAL-* IDs seeded by Phase 72 rmcp foundation evaluation research (reviews-mode revised)*` line at the bottom of the trailing footer block.

Do NOT rename, renumber, or reorder any existing REQ-IDs. Do NOT touch any `[x]` checkbox state.
  </action>
  <verify>
    <automated>grep -cE '^- \[ \] \*\*RMCP-EVAL-0[1-5]\*\*' .planning/REQUIREMENTS.md | grep -qx 5 && grep -cE '^\| RMCP-EVAL-0[1-5] \| Phase 72 \| Pending \|' .planning/REQUIREMENTS.md | grep -qx 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*RMCP-EVAL-' .planning/REQUIREMENTS.md` returns 5
    - `grep -c '| RMCP-EVAL-0[1-5] | Phase 72 | Pending |' .planning/REQUIREMENTS.md` returns 5
    - `grep -c '### rmcp Foundation Evaluation (Phase 72 research)' .planning/REQUIREMENTS.md` returns 1
    - `! grep -E 'RMCP-EVAL-0[6-9]' .planning/REQUIREMENTS.md`
    - The RMCP-EVAL-02 description references `C1` AND `C2` (revised option set)
    - The RMCP-EVAL-05 description references `DEFER`
    - Existing PARITY-* rows unmodified
  </acceptance_criteria>
  <done>REQUIREMENTS.md has 5 RMCP-EVAL rows (reviews-mode text), 5 matching Traceability rows, Coverage footer updated.</done>
</task>

<task type="auto">
  <name>Task 2: Write 72-INVENTORY.md (inversion inventory, ≥15 rows, 9-column evidence schema)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md</files>
  <read_first>
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 94-135 (§"Inversion Inventory" — the seed 24-row matrix; executor TRANSLATES and UPGRADES this table)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-REVIEWS.md §"HIGH-3 Inventory evidence standard too weak" (mandates 9-column schema)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md lines 45-46 (grep gate for row count)
    - src/types/mod.rs (confirm re-export surface; cite the exact line of `pub use protocol::*;` — NOT `:1`)
    - src/types/jsonrpc.rs (cite the line number of the `JsonRpcMessage` enum definition — NOT `:1`)
    - src/types/capabilities.rs (cite the line of `ServerCapabilities` struct definition)
    - src/types/tools.rs (cite the line of `Tool` struct definition)
    - src/types/prompts.rs, src/types/resources.rs, src/types/sampling.rs, src/types/content.rs, src/types/elicitation.rs, src/types/tasks.rs (cite defining struct lines for each)
    - src/types/mcp_apps.rs (cite defining item line)
    - src/shared/stdio.rs, src/shared/websocket.rs, src/shared/wasm_websocket.rs, src/shared/sse_parser.rs, src/shared/streamable_http.rs, src/shared/wasm_http.rs, src/shared/connection_pool.rs, src/shared/protocol.rs, src/shared/middleware.rs, src/shared/simd_parsing.rs, src/shared/cancellation.rs, src/shared/session.rs, src/shared/batch.rs, src/shared/event_store.rs, src/shared/logging.rs
    - Fetch https://docs.rs/rmcp/1.5.0/rmcp/model/index.html
    - Fetch https://docs.rs/rmcp/1.5.0/rmcp/transport/index.html
    - Fetch https://docs.rs/rmcp/1.5.0/rmcp/service/index.html
    - For each row's feature-flag column: consult root `Cargo.toml` `[features]` section
    - For each row's downstream-crates column: consult `crates/mcp-tester/Cargo.toml`, `crates/mcp-preview/Cargo.toml`, `cargo-pmcp/Cargo.toml`, `pmcp-macros/Cargo.toml` for uses of each symbol
  </read_first>
  <action>
Create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md` with the 9-column evidence schema mandated by 72-REVIEWS.md HIGH-3. The table header has 12 columns (9 mandated + 3 retained: overlap / gap / LOC impact).

File contents to write:

```markdown
# Phase 72 — Inversion Inventory (RMCP-EVAL-01, reviews-mode revised)

**Baseline pin:** rmcp 1.5.0 (inherited from Phase 69 D-02) / pmcp <executor fills current main version>.
**Inventory date:** 2026-04-19 (reviews-mode revision).
**Source of truth for pmcp:** `src/types/`, `src/shared/`, `src/server/cancellation.rs` on current `main`.
**Source of truth for rmcp:** https://docs.rs/rmcp/1.5.0/rmcp/ (module index + model/transport/service/handler pages).

**Evidence-schema revision (per 72-REVIEWS.md HIGH-3):** each row now carries 9 mandated evidence columns. `file:line` MUST point at a DEFINING item (struct/enum/trait/impl/macro), NOT `:1`. Downstream crates column names the workspace crates that import the symbol.

## Overlap Legend

| Rating | Meaning |
|--------|---------|
| EXACT | rmcp type/module matches pmcp 1:1 for spec surface; pmcp adds no behavior beyond it |
| Partial | rmcp covers most of pmcp's surface; pmcp has identifiable extra behavior or accessors |
| pmcp-superset | rmcp has a base but pmcp ships a materially richer layer (e.g., TypedTool on top of Tool) |
| pmcp-exclusive | rmcp 1.5.0 has no equivalent (verified against docs.rs) |
| UNVERIFIED | Needs direct source check in Plan 02 spike or via `cargo public-api` on an rmcp test crate |

## Serde-Compat Legend

| Rating | Meaning |
|--------|---------|
| EXACT | serde_json::to_value + from_value roundtrips bit-identically between pmcp and rmcp types |
| compatible-via-adapter | roundtrip holds with a trivial `From`/`Into` adapter (≤20 LOC per type) |
| INCOMPATIBLE | serde shape differs (field names, tagging, enum representation) — would force breaking change |
| N/A | not a serde-derived type (e.g., transport internals, handler traits) |

## Inventory Rows

| # | pmcp module family | pmcp file:line (defining item) | rmcp evidence | Exact symbols touched | Public API surface impacted | Owned impls/macros | Serde compat risk | Feature flag(s) | Downstream crates | Overlap | Gap from rmcp | Migration LOC impact |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | JSON-RPC envelope types | `src/types/jsonrpc.rs:<line of JsonRpcMessage enum>` | https://docs.rs/rmcp/1.5.0/rmcp/model/enum.JsonRpcMessage.html | JsonRpcMessage, JsonRpcRequest, JsonRpcResponse, JsonRpcNotification, JsonRpcError, RequestId | `pub use` at crate root; in `pmcp::prelude` | `#[derive(Serialize, Deserialize)]` on all; no custom impls; no macros | EXACT (pending spike confirmation) | none (always-on) | mcp-tester, cargo-pmcp, mcp-preview, pmcp-macros | EXACT | None identified | ~615 LOC deletable under A; ~0 under B (re-export only) |
| 2 | Protocol Request/Notification envelopes + version | `src/types/protocol/mod.rs:<line of Request struct>`, `src/types/protocol/version.rs:<LATEST_PROTOCOL_VERSION const line>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Request, RequestNoParam, RequestOptionalParam, Notification, LATEST_PROTOCOL_VERSION | crate-root `pub use`; `pmcp::prelude` | serde derives; PartialEq derives; no macros | EXACT | none | mcp-tester, cargo-pmcp, pmcp-macros (macro expansion references Request) | EXACT | Same spec pin 2025-11-25 | ~723 LOC deletable |
| 3 | Capabilities (server+client) | `src/types/capabilities.rs:<line of ServerCapabilities>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | ServerCapabilities, ClientCapabilities, ToolsCapability, PromptsCapability, ResourcesCapability, SamplingCapability, TasksCapability, RootsCapabilities, ElicitationCapability, FormElicitationCapability, CompletionCapabilities, LoggingCapabilities | crate-root `pub use` | serde derives; Default impls | UNVERIFIED (Completion + Logging capabilities — spike verifies) | none | mcp-tester, cargo-pmcp | EXACT (Completion/Logging UNVERIFIED) | Verify Completion + Logging coverage | ~793 LOC deletable |
| 4 | Tools (Tool, CallTool*, ToolAnnotations) | `src/types/tools.rs:<line of Tool struct>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Tool, ToolAnnotations, CallToolRequest, CallToolRequestParams, CallToolResult, ToolChoice, ToolChoiceMode, ToolUseContent, ToolResultContent | crate-root `pub use`; `pmcp::prelude` | serde derives; `TypedTool<I, O>` wrapper lives in `src/server/typed_tool.rs` (NOT in src/types/) | EXACT on base types | none | mcp-tester, cargo-pmcp, pmcp-macros (`#[mcp_tool]` macro) | EXACT | TypedTool kept; macro expansion unaffected | ~904 LOC deletable |
| 5 | Prompts | `src/types/prompts.rs:<line of Prompt>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Prompt, PromptMessage, PromptArgument, PromptReference, GetPromptRequest, GetPromptResult, ListPromptsRequest, ListPromptsResult | crate-root `pub use` | serde derives | EXACT | none | mcp-tester, cargo-pmcp | EXACT | None | ~500 LOC deletable |
| 6 | Resources | `src/types/resources.rs:<line of Resource>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Resource, ResourceTemplate, RawResource, RawResourceTemplate, ResourceContents, ResourceReference, EmbeddedResource, ReadResourceRequest, ReadResourceResult, SubscribeRequest | crate-root `pub use` | serde derives | EXACT | none | mcp-tester, cargo-pmcp | EXACT | None | ~405 LOC deletable |
| 7 | Sampling | `src/types/sampling.rs:<line of CreateMessageRequest>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | SamplingMessage, CreateMessageRequest, CreateMessageRequestParams, CreateMessageResult, ModelPreferences, ModelHint, ContextInclusion | crate-root `pub use` | serde derives | EXACT | none | mcp-tester | EXACT | None | ~556 LOC deletable |
| 8 | Content | `src/types/content.rs:<line of Content enum>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Content, TextContent, ImageContent, AudioContent, RawContent | crate-root `pub use`; `pmcp::prelude` | serde derives; pmcp MCP Apps wraps `Content` in UIResource | EXACT (base); compatible-via-adapter (Apps wrappers) | `mcp-apps` (gates UIResource wrappers) | mcp-tester, cargo-pmcp, mcp-preview | EXACT (base); pmcp-superset (Apps wrappers) | pmcp keeps MCP Apps content wrappers | ~649 LOC base deletable; Apps wrappers kept |
| 9 | Elicitation | `src/types/elicitation.rs:<line of ElicitationSchema>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | ElicitationSchema, CreateElicitationRequest, ElicitationAction, CreateElicitationResult; rmcp additionally offers `ElicitationSchemaBuilder` | crate-root `pub use` | serde derives | EXACT | none | mcp-tester | EXACT + rmcp builder ergonomics (pmcp-gap) | Builder could be adopted; candidate upstream PR under Option D | ~198 LOC deletable |
| 10 | Tasks | `src/types/tasks.rs:<line of Task struct>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Task, TaskList, TaskStatus, TaskSupport, TasksCapability, TaskRequestsCapability, CreateTaskResult, GetTaskResult, CancelTaskRequest | crate-root `pub use` | serde derives; pmcp task_store + call_tool_and_poll live in `src/server/` (NOT in src/types/) | EXACT for types | none (types); `pmcp-tasks` (planned) | mcp-tester, cargo-pmcp | EXACT on types | Orchestration kept; types deletable | ~499 LOC base deletable |
| 11 | Auth types | `src/types/auth.rs:<line of AuthInfo>` | search "auth" feature on docs.rs/rmcp/1.5.0 | AuthInfo, AuthScheme | crate-root `pub use` behind `auth` feature | serde derives; custom `impl From<_> for _` for scheme enum | UNVERIFIED (rmcp `auth` feature covers OAuth2 flow; model-level AuthInfo unconfirmed) | `auth` | mcp-tester (auth tests), cargo-pmcp | UNVERIFIED | Likely pmcp-exclusive at handler boundary | ~376 LOC: verify via spike, then decide |
| 12 | Notifications | `src/types/notifications.rs:<line of first notification type>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Notification, NotificationNoParam, per-variant notification types | crate-root `pub use` | serde derives; pmcp Client notification-channel wrappers in `src/client/` | EXACT on base types | none | mcp-tester, cargo-pmcp | EXACT for base | pmcp Client notification-channel surface is a DX choice (CLIENT-03 gap from Phase 69) | ~343 LOC base deletable |
| 13 | Completable / Completion | `src/types/completable.rs:<line of defining item>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Completable trait + Completion types | crate-root `pub use` | custom trait impls (pmcp-specific); serde derives on data types | UNVERIFIED (Phase 69 did not inventory rmcp completion surface) | none | mcp-tester | UNVERIFIED | If rmcp covers, deletable; else pmcp-exclusive | ~508 LOC: spike resolves |
| 14 | MCP Apps + UI | `src/types/mcp_apps.rs:<line of UIResource>`, `src/types/ui.rs:<line of WidgetMeta>` | (absence) https://docs.rs/rmcp/1.5.0/rmcp/model/ (no mcp_apps / ui modules) | UIResource, WidgetMeta, UIContent, TemplateWidgetInstance | crate-root `pub use` behind `mcp-apps` feature; `pmcp::prelude::apps` | serde derives; custom impls for UIResource adapters; pmcp-widget-utils macros | N/A (rmcp has no equivalent) | `mcp-apps` | mcp-preview, cargo-pmcp, pmcp-widget-utils, pmcp-macros-support | pmcp-exclusive | pmcp enterprise exclusive (SEP-1865 not in 1.5.0) | 0 — keep |
| 15 | stdio transport | `src/shared/stdio.rs:<line of stdio_transport fn or Stdio struct>` | https://docs.rs/rmcp/1.5.0/rmcp/transport/index.html | StdioTransport + io adapter | crate-root `pub use` | async_trait impls for Transport trait | N/A | none | mcp-tester, cargo-pmcp | EXACT | None | ~288 LOC deletable |
| 16 | WebSocket transport (client + WASM) | `src/shared/websocket.rs:<line of WebSocketTransport>`, `src/shared/wasm_websocket.rs:<line of WasmWebSocketTransport>` | (absence) https://docs.rs/rmcp/1.5.0/rmcp/transport/ (no ws-specific module — only generic Sink/Stream adapter) | WebSocketTransport, WasmWebSocketTransport | crate-root `pub use` behind feature gates | async_trait impls | N/A | `websocket`, `wasm` | mcp-tester | pmcp-exclusive | pmcp keeps (differentiator) | 0 — keep |
| 17 | SSE parser + reconnect | `src/shared/sse_parser.rs:<line of SseParser>`, `src/shared/sse_optimized.rs:<line of SimdParser>`, `src/shared/reconnect.rs:<line of ReconnectPolicy>` | rmcp `client-side-sse` helper via `sse-stream` dep | SseParser, SimdSseParser, ReconnectPolicy | crate-root `pub use` behind feature gates | hand-rolled state machine; unsafe SIMD | N/A | `sse`, `simd` | mcp-tester | Partial (pmcp SIMD parser + reconnect is differentiator) | pmcp keeps | 0 — keep |
| 18 | Streamable HTTP (client + server) | `src/shared/streamable_http.rs:<line of StreamableHttpTransport>`, `src/server/streamable_http_server.rs:<line of server impl>` | https://docs.rs/rmcp/1.5.0/rmcp/transport/ (StreamableHttpClientTransport + transport-streamable-http-server feature) | StreamableHttpTransport, server axum integration, DNS-rebinding guard | crate-root `pub use` behind `http` feature | axum integration; tower middleware; Lambda adapter | N/A | `http`, `lambda` | mcp-tester, cargo-pmcp | Partial — same spec; pmcp adds axum + DNS rebinding + Lambda | Hybrid-friendly (Option B) | ~996 LOC conditional |
| 19 | WASM HTTP transport | `src/shared/wasm_http.rs:<line of WasmHttpTransport>` | (absence) https://docs.rs/rmcp/1.5.0/rmcp/transport/ (no wasm32 transport) | WasmHttpTransport | crate-root `pub use` behind `wasm` feature | fetch API adapter | N/A | `wasm` | (none published yet) | pmcp-exclusive | pmcp WASM differentiator | 0 — keep |
| 20 | Connection pool | `src/shared/connection_pool.rs:<line of ConnectionPool struct>` | (absence) https://docs.rs/rmcp/1.5.0/rmcp/ | ConnectionPool, PoolConfig | crate-root `pub use` | custom pool impl | N/A | none | cargo-pmcp | pmcp-exclusive | pmcp keeps | 0 — keep |
| 21 | Protocol framing helpers | `src/shared/protocol.rs:<line of framing helper>`, `src/shared/protocol_helpers.rs:<line of helper fn>` | https://docs.rs/rmcp/1.5.0/rmcp/service/index.html | framing fns + Peer construction | crate-root `pub use` | custom impls | N/A | none | mcp-tester, cargo-pmcp | Partial — framing deletable, pmcp DX helpers kept | Helpers are DX sugar | Mixed — ~431 deletable, ~950 helpers kept |
| 22 | Middleware framework | `src/shared/middleware.rs:<line of MiddlewareLayer trait>` | (no first-party middleware module in rmcp; tower feature exists) | MiddlewareLayer, MiddlewareStack | crate-root `pub use` behind `middleware` feature | custom trait + impls | N/A | `middleware` | mcp-tester, cargo-pmcp | pmcp-exclusive | pmcp keeps (Phase 56 asset) | 0 — keep |
| 23 | SIMD parsing | `src/shared/simd_parsing.rs:<line of simd fn>` | (absence) | SimdJsonParser | crate-root `pub use` behind `simd` feature | unsafe SIMD intrinsics | N/A | `simd` | (none published) | pmcp-exclusive | pmcp keeps (perf differentiator) | 0 — keep |
| 24 | Cancellation / RequestHandlerExtra | `src/shared/cancellation.rs:<line>`, `src/server/cancellation.rs:<line of RequestHandlerExtra>` | https://docs.rs/rmcp/1.5.0/rmcp/service/ (RequestContext<RoleServer> via tokio_util::sync::CancellationToken) | RequestHandlerExtra, CancellationToken re-export | crate-root `pub use` | custom impl | N/A | none | mcp-tester, cargo-pmcp, pmcp-macros | Partial (shape parity after Phase 70) | Hybrid integration candidate | Low migration risk |
| 25 | Session | `src/shared/session.rs:<line of Session struct>` | https://docs.rs/rmcp/1.5.0/rmcp/transport/ (streamable-http-server-session feature) | Session, SessionStore | crate-root `pub use` behind `http` feature | custom impls; auth plumbing | UNVERIFIED | `http` | mcp-tester | UNVERIFIED | pmcp session includes auth plumbing | Zero to Low |
| 26 | Batch | `src/shared/batch.rs:<line of Batch struct>` | https://docs.rs/rmcp/1.5.0/rmcp/ (search) | BatchRequest, BatchResponse | crate-root `pub use` | serde derives | UNVERIFIED | none | mcp-tester | UNVERIFIED | Possibly deletable | Low |
| 27 | URI template | `src/shared/uri_template.rs:<line of UriTemplate>` | https://docs.rs/rmcp/1.5.0/rmcp/model/ (rmcp `resources` likely uses internal impl) | UriTemplate | crate-root `pub use` | custom impl | N/A | none | mcp-tester | Independent impls | Low-stakes duplication | Keep or evaluate |
| 28 | Event store | `src/shared/event_store.rs:<line of EventStore trait>` | (absence) | EventStore trait, InMemoryEventStore | crate-root `pub use` | custom impls | N/A | `http` (for streamable-http resume) | mcp-tester | pmcp-exclusive | pmcp keeps | 0 — keep |
| 29 | Logging | `src/shared/logging.rs:<line of LoggingLayer>` | (rmcp uses `tracing` directly; no logging module) | LoggingLayer | crate-root `pub use` | tracing-subscriber layer | N/A | `tracing` | cargo-pmcp | UNVERIFIED | Likely keep | Zero to Low |

## Totals (cite Strategy Matrix)

| Category | LOC impact | Notes |
|----------|-----------|-------|
| Strong-overlap / deletable under Full adopt (A) | ~6,400 LOC | Rows 1–10 + 15 (jsonrpc + protocol + capabilities + tools + prompts + resources + sampling + content-base + elicitation-base + tasks-base + stdio) |
| Types-only borrow (C1) — deletable | ~4,500 LOC | Rows 1–9 only (types); transports kept |
| Transports-only borrow (C2) — deletable | ~1,200 LOC | Row 15 (stdio) + row 18 partial (streamable-http client wrapper); types kept |
| Conditional (Hybrid-friendly, B) | ~2,500 LOC | Rows 18 (streamable_http), 25 (session), 26 (batch), 29 (logging) |
| pmcp-exclusive / retained under every option | ~9,000 LOC | Rows 14 (mcp_apps + ui), 16 (websocket), 17 (sse_parser + reconnect), 19 (wasm_http), 20 (connection_pool), 22 (middleware), 23 (simd_parsing), 28 (event_store) |
| UNVERIFIED rows needing Plan 02 spike | 5 | Row 3 (Completion/Logging capabilities), 11 (auth), 13 (completable), 25 (session), 26 (batch), 29 (logging) |

## Mapping to Strategy Matrix options (C split per reviews replan action #7)

- **Option A (Full adopt)** deletes rows 1–10, 15 (+ conditional 18/25/26/29 under spike confirmation).
- **Option B (Hybrid)** re-exports rows 1–10 types via facade; keeps pmcp transports (rows 16, 17, 19, 20, 22, 23, 28) first-party; conditional keep on row 18 (wrap rmcp streamable-http + add pmcp security layer).
- **Option C1 (Types only)** borrows rmcp for rows 1–10; keeps all transports first-party.
- **Option C2 (Transports only)** borrows rmcp for row 15 (stdio) and optionally row 18 client; keeps all types first-party.
- **Option D (Status quo)** keeps everything first-party; contributes ElicitationSchemaBuilder (row 9 gap) + any other Phase-69 gaps as upstream PRs.
- **Option E (Fork)** — contingency only, see 72-STRATEGY-MATRIX.md footnote.

## Feeds Into

- 72-STRATEGY-MATRIX.md rows A, B, C1, C2, D cite these totals for the "Maintenance reduction" column.
- 72-POC-PROPOSAL.md Slice 1 uses rows 1–10 as the re-export test surface.
- 72-DECISION-RUBRIC.md T1 and T8 cite these totals.
```

**Executor note:** every `<line of ...>` placeholder above MUST be replaced with the actual line number from the read_first step. This is the ONLY way the row satisfies HIGH-3's "defining item, not `:1`" requirement. Run `grep -n 'pub struct\|pub enum\|pub trait\|pub fn' <path>` on each source file and pick the first defining line. Do NOT commit this file with `<line of ...>` placeholders still in it — the final-verification grep explicitly checks for `[0-9]+` (must be a real number, not a placeholder).

After writing:
```bash
INV=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md
grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' "$INV"    # expect >= 15
! grep -qE ':1\b' "$INV" || echo "WARN: :1 placeholder still present on some row — re-check"
! grep -qE '<line of' "$INV"                     # MUST return no lines
```
  </action>
  <verify>
    <automated>INV=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md; test $(grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' $INV) -ge 15 && ! grep -iE '^(TBD|XXX|FIXME)$' $INV && ! grep -qE '<line of' $INV && grep -q 'Baseline pin:.*rmcp 1.5.0' $INV && grep -q 'Serde-Compat Legend' $INV && grep -q 'Downstream crates' $INV</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md`
    - `wc -l` ≥ 60
    - `grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' 72-INVENTORY.md` ≥ 15
    - `! grep -qE '<line of' 72-INVENTORY.md` (no unfilled placeholders)
    - 9-column schema headers present: grep for `Exact symbols touched`, `Public API surface impacted`, `Owned impls/macros`, `Serde compat risk`, `Feature flag`, `Downstream crates` — each ≥ 1 hit
    - `grep -c 'docs.rs/rmcp/1.5.0' 72-INVENTORY.md` ≥ 10
    - `grep -c '| EXACT |' 72-INVENTORY.md` ≥ 6
    - `grep -c '| pmcp-exclusive |' 72-INVENTORY.md` ≥ 5
    - `grep -c '| UNVERIFIED |' 72-INVENTORY.md` ≥ 3
    - Mapping section references `C1` AND `C2` AND `E (Fork)` as contingency
    - `! grep -iE '\bTBD\b|\bFIXME\b' 72-INVENTORY.md`
  </acceptance_criteria>
  <done>72-INVENTORY.md exists with ≥15 rows on the 9-column evidence schema, real file:line references (not `:1`), serde-compat + feature-flag + downstream-crates columns populated.</done>
</task>

<task type="auto">
  <name>Task 3: Write 72-STRATEGY-MATRIX.md (5 options A/B/C1/C2/D × 5 criteria; E as footnote only)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md</files>
  <read_first>
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 285-332 (§"Strategy Matrix" — per-option seed descriptions)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 263-283 (§"Comparable Precedents")
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-REVIEWS.md §"Recommended Replan Actions" #7 (split C, demote E)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md lines 47 (grep gate — UPDATED: 5 rows are A/B/C1/C2/D, NOT A/B/C/D/E)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (Task 2 output — Totals + Mapping sections)
  </read_first>
  <action>
Create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md` with the REVISED option set per reviews replan action #7. Option rows are {A, B, C1, C2, D}. E appears only as a footnote (H2 "## Contingency (not scored): E. Fork").

```markdown
# Phase 72 — Strategy Matrix (RMCP-EVAL-02, reviews-mode revised)

**Baseline pin:** rmcp 1.5.0 / pmcp current main.
**Depends on:** 72-INVENTORY.md (Totals table, Mapping section) for LOC columns.
**Scoring is directional; absolute numeric weights are set by CONTEXT.md and the decision rubric (not here).**

**Option-set revision (per 72-REVIEWS.md replan action #7):**
- Option C has been split into **C1 (types-only borrow)** and **C2 (transports-only borrow)** because they are materially different strategies with different migration costs and breaking-change profiles.
- Option E (Fork rmcp) has been DEMOTED from a scored row to a contingency footnote. E is not a primary strategy; it is an insurance policy attached to A or B when rmcp governance fails (T2).

## Options (rows — exactly 5 scored)

- **A. Full adopt** — pmcp re-exports rmcp's model + service + transport; deletes src/types/ (except mcp_apps, ui, auth) and src/shared/{stdio,protocol}.rs; refactors handlers to consume rmcp's RequestContext<RoleServer>.
- **B. Hybrid / wrapper** — pmcp depends on rmcp as a foundation; re-exports rmcp model types with a compatibility facade; keeps pmcp transports where pmcp has a real differentiator (WebSocket, WASM, security-hardened streamable-HTTP, SIMD SSE).
- **C1. Selective borrow — types only** — pmcp takes rmcp's model types (rows 1–10 from 72-INVENTORY.md Mapping), keeps all transports first-party. Maintenance reduction comes from type-layer only; ~4,500 LOC deletable.
- **C2. Selective borrow — transports only** — pmcp takes rmcp's stdio (row 15) and optionally streamable-http client wrapper, keeps all types first-party. Narrow scope, low migration cost.
- **D. Status quo + upstream PRs** — pmcp keeps all ~21,700 LOC first-party. Any rmcp ergonomic gaps pmcp has better answers to (e.g., row 9 ElicitationSchemaBuilder) get upstreamed as PRs. Zero migration cost.

## Criteria (columns — 5)

- **Maintenance reduction** — LOC removed from pmcp's protocol layer + spec-tracking burden transferred to rmcp.
- **Migration cost** — Engineering effort in PoC slices (cite 72-POC-PROPOSAL.md slice sizes) + downstream workspace ripple (mcp-tester, cargo-pmcp, pmcp-code-mode, pmcp-tasks).
- **Breaking-change surface** — Count of pmcp public type identities that change; impact on pmcp v3.0 SemVer planning.
- **Enterprise feature preservation** — Does the option preserve named extension-points for {TypedTool, workflow engine, mcp_apps, auth, middleware, mcp-preview, cargo-pmcp}? (This binds to T9 in the decision rubric.)
- **Spec-upgrade agility** — How quickly does pmcp track an MCP spec rev (weekly/monthly/quarterly) under this option?

## The Matrix

| Option | Maintenance reduction | Migration cost | Breaking-change surface | Enterprise feature preservation | Spec-upgrade agility |
|---|---|---|---|---|---|
| A. Full adopt | HIGH — ~6,400 LOC gone from src/types/ + ~1,200 from src/shared/ (cite 72-INVENTORY.md Totals row 1); spec-tracking burden transfers to rmcp's ~6-week cadence | HIGH — every pmcp import changes; PoC Slice 1 (see 72-POC-PROPOSAL.md) measures the compile-error count on a throwaway spike; downstream ripple into mcp-tester + cargo-pmcp + pmcp-code-mode + pmcp-tasks | HIGH — pmcp 3.0 major; pmcp::types::X changes identity to `pub use rmcp::model::X`; downstream pattern-matches / trait impls break | CONDITIONAL — Apps/workflow/auth are pmcp-exclusive (72-INVENTORY rows 11, 14, 22) and stay first-party; TypedTool touches types at every boundary, risk needs Slice 2 verification. Per T9 (enterprise-feature preservation checklist), A scores 1 only if all 7 extension-points have named preservation mechanisms. | HIGH — rmcp tracks spec ~quarterly; pmcp ships same-day once facade bumps |
| B. Hybrid / wrapper | MEDIUM — ~5,000 LOC gone (types re-exported, stdio delegated); workflow + transports stay first-party per 72-INVENTORY rows 14–23 | MEDIUM — facade construction effort ~1–2 weeks; less ripple than A because facade preserves pmcp::types::X paths; PoC Slice 2 gauges ergonomics friction | MEDIUM — v3.0 still required because identity changes under the facade; some imports stable, some break (custom From impls, enum-variant pattern matches) | FULL (naturally) — pmcp owns MCP Apps, workflow, auth, middleware, all pmcp-exclusive transports; T9 checklist passes by construction because facade preserves the surface | MEDIUM — still need to wrap new rmcp surfaces in the facade on each rmcp minor |
| C1. Selective borrow — types only | LOW-MEDIUM — ~4,500 LOC gone (72-INVENTORY rows 1–10); transports stay first-party | LOW-MEDIUM — facade on types only; narrower than B; less ripple | MEDIUM — type-identity changes still force v3.0 for downstream type-consumers; transport users unaffected | FULL — all pmcp-exclusive subsystems kept; T9 passes | MEDIUM — rmcp type bumps tracked; transports stay under pmcp control |
| C2. Selective borrow — transports only | LOW — ~1,200 LOC gone (stdio + streamable-http client wrapper, 72-INVENTORY row 15 + partial 18) | LOW — narrow scope, minimal ripple | LOW — types unchanged; only transport-level public APIs shift (behind feature flags) | FULL — types stay first-party, all pmcp-exclusive features stay; T9 passes | LOW — types still first-party; spec revs still hit pmcp directly |
| D. Status quo + upstream PRs | ZERO — pmcp keeps all ~21,700 LOC of src/types/ + src/shared/ per 72-INVENTORY Totals | ZERO migration; small ongoing cost of upstream PRs (Phase 69 gaps pmcp wants in rmcp, e.g., ElicitationSchemaBuilder from row 9) | ZERO — no pmcp public API changes | FULL — nothing changes | LOW — pmcp tracks spec manually; rmcp's tracking doesn't help |

## Precedent Crosswalk (from 72-RESEARCH.md §"Comparable Precedents")

| Option | Closest Rust precedent | Lesson |
|---|---|---|
| A | reqwest over hyper | Full wrapper hides foundation; advanced users need escape hatch |
| B | tonic over prost, axum over hyper | Proven layering; version-pin coupling is a real risk (axum 0.7 on hyper 1.0) |
| C1 | serde_derive → serde | Types-only borrow works when the borrowed crate is SemVer-stable |
| C2 | sqlx driver borrow | Transport-only borrow; narrow scope |
| D | (any SDK that kept wire + types first-party) | Zero churn, misses ecosystem goodwill |

## Risk Annotation

Three risks cut across all five scored options and must be carried into 72-DECISION-RUBRIC.md:
1. **rmcp governance SLA is unpublished** (72-RESEARCH.md §"Rmcp Trajectory Risk") — impacts A, B, C1, C2 (NOT D).
2. **pmcp v2.x breaking-change window open/closed** (locked in 72-CONTEXT.md T6) — impacts A, B, C1.
3. **pmcp downstream-user tolerance for v3.0** (locked in 72-CONTEXT.md T7) — impacts A, B, C1.

---

## Contingency (not scored): E. Fork rmcp into vendor/

E is NOT a primary strategy. Per 72-REVIEWS.md replan action #7, E is demoted to contingency only.

**When E is activated:** if rmcp governance responsiveness falls below T2 threshold (median issue close time ≥ 14 days) AFTER A or B has already been selected and migration is underway, pmcp forks rmcp into `crates/vendor/rmcp/` as an escape hatch.

**Cost profile:** maintenance burden doubles (pmcp now owns its own protocol layer AND a vendored rmcp copy). This is acceptable ONLY as insurance, never as the starting position.

**Directional scoring (informational only, not used for the Plan 03 recommendation):**
- Maintenance reduction: NEGATIVE
- Migration cost: HIGH (fork-sync tooling + CI ripple)
- Breaking-change surface: LOW externally, HIGH internally
- Enterprise feature preservation: FULL
- Spec-upgrade agility: LOW (vendored fork diverges)

**Rule for Plan 03:** E MUST NOT appear as the primary `**Recommendation:**` letter. The valid recommendation set is {A, B, C1, C2, D, DEFER}.

---

## Feeds Into

- 72-POC-PROPOSAL.md slice selection: Slice 1 disambiguates A vs B vs C1 (types feasibility); Slice 2 validates B; Slice 3 validates B workflow preservation. Slice 1 is EXECUTED in Plan 02 as a throwaway spike (new in reviews-mode revision).
- 72-DECISION-RUBRIC.md thresholds: maintenance-reduction column feeds T1 (LOC delta) and T8 (historical churn); enterprise-feature-preservation column feeds T9 (extension-point checklist).
- 72-RECOMMENDATION.md: final pick from {A, B, C1, C2, D, DEFER}. E is explicitly NOT a valid pick.
```

The matrix has exactly 5 option rows and 5 criteria columns — 25 scored cells. No cell says `TBD`. E appears only in the Contingency footnote section, not as a scored row.
  </action>
  <verify>
    <automated>MAT=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md; test $(grep -cE '^\| (A\. Full adopt|B\. Hybrid|C1\. Selective|C2\. Selective|D\. Status quo)' $MAT) -eq 5 && ! grep -qE '^\| E\. Fork' $MAT && grep -q '^## Contingency (not scored): E\. Fork' $MAT && ! grep -iE '\bTBD\b' $MAT && test $(wc -l < $MAT) -ge 40 && grep -q '72-INVENTORY.md' $MAT</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md`
    - `wc -l` ≥ 40
    - `grep -cE '^\| (A\. Full adopt|B\. Hybrid|C1\. Selective|C2\. Selective|D\. Status quo)' 72-STRATEGY-MATRIX.md` returns EXACTLY 5
    - `! grep -qE '^\| E\. Fork' 72-STRATEGY-MATRIX.md` (E is NOT a scored row)
    - `grep -q '^## Contingency (not scored): E\. Fork' 72-STRATEGY-MATRIX.md` (E is a footnote section)
    - `! grep -i 'TBD' 72-STRATEGY-MATRIX.md`
    - `grep -c '72-INVENTORY.md' 72-STRATEGY-MATRIX.md` ≥ 1
    - Contains strings `C1`, `C2` (revised option set)
    - Contains strings `T9` AND `T2` (rubric cross-reference)
    - `grep -q '^## The Matrix' 72-STRATEGY-MATRIX.md`
    - `grep -q 'rmcp 1.5.0' 72-STRATEGY-MATRIX.md`
  </acceptance_criteria>
  <done>72-STRATEGY-MATRIX.md exists with exactly 5 scored option rows {A, B, C1, C2, D}, E demoted to footnote, 25 scored cells, no TBD, cross-referenced to 72-INVENTORY.md.</done>
</task>

<task type="auto">
  <name>Task 4: Commit Plan 01 deliverables (reviews-mode revision)</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md,
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md,
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
  </files>
  <read_first>
    - CLAUDE.md §"Quality Gate Enforcement" (markdown-only commit)
  </read_first>
  <action>
Commit the four produced/modified files:

```bash
gsd-sdk query commit "docs(72): seed RMCP-EVAL REQ-IDs, CONTEXT locks, inversion inventory (9-col schema), strategy matrix (A/B/C1/C2/D, E-as-footnote) [reviews-mode]" \
  .planning/REQUIREMENTS.md \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
```

Fallback if `gsd-sdk query commit` fails:
```bash
git add .planning/REQUIREMENTS.md \
        .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md \
        .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md \
        .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
git commit -m "docs(72): seed RMCP-EVAL REQ-IDs, CONTEXT locks, inversion inventory (9-col schema), strategy matrix (A/B/C1/C2/D, E-as-footnote) [reviews-mode]"
```

Do NOT use `--no-verify`. Do NOT run `make quality-gate` (markdown-only).
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -q '72-CONTEXT.md' && git log -1 --name-only | grep -q '72-INVENTORY.md' && git log -1 --name-only | grep -q '72-STRATEGY-MATRIX.md' && git log -1 --name-only | grep -q 'REQUIREMENTS.md' && git log -1 --pretty=%s | grep -q 'reviews-mode'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches `docs(72):` AND contains `reviews-mode`
    - `git log -1 --name-only` lists all four files (REQUIREMENTS.md, 72-CONTEXT.md, 72-INVENTORY.md, 72-STRATEGY-MATRIX.md)
    - `git status --porcelain .planning/REQUIREMENTS.md` returns empty (clean)
    - `git status --porcelain .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/` shows only untracked `.gitkeep` or empty
  </acceptance_criteria>
  <done>Plan 01 reviews-mode deliverables committed; REQUIREMENTS.md + CONTEXT.md + INVENTORY.md + STRATEGY-MATRIX.md on current branch.</done>
</task>

</tasks>

<verification>
Plan 01 (reviews-mode revised) is complete when:
1. `72-CONTEXT.md` exists with explicit T6 and T7 fields (concrete value OR UNKNOWN + Resolution path).
2. `grep -c '^- \[ \] \*\*RMCP-EVAL-' .planning/REQUIREMENTS.md` returns 5 and the descriptions reference `C1`/`C2`/`DEFER`.
3. `grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' 72-INVENTORY.md` returns ≥ 15, AND `! grep -qE '<line of' 72-INVENTORY.md` (no unfilled placeholders), AND the 9-column schema headers are present.
4. `grep -cE '^\| (A\. Full adopt|B\. Hybrid|C1\. Selective|C2\. Selective|D\. Status quo)' 72-STRATEGY-MATRIX.md` returns exactly 5, AND E is present only in `## Contingency (not scored): E. Fork`.
5. `! grep -i 'TBD' 72-STRATEGY-MATRIX.md` succeeds.
6. Plan 01's commit is on `HEAD` with `reviews-mode` in the subject.

Cross-document consistency check (manual, logged in SUMMARY):
- Every pmcp-exclusive row in 72-INVENTORY.md aligns with the scored options' Enterprise-feature-preservation cells.
- Strategy matrix's Option A and B "Maintenance reduction" cells cite the same LOC totals as 72-INVENTORY.md Totals.
- 72-CONTEXT.md's T6/T7 values are internally consistent with evidence cited.
</verification>

<success_criteria>
- 72-CONTEXT.md exists with explicit T6 and T7 locks or UNKNOWN+Resolution-path.
- REQUIREMENTS.md contains RMCP-EVAL-01..05 rows (reviews-mode wording) + 5 Traceability rows; no pre-existing IDs modified.
- 72-INVENTORY.md exists with ≥15 rows on the 9-column evidence schema; real file:line references (not `:1` placeholders).
- 72-STRATEGY-MATRIX.md has exactly 5 scored option rows {A, B, C1, C2, D} and E as a footnote; all 25 cells scored; no TBD.
- One git commit on HEAD with message starting `docs(72):` and containing `reviews-mode`, covering all four modified files.
</success_criteria>

<output>
After completion, create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md` documenting:
- Reviews-mode revision summary (which findings were addressed: HIGH-3 9-col schema, replan #3 CONTEXT.md, replan #7 C-split + E-footnote)
- Row counts in 72-INVENTORY.md (total + per-overlap-rating breakdown + per-serde-compat-rating breakdown)
- T6 and T7 lock status from 72-CONTEXT.md (concrete value or UNKNOWN + resolution path)
- Number of UNVERIFIED rows carried into Plan 02 / spike
- Matrix scoring highlights (which options scored highest on which criteria directionally)
- Cross-doc consistency check result
- Any deviations from 72-RESEARCH.md framing introduced by reviews-mode revision (document explicitly)
</output>
