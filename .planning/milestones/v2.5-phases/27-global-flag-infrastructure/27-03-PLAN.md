---
phase: 27-global-flag-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cargo-pmcp/src/commands/validate.rs
autonomous: true
gap_closure: true
requirements:
  - FLAG-08
  - FLAG-09
must_haves:
  truths:
    - "All decorative output (banners, progress, success messages, warnings, informational text) is suppressed when --quiet is active"
    - "User can pass --quiet to any cargo pmcp command and only errors and explicitly requested output appear"
    - "validate.rs decorative println! calls are gated by PMCP_QUIET env var check, matching the pattern used in app.rs, connect.rs, landing/dev.rs, landing/init.rs, and landing/deploy.rs"
    - "Error messages (compilation failed, validation failed) remain unconditional and always show"
  artifacts:
    - path: "cargo-pmcp/src/commands/validate.rs"
      provides: "Quiet-aware decorative output using PMCP_QUIET env var guard"
      contains: "PMCP_QUIET"
  key_links:
    - from: "validate.rs validate_workflows()"
      to: "PMCP_QUIET env var"
      via: "let not_quiet = std::env::var(\"PMCP_QUIET\").is_err(); with if not_quiet { ... } guards"
      pattern: "not_quiet"
    - from: "validate.rs run_validation()"
      to: "PMCP_QUIET env var"
      via: "not_quiet parameter threaded from validate_workflows caller"
      pattern: "not_quiet"
---

<objective>
Fix the one gap found by verification: `cargo-pmcp/src/commands/validate.rs` has ~25 unconditional `println!` calls for decorative output that are not suppressed when `--quiet` is active. Add `PMCP_QUIET` env var checks following the exact same pattern used in `app.rs`, `connect.rs`, and `landing/*.rs`.

Purpose: Complete FLAG-09 satisfaction. After this fix, `--quiet` suppresses decorative output in ALL 25 command files (previously 24 of 25).

This is a gap closure plan. The original 27-02-PLAN.md covered validate.rs in its scope but the execution missed adding quiet guards to this file's private functions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-global-flag-infrastructure/27-VERIFICATION.md
@.planning/phases/27-global-flag-infrastructure/27-02-SUMMARY.md
@cargo-pmcp/src/commands/validate.rs
@cargo-pmcp/src/commands/mod.rs

<interfaces>
The established quiet pattern for functions that do NOT receive `&GlobalFlags` directly is:

```rust
let not_quiet = std::env::var("PMCP_QUIET").is_err();
if not_quiet {
    println!("decorative message");
}
```

This is the PMCP_QUIET env var approach used in: app.rs, connect.rs, landing/init.rs, landing/dev.rs, landing/deploy.rs.

The env var is set in main.rs line 300:
```rust
std::env::set_var("PMCP_QUIET", "1");
```

Error output MUST remain unconditional. Specifically in validate.rs:
- `anyhow::anyhow!("Compilation failed")` and `anyhow::anyhow!("Workflow validation failed")` — these are errors, KEEP.
- The `println!` at line 92-95 that says "Compilation failed. Fix errors..." — this is an error message, KEEP.
- The `println!` at lines 218-224 that says "Workflow validation failed: X of Y tests passed" — this is an error message, KEEP.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PMCP_QUIET guards to all decorative output in validate.rs</name>
  <files>cargo-pmcp/src/commands/validate.rs</files>
  <action>
  Apply the `PMCP_QUIET` env var pattern to `cargo-pmcp/src/commands/validate.rs`. The file has four private functions with unconditional decorative `println!` calls. Here is the exact fix:

  **1. In `validate_workflows()` (line 48):**
  Add `let not_quiet = std::env::var("PMCP_QUIET").is_err();` at the top of the function body.

  Wrap these decorative outputs with `if not_quiet { ... }`:
  - Line 49: banner println (`style("PMCP Workflow Validation")`)
  - Line 50: separator println (`style("━".repeat(50))`)
  - Lines 57-61: "Validating in: ..." message

  Pass `not_quiet` as a parameter to `run_validation()`.

  **2. In `run_validation()` (line 72):**
  Add `not_quiet: bool` parameter. Update the call site in `validate_workflows()`.

  Wrap these decorative outputs with `if not_quiet { ... }`:
  - Line 74: "Step 1: Checking compilation..."
  - Line 98: "Compilation successful" checkmark
  - Lines 101-104: "Step 2: Looking for workflow validation tests..."
  - Lines 110-112: "No workflow tests found. Generating scaffolding..."
  - Lines 115-118: "Generated validation test scaffolding" checkmark
  - Lines 119-123: "Run cargo pmcp validate workflows again to validate"
  - Lines 125-128: "No workflow validation tests found"
  - Lines 134-138: "Found N workflow test pattern(s)"
  - Lines 141-144: "Step 3: Running workflow validation tests..."
  - Lines 172-179: pattern results with "X passed, Y failed"
  - Lines 186-191: pattern results with "X passed"
  - Lines 193-198: pattern results with "no tests matched"
  - Line 202: separator
  - Lines 204-210: "All N workflow validation tests passed!" (success message — decorative)
  - Lines 212-216: "No workflow tests were executed"

  **DO NOT wrap these — they are error messages that MUST always show:**
  - Lines 92-95: "Compilation failed. Fix errors and try again." — this is the error output right before `return Err(...)`.
  - Lines 218-224: "Workflow validation failed: X of Y tests passed" — this is the error output right before `return Err(...)`.

  Also pass `not_quiet` to `print_test_guidance()` and `generate_validation_scaffolding()`.

  **3. In `print_test_guidance()` (line 428):**
  Add `not_quiet: bool` parameter. Wrap the entire function body in `if not_quiet { ... }` since all output is guidance/informational.

  **4. In `generate_validation_scaffolding()` (line 331):**
  Add `not_quiet: bool` parameter. Wrap the decorative `println!` calls with `if not_quiet { ... }`:
  - Lines 339-344: "Test file already exists" message
  - Lines 418-422: "Created: ..." message

  **5. In `print_failure_summary()` (line 308):**
  Leave as-is. This prints failure details which are part of error output and should always show.

  **6. Clean up execute():**
  Change `let _ = global_flags;` to remove the suppression. The comment `// quiet mode conveyed via PMCP_QUIET env var` is now accurate since we actually check PMCP_QUIET.

  Replace:
  ```rust
  let _ = global_flags; // quiet mode conveyed via PMCP_QUIET env var
  ```
  With nothing (just remove the line entirely, or keep just the comment):
  ```rust
  // quiet mode conveyed via PMCP_QUIET env var for downstream functions
  let _ = global_flags;
  ```
  Keep the `let _ = global_flags;` to avoid an unused variable warning, but fix the comment to be accurate.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo check -p cargo-pmcp 2>&1 | tail -5 && cargo clippy -p cargo-pmcp -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>
  - All ~25 decorative println! calls in validate.rs are gated by `not_quiet` (PMCP_QUIET env var check)
  - Error messages ("Compilation failed", "Workflow validation failed") remain unconditional
  - print_failure_summary() remains unconditional (error detail output)
  - Pattern matches app.rs, connect.rs, landing/*.rs approach exactly
  - `cargo check` and `cargo clippy` pass with zero warnings
  - FLAG-09 is now fully satisfied across all 25 command files
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p cargo-pmcp` passes
2. `cargo clippy -p cargo-pmcp -- -D warnings` passes with zero warnings
3. `grep -c "PMCP_QUIET" cargo-pmcp/src/commands/validate.rs` returns at least 1 (env var is checked)
4. `grep -c "not_quiet" cargo-pmcp/src/commands/validate.rs` returns at least 5 (guard variable used throughout)
5. Verify error messages at lines ~92 and ~218 are NOT wrapped in `if not_quiet` guards
6. Verify print_failure_summary() is NOT wrapped in quiet guards (error detail output)
</verification>

<success_criteria>
- Every decorative println! in validate.rs is gated by PMCP_QUIET env var check
- Error messages always show regardless of quiet mode
- The pattern matches the established approach in app.rs, connect.rs, landing/*.rs
- cargo check and cargo clippy pass with zero errors/warnings
- FLAG-09 is fully satisfied (25 of 25 command files honor --quiet)
</success_criteria>

<output>
After completion, create `.planning/phases/27-global-flag-infrastructure/27-03-SUMMARY.md`
</output>
