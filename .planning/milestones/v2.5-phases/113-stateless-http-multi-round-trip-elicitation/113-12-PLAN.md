---
phase: 113-stateless-http-multi-round-trip-elicitation
plan: 12
type: execute
wave: 7
depends_on: ["113-01", "113-02", "113-03", "113-04", "113-05", "113-06", "113-07", "113-08", "113-09", "113-10", "113-11", "113-13"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md
autonomous: true
requirements: [HTTP-01, HTTP-02, HTTP-03, HTTP-04, HTTP-05, CLNT-01, CLNT-02]

must_haves:
  truths:
    - "The whole phase compiles and passes on every feature combination the repo ships, not just --all-features"
    - "The milestone is still additive — cargo semver-checks requires no major bump against 2.17.0"
    - "make quality-gate passes, which is the repo's mandatory pre-commit and pre-push gate"
    - "The bounded fuzz run and a coverage measurement are part of the phase gate, not aspirational"
    - "A requirement is flipped to complete ONLY when the spec verdict is PUBLISHED-* (or a recorded exception was re-verified) AND the conformance manifest has no unmapped scenarios"
    - "HTTP-04's roadmap wording matches what shipped, and the SEP-2243 gap research surfaced is recorded rather than silently absorbed"
  artifacts:
    - path: ".planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md"
      provides: "recorded evidence of the feature/target build matrix, quality gate, coverage, fuzz and contract-first state"
      contains: "wasm32-unknown-unknown"
    - path: ".planning/ROADMAP.md"
      provides: "HTTP-04 reworded to the capability-gated opt-in that shipped, with the client half named"
      contains: "subscriptions/listen"
  key_links:
    - from: "113-SPEC-RECHECK.md ## Verdict"
      to: ".planning/REQUIREMENTS.md checkbox flips"
      via: "Task 3 refuses to mark requirements complete under an unresolved PENDING verdict"
      pattern: "PUBLISHED-CONFIRMED|PUBLISHED-DRIFT"
    - from: "113-CONFORMANCE-MANIFEST.md ## Unmapped"
      to: ".planning/REQUIREMENTS.md checkbox flips"
      via: "a non-empty Unmapped section blocks completion"
      pattern: "Unmapped"
---

<objective>
Close the phase: prove the feature/target build matrix (the repo's repeat false-green offender),
prove the milestone is still additive, run the mandatory quality gate INCLUDING coverage and the
bounded fuzz run, record the contract-first environment state, and reconcile the roadmap wording
with what actually shipped — flipping requirements to complete only when the evidence supports it.

Purpose: `cargo test --all-features` unifies the dev-dependency `pmcp` `full` feature with the
crate under test and has masked real feature-flag gaps twice in this repo's history. This phase
adds `ring` behind `streamable-http`, a `fuzzing` feature, and several cfg-gated modules, which is
exactly the shape that burns.

Cross-AI review raised four HIGH items here: contract-first work was scheduled AFTER every
implementation plan; the `../provable-contracts` checkout is not present in this workspace (VERIFIED
absent during replanning); PDMT/PMAT-proxy workflow tools were absent from the plans; and
requirements were to be marked complete even though the final-spec gate might not have run against
a published schema. Plan 01 now records the contract-first / PDMT / PMAT environment BEFORE any
implementation runs, and this plan's completion criteria are now evidence-gated.
Output: a recorded build matrix, a green quality gate with coverage and fuzz, and reconciled
planning documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-REVIEWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature/target build matrix and the additive-semver gate</name>
  <files>.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md</files>
  <read_first>
    - Cargo.toml (the `[features]` block, especially `streamable-http`, `http-client`, `full`, `fuzzing`, and the default feature set; plus the `ring`/`zeroize` entries plan 01 added)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (Pitfall 9 and Pitfall 10, and the "Environment Availability" row for `cargo semver-checks`)
    - .planning/phases/112-version-plumbing-spine/112-VERIFICATION.md (the Phase-112 semver baseline: 223 checks pass, no major bump required, cargo-semver-checks 0.49.0 / cargo-public-api 0.52.0 pinned)
    - src/server/request_state.rs, src/server/core.rs, src/client/subscriptions.rs (the cfg gates whose correctness this matrix proves)
  </read_first>
  <action>
Run and RECORD the full matrix. Use the ABSOLUTE rustup cargo path
(`$(rustup which cargo)`) for every row rather than a shell alias or tool proxy, so proxy output
corruption cannot silently alter results — the previous version of this plan said this in prose but
verified with a bare `cargo`, which review flagged (Codex Plan-12 MEDIUM). Capture the exact
command and its exit status for each row.

Matrix rows (each must exit 0):
1. `$(rustup which cargo) build --lib --no-default-features` — proves `request_state.rs`, the MRTR
   dispatch arms and the client subscription module are genuinely cfg-gated.
2. `$(rustup which cargo) build --lib --features streamable-http` — the phase's target deployment.
3. `$(rustup which cargo) build --lib --features full`.
4. `$(rustup which cargo) build --lib --target wasm32-unknown-unknown` — proves no `ring` and no
   MRTR server code leaked onto wasm (D-14). Also run it with the `--features` sets the repo
   supports on wasm.
5. `$(rustup which cargo) build --lib --features fuzzing` — proves the new fuzz-support feature
   compiles standalone and, separately, that `fuzzing` is NOT reachable from `full` or `default`
   (assert with `$(rustup which cargo) tree -p pmcp -e features --features full | grep -c fuzzing`
   being `0`).
6. **The dev-dependency-free all-features check, specified exactly (Codex Plan-12 MEDIUM).** The
   hazard is that `pmcp`'s own `[dev-dependencies]` entry on itself (with `full`) unifies features
   into the crate under test, so a `--lib`-only build that excludes dev-dependencies is the honest
   check. Run precisely:
   `$(rustup which cargo) build --lib --all-features --workspace` and, as the discriminating row,
   `$(rustup which cargo) rustc --lib --no-default-features --features streamable-http -- -D warnings`
   Record BOTH commands verbatim in the matrix file along with a one-line explanation of what each
   one would have caught, so the check is reproducible rather than described.
7. `$(rustup which cargo) build --examples --features full`.
8. `$(rustup which cargo) test --lib --features full`, `$(rustup which cargo) test --features full`
   (all integration tests), `make test-doc`.
9. `$(rustup which cargo) semver-checks check-release --baseline-version 2.17.0 -p pmcp` — must
   report NO required major bump and must NOT report `enum_variant_added`,
   `constructible_struct_adds_field`, `struct_marked_non_exhaustive`, or any other major lint.
   Compare the pass count against Phase 112's recorded 223 and note any delta.
10. `cargo public-api` diff against the 2.17.0 baseline — record the added public items and confirm
    every one is an ADDITION.

Write `113-FEATURE-MATRIX.md` with one row per command: the exact command, exit status, and for
rows 9-10 the relevant summary line. If any row fails, FIX the code (do not weaken the row) and
record what was fixed.

Explicitly enumerate in the record the new public API surface this phase adds, so the reviewer can
confirm additivity by reading rather than by trusting the tool:
- `src/types/mrtr.rs` public items ONLY (`InputRequest`, `InputRequestKind`, `InputRequests`,
  `InputResponse`, `InputResponses`, `InputRequiredResult`, `MrtrOutcome`, `MrtrSignal`,
  `MRTR_SIGNAL_META_KEY`) — the parsing helpers are `pub(crate)` by design;
- `src/types/subscriptions.rs` and its re-exports;
- `Transport::set_negotiated_protocol_version` / `supports_negotiated_protocol_version` (defaulted
  trait methods — additive);
- `ProtocolContext::with_mrtr_params`;
- the three `RequestHandlerExtra` MRTR accessors;
- `ReadResourceResult._meta`;
- the three error-code constants;
- `ClientBuilder::with_protocol_version` and `mrtr_round_limit`;
- `Client::server_discover`, `call_tool_mrtr`, `get_prompt_mrtr`, `read_resource_mrtr`,
  `subscriptions_listen`;
- `SubscriptionStream`;
- the `Error` marker constants and constructor/predicate trios (round-limit, input-required,
  retired-on-v2);
- the server builder's `with_request_state_key` / `with_request_state_previous_keys` /
  `with_request_state_ttl`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md` exists
    - The file contains the literal strings `--no-default-features`, `wasm32-unknown-unknown`, `semver-checks`, `public-api`, `rustup which cargo`, `fuzzing`
    - Every matrix row in the file records an exit status of 0
    - The file records BOTH dev-dep-free commands verbatim with an explanation of each
    - The file contains a list of the new public API items with each marked as an addition
    - `$(rustup which cargo) build --lib --no-default-features` exits 0
    - `$(rustup which cargo) build --lib --target wasm32-unknown-unknown` exits 0
    - `$(rustup which cargo) tree -p pmcp -e features --features full | grep -c fuzzing` is `0`
    - `$(rustup which cargo) semver-checks check-release --baseline-version 2.17.0 -p pmcp` exits 0 with no required major bump
  </acceptance_criteria>
  <verify>
    <automated>C=$(rustup which cargo) &amp;&amp; "$C" build --lib --no-default-features &amp;&amp; "$C" build --lib --target wasm32-unknown-unknown &amp;&amp; "$C" semver-checks check-release --baseline-version 2.17.0 -p pmcp &amp;&amp; test "$("$C" tree -p pmcp -e features --features full | grep -c fuzzing)" = "0"</automated>
  </verify>
  <done>Every feature and target combination the repo ships builds and tests green under the absolute rustup cargo, the two dev-dep-free commands are recorded verbatim, the `fuzzing` feature is proven unreachable from `full`, the additive-semver gate passes against the 2.17.0 baseline, and the new public surface is enumerated and recorded as additions.</done>
</task>

<task type="auto">
  <name>Task 2: Quality gate, complexity budget, coverage, fuzz run, and the contract-first record</name>
  <files>.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md</files>
  <read_first>
    - CLAUDE.md (the Toyota Way quality gates section, the CI PMAT cognitive-complexity gate at cog ≤ 25 with its hard cap of 50, the zero-SATD rule, the check-unwraps rule, the 80% coverage target, the ALWAYS fuzz/property/unit/example requirements, and the contract-first workflow)
    - Makefile (the `quality-gate` target and what it chains: fmt-check → lint → build → test-all → package gate → audit → unused-deps → check-todos → check-unwraps → validate-always → purity-check → comply; plus any existing coverage target)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md (the `## Contract-First Environment` section plan 01 recorded BEFORE implementation — this task consumes it rather than re-discovering it)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (the six refactor techniques P1-P6, if a complexity violation needs fixing)
    - src/server/request_state.rs, src/server/core.rs, src/server/streamable_http_server.rs, src/client/mod.rs (the four files most likely to carry a complexity hotspot from this phase)
  </read_first>
  <action>
1. **`make quality-gate`** — the repo's mandatory pre-commit and pre-push gate, and the only
   invocation that matches CI exactly. Run it and make it pass. Do NOT substitute individual
   `cargo fmt` / `cargo clippy` calls: the gate runs clippy with pedantic + nursery lint groups and
   workspace-wide `cargo fmt --all`, and a bare `cargo clippy -- -D warnings` is strictly weaker.

2. **Complexity budget.** Run
   `pmat analyze complexity --format json --max-cognitive 25 | jq '.violations[] | select(.path | startswith("src/"))'`
   and confirm this phase introduced no new violation. The likely hotspots by design are
   `request_state::verify`, `mrtr_ingest`, `mrtr_egress`, `own_reserved_result_fields`, the
   `sessions_active`/`resumability_active`/status-mapper composition, the listen-stream branch, and
   the client MRTR loop — every one was planned as a decomposed composition, so a violation means
   the decomposition was skipped and should be fixed by applying one of the P1-P6 techniques, not
   by adding an `#[allow]`. If a function is genuinely irreducible, the `// Why:`-annotated
   `#[allow(clippy::cognitive_complexity)]` template is the documented escape hatch and the hard cap
   is 50. `pmat` was VERIFIED present on PATH during replanning, so an "unavailable" note is not an
   acceptable outcome here.

3. **Zero SATD.** Confirm no `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` was introduced, and confirm
   `#[allow(dead_code)]` was REMOVED from `ResponseDisposition::InputRequired`.

4. **Coverage (Codex Plan-12 MEDIUM).** CLAUDE.md mandates an 80% coverage target and the previous
   version of this plan measured nothing. Run the repo's coverage target if the Makefile has one;
   otherwise run `cargo llvm-cov --lib --features full --summary-only` (or the repo's installed
   equivalent) and record the line-coverage percentage for the files this phase created or
   materially changed: `src/types/mrtr.rs`, `src/server/request_state.rs`,
   `src/types/subscriptions.rs`, `src/client/subscriptions.rs`, and the changed regions of
   `src/server/core.rs` / `src/server/streamable_http_server.rs` / `src/client/mod.rs`. Record the
   number. If a file is below 80%, either add the missing tests or record a specific,
   named justification — do not record a bare pass.

5. **The bounded fuzz run (Codex Plan-11/12 MEDIUM).** CLAUDE.md's ALWAYS requirements make fuzzing
   non-optional, and plan 03 only asserted `cargo fuzz build`. Run
   `cargo fuzz run fuzz_request_state -- -runs=20000` here as part of the phase gate and record the
   exit status and that no crash artifact was written. If the local toolchain cannot run
   `cargo fuzz`, record that explicitly with the exact error and flag it as a CI follow-up rather
   than silently dropping the requirement.

6. **Contract-first (Codex Plan-12 HIGH).** Read the `## Contract-First Environment` section plan 01
   recorded. `../provable-contracts` was VERIFIED ABSENT from this workspace during replanning, so
   the executable steps are: (a) copy that record into the matrix file so the phase's own evidence
   is self-contained; (b) run `pmat comply check --path .` and record its output — this is the
   in-repo compliance step `make quality-gate` already chains; (c) if the `../provable-contracts`
   checkout HAS appeared since plan 01 ran, update the contract YAML under
   `contracts/pmcp/` to cover the new v2 stateless-HTTP, MRTR and subscription behavior and re-run
   the check. Do not claim a contract was updated in a checkout that does not exist.
   Also copy plan 01's recorded `command -v pdmt` result: PDMT todo generation is a CLAUDE.md
   workflow tool, and if it is not installed the honest record is "unavailable in this environment;
   the plan tasks' `<acceptance_criteria>` + `<verify>` blocks carry the equivalent quality-gate and
   success-criteria structure". The same applies to the PMAT quality-proxy MCP server, which
   requires a running `pmat mcp-server --enable-quality-proxy` process that a plan executor cannot
   assume; the binding checks are `make quality-gate` locally and the PMAT job in CI.

Append `## Quality Gate`, `## Coverage`, `## Fuzz` and `## Contract-First` sections to
`113-FEATURE-MATRIX.md` recording each command, its exit status, and any fix applied.
  </action>
  <acceptance_criteria>
    - `113-FEATURE-MATRIX.md` contains `## Quality Gate`, `## Coverage`, `## Fuzz` and `## Contract-First` headings
    - The file records `make quality-gate` with exit status 0
    - The file records the `pmat analyze complexity` result with a violation count for `src/`
    - The file records a line-coverage percentage for each of the five named new/changed files
    - The file records `cargo fuzz run fuzz_request_state -- -runs=20000` with its exit status and a no-crash-artifact statement, or an explicit toolchain-unavailable note with the exact error
    - The file records the literal `ls -d ../provable-contracts/contracts/pmcp` result and the `pmat comply check --path .` output
    - `grep -rnE '\b(TODO|FIXME|XXX|HACK|PLACEHOLDER)\b' src/ --include='*.rs' | wc -l` is unchanged from the pre-phase baseline (record both numbers)
    - `grep -c '#\[allow(dead_code)\]' src/server/core.rs` reflects the removal of the `InputRequired` allow
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <verify>
    <automated>make quality-gate</automated>
  </verify>
  <done>The mandatory quality gate is green, no new cognitive-complexity violation or SATD was introduced, coverage is measured and recorded per changed file, the bounded fuzz run actually ran, the Phase-112 dead-code allow is gone, and the contract-first / PDMT / PMAT environment state is recorded from plan 01's pre-implementation checkpoint rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP and REQUIREMENTS with what shipped — evidence-gated</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 113 section: goal, requirements line, the five success criteria, and the Plans list)
    - .planning/REQUIREMENTS.md (the HTTP-01..05 and CLNT-01..02 entries and the traceability table)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md (the `## Verdict` and any `## Recorded Exception` — the gate on flipping requirements)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONFORMANCE-MANIFEST.md (the `## Unmapped` section — the second gate)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (the "HTTP-04 must be reworded" suggested text; Assumption A8 and Open Question 4 on SEP-2243 `x-mcp-header`; the `-32002` resolution)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md (D-11 and D-13 — the wording must reflect the owner's decision, not the research's first reading)
  </read_first>
  <action>
1. **Gate the completion flips on evidence (Codex Plan-12 HIGH).** Before flipping ANY checkbox,
   check two things and record both in the SUMMARY:
   - `113-SPEC-RECHECK.md`'s `## Verdict` is `PUBLISHED-CONFIRMED` or `PUBLISHED-DRIFT`. If it is
     `PENDING` with a `## Recorded Exception`, RE-RUN the schema check now: if the schema has since
     published, upgrade the verdict in place and re-verify that the three landed error-code values
     match; if it still has not published, do NOT flip the requirements — mark them
     `[~] pending final schema` (or the repo's equivalent partial marker), record why, and report
     the phase as blocked on publication rather than complete.
   - `113-CONFORMANCE-MANIFEST.md`'s `## Unmapped` section is EMPTY. A non-empty section means a
     pinned scenario has no local mirror, so the conformance claim is unproven and the affected
     requirements stay open.
   Only when BOTH gates pass do the flips below happen.

2. **Reword HTTP-04** in BOTH `.planning/REQUIREMENTS.md` and the Phase-113 success criteria in
   `.planning/ROADMAP.md`, to the capability-gated opt-in that actually shipped. Use the substance
   of RESEARCH's suggested text: on the v2 path `resources/subscribe`/`unsubscribe` and the HTTP GET
   stream endpoint are removed; v2 change notifications are delivered via a `subscriptions/listen`
   long-lived stream with `toolsListChanged`/`promptsListChanged`/`resourcesListChanged`/
   `resourceSubscriptions` opt-ins, `subscriptionId` tagging and an `acknowledged`-first frame; the
   stream is OPT-IN, pmcp's stateless enterprise default advertises no subscription-delivered
   capability, and answering `subscriptions/listen` with method-not-found in that configuration is
   conformant; a tripwire test enforces that advertising any subscription capability requires
   serving the stream. **Add the client half explicitly**: the pmcp `Client` exposes
   `subscriptions_listen` returning a typed notification stream, and the retired
   `resources/subscribe`/`unsubscribe` client methods fail fast on v2 — this is what plan 13 shipped
   and what makes HTTP-04's "v2 clients GET change notifications" wording true. Add one sentence
   recording D-11 (polling over Tasks remains pmcp's RECOMMENDED enterprise mechanism, documented as
   a pmcp extension, not a conformant substitute) and one recording the instance-local /
   sticky-routing constraint plan 10 documented.

3. **Mark the seven requirements complete** — flip HTTP-01..05 and CLNT-01..02 from `[ ]` to `[x]`
   in `.planning/REQUIREMENTS.md` and set their traceability-table Status to the repo's completed
   marker, matching how the VERS-* rows were marked after Phase 112. Subject to the Step-1 gates.

4. **Record the SEP-2243 `x-mcp-header` / `Mcp-Param-{Name}` gap** as a milestone-level requirements
   gap rather than absorbing it into Phase 113. Research found that clients MUST support
   `x-mcp-header` mirroring and that the header-mismatch validation table covers `Mcp-Param-*`, but
   no current requirement — VERS-05, HTTP-01..05, CLNT-01 — mentions it. Add it to
   `.planning/REQUIREMENTS.md` as a new, explicitly UNASSIGNED requirement with a note that it is
   closest to CLNT-01's header work, and add a line under the v2.5 milestone section of
   `.planning/ROADMAP.md` flagging it for phase assignment. Do not assign it to a phase yourself.

5. **Record the `-32002` resolution.** The draft schema states that `-32002` (resource not found,
   2025-11-25 and earlier) is reserved and replaced by `-32602`; the rename targets
   resource-not-found, NOT task-pending, so pmcp's proprietary `V1_TASK_PENDING` squat is unaffected
   and stays frozen. This closes the research pack's Open Verification Item that cross-cuts Phases
   112 and 114 — record the resolution where that item is tracked in `.planning/ROADMAP.md` (the
   final-spec checkpoint note) so Phase 114 does not re-litigate it.

6. **Update the Phase-113 Plans list** in `.planning/ROADMAP.md` to the THIRTEEN plans that shipped,
   grouped by the seven waves, matching the format used for Phase 112. Note in the entry for plan 13
   that it was added during the cross-AI review replan to close HTTP-04's client half.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` HTTP-04 text contains `opt-in`, `subscriptions/listen` AND `subscriptions_listen` (the client half)
    - `.planning/ROADMAP.md` Phase 113 success criterion 3 contains `opt-in` and names the client stream API
    - Subject to the Step-1 gates: `grep -c '^\- \[x\] \*\*HTTP-0' .planning/REQUIREMENTS.md` is `5` and `grep -c '^\- \[x\] \*\*CLNT-0[12]' .planning/REQUIREMENTS.md` is `2`
    - If either gate failed, the SUMMARY records which one and the affected requirements are NOT marked `[x]`
    - `.planning/REQUIREMENTS.md` contains `x-mcp-header` marked as unassigned
    - `.planning/ROADMAP.md` contains `x-mcp-header`
    - `.planning/ROADMAP.md` records that the `-32002` rename targets resource-not-found and leaves `V1_TASK_PENDING` frozen
    - `.planning/ROADMAP.md` Phase 113 Plans list names all thirteen plan files across seven waves
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'x-mcp-header' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'x-mcp-header' .planning/ROADMAP.md &amp;&amp; grep -q 'subscriptions_listen' .planning/REQUIREMENTS.md &amp;&amp; grep -c '113-13-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>
  <done>Requirements are flipped only when the spec verdict is published and the conformance manifest has no unmapped scenarios; HTTP-04's wording matches the capability-gated opt-in AND names the client stream API that plan 13 shipped; the SEP-2243 gap is recorded as unassigned; the -32002 resolution is written down for Phase 114; and the Plans list names all thirteen plans across seven waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build configuration → shipped artifact | A feature combination that is never built is a combination that is never verified |
| planning documents → future phases | An unrecorded gap or an unreconciled requirement becomes a silent omission downstream |
| evidence → completion claim | A checkbox flipped without evidence is a false assurance that propagates to Phase 118 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-38 | Tampering | crypto or MRTR code leaking onto a target where it is unsafe or unbuildable | mitigate | Explicit `--no-default-features` and `wasm32-unknown-unknown` build rows prove `ring` and the MRTR server path are genuinely cfg-gated (D-14), recorded with exit statuses rather than asserted |
| T-113-39 | Tampering | feature-unification false green hiding a real gap | mitigate | Two dev-dependency-free commands recorded VERBATIM with an explanation of what each would catch, run under the absolute rustup cargo (this exact hazard burned Phase 109) |
| T-113-40 | Repudiation | an accidental major-version break shipping as a minor | mitigate | `cargo semver-checks` against the 2.17.0 baseline plus a `cargo public-api` diff, with the new public surface enumerated by hand so additivity is confirmable by reading |
| T-113-41 | Information Disclosure | a security-relevant complexity hotspot going unreviewed | mitigate | `pmat analyze complexity` over `src/` with the seven expected hotspots named; a violation must be fixed by decomposition (P1-P6), not by an `#[allow]`; `pmat` is verified present so "unavailable" is not an acceptable outcome |
| T-113-42 | Repudiation | a known spec gap silently absorbed into a phase that did not implement it | mitigate | SEP-2243 `x-mcp-header` is recorded as an explicitly UNASSIGNED milestone requirement rather than folded into CLNT-01 |
| T-113-69 | Repudiation | requirements marked complete on unproven conformance or a pre-final schema | mitigate | Task 3 Step 1 gates every checkbox flip on a `PUBLISHED-*` verdict AND an empty `## Unmapped` manifest section, with a partial marker and a blocked-phase report as the documented alternative |
| T-113-70 | Repudiation | the mandated fuzz and coverage requirements being asserted rather than executed | mitigate | The 20k-run fuzz invocation and a per-file coverage measurement are both recorded rows of the phase gate with exit statuses and numbers |
| T-113-71 | Repudiation | claiming contract-first compliance against a checkout that does not exist | mitigate | Plan 01 records the environment BEFORE implementation; this task copies that record verbatim and runs the in-repo `pmat comply check` instead of fabricating an external contract update |
</threat_model>

<verification>
- Every row of `113-FEATURE-MATRIX.md` records exit status 0
- `make quality-gate` exits 0
- `$(rustup which cargo) semver-checks check-release --baseline-version 2.17.0 -p pmcp` requires no major bump
- `$(rustup which cargo) build --lib --no-default-features` and `--target wasm32-unknown-unknown` succeed
- `cargo fuzz run fuzz_request_state -- -runs=20000` ran and is recorded
- Coverage is recorded per new/changed file
- The full integration suite passes: `v2_required_headers`, `v2_stateless_http`, `v2_client`, `v2_mrtr_ingress`, `v2_mrtr`, `v2_subscriptions`, `v2_subscriptions_client`, `server_subscriptions`, `common_harness_smoke`
- `.planning/REQUIREMENTS.md` shows HTTP-01..05 and CLNT-01..02 complete, or explicitly records which gate blocked them
</verification>

<success_criteria>
- The phase builds and tests green across every feature and target combination the repo ships, under the absolute rustup cargo, with the dev-dep-free commands recorded verbatim
- The milestone is provably still additive against the 2.17.0 baseline
- The mandatory quality gate passes with no new complexity violation and no SATD, and coverage plus the bounded fuzz run are executed and recorded
- The contract-first / PDMT / PMAT environment state is recorded honestly from plan 01's pre-implementation checkpoint
- HTTP-04's wording matches what shipped including the client half, the SEP-2243 gap is recorded as unassigned, and the -32002 question is closed for Phase 114
- No requirement is marked complete without a published spec verdict and a fully mapped conformance manifest
</success_criteria>

<output>
Create `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-12-SUMMARY.md` when done
</output>
</content>
