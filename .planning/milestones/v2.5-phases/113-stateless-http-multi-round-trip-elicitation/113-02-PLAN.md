---
phase: 113-stateless-http-multi-round-trip-elicitation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/mrtr.rs
  - src/types/mod.rs
  - src/types/elicitation.rs
  - tests/common/mod.rs
  - tests/common/v2.rs
  - tests/common_harness_smoke.rs
autonomous: true
requirements: [HTTP-02, HTTP-03, CLNT-02]

must_haves:
  truths:
    - "The MRTR wire shape (inputRequests/inputResponses/requestState) is expressible as typed Rust and round-trips to the exact spec spelling"
    - "Parsing distinguishes ABSENT from INVALID: a malformed, oversized or wrong-shaped requestState is never silently treated as absent"
    - "Splicing REMOVES stale MRTR keys before writing current ones, so no round's data can leak into a later round"
    - "There is exactly ONE table naming the three MRTR-eligible methods and where each carries its logical name"
    - "A spec-shaped form elicitation that omits `mode` deserializes into ElicitRequestParams::Form"
    - "The client-facing `input_required` outcome is expressible as a public type, so a caller can receive an unfulfilled input_required result instead of a silently-empty success"
    - "Integration tests across the phase build a valid v2 request from one shared helper that always carries clientCapabilities"
  artifacts:
    - path: "src/types/mrtr.rs"
      provides: "MRTR wire types, canonical method table, params splice/extract, salient-param digest, InputRequiredResult + MrtrOutcome"
      min_lines: 260
      contains: "MRTR_ELIGIBLE_METHODS"
    - path: "src/types/mrtr.rs"
      provides: "Mcp-Name base64 sentinel encode/decode pair shared by client and server"
      contains: "=?base64?"
    - path: "src/types/mrtr.rs"
      provides: "typed parse outcome distinguishing absent from invalid"
      contains: "MrtrParseError"
    - path: "tests/common/v2.rs"
      provides: "shared v2 request/response harness for every tests/v2_*.rs file"
      min_lines: 100
    - path: "src/types/elicitation.rs"
      provides: "mode-optional Deserialize for ElicitRequestParams"
      contains: "impl<'de> Deserialize<'de> for ElicitRequestParams"
  key_links:
    - from: "src/types/mod.rs"
      to: "src/types/mrtr.rs"
      via: "pub mod mrtr + a NARROW re-export of only the authoring/result types"
      pattern: "pub mod mrtr"
    - from: "src/types/mrtr.rs"
      to: "src/types/elicitation.rs"
      via: "InputRequest::Elicitation carries ElicitRequestParams (D-10 one handler-facing type)"
      pattern: "ElicitRequestParams"
---

<objective>
Create the MRTR protocol-type layer: the wire types, the ONE canonical table of MRTR-eligible
methods and their logical-name locations, a params extract/splice pair that distinguishes
absent from invalid and clears stale keys, the AAD salient-param digest, the public
`InputRequiredResult`/`MrtrOutcome` client-facing outcome types, and the `ElicitRequestParams`
`mode`-optional deserialization fix. Also lift the Phase-112 HTTP test harness into
`tests/common/` so every Phase-113 integration test file builds valid v2 requests from one place.

Purpose: D-10 locks a single handler-facing elicitation type with the v2 wire shape adapted
internally. This plan is that adapter, and it is the only file the rest of the phase has to
trust for wire spelling. Cross-AI review raised three HIGH parsing defects here (absent-vs-
invalid conflation, stale-key leakage, an untagged response guess) and one architectural gap
(no public type could carry an unfulfilled `input_required` back to a caller) — all four are
fixed in this revision.
Output: `src/types/mrtr.rs`, a fixed `src/types/elicitation.rs`, and `tests/common/v2.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-PATTERNS.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-REVIEWS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; no exploration required. -->

Already in the tree (src/types/elicitation.rs):
  pub enum ElicitRequestParams { Form { message, requested_schema }, Url { message, elicitation_id, url } }   // #[serde(tag = "mode")]
  pub struct ElicitResult { pub action: ElicitAction, pub content: Option<HashMap<String, Value>> }

Already in the tree (src/types/protocol/mod.rs:544) — the method-tagged shape to mirror:
  #[serde(tag = "method", content = "params", rename_all = "camelCase")]
  pub enum ServerRequest {
    #[serde(rename = "sampling/createMessage")] CreateMessage(Box<CreateMessageParams>),
    #[serde(rename = "roots/list")] ListRoots,
    #[serde(rename = "elicitation/create")] ElicitationCreate(Box<ElicitRequestParams>),
  }

Already in the tree (src/server/streamable_http_server.rs:477) — the table this plan takes ownership of:
  fn logical_name_key(method: &str) -> Option<&'static str>   // "tools/call"|"prompts/get" => "name"; "resources/read" => "uri"
  fn is_name_bearing_method(method: &str) -> bool             // logical_name_key(..).is_some()
  fn require_three_headers(headers) -> Result<(String, String), &'static str>
    // VERIFIED during replanning: requires Mcp-Method AND Mcp-Name to be PRESENT on EVERY v2
    // request. cross_check_name() returns Ok for non-name-bearing methods (presence-only).
    // This is Phase-112 D-05, locked. => Mcp-Name is ALWAYS present; its value is the EMPTY
    // STRING for name-less methods. encode/decode_header_value MUST round-trip "" .

VERIFIED SEMVER CONSTRAINTS (do not violate):
  CallToolRequest      -> #[non_exhaustive]      (fields may be added)
  CallToolResult       -> #[non_exhaustive], and `content` has #[serde(default)]
                          => an input_required result DESERIALIZES into an EMPTY CallToolResult,
                             silently discarding inputRequests/requestState/resultType.
                             This is why InputRequiredResult/MrtrOutcome must exist.
  GetPromptResult      -> #[non_exhaustive]  (already has `_meta: Option<Value>`)
  ReadResourceResult   -> #[non_exhaustive]; `contents` has NO serde default and a custom
                          deserializer => an input_required result FAILS to deserialize.
  GetPromptRequest     -> NOT #[non_exhaustive]  (adding a public field is a MAJOR break)
  ReadResourceRequest  -> NOT #[non_exhaustive]  (adding a public field is a MAJOR break)
  pmcp::Error (enum)   -> NOT #[non_exhaustive]  (adding a variant is a MAJOR break)
=> `inputResponses`/`requestState` MUST NOT become typed fields on the three request structs.
   They ride as raw `params` JSON, spliced on emit and extracted on ingress by this module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/types/mrtr.rs — wire types, method table, fail-loud extract/splice, AAD digest, client outcome types</name>
  <files>src/types/mrtr.rs, src/types/mod.rs</files>
  <read_first>
    - src/types/elicitation.rs (the ElicitRequestParams/ElicitResult shapes and the module-doc + test style to copy)
    - src/types/protocol/mod.rs (lines 540-570: the `#[serde(tag = "method", content = "params")]` ServerRequest enum and the `#[serde(untagged)]` Request enum)
    - src/types/mod.rs (lines 1-60: the alphabetical `pub mod` list and the flat re-export block)
    - src/server/streamable_http_server.rs (lines 445-560: `require_three_headers`, `cross_check_name`, `logical_name_key`, `is_name_bearing_method`, `extract_body_method_and_name` — the table being lifted and the presence-always header contract)
    - src/types/tools.rs (the `CallToolResult` definition, to see that `content` has `#[serde(default)]` and therefore swallows an input_required result)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (section "The MRTR Wire Contract" — the authoritative field locations)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-PATTERNS.md (section "src/types/mrtr.rs (NEW)")
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-REVIEWS.md (Codex Plan 02 concerns — the three HIGH parsing defects this task fixes)
  </read_first>
  <behavior>
    - `serde_json::to_value(InputRequest::Elicitation(..))` produces `{"method":"elicitation/create","params":{...}}`
    - `InputRequest` deserializes all three of `elicitation/create`, `sampling/createMessage`, `roots/list`; any other method value is an Err
    - `InputResponse::decode_for(InputRequestKind::Elicitation, value)` decodes an `ElicitResult` and REJECTS a `CreateMessageResult`-shaped value
    - `InputResponse::decode_for(InputRequestKind::Sampling, value)` decodes a `CreateMessageResult` and rejects an `ElicitResult`-shaped value
    - `InputResponse::decode_for(InputRequestKind::Roots, value)` decodes a `ListRootsResult`
    - `mrtr_eligible(m)` is true for exactly `tools/call`, `prompts/get`, `resources/read` and false for `tools/list`, `server/discover`, `completion/complete`, `subscriptions/listen`, `initialize`
    - `logical_name_key("tools/call") == Some("name")`, `logical_name_key("prompts/get") == Some("name")`, `logical_name_key("resources/read") == Some("uri")`, `logical_name_key("tools/list") == None`
    - `encode_header_value("search")` returns `"search"` unchanged; `encode_header_value("")` returns `""` unchanged; `encode_header_value("日本語")` returns a value starting `=?base64?` and ending `?=`
    - `decode_header_value(encode_header_value(v)) == Some(v)` for ASCII, non-ASCII, and EMPTY `v`
    - `decode_header_value("=?base64?not-valid-b64?=")` is `None` and does not panic
    - `splice_mrtr_params` writes `inputResponses` and `requestState` as TOP-LEVEL siblings of `name`/`arguments`/`uri`, never inside `_meta` and never inside `arguments`
    - `splice_mrtr_params` with `MrtrRequestParams::default()` REMOVES any pre-existing `inputResponses` and `requestState` keys (stale-round data cannot survive)
    - `extract_mrtr_params` on a params object with no MRTR keys returns `Ok(MrtrRequestParams::default())`
    - `extract_mrtr_params` on a non-object params value returns `Ok(MrtrRequestParams::default())`
    - `extract_mrtr_params` with a `requestState` that is not a string returns `Err(MrtrParseError::RequestStateNotAString)`
    - `extract_mrtr_params` with a `requestState` longer than `MAX_REQUEST_STATE_LEN` returns `Err(MrtrParseError::RequestStateTooLong { len, max })`
    - `extract_mrtr_params` with an `inputResponses` that is not an object returns `Err(MrtrParseError::InputResponsesNotAnObject)`
    - `extract_mrtr_params` with more than `MAX_INPUT_RESPONSES` entries returns `Err(MrtrParseError::TooManyInputResponses { .. })`
    - `extract_mrtr_params` with a single entry whose serialized size exceeds `MAX_INPUT_RESPONSE_BYTES`, or with a total exceeding `MAX_INPUT_RESPONSES_TOTAL_BYTES`, or with nesting deeper than `MAX_INPUT_RESPONSE_DEPTH`, returns the corresponding `MrtrParseError`
    - `salient_param_digest` is stable across key-insertion-order permutations of the same logical params, and differs when `name`/`uri`/`arguments` differ
    - `salient_param_digest` is UNCHANGED when `_meta`, `inputResponses`, `requestState` are added/removed from the params object
    - `serde_json::from_value::<InputRequiredResult>(json!({"resultType":"input_required","inputRequests":{},"requestState":"abc"}))` succeeds and exposes both fields
    - `MrtrOutcome::<CallToolResult>::Complete(..)` and `MrtrOutcome::InputRequired(..)` both construct and pattern-match
  </behavior>
  <action>
Create `src/types/mrtr.rs` — the single place the 2026-07-28 MRTR wire shape is assembled and
parsed (D-10). Follow the module-doc + `#[serde(rename_all = "camelCase")]` +
`skip_serializing_if` + `mod tests` shape of `src/types/elicitation.rs`.

**Visibility discipline (addresses Codex Plan-02 MEDIUM).** D-10 describes an INTERNAL
adapter, so the parsing/plumbing helpers must NOT become public API surface. Exactly these
items are `pub` and re-exported: `InputRequest`, `InputRequestKind`, `InputRequests`,
`InputResponse`, `InputResponses`, `InputRequiredResult`, `MrtrOutcome`, `MrtrSignal`.
Everything else — `extract_mrtr_params`, `splice_mrtr_params`, `salient_param_digest`,
`logical_name_key`, `logical_name_of`, `encode_header_value`, `decode_header_value`,
`MrtrRequestParams`, `MrtrParseError`, `MRTR_ELIGIBLE_METHODS`, `mrtr_eligible`, and every
`MAX_*` bound — is `pub(crate)`. All consumers (`src/server/streamable_http_server.rs`,
`src/shared/streamable_http.rs`, `src/server/core.rs`, `src/client/mod.rs`) are in this crate,
so `pub(crate)` reaches all of them. This shrinks the `cargo public-api` delta plan 12 audits.

Items to define:

1. `InputRequestKind` — a small `pub` enum `{ Elicitation, Sampling, Roots }` with
   `pub fn wire_method(self) -> &'static str` returning `"elicitation/create"` /
   `"sampling/createMessage"` / `"roots/list"`. This is what makes response decoding
   kind-directed instead of a guess.
2. `InputRequest` — a `#[serde(tag = "method", content = "params", rename_all = "camelCase")]`
   enum with exactly three variants mirroring `ServerRequest`:
   `Elicitation(Box<ElicitRequestParams>)` renamed `elicitation/create`,
   `Sampling(Box<CreateMessageParams>)` renamed `sampling/createMessage`,
   `ListRoots` renamed `roots/list`. Reuse the EXISTING
   `crate::types::elicitation::ElicitRequestParams` and
   `crate::types::sampling::CreateMessageParams` — do not define parallel types (D-10).
   Add `pub fn kind(&self) -> InputRequestKind`.
3. `InputResponse` — an enum over the existing `ElicitResult`, `CreateMessageResult`,
   `ListRootsResult`. Do NOT rely on `#[serde(untagged)]` for decoding: the three shapes
   overlap and a future field could silently reclassify a response (Codex Plan-02 MEDIUM).
   Provide `pub fn decode_for(kind: InputRequestKind, value: serde_json::Value) -> Result<Self, serde_json::Error>`
   which dispatches on the kind of the ORIGINATING `InputRequest` and deserializes into
   exactly that variant. Keep `Serialize` derived (emission is unambiguous). If a
   `Deserialize` impl is needed for the untagged wire form (server ingress reading a client's
   `inputResponses` before it knows the kind), gate it behind an explicitly documented
   "best-effort, server-ingress-only" constructor `try_from_value_untagged` and state in its
   doc that `decode_for` is the correct path wherever the kind is known.
4. `InputRequests` / `InputResponses` — `BTreeMap<String, InputRequest>` /
   `BTreeMap<String, InputResponse>` type aliases (BTreeMap, not HashMap, so key order on the
   wire is deterministic for tests and digests, and duplicate keys are impossible).
5. `InputRequiredResult` — the PUBLIC client-facing carrier for an `input_required` result:
   `{ pub result_type: String, pub input_requests: Option<InputRequests>, pub request_state: Option<String>, pub meta: Option<serde_json::Value>, pub raw: serde_json::Value }`
   with camelCase serde on the wire fields and `raw` holding the verbatim result object so a
   caller loses nothing. Add `pub fn is_input_required(&self) -> bool`. Rustdoc that this
   exists because `CallToolResult::content` carries `#[serde(default)]` and would otherwise
   deserialize an `input_required` result into a silently EMPTY success.
6. `MrtrOutcome<T>` — `pub enum MrtrOutcome<T> { Complete(T), InputRequired(InputRequiredResult) }`
   with `pub fn complete(self) -> Option<T>` and `pub fn input_required(self) -> Option<InputRequiredResult>`.
   This is the additive public return type plan 07's `*_mrtr` methods use.
7. `MRTR_ELIGIBLE_METHODS: [&str; 3] = ["tools/call", "prompts/get", "resources/read"]` plus
   `pub(crate) fn mrtr_eligible(method: &str) -> bool` derived from it. Document that the spec
   forbids `input_required` on any other method and that this constant is the ONE source of
   truth for that rule.
8. `pub(crate) fn logical_name_key(method: &str) -> Option<&'static str>` — moved here verbatim
   from `src/server/streamable_http_server.rs:477` (same match arms, same doc contract), plus
   `pub(crate) fn logical_name_of(method: &str, params: &serde_json::Value) -> Option<String>`.
   Alongside them, the `Mcp-Name` VALUE ENCODING pair:
   - `pub(crate) fn encode_header_value(value: &str) -> String` — returns `value` unchanged
     when EVERY byte is in the allowed set, otherwise returns the sentinel form
     `=?base64?<b64>?=` where `<b64>` is standard base64 WITH padding of the UTF-8 bytes.
     The allowed set is exactly: printable US-ASCII `0x20..=0x7E`, EXCLUDING the byte `0x7F`
     (DEL, not in range anyway), EXCLUDING `"` (0x22), `,` (0x2C), `;` (0x3B), and `\` (0x5C)
     — the RFC 9110 field-value delimiters an intermediary may re-split on. An EMPTY string is
     allowed and returned unchanged (this is the name-less-method case; see the header rule
     below). The `=?base64?` prefix and `?=` suffix markers are case-sensitive literals; a
     value that itself begins with `=?base64?` MUST be sentinel-encoded even if otherwise
     safe, so decoding is unambiguous.
   - `pub(crate) fn decode_header_value(raw: &str) -> Option<String>` — the exact inverse:
     recognises the markers, base64-decodes, returns UTF-8; returns the input verbatim for a
     non-sentinel value; returns `None` for a malformed sentinel or invalid UTF-8. Bound the
     decoded length at `MAX_HEADER_VALUE_LEN` (reuse `streamable_http_server.rs`'s
     `MAX_V2_HEADER_VALUE_LEN` value) and return `None` past it.
   **THE HEADER RULE (locked, cross-plan):** `Mcp-Name` is PRESENT on EVERY v2 request. For a
   name-less method the value is the EMPTY STRING. The server's `require_three_headers`
   already demands presence (Phase-112 D-05) while `cross_check_name` skips value comparison
   for non-name-bearing methods. Put that sentence in the module doc and cite
   `113-SPEC-RECHECK.md`'s `## Mcp-Name Header Rule` section as its provenance. Plans 04 and
   05 both implement exactly this rule.
9. `pub(crate) struct MrtrRequestParams { pub input_responses: Option<InputResponses>, pub request_state: Option<String> }`
   deriving `Default`, plus a FAIL-LOUD parse pair (this replaces the previous tolerant
   `-> MrtrRequestParams` signature; addresses Codex Plan-02 HIGH #1 and Plan-06 HIGH #3):
   - `pub(crate) enum MrtrParseError { RequestStateNotAString, RequestStateTooLong { len: usize, max: usize }, InputResponsesNotAnObject, TooManyInputResponses { count: usize, max: usize }, InputResponseTooLarge { key: String, bytes: usize, max: usize }, InputResponsesTotalTooLarge { bytes: usize, max: usize }, InputResponseTooDeep { key: String, depth: usize, max: usize } }`
     implementing `std::fmt::Display` with messages that name the bound but NOT the offending
     content (no echo of attacker input into logs).
   - `pub(crate) fn extract_mrtr_params(params: &serde_json::Value) -> Result<MrtrRequestParams, MrtrParseError>` —
     a non-object params value or an absent key yields `Ok(Default::default())`; a PRESENT but
     wrong-shaped, oversized, over-deep or over-count value yields `Err`. Never panics.
   - `pub(crate) fn splice_mrtr_params(params: &mut serde_json::Value, mrtr: &MrtrRequestParams)` —
     FIRST removes both `inputResponses` and `requestState` from the params object
     unconditionally, THEN inserts each that is `Some`. This is what guarantees a later round
     cannot carry a previous round's data (Codex Plan-02 HIGH #2). No-op on a non-object value.
   Keep the pair adjacent and cross-reference them: one splices, one extracts, and the key
   spelling exists in exactly these two places.
   Define the bounds as `pub(crate) const`s with the values `MAX_REQUEST_STATE_LEN = 8192`,
   `MAX_INPUT_RESPONSES = 64`, `MAX_INPUT_RESPONSE_BYTES = 65_536`,
   `MAX_INPUT_RESPONSES_TOTAL_BYTES = 262_144`, `MAX_INPUT_RESPONSE_DEPTH = 32`, each with a
   doc comment naming the DoS surface it bounds (T-113-14).
10. `pub(crate) fn salient_param_digest(method: &str, params: &serde_json::Value) -> [u8; 32]` —
   the originating-request binding for the AEAD AAD (spec replay-prevention clause 5c,
   RESEARCH Pitfall 2). Build a canonical value from a per-method WHITELIST — never a
   blacklist: `tools/call` and `prompts/get` take `name` and `arguments`; `resources/read`
   takes `uri`; any other method yields the digest of an empty object. Canonicalize by
   recursively re-serializing objects with keys sorted lexicographically (the crate has
   serde_json `preserve_order` enabled, so insertion order would otherwise leak into the
   digest), then `sha2::Sha256` over the canonical bytes. Document that `_meta`,
   `inputResponses`, `requestState` are excluded by construction because they are not on the
   whitelist.
11. `pub const MRTR_SIGNAL_META_KEY: &str = "dev.pmcp/mrtr";` — the reserved result-`_meta` key
   a handler uses to signal "I need more input". Document that this key is pmcp-internal, is
   STRIPPED by the dispatch layer on EVERY path before serialization (v1 included — see plan
   09), and never appears on the wire. Define
   `pub struct MrtrSignal { pub input_requests: InputRequests, pub continuation: serde_json::Value }`
   with camelCase serde. It is `pub` because it is the handler AUTHORING surface.

Register the module in `src/types/mod.rs`: add `pub mod mrtr;` in the alphabetical list and a
flat re-export of ONLY `InputRequest, InputRequestKind, InputRequests, InputResponse,
InputResponses, InputRequiredResult, MrtrOutcome, MrtrSignal` alongside the existing
`pub use elicitation::{...}` block.

Write `#[cfg(test)] mod tests` covering every bullet in `<behavior>`, asserting the literal
wire key spellings (`json["inputRequests"]`, `json["inputResponses"]`, `json["requestState"]`,
`json["method"] == "elicitation/create"`) rather than only round-tripping pmcp's own output.

Add `proptest` tests asserting: `extract_mrtr_params` never panics over arbitrary JSON values;
`splice` then `extract` is an identity for arbitrary `requestState` strings under the length
bound; and `splice(params, &Default::default())` leaves NO `inputResponses`/`requestState` key
for arbitrary starting params objects.
  </action>
  <acceptance_criteria>
    - `src/types/mrtr.rs` exists and contains `MRTR_ELIGIBLE_METHODS`, `salient_param_digest`, `splice_mrtr_params`, `extract_mrtr_params`, `logical_name_key`
    - `src/types/mrtr.rs` contains `MRTR_SIGNAL_META_KEY`, `MAX_REQUEST_STATE_LEN`, `MAX_INPUT_RESPONSES`, `MAX_INPUT_RESPONSE_BYTES`, `MAX_INPUT_RESPONSES_TOTAL_BYTES`, `MAX_INPUT_RESPONSE_DEPTH`
    - `src/types/mrtr.rs` contains `pub enum MrtrOutcome`, `pub struct InputRequiredResult`, `pub enum InputRequestKind`, `fn decode_for`
    - `src/types/mrtr.rs` contains `enum MrtrParseError` and `extract_mrtr_params` returns `Result<`
    - `src/types/mrtr.rs` contains `encode_header_value` and `decode_header_value` and the literal sentinel marker `=?base64?`
    - `grep -c 'pub fn extract_mrtr_params\|pub fn splice_mrtr_params\|pub fn salient_param_digest' src/types/mrtr.rs` is `0` (they are `pub(crate)`, not `pub`)
    - `src/types/mod.rs` contains `pub mod mrtr;`
    - `grep -c 'input_responses\|request_state' src/types/prompts.rs src/types/resources.rs` reports `0` for both files (no typed fields added — semver constraint)
    - A test asserting `decode_header_value(encode_header_value("")) == Some("".to_string())` exists and passes
    - A test asserting `splice_mrtr_params` with a default `MrtrRequestParams` removes pre-existing keys exists and passes
    - A test asserting a non-string `requestState` yields `Err(MrtrParseError::RequestStateNotAString)` exists and passes
    - `cargo test --lib --features full -- types::mrtr` exits 0
    - `cargo build --lib --target wasm32-unknown-unknown` exits 0 (this module must stay wasm-clean; it holds no crypto)
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib --features full -- types::mrtr &amp;&amp; cargo build --lib --target wasm32-unknown-unknown</automated>
  </verify>
  <done>`src/types/mrtr.rs` is the single source of truth for the MRTR wire spelling, the eligible-method set, the logical-name table, the header-value codec, and the AAD digest; parsing distinguishes absent from invalid and enforces DoS bounds; splicing clears stale keys; the public surface is exactly the authoring/result/outcome types; every `<behavior>` bullet has a passing test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make `ElicitRequestParams` accept a spec-shaped form elicitation with no `mode`</name>
  <files>src/types/elicitation.rs</files>
  <read_first>
    - src/types/elicitation.rs (whole file — the `#[serde(tag = "mode")]` enum at ~line 25 and the existing round-trip tests at ~107-130)
    - src/types/resources.rs (lines 345-360: the `deserialize_with = "..."` custom-serde-module precedent)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (Pitfall 4)
  </read_first>
  <behavior>
    - `serde_json::from_value::<ElicitRequestParams>(json!({"message":"What is your name?","requestedSchema":{"type":"object"}}))` yields `Form` (no `mode` key present)
    - `from_value` with `"mode":"form"` still yields `Form` (unchanged)
    - `from_value` with `"mode":"url"` still yields `Url` and REQUIRES `elicitationId` + `url`
    - `from_value` with `"mode":"bogus"` is an `Err`
    - `serde_json::to_value(ElicitRequestParams::Form{..})["mode"] == "form"` — serialization is UNCHANGED (v1 byte compatibility)
  </behavior>
  <action>
The v2 schema makes `ElicitRequestFormParams.mode` OPTIONAL (implicit form) while
`ElicitRequestURLParams.mode: "url"` stays required. The current `#[serde(tag = "mode")]`
derive rejects a conforming server's form elicitation with "missing field `mode`", which
breaks the client half of CLNT-02 against every conformant server.

Replace ONLY the derived `Deserialize` on `ElicitRequestParams` with a hand-written
`impl<'de> Deserialize<'de> for ElicitRequestParams` that:
- deserializes into a `serde_json::Value` (or an internal helper struct), reads the optional
  `mode` field, and treats an absent `mode` as `"form"`;
- dispatches to internal `#[derive(Deserialize)] #[serde(rename_all = "camelCase")]` helper
  structs `FormShape { message, requested_schema }` and
  `UrlShape { message, elicitation_id, url }` so field-level camelCase handling and
  missing-required-field errors are still serde-generated, not hand-rolled;
- returns a `serde::de::Error::unknown_variant`-style error for any `mode` value other than
  `"form"` / `"url"`.

Leave the derived `Serialize` exactly as it is — the enum must keep emitting `"mode":"form"`
on the wire so v1 bytes are unchanged (D-10). Do NOT change the public enum shape, its
variants, or its field names: this is a serde-only change and must stay semver-additive.

Extend the existing `mod tests` with one test per `<behavior>` bullet, including an explicit
assertion that the serialized form still carries `"mode":"form"`.
  </action>
  <acceptance_criteria>
    - `src/types/elicitation.rs` contains `impl<'de> Deserialize<'de> for ElicitRequestParams`
    - `grep -c '#\[serde(tag = "mode")\]' src/types/elicitation.rs` is `0`
    - A test asserting deserialization of a `mode`-less form params object exists and passes
    - A test asserting `to_value(Form{..})["mode"] == "form"` exists and passes
    - `cargo test --lib --features full -- elicitation` exits 0
    - `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp` reports no required major bump
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib --features full -- elicitation</automated>
  </verify>
  <done>A conformant server's `mode`-less form elicitation deserializes into `ElicitRequestParams::Form`; url mode still requires its fields; serialization still emits `"mode":"form"`; the public type is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Lift the v2 HTTP test harness into tests/common/</name>
  <files>tests/common/mod.rs, tests/common/v2.rs, tests/common_harness_smoke.rs</files>
  <read_first>
    - tests/v2_required_headers.rs (whole file — especially the file header cfg gate at :1-19, `build_server` at :125-143, `spawn` at :238-244, `Resp` + `post` at :246-293, `call_body` at :295-319, and the trivial `SearchTool`/`GreetingPrompt`/`GreetingResource` handlers at :44-98)
    - src/types/protocol/context.rs (lines 110-200: the three reserved `_meta` keys, so the harness emits exactly what the server resolver reads)
    - src/types/mrtr.rs (the `encode_header_value` codec and the Mcp-Name presence rule from Task 1)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-PATTERNS.md (section "tests/v2_*.rs (NEW — integration tests)")
  </read_first>
  <action>
Create `tests/common/mod.rs` (a `pub mod v2;` re-export shim) and `tests/common/v2.rs` holding
the shared live-HTTP v2 harness that plans 04, 06, 08, 10, 11 and 13 all consume. Files under
`tests/common/` are not compiled as their own test binaries, so this is the correct home.

Carry over the file-header doctrine from `tests/v2_required_headers.rs` verbatim: the
`#![cfg(all(feature = "streamable-http", feature = "http-client", not(target_arch = "wasm32")))]`
gate, and the reliability rules (ephemeral port `127.0.0.1:0` with the address read back from
`start()`, readiness guaranteed by `start()` binding before returning, shutdown via
`JoinHandle::abort()`).

Export from `tests/common/v2.rs`:
- `pub const V2: &str` = the 2026-07-28 protocol version string, sourced from
  `pmcp::types::protocol::PROTOCOL_VERSION_2026_07_28` rather than re-typed.
- `pub struct Resp { pub status: u16, pub mcp_method: Option<String>, pub mcp_name: Option<String>, pub mcp_version: Option<String>, pub mcp_session_id: Option<String>, pub content_type: Option<String>, pub body: serde_json::Value, pub raw: String }`
  — `mcp_session_id` and `content_type` are ADDITIONS over the Phase-112 shape, needed by
  HTTP-01 (assert absent) and HTTP-04 (assert `text/event-stream`).
- `pub async fn post(addr, extra_headers, body) -> Resp` — lifted from
  `v2_required_headers.rs:246-293`, extended to capture the two new fields.
- `pub async fn post_raw(addr, extra_headers, raw_body: &str) -> Resp` — sends the body bytes
  verbatim with no JSON validation, so plan 04 can test malformed JSON, unknown methods and
  string ids at the wire level.
- `pub async fn get(addr, extra_headers) -> Resp` and `pub async fn delete(addr, extra_headers) -> Resp`
  — needed for the GET/DELETE → 405 assertions in plan 04.
- `pub fn v2_body(method: &str, id: serde_json::Value, params: serde_json::Value) -> String` —
  builds a JSON-RPC request whose `params._meta` carries ALL THREE reserved keys:
  `"io.modelcontextprotocol/protocolVersion": V2`,
  `"io.modelcontextprotocol/clientInfo": {"name":"pmcp-test-client","version":"0.0.0"}`, and
  `"io.modelcontextprotocol/clientCapabilities"` — the LAST is mandatory on every shared test
  request (Codex Plan-02 HIGH #3): plan 09's `-32021` check reads it, and a harness that omits
  it would make every MRTR test accidentally exercise the undeclared-capability path. Default
  it to `{"elicitation":{},"sampling":{},"roots":{}}` and expose
  `pub fn v2_body_with_caps(method, id, params, caps: serde_json::Value) -> String` for tests
  that deliberately under-declare. Construct `_meta` through pmcp's OWN `RequestMeta`
  serialization so the key spelling cannot drift. The `id` is a `serde_json::Value` PARAMETER
  (the Phase-112 helper hard-coded `1`; MRTR retries MUST use a different id, and plan 08
  needs string ids).
- `pub fn v2_headers(method: &str, name: &str) -> Vec<(String, String)>` — the three required
  v2 headers. `Mcp-Name` is ALWAYS emitted; pass `""` for a name-less method (this mirrors the
  locked header rule and what plan 05's client does). The name value is passed through
  `pmcp`'s `encode_header_value` equivalent so a non-ASCII name is sentinel-encoded — expose
  `pub fn v2_headers_raw(method, raw_name)` for tests that deliberately send a malformed
  sentinel.
- `pub fn spawn_default_config(server) -> (SocketAddr, JoinHandle<()>)` — spawns a
  `StreamableHttpServer` built with `StreamableHttpServerConfig::default()`, i.e. a STATEFUL
  config with a live `session_id_generator`. Document loudly that this is the deliberate
  deviation from the Phase-112 helper (which used `::stateless()`): per RESEARCH Pitfall 1,
  `stateless()` is a build-time config, so a test that uses it never exercises the per-request
  era gate at all.
- `pub fn spawn_stateless_config(server) -> (SocketAddr, JoinHandle<()>)` — the `::stateless()`
  spawn, kept for the tests that genuinely want it.
- `pub fn build_v2_server(...) -> pmcp::Server` — the opt-in builder from
  `v2_required_headers.rs:125-143` (accept-list containing both `2025-11-25` and V2, extensions
  capabilities) plus the trivial `SearchTool` / `GreetingPrompt` / `GreetingResource` handlers,
  so all three MRTR-eligible methods have a real handler.

Do NOT modify `tests/v2_required_headers.rs` in this plan — it must keep passing untouched as
the Phase-112 regression baseline.

Add `tests/common_harness_smoke.rs` with `#[tokio::test]`s that spawn `build_v2_server` via
`spawn_default_config` and assert: (a) `v2_body("tools/call", json!(1), ..)` with
`v2_headers("tools/call", "search")` returns 200 with a `result`; (b) `v2_body("tools/list",
json!(2), json!({}))` with `v2_headers("tools/list", "")` — the EMPTY `Mcp-Name` — also returns
200, proving the harness satisfies the server's presence-always header gate. That second
assertion is the cross-plan header-rule tripwire.
  </action>
  <acceptance_criteria>
    - `tests/common/mod.rs`, `tests/common/v2.rs` and `tests/common_harness_smoke.rs` exist
    - `tests/common/v2.rs` contains `pub fn spawn_default_config`, `pub fn spawn_stateless_config`, `pub async fn post_raw`, `pub fn v2_body_with_caps`, `pub fn v2_headers_raw`
    - `tests/common/v2.rs` contains the literal `io.modelcontextprotocol/clientCapabilities`
    - `tests/common/v2.rs` `Resp` struct contains `mcp_session_id` and `content_type`
    - `tests/common_harness_smoke.rs` contains a test asserting a `tools/list` request with an EMPTY `Mcp-Name` returns 200
    - `cargo test --test common_harness_smoke --features full` exits 0
    - `cargo test --test v2_required_headers --features full` still passes with at least 25 tests (Phase-112 baseline untouched)
    - `git diff --stat tests/v2_required_headers.rs` shows no changes
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test common_harness_smoke --features full &amp;&amp; cargo test --test v2_required_headers --features full</automated>
  </verify>
  <done>A shared `tests/common/v2.rs` harness exists with parameterized request ids, mandatory `clientCapabilities`, a raw-body poster, GET/DELETE helpers, session-id and content-type capture, and a `Default::default()` spawn; a smoke test proves both the happy path and the empty-`Mcp-Name` header rule; the Phase-112 test file is untouched and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server params JSON | `inputResponses` and `requestState` arrive here as attacker-controlled input |
| server → client result JSON | `inputRequests` and the minted `requestState` cross outward |
| header value → intermediary | `Mcp-Name` crosses WAFs and proxies that may re-split on delimiters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-03 | Elevation of Privilege | `salient_param_digest` (AAD originating-request binding) | mitigate | Per-method WHITELIST (`name`+`arguments`, or `uri`) with recursive key-sorted canonicalization before SHA-256, so a token minted for one tool/args cannot verify against another (spec replay clause 5c) |
| T-113-14 | Denial of Service | `extract_mrtr_params` parsing attacker-supplied MRTR fields | mitigate | Five explicit bounds (`MAX_REQUEST_STATE_LEN`, count, per-entry bytes, total bytes, nesting depth), each a distinct `MrtrParseError`; proptest asserts no panic over arbitrary JSON |
| T-113-15 | Tampering | MRTR field placement drift (`_meta` vs `params`) | mitigate | `splice_mrtr_params`/`extract_mrtr_params` are the only two places the key spelling exists, with tests asserting the literal top-level `params` placement |
| T-113-16 | Spoofing | Method-set drift letting `input_required` escape onto a forbidden method | mitigate | `MRTR_ELIGIBLE_METHODS` is a single const consumed by plan 09's tripwire; `logical_name_key` moved here so client and server read one table |
| T-113-44 | Tampering | a malformed or oversized `requestState` silently treated as ABSENT and thus bypassing the verdict table | mitigate | `extract_mrtr_params` returns `Result`; only a genuinely absent key yields the default. Plan 06 maps every `MrtrParseError` to a JSON-RPC error before dispatch |
| T-113-45 | Tampering | stale `inputResponses`/`requestState` from an earlier round surviving into a later retry | mitigate | `splice_mrtr_params` removes both keys unconditionally before inserting, with a proptest asserting the default-splice leaves neither key |
| T-113-46 | Spoofing | an `InputResponse` of one kind decoded as another via untagged overlap | mitigate | `InputResponse::decode_for(kind, ..)` is kind-directed from the originating `InputRequest`; the untagged path is an explicitly documented server-ingress-only fallback |
| T-113-47 | Tampering | header-value splitting via delimiters in a tool name or resource URI | mitigate | `encode_header_value` excludes `"`, `,`, `;`, `\` from the pass-through set and sentinel-encodes anything else, including a value that itself starts with the sentinel marker |
</threat_model>

<verification>
- `cargo test --lib --features full -- types::mrtr` and `-- elicitation` pass
- `cargo test --test common_harness_smoke --features full` passes
- `cargo test --test v2_required_headers --features full` still passes (Phase-112 baseline)
- `cargo build --lib --target wasm32-unknown-unknown` succeeds (mrtr.rs is crypto-free and wasm-clean)
- `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp` reports no major bump
</verification>

<success_criteria>
- The MRTR wire types serialize/deserialize to the exact spec spelling, asserted by literal-key tests
- Absent is distinguishable from invalid, and five DoS bounds are enforced at parse time
- Splicing cannot leak a previous round's data
- Exactly one table names the three MRTR-eligible methods and their logical-name keys, and one rule governs `Mcp-Name` presence
- `InputRequiredResult` / `MrtrOutcome` exist so plan 07 can hand an unfulfilled result back to a caller
- A `mode`-less form elicitation deserializes; serialization still emits `"mode":"form"`
- `tests/common/v2.rs` is consumable by six downstream plans and its smoke test proves both the happy path and the empty-`Mcp-Name` rule
</success_criteria>

<output>
Create `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-02-SUMMARY.md` when done
</output>
</content>
