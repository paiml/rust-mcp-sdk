---
phase: 113-stateless-http-multi-round-trip-elicitation
plan: 11
type: execute
wave: 5
depends_on: ["113-07", "113-09"]
files_modified:
  - tests/v2_mrtr.rs
  - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONFORMANCE-MANIFEST.md
  - examples/s47_v2_stateless_mrtr.rs
  - examples/s48_v2_mrtr_client.rs
  - Cargo.toml
autonomous: true
requirements: [HTTP-02, HTTP-03, CLNT-02]

must_haves:
  truths:
    - "Every sep-2322 scenario in the PINNED conformance-suite commit has a named, passing Rust test in this repo, and an unmapped scenario is a build-visible failure"
    - "A real pmcp Client completes a multi-round MRTR exchange against a real pmcp server over HTTP with no handshake and no session"
    - "A developer can run one command to see a stateless v2 server perform multi-round-trip elicitation, and a second command to see a v2 client fulfil it automatically"
  artifacts:
    - path: ".planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONFORMANCE-MANIFEST.md"
      provides: "scenario-id → local-test mapping generated from the pinned conformance commit"
      contains: "sep-2322"
    - path: "tests/v2_mrtr.rs"
      provides: "Rust mirror of the official sep-2322 conformance checks + real client↔server MRTR"
      min_lines: 340
      contains: "multi_round"
    - path: "examples/s47_v2_stateless_mrtr.rs"
      provides: "runnable stateless v2 SERVER demonstrating MRTR (CLAUDE.md ALWAYS requirement)"
      min_lines: 120
    - path: "examples/s48_v2_mrtr_client.rs"
      provides: "runnable v2 CLIENT that fulfils MRTR automatically against s47"
      min_lines: 80
  key_links:
    - from: "113-SPEC-RECHECK.md ## Conformance Suite Pin"
      to: "113-CONFORMANCE-MANIFEST.md"
      via: "the manifest is derived from the pinned commit's scenario ids, not from the research table"
      pattern: "sep-2322"
    - from: "113-CONFORMANCE-MANIFEST.md"
      to: "tests/v2_mrtr.rs"
      via: "one test per pinned scenario, named after it; a missing mapping fails the manifest check"
      pattern: "sep_2322"
    - from: "examples/s48_v2_mrtr_client.rs"
      to: "Cargo.toml [[example]] required-features"
      via: "both examples are buildable via cargo run --example"
      pattern: "s48_v2_mrtr_client"
---

<objective>
Prove MRTR end to end and make it demonstrable: a Rust mirror of every `sep-2322` scenario in the
PINNED conformance-suite commit, a manifest that makes an unmapped scenario a visible failure, a
real-pmcp-Client-against-real-pmcp-server multi-round exchange, and a runnable server+client
example pair.

Purpose: Phase 118 owns the official Node conformance suite in CI. This phase must be
self-verifying without a Node toolchain, so the specific `sep-2322-*` assertions are encoded as
native Rust integration tests now — which also means Phase 118 inherits a codebase that already
passes rather than discovering the gaps late.

Cross-AI review raised two HIGH items here: the tests were derived from the July-24 research
TABLE rather than a freshly pinned suite commit (so a scenario added upstream would be silently
unmapped), and the real-client tests depended on plan 07's then-unresolved return type. Plan 01
now re-pins the suite and enumerates its scenario ids; plan 07 now provides `MrtrOutcome` and
`Error::input_required_unfulfilled`; and plan 07's live tests moved HERE, which is why this plan
depends on both 07 and 09. Review also asked for a paired example CLIENT and a
non-environment-sensitive example check.
Output: a generated manifest, `tests/v2_mrtr.rs`, two examples, and their Cargo entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-PATTERNS.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-REVIEWS.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-07-SUMMARY.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-09-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the scenario manifest from the PINNED commit and mirror every check</name>
  <files>tests/v2_mrtr.rs, .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONFORMANCE-MANIFEST.md</files>
  <read_first>
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md (the `## Conformance Suite Pin` section — the 40-character commit sha and the enumerated `sep-2322` scenario ids. THIS, not the research table, is the authority)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (the table "Official conformance checks this phase is graded on" — use only as a cross-check against the pinned list; a discrepancy is a finding to record, not to ignore)
    - tests/common/v2.rs (`build_v2_server`, `spawn_default_config`, `post`, `post_raw`, `v2_body(method, id, params)`, `v2_body_with_caps`, `v2_headers`)
    - tests/v2_mrtr_ingress.rs (plan 06's verdict tests — do not duplicate them here; this file covers the round-trip scenarios)
    - src/server/core.rs (the `mrtr_egress` helper and the eligibility tripwire from plan 09, so the fixture handlers signal correctly)
    - src/types/mrtr.rs (`MrtrSignal::into_meta_entry` — the authoring surface the fixture handlers use)
  </read_first>
  <action>
**1. Write the manifest FIRST (Codex Plan-11 HIGH #1 and MEDIUM).** Create
`113-CONFORMANCE-MANIFEST.md` containing:
- the pinned conformance-suite commit sha copied from `113-SPEC-RECHECK.md`;
- a table with one row per PINNED `sep-2322*` / `input-required-result-capability-check` scenario
  id, its one-line assertion, and the EXACT local test fn name that mirrors it;
- a `## Unmapped` section that must be EMPTY. If the pinned commit contains a scenario id with no
  local mirror, list it there and treat the plan as incomplete — do not silently drop it;
- a `## Research-Table Delta` section recording any scenario present in the July-24 research table
  but absent from the pinned commit, or vice versa.

Derive the local test names mechanically from the scenario ids: dashes become underscores, so
`sep-2322-multi-round` maps to `sep_2322_multi_round`. That makes the manifest check a name
comparison rather than a judgement call.

**2. Create `tests/v2_mrtr.rs`** with a fixture server whose tool, prompt and resource handlers
return scripted `input_required` results driven by the `MrtrSignal` authoring surface. Carry the
cfg gate and reliability doctrine from `tests/v2_required_headers.rs`. Drive the wire with raw
`post`/`post_raw` (not the pmcp Client) so the assertions are on actual bytes — the Client path is
covered in task 2.

Write one test per manifest row. At minimum, the pinned list is expected to include:
- `sep_2322_elicitation_incomplete_then_complete` — `tools/call` → `input_required` with an
  `elicitation/create` entry; retry with `inputResponses.<key>` → complete.
- `sep_2322_sampling_incomplete_then_complete` — same for `sampling/createMessage`.
- `sep_2322_list_roots_incomplete_then_complete` — same for `roots/list`.
- `sep_2322_request_state_incomplete_then_complete` — a round trip carrying BOTH `inputRequests`
  and `requestState`.
- `sep_2322_multiple_inputs` — multiple `inputRequests` of DIFFERENT kinds in one result, all
  answered in one retry.
- `sep_2322_multi_round` — THREE rounds with an EVOLVING `requestState`; assert each round's token
  differs from the previous one.
- `sep_2322_missing_response_rerequests` — under-supply the responses; assert the server returns a
  NEW `input_required` re-requesting the missing entry rather than an error (server obligation 9).
- `sep_2322_non_tool_incomplete_then_complete` — the same flow on `prompts/get` AND on
  `resources/read`.
- `sep_2322_result_type_included` — assert `resultType` is EXPLICITLY present and equals
  `"input_required"` (not inferred from the presence of `inputRequests`).
- `sep_2322_not_on_unsupported_requests` — a handler signalling on `tools/list` produces a normal
  complete result, never `input_required`. (Plan 09 makes this an internal error rather than a
  silent pass; assert the concrete behavior plan 09 shipped, and record it in the manifest row.)
- `input_required_result_capability_check` — a server signalling for `sampling` when
  `_meta.clientCapabilities` declares none returns `-32021` at HTTP 400 with an OBJECT-shaped
  `data.requiredCapabilities`. Build this request with `v2_body_with_caps` so the under-declaration
  is deliberate rather than an artifact of the harness.

If the pinned commit contains scenario ids beyond this list, ADD a test for each; the manifest's
`## Unmapped` section is what makes that non-optional.

Additional wire-shape assertions the conformance suite depends on and which are the single most
likely silent interop failure (record these in the manifest as pmcp-added rows):
- `mrtr_fields_are_params_siblings` — assert that on the retry the server accepts
  `inputResponses`/`requestState` at `params` TOP LEVEL, and that a request placing them inside
  `params._meta` is NOT treated as MRTR (the operation re-elicits or errors rather than resuming).
- `mrtr_retry_uses_different_id` — assert the server accepts a retry whose JSON-RPC id differs
  from the original and echoes the retry's id back.
- `mrtr_signal_key_never_on_wire` — grep the raw response body of every scenario in this file for
  the literal `dev.pmcp/mrtr` and assert zero occurrences (plan 09's unconditional strip).

Assert on concrete JSON paths and HTTP statuses only.
  </action>
  <acceptance_criteria>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONFORMANCE-MANIFEST.md` exists and contains the pinned commit sha copied from `113-SPEC-RECHECK.md`
    - The manifest's `## Unmapped` section is empty
    - The manifest contains a `## Research-Table Delta` heading
    - Every test fn name listed in the manifest exists in `tests/v2_mrtr.rs`: for each manifest row, `grep -q "fn <name>" tests/v2_mrtr.rs` succeeds
    - `tests/v2_mrtr.rs` contains `sep_2322_multi_round`, `sep_2322_missing_response_rerequests`, `sep_2322_result_type_included`, `input_required_result_capability_check`, `mrtr_fields_are_params_siblings`, `mrtr_retry_uses_different_id`, `mrtr_signal_key_never_on_wire`
    - The `sep_2322_multi_round` test asserts three distinct `requestState` values
    - The `input_required_result_capability_check` test asserts `body["error"]["data"]["requiredCapabilities"].is_object()`
    - The `mrtr_fields_are_params_siblings` test asserts the `_meta`-placed variant does NOT resume
    - `cargo test --test v2_mrtr --features full` exits 0 with at least 14 tests passing
    - `cargo test --test v2_mrtr_ingress --features full`, `--test v2_stateless_http --features full`, `--test v2_client --features full` all still pass
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test v2_mrtr --features full &amp;&amp; test -f .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONFORMANCE-MANIFEST.md</automated>
  </verify>
  <done>A manifest generated from the pinned conformance commit maps every official sep-2322 scenario to a named passing Rust test with an empty Unmapped section, including the three-round evolving-state case, the under-supply re-request, the capability check, the params-vs-_meta placement assertion, and a wire grep proving the internal signal key never ships.</done>
</task>

<task type="auto">
  <name>Task 2: Real pmcp Client ↔ real pmcp server multi-round exchange</name>
  <files>tests/v2_mrtr.rs</files>
  <read_first>
    - tests/v2_mrtr.rs (the fixture server from task 1 — reuse it)
    - tests/v2_client.rs (plan 05's live tests and plan 07's mock-transport MRTR tests, and their recording-wrapper technique)
    - src/client/mod.rs (`with_protocol_version`, `mrtr_round_limit`, `send_with_mrtr`, `fold_input_requests`, `call_tool_mrtr`, and the v2 behavior of plain `call_tool`)
    - src/error/mod.rs (`is_mrtr_round_limit_exceeded`, `is_input_required_unfulfilled`, `input_required_result`)
  </read_first>
  <action>
Add a section to `tests/v2_mrtr.rs` that drives the SAME fixture server with a real pmcp `Client`
opted into v2, rather than with raw `post`. This is the interoperability proof that the server
half (plans 06/09) and the client half (plans 05/07) agree on the wire — each has been tested
against a hand-built counterpart until now. These tests live here rather than in plan 07 because
a scripted real server cannot emit `input_required` until plan 09 exists.

Tests, named exactly as listed:
- `client_server_mrtr_elicitation_roundtrip` — register a `HostElicitationHandler` on the client,
  call the fixture tool, assert the caller receives a COMPLETE result and the handler was invoked
  exactly once.
- `client_server_mrtr_three_rounds` — the fixture requires three rounds; assert the caller
  receives a complete result and the handler was invoked exactly three times.
- `client_server_mrtr_mixed_kinds` — register elicitation, sampling and roots handlers; the
  fixture returns all three kinds in one result; assert completion and one invocation of each.
- `client_server_mrtr_no_session_no_handshake` — assert the whole exchange happened with no
  `initialize` observed by the server and no `Mcp-Session-Id` on any request, using a recording
  wrapper on the server side.
- `client_server_mrtr_round_limit_typed_error` — configure `mrtr_round_limit(2)` against a fixture
  that never completes; assert the caller gets an error for which `is_mrtr_round_limit_exceeded()`
  is true and the server observed exactly 2 requests.
- `client_server_mrtr_outcome_input_required` — register NO handlers; call `call_tool_mrtr`;
  assert the caller receives `MrtrOutcome::InputRequired(r)` whose `input_requests` is non-empty
  and whose `request_state` is present, and that the server observed exactly ONE request (no
  resend).
- `client_server_mrtr_existing_method_typed_error` — the same no-handler scenario through plain
  `call_tool`; assert an `Err` for which `is_input_required_unfulfilled()` is true and
  `input_required_result()` carries the same `request_state` — and explicitly assert the caller did
  NOT receive an empty `CallToolResult`. This is the live proof of the review's most consequential
  finding.

Use invocation counters (an `Arc<AtomicUsize>` captured by the stub handlers) for the "exactly N
times" assertions rather than inferring from logs.
  </action>
  <acceptance_criteria>
    - `tests/v2_mrtr.rs` contains all seven test fn names listed in the action
    - The `client_server_mrtr_three_rounds` test asserts an exact handler invocation count of 3
    - The `client_server_mrtr_round_limit_typed_error` test asserts both `is_mrtr_round_limit_exceeded()` and an exact server-observed request count of 2
    - The `client_server_mrtr_outcome_input_required` test asserts an exact server-observed request count of 1
    - The `client_server_mrtr_existing_method_typed_error` test asserts `is_input_required_unfulfilled()` and that no empty `CallToolResult` was returned
    - `cargo test --test v2_mrtr --features full` exits 0 with at least 21 tests passing
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test v2_mrtr --features full</automated>
  </verify>
  <done>A real pmcp Client and a real pmcp server complete single-round, three-round and mixed-kind MRTR exchanges over HTTP with no handshake and no session; the round limit surfaces as the typed error with an exact request count; and an unfulfillable result reaches the caller as `MrtrOutcome::InputRequired` or as the typed error carrying the full result, never as an empty success.</done>
</task>

<task type="auto">
  <name>Task 3: Runnable stateless-v2 MRTR server AND client examples</name>
  <files>examples/s47_v2_stateless_mrtr.rs, examples/s48_v2_mrtr_client.rs, Cargo.toml</files>
  <read_first>
    - examples/t05_streamable_http_stateless.rs (whole file — the header/run-instructions/imports shape to copy; note its stale `cargo run --example 23_...` line does NOT match its own filename — do not copy that defect)
    - examples/s13_elicit_input.rs (the elicitation-handler authoring side, for the client example)
    - Cargo.toml (the existing `[[example]]` entries with `required-features`, to match their shape)
    - src/types/mrtr.rs (`MrtrSignal::into_meta_entry` — the authoring surface the server example must showcase)
    - src/client/mod.rs (`with_protocol_version`, `mrtr_round_limit`, `call_tool` — the client example's surface)
    - src/server/request_state.rs and src/server/mod.rs (the `PMCP_REQUEST_STATE_KEY` / `PMCP_REQUEST_STATE_TTL_SECS` env contract AND the `with_request_state_key` builder method, both to document in the example header)
  </read_first>
  <action>
Create TWO examples, satisfying CLAUDE.md's ALWAYS requirement that every new feature ships a
`cargo run --example` demonstration, and Codex Plan-11 MEDIUM's point that a server-only example
does not prove automatic fulfilment.

**`examples/s47_v2_stateless_mrtr.rs` — the server.** It must:
- open with a doc header naming ITS OWN file in the run instruction
  (`cargo run --example s47_v2_stateless_mrtr --features full`) and listing what it demonstrates:
  no `initialize` handshake, no `Mcp-Session-Id`, a handler that asks for more input, an
  AEAD-protected `requestState`, and resumption across the retry;
- build a `Server` opted into v2 via `with_supported_protocol_versions` alongside 2025-11-25, so
  the same binary still serves v1 clients — that dual-version story is the milestone's core value;
- register a tool whose handler asks for a missing argument on the first call by returning
  `MrtrSignal { input_requests, continuation }` through `into_meta_entry()`, then completes on the
  retry using `extra.mrtr_continuation()` and `extra.input_responses()`;
- run over `StreamableHttpServer` on a printed loopback address;
- print, at startup, the bound loopback address in the literal `127.0.0.1:<port>` form (the
  acceptance check greps for it), then the exact `curl` invocation for round 1, and explain that
  round 2 echoes the `requestState` from the round-1 response;
- stay running until signalled — it is a server, not a one-shot script;
- document the `PMCP_REQUEST_STATE_KEY` contract in the header: unset means a per-process key and
  a startup warning, multi-instance deployments must set it, and a MALFORMED value fails the
  server build. Also mention the `with_request_state_key` builder alternative. Show the warning
  being emitted as part of the demo rather than hiding it;
- contain no `unwrap()`/`expect()` outside clearly-labelled example setup.

**`examples/s48_v2_mrtr_client.rs` — the client.** It must:
- open with a doc header naming its own file and stating that it expects `s47` to be running,
  taking the server address as `argv[1]` with a `127.0.0.1:<port>` default;
- build a `Client` with `with_protocol_version(PROTOCOL_VERSION_2026_07_28)` and a registered
  `HostElicitationHandler` that answers programmatically (D-07's agent shape — NOT a blocking
  stdin read, so the example is scriptable);
- call the fixture tool with plain `call_tool` and print the COMPLETE result, demonstrating that
  the gather→resend loop is automatic and invisible to the caller;
- then run a second call with NO handler registered and print the
  `Error::input_required_unfulfilled` outcome, demonstrating the D-06 "returns the result to the
  caller" behavior;
- exit 0 when both demonstrations succeed, so it is verifiable as a one-shot script.

Register BOTH in `Cargo.toml` with `[[example]]` entries carrying
`required-features = ["streamable-http"]` (plus `http-client` for s48, and any other feature they
use), matching the shape of the existing entries.

**Verification without environment sensitivity (Codex Plan-11 LOW).** The `timeout`-returns-124
check depends on GNU/BSD `timeout` being present. Guard it: if `command -v timeout` succeeds, run
`timeout 12 cargo run --example s47_v2_stateless_mrtr` and require exit status `124`; otherwise
spawn the example in the background, sleep, assert the process is still alive and the log matched,
then kill it. Record which path ran in the SUMMARY. The s48 client example is verified by running
it against a backgrounded s47 and requiring exit status 0.

Add a companion doctest or rustdoc snippet on `MrtrSignal::into_meta_entry` (if not already added
in plan 09) that compiles under `make test-doc`.
  </action>
  <acceptance_criteria>
    - `examples/s47_v2_stateless_mrtr.rs` exists and its header contains the literal string `cargo run --example s47_v2_stateless_mrtr`
    - `examples/s48_v2_mrtr_client.rs` exists and its header contains the literal string `cargo run --example s48_v2_mrtr_client`
    - `grep -c 'cargo run --example 23_' examples/s47_v2_stateless_mrtr.rs` is `0` (the analog's stale-name defect is not copied)
    - `examples/s47_v2_stateless_mrtr.rs` contains `into_meta_entry`, `mrtr_continuation`, `input_responses`, and `PMCP_REQUEST_STATE_KEY`
    - `examples/s48_v2_mrtr_client.rs` contains `with_protocol_version` and `is_input_required_unfulfilled`
    - `Cargo.toml` contains `[[example]]` entries with `name = "s47_v2_stateless_mrtr"` and `name = "s48_v2_mrtr_client"`, each with `required-features`
    - `cargo build --example s47_v2_stateless_mrtr --features full` and `cargo build --example s48_v2_mrtr_client --features full` both exit 0
    - The s47 liveness check passes on whichever branch ran (timeout→124, or background-spawn→still alive), and the captured run log matches `127\.0\.0\.1:[0-9]+`
    - Running `s48` against a backgrounded `s47` exits 0
    - `make test-doc` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo build --example s47_v2_stateless_mrtr --features full &amp;&amp; cargo build --example s48_v2_mrtr_client --features full &amp;&amp; if command -v timeout >/dev/null 2>&amp;1; then timeout 12 cargo run --example s47_v2_stateless_mrtr --features full > target/s47_run.log 2>&amp;1; test "$?" = "124"; else cargo run --example s47_v2_stateless_mrtr --features full > target/s47_run.log 2>&amp;1 &amp; P=$!; sleep 12; kill -0 "$P"; R=$?; kill "$P" 2>/dev/null; test "$R" = "0"; fi &amp;&amp; grep -qE '127\.0\.0\.1:[0-9]+' target/s47_run.log</automated>
  </verify>
  <done>A self-explanatory runnable server example demonstrates a dual-version server performing multi-round-trip elicitation with an AEAD-protected requestState and a visible key-fallback warning, a paired client example proves automatic fulfilment AND the no-handler typed-error path, both are registered with their required features, and the liveness check works with or without `timeout` present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example → operator | The example teaches the deployment contract for a security-critical env var |
| test fixtures → CI | Fixtures encode the conformance rules the production code is graded on |
| upstream conformance suite → local tests | An upstream scenario with no local mirror is an unmeasured requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-17 | Spoofing | an operator deploying multi-instance without `PMCP_REQUEST_STATE_KEY` | mitigate | The server example documents the contract in its header AND shows the startup warning being emitted rather than suppressing it, so the degraded mode is visible in the first thing a developer runs |
| T-113-28 | Tampering | MRTR fields silently accepted from `_meta` instead of `params` | mitigate | `mrtr_fields_are_params_siblings` asserts the `_meta`-placed variant does NOT resume — an in-house-only round-trip test would pass while every conformance check failed |
| T-113-32 | Elevation of Privilege | capability check regressing to an array-shaped payload | mitigate | `input_required_result_capability_check` asserts `data.requiredCapabilities.is_object()` explicitly, driven by a deliberately under-declaring request |
| T-113-11 | Denial of Service | round-limit regression | mitigate | `client_server_mrtr_round_limit_typed_error` asserts an exact server-observed request count, not just that an error occurred |
| T-113-31 | Information Disclosure | the internal MRTR signal key reaching the wire | mitigate | `mrtr_signal_key_never_on_wire` greps every raw response body in the file for `dev.pmcp/mrtr` |
| T-113-37 | Information Disclosure | an example that leaks a real key or hardcodes one | mitigate | Neither example hardcodes a key; both rely on the env contract and the documented per-process fallback |
| T-113-56 | Repudiation | an unfulfilled `input_required` silently normalized into an empty success | mitigate | `client_server_mrtr_existing_method_typed_error` asserts the typed error and explicitly rules out an empty `CallToolResult` |
| T-113-65 | Repudiation | an upstream conformance scenario going unmeasured | mitigate | `113-CONFORMANCE-MANIFEST.md` is generated from the PINNED commit with a must-be-empty `## Unmapped` section and a mechanical id→test-name derivation |
</threat_model>

<verification>
- `cargo test --test v2_mrtr --features full` passes (≥21 tests)
- `113-CONFORMANCE-MANIFEST.md` exists with an empty `## Unmapped` section, and every listed test name is found in `tests/v2_mrtr.rs`
- `cargo test --test v2_mrtr_ingress --features full`, `--test v2_client --features full`, `--test v2_stateless_http --features full`, `--test v2_subscriptions --features full` all still pass
- Both examples build; s47 stays alive under the guarded liveness check and s48 exits 0 against it
- `make test-doc` passes
</verification>

<success_criteria>
- Every scenario in the pinned conformance commit has a named, passing Rust equivalent, with an empty Unmapped section proving it
- A real pmcp Client completes single-round, three-round and mixed-kind MRTR against a real pmcp server with no handshake and no session
- An unfulfillable `input_required` is proven to reach the caller in both API shapes
- The params-vs-`_meta` placement, the object-shaped capability payload, and the never-on-the-wire signal key are all asserted
- A developer can run one command to watch MRTR work server-side and a second to watch a client fulfil it automatically
</success_criteria>

<output>
Create `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-11-SUMMARY.md` when done
</output>
</content>
