---
phase: 118-conformance-against-the-official-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - conformance/package.json
  - conformance/package-lock.json
  - conformance/.npmrc
  - conformance/README.md
  - Cargo.toml
autonomous: true
requirements: [CONF-01]

must_haves:
  truths:
    - "`npm ci --prefix conformance --ignore-scripts` installs exactly one pinned version of @modelcontextprotocol/conformance, reproducibly, from a committed lockfile with integrity hashes, and the suite still runs afterwards"
    - "The pinned version supports `--requirements <revision>` (it is 0.2.0-alpha.11 or later, never the `latest` 0.1.16 line)"
    - "A `npm ci` run under Node 20 FAILS at install time, proved by execution — the engine floor is enforced by a committed .npmrc, not merely declared"
    - "`cargo package -p pmcp --list --allow-dirty` lists NO path under conformance/ and NO tests/ci_conformance_gate_wiring.rs"
    - "`make purity-check` still exits 0 with the Node manifest in the tree"
    - "conformance/README.md states the ONE canonical zero-check policy that plans 118-04, 118-05 and 118-08 all apply"
    - "conformance/README.md reconciles the new npm pin with the pre-existing conformance-repo SHA pin in tests/v2_conformance_pin.rs"
  artifacts:
    - path: "conformance/package.json"
      provides: "exact-pinned suite dependency + Node 22 engine floor + private marker"
      contains: "@modelcontextprotocol/conformance"
    - path: "conformance/package-lock.json"
      provides: "integrity-hash reproducibility (the mechanism D-01 is buying)"
      contains: "integrity"
    - path: "conformance/.npmrc"
      provides: "engine-strict=true, which is what actually makes engines.node refuse Node 20"
      contains: "engine-strict"
    - path: "conformance/README.md"
      provides: "the pin rationale, the re-pin procedure, the canonical zero-check policy, and the two-pin reconciliation"
      min_lines: 60
    - path: "Cargo.toml"
      provides: "exclude entries keeping the Node manifest and its reader out of the crates.io tarball"
      contains: "conformance/"
  key_links:
    - from: "conformance/package-lock.json"
      to: "registry.npmjs.org/@modelcontextprotocol/conformance"
      via: "resolved + integrity"
      pattern: "modelcontextprotocol/conformance"
    - from: "conformance/.npmrc"
      to: "conformance/package.json engines.node"
      via: "engine-strict turns a warning into a failure"
      pattern: "engine-strict"
    - from: "Cargo.toml"
      to: "conformance/"
      via: "exclude array"
      pattern: "conformance/"
---

<objective>
Land the pinned Node manifest for the official `@modelcontextprotocol/conformance` suite (D-01),
settle its packaging disposition so the first Rust-repo-adjacent Node manifest cannot ship in the
`pmcp` crates.io tarball, and write the ONE canonical statement of the zero-check policy that the
three measuring plans (118-04, 118-05, 118-08) must all apply identically.

Purpose: CONF-01's referee is a Node CLI. D-01 chose a committed `package.json` +
`package-lock.json` run with `npm ci` over `npx` (a yanked or re-published version silently changes
the referee) and over a submodule or container (cost with no added reproducibility). The ledger
entry `project_ci_purity_gate_unpinned_tooling_drift` records the Purity Gate bit-rotting from
exactly this failure mode. Pin it or it rots.

Output: `conformance/{package.json,package-lock.json,.npmrc,README.md}` and two `exclude` entries in
the root `Cargo.toml` with the reasoning stated inline, verified the same way `115-REVIEW.md` CR-01
was caught.

## Review corrections applied (D-20)

- **`engines.node` does not make npm refuse Node 20.** Without `engine-strict`, npm warns and
  continues — and the pre-review plan conceded `npm ci` succeeds on Node 20 while its must-have
  claimed the opposite. Task 1 now ships `conformance/.npmrc` with `engine-strict=true` and proves
  the refusal by EXECUTING an install under Node 20.
- **`npm ci` runs lifecycle scripts by default, including transitive ones.** Checking only the
  direct package's `postinstall` is incomplete. Every install in this phase is
  `npm ci --ignore-scripts`, and Task 1 proves the suite still runs after such an install.
- **The Task-1 acceptance criterion was impossible.** It expected `git ls-files conformance/` to
  list three files when Task 1 creates two, and `git ls-files` does not list untracked files at all.
  The criterion is now: the files exist on disk, are NOT git-ignored, and the tracked-ness check is
  deferred to the plan's own commit step.
- **The `cargo package` verification masked failure.** `cargo package … | grep …; test $? -eq 1`
  reports PASS when `cargo package` FAILS (grep sees empty input and returns 1). Task 2 now captures
  first, asserts the capture succeeded, and only then greps (D-19).
- **The zero-check policy is resolved HERE, once.** See Task 3 § 7.
- **The two pins are reconciled HERE.** See Task 3 § 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md

<interfaces>
<!-- Values the executor needs verbatim. -->

Package legitimacy (RESEARCH § Package Legitimacy Audit, disposition **Approved**, no checkpoint
required): scoped `@modelcontextprotocol/*` name; source repo
`git+https://github.com/modelcontextprotocol/conformance.git`; 27,587 weekly downloads;
`scripts.postinstall` empty (no install-time script); published via GitHub Actions OIDC trusted
publishing; `slopcheck install -e npm @modelcontextprotocol/conformance` -> `1 OK`.
NOTE: slopcheck MUST be passed `-e npm` in this repo — auto-detection picks crates.io and reports a
spurious `[ERR] registry unreachable`.

Version facts measured 2026-08-09:
  dist-tags: latest = 0.1.16 (2026-03-30), alpha = 0.2.0-alpha.11 (2026-08-07)
  0.1.16 ships NO requirements/ directory and NO --requirements flag -> unusable for D-04.
  Requirement sets available on the alpha line: 2025-11-25, 2026-07-28.
  Scored-set sizes measured: 33 scenarios at 2025-11-25, 50 at 2026-07-28.
  Known ZERO-CHECK scenarios measured (report `0 passed, 0 failed` and render as a green tick):
    `server-sse-polling`, `tasks-status-notifications`.

The in-repo analog manifest to copy the SHAPE of:
  tests/integration/typescript-interop/package.json  (name/version/description/type/scripts/
  dependencies/engines) and its package-lock.json (lockfileVersion 3, per-package
  resolved + integrity + license).

The PRE-EXISTING conformance pin this plan must reconcile with:
  tests/v2_conformance_pin.rs binds HTTP-08's grading to the conformance REPOSITORY at a specific
  commit. That sha is recorded at
  `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md:591`
  as `**Pinned sha** | a865118206d4d8cc8dbc5f5201607839281d0c3b`.
  Note `v2_conformance_pin.rs` already handles `.planning/` being excluded from the published crate
  by returning early (`recheck_doc()` at :100-111) — that is the in-repo precedent for a test whose
  data path is excluded, and it is why that file correctly SHIPS while this phase's reader will not.

`.gitignore:46` already contains `**/node_modules/`; `.gitignore:2` contains `target/`.
No `.gitignore` edit is needed anywhere in this phase — all generated suite output goes under
`target/conformance-results/` (D-20).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the suite exactly, enforce the Node floor, and install with scripts disabled</name>
  <files>conformance/package.json, conformance/package-lock.json, conformance/.npmrc</files>
  <read_first>
    - tests/integration/typescript-interop/package.json — the in-repo manifest shape to copy.
    - tests/integration/typescript-interop/package-lock.json — the lockfile shape
      (`lockfileVersion: 3`, per-package `resolved` + `integrity` + `license`).
    - .gitignore — confirm `**/node_modules/` at :46 so no ignore edit is needed, and `target/` at
      :2 so suite output under `target/conformance-results/` is already ignored.
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `conformance/package.json + conformance/package-lock.json`, which enumerates the deltas from
      the analog (exact pin, engines >=22, private: true).
    - .planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md — the two MEDIUM
      findings this task closes (`engine-strict`, lifecycle scripts).
  </read_first>
  <action>
Create the directory `conformance/` at the repo root and pin the suite there.

Step 1 — re-check the pin before using it. RESEARCH is valid until 2026-08-23 for this specific
value. Run `npm view @modelcontextprotocol/conformance dist-tags --json` and
`npm view @modelcontextprotocol/conformance time --json`. Choose the newest `0.2.0-alpha.*` (or a
newer stable if the alpha line has graduated). Requirements:
  - it MUST be `0.2.0-alpha.11` or later — the `latest` 0.1.16 line has no `--requirements` flag
    and no `requirements/` directory, so D-04 is impossible on it;
  - confirm `conformance list` on the chosen version reports both requirement sets `2025-11-25`
    and `2026-07-28`;
  - re-run `slopcheck install -e npm @modelcontextprotocol/conformance` (the `-e npm` is
    mandatory) and confirm `1 OK`;
  - confirm `npm view @modelcontextprotocol/conformance@<pin> scripts.postinstall` is empty;
  - ALSO record the package's declared upstream repository and, from
    `npm view @modelcontextprotocol/conformance@<pin> gitHead`, the upstream COMMIT the tarball was
    built from. Task 3 § 10 reconciles that commit with the pre-existing SHA pin. If `gitHead` is
    absent, say so explicitly and record the release tag instead — do not invent a commit.
Record all five results verbatim in the SUMMARY. If the chosen version differs from
`0.2.0-alpha.11`, say so and say why.

Step 2 — create `conformance/.npmrc` containing `engine-strict=true` and a comment stating exactly
why it exists: `engines.node` alone makes npm WARN and continue, so without this file the Node-20
install succeeds and the suite dies at first invocation with
`SyntaxError: The requested module 'fs' does not provide an export named 'globSync'`. This file is
what converts a declaration into an enforcement.

Step 3 — install with an exact pin and NO lifecycle scripts:
`npm install --save-exact --ignore-scripts @modelcontextprotocol/conformance@<pin>` inside
`conformance/`, run under Node 22+ (locally: `PATH=~/.nvm/versions/node/v22.22.2/bin:$PATH`).

Step 4 — hand-edit `conformance/package.json` to its final target state:
  - `"name": "pmcp-conformance-suite"`, `"version": "0.0.0"`, `"private": true`
  - `"description"` naming CONF-01 and the fact that this manifest exists ONLY to pin the referee
  - `"type": "module"`
  - `"engines": { "node": ">=22" }`
  - `"dependencies": { "@modelcontextprotocol/conformance": "<pin>" }` — exact, NO caret, NO tilde.
    The analog uses `^1.17.2`; D-01's reproducibility argument forbids a range here.
  - no `scripts` block that could hide the real invocation — the commands live in
    `scripts/run-conformance-suite.sh` (plan 118-08) so a wiring test can pin them AS DATA.

Step 5 — commit `package.json`, `package-lock.json` and `.npmrc`. Verify the lockfile carries a
`resolved` URL and an `integrity` `sha512-...` hash for `@modelcontextprotocol/conformance` — that
hash IS the reproducibility mechanism D-01 is buying. Do NOT commit `node_modules/`.

Step 6 — prove the three properties by EXECUTION, and record each transcript:
  (a) `rm -rf conformance/node_modules && npm ci --prefix conformance --ignore-scripts` succeeds
      under Node 22 and `npm ls --prefix conformance @modelcontextprotocol/conformance` reports the
      pinned version;
  (b) the suite still WORKS after a scripts-disabled install: `conformance list` on the installed
      binary prints both requirement sets. (If it does not, `--ignore-scripts` is not viable — that
      is a finding: record it, and fall back to a plain `npm ci` only after auditing every locked
      package's `hasInstallScript` flag and recording the audit in the SUMMARY.)
  (c) under Node 20 the install FAILS: with Node 20 first on `PATH`, `npm ci --prefix conformance
      --ignore-scripts` exits non-zero citing the engine constraint. This is the negative control
      for the `.npmrc`; quote the failure verbatim.
  </action>
  <verify>
    <automated>rm -rf conformance/node_modules && npm ci --prefix conformance --ignore-scripts && test -x conformance/node_modules/.bin/conformance && jq -e --arg v "$(jq -r '.dependencies["@modelcontextprotocol/conformance"]' conformance/package.json)" '.version == $v' conformance/node_modules/@modelcontextprotocol/conformance/package.json</automated>
  </verify>
  <acceptance_criteria>
    - The `<automated>` command above exits 0. Note its shape: it asserts the INSTALLED tree's
      version equals the manifest's pinned version via `jq -e`, with no pipe from `npm` into another
      command — a `npm ls | grep` pipeline would report `grep`'s status, and `npm ls` legitimately
      exits non-zero on an unrelated extraneous-package warning (D-19).
    - `test -f conformance/package.json && test -f conformance/package-lock.json && test -f conformance/.npmrc`
      succeeds, and none of the three is ignored:
      `git check-ignore -q conformance/package.json conformance/package-lock.json conformance/.npmrc`
      exits NON-zero (nothing matched). The tracked-ness check belongs to the commit step, not here —
      `git ls-files` does not list untracked new files.
    - `jq -r '.dependencies["@modelcontextprotocol/conformance"]' conformance/package.json` prints
      a bare version with no `^`, `~`, `>` or `*` character.
    - The pinned version is >= `0.2.0-alpha.11`, and
      `node conformance/node_modules/.bin/conformance list 2>&1 | grep -qF "2026-07-28"` matches.
    - `jq -r '.engines.node' conformance/package.json` prints a constraint containing `22`.
    - `jq -r '.private' conformance/package.json` prints `true`.
    - `grep -c 'engine-strict=true' conformance/.npmrc` returns 1.
    - `jq -r '.packages["node_modules/@modelcontextprotocol/conformance"].integrity' conformance/package-lock.json`
      prints a value starting with `sha512-`.
    - `git status --porcelain conformance/node_modules` prints nothing (never committed).
    - Negative control (c) executed and quoted: the Node-20 `npm ci --ignore-scripts` FAILED citing
      the engine constraint. A passing Node-20 install means `.npmrc` is not in effect — that halts
      the task.
    - Evidence that `--ignore-scripts` is viable: the `conformance list` transcript from step 6(b)
      is recorded in the SUMMARY.
    - SUMMARY records: the chosen pin, the dist-tags output, the slopcheck `-e npm` verdict, the
      empty `scripts.postinstall`, and the upstream `gitHead` (or the release tag plus an explicit
      statement that `gitHead` was absent).
  </acceptance_criteria>
  <done>`npm ci --prefix conformance --ignore-scripts` reproducibly installs the exact pinned suite version from a committed lockfile under Node 22, the same command FAILS under Node 20, and the suite runs after a scripts-disabled install.</done>
</task>

<task type="auto">
  <name>Task 2: Settle the packaging disposition — exclude the manifest AND its reader</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml — the `exclude` array (lines 15-59). It already encodes the exact decision
      procedure this task needs, with the reasoning in comments on three entries
      (`tests/team_contracts_conformance.rs`, `tests/phase115_contract_bindings.rs`,
      `tests/keyword_list_mirrors.rs`). Note that `.planning/` is excluded at :57.
    - tests/v2_conformance_pin.rs lines 85-111 — `phase_dir()` and `recheck_doc()`. This file reads
      the EXCLUDED `.planning/` tree yet correctly SHIPS, because it returns early when the path is
      absent instead of panicking. That is the third disposition (ship the reader, make it
      path-tolerant) and the SUMMARY must state why this phase's reader does not take it.
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `Cargo.toml exclude disposition`, which states the rule: *a test that reads a path at runtime
      must have the SAME disposition as the path it reads*, and the measured counter-example
      (ci.yml + run-severance-proofs.sh + ci_severance_gate_wiring.rs ALL ship).
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-01 trap (a).
  </read_first>
  <action>
Add exactly TWO entries to the root `Cargo.toml` `exclude` array, each with a comment stating the
rule in the same voice as the three existing commented entries.

Entry 1: `"conformance/"` — the Node manifest, lockfile and `.npmrc` are CI tooling, never crate
content. D-01 trap (a) locks this. `115-REVIEW.md` CR-01 is the precedent for what happens when a
CI-only artifact ships.

Entry 2: `"tests/ci_conformance_gate_wiring.rs"` — plan 118-09 creates this test and it reads
`conformance/package-lock.json` and `conformance/README.md` at runtime to pin the suite version and
the two-pin reconciliation structurally. Excluding the path it reads while shipping the reader would
make `cargo test` panic on the published crate, which is exactly the
`tests/team_contracts_conformance.rs` disposition. The two entries must NEVER be split: **ship both
or exclude both.** Write that sentence into the comment.

The comment must also state, in two sentences, why the other two dispositions were rejected here:
  - `tests/ci_severance_gate_wiring.rs` SHIPS because everything it reads
    (`.github/workflows/ci.yml`, `scripts/run-severance-proofs.sh`) also ships — consistency is the
    property, not exclusion;
  - `tests/v2_conformance_pin.rs` SHIPS while reading the EXCLUDED `.planning/` tree because it
    returns early on a missing path. That tolerance is deliberate there (the pin degrades to a
    no-op downstream) but is WRONG for the conformance reader, whose whole job is to fail when the
    wiring is absent — a reader that silently skips is the vacuous-gate defect this phase exists to
    prevent.

Then verify the tarball — capture first, assert the capture succeeded, THEN grep (D-19; the
pre-review spelling treated a FAILING `cargo package` as a PASS) — and confirm the two
tree-inspecting CI jobs are unaffected:
  - `cargo package -p pmcp --list --allow-dirty > target/118-02-package-list.txt` must exit 0, and
    the captured file must contain no line matching `conformance/|ci_conformance_gate_wiring`;
  - `make purity-check` must still exit 0 with the Node manifest in the tree (the Purity Gate is
    per-crate/per-feature over Cargo metadata, but confirm by running it rather than reasoning);
  - the `package-content` job in `.github/workflows/course.yml` tars only `pmcp-course/src/` and
    `pmcp-course/metadata.toml` — read it and confirm the new directory is outside its globs.
  </action>
  <verify>
    <automated>mkdir -p target && cargo package -p pmcp --list --allow-dirty > target/118-02-package-list.txt && ! grep -qE 'conformance/|ci_conformance_gate_wiring' target/118-02-package-list.txt</automated>
  </verify>
  <acceptance_criteria>
    - The `<automated>` command above exits 0. Note its shape: the `>` redirect preserves
      `cargo package`'s exit status, and `! grep -q` is the negative assertion — a FAILING
      `cargo package` now fails the check instead of reporting success.
    - `grep -cE '^conformance/' target/118-02-package-list.txt` returns 0 and
      `grep -c 'ci_conformance_gate_wiring' target/118-02-package-list.txt` returns 0.
    - `grep -c '"conformance/"' Cargo.toml` is >= 1 and
      `grep -c 'tests/ci_conformance_gate_wiring.rs' Cargo.toml` is >= 1.
    - The comment above the two entries contains the literal phrase "ship both or exclude both" and
      names BOTH rejected dispositions (`ci_severance_gate_wiring.rs`, `v2_conformance_pin.rs`).
    - `make purity-check` exits 0.
    - `grep -n 'pmcp-course' .github/workflows/course.yml` confirms the `package-content` tar globs
      are limited to `pmcp-course/`; record the verdict in the SUMMARY.
    - `wc -l < target/118-02-package-list.txt` is recorded in the SUMMARY alongside the pre-change
      count (obtain the pre-change count from `git stash`-free comparison against
      `git show HEAD:Cargo.toml` applied to a scratch checkout, or simply record the post-change
      count and the two excluded patterns), so a surprise mass-exclusion is visible.
  </acceptance_criteria>
  <done>Neither the Node manifest nor its future reader appears in `cargo package --list`, the reasoning and the two rejected dispositions are committed inline, and `make purity-check` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: conformance/README.md — the pin rationale, the ZERO-CHECK POLICY, and the two-pin reconciliation</name>
  <files>conformance/README.md</files>
  <read_first>
    - crates/mcp-tester/baselines/era-deltas.yaml — its header block (lines 1-68) is the in-repo
      role model for "a data/config artifact that explains itself to a reviewer, not to a machine".
    - .planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md — § Standard
      Stack (the version facts), § Pattern 1 (the two invocations and their exit-code semantics,
      lines 396-427), Pitfall 5 item 3 (the measured zero-check scenarios, lines 713-717), and
      Pitfall 6 (`--requirements` freezes MEMBERSHIP, not IMPLEMENTATION).
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-03, D-04, D-14
      (the scored-set boundary and the no-allowlist rule), and D-20 (the zero-check reconciliation
      obligation).
    - .planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md — the section where
      BOTH reviewers raised the zero-check gap independently (Codex HIGH #5; Gemini's
      `TOTAL_CHECKS >= 150` remedy). Read both before writing § 7 — the policy below is the
      adjudication of the two.
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md around
      line 591 — the `**Pinned sha**` row, for § 10.
  </read_first>
  <action>
Write `conformance/README.md` for a human reviewer arriving cold at a Node manifest in a Rust repo.
It must answer, in this order and with concrete values:

1. **What this directory is.** It pins the official `@modelcontextprotocol/conformance` suite, the
   external referee for CONF-01. It contains no source code and ships in no crate.
2. **Why a lockfile and not `npx`.** State D-01's argument: integrity hashes give commit-level
   reproducibility and survive a yanked or re-published version. Cite the in-repo precedent that
   an unpinned CLI plus a gitignored lock rotted the Purity Gate.
3. **Why this exact version and not `latest`.** `latest` is the 0.1.16 line, which has no
   `--requirements` flag and no `requirements/` directory, so the two-run shape D-04 requires is
   impossible on it. Name the pinned version.
4. **Why Node >= 22, and why `.npmrc`.** The suite imports `globSync` from `node:fs` at module scope
   and declares no `engines` of its own. Quote the exact `SyntaxError`. Then state plainly that
   `engines.node` alone only WARNS — `engine-strict=true` in `conformance/.npmrc` is what makes the
   wrong-Node install FAIL, and record that the refusal was proved by execution (Task 1 control (c)).
5. **Why `--ignore-scripts`.** `npm ci` runs lifecycle scripts by default, including TRANSITIVE
   ones; verifying the direct package's `postinstall` is empty does not cover the tree. Every
   install in this repo uses `npm ci --ignore-scripts`, and Task 1 proved the suite still runs
   afterwards.
6. **What the two runs prove.** One server process, two negotiations: `--requirements 2025-11-25`
   and `--requirements 2026-07-28`. Explain that a scenario applying to both must be run once under
   each and one run does not cover the other; and that D-06's single process is what makes it a
   claim about ONE binary rather than two.
7. **THE ZERO-CHECK POLICY — the canonical statement.** This section is normative for plans 118-04,
   118-05 and 118-08, which must all apply it identically; write it so a reader of any of those
   plans can be pointed here. State, as numbered rules:
   1. Some scored scenarios report `0 passed, 0 failed` and still render as a green tick. MEASURED
      on the pinned suite: `server-sse-polling` and `tasks-status-notifications`. This is a property
      of the SUITE, not of pmcp.
   2. Therefore a blanket rule "no scored scenario may report zero checks" is UNSATISFIABLE and is
      not the rule. It was the pre-review criterion in 118-04/118-05 and is hereby replaced.
   3. The rule is EXACT SET EQUALITY. `scripts/run-conformance-suite.sh` (plan 118-08) carries a
      committed closed list `ZERO_CHECK_SCENARIOS`, one entry per `<requirement-set>:<scenario>`
      pair with a one-line reason, populated from the measurements recorded by plans 118-04 and
      118-05. The driver compares the OBSERVED set of zero-check scored scenarios against that list
      and fails on ANY difference — a new zero-check scenario means something stopped being
      exercised; a listed scenario that starts reporting checks means the list is stale. Both
      directions fail, exactly like the era baseline.
   4. Independently, the driver asserts the TOTAL executed check count per requirement set is at
      least a hard-coded floor (`MIN_CHECKS_V1`, `MIN_CHECKS_V2`), recorded at pin time from
      `target/conformance-results/**/checks.json`. The floor is NEVER lowered; a re-pin raises it or
      restates it with the new measurement recorded in the re-pin commit.
   5. This is NOT a known-fail allowlist and does not violate D-03: every listed entry PASSES, the
      list records only where the referee ran no assertions, and rule 3 is bidirectional so it
      cannot hide a regression.
   6. Plans 118-04 and 118-05 apply the same rule at per-scenario measurement time: a scored
      scenario reporting zero checks is recorded as a FINDING in that plan's SUMMARY unless it is
      one of the measured entries, and the final list is AUTHORED in 118-08 from those recorded
      measurements — never invented.
8. **What can and cannot fail the job.** With `--requirements`, exit 1 iff a SCORED scenario has a
   FAILURE check; `not_scored` entries (reasons `extension`, `pending`, `added-after-release`) run,
   report, and cannot fail. That scored set is D-03's boundary per D-14. The Tasks extension is NOT
   implemented in the target example. The `-o target/conformance-results/` artifact is uploaded so
   not-scored results stay reviewable at re-pin time. State the output path explicitly and note that
   it lives under `target/` because `.gitignore` already ignores `target/` and does NOT ignore
   `results/` — writing to `results/` would dirty the worktree (D-20).
9. **What is FORBIDDEN.** No `--expected-failures` baseline and no known-fail allowlist (D-03). If a
   scenario genuinely does not apply, that is a conversation at re-pin time, not a standing
   exemption.
10. **THE TWO PINS, reconciled.** This repo now carries two independent pins to the same upstream
    project and a reader must not assume either subsumes the other:
    - `tests/v2_conformance_pin.rs` pins the conformance REPOSITORY at commit
      `a865118206d4d8cc8dbc5f5201607839281d0c3b` (recorded in `113-SPEC-RECHECK.md` § B.1) because
      HTTP-08's grading comes from a TypeScript predicate that has no specification sentence behind
      it.
    - `conformance/package-lock.json` pins the published npm PACKAGE at `<pin>`, built from upstream
      commit `<gitHead recorded in Task 1>`.
    State whether those two commits are the same, and if they are not, state which is newer and what
    that implies (the predicate the Phase-113 pin quotes may have moved in the package this phase
    runs). Give the reader the exact action for each case. Also state that `--requirements` freezes
    which scenarios are SCORED but NOT their implementations, which is why the lockfile and the
    requirement set are complementary rather than redundant.
11. **How to re-pin.** The exact command sequence: check `npm view ... dist-tags`, run
    `npm install --save-exact --ignore-scripts @modelcontextprotocol/conformance@<new>`, re-run
    `slopcheck install -e npm @modelcontextprotocol/conformance`, confirm `scripts.postinstall` is
    empty, re-read `gitHead` and update § 10, run `make test-conformance` locally, re-derive
    `ZERO_CHECK_SCENARIOS` and the two check floors, and review the diff in
    `target/conformance-results/`.
12. **How to run it locally.** Point at `make test-conformance` (created by plan 118-08) as the
    single local spelling, and state that the blocking enforcement lives in
    `.github/workflows/ci.yml`, not in the Makefile.
  </action>
  <verify>
    <automated>test -s conformance/README.md && grep -q -- '--requirements 2026-07-28' conformance/README.md && grep -q 'globSync' conformance/README.md && grep -q 'engine-strict' conformance/README.md && grep -q 'ignore-scripts' conformance/README.md && grep -q 'ZERO_CHECK_SCENARIOS' conformance/README.md && grep -q 'target/conformance-results' conformance/README.md && grep -q 'a865118206d4d8cc8dbc5f5201607839281d0c3b' conformance/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `conformance/README.md` exists and is at least 60 lines.
    - It contains the literal strings `--requirements 2025-11-25`, `--requirements 2026-07-28`,
      `globSync`, `--expected-failures`, `slopcheck install -e npm`, `engine-strict`,
      `--ignore-scripts`, `ZERO_CHECK_SCENARIOS`, `MIN_CHECKS_V1`, `MIN_CHECKS_V2`,
      `target/conformance-results`, `a865118206d4d8cc8dbc5f5201607839281d0c3b`, and the pinned
      version.
    - The pinned version string in the README matches
      `jq -r '.dependencies["@modelcontextprotocol/conformance"]' conformance/package.json`
      exactly — verified by command, not by eye, and the command recorded in the SUMMARY.
    - § 7 numbers all six rules and contains the literal sentence fragments "EXACT SET EQUALITY" and
      "is NOT a known-fail allowlist".
    - § 10 states explicitly whether the two pinned commits agree, and gives an action for each case.
    - `grep -c 'results/' conformance/README.md` counts only occurrences inside
      `target/conformance-results` — no bare `results/` path is recommended anywhere. State the
      audited lines in the SUMMARY.
    - `make quality-gate` exits 0 (confirms `make check-todos` sees no SATD in the new prose).
  </acceptance_criteria>
  <done>A reviewer arriving cold can learn the pin's rationale, the Node and lifecycle-script enforcement, the two-run semantics, the scored-set boundary, the ONE canonical zero-check policy, the two-pin reconciliation, and the exact re-pin procedure, without reading any plan document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> CI runner | Third-party JavaScript is downloaded and executed by a CI job |
| npm lifecycle scripts -> CI runner | `npm ci` executes install-time scripts from the WHOLE dependency tree, not just the direct package |
| Repo tree -> crates.io tarball | Anything not excluded ships to every downstream consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Spoofing | Dependency confusion / typosquat on `@modelcontextprotocol/conformance` | mitigate | Scoped `@modelcontextprotocol/*` name; `--save-exact` pin; committed lockfile with `sha512-` integrity hashes; `slopcheck install -e npm` re-run at pin time with `1 OK` required |
| T-118-06 | Tampering | Malicious or drifting lifecycle script in the suite OR IN A TRANSITIVE DEPENDENCY | mitigate | Every install is `npm ci --ignore-scripts` (never `npm install` in CI, never scripts enabled); Task 1 step 6(b) proves the suite still functions with scripts disabled, so the mitigation is not merely declared |
| T-118-07 | Tampering | Supply-chain drift from an unpinned CLI — the exact failure `project_ci_purity_gate_unpinned_tooling_drift` records | mitigate | Exact version pin (no caret/tilde, enforced by an acceptance criterion) + committed `package-lock.json`; re-pin is a reviewable commit that must also refresh § 10's `gitHead` |
| T-118-08 | Information Disclosure | CI tooling shipping to crates.io consumers | mitigate | `conformance/` excluded from the tarball, verified by a capture-then-assert `cargo package -p pmcp --list --allow-dirty` whose exit status is preserved (D-19) |
| T-118-09 | Denial of Service | A published crate whose `cargo test` panics on a missing path | mitigate | The reader `tests/ci_conformance_gate_wiring.rs` is excluded together with the path it reads; "ship both or exclude both" is written into the `Cargo.toml` comment, alongside the two rejected dispositions |
| T-118-54 | Repudiation | A green conformance run whose referee executed no assertions | mitigate | The canonical zero-check policy (README § 7): exact set equality against a committed, measured `ZERO_CHECK_SCENARIOS` list, plus hard-coded per-requirement-set check floors. Both reviewers raised this independently |
| T-118-55 | Spoofing | Two independent pins to the same upstream drifting apart unnoticed, so a predicate is graded at one commit and the scenarios at another | mitigate | README § 10 records both pins and their relation; plan 118-09's wiring test binds the README to the § B.1 sha literal so a silent divergence fails a test |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH § Package Legitimacy Audit disposition is **Approved** / `[OK]` (OIDC trusted publishing, MCP-org maintainers, 27.5k weekly downloads, empty postinstall). No `[ASSUMED]`/`[SUS]` package is installed, so no blocking human checkpoint is required; the slopcheck re-run at pin time plus `--ignore-scripts` are the standing controls |
</threat_model>

<verification>
- `npm ci --prefix conformance --ignore-scripts` from a clean `node_modules` installs exactly the
  pinned version under Node 22 and FAILS under Node 20.
- `cargo package -p pmcp --list --allow-dirty` succeeds and lists neither `conformance/` nor
  `tests/ci_conformance_gate_wiring.rs`.
- `make purity-check` exits 0.
- `make quality-gate` exits 0.

**D-19 note.** The Task-2 verification captures `cargo package --list` to a file with `>` (which
preserves cargo's exit status) and then asserts with `! grep -q`. The pre-review spelling
(`cargo package … | grep …; test $? -eq 1`) reported PASS when `cargo package` FAILED. Do not
restore it.

**Latency expectation (not a hang).** `npm ci --prefix conformance --ignore-scripts`,
`make purity-check` and `make quality-gate` are all multi-minute — expect **10-25 minutes** for the
trio. Let them finish; do not interrupt and retry, and do not add a watch-mode or
`--no-fail-fast`-style flag to make them feel faster.
</verification>

<success_criteria>
- The suite is pinned to a `--requirements`-capable version with a committed lockfile.
- The Node 22 floor is ENFORCED (not merely declared) by a committed `.npmrc`, proved by a failing
  Node-20 install.
- Lifecycle scripts are disabled on every install, and the suite is proved to work that way.
- The Node manifest and its future reader are both out of the published tarball.
- `conformance/README.md` is the single normative statement of the zero-check policy, the scored-set
  boundary, the no-allowlist rule, the two-pin reconciliation, and the re-pin procedure.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-02-SUMMARY.md` when done.
Record: the chosen pin and why; the five legitimacy re-checks verbatim including `gitHead`; the
Node-20 refusal transcript; the `conformance list` transcript after a `--ignore-scripts` install;
the `cargo package --list` entry count; the `package-content` / Purity Gate verdicts; and the § 10
verdict on whether the two pinned commits agree.
</output>
