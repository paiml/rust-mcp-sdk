---
phase: 118-conformance-against-the-official-suite
plan: 09
type: execute
wave: 5
depends_on: ["118-08"]
files_modified:
  - .github/workflows/ci.yml
  - tests/ci_conformance_gate_wiring.rs
  - tests/ci_severance_gate_wiring.rs
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]

must_haves:
  truths:
    - "A failing official-suite run turns the org-required `gate` check RED"
    - "A failing era-matrix run turns the org-required `gate` check RED"
    - "Each new job is wired into `gate` in all FOUR places: needs, the env binding, the if chain, and the failure echo"
    - "The reader proves a BIJECTION: for every job in gate.needs there is exactly one env binding of that job's result, that same variable is read in the conditional, and the failure-echo line carries the job=$VAR pair"
    - "The wiring is proved by PARSING .github/workflows/ci.yml with serde_yaml, never by string-matching it"
    - "The `feature-flags` job remains absent from gate.needs, keeping the file's live negative control honest"
    - "The suite runs on Node 22 provisioned by actions/setup-node with npm caching keyed on the committed lockfile"
    - "Both new jobs carry timeout-minutes set ABOVE the driver script's own budget, so the script's diagnostic failure wins the race"
    - "The results artifact is uploaded on GREEN runs too, so not-scored results stay reviewable at re-pin time"
    - "The Phase-109 team-servers harness is EXECUTED by a blocking CI job — today it runs in none"
    - "The reader binds conformance/README.md to the pre-existing conformance-repo SHA pin, so the two upstream pins cannot silently diverge"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "two new blocking jobs plus the four gate edits for each"
      contains: "setup-node"
    - path: "tests/ci_conformance_gate_wiring.rs"
      provides: "structural proof that both jobs block merge, that the scripts carry their fences, and that the two upstream pins are reconciled"
      contains: "MINIMUM_GATE_NEEDS"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/run-conformance-suite.sh"
      via: "a one-line run: step, so the commands stay pinnable as data"
      pattern: "run-conformance-suite.sh"
    - from: "tests/ci_conformance_gate_wiring.rs"
      to: ".github/workflows/ci.yml"
      via: "serde_yaml parse, then a bijective job -> env -> conditional -> echo mapping"
      pattern: "serde_yaml"
    - from: "tests/ci_conformance_gate_wiring.rs"
      to: "conformance/README.md"
      via: "the SPEC_RECHECK_PINNED_SHA literal, binding the npm pin's README to the Phase-113 conformance-repo pin"
      pattern: "SPEC_RECHECK_PINNED_SHA"
---

<objective>
Make the conformance claim capable of failing. Add two BLOCKING CI jobs — one for the official suite
(CONF-01), one for the era matrix (CONF-02/CONF-03) — wire each into the `gate` aggregate in all
four required places (D-02), and prove that wiring STRUCTURALLY from the workflow file, as a
BIJECTION rather than as a set of independent counts.

Purpose: *a conformance claim that cannot fail is not a claim.* Two measured facts make this the
riskiest plan in the phase. First, `gate.needs` does NOT list `security_audit` or `workspace-test`;
both were RED on PR #319 while `gate` was GREEN, so adding a job to `ci.yml` does NOT make it block.
Second, the Phase-109 team-servers harness runs in **zero** CI jobs today: `ci.yml`'s `test` job is
scoped to the root `pmcp` package, `org-gate-checks.yml`'s `workspace-test` is `--lib --bins` (which
excludes `tests/`) and is itself absent from `gate.needs`, and `make quality-gate` never reaches
`crates/pmcp-team-servers/tests/`. D-15 puts CONF-02's execution in the NEW job for exactly that
reason, keeping the deferred `gate.needs` widening genuinely deferred.

Output: two blocking jobs, eight gate edits, a bijective structural wiring tripwire, the two-pin
binding, and two raised floors in the existing severance wiring test.

## Review corrections applied (D-20)

- **The reader was weaker than its stated guarantee.** `env.len() >= needs.len()` does not prove a
  one-to-one mapping, and `run.contains(job_name)` does not prove the failure echo NAMES the job.
  Task 3 replaces both with a bijection: for EVERY job in `gate.needs`, find exactly one `env:`
  binding whose expression is that job's `result`, assert that same variable is READ in the
  conditional, and assert the failure-echo line carries the `job=$VAR` pair.
- **Task 3 was self-contradictory.** The pre-review version required the module doc to explain *why
  PyYAML was rejected* and simultaneously required `grep -ciE 'python|pyyaml|yq '` over that same
  file to return 0. Resolved: the rejection rationale is written WITHOUT naming the interpreter —
  it argues from "an undeclared, unversioned, out-of-band interpreter package" and cites
  `tests/ci_severance_gate_wiring.rs:44-59`, where the named argument already lives. The grep stays
  at 0, and a reader who wants the name follows the citation.
- **The job-count floor was set below the actual count.** `MINIMUM_JOBS` is currently `8` against
  10 real jobs. Post-change there are 12; the floor becomes 12, not 10.
- **Timeouts.** Both new jobs get `timeout-minutes`, set above the driver script's own budget.
- **npm caching** (Gemini, LOW but free): `cache: 'npm'` with
  `cache-dependency-path: 'conformance/package-lock.json'`.
- **The artifact path** is `target/conformance-results/`, matching plans 118-04/05/08.
- **The two upstream pins are bound.** `tests/v2_conformance_pin.rs` pins the conformance REPOSITORY
  at `a865118206d4d8cc8dbc5f5201607839281d0c3b`; `conformance/package-lock.json` pins the npm
  PACKAGE. Plan 118-02 § 10 reconciles them in prose; this plan binds that prose mechanically.
- **D-19.** No verification command masks the exit status of what it verifies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md
@.planning/phases/118-conformance-against-the-official-suite/118-02-SUMMARY.md
@.planning/phases/118-conformance-against-the-official-suite/118-08-SUMMARY.md

<interfaces>
<!-- Current state of ci.yml, measured 2026-08-09. Do NOT re-derive. -->

Jobs present today (10): test, feature-flags, quality-gate, benchmarks, msrv, purity-check,
v1-severance, pmcp-agent-targets, wasm32-purity, gate.  After this plan: 12.

The gate job's current shape (ci.yml:531-554):

  gate:
    runs-on: ubuntu-latest
    needs: [test, quality-gate, purity-check, pmcp-agent-targets, wasm32-purity, v1-severance]
    if: always()
    steps:
      - name: Evaluate required checks
        env:
          TEST_RESULT / QG_RESULT / PURITY_RESULT / AGENT_TARGETS_RESULT / WASM32_RESULT /
          SEVERANCE_RESULT, each bound to a `needs.<job>.result` expression
        run: |
          a shell `if` chain comparing every bound variable to "success", an echo naming every job
          and its result as `job=$VAR`, then `exit 1`; a trailing "All required checks passed." echo

FOUR edits per new job — the echo is the fourth, and ci_severance_gate_wiring.rs:588-596 asserts it:
  1. append the job name to `needs:`
  2. add a `<NAME>_RESULT` binding to `env:` bound to that job's result expression
  3. add a `[[ "$<NAME>_RESULT" != "success" ]]` clause to the `if` chain
  4. add `<job>=$<NAME>_RESULT` to the `Required checks failed: ...` echo

The blocking-job template (`v1-severance`, ci.yml:395-433): actions/checkout@v7,
dtolnay/rust-toolchain@stable, actions/cache@v6 with a JOB-PRIVATE cache key plus restore-keys, a
numbered rationale comment above the job body, and a one-line `run:` that calls the script.
It carries NO `timeout-minutes` — the new jobs do, and that deliberate divergence must be explained
in their rationale blocks rather than silently differing from the template.

Existing constants in tests/ci_severance_gate_wiring.rs (662 lines) that must be RAISED:
  const MINIMUM_GATE_NEEDS: usize = 6;   // :84  ->  8  (two new jobs)
  const MINIMUM_JOBS: usize = 8;         // :91  -> 12  (the floor was already BELOW the real 10;
                                         //               set it to the actual post-change count)
  const NON_BLOCKING_JOB: &str = "feature-flags";   // :76 — the LIVE negative control; keep it OUT

Reusable elements of tests/ci_severance_gate_wiring.rs to transfer:
  - the workflow parsed with `serde_yaml`, NEVER string-matched (a comment would "find" a job name)
  - `step_script_containing(job, needle)` asserting EXACTLY ONE matching `run:` step — fences are
    properties of a COMMAND, not of a job
  - comment-stripping before forbidden-flag checks
  - the `raw` vs `floored` reader split (`gate_needs_raw` / `gate_needs`)
  - the mutual-consistency test (:551-597) — the STARTING POINT for the bijection, not the end
  - the live negative control asserting `feature-flags` stays out of `gate.needs` (:603-625)
  - the module doc's headings, including `CORRECTION-116-DOC` and the interpreter rejection at
    :44-59

`serde_yaml = "0.9"` is a root dev-dependency (Cargo.toml:194). Do NOT shell out to an interpreter
anywhere, including in a verification command.

Artifact upload: `ci.yml` has NO `upload-artifact` step at all. The nearest in-repo shape is
.github/workflows/fuzz.yml:69-81, which uses `if: failure()` — the WRONG condition for D-14, which
needs the not-scored `extension`/`pending` results visible on GREEN runs too.

No `actions/setup-node` exists anywhere in `.github/workflows/` — this is the first.

Packaging disposition, already decided and landed by plan 118-02: `conformance/` and
`tests/ci_conformance_gate_wiring.rs` are BOTH in the root Cargo.toml `exclude` array
("ship both or exclude both"). This plan VERIFIES that and must not change it. The new reader also
reads `conformance/README.md`, which is under the same excluded directory — consistent.

The two upstream pins to bind:
  .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md:591
      | **Pinned sha** | `a865118206d4d8cc8dbc5f5201607839281d0c3b` |
  conformance/README.md § 10 (plan 118-02) records that sha AND the npm package's `gitHead`, and
  states whether they agree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two blocking CI jobs, each with a numbered rationale block, Node 22, caching and a timeout</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — read lines 311-440 (the `purity-check` job and the whole
      `v1-severance` job INCLUDING its rationale comment) and lines 525-560 (the `gate` job). You
      are editing this file; read its current state rather than assuming.
    - .github/workflows/fuzz.yml lines 60-85 — the only `upload-artifact` shape in the repo, and
      the `if: failure()` condition that is WRONG to copy here.
    - scripts/run-conformance-suite.sh and scripts/run-era-matrix.sh — the two scripts from plan
      118-08, so the `run:` steps invoke them and nothing else, and so each job's `timeout-minutes`
      is set ABOVE the script's own total budget rather than below it.
    - conformance/package.json and conformance/.npmrc — the `engines.node` floor and the
      `engine-strict` enforcement the job must satisfy.
    - .planning/phases/118-conformance-against-the-official-suite/118-08-SUMMARY.md — the scripts'
      environment requirements, the port they bind, the named per-run and total timeouts, and the
      measured wall-clock of a successful run, which is the input to choosing `timeout-minutes`.
  </read_first>
  <behavior>
    Job `conformance-suite`:
      checkout -> actions/setup-node@v4 (node-version 22, `cache: 'npm'`,
      `cache-dependency-path: 'conformance/package-lock.json'`) -> install Rust -> cache cargo with a
      job-private key -> `./scripts/run-conformance-suite.sh` as a ONE-LINE run step -> upload
      `target/conformance-results/` with `if: always()`. `PMCP_REQUEST_STATE_KEY` supplied as
      job-level `env:` from a fixed non-production value or a repository secret, never echoed.
      Carries `timeout-minutes`.

    Job `era-matrix`:
      checkout -> install Rust -> cache cargo with its own job-private key ->
      `./scripts/run-era-matrix.sh` as a ONE-LINE run step. Carries `timeout-minutes`.

    Neither job carries `continue-on-error`, `|| true`, or any conditional that could let it pass
    without running. Neither requests elevated `permissions:`.
  </behavior>
  <action>
Add two jobs to `.github/workflows/ci.yml`, placed after `v1-severance` and before `gate`, each
preceded by a numbered rationale comment block in the style of `ci.yml:344-394`.

`conformance-suite` rationale block must number and name at least these fences and their
false-greens:
  1. `actions/setup-node@v4` with `node-version: 22` — the first `setup-node` in this repo. The
     suite imports `globSync` from `node:fs` at module scope and the published package declares NO
     `engines`, so a Node-20 runner would pass a naive install and die at load with a `SyntaxError`.
     `conformance/.npmrc`'s `engine-strict=true` makes the install itself fail on Node 20; pinning
     the runner's Node is the first line of defence and the `.npmrc` is the second.
  2. `cache: 'npm'` keyed on `conformance/package-lock.json` — the lockfile is committed and exact,
     so the cache key is exact too. This removes a per-run network fetch and its flake; it does NOT
     weaken the pin, because `npm ci` still validates every integrity hash.
  3. `npm ci --ignore-scripts` (inside the script, not inline) — the committed lockfile is
     authoritative and lifecycle scripts run for the WHOLE dependency tree, not just the direct
     package.
  4. The commands live in `scripts/run-conformance-suite.sh`, not inline, so
     `tests/ci_conformance_gate_wiring.rs` can pin them AS DATA — including the vacuity guards, the
     zero-check gate and the check floors, none of which can live inside a `#![cfg]`-selected test
     file.
  5. `--requirements`, not `--spec-version`, and no `--expected-failures` — D-03 admits no
     known-fail allowlist; D-14 fixes the boundary at the suite's SCORED set.
  6. ONE server process for both runs (D-06), started from the BUILT BINARY and torn down by
     process group, because two processes would prove "pmcp can serve v1" and "pmcp can serve v2"
     separately rather than that one binary does both.
  7. `if: always()` on the artifact upload, NOT `if: failure()` — D-14 needs the not-scored
     `extension`/`pending` results reviewable on GREEN runs, which is precisely when a
     failure-conditioned upload would skip.
  8. `timeout-minutes`, set ABOVE the script's own total budget so the script's diagnostic failure
     wins the race and the job-level timeout is only the backstop. State BOTH numbers in the comment
     and say which is which — a job timeout BELOW the script budget would replace an actionable
     message with an opaque cancellation. Note explicitly that `v1-severance` has no
     `timeout-minutes` and that this is a deliberate divergence from that template, not an oversight.
  9. This job is in `gate.needs` and therefore BLOCKS merge; it must never be made advisory.

`era-matrix` rationale block must number and name at least:
  1. Why this job exists at all: **no CI job runs `crates/pmcp-team-servers/tests/` today.** The
     `test` job is scoped to the root `pmcp` package; `org-gate-checks.yml`'s `workspace-test` is
     `--lib --bins`, which excludes `tests/`, and is itself absent from `gate.needs`;
     `make quality-gate` never reaches that crate. Adding an era matrix to a harness nothing
     executes gates nothing.
  2. Why it is NOT wired by widening `workspace-test`: that job's absence from `gate.needs` is an
     explicitly DEFERRED item, and D-15 keeps it deferred by giving CONF-02 a gate it owns.
  3. The dev-dependency-free build fences and the nonzero-test-count guards live in the script,
     outside the compilation unit, because a `cfg!`-based guard inside a `#![cfg]`-selected file
     expands to `!false` and cannot fail.
  4. `era_matrix` and `era_baseline` need `--features http` (not in the crate's default set) and
     omitting it yields `running 0 tests` with exit 0 — the guard in the script is what catches it.
  5. `timeout-minutes`, same reasoning as `conformance-suite` fence 8.

Copy from the `v1-severance` template exactly: `actions/checkout@v7`,
`dtolnay/rust-toolchain@stable`, `actions/cache@v6` with a JOB-PRIVATE cache key
(`${{ runner.os }}-cargo-conformance-` and `${{ runner.os }}-cargo-era-matrix-`) plus
`restore-keys`, and a one-line `run:` calling the script.

For the artifact upload use `actions/upload-artifact@v7` with `if: always()`, a name including the
run identifier, and `path: target/conformance-results/`.

Supply `PMCP_REQUEST_STATE_KEY` as a job-level `env:` mapping from a fixed non-production
64-character test value or a repository secret. Never `echo` it and never place it on a command line.

Do NOT add `security_audit` or `workspace-test` to anything — both are explicitly deferred and out
of scope for this phase.
  </action>
  <verify>
    <automated>mkdir -p target && bash -o pipefail -c 'cargo test --features full --test ci_severance_gate_wiring 2>&1 | tee target/118-09-t1.log' && grep -qE '^running [1-9][0-9]* tests?$' target/118-09-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  conformance-suite:' .github/workflows/ci.yml` returns 1 and
      `grep -c '^  era-matrix:' .github/workflows/ci.yml` returns 1.
    - `grep -c 'actions/setup-node@v4' .github/workflows/ci.yml` is >= 1, the adjacent
      `node-version` value is `22` or higher, and the same step carries `cache: 'npm'` and
      `cache-dependency-path: 'conformance/package-lock.json'`.
    - `grep -c 'actions/upload-artifact' .github/workflows/ci.yml` is >= 1, the same step carries
      `if: always()` — NOT `if: failure()` — and its `path:` is `target/conformance-results/`. State
      the line numbers in the SUMMARY.
    - Both new jobs carry `timeout-minutes`, and each value is GREATER than the corresponding
      script's total budget recorded in 118-08-SUMMARY.md. State the four numbers (two budgets, two
      job timeouts) in the SUMMARY.
    - `grep -cE 'continue-on-error|\|\| true' .github/workflows/ci.yml` does not exceed
      `git show HEAD:.github/workflows/ci.yml | grep -cE 'continue-on-error|\|\| true'`; record both
      numbers.
    - Each new job's `run:` steps invoke exactly the two scripts and nothing else:
      `grep -c 'run-conformance-suite.sh' .github/workflows/ci.yml` is >= 1 and
      `grep -c 'run-era-matrix.sh' .github/workflows/ci.yml` is >= 1.
    - The cache keys are job-private: `grep -c 'cargo-conformance-' .github/workflows/ci.yml` is
      >= 1 and `grep -c 'cargo-era-matrix-' .github/workflows/ci.yml` is >= 1.
    - Neither job requests elevated permissions:
      `git diff .github/workflows/ci.yml | grep -cE '^\+\s*permissions:'` returns 0.
    - `PMCP_REQUEST_STATE_KEY`'s value is never echoed: no line in `ci.yml` both names the variable
      and contains `echo`. State the audited lines in the SUMMARY.
    - `git diff .github/workflows/ci.yml | grep -cE '^\+.*(security_audit|workspace-test)'` returns 0.
    - The workflow still parses: `cargo test --features full --test ci_severance_gate_wiring` exits
      0 — its `serde_yaml` reader is the parse check. Do NOT introduce an out-of-band interpreter.
  </acceptance_criteria>
  <done>Both jobs exist with numbered rationale blocks, provision Node 22 with lockfile-keyed npm caching, invoke only the two scripts, carry a timeout above the script budget, upload results on every run, and carry no advisory escape hatch.</done>
</task>

<task type="auto">
  <name>Task 2: The eight gate edits — needs, env, if chain, and failure echo, twice</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — the `gate` job as it stands after Task 1, lines ~525-560.
    - tests/ci_severance_gate_wiring.rs lines 551-597 — the test that asserts wirings 2 and 3 for
      MUTUAL CONSISTENCY (the env var that is BOUND must be the one that is READ) and asserts the
      failure echo names the job. That test defines what "correctly wired" means today; Task 3
      strengthens it into a bijection, so make these edits satisfy the STRONGER form by
      construction: exactly one binding per job, the bound name read in the conditional, and a
      `job=$VAR` pair on the echo line.
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `.github/workflows/ci.yml — the THREE gate edits`, which states that it is actually FOUR and
      warns that `MINIMUM_GATE_NEEDS` and `MINIMUM_JOBS` ripple into the existing severance test.
  </read_first>
  <behavior>
    After this task, `gate.needs` has 8 entries; `gate`'s evaluation step has 8 env bindings, an
    8-clause `if` chain, and a failure echo naming all 8 jobs as `job=$VAR` pairs. Every bound
    variable is read exactly once in the conditional, and every read variable is bound exactly once.
  </behavior>
  <action>
Apply all FOUR edits for EACH of the two new jobs to the `gate` job.

1. `needs:` — append `conformance-suite` and `era-matrix`, taking the list from 6 entries to 8.
2. `env:` — add a `CONFORMANCE_RESULT` binding to the `conformance-suite` job's result expression
   and an `ERA_MATRIX_RESULT` binding to the `era-matrix` job's result expression, following the
   exact form the existing six bindings use. Exactly ONE binding per job — a second binding of the
   same result would break the bijection Task 3 asserts.
3. The shell `if` chain — add `[[ "$CONFORMANCE_RESULT" != "success" ]]` and
   `[[ "$ERA_MATRIX_RESULT" != "success" ]]` clauses, preserving the existing line-continuation
   style so the final clause remains the one without a trailing continuation.
4. The `Required checks failed: ...` echo — append `conformance-suite=$CONFORMANCE_RESULT` and
   `era-matrix=$ERA_MATRIX_RESULT` so a failing gate names the cause. Use the job's WORKFLOW NAME on
   the left of the `=`, exactly as the existing six do; Task 3's reader matches on that pair.

Adding to `needs:` alone produces an *awaited but unchecked* job, which is strictly worse than not
adding it at all: the gate waits for the job and then ignores its result. All four edits or none.

Do not reorder or rename the existing six entries, and do not touch `if: always()` on the gate.
  </action>
  <verify>
    <automated>mkdir -p target && bash -o pipefail -c 'cargo test --features full --test ci_severance_gate_wiring 2>&1 | tee target/118-09-t2.log' && grep -qE '^running [1-9][0-9]* tests?$' target/118-09-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - The `gate` job's `needs:` list contains exactly 8 entries including `conformance-suite` and
      `era-matrix`; quote the line in the SUMMARY.
    - `grep -c 'CONFORMANCE_RESULT' .github/workflows/ci.yml` is exactly 3 (env binding, `if`
      clause, echo) and the same for `ERA_MATRIX_RESULT`. Exactly three, not "at least" — a fourth
      occurrence means a duplicate binding or a stray reference, and the bijection would fail.
    - The `if` chain's clause count equals the `env` binding count equals the `needs` length equals
      8 — state all four counts in the SUMMARY.
    - The failure echo names both new jobs by their workflow job name (`conformance-suite=` and
      `era-matrix=`).
    - `feature-flags` is still ABSENT from `gate.needs`:
      `grep -A 3 '^  gate:' .github/workflows/ci.yml | grep -c 'feature-flags'` returns 0.
    - `security_audit` and `workspace-test` are still absent from `gate.needs` — the deferred item
      stays deferred.
    - `cargo test --features full --test ci_severance_gate_wiring` still exits 0 (its floors are
      raised in Task 3; until then it must at least still parse and pass its consistency tests).
  </acceptance_criteria>
  <done>Both jobs are awaited, bound exactly once, evaluated and named in the failure echo, with the existing six wirings and the live negative control untouched.</done>
</task>

<task type="auto">
  <name>Task 3: tests/ci_conformance_gate_wiring.rs — the bijective reader, the two-pin binding, and the raised floors</name>
  <files>tests/ci_conformance_gate_wiring.rs, tests/ci_severance_gate_wiring.rs</files>
  <read_first>
    - tests/ci_severance_gate_wiring.rs — read the WHOLE file (662 lines). It is the single
      highest-value file to copy in this phase. Take: the module doc's headings
      (`CORRECTION-116-DOC`, the live counter-example, why THREE wirings not one, why the workflow
      is PARSED not string-matched, and why the out-of-band interpreter route was REJECTED at
      :44-59); the named constants and their non-vacuity floors; `parse_workflow` with its
      actionable panic; `step_script_containing` and why it exists; `proof_script_commands`'s
      comment-stripping; the `gate_needs_raw` / `gate_needs` split; the mutual-consistency test at
      551-597; the live negative control at 603-625; and the `env.len() >= needs.len()` invariant at
      631-662 — which you are REPLACING with something stronger, so read exactly what it does and
      does not prove.
    - .github/workflows/ci.yml — post-Task-2 state.
    - scripts/run-conformance-suite.sh and scripts/run-era-matrix.sh — the arrays and constants this
      test pins as data: `REQUIREMENT_SETS`, `ZERO_CHECK_SCENARIOS`, `MIN_CHECKS_V1`,
      `MIN_CHECKS_V2`, `MATRIX_TESTS`, and the two build fences.
    - conformance/README.md § 10 — the two-pin reconciliation this test binds.
    - Cargo.toml — confirm `tests/ci_conformance_gate_wiring.rs` and `conformance/` are BOTH in the
      `exclude` array (plan 118-02 added them). Do not change that disposition.
  </read_first>
  <behavior>
    `tests/ci_conformance_gate_wiring.rs` must FAIL when:
      - either new job is missing from `ci.yml`
      - either new job is missing from `gate.needs`
      - a job in `gate.needs` has zero, or more than one, matching `env:` binding
      - a bound env var is not READ in the `if` chain
      - the failure-echo line does not carry the `<job>=$<VAR>` pair for some job in `gate.needs`
      - `gate.needs` shrinks below `MINIMUM_GATE_NEEDS`
      - the parsed job count drops below `MINIMUM_JOBS`
      - either job stops invoking its script, or invokes it from more than one step
      - either new job loses its `timeout-minutes`
      - the artifact upload's condition is not `always()`
      - `scripts/run-conformance-suite.sh` stops declaring BOTH requirement-set names
      - `scripts/run-conformance-suite.sh` stops declaring `ZERO_CHECK_SCENARIOS`, `MIN_CHECKS_V1`
        or `MIN_CHECKS_V2`
      - `scripts/run-era-matrix.sh` stops declaring all three matrix test targets, or drops
        `--features http` from either target that needs it
      - a forbidden flag appears in either script's COMMANDS (after comment-stripping)
      - `conformance/README.md` stops naming `SPEC_RECHECK_PINNED_SHA`
      - `feature-flags` appears in `gate.needs` (the live negative control)
  </behavior>
  <action>
Create `tests/ci_conformance_gate_wiring.rs`, transferring `tests/ci_severance_gate_wiring.rs`'s
structure section by section, and raise the two floors in that existing file.

**Named constants for the new test:**
  - `WORKFLOW` / `WORKFLOW_REL` — as in the analog, `CARGO_MANIFEST_DIR`-anchored
  - `CONFORMANCE_JOB: &str = "conformance-suite"`, `ERA_MATRIX_JOB: &str = "era-matrix"`,
    `GATE_JOB: &str = "gate"`
  - `NON_BLOCKING_JOB: &str = "feature-flags"` — reuse the SAME live negative control
  - `MINIMUM_GATE_NEEDS: usize = 8`
  - `MINIMUM_JOBS: usize = 12` — the ACTUAL post-change job count. The analog's `8` was a floor
    below its own reality; a floor that is already slack cannot notice a deletion. Doc comment must
    say so and forbid lowering.
  - `CONFORMANCE_SCRIPT_REL` / `ERA_MATRIX_SCRIPT_REL` plus their `CARGO_MANIFEST_DIR`-anchored
    absolute twins
  - `REQUIREMENT_SETS: &[&str] = &["2025-11-25", "2026-07-28"]` — D-04 as DATA
  - `MATRIX_TESTS: &[&str] = &["conformance", "era_matrix", "era_baseline"]`
  - `HTTP_FEATURE_TARGETS: &[&str] = &["era_matrix", "era_baseline"]` — assert the era script
    carries `--features http` for exactly these
  - `ZERO_CHECK_DECLARATIONS: &[&str] = &["ZERO_CHECK_SCENARIOS", "MIN_CHECKS_V1", "MIN_CHECKS_V2"]`
  - `DEV_DEP_FREE_FENCES: &[&str] = &["-p pmcp-team-servers", "--all-features",
    "--no-default-features", "cargo build"]` — D-09's two build fences
  - `CONFORMANCE_FORBIDDEN_FLAGS: &[&str] = &["--expected-failures", "--spec-version", "--suite",
    "--all-features"]` and `ERA_MATRIX_FORBIDDEN_FLAGS: &[&str] = &["|| true", "continue-on-error"]`
  - `SPEC_RECHECK_PINNED_SHA: &str = "a865118206d4d8cc8dbc5f5201607839281d0c3b"` — the Phase-113
    conformance-repository pin, carried here as this fence's OWN literal (the standing
    `D-115-AI(4)` rule: a fence never derives its expected value from the artifact under test)
  - `CONFORMANCE_README_REL: &str = "conformance/README.md"`

**The reader.** Parse the workflow with `serde_yaml` through a `LazyLock` with an actionable panic.
NEVER string-match the workflow for a job name: a comment would "find" it.

**THE BIJECTION TEST — the core of this task.** Replace the analog's `env.len() >= needs.len()`
invariant with a genuine one-to-one mapping. For EVERY job name in `gate.needs`:
  1. collect the `env:` entries of the gate's evaluation step whose VALUE is that job's `result`
     expression, and assert there is EXACTLY ONE. Zero means the job is AWAITED but NEVER CHECKED;
     more than one means two variables carry the same result and a later edit can silently drop the
     one the conditional reads;
  2. take that binding's variable NAME and assert the `if` chain READS it — match the actual
     `[[ "$NAME" != "success" ]]` shape, not merely a substring occurrence of `NAME`;
  3. assert the failure-echo LINE contains the `<job>=$<NAME>` pair — the job name on the left, the
     SAME variable on the right. `run.contains(job_name)` is not sufficient: a comment or an
     unrelated mention would satisfy it.
Then assert the reverse direction: every `<NAME>_RESULT`-shaped binding in that step corresponds to
a job in `gate.needs`, so a stale binding for a removed job is also a failure.
Carry the analog's "AWAITED but NEVER CHECKED" failure wording into the case-1 message.

**The other tests to transfer:**
  - both new jobs exist in the parsed `jobs` map, and both appear in `gate.needs`
  - `step_script_containing(job, script_rel)` asserting EXACTLY ONE `run:` step per job invokes its
    script — zero means the command was renamed and every fence below would assert against nothing;
    more than one means a fence could be satisfied by a DIFFERENT command than the one it names
  - both new jobs declare `timeout-minutes` (read it from the parsed job map, not by grep)
  - the `conformance-suite` job has exactly one `upload-artifact` step and its `if:` is `always()`
  - the scripts' arrays pinned as data: every member of `REQUIREMENT_SETS`, `MATRIX_TESTS` and
    `ZERO_CHECK_DECLARATIONS` appears in the corresponding script source, and each member of
    `HTTP_FEATURE_TARGETS` appears on a line that also carries `--features http`
  - `DEV_DEP_FREE_FENCES`: every member appears in the era script
  - the forbidden-flag loops, run over COMMENT-STRIPPED script text (both scripts' headers explain
    at length why these flags are forbidden, so an unstripped grep would find the words in the prose
    and self-invalidate)
  - the era script declares the nonzero-test-count guard: the literal `running [1-9]` pattern
    appears in its source
  - **the two-pin binding**: `conformance/README.md` contains `SPEC_RECHECK_PINNED_SHA`. Failure
    message: the repo carries two independent pins to the same upstream project — the Phase-113
    conformance-REPOSITORY commit and this phase's npm PACKAGE lockfile — and § 10 of that README is
    the only place their relationship is recorded. If the sha is gone, either the README was
    rewritten or the Phase-113 pin moved, and in both cases a human must re-establish which commit
    grades HTTP-08
  - the live negative control, copied unchanged: `feature-flags` still exists as a job AND is still
    absent from `gate.needs`. Copy the analog's explanation — with every job wired, a reader that
    answered "yes, it's wired" to everything would pass all the tests in the file

**Module doc** must restate, in the analog's voice: blocking status is proved from the WORKFLOW,
never from the Makefile (`CORRECTION-116-DOC`); the live counter-example and why it is kept; why
FOUR wirings and not one, and why a BIJECTION and not a count; why the workflow is PARSED and not
string-matched; and why the out-of-band interpreter route was rejected — `serde_yaml` is already a
root dev-dependency, costing zero new packages, while the alternative is an undeclared, unversioned
package installed outside the project's dependency graph, with the full argument recorded at
`tests/ci_severance_gate_wiring.rs:44-59`. **Do NOT name that interpreter or its library in this
file** — the acceptance criteria assert the name is absent, and the citation is how a reader gets
it. (The pre-review plan required both the name and its absence; this is the resolution.)

**Then edit `tests/ci_severance_gate_wiring.rs`:** raise `MINIMUM_GATE_NEEDS` from 6 to 8 and
`MINIMUM_JOBS` from 8 to 12, updating each doc comment to record that Phase 118 added two jobs, that
the old `MINIMUM_JOBS` was already slack against the then-current 10, and the standing rule — if the
floor fires, FIX THE READER or restore the workflow; never lower the floor. Change nothing else in
that file; its `feature-flags` negative control and its consistency tests must keep passing
untouched.
  </action>
  <verify>
    <automated>mkdir -p target && bash -o pipefail -c 'cargo test --features full --test ci_conformance_gate_wiring 2>&1 | tee target/118-09-t3a.log' && grep -qE '^running [1-9][0-9]* tests?$' target/118-09-t3a.log && bash -o pipefail -c 'cargo test --features full --test ci_severance_gate_wiring 2>&1 | tee target/118-09-t3b.log' && grep -qE '^running [1-9][0-9]* tests?$' target/118-09-t3b.log</automated>
  </verify>
  <acceptance_criteria>
    - Both test binaries exit 0 with nonzero test counts (asserted by the `<automated>` command
      under `-o pipefail`).
    - `grep -c 'serde_yaml' tests/ci_conformance_gate_wiring.rs` is >= 1 and
      `grep -ciE 'python|pyyaml|yq ' tests/ci_conformance_gate_wiring.rs` returns 0 — the two are no
      longer in conflict, because the rejection is argued generically and cites the analog by line.
    - `grep -c 'MINIMUM_GATE_NEEDS: usize = 8' tests/ci_conformance_gate_wiring.rs` returns 1 and
      `grep -c 'MINIMUM_GATE_NEEDS: usize = 8' tests/ci_severance_gate_wiring.rs` returns 1.
    - `grep -c 'MINIMUM_JOBS: usize = 12' tests/ci_conformance_gate_wiring.rs` returns 1 and
      `grep -c 'MINIMUM_JOBS: usize = 12' tests/ci_severance_gate_wiring.rs` returns 1.
    - `grep -c 'SPEC_RECHECK_PINNED_SHA' tests/ci_conformance_gate_wiring.rs` is >= 2 (declaration +
      use), and its value is the 40-character sha literal.
    - `grep -c 'feature-flags' tests/ci_conformance_gate_wiring.rs` is >= 1 — the live negative
      control is reused, not dropped.
    - The bijection is real, not a count: the file contains no
      `env.len() >= needs.len()`-style assertion. `grep -c 'len() >= ' tests/ci_conformance_gate_wiring.rs`
      returns 0. Quote the bijection loop in the SUMMARY.
    - SEVEN negative controls, each executed by hand and recorded verbatim in the SUMMARY, then
      reverted:
      (a) remove `conformance-suite` from `gate.needs` -> the wiring test FAILS naming it;
      (b) remove the `ERA_MATRIX_RESULT` binding from `env:` while leaving the job in `needs:` ->
          the bijection FAILS with the "AWAITED but NEVER CHECKED" message;
      (c) add a SECOND env binding of the same `conformance-suite` result under a different variable
          name -> the bijection FAILS on the "exactly one" arm. This is the control that proves the
          bijection is stronger than the old count;
      (d) change the echo's `era-matrix=$ERA_MATRIX_RESULT` to `era-matrix=$WRONG` -> the echo-pair
          arm FAILS;
      (e) delete `2026-07-28` from the script's `REQUIREMENT_SETS` array -> the data pin FAILS;
      (f) add `--expected-failures` to a command line in `run-conformance-suite.sh` -> the
          forbidden-flag loop FAILS;
      (g) delete the sha line from `conformance/README.md` -> the two-pin binding FAILS.
      A wiring test that cannot fail is not a wiring test.
    - Packaging disposition unchanged and verified, capture-then-assert (D-19):
      `cargo package -p pmcp --list --allow-dirty > target/118-09-package-list.txt` exits 0 and
      `! grep -qE 'conformance/|ci_conformance_gate_wiring' target/118-09-package-list.txt` holds.
    - `make quality-gate` exits 0.
  </acceptance_criteria>
  <done>Both jobs are proved to block merge by a serde_yaml-parsing BIJECTIVE reader, the two upstream pins are mechanically bound, all seven negative controls were executed and reverted, the severance test's floors are raised to the actual counts, and neither new artifact ships in the crate tarball.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pull request -> `gate` required status check | The gate is the sole org-required check; whatever it does not evaluate cannot block a merge |
| CI job -> npm registry and a spawned server | The conformance job installs and executes third-party JavaScript and binds a port |
| Workflow file -> wiring test | The test's verdict is only as good as its reader; a string-matched read can be satisfied by a comment, and a COUNT-based read can be satisfied by a wrong mapping |
| Upstream conformance project -> two independent pins | A repository-commit pin and an npm-package pin can drift apart silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-45 | Repudiation | A job added to `ci.yml` that does not actually block merge — the measured PR #319 failure mode | mitigate | All FOUR wirings per job, asserted structurally by a `serde_yaml` parse with a BIJECTIVE job -> env -> conditional -> echo mapping; negative control (a) proves the test fires |
| T-118-46 | Repudiation | A job that is AWAITED but never CHECKED — strictly worse than not adding it | mitigate | The bijection's "exactly one binding" arm, with the analog's "AWAITED but NEVER CHECKED" wording; negative control (b) proves it fires |
| T-118-75 | Repudiation | A reader whose count-based invariant passes on a WRONG mapping (two bindings of one result, or an echo naming a variable nothing binds) | mitigate | `env.len() >= needs.len()` is REPLACED by the bijection; negative controls (c) and (d) each prove a distinct arm fires, and an acceptance criterion asserts no `len() >= ` invariant remains |
| T-118-47 | Tampering | A wiring test satisfied by a comment mentioning the job name | mitigate | The workflow is PARSED with `serde_yaml`, never string-matched; `step_script_containing` asserts EXACTLY ONE matching `run:` step so a fence cannot be satisfied by a different command |
| T-118-48 | Tampering | A known-fail allowlist reintroduced in the script to turn a red job green | mitigate | `CONFORMANCE_FORBIDDEN_FLAGS` loop over comment-stripped script text; negative control (f) proves it fires; the zero-check declarations are pinned as data so removing the gate is also a test failure |
| T-118-49 | Elevation of Privilege | An over-permissive CI job token, or a secret exposed to third-party JavaScript | mitigate | Neither new job requests elevated `permissions:` (asserted on the diff); `PMCP_REQUEST_STATE_KEY` is a fixed non-production value or a repository secret, is never echoed, and is never placed on a command line; the audited lines are recorded |
| T-118-50 | Tampering | Executing third-party JavaScript in CI from an unpinned source | mitigate | `npm ci --ignore-scripts` against the committed lockfile pinned and slopcheck-cleared in plan 118-02; the job never runs `npm install`; the npm cache is keyed on the lockfile so a cache hit cannot serve a different tree |
| T-118-51 | Information Disclosure | The uploaded results artifact carrying secrets | mitigate | The artifact path is `target/conformance-results/` only; the SUMMARY records an inspection of one uploaded artifact confirming it contains no environment dump |
| T-118-52 | Repudiation | The live negative control being lost, so every "is it wired?" answer becomes yes | mitigate | `feature-flags` is reused as the control in the NEW test and asserted to remain both present as a job and absent from `gate.needs` |
| T-118-76 | Denial of Service | A hung conformance run holding a CI job indefinitely | mitigate | `timeout-minutes` on both jobs, set ABOVE the driver script's own total budget so the script's actionable failure wins and the job timeout is only the backstop; the reader asserts the field is present |
| T-118-77 | Spoofing | The conformance-repository pin and the npm-package pin drifting apart, so a predicate is graded at one commit and the scenarios run at another | mitigate | `SPEC_RECHECK_PINNED_SHA` is carried as this fence's own literal and asserted present in `conformance/README.md`, whose § 10 records both pins and their relation; negative control (g) proves it fires |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | The only install is the plan-118-02 lockfile via `npm ci --ignore-scripts`; an out-of-band interpreter is explicitly rejected in favour of the already-resolved `serde_yaml` dev-dependency |
</threat_model>

<verification>
- `cargo test --features full --test ci_conformance_gate_wiring` exits 0 with a nonzero test count.
- `cargo test --features full --test ci_severance_gate_wiring` still exits 0 with the raised floors.
- All SEVEN negative controls were executed, failed as intended, and were reverted.
- `cargo package -p pmcp --list --allow-dirty` succeeds and lists neither `conformance/` nor
  `tests/ci_conformance_gate_wiring.rs`.
- `make quality-gate` exits 0.
- On the PR: both new jobs appear as checks, and `gate` is red when either is red — confirm from
  `gh api .../check-runs` rather than from a piped `gh pr checks`, which is known to corrupt under
  the rtk proxy.

**D-19 note.** Every `<automated>` block here runs `cargo test` under `bash -o pipefail` before any
downstream `grep`, and the `cargo package` assertion captures to a file with `>` (which preserves
cargo's exit status) before asserting with `! grep -q`.

**Latency expectation (not a hang).** `make quality-gate` is **8-20 minutes**; the seven negative
controls each require a re-run of the wiring test (fast), but the CI observation step waits on a
real GitHub Actions run, which includes a full `conformance-suite` job of 10-20 minutes. Let them
finish; do not interrupt and retry, and do not add a watch-mode or `--no-fail-fast`-style flag to
make them feel faster.
</verification>

<success_criteria>
- The official-suite job and the era-matrix job both BLOCK merge through the org-required `gate`
  check, wired in all four places each.
- The wiring is proved by PARSING the workflow into a BIJECTIVE job -> env -> conditional -> echo
  mapping, with a live negative control that keeps the proof honest and seven executed failure
  demonstrations.
- The Phase-109 team-servers harness is executed by CI for the first time.
- The results artifact is uploaded on green runs from `target/conformance-results/`, keeping the
  not-scored results reviewable at re-pin time.
- Both jobs carry a timeout above their driver's own budget, so a hang fails with a diagnostic
  rather than an opaque cancellation.
- The two upstream pins are mechanically bound and cannot silently diverge.
- `security_audit` and `workspace-test` remain untouched — the deferred item stays deferred.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-09-SUMMARY.md` when done.
Record: the final `gate.needs` line verbatim; the four counts (needs / env bindings / if clauses /
echo pairs); the two script budgets beside the two job timeouts; the quoted bijection loop; all
seven negative-control transcripts; the `cargo package --list` verification; the audited
`PMCP_REQUEST_STATE_KEY` lines; the inspected uploaded artifact; and the observed check-run states
from the PR.
</output>
