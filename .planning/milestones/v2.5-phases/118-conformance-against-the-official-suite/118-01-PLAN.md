---
phase: 118-conformance-against-the-official-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/streamable_http_server.rs
  - tests/v2_mcp_name_name_bearing_only.rs
  - contracts/mcp-protocol-sdk-v1.yaml
autonomous: true
requirements: [CONF-01]

must_haves:
  truths:
    - "A v2 POST for a non-name-bearing method (tools/list, resources/list, prompts/list, ping, completion/complete, server/discover) succeeds with NO Mcp-Name header at all"
    - "A v2 POST for an MRTR name-bearing method (tools/call, prompts/get, resources/read) with NO Mcp-Name header is still rejected"
    - "A v2 POST for a TASKS name-bearing method (tasks/get, tasks/update, tasks/cancel) with NO Mcp-Name header is ALSO rejected — the emitter and the validator resolve through the SAME combined table"
    - "A v2 POST for any name-bearing method whose Mcp-Name value disagrees with the body's routing name is still rejected"
    - "A v2 POST with no Mcp-Method header is still rejected regardless of method"
    - "A v2 POST with no MCP-Protocol-Version header is still rejected regardless of method"
    - "On a non-name-bearing method the name carried forward in V2GateOutcome::EnforceOk is the EMPTY STRING, whatever the client sent"
    - "The rustdoc at the v2 header gate records the Phase-113 DRIFT-1 adjudication AND its Phase-118 D-13 reversal, naming both"
    - "The gate never panics on arbitrary header BYTES or an arbitrary raw JSON-RPC body"
  artifacts:
    - path: "src/server/streamable_http_server.rs"
      provides: "v2 header gate with Mcp-Name required only on name-bearing methods, resolved through name_bearing_key"
      contains: "D-13"
    - path: "tests/v2_mcp_name_name_bearing_only.rs"
      provides: "wire-level proof of the relaxed rule over real HTTP, including all three tasks/* methods"
    - path: "contracts/mcp-protocol-sdk-v1.yaml"
      provides: "the contract-first equation for the v2 header gate (CLAUDE.md contract-first rule)"
      contains: "v2_header_gate"
  key_links:
    - from: "src/server/streamable_http_server.rs"
      to: "crate::types::mrtr::name_bearing_key"
      via: "is_name_bearing_method — the COMBINED table, the one the Mcp-Name emitter resolves through"
      pattern: "name_bearing_key"
    - from: "tests/v2_mcp_name_name_bearing_only.rs"
      to: "tests/common/v2.rs"
      via: "spawn_with / post / v2_headers / teardown"
      pattern: "mod common"
---

<objective>
Reverse the Phase-113 DRIFT-1 adjudication for the `Mcp-Name` header, per locked decision **D-13**:
`Mcp-Name` becomes REQUIRED only on name-bearing methods, matching the MCP 2026-07-28 transport
spec and the official `@modelcontextprotocol/conformance` suite. The strict name/body cross-check
is KEPT exactly where a name exists — and, per **D-18**, "name-bearing" now means the COMBINED
table, so `tasks/get` / `tasks/update` / `tasks/cancel` are validated as well as emitted.

Purpose: RESEARCH Pitfall 1 measured that pmcp's presence-on-every-request rule rejects the
official suite's v2 `tools/list` (and every other non-name-bearing v2 request) with
`-32020 "v2 requests must carry Mcp-Method, Mcp-Name and MCP-Protocol-Version headers"` **before
dispatch**, which fails effectively the whole v2 scored set. CONF-01's v2 leg cannot go green
without this. The user explicitly authorized this ONE server-behaviour change as an exception to
the phase boundary.

Output: a relaxed, still-fail-closed v2 header gate whose predicate is the SAME table the client
emitter uses; a rustdoc block recording BOTH adjudications; unit + property + byte-level arms over
the gate's whole input space; a wire-level integration test over real HTTP covering MRTR **and**
tasks methods; and the contract-first equation CLAUDE.md requires for a behaviour change.

## Review corrections applied (from 118-REVIEWS.md, locked as D-18 / D-20)

- **D-18 (Codex HIGH, confirmed against source).** The previous revision of this plan pinned
  `is_name_bearing_method` to `logical_name_key` (`src/types/mrtr.rs:297`), which covers only
  tools/prompts/resources. `name_bearing_key` (`src/types/mrtr.rs:313`) additionally covers
  `tasks/get` / `tasks/update` / `tasks/cancel`, and the SDK's own rustdoc at `mrtr.rs:290-297`
  names it *"the function the `Mcp-Name` EMITTER resolves through"*. The client already emits
  `Mcp-Name` for `tasks/*` (`src/shared/streamable_http.rs`, via `name_bearing_key`) while the
  server never required or cross-checked it — an emitter/validator asymmetry that contradicts
  D-13's own principle. Task 1 fixes the delegation; Task 2 adds explicit wire tests for all three
  `tasks/*` methods AND keeps a LITERAL contract test so the property's oracle is not the predicate
  under test.
- **D-20, Gemini finding (accepted).** `V2GateOutcome::EnforceOk { method, name }` is echoed back
  outbound by `apply_v2_outbound_headers`. Task 1 sanitizes `name` to `""` whenever the method is
  not name-bearing, so a `Mcp-Name` sent on `tools/list` cannot propagate downstream or be echoed.
- **D-20, FUZZ exemption DROPPED.** See the ALWAYS-rule disposition below.
- **D-20, contract-first.** Task 3 adds the `v2_header_gate` equation to
  `contracts/mcp-protocol-sdk-v1.yaml` and runs the repo's compliance gate.
- **D-19.** Every `<automated>` command below preserves the exit status of the thing it verifies.

## CLAUDE.md ALWAYS-rule disposition (FUZZ + PROPERTY + UNIT + EXAMPLE)

- **UNIT — supplied here.** Task 2's reworked in-module truth-table test, asserting the distinct
  error strings.
- **PROPERTY — supplied here.** Task 2 adds a `proptest` over the gate's entire TYPED input space
  (presence/absence and arbitrary values for all three headers, crossed with MRTR name-bearing,
  tasks name-bearing and non-name-bearing methods), asserting the truth table as an invariant.
- **FUZZ — supplied here. The previous exemption is WITHDRAWN (D-20).** CLAUDE.md's ALWAYS block
  states the arm explicitly as *"`cargo fuzz run <target>` OR using proptest for property-based
  testing"*. Task 2 supplies the arm in its **byte-level** form: a `proptest` that builds
  `HeaderValue`s from arbitrary `Vec<u8>` (via `HeaderValue::from_bytes`, so non-UTF-8 and
  delimiter bytes reach the gate) and drives `extract_body_method_and_name` with arbitrary raw
  body bytes, asserting total absence of panics. A `fuzz/` target is deliberately NOT added: the
  gate functions are private free functions, and reaching them from the `fuzz/` sub-workspace
  would require widening pmcp's public API for a test — a worse trade than the sanctioned proptest
  spelling. Record that reasoning in the SUMMARY. **This plan must not touch `fuzz/Cargo.toml`**;
  plan 118-03 owns that file in this same wave.
- **EXAMPLE — supplied downstream, deliberately.** The runnable-example arm is
  `examples/s54_v2_dual_conformance.rs` (plans 118-04/118-05), which drives this exact code path
  over real HTTP under the official conformance suite at both eras.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md

<interfaces>
<!-- Contracts the executor needs. Do NOT re-derive these by exploring. -->

From `src/types/mrtr.rs` — the TWO tables and their documented relationship:

  pub(crate) fn logical_name_key(method: &str) -> Option<&'static str>   // :297 — MRTR only:
      // tools/call -> "name", prompts/get -> "name", resources/read -> "uri"

  pub(crate) fn name_bearing_key(method: &str) -> Option<&'static str>   // :313 — COMBINED:
      // logical_name_key first, then TASK_NAME_BEARING_METHODS:
      //   tasks/get -> "taskId", tasks/update -> "taskId", tasks/cancel -> "taskId"
      // rustdoc, verbatim: "This is the function the `Mcp-Name` EMITTER resolves through."

  pub(crate) fn logical_name_of(method, params) -> Option<String>        // resolves via name_bearing_key
  pub(crate) fn frame_routing_pair(frame) -> Option<(&str, Option<String>)>  // :357 — via logical_name_of

  MEASURED CONSEQUENCE: the server's `body_name` already arrives correct for `tasks/*`
  (`method_and_name_of` -> `frame_routing_pair` -> `logical_name_of` -> `name_bearing_key`,
  so it reads `params.taskId`). The ONLY thing missing is that `is_name_bearing_method` gates on
  the narrower table, so `cross_check_name` short-circuits `Ok` for `tasks/*`. Fixing the
  delegation is a ONE-LINE change plus its tests.

From `src/server/streamable_http_server.rs` (current state, the four functions in play):

  fn require_three_headers(headers: &HeaderMap)                              // :1010
        -> std::result::Result<(String, String), &'static str>
  fn cross_check_method(mcp_method: &str, body_method: Option<&str>)         // :1023
        -> std::result::Result<(), &'static str>
  fn is_name_bearing_method(method: &str) -> bool                            // :1039
        // body today: `crate::types::mrtr::logical_name_key(method).is_some()`
  fn cross_check_name(mcp_name: &str, method: &str, body_name: Option<&str>) // :1057
        -> std::result::Result<(), &'static str>   // already method-aware; NEEDS NO EDIT

  Composition site — `classify_v2_request`, the `V2Classification::Enforce` arm at :1102-1113:
    require_three_headers -> cross_check_method -> cross_check_name -> V2GateOutcome::EnforceOk { method, name }
  Outbound echo: `apply_v2_outbound_headers(headers, method, name)` at :1163.

  Existing in-module tests that will move:
    :4567 require_three_headers_needs_all_three     (encodes the OLD rule — rework, do not delete)
    :4584 cross_check_method_and_name_fail_closed
    :4942 cross_check_name_accepts_resources_read_uri
    :5042 (asserts `!is_name_bearing_method("server/discover")` — still TRUE after D-18)

From `tests/common/v2.rs` — the launch/probe harness three severance proofs already use:

  pub const V1: &str;   // :72  LATEST_PROTOCOL_VERSION
  pub const V2: &str;   // :66  PROTOCOL_VERSION_2026_07_28
  pub fn build_v2_server_with(name: &str, capabilities: ServerCapabilities) -> Server   // :293
  pub async fn spawn_with(...)                                    // :394 binds before returning
  pub fn v2_headers(method: &str, name: &str) -> Vec<(String, String)>   // :698
  pub fn v2_headers_raw(method: &str, raw_name: &str) -> Vec<(String, String)>  // :704
  pub fn v2_body(method: &str, id: Value, params: Value) -> String       // :563
  pub async fn post(addr, extra: &[(String, String)], body: &str) -> ... // :816
  pub async fn teardown<S: Send>(handle, sockets)                        // :538

  There is NO "omit Mcp-Name" helper. Build one by FILTERING the vector `v2_headers` returns on
  `pmcp::shared::http_constants::MCP_NAME` — never by hand-writing header-name strings.

Rejection code to assert against (public):
  pmcp::types::protocol::error_codes::HEADER_MISMATCH == -32020   // src/types/protocol/error_codes.rs:188

Contract-first surface:
  contracts/mcp-protocol-sdk-v1.yaml — a top-level `equations:` map (`:16`) whose entries carry
  `formula` / `domain` / `codomain` / `invariants` / `preconditions` / `postconditions` /
  `lean_theorem`. `protocol_version_negotiation` (`:39`) is the nearest analog to copy the SHAPE of.
  Gate command: `make comply` (graceful; chained into `quality-gate`) and
  `make comply-bindings-check` (deterministic, fail-closed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax the v2 header gate, and route its predicate through the COMBINED name table</name>
  <files>src/server/streamable_http_server.rs</files>
  <read_first>
    - src/server/streamable_http_server.rs — read the whole v2 header-gate region, lines 1000-1170:
      the `require_three_headers` rustdoc + body, `cross_check_method`, `is_name_bearing_method`,
      `cross_check_name`, the `classify_v2_request` composition arm, and `apply_v2_outbound_headers`.
      You are editing it; read the current state, do not assume.
    - src/types/mrtr.rs lines 282-330 — `logical_name_key` AND `name_bearing_key` with their
      rustdoc, including the sentence naming `name_bearing_key` as the EMITTER's resolver, and the
      `# Scope (Phase 114, DQ4)` block on `logical_name_key` that will need its cross-reference
      updated once the server also resolves through the combined table.
    - src/shared/streamable_http.rs around the `(Mcp-Method, Mcp-Name)` derivation (search
      `name_bearing_key`) — the CLIENT half of the contract, which already covers `tasks/*`. This is
      the asymmetry D-18 closes.
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-13 and D-18
      verbatim.
    - .planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md — Consensus
      Summary item 3, the confirmed-against-source statement of the defect.
  </read_first>
  <behavior>
    The v2 header gate, as a truth table over (MCP-Protocol-Version present, Mcp-Method present,
    Mcp-Name present, method class):

    - version absent                              -> Err, regardless of anything else
    - Mcp-Method absent                           -> Err, regardless of anything else
    - method name-bearing (MRTR **or** tasks), Mcp-Name absent   -> Err
    - method name-bearing, Mcp-Name present       -> proceed to `cross_check_name` (unchanged strict
      cross-check: malformed sentinel -> Err; decoded value != body's routing name -> Err). For
      `tasks/*` the body's routing name is `params.taskId`, which `frame_routing_pair` already
      resolves — no new body reader is needed.
    - method NOT name-bearing, Mcp-Name absent    -> Ok (THE D-13 CHANGE)
    - method NOT name-bearing, Mcp-Name present   -> Ok, and the value is DISCARDED: the pair handed
      onward carries the empty string (the D-20/Gemini sanitization), so an inbound
      `Mcp-Name: anything` on `tools/list` can neither branch downstream logic nor be echoed back by
      `apply_v2_outbound_headers`. This also keeps the Phase-113 client's `Mcp-Name: ""` emission
      working, since absent and empty now converge on the same carried value.

    Totality: the gate returns `Err`, never panics, for every header byte sequence and every raw
    body byte sequence.
  </behavior>
  <action>
Change the v2 header gate so `Mcp-Name` presence is required exactly where the method carries a
ROUTING name — from the combined table, per D-13 as corrected by D-18 — and record the reversal in
rustdoc.

Concrete target state:

1. **The one-line correctness fix (D-18).** `is_name_bearing_method` must delegate to
   `crate::types::mrtr::name_bearing_key`, not `logical_name_key`. Rewrite its rustdoc to say: this
   is the SAME combined table the client's `Mcp-Name` emitter resolves through
   (`src/shared/streamable_http.rs`), so the two ends of the cross-check cannot disagree; it covers
   `tools/call` and `prompts/get` (`params.name`), `resources/read` (`params.uri`), and
   `tasks/get` / `tasks/update` / `tasks/cancel` (`params.taskId`); and that before Phase 118 it
   resolved through the narrower `logical_name_key`, which let the client emit a `Mcp-Name` for
   `tasks/*` that the server never required or cross-checked. Name D-18.

2. **Update the `logical_name_key` rustdoc cross-reference** in `src/types/mrtr.rs`. Its
   `# Scope (Phase 114, DQ4)` block currently asserts that `logical_name_key` "is ALSO the server's
   name-bearing predicate (`is_name_bearing_method`)". After this change that sentence is FALSE.
   Correct it to say the server's predicate now resolves through `name_bearing_key` (Phase 118
   D-18) and that `logical_name_key` remains the MRTR-only lookup. This is a rustdoc-only edit in a
   second file; declare nothing else there.

3. **Order the presence check after the method is known.** Today `require_three_headers` reads all
   three headers and demands all three in one `match`, before the method is known to
   `cross_check_name`. Restructure so `MCP-Protocol-Version` presence and `Mcp-Method` are
   established first, then `is_name_bearing_method(&method)` decides whether an absent `Mcp-Name`
   is an error. Keep reading header values through the existing `bounded_header_str` helper and the
   existing `MCP_PROTOCOL_VERSION` / `MCP_METHOD` / `MCP_NAME` constants — introduce no new
   header-name literals.

4. **Sanitize the carried name.** When the method is NOT name-bearing, the `(method, name)` pair
   handed onward carries `String::new()` regardless of what the client sent. `V2GateOutcome::
   EnforceOk { method, name }` keeps its existing shape and type, so no downstream caller changes.
   Add a comment naming the reason: the value is echoed outbound by `apply_v2_outbound_headers`,
   and echoing an unvalidated attacker-supplied string is a pointless surface.
   `cross_check_name` still returns `Ok` immediately for a non-name-bearing method, so it needs NO
   edit — do not touch it.

5. **Split the error strings** so a rejection names its actual cause instead of the current
   catch-all. Required distinct messages:
   - missing version or method: a message naming `Mcp-Method` and `MCP-Protocol-Version` and NOT
     naming `Mcp-Name` (it is no longer universally required and a message that named it would
     mislead operators);
   - missing `Mcp-Name` on a name-bearing method: a message that names `Mcp-Name` AND says the
     method carries a routing name.
   Both remain `&'static str`, so the error type is unchanged.

6. **Rename `require_three_headers` to `require_v2_headers`** (the "three" is no longer true) and
   update the call site in `classify_v2_request`. Keep it a private free function.

7. **Rewrite its rustdoc to record BOTH adjudications**, per the in-repo convention that a
   deliberate divergence is recorded in rustdoc with the deciding phase named:
   - the quoted rule block, updated: "`Mcp-Name` MUST be present on name-bearing methods —
     `tools/call` / `prompts/get` -> `params.name`; `resources/read` -> `params.uri`;
     `tasks/get` / `tasks/update` / `tasks/cancel` -> `params.taskId` — and is OPTIONAL, and
     IGNORED, on every other v2 method";
   - a paragraph stating that Phase-113 DRIFT-1 chose the stricter fail-closed
     presence-on-every-request rule, and that **Phase 118 D-13 REVERSES it**;
   - the reason, concretely: the transport spec requires the header only for name-bearing methods,
     and the official `@modelcontextprotocol/conformance` suite emits it only for those, so the
     stricter rule rejected effectively the entire v2 scored set with `-32020` before dispatch
     (RESEARCH Pitfall 1);
   - what is RETAINED: `Mcp-Method` and `MCP-Protocol-Version` stay mandatory on every v2 request,
     and the strict name/body cross-check in `cross_check_name` is unchanged;
   - the D-18 widening: the predicate now resolves through the combined table, so `tasks/*` is
     validated as well as emitted;
   - a note that the Phase-113 client emitting `Mcp-Name: ""` for a name-less method is still
     accepted.

Do NOT change `cross_check_method`, `cross_check_name`, `logical_name_key`'s BODY, or
`name_bearing_key`. Do NOT add a config flag or feature gate for the old behaviour — D-13 is a
straight reversal, not an option.
  </action>
  <verify>
    <automated>mkdir -p target && bash -o pipefail -c 'cargo test --features full --lib server::streamable_http_server 2>&1 | tee target/118-01-t1.log' && grep -qE '^running [1-9][0-9]* tests?$' target/118-01-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'require_three_headers' src/server/streamable_http_server.rs` returns 0.
    - `grep -c 'require_v2_headers' src/server/streamable_http_server.rs` is >= 2 (definition +
      call site).
    - The predicate resolves through the COMBINED table:
      `grep -c 'name_bearing_key' src/server/streamable_http_server.rs` is >= 1 AND
      `grep -c 'logical_name_key' src/server/streamable_http_server.rs` returns 0.
    - `grep -v '^\s*//' src/server/streamable_http_server.rs | grep -c 'is_name_bearing_method'`
      is >= 3 — the presence gate, the sanitization and `cross_check_name` all reach the shared
      table rather than restating the method set.
    - `grep -c 'DRIFT-1' src/server/streamable_http_server.rs` is >= 1 AND
      `grep -c 'D-13' src/server/streamable_http_server.rs` is >= 1 AND
      `grep -c 'D-18' src/server/streamable_http_server.rs` is >= 1.
    - No literal method-name list is introduced: `grep -c '"tools/call"' src/server/streamable_http_server.rs`
      does not exceed `git show HEAD:src/server/streamable_http_server.rs | grep -c '"tools/call"'`;
      record both numbers.
    - The `logical_name_key` rustdoc no longer claims to be the server predicate:
      `grep -c 'is_name_bearing_method' src/types/mrtr.rs` is >= 1 and the surrounding sentence
      names Phase 118 D-18 — quote it in the SUMMARY.
    - `RUSTFLAGS="-D warnings" cargo build --features full` exits 0.
  </acceptance_criteria>
  <done>The v2 header gate accepts a name-less v2 method with no `Mcp-Name`, still rejects a missing or mismatched `Mcp-Name` on every method the COMBINED table names (including all three `tasks/*`), discards a stray `Mcp-Name`, and its rustdoc names Phase-113 DRIFT-1, Phase-118 D-13 and Phase-118 D-18.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit, property, byte-level and wire-level proof — including the three tasks/* methods</name>
  <files>src/server/streamable_http_server.rs, tests/v2_mcp_name_name_bearing_only.rs</files>
  <read_first>
    - src/server/streamable_http_server.rs — the `#[cfg(test)]` module. Specifically
      `require_three_headers_needs_all_three` (~:4567), `cross_check_method_and_name_fail_closed`
      (~:4584), `cross_check_name_accepts_resources_read_uri` (~:4942) and the
      `!is_name_bearing_method("server/discover")` assertion (~:5042). UPDATE the first, do not
      delete it: it is the test that encodes the old rule and must now encode the new one.
    - src/types/mrtr.rs — `TASK_NAME_BEARING_METHODS` (:249) and `TASK_ID_KEY`, so the tasks rows
      assert against `params.taskId` and never against a retyped key string.
    - tests/common/v2.rs — `spawn_with` / `build_v2_server_with` / `post` / `v2_headers` /
      `v2_headers_raw` / `v2_body` / `teardown` / `ALLOW` / `V1` / `V2`. This is the ONLY sanctioned
      way to launch and probe a live v2 HTTP server from a test (RESEARCH § Don't Hand-Roll); it
      carries a hang-guard timeout.
    - tests/v2_verbs_405_on_severed_build.rs — the in-repo shape for a wire-level v2 assertion built
      on `tests/common/v2.rs`, including how it declares `mod common`.
  </read_first>
  <behavior>
    IN-MODULE UNIT (over `require_v2_headers` and `classify_v2_request` directly), one row per
    truth-table cell from Task 1, PLUS a LITERAL CONTRACT test:

      literal_contract: an explicitly written-out list of the six name-bearing method strings
      (`tools/call`, `prompts/get`, `resources/read`, `tasks/get`, `tasks/update`, `tasks/cancel`)
      and four non-name-bearing ones (`tools/list`, `ping`, `completion/complete`,
      `server/discover`), each asserted against `is_name_bearing_method`. This test's oracle is a
      hand-written literal, NOT the predicate — it is what stops the property test below from
      blessing a wrong table (the exact defect Codex identified). Its doc comment must say so.

    PROPERTY (typed input space): for arbitrary (version present?, method string drawn from a
    strategy mixing the six name-bearing methods with arbitrary other strings, name present?, name
    value), the gate returns `Ok` iff version present AND method present AND (`Mcp-Name` present OR
    the method is not name-bearing). Oracle: `is_name_bearing_method`. Additionally: when the method
    is not name-bearing and the gate returns `Ok`, the carried name is the empty string.

    FUZZ (byte-level, CLAUDE.md's sanctioned proptest spelling): for arbitrary `Vec<u8>` header
    values built through `HeaderValue::from_bytes` (skipping the values `HeaderValue` itself
    rejects) and arbitrary raw body bytes fed to `extract_body_method_and_name`, no input panics and
    every rejection is an `Err`/`Reject`, never an unwind.

    WIRE-LEVEL, against a live dual-version HTTP server:

    - v2 POST `tools/list` with version + method and NO `Mcp-Name`   -> 200 with a JSON-RPC `result`
    - v2 POST `tools/list` with `Mcp-Name: ""`                        -> 200 with a `result`
      (Phase-113 client backward compatibility)
    - v2 POST `tools/list` with `Mcp-Name: not-a-real-name`           -> 200 with a `result`, AND the
      RESPONSE's `Mcp-Name` header is empty (the sanitization is observable on the wire)
    - v2 POST `tools/call` with NO `Mcp-Name`                         -> rejected, body names `Mcp-Name`
    - v2 POST `tools/call` with a mismatched `Mcp-Name`               -> rejected (retained cross-check)
    - v2 POST `tools/call` with a correct `Mcp-Name`                  -> 200 with a `result`
    - v2 POST `tasks/get` with NO `Mcp-Name`                          -> rejected with
      `error.code == HEADER_MISMATCH`
    - v2 POST `tasks/update` with NO `Mcp-Name`                       -> rejected, same code
    - v2 POST `tasks/cancel` with NO `Mcp-Name`                       -> rejected, same code
    - v2 POST `tasks/get` with `Mcp-Name` set to the body's `params.taskId` -> the response's
      `error.code` is NOT `HEADER_MISMATCH` (the request PASSED the gate; whether the harness
      server implements `tasks/get` is irrelevant and must not be asserted)
    - v2 POST `tasks/get` with `Mcp-Name` set to a DIFFERENT value than `params.taskId`
      -> rejected with `error.code == HEADER_MISMATCH` (the cross-check now reaches tasks)
    - v2 POST `tools/list` with NO `Mcp-Method`                       -> rejected
    - a v1 POST is unaffected (negative control: the era gate is per-request)
  </behavior>
  <action>
Update the in-module tests and add a new integration test file.

In `src/server/streamable_http_server.rs`: rework `require_three_headers_needs_all_three` into a
test over `require_v2_headers` covering every row of the truth table, asserting on the RESULT and,
for the two error rows, on the distinct error strings introduced in Task 1 — so a future collapse
back to one catch-all message fails here. Add the LITERAL CONTRACT test described in `<behavior>`
as a SEPARATE `#[test]`, with a doc comment stating that it exists because the property test's
oracle is the predicate under test and therefore cannot detect a wrong table; name D-18.

Add the PROPERTY arm and the BYTE-LEVEL arm in the same `#[cfg(test)]` block using `proptest`
(already a root dev-dependency). Build `HeaderMap`s from the generated values via
`HeaderValue::from_bytes`, discarding only what `HeaderValue` itself refuses. Use
`is_name_bearing_method` — NOT a restated method list — as the property's oracle, so the property
cannot drift from the shared table; the literal contract test is what pins the table itself.

Create `tests/v2_mcp_name_name_bearing_only.rs`:
- declare `mod common;` and use `common::v2`'s `spawn_with` / `build_v2_server_with` / `post` /
  `v2_headers` / `v2_headers_raw` / `v2_body` / `teardown` — do NOT hand-roll a spawn or a `sleep`;
- build the "omit `Mcp-Name`" header vector by FILTERING `v2_headers(...)`'s result on
  `pmcp::shared::http_constants::MCP_NAME`; never hand-write a header-name string;
- source the version strings from `common::v2::V1` / `common::v2::V2`, and the rejection code from
  `pmcp::types::protocol::error_codes::HEADER_MISMATCH`, never as literals;
- cover every row in `<behavior>`, including all four `tasks/*` rows;
- add a module doc naming Phase 118 D-13 and D-18, stating that this file is the wire-level proof of
  the reversal AND of the emitter/validator symmetry, and that the remedy for a failure is to fix
  the gate, never to relax an assertion;
- use the three-part `FAILURE MODE:` / `CONSEQUENCE:` / `WHAT TO DO:` failure-message convention
  used throughout `tests/ci_severance_gate_wiring.rs`, echoing the observed status code and body;
- include one always-true smoke assertion so a `running 0 tests` run is visibly different from a
  green one (the 117-14 handoff rule).
  </action>
  <verify>
    <automated>mkdir -p target && bash -o pipefail -c 'cargo test --features full --test v2_mcp_name_name_bearing_only 2>&1 | tee target/118-01-t2.log' && grep -qE '^running [1-9][0-9]* tests?$' target/118-01-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features full --test v2_mcp_name_name_bearing_only` exits 0 AND its output
      contains a line matching `^running [1-9][0-9]* tests?$` — asserted by the `<automated>`
      command above, whose exit status is the CARGO status because of `-o pipefail`.
    - All three tasks methods are exercised over the wire:
      `for m in tasks/get tasks/update tasks/cancel; do grep -q "\"$m\"" tests/v2_mcp_name_name_bearing_only.rs || echo "MISSING $m"; done`
      prints nothing.
    - No readiness sleep: `grep -c 'sleep' tests/v2_mcp_name_name_bearing_only.rs` returns 0.
    - No retyped protocol literals: `grep -cE '2026-07-28|-32020' tests/v2_mcp_name_name_bearing_only.rs`
      returns 0 — the version comes from `common::v2::V2`, the code from `HEADER_MISMATCH`.
    - The in-module suite carries all four arms:
      `grep -c 'require_three_headers_needs_all_three' src/server/streamable_http_server.rs` returns
      0 (reworked, not left stale); `grep -c 'proptest!' src/server/streamable_http_server.rs` is
      >= 1; and the literal contract test exists — `grep -c 'from_bytes' src/server/streamable_http_server.rs`
      is >= 1 (the byte-level arm). Name the three test functions in the SUMMARY.
    - The property's oracle is the shared table, not a restated list: the proptest block contains
      `is_name_bearing_method` and no literal array of method names. Quote the block in the SUMMARY.
    - The literal contract test's list IS a literal: quote it in the SUMMARY and state that it
      contains all six name-bearing methods.
    - Negative control (a), executed by hand once and reported verbatim: temporarily restore the
      universal-presence rule, confirm the `tools/list`-without-`Mcp-Name` test FAILS naming the
      `-32020`-class rejection, then revert.
    - Negative control (b), executed by hand once and reported verbatim: temporarily point
      `is_name_bearing_method` back at `logical_name_key`, confirm the `tasks/get` mismatch test
      FAILS (the cross-check stops firing), then revert. This is the control that proves D-18's fix
      is load-bearing.
    - Record the proptest generated-case count for each of the two proptest arms in the SUMMARY.
  </acceptance_criteria>
  <done>Unit, literal-contract, property and byte-level arms pass in-module; the live-HTTP test covers MRTR and all three tasks methods with a nonzero test count; and both negative controls were executed, quoted and reverted.</done>
</task>

<task type="auto">
  <name>Task 3: Contract-first equation, repo sweep for the old rule, and the full gate</name>
  <files>contracts/mcp-protocol-sdk-v1.yaml, src/server/streamable_http_server.rs</files>
  <read_first>
    - contracts/mcp-protocol-sdk-v1.yaml — read the `metadata:` block (:1-15) and the
      `protocol_version_negotiation` equation (:39-57). That entry is the SHAPE to copy: a `formula`
      block, `domain`, `codomain`, `invariants`, `preconditions`, `postconditions`, `lean_theorem`.
    - Makefile — the `comply`, `comply-ci` and `comply-bindings-check` targets (~:810-890) and their
      comment block, which states exactly why `pmat comply check --path .` is INFORMATIONAL in this
      repo and what is actually fail-closed.
    - src/server/streamable_http_server.rs — post-Task-1 state.
    - docs/v1-sunset-policy.md — the in-repo prose form of "a deliberate divergence, with the
      deciding phase named"; check whether it restates the old `Mcp-Name` rule.
  </read_first>
  <action>
Two pieces of work: the contract-first record CLAUDE.md requires for a behaviour change, and the
repo sweep for restatements of the old rule.

**A. The contract equation.** Add a `v2_header_gate` entry to `contracts/mcp-protocol-sdk-v1.yaml`'s
`equations:` map, modelled on `protocol_version_negotiation`:
  - `formula`: the gate as a total function
    `gate: (HeaderMap, meta_is_v2, body_method, body_name) -> EnforceOk | Reject | Passthrough`,
    with the four accept conditions and the sanitization rule written out;
  - `invariants`: (1) `Mcp-Method` and `MCP-Protocol-Version` are required on every v2 request;
    (2) `Mcp-Name` is required exactly when `name_bearing_key(method)` is `Some`; (3) when present
    on a name-bearing method, the decoded value equals the body's routing name at that method's key;
    (4) when the method is not name-bearing the carried name is the empty string; (5) the gate is
    total — it never panics on arbitrary header or body bytes;
  - `preconditions` / `postconditions` naming the observable outcomes;
  - `domain` / `codomain`;
  - a comment above the entry naming Phase 118 D-13 and D-18 and recording that this REPLACES the
    Phase-113 DRIFT-1 presence-on-every-request rule.
Do NOT add a `binding.yaml` entry unless `contracts/team-servers/binding.yaml`'s scope actually
covers root-crate functions — read it first and record the verdict; a ghost binding fails
`make comply-bindings-check`.

**B. The sweep.** Find every place in the repo that restates the OLD rule
(`require_three_headers`, "`Mcp-Name` MUST be present on every v2 request", "all three headers",
"three v2 headers", "must carry Mcp-Method") and bring each into line with D-13/D-18, or delete it
if it is a stale duplicate. Search `src/`, `tests/`, `examples/`, `docs/`, `crates/` and `.github/`.
For each hit decide: (a) it is the new canonical rustdoc — leave it; (b) it is a client-side emitter
that sends `Mcp-Name: ""` for name-less methods — leave it, still valid and Task 2 pins that
compatibility; (c) it restates the old server-side requirement — update it to say `Mcp-Name` is
required on name-bearing methods only, citing Phase 118 D-13 and D-18.

Then run the full house gate. Cognitive complexity matters: if the restructured `require_v2_headers`
pushes any function over PMAT's cog-25 threshold, decompose it per the P1-P6 techniques in
`.planning/phases/75-fix-pmat-issues/75-RESEARCH.md`; do NOT add an `#[allow]`.
  </action>
  <verify>
    <automated>make comply-bindings-check && make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'require_three_headers' src/ tests/ examples/ docs/ crates/ .github/` returns no
      matches.
    - `grep -rn 'Mcp-Name' src/ docs/ | grep -v 'D-13' | grep -v 'D-18'` — every remaining hit is
      manually classified in the SUMMARY as (a), (b) or (c), with `file:line` and the verdict.
    - `grep -c 'v2_header_gate' contracts/mcp-protocol-sdk-v1.yaml` returns 1, and the entry carries
      all of `formula`, `domain`, `codomain`, `invariants`, `preconditions`, `postconditions`.
    - The contract file still parses as YAML — proved by `cargo test --features full --test phase115_contract_bindings`
      exiting 0 (that test reads `contracts/mcp-protocol-sdk-v1.yaml` at runtime). If that test does
      not in fact read this file, say so in the SUMMARY and substitute
      `python3 -c` is FORBIDDEN here — use `cargo test ... --test phase115_contract_bindings` or a
      `serde_yaml`-based check, never an out-of-band interpreter.
    - `make comply-bindings-check` exits 0, and the `make comply` report is recorded in the SUMMARY
      with the note that its project-level advisories are informational per CLAUDE.md D-07.
    - `make quality-gate` exits 0.
    - `pmat analyze complexity --format json --max-cognitive 25 | jq '.violations[] | select(.path | startswith("src/"))'`
      emits nothing for `streamable_http_server.rs`.
    - The SUMMARY states explicitly that the post-D-13 v2 scored-set profile is NOT measured here —
      that re-measurement is plan 118-05 Task 1's job and must not be pre-empted with a guess.
  </acceptance_criteria>
  <done>The contract carries a `v2_header_gate` equation naming D-13 and D-18, no stale restatement of the universal-presence rule survives anywhere in the repo, and `make comply-bindings-check` and `make quality-gate` both exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted HTTP client -> pmcp v2 POST gate | Attacker-controlled headers and body cross here, before dispatch |
| Gate -> outbound response headers | `apply_v2_outbound_headers` echoes the carried `(method, name)` pair back |
| Intermediary (WAF / proxy) -> pmcp | Middleboxes may have been configured against the OLD presence-on-every-request invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Spoofing | `require_v2_headers` — a caller impersonating a different routing name | mitigate | `cross_check_name` is UNCHANGED and still strictly compares the decoded `Mcp-Name` against the body's routing name; D-18 WIDENS its reach to `tasks/get` / `tasks/update` / `tasks/cancel`, closing the emitter/validator asymmetry; Task 2 pins the mismatch-rejection row for both MRTR and tasks |
| T-118-02 | Tampering | The relaxation widening what reaches dispatch | mitigate | `Mcp-Method` and `MCP-Protocol-Version` remain MANDATORY on every v2 request; `cross_check_method` unchanged; the relaxation is scoped by `name_bearing_key`, the same table the client emitter uses |
| T-118-03 | Repudiation | An operator's WAF rule that keyed on universal `Mcp-Name` presence silently stops matching | accept + document | Recorded in the new rustdoc and in the `v2_header_gate` contract equation; no runtime signal is added (D-11's no-new-warn stance) |
| T-118-04 | Denial of Service | Header parsing on an unauthenticated path | mitigate | Values are read through the existing `bounded_header_str` length-bounded helper; the byte-level proptest arm asserts totality over arbitrary header bytes and arbitrary raw body bytes |
| T-118-53 | Information Disclosure | An unvalidated attacker-supplied `Mcp-Name` on a non-name-bearing method propagating downstream or being echoed outbound | mitigate | Task 1 sanitizes the carried name to the empty string whenever the method is not name-bearing; Task 2 asserts the sanitization is observable on the RESPONSE header (the Gemini finding, accepted in D-20's adjudication table) |
| T-118-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages and does not touch `fuzz/Cargo.toml`. The suite pin and its legitimacy gate live in plan 118-02 |
</threat_model>

<verification>
- `cargo test --features full --lib server::streamable_http_server` exits 0 with a nonzero test count.
- `cargo test --features full --test v2_mcp_name_name_bearing_only` exits 0 with a nonzero test count.
- `make comply-bindings-check` exits 0.
- `make quality-gate` exits 0.
- Both executed negative controls (restore the old presence rule; restore `logical_name_key`) are
  recorded verbatim in the SUMMARY.

**D-19 note on the commands above.** Each `<automated>` block runs the build/test invocation under
`bash -o pipefail` before any downstream `grep`, so the reported status is the status of `cargo`,
not of the last pipe stage. Do not "simplify" any of them back into a bare `cmd | tee | tail`.

**Latency expectation (not a hang).** `make quality-gate` is the slowest command here — expect
**8-20 minutes**, not seconds. It runs fmt, clippy with pedantic+nursery, build, the full test suite,
`cargo audit` and `make comply`. Let it finish; do not interrupt and retry, and do not add a
watch-mode or `--no-fail-fast`-style flag to make it feel faster.
</verification>

<success_criteria>
- A v2 request for a non-name-bearing method with no `Mcp-Name` is served, proved over real HTTP.
- A v2 request for ANY name-bearing method — MRTR or tasks — with a missing or mismatched `Mcp-Name`
  is still rejected, proved over real HTTP for all six methods.
- A stray `Mcp-Name` on a non-name-bearing method is discarded, observable on the response header.
- The rustdoc names Phase-113 DRIFT-1, Phase-118 D-13 and Phase-118 D-18, stating what changed and
  what was retained; `contracts/mcp-protocol-sdk-v1.yaml` carries the matching equation.
- No restatement of the old universal-presence rule survives anywhere in the repo.
- The FUZZ arm exists (byte-level proptest), and the exemption recorded in the pre-review plan is
  gone.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-01-SUMMARY.md` when done.
Record: the exact new error strings; the quoted literal-contract list; both negative-control
transcripts; the proptest case counts; the `file:line` verdicts from the Task 3 sweep; the
`v2_header_gate` equation as committed; and the `make comply` report.
</output>
