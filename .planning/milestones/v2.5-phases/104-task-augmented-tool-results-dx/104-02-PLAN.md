---
phase: 104-task-augmented-tool-results-dx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/mod.rs
  - src/server/core.rs
  - src/server/task_dispatch.rs
  - tests/tool_output_passthrough.rs
autonomous: true
requirements: [TOUT-01]
must_haves:
  truths:
    - "A ToolHandler that returns ToolOutput::Result(CallToolResult) lands that result on the wire verbatim — _meta preserved, no text-wrap, no widget enrichment"
    - "A ToolHandler that does not override handle_output behaves exactly as before (default delegates to handle() and wraps as Payload)"
    - "The Payload path still runs the Phase 102 create-path gate (maybe_build_task_created) FIRST, preserving D-STORE-MINTS-ID"
    - "ToolOutput::Result BYPASSES response middleware (process_response) — the handler owns the full envelope (D-04 + D-04a, USER-APPROVED and LOCKED); ToolOutput::Payload and handler errors flow through the existing middleware chain unchanged and a response middleware still fires for them"
    - "REQUEST middleware (process_request) STILL runs before a ToolOutput::Result tool executes — only RESPONSE middleware is bypassed (D-04a hardening test #2)"
    - "Handler errors returned from handle_output() STILL route through the existing error handling (handle_tool_error / response-middleware error path) — the bypass applies ONLY to the Ok(ToolOutput::Result(_)) arm (D-04a hardening test #3)"
    - "Server (mod.rs) and ServerCore (core.rs) produce identical output for the same ToolOutput::Result"
    - "TOUT-01 targets the native Server/ServerCore wire dispatch ONLY (ToolHandler + Server are #[cfg(not(target_arch = \"wasm32\"))]); the wasm-served-tool path (WasmTool) and the workflow-internal tool-step path (execute_tool_with_middleware / WorkflowPromptHandler fallback) are explicitly OUT of scope"
  artifacts:
    - path: "src/server/mod.rs"
      provides: "ToolOutput enum (with loud bypass rustdoc on the Result variant) + ToolHandler::handle_output default + Payload/Result branch in handle_call_tool"
      contains: "enum ToolOutput"
    - path: "src/server/task_dispatch.rs"
      provides: "shared pass-through helper so both dispatchers branch identically (D-05)"
    - path: "src/server/core.rs"
      provides: "ServerCore twin ToolOutput branch"
    - path: "tests/tool_output_passthrough.rs"
      provides: "verbatim passthrough + default-delegation + dispatch-parity + response-middleware fires-on-Payload/bypassed-on-Result + request-middleware-still-runs + handler-error-path tests"
  key_links:
    - from: "src/server/mod.rs::handle_call_tool"
      to: "handler.handle_output"
      via: "native dispatch call-site swap (mod.rs ~1394)"
      pattern: "handle_output"
    - from: "src/server/mod.rs Payload arm"
      to: "task_dispatch::maybe_build_task_created"
      via: "create-path gate keeps precedence (D-04)"
      pattern: "maybe_build_task_created"
    - from: "src/server/mod.rs Result arm"
      to: "ToolMiddlewareChain::process_response"
      via: "deliberately NOT called on the Ok(Result) path (handler owns envelope, D-04/D-04a); request middleware + error path still run"
      pattern: "process_response"
---

<objective>
Add the core TOUT-01 mechanism: a `ToolOutput` enum + a default-impl `ToolHandler::handle_output()` method, and a shared dispatch branch (honored identically by `Server` and `ServerCore`, D-05) that sends `ToolOutput::Result` to the wire verbatim while `ToolOutput::Payload` keeps today's create-path gate + response-middleware + text-wrap behavior. Closes the `src/server/mod.rs:1493` double-wrap junction without any breaking change, and encodes the USER-APPROVED D-04a hardening around the response-middleware bypass.

Purpose: A hand-written `ToolHandler` (the pmcp.run case) can own the full `CallToolResult` envelope, `_meta` included, through the normal front door — while existing response middleware (redaction/sanitization) continues to fire for every `Payload`-path tool exactly as today, REQUEST middleware still fires for every tool, and handler errors still route through error handling.
Output: `ToolOutput` (with loud bypass rustdoc), `handle_output`, shared pass-through helper, both native dispatch tails branched, and the full D-04a regression battery (passthrough + response-middleware-behavior + request-middleware-still-runs + handler-error-path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-task-augmented-tool-results-dx/104-CONTEXT.md
@.planning/phases/104-task-augmented-tool-results-dx/104-RESEARCH.md
@.planning/phases/104-task-augmented-tool-results-dx/104-PATTERNS.md

<interfaces>
<!-- Verified against live source pmcp 2.11.0. -->

src/server/mod.rs:
  #[cfg(not(target_arch = "wasm32"))] #[async_trait] pub trait ToolHandler  // :227-238 — the WHOLE trait is not(wasm32)-gated
    async fn handle(&self, args: Value, extra: cancellation::RequestHandlerExtra) -> Result<Value>;  // :231
    fn metadata(&self) -> Option<ToolInfo> { None }  // :235 — precedent for a default method existing impls never override
  #[cfg(not(target_arch = "wasm32"))] pub struct Server { tools: HashMap<String, Arc<dyn ToolHandler>>, .. }  // :318-323 — Server is ALSO not(wasm32)-gated ⇒ handle_call_tool is native-only
  async fn handle_call_tool(...)  // :1282 — native dispatch. NATIVE middleware-wrapped block :1382-1418; call site handler.handle at :1394
    process_request(&req.name, &mut args, &mut extra, &context).await?;  // :1387-1391 — REQUEST middleware; runs BEFORE the handler for EVERY tool (must keep firing on the Result path)
    let mut result = handler.handle(args, extra).await;  // :1394 — result: Result<Value>
    process_response(&req.name, &mut result, &context)   // :1397-1406 — RESPONSE middleware; takes &mut Result<Value>
    handle_tool_error(&req.name, e, &context)            // :1408-1415 — on Err(result); the error path that must STILL run for handler errors
  #[cfg(target_arch = "wasm32")] let result = handler.handle(..)  // :1420-1422 — DEAD CODE: the enclosing impl/Server is not(wasm32); never compiled. Do NOT touch.
  unconditional token cleanup: self.cancellation_manager.remove_token(&request_id_str).await;  // :1424-1428 — must still run for the Result path
  Err(Error::ToolRejected{..}) => return Ok(serde_json::to_value(CallToolResult::rejected(..))?)  // :1438 — the verbatim early-return shape ToolOutput::Result mirrors (note it early-returns AFTER token cleanup)
  create-path gate call maybe_build_task_created(..)  // :1463-1490
  unconditional wrap: let text = result.to_string(); CallToolResult::new(vec![Content::text(text)]); with_widget_enrichment(info, result)  // :1493-1497
  ServerBuilder::tool_middleware(Arc<dyn ToolMiddleware>) -> Self  // :3431 — how a response/request middleware is registered for the regression tests

src/server/core.rs:
  enum ToolCallOutcome { .., Result(CallToolResult), TaskCreated{task_value} }  // :303
  NATIVE middleware-wrapped block: let mut result = handler.handle(args, extra).await;  // :519 — result: Result<Value>; process_request before it; process_response :522-531; handle_tool_error :534-540; block value at :560
  #[cfg(target_arch = "wasm32")] handler.handle(..)  // :549 — ServerCore wasm branch (leave as-is; not a ToolOutput site)
  own inline create-path gate  // ~:594-616 — HAND-DUPLICATED check, NOT a call to maybe_build_task_created (pre-existing Phase 102 divergence). OUT OF SCOPE to unify (WARNING 5).
  wrap tail :594-628 (summarize_structured_output + to_string_pretty branch :621-624); ToolCallOutcome::Result at :628, consumed :1127
  execute_tool_with_middleware(&self, tool_name, args, extra) -> Result<Value>  // :816-876; handler.handle at :852 — WORKFLOW-INTERNAL path (MiddlewareExecutor). OUT OF SCOPE — keep on handle()/Value.

src/server/workflow/prompt_handler.rs:
  execute_tool_step: middleware_executor.execute_tool_with_middleware(..)  // :637-639 (production path → core.rs:852)
  fallback: handler.handle(params, extra.clone()).await  // :647 — Result<Value>. OUT OF SCOPE — keep on handle()/Value (sibling of :852; both stay consistent).

src/server/builder_middleware_executor.rs:
  MiddlewareExecutor::execute_tool_with_middleware → handler.handle(args, extra.clone()).await  // :129 — THIRD workflow-internal implementer (Server/ServerBuilder-supplied middleware executor). OUT OF SCOPE — keep on handle()/Value, same rule as core.rs:852 and prompt_handler.rs:647.

src/server/tool_middleware.rs:
  #[async_trait] pub trait ToolMiddleware  // :223
    async fn on_request(&self, tool_name: &str, args: &mut Value, ctx) -> Result<()>  // request-side hook (for the request-middleware-still-runs test)
    async fn on_response(&self, tool_name: &str, result: &mut Result<Value>, context: &ToolContext) -> Result<()>  // :294 — operates on Value
  ToolMiddlewareChain::process_request / process_response(&self, tool_name, result: &mut Result<Value>, context) -> Result<()>  // :418-434 — the API the swap must stay compatible with

src/server/task_dispatch.rs:
  pub(crate) async fn maybe_build_task_created(&self, id, value:&Value, task_support, task_requested, auth) -> Option<JSONRPCResponse>  // :311 — gates on value.get("taskId") && value.get("status"); a CallToolResult has NEITHER at top level ⇒ passes for free
  // Why: production-reachability annotation template at :308-310
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolOutput enum + ToolHandler::handle_output default (native), with loud bypass rustdoc</name>
  <files>src/server/mod.rs</files>
  <read_first>
    - src/server/mod.rs (ToolHandler trait + metadata default :226-238 — note the WHOLE trait is #[cfg(not(target_arch = "wasm32"))]; the #[async_trait] convention)
    - src/types/tasks.rs (#[non_exhaustive] convention on Task :91)
    - src/server/typed_tool.rs (TypedTool/TypedToolWithOutput impl ToolHandler :253/:429 — confirm existing impls only define handle+metadata, so the default handle_output covers them)
  </read_first>
  <behavior>
    - a ToolHandler that implements only handle() (no handle_output override) routed through handle_output returns ToolOutput::Payload(v) equal to what handle() returned
    - ToolOutput is #[non_exhaustive]
  </behavior>
  <action>
    Add a public `ToolOutput` enum near the `ToolHandler` trait in src/server/mod.rs with variants `Payload(serde_json::Value)` and `Result(crate::types::CallToolResult)`; annotate `#[non_exhaustive]`; derive `Debug` (do NOT derive Serialize/Deserialize — it is a control enum, not a wire type). Place it under the same `#[cfg(not(target_arch = "wasm32"))]` gate as `ToolHandler` (:227).

    LOUD BYPASS RUSTDOC (D-04a hardening #1 — USER-APPROVED, LOCKED): on the `Result(CallToolResult)` variant write a prominent rustdoc paragraph stating that the contained `CallToolResult` is sent to the wire VERBATIM — it BYPASSES response middleware (redaction/sanitization/audit), text-wrapping, and widget enrichment. The tool is responsible for its OWN redaction/sanitization of `content` and `_meta`, at the same trust level as returning a raw `Value` today. REQUEST middleware still runs; handler errors still route through error handling. Point authors to the migration guide (Plan 05). This callout MUST live on the type itself, not only in migration docs.

    `ToolHandler` and `Server` are BOTH native-only, so there is NO wasm `ToolHandler` surface to mirror (the wasm-served-tool path is the unrelated synchronous `WasmTool` trait in src/server/wasm_server.rs, out of scope for this phase). Add a THIRD trait method to `ToolHandler` (mod.rs:229): `async fn handle_output(&self, args: Value, extra: cancellation::RequestHandlerExtra) -> Result<ToolOutput>` with a DEFAULT body `self.handle(args, extra).await.map(ToolOutput::Payload)` (mirror how `metadata()` at :235 is a default existing impls never override). Do NOT yet change the dispatch tails (Task 2). Add a unit test asserting the default delegates to handle → Payload.
  </action>
  <verify>
    <automated>cargo test --features full --lib server:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/server/mod.rs contains `enum ToolOutput` with `#[non_exhaustive]`, under the not(wasm32) gate
    - the `ToolOutput::Result` variant carries rustdoc that explicitly says it bypasses response middleware and the tool owns its own redaction (D-04a #1); grep for "bypass" near the variant
    - src/server/mod.rs contains `async fn handle_output` with a default body referencing `ToolOutput::Payload`
    - existing TypedTool/TypedToolWithOutput impls compile unchanged (no override added)
    - NO wasm `ToolHandler` mirroring was added (there is no wasm ToolHandler surface — confirmed dead-code branch at mod.rs:1422)
    - default-delegation unit test passes
  </acceptance_criteria>
  <done>ToolOutput + handle_output default exist on the native ToolHandler; the Result variant carries loud bypass rustdoc; existing handlers untouched; no vacuous wasm mirroring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared pass-through branch in both NATIVE dispatch tails (middleware-aware)</name>
  <files>src/server/task_dispatch.rs, src/server/mod.rs, src/server/core.rs, tests/tool_output_passthrough.rs</files>
  <read_first>
    - src/server/mod.rs (native middleware-wrapped block :1382-1418; process_request :1387-1391; handler.handle call site :1394; process_response :1397-1406; handle_tool_error :1408-1415; unconditional token cleanup :1424-1428; ToolRejected early-return :1438; create-path gate :1463-1490; wrap tail :1492-1500)
    - src/server/core.rs (ToolCallOutcome :303; native block + handler.handle :519; process_response :522-531; wrap tail + own inline task-shape gate :594-628; outcome consumption :1117-1130; execute_tool_with_middleware :816-876 — OUT OF SCOPE)
    - src/server/tool_middleware.rs (process_request/process_response signature :418-434; on_response :294 — the &mut Result<Value> contract the swap must stay compatible with)
    - src/server/task_dispatch.rs (maybe_build_task_created :311-336; // Why annotation template :308-310)
    - tests/tool_as_task_lifecycle.rs (in-process duplex harness — NOTE: its ready-made duplex setup covers ServerCore only, via ProtocolHandler; its header comment about Server lacking a TaskStore is stale post-Phase-102. Build the Server-side half of the parity test fresh: reuse the generic duplex Transport impl from this file with Server::run, or the HTTP-loopback pattern from tests/tool_as_task_lifecycle_http.rs — acceptance only requires equal serialized results, not a specific harness)
  </read_first>
  <behavior>
    - a handler returning ToolOutput::Result(CallToolResult::new(vec![Content::text("x")]).with_meta(map with _meta[related-task])) produces a wire result whose top-level `_meta` is present and whose content is NOT the stringified envelope
    - a handler returning ToolOutput::Payload(json) behaves byte-identically to the pre-change text-wrap (regression: existing example/tool output unchanged)
    - a Payload value that IS task-shaped ({taskId,status}) still routes through maybe_build_task_created (create-path precedence preserved)
    - Server and ServerCore produce the same serialized result for the same ToolOutput::Result
    - REQUEST middleware (process_request) runs before the handler on BOTH the Payload and the Result path (only RESPONSE middleware differs)
    - a handler that returns Err(_) from handle_output still flows through handle_tool_error (the bypass is scoped to Ok(ToolOutput::Result(_)) only)
    - the code compiles: process_request / process_response / handle_tool_error keep their existing `&mut Result<Value>` signature (no API change to ToolMiddlewareChain)
  </behavior>
  <action>
    LOCKED DECISION to encode (D-04 + D-04a, USER-APPROVED during cross-AI review): `ToolOutput::Result` BYPASSES RESPONSE middleware — `process_response` is NOT called on the successful Result path, because the handler owns the full envelope. What is NOT bypassed and MUST still run on the Result path: (a) REQUEST middleware (`process_request`) before the handler; (b) `handle_tool_error` for a handler that returns `Err(_)`; (c) the unconditional token cleanup. `ToolOutput::Payload` and handler errors continue through the existing middleware chain UNCHANGED. Add a `// Why:` comment at the branch stating this is a deliberate, LOCKED design choice (D-04a — user-approved: "keep the bypass, harden it"), not an implementation accident, so security-relevant RESPONSE middleware (redaction/sanitization) applies to Payload-path tools but a `tool_with_result`/`ToolOutput::Result` tool is trusted to emit its own envelope.

    In BOTH native dispatchers, swap the call site `handler.handle(...)` → `handler.handle_output(...)`: mod.rs:1394 and core.rs:519. Do NOT touch the dead wasm branch mod.rs:1422 (enclosing Server is not(wasm32) — never compiled) nor the ServerCore wasm branch core.rs:549. Do NOT swap `ServerCore::execute_tool_with_middleware` (core.rs:852), the `WorkflowPromptHandler` fallback (prompt_handler.rs:647), nor the third `MiddlewareExecutor` implementer in `src/server/builder_middleware_executor.rs:129` (the Server/ServerBuilder-supplied executor for workflow prompt steps): the workflow-internal tool-step path keeps the `handle()` → `Value` contract (a workflow step consumes tool output as internal data, not as a wire envelope; converting a `ToolOutput::Result` back to `Value` there would re-flatten the envelope and recreate the double-wrap bug class inside the workflow pipeline). The two workflow paths stay consistent with each other (both on `handle()`).

    Preserve the request-middleware and error paths: `process_request` already runs BEFORE the swapped call site (mod.rs:1387-1391 / the core.rs equivalent) — leave it there so it fires for EVERY tool regardless of `ToolOutput` variant. Restructure so the existing middleware block still compiles against the unchanged `process_response(&mut Result<Value>)` API: after `handler.handle_output(...)` returns `Result<ToolOutput>`, coerce it into the existing `result: Result<Value>` middleware variable for the Payload/error cases — `Ok(ToolOutput::Payload(v)) => Ok(v)`, `Err(e) => Err(e)` — then run `process_response` + `handle_tool_error` and the create-path gate + wrap tail EXACTLY as today (so a handler `Err(_)` STILL goes through `handle_tool_error`). For `Ok(ToolOutput::Result(r))` ONLY: skip the response-middleware block AND the wrap tail; run the unconditional token cleanup (mod.rs:1424-1428 / the core.rs equivalent) so cleanup parity is preserved, then emit `serde_json::to_value(r)` verbatim (mirror the ToolRejected verbatim-return shape at mod.rs:1438, which itself early-returns after cleanup). NO `maybe_build_task_created` routing, NO text-wrap, NO `with_widget_enrichment` on the Result arm.

    Introduce a single shared helper (a `pub(crate)` free fn or `TaskDispatch` method in src/server/task_dispatch.rs) that both dispatchers call to resolve a `ToolOutput` into "verbatim Result value" vs "Payload value fed to the existing tail", so the Payload-vs-Result decision + the response-middleware-bypass rule exist in ONE place (D-05 anti-drift). Add a `// Why:` reachability annotation and keep it ≤ cognitive complexity 25. Do NOT add implicit `from_value::<CallToolResult>` sniffing on Payload (D-02 — explicitly rejected).

    SCOPE FENCE (WARNING 5): unifying ServerCore's pre-existing inline create-path gate (core.rs ~594-616, a hand-duplicated check that is NOT a call to `task_dispatch::maybe_build_task_created`) is OUT of scope for this plan — that Phase 102 divergence stays as-is. ONLY the new `ToolOutput` match (and, in Plan 03, the tripwire) is the single shared unit.

    Extend tests/tool_output_passthrough.rs covering: verbatim _meta survival, Payload regression, task-shaped-Payload create-path precedence, and Server-vs-ServerCore parity for the same ToolOutput::Result. (The response-middleware-behavior, request-middleware-still-runs, and handler-error-path D-04a regressions are Task 3.)
  </action>
  <verify>
    <automated>cargo test --features full --test tool_output_passthrough 2>&1 | tail -8 && cargo test --features full --test tool_as_task_lifecycle 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep mod.rs and core.rs show the NATIVE dispatch call sites (mod.rs:1394, core.rs:519) now use `handle_output`; core.rs:852 and prompt_handler.rs:647 still call `handle` (unchanged)
    - `process_request` still runs before the handler on both dispatchers (unchanged position)
    - the Payload/Result decision + response-middleware-bypass rule is implemented once in src/server/task_dispatch.rs and called by both dispatchers (single copy)
    - `process_request` / `process_response` / `handle_tool_error` signatures unchanged; the crate compiles (`cargo build --features full` clean)
    - a `// Why:` comment documents that ToolOutput::Result deliberately bypasses RESPONSE middleware (D-04 + D-04a) while request middleware + error path still run
    - `cargo test --features full --test tool_output_passthrough` exits 0
    - passthrough test asserts `result._meta` present at top level AND content[0].text does NOT contain a stringified `_meta`
    - parity test asserts Server and ServerCore emit equal serialized results
    - Phase 101/102 regression `cargo test --features full --test tool_as_task_lifecycle` exits 0
    - no `from_value::<CallToolResult>` sniffing appears on the Payload path
  </acceptance_criteria>
  <done>ToolOutput::Result reaches the wire verbatim from both native dispatchers and bypasses RESPONSE middleware only; request middleware, error path, create-path precedence, and the Payload path are unchanged; no drift, no sniffing; workflow-internal path untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-04a middleware/error regression battery (response bypass + request-still-runs + error-path)</name>
  <files>tests/tool_output_passthrough.rs</files>
  <read_first>
    - src/server/tool_middleware.rs (ToolMiddleware trait + on_request + on_response :223-300 — implement a tiny recording/redaction middleware with BOTH hooks)
    - src/server/mod.rs (ServerBuilder::tool_middleware registration :3431; example middleware unit tests :4604-4830; handle_tool_error :1408-1415)
    - tests/tool_as_task_lifecycle.rs (in-process duplex Server + ServerCore setup)
  </read_first>
  <behavior>
    - RESPONSE bypass (D-04a): a response middleware whose `on_response` records the tool name and rewrites/redacts the Value FIRES for a tool that returns ToolOutput::Payload (the emitted result reflects the middleware mutation); the SAME middleware is BYPASSED for a tool that returns ToolOutput::Result (the emitted CallToolResult is verbatim; on_response did NOT record it and did NOT mutate _meta/content). Holds on Server AND ServerCore.
    - REQUEST still runs (D-04a #2): the same middleware's `on_request` records/mutates args and FIRES for the ToolOutput::Result tool BEFORE it executes (assert the request hook saw the Result tool even though the response hook did not). Holds on Server AND ServerCore.
    - ERROR path (D-04a #3): a ToolOutput::Result tool whose handler returns Err(_) still produces an error response routed through handle_tool_error (assert the wire result is an error / the error hook path ran) — i.e. the bypass is scoped to the Ok(ToolOutput::Result(_)) arm, not the Err arm.
  </behavior>
  <action>
    Implement a small test-only `ToolMiddleware` (e.g. a `RecordingMiddleware` holding an `Arc<Mutex<Vec<String>>>` of request-seen names and an `Arc<Mutex<Vec<String>>>` of response-seen names, plus a marker mutation in `on_response`). Register it via `ServerBuilder::tool_middleware`. Add tools: (1) a `Payload`-returning tool (default handle); (2) a `ToolOutput::Result`-returning tool (a hand-written `ToolHandler` overriding `handle_output`, so Task 3 does not depend on Plan 04); (3) a `ToolOutput::Result`-returning tool whose `handle_output` returns `Err(_)`. Drive `tools/call` for each through the in-process duplex Server, then repeat against ServerCore. Assert:
    - the RESPONSE-seen set CONTAINS the Payload tool and DOES NOT contain the successful Result tool; the Payload result shows the middleware mutation; the successful Result result is byte-verbatim (top-level `_meta` intact, content not stringified, no middleware marker) — this is the (d) regression that locks the D-04/D-04a "Result bypasses RESPONSE middleware" decision;
    - the REQUEST-seen set CONTAINS the successful Result tool (request middleware still fired before it executed) — D-04a hardening #2;
    - the erroring Result tool yields an error response (routed through handle_tool_error), proving the bypass does not swallow handler errors — D-04a hardening #3.
    All three assertion groups run on BOTH Server and ServerCore (D-05).
  </action>
  <verify>
    <automated>cargo test --features full --test tool_output_passthrough middleware 2>&1 | tail -8 && cargo test --features full --test tool_output_passthrough error_path 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - tests/tool_output_passthrough.rs contains a test-only ToolMiddleware implementing BOTH `on_request` and `on_response`
    - a test asserts the RESPONSE middleware fired for the Payload tool and was bypassed for the successful ToolOutput::Result tool (D-04a #1, locked)
    - a test asserts the REQUEST middleware fired for the ToolOutput::Result tool before it executed (D-04a #2)
    - a test asserts a ToolOutput::Result handler returning Err(_) still routes through handle_tool_error / yields an error response (D-04a #3)
    - all three are exercised for BOTH Server and ServerCore
    - `cargo test --features full --test tool_output_passthrough middleware` and `... error_path` exit 0
  </acceptance_criteria>
  <done>The D-04a hardening is proven: response middleware bypassed for successful Result, request middleware still fires, handler errors still route through error handling — on both dispatchers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool handler → dispatch → wire | handler-owned CallToolResult crosses to the wire un-re-wrapped |
| response middleware → tool result | redaction/sanitization middleware inspects/mutates the tool's Value before the wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02-01 | Spoofing/Tampering | handler smuggles a task envelope via ToolOutput::Result to bypass D-STORE-MINTS-ID | mitigate | ToolOutput::Result is NOT the create-path — it emits a CallToolResult, never mints a store task; a real task still requires task-shaped Payload through maybe_build_task_created; _meta[related-task].taskId in a Result is a client hint, tasks/get still enforces owner scoping server-side. |
| T-104-02-02 | Tampering | branch relocated above auth/request middleware path | accept/mitigate | The ToolOutput branch stays AFTER the existing auth + request middleware path (mod.rs:1367-1391); process_request still runs for every tool (asserted by the Task 3 request-still-runs test); the Result arm touches no auth. Do not relocate the branch above auth/request validation. |
| T-104-02-03 | Information Disclosure | ToolOutput::Result bypasses response-middleware redaction/sanitization | mitigate (USER-APPROVED, LOCKED — D-04a) | Codex rated this MEDIUM-HIGH; the user reviewed and chose "keep the bypass, harden it" (D-04a). The handler owns the full envelope, same trust level as today's Value return. Hardening SHIPPED: (1) loud rustdoc on the ToolOutput::Result variant AND tool_with_result (Plan 02 T1 / Plan 04 T1); (2) request-middleware-still-runs test (Plan 02 T3 #2); (3) handler-error-path regression (Plan 02 T3 #3); (4) migration-guide + rustdoc callout that a tool_with_result/ToolOutput::Result tool owns its own redaction (Plan 05 T3). Payload-path tools retain full response middleware. There is deliberately NO result-aware response-middleware hook (rejected by D-04a). |
| T-104-02-04 | Tampering | two dispatchers drift (D-05) | mitigate | Single shared decision helper in task_dispatch.rs (Payload-vs-Result + response-middleware-bypass rule) + parity test asserting byte-identical Server/ServerCore output; the Task 3 bypass/request/error assertions all run on both. |
| T-104-02-05 | Tampering | workflow-internal tool step re-flattens a ToolOutput::Result envelope | mitigate | execute_tool_with_middleware (core.rs:852) + WorkflowPromptHandler fallback (prompt_handler.rs:647) + the builder-supplied MiddlewareExecutor (builder_middleware_executor.rs:129) are explicitly kept on handle()/Value — never see ToolOutput — so no envelope round-trips through the workflow Value contract. |
</threat_model>

<verification>
- `cargo build --features full` clean (middleware block still compiles against unchanged process_request/process_response API).
- `cargo test --features full --test tool_output_passthrough` green (passthrough + parity + response-bypass + request-still-runs + error-path).
- `cargo test --features full --test tool_as_task_lifecycle` green (no Phase 101/102 regression).
- `pmat analyze complexity --max-cognitive 25` shows no new violation for the shared helper (or a `// Why:`-justified allow within the cog-50 hard cap).
</verification>

<success_criteria>
ToolOutput::Result lands verbatim through both Server and ServerCore and bypasses RESPONSE middleware by USER-APPROVED locked design (D-04a), while request middleware, handler-error routing, create-path precedence, and the Payload path are unchanged; default handle_output preserves existing behavior; workflow-internal path untouched.
</success_criteria>

<output>
Create `.planning/phases/104-task-augmented-tool-results-dx/104-02-SUMMARY.md` when done
</output>
