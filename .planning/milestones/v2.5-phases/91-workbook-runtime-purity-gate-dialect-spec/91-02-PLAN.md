---
phase: 91-workbook-runtime-purity-gate-dialect-spec
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - Cargo.toml
  - crates/pmcp-workbook-dialect/Cargo.toml
  - crates/pmcp-workbook-dialect/src/lib.rs
  - docs/workbook-dialect-spec.md
autonomous: true
requirements: [WBDL-01]
user_setup: []

must_haves:
  truths:
    - "The SDK owns a versioned dialect spec document (docs/workbook-dialect-spec.md) listing the flat 13-function whitelist + refuse-set"
    - "The WHITELIST const holds exactly the 13 flat first-class function names (D-05) — no two-tier 8-core/5-widened framing"
    - "A binding test fails the build if the spec doc's whitelist table and the WHITELIST const diverge (WBDL-01)"
    - "pmcp-workbook-dialect depends on pmcp-workbook-runtime and re-exports the finding types (LintFinding/Severity/LintReport) per D-03"
    - "The dialect crate is reader-free (no umya/quick-xml/swc/pmcp-code-mode) and is a registered workspace member (slot 2b)"
  artifacts:
    - path: "crates/pmcp-workbook-dialect/Cargo.toml"
      provides: "Reader-free dialect-contract leaf manifest; path-dep on runtime"
      contains: "pmcp-workbook-runtime"
    - path: "crates/pmcp-workbook-dialect/src/lib.rs"
      provides: "WHITELIST const (13 flat fns) + DialectRules + CandidateRole + re-exported finding types + binding test"
      contains: "WHITELIST"
    - path: "docs/workbook-dialect-spec.md"
      provides: "Published BA/auditor-facing dialect contract doc with the flat-13 whitelist table"
      contains: "VLOOKUP"
  key_links:
    - from: "crates/pmcp-workbook-dialect/src/lib.rs"
      to: "docs/workbook-dialect-spec.md"
      via: "binding test parses the whitelist table and asserts set-equality with WHITELIST (SPEC_PATH ../../docs/...)"
      pattern: "doc_whitelist_table_matches_const"
    - from: "crates/pmcp-workbook-dialect/src/lib.rs"
      to: "pmcp_workbook_runtime::finding"
      via: "re-export of LintFinding/LintReport/Severity (D-03)"
      pattern: "pmcp_workbook_runtime::finding"
    - from: "Cargo.toml"
      to: "crates/pmcp-workbook-dialect"
      via: "workspace members array"
      pattern: "crates/pmcp-workbook-dialect"
---

<objective>
Create the reader-free `pmcp-workbook-dialect` leaf crate (slot 2b, D-01) holding the dialect *contract*: the flat 13-function `WHITELIST` const (D-05), `DialectRules` + `CandidateRole` + colour-ontology constants lifted from the lighthouse compiler's `dialect/rules.rs`, the published `docs/workbook-dialect-spec.md`, and the doc↔const binding test. Re-export the runtime's finding types (D-03). Deliver only the contract — NOT the linter execution or `WorkbookMap` (those are Phase 93 per D-02).

Purpose: Establish the SDK-owned, independently-versionable governance contract that the future linter (Phase 93) enforces and that the spec doc and code can never drift from.

Output: A compiling, tested `crates/pmcp-workbook-dialect` crate + the ported spec doc; satisfies WBDL-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-CONTEXT.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-01-SUMMARY.md

<interfaces>
<!-- This plan depends on 91-01 (runtime). It re-exports the runtime's finding types. -->

From crates/pmcp-workbook-runtime/src/finding.rs (created in 91-01):
  pub enum Severity { Error, Warning, Info }
  pub struct LintFinding { severity, rule: String, sheet, cell: Option<String>, message, repair }
  pub struct LintReport { findings: Vec<LintFinding> }
  // re-export via: pub use pmcp_workbook_runtime::finding::{LintFinding, LintReport, Severity};

From crates/pmcp-sql-server/Cargo.toml:33 (SDK path-dep convention — always version + path together):
  pmcp-server-toolkit = { version = "0.1.0", path = "../pmcp-server-toolkit", features = [...] }

The 13 flat WHITELIST names (D-05 — drop the lighthouse 8-core/5-widened split):
  IF, VLOOKUP, INDEX, MATCH, SUMIF, SUM, ROUNDUP, CEILING, IFERROR, ISNUMBER, SEARCH, ROUND, TEXT

SPEC_PATH (relative to CARGO_MANIFEST_DIR; depth identical to lighthouse):
  "../../docs/workbook-dialect-spec.md"   // crates/pmcp-workbook-dialect → repo-root docs (two segments up)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dialect crate manifest + register in workspace</name>
  <files>Cargo.toml, crates/pmcp-workbook-dialect/Cargo.toml</files>
  <read_first>
    - crates/pmcp-server-toolkit/Cargo.toml (SDK package-header convention)
    - crates/pmcp-sql-server/Cargo.toml (line ~33 — the version+path workspace-internal dep shape)
    - Cargo.toml (root — the [workspace] members array; append after the runtime crate added in 91-01)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§crates/pmcp-workbook-dialect/Cargo.toml — the exact resulting manifest)
  </read_first>
  <action>
    Create `crates/pmcp-workbook-dialect/Cargo.toml`. [package]: name `pmcp-workbook-dialect`, literal `version = "0.1.0"`, `edition = "2021"`, `license = "MIT OR Apache-2.0"`, `repository = "https://github.com/paiml/rust-mcp-sdk"`, `description = "Versioned workbook dialect contract — function whitelist, refuse-set, colour ontology + spec-doc binding test"`, `exclude = [".planning/", ".pmat/", "fuzz/", "tests/"]`. [lib]: `name = "pmcp_workbook_dialect"`, `path = "src/lib.rs"`. [dependencies]: `pmcp-workbook-runtime = { version = "0.1.0", path = "../pmcp-workbook-runtime" }` (version + path together — SDK convention). Do NOT add `serde` — the lighthouse `rules.rs` types (`DialectRules`, `CandidateRole`, `WHITELIST`) carry no serde derives and need none this phase (research §Supporting). Then append `"crates/pmcp-workbook-dialect"` to the root `Cargo.toml` `[workspace] members` array (immediately after `"crates/pmcp-workbook-runtime"`); do NOT add it to `exclude`.
  </action>
  <verify>
    <automated>test -f crates/pmcp-workbook-dialect/Cargo.toml && grep -q 'pmcp-workbook-runtime = { version = "0.1.0", path = "../pmcp-workbook-runtime" }' crates/pmcp-workbook-dialect/Cargo.toml && grep -q 'crates/pmcp-workbook-dialect' Cargo.toml && ! grep -q 'version.workspace' crates/pmcp-workbook-dialect/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-workbook-dialect/Cargo.toml` exists with literal `version = "0.1.0"` and a `version + path` dep on `pmcp-workbook-runtime`.
    - No `serde` dependency, no `version.workspace`.
    - Root `Cargo.toml` `members` contains `crates/pmcp-workbook-dialect` and it is NOT in `exclude`.
  </acceptance_criteria>
  <done>The dialect crate is a registered workspace member depending on the runtime crate via the SDK version+path convention.</done>
</task>

<task type="auto">
  <name>Task 2: Port the dialect spec doc + flatten the whitelist table (D-05)</name>
  <files>docs/workbook-dialect-spec.md</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/docs/workbook-dialect-spec.md (155 LOC — the doc to port; the whitelist table at ~lines 49-70 uses `core` / `**D-09 widened**` category labels + a "Total: 13 names (8 core + 5 widened)" summary line at ~line 70; a note at ~line 72 points at `dialect::dialect_spec::doc_whitelist_table_matches_const`)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§docs/workbook-dialect-spec.md — the D-05 flatten instructions + the binding-test coupling note, Pitfall 4)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md (§Pattern 3 / Pitfall 4 — keep a parseable category column the binding test keys on)
  </read_first>
  <action>
    Copy lighthouse `docs/workbook-dialect-spec.md` to repo-root `docs/workbook-dialect-spec.md`. Flatten the whitelist table for D-05: relabel ALL 13 function rows to a single category value `whitelist` in the category column (drop the `core` / `**D-09 widened**` split), and remove the "8 core + 5 widened" parenthetical from the total line so it reads "Total: 13 names". KEEP a parseable category column the binding test will key on (the parser predicate in Task 3 will match `category == "whitelist"`) and KEEP each function name as the first backtick token on its row (the parser takes the first backtick token). Update the note that points at the binding test to the new SDK test path (`pmcp_workbook_dialect`). Preserve the refuse-set §5 and enforced-vs-deferred §6 sections verbatim. Do NOT remove any of the 13 names (D-07 — removing any breaks the reference workbook's clean lint in Ph93/96).
  </action>
  <verify>
    <automated>test -f docs/workbook-dialect-spec.md && for fn in IF VLOOKUP INDEX MATCH SUMIF SUM ROUNDUP CEILING IFERROR ISNUMBER SEARCH ROUND TEXT; do grep -q "$fn" docs/workbook-dialect-spec.md || { echo "MISSING $fn"; exit 1; }; done && ! grep -q 'D-09 widened' docs/workbook-dialect-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/workbook-dialect-spec.md` exists and contains all 13 function names.
    - The two-tier framing is gone (no `D-09 widened` label; no "8 core + 5 widened" parenthetical).
    - A parseable category column remains (each whitelist row carries the single category value the Task 3 parser keys on) with each function name as the first backtick token on its row.
  </acceptance_criteria>
  <done>The published spec doc lives at repo-root docs/ with a flat 13-row whitelist table, parseable by the binding test, all 13 names preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lift rules.rs + the binding test; flatten for D-05; re-export findings (D-03)</name>
  <files>crates/pmcp-workbook-dialect/src/lib.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dialect/rules.rs (WHITELIST const at ~lines 26-31 + DialectRules + CandidateRole + colour-ontology ARGB constants + the `whitelist_is_exactly_the_thirteen_names` test at ~147-161 — lift verbatim except the D-05 flatten)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dialect/mod.rs (the `#[cfg(test)] mod dialect_spec` binding test at ~lines 29-117: `doc_whitelist_table_matches_const` + `parse_doc_whitelist` + `first_backtick_token`; the `pub use finding::...` re-export at ~line 11; do NOT lift the `linter`/`lint` re-exports at ~16-18 — Phase 93, D-02)
    - crates/pmcp-workbook-runtime/src/finding.rs (the finding types to re-export — created in 91-01)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§crates/pmcp-workbook-dialect/src/lib.rs — the exact re-export surface + the two required D-05 binding-test adaptations + Pitfall 4 guard preservation)
    - docs/workbook-dialect-spec.md (the flattened table from Task 2 — the parser must match its category column)
  </read_first>
  <behavior>
    - Test (adapted, WBDL-01): `doc_whitelist_table_matches_const` — parse the 13 function names out of `docs/workbook-dialect-spec.md`'s whitelist table (rows whose category column == `whitelist`, taking the first backtick token), build a BTreeSet, assert set-equality with `WHITELIST`. KEEP the `!doc_set.is_empty()` guard (Pitfall 4 — catches a silent empty-parse if the table format drifts).
    - Test (flattened): `whitelist_is_exactly_the_thirteen_names` — assert `WHITELIST` is exactly the 13 names; drop the now-redundant D-09 sub-loop (all 13 are first-class under D-05).
  </behavior>
  <action>
    Create `crates/pmcp-workbook-dialect/src/lib.rs` by merging two lighthouse sources. (1) Lift `rules.rs` verbatim — the `WHITELIST` const, `DialectRules`, `CandidateRole`, and the colour-ontology ARGB constants — EXCEPT apply the D-05 flatten: the `WHITELIST` const is the 13 flat names `["IF", "VLOOKUP", "INDEX", "MATCH", "SUMIF", "SUM", "ROUNDUP", "CEILING", "IFERROR", "ISNUMBER", "SEARCH", "ROUND", "TEXT"]` with the two-tier "8 core + 5 D-09 widened" comment framing removed. (2) Lift the `dialect/mod.rs` `#[cfg(test)] mod dialect_spec` binding-test mechanism (`doc_whitelist_table_matches_const`, `parse_doc_whitelist`, `first_backtick_token`) with `const SPEC_PATH: &str = "../../docs/workbook-dialect-spec.md";`. Apply the two D-05 adaptations: change the row predicate from `cols[1].trim() == "core" || category.contains("D-09 widened")` to `category == "whitelist"` (matching the Task 2 flattened table), and KEEP the `!doc_set.is_empty()` guard. (3) Add the re-export surface: `pub use pmcp_workbook_runtime::finding::{LintFinding, LintReport, Severity};` and `pub use rules::...` (or inline the rules into lib.rs directly — single-file crate is acceptable) exposing `WHITELIST`, `DialectRules`, `CandidateRole`. Do NOT re-export `linter`/`lint` (Phase 93, D-02). Flatten the `whitelist_is_exactly_the_thirteen_names` test to assert the 13 names and drop the D-09 sub-loop. If the dialect crate has any value path, add the crate-level `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]` + `#![cfg_attr(test, allow(...))]` lints (D-10).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-dialect --lib -- --test-threads=1 && cargo test -p pmcp-workbook-dialect doc_whitelist_table_matches_const -- --test-threads=1 && grep -q 'pmcp_workbook_runtime::finding' crates/pmcp-workbook-dialect/src/lib.rs && grep -q 'WHITELIST' crates/pmcp-workbook-dialect/src/lib.rs && ! grep -q 'pub use.*lint' crates/pmcp-workbook-dialect/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-dialect doc_whitelist_table_matches_const -- --test-threads=1` exits 0 (doc↔const binding holds for the flat 13).
    - `cargo test -p pmcp-workbook-dialect --lib -- --test-threads=1` exits 0.
    - `crates/pmcp-workbook-dialect/src/lib.rs` re-exports `pmcp_workbook_runtime::finding::{LintFinding, LintReport, Severity}` and exposes `WHITELIST` + `DialectRules` + `CandidateRole`.
    - The file does NOT re-export `linter`/`lint` (those are Phase 93).
    - The `WHITELIST` const has exactly 13 entries; the binding test keeps the `!doc_set.is_empty()` guard.
  </acceptance_criteria>
  <done>The dialect crate exposes the flat-13 WHITELIST + DialectRules + re-exported findings, and the doc↔const binding test passes against the flattened spec doc; linter execution is deferred to Phase 93.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialect crate → served-binary dependency cone | The dialect crate is a leaf depending only on the runtime; must remain reader-free (no umya/quick-xml/swc/pmcp-code-mode) |
| published spec doc ↔ enforced WHITELIST const | The binding test is the trust link that prevents the auditor-facing contract and the enforced rule from drifting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91D-01 | Tampering | spec doc and WHITELIST const silently diverging (governance contract integrity) | mitigate | `doc_whitelist_table_matches_const` binding test fails the build on drift; the `!doc_set.is_empty()` guard catches a silent empty-parse if the table format changes (Pitfall 4) |
| T-91D-02 | Elevation of Privilege | reader/JS crate entering the dialect tree via a transitive of a future dep | mitigate | Dialect crate depends ONLY on the runtime (no serde, no third-party); Plan 03 purity gate negatively asserts reader/JS absence in the dialect tree per feature-combination |
| T-91D-03 | Tampering | accidentally pulling linter.rs/WorkbookMap into this phase (umya leak via the compiler half) | mitigate | Lift ONLY rules.rs + the binding test; explicitly do NOT re-export `linter`/`lint` (D-02); acceptance criterion greps that out |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-dialect --lib -- --test-threads=1` green (binding test + flattened whitelist test).
- `cargo tree -p pmcp-workbook-dialect` shows only `pmcp-workbook-runtime` + its transitives — no reader/JS crate.
- The dialect crate is a workspace member (`cargo test --workspace` picks it up).
- `docs/workbook-dialect-spec.md` exists with all 13 names and no two-tier framing.
</verification>

<success_criteria>
- `pmcp-workbook-dialect` exists, compiles, and is a registered workspace member (slot 2b) depending on the runtime.
- WBDL-01: the SDK owns the versioned spec doc bound to the flat-13 `WHITELIST` by a passing binding test.
- The finding types are re-exported from the runtime (D-03); the linter/WorkbookMap are NOT present (deferred to Phase 93).
</success_criteria>

<output>
Create `.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-02-SUMMARY.md` when done.
</output>
