---
phase: 91-workbook-runtime-purity-gate-dialect-spec
plan: 03
type: execute
wave: 3
depends_on: ["91-01", "91-02"]
files_modified:
  - Makefile
  - justfile
  - crates/pmcp-workbook-runtime/deny.toml
  - crates/pmcp-workbook-dialect/deny.toml
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-VALIDATION.md
  - docs/workbook-purity-gate.md
autonomous: true
requirements: [WBRT-04]
traceability_remap: [WBDL-03]

must_haves:
  truths:
    - "make purity-check fails the build if umya/quick-xml/calamine/swc_/pmcp-code-mode appears in the pmcp-workbook-runtime or pmcp-workbook-dialect dependency tree, per feature-combination"
    - "make purity-check FAILS CLOSED — any non-zero exit from cargo tree itself (broken invocation, bad -p package) aborts the gate as a FAILURE, never a silent pass (set -euo pipefail + explicit exit-status capture; no bare 2>/dev/null swallow)"
    - "make purity-check positively asserts rust_xlsxwriter IS present per-crate AND per-feature-combination scoped to pmcp-workbook-runtime (cargo tree -p), matching the negative matrix — a deleted renderer or a feature combo that drops it cannot make the gate vacuously pass, and another workspace member pulling rust_xlsxwriter cannot produce a false positive"
    - "make purity-check exits 0 against the current reader-free workspace; zip is permitted (enters via the writer)"
    - "Layer 2 cargo-deny is DELIVERED via crate-local deny.toml files invoked with cargo deny --manifest-path <crate>/Cargo.toml --config <crate>/deny.toml check bans — scoping the ban to that crate's tree as sole root, banning the reader/JS crates WITHOUT touching the infra-managed workspace-global deny.toml and WITHOUT breaking Phase 93's compiler"
    - "purity-check is part of the local make quality-gate flow (pre-commit single-source-of-truth) AND a merge-blocking CI job wired into the gate job's needs array"
    - "just purity-check exists as a recipe in the existing ./justfile and exits 0 against the reader-free workspace, delegating to the fail-closed make purity-check implementation — so BOTH entrypoints D-09 names (`just purity-check` AND CI/`make purity-check`) are literally satisfied, making ROADMAP Success-Criterion 3's `just purity-check` true without rewording D-09"
    - "WBDL-03 is re-mapped from Phase 91 to Phase 93 in both REQUIREMENTS.md and ROADMAP.md (not silently dropped — D-02)"
  artifacts:
    - path: "Makefile"
      provides: "purity-check target — fail-closed per-crate per-feature cargo-tree reader-absence + writer-presence + crate-local cargo-deny bans gate; wired into quality-gate"
      contains: "purity-check"
    - path: "crates/pmcp-workbook-runtime/deny.toml"
      provides: "Crate-scoped cargo-deny [bans] declaration (Layer 2) banning umya/quick-xml/calamine/swc_/pmcp-code-mode"
      contains: "[bans]"
    - path: "justfile"
      provides: "just purity-check recipe (D-09 + ROADMAP crit 3 `just` entrypoint) delegating to the fail-closed make purity-check"
      contains: "purity-check"
    - path: ".github/workflows/ci.yml"
      provides: "merge-blocking purity-check CI job wired into the gate job"
      contains: "purity-check"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WBDL-03 traceability re-mapped to Phase 93"
      contains: "WBDL-03 | Phase 93"
  key_links:
    - from: ".github/workflows/ci.yml gate job"
      to: "purity-check job"
      via: "needs array + result-evaluation script (makes it merge-blocking)"
      pattern: "needs:.*purity-check"
    - from: "Makefile purity-check"
      to: "pmcp-workbook-runtime / pmcp-workbook-dialect dependency trees"
      via: "cargo tree | grep -Ei reader/JS ban tokens (per feature-combo, fail-closed)"
      pattern: "umya\\|calamine\\|quick-xml\\|swc_\\|pmcp-code-mode"
    - from: "Makefile purity-check"
      to: "crates/pmcp-workbook-runtime/deny.toml (Layer 2)"
      via: "cargo deny --manifest-path <crate>/Cargo.toml --config <crate>/deny.toml check bans"
      pattern: "cargo deny.*--manifest-path.*--config.*check bans"
    - from: "Makefile quality-gate"
      to: "Makefile purity-check"
      via: "@$(MAKE) purity-check composition (local pre-commit single-source-of-truth)"
      pattern: "MAKE. purity-check"
    - from: "justfile purity-check recipe"
      to: "Makefile purity-check"
      via: "recipe body delegates to `make purity-check` (single fail-closed implementation; D-09 + ROADMAP crit 3 `just` entrypoint)"
      pattern: "purity-check:"
---

<objective>
Stand up the mechanically-provable purity gate (WBRT-04) BEFORE any `umya` code exists: a `make purity-check` target that FAILS CLOSED (cargo-tree per-crate, per-feature reader-absence + writer-presence assertions with explicit exit-status checks — a broken `cargo tree` invocation aborts the gate as a failure, never a silent pass) PLUS a crate-local `cargo-deny [bans]` Layer 2 declaration, and a merge-blocking CI `purity-check` job wired into the `gate` job. The three-layer gate is FULLY delivered: Layer 1 (cargo-tree, fail-closed) + Layer 2 (crate-local cargo-deny, scoped via `--manifest-path` so it never touches the infra-managed workspace `deny.toml` nor breaks Phase 93) + Layer 3 (crate split) all satisfy WBRT-04 and ROADMAP Success-Criterion 3. Also execute the blocking WBDL-03 → Phase 93 traceability re-map (D-02).

Purpose: Defend the reader/served-binary boundary from day one (Pitfall 1 — THE phase-91 risk), make the gate un-bypassable (fail-closed, positive writer assertion per-crate/per-feature), deliver the cargo-deny backstop the roadmap promises, and record the deliberate WBDL-03 re-map so it is not silently lost.

Output: A green, fail-closed, three-layer merge-blocking purity gate (wired into local `make quality-gate` + CI `gate`) + updated REQUIREMENTS/ROADMAP traceability; satisfies WBRT-04 and covers WBDL-03 (via re-map).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-CONTEXT.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-01-SUMMARY.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-02-SUMMARY.md

<interfaces>
<!-- This plan depends on 91-01 + 91-02 (both crates must exist for cargo tree -p / cargo deny --manifest-path to resolve). -->

The ban token set (D-09 — pmcp REMOVED; SDK runtime may depend on pmcp):
  umya|calamine|quick-xml|swc_|pmcp-code-mode
The positive assertion: rust_xlsxwriter MUST be present in the runtime tree, asserted per-crate (cargo tree -p pmcp-workbook-runtime) and per-feature-combination.
zip is PERMITTED — do NOT grep for it (enters legitimately via rust_xlsxwriter, zip 7.2.0).

cargo-deny scoping fact (verified — cargo-deny 0.18.3 `--manifest-path` docs):
  "if you specify a manifest path that is a member of a workspace, that crate will be
   the sole root of the crate graph, meaning only other workspace members that are
   dependencies of that workspace crate will be included in the graph."
  → `cargo deny --manifest-path crates/pmcp-workbook-runtime/Cargo.toml --config
     crates/pmcp-workbook-runtime/deny.toml check bans` scopes the ban to the runtime's
     tree ONLY. Phase 93's pmcp-workbook-compiler (a separate member, NOT a dep of the
     runtime) is unaffected — so a quick-xml/umya ban here does NOT break Phase 93.
     The infra-managed workspace-global deny.toml is NEVER touched.

From Makefile (in-repo convention analog):
  .PHONY: quality-gate  (line ~460) — the .PHONY + @$(MAKE) sub-target composition shape;
    its body is @$(MAKE) fmt-check / lint / build / test-all / audit / unused-deps /
    check-todos / check-unwraps / validate-always — APPEND @$(MAKE) purity-check here.
  .PHONY: lint (line ~143) — the clippy invocation shape.

From .github/workflows/ci.yml:
  quality-gate: job (line ~160) — checkout@v6 + dtolnay/rust-toolchain@stable + cargo cache scaffold to mirror
  gate: job (line ~281) — needs: [test, quality-gate]  ← APPEND purity-check + add PURITY_RESULT eval

From .planning/REQUIREMENTS.md (line ~103):
  | WBDL-03 | Phase 91 | Pending |   ← CHANGE Phase 91 → Phase 93

From ./justfile (repo root — ALREADY EXISTS, do NOT create it):
  Existing recipes use the simple shape `name:` header + indented command body
  (e.g. `quality-gate:` shells out to cargo, `clippy:` runs `cargo clippy -- -D warnings`).
  APPEND a `purity-check:` recipe of the same shape whose body is `make purity-check`
  (delegate — one fail-closed implementation in the Makefile; D-09 + ROADMAP crit 3 `just` entrypoint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WBDL-03 re-map — REQUIREMENTS.md + ROADMAP.md (BLOCKING doc edit, D-02)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the traceability table — the `| WBDL-03 | Phase 91 | Pending |` row at line ~103)
    - .planning/ROADMAP.md (Phase 91 section ~line 1734 `**Requirements**: ...WBDL-03`; Phase 93 section ~line 1770 `**Requirements**: WBCO-01...`)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-CONTEXT.md (D-02 — the re-map rationale: WorkbookMap + linter execution need umya, belong in Phase 93)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, change the traceability row `| WBDL-03 | Phase 91 | Pending |` to `| WBDL-03 | Phase 93 | Pending |`. In `.planning/ROADMAP.md`: (a) in the Phase 91 `**Requirements**:` line remove `WBDL-03` so it reads `WBRT-01, WBRT-02, WBRT-03, WBRT-04, WBDL-01`; (b) in the Phase 93 `**Requirements**:` line add `WBDL-03` to the list. This records the deliberate D-02 re-map (the `WorkbookMap` ingest model + linter execution need a real `.xlsx` via umya and belong where the reader lives — Phase 93). Phase 91 delivers only the dialect *contract* (the WHITELIST + spec doc + binding test from Plan 02), not a running linter.
  </action>
  <verify>
    <automated>grep -q 'WBDL-03 | Phase 93' .planning/REQUIREMENTS.md && ! grep -q 'WBDL-03 | Phase 91' .planning/REQUIREMENTS.md && awk '/### Phase 91:/{f=1} /### Phase 92:/{f=0} f && /\*\*Requirements\*\*:/{print}' .planning/ROADMAP.md | grep -vq 'WBDL-03' && awk '/### Phase 93:/{f=1} /### Phase 94:/{f=0} f && /\*\*Requirements\*\*:/{print}' .planning/ROADMAP.md | grep -q 'WBDL-03'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `WBDL-03 | Phase 93` and NO `WBDL-03 | Phase 91`.
    - The Phase 91 `**Requirements**:` line in ROADMAP.md (section-scoped via awk between `### Phase 91:` and `### Phase 92:`) no longer lists `WBDL-03`.
    - The Phase 93 `**Requirements**:` line in ROADMAP.md (section-scoped between `### Phase 93:` and `### Phase 94:`) now lists `WBDL-03`.
  </acceptance_criteria>
  <done>WBDL-03 is recorded as re-mapped to Phase 93 in both source artifacts — the requirement is not silently dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Add the fail-closed make purity-check target (Layer 1 cargo-tree + Layer 2 crate-local cargo-deny + Layer 3 crate split) and wire it into make quality-gate</name>
  <files>Makefile, justfile, crates/pmcp-workbook-runtime/deny.toml, crates/pmcp-workbook-dialect/deny.toml, docs/workbook-purity-gate.md, .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-VALIDATION.md</files>
  <read_first>
    - Makefile (the `.PHONY: quality-gate` target at ~lines 460-479 — the .PHONY + `@$(MAKE) sub-target` composition to mirror AND to extend by appending `@$(MAKE) purity-check`; note CLAUDE.md mandates `make quality-gate` so the SDK analogue is a Makefile target, NOT a justfile recipe — Pitfall 5)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/justfile (the `purity-check` recipe at ~lines 54-92 — the proven cargo-tree reader-vs-writer gate to lift + adapt; NOTE its `2>/dev/null` swallow is the false-pass bug the reviews flag — do NOT copy that swallow)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§Makefile purity-check target — the target body + the three deltas from the lighthouse recipe; HARDEN it per the review: add fail-closed exit-status handling and per-crate/per-feature positive assertion)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md (§Purity Gate Mechanics — the exact cargo tree invocations; ban set = umya|calamine|quick-xml|swc_|pmcp-code-mode with pmcp REMOVED; zip permitted; cargo-deny 0.18.3 available)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-REVIEWS.md (Codex HIGH false-pass finding + the consensus per-crate/per-feature positive-assertion fix + the Gemini crate-local deny.toml reconciliation)
    - ./justfile (the EXISTING repo-root justfile — confirm its recipe style before adding `purity-check`: simple recipes with a `recipe-name:` header and a TAB/4-space-indented command body, e.g. the existing `quality-gate:` / `clippy:` recipes that just shell out to cargo; the new `purity-check:` recipe follows the SAME shape and delegates to `make purity-check` so there is ONE fail-closed implementation. This adds the `just` entrypoint D-09 + ROADMAP Success-Criterion 3 require, WITHOUT rewording D-09)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-VALIDATION.md (the `## Manual-Only Verifications` table whose cargo-deny Layer-2 row is now STALE — this plan delivers Layer 2 automatically via crate-local `deny.toml`, so the row must move to the automated `## Per-Task Verification Map` under WBRT-04)
  </read_first>
  <action>
    Add a `.PHONY: purity-check` recipe to the Makefile, written FAIL-CLOSED. Make the recipe body a single shell invocation that begins `set -euo pipefail` (so any unhandled non-zero status aborts), and for EACH `cargo tree` invocation capture output+status explicitly rather than swallowing failure: run `tree=$$(cargo tree -p $$crate $$feat 2>&1)` then `status=$$?`; if `status -ne 0` echo "purity-check FAILED: cargo tree errored for $$crate ($$feat)" and `exit 1` (a broken/empty tree is a FAILURE, never a pass — this is the Codex-HIGH fix; do NOT use the lighthouse `cargo tree … | grep … 2>/dev/null` pipe that treats a failed tree as "no banned dep"). NEGATIVE arm — for each crate in `pmcp-workbook-runtime pmcp-workbook-dialect`, for each feature mode in `"" "--no-default-features" "--all-features"`: if the captured `$$tree` matches `grep -Ei 'umya|calamine|quick-xml|swc_|pmcp-code-mode'`, echo the crate+feature and `exit 1`. POSITIVE arm — run it PER-CRATE (scoped to `pmcp-workbook-runtime` via `cargo tree -p`, so another workspace member pulling rust_xlsxwriter cannot false-positive — Gemini) AND PER-FEATURE-COMBINATION (same `"" / --no-default-features / --all-features` matrix as the negative arm, so a feature combo that drops the renderer is caught — Codex): for each feature mode, `cargo tree -p pmcp-workbook-runtime $$feat` must contain `rust_xlsxwriter`; if absent for any mode, echo "purity-check FAILED: rust_xlsxwriter ABSENT from runtime tree ($$feat)" and `exit 1`. Three deltas from the lighthouse recipe still apply: (1) ban set is exactly `umya|calamine|quick-xml|swc_|pmcp-code-mode` — `pmcp` REMOVED (D-09); (2) do NOT grep `zip` (writer transitive — Anti-Pattern); (3) DROP the lighthouse engine-value-path grep arm (that is D-10's clippy job). LAYER 2 — after the cargo-tree arms, invoke the crate-local cargo-deny bans for each workbook crate: `cargo deny --manifest-path crates/pmcp-workbook-runtime/Cargo.toml --config crates/pmcp-workbook-runtime/deny.toml check bans` and the same for `pmcp-workbook-dialect` (under `set -e` a non-success cargo-deny aborts the recipe). Create `crates/pmcp-workbook-runtime/deny.toml` and `crates/pmcp-workbook-dialect/deny.toml`, each a minimal `[bans]`-only config: a `[bans]` section with a `deny = [...]` list naming `umya-spreadsheet`, `calamine`, `quick-xml`, `swc_*` crate names, and `pmcp-code-mode` (use the actual crate names; `umya` ships as `umya-spreadsheet`), plus the `[advisories]`/`[licenses]`/`[sources]` sections set to the most permissive form so the crate-local config checks ONLY bans (this file governs bans, the workspace `deny.toml` still governs advisories/licenses globally). Do NOT edit the workspace-global `deny.toml`. Finally, WIRE INTO LOCAL QUALITY-GATE: append `@$(MAKE) purity-check` to the `.PHONY: quality-gate` target's body (after `@$(MAKE) validate-always`) so the local pre-commit single-source-of-truth runs the purity gate too (Gemini LOW). Echo a clear pass message naming reader-free + writer-present (per-feature) + zip-permitted + cargo-deny-bans-clean. Create `docs/workbook-purity-gate.md` (NOT the dialect spec — keep the BA/auditor dialect spec clean per the Codex LOW note) documenting the three layers and that Layer 2 is delivered crate-locally via `--manifest-path` scoping (workspace deny.toml untouched). ADD THE `just` ENTRYPOINT (D-09 + ROADMAP Success-Criterion 3): a `justfile` ALREADY EXISTS at the repo root; add a new `purity-check` recipe to it (matching the existing simple-recipe style — a `purity-check:` header followed by an indented command body) whose body delegates to the make implementation by invoking `make purity-check` (single fail-closed source of truth — do NOT duplicate the cargo-tree/cargo-deny logic in the recipe; `make purity-check` stays the primary implementation per the in-repo convention + CI + `make quality-gate` wiring, Pitfall 5). This makes BOTH commands D-09 names exist (`just purity-check` AND `make purity-check`/CI), so ROADMAP Success-Criterion 3's `just purity-check` becomes literally true with NO rewording of the locked D-09 decision. Because both `just`/`make` entrypoints now exist, the ROADMAP wording at lines ~1739 (`just purity-check`) and ~1751 (`make purity-check`) is consistent — no ROADMAP edit to criterion 3 is required; do NOT remove the `just purity-check` reference. Finally, UPDATE `91-VALIDATION.md` (Issue-2 fix): in the `## Manual-Only Verifications` table, REMOVE the `cargo-deny [bans] backstop (Layer 2 of D-09)` row (it is no longer manual/deferred — this plan DELIVERS Layer 2 automatically via crate-local `deny.toml`); ADD a corresponding automated row to the `## Per-Task Verification Map` under WBRT-04 whose Automated Command is `cargo deny --manifest-path crates/pmcp-workbook-runtime/Cargo.toml --config crates/pmcp-workbook-runtime/deny.toml check bans` (and the dialect crate equivalent); and adjust the surrounding `*All other phase behaviors have automated verification.*` note / WBRT-04 row so the executor is NOT told to expect a manual-only Layer 2. Do NOT flip the `nyquist_compliant` / `wave_0_complete` frontmatter flags — those are post-execution status, not a planning edit.
  </action>
  <verify>
    <automated>make purity-check && grep -q '^purity-check:' justfile && just purity-check && grep -q 'set -euo pipefail' Makefile && grep -Eq 'cargo deny .*--manifest-path .*--config .*check bans' Makefile && grep -Eq 'umya\|calamine\|quick-xml\|swc_\|pmcp-code-mode' Makefile && ! grep -Eq 'cargo tree[^|]*\|[^|]*grep[^|]*2>/dev/null' Makefile && grep -q '\[bans\]' crates/pmcp-workbook-runtime/deny.toml && grep -q '\[bans\]' crates/pmcp-workbook-dialect/deny.toml && awk '/.PHONY: quality-gate$/{f=1} f && /MAKE. purity-check/{print; found=1} /quality-gate-strict/{f=0} END{exit !found}' Makefile && grep -q 'check bans' .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-VALIDATION.md && ! awk '/## Manual-Only Verifications/{m=1} /## Validation Sign-Off/{m=0} m && /cargo-deny .*Layer 2/{print; found=1} END{exit !found}' .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-VALIDATION.md && git diff --quiet deny.toml</automated>
  </verify>
  <acceptance_criteria>
    - `make purity-check` exits 0 against the current reader-free workspace.
    - The recipe begins `set -euo pipefail` and captures each `cargo tree` exit status explicitly; it does NOT use the `cargo tree … | grep … 2>/dev/null` swallow (fail-closed — Codex HIGH).
    - FAIL-CLOSED PROOF: substituting a nonexistent package (e.g. running the recipe's tree command with `-p pmcp-workbook-NONEXISTENT`) makes `cargo tree` exit non-zero and the recipe exits non-zero (a `cargo tree` failure is a gate FAILURE, never a pass). Document this proof in `docs/workbook-purity-gate.md` and confirm manually during execution.
    - The positive `rust_xlsxwriter`-present assertion runs scoped to `cargo tree -p pmcp-workbook-runtime` AND across the `"" / --no-default-features / --all-features` matrix (per-crate + per-feature — both reviewers).
    - The negative arm greps the ban set `umya|calamine|quick-xml|swc_|pmcp-code-mode` (and does NOT include `pmcp` alone or `zip` as ban tokens).
    - Layer 2: `crates/pmcp-workbook-runtime/deny.toml` and `crates/pmcp-workbook-dialect/deny.toml` each contain a `[bans]` deny list; the recipe invokes `cargo deny --manifest-path <crate>/Cargo.toml --config <crate>/deny.toml check bans` for each and it passes clean. The workspace-global `deny.toml` is unchanged (`git diff --quiet deny.toml`).
    - `make quality-gate`'s body contains `@$(MAKE) purity-check` (purity-check is part of the local pre-commit flow — Gemini LOW).
    - `just` ENTRYPOINT (D-09 + ROADMAP crit 3): `grep -q '^purity-check:' justfile` succeeds (the recipe exists in the existing justfile / `just --list` shows it) AND `just purity-check` exits 0 against the reader-free workspace AND the recipe delegates to the fail-closed `make purity-check` (so it fails closed by construction — no duplicated gate logic). BOTH `just purity-check` and `make purity-check` now exist, satisfying ROADMAP Success-Criterion 3's `just purity-check` literally, with NO rewording of D-09.
    - VALIDATION.md (Issue-2 fix): the `## Manual-Only Verifications` table NO LONGER contains the cargo-deny Layer-2 row (`awk`-scoped grep between `## Manual-Only Verifications` and `## Validation Sign-Off` returns no `cargo-deny … Layer 2` hit), and the `## Per-Task Verification Map` WBRT-04 row now references an automated `cargo deny --manifest-path … --config … check bans` command (`grep -q 'check bans' 91-VALIDATION.md`).
  </acceptance_criteria>
  <done>make purity-check is a green, fail-closed, non-vacuous per-crate/per-feature reader-absence + writer-presence gate backed by crate-local cargo-deny bans (Layer 2 delivered, workspace deny.toml untouched) and is part of `make quality-gate`; a `just purity-check` recipe exists in the repo justfile delegating to it (D-09 + ROADMAP crit 3 entrypoint), and 91-VALIDATION.md's stale Layer-2 Manual-Only row is moved to the automated WBRT-04 verification map.</done>
</task>

<task type="auto">
  <name>Task 3: Add the merge-blocking purity-check CI job + wire into the gate job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the `quality-gate` job at ~line 160 — checkout@v6 + dtolnay/rust-toolchain@stable + cargo cache scaffold to mirror; the `gate` job at ~line 281 — `needs: [test, quality-gate]` + its result-evaluation script/env)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§.github/workflows/ci.yml — the purity-check job shape + the three required gate-wiring edits; the strategy.matrix recommendation over [crate × feature])
    - CLAUDE.md (§CI Quality Gates — how a job becomes merge-blocking ONLY by being in the gate job's needs array AND its result-evaluation script; note `cargo-deny` must be installed in the CI job for Layer 2 to run)
  </read_first>
  <action>
    Add a new `purity-check` job to `.github/workflows/ci.yml` mirroring the `quality-gate` job scaffold (checkout@v6 → `dtolnay/rust-toolchain@stable` → cargo cache → install cargo-deny → run step). Install cargo-deny (e.g. via `taiki-e/install-action` with `tool: cargo-deny` or `cargo install cargo-deny --locked`) so the Layer-2 `cargo deny … check bans` calls in `make purity-check` resolve in CI. The run step executes `make purity-check` (which already loops crate×feature, runs fail-closed, and invokes the crate-local cargo-deny bans); optionally future-proof with a `strategy.matrix` over `crate: [pmcp-workbook-runtime, pmcp-workbook-dialect]` × `feature: ["", "--no-default-features", "--all-features"]`, though `make purity-check` already covers the matrix. Then make it MERGE-BLOCKING by editing the `gate` job: (a) append `purity-check` to its `needs:` array → `needs: [test, quality-gate, purity-check]`; (b) add `PURITY_RESULT: ${{ needs.purity-check.result }}` to the job's `env:`; (c) add `|| [[ "$PURITY_RESULT" != "success" ]]` to the result-evaluation `if` condition. Without these three edits the job is advisory, not blocking (CLAUDE.md / Pattern: CI gate-propagation).
  </action>
  <verify>
    <automated>grep -q 'purity-check:' .github/workflows/ci.yml && grep -Eq 'needs: \[test, quality-gate, purity-check\]|needs:.*purity-check' .github/workflows/ci.yml && grep -q 'PURITY_RESULT' .github/workflows/ci.yml && grep -Eq 'cargo-deny|cargo install cargo-deny' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` defines a `purity-check` job that installs cargo-deny and runs the per-crate per-feature fail-closed gate (`make purity-check`).
    - The `gate` job's `needs:` array contains `purity-check`.
    - The `gate` job's `env:` contains `PURITY_RESULT` and its `if` condition fails on a non-success purity result.
    - The YAML parses cleanly (`python3 yaml.safe_load`).
  </acceptance_criteria>
  <done>The fail-closed three-layer purity gate runs in CI per feature-combination and blocks merge via the org-required gate job — the reader can never silently reach the served tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any workspace crate → served-binary dependency cone | The purity gate is THE control proving the Excel reader (umya/quick-xml) never enters the runtime/dialect (served) trees |
| CI gate configuration → merge eligibility | A purity job that is not in the gate job's needs array is advisory only; the wiring is the actual enforcement |
| purity-check recipe internals → gate verdict | The recipe's own exit-status handling IS a trust boundary: a `cargo tree` invocation that fails for an unrelated reason must not be read as "no banned dependency" — the recipe must fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91P-01 | Elevation of Privilege / Tampering | Excel reader (umya/quick-xml) entering the served binary via feature unification | mitigate | Per-crate per-feature `cargo tree` ban (Layer 1) + crate-local `cargo-deny [bans]` (Layer 2, `--manifest-path`-scoped) + crate split (Layer 3); run per feature-combination, not just defaults (Pitfall 1) |
| T-91P-02 | Tampering | purity gate bypassable — only checks default features, or non-merge-blocking CI, or the positive writer assertion runs only on defaults | mitigate | Gate runs across `""`/`--no-default-features`/`--all-features` for BOTH negative and positive arms; positive `rust_xlsxwriter` assertion is per-crate (`-p`) + per-feature; wired into the merge-blocking `gate` job's needs + result-eval (Task 3) AND into local `make quality-gate` (Task 2) |
| T-91P-03 | Tampering / Spoofing | purity gate FALSE-PASSES because `cargo tree` itself fails (broken invocation, bad -p, transient error) and the recipe swallows the failure as "no banned dep" (Codex HIGH) | mitigate | Recipe uses `set -euo pipefail` + explicit per-invocation exit-status capture; any non-zero `cargo tree` exit aborts the recipe as a FAILURE; no `2>/dev/null` swallow; fail-closed proof documented (nonexistent `-p` → recipe exits non-zero) |
| T-91P-04 | Repudiation | WBDL-03 silently dropped during the Phase 91→93 re-map | mitigate | Task 1 records the re-map in both REQUIREMENTS.md and ROADMAP.md (D-02); tracked via this plan's `traceability_remap` field |
| T-91P-05 | Tampering | a crate-local cargo-deny ban accidentally scoped workspace-wide, breaking Phase 93's legitimate umya/quick-xml use | mitigate | `--manifest-path <crate>/Cargo.toml` makes the workbook crate the sole graph root (verified cargo-deny 0.18.3 behavior); Phase 93's compiler is a separate member, not a dep of the runtime/dialect, so its tree is never evaluated by these configs; the workspace-global infra-managed deny.toml is never edited |
</threat_model>

<verification>
- `make purity-check` exits 0; fails closed on any `cargo tree` error (set -euo pipefail + explicit status capture, no 2>/dev/null swallow); greps the correct ban set; positive rust_xlsxwriter assertion runs per-crate + per-feature; crate-local cargo-deny bans pass; `deny.toml` (workspace) unchanged.
- `make quality-gate` body includes `@$(MAKE) purity-check`.
- `just purity-check` recipe exists in the repo justfile and exits 0, delegating to `make purity-check` (D-09 + ROADMAP Success-Criterion 3 `just` entrypoint — both `just` and `make`/CI commands now exist).
- 91-VALIDATION.md: the cargo-deny Layer-2 row is moved out of `## Manual-Only Verifications` into the automated `## Per-Task Verification Map` under WBRT-04 (no longer deferred/manual).
- `.github/workflows/ci.yml` parses; `purity-check` job present (installs cargo-deny) and wired into `gate` (needs + PURITY_RESULT + if condition).
- `.planning/REQUIREMENTS.md` shows `WBDL-03 | Phase 93`; ROADMAP Phase 91/93 requirement lists updated (section-scoped).
- `make quality-gate` still passes (the new crates from 91-01/91-02 are workspace members and build clean).
</verification>

<success_criteria>
- WBRT-04: a fail-closed, merge-blocking, per-feature, per-crate cargo-tree purity gate (Layer 1) + crate-local cargo-deny bans (Layer 2, scoped via `--manifest-path`) + crate split (Layer 3) prove the reader can never enter the served tree — all three layers delivered.
- ROADMAP Success-Criterion 3 satisfied: the cargo-tree assertions ARE backed by a `cargo-deny [bans]` declaration (crate-local), without editing the infra-managed workspace deny.toml and without breaking Phase 93; AND both entrypoints D-09 names exist — `just purity-check` (new recipe delegating to make) and `make purity-check`/CI — so the criterion's literal `just purity-check` command is true with no D-09 rewording.
- WBDL-03: re-mapped to Phase 93 in REQUIREMENTS.md + ROADMAP.md (D-02) — recorded, not dropped.
- The gate is non-vacuous (per-crate/per-feature positive writer assertion), fail-closed (cargo tree errors abort), and the wiring is complete (local quality-gate + CI gate job needs + result eval).
</success_criteria>

<output>
Create `.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-03-SUMMARY.md` when done.
</output>
</content>
