# Phase 54.1: Protocol Type Construction DX - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-20
**Phase:** 54.1-protocol-type-construction-dx
**Areas discussed:** Default + constructor coverage, Content enum helpers, Non-exhaustive strategy, Fluent API depth

---

## Default + Constructor Coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Constructor + Default | ::new() for required fields, derive(Default) for ..Default::default() pattern | ✓ |
| Constructor only, no Default | Avoids semantically invalid empty instances | |
| Default for all, constructors as convenience | Every struct gets Default, constructors are sugar | |

**User's choice:** Constructor + Default
**Notes:** Matches how capabilities.rs already works

| Option | Description | Selected |
|--------|-------------|----------|
| impl Into<String> | No .to_string() everywhere, matches ToolInfo::new() | ✓ |
| Owned String | Explicit, no hidden allocations | |
| You decide | Claude picks based on existing patterns | |

**User's choice:** impl Into<String>

| Option | Description | Selected |
|--------|-------------|----------|
| All protocol structs | Every struct downstream users might construct gets ::new(), ~15-20 types | ✓ |
| Only reported painful types | Just ResourceInfo, PromptInfo, ResourceTemplate, ~5-8 types | |
| You decide | Claude audits and adds where needed | |

**User's choice:** All protocol structs — systematic coverage

---

## Content Enum Helpers

| Option | Description | Selected |
|--------|-------------|----------|
| Static methods | Content::text("hello"), Content::image(data, mime) — clean, discoverable | ✓ |
| From impls for simple cases | impl From<&str> for Content — terse but less discoverable | |
| Both static + From | Belt and suspenders | |
| You decide | Claude picks best match for SDK patterns | |

**User's choice:** Static methods

| Option | Description | Selected |
|--------|-------------|----------|
| All variants | text, image, resource, audio, resource_link — systematic | ✓ |
| Common three only | text, image, resource — niche ones use raw syntax | |

**User's choice:** All variants

---

## Non-Exhaustive Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, add non_exhaustive | Prevents struct literals, forces constructors, survives upgrades | ✓ |
| No, keep exhaustive + Default | Users can still use ..Default::default() pattern | |
| Only types with many Optional fields | Pragmatic middle ground for types with 3+ optionals | |

**User's choice:** Yes, add non_exhaustive

| Option | Description | Selected |
|--------|-------------|----------|
| All protocol structs | Uniform policy, no guessing which types will evolve | ✓ |
| Selective — only spec-facing types | Only MCP protocol message types | |
| You decide | Claude evaluates per type | |

**User's choice:** All protocol structs — uniform

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, update tests | SDK should dogfood constructors, tests become documentation | ✓ |
| Tests use #[cfg(test)] bypass | Less churn but tests don't validate public API | |

**User's choice:** Yes, update tests

---

## Fluent API Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Constructor + .with_*() for each Optional | One fluent method per optional field, ~3-8 methods per type | ✓ |
| Constructor + single .with_meta() only | Minimal API surface | |
| Full builder pattern | Separate builder struct, maximum flexibility | |
| You decide | Claude picks based on field count | |

**User's choice:** Constructor + .with_*() for each Optional field

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, impl Into<String> consistently | Matches constructor decision | ✓ |
| Take Option<impl Into<String>> | Explicit optionality but verbose | |
| You decide | Claude picks minimum boilerplate | |

**User's choice:** impl Into<String> consistently

| Option | Description | Selected |
|--------|-------------|----------|
| Normalize for consistency | Ensure all types follow same pattern in v2.0 window | ✓ |
| Leave good types alone | Only fix poor-DX types, less churn | |
| You decide | Claude adjusts only significant divergences | |

**User's choice:** Normalize for consistency

---

## Claude's Discretion

- Exact list of "protocol structs" vs internal types
- Method naming for ambiguous .with_*() cases
- Whether to keep ToolInfo convenience methods alongside normalized pattern
- Implementation order (which modules first)
- Default for types with empty required fields — documented as not standalone-valid

## Deferred Ideas

- derive_builder macro for complex types — future phase
- Spec-schema codegen for auto-generated constructors — deferred per Phase 54
- Content::from("hello") From impl — static methods are primary API
