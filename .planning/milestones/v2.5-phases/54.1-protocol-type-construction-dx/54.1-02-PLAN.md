---
phase: 54.1-protocol-type-construction-dx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/protocol/mod.rs
  - src/types/tasks.rs
  - src/types/sampling.rs
  - src/types/notifications.rs
  - src/types/capabilities.rs
  - src/types/tools.rs
autonomous: true
requirements: [PROTO-TYPE-DX]

must_haves:
  truths:
    - "Implementation can be constructed with ::new(name, version) and chained with .with_title(), .with_website_url(), .with_description(), .with_icons()"
    - "IconInfo can be constructed with IconInfo::new(src) without specifying optional fields"
    - "InitializeResult can be constructed with InitializeResult::new() without specifying all fields"
    - "Task can be constructed with Task::new(task_id, status) without specifying 5 optional fields"
    - "CreateMessageParams can be constructed with CreateMessageParams::new(messages) with all optional fields defaulting to None"
    - "ProgressNotification has #[non_exhaustive] and .with_total() fluent method"
    - "CallToolResult has #[non_exhaustive]"
    - "All struct literal tests in these modules are migrated to use constructors"
  artifacts:
    - path: "src/types/protocol/mod.rs"
      provides: "Implementation .with_*() fluent methods, IconInfo::new(), InitializeResult::new(), #[non_exhaustive] on Implementation/IconInfo/InitializeResult"
    - path: "src/types/tasks.rs"
      provides: "Task::new(), CreateTaskResult::new(), #[non_exhaustive] + Default on Task, ListTasksResult"
    - path: "src/types/sampling.rs"
      provides: "CreateMessageParams::new(), SamplingMessage::new(), #[non_exhaustive] + Default + constructors on sampling types"
    - path: "src/types/notifications.rs"
      provides: "#[non_exhaustive] on ProgressNotification, CancelledNotification, LogMessageParams, .with_*() methods"
    - path: "src/types/capabilities.rs"
      provides: "#[non_exhaustive] on ClientCapabilities, ServerCapabilities"
    - path: "src/types/tools.rs"
      provides: "#[non_exhaustive] on CallToolResult, ToolExecution .with_task_support() method"
  key_links:
    - from: "src/types/protocol/mod.rs"
      to: "downstream consumers"
      via: "#[non_exhaustive] on Implementation forces ::new() usage"
      pattern: "non_exhaustive.*struct Implementation"
    - from: "src/types/tasks.rs"
      to: "Phase 55 (Tasks with Polling)"
      via: "Task::new() constructor ready for Phase 55 consumption"
      pattern: "fn new\\(task_id.*status"
---

<objective>
Add constructors, Default impls, fluent builders, and #[non_exhaustive] to the remaining protocol type modules: protocol/mod.rs (Implementation, IconInfo, InitializeResult), tasks.rs, sampling.rs, notifications.rs, capabilities.rs, and tools.rs (normalization of CallToolResult and ToolExecution).

Purpose: Complete systematic DX coverage across ALL protocol types. The resource/prompt/content types are handled in Plan 01 (parallel). This plan covers the protocol core, task types (critical for Phase 55), sampling, notifications, and normalizes existing types that are missing #[non_exhaustive].

Output: Six updated type modules with the uniform pattern. All in-file tests migrated to dogfood constructors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-protocol-type-construction-dx/54.1-CONTEXT.md

<interfaces>
<!-- Current state of types being modified -->

From src/types/protocol/mod.rs (Implementation already has ::new()):
```rust
pub struct Implementation {
    pub name: String,
    pub title: Option<String>,
    pub version: String,
    pub website_url: Option<String>,
    pub description: Option<String>,
    pub icons: Option<Vec<IconInfo>>,
}

impl Implementation {
    pub fn new(name: impl Into<String>, version: impl Into<String>) -> Self { ... }
}
```

From src/types/protocol/mod.rs (IconInfo has no constructor):
```rust
pub struct IconInfo {
    pub src: String,
    pub mime_type: Option<String>,
    pub sizes: Option<Vec<String>>,
    pub theme: Option<IconTheme>,
}
```

From src/types/tasks.rs (Task has no constructor):
```rust
pub struct Task {
    pub task_id: String,
    pub status: TaskStatus,
    pub ttl: Option<u64>,
    pub created_at: String,
    pub last_updated_at: String,
    pub poll_interval: Option<u64>,
    pub status_message: Option<String>,
}
```

From src/types/tools.rs (CallToolResult missing #[non_exhaustive]):
```rust
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CallToolResult { ... }
// Already has ::new(), .with_*(), Default. Just needs #[non_exhaustive].
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protocol core types — Implementation, IconInfo, InitializeResult, RequestMeta</name>
  <files>src/types/protocol/mod.rs</files>
  <read_first>
    - src/types/protocol/mod.rs (current state — Implementation has ::new() but no .with_*(), IconInfo has no constructor, InitializeResult has no constructor)
    - src/types/tools.rs (reference pattern for .with_*() fluent methods)
  </read_first>
  <action>
**Implementation (already has ::new()):**
1. Add `Default` to derive list: `#[derive(Debug, Clone, Serialize, Deserialize, Default)]`
2. Add `#[non_exhaustive]` attribute
3. Add fluent methods to the existing impl block:
   - `pub fn with_title(mut self, title: impl Into<String>) -> Self` — sets title to Some(title.into())
   - `pub fn with_website_url(mut self, url: impl Into<String>) -> Self` — sets website_url to Some(url.into())
   - `pub fn with_description(mut self, description: impl Into<String>) -> Self` — sets description to Some(description.into())
   - `pub fn with_icons(mut self, icons: Vec<IconInfo>) -> Self` — sets icons to Some(icons)

**IconInfo:**
1. Add `Default` to derive list: `#[derive(Debug, Clone, Serialize, Deserialize, Default)]`
2. Add `#[non_exhaustive]` attribute
3. Add impl block with:
   - `pub fn new(src: impl Into<String>) -> Self` — sets src, all Optional fields to None
   - `pub fn with_mime_type(mut self, mime_type: impl Into<String>) -> Self`
   - `pub fn with_sizes(mut self, sizes: Vec<String>) -> Self`
   - `pub fn with_theme(mut self, theme: IconTheme) -> Self`

**InitializeResult:**
1. Add `Default` to derive list: `#[derive(Debug, Clone, Serialize, Deserialize, Default)]`
2. Add `#[non_exhaustive]` attribute
3. Add impl block with:
   - `pub fn new(server_info: Implementation, capabilities: ServerCapabilities) -> Self` — sets protocol_version to ProtocolVersion::default(), instructions to None
   - `pub fn with_instructions(mut self, instructions: impl Into<String>) -> Self`

**InitializeRequest:**
1. Add `#[non_exhaustive]` attribute (no Default — all fields required for protocol handshake)
2. Add impl block with:
   - `pub fn new(client_info: Implementation, capabilities: ClientCapabilities) -> Self` — sets protocol_version to crate::LATEST_PROTOCOL_VERSION.to_string()

**RequestMeta:**
Already has Default. Add `#[non_exhaustive]` attribute. Add:
   - `pub fn new() -> Self` — returns Self::default()
   - `pub fn with_progress_token(mut self, token: super::notifications::ProgressToken) -> Self`
   - `pub fn with_task_id(mut self, task_id: impl Into<String>) -> Self` — sets _task_id to Some(task_id.into())

**CompleteResult/CompletionResult:**
Add `Default` to CompletionResult derive, add `#[non_exhaustive]` to both. Add:
   - `CompletionResult::new(values: Vec<String>) -> Self` — has_more defaults to false, total to None
   - `CompletionResult::with_total(mut self, total: usize) -> Self`
   - `CompletionResult::with_has_more(mut self, has_more: bool) -> Self`

**Tests in protocol/mod.rs:**
Migrate struct literal tests to use constructors where applicable. For RequestMeta tests, use `RequestMeta::new().with_task_id("abc")` instead of struct literals.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo test --lib -- types::protocol::tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/protocol/mod.rs contains `#[non_exhaustive]` above `pub struct Implementation`
    - src/types/protocol/mod.rs contains `pub fn with_title(mut self, title: impl Into<String>) -> Self` in impl Implementation
    - src/types/protocol/mod.rs contains `pub fn with_website_url(mut self, url: impl Into<String>) -> Self` in impl Implementation
    - src/types/protocol/mod.rs contains `#[non_exhaustive]` above `pub struct IconInfo`
    - src/types/protocol/mod.rs contains `impl IconInfo` with `pub fn new(src: impl Into<String>) -> Self`
    - src/types/protocol/mod.rs contains `#[non_exhaustive]` above `pub struct InitializeResult`
    - src/types/protocol/mod.rs contains `impl InitializeResult` with `pub fn new(`
    - src/types/protocol/mod.rs contains `#[non_exhaustive]` above `pub struct RequestMeta`
    - protocol/mod.rs tests do NOT contain `RequestMeta {` struct literal syntax
    - All protocol module tests pass
  </acceptance_criteria>
  <done>Implementation has #[non_exhaustive] + Default + .with_*() fluent methods. IconInfo has #[non_exhaustive] + Default + ::new() + .with_*(). InitializeResult and InitializeRequest have constructors. RequestMeta has #[non_exhaustive] + .with_*() methods. In-file tests migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Tasks, Sampling, Notifications, Capabilities, and Tools normalization</name>
  <files>src/types/tasks.rs, src/types/sampling.rs, src/types/notifications.rs, src/types/capabilities.rs, src/types/tools.rs</files>
  <read_first>
    - src/types/tasks.rs (Task has no constructor, all tests use struct literals)
    - src/types/sampling.rs (CreateMessageParams has no Default or constructor, tests use struct literals)
    - src/types/notifications.rs (ProgressNotification has ::new() but no #[non_exhaustive], CancelledNotification/LogMessageParams have no constructors)
    - src/types/capabilities.rs (ClientCapabilities/ServerCapabilities missing #[non_exhaustive])
    - src/types/tools.rs (CallToolResult missing #[non_exhaustive], ToolExecution needs .with_task_support())
  </read_first>
  <action>
**tasks.rs — Task:**
1. Add `Default` to derive list, add `#[non_exhaustive]` attribute
2. Add impl block:
   - `pub fn new(task_id: impl Into<String>, status: TaskStatus) -> Self` — sets task_id, status, created_at and last_updated_at to empty string (caller must set timestamps), all Optional fields to None
   - `pub fn with_ttl(mut self, ttl: u64) -> Self`
   - `pub fn with_timestamps(mut self, created_at: impl Into<String>, last_updated_at: impl Into<String>) -> Self` — sets both timestamps
   - `pub fn with_poll_interval(mut self, interval: u64) -> Self`
   - `pub fn with_status_message(mut self, message: impl Into<String>) -> Self`

**tasks.rs — ListTasksResult:**
1. Add `Default` to derive list, add `#[non_exhaustive]` attribute
2. Add impl block:
   - `pub fn new(tasks: Vec<Task>) -> Self` — next_cursor defaults to None
   - `pub fn with_next_cursor(mut self, cursor: impl Into<String>) -> Self`

**tasks.rs — CreateTaskResult, GetTaskResult, CancelTaskResult:**
Add `#[non_exhaustive]` to each. Add `::new(task: Task) -> Self` constructor to each.

**tasks.rs — TaskCreationParams:**
Already has Default. Add `#[non_exhaustive]`. Add:
   - `pub fn new() -> Self` — returns Self::default()
   - `pub fn with_ttl(mut self, ttl: u64) -> Self`
   - `pub fn with_poll_interval(mut self, interval: u64) -> Self`

**tasks.rs — tests:**
Migrate all struct literal tests:
```rust
// FROM:
Task { task_id: "t-123".to_string(), status: TaskStatus::Working, ttl: Some(60000), created_at: "2025-11-25T00:00:00Z".to_string(), ... }
// TO:
Task::new("t-123", TaskStatus::Working)
    .with_timestamps("2025-11-25T00:00:00Z", "2025-11-25T00:01:00Z")
    .with_ttl(60000)
    .with_poll_interval(5000)
    .with_status_message("Processing...")
```

**sampling.rs — ModelPreferences:**
Already has Default. Add `#[non_exhaustive]`. Add:
   - `pub fn new() -> Self` — returns Self::default()
   - `pub fn with_cost_priority(mut self, priority: f64) -> Self`
   - `pub fn with_speed_priority(mut self, priority: f64) -> Self`
   - `pub fn with_intelligence_priority(mut self, priority: f64) -> Self`
   - `pub fn with_hints(mut self, hints: Vec<ModelHint>) -> Self`

**sampling.rs — ModelHint:**
Add `Default`, `#[non_exhaustive]`. Add:
   - `pub fn new(name: impl Into<String>) -> Self` — sets name to Some(name.into())

**sampling.rs — ToolChoice:**
Already has Default. Add `#[non_exhaustive]`. Add:
   - `pub fn auto() -> Self` — returns Self { mode: Some(ToolChoiceMode::Auto) }
   - `pub fn required() -> Self` — returns Self { mode: Some(ToolChoiceMode::Required) }
   - `pub fn none() -> Self` — returns Self { mode: Some(ToolChoiceMode::None) }

**sampling.rs — CreateMessageParams:**
Add `#[non_exhaustive]` (no Default — messages is required). Add:
   - `pub fn new(messages: Vec<SamplingMessage>) -> Self` — all Optional fields to None, include_context to IncludeContext::default()
   - `pub fn with_model_preferences(mut self, prefs: ModelPreferences) -> Self`
   - `pub fn with_system_prompt(mut self, prompt: impl Into<String>) -> Self`
   - `pub fn with_temperature(mut self, temp: f64) -> Self`
   - `pub fn with_max_tokens(mut self, tokens: u32) -> Self`
   - `pub fn with_tools(mut self, tools: Vec<super::tools::ToolInfo>) -> Self`
   - `pub fn with_tool_choice(mut self, choice: ToolChoice) -> Self`

**sampling.rs — CreateMessageResult:**
Add `#[non_exhaustive]` (no Default — content and model required). Add:
   - `pub fn new(content: super::content::Content, model: impl Into<String>) -> Self`
   - `pub fn with_usage(mut self, usage: TokenUsage) -> Self`
   - `pub fn with_stop_reason(mut self, reason: impl Into<String>) -> Self`

**sampling.rs — CreateMessageResultWithTools:**
Add `#[non_exhaustive]`. Add:
   - `pub fn new(model: impl Into<String>, role: Role, content: Vec<SamplingMessageContent>) -> Self`
   - `pub fn with_stop_reason(mut self, reason: impl Into<String>) -> Self`
   - `pub fn with_meta(mut self, meta: serde_json::Map<String, Value>) -> Self`

**sampling.rs — SamplingMessage:**
Add `#[non_exhaustive]`. Add:
   - `pub fn new(role: Role, content: SamplingMessageContent) -> Self`

**sampling.rs — TokenUsage:**
Add `Default`, `#[non_exhaustive]`. Add:
   - `pub fn new(input_tokens: u32, output_tokens: u32, total_tokens: u32) -> Self`

**sampling.rs — tests:**
Migrate struct literal tests to constructors:
```rust
// FROM:
ToolChoice { mode: Some(ToolChoiceMode::Auto) }
// TO:
ToolChoice::auto()
```

**notifications.rs — ProgressNotification:**
Already has ::new(). Add `#[non_exhaustive]`. Add:
   - `pub fn with_total(mut self, total: f64) -> Self`

**notifications.rs — CancelledNotification:**
Add `#[non_exhaustive]`. Add:
   - `pub fn new(request_id: super::RequestId) -> Self` — reason defaults to None
   - `pub fn with_reason(mut self, reason: impl Into<String>) -> Self`

**notifications.rs — LogMessageParams:**
Add `#[non_exhaustive]`. Add:
   - `pub fn new(level: LoggingLevel, message: impl Into<String>) -> Self` — logger and data default to None
   - `pub fn with_logger(mut self, logger: impl Into<String>) -> Self`
   - `pub fn with_data(mut self, data: Value) -> Self`

**notifications.rs — ResourceUpdatedParams:**
Add `#[non_exhaustive]`. Add:
   - `pub fn new(uri: impl Into<String>) -> Self`

**notifications.rs — tests:**
Migrate struct literal tests to constructors.

**capabilities.rs:**
Add `#[non_exhaustive]` to ClientCapabilities and ServerCapabilities. These already have Default + static factory methods (minimal(), full(), tools_only(), etc.), so they are well-covered. The `#[non_exhaustive]` addition prevents downstream struct literal construction. Note: The existing doctests that use `..Default::default()` will continue to work within the crate but need to use factory methods from outside.

**tools.rs — CallToolResult:**
Add `#[non_exhaustive]` attribute (already has Default + ::new() + .with_*() — just missing the attribute).

**tools.rs — ToolExecution:**
Already has Default. Add `#[non_exhaustive]`. Add:
   - `pub fn new() -> Self` — returns Self::default()
   - `pub fn with_task_support(mut self, support: TaskSupport) -> Self`

**tools.rs — CallToolRequest:**
Add `#[non_exhaustive]`. Add:
   - `pub fn new(name: impl Into<String>, arguments: Value) -> Self` — _meta and task default to None

**tools.rs — tests:**
Migrate ToolExecution struct literal in test_tool_info_with_execution:
```rust
// FROM:
tool.execution = Some(ToolExecution { task_support: Some(TaskSupport::Required) });
// TO:
tool.execution = Some(ToolExecution::new().with_task_support(TaskSupport::Required));
```
Migrate ToolExecution tests in test_tool_execution_serialization similarly.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo test --lib -- types::tasks::tests types::sampling::tests types::notifications::tests types::capabilities::tests types::tools::tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/tasks.rs contains `#[non_exhaustive]` above `pub struct Task`
    - src/types/tasks.rs contains `impl Task` with `pub fn new(task_id: impl Into<String>, status: TaskStatus)`
    - src/types/tasks.rs contains `pub fn with_timestamps(mut self`
    - src/types/tasks.rs tests do NOT contain `Task {` struct literal syntax (all migrated to ::new())
    - src/types/sampling.rs contains `#[non_exhaustive]` above `pub struct CreateMessageParams`
    - src/types/sampling.rs contains `impl CreateMessageParams` with `pub fn new(messages: Vec<SamplingMessage>)`
    - src/types/sampling.rs contains `pub fn auto() -> Self` in impl ToolChoice
    - src/types/sampling.rs tests do NOT contain `ToolChoice {` struct literal syntax
    - src/types/notifications.rs contains `#[non_exhaustive]` above `pub struct ProgressNotification`
    - src/types/notifications.rs contains `pub fn with_total(mut self, total: f64) -> Self`
    - src/types/notifications.rs contains `impl CancelledNotification` with `pub fn new(`
    - src/types/notifications.rs contains `impl LogMessageParams` with `pub fn new(`
    - src/types/notifications.rs tests do NOT contain `CancelledNotification {` struct literal syntax
    - src/types/capabilities.rs contains `#[non_exhaustive]` above `pub struct ClientCapabilities`
    - src/types/capabilities.rs contains `#[non_exhaustive]` above `pub struct ServerCapabilities`
    - src/types/tools.rs contains `#[non_exhaustive]` above `pub struct CallToolResult`
    - src/types/tools.rs contains `impl ToolExecution` with `pub fn with_task_support(`
    - src/types/tools.rs tests do NOT contain `ToolExecution {` struct literal syntax
    - All tests in tasks, sampling, notifications, capabilities, and tools modules pass
  </acceptance_criteria>
  <done>All remaining protocol type modules have #[non_exhaustive] + Default (where appropriate) + ::new() + .with_*() fluent methods. All in-file tests migrated to dogfood constructors. Task types are ready for Phase 55 consumption.</done>
</task>

</tasks>

<verification>
```bash
# All lib tests in the modified modules pass
cargo test --lib -- types::protocol::tests types::tasks::tests types::sampling::tests types::notifications::tests types::capabilities::tests types::tools::tests

# Full workspace build succeeds
cargo build --workspace 2>&1 | tail -5

# Clippy clean
cargo clippy --workspace -- -D warnings 2>&1 | tail -5
```
</verification>

<success_criteria>
- Implementation, IconInfo, InitializeResult, InitializeRequest, RequestMeta all have #[non_exhaustive] + constructors + .with_*()
- Task, ListTasksResult, CreateTaskResult, GetTaskResult, CancelTaskResult, TaskCreationParams all have #[non_exhaustive] + constructors
- CreateMessageParams, CreateMessageResult, CreateMessageResultWithTools, SamplingMessage, TokenUsage, ModelPreferences, ModelHint, ToolChoice all have #[non_exhaustive] + constructors
- ProgressNotification, CancelledNotification, LogMessageParams, ResourceUpdatedParams all have #[non_exhaustive] + constructors
- ClientCapabilities, ServerCapabilities have #[non_exhaustive]
- CallToolResult, ToolExecution, CallToolRequest have #[non_exhaustive] + any missing constructors
- ToolChoice has static factory methods: auto(), required(), none()
- All in-file tests migrated from struct literals to constructors
- cargo test --lib passes for all modified modules
</success_criteria>

<output>
After completion, create `.planning/phases/54.1-protocol-type-construction-dx/54.1-02-SUMMARY.md`
</output>
