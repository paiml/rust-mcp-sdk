---
phase: 92-bundlesource-served-tool-toolkit-module
plan: 03
type: execute
wave: 3
depends_on: ["92-01", "92-02"]
files_modified:
  - crates/pmcp-server-toolkit/src/workbook/mod.rs
  - crates/pmcp-server-toolkit/src/workbook/error.rs
  - crates/pmcp-server-toolkit/src/workbook/schema.rs
  - crates/pmcp-server-toolkit/src/workbook/input.rs
  - crates/pmcp-server-toolkit/src/workbook/handler.rs
autonomous: true
requirements: [WBSV-01, WBSV-02, WBSV-03, WBSV-04, WBSV-06, WBSV-07]
user_setup: []

must_haves:
  truths:
    - "calculate validates inputs (tier/dtype/enum) fail-closed and returns ALL named outputs ({value,unit} each) + a provenance stamp — no privileged headline"
    - "explain returns an ordered per-cell business-language derivation trace plus a generic manifest-declared annotations object (coil_band generalized away)"
    - "get_manifest returns the curated manifest projection; diff_version serves the hash-verified recorded changelog"
    - "Every domain failure returns isError:true in structuredContent (never a protocol Err) with code/reason/allowed/required/range + provenance stamp"
    - "Input and output schemas are projected entirely from the manifest (additionalProperties:false, per-column dtype/unit/meaning, mandatory non-empty outputSchema)"
    - "The provenance stamp surfaces bundle_id + version + a combined_hash field carrying the BUNDLE.lock combined hash (Codex HIGH #3 — the stamp field is named combined_hash, NOT workbook_hash, so it can never be confused with BundleLock.workbook_hash which is the source-workbook hash)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/workbook/error.rs"
      provides: "WorkbookToolError + to_iserror_result + ProvStamp (bundle_id + version + combined_hash)"
      contains: "to_iserror_result"
    - path: "crates/pmcp-server-toolkit/src/workbook/input.rs"
      provides: "validate_input with fail-closed WR-02/WR-05 arms"
      contains: "validate_input"
    - path: "crates/pmcp-server-toolkit/src/workbook/schema.rs"
      provides: "manifest→input/output schema projection (additionalProperties:false)"
      contains: "additionalProperties"
    - path: "crates/pmcp-server-toolkit/src/workbook/handler.rs"
      provides: "calculate/explain/get_manifest/diff_version ToolHandler impls"
      contains: "ToolHandler"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/workbook/handler.rs"
      to: "crates/pmcp-server-toolkit/src/workbook/input.rs"
      via: "validate_input before run"
      pattern: "validate_input"
    - from: "crates/pmcp-server-toolkit/src/workbook/handler.rs"
      to: "pmcp_workbook_runtime::run_executor"
      via: "executor over the loaded IR/DAG"
      pattern: "run_executor"
    - from: "crates/pmcp-server-toolkit/src/workbook/error.rs"
      to: "structuredContent"
      via: "to_iserror_result rides in structuredContent"
      pattern: "structuredContent|isError"
---

<objective>
Lift the lighthouse served-layer compute core into a feature-gated `workbook`
module in `pmcp-server-toolkit`: the isError envelope (error.rs), manifest→schema
projection (schema.rs), fail-closed input validation (input.rs), and four of the
five tool handlers (calculate / explain / get_manifest / diff_version) — with the
mandated scrub deltas (S-1 no headline, S-2 manifest-declared annotations,
S-3 bundle_id stamp, S-4 zero customer identifiers).

Purpose: Deliver the manifest-driven, fail-closed, TypedToolWithOutput-parity
served surface with zero per-workbook Rust. render_workbook + the resource land
in Plan 04; wiring + example + purity gate land in Plan 05.
Output: workbook/mod.rs (module skeleton) + error.rs + schema.rs + input.rs +
handler.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-RESEARCH.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-PATTERNS.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-01-SUMMARY.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-02-SUMMARY.md

<interfaces>
<!-- Runtime compute substrate the handlers reuse (do NOT re-implement). From
     pmcp-workbook-runtime lib.rs re-exports (read it). -->
WorkbookBundle { ir, dag, manifest, cell_map, layout, changelog, stamp }   // from BundleLoader; stamp.combined == BundleLock.combined
<!-- ProvStamp naming contract (Codex HIGH #3): the served ProvStamp carries
     { bundle_id, version, combined_hash } where combined_hash = BundleLock.combined.
     NEVER name this field workbook_hash — BundleLock.workbook_hash is the
     SOURCE-workbook hash and must not be conflated with the integrity stamp. -->
run_executor / build_dag                       // executor over IR/DAG
Manifest { ..., annotations: Vec<AnnotationDecl> }   // S-2 generic annotations
AnnotationDecl { name, target, meaning }
VersionChangelog                                // diff_version serves bundle.changelog
is_strict_constant(...)                         // V4 strict-constant override predicate

<!-- SDK idioms the lifted code must match (read these in-repo files): -->
crates/pmcp-server-toolkit/src/tools.rs        // ToolInfo::new/with_annotations constructors (~176-186), build_input_schema additionalProperties:false envelope (~193-208)
crates/pmcp-server-toolkit/src/sql/schema.rs   // manifest/columns → JSON-Schema projection analog
crates/pmcp-server-toolkit/src/error.rs        // ToolkitError + Result<T> alias
crates/pmcp-server-toolkit/src/sql/mod.rs      // #[non_exhaustive] thiserror ConnectorError shape (~218-272)
</interfaces>

<lift_source>
<!-- PRIVATE reference, scrub per D-13/S-1..S-4. Lift these lighthouse files:
  .../quote-pricing/crates/quote-pricing-server/src/workbook/error.rs   (to_iserror_result ~171-192 — lift verbatim, rename stamp workflow→bundle_id)
  .../workbook/schema.rs   (input_schema_for_manifest, *_output_schema, result_envelope_schema; DROP supply_total field ~96-98 S-1)
  .../workbook/input.rs    (validate_input; fail-closed arms ~114-123 WR-05, ~241-259 WR-02, ~133-138 strict-constant V4; negative tests ~456-477, ~592-611)
  .../workbook/handler.rs  (CalculateHandler/ExplainHandler/GetManifestHandler; project_outputs ~88-108 all-outputs; DROP read_supply_total ~58-77 S-1; replace coil_band keystone ~242-256 with manifest annotations S-2)
  .../workbook/diff_version.rs   (already generic — lift near-verbatim)
  NOT lifted (customer-specific): tools/quote.rs, rot.rs, quote_pricing_core, reload_catalog/get_catalog_info, value-schema:// resource.
-->
</lift_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend module skeleton + isError envelope (error.rs) + manifest schema projection (schema.rs) — WBSV-06/07, S-1/S-3</name>
  <files>crates/pmcp-server-toolkit/src/workbook/mod.rs, crates/pmcp-server-toolkit/src/workbook/error.rs, crates/pmcp-server-toolkit/src/workbook/schema.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/error.rs (ToolkitError + Result<T> alias the module errors integrate with)
    - crates/pmcp-server-toolkit/src/sql/mod.rs (#[non_exhaustive] + thiserror ConnectorError pattern ~218-272)
    - crates/pmcp-server-toolkit/src/tools.rs (build_input_schema additionalProperties:false envelope ~193-208; ToolInfo constructors ~176-186)
    - crates/pmcp-server-toolkit/src/sql/schema.rs (manifest→JSON-Schema projection analog)
    - crates/pmcp-server-toolkit/src/workbook/mod.rs (the MINIMAL skeleton created in 92-02 Task 0 — EXTEND it; do NOT recreate it. It already compiles under --features workbook with submodule decls kept commented)
    - lighthouse error.rs (to_iserror_result ~171-192; the WorkbookToolError code variants to document) + schema.rs (input/output projection; supply_total field ~96-98 to DROP) [private ref]
  </read_first>
  <behavior>
    - Test: to_iserror_result(err, stamp) yields an object with isError:true, code, reason, provenance (containing bundle_id+version+combined_hash), and only the present self-repair fields (field/allowed/range/required)
    - Test (Codex HIGH #3 contract): the provenance `combined_hash` emitted in the envelope equals `WorkbookBundle.stamp.combined` which equals `BundleLock.combined` for the tax-calc golden — asserted directly (the stamp can never carry the source-workbook hash)
    - Test: a manifest with 3 inputs projects an input schema with "additionalProperties": false, a "required" array, and one typed property per input (dtype/unit/meaning carried)
    - Test: the projected outputSchema is NON-EMPTY and contains every named output (no privileged supply_total field, S-1)
    - Test: result_envelope_schema accepts BOTH the success shape and the isError shape
    - Test (Gemini self-repair): every WorkbookToolError code variant is a documented, stable machine-readable string (the doc-comment table maps each code to its UI self-repair meaning)
  </behavior>
  <action>
    EXTEND the workbook/mod.rs skeleton created in 92-02 Task 0 (do NOT recreate it). Codex HIGH #2 — declare a submodule ONLY when its file exists in THIS task: this task creates error.rs and schema.rs, so add `pub mod error; pub mod schema;` now. Do NOT add `pub mod input;` or `pub mod handler;` yet — Task 2 adds `pub mod input;` when it creates input.rs, and Task 3a adds `pub mod handler;` when it creates handler.rs (declaring a module before its file exists does not compile). Re-export from mod.rs only the error/schema surface this task creates; later tasks extend the re-exports as their files land. Create workbook/error.rs by lifting the lighthouse `WorkbookToolError` enum (`#[non_exhaustive]`, thiserror, mirroring ConnectorError) and `to_iserror_result(err, stamp) -> serde_json::Value` VERBATIM, with S-3 + Codex HIGH #3: the `ProvStamp` struct fields are `bundle_id` + `version` + `combined_hash` (the BUNDLE.lock COMBINED hash) — the field is named `combined_hash`, NEVER `workbook_hash` (which is the runtime BundleLock SOURCE-workbook hash) and NEVER `workflow`; its `to_json()` emits `bundle_id`/`version`/`combined_hash`, and ProvStamp is constructed from `WorkbookBundle.stamp.combined`. Add a Gemini self-repair doc table to error.rs: a doc-comment enumerating every `WorkbookToolError` code as a stable machine-readable string with its UI self-repair meaning (these codes are the primary signal the MCP App widget reads). NOTE on error semantics (Codex LOW): `to_iserror_result` is for DOMAIN failures (invalid input, out-of-range) which return isError:true in structuredContent and never a protocol Err; INFRASTRUCTURE failures (a poisoned/malformed in-memory bundle state, a resource-handler internal fault, a genuine bug) MAY still surface as a protocol Err — document this distinction in the error.rs module doc so the lift does not blanket-swallow infrastructure faults as domain errors. Create workbook/schema.rs by lifting `input_schema_for_manifest`, the per-output `*_output_schema`, and `result_envelope_schema`; the input-schema envelope MUST be `{ "type":"object", "properties":..., "required":..., "additionalProperties": false }` (copy the exact tools.rs:202-207 shape). Apply S-1: project ALL named outputs uniformly from `manifest`/`cell_map.outputs` — DROP the top-level `supply_total` field entirely. The outputSchema MUST be non-empty (WBSV-07). Add the Codex HIGH #3 contract test asserting the envelope `combined_hash` == `WorkbookBundle.stamp.combined` == `BundleLock.combined` for the tax-calc golden. Honor `#![deny(unwrap_used, expect_used, panic)]` on value paths. Scrub S-4: zero `ufh`/`quote`/`coil`/`plot3`/`supply_total`/customer identifiers in code, comments, or tests.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook,http workbook::error workbook::schema 2>/dev/null || cargo test -p pmcp-server-toolkit --features workbook workbook::error workbook::schema</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isError" crates/pmcp-server-toolkit/src/workbook/error.rs` matches; to_iserror_result test asserts the full envelope shape incl. provenance with bundle_id + combined_hash
    - `grep -n "combined_hash" crates/pmcp-server-toolkit/src/workbook/error.rs` matches the ProvStamp field; `grep -rn "workbook_hash\|workflow" crates/pmcp-server-toolkit/src/workbook/error.rs` returns 0 hits (Codex HIGH #3 — stamp never uses workbook_hash/workflow)
    - The Codex HIGH #3 contract test passes: envelope `combined_hash` == `WorkbookBundle.stamp.combined` == `BundleLock.combined`
    - `grep -rn "supply_total" crates/pmcp-server-toolkit/src/workbook/error.rs crates/pmcp-server-toolkit/src/workbook/schema.rs` returns 0 hits (S-1)
    - mod.rs declares ONLY `pub mod error; pub mod schema;` after this task (NO `pub mod input;`/`pub mod handler;` until those files exist — Codex HIGH #2); `cargo build --features workbook` succeeds
    - error.rs carries a doc-comment table mapping every WorkbookToolError code to its self-repair meaning (Gemini); the module doc distinguishes domain failures (isError) from infrastructure failures (protocol Err) (Codex LOW)
    - `grep -n "additionalProperties" crates/pmcp-server-toolkit/src/workbook/schema.rs` matches a `false` value in the input-schema envelope
    - The outputSchema-non-empty test passes; result_envelope_schema-accepts-both test passes
    - `grep -rEi "ufh|quote|coil|towelrad|plot.?3" crates/pmcp-server-toolkit/src/workbook/error.rs crates/pmcp-server-toolkit/src/workbook/schema.rs crates/pmcp-server-toolkit/src/workbook/mod.rs` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>The extended module tree (error/schema declared, input/handler NOT yet), the isError envelope (bundle_id + combined_hash stamp, code-doc table, domain-vs-infra note), and the headline-free manifest→schema projection exist and are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed input validation (input.rs) — WBSV-06, WR-02/WR-05/V4</name>
  <files>crates/pmcp-server-toolkit/src/workbook/input.rs, crates/pmcp-server-toolkit/src/workbook/mod.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/error.rs (WorkbookToolError variants validate_input returns)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (the input schema validate_input enforces)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (CellRole, InputTier, Dtype, allowed_values: Option<Vec<String>> — the fields the gates read)
    - crates/pmcp-workbook-runtime/src/lib.rs (is_strict_constant predicate ~line 78)
    - lighthouse input.rs (validate_input; WR-05 ~114-123, WR-02 ~241-259, strict-constant ~133-138; negative tests ~456-477, ~592-611) [private ref]
  </read_first>
  <behavior>
    - Test: a supplied input whose cell_map entry has NO manifest role → WorkbookToolError invalid_input (WR-05 fail-closed; NOT an if-let-Some skip)
    - Test: a numeric value supplied for an enum (string allowed_values) input → rejected, string-only enum gate (WR-02)
    - Test: a per-call override of a strict-constant (BA-governed) cell → rejected (V4)
    - Test: a value outside a numeric range / wrong dtype → invalid_input with the self-repair field populated (allowed/range/required)
    - Test: a fully valid input set → Ok(validated env) ready for the executor
    - Test (Gemini Excel-edge seeds): an empty-string `""` supplied for an enum input is rejected (not silently coerced to a valid member); a JSON `null` supplied for a required input is rejected — both are seeded explicitly so the coercion boundary (empty string vs null) is covered, not left to random proptest draws
    - Property/fuzz test: validate_input over arbitrary/adversarial input maps (proptest-generated keys, types, and out-of-range/oversized values, with the empty-string and null edge cases included in the proptest seed corpus) NEVER panics and ALWAYS returns either Ok or a WorkbookToolError (total, fail-closed — CLAUDE.md ALWAYS-fuzz requirement, satisfied via proptest per CLAUDE.md "cargo fuzz ... OR using proptest")
  </behavior>
  <action>
    Add `pub mod input;` to workbook/mod.rs now that input.rs exists (Codex HIGH #2 — the declaration is added with the file, not before it). Create workbook/input.rs by lifting the lighthouse `validate_input` VERBATIM, preserving EVERY fail-closed arm — do NOT "simplify" any `?`-or-reject into an `if let Some(...)` (that fails open). Specifically: (WR-05) a `cell_map` entry with no manifest role rejects with `invalid_input`; (WR-02) the enum-membership test is STRING-ONLY and rejects a non-string value tested against a string `allowed_values` (read the runtime `allowed_values: Option<Vec<String>>`); (V4) reject any per-call override of a strict-constant cell using the runtime `is_strict_constant` predicate. Populate the self-repair fields (`allowed`/`range`/`required`) on every rejection so the isError envelope carries them. Use a `deny_unknown_fields` DTO for the input object. Lift the negative-path tests too. ADD a proptest-based fuzz test (`prop_validate_input_total`) that generates arbitrary/adversarial input maps (random keys, mixed JSON value types, out-of-range numbers, oversized strings) against the tax manifest and asserts validate_input is TOTAL — it never panics and always returns `Ok` or `Err(WorkbookToolError)` (satisfies the CLAUDE.md ALWAYS-fuzz requirement via proptest, avoiding cargo-fuzz nightly infra). Seed the proptest corpus with the Excel coercion edge cases Gemini flagged (empty string `""` vs JSON `null` for both enum and numeric inputs) so they are deterministically covered, not left to random draws. Ensure `proptest` is a `[dev-dependencies]` entry of the toolkit. Honor the deny lint. Scrub S-4 (tax-domain test inputs: gross_income, filing_status; zero customer identifiers).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook,http workbook::input 2>/dev/null || cargo test -p pmcp-server-toolkit --features workbook workbook::input</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "if let Some" crates/pmcp-server-toolkit/src/workbook/input.rs` does NOT appear on the manifest-role validation arm (WR-05 stays `?`-or-reject); the no-role-rejection test passes
    - The WR-02 string-only-enum test passes (a number against a string enum is rejected)
    - The V4 strict-constant-override test passes (rejected)
    - Every rejection populates at least one self-repair field; the valid-input test returns Ok
    - The proptest fuzz test `prop_validate_input_total` passes — validate_input is total over arbitrary/adversarial inputs (no panic; always Ok|Err): `cargo test -p pmcp-server-toolkit --features workbook prop_validate_input_total`
    - The empty-string-vs-null edge tests pass (`""` for an enum rejected; `null` for a required input rejected) and those cases are in the proptest seed corpus (Gemini)
    - `grep -n "pub mod input;" crates/pmcp-server-toolkit/src/workbook/mod.rs` matches (declared now that input.rs exists — Codex HIGH #2)
    - `grep -rEi "ufh|quote|coil|towelrad|first_fix|heat_source" crates/pmcp-server-toolkit/src/workbook/input.rs` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>validate_input enforces tier/dtype/enum/strict-constant fail-closed with all lighthouse negative-path arms preserved, self-repair fields populated, and a proptest fuzz test proving totality over adversarial inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3a: CalculateHandler + shared handler helpers (handler.rs) — WBSV-01, S-1, WR-06</name>
  <files>crates/pmcp-server-toolkit/src/workbook/handler.rs, crates/pmcp-server-toolkit/src/workbook/mod.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/input.rs (validate_input — called before run)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (the outputSchema calculate attaches)
    - crates/pmcp-server-toolkit/src/workbook/error.rs (to_iserror_result — the failure path)
    - crates/pmcp-server-toolkit/src/tools.rs (ToolInfo::new/with_annotations constructors ~176-186; the with_ui / structuredContent path)
    - crates/pmcp-workbook-runtime/src/lib.rs (run_executor, build_dag re-exports)
    - lighthouse handler.rs (CalculateHandler; project_outputs all-outputs ~88-108; read_supply_total ~58-77 to DROP S-1) [private ref]
  </read_first>
  <behavior>
    - Test: calculate with valid inputs returns a structuredContent object with EVERY named output as {value,unit} + a provenance stamp; NO supply_total/headline key
    - Test: calculate with an invalid input returns isError:true in structuredContent (never a protocol Err), carrying code/reason/self-repair + provenance
    - Test: every numeric output is finite — a NaN/Infinity output is surfaced as an error, not JSON null (WR-06)
  </behavior>
  <action>
    Add `pub mod handler;` to workbook/mod.rs now that handler.rs exists (Codex HIGH #2 — the last submodule declaration, added with its file). Create workbook/handler.rs and lift the shared handler scaffolding plus CalculateHandler (scrubbed). Establish the shared helpers the four handlers reuse so each handler fn stays decomposed and under cognitive complexity 25: a `project_outputs` helper returning ALL named outputs `{value,unit}` by iterating `cell_map.outputs` (S-1 — DROP `read_supply_total`/the headline), a finiteness-check helper (WR-06: every numeric output finite or surface an error, never JSON null), the ProvStamp-append helper, and the `to_iserror_result` failure routing into structuredContent. Implement CalculateHandler as a `ToolHandler`: construct `ToolInfo` via `ToolInfo::new`/`with_annotations` (NEVER a struct literal — `#[non_exhaustive]` discipline) with a non-empty `outputSchema`; call `validate_input`, then `run_executor` over the bundle IR/DAG, then `project_outputs`, then the finiteness check, then append the ProvStamp; return via the with_ui structuredContent path (WBSV-07). Every domain failure goes through `to_iserror_result` into structuredContent — NEVER `Err(pmcp::Error)`. Reuse runtime compute (run_executor/build_dag — never re-implement). Honor the deny lint. Scrub S-4. Keep every fn under cognitive complexity 25 (decompose via the shared helpers; do NOT annotate-allow).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook,http workbook::handler 2>/dev/null || cargo test -p pmcp-server-toolkit --features workbook workbook::handler</automated>
  </verify>
  <acceptance_criteria>
    - calculate test: structuredContent contains every named output as {value,unit} + provenance; `grep -n "supply_total" crates/pmcp-server-toolkit/src/workbook/handler.rs` returns 0 hits (S-1)
    - calculate-invalid test: returns isError:true in structuredContent (not a protocol Err)
    - The finiteness (WR-06) test passes (NaN/Infinity → error, not null)
    - The shared helpers (project_outputs, finiteness check, ProvStamp append) exist and CalculateHandler stays under cognitive complexity 25 (no `#[allow(clippy::cognitive_complexity)]`)
    - `grep -n "pub mod handler;" crates/pmcp-server-toolkit/src/workbook/mod.rs` matches (declared now that handler.rs exists — Codex HIGH #2)
    - `grep -rEi "ufh|quote|coil|towelrad|plot.?3|first_fix|heat_source|margin\b" crates/pmcp-server-toolkit/src/workbook/handler.rs` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>CalculateHandler (all-outputs, finite, bundle_id provenance, headline-free) plus the shared handler helpers exist, fail-closed into structuredContent, each fn under cog 25.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: ExplainHandler + GetManifestHandler + DiffVersionHandler (handler.rs) — WBSV-02/03/04, S-2</name>
  <files>crates/pmcp-server-toolkit/src/workbook/handler.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (the shared helpers + CalculateHandler from Task 3a — reuse validate_input wiring, ProvStamp/to_iserror_result helpers, ToolInfo discipline)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (the outputSchema each handler attaches)
    - crates/pmcp-workbook-runtime/src/lib.rs (run_executor, VersionChangelog re-exports)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (Manifest.annotations: Vec<AnnotationDecl> — S-2 generic annotations source)
    - lighthouse handler.rs (ExplainHandler; coil_band keystone ~242-256 to REPLACE S-2; GetManifestHandler) + diff_version.rs [private ref]
  </read_first>
  <behavior>
    - Test: explain returns an ordered per-cell trace (formula + operand values + manifest meaning) AND a generic "annotations" object keyed by the manifest-declared AnnotationDecl names (no coil_band)
    - Test: get_manifest returns the curated manifest projection (inputs/outputs/governed/versions/changelog) with no input
    - Test: diff_version returns the recorded VersionChangelog (per-output deltas, change class, severity, summary)
  </behavior>
  <action>
    Add the remaining three `ToolHandler` impls to workbook/handler.rs (lift, scrubbed), reusing the Task-3a shared helpers (validate_input, ProvStamp, to_iserror_result, ToolInfo constructors with non-empty outputSchema, with_ui structuredContent path). ExplainHandler: validate + run, emit the ordered per-cell derivation trace, and REPLACE the lighthouse `coil_band` keystone step (S-2) with a generic `annotations` object keyed by the manifest `annotations: Vec<AnnotationDecl>` — the engine reads only manifest-declared names, nothing domain-specific. GetManifestHandler: project the curated manifest (no input). DiffVersionHandler: serve `bundle.changelog` (the runtime `VersionChangelog`, already hash-verified at boot — do NOT recompute a diff). Every failure path goes through `to_iserror_result` into structuredContent — NEVER `Err(pmcp::Error)`. Honor the deny lint. Scrub S-4. Keep each handler fn under cognitive complexity 25 (lift the lighthouse helper decomposition; if a handler exceeds it, decompose, do not annotate-allow).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook,http workbook::handler 2>/dev/null || cargo test -p pmcp-server-toolkit --features workbook workbook::handler</automated>
  </verify>
  <acceptance_criteria>
    - explain test: emits a generic `annotations` object keyed by manifest names; `grep -n "coil_band" crates/pmcp-server-toolkit/src/workbook/handler.rs` returns 0 hits (S-2)
    - get_manifest + diff_version tests pass (curated projection; recorded changelog served, not recomputed)
    - Each of the three handler fns stays under cognitive complexity 25 (no `#[allow(clippy::cognitive_complexity)]`)
    - `grep -rEi "ufh|quote|coil|towelrad|plot.?3|first_fix|heat_source|margin\b" crates/pmcp-server-toolkit/src/workbook/handler.rs` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>ExplainHandler (generic manifest annotations), GetManifestHandler (curated projection), and DiffVersionHandler (recorded changelog) exist, fail-closed into structuredContent, each fn under cog 25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent tool input → calculate/explain | Untrusted JSON crosses into the executor; must be validated fail-closed |
| manifest skew → enum/role gates | A skewed manifest could fail-open a numeric-enum or role-less input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-09 | Tampering | malicious/adversarial tool input (out-of-tier/dtype/enum/oversized) | mitigate | validate_input fail-closed: WR-05 no-role rejection, WR-02 string-only enum gate, V4 strict-constant rejection; a proptest fuzz test proves validate_input is total (never panics) over arbitrary inputs (Task 2) |
| T-92-10 | Tampering | domain failure masquerading as success | mitigate | to_iserror_result returns isError:true in structuredContent; pmcp tool dispatch hardcodes is_error:false so structuredContent is the only machine-actionable path (Tasks 1,3) |
| T-92-11 | Tampering | NaN/Infinity output → JSON null masquerading as success | mitigate | Finiteness check (WR-06) on every numeric output; non-finite → error (Task 3) |
| T-92-12 | Spoofing | response not bound to a verified bundle | mitigate | Every response + error envelope carries the ProvStamp (bundle_id+version+combined_hash, the field unambiguously named so it can never be confused with the source-workbook hash — Codex HIGH #3) computed by the boot loader (Tasks 1,3) |
| T-92-13 | Information Disclosure | customer logic re-introduced during the lift | mitigate | S-1/S-2/S-4 scrub deltas with grep-gates on every workbook/*.rs file (all tasks) |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features workbook,http workbook::` green
- `grep -rEi "ufh|quote|coil|towelrad|plot.?3|supply_total" crates/pmcp-server-toolkit/src/workbook/` = 0 hits (S-1/S-4)
- `grep -rn "workbook_hash" crates/pmcp-server-toolkit/src/workbook/error.rs` = 0 hits (ProvStamp uses combined_hash — Codex HIGH #3)
- mod.rs declares submodules only after their files exist (no forward `pub mod` — Codex HIGH #2)
- `grep -rn "Err(pmcp::Error)\|Err(Error::" crates/pmcp-server-toolkit/src/workbook/handler.rs` — no domain-failure path returns a protocol Err (WBSV-06)
- The proptest fuzz test (`prop_validate_input_total`) passes — validate_input total over adversarial inputs (CLAUDE.md ALWAYS-fuzz)
- pmat complexity ≤25 on every handler fn (CI gate)
</verification>

<success_criteria>
- calculate returns ALL named outputs + provenance, no headline; invalid input → isError in structuredContent
- explain emits manifest-declared annotations (coil_band generalized); get_manifest + diff_version serve bundle-driven projections
- Schemas projected entirely from the manifest (additionalProperties:false, non-empty outputSchema)
- Zero customer identifiers in the lifted code
</success_criteria>

<output>
Create `.planning/phases/92-bundlesource-served-tool-toolkit-module/92-03-SUMMARY.md` when done
</output>
