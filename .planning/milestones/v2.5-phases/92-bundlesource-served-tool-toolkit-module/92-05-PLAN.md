---
phase: 92-bundlesource-served-tool-toolkit-module
plan: 05
type: execute
wave: 5
depends_on: ["92-02", "92-03", "92-04"]
files_modified:
  - crates/pmcp-server-toolkit/src/workbook/mod.rs
  - crates/pmcp-server-toolkit/src/lib.rs
  - crates/pmcp-server-toolkit/Cargo.toml
  - crates/pmcp-server-toolkit/examples/workbook_server_http.rs
  - crates/pmcp-server-toolkit/tests/workbook_integration.rs
  - Makefile
  - justfile
  - .github/workflows/ci.yml
autonomous: true
requirements: [WBSV-01, WBSV-05, WBSV-08, WBSV-09]
user_setup: []

must_haves:
  truths:
    - "A ServerBuilder gains .with_workbook_bundle(source) / .try_with_workbook_bundle(source) that loads + verifies a BundleSource at boot (fail-closed) and registers all five tools + the workbook:// resource"
    - "The toolkit re-exports the entire boot surface (BundleSource, both impls, loader/error types, builder-ext) so Shape A/B consumers never name pmcp-workbook-runtime"
    - "The workbook feature gates the module + the runtime dep (LocalDirSource only, no include_dir); workbook-embedded layers EmbeddedSource on top; neither is in default features (D-06)"
    - "A streamable-HTTP example (required-features workbook-embedded + http) boots a server from the embedded synthetic tax-calc@1.1.0 bundle and serves all five tools over HTTP, proven by an end-to-end boot smoke-run test (Codex MEDIUM #11), not just a cargo build"
    - "The purity gate asserts BOTH cargo tree -p pmcp-server-toolkit --features workbook AND --features workbook-embedded are reader-free (umya/quick-xml/calamine/swc_/pmcp-code-mode absent) as distinct per-feature checks — the embedded combo carries include_dir and must still be reader-free"
    - "make quality-gate (fmt/clippy pedantic+nursery/build/test/audit/purity) passes with the full workbook module and the extended purity gate active"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/workbook/mod.rs"
      provides: "WorkbookBuilderExt::with_workbook_bundle / try_ companion registering 5 tools + resource"
      contains: "with_workbook_bundle"
    - path: "crates/pmcp-server-toolkit/examples/workbook_server_http.rs"
      provides: "streamable-HTTP example, all 5 tools, embedded bundle"
      contains: "with_workbook_bundle"
    - path: "crates/pmcp-server-toolkit/tests/workbook_integration.rs"
      provides: "build-and-assert-registered + tamper-fails-boot integration tests"
      contains: "get_tool"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/workbook/mod.rs"
      to: "pmcp_workbook_runtime::BundleLoader"
      via: "BundleLoader::load at boot (fail-closed)"
      pattern: "BundleLoader::load|::load\\("
    - from: "Makefile"
      to: "cargo tree -p pmcp-server-toolkit --features workbook[-embedded]"
      via: "reader-absence assertion for BOTH combos in the Makefile (just delegates); distinct from PURITY_CRATES"
      pattern: "features workbook"
---

<objective>
Wire the workbook module into the toolkit and prove it end-to-end: the
`WorkbookBuilderExt::with_workbook_bundle` registration (D-09), the lib.rs
feature-gated module decl + boot-surface re-exports (D-10/D-11), the Cargo.toml
`workbook` feature + `[[example]]`, the streamable-HTTP example (D-12), the
integration tests (build-and-assert + tamper-fails-boot), and — the phase's top
risk — the purity-gate extension asserting the toolkit `workbook` feature is
reader-free.

Purpose: Freeze the consumer-side contract behind a single registration call,
make Shape A/B consumers depend only on `pmcp-server-toolkit`, and defend the
reader-free boundary the entire milestone rests on.
Output: builder-ext in mod.rs, lib.rs + Cargo.toml wiring, the HTTP example,
integration tests, and the extended purity gate (Makefile/justfile/CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-RESEARCH.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-PATTERNS.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-03-SUMMARY.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-04-SUMMARY.md

<interfaces>
<!-- The five handlers + resource from 92-03/92-04 (register all of them): -->
CalculateHandler, ExplainHandler, GetManifestHandler, DiffVersionHandler, RenderWorkbookHandler
RenderWorkbookResource   // workbook/render_resource.rs
BundleLoader::load(&dyn BundleSource) -> Result<WorkbookBundle, BundleLoadError>   // from runtime (92-01)
BundleSource / LocalDirSource / EmbeddedSource / BundleSourceError                  // from runtime (92-01)

<!-- Tamper helpers from 92-02 the integration test reuses (Task 2): -->
copy_golden_to_temp() -> tempfile::TempDir   // tests/support/tamper.rs (92-02 Task 3)
flip_byte(dir: &Path, member: &str)          // tests/support/tamper.rs (92-02 Task 3)

<!-- SDK idioms to mirror (read these in-repo files): -->
crates/pmcp-server-toolkit/src/builder_ext.rs   // ServerBuilderExt trait + panicking/try_ pair (~34, 260-318); tool_arc registration loop (~280-282); resources_arc; tracing::warn empty-state (~273-279)
crates/pmcp-server-toolkit/src/lib.rs           // #[cfg(feature="http")] pub mod http (~43-44); crate-root re-export block (~137-138); _ROOT_REEXPORT_SMOKE compile-const (~206-230)
crates/pmcp-server-toolkit/Cargo.toml           // http feature block (~98-107); [[example]] sql_server_http (~128-130); default=["code-mode"] (~74)
crates/pmcp-server-toolkit/examples/sql_server_http.rs   // streamable-HTTP serve helper (~42-50) + main (~52-72), machine-readable ADDR println
Makefile                                        // purity-check recipe (~496-545); BAN regex (~503); fail-closed capture (~506); extend-by-design comment (~492-494)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkbookBuilderExt registration + lib.rs module decl + boot-surface re-exports — WBSV-01/05/08/09, D-09/10/11</name>
  <files>crates/pmcp-server-toolkit/src/workbook/mod.rs, crates/pmcp-server-toolkit/src/lib.rs, crates/pmcp-server-toolkit/Cargo.toml</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/builder_ext.rs (ServerBuilderExt trait + panicking/try_ pair ~34,260-318; tool_arc loop ~280-282; resources_arc; tracing::warn empty-state ~273-279)
    - crates/pmcp-server-toolkit/src/lib.rs (http module-decl ~43-44; re-export block ~137-138; _ROOT_REEXPORT_SMOKE ~206-230)
    - crates/pmcp-server-toolkit/Cargo.toml (http feature block ~98-107; default ~74)
    - crates/pmcp-server-toolkit/src/workbook/mod.rs (the module skeleton from 92-03 — add the builder-ext here)
    - crates/pmcp-server-toolkit/src/workbook/handler.rs + render_resource.rs (the 5 handlers + resource to register)
  </read_first>
  <action>
    The Cargo feature pair (`workbook`/`workbook-embedded`), the optional `pmcp-workbook-runtime` + `base64` deps, and the `#[cfg(feature = "workbook")] pub mod workbook;` decl in lib.rs ALREADY EXIST from 92-02 Task 0 (the early wiring skeleton — Codex HIGH #1). This task does NOT recreate them; it EXTENDS the existing module + lib.rs. In workbook/mod.rs add `pub trait WorkbookBuilderExt: Sized` with `fn with_workbook_bundle(self, source: &dyn BundleSource) -> Self` (panicking; doc # Panics; prefer try_) and `fn try_with_workbook_bundle(self, source: &dyn BundleSource) -> Result<Self>` (fallible companion; doc # Errors), implemented for `ServerBuilder` exactly mirroring builder_ext.rs:260-318: `try_with_workbook_bundle` calls `Arc::new(BundleLoader::load(source)?)` (WBSV-08 fail-closed at boot), then registers all FIVE tools via the `self.tool_arc(name, handler)` loop idiom (builder_ext.rs:280-282) and the `workbook://` resource via `.resources_arc(Arc::new(RenderWorkbookResource::new(bundle)))` — DROP the DispatchingResource wrapper (single resource, A3). Emit a `tracing::warn!` if the bundle declares zero outputs (operator visibility, builder_ext.rs:273-279). In lib.rs add a crate-root re-export block exposing the FULL boot surface (D-11): `pub use crate::workbook::WorkbookBuilderExt;` plus re-exports of `BundleSource`, `LocalDirSource`, `EmbeddedSource` (cfg `workbook-embedded`), `BundleSourceError`, `BundleLoader`, `BundleLoadError`, `WorkbookBundle` (re-exported from the runtime through the toolkit so consumers never name pmcp-workbook-runtime). Extend the `_ROOT_REEXPORT_SMOKE` compile-const to reference the workbook surface. VERIFY (do NOT re-add) that Cargo.toml already carries the optional `pmcp-workbook-runtime` + `base64` deps and the `workbook`/`workbook-embedded` features from 92-02 Task 0; if any is missing, add it, but the `EmbeddedSource` re-export is `#[cfg(feature = "workbook-embedded")]` (NOT `embedded`) and `default = ["code-mode"]` stays unchanged (D-06/D-10).
  </action>
  <verify>
    <automated>cargo build -p pmcp-server-toolkit --features workbook && cargo build -p pmcp-server-toolkit --features workbook-embedded && cargo test -p pmcp-server-toolkit --features workbook workbook::mod 2>/dev/null; cargo build -p pmcp-server-toolkit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "with_workbook_bundle" crates/pmcp-server-toolkit/src/workbook/mod.rs` matches both the panicking and try_ methods
    - `grep -n "BundleLoader::load\|::load(source)" crates/pmcp-server-toolkit/src/workbook/mod.rs` shows the boot load inside try_with_workbook_bundle (fail-closed)
    - `grep -n "workbook = \[" crates/pmcp-server-toolkit/Cargo.toml` matches a value WITHOUT `pmcp-workbook-runtime/embedded` (LocalDirSource only); `grep -n "workbook-embedded = \[" crates/pmcp-server-toolkit/Cargo.toml` matches and includes `"workbook"` + `"pmcp-workbook-runtime/embedded"` (D-06 — created in 92-02 Task 0, verified here)
    - `grep -n "default = " crates/pmcp-server-toolkit/Cargo.toml` still shows `["code-mode"]` (neither workbook nor workbook-embedded in default)
    - `cargo tree -p pmcp-server-toolkit --features workbook 2>&1 | grep -c include_dir` returns 0 (plain workbook does NOT pull include_dir); `cargo tree -p pmcp-server-toolkit --features workbook-embedded 2>&1 | grep -q include_dir` (embedded combo does)
    - `cargo build -p pmcp-server-toolkit` (no features) AND `cargo build -p pmcp-server-toolkit --features workbook` both succeed
    - The re-export block exposes BundleSource + both impls + loader/error types from the crate root (the _ROOT_REEXPORT_SMOKE const compiles referencing them)
  </acceptance_criteria>
  <done>.with_workbook_bundle registers 5 tools + the resource after a fail-closed boot load; the toolkit re-exports the full boot surface; the workbook feature (from 92-02 Task 0) stays out of default.</done>
</task>

<task type="auto">
  <name>Task 2: Streamable-HTTP example + integration tests — WBSV-01/08/09, D-12 (as refined)</name>
  <files>crates/pmcp-server-toolkit/examples/workbook_server_http.rs, crates/pmcp-server-toolkit/tests/workbook_integration.rs, crates/pmcp-server-toolkit/Cargo.toml</files>
  <read_first>
    - crates/pmcp-server-toolkit/examples/sql_server_http.rs (serve helper ~42-50; main ~52-72; PMCP_ASSETS_DIR / --bundle-dir seam ~36,54; machine-readable ADDR println)
    - crates/pmcp-server-toolkit/src/http/ (StreamableHttpServer bind-to-ephemeral-port pattern — the smoke test boots on 127.0.0.1:0 and reads the bound addr)
    - crates/pmcp-server-toolkit/Cargo.toml ([[example]] sql_server_http ~128-130 — copy, swap features; dev-deps tokio rt-multi-thread)
    - crates/pmcp-server-toolkit/src/builder_ext.rs (#[cfg(test)] build-and-assert idiom ~381-448, get_tool assertion ~404-416)
    - crates/pmcp-server-toolkit/tests/support/tamper.rs (copy_golden_to_temp + flip_byte helpers from 92-02 Task 3 — the boot-fails test uses them)
    - crates/pmcp-server-toolkit/tests/support/fixture_gen.rs / fixtures dir (the embedded + local golden the example/test load)
  </read_first>
  <action>
    Create examples/workbook_server_http.rs mirroring sql_server_http.rs: copy the inline StreamableHttpServer serve helper (~42-50) and the main skeleton (~52-72), build via `Server::builder().name("workbook-tax-calc").version(...)`, chain `.try_with_workbook_bundle(&source)?` (in place of the SQL connector call), `.build()?`, then `serve(...)`, then `println!("..._ADDR=http://{addr}")`. Source selection (D-12 as refined — required-features = ["workbook-embedded", "http"] is the approved orchestrator refinement, remote-deploy story): default to the `EmbeddedSource` over the committed tests golden via an EXACT `include_dir!` invocation — `include_dir!("$CARGO_MANIFEST_DIR/tests/fixtures/tax-calc@1.1.0")` (Codex HIGH #5 — the committed golden is @1.1.0; NO examples/fixtures duplication — point the macro straight at the 92-02 committed golden) — with an optional `--bundle-dir` LocalDirSource override (mirror the PMCP_ASSETS_DIR seam). Add a doc-comment in the example (Gemini suggestion) explaining the EmbeddedSource→LocalDirSource transition: the embedded default bakes the bundle into the binary for a self-contained deploy, while `--bundle-dir` (LocalDirSource) is how a production operator points at a workbook updated out-of-band without rebuilding. Add the Cargo `[[example]]` block: `name = "workbook_server_http"`, `required-features = ["workbook-embedded", "http"]` — the example defaults to `EmbeddedSource` so it needs the embedded feature, not plain `workbook` (D-12 as refined + D-06; http already forwards pmcp/streamable-http). Create tests/workbook_integration.rs with: (1) a build-and-assert test — `Server::builder()...with_workbook_bundle(&LocalDirSource::new("tests/fixtures/tax-calc@1.1.0")).build()` then `assert!(server.get_tool("calculate").is_some())` for all five tool names (builder_ext.rs:404-416 idiom); (2) a tamper-fails-boot test — `copy_golden_to_temp()`, `flip_byte(dir.path(), "manifest.json")`, then `try_with_workbook_bundle(&LocalDirSource::new(dir.path()))` returns Err (WBSV-08 end-to-end through the builder); (3) an EXAMPLE SMOKE-RUN test (Codex MEDIUM #11 — `cfg(feature="http")`, gated `--features workbook-embedded,http`) that BOOTS the server end-to-end: build the same server the example builds (embedded bundle), serve it via StreamableHttpServer bound to an ephemeral port (127.0.0.1:0), then within a BOUNDED tokio timeout (e.g. 5s) either (a) initialize an in-process client and assert `tools/list` advertises all five tool names, or (b) at minimum assert the server boots and binds cleanly and the build exposes all five tools via `get_tool`, then SHUT DOWN the server (drop the serve task / abort the handle) — the test must not hang and must not leave a listening socket. This proves the `cargo run --example` path actually serves (not just compiles), satisfying the CLAUDE.md "cargo run --example" ALWAYS requirement. Reuse the tests/support module from 92-02 (declare `mod support;` and use `support::tamper::{copy_golden_to_temp, flip_byte}`).
  </action>
  <verify>
    <automated>cargo build --example workbook_server_http -p pmcp-server-toolkit --features workbook-embedded,http && cargo test -p pmcp-server-toolkit --features workbook-embedded,http --test workbook_integration</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --example workbook_server_http -p pmcp-server-toolkit --features workbook-embedded,http` succeeds
    - `grep -n "required-features = \[\"workbook-embedded\", \"http\"\]" crates/pmcp-server-toolkit/Cargo.toml` matches (D-12 as refined + D-06)
    - `grep -n 'include_dir!("\$CARGO_MANIFEST_DIR/tests/fixtures/tax-calc@1.1.0")' crates/pmcp-server-toolkit/examples/workbook_server_http.rs` matches (the EXACT @1.1.0 golden path; no examples/fixtures copy — Codex HIGH #5)
    - The build-and-assert test asserts all five tools (`calculate`/`explain`/`get_manifest`/`diff_version`/`render_workbook`) `.is_some()`
    - The tamper-fails-boot test: `try_with_workbook_bundle` over a byte-flipped golden (via `copy_golden_to_temp` + `flip_byte`) returns Err (WBSV-08 through the builder)
    - The example SMOKE-RUN test (Codex MEDIUM #11) boots the server on an ephemeral port within a bounded timeout, asserts all five tools are advertised/registered, and shuts down cleanly (no hang, no leaked socket): `cargo test -p pmcp-server-toolkit --features workbook-embedded,http --test workbook_integration`
    - The example carries a doc-comment on the EmbeddedSource→LocalDirSource (`--bundle-dir`) production transition (Gemini)
    - `grep -rEi "ufh|quote|coil|towelrad" crates/pmcp-server-toolkit/examples/workbook_server_http.rs` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>A streamable-HTTP example boots from the embedded synthetic @1.1.0 bundle (include_dir! over the committed golden) serving all five tools; integration tests prove registration + fail-closed boot through the builder + an end-to-end boot smoke run (Codex MEDIUM #11).</done>
</task>

<task type="auto">
  <name>Task 3: Extend the purity gate (Makefile implementation; just delegates) with a toolkit workbook reader-absence assertion — THE phase risk (carries WBRT-04 forward)</name>
  <files>justfile, Makefile, .github/workflows/ci.yml</files>
  <read_first>
    - justfile (the `purity-check` recipe at ~64-67 — it DELEGATES to `make purity-check`; the real implementation lives in the Makefile, so the substantive change is a Makefile edit and just needs no change beyond confirming the delegation still holds — Codex LOW)
    - Makefile (purity-check recipe ~496-545; BAN regex ~503; fail-closed status capture ~506; PURITY_CRATES loop; extend-by-design comment ~492-494)
    - .github/workflows/ci.yml (the purity job that runs the gate — find the purity-check step)
    - .planning/phases/92-bundlesource-served-tool-toolkit-module/92-RESEARCH.md (Pitfall 1: the toolkit is NOT unconditionally reader-free — add a DISTINCT per-feature assertion, do NOT append pmcp-server-toolkit to PURITY_CRATES)
  </read_first>
  <action>
Extend the purity gate in the Makefile — `just purity-check` already DELEGATES to `make purity-check` (justfile ~64-67), so the Makefile recipe is the single fail-closed implementation and just needs no substantive edit (Codex LOW — the prior "justfile-first" framing did not match this repo's delegation model). Add to the Makefile `purity-check` recipe a NEW per-feature-combination assertion that is DISTINCT from the unconditional `PURITY_CRATES` loop (the toolkit has code-mode/sql/http so it is NOT unconditionally reader-free, Pitfall 1/A5): run the reader-absence assertion against BOTH feature combinations (D-06 split): `cargo tree -p pmcp-server-toolkit --features workbook` (LocalDirSource only) AND `cargo tree -p pmcp-server-toolkit --features workbook-embedded` (the include_dir-bearing tree), each with the fail-closed status-capture idiom (Makefile:506) and each grepped against the reused `BAN='umya|calamine|quick-xml|swc_|pmcp-code-mode'` regex (Makefile:503) — FAIL the gate if any reader/JS crate is present in EITHER tree, and FAIL closed on a non-zero cargo status from either invocation. The `workbook-embedded` combo is the critical one: it pulls include_dir and must still be reader-free. Confirm the `just purity-check` recipe still delegates to `make purity-check` (no logic duplicated in the justfile; if the delegation comment references Phase 91, leave it). Confirm the CI purity job (.github/workflows/ci.yml) runs `make purity-check` (it invokes the make recipe, so no CI edit is needed beyond confirming the step exists; add the toolkit-workbook step to the job only if the gate is invoked via explicit per-crate steps rather than the recipe).
  </action>
  <verify>
    <automated>make purity-check && grep -E "features workbook\b" Makefile && grep -E "features workbook-embedded" Makefile && grep -E "make purity-check" justfile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "features workbook\b" Makefile` matches a `cargo tree -p pmcp-server-toolkit --features workbook` assertion AND `grep -n "features workbook-embedded" Makefile` matches the embedded-combo assertion (the Makefile is the single implementation, both per-feature trees gated)
    - `grep -n "make purity-check" justfile` confirms just still DELEGATES (no logic duplicated in the justfile — Codex LOW; the repo model is just→make delegation, not justfile-first)
    - The assertion is DISTINCT from the PURITY_CRATES loop (the toolkit is NOT appended to PURITY_CRATES — `grep -n "pmcp-server-toolkit" Makefile` does not add it to the unconditional crate list)
    - `make purity-check` (and therefore `just purity-check`) passes (BOTH the workbook and workbook-embedded trees are reader-free: umya/calamine/quick-xml/swc_/pmcp-code-mode absent in each)
    - The gate FAILS CLOSED on a non-zero cargo status (status-capture idiom reused)
  </acceptance_criteria>
  <done>The purity gate asserts BOTH the toolkit workbook and workbook-embedded features are reader-free as distinct merge-blocking per-feature checks, implemented in the Makefile with just delegating to it (Codex LOW), carrying WBRT-04 forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolkit workbook feature → runtime dep tree | A new/unified feature could pull a reader (umya/quick-xml/swc) into the served binary |
| bundle source → builder boot | A tampered bundle must fail the boot load through the builder, not just the loader unit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-19 | Tampering | reader crate enters the served tree via feature unification (esp. the include_dir-bearing embedded combo) | mitigate | Distinct `cargo tree --features workbook` AND `--features workbook-embedded` reader-absence assertions in the purity gate, merge-blocking, fail-closed on non-zero status (Task 3) |
| T-92-20 | Tampering | tampered bundle served because boot load was bypassed | mitigate | with_workbook_bundle loads via BundleLoader (fail-closed); the tamper-fails-boot integration test proves Err propagates through the builder (Tasks 1-2) |
| T-92-21 | Elevation of Privilege | consumer reaching the runtime crate directly, bypassing re-exports | accept | The toolkit re-exports the full boot surface (D-11) so consumers need not name the runtime; direct runtime use remains possible but is reader-free by the purity gate (Task 1) |
| T-92-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (include_dir gated in 92-01); the purity gate keeps slop/reader crates out of the served tree (Task 3) |
</threat_model>

<verification>
- `cargo build -p pmcp-server-toolkit` (no features) AND `--features workbook,http` both succeed
- `cargo test -p pmcp-server-toolkit --features workbook,http --test workbook_integration` green (5 tools registered; tamper fails boot)
- `cargo build --example workbook_server_http --features workbook-embedded,http` succeeds
- `make purity-check` (and `just purity-check`, which delegates to it) passes with the new toolkit-workbook AND toolkit-workbook-embedded reader-absence assertions
- `make quality-gate` green (fmt/clippy pedantic+nursery/build/test/audit/purity) before phase verification
</verification>

<success_criteria>
- .with_workbook_bundle registers all 5 tools + the resource after a fail-closed boot load
- The toolkit re-exports the full boot surface; workbook feature is out of default
- The streamable-HTTP example serves all five tools from the embedded synthetic @1.1.0 bundle, proven by a bounded end-to-end boot smoke-run test (Codex MEDIUM #11)
- The purity gate defends the reader-free boundary for BOTH the toolkit workbook and workbook-embedded features (distinct, merge-blocking, Makefile implementation with just delegating)
</success_criteria>

<output>
Create `.planning/phases/92-bundlesource-served-tool-toolkit-module/92-05-SUMMARY.md` when done
</output>
