---
phase: 92-bundlesource-served-tool-toolkit-module
plan: 04
type: execute
wave: 4
depends_on: ["92-03"]
files_modified:
  - crates/pmcp-server-toolkit/src/workbook/render_uri.rs
  - crates/pmcp-server-toolkit/src/workbook/render_resource.rs
  - crates/pmcp-server-toolkit/src/workbook/handler.rs
  - crates/pmcp-server-toolkit/src/workbook/mod.rs
  - docs/workbook-uri-spec.md
autonomous: true
requirements: [WBSV-05]
user_setup: []

must_haves:
  truths:
    - "render_workbook validates inputs then returns a provenance-bound workbook:// resource URI (inputs + provenance encoded), not the bytes"
    - "resources/read on a workbook:// URI statelessly regenerates the computed .xlsx: size-guard first, total panic-free decode, verify provenance == bundle stamp, re-validate inputs, re-run, render, base64"
    - "An oversized workbook:// URI errors before any decode (DoS guard); a cross-provenance URI errors before rendering (spoofing guard)"
    - "The workbook:// URI format is published as a documented public SDK contract, including a privacy warning that the URI encodes inputs and may be logged by clients/proxies (Codex MEDIUM #10) and a per-read render-cost note (Codex LOW)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/workbook/render_uri.rs"
      provides: "workbook:// codec with size-guard-first decode"
      contains: "MAX_ENCODED_URI_LEN"
    - path: "crates/pmcp-server-toolkit/src/workbook/render_resource.rs"
      provides: "RenderWorkbookResource ResourceHandler (stateless regen-on-read)"
      contains: "ResourceHandler"
    - path: "docs/workbook-uri-spec.md"
      provides: "the published workbook:// URI public contract (D-16)"
      contains: "workbook://"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/workbook/render_resource.rs"
      to: "crates/pmcp-server-toolkit/src/workbook/input.rs"
      via: "RE-VALIDATE decoded inputs before re-run"
      pattern: "validate_input"
    - from: "crates/pmcp-server-toolkit/src/workbook/render_resource.rs"
      to: "pmcp_workbook_runtime::render"
      via: "render_xlsx then base64"
      pattern: "render_xlsx"
---

<objective>
Complete the fifth tool and its resource: the `workbook://` URI codec
(render_uri.rs), the stateless regen-on-read resource handler
(render_resource.rs), the render_workbook tool handler (added to handler.rs),
the module wiring (mod.rs), and the published URI contract
(docs/workbook-uri-spec.md) — WBSV-05 / D-16.

Purpose: render_workbook returns a provenance-bound pointer; the bytes are
recomputed per resources/read from the decoded URI (Lambda-safe, no session),
with the URI treated as an untrusted attacker-controlled payload (size-guard,
provenance verification, re-validation before re-run).
Output: render_uri.rs, render_resource.rs, the render_workbook handler, mod.rs
module decls, docs/workbook-uri-spec.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-RESEARCH.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-PATTERNS.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-03-SUMMARY.md

<interfaces>
<!-- Reuse, don't re-implement (from prior plans + runtime): -->
validate_input(...)                               // workbook/input.rs — RE-VALIDATE on read
to_iserror_result / ProvStamp                     // workbook/error.rs — ProvStamp { bundle_id, version, combined_hash } (combined_hash = BundleLock.combined; NEVER workbook_hash — Codex HIGH #3)
pmcp_workbook_runtime::render::render_xlsx(layout, run) -> Vec<u8>   // writer-only, reader-free
run_executor / build_dag                          // re-run on read

<!-- SDK idioms (read these in-repo files): -->
crates/pmcp-server-toolkit/src/resources.rs       // #[async_trait] impl ResourceHandler list+read (~336-365); Content::resource_with_text/blob (~190-196); pmcp::Error::protocol(ErrorCode::..) (~359-362)
base64 0.22                                        // URL_SAFE_NO_PAD for the URI, STANDARD for xlsx bytes (declared by the TOOLKIT under its workbook feature — NOT by the runtime; Codex MEDIUM #6)
docs/workbook-dialect-spec.md                     // the published-contract precedent docs/workbook-uri-spec.md follows
</interfaces>

<lift_source>
<!-- PRIVATE reference, scrub per D-13. Lift verbatim:
  .../quote-pricing/crates/quote-pricing-server/src/workbook/render_uri.rs   (size guard ~129-136 checked FIRST; total panic-free decode)
  .../workbook/render_resource.rs   (provenance verify ~64-86; RE-VALIDATE ~88-92; re-run + render + base64)
  .../workbook/handler.rs   (RenderWorkbookHandler — returns the URI pointer, not bytes)
  DROP the DispatchingResource wrapper (A3): the workbook module has only one resource; register RenderWorkbookResource directly. The value-schema:// resource is NOT lifted.
-->
</lift_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: workbook:// URI codec (render_uri.rs) + render_workbook handler — WBSV-05, V12</name>
  <files>crates/pmcp-server-toolkit/src/workbook/render_uri.rs, crates/pmcp-server-toolkit/src/workbook/handler.rs, crates/pmcp-server-toolkit/src/workbook/mod.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (the existing 4 handlers from 92-03 — add the 5th alongside; reuse validate_input + ProvStamp + ToolInfo discipline)
    - crates/pmcp-server-toolkit/src/workbook/error.rs (to_iserror_result + ProvStamp)
    - crates/pmcp-server-toolkit/src/workbook/input.rs (validate_input — render_workbook validates before encoding)
    - lighthouse render_uri.rs (size guard ~129-136 FIRST; codec) + handler.rs RenderWorkbookHandler [private ref]
  </read_first>
  <behavior>
    - Test (property/round-trip): encode(inputs, provenance) then decode yields the same inputs + provenance for arbitrary valid input maps
    - Test: an encoded string longer than MAX_ENCODED_URI_LEN is REJECTED by decode before any base64 work (size-guard-first)
    - Test: a corrupted/truncated URI decodes to an Err, never a panic (#![deny(panic)] enforced)
    - Property/fuzz test: decode over arbitrary byte/string sequences (proptest-generated random strings, truncated/garbage base64, oversized payloads, non-UTF-8-ish input) NEVER panics and ALWAYS returns Ok or Err(WorkbookToolError) — total decode (CLAUDE.md ALWAYS-fuzz, via proptest)
    - Test: render_workbook with valid inputs returns a structuredContent pointer containing a workbook:// URI + provenance stamp (the bytes are NOT in the response)
    - Test: render_workbook with invalid inputs returns isError:true in structuredContent
  </behavior>
  <action>
    Create workbook/render_uri.rs by lifting the lighthouse codec VERBATIM: a `MAX_ENCODED_URI_LEN` const, an `encode(inputs, provenance) -> String` producing a `workbook://`-scheme URI (base64 URL_SAFE_NO_PAD over the inputs + provenance triple), and a `decode(uri) -> Result<DecodedRender, WorkbookToolError>` that checks the size guard FIRST (reject oversized before any decode — DoS mitigation V12), then does a TOTAL, panic-free decode. Add `pub mod render_uri;` to workbook/mod.rs. Add a RenderWorkbookHandler to handler.rs (5th `ToolHandler`): construct ToolInfo via the constructor (non-empty outputSchema), call `validate_input`, then `render_uri::encode(inputs, stamp)` and return the `workbook://` URI pointer + ProvStamp in structuredContent — return the POINTER, not the bytes (bytes come on resources/read). Failure path → to_iserror_result. Add proptest coverage for the codec (CLAUDE.md ALWAYS-fuzz, via proptest): (1) a round-trip + determinism property (`encode∘decode == identity`); and (2) a decode-fuzz property (`prop_decode_total`) feeding `decode` arbitrary/adversarial strings — proptest-generated random strings, truncated and garbage base64, oversized inputs past MAX_ENCODED_URI_LEN — asserting decode is TOTAL (never panics; always Ok or Err(WorkbookToolError)). Ensure `proptest` is a toolkit `[dev-dependencies]` entry. Honor the deny lint. Scrub S-4.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook,http workbook::render_uri 2>/dev/null || cargo test -p pmcp-server-toolkit --features workbook workbook::render_uri</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MAX_ENCODED_URI_LEN" crates/pmcp-server-toolkit/src/workbook/render_uri.rs` matches; the size guard is checked before any base64 decode (the oversized-URI test rejects before decode)
    - The proptest round-trip (`encode∘decode == identity`) passes; the determinism property passes
    - The decode-fuzz property `prop_decode_total` passes — decode is total (no panic; always Ok|Err) over arbitrary/adversarial byte/string sequences: `cargo test -p pmcp-server-toolkit --features workbook prop_decode_total`
    - The corrupted-URI test returns Err, no panic
    - render_workbook returns a workbook:// pointer (not bytes) + provenance in structuredContent; invalid input → isError:true
    - The encoded provenance uses the ProvStamp `combined_hash` field (NOT workbook_hash); `grep -rn "workbook_hash" crates/pmcp-server-toolkit/src/workbook/render_uri.rs` returns 0 hits (Codex HIGH #3)
    - `grep -rEi "ufh|quote|coil|towelrad|plot.?3" crates/pmcp-server-toolkit/src/workbook/render_uri.rs` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>The workbook:// codec (size-guard-first, panic-free, round-trip-proven, decode-fuzz-proven total over adversarial input) and the pointer-returning render_workbook handler exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stateless regen-on-read resource handler (render_resource.rs) — WBSV-05, V3/V12</name>
  <files>crates/pmcp-server-toolkit/src/workbook/render_resource.rs, crates/pmcp-server-toolkit/src/workbook/mod.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/resources.rs (#[async_trait] impl ResourceHandler list+read signatures ~336-365; Content::resource_with_text/blob ~190-196; pmcp::Error::protocol(ErrorCode::..) ~359-362)
    - crates/pmcp-server-toolkit/src/workbook/render_uri.rs (decode — from Task 1)
    - crates/pmcp-server-toolkit/src/workbook/input.rs (validate_input — RE-VALIDATE on read)
    - crates/pmcp-workbook-runtime/src/render/mod.rs (render_xlsx(layout, run) ~227)
    - lighthouse render_resource.rs (provenance verify ~64-86; re-validate ~88-92; re-run+render) [private ref]
  </read_first>
  <behavior>
    - Test: read(valid workbook:// URI) returns a ReadResourceResult carrying base64 .xlsx bytes; reading the SAME URI twice yields byte-identical bytes (stateless determinism)
    - Test: read() with a URI whose decoded provenance != the bundle stamp errors before rendering (cross-provenance spoofing guard)
    - Test: read() RE-VALIDATES decoded inputs through validate_input — an out-of-range decoded input errors, not renders (injection guard)
    - Test: list() returns the single workbook:// resource entry
  </behavior>
  <action>
    Create workbook/render_resource.rs with `pub struct RenderWorkbookResource` holding the Arc<WorkbookBundle>, and `#[async_trait] impl ResourceHandler` mirroring resources.rs:336-365: `list` returns the single `workbook://` resource entry; `read(uri, _extra)` performs the stateless regen pipeline — (1) `render_uri::decode(uri)` (size-guard already inside), (2) VERIFY the decoded provenance equals the bundle stamp (cross-provenance → error, lighthouse :64-86), (3) RE-VALIDATE the decoded inputs through `validate_input` (lighthouse :88-92), (4) re-run the executor, (5) `render_xlsx(layout, run)`, (6) base64-STANDARD-encode the bytes into a `ReadResourceResult` via the Content blob/text idiom (resources.rs:190-196). Error paths use `pmcp::Error::protocol(ErrorCode::.., msg)` exactly as resources.rs:359-362. Register `pub mod render_resource;` in workbook/mod.rs and re-export `RenderWorkbookResource`. Bytes are recomputed per read — NO server-side session (V3). Honor the deny lint; scrub S-4. Keep `read` under cognitive complexity 25 (decompose into helpers if needed — the lighthouse already does).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook,http workbook::render_resource 2>/dev/null || cargo test -p pmcp-server-toolkit --features workbook workbook::render_resource</automated>
  </verify>
  <acceptance_criteria>
    - read(valid URI) returns base64 .xlsx; reading twice is byte-identical (stateless determinism test passes)
    - The cross-provenance test errors BEFORE rendering (decoded `combined_hash` verified == bundle stamp `combined_hash`; `grep -rn "workbook_hash" crates/pmcp-server-toolkit/src/workbook/render_resource.rs` returns 0 hits — Codex HIGH #3)
    - The re-validation test: an out-of-range decoded input errors via validate_input, does not render
    - `grep -n "ResourceHandler" crates/pmcp-server-toolkit/src/workbook/render_resource.rs` matches; list() returns the single workbook:// entry
    - `grep -rEi "ufh|quote|coil|towelrad|value-schema|DispatchingResource" crates/pmcp-server-toolkit/src/workbook/render_resource.rs` returns 0 hits (S-4 + A3 single-resource)
  </acceptance_criteria>
  <done>The stateless regen-on-read resource verifies provenance, re-validates, re-runs, renders, and base64-returns bytes deterministically — no committed DispatchingResource.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the workbook:// URI public contract (docs/workbook-uri-spec.md) — D-16</name>
  <files>docs/workbook-uri-spec.md</files>
  <read_first>
    - docs/workbook-dialect-spec.md (the published-contract precedent: structure, versioning-decision note)
    - crates/pmcp-server-toolkit/src/workbook/render_uri.rs (the actual codec: scheme, payload fields, MAX_ENCODED_URI_LEN)
  </read_first>
  <action>
    Create docs/workbook-uri-spec.md following the structure of docs/workbook-dialect-spec.md: define the `workbook://` URI scheme (authority/path/query layout), the encoded payload (inputs map + provenance triple bundle_id/version/combined-hash), the base64 encoding (URL_SAFE_NO_PAD), the size bound (MAX_ENCODED_URI_LEN, stated as a number), the stateless-regeneration-on-read semantics (provenance-verified, re-validated, re-run, rendered), and a versioning-decision note (format changes are versioned decisions, not silent edits — D-16). Reference the security properties (untrusted payload; size-guard-first; provenance + re-validation gates). Add a PRIVACY WARNING section (Codex MEDIUM #10): the workbook:// URI ENCODES the caller's inputs in its payload, so the URI may be logged by MCP clients, proxies, or gateways — operators handling sensitive inputs must treat the URI as sensitive (and a future opaque-handle alternative is noted as a possible evolution). Add a RENDER-COST note (Codex LOW): resources/read re-runs the executor and re-renders the .xlsx on every read (stateless, no cache); document the MAX_ENCODED_URI_LEN bound as the input-size limit and note that read cost scales with workbook size, flagging future rate-limiting as an extension point. Use tax-domain examples only (S-4).
  </action>
  <verify>
    <automated>test -f docs/workbook-uri-spec.md && grep -q "workbook://" docs/workbook-uri-spec.md && grep -qi "MAX_ENCODED_URI_LEN\|size" docs/workbook-uri-spec.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/workbook-uri-spec.md exists and documents the scheme, payload fields, base64 encoding, size bound, stateless-regen semantics, and a versioning-decision note
    - `grep -qi "provenance" docs/workbook-uri-spec.md` matches (security properties documented)
    - `grep -qiE "logged|privacy|sensitive" docs/workbook-uri-spec.md` matches — the privacy warning that URIs encode inputs and may be logged by clients/proxies is present (Codex MEDIUM #10)
    - `grep -qiE "re-run|re-render|cost|rate" docs/workbook-uri-spec.md` matches — the render-cost / per-read recomputation note is present (Codex LOW)
    - `grep -rEi "ufh|quote|coil|towelrad" docs/workbook-uri-spec.md` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>The workbook:// URI format is published as a documented public SDK contract alongside the dialect spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-supplied workbook:// URI → resources/read | The pointer round-trips through the client; the URI is attacker-controlled |
| decoded inputs → executor | Decoded inputs must be re-validated before re-run (injection lever) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-14 | Denial of Service | oversized workbook:// URI | mitigate | MAX_ENCODED_URI_LEN size guard checked FIRST, before any decode (Task 1) |
| T-92-15 | Spoofing | cross-provenance / forged workbook:// URI | mitigate | render_resource verifies decoded provenance == bundle stamp before rendering (Task 2) |
| T-92-16 | Tampering | injected inputs via the round-tripped URI | mitigate | RE-VALIDATE decoded inputs through validate_input on every read before re-run (Task 2) |
| T-92-17 | Denial of Service | malformed URI panic | mitigate | Total, panic-free decode; #![deny(panic)] enforces it; a proptest decode-fuzz test (`prop_decode_total`) proves totality over arbitrary/adversarial input (Task 1) |
| T-92-18 | Information Disclosure | server-side render state leakage | accept | Stateless regen-on-read: no session, bytes recomputed per read (V3) — no server-side render state to leak (Task 2) |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features workbook,http workbook::render_uri workbook::render_resource` green
- Codec proptest round-trip + determinism + decode-fuzz (`prop_decode_total`) pass (CLAUDE.md ALWAYS-fuzz)
- `grep -rEi "ufh|quote|coil|towelrad|value-schema|DispatchingResource" crates/pmcp-server-toolkit/src/workbook/render_*.rs` = 0 hits
- docs/workbook-uri-spec.md published
</verification>

<success_criteria>
- render_workbook returns a provenance-bound workbook:// pointer; resources/read statelessly regenerates the .xlsx (verified, re-validated, re-run, rendered)
- Oversized and cross-provenance URIs error before rendering; decode is panic-free
- The URI format is a published, versioned public contract
</success_criteria>

<output>
Create `.planning/phases/92-bundlesource-served-tool-toolkit-module/92-04-SUMMARY.md` when done
</output>
