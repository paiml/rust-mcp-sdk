---
phase: 92-bundlesource-served-tool-toolkit-module
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [WBSV-01, WBSV-02, WBSV-05]
files_modified:
  - crates/pmcp-server-toolkit/tests/support/fixture_gen.rs
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock
  - crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs
  - crates/pmcp-server-toolkit/src/workbook/handler.rs

must_haves:
  truths:
    - "calculate({ gross_income: 100000 }) returns taxable_income.value = 88000.0 (caller input is honored, not the baked-in default 60000)"
    - "Role::Input cells are ABSENT from the committed golden IR; validate_input tier-default seeds survive the executor topo walk"
    - "The executor literal arm is seed-preserving: a pre-seeded caller value for an IR-literal key is never overwritten"
    - "The committed golden at tests/fixtures/tax-calc@1.1.0/ is byte-identical to a fresh regeneration after the build_ir() fix"
  artifacts:
    - path: "crates/pmcp-server-toolkit/tests/support/fixture_gen.rs"
      provides: "build_ir() that omits the three Role::Input cells from the IR"
      contains: "Input cells are deliberately ABSENT from the IR"
    - path: "crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs"
      provides: "seed-preserving literal arm"
      contains: "env.get"
    - path: "crates/pmcp-server-toolkit/src/workbook/handler.rs"
      provides: "value-asserting regression test for a non-default input"
      contains: "88000"
    - path: "crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json"
      provides: "regenerated golden IR without the input cells"
  key_links:
    - from: "input.rs validate_input"
      to: "executor.rs run"
      via: "seed env survives the topo walk"
      pattern: "env\\.get"
    - from: "fixture_gen.rs build_ir"
      to: "tests/fixtures/tax-calc@1.1.0/executable.ir.json"
      via: "regenerate_committed_golden"
      pattern: "1_Inputs!B2"
---

<objective>
Close Blocker 1 (CR-01) from 92-VERIFICATION.md: caller-supplied inputs are
silently ignored — every calculate / explain / render_workbook call computes
from the bundle's baked-in default inputs (gross_income=60000) regardless of
what the caller sends. The root cause is that the fixture generator emits the
three Role::Input cells as IR literals, which the executor's literal arm
unconditionally re-seeds at topo-walk time, overwriting validate_input's
caller-seeded values.

This plan applies the three required fixes plus the recommended defense-in-depth:
1. build_ir() omits Role::Input cells from the IR (honoring the executor's
   documented seed contract).
2. The executor literal arm becomes seed-preserving (a future compiler-emitted
   bundle that repeats this shape cannot reintroduce the bug silently).
3. A value-asserting regression test proves a non-default input flows through.
4. The committed golden is regenerated so it is byte-identical to the fixed
   generator (the always-on byte-stability test enforces this in CI).

Purpose: WBSV-01 (calculate returns all named outputs from the caller's inputs),
WBSV-02 (explain trace reflects the caller's inputs), WBSV-05 (render_workbook
regen-on-read reflects the caller's inputs) are all BLOCKED by this one defect.
The architecture is sound; the committed golden bundle violates the
architecture's own contract.

Output: a fixed generator, a seed-preserving executor, a regenerated golden, and
a regression test that would have caught this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-VERIFICATION.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-REVIEW.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md

<interfaces>
<!-- The executor seed contract (executor.rs:90-91, documented):
     "seed carries pre-loaded Role::Input cells (cells ABSENT from ir)". -->

From crates/pmcp-workbook-runtime/src/sheet_ir/eval_bridge.rs:
  pub fn seed_cell(mut self, key: impl Into<String>, value: &CellValue) -> Self  // unconditional HashMap::insert
  pub fn get(&self, key: &str) -> Option<&JsonValue>                              // presence probe — use this to make the literal arm seed-preserving

From crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs (literal arm, ~line 118-129):
  the topo walk's Some(Cell { expr: CellExpr::Literal(v), .. }) arm currently does:
    env = env.seed_cell(&key, v);  // unconditional — overwrites a caller seed

From crates/pmcp-server-toolkit/tests/support/fixture_gen.rs:
  const CELL_GROSS_INCOME: &str = "1_Inputs!B2";
  const CELL_FILING_STATUS: &str = "1_Inputs!B3";
  const CELL_DEDUCTIONS: &str = "1_Inputs!B4";
  fn build_ir() -> BTreeMap<String, Cell>  // lines ~126-178; currently inserts all three input literals
  pub fn generate_tax_calc_bundle(out_dir: &Path) -> std::io::Result<()>  // line 524 — rebuilds BUNDLE.lock via build_bundle_lock

From crates/pmcp-server-toolkit/src/workbook/handler.rs (test module, line ~581):
  fn golden_bundle() -> Arc<WorkbookBundle>  // loads tests/fixtures/tax-calc@1.1.0
  CalculateHandler::compute(&self, args: Value) -> Result<Value, _>  // success payload: v["outputs"][<key>]["value"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Role::Input cells from build_ir() and make the executor literal arm seed-preserving</name>
  <files>crates/pmcp-server-toolkit/tests/support/fixture_gen.rs, crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/support/fixture_gen.rs (read the full file; the relevant region is build_ir() lines ~126-178 and the input-role/manifest builders that follow — the input ROLES in the manifest and cell_map MUST stay; only the IR literal entries for the three input cells are removed)
    - crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs (lines 80-145 — the run() fn, the documented seed contract at lines 90-91, and the literal arm at lines 118-129)
    - crates/pmcp-workbook-runtime/src/sheet_ir/eval_bridge.rs (lines 55-70 — seed_cell and get signatures)
    - crates/pmcp-server-toolkit/src/workbook/input.rs (lines 100-133 — validate_input step 1 tier-default seeding that must remain the sole source of input values)
  </read_first>
  <action>
    In fixture_gen.rs build_ir(): delete the three IR literal entries for the input
    cells — CELL_GROSS_INCOME ("1_Inputs!B2", default 60000.0), CELL_DEDUCTIONS
    ("1_Inputs!B4", default 12000.0), and CELL_FILING_STATUS ("1_Inputs!B3",
    "single"). These three are currently inserted at the top of build_ir() via
    literal_num / literal_text. Keep ALL other IR entries: the four governed bracket
    cells (CELL_BRACKET1_BOUND, CELL_BRACKET1_RATE, CELL_BRACKET2_BOUND,
    CELL_BRACKET2_RATE) and the four output formula cells (CELL_TAXABLE_INCOME =
    gross_income - deductions, CELL_TAX_OWED, CELL_EFFECTIVE_RATE, CELL_MARGINAL_RATE).
    The output formulas still reference CELL_GROSS_INCOME / CELL_DEDUCTIONS by Ref —
    those refs now resolve from the seed env, NOT from an IR literal. Add a comment
    on the removal explaining the executor seed contract (input cells MUST be absent
    from the IR or an IR literal would clobber the caller's seed). Do NOT touch
    build_manifest, the input_role rows, or the cell_map inputs — the three input
    cells must still be declared as Role::Input in the manifest and listed in
    cell_map.inputs so validate_input seeds and dtype/enum-gates them.

    In executor.rs literal arm (lines ~118-129, the Some(Cell { expr:
    CellExpr::Literal(v), .. }) branch): make the seed_cell call seed-preserving —
    only seed when the key is not already present in env. Use env.get(&key).is_none()
    as the guard before calling env = env.seed_cell(&key, v). Leave the existing
    errs.insert / computed.insert behavior unchanged (a pre-seeded caller value still
    gets recorded in computed for that key). This is defense-in-depth: a future
    compiler-emitted bundle that repeats the input-literal shape can no longer
    silently clobber caller seeds. Update the literal-arm doc comment to state that
    a caller-seeded value wins over an IR literal of the same key.
  </action>
  <verify>
    <automated>grep -c "literal_num(CELL_GROSS_INCOME\|literal_num(CELL_DEDUCTIONS\|literal_text(CELL_FILING_STATUS" crates/pmcp-server-toolkit/tests/support/fixture_gen.rs | grep -qx 0 && echo "input literals removed"</automated>
    <automated>grep -q "env.get(&key).is_none()" crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs && echo "executor seed-preserving"</automated>
    <automated>cargo build -p pmcp-workbook-runtime -p pmcp-server-toolkit --features workbook 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "literal_num\(CELL_GROSS_INCOME|literal_num\(CELL_DEDUCTIONS|literal_text\(CELL_FILING_STATUS" crates/pmcp-server-toolkit/tests/support/fixture_gen.rs` returns ZERO matches (the three input cells are no longer emitted as IR literals).
    - `grep -c "input_role(" crates/pmcp-server-toolkit/tests/support/fixture_gen.rs` is unchanged (the manifest still declares the three input Role::Input rows — only the IR literals were removed).
    - The executor literal arm guards seed_cell with `env.get(&key).is_none()` — `grep "env.get(&key).is_none()" crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs` matches.
    - `cargo build -p pmcp-workbook-runtime -p pmcp-server-toolkit --features workbook` compiles with zero errors.
  </acceptance_criteria>
  <done>Input cells absent from IR generation; executor literal arm is seed-preserving; both crates compile under the workbook feature.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the committed golden and add the non-default-input regression test</name>
  <files>crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock, crates/pmcp-server-toolkit/src/workbook/handler.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/fixture_byte_stability.rs (lines 28-64 — regenerate_committed_golden ignored test and golden_regeneration_is_byte_identical guard; this is how the golden is refreshed and how CI enforces byte-stability)
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (lines 581-678 — the #[cfg(test)] mod tests block: golden_bundle(), CalculateHandler::new, .compute(json!(...)), and the existing calculate_returns_all_outputs_with_provenance_no_headline test that asserts only key presence)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json (the committed golden IR — it currently contains 1_Inputs!B2/B3/B4 literal entries that must vanish after regeneration)
  </read_first>
  <action>
    Regenerate the committed golden in place by running the ignored regeneration
    test: `cargo test -p pmcp-server-toolkit --features workbook --test
    fixture_byte_stability regenerate_committed_golden -- --ignored --exact`. This
    rewrites all seven members under tests/fixtures/tax-calc@1.1.0/ — executable.ir.json
    loses the three 1_Inputs!B2/B3/B4 entries and BUNDLE.lock's h_exec / combined
    hashes change to match. Commit the regenerated fixture files.

    Add a value-asserting regression test in the handler.rs #[cfg(test)] mod tests
    block (the same module as calculate_returns_all_outputs_with_provenance_no_headline,
    around line 632). Name it calculate_honors_non_default_input. Build a
    CalculateHandler::new(golden_bundle()), call
    .compute(json!({ "inputs": { "gross_income": 100000.0 } })).expect(...), and
    assert v["outputs"]["taxable_income"]["value"] == json!(88000.0) (100000 - 12000,
    the default deduction). Add a second assertion that a DIFFERENT non-default input
    also flows: compute({ "inputs": { "gross_income": 80000.0 } }) yields
    taxable_income value 68000.0 — this guards against any single-value coincidence.
    Include a comment referencing CR-01 / 92-VERIFICATION.md so the test's purpose is
    self-documenting.
  </action>
  <verify>
    <automated>grep -v '^#' crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json | grep -c "1_Inputs!B2\|1_Inputs!B3\|1_Inputs!B4" | grep -qx 0 && echo "input cells gone from golden IR"</automated>
    <automated>cargo test -p pmcp-server-toolkit --features workbook calculate_honors_non_default_input -- --test-threads=1 2>&1 | tail -5</automated>
    <automated>cargo test -p pmcp-server-toolkit --features workbook --test fixture_byte_stability golden_regeneration_is_byte_identical -- --test-threads=1 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "1_Inputs!B2|1_Inputs!B3|1_Inputs!B4" crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json` returns ZERO matches (the regenerated golden IR omits the input cells).
    - A test named `calculate_honors_non_default_input` exists in handler.rs and asserts `v["outputs"]["taxable_income"]["value"] == json!(88000.0)` for gross_income 100000.0 and `68000.0` for 80000.0.
    - `cargo test -p pmcp-server-toolkit --features workbook calculate_honors_non_default_input -- --test-threads=1` PASSES (it would have FAILED — returning 48000.0 — before this plan).
    - `cargo test -p pmcp-server-toolkit --features workbook --test fixture_byte_stability golden_regeneration_is_byte_identical` PASSES (committed golden equals a fresh regeneration of the fixed generator).
    - The existing default-input tests (calculate_returns_all_outputs_with_provenance_no_headline at line 597) still PASS — omitted/default inputs still resolve via validate_input step-1 tier-default seeding.
  </acceptance_criteria>
  <done>Committed golden regenerated without input IR literals; a non-default-input regression test asserts taxable_income=88000; byte-stability and all existing value-bearing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → calculate/explain/render_workbook | untrusted caller inputs cross into the compute path; they MUST drive the computation, not be silently discarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-06-01 | Tampering / Repudiation | executor literal arm | mitigate | Seed-preserving literal arm (env.get(&key).is_none() guard) prevents an IR literal from overwriting a validated caller seed — closes the silent-compute-from-defaults vector; a future bundle repeating the input-literal shape can no longer reintroduce it. |
| T-92-06-02 | Information disclosure (misrepresentation) | calculate success payload | mitigate | The regression test asserts the served numbers are the governed computation over the CALLER's inputs (88000 for 100000), so a response can no longer carry default-derived values under a valid provenance stamp. |
| T-92-06-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — pure source edits + fixture regeneration. |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features workbook -- --test-threads=1` passes in full (the new regression test plus all existing value-bearing and byte-stability tests).
- `cargo test -p pmcp-workbook-runtime -- --test-threads=1` passes (the seed-preserving executor change does not regress runtime tests).
- The committed golden IR no longer contains the three input cells.
- `make quality-gate` passes before committing (fmt + clippy pedantic+nursery + build + test + audit — matches CI exactly).
</verification>

<success_criteria>
- calculate({ gross_income: 100000 }) returns taxable_income.value = 88000.0 (not 48000.0).
- Role::Input cells are absent from the committed golden's executable.ir.json.
- The executor literal arm is seed-preserving.
- Golden regeneration is byte-identical; all existing tests pass.
- WBSV-01 / WBSV-02 / WBSV-05 are unblocked (caller inputs flow through compute, explain, and render_workbook).
</success_criteria>

<output>
Create `.planning/phases/92-bundlesource-served-tool-toolkit-module/92-06-SUMMARY.md` when done.
</output>
