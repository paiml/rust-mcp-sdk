---
phase: 92-bundlesource-served-tool-toolkit-module
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - crates/pmcp-server-toolkit/src/lib.rs
  - crates/pmcp-server-toolkit/src/workbook/mod.rs
  - crates/pmcp-server-toolkit/Cargo.toml
  - crates/pmcp-server-toolkit/tests/support/mod.rs
  - crates/pmcp-server-toolkit/tests/support/fixture_gen.rs
  - crates/pmcp-server-toolkit/tests/support/tamper.rs
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/manifest.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/cell_map.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/layout.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/evidence/changelog.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/evidence/parser_equivalence.json
  - crates/pmcp-server-toolkit/tests/fixture_byte_stability.rs
autonomous: true
requirements: [WBSV-08, WBSV-09]
user_setup: []

must_haves:
  truths:
    - "The toolkit gains a workbook/workbook-embedded feature pair and a gated #[cfg(feature=\"workbook\")] pub mod workbook skeleton so wave-3/4 tests can compile with --features workbook before the handlers land (Codex HIGH #1)"
    - "A synthetic tax-calc bundle (gross_income/filing_status inputs → multiple named tax outputs) exists as seven committed golden artifacts under tests/fixtures/tax-calc@1.1.0/ (the @1.1.0 golden carries the recorded v1.0.0→v1.1.0 changelog — Codex HIGH #5)"
    - "Any code change that would alter the tax-calc bundle artifacts is detected automatically in CI before merge (byte-stability check on the committed golden — an in-repo generator regenerates the bundle and the committed golden must match byte-for-byte)"
    - "The golden bundle loads cleanly through BundleLoader (it passes its own boot integrity gate)"
    - "Tamper helpers copy the golden to a tempdir and corrupt it (byte-flip / delete a member / desync the lock version / add an unexpected member) for negative-path tests"
    - "Zero customer identifiers appear anywhere in the fixture or generator (tax-domain names only; json_key not plot3_json_key)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/workbook/mod.rs"
      provides: "minimal #[cfg(feature=\"workbook\")] module skeleton (empty/placeholder) that wave 3 extends"
      contains: "pub mod"
    - path: "crates/pmcp-server-toolkit/tests/support/fixture_gen.rs"
      provides: "generate_tax_calc_bundle(dir) building the 7 artifacts via runtime Serialize types"
      contains: "build_bundle_lock"
    - path: "crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock"
      provides: "the committed golden lock (bundle_id, version 1.1.0, combined hash)"
      contains: "bundle_id"
    - path: "crates/pmcp-server-toolkit/tests/support/tamper.rs"
      provides: "copy-to-tempdir + corrupt helpers for WBSV-06/08 negatives"
      contains: "fn flip_byte"
  key_links:
    - from: "crates/pmcp-server-toolkit/tests/support/fixture_gen.rs"
      to: "crates/pmcp-workbook-runtime/src/artifact_model.rs"
      via: "build_bundle_lock so the golden lock matches the loader's recompute"
      pattern: "build_bundle_lock"
    - from: "crates/pmcp-server-toolkit/tests/fixture_byte_stability.rs"
      to: "crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0"
      via: "regenerate-then-diff equality assertion"
      pattern: "byte"
---

<objective>
Land the early toolkit feature wiring (so wave-3/4 plans compile with
`--features workbook` — Codex HIGH #1) and produce the synthetic tax-calc golden
bundle every WBSV test runs against (D-01..D-05): a hand-authored, full-surface
fixture (multiple named outputs, no headline; enum + numeric + string inputs
across tiers; units; governed rate table; a recorded v1.0.0→v1.1.0 changelog;
a layout descriptor; manifest-declared bracket-boundary annotations) produced by
an in-repo generator and committed as the byte-stable `tax-calc@1.1.0` golden,
plus tamper helpers for the negative paths.

Purpose: (1) Move the Cargo feature + gated `pub mod workbook` skeleton EARLY so
Plans 03/04 can `cargo test --features workbook` (the handlers fill the skeleton
in wave 3). (2) Break the chicken-and-egg with the Phase-93 compiler by freezing
a synthetic golden contract now; Phase 93 re-emits the same workbook and diffs.
Output: the toolkit feature/skeleton wiring, the seven committed `@1.1.0`
artifacts, the generator, the byte-stability check, and tamper helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-RESEARCH.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-PATTERNS.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-01-SUMMARY.md

<interfaces>
<!-- Runtime Serialize types the generator constructs (from 92-01; read
     crates/pmcp-workbook-runtime/src/{artifact_model.rs,manifest_model.rs,changelog.rs,render/layout.rs}). -->
build_bundle_lock(bundle_id, version, workbook_hash, ir_json, manifest_json, evidence_hash) -> BundleLock
BundleLock { bundle_id, version, workbook_hash /*source-workbook hash, KEPT*/, artifacts: ArtifactHashes{executable,manifest,evidence}, combined }
CellEntry { json_key, seed_coord, unit }   // renamed from plot3_json_key in 92-01
CellMap { inputs, outputs: Vec<CellEntry> }   // NO supply_total_cell after 92-01
Manifest { ..., annotations: Vec<AnnotationDecl> }   // additive field from 92-01
AnnotationDecl { name, target, meaning }
VersionChangelog / OutputDelta / OutputMeta / ChangeClass   // from changelog.rs
LayoutDescriptor / SheetLayout / CellLayout   // from render/layout.rs
BundleLoader::load(&dyn BundleSource) -> Result<WorkbookBundle, BundleLoadError>   // golden must pass it
LocalDirSource::new(path)

<!-- SDK feature/skeleton anchors for Task 0 (read these in-repo files): -->
crates/pmcp-server-toolkit/src/lib.rs           // #[cfg(feature="http")] pub mod http (~43-44)
crates/pmcp-server-toolkit/Cargo.toml           // [features] ~73; default=["code-mode"] ~74; http feature ~98-107
</interfaces>

<bundle_shape>
<!-- Seven-member shape reference ONLY (artifact names + BUNDLE.lock structure),
     NO content copy (D-01). Private ref:
  /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/quote-pricing-server/bundles/ufh-quote@1.0.0/
  Members: manifest.json, executable.ir.json, cell_map.json, layout.json,
  BUNDLE.lock, evidence/changelog.json, evidence/parser_equivalence.json -->
</bundle_shape>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Early toolkit workbook feature + gated module skeleton (Codex HIGH #1)</name>
  <files>crates/pmcp-server-toolkit/Cargo.toml, crates/pmcp-server-toolkit/src/lib.rs, crates/pmcp-server-toolkit/src/workbook/mod.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/Cargo.toml ([features] ~73; default=["code-mode"] ~74; http feature block ~98-107; [dev-dependencies])
    - crates/pmcp-server-toolkit/src/lib.rs (#[cfg(feature="http")] pub mod http ~43-44)
    - .planning/phases/92-bundlesource-served-tool-toolkit-module/92-01-SUMMARY.md (the runtime crate name/path + embedded feature)
  </read_first>
  <action>
    Land the feature wiring + module skeleton EARLY so Plans 03/04 compile under
    `--features workbook` (Codex HIGH #1 — the dep + feature previously only
    existed in Plan 05). In crates/pmcp-server-toolkit/Cargo.toml add the optional
    runtime dep `pmcp-workbook-runtime = { version = "0.1.0", path = "../pmcp-workbook-runtime", optional = true }` and `base64 = { version = "0.22", optional = true }` (base64 lives in the toolkit, not the runtime — Codex MEDIUM #6), and add the D-06 feature pair to `[features]`: `workbook = ["dep:pmcp-workbook-runtime", "dep:base64"]` (LocalDirSource only; does NOT pull include_dir) and `workbook-embedded = ["workbook", "pmcp-workbook-runtime/embedded"]` (layers EmbeddedSource + include_dir on top). Keep `default = ["code-mode"]` unchanged (neither in default, D-10). In src/lib.rs add `#[cfg(feature = "workbook")] pub mod workbook;` mirroring the http decl at ~43-44. Create a MINIMAL crates/pmcp-server-toolkit/src/workbook/mod.rs skeleton that compiles under `--features workbook` with no handlers yet: a module doc-comment naming the phase, and placeholder `// pub mod error; pub mod schema; pub mod input; pub mod handler;` lines kept COMMENTED until Plan 03 creates those files (Codex HIGH #2 — never declare `pub mod input;` before input.rs exists). The skeleton may expose nothing else; Plan 03 Task 1 uncomments/adds the submodule declarations as it creates each file, and Plan 05 adds the builder-ext + re-exports. Confirm `cargo build -p pmcp-server-toolkit --features workbook` and `--features workbook-embedded` both compile against the empty skeleton.
  </action>
  <verify>
    <automated>cargo build -p pmcp-server-toolkit --features workbook && cargo build -p pmcp-server-toolkit --features workbook-embedded && cargo build -p pmcp-server-toolkit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "workbook = \[" crates/pmcp-server-toolkit/Cargo.toml` matches a value WITHOUT `pmcp-workbook-runtime/embedded`; `grep -n "workbook-embedded = \[" crates/pmcp-server-toolkit/Cargo.toml` includes `"workbook"` + `"pmcp-workbook-runtime/embedded"` (D-06)
    - `grep -n "default = " crates/pmcp-server-toolkit/Cargo.toml` still shows `["code-mode"]` (neither workbook nor workbook-embedded in default)
    - `grep -n 'cfg(feature = "workbook")' crates/pmcp-server-toolkit/src/lib.rs` matches the `pub mod workbook;` decl
    - The workbook/mod.rs skeleton compiles under `--features workbook` with NO `pub mod input;`/`pub mod handler;` declared yet (those files do not exist until Plan 03 — Codex HIGH #2)
    - `cargo build -p pmcp-server-toolkit --features workbook` AND `--features workbook-embedded` AND `cargo build -p pmcp-server-toolkit` (no features) all succeed
  </acceptance_criteria>
  <done>The toolkit has the workbook/workbook-embedded feature pair, a gated pub mod workbook, and a minimal compiling skeleton — so wave-3/4 plans can test with --features workbook before the handlers land.</done>
</task>

<task type="auto">
  <name>Task 1: Synthetic tax-calc fixture generator (Serialize-types → 7 artifacts)</name>
  <files>crates/pmcp-server-toolkit/tests/support/mod.rs, crates/pmcp-server-toolkit/tests/support/fixture_gen.rs, crates/pmcp-server-toolkit/Cargo.toml</files>
  <read_first>
    - crates/pmcp-workbook-runtime/src/artifact_model.rs (build_bundle_lock signature, BundleLock/CellMap/CellEntry/ArtifactHashes shapes — post-92-01: bundle_id, json_key, no supply_total_cell)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (Manifest, CellRole, InputTier, Dtype, GovernedDatum, AnnotationDecl — the field set to populate)
    - crates/pmcp-workbook-runtime/src/changelog.rs (VersionChangelog/OutputDelta/OutputMeta/ChangeClass shapes for the v1.0.0→v1.1.0 changelog)
    - crates/pmcp-workbook-runtime/src/render/layout.rs (LayoutDescriptor/SheetLayout/CellLayout for the render descriptor)
    - crates/pmcp-server-toolkit/Cargo.toml ([dev-dependencies] block — add pmcp-workbook-runtime as a dev-dep with the embedded feature off; confirm tempfile dev-dep present)
  </read_first>
  <action>
    Create a test-support generator at tests/support/fixture_gen.rs with `pub fn generate_tax_calc_bundle(out_dir: &Path) -> std::io::Result<()>`. Author a SYNTHETIC progressive-tax workbook (D-02) at VERSION 1.1.0 (Codex HIGH #5 — the committed golden is `tax-calc@1.1.0`, whose recorded changelog documents the prior→current 1.0.0→1.1.0 diff, so `diff_version` serves a real prev→current pair): input sheet with `gross_income` (numeric, Variable tier, unit "USD"), `filing_status` (enum string input, allowed_values ["single","married_joint","head_of_household"]), and `deductions` (numeric, Variable, unit "USD"); a governed bracket rate table (GovernedDatum) of (lower_bound, rate) rows; output sheet with MULTIPLE named outputs each {value,unit} — e.g. `taxable_income` (USD), `tax_owed` (USD), `effective_rate` (ratio), `marginal_rate` (ratio) — NO single privileged headline (WBSV-01). Use the renamed `json_key` field on every CellEntry (NOT plot3_json_key — scrubbed in 92-01). Declare manifest annotations (D-18/S-2): an AnnotationDecl per bracket boundary (name e.g. "bracket_boundary_1", target = the cell/output it annotates, meaning a business-language note) to prove the generic annotations path. Build the IR (executable.ir.json) as pure-cell formulas computing those outputs from the inputs + rate table; build cell_map.json (inputs + outputs, no supply_total_cell); build layout.json via LayoutDescriptor; build evidence/changelog.json as a VersionChangelog whose `diff_version`/prev-version is "1.0.0" and current is "1.1.0", carrying per-output deltas + change class + severity + summary (so diff_version is real); build evidence/parser_equivalence.json. Compute BUNDLE.lock by calling the runtime `build_bundle_lock(bundle_id="tax-calc", version="1.1.0", workbook_hash, ir_json, manifest_json, evidence_hash)` so the golden lock matches exactly what BundleLoader recomputes. Serialize every artifact DETERMINISTICALLY (Codex MEDIUM #8): every map-valued field in any artifact MUST use a sorted/canonical representation — use `BTreeMap` (NOT HashMap) for any map the generator builds, or a canonical serializer that emits sorted keys, and use `serde_json::to_string_pretty` with a fixed config; no `HashMap` iteration order may leak into artifact bytes, or regeneration is non-reproducible. Use ONLY tax-domain identifiers — zero `ufh`/`quote`/`coil`/`plot3`/customer names. Declare `mod support;` from a tests/support/mod.rs and add `pmcp-workbook-runtime` (path) + `tempfile` to the toolkit `[dev-dependencies]` if not already present.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --test fixture_byte_stability generate 2>/dev/null || cargo build -p pmcp-server-toolkit --tests</automated>
  </verify>
  <acceptance_criteria>
    - `generate_tax_calc_bundle` compiles and, when invoked, writes all 7 members under out_dir (manifest.json, executable.ir.json, cell_map.json, layout.json, BUNDLE.lock, evidence/changelog.json, evidence/parser_equivalence.json)
    - The generated manifest declares ≥2 outputs (no headline) and ≥1 AnnotationDecl
    - The generated BUNDLE.lock has `version` "1.1.0" and is produced by the runtime `build_bundle_lock` (not a hand-written hash) (Codex HIGH #5)
    - The generated changelog records a prev "1.0.0" → current "1.1.0" diff (so diff_version is real)
    - Determinism: `grep -n "HashMap" crates/pmcp-server-toolkit/tests/support/fixture_gen.rs` returns 0 hits in artifact-body construction (BTreeMap/canonical only — Codex MEDIUM #8); a fixed serde_json config is used
    - `grep -rEi "ufh|quote|coil|towelrad|plot.?3|first_fix|heat_source|margin\b" crates/pmcp-server-toolkit/tests/support/` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>An in-repo generator emits the full-surface synthetic tax-calc@1.1.0 bundle deterministically (BTreeMap/canonical) via runtime Serialize types, with a real 1.0.0→1.1.0 changelog.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the golden + byte-stability CI check + golden-loads test</name>
  <files>crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/manifest.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/cell_map.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/layout.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/evidence/changelog.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/evidence/parser_equivalence.json, crates/pmcp-server-toolkit/tests/fixture_byte_stability.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/support/fixture_gen.rs (the generator from Task 1 — the byte-stability test invokes it into a tempdir)
    - crates/pmcp-workbook-runtime/src/bundle_loader.rs (BundleLoader::load — the golden must pass it)
    - crates/pmcp-workbook-runtime/src/bundle_source.rs (LocalDirSource::new — used to load the committed golden)
  </read_first>
  <action>
    Run the Task-1 generator once into `crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/` and COMMIT the seven resulting artifacts as the golden contract (D-03). Create tests/fixture_byte_stability.rs with two tests: (1) `golden_regeneration_is_byte_identical` — regenerate the bundle into a tempdir and assert every committed golden member is byte-for-byte equal to its freshly-generated counterpart (read both as bytes, assert_eq per member); (2) `golden_passes_boot_integrity` — `LocalDirSource::new("tests/fixtures/tax-calc@1.1.0")` then `BundleLoader::load(&source)` returns Ok (the golden passes its own WBSV-08 gate, and recompute_evidence_hash == lock.artifacts.evidence per Pitfall 2). The byte-stability test is the CI mechanism enforcing D-03.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --test fixture_byte_stability</automated>
  </verify>
  <acceptance_criteria>
    - All seven golden artifacts exist under tests/fixtures/tax-calc@1.1.0/ and are committed
    - `golden_regeneration_is_byte_identical` passes (regeneration == committed bytes, per member)
    - `golden_passes_boot_integrity` passes (BundleLoader::load returns Ok on the committed golden)
    - `grep -n "bundle_id" crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock` matches (D-17 on-disk contract); `grep -n "1.1.0" crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock` matches (Codex HIGH #5)
    - `grep -rEi "ufh|quote|coil|towelrad|plot.?3" crates/pmcp-server-toolkit/tests/fixtures/` returns 0 hits (S-4)
  </acceptance_criteria>
  <done>The byte-stable tax-calc@1.1.0 golden is committed and provably (a) regenerates identically and (b) passes the boot integrity gate.</done>
</task>

<task type="auto">
  <name>Task 3: Tamper helpers for WBSV-06/08 negative paths (D-05)</name>
  <files>crates/pmcp-server-toolkit/tests/support/tamper.rs, crates/pmcp-server-toolkit/tests/support/mod.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/support/fixture_gen.rs (member layout the tamper helpers copy + corrupt)
    - crates/pmcp-workbook-runtime/src/bundle_loader.rs (the BundleLoadError variants the tamper paths must provoke: IntegrityMismatch, StampMismatch, Parse, UnexpectedMember)
  </read_first>
  <action>
    Create tests/support/tamper.rs with copy-to-tempdir-then-corrupt helpers (D-05 — no committed corrupt fixtures): `pub fn copy_golden_to_temp() -> tempfile::TempDir` (copies the committed `tax-calc@1.1.0` golden tree into a fresh tempdir), `pub fn flip_byte(dir: &Path, member: &str)` (reads the member, flips one byte, rewrites — provokes IntegrityMismatch), `pub fn delete_artifact(dir: &Path, member: &str)` (removes a member — provokes a missing-member / Parse path), `pub fn desync_lock_version(dir: &Path)` (rewrites BUNDLE.lock so its version disagrees with the manifest — provokes StampMismatch), and `pub fn add_unexpected_member(dir: &Path)` (writes an extra file NOT in the bundle allow-set — provokes UnexpectedMember, exercising the Codex MEDIUM #9 fail-closed membership policy from 92-01). Each returns/operates on a `LocalDirSource`-loadable directory so a downstream test does `let dir = copy_golden_to_temp(); flip_byte(dir.path(), "manifest.json"); assert!(matches!(BundleLoader::load(&LocalDirSource::new(dir.path())), Err(BundleLoadError::IntegrityMismatch{..})))`. Declare the module from tests/support/mod.rs. Add one smoke test per helper asserting it provokes the intended BundleLoadError variant.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --test fixture_byte_stability tamper 2>/dev/null; cargo test -p pmcp-server-toolkit tamper</automated>
  </verify>
  <acceptance_criteria>
    - `flip_byte` provokes `Err(BundleLoadError::IntegrityMismatch{..})` (smoke test passes)
    - `delete_artifact` provokes a load error (missing member), not a panic
    - `desync_lock_version` provokes `Err(BundleLoadError::StampMismatch{..})`
    - `add_unexpected_member` provokes `Err(BundleLoadError::UnexpectedMember{..})` (fail-closed membership, Codex MEDIUM #9)
    - No committed corrupt fixtures (`ls crates/pmcp-server-toolkit/tests/fixtures/` shows only the one tax-calc@1.1.0 golden)
  </acceptance_criteria>
  <done>Tamper helpers exist, each provoking a distinct fail-closed BundleLoadError (incl. UnexpectedMember), with no committed corrupt fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed golden bytes → BundleLoader | The golden is the test oracle; it must pass the same integrity gate real bundles do |
| generator non-determinism → byte-stability | Map ordering / float formatting could make regeneration non-reproducible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05 | Tampering | golden fixture drift vs runtime hasher | mitigate | The golden BUNDLE.lock is produced by the runtime build_bundle_lock and `golden_passes_boot_integrity` asserts the loader accepts it (Task 2) |
| T-92-06 | Repudiation | non-deterministic regeneration | mitigate | `golden_regeneration_is_byte_identical` is a CI check; the generator uses a fixed serde config and BTreeMap/canonical serialization, no unordered iteration (Tasks 1-2, Codex MEDIUM #8) |
| T-92-07 | Information Disclosure | customer data leak into the fixture | mitigate | D-01 hard constraint; grep-gate `ufh|quote|coil|towelrad|plot.?3` = 0 in fixtures + support (Tasks 1-3) |
| T-92-08 | Tampering | negative-path test using a committed corrupt fixture | accept→mitigate | D-05: tamper-at-test-time only; one golden committed, corruption happens in a tempdir copy (Task 3) |
</threat_model>

<verification>
- `cargo build -p pmcp-server-toolkit --features workbook` AND `--features workbook-embedded` compile against the skeleton (Codex HIGH #1)
- `cargo test -p pmcp-server-toolkit --test fixture_byte_stability` green (regeneration byte-identical + golden passes boot)
- Tamper smoke tests provoke the four distinct BundleLoadError variants (incl. UnexpectedMember)
- `grep -rEi "ufh|quote|coil|towelrad|plot.?3" crates/pmcp-server-toolkit/tests/` = 0 hits
</verification>

<success_criteria>
- The toolkit has an early workbook/workbook-embedded feature + compiling gated skeleton so wave-3/4 plans build under --features workbook
- A full-surface synthetic tax-calc@1.1.0 golden (with a real 1.0.0→1.1.0 changelog) is committed and byte-stable
- The golden passes BundleLoader's WBSV-08 boot gate
- Tamper helpers provoke IntegrityMismatch / StampMismatch / missing-member / UnexpectedMember fail-closed paths
- Zero customer identifiers anywhere in the fixture or generator
</success_criteria>

<output>
Create `.planning/phases/92-bundlesource-served-tool-toolkit-module/92-02-SUMMARY.md` when done
</output>
