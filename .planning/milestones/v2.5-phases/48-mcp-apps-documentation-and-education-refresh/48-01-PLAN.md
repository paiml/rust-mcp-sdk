---
phase: 48-mcp-apps-documentation-and-education-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mcp-tester/README.md
  - crates/mcp-preview/README.md
  - pmcp-book/src/ch12-5-mcp-apps.md
  - pmcp-book/src/ch15-testing.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "mcp-tester README documents the apps subcommand with usage examples for standard, chatgpt, and claude-desktop modes"
    - "mcp-tester README documents --strict, --tool, and --mode flags for apps validation"
    - "mcp-preview README describes multi-host preview with --mode chatgpt flag"
    - "mcp-preview README documents Protocol tab, Bridge diagnostics, and ChatGPT emulation features"
    - "pmcp-book ch12-5 teaches standard-first SDK APIs (with_host_layer, ToolInfo::with_ui, UIResource::html_mcp_app) before ChatGPT-specific topics"
    - "pmcp-book ch12-5 covers ext-apps App class, required protocol handlers, Vite bundling, and structuredContent"
    - "pmcp-book ch15 includes a brief App Metadata Testing subsection referencing mcp-tester apps and cargo pmcp test apps"
  artifacts:
    - path: "crates/mcp-tester/README.md"
      provides: "Complete mcp-tester documentation including apps subcommand"
      contains: "mcp-tester apps"
    - path: "crates/mcp-preview/README.md"
      provides: "Complete mcp-preview documentation including multi-host preview and DevTools"
      contains: "--mode chatgpt"
    - path: "pmcp-book/src/ch12-5-mcp-apps.md"
      provides: "Rewritten MCP Apps chapter using GUIDE.md as source of truth"
      contains: "with_host_layer"
    - path: "pmcp-book/src/ch15-testing.md"
      provides: "Testing chapter with App Metadata Testing subsection"
      contains: "cargo pmcp test apps"
  key_links:
    - from: "pmcp-book/src/ch12-5-mcp-apps.md"
      to: "src/server/mcp_apps/GUIDE.md"
      via: "content alignment"
      pattern: "with_host_layer|ToolInfo::with_ui|html_mcp_app|structuredContent"
    - from: "pmcp-book/src/ch15-testing.md"
      to: "pmcp-book/src/ch12-5-mcp-apps.md"
      via: "cross-reference"
      pattern: "Chapter 12\\.5|mcp-tester apps"
---

<objective>
Update tool READMEs and book chapters to reflect current MCP Apps capabilities.

Purpose: The mcp-tester README has zero mention of the `apps` subcommand (added Phase 47). The mcp-preview README is a 40-line stub missing `--mode chatgpt`, Protocol/Bridge tabs, and multi-host preview. The book chapter ch12-5 uses the old ChatGptAdapter-first paradigm and is missing with_host_layer(), ToolInfo::with_ui(), ext-apps App class, structuredContent, outputSchema, Claude Desktop support, and test apps tooling. All must be updated to reflect Phases 34-47 changes using GUIDE.md as the authoritative source.

Output: Updated mcp-tester README, mcp-preview README, ch12-5-mcp-apps.md, and ch15-testing.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-mcp-apps-documentation-and-education-refresh/48-RESEARCH.md

Source of truth files:
@src/server/mcp_apps/GUIDE.md
@crates/mcp-tester/src/main.rs (lines 190-206 for apps subcommand CLI)
@crates/mcp-tester/src/app_validator.rs (validation checks)
@cargo-pmcp/src/commands/test/apps.rs (cargo pmcp test apps)
@crates/mcp-preview/src/server.rs (PreviewMode, PreviewConfig)
@crates/mcp-preview/src/lib.rs (feature doc comments)

<interfaces>
<!-- Key types the executor needs for accurate documentation -->

From crates/mcp-tester/src/main.rs (lines 190-206):
```rust
/// Validate MCP App metadata compliance
Apps {
    /// Server URL
    url: String,
    /// Validation mode: standard, chatgpt, claude-desktop
    #[arg(long, default_value = "standard")]
    mode: String,
    /// Test specific tool only
    #[arg(long)]
    tool: Option<String>,
    /// Strict mode (promote warnings to failures)
    #[arg(long)]
    strict: bool,
},
```

From cargo-pmcp/src/commands/test/apps.rs:
```rust
// cargo pmcp test apps --url <URL> [--mode <MODE>] [--tool <NAME>] [--strict]
```

AppValidationMode values: Standard, ChatGpt, ClaudeDesktop

Validation checks per tool:
1. _meta field present
2. ui.resourceUri present (nested or flat)
3. resourceUri format valid
4. Resource cross-reference (tool URI matches resources/list)
5. Resource MIME type valid
6. [ChatGPT mode] openai/* descriptor keys
7. [ChatGPT mode] flat ui/resourceUri legacy key
8. [if present] outputSchema structure valid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mcp-tester and mcp-preview READMEs</name>
  <files>crates/mcp-tester/README.md, crates/mcp-preview/README.md</files>
  <action>
**mcp-tester README (DOCS-01):**

Add a new `#### apps - Validate MCP App Metadata` section after the existing `health` command section (before `#### scenario`). Include:

1. Command synopsis: `mcp-tester apps <URL> [OPTIONS]`
2. Options table: `--mode <MODE>` (standard|chatgpt|claude-desktop, default: standard), `--tool <NAME>` (test specific tool), `--strict` (promote warnings to failures)
3. Usage examples showing all three modes, strict mode, and single-tool validation (use examples from RESEARCH.md)
4. Brief description of what gets validated (8 checks from app_validator.rs -- list them)
5. Sample output showing a passing and a warning result

Also add `apps` to the Test Categories section at the bottom (new "### App Metadata Tests" subsection listing: _meta field, ui.resourceUri, MIME type, resource cross-reference, outputSchema).

Do NOT change any other sections of the README -- only add the new apps content.

**mcp-preview README (DOCS-02):**

Rewrite the entire README from ~40 lines to ~150-200 lines. Structure:
1. Title and one-line description
2. Features section (expand existing bullets + add: `--mode chatgpt` for host-specific emulation, Protocol diagnostics tab, Bridge diagnostics tab, window.openai ChatGPT API stub, connection lifecycle with status indicator, resource picker for multi-widget servers)
3. Installation section: `cargo install cargo-pmcp` then `cargo pmcp preview`
4. Usage section with all flags: `--url`, `--mode` (standard|chatgpt), `--open`, `--widgets-dir`, `--port`
5. DevTools Tabs section describing: Bridge tab (call logging, timing, expandable details), Protocol tab (metadata compliance checks), Bridge diagnostics tab (PostMessage traffic, handshake trace)
6. Bridge Modes table (keep existing proxy/WASM table)
7. Multi-Host Preview section explaining standard vs chatgpt mode
8. Architecture section (keep existing)

Read source code at `crates/mcp-preview/src/server.rs` and `crates/mcp-preview/src/lib.rs` for accurate flag names and feature descriptions. Do NOT invent features that don't exist in the source.
  </action>
  <verify>
    <automated>grep -c "mcp-tester apps" crates/mcp-tester/README.md && grep -c "\-\-mode chatgpt" crates/mcp-preview/README.md && test $(wc -l < crates/mcp-preview/README.md) -gt 100</automated>
  </verify>
  <done>mcp-tester README has apps subcommand section with all flags and examples. mcp-preview README expanded to 150+ lines covering multi-host preview, DevTools tabs, and all current features.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite book ch12-5 MCP Apps chapter and add ch15 testing subsection</name>
  <files>pmcp-book/src/ch12-5-mcp-apps.md, pmcp-book/src/ch15-testing.md</files>
  <action>
**ch12-5-mcp-apps.md (DOCS-03) -- Major rewrite:**

Use `src/server/mcp_apps/GUIDE.md` as the authoritative source. The existing chapter uses a ChatGptAdapter-first paradigm that is outdated. Rewrite with this structure:

1. **Introduction** -- MCP Apps Extension overview. Two parts: server side (Rust, PMCP SDK) and widget side (JS/TS, ext-apps SDK). Architecture diagram from GUIDE.md showing host -> tools/list -> _meta.ui.resourceUri, tools/call -> structuredContent, resources/read -> HTML.

2. **Feature flag** -- `pmcp = { version = "1.16", features = ["mcp-apps"] }` (use current version from Cargo.toml)

3. **Quick Start** -- Keep the `cargo pmcp app new` scaffold section from the existing chapter (it's still accurate), but update the widget code to use the ext-apps `App` class pattern from GUIDE.md instead of `window.mcpBridge`.

4. **Server Side (Rust)** -- Follow GUIDE.md sections exactly:
   - Enable host layer: `Server::builder().with_host_layer(HostType::ChatGpt)` -- explain what it does (enriches _meta with openai/* keys)
   - Register tools with UI metadata: `ToolInfo::with_ui()` -- NOT the old UIResourceBuilder
   - Return structuredContent: `CallToolResult::with_structured_content()` -- explain content vs structuredContent
   - Register widget as resource: `UIResource::html_mcp_app()` and `UIResourceContents::html()` -- explain the MIME type
   - CSP for external domains: `WidgetCSP` -- explain why Claude.ai blocks external resources
   - outputSchema (optional): `with_output_schema()` -- top-level on ToolInfo, not in annotations

5. **Widget Side (JavaScript)** -- Follow GUIDE.md sections:
   - Widget lifecycle: create App, register handlers, connect, read hostContext, callServerTool
   - Required protocol handlers: `onteardown`, `ontoolinput`, `ontoolcancelled`, `onerror` -- CRITICAL warning about Claude Desktop connection teardown
   - Capabilities: do NOT pass `tools` to `new App()` -- ChatGPT rejects it
   - Minimal widget example from GUIDE.md
   - React widget example from GUIDE.md
   - Loading external images: fetch-blob pattern from GUIDE.md

6. **Bundling with Vite** -- from GUIDE.md: vite-plugin-singlefile, why CDN imports fail, build order

7. **Developer Tooling** section:
   - `cargo pmcp preview`: standard and chatgpt modes, Protocol tab checks table from GUIDE.md
   - `mcp-tester apps` / `cargo pmcp test apps`: validation modes, flags, what gets checked
   - Common failures: widget shows then connection drops, widget loads but no results, dual-App conflict, missing openai keys, blocked images, widget not rendered

8. **Reference implementations** -- from GUIDE.md

CRITICAL ordering rule: `with_host_layer` and standard SDK APIs MUST appear BEFORE any ChatGPT-specific content. If "ChatGptAdapter" appears before "with_host_layer", the order is wrong.

Preserve the chapter title "Chapter 12.5: MCP Apps Extension -- Interactive UIs". Keep the writing style consistent with the existing book (explanatory, with code examples).

**ch15-testing.md (DOCS-03) -- Add subsection:**

Add a new subsection "## App Metadata Testing" near the end of the chapter (before any summary/conclusion section). Include:
1. Brief intro (2-3 sentences): MCP Apps servers need metadata validation beyond protocol compliance
2. CLI commands: `mcp-tester apps <url>` and `cargo pmcp test apps --url <url>`
3. Mode options: standard, chatgpt, claude-desktop
4. What gets validated (brief list -- 4-5 key checks)
5. Cross-reference: "For full MCP Apps development guide, see Chapter 12.5"
6. Keep this brief -- 3-5 paragraphs + one code example. The full docs belong in ch12-5.
  </action>
  <verify>
    <automated>grep -c "with_host_layer" pmcp-book/src/ch12-5-mcp-apps.md && grep -c "ToolInfo::with_ui" pmcp-book/src/ch12-5-mcp-apps.md && grep -c "ext-apps" pmcp-book/src/ch12-5-mcp-apps.md && grep -c "cargo pmcp test apps" pmcp-book/src/ch15-testing.md</automated>
  </verify>
  <done>ch12-5 rewritten with GUIDE.md content (with_host_layer, ToolInfo::with_ui, ext-apps App class, structuredContent, required handlers, Vite bundling, developer tooling). ch15 has App Metadata Testing subsection with cross-reference to ch12-5. mdbook build pmcp-book succeeds.</done>
</task>

</tasks>

<verification>
- `grep -c "mcp-tester apps" crates/mcp-tester/README.md` returns >= 3
- `grep -c "with_host_layer" pmcp-book/src/ch12-5-mcp-apps.md` returns >= 3
- `grep -c "ext-apps" pmcp-book/src/ch12-5-mcp-apps.md` returns >= 2
- `grep -c "onteardown" pmcp-book/src/ch12-5-mcp-apps.md` returns >= 1
- `grep -c "cargo pmcp test apps" pmcp-book/src/ch15-testing.md` returns >= 1
- `test $(wc -l < crates/mcp-preview/README.md) -gt 100` passes
- `mdbook build pmcp-book 2>&1 | tail -5` shows no errors (if mdbook available)
</verification>

<success_criteria>
1. mcp-tester README has complete apps subcommand documentation with usage examples for all three modes
2. mcp-preview README expanded from 40 to 150+ lines covering all current features
3. pmcp-book ch12-5 rewritten using GUIDE.md as source, standard-first ordering, ext-apps App class
4. pmcp-book ch15 has App Metadata Testing subsection
5. No mention of deprecated patterns (ChatGptAdapter-first, text/html+mcp as primary, UIResourceBuilder, window.mcpBridge as primary widget API)
</success_criteria>

<output>
After completion, create `.planning/phases/48-mcp-apps-documentation-and-education-refresh/48-01-SUMMARY.md`
</output>
