---
phase: 115-json-schema-2020-12-structured-output-caching-hints
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/mcp-protocol-sdk-v1.yaml
  - contracts/binding.yaml
  - tests/phase115_contract_bindings.rs
autonomous: true
requirements: [SCHM-01, SCHM-02, SCHM-03]

commit_policy: |
  This plan changes ZERO production bytes (`contracts/` + one new test file only).
  Per the phase commit policy it runs the SCOPED pre-commit gate
  (`make fmt-check`, `make lint`, `make check-todos`, plus its own suite) rather
  than the full `make quality-gate`, and asserts `git diff --stat -- src/ Cargo.toml`
  is empty. The full `make quality-gate` runs once for the phase in 115-10.

must_haves:
  truths:
    - "The three behaviours this phase ships are written as provable-contract equations BEFORE any production code implements them"
    - "Every Phase 115 contract binding resolves to a function that actually exists in src/, and a binding pointing at a non-existent symbol fails a named test"
    - "The contract-location deviation (CLAUDE.md names ../provable-contracts/, which does not exist in this checkout; contracts live in-repo at contracts/) is recorded rather than silently worked around"
    - "pmat comply check --path . was run before and after the contract change and both transcripts are recorded"
  artifacts:
    - path: "contracts/mcp-protocol-sdk-v1.yaml"
      provides: "Three new equations: output_schema_draft_pin, structured_content_shape, result_caching_hints"
      contains: "result_caching_hints"
    - path: "contracts/binding.yaml"
      provides: "Bindings from the three new equations to the concrete pmcp functions this phase writes"
      contains: "output_schema_draft_pin"
    - path: "tests/phase115_contract_bindings.rs"
      provides: "Deterministic, pmat-independent binding-drift gate for the Phase 115 equations"
      min_lines: 120
  key_links:
    - from: "contracts/binding.yaml"
      to: "src/server/output_validation.rs, src/types/caching.rs, src/types/tools.rs"
      via: "function: entries resolved against source by tests/phase115_contract_bindings.rs"
      pattern: "output_schema_draft_pin"
---

<objective>
Write the contracts BEFORE the code. Add three equations to `contracts/mcp-protocol-sdk-v1.yaml`
covering exactly what SCHM-01/02/03 change, bind them to the functions the later plans will write,
and fence the bindings with a deterministic source-resolution test so contract coverage is
non-vacuous.

Purpose: CLAUDE.md § "Contract-First Development" makes this mandatory and the existing plan set
omitted it entirely — the cross-AI review's blocking finding #7. CLAUDE.md names
`../provable-contracts/contracts/<crate>/` as the location; **that directory does not exist in this
checkout** (`ls ../provable-contracts` → `No such file or directory`). The contracts this repo
actually uses live IN-REPO at `contracts/`: `contracts/mcp-protocol-sdk-v1.yaml` (452-line
`equations:` map, `target_crate: pmcp`) and `contracts/binding.yaml` (equation → function bindings).
This plan uses the in-repo location and RECORDS the deviation rather than creating a sibling
repository on inference.

Why the binding test exists: `make comply`'s `comply-bindings-check` (`Makefile:818`) resolves only
`contracts/team-servers/binding.yaml`. Nothing today resolves `contracts/binding.yaml`, so a Phase
115 binding could name a function that never gets written and every gate would stay green. This plan
adds the missing resolver for exactly the Phase 115 equations.

**Because this is wave 1 and the functions do not exist yet**, every Phase 115 binding lands with
`status: planned`. 115-10 flips them to `status: implemented` once the implementation plans land, and
the binding test asserts the two states honestly: `planned` bindings MAY be unresolved, `implemented`
bindings MUST resolve.

Output: three equations, their bindings, and `tests/phase115_contract_bindings.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
@contracts/mcp-protocol-sdk-v1.yaml
@contracts/binding.yaml

<interfaces>
<!-- The in-repo contract format, measured this session. -->

contracts/mcp-protocol-sdk-v1.yaml
  metadata: { version, created, author, description, references }
  equations:
    <equation_name>:
      formula: |            # multi-line pseudo-signature block
      domain: <string>
      codomain: <string>
      invariants: [<string>, ...]
      preconditions: [<string>, ...]
      postconditions: [<string>, ...]
      lean_theorem: Theorems.<CamelCase>      # optional; existing entries all carry one
  # then `falsification_tests:` (FALSIFY-PMCP-NNN), `kani_harnesses:` (KANI-PMCP-NNN)
  # and a trailing `id: F-PMCP-001` block at :401

  31 existing equations, e.g. `jsonrpc_framing`, `protocol_version_negotiation`,
  `session_lifecycle`, `tool_dispatch_integrity`, `payload_limits`, `error_code_mapping`.

contracts/binding.yaml
  version: 1.0.0
  target_crate: pmcp
  bindings:
  - contract: mcp-protocol-sdk-v1.yaml
    equation: <equation_name>
    function: <Type::method | free_fn | CONST_NAME>
    module_path: pmcp::<path>
    signature: '<the exact Rust signature>'
    status: implemented
    notes: <one or more lines, `>-` folded for long text>

The deterministic resolver idiom to mirror (Makefile:818-836, `comply-bindings-check`):
  for each `function:` in the binding file, `grep -rqE "fn <name>\b" <crate src dir>`;
  a `function:` that resolves to nothing is a GHOST BINDING and fails the gate.
</interfaces>

<measured_deviation>
<!-- Verified this session, at HEAD of fix/mcp-publisher-oidc-audience. -->
  ls ../provable-contracts          → No such file or directory
  ls contracts/                     → binding.yaml, mcp-protocol-sdk-v1.yaml,
                                      team-servers-v1.yaml, team-servers/
  Makefile comply-bindings-check    → resolves ONLY contracts/team-servers/binding.yaml
  make comply                       → `pmat comply check --path .` is INFORMATIONAL
                                      (`|| echo …`, CLAUDE.md D-07); only the
                                      team-servers binding drift is gate-blocking
  pmat --version                    → 3.15.0 (present on this machine)
</measured_deviation>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-change compliance baseline and add the three equations</name>
  <files>contracts/mcp-protocol-sdk-v1.yaml</files>
  <read_first>
    - contracts/mcp-protocol-sdk-v1.yaml — read lines 1-120 in full for the `metadata:` block and
      the `jsonrpc_framing` / `protocol_version_negotiation` / `session_lifecycle` equation shape
      (formula / domain / codomain / invariants / preconditions / postconditions / lean_theorem),
      then lines 280-413 for the `falsification_tests:` / `kani_harnesses:` sections you must NOT
      disturb
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
      § D-01, D-02, D-04, D-06, D-07, D-08, D-09, D-11, D-12 — the equations restate these decisions
      as invariants, so read them before writing prose that contradicts one
    - ./CLAUDE.md § "Contract-First Development" — the four mandated steps
  </read_first>
  <action>
FIRST, before editing anything, capture the pre-change compliance baseline:
run `pmat comply check --path . 2>&1 | tail -40` and save the transcript verbatim for the SUMMARY.
Its exit status is EXPECTED to be non-zero — `Makefile:797-808` documents that this repo is
intentionally mid-migration at the project level and that the holistic exit is informational
(CLAUDE.md D-07). Record the exit code and the report; do NOT treat non-zero as a blocker, and do NOT
change any Makefile or CI file to make it zero.

Then add exactly THREE equations to the `equations:` map in `contracts/mcp-protocol-sdk-v1.yaml`,
in the file's existing shape (two-space indent under `equations:`, keys in the order
`formula`, `domain`, `codomain`, `invariants`, `preconditions`, `postconditions`, `lean_theorem`).
Place them after the last existing equation in the map, before the `falsification_tests:` section.

**1. `output_schema_draft_pin`** (SCHM-01, D-01/D-02/D-03)
  formula: `validate: (era, outputSchema, structuredContent) -> Option<mismatch>` with the two era
  arms spelled out — `Era::V1 => jsonschema::validator_for(schema)` (auto-detect, unchanged) and
  `Era::V2 => draft202012::new(normalize_schema_dialect(schema))`.
  invariants, each a single checkable sentence:
    - "On Era::V2 the compiled validator enforces the schema's keywords even when the document
      declares a legacy `$schema` (draft-04/06/07); a legacy declaration is normalized to the
      2020-12 URI before compilation, never honoured and never used as a vocabulary source"
    - "On Era::V1 the verdict for any (schema, instance) pair is byte-identical to the pre-Phase-115
      verdict"
    - "The validator cache key is `(Era, canonical schema text)`, so one era's validator is never
      served to the other"
    - "An external `$ref` (`http://`, `https://`, `file://`, or a relative ref under an
      http-scheme `$id`) fails to COMPILE on both eras and performs zero network or filesystem I/O"
    - "`normalize_schema_dialect` is idempotent and alters only the root `$schema` key"
    - "Validation is warn-only on both eras: a mismatch never produces an error result and never
      panics"
  preconditions: "`schema` and `value` are already-parsed `serde_json::Value`s"
  postconditions: "`schema_mismatch` returns `Some(message)` on mismatch or invalid schema, `None`
  on conformance", "the returned message names `outputSchema` when the schema itself failed to
  compile"
  lean_theorem: `Theorems.OutputSchemaDraftPin`

**2. `structured_content_shape`** (SCHM-02, D-04/D-05/D-06)
  formula: `CallToolResult.structuredContent : Option<Value>` — any JSON value on v2; the v1 wire
  frozen.
  invariants:
    - "`structuredContent` accepts any JSON value — object, array, string, number, boolean or null"
    - "`Some(Value::Null)` serializes as `\"structuredContent\": null` and is NOT elided by
      `skip_serializing_if = \"Option::is_none\"`"
    - "`CallToolResult::structured`'s signature and behaviour are unchanged; the widening is the
      sibling constructor `structured_value`"
    - "A declared `outputSchema` still applies to a non-object payload (D-04); enforcement is
      warn-only"
    - "pmcp's pre-existing v1 permissiveness is FROZEN, not corrected — tightening v1 would itself
      be a v1 wire change (D-05)"
  lean_theorem: `Theorems.StructuredContentShape`

**3. `result_caching_hints`** (SCHM-03, D-07/D-08/D-09/D-11/D-12)
  formula: `project: (Value, Option<Era>, Cacheable) -> Value` with three arms —
  `Cacheable::No => identity`, `Some(Era::V2) => ensure(ttlMs, cacheScope)`,
  `otherwise => strip(ttlMs, cacheScope)`.
  invariants:
    - "Exactly six result types are `CacheableResult`: `DiscoverResult`, `ListToolsResult`,
      `ListResourcesResult`, `ListResourceTemplatesResult`, `ReadResourceResult`, `ListPromptsResult`"
    - "On Era::V2 a cacheable result carries both `ttlMs` and `cacheScope`; absent handler intent the
      values are `0` and `\"private\"`"
    - "A handler-set hint survives the v2 projection unmodified"
    - "No v1 response carries `ttlMs` or `cacheScope`, EVEN WHEN a handler set them — the projection
      strips on v1, and on a `None` protocol context, and on every dispatcher including
      `WasmMcpServer`"
    - "`cacheScope` is the closed union `\"public\" | \"private\"`; an unknown value fails to
      deserialize"
    - "The default is `cacheScope: \"private\"` — `\"public\"` authorizes reuse ACROSS authorization
      contexts and defaulting to it would be a cross-caller data leak"
    - "The hints are written in exactly ONE function; no result type projects independently (D-12)"
  lean_theorem: `Theorems.ResultCachingHints`

Do NOT add `falsification_tests:` or `kani_harnesses:` entries — those sections carry numbered IDs
(`FALSIFY-PMCP-001`..`007`, `KANI-PMCP-001`..`005`) and appending to them without a numbering audit is
the ID-collision defect Phase 114's ledger records. Note the omission in the SUMMARY as a deliberate
scope choice.
  </action>
  <verify>
    <automated>python3 -c "import sys,json;import subprocess" && python3 -c "
import re,sys
t=open('contracts/mcp-protocol-sdk-v1.yaml').read()
for k in ['output_schema_draft_pin','structured_content_shape','result_caching_hints']:
    assert re.search(r'^  '+k+r':$', t, re.M), k
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/mcp-protocol-sdk-v1.yaml` contains the three literal equation keys `output_schema_draft_pin:`, `structured_content_shape:` and `result_caching_hints:`, each at exactly two-space indent
    - Each new equation carries all of `formula:`, `domain:`, `codomain:`, `invariants:`, `preconditions:`, `postconditions:` and `lean_theorem:`
    - `grep -c 'FALSIFY-PMCP-' contracts/mcp-protocol-sdk-v1.yaml` is unchanged from its pre-change value (record both), and `grep -c 'KANI-PMCP-' contracts/mcp-protocol-sdk-v1.yaml` likewise
    - The file still parses: `python3 -c "import yaml,sys; yaml.safe_load(open('contracts/mcp-protocol-sdk-v1.yaml'))"` exits 0 (if PyYAML is absent, use `pmat comply check --path .` reaching the file without a parse error and say so)
    - `contracts/mcp-protocol-sdk-v1.yaml` contains the literal string `across authorization contexts` inside the `result_caching_hints` invariants
    - The pre-change `pmat comply check --path .` transcript and exit code are recorded in the SUMMARY
    - `git diff --stat -- src/ Cargo.toml` is EMPTY
  </acceptance_criteria>
  <done>The three behaviours this phase ships exist as written contract equations before any production code implements them, and the pre-change compliance baseline is on the record.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the equations to the functions the later plans will write</name>
  <files>contracts/binding.yaml</files>
  <read_first>
    - contracts/binding.yaml — read lines 1-60 for the entry shape and the `# === Section ===`
      comment convention, and the last 30 lines to find where to append
    - src/server/output_validation.rs — the CURRENT signatures of `warn_on_schema_mismatch`,
      `schema_mismatch` and `cached_validator`, so the recorded `signature:` values match what
      115-03 will actually write
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-03-PLAN.md
      § Task 2 — the exact post-change signatures (era threaded as the LAST parameter)
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-05-PLAN.md
      § Task 1 and Task 2 — `CacheScope`, `DEFAULT_TTL_MS`, `Cacheable`, `project_caching_hints`
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-06-PLAN.md
      § Task 1 and Task 2 — `inject_v2_result_envelope`, `request_is_cacheable`
  </read_first>
  <action>
Append a `# === JSON Schema 2020-12 + Caching Hints (Phase 115) ===` section to `contracts/binding.yaml`
with one entry per function below, each in the existing five-key shape
(`contract`, `equation`, `function`, `module_path`, `signature`, `status`, `notes`).

**Every Phase 115 entry lands with `status: planned`, not `implemented`** — this is wave 1 and none of
these functions exists yet. Write the `notes:` of each entry to name the plan that will write it, so a
reader who hits an unresolved binding knows whether it is drift or a not-yet-landed plan.

`equation: output_schema_draft_pin` (written by 115-03):
  - `warn_on_schema_mismatch` — `pmcp::server::output_validation` —
    `pub(crate) fn warn_on_schema_mismatch(tool: &str, schema: &Value, value: &Value, era: Option<Era>)`
  - `schema_mismatch` — `pmcp::server::output_validation` —
    `pub(crate) fn schema_mismatch(schema: &Value, value: &Value, era: Option<Era>) -> Option<String>`
  - `cached_validator` — `pmcp::server::output_validation` —
    `fn cached_validator(era: Option<Era>, schema: &Value) -> Result<Arc<jsonschema::Validator>, Arc<str>>`
  - `normalize_schema_dialect` — `pmcp::server::output_validation` —
    `fn normalize_schema_dialect(schema: &Value) -> Cow<'_, Value>`
  - `compile_2020_12` — `pmcp::server::output_validation` —
    `fn compile_2020_12(schema: &Value) -> Result<jsonschema::Validator, jsonschema::ValidationError<'static>>`

`equation: structured_content_shape` (written by 115-04):
  - `CallToolResult::structured_value` — `pmcp::types::tools` —
    `pub fn structured_value(value: Value) -> Self`
  - `CallToolResult::structured` — `pmcp::types::tools` —
    `pub fn structured(value: Value) -> Self` — `status: implemented` (this one EXISTS today; its
    binding records the D-06 freeze, and the notes must say the contract obligation is that its
    signature does NOT change)

`equation: result_caching_hints` (written by 115-05 / 115-06):
  - `CacheScope` — `pmcp::types::caching` — `pub enum CacheScope { Public, Private }`
  - `DEFAULT_TTL_MS` — `pmcp::types::caching` — `pub const DEFAULT_TTL_MS: u64`
  - `project_caching_hints` — `pmcp::types::caching` —
    `pub(crate) fn project_caching_hints(value: &mut Value, era: Option<Era>, cacheable: Cacheable)`
  - `Cacheable` — `pmcp::types::caching` — `pub(crate) enum Cacheable { Yes, No }`
  - `inject_v2_result_envelope` — `pmcp::server::core` —
    `pub(crate) fn inject_v2_result_envelope(response: &mut JSONRPCResponse, protocol_context: Option<&ProtocolContext>, server_info: &Implementation, disposition: ResponseDisposition, owner: ReservedFieldOwner, cacheable: Cacheable)`
  - `request_is_cacheable` — `pmcp::server::core` —
    `pub(crate) fn request_is_cacheable(request: &Request) -> Cacheable`

Do NOT modify any pre-existing binding entry other than adding the new section. If a signature you
record here turns out to differ from what the implementing plan writes, that is a FINDING for 115-10 —
the binding test in Task 3 resolves by FUNCTION NAME, so a signature drift is caught by review rather
than by the gate, and 115-10's booking must reconcile it.
  </action>
  <verify>
    <automated>python3 -c "
import re
t=open('contracts/binding.yaml').read()
for k in ['output_schema_draft_pin','structured_content_shape','result_caching_hints']:
    assert 'equation: '+k in t, k
assert t.count('status: planned') >= 12, t.count('status: planned')
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'equation: output_schema_draft_pin' contracts/binding.yaml` returns 5
    - `grep -c 'equation: structured_content_shape' contracts/binding.yaml` returns 2
    - `grep -c 'equation: result_caching_hints' contracts/binding.yaml` returns 6
    - `grep -c 'status: planned' contracts/binding.yaml` returns at least 12
    - `contracts/binding.yaml` contains `normalize_schema_dialect`, `project_caching_hints` and `request_is_cacheable`
    - Every new entry's `notes:` names the owning plan number (`115-03`, `115-04`, `115-05` or `115-06`)
    - `git diff contracts/binding.yaml | grep '^-' | grep -vc '^---'` returns 0 — the section was APPENDED, no pre-existing entry was removed or altered
    - `git diff --stat -- src/ Cargo.toml` is EMPTY
  </acceptance_criteria>
  <done>Every function Phase 115 will write is bound to an equation before it is written, marked `planned`, and attributed to the plan that owns it.</done>
</task>

<task type="auto">
  <name>Task 3: Make the Phase 115 contract coverage non-vacuous</name>
  <files>tests/phase115_contract_bindings.rs</files>
  <read_first>
    - Makefile lines 818-836 — `comply-bindings-check`, the deterministic source-resolution idiom
      this test mirrors in Rust (and the rationale comment at :795-817 explaining why the
      deterministic gate exists ALONGSIDE the informational `pmat comply` report)
    - contracts/binding.yaml — the file this test parses
    - tests/vendored_schema_provenance.rs lines 76-130 — `repo_root`, `rel`, and the
      "read at RUNTIME, not `include_str!`" reasoning for integration tests
    - tests/v2_tasks_tripwires.rs lines 82-152 — the anti-vacuity doctrine (`files.len() > 50`)
      and the `FAILURE MODE n:` / `WHAT TO DO:` failure-message voice
  </read_first>
  <action>
Create `tests/phase115_contract_bindings.rs`. It is the missing resolver: nothing in this repo today
checks that `contracts/binding.yaml` names real functions, so a Phase 115 binding could point at a
function that never gets written and every gate would stay green.

Do NOT add a YAML dependency. Parse `contracts/binding.yaml` with a small line-oriented reader
restricted to the flat two-space `- contract:` / `  equation:` / `  function:` / `  module_path:` /
`  status:` shape the file actually uses: accumulate a record on each `- contract:` line and close it
on the next one or at EOF. Read the file at RUNTIME from `env!("CARGO_MANIFEST_DIR")`, never
`include_str!`, so a binding edit moves the assertions without a rebuild.

Prefix EVERY test function with the file stem `phase115_contract_bindings_` so both
`binary(phase115_contract_bindings)` and `test(/phase115_contract_bindings/)` select it — a
`test(/stem/)` selector against a file whose test names lack the stem selects ZERO tests and exits 0
(115-RESEARCH § Pitfall 4).

Tests:

1. `phase115_contract_bindings_every_implemented_binding_resolves_to_real_source` — for every parsed
   binding whose `status:` is `implemented`, the `function:` value must resolve in the tree. Resolve
   by taking the LAST `::`-separated segment of `function:` and searching the source root implied by
   `module_path:` (`pmcp::` → `src/`) for any of: `fn <name>`, `enum <name>`, `struct <name>`,
   `const <name>`, `type <name>`. A binding that resolves to nothing is a GHOST BINDING — fail naming
   the equation, the function, the module path and the searched root, in the
   `FAILURE MODE:` / `WHAT TO DO:` voice.

2. `phase115_contract_bindings_planned_entries_are_scoped_to_phase_115` — every binding with
   `status: planned` must belong to one of the three Phase 115 equations. A `planned` entry on any
   OTHER equation means someone used `planned` to silence this gate for unrelated drift; fail naming
   it. This is what stops `planned` from becoming a universal escape hatch.

3. `phase115_contract_bindings_the_three_phase_115_equations_are_bound` — the parsed set contains at
   least 5 bindings for `output_schema_draft_pin`, at least 2 for `structured_content_shape` and at
   least 6 for `result_caching_hints`, and every one of them names a `module_path:` beginning
   `pmcp::`. Without this, a truncated or mis-parsed file passes tests 1 and 2 over nothing.

4. `phase115_contract_bindings_every_bound_equation_exists_in_the_contract` — read
   `contracts/mcp-protocol-sdk-v1.yaml` at runtime and assert every distinct `equation:` value in
   `binding.yaml` appears as a two-space-indented `<name>:` key under `equations:`. A binding to a
   non-existent equation is the mirror-image ghost and is equally silent today.

5. `phase115_contract_bindings_the_parse_is_not_vacuous` — assert the parser produced more than 40
   binding records (the file carries 31 distinct equations today across many more entries), that at
   least one has `status: implemented`, and that every record has a non-empty `function:` and
   `module_path:`. A parser that silently reads nothing must fail HERE, not pass everywhere else.

Every failure message names the remedy explicitly: fix the code or the binding, never delete the
assertion, and never flip `implemented` back to `planned` to make the gate pass.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'binary(phase115_contract_bindings)'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full -E 'binary(phase115_contract_bindings)'` exits 0 and its Summary line reports exactly 5 tests run (zero-selection is a FAILURE, not a pass)
    - Every test function name in `tests/phase115_contract_bindings.rs` begins with `phase115_contract_bindings_`
    - `grep -c 'include_str!' tests/phase115_contract_bindings.rs` returns 0
    - Negative control A: temporarily change one `status: planned` Phase 115 entry to `status: implemented`; test 1 MUST fail naming that function as a ghost binding (the function does not exist yet in wave 1); revert
    - Negative control B: temporarily add a binding with `equation: no_such_equation`; test 4 MUST fail naming it; revert
    - Negative control C: temporarily add `status: planned` to a pre-existing non-Phase-115 binding; test 2 MUST fail naming it; revert
    - Both observed failure messages from A and B are transcribed into the SUMMARY
    - `cargo fmt --all -- --check`, `make lint` and `make check-todos` all exit 0
    - `git diff --stat -- src/ Cargo.toml` is EMPTY — this plan changes zero production bytes
    - Post-change `pmat comply check --path . 2>&1 | tail -40` transcript and exit code are recorded and compared against the Task 1 baseline; a NEW class of advisory attributable to the three added equations is a finding to report, not to suppress
  </acceptance_criteria>
  <done>A Phase 115 binding that names a function nobody wrote fails a named test, a binding to a non-existent equation fails a named test, and `planned` cannot be used to silence unrelated drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract record → future reader | A contract equation is a claim about behaviour that a reader will trust without re-deriving |
| binding record → source tree | A binding asserts a named function implements an equation; an unresolved binding is a silent lie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-30 | Repudiation | `contracts/binding.yaml` | mitigate | Nothing in this repo resolved `contracts/binding.yaml` before this plan (`comply-bindings-check` covers only `contracts/team-servers/binding.yaml`, `Makefile:818`), and `make comply` treats `pmat comply` failure as informational. `tests/phase115_contract_bindings.rs` adds the deterministic resolver, with three negative controls proving it fires. |
| T-115-31 | Repudiation | `status: planned` | mitigate | `planned` is scoped by test 2 to the three Phase 115 equations only, so it cannot become a universal escape hatch for unrelated binding drift. 115-10 flips the Phase 115 entries to `implemented`, at which point test 1 becomes load-bearing for them. |
| T-115-32 | Repudiation | contract location deviation | mitigate | CLAUDE.md names `../provable-contracts/`, which does not exist in this checkout. This plan uses the in-repo `contracts/` tree, states the measurement in `<measured_deviation>`, and 115-10 books the deviation in the deferred-items ledger rather than leaving a future reader to rediscover it. |
| T-115-SC | Tampering | package installs | mitigate | This plan installs NO package, adds NO dependency (the binding file is parsed with a line reader rather than a YAML crate, deliberately), and touches no manifest. `115-RESEARCH.md` § Package Legitimacy Audit shows zero `[ASSUMED]`/`[SUS]`/`[SLOP]` entries. |
</threat_model>

<verification>
- `cargo nextest run --features full -E 'binary(phase115_contract_bindings)'` — green, exactly 5 tests
- All three negative controls (A, B, C) observed to fail the intended test, then reverted
- `git diff --stat -- src/ Cargo.toml` is EMPTY after the controls are reverted
- `cargo fmt --all -- --check`, `make lint`, `make check-todos` — all green
- `pmat comply check --path .` transcripts captured pre- and post-change and compared
</verification>

<success_criteria>
- Three equations covering SCHM-01/02/03 exist in `contracts/mcp-protocol-sdk-v1.yaml` BEFORE any production code implements them
- Thirteen bindings exist, twelve `planned` and one `implemented`, each attributed to its owning plan
- A ghost binding, a binding to a non-existent equation, and an out-of-scope `planned` all fail a named test
- The `../provable-contracts` vs in-repo `contracts/` deviation is measured and recorded
- Zero production bytes changed
</success_criteria>

<output>
Create `.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-11-SUMMARY.md` when done.
Record in it: the pre- and post-change `pmat comply check --path .` transcripts and exit codes, the
observed failure messages from the three negative controls, the final binding count per equation, and
the `../provable-contracts` deviation as a candidate deferred item for 115-10's ledger.
</output>
