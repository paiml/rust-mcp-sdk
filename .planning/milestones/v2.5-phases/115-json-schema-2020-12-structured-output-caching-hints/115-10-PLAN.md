---
phase: 115-json-schema-2020-12-structured-output-caching-hints
plan: 10
type: execute
wave: 6
depends_on: ["115-01", "115-02", "115-03", "115-04", "115-05", "115-06", "115-07", "115-08", "115-09", "115-11"]
files_modified:
  - src/server/output_validation.rs
  - src/types/tools.rs
  - README.md
  - pmcp-book/src
  - schema/vendored/ext-tasks/PROVENANCE.md
  - contracts/binding.yaml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md
autonomous: false
requirements: [SCHM-01, SCHM-02, SCHM-03]

commit_policy: |
  This plan may touch PRODUCTION rustdoc during the stale-doc sweep, so the sweep
  runs BEFORE the whole-phase gate (Task 1 before Task 2) rather than after it —
  the ordering defect the cross-AI review found. Task 2 runs the FULL
  `make quality-gate` over the swept tree. Completion markers (ROADMAP ticks,
  STATE position) are applied ONLY in Task 3, AFTER owner approval, so a rejected
  sign-off never leaves the repository recording a complete phase.

must_haves:
  truths:
    - "Every in-repo statement this phase falsified is corrected BEFORE the whole-phase gate runs, so the gate covers the final tree"
    - "make quality-gate is green across the whole workspace with every Phase 115 change in the tree, measured as deltas against a recorded phase base"
    - "The three SCHM requirements are booked with a verdict that cites measured evidence, not an inherited habit"
    - "Every deviation this phase took from its own requirement text is written down where a reader will hit it"
    - "Every out-of-scope discovery names an owner or says explicitly that it is unowned"
    - "The phase is signed off by the owner rather than self-approved, and no completion marker exists on disk before that approval"
  artifacts:
    - path: ".planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md"
      provides: "The phase's ledger of out-of-scope discoveries, each with an owner or an explicit unowned marker"
      min_lines: 120
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCHM-01/02/03 booked with cited evidence"
      contains: "SCHM-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "schema/vendored/core-2026-07-28/PROVENANCE.md"
      via: "the booking cites the pinned artifact as its published evidence"
      pattern: "core-2026-07-28"
---

<objective>
Sweep the stale docs, run the whole-phase gate over the swept tree, book the three SCHM requirements
on measured evidence, write down every deviation and every unowned discovery, and take a real
sign-off — applying completion markers only after that sign-off.

Purpose: D-15 states the phase's booking target plainly — *"Phase 115 has NO publication hold and
must not inherit a `[~]` booking from Phase 114 by habit."* The contingency D-15 kept available (the
Phase-113 HTTP-04 split) **did not fire**: `CacheableResult` is in the published core schema, and
115-01 vendored it at a pinned commit with digests. So this plan books `[x]`, and it books it by
CITING that artifact rather than by assertion.

**Two ordering defects the cross-AI review found, both fixed here.** The pre-review version ran the
whole-phase gate FIRST and then edited rustdoc, README, book text, provenance and planning state —
files absent from its own header — with no gate rerun; and it flipped requirement and roadmap
completion markers BEFORE the human checkpoint, so a rejected sign-off would leave the repository
recording a complete phase. This version sweeps first, gates second, and applies completion markers
only after approval.

Deviations this phase took, all of which must be recorded rather than buried:
1. `jsonschema` shipped at **0.49**, while SCHM-01's text says 0.48.
2. **SIX** result types carry caching hints, while SCHM-03's text and CONTEXT.md both say five —
   `DiscoverResult extends CacheableResult` in the pinned schema.
3. `server/discover` is included for that same reason, so a v2 client's FIRST call is conformant.
4. Contracts live IN-REPO at `contracts/`, not at the `../provable-contracts/` path CLAUDE.md names,
   because that directory does not exist in this checkout.

Output: a swept tree, a green gate, booked requirements, a deferred-items ledger, an owner sign-off,
and — only then — an updated ROADMAP and STATE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/114-tasks-extension-migration/deferred-items.md

<interfaces>
Gate targets (Makefile):
  quality-gate       :673  = fmt-check → lint → build → test-all → pmcp-package-gate → audit →
                             unused-deps → check-todos → check-unwraps → validate-always →
                             purity-check → comply
  test-all           :361  = test-unit test-doc test-property test-examples test-integration
  test-feature-flags :303
  wasm-build         :59   = `--no-default-features --features wasm` — does NOT cover `validation`
  validate-always    :366  = test-fuzz → test-property → test-examples   ← ALL THREE FAIL-OPEN

⚠ MEASURED fail-open gates (2026-08-01 replan). A green `validate-always` proves nothing:
  Makefile:236-244  test-fuzz      `… || echo "… completed"` — every non-zero fuzz exit becomes success
  Makefile:230-233  test-property  `-- --ignored property_` — selects only `#[ignore]`d tests, and
                                   `tests/property_tests.rs` has 13 `property_*` functions and ZERO
                                   `#[ignore]` attributes, so it selects NOTHING today
  Makefile:248+     test-examples  prints "⚠ … (skipped)" and continues on a build failure
This plan therefore re-runs the phase's real evidence BY NAME rather than trusting `validate-always`.

Requirement lines to book — .planning/REQUIREMENTS.md:143-145, currently all `[ ]`:
  SCHM-01  Schema validation runs Draft 2020-12 explicitly pinned (jsonschema 0.48, no `$schema`
           auto-detect), staying wasm-clean and SEP-2106-compliant (no external `$ref` dereference)
  SCHM-02  On v2, `structuredContent` accepts any JSON value (scalar/array/null/object); v1-negotiated
           tools keep the existing object-shaped behavior
  SCHM-03  The five list/read results carry `ttlMs`/`cacheScope` caching hints (additive fields)

Booking-format precedent: the TASK block at REQUIREMENTS.md:118-139 — a `[x]`/`[~]` marker, then an
indented block quote carrying the measured evidence and any judgement the booking makes EXPLICITLY
rather than absorbing.

Ledger-format precedent: `.planning/phases/114-tasks-extension-migration/deferred-items.md` — one
`## D-115-<letter>` heading per item, each naming filer, subject, owner (or **unowned**), and why it
was not fixed. ⚠ That file records an ID-collision incident: three plans appended without checking
for the next free letter, which defeated the point of an ID. Run `grep -c "^## D-115-"` before
assigning one.

ROADMAP already carries a planning-deviation note under Phase 115's Success Criteria (recorded
2026-07-31) covering the five-vs-six and 0.48-vs-0.49 deviations. VERIFY it against what actually
shipped rather than assuming it is still accurate, and correct it if the phase diverged further.
</interfaces>

<deviations_and_discoveries_to_record>
<!-- Assembled from the eleven plans, including findings the 2026-08-01 `--reviews` replan added.
     VERIFY each against the landed SUMMARYs rather than copying — a SUMMARY that contradicts a row
     here is itself a finding and must be reported. -->

Deviations from requirement text:
  jsonschema 0.49 vs SCHM-01's literal "0.48"             — 115-03 Task 1
  SIX cacheable results vs SCHM-03's "five"               — 115-01 Task 3, 115-05, 115-06, 115-07
  `server/discover` included as the sixth                 — 115-06 Task 2, 115-07 Task 1
  Exact version pin `=0.49.2` DECLINED for a published library crate — 115-03 Task 1

Contract-workflow deviations:
  CLAUDE.md names `../provable-contracts/contracts/<crate>/`; that directory does NOT exist in this
      checkout. Contracts live in-repo at `contracts/` (`mcp-protocol-sdk-v1.yaml` +
      `binding.yaml`). 115-11 used the in-repo location and recorded the deviation
  `make comply`'s `comply-bindings-check` resolves ONLY `contracts/team-servers/binding.yaml`;
      nothing resolved `contracts/binding.yaml` before 115-11 added
      `tests/phase115_contract_bindings.rs`
  Phase 115 bindings landed as `status: planned` in wave 1 and MUST be flipped to
      `status: implemented` by this plan once the implementing plans have landed

Verification-instrument findings (from the cross-AI review, all measured):
  `make test-fuzz` converts every non-zero fuzz exit into success (`Makefile:241`)
  `make test-property` selects ONLY `#[ignore]`d tests; the repo's 13 `property_*` functions are not
      ignored, so it selects ZERO tests. Not fixed here — adding `#[ignore]` would remove them from
      the default `cargo test` run
  `make test-examples` reports an unbuildable example as "skipped" and continues
  `make wasm-build` never compiles `jsonschema` (it passes only `--features wasm`), so SCHM-01's
      wasm-clean claim needed the explicit `--features "wasm,validation"` command 115-03 added
  `nextest -E 'test(/stem/)'` selects ZERO tests against a file whose test names lack the stem and
      exits 0 (115-RESEARCH § Pitfall 4)

Structural findings (measured during the replan):
  `src/server/traits.rs` is an ORPHAN — no `mod traits` declaration, never compiled on any target —
      yet it constructs `ListToolsResult`; 115-05 edited it anyway and a green build proves nothing
  `src/server/wasm_server_tests.rs` is a SECOND orphan — `#[cfg(all(test, target_arch = "wasm32"))]`,
      and `cargo check --target wasm32-unknown-unknown --profile test` cannot build the dev-dependency
      graph (`mio` has no wasm32 support); it also imports `CallToolParams`, `ListToolsParams` and
      `InitializeParams`, none of which exist in `src/types/` today
  `src/server/wasm_server.rs` is a THIRD dispatcher, `#[cfg(target_arch = "wasm32")]` only. 115-06
      wired `project_caching_hints(…, None, Cacheable::Yes)` into it to close a D-11 v1 leak, but
      NO gate executes that file — `make wasm-build` only compiles it, and 115-06's negative control
      MEASURED that removing the call leaves that build green. The only automated catch is
      115-08's source tripwire
  `extract_request_meta_value` (`src/server/core.rs:3833`) reads the typed `_meta` era signal from
      ONLY `CallTool`, `GetPrompt` and `ReadResource`, so four of the six cacheable methods cannot
      reach `Era::V2` through in-process `ServerCore` dispatch at all; 115-07 covers them over HTTP
      and asserts the bound at a named test
  Response middleware runs AFTER the projection (`src/server/core.rs:3249`,
      `src/shared/middleware.rs:481`), so it can still remove or forge `ttlMs`/`cacheScope`. Not
      reordered — that would change what middleware observes about Phase 114's
      `resultType`/`serverInfo`. Documented, tested (115-06 Task 3 test 10) and fenced (115-08 test 11)

Other deferred / unowned discoveries:
  `pmcp-agent` `src/iteration/decide.rs:218` `validator_for` is unpinned; allowlisted in 115-08 with
      a written justification (it validates agent submit-results, not the MCP `outputSchema` seam)
  `pmcp-server-toolkit` carries a DEAD optional `jsonschema` dep (zero usages anywhere);
      `make unused-deps` is currently a no-op (`Makefile:204`, "cargo machete not installed - skipping")
  No server-builder-level override for the three dispatcher-built cacheable results
      (`tools/list`, `prompts/list`, `server/discover`); they always emit the SDK default
  v2 `outputSchema` mismatch stays warn-only; escalation to a hard error is a NEW production failure
      mode and was deliberately not decided inside a plan (115-RESEARCH § Open Question 1)
  `Cargo.lock` is gitignored, so the jsonschema bump has no reviewable lockfile diff and re-resolves
      on every machine and CI run; an exact `=0.49.2` pin was declined for library-semver reasons
  The D-10 cross-import tripwire: declined at the types layer (115-05) in favour of reciprocal
      rustdoc; added as a cheap structural assertion at the tripwire layer (115-08 test 13)
  `CacheScope::Display` and four of the ten originally-planned builders were TRIMMED (115-05) as
      public surface the wire does not require
  `u64` for `ttlMs` has no upper bound in the schema (`type: integer`, no `maximum`); the residual is
      ACCEPTED at ~584 million years of milliseconds

Inherited items to re-assert or close:
  `D-114-R` — "published core schema not vendored" — **CLOSED by 115-01**
  `D-114-S` — nothing watches `modelcontextprotocol/ext-tasks` for publication — **still unowned**;
      115-01's generalized provenance test makes a second vendored tree cheap but is NOT a watcher,
      and CONTEXT.md deferred the watcher explicitly
  `D-113-U` — still needs an owner before this branch merges
  `D-114-U` — the +13 `make test-feature-flags` dead-code lints — still unowned, not this phase's
</deviations_and_discoveries_to_record>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep stale docs and write the deferred-items ledger — BEFORE the gate</name>
  <files>src/server/output_validation.rs, src/types/tools.rs, README.md, pmcp-book/src, schema/vendored/ext-tasks/PROVENANCE.md, contracts/binding.yaml, .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md</files>
  <read_first>
    - All eleven `115-*-SUMMARY.md` files — every deviation, declination and finding this task
      records must be VERIFIED against them, not copied from this plan's context block
    - .planning/phases/114-tasks-extension-migration/deferred-items.md lines 1-60 — the ledger form
      AND its recorded ID-collision incident
    - src/server/output_validation.rs — its module doc, amended by 115-03; verify the amendment
      landed and that no sentence still claims `$schema` auto-detect on both eras
    - src/types/tools.rs — the `structured_content` field rustdoc amended by 115-04
    - schema/vendored/ext-tasks/PROVENANCE.md — its statement about what `schema/` contains, which
      115-01 falsified by adding a second tree
    - contracts/binding.yaml — the thirteen Phase 115 bindings 115-11 wrote as `status: planned`
    - This plan's `<deviations_and_discoveries_to_record>` block
  </read_first>
  <action>
**This task runs BEFORE the whole-phase gate**, so the gate in Task 2 covers the final tree. The
pre-review ordering (gate, then edit rustdoc/README/book/provenance) meant the gate never saw the
files it was supposed to certify.

**(a) Flip the contract bindings.** In `contracts/binding.yaml`, change every Phase 115 binding
written by 115-11 from `status: planned` to `status: implemented`, and update each `signature:` to
the signature that ACTUALLY shipped (read it from the source, not from the plan). A signature that
differs from what 115-11 recorded is a FINDING — record it in the ledger and in the SUMMARY rather
than silently rewriting. Then re-run
`cargo nextest run --features full -E 'binary(phase115_contract_bindings)'`: 115-11's test 1 becomes
load-bearing at this point, because it now resolves every flipped binding against real source. A
ghost binding here means a plan did not deliver what the contract claims.

**(b) Sweep stale docs.** Find and fix every in-repo statement this phase falsified. At minimum
check `src/server/output_validation.rs`'s module doc (115-03 amended it — verify), `src/types/tools.rs`'s
`structured_content` rustdoc (115-04 amended it — verify), any rustdoc or book text asserting
`structuredContent` must be an object, README/`pmcp-book` mentions of schema-draft auto-detection,
and `schema/vendored/ext-tasks/PROVENANCE.md`'s statement about what `schema/` contains (115-01 added
a second tree beside it). Use:
  `grep -rn 'auto-detect\|autodetect' --include='*.rs' --include='*.md' src/ pmcp-book/src/ README.md`
  `grep -rn 'restricted to.*object' --include='*.rs' --include='*.md' src/ pmcp-book/src/`
  `grep -rn 'ext-tasks' --include='*.md' schema/`
Documentation CHAPTERS are Phase 119's (DOCS-05/06) — do NOT write new book chapters here. Fix
statements that are now FALSE and leave the rest. Record which hits were reviewed and deliberately
left true.

**(c) Write the ledger.** Create
`.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md` in the
Phase 114 form. Before assigning any ID run `grep -c "^## D-115-"` on the file to find the next free
letter — Phase 114's ledger records an ID-collision incident caused by exactly that omission.

Every item in this plan's `<deviations_and_discoveries_to_record>` block gets an entry, VERIFIED
against the landed SUMMARYs rather than copied. Each entry names an owner or says **unowned**
explicitly — an unowned item is acceptable, an undocumented one is not. Give the three
verification-instrument findings their own entries with sharp titles, because they affect every
future phase in this repo, not just this one:
  - `make test-fuzz` / `test-property` / `test-examples` are fail-open; `test-property` selects zero
    tests today
  - `make wasm-build` does not compile the `validation` feature
  - `nextest -E 'test(/stem/)'` silently selects zero tests against a non-prefixed file
Additionally:
  - record `D-114-R` ("published core schema not vendored") as **CLOSED by 115-01**, with a redirect
    note so a reader arriving from Phase 114's ledger finds the closure
  - re-assert `D-114-S` (nothing watches `ext-tasks` upstream) as **still unowned**
  - carry `D-113-U` forward as still needing an owner before this branch merges
  </action>
  <verify>
    <automated>test -f .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md && cargo nextest run --features full -E 'binary(phase115_contract_bindings)' && test "$(grep -c 'status: planned' contracts/binding.yaml)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'status: planned' contracts/binding.yaml` returns 0 — every Phase 115 binding was flipped to `implemented`
    - `cargo nextest run --features full -E 'binary(phase115_contract_bindings)'` exits 0 with exactly 5 tests — 115-11's ghost-binding test is now load-bearing over thirteen real bindings
    - `.planning/phases/115-.../deferred-items.md` exists, contains at least 16 `## D-115-` headings, and every heading's body contains either `Owner:` or the literal `**unowned**`
    - The ledger contains dedicated entries naming `test-property`, `wasm-build`, `traits.rs`, `wasm_server_tests.rs`, `extract_request_meta_value` and `process_response_with_context`
    - The ledger contains `D-114-R`, `D-114-S` and `D-113-U`
    - `grep -n 'D-115-' .planning/phases/115-*/deferred-items.md | awk -F'D-115-' '{print $2}' | cut -c1 | sort | uniq -d` returns nothing — no duplicate IDs
    - The stale-doc greps return no remaining FALSE statement; the SUMMARY records which hits were reviewed and left
    - `.planning/ROADMAP.md` and `.planning/STATE.md` are NOT modified by this task — completion markers belong to Task 3
    - `git diff --stat -- .planning/ROADMAP.md .planning/STATE.md` is EMPTY
  </acceptance_criteria>
  <done>The tree contains no statement this phase falsified, the contract bindings assert what actually shipped and are proven to resolve, and every out-of-scope discovery has an owner or an explicit unowned marker — all before the gate runs.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-phase gate over the swept tree, measured as deltas, and the requirement bookings</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-10-SUMMARY.md</files>
  <read_first>
    - Makefile lines 55-70, 120-135, 185-215, 228-252, 295-315, 355-375, 660-700 — `wasm-build`,
      `build`, `audit`, `unused-deps`, the three FAIL-OPEN ALWAYS targets, `test-feature-flags`,
      `test-all`, `validate-always`, `quality-gate`
    - ./CLAUDE.md § "CI Quality Gates" — the PMAT complexity gate pinned to 3.15.0 and the rule that
      it must not be disabled, weakened or removed
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md § Architecture Patterns — the six P1-P6
      refactor techniques, and the hard cap of cog 50 for an annotated allow
    - .planning/REQUIREMENTS.md lines 115-150 — the SCHM block and the TASK block above it, whose
      block-quote booking form is the house style
    - schema/vendored/core-2026-07-28/PROVENANCE.md — the artifact the booking cites
    - Every `115-*-SUMMARY.md`, for the per-plan test counts to re-verify
  </read_first>
  <action>
**(a) Establish a phase base FIRST, then measure deltas.** An absolute violation count says nothing
about whether THIS phase caused one. From the merge-base commit for this phase's work, capture and
record: the `pmat analyze complexity --format json --max-cognitive 25` violation count for `src/`,
the `make test-feature-flags` warning count, and `cargo public-api` output.

**(b) Run the gate on the CURRENT (swept) tree** and record each result:
  `make quality-gate`
  `make wasm-build`
  `cargo build --target wasm32-unknown-unknown --no-default-features --features "wasm,validation"`
      ← the command SCHM-01's wasm-clean claim actually needs; `make wasm-build` never compiles
        `jsonschema` (`Makefile:61` passes only `--features wasm`)
  `make test-feature-flags`
  `cargo public-api diff` (or `cargo semver-checks check-release`)
  `pmat analyze complexity --format json --max-cognitive 25`

**(c) Re-run every Phase 115 suite BY NAME**, because `make quality-gate`'s `validate-always` chain
is fail-open on all three of its ALWAYS targets (measured; see `<interfaces>`) and a zero-selection
can hide inside `test-all`:
  `cargo nextest run --features full -E 'binary(vendored_schema_provenance) + binary(v2_core_schema_facts) + binary(v1_lists_golden) + binary(structured_tool_output) + binary(v2_caching_hints) + binary(v2_schema_tripwires) + binary(property_tests) + binary(phase115_contract_bindings)'`
  `cargo nextest run --lib --features full -E 'test(/output_validation::/) + test(/types::caching/) + test(/inject_v2_result_envelope/)'`
  `cargo nextest run --features "full fuzzing" -E 'binary(property_tests)'`
  `cd fuzz && cargo fuzz run fuzz_schema_draft_pin -- -runs=0 corpus/fuzz_schema_draft_pin`
  `cd fuzz && timeout 120s cargo fuzz run fuzz_schema_draft_pin -- -max_total_time=60` then
      `ls fuzz/artifacts/fuzz_schema_draft_pin/` and assert it is EMPTY
  `timeout 30s cargo run --example s52_v2_caching_hints --features full`
Transcribe each Summary line's test count into the SUMMARY. A count BELOW the owning plan's
acceptance criterion is a FAILURE, not a rounding difference.

**(d) Assert as DELTAS:** zero NEW pmat complexity violations attributable to Phase 115 files; zero
NEW `test-feature-flags` warnings; `cargo public-api` shows ADDITIONS ONLY — expected additions are
`CacheScope`, `DEFAULT_TTL_MS`, `CallToolResult::structured_value`, the SIX
`with_ttl_ms` / `with_cache_scope` builders on the three resource-side result types, the six pairs of
result-type fields, and `Hash` on `Era` — with zero removals and zero signature changes. Note that
`Cacheable`, `project_caching_hints` and the `fuzz_support` seam must NOT appear: the first two are
`pub(crate)` and the third is behind `fuzzing`, which is in neither `default` nor `full`.

Do NOT weaken any gate to make it pass. If `pmat` flags a Phase 115 function, apply one of the six
P1-P6 refactors from `75-RESEARCH.md`; a `// Why:`-annotated `#[allow(clippy::cognitive_complexity)]`
is the last resort and is hard-capped at cog 50. If a gate cannot be made green, STOP and report
rather than adjusting the gate.

**(e) Book the requirements.** Set SCHM-01, SCHM-02 and SCHM-03 to `[x]` in
`.planning/REQUIREMENTS.md`, each followed by an indented block quote carrying:
  - MEASURED evidence by test binary name and count (e.g. `binary(v2_caching_hints)` — N tests over
    six methods, two eras and both native dispatchers)
  - the published-evidence citation: `schema/vendored/core-2026-07-28/` at pinned commit
    `271ecc9accafdd9b83a3c869fa67c22953b2af80`, digest-fenced by
    `tests/vendored_schema_provenance.rs`, with the wire facts re-derived by
    `tests/v2_core_schema_facts.rs`
  - an explicit sentence that this is `[x]` and NOT `[~]`: D-15's contingency did not fire,
    `CacheableResult` IS in the published core schema, and booking `[~]` here would be exactly the
    habit D-15 named
  - the deviations, INSIDE the booking rather than in a footnote:
      SCHM-01 — shipped `jsonschema = "0.49"`, not the literal `0.48`, because 0.48.0-0.48.2 carry
                packaging defects and 0.49 is additive-only over 0.48; an exact `=0.49.2` pin was
                declined for library-semver reasons
      SCHM-03 — SIX result types, not five: `DiscoverResult extends CacheableResult` in the pinned
                schema; excluding `server/discover` would have shipped a knowingly non-conformant
                first call for every v2 client
  - for SCHM-01, the judgement this booking makes rather than absorbs: the naive pin was MEASURED to
    be a silent validation bypass, so "Draft 2020-12 explicitly pinned" is satisfied by
    normalize-then-compile, fenced by a draft-07 test whose negative control was observed to fire
    (cite `115-03-SUMMARY.md`); AND that the wasm-clean half is proven by
    `cargo build --target wasm32-unknown-unknown --no-default-features --features "wasm,validation"`,
    because `make wasm-build` alone never compiles `jsonschema`
  - for SCHM-02, that Finding 6 held: there was no object-only guard in pmcp to remove; the
    constraint lived in v1 spec text, and pmcp's pre-existing v1 over-permissiveness is FROZEN by
    D-05 rather than corrected. State also that the v2 claim is proven with an IN-BAND era witness
    (`resultType`), because the pre-review tests would have run as v1
  - for SCHM-03, the scope bound: four of the six methods cannot reach `Era::V2` through in-process
    `ServerCore` dispatch (`extract_request_meta_value`), so their v2 evidence is over HTTP, and the
    bound is asserted at a named test rather than left implicit

Do NOT touch `.planning/ROADMAP.md` or `.planning/STATE.md` in this task. Completion markers are
Task 3's, and only after approval.
  </action>
  <verify>
    <automated>make quality-gate && make wasm-build && cargo build --target wasm32-unknown-unknown --no-default-features --features "wasm,validation" && test "$(grep -c '^- \[x\] \*\*SCHM-0' .planning/REQUIREMENTS.md)" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0
    - `make wasm-build` exits 0 AND `cargo build --target wasm32-unknown-unknown --no-default-features --features "wasm,validation"` exits 0
    - `make test-feature-flags` exits 0 with no MORE warnings than the recorded phase base
    - `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | startswith("src/"))] | length'` is not greater than the recorded phase-base count
    - `cargo public-api diff` (or `cargo semver-checks check-release`) reports additions only — zero removals, zero signature changes — and shows NO `Cacheable`, `project_caching_hints` or `fuzz_support` item
    - Every named Phase 115 suite reports a test count at least equal to its owning plan's acceptance criterion, and the counts are transcribed into the SUMMARY
    - `cargo fuzz run fuzz_schema_draft_pin -- -runs=0 corpus/fuzz_schema_draft_pin` exits 0, and after a 60-second run `fuzz/artifacts/fuzz_schema_draft_pin/` is EMPTY (checked by `ls`, not by `make test-fuzz`)
    - `timeout 30s cargo run --example s52_v2_caching_hints --features full` exits 0 and its stdout is transcribed
    - `grep -c '^- \[x\] \*\*SCHM-0' .planning/REQUIREMENTS.md` returns exactly 3, and no SCHM line carries `[~]`
    - `.planning/REQUIREMENTS.md` contains the literal strings `271ecc9accafdd9b83a3c869fa67c22953b2af80`, `0.49`, `DiscoverResult` and `wasm,validation` within the SCHM block
    - `git diff --stat` shows no gate file (`Makefile`, `.github/workflows/ci.yml`, `deny.toml`) modified anywhere in this phase
    - `git diff --stat -- .planning/ROADMAP.md .planning/STATE.md` is EMPTY — completion markers are not applied yet
  </acceptance_criteria>
  <done>The whole workspace is green under the real gate over the SWEPT tree, semver is additive-only, every Phase 115 suite is proven to have actually run rather than to have selected nothing, and the three requirements are booked with cited, re-derivable evidence — with the roadmap and state untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase sign-off, and completion markers ONLY after approval</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-10-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the three bookings written in Task 2
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md
    - .planning/ROADMAP.md lines 2512-2540 (the Phase 115 entry, including the existing
      planning-deviation note) and the milestone progress table near line 2578
    - .planning/STATE.md — the frontmatter progress counters, `## Current Position`, and the trailing
      `Stopped at:` / `Next:` lines
    - All eleven `115-*-SUMMARY.md` files
  </read_first>
  <what-built>
Phase 115 in full: the 2026-07-28 core schema vendored at a pinned commit with both digests known in
advance and a generalized provenance tripwire; the `CacheableResult` contract — including `ttlMs`'s
measured `type: "integer"` — re-derived from the pinned bytes; pre-change v1 golden byte fixtures for
five list/read responses with a proven-firing leak guard; three provable-contract equations written
BEFORE the code and bound to thirteen functions with a ghost-binding gate; `jsonschema` at 0.49 with
Draft 2020-12 pinned on v2 via normalize-then-compile, a pure `Cow`-returning normalizer, an
era-keyed validator cache and an uncached compile path, proven wasm-clean WITH validation enabled;
`CallToolResult::structured_value` plus non-object `structuredContent` coverage on both dispatchers
with an in-band `Era::V2` witness; a closed `CacheScope` enum and caching-hint slots on six result
types; a cfg-free projector that ensures on v2 and strips on v1 across all THREE dispatchers,
including the wasm one; on-the-wire coverage of six methods across two eras; SEP-2106 fenced against
cargo's declared AND resolved dependency graphs; single-projection, wasm-call-site and
middleware-ordering tripwires; a fuzz target with three TRUE invariants and a committed seed corpus;
property tests; a runnable example; and a green `make quality-gate` over a swept tree.
  </what-built>
  <action>
Present the sign-off below to the owner and STOP. Do not proceed past this task and do not answer it
yourself. If the checkpoint is returned unanswered, leave it unanswered and report that — Phase 114's
own record shows the correct handling: its sign-off was returned unanswered rather than
self-approved, and was then answered by the owner.

**No completion marker exists on disk at this point.** Task 2 wrote the three `[x]` bookings (which
are the EVIDENCE being reviewed) but deliberately did not tick the roadmap or advance STATE, so a
rejection leaves the repository correctly recording an open phase. That ordering is the fix for a
defect the cross-AI review found in the pre-review version of this plan.

**If the owner APPROVES**, and only then, apply the completion markers:
  - `.planning/ROADMAP.md`: tick the ELEVEN Phase 115 plan entries (ten original + 115-11, added by
    the `--reviews` replan), mark the phase entry complete, and update the milestone progress table
    row (`115. JSON Schema 2020-12 + Caching Hints`) to `11/11` with today's date. VERIFY the
    existing planning-deviation note under Phase 115's Success Criteria against what actually
    shipped and correct it if the phase diverged further; add the contract-location deviation to it.
  - `.planning/STATE.md`: update the frontmatter progress counters, `## Current Position`, and the
    trailing `Stopped at:` / `Next:` lines, carrying forward the standing obligations (D-114-S's
    watch, D-113-U's owner) that the previous entry already names.
  - Record in the SUMMARY who approved, when, and any corrections they made.

**If the owner REJECTS any item**, record the objection verbatim, apply NO completion marker, and do
NOT close the phase. If they reject a `[x]` booking specifically, revert that line to `[ ]` or `[~]`
per their instruction and record which.
  </action>
  <how-to-verify>
1. Read the three `[x]` bookings in `.planning/REQUIREMENTS.md` lines 143-145 and their block
   quotes. Confirm you accept the three recorded deviations:
   (a) `jsonschema` shipped at **0.49**, not the literal **0.48** in SCHM-01's text, and an exact
       `=0.49.2` pin was declined for library-semver reasons
   (b) **six** result types carry caching hints, not the **five** in SCHM-03's text —
       `server/discover` is included because `DiscoverResult extends CacheableResult` in the
       pinned schema
   (c) contracts live in-repo at `contracts/`, not at the `../provable-contracts/` path CLAUDE.md
       names, because that directory does not exist in this checkout
2. Read `deferred-items.md` and confirm you accept every item marked **unowned**, in particular:
   the unpinned `pmcp-agent` `validator_for`; the absent server-builder-level default override; the
   wasm server's strip being compile-checked and tripwire-checked but never EXECUTED by any gate;
   v2 `outputSchema` mismatch staying warn-only rather than escalating to a hard error; response
   middleware still being able to mutate the projected fields; and the three fail-open ALWAYS make
   targets (`test-fuzz`, `test-property`, `test-examples`).
3. Run `make quality-gate` yourself and confirm it exits 0.
4. Run `cargo build --target wasm32-unknown-unknown --no-default-features --features "wasm,validation"`
   and confirm it exits 0 — this, not `make wasm-build`, is SCHM-01's real wasm-clean evidence.
5. Run `cargo run --example s52_v2_caching_hints --features full` and confirm the printed output
   shows `ttlMs` / `cacheScope` on the v2 responses and NEITHER on the v1 response.
6. Confirm you accept the SCHM booking as `[x]` rather than `[~]`. Phase 114 is held on publication;
   Phase 115 is NOT, because its wire values come from the published core schema vendored at
   `schema/vendored/core-2026-07-28/`. If you disagree, say so — the hold is yours to impose, and it
   must not be released OR imposed by an agent.
  </how-to-verify>
  <acceptance_criteria>
    - The checkpoint is answered by the owner. It MUST NOT be self-approved by the executing agent
    - Before the answer, `git diff --stat -- .planning/ROADMAP.md .planning/STATE.md` is EMPTY
    - If approved: `.planning/ROADMAP.md`'s Phase 115 entry lists ELEVEN ticked plans and the progress table row reads `11/11`; `.planning/STATE.md` `## Current Position` names Phase 115 as complete and `Next:` names Phase 116 or 117; and the SUMMARY records who approved, when, and any corrections
    - If rejected: no completion marker was applied, the objection is recorded verbatim, and the SUMMARY states that Phase 115 remains open
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Sign-off' .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-10-SUMMARY.md</automated>
  </verify>
  <done>The owner has answered the sign-off. If approved, the completion markers were applied AFTER that answer and the SUMMARY records who, when and any corrections. If rejected, no completion marker exists, the objection is recorded verbatim, and Phase 115 stays open.</done>
  <resume-signal>Type "approved" to close Phase 115, or describe what must change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → project record | A booking, a ledger entry and a roadmap tick are claims a future reader will trust without re-deriving |
| agent → quality gate | A gate that can be weakened to pass is not a gate |
| agent → completion marker | A marker written before approval is a claim the owner never made |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-26 | Repudiation | requirement booking | mitigate | Each `[x]` cites a named test binary and count plus the pinned artifact and its commit SHA, so the claim is re-derivable. The three deviations are recorded INSIDE the booking, not in a footnote a reader can miss. SCHM-01's booking additionally cites the `wasm,validation` build, because `make wasm-build` alone never compiles `jsonschema`. |
| T-115-27 | Tampering | quality gate | mitigate | An acceptance criterion asserts no gate file (`Makefile`, `ci.yml`, `deny.toml`) was modified anywhere in the phase, and the plan states that a gate which cannot be made green is a STOP, not an adjustment. pmat and semver are measured as DELTAS against a recorded phase base. The three fail-open ALWAYS targets are named explicitly and replaced with direct commands, so a green `validate-always` is not mistaken for evidence. |
| T-115-28 | Repudiation | self-approval | mitigate | The sign-off is a `checkpoint:human-verify gate="blocking"` and the acceptance criteria state it must not be self-approved. It explicitly asks the owner to accept or reject the `[x]` booking, because a hold is the owner's to impose. |
| T-115-29 | Repudiation | undocumented discoveries | mitigate | Every out-of-scope item names an owner or is explicitly **unowned**, and a duplicate-ID check runs over the ledger — the exact defect Phase 114's ledger records having suffered. Sixteen entries are required, including dedicated ones for the three verification-instrument findings that affect every future phase. |
| T-115-43 | Repudiation | premature completion markers | mitigate | The pre-review version ticked ROADMAP and advanced STATE BEFORE the human checkpoint, so a rejected sign-off would have left the repository recording a complete phase. Markers now live in Task 3, applied only after an owner answer, and an acceptance criterion asserts `git diff --stat -- .planning/ROADMAP.md .planning/STATE.md` is EMPTY before the answer. |
| T-115-44 | Repudiation | ungated post-gate edits | mitigate | The pre-review version ran the whole-phase gate and THEN edited rustdoc, README, book text, provenance and planning state — files absent from its own header — with no rerun. The sweep is now Task 1, the gate is Task 2, and every swept file is declared in `files_modified`. |
| T-115-45 | Repudiation | contract bindings that never resolve | mitigate | 115-11's bindings land as `status: planned` in wave 1, when the functions do not exist. Task 1 flips them to `implemented` and re-runs `binary(phase115_contract_bindings)`, at which point the ghost-binding test becomes load-bearing over thirteen real bindings — a plan that did not deliver what its contract claims fails here. |
| T-115-SC | Tampering | package installs | mitigate | This plan installs NO package and modifies no dependency manifest. `115-RESEARCH.md` § Package Legitimacy Audit records `slopcheck install jsonschema` → `[OK]`, verdict Approved, with zero `[ASSUMED]`/`[SUS]`/`[SLOP]` entries across the phase, so no blocking legitimacy checkpoint applies. `make audit` runs inside `make quality-gate` in Task 2. |
</threat_model>

<verification>
- Task 1 completes BEFORE Task 2: the sweep and the ledger precede the gate
- `make quality-gate`, `make wasm-build`, `cargo build --target wasm32-unknown-unknown --no-default-features --features "wasm,validation"`, `make test-feature-flags` — all green
- `cargo public-api diff` — additions only, and no `pub(crate)` or `fuzzing`-gated item visible
- Every Phase 115 suite re-run by name with its test count transcribed; the fuzz corpus replayed; the example run
- `grep -c '^- \[x\] \*\*SCHM-0' .planning/REQUIREMENTS.md` returns 3
- `deferred-items.md` exists with >=16 items, no duplicate IDs, every item owned or explicitly unowned
- `contracts/binding.yaml` carries zero `status: planned` and `binary(phase115_contract_bindings)` is green
- ROADMAP and STATE are untouched until the sign-off is answered
- The sign-off checkpoint is answered by the owner
</verification>

<success_criteria>
- The stale-doc sweep and the ledger land BEFORE the whole-phase gate, so the gate certifies the final tree
- The full quality gate is green with every Phase 115 change in the tree, measured as deltas against a recorded phase base, with the fail-open ALWAYS targets replaced by direct commands
- SCHM-01/02/03 are booked `[x]` with cited, re-derivable evidence and their deviations stated inside the booking
- Every out-of-scope discovery names an owner or is explicitly unowned, with no duplicate IDs, including the three verification-instrument findings
- Contract bindings assert what actually shipped and are proven to resolve
- No completion marker exists on disk before the owner answers; ROADMAP and STATE are updated only after approval
- The phase is signed off by the owner, not self-approved
</success_criteria>

<output>
Create `.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-10-SUMMARY.md` when done.
Record in it: the phase-base vs current pmat / feature-flag / public-api figures; the per-suite test
counts; the fuzz corpus replay result and the artifacts-directory check; the example stdout; the
`wasm,validation` build result; any signature divergence found when flipping the contract bindings;
the full deferred-items ID list; and the sign-off (who, when, and any corrections made).
</output>
