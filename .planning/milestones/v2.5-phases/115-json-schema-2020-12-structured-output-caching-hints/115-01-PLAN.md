---
phase: 115-json-schema-2020-12-structured-output-caching-hints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/vendored/core-2026-07-28/schema.ts
  - schema/vendored/core-2026-07-28/schema.json
  - schema/vendored/core-2026-07-28/PROVENANCE.md
  - tests/vendored_schema_provenance.rs
  - tests/v2_core_schema_facts.rs
autonomous: true
requirements: [SCHM-01, SCHM-02, SCHM-03]

commit_policy: |
  This plan changes ZERO production bytes (`schema/` artifacts + two test files only).
  Per the phase commit policy it runs the SCOPED pre-commit gate
  (`make fmt-check`, `make lint`, `make check-todos`, plus its own suites) rather
  than the full `make quality-gate`, and asserts `git diff --stat -- src/ Cargo.toml`
  is empty. The full `make quality-gate` runs once for the phase in 115-10.

must_haves:
  truths:
    - "The 2026-07-28 core schema exists in-tree as a byte-pinned, digest-fenced artifact — every wire fact this phase implements is re-derivable offline"
    - "Adding a second vendored tree cannot leave it unverified: the provenance tripwire scans every subdirectory of schema/vendored/"
    - "The wire facts this phase builds on — CacheableResult's required key set, cacheScope's closed variant set, ttlMs's JSON Schema type and minimum, and the exact list of results that extend CacheableResult — are asserted against the pinned artifact, not against a network summary"
  artifacts:
    - path: "schema/vendored/core-2026-07-28/schema.ts"
      provides: "The 2026-07-28 TypeScript protocol source at pinned commit 271ecc9accafdd9b83a3c869fa67c22953b2af80"
      contains: "CacheableResult"
    - path: "schema/vendored/core-2026-07-28/schema.json"
      provides: "The 2026-07-28 generated JSON Schema at the same pinned commit"
      contains: "cacheScope"
    - path: "schema/vendored/core-2026-07-28/PROVENANCE.md"
      provides: "Fetch attribution, 40-char pinned commit, per-file SHA256 + git blob SHA-1, reproduce block, change protocol"
      contains: "271ecc9accafdd9b83a3c869fa67c22953b2af80"
    - path: "tests/v2_core_schema_facts.rs"
      provides: "Runtime re-derivation of the CacheableResult contract from the vendored artifact"
      min_lines: 150
  key_links:
    - from: "tests/vendored_schema_provenance.rs"
      to: "schema/vendored/core-2026-07-28/"
      via: "runtime read_dir over schema/vendored/*"
      pattern: "schema/vendored"
    - from: "tests/v2_core_schema_facts.rs"
      to: "schema/vendored/core-2026-07-28/schema.json"
      via: "runtime read_to_string + serde_json"
      pattern: "core-2026-07-28"
---

<objective>
Vendor the published MCP `2026-07-28` **core** schema at a pinned upstream commit, with a
`PROVENANCE.md` and a digest tripwire, and generalize the existing provenance test so it covers every
vendored tree rather than the single hardcoded `ext-tasks` path. Then re-derive — from the pinned
artifact, in a test — every wire fact the rest of this phase is built on, **including `ttlMs`'s JSON
Schema `type` and `minimum`, which the pre-review plan set inferred rather than measured**.

Purpose: D-14. Today `schema/` contains ONLY `vendored/ext-tasks/`; neither `cacheScope` nor
`structuredContent` appears anywhere under `schema/`. Without this plan, Phase 115 would build
against a *network summary* (CONTEXT.md § Measured Spec Evidence, whose own preamble says it is "a
strong prior, not a verified fact") — exactly the decaying-finding failure mode `PROVENANCE.md` was
written to eliminate. This plan is what makes SCHM-01/02/03 bookable `[x]` on published evidence
(D-15) instead of inheriting a `[~]` hold by habit.

Output: `schema/vendored/core-2026-07-28/{schema.ts,schema.json,PROVENANCE.md}`, a generalized
`tests/vendored_schema_provenance.rs`, and a new `tests/v2_core_schema_facts.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-PATTERNS.md
@schema/vendored/ext-tasks/PROVENANCE.md
@tests/vendored_schema_provenance.rs

<interfaces>
<!-- Reusable, DO NOT rewrite — from tests/vendored_schema_provenance.rs -->

Existing helpers (keep their bodies verbatim; only their call shape changes):
  fn repo_root() -> PathBuf                                  (:76)
  fn rel(path: &Path) -> String                              (:89)
  fn discover_vendored_files(dir: &Path, out: &mut Vec<PathBuf>)   (:101, already recursive)
  fn sha256_of(path: &Path) -> String                        (:128, in-process sha2)
  fn is_lower_hex(ch: char) -> bool                          (:144)
  fn maximal_hex_runs(text: &str) -> Vec<String>             (:155, stops a 64-hex digest
                                                              from satisfying a 40-hex search)
  fn recorded_digests(provenance: &str) -> BTreeSet<String>  (:196)

Constants that become per-tree rather than global (:62, :66, :74):
  const VENDORED_DIR: &str = "schema/vendored/ext-tasks";
  const PROVENANCE_FILE: &str = "PROVENANCE.md";
  const MINIMUM_VENDORED_FILES: usize = 2;

The five existing tests, all already prefixed with the file stem so
`test(/vendored_schema/)` selects correctly (:208, :239, :257, :295, :339):
  vendored_schema_dir_is_discovered_at_runtime_and_is_not_empty
  vendored_schema_provenance_md_exists_so_the_artifact_is_attributed
  vendored_schema_every_file_digest_is_recorded_in_provenance_md
  vendored_schema_every_recorded_digest_belongs_to_a_file_that_still_exists
  vendored_schema_provenance_pins_a_full_40_character_commit_sha
</interfaces>

<measured_provenance_data>
<!-- From 115-RESEARCH.md § Ready-made D-14 provenance data, PLUS the schema.json digests
     measured during the 2026-08-01 `--reviews` replan (research had not fetched that file). -->

Repository:                https://github.com/modelcontextprotocol/modelcontextprotocol
Pinned commit (40 chars):  271ecc9accafdd9b83a3c869fa67c22953b2af80
Commit committer date:     2026-07-28T16:42:34Z
Commit subject:            fix(schema): apply subscriptions/listen envelope and MetaObject rename to 2026-0…
Prior commit on the path:  b488c16623e5202a3961e551886044577ae0f096 ("Add 2026-07-28 MCP specification", 2026-07-28T15:56:05Z)

  schema/2026-07-28/schema.ts    98426 bytes, 3197 lines
      SHA256      742750af0bb8c716e7030c4977c992b55d1adc4407e9e66997db5846baedc2cd
      blob SHA-1  9b55feeb412bc3ae877f2eac10b5c01ba29a2eed
  schema/2026-07-28/schema.json  181474 bytes
      SHA256      ef70b61f99b6d2e5e3b46863822eab08dff6a45bedc7a08914e0e5b133f40203
      blob SHA-1  213c58f6d9a1c2ce6ad055afe90bbdb095a29ee8

BOTH digests are now known in advance, so the fetch is verifiable BEFORE `PROVENANCE.md` is written.
A mismatch on either file means the pin moved or the fetch was tampered with — STOP, do not vendor.

Not vendored (matches the ext-tasks precedent of two files): schema.mdx, examples/
</measured_provenance_data>

<measured_schema_facts>
<!-- Measured from the pinned artifact during the 2026-08-01 `--reviews` replan.
     These are the EXPECTED values for Task 3; the test must re-derive them from the
     vendored bytes, not restate them. Any disagreement is a finding, not a typo. -->

The JSON Schema uses `$defs`, NOT `definitions`. The top-level keys are exactly
`["$schema", "$defs"]`, so the resolvable pointer is `/$defs/CacheableResult`.
⚠ The pre-review plan set wrote `/definitions/CacheableResult/required` — that pointer does
   not resolve and the assertion would have failed.

$defs.CacheableResult:
  properties: _meta, cacheScope, resultType, ttlMs
  required (sorted): ["cacheScope", "resultType", "ttlMs"]     ← THREE, not two
  ⚠ The pre-review plan set asserted `["cacheScope", "ttlMs"]`. `resultType` is required on
    CacheableResult too — it is already implemented and injected by Phase 114's
    `inject_v2_result_envelope`, which is why nothing in this phase adds it. The assertion must
    include it or it fails on a correct artifact.

  cacheScope: { "type": "string", "enum": ["private", "public"] }
  ttlMs:      { "type": "integer", "minimum": 0 }               ← MEASURED
  ⚠ The TypeScript source says `ttlMs: number`. The GENERATED JSON Schema narrows it to
    `"type": "integer"` with `"minimum": 0`. This is what settles the Rust representation
    question the cross-AI review raised: `u64` is correct, and it is now measured rather than
    inferred from an `@minimum 0` annotation.

The SIX $defs carrying a `cacheScope` property (excluding CacheableResult itself):
  DiscoverResult, ListPromptsResult, ListResourceTemplatesResult,
  ListResourcesResult, ListToolsResult, ReadResourceResult
Each of the six ALSO lists `cacheScope`, `resultType` and `ttlMs` in its own `required` array,
alongside its own payload key. Examples:
  ListToolsResult.required     = ["cacheScope", "resultType", "tools", "ttlMs"]
  ReadResourceResult.required  = ["cacheScope", "contents", "resultType", "ttlMs"]
  DiscoverResult.required      = ["cacheScope", "capabilities", "resultType", "supportedVersions", "ttlMs"]

$defs.CallToolResult.properties.structuredContent carries a `description` and NO `type` —
i.e. any JSON value. The v1 sentence "Currently restricted to" is absent from the file.

$defs.Tool.properties.outputSchema =
  { "type": "object", "properties": { "$schema": { "type": "string" } }, "additionalProperties": {} }
i.e. the spec ITSELF declares the optional `$schema` that D-02 chooses to ignore.
</measured_schema_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the pinned core 2026-07-28 schema with a PROVENANCE.md</name>
  <files>schema/vendored/core-2026-07-28/schema.ts, schema/vendored/core-2026-07-28/schema.json, schema/vendored/core-2026-07-28/PROVENANCE.md</files>
  <read_first>
    - schema/vendored/ext-tasks/PROVENANCE.md — copy its section skeleton verbatim; it is the
      in-repo format of record (156 lines, 8 headings)
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-PATTERNS.md
      § "schema/vendored/core-2026-07-28/PROVENANCE.md" — the required heading order and the
      exclude-list reasoning to restate
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
      § "Ready-made D-14 provenance data"
    - Cargo.toml lines 35-50 — the `[package] exclude` list and the comment explaining why
      `schema/` is deliberately NOT excluded
  </read_first>
  <action>
Create `schema/vendored/core-2026-07-28/` and fetch exactly two files from
`https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/271ecc9accafdd9b83a3c869fa67c22953b2af80/schema/2026-07-28/` — `schema.ts` and `schema.json`. Fetch at the 40-character SHA, NEVER at `main`; `main` is force-pushable and that is the whole point of the pin.

Verify the fetch two independent ways BEFORE writing `PROVENANCE.md`. Unlike the pre-review version of
this plan, BOTH files' SHA256 values are known in advance, so both are hard preconditions:
(a) `shasum -a 256` on each downloaded file.
      `schema.ts`   MUST equal `742750af0bb8c716e7030c4977c992b55d1adc4407e9e66997db5846baedc2cd`
      `schema.json` MUST equal `ef70b61f99b6d2e5e3b46863822eab08dff6a45bedc7a08914e0e5b133f40203`
    If either differs, STOP and surface the mismatch — do not proceed with a different artifact.
(b) `git hash-object <file>` on each, cross-checked against the GitHub contents API blob SHA at the
    same pin: `schema.ts` = `9b55feeb412bc3ae877f2eac10b5c01ba29a2eed`,
    `schema.json` = `213c58f6d9a1c2ce6ad055afe90bbdb095a29ee8`.

Write `schema/vendored/core-2026-07-28/PROVENANCE.md` using the ext-tasks section skeleton in this order: title; `**Produced by:** Phase 115 plan 115-01, Task 1`; `**Fetch date (UTC):**`; `## What these files are`; `## THESE FILES ARE A READ-ONLY REFERENCE ARTIFACT` (copy the four bullets verbatim from the ext-tasks record); `## Source` (table carrying the full 40-char commit, its committer date, its subject, and the prior commit on the path); `## Vendored files` (table: Local path | Upstream path | Bytes | Lines | SHA256); `### Independent corroboration — git blob SHA-1` (table: local `git hash-object` vs GitHub contents API); `## Reproducing this fetch` (a runnable bash block retargeted to this repo/path/SHA); `## Why these are pre-final values / RE-VERIFICATION OBLIGATION`; `## Change protocol` (five numbered steps ending in the two nextest commands from Task 2 and Task 3).

Add one extra subsection, `### Schema shape notes for readers`, recording two facts that a reader
WILL get wrong otherwise and that cost the pre-review plan set two failing assertions: the generated
JSON Schema uses `$defs` (not `definitions`), and `CacheableResult.required` carries THREE entries —
`cacheScope`, `resultType`, `ttlMs` — because `resultType` is part of the same base and is already
implemented by Phase 114.

Restate the `exclude`-list arithmetic with THIS tree's real numbers rather than copying ext-tasks' `56,324`: the two new files are 279,900 bytes exactly (98,426 + 181,474), bringing `schema/` to roughly 336,000 bytes total, still immaterial against the crates.io package limit — and excluding `schema/` would break `tests/vendored_schema_provenance.rs` for anyone running `cargo test` on the published crate, the same failure mode that forced `tests/team_contracts_conformance.rs` out of the package when `contracts/` was excluded.

Do NOT add `schema/` to `Cargo.toml`'s `[package] exclude`. Do NOT vendor `schema.mdx` or the `examples/` subtree.
  </action>
  <verify>
    <automated>test -f schema/vendored/core-2026-07-28/schema.ts && test -f schema/vendored/core-2026-07-28/schema.json && test -f schema/vendored/core-2026-07-28/PROVENANCE.md && shasum -a 256 schema/vendored/core-2026-07-28/schema.ts | grep -q '^742750af0bb8c716e7030c4977c992b55d1adc4407e9e66997db5846baedc2cd ' && shasum -a 256 schema/vendored/core-2026-07-28/schema.json | grep -q '^ef70b61f99b6d2e5e3b46863822eab08dff6a45bedc7a08914e0e5b133f40203 ' && git hash-object schema/vendored/core-2026-07-28/schema.json | grep -q '^213c58f6d9a1c2ce6ad055afe90bbdb095a29ee8$'</automated>
  </verify>
  <acceptance_criteria>
    - `schema/vendored/core-2026-07-28/schema.ts` SHA256 is exactly `742750af0bb8c716e7030c4977c992b55d1adc4407e9e66997db5846baedc2cd`
    - `schema/vendored/core-2026-07-28/schema.json` SHA256 is exactly `ef70b61f99b6d2e5e3b46863822eab08dff6a45bedc7a08914e0e5b133f40203`
    - `git hash-object schema/vendored/core-2026-07-28/schema.ts` outputs `9b55feeb412bc3ae877f2eac10b5c01ba29a2eed`
    - `git hash-object schema/vendored/core-2026-07-28/schema.json` outputs `213c58f6d9a1c2ce6ad055afe90bbdb095a29ee8`
    - `wc -c schema/vendored/core-2026-07-28/schema.ts` is 98426 and `wc -c schema/vendored/core-2026-07-28/schema.json` is 181474
    - `grep -c 'CacheableResult' schema/vendored/core-2026-07-28/schema.ts` returns a value >= 6
    - `PROVENANCE.md` contains the literal string `271ecc9accafdd9b83a3c869fa67c22953b2af80`
    - `PROVENANCE.md` contains both computed SHA256 digests, each on its own table row
    - `PROVENANCE.md` contains the headings `## Source`, `## Vendored files`, `## Reproducing this fetch`, `## Change protocol`, and `### Schema shape notes for readers`
    - `PROVENANCE.md` contains the literal strings `$defs` and `resultType` inside the shape-notes subsection
    - `grep -n 'exclude' Cargo.toml` shows no `schema` entry added by this task
    - `ls schema/vendored/core-2026-07-28/` lists exactly three entries (no `schema.mdx`, no `examples/`)
  </acceptance_criteria>
  <done>The 2026-07-28 core schema is in-tree, byte-pinned to a named upstream commit, digest-recorded two independent ways with both digests known in advance, and attributed by a PROVENANCE.md in the repo's established format.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize the provenance tripwire to every tree under schema/vendored/</name>
  <files>tests/vendored_schema_provenance.rs</files>
  <read_first>
    - tests/vendored_schema_provenance.rs — read in full (the whole instrument is being reshaped);
      note the `FAILURE MODE 1/2/3` + `WHAT TO DO:` failure-message voice at :268-286
    - schema/vendored/core-2026-07-28/PROVENANCE.md — the record this test must now also cover
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
      § Pitfall 8 — the exact reason a green run after adding a tree is the FAILURE, not the success
  </read_first>
  <action>
Replace the single hardcoded `const VENDORED_DIR: &str = "schema/vendored/ext-tasks"` (:62) with a runtime enumeration of every immediate subdirectory of `schema/vendored/`, and reshape the five existing tests into a per-directory loop.

Concretely:
- Add `const VENDORED_ROOT: &str = "schema/vendored";` and a `fn vendored_trees() -> Vec<PathBuf>` that `read_dir`s `VENDORED_ROOT`, keeps directories only, and sorts by path for deterministic failure ordering.
- Add `const MINIMUM_VENDORED_TREES: usize = 2;` as the new anti-vacuity floor (ext-tasks + core-2026-07-28), and assert it inside `vendored_trees()` with a message naming the trees actually found. Keep `MINIMUM_VENDORED_FILES: usize = 2` but apply it PER TREE.
- Keep `PROVENANCE_FILE`, `discover_vendored_files`, `sha256_of`, `is_lower_hex`, `maximal_hex_runs` and `recorded_digests` byte-for-byte as they are. Only `vendored_dir()`, `provenance_path()` and `read_provenance_or_fail()` change shape — they take a `&Path` tree argument instead of reading a global constant.
- Keep all five test function names EXACTLY as they are so the existing `test(/vendored_schema/)` selector and any external references keep working; each now iterates `vendored_trees()`.
- Every assertion message must name the offending TREE as well as the file, and must preserve the existing `FAILURE MODE n:` + `WHAT TO DO:` two-part voice. The failure message is this file's load-bearing property — do not compress it.

Add ONE new test, `vendored_schema_every_tree_is_covered_and_the_scan_is_not_vacuous`, that asserts `vendored_trees().len() >= MINIMUM_VENDORED_TREES` and that the discovered tree names contain both `ext-tasks` and `core-2026-07-28`. This is the assertion whose absence is the entire Pitfall-8 defect: without it a future third tree can be added and silently go unverified.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'binary(vendored_schema_provenance)'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full -E 'binary(vendored_schema_provenance)'` exits 0 and its Summary line reports at least 6 tests run (zero-selection is a FAILURE, not a pass — 115-RESEARCH Pitfall 4)
    - `grep -c '"schema/vendored/ext-tasks"' tests/vendored_schema_provenance.rs` returns 0 — the hardcoded single-tree path is gone
    - `tests/vendored_schema_provenance.rs` contains `MINIMUM_VENDORED_TREES`
    - `tests/vendored_schema_provenance.rs` contains a test function named `vendored_schema_every_tree_is_covered_and_the_scan_is_not_vacuous`
    - All five pre-existing test function names still exist verbatim in the file
    - Negative control: temporarily `mv schema/vendored/core-2026-07-28/PROVENANCE.md /tmp/` and re-run — the suite MUST fail naming `core-2026-07-28`; restore the file afterwards and transcribe the observed message into the SUMMARY
  </acceptance_criteria>
  <done>Adding a vendored tree without a PROVENANCE.md, or with a stale digest, fails the suite by name. A green run after adding a tree now means the tree was checked, not skipped.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive the CacheableResult contract — including ttlMs's type — from the pinned artifact</name>
  <files>tests/v2_core_schema_facts.rs</files>
  <read_first>
    - schema/vendored/core-2026-07-28/schema.ts — locate `interface CacheableResult` and every
      `extends ... CacheableResult` declaration
    - schema/vendored/core-2026-07-28/schema.json — resolve `/$defs/CacheableResult` and read its
      `required` array, its `cacheScope.enum`, and its `ttlMs.type` / `ttlMs.minimum`
    - This plan's `<measured_schema_facts>` block — the EXPECTED values, and the two pointer/required
      corrections that the pre-review plan set got wrong
    - tests/v2_tasks_tripwires.rs lines 82-152 — `repo_root`, `rel`, `read`, and the
      runtime-discovery-over-hardcoded-lists doctrine; RESTATE these primitives rather than trying
      to import them (a Rust integration test is its own crate)
    - tests/vendored_schema_provenance.rs :95-102 — the "read at RUNTIME, not `include_str!`"
      reasoning for integration tests
  </read_first>
  <action>
Create `tests/v2_core_schema_facts.rs`. Its job is to make the wire facts the rest of Phase 115 implements FAIL LOUDLY if a re-vendoring changes them — the D-14 re-derivation obligation, and the direct answer to Pitfall 10 ("treating the CONTEXT table as verified").

Read both vendored files at runtime with `read_to_string` (not `include_str!`), from paths built off `env!("CARGO_MANIFEST_DIR")`. Prefix every test function with the file stem `v2_core_schema_facts_` so both `binary(v2_core_schema_facts)` and `test(/v2_core_schema_facts/)` select it.

Resolve the JSON pointer `/$defs/CacheableResult`. **It is `$defs`, not `definitions`** — the
top-level keys of `schema.json` are exactly `["$schema", "$defs"]`. Make a missing pointer a named
failure ("the vendored schema no longer defines CacheableResult at /$defs/CacheableResult") rather
than an unwrap panic, and record the resolved pointer in each test's rustdoc.

Assert exactly these facts:

1. `v2_core_schema_facts_cacheable_result_requires_all_three_fields` — the `CacheableResult`
   definition's `required` array, sorted, equals `["cacheScope", "resultType", "ttlMs"]`. The
   failure message must state that `resultType` belongs to the same base and is already implemented
   by Phase 114's `inject_v2_result_envelope`, so a two-element expectation is the reader's error and
   not the schema's.

2. `v2_core_schema_facts_cache_scope_is_the_closed_public_private_union` — the `cacheScope`
   property's `enum`, sorted, equals `["private", "public"]`, and its `type` is `"string"`. This is
   what discharges D-09's retired risk; if a re-vendor widens it, the Rust enum in 115-05 is wrong
   and this must say so.

3. `v2_core_schema_facts_ttl_ms_is_a_nonnegative_integer` — the `ttlMs` property's `type` is exactly
   `"integer"` and its `minimum` is exactly `0`. **This is the assertion the cross-AI review demanded
   and the pre-review plan set lacked.** Its rustdoc must state the reasoning chain explicitly: the
   TypeScript source says `number`, which admits fractions; the GENERATED JSON Schema — the artifact a
   peer actually validates against — narrows it to `integer` with `minimum: 0`; therefore `u64` is a
   MEASURED mapping rather than an inference from an `@minimum 0` annotation. The failure message must
   say that if a re-vendor changes `type` to `"number"`, `u64` in `src/types/caching.rs` can reject a
   conformant peer's response and the Rust representation must change with it — never this assertion.

4. `v2_core_schema_facts_exactly_six_definitions_extend_cacheable_result` — collect every `$defs`
   entry (other than `CacheableResult` itself) whose `properties` map contains a `cacheScope` key,
   and assert the sorted name set equals exactly
   `["DiscoverResult", "ListPromptsResult", "ListResourceTemplatesResult", "ListResourcesResult", "ListToolsResult", "ReadResourceResult"]`.
   Cross-check the same set against `schema.ts` by scanning for
   `export interface <Name> extends ...CacheableResult` (the `extends` clause may list
   `PaginatedResult` first) and assert the two derivations AGREE — a `.json`/`.ts` disagreement is
   itself a finding. The failure message must state that the phase requirement text says "five" and
   that `DiscoverResult` is the measured sixth (115-RESEARCH § Finding 5), so a reader who hits this
   knows which side to trust.

5. `v2_core_schema_facts_each_of_the_six_requires_both_hints` — for each of the six names from test
   4, its own `required` array contains all of `cacheScope`, `resultType` and `ttlMs`. This is what
   makes D-07's "required on the v2 projection" a measured claim about each concrete result type
   rather than about the shared base only.

6. `v2_core_schema_facts_structured_content_is_any_json_value` — from `schema.json`:
   `/$defs/CallToolResult/properties/structuredContent` exists, carries a `description`, and has NO
   `type` key and no `properties` key (i.e. it is unconstrained). From `schema.ts`: the v1 sentence
   `Currently restricted to` does NOT appear anywhere in the file. This is SCHM-02's premise,
   asserted rather than assumed.

7. `v2_core_schema_facts_output_schema_declares_an_optional_dollar_schema` — from `schema.json`:
   `/$defs/Tool/properties/outputSchema` has `type == "object"` and its `properties` map contains a
   `$schema` key of `type == "string"`, and `$schema` is NOT in its `required` array (or there is no
   `required` array). This is what makes D-02 spec-aware — the spec itself declares the `$schema` key
   that the 2020-12 pin chooses to ignore — rather than a workaround.

8. `v2_core_schema_facts_the_scan_is_not_vacuous` — assert both vendored files are non-empty, that
   `schema.ts` is over 90,000 bytes and `schema.json` over 150,000 bytes, that the top-level key set
   of `schema.json` is exactly `["$defs", "$schema"]` when sorted, that `$defs` has more than 100
   entries, and that the `cacheScope`-property scan in test 4 found at least one match before any set
   comparison ran. A scanner that silently finds nothing must fail here, not pass everywhere else.

Every failure message names the remedy: re-run the `## Change protocol` in
`schema/vendored/core-2026-07-28/PROVENANCE.md` and re-derive the Rust side, rather than editing the
assertion to match.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'binary(v2_core_schema_facts)'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full -E 'binary(v2_core_schema_facts)'` exits 0 and its Summary line reports exactly 8 tests run
    - `tests/v2_core_schema_facts.rs` contains the literal strings `$defs`, `cacheScope`, `ttlMs`, `resultType`, `DiscoverResult`, `integer` and `core-2026-07-28`
    - `grep -c 'definitions' tests/v2_core_schema_facts.rs` returns 0 — the wrong pointer spelling is not present anywhere, including in comments
    - `grep -c 'include_str!' tests/v2_core_schema_facts.rs` returns 0 — the artifact is read at runtime so a re-vendoring moves the assertions without a rebuild
    - Every test function name in the file begins with `v2_core_schema_facts_`
    - Negative control A: temporarily edit a COPY of the vendored `schema.json` so `CacheableResult.required` lists only `ttlMs`, point the test at it; the suite MUST fail on test 1 naming the JSON pointer; restore afterwards
    - Negative control B: temporarily edit a COPY so `ttlMs.type` is `"number"`; the suite MUST fail on test 3 with a message naming `u64` and `src/types/caching.rs`; restore afterwards
    - Both observed failure messages are transcribed into the SUMMARY
    - `cargo fmt --all -- --check`, `make lint` and `make check-todos` all exit 0
    - `git diff --stat -- src/ Cargo.toml` is EMPTY — this plan changes zero production bytes
  </acceptance_criteria>
  <done>Every wire fact SCHM-01/02/03 is built on — including `ttlMs`'s actual JSON Schema type and minimum — is asserted against the pinned artifact. A future re-vendoring that changes any of them breaks this test instead of silently invalidating the implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub → repo | An unauthenticated HTTPS fetch of a spec artifact crosses into the build tree |
| vendored artifact → tests | Test assertions in later plans derive their expected values from these bytes |
| vendored artifact → Rust type choice | `ttlMs`'s JSON Schema type decides whether `u64` can deserialize the full protocol range |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-07 | Tampering | `schema/vendored/core-2026-07-28/` | mitigate | Fetch pinned to the 40-char commit `271ecc9accafdd9b83a3c869fa67c22953b2af80`, never `main`; SHA256 asserted for BOTH files against values measured before the fetch; git blob SHA-1 cross-checked against the GitHub contents API for both; `tests/vendored_schema_provenance.rs` re-verifies every digest at runtime |
| T-115-08 | Repudiation | `PROVENANCE.md` | mitigate | Records fetch date, commit subject, committer date, prior commit on the path, and a runnable reproduce block — the fetch is replayable by a third party |
| T-115-09 | Tampering | provenance tripwire coverage | mitigate | `MINIMUM_VENDORED_TREES` anti-vacuity floor + a dedicated test asserting both tree names are discovered; a scanner that finds nothing fails instead of passing |
| T-115-33 | Tampering | Rust representation of `ttlMs` | mitigate | `v2_core_schema_facts_ttl_ms_is_a_nonnegative_integer` asserts the MEASURED `type: "integer"` / `minimum: 0` from the pinned artifact, with a negative control proving it fires on `"number"`. A re-vendor that widens the type breaks this test rather than silently leaving `u64` able to reject a conformant peer. |
| T-115-SC | Tampering | package installs | mitigate | This plan installs NO package. `115-RESEARCH.md` § Package Legitimacy Audit records `slopcheck install jsonschema` → `[OK]` for the single dependency this phase touches (bumped in 115-03, not here). No `[ASSUMED]` / `[SUS]` / `[SLOP]` entries exist, so no blocking legitimacy checkpoint is required. Do NOT run `slopcheck install` inside this repo — it invokes `cargo add`. |
</threat_model>

<verification>
- `cargo nextest run --features full -E 'binary(vendored_schema_provenance)'` — green, >=6 tests
- `cargo nextest run --features full -E 'binary(v2_core_schema_facts)'` — green, exactly 8 tests
- `shasum -a 256 schema/vendored/core-2026-07-28/*` matches both recorded digests
- `git status --short schema/` shows only the three intended new files
- `git diff --stat -- src/ Cargo.toml` is EMPTY
- `cargo fmt --all -- --check`, `make lint`, `make check-todos` — all green
</verification>

<success_criteria>
- The 2026-07-28 core schema is vendored at a pinned commit with a PROVENANCE.md in the repo's established format, both digests verified against pre-known values
- The provenance tripwire covers `schema/vendored/*` rather than one hardcoded path, and says so with an anti-vacuity floor
- The `CacheableResult` required-key set (three entries), the `cacheScope` variant set, **`ttlMs`'s `type: "integer"` / `minimum: 0`**, the six extending result types, `structuredContent: unconstrained` and `outputSchema.$schema` are all asserted against the pinned bytes
- No `Cargo.toml` `exclude` entry was added for `schema/`
- Zero production bytes changed
</success_criteria>

<output>
Create `.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-01-SUMMARY.md` when done.
Record in it: confirmation that both SHA256 digests matched the pre-known values; whether the
six-extender set matched exactly; the measured `ttlMs.type` and `minimum`; and the observed failure
messages from all three negative controls.
</output>
