---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-course/src/part8-advanced/ch21-tasks.md
  - pmcp-course/src/part8-advanced/ch21-01-lifecycle.md
  - pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md
  - pmcp-course/src/part8-advanced/ch21-exercises.md
  - pmcp-course/src/quizzes/ch21-tasks.toml
autonomous: true
requirements: []
tags: [docs, course, tasks, refresh]
must_haves:
  truths:
    - "Every protocol-version string, type name, method signature, and import path in the four Tasks course pages matches the current `pmcp-tasks` and `pmcp` crate API surface."
    - "Every behavioral-prose claim about Tasks (SSE, serverless, owner binding, experimental.tasks, TaskSupport::*, tasks/result, tasks/cancel, tasks/get, poll interval, pollInterval, CreateTaskResult) still accurately describes current `pmcp-tasks` behavior (revision R-5 — prose drift, not just type-name drift)."
    - "The structural narrative of all four chapters is preserved — no full rewrites, no section reordering, no pedagogy changes."
    - "The Tasks quiz (ch21-tasks.toml) exists with light refresh — only questions whose answers contradict current API behavior are updated; everything else stays. If the file does not exist, it is created with the bare minimum to honor CONTEXT.md D-09. The TOML parses cleanly under Python `tomllib` (revision R-3)."
    - "Structure-preserving verify blocks use BASELINES recorded in Task 1 (pre-edit wc -l, pre-edit exercise count, pre-edit H1) rather than hardcoded labels or line-count thresholds — so a successful no-op patch passes verify."
  artifacts:
    - path: pmcp-course/src/part8-advanced/ch21-tasks.md
      provides: "Tasks course landing chapter — targeted refresh."
      min_lines: 100
    - path: pmcp-course/src/part8-advanced/ch21-01-lifecycle.md
      provides: "Tasks course lifecycle sub-chapter — targeted refresh."
      min_lines: 250
    - path: pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md
      provides: "Tasks course capability-negotiation sub-chapter — targeted refresh."
      min_lines: 200
    - path: pmcp-course/src/part8-advanced/ch21-exercises.md
      provides: "Tasks course exercises page — targeted drift correction only; existing exercise set stays intact."
      min_lines: 150
    - path: pmcp-course/src/quizzes/ch21-tasks.toml
      provides: "Tasks course quiz — light refresh of any drifted answers; created with minimal 4-6 questions if it does not exist. TOML parses under Python `tomllib` (revision R-3)."
      min_lines: 40
  key_links:
    - from: pmcp-course/src/part8-advanced/ch21-tasks.md
      to: crates/pmcp-tasks/src/lib.rs
      via: "Type names + method signatures match live source"
      pattern: "TaskStore|TaskContext|InMemoryTaskStore"
---

<objective>
Targeted refresh of `pmcp-course/src/part8-advanced/ch21-tasks.md` + its three sub-chapters (`ch21-01-lifecycle.md`, `ch21-02-capability-negotiation.md`, `ch21-exercises.md`) and a light quiz refresh (`ch21-tasks.toml` — create with bare minimum if it does not exist; refresh-in-place if it does). Per CONTEXT.md D-03 and D-09: drift correction only, NO full rewrite, NO new exercise content (existing exercise set stays intact per D-09 "Tasks = light quiz refresh only"). Per revision R-5, drift correction explicitly includes behavioral-prose drift on the 12-term search list (SSE, serverless, owner binding, experimental.tasks, TaskSupport::Required, TaskSupport::Optional, tasks/result, tasks/cancel, tasks/get, poll interval, pollInterval, CreateTaskResult).

Revision B-3 + W-6 fix: Task 1 records pre-edit baselines (`wc -l`, exercise count, H1 line) for every file Tasks 2-4 touch. Tasks 2-4 use structure-preserving verify checks that reference those baselines instead of hardcoded thresholds or hardcoded label strings. This ensures a successful no-op patch (zero drift detected) passes verify, while still catching destructive edits (deleted exercises, removed H1, line-count blow-up).

Purpose: The Tasks course pages are structurally sound but predate the v2 protocol bump and current `pmcp-tasks` crate API. Targeted patches close the drift without re-litigating chapter pedagogy.

Output:
- `ch21-tasks.md` patched in place (minimal edits).
- `ch21-01-lifecycle.md` patched in place (minimal edits).
- `ch21-02-capability-negotiation.md` patched in place (minimal edits).
- `ch21-exercises.md` patched in place (drift-only; existing exercises stay).
- `ch21-tasks.toml` created or patched, TOML-parse-verified (revision R-3).
- A baselines file at `/tmp/81-06-baselines.txt` recording pre-edit measurements for all four pages.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md
@pmcp-course/src/part8-advanced/ch21-tasks.md
@pmcp-course/src/part8-advanced/ch21-01-lifecycle.md
@pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md
@pmcp-course/src/part8-advanced/ch21-exercises.md
@pmcp-course/src/quizzes/ch20-mcp-apps.toml
@crates/pmcp-tasks/src/lib.rs
@crates/pmcp-tasks/tests/lifecycle_integration.rs
@crates/pmcp-tasks/tests/workflow_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all four Tasks course pages for v2 drift (including behavioral-prose drift per revision R-5) + RECORD PRE-EDIT BASELINES + confirm quiz existence</name>
  <files>/tmp/81-06-baselines.txt (a new scratch file recording pre-edit measurements)</files>
  <read_first>
    - All four pages: `ch21-tasks.md` (~7.6K), `ch21-01-lifecycle.md` (~14.7K), `ch21-02-capability-negotiation.md` (~11.8K), `ch21-exercises.md` (~8K). Read each in full.
    - Apply the same drift-categorization framework as plan 81-03 Task 1: protocol version strings, type names (TaskStore, InMemoryTaskStore, TaskContext, TaskSupport, ToolExecution, CreateTaskResult, CallToolResult), method signatures (`pub fn` lines in `crates/pmcp-tasks/src/`), import paths (use lines in snippets), JSON examples (cross-check against `tests/lifecycle_integration.rs` and `tests/workflow_integration.rs`), capability response examples.
    - `ls pmcp-course/src/quizzes/` — confirm whether `ch21-tasks.toml` exists. (Earlier listing showed only `ch20-mcp-apps.toml` in that directory for Part VIII — likely the Tasks quiz does NOT exist.)
    - `crates/pmcp-tasks/src/lib.rs` — read for pub re-exports.

    **Behavioral-prose drift search list (revision R-5 — Codex MEDIUM):** The audit must specifically search for these terms across ALL FOUR course pages and verify each occurrence's surrounding sentence still matches current `crates/pmcp-tasks/src/lib.rs` behavior. These terms identify behavioral claims that the type-name + signature audit does NOT catch:

    ```
    SSE
    serverless
    owner binding
    experimental.tasks
    TaskSupport::Required
    TaskSupport::Optional
    tasks/result
    tasks/cancel
    tasks/get
    poll interval
    pollInterval
    CreateTaskResult
    ```

    For each term, per page:
    1. `grep -n '<term>' pmcp-course/src/part8-advanced/<page>.md` to enumerate occurrences.
    2. For each occurrence, read the surrounding sentence (and 1-2 sentences of context).
    3. Cross-check the behavioral claim against `crates/pmcp-tasks/src/lib.rs` (and the two integration tests under `crates/pmcp-tasks/tests/`). Examples of drift to surface:
       - Chapter says "SSE is the default transport for tasks notifications" but `lib.rs` now defaults to polling → log a drift entry.
       - Chapter says "the default poll interval is 5 seconds" but the constant changed → log a drift entry.
       - Chapter says "`TaskSupport::Optional` means the tool returns synchronously by default" but current semantics differ → log a drift entry.
    4. Record each drift entry as `BEHAVIORAL-PROSE DRIFT: <file>:<line> — chapter says "X" but crate behavior is "Y"`.

    Build internal drift list (one section per file). Categorize each entry. Note explicitly if the quiz file does not exist (this changes Task 5's behavior from "patch existing" to "create minimal").
  </read_first>
  <action>
    Two outputs from this task:

    **Output A — Internal drift list.** Per plan 81-03 Task 1 discipline: enumerate factual errors only. Do NOT enumerate expansion opportunities. The drift list MUST include results from the 12-term behavioral-prose search list (revision R-5) across all four pages. A healthy outcome is 5-30 drift items total across the four pages (revised up from earlier estimate since the R-5 search list adds candidates). Zero is possible. More than 50 escalates to a Phase 81 audit gap note.

    For each of the 12 terms, record per-page occurrence counts in the Output B baselines file (see below) so the SUMMARY has a durable audit trail that the search was performed, not silently skipped.

    **Output B — Pre-edit baselines file** at `/tmp/81-06-baselines.txt`. This file is the load-bearing input for Tasks 2-4's structure-preserving verify blocks (revision B-3 + W-6 fix). Run these measurements BEFORE any edits and write them into the baselines file in this exact format:

    ```bash
    # Generate baselines file (run during Task 1 before Tasks 2-4 begin editing)
    {
      echo "# Pre-edit baselines for plan 81-06 — used by Tasks 2-4 structure-preserving verify"
      echo ""
      for f in pmcp-course/src/part8-advanced/ch21-tasks.md \
               pmcp-course/src/part8-advanced/ch21-01-lifecycle.md \
               pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md \
               pmcp-course/src/part8-advanced/ch21-exercises.md; do
        echo "FILE=$f"
        echo "LINES=$(wc -l < "$f")"
        echo "H1=$(head -1 "$f")"
        echo "H2_COUNT=$(grep -c '^## ' "$f")"
        if [ "$f" = "pmcp-course/src/part8-advanced/ch21-exercises.md" ]; then
          echo "EXERCISE_COUNT=$(grep -c '^## Exercise' "$f")"
        fi
        # Per-term occurrence counts for the R-5 12-term behavioral-prose search list (audit trail)
        for term in 'SSE' 'serverless' 'owner binding' 'experimental.tasks' 'TaskSupport::Required' 'TaskSupport::Optional' 'tasks/result' 'tasks/cancel' 'tasks/get' 'poll interval' 'pollInterval' 'CreateTaskResult'; do
          COUNT=$(grep -c -F -- "$term" "$f" 2>/dev/null || echo 0)
          echo "R5_TERM_COUNT[${term}]=$COUNT"
        done
        echo ""
      done
    } > /tmp/81-06-baselines.txt
    ```

    Tasks 2-4 read this baselines file at verify time using `grep` or `awk` to extract the recorded values, then assert post-edit measurements are within tolerance (e.g., post-edit LINES within ±15 of pre-edit LINES; post-edit EXERCISE_COUNT equals pre-edit EXERCISE_COUNT; post-edit H1 line is bytewise unchanged).

    For the quiz file: confirm existence; if absent, log "ch21-tasks.toml does not exist — Task 5 creates minimal version (4-6 questions) to honor CONTEXT.md D-09."

    Same discipline as plan 81-03 Task 1: enumerate factual errors only. Do NOT enumerate expansion opportunities.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch21-tasks.md &amp;&amp; test -f pmcp-course/src/part8-advanced/ch21-01-lifecycle.md &amp;&amp; test -f pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md &amp;&amp; test -f pmcp-course/src/part8-advanced/ch21-exercises.md &amp;&amp; test -f crates/pmcp-tasks/src/lib.rs &amp;&amp; test -f /tmp/81-06-baselines.txt &amp;&amp; grep -q '^FILE=pmcp-course/src/part8-advanced/ch21-tasks.md' /tmp/81-06-baselines.txt &amp;&amp; grep -q '^FILE=pmcp-course/src/part8-advanced/ch21-01-lifecycle.md' /tmp/81-06-baselines.txt &amp;&amp; grep -q '^FILE=pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md' /tmp/81-06-baselines.txt &amp;&amp; grep -q '^FILE=pmcp-course/src/part8-advanced/ch21-exercises.md' /tmp/81-06-baselines.txt &amp;&amp; grep -q '^EXERCISE_COUNT=' /tmp/81-06-baselines.txt &amp;&amp; grep -q 'R5_TERM_COUNT' /tmp/81-06-baselines.txt</automated>
  </verify>
  <done>
    Drift list compiled per file, including 12-term behavioral-prose results (revision R-5). Quiz existence confirmed. Baselines file `/tmp/81-06-baselines.txt` exists with pre-edit `LINES`, `H1`, `H2_COUNT` for every file, `EXERCISE_COUNT` for ch21-exercises.md, AND per-term occurrence counts for the R-5 12-term search list. Executor knows whether Task 5 is "patch" or "create-minimal" mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply drift corrections to ch21-tasks.md (landing chapter)</name>
  <files>pmcp-course/src/part8-advanced/ch21-tasks.md</files>
  <read_first>
    - Drift list from Task 1 for this file specifically (including behavioral-prose drift per revision R-5).
    - `/tmp/81-06-baselines.txt` — extract pre-edit LINES and H1 for `ch21-tasks.md` (revision W-6 fix).
    - `pmcp-course/src/part8-advanced/ch21-tasks.md` line ranges flagged in the drift list.
  </read_first>
  <action>
    For each ch21-tasks.md drift item from Task 1, apply minimal patch (same discipline as plan 81-03 Task 2):
    - Protocol version strings → current value.
    - Type names → current names.
    - Method signatures → current signatures.
    - Import paths → current module paths.
    - JSON examples → current wire shape (verified against integration tests).
    - Capability response examples → current shape.
    - Behavioral-prose drift (revision R-5) → minimal sentence-level correction to match current crate behavior. Do NOT rewrite surrounding paragraphs for clarity.

    Discipline rules (same as 81-03):
    - NO section deletions / reorderings / new sections.
    - NO pedagogy changes.
    - NO new diagrams or examples.
    - Preserve voice and pacing.

    If Task 1 found zero drift for this file, this task is a no-op (the verify block below tolerates no-op by using baselines, not hardcoded thresholds).
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch21-tasks.md &amp;&amp; BASELINE_LINES=$(awk -F= '/^FILE=pmcp-course\/src\/part8-advanced\/ch21-tasks\.md$/{f=1;next} f &amp;&amp; /^LINES=/{print $2; exit}' /tmp/81-06-baselines.txt) &amp;&amp; BASELINE_H1=$(awk -F= '/^FILE=pmcp-course\/src\/part8-advanced\/ch21-tasks\.md$/{f=1;next} f &amp;&amp; /^H1=/{sub(/^H1=/,""); print; exit}' /tmp/81-06-baselines.txt) &amp;&amp; CURRENT_LINES=$(wc -l &lt; pmcp-course/src/part8-advanced/ch21-tasks.md) &amp;&amp; CURRENT_H1=$(head -1 pmcp-course/src/part8-advanced/ch21-tasks.md) &amp;&amp; [ "$(echo "$CURRENT_LINES - $BASELINE_LINES" | bc | tr -d -)" -le 15 ] &amp;&amp; [ "$CURRENT_H1" = "$BASELINE_H1" ]</automated>
  </verify>
  <done>
    Every drift item for ch21-tasks.md from Task 1 corrected (including behavioral-prose drift per revision R-5). Post-edit `wc -l` within ±15 of pre-edit baseline. H1 line bytewise unchanged (preserves "# MCP Tasks" or whatever the baseline H1 records — does not hardcode the expected string).
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply drift corrections to ch21-01-lifecycle.md and ch21-02-capability-negotiation.md</name>
  <files>pmcp-course/src/part8-advanced/ch21-01-lifecycle.md, pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md</files>
  <read_first>
    - Drift list from Task 1 for these two files (including behavioral-prose drift per revision R-5).
    - `/tmp/81-06-baselines.txt` — extract pre-edit LINES and H1 for both files.
    - Each file's flagged line ranges.
  </read_first>
  <action>
    Apply the drift corrections to both sub-chapter files using the same discipline as Task 2. Combined into one task because both are pedagogically structured the same way (sub-chapter content rather than landing index) and their drift items are likely similar (both reference `TaskStore` types, `tasks/get` wire shapes, capability JSON). Behavioral-prose drift (revision R-5) applies to both — these files are the most likely places the SSE/serverless/owner-binding/etc. terms appear.

    For each file, apply each drift item's minimal patch. Preserve all existing section headers, code-block boundaries, and explanatory prose.

    If Task 1 found zero drift for either file, that file's patch step is a no-op. Both files must still pass the verify automated check (existence + baseline-based line tolerance + H1 preservation).
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch21-01-lifecycle.md &amp;&amp; test -f pmcp-course/src/part8-advanced/ch21-02-capability-negotiation.md &amp;&amp; for F in ch21-01-lifecycle.md ch21-02-capability-negotiation.md; do BASELINE_LINES=$(awk -F= -v t="pmcp-course/src/part8-advanced/$F" '$0=="FILE="t{f=1;next} f &amp;&amp; /^LINES=/{print $2; exit}' /tmp/81-06-baselines.txt); BASELINE_H1=$(awk -F= -v t="pmcp-course/src/part8-advanced/$F" '$0=="FILE="t{f=1;next} f &amp;&amp; /^H1=/{sub(/^H1=/,""); print; exit}' /tmp/81-06-baselines.txt); CURRENT_LINES=$(wc -l &lt; "pmcp-course/src/part8-advanced/$F"); CURRENT_H1=$(head -1 "pmcp-course/src/part8-advanced/$F"); [ "$(echo "$CURRENT_LINES - $BASELINE_LINES" | bc | tr -d -)" -le 25 ] &amp;&amp; [ "$CURRENT_H1" = "$BASELINE_H1" ] || exit 1; done</automated>
  </verify>
  <done>
    Every drift item for both sub-chapter files corrected (including behavioral-prose drift per revision R-5). Each file's post-edit `wc -l` within ±25 of pre-edit baseline (slightly wider tolerance than ch21-tasks.md because these files are longer and a single JSON-shape correction can churn 5-10 lines). H1 lines bytewise unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply drift corrections to ch21-exercises.md (structure-preserving verify per revision B-3)</name>
  <files>pmcp-course/src/part8-advanced/ch21-exercises.md</files>
  <read_first>
    - Drift list from Task 1 for ch21-exercises.md (including behavioral-prose drift per revision R-5).
    - `/tmp/81-06-baselines.txt` — extract pre-edit LINES, H1, and EXERCISE_COUNT for ch21-exercises.md (these are the load-bearing baselines for the structure-preserving verify in this task).
    - Per CONTEXT.md D-09: existing exercise set stays intact. Drift corrections target specifically:
      * Wrong type names in exercise skeletons (e.g., a `TypedTool::new` signature that no longer exists).
      * Wrong import paths in exercise solutions.
      * Wrong wire shape in "Verify your solution" expected outputs.
      * Stale behavioral-prose claims (revision R-5) — e.g., a "Verify your solution" step that says "expect SSE notifications" when the current default is polling.
    - DO NOT modify exercise difficulty levels, exercise count, or exercise pedagogy.
  </read_first>
  <action>
    Apply each ch21-exercises.md drift item's minimal patch. Per D-09 "Tasks = light quiz refresh only" — exercises stay intact. The only corrections allowed are:
    - Updating type names in exercise skeleton code (e.g., the `GenerateReportArgs` struct's `TypedTool::new` signature should match current).
    - Updating import paths.
    - Updating "Verify your solution" mcp-tester output expectations if the wire shape changed.
    - Sentence-level corrections to behavioral-prose drift from the R-5 search list (revision R-5).

    Do NOT add or remove exercises. Do NOT change Difficulty labels. Do NOT change estimated time.

    If Task 1 found zero drift for this file, this task is a no-op — the structure-preserving verify below will still pass (it's a no-op tolerance check, not a hardcoded label check).
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch21-exercises.md &amp;&amp; BASELINE_LINES=$(awk -F= '/^FILE=pmcp-course\/src\/part8-advanced\/ch21-exercises\.md$/{f=1;next} f &amp;&amp; /^LINES=/{print $2; exit}' /tmp/81-06-baselines.txt) &amp;&amp; BASELINE_H1=$(awk -F= '/^FILE=pmcp-course\/src\/part8-advanced\/ch21-exercises\.md$/{f=1;next} f &amp;&amp; /^H1=/{sub(/^H1=/,""); print; exit}' /tmp/81-06-baselines.txt) &amp;&amp; BASELINE_EX_COUNT=$(awk -F= '/^FILE=pmcp-course\/src\/part8-advanced\/ch21-exercises\.md$/{f=1;next} f &amp;&amp; /^EXERCISE_COUNT=/{print $2; exit}' /tmp/81-06-baselines.txt) &amp;&amp; CURRENT_LINES=$(wc -l &lt; pmcp-course/src/part8-advanced/ch21-exercises.md) &amp;&amp; CURRENT_H1=$(head -1 pmcp-course/src/part8-advanced/ch21-exercises.md) &amp;&amp; CURRENT_EX_COUNT=$(grep -c '^## Exercise' pmcp-course/src/part8-advanced/ch21-exercises.md) &amp;&amp; [ "$(echo "$CURRENT_LINES - $BASELINE_LINES" | bc | tr -d -)" -le 15 ] &amp;&amp; [ "$CURRENT_H1" = "$BASELINE_H1" ] &amp;&amp; [ "$CURRENT_EX_COUNT" = "$BASELINE_EX_COUNT" ]</automated>
  </verify>
  <done>
    Drift corrections applied (including behavioral-prose drift per revision R-5). Structure-preserving checks pass:
    - Post-edit `wc -l` within ±15 of pre-edit baseline (no large additions/deletions).
    - H1 line bytewise unchanged (preserves whatever H1 the file shipped with — does not hardcode the expected string).
    - Post-edit `^## Exercise` count equals pre-edit count (exercises neither added nor removed).
    This replaces the previous hardcoded `**Difficulty:** Introductory` + `**Difficulty:** Intermediate` grep checks (which would false-fail on a successful no-op patch if the live file's labels differ — see revision B-3 rationale).
  </done>
</task>

<task type="auto">
  <name>Task 5: Light quiz refresh — patch or create ch21-tasks.toml (TOML-parse-verified per revision R-3)</name>
  <files>pmcp-course/src/quizzes/ch21-tasks.toml</files>
  <read_first>
    - Task 1's finding on whether `ch21-tasks.toml` exists.
    - If it exists: read it in full. Audit each [[questions]] block: does any answer contradict the current `pmcp-tasks` API behavior? E.g., a question about "what does `task_store()` return" with an outdated answer; a JSON shape question with a stale field name; a behavioral claim from the R-5 12-term list that the chapter has since corrected. Build a per-question refresh list.
    - If it does NOT exist: read `pmcp-course/src/quizzes/ch20-mcp-apps.toml` for structural reference.
    - Read the Tasks chapter pages (post-Task-2/3/4 patches) so any new quiz questions are answerable from chapter content.
  </read_first>
  <action>
    **Branch A — quiz file exists:** For each drifted [[questions]] block from Task 1's quiz audit, apply minimal patch. Replace stale answers/contexts with current API behavior (including any behavioral-prose corrections from the R-5 search list). Preserve question count, IDs, ordering, pass_threshold. Per D-09 "light quiz refresh only" — do NOT add new questions, do NOT delete existing ones, do NOT rewrite the question count.

    **Branch B — quiz file does NOT exist:** Create `pmcp-course/src/quizzes/ch21-tasks.toml` with a minimal 4-6 question set covering the Tasks chapter's core concepts. The bar is "verify reader comprehension of the load-bearing concepts," not "exhaustively quiz the chapter." Suggested topics for the minimal set (planner discretion on exact phrasing):

    1. **ShortAnswer:** What method on `Server::builder()` configures task support? Answer: `task_store`.
    2. **MultipleChoice:** Why is polling preferred over SSE for serverless deployments? Correct answer: stateless HTTP requests work across cold starts; SSE requires persistent connections incompatible with serverless. Distractors: SSE is slower / SSE requires WebSockets only / SSE uses more bandwidth.
    3. **MultipleChoice:** What does `TaskSupport::Optional` on a tool declare? Correct answer: the client MAY request task execution but may also call the tool synchronously. Distractors: task execution required / task execution forbidden / task execution only for serverless.
    4. **ShortAnswer:** Which method on `RequestHandlerExtra` lets a handler inspect whether the current request is task-augmented? Answer: `is_task_request`.
    5. **MultipleChoice:** Why does the `TaskStore` reject transitions like `completed -> working`? Correct answer: the store enforces the MCP state machine so a handler cannot corrupt task state. Distractors: it is a database constraint / clients enforce it / it is a protocol violation logged but not blocked.
    6. **MultipleChoice (optional 6th — only if Branch B chooses 6 questions):** Which crate provides `DynamoDbTaskStore` and `RedisTaskStore` for production use? Answer: `pmcp-tasks`. Distractors: `pmcp` core / `pmcp-tasks-store` / `mcp-tester`.

    Format: same TOML structure as `ch20-mcp-apps.toml`. Header:
    ```toml
    # Quiz: MCP Tasks - Long-Running Operations

    id = "ch21-tasks"
    title = "MCP Tasks: Long-Running Operations"
    lesson_id = "ch21"
    pass_threshold = 0.7
    ```

    IDs for new [[questions]] blocks: `"f21a1b2c-d3e4-5678-abcd-1234568000XX"` (XX = 01..06).

    **Per revision R-3 (Codex MEDIUM):** the TOML MUST parse cleanly under Python 3.11+ `tomllib`. The verify block uses `python3 -c 'import tomllib; tomllib.loads(open(...).read())'` to assert parse-cleanliness AND uses `grep -c '^\[\[questions\]\]'` to guard against parse-valid-but-structurally-wrong TOML.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/quizzes/ch21-tasks.toml &amp;&amp; python3 -c 'import sys; assert sys.version_info >= (3, 11), f"need Python 3.11+ for tomllib, got {sys.version_info.major}.{sys.version_info.minor}"' &amp;&amp; python3 -c 'import tomllib; tomllib.loads(open("pmcp-course/src/quizzes/ch21-tasks.toml").read())' &amp;&amp; grep -q 'id = "ch21-tasks"' pmcp-course/src/quizzes/ch21-tasks.toml &amp;&amp; grep -q 'lesson_id = "ch21"' pmcp-course/src/quizzes/ch21-tasks.toml &amp;&amp; [ "$(grep -c '^\[\[questions\]\]' pmcp-course/src/quizzes/ch21-tasks.toml)" -ge 4 ] &amp;&amp; [ "$(wc -l &lt; pmcp-course/src/quizzes/ch21-tasks.toml)" -ge 40 ]</automated>
  </verify>
  <done>
    `ch21-tasks.toml` exists with valid TOML (revision R-3 — `python3 -c 'import tomllib; tomllib.loads(...)'` exits 0), ≥ 4 [[questions]] blocks, all answers sourced from chapter content + crate source. If Branch A: question count unchanged from pre-edit. If Branch B: 4-6 minimal questions covering the core Tasks concepts.
  </done>
</task>

</tasks>

<threat_model>
Documentation-only change. No new authentication, authorization, input validation, or trust-boundary surface introduced. Threats considered:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-06-01 | Misleading | Tasks course pages drift correction | mitigate | Task 1 enumerates every drift item against the live `pmcp-tasks` crate source, including a 12-term behavioral-prose search list per revision R-5. Tasks 2-4 apply minimal patches without restructuring. Plan 81-07's grep-stable excerpt audit double-checks the final state. |
| T-81-06-02 | Scope creep | Tasks refresh | mitigate | The plan explicitly forbids section reordering, new sections, new code examples, pedagogy changes, and new exercise content. Per CONTEXT.md D-09, only the quiz gets a light refresh; exercises stay intact. |
| T-81-06-03 | Incorrect quiz answers | ch21-tasks.toml | mitigate | Every answer (whether refreshed in Branch A or fresh-authored in Branch B) is sourced from current chapter content + `crates/pmcp-tasks/src/lib.rs`. The `context` field cites the source. |
| T-81-06-04 | Verify false-positive (revision B-3 fix) | Tasks 2-4 verify blocks | mitigate | Verify blocks now reference pre-edit baselines recorded in Task 1 rather than hardcoded label strings. A successful no-op patch (zero drift) passes verify; a destructive edit (deleted exercise, removed H1, line-count blow-up) fails verify. |
| T-81-06-05 | Stale behavioral claims (revision R-5) | All four Tasks course pages | mitigate | Task 1's behavioral-prose search list explicitly covers the 12 terms Codex flagged as drift-prone. The baselines file records per-term occurrence counts as a durable audit trail. |
| T-81-06-06 | Quiz TOML structurally broken (revision R-3) | ch21-tasks.toml | mitigate | Verify block runs Python `tomllib.loads()` to assert the file parses, AND asserts ≥4 `[[questions]]` blocks exist to catch parse-valid-but-structurally-wrong TOML. |
</threat_model>

<verification>
- All four Tasks course pages exist with post-edit `wc -l` within ±15 (landing + exercises) or ±25 (sub-chapters) of pre-edit baselines recorded in `/tmp/81-06-baselines.txt`.
- Every drift item from Task 1's audit is closed in the corresponding file's patch, including behavioral-prose drift from the R-5 12-term search list.
- No section reordering anywhere (verifiable via `git diff` — no `## ` lines moved relative to neighbors).
- No new H2 sections added.
- Each file's H1 line bytewise unchanged (matches `/tmp/81-06-baselines.txt` recorded H1).
- ch21-exercises.md `^## Exercise` count matches recorded `EXERCISE_COUNT` (no exercises added or removed).
- `ch21-tasks.toml` exists with ≥ 4 questions and valid TOML — `python3 -c 'import tomllib; tomllib.loads(...)'` exits 0 (revision R-3).
- The baselines file records per-term occurrence counts for the R-5 12-term behavioral-prose search list (audit trail).
- No `rust,no_run` blocks added anywhere (doctest is book-Skills-only per D-10).
- mdBook build (plan 81-07 verifies) succeeds.
</verification>

<success_criteria>
- Every type name, method signature, import path, JSON example, and protocol version string across the four Tasks course pages matches the live codebase.
- Every behavioral-prose claim around the 12-term R-5 search list accurately describes current `pmcp-tasks` behavior.
- Chapter structure unchanged (baselines enforced via Task 1's recorded measurements).
- Tasks course quiz exists, passes light refresh, AND parses cleanly under Python `tomllib` (revision R-3).
- ROADMAP.md Phase 81 progress increments by 1 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-06-SUMMARY.md` recording:
- Per-file drift item count + per-item before/after snippets, categorized including the new behavioral-prose category from revision R-5.
- Contents of `/tmp/81-06-baselines.txt` quoted into the SUMMARY (per-file pre-edit LINES, H1, H2_COUNT, EXERCISE_COUNT, R5_TERM_COUNT[*]) so the audit trail is durable beyond the /tmp file's lifetime.
- Post-edit measurements for the same fields, with deltas vs. baselines.
- Whether Task 5 ran in Branch A (patch existing) or Branch B (create minimal) mode.
- Confirmation that `python3 -c 'import tomllib; tomllib.loads(...)'` exited 0 for ch21-tasks.toml (revision R-3).
- Final line counts for all five files (4 chapter pages + 1 quiz).
- Any "needs rewrite" sections logged as follow-on items (should be zero in a healthy outcome).
</output>
