---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-course/src/part8-advanced/ch22-code-mode.md
  - pmcp-course/src/part8-advanced/ch22-exercises.md
  - pmcp-course/src/quizzes/ch22-code-mode.toml
autonomous: true
requirements: []
tags: [docs, course, code-mode, exercises, quiz]
must_haves:
  truths:
    - "Course reader sees #[derive(CodeMode)] as the canonical happy path within the first content section, not buried after manual handler registration."
    - "Course reader follows the same s41_code_mode_graphql.rs worked example as the book chapter's Code Mode rewrite (D-08 reference style consistency)."
    - "Course reader has a comprehension quiz (ch22-code-mode.toml) — newly authored since CONTEXT.md indicates none exists today — and the TOML parses cleanly via `python3 -c 'import tomllib'` (revision R-3)."
    - "Course reader has an exercises page (ch22-exercises.md) — newly authored to match D-09's exercises-and-quiz mandate, since inspection found none exists today; every exercise has a falsifiable Verify-your-solution section (revision R-9)."
    - "All existing inline content from the current ch22-code-mode.md that remains accurate (HMAC pipeline, config.toml deployment, security properties) is preserved or rewritten against current crate sources."
  artifacts:
    - path: pmcp-course/src/part8-advanced/ch22-code-mode.md
      provides: "Code Mode course chapter — full rewrite against current pmcp-code-mode / pmcp-code-mode-derive API surface."
      min_lines: 300
      contains: "#[derive(CodeMode)]"
    - path: pmcp-course/src/part8-advanced/ch22-exercises.md
      provides: "Code Mode course exercises page — three exercises spanning Introductory / Intermediate / Advanced, mirroring ch21-exercises.md structure; each with falsifiable Verify-your-solution section (revision R-9)."
      min_lines: 80
      contains: "## Exercise 1"
    - path: pmcp-course/src/quizzes/ch22-code-mode.toml
      provides: "Code Mode course quiz — .ai.toml format matching ch20-mcp-apps.toml structure; TOML parses cleanly (revision R-3)."
      min_lines: 40
      contains: "id = \"ch22-code-mode\""
  key_links:
    - from: pmcp-course/src/part8-advanced/ch22-code-mode.md
      to: examples/s41_code_mode_graphql.rs
      via: "Inline excerpts + 'Full example:' GitHub permalink"
      pattern: "examples/s41_code_mode_graphql.rs"
    - from: pmcp-course/src/part8-advanced/ch22-code-mode.md
      to: crates/pmcp-code-mode/src/lib.rs
      via: "Type names and re-exports match the live crate"
      pattern: "pmcp_code_mode::"
    - from: pmcp-course/src/part8-advanced/ch22-exercises.md
      to: pmcp-course/src/part8-advanced/ch22-code-mode.md
      via: "Each exercise references concepts from the chapter"
      pattern: "ch22-code-mode.md"
---

<objective>
Full rewrite of `pmcp-course/src/part8-advanced/ch22-code-mode.md` (currently 223 lines) against the current API surface (D-02), AND author a comprehension quiz `ch22-code-mode.toml` AND a new exercises page `ch22-exercises.md` (D-09 — refresh exercises and quiz to match the derive-macro rewrite; CONTEXT.md says these exist but inspection shows none in `pmcp-course/src/quizzes/` or `pmcp-course/src/part8-advanced/` — so this plan creates them fresh, applying "refresh = create when missing" consistently to BOTH artifacts per the revision-mode B-1 directive). The course chapter mirrors the BOOK Code Mode rewrite (plan 81-02) for content per D-08, with course-style framing (Learning Objectives, Knowledge Check, exercise references) added.

NOTE on SUMMARY ownership (revision B-1): Plan 81-04 Task 4 owns the SUMMARY.md insertion for `ch22-exercises.md`. This plan creates the FILE but does NOT touch SUMMARY.md. Single-edit ownership of SUMMARY.md prevents merge conflicts.

Purpose: The current course chapter is 223 lines and predates v2 conventions. It still teaches the derive macro but is light on the validation pipeline and missing the worked example structure that the book chapter rewrite adopts. The rewrite restructures around the s41_code_mode_graphql.rs worked example. Both quiz and exercises are new because the inspection-found gap means CONTEXT.md D-09's "refresh" reduces to "create" for both artifacts — applying the rule consistently rather than only to the quiz.

Output:
- `pmcp-course/src/part8-advanced/ch22-code-mode.md` (~300-500 lines, rewritten).
- `pmcp-course/src/part8-advanced/ch22-exercises.md` (~80-160 lines, three exercises Introductory / Intermediate / Advanced, each with `### Verify your solution` per revision R-9).
- `pmcp-course/src/quizzes/ch22-code-mode.toml` (~50-100 lines, 6-8 questions, parses under Python `tomllib` per revision R-3).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md
@pmcp-course/src/part8-advanced/ch22-code-mode.md
@pmcp-course/src/part8-advanced/ch21-tasks.md
@pmcp-course/src/part8-advanced/ch21-exercises.md
@pmcp-course/src/part8-advanced/ch20-mcp-apps.md
@pmcp-course/src/quizzes/ch20-mcp-apps.toml
@pmcp-book/src/ch12-9-code-mode.md
@examples/s41_code_mode_graphql.rs
@crates/pmcp-code-mode/src/lib.rs
@crates/pmcp-code-mode-derive/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm exercises-page non-existence and quiz status</name>
  <files>(read-only — produces a finding for Task 2 + Task 4 + SUMMARY)</files>
  <read_first>
    - `ls pmcp-course/src/part8-advanced/` — confirm whether `ch22-exercises.md` exists. (Earlier discovery: it does NOT — only ch20-exercises.md and ch21-exercises.md appear in that directory.)
    - `ls pmcp-course/src/quizzes/` — confirm whether `ch22-code-mode.toml` exists. (Earlier discovery: it does NOT — only ch20-mcp-apps.toml appears in the quizzes/ directory for Part VIII.)
    - `head -50 pmcp-course/src/part8-advanced/ch22-code-mode.md` — confirm the current chapter has no exercises section and no quiz-link reference (it does not — the chapter ends with "Key Takeaways" per the earlier read).
  </read_first>
  <action>
    Internally confirm:
    - `ch22-exercises.md` does NOT exist → Task 4 creates it fresh (revision B-1 fix: applying "refresh = create when missing" rule consistently to BOTH artifacts, the same way Task 3 does for the quiz).
    - `ch22-code-mode.toml` does NOT exist → Task 3 creates it fresh.
    - Current `ch22-code-mode.md` has no exercises section.

    If any of the above turns out to differ from the inspection (e.g., a ch22-exercises.md file already exists), STOP and update the plan to "patch existing" mode rather than "create fresh" — the SUMMARY records the deviation so plan 81-04 Task 4's SUMMARY insertion remains consistent.

    Coordination note: plan 81-04 Task 4 owns the SUMMARY.md insertion for `ch22-exercises.md`. THIS plan creates the file but does NOT touch SUMMARY.md.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; ! test -f pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; ! test -f pmcp-course/src/quizzes/ch22-code-mode.toml &amp;&amp; test -f examples/s41_code_mode_graphql.rs</automated>
  </verify>
  <done>
    Inspection confirms: ch22-code-mode.md exists, ch22-exercises.md does NOT exist, ch22-code-mode.toml does NOT exist, s41_code_mode_graphql.rs exists. Plan scope: rewrite chapter + create quiz + create exercises page (per revision B-1 fix — three artifacts, not two). SUMMARY slot for ch22-exercises.md is owned by plan 81-04 Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full rewrite of ch22-code-mode.md against current API (course-style)</name>
  <files>pmcp-course/src/part8-advanced/ch22-code-mode.md</files>
  <read_first>
    - `pmcp-course/src/part8-advanced/ch21-tasks.md` — structural model for course-style chapter shape (Learning Objectives → "Why X Matters for Enterprise" → conceptual content → Knowledge Check). Mirror this voice exactly. Note: ch21-tasks.md uses sub-chapters (`ch21-01-lifecycle.md`, etc.); ch22-code-mode.md is a single-file chapter (matching ch20-mcp-apps.md / current ch22 structure). DO NOT add sub-chapters — single-file is the existing pattern (CONTEXT.md confirms current chapter is 223 lines in one file).
    - `pmcp-course/src/part8-advanced/ch20-mcp-apps.md` — single-file course chapter reference for tone calibration.
    - `pmcp-course/src/part8-advanced/ch22-code-mode.md` (current 223-line version) — read in full. Identify content to REUSE: locate the language→feature-flag table via `grep -n '| Language\|| GraphQL' pmcp-course/src/part8-advanced/ch22-code-mode.md` (revision R-6 — NOT hardcoded "lines 58-65"), the config.toml block via `grep -n '\[code-mode\]\|deployment_targets' pmcp-course/src/part8-advanced/ch22-code-mode.md` (revision R-6 — NOT hardcoded "lines 102-171"), and the Key Takeaways via `grep -n '^## Key Takeaways' pmcp-course/src/part8-advanced/ch22-code-mode.md`. Verify each accuracy against current crate sources before reusing. Identify content to REPLACE (the rest of the chapter — restructure around the s41 worked example, same way plan 81-02 restructures the book chapter).
    - `pmcp-book/src/ch12-9-code-mode.md` AFTER plan 81-02 has run — wait. Plan 81-02 and 81-05 both run in Wave 1, so neither can depend on the other's output. The course chapter must derive content from `examples/s41_code_mode_graphql.rs` directly + the existing book chapter (which is being rewritten in 81-02). Practical resolution: the course chapter and book chapter both rewrite against the same source (`examples/s41_code_mode_graphql.rs` + the crate source), so they will end up structurally similar without one depending on the other. Plan 81-07 audits cross-property consistency at the end.
    - `examples/s41_code_mode_graphql.rs` — same source-of-truth as plan 81-02. Locate the load-bearing landmarks via grep (revision R-6, NOT hardcoded line numbers): `grep -n '#\[derive(CodeMode)\]\|fn register_code_mode_tools\|--- Success Path\|--- Rejection Path' examples/s41_code_mode_graphql.rs`.
    - `crates/pmcp-code-mode/src/lib.rs` + `crates/pmcp-code-mode-derive/src/lib.rs` — same source-of-truth as plan 81-02.
    - `81-CONTEXT.md` <specifics> "Code Mode chapter — derive-macro-first framing" — same constraint as plan 81-02.
  </read_first>
  <action>
    Overwrite `pmcp-course/src/part8-advanced/ch22-code-mode.md` with a full rewrite. The chapter is single-file (no sub-chapters); structure follows ch20-mcp-apps.md / ch21-tasks.md voice.

    **H1 title:** `# Code Mode: Validated LLM Code Execution`

    **Lead paragraph (before any H2):** ~3-5 sentences. Frame: Code Mode is PMCP's framework for safely executing LLM-generated code (GraphQL, JavaScript, SQL, MCP compositions) with cryptographic approval tokens. PMCP ships Code Mode as two cooperating crates: `pmcp-code-mode` (runtime) and `pmcp-code-mode-derive` (`#[derive(CodeMode)]` macro). This chapter walks one worked example end-to-end: `examples/s41_code_mode_graphql.rs`. The FIRST appearance of `#[derive(CodeMode)]` in this chapter MUST be in this lead paragraph or in the "How Code Mode Works" section that immediately follows (revision R-2 — Audit F asserts that the first occurrence of `#[derive(CodeMode)]` precedes any phrase indicating manual handler registration; see anchors "manual handler", "without the derive", "manually register").

    **## Learning Objectives**
    Course-style — 5-7 measurable bullets:
    - Add Code Mode to any MCP server using `#[derive(CodeMode)]` with the four required field names.
    - Configure operation policies for your target system (GraphQL, JS/OpenAPI, SQL, MCP) via the `language` attribute.
    - Declare operations in `config.toml` for platform-level policy management.
    - Choose between a direct `CodeExecutor` impl and the three standard adapters (`JsCodeExecutor`, `SdkCodeExecutor`, `McpCodeExecutor`) based on backend pattern.
    - Trace a validated query from `validate_code` → HMAC token → `execute_code`, and explain why mutations are rejected before token generation under default config.
    - Apply the three layers of policy: runtime (`CodeModeConfig`), platform (`config.toml`), authorization (`PolicyEvaluator`).

    **## Why Code Mode Matters for Enterprise MCP**
    Course-style framing — 2-3 paragraphs. Pull from current ch22's three-question framing (Is it safe? Authorized? Tamper-free?) — locate via `grep -n 'safe\|authorized\|tamper' pmcp-course/src/part8-advanced/ch22-code-mode.md` (revision R-6):
    - Without Code Mode, servers hand-roll validation, token generation, and policy enforcement for every code-generating workflow.
    - Enterprise deployments need: a) consistent security boundaries across multiple servers, b) auditable policy decisions tied to user/session, c) tamper-proof binding between approved code and executed code. Code Mode provides all three.
    - One paragraph on the HMAC binding: token binds {code hash, user ID, session ID, server ID, context hash, risk level, expiry}. Any modification to the code after validation invalidates the token. Stateless — no server-side token store.

    **## How Code Mode Works (Pipeline Overview)**
    REUSE the ASCII pipeline diagram from current ch22 (the validation pipeline visualization). If current ch22 doesn't have one, REUSE the diagram from the book chapter `pmcp-book/src/ch12-9-code-mode.md` (locate via `grep -n 'validate_code → ValidationPipeline\|LLM generates code' pmcp-book/src/ch12-9-code-mode.md` — revision R-6, NOT hardcoded "lines 13-36"). The diagram is structurally correct against the current crate source.
    - One paragraph framing: pipeline stages are Parse → Security scan → Policy check → Explain → HMAC sign → User approves → Verify token → Execute. Each stage is a separate, observable step (you can intercept any of them by implementing the appropriate trait).

    **## Adding Code Mode with `#[derive(CodeMode)]`** (course-style numbered steps, mirroring the book chapter from plan 81-02 — this section is where `#[derive(CodeMode)]` MUST appear by line number BEFORE any "manual handler" / "without the derive" / "manually register" phrasing per revision R-2 Audit F check 3)

    `### Step 1: Add Dependencies` — TOML block with current pinned versions (sourced from Cargo.toml per plan 81-02 Task 1 discipline; if 81-02 has already landed and pinned versions, use the same values for consistency — but BOTH plans independently source from Cargo.toml, so they should arrive at the same answer). The `pmcp-code-mode = "..."` and `pmcp-code-mode-derive = "..."` version pins MUST be byte-equal between this chapter and `pmcp-book/src/ch12-9-code-mode.md` (revision W-4 — Audit E enforces).

    `### Step 2: Derive and Configure` — inline excerpt from `examples/s41_code_mode_graphql.rs` showing the struct definition with `#[derive(CodeMode)]` (locate via `grep -n '#\[derive(CodeMode)\]' examples/s41_code_mode_graphql.rs` and quote the whole struct + impl block, NOT hardcoded "lines 63-71" per revision R-6). Note the four required field names (`code_mode_config`, `token_secret`, `policy_evaluator`, `code_executor`).

    `### Step 3: Pick a Language` — language → feature-flag table (verify against `crates/pmcp-code-mode-derive/src/lib.rs` — match plan 81-02's table). If a language-feature table column with header "Language" (case-insensitive) is rendered in either this chapter or `pmcp-book/src/ch12-9-code-mode.md`, BOTH must contain a table with matching row count (revision R-2 — Audit F check 4 in plan 81-07).

    `### Step 4: Choose Your Executor` — same decision tree as plan 81-02 (direct impl vs. standard adapters). Inline excerpt of `GraphQLExecutor` impl from `examples/s41_code_mode_graphql.rs` (locate via `grep -n 'impl CodeExecutor for GraphQLExecutor\|impl pmcp_code_mode::CodeExecutor' examples/s41_code_mode_graphql.rs` — revision R-6, NOT hardcoded "lines 26-50"). 3-line snippet for standard adapters.

    `### Step 5: Register on the Builder and Build` — inline excerpt from `examples/s41_code_mode_graphql.rs` (locate via `grep -n 'register_code_mode_tools\|fn main' examples/s41_code_mode_graphql.rs` — revision R-6).

    **## Worked Example: GraphQL Round-Trip (`s41_code_mode_graphql.rs`)**
    Mirror the same section from plan 81-02:
    - **Success path:** inline excerpt of the success-path block (locate via `grep -n '--- Success Path' examples/s41_code_mode_graphql.rs` — revision R-6) + prose annotation.
    - **Rejection path:** inline excerpt of the rejection-path block (locate via `grep -n '--- Rejection Path' examples/s41_code_mode_graphql.rs` — revision R-6) + prose annotation. Emphasize: rejection happens *before* token minting; no token exists for rejected code.
    - One paragraph framing on the load-bearing security property.

    **## Configuration in `config.toml` (Platform-Level Policy)**
    REUSE current ch22 config.toml content (located via grep per revision R-6, NOT hardcoded "lines 102-171"). Verify against current `cargo pmcp deploy` schema. Three TOML blocks, one categorization table.
    - One paragraph framing: `config.toml` separates "what the runtime allows" from "what the platform permits"; administrators can toggle individual operations in the pmcp.run admin UI without redeploying.

    **## Policy Evaluation (Cedar / AVP / Custom)**
    Same as plan 81-02 — three subsections (Cedar, AWS Verified Permissions, Custom). Verify each feature flag against Cargo.toml.

    **## Security Properties Reference**
    Same bulleted list as plan 81-02 — tokens stateless, default TTL 5min, TokenSecret zeroize-on-drop, minimum 16-byte secret, code canonicalization.

    **## Hands-On (Run the Example)**
    Course-style "go do it" framing:
    - One paragraph: "Three follow-on exercises in `ch22-exercises.md` give you hands-on practice with the concepts above. Before you start them, run the worked example end-to-end:"
    - Commands:
      ```bash
      cargo run --example s41_code_mode_graphql --features full
      ```
    - List what to observe:
      - Success path prints `Validation: PASSED`, a risk level, an approval token prefix, and a mock result.
      - Rejection path prints `Validation: REJECTED (expected)` for a mutation, no approval token issued.
      - The example demonstrates BOTH paths in one binary run.
    - Modify-and-experiment tips: change `code_mode_config: CodeModeConfig::enabled()` to `CodeModeConfig { allow_mutations: true, ..CodeModeConfig::enabled() }` and re-run — observe that the mutation now succeeds.
    - Cross-link: "Next: [Chapter 22 Exercises](./ch22-exercises.md)."

    **## Knowledge Check** (course-style exit framing)
    - 3-4 questions:
      - What does the HMAC approval token bind to, and why does this prevent the LLM from re-using a previous token for a modified query?
      - Why is the `policy_evaluator` field required by `#[derive(CodeMode)]` even when using `NoopPolicyEvaluator` for local development?
      - When would you choose `JsCodeExecutor` vs. `SdkCodeExecutor` vs. implementing `CodeExecutor` directly?

    **Style invariants (same as plan 81-02):**
    - Same inline-excerpt + cross-link style as book Code Mode chapter (D-08).
    - All Rust snippets are `rust,ignore`. NO `rust,no_run` blocks (only Skills book chapter has a doctest per D-10).
    - Cross-link format: ``Full example: [`examples/s41_code_mode_graphql.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/s41_code_mode_graphql.rs)``.
    - If a fenced code block follows a `Full example: [path](URL)` anchor but is NOT a verbatim excerpt from the cited file (adapted/synthetic snippet for pedagogy), precede the fence with the HTML comment `<!-- synthetic -->` so 81-07 Audit A skips it.
    - Target length: 300-500 lines (delta vs. current 223).
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## Learning Objectives' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## Why Code Mode Matters for Enterprise MCP' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## How Code Mode Works' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -F -q '## Adding Code Mode with' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -F -q '#[derive(CodeMode)]' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## Worked Example' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -F -q '## Configuration in `config.toml`' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## Policy Evaluation' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## Security Properties Reference' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## Hands-On' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q '## Knowledge Check' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; grep -q 's41_code_mode_graphql.rs' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; ! grep -q 'rust,no_run' pmcp-course/src/part8-advanced/ch22-code-mode.md &amp;&amp; [ "$(wc -l &lt; pmcp-course/src/part8-advanced/ch22-code-mode.md)" -ge 300 ]</automated>
  </verify>
  <done>
    `ch22-code-mode.md` is fully rewritten with all course-style sections (Learning Objectives, Why X Matters, How It Works, Adding Code Mode, Worked Example, config.toml, Policy Evaluation, Security Properties, Hands-On, Knowledge Check). Line count ≥ 300 (delta from current 223). Leads with `#[derive(CodeMode)]` per derive-macro-first framing — its first occurrence by line number precedes any "manual handler" / "without the derive" / "manually register" phrasing (revision R-2 Audit F check 3). No `rust,no_run` blocks. Uses single-file structure (no sub-chapters) consistent with current pattern. Cross-links to `ch22-exercises.md` from Hands-On section.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author ch22-code-mode.toml quiz (TOML-parse-verified per revision R-3)</name>
  <files>pmcp-course/src/quizzes/ch22-code-mode.toml</files>
  <read_first>
    - `pmcp-course/src/quizzes/ch20-mcp-apps.toml` — structural reference for `.ai.toml` quiz format. Read same way as plan 81-04 Task 3.
    - The Code Mode course chapter content (ch22-code-mode.md from Task 2 above) — questions must be answerable from chapter content alone.
  </read_first>
  <action>
    Write `pmcp-course/src/quizzes/ch22-code-mode.toml`. Structure mirrors `ch20-mcp-apps.toml`.

    **Header block:**
    ```toml
    # Quiz: Code Mode - Validated LLM Code Execution

    id = "ch22-code-mode"
    title = "Code Mode: Validated LLM Code Execution"
    lesson_id = "ch22"
    pass_threshold = 0.7
    ```

    **Question 1 — MultipleChoice (HMAC token binding):**
    Prompt: "What does the HMAC approval token bind together to prevent tamper-after-approval attacks?"
    Distractors:
    - "The LLM model ID, the prompt text, and the temperature parameter."
    - "The server's IP address, the client's IP address, and the network protocol."
    - "The validation timestamp, the JWT issuer, and the JWT audience."
    Correct answer: "The code hash, the user ID, the session ID, the server ID, the context hash, the risk level, and the expiry timestamp."
    Context: explain that any modification to the code after validation invalidates the HMAC; tokens are stateless (no server-side store) and bound to user+session+context+code together.

    **Question 2 — ShortAnswer (derive macro attribute):**
    Prompt: "Which derive macro attribute selects the validation path (graphql / javascript / sql / mcp) at compile time? (Attribute name only.)"
    Answer: "language"
    Alternatives: ["language", "code_mode.language"]
    Context: explain `#[code_mode(context_from = "...", language = "graphql")]`. Default is `"graphql"` (no feature flag); `"javascript"` requires `openapi-code-mode`, `"sql"` requires `sql-code-mode`, `"mcp"` requires `mcp-code-mode`.

    **Question 3 — MultipleChoice (required field names):**
    Prompt: "When using `#[derive(CodeMode)]`, the annotated struct MUST contain four fields with specific names. Which set is correct?"
    Distractors:
    - "`config`, `secret`, `policy`, `executor`."
    - "`code_mode_cfg`, `hmac_secret`, `auth_policy`, `executor`."
    - "`mode_config`, `token`, `policies`, `code_runner`."
    Correct answer: "`code_mode_config`, `token_secret`, `policy_evaluator`, `code_executor`."
    Context: explain that the derive macro uses these field names by convention (v0.1.0) — renaming them breaks compilation.

    **Question 4 — MultipleChoice (rejection path):**
    Prompt: "Under the default `CodeModeConfig::enabled()`, what happens when an LLM generates a GraphQL mutation?"
    Distractors:
    - "The mutation runs but the result is filtered by the policy evaluator."
    - "The mutation succeeds and produces a token with a 'destructive' risk level."
    - "The mutation runs but its result is rolled back after execution."
    Correct answer: "Validation fails with `is_valid = false`; no approval token is generated, so the mutation cannot be executed."
    Context: explain that rejection happens *before* token minting. The default config has `allow_mutations: false`. Setting `allow_mutations: true` changes this behavior.

    **Question 5 — ShortAnswer (TokenSecret minimum length):**
    Prompt: "What is the minimum byte length enforced by `TokenSecret::new` at construction time?"
    Answer: "16"
    Alternatives: ["16", "16 bytes"]
    Context: explain that TokenSecret returns a `Result` from `new()` — a too-short secret produces an error, not a panic. Production secrets should be 32+ bytes.

    **Question 6 — MultipleChoice (three policy layers):**
    Prompt: "Code Mode has three layers of policy enforcement that operate at different times. Which list correctly orders them from compile-time/deploy-time to per-request?"
    Distractors:
    - "PolicyEvaluator → CodeModeConfig → config.toml"
    - "config.toml → PolicyEvaluator → CodeModeConfig"
    - "CodeModeConfig → PolicyEvaluator → config.toml"
    Correct answer: "config.toml (deploy time, platform-level) → CodeModeConfig (runtime, server-level) → PolicyEvaluator (per-request, authorization)."
    Context: explain that `config.toml` declares the operation surface for platform admins; `CodeModeConfig` controls runtime allowlists/blocklists/depth limits; `PolicyEvaluator` runs per-request per-user before token minting.

    **Question 7 — MultipleChoice (executor selection):**
    Prompt: "You are building a Cost Coach MCP server that makes HTTP calls to AWS Cost Explorer's REST API and reasons over the responses with LLM-generated JavaScript. Which executor adapter is the canonical choice?"
    Distractors:
    - "`McpCodeExecutor` — it routes JavaScript through MCP tool composition."
    - "`SdkCodeExecutor` — it executes JavaScript against the AWS SDK directly."
    - "A direct `CodeExecutor` impl — adapters are only for trivial demos."
    Correct answer: "`JsCodeExecutor` — it executes JavaScript that makes HTTP calls."
    Context: explain the three adapter use cases: `JsCodeExecutor` = JS + HTTP (Cost Coach); `SdkCodeExecutor` = JS + SDK (AWS SDK direct); `McpCodeExecutor` = MCP tool composition.

    **Question 8 — MultipleChoice (NoopPolicyEvaluator caveat):**
    Prompt: "Why does the s41_code_mode_graphql.rs example use `NoopPolicyEvaluator`, and what must change for production?"
    Distractors:
    - "It is the default for GraphQL servers; production uses CedarPolicyEvaluator instead."
    - "It is faster than real policy evaluators; production keeps it for low-latency paths."
    - "It is required for the validate_code/execute_code round-trip to function; production wraps it with audit logging."
    Correct answer: "It is a no-op stub for local development that allows all operations. Production servers MUST replace it with a real `PolicyEvaluator` implementation (Cedar, AVP, or custom)."
    Context: cite the warning comment in `examples/s41_code_mode_graphql.rs` (locate via `grep -n 'NoopPolicyEvaluator\|LOCAL DEV' examples/s41_code_mode_graphql.rs` — revision R-6, NOT hardcoded "lines 13 + 77-78"). NoopPolicyEvaluator is for testing/local development ONLY.

    Format: same TOML structure as ch20-mcp-apps.toml. Use UUID-like IDs: `"f22a1b2c-d3e4-5678-abcd-1234568000XX"` (XX = question number 01..08). For MultipleChoice, set `answer.position = 0` consistently or match ch20's convention (verify from reading).

    Total: 8 questions, ~90-110 lines.

    **Per revision R-3 (Codex MEDIUM):** the TOML MUST parse cleanly under Python 3.11+ `tomllib`. The verify block uses `python3 -c 'import tomllib; tomllib.loads(open(...).read())'` to assert parse-cleanliness AND uses `grep -c '^\[\[questions\]\]'` to guard against parse-valid-but-structurally-wrong TOML.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/quizzes/ch22-code-mode.toml &amp;&amp; python3 -c 'import sys; assert sys.version_info >= (3, 11), f"need Python 3.11+ for tomllib, got {sys.version_info.major}.{sys.version_info.minor}"' &amp;&amp; python3 -c 'import tomllib; tomllib.loads(open("pmcp-course/src/quizzes/ch22-code-mode.toml").read())' &amp;&amp; grep -q 'id = "ch22-code-mode"' pmcp-course/src/quizzes/ch22-code-mode.toml &amp;&amp; grep -q 'lesson_id = "ch22"' pmcp-course/src/quizzes/ch22-code-mode.toml &amp;&amp; [ "$(grep -c '^\[\[questions\]\]' pmcp-course/src/quizzes/ch22-code-mode.toml)" -ge 6 ] &amp;&amp; [ "$(wc -l &lt; pmcp-course/src/quizzes/ch22-code-mode.toml)" -ge 40 ]</automated>
  </verify>
  <done>
    `ch22-code-mode.toml` exists with `id = "ch22-code-mode"`, `lesson_id = "ch22"`, at least 6 questions (target 8), file length ≥ 40 lines. **TOML parses cleanly under Python 3.11+ `tomllib` (revision R-3) AND has at least 6 `[[questions]]` blocks — guards against both syntax errors and structurally-wrong-but-parseable TOML.** Every question answer is sourced from chapter content + crate source.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author ch22-exercises.md (Code Mode course exercises) — revision B-1 + R-9 fix</name>
  <files>pmcp-course/src/part8-advanced/ch22-exercises.md</files>
  <read_first>
    - `pmcp-course/src/part8-advanced/ch21-exercises.md` — structural reference for course exercise page format (H1, intro paragraph, exercises numbered with **Difficulty:** annotations + minutes, **Steps**, **Verify your solution**, **Questions to answer**, --- separators). Locate the structural anchors via `grep -n '^### Verify your solution\|^## Exercise\|\*\*Difficulty:\*\*' pmcp-course/src/part8-advanced/ch21-exercises.md` (revision R-6 — grep-anchored).
    - `pmcp-course/src/part8-advanced/ch20-exercises.md` — second reference (smaller) for tone calibration.
    - `pmcp-course/src/part8-advanced/ch22-code-mode.md` (the post-Task-2 rewrite) — every exercise must be answerable from chapter content.
    - `examples/s41_code_mode_graphql.rs` — the worked example each exercise builds on.
  </read_first>
  <action>
    Write `pmcp-course/src/part8-advanced/ch22-exercises.md`. Mirror the structure of `ch21-exercises.md` (intro paragraph + 3 exercises with consistent fields: **Difficulty**, **Steps**, **### Verify your solution** (H3), **Questions to answer**, --- separators).

    **H1 title:** `# Chapter 22 Exercises`

    **Intro paragraph (~2 sentences):** "These exercises build your fluency with PMCP Code Mode. Each one targets a different aspect of the `#[derive(CodeMode)]` workflow — from mechanical wiring (Exercise 1) to operating the rejection path (Exercise 2) to swapping the policy layer (Exercise 3)."

    **## Exercise 1: Wire `#[derive(CodeMode)]` into a Minimal Server**
    - **Difficulty:** Introductory (15 min)
    - Framing: practice the mechanical steps of adding Code Mode to a fresh server. Reader copies the four required field names, builds successfully, and confirms the registered `validate_code` and `execute_code` tools appear in `tools/list`.
    - **Steps:**
      1. Create a new binary project (or add to an existing test crate).
      2. Add `pmcp = { version = "...", features = ["full"] }`, `pmcp-code-mode = "..."`, `pmcp-code-mode-derive = "..."` to Cargo.toml — use the version pins from the chapter's Step 1.
      3. Define a server struct with the four required fields: `code_mode_config: CodeModeConfig`, `token_secret: TokenSecret`, `policy_evaluator: Arc<dyn PolicyEvaluator>`, `code_executor: Arc<dyn CodeExecutor>`.
      4. Annotate the struct with `#[derive(CodeMode)]` (default `language = "graphql"`).
      5. Implement a stub `CodeExecutor` that returns a `serde_json::json!({ "stub": true })` for any input.
      6. Use `NoopPolicyEvaluator` for the policy field (LOCAL DEV ONLY — chapter warning applies).
      7. Construct an instance, call `register_code_mode_tools(builder)` on `Server::builder()`, build the server.
      8. Run `mcp-tester stdio ./target/debug/your-server` and observe `validate_code` + `execute_code` in `tools/list`.
    - **### Verify your solution** (revision R-9): `tools/list` includes both `validate_code` and `execute_code` tools with the expected JSON schemas. If either is missing, the derive macro did not register both tools — recheck the `register_code_mode_tools(builder)` call.
    - **Questions to answer:**
      - What happens if you rename one of the four required struct fields (e.g., `code_mode_config` → `cfg`)? Why?
      - The `Arc<dyn PolicyEvaluator>` field needs to point at SOMETHING even for local dev. Why doesn't the derive macro provide a default?

    **## Exercise 2: Trigger and Inspect the Rejection Path**
    - **Difficulty:** Intermediate (30 min)
    - Framing: exercise the security-critical rejection path. Reader sends a GraphQL mutation through `validate_code` under the default `CodeModeConfig::enabled()` and confirms (a) no token is minted and (b) the same mutation succeeds after setting `allow_mutations: true`.
    - **Steps:**
      1. Start from the server you built in Exercise 1 (or copy `examples/s41_code_mode_graphql.rs`).
      2. Add a test (or `main()` block) that calls `validate_code` with a GraphQL mutation, e.g., `mutation { deleteUser(id: \"123\") }`.
      3. Assert that the response has `is_valid == false` and `approval_token` is empty/None.
      4. Capture the rejection reason string from the validation response.
      5. Change the config from `CodeModeConfig::enabled()` to `CodeModeConfig { allow_mutations: true, ..CodeModeConfig::enabled() }`.
      6. Re-run the same mutation. Assert `is_valid == true` and `approval_token` is non-empty.
      7. Now feed the approval token + a SLIGHTLY MODIFIED mutation (change `"123"` to `"124"`) into `execute_code`. Confirm it rejects (HMAC mismatch).
    - **### Verify your solution** (revision R-9): Tests/assertions all pass: (a) default config rejects mutation, (b) `allow_mutations: true` accepts mutation, (c) modifying the code after token mint invalidates the token. If (c) does NOT reject, your HMAC verification is bypassed — recheck `execute_code`'s token-verify path.
    - **Questions to answer:**
      - Step 7 demonstrates the load-bearing security property of HMAC binding. Describe in your own words what attack this prevents.
      - Why does Code Mode reject mutations by default rather than by opt-in? (Read CONTEXT.md or the chapter's "Why Code Mode Matters for Enterprise MCP" framing.)

    **## Exercise 3: Swap `NoopPolicyEvaluator` for a Custom `PolicyEvaluator`**
    - **Difficulty:** Advanced (60 min)
    - Framing: replace the local-dev stub with a real policy layer. Reader implements a `PolicyEvaluator` that denies operations whose user ID is `"banned-user"` and confirms denied operations never produce an approval token.
    - **Steps:**
      1. Implement a struct `BannedUserPolicyEvaluator` that implements the `PolicyEvaluator` trait.
      2. The `evaluate` method should return a `PolicyDecision::Deny { reason: "user is banned" }` when `context.user_id == Some("banned-user")` and `PolicyDecision::Allow` otherwise.
      3. Wire it into the server struct from Exercise 1 in place of `NoopPolicyEvaluator`.
      4. Write a test that calls `validate_code` with a `ValidationContext` whose `user_id` is `Some("banned-user")`. Assert no approval token is returned and the rejection reason mentions "banned".
      5. Repeat with `user_id = Some("alice")` (or any non-banned value). Assert the call succeeds with a non-empty approval token.
      6. Bonus: log the policy decision (the reason string) to stderr so the rejection path is observable.
    - **### Verify your solution** (revision R-9): Both test cases pass — banned user rejected, allowed user gets a token. The rejection log is visible. If both calls produce a token regardless of user_id, your `evaluate` method is not being invoked — recheck the policy_evaluator wiring on the struct.
    - **Questions to answer:**
      - In the production architecture, where would `PolicyEvaluator` typically run — in the same process as the server, or in a remote service? When does each choice make sense? (Hint: chapter section "Policy Evaluation (Cedar / AVP / Custom)".)
      - The chapter calls out that the policy evaluator runs *before* token generation. Why is that ordering security-critical?

    **Final note (cross-link):** Reference `examples/s41_code_mode_graphql.rs` as the canonical worked example. The first two exercises closely mirror that example; Exercise 3 extends it.

    **Style invariants:**
    - All Rust snippets are `rust,ignore` (NO `rust,no_run` blocks — doctest is book-Skills-only per D-10).
    - Difficulty labels: exactly `**Difficulty:** Introductory`, `**Difficulty:** Intermediate`, `**Difficulty:** Advanced` (match ch21-exercises.md's exact strings so verify checks pass).
    - Cross-link format: ``[Chapter 22: Code Mode](./ch22-code-mode.md)`` for back-references.
    - Target length: 80-160 lines.

    **Per revision R-9 (Codex LOW + Gemini exercise-clarity suggestion):** Every exercise MUST contain exactly one `### Verify your solution` H3 heading. The verify block enforces `grep -c '^### Verify your solution' == grep -c '^## Exercise'`.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; grep -q '## Exercise 1' pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; grep -q '## Exercise 2' pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; grep -q '## Exercise 3' pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; grep -q '\*\*Difficulty:\*\* Introductory' pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; grep -q '\*\*Difficulty:\*\* Intermediate' pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; grep -q '\*\*Difficulty:\*\* Advanced' pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; ! grep -q 'rust,no_run' pmcp-course/src/part8-advanced/ch22-exercises.md &amp;&amp; EX_COUNT=$(grep -c '^## Exercise' pmcp-course/src/part8-advanced/ch22-exercises.md) &amp;&amp; VERIFY_COUNT=$(grep -c '^### Verify your solution' pmcp-course/src/part8-advanced/ch22-exercises.md) &amp;&amp; [ "$EX_COUNT" -eq "$VERIFY_COUNT" ] &amp;&amp; [ "$VERIFY_COUNT" -ge 3 ] &amp;&amp; [ "$(wc -l &lt; pmcp-course/src/part8-advanced/ch22-exercises.md)" -ge 80 ]</automated>
  </verify>
  <done>
    `ch22-exercises.md` exists with three exercises spanning Introductory / Intermediate / Advanced, mirroring the `ch21-exercises.md` structure (Steps, **`### Verify your solution`** (H3, revision R-9), Questions to answer). Each exercise targets a different aspect of the Code Mode derive-macro flow. The count of `^### Verify your solution` headings equals the count of `^## Exercise` headings (revision R-9). File length ≥ 80 lines. No `rust,no_run` blocks. SUMMARY slot owned by plan 81-04 Task 4 (not edited here).
  </done>
</task>

</tasks>

<threat_model>
Documentation-only change. No new authentication, authorization, input validation, or trust-boundary surface introduced. Threats considered:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-05-01 | Misleading | ch22-code-mode.md security framing | mitigate | Same as plan 81-02: chapter explicitly states policy evaluator runs before token generation, tokens are HMAC-bound, no execution path bypasses token check. Sourced from `crates/pmcp-code-mode/src/lib.rs`. |
| T-81-05-02 | Misleading | NoopPolicyEvaluator usage | mitigate | The chapter (and the new quiz Question 8 + Exercise 3) surface the warning verbatim from `examples/s41_code_mode_graphql.rs`: NoopPolicyEvaluator is for testing/local development ONLY. |
| T-81-05-03 | Incorrect quiz answers | ch22-code-mode.toml | mitigate | Every answer sourced from chapter content + crate source. The `context` field cites the source location (file + line) where applicable. |
| T-81-05-04 | Inaccurate exercises | ch22-exercises.md | mitigate | Every exercise builds on `examples/s41_code_mode_graphql.rs` as the canonical reference impl. Exercise 2's HMAC-binding step exercises the actual security property; Exercise 3's custom PolicyEvaluator targets the production-replacement path the chapter warns about. |
| T-81-05-05 | Cross-plan coordination | SUMMARY.md ownership for ch22-exercises | mitigate | This plan creates the FILE; plan 81-04 Task 4 owns the SUMMARY entry. Single-edit ownership of SUMMARY.md prevents merge conflicts. Plan 81-07's SUMMARY audit verifies the entry resolves to this plan's file. |
| T-81-05-06 | Quiz TOML structurally broken (revision R-3) | ch22-code-mode.toml | mitigate | Verify block runs Python `tomllib.loads()` to assert the file parses, AND asserts ≥6 `[[questions]]` blocks exist to catch parse-valid-but-structurally-wrong TOML. |
| T-81-05-07 | Exercise verify ambiguous (revision R-9) | ch22-exercises.md | mitigate | Every `## Exercise` heading is paired with exactly one `### Verify your solution` H3 heading; the verify block asserts count parity. |
</threat_model>

<verification>
- `pmcp-course/src/part8-advanced/ch22-code-mode.md` rewritten (≥ 300 lines, delta from current 223). All mandatory H2 sections present in order.
- First substantive section after "How It Works" leads with `#[derive(CodeMode)]` — manual handler registration is not the canonical path. The first occurrence of `#[derive(CodeMode)]` by line number precedes any "manual handler" / "without the derive" / "manually register" phrasing (revision R-2 — Audit F check 3).
- Reproduced language→feature table matches `crates/pmcp-code-mode-derive/src/lib.rs`. If a Language-column table is present in both this chapter and `pmcp-book/src/ch12-9-code-mode.md`, their row counts match (revision R-2 — Audit F check 4).
- Dependency version pins match Cargo.toml AND are byte-equal between this chapter and the book chapter (revision W-4 — Audit E).
- `pmcp-course/src/quizzes/ch22-code-mode.toml` exists with ≥ 6 questions AND parses cleanly under Python `tomllib` (revision R-3).
- `pmcp-course/src/part8-advanced/ch22-exercises.md` exists with 3 exercises spanning the three difficulty levels (revision B-1), each with a `### Verify your solution` H3 (revision R-9 — count parity enforced).
- Chapter contains no `rust,no_run` blocks (course chapter is ignore-only per D-10).
- Exercises page contains no `rust,no_run` blocks.
- mdBook build (plan 81-07 verifies) succeeds.
- SUMMARY.md slot for ch22-exercises.md is owned by plan 81-04 Task 4 (not edited here).
</verification>

<success_criteria>
- Course reader of `ch22-code-mode.md` follows the same s41-worked-example structure as the book chapter (D-08 consistency).
- Course reader can take the ch22-code-mode.toml quiz (TOML parses cleanly per revision R-3) and validate comprehension.
- Course reader has three hands-on exercises (intro / intermediate / advanced) covering wiring, the rejection path, and policy-evaluator replacement; each with a falsifiable Verify-your-solution section (revision R-9).
- mdBook build succeeds for `pmcp-course/`.
- ROADMAP.md Phase 81 progress increments by 1 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-05-SUMMARY.md` recording:
- Path to rewritten chapter with new line count + delta vs. previous 223 lines.
- Path to new quiz file with question count and confirmation that `python3 -c 'import tomllib; tomllib.loads(...)'` exited 0 (revision R-3).
- Path to new exercises file with exercise count and `### Verify your solution` heading count, asserting parity (revisions B-1 + R-9).
- Catalog of inline excerpt sources (matching plan 81-02's catalog so cross-property consistency audit in 81-07 can compare). Use grep-anchored descriptions rather than hardcoded line numbers (revision R-6).
- Confirmation that no `rust,no_run` blocks were added.
- Confirmation that SUMMARY.md was NOT edited (slot for ch22-exercises.md owned by plan 81-04 Task 4).
- Resolved version pins for `pmcp-code-mode` and `pmcp-code-mode-derive` from Cargo.toml (must match those used in plan 81-02 per revision W-4 / Audit E).
</output>
