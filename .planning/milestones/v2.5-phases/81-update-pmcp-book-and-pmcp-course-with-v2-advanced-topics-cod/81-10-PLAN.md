---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 10
type: execute
wave: 2
depends_on: ["81-08", "81-09"]
files_modified: []
autonomous: true
requirements: []
tags: [docs, gap-closure, re-audit, mdbook, shippability, revision-pass-2]
gap_closure: true
must_haves:
  truths:
    - "`mdbook build` succeeds for both `pmcp-book/` and `pmcp-course/` verified by EXIT CODE captured from a non-sandboxed shell. If `mdbook` is unavailable in the executor's environment, Task 1 ABORTS with a precondition error — there is NO timestamp-evidence fallback (the closure cycle exists to retire that substitution)."
    - "Audit A under R-9 relaxations (clauses a/b/c/d/e) re-runs and reclassifies findings empirically: Findings 1, 2, 3 reclassified to PASS (R-1A.a expected); Finding 4 reclassified to WARN (R-1A.b expected); Finding 5 reclassified to WARN (R-1A.d expected — source-side cosmetic omission of source L146 comment + L169 println! without `// ...` marker yields contiguous match on elision). Actual disposition is whatever the empirical scan produces."
    - "W-7 floor: actual in-scope excerpt count N is recorded; classified PASS if N>=10, WARN if N<10 per R-1A.e. NOT a hard FAIL gate."
    - "R-1A.c contribution from the course Skills chapter (`ch23-skills.md` anchors at L175/176, L238/239, L341/342, L343/344) is empirically verified: each two-line anchor's following fenced block is examined for the source-match guard (language-fence match with `.rs` source, OR basename / function signature / struct name from the cited source appearing in the block). Blocks failing the guard are recorded OUT OF SCOPE, not FAIL."
    - "Audit E under the post-81-08 state PASSes: book and course `pmcp-code-mode = \"0.5.1\"` and `pmcp-code-mode-derive = \"0.2.0\"` pins are byte-equal."
    - "All 7 FAIL-severity findings from `81-07-AUDIT.md` are reclassified (PASS or WARN); zero FAILs remain in `81-10-AUDIT.md`."
    - "Audits B, C, D, F continue to pass (no regressions from the Wave 1 plans 81-08, 81-09 — they only touched `pmcp-book/src/ch12-9-code-mode.md` two lines and `81-07-PLAN.md` spec)."
    - "Overall Verdict in `81-10-AUDIT.md` is `PASS` (zero FAILs and zero WARNs) or `PASS WITH WARNINGS` (Finding 4 WARN, Finding 5 WARN, W-7 WARN if N<10 are the expected WARNs)."
  artifacts:
    - path: .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md
      provides: "Re-audit report under revision R-9 relaxations (clauses a/b/c/d/e) + post-81-08 pin alignment; final shippability verdict for Phase 81. Records empirical R-1A.c contribution count from course Skills chapter (with per-anchor source-match-guard outcomes)."
      min_lines: 60
      contains: "## Shippability"
  key_links:
    - from: 81-10-AUDIT.md
      to: 81-07-AUDIT.md
      via: "Re-audit explicitly references the 7 FAIL findings in 81-07-AUDIT.md and records their new empirical disposition (PASS or WARN)."
      pattern: "81-07-AUDIT|Finding [1-7]"
---

<!-- revision: addresses BLOCKER 1 (W-7 hardcoded -ge 10 verify removed; W-7 classified PASS|WARN per R-1A.e), BLOCKER 2 (Finding 5 explicit R-1A.d disposition path; source L146 + L169 check), BLOCKER 3 (mdbook fallback removed — task aborts on absent mdbook), BLOCKER 4 (R-1A.c source-match guard applied empirically per anchor), WARNING 5 (Task 2 split into 2a + 2b) -->

<objective>
Re-run the Phase 81 cross-property consistency audit after plans 81-08 (Audit E pin alignment) and 81-09 (Audit A spec relaxation per R-1A / revision R-9 clauses a/b/c/d/e) have landed. Produce a new audit report `81-10-AUDIT.md` that:

1. **Re-confirms `mdbook build` for both books via EXIT CODE from a non-sandboxed shell.** This is a precondition, not a soft requirement: if `mdbook` is unavailable in the executor's environment, Task 1 ABORTS and escalates to the orchestrator with a precondition error. There is NO timestamp-evidence fallback in this plan — that substitution is exactly what the closure cycle exists to retire (per user-locked decision).
2. **Re-runs Audit A under R-9 relaxations (clauses a/b/c/d/e)** and reclassifies each of `81-07-AUDIT.md`'s Findings 1–5 empirically (PASS / WARN / FAIL). Records the new in-scope excerpt count N (W-7 floor classified PASS if N>=10, WARN if N<10).
3. **Empirically verifies the R-1A.c contribution from the course Skills chapter.** For each of the four two-line anchors at `pmcp-course/src/part8-advanced/ch23-skills.md` L175/176, L238/239, L341/342, L343/344: locate the following fenced block within the same H2, apply the R-1A.c source-match guard (language fence consistency with `.rs` source OR basename / function signature / struct name from the cited source appearing in the block), and record per-anchor in-scope vs out-of-scope outcomes.
4. **Re-runs Audit E** under the post-81-08 state. Both pin lines MUST be byte-equal — this should now PASS.
5. **Re-runs Audits B, C, D, F** to confirm no regressions. Since plans 81-08 and 81-09 touched only `pmcp-book/src/ch12-9-code-mode.md` (two pin lines) and `81-07-PLAN.md` (spec doc), no regressions are expected.
6. **Produces a final shippability verdict** for Phase 81. With all 7 FAILs reclassified, the verdict MUST be `PASS` or `PASS WITH WARNINGS`. If any FAIL remains, the audit report explicitly identifies it and recommends a further gap-closure cycle.

Purpose: This is the closing audit for Phase 81's gap-closure cycle. The R-8 shippability gate from `81-07-AUDIT.md` ("FAIL → /gsd-plan-phase 81 --gaps before /gsd-verify-phase 81") is satisfied only when this re-audit records zero FAILs. After this plan completes, the orchestrator can route to `/gsd-verify-phase 81`.

Output:
- `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md` — new audit report.
- `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-SUMMARY.md` — one-page summary with verdict.
- No source file modifications.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-08-PLAN.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-09-PLAN.md

<interfaces>
<!-- revision: addresses BLOCKER 1, BLOCKER 2, BLOCKER 4 — describes per-finding disposition paths under R-1A clauses a/b/c/d/e (no inline spec invention; all clauses come from 81-09 amendment to 81-07-PLAN.md). -->
<!-- The seven FAIL findings in 81-07-AUDIT.md and their post-closure disposition under R-9 relaxations. -->

Finding mapping:
- 81-07-AUDIT.md Finding 1 (Audit A: book ch12-9 L246 de-indentation drift) → R-1A.a → expected PASS under R-9.
- 81-07-AUDIT.md Finding 2 (Audit A: book ch12-9 L285 de-indentation drift) → R-1A.a → expected PASS under R-9.
- 81-07-AUDIT.md Finding 3 (Audit A: book ch12-9 L343 de-indentation drift) → R-1A.a → expected PASS under R-9.
- 81-07-AUDIT.md Finding 4 (Audit A: course ch22 L196 de-indented + `// ...` placeholder) → R-1A.a + R-1A.b → expected WARN under R-9 (the placeholder triggers WARN).
- 81-07-AUDIT.md Finding 5 (Audit A: course ch22 L228 de-indented + missing source-side comment at source L146 + missing source-side println! at source L169) → R-1A.a + R-1A.d → expected WARN under R-9. Disposition rule (from 81-09 spec): the omitted source lines are non-load-bearing (L146 is a comment, L169 is a println! debug output — neither changes control-flow, struct fields, or API shape); if removing these from the source-side comparison yields a contiguous match against the de-indent-normalized block, record WARN per R-1A.d. If the match still fails after that elision, record FAIL.
- 81-07-AUDIT.md Finding 6 (Audit E: pmcp-code-mode pin drift) → 81-08 → expected PASS.
- 81-07-AUDIT.md Finding 7 (Audit E: pmcp-code-mode-derive pin drift) → 81-08 → expected PASS.

W-7 coverage floor (under R-1A.e, classified PASS if N>=10, WARN if N<10):
- 81-07-AUDIT.md recorded 5 in-scope excerpts under strict rules.
- Predicted contribution under R-1A relaxations:
  - book ch12-9-code-mode.md → 3 (Findings 1, 2, 3, now PASS in-scope).
  - course ch22-code-mode.md → 2 (Findings 4 WARN-in-scope, Finding 5 WARN-in-scope).
  - course ch23-skills.md → 0 to 4 (depends on R-1A.c source-match guard outcomes per anchor — see below).
  - book ch12-8-skills.md → 0 (anchors at L147/L203/L324 sit at section ends; under R-1A no change unless H2 boundaries re-resolved).
- **Best-case predicted total: 9** (one short of the 10 floor). **Worst-case: 5.** Plan 81-10 records the empirical N and classifies W-7 PASS or WARN accordingly per R-1A.e — there is NO hard `-ge 10` FAIL gate.

R-1A.c per-anchor source-match-guard check for `pmcp-course/src/part8-advanced/ch23-skills.md` (the four two-line anchors):
- Anchor at L175/176: cites `examples/s44_server_skills.rs` (Rust source). Next fenced block within the same H2 (Tier 2 section) is around L191–L203, a `markdown` block excerpting `examples/skills/refunds/SKILL.md` content (frontmatter + body). Language fence = `markdown` (NOT Rust); basename `s44_server_skills` does not appear; no Rust function signature or struct from `s44_server_skills.rs` appears. **Guard result: FAIL guard → OUT OF SCOPE under R-1A.c.** Contribution: 0.
- Anchor at L238/239: cites `examples/s44_server_skills.rs`. Next fenced block: examine empirically. If no fenced block follows within the same H2 (anchor sits at section end before `## Tier 3:` header), contribution = 0. If a block follows AND satisfies the guard, contribution = 1.
- Anchor at L341/342: cites `examples/s44_server_skills.rs`. The anchor at L341/342 is paired with another at L343/344 citing `examples/c10_client_skills.rs`. Both sit near the end of "Three Tiers Match Other Reference Implementations" H2 (L353). Examine empirically whether any fenced block follows within the H2 boundary, and apply the guard.
- Anchor at L343/344: cites `examples/c10_client_skills.rs`. Same H2 as L341/342; same analysis.

**Conclusion:** R-1A.c contribution from `ch23-skills.md` is empirically determined to be in the range [0, 4]. The L175/176 anchor's contribution is provably 0 (markdown block following a Rust anchor fails the guard). The other three anchors require empirical inspection. Plan 81-10 Task 2a enumerates each, records the per-anchor guard outcome, and reports the actual total in `81-10-AUDIT.md`.

Re-audit acceptance criteria:
- mdbook build EXIT=0 for both books from a non-sandboxed shell. Task ABORTS if mdbook is unavailable (NO fallback to timestamp evidence).
- Audit A: each of Findings 1–5 explicitly reclassified empirically (PASS / WARN / FAIL); W-7 floor recorded as actual N + classification (PASS if N>=10, WARN if N<10 per R-1A.e); `Synthetic excerpts skipped: M` reported.
- R-1A.c per-anchor contribution from `ch23-skills.md` is empirically enumerated with guard outcomes.
- Audit E: byte-equality holds → PASS.
- Audits B, C, D, F: continue to PASS (no regressions expected).
- Overall Verdict: PASS or PASS WITH WARNINGS. Zero FAILs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run mdbook build for both books (EXIT CODE verification, non-sandboxed shell precondition)</name>
  <files>(no edits — captures build output for the audit report)</files>
  <read_first>
    - Confirm `mdbook` is on PATH: `which mdbook`. **If absent, this task ABORTS with a precondition error** — escalate to the orchestrator. There is NO fallback to timestamp evidence (per user-locked decision in this revision pass: the closure cycle exists to retire that substitution).
    - Confirm Wave 1 closure plans have landed: `git log --oneline -5` should show commits from plans 81-08 and 81-09.
  </read_first>
  <action>
    <!-- revision: addresses BLOCKER 3 — no timestamp-evidence fallback. -->

    **Precondition check (HARD-GATED):**
    ```bash
    which mdbook || { echo "PRECONDITION FAIL: mdbook is not on PATH. This task requires a non-sandboxed shell with mdbook installed (per user-locked decision in revision pass 2 of plan 81-10). The timestamp-evidence fallback used in 81-07-AUDIT.md is RETIRED in this closure cycle — there is no fallback path. ABORTING. Escalate to orchestrator: re-run this plan from a shell that permits mdbook invocation, or install mdbook via 'cargo install mdbook --locked' if missing." >&2; exit 64; }
    ```

    If the precondition passes, run mdBook builds for both properties and capture exit codes and logs:

    ```bash
    (cd pmcp-book && mdbook build) 2>&1 | tee /tmp/mdbook-book-build-81-10.log
    BOOK_EXIT=${PIPESTATUS[0]}
    (cd pmcp-course && mdbook build) 2>&1 | tee /tmp/mdbook-course-build-81-10.log
    COURSE_EXIT=${PIPESTATUS[0]}
    echo "BOOK_EXIT=$BOOK_EXIT COURSE_EXIT=$COURSE_EXIT"
    ```

    Both exit codes MUST be 0. If either is non-zero, capture the relevant error lines from the log into the audit report's "mdBook Build Results" section as a FAIL finding.

    Per revision B-4 of `81-07-PLAN.md`, the pass signal is the EXIT CODE, not substring-grep of stderr.

    This re-confirmation REPLACES the timestamp-evidence substitution that the Wave 2 audit (81-07) was forced to use under a sandbox restriction. Non-sandboxed shell is a PRECONDITION of this task, not a soft requirement: if the executor's environment denies `mdbook` invocation, the task aborts with exit code 64 (`EX_USAGE`) and the orchestrator is responsible for re-running from an appropriate environment. There is NO graceful-degradation path — that substitution is what this closure cycle exists to retire.
  </action>
  <verify>
    <automated>which mdbook >/dev/null 2>&1 && (cd pmcp-book && mdbook build) && (cd pmcp-course && mdbook build) && echo "MDBOOK_RECONFIRM_PASS: both builds exit 0 from non-sandboxed shell"</automated>
  </verify>
  <done>
    `which mdbook` returns a valid path AND both `mdbook build` invocations exit 0. Logs captured at `/tmp/mdbook-{book,course}-build-81-10.log`. The audit report's "mdBook Build Results" section will quote the exit codes directly, retiring the timestamp-evidence substitution. If the precondition (mdbook on PATH) fails, this task aborts with `exit 64` and the orchestrator must re-run from an appropriate environment — no soft-fallback path exists.
  </done>
</task>

<task type="auto">
  <name>Task 2a: Run all six audits (A, B, C, D, E, F) under R-9 relaxations + post-81-08 pin state — capture raw results</name>
  <files>(no edits — produces in-memory / scratch-file raw audit results for Task 2b to consolidate)</files>
  <read_first>
    - `81-07-PLAN.md` Task 3 `<action>` (now amended by plan 81-09) for the Audit A algorithm + R-9 relaxations (clauses a/b/c/d/e) + all other audit specs.
    - `81-07-AUDIT.md` Findings 1–7 to know exactly which findings to reclassify and the block boundaries (chapter file:line ranges).
    - All chapter files listed in `81-07-PLAN.md` Task 3 `<read_first>` (six chapters + ch22-exercises.md + the two SUMMARY.md files).
    - Source files cited by chapters (especially `examples/s41_code_mode_graphql.rs` for Audit A re-verification of Findings 1–5).
    - `pmcp-course/src/part8-advanced/ch23-skills.md` lines 175–360 to empirically enumerate the four two-line anchors and the fenced blocks (if any) following each within the same H2.
  </read_first>
  <action>
    <!-- revision: addresses WARNING 5 (this task runs the audits and captures raw results; Task 2b consolidates into 81-10-AUDIT.md), BLOCKER 1 (no hardcoded N>=10 gate — capture actual N), BLOCKER 2 (Finding 5 explicit R-1A.d disposition path with source L146 + L169 check), BLOCKER 4 (R-1A.c per-anchor source-match-guard enumeration). -->

    Execute the same six audits (A, B, C, D, E, F) defined in `81-07-PLAN.md` Task 3, with the R-9 relaxations applied per the 81-09 amendment.

    **Audit A — apply R-9 relaxations per the spec amended by plan 81-09.** The clauses are:
    - (a) De-indent normalization: for each fenced block, compute minimum leading whitespace across non-empty lines; strip that count of leading whitespace from every line. Apply the same to the source file's text region before substring-matching.
    - (b) `// ...` placeholder reclassification: if a block contains a line whose trimmed content is exactly `// ...`, and the block would match the source as a contiguous substring with the placeholder line and the source-side lines it stands for elided, record as **WARN** (not FAIL).
    - (c) Two-line anchor format with source-match guard: the `Full example:` anchor scanner accepts BOTH single-line and two-line forms. **For each two-line anchor, apply the source-match guard to the next fenced block within the same H2.** Guard passes if AT LEAST ONE of: language fence consistent with source extension; source basename appears in the block; function signature, struct name, or type name from the cited source appears in the block. Blocks failing the guard are OUT OF SCOPE under R-1A.c, NOT FAIL.
    - (d) Source-side cosmetic omission: if the chapter block elides non-load-bearing source lines (commentary, `println!`, doc comments) WITHOUT a `// ...` marker, AND removing those source-side lines yields a contiguous-substring match against the de-indent-normalized block, record as **WARN**. "Non-load-bearing" = no struct field changes, no control-flow change, no API shape difference. Lines that change semantics still FAIL.
    - (e) W-7 floor: record actual N. PASS if N>=10. WARN if N<10. Do NOT FAIL.

    **Per-finding empirical scan (capture each result):**

    1. **Finding 1 (book ch12-9:246):** Extract block at L246–L262; apply R-1A.a de-indent; substring-match against `examples/s41_code_mode_graphql.rs` after applying same de-indent to source lines 79–93. Record outcome (expected PASS).

    2. **Finding 2 (book ch12-9:285):** Same as Finding 1, block at L285–L322 vs source lines 108–143. Record outcome (expected PASS).

    3. **Finding 3 (book ch12-9:343):** Same, block at L343–L371 vs source lines 145–171. Record outcome (expected PASS).

    4. **Finding 4 (course ch22:196):** Apply R-1A.a + R-1A.b. (i) Confirm the block contains a `// ...` placeholder at block line ~217. (ii) Identify the source lines the placeholder stands for (source L124–127 inner `if let Some(ref explanation) = ...` arm). (iii) Confirm match succeeds when both the placeholder line AND source L124–127 are elided. Record outcome (expected WARN per R-1A.b).

    5. **Finding 5 (course ch22:228) — EXPLICIT R-1A.d disposition path (BLOCKER 2 fix):** Apply R-1A.a + R-1A.d.
       (i) Confirm the block at L228–L254 contains NO `// ...` placeholder marker (R-1A.b does NOT apply).
       (ii) Identify the source-side omissions: source line 146 (`    // Demonstrates that mutations are rejected when allow_mutations is false (the default).`) and source line 169 (`            println!("This demonstrates that invalid code does NOT receive an approval token.");`).
       (iii) Confirm BOTH lines are absent from the chapter block (greppable: the block must NOT contain the substring `Demonstrates that mutations are rejected when allow_mutations is false` AND must NOT contain `This demonstrates that invalid code does NOT receive an approval token`).
       (iv) Confirm both lines are non-load-bearing per R-1A.d definition: L146 is a `//` comment (no struct field, no control flow, no API shape); L169 is a `println!` macro call (no struct field, no control flow, no API shape).
       (v) Apply R-1A.a de-indent to the chapter block AND source region (source L143–171 narrowed by removing L146 and L169 from the source-side comparison).
       (vi) Substring-match the de-indented block against the de-indented, narrowed source. If match succeeds → record **WARN per R-1A.d** with citation (`source L146 omitted (comment), source L169 omitted (println!), contiguous match on elision`). If match fails → record **FAIL** with first-differing-line citation.

    6. **Finding 6 (Audit E pmcp-code-mode pin):** Extract `pmcp-code-mode = "..."` from `pmcp-book/src/ch12-9-code-mode.md:89` and from `pmcp-course/src/part8-advanced/ch22-code-mode.md:67`. Byte-compare. Expected: both `"0.5.1"` → PASS.

    7. **Finding 7 (Audit E pmcp-code-mode-derive pin):** Same, `pmcp-code-mode-derive`, book L90 vs course L68. Expected: both `"0.2.0"` → PASS.

    **R-1A.c per-anchor enumeration (BLOCKER 4 fix — empirical guard application):**

    For `pmcp-course/src/part8-advanced/ch23-skills.md`, enumerate each two-line anchor and apply the R-1A.c source-match guard:

    - **Anchor at L175/176** (`[`examples/s44_server_skills.rs`](...)`):
      (i) Identify the next fenced block within the same H2 (Tier 2 / "Refunds Skill" — H2 starts at L183, ends before next `## ` header).
      (ii) Find: a `markdown` block at approximately L191–L203 excerpting `examples/skills/refunds/SKILL.md`.
      (iii) Apply guard: language fence = `markdown` (NOT `rust`/`rust,ignore`/`rust,no_run`); search for `s44_server_skills` literal — absent; search for Rust function signature or struct from `s44_server_skills.rs` — absent.
      (iv) **Guard result: FAIL → block OUT OF SCOPE under R-1A.c.** Contribution: 0.
      (v) Also check: is there a `rust,ignore` block later in the SAME H2 (e.g., around L213 `.skill(Skill::new("refunds", REFUNDS).with_path(...))`)? Apply the guard to it too: language fence `rust,ignore` (matches `.rs`); but anchor scope is "immediately following fenced block within same H2" — the markdown block is BETWEEN the anchor and the rust,ignore block. Per the algorithm in 81-07-PLAN.md, only the IMMEDIATELY following block counts. Record both outcomes for transparency.

    - **Anchor at L238/239** (`[`examples/s44_server_skills.rs`](...)`):
      (i) Identify next fenced block within the same H2 (Tier 2 continues until `## Tier 3:` at L249).
      (ii) Scan L240–L249: examine whether any fenced block appears. If none, record "no following block within H2; out of scope."
      (iii) If a block appears, apply the guard. Record outcome.

    - **Anchor at L341/342** (`[`examples/s44_server_skills.rs`](...)`):
      (i) H2 = "Cross-SDK Compatibility (Why Three Tiers Match Other Reference Implementations)" starting at L353 (or whatever the actual nearest preceding `## ` header is — empirically determine).
      (ii) Scan from L341 forward to the H2 boundary; identify next fenced block (if any).
      (iii) Apply guard; record outcome.

    - **Anchor at L343/344** (`[`examples/c10_client_skills.rs`](...)`):
      (i) Same H2 as L341/342.
      (ii) Scan from L343 forward; identify next fenced block (if any).
      (iii) Apply guard; record outcome.

    For each anchor, capture: (anchor location, cited source path, next-fenced-block location, language fence, guard pass/fail with which guard-criterion satisfied, in-scope/out-of-scope verdict, expected substring-match outcome if in-scope).

    **R-1A.c empirical total = sum of in-scope contributions across the four anchors.** Record explicitly in the raw results (this is the value Task 2b will use in `81-10-AUDIT.md`).

    **Run Audits B, C, D, E, F exactly as specified in `81-07-PLAN.md` Task 3 (no R-1A changes apply).** Capture raw pass/fail per check:
    - Audit B: enumerate all `https://github.com/paiml/rust-mcp-sdk/blob/main/...` permalinks across all six chapters; check each path resolves to a file under repo root.
    - Audit C: enumerate `pmcp-book/src/SUMMARY.md` and `pmcp-course/src/SUMMARY.md` entries; check each markdown link target exists.
    - Audit D: byte-equal check for Skills doctest (book ch12-8 L360–L375 vs `src/server/skills.rs` doctest L26–L42); grep for `rust,no_run` in course chapters (must be 0).
    - Audit E: byte-equal pins (covered above as Findings 6, 7).
    - Audit F: four checks per `81-07-PLAN.md` Task 3 Audit F (byte-equal anchor, citation count, derive-macro-first ordering, language-table row-count parity).

    **W-7 floor empirical recording (per R-1A.e):** sum the actual in-scope excerpt counts from all chapters:
    - book ch12-7-tasks.md: 0 (drift-refresh, no permalinks).
    - book ch12-8-skills.md: empirical count (anchors at L147/L203/L324 — re-verify whether following blocks fall within same H2; under R-1A no change unless H2 boundaries newly resolved).
    - book ch12-9-code-mode.md: 3 (Findings 1, 2, 3 — now in-scope under R-1A.a).
    - course ch21-tasks.md: 0 (drift-refresh).
    - course ch22-code-mode.md: 2 (Findings 4, 5 — in-scope under R-1A.a/b/d).
    - course ch23-skills.md: R-1A.c empirical contribution (0 to 4, as enumerated above).
    - course ch22-exercises.md, ch23-exercises.md: 0 (no permalinks).

    Record actual N. Classify W-7: PASS if N>=10, WARN if N<10. Do NOT FAIL on N<10 (per R-1A.e).

    **Capture all raw results to a scratch file** (e.g., `/tmp/81-10-raw-results.txt`) or in-memory structure that Task 2b will consume. Discipline: this task only RUNS the audits and CAPTURES results — it does NOT write the consolidated `81-10-AUDIT.md` (that's Task 2b's job).
  </action>
  <verify>
    <automated>test -f /tmp/81-10-raw-results.txt && grep -q 'Finding 1' /tmp/81-10-raw-results.txt && grep -q 'Finding 5' /tmp/81-10-raw-results.txt && grep -q 'R-1A\.c' /tmp/81-10-raw-results.txt && grep -q 'R-1A\.d\|source L146\|source L169' /tmp/81-10-raw-results.txt && grep -q 'W-7' /tmp/81-10-raw-results.txt && grep -qE 'Total excerpts checked.*N=[0-9]+|in-scope.*[0-9]+' /tmp/81-10-raw-results.txt && echo "Task 2a raw results captured: 7 findings + R-1A.c per-anchor + R-1A.d Finding 5 disposition + W-7 actual N."</automated>
  </verify>
  <done>
    Raw results scratch file `/tmp/81-10-raw-results.txt` exists and contains: per-finding disposition (Findings 1–7) with citation; R-1A.c per-anchor enumeration for `ch23-skills.md` (four anchors, each with guard outcome); R-1A.d disposition for Finding 5 with explicit checks for source L146 (comment) and source L169 (println!) being absent from the chapter block AND elision-yields-contiguous-match verification; W-7 actual N with PASS|WARN classification per R-1A.e (no hard FAIL on N<10); Audits B, C, D, E, F raw outcomes.
  </done>
</task>

<task type="auto">
  <name>Task 2b: Write consolidated re-audit report `81-10-AUDIT.md` from raw results + per-finding disposition narrative</name>
  <files>.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md</files>
  <read_first>
    - `/tmp/81-10-raw-results.txt` (output of Task 2a).
    - `81-07-AUDIT.md` structure (for the H2 template).
    - `81-07-PLAN.md` (now R-9-amended) for the audit-spec citations.
  </read_first>
  <action>
    <!-- revision: addresses WARNING 5 (consolidation task split from Task 2a), BLOCKER 1 (W-7 classified PASS|WARN, never hard FAIL), BLOCKER 2 (Finding 5 disposition narrative cites R-1A.d explicitly with source L146 + L169), BLOCKER 4 (R-1A.c per-anchor results recorded explicitly). -->

    Write the consolidated re-audit report to `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md` using the H2 structure below. Pull all empirical data from `/tmp/81-10-raw-results.txt` produced by Task 2a — do NOT re-run the audits here (Task 2a already did the work).

    ```markdown
    # Phase 81 Re-Audit (Gap-Closure Cycle)

    Run at: <timestamp>
    Closes: 81-07-AUDIT.md (7 FAIL findings)
    Audit spec: 81-07-PLAN.md as amended by plan 81-09 (revision R-9 / R-1A clauses a/b/c/d/e relaxations)
    Pin state: post plan 81-08 (Audit E pin alignment)

    ## Executor Environment Note
    mdbook re-confirmation completed via EXIT CODE from a non-sandboxed shell —
    the timestamp-evidence substitution from 81-07-AUDIT.md is RETIRED in this
    closure cycle per user-locked decision. Task 1 precondition (`which mdbook`)
    passed; both builds exited 0.

    ## mdBook Build Results
    - pmcp-book: PASS (exit code 0, log /tmp/mdbook-book-build-81-10.log)
    - pmcp-course: PASS (exit code 0, log /tmp/mdbook-course-build-81-10.log)

    ## Doctest Results
    - cargo test --doc -p pmcp --features skills,full: PASS|FAIL (test count, exit code)
    - cargo build -p pmcp (no features): PASS|FAIL (exit code)

    ## Inline Excerpt Drift Audit (Audit A under R-9 relaxations, clauses a/b/c/d/e)
    - Algorithm: per 81-07-PLAN.md Task 3 Audit A, amended by plan 81-09 revision R-9 (R-1A.a/b/c/d/e).
    - Total excerpts checked: N=<actual> (W-7 floor classification per R-1A.e: PASS if N>=10, WARN if N<10)
    - W-7 classification: PASS | WARN (per actual N)
    - Synthetic excerpts skipped: M=<actual>
    - Unanchored excerpts (out of scope): K=<actual>
    - Drift findings (FAIL): F (list each, including chapter file:line and source-line-range)
    - Drift findings (WARN, R-1A.b // ... placeholder): W1 (list each)
    - Drift findings (WARN, R-1A.d source-side cosmetic omission): W4 (list each, with elided source-line numbers)
    - Drift findings (WARN, R-1A trailing-whitespace only): W2 (list each)

    ### R-1A.c Per-Anchor Source-Match-Guard Outcomes (`ch23-skills.md`)
    | Anchor | Cited source | Next block | Lang fence | Guard | In-scope? |
    |---|---|---|---|---|---|
    | L175/176 | examples/s44_server_skills.rs | ~L191 | markdown | FAIL (markdown != rust; no basename; no signature) | OUT OF SCOPE |
    | L238/239 | examples/s44_server_skills.rs | <empirical> | <empirical> | <empirical> | <empirical> |
    | L341/342 | examples/s44_server_skills.rs | <empirical> | <empirical> | <empirical> | <empirical> |
    | L343/344 | examples/c10_client_skills.rs | <empirical> | <empirical> | <empirical> | <empirical> |

    R-1A.c contribution to in-scope total: <actual count, 0–4>.

    ### W-7 Coverage Floor (R-1A.e classification)
    - Actual in-scope N: <number>
    - Breakdown: book ch12-9 = 3; course ch22 = 2; course ch23 Skills = <0–4 per R-1A.c table>; other chapters = 0.
    - Classification: PASS (N>=10) | WARN (N<10) — per R-1A.e, NOT a hard FAIL.

    ## Cross-Link Audit (Audit B)
    - Total GitHub permalinks checked: N
    - Broken links (FAIL): N (expected 0)

    ## SUMMARY.md Audit (Audit C)
    - pmcp-book SUMMARY entries: N PASS, N FAIL
    - pmcp-course SUMMARY entries: N PASS, N FAIL

    ## Doctest Byte-Equality (Audit D)
    - Skills book vs skills.rs doctest body: PASS|FAIL
    - "No rust,no_run in course chapters" invariant: PASS|FAIL

    ## Version-Pin Consistency (Audit E — post plan 81-08)
    - pmcp-code-mode pin: PASS|FAIL (book='...' course='...')
    - pmcp-code-mode-derive pin: PASS|FAIL (book='...' course='...')

    ## Audit F — Cross-Property Prose Consistency
    - Check F1: PASS|FAIL
    - Check F2: PASS|FAIL
    - Check F3: PASS|FAIL
    - Check F4: PASS|FAIL

    ## Re-audit of 81-07-AUDIT.md Findings

    ### Finding 1 (Audit A — book ch12-9-code-mode.md:246 de-indent)
    - Original disposition: FAIL (strict contiguous-block check rejected 4-space de-indent).
    - Re-audit under R-9 (R-1A.a): <PASS|WARN|FAIL — empirical>.
    - Justification: <one paragraph; cite the de-indent normalization application and substring-match result>.

    ### Finding 2 (Audit A — book ch12-9-code-mode.md:285 de-indent)
    - Original disposition: FAIL.
    - Re-audit under R-9 (R-1A.a): <PASS|WARN|FAIL — empirical>.
    - Justification: <…>.

    ### Finding 3 (Audit A — book ch12-9-code-mode.md:343 de-indent)
    - Original disposition: FAIL.
    - Re-audit under R-9 (R-1A.a): <PASS|WARN|FAIL — empirical>.
    - Justification: <…>.

    ### Finding 4 (Audit A — course ch22-code-mode.md:196 de-indent + `// ...` placeholder)
    - Original disposition: FAIL.
    - Re-audit under R-9 (R-1A.a + R-1A.b): <PASS|WARN|FAIL — empirical, expected WARN>.
    - Justification: `// ...` placeholder at block line ~217 stands for source L124–127 (inner `if let Some(ref explanation) = ...` arm). With de-indent applied AND placeholder + source L124–127 elided, contiguous match <holds|fails>. WARN per R-1A.b.

    ### Finding 5 (Audit A — course ch22-code-mode.md:228 de-indent + source-side cosmetic omission)
    - Original disposition: FAIL (de-indented AND missing source-side comment + println! without `// ...` marker).
    - Re-audit under R-9 (R-1A.a + R-1A.d): <PASS|WARN|FAIL — empirical, expected WARN>.
    - Justification (R-1A.d explicit citation): The chapter block at L228–L254 contains NO `// ...` placeholder marker (R-1A.b N/A). The source-side omissions are: source L146 (`    // Demonstrates that mutations are rejected when allow_mutations is false (the default).` — a `//` comment, non-load-bearing per R-1A.d: no struct field, no control flow, no API shape change) and source L169 (`            println!("This demonstrates that invalid code does NOT receive an approval token.");` — a `println!` macro, non-load-bearing per R-1A.d). With de-indent applied to both block and source, and source L146 + L169 elided from the comparison, the de-indented block <matches|does not match> the narrowed source as a contiguous substring. <If matches: record WARN per R-1A.d with citation. If fails: record FAIL with first-differing-line citation.>

    ### Finding 6 (Audit E — pmcp-code-mode pin)
    - Original disposition: FAIL (`"0.5"` vs `"0.5.1"`).
    - Re-audit post-81-08: <PASS|FAIL>.
    - Justification: byte-equality between book L89 (`pmcp-code-mode = "0.5.1"`) and course L67 (`pmcp-code-mode = "0.5.1"`).

    ### Finding 7 (Audit E — pmcp-code-mode-derive pin)
    - Original disposition: FAIL.
    - Re-audit post-81-08: <PASS|FAIL>.
    - Justification: byte-equality between book L90 (`pmcp-code-mode-derive = "0.2.0"`) and course L68 (`pmcp-code-mode-derive = "0.2.0"`).

    ## Overall Verdict
    - PASS (zero findings) | PASS WITH WARNINGS (W warnings, no FAILs) | FAIL (F FAILs)

    ## Shippability
    - If verdict is PASS or PASS WITH WARNINGS: Phase 81 IS shippable. Recommended next step: `/gsd-verify-phase 81`.
    - If verdict is FAIL: Phase 81 is NOT shippable. Operator must run another `/gsd-plan-phase 81 --gaps` cycle to close the remaining FAILs.

    ## Findings (if any FAIL or WARN)
    1. <finding 1>
    ...

    ## Self-Check (post-write)
    - FOUND: all required H2 sections.
    - FOUND: `Total excerpts checked: N=<actual>` line with W-7 classification PASS or WARN per R-1A.e.
    - FOUND: `Synthetic excerpts skipped: M=<actual>` line.
    - FOUND: `### R-1A.c Per-Anchor Source-Match-Guard Outcomes` table with all four anchors enumerated.
    - FOUND: Finding 5 disposition cites R-1A.d explicitly and references source L146 + L169.
    - FOUND: Re-audit section with all 7 81-07-AUDIT.md findings re-dispositioned empirically.
    - FOUND: Shippability section with explicit verdict → next-step guidance.
    ```

    Discipline:
    - The audit is informational; do NOT modify any chapter file.
    - All findings explicit + cited (chapter file:line; source-line range or list).
    - Empirical results from Task 2a are the source of truth — do NOT substitute predictions.
    - If any FAIL remains, the Shippability section MUST escalate to another gap-closure cycle.

    Commit message template (use exactly):
    ```
    docs(81-10): re-audit Phase 81 post gap-closure (closes 7 FAILs, R-9 clauses a/b/c/d/e applied)

    Re-runs the cross-property audit under revision R-9 (R-1A clauses a/b/c/d/e
    relaxations, plan 81-09 pass-2 revision) and post-Audit-E-pin-alignment
    state (plan 81-08). Re-classifies the 7 FAIL findings from 81-07-AUDIT.md
    empirically (not predicted):

    - Audit A Findings 1, 2, 3: <PASS|WARN per actual scan> (R-1A.a de-indent)
    - Audit A Finding 4: <WARN|FAIL per actual scan> (R-1A.b // ... placeholder)
    - Audit A Finding 5: <WARN|FAIL per actual scan> (R-1A.d source-side cosmetic
      omission — source L146 comment + L169 println! omitted without // ... marker)
    - Audit E Findings 6, 7: PASS (byte-equal pins post 81-08)

    Re-confirms mdbook build via EXIT CODE from non-sandboxed shell (retires
    the timestamp-evidence substitution in 81-07-AUDIT.md — no fallback path).

    R-1A.c per-anchor source-match-guard enumeration for ch23-skills.md
    (L175/176, L238/239, L341/342, L343/344) recorded. The L175/176 anchor
    is OUT OF SCOPE under the guard (markdown block following a Rust anchor).
    Empirical R-1A.c contribution: <0–4>.

    W-7 floor: actual N=<number>; classified <PASS if N>=10 | WARN if N<10>
    per R-1A.e (NOT a hard FAIL).

    Overall Verdict: <PASS | PASS WITH WARNINGS> — Phase 81 shippable.
    Recommended next: /gsd-verify-phase 81.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## mdBook Build Results' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## Doctest Results' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## Inline Excerpt Drift Audit' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## Version-Pin Consistency' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## Audit F' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## Re-audit of 81-07-AUDIT.md Findings' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## Overall Verdict' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q '## Shippability' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -qE 'Total excerpts checked.*N=[0-9]+|Total excerpts checked: [0-9]+' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -qE 'W-7.*(PASS|WARN)' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q 'R-1A\.c Per-Anchor\|R-1A.c Per-Anchor' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q 'source L146\|L146' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q 'source L169\|L169' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q 'R-1A\.d\|R-1A.d' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && grep -q 'Synthetic excerpts skipped:' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && for n in 1 2 3 4 5 6 7; do grep -q "### Finding $n" .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md || { echo "MISSING Finding $n re-disposition"; exit 1; }; done && grep -E 'Overall Verdict' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md && (grep -q 'PASS (zero findings)' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md || grep -q 'PASS WITH WARNINGS' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md) && [ "$(wc -l < .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-AUDIT.md)" -ge 60 ] && echo "81-10-AUDIT.md complete; verdict PASS or PASS WITH WARNINGS; W-7 classified per R-1A.e; Finding 5 cites R-1A.d + L146 + L169; R-1A.c per-anchor table present."</automated>
  </verify>
  <done>
    `81-10-AUDIT.md` exists with all required H2 sections including `## Re-audit of 81-07-AUDIT.md Findings`, `### R-1A.c Per-Anchor Source-Match-Guard Outcomes` table, and `## Shippability`. Each of the 7 FAIL findings from `81-07-AUDIT.md` is empirically re-dispositioned. Finding 5's disposition explicitly cites R-1A.d AND references source L146 (comment) + L169 (println!) AND describes the elision-yields-contiguous-match check. The R-1A.c per-anchor table records guard outcomes for all four `ch23-skills.md` two-line anchors. The mdBook build re-confirmation is captured via EXIT CODE from a non-sandboxed shell (no timestamp-evidence substitution). W-7 is classified PASS (if N>=10) or WARN (if N<10) per R-1A.e — NOT a hard FAIL. The Overall Verdict is `PASS` (zero findings) or `PASS WITH WARNINGS` (no FAILs). The Shippability section explicitly recommends `/gsd-verify-phase 81` as the next step.
  </done>
</task>

</tasks>

<threat_model>
Read-only re-audit. No code modifications, no auth or trust-boundary surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-10-01 | Confirmation bias | Re-audit predicts PASS | mitigate | The R-9 amendment in plan 81-09 records reclassification PREDICTIONS; this plan REQUIRES the executor to empirically verify each outcome by running the relaxed algorithm against the actual chapter content (Task 2a captures raw results; Task 2b consolidates from those raw results, not from predictions). The verify block asserts the audit report contains explicit per-finding disposition narratives AND per-anchor R-1A.c guard outcomes — neither derivable from predictions alone. |
| T-81-10-02 | False PASS from too-loose R-9 | Finding 5 borderline case | mitigate | Finding 5 is explicitly called out with a deterministic decision rule in Task 2a step (v)–(vi): record WARN per R-1A.d only if (i) the block lacks a `// ...` marker, (ii) source L146 + L169 are confirmed absent from the block, (iii) both omitted lines are non-load-bearing, (iv) elision yields a contiguous match. If (iv) fails, record FAIL. The executor cannot silently mark it PASS or WARN without justification. |
| T-81-10-03 | mdbook environment regression (revision pass 2: no fallback) | Sandbox denies mdbook | mitigate | Task 1 ABORTS with `exit 64` if `mdbook` is not on PATH — there is NO timestamp-evidence fallback (per user-locked decision in this revision pass). The orchestrator is responsible for re-running from an appropriate environment. Non-sandboxed shell is a precondition, not a soft requirement. |
| T-81-10-04 | Shippability bypass | Phase verdict | mitigate | Verify block asserts that Overall Verdict is `PASS (zero findings)` or `PASS WITH WARNINGS`. If any FAIL remains, the verify FAILs and the orchestrator must route to another `/gsd-plan-phase 81 --gaps` cycle. |
| T-81-10-05 | R-1A.c source-match guard bypass | False in-scope on mismatched block | mitigate | Task 2a empirically enumerates all four two-line anchors in `ch23-skills.md`, applies the guard to each, and records per-anchor outcome with the guard-criterion that passed/failed. Task 2b consolidates the per-anchor table into `81-10-AUDIT.md`. The L175/176 anchor is provably OUT OF SCOPE (markdown block following Rust anchor) — this case is documented as the exemplar of the guard working as intended. |
| T-81-10-06 | W-7 silent FAIL | Hardcoded floor check | mitigate | The verify block for Task 2b checks for `W-7.*(PASS|WARN)` regex match — NOT a hardcoded `N>=10` check. This aligns with R-1A.e and the user-locked decision in this revision pass that W-7 is WARN-reclassifiable when N<10, not a hard FAIL gate. |
</threat_model>

<verification>
- `81-10-AUDIT.md` exists with all required H2 sections including `## Re-audit of 81-07-AUDIT.md Findings`, `### R-1A.c Per-Anchor Source-Match-Guard Outcomes`, and `## Shippability`.
- Each of the 7 FAIL findings (`### Finding 1` ... `### Finding 7`) is empirically re-dispositioned.
- Finding 5's disposition cites R-1A.d AND references source L146 + L169 AND describes the elision-yields-contiguous-match check.
- The R-1A.c per-anchor table enumerates all four `ch23-skills.md` two-line anchors with guard outcomes (the L175/176 anchor is OUT OF SCOPE — guard FAIL: markdown block following Rust anchor).
- `Total excerpts checked: N=<number>` with the actual count (any N), and W-7 explicitly classified PASS (if N>=10) or WARN (if N<10) per R-1A.e — NOT FAILed on N<10.
- `Synthetic excerpts skipped: M` line present (degenerate-skip guard).
- `mdbook build` re-confirmation is via EXIT CODE from a non-sandboxed shell (no fallback path; Task 1 aborts on missing mdbook).
- Audit E records both pins PASS (byte-equal `"0.5.1"` and `"0.2.0"`).
- Audits B, C, D, F continue to PASS (no regressions).
- Overall Verdict is `PASS` or `PASS WITH WARNINGS`. Zero FAILs.
- Shippability section explicitly recommends `/gsd-verify-phase 81`.
- `git diff --name-only HEAD` lists only `81-10-AUDIT.md` and `81-10-SUMMARY.md` — no source/chapter files modified.
</verification>

<success_criteria>
- All 7 FAIL findings from `81-07-AUDIT.md` are reclassified (PASS / WARN) in `81-10-AUDIT.md` with cited empirical justifications.
- Finding 5 disposition cites R-1A.d and explicitly references source L146 (comment) + L169 (println!) elision yielding contiguous match.
- R-1A.c per-anchor source-match-guard outcomes for all four `ch23-skills.md` two-line anchors are recorded.
- Overall Verdict in `81-10-AUDIT.md` is `PASS` or `PASS WITH WARNINGS`. Zero FAIL-severity findings.
- W-7 coverage floor is classified PASS (if N>=10) or WARN (if N<10) per R-1A.e — never as a hard FAIL.
- `mdbook build` exit codes captured for both books from a non-sandboxed shell (no timestamp-evidence substitution; Task 1 aborts on missing mdbook).
- Phase 81's R-8 shippability gate is satisfied — operator can route to `/gsd-verify-phase 81`.
- If any FAIL unexpectedly remains, the audit report explicitly identifies it and recommends another `/gsd-plan-phase 81 --gaps` cycle.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-10-SUMMARY.md` recording:
- Reference to `81-10-AUDIT.md`.
- One-line verdict: `Re-audit PASS (zero FAILs)` or `Re-audit PASS WITH WARNINGS (N WARNs, zero FAILs)`.
- Per-finding disposition table:
  ```
  | 81-07 Finding | Original | Re-audit | Justification |
  |---|---|---|---|
  | 1 | FAIL | PASS|WARN|FAIL | R-1A.a de-indent (empirical) |
  | 2 | FAIL | PASS|WARN|FAIL | R-1A.a de-indent (empirical) |
  | 3 | FAIL | PASS|WARN|FAIL | R-1A.a de-indent (empirical) |
  | 4 | FAIL | PASS|WARN|FAIL | R-1A.b // ... placeholder (empirical) |
  | 5 | FAIL | PASS|WARN|FAIL | R-1A.d source-side omission (L146 + L169) (empirical) |
  | 6 | FAIL | PASS | 81-08 pin alignment |
  | 7 | FAIL | PASS | 81-08 pin alignment |
  ```
- R-1A.c per-anchor empirical contribution count for `ch23-skills.md` (0–4).
- Confirmation that mdbook re-confirmation was via EXIT CODE from a non-sandboxed shell.
- W-7 floor classification: PASS (N>=10) or WARN (N<10) per R-1A.e, with actual N.
- Recommended next: `/gsd-verify-phase 81`.
</output>

## PLANNING COMPLETE
