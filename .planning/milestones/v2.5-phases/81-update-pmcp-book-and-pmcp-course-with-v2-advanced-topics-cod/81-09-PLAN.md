---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 09
type: execute
wave: 1
depends_on: []
files_modified: [".planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md"]
autonomous: true
requirements: []
tags: [docs, gap-closure, audit-relaxation, audit-a, spec-amendment, revision-pass-2]
gap_closure: true
must_haves:
  truths:
    - "Audit A in `81-07-PLAN.md` is amended per R-1A: de-indentation tolerant (strip common minimum-indent before substring match)."
    - "Audit A in `81-07-PLAN.md` reclassifies structural abbreviation (`// ...` placeholders) as WARN, not FAIL."
    - "Audit A in `81-07-PLAN.md` accepts the two-line anchor format `Full example:\\n[`path`](url)` (course Skills chapter style) — matching extends across the newline, GATED by a source-match guard that the next fenced block plausibly excerpts the cited source."
    - "Audit A in `81-07-PLAN.md` reclassifies source-side cosmetic omission (non-load-bearing commentary or `println!` lines absent from a chapter block without a `// ...` placeholder marker) as WARN, not FAIL."
    - "Audit A in `81-07-PLAN.md` reclassifies the W-7 coverage-floor finding (>=10 in-scope excerpts) as WARN when the relaxed scan yields N<10 in-scope excerpts; PASS when N>=10. The phase remains shippable either way."
    - "Wave 1 chapter content (`pmcp-book/src/ch12-9-code-mode.md`, `pmcp-course/src/part8-advanced/ch22-code-mode.md`, `pmcp-course/src/part8-advanced/ch23-skills.md`) is NOT modified — the audit *spec* changes, not the chapter content."
  artifacts:
    - path: .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md
      provides: "Phase 81 Wave 2 audit plan with Audit A `<verify_block>` and algorithm relaxed per R-1A (clauses a/b/c/d/e); new revision note R-9 (the R-1A amendment) appended."
      contains: "revision R-9"
  key_links:
    - from: 81-07-PLAN.md (Audit A in Task 3)
      to: 81-10-PLAN.md (re-audit re-runs Audit A under relaxed spec)
      via: "Audit A algorithm is the spec the re-audit executes"
      pattern: "Audit A|R-1A|R-9"
---

<!-- revision: addresses BLOCKER 1 (W-7 floor reclassification → R-1A.e), BLOCKER 2 (Finding 5 source-side omission → R-1A.d), BLOCKER 4 (R-1A.c source-match guard), WARNING 6 (commit message weakened to predictions) -->

<objective>
Amend Audit A's `<verify_block>` and algorithm in `81-07-PLAN.md` per user decision R-1A (audit relaxation) so that:

(a) Leading-whitespace runs are stripped to a common minimum-indent per block before contiguous-substring comparison (de-indentation tolerant).
(b) Structural abbreviation via `// ...` placeholders is reclassified as **WARN-severity drift** (not FAIL) — accepted as a documentation-pedagogy pattern.
(c) The two-line anchor format `Full example:\n[`path`](url)` (used in the course Skills chapter at lines 175/176, 238/239, 341/342, 343/344) is matched across the line break, **GATED by a source-match guard** — the immediately-following fenced block is in-scope under R-1A.c ONLY if its content is plausibly an excerpt of the cited `path` (filename mention, function signature, struct name from the cited file, OR a code-fence language consistent with the source extension). Markdown blocks, TOML blocks, and blocks whose language fence doesn't match the source extension are OUT of scope under R-1A.c.
(d) **Source-side cosmetic omission**: if a chapter block elides non-load-bearing source lines (commentary, `println!` debug output, doc comments) WITHOUT a `// ...` placeholder marker, AND removing those source-side lines from the comparison yields a contiguous-substring match, the drift is recorded as **WARN** (cosmetic drift, not algorithmic failure). "Non-load-bearing" is defined narrowly: no struct field changes, no control-flow change, no API shape difference. Lines that DO change semantics still produce FAIL.
(e) **W-7 floor reclassification when relaxed scan yields N<10 in-scope excerpts**: if the relaxed Audit A scan returns fewer than 10 in-scope excerpts, the W-7 coverage-floor finding is recorded as **WARN** (structural drift in chapter anchor density), not FAIL. The N>=10 ambition is preserved as a target; the WARN documents the cap honestly. The phase remains shippable.

Purpose: Audit A as originally written rejected 5 anchored excerpts in Wave 1's Code Mode chapters and missed 4 anchored excerpts in the course Skills chapter (two-line format). Per audit Section "Shippability" recommendation #2, **Option R-1A (relax the audit) is the user-locked decision** — the chapter excerpts are internally consistent, pedagogically appropriate (de-indented to column 0 for readability), and obviously derived from the cited source files. The strict-contiguous rule was over-engineered for documentation-quality verification.

This plan **does NOT modify chapter content**. The user's R-1A decision explicitly preserves Wave 1 chapter pedagogy. The amendment is a spec change to the audit; the audit *behavior* must follow the spec change, which lands here in 81-07-PLAN.md before plan 81-10 re-runs the audit.

Output:
- Edits to `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md`:
  - Audit A algorithm description amended in `<action>` of Task 3 with all five R-1A clauses (a, b, c, d, e).
  - Audit A `<verify>` block amended (W-7 floor is now a WARN-reclassifiable check rather than a hard FAIL; structural-abbreviation findings are WARN not FAIL).
  - Frontmatter `must_haves.truths` updated to reflect the relaxation.
  - A new revision note `R-9` appended explaining the amendment (per the established `B-N`/`R-N`/`W-N` convention used throughout this plan's history).
- No chapter content edits.
- No `81-07-AUDIT.md` edits (the existing audit report is historical evidence of the pre-relaxation run; the re-audit lands as `81-10-AUDIT.md`).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md

<interfaces>
<!-- revision: addresses BLOCKER 1, BLOCKER 2, BLOCKER 4 — describes the five R-1A clauses with source-match guard, source-side omission clause, and W-7 WARN-reclassification clause. -->
<!-- Key locations in 81-07-PLAN.md the executor must amend. Line numbers from the file as committed at 45499967. -->

Sections in 81-07-PLAN.md that need amendment:
- Frontmatter `must_haves.truths` (around L13–L20): the truth about contiguous-block matching needs a parenthetical "(relaxed per R-1A: clauses a/b/c/d/e — de-indentation tolerant; `// ...` is WARN; two-line anchor accepted with source-match guard; source-side cosmetic omission is WARN; W-7 floor is WARN if N<10)".
- `<objective>` line about Audit A (around L37–L38): note the R-1A relaxation alongside R-1, listing clauses a/b/c/d/e.
- Task 3 `<action>` Audit A algorithm description (around L150–L195): five changes — (a) de-indent before compare, (b) `// ...` is WARN, (c) two-line anchor regex WITH source-match guard, (d) source-side cosmetic omission is WARN, (e) W-7 floor reclassifies to WARN if N<10.
- Task 3 `<verify>` (around L385–L386): the W-7 floor check is REWRITTEN to no longer be a hard FAIL on N<10 — instead, the report records actual N and classifies W-7 as WARN if N<10 and PASS if N>=10. Update the explanatory `<done>` text (around L388–L395) to mention R-1A clauses a–e.
- `<threat_model>` row T-81-07-03 (around L407): note that the relaxation is intentional and the new failure mode (false negatives via too-loose match) is mitigated by the `// ...` WARN classification, the source-side-omission WARN classification, and the source-match guard on the two-line anchor.
- `<verification>` section bullet about Audit A contiguous-block matching (around L420): amend to reference R-1A relaxation with all five clauses.
- Append revision note R-9 at the end of the plan (after `<output>`).

Audit-report evidence the amendment closes:
- 81-07-AUDIT.md Findings 1, 2, 3 (Audit A FAILs, book ch12-9 de-indent) — recharacterized as PASS under R-1A.a.
- 81-07-AUDIT.md Finding 4 (Audit A FAIL, course ch22 L196 `// ...` placeholder) — recharacterized as WARN under R-1A.b.
- 81-07-AUDIT.md Finding 5 (Audit A FAIL, course ch22 L228 source-side comment + `println!` omission WITHOUT `// ...` marker) — recharacterized as WARN under R-1A.d (the new clause introduced in this revision pass).
- 81-07-AUDIT.md "Audit-A Coverage Floor Note (revision W-7)" — explicitly notes the W-7 floor is unreachable under strict single-line + strict-contiguous rules; R-1A.c (two-line anchor) un-narrows scope so Skills chapters MAY contribute in-scope excerpts; R-1A.e reclassifies as WARN if the relaxed scan still yields N<10.
- 81-07-AUDIT.md "Structural/methodological findings (informational, not FAIL)" — the two-line anchor format observation is the direct trigger for R-1A.c.

R-1A.c source-match guard rationale (evidence):
- Course Skills chapter line 175/176 anchor cites `examples/s44_server_skills.rs`. The next fenced block within the same H2 is a `markdown` block (around L191) that excerpts `examples/skills/refunds/SKILL.md` — a DIFFERENT source. Without the guard, R-1A.c would put that block in scope and the audit would compare it against the wrong source file. The guard requires the following block's language fence to be consistent with the source's extension (`.rs` → `rust`/`rust,ignore`/`rust,no_run`) OR for filename/signature/struct-name from the cited source to appear in the block.

R-1A.d source-side omission rationale (evidence):
- Course ch22 L228 (Finding 5) elides source line 146 (comment `// Demonstrates that mutations are rejected when allow_mutations is false (the default).`) AND source line 169 (`println!("This demonstrates that invalid code does NOT receive an approval token.");`) WITHOUT a `// ...` placeholder marker in the block. Both are non-load-bearing (commentary + println). R-1A.b (which covers `// ...` placeholders) does not apply because the block has no placeholder marker. R-1A.d is needed to cover this case.

R-1A.e W-7 reclassification rationale (math):
- Predicted in-scope excerpt totals under R-1A.a–d: book ch12-9 = 3 (Findings 1, 2, 3), course ch22 = 2 (Findings 4, 5), course ch23 Skills = at most 4 under R-1A.c, but the source-match guard may reject some (e.g., the L175/176 anchor is followed by a `markdown` block excerpting a DIFFERENT source, which fails the guard). Best-case = 9. The hard `>= 10` floor is one short of best-case and may be three short of empirical reality. Reclassifying W-7 as WARN when N<10 keeps the phase shippable without inventing artificial anchors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend Audit A spec in 81-07-PLAN.md per R-1A (clauses a/b/c/d/e)</name>
  <files>.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md</files>
  <read_first>
    - `81-07-PLAN.md` in full (or focus on lines 13–20 frontmatter, 37–45 objective, 150–195 Audit A algorithm, 385–395 Task 3 verify/done, 407 threat model T-81-07-03, 420 verification section). The file is ~457 lines.
    - `81-07-AUDIT.md` Findings 1–5 and the "Audit-A Coverage Floor Note (revision W-7)" section to confirm which findings are reclassified PASS, WARN, or remain FAIL under R-1A clauses a/b/c/d/e.
    - `pmcp-course/src/part8-advanced/ch23-skills.md` lines 175–200, 238–260, 341–360 to confirm the source-match guard examples (markdown block following `s44_server_skills.rs` anchor at L175/176 is a different-source excerpt → out of scope under R-1A.c guard).
    - `examples/s41_code_mode_graphql.rs` lines 140–171 to confirm the R-1A.d source-side omission rationale for Finding 5 (source lines 146 + 169 are commentary/println absent from the chapter block).
  </read_first>
  <action>
    <!-- revision: addresses BLOCKER 1, BLOCKER 2, BLOCKER 3 (via clarifying R-1A.e WARN path eliminates the hard-floor FAIL), BLOCKER 4 (source-match guard), WARNING 6 (commit message weakened) -->

    Apply the following edits to `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md` using the Edit tool. Make each edit minimal and surgical — do NOT rewrite whole sections. The pattern: each `Audit A` reference grows a parenthetical "(revision R-9: relaxed per R-1A — de-indentation tolerant; `// ...` is WARN; two-line anchor accepted with source-match guard; source-side cosmetic omission is WARN; W-7 floor is WARN if N<10)".

    ### Edit 1 — Frontmatter must_haves.truths (Audit A truth)

    Locate the bullet that currently reads (approximately):

    ```
    - "Every inline excerpt in the new/rewritten chapters matches its cited source file as a contiguous normalized substring (revision R-1 — strengthened from substring-grep to contiguous-block matching, with explicit `Full example:` anchor pattern, and synthetic-snippet opt-out via `<!-- synthetic -->`)."
    ```

    Replace with:

    ```
    - "Every inline excerpt in the new/rewritten chapters matches its cited source file as a contiguous normalized substring (revisions R-1 + R-9 — R-1 strengthened from substring-grep to contiguous-block matching, with explicit `Full example:` anchor pattern and synthetic-snippet opt-out via `<!-- synthetic -->`; R-9 relaxed per R-1A clauses a/b/c/d/e: (a) leading-whitespace runs stripped to common minimum-indent before comparison (de-indentation tolerant); (b) structural abbreviation via `// ...` placeholders is WARN-severity not FAIL; (c) two-line anchor format `Full example:\\n[`path`](url)` is matched across the line break, gated by a source-match guard (next block must plausibly excerpt the cited source — language-fence match OR filename/signature/struct-name appearance); (d) source-side cosmetic omission of non-load-bearing lines (commentary, `println!`, doc comments) without a `// ...` placeholder marker is WARN if elision yields a contiguous match; (e) the W-7 coverage floor of >=10 in-scope excerpts is reclassified to WARN when the relaxed scan yields N<10 — the phase remains shippable)."
    ```

    ### Edit 2 — Objective bullet 3 (Audit A description)

    Locate the bullet in `<objective>` that currently reads (approximately):

    ```
    3. **Inline excerpt drift audit (Audit A — STRENGTHENED per revision R-1):** Every inline excerpt in the new/rewritten chapters that is preceded (within the same H2 section) by an explicit `Full example: [`filename`](URL)` markdown anchor is verified via contiguous-block normalized-substring matching against the cited source file. Synthetic snippets opt out via an `<!-- synthetic -->` HTML comment immediately preceding the fenced block.
    ```

    Replace with:

    ```
    3. **Inline excerpt drift audit (Audit A — STRENGTHENED per revision R-1, RELAXED per revision R-9 = R-1A clauses a/b/c/d/e):** Every inline excerpt in the new/rewritten chapters that is preceded (within the same H2 section) by an explicit `Full example: [`filename`](URL)` markdown anchor (single-line OR two-line — see R-9(c)) is verified via contiguous-block normalized-substring matching against the cited source file. Normalization includes leading-whitespace stripping to a common minimum-indent per block (de-indentation tolerant — R-9(a)). Structural abbreviation via `// ...` placeholders is recorded as WARN-severity drift, not FAIL (R-9(b)). Source-side cosmetic omission (non-load-bearing commentary or `println!` lines absent from the chapter block without a `// ...` marker, where elision yields a contiguous match) is also WARN-severity (R-9(d)). The W-7 coverage floor (>=10 in-scope excerpts) is reclassified to WARN when the relaxed scan yields N<10 (R-9(e)) — the phase remains shippable. Synthetic snippets opt out via an `<!-- synthetic -->` HTML comment immediately preceding the fenced block.
    ```

    ### Edit 3 — Audit A algorithm (within Task 3 `<action>`)

    Locate the Audit A algorithm pseudo-shell block (the section starting `Algorithm (pseudo-shell — the executor may implement as a small Python/awk script):` followed by a fenced code block).

    Insert the following amendment BEFORE the algorithm block (so the algorithm still reads end-to-end), as a new paragraph:

    ```
    **Revision R-9 (R-1A) relaxations** — applied during normalization, matching, and finding classification:

    1. **De-indentation tolerant (R-1A.a):** Before comparing a fenced block against its source, compute the minimum leading-whitespace count across all non-empty lines of the block, and strip that count of leading whitespace from every line. Apply the SAME minimum-indent stripping to the source file's text before substring matching. This handles the documentation-pedagogy pattern where a chapter excerpt is presented at column 0 even though the source file's corresponding region is indented inside a `fn main()` body. (Audit 81-07 Findings 1, 2, 3 in `81-07-AUDIT.md` are 4-space main-fn de-indentation drifts; these are PREDICTED to PASS under R-1A.a — plan 81-10 records the empirical outcome.)

    2. **`// ...` placeholder reclassification (R-1A.b):** If the fenced block contains one or more lines whose trimmed content is exactly `// ...` (a structural-abbreviation placeholder), AND the contiguous-block match would succeed if the placeholder line and the source lines it stands for were elided, record the finding as **WARN** (not FAIL). The WARN entry MUST report the chapter line, the placeholder location, and the source-line range that was elided. This handles the pedagogically common pattern of compressing a multi-line block to a single `// ...` marker. (Audit 81-07 Finding 4 — `pmcp-course/src/part8-advanced/ch22-code-mode.md:196` — is PREDICTED to fall into this WARN category under R-1A.b.)

    3. **Two-line anchor format with source-match guard (R-1A.c):** The `Full example:` anchor pattern accepts BOTH single-line and two-line forms. The two-line form is:
       ```
       Full example:
       [`path`](https://github.com/paiml/rust-mcp-sdk/blob/main/path)
       ```
       (with optional intervening blank lines between the two physical lines). The anchor scanner MUST match across the line break. **Source-match guard (REQUIRED):** the next fenced block within the same H2 is in-scope under R-1A.c ONLY if its content is plausibly an excerpt of the cited `path`. A block satisfies the guard if AT LEAST ONE of these holds:
       - The block's language fence is consistent with the source's extension (e.g., `.rs` → `rust`, `rust,ignore`, `rust,no_run`; `.md` → `markdown`; `.toml` → `toml`).
       - The source file's basename (e.g., `s44_server_skills`) appears literally in the block's text.
       - A function signature, struct name, or type name defined in the cited source appears literally in the block.

       Blocks failing the guard are recorded as OUT OF SCOPE (not in-scope, not FAIL). Example: course Skills chapter L175/176 anchor cites `examples/s44_server_skills.rs` (Rust); the following fenced block at ~L191 is a `markdown` block excerpting `examples/skills/refunds/SKILL.md` — language fence is `markdown` (not Rust), no `s44_server_skills` substring, no Rust function signature → OUT OF SCOPE under R-1A.c guard, NOT a FAIL. (Audit 81-07 Section "Audit-A Coverage Floor Note (revision W-7)" identified the course Skills chapter at lines 175/176, 238/239, 341/342, 343/344 as using this two-line format; PLAN 81-10 enumerates each anchor and records the actual contribution count after applying the guard.)

    4. **Source-side cosmetic omission (R-1A.d):** If the chapter block elides non-load-bearing source lines (commentary, `println!` debug output, doc comments) WITHOUT a `// ...` placeholder marker, AND removing those source-side lines from the comparison yields a contiguous-substring match, the drift is recorded as **WARN** (cosmetic drift, not algorithmic failure). "Non-load-bearing" means: no struct field changes, no control-flow change (no added/removed `if`/`match`/`for`/`while`/early-return), no API shape difference (no function signature change, no trait-impl-set change). Lines that DO change semantics still produce FAIL. (Audit 81-07 Finding 5 — `pmcp-course/src/part8-advanced/ch22-code-mode.md:228` — is PREDICTED to fall into this WARN category under R-1A.d: source `examples/s41_code_mode_graphql.rs` L146 is a comment, L169 is a `println!` debug line; both are non-load-bearing; the block omits them without a `// ...` marker. Plan 81-10 verifies empirically.)

    5. **W-7 floor reclassification when N<10 (R-1A.e):** If the relaxed Audit A scan (applying clauses a–d) returns fewer than 10 in-scope excerpts, the W-7 coverage-floor finding is recorded as **WARN** (structural drift in chapter anchor density), not FAIL. The N>=10 ambition is preserved as a target; the WARN documents the cap honestly. Phase 81 remains shippable under PASS WITH WARNINGS in this case. Rationale: the strict `>= 10` floor was set when the audit scope was narrowly defined; with the relaxations un-narrowing scope, the floor remains the goal but its enforcement softens — anchors are a chapter-author decision and an audit cannot manufacture them.
    ```

    Then amend the original algorithm pseudo-shell block (keep it intact below the R-1A relaxation paragraph), making these inline changes:
    - The `normalize:` step changes from `normalize: strip trailing whitespace from each line; collapse runs of trailing-empty lines.` to `normalize: strip leading whitespace to common minimum-indent per block (R-9.a, applied symmetrically to block and source); strip trailing whitespace from each line; collapse runs of trailing-empty lines.`.
    - The anchor-scanner step gains: `accept two-line anchor format per R-9.c; apply the source-match guard before classifying the following block as in-scope.`.
    - The FAIL branch is split into three sub-branches:
      - `if the gap is bridged by a // ... placeholder in the block (R-9.b): record WARN with chapter line + elided-source-range citation`
      - `else if the gap is bridged by source-side elision of non-load-bearing lines (commentary/println!/doc-comments) per R-9.d: record WARN with chapter line + elided-source-line list`
      - `else: record FAIL with chapter line + first-differing-line citation`
    - A new W-7 reporting step is added: `record actual in-scope excerpt count N; if N >= 10 classify W-7 as PASS; if N < 10 classify W-7 as WARN per R-9.e (do NOT record W-7 as FAIL — the phase remains shippable).`

    ### Edit 4 — Task 3 `<verify>` block (W-7 hard-FAIL → WARN-reclassifiable)

    Locate the `<verify>` block of Task 3 that currently includes a hardcoded `Total excerpts checked` floor check. If the current verify shell pipeline asserts `EXCERPT_COUNT >= 10` as a hard FAIL, REPLACE that assertion with: count the actual N, record it in the audit report, and only FAIL if the audit report does NOT contain an explicit W-7 disposition (PASS or WARN per R-9.e). The verify must NOT short-circuit on `[ N -ge 10 ]`. Concretely:

    Replace any line resembling `[ "${EXCERPT_COUNT:-0}" -ge 10 ]` with a check that the report contains a `W-7` disposition line — for example, `grep -qE 'W-7.*(PASS|WARN)' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md` — and document in the surrounding comment that the W-7 disposition is PASS if N>=10 and WARN if N<10 per R-9.e.

    (If the current `<verify>` block in 81-07-PLAN.md does NOT have a hardcoded `-ge 10` check — it may delegate to the audit report's self-check — leave the shell pipeline as-is and instead extend the explanatory paragraph immediately above it to state: "the W-7 floor is no longer a hard FAIL gate per R-9.e; the audit report records W-7 as PASS when N>=10 and as WARN when N<10.")

    ### Edit 5 — Task 3 `<done>` text

    The `<done>` paragraph currently ends with the Shippability discussion. Append the following sentence at the END of `<done>` (after the existing closing period):

    ```
    Audit A is executed per revision R-9 (R-1A) relaxations: (a) de-indentation tolerant matching; (b) `// ...` placeholder reclassified as WARN-severity; (c) two-line anchor format accepted across line breaks WITH a source-match guard (next block must plausibly excerpt the cited source via language-fence match or filename/signature appearance); (d) source-side cosmetic omission of non-load-bearing commentary/`println!` lines without a `// ...` marker is WARN if elision yields a contiguous match; (e) the W-7 floor (`Total excerpts checked >= 10`) is reclassified to WARN when N<10 — the phase remains shippable.
    ```

    ### Edit 6 — Threat model T-81-07-03

    The current row (around L407) reads:

    ```
    | T-81-07-03 | False negative (revision R-1 fix) | Inline-excerpt drift audit | mitigate | The previous single-substring-grep heuristic could miss obvious drift (only one line of the excerpt needed to match). Audit A is rebuilt with explicit `Full example:` anchor + contiguous-block normalized-substring matching (Codex + Gemini convergent fix). Synthetic snippets opt out via `<!-- synthetic -->` AND are explicitly counted (revision R-1) — a degenerate audit that marks everything synthetic cannot pass because the verify block requires the synthetic count to be reported. |
    ```

    Append to the "Mitigation Plan" cell (the last column), AFTER the existing text:

    ```
    Revision R-9 (R-1A) relaxes the contiguous-match algorithm: (a) de-indentation tolerant matching, (b) `// ...` placeholders reclassified as WARN, (c) two-line anchor format accepted, (d) source-side cosmetic omission reclassified as WARN, (e) W-7 floor reclassified to WARN if N<10. The new failure modes — false-negative drift hidden behind aggressive de-indentation, `// ...` placeholders, two-line anchors with mismatched following blocks, or silent source-side omission — are mitigated by: (i) the de-indent normalization being symmetric (applied to BOTH block and source, so the substring relation is preserved); (ii) `// ...` placeholders being explicitly recorded as WARN findings with chapter line + source-line-range citations; (iii) the R-1A.c source-match guard rejecting blocks whose language fence or content doesn't plausibly correspond to the cited source (so a markdown block following a Rust anchor does NOT silently become in-scope); (iv) R-1A.d requiring the elided source-side lines to be non-load-bearing (no control-flow / API shape / struct-field changes) AND requiring elision to yield a contiguous match (so semantic drift still FAILs); (v) R-1A.e softening the W-7 floor to WARN keeps shippability while still surfacing the anchor-density gap as a documented finding.
    ```

    ### Edit 7 — Verification section

    Locate the bullet currently reading:

    ```
    - Audit A uses contiguous-block normalized-substring matching anchored on `Full example: [...](URL)` (revision R-1 — Codex + Gemini convergent fix).
    ```

    Replace with:

    ```
    - Audit A uses contiguous-block normalized-substring matching anchored on `Full example: [...](URL)` (revision R-1 — Codex + Gemini convergent fix), executed per revision R-9 (R-1A) relaxations: (a) de-indentation tolerant (common minimum-indent stripped before compare, applied symmetrically); (b) `// ...` placeholder reclassified as WARN-severity; (c) two-line anchor format accepted, gated by a source-match guard (language-fence consistency OR filename/signature/struct-name appearance); (d) source-side cosmetic omission of non-load-bearing lines without a `// ...` marker is WARN if elision yields a contiguous match; (e) the W-7 floor of `Total excerpts checked >= 10` is reclassified to WARN when N<10 — phase remains shippable.
    ```

    ### Edit 8 — Append revision note R-9 after `</output>`

    At the very END of the file (after the closing `</output>` tag), append the following section:

    ```markdown

    ## Revision Notes

    ### R-9 (2026-05-15) — Audit A relaxation per R-1A (user-locked decision in gap-closure, revised in pass 2 to add clauses d + e)

    **Trigger:** `81-07-AUDIT.md` recorded 5 FAIL-severity Audit A findings (Findings 1–5) for de-indented and structurally-abbreviated excerpts in Wave 1 Code Mode chapters, plus a W-7 coverage-floor finding (unreachable >=10 in-scope excerpts) due to the strict single-line anchor regex missing the course Skills chapter's two-line anchor format.

    **User decision (R-1A — locked):** Relax Audit A's algorithm rather than rewrite Wave 1 chapter content. Chapter pedagogy is preserved.

    **Changes (five clauses):**
    - (a) Normalization adds leading-whitespace stripping to a common minimum-indent per block (de-indentation tolerant). Applied symmetrically to block AND source.
    - (b) `// ...` placeholders reclassified as WARN-severity, not FAIL. WARN findings still surface in the report with chapter line + elided-source-range citations.
    - (c) `Full example:` anchor scanner accepts both single-line and two-line forms (`Full example:\n[`path`](url)`), matching across the line break, **gated by a source-match guard**: the next fenced block within the same H2 is in-scope under R-1A.c ONLY if its language fence is consistent with the source extension OR the source's basename / a function signature / a struct name from the cited source appears in the block. Blocks failing the guard are OUT OF SCOPE (not in-scope, not FAIL). This prevents a markdown block following a `.rs` anchor from being silently mis-compared.
    - (d) Source-side cosmetic omission reclassified as WARN. If a chapter block elides non-load-bearing source lines (commentary, `println!`, doc comments) WITHOUT a `// ...` placeholder marker, AND removing those source-side lines from the comparison yields a contiguous match, record WARN. "Non-load-bearing" is defined narrowly: no struct field changes, no control-flow change, no API shape difference. Lines that change semantics still produce FAIL.
    - (e) W-7 floor reclassified to WARN when the relaxed scan yields N<10. The N>=10 ambition is preserved as a target; the WARN documents the cap honestly. Phase 81 remains shippable under PASS WITH WARNINGS in this case.

    **Closure prediction (NOT a guarantee — actual disposition is recorded by plan 81-10's empirical re-audit):**
    - Findings 1, 2, 3 (book ch12-9 de-indent only) → predicted PASS under R-1A.a.
    - Finding 4 (course ch22 L196 de-indent + `// ...` placeholder) → predicted WARN under R-1A.b.
    - Finding 5 (course ch22 L228 de-indent + source-side commentary/println! omission without `// ...`) → predicted WARN under R-1A.d.
    - W-7 (coverage floor) → predicted WARN under R-1A.e (best-case in-scope N from named chapters is ~9; R-1A.c with source-match guard may add 0–4 from course Skills chapter depending on guard outcomes; plan 81-10 records actual N).

    **What this revision does NOT change:**
    - Chapter content in `pmcp-book/src/ch12-9-code-mode.md`, `pmcp-course/src/part8-advanced/ch22-code-mode.md`, `pmcp-course/src/part8-advanced/ch23-skills.md` — unchanged.
    - Audit B (cross-link), C (SUMMARY.md), D (doctest byte-equality), E (version-pin), F (cross-property prose) — unchanged.
    - The `Total excerpts checked: N` numeric reporting requirement — unchanged (still asserted; classification softened to WARN if N<10).
    - The `Synthetic excerpts skipped: M` reporting requirement — unchanged.

    **Re-audit:** Plan 81-10 re-runs the audit under R-9 (clauses a–e) and produces `81-10-AUDIT.md` recording the empirical verdict for each of the 7 findings in `81-07-AUDIT.md`.
    ```

    Commit message template (use this exactly):

    ```
    docs(81-09): relax Audit A spec per R-1A (revision R-9, clauses a/b/c/d/e)

    Amends Audit A's algorithm in 81-07-PLAN.md per user-locked decision R-1A
    from the gap-closure planning session (revision pass 2 adds clauses d, e
    after plan-checker identified BLOCKERs 1, 2 in pass 1). Five relaxations:

    (a) De-indentation tolerant: strip common minimum-indent per block before
        contiguous-substring match (symmetric: applied to block AND source).
    (b) `// ...` structural-abbreviation placeholders reclassified as
        WARN-severity drift (not FAIL).
    (c) Two-line anchor format `Full example:\n[path](url)` accepted across
        line breaks, GATED by a source-match guard (next block must plausibly
        excerpt the cited source — language-fence match or filename/signature
        appearance). Markdown/TOML blocks following a Rust anchor are OUT OF
        SCOPE under the guard, not silently mis-compared.
    (d) Source-side cosmetic omission of non-load-bearing lines (commentary,
        `println!`, doc comments) without a `// ...` placeholder marker is
        WARN if elision yields a contiguous match. Lines that change
        semantics still FAIL.
    (e) W-7 coverage floor (>=10 in-scope excerpts) reclassified to WARN
        when the relaxed scan yields N<10. The phase remains shippable; the
        WARN documents the anchor-density cap honestly.

    No chapter content modified — Wave 1 pedagogy is preserved per user
    decision. Predicts reclassification of 81-07-AUDIT.md Findings 1, 2, 3
    to PASS (R-1A.a); Finding 4 to WARN (R-1A.b); Finding 5 to WARN (R-1A.d);
    W-7 to WARN (R-1A.e) if relaxed-scan N<10. Actual disposition recorded
    in plan 81-10's `81-10-AUDIT.md` after empirical re-audit.
    ```
  </action>
  <verify>
    <automated>grep -q 'revision R-9' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'R-1A' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'de-indentation tolerant' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'WARN-severity' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'two-line anchor' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'source-match guard' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'R-1A\.d\|R-9\.d\|R-1A.d\|R-9.d' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'R-1A\.e\|R-9\.e\|R-1A.e\|R-9.e' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'source-side cosmetic omission\|source-side omission' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'non-load-bearing' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'W-7.*WARN\|W-7 floor reclassified\|W-7 coverage-floor' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q '## Revision Notes' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && grep -q 'R-9 (2026-05-15)' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md && [ "$(git diff --name-only HEAD -- pmcp-book/src/ch12-9-code-mode.md pmcp-course/src/part8-advanced/ch22-code-mode.md pmcp-course/src/part8-advanced/ch23-skills.md 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && echo "R-9 amendment (clauses a/b/c/d/e + source-match guard) present in 81-07-PLAN.md; no chapter content modified by this plan."</automated>
  </verify>
  <done>
    `81-07-PLAN.md` contains revision R-9 with all FIVE R-1A relaxations documented (a de-indentation tolerant, b `// ...` WARN, c two-line anchor WITH source-match guard, d source-side cosmetic omission WARN, e W-7 floor reclassified to WARN when N<10). Audit A algorithm description in Task 3 references R-1A clauses a/b/c/d/e. Frontmatter `must_haves.truths`, `<objective>`, `<verify>` (W-7 hard-FAIL removed in favor of WARN-reclassifiable), `<done>`, `<threat_model>` T-81-07-03, and `<verification>` are all updated. A `## Revision Notes` section exists at the end of the file with the R-9 entry covering all five clauses. NO chapter content files (Wave 1 deliverables) are modified by this plan — verified by `git diff --name-only` against `pmcp-book/src/ch12-9-code-mode.md`, `pmcp-course/src/part8-advanced/ch22-code-mode.md`, `pmcp-course/src/part8-advanced/ch23-skills.md`.
  </done>
</task>

</tasks>

<threat_model>
Spec-amendment only. No code, no chapter content, no auth or trust-boundary changes.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-09-01 | Audit too-loose (false negatives) | Audit A under R-1A | mitigate | R-1A.a applies normalization SYMMETRICALLY (block AND source) so substring relations are preserved. R-1A.b records `// ...` placeholders as WARN with explicit chapter line + elided-source-range citations — drift remains visible. R-1A.c source-match guard rejects blocks whose language fence or content doesn't correspond to the cited source (markdown after a Rust anchor → OUT OF SCOPE, not silent mis-compare). R-1A.d narrowly defines "non-load-bearing" (no control-flow / API shape / struct-field changes) AND requires elision to yield a contiguous match — semantic drift still FAILs. |
| T-81-09-02 | Spec-without-implementation drift | 81-07-PLAN.md vs re-audit | mitigate | Plan 81-10 explicitly re-runs the audit per the R-9 spec (clauses a–e). Plan 81-10's Task 2b records the per-finding disposition narrative from empirical scan results — predictions in 81-09 are NOT treated as ground truth. |
| T-81-09-03 | Reviewer confusion (multiple revisions + revision passes) | Plan file history | accept | The R-N revision-note convention is already established (R-1, R-2, R-8 visible). R-9 fits cleanly and the revision note explicitly records "pass 2 adds clauses d, e" so the audit history reads cleanly. |
| T-81-09-04 | W-7 over-soft (anchor-density gap hidden) | W-7 → WARN under R-1A.e | mitigate | R-1A.e records the actual N and surfaces the gap as a WARN finding (not silent PASS). The WARN documents the anchor-density cap; future phases may revisit the chapter anchor structure if reaching N>=10 becomes a priority. |
</threat_model>

<verification>
- `81-07-PLAN.md` contains `revision R-9` and `R-1A` strings.
- The strings `de-indentation tolerant`, `WARN-severity`, `two-line anchor`, `source-match guard`, `source-side cosmetic omission` (or `source-side omission`), `non-load-bearing`, and a W-7-related WARN reclassification phrase all appear in the amended Audit A algorithm description.
- The `## Revision Notes` section at the end of the file contains an `R-9 (2026-05-15)` entry covering clauses a/b/c/d/e.
- The Task 3 `<verify>` block does NOT hardcode `EXCERPT_COUNT >= 10` as a FAIL gate (W-7 is WARN-reclassifiable per R-9.e).
- `git diff --name-only HEAD` lists `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-PLAN.md` as modified, and does NOT list any file under `pmcp-book/src/` or `pmcp-course/src/`.
- Pre-commit hook (`make quality-gate`) passes — this is a planning-doc edit only.
</verification>

<success_criteria>
- Audit A's `<verify_block>` in `81-07-PLAN.md` is amended per R-1A clauses (a), (b), (c with source-match guard), (d), and (e).
- W-7 coverage-floor reclassification (WARN when N<10) is documented in the R-9 revision note.
- Source-match guard for R-1A.c is documented with concrete pass-criteria (language-fence consistency, basename appearance, or signature/struct-name appearance).
- Source-side cosmetic omission rule (R-1A.d) is documented with the "non-load-bearing" definition and the contiguous-match-on-elision requirement.
- Wave 1 chapter content is NOT modified.
- The amendment is committed with a message referencing R-1A clauses a–e and revision R-9 — and the commit message explicitly weakens dispositions to PREDICTIONS, not guarantees (actual disposition recorded by plan 81-10's empirical re-audit).
- Plan 81-10's re-audit executes the relaxed Audit A per clauses a–e and records empirical outcomes for each of the 7 findings in `81-07-AUDIT.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-09-SUMMARY.md` recording:
- Which sections of `81-07-PLAN.md` were edited (frontmatter, objective, Task 3 action including all five R-1A clauses, Task 3 verify with W-7 hard-FAIL removed, Task 3 done, threat model T-81-07-03, verification, new `## Revision Notes`).
- The five R-1A relaxations now in the spec: (a) de-indentation tolerant, (b) `// ...` WARN, (c) two-line anchor WITH source-match guard, (d) source-side cosmetic omission WARN, (e) W-7 floor WARN when N<10.
- Confirmation that NO chapter content files were modified.
- One-line verdict: `Audit A spec relaxed per R-1A clauses a/b/c/d/e; ready for plan 81-10 empirical re-audit.`
- Predicted re-audit outcomes under R-9 (PREDICTIONS — plan 81-10 records actuals): Findings 1, 2, 3 predicted PASS (R-1A.a); Finding 4 predicted WARN (R-1A.b); Finding 5 predicted WARN (R-1A.d); W-7 predicted WARN (R-1A.e) if relaxed scan yields N<10.
</output>

## PLANNING COMPLETE
