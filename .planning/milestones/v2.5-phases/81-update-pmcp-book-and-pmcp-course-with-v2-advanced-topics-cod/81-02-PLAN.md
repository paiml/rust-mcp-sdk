---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-book/src/ch12-9-code-mode.md
autonomous: true
requirements: []
tags: [docs, book, code-mode]
must_haves:
  truths:
    - "Reader sees #[derive(CodeMode)] as the canonical happy path within the first H2 section, not buried after manual handler registration."
    - "Reader knows which language attribute value enables which feature flag (graphql, javascript, sql, mcp)."
    - "Reader can choose between direct CodeExecutor impl and the three standard adapters (JsCodeExecutor, SdkCodeExecutor, McpCodeExecutor) based on backend pattern."
    - "Reader knows the HMAC security model: tokens stateless, default TTL 5min, TokenSecret zeroize-on-drop, minimum 16-byte secret enforced at construction."
    - "Reader can find a runnable end-to-end example (s41_code_mode_graphql.rs) and the source-of-truth crate docs (pmcp-code-mode + pmcp-code-mode-derive)."
  artifacts:
    - path: pmcp-book/src/ch12-9-code-mode.md
      provides: "Code Mode book chapter — full rewrite against current pmcp-code-mode / pmcp-code-mode-derive API surface."
      min_lines: 300
      contains: "#[derive(CodeMode)]"
  key_links:
    - from: pmcp-book/src/ch12-9-code-mode.md
      to: examples/s41_code_mode_graphql.rs
      via: "Inline excerpts + 'Full example:' GitHub permalink"
      pattern: "examples/s41_code_mode_graphql.rs"
    - from: pmcp-book/src/ch12-9-code-mode.md
      to: crates/pmcp-code-mode/src/lib.rs
      via: "Inline excerpts demonstrating CodeModeConfig, TokenSecret, ValidationContext, NoopPolicyEvaluator types"
      pattern: "pmcp_code_mode::"
    - from: pmcp-book/src/ch12-9-code-mode.md
      to: crates/pmcp-code-mode-derive/src/lib.rs
      via: "Inline excerpts demonstrating #[derive(CodeMode)] expansion behavior"
      pattern: "code_mode_derive"
---

<objective>
Full rewrite of `pmcp-book/src/ch12-9-code-mode.md` (currently a 146-line version — predates the `#[derive(CodeMode)]` macro and the split-out `pmcp-code-mode` / `pmcp-code-mode-derive` crates) against the current API surface. Per CONTEXT.md D-02 and 81-CONTEXT.md "derive-macro-first framing": the rewrite must NOT lead with manual handler registration as the canonical path; `#[derive(CodeMode)]` is the happy path, manual registration is the advanced/escape-hatch variant.

Purpose: The current 146-line chapter still teaches the derive macro but is light on the validation pipeline, missing the standard adapters (JsCodeExecutor / SdkCodeExecutor / McpCodeExecutor) framing as a decision tree, and missing the success/rejection round-trip demonstration that `s41_code_mode_graphql.rs` walks end-to-end. The rewrite restructures the chapter around a single worked example (s41_code_mode_graphql.rs) end-to-end (CONTEXT.md "Claude's Discretion" item — choosing one worked example over multiple smaller snippets is justified by the chapter's pedagogy goal: reader can `cargo run --example s41_code_mode_graphql --features full` after the chapter and see every concept fire).

Output:
- `pmcp-book/src/ch12-9-code-mode.md` (~300-450 lines, six H2 sections), replacing the existing 146-line content.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md
@pmcp-book/src/ch12-9-code-mode.md
@pmcp-book/src/ch12-7-tasks.md
@examples/s41_code_mode_graphql.rs
@crates/pmcp-code-mode/src/lib.rs
@crates/pmcp-code-mode-derive/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read current code-mode crate API surface and derive macro behavior</name>
  <files>(read-only — produces no file edits)</files>
  <read_first>
    - `crates/pmcp-code-mode/src/lib.rs` — read top of file for module re-exports, list of pub modules (config, handler, validation, policy, schema_exposure, templates, code_executor, policy_annotations). Catalog the major pub types: `CodeModeConfig`, `TokenSecret`, `NoopPolicyEvaluator`, `ValidationContext`, `ValidationPipeline`, `CodeExecutor`, `ExecutionError`, `ExecutionConfig`. (You will quote these names in chapter prose — getting them right is non-negotiable.)
    - `crates/pmcp-code-mode-derive/src/lib.rs` — read the doc comment for the `code_mode_derive` proc macro (locate via `grep -n 'pub fn code_mode_derive' crates/pmcp-code-mode-derive/src/lib.rs`). Catalog the supported attributes: `#[code_mode(context_from = "...", language = "...")]`, plus the four `language` values (`graphql`, `javascript`, `sql`, `mcp`) and their feature gates (`openapi-code-mode`, `sql-code-mode`, `mcp-code-mode`). The required field-name convention for the annotated struct: `code_mode_config: CodeModeConfig`, `token_secret: TokenSecret`, `policy_evaluator: Arc<impl PolicyEvaluator>`, `code_executor: Arc<impl CodeExecutor>`.
    - `examples/s41_code_mode_graphql.rs` — read end-to-end. This is the source of truth for the worked-example narrative in the chapter. The example walks: define server struct → derive CodeMode → register tools on builder → demonstrate success path (validate → token → execute) → demonstrate rejection path (mutation blocked by default config).
    - The current `pmcp-book/src/ch12-9-code-mode.md` — note what existing content can be reused (the security-properties summary, the language table, the config.toml deployment block — all roughly correct but need verification against current API).
    - Cargo.toml at the workspace root — confirm published versions of `pmcp-code-mode` and `pmcp-code-mode-derive` (you will cite them in the "Add Dependencies" section). Use the version pins under `[workspace.dependencies]` or each crate's own Cargo.toml; do NOT guess. If a version exists at `crates/pmcp-code-mode/Cargo.toml::version`, that is the source of truth.

    No file edits in this task — it is the discovery step that informs Task 2's rewrite content.
  </read_first>
  <action>
    Produce no file output. Internally catalog the following before Task 2:

    1. The exact pub re-export list from `pmcp-code-mode` (which types are reachable as `pmcp_code_mode::Foo` vs requiring a nested path).
    2. The exact derive macro attribute set + the required-field-name convention.
    3. The exact `language` → feature-flag table from the derive macro source.
    4. The current pinned versions of `pmcp-code-mode` and `pmcp-code-mode-derive` in this repo's Cargo.toml.
    5. The 5-step narrative of `s41_code_mode_graphql.rs` (server struct → derive → register on builder → success path → rejection path), with grep-anchored landmarks for the chapter excerpts in Task 2 (NOT hardcoded line numbers — see Task 2's grep anchor list).

    Output for this task is internal — Task 2 consumes these findings. The chapter rewrite would be wrong if attempted without this catalog.
  </action>
  <verify>
    <automated>test -f crates/pmcp-code-mode/src/lib.rs &amp;&amp; test -f crates/pmcp-code-mode-derive/src/lib.rs &amp;&amp; test -f examples/s41_code_mode_graphql.rs &amp;&amp; grep -q 'pub fn code_mode_derive' crates/pmcp-code-mode-derive/src/lib.rs &amp;&amp; grep -q '#\[derive(CodeMode)\]' examples/s41_code_mode_graphql.rs</automated>
  </verify>
  <done>
    Task 2's executor has cataloged: re-export list, derive macro attributes, language→feature table, current crate versions, and the s41 example structure. Verification confirms all source files exist; the executor proceeds with current-API-accurate content for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full rewrite of pmcp-book/src/ch12-9-code-mode.md against current API</name>
  <files>pmcp-book/src/ch12-9-code-mode.md</files>
  <read_first>
    - Output of Task 1 (the API catalog) — non-negotiable input.
    - `pmcp-book/src/ch12-7-tasks.md` — structural model for chapter shape (Title → Why → diagram → narrative with inline excerpts → "Full example" cross-link). Mirror this.
    - `pmcp-book/src/ch12-9-code-mode.md` (current 146-line version) — read again. Identify content to KEEP verbatim (the HMAC token diagram is structurally sound and matches the current security model; reuse it — see grep anchor below). Identify content to REPLACE (the rest of the chapter — "Using the Derive Macro" section onward — is correct but thin; rewrite for depth).
    - `examples/s41_code_mode_graphql.rs` — Re-read to pick grep-stable inline excerpts. Use the following grep anchors (revision R-6 — NOT hardcoded line ranges, which rot if the example is reformatted):
      - (a) The struct definition with `#[derive(CodeMode)]`: locate via `grep -n '#\[derive(CodeMode)\]' examples/s41_code_mode_graphql.rs | head -1`. Quote from the `#[derive(CodeMode)]` line through the closing `}` of `struct MyGraphQLServer { ... }` (typically 8-10 lines).
      - (b) The `register_code_mode_tools(builder)` invocation: locate via `grep -n 'register_code_mode_tools' examples/s41_code_mode_graphql.rs | head -1`. Quote from the preceding `let builder = pmcp::Server::builder();` line through the `println!` that follows the registration (typically 6-8 lines).
      - (c) The success-path validation block: locate via `grep -n -- '--- Success Path' examples/s41_code_mode_graphql.rs | head -1`. Quote from that `println!` line through the closing `}` of the outer `match pipeline.validate_graphql_query(...)` block (typically 30-35 lines).
      - (d) The rejection-path validation block: locate via `grep -n -- '--- Rejection Path' examples/s41_code_mode_graphql.rs | head -1`. Quote from that `println!` line through the closing `}` of the outer `match pipeline.validate_graphql_query(...)` block (typically 22-26 lines).

      Each excerpt must be a clean function or struct boundary so a future drift check can verify.
    - `81-CONTEXT.md` <specifics> section: "Code Mode chapter — derive-macro-first framing" — derive macro is the canonical happy path; manual handler registration is escape-hatch.
  </read_first>
  <action>
    Overwrite `pmcp-book/src/ch12-9-code-mode.md` with a full rewrite. Six H2 sections in this order:

    **H1 title:** `# Chapter 12.9: Code Mode — Validated LLM Code Execution`

    **Lead paragraph (before any H2):** Frame the problem. ~4-6 sentences. State that Code Mode is PMCP's framework for safely executing LLM-generated code (GraphQL, JavaScript, SQL, MCP compositions) with cryptographic approval tokens that bind the validated code to a specific user, session, and schema version. State that PMCP ships Code Mode as two cooperating crates: `pmcp-code-mode` (runtime types + validation pipeline) and `pmcp-code-mode-derive` (the `#[derive(CodeMode)]` proc macro). The chapter walks one worked example end-to-end: `examples/s41_code_mode_graphql.rs`.

    **## The Problem (Why Cryptographic Approval Tokens)**
    - Three questions framing (from current ch22 in the course — reuse the structure): Is the LLM-generated code safe? Authorized? Tamper-free between approval and execution?
    - State that without Code Mode, servers hand-roll validation, token generation, and policy enforcement for every code-generating workflow — leading to inconsistent security boundaries and duplicated boilerplate.
    - REUSE the ASCII pipeline diagram from the current chapter — locate via `grep -n '^## How It Works' pmcp-book/src/ch12-9-code-mode.md | head -1` (revision R-6 — NOT hardcoded "lines 13-36"). The diagram block starts on the line after that H2 anchor (the opening ```text fence) and runs through the matching closing ``` fence. Within it you should see the `validate_code → ValidationPipeline → approval_token → execute_code → CodeExecutor::execute` flow. This diagram is structurally correct against the current crate source.
    - One paragraph on HMAC binding: token binds {code hash, user ID, session ID, server ID, context hash, risk level, expiry}. Any modification to the code after validation invalidates the token. Tokens are stateless (HMAC-verified, not stored server-side); default TTL is 5 minutes.

    **## Adding Code Mode with `#[derive(CodeMode)]`** (this is the canonical happy path per 81-CONTEXT.md derive-macro-first framing)
    - One-paragraph framing: the derive macro eliminates ~80 lines of handler boilerplate per server.
    - Subsection-level numbered steps (use `### Step N: ...` H3 headers):

      `### Step 1: Add Dependencies`
      - `toml` fenced block (NOT rust) with the current version pins discovered in Task 1. Format:
        ```toml
        [dependencies]
        pmcp = { version = "X.Y.Z", features = ["full"] }
        pmcp-code-mode = "A.B.C"
        pmcp-code-mode-derive = "D.E.F"
        ```
        Replace placeholders with actual versions from Cargo.toml. If a version cannot be resolved from this repo's Cargo.toml workspace, fall back to `version = "*"` for that line and add a footnote: "See `crates/pmcp-code-mode/Cargo.toml` for the current pinned version." DO NOT make up version numbers.

      `### Step 2: Derive and Configure`
      - Inline excerpt from `examples/s41_code_mode_graphql.rs` — the `MyGraphQLServer` struct with `#[derive(CodeMode)]`, the four required fields, the constructor block. Locate via `grep -n '#\[derive(CodeMode)\]' examples/s41_code_mode_graphql.rs | head -1` and quote from that line through the closing `}` of the `struct MyGraphQLServer { ... }` block (~8-12 lines; clean struct boundary). NOT hardcoded "lines 63-71" per revision R-6.
      - Note: The four required field names (`code_mode_config`, `token_secret`, `policy_evaluator`, `code_executor`) are the v0.1.0 convention required by the derive macro. State this is a hard requirement, not a style guideline.
      - Note that `#[code_mode(context_from = "get_context", language = "graphql")]` is supported but the s41 example omits both because it uses the GraphQL default + the default `ValidationContext`. Show what a custom `context_from` looks like with a 3-line `rust,ignore` snippet — for the snippet shape, refer to the existing chapter's derive-macro example block, located via `grep -n 'fn get_context' pmcp-book/src/ch12-9-code-mode.md | head -1` (quote the 3-4 lines from the `fn get_context` signature through the `ValidationContext::new(...)` return). NOT hardcoded "lines 53-56" per revision R-6.

      `### Step 3: Pick a Language`
      - Reproduce the language attribute table from the current chapter, but verify against the derive macro source first. Locate the existing table via `grep -n '^| Language' pmcp-book/src/ch12-9-code-mode.md | head -1` and quote from that header row through the last data row (typically the `| MCP |` row). NOT hardcoded "lines 70-77" per revision R-6. Current expected table content:

        | Language | `language =` value | Feature Required |
        |----------|--------------------|------------------|
        | GraphQL | `"graphql"` (default) | *(none)* |
        | JavaScript | `"javascript"` | `openapi-code-mode` |
        | SQL | `"sql"` | `sql-code-mode` |
        | MCP | `"mcp"` | `mcp-code-mode` |

        Verify this table against `crates/pmcp-code-mode-derive/src/lib.rs` before reproducing — if the derive macro's match arms have changed, the table reflects the source not the existing chapter.

      `### Step 4: Choose Your Executor` (decision tree)
      - Two paragraphs framing: there are two paths — direct `CodeExecutor` impl or standard adapter.
      - **Direct impl** (when your backend is a database connection, an HTTP client to a specific service, or an in-process schema): show the `GraphQLExecutor` impl from `examples/s41_code_mode_graphql.rs` as the inline excerpt. Locate via `grep -n 'impl CodeExecutor for GraphQLExecutor' examples/s41_code_mode_graphql.rs | head -1` and quote from the `#[pmcp_code_mode::async_trait]` attribute line directly preceding the `impl` through the closing `}` of that `impl` block (~20 lines; clean impl boundary). NOT hardcoded "lines 26-50" per revision R-6. Cross-link: ``Full example: [`examples/s41_code_mode_graphql.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/s41_code_mode_graphql.rs)``.
      - **Standard adapters** (when your backend is HTTP, AWS SDK, or routing to another MCP server): reuse the 3-line snippet from the current chapter showing `JsCodeExecutor`, `SdkCodeExecutor`, `McpCodeExecutor` construction. Locate the existing snippet via `grep -n '^### Standard Adapters' pmcp-book/src/ch12-9-code-mode.md | head -1` and quote the fenced `rust,ignore` block that follows that H3 (the block contains three `let executor = Arc::new(...)` lines). NOT hardcoded "lines 84-93" per revision R-6. Note which feature flag each requires (research from the executor crate source if unclear). Briefly explain when each fits — Cost Coach uses `JsCodeExecutor`; an SDK-direct integration uses `SdkCodeExecutor`; a server-composition pattern uses `McpCodeExecutor`.

      `### Step 5: Register on the Builder and Build`
      - Inline excerpt from `examples/s41_code_mode_graphql.rs` — the `MyGraphQLServer { ... }` constructor + `register_code_mode_tools(builder)` invocation. Locate via `grep -n 'register_code_mode_tools' examples/s41_code_mode_graphql.rs | head -1` and quote from the `let server = MyGraphQLServer {` line that precedes the registration through the `println!("Registered validate_code and execute_code tools on builder.");` that immediately follows. ~14-16 lines including the surrounding builder context. NOT hardcoded "lines 79-93" per revision R-6.
      - One sentence stating that the derive macro generates `register_code_mode_tools(builder)` which adds `validate_code` and `execute_code` tools.

    **## Worked Example: GraphQL Round-Trip (`s41_code_mode_graphql.rs`)**
    - Frame: this section walks the success and rejection paths from `examples/s41_code_mode_graphql.rs`. Cross-link to the GitHub permalink so reader can `cargo run --example s41_code_mode_graphql --features full` after the chapter.
    - **Success path:** Inline excerpt of the `--- Success Path: Valid GraphQL Query ---` block. Locate via `grep -n -- '--- Success Path' examples/s41_code_mode_graphql.rs | head -1` and quote from that `println!("--- Success Path: Valid GraphQL Query ---");` line through the closing `}` of the outer `match pipeline.validate_graphql_query(query, &context) { ... }` block (typically 30-35 lines; clean match-arm boundary). NOT hardcoded "lines 108-143" per revision R-6. Annotate with prose explaining what each call does: `validate_graphql_query` runs the pipeline (parse → security scan → policy check → explain → HMAC sign); a non-empty `approval_token` means validation passed; `code_executor.execute(query, None).await` produces a mock result.
    - **Rejection path:** Inline excerpt of the `--- Rejection Path: Mutation Blocked by Config ---` block. Locate via `grep -n -- '--- Rejection Path' examples/s41_code_mode_graphql.rs | head -1` and quote from that `println!("\n--- Rejection Path: Mutation Blocked by Config ---");` line through the closing `}` of the outer `match pipeline.validate_graphql_query(mutation, &context) { ... }` block (typically 22-26 lines; clean match-arm boundary). NOT hardcoded "lines 147-171" per revision R-6. Annotate: `CodeModeConfig::enabled()` defaults to `allow_mutations: false`; a mutation produces `is_valid = false` and no `approval_token`; the rejection demonstrates that a denied operation never receives a token — there is no second chance, the LLM cannot "ask again with the previous token."
    - One paragraph framing on the security property: the rejection happens *before* the token is minted. No code can reach `execute_code` without a valid token; no token exists for code that was rejected. This is the load-bearing security property — make it explicit.

    **## Configuration in `config.toml` (Platform-Level Policy)**
    - REUSE the `config.toml` content from the current chapter — but verify against current `cargo pmcp deploy` schema. Locate the existing block via `grep -n '^## Deployment Configuration' pmcp-book/src/ch12-9-code-mode.md | head -1` and quote the fenced ```toml block that follows that H2 through the matching closing fence (the block contains `[server]`, `[code_mode]`, and at least one `[[code_mode.operations]]` entry — verify via `grep -n '\[\[code_mode\.operations\]\]' pmcp-book/src/ch12-9-code-mode.md`). NOT hardcoded "lines 97-122" per revision R-6. The OpenAPI / GraphQL / SQL three-way framing is correct. Keep all three examples.
    - One paragraph framing: `config.toml` declares operations for platform-level management. Administrators can enable/disable individual operations in the pmcp.run admin UI without redeploying the server. This separates "what the runtime allows" (`CodeModeConfig` in code) from "what the platform permits" (`config.toml` in deploy bundle).
    - Reproduce the categorization paragraph from the current chapter (operations sorted into read/write/delete/admin based on HTTP method, GraphQL operation type, SQL statement type, or explicit `operation_category` override). Locate via `grep -n 'automatically categorized\|operation_category' pmcp-book/src/ch12-9-code-mode.md | head -1` and quote the paragraph that contains that anchor through the end of that paragraph (one blank line). NOT hardcoded "lines 122-127" per revision R-6.

    **## Policy Evaluation (Cedar / AVP / Custom)**
    - REUSE current chapter content for the three-bullet Policy Evaluation block verbatim where it matches the current crate. Locate via `grep -n '^## Policy Evaluation' pmcp-book/src/ch12-9-code-mode.md | head -1` and quote from that H2 through the end of the section (which is the paragraph that ends with "A denied operation never receives a token." — find via `grep -n 'denied operation never receives' pmcp-book/src/ch12-9-code-mode.md | head -1`). NOT hardcoded "lines 125-133" per revision R-6. Verify by reading `crates/pmcp-code-mode/src/policy.rs` (and `avp.rs` if the `avp` feature exists per the file listing). Three subsections:
      - **Cedar:** local policy evaluation, no network. Available via the `cedar` feature flag (verify from Cargo.toml).
      - **AWS Verified Permissions:** remote evaluation. Available via the `avp` feature flag (verify).
      - **Custom:** implement the `PolicyEvaluator` trait. Briefly show the trait signature (find via `grep "trait PolicyEvaluator" crates/pmcp-code-mode/src/policy/` or similar). ~5-line `rust,ignore` snippet.
    - One paragraph on lifecycle: the policy evaluator runs after parsing and security scanning, before the approval token is generated. A denied operation never receives a token.

    **## Security Properties Reference**
    - Brief bulleted list of load-bearing security properties (the chapter's security-properties summary section). Locate the existing block via `grep -n '^## Security Properties' pmcp-book/src/ch12-9-code-mode.md | head -1` and reuse its bulleted list, then verify each bullet against the current crate source. NOT hardcoded "lines 135-141" per revision R-6. Required bullets:
      - Tokens are stateless (HMAC-verified, not stored server-side).
      - Default TTL: 5 minutes (configurable via `token_ttl_seconds`).
      - `TokenSecret` uses `secrecy::SecretBox` with zeroize-on-drop.
      - Minimum 16-byte secret enforced at `TokenSecret::new` construction (returns `Result`, no panic).
      - Code canonicalization prevents whitespace-based token reuse bypass.

    **Style invariants (enforce throughout):**
    - Same inline-excerpt + cross-link style as ch12-7-tasks.md / ch12-8-skills.md (D-07, D-08).
    - Every inline excerpt has a clear function/struct/section boundary (grep-stable).
    - All Rust snippets are `rust,ignore` — NO new doctests in this chapter (CONTEXT.md D-10: only the Skills chapter adds a doctest).
    - Cross-link format verbatim: ``Full example: [`examples/s41_code_mode_graphql.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/s41_code_mode_graphql.rs)``.
    - If a fenced code block follows a `Full example: [path](URL)` anchor but is NOT a verbatim excerpt from the cited file (adapted/synthetic snippet for pedagogy), precede the fence with the HTML comment `<!-- synthetic -->` so 81-07 Audit A skips it.
    - Replace the entire current file content. Do NOT cherry-pick — overwrite. Target length: 300-450 lines.
  </action>
  <verify>
    <automated>test -f pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -q '## The Problem' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -F -q '## Adding Code Mode with' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -F -q '#[derive(CodeMode)]' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -q '## Worked Example' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -F -q '## Configuration in `config.toml`' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -q '## Policy Evaluation' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -q '## Security Properties Reference' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; grep -q 's41_code_mode_graphql.rs' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; ! grep -q 'rust,no_run' pmcp-book/src/ch12-9-code-mode.md &amp;&amp; [ "$(wc -l &lt; pmcp-book/src/ch12-9-code-mode.md)" -ge 300 ]</automated>
  </verify>
  <done>
    `ch12-9-code-mode.md` is fully rewritten (≥300 lines) containing all six mandatory H2 sections in the specified order. The chapter leads with `#[derive(CodeMode)]` (per derive-macro-first framing). It contains zero `rust,no_run` blocks (Skills chapter is the only doctest-bearing chapter in Phase 81, per D-10). At least one explicit reference to `s41_code_mode_graphql.rs` exists. The reuse blocks (HMAC diagram, language table, config.toml examples, security properties) match current crate behavior — verified during Task 1's source-read step.
  </done>
</task>

</tasks>

<threat_model>
Documentation-only change. No new authentication, authorization, input validation, or trust-boundary surface introduced. Threats considered:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02-01 | Misleading | ch12-9-code-mode.md security framing | mitigate | The chapter explicitly states that the policy evaluator runs *before* token generation, that tokens are HMAC-bound to {code hash, user, session, server, context, risk, expiry}, and that no execution path bypasses the token check. These claims are sourced directly from `crates/pmcp-code-mode/src/lib.rs` and `examples/s41_code_mode_graphql.rs`, not from outdated chapter content. |
| T-81-02-02 | Misleading | Crate version pins in dependency snippet | mitigate | Task 1 verifies current pinned versions against `crates/pmcp-code-mode/Cargo.toml` and the workspace Cargo.toml before Task 2 writes the dependency table. If a version cannot be resolved, the chapter falls back to `version = "*"` + a footnote rather than guessing. |
| T-81-02-03 | Misleading | NoopPolicyEvaluator caveat | mitigate | The `s41_code_mode_graphql.rs` example itself includes the warning ("NoopPolicyEvaluator is used here for demonstration. In production, replace with CedarPolicyEvaluator or your custom evaluator"). The chapter rewrite must surface this warning in the "Policy Evaluation" section so readers do not deploy NoopPolicyEvaluator to production. |
</threat_model>

<verification>
- `pmcp-book/src/ch12-9-code-mode.md` is rewritten (line count ≥ 300; existing 146-line content fully replaced, not merged).
- All six mandatory H2 sections present in order.
- First substantive section after "The Problem" leads with `#[derive(CodeMode)]` — manual handler registration is not the canonical path.
- Reproduced language→feature table matches `crates/pmcp-code-mode-derive/src/lib.rs` (verified in Task 1).
- Dependency version pins match `crates/pmcp-code-mode/Cargo.toml` and `crates/pmcp-code-mode-derive/Cargo.toml`.
- Chapter contains no `rust,no_run` blocks (only the Skills chapter has a doctest in this phase).
- mdBook build (plan 81-07 verifies) succeeds.
</verification>

<success_criteria>
- Reader of `ch12-9-code-mode.md` chooses `#[derive(CodeMode)]` as the entry point.
- Reader can pick a language attribute value + corresponding feature flag from the table.
- Reader knows when to write a direct `CodeExecutor` impl vs. use a standard adapter.
- Reader sees the success and rejection paths walked from `s41_code_mode_graphql.rs` and can run that example after the chapter.
- mdBook build succeeds for `pmcp-book/`.
- ROADMAP.md Phase 81 progress increments by 1 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-02-SUMMARY.md` recording:
- Path to rewritten chapter file with new line count + delta vs. previous 146 lines.
- Catalog of inline excerpt sources keyed by grep anchor (e.g., "`grep -n '#\\[derive(CodeMode)\\]' examples/s41_code_mode_graphql.rs` → § Step 2: Derive and Configure") rather than hardcoded line numbers (revision R-6).
- Confirmation that no `rust,no_run` blocks were added.
- Confirmation that crate version pins were sourced from Cargo.toml (with the resolved versions listed).
</output>
</output>
