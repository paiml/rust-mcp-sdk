---
phase: 117-agents-tester-v1-severability
plan: 07
type: execute
wave: 2
depends_on: ["117-04"]
files_modified:
  - crates/pmcp-agent/src/invoker/factory.rs
  - crates/pmcp-agent/src/trace.rs
  - crates/pmcp-agent/tests/replay_safety.rs
  - crates/pmcp-agent/Cargo.toml
  - crates/pmcp-team-servers/Cargo.toml
  - cargo-pmcp/Cargo.toml
autonomous: true
requirements: [CLNT-03]

must_haves:
  truths:
    - "`pmcp-agent` reaches a v2 server end-to-end: `client_for` prefers v2, falls back to v1, and the four RED tests from plan 117-04 turn GREEN (D-07) — including `agent_drives_task_polling_to_terminal_on_v2`, which is CLNT-03's 'including task polling' clause and is unconditional"
    - "The era probe lives in `pmcp-agent`'s connector factory and NOWHERE ELSE — zero change to `pmcp::Client`, honouring the Phase-113 D-08 lock that the source carries as a literal 'do not restore the latter' comment (A-D08)"
    - "Fallback is decided by a TYPED reachability fact established BEFORE any error is stringified — measured: `src/shared/streamable_http.rs:1096` (connect failure) and `:1175`/`:1183` (non-2xx status) BOTH produce `Error::Transport(TransportError::Request(String))`, so the error VARIANT cannot classify them and neither can `InvokerError`"
    - "The silent-downgrade failure is impossible by CONSTRUCTION as well as by classification: era V1 is reported only when a real `initialize` SUCCEEDED, so a misclassification can cost a wasted round trip or the wrong reported error — never a false 'connected via v1'"
    - "The classification contract is defined ONCE and reused by `mcp-tester` (plan 117-11), not implemented twice as two fragile classifiers"
    - "The negotiated protocol version is recorded in `EffectTrace`, closing the `ReplayInvoker` hole where a v1-recorded trace replays as v2 — and the mismatch fails DETERMINISTICALLY, never by panic and never in silence (D-08)"
    - "Pre-117 era-less traces still deserialize, and a trace with no recorded version serializes byte-identically to a pre-117 one"
    - "The era is plumbed END TO END, not opt-in: the recording path that builds an `EffectTrace` from a live connector POPULATES the negotiated version, and the replay path DERIVES or REQUIRES the live era — an omission at either end re-opens the exact hole D-08 exists to close"
    - "The compatibility policy for a legacy (version-less) trace under an explicit v2 replay is stated in ONE sentence in the code and honoured by a test, rather than left to be inferred from the conservative fallback"
    - "The tasks coupling stays exactly one trait method, one call site and one impl — no new method naming a `tasks/*` wire API — so a Phase-114 sign-off change has a bounded blast radius (D-09)"
  artifacts:
    - path: "crates/pmcp-agent/src/invoker/factory.rs"
      provides: "Two-attempt era-pinned construction in client_for; era exposed on the connector"
      contains: "PROTOCOL_VERSION_2026_07_28"
    - path: "crates/pmcp-agent/src/trace.rs"
      provides: "Additive negotiated-version field + builder; ReplayInvoker era-mismatch guard"
      contains: "skip_serializing_if"
    - path: "crates/pmcp-agent/tests/replay_safety.rs"
      provides: "Deterministic era-mismatch case added to the existing AGNT-03 property harness"
      contains: "era"
  key_links:
    - from: "crates/pmcp-agent/src/invoker/factory.rs"
      to: "pmcp::Client::server_discover"
      via: "an era-PINNED v2 client built with with_protocol_version, then server_discover() to CONFIRM (never to decide)"
      pattern: "server_discover"
    - from: "crates/pmcp-agent/src/trace.rs EffectTrace"
      to: "pmcp::types::protocol::protocol_era"
      via: "classify the recorded version string at read time; unknown-to-V1 conservative fallback"
      pattern: "protocol_era"
---

<objective>
Turn plan 117-04's three RED tests GREEN by making `UrlConnectorClientFactory::client_for` a
two-attempt, era-pinned constructor, and record the negotiated era in `EffectTrace` so
`ReplayInvoker` can no longer replay a v1 trace as v2.

Purpose: CLNT-03. Today `client_for` (`crates/pmcp-agent/src/invoker/factory.rs:125-146`) calls
`initialize` UNCONDITIONALLY at `:141`, and v2 has no `initialize` — that one call is why the agent
cannot reach a v2 server. D-07 says prefer v2 and fall back to v1. A-D08 amends D-08: the probe lives
HERE, not in `Client`, because Phase 113 D-08 forbids an SDK auto-probe, `src/client/mod.rs:871-878`
carries that lock as a literal "do not 'restore' the latter" comment, and `server_discover()` opens
with `require_v2(...)` (`:892`) which fails LOCALLY with no round trip on a v1 client — so it can
CONFIRM a v2 selection but can never DECIDE one.

Output: a working two-attempt factory, an additive `EffectTrace` version field, a deterministic
replay guard, and a green `agent_v2_e2e`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
<!-- crates/pmcp-agent/src/invoker/factory.rs:125-146 — the function being replaced, verbatim. -->
async fn client_for(&self, endpoint: &str) -> Result<Arc<dyn ConnectorClient>, InvokerError> {
    let url = url::Url::parse(endpoint)
        .map_err(|e| InvokerError::Config(format!("invalid endpoint URL: {e}")))?;
    // T-108-05-05: only http(s) endpoints are dispatched (mirrors the
    // 108-04 scheme policy) — never a `file://`/`data:`/etc. scheme.
    match url.scheme() {
        "http" | "https" => {},
        other => return Err(InvokerError::UnsupportedScheme(other.to_string())),
    }
    let config = StreamableHttpTransportConfigBuilder::new(url).build();
    let transport = StreamableHttpTransport::new(config);
    let mut client = Client::new(transport);
    client
        .initialize(ClientCapabilities::default())      // <- :141 UNCONDITIONAL. The whole bug.
        .await
        .map_err(|e| InvokerError::Transport(e.to_string()))?;
    Ok(Arc::new(UrlConnectorClient { client }))
}

<!-- examples/s48_v2_mrtr_client.rs:96-107 — the canonical era-pinned client construction. -->
let builder = ClientBuilder::new(transport)
    .with_protocol_version(ProtocolVersion(PROTOCOL_VERSION_2026_07_28.to_string()))?;

<!-- src/client/mod.rs:871-878, :892, :707-721 — THE LOCK. Read before writing any probe. -->
/// It is EXPLICIT: pmcp never calls it implicitly, and never uses it to CHOOSE
/// an era (Phase-113 D-08 forbids exactly that auto-probe). ... do not "restore"
/// the latter.
pub async fn server_discover(&mut self) -> Result<ServerDiscoverResult> {
    self.require_v2(SERVER_DISCOVER_METHOD)?;     // fails LOCALLY, no round trip

fn require_v2(&self, method: &str) -> Result<()> {
    if self.is_v2() { return Ok(()); }
    Err(Error::InvalidState(format!("{method} requires the 2026-07-28 era — select it with ClientBuilder::with_protocol_version")))
}

<!-- crates/pmcp-agent/src/trace.rs:32-57 — the struct gaining a field, and its constructor. -->
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct EffectTrace {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub initial_state: Option<RunState>,          // <- the additive-field precedent, in this struct
    pub completions: Vec<CreateMessageResultWithTools>,
    #[serde(default)]
    pub tool_batches: Vec<Vec<ToolCallResult>>,
}
impl EffectTrace { pub fn new(completions: …, tool_batches: …) -> Self { … } }   // :48-57

<!-- crates/pmcp-agent/src/trace.rs:158-197 — ReplayInvoker and its DETERMINISTIC exhaustion path. -->
pub struct ReplayInvoker { batches: Vec<Vec<ToolCallResult>>, cursor: AtomicUsize }
impl ReplayInvoker { pub fn from_trace(trace: &EffectTrace) -> Self { Self::new(trace.tool_batches.clone()) } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-attempt era-pinned construction in `client_for`, classified by reachability</name>
  <files>crates/pmcp-agent/src/invoker/factory.rs</files>
  <read_first>
    - `crates/pmcp-agent/src/invoker/factory.rs` in full (185 lines) — `client_for` (`:125-146`,
      quoted in `<interfaces>`), the `ConnectorClient` trait (`:51-81`), `UrlConnectorClient` and its
      impl (`:148-185`, including the `wait_for_related_task` delegation at `:166-175`), and the full
      `InvokerError` enum
    - `crates/pmcp-agent/tests/agent_v2_e2e.rs` and `crates/pmcp-agent/tests/common/v2_server.rs` as
      written by plan 117-04 — these define exactly what must turn GREEN, including the accessor
      signature the summary of 117-04 names
    - `src/client/mod.rs` lines 700-730 (`require_v2`, `is_v2`), lines 860-900 (`server_discover` and
      its "do not restore" lock comment), and lines 4820-4870 (`with_protocol_version` and what it
      accepts)
    - `examples/s48_v2_mrtr_client.rs` lines 90-115 (the canonical era-pinned construction)
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q4.3 — the MEASURED
      four-row failure-signature table and the reachability rule; § Q4.2 for the sketch
    - `src/error/mod.rs` lines 100-140 (the marker-const + constructor + predicate pattern, in case a
      finer discriminator is needed — a new `Error` enum variant is semver-major and is FORBIDDEN)
  </read_first>
  <behavior>
    - Against a v2-accepting server: attempt 1 builds a `Client` pinned to
      `PROTOCOL_VERSION_2026_07_28` via `with_protocol_version` and calls `server_discover()`;
      success means era = V2 and that client is kept. No `initialize` is sent.
    - Against a v1-only server: attempt 1 fails with the server having ANSWERED; that client is
      DROPPED, a fresh default (v1) `Client` is built and `initialize(ClientCapabilities::default())`
      is called; success means era = V1. The v1 wire is byte-identical to the pre-117 agent.
    - Against an unreachable host: attempt 1 fails with the server NOT having answered; the error
      PROPAGATES. No v1 attempt is made and no connector is returned.
    - The negotiated protocol version is readable from the returned connector.
    - The existing `T-108-05-05` scheme policy still rejects non-`http(s)` schemes before any
      connection attempt.
  </behavior>
  <action>
    Rewrite `UrlConnectorClientFactory::client_for` as a two-attempt, era-pinned constructor.

    Keep the URL parse and the `T-108-05-05` scheme match EXACTLY as they are, including the comment
    — they must run before either attempt.

    Add two private helpers alongside `client_for`:
    - `try_v2(url) -> Result<Client<StreamableHttpTransport>, InvokerError>`: builds the transport,
      then `ClientBuilder::new(transport).with_protocol_version(ProtocolVersion(PROTOCOL_VERSION_2026_07_28.to_string()))?`
      and `.build()`, then calls `server_discover()`. `server_discover` takes `&mut self`, so this
      helper owns a `mut` client and moves it out on success.
    - `try_v1(url) -> Result<Client<StreamableHttpTransport>, InvokerError>`: the existing code path,
      byte-identical — default `Client::new(transport)` then
      `initialize(ClientCapabilities::default())`.

    Establish reachability as a TYPED FACT, not as a post-hoc reading of an error.

    MEASURED CONSTRAINT — read this before designing the classifier. `InvokerError` cannot carry the
    distinction, and neither can the `pmcp::Error` variant:
    - `src/shared/streamable_http.rs:1096` — a connect/send failure becomes
      `Error::Transport(TransportError::Request(e.to_string()))`.
    - `src/shared/streamable_http.rs:1175` and `:1183` — a non-2xx HTTP status ALSO becomes
      `Error::Transport(TransportError::Request("Request failed with status: NNN"))`.
    A third-party v1 server answering a plain `404` and a refused TCP connection are therefore the
    SAME variant carrying different prose. Classifying on the variant is wrong; classifying on the
    prose is string matching, which § Q4.3 forbids. `client_for` then stringifies again into
    `InvokerError::Transport(String)`, destroying what little was left.
    (Only the pmcp-server case is distinguishable: a v2-pinned client hitting a pmcp v1-only server
    takes the `is_v2()` JSON-RPC-envelope branch at `:1160-1171` and surfaces `Error::Protocol`. That
    covers pmcp's own servers and NOT the third-party case, so it cannot be the whole rule.)

    Therefore: determine reachability at the HOST layer with an explicit, unambiguous probe BEFORE
    attempt 1. Add `async fn endpoint_is_reachable(url: &Url) -> Result<(), InvokerError>` that
    resolves the host and opens a TCP connection to the resolved `host:port` (defaulting the port from
    the scheme), bounded by an explicit timeout constant. Its failure is unambiguously
    "did NOT answer"; its success means the endpoint is live and any subsequent protocol-level failure
    is "the server ANSWERED". Write the measured constraint above into its doc comment as the reason
    this exists instead of an error inspection.

    Then express the outcome as a TYPED enum local to this module — e.g.
    `enum ProbeOutcome { Answered(Box<pmcp::Error>), Unreachable(Box<pmcp::Error>) }` — constructed at
    the point of failure, before any `to_string()`. `client_for` matches on it: `Answered` ⇒ drop the
    v2 client and run `try_v1`; `Unreachable` ⇒ propagate immediately with no v1 attempt.

    Record the two known imprecisions honestly in the doc comment rather than hiding them:
    (a) a TLS handshake failure on `https` passes the TCP probe and is classified `Answered`, so a
        pointless v1 attempt is made — which then fails with the same TLS error and propagates. The
        outcome is still an error, never a false success;
    (b) a server that accepts TCP but never responds is classified `Answered`; the bounded timeout on
        attempt 1 then surfaces as an error either way.
    Both are acceptable because of the STRUCTURAL invariant, which must also be written down: era V1
    is returned ONLY when `try_v1`'s `initialize` actually SUCCEEDED. A misclassification can cost a
    wasted round trip or change WHICH error is reported; it can never produce the Pitfall-7 silent
    downgrade.

    String-matching `"Unsupported protocol version"` remains FORBIDDEN — § Q4.3 measured FOUR distinct
    rejection signatures and the message is neither stable across implementations nor the only one.

    Do NOT add a variant to `pmcp::Error` or `pmcp::TransportError` — neither is `#[non_exhaustive]`,
    so that is semver-major (116 D-03). `InvokerError` IS `#[non_exhaustive]`
    (`crates/pmcp-agent/src/invoker/factory.rs:33`) and `pmcp-agent` is experimental 0.x, so adding a
    variant THERE is permitted if the typed outcome needs to surface to callers; say in the summary
    whether one was added and why.

    Put the classification CONTRACT — the reachability rule, the two imprecisions, and the
    era-V1-only-on-success invariant — in a doc comment that plan 117-11 Task 1 is instructed to cite
    verbatim rather than re-derive. Two independently written classifiers is the failure this note
    prevents.

    Store the negotiated protocol version on `UrlConnectorClient` as an owned `String` and expose it
    through whatever accessor plan 117-04's summary specified. Extend the `ConnectorClient` trait with
    that ONE accessor if the tests require it — give it a DEFAULT implementation returning `None` so
    every existing implementor keeps compiling (the same backward-compatible-default discipline
    `list_tools` already uses at `factory.rs:76-80`).

    Do NOT add any new `ConnectorClient` method that names a `tasks/*` wire method. D-09 bounds the
    tasks coupling to exactly `ConnectorClient::wait_for_related_task`, its one caller
    (`invoker/client.rs:73-79`) and its one impl (`factory.rs:166-175`). Add a comment on that trait
    method naming it as THE seam, so a Phase-114 sign-off change has one place to look.

    Do NOT modify `crates/pmcp-agent/src/invoker/client.rs` — `ClientToolInvoker` needs no era code;
    everything routes through the era-agnostic `ConnectorClient` trait.

    Do NOT touch `src/client/mod.rs`. A-D08 is explicit and the source carries the lock.
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent --test agent_v2_e2e --features url-connector &amp;&amp; cargo build -p pmcp-agent --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - All FOUR tests in `crates/pmcp-agent/tests/agent_v2_e2e.rs` PASS, including
      `agent_drives_task_polling_to_terminal_on_v2`,
      `agent_falls_back_to_v1_when_the_server_answers_and_rejects_v2` and
      `an_unreachable_host_propagates_and_is_not_reported_as_era_v1`. The task-polling case is the
      CLNT-03 "including task polling" clause and is not optional.
    - `git diff --stat src/client/mod.rs` is empty — zero change to `pmcp::Client` (A-D08).
    - `git diff --stat crates/pmcp-agent/src/invoker/client.rs` is empty.
    - `grep -c 'Unsupported protocol version' crates/pmcp-agent/src/` returns 0 outside doc comments —
      no classification by string match.
    - No classification anywhere in `crates/pmcp-agent/src/` reads an error's text: `grep -n
      'to_string().contains\|\.contains("' crates/pmcp-agent/src/invoker/factory.rs` returns nothing
      in classification code.
    - The typed outcome exists and is constructed BEFORE stringification — `git diff` shows
      `InvokerError::Transport(e.to_string())` is no longer the sole product of a failed attempt.
    - `endpoint_is_reachable` exists, is bounded by an explicit timeout constant, and its doc comment
      quotes the measured constraint (it contains the tokens `streamable_http.rs:1096` and
      `TransportError::Request`).
    - The doc comment states the era-V1-only-on-`initialize`-success invariant and both named
      imprecisions (TLS-handshake, accept-but-never-respond).
    - `git diff --stat src/error/mod.rs` is empty — no `pmcp::Error` or `TransportError` variant was
      added.
    - A test proves the classification is not vacuous: an endpoint that accepts TCP but answers every
      request with a plain `404` and a non-JSON body (a raw `TcpListener` stub, which
      `crates/pmcp-agent/tests/http_sources_mock.rs` shows how to build) is classified `Answered` and
      triggers the v1 attempt — this is the exact third-party case the `Error::Protocol` branch does
      NOT cover.
    - `grep -c 'T-108-05-05' crates/pmcp-agent/src/invoker/factory.rs` is at least 1 (the scheme
      policy comment survived).
    - Any new `ConnectorClient` trait method has a default body
      (`cargo build -p pmcp-agent --features url-connector` and `cargo test -p pmcp-agent` both pass
      without editing any other implementor).
    - `cargo build -p pmcp-agent --target wasm32-unknown-unknown` exits 0 (the `pmcp-agent-targets`
      job at `ci.yml:374-377` IS in `gate.needs`).
    - `cargo test -p pmcp-agent` (default features) exits 0.
    - `cargo build -p pmcp-agent --all-features` exits 0 using an absolute rustup `cargo` path, so a
      dev-dependency `pmcp` `full` unification cannot mask a feature-flag gap.
  </acceptance_criteria>
  <done>
    `client_for` prefers v2 and falls back to v1 by reachability, the unreachable case propagates,
    `pmcp::Client` is untouched, and the tasks coupling is still exactly one trait method.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record the negotiated version in `EffectTrace`, additively</name>
  <files>crates/pmcp-agent/src/trace.rs, crates/pmcp-agent/Cargo.toml</files>
  <read_first>
    - `crates/pmcp-agent/src/trace.rs` in full (267 lines) — `EffectTrace` (`:32-43`, where
      `initial_state` at `:36-37` already carries the exact attribute pair the new field needs),
      `EffectTrace::new` (`:48-57`), `ReplaySource`'s deterministic exhaustion path (`:119-121`,
      `:150-155`), `ReplayInvoker` (`:158-197`), and the existing round-trip test at `:221-227`
    - `crates/pmcp-agent/tests/fixtures/golden_trace_end_turn.json` and
      `golden_trace_tool_loop.json` — these are ALREADY era-less pre-117 traces; they ARE the
      backward-compatibility test and MUST NOT be regenerated
    - `src/types/protocol/version.rs` lines 45-90 (`Era`, `protocol_era`, and the unknown-to-V1
      conservative fallback at `:63-67`); note `Era` does NOT derive `Serialize`/`Deserialize`
    - `crates/pmcp-agent/Cargo.toml` (the `version = "0.1.0"` line)
  </read_first>
  <behavior>
    - `EffectTrace` gains `negotiated_version: Option<String>` with
      `#[serde(default, skip_serializing_if = "Option::is_none")]`.
    - A trace with `negotiated_version: None` serializes to bytes IDENTICAL to a pre-117 trace of the
      same content.
    - Both existing era-less golden fixtures still deserialize without modification.
    - A `with_negotiated_version(...)` consuming builder exists; `EffectTrace::new`'s arity is
      UNCHANGED.
    - The recorded version is classified with `pmcp::types::protocol::protocol_era`, so any
      unrecognised value resolves conservatively to V1 rather than panicking.
  </behavior>
  <action>
    Add `negotiated_version: Option<String>` to `EffectTrace`, immediately after `initial_state`, with
    the same `#[serde(default, skip_serializing_if = "Option::is_none")]` attribute pair. Its doc
    comment states: this is the protocol version the trace was RECORDED against (e.g. `"2026-07-28"`);
    `None` means recorded before era tracking existed (pre-117); classify with
    `pmcp::types::protocol::protocol_era`, whose unknown-to-V1 conservative fallback makes any
    unrecognised value safe.

    Store the VERSION STRING, not an `Era`. `Era` does not derive `Serialize`/`Deserialize`
    (`src/types/protocol/version.rs:53`), and adding those derives would put a new wire spelling
    (`"V1"`/`"V2"`) onto the core's compatibility surface for no benefit. The string preserves more
    information and touches zero core API. Record this choice and its reason in the summary.

    Add `#[must_use] pub fn with_negotiated_version(self, version: impl Into<String>) -> Self` as a
    consuming builder. Do NOT widen `EffectTrace::new`'s arity — `EffectTrace` is a public
    all-`pub`-fields struct and `new` is its in-repo construction path (`trace.rs:222`).

    Extend the existing round-trip test at `trace.rs:221-227` with two cases:
    - a `None`-version trace serializes byte-identically to the same trace built pre-117 (assert the
      serialized string contains no `negotiatedVersion` key at all — `skip_serializing_if` must be
      doing its job, not merely emitting `null`);
    - both existing golden fixtures deserialize successfully and yield `negotiated_version == None`.

    Bump `crates/pmcp-agent/Cargo.toml`'s `version` from `0.1.0` to `0.2.0`. Adding a field to a
    public all-`pub`-fields struct breaks external struct-literal construction; `pmcp-agent` is
    designated experimental 0.x (CLAUDE.md publish-order item 14, "a failure here must not gate the
    core SDK release"), so a minor bump is the correct response.

    TWO WORKSPACE MANIFESTS PIN IT AND BOTH BREAK — this is MEASURED, not a possibility to check for:
    - `crates/pmcp-team-servers/Cargo.toml:22` — `pmcp-agent = { version = "0.1", path = "../pmcp-agent" }`
    - `cargo-pmcp/Cargo.toml:75` — `pmcp-agent = { version = "0.1", path = "../crates/pmcp-agent", features = ["openai-compat"] }`
    A path dependency still enforces its `version` requirement, so `^0.1` will not accept `0.2.0` and
    the WHOLE WORKSPACE fails to resolve. Update both pins to `"0.2"` in the SAME commit as the bump.
    Both files are listed in this plan's `files_modified` for that reason.

    Re-run the grep anyway (`grep -rn 'pmcp-agent' --include='Cargo.toml' .`) and record the output —
    if it now finds a THIRD pin the two above did not cover, that is a finding, not a surprise to
    absorb silently.

    `Cargo.lock` is GITIGNORED in this repository (`.gitignore:3`) and is not a tracked file, so there
    is no lockfile to add to `files_modified` and none to commit. Do not create one.

    PLUMB THE RECORDING END — a field nobody populates closes no hole. Derive the production path
    that builds an `EffectTrace` from a live run (`grep -rn 'EffectTrace::new\|EffectTrace {'
    --include='*.rs' crates/pmcp-agent/ src/ cargo-pmcp/`) and make EVERY such site that has a live
    connector in scope call `.with_negotiated_version(...)` using the era accessor Task 1 added to
    `ConnectorClient`. Record the derived site list in the summary with, for each site, either the
    call that was added or the reason the site has no connector to read from. A site left unpopulated
    without a recorded reason is the opt-in failure this paragraph exists to prevent.

    Do NOT regenerate `golden_trace_end_turn.json` or `golden_trace_tool_loop.json`. Their era-less
    shape IS the backward-compatibility evidence.
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent trace &amp;&amp; cargo test -p pmcp-agent</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-agent` exits 0 with a pass count at least as high as before this plan
      (record before and after).
    - `git diff --stat crates/pmcp-agent/tests/fixtures/` is empty — neither golden fixture changed.
    - `grep -c 'skip_serializing_if' crates/pmcp-agent/src/trace.rs` increased by exactly 1.
    - `grep -c 'pub fn new' crates/pmcp-agent/src/trace.rs` is unchanged and `EffectTrace::new`'s
      parameter list is byte-identical to before (`git diff` shows no change to its signature).
    - A test asserts the serialized form of a `None`-version trace contains NO `negotiatedVersion`
      key (not merely that it equals `null`).
    - The derived `EffectTrace` construction-site list is recorded, and every site with a live
      connector in scope now populates the negotiated version — verified by a test that records a
      trace through the live path against the plan-117-04 harness and asserts
      `negotiated_version.is_some()`.
    - `crates/pmcp-agent/Cargo.toml` reads `version = "0.2.0"`.
    - `crates/pmcp-team-servers/Cargo.toml` and `cargo-pmcp/Cargo.toml` both pin `pmcp-agent` at
      `"0.2"`, updated in the same commit as the bump.
    - `cargo metadata --no-deps` exits 0 AND `cargo build -p cargo-pmcp` and
      `cargo build -p pmcp-team-servers` both exit 0 — the workspace still resolves. A stale `^0.1`
      pin against a `0.2.0` path dependency fails resolution for the ENTIRE workspace, so this is the
      criterion that catches it.
    - The summary records the full `grep -rn 'pmcp-agent' --include='Cargo.toml' .` output and every
      pin updated; a third pin, if found, is reported as a finding.
    - `git diff --stat src/types/protocol/version.rs` is empty — no `Serialize`/`Deserialize` derive
      added to `Era`.
    - `cargo build -p pmcp-agent --target wasm32-unknown-unknown` exits 0.
  </acceptance_criteria>
  <done>
    The negotiated version is recorded additively, pre-117 traces still round-trip byte-identically,
    the core `Era` type is untouched, and the version pin situation is measured and recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `ReplayInvoker` fails deterministically on an era mismatch</name>
  <files>crates/pmcp-agent/src/trace.rs, crates/pmcp-agent/tests/replay_safety.rs</files>
  <read_first>
    - `crates/pmcp-agent/src/trace.rs` as edited by Task 2 — especially `ReplayInvoker` (`:158-197`),
      `ReplayInvoker::from_trace` (`:180-182`), and `ReplaySource`'s deterministic exhaustion path
      (`:119-121`, `:150-155`) which is the MODEL for "fail deterministically, do not panic"
    - `crates/pmcp-agent/tests/replay_safety.rs` in full (the AGNT-03 property harness) — especially
      `:135-152`, which asserts two runs over one trace produce EQUAL `DecisionTrace`s. Any mismatch
      behaviour must itself satisfy that equality
    - `src/types/protocol/version.rs` lines 60-90 (`protocol_era` and its unknown-to-V1 fallback)
  </read_first>
  <behavior>
    - `ReplayInvoker` carries the recorded era, derived from the trace's `negotiated_version` via
      `protocol_era` (with `None` classifying conservatively).
    - When the live era differs from the recorded era, `invoke_batch` returns a single
      `ToolCallResult::error` for the FIRST batch, naming both eras, and returns empty batches
      thereafter — the same deterministic shape as the existing exhaustion path.
    - Two runs over the same mismatched trace produce EQUAL results, satisfying the AGNT-03
      determinism property.
    - No panic. No silent proceed.
    - When the eras MATCH, behaviour is byte-identical to today — the existing golden tests still
      pass unchanged.
  </behavior>
  <action>
    Give `ReplayInvoker` a recorded-era field populated in `from_trace` by classifying
    `trace.negotiated_version` with `pmcp::types::protocol::protocol_era`. A `None` version
    classifies conservatively (the same unknown-to-V1 rule the core uses), so pre-117 traces keep
    working.

    Add a way to declare the LIVE era for a replay run. Prefer a `#[must_use]` consuming builder
    (`with_live_era` or equivalently `with_live_version`) over widening `from_trace`'s arity, so every
    existing caller keeps compiling. Record the chosen signature in the summary.

    THE DEFAULT IS A POLICY DECISION, NOT A CONVENIENCE — state it explicitly in the doc comment and
    honour it in a test. `ReplayInvoker::from_trace` alone leaves the live era UNSPECIFIED, and an
    unspecified live era MUST mean "no era claim was made, so no mismatch can be detected" — it must
    NOT be silently treated as matching. Make that legible: the doc says in one sentence that a replay
    constructed without `with_live_era` performs no era check at all, that this preserves every
    pre-117 caller byte-for-byte, and that any caller which cares about determinism is expected to
    declare the live era.

    Also state the LEGACY-TRACE policy in one sentence and test it: a trace with
    `negotiated_version == None` (every pre-117 fixture) classifies via `protocol_era`'s
    unknown-to-V1 conservative fallback, so replaying it under an explicitly declared V2 live era IS a
    mismatch and DOES fail. That is deliberate — a v1-recorded trace replayed as v2 is exactly the
    hole D-08 exists to close, and a legacy trace is a v1 trace. Write it down so a future contributor
    does not "fix" it into silence, and cover it with a named test.

    Implement the mismatch behaviour described in `<behavior>`. Its doc comment must state the three
    constraints and why: fail fast, fail DETERMINISTICALLY (because `tests/replay_safety.rs` asserts
    two runs over one trace produce equal `DecisionTrace`s), do NOT panic (a panic breaks the
    property harness), and do NOT silently proceed (silence is the exact hole D-08 exists to close).
    Model the shape on `ReplaySource`'s exhaustion path at `trace.rs:119-121`.

    Extend `crates/pmcp-agent/tests/replay_safety.rs` with:
    - `an_era_mismatch_fails_deterministically`: build a trace recorded at one version, replay it
      declaring the other live era, assert the first batch is a single error result naming both eras
      and subsequent batches are empty, and assert two runs produce EQUAL results.
    - `a_matching_era_replays_unchanged`: the same trace replayed at its own era behaves exactly as
      it does today.
    - A property test (`proptest`, already a `pmcp-agent` dev-dependency at `Cargo.toml`) over
      arbitrary batch counts asserting that under a mismatch, two independent replays are always
      equal.

    Do NOT change the behaviour of the existing tests in that file. If any of them now needs an
    explicit live era, that is a signal the default is wrong — fix the default, not the test.
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent --test replay_safety &amp;&amp; cargo test -p pmcp-agent</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-agent --test replay_safety` exits 0 with a pass count at least 3 higher than
      before this plan (record before and after).
    - `git diff crates/pmcp-agent/tests/replay_safety.rs` shows ONLY additions — no existing test body
      was modified.
    - The mismatch path contains no `panic!`, `unwrap()`, `expect(` or `unreachable!` — verified by
      `grep -n 'panic!\|unreachable!' ` over the changed region returning nothing.
    - The determinism assertion is present in both the example-based test and the property test.
    - A named test covers the LEGACY-TRACE policy: a `negotiated_version == None` trace replayed under
      an explicitly declared V2 live era FAILS with the deterministic mismatch shape, and the same
      trace replayed with NO declared live era behaves exactly as it does today.
    - The `with_live_era` doc comment states, in words, that an undeclared live era performs no era
      check at all — so the default is a recorded policy rather than an accident.
    - The error message produced on mismatch names BOTH the recorded and the live era (assert on the
      message content, not just on `is_err`).
    - `cargo test -p pmcp-agent` exits 0 and the two golden-trace tests
      (`golden_end_turn_completes_in_one_step` and its tool-loop sibling) still pass with the
      fixtures unmodified.
    - `cargo build -p pmcp-agent --target wasm32-unknown-unknown` exits 0.
  </acceptance_criteria>
  <done>
    A v1-recorded trace can no longer be replayed as v2 in silence; the failure is deterministic,
    non-panicking, names both eras, and satisfies the AGNT-03 equality property under proptest.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent host → unknown-era MCP server | The host chooses the protocol based on the server's answer |
| recorded trace → replayed run | A trace from one era replayed under another silently invalidates determinism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-21 | Spoofing / Tampering | `client_for` fallback | mitigate | Reachability rule: fall back ONLY when the server ANSWERED. An unreachable host propagates, proven by `an_unreachable_host_propagates_and_is_not_reported_as_era_v1`. Pitfall 7's "connected via v1 against a provably-v2 server" cannot occur |
| T-117-22 | Tampering | fallback classification | mitigate | MEASURED: `streamable_http.rs:1096` (connect failure) and `:1175`/`:1183` (non-2xx) share `TransportError::Request(String)`, so neither the variant nor the message can classify. Replaced by a typed `ProbeOutcome` built from an explicit TCP reachability probe BEFORE stringification, plus the structural invariant that era V1 is returned only on a real `initialize` success. Proven against a raw-`TcpListener` third-party-`404` stub |
| T-117-23 | Repudiation | deterministic replay (the trace module's sole guarantee) | mitigate | Era recorded in `EffectTrace` and POPULATED at every derived production construction site with a live connector; `ReplayInvoker` fails fast and deterministically on mismatch, proven by a proptest equality property, never by panic and never by silence. The undeclared-live-era and legacy-trace behaviours are stated as policy in the doc and each covered by a named test, so neither is an accident |
| T-117-24 | Elevation of Privilege | endpoint scheme dispatch | mitigate | The `T-108-05-05` scheme policy (`http`/`https` only) runs BEFORE either connection attempt and is asserted to survive the rewrite |
| T-117-25 | Denial of Service | task polling | mitigate | The hard `max_poll_duration_secs` cap in `ClientToolInvoker::dispatch` (`invoker/client.rs:74-79`) is untouched; no second poll loop is introduced, per D-09's named seam |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages added. `proptest` is already a `pmcp-agent` dev-dependency; the version bump to 0.2.0 adds no dependency |
</threat_model>

<verification>
- `cargo test -p pmcp-agent --test agent_v2_e2e --features url-connector` exits 0 with all four
  cases green, including `agent_drives_task_polling_to_terminal_on_v2`.
- `cargo test -p pmcp-agent` exits 0 with a pass count at least as high as before (recorded).
- `cargo test -p pmcp-agent --test replay_safety` exits 0 with at least 3 more tests than before.
- `cargo build -p pmcp-agent --target wasm32-unknown-unknown` exits 0.
- `cargo build -p pmcp-agent --all-features` exits 0 via an ABSOLUTE rustup `cargo` path — a
  dev-dependency `pmcp` `full` unification masks feature-flag gaps under `cargo test --all-features`,
  so the dev-dep-free build is the real check.
- `git diff --stat src/client/mod.rs src/types/protocol/version.rs src/error/mod.rs crates/pmcp-agent/src/invoker/client.rs crates/pmcp-agent/tests/fixtures/`
  is empty.
- `cargo metadata --no-deps`, `cargo build -p cargo-pmcp` and `cargo build -p pmcp-team-servers` all
  exit 0 — the `pmcp-agent` 0.2.0 bump did not break the two workspace pins.
- `make quality-gate` exits 0.
</verification>

<success_criteria>
- Plan 117-04's four RED tests are GREEN, including the unconditional task-polling case.
- The era probe exists only in `crates/pmcp-agent/src/invoker/factory.rs`; `pmcp::Client` is
  unchanged (A-D08).
- Fallback is by a TYPED reachability outcome established before stringification; no error-message
  string matching anywhere in agent source; and the classification contract is written once for plan
  117-11 to cite.
- The two workspace manifests that pin `pmcp-agent = "0.1"` are updated to `"0.2"` in the same commit,
  and the workspace still resolves.
- `EffectTrace` records the negotiated version additively; pre-117 traces round-trip byte-identically
  and their fixtures are unmodified.
- `ReplayInvoker` fails deterministically on an era mismatch, proven under proptest; the era is
  populated at every derived production recording site; and the undeclared-live-era and legacy-trace
  policies are each stated in the code and covered by a named test.
- The tasks coupling remains exactly one trait method, one caller and one impl (D-09).
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-07-SUMMARY.md` when done.
Record: the typed `ProbeOutcome` / `endpoint_is_reachable` implementation and why it needed no new
`pmcp::Error` variant (and whether an `InvokerError` variant was added); the third-party-`404` stub
test result; the chosen `ConnectorClient` era accessor signature; the derived `EffectTrace`
construction-site list with what each site now does; the `with_live_era` signature and the recorded
undeclared-live-era and legacy-trace policies; the before/after `cargo test -p pmcp-agent` pass
counts; and the full `grep -rn 'pmcp-agent' --include='Cargo.toml' .` output with both pin updates
plus the `cargo metadata` / `cargo build -p cargo-pmcp` / `cargo build -p pmcp-team-servers` results.
</output>
