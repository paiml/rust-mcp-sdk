---
phase: 117-agents-tester-v1-severability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/v1_byte_identity_after_cut.rs
autonomous: true
requirements: [SMPL-02]

must_haves:
  truths:
    - "The v1 session-lifecycle wire bytes as they exist TODAY, before any severance edit, are pinned in a golden file — captured BEFORE the cut, because capturing them after proves nothing"
    - "A future edit that changes a v1 initialize response, a `Mcp-Session-Id` header, or a `Last-Event-ID` replay by one byte FAILS a test whose message says 'this is a V1 WIRE BREAK, not a stale fixture'"
    - "The only permitted normalization is width-preserving substitution of genuinely per-run values (the session id), proven width-preserving by an explicit length assertion so a substitution cannot mask an added or removed byte"
    - "v1 GET (SSE stream) and DELETE (session teardown) behavior is pinned, so the D-03 verb split cannot silently change the v1 answer while making the v2 405 reachable"
    - "The GET fixtures use a BOUNDED SSE reader that stops after a known frame count. `common::v2::get` (`tests/common/v2.rs:835`) reads the response body to EOF, and the v1 GET endpoint is a LONG-LIVED `text/event-stream` — using it here would time out rather than return frames, and a timeout is not a golden"
    - "Header identity is asserted SEPARATELY from body identity, because `Resp` stores `Mcp-Session-Id` outside `raw` — a body-only fixture cannot speak for headers"
  artifacts:
    - path: "tests/v1_byte_identity_after_cut.rs"
      provides: "Golden v1 wire fixtures for initialize, session-header emission, Last-Event-ID replay, and GET/DELETE — body bytes AND headers — captured pre-cut, read through a bounded frame-counting SSE reader"
      min_lines: 250
      contains: "V1 WIRE BREAK"
  key_links:
    - from: "tests/v1_byte_identity_after_cut.rs"
      to: "tests/common/v2.rs"
      via: "mod common; use common::v2::{spawn_default_config, post, get, delete, header, teardown, v1_body, Resp}"
      pattern: "mod common"
    - from: "tests/v1_byte_identity_after_cut.rs"
      to: "src/server/streamable_http_server.rs session lifecycle"
      via: "live loopback HTTP round trip against a default-config (session-minting) server"
      pattern: "spawn_default_config"
---

<objective>
Pin the v1 session-lifecycle and SSE-resumability wire bytes exactly as they are today, before a
single line of `src/server/streamable_http_server.rs` moves. This file is the regression net the
whole D-03 cut is executed against.

Purpose: SMPL-02 requires the v2 path to shed session/SSE baggage WITHOUT changing v1 — the
byte-identity discipline held unbroken since Phase 112. Goldens captured after a refactor prove only
that the refactor is self-consistent. These must exist first.

Output: `tests/v1_byte_identity_after_cut.rs`, green against the unmodified tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
<!-- tests/common/v2.rs — the shared live-socket harness. Signatures the executor needs. -->
pub async fn spawn_default_config(server: Server) -> (SocketAddr, JoinHandle<()>);   // :383 — MINTS sessions
pub async fn spawn_stateless_config(server: Server) -> (SocketAddr, JoinHandle<()>); // :389 — no session_id_generator
pub async fn spawn_with(server: Server, config: StreamableHttpServerConfig) -> (SocketAddr, JoinHandle<()>); // :394
pub async fn teardown<S: Send>(handle: JoinHandle<()>, sockets: S);                  // :528 — drop -> abort -> await, ORDER is D-113-T
pub fn v1_body(method: &str, id: Value, params: Value) -> String;                    // :660
pub fn header(name: &str, value: &str) -> (String, String);                          // :703
pub struct Resp { /* :717 */ }
pub async fn post(addr: SocketAddr, extra: &[(String, String)], body: &str) -> Resp; // :801
pub async fn get(addr: SocketAddr, extra: &[(String, String)]) -> Resp;              // :835
pub async fn delete(addr: SocketAddr, extra: &[(String, String)]) -> Resp;           // :843
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the v1 initialize response and `Mcp-Session-Id` emission, captured pre-cut</name>
  <files>tests/v1_byte_identity_after_cut.rs</files>
  <read_first>
    - `tests/v1_lists_golden.rs` in full — specifically the module doc (`:1-60`, the "read this
      before you change a literal" framing and the raw-string-vs-parsed-JSON rationale), the
      `DynamicField` struct (`:97-127`), the `V1Golden` record and `assert_v1_bytes` four-step
      assertion (`:219-283`), `wire_break_message` (`:205-215`), and the spawn/shutdown/round_trip
      trio (`:452-478`)
    - `tests/common/v2.rs` lines 383-400 (`spawn_default_config` vs `spawn_stateless_config` — the
      difference is load-bearing here), `:528-532` (`teardown` and why the ORDER matters), `:660-720`
      (`v1_body`, `header`, `Resp`), `:801-850` (`post`, `get`, `delete`)
    - `tests/common/mod.rs` (7 lines — why `tests/common/` is not compiled as its own test binary)
    - `src/server/streamable_http_server.rs` lines 405-474 (the session era gate and
      `apply_session_header`, "the ONE place a `Mcp-Session-Id` response header is emitted") and
      lines 1717-1837 (`process_init_session`, `update_session_after_init`)
  </read_first>
  <behavior>
    - A v1 `initialize` POST against a `spawn_default_config` server returns a response whose raw
      bytes match a pinned literal after ONLY the `Mcp-Session-Id` value is placeholder-substituted.
    - The response carries an `Mcp-Session-Id` header; its value is pinned by SHAPE (a predicate),
      not by literal, and its presence and header name are pinned exactly.
    - A follow-up non-initialize POST carrying that session id succeeds and its raw bytes match a
      second pinned literal.
    - The width-preserving substitution assertion: substituting with `#`-padded tokens leaves the
      string length unchanged, and leaves the per-key occurrence count unchanged.
  </behavior>
  <action>
    Create `tests/v1_byte_identity_after_cut.rs`.

    Open with the same `#![cfg(all(feature = "streamable-http", feature = "http-client",
    not(target_arch = "wasm32")))]` gate used at `tests/v1_lists_golden.rs:61-71`, then `mod common;`
    and the `use common::v2::{...}` import list.

    Module doc (`//!`) must state, in the voice of `v1_lists_golden.rs`'s header: these literals were
    captured from the unmodified tree BEFORE the Phase-117 D-03 cut of
    `src/server/streamable_http_server.rs`; a diff in a literal here is a **V1 WIRE BREAK**, not a
    stale fixture; the correct response is to fix the cut, NOT to re-record the golden; and
    re-recording is exactly the failure this file exists to prevent because "the v1 suite still
    passes" is not byte-identity evidence.

    Port `DynamicField`, `substitute`, `key_occurrences`, `V1Golden`, `assert_v1_bytes` and
    `wire_break_message` from `tests/v1_lists_golden.rs` (a Rust integration test is its own crate,
    so these cannot be imported — restate them, and say so in a comment, as
    `v1_lists_golden.rs:88-96` already does about `v1_tasks_golden.rs`). Adjust
    `wire_break_message`'s text to name the Phase-117 cut as the likely cause.

    Use `spawn_default_config`, NOT `spawn_stateless_config`. `stateless()` has no
    `session_id_generator`, so `sessions_active_for(false, _)` is `false` and there is no
    `Mcp-Session-Id` to pin at all — a stateless spawn would produce a vacuously-green session
    fixture.

    Register a deterministic pinned server: exactly ONE tool and ONE prompt, for the reason
    `v1_lists_golden.rs:49-58` documents — `HashMap` iteration order is randomized per process, so a
    two-entry array is not byte-stable. This constraint applies to any list this file happens to
    touch.

    Declare a `SESSION_ID: DynamicField` whose `key` is the JSON/header key carrying the session id,
    whose `token` is a canonical placeholder, and whose `shape` predicate asserts the real value's
    form (non-empty, and whatever character class the default generator actually produces — READ the
    generator before writing the predicate; a predicate that accepts any string would let a reshaped
    value through unnoticed).

    Write the fixtures named in `<behavior>`. Every spawn is followed by `teardown` — a bare
    `handle.abort()` produces intermittent nextest `LEAK` noise (D-113-T).

    Capture the literals by running the test with a deliberately-wrong literal once, reading the
    actual bytes out of the failure message, and pasting them in. Record in the summary that the
    capture was taken against the unmodified tree, and record the `git rev-parse HEAD` at capture
    time — that commit is the anchor any later "the v1 wire is unchanged" claim is made against.
  </action>
  <verify>
    <automated>cargo test --test v1_byte_identity_after_cut --features "full"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0 with a non-zero test
      count and `0 failed`.
    - `grep -c 'V1 WIRE BREAK' tests/v1_byte_identity_after_cut.rs` is at least 1.
    - `grep -c 'spawn_default_config' tests/v1_byte_identity_after_cut.rs` is at least 1 and
      `grep -c 'spawn_stateless_config' tests/v1_byte_identity_after_cut.rs` is 0 for the session
      fixtures (a stateless spawn cannot mint a session id).
    - `grep -c 'teardown' tests/v1_byte_identity_after_cut.rs` is greater than or equal to the number
      of `spawn_` call sites.
    - The file contains a width-preserving length assertion whose message contains the phrase
      `width-preserving` (mirroring `v1_lists_golden.rs:270-274`).
    - Negative control, executed and recorded verbatim in the summary, then reverted: change one
      character inside one pinned literal, re-run — the test MUST fail and the failure message MUST
      contain `V1 WIRE BREAK`. Restore and re-run to green.
    - `git rev-parse HEAD` at capture time is recorded in the summary.
  </acceptance_criteria>
  <done>
    The v1 initialize response and `Mcp-Session-Id` emission are pinned byte-for-byte against the
    pre-cut tree, with a width-preserving normalizer proven not to mask byte changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin v1 `Last-Event-ID` replay and the v1 GET/DELETE verb answers, captured pre-cut</name>
  <files>tests/v1_byte_identity_after_cut.rs</files>
  <read_first>
    - `tests/v1_byte_identity_after_cut.rs` as written by Task 1 (current state, so the helpers are
      reused rather than duplicated)
    - `src/server/streamable_http_server.rs` lines 4318-4439 (`resolve_sse_session`,
      `replay_sse_events_from_header` — the ONLY reader of `LAST_EVENT_ID` in the server at `:4384`,
      `sse_event_for_message`, `attach_sse_response_headers`) and lines 4441-4555 (`handle_get_sse`
      and `handle_delete_session`, both of which open with `v2_verb_rejection`)
    - `src/server/streamable_http_server.rs` lines 476-564 (the resumability era gate, and the
      `:493-497` comment that hands severance to this phase by name)
    - `src/shared/http_constants.rs` (the `LAST_EVENT_ID` and `MCP_SESSION_ID` constant spellings —
      use the constants' actual header names in the fixtures, do not guess)
    - `tests/common/v2.rs` lines 835-850 (`get`, `delete`) and `:703` (`header`)
    - `.planning/REQUIREMENTS.md:992` (the standing "SSE resumability on the v2 path" out-of-scope
      ruling, which is why only the v1 side is pinned here)
  </read_first>
  <behavior>
    - A v1 GET against a session-minting server with a valid `Mcp-Session-Id` opens an SSE stream;
      the response STATUS, `Content-Type`, and the pinned SSE response headers match golden values.
    - A v1 GET replaying with a `Last-Event-ID` header against a server configured with an event
      store returns the replayed frames; the frame bytes match a pinned literal after only per-run
      values are substituted.
    - A v1 DELETE with a valid session id tears the session down and returns the pinned status/body;
      a subsequent request with that session id is rejected exactly as it is today.
    - Every stream read is bounded by a timeout so a hung stream FAILS the test rather than hanging
      it.
  </behavior>
  <action>
    Extend `tests/v1_byte_identity_after_cut.rs` with the three fixture groups named in
    `<behavior>`.

    BEFORE writing a single GET fixture, read `tests/common/v2.rs:835-850` and confirm for yourself
    what `get` does with the response body. It reads to EOF. The v1 GET endpoint (`handle_get_sse`,
    `streamable_http_server.rs:4441-4503`) returns a LONG-LIVED `text/event-stream` that does not
    end, so `get` will block until the timeout and return nothing to compare. A timeout is not a
    golden, and a fixture that times out and then asserts on an empty capture is vacuous.

    Therefore add a BOUNDED SSE reader LOCAL to this test file — do not modify `tests/common/v2.rs`,
    which serves other suites. It must:
    - issue the GET and take the response body as a stream rather than collecting it;
    - accumulate raw bytes and stop as soon as it has parsed N complete SSE frames, where N is an
      explicit named constant with a doc comment;
    - stop on the bounded timeout as a FAILURE path, never as a success path, with a message saying
      the stream produced fewer than N frames;
    - return the accumulated RAW BYTES (not parsed structs) so the byte-identity comparison is on the
      wire form, which is the whole point of this file;
    - return the response STATUS and HEADERS alongside those bytes.
    Record the reader's signature and N in the summary.

    Assert HEADERS separately from BODY. `Resp` (`tests/common/v2.rs:717`) stores `Mcp-Session-Id`
    outside `raw`, so a body fixture says nothing about headers. Pin the v1 GET response's status, its
    `Content-Type`, and each SSE hardening header `attach_sse_response_headers`
    (`streamable_http_server.rs:4426-4439`) emits — `Mcp-Session-Id`, `Cache-Control`, `Connection` —
    as an explicit name→value assertion set, with the session id placeholder-substituted by the same
    width-preserving `DynamicField` machinery Task 1 established. State in a comment that header
    identity and body identity are two distinct claims and that this file makes both.

    For the resumability fixture the spawned server needs an event store configured, so use
    `spawn_with(server, config)` (`tests/common/v2.rs:394`) with a
    `StreamableHttpServerConfig` that sets both `session_id_generator` and `event_store` — read the
    struct at `src/server/streamable_http_server.rs:174-202` and its `Default` at `:222-235` and
    construct with `..Default::default()` so the four v1-only fields this phase later gates are
    reached through the default, not through a literal that plan 117-13 would have to edit.

    Use the `LAST_EVENT_ID` / `MCP_SESSION_ID` header names from `src/shared/http_constants.rs`
    rather than string literals, so a constant rename shows up as a compile error rather than a
    silently-skipped fixture.

    Bound every SSE stream read with an explicit timeout constant in the style of
    `tests/common/v2.rs`'s `FRAME_TIMEOUT` (5 s) with a doc comment saying a hung stream must FAIL
    the test, not hang it. The timeout is the reader's FAILURE bound; the frame count N is its
    SUCCESS bound. Both are needed: a timeout alone cannot tell "the server sent nothing" from
    "the server is still streaming correctly".

    Add an ANTI-VACUITY test `the_replay_fixture_actually_replayed_something` asserting the replayed
    frame count is greater than zero, with a `FAILURE MODE: … / WHAT TO DO: …` message stating that a
    zero-frame replay would make the byte comparison pass over nothing.

    Do NOT pin any v2 GET/DELETE behavior in this file — that is the 405 rejection, which plan
    117-13's verb split owns and which already has coverage in the in-file test module. This file's
    subject is exclusively the v1 answers.
  </action>
  <verify>
    <automated>cargo test --test v1_byte_identity_after_cut --features "full"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0, reports at least 5
      tests, and reports `0 failed`.
    - The file references `LAST_EVENT_ID` and `MCP_SESSION_ID` via the `pmcp` constants
      (`grep -c 'LAST_EVENT_ID' tests/v1_byte_identity_after_cut.rs` is at least 1 and the reference
      resolves — proven by the file compiling).
    - `the_replay_fixture_actually_replayed_something` exists and its assertion message contains
      `FAILURE MODE`.
    - A timeout constant exists and is applied to every stream read
      (`grep -c 'timeout' tests/v1_byte_identity_after_cut.rs` is at least 1).
    - A BOUNDED SSE reader exists locally in this file with an explicit named frame-count constant,
      and `grep -c 'common::v2::get\|use common::v2::{[^}]*get' tests/v1_byte_identity_after_cut.rs`
      shows `get` is NOT used for any SSE-stream fixture. Its signature and N are recorded.
    - `git diff --stat tests/common/v2.rs` is empty — the shared harness was not modified.
    - The GET fixtures assert HEADERS as an explicit name→value set separate from the body-byte
      comparison, covering status, `Content-Type`, `Mcp-Session-Id`, `Cache-Control` and `Connection`.
    - Third negative control, executed, recorded, reverted: change one SSE hardening header's value in
      `attach_sse_response_headers`; the HEADER assertion (not the body assertion) MUST fail — proving
      the two claims are genuinely independent.
    - `cargo test --test v1_byte_identity_after_cut --features "full"` completes in under 60 seconds
      (a hang is a failure, not a slow pass).
    - Negative control, executed and recorded verbatim in the summary, then reverted: remove the
      `Last-Event-ID` header from the replay request and confirm the replay assertion fails.
    - `tests/v1_byte_identity_after_cut.rs` contains zero occurrences of `TODO`, `FIXME` or `XXX`.
  </acceptance_criteria>
  <done>
    v1 SSE resumability and the v1 GET/DELETE verb answers are pinned byte-for-byte against the
    pre-cut tree, with a bounded read and an anti-vacuity guard proving the replay fixture is not
    passing over an empty stream.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → HTTP transport | Untrusted `Mcp-Session-Id` and `Last-Event-ID` headers cross here |
| refactor → shipped v1 behavior | A cut that changes v1 bytes is a silent compatibility break |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-04 | Tampering | v1 wire compatibility across the D-03 cut | mitigate | Raw-byte goldens captured BEFORE the cut, with a failure message that names re-recording as the wrong remedy; anchored to a recorded `git rev-parse HEAD` |
| T-117-05 | Information Disclosure | `replay_sse_events_from_header` (`streamable_http_server.rs:4367-4399`) | mitigate | The v1 replay path is pinned here so the later cut cannot change WHICH events an attacker-supplied `Last-Event-ID` can reach; the v2 side of this threat (T-113-29/30, "must not even parse an attacker-supplied replay cursor") is owned by plan 117-13's null twin |
| T-117-06 | Denial of Service | test suite | mitigate | The v1 GET is a LONG-LIVED `text/event-stream` and `common::v2::get` reads to EOF, so a bounded LOCAL reader with an explicit frame-count constant is used instead; the timeout is its failure bound, never a success path. Every spawn is torn down with `teardown`'s drop→abort→await ORDER (D-113-T) to avoid intermittent nextest `LEAK` noise |
| T-117-07 | Repudiation | the "v1 wire is unchanged" claim for HEADERS | mitigate | `Resp` stores `Mcp-Session-Id` outside `raw`, so a body-only golden cannot speak for headers. Header identity is asserted as a separate explicit name→value set, and a third negative control mutating a header proves the two claims are independent |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages added; this plan creates one test file using existing dev-dependencies |
</threat_model>

<verification>
- `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0 with at least 5 tests and
  `0 failed`, in under 60 seconds.
- All three negative controls (a mutated golden literal; a removed `Last-Event-ID` header; a mutated
  SSE hardening header) were EXECUTED, produced the expected failure, and were reverted. Their output
  is recorded in the summary.
- `make quality-gate` exits 0.
- The capture anchor commit (`git rev-parse HEAD`) is recorded in the summary.
</verification>

<success_criteria>
- `tests/v1_byte_identity_after_cut.rs` exists, is green against the unmodified tree, and covers:
  the v1 initialize response, `Mcp-Session-Id` emission, a session-carrying follow-up request,
  `Last-Event-ID` replay, and the v1 GET and DELETE answers.
- The only normalization is width-preserving substitution of per-run values, proven width-preserving
  by an explicit length assertion and an occurrence-count assertion.
- A wire change fails with a message that says re-recording the golden is the WRONG remedy.
- An anti-vacuity guard proves the replay fixture is comparing a non-empty stream.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-02-SUMMARY.md` when done.
Record: the capture anchor commit; the bounded SSE reader's signature and its frame-count constant;
all three negative-control failure messages verbatim; and the exact list of fixtures pinned, stating
for each whether it pins BODY bytes, HEADERS, or both (so plans 117-09/12/13/14 know precisely what
they must keep green).
</output>
