---
phase: 117-agents-tester-v1-severability
plan: 12
type: execute
wave: 4
depends_on: ["117-09"]
files_modified:
  - src/server/streamable_http_server.rs
  - src/server/streamable_http_server/v1_session.rs
  - src/server/streamable_http_server/v1_session_off.rs
autonomous: true
requirements: [SMPL-02]

must_haves:
  truths:
    - "The v1 session-lifecycle functions and the SSE-replay functions live in the `v1-compat` module, with constant-answer twins on the v2 build — so the v2 path provably does not compile them (SMPL-02, D-03, D-10)"
    - "Every moved function's signature survives intact, including `session_id: Option<String>` threading through ~10 pipeline functions: the parameter STAYS, it is just always `None` on the v2 build. No pipeline surgery"
    - "The v2 build has NO reader of `Last-Event-ID` at all — the null twin returns BEFORE touching the header, preserving the security ordering the real path documents at `:4379-4383` ('an era that suppresses resumability must not even parse an attacker-supplied replay cursor')"
    - "The v1 wire stays byte-identical, proven by plan 117-02's pre-cut goldens, and the in-file spy tests proving v2 writes zero event-store traffic and replays nothing still pass"
  artifacts:
    - path: "src/server/streamable_http_server/v1_session.rs"
      provides: "Session lifecycle (7 fns), store_response_event, and the 4 SSE-replay fns"
      contains: "replay_sse_events_from_header"
    - path: "src/server/streamable_http_server/v1_session_off.rs"
      provides: "Constant-answer twins with NO header read, NO session map, NO event store"
      contains: "resolve_session_for_request"
    - path: "src/server/streamable_http_server.rs"
      provides: "Shared POST pipeline calling v1:: unconditionally, still zero #[cfg] at call sites"
      contains: "v1::"
  key_links:
    - from: "src/server/streamable_http_server.rs handle_post_fast_path_inner / handle_post_with_middleware_inner"
      to: "v1::process_init_session / v1::resolve_session_for_request / v1::store_response_event"
      via: "unqualified v1:: calls in the shared pipeline — ZERO #[cfg] at the call site"
      pattern: "v1::"
    - from: "src/server/streamable_http_server/v1_session_off.rs"
      to: "the Last-Event-ID security ordering"
      via: "the twin returns before any header access, so no attacker-supplied replay cursor is parsed on v2"
      pattern: "Last-Event-ID"
---

<objective>
Move the v1 session-lifecycle bodies and the SSE-replay bodies into the paired module, leaving the
shared POST pipeline calling `v1::` unconditionally.

Purpose: this is the bulk of SMPL-02's measured ≈762 lines of v1-only production surface. The
research's risk verdict names threading as the MEDIUM risk and its mitigation precisely: null twins
keep every signature identical, so `session_id: Option<String>` continues to flow through the ~10
pipeline functions — it is just always `None`. There is no pipeline surgery. The chokepoints these
bodies call already moved in plan 117-09, so they are being written against a compile-time contract
that exists.

Output: seven session-lifecycle functions, `store_response_event`, and four SSE-replay functions in
the pair, with twins that read no header and hold no state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
Functions moving in TASK 1 (session lifecycle), with line ranges in the pre-cut file:
`process_init_session` (`:1717-1764`, 48 lines) · `validate_non_init_session` (`:1766-1808`, 43) ·
`extract_negotiated_version` (`:1810-1822`, 13, reads `InitializeResult`) ·
`update_session_after_init` (`:1824-1837`, 14) ·
`validate_protocol_version_matches_session` (`:1960-2000`, 41, early-returns `Ok(())` when
`!sessions_active`) · `is_initialize_request` (`:2190-2200`, 11) ·
`resolve_session_for_request` (`:2202-2220`, 19).

Functions moving in TASK 2 (SSE replay + event store):
`store_response_event` (`:2465-2489`, 25, gated on `resumability_store`) ·
`resolve_sse_session` (`:4318-4365`, 48) ·
`replay_sse_events_from_header` (`:4367-4399`, 33 — the ONLY reader of `LAST_EVENT_ID` in the server,
at `:4384`) · `sse_event_for_message` (`:4401-4424`, 24) ·
`attach_sse_response_headers` (`:4426-4439`, 14).
Also the transport-local `EventStore` trait (`:38-56`, DISTINCT from `src/shared/event_store.rs`'s
6-method trait of the same name), `InMemoryEventStore` + its impl (`:65-132`), the `EventList` /
`EventsMap` aliases (`:58-61`), and the `SessionCallback` alias (`:133-134`).

MIXED functions that must NOT be moved wholesale — they are plan 117-13's subject:
`extract_session_and_protocol_headers` (`:2174-2188`, also reads `MCP_PROTOCOL_VERSION` which v2
needs) · `compute_outbound_protocol_version` (`:2285-2312`, only its `state.sessions.read()` branch at
`:2303-2310` is v1-only) · `handle_get_sse` (`:4441-4503`) · `handle_delete_session` (`:4505-4555`).

Genuinely v2-only surface that must NOT move: the `v2_*` helpers (`:648-1414`), `run_v2_header_gate`
(`:1348`), `HttpIngress` classification (`:1416-1542`), `assemble_discover_response_*` (`:2669`,
`:3761`), `assemble_tasks_update_*` (`:2787`, `:2840`), the whole `subscriptions/listen` block
(`:2917-3443`), `dispatch_request_or_retire` (`:2951`).

In-file tests that must keep passing: `spy_records_zero_event_store_traffic_for_a_v2_exchange`
(`:5972`), `spy_records_zero_replay_for_a_v2_get` (`:6015`),
`v2_response_is_never_routed_into_a_session_sse_stream` (`:6060`), and the spy harness at `:5946`,
`:5997`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the seven v1 session-lifecycle functions into the pair</name>
  <files>src/server/streamable_http_server.rs, src/server/streamable_http_server/v1_session.rs, src/server/streamable_http_server/v1_session_off.rs</files>
  <read_first>
    - `src/server/streamable_http_server.rs` lines 1710-1845 (`process_init_session`,
      `validate_non_init_session`, `extract_negotiated_version`, `update_session_after_init` — read
      all four bodies in full), lines 1955-2005
      (`validate_protocol_version_matches_session`, noting its early return when `!sessions_active`),
      and lines 2185-2225 (`is_initialize_request`, `resolve_session_for_request`)
    - Every CALL SITE of those seven. Derive it, do not assume:
      `grep -n 'process_init_session\|validate_non_init_session\|extract_negotiated_version\|update_session_after_init\|validate_protocol_version_matches_session\|is_initialize_request\|resolve_session_for_request' src/server/streamable_http_server.rs`
    - `src/server/streamable_http_server.rs` lines 3560-3600 and 4230-4270 —
      `handle_post_fast_path_inner` (`:3567`) and `handle_post_with_middleware_inner` (`:4237`), the
      shared pipeline that must stay `#[cfg]`-free
    - `src/server/streamable_http_server/v1_session.rs` and `v1_session_off.rs` as edited by plan
      117-09 (the chokepoints these bodies call are already there)
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q2.3 (the v1-only
      function table with line ranges) and § Q2.6 (the threading risk and its mitigation)
  </read_first>
  <action>
    Move all seven session-lifecycle functions into `v1_session.rs`, bodies unchanged, and write
    constant-answer twins in `v1_session_off.rs`.

    Twin answers, matching each real signature EXACTLY: `process_init_session` returns the same shape
    the real one returns for the no-sessions case (read its actual return type — the research sketches
    `Ok((None, false))` but the real signature is authoritative); `validate_non_init_session` returns
    `Ok(())`; `extract_negotiated_version` returns `None`; `update_session_after_init` is a no-op;
    `validate_protocol_version_matches_session` returns `Ok(())` — matching the real function's own
    early return when `!sessions_active`, which is the SAME answer, so this twin is not a behaviour
    change but a compile-time realisation of one that already holds; `is_initialize_request` returns
    `false` (v2 has no `initialize`); `resolve_session_for_request` returns `Ok(None)`.

    Make each twin a `const fn` where the signature allows.

    Keep every parameter, including the ones the twins ignore. `session_id: Option<String>` continues
    to thread through the pipeline; it is just always `None` on the v2 build. Write that reasoning as
    a comment at the top of the twin block: keeping the parameter avoids pipeline surgery and avoids
    tempting a call site into a second era decision.

    Update every call site found by the grep to `v1::…`. ZERO `#[cfg]` attributes may be added in
    `streamable_http_server.rs`.

    `SessionCallback` (`:133-134`) moves too, since the session-lifecycle bodies name it.

    Do NOT move `extract_session_and_protocol_headers` (`:2174-2188`) — it also reads
    `MCP_PROTOCOL_VERSION`, which v2 needs. Do NOT move `compute_outbound_protocol_version`
    (`:2285-2312`) wholesale — only its `state.sessions.read()` branch at `:2303-2310` is v1-only.
    Both are plan 117-13's subject; leave them exactly as they are and note in the summary that they
    were deliberately deferred.
  </action>
  <verify>
    <automated>cargo build -p pmcp --features full &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 &amp;&amp; cargo test --lib --features "full" &amp;&amp; cargo test --test v1_byte_identity_after_cut --features "full"</automated>
  </verify>
  <acceptance_criteria>
    - All seven function names appear in both `v1_session.rs` and `v1_session_off.rs`.
    - Each twin's parameter list is textually identical to its real counterpart's; the seven signature
      pairs are pasted side by side in the summary.
    - `grep -c '#\[cfg(feature = "v1-compat")\]' src/server/streamable_http_server.rs` is 0.
    - `cargo build -p pmcp --features full` exits 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with zero `warning:` lines.
    - `cargo test --lib --features "full"` pass count equals the pre-plan count EXACTLY (recorded
      before and after).
    - `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0 — including the
      initialize-response and session-header goldens, which are exactly what these seven functions
      produce.
    - `cargo test --test v1_severability_tripwire` exits 0 (the null-twin token check still passes).
    - `git diff src/server/streamable_http_server.rs` shows no change to
      `extract_session_and_protocol_headers` or `compute_outbound_protocol_version`.
    - `pmat analyze complexity --format json --max-cognitive 25` reports no NEW violation in the three
      touched files (before/after lists recorded).
  </acceptance_criteria>
  <done>
    The seven session-lifecycle functions live behind `v1-compat` with signature-identical constant
    twins; the pipeline is unchanged and `#[cfg]`-free; and the initialize/session-header goldens are
    still byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the SSE-replay path and the transport-local event store, with a twin that reads no header</name>
  <files>src/server/streamable_http_server.rs, src/server/streamable_http_server/v1_session.rs, src/server/streamable_http_server/v1_session_off.rs</files>
  <read_first>
    - `src/server/streamable_http_server.rs` lines 38-134 (the transport-local `EventStore` trait at
      `:38-56` — note it has THREE methods and is DISTINCT from `src/shared/event_store.rs`'s
      six-method trait of the same name; `EventList`/`EventsMap` at `:58-61`; `InMemoryEventStore` and
      its impl at `:65-132`)
    - `src/server/streamable_http_server.rs` lines 2460-2495 (`store_response_event`, gated on
      `resumability_store`) and lines 4315-4440 (`resolve_sse_session`,
      `replay_sse_events_from_header`, `sse_event_for_message`, `attach_sse_response_headers`) —
      read all five bodies in full
    - `src/server/streamable_http_server.rs` lines 4375-4390 SPECIFICALLY — the security ordering:
      `replay_sse_events_from_header` returns BEFORE reading the header when the store is `None`,
      documented as "an era that suppresses resumability must not even parse an attacker-supplied
      replay cursor" (threats T-113-29 / T-113-30). The null twin MUST preserve this by having no
      header access at all
    - `src/server/streamable_http_server.rs` lines 5940-6080 — the spy harness and the three tests
      that must keep passing: `spy_records_zero_event_store_traffic_for_a_v2_exchange` (`:5972`),
      `spy_records_zero_replay_for_a_v2_get` (`:6015`),
      `v2_response_is_never_routed_into_a_session_sse_stream` (`:6060`)
    - `src/server/streamable_http_server.rs` lines 585-650 — the security audit table at `:590-604`
      and `envelope_for_live_request` at `:626-644`; the audit table must be UPDATED if any site it
      lists moves
    - `src/shared/http_constants.rs` (the `LAST_EVENT_ID` constant — its per-const gating is plan
      117-13's job, not this one)
  </read_first>
  <action>
    Move `store_response_event`, `resolve_sse_session`, `replay_sse_events_from_header`,
    `sse_event_for_message` and `attach_sse_response_headers` into `v1_session.rs`, plus the
    transport-local `EventStore` trait, `InMemoryEventStore` and its impl, and the `EventList` /
    `EventsMap` aliases. Add a doc line on the moved trait stating it is DISTINCT from
    `src/shared/event_store.rs`'s trait of the same name — confusing the two is the obvious mistake
    and the research flagged it explicitly.

    `InMemoryEventStore` is publicly re-exported. Check where, with
    `grep -rn 'InMemoryEventStore' src/ --include='*.rs'`, and keep the `full` build's public API
    unchanged: the type must still be reachable at its current public path when `v1-compat` is on.
    Record the re-export path and how it was preserved.

    Write the five twins in `v1_session_off.rs`:
    - `store_response_event` — a no-op.
    - `resolve_sse_session` — the not-found / not-applicable answer matching the real return type.
    - `replay_sse_events_from_header` — returns the empty answer WITHOUT any header access. Give it a
      doc comment stating the security reason verbatim in substance: an era that suppresses
      resumability must not even PARSE an attacker-supplied replay cursor, so this twin returns before
      touching the header, preserving the ordering the real function documents at `:4379-4383`
      (threats T-113-29 / T-113-30). This is the single most important twin in the phase.
    - `sse_event_for_message` and `attach_sse_response_headers` — the constant answers matching their
      return types.

    The null twin must NOT name `LAST_EVENT_ID`, must NOT take a header map it then reads, and must
    NOT contain a `HashMap` or `RwLock`. Plan 117-06's source-content tripwire enforces this; run it.

    Check the security audit table at `:590-604` against the moved set and UPDATE it if it lists any
    moved site. Record whether it needed updating.

    Do NOT move `handle_get_sse` (`:4441-4503`) or `handle_delete_session` (`:4505-4555`) — both are
    MIXED (each begins with `v2_verb_rejection`) and are plan 117-13's verb-split subject. Do NOT gate
    the `LAST_EVENT_ID` constant in `src/shared/http_constants.rs`; both its readers (server at
    `:4384`, client at `src/shared/streamable_http.rs:639`) must be gated together in ONE edit, which
    is plan 117-13's job.
  </action>
  <verify>
    <automated>cargo build -p pmcp --features full &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 &amp;&amp; cargo test --lib --features "full" &amp;&amp; cargo test --test v1_byte_identity_after_cut --features "full" &amp;&amp; cargo test --test v1_severability_tripwire</automated>
  </verify>
  <acceptance_criteria>
    - All five function names plus `EventStore`, `InMemoryEventStore`, `EventList` and `EventsMap`
      appear in `v1_session.rs`; the five function names appear in `v1_session_off.rs`.
    - `grep -c 'LAST_EVENT_ID\|Last-Event-ID' src/server/streamable_http_server/v1_session_off.rs`
      is 0 outside doc comments, and the source-content tripwire
      (`cargo test --test v1_severability_tripwire`) passes.
    - The `replay_sse_events_from_header` twin's body contains no parameter dereference that reads a
      header — verified by reading it and pasting the whole twin (it should be a few lines) into the
      summary.
    - `cargo build -p pmcp --features full` exits 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with zero `warning:` lines.
    - `cargo test --lib --features "full"` pass count equals the pre-plan count EXACTLY, and the three
      named spy tests (`spy_records_zero_event_store_traffic_for_a_v2_exchange`,
      `spy_records_zero_replay_for_a_v2_get`,
      `v2_response_is_never_routed_into_a_session_sse_stream`) all pass.
    - `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0 — including the
      `Last-Event-ID` replay golden.
    - `InMemoryEventStore` is still reachable at its pre-plan public path under `--features full` —
      proven by a doctest or a compile-only test that names the full path, and recorded.
    - `make doc-check` exits 0.
    - The summary states whether the `:590-604` security audit table needed updating and what changed.
  </acceptance_criteria>
  <done>
    The SSE-replay path and the transport-local event store live behind `v1-compat`; the v2 build has
    no `Last-Event-ID` reader at all, preserving the T-113-29/30 ordering by construction; the spy
    tests and the replay golden are green; and the public path of `InMemoryEventStore` is unchanged on
    `full`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → HTTP transport | `Mcp-Session-Id` and `Last-Event-ID` are attacker-controlled inputs |
| refactor → security ordering | A twin that reads a header before returning re-opens a closed threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-29/30 (carried) | Tampering / Information Disclosure | `replay_sse_events_from_header` | mitigate | The real function returns before reading the header when the store is `None` (`:4379-4383`). The `full-v2` twin has NO header access at all, making the property structural rather than conditional. Enforced by reading the twin in review AND by the source-content tripwire asserting no `Last-Event-ID` token |
| T-113-07 (carried) | Information Disclosure | `build_response`'s `sse_streams` send | mitigate | `v2_response_is_never_routed_into_a_session_sse_stream` (`:6060`) must still pass — asserted by the EXACT in-file pass-count match |
| T-117-43 | Tampering | the security audit table at `:590-604` | mitigate | The table is explicitly re-checked against the moved set and updated if any listed site moved; the outcome is recorded either way, so an orphaned audit table is not left behind |
| T-117-44 | Denial of Service (build) | `InMemoryEventStore` public path | mitigate | Its public reachability under `full` is asserted by a path-naming test, so the move does not silently change the shipped API on the default build |
| T-117-45 | Repudiation | the "v2 writes zero event-store traffic" claim | mitigate | Proven by the existing spy tests, which the EXACT pass-count criterion prevents from being silently dropped during the move |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages added; pure in-crate move |
</threat_model>

<verification>
- `cargo build -p pmcp --features full` exits 0.
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0 with
  zero warnings — reported SEPARATELY from `make quality-gate`.
- `cargo test --lib --features "full"` pass count is EXACTLY the pre-plan count (recorded), with the
  three named spy tests green.
- `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0.
- `cargo test --test v1_severability_tripwire` exits 0.
- `make doc-check` exits 0.
- `make quality-gate` exits 0.
- PMAT complexity before/after for the three touched files, no new violation.
</verification>

<success_criteria>
- Twelve v1-only functions plus the transport-local event-store types live behind `v1-compat`.
- Every twin signature is textually identical to its real counterpart; `session_id: Option<String>`
  still threads the pipeline and no pipeline surgery occurred.
- The `full-v2` build has no `Last-Event-ID` reader, and the twin's no-header-access property is
  visible by inspection and enforced by the tripwire.
- The in-file spy tests, the v1 goldens and the severability tripwire are all green.
- `InMemoryEventStore`'s public path on `full` is unchanged.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-12-SUMMARY.md` when done.
Record: the twelve signature pairs side by side; the full body of the
`replay_sse_events_from_header` twin; before/after `cargo test --lib` pass counts; whether the
`:590-604` audit table needed updating; how `InMemoryEventStore`'s public path was preserved; and
before/after PMAT complexity lists.
</output>
