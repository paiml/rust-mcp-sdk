---
phase: 117-agents-tester-v1-severability
plan: 10
type: execute
wave: 3
depends_on: ["117-07"]
files_modified:
  - examples/s53_v2_agent_client.rs
  - Cargo.toml
autonomous: true
requirements: [CLNT-03]

must_haves:
  truths:
    - "The CLAUDE.md ALWAYS requirement for a runnable example is discharged for CLNT-03 by an example that actually runs and exits non-zero when a demonstration misbehaves — not by a snippet"
    - "The example is placed where `make test-examples` will actually build it: `make test-examples` globs root `examples/*.rs` ONLY, so an example under `crates/pmcp-agent/examples/` would never be built by `make test-all`"
    - "The example demonstrates BOTH directions D-07 names — the v2 happy path and the v1 fallback — because a v2-only demo hides exactly the failure mode CONTEXT.md warns about"
    - "The example number is chosen against the ACTUAL occupied set, not against a stale assumption: `s49` is already taken twice and `s50`/`s51`/`s52` exist"
    - "The dev-dependency is added in the SAME task that first needs it, so no acceptance criterion in this plan is unsatisfiable at the moment it is checked"
    - "`Cargo.lock` is GITIGNORED (`.gitignore:3`) and untracked, so there is no lockfile to declare in `files_modified` — confirmed with `git ls-files --error-unmatch`, not assumed"
  artifacts:
    - path: "examples/s53_v2_agent_client.rs"
      provides: "Runnable agent-against-v2-server demonstration, paired with an in-repo v2 server example"
      min_lines: 150
      contains: "cargo run --example s53_v2_agent_client"
    - path: "Cargo.toml"
      provides: "pmcp-agent root dev-dependency with url-connector (Task 1), plus the [[example]] declaration (Task 2)"
      contains: "s53_v2_agent_client"
  key_links:
    - from: "examples/s53_v2_agent_client.rs"
      to: "pmcp_agent::invoker::UrlConnectorClientFactory"
      via: "root [dev-dependencies] pmcp-agent path dep with features = [\"url-connector\"]"
      pattern: "pmcp-agent"
    - from: "Cargo.toml [[example]] s53_v2_agent_client"
      to: "make test-examples"
      via: "Makefile:254-266 globs examples/*.rs and builds each with --all-features then --features full"
      pattern: "required-features"
---

<objective>
Ship the CLAUDE.md ALWAYS runnable example for CLNT-03: an agent connecting to a v2 server, and the
same agent falling back to v1 — a one-shot script that exits 0 only when every demonstration behaved
as documented.

Purpose: CLAUDE.md requires fuzz + property + unit + a runnable `cargo run --example` for every new
feature, with no exceptions. `examples/s47_v2_stateless_mrtr.rs` (server) and
`examples/s48_v2_mrtr_client.rs` (client) are the Phase-114 precedent and the exact house shape.
Placement is a real decision, not a formality: `make test-examples` (`Makefile:254-266`) globs root
`examples/*.rs` only, so an example under `crates/pmcp-agent/examples/` would be invisible to
`make test-all`.

Output: `examples/s53_v2_agent_client.rs` and its `Cargo.toml` wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
Root `Cargo.toml` `[dev-dependencies]` already path-deps three workspace members (`:198-201`):
`pmcp-macros`, `pmcp-code-mode`, `pmcp-code-mode-derive` — the last two for example `s41`. A
dev-dependency cycle (root pmcp dev-deps a member that depends on pmcp) is legal in cargo and this
repo already relies on it. A fourth entry `pmcp-agent = { path = "crates/pmcp-agent", features =
["url-connector"] }` is the same shape.

The `[[example]]` block shape (root `Cargo.toml`, e.g. `:643-645`):
`name`, `path`, `required-features`. Note `s50_v2_tasks_server.rs` and `s51_v2_tasks_agent.rs` exist
on disk with NO explicit block (autoexamples discovery); `s52_v2_caching_hints` HAS one.

OCCUPIED example numbers on disk: `s49_sampling_host.rs`, `s49_v2_subscriptions_client.rs`,
`s50_v2_tasks_server.rs`, `s51_v2_tasks_agent.rs`, `s52_v2_caching_hints.rs`. Next free: `s53`.
Cargo example NAMES must be unique; the `sNN` sequence is already not a bijection and
`Cargo.toml:640-641` carries a note recording that.

`Makefile:254-266` (`test-examples`) iterates `ls examples/*.rs`, building each with
`--all-features`, falling back to `--features "full"`, and treating a double failure as a SKIP with a
yellow warning — so a broken example is skipped, not failed. That means "it builds" must be an
explicit acceptance criterion here, not an inference from a green `make test-examples`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dev-dependency, then write the runnable v2 agent-client example</name>
  <files>Cargo.toml, examples/s53_v2_agent_client.rs</files>
  <read_first>
    - `examples/s48_v2_mrtr_client.rs` in full (236 lines) — the house shape: the header contract at
      `:1-41` (run command, paired-process instruction, numbered "What this demonstrates" list, and
      the explicit exit-code contract "it exits 0 when every demonstration behaved as documented, and
      non-zero otherwise"), the era-pinned client construction at `:96-107`, and the numbered-demos
      main with a banner at `:110-131`
    - `examples/s47_v2_stateless_mrtr.rs` in full (296 lines) — the paired SERVER, including how it
      opts into the 2026-07-28 accept-list and which address it binds when given none
    - `crates/pmcp-agent/src/invoker/factory.rs` as edited by plan 117-07 — the public surface the
      example drives (`UrlConnectorClientFactory`, `ConnectorClientFactory::client_for`, the era
      accessor added there) and the module path it is exported under
    - `crates/pmcp-agent/src/invoker/client.rs` (141 lines) — `ClientToolInvoker::dispatch` and its
      `max_poll_duration_secs`-capped `wait_for_related_task`, so the example can show task polling
      without writing a second poll loop
    - `crates/pmcp-agent/tests/agent_v2_e2e.rs` and the 117-07 summary — the exact API shape that is
      known to work
    - `examples/s51_v2_tasks_agent.rs` — the nearest existing agent-flavoured v2 example, for tone
  </read_first>
  <action>
    FIRST add the dev-dependency, so this task's own build check can actually pass. Append to root
    `Cargo.toml` `[dev-dependencies]`:
    `pmcp-agent = { path = "crates/pmcp-agent", features = ["url-connector"] }` with an inline comment
    naming the example it serves, matching the style of the existing `pmcp-code-mode` entry's
    `# For code mode example (s41)` comment (`Cargo.toml:198-201`). Match whatever shape the existing
    three path dev-deps use for the `version` key.

    This ordering is deliberate. An earlier draft created the example in Task 1 and the manifest
    wiring in Task 2, which made Task 1's `cargo build --example` acceptance criterion unsatisfiable
    at the moment it was checked — an acceptance criterion that cannot pass is not an acceptance
    criterion. The `[[example]]` block and the RUN/severance proofs stay in Task 2, because those
    genuinely depend on the finished example.

    THEN create `examples/s53_v2_agent_client.rs`. Number `s53` is chosen because `s49` is occupied twice
    and `s50`, `s51`, `s52` all exist; record that derivation in a comment near the top the way
    `Cargo.toml:640-641` already records the `s49` collision.

    Header doc block, following `s48_v2_mrtr_client.rs:1-41` exactly:
    - one-line purpose: a `pmcp-agent` connector reaching a 2026-07-28 server, and falling back to
      2025-11-25 when the server does not serve v2;
    - the paired-server start command, naming whichever in-repo v2 server example it pairs with
      (prefer `s47_v2_stateless_mrtr`; if it does not expose a tool this example can call, say so and
      pair with `s50_v2_tasks_server` instead, and record the choice);
    - the literal run command `cargo run --example s53_v2_agent_client --features full`;
    - the `argv[1]` address convention with a default matching the paired server's default bind;
    - a numbered "What this demonstrates" list;
    - the explicit exit-code contract: exits 0 when every demonstration behaved as documented,
      non-zero otherwise.

    `main` follows the `s48:110-131` shape: parse the address, print a banner, call numbered demo
    functions in sequence, print a closing "All N demonstrations behaved as documented." line.

    Demonstrations, at minimum:
    1. `demo_v2_connection` — build a `UrlConnectorClientFactory`, `client_for` the v2 server,
       print the negotiated protocol version, list tools, call one, print the result.
    2. `demo_task_polling` — call a tool whose result carries a related task and drive it to terminal
       through `ConnectorClient::wait_for_related_task`. If the paired server exposes no such tool,
       say so in the doc and drop this demo rather than faking it — a demo that pretends is worse
       than one that is absent. Record the decision.
       Dropping it here does NOT weaken CLNT-03: the "including task polling" clause is discharged by
       `agent_drives_task_polling_to_terminal_on_v2` in `crates/pmcp-agent/tests/agent_v2_e2e.rs`
       (plan 117-04, turned GREEN by plan 117-07), which asserts task-id discovery, a server-observed
       poll count of at least 1, and a terminal state, with NO conditional. If this demo is dropped,
       the example's header MUST point the reader at that test by name, so the requirement's proof is
       discoverable from the example rather than merely absent.
    3. `demo_v1_fallback` — point the same factory at a v1-only endpoint and show it connecting
       successfully via v1, printing the negotiated version. This is the D-07 direction that hides
       bugs; the example must not be v2-only. Spawn a short-lived in-process v1 server for this demo
       rather than requiring the user to start a third process.
    4. `demo_unreachable_propagates` — point the factory at a closed loopback port and show the error
       PROPAGATING rather than silently reporting v1.

    Each demo returns `Result` and the example returns non-zero if any fails. Do NOT use `unwrap()`
    or `expect()` on anything that depends on the paired server being up — print an actionable
    message naming the start command instead.

    Do NOT write a second task-poll loop. Route task interaction through
    `ConnectorClient::wait_for_related_task` — the classifier and the loop live in the SDK primitive
    (`invoker/client.rs:9-11`), and a second loop is a second timeout policy (D-09's named seam).
  </action>
  <verify>
    <automated>cargo build --example s53_v2_agent_client --features "full"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/s53_v2_agent_client.rs` exists, is at least 150 lines, and its header contains the
      literal string `cargo run --example s53_v2_agent_client`.
    - The header states the exit-code contract in words ("exits 0 when every demonstration behaved as
      documented").
    - Four demo functions exist (or three, with a recorded justification for dropping the task-polling
      demo), and `demo_v1_fallback` and `demo_unreachable_propagates` are both present — a v2-only
      example fails this criterion.
    - If `demo_task_polling` was dropped, the example header names
      `agent_drives_task_polling_to_terminal_on_v2` as where the CLNT-03 task-polling proof lives —
      verified by `grep -c 'agent_drives_task_polling_to_terminal_on_v2' examples/s53_v2_agent_client.rs`
      being 1.
    - `grep -c '\.unwrap()\|\.expect(' examples/s53_v2_agent_client.rs` counted on lines that touch a
      network result is 0.
    - `grep -c 'wait_for_related_task' examples/s53_v2_agent_client.rs` is 1 or 0 — never more than
      one call site, and no hand-written poll loop
      (`grep -c 'loop {' examples/s53_v2_agent_client.rs` is 0).
    - `cargo build --example s53_v2_agent_client --features "full"` exits 0. This is now genuinely
      satisfiable within this task because the dev-dependency is added here, first.
    - `grep -c 'pmcp-agent' Cargo.toml` is at least 1, on a line inside `[dev-dependencies]` carrying
      an inline comment naming the example.
    - `cargo metadata --no-deps` exits 0 — the dev-dependency cycle (root `pmcp` dev-deps
      `pmcp-agent`, which depends on `pmcp`) resolves, as it already does for `pmcp-code-mode`.
    - The file contains a comment recording why `s53` was chosen and which numbers are occupied.
    - `examples/s53_v2_agent_client.rs` contains zero occurrences of `TODO`, `FIXME`, `XXX`.
  </acceptance_criteria>
  <done>
    The dev-dependency is wired and a runnable, self-checking example demonstrates the v2 happy path,
    the v1 fallback and the unreachable-host propagation, with the exit-code contract stated in its
    header and no hand-rolled poll loop — and it BUILDS, checked within this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the example into the root manifest and prove it runs</name>
  <files>Cargo.toml</files>
  <read_first>
    - `Cargo.toml` lines 195-205 (the existing `[dev-dependencies]` path deps for `pmcp-macros`,
      `pmcp-code-mode`, `pmcp-code-mode-derive`, each with an inline comment naming which example
      needs it) and lines 615-660 (the `[[example]]` blocks, including the `s49` collision note at
      `:640-641` and the `s52_v2_caching_hints` block at `:655-660`)
    - `crates/pmcp-agent/Cargo.toml` — the `url-connector` feature definition and the version as
      bumped by plan 117-07 (`0.2.0`)
    - `Makefile` lines 254-266 (`test-examples` — note it treats a double build failure as a SKIP with
      a yellow warning, so a green `make test-examples` does NOT prove this example builds)
    - `examples/s53_v2_agent_client.rs` as written by Task 1
  </read_first>
  <action>
    The `[dev-dependencies]` entry was added in Task 1 — verify it is present and unchanged rather
    than adding it again.

    Add an `[[example]]` block for `s53_v2_agent_client` with `name`, `path`, and
    `required-features` listing the pmcp features the example genuinely needs (at minimum
    `streamable-http` and `http-client`, matching the `s48_v2_mrtr_client` block). Place it adjacent
    to the other `sNN` v2 example blocks and add a comment recording the number derivation (`s49`
    occupied twice; `s50`/`s51`/`s52` on disk; `s53` next free).

    Verify the dev-dependency cycle is benign — root `pmcp` dev-deps `pmcp-agent`, which depends on
    `pmcp`. Cargo permits this and the repo already relies on it for `pmcp-code-mode`. Prove it rather
    than assuming: run `cargo metadata --no-deps > /dev/null` and `cargo build -p pmcp --features full`
    and record both results.

    `Cargo.lock` is GITIGNORED in this repository (`.gitignore:3`) and is not tracked by git — confirm
    with `git ls-files --error-unmatch Cargo.lock`, which errors. There is therefore no lockfile to add
    to `files_modified` and none to commit; do not create or commit one. Record that confirmation, so
    a reviewer expecting a lockfile change knows it was checked rather than forgotten.

    Note also that plan 117-07 bumps `pmcp-agent` to `0.2.0` and updates the two version pins in
    `crates/pmcp-team-servers/Cargo.toml:22` and `cargo-pmcp/Cargo.toml:75`. This plan's dev-dependency
    is PATH-ONLY at Task 1's chosen shape; if it carries a `version` key, it must match the version
    117-07 left in place. Check `crates/pmcp-agent/Cargo.toml`'s current `version` before writing the
    line and record what you found.

    CRITICALLY: confirm the new dev-dependency does NOT contaminate the severance build. Run
    `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` and
    `cargo tree -p pmcp --no-default-features --features full-v2 -e features | grep -c 'v1-compat'`
    (must be 0) and record both. The severance build is lib-only (`pmcp` declares no `[[bin]]`), so
    dev-dependencies are not compiled — but `url-connector` enables `pmcp/streamable-http`, so this is
    exactly the shape that would break assumption A-A1 if `-p` scoping did not hold. Measure it.

    Then RUN the example end to end: start the paired server example in the background, run
    `cargo run --example s53_v2_agent_client --features "full"`, capture stdout and the exit code, and
    paste both into the summary. Building is not running; CLAUDE.md's ALWAYS requirement is
    `cargo run --example`.
  </action>
  <verify>
    <automated>cargo build --example s53_v2_agent_client --features "full" &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --example s53_v2_agent_client --features "full"` exits 0.
    - `grep -c 's53_v2_agent_client' Cargo.toml` is at least 2 (the `name` and `path` keys).
    - `git ls-files --error-unmatch Cargo.lock` ERRORS, confirming the lockfile is untracked; its
      output is recorded and no `Cargo.lock` was committed.
    - If the dev-dependency carries a `version` key it matches `crates/pmcp-agent/Cargo.toml`'s
      current `version` (`0.2.0` after plan 117-07); the value found is recorded.
    - `cargo metadata --no-deps` exits 0 (the dev-dependency cycle resolves).
    - `cargo build -p pmcp --features full` exits 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with zero `warning:` lines, and
      `cargo tree -p pmcp --no-default-features --features full-v2 -e features | grep -c 'v1-compat'`
      is 0 — the new dev-dep did not contaminate the severance proof. Both outputs recorded.
    - `cargo test --test v1_severability_tripwire` exits 0 (the `full`/`full-v2` drift check still
      holds after a manifest edit).
    - The example was RUN against the paired server; its stdout and exit code are pasted into the
      summary and the exit code is 0.
    - `make test-examples` exits 0 AND its output shows
      `✓ Example s53_v2_agent_client built successfully` — not the yellow
      `⚠ Example ... requires specific features (skipped)` line, which would mean the example was
      silently skipped rather than built.
  </acceptance_criteria>
  <done>
    The example is declared, builds, RUNS to exit 0 against a live paired server, is visibly built
    (not skipped) by `make test-examples`, and is proven not to contaminate the severance build.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example process → local MCP server | The example opens real loopback connections |
| dev-dependency graph → severance proof | A dev-dep enabling `pmcp/streamable-http` is the exact shape that would break `-p` scoping if it did not hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-34 | Repudiation | the ALWAYS-example requirement | mitigate | `make test-examples` treats a double build failure as a SKIP with a yellow warning, so a green target does not prove the example builds. The acceptance criterion asserts the `✓ … built successfully` line specifically, and the example is additionally RUN with its exit code recorded |
| T-117-35 | Tampering | the severance proof | mitigate | The new root dev-dep transitively enables `pmcp/streamable-http`. Assumption A-A1 says `-p pmcp` does not unify with siblings; this plan RE-MEASURES it after the manifest edit rather than trusting the earlier measurement of a different manifest |
| T-117-36 | Spoofing | the demonstrated era | mitigate | The example prints the NEGOTIATED protocol version from the connector rather than asserting an era it assumed, and `demo_unreachable_propagates` shows the error propagating rather than a silent v1 downgrade (Pitfall 7) |
| T-117-37 | Denial of Service | example run | mitigate | No hand-rolled poll loop; task interaction routes through the SDK primitive's `max_poll_duration_secs`-capped `wait_for_related_task`, so a second timeout policy cannot be introduced |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero EXTERNAL packages added. The one manifest addition is a path dependency on an in-repo workspace member (`crates/pmcp-agent`), matching three existing root dev-deps; nothing is fetched from a registry |
</threat_model>

<verification>
- `cargo build --example s53_v2_agent_client --features "full"` exits 0.
- The example RUNS against the paired server with exit code 0; stdout recorded.
- `make test-examples` output contains `✓ Example s53_v2_agent_client built successfully`.
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0 and
  the `v1-compat` node count in its feature tree is 0 — recorded, SEPARATELY from `make quality-gate`.
- `cargo test --test v1_severability_tripwire` exits 0.
- `cargo metadata --no-deps` exits 0.
- `make quality-gate` exits 0.
</verification>

<success_criteria>
- `examples/s53_v2_agent_client.rs` demonstrates the v2 happy path, the v1 fallback and
  unreachable-host propagation, with an explicit exit-code contract and no hand-rolled poll loop.
- The number choice is derived from the actual occupied set and recorded in-file.
- The example is declared in root `Cargo.toml`, builds, and is visibly BUILT (not skipped) by
  `make test-examples`.
- The example was RUN with its stdout and exit code recorded.
- The new dev-dependency is proven not to contaminate the `full-v2` severance build.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-10-SUMMARY.md` when done.
Record: which paired server example was chosen and why; whether the task-polling demo was included or
dropped and the reason (and, if dropped, that the header cites the 117-04 test); the
`git ls-files --error-unmatch Cargo.lock` confirmation; the `pmcp-agent` version found in its manifest;
the example's full stdout and exit code; the post-edit severance build output
and `v1-compat` tree count; and the `make test-examples` line for this example verbatim.
</output>
