---
phase: 117-agents-tester-v1-severability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - tests/v1_severability_tripwire.rs
  - docs/v1-sunset-policy.md
  - Makefile
  - src/lib.rs
autonomous: true
requirements: [SMPL-01]

must_haves:
  truths:
    - "A `v1-compat` cargo feature exists, is default-on, and is in `full` — every existing consumer is unaffected (D-01)"
    - "A parallel `full-v2` feature list exists that is `full` minus exactly `v1-compat`, and `cargo build -p pmcp --no-default-features --features full-v2` compiles the REAL transport (axum/hyper present) rather than compiling no transport at all (D-02)"
    - "A feature added to `full` and forgotten in `full-v2` FAILS a test, derived from Cargo.toml at test time rather than from an enumerated list (D-02 drift hazard)"
    - "A reader can find a written, condition-gated v1 sunset policy that commits to NO date, NO `#[deprecated]`, and NO runtime warning (D-04)"
    - "The sunset-policy rustdoc is compiled by the blocking `make doc-check` gate, not merely written"
    - "A-A1 is recorded as CLOSED by re-measurement: `-p pmcp` does not unify features with workspace siblings, so the severance build is not a false green"
  artifacts:
    - path: "Cargo.toml"
      provides: "`v1-compat` marker feature in `default` and `full`; new `full-v2` severance-proof list"
      contains: "full-v2"
    - path: "tests/v1_severability_tripwire.rs"
      provides: "Derived full/full-v2 drift tripwire + default/full membership check, both with non-vacuity guards"
      min_lines: 90
    - path: "docs/v1-sunset-policy.md"
      provides: "The D-04 normative, condition-gated sunset policy"
      min_lines: 40
    - path: "Makefile"
      provides: "`v1-compat` added to the doc-check feature list so the policy rustdoc is gated"
      contains: "v1-compat"
    - path: "src/lib.rs"
      provides: "Crate-doc paragraph documenting the `v1-compat` feature and pointing at the policy"
      contains: "v1-compat"
  key_links:
    - from: "tests/v1_severability_tripwire.rs"
      to: "Cargo.toml [features]"
      via: "toml::from_str parse of CARGO_MANIFEST_DIR/Cargo.toml at test time"
      pattern: "CARGO_MANIFEST_DIR"
    - from: "Makefile doc-check feature list"
      to: "src/lib.rs v1-compat crate doc"
      via: "RUSTDOCFLAGS=-D warnings cargo doc --features ...,v1-compat"
      pattern: "v1-compat"
---

<objective>
Create the compile-time severability primitive the whole phase rests on: a default-on `v1-compat`
marker feature, a parallel `full-v2` proof set, a DERIVED tripwire that makes drift between the two
lists a test failure, and the written condition-gated sunset policy — wired into the blocking
rustdoc gate.

Purpose: SMPL-01 asks for a "clearly severable era-gated layer with a documented sunset policy".
D-01 makes severance a compile-time fact rather than a convention; D-02 makes the PROOF sound
(`--no-default-features` alone would prove nothing because `default = ["logging"]` — it would never
compile the transport at all); D-04 fixes what the policy may and may not commit to.

Output: `v1-compat` + `full-v2` in `Cargo.toml`, `tests/v1_severability_tripwire.rs`,
`docs/v1-sunset-policy.md`, the `Makefile:429` doc-check edit, and the `src/lib.rs` crate-doc
paragraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
<!-- Current Cargo.toml [features] head, Cargo.toml:203-205. This is the exact text being edited. -->
[features]
default = ["logging"]
full = ["websocket", "http", "streamable-http", "sse", "validation", "resource-watcher", "rayon", "schema-generation", "jwt-auth", "composition", "mcp-apps", "http-client", "logging", "macros", "testing"]

<!-- Already-available, zero-new-dependency parser. Cargo.toml:76 -->
toml = "1.0"   # plain [dependencies] entry of the root pmcp crate

<!-- The scope-note precedent for a dependency-free marker feature, Cargo.toml:236-243 -->
fuzzing = []      # carries a written "NOT in default, NOT in full" rationale comment
test-helpers = []
testing = []      # carries a written "folded into full because ..." rationale comment
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare `v1-compat` and `full-v2`, and prove the severance build compiles the real transport</name>
  <files>Cargo.toml</files>
  <read_first>
    - `Cargo.toml` lines 200-250 (the whole `[features]` block — see the current `default`/`full`
      text quoted in `<interfaces>` above, and the `fuzzing`/`testing` scope-note comment style at
      `:236-243` which is the pattern to copy)
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q3.1-Q3.3 (the exact
      proposed feature block) and § Q3.5 (the THREE false-green hazards)
    - `.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md` § "Pattern S2 — A
      dependency-free marker feature with a written scope note"
  </read_first>
  <action>
    Edit `Cargo.toml`'s `[features]` block:

    1. `default = ["logging", "v1-compat"]` — `v1-compat` joins `default` so no existing consumer
       changes behavior (D-01, "default-on").
    2. Append `"v1-compat"` to the existing `full` list, keeping every current entry in place.
    3. Add a new `full-v2` key listing the 15 entries of `full` EXCEPT `v1-compat`:
       `websocket, http, streamable-http, sse, validation, resource-watcher, rayon,
       schema-generation, jwt-auth, composition, mcp-apps, http-client, logging, macros, testing`.
    4. Add `v1-compat = []` as a dependency-free marker feature.

    Both `full-v2` and `v1-compat` carry a written scope-note comment in the style of the existing
    `fuzzing` / `testing` entries. `full-v2`'s comment states that it is `full` minus `v1-compat`,
    that it is kept in sync by `tests/v1_severability_tripwire.rs` which DERIVES both lists from
    this file, and that an inverted `v2-only` feature was REJECTED because cargo features are
    additive and cannot be subtracted (D-02). `v1-compat`'s comment states what it gates (MCP
    2025-11-25 initialize/session lifecycle and SSE resumability via `Last-Event-ID`), that it is
    default-on, that building without it is the SMPL-01 severance proof, that removing it is
    SMPL-F1 / pmcp 3.0, and points at `docs/v1-sunset-policy.md`.

    Do NOT add `v1-compat` to any other feature's dependency list — it is a pure marker with an
    empty array.

    Then run the severance build and the two false-green checks, and record their output verbatim
    in the plan summary:
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`
    - `cargo tree -p pmcp --no-default-features --features full-v2 -e features | grep -cE 'axum|hyper'`
      must be non-zero — a zero here is Pitfall 1 (the transport was never compiled).
    - `cargo tree -p pmcp --no-default-features --features full-v2 -e features | grep -c 'v1-compat'`
      must be 0 — this is the A-A1 re-confirmation that `-p pmcp` does not unify features with
      workspace siblings. A-A1 was already measured CLOSED before planning; this task RE-RECORDS the
      measurement against the manifest as edited, and records it in the summary as "A-A1 re-confirmed
      post-edit", not as a new spike.

    Do NOT add `--all-features`, `--all-targets`, `--tests` or `--examples` to the severance build.
    All three are false greens (`--all-features` enables `v1-compat` AND `full-v2` together;
    `--all-targets` drags in ~29 struct-literal sites in `tests/` and `examples/` for zero
    additional proof, since `pmcp` declares no `[[bin]]` so `cargo build` is lib-only).
  </action>
  <verify>
    <automated>RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 &amp;&amp; cargo build -p pmcp --features full</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^full-v2 = ' Cargo.toml` returns 1.
    - `grep -c '^v1-compat = \[\]' Cargo.toml` returns 1.
    - `grep '^default = ' Cargo.toml` output contains both `"logging"` and `"v1-compat"`.
    - `grep '^full = ' Cargo.toml` output contains `"v1-compat"`.
    - `grep '^full-v2 = ' Cargo.toml` output does NOT contain `v1-compat`, and DOES contain
      `"streamable-http"` (without it the severance build is Pitfall 1).
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with no `warning:` line in its output.
    - `cargo build -p pmcp --features full` exits 0 (the v1-compat build still works).
    - `cargo tree -p pmcp --no-default-features --features full-v2 -e features | grep -cE 'axum|hyper'`
      is greater than 0.
    - `cargo tree -p pmcp --no-default-features --features full-v2 -e features | grep -c 'v1-compat'`
      is exactly 0.
    - The lines above `full-v2 = ` and above `v1-compat = []` each contain at least one `#` comment
      line, and the `full-v2` comment text contains the token `v1_severability_tripwire`.
  </acceptance_criteria>
  <done>
    `v1-compat` is a default-on marker feature in `default` and `full`; `full-v2` is a parallel
    15-entry list that compiles axum/hyper and does not pull `v1-compat`; both carry written scope
    notes; the `-D warnings` severance build is green and A-A1 is re-confirmed against the edited
    manifest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derived full/full-v2 drift tripwire with non-vacuity guards</name>
  <files>tests/v1_severability_tripwire.rs</files>
  <read_first>
    - `tests/v2_bounded_reads_tripwire.rs` lines 144-197 (`repo_root`, `rel`, `collect_rs_files`,
      `scope_files` — the derived-scope + two-layer non-vacuity guard idiom; copy `repo_root`/`rel`
      verbatim) and lines 84-101 (why a scope constant carries its own rationale)
    - `tests/phase115_contract_bindings.rs` lines 87-90, 149, 432-438, 701-712 (the
      `FAILURE MODE: … / WHAT TO DO: …` two-line assertion-message form and the named-floor
      constant — this is the newest and preferred form)
    - `tests/v2_schema_tripwires.rs` lines 26-42 (the "manifests are NEVER read as text" rule and
      why `toml::from_str` PARSING is on the safe side of it)
    - `Cargo.toml` lines 203-250 as edited by Task 1
  </read_first>
  <behavior>
    - Test `full_and_full_v2_differ_by_exactly_v1_compat`: parsing `Cargo.toml`'s `[features]`,
      `full` minus `full-v2` == exactly `{"v1-compat"}`, and `full-v2` minus `full` is empty.
    - Test `v1_compat_is_in_default_and_full`: `default` contains `v1-compat`; `full` contains
      `v1-compat`.
    - Non-vacuity: a parse yielding fewer than 15 entries for `full` or fewer than 14 for `full-v2`
      FAILS with a message naming the broken reader as the cause, not the assertion.
    - Missing-key behavior: if `full-v2` is deleted from `Cargo.toml`, the test panics with a
      message naming the missing feature — it does not silently pass over an empty set.
  </behavior>
  <action>
    Create `tests/v1_severability_tripwire.rs`.

    Module doc (`//!`) states: this file is the SMPL-01 drift gate; `full` and `full-v2` are two
    enumerated lists that can drift, and a feature added to `full` and forgotten in `full-v2`
    silently SHRINKS the severance proof; the scope is DERIVED from `Cargo.toml` at test time
    because 116-14 proved an enumerated scope hides real defects.

    Implement a `feature_list(&toml::Value, name) -> BTreeSet<String>` helper that reads
    `concat!(env!("CARGO_MANIFEST_DIR"), "/Cargo.toml")`, parses it with `toml::from_str` (the `toml`
    crate is already a plain runtime dependency at `Cargo.toml:76` — add NO new dependency and do NOT
    hand-roll a line scanner), indexes `["features"][name]`, and panics with a message naming the
    missing feature when the key is absent or is not an array.

    Add named floor constants in the `tests/phase115_contract_bindings.rs:149` style —
    `MIN_FULL_ENTRIES: usize = 15` and `MIN_FULL_V2_ENTRIES: usize = 14` — each with a doc comment
    saying the floor exists so a broken parser cannot make the difference assertion pass over an
    empty set, and that the remedy is to fix the reader, never to lower the floor.

    Write the two tests named in `<behavior>`. The difference assertion's failure message must name
    the consequence in words ("a feature added to `full` and forgotten in `full-v2` silently shrinks
    the severance proof"), not just print two sets. The `default` membership assertion's message
    must say that dropping `v1-compat` from `default` silently breaks every existing user.

    Add a third test `the_feature_list_reader_is_not_vacuous` that asserts the parsed `full` and
    `full-v2` sets each meet their floor, using the `FAILURE MODE: …` / `WHAT TO DO: fix the reader,
    not the assertion.` two-line message form.

    Do NOT add a v1-module source-content check in this task — `v1_session_off.rs` does not exist
    yet. Plan 117-06 extends this same file with that check.
  </action>
  <verify>
    <automated>cargo test --test v1_severability_tripwire</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test v1_severability_tripwire` exits 0 and its output reports `3 passed`
      (or more) and `0 failed`, with a non-zero test count.
    - `grep -c 'CARGO_MANIFEST_DIR' tests/v1_severability_tripwire.rs` is at least 1.
    - `grep -c 'toml::from_str' tests/v1_severability_tripwire.rs` is at least 1.
    - `grep -v '^//' tests/v1_severability_tripwire.rs | grep -c 'MIN_FULL_ENTRIES'` is at least 2
      (declaration plus at least one use).
    - `grep -c 'FAILURE MODE' tests/v1_severability_tripwire.rs` is at least 1.
    - Negative control, run and recorded verbatim in the summary, then REVERTED: temporarily append
      a junk entry (e.g. `"skills"`) to `full` in `Cargo.toml` and re-run
      `cargo test --test v1_severability_tripwire`; it MUST fail, and the failure message must name
      `v1-compat` and the shrunk-proof consequence. Restore `Cargo.toml` and re-run to green.
    - `tests/v1_severability_tripwire.rs` contains zero occurrences of `TODO`, `FIXME` or `XXX`.
  </acceptance_criteria>
  <done>
    The drift between `full` and `full-v2` is a derived, non-vacuous test failure with an actionable
    message, proven by an executed negative control rather than asserted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the condition-gated sunset policy and wire its rustdoc into the blocking doc gate</name>
  <files>docs/v1-sunset-policy.md, Makefile, src/lib.rs</files>
  <read_first>
    - `docs/protocol-compatibility.md` lines 1-30 (house shape for a top-level normative `docs/`
      document: title, `## Overview`, status tables) and `docs/MIGRATION.md` for the sibling tone
    - `Makefile` lines 424-431 (the `doc-check` target and its hand-enumerated 15-feature list —
      the line to edit is the `--features composition,http,...,websocket` continuation)
    - `.github/workflows/ci.yml` lines 228-235 (proof that `make doc-check` runs inside the
      `quality-gate` job) and lines 441-445 (proof that `quality-gate` IS in `gate.needs`)
    - `src/lib.rs` crate-level `//!` doc block (the head of the file) — the conventional home for
      feature documentation
    - `.planning/REQUIREMENTS.md:979` (SMPL-F1's "adoption-gated" wording, which the policy's removal
      condition must match) and `:992` (the standing "SSE resumability on the v2 path" out-of-scope
      ruling)
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q7 (recommended homes
      and the DOCS-05 scope split)
  </read_first>
  <action>
    Create `docs/v1-sunset-policy.md` as a normative one-pager. It MUST contain, each as its own
    section:

    - What `v1-compat` gates: the MCP 2025-11-25 initialize/session lifecycle and SSE resumability
      (`Last-Event-ID` + event store). Name `src/server/streamable_http_server/v1_session.rs` and
      `src/shared/event_store.rs` as the gated surface.
    - Explicitly state that SSE **framing and parsing are SHARED and NOT gated** — v2's
      `subscriptions/listen` returns a live `text/event-stream`, so only SSE *resumability* is
      v1-only (A-D03). This paragraph exists to stop a future reader from "finishing the job" by
      gating `src/shared/sse_parser.rs` or `src/shared/sse_optimized.rs`.
    - The removal CONDITION: public-client v2 adoption, per SMPL-F1 (`REQUIREMENTS.md:979`), landing
      in a future pmcp 3.0. State explicitly that there is NO date and NO committed window.
    - What a consumer must do today: nothing. `v1-compat` is in `default`.
    - How to verify severability yourself: the exact command
      `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`, plus a
      sentence saying that `--all-features` and workspace-wide builds can NEVER prove severance
      because cargo features are additive.
    - Explicit non-commitments (D-04): no `#[deprecated]` attributes, no runtime warning on v1
      negotiation, and v1 wire behavior stays byte-identical.
    - A scope note stating that THIS document is the normative policy and that Phase 119 / DOCS-05
      writes the narrative migration guide which links here — so 119 does not duplicate or override
      it.

    Add a `## The `v1-compat` feature` paragraph to `src/lib.rs`'s crate-level `//!` doc block. It
    states the same three facts in two or three sentences — default-on, gates the 2025-11-25
    session/resumability layer, and building without it (`--no-default-features --features full-v2`)
    is the severability proof — and links to `docs/v1-sunset-policy.md` by relative path. Use no
    intra-doc link that rustdoc cannot resolve; a bare relative markdown link to the docs file is
    correct here.

    Edit `Makefile`'s `doc-check` target: append `,v1-compat` to the end of the `--features` list on
    the continuation line (the list currently ends with `websocket`). Without this the new crate-doc
    paragraph is not compiled by the blocking gate at `ci.yml:230-231`.

    Do NOT add a `#[deprecated]` attribute anywhere. Do NOT add a runtime warning. Do NOT write a
    date or a month/year window into the policy.
  </action>
  <verify>
    <automated>make doc-check</automated>
  </verify>
  <acceptance_criteria>
    - `docs/v1-sunset-policy.md` exists and is at least 40 lines.
    - `grep -c 'full-v2' docs/v1-sunset-policy.md` is at least 1, and the file contains the exact
      string `cargo build -p pmcp --no-default-features --features full-v2`.
    - `grep -ci 'deprecated' docs/v1-sunset-policy.md` is at least 1 AND every such occurrence is in
      a non-commitment sentence (verified by reading them); there is no `#[deprecated]` attribute
      added to any `.rs` file in this plan (`git diff --stat` shows no `src/**.rs` change other than
      `src/lib.rs`, and `git diff src/lib.rs | grep -c 'deprecated'` is 0).
    - `docs/v1-sunset-policy.md` contains the token `sse_parser` (the A-D03 do-not-gate paragraph).
    - `docs/v1-sunset-policy.md` matches none of the patterns `20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]`
      other than protocol version strings `2025-11-25` / `2026-07-28`, and contains no
      "12-month"/"12 month" window text.
    - `grep -c 'v1-compat' src/lib.rs` is at least 1.
    - `grep -c 'v1-compat' Makefile` is at least 1, and the `doc-check` `--features` line ends with
      `websocket,v1-compat`.
    - `make doc-check` exits 0 with zero rustdoc warnings.
  </acceptance_criteria>
  <done>
    A condition-gated, date-free sunset policy exists in `docs/`, its rustdoc half is compiled by the
    blocking `make doc-check` gate, and the A-D03 "SSE parsing is shared" correction is written down
    where a future contributor will read it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build configuration → shipped artifact | A feature-list mistake changes what code exists in a consumer's binary |
| repository → downstream crates | `v1-compat` leaving `default` silently strips v1 from every consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Denial of Service | `Cargo.toml` `[features]` | mitigate | An inverted `v2-only` feature is REJECTED (D-02): cargo features are additive, so any crate in the graph enabling it would strip v1 for every other consumer. `v1-compat` is additive and default-on; Task 2's `v1_compat_is_in_default_and_full` test fails if it is ever dropped from `default` |
| T-117-02 | Tampering | severance proof | mitigate | `full`/`full-v2` drift silently shrinks the proof. Task 2 derives both lists from `Cargo.toml` at test time with a named floor and an executed negative control, per the 116-14 derived-scope lesson |
| T-117-03 | Repudiation | severance build result | mitigate | The build is pinned to `-p pmcp --no-default-features --features full-v2` with `RUSTFLAGS="-D warnings"`; the `axum|hyper` presence check and the `v1-compat` absence check are separate recorded acceptance items, so a green that compiled no transport cannot be reported as a pass |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO external packages. `toml` (`Cargo.toml:76`) and `proptest` are pre-existing declared dependencies; no `cargo add` is run, so the Package Legitimacy Gate has no subject |
</threat_model>

<verification>
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0 with
  zero warnings — reported SEPARATELY from `make quality-gate`, which runs `--all-features`
  (`Makefile:135`) and can therefore NEVER prove severance.
- `cargo build -p pmcp --features full` exits 0.
- `cargo test --test v1_severability_tripwire` exits 0 with a non-zero test count.
- `make doc-check` exits 0.
- `make quality-gate` exits 0.
</verification>

<success_criteria>
- `v1-compat` exists as a default-on, dependency-free marker feature present in both `default` and
  `full`, with a written scope note.
- `full-v2` exists as `full` minus exactly `v1-compat`, contains `streamable-http`, and its build
  pulls axum/hyper while pulling zero `v1-compat`.
- Drift between the two lists is a derived, non-vacuous test failure with an actionable message,
  proven by an executed and reverted negative control.
- `docs/v1-sunset-policy.md` states a condition (not a date), commits to no `#[deprecated]` and no
  runtime warning, records the A-D03 shared-SSE correction, and scopes itself against Phase 119.
- The policy's rustdoc half is inside the blocking `make doc-check` gate.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-01-SUMMARY.md` when done.
Record verbatim: the severance build output, the two `cargo tree` counts (A-A1 re-confirmation), and
the negative-control failure message from Task 2.
</output>
