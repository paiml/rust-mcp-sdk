---
phase: 117-agents-tester-v1-severability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-agent/tests/common/v2_server.rs
  - crates/pmcp-agent/tests/agent_v2_e2e.rs
autonomous: true
requirements: [CLNT-03]

must_haves:
  truths:
    - "`crates/pmcp-agent/tests/` gains its first harness that drives a REAL in-process `StreamableHttpServer` — no such harness exists today, and the root `tests/common/v2.rs` lives in a different crate and cannot be imported"
    - "The D-07 negative case is a first-class test, not a footnote: an agent pointed at a v1-only server must fall back and succeed via v1, and the assertion is on OBSERVED wire behavior, not on a log line"
    - "An unreachable host PROPAGATES the transport error and must NOT be reported as era V1 — the Pitfall-7 silent-downgrade failure has its own test"
    - "The tests are written RED against the target `client_for` behavior BEFORE plan 117-07 implements it, so the implementation is driven by a contract that already exists"
    - "CLNT-03 says `pmcp-agent` works end-to-end against a v2 server *including its ToolInvoker and task polling*, so the harness GUARANTEES a task-associated tool result and the test asserts task-id discovery, at least one poll and a terminal state. Polling must not be conditional on what the server happened to return — an `if` there means the success criterion can go unproven"
  artifacts:
    - path: "crates/pmcp-agent/tests/common/v2_server.rs"
      provides: "In-crate spawn/teardown harness for a real pmcp StreamableHttpServer in both v2-accepting and v1-only configurations, including a tool whose result is GUARANTEED task-associated"
      min_lines: 100
    - path: "crates/pmcp-agent/tests/agent_v2_e2e.rs"
      provides: "Four live-socket cases: v2 happy path, GUARANTEED task polling, v1 fallback, unreachable-host propagation"
      min_lines: 180
      contains: "unreachable"
  key_links:
    - from: "crates/pmcp-agent/tests/agent_v2_e2e.rs"
      to: "crates/pmcp-agent/tests/common/v2_server.rs"
      via: "#[path = \"common/v2_server.rs\"] mod v2_server; (the in-crate shared-module convention proven by tests/common/duplex.rs)"
      pattern: "#\\[path = \"common/"
    - from: "crates/pmcp-agent/tests/agent_v2_e2e.rs"
      to: "crates/pmcp-agent/src/invoker/factory.rs client_for"
      via: "UrlConnectorClientFactory::client_for against a live loopback endpoint"
      pattern: "client_for"
---

<objective>
Build the missing live-socket harness for `pmcp-agent` and write the three CLNT-03 end-to-end cases
RED, before the factory changes. The negative cases — v1 fallback and unreachable-host propagation —
get equal weight to the happy path, because D-07 says fallback paths are where dual-version bugs
hide.

Purpose: CLNT-03 requires `pmcp-agent` (including `ToolInvoker` and task polling) to work end-to-end
against a v2 server. Today `UrlConnectorClientFactory::client_for`
(`crates/pmcp-agent/src/invoker/factory.rs:125-146`) calls `initialize` UNCONDITIONALLY at `:141`,
and v2 has no `initialize` — that single call is the whole bug. No `pmcp-agent` test currently drives
a real `StreamableHttpServer`, so the harness is real work, not a copy.

Output: `crates/pmcp-agent/tests/common/v2_server.rs` and
`crates/pmcp-agent/tests/agent_v2_e2e.rs`, both RED against the current factory (each failing for the
documented reason), turning GREEN in plan 117-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
<!-- crates/pmcp-agent/src/invoker/factory.rs — the seam under test. -->
#[async_trait]
pub trait ConnectorClient: Send + Sync {
    async fn call_tool(&self, name: &str, arguments: serde_json::Value) -> Result<CallToolResult, InvokerError>;
    async fn wait_for_related_task(&self, meta: &TaskMetadata, opts: WaitForTaskOptions) -> Result<CallToolResult, InvokerError>;
    async fn list_tools(&self) -> Result<Vec<ToolInfo>, InvokerError> { Ok(Vec::new()) }   // default: empty
}

#[async_trait]
pub trait ConnectorClientFactory: Send + Sync {
    async fn client_for(&self, endpoint: &str) -> Result<Arc<dyn ConnectorClient>, InvokerError>;
}

pub enum InvokerError {
    Transport(String),
    UnsupportedScheme(String),
    Config(String),
    /* ... read the full enum before matching on it ... */
}

<!-- crates/pmcp-agent/Cargo.toml — what is available to a test in this crate. -->
[dev-dependencies]
pmcp = { version = "2.17.0", path = "../..", features = ["full"] }   # StreamableHttpServer IS available
tokio = { version = "1", features = ["full"] }
proptest = "1.7"

[features]
url-connector = ["pmcp/streamable-http", "dep:url"]   # the era-probe path needs this
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the in-crate live v2/v1 server harness</name>
  <files>crates/pmcp-agent/tests/common/v2_server.rs</files>
  <read_first>
    - `crates/pmcp-agent/tests/common/duplex.rs` in full, plus one of its consumers
      (`crates/pmcp-agent/tests/real_loop_sampling.rs` line 13) — this proves the in-crate
      `#[path = "common/…"] mod …;` shared-module convention that must be followed here
    - `crates/pmcp-agent/tests/http_sources_mock.rs` lines 1-60 (the only existing socket test in
      this crate: a raw `TcpListener` mock — the shape to use for the NEGATIVE cases a real server
      cannot produce)
    - `crates/pmcp-agent/tests/invoker_task_augmented.rs` lines 1-60 (the
      `#![cfg(not(target_arch = "wasm32"))]` gate and the mock `ConnectorClient` `Behavior` +
      `AtomicUsize` counter shape)
    - `tests/common/v2.rs` lines 383-400 (`spawn_default_config`, `spawn_stateless_config`,
      `spawn_with`) and `:528-532` (`teardown` and why the drop→abort→await ORDER is D-113-T) — this
      is the DESIGN REFERENCE only; it is in a different crate and CANNOT be imported
    - `examples/s47_v2_stateless_mrtr.rs` — the in-repo canonical v2-accepting server setup, for how
      a server is opted into the 2026-07-28 accept-list
    - `crates/pmcp-agent/Cargo.toml` (dev-deps and the `url-connector` feature)
  </read_first>
  <action>
    Create `crates/pmcp-agent/tests/common/v2_server.rs`.

    Module doc (`//!`) states: this is the FIRST harness in `crates/pmcp-agent/tests/` that drives a
    real `pmcp::StreamableHttpServer`; the root `tests/common/v2.rs` is in a different crate and
    cannot be imported; files under `tests/common/` are not compiled as their own test binaries, so
    this is the correct home; consume it with `#[path = "common/v2_server.rs"] mod v2_server;`
    (the convention `tests/common/duplex.rs` already establishes in this crate).

    Provide, all `pub`:
    - `spawn_v2(server: Server) -> (SocketAddr, JoinHandle<()>)` — a server opted into the
      2026-07-28 accept-list so a v2 `server/discover` succeeds. Read
      `examples/s47_v2_stateless_mrtr.rs` for the exact opt-in call; do not guess the builder method
      name.
    - `spawn_v1_only(server: Server) -> (SocketAddr, JoinHandle<()>)` — a default-config server NOT
      opted into v2, so a v2 `server/discover` is rejected with the measured signature (v2 gate
      returns Passthrough at `streamable_http_server.rs:2277`, then
      `validate_protocol_version_supported` at `:1944-1958` → HTTP 400 / JSON-RPC `-32600`).
    - `teardown(handle: JoinHandle<()>)` — implementing the drop-sockets → `abort()` → `await` ORDER.
      Carry the D-113-T comment verbatim in spirit: a bare `abort()` produces intermittent nextest
      `LEAK` noise.
    - `endpoint(addr: SocketAddr) -> String` — the `http://{addr}/` string the factory takes.
    - `pinned_server() -> Server` — a deterministic server registering exactly TWO tools, both with
      total handlers (no panic path), so `tools/list` is byte-stable and every `tools/call`
      terminates:
        * a plain tool returning an immediate `CallToolResult`;
        * a tool whose result is GUARANTEED to carry a related-task envelope under `_meta`, and whose
          task reaches a TERMINAL state after a bounded, deterministic number of polls (at least one
          non-terminal poll, so the polling loop is genuinely exercised rather than short-circuited by
          an immediately-terminal task).
      CLNT-03's success criterion names task polling explicitly, so the fixture must GUARANTEE the
      task rather than leaving the test to discover whether one happened to appear. Read
      `examples/s50_v2_tasks_server.rs` and the Phase-114 tasks surface for how a handler attaches the
      related-task envelope; if that surface has moved under Phase 114's D-18 hold (D-09 flags it as
      PROVISIONAL), adapt to what the tree actually exposes and RECORD what you found — do not
      fabricate an envelope shape.
      Expose the terminal-state expectation as a `pub` constant so the test asserts against the
      harness's contract rather than a magic value.
    - `closed_loopback_endpoint() -> String` — bind an ephemeral port, capture the address, DROP the
      listener, and return the endpoint. That address is then guaranteed unreachable, which is how
      the unreachable-host case gets a deterministic target without depending on the network.

    Gate the file's consumers, not the file: the harness itself needs no `#![cfg]`, but every
    consuming test binary carries
    `#![cfg(all(feature = "url-connector", not(target_arch = "wasm32")))]` because the era-probe path
    needs `url-connector` and `StreamableHttpTransport` is `#[cfg(not(target_arch = "wasm32"))]`.

    Add a bounded-wait constant with a doc comment saying a hung server must FAIL the test, not hang
    it, and apply it to any await that could block indefinitely.
  </action>
  <verify>
    <automated>cargo build -p pmcp-agent --tests --features url-connector</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-agent/tests/common/v2_server.rs` exists and is at least 100 lines.
    - `cargo build -p pmcp-agent --tests --features url-connector` exits 0.
    - The file exports all eight items named in the action (`spawn_v2`, `spawn_v1_only`, `teardown`,
      `endpoint`, `pinned_server`, `closed_loopback_endpoint`, the bounded-wait constant, and the
      terminal-state constant), each `pub` — verified by
      `grep -c '^pub ' crates/pmcp-agent/tests/common/v2_server.rs` being at least 8.
    - `pinned_server` registers exactly two tools, and a manual `tools/call` against the
      task-returning one is shown in the summary to produce a result carrying a related-task envelope
      — pasted verbatim. If the Phase-114 surface differs from what the plan assumed, the ACTUAL shape
      found is recorded.
    - The task-returning tool requires at least one NON-terminal poll before reaching terminal —
      stated in the summary with how it is arranged (a call counter, a deadline, or equivalent).
    - The module doc contains the phrase `different crate` (the reason this is not a re-export of
      `tests/common/v2.rs`).
    - `teardown`'s body performs drop → `abort()` → `await` in that order, and carries a comment
      containing `LEAK`.
    - `crates/pmcp-agent/tests/common/v2_server.rs` contains zero occurrences of `TODO`, `FIXME`,
      `XXX`.
  </acceptance_criteria>
  <done>
    `pmcp-agent`'s test tree can spawn a real v2-accepting server, a real v1-only server, and a
    guaranteed-unreachable endpoint, and can tear each down without leaking.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the three CLNT-03 end-to-end cases RED</name>
  <files>crates/pmcp-agent/tests/agent_v2_e2e.rs</files>
  <read_first>
    - `crates/pmcp-agent/tests/common/v2_server.rs` as written by Task 1
    - `crates/pmcp-agent/src/invoker/factory.rs` in full (185 lines) — especially `client_for`
      (`:125-146`, the unconditional `initialize` at `:141` that is the bug), the `ConnectorClient`
      trait (`:51-81`), the `UrlConnectorClient` impl (`:166-175`), and the full `InvokerError` enum
    - `crates/pmcp-agent/src/invoker/client.rs` in full (141 lines) — `ClientToolInvoker::dispatch`
      (`:65-83`) and its `wait_for_related_task` call with the hard `max_poll_duration_secs` cap
      (`:74-79`). This file needs NO era code; the tests must not require any
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q4.3 (the measured
      failure-signature table and the REACHABILITY classification rule) and § Q4.6 (the named seam
      `ConnectorClient::wait_for_related_task`, D-09's blast-radius bound)
    - `crates/pmcp-agent/tests/invoker_task_augmented.rs` (the established mock-`ConnectorClient`
      shape, for the parts that do not need a socket)
  </read_first>
  <behavior>
    - `agent_reaches_a_v2_server_end_to_end`: against `spawn_v2`, `client_for` returns a connector;
      `list_tools` returns BOTH pinned tools; `call_tool` on the plain tool succeeds. NO `initialize`
      request is sent — asserted from the SERVER side (the server observed zero `initialize` calls),
      not from a client-side log.
    - `agent_drives_task_polling_to_terminal_on_v2`: against `spawn_v2`, `call_tool` on the
      GUARANTEED task-returning tool yields a result carrying a related-task envelope; the test
      asserts, as four separate assertions and with NO conditional guarding any of them:
      (1) a task id was discovered from the result; (2) `wait_for_related_task` was driven with a
      bounded `WaitForTaskOptions`; (3) the SERVER observed at least one task-poll request, so the
      loop genuinely ran rather than short-circuiting on an immediately-terminal task; (4) the final
      result reached the harness's `pub` terminal-state constant within the cap. This is the CLNT-03
      "including task polling" clause and it is unconditional — an `if` here would let the phase ship
      with the criterion unproven.
    - `agent_falls_back_to_v1_when_the_server_answers_and_rejects_v2`: against `spawn_v1_only`,
      `client_for` returns a working connector; `list_tools` and `call_tool` succeed; the negotiated
      era is recorded as v1.
    - `an_unreachable_host_propagates_and_is_not_reported_as_era_v1`: against
      `closed_loopback_endpoint`, `client_for` returns `Err` — and the test asserts BOTH that it is an
      error AND that no connector was produced. It must NOT succeed by silently downgrading.
    - Every case completes within the bounded-wait constant; a hang is a failure.
  </behavior>
  <action>
    Create `crates/pmcp-agent/tests/agent_v2_e2e.rs`.

    First line: `#![cfg(all(feature = "url-connector", not(target_arch = "wasm32")))]`. Then
    `#[path = "common/v2_server.rs"] mod v2_server;`.

    Module doc (`//!`) states: this file is the CLNT-03 contract; it is written RED against the
    target `client_for` behavior before plan 117-07 implements it; the two NEGATIVE cases carry equal
    weight to the happy path because D-07 says fallback paths are where dual-version bugs hide; and
    the fallback rule under test classifies by REACHABILITY (the server ANSWERED, any HTTP response
    or JSON-RPC error ⇒ era rejection ⇒ fall back; the server did NOT answer, DNS/TCP/TLS/timeout ⇒
    infrastructure ⇒ propagate), never by string-matching an error message, because
    `"Unsupported protocol version"` is neither stable across implementations nor the only signature.

    Write the four tests named in `<behavior>`.

    `agent_drives_task_polling_to_terminal_on_v2` is a first-class case, not an appendix to the happy
    path. Assert its four facts separately so a failure says WHICH one broke, and instrument the poll
    count on the SERVER side (the same per-method request counter the no-`initialize` assertion uses)
    — a client-side "I called wait_for_related_task" assertion proves the call, not the polling.

    For the "no `initialize` on the wire" assertion, instrument the SERVER side: the pinned server
    counts requests by method (an `Arc<AtomicUsize>` incremented from a handler, or an HTTP
    middleware — read what `StreamableHttpServerConfig.http_middleware` accepts before choosing).
    A client-side assertion cannot prove absence.

    For the era assertion in the fallback case, read the negotiated era through whatever surface
    plan 117-07 will expose. Since that surface does not exist yet, define the EXPECTATION here as a
    named constant or a small trait-object accessor call and leave the test failing to COMPILE or
    failing to ASSERT with a message that names plan 117-07 as the implementer. Record in the summary
    exactly which of the two RED shapes each test takes (compile-failure vs assertion-failure), so
    117-07 knows what it must satisfy.

    Do NOT add any new method to `ConnectorClient` that names a `tasks/*` wire method. D-09 bounds the
    tasks coupling to exactly `ConnectorClient::wait_for_related_task` plus its one caller and one
    impl; route any task interaction through that method or through `CallToolResult::related_task()`.
    State this constraint in the module doc so a later contributor does not widen it.

    Do NOT implement any probe inside `pmcp::Client`. A-D08 is explicit: `src/client/mod.rs:871-878`
    carries a literal "do not 'restore' the latter" comment and `server_discover()` opens with
    `require_v2(...)` (`:892`) which fails LOCALLY on a v1 client. The probe belongs in
    `pmcp-agent`'s connector factory, which plan 117-07 owns.
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent --test agent_v2_e2e --features url-connector 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - The four test functions exist with the exact names given in `<behavior>`, including
      `agent_drives_task_polling_to_terminal_on_v2`.
    - `agent_drives_task_polling_to_terminal_on_v2` contains no `if` or `let ... else` guarding any of
      its four assertions — verified by reading the body and pasting it into the summary. A
      conditionally-skipped assertion does not discharge CLNT-03's "including task polling" clause.
    - The server-observed task-poll count is asserted to be at least 1.
    - The tests are RED for the DOCUMENTED reason: running
      `cargo test -p pmcp-agent --test agent_v2_e2e --features url-connector` produces either a
      compile error naming the not-yet-existing era accessor, or at least one assertion failure whose
      message names plan 117-07. The exact output is pasted in the summary. A test that passes at
      this point is a DEFECT — it means it asserts nothing.
    - `crates/pmcp-agent/tests/agent_v2_e2e.rs` line 1 is
      `#![cfg(all(feature = "url-connector", not(target_arch = "wasm32")))]`.
    - `grep -c '#\[path = "common/v2_server.rs"\]' crates/pmcp-agent/tests/agent_v2_e2e.rs` is 1.
    - `grep -c 'contains(' crates/pmcp-agent/tests/agent_v2_e2e.rs` applied to error-classification
      code is 0 — no test classifies a fallback by string-matching an error message.
    - The module doc contains the token `wait_for_related_task` (the D-09 named seam) and the phrase
      `do not "restore"` or an explicit A-D08 reference.
    - `cargo build -p pmcp-agent --target wasm32-unknown-unknown` exits 0 (the new test file is
      `cfg`-gated off wasm32 and must not disturb the wasm build that `ci.yml:374-377` gates).
    - `crates/pmcp-agent/tests/agent_v2_e2e.rs` contains zero occurrences of `TODO`, `FIXME`, `XXX`.
  </acceptance_criteria>
  <done>
    The CLNT-03 contract exists as three executable tests — v2 happy path, v1 fallback,
    unreachable-host propagation — all RED for a recorded, documented reason, with the D-09 tasks
    seam and the A-D08 no-probe-in-`Client` constraint written into the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent host → unknown-era MCP server | The host chooses which protocol to speak based on the server's answer |
| network failure → era decision | A partial outage must not be interpreted as a protocol signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-10 | Spoofing / Tampering | era selection in `client_for` | mitigate | `an_unreachable_host_propagates_and_is_not_reported_as_era_v1` is a first-class test. Pitfall 7: an agent that silently downgrades on a network failure reports "connected via v1" against a provably-v2 server |
| T-117-11 | Tampering | fallback classification | mitigate | Classification is by REACHABILITY, never by `err.to_string().contains("Unsupported protocol version")` — that message is neither stable across implementations nor the only signature (§ Q4.3 measured four distinct ones). Enforced by an acceptance criterion asserting zero string-match classification |
| T-117-14 | Repudiation | the CLNT-03 "including task polling" clause | mitigate | The harness GUARANTEES a task-associated result requiring at least one non-terminal poll, and `agent_drives_task_polling_to_terminal_on_v2` asserts task-id discovery, a server-observed poll count ≥ 1, and terminal state with NO conditional. A conditional "if a task came back" would let the criterion go unproven |
| T-117-12 | Denial of Service | test suite / server harness | mitigate | Every spawn is torn down drop→abort→await (D-113-T); every await is bounded by the harness's wait constant so a hung server fails rather than hangs |
| T-117-13 | Elevation of Privilege | endpoint scheme dispatch | mitigate | The existing `T-108-05-05` scheme policy at `factory.rs:131-136` (only `http`/`https`) must survive plan 117-07's edit; this plan pins the surrounding behavior so a regression there is visible |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages added; the harness uses `pmcp` and `tokio`, both already `[dev-dependencies]` of `pmcp-agent` |
</threat_model>

<verification>
- `cargo build -p pmcp-agent --tests --features url-connector` exits 0 (Task 1 harness compiles).
- `cargo test -p pmcp-agent --test agent_v2_e2e --features url-connector` is RED, and the exact
  output is recorded. GREEN at this point is a defect.
- `cargo build -p pmcp-agent --target wasm32-unknown-unknown` exits 0.
- `cargo test -p pmcp-agent` (default features) exits 0 — the new files are feature-gated off and
  must not disturb the existing suite.
- `make quality-gate` exits 0.
</verification>

<success_criteria>
- `crates/pmcp-agent/tests/common/v2_server.rs` can spawn a real v2-accepting server, a real v1-only
  server, and a guaranteed-unreachable endpoint, with leak-free teardown.
- `crates/pmcp-agent/tests/agent_v2_e2e.rs` holds four tests: v2 end-to-end (with the no-`initialize`
  assertion made from the SERVER side), UNCONDITIONAL task polling to terminal, v1 fallback, and
  unreachable-host propagation.
- All four are RED for a recorded reason that names plan 117-07.
- The D-09 tasks-coupling bound and the A-D08 no-probe-in-`Client` rule are written into the file.
- wasm32 and the existing default-feature `pmcp-agent` suite are undisturbed.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-04-SUMMARY.md` when done.
Record: the exact RED output of each of the four tests, which RED shape each takes (compile-failure
vs assertion-failure), the exact accessor/signature plan 117-07 must provide to turn them GREEN, the
verbatim related-task envelope the harness's task tool produces (and the ACTUAL Phase-114 shape found
if it differs from what was assumed), and the body of
`agent_drives_task_polling_to_terminal_on_v2` showing its four unconditional assertions.
</output>
