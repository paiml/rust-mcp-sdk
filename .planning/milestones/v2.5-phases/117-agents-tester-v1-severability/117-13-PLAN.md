---
phase: 117-agents-tester-v1-severability
plan: 13
type: execute
wave: 6
depends_on: ["117-12", "117-14"]
files_modified:
  - src/server/streamable_http_server.rs
  - src/server/streamable_http_server/v1_session.rs
  - src/server/streamable_http_server/v1_session_off.rs
  - tests/v2_verbs_405_on_severed_build.rs
  - docs/v1-sunset-policy.md
autonomous: true
requirements: [SMPL-01, SMPL-02]

must_haves:
  truths:
    - "`handle_get_sse` and `handle_delete_session` are SPLIT, not moved: the v2 405 rejection stays always-present and reachable on the `full-v2` build, and only the v1 body goes behind the gate — the router keeps GET and DELETE routed, they just always 405"
    - "`StreamableHttpServerConfig`'s four v1-only public fields are gated, or — if that proves too hot — the documented fallback is taken deliberately and recorded, never abandoned silently"
    - "The GET/DELETE 405 is proven by a test that RUNS under `--no-default-features --features full-v2`, not by a test that runs under `full` while `full-v2` is merely built — a runtime claim needs a runtime execution"
    - "Per-const gating of `src/shared/http_constants.rs` — `LAST_EVENT_ID` and the A4-gated `MCP_SESSION_ID` — is plan 117-14's subject, not this plan's; this plan VERIFIES the outcome and reports its absence as a finding"
    - "The two MIXED functions plan 117-12 deferred by name — `extract_session_and_protocol_headers` and `compute_outbound_protocol_version` — are actually handled here, so the handoff is not orphaned"
    - "SMPL-02 is closed: the `full-v2` build compiles the real transport with `-D warnings` and contains no session lifecycle, no session map, no event store and no `Last-Event-ID` reader — and the sunset policy names what is now gated"
  artifacts:
    - path: "src/server/streamable_http_server.rs"
      provides: "Thin always-present verb heads (the 405) + v1:: body calls; router unchanged"
      contains: "v2_verb_rejection"
    - path: "tests/v2_verbs_405_on_severed_build.rs"
      provides: "The GET/DELETE 405 assertion, EXECUTED under --no-default-features --features full-v2"
      contains: "405"
    - path: "docs/v1-sunset-policy.md"
      provides: "The policy updated to enumerate exactly what v1-compat now gates, post-cut, on BOTH the server and the client"
      contains: "v1_session"
  key_links:
    - from: "src/server/streamable_http_server.rs build_mcp_router"
      to: "handle_get_sse / handle_delete_session"
      via: "GET and DELETE stay routed on both feature sets; the null twins make them always 405"
      pattern: "build_mcp_router"
    - from: "tests/v2_verbs_405_on_severed_build.rs"
      to: "the full-v2 lib build"
      via: "cargo test --test v2_verbs_405_on_severed_build --no-default-features --features full-v2 — RUN, not merely built"
      pattern: "full-v2"
---

<objective>
Close the D-03 cut: split the two MIXED HTTP verb handlers so the v2 405 stays reachable, gate the
four v1-only config fields, gate the `LAST_EVENT_ID` constant together with both its readers, and
update the sunset policy to describe what is now actually gated.

Purpose: SMPL-02's final increment. `handle_get_sse` (`:4441-4503`) and `handle_delete_session`
(`:4505-4555`) each open with `v2_verb_rejection` and are otherwise wholly v1 — so they SPLIT, they
do not move. The router (`build_mcp_router`, `:293-302`) stays unchanged: GET and DELETE remain
routed on both feature sets, they just always 405 under `full-v2`. Gating the four public
`StreamableHttpServerConfig` fields is semver-safe only because `full-v2` is a brand-new feature no
published consumer builds with; a documented fallback exists if it proves painful.

Output: the split verbs, the gated config fields, the per-const gates, and a policy document that
matches the code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
The two MIXED verb handlers, both of which begin with the v2 rejection:
`handle_get_sse` (`:4441-4503`, 63 lines; `v2_verb_rejection` at `:4447`) and
`handle_delete_session` (`:4505-4555`, 51 lines; `v2_verb_rejection` at `:4510`).
`build_mcp_router` (`:293-302`) routes `post`, `get` and `delete` on `/` and is UNCHANGED.

`StreamableHttpServerConfig` (`:174-202`) — PUBLIC struct, all-public fields, NO
`#[non_exhaustive]`, NO builder. The four v1-only fields:
`session_id_generator: Option<Box<dyn Fn() -> String + Send + Sync>>` (`:176`),
`event_store: Option<Arc<InMemoryEventStore>>` (`:180`),
`on_session_initialized: Option<SessionCallback>` (`:182`),
`on_session_closed: Option<SessionCallback>` (`:184`).
SHARED fields that stay: `enable_json_response` (`:178`), `http_middleware` (`:186`),
`allowed_origins` (`:196`), `max_request_bytes` (`:201`).
In-crate sites needing a matching `#[cfg]`: `Default` (`:222-235`), `stateless()` (`:250-261`),
`Debug` (`:204-220`), and the two rustdoc doctests (`:145-154`, `:157-172`).
`src/server/preset.rs:35-38,247-250` and `src/server/mod.rs:768-771,4773-4776` use
`..Default::default()` and do NOT break. ~29 struct-literal sites in `tests/` and `examples/` do not
break either, because the severance build is lib-only (`pmcp` declares no `[[bin]]`).

DOCUMENTED FALLBACK if the field gating proves too hot: keep the four config fields present in BOTH
builds and gate only the machinery. SMPL-02's wording is arguably still met — a store you can
configure but that is never read or written is inert data, not a code path — but it is weaker. Take
the fallback DELIBERATELY and record it; do not abandon the cut.

`src/shared/http_constants.rs` — module doc at `:4-8` says it is "deliberately UNGATED". Gate the
CONSTS, not the module. v1-only: `MCP_SESSION_ID` (`:12`), `LAST_EVENT_ID` (`:34`). v2-REQUIRED:
`MCP_METHOD` (`:23`), `MCP_NAME` (`:31`). Shared: `MCP_PROTOCOL_VERSION`, `ACCEPT`, `CONTENT_TYPE`,
`APPLICATION_JSON`, `TEXT_EVENT_STREAM`, `ACCEPT_STREAMABLE`, `DEFAULT_HTTP_SSE_BUFFERED_BYTES`.

`LAST_EVENT_ID` readers: server at `streamable_http_server.rs:4384` (moved to `v1_session.rs` by plan
117-12) and client at `src/shared/streamable_http.rs:639`. BOTH must be gated together.

ASSUMPTION A4, UNMEASURED: `MCP_SESSION_ID`'s read at `streamable_http_server.rs:3629` (the
middleware path) is claimed v1-reachable only. Gating the const without verifying `:3620-3640` would
break the v2 middleware build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the two MIXED verb handlers so the v2 405 stays always-present</name>
  <files>src/server/streamable_http_server.rs, src/server/streamable_http_server/v1_session.rs, src/server/streamable_http_server/v1_session_off.rs</files>
  <read_first>
    - `src/server/streamable_http_server.rs` lines 4435-4560 — `handle_get_sse` and
      `handle_delete_session` in full, including the `v2_verb_rejection` call that opens each
    - `src/server/streamable_http_server.rs` lines 1600-1650 (`v2_method_not_allowed`,
      `v2_verb_rejection`) — the 405 machinery that must remain ALWAYS COMPILED
    - `src/server/streamable_http_server.rs` lines 290-305 (`build_mcp_router`) — confirm for
      yourself it needs no change
    - `src/server/streamable_http_server/v1_session.rs` and `v1_session_off.rs` as edited by plan
      117-12 (the SSE and session helpers these bodies call already live there)
    - `tests/v1_byte_identity_after_cut.rs` as written by plan 117-02 — the v1 GET and DELETE goldens
      these must keep producing
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q2.5 Step 3
  </read_first>
  <action>
    Split each handler into a thin always-present head plus a gated body:

    `handle_get_sse` keeps its `v2_verb_rejection` check verbatim as the first statement, then
    delegates the remainder to `v1::handle_get_sse_body(...)`. `handle_delete_session` does the same
    with `v1::handle_delete_body(...)`. Move ONLY the post-rejection body into `v1_session.rs`.

    In `v1_session_off.rs`, both twins return an unconditional 405 using the SAME 405 construction the
    v2 rejection path uses — do not hand-roll a second response shape. If `v2_method_not_allowed` is
    not reachable from the twin's scope, expose it rather than duplicating it; a second 405
    constructor is a second answer that can drift.

    `build_mcp_router` (`:293-302`) is UNCHANGED. GET and DELETE stay routed on both feature sets.
    That is deliberate: an unrouted verb returns a different status than a routed-and-405'd one, and
    changing it would alter the v2 wire.

    Keep both heads at or under cognitive complexity 25. The split should REDUCE complexity in this
    file, not concentrate it; record the before/after PMAT numbers for both functions specifically.

    ALSO close plan 117-12's deferred handoff. 117-12 Task 1 says
    `extract_session_and_protocol_headers` (`:2174-2188`) and `compute_outbound_protocol_version`
    (`:2285-2312`) "are plan 117-13's subject" — this task is where that debt is paid, and no other
    task claims them. Handle both:
    - `extract_session_and_protocol_headers` also reads `MCP_PROTOCOL_VERSION`, which v2 needs, so it
      STAYS in `streamable_http_server.rs`. Route only its `MCP_SESSION_ID` read through a `v1::`
      function whose twin returns `None`.
    - `compute_outbound_protocol_version`'s `state.sessions.read()` branch (`:2303-2310`) is the last
      surviving raw session read. Express it as a `v1::` function returning an owned
      `Option<String>` (the negotiated version for that session), whose twin returns `None`. Do NOT
      add a raw accessor — plan 117-09 Task 1 records why a reference-to-collection accessor is
      unimplementable on the ZST twin.
    If either turns out to need no change, say so explicitly with the reason; silence is not an
    acceptable outcome for a named handoff.

    THEN clean up the twins whose only caller just moved. After the GET body moves into
    `v1_session.rs`, the `full-v2` build has no caller for `resolve_sse_session`,
    `replay_sse_events_from_header`, `sse_event_for_message` or `attach_sse_response_headers` — plan
    117-12 Task 2 required those twins because `handle_get_sse` still lived in the shared file at that
    point. Under `RUSTFLAGS="-D warnings"` an uncalled `pub(crate) fn` is a `dead_code` error, so
    DELETE the now-unreachable twins from `v1_session_off.rs` rather than adding an `allow`. Record
    exactly which twins were removed and confirm plan 117-06 Task 3's
    `the_v1_null_twin_declares_nothing_the_real_module_does_not` still passes (removing declarations
    can only help it).

    FINALLY, prove the 405 by EXECUTION on the severed build — see Task 2. A test that runs under
    `--features full` exercises the v1 implementation and says nothing about `full-v2`.
  </action>
  <verify>
    <automated>cargo build -p pmcp --features full &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 &amp;&amp; cargo test --lib --features "full" &amp;&amp; cargo test --test v1_byte_identity_after_cut --features "full"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff src/server/streamable_http_server.rs` shows `build_mcp_router` UNCHANGED.
    - `handle_get_sse` and `handle_delete_session` both still exist in
      `src/server/streamable_http_server.rs` (they were SPLIT, not moved) and each still calls
      `v2_verb_rejection` as its first statement.
    - `handle_get_sse_body` and `handle_delete_body` (or the chosen equivalent names) appear in BOTH
      `v1_session.rs` and `v1_session_off.rs`.
    - `grep -c '#\[cfg(feature = "v1-compat")\]' src/server/streamable_http_server.rs` is 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with zero `warning:` lines.
    - `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0 — the v1 GET and
      DELETE goldens still match.
    - `handle_get_sse` and `handle_delete_session` both still exist and the router is unchanged (see
      above); the 405 EXECUTION proof is Task 2's acceptance criterion, not an inference from this
      task's `--features full` test run.
    - Plan 117-12's two deferred functions are both accounted for: the summary states, for
      `extract_session_and_protocol_headers` and `compute_outbound_protocol_version` separately, what
      was done or why nothing was needed.
    - `git diff src/server/streamable_http_server/v1_session_off.rs` shows the now-uncalled SSE twins
      REMOVED, and `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`
      exits 0 with zero `warning:` lines (proving no `dead_code` was left behind).
    - `grep -c 'allow(dead_code)' src/server/streamable_http_server/v1_session_off.rs` is 0.
    - `cargo test --lib --features "full"` pass count equals the pre-plan count EXACTLY.
    - PMAT cognitive complexity for `handle_get_sse` and `handle_delete_session` is recorded before
      and after, and neither exceeds 25.
  </acceptance_criteria>
  <done>
    The two verbs are split with the 405 always compiled, the router is untouched, plan 117-12's two
    deferred functions are resolved, the now-unreachable SSE twins are deleted so `-D warnings` stays
    clean, and the v1 goldens are green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EXECUTE the GET/DELETE 405 assertion on the severed `full-v2` build</name>
  <files>tests/v2_verbs_405_on_severed_build.rs</files>
  <read_first>
    - `src/server/streamable_http_server.rs` as edited by Task 1 — the split `handle_get_sse` /
      `handle_delete_session` heads and `v2_method_not_allowed` / `v2_verb_rejection` (`:1600-1650`)
    - `src/server/streamable_http_server/v1_session_off.rs` as edited by Task 1 — the two 405 twins
    - `tests/common/v2.rs` lines 383-400 (`spawn_default_config`, `spawn_stateless_config`,
      `spawn_with`), `:528-532` (`teardown`), `:835-850` (`get`, `delete`). NOTE: `spawn_default_config`
      sets `session_id_generator`, which Task 3 GATES — so it may not compile on `full-v2`. MEASURE
      whether `tests/common/v2.rs` compiles under `--no-default-features --features full-v2` BEFORE
      depending on it
    - `src/server/streamable_http_server.rs` lines 174-262 (`StreamableHttpServerConfig`, its
      `Default`, and `stateless()`) — the construction paths that still compile once Task 2 lands
    - `Cargo.toml` `[features]` as edited by plan 117-01 — confirm `full-v2` carries `streamable-http`
      and `http-client`, which this test needs on both sides of the socket
  </read_first>
  <behavior>
    - On a server built from the `full-v2` lib, an HTTP `GET /` returns status `405` — asserted as the
      exact numeric status, never as "is an error" and never as "is not 200".
    - On the same server, an HTTP `DELETE /` returns status `405`.
    - Neither verb returns `404` (which is what an UNROUTED verb would return) and neither panics —
      the test distinguishes "routed and refused" from "not routed", because those are different wire
      answers and only the former is the v2 contract.
    - A `POST /` on the same server still succeeds, so the assertion is not passing because the server
      failed to start.
  </behavior>
  <action>
    Create `tests/v2_verbs_405_on_severed_build.rs`.

    Gate the whole file with
    `#![cfg(all(feature = "streamable-http", feature = "http-client", not(feature = "v1-compat"), not(target_arch = "wasm32")))]`.
    The `not(feature = "v1-compat")` predicate is what makes this file the SEVERED-build test: it is a
    `cfg` predicate inside one crate's own test compilation, NOT a negative cargo feature, so it does
    not touch the D-02 additivity rule that rejected a `v2-only` feature. Write that distinction into
    the module doc so nobody "fixes" it later.

    Module doc must also state the defect this file exists to close: a 405 asserted under
    `--features full` exercises the v1 implementation and proves nothing about the severed build; the
    only proof of a RUNTIME answer on `full-v2` is a test that RUNS on `full-v2`.

    First MEASURE whether `tests/common/v2.rs` compiles under
    `--no-default-features --features full-v2` once Task 3's config gating has landed. Record the
    command and its result.
    - If it DOES compile: use `mod common;` and its `spawn_with` / `get` / `delete` / `post` /
      `teardown` helpers, constructing the config with `..Default::default()` so no gated field is
      named.
    - If it does NOT: keep this file self-contained — spawn the server directly and drive the three
      verbs with the crate's own HTTP client. Do NOT edit `tests/common/v2.rs` to make it fit; that
      harness serves the v1 goldens and must stay as plan 117-02 captured it.
    Either way, do NOT use `spawn_default_config` — it names `session_id_generator`.

    Write the four assertions in `<behavior>`. Assert the numeric status with an equality comparison
    against `405` and, separately, assert it is not `404`, with a message explaining that a `404`
    means `build_mcp_router` stopped routing the verb, which is a DIFFERENT wire answer and a
    regression of Task 1's "router unchanged" requirement.

    Bound every await with an explicit timeout constant whose doc says a hung server must FAIL the
    test, not hang it. Tear down every spawned server drop → abort → await.

    Add the same test file to nothing else — it is intentionally invisible on the `full` build, where
    the in-file v1 tests already cover the v1 answers.
  </action>
  <verify>
    <automated>cargo test --test v2_verbs_405_on_severed_build --no-default-features --features full-v2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test v2_verbs_405_on_severed_build --no-default-features --features full-v2`
      exits 0, reports at least 3 tests and `0 failed`. The full command line and the test-count line
      are pasted into the summary — a run reporting `0 tests` is a FAILURE, not a pass, because the
      whole-file `cfg` would have compiled it out.
    - `cargo test --test v2_verbs_405_on_severed_build --features full` reports `0 tests` (the file is
      correctly inert on the v1-carrying build) — recorded, and NOT counted as evidence of anything.
    - `grep -c 'not(feature = "v1-compat")' tests/v2_verbs_405_on_severed_build.rs` is at least 1 and
      it appears in the file-level `#![cfg(...)]` on line 1.
    - `grep -c 'spawn_default_config' tests/v2_verbs_405_on_severed_build.rs` is 0.
    - `git diff --stat tests/common/v2.rs` is empty.
    - The GET and DELETE assertions each compare against the numeric `405` AND separately assert the
      status is not `404`, with the "router stopped routing the verb" message.
    - The POST control assertion is present and passes, so a failed server start cannot masquerade as
      a passing 405.
    - Negative control, executed, recorded, reverted: change one twin to return `404` instead of
      `405`; the corresponding test MUST fail naming both the observed and expected status.
    - The measurement of whether `tests/common/v2.rs` compiles under `full-v2` is recorded verbatim
      with its command, along with which of the two routes was taken.
    - `tests/v2_verbs_405_on_severed_build.rs` contains zero occurrences of `TODO`, `FIXME`, `XXX`.
  </acceptance_criteria>
  <done>
    The `full-v2` GET/DELETE 405 is proven by a test that actually RAN on the severed build, with a
    non-zero test count recorded, a 404-vs-405 discrimination, a POST control against a dead server,
    and an executed negative control.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate the four v1-only config fields, then close the sunset policy</name>
  <files>src/server/streamable_http_server.rs</files>
  <read_first>
    - `src/server/streamable_http_server.rs` lines 140-262 in full —
      `StreamableHttpServerConfig` (`:174-202`) with per-field docs, its `Debug` impl (`:204-220`),
      its `Default` impl (`:222-235`), `stateless()` (`:250-261`), and the two rustdoc doctests at
      `:145-154` and `:157-172`
    - `src/server/preset.rs` lines 30-45 and 240-260, and `src/server/mod.rs` lines 760-780 and
      4765-4785 — the `..Default::default()` users
    - `grep -rn 'StreamableHttpServerConfig {' --include='*.rs' src/ tests/ examples/ crates/ cargo-pmcp/`
      — the ~29 struct-literal sites the research says do NOT break because the severance build is
      lib-only. VERIFY that: confirm `pmcp` declares no `[[bin]]` with
      `grep -c '^\[\[bin\]\]' Cargo.toml`
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q2.5 Step 4, including
      the documented fallback and assumption A7
  </read_first>
  <action>
    Apply `#[cfg(feature = "v1-compat")]` to the four v1-only `StreamableHttpServerConfig` fields:
    `session_id_generator`, `event_store`, `on_session_initialized`, `on_session_closed`. Leave
    `enable_json_response`, `http_middleware`, `allowed_origins` and `max_request_bytes` ungated.

    Apply matching `#[cfg]` to the corresponding lines in the `Debug` impl (`:204-220`), the `Default`
    impl (`:222-235`) and `stateless()` (`:250-261`). The two rustdoc doctests at `:145-154` and
    `:157-172` must either become `#[cfg]`-aware or be adjusted so `make doc-check` stays clean under
    the feature list plan 117-01 established; read them and decide, and record which.

    Add a note in the config struct's doc comment stating that gating these four fields is semver-safe
    ONLY because `full-v2` is a brand-new feature no published consumer builds with (assumption A7),
    and that if `full-v2` ever enters a published crate's default set this becomes a break.

    IF the gating proves painful — measured by an in-crate compile failure the `#[cfg]` propagation
    cannot resolve cleanly, not by it merely being tedious — take the DOCUMENTED FALLBACK instead:
    keep the four fields present in both builds and gate only the machinery. Then:
    - record the decision, the specific failure that triggered it, and what was tried;
    - record that SMPL-02's wording is still met but WEAKER (a store you can configure but that is
      never read or written is inert data, not a code path);
    - note in `docs/v1-sunset-policy.md` that the four config fields remain present on `full-v2`.
    Degrading to the fallback is acceptable. Abandoning the cut is not.

    Do NOT edit any `tests/` or `examples/` struct-literal site to accommodate this. The severance
    build is lib-only, so they are unaffected — and if one DOES break, that is a finding meaning the
    build is not lib-only after all; report it rather than editing 29 files.

    THEN CLOSE THE PHASE by updating `docs/v1-sunset-policy.md` to describe what `v1-compat` NOW
    actually gates, post-cut, on BOTH sides of the wire. Replace any forward-looking wording with the
    enumerated reality:
    - SERVER: `src/server/streamable_http_server/v1_session.rs` (session lifecycle, SSE resumability,
      the transport-local event store), `src/shared/event_store.rs` (whole file), the v1 GET/DELETE
      bodies, and — if this task gated them — the four `StreamableHttpServerConfig` fields.
    - CLIENT: everything plan 117-14's SUMMARY enumerates — the `StreamableHttpTransportConfig`
      session and resumption fields, the stored session id, the `Last-Event-ID` writer, the DELETE
      session teardown, `Client::initialize` if 117-14 gated it, and anything 117-14 recorded as
      deliberately left ungated.
    - CONSTANTS: `LAST_EVENT_ID`, and `MCP_SESSION_ID` only if plan 117-14's A4 measurement held.

    Read plan 117-14's summary for that inventory rather than re-deriving it; if its HANDOFF section
    is missing, that is a finding — say so rather than guessing. VERIFY the constants outcome directly
    with `grep -B2 'LAST_EVENT_ID\|MCP_SESSION_ID' src/shared/http_constants.rs` and report a missing
    gate as a finding rather than silently applying it here.

    If this task took the config-field fallback, say so plainly in the policy. If 117-14 left an item
    ungated, the policy must NAME it. The policy must MATCH the code; a policy that overstates the
    severance is worse than one that understates it, because a consumer plans a 3.0 migration against
    it.

    Finally, run the full closing evidence set and record every result:
    `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`;
    `cargo test --test v2_verbs_405_on_severed_build --no-default-features --features full-v2`;
    `cargo test --test v1_severability_tripwire`; `cargo test --test v1_byte_identity_after_cut
    --features "full"`; `cargo test --lib --features "full"`; `make doc-check`; `make quality-gate`.
    Report the severance build and the severed-build 405 run SEPARATELY from `make quality-gate` —
    the latter runs `--all-features` (`Makefile:135`), which enables `v1-compat` AND `full-v2`
    together and can never prove severance.
  </action>
  <verify>
    <automated>cargo build -p pmcp --features full &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 &amp;&amp; cargo test --lib --features "full" &amp;&amp; cargo test --test v2_verbs_405_on_severed_build --no-default-features --features full-v2 &amp;&amp; cargo test --test v1_severability_tripwire &amp;&amp; make doc-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[\[bin\]\]' Cargo.toml` is 0, recorded — this is what makes the severance build
      lib-only and the 29 struct-literal sites irrelevant.
    - EITHER: the four named fields each carry `#[cfg(feature = "v1-compat")]` and the `Debug`,
      `Default` and `stateless()` sites carry matching attributes — OR the fallback was taken, with
      the triggering failure recorded verbatim.
    - `cargo build -p pmcp --features full` exits 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with zero `warning:` lines.
    - `cargo test --lib --features "full"` pass count equals the pre-plan count EXACTLY.
    - `git diff --stat tests/ examples/` is empty — no struct-literal site was edited.
    - `git diff --stat src/server/preset.rs src/server/axum_router.rs` is empty.
    - `make doc-check` exits 0, including the two config doctests.
    - The config struct doc contains the A7 semver note.
    - `grep -B2 'LAST_EVENT_ID' src/shared/http_constants.rs` shows plan 117-14's
      `#[cfg(feature = "v1-compat")]` present; its absence is reported as a finding, not fixed here.
    - `git diff --stat src/shared/http_constants.rs` is EMPTY for this plan — per-const gating is
      plan 117-14's, and this plan only verifies it.
    - `docs/v1-sunset-policy.md` enumerates the gated surface on BOTH server and client and matches
      the code — verified by cross-reading in BOTH directions: every path the policy names is actually
      gated, and every gated file (server AND client) is named. The cross-read table is pasted into
      the summary.
    - The policy names every item plan 117-14 recorded as deliberately NOT severed, if any, and says
      whether this task gated the config fields or took the fallback.
    - `cargo test --test v2_verbs_405_on_severed_build --no-default-features --features full-v2`
      exits 0 with a NON-ZERO test count.
    - `cargo test --test v1_severability_tripwire` and
      `cargo test --test v1_byte_identity_after_cut --features "full"` both exit 0.
    - `cargo build -p pmcp --no-default-features --features "streamable-http"` exits 0.
    - The summary records the full closing evidence set with the severance build and the severed-build
      405 run reported SEPARATELY from `make quality-gate`.
  </acceptance_criteria>
  <done>
    The four v1-only config fields are gated (or the fallback is taken deliberately with its trigger
    recorded), no test or example was edited, the rustdoc gate is clean, and
    `docs/v1-sunset-policy.md` enumerates exactly the gated surface on both server and client —
    cross-read in both directions against the code, with the full closing evidence set recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → HTTP transport (GET / DELETE) | v2 must answer 405; an unrouted verb answers differently |
| public config surface → downstream crates | Gating public fields changes the API under `full-v2` |
| documentation → user expectation | A policy overstating the severance misleads a consumer planning a 3.0 migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-46 | Tampering | v2 verb answers | mitigate | The verbs are SPLIT not moved: `v2_verb_rejection` stays always-compiled and `build_mcp_router` is unchanged. Asserted by `tests/v2_verbs_405_on_severed_build.rs`, which RUNS under `--no-default-features --features full-v2` with a non-zero test count, compares the numeric `405`, and separately rejects `404` (the unrouted answer). A test running under `--features full` would exercise the v1 implementation and prove nothing |
| T-117-47 | Denial of Service (build) | `StreamableHttpServerConfig` public fields | mitigate | Semver-safe only because `full-v2` is new (A7), recorded in the struct doc. A documented fallback exists and is taken deliberately with its trigger recorded, rather than the cut being abandoned or 29 files being edited |
| T-117-48 | Denial of Service (build) | per-const gating in `http_constants.rs` | transfer | Owned entirely by plan 117-14 (`LAST_EVENT_ID` + its client reader in one edit, and the A4-gated `MCP_SESSION_ID`). This plan VERIFIES the attributes are present, keeps `git diff --stat src/shared/http_constants.rs` empty, reports absence as a finding, and still runs `--no-default-features --features "streamable-http"` to prove the client transport compiles with `v1-compat` off |
| T-117-49 | Information Disclosure | `MCP_SESSION_ID` middleware read (`:3629`) | transfer | Assumption A4 is MEASURED in plan 117-14, which owns `http_constants.rs`; a false A4 leaves the const ungated with a recorded reason rather than breaking the v2 middleware build. This plan's policy update reports whichever outcome 117-14 recorded |
| T-117-50 | Repudiation | the sunset policy's severance claim | mitigate | The policy is cross-read against the code in BOTH directions and covers server AND client, including anything plan 117-14 recorded as not severable. Understating is acceptable; overstating is not |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages added |
</threat_model>

<verification>
- `cargo build -p pmcp --features full` exits 0.
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0 with
  zero warnings — reported SEPARATELY from `make quality-gate`, which runs `--all-features` and can
  never prove severance.
- `cargo build -p pmcp --no-default-features --features "streamable-http"` exits 0.
- `cargo test --lib --features "full"` pass count is EXACTLY the pre-plan count.
- `cargo test --test v1_severability_tripwire` and `cargo test --test v1_byte_identity_after_cut
  --features "full"` both exit 0.
- `cargo test --test v2_verbs_405_on_severed_build --no-default-features --features full-v2` exits 0
  with a NON-ZERO test count — this is the ONLY evidence accepted for the runtime 405 claim.
- `make doc-check` exits 0.
- `make quality-gate` exits 0.
- PMAT complexity for `handle_get_sse` and `handle_delete_session` before/after, neither over 25.
- `git diff --stat tests/ examples/ src/server/preset.rs src/server/axum_router.rs` is empty.
</verification>

<success_criteria>
- GET and DELETE are split with the 405 always compiled; the router is unchanged; and the 405 is
  proven by a test EXECUTED under `--no-default-features --features full-v2` with a non-zero test
  count, discriminating 405 from 404.
- Plan 117-12's two deferred functions are resolved, and the twins whose caller moved are deleted so
  `-D warnings` stays clean.
- The four v1-only config fields are gated, or the documented fallback is taken deliberately with its
  triggering failure recorded.
- Per-const gating in `http_constants.rs` is plan 117-14's; this plan verifies it is present and
  leaves that file untouched.
- `docs/v1-sunset-policy.md` enumerates exactly the gated surface on BOTH server and client and is
  cross-read against the code in both directions.
- The full closing evidence set is recorded, with the severance build reported separately.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-13-SUMMARY.md` when done.
Record: the `[[bin]]` count; whether the config fields were gated or the fallback taken and why; the
resolution of plan 117-12's two deferred functions; which null twins were deleted as newly
unreachable; whether `tests/common/v2.rs` compiles under `full-v2` and which route Task 1b took; the
severed-build 405 run's full command line and test count; before/after PMAT complexity for both verb handlers; the two-direction
policy-vs-code cross-read table covering server AND client; and the full closing evidence set with
the severance build and the severed-build 405 run reported separately from `make quality-gate`.
</output>
