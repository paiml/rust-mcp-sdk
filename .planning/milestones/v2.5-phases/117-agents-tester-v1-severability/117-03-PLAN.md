---
phase: 117-agents-tester-v1-severability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mcp-tester/tests/report_compat.rs
autonomous: true
requirements: [CLNT-04]

must_haves:
  truths:
    - "The single-run report output of mcp-tester 0.7.0 AS IT STANDS TODAY is pinned before any dual-run code exists, so the D-11/A-D11 additivity claim is proven by a test rather than asserted"
    - "The `--format json` contract — the serde shape of `TestReport` — is pinned byte-for-byte after width-preserving substitution of the two genuinely per-run values (`duration`, `timestamp`)"
    - "The `--format pretty` contract is pinned by an achievable criterion: byte-identity on a deliberately-pinned single-category, zero-duration, ANSI-disabled fixture, plus an ORDER-INSENSITIVE structural assertion on a multi-category fixture — because HashMap grouping, conditional ANSI and a >100ms conditional duration column make a naive multi-category byte golden impossible"
    - "Assumption A2 is re-measured, not inherited: whether `TestReport` is struct-literal-constructed anywhere outside `crates/mcp-tester/` decides what plan 117-11 is allowed to add"
    - "A-CI is recorded: no CI job runs mcp-tester against a live server, so CI is NOT a consumer of the report shape — the strict consumers are six in-repo library linkers, i.e. the Rust compiler"
  artifacts:
    - path: "crates/mcp-tester/tests/report_compat.rs"
      provides: "Golden single-run stdout for json (byte) and pretty (byte on pinned fixture + structural on multi-category), captured against 0.7.0"
      min_lines: 200
      contains: "print_to_writer"
  key_links:
    - from: "crates/mcp-tester/tests/report_compat.rs"
      to: "crates/mcp-tester/src/report.rs print_to_writer"
      via: "capture into Vec<u8> via the writer seam added for exactly this purpose (report.rs:238-255)"
      pattern: "print_to_writer"
    - from: "crates/mcp-tester/tests/report_compat.rs"
      to: "cargo-pmcp/src/commands/test/apps.rs:874-878"
      via: "recorded A2 grep result deciding whether TestReport may gain a field"
      pattern: "TestResult"
---

<objective>
Pin `mcp-tester`'s single-run report output as it stands at 0.7.0, before any dual-run code exists.
This is the D-11 / A-D11 additivity PROOF: plan 117-11 adds `--dual-run`, and this file is what
makes "single-run output is unchanged" a fact instead of a claim.

Purpose: A-D11 resolved D-11 to ADDITIVE because `cargo-pmcp` links `mcp-tester` as a LIBRARY —
it struct-literals `TestResult` (`cargo-pmcp/src/commands/test/apps.rs:874-878`) and matches
`TestCategory` exhaustively with no `_` arm (`.../conformance.rs:276-289`). The strict consumer is a
Rust compiler, not a JSON parser. This plan captures the baseline that additivity is measured
against and re-measures the one assumption (A2) that decides how far 117-11 may go.

Output: `crates/mcp-tester/tests/report_compat.rs`, green against the unmodified crate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
<!-- crates/mcp-tester/src/report.rs — the shapes being pinned. -->
pub struct TestResult {                      // :73-81 — 6 pub fields, NO #[non_exhaustive], NO builder
    pub name: String,
    pub category: TestCategory,
    pub status: TestStatus,
    pub duration: Duration,
    pub error: Option<String>,
    pub details: Option<String>,
}

pub struct TestReport {                      // :152-158
    pub tests: Vec<TestResult>,
    pub duration: Duration,                  // PER-RUN
    pub timestamp: DateTime<Utc>,            // PER-RUN
    pub summary: TestSummary,
}

pub struct TestSummary { pub total: usize, pub passed: usize, pub failed: usize, pub warnings: usize, pub skipped: usize }

impl TestReport {
    pub fn new() -> Self;                    // :186
    pub fn from_error(error: anyhow::Error) -> Self;  // :191-203, uses Duration::from_secs(0)
    pub fn add_test(&mut self, test: TestResult);
    /// Writer seam added by Phase 78 Plan 04 SO TESTS CAN ASSERT PRINTED BYTES.
    pub fn print_to_writer<W: Write>(&self, format: OutputFormat, w: &mut W) -> std::io::Result<()>; // :238-255
}

pub enum OutputFormat { Pretty, Json, Minimal, Verbose }   // :38-44
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the `--format json` contract byte-for-byte with width-preserving normalization</name>
  <files>crates/mcp-tester/tests/report_compat.rs</files>
  <read_first>
    - `crates/mcp-tester/src/report.rs` in full — especially `TestResult` (`:73-81`), `TestCategory`
      (`:55-71`), `TestStatus`, `TestReport` (`:152-158`), `TestReport::new`/`from_error`/`add_test`
      (`:186-210`), `print_to_writer` (`:238-255`), and `print_json` (`:460-463`, which emits
      `serde_json::to_string_pretty(&TestReport)` verbatim — so the binary's JSON contract IS the
      serde shape of `TestReport`)
    - `crates/mcp-tester/src/post_deploy_report.rs` lines 1-120 (the module doc that already argues
      the A-D11 case: "Additive field changes (new optional fields with `#[serde(default)]`) do NOT
      bump the version" and "Why a new struct (vs. extending `TestReport`)")
    - `tests/v1_lists_golden.rs` lines 97-127 (the `DynamicField` width-preserving substitution
      machinery) and `:219-283` (`assert_v1_bytes`'s four-step assertion) — restate, do not import;
      a Rust integration test is its own crate
    - `crates/mcp-tester/Cargo.toml` lines 15-45 (existing deps and dev-deps; note there is NO `toml`
      dependency and `serde_yaml = "0.9"` IS present)
  </read_first>
  <behavior>
    - A `TestReport` built from a fixed, deterministic fixture and rendered through
      `print_to_writer(OutputFormat::Json, &mut Vec::<u8>::new())` produces bytes that match a pinned
      literal after ONLY `duration` and `timestamp` values are placeholder-substituted.
    - The substitution is width-preserving: the substituted string has the same byte length as the
      raw one, and each substituted key occurs the same number of times before and after.
    - A test asserting the fixture is not empty: the pinned report has at least 3 `tests` entries and
      a non-zero `summary.total`, so the byte comparison is not passing over an empty array.
  </behavior>
  <action>
    Create `crates/mcp-tester/tests/report_compat.rs`.

    Module doc (`//!`) must state: these literals were captured against mcp-tester 0.7.0 BEFORE the
    Phase-117 `--dual-run` work; they are the D-11 / A-D11 ADDITIVITY PROOF; a diff here means the
    single-run output changed, which A-D11 forbids; and the correct remedy is to move the change
    into the opt-in `--dual-run` path or into a new top-level struct, NOT to re-record the golden.
    Cite `crates/mcp-tester/src/post_deploy_report.rs`'s header as the in-repo precedent for the
    "new top-level struct" escape hatch.

    Build the fixture with a deterministic constructor helper. Every `TestResult` in it uses
    `Duration::from_secs(0)` — the same choice `TestReport::from_error` (`report.rs:191-203`) already
    makes — so nothing downstream depends on a real elapsed time. Set `TestReport.duration` to
    `Duration::from_secs(0)` explicitly and leave `timestamp` as whatever `TestReport::new()`
    produces (it is `Utc::now()`); `timestamp` is then handled by the normalizer, not by the fixture.

    Restate `DynamicField`, `substitute` and `key_occurrences` from `tests/v1_lists_golden.rs:97-127`
    and add a comment saying they are restated rather than imported because a Rust integration test
    is its own crate. Declare dynamics for the `timestamp` key. `duration` serializes as a serde
    `Duration` (a struct with `secs`/`nanos`, not a string) — READ what `serde_json::to_string_pretty`
    actually emits for it before writing the dynamic, and if it is already fully deterministic at
    `Duration::from_secs(0)` then it needs no dynamic at all; record which it turned out to be in the
    summary.

    Write the assertion in the four-step `assert_v1_bytes` shape: same-width substitution → length
    equality → per-key occurrence equality → canonical substitution → `assert_eq!` against the
    literal, with a failure message naming the ADDITIVITY BREAK and the correct remedy.

    Add the anti-vacuity test named in `<behavior>` using the `FAILURE MODE: … / WHAT TO DO: …`
    two-line message form from `tests/phase115_contract_bindings.rs:701-712`.
  </action>
  <verify>
    <automated>cargo test -p mcp-tester --test report_compat</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mcp-tester --test report_compat` exits 0 with a non-zero test count and
      `0 failed`.
    - `grep -c 'print_to_writer' crates/mcp-tester/tests/report_compat.rs` is at least 1.
    - The file contains a length-equality assertion whose message contains `width-preserving`.
    - The file contains an anti-vacuity test whose message contains `FAILURE MODE`.
    - The module doc contains the token `post_deploy_report` (the cited precedent) and the phrase
      `do NOT re-record`.
    - Negative control, executed and recorded verbatim, then reverted: add a dummy field to the
      fixture's serialized output (e.g. change one `TestResult.name`) and confirm the byte assertion
      fails with the additivity-break message. Restore and re-run to green.
    - `crates/mcp-tester/tests/report_compat.rs` contains zero occurrences of `TODO`, `FIXME`,
      `XXX`.
  </acceptance_criteria>
  <done>
    The `--format json` single-run contract is pinned byte-for-byte against 0.7.0 with proven
    width-preserving normalization and an anti-vacuity guard.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the `--format pretty` contract with an achievable criterion, and re-measure assumption A2</name>
  <files>crates/mcp-tester/tests/report_compat.rs</files>
  <read_first>
    - `crates/mcp-tester/tests/report_compat.rs` as written by Task 1 (current state)
    - `crates/mcp-tester/src/report.rs` lines 255-330 — the three MEASURED non-determinism sources:
      `print_pretty` groups into a `std::collections::HashMap<String, Vec<&TestResult>>` and iterates
      it (`:262-282`, iteration order randomized per process); `colored` emits ANSI conditionally
      (`:257-259`, `:294-298`); `print_test_result_pretty` prints a duration column ONLY when
      `duration.as_millis() > 100` (`:305-309`, a conditional that changes line WIDTH)
    - `cargo-pmcp/src/commands/test/apps.rs` lines 870-885 (the `TestResult` struct literal) and
      `cargo-pmcp/src/commands/test/conformance.rs` lines 270-295 (the exhaustive `TestCategory`
      match with no `_` arm) — the two hard compile-break surfaces A-D11 names
    - `crates/mcp-tester/src/lib.rs` lines 40-80 (the module + re-export surface, so the A2 grep
      knows what is public)
    - `.github/workflows/mcp-tester-validation.yml` lines 55-75 (the binary stubbed to `echo` —
      the A-CI evidence) and `.github/workflows/ci.yml` lines 441-445 (`gate.needs`, which does not
      include that workflow)
  </read_first>
  <behavior>
    - `--format pretty` on a SINGLE-category, all-`Duration::from_secs(0)`, ANSI-disabled fixture
      produces bytes matching a pinned literal exactly. Single-category makes the `HashMap` a
      one-entry map, so its iteration order is deterministic by construction; zero durations mean the
      `>100ms` conditional column never fires; ANSI is pinned OFF explicitly rather than relying on
      tty detection, which differs between a terminal and a captured `Vec<u8>`.
    - `--format pretty` on a MULTI-category fixture is asserted STRUCTURALLY, not byte-wise: the
      MULTISET of output lines (after trimming) — line text mapped to occurrence count — equals a
      pinned multiset, and the per-category line blocks are each internally ordered. The only thing
      not asserted is the order OF the category blocks. A set would lose multiplicity and let a
      duplicated or dropped line pass.
    - `--format minimal` and `--format verbose` each produce a non-empty capture (smoke-level only;
      neither is a consumer contract).
  </behavior>
  <action>
    Extend `crates/mcp-tester/tests/report_compat.rs`.

    Add a header comment block above the pretty tests stating, with the three `report.rs` line
    citations, exactly WHY a byte-identical multi-category pretty golden is impossible and what is
    asserted instead. This is the "do not write an acceptance criterion that cannot pass" record —
    a future contributor must be able to see the criterion was chosen, not conceded.

    Pin ANSI off explicitly with `colored`'s control override (`colored::control::set_override(false)`
    — read the `colored` 3.x API surface actually available before writing the call) at the top of
    each pretty test, and add a comment saying tty detection differs between a terminal and a
    captured `Vec<u8>`, so relying on the default makes the test environment-dependent.

    Write the three groups in `<behavior>`. For the structural multi-category assertion, compare a
    MULTISET — a `BTreeMap<String, usize>` counting occurrences of each trimmed non-empty line — NOT a
    `BTreeSet`. A set collapses duplicates, so a line that appears twice where it should appear once
    (or once where it should appear twice) compares EQUAL and the regression escapes. Report a
    mismatch as the per-line count difference, naming the line and both counts. Separately assert that
    within each category block the test lines appear in insertion order — so the assertion is
    order-insensitive ONLY about the thing that is genuinely non-deterministic, which is the order OF
    the category blocks and nothing else.

    Then re-measure assumption A2 and RECORD it. Run, and paste the exact commands and their exact
    output into the summary:
    - `grep -rn 'TestReport *{' --include='*.rs' cargo-pmcp/ crates/ src/ tests/ | grep -v 'crates/mcp-tester/'`
    - `grep -rn 'TestResult *{' --include='*.rs' cargo-pmcp/ | head`
    - `grep -rn 'TestCategory::' --include='*.rs' cargo-pmcp/src/commands/test/conformance.rs`

    Interpret the result explicitly in the summary as a DECISION INPUT for plan 117-11:
    - If no `TestReport` struct literal exists outside `crates/mcp-tester/`, note that an optional
      `#[serde(default, skip_serializing_if = "Option::is_none")]` field on `TestReport` WOULD be
      safe — and note that 117-11 nevertheless takes the stronger A-D11 route (a NEW top-level
      struct, zero change to `TestReport`), because that route has zero risk and the measurement only
      confirms the weaker option was not needed.
    - If such a literal DOES exist, that is a contradiction of A2 and must be reported loudly: the
      optional-field route is FORBIDDEN and 117-11 has no choice.

    Also record the A-CI evidence in the summary as one line with its citation: no CI job runs
    `mcp-tester` against a live server (`mcp-tester-validation.yml:59-62` stubs the binary to `echo`,
    and that workflow is absent from `ci.yml`'s `gate.needs` at `:443`), so CI is not a consumer of
    the report shape.

    Do NOT add a field to `TestResult`. Do NOT add a variant to `TestCategory` or `TestStatus`. Do
    NOT change `ServerTester::new`'s arity. All four are hard compile breaks in `cargo-pmcp`.
  </action>
  <verify>
    <automated>cargo test -p mcp-tester --test report_compat &amp;&amp; cargo build -p cargo-pmcp</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mcp-tester --test report_compat` exits 0, reports at least 6 tests, `0 failed`.
    - `cargo build -p cargo-pmcp` exits 0 (this plan changed no `mcp-tester` source, so this is the
      recorded BASELINE that 117-11 must still satisfy).
    - The file contains the exact byte-golden pretty test for the single-category fixture and a
      separate structural test for the multi-category fixture (two distinct `#[test]` functions).
    - The multi-category comparison uses an occurrence-COUNTING map, not a set:
      `grep -c 'BTreeSet' crates/mcp-tester/tests/report_compat.rs` is 0 in the multi-category test.
    - Negative control, executed, recorded, reverted: duplicate one output line in the fixture; the
      multi-category structural test MUST fail naming the line and both counts. (A `BTreeSet`
      comparison would have passed.)
    - The file contains an explicit ANSI override call, and a comment containing the phrase
      `tty detection`.
    - The pretty section's header comment cites `report.rs:262-282`, `report.rs:305-309` and the
      `colored` conditional — all three non-determinism sources by line.
    - The three A2 grep commands were RUN and their exact output is pasted in the summary, followed
      by an explicit one-sentence verdict of the form "A2 CONFIRMED" or "A2 CONTRADICTED".
    - The summary contains the A-CI line with both citations
      (`mcp-tester-validation.yml:59-62`, `ci.yml:443`).
    - `git diff --stat crates/mcp-tester/src/` is empty — this plan touches no `mcp-tester` source.
  </acceptance_criteria>
  <done>
    The pretty contract is pinned by a criterion that can actually pass (byte on a pinned
    single-category fixture, structural on multi-category), the three non-determinism sources are
    documented in-file by line, A2 is re-measured with recorded output and an explicit verdict, and
    the `cargo build -p cargo-pmcp` baseline is recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp-tester library API → six in-repo linking crates | A type change here is a hard workspace compile break, not a runtime error |
| mcp-tester `--format json` stdout → `PostDeployReport` parser | `cargo-pmcp` spawns ITSELF and parses `PostDeployReport`, not `TestReport` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-07 | Denial of Service (build) | `mcp_tester::{TestResult, TestCategory, TestStatus}` | mitigate | Additive-only verdict (A-D11), enforced here by pinning single-run output AND by making `cargo build -p cargo-pmcp` an explicit recorded acceptance item — the compiler is the strict consumer |
| T-117-08 | Repudiation | the additivity claim | mitigate | The claim is a golden test captured pre-change, plus three RUN-and-recorded greps for A2, not an inherited research assertion |
| T-117-09 | Tampering | pretty-format golden | accept | Multi-category pretty output is genuinely non-deterministic (HashMap grouping). Accepted with a documented structural assertion instead of a byte one; the byte assertion is retained on a deliberately-pinned single-category fixture so the format is not left unpinned |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages added. `crates/mcp-tester/Cargo.toml` is NOT edited by this plan; the `toml`-vs-YAML dependency decision belongs to plan 117-08 |
</threat_model>

<verification>
- `cargo test -p mcp-tester --test report_compat` exits 0 with at least 6 tests and `0 failed`.
- `cargo build -p cargo-pmcp` exits 0 and is recorded as the pre-change baseline.
- `git diff --stat crates/mcp-tester/src/ crates/mcp-tester/Cargo.toml` is empty.
- The A2 verdict and the A-CI line are recorded in the summary with their command output.
- `make quality-gate` exits 0.
</verification>

<success_criteria>
- `crates/mcp-tester/tests/report_compat.rs` exists and is green against the unmodified 0.7.0 crate.
- The json contract is pinned byte-for-byte with proven width-preserving normalization.
- The pretty contract is pinned by an achievable criterion, with all three measured non-determinism
  sources cited by line in-file.
- Assumption A2 is re-measured with pasted command output and an explicit CONFIRMED/CONTRADICTED
  verdict that plan 117-11 can act on.
- A-CI is recorded with citations.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-03-SUMMARY.md` when done.
Record: the A2 grep commands and their exact output plus the verdict; the A-CI line; whether the
serde `Duration` needed a dynamic; both negative-control failure messages; and the
`cargo build -p cargo-pmcp` baseline result.
</output>
