---
phase: 117-agents-tester-v1-severability
plan: 08
type: execute
wave: 2
depends_on: ["117-03"]
files_modified:
  - crates/mcp-tester/baselines/era-deltas.yaml
  - crates/mcp-tester/src/era_diff.rs
  - crates/mcp-tester/src/lib.rs
  - crates/mcp-tester/tests/era_baseline.rs
  - fuzz/fuzz_targets/era_deltas_parser.rs
  - fuzz/Cargo.toml
autonomous: true
requirements: [CLNT-04]

must_haves:
  truths:
    - "The v1-vs-v2 expected-difference baseline exists as a checked-in, comment-carrying data file that a human can review AS A SPEC ARTIFACT — it is the written statement of what 'dual-version' means for this SDK (D-06)"
    - "Every baseline entry carries a `source` citation a reviewer can check without reading Rust, and entries owned by not-yet-signed-off phases are flagged `provisional` so a Phase-114/115 change produces a legible baseline edit rather than a mystery test failure"
    - "Every entry carries a stable `observation_id` naming the WIRE FACT it is about (`method.initialize`, `header.mcp_session_id`, `result.cache_scope`, …), because plan 117-11 joins on that ID. MEASURED: `TestResult` (`crates/mcp-tester/src/report.rs:74-81`) has no field for a header, a session id, a result-envelope key or an HTTP status, so a comparison keyed on human-facing test names could never observe most of these 14 entries"
    - "The format decision is made explicitly and recorded: YAML via `serde_yaml` (already a dependency at `crates/mcp-tester/Cargo.toml:26`, already used by `src/scenario.rs:234-249`) rather than TOML, which would be a NEW dependency on a published 0.7.0 crate"
    - "The baseline reader is non-vacuous: a file that failed to parse into zero entries FAILS a test with a named floor, because a vacuous baseline makes every later diff pass"
    - "The parser never panics on arbitrary bytes — the CLAUDE.md ALWAYS fuzz requirement is discharged for this feature"
  artifacts:
    - path: "crates/mcp-tester/baselines/era-deltas.yaml"
      provides: "14 seeded expected-difference entries, each with id/observation_id/subject/v1/v2/kind/source/provisional"
      min_lines: 120
      contains: "observation_id"
    - path: "crates/mcp-tester/src/era_diff.rs"
      provides: "New top-level module: the baseline model + loader. NOTHING here touches TestResult/TestCategory/TestStatus"
      min_lines: 120
      contains: "EraDelta"
    - path: "crates/mcp-tester/tests/era_baseline.rs"
      provides: "Baseline schema + id/observation_id uniqueness + non-empty-source + non-vacuity tripwire"
      min_lines: 90
    - path: "fuzz/fuzz_targets/era_deltas_parser.rs"
      provides: "CLAUDE.md ALWAYS fuzz target: the baseline parser must never panic"
      contains: "fuzz_target"
  key_links:
    - from: "crates/mcp-tester/src/era_diff.rs"
      to: "crates/mcp-tester/baselines/era-deltas.yaml"
      via: "serde_yaml::from_str, path derived from CARGO_MANIFEST_DIR"
      pattern: "serde_yaml"
    - from: "fuzz/fuzz_targets/era_deltas_parser.rs"
      to: "mcp_tester::era_diff"
      via: "fuzz/Cargo.toml already dev-deps mcp-tester at :53-54 — only a [[bin]] block is new"
      pattern: "mcp_tester"
---

<objective>
Write the expected-difference baseline — the artifact D-06 says turns the tester from a noise
generator into a live spec-drift detector — as a reviewable data file, plus its loader, its
non-vacuity tripwire, and its fuzz target.

Purpose: CLNT-04 / D-06. v2 legitimately differs from v1 by design (no `initialize`, no
`tasks/list`, `resultType` added, caching hints REQUIRED not optional), so a naive dual-run diff is
pure noise. Encoding the KNOWN deltas makes "deviation from expected" the finding. That encoding is
also a direct input to Phase 118's conformance work, and its maintenance IS the dual-version
contract, not overhead.

Output: `crates/mcp-tester/baselines/era-deltas.yaml`, `crates/mcp-tester/src/era_diff.rs`,
`crates/mcp-tester/tests/era_baseline.rs`, and a fuzz target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
<!-- crates/mcp-tester/Cargo.toml:19-45 — what is available. NOTE: there is NO `toml` dependency. -->
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"        # <- ALREADY PRESENT, already used by src/scenario.rs:234-249
[dev-dependencies]
tempfile = "3"
proptest = "1"

<!-- crates/mcp-tester/src/scenario.rs:230-256 — the existing checked-in-YAML loader shape to copy. -->
pub fn from_yaml_file<P: AsRef<Path>>(path: P) -> Result<Self> {
    let content = /* read */;
    serde_yaml::from_str(&content).map_err(/* ... */)
}

<!-- crates/mcp-tester/src/post_deploy_report.rs:10-15, :60-70 — the sanctioned "new top-level
     struct" escape hatch and its forward-compatible field shape. -->
//! Additive field changes (new optional fields with `#[serde(default)]`) do NOT bump the version.
#[serde(default, skip_serializing_if = "Option::is_none")]
pub mode: Option<String>,

<!-- fuzz/Cargo.toml:53-54 — mcp-tester is ALREADY a fuzz dependency. Only a [[bin]] block is new. -->
[dependencies.mcp-tester]
path = "../crates/mcp-tester"

<!-- fuzz/Cargo.toml:223-228 — the [[bin]] block shape. -->
[[bin]]
name = "fuzz_schema_draft_pin"
path = "fuzz_targets/fuzz_schema_draft_pin.rs"
test = false
doc = false
bench = false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the era-delta baseline and its loader</name>
  <files>crates/mcp-tester/baselines/era-deltas.yaml, crates/mcp-tester/src/era_diff.rs, crates/mcp-tester/src/lib.rs</files>
  <read_first>
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q5.2 — the 14 MEASURED
      era deltas with their citations; this table is the content, do not invent entries — and § Q5.3
      for the entry-schema rationale (`provisional`, `source`)
    - `crates/mcp-tester/Cargo.toml` lines 15-45 (confirm for yourself: `serde_yaml = "0.9"` is
      present, `toml` is NOT)
    - `crates/mcp-tester/src/scenario.rs` lines 225-260 (`from_yaml_file`, `from_file` — the existing
      checked-in-YAML loader idiom in this crate) and lines 295-375 (its inline YAML tests)
    - `crates/mcp-tester/src/post_deploy_report.rs` lines 1-120 (the module doc arguing the "new
      top-level struct, never a field on `TestResult`" case, and the forward-compatible field shape)
    - `crates/mcp-tester/src/lib.rs` lines 1-90 (the module declaration + re-export barrel)
    - `crates/mcp-tester/tests/report_compat.rs` and the 117-03 summary (the recorded A2 verdict,
      which bounds what may be added)
  </read_first>
  <action>
    Create `crates/mcp-tester/baselines/era-deltas.yaml`.

    FORMAT DECISION, to be stated in the file's own header comment and in the summary: **YAML**, not
    TOML. `serde_yaml = "0.9"` is already a dependency of this crate (`Cargo.toml:26`) and already
    loads checked-in data in `src/scenario.rs:234-249`, and `mcp-tester`'s own scenarios are
    `scenarios/*.yaml`. Adding `toml` would be a NEW dependency on a published 0.7.0 crate, which the
    CLAUDE.md package discipline requires justifying and which buys nothing here — YAML supports
    comments, which is the only property the "reviewable as a spec artifact" requirement actually
    needs.

    The file opens with a comment block stating: this file IS the written statement of what
    "dual-version" means for this SDK; every entry is a difference that is CORRECT BY DESIGN; any
    observed v1/v2 difference NOT listed here is a FINDING; any entry here that no longer reproduces
    is ALSO a finding (either the spec moved or we regressed); review this file like a spec, not like
    config.

    Top-level keys: `schema_version: 1`, `v1_protocol: "2025-11-25"`, `v2_protocol: "2026-07-28"`,
    and a `deltas:` list.

    Seed all 14 entries from RESEARCH § Q5.2, ids `ERA-01` … `ERA-14`, each with:
    `id`, `observation_id`, `subject`, `v1`, `v2`, `kind`, `source` (the citation verbatim from the
    research table), optional `note`, and `provisional` (a bool).

    `observation_id` is the STABLE, machine-facing name of the wire fact the entry is about —
    namespaced, lowercase, dot-separated, e.g. `method.initialize`, `method.tasks_list`,
    `header.mcp_session_id`, `header.last_event_id`, `result.result_type`, `result.cache_scope`,
    `capability.location`, `http.status.unknown_method`. It is NOT a human-facing test name and must
    never be renamed for readability. Its reason, which belongs in the file's header comment:
    plan 117-11 joins the observed v1/v2 difference set against this field, because `TestResult`
    (`crates/mcp-tester/src/report.rs:74-81`) carries only `{name, category, status, duration, error,
    details}` — no header, no session id, no result-envelope key, no HTTP status — so a comparison
    keyed on test names could never observe most of these entries and would report permanent false
    MISSING findings. Every `observation_id` must be UNIQUE across the file; plan 117-11 adds a probe
    for each one and a two-direction coverage test between the probe registry and this file. Mark `provisional: true` on the entries whose owning
    phase is not signed off — at minimum the two `tasks/*` rows (Phase 114's surface is PROVISIONAL
    per D-09) — and add an inline comment on each provisional entry naming the phase that owns it.
    Verify each `source` citation resolves before writing it: open the cited file at the cited line
    and confirm it says what the research says. Record any citation that did NOT resolve.

    Create `crates/mcp-tester/src/era_diff.rs`. Module doc (`//!`) must state, citing
    `post_deploy_report.rs:10-15` as the in-repo precedent, that this is a NEW TOP-LEVEL module and
    that the A-D11 rule is absolute: nothing here adds a field to `TestResult`, a variant to
    `TestCategory` or `TestStatus`, or a positional argument to `ServerTester::new` — each is a hard
    compile break in `cargo-pmcp` (`commands/test/apps.rs:874-878`,
    `commands/test/conformance.rs:276-289`, five `ServerTester::new` call sites).

    In it, define:
    - `pub struct EraDelta { … }` mirroring the YAML entry — including `observation_id` as a REQUIRED
      field — with `#[serde(default)]` on `note` and `provisional` so a future field addition stays
      additive.
    - `pub struct EraBaseline { pub schema_version: u32, pub v1_protocol: String,
      pub v2_protocol: String, pub deltas: Vec<EraDelta> }`.
    - `pub fn load_baseline(path) -> Result<EraBaseline>` and
      `pub fn default_baseline_path() -> PathBuf` deriving the checked-in file from
      `env!("CARGO_MANIFEST_DIR")`.
    - `pub fn parse_baseline(text: &str) -> Result<EraBaseline>` — the pure parse seam the fuzz target
      calls. It must return `Err`, never panic, on any input.
      VALIDATION BELONGS HERE, not only in the fuzz target: after deserializing, `parse_baseline`
      REJECTS a baseline in which any delta has an empty-after-trim `id` or an empty-after-trim
      `observation_id`, or in which two deltas share an `id` or an `observation_id`. That makes
      "a successfully parsed baseline has non-empty unique ids" a PARSER CONTRACT, so Task 3's fuzz
      invariant asserts something the parser actually guarantees instead of crashing the fuzzer on
      valid-YAML-but-empty-string input. Document each rejection in the function's doc comment.

    Register the module and re-export its types in `crates/mcp-tester/src/lib.rs`, following the
    existing barrel style.

    Do NOT add `DualRunReport` or any comparison logic here — plan 117-11 owns that. This task
    delivers the data and the reader only.
  </action>
  <verify>
    <automated>cargo test -p mcp-tester --lib era_diff &amp;&amp; cargo build -p mcp-tester &amp;&amp; cargo build -p cargo-pmcp</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mcp-tester/baselines/era-deltas.yaml` exists and
      `grep -c '^\s*- id:' crates/mcp-tester/baselines/era-deltas.yaml` is at least 14.
    - Every entry has a non-empty `source:` — verified by
      `grep -c 'source:' crates/mcp-tester/baselines/era-deltas.yaml` equalling the entry count.
    - Every entry has an `observation_id:` and all of them are unique —
      `grep -c 'observation_id:' …` equals the entry count, and
      `grep 'observation_id:' … | sort | uniq -d` is empty.
    - `parse_baseline` REJECTS (returns `Err` for) each of: an empty `id`, an empty `observation_id`,
      a duplicated `id`, a duplicated `observation_id` — proven by four unit tests.
    - At least 2 entries carry `provisional: true`, each with an adjacent comment naming the owning
      phase.
    - `git diff crates/mcp-tester/Cargo.toml` is EMPTY — no `toml` dependency was added.
    - `cargo build -p mcp-tester` exits 0 and `cargo build -p cargo-pmcp` exits 0 (the recorded
      117-03 baseline still holds).
    - `cargo test -p mcp-tester --test report_compat` exits 0 — the single-run report output is
      unchanged by this plan.
    - `git diff crates/mcp-tester/src/report.rs` is empty — `TestResult`, `TestCategory` and
      `TestStatus` are untouched.
    - `crates/mcp-tester/src/era_diff.rs`'s module doc contains the token `post_deploy_report` and
      the token `cargo-pmcp`.
    - The summary records: the format decision and its reason; and, for each of the 14 entries,
      whether its `source` citation was checked and resolved.
  </acceptance_criteria>
  <done>
    The baseline exists as reviewable, comment-carrying YAML with 14 cited entries and provisional
    flags; its loader is a new top-level module; and the `cargo-pmcp` build plus the single-run report
    golden are both still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Baseline schema + non-vacuity tripwire</name>
  <files>crates/mcp-tester/tests/era_baseline.rs</files>
  <read_first>
    - `crates/mcp-tester/baselines/era-deltas.yaml` and `crates/mcp-tester/src/era_diff.rs` as written
      by Task 1
    - `tests/phase115_contract_bindings.rs` lines 87-90 and 149 (file-path constants and the
      `MINIMUM_BINDINGS` named floor), lines 432-438 and 701-712 (the
      `FAILURE MODE: … / WHAT TO DO: fix the reader, not the assertion.` message form and the
      "do not lower the floor" instruction) — this is the exact analog for a checked-in-data-file
      schema tripwire
    - `crates/pmcp-cfn-renderer/tests/semantic_golden.rs` lines 35-50 (the alternative directory-walk
      form with a `checked >= 1` counter), for reference only
    - `crates/mcp-tester/Cargo.toml` `[dev-dependencies]` (what a test in this crate may use)
  </read_first>
  <behavior>
    - Test `every_delta_id_is_unique`: no two entries share an `id`.
    - Test `every_delta_observation_id_is_present_and_unique`: every entry has a non-empty
      `observation_id`, no two entries share one, and each is lowercase dot-separated with at least
      one `.` — this is the join key plan 117-11 diffs on, so a missing or duplicated one silently
      merges two distinct wire facts.
    - Test `every_delta_carries_a_nonempty_source`: every entry's `source` is non-empty after
      trimming, and is at least 10 characters (a citation shorter than that is a label, not a
      citation).
    - Test `the_baseline_parse_is_not_vacuous`: at least 14 entries parsed. Failure message uses the
      `FAILURE MODE: … / WHAT TO DO: fix the reader or restore the file; do not lower the floor.`
      form.
    - Test `the_protocol_versions_match_the_sdk_constants`: `v1_protocol` and `v2_protocol` equal the
      pmcp protocol-version constants, so a constant change surfaces here rather than silently
      desynchronising the baseline.
    - Test `provisional_entries_name_their_owning_phase`: every entry with `provisional: true` has a
      `note` (or an equivalent field) whose text names a phase number — so a churn there produces a
      legible edit, per Pitfall 6.
    - Test `the_parser_rejects_garbage_without_panicking`: `parse_baseline` on several malformed
      inputs returns `Err` and does not panic.
  </behavior>
  <action>
    Create `crates/mcp-tester/tests/era_baseline.rs`.

    Module doc (`//!`) states: the baseline is a SPEC ARTIFACT, so this file gates its schema, not its
    content; the non-vacuity floor exists because a reader that silently parses nothing would make
    every later era diff pass over an empty set; and the remedy for a floor failure is always to fix
    the reader or restore the file, never to lower the floor.

    Add named constants in the `tests/phase115_contract_bindings.rs:149` style:
    `BASELINE_FILE`, `MINIMUM_DELTAS: usize = 14`, `MIN_SOURCE_CHARS: usize = 10`, each with a doc
    comment explaining what a violation means and what the remedy is NOT.

    Derive the baseline path from `env!("CARGO_MANIFEST_DIR")` — no absolute path literal.

    Write the seven tests in `<behavior>`, all using the `FAILURE MODE: … / WHAT TO DO: …` form.

    For `the_protocol_versions_match_the_sdk_constants`, import the version constants from `pmcp`
    (`crates/mcp-tester/Cargo.toml:20` already depends on `pmcp` by path) rather than hardcoding the
    strings — a hardcoded string here would silently desynchronise exactly the way this test exists
    to prevent.
  </action>
  <verify>
    <automated>cargo test -p mcp-tester --test era_baseline</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mcp-tester --test era_baseline` exits 0, reports at least 7 tests, `0 failed`.
    - Third negative control, executed, recorded, reverted: duplicate one entry's `observation_id` —
      `every_delta_observation_id_is_present_and_unique` MUST fail naming the duplicate.
    - `grep -c 'FAILURE MODE' crates/mcp-tester/tests/era_baseline.rs` is at least 3.
    - `grep -c 'MINIMUM_DELTAS' crates/mcp-tester/tests/era_baseline.rs` is at least 2.
    - No absolute path literal appears in the file
      (`grep -c "\"/Users\|\"/home" crates/mcp-tester/tests/era_baseline.rs` is 0).
    - The protocol-version test imports the constants from `pmcp` — a `grep` shows no literal
      `"2026-07-28"` string on the assertion's expected side.
    - Negative control, executed and recorded verbatim, then reverted: delete three entries from
      `era-deltas.yaml` and re-run — `the_baseline_parse_is_not_vacuous` MUST fail with the
      "do not lower the floor" message. Restore and re-run to green.
    - Second negative control, executed and reverted: duplicate one entry's `id` —
      `every_delta_id_is_unique` MUST fail naming the duplicated id.
    - `crates/mcp-tester/tests/era_baseline.rs` contains zero occurrences of `TODO`, `FIXME`, `XXX`.
  </acceptance_criteria>
  <done>
    The baseline's schema, id uniqueness, citation presence, provisional-ownership and protocol-version
    synchronisation are all gated by non-vacuous tests whose failure modes were EXECUTED.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fuzz target for the baseline parser (CLAUDE.md ALWAYS)</name>
  <files>fuzz/fuzz_targets/era_deltas_parser.rs, fuzz/Cargo.toml</files>
  <read_first>
    - `fuzz/fuzz_targets/dcr_response_parser.rs` in full (44 lines — the shortest complete
      parser-fuzz target in the repo, and it carries the CLAUDE.md invocation line in its header)
    - `fuzz/Cargo.toml` lines 25-60 (the `pmcp` dependency with `default-features = false` and a
      per-entry JUSTIFIED feature list, and the existing `[dependencies.mcp-tester]` at `:53-54`) and
      lines 215-235 (the `[[bin]]` block shape)
    - `fuzz/fuzz_targets/app_widget_scanner.rs` (the existing target that already calls into
      `mcp_tester`, proving the dependency wiring works)
    - `crates/mcp-tester/src/era_diff.rs` as written by Task 1 (specifically `parse_baseline`'s
      signature — the pure seam the target calls)
  </read_first>
  <action>
    Create `fuzz/fuzz_targets/era_deltas_parser.rs`.

    Header doc, in the `dcr_response_parser.rs` shape: name the target, carry the literal CLAUDE.md
    invocation line `cargo fuzz run era_deltas_parser`, and state the invariant —
    `mcp_tester::era_diff::parse_baseline` must never panic on arbitrary bytes; error paths are
    acceptable, panics are not.

    Body: `#![no_main]`, `use libfuzzer_sys::fuzz_target;`, and a `fuzz_target!(|data: &[u8]| { … })`
    that converts the bytes to a `&str` (returning early on invalid UTF-8, as
    `dcr_response_parser.rs` returns early on a parse miss), calls `parse_baseline`, and on `Ok`
    asserts ONLY invariants `parse_baseline` actually GUARANTEES. Task 1 makes non-empty unique `id`
    and `observation_id` a parser contract — it returns `Err` for each violation — so asserting them
    here is legitimate: a violation means the parser broke its own contract, which is a real bug worth
    a crash. Assert exactly that set, plus `deltas.len()` matching what iterating the collection
    yields.

    Do NOT assert an invariant the parser does not enforce. Before this revision the target asserted
    non-empty `id` while `parse_baseline` was described as a plain serde parser — valid YAML with
    `id: ""` would have parsed fine and deliberately crashed the fuzzer on well-formed input. If you
    find any assertion here that `parse_baseline` does not guarantee, either move the validation INTO
    `parse_baseline` (Task 1's approach) or drop the assertion; record which you did. Cross-check by
    reading `parse_baseline`'s doc comment, which Task 1 requires to enumerate its rejections.

    Add the matching `[[bin]]` block to `fuzz/Cargo.toml` with `test = false`, `doc = false`,
    `bench = false`, placed alongside the existing blocks.

    `mcp-tester` is ALREADY a fuzz dependency (`fuzz/Cargo.toml:53-54`), so no manifest dependency
    churn is needed. If the target somehow needs a `pmcp` feature not currently listed, add it to the
    existing explicit feature list WITH a written prose rationale — that file's convention is that
    every feature entry is justified in a comment.

    Run the target briefly and record the invocation and result.
  </action>
  <verify>
    <automated>cargo fuzz build era_deltas_parser</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fuzz build era_deltas_parser` exits 0.
    - `cargo fuzz run era_deltas_parser -- -runs=20000` completes with no crash; the exact command and
      its summary line are recorded in the summary. (If the local toolchain cannot run libFuzzer,
      record that fact explicitly with the error, and record that `cargo fuzz build` succeeded — do
      NOT silently skip.)
    - `fuzz/fuzz_targets/era_deltas_parser.rs` header contains the literal string
      `cargo fuzz run era_deltas_parser`.
    - The target asserts at least one structural invariant on the `Ok` path, not merely discarding the
      result, AND every invariant it asserts is one `parse_baseline`'s doc comment enumerates as a
      rejection reason. The summary lists the target's assertions next to the parser's documented
      rejections and states that the two sets agree.
    - `fuzz/Cargo.toml` gained exactly one `[[bin]]` block with `test = false`, `doc = false`,
      `bench = false`, and gained zero new `[dependencies]` entries
      (`git diff fuzz/Cargo.toml` shows only the `[[bin]]` addition).
    - `cargo build -p mcp-tester` still exits 0.
  </acceptance_criteria>
  <done>
    The CLAUDE.md ALWAYS fuzz requirement is discharged for the baseline feature with a target that
    builds, runs, and asserts a real invariant — with the run result recorded rather than assumed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in data file → tester logic | A malformed or truncated baseline must fail loudly, not silently disable the diff |
| arbitrary bytes → `parse_baseline` | The parser is reachable from a user-supplied `--baseline` path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-26 | Tampering | `era-deltas.yaml` | mitigate | A vacuous baseline makes every later diff pass. Guarded by a named `MINIMUM_DELTAS` floor with a "do not lower the floor" remedy, proven to fire by an executed negative control |
| T-117-27 | Denial of Service | `parse_baseline` | mitigate | Fuzz target asserts no panic on arbitrary bytes; `parse_baseline` returns `Err` rather than panicking. Its `Ok`-path invariants are exactly the parser's own documented rejections (non-empty unique `id` / `observation_id`), so the target cannot crash on well-formed input the parser legitimately accepts |
| T-117-28 | Repudiation | the "conforms" verdict | mitigate | Pitfall 6: a stale baseline reports conformance against a spec that moved. Mitigated by mandatory `source` citations, `provisional` flags naming the owning phase, and a test asserting the protocol-version constants match the SDK's |
| T-117-29 | Denial of Service (build) | `cargo-pmcp` | mitigate | Everything new lives in a NEW top-level module; `TestResult`/`TestCategory`/`TestStatus` are untouched, asserted by an empty `git diff crates/mcp-tester/src/report.rs` and a green `cargo build -p cargo-pmcp` |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages. The TOML-vs-YAML decision is resolved to YAML precisely to avoid adding `toml` to a published 0.7.0 crate; `git diff crates/mcp-tester/Cargo.toml` empty is an acceptance criterion. `fuzz/Cargo.toml` gains only a `[[bin]]` block |
</threat_model>

<verification>
- `cargo test -p mcp-tester --test era_baseline` exits 0 with at least 7 tests.
- `cargo test -p mcp-tester --test report_compat` exits 0 — the single-run output is unchanged.
- `cargo build -p mcp-tester` and `cargo build -p cargo-pmcp` both exit 0.
- `cargo fuzz build era_deltas_parser` exits 0; the `-runs=20000` result is recorded.
- `git diff crates/mcp-tester/Cargo.toml` and `git diff crates/mcp-tester/src/report.rs` are both
  empty.
- `make quality-gate` exits 0.
</verification>

<success_criteria>
- A 14-entry, comment-carrying, citation-bearing `era-deltas.yaml` exists and is legible as a spec
  artifact, and every entry carries a unique stable `observation_id` that plan 117-11 joins on.
- `parse_baseline` enforces non-empty unique `id` and `observation_id` as a documented contract, so
  the fuzz invariant matches what the parser guarantees.
- The format decision (YAML over TOML) is made explicitly and recorded, with zero new dependencies on
  a published crate.
- The loader is a NEW top-level module; `TestResult`, `TestCategory` and `TestStatus` are untouched
  and `cargo build -p cargo-pmcp` still passes.
- Schema, uniqueness, citation, provisional-ownership and version-sync are non-vacuously gated, with
  executed negative controls.
- A fuzz target builds, runs, and asserts a real invariant.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-08-SUMMARY.md` when done.
Record: the YAML-over-TOML decision and its reason; the full `id` → `observation_id` table for all
14 entries; a per-entry note of whether each `source` citation was checked and resolved;
`parse_baseline`'s enumerated rejections alongside the fuzz target's asserted invariants with a
statement that the two sets agree; both negative-control outputs; and the exact `cargo fuzz run`
command with its result.
</output>
