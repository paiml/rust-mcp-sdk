---
phase: 117-agents-tester-v1-severability
plan: 09
type: execute
wave: 3
depends_on: ["117-02", "117-06"]
files_modified:
  - src/server/streamable_http_server.rs
  - src/server/streamable_http_server/v1_session.rs
  - src/server/streamable_http_server/v1_session_off.rs
autonomous: true
requirements: [SMPL-02]

must_haves:
  truths:
    - "`ServerState`'s three v1-only fields (`sse_streams`, `sessions`, `event_store`) collapse into ONE `v1: v1::V1State` field, which is a zero-sized type on the `full-v2` build — the STRUCTURAL proof that no session map is allocated on the v2 path (D-03, D-10)"
    - "All SEVEN era chokepoints move into the paired module with signatures unchanged — including the `era` parameter the null twins ignore but must NOT drop, because dropping it would invite a second era resolver, which Phase 112 D-11 / 113 Pitfall 2 forbid"
    - "The `sessions_active` / `resumability_active` truth tables and their property tests survive the move unchanged — the pure `const fn` rule / state-reader split is preserved, not flattened"
    - "The v1 wire is byte-identical across the move, proven by the goldens plan 117-02 captured BEFORE the cut"
    - "Zero `#[cfg]` attributes are added at any call site — the paired module exists precisely so D-03's rejection of in-place `#[cfg]` blocks is honoured"
    - "The null twin exposes BEHAVIOUR, never a reference to state it does not hold: a `&Arc<RwLock<HashMap<..>>>` accessor is unimplementable on a ZST, so the surviving `build_response` read at `:1936` becomes a `v1::` operation returning an owned answer"
  artifacts:
    - path: "src/server/streamable_http_server/v1_session.rs"
      provides: "V1State with the three real fields, plus the seven chokepoints and EventStoreHandle"
      contains: "sessions_active_for"
    - path: "src/server/streamable_http_server/v1_session_off.rs"
      provides: "ZST V1State and seven constant-answer twins with IDENTICAL signatures"
      contains: "sessions_active"
    - path: "src/server/streamable_http_server.rs"
      provides: "ServerState reduced to 4 fields; make_server_state is the ONE construction site edited; build_response routes through a v1:: operation"
      contains: "v1::V1State"
  key_links:
    - from: "src/server/streamable_http_server.rs make_server_state"
      to: "src/server/streamable_http_server/v1_session.rs V1State::new"
      via: "the single construction site (:303-330) builds the V1State from the config"
      pattern: "V1State"
    - from: "src/server/streamable_http_server.rs call sites"
      to: "v1::sessions_active / v1::resumability_store"
      via: "unqualified v1:: calls with ZERO #[cfg] at the call site"
      pattern: "v1::"
---

<objective>
Take the first structural step of the D-03 cut: collapse `ServerState`'s three v1-only fields into
one `V1State`, and move the seven era chokepoints into the paired module with signatures intact.

Purpose: SMPL-02 is satisfied STRUCTURALLY (D-10) — the v2 path must PROVABLY not compile
session/SSE code. Phase 113 Plan 08 already did the hard part: every session and resumability
decision in this 6,408-line file routes through seven chokepoint functions (`:416-474`, `:522-564`),
and the file's own comment at `:493-497` hands severance to Phase 117 by name. The measured
entanglement is LOW: three of `ServerState`'s six fields are exclusively v1, each already behind a
single documented accessor, and there is no field both eras mutate.

Output: `ServerState` with four fields, `V1State` real on `full` and a ZST on `full-v2`, and seven
chokepoints living in the pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-agents-tester-v1-severability/117-CONTEXT.md
@.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md
@.planning/phases/117-agents-tester-v1-severability/117-PATTERNS.md

<interfaces>
Current `ServerState` (`src/server/streamable_http_server.rs:271-291`) — 6 fields, 3 of them v1-only:
`server: Arc<tokio::sync::Mutex<Server>>`, `config: Arc<StreamableHttpServerConfig>`,
`allowed_origins: AllowedOrigins`,
`sse_streams: Arc<RwLock<HashMap<String, mpsc::UnboundedSender<TransportMessage>>>>` (v1-ONLY),
`sessions: Arc<RwLock<HashMap<String, SessionInfo>>>` (v1-ONLY),
`event_store: Option<EventStoreHandle>` (v1-ONLY).

Single construction site: `make_server_state(server, config) -> ServerState` at `:303-330`. It
resolves `allowed_origins`, type-erases `config.event_store` into an `EventStoreHandle`, and
allocates the two empty maps.

The SEVEN chokepoints, whose signatures must survive the move UNCHANGED (era parameter included):
- `const fn sessions_active_for(cfg_has_generator: bool, era: Option<Era>) -> bool` (`:416-421`)
- `fn sessions_active(state: &ServerState, era: Option<Era>) -> bool` (`:433-435`)
- `fn active_session_generator(state: &ServerState, era: Option<Era>) -> Option<&(dyn Fn() -> String + Send + Sync)>` (`:443-451`)
- `fn apply_session_header(headers: &mut HeaderMap, response_session_id: Option<&String>, sessions_on: bool)` (`:460-474`)
- `const fn resumability_active_for(cfg_has_event_store: bool, era: Option<Era>) -> bool` (`:522-533`)
- `fn resumability_active(state: &ServerState, era: Option<Era>) -> bool` (`:544-546`)
- `fn resumability_store(state: &ServerState, era: Option<Era>) -> Option<&EventStoreHandle>` (`:556-564`)

`EventStoreHandle` type alias lives at `:509`. `SessionInfo` (private) at `:264-269`.
`build_mcp_router` at `:293-302` is UNCHANGED by this plan.

The SEVERABILITY comment at `:493-497` hands severance to Phase 117 by name. UPDATE it; do not
delete it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the three v1-only `ServerState` fields into one `V1State`</name>
  <files>src/server/streamable_http_server.rs, src/server/streamable_http_server/v1_session.rs, src/server/streamable_http_server/v1_session_off.rs</files>
  <read_first>
    - `src/server/streamable_http_server.rs` lines 260-340 (`SessionInfo` at `:264-269`, `ServerState`
      at `:271-291` with its per-field doc comments, `build_mcp_router` at `:293-302`,
      `make_server_state` at `:303-330`) — the exact text being edited
    - `src/server/streamable_http_server/v1_session.rs` and `v1_session_off.rs` as created by plan
      117-06 (the `V1State` skeleton this task fills and wires)
    - Every current read of the three fields. Derive the list, do not assume it — run
      `grep -n 'sse_streams\|\.sessions\|event_store' src/server/streamable_http_server.rs` and work
      from the result. The research measured 5 `sse_streams` sites (`:1936`, `:4464`, `:4474`,
      `:4536`), 10 `sessions` sites (`:1732`, `:1748`, `:1791`, `:1831`, `:1979`, `:2304`, `:4337`,
      `:4354`, `:4521`, `:4539`) and exactly ONE `event_store` reader (`resumability_store` at
      `:556-564`)
    - `src/server/preset.rs` lines 30-45 and 240-260, and `src/server/mod.rs` lines 760-780 and
      4765-4785 — the `..Default::default()` users the research says do NOT break; confirm that
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q2.1 (the field-by-field
      verdict table) and § Q2.5 Step 1
  </read_first>
  <action>
    In `v1_session.rs`, give `V1State` the three real fields with their exact current types:
    `sse_streams: Arc<RwLock<HashMap<String, mpsc::UnboundedSender<TransportMessage>>>>`,
    `sessions: Arc<RwLock<HashMap<String, SessionInfo>>>`, and `event_store: Option<EventStoreHandle>`.
    Add `V1State::new(config: &StreamableHttpServerConfig) -> Self` performing the type-erasure that
    `make_server_state` currently does inline and allocating the two empty maps. Preserve the existing
    doc comment on the `event_store` field verbatim — it documents `EventStoreHandle` and the "reach
    it ONLY through `resumability_store`" rule, which the move must not lose.

    In `v1_session_off.rs`, `V1State` stays a ZST and gains a `new(_config)` constructor taking the
    same parameter and returning `Self`, so the single construction site stays `#[cfg]`-free. Its doc
    comment says the ZST is the structural proof that no session map is allocated on the v2 build.

    In `streamable_http_server.rs`: remove the three fields from `ServerState` and add
    `v1: v1::V1State`. Edit `make_server_state` — the ONE construction site — to call
    `v1::V1State::new(&config)` before the config is moved into the `Arc`. `build_mcp_router`
    (`:293-302`) is UNCHANGED.

    Update every field access found by the grep to route through `state.v1`.

    CRITICAL — expose BEHAVIOUR, not raw state. A raw accessor such as
    `fn sse_streams(&self) -> &Arc<RwLock<HashMap<..>>>` CANNOT be implemented by a zero-sized twin:
    there is no map to return a reference to. For every field read that must keep working on BOTH
    feature sets, add a `v1::` function that performs the OPERATION and returns an owned answer.
    Concretely, the one `sse_streams` read that survives every later plan is `build_response`'s at
    `:1936` (`if let Some(sender) = state.sse_streams.read().get(sid) { sender.send(response); … }`),
    and `build_response` is in NO plan's move list. Express it as a `v1::` function that attempts the
    send and reports whether it happened — for example
    `fn route_to_session_stream(state: &ServerState, session_id: &str, msg: TransportMessage) -> Option<TransportMessage>`
    returning `None` when the message was delivered into a live stream and `Some(msg)` when it was
    not, so the caller falls through to `build_sse_response_from_single_message`. The null twin
    returns `Some(msg)` unconditionally and holds nothing. Choose the exact signature from the real
    call site and record it in the summary.

    An `Option`-returning accessor IS fine where the twin can honestly answer `None` — e.g.
    `fn event_store(&self) -> Option<&EventStoreHandle> { None }`. A reference-to-collection accessor
    is not. Apply that test to every accessor before adding it.

    The remaining `sessions` reads (`:1732`, `:1748`, `:1791`, `:1831`, `:1979`, `:4337`, `:4354`,
    `:4521`, `:4539`) all sit inside functions plan 117-12 or 117-13 moves wholesale into
    `v1_session.rs`, so they need NO accessor at all — they follow their function. `:2304`
    (`compute_outbound_protocol_version`) is the one exception and is plan 117-13's subject; give it
    the same behavioural treatment there, not a raw accessor here. Verify that claim against your own
    grep and report any read you cannot place in one of those three buckets.

    `SessionInfo` (`:264-269`, private) moves to `v1_session.rs` in this task, since `V1State` names
    it. The null twin needs no `SessionInfo` at all.

    ZERO `#[cfg]` attributes may be added at any call site in `streamable_http_server.rs`. That is the
    entire point of the paired module: D-03 explicitly rejects in-place `#[cfg]` blocks, and the
    anti-pattern being replaced is `#[cfg(feature = "v1-compat")]` sprinkled at 12+ sites in
    `handle_post_fast_path_inner` and `handle_post_with_middleware_inner`.

    Remove the `#[allow(dead_code)]` that plan 117-06 Task 1 placed on the unwired `V1State`
    skeleton. 117-06 added it explicitly as a "plan 117-09 wires it in" placeholder; this task is
    that wiring, so the attribute is now unearned. It must not survive into the merged tree.

    Do NOT change `StreamableHttpServerConfig` in this task — its four v1-only public fields are plan
    117-13's subject. Do NOT touch `src/server/preset.rs` or `src/server/axum_router.rs`; both use
    `..Default::default()` or `make_server_state` and need no change. If either DOES break, that is a
    finding — report it rather than editing it silently.
  </action>
  <verify>
    <automated>cargo build -p pmcp --features full &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 &amp;&amp; cargo test --lib --features "full"</automated>
  </verify>
  <acceptance_criteria>
    - The `ServerState` struct body has exactly 4 fields, and none of them is named `sse_streams`,
      `sessions` or `event_store`.
    - `grep -c 'v1: v1::V1State' src/server/streamable_http_server.rs` is 1.
    - `grep -c '#\[cfg(feature = "v1-compat")\]' src/server/streamable_http_server.rs` is 0 — the only
      feature attributes in this file remain the two `cfg_attr` path attributes from plan 117-06.
    - `grep -rn 'allow(dead_code)' src/server/streamable_http_server/v1_session.rs
      src/server/streamable_http_server/v1_session_off.rs` returns no match — plan 117-06 Task 1's
      placeholder `#[allow(dead_code)]` on the `V1State` skeleton is removed now that this task wires
      it in. (Under `-D warnings` a stale `allow` does not itself fail the build, so it must be
      asserted rather than relied on.)
    - `cargo build -p pmcp --features full` exits 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with zero `warning:` lines.
    - `cargo test --lib --features "full"` pass count equals the pre-plan count EXACTLY (record before
      and after; a DROP means a test was silently removed by the move).
    - `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0 — the pre-cut goldens
      are still green.
    - `git diff --stat src/server/preset.rs src/server/axum_router.rs` is empty.
    - The summary records the re-derived grep list of all three fields' access sites, with counts, and
      compares them against the research's measured counts (5 / 10 / 1).
  </acceptance_criteria>
  <done>
    `ServerState` has four fields; the three v1-only ones live behind one `V1State` that is a ZST on
    the v2 build; there is exactly one construction site and zero `#[cfg]` at any call site; and the
    pre-cut goldens are green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the seven era chokepoints into the pair, signatures unchanged</name>
  <files>src/server/streamable_http_server.rs, src/server/streamable_http_server/v1_session.rs, src/server/streamable_http_server/v1_session_off.rs</files>
  <read_first>
    - `src/server/streamable_http_server.rs` lines 400-570 in full — the sessions era gate
      (`:405-474`, including the truth-table doc comment on `sessions_active_for` at `:405-415` and
      the "THE single reader", "the second (and last) permitted reader" and "the ONE place a
      `Mcp-Session-Id` response header is emitted" doc comments) and the resumability era gate
      (`:476-564`, including `EventStoreHandle` at `:509` and the `:493-497` SEVERABILITY comment)
    - `src/server/streamable_http_server.rs` lines 4560-4600 and 5870-5910 — the existing truth-table
      and property tests (`sessions_active_truth_table` at `:4568`, `v2_always_suppresses_sessions` at
      `:4585`, and the resumability equivalents at `:5879` and `:5894`). These must keep passing
      UNCHANGED
    - `src/server/streamable_http_server/v1_session.rs` and `v1_session_off.rs` as edited by Task 1
    - `src/types/protocol/version.rs` lines 45-90 (`Era` and `protocol_era`)
    - `.planning/phases/117-agents-tester-v1-severability/117-RESEARCH.md` § Q2.2 (the seven
      chokepoints table) and its Anti-Patterns entry "A second era resolver in the transport"
  </read_first>
  <action>
    Move all seven chokepoint functions plus the `EventStoreHandle` type alias (`:509`) from
    `streamable_http_server.rs` into `v1_session.rs`, and write the seven constant-answer twins in
    `v1_session_off.rs`.

    Preserve every signature EXACTLY, including the `era: Option<Era>` parameter that the null twins
    ignore. The twins take the `era` argument and ignore it; they do NOT drop it from the signature.
    Dropping it would invite a second era resolver at the call site, which Phase 112 D-11 and 113
    Pitfall 2 forbid — `streamable_http_server.rs:427-429` already carries that warning. Write that
    reason as a comment above the twins in `v1_session_off.rs`.

    Preserve the `const fn` pure-rule / `fn` state-reader split. `sessions_active_for` and
    `resumability_active_for` are `const fn` precisely so the RULE is unit- and property-testable
    without constructing a live `ServerState`. Do not flatten them into their callers.

    Move the truth-table doc comments verbatim with the functions — they ARE the specification the
    property tests assert against.

    The null twins' constant answers: `sessions_active_for` returns `false`, `sessions_active` returns
    `false`, `active_session_generator` returns `None`, `apply_session_header` returns `()` and writes
    NO header, `resumability_active_for` returns `false`, `resumability_active` returns `false`,
    `resumability_store` returns `None`. Make each a `const fn` where the signature allows.

    UPDATE the `:493-497` SEVERABILITY comment rather than deleting it. Its new text records that
    Phase 117 / SMPL-01 has now DONE what it handed forward: the trait, the store, the constant and
    the replay path are gated behind `v1-compat`, not deleted, because deletion is semver-major and
    belongs to SMPL-F1 / pmcp 3.0.

    The existing truth-table and property tests in the in-file `#[cfg(test)] mod tests` must keep
    passing with NO change to their bodies. If a test needs a new `use` import to see a moved
    function, adding only that import is acceptable; changing an assertion is not. If an assertion
    must change, STOP — that means the move changed behaviour, which is the failure this task exists
    to avoid, and it must be reported rather than accommodated.

    Do NOT move the session-lifecycle functions (`process_init_session`, `validate_non_init_session`,
    `extract_negotiated_version`, `update_session_after_init`,
    `validate_protocol_version_matches_session`, `is_initialize_request`,
    `resolve_session_for_request`), `store_response_event`, or any SSE-replay function in this task —
    those are plan 117-12's subject. Keeping the two moves in separate plans is deliberate: the
    chokepoints are the compile-time contract the later bodies are written against.

    Keep every moved function at or under cognitive complexity 25. The move should not concentrate
    branches; if it does, that is a signal the cut line is wrong, not a signal to add an `allow`.
  </action>
  <verify>
    <automated>cargo build -p pmcp --features full &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 &amp;&amp; cargo test --lib --features "full" &amp;&amp; cargo test --test v1_byte_identity_after_cut --features "full" &amp;&amp; cargo test --test v1_severability_tripwire</automated>
  </verify>
  <acceptance_criteria>
    - All seven chokepoint names appear in `v1_session.rs` and all seven appear in
      `v1_session_off.rs` — verified by grepping each name in both files and getting a non-zero count.
    - Each null twin's parameter list is textually identical to its real counterpart's, `era`
      parameter included — verified by comparing the two signature lines side by side and recording
      them in the summary.
    - `grep -c 'sessions_active_for\|resumability_active_for' src/server/streamable_http_server.rs`
      counted OUTSIDE the `#[cfg(test)]` module is 0 (the functions moved).
    - `cargo build -p pmcp --features full` exits 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
      with zero `warning:` lines.
    - `cargo test --lib --features "full"` pass count equals the pre-plan count EXACTLY, and the four
      named tests (`sessions_active_truth_table`, `v2_always_suppresses_sessions`, and the two
      resumability equivalents) all pass.
    - `git diff src/server/streamable_http_server.rs` shows, within the `#[cfg(test)] mod tests`
      region, ONLY added `use` lines — zero changed assertion bodies.
    - `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0.
    - `cargo test --test v1_severability_tripwire` exits 0, including plan 117-06 Task 3's SEMANTIC
      null-twin checks. Note explicitly: the twins MUST carry `sessions_active`, `sessions_active_for`,
      the `sessions_on` parameter, `cfg_has_event_store`, the `event_store` parameter and
      `EventStoreHandle` — 117-06's checks permit all six by design (see that plan's
      `<cross_plan_constraint>` block). What they forbid is holding state (`V1State` must stay a unit
      struct), performing a state or header operation, or declaring anything `v1_session.rs` does not
      also declare. If the tripwire rejects a required identifier, the TRIPWIRE is wrong — report it,
      do not rename the identifier.
    - The `:493-497` SEVERABILITY comment still exists and its text now says the gating is DONE.
    - `pmat analyze complexity --format json --max-cognitive 25` reports no NEW violation in
      `src/server/streamable_http_server.rs`, `v1_session.rs` or `v1_session_off.rs` (record the
      before and after violation lists for these three files).
  </acceptance_criteria>
  <done>
    The seven era chokepoints live in the paired module with byte-identical signatures and preserved
    truth-table docs; the existing property tests pass with unchanged bodies; the v1 goldens and the
    severability tripwire are green on both feature sets.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → HTTP transport | Session ids and era headers cross here; the chokepoints are where the era decision is consumed |
| refactor → shipped v1 behavior | A move that changes an era truth table changes who gets a session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-30 | Information Disclosure | `build_response`'s `sse_streams` send (`streamable_http_server.rs:1933-1938`) | mitigate | The `sessions_on` gate is preserved by moving `sessions_active` with its signature intact; `v2_response_is_never_routed_into_a_session_sse_stream` (`:6060`) must still run and pass — asserted by the unchanged `cargo test --lib` pass count |
| T-117-31 | Tampering | era resolution | mitigate | The null twins keep the `era` parameter and ignore it rather than dropping it, so no call site is tempted to re-resolve the era. Phase 112 D-11 / 113 Pitfall 2 forbid a second resolver; the reason is written as a comment above the twins |
| T-117-32 | Repudiation | the "v1 is unchanged" claim | mitigate | Proven by `tests/v1_byte_identity_after_cut.rs`, captured BEFORE the cut by plan 117-02, plus an EXACT (not "at least") in-file test pass-count match so a silently-dropped test is a failure |
| T-117-33 | Elevation of Privilege | `active_session_generator` / `apply_session_header` | mitigate | Both carry "THE single reader" / "the ONE place" doc comments that encode a single-writer invariant. The comments move verbatim with the functions so the invariant is not orphaned from its enforcement |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages added; this plan is a pure in-crate move |
</threat_model>

<verification>
- `cargo build -p pmcp --features full` exits 0.
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0 with
  zero warnings — reported SEPARATELY from `make quality-gate`, which runs `--all-features` and can
  never prove severance.
- `cargo test --lib --features "full"` pass count is EXACTLY the pre-plan count (recorded).
- `cargo test --test v1_byte_identity_after_cut --features "full"` exits 0.
- `cargo test --test v1_severability_tripwire` exits 0.
- `make doc-check` exits 0.
- `make quality-gate` exits 0.
- PMAT complexity before/after for the three touched files is recorded with no new violation.
</verification>

<success_criteria>
- `ServerState` has four fields; `V1State` holds the three v1-only ones and is a ZST under `full-v2`.
- There is exactly one `V1State` construction site and zero `#[cfg]` attributes at any call site.
- No accessor returns a reference to state the ZST twin cannot hold; the surviving `build_response`
  read is a `v1::` behavioural operation.
- All seven chokepoints live in the pair with textually identical signatures, `era` parameter
  included, and their truth-table doc comments moved verbatim.
- The in-file property tests pass with unchanged assertion bodies and an exact pass-count match.
- The pre-cut v1 goldens and the severability tripwire are green on both feature sets.
</success_criteria>

<output>
Create `.planning/phases/117-agents-tester-v1-severability/117-09-SUMMARY.md` when done.
Record: the re-derived field-access grep counts vs the research's measured 5/10/1, with each read
placed in one of the three buckets (moved-with-its-function / behavioural `v1::` operation /
deferred to 117-13); the chosen `route_to_session_stream` signature; the seven
signature pairs side by side; before/after `cargo test --lib` pass counts; and the before/after PMAT
complexity violation lists for the three touched files.
</output>
