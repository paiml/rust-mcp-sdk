---
phase: 90.1-openapi-built-in-server-examples-article-parity-finish-the-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml
  - crates/pmcp-openapi-server/examples/london-tube.toml
  - crates/pmcp-openapi-server/tests/parity_replay.rs
  - crates/pmcp-openapi-server/examples/london_tube_min.rs
autonomous: true
requirements: [P901-FIXTURE, P901-EXAMPLE]
must_haves:
  truths:
    - "Enriched london-tube.toml parses + validates through from_toml_strict_validated"
    - "The fixture ships three [[resources]] and one start_code_mode [[prompts]] block"
    - "Each tool's [tools.annotations] carries cost_hint (3-field SQL shape)"
    - "london_tube_fixture test asserts all three resource URIs + the prompt are present and cost_hint parses to an allowed value"
    - "A pointable examples/london-tube.toml ships with the published crate and parses+validates with the same resources/prompts/annotations surface"
    - "A runnable example (london_tube_min) demonstrating the resources/prompts-bearing config runs and exits without serving"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/fixtures/london-tube.toml"
      provides: "Enriched showcase config (resources + prompts + full annotations)"
      contains: "[[resources]]"
    - path: "crates/pmcp-openapi-server/examples/london-tube.toml"
      provides: "User-pointable showcase config (ships with crate; `--config` target)"
      contains: "[[resources]]"
    - path: "crates/pmcp-openapi-server/examples/london_tube_min.rs"
      provides: "Runnable build-and-exit example over the enriched config"
      contains: "build_server"
    - path: "crates/pmcp-openapi-server/tests/parity_replay.rs"
      provides: "Fixture-validity asserts for all three resource URIs + prompt + cost_hint value"
      contains: "docs://london-tube/schema"
  key_links:
    - from: "london-tube.toml [[prompts]].include_resources"
      to: "london-tube.toml [[resources]].uri"
      via: "URI exact-match (register_prompts resolves against StaticResourceHandler)"
      pattern: "docs://london-tube/schema"
    - from: "parity_replay.rs london_tube_fixture"
      to: "cfg.resources / cfg.prompts"
      via: "iter().any() presence + cost_hint value assert"
      pattern: "cfg\\.(resources|prompts)"
    - from: "examples/london-tube.toml"
      to: "examples/london_tube_min.rs CONFIG"
      via: "comment cross-reference; same enriched TfL surface kept in sync"
      pattern: "examples/london-tube.toml"
---

<objective>
Enrich the OpenAPI showcase fixture `london-tube.toml` to full SQL parity by adding
three `[[resources]]` blocks, one `start_code_mode` `[[prompts]]` block, and full
per-tool `[tools.annotations]` (adding `cost_hint` + `open_world_hint`). Extend the
`london_tube_fixture` test to assert the new surface parses (all three resource URIs +
prompt + cost_hint value). Ship TWO example artifacts: a user-pointable
`examples/london-tube.toml` config (runnable via `pmcp-openapi-server --config <path>`,
ships with the published crate) AND a runnable `london_tube_min.rs` cargo example that
mirrors the same enriched config.

Purpose: The london-tube fixture is the OpenAPI equivalent of SQL's `reference-config.toml`
showcase. Without resources/prompts/full annotations it does not demonstrate the
config-depth SQL ships. This closes scope items 1 + 2 of Phase 90.1 (P901-FIXTURE, P901-EXAMPLE).
Per the USER decision resolving the Codex-vs-Gemini divergence, P901-EXAMPLE delivers BOTH a
pointable `examples/london-tube.toml` (Codex: a config users can point at; unlike `tests/`,
the `examples/` dir ships with the published crate) AND the build-and-exit `london_tube_min.rs`
cargo example (Gemini: SQL `sql_server_min.rs` parity).
Output: enriched `tests/fixtures/london-tube.toml`, new pointable `examples/london-tube.toml`,
extended `parity_replay.rs::london_tube_fixture`, new `examples/london_tube_min.rs`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-RESEARCH.md
@.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-PATTERNS.md

<interfaces>
<!-- Config field names confirmed in crates/pmcp-server-toolkit/src/config.rs -->
ServerConfig.resources: Vec<ResourceDecl>   (config.rs:141)
ServerConfig.prompts:   Vec<PromptDecl>     (config.rs:137)

ResourceDecl fields (deny_unknown_fields, config.rs:765-785):
  uri (String, required), name (Option<String>), description (Option<String>),
  mime_type (Option<String>), content (Option<String>)

PromptDecl fields (deny_unknown_fields, config.rs:730-743):
  name (String), description (Option<String>),
  include_resources (Vec<String>, config.rs:739), arguments (Vec<PromptArgumentDecl>)

AnnotationsDecl fields (deny_unknown_fields, config.rs:701-721):
  read_only_hint (bool), destructive_hint (bool), idempotent_hint (bool),
  open_world_hint (bool), cost_hint (Option<String>)   // allowed values: "low" | "medium" | "high"

Example wiring entry points (crates/pmcp-openapi-server/examples/openapi_server_min.rs):
  pmcp_openapi_server::{build_server, dispatch}
  pmcp_server_toolkit::ServerConfig::from_toml_strict_validated(&str) -> Result<ServerConfig>
  dispatch(&cfg).await -> Result<(connector, http_exec)>
  build_server(&cfg, connector, http_exec, None) -> Result<Server>

Crate publishing note (crates/pmcp-openapi-server/Cargo.toml:14):
  exclude = [".planning/", ".pmat/", "fuzz/", "tests/"]
  → tests/fixtures/london-tube.toml is NOT published; examples/ IS published.
  This is WHY the pointable config must live under examples/, not tests/fixtures/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich london-tube.toml with resources, prompt, and full annotations</name>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml (the file being enriched — full)
    - crates/pmcp-sql-server/tests/fixtures/reference-config.toml lines 197-200, 224-227, 256-259 (annotation analog), 269-415 (resource analog), 661-670 (prompt analog)
    - .planning/phases/90.1-.../90.1-PATTERNS.md Patterns A/B/C (verbatim TfL excerpts)
  </read_first>
  <action>
    Edit `london-tube.toml` ADDITIVELY. Do NOT reformat or move existing blocks.

    (A) Enrich annotations (Pattern A): in `[tools.annotations]` for `get-tube-status`
    add `open_world_hint = true` and `cost_hint = "low"`. For `disrupted-lines-with-detail`
    add `idempotent_hint = true`, `open_world_hint = true`, and `cost_hint = "medium"`
    (multi-call fan-out). Use ONLY AnnotationsDecl fields listed in <interfaces> — a
    misspelled key is a hard parse error (deny_unknown_fields). `cost_hint` values MUST be
    one of the allowed strings "low" | "medium" | "high".

    (B) Add three `[[resources]]` blocks (Pattern B) AFTER the second tool's annotations
    and BEFORE the `[code_mode]` section, with exact `uri` values:
    `docs://london-tube/schema`, `docs://london-tube/examples`, `code-mode://learnings`.
    Each block sets `uri`, `name`, `description`, `mime_type = "text/markdown"`, and inline
    `content` (TfL-specific markdown: schema = endpoints `GET /Line/Mode/tube/Status` +
    `GET /Line/{lineId}/Disruption`, statusSeverity scale severity<10=disrupted, sample line
    ids victoria/central; examples = api.get script snippets; learnings = engine gotchas
    bind-to-const, lowercase line ids, args.maxLines). Mirror SQL STRUCTURE only — there is
    NO `[database]`/SQL content. Content is inert static markdown documentation (T-90.1-01: no
    executable payloads, no real secrets) — the server serves it as context, never runs it.

    (C) Add one `[[prompts]]` block (Pattern C) named `start_code_mode` with
    `include_resources` listing EXACTLY the three resource URIs above (Pitfall 2 /
    assemble.rs:204-213 resolves them — a typo points at a missing resource).

    LANDMINE: the line `base_url = "https://api.tfl.gov.uk"` (current line 37) must stay
    BYTE-IDENTICAL — `parity_replay.rs::temp_config_pointing_at` string-replaces it. Never
    reformat/re-indent/move it; add blocks around it.
  </action>
  <verify>
    <automated>cargo test -p pmcp-openapi-server --test parity_replay london_tube_fixture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[\[resources\]\]' crates/pmcp-openapi-server/tests/fixtures/london-tube.toml` returns 3
    - `grep -c '^\[\[prompts\]\]' crates/pmcp-openapi-server/tests/fixtures/london-tube.toml` returns 1
    - All three URIs `docs://london-tube/schema`, `docs://london-tube/examples`, `code-mode://learnings` each appear as a `[[resources]]` uri (grep each literal string)
    - `grep -q 'cost_hint = "low"' crates/pmcp-openapi-server/tests/fixtures/london-tube.toml` AND `grep -q 'cost_hint = "medium"' …` succeed (values are allowed enum strings, not arbitrary)
    - `grep -F 'base_url = "https://api.tfl.gov.uk"' crates/pmcp-openapi-server/tests/fixtures/london-tube.toml` matches the unchanged byte-identical line
    - The three `include_resources` URIs in the prompt are each present as a `[[resources]]` uri (no orphan reference)
    - No real `app_key`/credential committed — only `${TFL_APP_KEY}` placeholder + the dev-guarded inline token_secret remain
    - `cargo test -p pmcp-openapi-server --test parity_replay london_tube_fixture` exits 0
  </acceptance_criteria>
  <done>Enriched fixture parses through from_toml_strict_validated; 3 resources + 1 prompt + cost_hint annotations present with allowed values; base_url line byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Extend london_tube_fixture asserts (3 URIs + cost_hint value)</name>
  <read_first>
    - crates/pmcp-openapi-server/tests/parity_replay.rs lines 48-107 (london_tube_fixture) + 143-152 (temp_config_pointing_at — DO NOT edit)
    - crates/pmcp-server-toolkit/src/config.rs:701-721 (AnnotationsDecl — confirm cost_hint is Option<String> and the access path on the tool decl)
    - .planning/phases/90.1-.../90.1-PATTERNS.md (parity_replay extension section)
  </read_first>
  <action>
    In `parity_replay.rs::london_tube_fixture`, AFTER the existing tool-name asserts
    (after the `disrupted-lines-with-detail` assert), append presence/value asserts using the
    confirmed field names:

    (A) Resource-URI completeness — assert ALL THREE URIs are present (not only schema):
    `cfg.resources.iter().any(|r| r.uri == "docs://london-tube/schema")`,
    `… == "docs://london-tube/examples"`, and `… == "code-mode://learnings"`, each with a
    descriptive failure message that prints the collected `cfg.resources` URIs.

    (B) Prompt presence:
    `cfg.prompts.iter().any(|p| p.name == "start_code_mode")` with a failure message.

    (C) cost_hint VALUE check (not mere presence): locate the `get-tube-status` tool in
    `cfg.tools`, read its `annotations.cost_hint`, and assert it equals `Some("low")` (or the
    confirmed Option<String> shape per config.rs:701-721). Use an allowed-enum assertion: the
    parsed value MUST be one of "low" | "medium" | "high". This proves cost_hint parsed to its
    allowed value, not just that the field exists.

    Do NOT touch `temp_config_pointing_at` or the `REFERENCE_BASE_URL` constant (the
    byte-identical base_url anchor). Keep added test code cog ≤25, zero SATD.
  </action>
  <verify>
    <automated>cargo test -p pmcp-openapi-server --test parity_replay london_tube_fixture</automated>
  </verify>
  <acceptance_criteria>
    - `parity_replay.rs` contains all three literals `docs://london-tube/schema`, `docs://london-tube/examples`, `code-mode://learnings` in the london_tube_fixture asserts
    - `parity_replay.rs` contains `start_code_mode` in the london_tube_fixture asserts
    - `parity_replay.rs` contains a cost_hint VALUE assertion (compares against "low"/"medium"/"high"), not a mere `.is_some()` presence check
    - `cargo test -p pmcp-openapi-server --test parity_replay london_tube_fixture` exits 0 (asserts pass against the enriched fixture from Task 1)
    - `temp_config_pointing_at` and `REFERENCE_BASE_URL` are unchanged (grep the file: the constant string still equals `base_url = "https://api.tfl.gov.uk"`)
  </acceptance_criteria>
  <done>Fixture-validity test asserts all three resource URIs + the prompt are present and cost_hint parsed to an allowed enum value; test passes against the enriched fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Ship pointable examples/london-tube.toml + runnable london_tube_min.rs</name>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml (the enriched fixture from Task 1 — the surface to mirror)
    - crates/pmcp-sql-server/tests/fixtures/reference-config.toml (SQL pointable-config analog: the showcase a user points at)
    - crates/pmcp-openapi-server/examples/openapi_server_min.rs (full — sibling build-and-exit example; its header confirms `cargo run` does NOT hang)
    - crates/pmcp-sql-server/examples/sql_server_min.rs (inline-config + summary-print analog; build-and-exit shape)
    - .planning/phases/90.1-.../90.1-PATTERNS.md (london_tube_min.rs section)
  </read_first>
  <action>
    (A) Create `crates/pmcp-openapi-server/examples/london-tube.toml` — a user-pointable
    showcase config a user runs via the binary:
    `pmcp-openapi-server --config crates/pmcp-openapi-server/examples/london-tube.toml`.
    Mirror the enriched `tests/fixtures/london-tube.toml` surface: the same `[server]`,
    `[backend]`, two `[[tools]]` with full annotations (cost_hint "low"/"medium"), the SAME
    three `[[resources]]` (`docs://london-tube/schema`, `docs://london-tube/examples`,
    `code-mode://learnings`), and the SAME `start_code_mode` `[[prompts]]` block with matching
    `include_resources`. Because `examples/` SHIPS with the published crate (Cargo.toml:14
    excludes only `tests/`), this is the config "users can point at." It uses a placeholder
    `app_key` (`${TFL_APP_KEY}`) and the dev-guarded inline token_secret — NEVER a real
    credential (T-90.1-01). This file is NOT string-replaced by parity_replay.rs, so its
    `base_url` may use a normal `https://api.tfl.gov.uk` value (keep it consistent with the
    fixture, but the byte-identical landmine does NOT apply here).

    (B) Create `crates/pmcp-openapi-server/examples/london_tube_min.rs` — a runnable
    build-and-exit example mirroring `openapi_server_min.rs`: a doc header `//! cargo run
    --example london_tube_min -p pmcp-openapi-server` (its sibling header confirms run does not
    hang), plus a one-line comment pointing to `examples/london-tube.toml` as the standalone
    `--config` equivalent. Hold a trimmed enriched config in `const CONFIG: &str = r#"..."#;`
    that INCLUDES at least one `[[resources]]` block + the `start_code_mode` `[[prompts]]`
    block (the example's distinct value over openapi_server_min, kept close enough to
    `examples/london-tube.toml` that drift is obvious). A `#[tokio::main]` main calls
    `ServerConfig::from_toml_strict_validated(CONFIG)?`, `dispatch(&cfg).await?`,
    `build_server(&cfg, connector, http_exec, None)?`, prints a summary line
    (`cfg.server.name`, `cfg.tools.len()`, `cfg.resources.len()`, `cfg.prompts.len()`) and
    returns WITHOUT serving (never block on a listener — same as openapi_server_min). The
    CONFIG must use `allow_inline_token_secret_for_dev = true` so the example needs no env
    wiring; if any `${...}` env var is still required at wiring time, set it via
    `std::env::set_var` before wiring (analog sql_server_min.rs:25). `${TFL_APP_KEY}` stays a
    placeholder. Keep example code cog ≤25, zero SATD.

    Example gate (USER decision): prefer `cargo run --example london_tube_min` — the sibling
    `openapi_server_min.rs` header explicitly states even `cargo run` does not hang (main
    builds + exits, no live listener). Both `*_min.rs` examples in this repo build-and-return,
    so `cargo run` is CI-safe here.
  </action>
  <verify>
    <automated>cargo run --example london_tube_min -p pmcp-openapi-server</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/examples/london-tube.toml` exists and contains 3 `[[resources]]` blocks (the three URIs above) + 1 `start_code_mode` `[[prompts]]` block + `cost_hint` annotations (parse-mirror of the fixture surface)
    - `examples/london-tube.toml` parses+validates: `cargo run --example london_tube_min -p pmcp-openapi-server` succeeds AND a smoke parse of the pointable config holds — add a fixture-style assert OR a `--config` smoke check proving `ServerConfig::from_toml_strict_validated` accepts `examples/london-tube.toml` (e.g. a `#[test]` that reads the file and parses it, or a documented `pmcp-openapi-server --config examples/london-tube.toml` exit-0 note)
    - `examples/london-tube.toml` carries the SAME three resource URIs and the `start_code_mode` prompt as the enriched fixture (grep each literal)
    - `examples/london-tube.toml` contains NO real credential (only `${TFL_APP_KEY}` placeholder + dev-guarded token_secret)
    - `crates/pmcp-openapi-server/examples/london_tube_min.rs` exists, contains `build_server`, references `examples/london-tube.toml` in a comment, and embeds at least one `[[resources]]` + the `start_code_mode` `[[prompts]]` block in its CONFIG
    - `cargo run --example london_tube_min -p pmcp-openapi-server` exits 0 and returns without serving (does not hang)
  </acceptance_criteria>
  <done>A pointable examples/london-tube.toml ships with the crate and parses+validates with the full resources/prompts/annotations surface; a runnable london_tube_min.rs mirrors it, references it, and runs-and-exits cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed config/example → repo | Fixture, pointable example config, and Rust example are committed; secrets must never be real |
| config content → served MCP surface | Resource `content` is rendered as inert static markdown documentation, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.1-01 | Information Disclosure | `tests/fixtures/london-tube.toml`, `examples/london-tube.toml`, `london_tube_min.rs` `app_key` + token_secret | mitigate | All three artifacts keep `${TFL_APP_KEY}` placeholder (never a real key); token_secret stays the dev-only literal guarded by `allow_inline_token_secret_for_dev = true`. The new published `examples/london-tube.toml` is the highest-exposure artifact (it ships in the crate) — its acceptance criterion greps for absence of a real credential and presence of the `${TFL_APP_KEY}` placeholder. |
| T-90.1-02 | Tampering | `[[resources]]` `content` example scripts | accept | Scripts are inert illustrative TOML strings; the server serves them as static markdown documentation and never executes resource content. No execution path exists. |
| T-90.1-03 | Spoofing/Elevation | `[tools.annotations]` read_only/destructive hints | mitigate | All TfL endpoints are read-only GET; annotations set `read_only_hint=true`, no `destructive_hint=true`. Mis-annotating a GET tool as mutating-safe is avoided by setting only read-only hints. |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server --test parity_replay london_tube_fixture` exits 0
- `cargo run --example london_tube_min -p pmcp-openapi-server` exits 0 (build-and-exit, does not hang)
- `examples/london-tube.toml` parses through from_toml_strict_validated (smoke parse / `--config` check)
- base_url landmine line in tests/fixtures byte-identical; prompt include_resources URIs all resolve to defined resources in both fixture and pointable example
</verification>

<success_criteria>
- Enriched `tests/fixtures/london-tube.toml` ships 3 resources + 1 start_code_mode prompt + cost_hint annotations and validates; base_url anchor untouched
- New pointable `examples/london-tube.toml` ships with the published crate, carries the same resources/prompts/annotations surface, and parses+validates
- `london_tube_fixture` asserts all three resource URIs + the prompt + the cost_hint value and passes
- `london_tube_min.rs` runs-and-exits (CI-safe), references the pointable config, and demonstrates the resources/prompts surface
- No real secret committed in any of the three artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-01-SUMMARY.md`
</output>
</content>
</invoke>
