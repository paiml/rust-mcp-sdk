---
phase: 90.1-openapi-built-in-server-examples-article-parity-finish-the-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-book/src/openapi-built-in-server.md
  - pmcp-course/src/openapi-built-in-server.md
autonomous: true
requirements: [P901-DOCS-BOOK, P901-DOCS-COURSE]
must_haves:
  truths:
    - "The book chapter has a Resources & Prompts (Code Mode Context) section"
    - "The course chapter has the same section in its tighter voice"
    - "Both chapters show a [[resources]] + start_code_mode [[prompts]] TOML walkthrough"
    - "Both chapters lead with user-facing wiring framing and note resources are inert static markdown context"
    - "Both chapters carry a See-also cross-reference to the SQL server's reference-config.toml"
    - "Both books render (book via make book; course via mdbook build)"
  artifacts:
    - path: "pmcp-book/src/openapi-built-in-server.md"
      provides: "Resources/prompts config-walkthrough section"
      contains: "## Resources & Prompts"
    - path: "pmcp-course/src/openapi-built-in-server.md"
      provides: "Same section (course voice)"
      contains: "## Resources & Prompts"
  key_links:
    - from: "chapter [[prompts]].include_resources example"
      to: "chapter [[resources]].uri example"
      via: "matching URIs in the doc walkthrough (mirrors fixture)"
      pattern: "docs://london-tube/schema"
---

<objective>
Add a `## Resources & Prompts (Code Mode Context)` section to both the book and course
OpenAPI chapters — the one config-depth section SQL's chapters have that OpenAPI skips
(chapter structure is already at parity from Phase 90-09; this adds the missing depth).

Purpose: Closes scope item 3 of Phase 90.1 (P901-DOCS-BOOK, P901-DOCS-COURSE). Documents, in
user-facing terms first, that the server registers static resources and prompts at startup
from config (no recompile), with the `assemble.rs`/`resources_arc()`/`prompt_arc()` internals
kept as a secondary maintainer aside.
Output: a new section in each chapter, both mdbooks rendering.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-RESEARCH.md
@.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Resources & Prompts section to the book chapter</name>
  <read_first>
    - pmcp-book/src/openapi-built-in-server.md (the file being modified — locate the insertion point between `## Outgoing Authentication` ~line 173 and `## The OpenAPI Spec Is Optional (D-03)` ~line 175)
    - pmcp-book/src/ch12-10-config-driven-sql-servers.md lines 134-171 (§"Step 3: Customize Through Config" — the teaching-shape analog)
    - crates/pmcp-sql-server/tests/fixtures/reference-config.toml (the SQL pointable config the See-also link targets — confirm path + that its resources/prompts schema is identical)
    - .planning/phases/90.1-.../90.1-PATTERNS.md (book chapter section + Pattern B/C TfL excerpts)
  </read_first>
  <action>
    Insert a new `## Resources & Prompts (Code Mode Context)` section between the
    `## Outgoing Authentication` section and the `## The OpenAPI Spec Is Optional (D-03)`
    section. Mirror the teaching shape of ch12-10:134-171:
    (1) Lead with the `deny_unknown_fields` discipline note ("a misspelled key is a hard
    parse error, not a silent default").
    (2) Show a concrete `[[resources]]` block (uri `docs://london-tube/schema`,
    `mime_type = "text/markdown"`, inline content) + the `start_code_mode` `[[prompts]]`
    block whose `include_resources` lists the resource URIs. Reuse the EXACT TfL excerpts from
    PATTERNS.md Patterns B/C so the chapter stays in sync with the fixture and the course chapter.
    (3) Wiring explanation — USER-FACING FIRST: lead with "the server registers static
    resources and prompts at startup from config — change the TOML, restart, and the new
    surface is live (no recompile)." THEN, as a secondary maintainer aside (e.g. a short
    parenthetical or "Under the hood" sentence), name the internals: `assemble.rs` wires
    `cfg.resources` → `StaticResourceHandler` → `builder.resources_arc()` and `cfg.prompts` →
    `register_prompts` → `builder.prompt_arc()`. The internals are the aside, NOT the lead.
    (4) Add a one-sentence note that resources are INERT static markdown context (documentation
    the model reads), NOT executable scripts — the server never runs resource content.
    (5) Note the Pitfall-2 invariant: `include_resources` URIs must EXACTLY match defined
    `[[resources]]` uri values or the prompt resolves to a missing resource.
    (6) Add a "See also" cross-reference: link to the SQL server's
    `crates/pmcp-sql-server/tests/fixtures/reference-config.toml` (or the SQL chapter), noting
    the resources/prompts config schema is IDENTICAL across both config-driven servers.
    Match the chapter's existing voice (its `## Step 3` already uses a `[code_mode]`-style TOML
    walkthrough). Section heading wording is discretion but MUST contain "Resources" (the parity
    grep checks for it).
  </action>
  <verify>
    <automated>grep -q "Resources" pmcp-book/src/openapi-built-in-server.md && make book</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '## Resources' pmcp-book/src/openapi-built-in-server.md` succeeds
    - `grep -q 'docs://london-tube/schema' pmcp-book/src/openapi-built-in-server.md` succeeds (the resource URI from the walkthrough)
    - `grep -q 'start_code_mode' pmcp-book/src/openapi-built-in-server.md` succeeds (the prompt name)
    - `grep -q 'include_resources' pmcp-book/src/openapi-built-in-server.md` succeeds
    - The wiring explanation leads with user-facing framing ("registers static resources and prompts at startup from config"); the `assemble.rs`/`resources_arc`/`prompt_arc` names appear only as a secondary aside
    - A note that resources are inert static markdown context (not executable scripts) is present
    - A "See also" reference to `reference-config.toml` (the SQL pointable config) is present (grep for `reference-config.toml`)
    - `make book` exits 0 (project-script doc build per CLAUDE.md "prefer justfile/make over raw commands"; the `book` target runs `cd pmcp-book && mdbook build`)
    - The section sits between `## Outgoing Authentication` and `## The OpenAPI Spec Is Optional (D-03)`
    - No real `app_key`/credential value in the example block (placeholder only)
  </acceptance_criteria>
  <done>Book chapter has the Resources & Prompts walkthrough with user-facing wiring framing, inert-content note, See-also cross-ref, and matching resource/prompt URIs; `make book` renders.</done>
</task>

<task type="auto">
  <name>Task 2: Add the same section to the course chapter</name>
  <read_first>
    - pmcp-course/src/openapi-built-in-server.md (the file being modified — insertion point between `## The Spec Is Optional (D-03)` ~line 184 and `## Step 5: Deploy` ~line 191)
    - pmcp-course/src/part3-deployment/ch08-5-config-driven-sql-server.md §"Customize Through Config" (course-voice analog)
    - pmcp-book/src/openapi-built-in-server.md (the section just authored in Task 1 — reuse its TfL excerpts, user-facing framing, inert-note, and See-also verbatim for two-chapter sync)
  </read_first>
  <action>
    Insert a `## Resources & Prompts (Code Mode Context)` section between
    `## The Spec Is Optional (D-03)` and `## Step 5: Deploy`. Reuse the EXACT TfL
    `[[resources]]`/`[[prompts]]` TOML excerpts, the user-facing-first wiring explanation, the
    "inert static markdown context, not executable scripts" note, the Pitfall-2 URI-integrity
    point, and the "See also" cross-reference to the SQL `reference-config.toml` from the book
    section (Task 1) so the two chapters stay parity-aligned (the parity proof greps both for
    "Resources"). Tighten the prose to the course's voice but keep the same TOML blocks, the
    same `deny_unknown_fields` point, and the same See-also link. Section heading MUST contain
    "Resources".
  </action>
  <verify>
    <automated>grep -q "Resources" pmcp-course/src/openapi-built-in-server.md && (cd pmcp-course && mdbook build)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '## Resources' pmcp-course/src/openapi-built-in-server.md` succeeds
    - `grep -q 'docs://london-tube/schema' pmcp-course/src/openapi-built-in-server.md` succeeds
    - `grep -q 'start_code_mode' pmcp-course/src/openapi-built-in-server.md` succeeds
    - The user-facing-first wiring framing, the inert-content note, and the `reference-config.toml` See-also are present (same as the book section)
    - `(cd pmcp-course && mdbook build)` exits 0 — NOTE: there is no `make`/justfile target for the course book (only `make book` exists, which builds pmcp-book), so per CLAUDE.md "prefer project scripts" the explicit fallback is direct `mdbook build` in `pmcp-course` (this is the documented local chapter-render gate for the course)
    - The TfL resource/prompt TOML blocks match the book chapter's (same URIs, same prompt name)
    - No real credential value in the example block (placeholder only)
  </acceptance_criteria>
  <done>Course chapter has the matching Resources & Prompts section (user-facing framing, inert note, See-also); course mdbook builds via direct `mdbook build`; both chapters in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed docs → repo | Chapter TOML examples must not contain real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.1-01 | Information Disclosure | chapter `[backend.auth]`/`api_key` example TOML | mitigate | Documentation examples use the `${TFL_APP_KEY}` placeholder only — never a real key. Acceptance criterion forbids a committed credential value. |
| T-90.1-02 | Tampering | resource `content` example scripts in prose | accept | Doc example scripts are illustrative markdown, never executed; the chapter explicitly states resources are inert static markdown context, not executable scripts. No runtime path. |
</threat_model>

<verification>
- Both chapters grep-match "Resources" and the shared TfL URIs
- Book renders via `make book`; course renders via direct `mdbook build` in `pmcp-course`
- Both chapters carry the user-facing wiring framing, inert-content note, and `reference-config.toml` See-also
- Example TOML carries no real secret
</verification>

<success_criteria>
- Book + course chapters each gain the Resources & Prompts (Code Mode Context) section
- Both sections lead with user-facing wiring framing (internals as aside), note inert markdown context, and link to the SQL `reference-config.toml`
- Both sections share identical TfL resource/prompt TOML (two-chapter sync)
- Both mdbooks render (book via `make book`, course via `mdbook build`)
</success_criteria>

<output>
After completion, create `.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-02-SUMMARY.md`
</output>
</content>
</invoke>
