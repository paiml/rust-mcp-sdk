---
phase: 90.1-openapi-built-in-server-examples-article-parity-finish-the-o
plan: 01
subsystem: pmcp-openapi-server
tags: [openapi, showcase-config, resources, prompts, annotations, parity]
requires:
  - pmcp-server-toolkit ServerConfig (resources/prompts/annotations decls)
  - existing tests/fixtures/london-tube.toml + parity_replay.rs (Phase 90)
provides:
  - Enriched london-tube fixture (3 resources + start_code_mode prompt + cost_hint)
  - Pointable examples/london-tube.toml (ships with published crate)
  - Runnable examples/london_tube_min.rs (build-and-exit, resources/prompts-bearing)
  - Fixture + pointable-config validity asserts in parity_replay.rs
affects:
  - crates/pmcp-openapi-server (tests/fixtures, examples, tests)
tech-stack:
  added: []
  patterns:
    - "Mirror SQL reference-config.toml showcase shape onto OpenAPI london-tube"
    - "examples/ ships with crate; tests/ excluded — pointable config lives under examples/"
    - "build-and-exit example: main builds server, returns without serving (CI-safe)"
key-files:
  created:
    - crates/pmcp-openapi-server/examples/london-tube.toml
    - crates/pmcp-openapi-server/examples/london_tube_min.rs
    - .planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/deferred-items.md
  modified:
    - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml
    - crates/pmcp-openapi-server/tests/parity_replay.rs
decisions:
  - "P901-EXAMPLE delivers BOTH a pointable examples/london-tube.toml AND a runnable london_tube_min.rs (per USER decision resolving Codex-vs-Gemini divergence)"
  - "Resource content is inert static markdown (T-90.1-02 accept): served as context, never executed"
  - "All TfL tools read-only GET: read_only_hint=true, no destructive_hint (T-90.1-03 mitigate)"
metrics:
  duration: ~12m
  completed: 2026-05-29
  tasks: 3
  files-created: 3
  files-modified: 2
---

# Phase 90.1 Plan 01: Enrich london-tube showcase fixture + ship pointable example & runnable min example Summary

Brought the OpenAPI `london-tube` showcase to SQL-config parity: the fixture now ships
three `[[resources]]`, a `start_code_mode` `[[prompts]]` block, and full per-tool
annotations (`cost_hint` "low"/"medium" + `open_world_hint`); a new published
`examples/london-tube.toml` is the pointable equivalent and a runnable
`london_tube_min.rs` demonstrates the resources/prompts-bearing config build-and-exit.

## What Was Built

### Task 1 — Enrich `tests/fixtures/london-tube.toml` (commit `59f01810`)
- Added `open_world_hint = true` + `cost_hint = "low"` to `get-tube-status` and
  `idempotent_hint`/`open_world_hint`/`cost_hint = "medium"` to `disrupted-lines-with-detail`.
- Added three `[[resources]]` blocks (`docs://london-tube/schema`,
  `docs://london-tube/examples`, `code-mode://learnings`) as inert TfL markdown
  (endpoints, statusSeverity<10 scale, lowercase line ids, Code Mode gotchas).
- Added a `start_code_mode` `[[prompts]]` block whose `include_resources` lists
  exactly the three resource URIs (Pitfall 2 / no orphan reference).
- `base_url = "https://api.tfl.gov.uk"` kept byte-identical (parity_replay anchor).

### Task 2 — Extend `london_tube_fixture` asserts (commit `ce7ac9f5`)
- Asserts all three resource URIs present (loop over the URI set with a descriptive
  failure that prints the collected URIs).
- Asserts the `start_code_mode` prompt present.
- cost_hint VALUE assert: `get-tube-status` annotations `cost_hint == Some("low")`
  PLUS an allowed-enum `matches!(.., Some("low"|"medium"|"high"))` check (not a mere
  `.is_some()` presence check).
- `temp_config_pointing_at` / `REFERENCE_BASE_URL` left untouched.

### Task 3 — Pointable config + runnable example (commit `2562e555`)
- `examples/london-tube.toml`: full-surface pointable showcase (2 tools w/ full
  annotations, the same 3 resources, the same `start_code_mode` prompt). Ships with
  the crate (`examples/` is not in Cargo.toml `exclude`). `${TFL_APP_KEY}` placeholder
  + dev-guarded inline `token_secret` only — no real credential.
- `examples/london_tube_min.rs`: `#[tokio::main]` build-and-exit example over a
  trimmed enriched CONFIG (one tool + one `[[resources]]` + `start_code_mode` prompt),
  prints `name / tools.len() / resources.len() / prompts.len()`, references
  `examples/london-tube.toml` in a comment, never serves.
- `parity_replay.rs::pointable_example_config_parses_and_validates`: smoke test that
  reads `examples/london-tube.toml`, parses via `from_toml_strict_validated`, and
  asserts the three URIs + prompt + `${TFL_APP_KEY}` placeholder presence.

## Verification

| Check | Result |
|-------|--------|
| `cargo test -p pmcp-openapi-server --test parity_replay london_tube_fixture` | PASS |
| `cargo run --example london_tube_min -p pmcp-openapi-server` | exit 0, prints `1 tool(s), 1 resource(s), 1 prompt(s)`, does not hang |
| `cargo test -p pmcp-openapi-server --test parity_replay -- --test-threads=1` | 3 passed, 1 ignored (live), 0 failed |
| Full real-binary parity (`london_tube_parity_through_real_binary_path`) | PASS (base_url string-replace still works against enriched fixture) |
| Fixture greps (3 resources, 1 prompt, cost_hint low+medium, base_url byte-identical) | PASS |
| No real credential in either config (only `${TFL_APP_KEY}` + dev token_secret) | PASS |

## Deviations from Plan

None — plan executed as written. Two pre-existing, out-of-scope issues were
discovered in files this plan did not touch and logged to `deferred-items.md`
(NOT fixed, per the SCOPE BOUNDARY rule):
- `cargo fmt --check` flags `src/dispatch.rs:107` (byte-identical to base commit).
- `clippy::redundant_guards` warning in `pmcp-server-toolkit/src/config.rs:526`
  (toolkit crate; not clippy-gated in CI per repo MEMORY).

## Threat Model Outcomes

- **T-90.1-01 (Information Disclosure):** mitigated — all three artifacts keep the
  `${TFL_APP_KEY}` placeholder and a dev-only guarded `token_secret`; the published
  `examples/london-tube.toml` smoke test asserts the placeholder is present.
- **T-90.1-02 (Tampering):** accepted — resource `content` is inert markdown served
  as context, never executed.
- **T-90.1-03 (Spoofing/Elevation):** mitigated — every TfL tool sets only
  `read_only_hint=true` (GET-only), no `destructive_hint`.

## Known Stubs

None. The fixture and pointable config carry real (non-empty) resource content,
prompt wiring, and annotation values; the runnable example wires real dispatch +
build_server and prints live counts.

## Self-Check: PASSED

All 5 created/modified files exist on disk and all 4 commits (59f01810, ce7ac9f5, 2562e555, 4bc68c47) are present in git history. STATE.md and ROADMAP.md were not modified (orchestrator owns those).
