---
phase: 98-deploy-stack-ts-regeneration-guard-config-driven-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cargo-pmcp/src/deployment/config.rs
  - cargo-pmcp/tests/deploy_stack_ts_guard.rs
autonomous: true
requirements: [DSTK-02]
gap_closure: true
must_haves:
  truths:
    - "A curated deploy/lib/stack.ts is left byte-for-byte unchanged by the regenerate path when no regenerate flag is set (regression test asserts this and currently FAILS-RED before Plan 98-02 lands)"
    - "A [metadata] block (server_type, snapshot_baked) parses from .pmcp/deploy.toml into DeployConfig with serde defaults that preserve byte-identity when absent"
    - "DeployConfig carries a non-persisted regenerate_stack runtime flag that defaults to false and never serializes"
  artifacts:
    - path: "cargo-pmcp/src/deployment/config.rs"
      provides: "MetadataConfig struct + DeployConfig.metadata field + DeployConfig.regenerate_stack non-persisted field"
      contains: "struct MetadataConfig"
    - path: "cargo-pmcp/tests/deploy_stack_ts_guard.rs"
      provides: "RED regression tests reproducing the overwrite + config-metadata defects"
      contains: "preserved"
  key_links:
    - from: "cargo-pmcp/tests/deploy_stack_ts_guard.rs"
      to: "cargo_pmcp::deployment::config::DeployConfig"
      via: "toml::from_str into DeployConfig, assert metadata block parses"
      pattern: "MetadataConfig|\\[metadata\\]"
---

<objective>
Establish the shared config contract for Phase 98 and reproduce the defect as a failing regression test BEFORE any fix lands.

This plan adds the `[metadata]` block (`server_type`, `snapshot_baked`) to `DeployConfig` and a non-persisted `regenerate_stack` runtime flag — both are the shared surface that Plans 98-02 (guard + flag) and 98-03 (metadata plumbing) build on. Putting BOTH `config.rs` additions here makes 98-02 and 98-03 not collide on `config.rs`.

It also lands a regression test file that reproduces the two defects from `.planning/debug/deploy-overwrites-stack-ts.md`: (1) a curated `stack.ts` is silently overwritten, and (2) `[metadata]` literals are unreproducible-from-config. These tests are RED now and turn GREEN as 98-02/98-03 land.

Purpose: Lock the config contract and make the bug observable/regression-guarded before fixing it (Toyota Way: write the failing test first).
Output: `MetadataConfig` + `DeployConfig.metadata` + `DeployConfig.regenerate_stack` (config.rs); `tests/deploy_stack_ts_guard.rs` (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-deploy-stack-ts-regeneration-guard-config-driven-metadata/98-CONTEXT.md
@.planning/debug/deploy-overwrites-stack-ts.md

<interfaces>
<!-- DeployConfig already uses #[serde(skip)] for non-persisted runtime fields (project_root)
     and #[serde(default, skip_serializing_if = ...)] for optional blocks that must round-trip
     byte-identically when absent. Mirror BOTH patterns. Extracted from config.rs. -->

From cargo-pmcp/src/deployment/config.rs (existing DeployConfig field patterns to mirror):
- `#[serde(default, skip_serializing_if = "IamConfig::is_empty")] pub iam: IamConfig` — optional block, byte-identity preserved when empty (the D-05 contract)
- `#[serde(skip)] pub project_root: PathBuf` — non-persisted runtime-only field (never read from / written to toml)

DeployConfig is `#[derive(Debug, Clone, Serialize, Deserialize)]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add [metadata] config block + non-persisted regenerate_stack flag to DeployConfig</name>
  <files>cargo-pmcp/src/deployment/config.rs</files>
  <read_first>
    cargo-pmcp/src/deployment/config.rs (read lines 1-160 — the DeployConfig struct and its serde-field patterns; line numbers in CONTEXT are approximate, read current state)
  </read_first>
  <behavior>
    - A deploy.toml WITHOUT a `[metadata]` block deserializes into DeployConfig with `metadata` defaulting to all-None and re-serializes byte-identically (skip_serializing_if on the block / its fields)
    - A deploy.toml WITH `[metadata]\nserver_type = "graph-rag"\nsnapshot_baked = true` deserializes so `config.metadata.server_type == Some("graph-rag")` and `config.metadata.snapshot_baked == Some(true)`
    - `DeployConfig::default_for_server(...)` produces `metadata` = all-None and `regenerate_stack == false`
    - `regenerate_stack` is `#[serde(skip)]` — setting it true and serializing produces no `regenerate_stack` key in the toml
  </behavior>
  <action>
    Add a new `MetadataConfig` struct in config.rs: `#[derive(Debug, Clone, Default, Serialize, Deserialize)]` with two fields, `server_type: Option<String>` and `snapshot_baked: Option<bool>`, each `#[serde(default, skip_serializing_if = "Option::is_none")]`. Add an `is_empty(&self) -> bool` method returning true when BOTH fields are None (mirror the `IamConfig::is_empty` pattern).

    Add to `DeployConfig`: a `pub metadata: MetadataConfig` field annotated `#[serde(default, skip_serializing_if = "MetadataConfig::is_empty")]` so absent `[metadata]` blocks round-trip byte-identically (preserves the no-opt-in backward-compat contract from CONTEXT.md per DSTK-02).

    Also add to `DeployConfig`: a `pub regenerate_stack: bool` field annotated `#[serde(skip)]` — this is the runtime opt-out carrier (set by the `--regenerate-stack`/`--force` flag in Plan 98-02), mirroring the existing `#[serde(skip)] project_root` non-persisted pattern. It must default to `false`.

    Ensure every `DeployConfig { ... }` literal constructor in config.rs (e.g. `default_for_server`, `default_for_cloud_run_server`, any others the compiler flags) initializes `metadata: MetadataConfig::default()` and `regenerate_stack: false`. Do NOT widen any other struct. Keep cognitive complexity ≤25; zero SATD comments.
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp 2>&1 | grep -qv "error\[" && cargo test -p cargo-pmcp --lib deployment::config 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `MetadataConfig { server_type, snapshot_baked }` exists with both fields `Option`, `Default`, and serde skip-if-none
    - `DeployConfig.metadata: MetadataConfig` and `DeployConfig.regenerate_stack: bool` (`#[serde(skip)]`) compile and all existing config constructors are updated
    - `cargo build -p cargo-pmcp` succeeds with zero errors
  </acceptance_criteria>
  <done>config.rs carries the MetadataConfig block + DeployConfig.metadata + DeployConfig.regenerate_stack; crate compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED regression tests reproducing the overwrite + config-metadata defects</name>
  <files>cargo-pmcp/tests/deploy_stack_ts_guard.rs</files>
  <read_first>
    cargo-pmcp/tests/deploy_config_only.rs (read in full — mirror its integration-test harness style: how it builds a DeployConfig, writes a temp .pmcp/deploy.toml, and asserts)
    cargo-pmcp/tests/backward_compat_stack_ts.rs (read in full — golden-file reading helpers; the test module doc explains where render_stack_ts is reachable)
    cargo-pmcp/src/deployment/config.rs (re-confirm MetadataConfig + DeployConfig public surface from Task 1)
  </read_first>
  <behavior>
    - Test A (overwrite guard, EXPECTED RED until 98-02): write a curated `deploy/lib/stack.ts` containing the literals `mcp:serverType:'graph-rag'` and `mcp:snapshotBaked:'true'` into a TempDir; build a DeployConfig with `regenerate_stack = false`; invoke the public regenerate entrypoint; assert the file content is UNCHANGED (still contains the curated literals). This FAILS today because the write is unconditional.
    - Test B (config metadata parse, EXPECTED GREEN from Task 1): a deploy.toml string with `[metadata] server_type="graph-rag", snapshot_baked=true` parses into a DeployConfig whose metadata fields equal Some("graph-rag")/Some(true). This passes once Task 1 lands and guards DSTK-02 parse.
    - Test C (config-survives-render, EXPECTED RED until 98-03): with `regenerate_stack = true` and `[metadata]` set, the rendered/synth-context output advertises `mcp:serverType` = graph-rag and `mcp:snapshotBaked` = true. FAILS today (render path threads only IAM).
  </behavior>
  <action>
    Create `cargo-pmcp/tests/deploy_stack_ts_guard.rs` as an integration test (only `pub`/`pub(crate)` surface is reachable — use the same entry points `tests/deploy_config_only.rs` uses; if the regenerate entrypoint is not yet public, assert via the deploy executor or `render_stack_ts_for_deploy` re-export the existing tests use, and mark the as-yet-unreachable assertions behind a clearly-named RED test that 98-02/98-03 will make reachable + green).

    Write Test A, Test B, Test C per `<behavior>`. Each test MUST carry a doc comment naming which DSTK requirement and which plan turns it GREEN (e.g. "RED until 98-02 lands the exists-guard"). Use `tempfile::tempdir()` for filesystem isolation (mirror the pmcp_run deploy unit tests). Reference the curated literals exactly as `.planning/debug/deploy-overwrites-stack-ts.md` describes them so the regression maps 1:1 to the reported bug.

    These tests are intentionally RED now. To keep `make quality-gate`/`cargo test` green for THIS plan's CI, gate the not-yet-implementable assertions (A and C) behind `#[ignore]` with an inline reason string pointing at the plan that un-ignores them, OR structure them as `#[should_panic]`/documented-pending — pick whichever keeps the suite green while still committing the reproduction. Test B must NOT be ignored (it is green from Task 1). Document the un-ignore handoff in the file-level module doc.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test deploy_stack_ts_guard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tests/deploy_stack_ts_guard.rs` exists with Tests A, B, C, each documenting its DSTK requirement + un-ignore plan
    - Test B (config `[metadata]` parse) passes
    - Tests A and C are committed and either `#[ignore]`-gated with an un-ignore-plan reason or otherwise keep the suite green
    - `cargo test -p cargo-pmcp --test deploy_stack_ts_guard` exits 0
  </acceptance_criteria>
  <done>Regression file committed; defect reproduced and traceably handed to 98-02/98-03; suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator `.pmcp/deploy.toml` → DeployConfig | untrusted operator-authored TOML is parsed into the config struct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | `[metadata]` TOML parse in DeployConfig | mitigate | serde `Option`/`skip_serializing_if` defaults; malformed `[metadata]` yields a parse Err, never a panic (fuzz coverage added in 98-04) |
| T-98-02 | Denial-of-service | adversarial `[metadata]` TOML | accept | `toml::from_str` returns Err on adversarial input; bounded by the existing config-parse fuzz target extended in 98-04; no unbounded allocation introduced |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | no new package-manager installs in this phase (cargo-pmcp deps unchanged); slopcheck N/A — no install tasks |
</threat_model>

<verification>
- `cargo build -p cargo-pmcp` compiles with the new MetadataConfig + DeployConfig fields
- `tests/deploy_stack_ts_guard.rs` Test B (metadata parse) is green; A/C committed as documented-RED
- Absent `[metadata]` block: a DeployConfig round-trips byte-identically (no `regenerate_stack` or `[metadata]` keys serialized)
</verification>

<success_criteria>
- `MetadataConfig` + `DeployConfig.metadata` + `DeployConfig.regenerate_stack` exist and compile
- Defect reproduced as committed regression tests with explicit DSTK + un-ignore-plan handoff
- Backward compatibility: no serialized output change for configs that do not opt into `[metadata]`
</success_criteria>

<output>
Create `.planning/phases/98-deploy-stack-ts-regeneration-guard-config-driven-metadata/98-01-SUMMARY.md` when done
</output>
