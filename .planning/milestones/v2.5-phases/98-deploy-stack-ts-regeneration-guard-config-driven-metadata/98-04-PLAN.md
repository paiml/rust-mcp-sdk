---
phase: 98-deploy-stack-ts-regeneration-guard-config-driven-metadata
plan: 04
type: execute
wave: 4
depends_on: ["98-01", "98-02", "98-03"]
files_modified:
  - cargo-pmcp/fuzz/fuzz_targets/fuzz_metadata_config.rs
  - cargo-pmcp/fuzz/Cargo.toml
  - cargo-pmcp/tests/deploy_stack_ts_guard.rs
  - cargo-pmcp/tests/golden/pmcp-run-empty.ts
  - cargo-pmcp/tests/golden/aws-lambda-empty.ts
  - cargo-pmcp/examples/deploy_stack_metadata.rs
  - cargo-pmcp/docs/commands/deploy.md
autonomous: true
requirements: [DSTK-04]
gap_closure: true
must_haves:
  truths:
    - "A property test proves a config-declared server_type/snapshot_baked round-trips through render/to_cdk_context for arbitrary valid inputs"
    - "A fuzz target exercises [metadata] block TOML parsing without panicking"
    - "The golden files reflect the additive mcp:snapshotBaked template line (template change is byte-stable for non-opting servers; the new conditional line is the only diff)"
    - "A runnable example demonstrates the exists-guard + [metadata] reproduction workflow"
    - "deploy.md documents --regenerate-stack/--force and the [metadata] block"
    - "make quality-gate passes"
  artifacts:
    - path: "cargo-pmcp/fuzz/fuzz_targets/fuzz_metadata_config.rs"
      provides: "fuzz target for [metadata] / DeployConfig TOML parse"
      contains: "from_str"
    - path: "cargo-pmcp/examples/deploy_stack_metadata.rs"
      provides: "runnable cargo run --example demonstrating guard + config metadata"
      contains: "snapshot_baked"
    - path: "cargo-pmcp/docs/commands/deploy.md"
      provides: "--regenerate-stack/--force + [metadata] documentation"
      contains: "regenerate-stack"
  key_links:
    - from: "cargo-pmcp/tests/deploy_stack_ts_guard.rs"
      to: "render_stack_ts / to_cdk_context"
      via: "property test (proptest) over MetadataConfig inputs"
      pattern: "proptest|prop_"
    - from: "cargo-pmcp/tests/golden/pmcp-run-empty.ts"
      to: "init.rs template"
      via: "UPDATE_GOLDEN regeneration after the additive mcp:snapshotBaked line"
      pattern: "mcp:snapshotBaked|McpRoleArn"
---

<objective>
Close DSTK-04: complete the ALWAYS coverage mandated by CLAUDE.md (fuzz + property + unit + example + doctest/doc) and bring the golden files current for the additive template change — then prove `make quality-gate` passes.

98-01..03 added unit tests inline. This plan adds the remaining ALWAYS artifacts: a property test that a config-declared `server_type`/`snapshot_baked` survives into render/to_cdk_context for arbitrary inputs, a fuzz target for `[metadata]` TOML parsing, the golden-file update for the additive `mcp:snapshotBaked` line, a runnable example, and the `--regenerate-stack`/`--force` + `[metadata]` documentation.

Purpose: Satisfy the repo's non-negotiable ALWAYS requirements and the merge bar; lock the additive template change into the golden corpus.
Output: fuzz target + Cargo.toml entry, property test, regenerated goldens, example, deploy.md doc; green `make quality-gate`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-deploy-stack-ts-regeneration-guard-config-driven-metadata/98-CONTEXT.md
@.planning/phases/98-deploy-stack-ts-regeneration-guard-config-driven-metadata/98-01-SUMMARY.md
@.planning/phases/98-deploy-stack-ts-regeneration-guard-config-driven-metadata/98-02-SUMMARY.md
@.planning/phases/98-deploy-stack-ts-regeneration-guard-config-driven-metadata/98-03-SUMMARY.md

<interfaces>
<!-- Patterns to mirror, extracted from the repo. -->

From cargo-pmcp/fuzz/fuzz_targets/pmcp_config_toml_parser.rs (the existing config-parse fuzz style to mirror):
- `#![no_main]` + `use libfuzzer_sys::fuzz_target;`
- body: `let Ok(s) = std::str::from_utf8(data) else { return; }; let _: Result<T, _> = toml::from_str(s);`
- requires a `[[bin]]` entry in cargo-pmcp/fuzz/Cargo.toml (name + path + bench=false, doc=false) — mirror the existing pmcp_config_toml_parser entry.

From cargo-pmcp/tests/backward_compat_stack_ts.rs:
- goldens live at `cargo-pmcp/tests/golden/pmcp-run-empty.ts` and `cargo-pmcp/tests/golden/aws-lambda-empty.ts`
- regenerated via `UPDATE_GOLDEN=1 cargo test -p cargo-pmcp -- golden` (the `golden_*` tests in init.rs `wave1_stack_ts_tests` write them)
- the golden corpus is the EMPTY-metadata case → the additive `mcp:snapshotBaked` conditional line must NOT change the empty-case output if 98-03's conditional-add is correct; if the `const mcpSnapshotBaked = …` declaration line is unconditional, the golden gets exactly that additive line and nothing else.

From cargo-pmcp/examples/deploy_with_iam.rs (example style to mirror):
- module doc with `Run with: cargo run -p cargo-pmcp --example <name>`
- `use cargo_pmcp::deployment::config::DeployConfig;` + render/validate calls
- uses `include_str!("fixtures/<name>.deploy.toml")` for fixtures

From cargo-pmcp/docs/commands/deploy.md:
- "Deploy Options" table (lines ~17-24) lists `--target`, `--shared-pool`, `--no-oauth` — add `--regenerate-stack` row here
- add a `[metadata]` config section mirroring how other deploy.toml blocks are documented
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Property test (config-survives-render) + fuzz target for [metadata] TOML parse</name>
  <files>cargo-pmcp/tests/deploy_stack_ts_guard.rs, cargo-pmcp/fuzz/fuzz_targets/fuzz_metadata_config.rs, cargo-pmcp/fuzz/Cargo.toml</files>
  <read_first>
    cargo-pmcp/tests/deploy_stack_ts_guard.rs (current state after 98-01..03 — add the property test here)
    cargo-pmcp/fuzz/fuzz_targets/pmcp_config_toml_parser.rs (mirror exactly)
    cargo-pmcp/fuzz/Cargo.toml (read the [[bin]] entries to add a parallel one)
    cargo-pmcp/Cargo.toml (confirm proptest is already a dev-dependency; the repo uses proptest per CLAUDE.md ALWAYS — if absent under cargo-pmcp, check the workspace dev-deps)
  </read_first>
  <behavior>
    - Property test: for an arbitrary `server_type` String (restricted to a sane charset, non-empty) and arbitrary `snapshot_baked` bool, a DeployConfig carrying that `[metadata]` produces a render/to_cdk_context output that advertises exactly those values (server_type present; snapshotBaked present iff true). Round-trip invariant holds across the generated input space.
    - Fuzz target: arbitrary bytes → utf8 → `toml::from_str::<DeployConfig>` (or a `[metadata]`-focused struct) never panics (returns Ok or Err).
  </behavior>
  <action>
    Add a proptest-based property test to `tests/deploy_stack_ts_guard.rs` (gate with `#[cfg(test)]`/proptest macro as the repo does elsewhere). Generate `server_type` from a bounded ASCII-alnum-plus-dash strategy (avoid characters that would break the single-quoted `-c '…'` shell arg, since to_cdk_context single-quotes values) and `snapshot_baked: any::<bool>()`. Build a DeployConfig (via `default_for_server` + set `config.metadata`), apply the override + render/to_cdk_context, and assert: the output contains `mcp:serverType={server_type}`; it contains `mcp:snapshotBaked=true` iff `snapshot_baked`. This is the DSTK-04 config-survives-render property test.

    Create `cargo-pmcp/fuzz/fuzz_targets/fuzz_metadata_config.rs` mirroring `pmcp_config_toml_parser.rs`: `#![no_main]`, `fuzz_target!(|data: &[u8]| { let Ok(s) = std::str::from_utf8(data) else { return; }; let _: Result<cargo_pmcp::deployment::config::DeployConfig, _> = toml::from_str(s); })`. Use whatever public re-export path the existing fuzz target uses (it references `cargo_pmcp::test_support::...`; use the equivalent public path for DeployConfig — if DeployConfig is not publicly reachable from the fuzz crate, target a small public `[metadata]`-parse wrapper or the existing config re-export). Add the module doc noting the threat model (T-98-01/02 parser DoS) and the `cargo +nightly fuzz run fuzz_metadata_config` invocation, mirroring the existing target's doc.

    Add the matching `[[bin]]` entry to `cargo-pmcp/fuzz/Cargo.toml` (name = "fuzz_metadata_config", path, `test = false`, `doc = false`, `bench = false`) mirroring the existing entries.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test deploy_stack_ts_guard 2>&1 | tail -8 && (cd cargo-pmcp/fuzz && cargo +nightly check --bin fuzz_metadata_config 2>&1 | tail -5 || echo "nightly-check-skipped-on-stable")</automated>
  </verify>
  <acceptance_criteria>
    - proptest config-survives-render property test passes over the generated input space
    - `fuzz_metadata_config.rs` exists, mirrors the existing fuzz style, and is registered in fuzz/Cargo.toml
    - fuzz target compiles under `cargo +nightly check --bin fuzz_metadata_config` (or documented stable-skip)
  </acceptance_criteria>
  <done>Property + fuzz ALWAYS coverage present for the config-driven metadata path</done>
</task>

<task type="auto">
  <name>Task 2: Golden-file update, runnable example, and deploy.md documentation</name>
  <files>cargo-pmcp/tests/golden/pmcp-run-empty.ts, cargo-pmcp/tests/golden/aws-lambda-empty.ts, cargo-pmcp/examples/deploy_stack_metadata.rs, cargo-pmcp/docs/commands/deploy.md</files>
  <read_first>
    cargo-pmcp/tests/backward_compat_stack_ts.rs (the regeneration command + invariants it asserts)
    cargo-pmcp/tests/golden/pmcp-run-empty.ts (current golden — confirm the diff after 98-03's template change is ONLY the additive snapshotBaked line)
    cargo-pmcp/examples/deploy_with_iam.rs (example skeleton to mirror)
    cargo-pmcp/docs/commands/deploy.md (lines 1-60 — the options table + structure)
  </read_first>
  <behavior>
    - Goldens regenerated so `cargo test -p cargo-pmcp -- golden` passes; the only diff vs the prior golden is the additive `mcp:snapshotBaked` template handling line(s) from 98-03 (empty-metadata case still emits NO `mcp:snapshotBaked` key value, matching the conditional-add).
    - `cargo run -p cargo-pmcp --example deploy_stack_metadata` runs to completion, printing: (1) a deploy.toml with `[metadata] server_type="graph-rag", snapshot_baked=true` parsing into DeployConfig; (2) the rendered/synth-context output advertising both literals; (3) a demonstration that a pre-existing stack.ts is preserved without the flag.
    - deploy.md documents `--regenerate-stack` (and its `--force` alias) and the `[metadata]` block.
  </behavior>
  <action>
    Regenerate the golden files: run `UPDATE_GOLDEN=1 cargo test -p cargo-pmcp -- golden`, then run `cargo test -p cargo-pmcp -- golden` to confirm green. Inspect the diff and confirm it is ONLY the additive `mcp:snapshotBaked` handling introduced in 98-03 (per CONTEXT.md backward-compat: the golden updates only for the additive line). If the diff shows any unexpected change to the empty-metadata case, that signals 98-03's conditional-add is wrong — STOP and flag it in the SUMMARY rather than committing a breaking golden.

    Create `cargo-pmcp/examples/deploy_stack_metadata.rs` mirroring `deploy_with_iam.rs`: module doc with the `cargo run -p cargo-pmcp --example deploy_stack_metadata` invocation; parse an inline or `include_str!("fixtures/…")` deploy.toml that sets `[metadata] server_type="graph-rag"` + `snapshot_baked=true`; build the DeployConfig, apply the override, and print the rendered stack/to_cdk_context output showing `mcp:serverType:'graph-rag'` + `mcp:snapshotBaked`. Then demonstrate the exists-guard: write a curated stack.ts to a TempDir, call the guarded write with `regenerate_stack=false`, and print that it was preserved. Use only public API surface. If a fixture toml is used, add it under `examples/fixtures/`.

    Update `cargo-pmcp/docs/commands/deploy.md`: add a `--regenerate-stack` row to the Deploy Options table (note the `--force` alias and that it overwrites an existing `deploy/lib/stack.ts`; default behavior preserves it). Add a `### Config-driven stack metadata (`[metadata]`)` subsection documenting `server_type` and `snapshot_baked`, that they make `mcp:serverType`/`mcp:snapshotBaked` reproducible-from-config, and that absent the block behavior is unchanged. Keep prose consistent with the existing doc style.

    Finally run `make quality-gate` and ensure it passes (fmt --all, clippy pedantic+nursery, build, test, audit). This is the merge bar per CLAUDE.md.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp -- golden 2>&1 | tail -6 && cargo run -p cargo-pmcp --example deploy_stack_metadata 2>&1 | tail -8 && grep -q "regenerate-stack" cargo-pmcp/docs/commands/deploy.md && echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - goldens regenerated and green; diff is ONLY the additive snapshotBaked line (no breaking change to empty case)
    - `cargo run -p cargo-pmcp --example deploy_stack_metadata` runs to completion demonstrating guard + config metadata
    - deploy.md documents `--regenerate-stack`/`--force` and the `[metadata]` block
    - `make quality-gate` passes
  </acceptance_criteria>
  <done>ALWAYS coverage complete (golden + example + doc); merge bar green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator `.pmcp/deploy.toml` `[metadata]` → DeployConfig parse | adversarial TOML parsed by the fuzz target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | `[metadata]` TOML parse | mitigate | fuzz_metadata_config exercises adversarial bytes; `toml::from_str` returns Err, never panics (this task lands the coverage) |
| T-98-08 | Tampering | golden corpus drift hiding a breaking template change | mitigate | golden regeneration + diff inspection; STOP-and-flag rule if the empty case changes beyond the additive line |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs (proptest/libfuzzer-sys already in dev/fuzz deps); slopcheck N/A |
</threat_model>

<verification>
- property test (config-survives-render) green; fuzz target compiles/registered
- goldens green with only the additive snapshotBaked diff
- `cargo run --example deploy_stack_metadata` succeeds
- deploy.md documents the flag + `[metadata]`
- `make quality-gate` passes
</verification>

<success_criteria>
- DSTK-04 met: fuzz + property + unit (from 98-01..03) + example + golden update + doc all present and green
- `make quality-gate` passes — the phase is mergeable
</success_criteria>

<output>
Create `.planning/phases/98-deploy-stack-ts-regeneration-guard-config-driven-metadata/98-04-SUMMARY.md` when done
</output>
