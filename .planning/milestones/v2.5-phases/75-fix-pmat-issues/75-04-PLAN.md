---
phase: 75-fix-pmat-issues
plan: 04
type: execute
wave: 4
depends_on: ["75-03"]
files_modified:
  - crates/mcp-tester/src/diagnostics.rs
  - crates/mcp-tester/src/main.rs
  - crates/mcp-preview/src/handlers/websocket.rs
  - crates/mcp-preview/src/handlers/api.rs
  - crates/pmcp-server/pmcp-server-lambda/src/main.rs
  - .planning/phases/75-fix-pmat-issues/75-04-EXAMPLES-DECISION.md
  - .planning/phases/75-fix-pmat-issues/75-04-ESCALATION.md  # only created if chosen_path: (c)
  - .planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md
  - .planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md
  # examples/** and fuzz/** entries below are runtime-determined under examples/ and fuzz/,
  # scoped by Wave 0 spike result (75-W0-SPIKE-RESULTS.md::chosen_path) AND
  # PMAT-allow behavior (pmat-allow-behavior.md::outcome). See Tasks 4-B-A/B/C/D for the
  # branching logic.
  - examples/**
  - fuzz/**
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 5 scattered-hotspot functions in crates/mcp-tester/, crates/mcp-preview/, crates/pmcp-server/ have cog ≤25 OR ≤50 with `// Why:` `#[allow]`"
    - "examples/ violations (21) are addressed via the path chosen in 75-W0-SPIKE-RESULTS.md (path-filter OR bulk `#[allow]` with shared `// Why:` for demo code)"
    - "fuzz/ violations (3-4) are addressed via P5 with `// Why:` for fuzz-harness inline-variant rationale; test_auth_flow (cog 122) refactored mandatorily per D-03"
    - "SATD triage per D-04 completed: each of the 33 SATDs is either deleted (trivial/obsolete), replaced with `// See #NNN —` (real follow-up filed as GitHub issue), or fixed in-place (cheap-to-fix)"
    - "`pmat quality-gate --fail-on-violation --checks complexity` exits 0 — the badge can flip"
    - "Wave 4 commit message records `pmat-complexity: 0 (was MM)`"
  artifacts:
    - path: ".planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md"
      provides: "Per-SATD disposition record (delete / issue-NNN / fix-in-place) for D-04 audit trail"
      contains: "## SATD"
  key_links:
    - from: "75-W0-SPIKE-RESULTS.md::chosen_path"
      to: "examples/ violation handling strategy in this plan"
      via: "Wave 0 spike output drives Wave 4 task selection"
      pattern: "chosen_path:"
---

<objective>
Close out the remaining cognitive-complexity violations: 5 scattered hotspots in `crates/mcp-tester/`, `crates/mcp-preview/`, `crates/pmcp-server/` + the gating-relevant 21 examples/ violations + 3-4 fuzz/ violations. Apply per-D-04 SATD triage to the 33 outstanding TODO/FIXME comments. After this plan lands, `pmat quality-gate --fail-on-violation --checks complexity` exits 0 — the badge can flip (Wave 5 then enforces the gate going forward).

**Purpose:** Drop PMAT complexity violation count to 0 (or to whatever residual count the badge tolerates per `--fail-on-violation` semantics). Resolve D-09's examples/ handling per the empirical Wave 0 spike result. Triage SATD per D-04.

**Output:** 5 source files modified for scattered hotspots + bulk examples/ + fuzz/ updates per Wave 0 path + new `75-04-SATD-TRIAGE.md` audit document. After this plan merges, the gate is mechanically passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-fix-pmat-issues/75-CONTEXT.md
@.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
@.planning/phases/75-fix-pmat-issues/75-PATTERNS.md
@.planning/phases/75-fix-pmat-issues/75-VALIDATION.md
@.planning/phases/75-fix-pmat-issues/75-00-PLAN.md
@.planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md
@.planning/phases/75-fix-pmat-issues/75-03-SUMMARY.md
@CLAUDE.md
@crates/mcp-tester/src/diagnostics.rs

<refactor_techniques>
P1 dominant for the 5 scattered hotspots. P5 for examples/ and fuzz/ if Wave 0 spike says path-filter doesn't work.
</refactor_techniques>

<within_wave_parallelism>
Tasks 4-A (scattered hotspots), 4-B-* (examples + fuzz per spike result — branch on chosen_path), and 4-C (SATD triage) touch DISJOINT file sets and run in parallel. Task 4-B is split into 4-B-A / 4-B-B / 4-B-C (branch by chosen_path, only one runs) + 4-B-D (fuzz, always runs). Task 4-D (final gate verification) is sequential after all three complete.
</within_wave_parallelism>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client → mcp-preview | Untrusted WS frames; refactor of handle_socket must preserve message dispatch |
| Lambda event → pmcp-server-lambda | Untrusted lambda payloads; handler refactor must preserve event routing |
| GitHub issue tracker → repo | SATD triage may file new issues — no security implication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | Tampering | mcp-preview WS handler refactor could mis-route message types | mitigate | New `crates/mcp-preview/tests/handlers_integration.rs` per PATTERNS.md ("MANDATORY ... add ... before refactor"); per-message-type round-trip tests cover |
| T-75-04-02 | Information Disclosure | mcp-tester diagnostics refactor could leak / suppress diagnostic results | mitigate | Existing `crates/mcp-tester/tests/property_tests.rs` + integration tests cover |
| T-75-04-03 | Tampering | Lambda handler refactor could mis-route lambda events | mitigate | Existing in-file tests; if absent, add lambda_event_handler_smoke per PATTERNS.md |
| T-75-04-04 | None | examples/ bulk `#[allow]` is a documentation-clarity tradeoff, not a security issue | accept | Examples are demo code by definition; `// Why: illustrative demo` is the documented project-wide rationale |
| T-75-04-05 | Tampering | fuzz/test_auth_flow refactor (cog 122) could weaken fuzz coverage of auth flow variants | mitigate | Refactor extracts per-variant helpers but keeps the fuzzed input → variant mapping 1:1; `cargo fuzz run auth_flows` for ≥30s smoke per VALIDATION.md spirit |

**Refactor scope note:** Wave 4 closes out remaining violations and triages SATD. Test infrastructure must be added for mcp-preview before refactor (no existing test dir).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 4-A: Refactor scattered crate hotspots (5 functions, 5 files)</name>
  <files>crates/mcp-tester/src/diagnostics.rs, crates/mcp-tester/src/main.rs, crates/mcp-preview/src/handlers/websocket.rs, crates/mcp-preview/src/handlers/api.rs, crates/pmcp-server/pmcp-server-lambda/src/main.rs</files>
  <read_first>
    - crates/mcp-tester/src/diagnostics.rs (run_diagnostics_internal @ 28 cog 55)
    - crates/mcp-tester/src/main.rs (main @ 244 cog 40)
    - crates/mcp-preview/src/handlers/websocket.rs (handle_socket @ 50 cog 37)
    - crates/mcp-preview/src/handlers/api.rs (list_resources @ 179 cog 31)
    - crates/pmcp-server/pmcp-server-lambda/src/main.rs (handler @ 89 cog 26)
    - crates/mcp-tester/tests/property_tests.rs (existing coverage for tester)
    - src/server/streamable_http_server.rs lines 457-534 (analog dispatch shape for handle_socket per PATTERNS.md "For mcp-preview/src/handlers/websocket.rs::handle_socket specifically, the target shape is the same dispatch-table refactor")
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 4 — full hotspot table; "no general analog spans all 5 files — they're heterogeneous")
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 3 expansion table for these 5 fns)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Refactor 5 scattered hotspot functions, one atomic commit each.

    **Pre-refactor for mcp-preview (no existing test dir):**
    Add `crates/mcp-preview/tests/handlers_integration.rs` with at least one round-trip test per handler (one for `handle_socket`, one for `list_resources`). Use a minimal test client. Commit FIRST: `test(75-04): add mcp-preview handlers integration tests pre-refactor`.

    **Pre-refactor for pmcp-server-lambda (verify in-file tests):**
    If `crates/pmcp-server/pmcp-server-lambda/src/main.rs` lacks an in-file `#[cfg(test)] mod tests`, add a `lambda_event_handler_smoke` test that constructs a representative lambda event and asserts the handler returns Ok. Commit: `test(75-04): add lambda handler smoke test pre-refactor`.

    **Refactor 1: `crates/mcp-tester/src/diagnostics.rs::run_diagnostics_internal` @ 28, cog 55 → ≤25.** Apply **P1** — extract per-diagnostic helpers (e.g. `run_protocol_handshake_diagnostic`, `run_capability_negotiation_diagnostic`, `run_tool_listing_diagnostic`, etc. — read function body to enumerate the diagnostic stages). Commit: `refactor(75-04): run_diagnostics_internal (cog 55→MM)`.

    **Refactor 2: `crates/mcp-tester/src/main.rs::main` @ 244, cog 40 → ≤25.** Apply **P1** — extract per-subcommand entry points. Each `Command::Test`, `Command::List`, etc. arm becomes a `run_test_subcommand`, `run_list_subcommand`, etc. helper. Commit: `refactor(75-04): mcp-tester main (cog 40→MM)`.

    **Refactor 3: `crates/mcp-preview/src/handlers/websocket.rs::handle_socket` @ 50, cog 37 → ≤25.** Apply **P4** dispatch — analog is `streamable_http_server.rs:457-534` per PATTERNS.md. Extract per-message-type handlers (`handle_init_message`, `handle_tool_call_message`, etc.). Commit: `refactor(75-04): handle_socket (cog 37→MM)`.

    **Refactor 4: `crates/mcp-preview/src/handlers/api.rs::list_resources` @ 179, cog 31 → ≤25.** Apply **P1** — extract per-resource-class helpers. Commit: `refactor(75-04): list_resources (cog 31→MM)`.

    **Refactor 5: `crates/pmcp-server/pmcp-server-lambda/src/main.rs::handler` @ 89, cog 26 → ≤25.** Borderline P1 — extract per-event-class helpers. If residual is exactly 25 or 26, P1 with one extract should land. Commit: `refactor(75-04): lambda handler (cog 26→MM)`.

    After each commit (POST-REVIEW REVISION 2026-04-23 — Codex Concern #8: per-task tests scoped to affected packages):
    - `cargo test -p mcp-tester --lib -- --test-threads=1` (when modifying mcp-tester)
    - `cargo test -p mcp-preview --lib -- --test-threads=1` (when modifying mcp-preview)
    - `cargo test -p pmcp-server-lambda --lib -- --test-threads=1` (when modifying lambda — or skip if no in-package tests; rely on smoke test)
    - `cargo clippy -p <affected_package> --all-targets --all-features -- -D warnings`

    Full workspace test runs at WAVE-MERGE boundary (this plan's `<verification>` block),
    NOT after each per-function commit.

    Any function with residual >25 after P1 extraction: P5 with `// Why:` per W0 template, ≤50 ceiling.
  </action>
  <verify>
    <automated>cargo test -p mcp-tester --lib -- --test-threads=1 && cargo test -p mcp-preview --lib -- --test-threads=1 && cargo clippy -p mcp-tester -p mcp-preview --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path | test("crates/mcp-tester/src/(diagnostics|main)\\.rs|crates/mcp-preview/src/handlers/(websocket|api)\\.rs|crates/pmcp-server/pmcp-server-lambda/src/main\\.rs"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 5 named functions have cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - No function in any of the 5 files exceeds cog 50
    - `crates/mcp-preview/tests/handlers_integration.rs` exists with ≥2 tests (per-handler)
    - `cargo test -p mcp-tester --lib && cargo test -p mcp-preview --lib` exit 0 (full workspace test deferred to wave-merge per concern #8)
    - `cargo clippy -p mcp-tester -p mcp-preview --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 5
    - All new `#[allow]` sites have `// Why:` per template
  </acceptance_criteria>
  <done>5 scattered crate hotspots resolved; new test scaffolding added where missing.</done>
</task>

<!--
POST-REVIEW REVISION (2026-04-23 — Codex/Gemini HIGH concern #4 / Codex HIGH concern #5):

The original Task 4-B was undersized — researcher confirmed 21 examples/ + ~3-4 fuzz/
violations including a cog-122 fuzz function and 2 examples over the cog-50 hard cap.
A single task with one branching `<action>` is insufficient. Task 4-B is now SPLIT into
three explicit sub-tasks branching on the Wave 0 D-09 spike result. The executor reads
75-W0-SPIKE-RESULTS.md::chosen_path FIRST and runs ONLY the matching sub-task (4-B-A,
4-B-B, OR 4-B-C). Sub-task 4-B-D (fuzz/ refactor) ALWAYS runs regardless of path.
-->

<task type="auto">
  <name>Task 4-B-A: examples/ handled via path filter (chosen_path: a)</name>
  <files>.planning/phases/75-fix-pmat-issues/75-VALIDATION.md, .planning/phases/75-fix-pmat-issues/75-04-EXAMPLES-DECISION.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md (THE GATE — only run this task if `chosen_path: (a)`)
    - .planning/phases/75-fix-pmat-issues/75-VALIDATION.md (Wave 5 gate command)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-09)
    - .planning/phases/75-fix-pmat-issues/75-00-PLAN.md (Wave 0 Task 5 — D-11 badge/gate alignment; the path filter goes in BOTH commands per D-11-B)
  </read_first>
  <action>
    **PRECONDITION:** Read `75-W0-SPIKE-RESULTS.md`. If `chosen_path: (a)`, run this
    task. Otherwise SKIP this task and run 4-B-B or 4-B-C instead.

    Path filter approach: PMAT 3.15.0's `--include` (or equivalent) flag works for
    the complexity check. examples/ + fuzz/ violations stay in the source code but
    are excluded from the gate signal.

    Steps:

    1. Update `75-VALIDATION.md` to record the verified path-filter command. Add
       under "Wave 5 gate command":
       ```
       Wave 5 CI gate command (per D-09 chosen_path: a):
         pmat quality-gate --fail-on-violation --checks complexity --include 'src/**,crates/*/src/**,cargo-pmcp/src/**,pmcp-macros/src/**'
       ```
       (Use the EXACT flag string the spike verified — do not paraphrase.)

    2. Per CONTEXT.md D-11, the path filter MUST also land in `quality-badges.yml`
       in Wave 5 (otherwise the badge command and CI gate command diverge). Note
       this in `75-04-EXAMPLES-DECISION.md`:
       ```markdown
       ---
       chosen_path: (a) — path filter
       ---

       # Examples/Fuzz handling — Wave 4 decision

       Wave 0 spike (75-W0-SPIKE-RESULTS.md) determined `--include` works on PMAT 3.15.0.
       examples/ and fuzz/ violations remain in source but are excluded from gate.

       ## Wave 5 implication
       BOTH `.github/workflows/ci.yml` AND `.github/workflows/quality-badges.yml`
       must use the same `--include 'src/**,crates/*/src/**,cargo-pmcp/src/**,pmcp-macros/src/**'`
       flag. Per CONTEXT.md D-11, badge command alignment is mandatory for the phase goal.

       ## Source files NOT modified by this task
       - examples/ — no annotations added
       - fuzz/ — only fuzz/auth_flows::test_auth_flow refactored mandatorily per D-03 (Task 4-B-D)
       ```

    3. Verify the chosen path filter actually drops the gate count to the in-scope
       residual:
       ```bash
       pmat quality-gate --fail-on-violation --checks complexity --include 'src/**,crates/*/src/**,cargo-pmcp/src/**,pmcp-macros/src/**' --format json | jq '.violations | length'
       ```
       Should be the in-scope-only count (post-Wave-3 — should be 0 if Wave 3 cleared
       all in-scope hotspots; otherwise the residual remaining hotspot count).

    4. Commit: `chore(75-04): record examples/ path-filter decision per chosen_path: (a)`.

    No source files modified in examples/ or fuzz/ (Task 4-B-D handles fuzz/
    test_auth_flow refactor regardless of chosen_path).
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-04-EXAMPLES-DECISION.md && grep -E 'chosen_path: \(a\)' .planning/phases/75-fix-pmat-issues/75-04-EXAMPLES-DECISION.md && grep -E '--include' .planning/phases/75-fix-pmat-issues/75-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Only run if 75-W0-SPIKE-RESULTS.md says `chosen_path: (a)`
    - 75-04-EXAMPLES-DECISION.md exists with `chosen_path: (a)`
    - 75-VALIDATION.md records the exact path-filter flag string
    - Wave 5 plan (75-05-PLAN.md) reads the decision doc and patches BOTH ci.yml AND quality-badges.yml
    - No examples/ or fuzz/ source files modified by this task
  </acceptance_criteria>
  <done>Path-filter decision recorded; Wave 5 has the exact flag string + scope to patch both workflows.</done>
</task>

<task type="auto">
  <name>Task 4-B-B: examples/ handled via bulk #[allow] (chosen_path: b — moderate)</name>
  <files>examples/** (specific list determined at runtime), .planning/phases/75-fix-pmat-issues/75-04-EXAMPLES-DECISION.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md (THE GATE — only run this task if `chosen_path: (b)`)
    - .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json (Wave 0 Task 6 — authoritative examples/ violation list)
    - .planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md (Wave 0 Task 4 — D-10 outcome MUST be A; if D-10-B, this task is INVALID and must escalate to refactor)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Pitfall 5/6 — examples/ list)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Cross-Cutting — examples/ Why: template)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-03 — cog >50 examples STILL require refactor, not bulk allow)
    - .planning/phases/75-fix-pmat-issues/75-00-PLAN.md (`<project_wide_why_template>`)
  </read_first>
  <action>
    **PRECONDITIONS:**
    1. Read `75-W0-SPIKE-RESULTS.md`. If `chosen_path: (b)`, run this task. Otherwise
       SKIP this task and run 4-B-A or 4-B-C instead.
    2. Read `pmat-allow-behavior.md`. If `outcome: D-10-B` (PMAT ignores `#[allow]`),
       this task is INVALID — bulk-allow does not reduce the gate count. ESCALATE
       to operator: surface "BULK ALLOW APPROACH FAILS UNDER D-10-B" via 75-04-SUMMARY
       and HALT this task. The user must decide between (i) refactor every examples/
       violation down to ≤25 (large effort), (ii) re-run Wave 0 spike to find a
       working path filter, or (iii) accept badge stays red.

    Bulk `#[allow]` approach: PMAT path filter doesn't work but PMAT honors the
    `#[allow]`. Each examples/ violation gets a project-wide `// Why:` justified
    `#[allow(clippy::cognitive_complexity)]`.

    Steps:

    1. Re-measure the examples/ violation list from the live inventory:
       ```bash
       jq -r '.violations[] | select(.path | startswith("examples/")) | "\(.path):\(.line) \(.name) cog=\(.complexity)"' \
           .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json
       ```

    2. **Triage by D-03 ceiling:**
       - **cog ≤50:** P5-eligible — bulk `#[allow]` per the project-wide `// Why:`
         template:
         ```rust
         // Why: illustrative demo code for documentation, intentionally inline for reader clarity
         #[allow(clippy::cognitive_complexity)]
         fn main() { /* example body */ }
         ```
       - **cog >50** (e.g. `wasm-mcp-server/src/lib.rs::main` cog 83,
         `27-course-server-minimal/src/main.rs::load_course_content` cog 66):
         **REFACTOR REQUIRED — D-03 hard cap is 50 with allow.** Apply P1
         extraction until residual ≤50, THEN add the `#[allow]`. These functions
         CANNOT be bulk-allowed at their current cog.

    3. For each examples/ function in the cog-≤50 bucket, edit the file:
       - Add the `// Why:` line + `#[allow]` immediately above the function signature
       - Single-line per-file commit message: `chore(75-04): #[allow] cog
         <fn_name> in examples/<path> per D-09 chosen_path: (b)`

    4. For each examples/ function in the cog->50 bucket:
       - Apply P1 (extract per-step helpers) until residual ≤50
       - Add `#[allow]` with the standard `// Why:` template
       - Per-function commit: `refactor(75-04): examples/<path>::<fn_name> (cog NN→MM) per D-03`

    5. Verify:
       ```bash
       cargo build --workspace --examples
       pmat quality-gate --fail-on-violation --checks complexity
       ```
       Should exit 0 (or with only fuzz/ residual that 4-B-D handles).

    6. Write `75-04-EXAMPLES-DECISION.md`:
       ```markdown
       ---
       chosen_path: (b) — bulk #[allow]
       ---

       # Examples/ handling — Wave 4 decision

       Wave 0 spike: `--include` does NOT work on PMAT 3.15.0.
       Wave 0 D-10 verification: PMAT honors `#[allow(clippy::cognitive_complexity)]`.
       Therefore: per-examples/-function `#[allow]` with `// Why: illustrative demo`.

       ## Functions annotated (cog ≤50, bulk-allow)
       - examples/wasm-mcp-server/src/lib.rs::main (cog 83 → refactored to MM, then #[allow])
       - examples/27-course-server-minimal/src/main.rs::load_course_content (cog 66 → refactored to MM, then #[allow])
       - (full list — fill in from inventory)

       ## Functions refactored down before allow (cog >50 — D-03)
       - <list>

       ## Wave 5 implication
       Only ci.yml needs the gate step (no `--include` flag needed). quality-badges.yml
       unchanged unless D-11-B (other dimensions also fail bare gate).
       ```

  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-04-EXAMPLES-DECISION.md && grep -E 'chosen_path: \(b\)' .planning/phases/75-fix-pmat-issues/75-04-EXAMPLES-DECISION.md && cargo build --workspace --examples && pmat quality-gate --fail-on-violation --checks complexity 2>&1 | grep -qE 'passed|0 violations' || true</automated>
  </verify>
  <acceptance_criteria>
    - Only run if 75-W0-SPIKE-RESULTS.md says `chosen_path: (b)` AND pmat-allow-behavior.md says `outcome: D-10-A`
    - 75-04-EXAMPLES-DECISION.md exists with `chosen_path: (b)` + per-function disposition list
    - All examples/ functions with cog ≤50 have `// Why:` annotated `#[allow]`
    - All examples/ functions with cog >50 are refactored to ≤50 then have `#[allow]`
    - `cargo build --workspace --examples` exits 0
    - PMAT examples/ violation count = 0 in `pmat analyze complexity` output
  </acceptance_criteria>
  <done>Bulk-allow path executed; examples/ no longer contributes to gate count.</done>
</task>

<task type="auto">
  <name>Task 4-B-C: ESCALATE — path filter fails AND many >50 examples (chosen_path: c)</name>
  <files>.planning/phases/75-fix-pmat-issues/75-04-ESCALATION.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md (THE GATE — only run this task if `chosen_path: (c)`)
    - .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-03 hard cap; D-09 fallback (d))
  </read_first>
  <action>
    **PRECONDITION:** Read `75-W0-SPIKE-RESULTS.md`. If `chosen_path: (c)` (path
    filter doesn't work AND there are many cog>50 examples requiring real refactor),
    this becomes its own escalation. Run this task only in that case.

    This task does NOT attempt the work itself — instead it surfaces the scope
    expansion to the operator via a checkpoint and HALTS.

    Steps:

    1. Run the inventory to enumerate the over-cap examples/ functions:
       ```bash
       jq -r '.violations[] | select(.path | startswith("examples/")) | select(.complexity > 50) | "\(.path):\(.line) \(.name) cog=\(.complexity)"' \
           .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json
       ```

    2. Write `75-04-ESCALATION.md`:
       ```markdown
       ---
       escalation: D-09 chosen_path: (c) — examples/ refactor as own wave
       date: <today>
       ---

       # Phase 75 — Examples/ Refactor Escalation

       ## Why this escalation
       - Wave 0 spike (75-W0-SPIKE-RESULTS.md): `--include` doesn't work in PMAT 3.15.0.
       - Inventory (pmat-inventory-2026-04-22.json): N examples/ functions over cog 50,
         which exceeds D-03 hard cap and CANNOT be bulk-allowed.
       - Bulk-allow approach (4-B-B) covers cog ≤50 only; the cog >50 cohort needs
         real refactor effort, which exceeds Wave 4 budget.

       ## Functions requiring refactor (cog >50)
       <list per inventory>

       ## Recommended action (operator decides)
       1. **Promote to its own wave (75-04a):** budget another ~1 week of refactor
          effort on examples/ specifically.
       2. **Demote examples/ to deferred:** mark these functions as
          out-of-scope for Phase 75; accept the badge will stay red until they're
          addressed in Phase 76+. (Conflicts with D-01 stated goal — use only as
          last resort.)
       3. **Bypass the gate for examples/ via per-file rewrite:** rewrite the
          examples to be smaller (often acceptable since examples are
          documentation, not production). This is real engineering work but
          may be cheaper than P1 extraction.

       ## What this task does NOT do
       This task records the escalation and HALTS Wave 4. Tasks 4-B-D, 4-A, 4-C
       can still proceed in parallel — they don't depend on the examples/ outcome.
       Task 4-D (final gate verification) cannot complete until examples/ is
       resolved.
       ```

    3. Surface a CHECKPOINT to the operator:
       - In `75-04-SUMMARY.md` (when Wave 4 completes other tasks), include
         "WAVE 4 INCOMPLETE — chosen_path (c) ESCALATION PENDING"
       - Block Task 4-D until operator decision

  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-04-ESCALATION.md && grep -E 'escalation: D-09 chosen_path: \(c\)' .planning/phases/75-fix-pmat-issues/75-04-ESCALATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Only run if 75-W0-SPIKE-RESULTS.md says `chosen_path: (c)` AND inventory has many cog >50 examples
    - 75-04-ESCALATION.md exists with escalation header and the over-cap function list
    - 75-04-SUMMARY.md notes "WAVE 4 INCOMPLETE — operator decision pending"
    - Task 4-D is BLOCKED until operator resolves
  </acceptance_criteria>
  <done>Escalation surfaced; operator must decide whether to promote, demote, or rewrite. Wave 4 partially completes (4-A + 4-B-D + 4-C still run).</done>
</task>

<task type="auto">
  <name>Task 4-B-D: fuzz/ violations (always runs, regardless of chosen_path)</name>
  <files>fuzz/auth_flows/* (refactor + #[allow] sites determined at runtime)</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json (authoritative fuzz/ violation list)
    - .planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md (D-10 outcome — if D-10-B, P5 sites become refactor sites)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (fuzz Why: template)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-03 hard cap 50)
    - .planning/phases/75-fix-pmat-issues/75-00-PLAN.md (`<project_wide_why_template>`)
  </read_first>
  <action>
    fuzz/ violations are handled the same way regardless of chosen_path. The
    `test_auth_flow` cog 122 function MUST be refactored (D-03 hard cap 50 — bulk
    allow forbidden); the smaller fuzz functions get P5 if D-10-A, refactor if D-10-B.

    Steps:

    1. **`fuzz/auth_flows::test_auth_flow` (cog 122) — D-03 mandates refactor.**
       Apply **P1** to extract per-flow-variant helpers (e.g.
       `fuzz_oauth_pkce_flow`, `fuzz_oauth_authorization_code_flow`,
       `fuzz_device_flow`). The fuzzed input → variant mapping must remain 1:1
       (per threat_model T-75-04-05). After refactor:
       - `cargo fuzz build` (verifies harness still compiles)
       - Optional: `cargo fuzz run auth_flows -- -max_total_time=30` (smoke)
       - Commit: `refactor(75-04): fuzz/auth_flows::test_auth_flow (cog 122→MM)`

    2. **Remaining fuzz/ violations** (`test_pkce_flow` cog 45,
       `simulate_transport_operations` cog 46, `test_websocket_framing` cog 30):
       - **If pmat-allow-behavior.md says outcome: D-10-A** (allow works):
         apply **P5** with shared `// Why:` per PATTERNS.md:
         ```rust
         // Why: fuzz harness — variant enumeration must remain inline so each branch
         // maps directly to a fuzzed input class.
         #[allow(clippy::cognitive_complexity)]
         fn test_pkce_flow(...) { ... }
         ```
         All ≤50 so D-03 permits P5. Commit: `refactor(75-04): #[allow] for fuzz/ harness functions per Wave 0 plan`.
       - **If pmat-allow-behavior.md says outcome: D-10-B** (allow ignored):
         apply P1 to extract per-variant helpers. Commit: `refactor(75-04): fuzz/<fn_name> (cog NN→MM) — D-10-B requires extraction`.

    3. After all fuzz/ refactors:
       - `cd fuzz && cargo build` (fuzz targets still compile)
       - `cargo build --workspace`
       - `pmat analyze complexity --format json | jq '[.violations[] | select(.path | startswith("fuzz/"))] | length'` should be 0 (or only the P5'd ones if D-10-A and PMAT counts despite annotation; that's the verification of the assumption baked into D-10-A).
  </action>
  <verify>
    <automated>cd fuzz && cargo build && cd .. && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - fuzz/auth_flows::test_auth_flow refactored to ≤50 (cog 122 was over D-03 ceiling)
    - All other fuzz/ violations have `// Why:` `#[allow]` (if D-10-A) OR are refactored (if D-10-B)
    - `cd fuzz && cargo build` exits 0
    - `cargo build --workspace` exits 0
    - PMAT fuzz/ violation count is 0 (or matches the D-10-A expected residual)
  </acceptance_criteria>
  <done>fuzz/ violations resolved; gate counts no longer include fuzz/ contributions.</done>
</task>

<task type="auto">
  <name>Task 4-C: SATD triage per D-04 (33 comments — grouped-by-directory approach)</name>
  <files>.planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md, (multiple source files per triage outcome)</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-04 three-way policy: trivial → delete; real follow-up → file issue + `// See #NNN —` ref; cheap-to-fix → fix in-place)
    - CLAUDE.md ("Zero SATD comments allowed" + project SATD policy)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (SATD context — 33 total per baseline)
    - .planning/phases/75-fix-pmat-issues/75-REVIEWS.md (concern #7: scope reduction — group by directory rather than per-comment GitHub issues)
  </read_first>
  <action>
    Triage all 33 SATD (TODO/FIXME/HACK/XXX) comments per D-04's three-way policy.

    **POST-REVIEW REVISION (2026-04-23 — Both reviewers concern #7, path A):**
    The original Task 4-C planned up to 33 individual `gh issue create` calls — fragile
    (rate limits, network failures) and timeout-prone for a single sub-agent run.
    REVISED approach: group "real follow-up" SATDs (Disposition B) by hotspot
    DIRECTORY into ~5 umbrella issues, each with a checklist body listing the
    individual SATDs. This reduces 10-15 `gh issue create` calls to ~5 while
    preserving D-04's intent.

    **Step 1: Inventory.** Generate the current SATD list:
    ```bash
    pmat analyze satd --format json > /tmp/satd-inventory.json 2>/dev/null \
      || grep -rn -E '(TODO|FIXME|HACK|XXX)' src/ crates/*/src/ cargo-pmcp/src/ pmcp-macros/src/ \
         | grep -v 'target/' > /tmp/satd-inventory.txt
    ```

    **Step 2: Triage each into a per-comment row in `75-04-SATD-TRIAGE.md`:**
    ```markdown
    # Phase 75 — SATD Triage Audit

    Per D-04: each SATD is (a) trivial/obsolete → DELETE, (b) real follow-up →
    GROUP BY DIRECTORY into umbrella issue + replace with `// See #NNN`,
    (c) cheap fix (<30min) → FIX-IN-PLACE.

    Per post-review revision: Disposition B SATDs are GROUPED by directory into
    umbrella issues rather than per-SATD issues.

    | # | File:line | SATD text | Disposition | Group / Action |
    |---|-----------|-----------|-------------|----------------|
    | 1 | src/server/foo.rs:42 | `TODO: handle malformed input` | (b) issue | Group: src/server (umbrella issue #NNN); replaced with `// See #NNN — defer to error-handling phase` |
    | 2 | src/client/bar.rs:99 | `FIXME: deprecated API` | (a) delete | Code already removed in commit XYZ; comment was stale |
    | 3 | crates/foo/lib.rs:7 | `TODO: validate length` | (c) fix | Added `if input.len() > MAX { return Err(...) }`; SATD comment removed |
    | ... | (33 rows total) |
    ```

    **Step 3: Bias the triage per CONTEXT.md `<specifics>`:**
    - SATDs in `pmcp-code-mode/`, `pentest/`, `deployment/` (newer code) → bias toward
      (b) issue (active follow-up).
    - SATDs in `streamable_http_server.rs` (mature src/) → bias toward (a) delete or
      (c) fix-in-place.

    **Step 4: Group Disposition B SATDs by hotspot directory.** Cluster (b) SATDs into
    ~5 umbrella issues. Suggested grouping (adjust per actual triage):
    - `paiml/rust-mcp-sdk` umbrella issue: "Phase 75 SATD triage — src/server/* outstanding follow-ups"
    - umbrella issue: "Phase 75 SATD triage — crates/pmcp-code-mode/ outstanding"
    - umbrella issue: "Phase 75 SATD triage — cargo-pmcp/src/{pentest,deployment}/ outstanding"
    - umbrella issue: "Phase 75 SATD triage — pmcp-macros/ outstanding"
    - umbrella issue: "Phase 75 SATD triage — misc / cross-cutting outstanding"

    Each umbrella issue body has a checkbox list:
    ```markdown
    ## Outstanding SATDs from Phase 75 triage

    From src/server/* hotspot directory:

    - [ ] `src/server/foo.rs:42` — TODO: handle malformed input. Defer to error-handling phase.
    - [ ] `src/server/bar.rs:88` — FIXME: race condition in state machine. Defer to concurrency review.
    - [ ] `src/server/baz.rs:201` — TODO: rate-limit telemetry emission. Low-pri.

    Each SATD location was annotated with `// See #NNN — <one-line reason>` in commit XYZ.
    ```

    **Step 5: Execute dispositions.**
    - (a) Delete the comment (and any attached dead code). Atomic commit:
      `chore(75-04): delete obsolete SATD <file>:<line>`.
    - (b) [GROUPED] After ALL (b) SATDs are triaged and grouped, file ~5 umbrella
      issues using `gh issue create -R paiml/rust-mcp-sdk --title "<title>" --body "$(cat <<EOF ... EOF)"`.
      Then in a SEPARATE commit per directory, replace the SATD lines:
      ```
      // See #NNN — <one-line directory-level reason>
      ```
      Per-directory commit: `chore(75-04): replace src/server/ SATDs with #NNN refs (umbrella issue)`.
    - (c) Apply the cheap fix in-place. Remove the SATD comment. Atomic commit:
      `fix(75-04): address SATD <file>:<line>`.

    **Step 6: Verify count drop.**
    ```bash
    pmat quality-gate --fail-on-violation --checks satd 2>&1 | tail -3
    ```
    Per D-01 SATD is best-effort, not gating; even getting it down to 5-10 is meaningful.

    Commit the audit doc: `docs(75-04): SATD triage audit per D-04 (post-review grouped issues)`.

    **Scope constraint (D-04 boundary):** The 33 SATD baseline count from CONTEXT.md may
    include `examples/`, `tests/`, `benches/`, and `fuzz/` paths. This triage scopes ONLY
    to `src/`, `crates/*/src/`, `cargo-pmcp/src/`, `pmcp-macros/src/` per the Step 1
    inventory grep above. If the post-triage `pmat analyze satd` count exceeds the
    in-scope row count from `75-04-SATD-TRIAGE.md`, document the residual under
    "deferred — out of D-04 scope" rather than triaging.

    **NETWORK FAILURE FALLBACK:** If `gh issue create` fails (rate limit, network,
    auth), pause the per-directory grouping commits and surface a checkpoint:
    "GH ISSUE FILING FAILED — retry after fix or defer remaining (b) dispositions
    to Phase 76". The (a) and (c) dispositions can land independently.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md && grep -cE '^\| [0-9]+ \|' .planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md | xargs -I{} test {} -ge 30 && cargo test -p pmcp --lib -- --test-threads=1 && cargo clippy -p pmcp --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `75-04-SATD-TRIAGE.md` exists with ≥30 rows (covering 33 SATDs minus any duplicates the inventory may dedupe)
    - Each row has a disposition letter (a/b/c) and a "Group / Action" description
    - For (b) dispositions, the action mentions a filed `paiml/rust-mcp-sdk#NNN` UMBRELLA issue (NOT one issue per SATD)
    - Total `gh issue create` calls ≤ 5 (one per hotspot directory grouping)
    - For (c) dispositions, the source file shows the in-place fix (verifiable by `git diff`)
    - `pmat quality-gate --fail-on-violation --checks satd 2>&1 | grep "violations"` shows count strictly less than 33
    - Per-task verification: `cargo test -p pmcp --lib -- --test-threads=1` exits 0 (full workspace test deferred to wave-merge per concern #8)
    - `cargo clippy -p pmcp --all-targets --all-features -- -D warnings` exits 0
  </acceptance_criteria>
  <done>SATD count drops from 33 via grouped-issue approach; per-comment triage record exists; D-04 satisfied without 33 individual gh calls.</done>
</task>

<task type="auto">
  <name>Task 4-D: Final pre-Wave-5 gate verification (the badge can flip)</name>
  <files>.planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md (the chosen path)
    - .planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md (triage outcomes)
    - .planning/phases/75-fix-pmat-issues/75-VALIDATION.md (Phase gate criteria)
  </read_first>
  <action>
    Run the FULL gate locally and record the outcome. This is the proof point that Wave 5 (CI enforcement) can land without immediately turning the gate red.

    1. Run `pmat quality-gate --fail-on-violation --checks complexity --format json > /tmp/final-complexity.json; echo "exit=$?"`. Record the exit code AND the full JSON's violation count.

    2. Run `pmat quality-gate --fail-on-violation --format json > /tmp/final-all-checks.json; echo "exit=$?"`. Record exit code and per-check breakdown (complexity, satd, duplicate, entropy, sections counts).

    3. Run `make quality-gate` to confirm the project-standard gate is also green.

    4. Run `cargo test --workspace --all-features -- --test-threads=1` for the final regression check.

    5. Write `.planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md`:
    ```markdown
    # Phase 75 — Final Count Pre-Wave-5

    Date: <today>
    Branch: <current branch>
    PMAT version: <pmat --version>

    ## --checks complexity (the gate signal per D-01)
    Exit code: 0 (or 1 with explanation)
    Violations: NN (was 94 baseline)
    Path filter applied: <command line used, per Wave 0 chosen_path>

    ## --fail-on-violation (all checks)
    Exit code: 0 (or 1 with breakdown)
    - complexity: NN
    - satd: NN (was 33)
    - duplicate: NN (was 1554; expected unchanged per RESEARCH.md Pitfall 3)
    - entropy: NN (was 4)
    - sections: NN (was 2)

    ## make quality-gate
    Exit code: 0

    ## cargo test --workspace --all-features
    Exit code: 0

    ## Verdict
    READY FOR WAVE 5: yes/no
    Rationale: <one paragraph>
    ```

    6. Commit: `docs(75-04): final pre-Wave-5 gate verification — pmat-complexity: NN (was 94)`.

    If `READY FOR WAVE 5: no` (i.e. complexity violations remain), STOP and reopen the upstream tasks that failed to clear them. Do NOT proceed to Wave 5 with a still-red complexity gate.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md && grep -E 'READY FOR WAVE 5: yes' .planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md && pmat quality-gate --fail-on-violation --checks complexity</automated>
  </verify>
  <acceptance_criteria>
    - `75-04-FINAL-COUNT.md` exists with verdict "READY FOR WAVE 5: yes"
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0
    - `make quality-gate` exits 0
    - `cargo test --workspace --all-features -- --test-threads=1` exits 0
    - Final-count doc records the per-check breakdown for audit
  </acceptance_criteria>
  <done>Phase 75 gate is mechanically passing; Wave 5 can land enforcement without immediately blocking PRs.</done>
</task>

</tasks>

<verification>
After all 4 Wave 4 tasks complete:

1. **The badge gate signal**: `pmat quality-gate --fail-on-violation --checks complexity` exits 0.
2. **Per-directory zero-violations** for the 5 scattered crates files: `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | test("crates/(mcp-tester|mcp-preview|pmcp-server)/"))] | length'` = 0.
3. **Examples/fuzz handled per spike**: Either via path filter (no source changes) or via bulk `#[allow]` (source changes recorded in 75-04-SATD-TRIAGE.md sister doc).
4. **SATD count drops**: ≤ 5 (target — exact count depends on triage outcomes; D-04 doesn't mandate zero).
5. **D-02 conformance**: Every `#[allow(clippy::cognitive_complexity)]` added in this wave has `// Why:` directly above.
6. **D-03 conformance**: No function in any modified file exceeds cog 50.
7. **Workspace test green**: `cargo test --workspace --all-features -- --test-threads=1` exits 0.
8. **`make quality-gate` exits 0**.
9. **Final-count doc exists** with `READY FOR WAVE 5: yes`.

Wave-merge commit message format: `refactor(75-04): wave 4 complete — pmat-complexity: 0 (was MM)`.
</verification>

<success_criteria>
- 5 scattered hotspots refactored (Task 4-A)
- examples/ + fuzz/ violations addressed per Wave 0 chosen_path (Task 4-B)
- 33 SATDs triaged per D-04 with per-comment audit record (Task 4-C)
- Final-count doc verifies gate signal exits 0 (Task 4-D)
- Wave 5 can land CI enforcement without immediately blocking PRs
</success_criteria>

<output>
After completion, create `.planning/phases/75-fix-pmat-issues/75-04-SUMMARY.md` per the GSD summary template, including:
- Per-function before/after cog scores for the 5 scattered hotspots
- Path chosen for examples/fuzz (a/b/c) and rationale
- SATD triage outcome breakdown (count of a/b/c)
- Final gate exit code recorded
- Wave-rollup PMAT count delta (target: 0 violations)
</output>
