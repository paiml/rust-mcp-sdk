---
phase: 75-fix-pmat-issues
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/quality-badges.yml
  - pmcp-macros/tests/expansion_snapshots.rs
  - pmcp-macros/tests/fixtures/example_mcp_tool/Cargo.toml
  - pmcp-macros/tests/fixtures/example_mcp_tool/src/main.rs
  - pmcp-macros/tests/fixtures/example_mcp_server/Cargo.toml
  - pmcp-macros/tests/fixtures/example_mcp_server/src/main.rs
  - pmcp-macros/tests/fixtures/example_mcp_resource/Cargo.toml
  - pmcp-macros/tests/fixtures/example_mcp_resource/src/main.rs
  - pmcp-macros/tests/fixtures/example_mcp_prompt/Cargo.toml
  - pmcp-macros/tests/fixtures/example_mcp_prompt/src/main.rs
  - pmcp-macros/Cargo.toml
  - crates/pmcp-code-mode/tests/eval_semantic_regression.rs
  - .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md
  - .planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md
  - .planning/phases/75-fix-pmat-issues/badge-vs-gate-verification.md
  - .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json
  - .planning/phases/75-fix-pmat-issues/pmat-inventory-summary.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 75 has a recorded empirical answer for whether `pmat quality-gate --include 'src/**' --checks complexity` excludes examples/ from the gate (Task 0 — D-09)"
    - "Phase 75 has a recorded empirical answer for whether PMAT honors `#[allow(clippy::cognitive_complexity)]` (Task 4 — D-10 load-bearing for Waves 1-4)"
    - "Phase 75 has a recorded empirical answer for whether the README badge command (`pmat quality-gate --fail-on-violation`) and the planned CI gate command (`--checks complexity`) produce the same exit code on baseline (Task 5 — D-11 stated-goal-failure check)"
    - "Phase 75 has a single committed machine-readable inventory snapshot (Task 6) — every later wave's success criteria derive from it via jq, NOT from prose counts in CONTEXT.md or RESEARCH.md"
    - "`pmcp-macros/tests/expansion_snapshots.rs` exists and contains accepted insta snapshots for expand_mcp_server, expand_mcp_tool, expand_mcp_resource, expand_mcp_prompt — captured via `cargo expand` on per-fixture sub-projects (NOT via `__test_internal` re-export — that path is impossible per `proc-macro = true`)"
    - "`crates/pmcp-code-mode/tests/eval_semantic_regression.rs` exists with passing tests asserting JsonValue outputs for representative ValueExpr programs against evaluate_with_scope and evaluate_array_method_with_scope — INCLUDING a 20-30 entry real-expression corpus that exercises parser↔evaluator interactions"
    - "`.github/workflows/ci.yml` and `.github/workflows/quality-badges.yml` install a pinned PMAT version (no floating `cargo install pmat`)"
    - "The single project-wide `// Why:` justification format is pinned in this plan and reused by all later waves"
  artifacts:
    - path: ".planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md"
      provides: "Empirical record of PMAT path-filter behavior on this repo with PMAT 3.15.0 (D-09)"
      contains: "include_works:"
    - path: ".planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md"
      provides: "Empirical record of whether PMAT honors `#[allow(clippy::cognitive_complexity)]` (D-10)"
      contains: "outcome: D-10-"
    - path: ".planning/phases/75-fix-pmat-issues/badge-vs-gate-verification.md"
      provides: "Empirical record of whether badge gate command and CI gate command produce same exit code on baseline (D-11)"
      contains: "outcome: D-11-"
    - path: ".planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json"
      provides: "Authoritative machine-readable PMAT violation snapshot — single source of truth for all wave deltas"
      min_lines: 1
    - path: ".planning/phases/75-fix-pmat-issues/pmat-inventory-summary.md"
      provides: "Human-readable summary + per-directory bucketing of pmat-inventory-2026-04-22.json"
      contains: "inventory_snapshot:"
    - path: "pmcp-macros/tests/expansion_snapshots.rs"
      provides: "insta snapshot baselines for the 4 expand fns via cargo-expand on per-fixture sub-projects"
      min_lines: 40
    - path: "crates/pmcp-code-mode/tests/eval_semantic_regression.rs"
      provides: "Semantic regression baseline for the 2 highest-cog evaluator fns + 20-30 entry real-expression corpus"
      min_lines: 120
    - path: ".github/workflows/ci.yml"
      provides: "PMAT version pin"
      contains: "pmat --version"
    - path: ".github/workflows/quality-badges.yml"
      provides: "PMAT version pin"
      contains: "pmat --version"
  key_links:
    - from: "pmcp-macros/tests/expansion_snapshots.rs"
      to: "pmcp-macros/src/{mcp_server,mcp_tool,mcp_resource,mcp_prompt}.rs"
      via: "macro expansion via generated TokenStream"
      pattern: "expand_mcp_(server|tool|resource|prompt)"
    - from: "crates/pmcp-code-mode/tests/eval_semantic_regression.rs"
      to: "crates/pmcp-code-mode/src/eval.rs"
      via: "evaluate_with_scope and evaluate_array_method_with_scope direct calls"
      pattern: "evaluate_with_scope|evaluate_array_method_with_scope"
    - from: ".github/workflows/ci.yml"
      to: "PMAT 3.15.0 binary"
      via: "cargo install pmat --version =3.15.0 --locked"
      pattern: "pmat --version =?3\\.15\\.0"
---

<objective>
Establish the regression-detection baseline that every later Wave depends on, and resolve the open empirical question that materially changes the phase scope. Without Wave 0, Wave 1b (pmcp-macros) and Wave 3 (pmcp-code-mode) refactors have no semantic safety net, and the D-09 implementation path stays unknown.

**Purpose:** Lock the project-wide `// Why:` justification format (D-02), pin PMAT in CI (Pitfall 1), capture macro-expansion + evaluator semantic snapshots BEFORE refactor begins (Pitfall 4), and empirically answer whether `pmat quality-gate --include 'src/**'` excludes examples/ violations from the gate (Pitfall 3 / D-09).

**Output:** 4 net-new artifacts (3 files + 1 spike doc), 2 modified CI workflow files, and a pinned `// Why:` template that all later plans reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-fix-pmat-issues/75-CONTEXT.md
@.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
@.planning/phases/75-fix-pmat-issues/75-PATTERNS.md
@.planning/phases/75-fix-pmat-issues/75-VALIDATION.md
@CLAUDE.md
@.github/workflows/ci.yml
@.github/workflows/quality-badges.yml
@pmcp-macros/Cargo.toml
@pmcp-macros/tests/mcp_server_tests.rs
@crates/pmcp-code-mode/tests/property_tests.rs
@crates/pmcp-code-mode/src/eval.rs

<project_wide_why_template>
This template is the SINGLE canonical format for every `#[allow(clippy::cognitive_complexity)]` site touched in Phase 75 (existing retro-justifications AND new allows). Per D-02, no bare allows are permitted; per D-03, no allow may sit above cognitive complexity 50.

```rust
/// <existing rustdoc, unchanged>
// Why: <one-line rationale — what makes this irreducible AND what would be lost
// by extracting helpers>
#[allow(clippy::cognitive_complexity)]
fn function_name(...) { ... }
```

Worked example (the existing `src/shared/sse_optimized.rs:225` site that Wave 1a will retro-justify):

```rust
/// Connect to SSE endpoint
// Why: SSE reconnection state — header negotiation, retry-after parsing,
// last-event-id replay, and 3 error-class branches share local state;
// extracting helpers would require an awkward shared mutable struct.
#[allow(clippy::cognitive_complexity)]
async fn connect_sse(...) { ... }
```

Acceptance check that all later waves use:
```bash
grep -B1 'allow(clippy::cognitive_complexity)' <file> | grep -q '// Why:'
```
</project_wide_why_template>

<interfaces>
From `pmcp-macros/Cargo.toml` (already declared dev-dependencies — no new dependencies needed):
- `insta = "1.43"` (snapshot framework)
- `trybuild = "1.0"` (compile-fail tests)
- `proptest = "1.6"`

From `crates/pmcp-code-mode/src/eval.rs` (signatures the new test file calls):
```rust
pub fn evaluate_with_scope<V: VariableProvider>(
    expr: &ValueExpr,
    global_vars: &V,
    local_vars: &HashMap<String, JsonValue>,
) -> Result<JsonValue, ExecutionError>;

pub fn evaluate_array_method_with_scope<V: VariableProvider>(
    array: &ValueExpr,
    method: &ArrayMethodCall,
    global_vars: &V,
    local_vars: &HashMap<String, JsonValue>,
) -> Result<JsonValue, ExecutionError>;
```
(Test file should re-read crates/pmcp-code-mode/src/eval.rs to confirm exact signatures before writing tests.)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → external crates.io | `cargo install pmat` fetches a pinned binary; pin uses `--locked` |
| Test harness → macro expansion | Snapshot baseline is read-only; cannot affect runtime semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-00-01 | Tampering | PMAT binary version drift between local and CI (Pitfall 1) | mitigate | Pin `pmat` to `=3.15.0 --locked` in both workflow files; record version in spike doc |
| T-75-00-02 | Information Disclosure | Snapshot baselines could leak private API shapes if committed accidentally | accept | Snapshots are over public macro expansion only — already public via `cargo expand` |
| T-75-00-03 | Denial of Service | Heavy snapshot tests could slow CI | accept | 4 snapshots only; runtime negligible (<1s) |

**Refactor scope note:** Wave 0 is purely additive (new test files, CI version pin, spike doc). It changes ZERO production behavior, so the existing threat surface is unchanged.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: PMAT path-filter empirical spike (resolves D-09)</name>
  <files>.planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-09 mechanism question)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Pitfall 3 + Open Question 2)
    - .planning/phases/75-fix-pmat-issues/75-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Resolve D-09's "preferred order (a)→(d)" by empirically testing whether PMAT 3.15.0's `quality-gate` honors any path-filter mechanism for the complexity check. Run the following experiments in order and record each result in a new file `.planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md`:

    1. Establish baseline: `pmat quality-gate --fail-on-violation --checks complexity --format json 2>&1 | tee /tmp/spike-baseline.json` — record total violation count and the count split between `examples/`, `fuzz/`, and in-scope (`src/`, `crates/*/src/`, `cargo-pmcp/src/`, `pmcp-macros/src/`).

    2. Run `pmat quality-gate --help 2>&1 | tee /tmp/spike-help.txt` and record EVERY flag that mentions `include`, `exclude`, `path`, `glob`, `ignore`, or `filter`.

    3. For each candidate flag from step 2, run `pmat quality-gate --fail-on-violation --checks complexity <flag>=<value> --format json` and record whether the violation count drops AND whether examples/ violations specifically are excluded. Test at minimum: `--include 'src/**'`, `--exclude 'examples/**'`, `--exclude 'examples/**,fuzz/**'`.

    4. Test `.pmatignore` file mechanism: `printf 'examples/\nfuzz/\n' > .pmatignore && pmat quality-gate --fail-on-violation --checks complexity --format json` then `rm .pmatignore`. Record whether count changes.

    5. Test `[analysis] exclude_patterns` in `.pmat/project.toml` (already half-tested per RESEARCH.md Pitfall 3 for duplicates — re-test for `--checks complexity`).

    6. Write `75-W0-SPIKE-RESULTS.md` with this exact frontmatter + body:

    ```markdown
    ---
    spike: D-09 path-filter resolution
    pmat_version: <output of `pmat --version`>
    date: <today>
    ---

    # Wave 0 spike: PMAT path-filter empirical results

    ## Baseline
    - Total complexity violations: NN
    - In-scope violations (src/, crates/*/src/, cargo-pmcp/src/, pmcp-macros/src/): NN
    - examples/ violations: NN
    - fuzz/ violations: NN

    ## Mechanisms tested

    | Mechanism | Command | Result count | Examples excluded? |
    |-----------|---------|--------------|---------------------|
    | (rows from steps 2-5) | | | |

    ## Decision

    include_works: true | false
    chosen_path: (a) --include flag scopes gate | (b) bulk #[allow] on examples/ | (c) other

    Wave 5 (D-07 CI gate) implementation: <one paragraph>
    Examples-violations handling for Wave 4 (or new Wave): <one paragraph>
    ```

    This document becomes the input to Wave 4 (handling examples/ violations) and Wave 5 (CI gate flag selection). The decision MUST be a single boolean (include_works yes/no) plus the chosen path letter — Wave 5 plan reads this verbatim.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md && grep -E '^include_works: (true|false)' .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md && grep -E '^chosen_path:' .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `75-W0-SPIKE-RESULTS.md` exists with the exact frontmatter shown
    - `include_works:` line is `true` or `false`
    - `chosen_path:` is one of (a), (b), (c)
    - Mechanisms table has at least 4 rows (one per tested mechanism)
    - `pmat --version` output is recorded in the doc
    - PMAT total complexity violation count is unchanged from baseline (Wave 0 doesn't refactor production code)
  </acceptance_criteria>
  <done>Empirical record of PMAT 3.15.0 path-filter behavior committed; D-09 resolved with a single boolean and chosen path letter.</done>
</task>

<task type="auto">
  <name>Task 1: pmcp-macros insta snapshot baseline (precondition for Wave 1b)</name>
  <files>pmcp-macros/tests/expansion_snapshots.rs, pmcp-macros/Cargo.toml</files>
  <read_first>
    - pmcp-macros/Cargo.toml (verify insta version available; should be 1.43 dev-dep)
    - pmcp-macros/tests/mcp_server_tests.rs (existing test setup; structural analog)
    - pmcp-macros/src/mcp_server.rs (expand_mcp_server signature, line 111)
    - pmcp-macros/src/mcp_tool.rs (expand_mcp_tool signature, line 66)
    - pmcp-macros/src/mcp_resource.rs (expand_mcp_resource signature, line 78)
    - pmcp-macros/src/mcp_prompt.rs (expand_mcp_prompt signature, line 47)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Pitfall 4 — why this matters)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 1b "Test Analogs" row for the new file)
  </read_first>
    <action>
    Create a new integration test file `pmcp-macros/tests/expansion_snapshots.rs` that pins the macro-expansion output of the four worst Wave 1b offenders:
    - `expand_mcp_server` (mcp_server.rs:111, current cog 36)
    - `expand_mcp_tool` (mcp_tool.rs:66, current cog 40)
    - `expand_mcp_resource` (mcp_resource.rs:78, current cog 71)
    - `expand_mcp_prompt` (mcp_prompt.rs:47, current cog 42)

    These are the "regression contract" — Wave 1b must keep snapshots byte-identical (or developer must `cargo insta accept` an intentional change with documented justification per Pitfall 4).

    **POST-REVIEW REVISION (2026-04-23):** The original plan offered two paths
    (a `__test_internal` re-export OR a `cargo expand` fallback). The first is
    PHYSICALLY IMPOSSIBLE — `pmcp-macros/Cargo.toml` has `[lib] proc-macro = true`
    (verified) and the Rust compiler PROHIBITS exporting non-proc-macro `pub`
    items from a proc-macro crate. The two reviewers (Gemini HIGH, Codex MEDIUM)
    both flagged this. The `__test_internal` re-export branch is therefore
    DELETED. Use the `cargo expand` snapshot path described below as the SINGLE
    implementation path — there is no longer a fork. Step 0 (path selection) is
    removed.

    **Implementation: `cargo expand` snapshot path (the only path)**

    1. Verify dev-dependencies. `pmcp-macros/Cargo.toml` already has
       `insta = "1.43"` and `trybuild = "1.0"` in `[dev-dependencies]`
       (verified). Add `cargo-expand` as a budget item — it ships as a
       separate cargo subcommand (`cargo install cargo-expand`); the test
       harness shells out to it via `Command::new("cargo").args(["expand", ...])`.

       **Budget the install in Wave 0:** add a CI-side and local-side step:
       ```bash
       cargo install cargo-expand --locked --version =1.0.107  # or whatever 'pmat 3.15.0' age peer
       ```
       Pin the version so CI and local agree (mirror the PMAT pin pattern from Task 3).

    2. Create fixture files. `pmcp-macros/tests/fixtures/` directory with one
       file per macro target. Each fixture is a minimal but representative
       use of the macro:

       ```
       pmcp-macros/tests/fixtures/example_mcp_tool.rs
       pmcp-macros/tests/fixtures/example_mcp_server.rs
       pmcp-macros/tests/fixtures/example_mcp_resource.rs
       pmcp-macros/tests/fixtures/example_mcp_prompt.rs
       ```

       Example fixture (`example_mcp_tool.rs`) — keep input small but
       cover representative attribute shapes per Step 5 below:

       ```rust
       use pmcp_macros::mcp_tool;

       struct EchoServer;

       #[mcp_tool(name = "echo", description = "Echo a message back to the caller")]
       async fn echo(&self, msg: String) -> Result<String, anyhow::Error> {
           Ok(msg)
       }

       fn main() {}
       ```

    3. Write `pmcp-macros/tests/expansion_snapshots.rs` with 4 tests, each
       shaped like:

       ```rust
       use std::process::Command;
       use insta::assert_snapshot;

       fn expand_fixture(fixture_basename: &str) -> String {
           let fixture_path = format!("tests/fixtures/{}.rs", fixture_basename);
           // `cargo expand --test` requires the fixture to be a registered
           // test target. Easier: invoke `cargo expand` on a tiny ad-hoc
           // package, or use rustc directly with the proc-macro crate as a
           // dependency. Implementer chooses; the simplest pattern is:
           let output = Command::new("cargo")
               .args(["expand", "--manifest-path", &fixture_manifest_for(fixture_basename)])
               .output()
               .expect("cargo expand failed — is `cargo-expand` installed?");
           assert!(output.status.success(),
               "cargo expand failed:\n{}", String::from_utf8_lossy(&output.stderr));
           String::from_utf8(output.stdout).expect("cargo expand output not UTF-8")
       }

       #[test]
       fn snapshot_expand_mcp_tool_basic_signature() {
           let expanded = expand_fixture("example_mcp_tool");
           assert_snapshot!(expanded);
       }

       // ... three more snapshot tests, one per fixture ...
       ```

       **NOTE on `fixture_manifest_for` shape:** the simplest setup is to
       create one tiny Cargo workspace member per fixture under
       `pmcp-macros/tests/fixtures/<name>/` with a `Cargo.toml` and
       `src/main.rs`. The test then runs
       `cargo expand --manifest-path tests/fixtures/example_mcp_tool/Cargo.toml`.
       Implementer picks whatever pattern keeps fixtures self-contained
       and `cargo expand`-able. Document the chosen shape in
       `75-00-SUMMARY.md` for Wave 1b reference.

    4. Run the new test: `cargo test -p pmcp-macros --test expansion_snapshots`.
       First run creates `.snap.new` files; review and accept with
       `cargo insta accept --workspace` (or `cargo insta accept -p pmcp-macros`).
       Commit the accepted `.snap` files alongside this task.

    5. Each fixture must be REPRESENTATIVE — cover the typical attribute
       argument shape (`name`, `description`, optional fields, async fn with
       multiple parameters). Use the existing
       `pmcp-macros/tests/mcp_server_tests.rs` and `mcp_tool_tests.rs` as
       references for what realistic input looks like.

    Do NOT use this task to refactor the expand fns — that's Wave 1b's job.
    This task ONLY captures their current behavior via the macro's consumer-
    facing surface. Snapshot fidelity is the regression contract.

    **Why this path (`cargo expand`) over the alternative (`pmcp-macros-support`):**
    Moving the expand_* functions to `pmcp-macros-support` would require
    redesigning the proc-macro crate as a thin wrapper, exposing all the
    expansion helpers through a non-proc-macro API surface. That's a real
    refactor with hidden risk (e.g. reachability of `proc_macro::TokenStream`
    types from outside the proc-macro context). `cargo expand` snapshots the
    actual end-user-facing macro output and requires no source-code changes
    to the macro crate at all. Lower risk, less disruption to Wave 1b.
  </action>
  <verify>
    <automated>cargo test -p pmcp-macros --test expansion_snapshots -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - File `pmcp-macros/tests/expansion_snapshots.rs` exists
    - File contains exactly 4 `#[test] fn snapshot_expand_*` functions, one per expand fn
    - 4 corresponding `.snap` files (or one consolidated snap module) exist under `pmcp-macros/tests/snapshots/`
    - `cargo test -p pmcp-macros --test expansion_snapshots` exits 0
    - `cargo test --workspace --all-features -- --test-threads=1` exits 0 (no downstream regressions)
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count is unchanged from baseline (purely additive task)
  </acceptance_criteria>
  <done>Snapshot baseline committed and accepted; Wave 1b can begin without losing semantic regression detection.</done>
</task>

<task type="auto">
  <name>Task 2: pmcp-code-mode semantic regression baseline (precondition for Wave 3)</name>
  <files>crates/pmcp-code-mode/tests/eval_semantic_regression.rs</files>
  <read_first>
    - crates/pmcp-code-mode/src/eval.rs (signatures of evaluate_with_scope @ line 59 and evaluate_array_method_with_scope @ line 506; ValueExpr enum definition)
    - crates/pmcp-code-mode/tests/property_tests.rs (existing 14KB test file — structural analog, but it's property tests so it asserts "doesn't panic" not "produces specific value")
    - crates/pmcp-code-mode/src/lib.rs (verify VariableProvider trait and JsonValue type are public)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 0 Gaps section: "Confirm crates/pmcp-code-mode/tests/ covers evaluate_with_scope ... at semantic level")
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 3 Test Analogs row)
  </read_first>
  <action>
    Add a new test file `crates/pmcp-code-mode/tests/eval_semantic_regression.rs` that pins the EXACT JsonValue output for representative `ValueExpr` and `ArrayMethodCall` programs. This file is the regression contract for Wave 3's mandatory refactor of `evaluate_with_scope` (current cog 123, must drop ≤50) and `evaluate_array_method_with_scope` (current cog 117, must drop ≤50).

    The existing `property_tests.rs` only asserts "doesn't panic" — it cannot detect semantic drift where the refactor returns the wrong value. This file fills that gap.

    Implementation:

    1. Read `crates/pmcp-code-mode/src/eval.rs` lines 59-505 to inventory every `ValueExpr` variant `evaluate_with_scope` matches on (Variable, Literal, PropertyAccess, ArrayIndex, ObjectLiteral, ArrayLiteral, BinaryOp, UnaryOp, Ternary, OptionalChain, NullishCoalesce, ArrayMethod, NumberMethod, Block, BuiltinCall, etc. — see PATTERNS.md Wave 3 "target shape" code sample for the full list).

    2. Read lines 506-770 to inventory every `ArrayMethodCall` variant (Map, Filter, Reduce, Find, Some, Every, etc.).

    3. Build a minimal `MockVariableProvider` (or use whatever the existing test files use — read property_tests.rs first to follow convention).

    4. Write at least one `#[test]` per `ValueExpr` variant and per `ArrayMethodCall` variant. Each test:
       - Constructs a representative `ValueExpr` programmatically
       - Calls `evaluate_with_scope` (or `evaluate_array_method_with_scope`)
       - Asserts the EXACT `JsonValue` output with `assert_eq!`

    Example shape (adapt to actual API after reading eval.rs):

    ```rust
    use pmcp_code_mode::eval::{evaluate_with_scope, evaluate_array_method_with_scope};
    use pmcp_code_mode::expr::{ValueExpr, ArrayMethodCall, BinaryOpKind};
    use serde_json::json;
    use std::collections::HashMap;

    struct EmptyVars;
    impl pmcp_code_mode::VariableProvider for EmptyVars {
        fn get(&self, _name: &str) -> Option<serde_json::Value> { None }
    }

    #[test]
    fn evaluate_literal_passthrough() {
        let expr = ValueExpr::Literal(json!(42));
        let out = evaluate_with_scope(&expr, &EmptyVars, &HashMap::new()).unwrap();
        assert_eq!(out, json!(42));
    }

    #[test]
    fn evaluate_binop_addition() {
        let expr = ValueExpr::BinaryOp {
            left: Box::new(ValueExpr::Literal(json!(2))),
            op: BinaryOpKind::Add,
            right: Box::new(ValueExpr::Literal(json!(3))),
        };
        let out = evaluate_with_scope(&expr, &EmptyVars, &HashMap::new()).unwrap();
        assert_eq!(out, json!(5));
    }

    #[test]
    fn evaluate_array_method_map_doubles_each_element() {
        // assert specific JsonValue, not "doesn't panic"
    }
    ```

    5. Coverage minimum: at least 12 tests for `evaluate_with_scope` (one per major ValueExpr variant) + at least 6 tests for `evaluate_array_method_with_scope` (one per common ArrayMethodCall variant: map, filter, reduce, find, some, every).

    6. The exact `assert_eq!` payloads ARE the regression contract. Wave 3's refactor must keep them green byte-identical. If Wave 3 finds a semantic bug (test produces unexpected value PRE-refactor), record the discrepancy in `75-W0-SPIKE-RESULTS.md` rather than "fixing" the test — the test pins CURRENT behavior, not desired behavior.

    **POST-REVIEW REVISION (2026-04-23 — Codex Concern #9):** Hand-built
    `ValueExpr` programs alone are insufficient — they will miss
    parser↔evaluator and scope/lifetime interactions. EXTEND the test file
    with a "real expression corpus" subsection:

    7. **Real expression corpus** (in addition to the per-variant tests in
       step 4-6 above): collect 20-30 representative input programs from
       existing tests/examples that exercise parser↔evaluator and
       scope/lifetime interactions, not just hand-built ASTs.

       Sources to harvest from:
       - `crates/pmcp-code-mode/tests/property_tests.rs` — the "happy path"
         programs the property tests run against (extract the literal ones)
       - `crates/pmcp-code-mode/examples/*.rs` — any example programs
         (if present)
       - `crates/pmcp-code-mode/src/*.rs` doc examples — scan
         `///` rustdoc blocks for `let expr = parse("...");` style examples
       - Any `crates/pmcp-code-mode/tests/fixtures/` directory contents

       Each corpus program is a `(input_string, expected_json_value)` pair:

       ```rust
       const CORPUS: &[(&str, &str)] = &[
           ("1 + 2", "3"),
           ("[1, 2, 3].map(x => x * 2)", "[2, 4, 6]"),
           ("let x = 5; x * x", "25"),
           ("{ a: 1, b: 2 }.b", "2"),
           // ... 20-30 more representative programs ...
       ];

       #[test]
       fn corpus_evaluator_semantic_baseline() {
           for (input, expected) in CORPUS {
               let parsed = pmcp_code_mode::parse(input).expect(input);
               let result = evaluate_with_scope(&parsed, &EmptyVars, &HashMap::new())
                   .expect(input);
               let expected_json: serde_json::Value =
                   serde_json::from_str(expected).expect(expected);
               assert_eq!(result, expected_json,
                   "corpus mismatch for input: {}", input);
           }
       }
       ```

       This catches the parser↔evaluator interaction bugs that pure AST
       construction tests cannot. Wave 3's refactor must keep this corpus
       byte-identical (no behavior drift, even on programs that go
       parser → evaluator).

    Coverage minimum updated: at least 12 per-variant tests for
    `evaluate_with_scope` + at least 6 per-method tests for
    `evaluate_array_method_with_scope` + 20-30 corpus programs.

    Do NOT touch `eval.rs` in this task — it's a pure test addition.
  </action>
  <verify>
    <automated>cargo test -p pmcp-code-mode --test eval_semantic_regression -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/pmcp-code-mode/tests/eval_semantic_regression.rs` exists
    - File contains at least 18 distinct `#[test]` functions (12 + 6 minimum per coverage rule)
    - Every test uses `assert_eq!` with a literal expected `JsonValue` (no `.is_ok()` only / no panic-only assertions)
    - `cargo test -p pmcp-code-mode --test eval_semantic_regression -- --test-threads=1` exits 0
    - `cargo test --workspace --all-features -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count is unchanged from baseline (purely additive task)
  </acceptance_criteria>
  <done>Semantic regression baseline committed for Wave 3's two highest-cog functions; refactor cannot silently change behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Pin PMAT version in CI workflows (Pitfall 1 mitigation)</name>
  <files>.github/workflows/ci.yml, .github/workflows/quality-badges.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current `cargo install pmat` invocation, if any; the existing `quality-gate` job around lines 167-205)
    - .github/workflows/quality-badges.yml (current `cargo install pmat --locked` invocation around line 38)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Pitfall 1 — version drift problem)
    - .planning/phases/75-fix-pmat-issues/75-VALIDATION.md (Wave 0 Requirements: PMAT version pin item)
  </read_first>
  <action>
    Pin PMAT to version `=3.15.0` in BOTH workflow files so the gate semantics don't drift between local and CI as PMAT releases.

    Steps:

    1. In `.github/workflows/quality-badges.yml`: locate the line `cargo install pmat --locked` (per RESEARCH.md, around line 38). Change it to `cargo install pmat --version =3.15.0 --locked`. Add a step BEFORE the install that prints the existing pmat version if any (for cache visibility) and a step AFTER that runs `pmat --version` and asserts the output starts with `pmat 3.15.0`.

    2. In `.github/workflows/ci.yml`: search for any `cargo install pmat` invocation. If none exists today (Wave 5 will add one), this task only modifies `quality-badges.yml` and leaves `ci.yml` untouched UNTIL Wave 5 — but Wave 5 must use the same `--version =3.15.0 --locked` form. To make this binding now, add a comment in `ci.yml` near the existing `quality-gate` job stating: `# When adding PMAT to CI gate (Phase 75 Wave 5 / D-07), pin: cargo install pmat --version =3.15.0 --locked`. This is a 1-line comment edit; no behavior change.

    3. Verify the workflow syntax is still valid. The simplest local check is:
       ```bash
       # If `act` or `actionlint` is available, run it; otherwise visually inspect.
       which actionlint && actionlint .github/workflows/quality-badges.yml .github/workflows/ci.yml
       ```
       If neither is available, lint via GitHub UI on the PR.

    4. Document the pin choice in the spike doc (75-W0-SPIKE-RESULTS.md from Task 0): add a "PMAT version pin rationale" section noting "pinned to 3.15.0 to match local development environment; bump is intentional and separate".

    Do NOT add the actual quality-gate gate step to ci.yml in this task — that's Wave 5's job. This task ONLY pins the version.
  </action>
  <verify>
    <automated>grep -E 'cargo install pmat --version =3\.15\.0 --locked' .github/workflows/quality-badges.yml && grep -E 'pmat --version' .github/workflows/quality-badges.yml && (grep -E 'Phase 75 Wave 5.*pmat --version =3\.15\.0' .github/workflows/ci.yml || echo "ci.yml comment optional if Wave 5 pin happens directly")</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/quality-badges.yml` `cargo install pmat` invocation is pinned to `--version =3.15.0 --locked`
    - `.github/workflows/quality-badges.yml` has a verification step that runs `pmat --version` (visible in CI logs)
    - `.github/workflows/ci.yml` either has the pin (if Wave 5 prep) or a comment stating the pin requirement for Wave 5
    - PMAT total complexity violation count is unchanged from baseline (workflow change doesn't affect repo source)
    - Workflow YAML is syntactically valid (no parse errors when pushed)
  </acceptance_criteria>
  <done>PMAT pinned to 3.15.0 in CI; gate semantics will not drift between local and CI runs.</done>
</task>


<task type="auto">
  <name>Task 4: Verify PMAT honors `#[allow(clippy::cognitive_complexity)]` (D-10 load-bearing check)</name>
  <files>.planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md, /tmp/pmat-allow-fixture/</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-10 — branch-on-result decision tree)
    - .planning/phases/75-fix-pmat-issues/75-REVIEWS.md (Codex HIGH concern: "PMAT `#[allow]` suppression is load-bearing but UNVERIFIED")
  </read_first>
  <action>
    Empirically verify whether PMAT 3.15.0's complexity check honors
    `#[allow(clippy::cognitive_complexity)]`. The result drives whether P5
    remains a valid technique throughout Waves 1-4 (per CONTEXT.md D-10).

    Steps:

    1. Create a throwaway fixture project at `/tmp/pmat-allow-fixture/`
       with a single Rust file containing one function with cognitive
       complexity ≥30. The function should be deeply nested so it's
       unambiguously over the 25-cog threshold:

       ```rust
       // /tmp/pmat-allow-fixture/src/lib.rs
       pub fn deeply_nested_for_pmat_test(input: i32) -> i32 {
           match input {
               0 => match input + 1 {
                   1 => match input + 2 {
                       2 => if input == 0 { 1 } else { 2 },
                       _ => match input + 3 { 3 => 4, _ => 5 },
                   },
                   _ => match input + 2 { 2 => 6, _ => match input + 3 { 3 => 7, _ => 8 } },
               },
               1 => match input + 1 { 2 => match input + 2 { 3 => 9, _ => 10 }, _ => 11 },
               2 => if input > 0 { match input { 2 => 12, _ => 13 } } else { 14 },
               3 => match input + 1 { 4 => 15, _ => match input + 2 { 5 => 16, _ => 17 } },
               4 => match input + 1 { 5 => 18, _ => match input + 2 { 6 => 19, _ => 20 } },
               _ => 0,
           }
       }
       ```

       Plus a minimal `Cargo.toml`:
       ```toml
       [package]
       name = "pmat-allow-fixture"
       version = "0.0.0"
       edition = "2021"
       [lib]
       path = "src/lib.rs"
       ```

    2. Baseline measurement: run
       `cd /tmp/pmat-allow-fixture && pmat analyze complexity --format json --max-cognitive 25`.
       CONFIRM the function appears in the violations list with cog ≥30.
       Record the exact cog score.

    3. Add the project-wide `// Why:` justified `#[allow]` per the
       `<project_wide_why_template>` in this plan's `<context>` section:

       ```rust
       // Why: test fixture for PMAT allow-suppression verification per Phase 75 D-10
       #[allow(clippy::cognitive_complexity)]
       pub fn deeply_nested_for_pmat_test(input: i32) -> i32 { /* unchanged */ }
       ```

    4. Re-run `pmat analyze complexity --format json --max-cognitive 25`.
       Record whether the function STILL appears in the violations list.

    5. Also test the gate command path (the one Wave 5 will use):
       `pmat quality-gate --fail-on-violation --checks complexity --format json`.
       Record whether the gate exits 0 (suppression works) or 1
       (suppression ignored).

    6. Write `.planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md`:

       ```markdown
       ---
       verification: D-10 PMAT allow-suppression behavior
       pmat_version: <pmat --version output>
       date: <today>
       fixture_path: /tmp/pmat-allow-fixture/
       ---

       # PMAT allow-suppression empirical verification

       ## Baseline
       - Function: deeply_nested_for_pmat_test
       - Cognitive complexity (PMAT-reported): NN
       - `pmat analyze complexity` reports violation: yes

       ## After adding `#[allow(clippy::cognitive_complexity)]` with `// Why:` comment
       - `pmat analyze complexity` reports violation: yes/no
       - `pmat quality-gate --fail-on-violation --checks complexity` exit code: 0/1

       ## Decision (per CONTEXT.md D-10)
       outcome: D-10-A | D-10-B
       (D-10-A: PMAT honors the allow → P5 remains valid in Waves 1-4)
       (D-10-B: PMAT IGNORES the allow → P5 removed from toolkit; all functions must reduce to ≤25)

       ## Implications for Waves 1-4 (only filled if outcome = D-10-B)
       - Wave 1a projected count drop: was -8, now (without P5) requires more refactor effort
       - Wave 1b projected count drop: was -7, now requires deeper extraction
       - Wave 2: ...
       - Wave 3: evaluate_with_scope and evaluate_array_method_with_scope must reach ≤25 mandatorily (not just ≤50)
       - Wave 4: examples/ and fuzz/ must use path-filter (D-09 chosen_path: a) — bulk-allow falls back to refactor

       ## Recommendation to user (only if outcome = D-10-B)
       Surface to operator via 75-00-SUMMARY.md "SCOPE EXPANSION DETECTED": the
       phase budget no longer fits the original wave structure; user should
       decide between (i) splitting Phase 75 into 75 + 75.5, (ii) accepting
       additional refactor effort, (iii) raising the cog threshold (rejected
       per CONTEXT.md deferred ideas).
       ```

    7. Cleanup: `rm -rf /tmp/pmat-allow-fixture/`. The fixture is throwaway;
       only the `pmat-allow-behavior.md` artifact persists in the repo.

    If outcome is **D-10-B (PMAT ignores allow)**, this task's
    `<acceptance_criteria>` REQUIRES surfacing a "SCOPE EXPANSION DETECTED"
    note in `75-00-SUMMARY.md` and HALTING Wave 1 until the operator
    decides path forward. The executor MUST NOT silently proceed under
    inflated count-drop projections.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md && grep -E '^outcome: D-10-(A|B)' .planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md && grep -E 'pmat_version:' .planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pmat-allow-behavior.md` exists with the exact frontmatter shown
    - `outcome:` line is `D-10-A` or `D-10-B`
    - PMAT version recorded
    - Both `pmat analyze` and `pmat quality-gate` results recorded
    - If outcome is D-10-B: 75-00-SUMMARY.md contains "SCOPE EXPANSION DETECTED" header AND Wave 1 plans are marked `BLOCKED — awaiting operator decision`
    - If outcome is D-10-A: no further action needed; Waves 1-4 proceed as planned
    - Fixture project at /tmp/pmat-allow-fixture/ deleted
  </acceptance_criteria>
  <done>D-10 resolved with binary outcome; downstream waves know whether P5 is valid.</done>
</task>

<task type="auto">
  <name>Task 5: Verify badge command vs CI gate command alignment (D-11 stated-goal-failure check)</name>
  <files>.planning/phases/75-fix-pmat-issues/badge-vs-gate-verification.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-11 — branch-on-result decision tree)
    - .planning/phases/75-fix-pmat-issues/75-REVIEWS.md (Codex HIGH concern #4: "Badge/gate semantic mismatch")
    - .github/workflows/quality-badges.yml lines 69-83 (the bare gate command that drives the badge)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md "Badge/Gate Command Verification" section (post-review addition)
  </read_first>
  <action>
    Empirically verify whether the README badge command and the planned CI
    gate command produce the same exit code on the current baseline. The
    result drives whether Wave 5 must edit `quality-badges.yml` in addition
    to `ci.yml`. Per CONTEXT.md D-11, the phase goal is "restore the badge",
    not "make CI green" — so any mismatch requires Wave 5 to align both.

    Steps:

    1. Run BOTH gate commands on the current baseline branch and capture
       per-check breakdowns:

       ```bash
       # Bare gate — what the badge uses (quality-badges.yml line 72)
       pmat quality-gate --fail-on-violation --format json > /tmp/bare-gate.json 2>&1
       BARE_EXIT=$?

       # Complexity-only gate — what Wave 5 plans to add to ci.yml
       pmat quality-gate --fail-on-violation --checks complexity --format json > /tmp/cx-gate.json 2>&1
       CX_EXIT=$?

       # Per-check breakdown for the bare gate
       jq '[.violations[] | .check] | group_by(.) | map({check: .[0], count: length})' /tmp/bare-gate.json
       # Output shape: [ {"check": "complexity", "count": NN}, {"check": "satd", "count": NN}, ... ]
       ```

    2. Determine which dimensions cause the bare gate's non-zero exit.
       Cross-reference with the baseline counts in CONTEXT.md (complexity
       94, satd 33, duplicate 439, entropy 4, sections 2). Some of these
       may be reported but not actually failure-counted (PMAT may treat
       some as informational by default).

    3. Write
       `.planning/phases/75-fix-pmat-issues/badge-vs-gate-verification.md`:

       ```markdown
       ---
       verification: D-11 badge/gate command alignment
       pmat_version: <pmat --version>
       date: <today>
       ---

       # Badge vs CI Gate Command Alignment Verification

       ## Bare gate command (drives the README badge today)
       Command: `pmat quality-gate --fail-on-violation`
       Exit code: NN
       Per-check failure contribution:
       - complexity: count NN — fails gate? yes/no
       - satd: count NN — fails gate? yes/no
       - duplicate: count NN — fails gate? yes/no
       - entropy: count NN — fails gate? yes/no
       - sections: count NN — fails gate? yes/no

       ## Complexity-only gate command (planned for Wave 5 ci.yml)
       Command: `pmat quality-gate --fail-on-violation --checks complexity`
       Exit code: NN
       Violation count: NN

       ## Decision (per CONTEXT.md D-11)
       outcome: D-11-A | D-11-B

       ### If D-11-A (only complexity fails today)
       After Wave 1-4 land and complexity hits 0, the bare gate will also
       exit 0 and the badge will flip green automatically. Wave 5 only
       needs to edit `ci.yml` (no `quality-badges.yml` change required).

       ### If D-11-B (other dimensions also fail)
       Wave 5 Task 5-01 MUST ALSO update `quality-badges.yml` line ~72
       to match the CI gate command. Two equivalent patches:
       - Both use `--checks complexity` (simplest; badge reflects only
         complexity per D-01)
       - Both use a path filter from D-09 (e.g.
         `--include 'src/**,crates/*/src/**,...'`)

       Recommended patch shape (D-11-B branch):
       ```yaml
       # In quality-badges.yml line 72, replace:
       if pmat quality-gate --fail-on-violation --format json > quality_gate.json 2>/dev/null; then
       # With:
       if pmat quality-gate --fail-on-violation --checks complexity --format json > quality_gate.json 2>/dev/null; then
       ```

       ## Wave 5 plan delta (filled in based on outcome)
       Files to modify in Wave 5:
       - .github/workflows/ci.yml: yes (always)
       - .github/workflows/quality-badges.yml: only if outcome = D-11-B

       ## Implications for Wave 5 acceptance criteria
       The "badge flipped" verification (Task 5-03) is the binding goal.
       If the bare gate is still failing post-Wave 4 due to non-complexity
       dimensions, the badge will NOT flip — and Wave 5 was incomplete.
       ```

    4. The Wave 5 plan (75-05-PLAN.md) is authored to handle BOTH
       branches; this task's output narrows to one. Wave 5 Task 5-01
       reads this doc to decide whether to edit `quality-badges.yml`.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/badge-vs-gate-verification.md && grep -E '^outcome: D-11-(A|B)' .planning/phases/75-fix-pmat-issues/badge-vs-gate-verification.md && grep -E 'pmat_version:' .planning/phases/75-fix-pmat-issues/badge-vs-gate-verification.md</automated>
  </verify>
  <acceptance_criteria>
    - File `badge-vs-gate-verification.md` exists with the exact frontmatter shown
    - `outcome:` line is `D-11-A` or `D-11-B`
    - Per-check failure contribution table is filled in (one row per dimension)
    - "Wave 5 plan delta" section explicitly lists which workflow file(s) Wave 5 must edit
    - PMAT total complexity violation count unchanged from baseline (this task is read-only)
  </acceptance_criteria>
  <done>D-11 resolved; Wave 5 knows whether to edit only ci.yml or both ci.yml + quality-badges.yml.</done>
</task>

<task type="auto">
  <name>Task 6: Generate machine-readable inventory snapshot (concern #6 reconciliation)</name>
  <files>.planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json, .planning/phases/75-fix-pmat-issues/pmat-inventory-summary.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md "Inventory Reconciliation" section (post-review addition)
    - .planning/phases/75-fix-pmat-issues/75-REVIEWS.md (HIGH concern #6: 94 vs 97 inventory math)
  </read_first>
  <action>
    The 94/73/21/3 figures in CONTEXT.md and RESEARCH.md don't add up
    cleanly (21+3+73 = 97, not 94). The Codex reviewer flagged this as
    "every wave delta and exit criterion is suspect" until reconciled.
    This task generates ONE authoritative machine-readable inventory snapshot
    that every later wave references via `jq` queries — eliminating the
    prose-vs-truth drift entirely.

    Steps:

    1. Generate the inventory:

       ```bash
       pmat analyze complexity --format json --max-cognitive 25 \
           > .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json
       ```

       Verify the file is valid JSON:
       ```bash
       jq '. | length' .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json
       jq '.violations | length' .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json
       ```

    2. Cross-check against the gate command (the badge signal):

       ```bash
       pmat quality-gate --fail-on-violation --checks complexity --format json \
           > /tmp/gate-snapshot.json
       jq '.violations | length' /tmp/gate-snapshot.json
       ```

       Record both counts. The two numbers may differ slightly because
       `analyze complexity` and `quality-gate --checks complexity` can
       use different thresholds/include-patterns. The `quality-gate`
       count is the badge-relevant truth.

    3. Generate per-directory bucketing:

       ```bash
       jq -r '.violations[] | .path' \
           .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json \
       | awk -F/ '{print $1}' | sort | uniq -c | sort -rn \
           > /tmp/inventory-by-toplevel-dir.txt
       cat /tmp/inventory-by-toplevel-dir.txt
       ```

    4. Write `.planning/phases/75-fix-pmat-issues/pmat-inventory-summary.md`:

       ```markdown
       ---
       inventory_snapshot: pmat-inventory-2026-04-22.json
       pmat_version: <pmat --version>
       date: <today>
       commit_sha: <git rev-parse HEAD>
       ---

       # PMAT Inventory Snapshot — Authoritative Counts

       ## Source of truth
       Every Wave-N success criterion derives from
       `pmat-inventory-2026-04-22.json` via `jq`, NOT from prose
       counts in CONTEXT.md or RESEARCH.md.

       ## Top-line counts
       - `pmat analyze complexity --format json --max-cognitive 25`
         total violations: NN
       - `pmat quality-gate --fail-on-violation --checks complexity`
         total violations: NN
       - **Badge-relevant count (use this for wave deltas):** NN

       ## Per-top-level-directory breakdown
       (from jq aggregation of pmat-inventory-2026-04-22.json)
       - src/: NN
       - crates/: NN
       - cargo-pmcp/: NN
       - pmcp-macros/: NN
       - examples/: NN
       - fuzz/: NN
       - tests/: NN (if any)
       - other: NN

       ## Reconciliation note
       Earlier prose counts (94/73/21/3) approximated this snapshot but
       did not align cleanly. Discrepancy explanations:
       - <fill in based on actual numbers — likely some functions
         straddle the threshold, or per-rule counting differs>

       ## How later waves use this
       Each Wave's `<verification>` block now includes:
       ```bash
       BASELINE=$(jq '.violations | length' .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json)
       CURRENT=$(pmat analyze complexity --format json --max-cognitive 25 | jq '.violations | length')
       echo "wave delta: $((BASELINE - CURRENT))"
       ```
       Expected delta per wave (informational, not gating):
       - Wave 1a: -8 (streamable_http_server hotspots)
       - Wave 1b: -7 (pmcp-macros hotspots)
       - Wave 2: -19 (cargo-pmcp/pentest + deployment + scattered cargo-pmcp/commands)
       - Wave 3: -5 (pmcp-code-mode hotspots)
       - Wave 4: remainder to 0
       ```

    5. Commit BOTH files (the JSON snapshot AND the summary doc) so the
       inventory is reproducible and reviewable. The JSON snapshot is
       moderately large (~hundreds of KB) but is the source of truth —
       commit it so future executors can compare against it without
       re-running PMAT.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json && jq -e '.violations | length' .planning/phases/75-fix-pmat-issues/pmat-inventory-2026-04-22.json && test -f .planning/phases/75-fix-pmat-issues/pmat-inventory-summary.md && grep -E 'inventory_snapshot:' .planning/phases/75-fix-pmat-issues/pmat-inventory-summary.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pmat-inventory-2026-04-22.json` exists and is valid JSON
    - File `pmat-inventory-summary.md` exists with frontmatter and per-directory breakdown
    - Both top-line counts (`analyze` vs `quality-gate`) recorded
    - Per-directory breakdown summed equals top-line `analyze` count
    - PMAT version recorded; commit SHA recorded
    - Wave-merge commits in Waves 1-5 reference these files in their delta calculations (this is enforced via downstream wave plans, not this task)
  </acceptance_criteria>
  <done>One authoritative inventory file commits to repo; all later wave deltas derive from it via jq.</done>
</task>

</tasks>

<verification>
After all 7 tasks complete (Tasks 0-6 — original 0/1/2/3 + post-review additions 4/5/6):

1. `pmat --version` on local matches CI pin (3.15.0).
2. `cargo test --workspace --all-features -- --test-threads=1` exits 0 — no regressions from added test files.
3. `cargo clippy --all-targets --all-features -- -D warnings` exits 0.
4. `make quality-gate` exits 0.
5. `pmat quality-gate --fail-on-violation --checks complexity --format json | jq '.violations | length'` is unchanged from baseline (Wave 0 is purely additive — no production-code refactor).
6. The 6 new artifacts exist with the required content:
   - `75-W0-SPIKE-RESULTS.md` (Task 0 — D-09 path-filter resolution)
   - `pmcp-macros/tests/expansion_snapshots.rs` + `pmcp-macros/tests/fixtures/*` (Task 1 — cargo-expand snapshots)
   - `crates/pmcp-code-mode/tests/eval_semantic_regression.rs` (Task 2 — incl. real-expression corpus)
   - `pmat-allow-behavior.md` (Task 4 — D-10 PMAT-allow verification)
   - `badge-vs-gate-verification.md` (Task 5 — D-11 badge/gate alignment)
   - `pmat-inventory-2026-04-22.json` + `pmat-inventory-summary.md` (Task 6 — authoritative inventory)
7. The single `// Why:` template in this plan's `<context>` section is the source-of-truth for all later waves.
8. **D-10 + D-11 outcomes recorded.** If outcome is D-10-B (PMAT ignores allow) OR D-11-B (badge/gate mismatch), 75-00-SUMMARY.md surfaces "SCOPE EXPANSION DETECTED" / "WAVE 5 ADDITIONAL EDIT REQUIRED" headers and Wave 1 / Wave 5 plans note the implication. The executor MUST NOT silently proceed under inflated assumptions.

Per VALIDATION.md Wave 0 sign-off:
- [ ] All 7 W0 items addressed (path-filter spike, expansion snapshots, semantic baseline, version pin, PMAT-allow verification, badge/gate verification, inventory snapshot)
- [ ] `nyquist_compliant: true` can be set in VALIDATION.md frontmatter after this plan merges
</verification>

<success_criteria>
- Wave 0 commit message includes `pmat-complexity: NN (was NN)` showing count is unchanged (Wave 0 is additive only)
- 75-W0-SPIKE-RESULTS.md `include_works:` boolean is committed
- pmat-allow-behavior.md `outcome:` is committed (D-10-A or D-10-B)
- badge-vs-gate-verification.md `outcome:` is committed (D-11-A or D-11-B)
- pmat-inventory-2026-04-22.json + pmat-inventory-summary.md are committed
- 4 expand-fn snapshots (via cargo expand on per-fixture sub-projects) are accepted and committed
- ≥18 semantic regression tests + ≥20 corpus tests for the eval.rs hotspots are committed
- PMAT pin lands in CI workflows
- Wave 1 can start with confidence that its refactors will be detected if they regress, AND with verified knowledge of whether P5 is a valid technique (D-10) and whether Wave 5 must edit `quality-badges.yml` (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/75-fix-pmat-issues/75-00-SUMMARY.md` per the GSD summary template. The SUMMARY MUST include:
- D-10 outcome (A or B) and implications for Waves 1-4
- D-11 outcome (A or B) and implications for Wave 5
- If outcome is D-10-B: a "SCOPE EXPANSION DETECTED" header at the top with explicit re-budgeting recommendation
- If outcome is D-11-B: a "WAVE 5 ADDITIONAL EDIT REQUIRED" header noting `quality-badges.yml` must be patched
- Inventory delta (jq counts from `pmat-inventory-2026-04-22.json`)

Update `.planning/phases/75-fix-pmat-issues/75-VALIDATION.md` frontmatter `wave_0_complete: true` and `nyquist_compliant: true` once all 7 tasks merge.
</output>
