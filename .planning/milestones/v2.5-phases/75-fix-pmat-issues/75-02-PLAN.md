---
phase: 75-fix-pmat-issues
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - cargo-pmcp/src/pentest/attacks/data_exfiltration.rs
  - cargo-pmcp/src/pentest/attacks/prompt_injection.rs
  - cargo-pmcp/src/pentest/attacks/protocol_abuse.rs
  - cargo-pmcp/src/pentest/attacks/auth_flow.rs
  - cargo-pmcp/src/deployment/targets/cloudflare/init.rs
  - cargo-pmcp/src/deployment/targets/pmcp_run/deploy.rs
  - cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs
  - cargo-pmcp/src/main.rs
  - cargo-pmcp/src/commands/test/check.rs
  - cargo-pmcp/src/commands/test/run.rs
  - cargo-pmcp/src/commands/test/upload.rs
  - cargo-pmcp/src/commands/test/apps.rs
  - cargo-pmcp/src/commands/test/list.rs
  - cargo-pmcp/src/commands/deploy/mod.rs
  - cargo-pmcp/src/commands/doctor.rs
  - cargo-pmcp/src/commands/add.rs
  - cargo-pmcp/src/commands/validate.rs
  - cargo-pmcp/src/commands/dev.rs
  - cargo-pmcp/src/commands/pentest.rs
  - cargo-pmcp/src/commands/preview.rs
  - cargo-pmcp/src/commands/landing/init.rs
  - cargo-pmcp/src/commands/landing/deploy.rs
  - cargo-pmcp/src/commands/loadtest/run.rs
  - cargo-pmcp/src/loadtest/vu.rs
  - cargo-pmcp/src/loadtest/summary.rs
  - cargo-pmcp/src/landing/template.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 41 cargo-pmcp/ hotspot functions are cog ≤25 OR ≤50 with `// Why:` justified `#[allow]`"
    - "Pentest attack probe URI lists and response-evaluation logic preserved byte-for-byte"
    - "cargo-pmcp CLI sub-commands invoke same external behavior (test/run/upload/apps, deploy, doctor, etc.)"
    - "Wave 2 commit message records `pmat-complexity: NN (was MM)` with NN < MM by ~41"
  artifacts:
    - path: "cargo-pmcp/src/main.rs"
      provides: "execute_command refactored to thin dispatch (cog 48→≤25)"
    - path: "cargo-pmcp/src/commands/test/check.rs"
      provides: "execute refactored from cog 105 → ≤50 (mandatory per D-03)"
    - path: "cargo-pmcp/src/commands/deploy/mod.rs"
      provides: "handle_oauth_action refactored from cog 91 → ≤50 (mandatory per D-03)"
    - path: "cargo-pmcp/src/deployment/targets/cloudflare/init.rs"
      provides: "Shared scan_for_package helper extracted; 6 functions refactored"
  key_links:
    - from: "cargo-pmcp/src/main.rs::execute_command"
      to: "per-subcommand execute helpers"
      via: "thin match arm dispatch"
      pattern: "execute_(add|landing|preview)"
    - from: "cargo-pmcp/src/deployment/targets/cloudflare/init.rs::find_*_package"
      to: "scan_for_package shared helper"
      via: "predicate-based directory scanning"
      pattern: "scan_for_package"
---

<objective>
Refactor 41 cargo-pmcp/ hotspot functions from RESEARCH.md's expanded Wave 2 inventory (D-08) — 9 in pentest/ + 10 in deployment/ + 22 in commands/ + main.rs + loadtest + landing. Two mandatory refactors stand out: `commands/test/check.rs::execute` at cog 105 (highest in cargo-pmcp) and `commands/deploy/mod.rs::handle_oauth_action` at cog 91 — both are 2× over D-03 ceiling 50, so P5 alone is forbidden.

**Purpose:** Drop PMAT complexity violation count by ~41 (the second-largest contribution to badge flip). cargo-pmcp/ has no `tests/` directory and no pre-existing bare `#[allow]` sites — all P5 in this wave are NEW allows requiring `// Why:` per D-02.

**Output:** 26 files modified in place. Sub-wave 2a (pentest) is independent of 2b (deployment + commands + main.rs); within 2b, three task lanes split by file group to stay within ~50% context budget per task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-fix-pmat-issues/75-CONTEXT.md
@.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
@.planning/phases/75-fix-pmat-issues/75-PATTERNS.md
@.planning/phases/75-fix-pmat-issues/75-VALIDATION.md
@.planning/phases/75-fix-pmat-issues/75-00-PLAN.md
@.planning/phases/75-fix-pmat-issues/75-01-SUMMARY.md
@CLAUDE.md
@cargo-pmcp/src/main.rs
@cargo-pmcp/src/pentest/attacks/data_exfiltration.rs
@cargo-pmcp/src/deployment/targets/cloudflare/init.rs

<refactor_techniques>
P1-P5 from 75-01-PLAN.md (P6 is Wave 3 only). cargo-pmcp/ refactors are nearly all P1 (extract per-arm helpers).
</refactor_techniques>

<within_wave_parallelism>
Sub-wave 2a (pentest) and sub-wave 2b (deployment + commands + main.rs) touch DISJOINT file sets and can run in parallel. Sequential within each task. The hard ordering constraint within Wave 2 is: complete BEFORE Wave 3 starts (Wave 3 depends on Wave 2 for monotonic count decrease).

Within sub-wave 2b: 3 task lanes split by file group:
- Task 2b-A: deployment/ (10 fns, 3 files)
- Task 2b-B: 2 monster commands (check::execute cog 105, handle_oauth_action cog 91) — separate task because each touches one large file with mandatory refactor depth
- Task 2b-C: remaining 20 commands + main.rs + loadtest + landing (sequential file lane)

Tasks 2b-A, 2b-B, 2b-C touch disjoint files (verified against `files_modified`) so they can run in parallel within sub-wave 2b.
</within_wave_parallelism>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pentest probe → target server | Network-touching attack probes; refactor must preserve probe URI lists and response-evaluation logic byte-for-byte |
| CLI user → cargo-pmcp commands | User-initiated commands (deploy, test, doctor, oauth) execute against external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-02-01 | Tampering | Pentest probe URI list refactor could omit a probe class, missing real vulnerabilities | mitigate | Refactor extracts per-probe HELPERS but the URI list literal stays in `run_de01_ssrf` etc.; manual `cargo run -p cargo-pmcp -- pentest --dry-run` smoke per VALIDATION.md |
| T-75-02-02 | Spoofing | OAuth handler refactor (handle_oauth_action cog 91) could mis-route credentials between dev/staging/prod profiles | mitigate | Per-OAuthAction-variant helpers extracted; existing in-file tests + dry-run smoke; refactor MUST preserve `OAuthAction` variant semantics 1:1 |
| T-75-02-03 | Denial of Service | Loadtest VU loop refactor could change concurrency semantics | mitigate | `vu_loop_inner` (cog 37) refactor extracts per-stage helpers; existing `cargo test -p cargo-pmcp loadtest::` covers; manual smoke against local server |
| T-75-02-04 | Information Disclosure | check::execute (cog 105) refactor could leak diagnostic info between sub-checks | mitigate | Refactor is structural — sub-checks become helpers but produce identical stdout; pre-refactor regression test added per Task 2b-B |

**Refactor scope note:** Wave 2 is internal-control-flow only. No new ingress, no protocol changes. Pentest behavior is the highest-risk surface — probe URI lists and response evaluation MUST stay byte-identical. Existing tests + manual `pentest --dry-run` smoke cover.
</threat_model>

<tasks>

<!-- ===================== SUB-WAVE 2a: pentest/ ===================== -->

<task type="auto">
  <name>Task 2a: Refactor cargo-pmcp/src/pentest/attacks/* (9 functions across 4 files)</name>
  <files>cargo-pmcp/src/pentest/attacks/data_exfiltration.rs, cargo-pmcp/src/pentest/attacks/prompt_injection.rs, cargo-pmcp/src/pentest/attacks/protocol_abuse.rs, cargo-pmcp/src/pentest/attacks/auth_flow.rs</files>
  <read_first>
    - cargo-pmcp/src/pentest/attacks/data_exfiltration.rs (full file — 3 hotspots: run_de01_ssrf @ 121 cog 45, run_de03_content_injection @ 306 cog 27, run_de02_path_traversal @ 230 cog 25)
    - cargo-pmcp/src/pentest/attacks/data_exfiltration.rs lines 21-41 (`pub async fn run` orchestrator — the analog shape per PATTERNS.md)
    - cargo-pmcp/src/pentest/attacks/prompt_injection.rs (check_value_for_markers @ 98 cog 44, run_deep_fuzz @ 576 cog 40)
    - cargo-pmcp/src/pentest/attacks/protocol_abuse.rs (3 hotspots; line 346 `run_pa03_batch_abuse` is the under-threshold analog target shape)
    - cargo-pmcp/src/pentest/attacks/auth_flow.rs (run_af03_jwt_algorithm_confusion @ 322 cog 30)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 2a — full hotspot table + concrete extractions)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 2a; "Risk: Medium — newer code, less battle-tested — refactor must preserve probe URI lists and response-evaluation logic byte-for-byte")
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Refactor 9 hotspot functions across 4 pentest files. Each refactor is its own atomic commit. Apply **P1** uniformly — the analog `pub async fn run` at `data_exfiltration.rs:21-41` shows the target dispatcher shape, and `run_pa03_batch_abuse` at `protocol_abuse.rs:346` is a worked example of a per-probe function already meeting ≤25.

    **Critical preservation constraint** (from PATTERNS.md / threat_model): Probe URI lists and response-evaluation conditions must stay byte-identical. The refactor extracts the EVALUATION HELPERS, not the probe data.

    Per-file actions:

    **data_exfiltration.rs (3 fns):**
    1. `run_de01_ssrf` @ 121, cog 45 → ≤25. Extract per PATTERNS.md "Concrete extractions": `evaluate_probe_response(uri, response, surface) -> Option<SecurityFinding>`, `build_finding_for(category, severity, attack_id, uri, evidence) -> SecurityFinding`, and the `for uri in PROBE_URIS { ... }` body → `probe_single_uri(tester, uri, ...) -> Option<SecurityFinding>`. URI literal arrays remain in the outer fn.
    2. `run_de03_content_injection` @ 306, cog 27 → ≤25. Same shape — extract `evaluate_injection_response`, `probe_single_payload`.
    3. `run_de02_path_traversal` @ 230, cog 25. Borderline — may auto-resolve from companion refactors. If still 25 after #1 and #2 land, apply same P1 pattern.

    **prompt_injection.rs (2 fns):**
    1. `check_value_for_markers` @ 98, cog 44 → ≤25. Extract `check_marker_in_string`, `check_marker_in_array`, `check_marker_in_object` per Value variant.
    2. `run_deep_fuzz` @ 576, cog 40 → ≤25. P1 + extract per-payload-class loop into `fuzz_with_payload_class(class, tester, surface) -> Vec<SecurityFinding>`.

    **protocol_abuse.rs (3 fns):**
    1. `run_pa02_oversized_request` @ 214, cog 36 → ≤25. P1.
    2. `run_pa01_malformed_jsonrpc` @ 101, cog 34 → ≤25. P1.
    3. `run_pa04_notification_flooding` @ 457, cog 28 → ≤25. P1.

    **auth_flow.rs (1 fn):**
    1. `run_af03_jwt_algorithm_confusion` @ 322, cog 30 → ≤25. P1.

    Per-refactor commit: `refactor(75-02a): <fn_name> in <file> (cog NN→MM)`.

    After refactor, run:
    - `cargo test -p cargo-pmcp pentest:: -- --test-threads=1` (in-file tests if present)
    - Manual smoke per VALIDATION.md: `cargo run -p cargo-pmcp -- pentest --help` (verify subcommand plumbing intact). The full `--dry-run` against a real server is a manual checkpoint listed in VALIDATION.md "Manual-Only Verifications" — not blocking the refactor commit but called out in the task SUMMARY.
    - `cargo clippy --all-targets --all-features -- -D warnings`

    Cog ≤25 should be reachable for all 9 functions with P1 alone — PATTERNS.md notes no P5 expected. If residual cog 26-50 after extraction, P5 with substantive `// Why:` per W0 template.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp -- --test-threads=1 && cargo clippy --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path | test("cargo-pmcp/src/pentest/attacks/"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 9 named functions have cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - No function in any of the 4 pentest files exceeds cog 50
    - Probe URI literal arrays unchanged (verifiable by `git diff` showing extracts but URI arrays untouched)
    - `cargo test -p cargo-pmcp -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 9
    - For every `#[allow]` added: `grep -B1 'allow(clippy::cognitive_complexity)' <file> | grep '// Why:'` returns lines
  </acceptance_criteria>
  <done>9 pentest hotspots resolved; probe behavior preserved; SUMMARY notes the manual `pentest --dry-run` checkpoint for the user.</done>
</task>

<!-- ===================== SUB-WAVE 2b: deployment + commands + main.rs ===================== -->

<task type="auto">
  <name>Task 2b-A: Refactor cargo-pmcp/src/deployment/* (10 functions across 3 files)</name>
  <files>cargo-pmcp/src/deployment/targets/cloudflare/init.rs, cargo-pmcp/src/deployment/targets/pmcp_run/deploy.rs, cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs</files>
  <read_first>
    - cargo-pmcp/src/deployment/targets/cloudflare/init.rs (6 hotspots: find_any_package @ 193 cog 65, try_find_pmcp_in_cargo_toml @ 381 cog 41, try_find_workspace_pmcp @ 436 cog 41, auto_detect_server_package @ 98 cog 35, find_core_package @ 159 cog 35, detect_pmcp_dependency @ 342 cog 33)
    - cargo-pmcp/src/deployment/targets/cloudflare/init.rs line 238 (`try_package_dir` — analog small-helper shape called by both find_core_package @ 178 and find_any_package @ 213)
    - cargo-pmcp/src/deployment/targets/pmcp_run/deploy.rs (deploy_to_pmcp_run @ 77 cog 65, extract_version_from_cargo @ 21 cog 27)
    - cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs (fetch_pmcp_config @ 142 cog 35, start_callback_server @ 582 cog 26)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 2b "deployment/" + "Closest Analog" with the worked example for `scan_for_package` shared helper)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 2b deployment table)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Refactor 10 deployment functions. The cloudflare/init.rs cluster shares a duplicated `for search_dir in search_dirs { for entry in fs::read_dir(...).flatten() { ... } }` walk that PATTERNS.md flags as a 3-bird kill (extract one helper → reduces cog of 2 functions + removes duplicate code).

    **cloudflare/init.rs — extract shared helper FIRST (one commit):**
    Per PATTERNS.md target shape, extract `scan_for_package(dirs: &[PathBuf], accept: impl Fn(&str) -> bool) -> Result<Option<(String, PathBuf)>>` covering the duplicated scan loop. Then refactor `find_core_package` and `find_any_package` as one-liners that call this helper with different predicates:
    ```rust
    fn find_core_package(root: &Path) -> Result<Option<...>> {
        scan_for_package(&core_search_dirs(root), |name| name.ends_with("-core"))
    }
    fn find_any_package(root: &Path) -> Result<Option<...>> {
        scan_for_package(&any_search_dirs(root), |_| true)
    }
    ```
    Commit subject: `refactor(75-02b): extract scan_for_package; refactor find_*_package in cloudflare/init.rs (cog 65/35→<25)`.

    **cloudflare/init.rs — remaining 4 fns (4 commits):**
    - `auto_detect_server_package` @ 98, cog 35 → ≤25. P1 extract per-strategy helpers (`detect_from_cargo_toml`, `detect_from_workspace_members`, `detect_from_filesystem`).
    - `detect_pmcp_dependency` @ 342, cog 33 → ≤25. P1.
    - `try_find_pmcp_in_cargo_toml` @ 381, cog 41 → ≤25. P1 extract per-table-section parsing (`scan_dependencies_table`, `scan_workspace_table`).
    - `try_find_workspace_pmcp` @ 436, cog 41 → ≤25. P1 same shape as the previous.
    Per-fn commit: `refactor(75-02b): <fn_name> in cloudflare/init.rs (cog NN→MM)`.

    **pmcp_run/deploy.rs (2 fns, 2 commits):**
    1. `deploy_to_pmcp_run` @ 77, cog 65 → ≤25 (refactor mandatory; P5 forbidden until ≤50). P1 extract per-stage helpers (`prepare_deployment_payload`, `upload_artifact`, `verify_deployment`, `report_status`).
    2. `extract_version_from_cargo` @ 21, cog 27 → ≤25. P1 borderline; one extract should do it.

    **pmcp_run/auth.rs (2 fns, 2 commits):**
    1. `fetch_pmcp_config` @ 142, cog 35 → ≤25. P1.
    2. `start_callback_server` @ 582, cog 26 → ≤25. Borderline P1 — extract request handler closure body.

    After EACH commit: `cargo test -p cargo-pmcp deployment:: -- --test-threads=1` + `cargo clippy --all-targets --all-features -- -D warnings`.

    For any function residual >25 after extraction, apply P5 with `// Why:` per W0 template (≤50 ceiling).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp -- --test-threads=1 && cargo clippy --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path | test("cargo-pmcp/src/deployment/"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 10 named functions have cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - `scan_for_package` shared helper exists in cloudflare/init.rs and is called by both find_core_package and find_any_package (verifiable by `grep -c 'scan_for_package' cargo-pmcp/src/deployment/targets/cloudflare/init.rs` ≥ 3)
    - No function in any of the 3 files exceeds cog 50
    - `cargo test -p cargo-pmcp -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 10
    - All new `#[allow]` sites have `// Why:` per template
  </acceptance_criteria>
  <done>10 deployment hotspots resolved; shared scan helper kills duplicate loop; deployment behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2b-B: Refactor 2 monster commands — check::execute (cog 105) + handle_oauth_action (cog 91)</name>
  <files>cargo-pmcp/src/commands/test/check.rs, cargo-pmcp/src/commands/deploy/mod.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/test/check.rs (full file — `execute` @ line 20, current cog 105; setup section lines 27-43 already a clear "stage" per PATTERNS.md)
    - cargo-pmcp/src/commands/deploy/mod.rs (full file context for `handle_oauth_action` @ line 796 cog 91; `resolve_oauth_config` already extracted at line 818 — that's the analog small-helper shape)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 2b — "For handle_oauth_action (cog 91): The analog is resolve_oauth_config..."; "For the commands/test/* execute family: ... Extract per-stage helpers")
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (these are the 2 highest-cog in cargo-pmcp; refactor mandatory per D-03)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02, D-03 — both functions are >50 so P5 alone is forbidden until refactor brings them ≤50)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md "Test Analogs" — both fns lack pre-existing tests; **add semantic regression tests BEFORE refactor**
  </read_first>
  <action>
    Two mandatory refactors. Both are >2× over D-03 ceiling 50, so P5 alone is forbidden — refactor MUST bring them under 50 (target ≤25 with P1).

    **PRE-REFACTOR for both: Add semantic regression tests (in-file `#[cfg(test)] mod tests`):**

    For `check.rs::execute`: capture stdout for at least one known-good and one known-bad target URL (mock or fixture). Use `assert_cmd` if available; otherwise use a minimal fixture HTTP server. Commit FIRST: `test(75-02b): add regression tests for check::execute pre-refactor`.

    For `deploy/mod.rs::handle_oauth_action`: add at least one unit test per `OAuthAction` variant (Enable, Disable, Status — at minimum). Use mock credentials. Commit FIRST: `test(75-02b): add regression tests for handle_oauth_action pre-refactor`.

    **REFACTOR 1: `commands/test/check.rs::execute` @ line 20, cog 105 → target ≤25.**

    Apply **P1** — extract per-stage helpers. Per PATTERNS.md, the setup section (lines 27-43) is already a clear stage that should become:
    - `print_check_header(url, transport, global_flags)`
    - `prepare_environment(url, transport) -> Result<CheckEnv>`
    - `run_diagnostic_stage(env: &CheckEnv) -> Vec<DiagnosticResult>`
    - `report_results(results: &[DiagnosticResult])`

    Each is a small focused fn. The orchestrator becomes a 4-step pipeline with early-return on errors.

    Run `cargo test -p cargo-pmcp commands::test::check::` (the new regression tests) after each extraction step. Commit subject: `refactor(75-02b): commands/test/check.rs::execute (cog 105→MM)`.

    If residual after extraction is 26-50, apply P5 with `// Why: cargo-pmcp test orchestrator coordinates 6+ diagnostic stages with shared environment; further extraction would split shared CheckEnv lifetime`. If residual >50, extract more — D-03 hard cap.

    **REFACTOR 2: `commands/deploy/mod.rs::handle_oauth_action` @ line 796, cog 91 → target ≤25.**

    Apply **P1** — per-OAuthAction-variant handlers. Per PATTERNS.md target shape: each `OAuthAction::Enable { ... } => { ... }` arm becomes a `handle_oauth_enable(action_fields, credentials) -> Result<()>` call. The outer match becomes a thin dispatcher:
    ```rust
    fn handle_oauth_action(action: OAuthAction, ctx: &Ctx) -> Result<()> {
        match action {
            OAuthAction::Enable { fields } => handle_oauth_enable(fields, ctx),
            OAuthAction::Disable { fields } => handle_oauth_disable(fields, ctx),
            OAuthAction::Status { fields } => handle_oauth_status(fields, ctx),
            // ... other variants ...
        }
    }
    ```

    `resolve_oauth_config` (already extracted at line 818) is the analog shape — small, focused. Each per-variant handler should match that shape.

    Run `cargo test -p cargo-pmcp commands::deploy::` (the new regression tests) after each extraction step. Commit subject: `refactor(75-02b): commands/deploy/mod.rs::handle_oauth_action (cog 91→MM)`.

    Run `cargo clippy --all-targets --all-features -- -D warnings` after both refactors before returning.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp -- --test-threads=1 && cargo clippy --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select((.path == "cargo-pmcp/src/commands/test/check.rs" and .name == "execute") or (.path == "cargo-pmcp/src/commands/deploy/mod.rs" and .name == "handle_oauth_action"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - `commands/test/check.rs::execute` has cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - `commands/deploy/mod.rs::handle_oauth_action` has cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - Neither function exceeds cog 50 (D-03 hard cap)
    - Pre-refactor regression tests exist in both files (`#[cfg(test)] mod tests` blocks with at least 2 tests per file)
    - `cargo test -p cargo-pmcp -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 2
  </acceptance_criteria>
  <done>2 monster commands resolved; both ≤50; per-action / per-stage helpers extracted; regression tests committed.</done>
</task>

<task type="auto">
  <name>Task 2b-C: Refactor remaining cargo-pmcp/ commands + main.rs + loadtest + landing (20 functions)</name>
  <files>cargo-pmcp/src/main.rs, cargo-pmcp/src/commands/test/run.rs, cargo-pmcp/src/commands/test/upload.rs, cargo-pmcp/src/commands/test/apps.rs, cargo-pmcp/src/commands/test/list.rs, cargo-pmcp/src/commands/deploy/mod.rs, cargo-pmcp/src/commands/doctor.rs, cargo-pmcp/src/commands/add.rs, cargo-pmcp/src/commands/validate.rs, cargo-pmcp/src/commands/dev.rs, cargo-pmcp/src/commands/pentest.rs, cargo-pmcp/src/commands/preview.rs, cargo-pmcp/src/commands/landing/init.rs, cargo-pmcp/src/commands/landing/deploy.rs, cargo-pmcp/src/commands/loadtest/run.rs, cargo-pmcp/src/loadtest/vu.rs, cargo-pmcp/src/loadtest/summary.rs, cargo-pmcp/src/landing/template.rs</files>
  <read_first>
    - cargo-pmcp/src/main.rs lines 407-490 (`execute_command` @ 407 cog 48 — many arms already thin, the "fat" arms `Commands::Add` 412-427, `Commands::Landing` 452-456, `Commands::Preview` 482+ need extraction per PATTERNS.md "For execute_command")
    - cargo-pmcp/src/commands/deploy/mod.rs (`detect_server_name` @ 8 cog 64 — separate from Task 2b-B's handle_oauth_action; both in same file but different fns)
    - cargo-pmcp/src/commands/doctor.rs (`execute` @ 15 cog 60)
    - cargo-pmcp/src/commands/add.rs (`server` @ 12 cog 56)
    - cargo-pmcp/src/commands/validate.rs (`run_validation` @ 71 cog 66, `parse_test_output` @ 286 cog 30)
    - cargo-pmcp/src/commands/test/{run,upload,apps,list}.rs (per-file `execute` fns, cog 46/44/43/36)
    - cargo-pmcp/src/commands/dev.rs (`resolve_server_binary` @ 22 cog 34, `execute` @ 113 cog 33)
    - cargo-pmcp/src/commands/pentest.rs (`execute_pentest` @ 80 cog 38)
    - cargo-pmcp/src/commands/preview.rs (`execute` @ 9 cog 27)
    - cargo-pmcp/src/commands/landing/init.rs (`detect_server_name` @ 144 cog 30)
    - cargo-pmcp/src/commands/landing/deploy.rs (`deploy_landing_page` @ 11 cog 27)
    - cargo-pmcp/src/commands/loadtest/run.rs (`execute_run` @ 19 cog 26)
    - cargo-pmcp/src/loadtest/vu.rs (`vu_loop_inner` @ 243 cog 37)
    - cargo-pmcp/src/loadtest/summary.rs (`render_summary` @ 56 cog 26)
    - cargo-pmcp/src/landing/template.rs (`find_local_template` @ 90 cog 26)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 2b commands/ table; "For execute_command in main.rs"; "For commands/test/*::execute family")
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 2b cargo-pmcp/ extension table)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Refactor 20 functions across 18 files. Apply **P1** uniformly. Per-file commits in this order (simplest to complex), each: `refactor(75-02b): <fn_name> in <file> (cog NN→MM)`:

    **Group A — main.rs orchestrator and CLI entry points (low risk, structural only):**

    1. `cargo-pmcp/src/main.rs::execute_command` @ 407, cog 48 → ≤25. Per PATTERNS.md target shape: extract the 3 "fat" arms into helpers — `execute_add(args, global_flags)`, `execute_landing(args, global_flags)`, `execute_preview(args, global_flags)`. Match the existing thin-arm pattern that other arms (`Test`, `Auth`, `Schema`, `Validate`, `Secret`, `Loadtest`, `App`) already use (one-line `command.execute(global_flags)?`). The refactor is purely structural — moves bodies, doesn't modify them.

    2. `cargo-pmcp/src/commands/preview.rs::execute` @ 9, cog 27 → ≤25. P1 borderline.

    3. `cargo-pmcp/src/commands/landing/init.rs::detect_server_name` @ 144, cog 30 → ≤25. P1.

    4. `cargo-pmcp/src/commands/landing/deploy.rs::deploy_landing_page` @ 11, cog 27 → ≤25. P1.

    5. `cargo-pmcp/src/commands/loadtest/run.rs::execute_run` @ 19, cog 26 → ≤25. P1 borderline.

    **Group B — commands/test/*::execute family (apply same per-stage extraction shape per PATTERNS.md):**

    6. `cargo-pmcp/src/commands/test/run.rs::execute` @ 12, cog 46 → ≤25. P1 — extract `prepare_environment`, `run_stage`, `report_results` per PATTERNS.md "For the commands/test/*::execute family".
    7. `cargo-pmcp/src/commands/test/upload.rs::execute` @ 11, cog 44 → ≤25. Same shape.
    8. `cargo-pmcp/src/commands/test/apps.rs::execute` @ 16, cog 43 → ≤25. Same shape.
    9. `cargo-pmcp/src/commands/test/list.rs::execute` @ 10, cog 36 → ≤25. P1.

    **Group C — heavier commands:**

    10. `cargo-pmcp/src/commands/deploy/mod.rs::detect_server_name` @ 8, cog 64 → ≤25. P1 extract per-detection-strategy helpers.
    11. `cargo-pmcp/src/commands/doctor.rs::execute` @ 15, cog 60 → ≤25. P1 extract per-check-class helpers.
    12. `cargo-pmcp/src/commands/add.rs::server` @ 12, cog 56 → ≤25. P1.
    13. `cargo-pmcp/src/commands/validate.rs::run_validation` @ 71, cog 66 → ≤25. P1 extract per-validation-stage helpers.
    14. `cargo-pmcp/src/commands/validate.rs::parse_test_output` @ 286, cog 30 → ≤25. P1.
    15. `cargo-pmcp/src/commands/dev.rs::resolve_server_binary` @ 22, cog 34 → ≤25. P1.
    16. `cargo-pmcp/src/commands/dev.rs::execute` @ 113, cog 33 → ≤25. P1.
    17. `cargo-pmcp/src/commands/pentest.rs::execute_pentest` @ 80, cog 38 → ≤25. P1.

    **Group D — loadtest + landing utilities:**

    18. `cargo-pmcp/src/loadtest/vu.rs::vu_loop_inner` @ 243, cog 37 → ≤25. P1 extract per-stage helpers (request, record, sleep). PRESERVE concurrency semantics — refactor must not change tokio task spawning or backpressure handling.
    19. `cargo-pmcp/src/loadtest/summary.rs::render_summary` @ 56, cog 26 → ≤25. P1 borderline.
    20. `cargo-pmcp/src/landing/template.rs::find_local_template` @ 90, cog 26 → ≤25. P1 borderline.

    After EACH commit: `cargo test -p cargo-pmcp -- --test-threads=1` + `cargo clippy --all-targets --all-features -- -D warnings`.

    Any function with residual >25 after P1 extraction: apply P5 with `// Why:` per W0 template (≤50 ceiling). PATTERNS.md predicts none in this group should need P5, but the borderline ones (functions already at 26-30) might.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp -- --test-threads=1 && cargo clippy --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path | test("cargo-pmcp/src/(main\\.rs|commands/|loadtest/|landing/)"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 20 named functions have cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - No function in any of the 18 modified files exceeds cog 50
    - `cargo test -p cargo-pmcp -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - `cargo run -p cargo-pmcp -- --help` succeeds (CLI plumbing intact after main.rs refactor)
    - PMAT total complexity violation count strictly decreases by at least 20
    - All new `#[allow]` sites have `// Why:` per template
  </acceptance_criteria>
  <done>20 cargo-pmcp commands + main.rs + loadtest + landing hotspots resolved; CLI behavior unchanged; per-arm and per-stage helper extraction is the dominant pattern.</done>
</task>

</tasks>

<verification>
After all 4 Wave 2 tasks complete:

1. **PMAT complexity rollup**: `pmat quality-gate --fail-on-violation --checks complexity --format json | jq '.violations | length'` strictly less than after Wave 1 by at least 41.
2. **Per-directory zero-violations check** for cargo-pmcp/: `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | startswith("cargo-pmcp/"))] | length'` = 0.
3. **D-02 conformance**: `for f in <26 files>; do test "$(grep -c 'allow(clippy::cognitive_complexity)' $f)" -eq "$(grep -B1 'allow(clippy::cognitive_complexity)' $f | grep -c '// Why:')"; done` (no bare allows added).
4. **D-03 conformance**: `pmat analyze complexity --format json | jq '[.violations[] | select(.path | startswith("cargo-pmcp/")) | select(.complexity > 50)] | length'` = 0.
5. **Workspace test green**: `cargo test --workspace --all-features -- --test-threads=1` exits 0.
6. **CLI smoke**: `cargo run -p cargo-pmcp -- --help` succeeds.
7. **Manual checkpoint** (per VALIDATION.md): SUMMARY notes that the user should manually run `cargo run -p cargo-pmcp -- pentest --dry-run --target localhost:1234` before final phase sign-off and diff stdout against the pre-refactor baseline.
8. **`make quality-gate` exits 0**.

Wave-merge commit message format: `refactor(75-02): wave 2 complete — pmat-complexity: NN (was MM)` where NN < MM by at least 41.
</verification>

<success_criteria>
- 9 pentest hotspots refactored (Task 2a)
- 10 deployment hotspots refactored with shared scan_for_package helper (Task 2b-A)
- 2 monster commands (cog 105 + 91) refactored to ≤50 with pre-refactor regression tests (Task 2b-B)
- 20 remaining cargo-pmcp/ hotspots refactored (Task 2b-C)
- PMAT total complexity violation count drops by at least 41
- All new `#[allow]` sites in cargo-pmcp/ conform to D-02 (`// Why:` template)
- No function in cargo-pmcp/ exceeds D-03 ceiling 50
- Wave-merge commit records `pmat-complexity: NN (was MM)` for git-log auditability
</success_criteria>

<output>
After completion, create `.planning/phases/75-fix-pmat-issues/75-02-SUMMARY.md` per the GSD summary template, including:
- Per-function before/after cog scores (41 entries)
- List of new P5 sites added (with Why text)
- Confirmation that `pentest --dry-run` manual smoke is logged for final UAT
- Wave-rollup PMAT count delta
</output>
