---
phase: 75-fix-pmat-issues
plan: 01
type: execute
wave: 1
depends_on: ["75-00"]
files_modified:
  - src/server/streamable_http_server.rs
  - src/server/path_validation.rs
  - src/server/schema_utils.rs
  - src/server/workflow/task_prompt_handler.rs
  - src/utils/json_simd.rs
  - src/server/elicitation.rs
  - src/server/notification_debouncer.rs
  - src/server/resource_watcher.rs
  - src/server/transport/websocket_enhanced.rs
  - src/server/mod.rs
  - src/shared/sse_optimized.rs
  - src/shared/connection_pool.rs
  - src/shared/logging.rs
  - src/client/mod.rs
  - src/client/http_logging_middleware.rs
  - pmcp-macros/src/mcp_server.rs
  - pmcp-macros/src/mcp_resource.rs
  - pmcp-macros/src/mcp_prompt.rs
  - pmcp-macros/src/mcp_tool.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every cognitive-complexity violation in src/ and pmcp-macros/src/ is either ≤25 (refactored) or ≤50 with a `// Why:` justified `#[allow]` (D-03 ceiling)"
    - "All 13 pre-existing bare `#[allow(clippy::cognitive_complexity)]` sites in src/ have a `// Why:` line directly above the attribute"
    - "pmcp-macros expansion snapshots from Wave 0 still pass byte-identical (Pitfall 4)"
    - "streamable_http_server transport behavior unchanged per `tests/streamable_http_*` (existing test files stay green)"
    - "Wave 1 commit message records `pmat-complexity: NN (was MM)` with NN < MM"
  artifacts:
    - path: "src/server/streamable_http_server.rs"
      provides: "6 hotspot functions refactored or `#[allow]`-justified per D-02/D-03"
      contains: "fn handle_post_with_middleware"
    - path: "pmcp-macros/src/mcp_server.rs"
      provides: "4 expand/collect helpers refactored, P5 fallback only with `// Why:`"
    - path: "pmcp-macros/src/mcp_resource.rs"
      provides: "expand_mcp_resource refactored or `#[allow]`-justified ≤50"
  key_links:
    - from: "pmcp-macros/tests/expansion_snapshots.rs (Wave 0)"
      to: "pmcp-macros/src/{mcp_server,mcp_tool,mcp_resource,mcp_prompt}.rs"
      via: "macro expansion comparison"
      pattern: "expand_mcp_(server|tool|resource|prompt)"
    - from: "tests/streamable_http_*"
      to: "src/server/streamable_http_server.rs"
      via: "integration test invocations"
      pattern: "handle_(post|get_sse|post_with_middleware)"
---

<objective>
Refactor (or `#[allow]`-justify) all in-scope `src/` and `pmcp-macros/src/` cognitive-complexity violations from RESEARCH.md's Wave 1a + 1b inventory. This is the highest-impact wave for badge progress: 13 hotspot functions in src/ + 7 in pmcp-macros/ + 13 retro-justifications of pre-existing bare allows = ~33 violation reductions.

**Purpose:** Drop PMAT complexity violation count by ~33 (the largest single contribution to badge flip), while leaving downstream crates compilable and the existing transport+macro test suites green.

**Output:** 19 files modified in place. Sub-waves 1a (src/) and 1b (pmcp-macros/) execute in this order — pmcp-macros refactors trigger workspace rebuilds that exercise downstream consumers (every crate that uses `#[mcp_tool]`, `#[mcp_server]`, etc.), so the snapshot baseline from Wave 0 is the safety net.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-fix-pmat-issues/75-CONTEXT.md
@.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
@.planning/phases/75-fix-pmat-issues/75-PATTERNS.md
@.planning/phases/75-fix-pmat-issues/75-VALIDATION.md
@.planning/phases/75-fix-pmat-issues/75-00-PLAN.md
@.planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md
@CLAUDE.md
@src/server/streamable_http_server.rs
@src/server/path_validation.rs
@src/server/schema_utils.rs
@src/utils/json_simd.rs
@pmcp-macros/src/mcp_server.rs

<why_template_reminder>
The single project-wide `// Why:` justification template is pinned in 75-00-PLAN.md `<project_wide_why_template>`. EVERY `#[allow(clippy::cognitive_complexity)]` site touched in this wave (existing or new) MUST conform. Acceptance check is `grep -B1 'allow(clippy::cognitive_complexity)' <file> | grep -q '// Why:'`.
</why_template_reminder>

<refactor_techniques>
From RESEARCH.md Architecture Patterns / PATTERNS.md catalog (these are the ONLY 6 sanctioned techniques — no inventing new ones):
- **P1**: Extract-method on long match arms
- **P2**: Replace nested validation with early-return chain
- **P3**: Extract validation closures into helper functions
- **P4**: State enum + dispatch table for protocol handlers
- **P5**: `#[allow(clippy::cognitive_complexity)]` + `// Why:` (bounded by D-03 ≤50 hard cap)
- **P6**: Decompose AST evaluation by syntactic category (Wave 3 only)
</refactor_techniques>

<within_wave_parallelism>
Wave 1 has TWO sub-waves with a hard ordering constraint: 1a (src/) MUST complete before 1b (pmcp-macros/) so that workspace builds during 1b's refactors are not blamed on stale src/ changes.

WITHIN sub-wave 1a, tasks 1a-A through 1a-D touch INDEPENDENT files (no overlap in `files_modified`) and can run in parallel.

WITHIN sub-wave 1b, tasks 1b-A through 1b-D touch INDEPENDENT pmcp-macros files (mcp_server.rs has 4 hotspots so all four go in one task; the other three each get one task).

Sequential ordering within each task: read → snapshot baseline check → refactor → verify cog drop → verify tests → verify clippy → commit.
</within_wave_parallelism>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → streamable_http_server | Untrusted HTTP/SSE input crosses here; header parsing, origin validation, session lifecycle are security-sensitive |
| Macro consumer → expanded code | pmcp-macros generates production code in every downstream crate; refactor must not change generated output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01-01 | Tampering | streamable_http_server header validation refactor could change accepted-headers set | mitigate | Existing `tests/streamable_http_unit_tests.rs` covers header validation; `validate_headers` refactor (P3) extracts per-header closures without changing the set; full test suite green per acceptance criteria |
| T-75-01-02 | Spoofing | Origin/session validation refactor could weaken DNS-rebinding protection | mitigate | `validate_protocol_version` (P2) and session-id checks are pure validation extracts — same checks in same order; integration tests cover |
| T-75-01-03 | Information Disclosure | path_validation.rs refactor could weaken base-dir confinement | mitigate | `validate_path` (P1+P2) extracts `enforce_base_dir_confinement` as a separate fn — checks remain identical; existing in-file tests + property tests cover |
| T-75-01-04 | Tampering | pmcp-macros expand fn refactor could change generated code (e.g. wrong field name in deserialization) | mitigate | Wave 0 insta snapshots are the regression contract; `cargo test --workspace --all-features` exercises every downstream macro consumer |
| T-75-01-05 | Elevation of Privilege | json_simd.rs SIMD-path refactor could mis-parse adversarial input | mitigate | Existing `tests/simd_parsing_tests.rs` + in-file `mod tests` cover; refactor is method-extraction only |

**Refactor scope note:** Wave 1 is internal-control-flow only. No new ingress, no parser changes, no auth flow changes. Behavior preserved by existing transport integration tests + Wave 0 snapshot baselines. **Macro refactor in Wave 1b is the highest-risk surface** — a refactor that changes generated code could alter security-sensitive behavior in downstream crates (e.g. how arguments are deserialized). Insta snapshots from Wave 0 detect any change including invisible whitespace.
</threat_model>

<tasks>

<!-- ===================== SUB-WAVE 1a: src/ ===================== -->

<task type="auto">
  <name>Task 1a-A: Refactor streamable_http_server.rs (6 functions, P3+P4 dispatch)</name>
  <files>src/server/streamable_http_server.rs</files>
  <read_first>
    - src/server/streamable_http_server.rs (the file being modified — full read; current bare `#[allow]` at line 1004)
    - src/server/streamable_http_server.rs lines 457-563 (the analog shape — `validate_non_init_session` and 3 sibling small helpers)
    - tests/streamable_http_server_tests.rs (existing integration coverage — must stay green)
    - tests/streamable_http_unit_tests.rs (existing unit coverage — must stay green)
    - tests/streamable_http_properties.rs (existing property coverage — must stay green)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 1a hotspot table; Patterns 3, 4, 1, 2; Pitfall 1 PMAT version)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 1a "Closest Analog" + "Concrete extractions for handle_post_fast_path")
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02 // Why: rule, D-03 ≤50 ceiling)
  </read_first>
  <action>
    Refactor 6 hotspot functions in `src/server/streamable_http_server.rs`. Per-function targets:

    1. **`handle_post_with_middleware` at line 1005, current cog 59 → target ≤25.** Apply **P4** (state enum + dispatch table per protocol message type) following the in-file analog at lines 457-563 (`validate_non_init_session`, `process_init_session`). Remove the existing bare `#[allow(clippy::cognitive_complexity)]` at line 1004 IF the refactor brings cog ≤25; if residual is 26-50 keep the allow but add `// Why: <one-line reason>` per the W0 template. **Refactor is mandatory: cog 59 > D-03 ceiling 50.** Acceptance: `pmat analyze complexity --format json | jq '.violations[] | select(.path == "src/server/streamable_http_server.rs" and .name == "handle_post_with_middleware") | .complexity'` returns ≤25 (or ≤50 with Why).

    2. **`handle_post_fast_path` at line 841, current cog 48 → target ≤25.** Apply **P4** + extract pre/post hooks following PATTERNS.md Wave 1a "Concrete extractions" list:
       - `read_body_with_limit(body, max_bytes) -> Result<String, Response>` (lines 848-859)
       - `extract_session_and_protocol_headers(headers) -> (Option<String>, Option<String>)` (lines 879-889)
       - `is_initialize_request(message: &TransportMessage) -> bool` (lines 891-896)
       - `compute_outbound_protocol_version(state, response_session_id, is_init, negotiated) -> String` (lines 968-987)
       - `attach_response_headers(response: &mut Response, session_id, protocol_version)` (lines 962-991)
       After these extractions `handle_post_fast_path` becomes a sequential pipeline.

    3. **`validate_headers` at line 391, current cog 40 → target ≤25.** Apply **P3** — extract per-header validation closures. Each header check (Accept, Content-Type, Origin, Mcp-Session-Id, etc.) becomes a small `fn validate_<header_name>(headers: &HeaderMap) -> Result<(), Response>` and `validate_headers` becomes the chain.

    4. **`handle_get_sse` at line 1262, current cog 35 → target ≤25.** Apply **P4** dispatch shape. Extract per-mode handlers (initial connection vs reconnection vs error). Analog: same lines 457-534 helpers.

    5. **`validate_protocol_version` at line 674, current cog 34 → target ≤25.** Apply **P2** (early-return chain). Each invariant check that currently nests becomes `if !invariant { return Err(...) }`.

    6. **`build_response` at line 566, current cog 30 → target ≤25.** Apply **P1** — extract per-status-code arms into named helpers (`build_400_response`, `build_500_response`, etc.).

    Constraints:
    - Each refactor is its own atomic commit with subject `refactor(75-01a): <fn_name> in streamable_http_server.rs (cog NN→MM)`.
    - Run `cargo test --test streamable_http_server_tests --test streamable_http_unit_tests --test streamable_http_properties` after EACH refactor before committing the next.
    - Do NOT change public function signatures or change observed HTTP behavior. The refactor is structural only.
    - If any refactor cannot reach ≤25, document residual cog + apply P5 with the W0 `// Why:` template, ensuring residual ≤50 (D-03).
    - Existing bare `#[allow]` at line 1004: removed if refactor drops cog ≤25; otherwise replaced with `// Why: <reason> #[allow(clippy::cognitive_complexity)]` per template.
  </action>
  <verify>
    <automated>cargo test --test streamable_http_server_tests --test streamable_http_unit_tests --test streamable_http_properties -- --test-threads=1 && cargo clippy --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path == "src/server/streamable_http_server.rs")] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 6 named functions have cog ≤25 in `pmat analyze complexity --format json`, OR ≤50 with a `// Why:` annotated `#[allow]`
    - Zero functions in this file exceed cog 50
    - For every `#[allow(clippy::cognitive_complexity)]` in this file: `grep -B1 'allow(clippy::cognitive_complexity)' src/server/streamable_http_server.rs | grep -A0 '// Why:'` returns lines (the previous bare allow at line 1004 is fixed too)
    - `cargo test --test streamable_http_server_tests --test streamable_http_unit_tests --test streamable_http_properties -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 6 (one per refactored fn, more if refactor side-effects clean up sibling fns)
    - No public function signatures changed
  </acceptance_criteria>
  <done>streamable_http_server.rs no longer contributes >25-cog functions to the gate; transport behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 1a-B: Refactor sibling src/server/ + src/utils/ hotspots (7 functions)</name>
  <files>src/server/path_validation.rs, src/server/schema_utils.rs, src/server/workflow/task_prompt_handler.rs, src/utils/json_simd.rs</files>
  <read_first>
    - src/server/path_validation.rs lines 60-180 (validate_path at 65, current cog 103 — the worst in src/)
    - src/server/schema_utils.rs lines 23-32 (Default for NormalizerConfig — analog clean shape) and lines 252-270 (simple_schema — analog)
    - src/server/schema_utils.rs lines 61-300 (the 3 hotspots: normalize_schema_with_config, inline_refs_with_context, inline_refs)
    - src/server/workflow/task_prompt_handler.rs lines 510-600 (classify_resolution_failure at 523)
    - src/utils/json_simd.rs lines 11-200 (parse_json_fast at 11 cog 59, pretty_print_fast at 113 cog 36; the fallback variant at 73-75 is the analog shape)
    - tests/simd_parsing_tests.rs (existing coverage for json_simd.rs)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 1a "Closest Analog" sections for each file)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 1a expansion table — these 7 functions)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02 // Why:, D-03 ≤50)
  </read_first>
  <action>
    Refactor 7 hotspot functions across 4 files. Each refactor is its own atomic commit.

    1. **`src/server/path_validation.rs::validate_path` at line 65, cog 103 → target ≤25.** Cog 103 is 2× over D-03 ceiling 50 — refactor mandatory. Apply **P1+P2 combined**: extract `canonicalize_with_fallback(path_buf: &Path) -> crate::Result<PathBuf>` (lines 113-153), `enforce_base_dir_confinement(canonical: &Path, base_dir: &Path) -> crate::Result<()>`, `enforce_pattern_blocklist(canonical: &Path, blocklist: &[Pattern]) -> crate::Result<()>`. Use the constructor-style helpers on `PathValidationConfig` at lines 26-62 as the target shape (single-purpose, returns Self/Result, no nesting). After extraction `validate_path` becomes a 4-step early-return chain.

    2. **`src/server/schema_utils.rs::normalize_schema_with_config` at line 61, cog 56 → target ≤25.** Apply **P1**: extract `extract_definitions_block(schema: &mut Value) -> Option<Value>`, `inline_root_ref(schema: &mut Value, defs: &Value) -> Result<()>`, `strip_metadata(schema: &mut Value)`. Each is a focused mutation; the orchestrator becomes 3 calls.

    3. **`src/server/schema_utils.rs::inline_refs_with_context` at line 149, cog 55 → target ≤25.** Apply **P1**: extract `try_inline_single_ref(map: &mut Map<String, Value>, context: &InlineContext) -> bool`, recurse through a separate walker fn `walk_and_inline(value: &mut Value, context: &InlineContext)`.

    4. **`src/server/schema_utils.rs::inline_refs` at line 210, cog 41 → target ≤25.** Same shape as #3. **Read line 209 first**: it has `#[allow(dead_code)]`. Confirm whether the function is actually unused — if dead, DELETE the function entirely (zero residual cog) and remove the allow. If used (grep for callers in workspace), apply same P1 pattern as #3.

    5. **`src/server/workflow/task_prompt_handler.rs::classify_resolution_failure` at line 523, cog 43 → target ≤25.** Apply **P1** — per-failure-class arms. Each variant of the failure enum becomes a small `classify_<variant>(...) -> ResolutionFailureClass` helper. The outer match is thin.

    6. **`src/utils/json_simd.rs::parse_json_fast` at line 11, cog 59 → target ≤25.** Apply **P1**: extract `strip_whitespace_simd_aware(input, ws_positions) -> Vec<u8>`. The fallback path at lines 73-75 is the analog shape (small, focused). Take care: this is a SIMD hot path — refactor must NOT change byte-for-byte equivalence with adversarial input. Run `cargo test --test simd_parsing_tests` after refactor.

    7. **`src/utils/json_simd.rs::pretty_print_fast` at line 113, cog 36 → target ≤25.** Apply **P1**: extract `format_byte_with_indent_state(byte, ctx)` and the SIMD path into `pretty_print_simd(value)`.

    Per-refactor commit subject: `refactor(75-01a): <fn_name> in <file> (cog NN→MM)`.

    For any function that cannot reach ≤25 after extraction, apply P5 with `// Why: <reason>` per the W0 template, ensuring residual ≤50.

    Run `cargo test -p pmcp --lib -- --test-threads=1` AND `cargo clippy -p pmcp --all-targets --all-features -- -D warnings` after EACH refactor before committing the next.

    **POST-REVIEW REVISION (2026-04-23 — Codex Concern #8):** Per-task verification is now scoped to the affected package (here: pmcp main crate). Full workspace tests run only at WAVE-MERGE boundary (the bottom `<verification>` block of this plan), not after every per-function commit. This drops per-task verification cost from ~3 min to ~30s while preserving regression detection for the changed code path.
  </action>
  <verify>
    <automated>cargo test -p pmcp --lib -- --test-threads=1 && cargo test --test simd_parsing_tests -- --test-threads=1 && cargo clippy -p pmcp --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path | test("src/server/path_validation\\.rs|src/server/schema_utils\\.rs|src/server/workflow/task_prompt_handler\\.rs|src/utils/json_simd\\.rs"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 7 functions named in the action have cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - No function in any of the 4 modified files exceeds cog 50
    - Every `#[allow(clippy::cognitive_complexity)]` added in this task has a `// Why:` line directly above
    - `cargo test -p pmcp --lib -- --test-threads=1` exits 0 (workspace test runs at wave-merge, not per-task)
    - `cargo test --test simd_parsing_tests` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 7 (or by 8 if `inline_refs` was deleted as dead code)
    - SIMD parsing produces byte-identical output for fixture inputs (covered by existing simd_parsing_tests)
  </acceptance_criteria>
  <done>4 sibling src/ files no longer contribute >25-cog functions; semantic behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 1a-C: Retro-justify 13 pre-existing bare `#[allow(clippy::cognitive_complexity)]` sites in src/</name>
  <files>src/server/elicitation.rs, src/server/notification_debouncer.rs, src/server/resource_watcher.rs, src/server/transport/websocket_enhanced.rs, src/server/mod.rs, src/shared/sse_optimized.rs, src/shared/connection_pool.rs, src/shared/logging.rs, src/client/mod.rs, src/client/http_logging_middleware.rs</files>
  <read_first>
    - src/server/elicitation.rs (line 66 `elicit_input`)
    - src/server/notification_debouncer.rs (line 240 `flush_pending`)
    - src/server/resource_watcher.rs (line 111 `process_events`)
    - src/server/transport/websocket_enhanced.rs (lines 232 `handle_client_send`, 271 `handle_connection`)
    - src/server/mod.rs (line 1061 `handle_call_tool`)
    - src/shared/sse_optimized.rs (line 225 `connect_sse` — RESEARCH.md provides worked example for this exact site)
    - src/shared/connection_pool.rs (line 159 `start`)
    - src/shared/logging.rs (line 433 `log`)
    - src/client/mod.rs (line 1912 `send_request`)
    - src/client/http_logging_middleware.rs (lines 332 `log_request`, 401 `log_response`)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02 mandates `// Why:` on every allow; D-03 ≤50 ceiling)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Pitfall 2 — list of existing bare allows)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 1a "Existing Bare `#[allow]` Sites" table; the worked example for `connect_sse`)
    - .planning/phases/75-fix-pmat-issues/75-00-PLAN.md `<project_wide_why_template>` block
  </read_first>
  <action>
    For EACH of the 13 sites listed in PATTERNS.md Wave 1a "Existing Bare `#[allow]` Sites" table, perform a 3-step measure-decide-justify edit:

    **Step A (measure)**: Run `pmat analyze complexity --top-files 0 --format json | jq '.violations[] | select(.path == "<file>" and .line == <line>) | {name, complexity}'` for each site. Record the actual cognitive complexity score (RESEARCH.md only listed estimated "likely ≤50" without measurement).

    **Step B (decide per D-03)**:
    - If measured cog ≤50 → KEEP the `#[allow]` and add a `// Why:` line directly above it per the W0 template.
    - If measured cog >50 → REMOVE the `#[allow]` and refactor the function using the most fitting technique (P1/P2/P3/P4) until cog ≤25 or ≤50 with new `// Why:`.

    **Step C (annotate)**: For sites that keep the allow, add the `// Why:` immediately above the `#[allow]` line. Use the worked example from PATTERNS.md / RESEARCH.md as the format. Each rationale should answer: (1) what makes it irreducible, (2) what would be lost by extracting helpers.

    Per-site commit subject: `refactor(75-01a): retro-justify cognitive_complexity allow in <file>:<line>`.

    Specific worked examples (use these or similar — adapt to actual function semantics after reading):

    `src/shared/sse_optimized.rs:225` (per RESEARCH.md):
    ```rust
    /// Connect to SSE endpoint
    // Why: SSE reconnection state — header negotiation, retry-after parsing,
    // last-event-id replay, and 3 error-class branches share local state;
    // extracting helpers would require an awkward shared mutable struct.
    #[allow(clippy::cognitive_complexity)]
    async fn connect_sse(...) { ... }
    ```

    `src/server/transport/websocket_enhanced.rs:232,271`:
    ```rust
    /// Handle a client send loop iteration.
    // Why: WebSocket frame routing across 5+ MessageType variants with
    // shared backpressure tracking; per-variant extraction would duplicate
    // the shared mutable state machine.
    #[allow(clippy::cognitive_complexity)]
    fn handle_client_send(...) { ... }
    ```

    `src/server/mod.rs:1061` (`handle_call_tool`):
    ```rust
    /// Handle a tool-call request.
    // Why: Protocol dispatch over 6+ tool-result variants with arg validation,
    // permissions, telemetry, and structured error reporting branches that
    // share request-scope context; extraction yields helpers with too many params.
    #[allow(clippy::cognitive_complexity)]
    fn handle_call_tool(...) { ... }
    ```

    For sites whose measured cog is BELOW 25 (i.e. the allow is no longer needed because the underlying function has been simplified by other commits since the allow was added): REMOVE the `#[allow]` entirely. Verify clippy still passes.

    Run `cargo test -p pmcp --lib -- --test-threads=1` AND `cargo clippy -p pmcp --all-targets --all-features -- -D warnings` after each commit before the next.

    **Order of operations**: Process sites in the order they appear in PATTERNS.md table to keep diffs reviewable.
  </action>
  <verify>
    <automated>for f in src/server/elicitation.rs src/server/notification_debouncer.rs src/server/resource_watcher.rs src/server/transport/websocket_enhanced.rs src/server/mod.rs src/shared/sse_optimized.rs src/shared/connection_pool.rs src/shared/logging.rs src/client/mod.rs src/client/http_logging_middleware.rs; do count_allow=$(grep -c 'allow(clippy::cognitive_complexity)' "$f" 2>/dev/null || echo 0); count_why=$(grep -B1 'allow(clippy::cognitive_complexity)' "$f" 2>/dev/null | grep -c '// Why:'); test "$count_allow" -eq "$count_why" || { echo "MISMATCH in $f: $count_allow allows, $count_why Whys"; exit 1; }; done && cargo test -p pmcp --lib -- --test-threads=1 && cargo clippy -p pmcp --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 10 listed files, every `#[allow(clippy::cognitive_complexity)]` has a `// Why:` line immediately preceding it (verified by the per-file `grep -B1 ... | grep -c '// Why:'` count match)
    - Zero functions remain with cog >50 in any of the 10 listed files
    - `cargo test --workspace --lib -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count is unchanged or decreases (retro-justification doesn't remove violations from the count, but removing-allows-on-simplified-functions might add them — refactor those down per D-03)
  </acceptance_criteria>
  <done>All 13 historical bare allows in src/ are either justified per D-02 or removed because the underlying function no longer needs suppression.</done>
</task>

<!-- ===================== SUB-WAVE 1b: pmcp-macros/ ===================== -->

<task type="auto">
  <name>Task 1b-A: Refactor pmcp-macros/src/mcp_server.rs (4 hotspot functions)</name>
  <files>pmcp-macros/src/mcp_server.rs</files>
  <read_first>
    - pmcp-macros/src/mcp_server.rs (full file — 4 hotspots: collect_resource_methods @ 743 cog 80, collect_tool_methods @ 480 cog 44, collect_prompt_methods @ 625 cog 42, expand_mcp_server @ 111 cog 36)
    - pmcp-macros/src/mcp_server.rs lines 577-599 (`parse_mcp_tool_attr` — the textbook analog per PATTERNS.md Wave 1b)
    - pmcp-macros/tests/expansion_snapshots.rs (Wave 0 baseline — MUST stay byte-identical after refactor)
    - pmcp-macros/tests/mcp_server_tests.rs (existing semantic tests)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 1b table; Pattern 5 escape hatch; Pitfall 4 downstream-break risk)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 1b "Closest Analog" + target shape sketch)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02 // Why:, D-03 ≤50)
  </read_first>
  <action>
    Refactor 4 hotspot functions in `pmcp-macros/src/mcp_server.rs`. Each is its own atomic commit. **Wave 0 snapshot must stay byte-identical after every commit** — run `cargo test -p pmcp-macros --test expansion_snapshots` after every refactor.

    1. **`expand_mcp_server` at line 111, cog 36 → target ≤25.** Apply **P1** — extract per-section code-gen helpers. PATTERNS.md identifies these natural sections (lines 140-220): `generate_handler_struct(method_info, server_type, ...)`, `generate_args_deserialization(method_info)`, `generate_call_args(method_info)`, `generate_fn_call(method_info)`. Each returns `TokenStream`; the orchestrator becomes a sequence of `quote!` interpolations.

    2. **`collect_tool_methods` at line 480, cog 44 → target ≤25.** Apply **P1** — extract per-attribute parsing using `parse_mcp_tool_attr` (already at line 581-599) as the analog. Add a sibling `classify_params_for_tool(&method.sig) -> Result<(ArgsType, bool, ParamOrder)>` and a `build_tool_method_info(method, macro_args, args_type, has_extra, param_order) -> Result<MethodInfo>` constructor. The outer `for item in &impl_block.items` loop becomes the small block shown in PATTERNS.md "Refactor Technique Mapping":
    ```rust
    for item in &impl_block.items {
        let ImplItem::Fn(method) = item else { continue };
        let Some(attr) = find_mcp_attr(method, "mcp_tool") else { continue };
        let macro_args = parse_mcp_tool_attr(attr, method)?;
        let (args_type, has_extra, param_order) = classify_params_for_tool(&method.sig)?;
        methods.push(build_tool_method_info(method, macro_args, args_type, has_extra, param_order)?);
    }
    ```

    3. **`collect_prompt_methods` at line 625, cog 42 → target ≤25.** Same pattern as `collect_tool_methods` — extract `parse_mcp_prompt_attr`, `classify_params_for_prompt`, `build_prompt_method_info`. The outer loop is identical shape.

    4. **`collect_resource_methods` at line 743, cog 80 → target ≤45 (NOT ≤25).** This is PATTERNS.md's flagged P5 candidate. Try P1 first using same shape as collect_tool_methods (extract `parse_mcp_resource_attr`, `classify_params_for_resource`, `build_resource_method_info`). If residual cog after extraction is ≤25, drop the P5 plan. If residual is 26-45, apply **P5** with this `// Why:` (per PATTERNS.md):
    ```rust
    // Why: resource URI-template parsing requires coupling extraction to
    // type-classification decisions; further splitting would create awkward
    // shared mutable state across helpers.
    #[allow(clippy::cognitive_complexity)]
    fn collect_resource_methods(...) { ... }
    ```
    If residual is >45 after extraction, the refactor is incomplete — extract more. **D-03 ceiling is 50; this function MUST come down from 80.**

    Per-refactor commit subject: `refactor(75-01b): <fn_name> in pmcp-macros/src/mcp_server.rs (cog NN→MM)`.

    Constraint: Generated `TokenStream` MUST be byte-identical pre/post refactor. The Wave 0 `expansion_snapshots.rs` is the regression contract — if it shows ANY diff after a refactor, either the refactor changed semantics (revert) or the snapshot needs intentional update (`cargo insta accept` ONLY with explicit justification in the commit message).

    After ALL 4 commits, run `cargo test -p pmcp-macros -- --test-threads=1` (snapshots already cover regression) AND `cargo build --workspace` (verifies downstream crates still compile against the regenerated macros). Per Codex Concern #8 (post-review revision), the full `cargo test --workspace --all-features` runs at WAVE-MERGE boundary in the bottom `<verification>` block — NOT after every per-function commit. Snapshot regression in `expansion_snapshots.rs` is the primary semantic safety net for pmcp-macros.
  </action>
  <verify>
    <automated>cargo test -p pmcp-macros -- --test-threads=1 && cargo build --workspace && cargo clippy -p pmcp-macros --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path == "pmcp-macros/src/mcp_server.rs")] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 4 functions in mcp_server.rs have cog ≤25 OR ≤50 with `// Why:` `#[allow]` (collect_resource_methods is the most likely P5)
    - No function in this file exceeds cog 50
    - `cargo test -p pmcp-macros --test expansion_snapshots -- --test-threads=1` exits 0 (snapshots unchanged OR explicitly accepted with documented justification)
    - `cargo build --workspace` exits 0 (downstream macro consumers still compile; full `cargo test --workspace` runs at wave-merge per concern #8)
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 4
    - For every `#[allow]` in this file: `grep -B1 'allow(clippy::cognitive_complexity)' pmcp-macros/src/mcp_server.rs | grep '// Why:'` returns lines
  </acceptance_criteria>
  <done>mcp_server.rs's 4 hotspots resolved; macro expansion snapshots byte-identical; all downstream crates still compile.</done>
</task>

<task type="auto">
  <name>Task 1b-B: Refactor pmcp-macros/src/mcp_resource.rs (expand_mcp_resource cog 71)</name>
  <files>pmcp-macros/src/mcp_resource.rs</files>
  <read_first>
    - pmcp-macros/src/mcp_resource.rs (full file — expand_mcp_resource @ line 78, current cog 71)
    - pmcp-macros/src/mcp_server.rs lines 577-599 (parse_mcp_tool_attr — analog small-helper shape)
    - pmcp-macros/tests/expansion_snapshots.rs (Wave 0 baseline)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 1b — same target shape as mcp_server.rs expand_*)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Refactor `expand_mcp_resource` at line 78, current cog 71 → target ≤35 (P5-allowed up to 50 with Why; aim for ≤25 with P1 first).

    Apply **P1** following the same per-section extraction pattern as `expand_mcp_server`:
    - `generate_resource_handler_struct(method_info, server_type, ...) -> TokenStream`
    - `generate_uri_template_parser(method_info) -> TokenStream`
    - `generate_resource_args_deserialization(method_info) -> TokenStream`
    - `generate_resource_fn_call(method_info) -> TokenStream`

    If residual cog after extraction is ≤25, done. If 26-35, lightly justified P5 (`// Why: resource URI-template + content-type negotiation share state`). If residual is 36-50, P5 is allowed but the rationale must be substantive. If residual >50, extract MORE — D-03 hard cap.

    Snapshot regression: `cargo test -p pmcp-macros --test expansion_snapshots -- expand_mcp_resource` must stay green. `cargo build --workspace` must succeed (full workspace test runs at wave-merge per concern #8).

    Commit subject: `refactor(75-01b): expand_mcp_resource (cog 71→MM)`.
  </action>
  <verify>
    <automated>cargo test -p pmcp-macros --test expansion_snapshots -- --test-threads=1 && cargo build --workspace && cargo clippy -p pmcp-macros --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path == "pmcp-macros/src/mcp_resource.rs")] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - expand_mcp_resource has cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - No function in this file exceeds cog 50
    - Snapshot test passes byte-identical
    - `cargo build --workspace` exits 0 (full `cargo test --workspace` deferred to wave-merge per concern #8)
    - `cargo clippy -p pmcp-macros --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by 1
  </acceptance_criteria>
  <done>expand_mcp_resource cog ≤50; snapshot intact.</done>
</task>

<task type="auto">
  <name>Task 1b-C: Refactor pmcp-macros/src/mcp_prompt.rs and mcp_tool.rs (2 functions)</name>
  <files>pmcp-macros/src/mcp_prompt.rs, pmcp-macros/src/mcp_tool.rs</files>
  <read_first>
    - pmcp-macros/src/mcp_prompt.rs (expand_mcp_prompt @ line 47, current cog 42)
    - pmcp-macros/src/mcp_tool.rs (expand_mcp_tool @ line 66, current cog 40)
    - pmcp-macros/tests/expansion_snapshots.rs (Wave 0 baseline)
    - pmcp-macros/src/mcp_server.rs lines 577-599 (analog shape)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 1b — both should hit ≤25 with P1; "None should need P5")
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md
  </read_first>
  <action>
    Two refactors, two atomic commits:

    1. **`pmcp-macros/src/mcp_prompt.rs::expand_mcp_prompt` at line 47, cog 42 → target ≤25.** Apply **P1** — extract `generate_prompt_handler_struct`, `generate_prompt_args_deserialization`, `generate_prompt_fn_call`. Same shape as `expand_mcp_server`. PATTERNS.md says this should hit ≤25 cleanly without P5. Commit: `refactor(75-01b): expand_mcp_prompt (cog 42→MM)`.

    2. **`pmcp-macros/src/mcp_tool.rs::expand_mcp_tool` at line 66, cog 40 → target ≤25.** Apply **P1** — extract `generate_tool_handler_struct`, `generate_tool_args_deserialization`, `generate_tool_fn_call`. Should hit ≤25. Commit: `refactor(75-01b): expand_mcp_tool (cog 40→MM)`.

    For each: snapshot test + workspace test must stay green.

    If residual after P1 extraction is 26-50, apply P5 with substantive `// Why:` per W0 template — but PATTERNS.md predicts neither needs it.
  </action>
  <verify>
    <automated>cargo test -p pmcp-macros --test expansion_snapshots -- --test-threads=1 && cargo build --workspace && cargo clippy -p pmcp-macros --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path | test("pmcp-macros/src/mcp_(prompt|tool)\\.rs"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - expand_mcp_prompt and expand_mcp_tool both have cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - No function in either file exceeds cog 50
    - Snapshot tests pass byte-identical
    - `cargo build --workspace` exits 0 (full `cargo test --workspace` deferred to wave-merge per concern #8)
    - `cargo clippy -p pmcp-macros --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by 2
  </acceptance_criteria>
  <done>expand_mcp_prompt and expand_mcp_tool refactored; snapshots intact; downstream consumers compile.</done>
</task>

</tasks>

<verification>
After all sub-wave 1a + 1b tasks complete:

1. **PMAT complexity rollup**: `pmat quality-gate --fail-on-violation --checks complexity --format json | jq '.violations | length'` strictly less than baseline (target: ~33 fewer than start, depending on side-effect cleanups).
2. **Per-file zero-violations check** for the 4 src/ hotspot files + 4 pmcp-macros files: `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | test("src/server/(streamable_http_server|path_validation|schema_utils|workflow/task_prompt_handler)|src/utils/json_simd|pmcp-macros/src/(mcp_server|mcp_resource|mcp_prompt|mcp_tool))\\.rs"))] | length'` = 0.
3. **D-02 conformance**: `for f in <19 files>; do test "$(grep -c 'allow(clippy::cognitive_complexity)' $f)" -eq "$(grep -B1 'allow(clippy::cognitive_complexity)' $f | grep -c '// Why:')"; done` (no bare allows).
4. **D-03 conformance**: `pmat analyze complexity --format json | jq '[.violations[] | select(.complexity > 50)] | length'` = 0 across the 19 modified files.
5. **Snapshot regression**: `cargo test -p pmcp-macros --test expansion_snapshots -- --test-threads=1` exits 0 with no `.snap.new` files staged.
6. **Workspace test green**: `cargo test --workspace --all-features -- --test-threads=1` exits 0.
7. **`make quality-gate` exits 0** (fmt + clippy + build + test + audit).

Wave-merge commit message format: `refactor(75-01): wave 1 complete — pmat-complexity: NN (was MM)` where NN < MM.
</verification>

<success_criteria>
- 13 src/ hotspot functions refactored to cog ≤25 OR ≤50 with Why-justified allow (Tasks 1a-A, 1a-B)
- 13 pre-existing bare allows in src/ retro-justified per D-02 (Task 1a-C)
- 7 pmcp-macros hotspot functions refactored to cog ≤25 OR ≤50 with Why (Tasks 1b-A, 1b-B, 1b-C)
- Macro expansion snapshots byte-identical (Wave 0 contract preserved)
- Workspace test green; downstream consumers compile
- PMAT total complexity violation count drops by at least 20 (the named hotspots' contribution; retro-justifications don't change the count)
- Wave-merge commit records `pmat-complexity: NN (was MM)` for git-log auditability
</success_criteria>

<output>
After completion, create `.planning/phases/75-fix-pmat-issues/75-01-SUMMARY.md` per the GSD summary template, including:
- Per-function before/after cog scores
- List of any P5 sites added (with Why text)
- Snapshot diff status (no diff / explicitly accepted with reason)
- Wave-rollup PMAT count delta
</output>
