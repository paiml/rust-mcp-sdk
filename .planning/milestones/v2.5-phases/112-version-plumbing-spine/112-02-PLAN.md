---
phase: 112-version-plumbing-spine
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - src/server/cancellation.rs
autonomous: true
requirements: [VERS-01, VERS-03, VERS-09]
must_haves:
  truths:
    - "A handler can read the resolved protocol era via extra.era()"
    - "A handler can read per-request client identity via extra.client_info() and extra.protocol_version()"
    - "A handler can read W3C trace-context via extra.trace_context()"
    - "protocol_context is added to the native RequestHandlerExtra (src/server/cancellation.rs), the only field-carrying RequestHandlerExtra compiled into the crate; the wasm32 build's RequestHandlerExtra is a separate zero-field stub (src/server/mod.rs:162) that is OUT OF SCOPE this phase (tied to WasmServerCore being out of scope)"
    - "The additive change does not break the wasm32 build (cargo build --lib --target wasm32-unknown-unknown stays green)"
    - "Existing RequestHandlerExtra construction (new/with_*) still compiles unchanged for every current caller"
  artifacts:
    - path: "src/server/cancellation.rs"
      provides: "native RequestHandlerExtra.protocol_context field + with_protocol_context + accessors"
      contains: "protocol_context"
  key_links:
    - from: "src/server/cancellation.rs"
      to: "src/types/protocol/context.rs"
      via: "protocol_context field holds Option<ProtocolContext>"
      pattern: "protocol_context: Option<.*ProtocolContext>"
---

<objective>
Add the per-request `protocol_context` carrier and its typed accessors to the native `RequestHandlerExtra` (`src/server/cancellation.rs`) — the handler-facing surface for VERS-01/03/09 — following the Phase-109 `request_meta` additive-field precedent exactly.

**Scope note (verified against the live tree):** `src/server/cancellation.rs` is the ONLY field-carrying `RequestHandlerExtra` compiled into the crate. It is gated `#[cfg(not(target_arch = "wasm32"))]` (src/server/mod.rs:49). On wasm32, `RequestHandlerExtra` is a separate zero-field stub `pub struct RequestHandlerExtra;` (src/server/mod.rs:162) that carries no per-request context — extending it is OUT OF SCOPE this phase (tied to `WasmServerCore` being out of scope). `src/shared/cancellation.rs` is a DEAD ORPHAN file — it is NOT declared in `src/shared/mod.rs` (no `pub mod cancellation`) and is never compiled on any target; do NOT edit it. This phase adds `protocol_context` to the native struct only; the wasm build must merely stay green.

Purpose: Handlers must read the resolved era and client identity via typed accessors, never via ambient session state. This is the "handler surface" tier; Plan 04 wires the dispatch layer that populates the field (and adds the end-to-end handler-visibility test).
Output: `protocol_context` field + `with_protocol_context()` builder + `era()/protocol_version()/client_info()/client_capabilities()/trace_context()` accessors on the native `src/server/cancellation.rs` `RequestHandlerExtra`, with a unit test and self-reported-identity rustdoc, and a green wasm32 build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md
@.planning/phases/112-version-plumbing-spine/112-01-PLAN.md

<interfaces>
<!-- The additive-field triplet to replicate, from the Phase-109 request_meta precedent. Line numbers are plan-time; the file changed since — locate by symbol, not by number. -->

Native RequestHandlerExtra (src/server/cancellation.rs) — the ONLY compiled field-carrying struct, edit THIS file:
  #[non_exhaustive]
  pub struct RequestHandlerExtra { ... }
  pub auth_context: Option<crate::server::auth::AuthContext>,     // native-only field
  pub request_meta: Option<serde_json::Value>,                    // Phase-109 additive precedent
  pub fn new(request_id: String, cancellation_token: CancellationToken) -> Self { ... request_meta: None, ... }
  pub fn with_request_meta(mut self, meta: Option<serde_json::Value>) -> Self { self.request_meta = meta; self }

DO NOT edit src/shared/cancellation.rs — it is a dead orphan (not declared in src/shared/mod.rs, never compiled). The wasm32 RequestHandlerExtra is the zero-field stub at src/server/mod.rs:162 and is out of scope.

New types from Plan 01 (crate::types::protocol):
  ProtocolContext { era, negotiated_version, client_info, client_capabilities }
  TraceContext::from_meta(&serde_json::Value) -> Option<TraceContext>   // bounded-validated, RAW/untrusted
  Era { V1, V2 }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add protocol_context field + builder + era/identity accessors (native RequestHandlerExtra)</name>
  <files>src/server/cancellation.rs</files>
  <read_first>
    - src/server/cancellation.rs (the RequestHandlerExtra struct; the new() default block; with_request_meta; the with_auth_context accessor pattern to mirror — locate by symbol, line numbers drifted)
    - src/server/mod.rs (lines 48-49 confirm src/server/cancellation.rs is #[cfg(not(target_arch = "wasm32"))]; line 161-166 is the wasm32 zero-field stub you must NOT touch)
    - src/types/protocol/context.rs (ProtocolContext shape from Plan 01)
  </read_first>
  <behavior>
    - RequestHandlerExtra::new(...) defaults protocol_context to None
    - .with_protocol_context(Some(ctx)) sets the field and returns Self (builder chain compatible with existing .with_* calls)
    - extra.era() returns Some(Era::V2) when a v2 ProtocolContext is attached, None when the field is None
    - extra.protocol_version() returns the negotiated ProtocolVersion when a context is attached, None otherwise
    - extra.client_info() returns Some(&Implementation) when the context carries clientInfo, None otherwise
    - extra.client_capabilities() returns Some(&ClientCapabilities) when present, None otherwise
    - The accessor signatures follow the existing with_auth_context / with_request_meta style
  </behavior>
  <action>
    In `src/server/cancellation.rs` (the native, and only compiled, `RequestHandlerExtra`): add an additive public field `pub protocol_context: Option<crate::types::protocol::ProtocolContext>` to the `#[non_exhaustive]` `RequestHandlerExtra` struct, default it to `None` in every `new()` constructor, and add `#[must_use] pub fn with_protocol_context(mut self, ctx: Option<ProtocolContext>) -> Self`. Add typed accessors: `era() -> Option<Era>`, `protocol_version() -> Option<&ProtocolVersion>` (or owned clone — match the existing accessor style), `client_info() -> Option<&Implementation>`, `client_capabilities() -> Option<&ClientCapabilities>`. Each accessor reads through `self.protocol_context.as_ref()`.

    Scope correction (supersedes the earlier "shared cfg-safe impl / native+wasm parity" framing, which was based on a stale premise): there is NO second field-carrying RequestHandlerExtra to mirror. `src/shared/cancellation.rs` is a dead orphan (never compiled) and the wasm32 `RequestHandlerExtra` (src/server/mod.rs:162) is a zero-field stub carrying no context — both are out of scope. Implement the field + accessors ONCE on the native struct. Do NOT create, edit, or add a `pub mod cancellation` for `src/shared/cancellation.rs`.

    Do NOT change the type of any existing field (auth_context/request_meta) — overloading an existing field is an accidental-3.0 break (Pitfall 8). This is purely additive.

    Reviewer concern addressed (Codex MEDIUM — "add rustdoc explicitly marking client_info and client_capabilities as self-reported"): rustdoc `client_info()` and `client_capabilities()` (and `era()`/`protocol_version()`) as SELF-REPORTED, client-supplied `_meta` values that are informational ONLY and MUST NOT be used as an authorization anchor — real identity binds to the OAuth token (enforced in Phase 114 / TASK-05). No auth decision is made from these accessors in this phase.

    Add a unit test mirroring the existing cancellation-module tests: construct a RequestHandlerExtra with a v2 ProtocolContext via new()+with_protocol_context and assert `era()==Some(Era::V2)` and `client_info()` readback.
  </action>
  <verify>
    <automated>cargo test --lib cancellation::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'protocol_context' src/server/cancellation.rs` ≥ 3 (field, default, accessor)
    - `src/server/cancellation.rs` contains `pub fn with_protocol_context` and `pub fn era(`
    - `src/shared/cancellation.rs` is NOT modified (`git diff --stat src/shared/cancellation.rs` shows no change — it is a dead orphan)
    - `client_info()`/`client_capabilities()` carry rustdoc marking them self-reported / not-for-authorization
    - `cargo build --lib` exits 0 AND `cargo build --lib --target wasm32-unknown-unknown` exits 0 (the native-only additive change must not break the wasm build)
    - `cargo test --lib cancellation::` exits 0 including the new test
    - `grep -n 'pub auth_context' src/server/cancellation.rs` shows the field type unchanged (no existing field mutated)
  </acceptance_criteria>
  <done>The native RequestHandlerExtra carries protocol_context + builder + era/identity accessors; identity accessors rustdoc'd self-reported; existing callers still compile; wasm build stays green; the orphan shared/cancellation.rs is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add trace_context() accessor over request_meta (VERS-09)</name>
  <files>src/server/cancellation.rs</files>
  <read_first>
    - src/server/cancellation.rs (the request_meta field; the accessors added in Task 1 — locate by symbol)
    - src/types/protocol/context.rs (TraceContext::from_meta from Plan 01 — bounded-validated)
  </read_first>
  <behavior>
    - extra.trace_context() returns Some(TraceContext) when request_meta carries a traceparent key (within bounds)
    - extra.trace_context() returns None when request_meta is None or lacks traceparent
    - traceparent/tracestate/baggage values round-trip from request_meta JSON into the returned TraceContext, subject to Plan 01's bounded-length policy
  </behavior>
  <action>
    In `src/server/cancellation.rs`, add `pub fn trace_context(&self) -> Option<crate::types::protocol::TraceContext>` that reads the existing `self.request_meta` (`Option<serde_json::Value>`) and delegates to `TraceContext::from_meta(...)`. Do NOT add a new field for trace context — VERS-09 keys live in the existing `_meta` flatten-map JSON already carried by `request_meta` (Pattern 3). (Native struct only — the orphan `src/shared/cancellation.rs` and the wasm zero-field stub are out of scope, per Task 1's scope correction.)

    Reviewer contract note (Codex MEDIUM — "decide whether trace values are raw or validated and document that contract"): the CONTRACT is decided in Plan 01 — values are RAW/unvalidated except for length-bounding (`MAX_TRACE_VALUE_LEN`), self-reported, not trusted. Rustdoc `trace_context()` to point at that contract so handlers know these are untrusted, bounded, raw W3C values.

    Add unit tests: build a RequestHandlerExtra via new()+with_request_meta(Some(json!({"traceparent":"00-abc-01","tracestate":"a=1","baggage":"k=v"}))) and assert `trace_context()` returns the exact traceparent/tracestate/baggage; build one with request_meta=None and assert `trace_context()` is None.
  </action>
  <verify>
    <automated>cargo test --lib cancellation::</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/cancellation.rs` contains `pub fn trace_context`
    - No new struct field was added for trace context (grep shows trace_context only as a method, reading request_meta)
    - `trace_context()` rustdoc references the raw/bounded/untrusted contract
    - `cargo test --lib cancellation::` exits 0 including the trace_context tests
    - `cargo build --lib --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>trace_context() surfaces W3C keys from the existing native request_meta, tested, with no new field, documented as raw/bounded/untrusted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client _meta → handler | Self-reported clientInfo / trace keys surfaced to handler code as read-only accessors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02 | Spoofing | `RequestHandlerExtra::client_info()` | accept (documented) | `client_info()` surfaces SELF-REPORTED identity from `_meta` — rustdoc states it is informational only and MUST NOT be used as an authorization anchor; real identity binds to the OAuth token, enforced in Phase 114 (TASK-05). No auth decision is made in this phase from this value |
| T-112-08 | Elevation (semver) | additive field on `#[non_exhaustive]` structs | mitigate | Additive field + `with_*` builder + accessor keeps `new()`/`with_*` callers source-compatible; `cargo semver-checks` at the phase gate confirms minor. Never change an existing field's type |
</threat_model>

<verification>
- `cargo test --lib cancellation::` green (protocol_context + trace_context tests)
- `cargo build --lib` and `cargo build --lib --target wasm32-unknown-unknown` both clean (the native-only additive change must not break the wasm build)
- `grep -c protocol_context src/server/cancellation.rs` shows the field present; `git diff --stat src/shared/cancellation.rs` shows no change (orphan untouched)
</verification>

<success_criteria>
- Handlers can call `extra.era()`, `extra.protocol_version()`, `extra.client_info()`, `extra.client_capabilities()`, `extra.trace_context()` on the native RequestHandlerExtra
- Identity/trace accessors rustdoc'd as self-reported/untrusted
- No existing RequestHandlerExtra field type changed; all current callers still compile; wasm build stays green; the orphan shared/cancellation.rs is untouched
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-02-SUMMARY.md` when done
</output>
