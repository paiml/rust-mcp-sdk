---
phase: 112-version-plumbing-spine
plan: 07
type: execute
wave: 5
depends_on: [112-03, 112-04, 112-05]
files_modified:
  - src/server/core.rs
  - src/server/mod.rs
  - src/server/task_dispatch.rs
  - src/types/jsonrpc.rs
autonomous: true
requirements: [VERS-06]
must_haves:
  truths:
    - "Every production error-emission site in the server dispatch layer resolves its code from an error_codes:: constant, not a bare integer literal"
    - "The frozen -32002 task-pending wire value is emitted via error_codes::V1_TASK_PENDING and is byte-identical (the locking test pending_tasks_result_preserves_minus_32002 stays green, untouched)"
    - "The frozen -32601 method-not-supported sites emit via error_codes::METHOD_NOT_FOUND with an identical wire value"
    - "jsonrpc.rs production error construction resolves its code from the centralized table"
    - "A repo-wide semantic audit confirms no production protocol-error literal (including feature-gated modules and struct-literal constructions) remains outside the centralized table after this + Plan 08 land"
  artifacts:
    - path: "src/server/core.rs"
      provides: "dispatch error literals (-32603/-32601/-32002) migrated to error_codes:: constants"
      contains: "error_codes::"
    - path: "src/server/mod.rs"
      provides: "twin-site dispatch error literals migrated to error_codes:: constants"
      contains: "error_codes::"
    - path: "src/server/task_dispatch.rs"
      provides: "tasks/result store-first ladder (-32603/-32601/frozen -32002) migrated to error_codes:: constants"
      contains: "error_codes::V1_TASK_PENDING"
    - path: "src/types/jsonrpc.rs"
      provides: "production JSONRPCError construction migrated to error_codes:: constants"
      contains: "error_codes::"
  key_links:
    - from: "src/server/task_dispatch.rs"
      to: "src/types/protocol/error_codes.rs"
      via: "the frozen -32002 at task_dispatch.rs:576 now reads error_codes::V1_TASK_PENDING (== -32002); wire value unchanged"
      pattern: "error_codes::V1_TASK_PENDING"
    - from: "src/server/core.rs"
      to: "src/types/protocol/error_codes.rs"
      via: "the frozen -32002 at core.rs:1145 now reads error_codes::V1_TASK_PENDING; standard codes read error_codes::INTERNAL_ERROR / METHOD_NOT_FOUND"
      pattern: "error_codes::(V1_TASK_PENDING|INTERNAL_ERROR|METHOD_NOT_FOUND)"
---

<objective>
Make the centralized version-gated error-code table from Plan 03 the ACTUAL source of truth: migrate the ~40 raw integer-literal error-code call sites in the server dispatch layer (`core.rs`, `mod.rs`, `task_dispatch.rs`) and in `jsonrpc.rs` to read from `error_codes::*` constants — including the two frozen `-32002` task-pending sites — so VERS-06's "all error codes resolve from one centralized table" is satisfied on the wire, and run a repo-wide semantic audit (with Plan 08) so the completeness claim is honest.

Purpose: Plan 03 declared the table (VERS-06 structure-first) but left the emitting call sites on bare literals, so the table was a shadow copy disconnected from the code that puts errors on the wire (checker Blocker 1). This plan closes that gap. It is the LAST wave (5) precisely because it rewrites `core.rs`/`mod.rs` after Plans 04/05 have finished adding their dispatch arms — no same-wave file overlap.
Output: All migrated production error-emission sites reference `error_codes::*`; frozen `-32002`/`-32601` wire values byte-identical; frozen locking test untouched and green; a repo-wide audit acceptance gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-RESEARCH.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md
@.planning/phases/112-version-plumbing-spine/112-03-PLAN.md

<interfaces>
<!-- The centralized constants (Plan 03) and the exact literal call sites to migrate. -->

From Plan 03 (src/types/protocol/error_codes.rs — import as `crate::types::protocol::error_codes`):
  pub const INVALID_REQUEST:  i32 = -32600;
  pub const METHOD_NOT_FOUND: i32 = -32601;
  pub const INVALID_PARAMS:   i32 = -32602;
  pub const INTERNAL_ERROR:   i32 = -32603;
  pub const PARSE_ERROR:      i32 = -32700;
  pub const V1_TASK_PENDING:  i32 = -32002;   // FROZEN — re-export of the task-pending code

Literal call sites to migrate (verified against the 2.17.0 tree; test-module and doc-comment literals are LEFT AS-IS):
  src/server/core.rs
    997/1135/1155/1194/1254/1261/1268/1276/1284/1292  Self::error_response(id, -32603, ...)   → INTERNAL_ERROR
    1145                                               -32002 (FROZEN task-pending)            → V1_TASK_PENDING
    1307/1311                                          -32601 "Method not supported"           → METHOD_NOT_FOUND
    1615  assert_eq!(e.code, -32002)   // TEST — leave literal (asserts the wire value)
  src/server/mod.rs
    1287  code: -32601   → METHOD_NOT_FOUND
    1416  code: -32603   → INTERNAL_ERROR
    5036  assert_eq!(error.code, -32601)   // TEST — leave literal
  src/server/task_dispatch.rs
    442/450/457/555/567/600/608/634/642/665/673  error_response(id, -32603, ...)  → INTERNAL_ERROR
    576                                            -32002 (FROZEN task-pending)     → V1_TASK_PENDING
    580/611/645/676/703                            error_response(id, -32601, ...)  (FROZEN) → METHOD_NOT_FOUND
  src/types/jsonrpc.rs
    340  Self::new(-32603, err.to_string())   → INTERNAL_ERROR   (production From impl)
    // 179/194 are doc-comment examples; 438/443/512/551/576/578/595/600/605/608 are #[cfg(test)] — LEAVE all of these literal

FROZEN discipline (Pitfall 6): src/server/task_dispatch_tests.rs:355 pending_tasks_result_preserves_minus_32002 asserts the WIRE value -32002; V1_TASK_PENDING == -32002 keeps it byte-identical. DO NOT edit that test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate server-dispatch error literals (core.rs, mod.rs, task_dispatch.rs) to error_codes:: constants</name>
  <files>src/server/core.rs, src/server/mod.rs, src/server/task_dispatch.rs</files>
  <read_first>
    - src/types/protocol/error_codes.rs (the constants from Plan 03 — confirm INTERNAL_ERROR/METHOD_NOT_FOUND/V1_TASK_PENDING names)
    - src/server/core.rs (error_response literal sites listed in <interfaces>; the frozen -32002 at 1145; test assert at 1615)
    - src/server/mod.rs (code: literals at 1287/1416; test assert at 5036)
    - src/server/task_dispatch.rs (error_response ladder; frozen -32002 at 576; frozen -32601 sites)
    - .planning/phases/112-version-plumbing-spine/112-RESEARCH.md (Pitfall 6 — frozen -32002 discipline)
  </read_first>
  <action>
    Add `use crate::types::protocol::error_codes;` (or reference the fully-qualified path) in each of `core.rs`, `mod.rs`, `task_dispatch.rs`, and replace every PRODUCTION error-code integer literal with its named constant: `-32603` → `error_codes::INTERNAL_ERROR`, `-32601` → `error_codes::METHOD_NOT_FOUND`, `-32602` → `error_codes::INVALID_PARAMS`, `-32600` → `error_codes::INVALID_REQUEST`, and the two FROZEN task-pending sites (`core.rs:1145`, `task_dispatch.rs:576`) `-32002` → `error_codes::V1_TASK_PENDING`.

    CRITICAL (Pitfall 6): this is a NAME-for-VALUE swap only — the emitted wire value must stay byte-identical. `error_codes::V1_TASK_PENDING == -32002` and `error_codes::METHOD_NOT_FOUND == -32601`, so the FROZEN wire values do NOT change. Do NOT edit `pending_tasks_result_preserves_minus_32002` (task_dispatch_tests.rs:355) and do NOT reconcile pmcp's task-pending `-32002` with the spec's resource-not-found rename.

    LEAVE test-module literals AS-IS: the `#[cfg(test)]` assertions (`core.rs:1615`, `mod.rs:5036`) keep bare literals — they are the independent oracle that proves the constant still equals the frozen wire value. Migrating them would make the test tautological. Doc-comment `///` examples also stay literal.

    NOTE: the line numbers above are plan-time positions and WILL have shifted after Plans 04/05 edited core.rs/mod.rs — locate the sites by pattern (the `error_response`/`code:` construction), not by trusting the numbers.
  </action>
  <verify>
    <automated>cargo test --lib server::core && cargo test --lib server::task_dispatch && cargo test --lib pending_tasks_result_preserves_minus_32002 && cargo build --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'error_codes::V1_TASK_PENDING' src/server/task_dispatch.rs` ≥ 1 AND `grep -c 'error_codes::V1_TASK_PENDING' src/server/core.rs` ≥ 1
    - `grep -c 'error_codes::' src/server/core.rs` ≥ 3 AND `grep -c 'error_codes::' src/server/mod.rs` ≥ 1 AND `grep -c 'error_codes::' src/server/task_dispatch.rs` ≥ 3
    - No bare-literal production error_response calls remain: `grep -cE 'error_response\(id, -326' src/server/task_dispatch.rs` == 0 AND `grep -cE 'error_response\(id, -326' src/server/core.rs` == 0
    - No bare-literal frozen task-pending line remains outside tests: `grep -nE '^\s*-32002,\s*$' src/server/core.rs` returns nothing
    - `git diff --stat src/server/task_dispatch_tests.rs` shows NO change (frozen test untouched)
    - `cargo test --lib pending_tasks_result_preserves_minus_32002` exits 0 (frozen wire value preserved)
    - `cargo build --lib` and existing dispatch tests (`cargo test --lib server::core`, `server::task_dispatch`) exit 0
  </acceptance_criteria>
  <done>All production error-code literals in core.rs/mod.rs/task_dispatch.rs resolve from error_codes:: constants; frozen -32002/-32601 wire values byte-identical; the frozen locking test is untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate jsonrpc.rs production error construction + repo-wide error-code audit gate</name>
  <files>src/types/jsonrpc.rs</files>
  <read_first>
    - src/types/jsonrpc.rs (production From impl at 340 `Self::new(-32603, ...)`; note doc examples at 179/194 and the #[cfg(test)] block are LEFT literal)
    - src/types/protocol/error_codes.rs (constants; jsonrpc.rs is a sibling under src/types/ — import via `crate::types::protocol::error_codes`)
  </read_first>
  <action>
    In `src/types/jsonrpc.rs`, migrate the PRODUCTION error-code literal(s) — the `From`-impl default at line 340 `Self::new(-32603, err.to_string())` → `Self::new(error_codes::INTERNAL_ERROR, err.to_string())`. If any other non-test, non-doc production construction uses a bare `-326xx`/`-32700`, migrate it (e.g. a real parse-error path → `error_codes::PARSE_ERROR`).

    LEAVE the `#[cfg(test)]` module literals and the `///` doctest examples AS-IS — the tests are the oracle for the constant values. Do NOT change any public type or the numeric values.

    **Reviewer concern addressed (Codex MEDIUM — "error-code audit may still be incomplete"):** run a REPO-WIDE semantic audit as the final VERS-06 acceptance gate (this is the last dispatch/jsonrpc migration; combined with Plan 08's transport migration, the phase must leave no production protocol-error literal outside the table). Grep across ALL of `src/` (production only — exclude `#[cfg(test)]` blocks and `///` doc lines) for bare protocol-error literals in error construction — INCLUDING (a) other transports (stdio/wasm/websocket), (b) error construction via struct literals (`JSONRPCError { code: -32xxx, .. }` / `ErrorObject { code: -32xxx }`), (c) casts from `ProtocolErrorCode`, and (d) feature-gated modules (run the grep with the relevant features considered). Record the audit command + result in the SUMMARY. Any surviving production literal outside Plan 07/08's migrated files is a finding — either migrate it (if this plan owns the file) or record it as a follow-up VERS-06 item with the owning file. The surviving `ProtocolErrorCode` discriminants (the parallel numeric definition) are noted per Plan 03's audit note — the consistency test is the binding guard.
  </action>
  <verify>
    <automated>cargo test --lib jsonrpc && cargo build --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'error_codes::' src/types/jsonrpc.rs` ≥ 1
    - The production From impl no longer constructs with a bare literal: `grep -c 'Self::new(-32603' src/types/jsonrpc.rs` == 0
    - `#[cfg(test)]` assertions still reference raw literals (oracle preserved) and `cargo test --lib jsonrpc` exits 0
    - The SUMMARY records the repo-wide audit command + result: no production protocol-error literal remains outside error_codes:: across src/ (excluding tests/docs; including struct-literal constructions + feature-gated modules), or any finding is recorded with its owning file
    - `cargo build --lib` exits 0
  </acceptance_criteria>
  <done>jsonrpc.rs production error construction resolves from the centralized error_codes:: table; a repo-wide semantic audit (incl. struct literals + feature-gated modules) confirms VERS-06 completeness or records findings; test/doc literals remain the value oracle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| centralized table ↔ wire output | The migrated call sites must emit the SAME numeric code the table defines — a name/value drift would silently change the wire contract |
| frozen -32002/-32601 ↔ locking tests | The frozen wire values must survive the literal→constant swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-06b | Tampering | frozen `-32002`/`-32601` wire value during literal→constant migration | mitigate | Name-for-value swap only; `V1_TASK_PENDING == -32002`, `METHOD_NOT_FOUND == -32601` proven by Plan 03's consistency test; frozen locking test left untouched and re-run as the byte-identity guard |
| T-112-06c | Tampering | table/emit drift (shadow-copy regression) | mitigate | The emitting sites now READ the table (single source of truth); Plan 03's consistency test asserts consts == ProtocolErrorCode enum; repo-wide audit confirms no production literal escapes |
</threat_model>

<verification>
- `cargo test --lib pending_tasks_result_preserves_minus_32002` green with `git diff` clean on task_dispatch_tests.rs (frozen)
- `cargo test --lib server::core`, `server::task_dispatch`, `jsonrpc` green (v1 dispatch/serialization unchanged)
- `grep -c 'error_codes::' src/server/core.rs src/server/mod.rs src/server/task_dispatch.rs src/types/jsonrpc.rs` all ≥ 1
- No production `error_response(id, -326...)` bare literals remain (grep gate == 0); repo-wide audit recorded in SUMMARY
- Phase-end AUTHORITATIVE gate (run after BOTH wave-5 migrations land): `cargo semver-checks check-release` classifies minor (no `major`, no `enum_variant_added` on public request enums); `make quality-gate` green
</verification>

<success_criteria>
- The centralized error_codes:: table is the actual source of truth for the server dispatch layer + jsonrpc.rs production paths
- Frozen -32002/-32601 wire values byte-identical; frozen locking test untouched and green
- Repo-wide audit confirms VERS-06 completeness (or records findings with owners)
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-07-SUMMARY.md` when done
</output>
