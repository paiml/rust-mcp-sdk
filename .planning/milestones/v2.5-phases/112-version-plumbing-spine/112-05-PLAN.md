---
phase: 112-version-plumbing-spine
plan: 05
type: execute
wave: 4
depends_on: [112-03, 112-04]
files_modified:
  - src/server/core.rs
  - src/server/mod.rs
  - src/shared/protocol_helpers.rs
autonomous: true
requirements: [VERS-03, VERS-04, VERS-07, VERS-08]
must_haves:
  truths:
    - "A v2 client calling server/discover receives a read-only projection of the already-computed ServerCore capabilities, including the extensions map, routed via the crate-private internal dispatch (no public ClientRequest variant)"
    - "A v1-era (or non-opted-in) request for server/discover receives standard -32601 method-not-found"
    - "Every v2 OBJECT result carries a resultType discriminator that defaults to complete, unless the handler already set a non-default disposition (the path Phases 113/114 use)"
    - "v1 responses are byte-identical to today — no resultType envelope, no serverInfo injection — proven by golden wire fixtures"
    - "v2 object results carry serverInfo; scalar/array/null results and error/notification responses are left unchanged (documented)"
    - "The discover routing and the resultType/serverInfo injection are factored into shared helpers that BOTH native dispatch sites (core.rs, mod.rs) call — one implementation, not per-site copies"
  artifacts:
    - path: "src/server/core.rs"
      provides: "server/discover era-gated arm (internal dispatch) + resultType(v2-only) + serverInfo(v2) injection at serialization"
      contains: "handle_discover"
    - path: "src/shared/protocol_helpers.rs"
      provides: "method-string interception routing server/discover to internal dispatch before the public-enum conversion"
      contains: "classify_internal_method"
    - path: "src/server/mod.rs"
      provides: "twin-site discover/resultType/serverInfo parity"
      contains: "resultType"
  key_links:
    - from: "src/server/core.rs"
      to: "ServerCore.capabilities"
      via: "discover projects already-computed capabilities read-only (no recompute)"
      pattern: "self\\.capabilities"
---

<objective>
Add the era-gated dispatch behaviors that ride on the spine from Plan 04: `server/discover` as a read-only capability projection routed via Plan 03's crate-private internal dispatch (V2 only; V1 gets `-32601`), the `resultType` envelope injected at serialization (v2-only, defaulting to `complete`), and `serverInfo` on v2 results — all without touching v1 wire output.

**Scope note (wasm):** `WasmServerCore` (`src/server/wasm_core.rs`) is a separate minimal standalone impl with no capabilities/opt-in/per-request-context surface and is OUT OF SCOPE for this phase; v2 support for it (server/discover, resultType, serverInfo) is a separate future phase. This plan touches ONLY the native dispatch sites (core.rs, mod.rs) and the shared method-interception seam (protocol_helpers.rs). Verified: `src/server/core.rs`/`mod.rs` are `#[cfg(not(target_arch = "wasm32"))]` (excluded from wasm32 entirely, src/server/mod.rs:37) — there is no "wasm build of core.rs" and v2 is unreachable on wasm (only the out-of-scope WasmServerCore exists there). Nothing wasm needs discover/envelope injection; the only wasm obligation is that `cargo build --lib --target wasm32-unknown-unknown` stays green.

Purpose: `server/discover` is the stateless replacement for initialize's capability exchange (D-09); `resultType` is the discriminator Phases 113 (`input_required`) and 114 (`task`) will set. Keeping all of it v2-only (D-07/D-08) means zero v1 fixture churn and no risk to strict v1 consumers.

**Reviewer concern addressed (Codex HIGH #4 recap):** server/discover is dispatched via the crate-private `classify_internal_method`/`InternalClientRequest` from Plan 03 — the public `ClientRequest`/`Request` enums stay UNCHANGED. **Reviewer concern addressed (Codex HIGH #5 — generic resultType/serverInfo injection is underspecified):** this plan pins the exact envelope model (object vs scalar/array/null, collision behavior, error/notification exclusion) and the disposition path Phases 113/114 use, keeping injection at the serialization boundary (D-07/D-08 locked, answered-with-rationale below).

Output: `handle_discover` projecting `ServerCore.capabilities` (incl. `extensions`) behind an internal conversion + golden fixtures; internal `ResponseDisposition`/`ResultType` injected at the era-gated serialization boundary via a shared helper both native dispatch sites call; `serverInfo` on v2 object results; V1 `server/discover` → `-32601`; v1 + v2 golden wire fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-RESEARCH.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md
@.planning/phases/112-version-plumbing-spine/112-03-PLAN.md
@.planning/phases/112-version-plumbing-spine/112-04-PLAN.md

<interfaces>
<!-- Dispatch + capability anchors. -->

Capability source (read-only projection — do NOT recompute):
  src/server/core.rs:461   capabilities: self.capabilities.clone(),   // build-time projection
  src/server/core.rs:1045  &self.capabilities                          // accessor
  ServerCapabilities.extensions: Option<HashMap<String, Value>>  (src/types/capabilities.rs:109)

Era-gate arm precedent (src/server/core.rs:1125-1148):
  match on request → Initialize arm, is_initialized() -32002 guard, success_response at ~1134
  handle_request_internal now carries protocol_context (Plan 04)

Method-string interception seam (src/shared/protocol_helpers.rs):
  parse_request at 15-31 → Err(method_not_found) for unknown methods (gives v1 discover its -32601 free)
  classify_internal_method(method, params) -> Option<InternalClientRequest>  (Plan 03, crate-private)

serverInfo shape (src/types/protocol/mod.rs):
  InitializeResult.server_info: Implementation (line 266)

wasm reality (verified): core.rs/mod.rs are excluded from wasm32 (src/server/mod.rs:37) — no "wasm build of core.rs"; v2 is unreachable on wasm (only the out-of-scope WasmServerCore exists there). No discover/envelope injection is added anywhere for wasm; wasm_core.rs is untouched; the wasm build just stays green.

From Plan 03: ServerDiscoverRequest + InternalClientRequest::ServerDiscover + classify_internal_method; error_codes::METHOD_NOT_FOUND (-32601)
From Plan 04: extra/protocol_context era available at dispatch; is_v2_opted_in(); resolved ProtocolContext threaded (do NOT re-resolve)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server/discover via internal dispatch — v2 capability projection incl. extensions; v1 → -32601</name>
  <files>src/shared/protocol_helpers.rs, src/server/core.rs, src/server/mod.rs</files>
  <read_first>
    - src/shared/protocol_helpers.rs (parse_request 15-31; classify_internal_method from Plan 03 — the interception point BEFORE public-enum conversion)
    - src/server/core.rs (dispatch match around 1125-1148; capabilities at 461/1045; success_response boundary at ~1134; handle_request entry at 973 where method string + resolved context are available)
    - src/server/mod.rs (dispatch around 1262; InitializeResult construction)
    - src/types/capabilities.rs (ServerCapabilities incl. extensions:109)
    - .planning/phases/112-version-plumbing-spine/112-CONTEXT.md (D-09, D-10)
  </read_first>
  <behavior>
    - A v2 (era==V2) server/discover request returns a result carrying the same capabilities projection the server computed at build (tools/prompts/resources/extensions), read-only, WITHOUT initialize side effects (no is_initialized mutation)
    - The discover result includes the extensions map when the server populated it via .with_extension
    - A v1-era (or non-opted-in) server/discover request returns JSON-RPC error -32601 method-not-found (D-10) — via the existing parse_request method-not-found path, no new code needed for the reject
    - discover does not recompute capabilities — it projects self.capabilities
    - The discover WIRE SHAPE is produced only through an internal conversion function (isolated, so a final-spec change stays localized)
  </behavior>
  <action>
    Wire the crate-private interception (Reviewer concern Codex HIGH #4): where the server converts an incoming request, consult `classify_internal_method(method, params)` (Plan 03) BEFORE/around the public `parse_request` conversion. When it returns `Some(InternalClientRequest::ServerDiscover(..))` AND the resolved `ProtocolContext.era == Era::V2` (from Plan 04, threaded — do NOT re-resolve `_meta`), route to `handle_discover()`. When `classify_internal_method` matches but era is V1 / the server is non-opted-in, do NOT special-case: let it fall through to the existing `parse_request` → `Error::method_not_found` → `-32601` (D-10 for free). Apply the same interception at the mod.rs twin for parity. Factor the interception into a shared helper both native dispatch sites call (one implementation, not per-site copies). NOTE: on the HTTP transport, Plan 06's v2 header enforcement still applies to discover (it gates on the resolved v2 signal regardless of method) — discover does not bypass headers. (wasm: core.rs is excluded from wasm32 and v2 is unreachable there, so no discover routing is added for wasm; wasm_core.rs is untouched — objective scope note.)

    Add `handle_discover()` (read-only) that projects `self.capabilities` (already-computed `ServerCapabilities`, incl. `extensions`) into a discover result — do NOT recompute and do NOT trigger any initialize-style side effect (no `is_initialized()` mutation). Define the discover result shape as an additive serde struct (camelCase, `#[non_exhaustive]`), carrying at minimum the capability projection + serverInfo, and PRODUCE it only via an isolated internal conversion fn (Reviewer concern Codex MEDIUM "server/discover wire shape is provisional") so a final-spec adjustment is localized. Reuse existing `ServerCapabilities`/`Implementation` types — do NOT invent parallel capability types. Add a golden fixture snapshot of the discover result so a wire-shape change is caught.

    Tests: a v2 server/discover returns capabilities including a `.with_extension`-populated key; a v1 server/discover returns `-32601`; assert discover does not flip initialization state; a golden-fixture assertion on the discover wire shape.
  </action>
  <verify>
    <automated>cargo test --lib server_discover</automated>
  </verify>
  <acceptance_criteria>
    - protocol_helpers.rs / core.rs route `server/discover` via `classify_internal_method` (grep shows the interception), NOT via a public `ClientRequest::ServerDiscover` variant
    - core.rs contains a `handle_discover`-style projection reading `self.capabilities`, gated on `Era::V2`, produced via an isolated internal conversion fn
    - A test proves v2 server/discover returns the extensions map populated via `.with_extension`
    - A test proves v1/non-opted-in server/discover returns error code -32601
    - A test proves discover does NOT mutate initialization state (no side effects)
    - A golden fixture pins the discover wire shape
    - `cargo build --lib` and `cargo build --lib --target wasm32-unknown-unknown` exit 0 (wasm_core.rs untouched; v2 unreachable on the wasm build)
  </acceptance_criteria>
  <done>server/discover routes via crate-private internal dispatch, projects computed capabilities incl. extensions on v2 only; v1 gets -32601; no recomputation/side effects; wire shape isolated + golden-pinned; parity across native sites via a shared helper; wasm_core.rs out of scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resultType envelope (v2-only) + serverInfo on v2 object results; v1 byte-identical (golden-fixtured)</name>
  <files>src/server/core.rs, src/server/mod.rs</files>
  <read_first>
    - src/server/core.rs (success_response serialization boundary ~1134; extra chain has protocol_context from Plan 04)
    - src/server/mod.rs (InitializeResult construction at 1262; serverInfo field)
    - .planning/phases/112-version-plumbing-spine/112-CONTEXT.md (D-07, D-08)
    - .planning/phases/112-version-plumbing-spine/112-RESEARCH.md (Pitfall 7 — v1 fixture churn)
  </read_first>
  <behavior>
    - A v2 SUCCESS response whose result is a JSON OBJECT carries "resultType":"complete" (inside the inner result object), unless the handler already supplied a resultType/disposition (then that value is preserved — the 113/114 path)
    - A v1 response carries NO resultType key and is byte-identical to today's output (golden-fixtured)
    - A missing resultType is interpreted as complete by readers (default)
    - v2 OBJECT results carry serverInfo; v1 results do not gain it
    - Non-object v2 results (scalar/array/null) are left unchanged (documented — no key can be added to a non-object; in-scope v2 methods return objects); error responses and notifications (no result) get NO injection
  </behavior>
  <action>
    **Answered-with-rationale (LOCKED D-07/D-08 stands):** `resultType`/`serverInfo` are injected at the era-gated serialization boundary (NOT added as public fields on Result structs). This is a deliberate decision — handlers keep returning today's types (semver-safe, zero public-API churn), and v1 stays byte-identical. Codex's "underspecified" concern is resolved by pinning the envelope model below, NOT by moving the mechanism.

    Define an INTERNAL `ResponseDisposition` enum (`Complete`/`InputRequired`/`Task`) plus (if useful) a `ResultType` serde tag — NOT a public field on any Result struct (D-08). This phase only emits `Complete`; the ENUM PLUS the injection point that READS a per-response disposition (defaulting to `Complete`) establishes the concrete PATH Phases 113/114 use to select `InputRequired`/`Task` (Reviewer concern Codex HIGH #5 — "no mechanism by which 113/114 select the discriminator"): the serialization layer reads a disposition value threaded with the response, so a later phase sets it at dispatch without touching this envelope code.

    Envelope model (pin every case — Codex HIGH #5):
      - era==V2 AND result is a JSON object → inject `"resultType"` (from the disposition, `complete` this phase) into the INNER result object (per Gemini finding #1 — inside `result`, not at the top level), UNLESS the object already carries `resultType` (respect the handler's value; do NOT overwrite). Also attach `serverInfo` on the v2 object result.
      - era==V2 AND result is scalar/array/null → leave unchanged (cannot key a non-object; documented — no in-scope v2 method returns a non-object).
      - Error responses / notifications (no `result`) → NO injection.
      - era==V1 (any) → emit nothing new; byte-identical.
    Factor the injection into a shared helper BOTH native dispatch sites (core.rs, mod.rs) call — one implementation, not per-site copies. (wasm: core.rs is excluded from wasm32 and v2 is unreachable there, so no injection is added for wasm; `wasm_core.rs` is a separate standalone impl and OUT OF SCOPE — objective scope note.)

    Do NOT add `resultType` to handler-facing Result types; handlers keep returning today's types.

    Tests: serialize a v2 object response and assert inner-result `resultType == "complete"` + a `serverInfo` object; serialize a v2 response whose handler already set a disposition and assert it is preserved; serialize a v2 scalar/null result and assert unchanged; serialize a v1 response and assert NO `resultType`/serverInfo. Add GOLDEN WIRE FIXTURES (Reviewer concern Codex MEDIUM "v1 byte-identity claims need stronger verification"): pin a v1 golden serialized response for at least a representative success op AND an error/task-pending response, asserting byte-identity; pin a v2 golden for the same op showing the added resultType/serverInfo.
  </action>
  <verify>
    <automated>cargo test --lib result_type_envelope</automated>
  </verify>
  <acceptance_criteria>
    - An internal `ResponseDisposition` (Complete/InputRequired/Task) exists; it is NOT a public field on any Result struct (grep shows no `pub result_type:`/`pub disposition:` added to Result structs); the serialization layer READS a threaded disposition (not just defines the enum)
    - A test asserts a v2 object response has inner-result `"resultType":"complete"` + a serverInfo object; a handler-set disposition is preserved (not overwritten)
    - A test asserts a v2 scalar/null result is unchanged and error/notification responses get no injection
    - Golden fixtures pin v1 byte-identity (success + error/task-pending) AND the v2 envelope
    - The injection lives in a shared helper both native dispatch sites (core.rs, mod.rs) call (grep shows one helper, not per-site copies)
    - Existing v1 response/snapshot tests remain green: `cargo test --lib server::` exits 0; `cargo build --lib --target wasm32-unknown-unknown` exits 0 (wasm branch unchanged; wasm_core.rs untouched)
  </acceptance_criteria>
  <done>resultType + serverInfo are injected v2-only at serialization (object results only, collision-safe, error/notification-excluded) via an internal disposition the 113/114 path sets, factored into one shared helper both native sites call; v1 output is byte-identical (golden-fixtured); handler Result types untouched; wasm_core.rs out of scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v2 client → server/discover | Read-only capability projection; must not leak initialize side effects |
| serialization layer → v1 consumers | v1 wire output must not change (strict v1 consumers) — golden-fixtured |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-10 | Information Disclosure | `server/discover` capability projection | mitigate | Projection is read-only over the SAME capabilities computed at build (no recompute → no drift/over-disclosure); no auth-gated data added; discover has no side effects |
| T-112-07 | Tampering (compat) | v1 response serialization | mitigate | resultType + serverInfo injected ONLY when era==V2 into OBJECT results (D-07); v1 output byte-identical; golden v1 fixtures (success + error/task-pending) guard regression (Pitfall 7) |
| T-112-04b | Spoofing | v1 request reaching a v2-only method | mitigate | server/discover era-gated to V2 via internal dispatch; V1/non-opted-in → -32601 through the existing method-not-found path, clean era separation (D-10) |
</threat_model>

<verification>
- `cargo test --lib server_discover` and `cargo test --lib result_type_envelope` green
- `cargo test --lib server::` green (v1 golden fixtures unchanged)
- `cargo build --lib` and `cargo build --lib --target wasm32-unknown-unknown` clean (wasm branch unchanged; wasm_core.rs untouched)
- Phase-end gate (Plan 07/08): `cargo semver-checks check-release` classifies minor; `make quality-gate` green
</verification>

<success_criteria>
- v2 server/discover (internal dispatch) projects capabilities incl. extensions; v1 → -32601; wire shape isolated + golden-pinned
- v2 object responses carry resultType:"complete" + serverInfo (collision-safe, non-object/error excluded, 113/114 path established) via one shared helper both native sites call; v1 responses byte-identical (golden); wasm_core.rs out of scope
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-05-SUMMARY.md` when done
</output>
</output>
