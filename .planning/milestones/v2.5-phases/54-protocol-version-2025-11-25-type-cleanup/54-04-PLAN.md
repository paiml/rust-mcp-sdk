---
phase: 54-protocol-version-2025-11-25-type-cleanup
plan: 04
type: execute
wave: 3
depends_on: [54-01, 54-02, 54-03]
files_modified:
  - tests/streamable_http_integration.rs
  - tests/notification_properties.rs
  - examples/17_completable_prompts.rs
  - examples/18_resource_watcher.rs
  - examples/mcp-apps-chess/src/main.rs
  - examples/mcp-apps-dataviz/src/main.rs
  - examples/mcp-apps-map/src/main.rs
  - cargo-pmcp/src/templates/mcp_app.rs
  - MIGRATION.md
autonomous: true
requirements: [TYPE-CLEANUP]

must_haves:
  truths:
    - "All examples using `pmcp::types::protocol::X` compile with updated imports"
    - "All integration tests compile and pass with updated imports"
    - "cargo-pmcp template strings use flat import path"
    - "MIGRATION.md documents every breaking change with find-and-replace format"
    - "`cargo build --examples` succeeds"
    - "`cargo check --workspace` succeeds"
    - "All existing tests pass after workspace-wide import cleanup"
  artifacts:
    - path: "MIGRATION.md"
      provides: "Find-and-replace guide for all breaking changes from v1.x to v2.0"
      min_lines: 100
  key_links:
    - from: "examples/18_resource_watcher.rs"
      to: "src/types/content.rs"
      via: "updated import from pmcp::types:: (flat)"
      pattern: "use pmcp::types::"
    - from: "cargo-pmcp/src/templates/mcp_app.rs"
      to: "pmcp::types::"
      via: "template strings with updated import path"
      pattern: "pmcp::types::"
---

<objective>
Fix all external consumer import paths (examples, tests, workspace crates) and write MIGRATION.md.

Purpose: Plan 03 fixed all internal src/ files. This plan completes the cleanup by fixing the remaining 8 consumer files outside src/ and producing the comprehensive migration guide. After this plan, the entire workspace compiles and the v2.0 breaking changes are fully documented.

Output: All workspace files compile cleanly, MIGRATION.md at repository root
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-CONTEXT.md
@.planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-RESEARCH.md
@.planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-01-SUMMARY.md
@.planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-02-SUMMARY.md
@.planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-03-SUMMARY.md

<interfaces>
<!-- External files using pmcp::types::protocol:: (from research grep) -->

External (pmcp::types::protocol::) -- 8 files:
- examples/mcp-apps-map/src/main.rs
- examples/mcp-apps-dataviz/src/main.rs
- examples/mcp-apps-chess/src/main.rs
- examples/18_resource_watcher.rs
- examples/17_completable_prompts.rs
- cargo-pmcp/src/templates/mcp_app.rs
- tests/streamable_http_integration.rs
- tests/notification_properties.rs

Breaking changes to document in MIGRATION.md:
- Import path: pmcp::types::protocol:: -> pmcp::types::
- 11 removed type aliases
- IncludeContext variant renames (All -> AllServers, ThisServerOnly -> ThisServer)
- ToolInfo.execution type change (Value -> ToolExecution)
- LogMessageParams.level type change (LogLevel -> LoggingLevel)
- SamplingMessage.content type change (Content -> SamplingMessageContent)
- Elicitation API complete replacement
- New required Option fields on Implementation, ResourceInfo, PromptInfo, ToolInfo, ResourceTemplate
- Version negotiation changes
- New types added
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix external consumer imports and struct literals</name>
  <files>
    tests/streamable_http_integration.rs
    tests/notification_properties.rs
    examples/17_completable_prompts.rs
    examples/18_resource_watcher.rs
    examples/mcp-apps-chess/src/main.rs
    examples/mcp-apps-dataviz/src/main.rs
    examples/mcp-apps-map/src/main.rs
    cargo-pmcp/src/templates/mcp_app.rs
  </files>
  <read_first>
    tests/streamable_http_integration.rs
    tests/notification_properties.rs
    examples/17_completable_prompts.rs
    examples/18_resource_watcher.rs
    examples/mcp-apps-chess/src/main.rs
    examples/mcp-apps-dataviz/src/main.rs
    examples/mcp-apps-map/src/main.rs
    cargo-pmcp/src/templates/mcp_app.rs
  </read_first>
  <action>
Fix all import paths in files outside src/ that reference `pmcp::types::protocol::`.

**Strategy:** Replace all occurrences of `pmcp::types::protocol::` with `pmcp::types::` (for examples, tests, and external crates).

**External files:**

1. `examples/17_completable_prompts.rs` -- `pmcp::types::protocol::{Content, ...}`
   Change to: `pmcp::types::{Content, ...}`

2. `examples/18_resource_watcher.rs` -- `pmcp::types::protocol::{Content, ...}`
   Change to: `pmcp::types::{Content, ...}`

3. `examples/mcp-apps-chess/src/main.rs` -- `pmcp::types::protocol::Content`
   Change to: `pmcp::types::Content`

4. `examples/mcp-apps-dataviz/src/main.rs` -- `pmcp::types::protocol::Content`
   Change to: `pmcp::types::Content`

5. `examples/mcp-apps-map/src/main.rs` -- `pmcp::types::protocol::Content`
   Change to: `pmcp::types::Content`

6. `cargo-pmcp/src/templates/mcp_app.rs` -- template strings containing `pmcp::types::protocol::`
   Update template strings to use `pmcp::types::` path

7. `tests/streamable_http_integration.rs` -- may reference `pmcp::types::protocol::`
   Change to `pmcp::types::`

8. `tests/notification_properties.rs` -- may reference `pmcp::types::protocol::`
   Change to `pmcp::types::`

**After all import fixes, check for struct literal breakage outside src/:**

Search tests/, examples/, crates/, cargo-pmcp/ for struct literals of types that gained new fields in Plan 02:
- `ResourceInfo {` -- needs `title: None, icons: None, annotations: None`
- `PromptInfo {` -- needs `title: None, icons: None, meta: None`
- `Implementation {` -- needs `title: None, website_url: None, description: None, icons: None`
- `ResourceTemplate {` -- needs `title: None, icons: None, annotations: None, meta: None`
- `ToolInfo` constructors -- needs `title: None, icons: None`

Fix ALL occurrences by adding the new Option fields as `None`.

**Also update references to renamed/removed types in these files:**
- `IncludeContext::All` -> `IncludeContext::AllServers`
- `IncludeContext::ThisServerOnly` -> `IncludeContext::ThisServer`
- Old alias names (MessageContent, CallToolParams, etc.) -> canonical names
- `SamplingMessage` content field -- if any test constructs SamplingMessage, update content to use SamplingMessageContent

**Verify compilation:**
Run `cargo check --workspace` and `cargo build --examples`.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo check --workspace && cargo build --examples 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 external consumer files updated with correct import paths, workspace compiles, examples build</done>
</task>

<task type="auto">
  <name>Task 2: Write MIGRATION.md</name>
  <files>
    MIGRATION.md
  </files>
  <read_first>
    src/lib.rs
    .planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-RESEARCH.md
    .planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-CONTEXT.md
  </read_first>
  <action>
Create `MIGRATION.md` at the repository root with a structured find-and-replace guide covering every breaking change from v1.x to v2.0.

```markdown
# Migration Guide: pmcp v1.x to v2.0

## Overview

pmcp v2.0 upgrades to MCP protocol 2025-11-25 with a major type system cleanup.
This is a semver major release with breaking changes.

## Import Path Changes

All types previously in `pmcp::types::protocol::` are now in domain sub-modules
with re-exports from `pmcp::types::`. The recommended import pattern is:

```rust
// v2.0 (recommended)
use pmcp::types::{ToolInfo, Content, ResourceInfo};

// v1.x (no longer works)
use pmcp::types::protocol::{ToolInfo, Content, ResourceInfo};
```

### Find and Replace

| Search | Replace |
|--------|---------|
| `pmcp::types::protocol::` | `pmcp::types::` |
| `crate::types::protocol::` | `crate::types::` |

## Removed Type Aliases

These aliases have been removed. Use the canonical name instead.

| Removed Alias | Use Instead |
|---------------|-------------|
| `InitializeParams` | `InitializeRequest` |
| `ListToolsParams` | `ListToolsRequest` |
| `CallToolParams` | `CallToolRequest` |
| `ListPromptsParams` | `ListPromptsRequest` |
| `GetPromptParams` | `GetPromptRequest` |
| `ListResourcesParams` | `ListResourcesRequest` |
| `ReadResourceParams` | `ReadResourceRequest` |
| `CancelledParams` | `CancelledNotification` |
| `Progress` | `ProgressNotification` |
| `MessageContent` | `Content` |
| `CreateMessageRequest` | `CreateMessageParams` |

## Renamed Enum Variants

| Type | Old Variant | New Variant | Wire Format |
|------|-------------|-------------|-------------|
| `IncludeContext` | `All` | `AllServers` | `"allServers"` |
| `IncludeContext` | `ThisServerOnly` | `ThisServer` | `"thisServer"` |

## Changed Field Types

| Struct | Field | Old Type | New Type |
|--------|-------|----------|----------|
| `ToolInfo` | `execution` | `Option<Value>` | `Option<ToolExecution>` |
| `LogMessageParams` | `level` | `LogLevel` | `LoggingLevel` |
| `SamplingMessage` | `content` | `Content` | `SamplingMessageContent` |

## New Required Fields (on existing structs)

These structs gained new `Option<T>` fields. If you construct them with struct
literal syntax, add the new fields:

### `Implementation`
```rust
// v1.x
Implementation { name: "my-server".into(), version: "1.0".into() }

// v2.0
Implementation {
    name: "my-server".into(),
    title: None,
    version: "1.0".into(),
    website_url: None,
    description: None,
    icons: None,
}
```

### `ResourceInfo`
```rust
// Add: title: None, icons: None, annotations: None
```

### `ResourceTemplate`
```rust
// Add: title: None, icons: None, annotations: None, meta: None
```

### `PromptInfo`
```rust
// Add: title: None, icons: None, meta: None
```

### `ToolInfo`
```rust
// Add: title: None, icons: None
```

## Elicitation API (Complete Replacement)

The elicitation API has been completely rewritten to match MCP spec 2025-11-25.

| v1.x (PMCP Proprietary) | v2.0 (MCP Spec) |
|--------------------------|-----------------|
| `ElicitInputRequest` | `ElicitRequestParams` (form or url mode) |
| `ElicitInputResponse` | `ElicitResult` (accept/decline/cancel) |
| `InputType` (16 variants) | JSON Schema `requested_schema` |
| `InputValidation` | Standard JSON Schema validation |
| `SelectOption` | JSON Schema enum type |
| `ElicitInputBuilder` | Construct `ElicitRequestParams` directly |
| `ServerRequest::ElicitInput` | `ServerRequest::ElicitationCreate` |
| `ClientRequest::ElicitInputResponse` | Removed (result is JSON-RPC response) |

### Method Name Change
```
// v1.x wire format
{"method": "elicitation/elicitInput", ...}

// v2.0 wire format
{"method": "elicitation/create", ...}
```

## Sampling API Changes

### SamplingMessage Content Expansion

`SamplingMessage.content` changed from `Content` to `SamplingMessageContent`,
which is an enum supporting text, image, audio, tool_use, and tool_result variants.

### New Type: CreateMessageResultWithTools

`CreateMessageResultWithTools` extends `CreateMessageResult` with array content
that can include tool use and tool result items for multi-turn interactions.

### CreateMessageParams Expansion

`CreateMessageParams` gained two new optional fields:
- `tools: Option<Vec<ToolInfo>>` -- tool definitions for the model
- `tool_choice: Option<ToolChoice>` -- tool selection mode

## Version Negotiation

| Constant | v1.x | v2.0 |
|----------|------|------|
| `LATEST_PROTOCOL_VERSION` | `"2025-06-18"` | `"2025-11-25"` |
| `SUPPORTED_PROTOCOL_VERSIONS` | 4 versions (incl. 2024) | 3 versions (2025 only) |

Unsupported versions now return `LATEST_PROTOCOL_VERSION` instead of
`DEFAULT_PROTOCOL_VERSION`.

## New Types (2025-11-25)

### Content
- `Content::Audio` -- audio content with base64 data
- `Content::ResourceLink` -- resource link with metadata
- `Annotations` -- content annotation metadata

### Tasks
- `Task`, `TaskStatus`, `TaskCreationParams`, `RelatedTaskMetadata`
- `CreateTaskResult`, `GetTaskRequest`, `GetTaskResult`
- `GetTaskPayloadRequest`, `ListTasksRequest`, `ListTasksResult`
- `CancelTaskRequest`, `CancelTaskResult`, `TaskStatusNotification`
- `RELATED_TASK_META_KEY` constant

### Tools
- `ToolExecution`, `TaskSupport` -- typed task support declaration

### Sampling
- `ToolChoice`, `ToolChoiceMode` -- tool selection for sampling
- `ToolUseContent`, `ToolResultContent` -- tool use in sampling messages
- `SamplingMessageContent` -- expanded content union for sampling
- `CreateMessageResultWithTools` -- sampling result with tool use array content

### Protocol
- `IconInfo`, `IconTheme` -- icon metadata for entities
- `ProtocolErrorCode` -- MCP-specific JSON-RPC error codes

### Elicitation
- `ElicitRequestParams` (form/url), `ElicitResult`, `ElicitAction`
- `ElicitationCompleteNotification`

### Capabilities
- `ServerTasksCapability`, `ClientTasksCapability`
- `FormElicitationCapability`
- Expanded `ElicitationCapabilities` (form + url)
- Expanded `SamplingCapabilities` (context + tools)

### Logging
- `LoggingLevel` expanded: added `Notice`, `Alert`, `Emergency`
```

**Verify full workspace compilation after creating MIGRATION.md:**

Run `cargo check --workspace`, `cargo build --examples`, `cargo test --lib -p pmcp`, `cargo clippy -p pmcp -- -D warnings`, `cargo fmt --all -- --check`.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && test -f MIGRATION.md && wc -l MIGRATION.md | awk '{print ($1 >= 100) ? "PASS: "$1" lines" : "FAIL: only "$1" lines"}'</automated>
  </verify>
  <done>MIGRATION.md at repository root with 100+ lines documenting every breaking change in find-and-replace format</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full quality check:
1. `cargo check --workspace` -- all crates compile
2. `cargo build --examples` -- all examples build
3. `cargo test --lib -p pmcp` -- all SDK tests pass
4. `cargo clippy -p pmcp -- -D warnings` -- no clippy warnings
5. `cargo fmt --all -- --check` -- formatting correct
6. MIGRATION.md exists and documents all breaking changes
7. No remaining `types::protocol::` import paths in source (only in MIGRATION.md docs)
</verification>

<success_criteria>
- Zero compilation errors across the entire workspace
- Zero remaining references to `types::protocol::` in Rust source files (outside MIGRATION.md)
- MIGRATION.md at repository root with comprehensive find-and-replace guide (100+ lines)
- All existing tests pass, no regression
- All examples build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/54-protocol-version-2025-11-25-type-cleanup/54-04-SUMMARY.md`
</output>
