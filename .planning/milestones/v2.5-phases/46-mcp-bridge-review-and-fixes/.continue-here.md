---
phase: 46-mcp-bridge-review-and-fixes
task: 4
total_tasks: 4
status: completed
last_updated: 2026-03-11T23:43:35.227Z
---

<current_state>
Phase 46 MCP Apps cross-host compatibility fixes are complete and committed (889dd4f).
All three hosts (ChatGPT, Claude.ai, mcp-preview) confirmed working.
</current_state>

<completed_work>

- Task 1: Remove McpAppsAdapter bridge injection for Vite-bundled widgets (prevents dual-App postMessage conflict)
- Task 2: Fix ChatGPT compatibility — remove tools capability from App constructor (ChatGPT Zod rejects it), switch imports from CDN to npm
- Task 3: Fix Claude.ai/Claude Desktop — add required protocol handlers, add WidgetCSP with Flickr CDN domains, add fetch-to-blob image loading with revokeObjectURL
- Task 4: Update GUIDE.md with CSP configuration, external images pattern, capabilities warnings, cross-host debugging
</completed_work>

<remaining_work>

- No remaining tasks in phase 46
- Open-images widgets deployed and working on all three hosts
- Next phases (28-32) of v1.6 CLI DX Overhaul are not started
</remaining_work>

<decisions_made>

- Do NOT pass tools capability to new App() — ChatGPT rejects both true and {} via Zod validation
- Use _meta.ui.csp with resourceDomains/connectDomains for external image domains (spec-compliant)
- Keep fetch-to-blob as defense-in-depth alongside CSP declarations
- Use URL.revokeObjectURL() in img.onload to prevent blob URL memory leaks
- Protocol handlers (onteardown, ontoolinput, ontoolcancelled) can be no-ops but MUST be registered before connect()
</decisions_made>

<blockers>
None — all blockers resolved.
</blockers>

<context>
Key learning: MCP Apps must work across three very different host environments:
1. mcp-preview — most permissive, good for dev but masks issues
2. ChatGPT — strict Zod validation on appCapabilities, has its own adapter layer
3. Claude.ai/Claude Desktop — strict CSP on iframes, requires _meta.ui.csp declarations and ALL protocol handlers
The GUIDE.md is now comprehensive with all these learnings documented.
</context>

<next_action>
Phase 46 is complete. Next work would be phases 28-32 (v1.6 CLI DX Overhaul) or any new priorities.
</next_action>
