---
phase: 97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run
plan: 04
type: execute
wave: 4
depends_on: [97-03]
files_modified:
  - cargo-pmcp/docs/commands/github.md
  - cargo-pmcp/docs/commands/deploy.md
  - cargo-pmcp/README.md
  - cargo-pmcp/fuzz/fuzz_targets/fuzz_github_workflow.rs
  - cargo-pmcp/fuzz/Cargo.toml
autonomous: false
requirements: [GHDEP-01, GHDEP-02, GHDEP-03, GHDEP-04, GHDEP-05, GHDEP-06]
must_haves:
  truths:
    - "Docs clearly separate GitHub Actions deploy from pmcp.run connected-repository deploy"
    - "Property/fuzz coverage protects workflow rendering from malformed branch/environment strings"
    - "The future deploy-action is documented as a stable abstraction, not required for the initial generated workflow"
---

<objective>
Finish Phase 97 with documentation, examples, and robustness coverage for the
GitHub workflow renderer and auth model.
</objective>

<context>
@.planning/phases/97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run/97-CONTEXT.md
@.planning/phases/97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run/97-RESEARCH.md
@.planning/phases/97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run/97-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Document `cargo pmcp github`</name>
  <files>cargo-pmcp/docs/commands/github.md, cargo-pmcp/README.md</files>
  <read_first>
    - cargo-pmcp/docs/commands/deploy.md
    - cargo-pmcp/README.md
    - cargo-pmcp/src/commands/github/mod.rs
  </read_first>
  <action>
    Add `cargo-pmcp/docs/commands/github.md` documenting usage, options, target matrix, auth model, and examples. Include exact examples for `cargo pmcp github init --target-type pmcp-run --branch main --environment production`, `cargo pmcp github init --target-type aws-lambda --branch main`, and `cargo pmcp github init --target-type google-cloud-run --dry-run`. Update `cargo-pmcp/README.md` command list to include `github` with one sentence: `Generate GitHub Actions workflows for cargo-pmcp deployments.`
  </action>
  <verify>
    <automated>grep -n "cargo pmcp github init --target-type pmcp-run" cargo-pmcp/docs/commands/github.md && grep -n "Generate GitHub Actions workflows" cargo-pmcp/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo-pmcp/docs/commands/github.md` exists
    - Docs contain all three exact command examples listed in action
    - README contains `github` and `Generate GitHub Actions workflows`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document pmcp.run connected repo distinction</name>
  <files>cargo-pmcp/docs/commands/github.md, cargo-pmcp/docs/commands/deploy.md</files>
  <read_first>
    - cargo-pmcp/docs/commands/github.md
    - cargo-pmcp/docs/commands/deploy.md
    - .planning/phases/97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run/97-CONTEXT.md
  </read_first>
  <action>
    Add a section titled `## GitHub Actions vs pmcp.run connected repositories` to `github.md`. The section must state: GitHub Actions deploy runs in the user's GitHub runner; pmcp.run connected repository deploy runs in pmcp.run AWS infrastructure; non-pmcp.run cloud credentials stay in the user's cloud/GitHub account; pmcp.run connected repositories own service features such as deployment history, logs, rollback, and preview deployments. Add a short cross-link from `deploy.md` to `github.md`.
  </action>
  <verify>
    <automated>grep -n "GitHub Actions vs pmcp.run connected repositories" cargo-pmcp/docs/commands/github.md && grep -n "connected repository" cargo-pmcp/docs/commands/deploy.md</automated>
  </verify>
  <acceptance_criteria>
    - `github.md` contains `GitHub Actions deploy runs in the user's GitHub runner`
    - `github.md` contains `pmcp.run connected repository deploy runs in pmcp.run AWS infrastructure`
    - `github.md` contains `preview deployments`
    - `deploy.md` links to `github.md`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add property and fuzz coverage for workflow inputs</name>
  <files>cargo-pmcp/src/commands/github/workflow.rs, cargo-pmcp/fuzz/fuzz_targets/fuzz_github_workflow.rs, cargo-pmcp/fuzz/Cargo.toml</files>
  <read_first>
    - cargo-pmcp/src/commands/github/workflow.rs
    - cargo-pmcp/tests/property_tests.rs
    - cargo-pmcp/fuzz/fuzz_targets/fuzz_config_parse.rs
    - cargo-pmcp/fuzz/Cargo.toml
  </read_first>
  <action>
    Add property tests for branch and environment strings accepted by `WorkflowSpec` sanitization/validation. The invariant: rendered workflow output must not contain raw newline characters injected from branch or environment values except YAML line separators produced by the renderer. Add a fuzz target `fuzz_github_workflow.rs` that constructs random branch/environment strings, calls the validation path, and renders only when validation accepts input. Register the fuzz target in `cargo-pmcp/fuzz/Cargo.toml`.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp github_workflow_property && cargo fuzz run fuzz_github_workflow -- -runs=1000</automated>
  </verify>
  <acceptance_criteria>
    - `workflow.rs` contains a test name matching `github_workflow_property`
    - `cargo-pmcp/fuzz/fuzz_targets/fuzz_github_workflow.rs` exists
    - `cargo-pmcp/fuzz/Cargo.toml` contains `fuzz_github_workflow`
    - Fuzz target runs 1000 iterations without panic
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add deploy-action handoff notes</name>
  <files>cargo-pmcp/docs/commands/github.md</files>
  <read_first>
    - cargo-pmcp/docs/commands/github.md
    - .planning/phases/97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run/97-RESEARCH.md
  </read_first>
  <action>
    Add a section titled `## Future deploy action` explaining that a future `pmcp/deploy-action@v1` can wrap install/cache/auth/deploy steps after the generated workflow contract stabilizes. The section must explicitly state that Phase 97 does not require the action and that generated workflows remain plain YAML using `cargo pmcp deploy`.
  </action>
  <verify>
    <automated>grep -n "Future deploy action" cargo-pmcp/docs/commands/github.md && grep -n "plain YAML using `cargo pmcp deploy`" cargo-pmcp/docs/commands/github.md</automated>
  </verify>
  <acceptance_criteria>
    - `github.md` contains `pmcp/deploy-action@v1`
    - `github.md` contains `Phase 97 does not require the action`
    - `github.md` contains `plain YAML using \`cargo pmcp deploy\``
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cargo test -p cargo-pmcp github`
- `cargo fuzz run fuzz_github_workflow -- -runs=1000`
- `cargo fmt --all -- --check`
- `make quality-gate`
</verification>

<success_criteria>
- User-facing docs explain both GitHub deployment products
- Property/fuzz tests cover workflow input robustness
- The future GitHub Action is documented as a follow-up abstraction, not a blocker
</success_criteria>
