---
phase: 97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run
plan: 02
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - cargo-pmcp/src/main.rs
  - cargo-pmcp/src/commands/github/mod.rs
  - cargo-pmcp/src/commands/github/init.rs
  - cargo-pmcp/tests/cli_acceptance.rs
autonomous: false
requirements: [GHDEP-01, GHDEP-02, GHDEP-05]
must_haves:
  truths:
    - "`cargo pmcp github init` writes `.github/workflows/pmcp-deploy.yml` idempotently"
    - "`--dry-run` prints workflow YAML and writes no files"
    - "Existing workflow files are not overwritten unless `--yes` is supplied"
---

<objective>
Wire the GitHub workflow renderer into the `cargo pmcp` CLI as a top-level
`github` command group with an `init` subcommand.
</objective>

<context>
@.planning/phases/97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run/97-CONTEXT.md
@.planning/phases/97-github-deploy-automation-for-cargo-pmcp-and-pmcp-run/97-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add top-level `github` command group</name>
  <files>cargo-pmcp/src/main.rs, cargo-pmcp/src/commands/github/mod.rs</files>
  <read_first>
    - cargo-pmcp/src/main.rs
    - cargo-pmcp/src/commands/landing/mod.rs
    - cargo-pmcp/src/commands/deploy/mod.rs
  </read_first>
  <action>
    Add a `Github { command: commands::github::GithubCommand }` variant to the top-level `Commands` enum in `main.rs` with help text `Generate and validate GitHub Actions deployment workflows`. Implement `GithubCommand` in `commands/github/mod.rs` as a clap subcommand enum with `Init(init::InitCommand)`. Wire dispatch through `dispatch_trait_based` or the existing explicit dispatch pattern so `GithubCommand::execute(&GlobalFlags)` runs.
  </action>
  <verify>
    <automated>cargo run -p cargo-pmcp -- pmcp github --help</automated>
  </verify>
  <acceptance_criteria>
    - `cargo-pmcp/src/main.rs` contains `Github`
    - `cargo-pmcp/src/commands/github/mod.rs` contains `pub enum GithubCommand`
    - `cargo run -p cargo-pmcp -- pmcp github --help` exits 0 and output contains `init`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `github init` file behavior</name>
  <files>cargo-pmcp/src/commands/github/init.rs, cargo-pmcp/src/commands/github/mod.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/github/workflow.rs
    - cargo-pmcp/src/commands/deploy/mod.rs
    - cargo-pmcp/src/commands/configure/workspace.rs
  </read_first>
  <action>
    Create `init.rs` with `InitCommand` args: `--target-type <TARGET>` required, `--branch <BRANCH>` default `main`, `--environment <ENV>` optional, `--output <PATH>` optional default `.github/workflows/pmcp-deploy.yml`, `--dry-run`, `--yes`, and `--locked` default true. Resolve the project root with the same workspace/deploy-root helper style used by deploy commands. If `--dry-run` is set, print rendered YAML to stdout and do not create directories or files. If the output file exists and `--yes` is false, return an error containing `already exists`. If `--yes` is true, overwrite with rendered YAML. Always create the parent `.github/workflows` directory when writing.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp github_init</automated>
  </verify>
  <acceptance_criteria>
    - `InitCommand` has clap args for `target_type`, `branch`, `environment`, `output`, `dry_run`, `yes`, and `locked`
    - Unit test `github_init_dry_run_writes_no_files` passes
    - Unit test `github_init_refuses_existing_file_without_yes` passes
    - Unit test `github_init_overwrites_existing_file_with_yes` passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI acceptance coverage</name>
  <files>cargo-pmcp/tests/cli_acceptance.rs</files>
  <read_first>
    - cargo-pmcp/tests/cli_acceptance.rs
    - cargo-pmcp/src/commands/github/init.rs
  </read_first>
  <action>
    Add CLI acceptance tests that execute the binary in a temp project. Cover `cargo pmcp github init --target-type pmcp-run --dry-run`, `cargo pmcp github init --target-type aws-lambda --output .github/workflows/pmcp-deploy.yml --yes`, and an invalid target string. The dry-run assertion must verify stdout contains `id-token: write` for pmcp-run and that `.github/workflows/pmcp-deploy.yml` does not exist. The aws-lambda write assertion must verify the file exists and contains `--target-type aws-lambda`.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test cli_acceptance github</automated>
  </verify>
  <acceptance_criteria>
    - `cli_acceptance.rs` contains a test name matching `github_init_pmcp_run_dry_run`
    - `cli_acceptance.rs` contains a test name matching `github_init_aws_lambda_writes_workflow`
    - Invalid target test asserts stderr contains `unsupported GitHub deploy target` or the exact parser error string chosen in Task 1
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cargo test -p cargo-pmcp github_init`
- `cargo test -p cargo-pmcp --test cli_acceptance github`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
- The top-level command exists and is discoverable in help output
- `github init` can dry-run, refuse accidental overwrite, and write workflow YAML
- CLI tests prove pmcp-run and one non-pmcp-run target path
</success_criteria>
