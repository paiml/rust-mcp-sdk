# Phase 90.2: OpenAPI Built-In Server — Advanced Example (Contoso M365) - Research

**Researched:** 2026-05-30
**Domain:** Config-driven OpenAPI→MCP showcase (oauth_passthrough + Microsoft Graph Excel range-read), offline parity testing
**Confidence:** HIGH

## Summary

Phase 90.2 is a **pure config + curated-spec + fixture + docs + example/test-Rust** phase that mirrors the just-completed Phase 90.1 london-tube showcase. Every piece of machinery it needs — `AuthConfig::OAuthPassthrough`, the `TokenCaptureAuthProvider → AuthContext → HttpCodeExecutor → outbound forward` chain, single-call tool path/query parameter mapping, the wiremock offline parity harness, and the Code Mode `validate_code`/`execute_code` engine — already ships and is unit-tested. The phase **configures** that machinery for a fictional Contoso M365 instance; it touches **no `src/`**. [VERIFIED: codebase reads of auth.rs, assemble.rs, tools.rs, client.rs, code_mode.rs, parity_replay.rs, oauth_passthrough_e2e.rs]

All four research questions resolve cleanly with file:line evidence:

- **RQ-1 (passthrough proof) — RESOLVED, the YAML replay path CAN assert it.** `ServerTester::new`'s 4th argument `api_key: Option<&str>` injects `Authorization: Bearer <key>` into the streamable-HTTP transport's `extra_headers` on every MCP request (`crates/mcp-tester/src/tester.rs:96-101,154-162`). The server's `TokenCaptureAuthProvider` captures that inbound `Authorization` header into `AuthContext.token` (`crates/pmcp-openapi-server/src/assemble.rs:97-110`), and the passthrough provider forwards it to the Graph backend. So a parity test can pass `Some("contoso-user-tok")` to `ServerTester::new` and assert the wiremock Graph mock **required** `header("authorization", "Bearer contoso-user-tok")`. A standalone Rust integration test (the `oauth_passthrough_e2e.rs` pattern) is a viable belt-and-suspenders alternative but is **not required** — the replay path suffices.
- **RQ-2 (Code Mode result assertion) — `validate_code` only validates; `execute_code` runs+returns but requires an approval token.** A `validate_code` YAML step (london-tube's pattern) asserts `success` only, not a computed value. `execute_code` runs the script and returns the computed result (`crates/pmcp-server-toolkit/src/code_mode.rs:572-623`) **but verifies an HMAC `approval_token` + code-hash minted by `validate_code` first** — a two-call dance a flat YAML scenario can't thread. **Therefore P902-CODEMODE must be a Rust test** that drives the `JsCodeExecutor` directly over a token-threaded `HttpCodeExecutor` (the exact `oauth_passthrough_e2e.rs:63-72,99-103` seam) and asserts the deterministic customer set.
- **RQ-3 / RQ-4 (Graph range addressing + curated spec) — RESOLVED.** Graph's range-read URL is `GET /drives/{drive-id}/items/{item-id}/workbook/worksheets/{worksheet-id}/range(address='A1:D8')?$select=values` [CITED: learn.microsoft.com/graph/api/range-get]. The toolkit's single-call synthesizer makes every `{...}` path-template segment a **required path param** and every other declared param a **query param** (`crates/pmcp-server-toolkit/src/tools.rs:499-537`); path substitution is a literal `str::replace` with **no URL re-encoding** (`crates/pmcp-server-toolkit/src/http/client.rs:150-163`, `join_url` at `http/mod.rs:76`). Because the connector does **no arithmetic**, `customer_id`→row math cannot happen in the connector — so the demo's cleanest fit is a **per-tool literal path** where the worksheet name encodes the sheet and the range address is templated by a parameter (see Architecture Patterns).

**Primary recommendation:** Copy the 90.1 file set verbatim and re-skin it for Contoso. The single genuinely new design decision is how `get_customer(customer_id)` and `get_customer_orders(customer_id)` map a `customer_id` onto a Graph range address without connector arithmetic — resolve this by giving each tool a fixed worksheet-scoped path and making the **range address a templated path/query parameter the tool fills from `customer_id`**, OR (simpler, recommended) make each tool a **script tool** whose JS body templates the address (Code Mode CAN do arithmetic). The Rust Code Mode test (P902-CODEMODE) is non-negotiable per RQ-2.

## User Constraints (from CONTEXT.md)

### Locked Decisions
- **D-01 (workbook data model):** Lean business shape. **Customers** sheet columns: `customer_id`, `name`, `segment`, `region`. **Orders** sheet columns: `order_id`, `customer_id`, `order_date`, `amount`. Enough for the headline query AND richer Code Mode demos, while keeping the fixture small and deterministic.
- **D-02 (id→range addressing):** Simple sequential **row convention** — `customer_id` maps to a known row, and the tool templates the Excel range address from the id (e.g., `C007` → row = id index + header offset → `range(address='A8:D8')`). Smallest fixture, stays within the single range-read Graph op. Rejected the "realistic Excel-table lookup" alternative.
- **D-03 (`get_customer_orders`):** returns that customer's orders by reading an **addressable contiguous orders block** for the id. The Orders sheet must be laid out (grouped/sorted by `customer_id`) so each customer's orders form a contiguous, address-derivable range. Exact block addressing finalized in planning — keep it within range-read-only.
- **D-04 (no server-side filtering):** the connector is a thin HTTP mapper. All joining/filtering/aggregation (incl. "bought >100 in last 3 months") is Code Mode's job.
- **D-05 (docs depth):** Concise — config walkthrough + short governance note (1-2 paragraphs): admin consents once to a bounded scope; the signed-in user's forwarded token governs per-file access; the server holds no standing credentials. Match 90.1 chapter depth. NOT a Mermaid sequence diagram or a dedicated threat subsection.
- **D-06 (Contoso framing):** Lean into fictional **Contoso** org: named SharePoint site (e.g. "Sales"), workbook like `Customers.xlsx`, Microsoft-canonical fictional customer names (Northwind Traders, Fabrikam, Tailspin Toys).

### Claude's Discretion
- Exact wiremock response JSON shapes, range-address strings, and the curated-spec operationId/parameter names — within the decisions above.
- Number of sample customers/orders (enough for ≥1 match and ≥1 non-match on the headline query; a handful is fine).
- Chapter filenames / nav placement (suggested `openapi-graph-m365.md` in both books).

### Deferred Ideas (OUT OF SCOPE)
- Excel write-back / workbook-session — separate future spike.
- `@odata.nextLink` pagination — connector feature gap; not needed.
- `oauth2_client_credentials` (app-only) contrast example — passthrough is the only hero (≤1 doc sentence).
- Realistic Excel-table / named-range lookup addressing — chose the simple row convention (D-02).
- Full security deep-dive docs (Mermaid sequence + threat subsection) — chose the concise note (D-05).
- SharePoint/OneDrive file-discovery navigation workflow — workbook addressed by pinned ids.

## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| P902-SPEC | Curated/trimmed Graph OpenAPI spec for the Excel range-read op | `OpenApiSchema::parse` accepts a minimal OpenAPI 3.0 doc (`tests/fixtures/london-tube-api.yaml` is the template, 55 lines); path-templated params declared `in: path`/`in: query`. Graph URL shape confirmed (Architecture Patterns §Graph range op). |
| P902-CONFIG | `contoso-m365.toml` with `oauth_passthrough` + two id-keyed tools | `AuthConfig::OAuthPassthrough { target_header, required }` (`auth.rs:123-130`); TOML shape `[backend.auth] type = "oauth_passthrough"`. Exact decl field names confirmed (Code Examples §config). |
| P902-FIXTURE | Date-pinned Customers/Orders workbook mock data | wiremock `set_body_json` returns a Graph `workbookRange` shape `{ "values": [[...],[...]] }`; reference date embedded in fixture rows. No `Date` builtin in JS engine ⇒ determinism is structural (Pitfall 1). |
| P902-PARITY | Offline tool + forwarded-bearer passthrough test | **RQ-1 RESOLVED**: `ServerTester::new(url, .., Some("tok"), Some("http"), None)` injects inbound bearer; wiremock mock requires `header("authorization", "Bearer tok")`. Template: `parity_replay.rs` + `oauth_passthrough_e2e.rs`. |
| P902-CODEMODE | Replayed headline Code Mode workflow with deterministic result | **RQ-2 RESOLVED**: must be a **Rust test** (execute_code needs an approval token; YAML can't thread it). Drive `JsCodeExecutor::execute` over a token-threaded `HttpCodeExecutor` (`oauth_passthrough_e2e.rs:63-103`), assert the exact customer-id set. |
| P902-EXAMPLE | Pointable `examples/contoso-m365.toml` + `contoso_m365_min.rs` | Templates: `examples/london-tube.toml` + `examples/london_tube_min.rs` (build-and-exit via `dispatch` + `build_server`, returns without serving). `examples/` ships, `tests/` excluded (`Cargo.toml:13`). |
| P902-DOCS-BOOK | New book chapter wired into `pmcp-book/src/SUMMARY.md` | Sibling of `openapi-built-in-server.md` (SUMMARY.md:38). `make book` builds (`Makefile:420-427` → `cd pmcp-book && mdbook build`). |
| P902-DOCS-COURSE | New course chapter wired into `pmcp-course/src/SUMMARY.md` | Sibling of course `openapi-built-in-server.md` (SUMMARY.md:78). `cd pmcp-course && mdbook build`. |

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Per-user delegated auth (token capture) | MCP server (inbound auth) | — | `TokenCaptureAuthProvider` captures inbound `Authorization` → `AuthContext` (assemble.rs:93-115). Server-side; never client. |
| Token forwarding to Graph | Outbound HTTP auth provider | — | `OAuthPassthroughAuth::apply` forwards captured token to backend (auth.rs:415-462). Operator owns destination header. |
| `customer_id` → Excel range address | Code Mode script (JS) — recommended | Single-call tool path/query param | Connector does NO arithmetic (tools.rs/client.rs); address math belongs in the JS engine or is supplied as a literal param. |
| Row filtering / join / aggregation | Code Mode (`execute_code`) | — | D-04 locked: connector is a thin mapper; all business logic is Code Mode. |
| HTTP range-read execution | `HttpConnector` (single-call) / `HttpCodeExecutor` (Code Mode) | — | Both call Graph `range(address='...')`; same outbound auth. |
| Offline backend simulation | wiremock (test tier) | — | No live Graph in CI; pure-Rust mock asserts forwarded bearer + returns canned `workbookRange`. |

## Standard Stack

### Core (already in the workspace — CONFIGURE, do not add)
| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| `pmcp-openapi-server` | 0.1.0 (path) | The Shape-A binary under test; `run_serving`, `Args`, `dispatch`, `build_server` | [VERIFIED: Cargo.toml] The crate this phase ships fixtures/examples/tests for |
| `pmcp-server-toolkit` | path dep, `openapi-code-mode` feature | `AuthConfig`, `OpenApiSchema`, `ServerConfig::from_toml_strict_validated`, `HttpCodeExecutor`, `JsCodeExecutor`, `code_mode::*` | [VERIFIED] All passthrough + code-mode machinery |
| `mcp-tester` | 0.5.0 | `ServerTester`, `ScenarioExecutor`, `TestScenario` for YAML replay | [VERIFIED: parity_replay.rs:32] The 90.1 replay harness |
| `wiremock` | (dev-dep, as in parity_replay) | Offline HTTP mock for the Graph backend | [VERIFIED: parity_replay.rs:37-38] Pure-Rust, no Docker |
| `tokio` / `serde_json` / `tempfile` | (dev-deps) | async test runtime, JSON bodies, temp config files | [VERIFIED: parity_replay.rs:30-36,246] |

### Supporting
| Library | Version | Purpose | When to Use |
|---------|---------|---------|-------------|
| `pmcp-code-mode` | path | `CodeExecutor`, `JsCodeExecutor`, `ExecutionConfig`, `ValidationPipeline` (re-exported via toolkit) | The Rust Code Mode test (P902-CODEMODE) drives the toolkit's re-exported `JsCodeExecutor` to avoid a direct dep |

### Alternatives Considered
| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| YAML `execute_code` step for the headline query | Rust test driving `JsCodeExecutor` directly | `execute_code` needs an approval token from `validate_code`; YAML can't thread the token between steps (RQ-2). Rust test is mandatory for a computed-result assertion. |
| Single-call tools with templated range param | Script tools (JS body templates the address) | Script tools can do the `customer_id`→address arithmetic the connector can't (RQ-3); recommended for clean id→range mapping. |

**Installation:** No new crates. All deps already in `crates/pmcp-openapi-server/Cargo.toml`. [VERIFIED: Cargo.toml]

**Version verification:** Not applicable — no new external packages; all are in-workspace path deps already compiled in 90.1.

## Architecture Patterns

### System Architecture Diagram

```
MCP client (e.g. ChatGPT)                         OFFLINE TEST HARNESS
   │  Authorization: Bearer <user-tok>            (parity_replay / Rust code-mode test)
   ▼                                                       │
ServerTester.new(.., Some("<user-tok>"), Some("http"), ..) ◄── injects bearer into extra_headers
   │  MCP tools/call over streamable HTTP                  │ (tester.rs:96-101)
   ▼
pmcp-openapi-server  (run_serving → assemble::build_server)
   │
   ├─ TokenCaptureAuthProvider.validate_request(Some("Bearer <user-tok>"))
   │     → AuthContext{ token: Some("Bearer <user-tok>") }   (assemble.rs:97-110)
   │
   ├─ single-call tool (get_customer)  ──┐
   │     HttpToolHandler.handle           │  per-request executor re-derived
   │                                       │  from AuthContext (request_executor_from_extra)
   └─ Code Mode (execute_code)  ─────────┤
         JsCodeExecutor over              │
         HttpCodeExecutor.with_inbound_token
                                          ▼
              OAuthPassthroughAuth.apply(headers, query, inbound_token)
                 → sets Authorization: Bearer <user-tok> on OUTBOUND request   (auth.rs:415-462)
                                          │
                                          ▼
              GET {base}/drives/{drive}/items/{item}/workbook/worksheets/{ws}/range(address='A8:D8')?$select=values
                                          │
                                          ▼
              wiremock Graph mock  ── requires header("authorization","Bearer <user-tok>")  ◄── PASSTHROUGH PROOF
                 returns workbookRange JSON: { "values": [[ ...row... ]] }
```

### Recommended Project Structure (all NEW files mirror 90.1)
```
crates/pmcp-openapi-server/
├── tests/
│   ├── fixtures/
│   │   ├── contoso-m365.toml            # P902-CONFIG (NOT shipped — tests/ excluded)
│   │   ├── contoso-m365-api.yaml        # P902-SPEC curated Graph spec
│   │   └── contoso-m365-scenarios.yaml  # P902-PARITY tool-correctness scenario
│   ├── contoso_m365_parity.rs           # P902-PARITY (mirror parity_replay.rs)
│   └── contoso_m365_code_mode.rs        # P902-CODEMODE (mirror oauth_passthrough_e2e.rs; #![cfg(feature="openapi-code-mode")])
├── examples/
│   ├── contoso-m365.toml                # P902-EXAMPLE pointable config (SHIPPED)
│   └── contoso_m365_min.rs              # P902-EXAMPLE build-and-exit
pmcp-book/src/openapi-graph-m365.md      # P902-DOCS-BOOK (+ SUMMARY.md entry)
pmcp-course/src/openapi-graph-m365.md    # P902-DOCS-COURSE (+ SUMMARY.md entry)
```

### Pattern 1: oauth_passthrough backend config (P902-CONFIG)
**What:** Replace 90.1's `[backend.auth] type = "api_key"` with passthrough.
**When to use:** The whole phase.
```toml
# Source: AuthConfig::OAuthPassthrough — auth.rs:123-130 (target_header defaults to "Authorization", required defaults true)
[backend]
base_url = "https://graph.microsoft.com/v1.0"

[backend.auth]
type = "oauth_passthrough"
# target_header optional (defaults "Authorization"); required optional (defaults true).
# For the offline parity test, leaving required = true is fine — the test supplies the bearer.
target_header = "Authorization"
required = true
```
The `[code_mode]` block keeps the same dev-guarded inline secret pattern as 90.1 so the offline test needs no extra env wiring:
```toml
[code_mode]
enabled = true
token_secret = "contoso-m365-parity-dev-secret-32bytes"
allow_inline_token_secret_for_dev = true
```

### Pattern 2: id-keyed tool → Graph range address (RQ-3) — two viable shapes
**RECOMMENDED — script tool (JS templates the address; connector does no arithmetic):**
```toml
# get_customer: customer_id → row → range(address='A{row}:D{row}'). The JS engine
# does the arithmetic (Math.* is available; build the address string, then api.get).
[[tools]]
name = "get_customer"
description = "Fetch one customer row (customer_id, name, segment, region) from the Customers sheet."
script = """
const idx = parseInt(args.customer_id.slice(1), 10);   // 'C007' → 7
const row = idx + 1;                                    // header offset (row 1 = header)
const addr = `A${row}:D${row}`;
const r = await api.get(`/drives/CONTOSO_DRIVE/items/CUSTOMERS_ITEM/workbook/worksheets/Customers/range(address='${addr}')?$select=values`);
return r.values;
"""
[[tools.parameters]]
name = "customer_id"
type = "string"
description = "Customer id, e.g. 'C007'."
required = true
```
**ALTERNATIVE — single-call tool with a templated range param** (only if the planner wants a pure single-call op): make the range a `{...}` path segment so the synthesizer treats it as a path param (tools.rs:501-515). Note `customer_id` arithmetic must then happen client-side (the model fills the address), which is less of a clean "id-keyed" tool. The recommended script-tool shape keeps the tool truly `customer_id`-keyed.

**Verified mechanics:** `build_operation` (tools.rs:499-537) — `{...}` segments → required path params; other declared params → query params. Substitution is literal `str::replace` (client.rs:158-159) with no re-encode (`join_url`, mod.rs:76-83). reqwest performs URL parsing/encoding on send, so the OData `range(address='A8:D8')` segment (parens, quotes, colon) is carried through. **Smoke-test the spec + a sample call in the parity test** to confirm reqwest accepts the literal address (Pitfall 4).

### Pattern 3: Graph range-read mock (P902-FIXTURE)
**What:** wiremock returns a Graph `workbookRange` body. [CITED: learn.microsoft.com/graph/api/resources/workbookrange]
```rust
// Source: parity_replay.rs:192-211 mount pattern + Graph workbookRange shape
Mock::given(method("GET"))
    .and(path("/drives/CONTOSO_DRIVE/items/CUSTOMERS_ITEM/workbook/worksheets/Customers/range(address='A8:D8')"))
    .and(header("authorization", "Bearer contoso-user-tok"))   // PASSTHROUGH PROOF
    .respond_with(ResponseTemplate::new(200).set_body_json(json!({
        "address": "Customers!A8:D8",
        "values": [["C007", "Northwind Traders", "Enterprise", "West"]]
    })))
    .mount(&backend).await;
```

### Pattern 4: Inbound bearer in the YAML replay (RQ-1, P902-PARITY)
```rust
// Source: parity_replay.rs:265-273 — pass Some(token) as the 4th arg (was None for london-tube)
let mut tester = ServerTester::new(
    &url,
    Duration::from_secs(30),
    false,                       // insecure
    Some("contoso-user-tok"),    // ◄── inbound bearer (tester.rs:96-101 injects it)
    Some("http"),                // force_transport
    None,                        // http_middleware_chain
)?;
```
Then, after the scenario, assert every recorded backend request carried the forwarded bearer (mirror parity_replay.rs:316-337 but match `authorization` header instead of `app_key` query):
```rust
for req in &recorded {
    assert_eq!(
        req.headers.get("authorization").map(|v| v.to_str().unwrap()),
        Some("Bearer contoso-user-tok"),
        "every Graph request carries the FORWARDED user bearer (passthrough)"
    );
}
```

### Pattern 5: Deterministic Code Mode headline query (RQ-2, P902-CODEMODE) — Rust test
```rust
// Source: oauth_passthrough_e2e.rs:36-103 — drive JsCodeExecutor over a token-threaded HttpCodeExecutor.
// The JS engine has NO Date builtin (Pitfall 1) → the reference date is a literal in the script.
const HEADLINE: &str = r#"
const REF = "2026-05-30";                       // pinned reference date (literal, never `now`)
const customers = await api.get("/drives/.../worksheets/Customers/range(address='A2:D9')?$select=values");
const orders    = await api.get("/drives/.../worksheets/Orders/range(address='A2:D40')?$select=values");
// join + filter trailing-3-months + sum > 100, all in JS string/array math
return result;   // deterministic set of customer ids
"#;
// ...build passthrough_exec + extra_with_token(Some("Bearer contoso-user-tok")),
// JsCodeExecutor::new(...).execute(HEADLINE, None) → assert_eq!(result, json!(["C007","C012"]));
```
**Date handling:** because the engine has no `Date`/`Date.now()` (verified — only Rust `Instant::now()` exists), the script compares `order_date` strings against the pinned `REF` constant using string/lexicographic comparison on ISO `YYYY-MM-DD` dates, or simple integer month math. The "last 3 months" window is `[REF - 3 months, REF]` computed from the literal — never wall-clock.

### Anti-Patterns to Avoid
- **Putting `customer_id`→row arithmetic in the single-call connector:** the connector does literal `str::replace` only (client.rs:158) — no math. Use a script tool or supply the address as a param.
- **Asserting a computed Code Mode result via a YAML `execute_code` step:** `execute_code` requires an approval token (code_mode.rs:589-616); YAML can't mint+thread it. Use the Rust test.
- **Using wall-clock `now` anywhere in the headline query:** the JS engine has no Date; even if it did, the fixture would rot. Pin a reference date literal.
- **Re-encoding the range address by hand:** don't double-encode; let reqwest handle it. Just verify the call lands via the parity test.
- **Adding a second Graph op for orders:** D-03 keeps it range-read-only; lay the Orders sheet out contiguously by `customer_id` and address a block.

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Inbound token capture | A custom auth provider | `TokenCaptureAuthProvider` (already wired by `build_server`, assemble.rs:298) | Ships + tested; this phase only configures it |
| Outbound token forwarding | Manual header injection | `AuthConfig::OAuthPassthrough` → `OAuthPassthroughAuth` (auth.rs:415-462) | Tested for required:true/false, present/absent (oauth_passthrough_e2e.rs) |
| Inbound bearer in tests | Custom transport / header plumbing | `ServerTester::new(.., Some(token), Some("http"), None)` (tester.rs:96-101) | Existing hook; no new harness |
| Per-request executor derivation | Re-implementing token threading | `request_executor_from_extra(base, extra)` (toolkit, re-exported) | The exact seam the handlers use |
| Offline backend | testcontainers / Docker / live Graph | wiremock (parity_replay.rs) | Pure-Rust, no Docker (project rule); single-threaded offline |
| Config parsing/validation | Custom TOML reader | `ServerConfig::from_toml_strict_validated` | deny_unknown_fields strict path the binary uses |

**Key insight:** Phase 90 (Plan 90-10) already proved passthrough end-to-end at the executor seam. 90.2 adds **business framing + a deterministic Excel fixture + the YAML-replay passthrough assertion** — zero new mechanism.

## Common Pitfalls

### Pitfall 1: Assuming the JS engine has `Date`/`Date.now()`
**What goes wrong:** Authoring the headline query with `new Date()` / `Date.now()` — it won't evaluate (no such builtin), or if added later, the test rots.
**Why it happens:** JS habit; the engine supports `Math.*`, `parseFloat`, `Object.keys`, array methods (eval.rs:1197-1250) but NOT Date (verified: only Rust `Instant::now()` exists in the engine).
**How to avoid:** Embed the reference date as a string literal in the script; compare ISO `YYYY-MM-DD` order dates lexicographically or with integer month math.
**Warning signs:** A test that passes today but would change result tomorrow; any `now`/`Date` token in a fixture or script.

### Pitfall 2: deny_unknown_fields — exact field names
**What goes wrong:** A misspelled key (`type` vs `type_`, `parameters` vs `params`, `mime_type` vs `mimetype`) is a hard parse error, not a warning.
**Why it happens:** Every config struct carries `#[serde(deny_unknown_fields)]` (config.rs:8,76,101,…).
**How to avoid:** Use exact names — `ParamDecl`: `name`, `type` (renamed from `param_type`), `description`, `required`, `default`, `max_length`, `minimum`, `maximum`, `enum` (config.rs:670-699). `ToolDecl`: `name`, `description`, `path`, `method`, `base_url`, `script`, `parameters` (config.rs:579-614). `ResourceDecl`: `uri`, `name`, `description`, `mime_type`, `content` (config.rs:767-784). `PromptDecl`: `name`, `description`, `include_resources` (config.rs:730-739). `AnnotationsDecl`: `read_only_hint`, `destructive_hint`, `idempotent_hint`, `open_world_hint`, `cost_hint` (config.rs:705-720). `[backend.auth] type = "oauth_passthrough"` with `target_header`, `required`.
**Warning signs:** `from_toml_strict_validated` returns an "unknown field" error.

### Pitfall 3: include_resources must match a defined resource URI
**What goes wrong:** A `start_code_mode` prompt referencing a typo'd resource URI fails resolution at assembly (register_prompts resolves each against the StaticResourceHandler — assemble.rs:204-213).
**Why it happens:** Same as 90.1's "Pitfall 2" baked into london-tube.toml.
**How to avoid:** Every URI in `[[prompts]] include_resources` must equal a `[[resources]] uri` exactly. Mirror 90.1: define a `assert_contoso_code_mode_surface` helper asserting the resource URIs + prompt name in BOTH fixture and pointable example (parity_replay.rs:56-73).

### Pitfall 4: Range-address path segment + reqwest URL parsing
**What goes wrong:** The OData `range(address='A8:D8')` segment contains `'`, `(`, `)`, `=`, `:` — if substituted into a `{...}` path param it is NOT re-encoded by the toolkit (client.rs:158, join_url mod.rs:76); reqwest must accept it.
**Why it happens:** `join_url` is plain `format!` concatenation; no percent-encoding.
**How to avoid:** Prefer the **script-tool** shape (the address is built inside JS and passed to `api.get` as a literal path — proven to work for london-tube's templated `/Line/${id}/Disruption`). The parity test's wiremock `path(...)` matcher confirms the request lands; if the address must be a path param of a single-call tool, smoke-test one call in the parity test before relying on it.
**Warning signs:** A 404 at the mock, or a reqwest URL-parse error, on the range call.

### Pitfall 5: positional verify-filter test-fn naming
**What goes wrong:** GSD's verify step runs a positional name filter; mismatched test-fn names won't be selected.
**Why it happens:** Documented in oauth_passthrough_e2e.rs:18-20 ("test fns are `oauth_passthrough_e2e_`-prefixed so the positional verify filter resolves").
**How to avoid:** Prefix the parity test fns `contoso_m365_*` and the code-mode test fns `contoso_m365_code_mode_*` consistently with the file stem.

### Pitfall 6: tests/ is excluded from the crate tarball
**What goes wrong:** Putting the pointable config under `tests/` means users can't `pmcp-openapi-server --config <it>` from the published crate.
**Why it happens:** `Cargo.toml:13` `exclude = [..., "tests/"]`; `examples/` is NOT excluded.
**How to avoid:** The pointable `contoso-m365.toml` MUST live under `examples/` (the fixture copy under `tests/fixtures/` is for the offline test only). Add a smoke test parsing `examples/contoso-m365.toml` via `from_toml_strict_validated` (parity_replay.rs:80-95 pattern).

## Code Examples

### Curated Graph spec (P902-SPEC) — minimal, parses via OpenApiSchema::parse
```yaml
# Source: tests/fixtures/london-tube-api.yaml template + Graph range-get URL
# [CITED: learn.microsoft.com/graph/api/range-get]
openapi: 3.0.0
info:
  title: Contoso M365 — Excel range-read subset (Microsoft Graph)
  version: "1.0.0"
  description: Curated slice of Microsoft Graph covering ONLY the Excel worksheet range-read op.
servers:
  - url: https://graph.microsoft.com/v1.0
paths:
  /drives/{drive-id}/items/{item-id}/workbook/worksheets/{worksheet-id}/range(address='{address}'):
    get:
      operationId: getWorksheetRange
      summary: Read a worksheet range by address.
      parameters:
        - { name: drive-id,     in: path,  required: true,  schema: { type: string } }
        - { name: item-id,      in: path,  required: true,  schema: { type: string } }
        - { name: worksheet-id, in: path,  required: true,  schema: { type: string } }
        - { name: address,      in: path,  required: true,  schema: { type: string } }
        - { name: $select,      in: query, required: false, schema: { type: string } }
      responses:
        "200": { description: A workbookRange object with the requested values. }
```
NOTE: validate this exact path string parses through `OpenApiSchema::parse` in the fixture-validity test (Wave 0 risk — the OData `(address='{address}')` segment is unusual). If `OpenApiSchema::parse` rejects the parenthesized segment, fall back to a plain `.../range` path and document the address as a `$select`/query-style param, OR rely on the curated spec being served only as the `api_schema` context resource (it is NOT used to dispatch — tools dispatch from `[[tools]]` path/script, assemble.rs:266-291). The spec's job is context + acceptance ("parses + contains the range op"), not runtime routing.

### Build-and-exit example (P902-EXAMPLE)
```rust
// Source: examples/london_tube_min.rs:19-50 — dispatch + build_server, return without serving
use pmcp_openapi_server::{build_server, dispatch};
use pmcp_server_toolkit::ServerConfig;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let cfg = ServerConfig::from_toml_strict_validated(CONTOSO_CONFIG)?;
    let (connector, http_exec) = dispatch(&cfg).await?;     // no network
    let _server = build_server(&cfg, connector, http_exec, None)?;  // curated-only, no spec
    println!("{} assembled: {} tool(s), {} resource(s), {} prompt(s)",
        cfg.server.name, cfg.tools.len(), cfg.resources.len(), cfg.prompts.len());
    Ok(())   // returns WITHOUT serving — CI-safe
}
const CONTOSO_CONFIG: &str = r#" ...trimmed contoso config... "#;
```

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| `with_inbound_token` on the binary | toolkit `request_executor_from_extra` (handlers live in toolkit) | Phase 90 Plan 90-10 (WR-01) | The dead binary `request_executor` was removed; use the toolkit re-export (assemble.rs:36, oauth_passthrough_e2e.rs:24-26) |
| api_key query-param auth (90.1) | oauth_passthrough delegated auth (90.2) | This phase | The headline auth path; same chain, different `[backend.auth] type` |

**Deprecated/outdated:** none relevant; the passthrough chain is current (Phase 90, 2026-05).

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | `OpenApiSchema::parse` accepts the OData parenthesized path segment `range(address='{address}')` | Code Examples §spec | LOW — if rejected, use a plain `/range` path; the spec is context-only (not used for dispatch), so a simplified path still satisfies P902-SPEC. Verify in Wave 0 fixture-validity test. |
| A2 | reqwest accepts the literal range-address segment in the substituted URL without manual encoding | Pitfall 4 | LOW — london-tube proves template-literal path interpolation works (`/Line/${id}/Disruption`); the parity test's `path(...)` matcher confirms. Use the script-tool shape to keep address-building in JS. |
| A3 | Graph `workbookRange` response shape is `{ "address", "values": [[...]] }` | Pattern 3 | LOW — fixture is fully controlled by us (wiremock); the shape only needs to be self-consistent with the Code Mode script. [CITED: workbookrange resource type] |
| A4 | The JS code-mode engine has NO `Date` builtin | Pitfall 1, Pattern 5 | LOW — verified by grep (only Rust `Instant::now()` present); reference-date literal is the correct determinism strategy regardless. |

## Open Questions (RESOLVED)

1. **Does `OpenApiSchema::parse` accept the OData `(address='{address}')` path segment?**
   - **RESOLVED:** Handled as a Plan 90.2-01 Task 2 fixture-validity gate with a documented fallback. The Plan 02 `contoso_m365_fixture` test asserts `OpenApiSchema::parse` / `from_toml_strict_validated` accept the spec; if the parenthesized OData segment is rejected, Plan 01 Task 2's acceptance criteria + SUMMARY fall back to a plain `.../range` path with the address as a query/`$select`-style parameter. The spec is served as context, not used for runtime routing, so either form satisfies P902-SPEC. Low risk.
   - What we know: it accepts standard `{param}` path templates (london-tube-api.yaml). The parenthesized OData function segment is non-standard OpenAPI.
   - Recommendation: Plan 01 Task 2 records which path form was used in its SUMMARY; Plan 02 Task 1 confirms it parses.

2. **Single-call tool vs script tool for `get_customer`/`get_customer_orders`?**
   - **RESOLVED:** Script tools chosen (RESEARCH Pattern 2 + CONTEXT D-02/D-03). All four plans implement `customer_id`-keyed script tools that do the row→address math in JS (the connector does no arithmetic). Consistent across the plan set.
   - What we know: connector does no arithmetic; script tools (JS) can template the address (RQ-3).
   - Recommendation: use **script tools** — they keep the tool truly `customer_id`-keyed while doing the row→address math in JS. This matches D-02/D-03 ("the tool templates the Excel range address from the id").

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| wiremock | offline parity/code-mode tests | ✓ (dev-dep, used in 90.1) | as in workspace lock | — |
| mcp-tester | YAML replay | ✓ | 0.5.0 | — |
| mdbook | `make book` / course build | check locally | — | `cargo install mdbook` (Makefile:422-425 auto-installs) |
| Live Microsoft Graph / Azure AD tenant | optional live test only | ✗ (not needed) | — | `#[ignore]`+env-gated scaffold; offline parity is the gate (SPEC boundary) |

**Missing dependencies with no fallback:** none — the entire completion gate is offline.
**Missing dependencies with fallback:** live Graph (gated/optional, like `parity_live_tfl`).

## Validation Architecture

### Test Framework
| Property | Value |
|----------|-------|
| Framework | Rust built-in `#[tokio::test]` + wiremock + mcp-tester scenario replay |
| Config file | none (workspace cargo); tests live in `crates/pmcp-openapi-server/tests/` |
| Quick run command | `cargo test -p pmcp-openapi-server --test contoso_m365_parity -- --test-threads=1` |
| Full suite command | `make quality-gate` (fmt, clippy pedantic+nursery, build, test, audit) |

### Phase Requirements → Test Map
| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| P902-SPEC | curated Graph spec parses via `OpenApiSchema::parse`, contains range op, no write/list ops | unit | `cargo test -p pmcp-openapi-server --test contoso_m365_parity contoso_m365_fixture -- --test-threads=1` | ❌ Wave 0 |
| P902-CONFIG | `contoso-m365.toml` parses via `from_toml_strict_validated`; type=oauth_passthrough; both tools w/ customer_id; no real secret | unit + grep | `cargo test ... contoso_m365_fixture` + `grep oauth_passthrough/get_customer/customer_id` | ❌ Wave 0 |
| P902-FIXTURE | mock workbook date-pinned; ≥1 match + ≥1 non-match on headline | unit (asserted via P902-CODEMODE result) | covered by code-mode test | ❌ Wave 0 |
| P902-PARITY | both tools return fixture rows; forwarded `Authorization: Bearer` asserted at backend; fails if absent | integration (wiremock replay) | `cargo test ... --test contoso_m365_parity -- --test-threads=1` | ❌ Wave 0 |
| P902-CODEMODE | headline query computes exact deterministic customer-id set; reference date pinned | integration (Rust, JsCodeExecutor) | `cargo test -p pmcp-openapi-server --features openapi-code-mode --test contoso_m365_code_mode -- --test-threads=1` | ❌ Wave 0 |
| P902-EXAMPLE | `cargo run --example contoso_m365_min` exits 0; `examples/contoso-m365.toml` parses+validates | example + smoke | `cargo run --example contoso_m365_min -p pmcp-openapi-server` + smoke test | ❌ Wave 0 |
| P902-DOCS-BOOK | new chapter in nav; `make book` exits 0; required-content greps | doc build + grep | `make book` + `grep oauth_passthrough/get_customer/governance pmcp-book/src/openapi-graph-m365.md` | ❌ Wave 0 |
| P902-DOCS-COURSE | new chapter in nav; `mdbook build` exits 0; same greps | doc build + grep | `cd pmcp-course && mdbook build` + greps | ❌ Wave 0 |
| (live) | optional best-effort live Graph | `#[ignore]`+env-gated | `PMCP_OPENAPI_LIVE_TEST=1 ... -- --ignored --test-threads=1` | ❌ Wave 0 |

### Sampling Rate
- **Per task commit:** `cargo test -p pmcp-openapi-server --test contoso_m365_parity -- --test-threads=1`
- **Per wave merge:** `cargo test -p pmcp-openapi-server --features openapi-code-mode -- --test-threads=1` + `make book`
- **Phase gate:** `make quality-gate` green + both mdbooks build + no `src/` diff under the two crates.

### Wave 0 Gaps
- [ ] `tests/fixtures/contoso-m365.toml` — P902-CONFIG (oauth_passthrough + two script tools + code_mode + resources/prompts)
- [ ] `tests/fixtures/contoso-m365-api.yaml` — P902-SPEC (validate OData path parses; fallback path ready)
- [ ] `tests/fixtures/contoso-m365-scenarios.yaml` — P902-PARITY tool-correctness scenario
- [ ] `tests/contoso_m365_parity.rs` — P902-PARITY (mirror parity_replay.rs; pass `Some(token)`, assert forwarded bearer)
- [ ] `tests/contoso_m365_code_mode.rs` — P902-CODEMODE (mirror oauth_passthrough_e2e.rs; assert deterministic set)
- [ ] `examples/contoso-m365.toml` + `examples/contoso_m365_min.rs` — P902-EXAMPLE
- [ ] `pmcp-book/src/openapi-graph-m365.md` + SUMMARY.md entry — P902-DOCS-BOOK
- [ ] `pmcp-course/src/openapi-graph-m365.md` + SUMMARY.md entry — P902-DOCS-COURSE
- [ ] No framework install needed (tokio/wiremock/mcp-tester already in 90.1)

## Security Domain

### Applicable ASVS Categories
| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | yes | `oauth_passthrough` — delegated per-user bearer; server holds NO standing credential (the governance story, D-05) |
| V3 Session Management | no | Stateless MCP request; token captured per-request into `AuthContext`, not stored |
| V4 Access Control | yes (narrative) | Admin consents once to a bounded scope (ceiling); the forwarded user token governs per-file access — documented, not code |
| V5 Input Validation | yes | `customer_id` bounded by tool input schema (`additionalProperties:false`, tools.rs:556-558); range address built from validated id |
| V6 Cryptography | yes | HMAC approval-token (code_mode) — toolkit-owned, never hand-rolled; dev-guarded inline `token_secret` for offline tests only |

### Known Threat Patterns for oauth_passthrough + Graph
| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Token redirected to attacker header | Tampering | `target_header` is operator-fixed in committed config; client controls only the VALUE (auth.rs WR-04 doc) |
| Header injection via malformed token | Tampering | `HeaderValue::try_from` rejects control chars (auth.rs:442-444) — malformed token rejected, not relayed |
| Real credential leaking into repo | Info Disclosure | No real Graph client/tenant/user token committed; `${VAR}` placeholders + dev-guarded inline secret; tests assert resolved (not literal) values |
| Server acting beyond user's rights | Elevation of Privilege | Server holds no standing credential; can only ever act AS the calling user (the governance note, D-05) |

## Sources

### Primary (HIGH confidence — codebase, this session)
- `crates/pmcp-server-toolkit/src/http/auth.rs:123-130,408-462` — `AuthConfig::OAuthPassthrough`, `OAuthPassthroughAuth::apply`
- `crates/pmcp-openapi-server/src/assemble.rs:22-31,86-115,266-298` — passthrough wiring + `TokenCaptureAuthProvider`
- `crates/pmcp-openapi-server/tests/oauth_passthrough_e2e.rs` (whole) — the executor-seam passthrough proof template
- `crates/pmcp-openapi-server/tests/parity_replay.rs` (whole) — offline wiremock replay harness + `ServerTester` usage
- `crates/mcp-tester/src/tester.rs:79-224,316-321,379-382` — `ServerTester::new` `api_key` inbound-bearer hook (RQ-1)
- `crates/pmcp-server-toolkit/src/code_mode.rs:422-631` — `ValidateCodeHandler` / `ExecuteCodeHandler` (approval-token requirement, RQ-2)
- `crates/pmcp-server-toolkit/src/tools.rs:387-537` — single-call tool synth, `build_operation` path/query mapping (RQ-4)
- `crates/pmcp-server-toolkit/src/http/client.rs:143-215` + `http/mod.rs:76-83` — path substitution + `join_url` (no re-encode, RQ-3)
- `crates/pmcp-server-toolkit/src/config.rs:578-784` — exact decl field names (deny_unknown_fields)
- `crates/pmcp-openapi-server/tests/fixtures/london-tube.toml`, `london-tube-api.yaml`, `london-tube-scenarios.yaml`, `examples/london_tube_min.rs`, `examples/london-tube.toml` — structural templates
- `crates/pmcp-openapi-server/Cargo.toml:13` — `exclude = ["tests/"]` packaging constraint
- `Makefile:419-427` — `make book`; `.planning/ROADMAP.md:1661-1677` — Phase 90.2 roadmap

### Secondary (MEDIUM-HIGH confidence — official docs)
- Microsoft Graph "Get Range": https://learn.microsoft.com/en-us/graph/api/range-get?view=graph-rest-1.0
- Microsoft Graph "Worksheet: Range": https://learn.microsoft.com/en-us/graph/api/worksheet-range?view=graph-rest-1.0
- Microsoft Graph "workbookRange resource type": https://learn.microsoft.com/en-us/graph/api/resources/workbookrange?view=graph-rest-1.0
- Microsoft Graph "Working with Excel": https://learn.microsoft.com/en-us/graph/api/resources/excel?view=graph-rest-1.0

### Tertiary (LOW confidence)
- none — all critical claims verified against codebase or official docs.

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — all deps already compiled in 90.1; zero new crates.
- Architecture / passthrough chain: HIGH — read every file in the chain; e2e test already proves it.
- RQ-1 (inbound bearer in replay): HIGH — exact `ServerTester::new` hook verified at tester.rs:96-101.
- RQ-2 (code-mode result assertion): HIGH — approval-token requirement verified at code_mode.rs:589-616; Rust test mandated.
- RQ-3/RQ-4 (range addressing + spec): HIGH on mechanics (tools.rs/client.rs); MEDIUM (A1) on whether `OpenApiSchema::parse` tolerates the OData path segment — flagged for Wave 0 verification with a documented fallback.
- Pitfalls: HIGH — drawn from in-tree code comments and the 90.1 fixture's baked-in lessons.

**Research date:** 2026-05-30
**Valid until:** 2026-06-29 (stable in-tree machinery; Graph URL shape is stable v1.0 API)
