---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs
autonomous: true
requirements: [P902-CODEMODE, P902-FIXTURE]
must_haves:
  truths:
    - "An offline Rust Code Mode test computes 'customers who bought >100 in last 3 months' and asserts the EXACT deterministic customer-id set LOADED from the canonical contoso-m365-workbook.json (expected_headline_set), not a re-typed literal"
    - "The headline script uses a PINNED reference-date literal (REF) + a pinned window_start (both from the canonical json), never wall-clock now"
    - "The Code Mode executor forwards the inbound user bearer to the Graph mock for both Customers and Orders reads, and each read is invoked exactly once"
    - "The script reads via the SAME Graph range-read API the get_customer/get_customer_orders tools expose (reconciled with P902-CODEMODE 'via the tools')"
    - "Changing fixture amounts/dates would change the asserted set"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs"
      provides: "JsCodeExecutor-driven deterministic headline-query proof over token-threaded HttpCodeExecutor; expected set + REF + window_start loaded from the canonical workbook"
      contains: "JsCodeExecutor"
  key_links:
    - from: "contoso-m365-workbook.json (canonical, Plan 01)"
      to: "REF + window_start + expected_headline_set + mock rows in contoso_m365_code_mode.rs"
      via: "the test LOADS them from the json (no re-typed expected set or rows)"
      pattern: "contoso-m365-workbook"
    - from: "passthrough_exec + extra_with_token"
      to: "wiremock Customers + Orders range mocks (authorization-matched, .expect(1) each)"
      via: "request_executor_from_extra -> JsCodeExecutor.execute(HEADLINE)"
      pattern: "request_executor_from_extra"
    - from: "HEADLINE script REF/START constants"
      to: "asserted customer-id set"
      via: "join + trailing-3-month filter (month-rollover-safe) + sum>100, all from the pinned literals"
      pattern: "const REF ="
---

<objective>
Prove the demo's hero query end-to-end offline: an `execute_code`-style Code Mode workflow that loads Customers + Orders via the SAME Graph range-read API the two tools expose, joins on `customer_id`, filters orders to the trailing 3 months of a PINNED reference date, sums `amount` per customer, and returns the deterministic set with total > 100. This MUST be a Rust test (not a YAML step) because `execute_code` requires an HMAC approval token a flat scenario cannot thread (RQ-2) — so the test drives `JsCodeExecutor` directly over the same token-threaded `HttpCodeExecutor` the toolkit handlers use. The REF date, the window_start lower bound, the workbook rows, and the expected result set are all LOADED from the canonical `tests/fixtures/contoso-m365-workbook.json` (Plan 01) — never re-typed — so the Code Mode assertion cannot drift from the config/parity/docs copies.

Purpose: P902-CODEMODE (deterministic computed result) + P902-FIXTURE (>=1 match, >=1 non-match proven by the asserted set).
Output: `tests/contoso_m365_code_mode.rs`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-SPEC.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-RESEARCH.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-PATTERNS.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-01-SUMMARY.md

<interfaces>
<!-- From crates/pmcp-openapi-server/tests/oauth_passthrough_e2e.rs:24-72 — copy these helpers verbatim: -->
<!-- use pmcp_server_toolkit::code_mode::{request_executor_from_extra, CodeExecutor, ExecutionConfig, HttpCodeExecutor, JsCodeExecutor}; -->
<!-- use pmcp_server_toolkit::http::auth::{create_passthrough_auth_provider, AuthConfig}; -->
<!-- fn passthrough_exec(base_url: String, required: bool) -> HttpCodeExecutor  (lines 36-46) -->
<!-- fn extra_with_token(token: Option<&str>) -> pmcp::RequestHandlerExtra      (lines 50-61) -->
<!-- fn code_mode_executor(base, extra) -> JsCodeExecutor<HttpCodeExecutor>     (lines 66-72; = request_executor_from_extra + JsCodeExecutor::new) -->
<!-- executor.execute(SCRIPT, None).await -> serde_json::Value (the computed result) -->

<!-- JS engine reality (Phase 90 Plan 05 + RESEARCH Pitfall 1/A4): NO Date builtin. -->
<!-- Available: Math.*, parseInt/parseFloat, Object.keys, array methods, string methods, template literals. -->
<!-- api.get path must be a string/template literal; bind ApiCall to a const before return; .slice() bounds need numeric literals. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contoso_m365_code_mode.rs — deterministic headline query over token-threaded JsCodeExecutor, reading via the tools' Graph range-read API, set loaded from the canonical workbook</name>
  <files>crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs</files>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json (Plan 01 — THE canonical source: REF (`ref_date`), `window_start`, the Customers/Orders rows + block addresses, and `expected_headline_set` — ALL loaded from here, none re-typed)
    - crates/pmcp-openapi-server/tests/oauth_passthrough_e2e.rs (THE analog — whole file; copy passthrough_exec/extra_with_token/code_mode_executor helpers + the required_true_token_present test body shape at 82-105; #![cfg(feature = "openapi-code-mode")] at line 22; positional-filter naming lesson at 18-20)
    - 90.2-01-SUMMARY.md (the EXACT workbook rows + which customers match vs don't-match + the boundary customer at window_start + the Customers/Orders range addresses the script reads — sourced from the canonical json)
    - .planning/phases/90.2-.../90.2-RESEARCH.md (Pattern 5 headline script + Pitfall 1 date handling + the JS-subset engine-accuracy rules)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (§ contoso_m365_code_mode.rs — the re-skin diffs + the HEADLINE script skeleton)
  </read_first>
  <action>
    Create tests/contoso_m365_code_mode.rs gated `#![cfg(feature = "openapi-code-mode")]` (line-1-region, mirror oauth_passthrough_e2e.rs:22). Prefix EVERY test fn `contoso_m365_code_mode_*` (positional verify-filter lesson).
    Copy the three helpers VERBATIM from oauth_passthrough_e2e.rs: `passthrough_exec(base_url, required)`, `extra_with_token(token)`, `code_mode_executor(base, extra)`.
    Load the canonical dataset: read tests/fixtures/contoso-m365-workbook.json (e.g. `include_str!` + `serde_json::from_str`) to obtain `ref_date`, `window_start`, the Customers/Orders block addresses, the row values, and `expected_headline_set`. The Rust assertion compares the computed result against the LOADED `expected_headline_set` — NOT a hand-typed literal. The mock response bodies are built from the LOADED rows.
    "VIA THE TOOLS" RECONCILIATION (review item 3 — P902-CODEMODE frames the query as composed over get_customer/get_customer_orders): the connector tools are thin literal str::replace mappers over the Graph range-read op (no aggregation), so the deterministic headline aggregation reads the SAME Graph range-read API the tools expose — it issues `api.get` against the identical `/drives/.../workbook/worksheets/{sheet}/range(address='...')?$select=values` endpoint the `get_customer`/`get_customer_orders` scripts call, but over the all-rows block addresses (`all_customers_address`, `all_orders_address`) so the join+filter+sum is computable in one pass. Document THIS in the test header comment + the SUMMARY: the Code Mode workflow uses the same tool-exposed Graph range-read API (not a different/raw endpoint); reading the whole-table blocks rather than per-id ranges is required for the cross-row aggregation and stays within the single range-read op (no new Graph op, no scope expansion). This satisfies P902-CODEMODE without changing the locked tool interface.
    Author the `HEADLINE` script const computing the hero query, engine-accurate (NO Date builtin):
      - First lines `const REF = "<ref_date from the canonical json>";` and `const START = "<window_start from the canonical json>";` (string literals — NEVER wall-clock `now`). Including START as a pinned literal eliminates the JS month-rollover risk for the lower bound (review item 5 — see month helper below).
      - MONTH-ROLLOVER-SAFE lower-bound derivation (review item 5, Gemini): even though START is pinned, the script must DEMONSTRATE the trailing-3-month logic. Implement a pure-integer month subtraction with explicit year rollover: parse REF into `y`,`m`,`d` via `parseInt` on the `YYYY-MM-DD` substrings; compute `m2 = m - 3` and `y2 = y`; `if (m2 < 1) { m2 += 12; y2 -= 1; }`; zero-pad month back to two digits via string concat (no Date); assemble `computedStart = y2 + "-" + pad(m2) + "-" + padDay(d)`. Assert in Rust (or via the script's returned debug field) that `computedStart === START` so the rollover math is provably correct against the pinned literal.
      - `const customers = await api.get("/drives/CONTOSO_DRIVE/items/CUSTOMERS_ITEM/workbook/worksheets/Customers/range(address='<all_customers_address from the canonical json>')?$select=values");` then read `customers.values`.
      - `const orders = await api.get("/drives/CONTOSO_DRIVE/items/ORDERS_ITEM/workbook/worksheets/Orders/range(address='<all_orders_address from the canonical json>')?$select=values");` then `orders.values`.
      - Join Orders->Customers on `customer_id`; filter `order_date` into the trailing-3-month window `[START, REF]` using ISO `YYYY-MM-DD` lexicographic string comparison (inclusive of START — the boundary customer at START must be classified IN per Plan 01); sum `amount` per customer; collect the customer ids whose total `> 100`; sort for a stable order; bind the array to a `const` and `return` it.
    Test body `contoso_m365_code_mode_headline_query_returns_deterministic_set` (mirror oauth_passthrough_e2e_required_true_token_present...): start a `MockServer`; mount a GET mock for the Customers all-rows range address AND one for the Orders all-rows range address, EACH `.and(header("authorization", "Bearer contoso-user-tok"))` (passthrough forwarded into the Code Mode reads too) AND EACH with `.expect(1)` (review item: assert request COUNT so a hardcoded result or missing backend read cannot pass accidentally), responding with the workbookRange `{ "values": [[...]] }` bodies built from the LOADED canonical rows; build `passthrough_exec(server.uri(), true)` + `extra_with_token(Some("Bearer contoso-user-tok"))` + `code_mode_executor`; `executor.execute(HEADLINE, None).await`; assert the result EQUALS the LOADED `expected_headline_set` (e.g. `assert_eq!(result, json!(expected_set))`); let the MockServer `.expect(1)` guards verify exactly two backend reads occurred.
    Run single-threaded.
  </action>
  <verify>
    <automated>cargo test -p pmcp-openapi-server --features openapi-code-mode --test contoso_m365_code_mode -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-openapi-server --features openapi-code-mode --test contoso_m365_code_mode -- --test-threads=1` passes
    - Expected set + REF + window_start are LOADED from the canonical json (not re-typed): `grep -c 'contoso-m365-workbook' crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs` returns >= 1
    - `grep -c 'const REF =' ...contoso_m365_code_mode.rs` returns 1 (pinned reference-date literal) AND `grep -c 'const START =' ...` returns 1 (pinned lower bound)
    - Month-rollover-safe derivation present: `grep -Ec 'm2 < 1|\+= 12|computedStart' ...contoso_m365_code_mode.rs` returns >= 1, and the test asserts the script's computed start equals the pinned START
    - Boundary classification: the boundary customer whose newest qualifying order_date == window_start (per Plan 01 SUMMARY) IS in the asserted set, and the just-outside customer is NOT — verified by the set equality assertion (record in SUMMARY)
    - NO wall-clock: `grep -E 'Date\.|new Date|Instant::now|SystemTime' ...contoso_m365_code_mode.rs` returns nothing in the HEADLINE script region
    - `grep -c 'Bearer contoso-user-tok' ...contoso_m365_code_mode.rs` returns >= 2 (Customers mock + Orders mock both require the forwarded bearer)
    - Request-count guard: `grep -c '.expect(1)' ...contoso_m365_code_mode.rs` returns >= 2 (both reads invoked exactly once — guards against hardcoded results)
    - `grep -c 'request_executor_from_extra\|JsCodeExecutor' ...contoso_m365_code_mode.rs` returns >= 1 (drives the executor directly, not a YAML step)
    - "Via the tools" reconciliation is documented in the test header comment AND the SUMMARY (same Graph range-read API as the tools; whole-table block read justified for aggregation; no new Graph op)
    - Determinism sensitivity: the asserted set is the LOADED `expected_headline_set` derived only from the fixture rows + the pinned REF/START (not `now`) — re-running on any later date yields the identical set.
  </acceptance_criteria>
  <done>The Rust Code Mode test passes offline single-threaded; LOADS REF/window_start/expected_set + rows from the canonical workbook; computes the headline >100-in-3-months set with month-rollover-safe math validated against the pinned START, over token-threaded reads (each invoked exactly once) issued against the SAME Graph range-read API the tools expose; asserts the exact loaded set (>=1 match, >=1 non-match, boundary verified); reconciliation with "via the tools" documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code Mode executor -> Graph backend | the per-request forwarded user bearer is the only credential reaching the Customers/Orders reads |
| script logic -> time | the "last 3 months" window is computed from a committed literal, not the host clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.2-passthrough-03 | Elevation of Privilege | Code Mode reads to Graph | mitigate | Both wiremock mocks REQUIRE `authorization: Bearer contoso-user-tok` AND `.expect(1)`; the test fails if the Code Mode executor does not forward the inbound user token or does not actually read the backend — proves Code Mode runs AS the calling user, no standing credential, against the tool-exposed range-read API. |
| T-90.2-content-03 | Tampering | inert workbook fixture rows | accept | Static test data authored by us in the canonical workbook json, loaded (not re-typed); deterministic; no live Graph in CI. |
| T-90.2-rot-01 | Repudiation/Reliability | wall-clock dependence | mitigate | Pinned REF + START literals (from the canonical json); acceptance grep rejects any `Date`/`now`/`SystemTime` in the script; month-rollover math validated against the pinned START — the asserted set cannot rot over time. |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server --features openapi-code-mode --test contoso_m365_code_mode -- --test-threads=1` green.
- The asserted set is the LOADED `expected_headline_set` from the canonical workbook; both backend reads invoked exactly once.
- The month-rollover-safe `computedStart` equals the pinned `window_start`.
</verification>

<success_criteria>
- The headline query computes the exact deterministic customer-id set offline, from a pinned reference date + window_start, with the inbound bearer forwarded to both reads (each invoked once), via the same Graph range-read API the tools expose.
- No wall-clock/`Date` dependence anywhere; month-rollover math is explicit and validated; the set is stable across calendar time.
</success_criteria>

<output>
After completion, create `.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-03-SUMMARY.md`. RECORD: the pinned REF + window_start (confirmed loaded from the canonical json), the asserted customer-id set (= expected_headline_set), the boundary-customer classification result, the month-rollover-safe trailing-3-month approach used, and the "via the tools" reconciliation (same Graph range-read API; whole-table block read justified for aggregation) — so the docs chapter in Plan 04 can show the same query accurately.
</output>
</content>
