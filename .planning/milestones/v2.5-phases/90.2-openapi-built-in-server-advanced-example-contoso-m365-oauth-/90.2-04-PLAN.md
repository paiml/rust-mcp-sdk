---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - pmcp-book/src/openapi-graph-m365.md
  - pmcp-book/src/SUMMARY.md
  - pmcp-course/src/openapi-graph-m365.md
  - pmcp-course/src/SUMMARY.md
autonomous: true
requirements: [P902-DOCS-BOOK, P902-DOCS-COURSE]
must_haves:
  truths:
    - "A new dedicated book chapter exists, is wired into pmcp-book SUMMARY.md, and make book exits 0"
    - "A new dedicated course chapter exists, is wired into pmcp-course SUMMARY.md, and mdbook build exits 0"
    - "Both chapters explain admin-consent governance + per-user passthrough + no standing credentials, show the oauth_passthrough config block (quoted from the SHIPPED examples/contoso-m365.toml), both tool names, and the headline Code Mode query with its deterministic result framing (from 90.2-03-SUMMARY)"
    - "The chapters' resource URIs match the SHIPPED config's URIs verbatim (cross-checked) so a typo cannot rot the docs"
  artifacts:
    - path: "pmcp-book/src/openapi-graph-m365.md"
      provides: "Book advanced-example chapter (governance + Excel narrative + two tools + headline query)"
      contains: "oauth_passthrough"
    - path: "pmcp-course/src/openapi-graph-m365.md"
      provides: "Course chapter, same substance in course voice"
      contains: "oauth_passthrough"
  key_links:
    - from: "pmcp-book/src/SUMMARY.md"
      to: "openapi-graph-m365.md"
      via: "nav line sibling of the existing OpenAPI chapter"
      pattern: "openapi-graph-m365.md"
    - from: "pmcp-course/src/SUMMARY.md"
      to: "./openapi-graph-m365.md"
      via: "nav line sibling of the existing OpenAPI chapter"
      pattern: "openapi-graph-m365.md"
    - from: "chapters' resource URIs + tool names + headline result"
      to: "examples/contoso-m365.toml (Plan 02) + 90.2-03-SUMMARY deterministic set"
      via: "the chapters quote the SHIPPED config + describe the proven result (not PATTERNS.md aspiration)"
      pattern: "docs://contoso-m365"
---

<objective>
Write the two dedicated advanced-example chapters (book + course) telling the Contoso M365 story: keep your existing Excel files, connect them to AI via MCP; a business analyst curates the API slice; `oauth_passthrough` means an admin consents once to a bounded scope (the ceiling) while the signed-in user's forwarded token governs per-file access and the server holds no standing credentials. Walk through the two `customer_id`-keyed tools and the headline Code Mode query. Because this plan now runs in Wave 3 AFTER the fixture (Plan 01), parity (Plan 02), and Code Mode (Plan 03) have shipped, the chapters describe SHIPPED behavior: they quote the actual `examples/contoso-m365.toml` config block and reference the deterministic headline result proven in 90.2-03-SUMMARY. Match 90.1 chapter depth (D-05: concise config walkthrough + 1-2 paragraph governance note — NOT a Mermaid diagram or a threat subsection).

Purpose: P902-DOCS-BOOK + P902-DOCS-COURSE.
Output: `pmcp-book/src/openapi-graph-m365.md` (+ SUMMARY nav), `pmcp-course/src/openapi-graph-m365.md` (+ SUMMARY nav).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-SPEC.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-CONTEXT.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-PATTERNS.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-01-SUMMARY.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pmcp-book/src/openapi-graph-m365.md + wire into pmcp-book SUMMARY.md</name>
  <files>pmcp-book/src/openapi-graph-m365.md, pmcp-book/src/SUMMARY.md</files>
  <read_first>
    - crates/pmcp-openapi-server/examples/contoso-m365.toml (Plan 02 — the SHIPPED pointable config; QUOTE the `[backend.auth] type = "oauth_passthrough"` block + the resource URIs FROM HERE, not from PATTERNS.md, so the chapter matches what shipped)
    - .planning/phases/90.2-.../90.2-01-SUMMARY.md (the resource URIs + prompt name VERBATIM, the tool names, the workbook framing — the chapter's URIs must match these exactly)
    - .planning/phases/90.2-.../90.2-03-SUMMARY.md (the deterministic headline result set + the trailing-3-months approach — reference the result framing accurately, e.g. "returns the customers whose 3-month spend exceeds 100")
    - pmcp-book/src/openapi-built-in-server.md (THE analog — 295 lines; reuse section skeleton: "The Problem (Why Config, Not Code)" line 17, "Outgoing Authentication" line 144, "Customize Through Config — Two Kinds of Tools" line 103, "Resources & Prompts (Code Mode Context)" line 175, "The OpenAPI Spec Is Optional" line 254, "What You Built" line 282)
    - pmcp-book/src/SUMMARY.md:38 (existing OpenAPI nav line — add a sibling line immediately after it)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (§ pmcp-book/src/openapi-graph-m365.md heading reuse map; use it for STRUCTURE only — the config block + URIs + result come from the shipped artifacts above)
  </read_first>
  <action>
    Create pmcp-book/src/openapi-graph-m365.md re-skinning the analog's section flow to Contoso/Graph/Excel (D-06 framing: org "Contoso", "Sales" SharePoint site, `Customers.xlsx`, customers Northwind Traders / Fabrikam / Tailspin Toys).
    - "The Problem" section -> the "keep your existing Excel files, connect them to AI" headline + the business-analyst-curates-the-API-slice point.
    - "Outgoing Authentication" section -> THE HEADLINE governance note (1-2 paragraphs, D-05) with the FULL DOUBLE-LOCK narrative (review item 7): QUOTE the `[backend.auth] type = "oauth_passthrough"` block from the shipped examples/contoso-m365.toml; explain that the admin's one-time consent sets the CEILING of what the server may ever request (the bounded scope), AND that the signed-in user's forwarded per-user token FURTHER restricts actual access to that user's own files — both locks apply; the server holds NO standing credentials and can only ever act as the calling user. At most ONE sentence may mention `oauth2_client_credentials` as a contrast (boundary — do not build it).
    - "Two Kinds of Tools" section -> the two script tools `get_customer` and `get_customer_orders` (each `customer_id`-keyed, templating the Excel range address), with everything richer left to Code Mode.
    - "Resources & Prompts" section -> list the three resource URIs EXACTLY as they appear in the shipped config / 90.2-01-SUMMARY (verbatim — the acceptance greps the same literals to prevent doc rot).
    - "OpenAPI Spec Is Optional" section -> re-skin briefly.
    - "What You Built" -> the headline Code Mode query "customers who bought more than 100 in the last 3 months" (use the exact phrasing) AND reference the deterministic result framing from 90.2-03-SUMMARY (the proven set / "stable across time because the reference date is pinned").
    Then add the SUMMARY.md nav line immediately after pmcp-book/src/SUMMARY.md:38, e.g. `- [Chapter 12.12: OpenAPI over Microsoft Graph (Contoso M365)](openapi-graph-m365.md)`.
    Use doctest-safe / mdbook-safe fenced blocks (no executable rust fences that would fail `make book`'s build; toml/text/json fences are fine).
  </action>
  <verify>
    <automated>make book</automated>
  </verify>
  <acceptance_criteria>
    - `make book` exits 0
    - `grep -c 'oauth_passthrough' pmcp-book/src/openapi-graph-m365.md` returns >= 1
    - `grep -c 'get_customer' pmcp-book/src/openapi-graph-m365.md` returns >= 1 AND `grep -c 'get_customer_orders' ...` returns >= 1
    - `grep -Ec 'last 3 months|bought more than 100|>100' pmcp-book/src/openapi-graph-m365.md` returns >= 1 (the headline query)
    - FULL double-lock governance (review item 7 — require BOTH locks, not any-one phrase): the chapter contains the admin-consent CEILING idea AND the per-user-token FURTHER-restricts idea AND the no-standing-credential idea. Falsifiable: `grep -Eic 'admin consent|bounded scope|ceiling' pmcp-book/src/openapi-graph-m365.md` returns >= 1 AND `grep -Eic 'per-user|forwarded token|the user.?s (own )?token|further restrict' pmcp-book/src/openapi-graph-m365.md` returns >= 1 AND `grep -Eic 'no standing credential|holds no .* credential|act as the calling user' pmcp-book/src/openapi-graph-m365.md` returns >= 1
    - Resource-URI cross-check (review item 6 — no doc rot): every `docs://contoso-m365` / `code-mode://learnings` URI string in the chapter EXACTLY matches a URI present in the shipped examples/contoso-m365.toml. Falsifiable: `for u in $(grep -Eo '(docs://contoso-m365[^ )"`]*|code-mode://learnings)' pmcp-book/src/openapi-graph-m365.md | sort -u); do grep -qF "$u" crates/pmcp-openapi-server/examples/contoso-m365.toml || { echo "URI in docs not in shipped config: $u"; exit 1; }; done` exits 0
    - `grep -c 'openapi-graph-m365.md' pmcp-book/src/SUMMARY.md` returns 1 (nav wired)
    - At most ONE mention of client-credentials: `grep -ci 'client_credentials\|client-credentials' pmcp-book/src/openapi-graph-m365.md` returns <= 1
  </acceptance_criteria>
  <done>Book chapter exists with the full double-lock governance note + oauth_passthrough block quoted from the shipped config + both tool names + headline query with its deterministic result framing; resource URIs match the shipped config verbatim; wired into SUMMARY.md; make book exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Write pmcp-course/src/openapi-graph-m365.md + wire into pmcp-course SUMMARY.md</name>
  <files>pmcp-course/src/openapi-graph-m365.md, pmcp-course/src/SUMMARY.md</files>
  <read_first>
    - crates/pmcp-openapi-server/examples/contoso-m365.toml (Plan 02 — SHIPPED config; quote the oauth_passthrough block + resource URIs from here)
    - .planning/phases/90.2-.../90.2-03-SUMMARY.md (the deterministic headline result + trailing-3-months approach to describe accurately)
    - pmcp-course/src/openapi-built-in-server.md (THE analog — 319 lines; reuse skeleton: "What You'll Learn" line 20, "Step 4: Authenticate to the Backend" line 154, "Step 3: Two Kinds of Tools" line 112, "Resources & Prompts (Code Mode Context)" line 191, "Exercise:" line 288, "Key Takeaways" line 307)
    - pmcp-course/src/SUMMARY.md:78 (existing OpenAPI nav line — add a sibling line after it)
    - pmcp-book/src/openapi-graph-m365.md (Task 1 — same substance; the course chapter is the tighter-voice sibling, including the same verbatim resource URIs)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (§ pmcp-course/src/openapi-graph-m365.md — heading reuse map for STRUCTURE only)
  </read_first>
  <action>
    Create pmcp-course/src/openapi-graph-m365.md with the SAME substance as the book chapter (Task 1) in the tighter course voice, reusing the course skeleton headings. Re-skin "Step 4: Authenticate to the Backend" to the passthrough governance note (D-05) with the FULL DOUBLE-LOCK narrative (admin-consent CEILING + per-user forwarded token that FURTHER restricts + no standing credentials), quoting the oauth_passthrough block from the shipped examples/contoso-m365.toml. Re-skin "Step 3: Two Kinds of Tools" to `get_customer`/`get_customer_orders`. Include the headline Code Mode query phrasing AND its deterministic result framing (from 90.2-03-SUMMARY), plus an "Exercise:" + "Key Takeaways" closing. List the three resource URIs EXACTLY as in the shipped config. At most ONE sentence on client-credentials.
    Add the SUMMARY.md nav line after pmcp-course/src/SUMMARY.md:78, e.g. `- [OpenAPI over Microsoft Graph (Contoso M365)](./openapi-graph-m365.md)`.
    mdbook-safe fences only.
  </action>
  <verify>
    <automated>cd pmcp-course && mdbook build</automated>
  </verify>
  <acceptance_criteria>
    - `cd pmcp-course && mdbook build` exits 0
    - `grep -c 'oauth_passthrough' pmcp-course/src/openapi-graph-m365.md` returns >= 1
    - `grep -c 'get_customer' pmcp-course/src/openapi-graph-m365.md` returns >= 1 AND `grep -c 'get_customer_orders' ...` returns >= 1
    - `grep -Ec 'last 3 months|bought more than 100|>100' pmcp-course/src/openapi-graph-m365.md` returns >= 1
    - FULL double-lock governance (review item 7): `grep -Eic 'admin consent|bounded scope|ceiling' pmcp-course/src/openapi-graph-m365.md` returns >= 1 AND `grep -Eic 'per-user|forwarded token|the user.?s (own )?token|further restrict' pmcp-course/src/openapi-graph-m365.md` returns >= 1 AND `grep -Eic 'no standing credential|holds no .* credential|act as the calling user' pmcp-course/src/openapi-graph-m365.md` returns >= 1
    - Resource-URI cross-check (review item 6): `for u in $(grep -Eo '(docs://contoso-m365[^ )"`]*|code-mode://learnings)' pmcp-course/src/openapi-graph-m365.md | sort -u); do grep -qF "$u" crates/pmcp-openapi-server/examples/contoso-m365.toml || { echo "URI in docs not in shipped config: $u"; exit 1; }; done` exits 0
    - `grep -c 'openapi-graph-m365.md' pmcp-course/src/SUMMARY.md` returns 1
    - At most ONE mention of client-credentials: `grep -ci 'client_credentials\|client-credentials' pmcp-course/src/openapi-graph-m365.md` returns <= 1 (passthrough-only boundary)
  </acceptance_criteria>
  <done>Course chapter exists with the same double-lock substance in course voice, oauth_passthrough block quoted from the shipped config, both tool names, headline query + deterministic result framing, resource URIs matching the shipped config verbatim; wired into the course SUMMARY.md; mdbook build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> reader | the governance narrative shapes how operators reason about the trust model; inaccuracy would mislead |
| committed doc -> public | published mdbook content is public; no secret may appear |
| shipped config -> docs | resource URIs/tool names/result described in docs must match the shipped artifacts; drift rots the docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.2-docs-01 | Information Disclosure | chapter config blocks | mitigate | Config snippets are quoted from the shipped examples/contoso-m365.toml which carries only `${VAR}` placeholders + the dev-guarded inline token_secret with a LOUD replace-for-production note; no real tenant/client/user credential. |
| T-90.2-docs-02 | Tampering (misinformation) | governance narrative | mitigate | The governance claims (admin-consent CEILING, per-user forwarded token that further restricts, no standing credential) are traced to the behavior proven by Plan 02/03 passthrough tests — not aspirational; the chapter references the deterministic result proven in 90.2-03-SUMMARY. |
| T-90.2-docs-03 | Tampering (doc rot) | resource URIs / tool names in chapters | mitigate | Wave-3 placement means the shipped config exists; acceptance cross-checks every doc resource URI against examples/contoso-m365.toml so a typo cannot silently rot the docs. |
</threat_model>

<verification>
- `make book` exits 0; `cd pmcp-course && mdbook build` exits 0.
- All required-content greps pass in both chapters (full double-lock governance, headline query + result framing); both wired into their SUMMARY.md.
- Every doc resource URI matches a URI in the shipped examples/contoso-m365.toml (cross-check exits 0).
</verification>

<success_criteria>
- Two dedicated chapters explain the full double-lock governance + Excel narrative, quote the shipped oauth_passthrough block, both tool names, and the headline query with its deterministic result; both build cleanly and are nav-wired.
- Resource URIs match the shipped config verbatim (no doc rot); no real secret in either chapter; at most one client-credentials mention each.
</success_criteria>

<output>
After completion, create `.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-04-SUMMARY.md` noting the chapter numbers/nav placement, confirming both books build, confirming the config blocks were quoted from the shipped examples/contoso-m365.toml, and that the resource-URI cross-check passed.
</output>
</content>
