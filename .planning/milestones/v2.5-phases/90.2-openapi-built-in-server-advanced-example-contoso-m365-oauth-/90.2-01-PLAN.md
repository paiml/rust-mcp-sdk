---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json
  - crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml
  - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml
  - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml
autonomous: true
requirements: [P902-SPEC, P902-CONFIG, P902-FIXTURE]
must_haves:
  truths:
    - "A single canonical workbook dataset (contoso-m365-workbook.json) is the ONE source of the Customers/Orders rows + the pinned reference date + the headline match/non-match expected set; Plans 02 and 03 LOAD it rather than re-typing rows"
    - "contoso-m365.toml parses via from_toml_strict_validated"
    - "Config uses oauth_passthrough backend auth (not api_key)"
    - "Two script tools get_customer and get_customer_orders each take a customer_id parameter"
    - "Curated Graph spec parses via OpenApiSchema::parse and contains only the range-read op (no write, no file-listing)"
    - "Workbook layout is date-pinned to a fixed reference date with >=1 headline match and >=1 non-match"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json"
      provides: "CANONICAL deterministic dataset: ref_date, customers rows, orders rows, per-id range addresses, expected headline set — loaded by Plans 02/03 (kills 4-way drift)"
      contains: "ref_date"
    - path: "crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml"
      provides: "oauth_passthrough config + two script tools + resources + prompt + code_mode"
      contains: 'type = "oauth_passthrough"'
    - path: "crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml"
      provides: "Curated Graph Excel range-read OpenAPI spec"
      contains: "getWorksheetRange"
    - path: "crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml"
      provides: "Tool-correctness scenario for the parity replay"
      contains: "get_customer"
  key_links:
    - from: "contoso-m365-workbook.json"
      to: "contoso-m365.toml script addresses + contoso-m365-scenarios.yaml asserted values"
      via: "the toml/yaml row+address values are TRANSCRIBED FROM the canonical json (not invented independently)"
      pattern: "range\\(address="
    - from: "contoso-m365.toml [[prompts]] include_resources"
      to: "contoso-m365.toml [[resources]] uri"
      via: "exact URI string match"
      pattern: "docs://contoso-m365"
    - from: "get_customer script"
      to: "Graph range-read path"
      via: "customer_id -> row -> range(address=...) built in JS"
      pattern: "range\\(address="
---

<objective>
Author the canonical Contoso M365 workbook dataset PLUS the three fixture files that are the foundation for every downstream test, example, and doc. The new canonical artifact `tests/fixtures/contoso-m365-workbook.json` is the SINGLE source of the Customers/Orders rows, the pinned reference date, the per-id Excel range addresses, and the expected headline match/non-match set — Plans 02 (wiremock parity) and 03 (Code Mode) LOAD it instead of re-typing rows, and Plan 04's chapters quote the deterministic result derived from it. This kills the 4-way drift (config / parity-mock / code-mode-mock / docs) the reviewers flagged. The `oauth_passthrough` config, the curated Graph range-read OpenAPI spec, and the replay scenario are authored consistently FROM that canonical dataset.

Purpose: Mirror the 90.1 london-tube fixture set, re-skinned to Contoso/Graph/passthrough, with a canonical dataset added so the deterministic data cannot drift across artifacts. No `src/` change — this phase only CONFIGURES machinery that already ships from Phase 90.
Output: `tests/fixtures/contoso-m365-workbook.json` (canonical), `tests/fixtures/contoso-m365.toml`, `tests/fixtures/contoso-m365-api.yaml`, `tests/fixtures/contoso-m365-scenarios.yaml`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-SPEC.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-CONTEXT.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-RESEARCH.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the CANONICAL contoso-m365-workbook.json dataset (rows + pinned ref date + per-id addresses + expected headline set)</name>
  <files>crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json</files>
  <read_first>
    - .planning/phases/90.2-.../90.2-SPEC.md (P902-FIXTURE — date-pinned, >=1 match + >=1 non-match; D-01 workbook columns; D-02/D-03 row + orders-block addressing)
    - .planning/phases/90.2-.../90.2-CONTEXT.md (D-01 columns: Customers = customer_id/name/segment/region, Orders = order_id/customer_id/order_date/amount; D-06 Contoso framing — Northwind Traders / Fabrikam / Tailspin Toys)
    - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml (the 90.1 analog this phase re-skins — read for the addressing conventions the script tools expect)
  </read_first>
  <action>
    Create tests/fixtures/contoso-m365-workbook.json as the ONE canonical, machine-readable, deterministic dataset that every other artifact in this phase TRANSCRIBES FROM (config addresses, wiremock rows, Code Mode mock rows, expected set, docs result). Keep it small and pinned.
    Top-level keys:
      - `ref_date`: a PINNED ISO `YYYY-MM-DD` string (e.g. `"2026-05-30"`) — the fixed "now" the headline query's trailing-3-month window is measured from. This is the SINGLE place the reference date is defined; Plan 03's `const REF` copies this exact value.
      - `window_start`: the PINNED lower-bound ISO date = ref_date minus 3 calendar months, computed by hand and written as a literal (e.g. ref `"2026-05-30"` -> `"2026-02-28"`) so the boundary is unambiguous and Plan 03 can assert against it. Choose a ref_date whose minus-3-months is calendar-unambiguous (avoid the 31->30/28 trap; e.g. a ref day <= 28).
      - `drive_id`, `customers_item_id`, `orders_item_id`: the pinned Graph drive/item id PLACEHOLDER strings the addresses use (e.g. `"CONTOSO_DRIVE"`, `"CUSTOMERS_ITEM"`, `"ORDERS_ITEM"`).
      - `customers`: an array of objects `{customer_id, name, segment, region, row, address}` where `row` is the Customers-sheet row (header offset applied per D-02) and `address` is the single-customer range like `"A8:D8"`. Use D-06 names (Northwind Traders, Fabrikam, Tailspin Toys, plus a couple more for >=1 match and >=1 non-match). Include the `all_customers_address` block address as a sibling top-level key (the full Customers table range the Code Mode HEADLINE reads at once).
      - `orders`: an array of `{order_id, customer_id, order_date, amount}` laid out grouped/sorted by customer_id (D-03 — each customer's orders form a contiguous block). For each customer include an `orders_block` mapping `customer_id -> address` (the contiguous Orders range for that id) as a top-level `orders_blocks` object, plus an `all_orders_address` block address (the full Orders table the HEADLINE reads at once).
      - `expected_headline_set`: the SORTED array of `customer_id`s whose summed `amount` over orders with `order_date` in `[window_start, ref_date]` is `> 100` — computed by hand from the rows above. MUST contain >=1 id (a match) AND the rows MUST contain >=1 customer that is excluded (a non-match: either no in-window orders, or in-window total <= 100). Include EXACTLY one BOUNDARY customer whose newest qualifying order_date == window_start (just inside) and verify it is classified IN; and ensure at least one customer's only order is dated just before window_start (just outside) and is classified OUT — so the date-window logic is provably exercised at the edge.
    NO secrets in this file (it is inert business data). NO `Date`-derived values — every date is a hand-written literal.
    This JSON is the source of truth: Tasks 2/3 of THIS plan, and Plans 02/03, transcribe addresses/rows/REF/expected-set FROM here; they MUST NOT invent independent values.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json')); assert d['ref_date'] and d['window_start'] and d['expected_headline_set'] and d['customers'] and d['orders']; print('OK', d['ref_date'], d['expected_headline_set'])"</automated>
  </verify>
  <acceptance_criteria>
    - The JSON parses (the python verify above exits 0) and has keys `ref_date`, `window_start`, `customers`, `orders`, `orders_blocks`, `all_customers_address`, `all_orders_address`, `expected_headline_set`
    - `expected_headline_set` is non-empty AND there exists at least one customer_id in `customers` NOT in `expected_headline_set` (>=1 match, >=1 non-match): `python3 -c "import json; d=json.load(open('crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json')); ids={c['customer_id'] for c in d['customers']}; s=set(d['expected_headline_set']); assert s and (ids - s), 'need >=1 match and >=1 non-match'"` exits 0
    - `window_start` equals `ref_date` minus 3 calendar months written as a literal (recorded + justified in SUMMARY); the ref day is <= 28 (no month-length ambiguity)
    - A boundary customer with newest qualifying order_date == `window_start` IS in `expected_headline_set`; a customer whose only order is dated < `window_start` is NOT (record both in SUMMARY)
    - NO secrets / no Date: `grep -E 'eyJ|new Date|Date\.now' crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json` returns nothing
  </acceptance_criteria>
  <done>contoso-m365-workbook.json exists as the single canonical dataset: pinned ref_date + window_start, full Customers/Orders rows with per-id + block range addresses, and the hand-computed expected_headline_set (>=1 match, >=1 non-match, boundary verified). Tasks 2/3 and Plans 02/03 transcribe from it.</done>
</task>

<task type="auto">
  <name>Task 2: Author contoso-m365.toml config (oauth_passthrough + two script tools + resources + prompt + code_mode), TRANSCRIBED from the canonical workbook</name>
  <files>crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml</files>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json (Task 1 — THE canonical source for drive/item ids, the customer_id->row->address mapping, and the orders-block addressing the scripts must reproduce)
    - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml (THE analog — copy structure verbatim: [server]/[backend]/[backend.auth]/[backend.http]/[[tools]] with [[tools.parameters]] + [tools.annotations]/[[resources]] x3/[[prompts]] x1/[code_mode])
    - crates/pmcp-server-toolkit/src/http/auth.rs:122-130 (AuthConfig::OAuthPassthrough — target_header defaults "Authorization", required defaults true)
    - crates/pmcp-server-toolkit/src/config.rs:578-784 (deny_unknown_fields exact field names: ParamDecl, ToolDecl, ResourceDecl, PromptDecl, AnnotationsDecl)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (Pattern Assignments § contoso-m365.toml — the re-skin diffs)
  </read_first>
  <action>
    Re-skin london-tube.toml into tests/fixtures/contoso-m365.toml per D-01/D-02/D-03/D-06. The drive/item ids and the customer_id->row->address arithmetic MUST match contoso-m365-workbook.json exactly (the script's computed address for `C007` must equal that customer's `address` in the canonical json).
    Replace the api_key backend with: `[backend] base_url = "https://graph.microsoft.com/v1.0"` and `[backend.auth] type = "oauth_passthrough"` + `target_header = "Authorization"` + `required = true`.
    Define BOTH tools as SCRIPT tools (the connector does literal str::replace, no math — per D-04 / RESEARCH Pattern 2):
      Tool 1 `get_customer` with one required `[[tools.parameters]]` `name = "customer_id"`, `type = "string"`; script parses `args.customer_id` (e.g. 'C007' -> index 7), computes the Customers-sheet row = index + header-offset (the SAME offset the canonical json's `row`/`address` use), builds `addr` = `A{row}:D{row}`, binds the result of `await api.get` against path `/drives/CONTOSO_DRIVE/items/CUSTOMERS_ITEM/workbook/worksheets/Customers/range(address='${addr}')?$select=values` to a `const` before `return`, returns `.values`.
      Tool 2 `get_customer_orders` same shape, one required `customer_id` param; script reads the contiguous Orders block for that id (D-03 — the block address must match the canonical json's `orders_blocks[customer_id]`), worksheet `Orders`, path `/drives/CONTOSO_DRIVE/items/ORDERS_ITEM/workbook/worksheets/Orders/range(address='${ordersAddr}')?$select=values`.
      Both tools carry `[tools.annotations]` with `read_only_hint = true`, `idempotent_hint = true`, `open_world_hint = true`, `cost_hint = "low"`.
    NO `Date` use anywhere in either script (the JS engine has no Date builtin — RESEARCH Pitfall 1/A4). Bind every `api.get` to a `const` before `return` (engine-accuracy rule from Phase 90 Plan 05 — RESEARCH Pattern 2).
    Add three `[[resources]]` with Contoso URIs `docs://contoso-m365/schema`, `docs://contoso-m365/examples`, `code-mode://learnings` (Customers/Orders sheet schema, sample request/response payloads, Code Mode learnings). Add one `[[prompts]]` `start_code_mode` whose `include_resources` lists those three URIs EXACTLY (Pitfall 3 — a typo'd URI fails assembly). These exact URI strings are also greppe'd by Plan 04's chapters (a typo here rots the docs — see Plan 04 cross-check); record them verbatim in the SUMMARY.
    Add `[code_mode]` with `enabled = true`, dev-guarded inline `token_secret = "contoso-m365-parity-dev-secret-32bytes"`, `allow_inline_token_secret_for_dev = true`.
    Use Contoso framing values (D-06): customer names Northwind Traders / Fabrikam / Tailspin Toys etc.; SharePoint "Sales" site, `Customers.xlsx` in resource docs.
    SECRET RULE (clarified per review item 7): real tenant/client/secret VALUES are forbidden; `${VAR}` placeholders ARE allowed and expected. So: any tenant_id / client_id / client_secret appears ONLY as a `${VAR}` placeholder (e.g. `${TENANT_ID}`), never as a literal GUID or secret. The ONLY non-placeholder literal credential value permitted is the dev-guarded inline `token_secret`, which is explicitly flagged `allow_inline_token_secret_for_dev = true`.
  </action>
  <verify>
    <automated>echo "deferred: config validity is asserted by Plan 02's contoso_m365_fixture test (from_toml_strict_validated). This task only authors the fixture — the grep acceptance_criteria below verify its structure."</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type = "oauth_passthrough"' crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml` returns 1
    - `grep -c 'name = "get_customer"' ...contoso-m365.toml` returns 1 AND `grep -c 'name = "get_customer_orders"' ...` returns 1
    - `grep -c 'name = "customer_id"' ...contoso-m365.toml` returns 2 (one per tool)
    - `grep -c 'range(address=' ...contoso-m365.toml` returns >= 2 (both script tools template a range address)
    - `grep -c 'allow_inline_token_secret_for_dev = true' ...contoso-m365.toml` returns 1
    - `grep -c 'docs://contoso-m365' ...contoso-m365.toml` returns >= 2 (resource defs + prompt include_resources)
    - No Date use: `grep -E 'Date\.|new Date' ...contoso-m365.toml` returns nothing
    - No real secret value (placeholders OK): every tenant/client/secret reference is a `${VAR}` placeholder, and there is NO literal GUID-shaped or JWT-shaped credential. Falsifiable: `grep -Eo '\$\{[A-Z_]+\}' ...contoso-m365.toml` lists only placeholder names; `grep -E 'eyJ[A-Za-z0-9_-]{8,}' ...contoso-m365.toml` (JWT-shaped) returns nothing; the ONLY non-placeholder credential literal present is the dev-guarded `token_secret` line.
    - The script's computed address for a sample id matches that id's `address` in contoso-m365-workbook.json (cross-checked in SUMMARY)
    - The Task-3 + Plan-02 fixture-validity test will assert `from_toml_strict_validated` accepts it and both tools `is_script_tool()`
  </acceptance_criteria>
  <done>contoso-m365.toml exists with oauth_passthrough auth, both script tools (each customer_id-keyed, building a range address in JS matching the canonical workbook), three resources, one start_code_mode prompt with exact-URI include_resources, and dev-guarded code_mode; no real secret value (placeholders only); no Date use.</done>
</task>

<task type="auto">
  <name>Task 3: Author contoso-m365-api.yaml curated Graph spec (decide OData path form in THIS task) + contoso-m365-scenarios.yaml replay scenario, both TRANSCRIBED from the canonical workbook</name>
  <files>crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml, crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml</files>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-workbook.json (Task 1 — THE canonical source for the customer rows/addresses the scenario contains-assertions must match)
    - crates/pmcp-openapi-server/tests/fixtures/london-tube-api.yaml (analog — 55-line minimal OpenAPI 3.0 with in:path + in:query params)
    - crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml (analog — list_tools/list_resources/list_prompts + validate_code + per-tool tool_call steps with type:contains assertions)
    - crates/pmcp-openapi-server/tests/parity_replay.rs:165-179 (how london-tube parses its spec via OpenApiSchema::parse + operation_for — used to DECIDE the path form in this task)
    - .planning/phases/90.2-.../90.2-RESEARCH.md (Code Examples § curated Graph spec + Assumption A1 fallback; Open Question 1)
  </read_first>
  <action>
    Author tests/fixtures/contoso-m365-api.yaml as a minimal OpenAPI 3.0 doc (mirror london-tube-api.yaml): `servers: [{url: https://graph.microsoft.com/v1.0}]`; ONE GET path declaring the Graph Excel range-read op with `operationId: getWorksheetRange`, parameters `drive-id`/`item-id`/`worksheet-id`/`address` as `in: path required: true type: string` and `$select` as `in: query required: false`; a single `"200"` response describing a workbookRange object. NO write op, NO SharePoint file-listing op (P902-SPEC boundary).
    OData path DECISION — make it crisp and EARLY, IN THIS TASK (review item 4, both reviewers):
      1. Write the PRIMARY path string `/drives/{drive-id}/items/{item-id}/workbook/worksheets/{worksheet-id}/range(address='{address}')`.
      2. IMMEDIATELY run a smoke parse: `cargo test -p pmcp-openapi-server --test parity_replay 2>/dev/null` is the london-tube harness — instead, do a direct check by adding a throwaway `dbg` is not allowed; use the existing parser entrypoint via a one-off: run the Plan-02-style assertion early by invoking `OpenApiSchema::parse` over this file. Concretely: if a quick parse rejects the literal parens/quotes, DO NOT defer — apply the fallback in THIS task.
      3. FALLBACK (decided here, not later): per Gemini, prefer keeping the `[[tools]]` script interface clean — the SCRIPT in contoso-m365.toml already builds the full `range(address='...')` URL string and passes it to `api.get`, so the curated SPEC may use the simplified path `/drives/{drive-id}/items/{item-id}/workbook/worksheets/{worksheet-id}/range` with `address` documented as a query param WITHOUT changing the tool interface. The spec is CONTEXT-only (tools dispatch from the `[[tools]]` script, not the spec — assemble.rs:266-291), so the simplified spec path still satisfies P902-SPEC while the runtime script still calls the real Graph OData path.
      4. RECORD in the SUMMARY which path form shipped (PRIMARY OData parens vs FALLBACK plain `/range` + query `address`) and note that the script's runtime URL is unchanged either way.
    Author tests/fixtures/contoso-m365-scenarios.yaml (mirror london-tube-scenarios.yaml): `list_tools` step asserting both tool names present; `list_resources` + `list_prompts` discovery steps; per-tool `tool_call` steps — `get_customer` with `arguments: {customer_id: "<a match id from the canonical json>"}` asserting `type: contains` value = that id's `name` in contoso-m365-workbook.json, and `get_customer_orders` with the same id asserting a known order value FROM the canonical json. Do NOT add a computed-result assertion to a code-mode step here (RQ-2 — the deterministic headline result lives in the Plan 03 Rust test; the YAML validate_code/tool_call steps assert success/contains only).
    Keep ALL scenario asserted values TRANSCRIBED from contoso-m365-workbook.json (same ids/names/addresses), since Plan 02's wiremock mock returns those exact rows for those exact addresses.
  </action>
  <verify>
    <automated>test -f crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml && test -f crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getWorksheetRange' ...contoso-m365-api.yaml` returns 1
    - `grep -Ec 'in: path' ...contoso-m365-api.yaml` returns >= 3 (drive-id/item-id/worksheet-id at minimum)
    - NO write/list op: `grep -Ei 'post:|put:|patch:|delete:|listChildren|/children' ...contoso-m365-api.yaml` returns nothing
    - `grep -c 'get_customer' ...contoso-m365-scenarios.yaml` returns >= 1 AND `grep -c 'get_customer_orders' ...` returns >= 1
    - The scenario customer_id + asserted name/order value EXACTLY match a row in contoso-m365-workbook.json (cross-checked in SUMMARY)
    - The SUMMARY records the OData path DECISION made IN THIS TASK (PRIMARY parens vs FALLBACK plain `/range`), with the parse result that drove it; Plan 02 Task 1 re-asserts `OpenApiSchema::parse` accepts whichever form shipped
  </acceptance_criteria>
  <done>Curated Graph spec exists (range-read op only, no write/list), with the OData path form DECIDED and recorded in this task; replay scenario exists with both tool_call steps using a canonical-json customer_id and contains-assertions transcribed from the canonical workbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo commit -> published crate | tests/ is excluded from the tarball; these fixtures stay test-only, but committed-to-git literals are still public |
| config -> backend (Graph) | the only credential path is the per-user forwarded token; the config holds NO standing credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.2-secrets-01 | Information Disclosure | contoso-m365.toml (committed fixture) | mitigate | Real tenant/client/secret VALUES are forbidden; only `${VAR}` placeholders are used for them (placeholders are explicitly allowed). The single non-placeholder credential literal is the dev-guarded inline `token_secret` flagged `allow_inline_token_secret_for_dev = true` (mirrors london-tube). Acceptance greps assert no JWT-shaped/literal credential beyond the dev-guarded secret. |
| T-90.2-content-01 | Tampering | canonical workbook json + scenario assertions | accept | Inert static data authored by us in ONE canonical file; never executed; deterministic pinned reference date + window_start. Downstream artifacts transcribe from it (no independent drift). No external input shapes it. |
| T-90.2-passthrough-01 | Elevation of Privilege | [backend.auth] type | mitigate | `oauth_passthrough` means the server holds no standing credential — it can only act AS the calling user; `target_header` is operator-fixed in committed config (client controls only the VALUE). Proven at runtime in Plan 02/03. |
</threat_model>

<verification>
- The canonical contoso-m365-workbook.json parses, has >=1 headline match and >=1 non-match, and a verified boundary customer at window_start.
- `from_toml_strict_validated` (run by Plan 02 Task 1) accepts contoso-m365.toml; both tools `is_script_tool()`; auth is `OAuthPassthrough`.
- `OpenApiSchema::parse` (Plan 02 Task 1) accepts the shipped spec path form (primary OData or documented fallback decided in Task 3).
- All acceptance greps above pass; config/scenario values transcribe from the canonical json.
</verification>

<success_criteria>
- Four fixture files exist; the canonical workbook json is the single source of rows/dates/addresses/expected-set; config uses oauth_passthrough + two customer_id-keyed script tools + 3 resources + 1 prompt + dev-guarded code_mode; curated spec contains only the range-read op; scenario exercises both tools with values transcribed from the canonical json.
- No real secret value committed (placeholders OK); no `Date` use in any script.
- Workbook layout pinned to a reference date + window_start with >=1 headline match and >=1 non-match and a verified boundary (the exact set asserted by Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-01-SUMMARY.md`. RECORD: (a) the canonical workbook json path + its `ref_date`/`window_start` + the customer_id->row/address mapping convention, (b) the exact workbook rows (ids, names, segments, regions, order ids/dates/amounts) including which customers match/don't-match the headline query AND the boundary customer at window_start, (c) `expected_headline_set` value (Plan 03 asserts this), (d) which spec path form shipped (primary OData vs fallback) and the parse result that drove the decision, (e) the resource URIs + prompt name VERBATIM (Plan 02/03, the pointable example, AND Plan 04's chapters must reuse these EXACTLY). State clearly that Plans 02/03 LOAD/transcribe from the canonical json rather than re-typing rows.
</output>
</content>
</invoke>
