---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 04
subsystem: docs
tags: [openapi, microsoft-graph, oauth-passthrough, excel, code-mode, contoso, mdbook, docs]

# Dependency graph
requires:
  - phase: 90.2-01
    provides: "contoso-m365 resource URIs + start_code_mode prompt name (verbatim), workbook framing, expected_headline_set"
  - phase: 90.2-02
    provides: "examples/contoso-m365.toml (SHIPPED pointable config) — the oauth_passthrough block + script tools + resource URIs quoted by the chapters"
  - phase: 90.2-03
    provides: "deterministic headline result [C001,C003,C005] + pinned-reference-date / trailing-3-months framing referenced by the chapters"
provides:
  - "pmcp-book/src/openapi-graph-m365.md — Chapter 12.12, the Contoso M365 OpenAPI/Graph advanced-example book chapter (double-lock governance + Excel narrative + two tools + headline query)"
  - "pmcp-course/src/openapi-graph-m365.md — the course-voice sibling chapter with Exercise + Key Takeaways"
affects: [90.2-pointable-example]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Docs quote the SHIPPED examples/contoso-m365.toml (oauth_passthrough block + resource URIs verbatim) — no PATTERNS.md aspiration; acceptance cross-checks every doc resource URI against the shipped config so a typo cannot rot the docs"
    - "Double-lock governance narrative: admin-consent CEILING AND per-user forwarded token that FURTHER restricts AND no standing credential (server acts only as the calling user)"

key-files:
  created:
    - pmcp-book/src/openapi-graph-m365.md
    - pmcp-course/src/openapi-graph-m365.md
  modified:
    - pmcp-book/src/SUMMARY.md
    - pmcp-course/src/SUMMARY.md

key-decisions:
  - "Book chapter numbered 12.12, a sibling immediately after the existing 12.11 OpenAPI chapter in pmcp-book/src/SUMMARY.md (nav line: `- [Chapter 12.12: OpenAPI over Microsoft Graph (Contoso M365)](openapi-graph-m365.md)`)"
  - "Course chapter wired as an unnumbered sibling immediately after the existing `Config-Driven OpenAPI Servers: From Scaffold to Deploy` entry in pmcp-course/src/SUMMARY.md (`- [OpenAPI over Microsoft Graph (Contoso M365)](./openapi-graph-m365.md)`)"
  - "The oauth_passthrough config block + all three resource URIs are quoted VERBATIM from the SHIPPED crates/pmcp-openapi-server/examples/contoso-m365.toml (not from the fixture or PATTERNS.md); the headline result framing ([C001,C003,C005], pinned 2026-05-28 reference date, inclusive [2026-02-28,2026-05-28] window, C003 boundary IN / C004 just-outside OUT) is referenced as proven in 90.2-03-SUMMARY"
  - "D-05 governance depth honored: a 1-2 paragraph double-lock narrative (no Mermaid diagram, no threat subsection); exactly ONE oauth2_client_credentials contrast sentence in each chapter (boundary, not built)"

requirements-completed: [P902-DOCS-BOOK, P902-DOCS-COURSE]

# Metrics
duration: ~15min
completed: 2026-05-30
---

# Phase 90.2 Plan 04: Contoso M365 OpenAPI/Graph Book + Course Chapters Summary

**Two dedicated advanced-example chapters (book Chapter 12.12 + the course-voice sibling) tell the Contoso M365 story — keep your existing Excel files on Microsoft 365 and connect them to AI via MCP — leading with the `oauth_passthrough` DOUBLE LOCK (admin-consent ceiling AND per-user forwarded token that further restricts AND no standing credential, so the server acts only as the calling user), quoting the oauth_passthrough config block + the three resource URIs VERBATIM from the SHIPPED examples/contoso-m365.toml, walking the two `customer_id`-keyed script tools, and closing on the deterministic headline Code Mode query ("customers who bought more than 100 in the last 3 months" → [C001,C003,C005], time-stable via a pinned reference date) proven in Plan 03. Both books build clean and both chapters are nav-wired.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2
- **Files created:** 2 (+ 2 nav files modified)

## Accomplishments

- **`pmcp-book/src/openapi-graph-m365.md` (Chapter 12.12)** re-skins the analog `openapi-built-in-server.md` section flow to the Contoso/Graph/Excel story: "The Problem (Keep Your Excel Files, Connect Them to AI)" → "Outgoing Authentication — The Double Lock" (the headline) → "Two Kinds of Tools (`get_customer` / `get_customer_orders`)" → "Resources & Prompts (Code Mode Context)" → "The OpenAPI Spec Is Optional (D-03)" → "What You Built — The Headline Query".
- **`pmcp-course/src/openapi-graph-m365.md`** carries the SAME substance in tighter course voice with the course skeleton: "What You'll Learn" → "Step 4: Authenticate to the Backend — The Double Lock" → "Step 3: Two Kinds of Tools" → "Resources & Prompts" → "The Headline Query" → "Exercise: Add a Segment Filter" → "Key Takeaways".
- Both chapters quote the `[backend.auth] type = "oauth_passthrough"` block and the three resource URIs (`docs://contoso-m365/schema`, `docs://contoso-m365/examples`, `code-mode://learnings`) verbatim from the SHIPPED `crates/pmcp-openapi-server/examples/contoso-m365.toml`.
- Both wired into their SUMMARY.md as siblings of the existing OpenAPI chapter; `make book` exits 0 and `cd pmcp-course && mdbook build` exits 0.

## (a) Config blocks quoted from the SHIPPED example (confirmed)

The `oauth_passthrough` block, the two script tools (`get_customer` / `get_customer_orders` with their literal `customer_id → range-address` if-chains), and all three `[[resources]]` URIs + the `start_code_mode` prompt are transcribed from `crates/pmcp-openapi-server/examples/contoso-m365.toml` (Plan 02's SHIPPED pointable config) — NOT from PATTERNS.md and NOT from the excluded `tests/fixtures/` copy.

## (b) Resource-URI cross-check PASSED (no doc rot)

For both chapters, every `docs://contoso-m365/*` and `code-mode://learnings` URI in the chapter exactly matches a URI present in the shipped `examples/contoso-m365.toml`:

```
for u in $(grep -Eo '(docs://contoso-m365[^ )"`]*|code-mode://learnings)' <chapter> | sort -u); do
  grep -qF "$u" crates/pmcp-openapi-server/examples/contoso-m365.toml || exit 1
done
```

→ exits 0 for both chapters (book + course).

## (c) Full double-lock governance (review item 7) present in both

Each chapter contains, falsifiably:
- the admin-consent **CEILING** idea (`admin consent | bounded scope | ceiling`),
- the per-user **forwarded token that FURTHER restricts** idea (`per-user | forwarded token | the user's own token | further restrict`),
- the **no-standing-credential / act-as-the-calling-user** idea (`no standing credential | holds no ... credential | act as the calling user`).

Exactly ONE `oauth2_client_credentials` contrast sentence per chapter (boundary — not built).

## (d) Headline query + deterministic result framing

Both chapters use the exact headline phrasing ("customers who bought more than 100 in the last 3 months") and reference the proven framing from 90.2-03-SUMMARY: result `["C001", "C003", "C005"]`; pinned reference date `2026-05-28` giving the inclusive window `[2026-02-28, 2026-05-28]`; C003 the boundary customer classified IN, C004 just-outside (one day before the window, excluded by the date filter not the amount), C006 no orders; "stable across calendar time because the reference date is pinned" (no `Date` builtin).

## Verification

- `make book` → exits 0 (`✓ PMCP book built`).
- `cd pmcp-course && mdbook build` → exits 0.
- Book acceptance greps: `oauth_passthrough`=3, `get_customer`=5, `get_customer_orders`=3, headline=2, ceiling-lock=5, per-user-lock=6, no-standing-credential-lock=4, client-credentials=1 (≤1), SUMMARY nav=1, URI cross-check PASS, no secret literal.
- Course acceptance greps: `oauth_passthrough`=6, `get_customer`=10, `get_customer_orders`=5, headline=4, ceiling-lock=6, per-user-lock=8, no-standing-credential-lock=6, client-credentials=1 (≤1), SUMMARY nav=1, URI cross-check PASS, no secret literal.

## Threat model outcomes

- **T-90.2-docs-01 (mitigate):** config snippets are quoted from the shipped example which carries only `${VAR}`-style placeholders + a dev-guarded inline token_secret; neither chapter reproduces the dev token_secret literal nor any real tenant/client/user credential (`secret-check: NONE`).
- **T-90.2-docs-02 (mitigate):** the governance claims trace to the behavior proven by Plan 02 (forwarded-bearer passthrough) and Plan 03 (deterministic headline set); the chapters reference the proven result, not aspiration.
- **T-90.2-docs-03 (mitigate):** the resource-URI cross-check against the shipped config passes for both chapters, so a typo cannot silently rot the docs.

## Task Commits

1. **Task 1: pmcp-book/src/openapi-graph-m365.md + SUMMARY nav** — `bdd37100` (docs)
2. **Task 2: pmcp-course/src/openapi-graph-m365.md + SUMMARY nav** — `1e09a074` (docs)

## Files Created/Modified

- `pmcp-book/src/openapi-graph-m365.md` — Chapter 12.12 book chapter (created)
- `pmcp-book/src/SUMMARY.md` — nav line added (modified)
- `pmcp-course/src/openapi-graph-m365.md` — course-voice chapter (created)
- `pmcp-course/src/SUMMARY.md` — nav line added (modified)

## Deviations from Plan

None — plan executed exactly as written. (The chapters quote the SHIPPED `examples/contoso-m365.toml` rather than the fixture, per the plan's Wave-3-placement guidance.)

## Known Stubs

None — both chapters are complete prose with the full governance narrative, quoted config, both tool names, and the proven headline result.

## Issues Encountered

None. (Initial Write/Edit attempts against the shared-checkout path were redirected to the worktree copy by the harness; re-applied to the worktree path with no content change.)

## User Setup Required

None — docs-only; no external service configuration; no real secrets.

## Self-Check: PASSED

- Both chapter files present on disk: `pmcp-book/src/openapi-graph-m365.md`, `pmcp-course/src/openapi-graph-m365.md`.
- Both SUMMARY navs contain `openapi-graph-m365.md` (count 1 each).
- Both task commits found in git log: `bdd37100`, `1e09a074`.
- `make book` exits 0; `cd pmcp-course && mdbook build` exits 0.
- All required-content greps + the resource-URI cross-check pass in both chapters.
- No STATE.md / ROADMAP.md changes (per parallel-execution constraint).

---
*Phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-*
*Completed: 2026-05-30*
