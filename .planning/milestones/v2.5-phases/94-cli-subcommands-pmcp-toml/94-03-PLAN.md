---
phase: 94-cli-subcommands-pmcp-toml
plan: 03
type: execute
wave: 3
depends_on: ["94-00", "94-01", "94-02"]
files_modified:
  - cargo-pmcp/src/commands/workbook/compile.rs
autonomous: true
requirements: [WBCL-01, WBCL-04]
must_haves:
  truths:
    - "`cargo pmcp workbook compile <wb.xlsx> --workflow <id> --approver <X>` runs the SEED lane (ingest→lint→synth→compile→reconcile→write) via compile_workbook for a first version, gate is N/A on the seed lane (WBCL-01, D-12)"
    - "`--approver <name>` is MANDATORY on compile; --accept cannot be satisfied without it (D-06)"
    - "`workbook compile <bundle-id>` resolves path/out_dir from pmcp.toml; bare `workbook compile` compiles ALL declared workbooks, one workbook's error does NOT abort the rest, worst-status-wins (D-05/WBCL-04/concern I)"
    - "On a gate block the CLI prints GateBlock::render() verbatim and signals a DISTINCT gate-block exit code via WorkbookExit::gate_block (Plan 02 transport), writing NOTHING (gate before write — D-07/D-10/WBCL-01)"
    - "The CLI calls library verbs (read_workbook_version / prepare_candidate / compile_workbook / gate::gate / gate::corpus::derive_corpus / accept::accept / accept::promote) and reimplements NO gate fingerprint, corpus-replay, candidate-build, or promotion logic (thin-shell invariant)"
  artifacts:
    - path: "cargo-pmcp/src/commands/workbook/compile.rs"
      provides: "CompileArgs + compile handler (seed lane via compile_workbook; gated-update lane via prepare_candidate→gate::gate→block-or-promote) + gate rendering + --accept flow + compile-all"
      contains: "pub struct CompileArgs"
      min_lines: 180
  key_links:
    - from: "cargo-pmcp/src/commands/workbook/compile.rs"
      to: "pmcp_workbook_compiler::{compile_workbook, prepare_candidate, read_workbook_version, gate::gate, gate::accept::accept}"
      via: "seed-lane / gated-update-lane (prepare_candidate→gate::gate→block-or-promote) / accept verb calls"
      pattern: "compile_workbook|prepare_candidate|gate::gate|accept::accept|::accept\\("
    - from: "cargo-pmcp/src/commands/workbook/compile.rs"
      to: "GateBlock::render() + WorkbookExit::gate_block"
      via: "print the library's gate-block text verbatim then signal exit code 2"
      pattern: "\\.render\\(\\)"
---

<objective>
Implement the `workbook compile` handler (WBCL-01) — the BA's primary verb. It orchestrates the
Phase 93 library: first-version seed lane (`compile_workbook`, no gate, D-12) vs gated-update lane
(`prepare_candidate` → `gate::gate` → block-or-`accept::promote`). The gate runs BEFORE any write.
It surfaces the `--accept --approver --effective-date` flow (D-07), resolves bundle-ids/compile-all
through `pmcp.toml` (D-05/WBCL-04), and renders the gate block + findings in human/JSON (D-09) with
the errors-fail / warnings-pass / gate-block-distinct exit codes (D-10, via the Plan-02 WorkbookExit
transport).

This plan adds NO compiler logic. Every verb already exists in `pmcp-workbook-compiler` — the two
seams the gated lane needs (`read_workbook_version` for the workbook-declared version, and
`prepare_candidate` for the gated-update candidate build) are PUBLIC API as of Plan 94-00, so the
gated lane is now a thin shell and ships THIS phase (the prior blocking decision is resolved: the
library-facade was added in 94-00).
Purpose: One command the BA learns for both build and approval; the gate literally prints the next action.
Output: `cargo-pmcp/src/commands/workbook/compile.rs` (replaces the Plan-02 neutral handler).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md
@.planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md
@cargo-pmcp/src/commands/workbook/config.rs
@cargo-pmcp/src/commands/workbook/lint.rs

<interfaces>
From Plan 00 (`pmcp-workbook-compiler` library seams — PUBLIC as of 94-00):
- `read_workbook_version(workbook_path: &Path) -> Result<String, CompileError>` — the workbook-declared version
  (D-02/D-11: never from toml/flag). Errors loudly if the workbook declares none.
- `prepare_candidate(workbook_path: &Path, workflow: &str) -> Result<Candidate, CompileError>` — builds the gated-
  update candidate WITHOUT writing. `Candidate { ir, dag, manifest, computed: BTreeMap<String,f64>,
  candidate_workbook_hash, parser_equivalence, layout, version }`; its fields line up 1:1 with `PromoteInputs`.
- `write_gate_marker` exists too (used by 94-04, not here).

From Plan 01 (`workbook/config.rs`): `PmcpToml::load(&Path) -> Result<Option<PmcpToml>>`,
`PmcpToml::resolve(&str) -> Result<&WorkbookEntry>`, `PmcpToml::all_entries() -> &[WorkbookEntry]`,
`WorkbookEntry { path, bundle_id, out_dir }`, `validate(project_root)`.
From Plan 02 (`workbook/lint.rs`): `format_lint_report(&LintReport, &str) -> Result<String>`,
`print_lint_report(&LintReport, &str, bool) -> Result<()>`, `lint_exit_code(&LintReport) -> i32` — REUSE
for the lint phase inside compile (do not re-render).
From Plan 02 (`workbook/mod.rs`): `EXIT_OK`/`EXIT_ERROR`/`EXIT_GATE_BLOCK` constants and
`WorkbookExit::gate_block(message) -> WorkbookExit` (the typed error main.rs downcasts to reach
`std::process::exit(2)`). On a gate block the handler prints `block.render()` THEN returns
`Err(anyhow::Error::new(super::WorkbookExit::gate_block(block.render())))` so the DISTINCT code 2 reaches
the shell (D-10) — a plain `bail!` would collapse to 1.

Library verbs (re-exported from `pmcp-workbook-compiler`) — VERIFIED signatures:
- SEED LANE (first version, NO gate — D-12): `compile_workbook(workbook_path: &Path, out_root: &Path,
  workflow: &str, version: &str, approver: &str) -> Result<BundleLock, CompileError>` (lib.rs:188-203). The CLI
  passes `version` from `read_workbook_version` (NEVER a flag/toml). `CompileError` variants:
  `Ingest | Lint | Reconcile | Emit | Gate` (error.rs).
- GATED UPDATE LANE (prior baseline exists): build the candidate with `prepare_candidate(path, workflow)` (94-00),
  derive the prior-baseline corpus via `gate::corpus::derive_corpus(manifest, prior_ir, prior_dag)` (corpus.rs:359)
  and grade with `gate::gate(case: &ApprovalCase, computed: &BTreeMap<String,f64>, candidate_workbook_hash: &str,
  prev_bundle_hash: &str, change_classes: &[ChangeClass], approvals: &[ApprovalRecord]) -> GateDecision`
  (gate/mod.rs:133-179). `GateDecision::{ Pass { fingerprint }, Blocked(Box<GateBlock>) }`. On `Pass` →
  `gate::accept::promote(&EmitLane::GatedUpdate { prior_version }, out_root, &PromoteInputs{ .. from Candidate .. })`.
  The CLI assembles `PromoteInputs` by borrowing the `Candidate` fields (they line up 1:1 — 94-00 guarantees it).
- `GateBlock::render() -> String` (gate/mod.rs:79-106) — print VERBATIM on a block; carries deltas + change
  classes + the copy-pasteable `accept_command`. Do NOT reformat the deltas.
- `--accept` flow: `gate::accept::accept(case, out_root, bundle_id, computed, candidate_workbook_hash,
  prev_bundle_hash, change_classes, approver, effective_date) -> Result<(ApprovalCase, ApprovalRecord), AcceptError>`
  (accept.rs:76-116) — re-baselines + writes the fingerprint-bound ApprovalRecord ATOMICALLY.
- `gate::accept::EmitLane::{ Seed, GatedUpdate { prior_version } }` (accept.rs:123-134); `accept::promote`
  (accept.rs:204) writes the `{bundle_id}@{version}/` dir via `atomic_promote_dir`, refusing to overwrite.
- On-disk approval I/O (do NOT re-implement): `gate::governed_artifact::{write_approval, read_approvals,
  approvals_dir, atomic_promote_dir}`.
- Corpus: `gate::corpus::{derive_corpus, replay_candidate, ApprovalRecord, ApprovalCase, candidate_fingerprint,
  approval_matches}`.

Per CONTEXT D-discretion: whether ONE code path branches on "prior accepted baseline exists" or the CLI
selects the lane is Claude's call. The fingerprint is computed by the library (`candidate_fingerprint`) and
binds prev_bundle_hash + candidate_workbook_hash + region deltas — an approval can NEVER be inherited by a
different transition (T-94-03-REPLAY).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CompileArgs + the seed lane (always) and the gated-update lane (via prepare_candidate→gate::gate) with gate-before-write</name>
  <files>cargo-pmcp/src/commands/workbook/compile.rs</files>
  <behavior>
    - `compile <wb.xlsx> --workflow quote --approver alice` with NO prior baseline runs the seed lane
      (`compile_workbook`, no gate, D-12) and writes the seven-member bundle into `{workflow}@{version}/`, with the
      version read from the workbook via `read_workbook_version` (NEVER a flag/toml).
    - `--approver` omitted ⇒ clap rejects the invocation (required arg) — compile CANNOT proceed without it (D-06).
    - gated lane: a re-compile WITH a prior baseline AND an over-tolerance named-output delta + NO covering approval
      ⇒ `prepare_candidate` builds the candidate, `gate::gate` returns `Blocked`; the CLI prints `block.render()`
      verbatim and signals the gate-block code via `WorkbookExit::gate_block`, writing NOTHING (gate before write).
    - gated lane: the same blocked transition re-run with `--accept --approver alice --effective-date 2026-06-12`
      ⇒ records a fingerprint-bound ApprovalRecord, re-baselines, promotes the new version, exits 0 (D-07).
    - compile-all: bare `compile` over a multi-entry pmcp.toml builds every declared workbook; ONE workbook's
      error does NOT abort the others; the process exit is the WORST per-workbook status (concern I).
    - exit-code mapping uses the shared constants: ok⇒EXIT_OK, compile/lint error⇒EXIT_ERROR, gate-block⇒
      EXIT_GATE_BLOCK (distinct, via WorkbookExit) — unit-tested.
  </behavior>
  <read_first>
    - crates/pmcp-workbook-compiler/src/lib.rs (the `compile_workbook` signature at :188-203; the NEW PUBLIC seams
      `read_workbook_version` and `prepare_candidate` + `Candidate` added in 94-00 — read their re-export lines and the
      `Candidate` field list so the gated lane assembles `PromoteInputs` directly from a `Candidate`; the seed-lane
      internal `PromoteInputs` assembly + `accept::promote(&EmitLane::Seed, ...)` at :282-294 is the exact shape the
      gated lane mirrors with `EmitLane::GatedUpdate`)
    - crates/pmcp-workbook-compiler/src/gate/mod.rs (`gate()` :132-179, `GateDecision` :48-60, `GateBlock::render()`
      :79-106, `accept_command()` :110-115)
    - crates/pmcp-workbook-compiler/src/gate/accept.rs (`accept()` :76-116, `promote()` + `EmitLane` :118-134,
      `PromoteInputs` :170-187, `AcceptError` :37-48)
    - crates/pmcp-workbook-compiler/src/gate/corpus.rs (`derive_corpus` :359, `replay_candidate` :385,
      `candidate_fingerprint` :147, `approval_matches` :184, `ApprovalRecord` :116, `ApprovalCase` :90)
    - crates/pmcp-workbook-compiler/src/gate/governed_artifact.rs (`write_approval` :110, `read_approvals` :138,
      `approvals_dir` :29, `atomic_promote_dir` :82 — the CLI calls these, never re-implements persistence)
    - cargo-pmcp/src/commands/app.rs (the `build_all` handler skeleton + quiet-gated output at :219-255, :77)
    - cargo-pmcp/src/commands/workbook/lint.rs (Plan 02 — REUSE `format_lint_report`/`print_lint_report` +
      `lint_exit_code` for the internal lint pass; do not re-render findings)
    - cargo-pmcp/src/commands/workbook/mod.rs (Plan 02 — REUSE `EXIT_*` constants + `WorkbookExit::gate_block` for
      the distinct gate-block exit code)
    - cargo-pmcp/src/commands/workbook/config.rs (Plan 01 — `PmcpToml::load/resolve/all_entries` for id-resolution;
      pass the discovered `project_root` into `load`/`validate`)
    - cargo-pmcp/src/commands/configure/workspace.rs (`find_workspace_root` — the project-root discovery to pass
      into `PmcpToml::load`, mirroring main.rs:478-481)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md (section "compile.rs" — the seed-lane vs gated
      lane discussion, the CompileArgs flag list, and the GateBlock::render() "print verbatim" rule)
  </read_first>
  <action>
    Replace the Plan-02 `compile.rs` neutral handler. Define `#[derive(Debug, clap::Args)] pub struct CompileArgs`:
    positional `bundle_id_or_path: Option<String>` (D-03/D-05: bare path, bundle-id, or no-arg compile-all);
    `#[arg(long)] pub workflow: Option<String>` (required when a bare PATH is given — a bundle-id supplies its own
    workflow from pmcp.toml); `#[arg(long, required = true)] pub approver: String` (MANDATORY — D-06, no
    git-identity fallback); `#[arg(long)] pub accept: bool` and `#[arg(long)] pub effective_date: Option<String>`
    (D-07; `--accept` REQUIRES `--effective-date` — enforce in handler, bail! if accept && effective_date.is_none());
    `#[arg(long)] pub out: Option<PathBuf>` (out-dir override vs toml-declared, Claude's discretion on precedence;
    validate a `--out` override through the same `resolves_outside(project_root, ..)` predicate from Plan 01);
    `#[arg(long, default_value = "text")] pub format: String` (D-09). There is NO `--version` flag (D-02/D-11).

    Implement `pub fn execute(args: CompileArgs, gf: &GlobalFlags) -> Result<()>`. Discover `project_root` via
    `find_workspace_root` (fallback `current_dir`), then resolve the target set:
    (a) `Some(path-that-is-a-file)` → single ad-hoc compile (workflow from `--workflow`, bail! if absent);
    (b) `Some(bundle-id)` → `PmcpToml::load(project_root)?` then `resolve(id)?` → (path, out_dir, workflow=bundle_id);
    (c) `None` → load pmcp.toml, iterate `all_entries()` (compile-ALL, D-05). For EACH target call a single
    `compile_one(entry_or_path, &args, gf) -> Result<i32>` returning the per-workbook exit code; COLLECT all results
    (concern I — do NOT `?`-propagate the first error: catch each `compile_one` Err, print a per-workbook line,
    record its code as EXIT_ERROR, and CONTINUE the loop), then reduce to WORST-status-wins
    (`EXIT_GATE_BLOCK` > `EXIT_ERROR` > `EXIT_OK`) and surface that as the process exit (via `WorkbookExit` if the
    worst is a gate block, else `bail!` on EXIT_ERROR, else Ok).

    Inside `compile_one`: run the lint phase first (ingest + `dialect::linter::lint` + `print_lint_report` reusing
    Plan 02; if `lint_exit_code` says error, return `EXIT_ERROR` WITHOUT compiling). Read the VERSION via
    `read_workbook_version(path)` (94-00 — NEVER from pmcp.toml or a flag, D-02/D-11). Select the lane: detect
    whether a prior accepted baseline exists under `out_dir/{bundle_id}` via `read_approvals` / baseline-dir presence
    (Claude's discretion on the exact probe). If NONE → SEED LANE: call `compile_workbook(path, out_root, workflow,
    &version, &args.approver)`, map `CompileError` → anyhow with context, return `EXIT_OK` on success. If a prior
    baseline EXISTS → GATED UPDATE LANE: call `prepare_candidate(path, workflow)` (94-00 — NEVER reassemble
    `ir`/`manifest`/`computed` by hand), derive the prior corpus (`derive_corpus` over the prior baseline IR/DAG; load
    the prior `approvals` via `read_approvals`), and call `gate::gate(&case, &candidate.computed,
    &candidate.candidate_workbook_hash, prev_bundle_hash, &change_classes, &approvals)`. On
    `GateDecision::Blocked(block)`: if `args.accept` is false → print `block.render()` verbatim (text mode) or
    serialize the `GateBlock` (json mode, D-09), then return an Err carrying `WorkbookExit::gate_block(block.render())`
    so the DISTINCT code reaches the shell (D-10) — and write NOTHING. If `args.accept` is true → call
    `gate::accept::accept(...)` then `accept::promote(&EmitLane::GatedUpdate { prior_version }, out_root,
    &PromoteInputs{ ..borrowed from candidate.. })`, print a success line, return `EXIT_OK`. On
    `GateDecision::Pass { .. }` → `accept::promote(&EmitLane::GatedUpdate { prior_version }, ...)`, return `EXIT_OK`.

    Gate decorative output on `gf.should_output()` / `PMCP_QUIET`. Every function cognitive complexity ≤25 (factor
    `compile_one`, `run_seed_lane`, `run_gated_lane`, `resolve_targets` as separate fns to stay under the cap).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --lib workbook::compile</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --lib workbook::compile` passes including the exit-code mapping test
      (ok⇒EXIT_OK, error⇒EXIT_ERROR, gate-block⇒EXIT_GATE_BLOCK — D-10, using the shared constants).
    - `cargo run -p cargo-pmcp -- workbook compile x.xlsx --workflow q 2>&1 | grep -qi approver` — invoking
      WITHOUT `--approver` is rejected by clap citing the required `--approver` arg (D-06).
    - A unit/integration test asserts `--accept` without `--effective-date` `bail!`s (D-07 pairing).
    - A test asserts the gate-block render output contains the substring `--accept`, `--approver`, and
      `--effective-date` (the copy-pasteable line from `accept_command`, D-07) AND that no bundle dir is written on a
      block (gate-before-write, WBCL-01) AND that the block path returns a `WorkbookExit` whose `code == EXIT_GATE_BLOCK`.
    - `grep -c 'compile_workbook\|prepare_candidate\|gate::gate\|::accept(' cargo-pmcp/src/commands/workbook/compile.rs`
      ≥ 3 (seed lane + gated lane both call the library verbs, do not reimplement).
    - NO reimplementation of gate/promotion/candidate-build internals:
      `grep -c 'fn.*fingerprint\|fn promote\b\|fn.*atomic_promote\|fn build_ir_and_dag\|fn seed_from_inputs'
      cargo-pmcp/src/commands/workbook/compile.rs` returns 0 (the CLI never defines fingerprint/promote/replay/
      candidate-build logic — it only CALLS the library; thin-shell invariant).
    - `grep -v '^#' cargo-pmcp/src/commands/workbook/compile.rs | grep -c 'version.*=.*pmcp.toml\|--version'`
      returns 0 (version is read from the workbook via `read_workbook_version`, never from toml/a flag — D-02/D-11).
    - compile-all over a two-entry pmcp.toml where the FIRST entry errors still attempts the SECOND (concern I) —
      asserted by a test that the second workbook is attempted despite the first's failure.
    - `cargo clippy -p cargo-pmcp` zero warnings on compile.rs.
  </acceptance_criteria>
  <done>`workbook compile` orchestrates the seed lane (always) and the gated lane (prepare_candidate→gate::gate→block-or-promote) with gate-before-write, requires --approver, resolves bundle-ids + compile-all from pmcp.toml (one error never aborts the rest, worst-status-wins), prints GateBlock::render() verbatim on a block and signals the distinct gate-block code via WorkbookExit, reads the version from the workbook via read_workbook_version, and reimplements no gate/promotion/candidate-build logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA CLI invocation → recorded ApprovalRecord | the `--approver`/`--accept` act is an audit-bearing governance event |
| candidate workbook output → promoted served baseline | the gate is the trust boundary between a recompiled sheet and served truth |
| pmcp.toml `out_dir`/`--out` → bundle write target | the resolved write location (shared boundary with Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03-APPROVER | Spoofing / Repudiation | silent self-approval via `--accept` with no approver | mitigate | `--approver` is `required = true` on `CompileArgs`; `--accept` additionally requires `--effective-date`. There is NO git-identity fallback (D-06). The recorded `ApprovalRecord.approved_by` is the explicit flag value — unit-tested that accept-without-approver cannot construct. |
| T-94-03-REPLAY | Tampering | replaying an approval against a DIFFERENT candidate output | mitigate | The CLI passes the candidate's `candidate_workbook_hash` (from `prepare_candidate`) + `prev_bundle_hash` to `gate::gate`; `candidate_fingerprint` binds them + the region deltas, and `approval_matches` rejects a record whose fingerprint does not reproduce THIS transition (T-93-06-INHERIT). The CLI never hand-builds or relaxes the fingerprint (grep asserts no fingerprint/promote/candidate-build logic in compile.rs). |
| T-94-03-WRITE | Tampering | a gate block leaking a partial/ungated write | mitigate | On `GateDecision::Blocked` the handler prints `render()` then returns the `WorkbookExit::gate_block` error BEFORE any `promote`/emit call; `prepare_candidate` itself writes nothing (94-00) — gate runs before any write (WBCL-01). Test asserts no bundle dir appears after a block AND the returned code is `EXIT_GATE_BLOCK`. |
| T-94-03-PATH | Tampering / Elevation | `out_dir`/`--out` resolved write target | mitigate | Write targets resolve through `PmcpToml` (validated in Plan 01 against `project_root`); a `--out` override is run through the same `resolves_outside(project_root, ..)` containment predicate. |
| T-94-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (the only dep edge was added in Plan 01 and is offline cargo-pmcp tooling). N/A — documented, no install task. |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --lib workbook::compile` passes (exit codes via shared constants, accept pairing, gate-before-write, gated-lane block-and-promote, compile-all continue-on-error).
- `--approver` mandatory; `--accept` requires `--effective-date`.
- Gate block prints `GateBlock::render()` verbatim and signals `EXIT_GATE_BLOCK` via `WorkbookExit`.
- No fingerprint/promote/replay/candidate-build logic reimplemented in compile.rs (grep == 0).
- `cargo clippy -p cargo-pmcp` zero warnings.
</verification>

<success_criteria>
- WBCL-01 satisfied UNCONDITIONALLY for BOTH lanes: the seed lane (first version, no gate, D-12) AND the gated lane
  (prepare_candidate→gate::gate→block-or-promote, gate before any write) ship this phase.
- WBCL-04 satisfied: bundle-id resolution + bare compile-all through pmcp.toml, continue-on-error + worst-status-wins.
- D-06 (mandatory approver), D-07 (--accept flow), D-09 (human/json), D-10 (distinct exit codes via WorkbookExit) all met.
- Thin-shell invariant upheld: the CLI calls library verbs (incl. the 94-00 seams) and reimplements no
  gate/promotion/candidate-build logic.
</success_criteria>

<output>
Create `.planning/phases/94-cli-subcommands-pmcp-toml/94-03-SUMMARY.md` when done.
</output>
