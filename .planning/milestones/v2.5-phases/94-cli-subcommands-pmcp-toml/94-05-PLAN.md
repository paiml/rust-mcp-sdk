---
phase: 94-cli-subcommands-pmcp-toml
plan: 05
type: execute
wave: 4
depends_on: ["94-00", "94-01", "94-02", "94-03", "94-04"]
files_modified:
  - cargo-pmcp/tests/workbook_cli_integration.rs
  - cargo-pmcp/examples/workbook_cli_demo.rs
autonomous: true
requirements: [WBCL-01, WBCL-02, WBCL-03, WBCL-04]
must_haves:
  truths:
    - "An end-to-end test drives `workbook lint`, `workbook compile`, and `workbook emit` over a real fixture (CLAUDE.md ALWAYS: integration coverage)"
    - "The purity gate still passes after cargo-pmcp links pmcp-workbook-compiler — umya stays out of every SERVED tree — confirmed as an EXPLICIT verification step + an #[ignore]-d guard test, NOT a normal #[test] that shells make (CLAUDE.md offline-cone/purity gate)"
    - "A runnable example demonstrates the three workbook subcommands end-to-end (CLAUDE.md ALWAYS: cargo run --example)"
    - "compile-all over a multi-entry pmcp.toml builds every declared workbook (WBCL-04/D-05 end-to-end)"
  artifacts:
    - path: "cargo-pmcp/tests/workbook_cli_integration.rs"
      provides: "end-to-end CLI integration tests: lint exit codes, compile seed lane, --approver enforcement, emit hash-covered ungated marker (read_gate_marker), compile-all; + an #[ignore]-d purity-gate guard"
      contains: "workbook"
      min_lines: 120
    - path: "cargo-pmcp/examples/workbook_cli_demo.rs"
      provides: "runnable demonstration of workbook lint/compile/emit over a fixture"
      contains: "workbook"
      min_lines: 40
  key_links:
    - from: "cargo-pmcp/tests/workbook_cli_integration.rs"
      to: "cargo pmcp workbook {lint,compile,emit}"
      via: "assert_cmd / Command spawning the built cargo-pmcp binary"
      pattern: "workbook"
    - from: "make purity-check"
      to: "PURITY_CRATES (served trees only)"
      via: "explicit verification step + #[ignore] guard confirms cargo-pmcp is NOT in the served set"
      pattern: "purity-check"
---

<objective>
Close the phase with the CLAUDE.md ALWAYS requirements that the per-handler plans cannot cover alone:
(1) an end-to-end CLI integration test driving lint/compile/emit over a real fixture, (2) a confirmation
that the purity gate still passes now that cargo-pmcp links the compiler (umya must stay out of every
SERVED tree) — as an EXPLICIT verification step plus an `#[ignore]`-d durable guard, NOT a normal test
that shells `make` (slow/recursive/flaky — concern E), and (3) a runnable `cargo run --example`
demonstrating the three subcommands.

Purpose: Prove the thin CLI shell works end-to-end as a unit, and prove the new dependency edge did
NOT breach the reader-free served boundary.
Output: `cargo-pmcp/tests/workbook_cli_integration.rs`, `cargo-pmcp/examples/workbook_cli_demo.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md
@.planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md
@cargo-pmcp/src/commands/workbook/config.rs
@cargo-pmcp/src/commands/workbook/lint.rs
@cargo-pmcp/src/commands/workbook/compile.rs
@cargo-pmcp/src/commands/workbook/emit.rs

<interfaces>
Built binary: `cargo pmcp workbook {compile,lint,emit}` (Plans 02/03/04). The integration test spawns the
built `cargo-pmcp` binary. The established convention is `assert_cmd::Command::cargo_bin("cargo-pmcp")`
(`assert_cmd = "2"`, `predicates = "3"`, and `tempfile = "3"` are ALREADY dev-deps of cargo-pmcp — confirm
the exact spawn form from an existing test under `cargo-pmcp/tests/`, e.g. `cli_acceptance.rs` /
`configure_integration.rs`, including whether a leading `pmcp` arg is passed).

Fixtures — VERIFIED inventory: the compiler crate ships EXACTLY one workbook fixture,
`crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx`, plus its
`tax-calc.provenance-override.json`. This fixture is a NON-genuine-Excel cache: the PRODUCTION
`compile_workbook` REFUSES it on the freshness/provenance gate (see `reemit_golden.rs:246` — the override is
reachable only under `#[cfg(test)]` of the compiler crate, NOT from cargo-pmcp). There is NO two-version /
gate-block fixture. CONSEQUENCES for this plan:
- The lint path CAN run over `tax-calc.xlsx` (lint does not enforce provenance).
- A FULL seed-lane compile/emit over `tax-calc.xlsx` may be REFUSED by the production provenance gate from
  cargo-pmcp (the override is not reachable here). The executor MUST first determine, by running
  `cargo pmcp workbook compile tax-calc.xlsx ...` against the fixture, whether the production path accepts it.
  If it is refused, assert the REFUSAL (the security boundary working) for the compile/emit happy-path items and
  cover the seed-lane WRITE behavior via the Plan-03/04 unit tests instead — do NOT hand-author a new genuine
  workbook (that would need a real Excel save; out of scope). Record which fixtures were usable in the summary.
- The gate-BLOCK E2E (a real over-tolerance second version) is NOT constructible from the available fixtures.
  It is unit-tested in Plan 03. Do NOT add a TODO note (zero-SATD); instead document this as a known residual
  risk in the 94-05 SUMMARY (see Task 2 action) — no TODO/STUB/FIXME token anywhere in the test file.

94-00 library seams (the gated lane + the marker now ship): the gated compile lane (`prepare_candidate` →
`gate::gate` → block-or-promote) is delivered in 94-03 and the HASH-COVERED ungated marker channel
(`pmcp_workbook_compiler::write_gate_marker` / `read_gate_marker`, `evidence/gate.json` + `evidence/gate.sha256`)
in 94-04. The emit E2E asserts the marker via `read_gate_marker(bundle_dir) == (false, true)` (gated false +
digest_ok). The gate-block E2E still needs a two-version fixture (not available) — its tamper-evident marker and
gate-before-write behavior are unit-covered in 94-00/94-03/94-04; only the over-tolerance second-version E2E
remains the documented SUMMARY residual risk.

Purity gate: `make purity-check` (Makefile :499) scans only `PURITY_CRATES := pmcp-workbook-runtime
pmcp-workbook-dialect` (Makefile :496) — cargo-pmcp is NOT in that set, so linking the compiler into cargo-pmcp
is by-design safe. This plan CONFIRMS the gate still passes (no Makefile edit) as an explicit verification step
plus an `#[ignore]`-d guard (concern E — NOT a normal `#[test]` that shells `make`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the purity gate still passes after cargo-pmcp links the compiler (explicit step + #[ignore] guard)</name>
  <files>cargo-pmcp/tests/workbook_cli_integration.rs</files>
  <read_first>
    - Makefile (the `purity-check` target at :499-520 + `PURITY_CRATES`/`PURITY_WRITER_CRATES` at :496-497 —
      confirm cargo-pmcp is NOT in PURITY_CRATES so the gate does not scan it)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md (the offline-cone note in the
      "cargo-pmcp/Cargo.toml (modified)" section — cargo-pmcp is OFFLINE tooling, not served)
    - crates/pmcp-workbook-runtime/deny.toml, crates/pmcp-workbook-dialect/deny.toml (the crate-local bans the
      Layer-2 gate enforces — confirm cargo-pmcp has no such deny.toml because it is not a served crate)
  </read_first>
  <action>
    Run `make purity-check` as the LOAD-BEARING verification (this is an explicit phase verification step, not a
    routine unit test) and confirm it passes (exit 0) with the new `cargo-pmcp → pmcp-workbook-compiler` edge in
    place. The gate scans ONLY the served reader-free crates (`pmcp-workbook-runtime`, `pmcp-workbook-dialect`) —
    cargo-pmcp is offline tooling and intentionally OUT of that set, so no Makefile edit is expected. Additionally,
    ASSERT POSITIVELY that umya entered cargo-pmcp's tree (proving the dependency actually linked) AND assert it is
    ABSENT from the served crates' trees via `cargo tree` (these run in <60s and are the fast durable assertions).

    DURABLE GUARD (concern E — do NOT shell `make purity-check` from a normal `#[test]`; it is slow/recursive/
    flaky and can re-enter the test runner): add the purity confirmation to `workbook_cli_integration.rs` as an
    `#[test] #[ignore = "shells `make purity-check`; run explicitly via `cargo test -p cargo-pmcp --test workbook_cli_integration -- --ignored purity`"]`
    test that shells `make purity-check` and asserts success — `#[ignore]` keeps it out of the default `cargo test`
    run (no recursion/flake) while documenting the exact command. Complement it with NON-ignored fast `cargo tree`
    assertions: a normal `#[test]` that runs `cargo tree -p pmcp-workbook-runtime` / `-p pmcp-workbook-dialect` and
    asserts `umya`/`quick-xml` are ABSENT, and `cargo tree -p cargo-pmcp -i pmcp-workbook-compiler` shows the edge
    (non-vacuous). If the purity gate FAILS, do NOT weaken it — stop and report (a failure would mean cargo-pmcp
    was somehow added to a served set, a real boundary breach).
  </action>
  <verify>
    <automated>make purity-check && ! cargo tree -p pmcp-workbook-runtime | grep -qi umya</automated>
  </verify>
  <acceptance_criteria>
    - `make purity-check` exits 0 with the new dependency edge in place (explicit verification step).
    - `cargo tree -p pmcp-workbook-runtime` does NOT contain `umya` or `quick-xml` (served tree stays reader-free).
    - `cargo tree -p pmcp-workbook-dialect` does NOT contain `umya` or `quick-xml`.
    - `cargo tree -p cargo-pmcp -i pmcp-workbook-compiler` shows the edge (the link actually happened, non-vacuous).
    - The `make purity-check` test in the integration file carries `#[ignore]` (NOT a default-run `#[test]`) — concern E:
      `grep -c '#\[ignore' cargo-pmcp/tests/workbook_cli_integration.rs` ≥ 1 for the purity test.
    - The fast `cargo tree` negative assertions ARE a normal default-run `#[test]`.
    - No Makefile edit was required (PURITY_CRATES unchanged).
  </acceptance_criteria>
  <done>The purity gate passes with cargo-pmcp linking the compiler (explicit step); umya is present in cargo-pmcp's offline tree and absent from every served tree, with fast default-run cargo-tree assertions plus an #[ignore]-d make-purity-check guard (no recursive/flaky normal test).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end CLI integration test driving lint/compile/emit + compile-all</name>
  <files>cargo-pmcp/tests/workbook_cli_integration.rs</files>
  <behavior>
    - `workbook lint <tax-calc.xlsx>` runs and produces a deterministic exit code consistent with the fixture's
      findings (D-10); a constructed clean/warning case exits 0.
    - `workbook compile <fixture> --workflow <id> --approver alice`: assert the OUTCOME the production provenance
      gate actually produces over the available fixture — either a written `{id}@{version}/` seven-member bundle
      (WBCL-01, D-12) if accepted, OR a clean typed REFUSAL (the security boundary) if the non-genuine-Excel
      fixture is rejected. The executor determines which by running it once (see read_first fixtures note).
    - `workbook compile <fixture> --workflow <id>` WITHOUT `--approver` exits non-zero (clap rejects, D-06) —
      this is fixture-independent and ALWAYS asserted.
    - `workbook emit <fixture> --workflow <id>`: if the fixture is accepted, assert the bundle's HASH-COVERED marker via
      `pmcp_workbook_compiler::read_gate_marker(bundle_dir)` returns `(false, true)` (`gated: false` AND `digest_ok`)
      and the `UNGATED` banner appears on stderr (WBCL-03, D-08); if refused, assert the refusal (emit-marker write
      behavior is unit-covered in Plan 04).
    - bare `workbook compile` with a two-entry `pmcp.toml` attempts BOTH declared workbooks (compile-all, WBCL-04/
      D-05) — assert continue-on-error: the second entry is attempted even if the first fails.
    - `--format json` emits valid JSON on stdout (the library LintReport/changelog serde types, D-09).
  </behavior>
  <read_first>
    - cargo-pmcp/tests/cli_acceptance.rs AND cargo-pmcp/tests/configure_integration.rs (existing cargo-pmcp
      integration tests — learn the binary-spawn convention: `assert_cmd::Command::cargo_bin("cargo-pmcp")`, the
      leading `pmcp` arg form, and `tempfile`/tmpdir usage; mirror it exactly so the test runner and
      `--test-threads=1` convention hold)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx (the SOLE workbook fixture to REUSE; and its
      `tax-calc.provenance-override.json` — note the override is NOT reachable from cargo-pmcp, so the production
      path may refuse this fixture; do NOT hand-author a workbook)
    - cargo-pmcp/src/commands/workbook/config.rs (Plan 01 — write a two-entry `pmcp.toml` into a tmpdir for the
      compile-all case using the exact `[[workbook]]` schema the parser expects)
    - cargo-pmcp/src/commands/workbook/{compile,lint,emit}.rs (Plans 02/03/04 — the exact flag names + exit-code
      contract the test asserts)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md (D-05/D-06/D-08/D-09/D-10 — the behaviors to assert)
  </read_first>
  <action>
    Create `cargo-pmcp/tests/workbook_cli_integration.rs`. Using the same binary-spawn + tmpdir convention as the
    existing cargo-pmcp integration tests (`assert_cmd::Command::cargo_bin("cargo-pmcp")` + `tempfile`), write tests
    covering: (1) lint over `tax-calc.xlsx` producing a deterministic exit code + a constructed warning-only case
    exiting 0 (D-10); (2) the seed-lane compile — assert the ACTUAL production outcome over the available fixture
    (written seven-member bundle OR a clean typed refusal; the executor runs it once to learn which and asserts the
    real behavior, per the fixtures note) (WBCL-01); (3) compile WITHOUT `--approver` failing (D-06, always
    asserted); (4) emit — if the fixture is accepted, assert the emitted bundle's marker via `read_gate_marker(bundle_dir) == (false, true)`
    (hash-covered, tamper-evident — 94-00) + the `UNGATED` banner on stderr (WBCL-03/D-08), else assert the refusal; (5) bare `compile` over
    a two-entry `pmcp.toml` written into the tmpdir attempting BOTH workbooks with continue-on-error (WBCL-04/D-05);
    (6) `--format json` emitting parseable JSON on stdout (D-09). REUSE the Phase 93 fixture; copy it into the
    tmpdir as needed. Keep tests deterministic and runnable under `--test-threads=1`. Do NOT assert the gate-BLOCK
    path here — a real over-tolerance second version is NOT constructible from the available fixtures (no two-version
    fixture exists); that path is unit-tested in Plan 03. Do NOT write a TODO/STUB/FIXME comment about it
    (zero-SATD); instead, RECORD the gate-block-E2E coverage gap as a "Known residual risk" bullet in the 94-05
    SUMMARY (the durable, non-SATD place for it) noting the Plan-03 unit coverage and that a two-version fixture
    would close it.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test workbook_cli_integration -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test workbook_cli_integration -- --test-threads=1` passes.
    - The test asserts a deterministic lint exit code over the fixture AND a constructed warning-only case exits 0 (D-10).
    - The test asserts the seed-lane compile's ACTUAL outcome (written `{id}@{version}/` bundle OR clean refusal) over
      the available fixture.
    - The test asserts compile-without-`--approver` fails (D-06) — fixture-independent.
    - If the fixture is accepted, the test asserts `read_gate_marker(bundle_dir)` returns `(false, true)` (hash-covered
      `gated: false` + `digest_ok`) + the `UNGATED` banner on stderr (D-08).
    - The test asserts bare `compile` over a two-entry pmcp.toml attempts both workbooks with continue-on-error (WBCL-04/D-05).
    - `grep -E 'TODO|STUB|FIXME' cargo-pmcp/tests/workbook_cli_integration.rs` returns NOTHING (zero-SATD — concern E;
      the gate-block residual risk lives in the SUMMARY, not as a TODO).
    - `cargo clippy -p cargo-pmcp --tests` zero warnings.
  </acceptance_criteria>
  <done>An end-to-end integration test drives lint/compile/emit + compile-all over the real Phase-93 fixture, asserting the WBCL-01/02/03/04 behaviors and the D-06/D-08/D-09/D-10 decisions (asserting the fixture's ACTUAL production outcome), with the un-constructible gate-block E2E recorded as a SUMMARY residual-risk bullet (no SATD token).</done>
</task>

<task type="auto">
  <name>Task 3: Runnable workbook CLI example (CLAUDE.md ALWAYS: cargo run --example)</name>
  <files>cargo-pmcp/examples/workbook_cli_demo.rs</files>
  <read_first>
    - cargo-pmcp/examples/ (an existing cargo-pmcp example, if any, for the example convention; else a workspace
      example under examples/ for the `01_`/`02_` naming + doc-comment style)
    - cargo-pmcp/src/commands/workbook/{compile,lint,emit}.rs (the exact subcommand flags to demonstrate; the
      handler `execute` fns the example may call directly)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx (the SOLE fixture the example uses — REUSE via
      `CARGO_MANIFEST_DIR`-relative path resolution, do not author; note it may be provenance-refused on the
      production path, so the example should narrate the lint path + the seed-compile OUTCOME honestly)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md (the BA-lifecycle lens: author → first build →
      fix → build → emit — the narrative the example should walk)
  </read_first>
  <action>
    Create `cargo-pmcp/examples/workbook_cli_demo.rs` — a runnable example that demonstrates the three workbook
    subcommands end-to-end over the REUSED Phase-93 fixture (`tax-calc.xlsx`, resolved via `CARGO_MANIFEST_DIR` so
    the path is stable). It should (a) write a tmp `pmcp.toml` declaring the fixture → a bundle_id → a tmp out_dir,
    (b) invoke the lint path, (c) invoke the seed-lane compile path with an `--approver` and narrate its OUTCOME
    (written bundle or the honest provenance refusal — do not pretend a write that did not happen), (d) invoke the
    emit path showing the UNGATED banner + the hash-covered `gated: false` marker (via `read_gate_marker`) when accepted, and print a
    short narrative tying each step to the BA lifecycle (CONTEXT specifics). It MAY call the handler `execute`
    functions directly (constructing the `Args` structs) OR shell the built binary — direct handler calls keep it
    hermetic and are preferred. The example must compile and run via `cargo run -p cargo-pmcp --example
    workbook_cli_demo` and exit 0 on the happy path (an honest provenance refusal is a SUCCESSFUL demonstration of
    the boundary — the example exits 0 having narrated it). Keep functions cognitive complexity ≤25; no SATD comments.
  </action>
  <verify>
    <automated>cargo run -p cargo-pmcp --example workbook_cli_demo</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p cargo-pmcp --example workbook_cli_demo` builds and exits 0.
    - The example exercises lint, compile (seed lane, with --approver), and emit (showing UNGATED) over the reused
      fixture resolved via `CARGO_MANIFEST_DIR` — no hand-authored workbook.
    - `cargo clippy -p cargo-pmcp --examples` zero warnings.
  </acceptance_criteria>
  <done>A runnable example demonstrates the full workbook CLI surface end-to-end over the reused fixture, narrating the honest production outcome, satisfying the CLAUDE.md ALWAYS cargo-run-example requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new dependency edge → served reader-free trees | linking the compiler into cargo-pmcp must NOT pull umya into any served binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-05-PURITY | Tampering / Elevation | umya leaking into a served tree via the new edge | mitigate | `make purity-check` (unchanged, scans served crates only — run as an explicit step + an `#[ignore]`-d guard so it never runs as a slow/recursive normal test) plus FAST default-run `cargo tree` negative assertions on `pmcp-workbook-runtime`/`pmcp-workbook-dialect` confirm umya/quick-xml are ABSENT from every served tree, while present (by design) in cargo-pmcp's offline tree. Failing closed: a purity-gate failure stops the phase, never weakened. |
| T-94-05-FIXTURE | Tampering | a hand-authored fixture passing a fabricated-identity freshness gate | accept | The integration test REUSES the Phase-93 golden fixture (already provenance-validated) rather than authoring new workbooks; the umya-stamped fabricated-identity gate (WBCO-07) is the compiler's concern, not re-litigated here. The test asserts the fixture's ACTUAL production outcome (write or refusal). |
| T-94-05-SC | Tampering | npm/pip/cargo installs (dev-deps) | accept | `assert_cmd`/`tempfile`/`predicates` are ALREADY dev-deps of cargo-pmcp (Cargo.toml :71,:99,:100) — NO new dev-dep is introduced, so the Package Legitimacy Gate is N/A. Reuse the existing harness. |
</threat_model>

<verification>
- `make purity-check` passes (explicit step); served trees reader-free; cargo-pmcp links the compiler.
- `cargo test -p cargo-pmcp --test workbook_cli_integration -- --test-threads=1` passes.
- `cargo run -p cargo-pmcp --example workbook_cli_demo` exits 0.
- No TODO/STUB/FIXME tokens in the test file; the purity make-shell is `#[ignore]`-d.
- `make quality-gate` passes (fmt/clippy/build/test/audit) before commit.
</verification>

<success_criteria>
- End-to-end coverage of WBCL-01/02/03/04 via a real-fixture integration test (asserting actual production outcomes).
- Purity boundary confirmed intact after the new dependency edge (CLAUDE.md offline-cone requirement) via an explicit step + #[ignore] guard + fast cargo-tree assertions (concern E resolved — no slow/recursive normal test).
- Runnable example satisfies the CLAUDE.md ALWAYS cargo-run-example requirement.
- Zero SATD: the un-constructible gate-block E2E is a SUMMARY residual-risk bullet, not a TODO.
</success_criteria>

<output>
Create `.planning/phases/94-cli-subcommands-pmcp-toml/94-05-SUMMARY.md` when done.
</output>
