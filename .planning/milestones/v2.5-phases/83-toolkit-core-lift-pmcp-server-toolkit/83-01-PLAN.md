---
phase: 83-toolkit-core-lift-pmcp-server-toolkit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/pmcp-server-toolkit/Cargo.toml
  - crates/pmcp-server-toolkit/README.md
  - crates/pmcp-server-toolkit/src/lib.rs
  - crates/pmcp-server-toolkit/src/error.rs
  - crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml
  - crates/pmcp-server-toolkit/tests/fixtures/imdb-config.toml
  - crates/pmcp-server-toolkit/tests/fixtures/msr-vtt-config.toml
  - CLAUDE.md
autonomous: true
requirements:
  - TKIT-01
tags:
  - toolkit
  - scaffold
  - workspace
  - cargo
user_setup:
  - service: pmcp-run sibling repo
    why: "Reference config.toml fixtures (open-images, imdb, msr-vtt) live in an external repo and must be copied into crates/pmcp-server-toolkit/tests/fixtures/. RESEARCH §Assumptions Log A4 marks this MEDIUM risk. Per review R10, fallback honors `$PMCP_RUN_PATH` env-var override."
    dashboard_config:
      - task: "Provide read-access path to ${PMCP_RUN_PATH:-$HOME/Development/mcp/sdk/pmcp-run}/built-in/sql-api/servers/{open-images,imdb,msr-vtt}/config.toml, OR supply operator-snapshot copies, before Task 4 runs"
        location: "Operator filesystem"

must_haves:
  truths:
    - "`crates/pmcp-server-toolkit/` exists as a workspace member declared in root `Cargo.toml` (D-01, D-08, TKIT-01)"
    - "`crates/pmcp-server-toolkit/Cargo.toml` declares `name = \"pmcp-server-toolkit\"`, `version = \"0.1.0\"`, `edition = \"2021\"` (D-07)"
    - "`crates/pmcp-server-toolkit/Cargo.toml` uses the workspace-version dep pattern for pmcp: `pmcp = { version = \"2.8.1\", path = \"../..\", default-features = false }` (D-05, Pattern H)"
    - "Feature matrix matches D-14 exactly: `default = [\"code-mode\"]`, optional `code-mode`, `aws`, `avp`, `input-validation`, `sqlite` — and does NOT include `ddb`, `dynamo-config`, `openapi-code-mode`, `js-runtime`, `mcp-code-mode`"
    - "AWS feature-gated deps include `default-features = false` and `default-https-client` to avoid RUSTSEC-2026-0098/0099/0104"
    - "Cargo.toml `exclude = [\".planning/\", \".pmat/\", \"fixtures/\", \"tests/\", \"fuzz/\"]` is set to keep the published artifact under the 10MB crates.io limit (Pitfall 6)"
    - "Per review R5, `[dev-dependencies]` includes `trybuild = \"1\"` (Plan 02 Task 2 needs this for compile-fail tests on `SecretValue`'s negative trait invariant)"
    - "Per review R6, `[dependencies]` includes `secrecy = \"0.10\"` UNCONDITIONALLY (toolkit-owned `SecretValue` ships in Plan 02 regardless of `code-mode` feature state)"
    - "`crates/pmcp-server-toolkit/src/lib.rs` declares the public module skeleton `pub mod {auth, secrets, config, prompts, resources, code_mode, tools, sql}` with empty/stub modules whose contents land in later plans (D-15)"
    - "`crates/pmcp-server-toolkit/src/lib.rs` carries the attribution header `// Originated from pmcp-run/built-in/shared/mcp-server-common` (Pattern I)"
    - "`crates/pmcp-server-toolkit/src/error.rs` exports a `#[non_exhaustive]` `ToolkitError` `thiserror` enum and a `pub type Result<T> = std::result::Result<T, ToolkitError>` alias (Pattern B). Note: Plan 04 adds a `ConfigValidationError` enum + `ToolkitError::Validation` variant per review R8; Plan 04 also adds `ConfigValidationError::InlineSecretRejected` per review R9."
    - "All three reference fixtures `tests/fixtures/{open-images,imdb,msr-vtt}-config.toml` exist as physical copies (not symlinks) — D-17, Pattern §14"
    - "Per review R10, fixture-snapshot fallback honors `$PMCP_RUN_PATH` env-var override; halts loudly when sources are unreadable rather than fabricating fake fixtures"
    - "`CLAUDE.md` §Release & Publish Workflow lists publish order `pmcp-widget-utils → pmcp → pmcp-server-toolkit → mcp-tester → mcp-preview → cargo-pmcp` (D-08)"
    - "`cargo build -p pmcp-server-toolkit --no-default-features` succeeds"
    - "`cargo build -p pmcp-server-toolkit` succeeds (with `code-mode` default feature)"
  artifacts:
    - path: crates/pmcp-server-toolkit/Cargo.toml
      provides: "Crate manifest, version 0.1.0, slim 5-feature matrix, workspace-version pmcp dep, exclude guard, secrecy + trybuild deps (R5, R6)"
      contains: "name = \"pmcp-server-toolkit\""
    - path: crates/pmcp-server-toolkit/src/lib.rs
      provides: "Module declarations (auth, secrets, config, prompts, resources, code_mode, tools, sql), attribution header"
      contains: "pub mod"
    - path: crates/pmcp-server-toolkit/src/error.rs
      provides: "ToolkitError enum + Result<T> alias (Plan 04 extends with ConfigValidationError + Validation variant)"
      contains: "pub enum ToolkitError"
    - path: Cargo.toml
      provides: "Workspace member entry for crates/pmcp-server-toolkit slotted after pmcp, before mcp-tester (D-08)"
      contains: "crates/pmcp-server-toolkit"
    - path: CLAUDE.md
      provides: "Updated publish-order line listing pmcp-server-toolkit in slot 3"
      contains: "pmcp-server-toolkit"
    - path: crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml
      provides: "Reference Athena config snapshot for SC-2 superset verification"
      min_lines: 10
    - path: crates/pmcp-server-toolkit/tests/fixtures/imdb-config.toml
      provides: "Reference second-backend config snapshot"
      min_lines: 10
    - path: crates/pmcp-server-toolkit/tests/fixtures/msr-vtt-config.toml
      provides: "Reference third-backend config snapshot"
      min_lines: 10
  key_links:
    - from: "Cargo.toml [workspace.members]"
      to: "crates/pmcp-server-toolkit"
      via: "members array entry"
      pattern: "crates/pmcp-server-toolkit"
    - from: "crates/pmcp-server-toolkit/Cargo.toml [dependencies]"
      to: "../.. (root pmcp crate)"
      via: "workspace-version trick"
      pattern: "path\\s*=\\s*\"\\.\\.\\/\\.\\.\""
    - from: "crates/pmcp-server-toolkit/src/lib.rs"
      to: "src/error.rs + each empty module"
      via: "pub mod declarations"
      pattern: "pub mod (auth|secrets|config|prompts|resources|code_mode|tools|sql|error)"
---

<notes>
- Plan 01 ships 5 tasks (scaffold-heavy: manifest, error+lib.rs, module stubs+README+CLAUDE.md, fixture snapshots, quality gate). This is at the upper edge of the standard 2-3 task target. The decision to keep 5 instead of splitting Task 3 is intentional: every stub file shares one identical pattern (attribution header + one-line `//!` rustdoc + empty body) and the README/CLAUDE.md edits are tiny — splitting would inflate task count without making executor work clearer. Acknowledged checker warning (scope_sanity dimension).
- Per review R5 + R6, this plan adds two new deps to support later plans: `trybuild = "1"` in `[dev-dependencies]` for Plan 02's compile-fail tests, and `secrecy = "0.10"` in `[dependencies]` (unconditionally) for Plan 02's toolkit-owned `SecretValue` newtype that does NOT depend on the `code-mode` feature.
</notes>

<objective>
Scaffold the `pmcp-server-toolkit` workspace crate so every subsequent plan in Phase 83 has a place to land code. Slots the crate into the workspace per D-01/D-08, declares the slim D-14 feature matrix, emits the Pitfall 6 publish-size exclude guard, declares the flat module skeleton from D-15, and captures the three reference fixtures the Plan 04 superset verification depends on.

Per review R10, Task 4 (the cross-repo fixture snapshot) honors the `$PMCP_RUN_PATH` env-var override (default `$HOME/Development/mcp/sdk/pmcp-run`) and halts cleanly with an operator-actionable error when the source configs are unreadable — does NOT fabricate placeholder fixtures.

Per reviews R5 and R6, the manifest pre-stages dependencies that downstream plans need: `trybuild` (R5 — compile-fail tests in Plan 02), `secrecy` unconditionally (R6 — `SecretValue` ships in Plan 02 regardless of `code-mode` state).

Purpose: Per RESEARCH §"Phase-Split Risk Assessment", Plan 01 is sequential Wave-1 work that gates Plans 02–07. Without it, no later plan has a Cargo.toml to add deps to or a module to land code in.

Output: A buildable empty `pmcp-server-toolkit` crate in the workspace, with reference fixtures snapshotted into `tests/fixtures/`, and CLAUDE.md edited to reflect the new publish-order slot.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-REVIEWS.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-VALIDATION.md
@.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Analog crate manifests to mirror — extracted from workspace. Executor uses these directly. -->

From `crates/pmcp-tasks/Cargo.toml:1-30` (modern AWS feature-gated deps + workspace-version):
```toml
[package]
name = "pmcp-tasks"
version = "0.1.0"
edition = "2021"

[dependencies]
pmcp = { version = "...", path = "../.." }
# Why: default features enable rustls → ... RUSTSEC-2026-0098/0099/0104
aws-sdk-secretsmanager = { version = "1", default-features = false, features = ["default-https-client", "rt-tokio", "behavior-version-latest"], optional = true }
```

From `crates/mcp-tester/Cargo.toml:1-30` (workspace-version pattern):
```toml
[dependencies]
pmcp = { version = "2.8.1", path = "../../", features = [...] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add workspace member entry + slot manifest (with R5/R6 dep additions)</name>
  <read_first>
    - `Cargo.toml` (root) lines 1-50, 530-560 — confirm workspace.members shape, current entries, and dependencies/workspace.package shape
    - `crates/pmcp-tasks/Cargo.toml` (full, primary analog) — feature-gated AWS pattern, RUSTSEC `# Why:` comment, workspace-version, edition, license
    - `crates/mcp-tester/Cargo.toml` (full, secondary analog) — exact `pmcp = { version = "...", path = "../../", ... }` syntax verified at line 21
    - `crates/pmcp-code-mode/Cargo.toml` (full) — `[package.metadata.docs.rs]` + feature matrix shape; cross-check current `pmcp-code-mode` published version
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §1 "Cargo.toml" — the Pattern Map shows the exact `[package]`, `[features]`, `exclude`, and AWS-dep block
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` §"Decisions" D-05, D-07, D-08, D-14 — locked decisions to mirror verbatim
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-REVIEWS.md` R5 (trybuild) + R6 (secrecy unconditional)
  </read_first>
  <action>
Insert `"crates/pmcp-server-toolkit"` into the root `Cargo.toml` `[workspace.members]` array immediately AFTER `"crates/pmcp-code-mode-derive"` (or wherever `pmcp-code-mode` family ends) and BEFORE `"crates/mcp-tester"`. Keep alphabetical/grouped style of the surrounding lines.

Create `crates/pmcp-server-toolkit/Cargo.toml` with this exact shape (per PATTERNS §1):

- `[package]`: `name = "pmcp-server-toolkit"`, `version = "0.1.0"` (D-07), `edition = "2021"`, `license = "MIT OR Apache-2.0"`, `repository = "https://github.com/paiml/rust-mcp-sdk"`, `description = "Runtime library for config-driven MCP servers — auth, secrets, static resources/prompts, [[tools]] synthesizer, code-mode wiring"`, `readme = "README.md"`, `keywords = ["mcp", "toolkit", "config-driven", "code-mode", "lambda"]`, `categories = ["development-tools", "web-programming"]`.
- `exclude = [".planning/", ".pmat/", "fixtures/", "tests/", "fuzz/"]` (Pitfall 6 — verify after Task 4 with `cargo package --list -p pmcp-server-toolkit`).
- `[package.metadata.docs.rs]`: `all-features = true`, `rustdoc-args = ["--cfg", "docsrs"]`.
- `[dependencies]`:
  - `pmcp = { version = "2.8.1", path = "../..", default-features = false }` (D-05, Pattern H)
  - `pmcp-code-mode = { version = "0.5.1", path = "../pmcp-code-mode", default-features = false, optional = true }` (D-06)
  - `serde = { version = "1", features = ["derive"] }`
  - `serde_json = "1"`
  - `toml = "1"`
  - `async-trait = "0.1"`
  - `thiserror = "2"`
  - `indexmap = { version = "2.10", features = ["serde"] }`
  - `tracing = "0.1"`
  - **`secrecy = "0.10"`** (per review R6 — unconditional, since Plan 02's toolkit-owned `SecretValue` ships regardless of `code-mode` feature state)
  - Optional (feature-gated, mirror `crates/pmcp-tasks/Cargo.toml:21-26` with the `# Why:` RUSTSEC comment block VERBATIM, then add the three SDKs):
    - `aws-sdk-secretsmanager` v1 `default-features = false`, features `["default-https-client", "rt-tokio", "behavior-version-latest"]`, `optional = true`
    - `aws-sdk-ssm` same shape
    - `aws-config` v1 `default-features = false`, features `["default-https-client", "rt-tokio", "credentials-process", "sso", "behavior-version-latest"]`, `optional = true`
  - `jsonschema = { version = "0.46", default-features = false, optional = true }` for `input-validation`
  - `rusqlite = { version = "0.39", features = ["bundled"], optional = true }` for `sqlite`
- `[features]` (verbatim per D-14):
  - `default = ["code-mode"]`
  - `code-mode = ["dep:pmcp-code-mode"]`
  - `aws = ["dep:aws-config", "dep:aws-sdk-secretsmanager", "dep:aws-sdk-ssm"]`
  - `avp = ["code-mode", "pmcp-code-mode/avp"]`
  - `input-validation = ["dep:jsonschema"]`
  - `sqlite = ["dep:rusqlite"]`
- `[dev-dependencies]`:
  - `proptest = "1.7"`
  - `tokio = { version = "1", features = ["macros", "rt-multi-thread"] }`
  - **`trybuild = "1"`** (per review R5 — Plan 02 Task 2 uses this for compile-fail tests on `SecretValue`'s negative trait invariant)

DO NOT include any of these features (per D-14 anti-pattern from PATTERNS §"Anti-Patterns" #9): `ddb`, `dynamo-config`, `openapi-code-mode`, `js-runtime`, `mcp-code-mode`.

Verify with `cargo metadata --format-version 1 --no-deps | jq '.workspace_members[]' | grep pmcp-server-toolkit` returning a result.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo metadata --format-version 1 --no-deps 2>&1 | grep -q '"pmcp-server-toolkit"' && grep -q "crates/pmcp-server-toolkit" Cargo.toml && grep -qE '^name = "pmcp-server-toolkit"$' crates/pmcp-server-toolkit/Cargo.toml && grep -qE '^version = "0.1.0"$' crates/pmcp-server-toolkit/Cargo.toml && grep -qE '^secrecy = ' crates/pmcp-server-toolkit/Cargo.toml && grep -qE '^trybuild = ' crates/pmcp-server-toolkit/Cargo.toml && ! grep -qE '^(ddb|dynamo-config|openapi-code-mode|js-runtime|mcp-code-mode) = ' crates/pmcp-server-toolkit/Cargo.toml</automated>
  </verify>
  <done>
    - Root `Cargo.toml` lists `crates/pmcp-server-toolkit` between `crates/pmcp-code-mode-derive` and `crates/mcp-tester`.
    - `crates/pmcp-server-toolkit/Cargo.toml` exists with all fields in `<action>`.
    - `cargo metadata` finds `pmcp-server-toolkit` as a workspace member.
    - `secrecy = "0.10"` is in `[dependencies]` (unconditional per R6).
    - `trybuild = "1"` is in `[dev-dependencies]` (per R5).
    - None of `ddb`, `dynamo-config`, `openapi-code-mode`, `js-runtime`, `mcp-code-mode` appear as features.
    - The `exclude` field contains `.planning/`, `.pmat/`, `fixtures/`, `tests/`, `fuzz/`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Emit ToolkitError + Result alias + lib.rs module skeleton</name>
  <read_first>
    - `crates/pmcp-server-toolkit/Cargo.toml` (just-written, from Task 1)
    - `crates/pmcp-code-mode/src/lib.rs` lines 1-25 — attribution header pattern (Pattern I) + initial clippy-allow set (start small per PATTERNS §"Anti-Patterns" #10)
    - `crates/pmcp-code-mode/src/token.rs` lines 145-170 — `thiserror` `#[derive(Error)]` shape (Pattern B template)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §2 (lib.rs) and §"Shared Patterns" Pattern B
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` D-15 (flat module set) and D-16 (code_mode submodule re-export shape)
  </read_first>
  <action>
Create `crates/pmcp-server-toolkit/src/lib.rs` with:

1. Attribution header (Pattern I) — three-line comment block:
   - `// Originated from pmcp-run/built-in/shared/mcp-server-common (https://github.com/guyernest/pmcp-run)`
   - `// Promoted to rust-mcp-sdk workspace as a public SDK crate for Phase 83.`
2. Crate-level rustdoc summary describing the toolkit's role (runtime library for config-driven MCP servers; lists the public modules; cites D-15).
3. NO blanket clippy-allow lines (PATTERNS §"Anti-Patterns" #10 — start clean, add only what clippy actually flags as later plans land code).
4. Module declarations (D-15) — every module declared `pub mod NAME;` with an empty file Task 3 creates for each except `error.rs`:
   - `pub mod auth;`
   - `pub mod secrets;`
   - `pub mod config;`
   - `pub mod prompts;`
   - `pub mod resources;`
   - `pub mod tools;`
   - `pub mod sql;`
   - `pub mod builder_ext;` (per PATTERNS §13)
   - `pub mod error;`
   - `#[cfg(feature = "code-mode")] pub mod code_mode;`
5. Crate-root re-exports — DO NOT add the headline re-exports yet (those land in later plans as types appear); just `pub use error::{Result, ToolkitError};`.

Create `crates/pmcp-server-toolkit/src/error.rs` with:

- Module-level rustdoc explaining the error shape and `#[non_exhaustive]` discipline (Pattern B + Pattern C).
- `pub type Result<T> = std::result::Result<T, ToolkitError>;`
- `#[derive(Debug, thiserror::Error)] #[non_exhaustive] pub enum ToolkitError` with these initial variants:
  - `Parse(#[from] toml::de::Error)` — config TOML parse failure
  - `MissingField(String)` — required field absent during synthesis
  - `Synth(String)` — tool synthesis failure (TKIT-07)
  - `CodeMode(String)` — code-mode wiring failure (TKIT-09)
  - `Io(#[from] std::io::Error)` — filesystem failures
- Note: Plan 04 extends this enum with `Validation(#[from] ConfigValidationError)` per review R8. Plan 04 defines `ConfigValidationError` with the `InlineSecretRejected` variant per review R9. This task does NOT need to add those yet — Plan 04 will edit `error.rs` in addition to its own `config.rs`.
- Add at least one doctest on the `ToolkitError` type proving it implements `std::error::Error` (per TEST-03).

DO NOT use struct-literal syntax for any `#[non_exhaustive]` type from `pmcp` (PATTERNS §"Anti-Patterns" #5).
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo build -p pmcp-server-toolkit --no-default-features 2>&1 | tee /tmp/p83-01-t2.log && grep -E "^(error|warning):" /tmp/p83-01-t2.log; grep -q "^pub mod error" crates/pmcp-server-toolkit/src/lib.rs && grep -q "^pub enum ToolkitError" crates/pmcp-server-toolkit/src/error.rs && grep -q "non_exhaustive" crates/pmcp-server-toolkit/src/error.rs && grep -q "pub type Result" crates/pmcp-server-toolkit/src/error.rs && cargo test --doc -p pmcp-server-toolkit --no-default-features error 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <done>
    - `src/lib.rs` declares `auth`, `secrets`, `config`, `prompts`, `resources`, `tools`, `sql`, `builder_ext`, `error`, plus feature-gated `code_mode`.
    - `src/error.rs` exports `ToolkitError` (`#[non_exhaustive]`, `thiserror::Error`) with at least the 5 variants in `<action>` and a passing doctest.
    - `cargo build -p pmcp-server-toolkit --no-default-features` succeeds with zero errors (warnings about unused empty modules are acceptable; Task 3 creates them).
    - `cargo test --doc -p pmcp-server-toolkit error` passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create empty module file stubs + CRATE-README + CLAUDE.md publish-order edit</name>
  <read_first>
    - `crates/pmcp-server-toolkit/src/lib.rs` (just-written, Task 2) — confirm which modules need stub files
    - `crates/pmcp-code-mode/README.md` (if it exists, else `crates/pmcp-tasks/README.md`) — CRATE-README shape for docs.rs
    - `CLAUDE.md` §"Release & Publish Workflow" lines covering "Workspace Crates (publish order)" — find the existing ordered list with `1. pmcp-widget-utils` … `5. cargo-pmcp` shape
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` D-08 + Deferred Ideas entry "CLAUDE.md §Release & Publish Workflow edit"
  </read_first>
  <action>
For each module declared in `src/lib.rs` (auth, secrets, config, prompts, resources, tools, sql, builder_ext, code_mode), create an empty file at the corresponding path with:

- Attribution header (Pattern I) — three lines naming the analog source (e.g. `auth.rs` cites `mcp-server-common/src/auth.rs`; `tools.rs` cites "net-new code for Phase 83 TKIT-07"; etc.)
- One-line module-level rustdoc (`//!`) summarizing the module's responsibility from the Architectural Responsibility Map (RESEARCH §"Architectural Responsibility Map"):
  - `auth.rs` — `//! AuthProvider impls (StaticAuthProvider, BearerAuthProvider). Lifted from mcp-server-common.`
  - `secrets.rs` — `//! SecretsProvider trait + env/aws impls. Lifted from mcp-server-common; toolkit-owned SecretValue newtype (review R6).`
  - `config.rs` — `//! ServerConfig + sub-sections. Strict #[serde(deny_unknown_fields)] per D-13.`
  - `prompts.rs` — `//! StaticPromptHandler. Lifted from mcp-server-common; impls pmcp::PromptHandler.`
  - `resources.rs` — `//! StaticResourceHandler. Lifted from mcp-server-common; impls pmcp::ResourceHandler.`
  - `tools.rs` — `//! synthesize_from_config — [[tools]] → ToolInfo + Arc<dyn ToolHandler>. Net-new for TKIT-07.`
  - `sql/mod.rs` — `//! SqlConnector trait stub (2-method MVP per review R2) + Dialect enum.`
  - `builder_ext.rs` — `//! ServerBuilderExt trait — tools_from_config + try_tools_from_config (review R7) + code_mode_from_config + try_code_mode_from_config builder extensions.`
  - `code_mode.rs` (gated `#[cfg(feature = "code-mode")]`) — `//! Code-mode wiring: re-exports from pmcp-code-mode + executor_from_config + assemble_code_mode_prompt.`

For `sql/`, create `sql/mod.rs` (since lib.rs declares `pub mod sql;`).

Body of each file: nothing else (no symbols yet). Later plans populate.

Create `crates/pmcp-server-toolkit/README.md` with:

- Title `# pmcp-server-toolkit`
- Tagline matching the Cargo.toml description
- "Status: 0.1.0 — early access. Public API may evolve as DX matures across Phases 84–89."
- One short section "What this crate is" listing the public modules from D-15
- One short section "What this crate is NOT" listing the dropped features (per D-14: no `ddb`, `dynamo-config`, `openapi-code-mode`, `js-runtime`, `mcp-code-mode`) and WASM (RESEARCH §"Open Questions" #3 — no `wasm32-*` support in 0.1.0)
- One short section "Quickstart" referencing the eventual Shape C 15-line `main.rs` (Phase 86 will own the runnable; here just sketch the API)
- Pointer to `.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md` for design context

Edit `CLAUDE.md` §"Release & Publish Workflow" → "Workspace Crates (publish order)" list. Current order is:
```
1. pmcp-widget-utils
2. pmcp
3. mcp-tester
4. mcp-preview
5. cargo-pmcp
```
Insert `pmcp-server-toolkit` as new step 3 (after `pmcp`, before `mcp-tester`), and renumber `mcp-tester` to 4, `mcp-preview` to 5, `cargo-pmcp` to 6 per D-08. Brief one-line annotation: `pmcp-server-toolkit (runtime library; depends on pmcp)`.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo build -p pmcp-server-toolkit --no-default-features 2>&1 | grep -E "^error" && exit 1; cargo build -p pmcp-server-toolkit 2>&1 | grep -E "^error" && exit 1; test -f crates/pmcp-server-toolkit/src/auth.rs && test -f crates/pmcp-server-toolkit/src/secrets.rs && test -f crates/pmcp-server-toolkit/src/config.rs && test -f crates/pmcp-server-toolkit/src/prompts.rs && test -f crates/pmcp-server-toolkit/src/resources.rs && test -f crates/pmcp-server-toolkit/src/tools.rs && test -f crates/pmcp-server-toolkit/src/sql/mod.rs && test -f crates/pmcp-server-toolkit/src/builder_ext.rs && test -f crates/pmcp-server-toolkit/src/code_mode.rs && test -f crates/pmcp-server-toolkit/README.md && grep -q "pmcp-server-toolkit" CLAUDE.md && grep -A 12 "Workspace Crates (publish order)" CLAUDE.md | grep -q "3. pmcp-server-toolkit"; echo "ok"</automated>
  </verify>
  <done>
    - Every module declared in `src/lib.rs` has a corresponding stub file (8 .rs files + `sql/mod.rs`).
    - Each stub file has the Pattern I attribution header + a one-line `//!` rustdoc summary.
    - `crates/pmcp-server-toolkit/README.md` exists with all 5 sections in `<action>`.
    - `CLAUDE.md` Workspace Crates publish-order list contains `pmcp-server-toolkit` in position 3.
    - `cargo build -p pmcp-server-toolkit` (default features) and `--no-default-features` both succeed with no errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Snapshot the three reference config.toml fixtures (R10 path-fallback)</name>
  <read_first>
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Risks + Landmines" #4 + §"Assumptions Log" A4 — pmcp-run is an EXTERNAL sibling repo; this task explicitly fetches snapshots
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` §"Canonical References" lines naming the three reference configs (open-images Athena 394 lines; imdb; msr-vtt)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §14 — fixtures are physical copies, NOT symlinks (cross-platform Windows constraint)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-REVIEWS.md` R10 (path fallback)
  </read_first>
  <action>
**Step A — lift-fallback path resolution (review R10):**

```sh
SRC_ROOT="${PMCP_RUN_PATH:-$HOME/Development/mcp/sdk/pmcp-run}/built-in/sql-api/servers"
for name in open-images imdb msr-vtt; do
  if [ ! -r "$SRC_ROOT/$name/config.toml" ]; then
    echo "ERROR: cannot read $SRC_ROOT/$name/config.toml" >&2
    echo "Set PMCP_RUN_PATH=<path-to-pmcp-run-checkout> in the agent environment and re-run." >&2
    exit 1
  fi
done
```

If any fixture is unreadable: HALT with a clear non-fatal error naming the three expected source paths AND the env-var override. DO NOT fabricate placeholder configs (RESEARCH §"Risks + Landmines" #4 — without real configs the SC-2 superset verification in Plan 04 fails).

**Step B — copy with header insertion:**

Copy each VERBATIM (no modifications, preserve all comments) from `${PMCP_RUN_PATH:-$HOME/Development/mcp/sdk/pmcp-run}/built-in/sql-api/servers/<name>/config.toml` to `crates/pmcp-server-toolkit/tests/fixtures/<name>-config.toml`.

Add a header comment to the TOP of EACH copied file (TOML `#` line comment), preserving the rest:
```
# Snapshot from pmcp-run/built-in/sql-api/servers/<name>/config.toml (operator-supplied 2026-05-18).
# Source-of-truth — DO NOT edit. Re-snapshot via the task documented in
# .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-01-PLAN.md Task 4.
```

**Step C — verify each parses as TOML (does NOT need to parse as `ServerConfig` yet):**

```sh
python3 -c "import tomllib; [tomllib.loads(open(p,'rb').read()) for p in ['crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml','crates/pmcp-server-toolkit/tests/fixtures/imdb-config.toml','crates/pmcp-server-toolkit/tests/fixtures/msr-vtt-config.toml']]"
```

If python3 ≥ 3.11 with tomllib unavailable, fall back to `cargo install --quiet taplo-cli && taplo fmt --check crates/pmcp-server-toolkit/tests/fixtures/*.toml`.

Verify each file is at least 10 lines (no truncated/empty stubs).

CRITICAL anti-pattern (PATTERNS §14): symlinks are forbidden — must be physical copies.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && test -f crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml && test -f crates/pmcp-server-toolkit/tests/fixtures/imdb-config.toml && test -f crates/pmcp-server-toolkit/tests/fixtures/msr-vtt-config.toml && ! test -L crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml && [ "$(wc -l < crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml)" -ge 10 ] && [ "$(wc -l < crates/pmcp-server-toolkit/tests/fixtures/imdb-config.toml)" -ge 10 ] && [ "$(wc -l < crates/pmcp-server-toolkit/tests/fixtures/msr-vtt-config.toml)" -ge 10 ] && head -3 crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml | grep -q "Snapshot from pmcp-run" && python3 -c "import tomllib; [tomllib.loads(open(p,'rb').read()) for p in ['crates/pmcp-server-toolkit/tests/fixtures/open-images-config.toml','crates/pmcp-server-toolkit/tests/fixtures/imdb-config.toml','crates/pmcp-server-toolkit/tests/fixtures/msr-vtt-config.toml']]" && echo "ok"</automated>
  </verify>
  <done>
    - Path-fallback step (R10) honored `$PMCP_RUN_PATH` or default; halted loudly if unreadable.
    - All three fixture files exist as physical (non-symlink) copies in `crates/pmcp-server-toolkit/tests/fixtures/`.
    - Each starts with the three-line `# Snapshot from pmcp-run/...` comment header.
    - Each parses as valid TOML (syntactic only; semantic ServerConfig parse comes in Plan 04).
    - Each is at least 10 lines long.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run quality gate baseline</name>
  <read_first>
    - `CLAUDE.md` §"Quality Gate Enforcement" — `make quality-gate` is the CI-equivalent
    - `Makefile` (root) — confirm `quality-gate` target exists and invokes `make lint` with `--features "full"` per CLAUDE.md
  </read_first>
  <action>
Run `make quality-gate` from the workspace root and capture output to `/tmp/p83-01-quality-gate.log`. Expected: passes (this plan only adds an empty crate manifest + module stubs + readme + fixtures + CLAUDE.md edit; no clippy violations expected).

If `make quality-gate` fails:
1. Identify the specific failing step (fmt, clippy, build, test, audit).
2. Resolve in-place (e.g. `cargo fmt --all`, fix specific clippy lints) WITHOUT adding blanket `#![allow(clippy::*)]` lines to `lib.rs` (PATTERNS §"Anti-Patterns" #10).
3. Re-run `make quality-gate` until green.

Commit nothing yet — execute-plan owns commits. Just leave the worktree green.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && make quality-gate 2>&1 | tail -50 | grep -E "(Quality Gate.*PASSED|passed.*tests|all checks passed)" && echo "ok"</automated>
  </verify>
  <done>
    - `make quality-gate` exits 0.
    - No new `#![allow(clippy::*)]` lines added to `lib.rs` beyond what the analog `pmcp-code-mode/src/lib.rs` carries (zero is the ideal — PATTERNS §"Anti-Patterns" #10).
    - Worktree clean of warnings on `cargo build -p pmcp-server-toolkit --all-features`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply-chain | Adding `pmcp-server-toolkit` to crates.io extends the public attack surface that downstream Lambda consumers depend on |
| workspace-cargo | Root `Cargo.toml` member list controls what `cargo publish --workspace` will emit; mis-slotted entry could publish an unintended crate |
| cross-repo-source→fixture-copy (R10) | Per review R10, Task 4 fetches fixtures from an external repo path; the fallback contract must halt loudly on unreadable source rather than fabricating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01-01 | Tampering | crates/pmcp-server-toolkit/Cargo.toml feature matrix | mitigate | D-14 dropped features (`ddb`, `dynamo-config`, `openapi-code-mode`, `js-runtime`, `mcp-code-mode`) MUST NOT appear; verified by Task 1 grep in `<verify>` |
| T-83-01-02 | Information Disclosure | crates/pmcp-server-toolkit publish artifact | mitigate | `exclude = [".planning/", ".pmat/", "fixtures/", "tests/", "fuzz/"]` (Pitfall 6); verified by `cargo package --list` in Plan 09's pre-publish gate |
| T-83-01-03 | Tampering | AWS feature-gated SDKs | mitigate | All AWS deps use `default-features = false` + `default-https-client` per Pattern §1 |
| T-83-01-04 | Tampering | tests/fixtures/*.toml — reference snapshots | mitigate | Each file carries `# Snapshot from pmcp-run/...` comment; physical copies (not symlinks); Task 4 fails if files are < 10 lines or missing the header |
| T-83-01-05 | Repudiation | CLAUDE.md publish-order edit | mitigate | `pmcp-server-toolkit` inserted in position 3 per D-08 |
| T-83-01-06 | Repudiation | Fixture snapshot fabricated when pmcp-run source unreadable (review R10) | mitigate | Task 4 honors `$PMCP_RUN_PATH` override and HALTS LOUDLY when sources are unreadable. No fabricated stubs |
</threat_model>

<verification>
Plan 01 success conditions, in order:
1. `cargo metadata --format-version 1 --no-deps` lists `pmcp-server-toolkit` as a workspace member.
2. `cargo build -p pmcp-server-toolkit --no-default-features` succeeds.
3. `cargo build -p pmcp-server-toolkit` (default features) succeeds.
4. `cargo test --doc -p pmcp-server-toolkit error` passes (one doctest minimum on `ToolkitError`).
5. `make quality-gate` passes.
6. All three reference fixtures exist, are physical files (not symlinks), and parse as valid TOML.
7. `CLAUDE.md` publish-order list shows `pmcp-server-toolkit` at position 3.
8. No D-14-dropped features appear in the manifest.
9. `secrecy = "0.10"` in `[dependencies]` (review R6 prep); `trybuild = "1"` in `[dev-dependencies]` (review R5 prep).
</verification>

<success_criteria>
- Workspace member `pmcp-server-toolkit` exists, parses, and builds with all locked-decision constraints (D-05/D-07/D-08/D-14/D-15) satisfied.
- `ToolkitError` + `Result<T>` alias exist with a passing doctest establishing baseline TEST-03 coverage.
- Module skeleton creates the landing surface for Plans 02–08; every public module from D-15 has a stub file with attribution + summary.
- Reference fixtures captured so Plan 04 can run the SC-2 superset integration test — review R10 fallback contract honored.
- Review R5 + R6 prep: `trybuild` + `secrecy` deps pre-staged for Plan 02.
- `CLAUDE.md` publish-order edit committed so release operators know the new slot.
- `make quality-gate` passes on the worktree.
</success_criteria>

<output>
After completion, create `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-01-SUMMARY.md` per the standard template.
</output>
