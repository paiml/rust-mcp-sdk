---
phase: 83-toolkit-core-lift-pmcp-server-toolkit
plan: 09
type: execute
wave: 6
depends_on:
  - 02
  - 03
  - 04
  - 05
  - 06
  - 07
  - 08
files_modified:
  - crates/pmcp-server-toolkit/fuzz/Cargo.toml
  - crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs
  - contracts/binding.yaml
  - .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md
  - .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md
  - crates/pmcp-server-toolkit/Cargo.toml
autonomous: true
requirements:
  - TKIT-01
  - TKIT-08
  - TEST-02
  - TEST-03

tags:
  - toolkit
  - fuzz
  - contract
  - shim-diff
  - migration-guide
  - publish-gate

must_haves:
  truths:
    - "`crates/pmcp-server-toolkit/fuzz/Cargo.toml` declares a `pmcp-server-toolkit-fuzz` workspace-isolated crate (Phase 77 pattern verbatim — PATTERNS §17)"
    - "`crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs` ships a libFuzzer target stressing `ServerConfig::from_toml(arbitrary_bytes)` (CLAUDE.md ALWAYS-fuzz)"
    - "`cargo +nightly fuzz run pmcp_server_toolkit_config_parser -- -max_total_time=60` exits 0 with no panics OR is documented as a CI/nightly target (Phase 77 disposition)"
    - "`cargo +nightly check -p pmcp-server-toolkit-fuzz` succeeds locally (Phase 77 stable-check disposition)"
    - "Per review R3, `contracts/binding.yaml` is extended with ≥10 rows covering toolkit public symbols. Each row's `module_path` is the **CRATE-ROOT** form (e.g. `pmcp_server_toolkit::AuthProvider`), NOT the internal submodule form (`pmcp_server_toolkit::auth::AuthProvider`), to align with the D-15 headline DX promise. Use the rustdoc output as canonical source"
    - "`pmat comply check` exits 0 against the toolkit crate (or documents the absence per CLAUDE.md if pmat is not available locally)"
    - "`.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md` exists as the operator-handoff artifact (D-04). Per review R10, the apply instructions honor `$PMCP_RUN_PATH` env-var override"
    - "`.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md` exists as the developer-facing migration guide describing the one-page recipe for moving from in-tree path-deps to the public toolkit (REF-03 anchor; Phase 89 owns the book chapter). Migration guide example shows SINGLE crate-root import line per review R3"
    - "`Cargo.toml` `exclude = [...]` validated by `cargo package --list -p pmcp-server-toolkit` — output must NOT include `.planning/`, `.pmat/`, `fixtures/`, `tests/`, `fuzz/` (Pitfall 6)"
    - "Final `make quality-gate` is green across the whole workspace (NOT just `-p pmcp-server-toolkit`)"
    - "Workspace-wide cross-feature builds pass: `cargo build --workspace`, `cargo test --workspace --no-fail-fast`"
    - "`cargo publish --dry-run -p pmcp-server-toolkit` succeeds (publish-gate verification per CLAUDE.md §Release & Publish Workflow)"
  artifacts:
    - path: crates/pmcp-server-toolkit/fuzz/Cargo.toml
      provides: "Workspace-isolated fuzz crate manifest"
      contains: "cargo-fuzz = true"
    - path: crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs
      provides: "libFuzzer target for ServerConfig::from_toml DoS protection"
      contains: "ServerConfig"
    - path: contracts/binding.yaml
      provides: "Toolkit public-API contract entries (crate-root module paths per review R3)"
      contains: "pmcp_server_toolkit::AuthProvider"
    - path: .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md
      provides: "Operator handoff: re-export shim for pmcp-run/built-in/shared/mcp-server-common (D-04)"
      contains: "pub use pmcp_server_toolkit"
    - path: .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md
      provides: "Developer migration guide (REF-03 anchor; Phase 89 expands to book chapter)"
      contains: "pmcp-server-toolkit"
  key_links:
    - from: "fuzz target"
      to: "ServerConfig::from_toml"
      via: "libFuzzer + toml::from_str"
      pattern: "ServerConfig::from_toml|toml::from_str.*ServerConfig"
    - from: "contracts/binding.yaml extension rows"
      to: "pmcp_server_toolkit::* (crate-root paths per review R3)"
      via: "module_path field — crate-root form, not submodule form"
      pattern: "pmcp_server_toolkit::[A-Z]"
    - from: "shim-pmcp-run-shared.md"
      to: "pmcp-run/built-in/shared/mcp-server-common/src/lib.rs"
      via: "pub use pmcp_server_toolkit::*"
      pattern: "pub use pmcp_server_toolkit"
---

<objective>
Wave-5 capstone: ship the ALWAYS-fuzz target, the `contracts/binding.yaml` extension with one row per TKIT-02..10 public symbol using CRATE-ROOT module paths (review R3), the operator-handoff shim diff (D-04) with `$PMCP_RUN_PATH`-aware apply instructions (review R10), the developer migration guide (single-line import per review R3), and the publish-gate verification (`cargo publish --dry-run` + `cargo package --list`).

Per review R3, every contract row's `module_path` MUST be the crate-root form (`pmcp_server_toolkit::AuthProvider`, NOT `pmcp_server_toolkit::auth::AuthProvider`) so the public API surface contracts AGAINST the D-15 headline DX promise. The submodule paths still exist internally and remain valid (compatibility); the canonical surface for contracts and docs is the crate root.

Per review R10, the shim apply instructions honor `$PMCP_RUN_PATH` (with the documented default `$HOME/Development/mcp/sdk/pmcp-run`) so an operator running in CI or on a different machine can apply the shim without hardcoded paths.

Purpose: TEST-02 fuzz (ALWAYS-fuzz CLAUDE.md requirement); CMSUP-05 / Contract-First Development requirement; D-04 operator handoff; REF-03 migration recipe anchor; TKIT-01 publishability verification.

Output: The toolkit crate is publishable, fuzz-protected against malformed TOML, contractually documented with crate-root paths, AND the cross-repo operator handoff bundle is ready for the pmcp-run team to apply.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-REVIEWS.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-08-SUMMARY.md
@CLAUDE.md
@contracts/binding.yaml

<interfaces>
<!-- Phase 77 fuzz target shape — PATTERNS §17 — mirror with a different `T` for toml::from_str. -->

From `cargo-pmcp/fuzz/Cargo.toml:1-50`:
```toml
[package]
name = "cargo-pmcp-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
cargo-pmcp = { path = ".." }
toml = "1.0"

[workspace]

[[bin]]
name = "pmcp_config_toml_parser"
path = "fuzz_targets/pmcp_config_toml_parser.rs"
doc = false
test = false
bench = false
```

From `contracts/binding.yaml:1-90` (existing-row shape — PATTERNS §18):
```yaml
- contract: mcp-protocol-sdk-v1.yaml
  equation: jsonrpc_framing
  function: JSONRPCRequest::validate
  module_path: pmcp::types::jsonrpc
  signature: 'pub fn validate(&self) -> Result<(), crate::Error>'
  status: implemented
  notes: Validates jsonrpc field equals "2.0"; returns Error::Validation on mismatch
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ship the fuzz target (CLAUDE.md ALWAYS-fuzz)</name>
  <read_first>
    - `cargo-pmcp/fuzz/Cargo.toml` (full — primary analog per PATTERNS §17)
    - `cargo-pmcp/fuzz/fuzz_targets/pmcp_config_toml_parser.rs` (full — primary analog)
    - `crates/pmcp-server-toolkit/src/config.rs` (Plan 04) — `ServerConfig::from_toml` target signature
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §17 (verbatim mirror)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Validation Architecture" Wave 0 entry for fuzz target
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` §"Decisions" D-17 (fuzz target is a P83 deliverable)
  </read_first>
  <action>
Create `crates/pmcp-server-toolkit/fuzz/Cargo.toml`:

```toml
[package]
name = "pmcp-server-toolkit-fuzz"
version = "0.0.0"
publish = false
edition = "2021"
license = "MIT OR Apache-2.0"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
pmcp-server-toolkit = { path = ".." }
toml = "1.0"

[workspace]

[[bin]]
name = "pmcp_server_toolkit_config_parser"
path = "fuzz_targets/pmcp_server_toolkit_config_parser.rs"
doc = false
test = false
bench = false
```

Create `crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs`:

```rust
//! Phase 83 fuzz target — stress `pmcp_server_toolkit::ServerConfig::from_toml`
//! against arbitrary byte sequences. The parser must not panic on adversarial
//! input — `toml::from_str` either succeeds (valid TOML matching schema) or
//! returns Err.
//!
//! Threat model: T-83-04-01 (adversarial TOML → parser DoS via panic).

#![no_main]

use libfuzzer_sys::fuzz_target;
use pmcp_server_toolkit::ServerConfig;

fuzz_target!(|data: &[u8]| {
    let Ok(s) = std::str::from_utf8(data) else {
        return;
    };
    let _: Result<ServerConfig, _> = ServerConfig::from_toml(s);
});
```

Verify the target compiles:
```
cd crates/pmcp-server-toolkit/fuzz && cargo +nightly check --bin pmcp_server_toolkit_config_parser
```

If `cargo +nightly` unavailable, document the Phase 77 disposition (source lands, CI nightly exercises).

Verify `cargo build --workspace` from workspace root still succeeds (isolated `[workspace]` line keeps fuzz out of main workspace).
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && test -f crates/pmcp-server-toolkit/fuzz/Cargo.toml && test -f crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs && grep -q "cargo-fuzz = true" crates/pmcp-server-toolkit/fuzz/Cargo.toml && grep -q "^\\[workspace\\]" crates/pmcp-server-toolkit/fuzz/Cargo.toml && grep -q "pmcp_server_toolkit::ServerConfig" crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs && grep -q "ServerConfig::from_toml" crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs && (cd crates/pmcp-server-toolkit/fuzz && (cargo +nightly check --bin pmcp_server_toolkit_config_parser 2>&1 || cargo check --bin pmcp_server_toolkit_config_parser 2>&1) | tee /tmp/p83-09-t1.log; ! grep -E "^error\\[" /tmp/p83-09-t1.log) && cargo build --workspace --quiet && echo "ok"</automated>
  </verify>
  <done>
    - `fuzz/Cargo.toml` exists with `cargo-fuzz = true` + isolated `[workspace]` line.
    - `fuzz_targets/pmcp_server_toolkit_config_parser.rs` exists, references `pmcp_server_toolkit::ServerConfig` (CRATE-ROOT path per review R3) + calls `ServerConfig::from_toml`.
    - The target compiles under nightly (or stable).
    - `cargo build --workspace` (main workspace) still succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend contracts/binding.yaml with toolkit public-API rows — CRATE-ROOT paths per review R3</name>
  <read_first>
    - `contracts/binding.yaml` (full — confirm existing row shape; check the trailing newline + comment convention)
    - `crates/pmcp-server-toolkit/src/lib.rs` (final, post-Plan 08) — every `pub use` at the CRATE ROOT is a contract candidate; submodule-only items don't go in the contract
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §18 (append-row pattern)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Open Questions" #1 (extend, not new file)
    - `CLAUDE.md` §"Contract-First Development" (use `pmat comply check` to validate)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-REVIEWS.md` R3 (canonical paths = crate root)
  </read_first>
  <action>
**Sub-step 2a — generate rustdoc for signature extraction:**

```sh
cargo doc -p pmcp-server-toolkit --no-deps
# Inspect the rustdoc index — confirm crate-root re-exports
```

**Sub-step 2b — pick the Result alias treatment ONCE and apply globally:**

Pick ONE form for all toolkit rows:
- (a) Alias form: `Result<T>`
- (b) Concrete form: `Result<T, ToolkitError>`

Use form (b) — concrete — for binding.yaml because contract validators benefit from explicit types. Apply uniformly to ALL toolkit rows.

**Sub-step 2c — append the rows (per review R3: `module_path` is CRATE-ROOT, not submodule):**

Append to `contracts/binding.yaml`:

```yaml

# === Toolkit Core (Phase 83) ===
# Per Phase 83 review R3, every module_path is the CRATE-ROOT form
# (e.g. pmcp_server_toolkit::AuthProvider), aligning with the D-15
# headline DX promise. The submodule paths still exist internally and
# remain valid; the canonical surface for contracts is the crate root.

- contract: mcp-protocol-sdk-v1.yaml
  equation: config_strict_parse
  function: ServerConfig::from_toml
  module_path: pmcp_server_toolkit::ServerConfig
  signature: 'pub fn from_toml(toml_str: &str) -> Result<Self, ToolkitError>'
  status: implemented
  notes: TKIT-01 — strict-parse entry point; #[serde(deny_unknown_fields)] on every section (D-13)

- contract: mcp-protocol-sdk-v1.yaml
  equation: config_strict_validated
  function: ServerConfig::from_toml_strict_validated
  module_path: pmcp_server_toolkit::ServerConfig
  signature: 'pub fn from_toml_strict_validated(toml_str: &str) -> Result<Self, ToolkitError>'
  status: implemented
  notes: TKIT-01 — parse + validate convenience per review R8; catches empty required values

- contract: mcp-protocol-sdk-v1.yaml
  equation: auth_provider_trait
  function: AuthProvider
  module_path: pmcp_server_toolkit::AuthProvider
  signature: '#[async_trait] pub trait AuthProvider: Send + Sync'
  status: implemented
  notes: TKIT-02 — re-exported from pmcp::server::auth::AuthProvider at the toolkit crate root per review R3 (the headline D-15 DX promise)

- contract: mcp-protocol-sdk-v1.yaml
  equation: auth_static_provider
  function: StaticAuthProvider::new
  module_path: pmcp_server_toolkit::StaticAuthProvider
  signature: 'pub fn new(expected_token: impl Into<String>) -> Self'
  status: implemented
  notes: TKIT-02 — concrete impl of pmcp::server::auth::AuthProvider (dev/test bearer-token only)

- contract: mcp-protocol-sdk-v1.yaml
  equation: secrets_provider_trait
  function: SecretsProvider::get
  module_path: pmcp_server_toolkit::SecretsProvider
  signature: 'async fn get(&self, name: &str) -> Result<SecretValue, ToolkitError>'
  status: implemented
  notes: >-
    TKIT-03 — toolkit's secrets surface. Return type is the toolkit-owned
    SecretValue (per review R6 — NOT pmcp_code_mode::TokenSecret, which would
    leak the code-mode feature). EnvSecrets is the default impl.

- contract: mcp-protocol-sdk-v1.yaml
  equation: secret_value
  function: SecretValue::expose_secret
  module_path: pmcp_server_toolkit::SecretValue
  signature: 'pub fn expose_secret(&self) -> &[u8]'
  status: implemented
  notes: >-
    TKIT-03 — toolkit-owned secret newtype. Per review R5, lacks Debug/Clone/
    Serialize (trybuild compile-fail tests at tests/compile_fail/ enforce).
    Per review R6, feature-independent — works under --no-default-features.

- contract: mcp-protocol-sdk-v1.yaml
  equation: static_resource_handler
  function: StaticResourceHandler::new
  module_path: pmcp_server_toolkit::StaticResourceHandler
  signature: 'pub fn new(resources: IndexMap<String, ResourceEntry>) -> Self'
  status: implemented
  notes: TKIT-04 — impl of pmcp::ResourceHandler; MIME-typed wire via Content::resource_with_text

- contract: mcp-protocol-sdk-v1.yaml
  equation: static_prompt_handler
  function: StaticPromptHandler::handle
  module_path: pmcp_server_toolkit::StaticPromptHandler
  signature: 'async fn handle(&self, args: HashMap<String, String>, _extra: RequestHandlerExtra) -> Result<GetPromptResult>'
  status: implemented
  notes: TKIT-05 — impl of pmcp::PromptHandler; arg-validation returns pmcp::Error::validation

- contract: mcp-protocol-sdk-v1.yaml
  equation: hmac_token_reexport
  function: HmacTokenGenerator
  module_path: pmcp_server_toolkit::code_mode::HmacTokenGenerator
  signature: 'pub use pmcp_code_mode::HmacTokenGenerator'
  status: implemented
  notes: TKIT-06 — re-export only (D-16). No duplicate HMAC code in the toolkit. Lives under `code_mode` submodule (feature-gated).

- contract: mcp-protocol-sdk-v1.yaml
  equation: tool_synthesis
  function: synthesize_from_config
  module_path: pmcp_server_toolkit::tools::synthesize_from_config
  signature: 'pub fn synthesize_from_config(config: &ServerConfig) -> Result<Vec<(String, ToolInfo, Arc<dyn ToolHandler>)>, ToolkitError>'
  status: implemented
  notes: >-
    TKIT-07 — turns [[tools]] entries into pmcp ToolInfo + handler pairs. Per
    review R3, lives under `tools` submodule because it's the power-user surface;
    Shape C consumers use the higher-level `ServerBuilderExt::tools_from_config`.

- contract: mcp-protocol-sdk-v1.yaml
  equation: backend_core_smoke
  function: backend_core_construction_surface_smoke
  module_path: pmcp_server_toolkit (integration test)
  signature: '#[tokio::test] async fn backend_core_construction_surface_smoke()'
  status: implemented
  notes: TKIT-08 — D-03 anchor; replays mcp-sql/graphql/openapi-core construction without cross-repo CI

- contract: mcp-protocol-sdk-v1.yaml
  equation: code_mode_wiring
  function: executor_from_config
  module_path: pmcp_server_toolkit::code_mode::executor_from_config
  signature: 'pub fn executor_from_config(config: &ServerConfig) -> Result<Box<dyn CodeExecutor>, ToolkitError>'
  status: implemented
  notes: >-
    TKIT-09 — wires [code_mode] block + [code_mode.limits] into pmcp_code_mode::CodeExecutor.
    Per review R1, the exact constructor surface was verified via CODE_MODE_API_NOTES.md;
    no `todo!()` survived to commit. Per review R9, inline token_secret literals are
    rejected unless allow_inline_token_secret_for_dev=true.

- contract: mcp-protocol-sdk-v1.yaml
  equation: code_mode_prompt_assembly
  function: assemble_code_mode_prompt
  module_path: pmcp_server_toolkit::code_mode::assemble_code_mode_prompt
  signature: 'pub async fn assemble_code_mode_prompt(connector: &dyn SqlConnector, config: &ServerConfig) -> Result<String, ToolkitError>'
  status: implemented
  notes: TKIT-10 — combines connector.schema_text() with [[database.tables]] descriptions

- contract: mcp-protocol-sdk-v1.yaml
  equation: sql_connector_trait
  function: SqlConnector
  module_path: pmcp_server_toolkit::SqlConnector
  signature: '#[async_trait] pub trait SqlConnector: Send + Sync + ''static { fn dialect(&self) -> Dialect; async fn schema_text(&self) -> Result<String, ConnectorError>; }'
  status: stub
  notes: >-
    TKIT-10 trait stub (MINIMIZED to 2 methods per review R2 — execute() and
    placeholder translation deferred to Phase 84 once the first real connector
    validates the contract).

- contract: mcp-protocol-sdk-v1.yaml
  equation: server_builder_ext
  function: ServerBuilderExt::tools_from_config
  module_path: pmcp_server_toolkit::ServerBuilderExt
  signature: 'fn tools_from_config(self, config: &ServerConfig) -> Self'
  status: implemented
  notes: D-10 — wires synthesize_from_config into pmcp::ServerBuilder via Phase 82 tool_arc

- contract: mcp-protocol-sdk-v1.yaml
  equation: server_builder_ext_fallible
  function: ServerBuilderExt::try_tools_from_config
  module_path: pmcp_server_toolkit::ServerBuilderExt
  signature: 'fn try_tools_from_config(self, config: &ServerConfig) -> Result<Self, ToolkitError>'
  status: implemented
  notes: >-
    D-10 + review R7 — fallible companion to tools_from_config; production
    servers should prefer this over the panicking variant.
```

Adjust signatures to match actual implementations from Plans 02–08. If the rustdoc-emitted signature differs, the rustdoc form wins (it IS the canonical surface).

Run `pmat comply check` from workspace root if pmat is available. If errors, fix contract entries to satisfy the validator.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && grep -q "Toolkit Core (Phase 83)" contracts/binding.yaml && grep -q "module_path: pmcp_server_toolkit::ServerConfig" contracts/binding.yaml && grep -q "module_path: pmcp_server_toolkit::AuthProvider" contracts/binding.yaml && grep -q "module_path: pmcp_server_toolkit::SecretsProvider" contracts/binding.yaml && grep -q "module_path: pmcp_server_toolkit::SqlConnector" contracts/binding.yaml && grep -q "module_path: pmcp_server_toolkit::ServerBuilderExt" contracts/binding.yaml && [ "$(grep -c "module_path: pmcp_server_toolkit" contracts/binding.yaml)" -ge 10 ] && (pmat comply check 2>&1 || pmat --version 2>&1 | grep -q "command not found" && echo "pmat-absent-acceptable") && echo "ok"</automated>
  </verify>
  <done>
    - `contracts/binding.yaml` extended with ≥10 rows.
    - Crate-root `module_path` form used for D-15 surface (`pmcp_server_toolkit::AuthProvider`, `::SecretsProvider`, `::SqlConnector`, etc.). Power-user / submodule surface (`pmcp_server_toolkit::tools::synthesize_from_config`, `::code_mode::*`) uses the submodule path where the symbol is genuinely below the crate root.
    - Result-alias treatment consistent (concrete `Result<T, ToolkitError>` form throughout).
    - `pmat comply check` exits 0 (or is documented absent).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write shim diff artifact (R10 path-fallback) + migration guide (R3 single-import)</name>
  <read_first>
    - External (with `PMCP_RUN_PATH` override): `${PMCP_RUN_PATH:-$HOME/Development/mcp/sdk/pmcp-run}/built-in/shared/mcp-server-common/Cargo.toml` + `src/lib.rs`
    - `crates/pmcp-server-toolkit/src/lib.rs` (final — what the shim re-exports through)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` §"Decisions" D-01, D-02, D-04
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-REVIEWS.md` R3 (single-import example) + R10 (path-fallback)
  </read_first>
  <action>
**Step A — operator handoff (D-04): `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md`**

```markdown
# Phase 83 D-04 Operator Handoff — pmcp-run shim diff

**Target repository:** `pmcp-run` (sibling repo; external to rust-mcp-sdk)
**Target path:** `pmcp-run/built-in/shared/mcp-server-common/`
**Operator:** the owner of the `pmcp-run` repo
**Apply after:** `pmcp-server-toolkit 0.1.0` ships to crates.io
**Status:** P83 ships this artifact; cross-repo PR is tracked separately

## Why this exists

Per CONTEXT.md D-01 / D-02, the toolkit publish in Phase 83 happens FIRST.
The `pmcp-run/built-in/shared/mcp-server-common/` crate then becomes a pure
re-export shim that `pub use pmcp_server_toolkit::*` to keep the three
backend cores (`mcp-sql-server-core`, `mcp-graphql-server-core`,
`mcp-openapi-server-core`) building with zero source diff in those cores'
files.

## File 1: `pmcp-run/built-in/shared/mcp-server-common/src/lib.rs`

Replace the entire file with:

```rust
// Phase 83 shim: mcp-server-common now re-exports pmcp-server-toolkit.

pub use pmcp_server_toolkit::*;

#[cfg(feature = "avp")]
pub use pmcp_server_toolkit::code_mode::{AvpClient, AvpConfig, AvpPolicyEvaluator};

// Note: DDB (ddb / dynamo-config features) were dropped from the toolkit
// per Phase 83 D-14. If backend cores need DDB types, depend on the
// appropriate pmcp-run-owned crate directly.
```

## File 2: `pmcp-run/built-in/shared/mcp-server-common/Cargo.toml`

Diff to apply:

```diff
 [dependencies]
- pmcp = { ... }
- async-trait = { ... }
- serde = { ... }
+ pmcp-server-toolkit = { version = "0.1.0", features = ["code-mode"] }

 [features]
- default = []
- code-mode = []
- avp = []
- aws = []
+ default = []
+ code-mode = ["pmcp-server-toolkit/code-mode"]
+ avp = ["pmcp-server-toolkit/avp"]
+ aws = ["pmcp-server-toolkit/aws"]
```

## Apply instructions

Per Phase 83 review R10, the apply commands honor `$PMCP_RUN_PATH` (default
`$HOME/Development/mcp/sdk/pmcp-run`) so an operator running in CI or on a
different machine can apply the shim without hardcoded paths:

```sh
# Locate the pmcp-run checkout (R10 path override)
export PMCP_RUN_PATH="${PMCP_RUN_PATH:-$HOME/Development/mcp/sdk/pmcp-run}"
if [ ! -d "$PMCP_RUN_PATH" ]; then
  echo "Set PMCP_RUN_PATH to the pmcp-run checkout root and re-run." >&2
  exit 1
fi

cd "$PMCP_RUN_PATH"
git checkout -b chore/p83-toolkit-shim

# Replace src/lib.rs (paste File 1 content over the existing file)
$EDITOR built-in/shared/mcp-server-common/src/lib.rs

# Apply the Cargo.toml diff (manual merge — exact lines vary)
$EDITOR built-in/shared/mcp-server-common/Cargo.toml

# Verify each backend core still builds
cargo build -p mcp-sql-server-core
cargo build -p mcp-graphql-server-core
cargo build -p mcp-openapi-server-core
cargo test --workspace

# Submit PR
git add built-in/shared/mcp-server-common
git commit -m "chore(shared): swap mcp-server-common to pmcp-server-toolkit re-export shim (Phase 83 D-02)"
git push -u origin chore/p83-toolkit-shim
```

## Rollback

If a backend core breaks: either re-export name diverged (file a bug against
`pmcp-server-toolkit`) or one of the dropped features (`ddb`, `dynamo-config`,
`openapi-code-mode`, `js-runtime`, `mcp-code-mode`) was still in use. In the
latter case, restore the specific dep line in `mcp-server-common/Cargo.toml`
and update its `lib.rs` to also re-export from the relevant non-toolkit crate.

## Reference: P83 SC-5

Per ROADMAP §Phase 83 SC-5, the in-toolkit smoke test
(`crates/pmcp-server-toolkit/tests/backend_core_smoke.rs`, Plan 08 Task 2)
PROVES the toolkit's public API surface covers what the backend cores
construct today. This artifact is the cross-repo apply step.
```

**Step B — developer migration guide: `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md`**

```markdown
# Migrating to `pmcp-server-toolkit`

**Audience:** Developers who currently build MCP servers against `pmcp-run`'s
internal `mcp-server-common`, or who hand-rolled the AuthProvider /
SecretsProvider / static-handler / HMAC-token plumbing themselves.

**Source of truth:** This document is the Phase 83 anchor for REF-03. Phase 89
expands this into a full book chapter in `pmcp-book/src/`; this file is the
developer-facing one-pager.

## What changed in Phase 83

The proto-SDK that lived at `pmcp-run/built-in/shared/mcp-server-common/` is now
a public crates.io crate: `pmcp-server-toolkit`.

It ships:
- `AuthProvider` impls (`StaticAuthProvider` + AWS-gated)
- `SecretsProvider` trait + `EnvSecrets` (+ AWS-gated). Per Phase 83 review R6,
  the secret type is the toolkit-owned `SecretValue` (NOT `pmcp_code_mode::TokenSecret`)
  so the trait works under `--no-default-features`.
- `StaticResourceHandler`, `StaticPromptHandler`
- HMAC token machinery (re-exported from `pmcp-code-mode`)
- `[[tools]]` → `ToolInfo` synthesizer with zero per-tool Rust handlers
- `[code_mode]` config → `CodeExecutor` wiring. Per Phase 83 review R9,
  `token_secret` is `env:`-only by default; inline literals are rejected unless
  `allow_inline_token_secret_for_dev = true` is explicitly set.
- `SqlConnector` trait stub (Phase 83 ships 2 methods per review R2;
  Phase 84 ships `execute()` + placeholder translation once real connectors
  validate the contract)

## One-page migration

### Before

Your `Cargo.toml`:
```toml
[dependencies]
mcp-server-common = { path = "../shared/mcp-server-common" }
pmcp = "..."
async-trait = "..."
# ... and all the deps mcp-server-common needed transitively
```

Your `main.rs`:
```rust
use mcp_server_common::{StaticAuthProvider, SecretsProvider, ...};
```

### After

Your `Cargo.toml`:
```toml
[dependencies]
pmcp = "2.8.1"
pmcp-server-toolkit = { version = "0.1.0", features = ["code-mode"] }
# That's it — the toolkit pulls the rest transitively
```

Your `main.rs` — per Phase 83 review R3, this is ONE crate-root import line
(the headline D-15 DX promise):

```rust
use pmcp_server_toolkit::{
    AuthProvider, ServerBuilderExt, ServerConfig, ServerSection,
    SecretValue, SecretsProvider,
    StaticAuthProvider, StaticPromptHandler, StaticResourceHandler,
};

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let cfg = ServerConfig::from_toml_strict_validated(include_str!("config.toml"))?;

    // Per review R7, use the try_* variants in production:
    let server = pmcp::Server::builder()
        .name(&cfg.server.name)
        .version(&cfg.server.version)
        .try_tools_from_config(&cfg)?
        .try_code_mode_from_config(&cfg)?
        .resources_arc(std::sync::Arc::new(StaticResourceHandler::from(&cfg)))
        .auth_provider_arc(std::sync::Arc::new(StaticAuthProvider::new("dev-token")))
        .build()?;
    server.run_stdio().await?;
    Ok(())
}
```

## Symbol mapping

| Old (mcp-server-common) | New (pmcp-server-toolkit — crate root) |
|-------------------------|-----------------------------------------|
| `mcp_server_common::AuthProvider` | `pmcp_server_toolkit::AuthProvider` |
| `mcp_server_common::StaticAuthProvider` | `pmcp_server_toolkit::StaticAuthProvider` |
| `mcp_server_common::SecretsProvider` | `pmcp_server_toolkit::SecretsProvider` |
| `mcp_server_common::EnvSecrets` | `pmcp_server_toolkit::EnvSecrets` |
| `mcp_server_common::StaticResourceHandler` | `pmcp_server_toolkit::StaticResourceHandler` |
| `mcp_server_common::StaticPromptHandler` | `pmcp_server_toolkit::StaticPromptHandler` |
| (custom HMAC code) | `pmcp_server_toolkit::code_mode::HmacTokenGenerator` (re-exported from `pmcp-code-mode`) |
| (custom ToolInfo construction) | `pmcp_server_toolkit::tools::synthesize_from_config` (power user) OR `pmcp_server_toolkit::ServerBuilderExt::try_tools_from_config` (common path, review R7) |
| (custom code-mode glue) | `pmcp_server_toolkit::code_mode::executor_from_config` (power user) OR `pmcp_server_toolkit::ServerBuilderExt::try_code_mode_from_config` (common path, review R7) |

## Dropped from `mcp-server-common`

Per CONTEXT.md D-14 these features were intentionally NOT lifted:

- `ddb`, `dynamo-config` — pmcp-run-specific concerns.
- `openapi-code-mode`, `js-runtime`, `mcp-code-mode` — Phase 3 OpenAPI territory.

If your server uses any of these, keep the corresponding deps explicit in your
`Cargo.toml`.

## Phase 83 review-driven behaviors to know about

- **Review R3 — single crate-root import:** Every public symbol you need is
  re-exported at the toolkit crate root. If you find yourself writing
  `pmcp_server_toolkit::auth::*`, file an issue — the re-export is missing.
- **Review R5 — secret-leak compile guards:** `SecretValue` does NOT impl
  `Debug`/`Clone`/`Serialize`. `format!("{:?}", secret)` won't compile.
  Trybuild tests at `crates/pmcp-server-toolkit/tests/compile_fail/` enforce.
- **Review R6 — secret type stable across features:** `SecretsProvider::get`
  returns `SecretValue` (toolkit-owned, feature-independent), not the
  `code-mode`-gated `TokenSecret`.
- **Review R7 — try_* variants:** Production code should prefer
  `try_tools_from_config` / `try_code_mode_from_config` over the panicking
  forms.
- **Review R8 — `validate()`:** Prefer `from_toml_strict_validated` over
  `from_toml` to catch empty required fields.
- **Review R9 — `token_secret` env-only by default:** Inline literals are
  rejected. Use `env:VAR_NAME`. If a dev/test workflow truly needs an inline
  literal, set `[code_mode] allow_inline_token_secret_for_dev = true` and
  document it loudly.

## REF-01 superset guarantee

Your existing `config.toml` from a pmcp-run sql-api server (open-images / imdb
/ msr-vtt) parses unchanged through `ServerConfig::from_toml`. If yours
doesn't, it's a P83 bug — file an issue.

## Where to go next

- Phase 84: per-backend `SqlConnector` impls (Postgres / MySQL / Athena +
  SQLite feature). This phase will also land the FULL trait surface (`execute()`
  + placeholder translation per review R2's semver-evolution plan).
- Phase 85: `pmcp-sql-server` Shape A — a pure-config-driven binary.
- Phase 86: `cargo pmcp new --kind sql-server` scaffold + Shape C ≤15-line
  `main.rs` example.
- Phase 87: `pmcp-config-helper` — Type 2 MCP authoring skills server.
- Phase 89: this one-pager expands into a full book chapter.

## Reporting issues

`pmcp-server-toolkit` issues belong in the `paiml/rust-mcp-sdk` issue tracker.
Cross-reference Phase 83 by including `phase-83` in the issue labels.
```

Both files are static documentation; verify with `markdownlint` if available,
otherwise just confirm they exist and contain the expected sections.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && test -f .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md && test -f .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md && grep -q "pub use pmcp_server_toolkit::\\*" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md && grep -q "## Apply instructions" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md && grep -q "PMCP_RUN_PATH" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md && grep -q "## Symbol mapping" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md && grep -q "REF-01 superset" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md && grep -q "use pmcp_server_toolkit::" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md && [ "$(wc -l < .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md)" -ge 50 ] && [ "$(wc -l < .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md)" -ge 50 ] && echo "ok"</automated>
  </verify>
  <done>
    - `shim-pmcp-run-shared.md` exists with all 5 sections (Why, File 1 lib.rs, File 2 Cargo.toml diff, Apply instructions WITH `PMCP_RUN_PATH` override per R10, Rollback, Reference).
    - `pmcp-server-toolkit-migration.md` exists with all sections including the "Phase 83 review-driven behaviors" section that surfaces R3/R5/R6/R7/R8/R9 for migrators.
    - Migration guide's `main.rs` example uses single crate-root import block (review R3).
    - Both files >50 lines.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Publish-gate verification + final quality gate</name>
  <read_first>
    - `crates/pmcp-server-toolkit/Cargo.toml` (Plan 01 — `exclude = [...]` setting + all metadata)
    - `CLAUDE.md` §"Release & Publish Workflow" — pre-flight checklist
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Pitfall 6" (10MB publish limit)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §1 (Cargo.toml `exclude` whitelist)
  </read_first>
  <action>
**Step A — `cargo package --list` audit.**

```sh
cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk
cargo package --list -p pmcp-server-toolkit > /tmp/p83-pkg-list.txt 2>&1
```

Expected files in tarball:
- `Cargo.toml`, `Cargo.toml.orig`
- `src/lib.rs` + every `src/*.rs` module file
- `src/sql/mod.rs`
- `README.md`, `LICENSE*`
- `examples/e01_toolkit_minimal.rs`

NOT expected:
- `.planning/` anything
- `.pmat/` anything
- `tests/` anything (including `tests/compile_fail/`, `tests/fixtures/`)
- `fuzz/` anything

```sh
! grep -E "^(\.planning|\.pmat|tests|fuzz)/" /tmp/p83-pkg-list.txt
```

If forbidden paths leak, update `exclude = [...]` in `crates/pmcp-server-toolkit/Cargo.toml`.

**Step B — `cargo publish --dry-run`.**

```sh
cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty 2>&1 | tee /tmp/p83-publish-dry-run.log
```

**Step C — workspace-wide quality gate.**

```sh
make quality-gate 2>&1 | tee /tmp/p83-final-quality-gate.log
```

**Step D — workspace-wide build matrix.**

```sh
cargo build --workspace
cargo build --workspace --all-features
cargo test --workspace --no-fail-fast 2>&1 | tee /tmp/p83-workspace-test.log | tail -50
```

**Step E — `--no-default-features` cross-feature check (per review R6).**

```sh
cargo build -p pmcp-server-toolkit --no-default-features
cargo test -p pmcp-server-toolkit --no-default-features --lib
```

Per review R6, `SecretValue` and `SecretsProvider` must be available under `--no-default-features`. If this step fails, the cross-feature type trap from R6 was reintroduced — fix Plan 02's design before proceeding.

**Step F — Phase-83 VALIDATION.md sign-off.**

Edit `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-VALIDATION.md`:
- Set `nyquist_compliant: true` in the frontmatter.
- Mark every row as `✅` if the verifying test passed.
- Add new rows for:
  - `83-02-02` — trybuild compile-fail tests (R5)
  - `83-04-02` — ServerConfig::validate() unit tests (R8)
  - `83-06-00` — CODE_MODE_API_NOTES.md preflight (R1)
  - `83-06-02` — inline token_secret rejected without dev-flag (R9)
  - `83-07-01` — minimized SqlConnector trait shape (R2 enforcement)
  - `83-08-04` — backend_core_minimum_imports_compile (R3)
  - `83-08-05` — try_* doctests (R7)
- Update the `Approval:` line to `Approval: pending operator review`.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo package --list -p pmcp-server-toolkit > /tmp/p83-pkg-list.txt 2>&1 && ! grep -qE "^(\.planning|\.pmat|tests|fuzz)/" /tmp/p83-pkg-list.txt && cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty 2>&1 | tee /tmp/p83-publish.log | grep -qE "(Packaged|Verifying|Compiling)" && cargo build -p pmcp-server-toolkit --no-default-features && make quality-gate 2>&1 | tail -50 | grep -E "(Quality Gate.*PASSED|all checks passed)" && cargo build --workspace --quiet && grep -q "nyquist_compliant: true" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-VALIDATION.md && echo "ok"</automated>
  </verify>
  <done>
    - `cargo package --list -p pmcp-server-toolkit` outputs ONLY allowed files.
    - `cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty` succeeds.
    - `cargo build -p pmcp-server-toolkit --no-default-features` succeeds — review R6 stability check.
    - `make quality-gate` passes workspace-wide.
    - `cargo build --workspace` and `cargo test --workspace` succeed.
    - `83-VALIDATION.md` frontmatter has `nyquist_compliant: true`; matrix updated with new review-driven rows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted-toml→fuzz-target | Fuzz target feeds arbitrary bytes to the parser; any panic is a defect |
| crates.io-publish→public | Once published, every byte of the toolkit's tarball is auditable; secrets/PII/fixtures leaking through the `exclude` filter is information disclosure |
| contracts.yaml→audit | Contract entries claim what the toolkit guarantees; out-of-sync entries become provable false claims (Repudiation if audited) |
| operator-handoff→cross-repo | Per review R10, the shim apply instructions need `$PMCP_RUN_PATH` indirection so they work in any operator environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-09-01 | Denial of Service | ServerConfig::from_toml panic on adversarial TOML | mitigate | libFuzzer target ships in Task 1; CI nightly runs it |
| T-83-09-02 | Information Disclosure | Crates.io tarball ships `.planning/` / fixtures / .pmat | mitigate | Task 4 Step A audits `cargo package --list`; fails if forbidden paths appear |
| T-83-09-03 | Repudiation | contracts/binding.yaml entries diverge from actual public API | mitigate | `pmat comply check` exits 0. Per review R3, every row's `module_path` matches the crate-root form rustdoc emits — fewer ways for drift to creep in |
| T-83-09-04 | Tampering | Operator applies the shim diff and breaks pmcp-run backend cores | accept | Plan 08's in-toolkit smoke test proves the import surface; the shim diff is the cross-repo apply. Rollback section documents recovery |
| T-83-09-05 | Spoofing | Migration guide misleads developers about feature parity | mitigate | "Dropped from mcp-server-common" + "Phase 83 review-driven behaviors" sections explicitly enumerate R3/R5/R6/R7/R8/R9 so developers don't trip on the safety improvements |
| T-83-09-06 | Repudiation | Shim apply instructions hardcoded to `$HOME/Development/mcp/sdk/pmcp-run` break in CI (review R10) | mitigate | Per review R10, the apply instructions use `${PMCP_RUN_PATH:-$HOME/Development/mcp/sdk/pmcp-run}` indirection. CI / different-machine operators set the env var |
</threat_model>

<verification>
1. `crates/pmcp-server-toolkit/fuzz/Cargo.toml` and the fuzz target file exist; fuzz target uses CRATE-ROOT `pmcp_server_toolkit::ServerConfig` path (review R3).
2. `cargo +nightly check --bin pmcp_server_toolkit_config_parser` succeeds (or stable check, or documented absence).
3. `cargo build --workspace` succeeds.
4. `contracts/binding.yaml` extended with ≥10 toolkit rows; `module_path` for D-15-surface rows is the CRATE-ROOT form (review R3).
5. `pmat comply check` exits 0 (or documented absent).
6. `shim-pmcp-run-shared.md` exists, has all 5 sections, references `$PMCP_RUN_PATH` (review R10).
7. `pmcp-server-toolkit-migration.md` exists with R3/R5/R6/R7/R8/R9 behavior notes.
8. `cargo package --list -p pmcp-server-toolkit` shows NO `.planning/`, `.pmat/`, `tests/`, `fuzz/` entries.
9. `cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty` succeeds.
10. `cargo build -p pmcp-server-toolkit --no-default-features` succeeds (review R6 stability).
11. `make quality-gate` passes workspace-wide.
12. `cargo test --workspace --no-fail-fast` passes.
13. `83-VALIDATION.md` has `nyquist_compliant: true`.
</verification>

<success_criteria>
- TEST-02 ALWAYS-fuzz: fuzz target ships and is wired into the toolkit's `fuzz/` workspace.
- TKIT-01 publishability: `cargo publish --dry-run` succeeds; tarball excludes `.planning/`, `.pmat/`, `tests/`, `fuzz/`.
- TKIT-08 (cross-repo handoff): operator-facing shim diff (with `$PMCP_RUN_PATH` indirection per R10) + developer-facing migration guide (single crate-root import per R3) both shipped.
- Contract-First Development (CLAUDE.md): `contracts/binding.yaml` extended with one row per TKIT-XX public symbol; crate-root `module_path` form per review R3.
- Workspace-wide green: `make quality-gate` + `cargo test --workspace` pass.
- 83-VALIDATION.md sign-off: `nyquist_compliant: true` set; new R-driven rows added.
- Review R3 satisfied at the docs/contract layer (crate-root paths + single-import example).
- Review R10 satisfied at the operator-handoff layer (`$PMCP_RUN_PATH` indirection).
</success_criteria>

<output>
After completion, create `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-09-SUMMARY.md`. Include:
- `cargo package --list` output (or count of files in the published tarball).
- `cargo publish --dry-run` exit code + tarball size in KB.
- Number of contract rows added; how many use the crate-root `module_path` form (R3 traceability).
- Confirmation that `cargo build -p pmcp-server-toolkit --no-default-features` succeeds (review R6 stability check).
- Confirmation that all 12 P83 requirements (TKIT-01..10 + TEST-02 + TEST-03) are now covered by at least one passing test or artifact.
- Cross-repo handoff: link to the shim diff artifact + the migration guide; recommendation to operator on when to submit the pmcp-run PR (only after `pmcp-server-toolkit 0.1.0` actually ships to crates.io).
</output>
