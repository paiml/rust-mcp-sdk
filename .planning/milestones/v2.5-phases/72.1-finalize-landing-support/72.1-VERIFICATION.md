---
phase: 72.1
status: PASSED
verified: 2026-04-20
score: 13/13 must-haves verified (all 12 CR-03 ACs + AC-1 scaffold-smoke resolve)
verifier: Claude (gsd-verifier)
re_verification:
  previous: none (initial verification)
must_haves:
  truths:
    - "Scaffold produces zero NEXT_PUBLIC_COGNITO_*/LANDING_CLIENT_ID/SIGNUP_REDIRECT_AFTER reads"
    - "MCP_SERVER_NAME branding preserved in ≥1 file (actual: 6)"
    - "lib/useLandingConfig.ts exists; fetch('/landing-config' appears exactly once in the template"
    - "All four consumers (signup, signup/callback, connect, Header) import useLandingConfig"
    - "/signup renders 'Sign-up is not available for this server.' when disabled"
    - "/signup redirects to Cognito hosted UI when cfg.signup populated"
    - "/signup/callback redirects to cfg.signup.signupRedirectAfter or '/connect'"
    - "/connect interpolates cfg.mcpEndpoint in every snippet (≥4 refs, no legacy URL)"
    - "Header renders Sign-up link only when cfg loaded and cfg.signup !== null"
    - "next build succeeds with no NEXT_PUBLIC_* env vars set"
    - "AC-11 manual gate: operator-attested sign-off recorded"
    - "config.rs has zero NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER; cargo doc -p cargo-pmcp succeeds"
    - "cargo-pmcp version = 0.8.1"
  artifacts:
    - path: "cargo-pmcp/templates/landing/nextjs/lib/useLandingConfig.ts"
      status: VERIFIED
    - path: "cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx"
      status: VERIFIED
    - path: "cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx"
      status: VERIFIED
    - path: "cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx"
      status: VERIFIED
    - path: "cargo-pmcp/templates/landing/nextjs/app/components/Header.tsx"
      status: VERIFIED
    - path: "cargo-pmcp/src/landing/config.rs"
      status: VERIFIED
    - path: "cargo-pmcp/Cargo.toml"
      status: VERIFIED
---

# Phase 72.1 — Verification Report

**Phase:** 72.1 — Finalize landing support
**Verified:** 2026-04-20
**Status:** PASSED
**Re-verification:** No — initial verification
**Verifier:** Claude (gsd-verifier)

## Goal Alignment

**ROADMAP goal:** Ship CR-03 rev-2 — replace build-time `NEXT_PUBLIC_*` env vars in the landing Next.js template with a runtime `fetch('/landing-config')` via a new required shared hook `useLandingConfig`, fix 3 stale rustdoc references in `cargo-pmcp/src/landing/config.rs`, and bump `cargo-pmcp` 0.8.0 → 0.8.1.

The delivered code meets every part of the goal. `lib/useLandingConfig.ts` exists as the single fetch site (confirmed: `fetch('/landing-config'` appears **exactly once** in the template, at `lib/useLandingConfig.ts:36`). All four consumer files (`signup`, `signup/callback`, `connect`, `Header`) import and route through the hook. Every `NEXT_PUBLIC_COGNITO_*`, `NEXT_PUBLIC_LANDING_CLIENT_ID`, and `NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER` read has been deleted (grep returns zero matches across the template). `MCP_SERVER_NAME` branding reads remain intact across the expected six files (`app/layout.tsx`, `app/components/Hero.tsx`, `app/components/Installation.tsx`, `app/components/Header.tsx`, `lib/config.ts`, `next.config.js`).

Running `cargo run -p cargo-pmcp -- landing init --template nextjs ...` against a temp dir produces a scaffold whose four consumers all import `useLandingConfig` and contain zero `NEXT_PUBLIC_*` reads. `next@14 build` with all four `NEXT_PUBLIC_*` vars unset compiles successfully and prerenders all 7 routes. `cargo-pmcp/Cargo.toml` is at `0.8.1`. `cargo doc --no-deps -p cargo-pmcp` exits 0 with only unrelated intra-doc-link warnings. `make quality-gate` exits 0. The three stale `NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER` rustdoc references in `cargo-pmcp/src/landing/config.rs` have been rewritten to describe the runtime `/landing-config` mechanism (lines ~29, ~181, ~448). The phase goal is achieved.

## Acceptance Criteria Results (CR-03 §Acceptance criteria)

| AC | Description | Check | Result | Evidence |
|----|-------------|-------|--------|----------|
| AC-1 | `pmcp new --template landing` scaffolds cleanly | Ran `cargo run -p cargo-pmcp -- landing init --template nextjs --output $SMOKE/smoke-landing --server-name smoke-test`; verified `lib/useLandingConfig.ts` present; all 4 consumers import the hook; 0 `NEXT_PUBLIC_*` reads | **PASS** | `AC-1 scaffold smoke: PASS` — `hook present: YES`; all 4 consumers "uses useLandingConfig"; grep for stale `NEXT_PUBLIC_*` returned nothing |
| AC-2 | Zero `NEXT_PUBLIC_COGNITO_*` / `_LANDING_CLIENT_ID` / `_SIGNUP_REDIRECT_AFTER` reads in template | `grep -rE 'NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID\|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/templates/landing/nextjs/` | **PASS** | exit 1 (no matches) |
| AC-3 | `MCP_SERVER_NAME` branding preserved in ≥1 file | `grep -rl 'MCP_SERVER_NAME' cargo-pmcp/templates/landing/nextjs/` | **PASS** | 6 files: `Hero.tsx`, `Installation.tsx`, `Header.tsx`, `layout.tsx`, `next.config.js`, `lib/config.ts` |
| AC-4 | `useLandingConfig` hook exists and is the single fetch site | `grep -rnE "fetch\\('/landing-config'" cargo-pmcp/templates/landing/nextjs/`; `grep -rln "useLandingConfig"` | **PASS** | Exactly 1 fetch at `lib/useLandingConfig.ts:36`; 5 files reference `useLandingConfig` (hook + 4 consumers) |
| AC-5 | `/signup` renders "Sign-up is not available for this server." copy | Source inspection of `app/signup/page.tsx` | **PASS** | Line 34: `{disabled ? 'Sign-up is not available for this server.' : 'Redirecting to sign-up…'}` — `disabled = cfg === 'error' \|\| (cfg !== null && !cfg.signup)` |
| AC-6 | `/signup` builds Cognito redirect from `cfg.signup` when populated | Source inspection | **PASS** | Lines 17–27: destructures `cognitoDomain`, `cognitoRegion`, `landingClientId` from `cfg.signup`, builds `https://${cognitoDomain}.auth.${cognitoRegion}.amazoncognito.com/signup` URL with `client_id`, `response_type=code`, `scope=openid email`, `redirect_uri=${origin}/signup/callback`, then `window.location.replace` |
| AC-7 | `/signup/callback` redirects to `cfg.signup?.signupRedirectAfter` or `/connect` | Source inspection of `app/signup/callback/page.tsx` | **PASS** | Line 22–23: `const redirect = (cfg !== 'error' && cfg.signup?.signupRedirectAfter) \|\| '/connect'`; `router.replace(redirect)` on line 24 |
| AC-8 | `/connect` uses `cfg.mcpEndpoint` throughout (≥4), no legacy URL | `grep -c 'cfg.mcpEndpoint' connect/page.tsx`; `grep -E '\\.us-east\\.true-mcp\\.com\|\\$\\{serverName\\}' connect/page.tsx` | **PASS** | 4 `cfg.mcpEndpoint` references (lines 48, 54, 59, 65); 0 legacy URL matches (exit 1) |
| AC-9 | Header renders Sign-up link only when signup enabled | Source inspection of `Header.tsx` | **PASS** | Line 10: `signupEnabled = cfg !== null && cfg !== 'error' && cfg.signup !== null`; line 25: `{signupEnabled && (<Link href="/signup">...`. `MCP_SERVER_NAME`-driven logo label preserved on line 6 |
| AC-10 | `next build` succeeds with no `NEXT_PUBLIC_*` vars set | `env -u NEXT_PUBLIC_COGNITO_DOMAIN -u NEXT_PUBLIC_COGNITO_REGION -u NEXT_PUBLIC_LANDING_CLIENT_ID -u NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER MCP_SERVER_NAME=verify-smoke npx -y next@14 build` | **PASS** | "✓ Compiled successfully" + "✓ Generating static pages (7/7)"; 5 routes pre-rendered (`/`, `/_not-found`, `/connect`, `/signup`, `/signup/callback`) |
| AC-11 | Manual offline gate: `/landing-config` network-blocked returns `'error'` state | Operator-attested sign-off recorded 2026-04-20 | **VERIFIED (operator-attested)** | "approved — AC-11 all PASS — operator guy 2026-04-20" per phase context and SUMMARY `key-decisions` |
| AC-12 | `config.rs` has zero `NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER`; `cargo doc` succeeds | `grep -nE 'NEXT_PUBLIC_*' cargo-pmcp/src/landing/config.rs`; `cargo doc --no-deps -p cargo-pmcp` | **PASS** | 0 `NEXT_PUBLIC_*` matches; `cargo doc` exits 0 (6 unrelated intra-doc-link warnings, none about landing::config or signup). The 3 rustdoc edits land at lines ~29 (`signup` field doc), ~181 (`validate_relative_path` doc), ~448 (regression-test doc) and all describe the runtime `/landing-config` mechanism |

## Guardrail Matrix (G1–G8)

| Guardrail | Command | Expected | Actual | Result |
|-----------|---------|----------|--------|--------|
| G1 | `grep -rE 'NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID\|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/templates/landing/nextjs/` | 0 matches | 0 matches (exit 1) | **PASS** |
| G2 | `grep -rl 'MCP_SERVER_NAME' cargo-pmcp/templates/landing/nextjs/ \| wc -l` | ≥ 1 | 6 | **PASS** |
| G3 | `grep -rE "fetch\\('/landing-config'" cargo-pmcp/templates/landing/nextjs/` | exactly 1 match | 1 match (`lib/useLandingConfig.ts:36`) | **PASS** |
| G4 | `grep -l 'useLandingConfig' {signup/page,signup/callback/page,connect/page,components/Header}.tsx` | 4 matches | 4 matches | **PASS** |
| G5 | `≥4 cfg.mcpEndpoint` AND zero `\.us-east\.true-mcp\.com\|\$\{serverName\}` in `connect/page.tsx` | both clauses pass | 4 refs; 0 hand-assembled URLs (exit 1) | **PASS** |
| G6 | `grep -nE 'NEXT_PUBLIC_*' cargo-pmcp/src/landing/config.rs` + `cargo doc --no-deps -p cargo-pmcp` | 0 matches + exit 0 | 0 matches; cargo doc exit 0 | **PASS** |
| G7 | Scaffold smoke (AC-1) | scaffolded hook + 4 consumers + 0 NEXT_PUBLIC | all five checks green | **PASS** |
| G8 | LOC budget (Codex M2): `git diff --numstat 014c7307~1..HEAD -- <4 consumers> \| awk sum` | ≤ ~100 added | 77 added (14 + 34 + 13 + 16) | **PASS** |

## Key Links (Wiring)

| From | To | Via | Status |
|------|-----|-----|--------|
| `app/signup/page.tsx` | `lib/useLandingConfig.ts` | `import { useLandingConfig } from '@/lib/useLandingConfig'` (line 4) | **WIRED** — hook called on line 11; result drives useEffect + disabled state |
| `app/signup/callback/page.tsx` | `lib/useLandingConfig.ts` | `import { useLandingConfig } from '@/lib/useLandingConfig'` (line 5) | **WIRED** — hook called on line 18; result drives useEffect redirect |
| `app/connect/page.tsx` | `lib/useLandingConfig.ts` | `import { useLandingConfig } from '@/lib/useLandingConfig'` (line 5) | **WIRED** — hook called on line 8; result gates loading/error branches and feeds `cfg.mcpEndpoint` into 4 snippets |
| `app/components/Header.tsx` | `lib/useLandingConfig.ts` | `import { useLandingConfig } from '@/lib/useLandingConfig'` (line 4) | **WIRED** — hook called on line 9; drives `signupEnabled` conditional render |
| browser | Amplify 200-rewrite → API GW → Lambda | `GET /landing-config` (same-origin, `cache: 'no-store'`) | **WIRED (template-side)** — fetch contract at `useLandingConfig.ts:36`. Downstream platform side is out of scope for this phase (CR-03 §Contract states platform already serves the endpoint) |

## Data-Flow Trace (Level 4)

The hook's fetched `cfg` actually flows to user-visible output — not a hardcoded shell:

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| `lib/useLandingConfig.ts` | `cfg` state | `fetch('/landing-config', { cache: 'no-store' })` → JSON → `setCfg` | Platform contract (CR-03 §Contract); null/error fallback handled | **FLOWING** |
| `app/signup/page.tsx` | `cfg.signup.cognitoDomain/Region/landingClientId` | From `useLandingConfig()` | Used to build redirect URL on line 17–27 | **FLOWING** |
| `app/signup/callback/page.tsx` | `cfg.signup.signupRedirectAfter` | From `useLandingConfig()` | Used as redirect target on line 22–24 | **FLOWING** |
| `app/connect/page.tsx` | `cfg.mcpEndpoint` | From `useLandingConfig()` | Interpolated into 4 client-config snippets (lines 48, 54, 59, 65) | **FLOWING** |
| `app/components/Header.tsx` | `cfg.signup !== null` | From `useLandingConfig()` | Drives `signupEnabled` → conditional `<Link>` render (line 25) | **FLOWING** |

No props are hardcoded empty at call sites; no static fallback is rendered in the success branch. Data truly flows through every wired artifact.

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Scaffold produces runnable template | `cargo run -p cargo-pmcp -- landing init --template nextjs --output $SMOKE/smoke-landing --server-name smoke-test` | "Landing page initialized successfully" + all expected files | **PASS** |
| Hook is the single fetch site | `grep -rc "fetch\\('/landing-config'" templates/landing/nextjs/` | 1 file, 1 match | **PASS** |
| `next build` green in the scaffold with no NEXT_PUBLIC_* set | `npm install && env -u NEXT_PUBLIC_* npx -y next@14 build` | "✓ Compiled successfully"; 5 pre-rendered routes | **PASS** |
| `cargo doc -p cargo-pmcp` green | `cargo doc --no-deps -p cargo-pmcp` | exit 0; 0 landing-config-related warnings | **PASS** |
| `make quality-gate` green | `make quality-gate` | exit 0; `✓ Code formatting OK`, `✓ No lint issues` | **PASS** |

## Quality Gate

`make quality-gate` → **EXIT 0**.

- `✓ Code formatting OK` (cargo fmt --all --check)
- `✓ No lint issues` (clippy pedantic + nursery + cargo, `-D warnings`)
- Full test suite ran across all crates (`src/lib.rs` unit tests + ~40 integration test binaries + doctests); no test failures.
- Notes: `cargo fuzz build` phase emitted errors ("option `Z` only accepted on nightly") because the local stable toolchain does not satisfy fuzz's nightly requirement. The Makefile tolerates this for fuzz targets and the overall gate still exits 0. CI will re-verify on a toolchain that supports fuzz. This is a pre-existing local-env characteristic unrelated to Phase 72.1.
- `cargo doc --no-deps -p cargo-pmcp` → **EXIT 0** with only pre-existing intra-doc-link warnings in the `pentest` module (not landing-config).

## Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| LAND-CR03-01 | `.planning/REQUIREMENTS.md:72` / `:167` | cargo-pmcp 0.8.1 — landing template uses runtime `/landing-config` via `useLandingConfig`; all 4 consumers route through the hook; `NEXT_PUBLIC_*` reads deleted; `MCP_SERVER_NAME` branding kept; 3 rustdoc refs rewritten; patch version bump | **SATISFIED** | 12 ACs PASS + G1–G8 PASS + commit chain `72e63915` (hook) → `a488184f` (4 consumers) → `c5b392b8` (3 rustdoc edits) → `e6b72a87` (version bump) → `50ce9ec4` (done marker) |

`.planning/REQUIREMENTS.md` `:167` table entry: `LAND-CR03-01 \| Phase 72.1 \| Complete`. `.planning/ROADMAP.md` `:970–978` has Phase 72.1 marked `[x]`.

## Out-of-Scope Regression Checks

- **Test harness added?** No. `package.json` `dependencies` = `next 14.2.10`, `react ^18.2.0`, `react-dom ^18.2.0` only. `devDependencies` = `@types/node`, `@types/react`, `@types/react-dom`, `autoprefixer`, `postcss`, `tailwindcss`, `typescript`. No `jest`, `vitest`, or `playwright`.
- **New npm deps?** No (same as above).
- **Struct/signature changes in `config.rs`?** No — only doc-comment prose differs. `LandingConfig`, `SignupConfig`, and `redirect_after` shapes unchanged.
- **`v0.8.1` tag leaked?** No. `git tag -l 'v0.8.1'` empty; `git ls-remote --tags origin | grep 'v0\.8\.1'` empty. Publishing is deferred to the release workflow.
- **`MCP_SERVER_NAME` scope boundary (stricter than G2):** All six expected files still reference `MCP_SERVER_NAME`: `app/layout.tsx`, `app/components/Hero.tsx`, `app/components/Installation.tsx`, `app/components/Header.tsx`, `lib/config.ts`, `next.config.js`. No boundary regression.

## Deviations from Plan

Both deviations are explicitly documented in `72.1-01-SUMMARY.md` `key-decisions` and are verified to be benign:

1. **Four consumer files bundled in a single commit (`a488184f`)** — explicitly permitted by the planner to keep the hook + 4 callers type-checking at every intermediate state. Confirmed via `git log --oneline` — this was a deliberate atomic commit.
2. **Explicit `cfg !== null` in the `disabled` expression in `signup/page.tsx`** — CR-03 §2 suggested `cfg === 'error' || (cfg && !cfg.signup)`; the shipped code uses `cfg === 'error' || (cfg !== null && !cfg.signup)` (line 30). Since the `LandingConfigState` union includes the string literal `'error'` (truthy), the explicit `cfg !== null` form is strictly more defensive and behaviourally identical. Confirmed.

No undocumented deviations detected.

## Deferred Items (confirmed still open)

These are explicitly scoped out of CR-03 rev-2 and not blocking:

- **CI for runtime redirect/disabled-state behavior (Codex L4)** — no Playwright / e2e harness added; AC-11 relied on manual operator gate. Fine for 0.8.1 patch.
- **Module-level cache for `useLandingConfig`** — each mount re-fetches; acceptable because `/landing-config` is small and same-origin.
- **Sign-in flow, SSR variant, custom-domain template** — explicitly CR-03 out-of-scope.
- **Publishing `cargo-pmcp 0.8.1` to crates.io** — handled by the release workflow on a `v*` tag push, not this phase.
- **`cargo fuzz build` on nightly in local env** — not a Phase 72.1 gap; pre-existing env characteristic; CI handles fuzz on the correct toolchain.

## Operator Sign-Off

**AC-11 manual offline gate**: approved — AC-11 all PASS — operator `guy` on 2026-04-20. Recorded in phase context and reflected in SUMMARY `key-decisions`.

## Verdict

All twelve CR-03 acceptance criteria pass, all eight guardrails (G1–G8) pass, the scaffold-smoke integration test reproduces the expected behavior from a clean temp dir, `next@14 build` compiles successfully with no `NEXT_PUBLIC_*` vars set, `cargo-pmcp 0.8.1` is reflected in `Cargo.toml`, `make quality-gate` exits 0, and `cargo doc -p cargo-pmcp` exits 0. Every artifact exists, is substantive, is wired into consumers, and carries real data end-to-end. No undocumented deviations, no regression in scope boundaries, no leaked tags or dependencies. Phase 72.1 ships CR-03 rev-2 exactly as the ROADMAP promised and unblocks pmcp.run Phase 71 UAT Test 7 and the Cost Coach production launch. Ready for the release workflow to pick up the `0.8.1` version bump.

## VERIFICATION PASSED

_Verified: 2026-04-20_
_Verifier: Claude (gsd-verifier)_
