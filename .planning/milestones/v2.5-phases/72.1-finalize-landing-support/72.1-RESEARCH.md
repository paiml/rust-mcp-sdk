# Phase 72.1: Finalize landing support — Research

**Researched:** 2026-04-20
**Revised:** 2026-04-20 (rev-2 — Codex review M1 / M3 / L2 wording fixes)
**Domain:** Next.js 14 static-export landing template + cargo-pmcp rustdoc
**Confidence:** HIGH (authoritative PRD locked in CR-03; template "before" state re-verified on disk)

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

- **Authoritative PRD:** `72.1-CR-03-SOURCE.md` rev-2. Every contract decision (endpoint shape, response type, acceptance criteria) is locked there — do not re-derive.
- **Contract:** `GET {origin}/landing-config`, always HTTP 200, `Cache-Control: no-store`, same-origin via Amplify rewrite. Response: `{ mcpEndpoint: string, signup: SignupConfig | null }`. 404 treated identically to `'error'`.
- **No `NEXT_PUBLIC_*` fallback window** — reads are **deleted**, not defaulted. Platform actively strips them every deploy.
- **`MCP_SERVER_NAME` STAYS** — build-time brand injection in 6 template files + `next.config.js`. Only the **endpoint-URL assembly** in `connect/page.tsx` is replaced by `cfg.mcpEndpoint`. Header/title/hero/install/`config.ts`/`connect`-branding uses stay untouched.
- **Hook is REQUIRED** (not optional). `lib/useLandingConfig.ts` is a new file; all four consumers route through it. `fetch('/landing-config', …)` must appear exactly once in the template (inside the hook).
- **Hook return type:** `LandingConfigState = LandingConfig | null | 'error'`. Three narrowable states: `null` = loading, `'error'` = failure, `LandingConfig` = loaded (then branch on `cfg.signup`). No retry, no backoff, no module-level cache.
- **Files changed (template):** `lib/useLandingConfig.ts` (NEW), `app/signup/page.tsx`, `app/signup/callback/page.tsx`, `app/connect/page.tsx` (server→client flip), `app/components/Header.tsx` (note: repo path is `app/components/Header.tsx`, NOT `components/Header.tsx` as the CR text says).
- **Files changed (Rust):** `cargo-pmcp/src/landing/config.rs` at lines 29, 180, 445 — rustdoc-only. No struct/signature/serde changes.
- **Version bump:** `cargo-pmcp` 0.8.0 → 0.8.1 (patch, additive). Bump lands last, after all quality gates pass. No downstream workspace crate pins cargo-pmcp; bump is solo.
- **No release in this phase** — tag/push/publish happens separately via `/gsd-ship` or manual `git tag` per CLAUDE.md.

### Claude's Discretion

- Snippet text/ergonomics in `connect/page.tsx` (minimum: 4 snippets interpolating `cfg.mcpEndpoint` verbatim — Claude Desktop, Claude Code, ChatGPT, Generic MCP CLI).
- Loading-state copy ("Loading connection instructions…", "Connection instructions unavailable. Please try again.").
- Wave grouping (planner's call; single wave recommended given tight coupling).
- Task granularity in PLAN.md (per-file or bundled — both acceptable).

### Deferred Ideas (OUT OF SCOPE)

- Sign-in flow.
- SSR variant of the template.
- Module-level cache / SWR-style dedup for `useLandingConfig`.
- Playwright / e2e harness (template has no test harness today; not introducing one).
- CI coverage for runtime redirect / disabled-state behavior (Codex L4 — listed in CONTEXT.md deferred).
- JSON shape validation in the hook (Codex M1 disposition "a" — relaxed to match CR-03 verbatim).
- Custom-domain template for non-pmcp.run targets.
- Deletion of the old `/signup-config` platform route (pmcp.run follow-up phase, not SDK work).
</user_constraints>

## Phase Summary

Phase 72.1 ships a thin, PRD-complete consolidation of CR-03 rev-2: a new required React hook (`lib/useLandingConfig.ts`) plus surgical rewrites of four consumer files in the landing Next.js template, three rustdoc-only edits in `cargo-pmcp/src/landing/config.rs`, and a patch bump from 0.8.0 → 0.8.1. The driver is Phase 71.1 platform behavior: `deploy-landing` now actively strips the four `NEXT_PUBLIC_COGNITO_*` / `NEXT_PUBLIC_LANDING_CLIENT_ID` / `NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER` env vars on every deploy, so any build-time read returns `undefined`. The template has been permanently broken for signup since the strip landed; this phase closes the gap by moving config discovery from `next build` time to browser runtime via a same-origin `GET /landing-config` fetch.

The change surface is small (~100 LOC of TypeScript + 3 rustdoc edits + 1 version bump) but tightly coupled — all four template files must shift atomically (or in an ordering that keeps the template type-checkable at every step), and the `connect/page.tsx` is the only page that flips from server to client component. The contract shape is fully locked in CR-03; research adds no new decisions. The remaining risk surface is (a) getting the `'use client'` flip on `connect/page.tsx` right (module-scope `mcpClientConfig` must move inside the component), (b) not overreaching into `MCP_SERVER_NAME` reads (scope boundary), and (c) validating AC-11 (offline `next build && next start`) which has no automation path — it's a manual gate by design.

Primary recommendation: single-wave, single PLAN.md with ~5 tasks. Hook creation, 4 consumer rewrites (bundle or split — planner's call), rustdoc edits, version bump, final `make quality-gate`. All grep guardrails from CONTEXT.md lift into `must_haves` verbatim. **rev-2 addition:** the final task now includes a G7 scaffold smoke step exercising `cargo pmcp landing init --template nextjs` against a temp dir, added per Codex M3 to directly exercise AC-1 rather than infer it.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Runtime config discovery (endpoint + signup) | Browser / Client | — | `output: 'export'` forbids server fetch; same-origin rewrite means no CORS preflight; platform guarantees always-200 response |
| Cognito hosted-UI redirect URL assembly | Browser / Client | — | Needs `window.location.origin` for `redirect_uri`; only known at runtime |
| Signup button visibility decision | Browser / Client | — | Requires `cfg.signup !== null` which is only known post-fetch |
| MCP endpoint interpolation in snippets | Browser / Client | — | Replaces build-time `MCP_SERVER_NAME` assembly with runtime `cfg.mcpEndpoint` |
| Brand text (title, logo label, hero, install copy) | Frontend build (static) | — | `MCP_SERVER_NAME` stays — rare intentional redeploy event, safe to bake |
| TOML config documentation (rustdoc) | Rust crate | — | Pure doc hygiene; `SignupConfig.redirect_after` behavior unchanged |

## Technical Approach

### Hook design invariants (`lib/useLandingConfig.ts`)

The planner's tasks MUST enforce these invariants — they are the contract the four consumers depend on:

1. **File path:** `cargo-pmcp/templates/landing/nextjs/lib/useLandingConfig.ts` (NEW). The `lib/` directory already exists (`lib/config.ts` is there today).
2. **Directive:** `'use client'` at line 1.
3. **Exports:** `export type LandingConfig`, `export type LandingConfigState = LandingConfig | null | 'error'`, `export function useLandingConfig(): LandingConfigState`. Types must be exported so consumers narrow via the same symbols.
4. **Ignore-after-unmount (not "abort-on-unmount" — Codex L2):** `useEffect` must set a `cancelled` flag in cleanup and guard both `setCfg` calls with it. This prevents the React warning "Can't perform a state update on an unmounted component" on route churn. It does NOT abort the in-flight fetch — there is no `AbortController` by design (CR-03 §1 verbatim). Earlier drafts called this "abort-on-unmount"; the wording has been corrected throughout.
5. **Fetch:** `fetch('/landing-config', { cache: 'no-store' })`. The literal string `'/landing-config'` is the only matching occurrence allowed in the template (AC-4 guardrail).
6. **Error contract (Codex M1 — corrected):** anything non-OK (HTTP 4xx/5xx, network failure, non-JSON parse failure) → `setCfg('error')`. The hook does NOT validate the JSON shape of a 200 response — `setCfg(c as LandingConfig)` casts any 200 JSON payload without checking field types. The `'error'` state therefore covers transport / HTTP / parse failures only, NOT shape mismatches. Earlier drafts claimed `'error'` covers "unexpected shape", which is inaccurate relative to the verbatim CR-03 hook body; CR-03 §1 is authoritative and specifies the hook verbatim, so shape validation would invent a contract the platform did not agree to. Disposition per review: option "a" — relax the plan text, not option "b" — add shape check. No retry, no backoff, no telemetry.
7. **State narrowing:** consumers distinguish via `cfg === null` (loading), `cfg === 'error'` (failed), else loaded — then branch on `cfg.signup`.

### Consumer rewrite patterns — what changes per file

| File | Delete | Add | Keep |
|------|--------|-----|------|
| `app/signup/page.tsx` | 3 `process.env.NEXT_PUBLIC_COGNITO_*` + `NEXT_PUBLIC_LANDING_CLIENT_ID` reads (lines 11–13), `console.error` missing-env guard (lines 15–22) | `useLandingConfig()` call, three-state narrowing inside `useEffect`, "Sign-up is not available for this server." disabled-state copy | `'use client'`, `useEffect`, Cognito URL assembly pattern (now sourced from `cfg.signup`), **existing Tailwind classNames and copy that CR-03 §2 does not touch (Codex M2 preservation rule)** |
| `app/signup/callback/page.tsx` | `process.env.NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER` read (line 17) | `useLandingConfig()` call, fallback `'/connect'` on error/null | `'use client'`, `useRouter`, `useEffect`, **existing classNames/copy (Codex M2)** |
| `app/connect/page.tsx` | Module-scope `serverName` const (line 6), module-scope `mcpClientConfig` object (lines 8–15), hand-assembled `https://${serverName}.us-east.true-mcp.com` in ChatGPT snippet (line 44), `pmcp connect ${serverName}` command string (line 50) | `'use client'` at line 1, `useLandingConfig()` inside component, per-render `endpoint = cfg.mcpEndpoint`, loading/error JSX branches | `<Header />` / `ConnectSnippet` imports; 4 snippets structure (Claude Desktop, Claude Code, ChatGPT, Generic MCP CLI); any remaining `MCP_SERVER_NAME` branding/heading copy; **existing snippet titles, instructions, headings, and classNames (Codex M2)** |
| `app/components/Header.tsx` | Unconditional `<Link href="/signup">Sign up</Link>` (lines 19–24) | `'use client'`, `useLandingConfig()` call, conditional render `signupEnabled && <Link …>` where `signupEnabled = cfg !== null && cfg !== 'error' && cfg.signup !== null` | existing `MCP_SERVER_NAME`-driven logo label (line 3), `<nav>` structure, "Connect" link, **any additional nav links / classNames / headings (Codex M2)** |

**Preservation rule (Codex M2 — lift into PLAN.md):** CR-03 §2–§5 inline code blocks are a specification of the new behavior, NOT an instruction to replace whole files. When a CR-03 snippet contains only a subset of what's in the current file, merge that subset into the existing file rather than overwriting. Preserve all existing styling (Tailwind classNames), copy, imports, component structure, and comments that are not required to change by CR-03. Target LOC delta across the 4 consumer files: ≤ 100 lines added.

### Server→client conversion — `connect/page.tsx` landmine

Today the file has no `'use client'` directive — it's a React Server Component. The module-scope consts `serverName` (line 6) and `mcpClientConfig` (lines 8–15) read `process.env.MCP_SERVER_NAME` at SSR time, which is fine for a server component but **will not work inside a client component** the same way (Next.js inlines `process.env.MCP_SERVER_NAME` at build via `next.config.js`'s `env:` block — so it technically *would* still resolve, but the moral semantics change). More importantly, `mcpClientConfig` references `cfg.mcpEndpoint` once the fetch lands — and `cfg` is only available per-render, not at module scope. So the constant **must move inside the component body**.

Concrete order of operations in the rewrite:

1. Add `'use client'` as line 1.
2. Replace `import Header from '../components/Header'` / `import ConnectSnippet from '../components/ConnectSnippet'` imports left as-is.
3. Delete module-scope `serverName` and `mcpClientConfig`.
4. Inside `export default function Connect()`: call `useLandingConfig()`, handle `null` / `'error'` with JSX early returns, then compute `const endpoint = cfg.mcpEndpoint` and build per-render `mcpClientConfig` if needed.
5. Interpolate `endpoint` (or `cfg.mcpEndpoint`) into each of the 4 snippets. Do NOT reassemble a URL from any `serverName` variable. Ensure at least 4 literal occurrences of `cfg.mcpEndpoint` remain in the file (G5 tightened per Codex L3 now asserts `grep -c 'cfg.mcpEndpoint' >= 4` in addition to the negative grep).
6. Existing `<Header />` usage stays. Header is independently converted to a client component in the Header rewrite — no SSR regression because no ancestor is currently server-rendered except this page.

### Rustdoc edit strategy

Three mechanical text replacements in `cargo-pmcp/src/landing/config.rs`:

| Line | Before (confirmed via Read) | After |
|------|------------------------------|-------|
| 29 | `/// injected into the Next.js build as \`NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER\`.` | `/// served by the platform at runtime via GET /landing-config and consumed by the template's useLandingConfig hook.` |
| 180 | `/// the landing template consumes \`NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER\`.` | `/// the landing template reads the resolved redirect target from /landing-config at runtime; this TOML value is what deploy-landing writes into ServerOAuthConfig.signupRedirectAfter, which /landing-config returns.` |
| 445 | `    /// and inject \`NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER\` into the build env.` | `    /// the platform writes this into ServerOAuthConfig.signupRedirectAfter; the runtime /landing-config endpoint returns it to the browser.` |

Line 445 is inside a `#[test]` doc comment (`test_signup_section_round_trips_through_toml`) — the rewrite only touches the comment body, never the test logic. No `SignupConfig` / `redirect_after` signature change. No serde rename. No compile-ordering risk. `cargo clippy` does not inspect doc text for the banned identifiers; only `grep` does. `cargo doc --no-deps -p cargo-pmcp` will regenerate the rustdocs with the new text — validation is grep (0 matches) + cargo doc (zero warnings/errors).

### Scaffold smoke test (G7 — Codex M3, rev-2 addition)

The real scaffold command in this repo is `cargo pmcp landing init --template nextjs --output <dir> --server-name <name>`. Verified via `cargo-pmcp/src/commands/landing/init.rs`:

```rust
pub async fn init_landing_page(
    project_root: PathBuf,
    template_name: String,
    output: PathBuf,
    server_name: Option<String>,
) -> Result<()>
```

The function validates `template_name == "nextjs"`, resolves a server name (from `--server-name` or fallback detection), creates the output directory, clones the template from git, and runs `template::replace_variables_in_files(&output, &vars)` to substitute `{{SERVER_NAME}}`/`{{TITLE}}`/etc. placeholders.

CR-03 AC-1's phrasing `pmcp new --template landing` is **loose platform-team wording** — there is no `pmcp new` subcommand in this repo. G7 therefore runs the real command:

```bash
SMOKE=$(mktemp -d)
( cd "$SMOKE" && cargo run --quiet -p cargo-pmcp -- landing init --template nextjs --output smoke-landing --server-name smoke-test )
test -f "$SMOKE/smoke-landing/lib/useLandingConfig.ts" \
  && grep -q useLandingConfig "$SMOKE/smoke-landing/app/signup/page.tsx" \
  && grep -q useLandingConfig "$SMOKE/smoke-landing/app/signup/callback/page.tsx" \
  && grep -q useLandingConfig "$SMOKE/smoke-landing/app/connect/page.tsx" \
  && grep -q useLandingConfig "$SMOKE/smoke-landing/app/components/Header.tsx" \
  && ! grep -rE 'NEXT_PUBLIC_COGNITO|NEXT_PUBLIC_LANDING_CLIENT_ID|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' "$SMOKE/smoke-landing/" \
  && echo "AC-1 scaffold smoke: PASS"
```

This is cheap (seconds) and closes the biggest coverage gap identified by the Codex review — without it, AC-1 is only inferred from "template source was changed" rather than directly exercised. Full definition lives in PLAN.md Task 5 Step 5.4b.

Open question for future phases: the scaffold clones from a git repo (`template::clone_template`); a CI-time check could alternatively assert the git ref points at the current commit so the scaffold always uses the in-repo template source. For Phase 72.1 the local `cargo run -p cargo-pmcp` invocation picks up the workspace binary, which picks up the local template dir, so G7 is accurate for local runs.

## Risks & Landmines

1. **`output: 'export'` incompatibility with server-side fetch.** `next.config.js` locks `output: 'export'`. Any `getServerSideProps` or Route Handler that calls `fetch('/landing-config')` at build time would fail or bake a stale value. Client-side fetch inside `useEffect` is the only path. Planner should not consider alternatives.

2. **Duplicate hook fetches across consumers, max ~2 per page (Codex L1 — corrected from "4 concurrent fetches").** Under the current route layout, `Header` always mounts plus the current-page component (signup / callback / connect) — each fires its own `useLandingConfig` fetch on hydration. So the realistic maximum is ~2 fetches per page, not 4. The `/landing-config` response is <200B and same-origin; HTTP caching is explicitly off (`no-store`). CR-03 §"Why bundle" + CONTEXT §"Hook design" both accept this. Planner should NOT add module-level caching. Note in plan; don't fix.

3. **`cargo doc` regenerates from source.** Any regression to the rustdoc text (e.g., an autofix that touches the file) would silently hide the fix. Validation must include both `grep -n 'NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/src/landing/config.rs` (expect 0) AND `cargo doc --no-deps -p cargo-pmcp` (expect success). `cargo doc` is not currently part of `make quality-gate`, so the plan task MUST invoke it explicitly.

4. **No template test harness.** `package.json` has no jest/vitest/playwright. Verification is: grep guardrails (cheap, deterministic), `tsc --noEmit` (catches hook/consumer type drift), `next build` (catches static-export-breaking errors), **G7 scaffold smoke** (rev-2 — exercises the full `cargo pmcp landing init` pipeline against a temp dir), manual `next start` check (AC-11). Planner should NOT propose adding a test framework — explicitly deferred.

5. **`cargo clippy --features full` pedantic lints.** The rustdoc edit is pure comment text; clippy's `missing_docs_in_private_items` / `doc_markdown` lints *could* fire if the new text contains bare identifiers rendered as `CamelCase` items (e.g., `ServerOAuthConfig.signupRedirectAfter`). Mitigation: wrap bare identifiers in backticks (`` `ServerOAuthConfig` ``, `` `signupRedirectAfter` ``) OR test with `make quality-gate` and fix only if it fires. Flag for planner as a verification-step concern, not a blocking design issue.

6. **Scope boundary drift.** A naive "remove env vars from landing template" search-and-replace will delete `MCP_SERVER_NAME` reads and churn branding in 6 files. This is the exact failure mode CR-03 §0 was written to prevent. Guardrail: `grep -r 'MCP_SERVER_NAME' cargo-pmcp/templates/landing/nextjs/` → **≥1** (today's baseline: 7 files matching; after this phase still 7 or 6 if `connect/page.tsx` branding is relocated — never 0).

7. **Version-bump ordering.** Bumping `Cargo.toml` to 0.8.1 before quality-gate passes means a failed build still mutates the version. Must be last task in the plan.

8. **Consumer rewrite regression churn (Codex M2).** Full-file replacements for the 4 consumer files risk losing existing classNames, copy, or local branding that CR-03 did not require to change. Mitigation: preservation rule (merge behavior changes into existing files) + LOC budget (≤100 added lines across the 4 files, enforced in Task 2 verify).

9. **Scaffold pipeline drift (Codex M3 — rev-2).** If a future change adds an explicit file manifest (e.g., `include_bytes!` for each template file, or an index.json listing template paths), a newly-added template file like `lib/useLandingConfig.ts` could be silently omitted from the scaffold output. G7 scaffold smoke detects this by running the full `cargo pmcp landing init` against a temp dir and asserting the hook file is present in the output.

## Validation Architecture

### Test Framework
| Property | Value |
|----------|-------|
| Framework | **Rust:** `cargo test` (workspace) — existing. **Template:** TypeScript `tsc --noEmit` + `next build` — no unit-test framework exists (by design). |
| Config file | `cargo-pmcp/Cargo.toml`, `cargo-pmcp/templates/landing/nextjs/next.config.js`, `cargo-pmcp/templates/landing/nextjs/tsconfig.json` (assumed present per Next.js 14 scaffold) |
| Quick run command | `make quality-gate` (matches CI) |
| Full suite command | `make quality-gate && cargo doc --no-deps -p cargo-pmcp && (cd cargo-pmcp/templates/landing/nextjs && npx -y typescript@5 tsc --noEmit && pnpm install --frozen-lockfile=false && pnpm build) && <G7 scaffold smoke>` |
| Phase gate | `make quality-gate` green + `cargo doc` green + `next build` green + **G7 scaffold smoke PASS (rev-2)** + all 12 ACs' grep checks pass |

**rev-2 note:** G7 (scaffold smoke) has been added to the Validation Architecture and to PLAN.md Task 5 per Codex M3. It directly exercises AC-1 rather than inferring it from "template source changed". See PLAN.md Step 5.4b and VALIDATION.md's grep-guardrail matrix for the canonical definition.

### Phase Requirements → Test Map

Phase 72.1 has no existing REQ-IDs (`phase_req_ids: null`). The planner may optionally propose `LAND-CR03-01` during planning; otherwise, map to the 12 acceptance criteria in CR-03 §"Acceptance criteria":

| AC | Behavior | Test Type | Automated Command | File Exists? |
|----|----------|-----------|-------------------|-------------|
| AC-1 | `pmcp new --template landing` scaffolds clean | integration (**G7 scaffold smoke — rev-2**) | see G7 under "Regression Guard-Rails" below; real command is `cargo pmcp landing init --template nextjs --output <tempdir> --server-name smoke-test` | Wave 0 gap: none |
| AC-2 | 0 `NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID\|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER` in template | grep | `! grep -rE 'NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID\|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/templates/landing/nextjs/` | none |
| AC-3 | ≥1 `MCP_SERVER_NAME` in template (scope boundary) | grep | `grep -r 'MCP_SERVER_NAME' cargo-pmcp/templates/landing/nextjs/ \| wc -l` (expect ≥1; today baseline 7) | none |
| AC-4 | hook exists; imported by 4 consumers; `fetch('/landing-config'` exactly 1 | file-exists + grep | `test -f cargo-pmcp/templates/landing/nextjs/lib/useLandingConfig.ts && [ "$(grep -rcE \"fetch\\('/landing-config'\" cargo-pmcp/templates/landing/nextjs/ \| awk -F: '{s+=$2} END{print s}')" = "1" ]` | Wave 0 gap: `lib/useLandingConfig.ts` |
| AC-5 | `/signup` renders disabled copy on error/null | source inspection | `grep -q "Sign-up is not available for this server." cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx` | none |
| AC-6 | `/signup` redirects to Cognito when populated | source inspection | `grep -q 'amazoncognito.com/signup' cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx` | none |
| AC-7 | `/signup/callback` redirects to `signupRedirectAfter` / `/connect` | source inspection | `grep -q "signupRedirectAfter" cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx && grep -q "'/connect'" cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx` | none |
| AC-8 | `/connect` uses `cfg.mcpEndpoint` (not `MCP_SERVER_NAME`-assembled) | grep (G5 tightened — Codex L3) | `[ "$(grep -c 'cfg.mcpEndpoint' cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx)" -ge 4 ] && ! grep -qE 'https://\$\{[a-zA-Z_]+\}\.us-east\.true-mcp\.com' cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx` | none |
| AC-9 | Header conditional render on `cfg.signup !== null` | source inspection | `grep -q 'cfg.signup !== null' cargo-pmcp/templates/landing/nextjs/app/components/Header.tsx` | none |
| AC-10 | `next build` succeeds without `NEXT_PUBLIC_*` env | integration | `cd cargo-pmcp/templates/landing/nextjs && pnpm install && env -u NEXT_PUBLIC_COGNITO_DOMAIN -u NEXT_PUBLIC_COGNITO_REGION -u NEXT_PUBLIC_LANDING_CLIENT_ID -u NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER pnpm build` | Node 20.8.1 + pnpm 10.15.0 verified available locally |
| AC-11 | `next build && next start` works offline (hook→error state) | **manual** | human operator runs `pnpm build && npx serve out`, navigates to `/signup`, `/signup/callback`, `/connect`, confirms disabled-state copy renders without network | manual gate; task marked `autonomous: false` |
| AC-12 | rustdoc `NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER` → 0; `cargo doc` succeeds | grep + cargo | `! grep -n 'NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/src/landing/config.rs && cargo doc --no-deps -p cargo-pmcp` | none |

### Sampling Rate

- **Per task commit:** `make quality-gate` (fmt, clippy pedantic+nursery, build, test, audit — matches CI exactly).
- **Per wave merge:** full suite — `make quality-gate && cargo doc --no-deps -p cargo-pmcp && (template tsc --noEmit + next build) && G7 scaffold smoke`.
- **Phase gate:** all 12 ACs pass (11 automated incl. G7 + 1 manual AC-11) before `/gsd-verify-work`.

### Wave 0 Gaps

- [ ] `cargo-pmcp/templates/landing/nextjs/lib/useLandingConfig.ts` — the hook file must exist before any consumer rewrite type-checks. Either Wave 0 creates the hook and Wave 1 rewrites consumers, OR everything ships in a single atomic commit (CONTEXT §"Ordering" prefers the atomic option).
- No framework install required — `tsc`, `next build`, `cargo doc`, `make quality-gate`, `cargo run -p cargo-pmcp` (G7) all work with current environment.

### Regression Guard-Rails (Grep Matrix — lift into PLAN.md `must_haves`)

| # | Command | Expected | Enforces |
|---|---------|----------|----------|
| G1 | `grep -rE 'NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID\|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/templates/landing/nextjs/` | 0 matches | AC-2 |
| G2 | `grep -r 'MCP_SERVER_NAME' cargo-pmcp/templates/landing/nextjs/ \| wc -l` | ≥1 (today: 7) | AC-3 — scope boundary |
| G3 | `grep -rE "fetch\\('/landing-config'" cargo-pmcp/templates/landing/nextjs/` | exactly 1 match | AC-4 — single fetch site |
| G4 | `grep -l 'useLandingConfig' cargo-pmcp/templates/landing/nextjs/app/` | 4 files (signup/page, signup/callback/page, connect/page, components/Header) | AC-4 — hook adoption |
| G5 (tightened — Codex L3) | `[ "$(grep -c 'cfg.mcpEndpoint' cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx)" -ge 4 ] && ! grep -E '\\.us-east\\.true-mcp\\.com\|\\$\\{serverName\\}' cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx` | ≥4 cfg.mcpEndpoint refs AND 0 hand-assembled URL matches | AC-8 — every MCP endpoint flows through `cfg.mcpEndpoint`; no legacy URL assembly. Tightened from the earlier loose `grep -c 'cfg.mcpEndpoint\|endpoint'` which only proved the file mentioned `endpoint` enough times. |
| G6 | `grep -n 'NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/src/landing/config.rs` | 0 matches | AC-12 — rustdoc fix |
| **G7 (new — Codex M3)** | `SMOKE=$(mktemp -d); ( cd "$SMOKE" && cargo run --quiet -p cargo-pmcp -- landing init --template nextjs --output smoke-landing --server-name smoke-test ) && test -f "$SMOKE/smoke-landing/lib/useLandingConfig.ts" && grep -q useLandingConfig "$SMOKE/smoke-landing/app/signup/page.tsx" && grep -q useLandingConfig "$SMOKE/smoke-landing/app/signup/callback/page.tsx" && grep -q useLandingConfig "$SMOKE/smoke-landing/app/connect/page.tsx" && grep -q useLandingConfig "$SMOKE/smoke-landing/app/components/Header.tsx" && ! grep -rE 'NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID\|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' "$SMOKE/smoke-landing/" && rm -rf "$SMOKE"` | `AC-1 scaffold smoke: PASS` | AC-1 — the scaffold command actually produces a working project from the updated template source (hook present, all 4 consumers import it, zero NEXT_PUBLIC_ reads) |
| G8 (Codex M2 LOC budget) | `[ "$(git diff --numstat cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx cargo-pmcp/templates/landing/nextjs/app/components/Header.tsx \| awk '{s+=$1} END{print s+0}')" -le 100 ]` | ≤100 lines added across the 4 consumer files | regression guard against wholesale full-file rewrites |

## Dependencies

- **cargo-pmcp workspace crate:** 0.8.0 → 0.8.1 (patch, additive). Confirmed via `cargo-pmcp/Cargo.toml` line 3.
- **No workspace crate depends on `cargo-pmcp`** (verified: `pmcp`, `mcp-tester`, `mcp-preview` are all dependencies *of* cargo-pmcp, not vice versa). Solo bump; no downstream crate version cascade.
- **Node / pnpm:** Node 20.8.1, pnpm 10.15.0, npx 10.1.0 — all verified available locally (AC-10 automation is achievable). If a CI runner lacks these, AC-10 can be marked `autonomous: false` for that runner; locally it runs clean.
- **No new Rust dependencies.** No `Cargo.toml` `[dependencies]` additions.
- **No new npm dependencies.** `package.json` stays byte-identical — hook is stdlib React only (`useEffect`, `useState` from `react`).

## Project Constraints (from CLAUDE.md)

| Constraint | Impact on this phase |
|------------|----------------------|
| Toyota Way zero-defect; `make quality-gate` mandatory pre-commit | Every task commit goes through `make quality-gate`. Pre-commit hook will block otherwise. |
| `cargo fmt --all -- --check` + `cargo clippy --workspace --all-targets --features full -- -D warnings` | Rustdoc edits must not introduce clippy warnings (see Risk #5 — backtick bare identifiers). |
| No SATD comments | Template comments: avoid "TODO" / "FIXME" — use descriptive prose. |
| Cognitive complexity ≤25 per function | Trivially satisfied — hook is ~10 LOC, consumer rewrites are small. |
| ALWAYS: fuzz + property + unit + example tests for new features | **Not applicable** — this phase ships no new Rust API surface (rustdoc only + version bump). The template is not a "feature" in the CLAUDE.md sense; CONTEXT.md explicitly defers template test harness. Planner should note this explicitly in the PLAN.md to preempt the checker flagging it. |
| Emergency override requires justification | Not applicable. |
| Contract-first methodology (provable-contracts) | Not applicable — no contract YAML exists for `cargo-pmcp/templates` or the rustdoc edits. Pure doc + template work. |

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | `cargo-pmcp/templates/landing/nextjs/tsconfig.json` exists (standard Next.js 14 scaffold) | Validation Architecture | Low — if missing, `tsc --noEmit` fails fast with a clear error; planner can add a check task |
| A2 | Local clippy pedantic+nursery will not flag the rewritten rustdoc text (bare `ServerOAuthConfig.signupRedirectAfter`) | Risks §5 | Low — `make quality-gate` will surface it; mitigation is one-line backtick wrapping |
| A3 | `env -u VAR` syntax on macOS/Linux successfully clears the env var for the AC-10 test command | Validation Architecture | Low — well-documented POSIX behavior |
| A4 | Duplicate hook fetches per page (~2 with current route layout) remain acceptable (CR explicitly accepts) | Risks §2 | None — locked by CR (wording corrected from "4 concurrent" per Codex L1) |
| A5 | `tsconfig.json` path alias `@/lib/useLandingConfig` resolves (per CR-03 sample code using `@/lib/…`) | Technical Approach | Low — if missing, use relative imports (`../../lib/useLandingConfig`); purely cosmetic |
| A6 (rev-2) | `cargo run -p cargo-pmcp -- landing init --template nextjs --output <dir> --server-name <name>` matches the real CLI signature | Scaffold smoke (G7) | Low — confirmed against `cargo-pmcp/src/commands/landing/init.rs` signature; if clap argument names differ, G7 fails fast and the executor re-inspects `cargo-pmcp/src/main.rs` (or the actual clap wiring) to fix |

**If no tsconfig path alias exists:** the CR-03 sample `import { useLandingConfig } from '@/lib/useLandingConfig'` should be replaced with `import { useLandingConfig } from '../../../lib/useLandingConfig'` (depth-adjusted per consumer). Planner task for "hook consumer rewrite" should verify the alias exists or use relative imports throughout.

## Open Questions

1. **Optional: new REQ-ID `LAND-CR03-01`?** `phase_req_ids: null` today. Adding a single requirement entry referencing AC-1..AC-12 would give verification a stable anchor. Planner's call — not a blocker.
2. **Loading-state flash in `Header.tsx`.** CR-03 says "No button flash during the pre-fetch state (show nothing until resolved)." That's a visual/UX concern, not a testable grep. Planner task for Header MUST state this as acceptance prose so the executor doesn't default-show the button during `cfg === null`.

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Rust toolchain (stable) | `cargo build/test/clippy/doc/fmt` / `cargo run -p cargo-pmcp` (G7) | ✓ | (existing project baseline) | — |
| `make` | quality-gate entry point | ✓ | (existing project baseline) | — |
| Node.js | `next build` (AC-10) | ✓ | 20.8.1 (≥18 required) | — |
| pnpm | `pnpm install && pnpm build` | ✓ | 10.15.0 | `npm ci && npm run build` |
| npx | `npx -y typescript tsc --noEmit` | ✓ | 10.1.0 | — |

**Missing dependencies with no fallback:** none.
**Missing dependencies with fallback:** none.

## Handoff to Planner

A single `PLAN.md` is correct for this phase. The surface is tight (~100 LOC TypeScript + 3 rustdoc lines + 1 Cargo.toml line), coupling is high (hook must precede consumers or ship atomically), and CR-03 leaves almost no design discretion. Suggested task shape (5 tasks; planner may split or merge):

1. **Create hook** (`lib/useLandingConfig.ts`) — NEW file; exact body from CR-03 §1 with ignore-after-unmount cancel flag. Must not import anything beyond `react`. No JSON shape validation.
2. **Rewrite 4 consumers** — `signup/page.tsx`, `signup/callback/page.tsx`, `connect/page.tsx` (server→client flip, relocate module-scope consts), `components/Header.tsx` (conditional button). Planner's discretion to bundle or split. Each file's `must_haves` lift from the Consumer Rewrite Patterns table above. **Apply preservation rule + ≤100 LOC budget (Codex M2).**
3. **Rustdoc fix** — three surgical replacements in `cargo-pmcp/src/landing/config.rs` at lines 29, 180, 445.
4. **Version bump** — `cargo-pmcp/Cargo.toml` 0.8.0 → 0.8.1. Single line change. LAST task in the plan.
5. **Final verification** — run `make quality-gate && cargo doc --no-deps -p cargo-pmcp && (cd cargo-pmcp/templates/landing/nextjs && pnpm install && env -u NEXT_PUBLIC_COGNITO_DOMAIN -u NEXT_PUBLIC_COGNITO_REGION -u NEXT_PUBLIC_LANDING_CLIENT_ID -u NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER pnpm build) && <G7 scaffold smoke> && run all 7 grep guardrails`. AC-11 flagged as manual (`autonomous: false`) — human operator does the `pnpm start` offline check.

Waves are optional; single wave is fine. Planner should lift the full Regression Guard-Rails grep matrix (G1..G7, plus G8 LOC budget) into the plan's top-level `must_haves` so every task inherits the scope-boundary discipline.

## Sources

### Primary (HIGH confidence)
- `.planning/phases/72.1-finalize-landing-support/72.1-CR-03-SOURCE.md` rev-2 — authoritative PRD (all contract decisions)
- `.planning/phases/72.1-finalize-landing-support/72.1-CONTEXT.md` — locked decisions, grep guardrails, specifics
- `.planning/phases/72.1-finalize-landing-support/72.1-REVIEWS.md` — Codex cross-AI review (single reviewer; Gemini CLI crashed); rev-2 applies M1 / M2 / M3 / L1 / L2 / L3
- `cargo-pmcp/src/commands/landing/init.rs` — authoritative scaffold command signature (used by G7 smoke test)
- `cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx` (read lines 1–42) — confirms NEXT_PUBLIC_ reads on lines 11–13
- `cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx` (read lines 1–27) — confirms NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER on line 17
- `cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx` (read lines 1–55) — confirms server-component status, module-scope `serverName`/`mcpClientConfig`, hand-assembled URL on line 44
- `cargo-pmcp/templates/landing/nextjs/app/components/Header.tsx` (read lines 1–29) — confirms unconditional `<Link href="/signup">` on lines 19–24
- `cargo-pmcp/templates/landing/nextjs/next.config.js` — confirms `output: 'export'`
- `cargo-pmcp/templates/landing/nextjs/package.json` — confirms no test harness (no jest/vitest/playwright)
- `cargo-pmcp/Cargo.toml` line 3 — confirms version 0.8.0
- `cargo-pmcp/src/landing/config.rs` lines 29, 180, 445 — rustdoc targets verified present in exact wording
- `CLAUDE.md` — Toyota Way + release workflow constraints
- `grep -l 'MCP_SERVER_NAME' cargo-pmcp/templates/landing/nextjs/` — 7 files confirmed (scope boundary baseline)
- `which node && node --version && pnpm --version && npx --version` — 20.8.1 / 10.15.0 / 10.1.0 (environment ready for AC-10 automation)

### Secondary (MEDIUM confidence)
- None — all claims directly verified on disk.

### Tertiary (LOW confidence)
- A1 (tsconfig existence) and A5 (path alias resolution) — standard Next.js 14 scaffold assumptions. Verified in Assumptions Log with fallback guidance.

## Metadata

**Confidence breakdown:**
- Contract / endpoint / hook design: HIGH — locked in CR-03 rev-2, no ambiguity. Codex M1 clarified that `'error'` covers transport/HTTP/parse failures only, not shape — resolved in rev-2.
- File-level rewrite targets: HIGH — every "before" state re-verified on disk during research.
- Rustdoc targets: HIGH — lines 29, 180, 445 read directly; exact current wording confirmed.
- Validation commands: HIGH — all tools (`make`, `cargo`, `pnpm`, `npx`, `node`) verified available; grep syntax POSIX-standard. G7 scaffold smoke command verified against `cargo-pmcp/src/commands/landing/init.rs` signature.
- Risks: HIGH — all landmines derived from observed file state, not speculation.

**Research date:** 2026-04-20 (rev-2: 2026-04-20, same day; Codex review feedback applied)
**Valid until:** 2026-05-20 (30 days — CR-03 is locked; only risk is upstream template churn outside this phase).

## RESEARCH COMPLETE

**Phase:** 72.1 — Finalize landing support
**Confidence:** HIGH

### Key Findings

- CR-03 rev-2 is complete and unambiguous — all 12 ACs map to grep / build / manual checks with no design gaps.
- Every "before" state file re-verified on disk matches CR-03 assumptions. `connect/page.tsx` is the only server→client flip; module-scope `serverName` + `mcpClientConfig` must relocate into the component body.
- Rustdoc targets at lines 29, 180, 445 confirmed present with exact CR-03-cited wording.
- Environment is ready for full automation of AC-1 through AC-10 and AC-12 (AC-1 now directly exercised via G7 — Codex M3). AC-11 remains a manual gate by design (no Playwright harness, explicitly deferred).
- No new dependencies (Rust or npm). No downstream workspace crate pins `cargo-pmcp` — 0.8.1 bump is solo.
- `CLAUDE.md` "ALWAYS: fuzz/property/unit/example" rule does not apply — this phase ships no new Rust API surface, only rustdoc + template changes. Planner should note this explicitly to preempt the checker.
- **rev-2 (Codex review):** hook `'error'` state covers transport/HTTP/parse failures, NOT shape mismatches (M1, disposition "a"); preservation rule + ≤100 LOC budget for consumer rewrites (M2); G7 scaffold smoke added for AC-1 (M3); fan-out corrected from "~4 concurrent" to "~2/page under current route layout" (L1); "abort-on-unmount" renamed to "ignore-after-unmount" throughout (L2); G5 tightened to `grep -c 'cfg.mcpEndpoint' >= 4` + negative URL grep (L3).

### File Created

`.planning/phases/72.1-finalize-landing-support/72.1-RESEARCH.md` (rev-2)

### Confidence Assessment

| Area | Level | Reason |
|------|-------|--------|
| Contract / hook design | HIGH | Locked verbatim in CR-03 §1; no derivation needed. Codex M1 clarified error-state scope. |
| File rewrite surface | HIGH | Every "before" file re-read; line numbers and module-scope relocations verified. |
| Validation architecture | HIGH | All tools probed and available; 11/12 ACs automatable (incl. G7 scaffold smoke for AC-1), AC-11 explicitly manual. |
| Risks | HIGH | Landmines observed in code, not speculated. |

### Open Questions

- Optional: should planner propose a new REQ-ID `LAND-CR03-01` (currently `phase_req_ids: null`)? Planner's discretion — not a blocker.
- Does `tsconfig.json` expose `@/lib/*` path alias? Assumption A5 — fallback is relative imports.

### Ready for Planning

Research complete (rev-2). Planner can produce a single PLAN.md with ~5 tasks (hook create, 4 consumer rewrites, rustdoc edits, version bump, verification including G7 scaffold smoke). Lift the G1..G7 + G8 grep matrix into plan-level `must_haves`. AC-11 task gets `autonomous: false`.
