---
phase: 114-tasks-extension-migration
plan: 18
type: execute
wave: 12
depends_on: [114-01, 114-02, 114-03, 114-04, 114-05, 114-06, 114-07, 114-08, 114-09, 114-10, 114-11, 114-12, 114-13, 114-14, 114-15, 114-16, 114-17, 114-19, 114-20]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/114-tasks-extension-migration/114-SPEC-RECHECK.md
  - .planning/phases/114-tasks-extension-migration/deferred-items.md
  - src/server/task_dispatch.rs
  - crates/pmcp-tasks/src/constants.rs
autonomous: false
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06]
must_haves:
  truths:
    - "The whole phase passes make quality-gate, semver-checks 223/223 and the pmcp-tasks feature matrix in one run"
    - "No doc in the tree still describes the pre-114 behaviour as current"
    - "All six TASK requirements are booked [~] implemented; pending final schema, with a runnable re-verification procedure"
    - "Every deferred or unowned item this phase found has an owner or a written record"
    - "The contract-first question was answered by the owner at wave 1 and is cited, not decided, here"
    - "A proven TASK-05 security defect blocks sign-off rather than being booked as implemented"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "TASK-01..06 marked [~] with the pending-final-schema qualifier"
      contains: "pending final schema"
    - path: ".planning/phases/114-tasks-extension-migration/114-SPEC-RECHECK.md"
      provides: "Finalized hold record with every wire value cross-referenced to its landed source location"
      contains: "Wire-Value Inventory"
    - path: ".planning/phases/114-tasks-extension-migration/deferred-items.md"
      provides: "Owners or written records for every item this phase deferred or discovered"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/114-tasks-extension-migration/114-SPEC-RECHECK.md"
      via: "each [~] requirement pointing at the hold record that will flip it"
      pattern: "SPEC-RECHECK"
---

<objective>
Run the whole-phase gate, sweep the docs this phase falsified, book the six requirements under the D-18
hold, and give every discovered item an owner.

Purpose: D-18 books all six TASK requirements at `[~]` *implemented; pending final schema*, flipped
together when the hold condition clears. That bookkeeping only means something if the hold record is
runnable and every wire value in the tree is walkable from it. This plan is also the phase's single
whole-tree gate run: the individual plans each ran scoped tests, and CLAUDE.md requires `make quality-gate`
before any commit or push.

**Known tradeoff, recorded by D-18 rather than discovered here:** this phase's requirements cannot partially
close, so each review reopens all six. The alternative — splitting the wire-exact TASK-02/04 from the
schema-independent TASK-01/03/05/06 — was presented in discussion and **not** chosen; uniform consistency
with Phase 113's bookkeeping was preferred. If the phase stalls in review for the reason 113's HTTP-04 split
was created to fix, that split is the recorded remedy.

Output: a green gate, corrected docs, six `[~]` requirements, a finalized recheck record, and a
deferred-items ledger.
</objective>

<amendment date="2026-07-29" reason="the core specification published; the tasks extension did not">
**READ THIS BEFORE THE D-18 BOOKING TASK. The ground moved after this plan was written.**

On 2026-07-28 this plan was authored against a world where NEITHER repository had published. That
is no longer the state. Measured 2026-07-29 and recorded in full at
`114-SPEC-RECHECK.md` § `### Verdict re-verification` → `#### 2026-07-29`:

| Repository | State | DQ6 condition |
|---|---|---|
| `modelcontextprotocol/modelcontextprotocol` | **`schema/2026-07-28/` PUBLISHED**, `LATEST_PROTOCOL_VERSION = "2026-07-28"` | met |
| `modelcontextprotocol/ext-tasks` | **`draft/` only.** No tags, no releases. Unchanged since 2026-05-22 | **NOT met** |

**The landing is `STILL-ABSENT` (partial publication), and that is already recorded.** Do NOT
re-litigate it and do NOT upgrade the verdict.

### What this REMOVES from your job

- **Do not flip TASK-01…TASK-06.** `## Third Outcome Policy` rule 5 is explicit that partial
  publication is `STILL-ABSENT`, not a fourth state. Book all six `[~]` exactly as planned.
- **Do not run `## Procedure` steps 2–3.** Step 1 states they are *"not executable"* when either
  repository lacks a versioned directory, and rule 5 forbids running them against a half-published
  pair. **No inventory row may be marked CONFIRMED by this plan.**
- **Do not treat the published core schema as releasing the hold.** Five of six requirements live
  in the extension, which has not published. This is exactly the failure DQ6 was written to prevent
  — Phase 113's original wording would have released the hold today.
- **The contract-first waiver stays carried, not discharged** (same DQ6 condition). Cite
  `114-CONTRACT-DECISION.md` § 4 (`Chosen: option-b`, owner-decided). You may not choose its
  outcome (b) on your own authority — that is T-114-106.

### What this ADDS to your job

1. **Carry two findings into the requirement booking**, both from the 2026-07-29 run's *advance
   observations* (which are observations, NOT row confirmations):
   - **`resultType` is narrower and non-optional upstream than this phase assumed.** Published core:
     `resultType: ResultType` on `Result` (required) with
     `ResultType = "complete" | "input_required" | string`. pmcp mints `"task"` (rows 17-18), which
     is admissible only via the open `| string` tail. **Decide explicitly** whether that is
     conformant-by-extension or prospective DRIFT — do not absorb it. Also: upstream names
     `"input_required"` as a *resultType* while row 12 uses it as a *TaskStatus*; that overlap needs
     its own inventory row.
   - **The `-32003` vs `-32021` disagreement PERSISTS**, so DQ3's split stands. The published core
     says `-32021`; the ext-tasks prose saying `-32003` is still unpublished `draft`. Record it as
     persisting. Note `-32003`'s absence from the published core codes is the EXPECTED result and
     **confirms** DQ3 — row 31 sources it to pmcp's own `AUTHENTICATION_REQUIRED`, never to core.
2. **Amend the § Verdict framing if you touch it.** The original `## Verdict` prose said no
   versioned directory existed in *"either"* repository. That sentence is now false for core and has
   already been corrected in place — verify rather than re-correct.
3. **Record the remaining trigger precisely.** The sole outstanding condition is now
   **`ext-tasks` publishing a versioned (non-`draft`) directory**. State that, so the next re-run is
   a one-repository check rather than a two-repository one.
4. **Re-run `114-01`'s provenance tripwire** (`cargo nextest run --features full -E
   'binary_id(pmcp::vendored_schema_provenance)'`). The 2026-07-29 run could not — no shell was
   available — so the vendored bytes are asserted-unchanged-by-inference, not by test, in that
   record. Note the selector: `test(/…/)` matches test NAMES and would select 0 or pass vacuously.

### Method caveat you inherit

The 2026-07-29 listings were taken over HTTP (GitHub tree pages + `raw.githubusercontent.com`),
**not** via the `gh api … --jq` form `## Procedure` step 1 prescribes, because no shell was
available. **Re-take both listings with the prescribed form** before relying on them.
</amendment>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-tasks-extension-migration/114-CONTEXT.md
@.planning/phases/114-tasks-extension-migration/114-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale-doc sweep across everything this phase falsified</name>
  <read_first>
    - `src/server/task_dispatch.rs` — `is_v1_task_era`'s rustdoc and `V2_TASKS_NOT_NEGOTIATED`'s doc, both
      of which earlier plans were required to correct (114-05 and 114-08). VERIFY they were, rather than
      trusting the plan text: 113-29 records the exact failure class of a comment saying "deliberately does
      NOT do X" after X became true.
    - `crates/pmcp-tasks/src/constants.rs` — `METHOD_TASKS_LIST` and `METHOD_TASKS_RESULT` (v1-only after
      this phase) and `MODEL_IMMEDIATE_RESPONSE_META_KEY` (a SEP-1686 v1 concept with no counterpart in the
      v2 extension schema)
    - `src/types/tasks.rs` — the v1 `Task`/`CreateTaskResult`/`GetTaskResult`/`CancelTaskResult` rustdocs,
      which should now say they are the v1 wire shapes and name the v2 projection type
    - `src/server/task_store.rs`, `src/server/tasks.rs`, `src/client/mod.rs` — any rustdoc that describes
      the tasks surface as v1-only or as not-yet-implemented
  </read_first>
  <files>src/server/task_dispatch.rs, crates/pmcp-tasks/src/constants.rs</files>
  <action>
Sweep every doc comment this phase falsified. Grep-driven, not memory-driven: search the tree for the
claims that are now false and fix each, or record that an earlier plan already did.

The known set, from research's "Deprecated/outdated in this repo" list:
  - `is_v1_task_era`'s "gates ONLY the `-32002` emission" and "`tasks/get`, `tasks/list` and `tasks/cancel`
    are unchanged on every era" — should have been corrected by 114-08; VERIFY.
  - `V2_TASKS_NOT_NEGOTIATED`'s "pmcp advertises no `io.modelcontextprotocol/tasks` entry" — should have
    been corrected by 114-05; VERIFY.
  - `pmcp_tasks::constants::METHOD_TASKS_LIST` / `METHOD_TASKS_RESULT` — keep the constants (v1 still uses
    them) but their rustdocs must now say they are **v1-only** and that both methods answer `-32601` on v2.
  - `MODEL_IMMEDIATE_RESPONSE_META_KEY` — a v1 SEP-1686 concept with no v2 counterpart; say so in its
    rustdoc rather than leaving a reader to infer it applies to both eras.
  - The v1 result-type rustdocs in `src/types/tasks.rs` — each should name itself as the v1 shape and point
    at the v2 projection type added by 114-11.

Then run a general sweep: grep the tree for `tasks/list`, `tasks/result`, `experimental.tasks` and
`capabilities.tasks` in doc comments and check each hit still reads true post-phase. Record the count of
hits reviewed and the count changed in the SUMMARY, so the sweep is auditable rather than asserted.

Do not change any behaviour in this task. Doc-only.
  </action>
  <verify>
    <automated>make doc-check 2>&1 | tail -20 && cargo doc --no-deps --features full 2>&1 | grep -c warning</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "gates ONLY the" src/server/task_dispatch.rs` returns nothing
    - `grep -rn "advertises no" src/server/task_dispatch.rs` returns nothing
    - `METHOD_TASKS_LIST` and `METHOD_TASKS_RESULT` rustdocs each contain the string `v1` and reference the v2 `-32601` answer
    - `MODEL_IMMEDIATE_RESPONSE_META_KEY`'s rustdoc states it has no v2 counterpart
    - Each of the four v1 result-type rustdocs in `src/types/tasks.rs` names the v2 projection type
    - The SUMMARY records the number of doc hits reviewed and changed for each grep term
    - `cargo doc --no-deps --features full` produces no NEW warnings versus a baseline measured at the phase's base commit and recorded in the SUMMARY
    - `git diff` for this task touches only comments and rustdoc: no non-comment line changed
  </acceptance_criteria>
  <done>No doc in the tree still describes pre-114 behaviour as current, and the sweep is auditable by counts.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-phase gate run</name>
  <read_first>
    - `CLAUDE.md` § Quality Gate Enforcement and § Release — `make quality-gate` is the gate CI runs and is
      STRONGER than bare cargo commands; `make lint` applies pedantic + nursery + cargo groups that
      `cargo clippy -- -D warnings` misses (measured: five consecutive Phase-113 plans broke on exactly this)
    - `Makefile` lines 295-340 — `make test-feature-flags`, the 4-row pmcp-tasks matrix
      (`--no-default-features` / `dynamodb` / `redis` / `dynamodb,redis`) x check + clippy `-D warnings` +
      test + doc. This IS D-14 item 4 — reuse it, do not write a new script.
    - `Makefile` around line 842 — the `comply` target: it runs `pmat comply check --path .` against THIS
      repo and treats project-level advisories as informational (`|| echo`), then runs the repo-local
      `comply-bindings-check`
  </read_first>
  <files>.planning/phases/114-tasks-extension-migration/deferred-items.md</files>
  <action>
Run the full gate and record every number, not a summary adjective. Each item below must appear in the
SUMMARY with its measured value:

  1. `make quality-gate` — exit status, the count of test-result lines, passed and failed counts.
  2. `make lint` — exit status and warning count (must be zero). Never substitute a bare
     `cargo clippy -- -D warnings`.
  3. `cargo semver-checks check-release` — the ratio. D-14 item 3 requires **223/223, no update required**.
     If the ratio moved, that is a finding: identify which item changed and why before proceeding.
  4. `cargo public-api diff` — zero REMOVED items, and the list of ADDED items (additive public surface is
     expected from 114-03/114-04/114-17; a public item nobody planned is a finding).
  5. `cargo nextest run -p pmcp-tasks` — D-14 item 1: green with `git diff --stat -- crates/pmcp-tasks/tests/`
     listing ONLY files this phase created.
  6. `make test-feature-flags` — D-14 item 4, all four rows.
  7. `make wasm-build` — green, with the warning count compared against a baseline MEASURED at the phase's
     base commit (not assumed; 113 records that assumed baselines mislead).
  8. `pmat analyze complexity --format json --max-cognitive 25` — the violation list. **STATE.md records the
     gate at 3 pre-existing violations, including D-113-U (`write_canonical` cog 26, unowned).** This phase
     must add no fourth. Report the count and name each violation so the delta is explicit.
  9. `cargo run --example s50_v2_tasks_server` + `s51_v2_tasks_agent` — the end-to-end pair, both exit 0.
 10. `cargo fuzz run fuzz_tasks_update -- -runs=20000` — exit 0, artifacts dir empty.
 11. `git diff --stat -- Cargo.toml Cargo.lock crates/pmcp-tasks/Cargo.toml` — must be EMPTY. The
     zero-new-runtime-deps constraint is a locked milestone requirement, not a preference.

**Contract-first: CITE the owner's decision, do not make one here.** An earlier draft of this plan
decided, on its own authority, that no contract YAML would be authored — after seventeen implementation
plans had already run. Cross-AI review correctly called that a blocker: a plan cannot grant itself an
exemption from a rule CLAUDE.md states without qualification, and answering the question at the END of
the phase means the answer arrives after all the code it was meant to constrain.

The decision now belongs to **114-20** (wave 1, blocking `checkpoint:decision`), which measures the
ground truth and puts Option A (author the contract) and Option B (recorded owner waiver) to the owner
before implementation begins. This task's job is only to:
  - read `114-CONTRACT-DECISION.md` and record which option was chosen, by whom, and on what date;
  - if **option-a**: confirm the contract YAML exists at the recorded path and capture `pmat comply check` output;
  - if **option-b**: confirm the waiver's follow-up row exists in `114-SPEC-RECHECK.md`;
  - either way, confirm by measurement that `make comply` exits 0 and record its output.
If `114-CONTRACT-DECISION.md` has no filled `## Decision`, STOP — the phase cannot sign off on an
unanswered mandatory-rule question.

**Baseline manifest instead of hard-coded totals.** Cross-AI review noted that repository-wide
constants (`223/223`, "3 pmat violations", expected test counts) age across an 18-plan phase and will
produce false findings. Before running the gates, capture a `## Phase base manifest` in the SUMMARY at
the phase's BASE commit — `cargo semver-checks` ratio, `pmat` violation list, `cargo public-api` item
count, `make wasm-build` warning count — then assert **deltas** against it. The numbers quoted below
are the values measured at planning time and are the expected baseline, not the assertion: if the base
manifest differs, the manifest wins and the difference is recorded, not treated as a regression.

Any failure at any numbered step stops the plan. Record it and fix it in a follow-up plan rather than
loosening a gate.
  </action>
  <verify>
    <automated>make quality-gate 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0, with the test-result line count and passed/failed counts recorded in the SUMMARY
    - `make lint` exits 0 with zero warnings
    - A `## Phase base manifest` section exists in the SUMMARY with the semver ratio, pmat violation list, public-api item count and wasm warning count MEASURED at the phase base commit
    - `cargo semver-checks check-release` reports **no update required**, and its ratio equals the base manifest's ratio (223/223 at planning time); any deviation is investigated and explained
    - `114-CONTRACT-DECISION.md` has a filled `## Decision`, and its chosen option's follow-up obligation is confirmed present
    - `cargo public-api diff` shows zero REMOVED items, and every ADDED item is attributable to a specific plan
    - `cargo nextest run -p pmcp-tasks` exits 0 and `git diff --stat -- crates/pmcp-tasks/tests/` lists only files created by 114-07
    - `make test-feature-flags` exits 0 for all four rows
    - `make wasm-build` green, warning count compared against a MEASURED base-commit baseline
    - `pmat analyze complexity --max-cognitive 25` reports no violations beyond the base manifest's list (3 at planning time), each named
    - **No `## BLOCKING: TASK-05 security defect` heading exists in `114-15-SUMMARY.md`.** If one does, this plan STOPS: TASK-05 must not be booked and the sign-off checkpoint must not run until a follow-up security-fix plan has closed it (114-15's recorded obligation)
    - Both examples exit 0; the fuzz campaign exits 0 with an empty artifacts dir
    - `git diff --stat -- Cargo.toml Cargo.lock crates/pmcp-tasks/Cargo.toml` is EMPTY
    - `make comply` exits 0 and its output is recorded, with the DQ7 decision written into the SUMMARY
  </acceptance_criteria>
  <done>Every gate is green with measured numbers recorded, and the contracts question is answered by measurement rather than skipped.</done>
</task>

<task type="auto">
  <name>Task 3: Book the six requirements, finalize the hold record, and assign every open item</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 82-90 — TASK-01..06 and the `[~]` convention used by CLNT-01/02/05
      (`- [~] **CLNT-01**: … — *implemented; pending final schema*`). Copy that exact form.
    - `.planning/phases/114-tasks-extension-migration/114-SPEC-RECHECK.md` (114-01) — the hold record to
      finalize: `## Wire-Value Inventory` must now name, per row, the FILE and CONSTANT where each value
      actually landed, so the re-verification run is a mechanical walk
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md` — the ledger
      format to mirror (item id, severity, what was measured, the fix shape, owner)
    - Every `114-*-SUMMARY.md` — the source of findings, deferrals and negative-control records this task
      consolidates
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/114-tasks-extension-migration/114-SPEC-RECHECK.md, .planning/phases/114-tasks-extension-migration/deferred-items.md</files>
  <action>
Three bookkeeping outputs.

**(a) `.planning/REQUIREMENTS.md`** — mark TASK-01..06 as `[~]` with the exact
`— *implemented; pending final schema*` qualifier the CLNT rows use, and update the traceability table's
Status column for each. Do NOT mark any of them `[x]`: D-18 books all six under the hold and they flip
together, only when the hold condition clears. Add a one-line pointer from the section to
`114-SPEC-RECHECK.md` so a reader of REQUIREMENTS.md alone can find the obligation.

**(b) `114-SPEC-RECHECK.md`** — finalize. Walk `## Wire-Value Inventory` and fill each row with the FILE
and CONSTANT/field where the value actually landed (e.g. the extension key in `src/types/capabilities.rs`,
the retirement message in `src/server/task_dispatch.rs`, `ttlMs`/`pollIntervalMs` on the projection type in
`src/types/tasks.rs`, `inputResponses` at the `tasks/update` route, `-32602`/`-32021`/`-32003` at their
emission sites, `Mcp-Name` in the tasks name-key table). A row without a landing site is either an
unimplemented value or a stale row — resolve which. Keep `## Verdict` at `PENDING`, keep the three-branch
`## Third Outcome Policy`, and keep DQ6's both-repos condition. Re-measure the condition on the run date and
record the measurement with a timestamp, so "we checked and it was absent" stays distinguishable from
"nobody checked".

**(c) `deferred-items.md`** — a ledger with an entry per item, mirroring 113's format (id, severity, what
was MEASURED, the fix shape, owner). It must include at minimum:
  - every item any `114-*-SUMMARY.md` recorded as a finding or a defect-not-fixed;
  - the still-deferred CONTEXT.md items, restated so a reader knows they were considered and declined:
    the broader server-directed-handle client-compatibility question (the part of the original deferral
    that DQ1 did NOT absorb), a configurable proxy-header / claim-based identity source for v2 owner
    binding, per-tool configurability of the `tasks/update` transition, and UNAS-01 (SEP-2243
    `x-mcp-header` / `Mcp-Param-{Name}`, still unassigned milestone-wide and explicitly NOT folded in here);
  - server-side `Mcp-Name` enforcement for tasks methods, which DQ4 deliberately left OFF this phase;
  - the `notifications/tasks` push surface, which the spec marks MAY and this phase declines;
  - the inherited unowned items this phase must not silently adopt: **D-113-U** (`write_canonical` cog 26,
    which STATE.md says needs an owner before this branch merges), D-113-Q, D-113-R, D-113-S, D-113-T,
    D-113-V, D-113-W.
Each entry names an owner or states explicitly that it is unowned — an unowned item is acceptable, an
undocumented one is not.
  </action>
  <verify>
    <automated>grep -c "pending final schema" .planning/REQUIREMENTS.md && grep -c "PENDING" .planning/phases/114-tasks-extension-migration/114-SPEC-RECHECK.md && grep -c "D-113-U" .planning/phases/114-tasks-extension-migration/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - All six of TASK-01..06 in `.planning/REQUIREMENTS.md` are `[~]` with the `pending final schema` qualifier; none is `[x]`
    - The traceability table's Status column is updated for all six
    - `114-SPEC-RECHECK.md` `## Wire-Value Inventory` has a landing FILE + identifier for every row, and no row is blank
    - `## Verdict` is still `PENDING`; the three-branch policy and the both-repos condition are intact
    - The hold condition is RE-MEASURED on the run date with a recorded timestamp
    - `deferred-items.md` contains an entry for every item listed in the action, each with an owner or an explicit "unowned"
    - `deferred-items.md` names D-113-U and states it needs an owner before the branch merges
    - `gsd-sdk query frontmatter.validate` passes for every `114-*-PLAN.md` in the phase directory
  </acceptance_criteria>
  <done>The six requirements are booked under a runnable hold, every wire value is walkable to its source, and no discovered item is undocumented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase sign-off checkpoint</name>
  <files>.planning/phases/114-tasks-extension-migration/114-18-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/114-tasks-extension-migration/114-18-SUMMARY.md` (Tasks 1-3 of this plan) — the
      recorded gate numbers, the sweep counts and the bookkeeping state the reviewer checks
    - Every `114-*-PLAN.md` `<objective>` block carrying a `DQ` decision — DQ1 (114-12, **user-approved
      2026-07-27**), DQ2 (114-10), DQ3 (114-09), DQ4 (114-06, **user-approved 2026-07-27**), DQ5 (114-13),
      DQ6 (114-01), DQ7 (114-18 Task 2), DQ8 (114-17)
  </read_first>
  <action>
Present the phase for human sign-off.

**Scope is already settled — this checkpoint verifies OUTCOMES, it does not re-litigate scope.** The two
decisions that widened the phase beyond a literal reading of CONTEXT.md's deferrals — **DQ1**
(server-directed creation in scope) and **DQ4** (client-side `Mcp-Name` = `taskId`) — were put to the user
at the plan-phase checker gate and **explicitly approved on 2026-07-27** ("Approve DQ1 + DQ4"), before any
code was written. Do not ask the user to reconsider them here. Present them as settled context, and ask
only whether the DELIVERED behaviour matches what was approved.

The remaining five decisions (DQ2, DQ3, DQ5, DQ6, DQ7) resolved questions CONTEXT.md and RESEARCH.md left
open without widening scope; surface them for confirmation. Do not proceed past this task without an
explicit response, because a disagreement about a delivered outcome is a replan, not a patch, and must
surface here rather than at verification.
  </action>
  <what-built>
    The full Phase 114 tasks-extension migration: extension-map negotiation on both halves,
    `tasks/update` end to end across three storage backends, `tasks/list` / `tasks/result` retired on v2,
    the flat v2 result shapes with `resultType:"task"`, fail-closed v2 owner binding, and the paired
    runnable example — with v1 proven byte-identical against golden fixtures.
  </what-built>
  <how-to-verify>
    1. Run the pair and watch the whole lifecycle:
       - Terminal A: `cargo run --features full --example s50_v2_tasks_server`
       - Terminal B: `cargo run --features full --example s51_v2_tasks_agent`
       - Expect: `s51` exits 0 and its output shows negotiation, a `resultType:"task"` create, an
         `input_required` poll, a `tasks/update`, and a terminal status with an inlined result.
    2. Confirm the gate numbers in `114-18-SUMMARY.md`: `make quality-gate` exit 0, `make lint` zero
       warnings, `cargo semver-checks` 223/223 no-update-required, `pmat` no fourth cog-25 violation,
       `Cargo.lock` untouched.
    3. Confirm the bookkeeping: TASK-01..06 are `[~] pending final schema` in `.planning/REQUIREMENTS.md`
       (NOT `[x]`), `114-SPEC-RECHECK.md` `## Verdict` is `PENDING` with every wire-value row pointing at a
       real source location, and `deferred-items.md` names D-113-U as needing an owner before merge.
    4. **DQ1 and DQ4 were already approved by you on 2026-07-27 and are NOT up for review here** — confirm
       only that what shipped matches them: a declaring v2 client gets a server-directed task handle
       (DQ1), and tasks requests carry `Mcp-Name: <taskId>` from the client while `MRTR_METHODS` still
       holds exactly three methods (DQ4).
    5. Confirm the five decisions that did not widen scope: DQ2 (explicit reserved-field ownership),
       DQ3 (`-32003` auth vs `-32021` missing-capability), DQ5 (`tasks/update` via `InternalClientRequest`,
       no public-enum variant), DQ6 (both repos must publish before the hold clears), DQ7 (no contract
       YAML — `../provable-contracts/` is absent and `make comply` is repo-local and informational).
       A disagreement about any delivered outcome is a replan, not a patch.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which decision or gate number you want changed</resume-signal>
  <verify>
    <automated>grep -c "quality-gate" .planning/phases/114-tasks-extension-migration/114-18-SUMMARY.md</automated>
    <human-check>The example pair runs end to end; the gate numbers are as recorded; the delivered DQ1/DQ4 outcomes match the 2026-07-27 approval; DQ2/DQ3/DQ5/DQ6/DQ7 are confirmed</human-check>
  </verify>
  <done>Human responded "approved" on the delivered outcomes, or the disagreement is recorded and routed to a replan. DQ1/DQ4 scope is not re-opened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement checkbox → downstream reader | A `[x]` on a value read from an unpublished draft would claim conformance the tree cannot support |
| documentation → future implementer | A doc describing pre-114 behaviour as current actively misleads, which 113-29 records as a real cause of shipped defects |
| unowned finding → merge | An undocumented finding is indistinguishable from one nobody looked for |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-93 | Repudiation | claiming conformance against an unpublished draft | mitigate | All six requirements booked `[~] pending final schema` under D-18; `## Verdict` stays `PENDING`; the hold condition re-measured with a timestamp on the run date |
| T-114-94 | Repudiation | a wire value with no traceable provenance at the gate | mitigate | Every `## Wire-Value Inventory` row must name a landing file and identifier; a blank row is a resolvable finding, and 114-16's provenance tripwire enforces the rustdoc side |
| T-114-95 | Tampering | stale docs asserting a superseded invariant | mitigate | Grep-driven sweep with reviewed/changed counts recorded per term; the two known false rustdocs are verified corrected rather than trusted |
| T-114-96 | Tampering | accidental dependency addition | mitigate | `git diff --stat -- Cargo.toml Cargo.lock crates/pmcp-tasks/Cargo.toml` asserted EMPTY; the milestone's zero-new-runtime-deps constraint is locked |
| T-114-97 | Repudiation | a documented gate silently skipped | mitigate | DQ7 answers the contracts question by measurement (`make comply` exits 0, repo-local, informational) and records the decision; the plan stops and raises rather than working around it |
| T-114-98 | Denial of Service | PMAT gate breach blocking merge | mitigate | Violation list reported by name with an explicit delta against the 3 pre-existing; D-113-U recorded as needing an owner before merge |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed across the entire phase; asserted by an empty `Cargo.toml`/`Cargo.lock` diff at this gate. 114-RESEARCH § Package Legitimacy Audit: NOT APPLICABLE |
</threat_model>

<verification>
- `make quality-gate` exits 0 with recorded counts
- `cargo semver-checks check-release` 223/223 no update required; `cargo public-api diff` zero REMOVED
- `make test-feature-flags`, `make wasm-build`, `make comply` all exit 0
- Both examples exit 0; the fuzz campaign exits 0
- `Cargo.toml` / `Cargo.lock` / `crates/pmcp-tasks/Cargo.toml` diffs EMPTY
- TASK-01..06 all `[~]`; `114-SPEC-RECHECK.md` `## Verdict` `PENDING`
</verification>

<success_criteria>
- One green whole-tree gate run with every number measured and recorded
- No stale doc describing pre-114 behaviour as current, with an auditable sweep count
- Six requirements booked under a runnable, re-measured hold whose inventory walks to real source locations
- Every deferred, declined and inherited item has an owner or an explicit unowned record
- Human sign-off on the five resolved scope decisions plus DQ7
</success_criteria>

<output>
Create `.planning/phases/114-tasks-extension-migration/114-18-SUMMARY.md` when done
</output>
