---
phase: 106-client-host-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/roots.rs
  - src/types/mod.rs
  - src/server/roots.rs
  - src/client/host/mod.rs
  - src/client/host/sampling.rs
  - src/client/host/elicitation.rs
  - src/client/host/roots.rs
  - src/client/mod.rs
  - tests/client_host_roundtrip.rs
  - examples/s49_sampling_host.rs
  - Cargo.toml
  - examples/README.md
autonomous: true
requirements: [HOST-01, HOST-02, HOST-03, HOST-06]
must_haves:
  truths:
    - "A server tool that calls extra.peer().sample() during an in-flight client tool call receives a completion produced by the client's registered host sampling handler"
    - "A server tool that calls extra.peer().list_roots() during an in-flight client call receives the roots produced by the client's registered roots provider"
    - "A raw inbound ServerRequest::ElicitationCreate frame is answered by the client's registered host elicitation handler while a client request is in flight (raw duplex pump; PeerHandle is NOT extended this phase)"
    - "An inbound sampling/createMessage that parses as Request::Client(CreateMessage) still reaches the host sampling handler (parse-ambiguity handled)"
    - "A KNOWN host request type with no registered handler returns a JSON-RPC -32601 method-not-found response, and the receive loop continues (a subsequent normal client call still completes) — truly-unknown methods are rejected earlier by parse_request and never reach dispatch"
    - "The client::host module compiles cfg-agnostically on wasm32-unknown-unknown because Root/ListRootsResult are relocated to the target-agnostic src/types/roots module (server::roots keeps a back-compat re-export)"
    - "The legacy Client::create_message path compiles and behaves unchanged; its rustdoc names it the LLM-server pattern and links pmcp::SamplingHandler (NOT the nonexistent server::traits path)"
  artifacts:
    - path: "src/types/roots.rs"
      provides: "target-agnostic Root + ListRootsResult (relocated from server::roots)"
      contains: "pub struct ListRootsResult"
    - path: "src/client/host/sampling.rs"
      provides: "HostSamplingHandler trait + ApprovalDecision + PreflightApproval + SamplingResultReview types"
      contains: "trait HostSamplingHandler"
    - path: "src/client/host/elicitation.rs"
      provides: "HostElicitationHandler trait"
      contains: "trait HostElicitationHandler"
    - path: "src/client/host/roots.rs"
      provides: "RootsProvider closure alias returning Result<ListRootsResult>"
      contains: "RootsProvider"
    - path: "src/client/host/mod.rs"
      provides: "ClientHostRegistry + public re-exports"
      contains: "struct ClientHostRegistry"
    - path: "src/client/mod.rs"
      provides: "host_registry field on every Client constructor + Clone, generic builder methods, dispatch_host_request + pure classify_host_request, create_message LLM-server-pattern rustdoc"
      contains: "dispatch_host_request"
    - path: "tests/client_host_roundtrip.rs"
      provides: "duplex round-trips: sampling+roots via peer, elicitation via raw pump"
      contains: "duplex"
    - path: "examples/s49_sampling_host.rs"
      provides: "runnable sampling-host example"
      contains: "HostSamplingHandler"
    - path: "Cargo.toml"
      provides: "[[example]] registration for s49_sampling_host"
      contains: "s49_sampling_host"
  key_links:
    - from: "src/client/mod.rs send_request receive loop"
      to: "self.dispatch_host_request"
      via: "replaced TransportMessage::Request error arm"
      pattern: "dispatch_host_request"
    - from: "src/client/mod.rs dispatch_host_request"
      to: "classify_host_request + host_registry sampling/elicitation/roots"
      via: "pure classification then route by kind"
      pattern: "classify_host_request"
    - from: "src/server/roots.rs"
      to: "src/types/roots.rs"
      via: "back-compat re-export of Root + ListRootsResult"
      pattern: "pub use crate::types::roots"
---

<objective>
Make a pmcp `Client` answer server->client `sampling/createMessage`, `elicitation/create`, and `roots/list` requests by introducing a client host handler registry and a dispatch arm that replaces the "Unexpected message type" error at `src/client/mod.rs:2234`. Covers HOST-01/02/03, and lands the HOST-06 rustdoc disambiguation adjacent to the code it describes (trait cross-links + the `create_message` "LLM-server pattern" note, using the REAL public path `pmcp::SamplingHandler`).

This revision incorporates the cross-AI review (`106-REVIEWS.md` consensus): the roots wire types are relocated target-agnostically so the host module is genuinely wasm-clean; the roots provider returns `Result`; the dead `server::traits::SamplingHandler` path is replaced everywhere; every `Client` constructor and the `Clone` impl gain the new field; the `-32601` claim is narrowed to KNOWN-but-unhandled requests; the elicitation round-trip uses a raw duplex pump (PeerHandle is not extended); and the example is registered per repo convention. Approval TYPES are defined here (preflight + optional result-review, owned params, generic builder methods); their INVOCATION lands in plan 02.

Purpose: The host-answering direction is the spec-compliant surface Phase 108's `SamplingSource` builds on. Fully additive; the pmcp 2.15.0 -> 2.16.0 minor bump is executed as a task in plan 02 (phase-end).
Output: New `pmcp::client::host` module, relocated roots types, wired dispatch on `Client`, duplex round-trip tests, and a runnable + registered sampling-host example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-client-host-surface/106-CONTEXT.md
@.planning/phases/106-client-host-surface/106-RESEARCH.md
@.planning/phases/106-client-host-surface/106-PATTERNS.md
@.planning/phases/106-client-host-surface/106-REVIEWS.md

<interfaces>
<!-- Contracts VERIFIED against the working tree 2026-07-17 (incl. review corrections). Use directly. -->

Wire types (already exist — DO NOT recreate; sampling/elicitation are target-agnostic, roots is being relocated by this plan):
- src/types/protocol/mod.rs:509-519 — `ServerRequest::{ CreateMessage(Box<CreateMessageParams>), ListRoots, ElicitationCreate(Box<ElicitRequestParams>) }` (serde tag = "method").
- src/types/sampling.rs — `CreateMessageParams` (carries `tools`, `tool_choice`), `CreateMessageResult` (`.new(Content, model)` ctor). TARGET-AGNOSTIC (types/mod.rs `pub mod sampling;` ungated).
- src/types/elicitation.rs:26,51 — `ElicitRequestParams` (enum: Form/Url), `ElicitResult`. TARGET-AGNOSTIC (`pub mod elicitation;` ungated).
- src/server/roots.rs:16,26 — `Root`, `ListRootsResult { roots: Vec<Root> }`. CURRENTLY wasm-gated (server/mod.rs:170-179 `#[cfg(not(target_arch = "wasm32"))] pub mod roots;`). THIS PLAN relocates them to src/types/roots.rs.

LIVE server sampling trait (the collision reference, correct public path): `pmcp::SamplingHandler` — defined at src/server/mod.rs:353, re-exported at src/lib.rs:80. THERE IS NO `server::traits` module (src/server/mod.rs has no `mod traits;`; the src/server/traits.rs FILE is dead code). Never reference `server::traits::SamplingHandler`.

PeerHandle (src/server/peer_impl.rs): methods are `sample`, `list_roots`, `progress_notify` ONLY — NO elicit. A tool can drive sampling and roots via `extra.peer()`; elicitation must be driven by a raw duplex `ServerRequest::ElicitationCreate` frame (do NOT extend PeerHandle this phase).

parse_request (src/shared/protocol_helpers.rs:16-31): tries client grammar, then server grammar, else returns `Err(Error::method_not_found)`. Inbound `sampling/createMessage` parses as `Request::Client(ClientRequest::CreateMessage)` (Pitfall 1). Truly-unknown methods NEVER become a TransportMessage::Request at the client — so the client-side -32601 path applies only to KNOWN-but-unregistered/unhandled typed requests.

Client construction sites needing the new `host_registry` field (src/client/mod.rs): the `Self { .. }` literals in `with_info` (~:172) and `with_options` (~:214), plus the `Clone for Client<T>` impl (~:2421). `new` (~:149) delegates to `with_info` — confirm. Grep `Self {` inside the client impl to catch any others.

JSONRPCResponse constructors (src/types/jsonrpc.rs:229,238): `JSONRPCResponse::success(id, result)` and `JSONRPCResponse::error(id, error)` — USE THESE, do not hand-build struct fields.

Duplex harness (tests/common/duplex.rs): `DuplexTransport::pair()`, `call_via_server(server, name, args)`. Include via `#[path = "common/duplex.rs"] mod duplex;`; gate `#![cfg(not(target_arch = "wasm32"))]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate roots wire types + create the wasm-clean client::host module (traits, roots provider, registry, preflight/result-review approval types)</name>
  <read_first>
    - src/server/roots.rs (full — the Root + ListRootsResult definitions to relocate, and anything else the module holds that must stay)
    - src/server/mod.rs (lines 165-182 — the wasm cfg gating of pub mod roots/elicitation that motivates the move)
    - src/types/mod.rs (the ungated `pub mod sampling;`/`pub mod elicitation;` lines — add `pub mod roots;` alongside)
    - src/server/mod.rs (line 353 — the LIVE `pub trait SamplingHandler`, the collision reference; correct public path is `pmcp::SamplingHandler`)
    - src/lib.rs (line 80 — confirms `pmcp::SamplingHandler` re-export)
    - .planning/phases/106-client-host-surface/106-PATTERNS.md (sampling.rs/elicitation.rs/roots.rs/mod.rs shapes — adapt per review)
    - .planning/phases/106-client-host-surface/106-REVIEWS.md (Concerns 1/2/4 + Consensus items 3/4/5)
  </read_first>
  <action>
    Step A — relocate roots wire types (additive, no breaking change): create `src/types/roots.rs` containing the `Root` and `ListRootsResult` struct definitions moved verbatim from `src/server/roots.rs` (keep derives, serde attrs, and doc comments). Add `pub mod roots;` to `src/types/mod.rs` (ungated, next to sampling/elicitation). In `src/server/roots.rs`, DELETE the moved struct bodies and replace with `pub use crate::types::roots::{Root, ListRootsResult};` so the existing `pmcp::server::roots::{Root, ListRootsResult}` path keeps resolving on native. If `src/lib.rs` or elsewhere re-exports these at the crate root, leave those working (they now resolve through the re-export). Verify the whole workspace still builds.
    Step B — create `src/client/host/` (all cfg-agnostic; import `ListRootsResult` from `crate::types::roots`, sampling/elicitation from their types modules — never from server::roots/server::elicitation):
    sampling.rs: `#[async_trait] pub trait HostSamplingHandler: Send + Sync` with `async fn handle_create_message(&self, params: CreateMessageParams) -> Result<CreateMessageResult>`. Rustdoc cross-links `pmcp::SamplingHandler` (the LLM-server pattern) — NOT `server::traits`. Approval types (owned params so they move cleanly into a `'static` future — the reviewed `&CreateMessageParams` signature is a compile-time lifetime violation): `pub enum ApprovalDecision { Allow, Deny(String) }`; `pub type PreflightApproval = Arc<dyn Fn(CreateMessageParams) -> futures::future::BoxFuture<'static, ApprovalDecision> + Send + Sync>` (mandatory pre-handler gate); `pub type SamplingResultReview = Arc<dyn Fn(CreateMessageParams, CreateMessageResult) -> futures::future::BoxFuture<'static, ApprovalDecision> + Send + Sync>` (optional post-handler review). No tokio::spawn.
    elicitation.rs: `#[async_trait] pub trait HostElicitationHandler: Send + Sync` with `async fn handle_elicitation(&self, params: ElicitRequestParams) -> Result<ElicitResult>`.
    roots.rs: `pub type RootsProvider = Arc<dyn Fn() -> futures::future::BoxFuture<'static, Result<ListRootsResult>> + Send + Sync>` — note the `Result` so provider failure maps to -32603 like the other two handlers (review Concern 4).
    mod.rs: `#[derive(Clone, Default)] pub struct ClientHostRegistry` with `pub(crate)` fields `sampling: Option<Arc<dyn HostSamplingHandler>>`, `elicitation: Option<Arc<dyn HostElicitationHandler>>`, `roots: Option<RootsProvider>`, `approval: Option<PreflightApproval>`, `result_review: Option<SamplingResultReview>`. Manual `Debug` printing `has_*` booleans. Re-export the traits + `ApprovalDecision`, `PreflightApproval`, `SamplingResultReview`, `RootsProvider`. Add `pub mod host;` + a `pub use host::{...}` line to `src/client/mod.rs`.
    Unit tests: `ClientHostRegistry::default()` all-None; Debug output contains `has_sampling: false`.
  </action>
  <verify>
    <automated>cargo build --lib && cargo test --lib client::host && cargo check --lib --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/roots.rs` contains `pub struct ListRootsResult` and `pub struct Root`; `src/types/mod.rs` has `pub mod roots;`; `src/server/roots.rs` contains `pub use crate::types::roots::{Root, ListRootsResult}` and no longer defines those structs.
    - `grep -q 'server::traits::SamplingHandler' src/client/host/sampling.rs` returns NOTHING; instead the rustdoc references `pmcp::SamplingHandler` (grep `SamplingHandler` present with the `pmcp::` or `crate::` public path).
    - `src/client/host/roots.rs` `RootsProvider` future returns `Result<ListRootsResult>` (grep `Result<ListRootsResult>` or `Result < ListRootsResult`).
    - `src/client/host/sampling.rs` defines `PreflightApproval` (owned `CreateMessageParams`, no `&`) and `SamplingResultReview` (owned params + result); `enum ApprovalDecision`.
    - `src/client/host/mod.rs` has `struct ClientHostRegistry` with `approval` and `result_review` fields and re-exports the traits.
    - `cargo build --lib` succeeds; `cargo test --lib client::host` green; `cargo check --lib --target wasm32-unknown-unknown` succeeds (host module compiles on wasm — the relocation makes ListRootsResult available there). If the wasm target is not installed, the executor installs it (`rustup target add wasm32-unknown-unknown`) or records why it could not and that the module is cfg-agnostic by inspection.
    - Back-compat: `pmcp::server::roots::ListRootsResult` still resolves (workspace `cargo build` green).
    - No `tokio::spawn` in any src/client/host/*.rs file.
  </acceptance_criteria>
  <done>Roots wire types are target-agnostic, the client::host module compiles on wasm, and it exports the three handler traits + preflight/result-review approval types + a Result-returning roots provider.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Client — field on EVERY constructor + Clone, generic builder methods, dispatch_host_request + pure classify_host_request, sanitized errors, create_message rustdoc</name>
  <read_first>
    - src/client/mod.rs (lines 100-135 — Client struct + Debug; ~:149/:172/:214 — new/with_info/with_options Self literals; ~:2300-2418 — ClientBuilder fields/methods/build(); ~:2420 — Clone impl; line 1846 — legacy create_message; the send_request receive loop `TransportMessage::Request { .. }` "Unexpected message type" arm near :2234)
    - src/types/jsonrpc.rs (lines 226-245 — JSONRPCResponse::success / ::error constructors to use)
    - src/server/mod.rs (line 353 — pmcp::SamplingHandler for the create_message rustdoc cross-link)
    - src/shared/protocol_helpers.rs (lines 16-31 and 467-483 — parse ordering; inbound sampling parses as Request::Client; truly-unknown methods rejected before dispatch)
    - src/types/protocol/mod.rs (lines 490-529 — Request / ClientRequest / ServerRequest enums)
    - .planning/phases/106-client-host-surface/106-REVIEWS.md (Consensus items 4/7, Codex unknown-method + constructor concerns, Gemini builder-ergonomics + JSONRPCResponse-ctor suggestions)
  </read_first>
  <action>
    Struct + constructors — add `host_registry: crate::client::host::ClientHostRegistry` to `Client<T>`. Initialize it (`ClientHostRegistry::default()`) in EVERY `Self { .. }` construction site: `with_info` (~:172), `with_options` (~:214), and any other literal found by grepping `Self {` in the client impl; add `host_registry: self.host_registry.clone()` to the `Clone for Client<T>` impl (~:2421). Update the manual Debug impl if it enumerates fields.
    Builder — add `host_registry: ClientHostRegistry` to `ClientBuilder` (next to `middleware_chain`). Add builder methods GENERIC over closures (Gemini ergonomics — do not force callers to construct Arc aliases): `on_sampling(self, h: impl HostSamplingHandler + 'static)`; `on_elicitation(self, h: impl HostElicitationHandler + 'static)`; `on_roots<F, Fut>(self, f: F) where F: Fn() -> Fut + Send + Sync + 'static, Fut: Future<Output = Result<ListRootsResult>> + Send + 'static` (box into `RootsProvider`); `on_sampling_approval<F, Fut>(self, cb: F) where F: Fn(CreateMessageParams) -> Fut + Send + Sync + 'static, Fut: Future<Output = ApprovalDecision> + Send + 'static` (preflight); `on_sampling_result_review<F, Fut>(self, cb: F) where F: Fn(CreateMessageParams, CreateMessageResult) -> Fut ...` (result review). Each returns `Self` and wraps the closure via `Arc::new(move |..| Box::pin(cb(..)))`. Thread `host_registry` onto the Client in `build()` like `middleware_chain`.
    Pure routing helper — add `pub(crate) enum HostRequestKind { Sampling, Elicitation, Roots, Unhandled }` and `pub(crate) fn classify_host_request(request: &Request) -> HostRequestKind` in `client::host` (or client mod): map `Request::Client(ClientRequest::CreateMessage)` OR `Request::Server(ServerRequest::CreateMessage)` -> Sampling (Pitfall 1); `ServerRequest::ElicitationCreate` -> Elicitation; `ServerRequest::ListRoots` -> Roots; everything else -> Unhandled. This function is pure/sync so plan 02 can fuzz it.
    Dispatch — add `async fn dispatch_host_request(&self, id, request: Request) -> JSONRPCResponse`. Call `classify_host_request(&request)`, then: Sampling -> if `host_registry.sampling` is Some, call it (approval invocation is plan 02 — for now call handler directly); Elicitation/Roots similarly; Unhandled OR a None handler for a known kind -> `JSONRPCResponse::error(id, <-32601 method-not-found>)`. A handler/provider returning `Err(e)` -> log the FULL error locally with `tracing::error!` and return `JSONRPCResponse::error(id, <-32603 with a sanitized/generic message>)` — do NOT forward internal error text (paths/credentials) to the remote server (review Concern: error sanitization). Build all responses with `JSONRPCResponse::success`/`::error`.
    Replace the `TransportMessage::Request { .. }` error arm near :2234: destructure `{ id, request }`, `let response = self.dispatch_host_request(id, request).await;`, send `TransportMessage::Response(response)`, then CONTINUE the loop (do NOT remove `request_id` — original response still pending).
    HOST-06 rustdoc — on `create_message` (:1846) add a paragraph naming it the "LLM-server pattern" (client asks a server whose `pmcp::SamplingHandler` runs the LLM — the INVERSE of spec host sampling); cross-link `pmcp::client::host::HostSamplingHandler`. Do not change behavior/signature.
    Keep `dispatch_host_request` cognitive complexity <= 25 by delegating classification to `classify_host_request` and each kind to a small helper (PMAT CI gate).
    Unit tests: inbound `Request::Client(CreateMessage)` reaches a registered mock HostSamplingHandler (not -32601); a KNOWN request kind with NO handler registered yields a -32601 error Response; `classify_host_request` maps both sampling variants to Sampling.
  </action>
  <verify>
    <automated>cargo test --lib client && cargo build --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'fn dispatch_host_request' src/client/mod.rs` and `grep -q 'fn classify_host_request' src/client/mod.rs src/client/host/*.rs` succeed.
    - `host_registry` initialized in with_info, with_options, any other Self literal, AND the Clone impl (grep `host_registry` count >= 4 in src/client/mod.rs).
    - `grep -q 'on_sampling'` and the `on_elicitation`/`on_roots`/`on_sampling_approval`/`on_sampling_result_review` builder methods are all present and generic over closures (no `PreflightApproval`/Arc-alias params in the public signatures).
    - `grep -c 'Unexpected message type while waiting for response' src/client/mod.rs` returns 0.
    - Dispatch uses `JSONRPCResponse::success`/`::error` (grep both) and never hand-builds the struct; -32601 for known-unhandled, -32603 for handler/provider error.
    - Handler/provider error path calls `tracing::error!` locally and returns a generic message (grep `tracing::error` near the dispatch error mapping); the raw error text is not placed in the JSON-RPC message field.
    - `grep -q 'LLM-server pattern' src/client/mod.rs` and the create_message rustdoc references `client::host::HostSamplingHandler` and `pmcp::SamplingHandler` (NOT server::traits).
    - Unit tests: sampling-alias routing passes; known-unhandled -> -32601 passes; classify maps both variants to Sampling.
    - `cargo build --lib` succeeds.
  </acceptance_criteria>
  <done>Client owns host_registry across all constructors + Clone, exposes generic host-registration builders, dispatches inbound sampling/elicitation/roots (answer or -32601), sanitizes handler errors to -32603 with local logging, and create_message rustdoc names the LLM-server pattern via the real public path.</done>
</task>

<task type="auto">
  <name>Task 3: Duplex round-trips (sampling+roots via peer, elicitation via raw pump), passthrough proptest, registered s49 example</name>
  <read_first>
    - tests/common/duplex.rs (full — DuplexTransport::pair, call_via_server; `#[path]` include convention)
    - tests/handler_peer_integration.rs (lines 1-80 — dispatcher round-trip assertion style + canned result shapes)
    - examples/s43_handler_peer_sample.rs (full — file header/cfg conventions, MockPeer shape, CreateMessageResult::new usage)
    - src/client/host/sampling.rs (HostSamplingHandler signature the test/example implement)
    - Cargo.toml (lines 240-270 — existing [[example]] entries; add s49 the same way)
    - examples/README.md (the maintained examples index format)
    - .planning/phases/106-client-host-surface/106-REVIEWS.md (Consensus item 6 — elicitation needs a raw duplex pump, not PeerHandle; example registration gap)
  </read_first>
  <action>
    Create `tests/client_host_roundtrip.rs` gated `#![cfg(not(target_arch = "wasm32"))]` with `#[path = "common/duplex.rs"] mod duplex;`.
    Sampling + roots harness (high-level): build a `Server` whose tool handler calls `extra.peer().sample(params)` (resp. `extra.peer().list_roots()`), pair with a `Client` built via `ClientBuilder::new(client_t).on_sampling(MockHostHandler)` (resp. `.on_roots(|| async { Ok(ListRootsResult { roots: vec![] }) })`), `initialize`, then `call_tool`; assert the tool result reflects the mock. Two `#[tokio::test]`s (HOST-01, HOST-03).
    Elicitation harness (raw duplex pump — PeerHandle has NO elicit): build the duplex pair; spawn a "fake server" task on `server_t` that (1) reads the client's outbound initialize + first request, (2) writes a `TransportMessage::Request { id, request: Request::Server(ServerRequest::ElicitationCreate(params)) }`, (3) reads the client's `TransportMessage::Response` and asserts its payload is the `ElicitResult` from the registered `HostElicitationHandler`. The client registers `.on_elicitation(MockElicit)`, `initialize`s, and issues a request that keeps its receive loop alive so the inbound elicitation is answered nested. One `#[tokio::test]` (HOST-02). Document in a comment that this raw pump exists because `PeerHandle` intentionally is not extended with elicitation this phase.
    Proptest `prop_sampling_passthrough`: generate `CreateMessageParams` varying `tools`/`tool_choice` + `tool_use`/`tool_result`; assert the params observed by the host handler equal what the server sent.
    Example: create `examples/s49_sampling_host.rs` gated `#![cfg(not(target_arch = "wasm32"))]`, header `//! Run with: cargo run --example s49_sampling_host`, `use anyhow::Result;`, `#[tokio::main]`; a mock `HostSamplingHandler` returning `CreateMessageResult::new(Content::text("..."), "mock-model")`; a sampling-requesting `Server` tool + a `Client` built with `.on_sampling(...)` over a duplex transport; drive `client.call_tool(...)` and print the round-trip result. Register it: add an `[[example]]` block `name = "s49_sampling_host"` `path = "examples/s49_sampling_host.rs"` (mirror the neighbouring entries incl. any `required-features`) to `Cargo.toml`, and add a one-line entry to `examples/README.md`.
  </action>
  <verify>
    <automated>cargo test --test client_host_roundtrip && cargo run --example s49_sampling_host</automated>
  </verify>
  <acceptance_criteria>
    - `tests/client_host_roundtrip.rs` exists with `#[path = "common/duplex.rs"] mod duplex;` and separate tests for sampling (peer), roots (peer), and elicitation (raw ServerRequest::ElicitationCreate pump).
    - `cargo test --test client_host_roundtrip` passes all three round-trips + `prop_sampling_passthrough`.
    - The elicitation test writes a raw `ServerRequest::ElicitationCreate` frame over the duplex transport (grep `ElicitationCreate` in the test) and does NOT call any peer elicit method.
    - `examples/s49_sampling_host.rs` exists; `Cargo.toml` has an `[[example]]` block with `name = "s49_sampling_host"`; `examples/README.md` references `s49_sampling_host`.
    - `cargo run --example s49_sampling_host` exits 0 and prints a round-trip result.
    - `cargo check --target wasm32-unknown-unknown --lib` still succeeds (tests/example are native-gated; the library surface stays wasm-clean).
  </acceptance_criteria>
  <done>Duplex round-trips prove sampling+roots (via peer) and elicitation (via raw pump) are answered by client host handlers, a passthrough proptest guards tool params, and a registered runnable s49 example demonstrates the nested sampling-host flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> client (inbound request) | A connected MCP server can now push `sampling/createMessage`, `elicitation/create`, and `roots/list` requests that the client answers. New inbound attack surface: untrusted, server-controlled JSON crosses into client handler code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Denial of Service | `dispatch_host_request` deserialization of inbound params | mitigate | Typed serde; handler/provider errors are logged locally and returned as a sanitized -32603 (no panic, no teardown). Routing fuzzed in plan 02. |
| T-106-02 | Denial of Service | known-but-unregistered inbound method | mitigate | Returns -32601 and the receive loop continues; a subsequent normal client call still completes (duplex-tested). Truly-unknown methods are rejected by parse_request upstream. |
| T-106-03 | Elevation of Privilege | server coerces client into an unapproved LLM call | mitigate (types) / wired in plan 02 | Preflight approval TYPE (owned params, pre-handler) is defined here so plan 02 can gate the LLM call BEFORE it happens; default allow. |
| T-106-10 | Information Disclosure | handler error text (paths/credentials) leaked to a remote server | mitigate | Handler/provider `Err` -> full error to local `tracing::error!`, generic sanitized message in the JSON-RPC -32603 response. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — additive code within the existing pmcp crate (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo build --lib` + `cargo test --lib client::host` + `cargo test --lib client` green.
- `cargo check --lib --target wasm32-unknown-unknown` succeeds (host module + relocated roots types compile on wasm).
- `cargo test --test client_host_roundtrip` green (sampling/roots via peer, elicitation via raw pump, passthrough proptest).
- `cargo run --example s49_sampling_host` exits 0.
- Back-compat: workspace `cargo build` green (server::roots re-export intact).
- Legacy regression: existing `create_message` tests remain green.
- `make quality-gate` green before phase verification.
</verification>

<success_criteria>
- Server `peer.sample()`/`peer.list_roots()` and a raw inbound elicitation are answered by client-registered host handlers across a real transport.
- Inbound sampling that parses as `Request::Client(CreateMessage)` still reaches the host handler.
- Known-but-unregistered inbound methods return -32601 without dropping the connection.
- The host module compiles cfg-agnostically on wasm32 (roots types relocated target-agnostically).
- The legacy `create_message` path is unchanged and its rustdoc names the LLM-server pattern via the real `pmcp::SamplingHandler` path.
</success_criteria>

<output>
Create `.planning/phases/106-client-host-surface/106-01-SUMMARY.md` when done.
</output>
