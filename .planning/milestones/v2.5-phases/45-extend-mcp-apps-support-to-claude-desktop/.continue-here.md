---
phase: 45-extend-mcp-apps-support-to-claude-desktop
task: MCP Apps examples and mcp-preview fixes
total_tasks: 3
status: complete
last_updated: 2026-03-10T04:02:39Z
---

<current_state>
Phase 45 plans (45-01, 45-02, 45-03) complete. Examples rework and mcp-preview
fixes committed as `04317e9`. All 3 examples (chess, map, dataviz) migrated to
TypedToolWithOutput with outputSchema + structuredContent. mcp-preview has 3 bug
fixes shipped. Ready to move to v1.6 Phase 28 (Flag Normalization).
</current_state>

<completed_work>

- Migrated chess/map/dataviz examples to TypedToolWithOutput with outputSchema
- Added ui/notifications/tool-result postMessage listeners to all 3 widgets
- Fix 1: host-initiated tool results now reach widgets in standard mode
  (removed chatgpt-only guard on ui/notifications/tool-result postMessage)
- Fix 2: WASM bridge CORS — added /api/mcp same-origin proxy endpoint with
  MCP session header forwarding; WASM client connects through proxy
- Fix 3: protocol tab in standard mode — returns "ui" descriptor key and
  validates structuredContent presence on tool calls
- Refactored proxy.rs: extracted mcp_post(), check_response(), extract_header(),
  attach_session_id() helpers and MCP_SESSION_ID/MCP_PROTOCOL_VERSION constants
- /simplify review passed twice — all findings addressed
- Committed as 04317e9
</completed_work>

<remaining_work>

- Phase 45 examples: may need testing with Claude Desktop when available
- v1.6 Phase 28 (Flag Normalization) is next on the roadmap
- Phase 33 (fix mcp-tester failure) has incomplete plan (no SUMMARY)
</remaining_work>

<decisions_made>

- AppBridge sendToolResult only fires in ChatGPT mode — no standard widget uses ontoolresult
- WASM proxy is raw JSON-RPC forward (no ensure_initialized) — WASM client manages own session
- Standard-mode descriptor key is "ui" (top-level key in nested _meta.ui.resourceUri)
- Tool call validation runs in all modes with structuredContent check as extra
</decisions_made>

<blockers>
None.
</blockers>

<context>
The MCP Apps examples are now functional in mcp-preview for both proxy and WASM
modes, in both standard and ChatGPT modes. The protocol tab validates in all modes.
Next milestone work is v1.6 CLI DX — Phase 28 Flag Normalization.
</context>

<next_action>
Plan Phase 28 (Flag Normalization): /gsd:plan-phase 28
</next_action>
