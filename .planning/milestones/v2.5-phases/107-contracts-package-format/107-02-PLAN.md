---
phase: 107-contracts-package-format
plan: 02
type: execute
wave: 2
depends_on: ["107-01", "107-03"]
files_modified:
  - crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json
  - crates/pmcp-package/tests/golden_fixtures/team_small_review_v1.json
  - crates/pmcp-package/tests/golden_fixtures/canonical/**
  - crates/pmcp-package/tests/digest_stability.rs
  - Cargo.toml
  - CLAUDE.md
  - Makefile
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [PKG-02]
must_haves:
  truths:
    - "Golden fixtures cover all four package kinds: server, workflow, agent, team"
    - "digest_stability.rs pins an expected canonical SHA-256 digest constant per package kind and asserts the current serializer reproduces it exactly (a wire freeze, not just determinism)"
    - "digest_stability.rs asserts each fixture's canonical JSON bytes equal a checked-in canonical snapshot (catches silent field add/remove)"
    - "digest_stability.rs retains the ≥100-recomputation determinism assertion per kind"
    - "Makefile quality-gate runs cargo fmt --check, cargo clippy --all-targets -- -D warnings, and cargo test for pmcp-package via --manifest-path (workspace-excluded crate is now gated locally)"
    - ".github/workflows/ci.yml runs the same standalone pmcp-package fmt/clippy/test checks via --manifest-path (CI blind spot closed)"
    - "tests/team_contracts_conformance.rs is added to the root Cargo.toml package exclude list (it ships nowhere without contracts/, which is already excluded)"
    - "pmcp-package is listed in CLAUDE.md publish order as a leaf before cargo-pmcp"
    - "release.yml has a publish step for pmcp-package using --manifest-path (not -p, since the crate is workspace-excluded)"
    - "PKG-02 state 1 (this phase) is proven: cargo publish --dry-run (failure-preserving) succeeds and cargo package --list shows README/CHANGELOG/both licenses"
    - "cargo metadata at repo root succeeds (isolation verified; exclude entry added only if required)"
  artifacts:
    - path: "crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json"
      provides: "AgentPackage golden fixture (wire-freeze coverage)"
    - path: "crates/pmcp-package/tests/golden_fixtures/team_small_review_v1.json"
      provides: "TeamPackage golden fixture (wire-freeze coverage)"
    - path: "crates/pmcp-package/tests/golden_fixtures/canonical"
      provides: "Checked-in canonical-JSON snapshots (one per kind) asserted byte-equal"
    - path: "Makefile"
      provides: "Standalone pmcp-package quality gate wired into quality-gate"
      contains: "manifest-path crates/pmcp-package/Cargo.toml"
    - path: ".github/workflows/ci.yml"
      provides: "CI standalone pmcp-package fmt/clippy/test step"
      contains: "manifest-path crates/pmcp-package/Cargo.toml"
    - path: ".github/workflows/release.yml"
      provides: "crates.io publish step for pmcp-package"
      contains: "pmcp-package"
  key_links:
    - from: "crates/pmcp-package/tests/digest_stability.rs"
      to: "crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json"
      via: "pinned-digest test deserializes the fixture and asserts its canonical digest equals a checked-in constant"
      pattern: "agent_.*_v1|team_.*_v1|EXPECTED_.*_DIGEST"
    - from: ".github/workflows/release.yml"
      to: "crates/pmcp-package/Cargo.toml"
      via: "cargo publish --manifest-path crates/pmcp-package/Cargo.toml"
      pattern: "manifest-path crates/pmcp-package"
    - from: "Makefile"
      to: "crates/pmcp-package/Cargo.toml"
      via: "quality-gate target runs fmt/clippy/test via --manifest-path"
      pattern: "manifest-path crates/pmcp-package"
  notes:
    - "PKG-02 completion is TWO states (Codex HIGH). STATE 1 (delivered here, in-phase): publish-ready + release-wired + quality-gated, proven by Task 3's failure-preserving `cargo publish --dry-run` and `cargo package --list` assertions. STATE 2 (REQUIRED release-checkpoint follow-up, OUT of this phase): the tag-triggered release.yml runs `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`, then `cargo search pmcp-package` confirms 0.1.0 on crates.io, then a throwaway consumer crate with `pmcp-package = \"0.1\"` (no path override) runs `cargo check`. PKG-02 MUST NOT be marked Shipped until state 2 passes. This is recorded here, not silently deferred."
---

<objective>
Complete the PKG-02 wire-freeze story with real teeth: extend golden-fixture coverage to all four
serialized package kinds AND pin an expected canonical digest per kind so a shape change in ANY kind
fails CI (determinism alone did not prove a freeze — Codex HIGH). Wire `pmcp-package` into the repo's
tooling on both axes: the quality machinery (Makefile + ci.yml standalone gates, closing the
workspace-exclusion blind spot both reviewers rated HIGH) and the publish machinery (publish-order
docs + release workflow). Fix the published-`pmcp`-package hygiene gap (Gemini MEDIUM) and make the
two-state PKG-02 acceptance explicit.

Purpose: The wire-freeze claim ("0.1.x digest-stable") is only real if every serialized shape is
pinned-digest-guarded; the crate is only trustworthy if it is actually fmt/clippy/test-gated; and it
can only ship if the release workflow reaches it via `--manifest-path` (it is workspace-excluded, so
`-p pmcp-package` does NOT resolve it).
Output: agent + team golden fixtures + pinned-digest/canonical-snapshot tests for all four kinds;
Makefile + ci.yml standalone gates; root Cargo.toml hygiene (A4 isolation + conformance-test
exclude); CLAUDE.md publish-order entry; release.yml manifest-path publish step; and a proven
STATE-1 PKG-02 acceptance with STATE-2 recorded as a required release follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-contracts-package-format/107-RESEARCH.md
@.planning/phases/107-contracts-package-format/107-PATTERNS.md
@.planning/phases/107-contracts-package-format/107-01-SUMMARY.md

<interfaces>
<!-- pmcp-package digest API (from crates/pmcp-package/src/digest/canonical.rs) — use directly. -->
```rust
pub struct ManifestDigest(String);
impl ManifestDigest { pub fn as_str(&self) -> &str; }
pub fn canonicalize<T: Serialize>(value: &T) -> Result<Vec<u8>>;   // olpc-cjson canonical bytes
pub fn manifest_digest<T: Serialize>(value: &T) -> Result<ManifestDigest>; // canonicalize-then-SHA256
// re-exported at pmcp_package::digest::{manifest_digest, canonicalize, ManifestDigest}
```
Existing test helper in digest_stability.rs:
`fn read_fixture(name: &str) -> Vec<u8>` — reads tests/golden_fixtures/<name> via CARGO_MANIFEST_DIR.
</interfaces>

<notes>
- `pmcp-package` has its own `[workspace]` table and is NOT a root workspace member. Therefore
  `cargo publish -p pmcp-package` from the root will NOT find it, and root `cargo fmt/clippy/test`
  IGNORE it — every command that must reach it uses `--manifest-path crates/pmcp-package/Cargo.toml`.
- Publish order: `pmcp-package` is a LEAF (no internal deps). It only needs to publish BEFORE
  cargo-pmcp (which pins it in Phase 110). Placing it as an early leaf is correct.
- A4 resolution: root Cargo.toml `members` is an explicit list and the crate has its own
  `[workspace]` table, so it is already isolated. Only add a root workspace `exclude` entry if
  `cargo metadata` at the repo root errors on it.
- Published-`pmcp` hygiene (Gemini MEDIUM): the root `pmcp` package already excludes `contracts/`
  (root Cargo.toml exclude list, ~line 41) to stay under the crates.io size limit, but `tests/` are
  packaged. The Plan-03 test `tests/team_contracts_conformance.rs` reads `contracts/...` at runtime,
  so a downstream `cargo test` on the published crate would fail to find its data. Fix: add
  `"tests/team_contracts_conformance.rs"` to that exclude list. (The test itself already resolves
  paths via `CARGO_MANIFEST_DIR` — see 107-03.)
- Determinism ≠ wire freeze (Codex HIGH): the pinned-digest constant is the primary gate. A removed
  struct field silently drops from the old fixture on deserialize, and a defaulted new field is
  filled in — either changes the canonical bytes and thus the digest, so a pinned assertion fails.
  The canonical-snapshot byte comparison is the belt-and-suspenders second gate.
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: All-four-kind golden fixtures with PINNED canonical digests + snapshots</name>
  <files>crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json, crates/pmcp-package/tests/golden_fixtures/team_small_review_v1.json, crates/pmcp-package/tests/golden_fixtures/canonical/*.canonical.json, crates/pmcp-package/tests/digest_stability.rs</files>
  <read_first>
    - crates/pmcp-package/tests/digest_stability.rs (existing server/workflow determinism tests + the read_fixture helper + the inline AgentPackage example showing the exact field set)
    - crates/pmcp-package/tests/golden_fixtures/server_team_fs_v1.json (canonical single-line JSON shape to match)
    - crates/pmcp-package/src/digest/canonical.rs (canonicalize / manifest_digest / ManifestDigest::as_str signatures)
    - crates/pmcp-package/src/package/agent.rs (AgentPackage struct — authoritative field names/types)
    - crates/pmcp-package/src/package/team.rs (TeamPackage struct — authoritative field names/types)
  </read_first>
  <behavior>
    - For each of the 4 kinds (ServerPackage, WorkflowManifest, AgentPackage, TeamPackage): deserialize the fixture, and assert manifest_digest(value).as_str() == EXPECTED_<KIND>_DIGEST (a checked-in lowercase-hex constant). Changing any serialized field breaks this.
    - For each kind: assert canonicalize(value) == include_bytes!("golden_fixtures/canonical/<kind>.canonical.json") — byte-equal to the checked-in canonical snapshot.
    - For each kind: retain manifest_digest stable across ≥100 recomputations.
    - Agent and team fixtures deserialize without error (round-trip parses).
  </behavior>
  <action>
    Author two new input fixtures matching the compact canonical-JSON style of
    `server_team_fs_v1.json`. `agent_pto_researcher_v1.json` serializes a valid `AgentPackage`
    (fields per src/package/agent.rs, cross-checked against the inline example already in
    digest_stability.rs: name, version, instructions, llm, max_tokens, max_iterations, connectors,
    tool_selection, input_schema, output_schema, importance, finalizer_role, budget_defaults).
    `team_small_review_v1.json` serializes a valid `TeamPackage` (fields per src/package/team.rs).
    Produce each by serializing a constructed struct so `serde_json::from_slice` round-trips.
    Then, for ALL FOUR kinds (server, workflow, agent, team), extend `digest_stability.rs`:
    (1) Compute the canonical digest once during authoring and check it in as a `const
    EXPECTED_<KIND>_DIGEST: &str = "<64-hex>"`; add a test that asserts
    `manifest_digest(&value).unwrap().as_str() == EXPECTED_<KIND>_DIGEST`. This is the PRIMARY
    wire-freeze gate (a serialized-shape change ⇒ different digest ⇒ failure — the 0.2.0 trigger).
    (2) Write the crate's `canonicalize(&value)` output for each kind to
    `tests/golden_fixtures/canonical/<kind>.canonical.json` and add a test asserting
    `canonicalize(&value).unwrap() == include_bytes!(".../canonical/<kind>.canonical.json")`
    (secondary snapshot gate against silent field add/remove).
    (3) KEEP the existing ≥100-recomputation determinism assertions for all kinds.
    All four kinds (including the pre-existing server/workflow) get the pinned-digest + snapshot
    treatment — not just the two new kinds.
  </action>
  <verify>
    <automated>cd crates/pmcp-package && cargo test --test digest_stability</automated>
  </verify>
  <acceptance_criteria>
    - `agent_pto_researcher_v1.json` parses as an `AgentPackage`; `team_small_review_v1.json` parses as a `TeamPackage`
    - `digest_stability.rs` defines an `EXPECTED_<KIND>_DIGEST` constant for each of server, workflow, agent, team and asserts equality against `manifest_digest(...).as_str()`
    - `tests/golden_fixtures/canonical/` contains a `<kind>.canonical.json` snapshot for each of the four kinds, each asserted byte-equal via `canonicalize()`
    - The ≥100-recomputation determinism assertions remain for all four kinds
    - `cargo test --test digest_stability` exits 0
    - Manually mutating a fixture (e.g. dropping a field) makes the pinned-digest test FAIL (spot-checked during execution)
  </acceptance_criteria>
  <done>All four package kinds have checked-in fixtures whose canonical digest AND canonical bytes are pinned, so any serialized-shape change fails CI — a real wire freeze, not just determinism.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pmcp-package into repo tooling — quality gates, publish order, workspace hygiene</name>
  <files>Makefile, .github/workflows/ci.yml, .github/workflows/release.yml, CLAUDE.md, Cargo.toml</files>
  <read_first>
    - Makefile (the `quality-gate` target ~line 660 chaining fmt-check/lint/build/test-all/audit; the `lint` target ~line 150 for clippy-flag style)
    - .github/workflows/ci.yml (the `test` job steps ~lines 58-96: Format check / Clippy / Run tests — mirror these for the excluded crate)
    - .github/workflows/release.yml (the `publish-crates` job; the `Publish pmcp-widget-utils` leaf block ~lines 99-115 with its "already exists"→continue guard + 30s index wait to mirror)
    - CLAUDE.md (the "### Workspace Crates (publish order)" numbered list, items 1–12)
    - Cargo.toml (root `[package] exclude` list ~lines 15-46 incl. `contracts/`; the `[workspace] members` ~line 578 and `exclude` ~line 580)
  </read_first>
  <action>
    (A) Quality gates (close the workspace-exclusion blind spot — both reviewers HIGH):
      - Makefile: add a `.PHONY: pmcp-package-gate` target that runs, via
        `--manifest-path crates/pmcp-package/Cargo.toml`:
        `cargo fmt --manifest-path crates/pmcp-package/Cargo.toml --all -- --check`,
        `cargo clippy --manifest-path crates/pmcp-package/Cargo.toml --all-targets -- -D warnings`,
        and `cargo test --manifest-path crates/pmcp-package/Cargo.toml`. Add `pmcp-package-gate`
        to the `quality-gate` target's chain (e.g. after `test-all`, before `audit`) so
        `make quality-gate` (and thus the CI quality-gate job) exercises it.
      - ci.yml: in the `test` job, add a step named "pmcp-package standalone checks" after the
        existing "Run tests" step that runs the same three `--manifest-path` commands (fmt --check,
        clippy --all-targets -- -D warnings, test). This gives the excluded crate an explicit CI
        block even in the light `test` job path.
    (B) Published-`pmcp` hygiene (Gemini MEDIUM): add `"tests/team_contracts_conformance.rs"` to the
      root `Cargo.toml` `[package] exclude` list (the list that already contains `contracts/`), so
      the Plan-03 conformance test never ships in the published `pmcp` crate (its `contracts/` data
      is excluded, so a downstream `cargo test` would otherwise fail to compile/run).
    (C) Workspace isolation (A4): run `cargo metadata --format-version 1 --no-deps` at the repo root.
      If it succeeds and does not err on `crates/pmcp-package`, leave `[workspace] members`/`exclude`
      UNCHANGED. Only if it errors referencing `crates/pmcp-package`, add `"crates/pmcp-package"` to
      the `[workspace] exclude` array. Do NOT add it to `members`.
    (D) Publish order docs: insert a `pmcp-package` entry in CLAUDE.md's "Workspace Crates (publish
      order)" list as a LEAF with no internal deps, noting it is standalone/workspace-excluded and
      published via `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`, and that it must
      publish BEFORE `cargo-pmcp` (which pins `pmcp-package = "0.1"` in Phase 110). Place it as an
      early leaf entry (e.g. "1b." right after `pmcp-widget-utils`).
    (E) release.yml: add a publish step for pmcp-package in the `publish-crates` job, placed as an
      early leaf (right after the `Publish pmcp-widget-utils` block, before its downstream deps).
      Mirror the widget-utils step's structure and its skip guard, but classify failure precisely:
      continue ONLY when the output matches "already exists" (i.e. `pmcp-package 0.1.0` already
      published); ALL other failures must `exit 1`. Use
      `cargo publish --manifest-path crates/pmcp-package/Cargo.toml` (NOT `-p pmcp-package`). Add a
      `sleep 30` crates.io index wait after it, matching the surrounding steps.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo metadata --format-version 1 --no-deps >/dev/null && grep -q "manifest-path crates/pmcp-package/Cargo.toml" Makefile && grep -q "pmcp-package-gate" Makefile && grep -q "manifest-path crates/pmcp-package/Cargo.toml" .github/workflows/ci.yml && grep -q "manifest-path crates/pmcp-package/Cargo.toml" .github/workflows/release.yml && ! grep -q "cargo publish -p pmcp-package" .github/workflows/release.yml && grep -q "tests/team_contracts_conformance.rs" Cargo.toml && grep -q "pmcp-package" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` defines a `pmcp-package-gate` target running fmt --check + clippy --all-targets -D warnings + test via `--manifest-path crates/pmcp-package/Cargo.toml`, and `quality-gate` chains it
    - `.github/workflows/ci.yml` `test` job contains a step running the same three `--manifest-path crates/pmcp-package/Cargo.toml` checks
    - Root `Cargo.toml` `[package] exclude` list contains `"tests/team_contracts_conformance.rs"`
    - `cargo metadata --format-version 1 --no-deps` at repo root exits 0; root `[workspace] members` does NOT contain `crates/pmcp-package`
    - `CLAUDE.md` "Workspace Crates (publish order)" list contains a `pmcp-package` leaf entry published before cargo-pmcp
    - `.github/workflows/release.yml` contains `cargo publish --manifest-path crates/pmcp-package/Cargo.toml` and does NOT contain `cargo publish -p pmcp-package`; the step continues only on an "already exists" match and `exit 1`s otherwise
  </acceptance_criteria>
  <done>The excluded crate is fmt/clippy/test-gated locally and in CI, its conformance test is excluded from the published pmcp crate, its isolation is verified, and it is documented + wired into release.yml as an early leaf via --manifest-path with precise failure classification.</done>
</task>

<task type="auto">
  <name>Task 3: PKG-02 STATE-1 acceptance (publish-ready proof) + record STATE-2 follow-up</name>
  <files>.planning/phases/107-contracts-package-format/107-02-SUMMARY.md</files>
  <read_first>
    - crates/pmcp-package/Cargo.toml (publish metadata + docs.rs table from Plan 01)
    - .github/workflows/release.yml (the pmcp-package publish step wired in Task 2)
  </read_first>
  <action>
    Prove PKG-02 STATE 1 (publish-ready + release-wired) without publishing (publishes never run from
    a plan — repo precedent). Run, failure-preserving (no swallowing pipe): from
    `crates/pmcp-package/`, `cargo publish --dry-run --allow-dirty` (exit code honored), then
    `cargo package --list --allow-dirty` and assert README.md, CHANGELOG.md, LICENSE-MIT, and
    LICENSE-APACHE are all present in the archive listing. In `107-02-SUMMARY.md`, record the
    explicit TWO-STATE PKG-02 acceptance: STATE 1 = proven here (dry-run + package-list + digest
    freeze + quality gates + release wiring); STATE 2 (REQUIRED release-checkpoint follow-up, OUT of
    this phase) = after the tag-triggered `release.yml` runs `cargo publish --manifest-path
    crates/pmcp-package/Cargo.toml`, verify `cargo search pmcp-package --limit 1` reports `0.1.0`,
    then create a throwaway consumer crate depending on `pmcp-package = "0.1"` (no path override) and
    run `cargo check`. State plainly that PKG-02 MUST NOT be marked Shipped until STATE 2 passes.
  </action>
  <verify>
    <automated>cd crates/pmcp-package && cargo publish --dry-run --allow-dirty && cargo package --list --allow-dirty > /tmp/pkg107_2.list && grep -q '^README.md$' /tmp/pkg107_2.list && grep -q '^CHANGELOG.md$' /tmp/pkg107_2.list && grep -q '^LICENSE-MIT$' /tmp/pkg107_2.list && grep -q '^LICENSE-APACHE$' /tmp/pkg107_2.list</automated>
  </verify>
  <acceptance_criteria>
    - `cargo publish --dry-run --allow-dirty` from `crates/pmcp-package` exits 0 (exit code honored, not masked by a pipe)
    - `cargo package --list` output contains README.md, CHANGELOG.md, LICENSE-MIT, LICENSE-APACHE
    - `107-02-SUMMARY.md` records the two-state PKG-02 acceptance and names STATE 2 (publish + `cargo search` + external-consumer `cargo check`) as a REQUIRED follow-up before PKG-02 is Shipped
  </acceptance_criteria>
  <done>PKG-02 STATE 1 is mechanically proven (publish-ready, gated, release-wired) and STATE 2 (published + externally resolvable) is recorded as a required, non-silent release-checkpoint follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized package → SHA-256 digest | Pinned-digest + canonical-snapshot guarded wire-freeze boundary across all four kinds |
| workspace-excluded crate → repo quality gates | Excluded crate bypasses root fmt/clippy/test unless wired via --manifest-path |
| published pmcp crate → downstream `cargo test` | Test data (`contracts/`) is excluded; a test that reads it must not ship |
| repo tag push → crates.io publish | Release workflow publishes the new crate; wrong invocation (`-p`) silently skips it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-04 | Tampering | agent/team/server/workflow serialized shape | mitigate | Pinned `EXPECTED_<KIND>_DIGEST` constants + canonical-snapshot byte assertions for all four kinds fail CI on any shape change (the 0.2.0 trigger); determinism-only gap (Codex HIGH) closed |
| T-107-05 | Denial of Service (release integrity) | release.yml publish step | mitigate | Use `--manifest-path` (workspace-excluded crate cannot be reached by `-p`); precise failure classification continues only on "already exists", all other failures `exit 1` |
| T-107-10 | Tampering / CI integrity | workspace-excluded crate quality gate | mitigate | Makefile `pmcp-package-gate` + ci.yml standalone step run fmt/clippy/test via `--manifest-path`, so a clippy/format/test regression in the excluded crate now fails the org-required gate |
| T-107-11 | Denial of Service | published `pmcp` crate downstream tests | mitigate | `tests/team_contracts_conformance.rs` added to the root `exclude` list so the published crate never ships a test whose `contracts/` data is excluded (would break downstream `cargo test`) |
| T-107-SC | Tampering | dependency set (supply chain) | accept | No new packages introduced; deps already pinned + audited in Plan 01; `cargo audit` in `make quality-gate` |
</threat_model>

<verification>
- `cd crates/pmcp-package && cargo test --test digest_stability` passes with pinned digests + snapshots for all four kinds.
- `make quality-gate` runs the standalone pmcp-package fmt/clippy/test block (grep confirms the target + chain).
- `.github/workflows/ci.yml` and `release.yml` contain the `--manifest-path crates/pmcp-package/Cargo.toml` invocations; neither release nor ci uses `-p pmcp-package`.
- Root `Cargo.toml` excludes `tests/team_contracts_conformance.rs`; `cargo metadata --no-deps` exits 0.
- `cargo publish --dry-run` (failure-preserving) + `cargo package --list` prove STATE 1; SUMMARY records STATE 2.
- `make quality-gate` (repo-wide) green before phase gate.
</verification>

<success_criteria>
All four package kinds are pinned-digest + canonical-snapshot guarded for wire-freeze; `pmcp-package`
is fmt/clippy/test-gated locally and in CI via `--manifest-path`; its conformance test is excluded
from the published `pmcp` crate; it is documented in the publish order and wired into release.yml via
`--manifest-path`; workspace isolation is confirmed; PKG-02 STATE 1 is proven and STATE 2 recorded as
a required release follow-up.
</success_criteria>

<output>
Create `.planning/phases/107-contracts-package-format/107-02-SUMMARY.md` when done.
</output>
