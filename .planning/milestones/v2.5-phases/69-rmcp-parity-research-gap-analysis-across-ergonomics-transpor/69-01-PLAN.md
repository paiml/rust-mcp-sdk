---
phase: 69
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-01-SUMMARY.md
autonomous: true
estimated_tasks: 3
requirements: []  # Phase 69 is a seed phase — PARITY-<SURFACE>-NN IDs land via Plan 03 into REQUIREMENTS.md
user_setup: []

must_haves:
  truths:
    - "69-RESEARCH.md exists in the phase directory"
    - "The exact rmcp crates.io version string and matching git tag URL are recorded near the top of the document"
    - "The pmcp baseline is recorded as v2.3.0 plus a short statement of which APIs are post-v2.3.0 main-only"
    - "Every MUST surface (D-05 tool macros, D-06 builder APIs, D-07 typed wrappers, D-08 handler signatures + state/extra) has a dedicated H2 section with a gap-matrix table"
    - "Every INCLUDE surface (D-09 client ergonomics, D-10 error types) has a dedicated H2 section with a gap-matrix table"
    - "Every row in every table has a stable Row ID of the form <SURFACE>-NN (MACRO-01, BUILDER-01, TYPED-01, HANDLER-01, CLIENT-01, ERR-01) in the first column"
    - "Every row in every table has a non-empty evidence citation for BOTH the rmcp and pmcp columns (file path + line number for local code; GitHub blob URL with line fragment preferred for rmcp — docs.rs anchor permitted only as fallback)"
    - "Every pmcp evidence cell ends with the baseline tag [v2.3.0] (API present in v2.3.0) or [main] (post-v2.3.0 main-only)"
    - "Every row in every table has a Severity cell containing exactly one of: High, Medium, Low"
    - "Rows where both SDKs take the same approach are marked 'Parity' in the Gap cell with Severity = Low, not omitted"
    - "Rows where pmcp does something better than rmcp are marked 'Strength to preserve' in the Gap cell, not framed as a gap"
  artifacts:
    - path: ".planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md"
      provides: "Evidence-backed ergonomics gap matrix across 6 surfaces with stable row IDs"
      contains: "## Tool Macros"
      contains_alt: "## Builder APIs"
      contains_alt_2: "## Typed Tool / Prompt Wrappers"
      contains_alt_3: "## Handler Signatures + State / Extra Injection"
      contains_alt_4: "## Client-Side Ergonomics"
      contains_alt_5: "## Error Types + Result Wrappers"
      min_lines: 200
  key_links:
    - from: "69-RESEARCH.md rmcp-version line"
      to: "https://crates.io/crates/rmcp"
      via: "version string match"
      pattern: "rmcp.*[0-9]+\\.[0-9]+\\.[0-9]+"
    - from: "69-RESEARCH.md each row"
      to: "pmcp source file"
      via: "file-path:line-number citation"
      pattern: "src/.*\\.rs:[0-9]+"
    - from: "69-RESEARCH.md each row"
      to: "stable row ID"
      via: "<SURFACE>-NN identifier in first column"
      pattern: "(MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}"

---

<objective>
Produce `69-RESEARCH.md`, the evidence-backed gap matrix comparing pmcp (v2.3.0 + current `feat/sql-code-mode` main) against rmcp (latest stable crates.io release as of 2026-04-16) across the six ergonomics surfaces defined by CONTEXT.md D-05 through D-10.

Purpose: This document is the factual foundation for Phase 69 Plan 02 (which derives follow-on phase proposals from High-severity gaps). Without this plan's output, Plan 02 has nothing to score. Stable row IDs (e.g., `MACRO-03`) make the row-to-proposal mapping in Plan 02 and the quality-gate bijection in Plan 03 precise and bundle-safe.

Output: A single markdown file at `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` containing one H2 section per surface, each with a gap-matrix table and short per-row paragraphs only where a table cell cannot carry the nuance. No Rust source code is changed.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md

# pmcp-side baseline evidence (D-04) — read to populate the pmcp column of every matrix row
@CRATE-README.md
@src/lib.rs
@src/server/builder.rs
@src/server/traits.rs
@src/server/cancellation.rs
@src/shared/cancellation.rs
@src/server/typed_tool.rs
@src/server/typed_prompt.rs
@src/server/wasm_typed_tool.rs
@src/types/protocol/mod.rs
@src/types/capabilities.rs
@pmcp-macros/README.md
@pmcp-macros/src/lib.rs

# Prior-phase pattern reference (for document style / citation discipline)
@.planning/phases/67-docs-rs-pipeline-and-feature-flags/67-CONTEXT.md

# Anti-patterns to avoid in the matrix
@.claude/get-shit-done/references/universal-anti-patterns.md

<interfaces>
The gap-matrix table shape is locked. Every H2 surface section uses exactly this column set, in this order (Row ID is the FIRST column and is mandatory):

| Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
|--------|------------------|---------------|---------------|---------------|---------------|-----|----------|

- **Row ID**: stable identifier of the form `<SURFACE>-NN` where `<SURFACE>` is one of `MACRO`, `BUILDER`, `TYPED`, `HANDLER`, `CLIENT`, `ERR` (the surface prefix matches the H2 section) and `NN` is a two-digit zero-padded counter starting at `01` per surface. IDs are assigned in document order and MUST be unique. Once written, an ID is persistent (do not renumber across revisions; if a row is deleted, its ID is retired, not reused). Plan 02 proposals cite these IDs verbatim in their `### Rationale` subsection; Plan 03 quality gate compares High-severity row IDs against proposal citations.
- Surface subtopic: a specific ergonomic aspect of the surface (e.g., for Tool Macros: "async fn support", "attribute-parameter richness", "schema derivation", "error-type conversion", "state injection").
- rmcp approach: 1–3 sentence description. Factual, not judgmental. Phrased as "what rmcp does", not "rmcp is better/worse at".
- rmcp evidence (citation standard): **Primary: GitHub blob URL with `#L<N>` line fragment** pointing into the rmcp source tree at the pinned git tag, targeting `crates/rmcp/src/...` under the rust-sdk workspace layout (e.g., `https://github.com/modelcontextprotocol/rust-sdk/blob/v0.2.1/crates/rmcp/src/service.rs#L42`). Sibling crates like `rmcp-macros` follow the same workspace path convention (`crates/rmcp-macros/src/...`). **Fallback: docs.rs rustdoc anchor URL**, permitted ONLY when the claim is about the rendered API surface (e.g., trait signatures visible on docs.rs but not cleanly attributable to a single source line). Bare URLs without line numbers or anchors are NOT acceptable. Acceptance criterion: at least 80% of rmcp evidence citations across the document must be GitHub blob URLs with `#L<N>` line fragments; up to 20% may be docs.rs anchors for legitimate surface-level claims.
- pmcp approach: 1–3 sentence description of the pmcp equivalent.
- pmcp evidence: file path + line number (e.g., `src/server/builder.rs:142`), followed by a baseline tag `[v2.3.0]` or `[main]`:
  - `[v2.3.0]` — the cited API is present in the published v2.3.0 release.
  - `[main]` — the cited API exists only on post-v2.3.0 main (current `feat/sql-code-mode` branch) and has not yet shipped to crates.io.
  Line numbers MUST match the repo state at the HEAD commit used for this research. Record the commit SHA in the document header. Every pmcp evidence cell MUST end with one of the two tags.
- Gap: one of three values: (a) a specific gap description (e.g., "pmcp lacks attribute-level `#[tool(description = ...)]` override; description is derived from rustdoc only"), (b) "Parity" if both SDKs take the same approach, (c) "Strength to preserve" if pmcp does it better than rmcp.
- Severity: exactly one of "High", "Medium", "Low", per the D-14 rubric:
  - **High**: affects the typical first-hour user path AND a clean specific fix can be articulated.
  - **Medium**: affects less common paths or has no single clean fix.
  - **Low**: cosmetic or theoretical. "Parity" rows and "Strength to preserve" rows are always Low.

Document header shape (write verbatim at the top of 69-RESEARCH.md, filling in the bracketed values):

```markdown
# Phase 69: rmcp Parity Research — Ergonomics Gap Matrix

**Researched:** 2026-04-16
**rmcp version pinned:** [exact crates.io version string, e.g., "0.2.1"]
**rmcp source tag:** [exact git tag URL, e.g., "https://github.com/modelcontextprotocol/rust-sdk/releases/tag/v0.2.1"]
**pmcp baseline:** v2.3.0 + current `feat/sql-code-mode` main at commit [exact HEAD SHA, short form]
**Surfaces covered:** tool macros, builder APIs, typed wrappers, handler signatures + state/extra injection, client-side ergonomics, error types + result wrappers
**Surfaces excluded:** Tower / service integration (Phase 56), transport construction API (deferred per D-12)

### Source file decisions

[Populated during Task 2 if any canonical source file for client-side ergonomics (D-09) or error types (D-10) was not found at the named path and a substitute was chosen. Leave empty if all named canonical sources existed.]

## Executive Summary

[2–4 sentences: top-line finding — how many High-severity gaps, which surface has the most gap pressure, where pmcp already holds strength. Do NOT editorialize beyond the matrix evidence.]
```

After the header, the six H2 sections appear in the order: Tool Macros → Builder APIs → Typed Tool / Prompt Wrappers → Handler Signatures + State / Extra Injection → Client-Side Ergonomics → Error Types + Result Wrappers.

A brief per-surface introduction (2–4 sentences) MAY precede each table, establishing what the surface covers. The table is mandatory. A short per-row follow-up paragraph MAY appear after the table only for rows whose nuance cannot fit in cells.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin rmcp baseline and write document skeleton</name>
  <files>.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md</files>
  <read_first>
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` — specifically D-01..D-04 (rmcp baseline + pmcp baseline) and D-13 (table shape).
    - `.planning/ROADMAP.md` lines 913-927 — Phase 69 goal and deliverables.
    - Fetch `https://crates.io/crates/rmcp` — read the "Versions" list to identify the latest stable (non-prerelease) version as of 2026-04-16. Record the exact version string.
    - Fetch `https://github.com/modelcontextprotocol/rust-sdk/releases` — identify the git release tag matching the pinned crates.io version. Record the tag URL (e.g., `https://github.com/modelcontextprotocol/rust-sdk/releases/tag/vX.Y.Z`).
    - Run `git rev-parse --short HEAD` in the repo root to capture the pmcp HEAD commit SHA at research start. This pins the pmcp baseline for line-number reproducibility.
  </read_first>
  <action>
    Create `69-RESEARCH.md` in the phase directory with the document header block specified in the `<interfaces>` section of this plan, filling in the bracketed values from the readings above:
    - `**rmcp version pinned:**` → exact version string from crates.io
    - `**rmcp source tag:**` → exact git release tag URL
    - `**pmcp baseline:**` → `v2.3.0 + feat/sql-code-mode at commit <short-SHA>`
    - `### Source file decisions` → leave placeholder empty (populated by Task 2 only if substitutions were required).
    - `## Executive Summary` → placeholder text `[TO BE FILLED AFTER MATRIX COMPLETE]` — this is filled in Task 3 after all rows are populated.

    Then append the six H2 section scaffolds in this exact order, each with just the H2 heading, a one-line surface description derived from CONTEXT.md D-05..D-10, and an empty table with only the header row:

    1. `## Tool Macros` (D-05) — row IDs will be `MACRO-NN`
    2. `## Builder APIs` (D-06) — row IDs will be `BUILDER-NN`
    3. `## Typed Tool / Prompt Wrappers` (D-07) — row IDs will be `TYPED-NN`
    4. `## Handler Signatures + State / Extra Injection` (D-08) — row IDs will be `HANDLER-NN`
    5. `## Client-Side Ergonomics` (D-09) — row IDs will be `CLIENT-NN`
    6. `## Error Types + Result Wrappers` (D-10) — row IDs will be `ERR-NN`

    Each empty table uses exactly this header row (copy verbatim, note Row ID is first column):

    ```markdown
    | Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
    |--------|------------------|---------------|---------------|---------------|---------------|-----|----------|
    ```

    Do NOT fill any data rows in this task — those are Task 2. This task only establishes the document skeleton and locks the rmcp/pmcp version pins so subsequent tasks cite against a stable baseline.
  </action>
  <verify>
    <automated>test -f .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^\*\*rmcp version pinned:\*\* [0-9]" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^## Tool Macros$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^## Builder APIs$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^## Typed Tool / Prompt Wrappers$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^## Handler Signatures + State / Extra Injection$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^## Client-Side Ergonomics$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^## Error Types + Result Wrappers$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -q "^| Row ID | Surface subtopic |" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - 69-RESEARCH.md exists.
    - The rmcp version string is a concrete semver triple (e.g., "0.2.1"), not a placeholder.
    - The rmcp source tag is a concrete GitHub release URL (e.g., `https://github.com/modelcontextprotocol/rust-sdk/releases/tag/v0.2.1`), not a bare repo URL.
    - The pmcp baseline line contains a short git SHA (7-char hex).
    - All six H2 section headings are present in the specified order.
    - Each section has an empty matrix table with the exact header row from `<interfaces>` — the Row ID column is the first column and is present in all six tables.
    - The `### Source file decisions` placeholder subsection exists in the header (Task 2 populates it only if needed).
    - No data rows are filled (those are populated in Task 2).
  </acceptance_criteria>
  <done>Document skeleton with pinned baselines exists; six empty matrix tables ready to be populated with Row IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Populate matrix rows for all six surfaces with stable Row IDs</name>
  <files>.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md</files>
  <read_first>
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` D-05 through D-10 — scope of each surface, and D-13/D-14 (matrix shape + severity rubric).
    - For the pmcp column of each surface, read the following file(s) in full and note the concrete file:line citations to use:
      - **Surface 1 (Tool Macros, D-05):** `pmcp-macros/src/lib.rs` (macro definitions for `#[mcp_tool]`, `#[mcp_server]`, `#[mcp_prompt]`, `#[mcp_resource]`), `pmcp-macros/README.md` (user-facing surface), and search the repo for `#[mcp_tool]` usage sites in `examples/` for idiomatic patterns.
      - **Surface 2 (Builder APIs, D-06):** `src/server/builder.rs` (`ServerCoreBuilder` surface), and the builder usage sites in `src/lib.rs` Quick Start and `CRATE-README.md`.
      - **Surface 3 (Typed Wrappers, D-07):** `src/server/typed_tool.rs` (`TypedTool`, `TypedToolWithOutput`, `TypedSyncTool`), `src/server/typed_prompt.rs` (`TypedPrompt`), `src/server/wasm_typed_tool.rs` (`WasmTypedTool`). Note: Phase 54.1 landed uniform constructors; cross-reference constructor patterns.
      - **Surface 4 (Handler Signatures + State/Extra, D-08):** `src/server/traits.rs` (handler trait signatures), `src/server/cancellation.rs` (`RequestHandlerExtra` non-wasm), `src/shared/cancellation.rs` (wasm path).
      - **Surface 5 (Client-side, D-09) — named canonical sources:** `src/client/mod.rs`, `src/client/core.rs`, `examples/09_client_with_tool.rs`. These are the authoritative pmcp client entry points. If any of these paths does not exist at execution time, select a substitute (e.g., another `src/client/*.rs` file or another client example) AND record the substitution in the `### Source file decisions` subsection of the document header (which file was substituted, for what reason). Do not silently deviate.
      - **Surface 6 (Errors, D-10) — named canonical sources:** `src/error.rs`, `src/types/error.rs` (if exists), and `src/lib.rs` `Result<T>` alias export line. Same substitution rule as above — if a named file is absent, record the substitute in `### Source file decisions`.
    - For the rmcp column of each surface, fetch the following URLs against the pinned tag recorded in Task 1's header:
      - `https://docs.rs/rmcp/<pinned-version>/rmcp/` — rustdoc index (fallback source only).
      - `https://github.com/modelcontextprotocol/rust-sdk/tree/<pinned-tag>/crates/rmcp/src/` — PRIMARY source tree. Browse for the analogs of each surface:
        - Tool macros: look in `crates/rmcp-macros/src/` sibling crate or the macros module.
        - Builder/server: look for `ServerHandler`, `ServiceExt`, `Service` traits under `crates/rmcp/src/`.
        - Typed wrappers: look for `CallToolHandler`, tool-registration patterns.
        - Handler signatures: trace the handler trait definition.
        - Client: look for `ClientHandler`, `ServiceExt::serve` on the client side.
        - Errors: look for the top-level error enum in `crates/rmcp/src/error.rs`.
      - **Citation preference:** GitHub blob URL with `#L<n>` line fragment pinned to the tag and targeting `crates/rmcp/src/...` or `crates/rmcp-macros/src/...` workspace path (e.g., `https://github.com/modelcontextprotocol/rust-sdk/blob/v0.2.1/crates/rmcp/src/service.rs#L42`). **Fallback:** docs.rs anchor URL, permitted only for surface-level API claims where a single source line is not attributable. Target: ≥80% GitHub blob URLs.
  </read_first>
  <action>
    Fill every matrix table in 69-RESEARCH.md. Work through the six surfaces in order. Assign stable Row IDs as you go — `MACRO-01, MACRO-02, ...` for Tool Macros rows in document order; `BUILDER-01, ...` for Builder APIs; etc.

    **For each surface, produce between 4 and 8 rows.** Each row covers one specific subtopic within the surface. Aim for 4 if the two SDKs are close, 8 if divergent. The minimum-row targets per surface are (write at least this many):

    - Surface 1 (Tool Macros, MACRO-NN): 5 rows minimum. Suggested subtopics: attribute richness (description/name override), async support, schema derivation mechanism, state injection mechanism, error-type conversion, compile-fail test discipline.
    - Surface 2 (Builder APIs, BUILDER-NN): 4 rows minimum. Suggested subtopics: construction entrypoint (builder vs constructor), tool registration shape, prompt registration shape, capabilities declaration shape.
    - Surface 3 (Typed Wrappers, TYPED-NN): 4 rows minimum. Suggested subtopics: input-type derivation, output-type derivation (output schema support), sync vs async variants, wasm variant availability.
    - Surface 4 (Handler Signatures + State/Extra, HANDLER-NN): 4 rows minimum. Suggested subtopics: handler fn shape (async / return type), state injection pattern, cancellation token access, request metadata (`RequestHandlerExtra`-equivalent).
    - Surface 5 (Client-Side Ergonomics, CLIENT-NN): 4 rows minimum. Suggested subtopics: client construction entrypoint, request-building ergonomics, notification handling, progress tracking.
    - Surface 6 (Error Types + Result Wrappers, ERR-NN): 4 rows minimum. Suggested subtopics: top-level error enum shape, `thiserror` usage, `?`-operator friction (From impls), result alias convention.

    **Cell-filling rules (STRICT):**
    - `Row ID`: `<SURFACE>-NN` where SURFACE matches the section prefix and NN is a zero-padded two-digit counter starting at 01 per surface, assigned in document order.
    - `rmcp approach` and `pmcp approach` cells: 1–3 sentences each. Factual only. Do NOT frame rmcp negatively (CONTEXT.md "Specific Ideas"). When pmcp does it better, frame it as "Strength to preserve" in the Gap cell, not as an implicit put-down.
    - `rmcp evidence` cells: every cell MUST contain either (a) a GitHub blob URL with `#L<n>` fragment pinned to the research tag targeting `crates/rmcp/src/...` or `crates/rmcp-macros/src/...` (PRIMARY), OR (b) a docs.rs rustdoc URL pinned to the version with a type/macro anchor (FALLBACK, for surface-level claims only). Bare URLs without line numbers or anchors are rejected. If the analog does not exist in rmcp, write `N/A — no rmcp analog; searched [module paths]` and list the rmcp source paths you searched.
    - `pmcp evidence` cells: every cell MUST contain a `path/to/file.rs:LINENO` reference followed by a baseline tag. Line numbers must match the HEAD SHA recorded in the document header. The cell must END with either `[v2.3.0]` (API was present in the published v2.3.0 release) or `[main]` (API is post-v2.3.0 main-only). Example: `src/server/builder.rs:142 [v2.3.0]` or `src/server/typed_tool.rs:298 [main]`.
    - `Gap` cells: one of three forms — (a) a specific gap description (include the direction, e.g., "pmcp lacks X" or "rmcp lacks Y"), (b) `Parity` (both SDKs equivalent on this subtopic), (c) `Strength to preserve` (pmcp does this better and should keep doing it).
    - `Severity` cells: `High`, `Medium`, or `Low`. A row with `Gap = Parity` or `Gap = Strength to preserve` MUST have Severity `Low`. A row with a real gap gets High/Medium/Low per D-14 rubric.

    **Source file decisions:** If during reading you find that any named canonical source file for Surface 5 (Client) or Surface 6 (Errors) does not exist at the specified path, populate the `### Source file decisions` subsection at the top of the document with an entry per substitution:
    - `- {original-path}: not found at HEAD {short-SHA}; substituted {actual-path} because {one-line reason}.`
    If all named sources were present, leave the subsection body empty or write `- All named canonical sources were present; no substitutions required.`

    **Per-row follow-up paragraphs:** After a surface's table, write a short `#### Notes` subsection ONLY for rows whose severity rationale or nuance cannot fit in the cells. Typical case: 0–2 notes per surface. Do not write notes for straightforward rows — the table speaks.

    **No editorializing beyond the matrix evidence.** If the evidence does not support a claim, do not make the claim. Cherry-picking is an anti-pattern (see `universal-anti-patterns.md`). If a subtopic cannot be evidenced from source reading alone (per D-03), write the row with a `Severity: Low` and a Gap cell that says `Insufficient source-reading evidence — deferred hands-on spike` — do NOT fabricate.

    **80/20 citation ratio:** After populating all six tables, scan all rmcp evidence cells and compute the percentage that are GitHub blob URLs with `#L<n>` fragments versus docs.rs anchors. Target is ≥80% GitHub blob URLs. If the ratio is below 80%, upgrade docs.rs citations to blob URLs where a single source line is attributable before declaring the task done.
  </action>
  <verify>
    <automated>awk '/^## Tool Macros$/,/^## Builder APIs$/' .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md | grep -c "^| MACRO-[0-9][0-9] " | awk '$1 >= 5 { print "PASS tool-macros rows: " $1; exit 0 } { print "FAIL tool-macros rows: " $1 " (need >= 5)"; exit 1 }' && awk '/^## Builder APIs$/,/^## Typed Tool/' .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md | grep -c "^| BUILDER-[0-9][0-9] " | awk '$1 >= 4 { print "PASS builder rows: " $1; exit 0 } { print "FAIL builder rows: " $1 " (need >= 4)"; exit 1 }' && awk '/^## Typed Tool/,/^## Handler Signatures/' .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md | grep -c "^| TYPED-[0-9][0-9] " | awk '$1 >= 4 { print "PASS typed rows: " $1; exit 0 } { print "FAIL typed rows: " $1 " (need >= 4)"; exit 1 }' && awk '/^## Handler Signatures/,/^## Client-Side/' .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md | grep -c "^| HANDLER-[0-9][0-9] " | awk '$1 >= 4 { print "PASS handler rows: " $1; exit 0 } { print "FAIL handler rows: " $1 " (need >= 4)"; exit 1 }' && awk '/^## Client-Side/,/^## Error Types/' .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md | grep -c "^| CLIENT-[0-9][0-9] " | awk '$1 >= 4 { print "PASS client rows: " $1; exit 0 } { print "FAIL client rows: " $1 " (need >= 4)"; exit 1 }' && awk '/^## Error Types/,0' .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md | grep -c "^| ERR-[0-9][0-9] " | awk '$1 >= 4 { print "PASS error rows: " $1; exit 0 } { print "FAIL error rows: " $1 " (need >= 4)"; exit 1 }' && python3 -c "
import re, sys
content = open('.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md').read()
# Every pmcp evidence cell — look for .rs:LINENO — must be followed by [v2.3.0] or [main] somewhere in the same cell
# Grep approximation: count .rs:N occurrences and [v2.3.0]/[main] tags; they should be comparable in magnitude.
rs_cites = len(re.findall(r'\.rs:[0-9]+', content))
v_tags = len(re.findall(r'\[v2\.3\.0\]', content)) + len(re.findall(r'\[main\]', content))
if rs_cites == 0:
    print('FAIL: no .rs:LINENO citations found'); sys.exit(1)
if v_tags < int(rs_cites * 0.8):
    print(f'FAIL: pmcp baseline tags {v_tags} < 80%% of pmcp citations {rs_cites}'); sys.exit(1)
# Row IDs: every non-header table row starting with | <SURFACE>-NN
row_ids = re.findall(r'^\| (MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2} \|', content, re.MULTILINE)
if len(row_ids) < 25:
    print(f'FAIL: only {len(row_ids)} Row-ID-prefixed rows found (need >= 25 across 6 surfaces)'); sys.exit(1)
# No duplicate Row IDs within the same surface
full_ids = re.findall(r'^\| ((?:MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}) \|', content, re.MULTILINE)
if len(full_ids) != len(set(full_ids)):
    dupes = [i for i in set(full_ids) if full_ids.count(i) > 1]
    print(f'FAIL: duplicate Row IDs: {dupes}'); sys.exit(1)
# rmcp evidence 80/20 rule: count GitHub blob URLs vs docs.rs anchors used as evidence
blob_urls = len(re.findall(r'https://github\.com/modelcontextprotocol/rust-sdk/blob/[^ )|]+#L[0-9]+', content))
docsrs = len(re.findall(r'https://docs\.rs/rmcp/', content))
total_rmcp = blob_urls + docsrs
if total_rmcp == 0:
    print('FAIL: no rmcp evidence URLs found'); sys.exit(1)
ratio = blob_urls / total_rmcp
if ratio < 0.8:
    print(f'FAIL: blob-URL ratio {ratio:.2f} < 0.80 (blob={blob_urls}, docs.rs={docsrs})'); sys.exit(1)
print(f'PASS: {len(row_ids)} rows, baseline-tag ratio {v_tags}/{rs_cites}, blob ratio {ratio:.2f}')
"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the six H2 surface tables has the minimum row count specified above (Tool Macros ≥5, others ≥4).
    - Every data row in every table starts with a Row ID in the first column matching `<SURFACE>-NN` where SURFACE matches the surface (MACRO / BUILDER / TYPED / HANDLER / CLIENT / ERR) and NN is two-digit zero-padded.
    - Row IDs are unique across the document (no duplicates within or across surfaces).
    - Every data row in every table has non-empty `rmcp approach`, `rmcp evidence`, `pmcp approach`, `pmcp evidence`, `Gap`, and `Severity` cells.
    - Every `pmcp evidence` cell matches the regex `[a-zA-Z_/-]+\\.rs:[0-9]+` (file path + line number) OR points to a `.md` file with a line number, AND ends with a baseline tag `[v2.3.0]` or `[main]`.
    - At least 80% of pmcp evidence cells are followed by a baseline tag (the verify block enforces this via regex-ratio check).
    - Every `rmcp evidence` cell matches one of: GitHub blob URL with `#L<n>` fragment targeting `crates/rmcp/src/...` or `crates/rmcp-macros/src/...`, docs.rs URL with a type/macro anchor, OR begins with the literal `N/A — no rmcp analog`.
    - At least 80% of rmcp evidence URLs are GitHub blob URLs with `#L<n>` fragments (the verify block enforces this).
    - Every `Severity` cell is exactly `High`, `Medium`, or `Low` (case-sensitive).
    - Rows with `Gap = Parity` or `Gap = Strength to preserve` all have `Severity = Low`.
    - If any named canonical source file for Surface 5 or Surface 6 was not found, the `### Source file decisions` subsection records the substitute and reason.
    - No row claims a gap without an evidence citation supporting the claim (manual spot-check during execution).
  </acceptance_criteria>
  <done>All six matrix tables populated with evidence-backed rows; Row IDs assigned; severity assigned per D-14 rubric; pmcp baseline tags and rmcp blob-URL-preferred ratios satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Write Executive Summary and consistency-sweep the document</name>
  <files>.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md</files>
  <read_first>
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` — re-read the full document to produce the executive summary and sweep for consistency issues.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` "Specific Ideas" section — reminder that rmcp is framed as credibility benchmark, not adversary; pmcp-strengths are called out as "Strength to preserve".
  </read_first>
  <action>
    1. **Replace the Executive Summary placeholder.** Write 4–8 sentences (per D-23 "Claude's Discretion" — include if matrix runs long; the matrix will be long, so include). The summary MUST state:
       - Count of High-severity gaps found (exact integer).
       - Count of Medium-severity gaps found.
       - Count of Low-severity rows (including Parity + Strength-to-preserve).
       - Which surface(s) concentrate the High-severity gaps (reference surface names, and list the specific Row IDs of High rows for forward-referenceability, e.g. "High rows: MACRO-02, MACRO-05, CLIENT-01").
       - Which surface(s) show pmcp strengths worth preserving (if any).
       - A one-sentence forward pointer: "Each High-severity gap is proposed as a follow-on phase in `69-PROPOSALS.md` (Plan 02 output), cited by Row ID."

       Do NOT editorialize beyond what the matrix supports. If zero High-severity gaps were found, say so explicitly — that is a valid outcome and means Plan 02 produces zero proposals (which triggers the D-15 re-read check).

    2. **Internal consistency sweep.** Walk through the entire document and verify:
       - Surface order matches D-05..D-10 (Tool Macros → Builder → Typed Wrappers → Handler Signatures → Client → Errors).
       - Every `Severity: Low` row with `Gap: Parity` or `Gap: Strength to preserve` is consistent.
       - No severity is High unless a clean fix can be articulated in the Gap cell (per D-14). If a High row's Gap cell is vague, either tighten the Gap description or downgrade to Medium.
       - Row IDs are sequential per surface (no gaps: MACRO-01, MACRO-02, MACRO-03, ... — if a row was deleted during revision, document it in Methodology Notes rather than renumbering).
       - The pmcp commit SHA in the header matches `git rev-parse --short HEAD` at the time of the sweep (re-verify — if it drifted, note that line numbers are pinned to the original SHA).
       - The rmcp version string in the header is unchanged from Task 1 and still matches crates.io latest-stable (re-check — if rmcp published a new version during the research window, record that fact as a footnote but do NOT re-baseline; per D-02, unreleased work does not affect scoring, and mid-research re-baselining would invalidate Task 2's citations).

    3. **Add a trailing `## Methodology Notes` section** at the end of the document (after all six surface sections) stating:
       - Research approach: source + docs reading only per D-03; no hands-on example-building.
       - Evidence standard: file path + line number with `[v2.3.0]` or `[main]` baseline tag for local code; GitHub blob URL with `#L<n>` targeting `crates/rmcp/src/...` workspace path (primary) or docs.rs URL with anchor (fallback for surface-level claims) for rmcp.
       - Row ID convention: `<SURFACE>-NN` (MACRO/BUILDER/TYPED/HANDLER/CLIENT/ERR) assigned sequentially per surface; persistent once written.
       - Severity rubric: verbatim restatement of D-14 (3 levels, assigned during research).
       - Out-of-scope reminder: Tower / service integration (D-11, Phase 56 handled) and transport construction (D-12, deferred unless High-severity).
       - Reproducibility note: "Running this research against a different rmcp version or pmcp commit will produce different line numbers; the pinned versions in the header are the canonical baseline for this document."
  </action>
  <verify>
    <automated>grep -q "^## Methodology Notes$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && ! grep -q "\[TO BE FILLED AFTER MATRIX COMPLETE\]" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && grep -qE "High-severity gaps? found.*[0-9]+" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## Executive Summary` section contains explicit integer counts for High, Medium, and Low severity rows.
    - The Executive Summary lists the Row IDs of all High-severity rows explicitly (so Plan 02 can derive proposals by Row ID directly).
    - The placeholder text `[TO BE FILLED AFTER MATRIX COMPLETE]` is gone.
    - The `## Methodology Notes` section exists at the end of the document and documents the Row ID convention, evidence standards, and pmcp baseline tag scheme.
    - A forward pointer to `69-PROPOSALS.md` is present in the summary.
    - No `Severity: High` row has a Gap cell shorter than 10 characters (a proxy for "vague description") — manual spot-check during execution.
  </acceptance_criteria>
  <done>69-RESEARCH.md is a complete, internally consistent, evidence-backed gap matrix with stable Row IDs, executive summary (listing High Row IDs), and methodology notes. Ready to be consumed by Plan 02 by Row ID reference.</done>
</task>

</tasks>

<threat_model>
Documentation-only phase. No code, config, or API surface changes. Threats considered: (1) inaccurate evidence citations leading downstream phases astray — mitigated by requiring file-path+line-number citations and a re-read consistency sweep in Task 3; (2) biased gap severity scoring leading to poor prioritization — mitigated by the explicit severity rubric in CONTEXT.md D-14/D-16 and by requiring every High row to articulate a clean fix in the Gap cell itself; (3) row-identity drift during revision — mitigated by stable `<SURFACE>-NN` Row IDs assigned once and never renumbered, so Plan 02 proposals and Plan 03 bijection checks can cite rows precisely even if subtopic wording is edited later.
</threat_model>

<verification>
Grep-verifiable post-conditions:
- `grep -c "^|" 69-RESEARCH.md` ≥ 45 (6 surfaces × 4+ data rows × ~1.5 pipe lines per row, including header rows).
- `grep -c "^## " 69-RESEARCH.md` ≥ 8 (Executive Summary + 6 surfaces + Methodology Notes).
- `grep -cE " High$| Medium$| Low$" 69-RESEARCH.md` ≥ 25 (severity cells across all tables).
- `grep -cE "^\| (MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2} \|" 69-RESEARCH.md` ≥ 25 (Row-ID-prefixed rows across 6 surfaces).
- `grep -cE "https://github\.com/modelcontextprotocol/rust-sdk/blob/.*crates/rmcp.*#L[0-9]+" 69-RESEARCH.md` ≥ 16 (primary rmcp source citations into workspace path).
- `grep -cE "^\\| .+ \\| .+ \\| .+\\.rs:[0-9]+" 69-RESEARCH.md` ≥ 20 (pmcp file:line citations) — approximate.
- `grep -cE "\[v2\.3\.0\]|\[main\]" 69-RESEARCH.md` ≥ 20 (pmcp baseline tags on evidence cells).
</verification>

<success_criteria>
- `69-RESEARCH.md` exists and is 200+ lines.
- All six MUST + INCLUDE surfaces have populated matrix tables with Row IDs.
- Every row has a stable Row ID, evidence citations for both SDKs, and a pmcp baseline tag.
- Severity cells are one of High/Medium/Low.
- Executive summary contains integer counts per severity level AND lists the Row IDs of all High rows.
- rmcp version, rmcp source tag, and pmcp commit SHA are pinned in the document header.
- `### Source file decisions` is present (empty or populated); named canonical sources for Surfaces 5 and 6 were honored or substitutions documented.
</success_criteria>

<output>
After completion, create `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-01-SUMMARY.md` summarizing: rmcp version pinned, pmcp commit SHA, row counts per surface, High/Medium/Low totals, the list of High-severity Row IDs, blob-URL-vs-docs.rs citation ratio, and a one-line handoff note to Plan 02 ("N High-severity gaps identified — Row IDs: <list>; see RESEARCH.md §<surface> rows for details").
</output>
</output>
