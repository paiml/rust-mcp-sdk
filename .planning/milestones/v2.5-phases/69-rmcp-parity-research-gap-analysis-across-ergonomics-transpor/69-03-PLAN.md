---
phase: 69
plan: 03
type: execute
wave: 3
depends_on: [69-01, 69-02]
files_modified:
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-03-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
estimated_tasks: 3
requirements: []  # Phase 69 is a seed phase — PARITY-<SURFACE>-NN IDs land via Plan 03 into REQUIREMENTS.md
user_setup: []

must_haves:
  truths:
    - "Every claim in 69-RESEARCH.md has an evidence citation (file:line or URL with line fragment/anchor)"
    - "Every proposal in 69-PROPOSALS.md has all six required template subsections and exactly ONE PARITY-<SURFACE>-NN REQ-ID"
    - "PARITY-* requirement IDs from 69-PROPOSALS.md are landed in REQUIREMENTS.md (1-to-1: one ID per proposal)"
    - "STATE.md reflects Phase 69 completion — new decisions bullet AND reconciled Current focus / Current Position fields"
    - "PROJECT.md Key Decisions (if the table exists) reflects the Phase 69 research outcome"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Landed PARITY-* requirement IDs traceable to proposals"
      contains: "PARITY-"
    - path: ".planning/STATE.md"
      provides: "Session continuity with Phase 69 complete and stale focus fields reconciled"
      contains: "Phase 69"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md"
      via: "PARITY-* ID reference"
      pattern: "PARITY-(MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)"

---

<objective>
Final quality gate for Phase 69. Cross-check that `69-RESEARCH.md` and `69-PROPOSALS.md` satisfy every CONTEXT.md MUST (D-05..D-19) and the CONTEXT.md evidence standards, then land the seeded `PARITY-*` requirement IDs in `REQUIREMENTS.md` (one per proposal), and update `STATE.md` + `PROJECT.md` to reflect phase completion (including reconciliation of stale `Current focus:` / `Current Position:` fields).

**Traceability model (author-locked, matches Plan 02):** One `PARITY-<SURFACE>-NN` requirement ID per proposal, mapping to the proposal as a whole. Not one ID per success criterion. The proposal's 3–5 success criteria are internal acceptance tests. Landing in REQUIREMENTS.md is 1-to-1 with proposals.

**Bijection check (on stable Row IDs):** The quality gate compares High-severity Row IDs from `69-RESEARCH.md` against cited Row IDs in `69-PROPOSALS.md`. Uses `<SURFACE>-NN` identifiers, not free-text `(surface, subtopic)` strings — stable under revision and bundle-safe.

Purpose: Without this plan, Phase 69's deliverables would sit in the phase directory unreferenced from the roadmap/requirements/state. This plan does the integration work that makes the research actionable for downstream planning (e.g., `/gsd-plan-phase` invocations for the follow-on phases).

Output: Verified and integrated Phase 69 deliverables. No new SDK code. The integration touches `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, and `.planning/PROJECT.md` only; the two research documents get corrective edits only if quality issues are found.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md

# Plan 01 and Plan 02 outputs — the subjects of this quality gate
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md

<interfaces>
**REQUIREMENTS.md landing shape.** Under the v2.1 Requirements section (existing file), add a new subsection:

```markdown
### rmcp Parity (Phase 69 research — seeds follow-on phases)

Seeded by Phase 69 research (`.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md`). One REQ-ID per proposal, mapping to the proposal as a whole; the proposal's 3–5 success criteria are its internal acceptance tests. Status remains pending until the follow-on phase ships.

- [ ] **PARITY-<SURFACE>-NN**: [one-line description copied verbatim from the proposal's Suggested Requirement ID entry]
- [ ] **PARITY-<SURFACE>-NN**: [...]
[...]
```

This subsection appears AFTER the existing "Code Mode Support" subsection and BEFORE "Previous Requirements".

**Traceability table landing shape.** In the `## Traceability` table at the bottom of REQUIREMENTS.md, append one row per PARITY-* ID:

```markdown
| PARITY-<SURFACE>-NN | Phase [suggested-number from proposal] | Pending |
```

**STATE.md updates.** In the `## Accumulated Context` > `### Decisions` > "v2.1 decisions" bullet list, append a new bullet:

```markdown
- [Phase 69]: rmcp parity research complete — 69-RESEARCH.md (gap matrix, N rows total, M High-severity) + 69-PROPOSALS.md (K proposals). K PARITY-* requirement IDs landed in REQUIREMENTS.md (one per proposal); follow-on phases not yet scheduled.
```

In the `## Session Continuity` block, update `Stopped at:` to `Phase 69 complete — follow-on proposals ready for ROADMAP slotting` and `Resume:` to `Run /gsd:add-phase to slot a 69-PROPOSALS.md entry into the roadmap.`

**Reconcile stale focus fields** (reviewer M-5): the plain-text body of STATE.md currently contains:
- `**Current focus:** Phase 65 — examples-cleanup-protocol-accuracy` — stale.
- `## Current Position` block with `Phase: 67.2` and `Plan: Not started` — stale.

Update these:
- `**Current focus:**` → `Phase 69 — rmcp parity research (complete); follow-on proposals pending ROADMAP slotting`.
- `## Current Position` → `Phase: 69` and `Plan: 03 (complete)`.

Use read-then-assert-delta pattern so re-running the task is idempotent (only updates fields whose pre-edit value differs from the expected post-edit value).

Update the progress counters in the STATE.md frontmatter:
- `completed_phases` increments by 1
- `completed_plans` increments by 3 (this plan + Plan 01 + Plan 02)

**PROJECT.md updates.** If `PROJECT.md` contains a "Key Decisions" table, append one row:

```markdown
| 2026-04-16 | Phase 69 rmcp ergonomics parity research | K proposals seeded; follow-on phases to slot into late v2.1 / v2.2 | .planning/phases/69-.../69-PROPOSALS.md |
```

If PROJECT.md does not have such a table, skip this step — Plan 03 does not introduce a new table structure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quality-gate 69-RESEARCH.md and 69-PROPOSALS.md</name>
  <files>
    .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md
    .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md
  </files>
  <read_first>
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` — in full.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` — in full.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` D-05 through D-19 — the full decision set to check against.
  </read_first>
  <action>
    Execute the following quality-gate checks. For each check, log PASS or FAIL with specifics. If any FAIL, fix inline (edit the relevant file) before proceeding.

    **A. MUST surface coverage (D-05..D-10):**
    - A1: 69-RESEARCH.md has `## Tool Macros` H2 with a populated table (≥5 MACRO-NN data rows). [MUST D-05]
    - A2: 69-RESEARCH.md has `## Builder APIs` H2 with a populated table (≥4 BUILDER-NN data rows). [MUST D-06]
    - A3: 69-RESEARCH.md has `## Typed Tool / Prompt Wrappers` H2 with a populated table (≥4 TYPED-NN data rows). [MUST D-07]
    - A4: 69-RESEARCH.md has `## Handler Signatures + State / Extra Injection` H2 with a populated table (≥4 HANDLER-NN data rows). [MUST D-08]
    - A5: 69-RESEARCH.md has `## Client-Side Ergonomics` H2 with a populated table (≥4 CLIENT-NN data rows). [INCLUDE D-09]
    - A6: 69-RESEARCH.md has `## Error Types + Result Wrappers` H2 with a populated table (≥4 ERR-NN data rows). [INCLUDE D-10]

    **B. Baseline pinning (D-01..D-04):**
    - B1: 69-RESEARCH.md header contains `**rmcp version pinned:**` with a concrete version string (regex `[0-9]+\.[0-9]+\.[0-9]+`). [D-01]
    - B2: 69-RESEARCH.md header contains `**rmcp source tag:**` with a GitHub release URL (regex `github\.com/modelcontextprotocol/rust-sdk/releases/tag/`). [D-01]
    - B3: 69-RESEARCH.md header contains `**pmcp baseline:**` with a short git SHA (7-char hex). [D-04]
    - B4: Every pmcp evidence cell ends with `[v2.3.0]` or `[main]` (reviewer M-2). Verify the ratio: count `.rs:NN` citations and count `[v2.3.0]`+`[main]` tags; tag count must be ≥ 80% of citation count.
    - B5: No row in 69-RESEARCH.md cites unreleased rmcp work as evidence for a Severity rating. Scan for the word "unreleased", "unpublished"; if found, verify the citation is a footnote/context only, not the severity-scoring evidence. [D-02]

    **C. Evidence citation standard (D-03, "Specific Ideas" rule, reviewer M-3):**
    - C1: Every `pmcp evidence` cell across all tables in 69-RESEARCH.md contains a file:line citation. Heuristic: grep for `\.rs:[0-9]+` in the document; expected count ≥ sum of minimum row counts (≥ 25 across all surfaces).
    - C2: Every `rmcp evidence` cell across all tables in 69-RESEARCH.md contains either a GitHub blob URL with `#L<n>` fragment, a docs.rs URL with an anchor, OR the exact phrase "N/A — no rmcp analog".
    - C3: Reviewer M-3 ratio: ≥80% of rmcp evidence URLs are GitHub blob URLs with `#L<n>` fragments targeting `crates/rmcp/src/...` or `crates/rmcp-macros/src/...`. Measure `(blob_url_count) / (blob_url_count + docs_rs_count) >= 0.80`.
    - C4: No table row in 69-RESEARCH.md makes a claim without an evidence citation. Spot-check: if any row has both evidence cells filled with generic statements (e.g., just a module path without a line number), flag and fix.

    **D. Severity rubric (D-14, D-16):**
    - D1: Every `Severity` cell in 69-RESEARCH.md is exactly `High`, `Medium`, or `Low`.
    - D2: No `Severity: High` row has a `Gap:` cell that is vague (≥ 10 characters AND contains a specific action phrase like "add", "replace", "expose", "support", "derive"). [D-14 "clean articulable fix"]
    - D3: Every row with `Gap: Parity` or `Gap: Strength to preserve` has `Severity: Low`.
    - D4: Row IDs are present in column 1 of every matrix data row (pattern `^\| <SURFACE>-NN \|`); Row IDs are unique across the document; counters per surface are sequential starting from 01.

    **E. Proposal completeness (D-17, D-18) — one-REQ-ID-per-proposal model:**
    - E1: 69-PROPOSALS.md header's `**Proposal count:**` integer matches the number of `## Proposal N:` sections in the file.
    - E2: Every proposal has all six template subsections: `### Goal`, `### Scope`, `### Success Criteria`, `### Suggested Requirement ID` (singular), `### Estimated Plan Count`, `### Rationale / Evidence`.
    - E3: Every `### Estimated Plan Count` is an integer in {3, 4, 5}.
    - E4: Every `### Success Criteria` section has 3–5 bulleted items.
    - E5: Every proposal's `Derived from:` line AND `### Rationale / Evidence` subsection cites at least one RESEARCH.md Row ID of the form `<SURFACE>-NN`. [D-18]
    - E6: Every proposal's `### Suggested Requirement ID` block contains EXACTLY ONE `PARITY-<SURFACE>-NN` ID (not multiple; the 2–4-per-proposal model is retired).
    - E7: Every proposal's Goal sentence 1 begins with a functional-capability verb (Add / Ship / Expose / Introduce / Support / Enable / Extend / Provide) — not "Research", "Investigate", "This proposal", or "The goal".

    **F. File separation (D-19):**
    - F1: 69-RESEARCH.md and 69-PROPOSALS.md are two distinct files in the phase directory. [D-19]

    **G. Anti-pattern guard (CONTEXT.md "Specific Ideas" + REQUIREMENTS.md Out of Scope):**
    - G1: No proposal in 69-PROPOSALS.md contains the phrase "adopt rmcp's" or "copy rmcp's" or proposes mimicking rmcp's trait-based architecture. If found, rewrite the Goal to express the fix in pmcp-native terms.
    - G2: No row in 69-RESEARCH.md frames rmcp negatively. If found, rewrite to factual neutrality per CONTEXT.md "Specific Ideas".

    **H. Cardinality check (D-15) + Row-ID bijection:**
    - H1: Proposal count in 69-PROPOSALS.md is 2–5 OR the document explicitly documents why it is outside that range.
    - H2: **Row-ID bijection (surjective onto proposals).** Build `S_research_high` = set of Row IDs in 69-RESEARCH.md with `Severity=High`. Build `S_cited` = set of `<SURFACE>-NN` tokens appearing anywhere in 69-PROPOSALS.md (on `Derived from:` lines or in `### Rationale / Evidence` subsections). Verify `S_research_high ⊆ S_cited` (every High Row ID is cited by at least one proposal) AND `S_cited ⊆ S_research_high` (no proposal cites a non-High Row ID). This replaces the old brittle `(surface, subtopic)` string comparison.

    After running all checks, write a `## Phase 69 Quality Gate Report` appendix to the bottom of 69-RESEARCH.md (NOT to PROPOSALS.md — the research doc is the reference) summarizing PASS/FAIL for each check A1 through H2 in a short table. If all PASS, the table is the final content of the document and Plan 03 can proceed to Task 2. The verify block below rejects any remaining FAIL rows in the report (the executor must fix inline before the report is finalized).
  </action>
  <verify>
    <automated>grep -q "^## Phase 69 Quality Gate Report$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && ! grep -qE "\| FAIL \|" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - A `## Phase 69 Quality Gate Report` section exists at the bottom of 69-RESEARCH.md.
    - The report table covers checks A1 through H2 (inclusive).
    - No check reports FAIL (all inline fixes applied, or the FAIL row is removed after the fix is verified).
    - All MUST surfaces (D-05..D-10) have populated tables with the minimum row counts from Plan 01.
    - Every proposal in 69-PROPOSALS.md is template-complete per D-18 and has exactly one PARITY-<SURFACE>-NN REQ-ID.
    - Every High Row ID in RESEARCH.md appears in at least one proposal's cited Row IDs (H2 bijection passes).
    - Baseline pinning (rmcp version, rmcp tag, pmcp SHA) is concrete and present in 69-RESEARCH.md.
    - Every pmcp evidence cell ends with a `[v2.3.0]` or `[main]` baseline tag (B4 passes).
    - ≥80% of rmcp evidence URLs are GitHub blob URLs with line fragments (C3 passes).
  </acceptance_criteria>
  <done>Both deliverables pass the full CONTEXT.md checklist; the quality-gate report is appended to 69-RESEARCH.md for auditability; the Row-ID bijection replaces the free-text string comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Land PARITY-* requirement IDs in REQUIREMENTS.md (1-to-1 with proposals, derived counts)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — in full, to locate the exact insertion point and existing formatting conventions. **Also read the Traceability table in full and count existing `- [ ]`/`- [x]` v2.1 checklist items under the `## v2.1 Requirements` H2 (or equivalent). These counts are used in step 4 to derive (not hardcode) the coverage math.**
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` — extract every `### Suggested Requirement ID` block verbatim. Collect `(ID, description, suggested-phase-number)` tuples. With the one-REQ-ID-per-proposal model, the number of tuples equals the proposal count.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` D-18 — confirms proposals are expected to seed new REQ-IDs.
  </read_first>
  <action>
    **Preamble step — capture pre-edit baseline (for zero-proposal delta check and coverage derivation).**

    Before mutating REQUIREMENTS.md, capture the pre-edit state into a scratch file so the verify block can compute deltas independent of absolute values:
    ```bash
    python3 -c "
    import re, json
    req = open('.planning/REQUIREMENTS.md').read()
    pre_parity_ids = len(re.findall(r'PARITY-(?:MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}', req))
    # Count v2.1 active checklist items under the relevant section — heuristic:
    # count '- [ ]' or '- [x]' items that appear between '## v2.1 Requirements' and '## Previous Requirements' (or end of file).
    m = re.search(r'^## v2\.1 Requirements[\s\S]*?(?=^## Previous Requirements|^## Traceability|\Z)', req, re.MULTILINE)
    active_section = m.group(0) if m else ''
    pre_v21_items = len(re.findall(r'^- \[[ x]\] \*\*', active_section, re.MULTILINE))
    import pathlib
    pathlib.Path('/tmp/gsd-69-req-baseline.json').write_text(json.dumps({'pre_parity_ids': pre_parity_ids, 'pre_v21_items': pre_v21_items}))
    print(f'Pre-edit baseline: parity_ids={pre_parity_ids}, v2.1 checklist items={pre_v21_items}')
    "
    ```

    **0. Read the proposal count from 69-PROPOSALS.md.** Grep for `^\*\*Proposal count:\*\* ([0-9]+)` in the PROPOSALS.md header (Plan 02's Task 1 step 4 writes this exact header). Capture the integer as `proposal_count`. With the one-REQ-ID-per-proposal model, the number of PARITY-* IDs to land equals `proposal_count` exactly.

    **Zero-proposal branch (reviewer L-1 fix).** If `proposal_count == 0` (valid per D-15 when zero High-severity gaps were found), skip steps 1–4 entirely. The `### rmcp Parity` subsection and traceability rows are NOT added. The expected post-edit count of `PARITY-<SURFACE>-NN` IDs in REQUIREMENTS.md is `pre_parity_ids` (unchanged), not `0` — this allows re-runs and future parity phases that may have already landed IDs. In the zero-proposal branch, still perform step 5 (update the `*Last updated:*` footer with the note: `2026-04-16 — Phase 69 rmcp parity research completed with zero High-severity gaps; no new PARITY-* IDs added`). Then exit Task 2.

    **Non-zero-proposal branch (proposal_count ≥ 1):** proceed with steps 1–5 below.

    1. **Locate the insertion point.** In REQUIREMENTS.md, find the line immediately after the last item of the "Code Mode Support" subsection (CMSUP-06) and immediately before the `## Previous Requirements` H2. Insert a new H3 subsection there.

    2. **Write the new subsection** using the exact shape from `<interfaces>`:

       ```markdown
       ### rmcp Parity (Phase 69 research — seeds follow-on phases)

       Seeded by Phase 69 research (`.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md`). One REQ-ID per proposal, mapping to the proposal as a whole; the proposal's 3–5 success criteria are its internal acceptance tests. Status remains pending until the follow-on phase ships.

       - [ ] **PARITY-<SURFACE>-NN**: [description verbatim]
       - [ ] **PARITY-<SURFACE>-NN**: [description verbatim]
       [...]
       ```

       Include every PARITY-* ID from every proposal in 69-PROPOSALS.md — one per proposal. Preserve the order: proposals in the order they appear in PROPOSALS.md.

    3. **Update the Traceability table** at the bottom of REQUIREMENTS.md. Append one row per PARITY-* ID:

       ```markdown
       | PARITY-<SURFACE>-NN | Phase [suggested-number from proposal, or TBD] | Pending |
       ```

       If the proposal's suggested phase number is `TBD — slotted by /gsd-add-phase`, use literal `TBD` in the Phase column.

    4. **Update the coverage counters** at the end of REQUIREMENTS.md (the `**Coverage:**` block) — **derive counts from file state, do NOT hardcode** (reviewer M-1 fix):
       - Re-read REQUIREMENTS.md AFTER writing the new subsection and traceability rows.
       - Compute `N_post = count of '- [ ]' / '- [x]' checklist items under the v2.1 active section` (same heuristic as preamble step).
       - Compute `K = proposal_count` (number of PARITY-* IDs just landed).
       - Compute `N_pre = N_post - K` (items present before this plan landed — derived, not fixed).
       - Write the coverage line as: `v2.1 requirements: N_post total (N_pre pre-seed + K seeded by Phase 69)`. This format replaces the old hardcoded `(14 original + 6 inserted via Phase 67.1 + K seeded by Phase 69)`, which would drift if any future phase inserts or removes items.
       - `Mapped to phases:` increment by K — all PARITY-* IDs map to future phases whose numbers are TBD but are recorded as "Phase [suggested]" in the traceability table.
       - `Unmapped:` stays 0 (PARITY-* IDs are mapped, even if the phase number is TBD).

    5. **Update the footer date** at the bottom of REQUIREMENTS.md:
       - Change `*Last updated:*` to `*Last updated: 2026-04-16 — added K PARITY-* IDs seeded by Phase 69 rmcp parity research*` (replace K with the actual count).

    **Post-edit verification helper:** also write the post-edit PARITY-ID count and v2.1 checklist count back to the baseline scratch file so the verify block can assert deltas precisely:
    ```bash
    python3 -c "
    import re, json
    req = open('.planning/REQUIREMENTS.md').read()
    post_parity_ids = len(re.findall(r'PARITY-(?:MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}', req))
    import pathlib, json as _json
    baseline_path = '/tmp/gsd-69-req-baseline.json'
    b = _json.loads(pathlib.Path(baseline_path).read_text())
    b['post_parity_ids'] = post_parity_ids
    pathlib.Path(baseline_path).write_text(_json.dumps(b))
    print(f'Post-edit: parity_ids={post_parity_ids} (delta from pre={post_parity_ids - b[\"pre_parity_ids\"]})')
    "
    ```
  </action>
  <verify>
    <automated>python3 -c "
import re, sys, json, os
prop_path = '.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md'
req_path = '.planning/REQUIREMENTS.md'
baseline_path = '/tmp/gsd-69-req-baseline.json'

if not os.path.exists(baseline_path):
    print(f'FAIL: baseline file {baseline_path} not found — preamble step must capture pre-edit state'); sys.exit(1)
baseline = json.loads(open(baseline_path).read())
pre_parity_ids = int(baseline['pre_parity_ids'])

prop_content = open(prop_path).read()
req_content = open(req_path).read()

m = re.search(r'^\*\*Proposal count:\*\* (\d+)', prop_content, re.MULTILINE)
if not m:
    print('FAIL: Proposal count not found in PROPOSALS.md header'); sys.exit(1)
proposal_count = int(m.group(1))

parity_ids_in_req = re.findall(r'PARITY-(?:MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}', req_content)
post_parity_ids = len(set(parity_ids_in_req))
delta = post_parity_ids - pre_parity_ids

if proposal_count == 0:
    # Zero-proposal branch: no NEW PARITY IDs landed since pre-edit baseline
    if delta != 0:
        print(f'FAIL: proposal_count == 0 but PARITY-ID count changed: pre={pre_parity_ids} post={post_parity_ids} delta={delta} (expected 0)'); sys.exit(1)
    if '### rmcp Parity' in req_content:
        print('FAIL: proposal_count == 0 but REQUIREMENTS.md has the rmcp Parity subsection'); sys.exit(1)
    if '2026-04-16 — Phase 69 rmcp parity research completed with zero High-severity gaps' not in req_content:
        print('FAIL: zero-proposal branch requires specific Last-updated annotation'); sys.exit(1)
    print(f'PASS: zero proposals, delta=0, baseline unchanged (pre={pre_parity_ids}, post={post_parity_ids})'); sys.exit(0)

# Non-zero branch: exactly proposal_count new distinct PARITY IDs landed (1-to-1 with proposals)
if '### rmcp Parity' not in req_content:
    print('FAIL: proposal_count >= 1 but REQUIREMENTS.md missing ### rmcp Parity subsection'); sys.exit(1)

# Count UNIQUE PARITY IDs in the new subsection only (not including Traceability duplicates)
section_match = re.search(r'### rmcp Parity \(Phase 69[\s\S]*?(?=^### |^## )', req_content, re.MULTILINE)
if not section_match:
    print('FAIL: could not extract rmcp Parity subsection'); sys.exit(1)
section_ids = set(re.findall(r'PARITY-(?:MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}', section_match.group(0)))
if len(section_ids) != proposal_count:
    print(f'FAIL: rmcp Parity subsection has {len(section_ids)} unique IDs, expected exactly {proposal_count} (1-to-1 with proposals)'); sys.exit(1)

# Delta check: post should be pre + proposal_count (exact match, not >= minimum)
if delta != proposal_count:
    print(f'FAIL: PARITY-ID delta wrong — pre={pre_parity_ids} post={post_parity_ids} delta={delta} expected={proposal_count} (1-to-1 with proposals)'); sys.exit(1)

if '2026-04-16 — added' not in req_content:
    print('FAIL: Last-updated footer missing PARITY annotation'); sys.exit(1)

# Coverage math derivation check: the v2.1 coverage line must not contain the old hardcoded formula
if '(14 original + 6 inserted via Phase 67.1' in req_content:
    print('FAIL: REQUIREMENTS.md still contains hardcoded coverage formula (14 + 6 + K); must derive counts from file state'); sys.exit(1)

print(f'PASS: {proposal_count} new PARITY IDs landed 1-to-1 with proposals (pre={pre_parity_ids}, post={post_parity_ids}, delta={delta})')
"</automated>
  </verify>
  <acceptance_criteria>
    - If proposal_count >= 1: REQUIREMENTS.md contains a new `### rmcp Parity (Phase 69 research — seeds follow-on phases)` subsection.
    - If proposal_count >= 1: EXACTLY `proposal_count` unique PARITY-* IDs are in the new subsection (1-to-1 with proposals).
    - If proposal_count >= 1: every new PARITY-* ID also appears in the Traceability table with a phase column entry.
    - If proposal_count >= 1: the `**Coverage:**` counter line has the form `v2.1 requirements: {N_post} total ({N_pre} pre-seed + {K} seeded by Phase 69)` with counts DERIVED from file state, not hardcoded.
    - If proposal_count == 0: REQUIREMENTS.md is otherwise unchanged; only the `*Last updated:*` footer is updated with the zero-gap annotation; the pre-edit PARITY-ID count is preserved.
    - The `*Last updated:*` footer is dated 2026-04-16.
    - No PARITY-* ID is duplicated in the new subsection.
  </acceptance_criteria>
  <done>Every PARITY-* ID from 69-PROPOSALS.md is landed in REQUIREMENTS.md 1-to-1 with proposals; coverage counters are derived from file state; zero-proposal branch preserves pre-edit PARITY-ID count.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and PROJECT.md to reflect Phase 69 completion (including reconciled focus fields)</name>
  <files>
    .planning/STATE.md
    .planning/PROJECT.md
  </files>
  <read_first>
    - `.planning/STATE.md` — in full, to locate the exact insertion points for the new v2.1 decisions bullet and the Session Continuity block, AND to read the current stale values of `**Current focus:**` (in the plain-text body, likely line ~22) and the `## Current Position` block (Phase and Plan fields, likely lines ~25–28) so step 3 can perform a read-then-assert-delta replacement.
    - `.planning/PROJECT.md` — check whether a "Key Decisions" table exists. If not, this task skips PROJECT.md editing per the `<interfaces>` spec.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` — scan the `## Executive Summary` to extract the integer counts (N rows total, M High-severity).
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` — scan the header to extract the K proposal count.
  </read_first>
  <action>
    **STATE.md edits:**

    1. **Decisions bullet:** In `## Accumulated Context` → `### Decisions` → "v2.1 decisions:" bullet list, append a new final bullet:
       ```markdown
       - [Phase 69]: rmcp parity research complete — 69-RESEARCH.md (gap matrix, N rows total, M High-severity) + 69-PROPOSALS.md (K proposals). K PARITY-* requirement IDs landed in REQUIREMENTS.md (one per proposal); follow-on phases not yet scheduled.
       ```
       Replace N, M, K with the integers read from the deliverables.

    2. **Session Continuity block:** Replace the existing `Last session:` / `Stopped at:` / `Resume:` values with:
       - `Last session: 2026-04-16T<current-ISO-timestamp>` (use the current timestamp).
       - `Stopped at: Phase 69 complete — follow-on proposals ready for ROADMAP slotting`
       - `Resume: Run /gsd:add-phase to slot a 69-PROPOSALS.md entry into the roadmap, or /gsd-plan-phase for a scheduled v2.1 phase.`

    3. **Reconcile stale focus fields** (reviewer M-5). Two distinct locations in the STATE.md body:

       (a) **Current focus line** (in the `## Project Reference` section, plain-text body):
         - Read the current value of the `**Current focus:**` line.
         - Expected-pre-edit value: `**Current focus:** Phase 65 — examples-cleanup-protocol-accuracy` (or similar earlier-phase focus).
         - Expected-post-edit value: `**Current focus:** Phase 69 — rmcp parity research (complete); follow-on proposals pending ROADMAP slotting`.
         - Apply the replacement. If the pre-edit value is already the post-edit value (idempotent re-run), skip the write.
         - If the pre-edit value differs from BOTH the expected-pre-edit and expected-post-edit values (i.e., some other phase was landed between plans), replace anyway with the expected-post-edit value but note the unexpected pre-state in the task output.

       (b) **Current Position block** (H2 section `## Current Position` with `Phase:` and `Plan:` fields):
         - Read the current values.
         - Expected-pre-edit values: `Phase: 67.2` / `Plan: Not started` (or similar stale state).
         - Expected-post-edit values: `Phase: 69` / `Plan: 03 (complete)`.
         - Apply the replacement with the same read-then-assert-delta idempotency guard.

    4. **Frontmatter counters (read-then-mutate-then-assert pattern):** In the YAML frontmatter at the top of STATE.md:
       - **Before editing, capture the pre-edit baseline.** Grep the current values of `completed_phases:` and `completed_plans:` from STATE.md and store them as `pre_phases` and `pre_plans` in `/tmp/gsd-69-state-baseline.json` (scratch file the verify block re-reads).
       - **Apply the edits.** Set `completed_phases` to `pre_phases + 1` (Phase 69 completion) and `completed_plans` to `pre_plans + 3` (Plans 69-01, 69-02, 69-03). Do NOT hardcode absolute values.
       - Recompute `percent` using `(completed_phases / total_phases) * 100` rounded to integer.
       - Update `last_updated:` to the current ISO timestamp.
       - Update `stopped_at:` to match the Session Continuity value.
       - Leave `status:` and `milestone:` fields as-is. Do NOT assert a specific milestone state here — read STATE.md at execution time and preserve whatever milestone status is current (reviewer L-3: the old comment "v2.0 is already Milestone complete" may not match the actual file; Plan 03 does not depend on a specific milestone state and must not override it).

    **PROJECT.md edits (conditional):**

    5. Check whether PROJECT.md contains a section heading like `## Key Decisions` or `### Key Decisions` with a markdown table. If it does:
       - Append one row:
         ```markdown
         | 2026-04-16 | Phase 69 rmcp ergonomics parity research | K proposals seeded; follow-on phases to slot into late v2.1 / v2.2 | .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md |
         ```
       - Replace K with the actual proposal count.
       - Match the column count and ordering of the existing table header — if the existing table has different columns (e.g., "Decision | Impact | Status"), adapt the row shape to match.

       If PROJECT.md does NOT have a Key Decisions table, write nothing to PROJECT.md (the edit is conditional). Log the skip in the task output.

    **Verification pass:** After edits, re-read STATE.md and confirm:
    - The frontmatter `completed_phases` increment is reflected.
    - The new v2.1 decisions bullet is present.
    - The Session Continuity values are updated.
    - The `Current focus:` line points at Phase 69.
    - The `Current Position` block shows Phase 69 / Plan 03.
  </action>
  <verify>
    <automated>grep -q "\[Phase 69\]: rmcp parity research complete" .planning/STATE.md && grep -q "^Stopped at: Phase 69 complete" .planning/STATE.md && grep -q "^Resume: Run /gsd:add-phase" .planning/STATE.md && grep -q "^\*\*Current focus:\*\* Phase 69" .planning/STATE.md && python3 -c "
import re, json, os, sys
baseline_path = '/tmp/gsd-69-state-baseline.json'
if not os.path.exists(baseline_path):
    print(f'FAIL: pre-edit baseline file {baseline_path} not found — Task 3 action step 4 must capture pre_phases/pre_plans before mutating STATE.md')
    sys.exit(1)
with open(baseline_path) as f:
    baseline = json.load(f)
pre_phases = int(baseline['pre_phases'])
pre_plans = int(baseline['pre_plans'])
content = open('.planning/STATE.md').read()
m_phases = re.search(r'^completed_phases: (\d+)', content, re.MULTILINE)
m_plans = re.search(r'^completed_plans: (\d+)', content, re.MULTILINE)
if not m_phases or not m_plans:
    print('FAIL: completed_phases or completed_plans not found in STATE.md frontmatter'); sys.exit(1)
post_phases = int(m_phases.group(1))
post_plans = int(m_plans.group(1))
expected_phases = pre_phases + 1
expected_plans = pre_plans + 3
if post_phases != expected_phases:
    print(f'FAIL: completed_phases delta wrong — pre={pre_phases} post={post_phases} expected={expected_phases} (pre+1)'); sys.exit(1)
if post_plans != expected_plans:
    print(f'FAIL: completed_plans delta wrong — pre={pre_plans} post={post_plans} expected={expected_plans} (pre+3)'); sys.exit(1)
# Current Position reconciliation check (M-5)
current_position_match = re.search(r'^## Current Position\s*\n\s*\nPhase: (\S+)\s*\nPlan: (.+)$', content, re.MULTILINE)
if not current_position_match:
    print('FAIL: could not locate or parse ## Current Position block'); sys.exit(1)
cp_phase, cp_plan = current_position_match.group(1), current_position_match.group(2)
if not cp_phase.startswith('69'):
    print(f'FAIL: Current Position phase is {cp_phase!r}, expected starts with \"69\"'); sys.exit(1)
if '03' not in cp_plan:
    print(f'FAIL: Current Position plan is {cp_plan!r}, expected to reference plan 03'); sys.exit(1)
print(f'PASS: completed_phases {pre_phases} -> {post_phases} (+1), completed_plans {pre_plans} -> {post_plans} (+3), Current Position={cp_phase}/{cp_plan}')
"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter `completed_phases` is incremented by exactly 1 from the pre-edit baseline.
    - STATE.md frontmatter `completed_plans` is incremented by exactly 3 from the pre-edit baseline.
    - STATE.md frontmatter `last_updated` is a fresh ISO timestamp.
    - STATE.md frontmatter `milestone` and `status` fields are NOT force-overwritten (preserved as-is from the pre-edit file).
    - STATE.md `### Decisions` "v2.1 decisions" block contains a new bullet starting with `- [Phase 69]: rmcp parity research complete`.
    - STATE.md Session Continuity block's `Stopped at:` and `Resume:` are updated per the action spec.
    - STATE.md body's `**Current focus:**` line is updated to reference Phase 69.
    - STATE.md `## Current Position` block shows `Phase: 69` and `Plan: 03 (complete)`.
    - PROJECT.md is either updated (if Key Decisions table exists) OR explicitly unchanged (documented in the task output).
    - No other sections of STATE.md or PROJECT.md are disturbed.
  </acceptance_criteria>
  <done>Phase 69 completion is reflected in STATE.md (counters, decisions bullet, session continuity, and reconciled Current focus + Current Position fields) and PROJECT.md if applicable; downstream consumers reading STATE.md see a consistent post-Phase-69 state.</done>
</task>

</tasks>

<threat_model>
Documentation-only phase. No code, config, or API surface changes. Threats considered: (1) inaccurate evidence citations leading downstream phases astray — mitigated by the Task 1 quality-gate sweep that verifies every citation passes the file:line / URL-with-anchor standard and the B4 baseline-tag check; (2) biased gap severity scoring leading to poor prioritization — mitigated by Task 1 check D2 which requires every High row's Gap cell to articulate a specific action verb, and by the D-15 cardinality re-check; (3) row-identity drift between matrix and proposals — mitigated by Row-ID-based bijection (H2) using stable `<SURFACE>-NN` IDs rather than free-text subtopic comparison; (4) stale STATE.md counters or focus fields drift from reality — mitigated by Task 3 read-then-assert-delta pattern for counters AND explicit reconciliation of `Current focus:` and `Current Position` fields; (5) REQUIREMENTS.md coverage formula drift — mitigated by deriving counts from file state in Task 2 step 4 rather than hardcoding a fixed formula.
</threat_model>

<verification>
Grep-verifiable post-conditions:
- `grep -q "^## Phase 69 Quality Gate Report$" 69-RESEARCH.md`
- `! grep -qE "\| FAIL \|" 69-RESEARCH.md` (no FAIL row remaining in the report table)
- `grep -q "^### rmcp Parity" .planning/REQUIREMENTS.md` (only when proposal_count >= 1)
- `grep -cE "PARITY-(MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}" .planning/REQUIREMENTS.md` delta from pre-edit equals proposal_count exactly (verified in Task 2's Python verify block)
- `grep -q "\[Phase 69\]: rmcp parity research complete" .planning/STATE.md`
- `grep -q "^Stopped at: Phase 69 complete" .planning/STATE.md`
- `grep -q "^\*\*Current focus:\*\* Phase 69" .planning/STATE.md`
- STATE.md `## Current Position` block has `Phase: 69` and a Plan line referencing `03`.
</verification>

<success_criteria>
- Both deliverables pass the full CONTEXT.md MUST checklist (D-05..D-19) via the Task 1 report.
- Row-ID bijection (H2) passes: every High Row ID in 69-RESEARCH.md is cited by at least one proposal in 69-PROPOSALS.md, and no proposal cites a non-High Row ID.
- Every PARITY-* ID from 69-PROPOSALS.md (one per proposal) is landed in REQUIREMENTS.md with full traceability; coverage counters are derived from file state, not hardcoded.
- STATE.md reflects Phase 69 completion: frontmatter counters, decisions bullet list, session continuity, AND reconciled `Current focus:` / `Current Position` fields.
- PROJECT.md is updated if a Key Decisions table exists; otherwise explicitly skipped.
- Phase 69 deliverables are discoverable from STATE.md → RESEARCH.md / PROPOSALS.md → REQUIREMENTS.md PARITY-* IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-03-SUMMARY.md` summarizing: quality-gate results (all PASS, or list any inline fixes applied), count of PARITY-* IDs landed in REQUIREMENTS.md (= proposal_count), STATE.md counter deltas and focus-field reconciliation, and a one-line handoff ("Phase 69 complete; 69-PROPOSALS.md ready for /gsd:add-phase consumption").
</output>
</output>
