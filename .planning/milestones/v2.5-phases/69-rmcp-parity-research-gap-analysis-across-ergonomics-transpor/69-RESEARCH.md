# Phase 69: rmcp Parity Research — Ergonomics Gap Matrix

**Researched:** 2026-04-16
**rmcp version pinned:** 1.5.0
**rmcp source tag:** https://github.com/modelcontextprotocol/rust-sdk/releases/tag/rmcp-v1.5.0
**pmcp baseline:** v2.3.0 + current `feat/sql-code-mode` main at commit dbaee6cc
**Surfaces covered:** tool macros, builder APIs, typed wrappers, handler signatures + state/extra injection, client-side ergonomics, error types + result wrappers
**Surfaces excluded:** Tower / service integration (Phase 56), transport construction API (deferred per D-12)

### Source file decisions

- `src/client/core.rs`: not found at HEAD dbaee6cc; the pmcp client module is flat at `src/client/mod.rs` (client implementation) with submodules `auth.rs`, `http_middleware.rs`, `oauth.rs`, `transport/`. All client-side citations use `src/client/mod.rs`.
- `examples/09_client_with_tool.rs`: not found at HEAD dbaee6cc; the examples directory uses the Phase 65 role-prefix convention. Substituted `examples/c02_client_tools.rs` (client role, tool discovery and invocation — the closest functional equivalent).
- `src/error.rs`: not found at HEAD dbaee6cc; the error module is a directory `src/error/` containing `mod.rs` and `recovery.rs`. All error-surface citations use `src/error/mod.rs`.
- `src/types/error.rs`: not present — pmcp does not have a separate `types::error` module; the single error type lives in `src/error/mod.rs` with the `Result<T>` alias defined at line 12 and re-exported from `src/lib.rs:55` (`pub use error::{Error, ErrorCode, Result};`).

## Executive Summary

The matrix contains 32 rows across 6 surfaces: **4 High-severity gaps**, **7 Medium-severity gaps**, and **21 Low-severity rows** (including Parity rows and Strength-to-preserve rows where pmcp already matches or exceeds rmcp).

**High-severity rows:** MACRO-02 (tool-description rustdoc fallback), HANDLER-02 (missing request-scoped `Extensions` typemap on `RequestHandlerExtra`), HANDLER-05 (missing peer handle in `RequestHandlerExtra` for server-to-client RPCs from inside handlers), CLIENT-02 (typed `call_tool_typed` and `list_all_*` auto-pagination convenience methods). These are the four gaps that affect the typical first-hour user path AND admit a clean articulable fix.

**Gap pressure concentrates on Handler Signatures + State/Extra Injection and Client-Side Ergonomics** — together those two surfaces account for 3 of the 4 High-severity findings, suggesting Plan 02 should prioritize follow-on phase proposals targeting the `RequestHandlerExtra` extension surface and the client typed-request/auto-pagination helpers. Tool Macros contributes the fourth High (MACRO-02) and is a narrower, single-fix proposal.

**pmcp strengths identified for preservation:** `ServerCoreBuilder` fluent builder (BUILDER-01), `State<T>` + impl-block dual state-injection paths (MACRO-05 / HANDLER-04), `trybuild` compile-fail test discipline in pmcp-macros (MACRO-06), the `#[mcp_resource]` macro with URI-template matching (MACRO-07), `Client::new` one-line client construction (CLIENT-01), task-aware `ToolCallResponse` enum + `call_tool_and_poll` (CLIENT-05), the crate-root `Result<T>` alias (ERR-04), and the `Other(#[from] anyhow::Error)` zero-friction `?`-operator variant (ERR-03). Future proposals must not regress any of these.

Each High-severity gap is proposed as a follow-on phase in `69-PROPOSALS.md` (Plan 02 output), cited by Row ID.

## Tool Macros

Covers `#[mcp_tool]`, `#[mcp_server]`, `#[mcp_prompt]`, and `#[mcp_resource]` attribute macros in pmcp-macros 0.5.0 versus the rmcp `#[tool]`/`#[tool_router]`/`#[tool_handler]` macro family in `rmcp-macros` 1.5.0. Evaluates attribute richness, async support, schema derivation, state injection, and compile-fail test discipline.

| Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
|--------|------------------|---------------|---------------|---------------|---------------|-----|----------|
| MACRO-01 | Primary tool macro entry point | Attribute macro `#[tool]` applied to inherent-impl methods; companion `#[tool_router]` on the impl block and `#[tool_handler]` on `impl ServerHandler for T` wire the router into the server. A single "tools-only" shorthand flag `server_handler` on `#[tool_router]` elides the separate `#[tool_handler]` block. | [rmcp-macros/src/lib.rs#L37-L44](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L37), [rmcp-macros/src/lib.rs#L126-L133](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L126), [rmcp-macros/src/lib.rs#L194-L201](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L194) | Attribute macro `#[mcp_tool]` applied to a standalone `fn`/`async fn` that generates a dedicated `{Name}Tool` struct implementing `ToolHandler` plus a constructor `fn name() -> {Name}Tool`. No separate router/handler macros — register with `ServerCoreBuilder::tool("name", name())`. An optional `#[mcp_server]` macro aggregates methods from an `impl` block into one `McpServer` registration. | `pmcp-macros/src/lib.rs:84` [v2.3.0], `pmcp-macros/src/mcp_tool.rs:67` [v2.3.0], `pmcp-macros/README.md:23` [v2.3.0] | Parity | Low |
| MACRO-02 | Tool description source | `description = "..."` attribute is optional; when omitted the macro harvests the function's rustdoc comment as the description. | [rmcp-macros/src/lib.rs#L19-L22](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L19) (attribute table documents: "A description of the tool. The document of this function will be used.") | `description = "..."` attribute is mandatory and enforced at compile time; there is no rustdoc-harvesting fallback. Errors with "mcp_tool requires at least `description = \"...\"` attribute". | `pmcp-macros/src/mcp_tool.rs:72-75` [v2.3.0] | pmcp requires explicit `description`; rmcp derives from rustdoc when omitted — pmcp users writing rustdoc must also repeat the description in the attribute. Clean fix: accept rustdoc fallback when `description` is absent and fail only if neither is present. | High |
| MACRO-03 | Async vs sync function support | Both async and sync methods are supported on the same impl via `#[tool]`; the macro adapts the method shape to the `SyncTool` or `AsyncTool` trait implementation. | [crates/rmcp/src/handler/server/tool.rs#L4-L34](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server/tool.rs#L4) (module doctest shows `fn add(...)` sync tool used identically to async tools) | `#[mcp_tool]` supports both `async fn` and `fn`; a sync tool is wrapped into a `TypedSyncTool` that adapts to the async `ToolHandler` trait on the dispatch side. | `src/server/typed_tool.rs:279` [v2.3.0], `pmcp-macros/src/lib.rs:82` [v2.3.0] | Parity | Low |
| MACRO-04 | Schema derivation mechanism | Schema is generated from `Parameters<T>` where `T: JsonSchema` (via `schemars`); an optional `input_schema = <expr>` attribute can override with a hand-written schema expression. | [rmcp-macros/src/lib.rs#L20-L23](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L20), [crates/rmcp/src/handler/server/wrapper/parameters.rs#L1-L45](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L20) | Schema is generated from the plain arg type `T: JsonSchema` directly (no `Parameters<T>` wrapper needed); override uses `new_with_schema` on the typed tool rather than a macro attribute. | `src/server/typed_tool.rs:67-72` [v2.3.0] (`generate_schema::<T>()` under `#[cfg(feature = "schema-generation")]`) | pmcp lacks a macro-attribute-level schema override (`input_schema = ...`). Not a blocker — users can fall back to `TypedTool::new_with_schema` — but the macro doesn't expose this. Clean fix: add optional `schema = <expr>` attribute to `#[mcp_tool]`. | Medium |
| MACRO-05 | State injection mechanism | Tools live on an `impl MyServer` block; methods access shared state via `&self`/`&mut self` on the implementing type. No extractor pattern — the impl receiver IS the state. | [crates/rmcp/src/handler/server/tool.rs#L15-L33](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L23) (docstring example: `#[tool_router] impl Server { #[tool] fn add(&self, ...) }`) | Standalone functions use an explicit `State<T>` extractor parameter (Axum-style). `State<T>` wraps `Arc<T>`, auto-derefs, and is provided at registration time via `.with_state(...)`. Compile-time check: at most one `State<T>` parameter per tool; missing state triggers a compile-time panic with a clear fix. | `src/server/state.rs:28` [v2.3.0], `pmcp-macros/src/mcp_tool.rs:124` [v2.3.0], `pmcp-macros/src/mcp_tool.rs:202` [v2.3.0] | Strength to preserve (pmcp offers standalone-fn ergonomics with explicit `State<T>` extractor AND impl-block state-via-self through `#[mcp_server]` — both paths). rmcp is impl-block-only. | Low |
| MACRO-06 | Compile-fail test discipline | rmcp-macros ships a `src/`-only crate with no `tests/ui/` compile-fail test harness; the `rmcp-macros/Cargo.toml` manifest shows no `trybuild` dev-dep and no `tests/` directory for macro rejection cases. | [crates/rmcp-macros/Cargo.toml](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/Cargo.toml) (no `trybuild` entry under `[dev-dependencies]`; no `tests/ui/` subdirectory) | pmcp-macros ships `tests/ui/` with `trybuild` compile-fail snapshots for rejection cases (missing description, multiple args) across `mcp_tool` and `mcp_prompt`. | `pmcp-macros/tests/ui/mcp_tool_missing_description.rs:1` [v2.3.0], `pmcp-macros/tests/ui/mcp_tool_multiple_args.rs:1` [v2.3.0], `pmcp-macros/tests/ui/mcp_prompt_missing_description.rs:1` [v2.3.0] | Strength to preserve (pmcp locks rejection messages against regression via `trybuild` snapshots; rmcp relies on hand-maintained error messages with no snapshot coverage). | Low |
| MACRO-07 | Prompt / resource / server macro coverage | rmcp-macros provides `#[prompt]`, `#[prompt_router]`, `#[prompt_handler]`, and `#[task_handler]`; resource macros are NOT shipped (resources remain manually implemented). | [rmcp-macros/src/lib.rs#L223-L230](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L223), [rmcp-macros/src/lib.rs#L310-L314](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L310) | pmcp-macros provides `#[mcp_tool]`, `#[mcp_prompt]`, `#[mcp_resource]`, and `#[mcp_server]`. Resource macro supports URI-template matching and parameter extraction. | `pmcp-macros/src/lib.rs:84` [v2.3.0], `pmcp-macros/src/mcp_prompt.rs:1` [v2.3.0], `pmcp-macros/src/mcp_resource.rs:1` [v2.3.0], `pmcp-macros/src/mcp_server.rs:1` [v2.3.0] | Strength to preserve (pmcp ships a first-class `#[mcp_resource]` with URI template matching; rmcp requires hand-rolled resource handlers). | Low |

## Builder APIs

Covers server construction entrypoints: pmcp's `ServerCoreBuilder` fluent builder versus rmcp's `ServiceExt::serve` combined with `ServerHandler` trait implementation. Evaluates fluent builder shape, tool/prompt/resource registration, and capabilities declaration.

| Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
|--------|------------------|---------------|---------------|---------------|---------------|-----|----------|
| BUILDER-01 | Primary construction entrypoint | Users define a struct that implements the `ServerHandler` trait (typically via `#[tool_router]` + `#[tool_handler]` macros), then call `ServiceExt::serve(transport)` to start. There is no named fluent builder type; configuration is declared via macro attributes and trait-default-method overrides. | [crates/rmcp/src/service.rs#L161-L188](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L161), [crates/rmcp/src/handler/server.rs#L375-L381](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server.rs#L375) | `ServerCoreBuilder::new()` returns a fluent builder. Chain `.name()`, `.version()`, `.tool()`, `.prompt()`, `.resources()`, `.capabilities()`, then `.build()` to produce a `ServerCore`. No trait implementation required for the common path. | `src/server/builder.rs:56` [v2.3.0], `src/server/builder.rs:100` [v2.3.0], `src/server/builder.rs:854` [v2.3.0] | Strength to preserve (pmcp offers a discoverable fluent builder with typed field setters; rmcp requires users to internalize a trait-impl mental model). | Low |
| BUILDER-02 | Tool registration shape | Tool registration is implicit via the `#[tool_router]`/`#[tool]` macros on an impl block; the generated `tool_router()` method is invoked internally by `#[tool_handler]`. Users never call an explicit `.tool(...)` registrar on the server. | [crates/rmcp-macros/src/lib.rs#L127-L133](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L127) (the `tool_router` macro "creates a function that returns a `ToolRouter` instance"), [crates/rmcp/src/handler/server/tool.rs#L15-L32](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server/tool.rs#L15) | Explicit registration: `.tool("name", handler)` takes any `ToolHandler + 'static`. Tools registered via the builder are cached in a `HashMap<String, Arc<dyn ToolHandler>>` with metadata computed at registration time (avoids per-request cloning). Capabilities auto-enable when the first tool is registered. | `src/server/builder.rs:168` [v2.3.0], `src/server/builder.rs:60-65` [v2.3.0], `src/server/builder.rs:182-186` [v2.3.0] | Parity (both approaches work; rmcp is terser for pure-impl-block servers, pmcp is more explicit and supports mixed-source tools — neither is strictly better for typical users). | Low |
| BUILDER-03 | Prompt registration shape | `#[prompt]`/`#[prompt_router]`/`#[prompt_handler]` macros mirror the tool story; registration is via impl-block method annotation and the router is retrieved internally. | [crates/rmcp-macros/src/lib.rs#L224-L230](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L224), [crates/rmcp-macros/src/lib.rs#L262-L268](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L262) | Explicit `.prompt("name", handler)` on the builder, symmetric with `.tool()`. | `src/server/builder.rs:218` [v2.3.0], `src/server/builder.rs:233-237` [v2.3.0] | Parity | Low |
| BUILDER-04 | Capabilities declaration | Capabilities are declared via the `get_info()` trait method (default impl on `ServerHandler`) or via the `#[tool_handler]` macro which auto-generates a default `get_info()` that enables the tools capability. Explicit declaration uses `ServerCapabilities::builder().enable_tools().build()`. | [crates/rmcp/src/handler/server.rs#L145-L160](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server.rs#L145), [crates/rmcp-macros/src/lib.rs#L193-L220](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L193) (docstring describes auto-generated `get_info` with tools capability enabled) | `.capabilities(ServerCapabilities::tools_only())` is explicit on the builder; additionally, registering a tool/prompt auto-enables the matching capability flag on `self.capabilities` so correct behavior happens without the explicit call. | `src/server/builder.rs:160` [v2.3.0], `src/server/builder.rs:181-186` [v2.3.0] | Parity — both auto-enable capabilities in the typical path. Slight pmcp strength: the explicit `.capabilities(...)` setter exists as an escape hatch; rmcp requires overriding a trait method. | Low |
| BUILDER-05 | Type-state / build-time validation | Build-time validation is embedded in the trait-impl compile errors; there is no fluent-builder "missing field" `build()` error. Users get a compile error if the `ServerHandler` methods are implemented incorrectly. | [crates/rmcp/src/service.rs#L178-L188](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L178) (trait-bound errors at `serve` call site) | `ServerCoreBuilder::build()` returns `Result<ServerCore>`, validating at runtime that required fields (name, version) are present. Missing fields return an `Error::Validation` with a specific message rather than silently defaulting. | `src/server/builder.rs:854` [v2.3.0] | pmcp uses runtime validation (`build() -> Result<ServerCore>`) rather than type-state guardrails (`FinalizedBuilder` phantom-state). rmcp compile errors catch trait mismatches but not missing metadata. Neither SDK uses a full type-state builder. Clean fix on pmcp side: introduce phantom-generic type-state so `.build()` is only callable after `.name()` and `.version()` have been set. | Medium |

## Typed Tool / Prompt Wrappers

Covers pmcp's `TypedTool`, `TypedToolWithOutput`, `TypedPrompt`, `TypedSyncTool`, and `WasmTypedTool` wrappers versus rmcp's equivalent patterns (typically generated by the `#[tool]` macro). Evaluates input-type derivation, output-schema support, sync vs async variants, and wasm variant availability.

| Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
|--------|------------------|---------------|---------------|---------------|---------------|-----|----------|
| TYPED-01 | Input-type derivation | Input type is extracted via the `Parameters<T>` wrapper (a transparent `#[serde(transparent)] struct Parameters<P>(pub P)`); `T: JsonSchema` derives the input schema automatically. Users write `fn my_tool(Parameters(args): Parameters<MyArgs>)`. | [crates/rmcp/src/handler/server/wrapper/parameters.rs#L43-L48](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server/wrapper/parameters.rs#L43) | `TypedTool::new(name, handler)` takes `handler: Fn(T, RequestHandlerExtra) -> Future<Output = Result<Value>>` where `T: DeserializeOwned + JsonSchema`. No wrapper struct; the type parameter `T` appears directly in the fn signature. Schema is generated under `#[cfg(feature = "schema-generation")]` via `generate_schema::<T>()`. | `src/server/typed_tool.rs:25-40` [v2.3.0], `src/server/typed_tool.rs:67-83` [v2.3.0] | Parity (both derive schema from `JsonSchema`). Stylistic difference: rmcp's `Parameters<T>` wrapper is visible in handler signatures; pmcp's typed wrappers pass `T` directly. Neither is strictly better — the pattern-destructure `Parameters(args)` of rmcp is one extra token vs pmcp's direct binding. | Low |
| TYPED-02 | Output-type typing + schema | `Json<T>` wrapper struct types the return value and triggers output schema derivation via `schema_for_output::<T>()`. `#[tool]` auto-detects `Json<T>` / `Result<Json<T>, E>` return types and emits the output schema. | [crates/rmcp/src/handler/server/wrapper.rs#L1-L4](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server/wrapper.rs#L1), [crates/rmcp-macros/src/tool.rs#L10-L43](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/tool.rs#L10) (detects `Json<T>` inner type and generates `schema_for_output::<T>()`) | `TypedToolWithOutput<TIn, TOut, F>` is a dedicated struct parameterized over both input and output types; it stores an optional `output_schema: Option<Value>` alongside the input schema. Output schema reflects the MCP 2025-11-25 `outputSchema` top-level field and serializes to `structuredContent`. | `src/server/typed_tool.rs:519-536` [v2.3.0] | Parity. Semantic difference worth noting: rmcp's macro auto-detects `Json<T>` returns to wire output schema; pmcp requires choosing `TypedToolWithOutput` explicitly or using the `#[mcp_tool]` macro which derives it from the return type. No user-facing ergonomics gap. | Low |
| TYPED-03 | Sync vs async tool variants | `SyncTool` and `AsyncTool` are separate traits (`ToolBase` common base); the `#[tool]` macro picks the right one based on whether the fn is `async`. `SyncTool::invoke(&Self, P) -> Result<O, E>` runs synchronously. | [crates/rmcp/src/handler/server/tool.rs#L46-L80](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server/tool.rs#L46) (module doctest shows `impl SyncTool<MyServer> for ComplexTool1`) | `TypedSyncTool<T, F>` is a parallel struct to `TypedTool` for `Fn(T, RequestHandlerExtra) -> Result<Value>` (non-async). Both adapt to the async `ToolHandler` trait at dispatch. | `src/server/typed_tool.rs:279` [v2.3.0] | Parity | Low |
| TYPED-04 | WASM variant availability | rmcp supports WASM via the `local` feature flag (single-threaded, `!Send` futures). There is no separate WASM-only typed-tool type; the same `ToolRouter` works with `local` feature. | [crates/rmcp/src/service.rs#L720-L740](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L720) (`MaybeSendFuture` trait-bound conditional on `local` feature), [crates/rmcp-macros/Cargo.toml](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/Cargo.toml) (`local` feature flag on rmcp-macros) | pmcp ships `WasmTypedTool<T, F>` — a dedicated WASM-compatible wrapper with `Fn(T) -> Result<Value>` (sync-only due to WASM async-in-browser constraints). Output typing is explicitly NOT supported in the WASM path (documented gap in module rustdoc). | `src/server/wasm_typed_tool.rs:22-34` [v2.3.0], `src/server/wasm_typed_tool.rs:1-9` [v2.3.0] (module docstring documents the constraint) | rmcp uses a single unified path for native and WASM (via `local` feature flag on `MaybeSendFuture`); pmcp splits into `TypedTool` (native async) vs `WasmTypedTool` (WASM sync, input-only). pmcp's split is explicit about WASM's limitations but forces users to pick the right struct. Clean fix: unify under one generic with cfg-gated impls, matching rmcp's single-path model, OR ship `WasmTypedToolWithOutput` to close the output-schema gap on the WASM path. | Medium |
| TYPED-05 | Typed prompt wrapper | rmcp's `#[prompt]` macro supports typed prompt arguments via the same `Parameters<T>` wrapper; the prompt handler fn takes `Parameters<MyPromptArgs>` and the schema derives from `T: JsonSchema`. | [crates/rmcp-macros/src/lib.rs#L224-L257](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp-macros/src/lib.rs#L224) | `TypedPrompt<T, F>` wraps `Fn(T, RequestHandlerExtra) -> Future<GetPromptResult>`. Deserialization is constrained: MCP prompt args are transmitted as `HashMap<String, String>` on the wire, so `T`'s fields must be `String` or `Option<String>` (documented in the module rustdoc — non-string fields will fail deserialization). | `src/server/typed_prompt.rs:63-76` [v2.3.0], `src/server/typed_prompt.rs:28-38` [v2.3.0] (module rustdoc documents the string-only constraint) | pmcp's `TypedPrompt` has a documented string-only constraint on field types. rmcp's prompt typing inherits the same `Parameters<T>`/`JsonSchema` path and has the same wire-protocol limitation (prompt args are strings in MCP). Both SDKs share the constraint; pmcp documents it explicitly. Parity. | Low |

## Handler Signatures + State / Extra Injection

Covers the async handler function shape, `RequestHandlerExtra` (pmcp) versus `RequestContext` (rmcp), cancellation token access, and state injection patterns (`Arc<State>` composition). Evaluates how handlers receive protocol metadata and application state.

| Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
|--------|------------------|---------------|---------------|---------------|---------------|-----|----------|
| HANDLER-01 | Handler fn shape (return type) | `ServerHandler::call_tool` returns `impl Future<Output = Result<CallToolResult, McpError>> + MaybeSendFuture + '_`; return-position `impl Trait` rather than `async fn` trait method. `MaybeSendFuture` is `Send`-bound in the default build and `?Send` with the `local` feature. | [crates/rmcp/src/handler/server.rs#L261-L266](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server.rs#L261) | `ToolHandler::call_tool(&self, req) -> Result<CallToolResult>` is an `async fn` trait method powered by `#[async_trait]`. Uses the SDK's `Error` type directly; no dual-dispatch `MaybeSendFuture` indirection. | `src/server/traits.rs:25-36` [v2.3.0] | Parity (both work). Stylistic: rmcp avoids the `async-trait` macro and uses RTN (return-type notation) via `impl Future`, giving zero-allocation futures; pmcp's `async_trait` boxes the future. This is a perf delta at scale but not an ergonomics gap for typical users. | Low |
| HANDLER-02 | Request metadata surface | `RequestContext<RoleServer>` is non-exhaustive and carries `ct: CancellationToken`, `id: RequestId`, `meta: Meta`, `extensions: Extensions`, `peer: Peer<R>`. The `extensions` field gives untyped key-value access to request-scoped data; `peer` gives a handle to call the client back. | [crates/rmcp/src/service.rs#L651-L665](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L651) | `RequestHandlerExtra` carries `cancellation_token`, `request_id: String`, `session_id: Option<String>`, `auth_info: Option<AuthInfo>`, and `auth_context: Option<AuthContext>` (non-wasm only). Shape is fixed struct fields, not a bag of extensions. | `src/shared/cancellation.rs:38-51` [v2.3.0], `src/server/cancellation.rs:1-13` [v2.3.0] | pmcp lacks a general-purpose `Extensions` bag for request-scoped state (rmcp's `context.extensions` is an `http::Extensions`-style typemap for cross-middleware state transfer). pmcp has specific typed fields (auth, session) but no extension mechanism for user-defined per-request data. Clean fix: add a typemap `extensions` field to `RequestHandlerExtra`, guarded by a feature flag for non-breaking rollout. | High |
| HANDLER-03 | Cancellation access | `context.ct` is a `tokio_util::sync::CancellationToken` directly on the context struct; handlers check via `context.ct.is_cancelled()` or await `context.ct.cancelled()`. | [crates/rmcp/src/service.rs#L654-L655](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L654) | `RequestHandlerExtra::cancellation_token` is a runtime-agnostic `CancellationToken` (shared module uses a simple `AtomicBool`-backed token; non-wasm server uses `tokio_util::sync::CancellationToken`). Both support `is_cancelled()`. `RequestHandlerExtra::is_cancelled()` is provided as a convenience. | `src/shared/cancellation.rs:11-33` [v2.3.0], `src/shared/cancellation.rs:94-97` [v2.3.0], `src/server/cancellation.rs:11-13` [v2.3.0] | pmcp's runtime-agnostic token (WASM-friendly `AtomicBool`) is a strength but lacks `await`-on-cancellation for tasks that want to race against cancellation. rmcp's native `tokio_util` token is richer on non-wasm. Neither is strictly better but pmcp could expose a `cancelled().await` method on the shared token. Medium severity: real but not in the first-hour user path. | Medium |
| HANDLER-04 | State injection pattern (standalone fn) | No standalone-fn state injection pattern — state flows through the `&self` receiver on the impl block. Shared state is a struct field (`Arc<T>` typically) on the `ServerHandler`-implementing type. | [crates/rmcp/src/handler/server/tool.rs#L24-L33](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server/tool.rs#L24) (module doctest shows `struct Server` with `&self` methods accessing fields) | Two patterns: (a) standalone `async fn` with `State<T>` extractor parameter wired at registration via `.with_state(...)`; (b) impl-block methods via `#[mcp_server]` macro using `&self`. Pattern (a) is Axum-like; pattern (b) matches rmcp. | `src/server/state.rs:9-28` [v2.3.0], `pmcp-macros/src/mcp_tool.rs:124` [v2.3.0] (compile-time enforcement of at most one `State<T>`) | Strength to preserve (pmcp offers both standalone-fn + impl-block; rmcp is impl-only). See MACRO-05 for the same finding on the macro side. | Low |
| HANDLER-05 | Peer/remote-call access from handler | `context.peer: Peer<R>` inside every handler gives direct access to call back to the client (e.g., `context.peer.list_roots()`, `context.peer.create_message(params)`). Richly typed — `Peer<RoleServer>` exposes server-side callbacks, `Peer<RoleClient>` exposes client-side ones. | [crates/rmcp/src/service.rs#L382-L390](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L382), [crates/rmcp/src/service.rs#L660](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L660) | `RequestHandlerExtra` does NOT carry a back-channel to the client. Handlers that need to sample the LLM or list roots go through separate SDK paths (e.g., `ServerCoreBuilder::sampling(handler)` at registration time). No direct peer handle from inside a tool handler. | `src/shared/cancellation.rs:38-51` [v2.3.0] (RequestHandlerExtra fields — no Peer field) | pmcp lacks a peer handle in `RequestHandlerExtra`, so tools cannot initiate server-to-client RPCs from within the handler body. This is a typical-path gap for tools that need LLM sampling mid-execution. Clean fix: add a `peer: Option<Arc<dyn Peer>>` field to `RequestHandlerExtra` exposing `sample`, `list_roots`, `progress_notify`. | High |

## Client-Side Ergonomics

Covers building an MCP client: connection/construction entrypoint, request-building ergonomics, notification handling, and progress tracking. Evaluates the surface users exercise when writing custom MCP clients.

| Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
|--------|------------------|---------------|---------------|---------------|---------------|-----|----------|
| CLIENT-01 | Construction entrypoint | Users define a struct that implements `ClientHandler` (default impl suffices for a passive client), then call `().serve(transport)` via `ServiceExt::serve` — the unit type `()` is a valid `ClientHandler`. Returns a `RunningService<RoleClient, _>` whose `.peer()` exposes `Peer<RoleClient>` for outbound calls. | [crates/rmcp/src/handler/client.rs#L1-L14](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/client.rs#L1), [crates/rmcp/src/service.rs#L161-L178](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L161) | `Client::new(transport)` or `Client::with_info(transport, info)` constructs the client directly with concrete types. `initialize()` runs the handshake; method calls on the client are plain `async fn`s (no `Peer` indirection). | `src/client/mod.rs:100-105` [v2.3.0], `src/client/mod.rs:121-135` [v2.3.0], `src/client/mod.rs:210-212` [v2.3.0] | Strength to preserve (pmcp's `Client::new` is a one-line construction; rmcp requires knowing about `ServiceExt`, `ClientHandler`, and `RunningService`). rmcp is more uniform (client/server share `Service`) but higher learning curve for client-only users. | Low |
| CLIENT-02 | Request-building ergonomics | Request methods are method-macro-generated on `Peer<RoleClient>` (e.g., `call_tool`, `list_tools`, `subscribe`). Each method takes strongly-typed params (e.g., `CallToolRequestParams`) rather than builder args. Convenience `list_all_*` wrappers handle pagination. | [crates/rmcp/src/service/client.rs#L355-L400](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service/client.rs#L355), [crates/rmcp/src/service/client.rs#L378-L395](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service/client.rs#L378) (`list_all_tools` auto-paginates) | `Client::call_tool(name: String, arguments: Value)` takes untyped-at-compile-time `serde_json::Value` for arguments. `Client::list_tools(cursor)` is manual-pagination. No `list_all_tools` convenience; users must loop on the cursor themselves. | `src/client/mod.rs:416-441` [v2.3.0], `src/client/mod.rs:339-340` [v2.3.0], `examples/c02_client_tools.rs:35` [v2.3.0] | pmcp's `Client::call_tool` takes `serde_json::Value` for arguments rather than a typed `CallToolRequestParams`. pmcp also lacks `list_all_*` auto-pagination convenience methods. Clean fix: (a) add typed `call_tool_typed<T: Serialize>(name, args: T)` that serializes internally; (b) add `list_all_tools`, `list_all_prompts`, `list_all_resources` helpers that internally loop on cursor. | High |
| CLIENT-03 | Notification handling (inbound) | `ClientHandler` trait has typed `on_cancelled`, `on_progress`, `on_logging_message`, `on_resource_updated`, `on_tool_list_changed` callbacks with default no-op impls. Users override exactly what they need. | [crates/rmcp/src/handler/client.rs#L46-L73](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/client.rs#L46) | pmcp's `Client` exposes a single `notification_tx: Option<mpsc::Sender<Notification>>` channel — users subscribe once and match on the `Notification` enum themselves. No per-notification-type callback trait. | `src/client/mod.rs:69` [v2.3.0] (notification_tx field), `src/client/mod.rs:62-71` [v2.3.0] (Client struct fields) | pmcp's channel-based notification surface is less typed than rmcp's per-notification trait methods. Users must match on `Notification` variants manually. Clean fix: add a `ClientNotificationHandler` trait with per-variant default impls, wired via a new `Client::with_notification_handler(h)` constructor option. | Medium |
| CLIENT-04 | Progress tracking | `Peer::send_request_with_option` automatically attaches a `progress_token` (via `AtomicU32ProgressTokenProvider`) to every outbound request and returns a `RequestHandle` carrying the token. `ProgressDispatcher` on the client side routes incoming progress notifications by token to per-request subscribers. | [crates/rmcp/src/service.rs#L457-L490](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/service.rs#L457), [crates/rmcp/src/handler/client/progress.rs#L1-L40](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/client/progress.rs#L1) | pmcp's server-side `ProgressReporter` trait handles outbound progress from tools; client-side progress tracking is manual — the user receives `ProgressNotification` variants through the `notification_tx` channel and correlates them against tokens they generated themselves. No client-side `ProgressDispatcher` equivalent. | `src/server/progress.rs:17-37` [v2.3.0] (server-side trait), `src/client/mod.rs:17-18` [v2.3.0] (`ProgressNotification` imported but no dispatcher in client struct) | pmcp lacks a client-side per-request progress subscriber (rmcp's `ProgressDispatcher` lets callers `subscribe(token).next().await` to receive progress for a specific request). Medium-severity: not every client cares about progress, but the absence forces users to reimplement the dispatch when they do. | Medium |
| CLIENT-05 | Task-aware tool calls (MCP 2025-11-25) | rmcp supports task-based tool invocation via `CallToolRequestParams.task` field; the server routes to `enqueue_task` or `call_tool` depending on `TaskSupport` declaration. Clients opt in by setting the `task` field. | [crates/rmcp/src/handler/server.rs#L68-L101](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server.rs#L68) | pmcp's `Client::call_tool_with_task(name, args)` returns `ToolCallResponse::Task(task) | Result(res)` — typed variant return that captures both sync and async completion paths in one API call. `call_tool_and_poll` convenience method wraps the full task poll loop. | `src/client/mod.rs:463-467` [v2.3.0], `src/client/mod.rs:620` [v2.3.0], `src/client/mod.rs:49-56` [v2.3.0] (ToolCallResponse enum) | Strength to preserve (pmcp's `ToolCallResponse` enum + `call_tool_and_poll` convenience give task-aware clients a one-call UX; rmcp's `CallToolRequestParams.task` is lower-level). | Low |

## Error Types + Result Wrappers

Covers top-level error enum shape, `thiserror` usage, `?`-operator friction (From impls), and `Result<T>` alias convention. Evaluates error ergonomics when authoring tools, prompts, and handlers.

| Row ID | Surface subtopic | rmcp approach | rmcp evidence | pmcp approach | pmcp evidence | Gap | Severity |
|--------|------------------|---------------|---------------|---------------|---------------|-----|----------|
| ERR-01 | Top-level error enum shape | Two error types: (a) `ErrorData` (formerly `Error`, now deprecated alias) — the JSON-RPC-shape error with `code`, `message`, `data`; (b) `RmcpError` — the service-layer umbrella using `thiserror::Error` derive, with variants `Service`, `ClientInitialize`, `ServerInitialize`, `Runtime` (tokio JoinError), `TransportCreation`, `TaskError`. Marked `#[non_exhaustive]`. | [crates/rmcp/src/error.rs#L1-L50](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/error.rs#L1), [crates/rmcp/src/error.rs#L21-L50](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/error.rs#L21) | Single `Error` enum with `#[derive(thiserror::Error)]`; variants `Protocol { code, message, data }`, `Transport(TransportError)`, `Serialization(serde_json::Error)`, `Validation(String)`, `Authentication(String)`, `Timeout(u64)`, `UnsupportedCapability`, `Internal`, `NotFound`, `InvalidState`, `Cancelled`, `RateLimited`, `CircuitBreakerOpen`, `Other(anyhow::Error)`. Not marked `#[non_exhaustive]`. | `src/error/mod.rs:15-79` [v2.3.0] | pmcp's `Error` is not `#[non_exhaustive]`, meaning adding a new variant is a breaking change. rmcp's `RmcpError` is `#[non_exhaustive]`, reserving room to grow. Clean fix: add `#[non_exhaustive]` to `Error` as part of the next major bump (v3.0), document it as a compatibility reservation. | Medium |
| ERR-02 | `thiserror` usage discipline | Uses `thiserror::Error` derive with `#[from]` attributes for automatic From impls (`#[from] crate::ServiceError`, `#[from] tokio::task::JoinError`). Each variant has a `#[error("...")]` format string. | [crates/rmcp/src/error.rs#L21-L50](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/error.rs#L21) | Uses `thiserror::Error` derive with `#[from]` attributes for `TransportError`, `serde_json::Error`, and `anyhow::Error`. Each variant has a `#[error("...")]` format string. | `src/error/mod.rs:15` [v2.3.0], `src/error/mod.rs:29-34` [v2.3.0], `src/error/mod.rs:77-79` [v2.3.0] | Parity | Low |
| ERR-03 | `?`-operator friction (From impls) | `From<ServiceError>`, `From<JoinError>`, and bespoke constructor (`RmcpError::transport_creation::<T>()`) cover common failure paths. No blanket `From<String>` or `From<&str>` — ad-hoc string errors require `RmcpError::TaskError(msg)`. | [crates/rmcp/src/error.rs#L52-L65](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/error.rs#L52) | `From<TransportError>`, `From<serde_json::Error>`, `From<anyhow::Error>` via `#[from]`. Also has a final `Other(#[from] anyhow::Error)` variant that swallows any `std::error::Error`-implementing type via `anyhow::Error`. Effectively zero-friction `?` against arbitrary error types. | `src/error/mod.rs:29-34` [v2.3.0], `src/error/mod.rs:77-79` [v2.3.0] (`Other(#[from] anyhow::Error)`) | Strength to preserve (pmcp's `Other(#[from] anyhow::Error)` variant gives `?`-friendly behavior against arbitrary error types; rmcp users must convert manually into a specific variant). | Low |
| ERR-04 | Result alias convention | No `Result<T>` alias at the crate root; handlers return `Result<T, ErrorData>` or `Result<T, RmcpError>` explicitly. Specific types per call site. | [crates/rmcp/src/lib.rs#L1-L45](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/lib.rs#L1) (no `pub type Result` export; each module carries its own `Result<T, E>` spellings) | Crate-root `pub type Result<T> = std::result::Result<T, Error>` defined in `src/error/mod.rs:12` and re-exported from `src/lib.rs:55`. Handlers return `pmcp::Result<T>` without naming the error type. | `src/error/mod.rs:12` [v2.3.0], `src/lib.rs:55` [v2.3.0] | Strength to preserve (pmcp's short `Result<T>` alias reduces boilerplate; rmcp requires spelling the error type at every call site). | Low |
| ERR-05 | Error-code constants discipline | `ErrorCode` constants are scattered across the `model` module (e.g., `ErrorCode::METHOD_NOT_FOUND`, `ErrorCode::INVALID_PARAMS`). Presented as associated consts on the `ErrorCode` type imported from `crate::model`. | [crates/rmcp/src/handler/server.rs#L74-L81](https://github.com/modelcontextprotocol/rust-sdk/blob/rmcp-v1.5.0/crates/rmcp/src/handler/server.rs#L74) (use site of `ErrorCode::METHOD_NOT_FOUND`) | Centralized `ErrorCode` newtype struct with 11 associated consts covering both JSON-RPC standard codes (`PARSE_ERROR`, `INVALID_REQUEST`, etc.) and pmcp-specific codes (`REQUEST_TIMEOUT`, `RATE_LIMITED`, `CIRCUIT_BREAKER_OPEN`). `ErrorCode::other(code)` for custom codes. | `src/error/mod.rs:82-118` [v2.3.0] | Parity (both SDKs expose named constants; shape differs — rmcp uses a model-level type, pmcp exposes from `crate::error`). Neither is a user-facing ergonomics gap. | Low |

## Methodology Notes

**Research approach:** Source and docs reading only (per D-03). No hands-on "build a tiny example in both SDKs" — the depth-to-cost ratio was judged insufficient to triple research time. All rmcp evidence comes from reading the `modelcontextprotocol/rust-sdk` source tree at the pinned git tag, rmcp-macros crate docstrings and attribute tables, and rustdoc where a single source line was not cleanly attributable. All pmcp evidence comes from reading local source files in the `rust-mcp-sdk` checkout at the pinned HEAD commit.

**Evidence standard:**
- pmcp column: `path/to/file.rs:LINENO` (or `path:LINE-RANGE`) followed by a baseline tag — either `[v2.3.0]` (the API was present in the published v2.3.0 release) or `[main]` (post-v2.3.0 main-only, present on the `feat/sql-code-mode` branch at commit dbaee6cc but not yet shipped to crates.io). All pmcp citations in this document end with `[v2.3.0]` — no `[main]` citations were required, which indicates the compared surface is stable at the v2.3.0 release.
- rmcp column: GitHub blob URL with `#L<N>` line fragment pinned to the `rmcp-v1.5.0` tag, targeting `crates/rmcp/src/...` or `crates/rmcp-macros/src/...` workspace paths. The 80/20 acceptance bar (≥80% blob URLs, ≤20% docs.rs anchors) was met — 100% of rmcp evidence citations in this document are GitHub blob URLs.

**Row ID convention:** `<SURFACE>-NN` where `<SURFACE>` is one of `MACRO` / `BUILDER` / `TYPED` / `HANDLER` / `CLIENT` / `ERR` (matching the H2 section prefix) and `NN` is a two-digit zero-padded counter starting at `01` per surface, assigned in document order. IDs are persistent once written — if a row is deleted in a later revision, its ID is retired rather than reused, and the deletion should be noted here rather than fixed by renumbering. Plan 02 proposals cite these Row IDs verbatim in their `### Rationale` subsection; Plan 03's quality gate compares High-severity Row IDs against proposal citations for bijection.

**Severity rubric (D-14, verbatim):**
- **High:** affects the typical first-hour user path AND a clean specific fix can be articulated.
- **Medium:** affects less common paths, or has no single clean fix.
- **Low:** cosmetic or theoretical. "Parity" rows and "Strength to preserve" rows are always Low.

**Out-of-scope reminder:** Tower / service integration (D-11) was handled in Phase 56 and is NOT compared here. Transport construction API shape (D-12) is deferred — the matrix did not surface it as a High-severity ergonomics gap, so it remains outside Phase 69's proposal scope. If a future hands-on spike reveals a High-severity transport-construction ergonomics issue, it can enter the pipeline via `69-PROPOSALS.md` amendment or a separate phase proposal.

**Reproducibility note:** Running this research against a different rmcp version or a different pmcp commit will produce different line numbers, and potentially different gap severities as either SDK evolves. The pinned versions in the document header (rmcp 1.5.0 at tag `rmcp-v1.5.0`; pmcp v2.3.0 + main at commit `dbaee6cc`) are the canonical baseline for this document. Plan 02 proposals derived from this matrix remain valid as long as the cited Row IDs still map to the gaps described; if rmcp publishes a new version that closes a High-severity gap on its side, the corresponding pmcp proposal should be re-evaluated rather than auto-executed.

## Phase 69 Quality Gate Report

Generated 2026-04-16 by Plan 03 Task 1. Cross-checks `69-RESEARCH.md` and `69-PROPOSALS.md` against every CONTEXT.md MUST (D-05..D-19) and evidence standard. Row-ID bijection (H2) uses the corrected regex per the Plan 02 `**Validated:**` footer (accounts for trailing `|` delimiter on markdown pipe-table rows).

| Check | Category | Result | Detail |
|-------|----------|--------|--------|
| A1 | MUST surfaces (D-05) | PASS | 7 MACRO-NN rows (threshold ≥5) |
| A2 | MUST surfaces (D-06) | PASS | 5 BUILDER-NN rows (threshold ≥4) |
| A3 | MUST surfaces (D-07) | PASS | 5 TYPED-NN rows (threshold ≥4) |
| A4 | MUST surfaces (D-08) | PASS | 5 HANDLER-NN rows (threshold ≥4) |
| A5 | INCLUDE surfaces (D-09) | PASS | 5 CLIENT-NN rows (threshold ≥4) |
| A6 | INCLUDE surfaces (D-10) | PASS | 5 ERR-NN rows (threshold ≥4) |
| B1 | Baseline pinning (D-01) | PASS | rmcp version `1.5.0` pinned in header |
| B2 | Baseline pinning (D-01) | PASS | rmcp source tag URL `github.com/modelcontextprotocol/rust-sdk/releases/tag/rmcp-v1.5.0` pinned |
| B3 | Baseline pinning (D-04) | PASS | pmcp baseline SHA `dbaee6cc` pinned |
| B4 | Baseline tags (reviewer M-2) | PASS | 71 `[v2.3.0]`/`[main]` tags over 69 `.rs:NN` citations → ratio 1.03 (≥0.80) |
| B5 | No unreleased rmcp (D-02) | PASS | No rows rely on unreleased rmcp work for severity scoring |
| C1 | file:line citations (D-03) | PASS | 69 `.rs:N` citations across all surfaces (≥25 threshold) |
| C2 | rmcp URL anchors (D-03) | PASS | 45 GitHub blob URLs with `#L<n>` fragments |
| C3 | Evidence ratio (reviewer M-3) | PASS | 45/45 blob URLs (ratio 1.00; ≥0.80) |
| C4 | Row-level evidence (reviewer M-3) | PASS | 32 rows; spot-check confirms both evidence columns populated |
| D1 | Severity rubric (D-14) | PASS | 32 Severity cells, all ∈ {High, Medium, Low} |
| D2 | High-gap action verbs (D-14) | PASS | 4 High rows (MACRO-02, HANDLER-02, HANDLER-05, CLIENT-02); Gap cells articulate clean fixes |
| D3 | Parity/Strength = Low (D-14) | PASS | 0 Parity/Strength rows with non-Low severity |
| D4 | Row IDs unique & sequential | PASS | 32 IDs, all unique; counters sequential per surface |
| E1 | Proposal count header (D-17) | PASS | Declared count 3 == actual 3 |
| E2 | Template completeness (D-18) | PASS | All 3 proposals have Goal / Scope / Success Criteria / Suggested Requirement ID / Estimated Plan Count / Rationale / Evidence |
| E3 | Plan counts in {3,4,5} (D-17) | PASS | Plan counts: 4, 3, 3 |
| E4 | Success-Criteria bullet counts | PASS | All proposals have 3–5 bulleted success criteria (observed: 5, 5, 5) |
| E5 | Derived-from cites Row IDs (D-18) | PASS | Every proposal's `Derived from:` line AND Rationale subsection cite ≥1 `<SURFACE>-NN` ID |
| E6 | One PARITY ID per proposal (D-18) | PASS | Each proposal has EXACTLY ONE `PARITY-<SURFACE>-NN` ID in its `### Suggested Requirement ID` block |
| E7 | Functional-capability verbs (D-18) | PASS | Goal sentence-1 verbs: Extend (P1), Add (P2), Enable (P3) |
| F1 | Two distinct files (D-19) | PASS | `69-RESEARCH.md` and `69-PROPOSALS.md` both present in phase directory |
| G1 | No rmcp-mimicry framing | PASS | Proposal bodies contain no "adopt rmcp's" / "copy rmcp's" / "mimick" phrasing (Validated footer meta-reference excluded from scan per intent) |
| G2 | No negative rmcp framing | PASS | Matrix frames rmcp as "what rmcp does better" / "strength to preserve" per CONTEXT.md "Specific Ideas" rule |
| H1 | Proposal cardinality (D-15) | PASS | 3 proposals (within expected 2–5 range) |
| H2 | Row-ID bijection | PASS | S_research_high = {MACRO-02, HANDLER-02, HANDLER-05, CLIENT-02}; S_cited (Derived-from + Rationale) = {MACRO-02, HANDLER-02, HANDLER-05, CLIENT-02}; surjective ∧ injective |

**Summary:** 31/31 checks PASS. Both deliverables satisfy the full CONTEXT.md MUST checklist (D-05..D-19). Row-ID bijection (H2) verified with the corrected regex from Plan 02's `**Validated:**` footer — all 4 High-severity Row IDs in `69-RESEARCH.md` are cited by at least one proposal's `Derived from:` line or `### Rationale / Evidence` subsection, and no proposal cites a non-High Row ID in those contexts (Medium-severity Row IDs like CLIENT-03, CLIENT-04, MACRO-04 appear only in Out-of-scope bullets as explicit exclusions, consistent with D-16).
