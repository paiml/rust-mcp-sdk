---
phase: 85-shape-a-pure-config-binary-reference-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-server-toolkit/src/config.rs
  - crates/pmcp-server-toolkit/src/code_mode.rs
  - crates/pmcp-server-toolkit/tests/config_superset.rs
  - crates/pmcp-server-toolkit/tests/env_expansion.rs
  - crates/pmcp-server-toolkit/tests/fixtures/reference-config.toml
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "The SQLite Chinook reference config.toml parses cleanly into ServerConfig (today it FAILS on file_path)"
    - "All four reference configs (open-images, imdb, msr-vtt, Chinook) parse + validate"
    - "Athena configs with ${VAR} in output_location parse (stored verbatim — SC-2 parse-only not broken)"
    - "Renames are still rejected — deny_unknown_fields is NOT loosened"
    - "${VAR} token-secret syntax expands from the named env var"
    - "A missing/empty ${VAR} env var produces a clean error, never a panic"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/config.rs"
      provides: "Additive superset fields file_path / is_reference / [shared_policy_store]"
      contains: "file_path"
    - path: "crates/pmcp-server-toolkit/tests/config_superset.rs"
      provides: "REF-01 regression gate covering all 4 configs + ${VAR}-literal parse"
    - path: "crates/pmcp-server-toolkit/tests/env_expansion.rs"
      provides: "${VAR} expansion unit + proptest no-panic invariant"
    - path: "crates/pmcp-server-toolkit/tests/fixtures/reference-config.toml"
      provides: "Vendored Chinook reference config snapshot"
  key_links:
    - from: "config.rs DatabaseSection"
      to: "reference-config.toml [database] file_path"
      via: "serde(default) Option<String> field"
      pattern: "file_path"
    - from: "code_mode.rs resolve_token_secret"
      to: "${CODE_MODE_SECRET} env var"
      via: "${VAR} expansion branch alongside env: prefix"
      pattern: "strip_prefix"
---

<objective>
Close RESEARCH Gap #1 (REF-01 superset) and Gap #3 (`${VAR}` expansion) in the toolkit so the SQLite Chinook reference config parses. Today `ServerConfig::from_toml` FAILS on the Chinook config — `[database] file_path`, `[server] is_reference`, and the `[shared_policy_store]` section are unknown fields under `#[serde(deny_unknown_fields)]`. All four reference configs use `token_secret = "${CODE_MODE_SECRET}"`, which the toolkit rejects (only `env:VAR` is accepted today).

Purpose: This is the foundation gate — Wave 2's binary cannot build a server from the reference config until these additive fields land and `${VAR}` resolves. REF-01's "superset, no renames" invariant is the load-bearing constraint.
Output: Additive config fields + `${VAR}` expansion + two regression tests + vendored Chinook fixture.

**REVIEW FIX (Codex MEDIUM #6 — `${VAR}` expansion scope; justified token-secret-only for Phase 85).** The Athena reference configs ALSO use `${...}` outside `token_secret` — `output_location = "s3://...-${AWS_ACCOUNT_ID}-${AWS_REGION}/..."` (VERIFIED: open-images/imdb/msr-vtt config.toml line 30). This does NOT require general config-load-time expansion in Phase 85, because: (1) `output_location` / `region` / `workgroup` are stored as plain `Option<String>` fields — the parser keeps the `${...}` text VERBATIM as a literal string, so SC-2 *parse-only* succeeds with no expansion (Task 1 adds an explicit test proving this). (2) The only `${VAR}` that needs *resolution* in Phase 85's automated scope is `token_secret`, consumed whenever a `[code_mode]` server is built — including the Chinook parity target. (3) Athena `output_location`/region resolution only matters for LIVE query execution, which is out of automated scope per D-02 (parse-only + lazy startup; no cloud creds in CI). A general `ServerConfig::from_toml_with_env_expansion` raw-TOML pre-pass is a deferred forward-compatible enhancement, NOT a Phase 85 blocker — recorded here so the verifier treats token-secret-only `${VAR}` as an approved scope reading, not a gap.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-CONTEXT.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-RESEARCH.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-PATTERNS.md

<interfaces>
From crates/pmcp-server-toolkit/src/config.rs (the file being modified — insertion points):
```rust
#[serde(deny_unknown_fields)]
pub struct ServerConfig {
    #[serde(default)] pub server: ServerSection,      // add is_reference here
    #[serde(default)] pub metadata: MetadataSection,
    #[serde(default)] pub database: DatabaseSection,  // add file_path here
    #[serde(default)] pub code_mode: Option<CodeModeSection>,
    #[serde(default)] pub tools: Vec<ToolDecl>,
    #[serde(default)] pub prompts: Vec<PromptDecl>,
    #[serde(default)] pub resources: Vec<ResourceDecl>,
    // ADD: #[serde(default)] pub shared_policy_store: Option<SharedPolicyStoreSection>,
}

pub struct DatabaseSection {
    #[serde(default, rename = "type")] pub backend_type: Option<String>,
    #[serde(default)] pub database: Option<String>,
    // ... output_location, workgroup, query_timeout_ms, tables ...
    #[serde(default)] pub url: Option<String>,        // ADD file_path after this
    #[serde(default)] pub pool: Option<DatabasePoolSection>,
}

pub struct ServerSection {
    #[serde(default)] pub id: Option<String>,
    #[serde(default)] pub name: String,
    #[serde(default)] pub description: Option<String>,
    #[serde(default, rename = "type")] pub server_type: Option<String>,
    #[serde(default)] pub version: String,            // ADD is_reference after this
}
```

From crates/pmcp-server-toolkit/src/code_mode.rs (resolve_token_secret — the `${VAR}` seam):
```rust
fn resolve_token_secret(section: &CodeModeSection) -> Result<SecretValue> {
    let raw = section.token_secret.as_ref().ok_or_else(/*...*/)?;
    if let Some(var) = raw.strip_prefix("env:") {
        return std::env::var(var).map(/*...*/).map_err(/*...*/);
    }
    // ADD a ${VAR} expansion branch here (coexists with env:VAR)
    if section.allow_inline_token_secret_for_dev { /* ... */ }
    Err(ToolkitError::Validation(ConfigValidationError::InlineSecretRejected))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add REF-01 superset fields (file_path / is_reference / [shared_policy_store]) additively</name>
  <files>crates/pmcp-server-toolkit/src/config.rs, crates/pmcp-server-toolkit/tests/fixtures/reference-config.toml, crates/pmcp-server-toolkit/tests/config_superset.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/config.rs (lines 99-132 ServerConfig, 231-252 ServerSection, 291-323 DatabaseSection, module doc 1-52 — the "Always ADD the missing field, never loosen deny_unknown_fields" mandate at line 24)
    - crates/pmcp-server-toolkit/tests/reference_configs.rs (the existing 3-config parse-probe test this extends; lines 19-54)
    - /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/config.toml (the Chinook reference — the exact fields to support: [server] is_reference, [database] file_path + [database.pool], [shared_policy_store] creates_shared_store/export_to_ssm/ssm_path/templates array, [metadata], [code_mode] + [code_mode.limits])
    - /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/servers/open-images/config.toml (CONFIRM the `output_location = "s3://...-${AWS_ACCOUNT_ID}-${AWS_REGION}/..."` line 30 — this ${VAR}-in-string must PARSE verbatim, no expansion at parse time)
  </read_first>
  <behavior>
    - Test: Chinook reference-config.toml parses + validates via ServerConfig::from_toml_strict_validated (today FAILS on `unknown field 'file_path'`)
    - Test: cfg.database.backend_type == Some("sqlite") and cfg.database.file_path == Some("/var/task/assets/chinook.db")
    - Test: the existing 3 Athena/MySQL configs still parse + validate (additive fields cannot break them)
    - Test (REVIEW FIX #6): the Athena open-images config parses AND `cfg.database.output_location` retains the verbatim `${AWS_ACCOUNT_ID}`/`${AWS_REGION}` substring — proving ${VAR}-in-non-secret-fields does NOT break SC-2 parse-only (no expansion attempted at config-load)
    - Test: a config with a RENAMED field (e.g. `[database] filepath` typo) is REJECTED — deny_unknown_fields stays strict
  </behavior>
  <action>
    Vendor the Chinook reference config to `crates/pmcp-server-toolkit/tests/fixtures/reference-config.toml` (copy verbatim from `pmcp-run/built-in/sql-api/reference/config.toml`). Add three additive fields to config.rs, each `#[serde(default)]`, mirroring the existing `pub url: Option<String>` pattern (config.rs:311-318): (1) `pub file_path: Option<String>` on DatabaseSection after `url`; (2) `pub is_reference: bool` on ServerSection after `version`; (3) `pub shared_policy_store: Option<SharedPolicyStoreSection>` on ServerConfig after `resources`. Define a new `#[serde(deny_unknown_fields)] pub struct SharedPolicyStoreSection` with `#[serde(default)]` fields: `creates_shared_store: bool`, `export_to_ssm: bool`, `ssm_path: Option<String>`, `templates: Vec<String>` (the reference config carries a templates array — see read_first). Do NOT loosen deny_unknown_fields anywhere (per D-05 redaction guarantee + config.rs:24). Update the config.rs module-doc section list (lines 43-52) to mention the new sections. Create `tests/config_superset.rs` modeled on `reference_configs.rs:19-54` with one test per config (the 3 existing + the NEW Chinook test asserting file_path/backend_type), a `renames_rejected` negative test (a config with `filepath` typo → `from_toml` returns Err), AND the REVIEW-FIX #6 `var_in_output_location_parses_verbatim` test (assert the open-images config parses and `output_location` still contains `${AWS_ACCOUNT_ID}` — confirming SC-2 parse-only is unaffected by token-secret-only `${VAR}` expansion). This implements REF-01 SC-2.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --test config_superset -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-server-toolkit --test config_superset` passes with the Chinook test green (was impossible before — config did not parse)
    - Source assertion: `grep -n "pub file_path" crates/pmcp-server-toolkit/src/config.rs` matches; `grep -n "pub is_reference" ...` matches; `grep -c "deny_unknown_fields" crates/pmcp-server-toolkit/src/config.rs` is >= the pre-change count (no removal)
    - `cargo test -p pmcp-server-toolkit --test reference_configs` (the original 3-config test) still passes
    - Negative test `renames_rejected` confirms a typo'd field name errors
    - REVIEW-FIX #6: `var_in_output_location_parses_verbatim` green — the Athena config parses with `${VAR}` kept literal in output_location
  </acceptance_criteria>
  <done>The Chinook reference config parses + validates; the 3 prior configs still pass; ${VAR}-in-output_location parses verbatim (SC-2 intact); renames still rejected; SharedPolicyStoreSection modeled additively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ${VAR} env-expansion for token_secret (coexists with env:VAR)</name>
  <files>crates/pmcp-server-toolkit/src/code_mode.rs, crates/pmcp-server-toolkit/tests/env_expansion.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/code_mode.rs (resolve_token_secret lines 284-309 — the function being modified; note the existing `env:` prefix branch and the InlineSecretRejected fallback)
    - crates/pmcp-server-toolkit/tests/code_mode_wiring.rs (lines 44-184 — existing R9 inline-secret + pipeline policy tests; the env_expansion test mirrors this style and MUST NOT regress R9)
    - 85-RESEARCH.md Pitfall 3 + Security Domain (V6: min secret length is HmacTokenGenerator::MIN_SECRET_LEN = 16 bytes; missing var must error cleanly never panic — this is a threat-model item)
  </read_first>
  <behavior>
    - Test: token_secret = "${MY_SECRET}" with MY_SECRET set to a >=16-byte value resolves to that value
    - Test: token_secret = "${MISSING_VAR}" with the var unset returns Err (ToolkitError::CodeMode), does NOT panic
    - Test: the existing env:VAR convention still resolves (no regression)
    - Test: the existing R9 inline-secret rejection still fires for a bare inline literal
    - Proptest: for arbitrary `${name}` strings, resolve_token_secret never panics (returns Ok or Err)
  </behavior>
  <action>
    Add a `${VAR}` expansion branch to `resolve_token_secret` (code_mode.rs:284) immediately after the existing `env:` branch and before the `allow_inline_token_secret_for_dev` branch. The branch matches a string of the exact shape `${NAME}` (starts `${`, ends `}`), extracts NAME, and resolves via `std::env::var(NAME)` → `SecretValue::new(bytes)`; a missing var returns `Err(ToolkitError::CodeMode(format!("env var '{name}' not set ...")))` — mirror the existing `env:` error message style. The `${VAR}` form must coexist with `env:VAR` (do not remove or alter the existing branch). A string that merely contains `${` but is not the full `${NAME}` shape must fall through to the existing inline-secret handling (still rejected unless the dev flag is set) — this preserves the R9 guarantee. NOTE (REVIEW FIX #6): this expansion is intentionally SCOPED to `token_secret` only — non-secret config fields like Athena `output_location` keep `${...}` verbatim (Task 1's parse test). Keep cognitive complexity <= 25 (extract a small `expand_braced_var(&str) -> Option<&str>` helper if needed). Create `tests/env_expansion.rs` with the five behaviors above; the proptest uses `proptest!` over arbitrary env-var-name-like strings asserting no panic. This closes REF-01 Gap #3 and the token-secret threat-surface item (T-85-01).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --test env_expansion -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-server-toolkit --test env_expansion` passes (incl. missing-var → Err, no panic; proptest no-panic)
    - `cargo test -p pmcp-server-toolkit --test code_mode_wiring` still passes (R9 inline rejection NOT regressed)
    - Source assertion: `grep -n 'strip_prefix("env:")' crates/pmcp-server-toolkit/src/code_mode.rs` still matches (env: branch preserved)
    - PMAT: `resolve_token_secret` (and any helper) cognitive complexity <= 25
  </acceptance_criteria>
  <done>${VAR} resolves from env, missing var errors cleanly without panic, env:VAR + R9 rejection unchanged; expansion scoped to token_secret only (non-secret fields verbatim).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed config.toml → toolkit parser | admin-authored TOML crosses into ServerConfig; malformed input must error, never panic |
| `${VAR}`/`env:VAR` → process env → HMAC secret | the resolved secret signs code-mode approval tokens; a weak/empty/missing secret is a forgery risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01-01 | Information disclosure | `resolve_token_secret` `${VAR}` branch | mitigate | Missing/unset var returns `Err(ToolkitError::CodeMode)` — never falls back to a weak/empty secret; min length enforced downstream by `HmacTokenGenerator::MIN_SECRET_LEN` (16 bytes) |
| T-85-01-02 | Tampering | `ServerConfig` `deny_unknown_fields` | mitigate | Additive fields only; deny_unknown_fields preserved so a renamed/typo'd field is rejected at parse time (negative test `renames_rejected`) |
| T-85-01-03 | Denial of service | TOML parse of malformed config | accept | The existing `pmcp_server_toolkit_config_parser` fuzz target (Phase 84) already asserts no-panic on malformed input; the new additive fields are covered by extending its corpus (Plan 06) |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --test config_superset --test env_expansion --test reference_configs --test code_mode_wiring -- --test-threads=1` all green
- No `deny_unknown_fields` removed; all new fields `#[serde(default)]`
</verification>

<success_criteria>
- All four reference configs (incl. Chinook) parse + validate into ServerConfig (REF-01 SC-2)
- Athena `${VAR}` in output_location parses verbatim — SC-2 parse-only unaffected (REVIEW FIX #6)
- Renames still rejected (REF-01 invariant)
- `${VAR}` token-secret expansion works; missing var errors cleanly without panic
</success_criteria>

<output>
After completion, create `.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-01-SUMMARY.md`
</output>
</content>
</invoke>
