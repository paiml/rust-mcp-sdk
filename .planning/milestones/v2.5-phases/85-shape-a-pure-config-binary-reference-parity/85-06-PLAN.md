---
phase: 85-shape-a-pure-config-binary-reference-parity
plan: 06
type: execute
wave: 4
depends_on: [85-05]
files_modified:
  - crates/pmcp-sql-server/tests/parity_chinook.rs
  - crates/pmcp-sql-server/examples/sql_server_min.rs
  - crates/pmcp-sql-server/src/lib.rs
  - CLAUDE.md
  - crates/pmcp-server-toolkit/fuzz/corpus/pmcp_server_toolkit_config_parser/seed-chinook-superset.toml
autonomous: true
requirements: [REF-02, SHAP-A-01]
must_haves:
  truths:
    - "The parity test invokes the REAL --config --schema binary path (lib::run / serve), not an injected connector — proving the zero-Rust pure-config claim (SC-1)"
    - "Replaying generated.yaml against the live HTTP server passes all 29 scenarios"
    - "The replay validates curated tools + validate_code (8×) + execute_code + start_code_mode prompt + 3 resources in one harness (SC-3 + SC-4)"
    - "A Shape C ≤15-line example builds and runs"
    - "Public API items carry doctests"
    - "pmcp-sql-server is in the CLAUDE.md publish order after the per-backend connector crates"
    - "The config-parser fuzz corpus includes a Chinook-superset seed mixing ${VAR} and env:VAR forms (ALWAYS fuzz)"
  artifacts:
    - path: "crates/pmcp-sql-server/tests/parity_chinook.rs"
      provides: "REF-02/SC-3/SC-4 REAL-binary-path spawn (temp config + temp schema → lib::run) + replay generated.yaml"
      contains: "result.success"
    - path: "crates/pmcp-sql-server/examples/sql_server_min.rs"
      provides: "Shape C ≤15-line runnable example"
    - path: "CLAUDE.md"
      provides: "pmcp-sql-server publish-order slot"
      contains: "pmcp-sql-server"
  key_links:
    - from: "tests/parity_chinook.rs"
      to: "temp config.toml (file_path → vendored chinook.db) + temp --schema DDL → lib::run via Args"
      via: "REAL binary entry point, not build_server injection"
      pattern: "Args"
    - from: "spawned StreamableHttpServer"
      to: "mcp_tester ScenarioExecutor replay of generated.yaml"
      via: "ServerTester::new(http url, force_transport=http) → execute(TestScenario::from_file)"
      pattern: "ScenarioExecutor"
    - from: "parity result"
      to: "all 29 scenarios pass"
      via: "result.success assertion"
      pattern: "assert!.*result.success"
---

<objective>
Land the binding REF-02 parity assertion (D-10/D-11): spawn the Shape A reproduction THROUGH THE REAL `--config --schema` binary path against the vendored data-bearing Chinook fixture, replay `generated.yaml` via the mcp-tester library, and assert all scenarios pass. The scenarios encode the production server's behavior — passing them = result parity (SC-4), and because they exercise `validate_code` (8×), `execute_code`, the `start_code_mode` prompt, curated tools, and 3 resources, this single replay also proves code-mode policy parity (SC-3).

**REVIEW FIX (Codex HIGH #5 — parity test must exercise the REAL binary path, not an injected connector).** The prior draft built the server by injecting an in-memory connector directly into `build_server`, which proves ASSEMBLY but NOT the pure-config binary (SC-1's "zero Rust" claim). **FIX:** the parity test must (1) write a temp `config.toml` (start from the vendored `reference-config.toml`, override `[database] file_path` to point at the vendored DATA-BEARING `tests/fixtures/chinook.db` — copy it to a temp path if a writable DB is needed), (2) write a temp `--schema` DDL file (the vendored `chinook.ddl`), (3) invoke the REAL run/serve entry point via `Args { config: <temp toml>, schema: <temp ddl>, http: "127.0.0.1:0" }` → `lib::run` (or the testable `serve` helper lib::run delegates to) over streamable HTTP, (4) replay `generated.yaml` via the mcp-tester library and assert `result.success`. It must NOT short-circuit by injecting an in-memory connector into `build_server`. This exercises clap-free programmatic `Args`, `ServerConfig::from_toml`, `dispatch` (the real `[database] type → connector` seam reading `file_path`), `build_server`, and the HTTP serve — the full pure-config path.

**REVIEW FIX (count correction).** The actual `generated.yaml` has **29 named scenarios** (VERIFIED by `grep -c "^- name:"`), not 31. Assert on `result.success` (all scenarios pass) rather than a hardcoded count, and note 29 in the SUMMARY. The histogram: list_tools×1, list_resources×1, list_prompts×1, tool_call (search_tracks/list_artists/get_album_tracks + validate_code×8 + execute_code-with-invalid-token), get_prompt×1 (start_code_mode), read_resource×3 (docs://chinook/schema, docs://chinook/examples, code-mode://learnings). The DELETE/DDL/no-LIMIT validate_code `failure` assertions + the invalid-token execute_code `failure` ARE the policy-enforcement (SC-3) proof.

Plus the ALWAYS-matrix closeout: a Shape C ≤15-line runnable example, doctests on public items, the CLAUDE.md publish-order slot, and a config-parser fuzz corpus seed for the new Chinook-superset fields.

Purpose: This is the phase's headline proof — the toolkit lift reproduces a production pmcp-run server with zero user Rust, exercised through the real binary entry point. It is also the ALWAYS-coverage gate.
Output: the REAL-binary-path parity integration test, the example, doctests, the publish-order edit, and the fuzz seed.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-CONTEXT.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-RESEARCH.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-PATTERNS.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-05-SUMMARY.md

<interfaces>
mcp-tester library replay (VERIFIED — result accessor is `.success`, NOT `all_passed()`):
```rust
// crates/mcp-tester/src/lib.rs (run_scenario_with_transport pattern, lines 205-260)
use mcp_tester::{ServerTester, ScenarioExecutor, TestScenario};
let mut tester = ServerTester::new(
    &format!("http://{addr}"), Duration::from_secs(30),
    false /*insecure*/, None /*api_key*/, Some("http") /*force_transport*/, None /*middleware*/)?;
tester.initialize().await?;
let scenario = TestScenario::from_file("tests/fixtures/generated.yaml")?;
let mut exec = ScenarioExecutor::new(&mut tester, true /*detailed*/);
let result = exec.execute(scenario).await?;
assert!(result.success, "all parity scenarios must pass: {result:?}");  // ScenarioResult.success: bool (scenario.rs:202)
```

REAL binary-path spawn (REVIEW FIX #5 — use lib::run / serve via programmatic Args, NOT injected connector):
```rust
use pmcp_sql_server::{run, Args};  // or the testable serve entry point lib::run delegates to (read 85-05-SUMMARY)
// 1. temp config: start from tests/fixtures/reference-config.toml, override [database] file_path to a temp copy of chinook.db
// 2. temp schema: write tests/fixtures/chinook.ddl to a temp .ddl file
// 3. Args { config: temp_toml_path, schema: temp_ddl_path, http: "127.0.0.1:0".into() }
// 4. spawn run/serve on a task; capture the REAL bound addr (start() returns it); poll readiness; replay; abort
```
The point: dispatch reads the temp config's `file_path` and opens the data-bearing chinook.db — the SAME path a real `cargo run -- --config X --schema Y` takes. No build_server injection.

Env setup (Pitfall 3 / Runtime State): the harness MUST set CODE_MODE_SECRET to >=16 bytes BEFORE spawning (the Chinook config uses token_secret = "${CODE_MODE_SECRET}", resolved by Plan 01's ${VAR} expansion).

Data-bearing fixture (Plan 03 REVIEW FIX #1): tests/fixtures/chinook.db is the populated DB (real rows for "Rock"/"AC/DC"/"Angus Young"/"Bohemian"). Point the temp config's file_path at a temp copy of it (copy to a tempfile so a writable path is available if INSERT scenarios run). tests/fixtures/chinook.ddl is the SEPARATE --schema text.

generated.yaml shape (VERIFIED): 29 scenarios — list_tools×1, list_resources×1, list_prompts×1, tool_call (search_tracks/list_artists/get_album_tracks + validate_code×8 + execute_code-invalid-token), get_prompt×1, read_resource×3. Assert result.success (all 29), do NOT hardcode a count.

Shape C example target: SHAP-A-01 wants a runnable example; VALIDATION names examples/sql_server_min.rs. The ≤15-line body mirrors e01_toolkit_minimal.rs but uses a connector + the binary's run/build_server. (SHAP-C-01's strict ≤15-line library example is owned by Phase 86; this is the ALWAYS-matrix runnable example for the binary crate — a small smoke example only, do NOT pre-ship the Phase 86 contract.)

CLAUDE.md publish order: pmcp-sql-server depends on pmcp-server-toolkit + the 4 connectors → slot it AFTER the per-backend connector crates (after pmcp-toolkit-athena, item 8). Add a line to the numbered publish-order list + a note that it has no inter-deps with mcp-tester.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parity_chinook.rs — spawn through the REAL --config --schema binary path + replay generated.yaml, assert all 29 scenarios pass (REF-02/SC-3/SC-4)</name>
  <files>crates/pmcp-sql-server/tests/parity_chinook.rs</files>
  <read_first>
    - crates/mcp-tester/src/lib.rs (lines 205-260 run_scenario_with_transport — the exact load → ServerTester::new → ScenarioExecutor::new → execute → result.success flow; result.success is the accessor, scenario.rs:202)
    - crates/mcp-tester/src/scenario.rs (lines 200-215 ScenarioResult — confirm .success: bool; Operation/Assertion enums map to generated.yaml's list_tools/list_resources/list_prompts/tool_call/get_prompt/read_resource + success/contains/exists/failure)
    - tests/streamable_http_integration.rs (lines 32-90 — 127.0.0.1:0 spawn + start() → (addr, handle) + StreamableHttpTransport client; the test-threads=1-safe ephemeral-port pattern)
    - crates/pmcp-sql-server/src/lib.rs (the REAL run/serve entry points from Plan 05 to invoke via programmatic Args — read 85-05-SUMMARY for whether run is directly testable or delegates to a serve helper that returns (addr, handle))
    - crates/pmcp-sql-server/tests/fixtures/{generated.yaml,reference-config.toml,chinook.db,chinook.ddl} (Plan 03 vendored fixtures — the parity contract + config + DATA-BEARING DB + schema text)
    - /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/scenarios/generated.yaml (confirm 29 scenarios + the data-value assertions on lines 51/62/65/76/107 + the 3 read_resource scenarios on lines 295-318 + the get_prompt on line 327)
  </read_first>
  <behavior>
    - Test: set CODE_MODE_SECRET (>=16 bytes); write a temp config.toml (from reference-config.toml with [database] file_path overridden to a temp copy of the data-bearing chinook.db); write the chinook.ddl to a temp --schema file; invoke the REAL lib::run/serve via Args { config, schema, http: "127.0.0.1:0" }; capture the real bound addr; poll readiness; replay generated.yaml; assert result.success is true
    - Test: the replay covers all 29 scenarios — including the data-value assertions (search_tracks returns "Rock"/"AC/DC", get_album_tracks returns "For Those About To Rock...") which ONLY pass because the fixture is data-bearing (REVIEW FIX #1 proof), the 3 read_resource scenarios (REVIEW FIX #2 proof — all three configured resources resolve), and the start_code_mode get_prompt (REVIEW FIX #3 proof — the configured prompt resolves)
    - Test: the `failure` assertions (DELETE/DDL/no-LIMIT validate_code + invalid-token execute_code) pass — i.e. the policy correctly REJECTS those (SC-3 enforcement)
  </behavior>
  <action>
    Create `tests/parity_chinook.rs`. Setup: `std::env::set_var("CODE_MODE_SECRET", "<at-least-16-byte-test-secret>")` (V6 min length). REVIEW FIX #5 (REAL binary path — do NOT inject a connector into build_server): (1) copy the vendored data-bearing `tests/fixtures/chinook.db` to a tempfile path (so the connector has a real, writable populated DB); (2) read the vendored `tests/fixtures/reference-config.toml`, override the `[database] file_path` value to that temp DB path (string-replace or parse→edit→reserialize the TOML), write it to a temp config.toml; (3) write `tests/fixtures/chinook.ddl` to a temp `.ddl` file; (4) construct `Args { config: temp_toml, schema: temp_ddl, http: "127.0.0.1:0".into() }` and invoke the REAL `lib::run`/`serve` entry point (read 85-05-SUMMARY — if `run` blocks, call the inner `serve(server, addr)` helper after letting `run`'s assembly run, OR refactor `run` to expose a testable `serve` that returns `(addr, handle)`; the test must go through ServerConfig::from_toml + dispatch + build_server + StreamableHttpServer, NOT a hand-built connector). Capture the REAL bound addr from start(). Poll readiness (prefer a poll loop calling `tester.initialize()` with backoff over a fixed sleep — D-disc). Replay: `ServerTester::new(http url, Duration::from_secs(30), false, None, Some("http"), None)` → `TestScenario::from_file("tests/fixtures/generated.yaml")` → `ScenarioExecutor::new(&mut tester, true).execute(scenario).await?` → `assert!(result.success, "...{result:?}")` (29 scenarios; assert success, do not hardcode the count). `handle.abort()` at the end. Runs with `--test-threads=1` (ephemeral port + single process). This is the REF-02/SC-3/SC-4 binding assertion. Record in the SUMMARY: the verifier note that REF-02's literal "open-images" wording is intentionally satisfied by the SQLite Chinook reference that owns the scenarios (D-01 approved scope reading, not a gap), AND that the test exercises the real --config --schema path (not connector injection).
  </action>
  <verify>
    <automated>cargo test -p pmcp-sql-server --no-default-features --features sqlite --test parity_chinook -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-sql-server --test parity_chinook -- --test-threads=1` passes; result.success is true (all 29 scenarios)
    - REVIEW FIX #5: the test invokes lib::run/serve via programmatic Args with a temp config + temp schema (NOT build_server with an injected connector) — `grep -n "Args" crates/pmcp-sql-server/tests/parity_chinook.rs` matches and there is NO direct `build_server(.., <injected connector>, ..)` call bypassing dispatch
    - The data-value scenarios (search_tracks "Rock"/"AC/DC") pass — proving the data-bearing fixture (REVIEW FIX #1)
    - The 3 read_resource scenarios pass — proving all configured resources resolve (REVIEW FIX #2)
    - The start_code_mode get_prompt scenario passes — proving the configured prompt resolves (REVIEW FIX #3)
    - The DELETE/DDL/no-LIMIT/invalid-token failure scenarios pass — policy enforcement confirmed (SC-3)
    - Source assertion: `grep -n "result.success" crates/pmcp-sql-server/tests/parity_chinook.rs` matches; `grep -n "generated.yaml" ...` matches
    - Test uses the spawned HTTP server (force_transport "http") and aborts the handle (no leaked task)
  </acceptance_criteria>
  <done>The Shape A repro, exercised through the REAL --config --schema binary path, replays generated.yaml with all 29 scenarios passing — curated tools (real data) + code-mode pair + the configured prompt + all 3 resources, including DELETE/DDL/invalid-token policy rejection (REF-02 / SC-3 / SC-4).</done>
</task>

<task type="auto">
  <name>Task 2: Shape C example + doctests + CLAUDE.md publish-order + fuzz corpus seed (ALWAYS closeout)</name>
  <files>crates/pmcp-sql-server/examples/sql_server_min.rs, crates/pmcp-sql-server/src/lib.rs, CLAUDE.md, crates/pmcp-server-toolkit/fuzz/corpus/pmcp_server_toolkit_config_parser/seed-chinook-superset.toml</files>
  <read_first>
    - crates/pmcp-server-toolkit/examples/e01_toolkit_minimal.rs (the ≤15-line Shape C body shape — single crate-root import, try_* builder chain, no panic)
    - crates/pmcp-sql-server/src/lib.rs (the public run/build_server surface that needs doctests; the example calls into this)
    - CLAUDE.md (§"Release & Publish Workflow" — the numbered publish-order list 1-11; pmcp-sql-server slots after item 8 pmcp-toolkit-athena, with a no-inter-deps-with-mcp-tester note like the connector-crate note)
    - crates/pmcp-server-toolkit/fuzz/corpus/pmcp_server_toolkit_config_parser/ (existing named seed-*.toml convention from Phase 84 — add ONE seed exercising the new file_path/is_reference/[shared_policy_store] fields; named seed only, libfuzzer hash entries left untracked)
    - crates/pmcp-server-toolkit/tests/reference_configs.rs (lines 124-157 — the seed-parse smoke test convention; the new seed must parse, not panic)
  </read_first>
  <action>
    (a) Create `examples/sql_server_min.rs` — a SMALL runnable smoke example demonstrating library use of the binary crate: build a server from an inline minimal config + a connector opened on an in-memory or fixture SQLite DB + a short DDL schema string, in ≤15 lines of `main` body (mirror e01_toolkit_minimal.rs; set the token secret env first; use the try_* / build_server path; print a confirmation). This is the ALWAYS-matrix runnable example only — NOT the Phase 86 ≤15-line library contract (keep it a smoke example, do not over-build). Register it as an `[[example]]` in the crate Cargo.toml if needed and ensure it runs under `--features sqlite`. (b) Add doctests to the public API items in `src/lib.rs` (run, build_server if pub, and any pub dispatch/cli re-exports) — `rust,no_run` doctests showing usage; every public item must have one (TEST-03 ALWAYS convention). (c) Edit CLAUDE.md §"Release & Publish Workflow": insert `pmcp-sql-server` into the numbered publish-order list AFTER the three per-backend connector crates (after item 8 `pmcp-toolkit-athena`), with a one-line note that it depends on pmcp-server-toolkit + the four connectors and has no inter-dep with mcp-tester (renumber the trailing items, or insert as 8.x if renumbering is risky — match the existing list style). (d) Add `fuzz/corpus/pmcp_server_toolkit_config_parser/seed-chinook-superset.toml` — a small valid config exercising `[database] file_path`, `[server] is_reference`, and `[shared_policy_store]` (the Plan 01 superset fields) AND mixing BOTH `${VAR}` and `env:VAR` token-secret forms in comments/alternates so the fuzz target exercises the expanded resolution logic (Gemini suggestion — fuzz corpus diversity). Track only the named seed file.
  </action>
  <verify>
    <automated>cargo run -p pmcp-sql-server --example sql_server_min --no-default-features --features sqlite && cargo test -p pmcp-sql-server --doc --no-default-features --features sqlite</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p pmcp-sql-server --example sql_server_min --features sqlite` exits 0 and prints a confirmation
    - `cargo test -p pmcp-sql-server --doc` passes; every public item in lib.rs has a doctest (`cargo doc` shows no missing-docs warning if missing_docs is denied)
    - Source assertion: `grep -n "pmcp-sql-server" CLAUDE.md` matches in the publish-order section
    - The fuzz seed parses: `cargo test -p pmcp-server-toolkit --test reference_configs` (seed-parse smoke) still green with the new seed; the seed mixes ${VAR} and env:VAR forms
    - `make quality-gate` passes (or, if blocked by the documented pre-existing pmcp-widget-utils pedantic lints noted in Phase 84, scope the verification sweep to the touched crates and record the deferral in the SUMMARY)
  </acceptance_criteria>
  <done>Shape C smoke example runs; doctests on public items pass; CLAUDE.md publish-order updated; fuzz corpus seeded for the new superset fields mixing ${VAR}/env:VAR — ALWAYS matrix complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replayed scenarios → spawned HTTP server (via real binary path) | the parity harness drives the full MCP surface incl. code-mode; the failure assertions verify policy rejects writes/DDL/invalid-token |
| malformed config (fuzz) → toolkit parser | the new superset fields must never panic the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02-02 (re-verified) | Elevation of privilege | code-mode policy via replay | mitigate | The generated.yaml `failure` assertions on DELETE/DDL/no-LIMIT validate_code + invalid-token execute_code confirm the static policy rejects writes/DDL end-to-end through HTTP via the real binary path (SC-3 black-box proof on top of Plan 02's white-box unit test) |
| T-85-06-01 | Denial of service | new config fields under fuzz | mitigate | seed-chinook-superset.toml extends the existing pmcp_server_toolkit_config_parser fuzz target so file_path/is_reference/[shared_policy_store] + ${VAR}/env:VAR parsing is covered by the no-panic invariant (ALWAYS fuzz) |
| T-85-06-02 | Spoofing | parity harness HTTP transport | accept | Test-only loopback (127.0.0.1:0) with force_transport "http"; the SDK transport sends correct Content-Type/Accept headers (Pitfall 5) — no production exposure |
</threat_model>

<verification>
- `cargo test -p pmcp-sql-server --no-default-features --features sqlite -- --test-threads=1` green (incl. parity_chinook via the real binary path)
- `cargo run -p pmcp-sql-server --example sql_server_min --features sqlite` exits 0
- `cargo test -p pmcp-sql-server --doc` green
- `make quality-gate` green (or scoped per documented Phase 84 deferral)
</verification>

<success_criteria>
- Shape A repro replays generated.yaml through the REAL --config --schema binary path — all 29 scenarios pass (REF-02 / SC-3 / SC-4), proving the zero-Rust pure-config claim
- Data-value, 3-resource, and configured-prompt scenarios all pass (REVIEW FIXES #1/#2/#3 end-to-end proof)
- ALWAYS matrix complete: fuzz (seed mixing ${VAR}/env:VAR) + property (Plans 01/02/04) + unit (prior) + doctest + integration (parity) + runnable example
- pmcp-sql-server in CLAUDE.md publish order after the per-backend connectors
</success_criteria>

<output>
After completion, create `.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-06-SUMMARY.md`
</output>
</content>
