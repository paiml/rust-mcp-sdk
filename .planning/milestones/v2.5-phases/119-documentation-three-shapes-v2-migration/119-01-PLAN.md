---
phase: 119-documentation-three-shapes-v2-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [DOCS-05]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-01: `113-SPEC-RECHECK.md` `## Verdict` reads `PUBLISHED-CONFIRMED`, not `PENDING`."
    - "D-01: arm 1 (schema) AND arm 2 (conformance suite) were both RUN BY THIS PLAN and both runs are recorded in a dated sub-section — not cited from 119-RESEARCH.md."
    - "D-01: the eleven requirements HTTP-01..HTTP-08, CLNT-01, CLNT-02, CLNT-05 read `- [x]` in `.planning/REQUIREMENTS.md`."
    - "D-01: the three authorized requirement-TEXT corrections (HTTP-08 citation range, the caveat SATISFIED mark, the `resources.subscribe` naming) are applied."
    - "D-01: PR #2678's state is re-checked and recorded, as `## Third Outcome Policy` mandates at every run."
    - "D-01: `.planning/ROADMAP.md`'s Phase 113 marker records the discharge of reason (2) and states that reason (1) is untouched."
  artifacts:
    - ".planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md — `## Verdict` upgraded + a new dated `### Verdict re-verification — Phase 119 task zero (<date>)` sub-section"
    - ".planning/REQUIREMENTS.md — eleven `[~]` → `[x]` flips + three text corrections"
    - ".planning/ROADMAP.md — Phase 113 marker amended"
  key_links:
    - "arm 1 landing state → `## Verdict` upgrade → requirement flips: the flips are authorized ONLY by a `PUBLISHED-CONFIRMED` landing with BOTH arms run and recorded"
    - "`schema/vendored/core-2026-07-28/schema.ts` blob SHAs ↔ upstream `main` blob SHAs: identity is what makes the vendored copy a valid stand-in for step 2/3 greps"
  prohibitions:
    - "TASK-01..TASK-06 are NOT flipped — they are governed by `114-SPEC-RECHECK.md` under a different trigger (DQ6 both-repositories) whose landing state is still `STILL-ABSENT`; measured 2026-08-18: `ext-tasks` has `draft/` only, 0 tags, 0 releases."
    - "HTTP-07's requirement wording is NOT changed — plan 113-23 measured it and recorded that the current wording is correct."
    - "The `## Verdict` is NOT upgraded on arm 1 alone; a verdict upgraded on one arm is invalid and its flips must be reverted."
    - "No requirement outside the eleven named in `## Third Outcome Policy § The rule` item 2 is flipped."
    - "`.planning/WINDOWS.md` is NOT touched by this plan (plan 119-05 owns the sentinel edits)."
---

<objective>
Task zero. Formally run and record Phase 113's re-verification obligation, upgrade
`113-SPEC-RECHECK.md`'s `## Verdict` to `PUBLISHED-CONFIRMED`, and flip the eleven requirements
the obligation gates. Every downstream Phase-119 chapter is written against the surface this
plan settles, so it runs first and alone in its wave.

Purpose: D-01. Docs written against a `[~]` surface would either hedge or conceal. The
obligation is runnable today and lands clean; this plan discharges it with its OWN evidence.
Output: an upgraded verdict, a dated run record, eleven `[x]` requirements, three authorized
requirement-text corrections, and an amended Phase-113 ROADMAP marker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-CONTEXT.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide manifest)

Newly-created symbols across all ten Phase-119 plans. The plan-review-convergence
source-grounding pass reads this list to exclude these from drift verification — none of them
exists at phase start.

**New book chapter files (`pmcp-book/src/`)**
- `ch12-15-agents-as-mcp-clients.md` (plan 119-02)
- `ch12-16-agent-teams.md` (plan 119-07)
- `ch12-17-migrating-to-mcp-2026-07-28.md` (plan 119-05)

**New course files (`pmcp-course/src/`)**
- `part8-advanced/ch24-agents-and-teams.md` (plan 119-06)
- `part8-advanced/ch24-exercises.md` (plan 119-06)

**New Rust test files (`tests/`)**
- `docs04_examples_run.rs` — fns `s50_standalone_vs_sampled_runs_to_completion` (119-02),
  `s49_sampling_host_runs_to_completion` (119-04),
  `doc_review_team_runs_to_completion` (119-04)
- `docs06_v2_examples_run.rs` — fn `s47_serves_both_v2_client_examples_end_to_end` (119-04)
- `windows_disclosure_tripwire.rs` — fn
  `every_consumer_observable_window_is_cited_in_the_migration_chapter` (119-10)

**New test-helper function**
- `tests/common/example_process.rs::run_example_to_completion(rel_path: &str, args: &[&str], timeout: Duration) -> std::process::Output`
  (plan 119-02). The `timeout` parameter is mandatory — see 119-02's review disposition for the
  bounded-subprocess requirement it satisfies.

**New per-leg timeout constants** (each declared in its consuming test file, never shared)
- `S50_TIMEOUT` (119-02); `S49_TIMEOUT`, `DOC_REVIEW_TEAM_TIMEOUT` (119-04, `docs04_examples_run.rs`);
  `S48_TIMEOUT`, `S53_TIMEOUT` (119-04, `docs06_v2_examples_run.rs`)

**New script + Makefile target**
- `scripts/run-example-builds.sh` (plan 119-03)
- `Makefile` target `test-examples` is REPAIRED, not new (plan 119-03)

**New ledger token**
- `.planning/WINDOWS.md` description sentinel `[CONSUMER-OBSERVABLE]`, prefixed to entries
  12, 13, 19, 20, 23 (plan 119-05)

**New README `##` sections**
- `## Agents & Teams` (plan 119-02)
- `## Protocol Versions` (plan 119-09)

**New book SUMMARY entries**
- `- [Chapter 12.15: Agents as MCP Clients](ch12-15-agents-as-mcp-clients.md)` with child
  `  - [Sampling & Hosting](ch17-04-sampling-hosting.md)` re-parented from Part V (119-02)
- `- [Chapter 12.16: Agent Teams](ch12-16-agent-teams.md)` (119-07)
- `- [Chapter 12.17: Migrating to MCP 2026-07-28 (v2)](ch12-17-migrating-to-mcp-2026-07-28.md)` (119-05)

**New course SUMMARY entries**
- `- [Agents & Teams: Deploy-Anywhere Agent Loops](./part8-advanced/ch24-agents-and-teams.md)`
  with child `  - [Chapter 24 Exercises](./part8-advanced/ch24-exercises.md)` (119-06)

**New phase record**
- `.planning/phases/119-documentation-three-shapes-v2-migration/deferred-items.md` (119-03)
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions (spec-less probe fallback, §C — surfaced, never silently dropped)

No `119-SPEC.md` exists, so there is no `## Edge Coverage` / `## Prohibitions` section to lift.
The deterministic edge probe was run against all three phase requirements and returned
`unclassified` / `unresolved` for every one:

| Requirement | Probe result | Disposition |
|---|---|---|
| DOCS-04 | `unclassified` / `unresolved` — the probe's classifier does not handle documentation requirements | **Flagged assumption.** Predicates authored instead from `119-VALIDATION.md`'s measured Requirement→Test map (structural `mdbook build`, three run-test legs, the `cargo pmcp` verb-set check). Not resolved by the probe; not fabricated as resolved. |
| DOCS-05 | `unclassified` / `unresolved` | **Flagged assumption.** Predicates authored from `119-VALIDATION.md`: the ledger assertion (eleven `[x]` + verdict ≠ `PENDING`) and the D-03 tripwire. The "does not restate the sunset policy" item stays manual-only by design. |
| DOCS-06 | `unclassified` / `unresolved` | **Flagged assumption.** Predicates authored from `119-VALIDATION.md`: the s47/s48/s53 socket leg and the strict-gate negative control. |

Coverage: applicable 3, resolved 0, unresolved 3, byVerification `{explicit: 0, backstop: 0}`.
These three remain **explicit assumptions carried into execution**, not closed edges.

**Additional assumptions this plan carries:**

- **A-01 (corrects RESEARCH § Environment Availability).** Research recorded `mdbook` as
  **absent locally**. It is **present**: `mdbook v0.4.52` and `mdbook-mermaid` are both on
  `PATH`, and both books build clean today (`book_rc=0`, `course_rc=0`, measured during
  planning). CI pins **v0.4.40** (`.github/workflows/docs.yml:43`) — a minor-version skew the
  plan accepts; `create-missing` semantics are stable across it. RESEARCH's Wave-0 "install
  mdbook" gap is therefore **already closed**, and open question 4's negative control is
  directly runnable (plan 119-02 Task 3).
- **A-02 (new, found during planning — not in RESEARCH or PATTERNS).** Running
  `cd pmcp-course && mdbook build` **rewrites two tracked files**:
  `pmcp-course/src/theme/exercises.css` (+215 lines) and
  `pmcp-course/src/theme/exercises.js` (+284/-88). The locally-installed
  `mdbook-exercises v0.1.5` reinstalls its assets into the theme directory on every build, and
  those assets differ from what is committed. **Every plan that builds the course book must
  assert `git diff --quiet -- pmcp-course/src/theme/` before committing** and restore with
  `git checkout -- pmcp-course/src/theme/` if dirty. Without this, a 411-line unrelated theme
  diff ships silently.
- **A-03.** Arm 2 is re-run rather than cited (RESEARCH open question 1, recommendation
  adopted). The landing-state rule says both arms must be "run and recorded"; whether "run"
  means "run by the discharging plan" is genuinely ambiguous, and one `gh api` call plus one
  `cargo nextest` invocation removes the ambiguity permanently.
- **A-04.** Step 2 and step 3 of arm 1 are run against
  `schema/vendored/core-2026-07-28/schema.ts` rather than a fresh upstream fetch. That is valid
  **only because step 1c proves the vendored blobs are byte-identical to upstream `main`**. If
  step 1c shows a mismatch, the landing state is not `PUBLISHED-CONFIRMED` and this plan STOPS.
</flagged_assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: authorize the one-way requirements-ledger discharge (D-01)</name>
  <decision>Upgrade `113-SPEC-RECHECK.md`'s `## Verdict` from `PENDING` to `PUBLISHED-CONFIRMED` and flip eleven requirements from `[~]` to `[x]`.</decision>
  <context>
CONTEXT.md rates D-01 **one-way**: flipping eleven requirements and upgrading a formal verdict
discharges a recorded obligation traceable to a named person and date. Unflipping would require
reopening Phase 113. REVERSIBILITY_GATES is on, so this checkpoint precedes the task that
implements it.

The evidence is not in doubt — the measurement was taken twice (during `/gsd-discuss-phase`
2026-08-18 and again during research the same day) and came back clean both times: upstream
`schema/2026-07-28/` exists, its `main` blob SHAs are byte-identical to the vendored pin, nothing
has touched the path in 21 days, and all three error constants plus all three payload shapes
match. Arm 2 ran 2026-07-27 with verdict NO DRIFT.

What is being authorized is not the *finding* — it is the **act of recording a formal verdict
change and discharging the obligation**. Tasks 1–3 below re-run every check and record their own
output; this checkpoint exists so the ledger mutation is a decision, not a side effect.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — run arm 1 + arm 2 now and discharge</name>
      <pros>Phase 119's prose is written against a settled surface with no `[~]` hedging on the eleven HTTP/CLNT requirements; the obligation stops rolling forward; the recorded exception is spent down.</pros>
      <cons>One-way. If a later reader disputes the landing state, reopening Phase 113 is the only route back.</cons>
    </option>
    <option id="defer">
      <name>Defer — leave the verdict `PENDING` and write the docs with `[~]` caveats</name>
      <pros>No ledger mutation.</pros>
      <cons>Contradicts D-01 directly. The migration chapter would have to hedge eleven shipped, measured behaviours, and the phase would ship documentation weaker than the code it describes.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, or defer</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Run BOTH arms of the re-verification obligation and the mandated PR #2678 re-check</name>
  <files>target/119-01-taskzero-capture.txt (scratch capture only — this task mutates no tracked file)</files>
  <precondition>`gh` CLI is installed and authenticated, and `api.github.com` is reachable — arm 1 step 1 and arm 2 are not runnable without it.</precondition>
  <read_first>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` — read `### Re-verification obligation (binding)` (the TRIGGER block and arm 1's four steps, incl. **step 4's three-branch landing table**), `## Third Outcome Policy` (especially `### Two measured facts a future re-runner must carry`, which mandates the PR #2678 re-check at EVERY run), and `### B.6` / `§ B.6.5` (arm 2's recorded NO-DRIFT run)
    - `schema/vendored/core-2026-07-28/PROVENANCE.md` — the pin `271ecc9accafdd9b83a3c869fa67c22953b2af80`, the recorded blob SHA-1s in `§ Independent corroboration`, and `§ Reproducing this fetch`
    - `.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md` § "F-4 — Task zero (D-01)" and § "Code Examples → The task-zero arm-1 command block (D-01), verified executable" — the exact command block, already proven runnable
    - `tests/v2_conformance_pin.rs` — arm 2's in-repo half; the test binary name is `v2_conformance_pin`
  </read_first>
  <action>
Run every check yourself and capture its literal output. Do NOT cite 119-RESEARCH.md's recorded
output as the evidence — D-01 requires this plan's own run. Research's block is a script to
execute, not a result to quote.

**Arm 1, step 1 — the trigger condition.** List `repos/modelcontextprotocol/modelcontextprotocol/contents/schema`
via `gh api` and confirm a versioned `2026-07-28` directory exists (the trigger is the artifact,
not the calendar).

**Arm 1, step 1b/1c — the pin still resolves and `main` has not drifted.** Resolve the pinned
commit; list commits touching `schema/2026-07-28` (newest must still be the pin); and fetch
`contents/schema/2026-07-28?ref=main`, comparing both blob SHAs and both sizes against
`PROVENANCE.md § Independent corroboration`. The expected values are `schema.ts` sha
`9b55feeb412bc3ae877f2eac10b5c01ba29a2eed` size `98426` and `schema.json` sha
`213c58f6d9a1c2ce6ad055afe90bbdb095a29ee8` size `181474`. **If either blob differs, STOP** — the
landing state is not `PUBLISHED-CONFIRMED`, do not upgrade the verdict, do not flip anything, and
report the mismatch as the plan's outcome.

**Arm 1, step 2 — the three identifiers.** Grep the vendored `schema/vendored/core-2026-07-28/schema.ts`
for `HEADER_MISMATCH`, `MISSING_REQUIRED_CLIENT_CAPABILITY`, `UNSUPPORTED_PROTOCOL_VERSION`.
Expect two hits each (a `const` declaration and a `code: typeof …` field). This grep is only
valid because step 1c proved byte-identity with upstream `main`.

**Arm 1, step 3 — values, HTTP mappings, payload shapes.** Read `schema.ts` lines 425–530 and
assert nine things: the three constants equal `-32020` / `-32021` / `-32022`; all three error
interfaces state the response status MUST be `400 Bad Request`; and the payload shapes are
`supported: string[]`, `requested: string`, `requiredCapabilities: ClientCapabilities`.

**The PR #2678 re-check (mandated, not optional).** Fetch
`repos/modelcontextprotocol/modelcontextprotocol/pulls/2678` and record `state`, `merged`,
`draft`, `title` and `updated_at`. `## Third Outcome Policy` requires this at every run of the
obligation. Note whether it touches the three constants under exception (as of 2026-06-23 it
proposes `-32000`/`-32001`/`-32002` and does not).

**Arm 2 — re-run it, do not merely cite § B.6.5** (RESEARCH open question 1, recommendation
adopted; see flagged assumption A-03). Two halves: (a) confirm the pinned conformance-repo
commit still resolves and the `advertisesSubscriptions` predicate is unchanged, per `§ B.6.2`'s
quotation and `§ B.6.5`'s method; (b) run the in-repo pin test with
`cargo nextest run -E 'binary(v2_conformance_pin)'`. Use `binary(...)`, never `test(...)` —
`test(...)` selects zero tests and still exits 0, and this repo has been bitten by it seven
times in one phase. Assert a NON-ZERO test count in the output.

Write every captured output to a scratch file for Task 2 to transcribe. Do not edit any ledger
file in this task.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(v2_conformance_pin)' --features full</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/modelcontextprotocol/modelcontextprotocol/contents/schema --jq '.[].name'` output contains the literal `2026-07-28`
    - The `main` blob SHA for `schema.ts` equals `9b55feeb412bc3ae877f2eac10b5c01ba29a2eed` and for `schema.json` equals `213c58f6d9a1c2ce6ad055afe90bbdb095a29ee8`; both sizes equal `98426` and `181474`
    - `grep -c 'HEADER_MISMATCH' schema/vendored/core-2026-07-28/schema.ts` equals `2`; the same count holds for `MISSING_REQUIRED_CLIENT_CAPABILITY` and for `UNSUPPORTED_PROTOCOL_VERSION`
    - `grep -c '\-32020' schema/vendored/core-2026-07-28/schema.ts` is at least 1, and the same for `-32021` and `-32022`
    - The PR #2678 fetch returns a JSON object carrying a `state` field, and its value is recorded verbatim in the scratch capture
    - `cargo nextest run -E 'binary(v2_conformance_pin)' --features full` exits 0 AND its summary line reports a test count greater than zero (a zero-count run is a FAILURE of this criterion, not a pass)
    - The scratch capture file contains, at minimum: the six-directory listing, the pin commit date, both `main` blob SHAs, the six step-2 grep hits, the nine step-3 assertions, the PR #2678 state, and the arm-2 output
  </acceptance_criteria>
  <done>Both arms have been executed by this plan and their literal outputs are captured; the landing state computed from arm 1 step 4's table is `PUBLISHED-CONFIRMED`.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the `## Verdict` and append the dated re-verification record</name>
  <files>.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md</files>
  <reversibility rating="one-way">A formal verdict change discharges a recorded, person-and-date-attributed obligation; reverting requires reopening Phase 113.</reversibility>
  <read_first>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` lines 232–295 — the current `## Verdict` body (`PENDING`, at line 232, body `:233-253`) and the EXISTING `### Verdict re-verification — plan 12 Task 3 (2026-07-26)` sub-section at `:255`, whose heading pattern and body shape the new sub-section must match
    - `.planning/phases/113-.../113-SPEC-RECHECK.md` `## Third Outcome Policy` rule 4 — *"the run is still recorded … so that 'we checked and it was absent' is distinguishable from 'nobody checked'"*, which applies a fortiori to a confirming run
    - The Task 1 scratch capture
  </read_first>
  <action>
Two edits to `113-SPEC-RECHECK.md`, both scoped — use `Edit`, never `Write`, on this 1000+ line
record.

**Edit A — the `## Verdict` body.** Replace the `PENDING` verdict body (`:233-253`) with a
`PUBLISHED-CONFIRMED` body. It must state: the landing state and the arm-1 step-4 row that
produced it; that the record was produced against the published, vendored
`schema/vendored/core-2026-07-28/` (byte-identical to upstream `main`) rather than
`schema/draft/schema.ts @ 71e30695…`; that BOTH arms have now been run and recorded (arm 1 by
this plan on the run date, arm 2 both by its 2026-07-27 record at `§ B.6.5` and by this plan's
re-run); and that the eleven requirements are consequently flipped. Preserve the existing
sentence recording that the exception WAS granted and the pointer to `## Recorded Exception`.
Keep the heading text `## Verdict` unchanged.

**Edit B — the run record.** Append, immediately after the existing
`### Verdict re-verification — plan 12 Task 3 (2026-07-26)` sub-section, a new sibling
sub-section titled `### Verdict re-verification — Phase 119 task zero (<the run date>)`. It
transcribes Task 1's captured output verbatim, in this order: (1) the six-directory listing;
(2) the pin resolution and the "nothing has touched the path since" commit list; (3) both `main`
blob SHAs and sizes against `PROVENANCE.md`'s recorded values; (4) the six step-2 grep hits with
line numbers; (5) the nine step-3 assertions as a table; (6) the PR #2678 state, with an explicit
sentence on whether it touches the three constants under exception; (7) the arm-2 result, naming
both its 2026-07-27 record and this plan's re-run and test count; (8) the computed landing state
and the arm-1 step-4 row it comes from.

Add one closing paragraph recording the boundary explicitly: this discharge covers ONLY the
eleven requirements `## Third Outcome Policy § The rule` item 2 names, and **TASK-01..TASK-06 are
untouched** — they are governed by `114-SPEC-RECHECK.md` under the DQ6 both-repositories trigger,
whose landing state is `STILL-ABSENT`; measured on 2026-08-18, `modelcontextprotocol/ext-tasks`
carries only `draft/`, with zero tags and zero releases.

Do not restructure, renumber or reflow anything else in the file.
  </action>
  <verify>
    <automated>test "$(grep -c '^## Verdict' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md)" -eq 1 && grep -A3 '^## Verdict' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md | grep -q 'PUBLISHED-CONFIRMED'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A3 '^## Verdict' .planning/phases/113-*/113-SPEC-RECHECK.md` contains `PUBLISHED-CONFIRMED` and does NOT contain `PENDING` within the verdict body
    - `grep -c '^### Verdict re-verification' .planning/phases/113-*/113-SPEC-RECHECK.md` equals `2` (the 2026-07-26 record plus the new one)
    - The new sub-section contains the literal `271ecc9accafdd9b83a3c869fa67c22953b2af80`, the literal `9b55feeb412bc3ae877f2eac10b5c01ba29a2eed`, and the literal `2678`
    - The new sub-section contains the literal `STILL-ABSENT` in the closing boundary paragraph naming TASK-01..TASK-06
    - `grep -c 'ext-tasks' .planning/phases/113-*/113-SPEC-RECHECK.md` is at least 1
    - `git diff --stat .planning/phases/113-*/113-SPEC-RECHECK.md` shows deletions confined to the verdict body range — no other section loses lines
  </acceptance_criteria>
  <done>`## Verdict` reads `PUBLISHED-CONFIRMED`, a second dated `### Verdict re-verification` sub-section transcribes this plan's own run of both arms plus the PR #2678 check, and the TASK-01..06 boundary is recorded in writing.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the eleven requirements, apply the three authorized text corrections, amend the ROADMAP marker</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <reversibility rating="one-way">Eleven requirement checkboxes move to `[x]` in the project's requirements ledger.</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 36–50 (HTTP-01..HTTP-08) and 905–920 (CLNT-01/02/05) — the current `- [~] **ID**: …` form, so the flip preserves everything after the checkbox
    - `.planning/phases/113-.../113-SPEC-RECHECK.md` `### Authorised for the re-verification run — requirement TEXT corrections` — the three-row table, and the paragraph immediately below it forbidding any change to HTTP-07's wording
    - `.planning/phases/113-.../113-SPEC-RECHECK.md` `## Third Outcome Policy § The rule` item 2 — the authoritative eleven-requirement list
    - `.planning/ROADMAP.md` around the Phase 113 marker (search for `113-SPEC-RECHECK`) and the `⚠ Phase 113's Complete counts PLANS, not REQUIREMENTS` block after the v2.5 progress table
  </read_first>
  <action>
**Edit A — the eleven flips.** In `.planning/REQUIREMENTS.md`, change the checkbox from `[~]` to
`[x]` on exactly these eleven lines, leaving the `**ID**:` label and all following prose byte-identical:
HTTP-01 (`:36`), HTTP-02 (`:37`), HTTP-03 (`:38`), HTTP-04 (`:39`), HTTP-05 (`:40`), HTTP-06
(`:41`), HTTP-07 (`:42`), HTTP-08 (`:50`), CLNT-01 (`:911`), CLNT-02 (`:912`), CLNT-05 (`:915`).
Re-locate each line by its `**ID**` label rather than trusting the line number, and flip nothing
else. The `[~]` entries for TASK-01..TASK-06 (`:105-110`) stay `[~]`.

**Edit B — the three authorized TEXT corrections**, all inside HTTP-08's requirement text and its
`⚠` caveat block. These were deliberately deferred to this exact run (plan 113-28 left
`REQUIREMENTS.md` with a 0-byte diff so every requirement-text change would land in one
reviewable place):
  1. Replace the citation range `stateless.ts:988-1015` with `stateless.ts:983-1016`. Measured
     rationale: 988 is exact for the start, the consuming closure terminates at 1016 (not 1015),
     and 983–987 is the suite's own rationale comment.
  2. Mark the caveat sentence beginning *"The gate needs a second arm pinning a conformance-repo
     sha"* as SATISFIED, noting that plan 113-32 added exactly that arm (`§ B.6` and arm 2).
  3. Where HTTP-08 describes what **gates the stream**, name the `resources.subscribe`
     capability — that is what the conformance predicate reads and what pmcp reads. Keep the
     word `resourceSubscriptions` only where the text describes the `SubscriptionFilter` **field**
     a client sends. The two are different surfaces; this is a clarity correction, not a
     correctness one.

Apply no fourth correction. HTTP-07's wording is explicitly excluded from the authorization
table and must not change.

**Edit C — the ROADMAP marker.** Amend the Phase 113 marker's clause instructing a future reader
to re-run the `113-SPEC-RECHECK.md` checkpoint: record that the obligation was RUN and DISCHARGED
by Phase 119 task zero on the run date, that the verdict is now `PUBLISHED-CONFIRMED`, and that
the eleven requirements are flipped. State explicitly that this discharges reason **(2)** only —
reason **(1)**, the three open codebase gaps, is a separate matter Phase 119 does not touch, so
the phase marker's overall status is not changed by this plan. Use `Edit` with a scoped
replacement; a whole-file `Write` on `ROADMAP.md` would destroy every phase entry outside the
diff window.
  </action>
  <verify>
    <automated>test "$(grep -cE '^- \[~\] \*\*(HTTP-0[1-8]|CLNT-0[125])\*\*' .planning/REQUIREMENTS.md)" -eq 0 && test "$(grep -cE '^- \[x\] \*\*(HTTP-0[1-8]|CLNT-0[125])\*\*' .planning/REQUIREMENTS.md)" -eq 11</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[~\] \*\*(HTTP-0[1-8]|CLNT-0[125])\*\*' .planning/REQUIREMENTS.md` equals `0`
    - `grep -cE '^- \[x\] \*\*(HTTP-0[1-8]|CLNT-0[125])\*\*' .planning/REQUIREMENTS.md` equals `11`
    - `grep -cE '^- \[~\] \*\*TASK-0[1-6]\*\*' .planning/REQUIREMENTS.md` equals `6` — unchanged
    - `grep -c 'stateless.ts:988-1015' .planning/REQUIREMENTS.md` equals `0` AND `grep -c 'stateless.ts:983-1016' .planning/REQUIREMENTS.md` is at least `1`
    - `grep -c 'resources.subscribe' .planning/REQUIREMENTS.md` is at least `1`
    - `git diff --numstat .planning/REQUIREMENTS.md` reports changes confined to the HTTP-0x, CLNT-0x and HTTP-08-caveat regions — no TASK-0x line appears in `git diff .planning/REQUIREMENTS.md`
    - `git diff .planning/ROADMAP.md` touches only the Phase 113 marker region, and the diff text contains both `PUBLISHED-CONFIRMED` and a statement that reason (1) is untouched
    - `git diff --stat .planning/ROADMAP.md` shows no net loss of `### Phase ` headings: `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged from its pre-edit value
  </acceptance_criteria>
  <done>Eleven requirements read `[x]`, the six TASK requirements still read `[~]`, HTTP-08 carries the three authorized corrections, HTTP-07's wording is byte-identical, and the ROADMAP's Phase 113 marker records the discharge of reason (2) only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub → this repo's ledgers | Read-only `gh api` responses become the evidence for a formal verdict change. Nothing is written upstream; the risk is entirely in what we *record* on the strength of what we read. |
| the requirements ledger → every downstream consumer | A `[x]` requirement is a claim that shipped behaviour is verified. Phase 119's chapters, and every later phase, read it as settled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-01 | Repudiation | `113-SPEC-RECHECK.md` `## Verdict` | high | mitigate | Task 2 appends a dated, named sub-section transcribing this plan's own captured output for both arms plus PR #2678, so "we checked and it agreed" is distinguishable from "nobody checked" (`## Third Outcome Policy` rule 4). |
| T-119-02 | Tampering | `.planning/REQUIREMENTS.md` eleven flips | high | mitigate | The flips are authorized ONLY by arm 1 step 4 row 1 with BOTH arms run and recorded. Task 1 re-runs arm 2 rather than citing it (A-03). Task 1 STOPS the plan if step 1c shows a blob mismatch — a pre-final wire constant baked into a released SDK is a wire-visible break for every downstream client (threat T-113-43). |
| T-119-03 | Tampering | scope creep onto TASK-01..06 | high | mitigate | An explicit prohibition, a boundary paragraph in the run record naming `STILL-ABSENT`, and an acceptance criterion asserting `grep -cE '^- \[~\] \*\*TASK-0[1-6]\*\*'` still equals 6. |
| T-119-04 | Tampering | `.planning/ROADMAP.md` whole-file rewrite | medium | mitigate | Edit C mandates scoped `Edit`, never `Write`, plus an acceptance criterion that the `### Phase ` heading count is unchanged. |
| T-119-05 | Spoofing | a stale or forged upstream response | low | accept | `gh api` is TLS-authenticated to `api.github.com` and the blob SHAs are content-addressed; a substituted response cannot match `9b55feeb…`/`213c58f6…` without a SHA-1 collision. No further control is warranted at ASVS L1. |
| T-119-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs ZERO packages — no `[dependencies]`, no `[dev-dependencies]`, no `cargo install` step (119-RESEARCH.md § Package Legitimacy Audit, verified by construction: D-12 forbids new example code and the new tests use only `std` + existing dev-dependencies). The audit table is empty by design, so no package-legitimacy checkpoint applies. |
</threat_model>

<verification>
Run in order, from the repository root:

1. `cargo nextest run -E 'binary(v2_conformance_pin)' --features full` — exits 0 with a non-zero test count (arm 2's in-repo half).
2. `test "$(grep -cE '^- \[~\] \*\*(HTTP-0[1-8]|CLNT-0[125])\*\*' .planning/REQUIREMENTS.md)" -eq 0`
3. `test "$(grep -cE '^- \[x\] \*\*(HTTP-0[1-8]|CLNT-0[125])\*\*' .planning/REQUIREMENTS.md)" -eq 11`
4. `test "$(grep -cE '^- \[~\] \*\*TASK-0[1-6]\*\*' .planning/REQUIREMENTS.md)" -eq 6`
5. `grep -A3 '^## Verdict' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md` prints `PUBLISHED-CONFIRMED`.
6. `test "$(grep -c '^### Verdict re-verification' .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-SPEC-RECHECK.md)" -eq 2`
7. `make quality-gate` exits 0 before the commit (CLAUDE.md, non-negotiable). This plan touches no `.rs` file, so the expected cost is the full suite with no new compilation.

This plan edits no Rust and no book file, so no `mdbook build` step is required here.
</verification>

<success_criteria>
- `113-SPEC-RECHECK.md` `## Verdict` is `PUBLISHED-CONFIRMED` and carries a second dated re-verification sub-section produced by THIS plan's run.
- Both arms were executed by this plan; the PR #2678 re-check was executed and recorded.
- Exactly eleven requirements flipped `[~]` → `[x]`; TASK-01..06 are untouched; HTTP-07's wording is byte-identical.
- The three authorized HTTP-08 text corrections are applied and no fourth correction exists.
- `.planning/ROADMAP.md`'s Phase 113 marker records the discharge of reason (2) and states reason (1) is untouched.
- `make quality-gate` exits 0.
</success_criteria>

<output>
Create `.planning/phases/119-documentation-three-shapes-v2-migration/119-01-SUMMARY.md` when done.
</output>