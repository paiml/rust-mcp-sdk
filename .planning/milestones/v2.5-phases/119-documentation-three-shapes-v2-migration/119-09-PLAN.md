---
phase: 119-documentation-three-shapes-v2-migration
plan: 09
type: execute
wave: 4
depends_on: ["119-02", "119-04", "119-05"]
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-04, DOCS-05]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-11: README gains a `## Protocol Versions` section carrying the dual-version story and linking the new migration chapter — today the eras have nothing in the README at all."
    - "D-11: the `## Examples` block gains runnable commands for the Agents & Teams and v2 example groups, in the full-invocation form the block already uses."
    - "D-11: the stale `## Latest Release: v2.0.0` header is refreshed — it is 18 minor versions old and its body claims full alignment with 2025-11-25 as the LATEST specification, which this milestone directly falsifies."
    - "D-05: the refreshed header writes the crate name-attached and does not use a bare version string to mean a protocol era — the stale header is the exact confusion D-05 exists to prevent."
    - "D-10: every example command added is a full runnable cargo invocation; the two v2 Tasks examples carry NO feature flag because neither is declared in `Cargo.toml`."
    - "The measured example count replaces the stale claim in both README locations."
  artifacts:
    - "README.md — new `## Protocol Versions` section, refreshed release header, extended `## Examples` block, corrected example counts"
    - "CHANGELOG.md — two mislabelled release headings corrected"
  key_links:
    - "README `## Protocol Versions` ↔ `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md`: README is the index tier, the book is the long-form tier — the section points, it does not retell"
    - "README release header ↔ crates.io and root `Cargo.toml:3`: the version claimed must be the version published"
  prohibitions:
    - "No bare version string is used to mean a protocol era — the crate is always name-attached (D-05)."
    - "No CLI verb is named beyond `cargo pmcp agent new`, `cargo pmcp agent dev` and `cargo pmcp team dev`."
    - "The `## Agents & Teams` section added by plan 119-02 is NOT duplicated — this plan extends the README, it does not re-add that section."
    - "`docs/v1-sunset-policy.md` is linked from the book, not restated in the README."
    - "No new example source code is written (D-12)."
---

<objective>
Finish the README shape: add the missing `## Protocol Versions` section, extend the `## Examples`
block with the commands this phase's chapters cite, refresh the release header that has been
stale for eighteen minor versions, and correct two directly-checkable falsehoods in passing.

Purpose: D-11. The README is the SDK's most-read page. Today it has no era story at all, while
its release header advertises "Protocol v2025-11-25: Full alignment with the latest MCP
specification" — a claim this entire milestone falsifies — and claims an example count the tree
has long since passed.
Output: an accurate, era-aware README index tier that points into the book rather than retelling it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-CONTEXT.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-PATTERNS.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md
</context>

<artifacts_this_plan_produces>
## Artifacts this plan produces

- **New README `##` section** `## Protocol Versions`
- **New comment groups** inside the existing `## Examples` fenced block
- **Refreshed** `## Latest Release:` heading and body

No new files. Exclude the new section heading from drift verification.
</artifacts_this_plan_produces>

<review_dispositions>
## Cross-AI review dispositions (`119-REVIEWS.md`)

| Finding | Severity | Disposition |
|---|---|---|
| Task 2's example-command cross-check reads both run-test files "as written by plans 119-02 and 119-04", so this plan consumes 119-04's output without declaring it in `depends_on` | LOW (consensus downgrade from Codex MEDIUM: not a live race, because the wave barrier already orders wave-4 after wave-3, and the build commands independently verify most invocations) | **INCORPORATED.** `depends_on` now names `119-04`. No wave change was needed or made: this plan is wave 4 and 119-04 is wave 3, so the declared dependency and the wave assignment already agree. The stated character-for-character consistency between the README's cited invocations and the tests' assertions is now backed by an ordering guarantee rather than by luck. |
</review_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Add the `## Protocol Versions` section and refresh the stale release header</name>
  <files>README.md</files>
  <read_first>
    - `README.md` lines 440–465 — the `---` rule at 445, the `## Latest Release: v2.0.0` heading at 447, its body at 449–459 (including the "Protocol v2025-11-25: Full alignment with the latest MCP specification" bullet and the "60+ Examples" bullet at 458), and the `---` at 461. Note how section boundaries use `---` rules.
    - `README.md` lines 19–30 — the eight-bullet `## Overview` list and its convention: emoji, bolded name, a space-hyphen-space separator (NOT an em dash), one sentence naming crates in backticks, and a trailing driven-from clause
    - `README.md` as it stands after plan 119-02 — the `## Agents & Teams` section that plan added, so this plan extends rather than duplicates it
    - `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md` as written by plan 119-05 — the link target and the story this section compresses to a pointer
    - Root `Cargo.toml:3` — the current crate version. **RESEARCH correction C-2: crates.io carries `pmcp 2.18.0`, not 2.17.0**, and the root manifest agrees. Re-measure at execution time with `cargo search pmcp --limit 3` rather than trusting either this plan or CONTEXT.md; versions move.
    - `CHANGELOG.md` line 8 — the top `## [2.19.0] - Unreleased` heading, the in-flight line
  </read_first>
  <action>
**Edit A — the new `## Protocol Versions` section.** Add it immediately after the
`## Agents & Teams` section plan 119-02 created, separated by the `---` rules this file uses
between sections. Keep it SHORT — the README is the index tier and the book is the long-form tier.
It must carry four things and no more:
  - The dual-version fact: pmcp speaks both `v1` (2025-11-25) and `v2` (2026-07-28), negotiated
    PER REQUEST, from one binary. You do not run two servers.
  - The one-line-per-role summary of how to opt in — nothing for a server, an explicit builder
    call for a client, automatic preference-with-fallback for an agent — each a single sentence.
  - The v1 opt-OUT command, since it is the only server-side lever and is easy to get backwards:
    `cargo build -p pmcp --no-default-features --features full-v2`.
  - A link to Chapter 12.17 for everything else, and a link to `docs/v1-sunset-policy.md` for the
    sunset. Do not restate the policy here either.

**Vocabulary (D-05), and this section is where it matters most.** Protocol eras are written `v1`
and `v2` with their date strings; the crate is ALWAYS written name-attached, as in "pmcp 2.18".
Never write a bare version to mean an era.

**Edit B — the release header.** The heading and its body are both stale. Re-measure the published
version first (`cargo search pmcp --limit 3` plus root `Cargo.toml:3`) and write what you measured,
not what this plan predicts. Update the heading to name the published crate version in
name-attached form, and rewrite the body so it:
  - names the current published release and, separately, the in-flight unreleased line from
    `CHANGELOG.md`;
  - REPLACES the "full alignment with the latest MCP specification" claim about the v1 date with
    an accurate dual-era statement, linking the new `## Protocol Versions` section. That bullet is
    directly falsified by this milestone and is the single most misleading line in the file;
  - keeps the other feature bullets that are still true, and drops or corrects any that are not;
  - replaces the stale example-count bullet with a MEASURED count. Count the root example files
    at execution time rather than copying a number: research measured 85 root example files, but
    re-measure. Correct the same claim in the `## Examples` intro line as well — the stale figure
    appears in two places and fixing one would leave the file disagreeing with itself.
Preserve the `**Full changelog**: [CHANGELOG.md](CHANGELOG.md)` pointer and the surrounding `---`
rules.
  </action>
  <verify>
    <automated>test "$(grep -c '^## Protocol Versions' README.md)" -eq 1 && test "$(grep -c '^## Latest Release: v2.0.0' README.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Protocol Versions' README.md` equals `1`
    - `grep -c '^## Agents & Teams' README.md` equals `1` — plan 119-02's section is extended, not duplicated
    - `grep -c '^## Latest Release: v2.0.0' README.md` equals `0`
    - `grep -c '^## Latest Release' README.md` equals `1` — the header was refreshed, not deleted
    - The refreshed header names the version that `cargo search pmcp --limit 3` reports as published, and the plan SUMMARY records both the measured value and the literal command output
    - `grep -cF 'Full alignment with the latest MCP specification' README.md` equals `0`
    - `grep -c 'full-v2' README.md` is at least `1`
    - `grep -c 'ch12-17-migrating-to-mcp-2026-07-28' README.md` is at least `1`
    - `grep -c 'v1-sunset-policy.md' README.md` is at least `1`
    - `grep -cF '60+ Examples' README.md` equals `0` and `grep -cF 'includes 60+ comprehensive examples' README.md` equals `0`
    - The replacement count in both locations equals the measured value of `ls examples/*.rs | wc -l`, and the plan SUMMARY records that measured number
    - `grep -ohE 'cargo pmcp [a-z-]+ [a-z-]+' README.md | sort -u` prints only lines within {`cargo pmcp agent new`, `cargo pmcp agent dev`, `cargo pmcp team dev`}
  </acceptance_criteria>
  <done>The README carries an era story for the first time, its release header names the actually-published version with an accurate dual-era claim, and both stale example counts are replaced with a measured figure.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the `## Examples` block with the phase's cited invocations</name>
  <files>README.md</files>
  <read_first>
    - `README.md` lines 532–562 — the `## Examples` section: an intro line, one fenced bash block containing six comment-grouped sections (Basic / Type-safe / Advanced / Agent Skills / Testing / AI-assisted), then a pointer to `examples/README.md`. Lines 551–553 are the Agent Skills group and are the exact template to copy.
    - `Cargo.toml` — the `[[example]]` blocks. **RESEARCH correction C-4: `s50_v2_tasks_server` and `s51_v2_tasks_agent` have NO `[[example]]` declaration** — they are auto-discovered and build under DEFAULT features, so citing them with a feature flag would be wrong.
    - `.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md` § F-6 — the measured minimal invocation for each of the six D-15 examples
    - `tests/docs04_examples_run.rs` and `tests/docs06_v2_examples_run.rs` as written by plans 119-02 and 119-04 — the invocations the tests actually exercise. The README must not disagree with them.
  </read_first>
  <action>
Add two new comment groups inside the existing `## Examples` fenced bash block, matching the Agent
Skills group's form exactly: a `# Group name — short qualifier` comment line followed by one
runnable command per line, no shell prompt characters, no output shown.

**Group 1 — Agents & Teams.** The three DOCS-04 examples:
`cargo run --example s49_sampling_host` (needs no features),
`cargo run -p pmcp-agent --example s50_standalone_vs_sampled` (default features), and
`cargo run -p pmcp-team-servers --example doc_review_team --features runtime`. Use the
`--features runtime` form, consistent with plans 119-04, 119-06 and 119-07 and with what the
tests assert.

**Group 2 — MCP 2026-07-28 (v2).** The stateless server plus its two clients, and the two Tasks
examples: `s47_v2_stateless_mrtr`, `s48_v2_mrtr_client` and `s53_v2_agent_client` under
`--features full`, and `s50_v2_tasks_server` and `s51_v2_tasks_agent` with NO feature flag at all.
Add a short comment noting that the s47 server must be running before either client is run, and
that both clients take the server address as their argument — a reader who runs the client alone
will see a connection failure and blame the example.

**Every command must be one that runs.** Do not carry a feature flag onto an example that has no
`[[example]]` declaration, and do not drop one from an example that needs it. Cross-check each
line against `Cargo.toml`'s `[[example]]` set and against the two run-test files; the README, the
book chapters and the tests must all name the same invocations.

Do not renumber any example, do not add an index page, and do not cite an example by a bare number
anywhere (D-10). The numbered namespace genuinely collides — `s49` appears twice inside root
`examples/` and `s50` appears in both root `examples/` and `crates/pmcp-agent/examples/` — so the
`-p` flag is load-bearing, not decorative.
  </action>
  <verify>
    <automated>cargo build --example s50_v2_tasks_server --example s51_v2_tasks_agent && cargo build --features full --example s47_v2_stateless_mrtr --example s48_v2_mrtr_client --example s53_v2_agent_client</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cargo run --example s49_sampling_host' README.md` is at least `1`
    - `grep -c 'cargo run -p pmcp-agent --example s50_standalone_vs_sampled' README.md` is at least `1`
    - `grep -c 'cargo run -p pmcp-team-servers --example doc_review_team --features runtime' README.md` is at least `1`
    - `grep -c 's47_v2_stateless_mrtr' README.md` is at least `1`, and the same for `s48_v2_mrtr_client` and `s53_v2_agent_client`
    - `grep -c 's50_v2_tasks_server' README.md` is at least `1` and `grep -c 's51_v2_tasks_agent' README.md` is at least `1`
    - Neither v2 Tasks example carries a feature flag: `grep -c 's50_v2_tasks_server --features' README.md` equals `0` and `grep -c 's51_v2_tasks_agent --features' README.md` equals `0`
    - `cargo build --example s50_v2_tasks_server --example s51_v2_tasks_agent` exits 0 with no feature flag
    - `cargo build --features full --example s47_v2_stateless_mrtr --example s48_v2_mrtr_client --example s53_v2_agent_client` exits 0
    - `grep -c '^## Examples' README.md` equals `1` — the block was extended, not duplicated
    - Every example invocation added to README also appears, character-for-character in its example-name-and-flags portion, in either a book chapter or a run test — the plan SUMMARY records that cross-check
  </acceptance_criteria>
  <done>The `## Examples` block carries two new groups covering all six D-15 examples plus the two v2 Tasks examples, every invocation is proven to build with exactly the flags shown, and none disagrees with the tests or the chapters.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two mislabelled CHANGELOG release headings</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1–20 — the top of the file and the `## [2.19.0] - Unreleased` heading at line 8
    - `CHANGELOG.md` line 99 — `## [2.18.0] - Unreleased`, and line 176 — `## [2.17.0] - Unreleased`. Both versions ARE published on crates.io.
    - The heading form used by any correctly-dated release entry further down the file, so the corrected headings match the file's own convention rather than inventing one
  </read_first>
  <action>
An incidental fix, in scope only because D-11 already opens `CHANGELOG.md`'s neighbourhood for the
release-header refresh and because it removes a directly checkable falsehood from the repository's
most-read changelog.

`## [2.18.0] - Unreleased` and `## [2.17.0] - Unreleased` are both wrong: both versions are
published on crates.io. Re-measure before editing — run `cargo search pmcp --limit 3` and confirm
which versions are actually published, then correct only the headings that measurement shows to be
mislabelled. Replace `Unreleased` with each version's release date, matching the date format the
file's correctly-labelled entries already use. Find the dates from the git tags
(`git tag --list 'v2.1[78].*'` plus `git log -1 --format=%ad` on each) rather than guessing.

**Leave `## [2.19.0] - Unreleased` alone** — that one is accurate; 2.19.0 is genuinely the
in-flight line.

Change no changelog CONTENT — only the two heading suffixes. If the measurement shows a version is
in fact unpublished, leave its heading as-is and record that in the plan SUMMARY. This fix is
optional in the sense that skipping it costs nothing; what is NOT optional is that it be based on
measurement rather than on this plan's assumption.
  </action>
  <verify>
    <automated>test "$(grep -c '^## \[2.19.0\] - Unreleased' CHANGELOG.md)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## \[2.19.0\] - Unreleased' CHANGELOG.md` equals `1` — the in-flight line is untouched
    - For every version that `cargo search pmcp --limit 3` and the git tags show as PUBLISHED, `grep -c "^## \[<that version>\] - Unreleased" CHANGELOG.md` equals `0`
    - The corrected headings carry a date in the same format the file's other dated entries use
    - `git diff --numstat CHANGELOG.md` reports at most `2` added and `2` removed lines — headings only, no content change
    - The plan SUMMARY records the literal `cargo search pmcp --limit 3` output and the git-tag dates used
  </acceptance_criteria>
  <done>Every published version's changelog heading carries its release date instead of `Unreleased`, the genuinely-unreleased 2.19.0 heading is untouched, and no changelog content changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → every new user | The README is the first and often only page a prospective user reads. A false claim there propagates further than the same claim anywhere else in the repository. |
| README example commands → user's terminal | Commands are copy-pasted verbatim. A wrong feature flag produces a confusing failure the user attributes to the SDK. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-44 | Repudiation | the README claiming full alignment with the v1 specification as "the latest" | medium | mitigate | The bullet is replaced with an accurate dual-era statement; an acceptance criterion greps for the stale phrase and requires zero matches. |
| T-119-45 | Repudiation | the README naming a crate version that is not published | medium | mitigate | The version is re-measured with `cargo search` at execution time rather than taken from this plan or from CONTEXT.md (which recorded 2.17.0 while 2.18.0 was live); the measured output is recorded in the SUMMARY. |
| T-119-46 | Spoofing | a README command that does not run — a feature flag on an undeclared example, or a missing one | medium | mitigate | Task 2's `<verify>` BUILDS every added invocation with exactly the flags shown; acceptance criteria assert the two auto-discovered v2 Tasks examples carry no flag. |
| T-119-47 | Information disclosure | a bare version string read as a protocol era, leading a reader to the wrong migration path | medium | mitigate | D-05's name-attached convention is applied throughout the new section and the refreshed header; the stale header being removed is itself the mitigation, since it is the origin of the confusion. |
| T-119-48 | Repudiation | correcting one stale example count and leaving the other | low | mitigate | Acceptance criteria grep for BOTH stale phrasings and require zero matches, and require both replacements to equal one measured value. |
| T-119-49 | Tampering | a changelog content change smuggled in with the heading fix | low | mitigate | An acceptance criterion caps the diff at two added and two removed lines. |
| T-119-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; this plan edits two markdown files. |
</threat_model>

<verification>
1. `cargo build --example s50_v2_tasks_server --example s51_v2_tasks_agent` — exits 0 with NO feature flag.
2. `cargo build --features full --example s47_v2_stateless_mrtr --example s48_v2_mrtr_client --example s53_v2_agent_client` — exits 0.
3. `cargo build --example s49_sampling_host` and `cargo build -p pmcp-agent --example s50_standalone_vs_sampled` — exit 0.
4. `cargo build -p pmcp-team-servers --example doc_review_team --features runtime` — exits 0.
5. `test "$(grep -c '^## Protocol Versions' README.md)" -eq 1`
6. `cargo search pmcp --limit 3` — run it and record the output; the README and CHANGELOG must agree with it.
7. `make quality-gate` — exits 0 before the commit.

This plan touches no book file, so no `mdbook build` is required — but if the wave's other plans
touched one, run both builds and then assert `git diff --quiet -- pmcp-course/src/theme/`.
</verification>

<success_criteria>
- The README carries `## Protocol Versions` with the dual-version story, the v1 opt-out command, and links to Chapter 12.17 and the sunset policy.
- The release header names the measured published version and makes no false specification-alignment claim.
- Both stale example counts are replaced with one measured figure.
- The `## Examples` block covers all six D-15 examples plus both v2 Tasks examples, and every invocation is proven to build with exactly the flags shown.
- Only the three real `cargo pmcp` verbs appear anywhere in the README.
- Published versions no longer carry `Unreleased` headings; 2.19.0 still does; no changelog content changed.
- `make quality-gate` exits 0.
</success_criteria>

<output>
Create `.planning/phases/119-documentation-three-shapes-v2-migration/119-09-SUMMARY.md` when done.
</output>
