---
phase: 109-team-reference-servers
plan: 08
type: execute
wave: 4
depends_on: ["109-01", "109-02", "109-03", "109-04", "109-05", "109-06", "109-07"]
files_modified:
  - contracts/team-servers/binding.yaml
  - Makefile
  - .github/workflows/ci.yml
  - crates/pmcp-team-servers/examples/doc_review_team.rs
  - crates/pmcp-team-servers/tests/dev_binary_smoke.rs
autonomous: true
requirements: [TEAM-06]
must_haves:
  truths:
    - "contracts/team-servers/binding.yaml is finalized (status: implemented, real signatures) binding each team-servers-v1 equation to a concrete reference-server function"
    - "The pmat compliance invocation is correct: `pmat comply check --path .` (a project path, NOT a binding-file positional argument)"
    - "A fail-closed `comply-ci` target (no command-v guard) runs in CI; a graceful `comply` target (command -v pmat guard) is chained into quality-gate for local dev"
    - "A negative compliance test proves a deliberately broken binding is rejected"
    - "The doc-review E2E example runs the full four-server flow on an injected FixedSource and exits 0"
    - "ALL FOUR dev binaries are launched as REAL subprocesses (via the SDK stdio client/transport, not hand-written JSON framing) and each responds to tools/list; children are bounded and not leaked"
  artifacts:
    - path: "contracts/team-servers/binding.yaml"
      provides: "finalized equation → reference-fn bindings for team-servers-v1 (D-18)"
      contains: "status: implemented"
    - path: "crates/pmcp-team-servers/examples/doc_review_team.rs"
      provides: "doc-review E2E narrative across all four servers (D-16)"
      contains: "TeamRuntime"
    - path: "crates/pmcp-team-servers/tests/dev_binary_smoke.rs"
      provides: "subprocess-spawn smoke test across all four dev binaries via the SDK stdio client"
      contains: "CARGO_BIN_EXE"
  key_links:
    - from: "Makefile (comply-ci target)"
      to: ".github/workflows/ci.yml"
      via: "CI invokes make comply-ci (fail closed)"
      pattern: "comply-ci"
    - from: "tests/dev_binary_smoke.rs"
      to: "src/bin/{team_fs,mem_mcp,approval_mcp,team_mcp}.rs (real processes)"
      via: "SDK stdio client over each CARGO_BIN_EXE child"
      pattern: "CARGO_BIN_EXE"
---

<objective>
Close the contract-first loop for team-servers (D-18) corrected against every 109-08 review HIGH. Finalize `contracts/team-servers/binding.yaml` (the skeleton was authored contract-first in 109-01) by flipping each equation binding to `status: implemented` with the real reference-server function signatures. Wire compliance with the CORRECT invocation — `pmat comply check --path .` takes a PROJECT PATH, not a binding-file positional argument — via TWO Makefile targets: a fail-closed `comply-ci` (asserts pmat present, fails on violation, invoked by the CI quality-gate job) and a graceful `comply` (guarded by `command -v pmat`, chained into `make quality-gate` so local dev without pmat still passes). Add a NEGATIVE compliance test proving a deliberately broken binding is rejected. Ship the doc-review E2E example (D-16) on an injected `FixedSource`. Finally, add a subprocess-spawn smoke test that launches ALL FOUR dev binaries as REAL child processes and verifies each responds to `tools/list` — using the SDK's stdio client/transport (NOT hand-written JSON framing) with deterministic injected packages/sources, bounded runtime, and no leaked processes — parameterizing TEAM-01's "all four runnable binaries" claim.

Purpose: Operationalize the house contract-first rule with the right CLI + fail-closed CI, deliver the BA-followable narrative demo, and prove all four dev binaries actually run out-of-process.
Output: finalized `contracts/team-servers/binding.yaml`, `comply` + `comply-ci` Makefile targets (+ CI wiring), `examples/doc_review_team.rs`, and `tests/dev_binary_smoke.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
From contracts/team-servers/binding.yaml (skeleton from 109-01, status: planned): `version`, `target_crate: pmcp-team-servers`, `bindings[]` each `{ contract, equation, function, module_path, signature, status, notes }`.
From contracts/team-servers-v1.yaml (rev'd 1.1.0 in 109-01): equations `fs_tool_surface`, `mem_tool_surface`, `approval_tool_surface`, `team_dispatch_surface`.
Reference-server functions to bind (final signatures from prior plans): `pmcp_team_servers::fs::server::build_team_fs_server` (109-02), `mem::server::build_mem_mcp_server` (109-03), `approval::server::build_approval_mcp_server` (109-04), `team::server::build_team_mcp_server` (109-05); plus `compose::derive::derive_attachment` + `compose::wiring::TeamRuntime` notes.
From the dev binaries (feature-gated [[bin]]s under `--all-features`): cargo exposes `env!("CARGO_BIN_EXE_team-fs")` (+ `_mem-mcp`, `_approval-mcp`, `_team-mcp`) to integration tests. Each bin takes `--package <file> --stdio` (+ team-fs/team-mcp `--data-dir`); team-mcp needs a resolvable member AgentPackage on disk (LocalDirPackageResolver) + FixedSource.
From pmcp: the SDK stdio client transport (grep `Stdio` / child-process transport in src/) — reuse it to drive a spawned child over stdin/stdout instead of hand-writing newline-delimited JSON-RPC.
From Makefile: `quality-gate:` chains fmt/lint/build/test/etc.; `pmat-quality` shows the `@if command -v pmat ...; then ... else <warn> fi` guard pattern.
From .github/workflows/ci.yml: the `quality-gate` job (already runs `pmat quality-gate` for complexity); add a `make comply-ci` step there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize binding.yaml (status: implemented, real signatures) + negative compliance test</name>
  <files>contracts/team-servers/binding.yaml</files>
  <read_first>
    - contracts/team-servers/binding.yaml (skeleton from 109-01 — flip status + fill signatures)
    - contracts/team-servers-v1.yaml (the four equations)
    - crates/pmcp-team-servers/src/{fs,mem,approval,team}/server.rs (final function signatures)
    - .planning/phases/109-team-reference-servers/109-REVIEWS.md (109-08: correct pmat invocation, contract-first, fail-closed)
  </read_first>
  <action>
    Update `contracts/team-servers/binding.yaml`: set each binding's `status` from `planned` to `implemented` and fill the REAL `signature` from the built reference-server functions (`build_team_fs_server`, `build_mem_mcp_server`, `build_approval_mcp_server`, `build_team_mcp_server`) with `module_path: pmcp_team_servers::...` and a `notes` line referencing the D-decision it satisfies. Optionally add `derive_attachment` / `TeamRuntime` binding notes for the composition rule. Keep field names byte-for-byte compatible with `contracts/binding.yaml`. Add a small companion negative fixture the test in Task 2 uses: a deliberately-broken binding (e.g. an equation pointing at a nonexistent function) placed under a test path (NOT the real binding) so a negative compliance check can assert rejection — Claude's discretion on where the broken fixture lives (a `contracts/team-servers/binding.broken.yaml` test fixture is fine).
  </action>
  <verify>
    <automated>test -f contracts/team-servers/binding.yaml && grep -q "status: implemented" contracts/team-servers/binding.yaml && grep -c "equation:" contracts/team-servers/binding.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/team-servers/binding.yaml` has `target_crate: pmcp-team-servers`, one `implemented` binding per equation (`fs_tool_surface`, `mem_tool_surface`, `approval_tool_surface`, `team_dispatch_surface`) with a real signature and `module_path: pmcp_team_servers::...`
    - a deliberately-broken binding fixture exists for the negative compliance test
  </acceptance_criteria>
  <done>binding.yaml finalized (status: implemented, real signatures) binding all four equations; a broken-binding fixture is present for the negative test.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pmat comply — fail-closed `comply-ci` (CI) + graceful `comply` (quality-gate) with correct --path invocation + negative test</name>
  <files>Makefile, .github/workflows/ci.yml</files>
  <read_first>
    - Makefile (quality-gate target + the `pmat-quality` `command -v pmat` guard pattern)
    - .github/workflows/ci.yml (the quality-gate job running the pmat complexity gate)
    - contracts/team-servers/binding.yaml + binding.broken.yaml (from Task 1)
  </read_first>
  <action>
    Add TWO `.PHONY` Makefile targets using the CORRECT invocation `pmat comply check --path .` (project path — NOT a binding-file positional; if the confirmed CLI from 109-01's probe differs, use the confirmed form): (a) `comply` — GRACEFUL: guarded by `@if command -v pmat &> /dev/null; then pmat comply check --path . ; else echo "warn: pmat absent, skipping comply"; fi` — so a dev machine without pmat still passes; chain `$(MAKE) comply` into the `quality-gate` target alongside `validate-always`/`purity-check` (do NOT weaken any existing step). (b) `comply-ci` — FAIL CLOSED: NO `command -v` guard; it asserts pmat is present and runs `pmat comply check --path .`, failing the target on any violation or if pmat is missing (this is the mechanism that makes drift gate-blocking, closing the "vacuous guard" review concern). Add a negative check target/step (or fold into a test) that runs `pmat comply check` against the broken-binding fixture and asserts NON-zero exit (proves the gate actually rejects bad bindings). In `.github/workflows/ci.yml` add a `make comply-ci` step to the `quality-gate` job so CI fails closed on compliance drift while local `make quality-gate` stays graceful.
  </action>
  <verify>
    <automated>grep -q "^comply:" Makefile && grep -q "^comply-ci:" Makefile && grep -q "comply-ci" .github/workflows/ci.yml && grep -q "comply" Makefile && make comply 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Makefile has `comply:` (graceful, `command -v pmat` guarded, `pmat comply check --path .`) chained into `quality-gate`, and `comply-ci:` (fail closed, no guard)
    - the compliance invocation is `pmat comply check --path .` (project path), NOT a binding-file positional argument
    - `.github/workflows/ci.yml` quality-gate job invokes `make comply-ci`
    - a negative check asserts a broken binding yields non-zero exit
    - `make comply` exits 0 locally (runs pmat comply or warns pmat-absent) without breaking the gate
  </acceptance_criteria>
  <done>Compliance is wired with the correct `--path .` invocation via a fail-closed CI target and a graceful local target; drift is gate-blocking in CI; a negative test proves rejection.</done>
</task>

<task type="auto">
  <name>Task 3: Doc-review E2E example across all four servers (injected FixedSource)</name>
  <files>crates/pmcp-team-servers/examples/doc_review_team.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/compose/wiring.rs (TeamRuntimeBuilder from 109-06)
    - crates/pmcp-team-servers/tests/small_team.rs (FixedSource TeamPackage + LocalDirPackageResolver construction from 109-06)
    - crates/pmcp-agent/src/adapter/factory.rs (FixedSourceFactory)
    - .planning/phases/109-team-reference-servers/109-CONTEXT.md (D-16 doc-review flow narrative)
  </read_first>
  <action>
    Create `examples/doc_review_team.rs`: a runnable, commented BA-followable narrative that, with an INJECTED `FixedSourceFactory` override (CI-deterministic; real LLM optional) and a `LocalDirPackageResolver` over a temp dir holding member AgentPackages, `TeamRuntimeBuilder::build`s a 2-member team with a human role and team-fs + mem-mcp opt-ins, then walks the doc-review flow (D-16): member A drafts a file via team-fs → `fs__sync_to_review` → asks approval via `team_approval__ask_<role>` → resolve via `resolve_approval` (the human verdict) → member B reads the file and stores a summary in `mem__add` — the agent-facing dispatch going through `team_mcp__<member>` tools and surfacing related-task `_meta` (under `RELATED_TASK_META_KEY`). Print each step. Exit 0 deterministically.
  </action>
  <verify>
    <automated>cargo run -p pmcp-team-servers --example doc_review_team --all-features 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p pmcp-team-servers --example doc_review_team --all-features` exits 0
    - the example drives team-fs write + sync_to_review, approval ask + resolve_approval, mem__add, and team_mcp__<member> dispatch, printing the narrative steps
    - it uses an injected `FixedSourceFactory` (no live network/LLM)
  </acceptance_criteria>
  <done>The doc-review E2E example runs the full four-server story deterministically on an injected FixedSource and exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Subprocess-spawn smoke test across ALL FOUR dev binaries (SDK stdio client)</name>
  <files>crates/pmcp-team-servers/tests/dev_binary_smoke.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/bin/*.rs (the four bins; --package/--stdio/--data-dir args)
    - crates/pmcp-package/src/package/{team,agent}.rs (serialize minimal TeamPackage + member AgentPackage to temp files)
    - crates/pmcp-team-servers/tests/small_team.rs (minimal TeamPackage construction from 109-06)
    - pmcp stdio client/transport (grep `Stdio` in src/ — reuse instead of hand-writing JSON framing)
  </read_first>
  <action>
    Create `tests/dev_binary_smoke.rs`: an integration test that launches EACH of the four dev binaries (`team-fs`, `mem-mcp`, `approval-mcp`, `team-mcp`) as a REAL subprocess (via `env!("CARGO_BIN_EXE_<bin>")`) and proves each responds to `tools/list` over stdio — parameterizing TEAM-01's "all four runnable binaries" claim (removing the Manual-Only launch verification). Use a table/loop over the four bins. For each: write a minimal valid `TeamPackage` (and, for team-mcp, a resolvable member `AgentPackage` in the data dir + a FixedSource-compatible config) to a `tempfile::TempDir`, spawn `Command::new(bin).args(["--stdio","--package",<pkg>,"--data-dir",<tmp>])` with piped stdio, and DRIVE THE HANDSHAKE VIA THE SDK'S STDIO CLIENT/TRANSPORT (initialize + list_tools) rather than hand-writing newline-delimited JSON-RPC (109-08 review — reuse the SDK's real framing). Assert each server advertises its expected surface (at least: team-fs → `fs__list`, mem-mcp → `mem__add`, approval-mcp → `resolve_approval`, team-mcp → one `team_mcp__<member>`). Bound the runtime with a timeout, kill/await each child (no leak). Gate the test under `--all-features` (CARGO_BIN_EXE_* are defined then); `#[cfg]`-guard the body if narrowing is needed. Keep it offline (FixedSource for team-mcp).
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers --test dev_binary_smoke --all-features 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dev_binary_smoke.rs` spawns ALL FOUR bins via `env!("CARGO_BIN_EXE_...")` (grep confirms each) — real subprocesses, not in-process Servers
    - the handshake uses the SDK stdio client/transport (grep confirms; no hand-written newline JSON-RPC framing)
    - each server's expected surface is asserted (fs__list / mem__add / resolve_approval / team_mcp__<member>)
    - children are awaited/killed (no leak) with a bounded timeout, offline
    - `cargo test -p pmcp-team-servers --test dev_binary_smoke --all-features` exits 0
  </acceptance_criteria>
  <done>All four dev binaries are proven to run out-of-process and answer tools/list via a real subprocess smoke test driven by the SDK stdio client, removing the TEAM-01 Manual-Only launch verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| binding.yaml → pmat comply | Trusted in-repo contract-binding metadata |
| example → in-process servers | Injected FixedSource, no network |
| smoke test → spawned dev binaries | Child processes over stdio pipes; local trusted operator, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-08-01 | Repudiation | contract equations unbound / drift unverified | mitigate | binding.yaml binds every equation to a concrete function; fail-closed `comply-ci` in CI makes drift gate-blocking; a negative test proves a broken binding is rejected (Task 1/2). |
| T-109-08-02 | Denial of Service | pmat guard makes the gate pass vacuously | mitigate | `comply-ci` has NO command-v guard and fails closed in CI; the graceful `comply` is local-only convenience (Task 2). |
| T-109-08-03 | Denial of Service | spawned child binaries leak / hang the test suite | mitigate | Smoke test bounds runtime with a timeout and kills/awaits each child; FixedSource team-mcp needs no network (Task 4). |
| T-109-08-SC | Tampering | dependency graph | mitigate | No new registry packages; example + smoke test compose in-repo crates on injected FixedSource / local processes; smoke test reuses the SDK stdio transport. |
</threat_model>

<verification>
- `contracts/team-servers/binding.yaml` finalized (implemented, real signatures); `make comply` graceful + in quality-gate; `make comply-ci` fail-closed + in CI; negative test rejects a broken binding
- `cargo run -p pmcp-team-servers --example doc_review_team --all-features` exits 0
- `cargo test -p pmcp-team-servers --test dev_binary_smoke --all-features` exits 0 (all four binaries via SDK stdio client)
</verification>

<success_criteria>
- Contract-first loop closed (D-18): binding.yaml finalized + correct `pmat comply check --path .` wired fail-closed in CI and graceful locally, with a negative-rejection test
- Doc-review E2E example (D-16) exercises every contract surface deterministically on injected FixedSource
- TEAM-01 runnable binaries proven out-of-process for ALL FOUR via a real subprocess smoke test using the SDK stdio client
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-08-SUMMARY.md` when done
</output>
