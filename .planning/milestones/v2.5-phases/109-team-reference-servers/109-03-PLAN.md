---
phase: 109-team-reference-servers
plan: 03
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - crates/pmcp-team-servers/src/mem/backend.rs
  - crates/pmcp-team-servers/src/mem/bm25.rs
  - crates/pmcp-team-servers/src/mem/server.rs
  - crates/pmcp-team-servers/src/bin/mem_mcp.rs
  - crates/pmcp-team-servers/tests/mem_props.rs
autonomous: true
requirements: [TEAM-04]
must_haves:
  truths:
    - "mem-mcp advertises exactly the 6 mem__* tools; unknown mem__* names error, never panic"
    - "mem__search ranks stored memories by a hand-rolled keyword/BM25 scorer with no embedder dependency"
    - "The scorer short-circuits to 0.0 when the corpus is empty / average document length is 0 (no division by zero) and floors IDF at 0.0 (no negative penalty for common terms)"
    - "The in-memory backend uses a deterministic ID/clock seam so conformance fixtures get stable ids (not random UUIDs)"
    - "mem__add then mem__get round-trips; mem__delete removes; mem__list_recent returns newest-first; search ties break deterministically (score desc, then creation ordinal)"
    - "The mem-mcp dev binary serves HTTP when built with the http feature and stdio otherwise"
  artifacts:
    - path: "crates/pmcp-team-servers/src/mem/backend.rs"
      provides: "TeamMemoryBackend object-safe async trait (6 mem__* ops) + MemError + deterministic Id/Clock seam"
      exports: ["TeamMemoryBackend", "MemError"]
    - path: "crates/pmcp-team-servers/src/mem/bm25.rs"
      provides: "Hand-rolled zero-dep BM25/keyword scorer with L_avg==0 short-circuit + IDF floor"
      contains: "fn score"
    - path: "crates/pmcp-team-servers/tests/mem_props.rs"
      provides: "proptest invariants (non-negativity, determinism, finite, stable tie-break, tf-monotonicity at fixed doc length)"
      contains: "proptest!"
  key_links:
    - from: "src/mem/server.rs"
      to: "TeamMemoryBackend"
      via: "each mem__* tool handler calls one backend method"
      pattern: "mem__(add|get|search|list_recent|delete)"
    - from: "src/mem/backend.rs (in-memory impl)"
      to: "src/mem/bm25.rs"
      via: "mem__search ranks via the scorer"
      pattern: "score|bm25"
---

<objective>
Implement the mem-mcp reference server (TEAM-04): a `TeamMemoryBackend` object-safe async trait (6 `mem__*` operations) with an in-memory dev backend whose `mem__search` ranks by a hand-rolled, zero-dependency keyword/BM25 scorer (NO embedder dependency — TEAM-04 + milestone out-of-scope; the `bm25` crate is rejected for bundling an embedder). Apply the 109-03 review's correctness fixes: short-circuit the scorer to 0.0 when the corpus is empty or average document length is 0 (no division-by-zero), floor IDF at 0.0 (or use smoothed IDF) so common terms are never penalized negatively, replace the invalid monotonicity property with safer invariants, give the backend a deterministic ID/clock seam (so conformance fixtures get stable ids like `mem-001` instead of random UUIDs), declare `parking_lot` as the explicit sync primitive, add configurable dev limits, and specify stable tie-breaking (score desc, then creation ordinal). Wire the `mem-mcp` Server builder (exactly 6 tools, unknown names error) and fill the HTTP-first `mem-mcp` dev binary.

Purpose: Ship the memory reference server on the same trait-seam model as team-fs, with a numerically-safe, deterministic, property-tested keyword scorer and stable ids for conformance.
Output: `src/mem/{backend,bm25,server}.rs`, `src/bin/mem_mcp.rs`, and `tests/mem_props.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
From crates/pmcp-tasks/src/store/backend.rs (object-safe async trait + error enum + in-file exhaustive tests — same model as the fs backend).
From crates/pmcp-agent/src/adapter/server.rs lines 211-258 (Server::builder + TypedTool + Arc-cloning handler).
From crates/pmcp-sql-server (thin bin + StreamableHttpServer serve pattern — identical to team-fs bin).
Contract (contracts/team-servers-v1.yaml `mem_tool_surface`): the 6 mem__* names are exactly `mem__add mem__get mem__search mem__list_recent mem__delete mem__complete_task`. `mem__complete_task` follows SEP-1686 (ToolOutput::Result, related-task under RELATED_TASK_META_KEY).
`parking_lot` is a declared crate dependency (109-01). NOTE: `src/mem/bm25.rs` has NO in-repo analog — hand-roll per RESEARCH "Don't Hand-Roll"; prove via proptest invariants.
Cargo accepts ONE positional test filter; use a single module substring (e.g. `mem`) per `cargo test` invocation, never two positional filters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-rolled BM25 scorer (zero-dep, L_avg==0 short-circuit + IDF floor) + TeamMemoryBackend trait + deterministic in-memory backend</name>
  <files>crates/pmcp-team-servers/src/mem/bm25.rs, crates/pmcp-team-servers/src/mem/backend.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/mem/bm25.rs, src/mem/backend.rs (empty documented modules from 109-01)
    - crates/pmcp-tasks/src/store/backend.rs (trait + error enum + in-file test density to mirror)
    - .planning/phases/109-team-reference-servers/109-REVIEWS.md (109-03 BM25 edge-case + tie-break + determinism concerns)
    - .planning/phases/109-team-reference-servers/109-RESEARCH.md (Alternatives Considered: hand-roll recommended, bm25 crate rejected)
  </read_first>
  <behavior>
    - tokenize lowercases + splits on non-alphanumeric; deterministic
    - score is EXACTLY 0.0 when: corpus empty, average doc length is 0, or no query term appears in the doc
    - score is strictly positive when at least one query term appears; score is always finite (never NaN/Inf) and non-negative
    - IDF is floored at 0.0 (or smoothed `ln(1 + (N-n+0.5)/(n+0.5))`) so a term appearing in >half the docs never contributes a negative penalty
    - determinism: identical (corpus, query) → identical ranking, every time
    - stable tie-break: equal scores order by creation ordinal (then id) — never nondeterministic
    - at FIXED document length, increasing a query term's frequency does not decrease that doc's score relative to a doc lacking the term
  </behavior>
  <action>
    In `src/mem/bm25.rs` implement a self-contained scorer: `tokenize(&str) -> Vec<String>` (lowercase, split on non-alphanumeric), a corpus/index type holding per-doc term frequencies + doc lengths + a document-frequency map + corpus size, and `score(query_terms, doc_id) -> f64` implementing BM25 (k1≈1.2, b≈0.75). CRITICAL numeric guards (109-03 review): if corpus size == 0 OR average document length `L_avg == 0.0`, return `0.0` immediately (no division by zero); compute IDF as `((N - n + 0.5)/(n + 0.5)).ln().max(0.0)` (floor at 0) OR the smoothed form `(1.0 + (N - n + 0.5)/(n + 0.5)).ln()` — document which and why (common-term terms never contribute negative score). Document the k1/b constants and the formula. In-file `#[cfg(test)] mod tests`: empty corpus → 0.0; empty query → 0.0; single-doc term-present > term-absent; a term present in every doc yields a NON-negative contribution.
    In `src/mem/backend.rs` define `#[async_trait] pub trait TeamMemoryBackend: Send + Sync` with the 6 ops `add`, `get`, `search`, `list_recent`, `delete`, `complete_task` (each `Result<_, MemError>` with `# Errors` rustdoc). Define `pub enum MemError` via `thiserror` (`NotFound`, `InvalidArgs`, `LimitExceeded`). Define a `Memory` record (`serde` derives: id, text, optional tags, `created_ordinal: u64`). Introduce a DETERMINISTIC ID/CLOCK SEAM: a small `IdSource` trait (or an injectable `next_id: impl FnMut -> String` + monotonic ordinal counter) so tests/conformance inject a deterministic sequence (`mem-001`, `mem-002`, …) while production uses uuid v4; the backend constructor takes the seam (default = uuid). Implement `pub struct InMemoryMemoryBackend` using `parking_lot::RwLock` over `Vec<Memory>` + the bm25 index, with CONFIGURABLE DEV LIMITS (max item count, max text length, max query length, max result limit) returning `MemError::LimitExceeded` past them: `add` mints an id via the seam and appends (bumping the ordinal), `get`/`delete` by id, `list_recent` newest-first by ordinal, `search` builds/uses the bm25 index and returns ranked records with STABLE tie-break (score desc, then creation ordinal ascending, then id). In-file tests: add→get round-trip, delete removal, list_recent ordering, search relevance ordering, deterministic-id seam yields `mem-001…`, tie-break stability, and a limit-exceeded path.
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers mem 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/mem/bm25.rs` has `fn score`, documents k1/b, floors/smooths IDF, and short-circuits to 0.0 when corpus empty / L_avg==0; no `use bm25`/`use tantivy` anywhere in the crate
    - `src/mem/backend.rs` has `pub trait TeamMemoryBackend` (6 ops) + `pub enum MemError` + a deterministic ID/clock seam and configurable dev limits
    - tests prove: empty-corpus score 0.0, L_avg==0 no panic, common-term IDF non-negative, deterministic `mem-001…` ids, stable tie-break, and a limit-exceeded path
    - `cargo test -p pmcp-team-servers mem` exits 0
  </acceptance_criteria>
  <done>Numerically-safe zero-dep BM25 scorer + TeamMemoryBackend + deterministic, limit-bounded in-memory backend implemented and unit-tested; no embedder/registry search dependency.</done>
</task>

<task type="auto">
  <name>Task 2: mem-mcp Server builder (6 mem__* tools) + HTTP-first dev binary + BM25 safe-invariant proptests</name>
  <files>crates/pmcp-team-servers/src/mem/server.rs, crates/pmcp-team-servers/src/bin/mem_mcp.rs, crates/pmcp-team-servers/tests/mem_props.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/mem/backend.rs (TeamMemoryBackend + scorer from Task 1)
    - crates/pmcp-team-servers/src/mem/server.rs (empty documented module from 109-01)
    - crates/pmcp-team-servers/src/fs/server.rs (sibling server-builder pattern from 109-02)
    - crates/pmcp-sql-server/src/main.rs + src/lib.rs lines 156-164 (bin + serve)
    - .planning/phases/109-team-reference-servers/109-REVIEWS.md (109-03: replace monotonicity property; add mem_props.rs to files_modified)
  </read_first>
  <action>
    In `src/mem/server.rs` add `pub fn build_mem_mcp_server(backend: Arc<dyn TeamMemoryBackend>) -> pmcp::Result<Server>` registering exactly 6 `TypedTool`s named `mem__add … mem__complete_task`, each with an explicit input schema and a closure handler cloning the backend Arc and dispatching. The fixed 6-tool set means unknown `mem__*` names are not advertised (pmcp returns "not found", never panic). `mem__complete_task` returns via `ToolOutput::Result` with related-task `_meta` under `RELATED_TASK_META_KEY` when it carries one. Keep the builder CLI-free.
    Overwrite `src/bin/mem_mcp.rs` with the same thin `#[tokio::main]` + clap `Args` shape as team-fs (`--package`, `--port`, `--data-dir`, `--stdio`): load the TeamPackage, construct the in-memory backend (uuid id seam in production), build via `build_mem_mcp_server`, `#[cfg(feature="http")]` serve HTTP-by-default with stdio fallback, stdio-only when built without `http`. Init `tracing-subscriber`.
    Create `tests/mem_props.rs` (added to files_modified per review) with SAFE proptest invariants (replacing the invalid length-normalization monotonicity claim): (a) NON-NEGATIVITY — every score ≥ 0.0; (b) ZERO FOR NO OVERLAP — a doc sharing no query term scores 0.0; (c) DETERMINISM — identical (corpus, query) yields identical ranking across repeated runs; (d) FINITE — no NaN/Inf for any generated input; (e) STABLE TIE-BREAK — equal-score docs always order by creation ordinal; (f) TF-MONOTONICITY AT FIXED DOC LENGTH — increasing a query term's frequency in a doc of fixed length does not lower its score versus a same-length doc lacking the term. This is the ALWAYS property requirement for mem-mcp.
  </action>
  <verify>
    <automated>cargo build -p pmcp-team-servers --features "mem-mcp http" --bin mem-mcp && cargo test -p pmcp-team-servers --test mem_props 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/mem/server.rs` has `pub fn build_mem_mcp_server` registering all 6 mem__* names
    - a test asserts `tools/list` returns exactly the 6 mem__* names
    - `tests/mem_props.rs` proves non-negativity, zero-for-no-overlap, determinism, finiteness, stable tie-break, and fixed-length tf-monotonicity (NOT the invalid global monotonicity)
    - `cargo build -p pmcp-team-servers --features "mem-mcp http" --bin mem-mcp` exits 0
    - `src/bin/mem_mcp.rs` contains `--package`, `--stdio`, a `#[cfg(feature="http")]` StreamableHttpServer path, and an stdio-only path
  </acceptance_criteria>
  <done>mem-mcp advertises exactly 6 mem__* tools over the TeamMemoryBackend, ranks via the numerically-safe property-tested scorer, and runs as an HTTP-first (under http feature) dev binary with stdio otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → mem__* tool args | Untrusted memory text/query strings cross into the in-memory store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-03-01 | Denial of Service | scorer division-by-zero / unbounded growth | mitigate | L_avg==0 / empty-corpus short-circuit to 0.0 (no div-by-zero); configurable dev limits (item/text/query/result caps) return LimitExceeded (Task 1). |
| T-109-03-02 | Tampering | malformed/huge query into the scorer | mitigate | tokenize + score are total functions returning finite non-negative scores; proptest covers adversarial inputs (Task 2). |
| T-109-03-SC | Tampering | dependency graph | mitigate | No new registry packages — `bm25`/`tantivy` NOT adopted; scorer hand-rolled zero-dep; `parking_lot` is an existing workspace dep. |
</threat_model>

<verification>
- `cargo test -p pmcp-team-servers mem` green (scorer edge cases, backend round-trips, deterministic ids, exact 6-tool surface)
- `cargo test -p pmcp-team-servers --test mem_props` green (safe invariants)
- `cargo build -p pmcp-team-servers --features "mem-mcp http" --bin mem-mcp` succeeds
</verification>

<success_criteria>
- TEAM-04 satisfied: `mem__*` over a `TeamMemoryBackend` trait with a keyword/BM25 in-memory dev backend, no embedder dependency
- Scorer is numerically safe (no div-by-zero, non-negative, finite) and deterministic with stable tie-break; deterministic id/clock seam for conformance
- Exact 6-tool surface; HTTP-first under http feature with stdio otherwise
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-03-SUMMARY.md` when done
</output>
