---
phase: 109-team-reference-servers
plan: 05
type: execute
wave: 2
depends_on: ["109-00", "109-01"]
files_modified:
  - crates/pmcp-team-servers/src/team/guards.rs
  - crates/pmcp-team-servers/src/team/member.rs
  - crates/pmcp-team-servers/src/team/server.rs
  - crates/pmcp-team-servers/src/compose/resolver.rs
  - crates/pmcp-team-servers/src/bin/team_mcp.rs
  - crates/pmcp-team-servers/tests/team_props.rs
  - crates/pmcp-team-servers/fuzz/fuzz_targets/team_guards.rs
autonomous: true
requirements: [TEAM-05]
must_haves:
  truths:
    - "team-mcp advertises one team_mcp__<member> tool per roster member (dynamic family), member identity derived from the member's ComponentRef via MemberId (TeamMember has no separate id field)"
    - "Guard state (depth, caller id, ancestor chain) is read from the request's namespaced _meta via RequestHandlerExtra.request_meta (enabled by 109-00)"
    - "The member hop uses the new Client::call_tool_with_task_and_meta forwarding both task augmentation AND the incremented guard _meta"
    - "The member-task forwarding contract is explicit: for a ToolCallResponse::Task, team-mcp polls the member task to terminal (wait_for_task) and synthesizes a synchronous CallToolResult carrying related-task _meta under RELATED_TASK_META_KEY; for a ToolCallResponse::Result it re-emits, stripping member _meta beyond related-task"
    - "x-pmcp-team-depth is parsed strictly (garbage → Error, never 0); absent depth means root call (depth 0)"
    - "depth > max_team_depth, self-call (MemberId compare), and ancestor-cycle all yield errors"
    - "resolve_member_factory resolves the member's MANDATORY AgentPackage.llm ConfigSlot via SlotResolver into a concrete CompletionSourceFactory, OR uses an explicitly injected factory override (for tests/CI/FixedSource) — there is no unreachable no-slot branch"
    - "The team-mcp binary resolves each member ComponentRef into an AgentPackage via a PackageResolver"
  artifacts:
    - path: "crates/pmcp-team-servers/src/team/guards.rs"
      provides: "strict depth parse + self-call + ancestor-cycle guards over namespaced _meta from RequestHandlerExtra"
      exports: ["parse_depth_strict"]
    - path: "crates/pmcp-team-servers/src/team/member.rs"
      provides: "per-member pmcp::Client over DuplexTransport; task+_meta-forwarding dispatch with explicit task-forwarding contract; resolve_member_factory (injected-override, D-15)"
      contains: "call_tool_with_task_and_meta"
    - path: "crates/pmcp-team-servers/src/team/server.rs"
      provides: "team-mcp Server; team_mcp__<member> → ToolOutput::Result + related-task under RELATED_TASK_META_KEY"
      contains: "ToolOutput::Result"
    - path: "crates/pmcp-team-servers/src/compose/resolver.rs"
      provides: "LocalDirPackageResolver (ComponentRef -> AgentPackage from a local dir) impl of the 109-01 PackageResolver trait"
      exports: ["LocalDirPackageResolver"]
  key_links:
    - from: "src/team/server.rs (handle_output)"
      to: "src/team/member.rs (member hop)"
      via: "guards from request_meta → task+_meta forward → synthesize/re-emit"
      pattern: "ToolOutput::Result"
    - from: "src/team/member.rs"
      to: "pmcp::Client::call_tool_with_task_and_meta"
      via: "member dispatch forwarding task + guard _meta"
      pattern: "call_tool_with_task_and_meta"
    - from: "src/bin/team_mcp.rs"
      to: "compose::resolver::LocalDirPackageResolver + team::member::resolve_member_factory"
      via: "resolve each member ComponentRef → AgentPackage → CompletionSourceFactory"
      pattern: "resolve_member_factory|LocalDirPackageResolver"
---

<objective>
Implement the team-mcp reference server (TEAM-05) — the worked migration template replacing the platform's raw-JSON-RPC bypass — corrected against every 109-05 review HIGH. Guard state now travels as namespaced `_meta` on `tools/call` for real (via the 109-00 pmcp-core enablement: extensible `RequestMeta` + `RequestHandlerExtra.request_meta`); the handler reads depth/caller/ancestor from `extra.request_meta`. Each roster member is a Phase 108 `AgentServer` reached over an in-memory `DuplexTransport` via a `pmcp::Client` per member (D-13), identified by a `MemberId` DERIVED FROM the member's `ComponentRef` (TeamMember has no separate id/display-name field). The member hop uses the NEW `Client::call_tool_with_task_and_meta` (109-00) to forward BOTH task augmentation AND the incremented guard `_meta`. The member-task forwarding contract is EXPLICIT (`MemberTaskForwarding::Synthesize`, defined in 109-01): a required member tool returns `ToolCallResponse::Task`, so team-mcp polls it to terminal via `Client::wait_for_task` and SYNTHESIZES a synchronous `CallToolResult` carrying related-task `_meta` under the SDK constant `RELATED_TASK_META_KEY` (`io.modelcontextprotocol/related-task`); a `ToolCallResponse::Result` is re-emitted with member `_meta` stripped to related-task only (tight re-emit, Pitfall 5). LLM resolution is fixed: `AgentPackage.llm` is MANDATORY, so `resolve_member_factory` resolves the llm `ConfigSlot` via `SlotResolver` into a concrete source OR uses an EXPLICITLY INJECTED factory override for tests/CI/FixedSource — the old "no-slot fallback" branch (unreachable) is gone. The team-mcp binary resolves each member `ComponentRef` into an `AgentPackage` via a `PackageResolver` (a `LocalDirPackageResolver` dev impl) and maps the `x-pmcp-team-depth` HTTP header into `_meta` at the edge (D-14). Fill the ALWAYS `team_guards` fuzz target.

Purpose: Prove the real MCP hop surfaces related-task end-to-end with the CORRECT key and the CORRECT task-response handling — the single highest-risk deliverable — and ship a binary that resolves members from packages and runs them against real or injected LLMs.
Output: `src/team/{guards,member,server}.rs`, `src/compose/resolver.rs` (LocalDirPackageResolver), `src/bin/team_mcp.rs`, `tests/team_props.rs`, and the filled `team_guards` fuzz target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
From 109-00 (pmcp core, this phase's prerequisite):
- `RequestMeta::with_meta(key, value)` / `get_meta(key)` — arbitrary namespaced `_meta`.
- `RequestHandlerExtra.request_meta: Option<serde_json::Value>` — the request's `_meta` object visible to handlers.
- `Client::call_tool_with_task_and_meta(name, args, meta: RequestMeta) -> Result<ToolCallResponse>` — forwards task + `_meta`.
From src/client/mod.rs: `pub enum ToolCallResponse { Result(CallToolResult), Task(Task) }`; `Client::wait_for_task(task_id, WaitForTaskOptions)` and `wait_for_related_task(meta, opts)`; `CallToolResult::related_task() -> Option<TaskMetadata>`.
From src/types/tasks.rs:9: `pub const RELATED_TASK_META_KEY: &str = "io.modelcontextprotocol/related-task";` — use this everywhere; never a bare `related_task`.
From src/server/mod.rs lines 246-311 (ToolOutput): `pub enum ToolOutput { Payload(Value), Result(CallToolResult) }` — `Result` is verbatim + BYPASSES middleware (handler owns redaction — read the bypass warning FIRST). Override `ToolHandler::handle_output`.
From crates/pmcp-agent/src/adapter/server.rs: `AgentServer::builder(package, config, factory, invoker, store)...build()?` then `.run(transport)`; member returns bare answer for non-task calls (line 323); built `with_task_support(TaskSupport::Required)` (line 242) → the task-augmented call is required. `FixedSourceFactory::new(source)` (factory.rs) — the INJECTED override for CI (NOT a "no-slot fallback").
From crates/pmcp-agent/src/config/ (Phase 108 D-14/D-16): `SlotResolver` trait with `resolve_slot(&ConfigSlot) -> Result<ResolvedValue>` (returns a model/provider string + endpoint/secret via the endpoint map); `EnvVarResolver::new()`. `CompletionSourceFactory` is what a resolved llm slot maps to — resolve_member_factory constructs the concrete OpenAI-compat/Anthropic source from the resolved value + endpoint/secret.
From crates/pmcp-package: `AgentPackage.llm: ConfigSlot` is MANDATORY (not Option); `TeamMember { agent: ComponentRef, role }`; `TeamPackage.limits.max_team_depth`.
From crates/pmcp-team-servers (109-01): `transport::DuplexTransport::pair()`; `compose::resolver::PackageResolver` trait + `ResolveError`; `team::identity::{MemberId, MemberTaskForwarding}`.
From crates/pmcp-team-servers/fuzz/ (109-01): `team_guards` `[[bin]]` already declared — fill ONLY `fuzz_targets/team_guards.rs`; do NOT edit `fuzz/Cargo.toml`.
Cargo accepts ONE positional test filter per invocation.
Contract (`team_dispatch_surface`): lookup by MemberId (from ComponentRef); strict depth parse or Error; depth>max_team_depth Error; self-call (target MemberId == caller MemberId) Error; ancestor-cycle Error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guards (strict depth, self-call, ancestor-cycle) reading namespaced _meta from RequestHandlerExtra + proptest + team_guards fuzz body</name>
  <files>crates/pmcp-team-servers/src/team/guards.rs, crates/pmcp-team-servers/tests/team_props.rs, crates/pmcp-team-servers/fuzz/fuzz_targets/team_guards.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/team/guards.rs (empty documented module from 109-01)
    - crates/pmcp-team-servers/src/team/identity.rs (MemberId from 109-01)
    - crates/pmcp-team-servers/fuzz/fuzz_targets/team_guards.rs (no-op stub from 109-01 — replace the body)
    - src/server/cancellation.rs (RequestHandlerExtra.request_meta from 109-00)
    - contracts/team-servers-v1.yaml `team_dispatch_surface` (guard equations)
    - .planning/phases/109-team-reference-servers/109-REVIEWS.md (109-05: IDs-not-names collapses to ComponentRef identity; missing-depth semantics; strict parse)
  </read_first>
  <behavior>
    - parse_depth_strict("3") == 3; parse_depth_strict("x")/("")/("1.5")/("-") → Error (never 0)
    - guard state is read from the namespaced _meta on RequestHandlerExtra.request_meta (keys are crate constants, e.g. `x-pmcp-team-depth`, caller id, ancestor chain)
    - ABSENT depth _meta means a root/entry call → depth 0 (documented); PRESENT-but-garbage depth → Error
    - guard_depth(depth, max): depth>max → Error; depth<=max → Ok
    - guard_self_call(target: MemberId, caller: MemberId): equal → Error; different → Ok
    - guard_ancestor_cycle(target: MemberId, ancestors: &[MemberId]): target in ancestors → Error; absent → Ok
    - unknown member id → Error("unknown member")
  </behavior>
  <action>
    In `src/team/guards.rs` define crate constants for the namespaced `_meta` keys (e.g. `pub const META_DEPTH: &str = "x-pmcp-team-depth";`, caller-id, ancestor-chain keys — follow pmcp `_meta` conventions). Implement `pub fn read_guard_state(extra: &RequestHandlerExtra) -> Result<GuardState, GuardError>` that reads `extra.request_meta` (the 109-00-propagated request `_meta`) and extracts depth/caller/ancestors: an ABSENT depth key means a root call (`depth = 0`); a PRESENT depth is parsed by `parse_depth_strict`. Implement `pub fn parse_depth_strict(raw: &str) -> Result<i64, GuardError>` via `str::parse::<i64>()` returning `GuardError::MalformedDepth` on ANY failure (garbage NEVER defaults to 0). `guard_depth(depth, max) -> Result<(), GuardError>` (Error when depth > max). `guard_self_call(target: &MemberId, caller: &MemberId)` comparing MemberId (derived from ComponentRef — NOT tool display strings). `guard_ancestor_cycle(target: &MemberId, ancestors: &[MemberId])`. A `lookup_member(id, roster)` erroring `unknown member`. `pub enum GuardError` (thiserror; `MalformedDepth`, `ExcessiveDepth`, `SelfCall`, `AncestorCycle`, `UnknownMember`) with messages matching the contract error text.
    In `tests/team_props.rs` add proptest coverage for all guard paths: strict depth parse rejects any non-integer string (never 0 for garbage); self-call rejects equal MemberIds and accepts distinct ones; ancestor-cycle rejects membership and accepts absence; depth bound behaves at/above/below max; absent-depth _meta yields depth 0.
    Fill the fuzz target `fuzz/fuzz_targets/team_guards.rs`: feed arbitrary bytes as the raw depth string + ancestor-chain data into `parse_depth_strict` and the guards, asserting no panic. Do NOT edit `fuzz/Cargo.toml`.
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers --test team_props 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/team/guards.rs` has `parse_depth_strict` (errors, not 0, for `"x"`/`""`/`"1.5"`) and `read_guard_state` reading `extra.request_meta`; absent depth → 0
    - self-call/ancestor guards compare `MemberId` (not display strings); contract-matching error messages
    - `tests/team_props.rs` has ≥5 proptest invariants (guards + absent-depth-is-0)
    - `fuzz/fuzz_targets/team_guards.rs` targets the guard/depth parser; `fuzz/Cargo.toml` unchanged
    - `cargo test -p pmcp-team-servers --test team_props` exits 0
  </acceptance_criteria>
  <done>All guards read guard state from the propagated request `_meta`, compare MemberId identities, parse depth strictly (absent=0, garbage=Error), and are property-tested + fuzzed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Member hop (task+_meta forward, explicit task-forwarding contract) + resolve_member_factory (injected override) + LocalDirPackageResolver + team-mcp server</name>
  <files>crates/pmcp-team-servers/src/team/member.rs, crates/pmcp-team-servers/src/team/server.rs, crates/pmcp-team-servers/src/compose/resolver.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/team/member.rs, src/team/server.rs (empty documented modules from 109-01)
    - crates/pmcp-team-servers/src/team/guards.rs (from Task 1) + src/team/identity.rs (MemberId, MemberTaskForwarding)
    - crates/pmcp-team-servers/src/compose/resolver.rs (PackageResolver trait from 109-01 — add LocalDirPackageResolver here)
    - crates/pmcp-team-servers/src/transport.rs (DuplexTransport::pair from 109-01)
    - src/client/mod.rs (call_tool_with_task_and_meta from 109-00; ToolCallResponse; wait_for_task; CallToolResult::related_task)
    - src/server/mod.rs lines 246-311 (ToolOutput::Result + bypass warning)
    - src/types/tasks.rs (RELATED_TASK_META_KEY)
    - crates/pmcp-agent/src/adapter/server.rs lines 121-330 (AgentServer::builder/run; non-task vs task return)
    - crates/pmcp-agent/src/adapter/factory.rs (FixedSourceFactory — the injected override) + crates/pmcp-agent/src/config/resolver.rs (SlotResolver/EnvVarResolver + resolve_slot returning model/provider + endpoint/secret)
  </read_first>
  <behavior>
    - dispatch to a live FixedSource member returns a CallToolResult whose top-level _meta[RELATED_TASK_META_KEY].taskId is present and non-null
    - when the member returns ToolCallResponse::Task, team-mcp polls it to terminal (wait_for_task) and synthesizes a result linked to that member task (related-task _meta under the correct key); when it returns Result, team-mcp re-emits with member _meta stripped to related-task only
    - guard error paths (malformed/excessive depth, self-call, ancestor-cycle, unknown member) each return an error outcome, no panic
    - resolve_member_factory returns the INJECTED override when one is supplied (tests/CI), else resolves the mandatory llm ConfigSlot via SlotResolver into a concrete CompletionSourceFactory
    - LocalDirPackageResolver loads an AgentPackage for a ComponentRef from a local directory
  </behavior>
  <action>
    In `src/compose/resolver.rs` add `pub struct LocalDirPackageResolver { root: PathBuf }` implementing the 109-01 `PackageResolver` trait: `resolve_agent(&self, r: &ComponentRef)` loads `<root>/<r.name()>[@version].json` (or a documented layout), deserializes an `AgentPackage`, and returns `ResolveError::NotFound`/`Parse` on failure. In-file tests round-trip a written AgentPackage file.
    In `src/team/member.rs` define `pub struct MemberHandle { id: MemberId, tool_name: String, client: Arc<Client<...>>, forwarding: MemberTaskForwarding }`. Add a spawn helper that, given a member `AgentServer`, uses `DuplexTransport::pair()`, spawns `agent_server.run(server_half)`, connects a `Client` on the client half, and `initialize`s once. Add `pub async fn dispatch(&self, args: Value, forward_meta: RequestMeta) -> Result<CallToolResult>` that calls `Client::call_tool_with_task_and_meta(self.tool_name, args, forward_meta)` (the 109-00 API — forwards task + the incremented guard `_meta`) and implements the EXPLICIT forwarding contract: match `ToolCallResponse::Result(r)` → return `r` (re-emit path); match `ToolCallResponse::Task(t)` → `Client::wait_for_task(t.id, WaitForTaskOptions::default())` to terminal, fetch the result, and SYNTHESIZE a `CallToolResult` carrying the member's content plus related-task `_meta` under `RELATED_TASK_META_KEY` (taskId = the member task id). Either way the returned `CallToolResult` carries related-task under the correct key.
    ALSO add the SHARED member-LLM resolver used by BOTH the dev binary (Task 3) and `TeamRuntime` (109-06): `pub async fn resolve_member_factory(agent_pkg: &AgentPackage, resolver: &dyn SlotResolver, override_factory: Option<Arc<dyn CompletionSourceFactory>>) -> Result<Arc<dyn CompletionSourceFactory>>`. Contract: if `override_factory` is `Some`, return it (the explicit injection for tests/CI/FixedSource — NOT a no-slot branch); otherwise resolve the member's MANDATORY `agent_pkg.llm` `ConfigSlot` via `resolver.resolve_slot(&agent_pkg.llm)` (which yields the model/provider string + endpoint/secret from the endpoint map) and CONSTRUCT the concrete `CompletionSourceFactory` (OpenAI-compat/Ollama or Anthropic per the resolved provider + endpoint + `RedactedSecret`). Keys flow through `RedactedSecret`; never log them.
    In `src/team/server.rs` add `pub fn build_team_mcp_server(members: Vec<MemberHandle>, max_team_depth: i64, roster: Vec<MemberId>) -> pmcp::Result<Server>` registering one `team_mcp__<member>` tool per member (dynamic family from the roster; tool name derived from MemberId). Each tool's handler OVERRIDES `handle_output` to: `read_guard_state(&extra)` (Task 1) → `guard_depth` → `guard_self_call` → `guard_ancestor_cycle` (and reject unknown member), then build the OUTGOING `RequestMeta` with depth+1, caller id, and target appended to the ancestor chain (via `RequestMeta::with_meta`), call `member.dispatch(args, forward_meta)`, and return `Ok(ToolOutput::Result(result))`. On the Result re-emit path, STRIP member `_meta` to related-task only (do not echo unsanitized member `_meta` — Pitfall 5); on the synthesize path the result already carries only related-task. Add integration tests: build a FixedSource member `AgentServer` (via `resolve_member_factory` with an injected FixedSourceFactory override), wire a `MemberHandle`, build team-mcp, drive a `team_mcp__<member>` call through a real `Client` over the crate `DuplexTransport` with depth `_meta`, and assert the response `_meta[RELATED_TASK_META_KEY].taskId` is present (mirrors `team_mcp__member.success`). Error-path tests: malformed-depth, excessive-depth, self-call, ancestor-cycle, unknown-member. Unit test `resolve_member_factory`: injected override returned as-is; and a slot-resolved factory constructed from a stub resolver returning a provider+endpoint. Member identity: assert the `team_mcp__<member>` name and lookups use `MemberId::from_ref` (identity IS the ComponentRef; distinct members have distinct MemberIds) — do NOT attempt a same-name/different-id test (no such field exists).
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers team:: 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `src/team/member.rs` calls `call_tool_with_task_and_meta` (grep; no plain `call_tool(` for the member hop) and handles BOTH `ToolCallResponse::Task` (wait_for_task + synthesize) and `Result` (re-emit)
    - the synthesized/re-emitted result carries related-task under `RELATED_TASK_META_KEY` (grep; no bare `related_task`)
    - `resolve_member_factory` returns the injected override when supplied, else resolves the mandatory llm slot — NO "no llm slot" branch
    - `src/compose/resolver.rs` has `LocalDirPackageResolver` implementing `PackageResolver`
    - `src/team/server.rs` overrides `handle_output`, returns `ToolOutput::Result(`, and strips member `_meta` to related-task on re-emit
    - a passing integration test asserts a successful dispatch's response contains a non-null `_meta[RELATED_TASK_META_KEY].taskId`; error-path tests cover all four guards + unknown-member
    - `cargo test -p pmcp-team-servers team::` exits 0
  </acceptance_criteria>
  <done>team-mcp dispatches members via a task+_meta-forwarding hop with an explicit task-forwarding contract (poll-and-synthesize or re-emit), surfaces related-task under the correct key, resolves member LLMs via SlotResolver or an injected override, resolves member packages via LocalDirPackageResolver, and enforces all guards.</done>
</task>

<task type="auto">
  <name>Task 3: HTTP-first team-mcp binary — PackageResolver-driven member wiring, SlotResolver LLM (D-15), x-pmcp-team-depth header → _meta edge map</name>
  <files>crates/pmcp-team-servers/src/bin/team_mcp.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/team/server.rs + src/team/member.rs (resolve_member_factory, MemberHandle spawn — Task 2)
    - crates/pmcp-team-servers/src/compose/resolver.rs (LocalDirPackageResolver — Task 2)
    - crates/pmcp-team-servers/src/bin/team_mcp.rs (stub from 109-01)
    - crates/pmcp-agent/src/config/resolver.rs (EnvVarResolver::new — the dev-binary resolver)
    - crates/pmcp-sql-server/src/main.rs + src/lib.rs lines 156-170 (bin + StreamableHttpServer serve)
    - crates/pmcp-package/src/package/team.rs (TeamPackage.limits.max_team_depth, members)
  </read_first>
  <action>
    Overwrite `src/bin/team_mcp.rs` with a thin `#[tokio::main]` + clap `Args` (`--package`, `--port`, `--data-dir` = the AgentPackage resolver root, `--stdio`). Load the `TeamPackage`, read `limits.max_team_depth` and the member roster. Construct a `LocalDirPackageResolver` (rooted at `--data-dir`) and an `EnvVarResolver::new()` SlotResolver ONCE. For EACH member: `resolver.resolve_agent(&member.agent)` → `AgentPackage`, then `resolve_member_factory(&agent_pkg, &slot_resolver, None)` (None = resolve the mandatory llm slot; a member with a configured Ollama/Anthropic slot runs against it) to get its `CompletionSourceFactory`, build the member `AgentServer`, and wrap it as a `MemberHandle` (id = `MemberId::from_ref(&member.agent)`). This is the SAME member-wiring path `TeamRuntime` (109-06) uses. Build via `build_team_mcp_server(members, max_team_depth, roster)`, and `#[cfg(feature="http")]` serve HTTP-by-default via `StreamableHttpServer` (SDK owns DNS-rebinding/CORS) with stdio fallback, stdio-only otherwise. At the HTTP edge, map an incoming `x-pmcp-team-depth` HTTP header into the request's namespaced `_meta` (via the 109-00 `RequestMeta`/request-meta mechanism) BEFORE dispatch (D-14) so guards work identically in-memory and over HTTP — new binary-layer edge code. Init `tracing-subscriber`.
  </action>
  <verify>
    <automated>cargo build -p pmcp-team-servers --features "team-mcp http" --bin team-mcp 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p pmcp-team-servers --features "team-mcp http" --bin team-mcp` exits 0
    - `src/bin/team_mcp.rs` resolves each member via `LocalDirPackageResolver` + `resolve_member_factory` (grep), passing `None` for the override so real slots resolve
    - `src/bin/team_mcp.rs` contains `--package`, `--stdio`, reads `max_team_depth`, maps `x-pmcp-team-depth` into `_meta`, and has a `#[cfg(feature="http")]` StreamableHttpServer path
  </acceptance_criteria>
  <done>team-mcp runs as an HTTP-first (under http feature) dev binary that resolves each member ComponentRef→AgentPackage via a PackageResolver, resolves each member's mandatory LLM slot via SlotResolver, and maps the depth header into _meta at the edge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → team_mcp__<member> args + _meta | Untrusted depth/caller/ancestor guard state crosses into dispatch (read via RequestHandlerExtra.request_meta) |
| team-mcp → member AgentServer (in-process hop) | Trusted in-process member envelope, re-emitted tight (member _meta stripped to related-task) |
| HTTP header → _meta | x-pmcp-team-depth header mapped into guard state at the edge |
| member AgentPackage llm ConfigSlot → SlotResolver | Member LLM credentials resolved via Phase 108 RedactedSecret machinery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05-01 | Denial of Service | unbounded recursion (missing/forged/garbage depth) | mitigate | Absent depth = 0 (root); garbage → Error (never 0); depth>max reject + ancestor-cycle guard; property-tested + fuzzed (Task 1). HIGH — blocking. |
| T-109-05-02 | Denial of Service | self-call / cycle loop between members | mitigate | Self-call guard (MemberId compare) + ancestor-chain guard (Task 1/2). HIGH — blocking. |
| T-109-05-03 | Tampering | unsanitized member `_meta` re-emit via ToolOutput::Result | mitigate | Re-emit strips member `_meta` to related-task only; synthesize path carries only related-task under the correct key (Task 2, Pitfall 5). |
| T-109-05-04 | Spoofing | member lookup by display name allows impersonation | mitigate | Lookup by MemberId derived from ComponentRef; guards compare MemberId (Task 1/2). |
| T-109-05-05 | Information Disclosure | member LLM key handling in resolve_member_factory | mitigate | Keys flow through SlotResolver/RedactedSecret; never logged; injected FixedSource override needs no secret (Task 2/3). |
| T-109-05-SC | Tampering | dependency graph | mitigate | No new registry package; `libfuzzer-sys` fuzz-only in the excluded sub-package; core deps are in-repo path crates. |
</threat_model>

<verification>
- `cargo test -p pmcp-team-servers team::` + `--test team_props` green (guards from request_meta, member hop related-task under correct key, task-forwarding contract, resolve_member_factory override + slot branches, LocalDirPackageResolver, all error paths)
- `cargo build -p pmcp-team-servers --features "team-mcp http" --bin team-mcp` succeeds
- `team_guards` fuzz target filled
</verification>

<success_criteria>
- TEAM-05 satisfied: per-member tools return ToolOutput::Result with top-level related-task under RELATED_TASK_META_KEY, via a task+_meta-forwarding hop with an explicit Task/Result forwarding contract
- Guard state travels via the 109-00 namespaced _meta; guards compare MemberId (ComponentRef identity); strict depth (absent=0, garbage=Error); property-tested + fuzzed
- Member LLM via SlotResolver on the mandatory llm slot OR an explicit injected override (no unreachable no-slot branch); members resolved from packages via PackageResolver; depth header→_meta at the edge (D-14)
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-05-SUMMARY.md` when done
</output>
