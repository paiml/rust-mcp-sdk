---
phase: 109-team-reference-servers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - CLAUDE.md
  - crates/pmcp-team-servers/Cargo.toml
  - crates/pmcp-team-servers/src/lib.rs
  - crates/pmcp-team-servers/src/transport.rs
  - crates/pmcp-team-servers/src/compose/mod.rs
  - crates/pmcp-team-servers/src/compose/derive.rs
  - crates/pmcp-team-servers/src/compose/wiring.rs
  - crates/pmcp-team-servers/src/compose/resolver.rs
  - crates/pmcp-team-servers/src/fs/mod.rs
  - crates/pmcp-team-servers/src/fs/backend.rs
  - crates/pmcp-team-servers/src/fs/local.rs
  - crates/pmcp-team-servers/src/fs/server.rs
  - crates/pmcp-team-servers/src/mem/mod.rs
  - crates/pmcp-team-servers/src/mem/backend.rs
  - crates/pmcp-team-servers/src/mem/bm25.rs
  - crates/pmcp-team-servers/src/mem/server.rs
  - crates/pmcp-team-servers/src/approval/mod.rs
  - crates/pmcp-team-servers/src/approval/channels.rs
  - crates/pmcp-team-servers/src/approval/repository.rs
  - crates/pmcp-team-servers/src/approval/server.rs
  - crates/pmcp-team-servers/src/team/mod.rs
  - crates/pmcp-team-servers/src/team/identity.rs
  - crates/pmcp-team-servers/src/team/member.rs
  - crates/pmcp-team-servers/src/team/guards.rs
  - crates/pmcp-team-servers/src/team/server.rs
  - crates/pmcp-team-servers/src/conformance/mod.rs
  - crates/pmcp-team-servers/src/conformance/runner.rs
  - crates/pmcp-team-servers/src/bin/team_fs.rs
  - crates/pmcp-team-servers/src/bin/mem_mcp.rs
  - crates/pmcp-team-servers/src/bin/approval_mcp.rs
  - crates/pmcp-team-servers/src/bin/team_mcp.rs
  - crates/pmcp-team-servers/fuzz/Cargo.toml
  - crates/pmcp-team-servers/fuzz/fuzz_targets/team_guards.rs
  - crates/pmcp-team-servers/fuzz/fuzz_targets/fs_resolve.rs
  - crates/pmcp-team-servers/tests/derive_props.rs
  - contracts/team-servers-v1.yaml
  - contracts/team-servers/binding.yaml
autonomous: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "The workspace builds with the new crate: `cargo build -p pmcp-team-servers --all-features` succeeds"
    - "Reduced-feature builds work (per-server isolation): `cargo build -p pmcp-team-servers --no-default-features --features team-fs` succeeds"
    - "`derive_attachment` returns team_mcp=true iff members>=2 and approval_mcp=true iff human_roles non-empty, implemented atomically (never exported as a placeholder)"
    - "A PackageResolver seam (ComponentRef -> AgentPackage) and a MemberId identity type (derived from ComponentRef) exist for downstream plans"
    - "A crate-local in-memory DuplexTransport is exported for non-test use"
    - "The contract rev documents the related-task key as the SDK constant io.modelcontextprotocol/related-task (never a bare related_task)"
    - "A binding.yaml skeleton (status: planned) exists before the servers are implemented (contract-first)"
    - "The existing structural conformance test still passes after the contract rev"
  artifacts:
    - path: "crates/pmcp-team-servers/Cargo.toml"
      provides: "Feature-gated crate manifest with a deliberately-documented default feature set"
      contains: "[features]"
    - path: "crates/pmcp-team-servers/src/compose/derive.rs"
      provides: "Pure derive_attachment fn + AttachmentSet + composition snapshot (implemented atomically)"
      exports: ["derive_attachment", "AttachmentSet"]
    - path: "crates/pmcp-team-servers/src/compose/resolver.rs"
      provides: "PackageResolver trait (ComponentRef -> AgentPackage) seam"
      exports: ["PackageResolver"]
    - path: "crates/pmcp-team-servers/src/team/identity.rs"
      provides: "MemberId identity type derived from ComponentRef + MemberTaskForwarding contract enum"
      exports: ["MemberId"]
    - path: "contracts/team-servers/binding.yaml"
      provides: "binding.yaml skeleton (status: planned) authored contract-first"
      contains: "target_crate: pmcp-team-servers"
  key_links:
    - from: "Cargo.toml (root [workspace] members)"
      to: "crates/pmcp-team-servers"
      via: "workspace member entry"
      pattern: "crates/pmcp-team-servers"
    - from: "crates/pmcp-team-servers/src/compose/derive.rs"
      to: "pmcp_package::TeamPackage"
      via: "reads members.len() and human_roles.is_empty()"
      pattern: "members\\.len\\(\\)|human_roles"
    - from: "crates/pmcp-team-servers/src/compose/resolver.rs"
      to: "pmcp_package::AgentPackage"
      via: "resolve_agent(ComponentRef) -> AgentPackage"
      pattern: "AgentPackage"
---

<objective>
Scaffold `crates/pmcp-team-servers` (0.x, experimental), lay down the compiling module skeleton as EMPTY DOCUMENTED modules (NO placeholder function bodies, NO "filled later" SATD comments — zero-SATD policy), promote the in-process `DuplexTransport` into `src/`, stand up the cargo-fuzz sub-package with both targets stubbed centrally, implement `derive_attachment` ATOMICALLY (D-05/D-07 — the first export is the real implementation, never a plausible-but-wrong placeholder), define the two seams the review flagged as missing — a `PackageResolver` (ComponentRef → AgentPackage) and a `MemberId` identity type derived from the member's `ComponentRef` (TeamMember has NO separate id/display-name field) — plus the member-task-forwarding contract enum, rev `team-servers-v1.yaml` additively using the CORRECT related-task key constant `io.modelcontextprotocol/related-task`, and author a contract-first `binding.yaml` skeleton (`status: planned`) after probing the real `pmat comply` CLI.

Purpose: Establish crate contracts + seams Wave 2 builds against, land the pure artifact atomically, own the fuzz manifest centrally, and satisfy contract-first — fixing every HIGH/MEDIUM 109-01 review concern (extensible `_meta` now real via 109-00; correct related-task key; no placeholder derive; no SATD; stdio bins buildable without HTTP; deliberate default features; resolver + member-identity seams).
Output: A building feature-flagged crate with documented modules, exported transport, PackageResolver + MemberId seams, a fuzz sub-package, an atomically-implemented property-tested `derive_attachment`, a rev'd contract (v1.1.0, correct key), and a `binding.yaml` skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From crates/pmcp-package/src/package/team.rs (wire-frozen 0.1 — do NOT change serialization):
- `pub struct TeamPackage { name, version, entry_point: ComponentRef, members: Vec<TeamMember>, human_roles: Vec<HumanRole>, limits: TeamLimits, built_in_servers: Vec<ComponentRef>, finalizer_agents, budget_defaults, config_slots }`
- `pub struct TeamMember { agent: ComponentRef, role: TeamRole }` — member identity IS the `agent` ComponentRef; NO separate stable-id/display-name field exists.
- `pub struct HumanRole { role, description, responsibilities, channel_hints }`; `pub struct TeamLimits { max_team_depth: i64, ... }`.

From crates/pmcp-package/src/reference.rs:
- `pub enum ComponentRef { Range { name, range, component_type }, Pinned(PinnedRef) }` with `.name()`, `.component_type()`, `.as_pinned()`, `.is_pinned()`. Version: `as_pinned().version` for pins; range for ranges.

From crates/pmcp-package/src/package/agent.rs:
- `pub struct AgentPackage { name, version, instructions, llm: ConfigSlot (MANDATORY — not Option), max_tokens, max_iterations, connectors, ... }`.

From crates/pmcp-agent/Cargo.toml: `pmcp = { version = "2.17.0", path = "../..", default-features = false }`; `reqwest = { ..., optional = true }`.
From crates/pmcp-sql-server/Cargo.toml: feature-gated `[[bin]]` + `http = ["pmcp/streamable-http"]` + tarball excludes.
From tests/common/duplex.rs: `pub struct DuplexTransport` + `pub fn pair() -> (Self, Self)` implementing `pmcp::shared::Transport`.
From src/types/tasks.rs:9: `pub const RELATED_TASK_META_KEY: &str = "io.modelcontextprotocol/related-task";` — the ONLY correct related-task key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core scaffold — manifest (deliberate features, stdio-buildable bins), workspace + publish wiring, lib.rs, promoted DuplexTransport</name>
  <files>Cargo.toml, CLAUDE.md, crates/pmcp-team-servers/Cargo.toml, crates/pmcp-team-servers/src/lib.rs, crates/pmcp-team-servers/src/transport.rs, crates/pmcp-team-servers/src/bin/team_fs.rs, crates/pmcp-team-servers/src/bin/mem_mcp.rs, crates/pmcp-team-servers/src/bin/approval_mcp.rs, crates/pmcp-team-servers/src/bin/team_mcp.rs</files>
  <read_first>
    - crates/pmcp-agent/Cargo.toml (core-dep pattern, wasm-clean default-features=false, optional reqwest, dev-deps)
    - crates/pmcp-sql-server/Cargo.toml (feature-gated [[bin]] + `http = ["pmcp/streamable-http"]` + tarball excludes)
    - tests/common/duplex.rs (the DuplexTransport to promote verbatim into src/transport.rs)
    - Cargo.toml (root [workspace] members list; fuzz-exclude note)
    - CLAUDE.md (Release & Publish Workflow "Workspace Crates (publish order)" — item 14 pmcp-agent is the anchor)
  </read_first>
  <action>
    Create `crates/pmcp-team-servers/Cargo.toml` mirroring `crates/pmcp-agent/Cargo.toml`: `pmcp = { version = "2.17.0", path = "../..", default-features = false }` (caret resolves to the local 2.17+ carrying the 109-00 `_meta` APIs — path dep provides source regardless of exact minor), `pmcp-agent = { version = "0.1", path = "../pmcp-agent" }`, `pmcp-package = { version = "0.1", path = "../pmcp-package" }`, plus `serde` (derive), `serde_json` (preserve_order), `async-trait`, `thiserror = "2.0"`, `uuid` (v4), `tracing`, `tokio` (macros, rt-multi-thread), `parking_lot` (the DECLARED sync primitive for in-memory backends, per 109-03 review), and `reqwest = { version = "0.13", default-features=false, features=["json","rustls"], optional = true }`. `clap = { version = "4", features = ["derive","env"] }` and `tracing-subscriber = { version="0.3", features=["env-filter"] }` are regular deps used only by the `[[bin]]` targets (wasm-compatible; do not describe as a special class). Dev-deps: `pmcp` `features=["full"]`, `tokio` full, `proptest = "1.7"`, `pretty_assertions = "1.4"`, `tempfile = "3"`, `semver = "1"`. `[package]` name `pmcp-team-servers`, version `0.1.0`, edition/rust matching siblings, exclude `.planning/`, `fuzz/`, `tests/` from the tarball.
    Define `[features]` with a DELIBERATE, comment-documented default (`# default = the full reference-server set; this crate's purpose IS all four servers`): `default = ["team-fs","mem-mcp","approval-mcp","team-mcp","conformance"]`; `team-fs = []`; `mem-mcp = []`; `approval-mcp = []`; `team-mcp = []`; `conformance = []`; `webhook = ["dep:reqwest"]`; `http = ["pmcp/streamable-http"]`; and an aggregate `runtime = ["team-fs","mem-mcp","approval-mcp","team-mcp"]` (compose::wiring in 109-06 cfg-gates attachment branches behind it). `webhook`+`http` are the ONLY non-default toggles (off by default → wasm/reqwest-clean). Add one `[[bin]]` per server with EXPLICIT hyphenated `name` + underscored `path` pairs (following the `crates/pmcp-sql-server/Cargo.toml` precedent so downstream plans' `--bin team-fs` / `env!("CARGO_BIN_EXE_team-fs")` resolve): `[[bin]] name = "team-fs", path = "src/bin/team_fs.rs", required-features = ["team-fs"]`; `name = "mem-mcp", path = "src/bin/mem_mcp.rs", required-features = ["mem-mcp"]`; `name = "approval-mcp", path = "src/bin/approval_mcp.rs", required-features = ["approval-mcp"]`; `name = "team-mcp", path = "src/bin/team_mcp.rs", required-features = ["team-mcp"]`. Each `required-features` is gated ONLY on its server feature, NOT on `http`, so a stdio-only build works without the HTTP stack (each bin's HTTP serve path is `#[cfg(feature="http")]`; without `http` it serves stdio only). Because `default` enables every server feature, all four `[[bin]]` targets are SELECTED in the default build and Cargo validates each declared `path` at manifest-resolution time — so in THIS task also create a TRIVIAL placeholder `fn main() {}` file at each of `src/bin/{team_fs,mem_mcp,approval_mcp,team_mcp}.rs` (Task 2 overwrites them with the real eprintln stubs) so `cargo build -p pmcp-team-servers` resolves every declared bin target rather than failing with "can't find bin ... at path src/bin/*.rs".
    Create `src/lib.rs` MINIMAL: crate-level doc + `pub mod transport;` + `pub use transport::DuplexTransport;`. Do NOT declare server modules or re-export `derive_attachment` yet.
    Promote `tests/common/duplex.rs` into `src/transport.rs`: copy `DuplexTransport` + `pair()` + its `pmcp::shared::Transport` impl, make `pub`, drop test-only framing (keep any wasm cfg the impl needs). Do NOT copy `call_via_server`/`call_via_core`.
    Add `"crates/pmcp-team-servers"` to root `[workspace] members`. Add a CLAUDE.md publish-order entry after item 14 `pmcp-agent` (0.x experimental leaf; failure must not gate the core SDK release; pins pmcp/pmcp-agent/pmcp-package via path deps).
  </action>
  <verify>
    <automated>cargo build -p pmcp-team-servers 2>&1 | tail -5 && cargo build -p pmcp-team-servers --no-default-features --features team-fs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p pmcp-team-servers` (default, transport-only lib) exits 0
    - `cargo build -p pmcp-team-servers --no-default-features --features team-fs` exits 0 (per-server isolation)
    - Cargo.toml contains `webhook = ["dep:reqwest"]`, `http = ["pmcp/streamable-http"]`, `runtime = [`, and `parking_lot` as a dep
    - the `pmcp` dep line contains `default-features = false`
    - each `[[bin]]` declares an explicit hyphenated `name = "team-fs"` (etc.) + underscored `path = "src/bin/team_fs.rs"`; `required-features` lists its server feature but NOT `http`; a placeholder `fn main() {}` file exists at each `src/bin/*.rs` path so the default-feature build resolves all four declared bin targets
    - root `Cargo.toml` `[workspace] members` contains `crates/pmcp-team-servers`; CLAUDE.md publish list has a `pmcp-team-servers` entry after `pmcp-agent`
    - `src/transport.rs` exports `pub struct DuplexTransport` with `pub fn pair(`; `src/lib.rs` has `pub use transport::DuplexTransport;`
  </acceptance_criteria>
  <done>Manifest (deliberate features, stdio-buildable bins), workspace + publish wiring, minimal lib.rs, and the promoted public DuplexTransport land and compile under default AND reduced features.</done>
</task>

<task type="auto">
  <name>Task 2: Empty documented module tree + PackageResolver + MemberId seams + bin stubs + cargo-fuzz sub-package</name>
  <files>crates/pmcp-team-servers/src/lib.rs, crates/pmcp-team-servers/src/compose/mod.rs, crates/pmcp-team-servers/src/compose/derive.rs, crates/pmcp-team-servers/src/compose/wiring.rs, crates/pmcp-team-servers/src/compose/resolver.rs, crates/pmcp-team-servers/src/fs/mod.rs, crates/pmcp-team-servers/src/fs/backend.rs, crates/pmcp-team-servers/src/fs/local.rs, crates/pmcp-team-servers/src/fs/server.rs, crates/pmcp-team-servers/src/mem/mod.rs, crates/pmcp-team-servers/src/mem/backend.rs, crates/pmcp-team-servers/src/mem/bm25.rs, crates/pmcp-team-servers/src/mem/server.rs, crates/pmcp-team-servers/src/approval/mod.rs, crates/pmcp-team-servers/src/approval/channels.rs, crates/pmcp-team-servers/src/approval/repository.rs, crates/pmcp-team-servers/src/approval/server.rs, crates/pmcp-team-servers/src/team/mod.rs, crates/pmcp-team-servers/src/team/identity.rs, crates/pmcp-team-servers/src/team/member.rs, crates/pmcp-team-servers/src/team/guards.rs, crates/pmcp-team-servers/src/team/server.rs, crates/pmcp-team-servers/src/conformance/mod.rs, crates/pmcp-team-servers/src/conformance/runner.rs, crates/pmcp-team-servers/src/bin/team_fs.rs, crates/pmcp-team-servers/src/bin/mem_mcp.rs, crates/pmcp-team-servers/src/bin/approval_mcp.rs, crates/pmcp-team-servers/src/bin/team_mcp.rs, crates/pmcp-team-servers/fuzz/Cargo.toml, crates/pmcp-team-servers/fuzz/fuzz_targets/team_guards.rs, crates/pmcp-team-servers/fuzz/fuzz_targets/fs_resolve.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/lib.rs (minimal from Task 1)
    - crates/pmcp-team-servers/Cargo.toml (features from Task 1)
    - crates/pmcp-package/src/reference.rs (ComponentRef accessors for MemberId)
    - crates/pmcp-package/src/package/agent.rs (AgentPackage — PackageResolver return type)
    - crates/pmcp-sql-server/src/main.rs (bin shape reference)
    - any repo `fuzz/Cargo.toml` (cargo-fuzz layout + libfuzzer-sys + [[bin]] target shape)
  </read_first>
  <action>
    Expand `src/lib.rs`: `pub mod compose;`, `#[cfg(feature="team-fs")] pub mod fs;`, `#[cfg(feature="mem-mcp")] pub mod mem;`, `#[cfg(feature="approval-mcp")] pub mod approval;`, `#[cfg(feature="team-mcp")] pub mod team;`, `#[cfg(feature="conformance")] pub mod conformance;`. Do NOT re-export `derive_attachment` yet (Task 3).
    Create the module tree as EMPTY DOCUMENTED modules — each leaf file gets a `//!` module doc stating what it will contain and which plan implements it (e.g. `//! team-fs local-directory backend (TEAM-02, implemented in 109-02).`). NO placeholder fn bodies, NO `todo!()`/`unimplemented!()`, NO inline `// filled by NNN` comments (that phrasing is SATD — the module doc replaces it). `compose/mod.rs`: `pub mod derive; pub mod wiring; pub mod resolver;`. `compose/derive.rs` + `compose/wiring.rs` empty documented (derive implemented in Task 3). `fs/mod.rs`: `pub mod backend; pub mod local; pub mod server;` (empty documented). Same for `mem/`, `approval/` (incl. `pub mod repository;`), `team/` (incl. `pub mod identity;`), `conformance/`.
    Implement TWO seams ATOMICALLY (contracts, not placeholders): In `src/compose/resolver.rs` define `#[async_trait::async_trait] pub trait PackageResolver: Send + Sync { async fn resolve_agent(&self, r: &pmcp_package::ComponentRef) -> Result<pmcp_package::AgentPackage, ResolveError>; }` + `pub enum ResolveError` (thiserror; `NotFound`, `Io`, `Parse`) with per-method `# Errors` rustdoc — the ComponentRef→AgentPackage seam 109-05/06 implement with a local file/dir impl. In `src/team/identity.rs` define `#[derive(Clone, PartialEq, Eq, Hash, Debug)] pub struct MemberId(String)` with `pub fn from_ref(r: &pmcp_package::ComponentRef) -> Self` deriving a STABLE identity from the ComponentRef (name + version discriminator, e.g. `format!("{}@{}", r.name(), version_str)`, so same-name/different-version members are distinct; document that identity IS the ComponentRef since TeamMember has no separate id field), `as_str()`, and `Display`. Also `pub enum MemberTaskForwarding { Synthesize, ReturnEnvelope }` documenting the 109-05 forwarding-contract choice (default `Synthesize`: poll the member task to terminal and synthesize a synchronous result carrying related-task `_meta`). Add in-file unit tests for `MemberId::from_ref` (same name/different version → distinct; same ref → equal).
    OVERWRITE the four placeholder bin stubs created in Task 1 (`src/bin/{team_fs,mem_mcp,approval_mcp,team_mcp}.rs`), each becoming `fn main() { eprintln!("pmcp-team-servers: <server> binary — implemented in a later 109 plan"); }` (no tokio; downstream server plans overwrite each again with the real `#[tokio::main]` binary). The `[[bin]]` manifest declarations already exist from Task 1 — do NOT re-declare them here.
    Stand up the cargo-fuzz sub-package centrally: `crates/pmcp-team-servers/fuzz/Cargo.toml` (cargo-fuzz layout, `publish = false`, path dep on `pmcp-team-servers` `features=["team-fs","team-mcp"]`, `libfuzzer-sys`, its OWN `[workspace]` table so it is EXCLUDED from root) declaring TWO `[[bin]]` targets: `team_guards` (body filled by 109-05) and `fs_resolve` (body filled by 109-02). Both stub target files are panic-free no-ops: `#![no_main] use libfuzzer_sys::fuzz_target; fuzz_target!(|data: &[u8]| { let _ = data; });` (module doc naming the implementing plan is fine; no `// filled by` inline comment).
  </action>
  <verify>
    <automated>cargo build -p pmcp-team-servers --all-features 2>&1 | tail -5 && cargo test -p pmcp-team-servers team::identity 2>&1 | tail -5 && test -f crates/pmcp-team-servers/fuzz/fuzz_targets/team_guards.rs && grep -q "libfuzzer-sys" crates/pmcp-team-servers/fuzz/Cargo.toml && ! grep -rns "todo!()\|unimplemented!()\|filled by" crates/pmcp-team-servers/src && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p pmcp-team-servers --all-features` exits 0 (all modules + four bin stubs)
    - `src/compose/resolver.rs` defines `pub trait PackageResolver` returning `AgentPackage`; `src/team/identity.rs` defines `pub struct MemberId` with `from_ref` + tests
    - `grep -rns "todo!()\|unimplemented!()\|filled by" crates/pmcp-team-servers/src` returns NOTHING (zero-SATD, no placeholder bodies)
    - `fuzz/Cargo.toml` declares BOTH `team_guards` + `fs_resolve` `[[bin]]` targets and has its own `[workspace]` table
    - `cargo test -p pmcp-team-servers team::identity` exits 0
  </acceptance_criteria>
  <done>All skeleton modules are empty-but-documented (no SATD/placeholders); PackageResolver + MemberId + MemberTaskForwarding seams are implemented atomically; four bin stubs compile; the fuzz sub-package is owned centrally with both target stubs.</done>
</task>

<task type="auto">
  <name>Task 3: Pure composition-derivation function derive_attachment (D-05/D-06/D-07) implemented atomically + proptest</name>
  <files>crates/pmcp-team-servers/src/compose/derive.rs, crates/pmcp-team-servers/src/lib.rs, crates/pmcp-team-servers/tests/derive_props.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/compose/derive.rs (empty documented module from Task 2 — implement it now)
    - crates/pmcp-package/src/package/team.rs (TeamPackage / TeamMember / HumanRole exact fields)
    - crates/pmcp-package/src/reference.rs (ComponentRef for opt_ins)
    - .planning/phases/109-team-reference-servers/109-RESEARCH.md ("Code Examples > Composition-derived attachment")
  </read_first>
  <action>
    In `src/compose/derive.rs` define `pub struct AttachmentSet { pub team_mcp: bool, pub approval_mcp: bool, pub opt_ins: Vec<pmcp_package::ComponentRef> }` and a composition-snapshot type (`CompositionSnapshot { ai_agent_count: usize, human_role_count: usize, opt_ins: Vec<ComponentRef> }`) capturing counts resolved once at entry (D-07 snapshot-at-entry). Implement `#[must_use] pub fn derive_attachment(pkg: &pmcp_package::TeamPackage) -> AttachmentSet` ATOMICALLY (this is its FIRST export — the real rule, never a placeholder): `team_mcp = pkg.members.len() >= 2` (N≥2 AI agents), `approval_mcp = !pkg.human_roles.is_empty()` (M≥1 human role; channel initiator implicit, never counted — D-05), `opt_ins = pkg.built_in_servers` DEDUPLICATED by `(name, component_type)` preserving first-seen order (D-06 — team-fs/mem-mcp attach ONLY when explicitly listed; the wiring layer 109-06 fail-closes on unknown/uncompiled opt-in names, so derive keeps opt_ins as-is minus duplicates). Document that `built_in_servers` is demoted to opt-in extras and that team-of-one/zero-humans → both derived flags false. Add in-file `#[cfg(test)] mod tests`: N=1,M=0 → both false + opt_ins honored; N=2 → team_mcp; M=1 → approval_mcp; duplicate built_in_servers entry deduped.
    In `src/lib.rs` add `pub use compose::derive::{derive_attachment, AttachmentSet};` (the re-export lands ONLY now that the real implementation exists).
    In `tests/derive_props.rs` add proptest invariants over generated rosters/human-role lists: (a) `team_mcp <=> members.len() >= 2`; (b) `approval_mcp <=> !human_roles.is_empty()`; (c) `opt_ins` is the deduplicated `built_in_servers` (order preserved, no duplicate `(name,type)`); (d) team-of-one blessing: a 1-member 0-human package always yields `{team_mcp:false, approval_mcp:false}`. Construct `TeamPackage` values via `semver::Version` (dev-dep `semver`).
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers --test derive_props && cargo test -p pmcp-team-servers derive::tests 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/compose/derive.rs` contains `pub fn derive_attachment(` with `members.len() >= 2` and `human_roles.is_empty()` and dedup of `built_in_servers`
    - `src/lib.rs` re-exports `derive_attachment` and `AttachmentSet` (`grep -q "derive_attachment" crates/pmcp-team-servers/src/lib.rs`)
    - `tests/derive_props.rs` has ≥4 proptest invariants incl. the team-of-one blessing and opt-in dedup
    - `cargo test -p pmcp-team-servers --test derive_props` exits 0
  </acceptance_criteria>
  <done>derive_attachment is pure, `#[must_use]`, implemented atomically at first export, with opt-in dedup and the N/M matrix + team-of-one blessing proven by unit + property tests.</done>
</task>

<task type="auto">
  <name>Task 4: Additive contract rev (correct related-task key) + probe pmat comply + author binding.yaml skeleton (contract-first)</name>
  <files>contracts/team-servers-v1.yaml, contracts/team-servers/binding.yaml</files>
  <read_first>
    - contracts/team-servers-v1.yaml (full — metadata version 1.0.0, approval_tool_surface, team_dispatch_surface)
    - tests/team_contracts_conformance.rs (STATIC_TOOL_NAMES / DYNAMIC_TOOL_PREFIXES / EQUATION_KEYS — must stay green)
    - contracts/binding.yaml (existing binding schema to mirror: version/target_crate/bindings[]/critical_path)
    - src/types/tasks.rs line 9 (RELATED_TASK_META_KEY constant — the ONLY correct key)
    - .planning/phases/109-team-reference-servers/109-RESEARCH.md (Pitfall 2 + Open Question 1: probe pmat comply first)
  </read_first>
  <action>
    Bump `metadata.version` 1.0.0 → 1.1.0 (additive minor). Remove the "no binding.yaml is authored / DEFERRED to Phase 109" language (this plan authors the skeleton; 109-08 finalizes). In `approval_tool_surface`: add an invariant + postcondition documenting the OPTIONAL `subject_task_id`/`subject_ref` fields on ask/resolve records echoed by `get_approval`/`resolve_approval` (D-12). In `team_dispatch_surface`: add an invariant documenting that depth + ancestor-chain guard state travels as namespaced `_meta` on `tools/call` (transport-agnostic; the HTTP binary maps `x-pmcp-team-depth` into that `_meta` at the edge — D-14), AND document that the surfaced related-task metadata uses the EXACT SDK key `io.modelcontextprotocol/related-task` (never a bare `related_task`) — if the existing YAML anywhere references `related_task`, correct it to the full key. Do NOT change any existing static tool name, the two unnamespaced legacy names (`resolve_approval`, `get_approval`), the dynamic prefixes, or the 19-static enumeration — purely additive.
    Probe the real compliance CLI WITHOUT authoring blind: run `pmat comply --help` and `pmat comply check --path .` (the invocation takes a PROJECT PATH `--path`, NOT a binding-file positional argument) and record the accepted form + whether it resolves `function`/`module_path` against source or a compiled crate. If `pmat` is absent, note it and proceed with the fallback (mirror the existing `contracts/binding.yaml` shape byte-for-byte).
    Author `contracts/team-servers/binding.yaml` as a CONTRACT-FIRST SKELETON (exists before implementation): `version: 1.1.0`, `target_crate: pmcp-team-servers`, one `bindings[]` entry per equation (`fs_tool_surface`, `mem_tool_surface`, `approval_tool_surface`, `team_dispatch_surface`) each `{ contract: team-servers-v1.yaml, equation, function, module_path: pmcp_team_servers::..., signature: <planned>, status: planned, notes: <D-decision + implementing plan> }` pointing at the planned reference-server function paths (`fs::server::build_team_fs_server`, `mem::server::build_mem_mcp_server`, `approval::server::build_approval_mcp_server`, `team::server::build_team_mcp_server`). 109-08 flips each `status` to `implemented` with the real signature. Keep field names byte-for-byte compatible with `contracts/binding.yaml`.
  </action>
  <verify>
    <automated>cargo test --test team_contracts_conformance 2>&1 | tail -5 && test -f contracts/team-servers/binding.yaml && grep -q "target_crate: pmcp-team-servers" contracts/team-servers/binding.yaml && grep -q "io.modelcontextprotocol/related-task" contracts/team-servers-v1.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/team-servers-v1.yaml` `metadata.version` is `1.1.0`; contains `subject_task_id`/`subject_ref` in the approval equation and `x-pmcp-team-depth` as an `_meta` field in `team_dispatch_surface`
    - the contract references the related-task key as `io.modelcontextprotocol/related-task` (no bare `related_task` remains)
    - `cargo test --test team_contracts_conformance` exits 0 (19 static + 2 dynamic prefixes unchanged)
    - `contracts/team-servers/binding.yaml` exists with `target_crate: pmcp-team-servers`, one binding per equation, `status: planned`
    - the SUMMARY records the confirmed `pmat comply` invocation (`--path .`) or notes pmat absence
  </acceptance_criteria>
  <done>Contract rev'd to v1.1.0 additively with the CORRECT related-task key; a contract-first binding.yaml skeleton (status: planned) is authored after probing the real `pmat comply --path` CLI; structural conformance stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/dependency graph | New crate pulls path deps + one feature-gated registry dep (reqwest); fuzz-only libfuzzer-sys in the excluded sub-package |
| TeamPackage input → derive_attachment | Untrusted package config drives attachment decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01-01 | Tampering | derive_attachment on malformed/adversarial TeamPackage | mitigate | Pure total function over counts + dedup only; no panics; proptest covers empty/large rosters, empty human_roles, duplicate opt-ins (Task 3) |
| T-109-01-SC | Tampering | cargo dependency graph | mitigate | No new third-party registry package in the default build — core deps are in-repo path crates; `reqwest`/`parking_lot` already vendored, feature-gated / workspace deps; `bm25` NOT adopted; `libfuzzer-sys` fuzz-only in the workspace-excluded `fuzz/` sub-package. No `[ASSUMED]`/`[SUS]` packages → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo build -p pmcp-team-servers --all-features` and `--no-default-features --features team-fs` both succeed
- `cargo test -p pmcp-team-servers` green (derive unit + property, MemberId tests)
- `cargo test --test team_contracts_conformance` green (contract rev additive)
- fuzz sub-package present with both stub targets; no SATD/placeholder markers in `src/`
</verification>

<success_criteria>
- New feature-flagged crate builds under default + all-features + reduced-feature isolation
- Module tree is empty-but-documented (zero-SATD, no placeholder bodies); PackageResolver + MemberId + MemberTaskForwarding seams implemented atomically
- Fuzz sub-package owned centrally (both targets); DuplexTransport public; `derive_attachment` implemented atomically + property-tested (D-05/D-06/D-07)
- Contract at v1.1.0 with the CORRECT related-task key; contract-first binding.yaml skeleton authored; structural conformance green
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-01-SUMMARY.md` when done
</output>
