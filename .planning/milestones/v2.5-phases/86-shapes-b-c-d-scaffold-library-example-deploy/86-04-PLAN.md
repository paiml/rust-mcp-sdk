---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 04
type: execute
wave: 4
depends_on: [86-03]
files_modified:
  - cargo-pmcp/tests/scaffold_sql_server.rs
  - cargo-pmcp/tests/support/scaffold_patch.rs
  - cargo-pmcp/Cargo.toml
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "A tempdir scaffold via the REAL `cargo-pmcp new --kind sql-server` binary builds and `cargo run`s against an embedded SQLite"
    - "The spawned server answers tools/list and at least one tools/call"
    - "The unpublished pmcp-server-toolkit 0.1.0 is resolvable in the tempdir via a [patch.crates-io]/path override (Pitfall §1), via a reusable helper Plan 06 also consumes (M1)"
    - "Every spawned child process is protected by a ChildGuard/Drop-kill so a panic cannot leak it (M1)"
  artifacts:
    - path: "cargo-pmcp/tests/scaffold_sql_server.rs"
      provides: "TEST-05 end-to-end: real-binary scaffold → patch deps → cargo run → ChildGuard → poll → tools/list + tools/call"
      contains: "test_tools_list"
    - path: "cargo-pmcp/tests/support/scaffold_patch.rs"
      provides: "reusable [patch.crates-io] writer + ChildGuard, shared with Plan 06 (M1)"
      contains: "patch.crates-io"
  key_links:
    - from: "scaffold_sql_server.rs"
      to: "the built cargo-pmcp binary"
      via: "env!(\"CARGO_BIN_EXE_cargo-pmcp\") subprocess (real command surface — M1, not in-process new::execute)"
      pattern: "CARGO_BIN_EXE_cargo-pmcp"
    - from: "scaffold_sql_server.rs tempdir Cargo.toml"
      to: "in-repo path crates (pmcp, pmcp-server-toolkit)"
      via: "[patch.crates-io] override (Pitfall §1 / Assumption A1) via the shared helper"
      pattern: "patch.crates-io"
    - from: "scaffold_sql_server.rs"
      to: "spawned cargo run subprocess HTTP port"
      via: "PMCP_SQL_SERVER_ADDR line parse + 20-attempt readiness poll (parity_chinook.rs idiom)"
      pattern: "PMCP_SQL_SERVER_ADDR|test_initialize"
---

<objective>
Build TEST-05: an end-to-end integration test that scaffolds a project by invoking the REAL
built `cargo-pmcp` binary (`cargo pmcp new --kind sql-server`) into a tempdir — exercising the
ACTUAL command surface per SHAP-B-01/TEST-05 (Review M1 / Codex 86-04 MEDIUM: do NOT weaken to
an in-process `new::execute` call), makes the unpublished `pmcp-server-toolkit 0.1.0` resolvable
via a `[patch.crates-io]`/path override (RESEARCH Pitfall §1, Assumption A1 — the crate is not yet
on crates.io), shells out to a REAL `cargo run` subprocess in that tempdir (SC-1's "verified
end-to-end" promise), parses the server's MACHINE-READABLE `PMCP_SQL_SERVER_ADDR=` line from
stdout (Plan 02/03 print it — M1, do NOT scrape cargo build output), polls for HTTP readiness using
the verbatim `parity_chinook.rs` 20-attempt backoff loop, then asserts `tools/list` shows the curated
tool and one `tools/call` succeeds. Every child is wrapped in a ChildGuard/Drop-kill (M1). The
`[patch.crates-io]` writer + ChildGuard live in a shared test-support module that Plan 06 reuses (M1).
`--test-threads=1` (ephemeral port + process env, Pitfall §5).

Note (H1): the scaffolded `main.rs` reads `config.toml`/`schema.sql` via `pmcp::assets::load_string`,
which resolves the CWD locally. Because the test runs `cargo run` with the scaffold crate dir as the
current_dir (and the emitted assets sit at that crate root), no `PMCP_ASSETS_DIR` is needed — the
files resolve relative to cwd. The SQLite DB bootstraps to the local `demo.db` (demo_db_path() returns
a relative path off-Lambda).

Purpose: prove the Shape B scaffold actually runs and serves out of the box, through the real command.
Output: `cargo-pmcp/tests/scaffold_sql_server.rs` + a reusable `tests/support/scaffold_patch.rs`
(CI-runnable, zero creds).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-03-SUMMARY.md

<interfaces>
<!-- spawn-poll-tester pattern + the tempdir dep-resolution fix. -->
From crates/pmcp-sql-server/tests/parity_chinook.rs (verified, lines 113-211 — copy the poll verbatim):
```rust
let mut tester = ServerTester::new(&url, Duration::from_secs(30), false, None, Some("http"), None)?;
for attempt in 0..20u32 {
    if matches!(tester.test_initialize().await.status, mcp_tester::report::TestStatus::Passed) { break; }
    tokio::time::sleep(Duration::from_millis(50 * u64::from(attempt + 1))).await;
}
```
mcp-tester: test_tools_list() (tester.rs:1343) ; test_tool(name, args) (tester.rs:1501).
Pitfall §1: tempdir Cargo.toml needs `[patch.crates-io] pmcp = { path = "<repo>" }` + `pmcp-server-toolkit = { path = "<repo>/crates/pmcp-server-toolkit" }` (+ any transitive: pmcp-code-mode, pmcp-widget-utils) so `cargo run` resolves the unpublished 0.1.0. Repo root = env!("CARGO_MANIFEST_DIR") joined up from cargo-pmcp/.
Real-binary invocation: `std::process::Command::new(env!("CARGO_BIN_EXE_cargo-pmcp"))` — Cargo sets this env for the crate's own bin in integration tests (M1).
Bound-address line printed by the scaffold (Plan 02/03): `PMCP_SQL_SERVER_ADDR=http://127.0.0.1:<port>`.
Existing cargo-pmcp dev-deps to mirror (Cargo.toml): mcp-tester (path), tempfile, tokio.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable test-support module — [patch.crates-io] writer + ChildGuard</name>
  <read_first>
    - cargo-pmcp/Cargo.toml (the file being modified — read `[dev-dependencies]` for mcp-tester (path), tempfile, tokio; and whether a `[[test]]` with shared modules / `path =` convention exists; integration tests can `mod support;` or `#[path = "support/scaffold_patch.rs"]`)
    - cargo-pmcp/src/templates/sql_server.rs (the emitter under test — confirm the emitted Cargo.toml dep names/versions the patch override must shadow; Plan 03 output)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Pitfall §1 dep resolution)
  </read_first>
  <action>
    Create cargo-pmcp/tests/support/scaffold_patch.rs as a shared test-support module exposing:
    (1) `pub fn append_crates_io_patch(crate_dir: &std::path::Path)` — reads the tempdir crate's `Cargo.toml`, appends a `[patch.crates-io]` block pointing `pmcp`, `pmcp-server-toolkit` (and any transitive path deps the build needs: `pmcp-code-mode`, `pmcp-widget-utils`) at the in-repo crates resolved from the repo root (`env!("CARGO_MANIFEST_DIR")` joined up from `cargo-pmcp/`), and writes it back. Document that it must cover transitive unpublished crates (Gemini suggestion).
    (2) `pub struct ChildGuard(pub std::process::Child)` with `impl Drop { fn drop(&mut self) { let _ = self.0.kill(); let _ = self.0.wait(); } }` so a panic cannot leak a spawned process (M1).
    (3) `pub fn repo_root() -> std::path::PathBuf` — the in-repo root the patch paths derive from.
    This module is referenced by BOTH scaffold_sql_server.rs (this plan) AND deploy_config_only.rs (Plan 06) via `#[path = "support/scaffold_patch.rs"] mod scaffold_patch;` so the patch logic + ChildGuard are written once.
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp --tests 2>&1 | tail -5; test -f cargo-pmcp/tests/support/scaffold_patch.rs && grep -q 'patch.crates-io' cargo-pmcp/tests/support/scaffold_patch.rs && grep -q 'impl Drop' cargo-pmcp/tests/support/scaffold_patch.rs && echo SUPPORT_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `cargo-pmcp/tests/support/scaffold_patch.rs` exists with `append_crates_io_patch`, `ChildGuard` (with `impl Drop`), and `repo_root` (verify command prints `SUPPORT_OK`).
    - Source assertion: the patch writer covers `pmcp`, `pmcp-server-toolkit`, and transitive `pmcp-code-mode`/`pmcp-widget-utils` (Gemini transitive-dep note).
    - Behavior: `cargo build -p cargo-pmcp --tests` succeeds.
  </acceptance_criteria>
  <done>A reusable ChildGuard + [patch.crates-io] writer exists for both TEST-05 and TEST-06 (M1).</done>
</task>

<task type="auto">
  <name>Task 2: Tempdir scaffold via the REAL cargo-pmcp binary + patch deps + subprocess cargo run + ChildGuard + address-line poll + tools/list + tools/call</name>
  <read_first>
    - crates/pmcp-sql-server/tests/parity_chinook.rs (the canonical spawn → readiness-poll → ServerTester drive pattern, lines 113-211; the tempfile::tempdir idiom lines 113-163)
    - crates/mcp-tester/src/tester.rs (ServerTester::new signature; test_initialize; test_tools_list :1343; test_tool :1501)
    - cargo-pmcp/Cargo.toml (the file being modified — confirm/add `[dev-dependencies]` mcp-tester (path), tempfile, tokio (rt-multi-thread+macros), serde_json)
    - cargo-pmcp/tests/support/scaffold_patch.rs (the shared helper from Task 1 — use append_crates_io_patch + ChildGuard + repo_root)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "cargo-pmcp/tests/scaffold_sql_server.rs (NEW)" — the KEY DIFFERENCE note: subprocess cargo run + patch override + --test-threads=1)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Pitfall §1 dep resolution; Code Examples §4 spawn+poll; Pitfall §5 --test-threads=1)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-03-SUMMARY.md (confirm the emitted file names + that main.rs prints `PMCP_SQL_SERVER_ADDR=` to stdout)
  </read_first>
  <action>
    Create cargo-pmcp/tests/scaffold_sql_server.rs as a `#[tokio::test]` (multi-thread) that pulls in the shared module via `#[path = "support/scaffold_patch.rs"] mod scaffold_patch;`:
    (1) `tempfile::tempdir()`;
    (2) invoke the REAL built binary to scaffold (M1 — exercise the actual command surface): `std::process::Command::new(env!("CARGO_BIN_EXE_cargo-pmcp")).args(["new","--kind","sql-server","<name>"]).current_dir(tmp).status()` and assert success — do NOT call `new::execute` in-process;
    (3) `scaffold_patch::append_crates_io_patch(&crate_dir)` to make the unpublished toolkit resolve (Pitfall §1);
    (4) spawn `std::process::Command::new("cargo").args(["run"]).current_dir(&crate_dir).stdout(Stdio::piped()).stderr(Stdio::piped()).spawn()?`, IMMEDIATELY wrap the child in `scaffold_patch::ChildGuard` (M1);
    (5) read the child's stdout line-by-line until the `PMCP_SQL_SERVER_ADDR=` line appears and parse the URL from THAT (M1 — not from cargo build output); enforce an overall wall-clock timeout while reading;
    (6) build the `url` and run the parity_chinook.rs 20-attempt readiness poll VERBATIM; on timeout, FAIL with the captured stdout+stderr in the panic message (M1);
    (7) `assert!(initialized)`;
    (8) assert `tester.test_tools_list().await` includes the curated `list_books` tool;
    (9) assert `tester.test_tool("list_books", serde_json::json!({"limit": 5})).await` succeeds;
    (10) ChildGuard Drop kills the subprocess; tempdir auto-cleans.
    Ensure cargo-pmcp `[dev-dependencies]` has mcp-tester (path), tempfile, tokio (rt-multi-thread, macros), serde_json — add any missing. Document in a top-of-file comment that this test requires `--test-threads=1` (Pitfall §5) and that it builds the scaffold crate (slow; first run compiles the toolkit — give the read/poll a generous timeout). Per Gemini LOW (CI build perf): note that this test belongs to the `--test-threads=1` group so the heavy tempdir build does not contend.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test scaffold_sql_server -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cargo test -p cargo-pmcp --test scaffold_sql_server -- --test-threads=1` exits 0.
    - Source assertion: the test scaffolds via the REAL binary — `grep -n "CARGO_BIN_EXE_cargo-pmcp" cargo-pmcp/tests/scaffold_sql_server.rs` present; the test does NOT call `new::execute` in-process and does NOT call `run_serving` (M1, real command surface).
    - Source assertion: the test wraps the spawned `cargo run` child in `ChildGuard` and parses the address from the `PMCP_SQL_SERVER_ADDR=` line (grep both — M1).
    - Source assertion: the test uses `scaffold_patch::append_crates_io_patch` to write the `[patch.crates-io]` block (Pitfall §1).
    - Behavior assertion: the test asserts BOTH `test_tools_list` (shows `list_books`) AND `test_tool("list_books", ...)` succeeds (TEST-05 requires tools/list + one tools/call).
    - On a readiness timeout the test panics WITH the captured stdout/stderr (M1).
    - The readiness poll is the parity_chinook.rs 20-attempt linear-backoff loop (no fixed sleep).
  </acceptance_criteria>
  <done>TEST-05 scaffolds via the real cargo-pmcp binary, patches deps, runs the real scaffold binary under a ChildGuard, parses the machine-readable address, polls readiness, and proves tools/list + tools/call end-to-end in CI with zero creds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → spawned scaffold HTTP server | the test acts as an MCP client crossing into the server's tools surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04-01 | Tampering | tempdir Cargo.toml patch override pointing at arbitrary paths | accept | The patch paths are derived from `env!("CARGO_MANIFEST_DIR")` (this repo), not external input; the tempdir is auto-cleaned. Test-only code. |
| T-86-04-02 | Denial of Service | leaked subprocess if an assertion panics before kill | mitigate | The child is wrapped in `ChildGuard` whose `Drop` kills+waits it (M1) — a panic anywhere after spawn cannot leak the process; `--test-threads=1` serializes; the scaffold binds an ephemeral/local port. |
| T-86-04-03 | Information Disclosure | scaffold's inline DEV token_secret surfaced in test logs | accept | The secret is the public DEV-ONLY literal already in the emitted template (D-06); no real credential is involved. |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --test scaffold_sql_server -- --test-threads=1` passes.
- Real-binary scaffold (CARGO_BIN_EXE_cargo-pmcp); ChildGuard-protected `cargo run` (not in-process); address parsed from PMCP_SQL_SERVER_ADDR; `[patch.crates-io]` override via the shared helper; tools/list + tools/call asserted; captured output on timeout.
</verification>

<success_criteria>
- TEST-05 proves `cargo pmcp new --kind sql-server` (real binary) → `cargo run` → tools/list + tools/call end-to-end in a tempdir (SC-1, TEST-05).
- Zero creds, CI-runnable; the unpublished toolkit resolves via the patch override; no leaked processes (M1); the patch helper + ChildGuard are reusable by Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-04-SUMMARY.md`
</output>
