---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 06
type: execute
wave: 5
depends_on: [86-04, 86-05]
files_modified:
  - cargo-pmcp/tests/deploy_config_only.rs
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "A real pmcp.run config-only deploy runs the Phase 79 post-deploy lifecycle (check + conformance + apps) cleanly"
    - "The test is gated by PMCP_RUN_DEPLOY_TEST and SKIPS (prints reason + returns, never fails) when the gate is absent"
    - "The gated-on path invokes the REAL built cargo-pmcp binary as a subprocess and asserts exit code (M1 — NOT in-process deploy, which can std::process::exit), reusing the Plan 04 [patch.crates-io] helper + ChildGuard"
    - "Normal CI (no creds) stays green; the gated test is the SC-4 deliverable (D-11)"
  artifacts:
    - path: "cargo-pmcp/tests/deploy_config_only.rs"
      provides: "env-gated real pmcp.run deploy integration test (real-binary subprocess) asserting the Phase 79 lifecycle ran clean"
      contains: "PMCP_RUN_DEPLOY_TEST"
  key_links:
    - from: "deploy_config_only.rs gate"
      to: "early-return skip when PMCP_RUN_DEPLOY_TEST absent"
      via: "npm_skip_gate idiom (widgets_orchestrator.rs:39)"
      pattern: "PMCP_RUN_DEPLOY_TEST"
    - from: "deploy_config_only.rs"
      to: "the built cargo-pmcp binary `deploy` subprocess (exit-0 ⇒ run_post_deploy_tests ran clean)"
      via: "env!(\"CARGO_BIN_EXE_cargo-pmcp\") + ChildGuard (Plan 04 support module, M1)"
      pattern: "CARGO_BIN_EXE_cargo-pmcp|run_post_deploy_tests"
---

<objective>
Build TEST-06: an env-gated integration test that, ONLY when `PMCP_RUN_DEPLOY_TEST` is set (and
pmcp.run/AWS creds + `cargo lambda` are present), scaffolds + deploys a config-only server to a
REAL pmcp.run target via the Plan 05 deploy path and asserts the Phase 79 post-deploy lifecycle
(`check` + `conformance` + `apps`) runs cleanly (D-11, SC-4). When the gate is absent the test prints
a reason and `return`s — it NEVER fails the suite (the `npm_skip_gate` idiom), so normal CI stays
green without creds.

Per the review feedback (M1 / Codex 86-06):
- The gated-on path drives deploy by invoking the REAL built `cargo-pmcp` binary as a SUBPROCESS
  (`env!("CARGO_BIN_EXE_cargo-pmcp")`) and asserting exit code — NOT an in-process deploy call, because
  the deploy path may `std::process::exit` on a post-deploy failure and would abort the test process.
  A clean exit (0) means `run_post_deploy_tests` (check + conformance + apps) ran clean inside the
  command.
- It REUSES the Plan 04 shared `tests/support/scaffold_patch.rs` (the `[patch.crates-io]` writer so the
  scaffolded tempdir builds the unpublished toolkit, AND the `ChildGuard` so a panic cannot leak the
  deploy subprocess).
- URL/lifecycle capture: assert the deploy command exit code (which itself runs the Phase 79 lifecycle),
  and additionally read the `.pmcp/deployment.toml` the deploy writes (verified `save_deployment_info`
  in deploy/mod.rs) to capture the deployed URL for the SUMMARY record.
- Teardown: target a DISPOSABLE/test server name and, on success, best-effort `cargo pmcp deploy destroy
  --yes` (tolerate + log cleanup failure) so repeated gated runs don't accumulate live resources.
- Also add `#[ignore]` in addition to the env gate so a bare `cargo test` never even constructs the
  deploy path (defense in depth alongside the early-return skip).

Purpose: authentic proof that a config-only server deploys and passes the hosted lifecycle.
Output: `cargo-pmcp/tests/deploy_config_only.rs` (env-gated + #[ignore], skipped in normal CI).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-04-SUMMARY.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-05-SUMMARY.md

<interfaces>
<!-- gate idiom + the lifecycle to assert + the reusable support module. Sources verified this session. -->
From cargo-pmcp/tests/widgets_orchestrator.rs (npm_skip_gate idiom, lines 36-45):
```rust
fn deploy_gate() -> Option<&'static str> {
    if std::env::var("PMCP_RUN_DEPLOY_TEST").is_ok() { None }
    else { Some("PMCP_RUN_DEPLOY_TEST not set — skipping real pmcp.run deploy integration test") }
}
// at test top: if let Some(reason) = deploy_gate() { eprintln!("{reason}"); return; }  // SKIP, do NOT fail
```
From cargo-pmcp/src/deployment/post_deploy_tests.rs (verified, line 791):
```rust
pub async fn run_post_deploy_tests(
    url: &str, target_id: &str /* "pmcp-run" */, widgets_present: bool,
    config: &PostDeployTestsConfig,  // .checks default = ["connectivity","conformance","apps"]
    quiet: bool,
) -> std::result::Result<(), OrchestrationFailure>;
```
The real `cargo pmcp deploy` command invokes run_post_deploy_tests itself (deploy/mod.rs None arm) — a clean exit means the lifecycle passed.
Deploy writes `.pmcp/deployment.toml` with `server_id` + `endpoint` (save_deployment_info, deploy/mod.rs:1032) — read it to capture the URL.
Plan 04 shared module: `#[path = "support/scaffold_patch.rs"] mod scaffold_patch;` → `append_crates_io_patch`, `ChildGuard`, `repo_root` (M1 reuse).
Real-binary invocation: `std::process::Command::new(env!("CARGO_BIN_EXE_cargo-pmcp"))`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Env-gated + #[ignore] real pmcp.run config-only deploy via the real binary subprocess + Phase 79 lifecycle assertion</name>
  <read_first>
    - cargo-pmcp/tests/widgets_orchestrator.rs (the `npm_skip_gate` early-return idiom, lines 36-45 — copy the shape, env var PMCP_RUN_DEPLOY_TEST)
    - cargo-pmcp/tests/support/scaffold_patch.rs (the Plan 04 shared module — reuse append_crates_io_patch + ChildGuard + repo_root; M1)
    - cargo-pmcp/src/deployment/post_deploy_tests.rs (read `run_post_deploy_tests` at line 791 — the default checks ["connectivity","conformance","apps"]; a clean deploy exit ⇒ this ran clean)
    - cargo-pmcp/src/commands/deploy/mod.rs (the Plan 05 deploy path the test drives via the CLI; `save_deployment_info` ~1032 writes `.pmcp/deployment.toml` with the URL)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "cargo-pmcp/tests/deploy_config_only.rs (NEW)" — gate idiom + what to assert; the "DO NOT fail the suite" contract)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Code Examples §5 gate idiom; §6 run_post_deploy_tests signature; D-11 tension; Assumption A5 creds-AND-gate; Runtime State Inventory teardown note)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-05-SUMMARY.md (confirm the deploy detection seam + scaffold deploy.toml + single-crate builder support are in place)
  </read_first>
  <action>
    Create cargo-pmcp/tests/deploy_config_only.rs that pulls in the shared module via
    `#[path = "support/scaffold_patch.rs"] mod scaffold_patch;` and contains:
    (1) a `deploy_gate()` fn (the widgets_orchestrator.rs:36-45 idiom, env var `PMCP_RUN_DEPLOY_TEST`);
    (2) a `#[tokio::test] #[ignore = "real pmcp.run deploy — set PMCP_RUN_DEPLOY_TEST + creds and run with --ignored"] config_only_deploy_runs_phase79_lifecycle` that FIRST does `if let Some(reason) = deploy_gate() { eprintln!("{reason}"); return; }` (skip, never fail — defense in depth WITH `#[ignore]`).
    When gated ON:
    (a) scaffold a config-only project into a tempdir by invoking the REAL `cargo-pmcp` binary
        (`env!("CARGO_BIN_EXE_cargo-pmcp")` with `new --kind sql-server <disposable-name>`) — use a
        DISPOSABLE/unique server name (e.g. a timestamp/uuid suffix) per the Runtime State Inventory
        teardown note so repeated runs are safe;
    (b) `scaffold_patch::append_crates_io_patch(&crate_dir)` so `cargo lambda build` resolves the
        unpublished toolkit (M1 / Codex 86-06 HIGH — the tempdir needs the patch overrides);
    (c) drive the REAL deploy by spawning `Command::new(env!("CARGO_BIN_EXE_cargo-pmcp")).args(["deploy"]).current_dir(&crate_dir)` (the emitted deploy.toml already sets `target_type="pmcp-run"` — Plan 05/M3), wrap the child in `scaffold_patch::ChildGuard`, wait for it, and ASSERT exit code 0 — a clean exit means the command's own `run_post_deploy_tests` (check + conformance + apps) passed (M1 — do NOT call deploy in-process; it may `std::process::exit`);
    (d) capture the deployed URL by reading `<crate_dir>/.pmcp/deployment.toml` (`endpoint`) and record it in the SUMMARY; optionally call `run_post_deploy_tests(url, "pmcp-run", widgets_present, &PostDeployTestsConfig::default(), quiet)` and assert `Ok(())` for an explicit second confirmation;
    (e) TEARDOWN: on success, best-effort `Command::new(env!("CARGO_BIN_EXE_cargo-pmcp")).args(["deploy","destroy","--yes"]).current_dir(&crate_dir)` — tolerate and clearly log a cleanup failure (do not fail the test on cleanup error; D-11 records a deliberately-left server is acceptable).
    Document at the top of the file that the test requires BOTH `PMCP_RUN_DEPLOY_TEST=1` AND live pmcp.run/AWS creds + `cargo lambda` (Assumption A5); a gate set without creds fails fast with an install/cred hint (operator error, not a CI path). `--test-threads=1`. Do NOT add an always-on mock (D-11 — the gated test is the SC-4 deliverable).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test deploy_config_only -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Behavior (CI default — gate ABSENT, not --ignored): `cargo test -p cargo-pmcp --test deploy_config_only -- --test-threads=1` exits 0 with the test SKIPPED (printed reason + return, never failing; also `#[ignore]` keeps it out of a default run) — it does NOT attempt a deploy.
    - Source assertion: the gate fn keys on `PMCP_RUN_DEPLOY_TEST` and early-returns (`grep -n "PMCP_RUN_DEPLOY_TEST"`); the skip path uses `eprintln! + return`, NOT `panic!`/`assert!`; the test also carries `#[ignore]`.
    - Source assertion (M1): the gated path drives deploy via the REAL binary subprocess (`grep -n "CARGO_BIN_EXE_cargo-pmcp"`) and asserts its exit code — it does NOT call the deploy fn in-process; it reuses `scaffold_patch::append_crates_io_patch` + `ChildGuard`.
    - Source assertion: the gated path asserts the Phase 79 lifecycle ran clean — deploy exit-0 (which runs check + conformance + apps) and/or an explicit `run_post_deploy_tests` Ok; the URL is read from `.pmcp/deployment.toml`.
    - Source assertion: a best-effort `deploy destroy --yes` teardown exists and tolerates cleanup failure (logged), with a disposable server name.
    - The test adds NO new deploy subsystem code (reuses the Plan 05 path) and NO always-on mock (D-11).
    - Manual gate (operator, documented in SUMMARY): with `PMCP_RUN_DEPLOY_TEST=1` + creds + `--ignored`, the test deploys and the lifecycle passes (86-VALIDATION.md Manual-Only Verifications row).
  </acceptance_criteria>
  <done>TEST-06 exists as an env-gated + #[ignore] authentic deploy test that skips cleanly in CI and, when enabled, drives the REAL cargo-pmcp deploy subprocess (patch-helper + ChildGuard reused), asserts exit-0 / the Phase 79 lifecycle, captures the URL, and tears down (SC-4 / D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real pmcp.run platform | the gated test deploys a live function + bundled demo DB to a hosted target |
| gate env var → deploy activation | the presence of PMCP_RUN_DEPLOY_TEST + creds activates a real cloud action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-06-01 | Elevation of Privilege | an accidental real deploy from CI | mitigate | Double-gated: the test deploys ONLY when `PMCP_RUN_DEPLOY_TEST` is set AND creds are present, AND it is `#[ignore]` so a default `cargo test` never runs it; CI (no gate) skips with a printed reason and never reaches the deploy subprocess (npm_skip_gate contract). |
| T-86-06-02 | Information Disclosure | live creds/secret leaked in test output | mitigate | The test does not print creds; deploy secrets resolve transiently via the existing pipeline (redacted-at-source errors); `token_secret` comes from the `${CODE_MODE_SECRET}` ref on the deploy path (Plan 05 / D-06 / H4), not an inline literal. |
| T-86-06-03 | Denial of Service / cost | repeated runs leaving orphaned deployed servers | mitigate | Disposable/unique server name + a best-effort `deploy destroy --yes` teardown on success (tolerating logged cleanup failure); the spawned deploy child is wrapped in `ChildGuard` so a panic cannot leak the process (M1). D-11 records that a deliberately-left server is acceptable. |
</threat_model>

<verification>
- CI default: `cargo test -p cargo-pmcp --test deploy_config_only -- --test-threads=1` passes by SKIPPING (gate absent + `#[ignore]`), never failing, never deploying.
- Gate keys on `PMCP_RUN_DEPLOY_TEST`; gated-on path drives the REAL cargo-pmcp deploy subprocess (M1, patch-helper + ChildGuard reused), asserts exit-0 / the Phase 79 lifecycle (check + conformance + apps), captures the URL, and tears down.
- No new deploy code; no always-on mock (D-11).
</verification>

<success_criteria>
- TEST-06 is an authentic env-gated + #[ignore] deploy test (SC-4 deliverable per D-11), green-by-skip in normal CI.
- When enabled with creds, it deploys a config-only single-crate server to pmcp.run via the real binary and confirms the Phase 79 post-deploy lifecycle runs cleanly, then tears down.
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-06-SUMMARY.md`
</output>
