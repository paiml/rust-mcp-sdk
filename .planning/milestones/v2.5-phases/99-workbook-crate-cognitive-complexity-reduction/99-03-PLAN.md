---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-runtime/src/bundle_loader.rs
autonomous: true
requirements: [CPLX-01]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists bundle_loader.rs::load"
    - "bundle-load goldens still green (load_valid_golden_returns_populated_bundle)"
  artifacts:
    - path: "crates/pmcp-workbook-runtime/src/bundle_loader.rs"
      provides: "load decomposed into per-part parse helpers, cog <= 23"
      contains: "fn load"
  key_links:
    - from: "load"
      to: "per-bundle-part parse helpers"
      via: "thin orchestrator over named helpers"
      pattern: "fn load"
---

<objective>
Refactor `pmcp-workbook-runtime/src/bundle_loader.rs::load` (cog 28) below the PMAT gate (target cog <= 23), preserving bundle-load and error semantics.

Purpose: load is a flagged runtime function blocking PR #279's PMAT gate. It owns its own file (no conflict with plans 01/02), so it runs in parallel.
Output: load reduced to a thin orchestrator over per-part helpers; gate clears; bundle goldens green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-runtime/src/bundle_loader.rs — load at ~line 268 (verify with `grep -n "fn load" crates/pmcp-workbook-runtime/src/bundle_loader.rs`). It reads a BundleSource, validates/parses the bundle parts, and assembles a WorkbookBundle, surfacing BundleLoadError variants. The test `load_valid_golden_returns_populated_bundle` (~line 505, same file) is the regression guard.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose load into per-part parse helpers</name>
  <files>crates/pmcp-workbook-runtime/src/bundle_loader.rs</files>
  <action>
  Confirm live cog via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("bundle_loader.rs"))'` and run `cargo test -p pmcp-workbook-runtime` for the green baseline. Extract each distinct parse/validate step of load (read-and-validate each required part, parse part bytes into its struct, assemble the bundle) into a named private helper (e.g. `load_manifest_part`, `validate_required_parts`, `assemble_bundle`). Apply early-return guard clauses on the BundleLoadError paths so missing/malformed parts return early rather than nest. load becomes a thin orchestrator. Preserve every BundleLoadError variant and the exact part-presence requirements. Target cog <= 23 for load and each helper.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("bundle_loader.rs"))')" && cargo test -p pmcp-workbook-runtime 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>load no longer in PMAT violations; bundle-load test green.</done>
</task>

<task type="auto">
  <name>Task 2: Verify clippy + commit</name>
  <files>crates/pmcp-workbook-runtime/src/bundle_loader.rs</files>
  <action>
  Run `cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings` clean and `cargo test -p pmcp-workbook-runtime` green. Commit: `refactor(99): decompose bundle_loader::load below PMAT gate`.
  </action>
  <verify>
    <automated>cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings 2>&1 | grep -qv "^error" && cargo test -p pmcp-workbook-runtime 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>clippy + tests green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("bundle_loader.rs"))'` empty.
- `cargo test -p pmcp-workbook-runtime` green; `cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings` clean.
</verification>

<success_criteria>
bundle_loader::load (was 28) cleared from the gate; bundle-load behavior unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-03-SUMMARY.md` when done.
</output>
