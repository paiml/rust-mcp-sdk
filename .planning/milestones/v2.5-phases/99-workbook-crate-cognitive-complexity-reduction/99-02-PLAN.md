---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs
  - crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs
autonomous: true
requirements: [CPLX-01]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists eval_expr, f_index, or f_search"
    - "scalar_eval unit tests and quirk/reconcile fixtures still green"
  artifacts:
    - path: "crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs"
      provides: "eval_expr decomposed via match-to-helper dispatch, cog <= 23"
      contains: "fn eval_expr"
    - path: "crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs"
      provides: "f_index and f_search flattened, cog <= 23 each"
      contains: "fn f_search"
  key_links:
    - from: "eval_expr"
      to: "per-expression-variant helpers"
      via: "match arm dispatch to named eval helpers"
      pattern: "fn eval_expr"
---

<objective>
Refactor the sheet-IR evaluator hotspots in `pmcp-workbook-runtime`: `sheet_ir/executor.rs::eval_expr` (cog 58 — over the 50 cap, genuine decomposition) and `sheet_ir/semantics.rs::f_index` (cog 24) / `f_search` (cog 31), all to target cog <= 23, preserving exact evaluation semantics.

Purpose: eval_expr is one of the 5 over-cap functions blocking the PMAT gate on PR #279. The two semantics fns (INDEX/SEARCH) are smaller but still flagged. All in the runtime crate's `sheet_ir` subsystem.
Output: eval_expr is a thin match-dispatch over per-variant helpers; f_index/f_search flattened; gate clears; scalar_eval tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs — eval_expr at ~line 168 (verify with `grep -n "fn eval_expr"`). This is the core expression evaluator: a large match over expression variants (literals, refs, binary ops, function calls, ranges). The scalar_eval unit tests guard it.
- crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs — f_index at ~line 219, f_search at ~line 339 (verify with grep). These implement the INDEX and SEARCH worksheet functions with arg-count/type validation branches.
- The Excel quirk corpus (1900-leap, half-rounding, empty-cell) and penny-reconcile fixtures exercise these paths — do not alter numeric/rounding behavior.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose eval_expr via per-variant helper dispatch</name>
  <files>crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs</files>
  <action>
  First confirm live cog with `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("executor.rs"))'` and run `cargo test -p pmcp-workbook-runtime` for the GREEN baseline. Then extract each non-trivial match arm of eval_expr into a named private helper (e.g. `eval_binary_op`, `eval_function_call`, `eval_range_ref`, `eval_cell_ref`). eval_expr's match keeps thin arms that delegate to helpers. Where binary-op or function dispatch is itself a long ladder, push it into a sub-helper with a flat match. Preserve recursion structure (eval_expr calls itself for operands) and every error/coercion path exactly — the quirk corpus asserts numeric edge cases. Target cog <= 23 for eval_expr and each new helper.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("executor.rs"))')" && cargo test -p pmcp-workbook-runtime 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>eval_expr no longer in PMAT violations; scalar_eval/quirk tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten f_index and f_search in semantics.rs</name>
  <files>crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs</files>
  <action>
  Verify live cog of f_index (~24) and f_search (~31) via `pmat analyze complexity ... | jq 'select(.path | test("semantics.rs"))'`. For each: apply early-return guard clauses for arg-count and type validation (the leading `if args.len() ...` / type-mismatch branches are the nesting drivers), and extract any cohesive lookup/match-position sub-step into a named private helper (e.g. `index_lookup`, `search_find_position`). Preserve INDEX/SEARCH semantics (1-based positions, case-insensitive SEARCH, error values on out-of-range) exactly. Target cog <= 23 each.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("semantics.rs"))')" && cargo test -p pmcp-workbook-runtime 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>f_index and f_search no longer in PMAT violations; semantics tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify subsystem gate + clippy and commit</name>
  <files>crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs, crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("sheet_ir"))'` — MUST be empty. Run full `cargo test -p pmcp-workbook-runtime` (scalar_eval, quirk corpus, reconcile) green. Run `cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings` clean. Commit: `refactor(99): decompose eval_expr + flatten INDEX/SEARCH below PMAT gate`.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("sheet_ir"))')" && cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings 2>&1 | grep -qv "error" && echo OK</automated>
  </verify>
  <done>No sheet_ir function in PMAT violations; tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("sheet_ir"))'` empty.
- `cargo test -p pmcp-workbook-runtime` green.
- `cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings` clean.
</verification>

<success_criteria>
eval_expr (was 58), f_index (24), f_search (31) cleared from the gate; evaluation semantics unchanged (scalar_eval + quirk corpus green); no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-02-SUMMARY.md` when done.
</output>
