---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-server-toolkit/src/workbook/input.rs
autonomous: true
requirements: [CPLX-03]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists workbook/input.rs::validate_input"
    - "workbook input-validation tests still green"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/workbook/input.rs"
      provides: "validate_input decomposed into per-rule check helpers, cog <= 23"
      contains: "fn validate_input"
  key_links:
    - from: "validate_input"
      to: "per-rule validation helpers"
      via: "thin orchestrator over named check helpers"
      pattern: "fn validate_input"
---

<objective>
Refactor `pmcp-server-toolkit/src/workbook/input.rs::validate_input` (cog 33) below the PMAT gate (target cog <= 23), preserving input-validation behavior exactly. This is the sole flagged function in the server-toolkit crate (CPLX-03).

Purpose: validate_input is the last flagged function blocking PR #279's PMAT gate, in the third affected crate. Own file/crate → parallel with all other refactor plans.
Output: validate_input is a thin orchestrator over per-rule check helpers; gate clears; input-validation tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-server-toolkit/src/workbook/input.rs — validate_input at ~line 91 (verify with `grep -n "fn validate_input" crates/pmcp-server-toolkit/src/workbook/input.rs`). It validates a workbook tool-input value against required fields / types / bounds, surfacing validation errors. The per-rule branches are the complexity driver. The input-validation unit tests in this file (or this crate's workbook tests) are the regression guard.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose validate_input into per-rule check helpers</name>
  <files>crates/pmcp-server-toolkit/src/workbook/input.rs</files>
  <action>
  Confirm live cog via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("workbook/input.rs"))'` and run `cargo test -p pmcp-server-toolkit` for the green baseline. Extract each cohesive validation rule (required-field presence, type check, bounds/range check, per-field shape) into a named private helper returning Result/Option of the validation error. validate_input becomes a thin orchestrator that runs the checks with early-return on the first failure (or accumulates, matching the original's behavior — preserve whichever it does). Preserve the exact error messages/variants and pass/fail decision for every input. Target cog <= 23 for validate_input and each helper.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("workbook/input.rs"))')" && cargo test -p pmcp-server-toolkit 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>validate_input no longer in PMAT violations; input-validation tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Verify clippy + commit</name>
  <files>crates/pmcp-server-toolkit/src/workbook/input.rs</files>
  <action>
  Run `cargo clippy -p pmcp-server-toolkit --all-features -- -D warnings` clean and `cargo test -p pmcp-server-toolkit` green. Commit: `refactor(99): decompose workbook validate_input below PMAT gate`.
  </action>
  <verify>
    <automated>cargo clippy -p pmcp-server-toolkit --all-features -- -D warnings 2>&1 | grep -qv "^error" && cargo test -p pmcp-server-toolkit 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>clippy + tests green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("workbook/input.rs"))'` empty.
- `cargo test -p pmcp-server-toolkit` green; `cargo clippy -p pmcp-server-toolkit --all-features -- -D warnings` clean.
</verification>

<success_criteria>
validate_input (was 33) cleared from the gate; input-validation behavior unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-10-SUMMARY.md` when done.
</output>
