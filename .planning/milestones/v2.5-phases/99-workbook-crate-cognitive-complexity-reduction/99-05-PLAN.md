---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/src/formula/token.rs
autonomous: true
requirements: [CPLX-02]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists tokenize, lex_quoted_sheet_ref, or scan_atom_run"
    - "formula tokenizer tests + dialect linter goldens still green"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/formula/token.rs"
      provides: "tokenize match-to-helper dispatch; lex/scan helpers flattened; each cog <= 23"
      contains: "fn tokenize"
  key_links:
    - from: "tokenize"
      to: "per-token-kind scan helpers"
      via: "main lex loop dispatches to named scan_* helpers"
      pattern: "fn tokenize"
---

<objective>
Refactor the formula tokenizer in `pmcp-workbook-compiler/src/formula/token.rs`: `tokenize` (cog 52 — over the 50 cap, genuine decomposition), `lex_quoted_sheet_ref` (cog 33), and `scan_atom_run` (cog 30). All three live in one file → one plan (CONTEXT.md grouping rule). Target cog <= 23 each, preserving the exact token stream.

Purpose: tokenize is one of the 5 over-cap functions blocking PR #279's PMAT gate; the two lex helpers are flagged in the same file.
Output: tokenize is a thin lex loop dispatching to per-token-kind scan helpers; lex_quoted_sheet_ref/scan_atom_run flattened; gate clears; tokenizer tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-compiler/src/formula/token.rs — tokenize at ~134, lex_quoted_sheet_ref at ~360, scan_atom_run at ~470 (verify all three with `grep -n "fn tokenize\|fn lex_quoted_sheet_ref\|fn scan_atom_run" crates/pmcp-workbook-compiler/src/formula/token.rs`). tokenize is the main lexer loop over chars producing Vec<Token>, surfacing LexError; the two helpers handle quoted sheet refs and atom (identifier/number) runs. The tokenizer unit tests in this file + the dialect linter goldens (which tokenize formulas) are the regression guard.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose tokenize into per-token-kind scan helpers</name>
  <files>crates/pmcp-workbook-compiler/src/formula/token.rs</files>
  <action>
  Confirm live cog via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("formula/token.rs"))'` and run `cargo test -p pmcp-workbook-compiler` for the green baseline. In tokenize, the main lex loop's per-character dispatch (string literal, quoted sheet ref, number, operator, paren, atom/identifier) is the complexity driver — extract each branch into a named private scan helper (e.g. `scan_string_literal`, `scan_number`, `scan_operator`) that consumes from the current position and returns (Token, new_index) or a LexError. tokenize becomes a thin loop that matches the lead char and delegates. Apply early-return on LexError paths. Preserve the exact token sequence and error positions. Target cog <= 23 for tokenize and each helper.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>tokenize refactored; tokenizer tests green (cog re-checked in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Flatten lex_quoted_sheet_ref and scan_atom_run</name>
  <files>crates/pmcp-workbook-compiler/src/formula/token.rs</files>
  <action>
  Verify live cog of both helpers via jq. For lex_quoted_sheet_ref (cog 33): apply early-return guard clauses for the quote-escape / unterminated-ref branches and extract the escaped-quote handling into a sub-helper if needed. For scan_atom_run (cog 30): flatten the character-class accept loop (is-letter/digit/dot/bool-keyword branches) with guard clauses or a small classifier helper that returns whether the char continues the atom. Preserve the exact run boundaries and the bool flags scan_atom_run returns. Target cog <= 23 each.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>Both lex helpers refactored; tokenizer tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify file gate + clippy and commit</name>
  <files>crates/pmcp-workbook-compiler/src/formula/token.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("formula/token.rs"))'` — MUST be empty (tokenize, lex_quoted_sheet_ref, scan_atom_run, and all new helpers <= 23). If any still flags, split further. Run `cargo test -p pmcp-workbook-compiler` (tokenizer + dialect linter goldens) green and `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean. Commit: `refactor(99): decompose formula tokenizer below PMAT gate`.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("formula/token.rs"))')" && cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings 2>&1 | grep -qv "^error" && echo OK</automated>
  </verify>
  <done>No formula/token.rs function in PMAT violations; tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("formula/token.rs"))'` empty.
- `cargo test -p pmcp-workbook-compiler` green; `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean.
</verification>

<success_criteria>
tokenize (was 52), lex_quoted_sheet_ref (33), scan_atom_run (30) cleared from the gate; token stream + lex errors unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-05-SUMMARY.md` when done.
</output>
