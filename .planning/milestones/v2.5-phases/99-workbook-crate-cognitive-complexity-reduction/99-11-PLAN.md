---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 11
type: execute
wave: 2
depends_on: [99-01, 99-02, 99-03, 99-04, 99-05, 99-06, 99-07, 99-08, 99-09, 99-10]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CPLX-01, CPLX-02, CPLX-03, CPLX-04]
gap_closure: true

must_haves:
  truths:
    - "pmat quality-gate --fail-on-violation --checks complexity exits 0 with ZERO violations workspace-wide"
    - "full workspace test suite green (no behavior regressions)"
    - "make quality-gate green"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CPLX-01..04 marked complete in the requirements matrix"
      contains: "CPLX-04"
  key_links:
    - from: "all 21 refactored functions"
      to: "PMAT complexity gate"
      via: "zero violations across the workspace"
      pattern: "quality-gate"
---

<objective>
Phase-level gate closure: prove the org-required PMAT complexity gate now passes workspace-wide with ZERO violations after all 10 refactor plans land, the full test suite is green (no behavior drift), and `make quality-gate` (the merge bar) is green. This is the CPLX-04 verification gate; it also confirms CPLX-01/02/03 collectively cleared.

Purpose: PR #279's CI complexity gate must go green. This plan is the single source of truth that all 21 functions were cleared and nothing regressed. Runs in its own wave after every refactor plan.
Output: zero-violation gate proof, green workspace tests, green make quality-gate; requirements matrix updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-INVENTORY.txt
@.planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md

<read_first>
- .planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-INVENTORY.txt — the authoritative list of the original 21 violations; cross-check that NONE remain.
- .pmatignore — confirm NO production crate (`crates/pmcp-workbook-*`, `crates/pmcp-server-toolkit`) was added; exclusions must still be only fuzz/packages/examples.
- CLAUDE.md "CI Quality Gates" section — the exact gate command + the merge bar (`make quality-gate`).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the org-required PMAT complexity gate workspace-wide</name>
  <files>.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-INVENTORY.txt</files>
  <action>
  Run the EXACT CI gate command `pmat quality-gate --fail-on-violation --checks complexity` and capture its exit code — it MUST exit 0. Then run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("workbook") or (.path | test("pmcp-server-toolkit")))'` — output MUST be empty (all 21 cleared, against both the 25 and recommended-23 tiers). Cross-reference against 99-INVENTORY.txt: confirm none of the original 21 file:function pairs still appears. If ANY violation remains, STOP — report which function still flags so the owning plan (per the crate/file map) is re-run; do not mark the phase complete.
  </action>
  <verify>
    <automated>pmat quality-gate --fail-on-violation --checks complexity; test $? -eq 0 && echo GATE_PASS</automated>
  </verify>
  <done>PMAT complexity gate exits 0 with zero violations; no original-21 function remains flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no gate-weakening and run the full workspace test suite</name>
  <files>.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-INVENTORY.txt</files>
  <action>
  Confirm `.pmatignore` contains NO production crate (grep it for `pmcp-workbook` / `pmcp-server-toolkit` — must be absent) and that no `#[allow(clippy::cognitive_complexity)]` was added to clear the gate (`grep -rn "allow(clippy::cognitive_complexity)" crates/pmcp-workbook-runtime crates/pmcp-workbook-compiler crates/pmcp-server-toolkit/src/workbook` — none should have been relied on for these functions; per D-10-B such an allow would be a no-op anyway). Then run the full workspace test suite covering all three crates plus the golden/reconcile/quirk nets: `cargo test -p pmcp-workbook-runtime -p pmcp-workbook-compiler -p pmcp-server-toolkit` — all green (penny-reconcile, quirk corpus, dialect linter, provenance gate, scalar_eval, backward_compat goldens). Any red = behavior regression; STOP and report.
  </action>
  <verify>
    <automated>! grep -qE "pmcp-workbook|pmcp-server-toolkit" .pmatignore && cargo test -p pmcp-workbook-runtime -p pmcp-workbook-compiler -p pmcp-server-toolkit 2>&1 | grep -q "test result: ok" && echo NO_WEAKEN_AND_GREEN</automated>
  </verify>
  <done>.pmatignore unchanged for production crates; all three crates' test suites + regression nets green.</done>
</task>

<task type="auto">
  <name>Task 3: Run make quality-gate (merge bar) and close out requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
  Run `make quality-gate` (fmt --all, clippy pedantic+nursery, build, test, audit) — MUST be green; this is the PR #279 merge bar. If clippy flags any extracted helper, fix and re-run. Once green: mark CPLX-01, CPLX-02, CPLX-03, CPLX-04 complete in `.planning/REQUIREMENTS.md` (checkboxes `[x]` and matrix rows `Pending` -> `Complete`). Commit: `docs(99): close PMAT complexity gate — 21/21 functions cleared, gate green`.
  </action>
  <verify>
    <automated>make quality-gate 2>&1 | tail -5 | grep -qiE "passed|success|ok" && grep -q "CPLX-04.*Complete" .planning/REQUIREMENTS.md && echo CLOSED</automated>
  </verify>
  <done>make quality-gate green; CPLX-01..04 marked complete; committed.</done>
</task>

</tasks>

<verification>
- `pmat quality-gate --fail-on-violation --checks complexity` exits 0; zero workbook/toolkit violations.
- `.pmatignore` has no production crate; no `#[allow]` relied on.
- `cargo test -p pmcp-workbook-runtime -p pmcp-workbook-compiler -p pmcp-server-toolkit` green.
- `make quality-gate` green.
</verification>

<success_criteria>
CPLX-04 satisfied: PMAT complexity gate passes workspace-wide with ZERO violations; all 21 functions cleared (CPLX-01/02/03); no production crate in `.pmatignore`; no `#[allow]` reliance; full test suite + `make quality-gate` green; PR #279's CI complexity gate will go green on next run.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-11-SUMMARY.md` when done.
</output>
