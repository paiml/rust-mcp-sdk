---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/src/ingest/mod.rs
autonomous: true
requirements: [CPLX-02]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists ingest or references_external_workbook"
    - "ingest goldens still green (ingests_authored_colours_and_workbook_metadata)"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/ingest/mod.rs"
      provides: "ingest decomposed into per-phase helpers; references_external_workbook flattened; each cog <= 23"
      contains: "fn ingest"
  key_links:
    - from: "ingest"
      to: "per-phase ingest helpers"
      via: "thin orchestrator over parse/validate/build helpers"
      pattern: "fn ingest"
---

<objective>
Refactor the workbook ingest entry point in `pmcp-workbook-compiler/src/ingest/mod.rs`: `ingest` (cog 57 — over the 50 cap, genuine decomposition) and `references_external_workbook` (cog 31). Both in one file → one plan. Target cog <= 23 each, preserving ingest output (WorkbookMap + LintFindings) and external-ref detection exactly.

Purpose: ingest is one of the 5 over-cap functions blocking PR #279's PMAT gate; references_external_workbook is flagged in the same file.
Output: ingest is a thin orchestrator over per-phase helpers; references_external_workbook flattened; gate clears; ingest goldens green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-compiler/src/ingest/mod.rs — references_external_workbook at ~69, ingest at ~106 (verify with `grep -n "fn ingest\|fn references_external_workbook" crates/pmcp-workbook-compiler/src/ingest/mod.rs`). ingest reads an xlsx Path and returns (WorkbookMap, Vec<LintFinding>), surfacing IngestError. The test `ingests_authored_colours_and_workbook_metadata` (~569, same file) is the regression guard; `ingest_with_cap` (~812) is a test helper.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose ingest into per-phase helpers</name>
  <files>crates/pmcp-workbook-compiler/src/ingest/mod.rs</files>
  <action>
  Confirm live cog via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("ingest/mod.rs"))'` and run `cargo test -p pmcp-workbook-compiler` for the green baseline. Split ingest into its natural phases — each becomes a named private helper (e.g. open/parse the xlsx parts, extract sheets+cells into the WorkbookMap, collect colour/metadata, run per-cell lint checks accumulating LintFindings). ingest becomes a thin orchestrator threading the WorkbookMap + findings vec through the helpers. Apply early-return on IngestError paths. Preserve the exact WorkbookMap contents, LintFinding set/order, and error variants. Target cog <= 23 for ingest and each helper.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>ingest refactored; ingest goldens green (cog re-checked in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Flatten references_external_workbook</name>
  <files>crates/pmcp-workbook-compiler/src/ingest/mod.rs</files>
  <action>
  Verify live cog (~31) via jq. references_external_workbook scans a formula string for external-workbook reference syntax (e.g. `[...]Sheet!`, bracketed indices, quoted external paths). Apply early-return guard clauses (return true on first external-ref match) and extract any cohesive scan sub-step into a named helper. Preserve the exact true/false decision for every formula shape. Target cog <= 23.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>references_external_workbook refactored; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify file gate + clippy and commit</name>
  <files>crates/pmcp-workbook-compiler/src/ingest/mod.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("ingest/mod.rs"))'` — MUST be empty. Run `cargo test -p pmcp-workbook-compiler` green and `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean. Commit: `refactor(99): decompose ingest + references_external_workbook below PMAT gate`.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("ingest/mod.rs"))')" && cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings 2>&1 | grep -qv "^error" && echo OK</automated>
  </verify>
  <done>No ingest/mod.rs function in PMAT violations; tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("ingest/mod.rs"))'` empty.
- `cargo test -p pmcp-workbook-compiler` green; `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean.
</verification>

<success_criteria>
ingest (was 57) and references_external_workbook (31) cleared from the gate; ingest output + external-ref detection unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-06-SUMMARY.md` when done.
</output>
