---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/src/gate/corpus.rs
autonomous: true
requirements: [CPLX-02]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists derive_case_grid or no_seeded_value_outside_allowed"
    - "gate/corpus tests still green"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/gate/corpus.rs"
      provides: "derive_case_grid and no_seeded_value_outside_allowed decomposed, each cog <= 23"
      contains: "fn derive_case_grid"
  key_links:
    - from: "no_seeded_value_outside_allowed"
      to: "per-case check helper"
      via: "thin loop over named check helper"
      pattern: "fn no_seeded_value_outside_allowed"
---

<objective>
Refactor the approval-corpus gate in `pmcp-workbook-compiler/src/gate/corpus.rs`: `derive_case_grid` (cog 34) and `no_seeded_value_outside_allowed` (cog 46). Both in one file → one plan. Target cog <= 23 each, preserving the generated case grid and the seeded-value validation decision exactly.

Purpose: these are flagged gate-corpus functions blocking PR #279's PMAT gate.
Output: both functions decomposed into named helpers; gate clears; corpus tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-compiler/src/gate/corpus.rs — derive_case_grid at ~257, no_seeded_value_outside_allowed at ~441 (verify with `grep -n "fn derive_case_grid\|fn no_seeded_value_outside_allowed" crates/pmcp-workbook-compiler/src/gate/corpus.rs`). derive_case_grid builds a Vec of (String, BTreeMap<...>) case rows from a Manifest; no_seeded_value_outside_allowed validates seeded values against an allowed set across approval cases. The corpus unit tests in this file are the regression guard.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose derive_case_grid</name>
  <files>crates/pmcp-workbook-compiler/src/gate/corpus.rs</files>
  <action>
  Confirm live cog via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("gate/corpus.rs"))'` and run `cargo test -p pmcp-workbook-compiler` for the green baseline. derive_case_grid's per-cell / per-case grid construction is the driver — extract the per-row (or per-cell) build step into a named helper (e.g. `build_case_row`) and apply guard-clause flattening on any skip/filter branches. Preserve the exact grid rows + BTreeMap contents and their order. Target cog <= 23.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>derive_case_grid refactored; corpus tests green (cog re-checked in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Decompose no_seeded_value_outside_allowed</name>
  <files>crates/pmcp-workbook-compiler/src/gate/corpus.rs</files>
  <action>
  Verify live cog (~46) via jq. The nested loop over approval cases × seeded values with the allowed-set check is the driver — extract the per-case (or per-value) validation into a named helper (e.g. `case_seeded_values_allowed` returning a bool or the offending finding) and apply early-return on the first violation. Preserve the exact accept/reject decision and any returned finding/message. Target cog <= 23.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>no_seeded_value_outside_allowed refactored; corpus tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify file gate + clippy and commit</name>
  <files>crates/pmcp-workbook-compiler/src/gate/corpus.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("gate/corpus.rs"))'` — MUST be empty. Run `cargo test -p pmcp-workbook-compiler` green and `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean. Commit: `refactor(99): decompose gate corpus functions below PMAT gate`.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("gate/corpus.rs"))')" && cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings 2>&1 | grep -qv "^error" && echo OK</automated>
  </verify>
  <done>No gate/corpus.rs function in PMAT violations; tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("gate/corpus.rs"))'` empty.
- `cargo test -p pmcp-workbook-compiler` green; `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean.
</verification>

<success_criteria>
derive_case_grid (34) and no_seeded_value_outside_allowed (46) cleared from the gate; case-grid generation + seeded-value validation unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-08-SUMMARY.md` when done.
</output>
