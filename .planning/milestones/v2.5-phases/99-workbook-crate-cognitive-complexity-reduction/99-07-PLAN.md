---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs
  - crates/pmcp-workbook-compiler/src/provenance/gate.rs
autonomous: true
requirements: [CPLX-02]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists parse_calc_pr, parse_app_props, or gate_inner"
    - "provenance gate tests still green"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs"
      provides: "parse_calc_pr and parse_app_props decomposed into per-field parse helpers, each cog <= 23"
      contains: "fn parse_calc_pr"
    - path: "crates/pmcp-workbook-compiler/src/provenance/gate.rs"
      provides: "gate_inner flattened, cog <= 23"
      contains: "fn gate_inner"
  key_links:
    - from: "parse_calc_pr"
      to: "per-attribute parse helpers"
      via: "thin parse loop over named helpers"
      pattern: "fn parse_calc_pr"
---

<objective>
Refactor the provenance subsystem in `pmcp-workbook-compiler`: `provenance/raw_parts.rs::parse_calc_pr` (cog 44) and `parse_app_props` (cog 39), plus `provenance/gate.rs::gate_inner` (cog 29). Two files, both in the provenance subsystem → one plan. Target cog <= 23 each, preserving parsed RawCalcPr/RawAppProps values and gate decisions exactly.

Purpose: these are flagged provenance functions blocking PR #279's PMAT gate. raw_parts.rs's two parsers (~44/~39) are the heavy items; gate_inner (~29) is lighter.
Output: parsers decomposed into per-field helpers; gate_inner flattened; gate clears; provenance tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs — parse_calc_pr at ~165, parse_app_props at ~276 (verify with grep). Both parse XML part bytes into Raw* structs, surfacing ProvenanceError — branchy attribute/element extraction loops.
- crates/pmcp-workbook-compiler/src/provenance/gate.rs — gate_inner at ~272 (verify with grep). The provenance gate decision logic.
- The provenance gate tests (and backward_compat_* provenance goldens) are the regression guard.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose parse_calc_pr and parse_app_props into per-field helpers</name>
  <files>crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs</files>
  <action>
  Confirm live cog of both via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("raw_parts.rs"))'` and run `cargo test -p pmcp-workbook-compiler` for the green baseline. For each parser: the per-attribute / per-element extraction branches are the complexity driver — extract attribute parsing into a named helper (e.g. `parse_calc_pr_attr` returning the parsed field, or a match over attribute name) and apply early-return on ProvenanceError paths. Where the XML walk has nested element handling, pull each element handler into its own helper. Preserve the exact RawCalcPr / RawAppProps field values and error variants for every input. Target cog <= 23 for both parsers and helpers.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("raw_parts.rs"))')" && cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>parse_calc_pr and parse_app_props no longer in PMAT violations; provenance tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten gate_inner in gate.rs</name>
  <files>crates/pmcp-workbook-compiler/src/provenance/gate.rs</files>
  <action>
  Verify live cog of gate_inner (~29) via jq on `gate.rs`. Apply early-return guard clauses on each fail-fast provenance check and extract any cohesive check cluster into a named helper (e.g. `check_provenance_invariant`). Preserve the exact pass/fail decision and error/finding output. Target cog <= 23.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("provenance/gate.rs"))')" && cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>gate_inner no longer in PMAT violations; provenance gate tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify subsystem gate + clippy and commit</name>
  <files>crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs, crates/pmcp-workbook-compiler/src/provenance/gate.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("provenance"))'` — MUST be empty. Run `cargo test -p pmcp-workbook-compiler` green and `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean. Commit: `refactor(99): decompose provenance parsers + gate_inner below PMAT gate`.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("provenance"))')" && cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings 2>&1 | grep -qv "^error" && echo OK</automated>
  </verify>
  <done>No provenance function in PMAT violations; tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("provenance"))'` empty.
- `cargo test -p pmcp-workbook-compiler` green; `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean.
</verification>

<success_criteria>
parse_calc_pr (44), parse_app_props (39), gate_inner (29) cleared from the gate; provenance parse + gate behavior unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-07-SUMMARY.md` when done.
</output>
