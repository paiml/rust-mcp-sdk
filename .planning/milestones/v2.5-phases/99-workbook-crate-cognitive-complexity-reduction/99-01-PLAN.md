---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-runtime/src/render/mod.rs
autonomous: true
requirements: [CPLX-01]
gap_closure: true

must_haves:
  truths:
    - "pmat quality-gate --checks complexity no longer lists render_xlsx"
    - "render_xlsx behavior unchanged — golden/reconcile/quirk fixtures still green"
  artifacts:
    - path: "crates/pmcp-workbook-runtime/src/render/mod.rs"
      provides: "render_xlsx decomposed into orchestrator + named private helpers, each cog <= 23"
      contains: "fn render_xlsx"
  key_links:
    - from: "render_xlsx"
      to: "extracted private helpers"
      via: "thin orchestrator calls per-phase helpers"
      pattern: "fn render_xlsx"
---

<objective>
Refactor `pmcp-workbook-runtime/src/render/mod.rs::render_xlsx` (cognitive complexity 93 — the single highest in the phase) below the PMAT gate threshold (target cog <= 23) by genuine structural decomposition, preserving exact byte-deterministic XLSX output.

Purpose: render_xlsx is one of the 5 functions over the cog-50 hard cap. It blocks PR #279's org-required PMAT complexity gate. `#[allow]` is a no-op for PMAT (Phase 75 D-10-B) — refactor is the only path.
Output: render_xlsx reduced to a thin orchestrator over named private helpers; gate clears; render goldens green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md
@.planning/phases/75-fix-pmat-issues/pmat-allow-behavior.md

<read_first>
- crates/pmcp-workbook-runtime/src/render/mod.rs — render_xlsx at line ~227 (line numbers may have drifted; locate via `grep -n "fn render_xlsx" crates/pmcp-workbook-runtime/src/render/mod.rs`). The same file holds the render unit tests (render_xlsx_produces_valid_zip_container, render_xlsx_is_deterministic_byte_identical, render_xlsx_writes_formula_with_finite_cached_result, render_xlsx_replays_merge_top_left_only, render_xlsx_writes_text_and_bool_and_falls_back_on_error_value, etc.) — these are the regression guard; do not change their assertions.
- crates/pmcp-workbook-runtime/src/render/mod.rs LayoutDescriptor + RunResult input types — note what render_xlsx reads so helper signatures borrow rather than clone where the original did.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot the render_xlsx baseline and its live complexity</name>
  <files>crates/pmcp-workbook-runtime/src/render/mod.rs</files>
  <action>
  Before editing: run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("render/mod.rs"))'` to confirm render_xlsx's live cog (expected ~93) and current line. Run `cargo test -p pmcp-workbook-runtime` and record that the render_xlsx_* unit tests pass — this is the GREEN baseline you must preserve. Read the full body of render_xlsx and identify its natural phases (typically: build workbook/sheet scaffold, write headers/labels, iterate cells writing values vs formulas vs merges, apply styles/formats, serialize to bytes). Note each distinct sub-step as a future helper boundary. Do not edit yet.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-runtime 2>&1 | grep -q "test result: ok" && echo BASELINE_GREEN</automated>
  </verify>
  <done>Live cog of render_xlsx confirmed; render_xlsx_* tests pass; helper-boundary plan noted.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose render_xlsx into per-phase private helpers</name>
  <files>crates/pmcp-workbook-runtime/src/render/mod.rs</files>
  <action>
  Extract each cohesive sub-step of render_xlsx into a named private helper (e.g. `write_cell_value`, `write_formula_cell`, `apply_merges`, `apply_cell_style`, `build_sheet_scaffold`, `serialize_workbook_bytes` — exact names at your discretion). render_xlsx becomes a thin orchestrator that calls them in order. Apply early-return guard clauses where the original nests error handling (non-finite computed value rejection, malformed addr surfaced as error, single-cell merge rejection — preserve these exact error paths, the tests assert them). The per-cell write loop is the dominant complexity driver: pull the value/formula/bool/text/error-fallback dispatch into a helper with a flat match. Keep all helpers private `fn` (no doc requirement). Preserve byte-deterministic output exactly — do not reorder writes. Per CPLX-01: target cog <= 23 for render_xlsx AND every new helper.
  </action>
  <verify>
    <automated>grep -q "fn render_xlsx" crates/pmcp-workbook-runtime/src/render/mod.rs && cargo build -p pmcp-workbook-runtime 2>&1 | grep -qE "Compiling|Finished" && echo BUILT</automated>
  </verify>
  <done>render_xlsx is a thin orchestrator over named private helpers; crate compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Verify gate clears and render behavior is preserved</name>
  <files>crates/pmcp-workbook-runtime/src/render/mod.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("render/mod.rs"))'` — output MUST be empty (render_xlsx and all new helpers <= 23; an empty result means none flagged against either the 25 or recommended-23 tier). If any helper still flags, split it further. Run `cargo test -p pmcp-workbook-runtime` — all render_xlsx_* and scalar_eval tests green (byte-determinism, formula caching, merge replay, text/bool fallback all unchanged). Run `cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings` to confirm no new lint (extracted private helpers need no docs). Commit: `refactor(99): decompose render_xlsx below PMAT cog gate`.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("render/mod.rs"))')" && cargo test -p pmcp-workbook-runtime 2>&1 | grep -q "test result: ok" && echo GATE_CLEAR_AND_GREEN</automated>
  </verify>
  <done>No render/mod.rs function appears in PMAT violations; pmcp-workbook-runtime tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("render/mod.rs"))'` returns empty.
- `cargo test -p pmcp-workbook-runtime` green (render goldens, byte-determinism, formula caching).
- `cargo clippy -p pmcp-workbook-runtime --all-features -- -D warnings` clean.
</verification>

<success_criteria>
render_xlsx (was cog 93) no longer appears in the PMAT complexity gate; render behavior is byte-identical (existing render_xlsx_* tests green); no behavior change, no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-01-SUMMARY.md` when done.
</output>
