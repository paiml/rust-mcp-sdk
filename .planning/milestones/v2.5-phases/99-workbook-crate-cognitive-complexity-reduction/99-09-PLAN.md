---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/src/dialect/linter.rs
  - crates/pmcp-workbook-compiler/src/fixture_author.rs
  - crates/pmcp-workbook-compiler/src/dag/resolve.rs
autonomous: true
requirements: [CPLX-02]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists extract_function_tokens, author_xlsx, or walk"
    - "dialect linter, fixture-author, and dag-resolve tests still green"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/dialect/linter.rs"
      provides: "extract_function_tokens flattened, cog <= 23"
      contains: "fn extract_function_tokens"
    - path: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      provides: "author_xlsx decomposed, cog <= 23"
      contains: "fn author_xlsx"
    - path: "crates/pmcp-workbook-compiler/src/dag/resolve.rs"
      provides: "walk flattened, cog <= 23"
      contains: "fn walk"
  key_links:
    - from: "author_xlsx"
      to: "per-part write helpers"
      via: "thin orchestrator over named helpers"
      pattern: "fn author_xlsx"
---

<objective>
Refactor the three remaining single-function compiler hotspots, each in its own file: `dialect/linter.rs::extract_function_tokens` (cog 29), `fixture_author.rs::author_xlsx` (cog 29), and `dag/resolve.rs::walk` (cog 25 — flagged against the recommended-23 tier, a single guard-clause extraction likely clears it). Target cog <= 23 each, preserving behavior.

Purpose: these are the last three flagged compiler functions (each below the 50 cap) blocking PR #279's PMAT gate. Grouped because each owns its file — no intra-file conflict.
Output: all three flattened/decomposed; gate clears; affected tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-compiler/src/dialect/linter.rs — extract_function_tokens at ~339 (verify with grep). Scans a formula string and returns Vec<String> of function-name tokens; the dialect linter goldens guard it.
- crates/pmcp-workbook-compiler/src/fixture_author.rs — author_xlsx at ~171 (verify with grep). Writes a WorkbookSpec out to an xlsx path, surfacing XlsxError; the per-part write branches drive complexity. Fixture-author tests guard it.
- crates/pmcp-workbook-compiler/src/dag/resolve.rs — walk at ~133 (verify with grep). DAG traversal; near-threshold, do not over-engineer.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten extract_function_tokens and walk</name>
  <files>crates/pmcp-workbook-compiler/src/dialect/linter.rs, crates/pmcp-workbook-compiler/src/dag/resolve.rs</files>
  <action>
  Confirm live cog of both via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("dialect/linter.rs") or (.path | test("dag/resolve.rs")))'` and run `cargo test -p pmcp-workbook-compiler` for the green baseline. For extract_function_tokens (~29): the char-scan loop that detects function-name + `(` patterns is the driver — extract the token-recognition step into a named helper (e.g. `scan_function_token`) and apply guard-clause flattening on skip branches; preserve the exact token list. For walk (~25, recommended-23 tier): apply early-continue/guard-clause flattening on the visited-set check or extract the per-node visit body into a helper — a single extraction; do not over-engineer. Target cog <= 23 each.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("dialect/linter.rs") or (.path | test("dag/resolve.rs")))')" && cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>extract_function_tokens and walk no longer in PMAT violations; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose author_xlsx into per-part write helpers</name>
  <files>crates/pmcp-workbook-compiler/src/fixture_author.rs</files>
  <action>
  Verify live cog (~29) via jq on `fixture_author.rs`. The per-part / per-sheet write branches are the driver — extract each cohesive write step (write sheet, write cells, write styles/colours, write metadata) into a named private helper. author_xlsx becomes a thin orchestrator. Apply early-return on XlsxError paths. Preserve the exact bytes/structure written so fixture-author tests still pass. Target cog <= 23.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("fixture_author.rs"))')" && cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>author_xlsx no longer in PMAT violations; fixture-author tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify gate for all three files + clippy and commit</name>
  <files>crates/pmcp-workbook-compiler/src/dialect/linter.rs, crates/pmcp-workbook-compiler/src/fixture_author.rs, crates/pmcp-workbook-compiler/src/dag/resolve.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("dialect/linter.rs") or (.path | test("fixture_author.rs")) or (.path | test("dag/resolve.rs")))'` — MUST be empty. Run `cargo test -p pmcp-workbook-compiler` green and `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean. Commit: `refactor(99): flatten linter/fixture-author/dag-walk below PMAT gate`.
  </action>
  <verify>
    <automated>cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings 2>&1 | grep -qv "^error" && cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>None of the three functions in PMAT violations; tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ...` shows none of extract_function_tokens / author_xlsx / walk.
- `cargo test -p pmcp-workbook-compiler` green; `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean.
</verification>

<success_criteria>
extract_function_tokens (29), author_xlsx (29), walk (25) cleared from the gate; behavior unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-09-SUMMARY.md` when done.
</output>
