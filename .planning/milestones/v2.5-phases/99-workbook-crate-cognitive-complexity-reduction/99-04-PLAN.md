---
phase: 99-workbook-crate-cognitive-complexity-reduction
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/src/change_class/mod.rs
  - crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs
autonomous: true
requirements: [CPLX-02]
gap_closure: true

must_haves:
  truths:
    - "pmat gate no longer lists classify_cell_roles or dependency_order"
    - "change-classification / IR-identity tests still green"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/change_class/mod.rs"
      provides: "classify_cell_roles decomposed, cog <= 23"
      contains: "fn classify_cell_roles"
    - path: "crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs"
      provides: "dependency_order flattened, cog <= 23"
      contains: "fn dependency_order"
  key_links:
    - from: "classify_cell_roles"
      to: "per-role classification helpers"
      via: "thin orchestrator over named helpers"
      pattern: "fn classify_cell_roles"
---

<objective>
Refactor the `pmcp-workbook-compiler` change-classification subsystem: `change_class/mod.rs::classify_cell_roles` (cog 74 — over the 50 cap, genuine decomposition) and `change_class/ir_identity.rs::dependency_order` (cog 24 — flagged against the recommended-23 tier, a single guard-clause extraction likely clears it). Target cog <= 23 each, preserving classification + ordering behavior.

Purpose: classify_cell_roles is one of the 5 over-cap functions blocking PR #279's PMAT gate. dependency_order is a near-threshold sibling in the same subsystem.
Output: classify_cell_roles is a thin orchestrator over per-role helpers; dependency_order flattened; gate clears; change_class tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-CONTEXT.md

<read_first>
- crates/pmcp-workbook-compiler/src/change_class/mod.rs — classify_cell_roles at ~line 153 (verify with grep). It diffs prev vs current Manifest and pushes (ChangeRole, ...) tuples into `out`. The branchy core is the per-cell role decision (added/removed/modified/formula-changed/value-only/etc.).
- crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs — dependency_order at ~line 174 (verify with grep). Topological/visited-set ordering over an IR HashMap.
- The change-classification and ir-identity unit tests in these files (and any backward_compat_* goldens that exercise change classes) are the regression guard.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose classify_cell_roles into per-role helpers</name>
  <files>crates/pmcp-workbook-compiler/src/change_class/mod.rs</files>
  <action>
  Confirm live cog via `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("change_class/mod.rs"))'` and run `cargo test -p pmcp-workbook-compiler` for the green baseline. Extract the per-cell role-decision logic into a named private helper that returns the ChangeRole (or pushes one tuple), e.g. `classify_single_cell` taking prev-cell + current-cell and returning the role. Pull sub-decisions (formula-changed vs value-only, added vs removed, style/format-only change) into further small helpers or a flat match. classify_cell_roles becomes a loop that delegates per cell. Apply early-return/continue guard clauses to flatten the diff loop. Preserve the exact set and ordering of emitted (ChangeRole, ...) tuples. Target cog <= 23 for classify_cell_roles and helpers.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("change_class/mod.rs"))')" && cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>classify_cell_roles no longer in PMAT violations; change_class tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten dependency_order in ir_identity.rs</name>
  <files>crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs</files>
  <action>
  Verify live cog of dependency_order (~24) via jq on `ir_identity.rs`. Apply guard-clause / early-continue flattening to the visited-set traversal, and if needed extract the per-node visit step into a named helper (e.g. `visit_dependencies`). Do not over-engineer (CONTEXT.md: near-threshold fns need a single extraction). Preserve the exact ordering the function produces. Target cog <= 23.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("ir_identity.rs"))')" && cargo test -p pmcp-workbook-compiler 2>&1 | grep -q "test result: ok" && echo OK</automated>
  </verify>
  <done>dependency_order no longer in PMAT violations; ir_identity tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify subsystem gate + clippy and commit</name>
  <files>crates/pmcp-workbook-compiler/src/change_class/mod.rs, crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs</files>
  <action>
  Run `pmat analyze complexity --max-cognitive 25 --format json | jq '.violations[] | select(.path | test("change_class"))'` — MUST be empty. Run `cargo test -p pmcp-workbook-compiler` green and `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean. Commit: `refactor(99): decompose classify_cell_roles + dependency_order below PMAT gate`.
  </action>
  <verify>
    <automated>test -z "$(pmat analyze complexity --max-cognitive 25 --format json | jq -c '.violations[] | select(.path | test("change_class"))')" && cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings 2>&1 | grep -qv "^error" && echo OK</automated>
  </verify>
  <done>No change_class function in PMAT violations; tests + clippy green; committed.</done>
</task>

</tasks>

<verification>
- `pmat analyze complexity ... | jq 'select(.path | test("change_class"))'` empty.
- `cargo test -p pmcp-workbook-compiler` green; `cargo clippy -p pmcp-workbook-compiler --all-features -- -D warnings` clean.
</verification>

<success_criteria>
classify_cell_roles (was 74) and dependency_order (24) cleared from the gate; change-classification + IR-identity behavior unchanged; no `#[allow]`, no `.pmatignore` edit.
</success_criteria>

<output>
Create `.planning/phases/99-workbook-crate-cognitive-complexity-reduction/99-04-SUMMARY.md` when done.
</output>
