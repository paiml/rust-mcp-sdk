---
phase: 124-release-publish-order
plan: 05
type: execute
wave: 5
depends_on: ["124-03", "124-04"]
files_modified:
  - Cargo.toml
  - CHANGELOG.md
  - crates/pmcp-package/Cargo.toml
  - cargo-pmcp/Cargo.toml
  - cargo-pmcp/src/templates/agent.rs
  - cargo-pmcp/tests/pmcp_package_pin.rs
  - crates/pmcp-agent/Cargo.toml
  - crates/pmcp-team-servers/Cargo.toml
  - crates/pmcp-cfn-renderer/Cargo.toml
  - crates/pmcp-openapi-server/tests/pmcp_package_pin.rs
  - crates/pmcp-code-mode-derive/Cargo.toml
  - crates/pmcp-workbook-runtime/Cargo.toml
  - crates/pmcp-workbook-compiler/Cargo.toml
  - .planning/phases/124-release-publish-order/124-expected-release.json
autonomous: true
requirements: [PKGR-01]

estimate:
  tokens: 42000
  raw_tokens: 84000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "The root `pmcp` crate's version is 2.19.1, one patch above the version currently published on crates.io (D-03)."
    - "Exactly the set of `(crate, target version)` pairs recorded at plan 03's checkpoint has been bumped — no crate outside that list has a changed version literal."
    - "If `pmcp-package`'s version moved, every one of its nine version emitters moved in the SAME commit: the crate's own `[package].version`, the four consuming manifests, `PMCP_PACKAGE_VERSION_REQ` in cargo-pmcp's agent template, the two pin-tripwire constants, and `crates/pmcp-openapi-server/Cargo.toml`'s path-only dev-dep — the ninth, whose correct action is to remain UNCHANGED and which is therefore the one most easily dropped from the list."
    - "SEPARATE FROM THE NINE — if `pmcp-package`'s version moved by a semver-INCOMPATIBLE step, the four crates that pin it each need their own `[package].version` bump as a consequence, because their published artifacts change. Phase 122 tracked exactly this as row C1 (`cargo-pmcp` 0.22.0 -> 0.23.0), OUTSIDE the nine-emitter set. CLAUDE.md item 13's ⚠ ORDERING CONSTRAINT states it normatively: `pmcp-package` and the crates that pin it move as one set, or not at all."
    - "`cargo test -p cargo-pmcp --test pmcp_package_pin` passes, and `cargo-pmcp`'s library test `emitted_package_requirement_matches_workspace_major_minor_line` passes with a NON-ZERO test count — that emitter is invisible to `cargo build`, so a green build does not cover it, and a filter selecting zero tests passes vacuously."
    - "`crates/pmcp-openapi-server`'s `pmcp-package` dev-dependency is still path-only with no version key — that crate publishes several steps before pmcp-package, and a retained version requirement cannot resolve at publish time."
    - "No `mcp-tester` version requirement changed in any manifest, unless plan 03's checkpoint explicitly authorised it together with a resolution for the four crates that pin it and publish BEFORE it."
    - "All version changes authorised by plan 03's checkpoint land in ONE commit, so no intermediate commit exists in which a version-emitter set is half-moved."
    - "Running `.github/workflows/release.yml`'s own changelog-extraction awk for the shipped version against CHANGELOG.md yields NON-EMPTY output."
    - "`124-expected-release.json` exists, is committed, and enumerates two disjoint sets — crates expected to publish NEWLY at this tag, and crates expected to be SKIPPED because their version is already published — so plans 06 and 07 verify against a committed artifact rather than against branch-local summaries."
  artifacts:
    - "CHANGELOG.md section `## [2.19.1] - <date>`"
    - "Bumped version literals in exactly the manifests the checkpoint authorised"
    - ".planning/phases/124-release-publish-order/124-expected-release.json — the committed release manifest (tag, expected-new pairs, expected-skips, registry baselines, authorised exceptions)"
    - "A per-manifest before/after version table keyed by manifest path"
  key_links:
    - "the CHANGELOG heading form `## [X.Y.Z] - YYYY-MM-DD` is load-bearing: release.yml awk-matches `## \\[<version-without-v>\\]` and yields an empty string, exit 0, on no match — the GitHub Release is then created with empty notes, silently"
    - "pmcp-package's version <-> four consumer manifest pins <-> PMCP_PACKAGE_VERSION_REQ <-> the two pin-tripwire constants <-> the openapi-server path-only dev-dep: nine emitters, one set, one commit"
    - "the nine emitters govern FORMAT COMPATIBILITY; the four consumers' own [package].version bumps are a SEPARATE consequence set (Phase 122's row C1) — conflating them is how a consumer ships unbumped against an incompatible package"
    - "the tag chosen in D-06 (v2.19.1) must equal the version in the CHANGELOG heading, or the extraction misses"
    - "124-expected-release.json is the state-continuity mechanism: it is committed into the release PR, so it survives the merge and is readable from merged main by plans 06 and 07, which branch-local summaries are not"
  prohibitions:
    - "Must not bump any crate that was not explicitly authorised at plan 03's checkpoint, and must not skip one that was. Every published version number is consumed permanently."
    - "Must not bump only part of a version-emitter set. One emitter — the scaffold template constant — is invisible to `cargo build`; a partial bump can commit with a green build and fail only in a library test."
    - "Must not treat the nine-emitter set as covering the four consumers' own `[package].version` numbers. Those are a separate consequence set; moving the pins without moving the versions ships consumers whose published artifacts changed without a new version."
    - "Must not change any `mcp-tester` version requirement in `crates/pmcp-server-toolkit/Cargo.toml:192`, `crates/pmcp-sql-server/Cargo.toml:57`, `crates/pmcp-openapi-server/Cargo.toml:63` or `crates/pmcp-workbook-server/Cargo.toml:58` unless plan 03's checkpoint authorised it WITH a stated ordering resolution. All four publish before `mcp-tester` itself (:401) and carry retained version requirements, so moving them to an unpublished version kills the release job at the first of them."
    - "Must not bump `crates/mcp-tester` or `cargo-pmcp` on account of the `pmcp` 2.19.0 -> 2.19.1 move. Both pin `pmcp = \"2.19.0\"` and `^2.19.0` already admits 2.19.1; a reviewer's call to bump them is rejected on the mechanics, and plan 04 records the caret exception in the ledger."
    - "Must not add a `version` key to `crates/pmcp-openapi-server`'s `pmcp-package` dev-dependency while editing pins elsewhere; it must remain path-only."
    - "Must not add a crypto or signing dependency to `crates/pmcp-package` while editing its manifest — that crate's no-crypto boundary is machine-checked and must stay clean."
    - "Must not verify the authorised bump set by counting `^+version = ` lines in a unified diff. That count does not associate a line with its manifest and is blind to dependency requirement changes, so it can equal the expected number while the WRONG crates moved."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land any change in this phase."
---

<objective>
Consume exactly the version numbers plan 03's checkpoint authorised, move every emitter of a
bumped version as one set, and write the CHANGELOG section without which the GitHub Release is
created with empty notes.

Purpose: this is the point of no return's last reversible step. The bumps decided here are what
the tag publishes; a missed emitter ships a CLI pinned to a package version it cannot read, and
a missed CHANGELOG heading silently empties the release notes.
Output: bumped manifests, a synchronised emitter set, and a `## [2.19.1]` section proven
non-empty by re-running the workflow's own extraction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-PATTERNS.md
@.planning/phases/124-release-publish-order/124-03-SUMMARY.md
@.planning/phases/124-release-publish-order/124-04-SUMMARY.md
@CHANGELOG.md
@Cargo.toml
</context>

<review_dispositions>
## Cross-AI review findings dispositioned in this plan

Source: `124-REVIEWS.md`. With plan 03, this is where the review concentrated the irreversible
risk.

| Finding | Severity | Disposition |
|---|---|---|
| The "nine emitters" inventory is internally inconsistent and totals eight (Codex HIGH) | HIGH — **corrected** | **Rejected as stated; this plan was already correct.** `122-08-SUMMARY.md:464-476` lists exactly nine, row 6 being the openapi-server path-only UNCHANGED entry, and this plan's must_haves already carried it. The count is not reduced. The actionable residue is CLAUDE.md's PROSE, which says "Nine" and enumerates eight — fixed in plan 04 Task 2 Edit 3, not here. |
| The substantive risk underneath it: if `pmcp-package` moves 0.3 -> 0.4, the four crates pinning it may each need their own `[package].version` bump — a separate set the nine-emitter model does not cover (review's sharpened form of the finding) | HIGH | **Incorporated** as its own must_have, its own prohibition, and Task 2 Step F. Phase 122 tracked this as row C1 (`cargo-pmcp` 0.22.0 -> 0.23.0) OUTSIDE the nine; CLAUDE.md item 13's ⚠ ORDERING CONSTRAINT states it normatively. |
| Plan 05 must bump `mcp-tester` and `cargo-pmcp` for `pmcp` 2.19.1 (Codex HIGH) | — **rejected** | Wrong on the mechanics: `^2.19.0` admits 2.19.1. Recorded as an explicit prohibition so an executor does not "helpfully" bump them, and as a stated non-decision at plan 03's checkpoint. |
| Task 1's `files_modified` cannot cover dynamically discovered downstream pins (Codex HIGH) | HIGH | **Incorporated.** Task 1 now performs and records a workspace-wide requirement search per bumped crate, and the resulting set is written into `124-expected-release.json`. The `files_modified` list is the measured expectation; the authorised set is the checkpoint record and the manifest artifact. |
| The version-line count assertion is false confidence — a diff count does not associate lines with manifests and misses dependency requirements (Codex HIGH) | HIGH | **Incorporated** as a prohibition plus a replacement criterion: a per-manifest-path keyed before/after table compared against the authorised `(crate, version)` map exactly. |
| The `mcp-tester` dev-dep pin is a live release hazard no gate or checkpoint covers ("Beyond both reviewers", HIGH) | HIGH | **Incorporated** as a named prohibition listing all four before-publish pin sites by file:line, matching the named decision row added at plan 03's checkpoint. |
| Test selector is imprecise — the source test is `emitted_package_requirement_matches_workspace_major_minor_line` (Codex MEDIUM) | MEDIUM | **Incorporated.** The exact test name is used and a non-zero test count is asserted. |
| "Same commit" is required but task structure does not enforce commit boundaries (Codex MEDIUM) | MEDIUM | **Incorporated.** Task 2 Step G defines the atomic commit explicitly, and a must_have requires all authorised version changes to land in one commit. |
| Introduce a committed release manifest shared by plans 03/05/06/07 (Codex cross-plan fix 3) | HIGH | **Incorporated** as new Task 4, producing `124-expected-release.json` — committed, so it survives the merge and is readable from merged `main`, which branch-local summaries are not. This is also the fix for the plan 06/07 state-continuity findings. |
</review_dispositions>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 05)

- `CHANGELOG.md`: a new `## [2.19.1] - <date>` heading and its section body.
- `.planning/phases/124-release-publish-order/124-expected-release.json` (NEW FILE): JSON keys
  `tag`, `expected_new` (array of `{crate, version, manifest_path}`), `expected_skips` (array
  of `{crate, version, reason}`), `registry_baselines` (array of
  `{crate, published_version, provenance}`), `authorised_exceptions` (array of strings),
  `generated_from` (the plan-03 checkpoint SUMMARY path), `generated_at`.
- No new Rust symbols. The constants touched (`EXPECTED_PIN` in
  `cargo-pmcp/tests/pmcp_package_pin.rs`, `EXPECTED_VERSION_LINE` in
  `crates/pmcp-openapi-server/tests/pmcp_package_pin.rs`, `PMCP_PACKAGE_VERSION_REQ` in
  `cargo-pmcp/src/templates/agent.rs`) all already exist; this plan changes their values only
  if the checkpoint authorised a `pmcp-package` bump.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump the pmcp core to 2.19.1 and every other crate the checkpoint authorised</name>
  <files>Cargo.toml, crates/pmcp-code-mode-derive/Cargo.toml, crates/pmcp-workbook-runtime/Cargo.toml, crates/pmcp-workbook-compiler/Cargo.toml</files>
  <read_first>
    - .planning/phases/124-release-publish-order/124-03-SUMMARY.md (the closed list of `(crate, target version)` pairs this plan is authorised to bump — this is the ONLY source for what moves)
    - Cargo.toml (the root manifest: the `pmcp` `version` line, and the `jsonwebtoken` requirement whose in-tree move D-03 ships)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-03, D-05, D-06)
    - CLAUDE.md, "Version Bump Rules" — a downstream crate that pins a bumped dependency must also be bumped
  </read_first>
  <action>
Bump the root `pmcp` crate from its current version to 2.19.1 (D-03). The change being shipped
is the in-tree `jsonwebtoken` requirement move that landed under an already-published 2.19.0 —
exactly the phantom-delta class this phase exists to catch. The patch axis is justified by
plan 03's public-API guard, which must have come back with zero `jsonwebtoken` occurrences; if
it did not, plan 03's checkpoint will have recorded a revised decision and this task follows
that instead.

Then bump each additional crate on plan 03's authorised list to its recorded target version,
and only those. Read that list as a closed set: a crate not on it does not move, and a crate on
it is not skipped. RESEARCH's prior measurement suggested candidates
(`pmcp-code-mode-derive`, `pmcp-workbook-runtime`, `pmcp-workbook-compiler`) but the authority
is the checkpoint record, not the research table.

For each crate bumped, apply CLAUDE.md's Version Bump Rules **as amended by plan 04's caret
exception**: a crate that pins the bumped crate by version must also be bumped **only when the
bump is semver-incompatible with the existing requirement**. A PATCH bump does not require it,
because `^X.Y.Z` already admits `X.Y.Z+1`.

The live instance is `pmcp` itself: `crates/mcp-tester/Cargo.toml:21` and
`cargo-pmcp/Cargo.toml:68` both pin `pmcp = "2.19.0"`, and `^2.19.0` admits 2.19.1, so
**neither crate is bumped on account of the `pmcp` move**. A reviewer called for bumping both;
that is wrong on the mechanics and this plan's prohibitions forbid it. Do not "helpfully"
update those pins.

Determine the affected set by searching every workspace manifest for a requirement on each
bumped crate's name — do not rely on recollection — and record, per hit, the requirement string
found and whether the bump is compatible with it. That record is what Task 4 writes into the
release manifest.

Confirm the semver axis for each bumped crate with `cargo semver-checks check-release` against
its published baseline where the tool supports the crate, and record the verdict. For a
pre-1.0 crate gaining public API, the crate-family precedent in this repo is a minor bump, not
a patch — but the axis for each crate is whatever the checkpoint recorded.

Do not touch `crates/pmcp-package`'s version in this task; that is Task 2's audit-driven
decision and it carries a nine-emitter obligation.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 --offline > /tmp/124-meta.json; /usr/bin/grep -c '"version":"2.19.1"' /tmp/124-meta.json</automated>
  </verify>
  <acceptance_criteria>
    - The root manifest's version line reads 2.19.1: `/usr/bin/grep -c '^version = "2.19.1"' Cargo.toml` returns 1
    - **Per-manifest keyed comparison, not a diff line count.** Build a before/after table keyed by MANIFEST PATH — for every `Cargo.toml` under `git ls-files '*Cargo.toml'`, the crate name, its `[package].version`, and every dependency requirement string — from `HEAD` and from the working tree. Assert the set of differing `(manifest_path, field, before, after)` rows equals EXACTLY the authorised `(crate, version)` map from plan 03's checkpoint, with no extra rows and no missing rows. The table is reproduced in the SUMMARY. (A `grep -c '^+version = '` over a unified diff cannot associate a line with its manifest and is blind to dependency requirement changes, so it can hit the expected number while the wrong crates moved.)
    - `/usr/bin/grep -c 'mcp-tester = ' crates/pmcp-server-toolkit/Cargo.toml crates/pmcp-sql-server/Cargo.toml crates/pmcp-openapi-server/Cargo.toml crates/pmcp-workbook-server/Cargo.toml` shows every one still at its pre-phase requirement, unless plan 03 authorised otherwise — recorded in the SUMMARY either way
    - The `pmcp` pins are untouched: `/usr/bin/grep -c 'pmcp = { version = "2.19.0"' crates/mcp-tester/Cargo.toml` and the equivalent for `cargo-pmcp/Cargo.toml` each still return their pre-phase count, and the SUMMARY states the caret rationale
    - `cargo semver-checks check-release --baseline-version <published pmcp version>` exits 0 for `pmcp` and the verdict is recorded
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>Exactly the authorised crates carry their authorised new versions, the semver axis of each is confirmed rather than assumed, and the workspace still builds and passes every gate.</done>
  <reversibility rating="costly">A version literal is trivially revertible in the repo, but once the tag publishes it the number is consumed on crates.io forever. It is only reversible while it is still un-tagged — which is why the deciding checkpoint sits in plan 03, upstream of this task.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Audit pmcp-package (D-04) and, if it moves, move all nine emitters in one commit</name>
  <files>crates/pmcp-package/Cargo.toml, cargo-pmcp/Cargo.toml, cargo-pmcp/src/templates/agent.rs, cargo-pmcp/tests/pmcp_package_pin.rs, crates/pmcp-agent/Cargo.toml, crates/pmcp-team-servers/Cargo.toml, crates/pmcp-cfn-renderer/Cargo.toml, crates/pmcp-openapi-server/tests/pmcp_package_pin.rs</files>
  <read_first>
    - .planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-08-SUMMARY.md (the full nine-emitter inventory with each emitter's guard — this is D-04's sweep checklist)
    - crates/pmcp-package/Cargo.toml (its own `[package].version`, and the `publish = true` / own `[workspace]` table that makes it workspace-excluded)
    - cargo-pmcp/Cargo.toml (the `pmcp-package` requirement, around :88), crates/pmcp-agent/Cargo.toml (around :18), crates/pmcp-team-servers/Cargo.toml (around :24), crates/pmcp-cfn-renderer/Cargo.toml (around :10)
    - cargo-pmcp/src/templates/agent.rs (the `PMCP_PACKAGE_VERSION_REQ` constant at :67 — the compiler-invisible ninth emitter; its drift test is its only tripwire)
    - cargo-pmcp/tests/pmcp_package_pin.rs (`EXPECTED_PIN` at :39) and crates/pmcp-openapi-server/tests/pmcp_package_pin.rs (`EXPECTED_VERSION_LINE` at :89, `EXPECTED_DEP_PATH` at :92, and `pmcp_package_dev_dep_is_path_only`)
    - CLAUDE.md item 13 (the ⚠ ORDERING CONSTRAINT and the one-set rule) and item 9b (the CR-01 path-only constraint on the openapi-server dev-dep)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-04 — the decision procedure, not a locked number)
  </read_first>
  <action>
D-04 encodes a decision *procedure*, not a version. Execute the procedure, then act on what it
returns.

Step A — audit. Diff `crates/pmcp-package` since the commit that set its current version
(`6430afae` set 0.3.0; confirm that is still the version-setting commit rather than assuming
it). Classify every added line: a change confined to doc comments, tests and fixtures means
ship the current version as-is; a source-visible additive API change means a minor bump on the
pre-1.0 axis, per this crate's own Phase-122 precedent. Note that a second commit
(`85ee222f`) also appears in the crate's path log — determine whether it actually touched
crate source rather than inferring from the path. Record the audit's evidence, not just its
verdict: which files changed, and what the added non-doc lines were.

Step B — if the audit says ship as-is, change nothing in this task beyond recording the
finding, and confirm all nine emitters are already mutually consistent.

Step C — if the audit says bump, move ALL NINE emitters in this one commit:
  1. `crates/pmcp-package/Cargo.toml`'s own `[package].version`
  2. `cargo-pmcp/Cargo.toml`'s `pmcp-package` requirement
  3. `crates/pmcp-agent/Cargo.toml`'s `pmcp-package` requirement
  4. `crates/pmcp-team-servers/Cargo.toml`'s `pmcp-package` requirement
  5. `crates/pmcp-cfn-renderer/Cargo.toml`'s `pmcp-package` requirement
  6. `PMCP_PACKAGE_VERSION_REQ` in `cargo-pmcp/src/templates/agent.rs`
  7. `EXPECTED_PIN` in `cargo-pmcp/tests/pmcp_package_pin.rs`
  8. `EXPECTED_VERSION_LINE` in `crates/pmcp-openapi-server/tests/pmcp_package_pin.rs`
  9. `crates/pmcp-openapi-server/Cargo.toml`'s `pmcp-package` dev-dependency (around :124) —
     **whose correct action is to remain UNCHANGED, path-only, no version key.** It is listed
     precisely because inaction is its right move: an emitter you must deliberately not touch
     is the one a reader drops from a checklist, and dropping it is how someone later
     "restores a pin" there and breaks the publish order. `122-08-SUMMARY.md:464-476` records
     it as row 6 for exactly this reason.

Four of these fail `cargo build` if left behind and two fail `cargo test`, but
`PMCP_PACKAGE_VERSION_REQ` is invisible to the compiler — it is emitted into projects created
by the scaffold — so a partial bump can commit with a green build and fail only in a
`cargo-pmcp` library test. Treat that emitter as the one most likely to be missed.

**Step C2 — the SEPARATE consequence set (Phase 122's row C1), which the nine do NOT cover.**
The nine emitters govern *format compatibility* — they keep every reference to
`pmcp-package`'s version mutually consistent. They say nothing about whether the crates that
pin it need new versions of their OWN. If the `pmcp-package` move is semver-INCOMPATIBLE with
the existing `^0.3` requirements, then `cargo-pmcp`, `pmcp-agent`, `pmcp-team-servers` and
`pmcp-cfn-renderer` each publish a changed artifact and each needs its own `[package].version`
bump. Phase 122 tracked precisely this as **row C1** (`cargo-pmcp` 0.22.0 -> 0.23.0) and
recorded it OUTSIDE the nine. CLAUDE.md item 13's ⚠ ORDERING CONSTRAINT states it normatively:
"`pmcp-package` and the three crates that pin it must move as one set, or not at all", and the
type-crossing inventory there — `stack_routing.rs:93` returning
`pmcp_package::package::DeployDescriptor` into `pmcp_cfn_renderer::render`, `team/dev.rs`
pairing `pmcp_package::{AgentPackage, TeamPackage}` with `pmcp_team_servers`, `agent/dev.rs:29`
pairing `pmcp_package::AgentPackage` with `pmcp_agent` — is why it is not optional. A published
`cargo-pmcp` resolving two semver-incompatible `pmcp-package` copies compiles for Cargo and
fails for the type checker.

Determine compatibility explicitly and record the determination. If the move is compatible
(for example 0.3.0 -> 0.3.1), say so and bump nothing extra. If incompatible, the four
consumer `[package].version` bumps are part of THIS commit and must also appear on plan 03's
authorised list — if they do not, stop and return to the user rather than consuming four
unauthorised version numbers.

Step D — regardless of the verdict, verify the CR-01 constraint is intact:
`crates/pmcp-openapi-server`'s `pmcp-package` dev-dependency must carry a `path` and NO
`version` key, because that crate publishes several steps before `pmcp-package` and Cargo
retains a dev-dep in the published manifest whenever it carries a version requirement. Do not
"restore a pin" there while editing pins elsewhere.

Step E — verify the crate's no-crypto boundary is unaffected by any manifest edit.

**Step F — do not touch `mcp-tester` requirements.** While editing pins across the workspace it
is easy to sweep `mcp-tester = { version = "0.8.0", path = ... }` along with the rest. Four of
its six pin sites publish BEFORE `mcp-tester` itself and carry retained version requirements:
`crates/pmcp-server-toolkit/Cargo.toml:192` (:263), `crates/pmcp-sql-server/Cargo.toml:57`
(:329), `crates/pmcp-openapi-server/Cargo.toml:63` (:344),
`crates/pmcp-workbook-server/Cargo.toml:58` (:383), against `mcp-tester` at `release.yml:401`.
Moving any of them to a version not yet on crates.io kills the release job at the first. Leave
them unless plan 03's checkpoint authorised the move WITH a stated ordering resolution;
confirm which case applies and record it.

**Step G — the atomic commit.** Everything from Task 1 and Task 2 — every authorised
`[package].version`, every emitter, every consequence bump — lands in ONE commit. Do not split
by task or by crate. The whole point of the one-set rule is that no intermediate state exists
in which the set is half-moved; a two-commit sequence creates exactly that state on `main` and
inside the PR's history. Stage everything, run `RUSTFLAGS="" make quality-gate`, then commit
once with a message naming the complete `(crate, old -> new)` list.

Record in the SUMMARY: the audit verdict with its evidence, the nine-row emitter table with
each one's before/after value and the guard that covers it (row 9 showing UNCHANGED), the
compatibility determination and any row-C1 consequence bumps, the CR-01 confirmation, the
`mcp-tester` confirmation, and the single commit SHA.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test pmcp_package_pin</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test pmcp_package_pin` exits 0
    - **The exact library test name is used**, not a shortened filter: `cargo test -p cargo-pmcp --lib emitted_package_requirement_matches_workspace_major_minor_line` exits 0 AND its output reports exactly 1 test run. Parse the `N passed` figure and assert it is non-zero — a filter selecting zero tests passes vacuously, and this project's memory records that whole class of false green.
    - `make test-openapi-server` exits 0 (this is the harness that runs `pmcp_package_dev_dep_is_path_only`)
    - `/usr/bin/grep -n 'pmcp-package' crates/pmcp-openapi-server/Cargo.toml > /tmp/124-cr01.txt` then `/usr/bin/grep -c 'version' /tmp/124-cr01.txt` returns 0 (the dev-dep is path-only)
    - The SUMMARY contains a NINE-row emitter table with before/after values and, for each row, the guard that would catch it being left behind. Row 9 is `crates/pmcp-openapi-server/Cargo.toml` with before == after and the annotation UNCHANGED / path-only / CR-01. The table has nine rows whether or not the audit says bump.
    - The SUMMARY records the semver-compatibility determination for any `pmcp-package` move, and — if incompatible — the four consumer `[package].version` consequence bumps, each cross-checked against plan 03's authorised list
    - The `mcp-tester` requirements are unchanged at all four before-publish sites, or the SUMMARY records the checkpoint authorisation and the ordering resolution
    - **One commit.** `git log --oneline HEAD~1..HEAD` shows a single commit containing every version change from Tasks 1 and 2, and `git show --stat HEAD` lists every manifest and constant file the SUMMARY's tables name. No intermediate commit exists in which the emitter set is half-moved.
    - If the audit verdict is "ship as-is": `git diff --unified=0 -- crates/pmcp-package/Cargo.toml > /tmp/124-pkg-diff.txt` then `/usr/bin/grep -c '^+version = ' /tmp/124-pkg-diff.txt` returns 0
    - If the audit verdict is "bump": emitters 1-8 show the new value and emitter 9 is unchanged, verified one by one and listed in the SUMMARY
    - `make no-crypto-check` exits 0
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>`pmcp-package`'s ship version is decided by measurement rather than by assumption, and if it moved, every emitter of it moved in the same commit — including the one the compiler cannot see.</done>
  <reversibility rating="costly">Same as Task 1: revertible until the tag publishes, permanent afterwards. The nine-emitter obligation is what makes a partial change dangerous rather than merely wrong.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Write the CHANGELOG section and prove the release workflow can extract it</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-40 (the file header and the current head section `## [2.19.0] - 2026-08-20` — the heading form is the contract)
    - .github/workflows/release.yml lines 20-45 (the `Read changelog` step: the awk that matches `## \[<version-without-v>\]`, and the fact that an unmatched version yields an empty string with exit 0)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 7 — why an absent section fails silently, and the requirement to re-run that exact awk as the verification)
    - .planning/phases/124-release-publish-order/124-03-SUMMARY.md and 124-05 Tasks 1-2 findings (what actually ships, so the section describes the real release)
  </read_first>
  <action>
Add a `## [2.19.1] - <today's date in YYYY-MM-DD>` section at the head of `CHANGELOG.md`,
immediately above the existing `## [2.19.0]` section. The heading form is load-bearing, not
stylistic: `release.yml`'s `Read changelog` step awk-matches `## \[<version-without-v>\]` and,
on no match, yields an empty string and exits 0 — so the GitHub Release is created with empty
notes and nobody notices until someone reads the release page.

Content: describe what this release actually ships, sourced from this phase's own measurements
rather than composed from imagination.
  - The `pmcp` core patch: the `jsonwebtoken` requirement move that had landed under an
    already-published 2.19.0 and would otherwise have shipped silently never. State that no
    `jsonwebtoken` type crosses the public API, which is why it is a patch.
  - Every other crate bumped in Tasks 1 and 2, one line each, naming what changed.
  - The crates already ahead of the registry that ride this tag as-is (phases 120–123's work:
    config-server packaging, the local round-trip, attestation carriage, the save/load/pull
    verbs) — a reader of the release page should be able to see the whole surface.
  - The release-hygiene work itself: the coverage gate now covers workspace-excluded
    publishable crates, the publish order for the `pmcp-package` cluster is machine-checked,
    and `make release-sweep` reports version drift against the registry.
Follow the existing sections' structure and heading conventions.

Then prove the extraction works by running `release.yml`'s own awk — the same program, not a
paraphrase of it — against `CHANGELOG.md` for the version being tagged, and asserting the
output is non-empty. Save the extracted text to a file and record its first lines in the
SUMMARY, so the release notes are seen before the tag is pushed rather than after.

Confirm the version in the heading equals the tag chosen in D-06 (`v2.19.1`) with the leading
`v` removed. A mismatch here is the silent-empty-notes failure.
  </action>
  <verify>
    <automated>awk -v ver="## \\[2.19.1\\]" '$0 ~ ver {p=1; next} /^## \[/ && p {exit} p {print}' CHANGELOG.md > /tmp/124-notes.txt; test -s /tmp/124-notes.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c '^## \[2.19.1\] - ' CHANGELOG.md` returns 1
    - The extraction file `/tmp/124-notes.txt` is non-empty and its first 20 lines are reproduced in the SUMMARY
    - `/usr/bin/grep -c '^## \[2.19.0\]' CHANGELOG.md` returns 1 (the previous section is intact, not overwritten)
    - The new section appears BEFORE the 2.19.0 section: `/usr/bin/grep -n '^## \[' CHANGELOG.md > /tmp/124-headings.txt` and the 2.19.1 line number is smaller than the 2.19.0 line number
    - Every crate bumped in Tasks 1 and 2 is named in the new section — checked crate by crate against the SUMMARY's bump list
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>The release notes exist in the form the workflow can read, proven by running the workflow's own extraction rather than by inspection, and they describe the release that is actually shipping.</done>
  <reversibility rating="reversible">A CHANGELOG section is editable until the tag is pushed; after that it is baked into a GitHub Release, which is itself editable.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Emit and COMMIT the release manifest that plans 06 and 07 verify against</name>
  <files>.planning/phases/124-release-publish-order/124-expected-release.json</files>
  <read_first>
    - .planning/phases/124-release-publish-order/124-03-SUMMARY.md (the authorised `(crate, target version)` list, the per-crate registry baselines with their provenance, and the mcp-tester decision)
    - The Task 1 per-manifest before/after table and the Task 2 emitter table (this plan's own working notes) — the manifest is generated from measurements, not retyped
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (the registry table's ageing warning — the baselines recorded here are a snapshot with a timestamp, not a standing truth)
  </read_first>
  <action>
Write `.planning/phases/124-release-publish-order/124-expected-release.json` and commit it as
part of this phase's work, so it travels into the release PR and survives the merge onto
`main`.

**Why this file exists.** Plans 06 and 07 currently depend on plan summaries that are created
AFTER the events they describe — `124-06-SUMMARY.md` is written after the user merges, so it
cannot be inside the merged PR, and plan 07 then checks out merged `main` and expects to read
it. That is a broken handoff: the state plan 07 needs must be committed BEFORE the merge, not
after. A committed manifest is the fix, and it is a better oracle besides, because it is one
artifact both later plans read rather than two summaries they read differently.

Contents — all of it derived from measurements already taken in this wave, none of it retyped
from memory:

  - `tag`: `v2.19.1` (D-06), matching the CHANGELOG heading written in Task 3 with the leading
    `v` removed.
  - `expected_new`: every crate this release intends to publish for the first time at this
    version — `{crate, version, manifest_path}` per entry. This is exactly the authorised set
    from plan 03's checkpoint plus any row-C1 consequence bumps from Task 2.
  - `expected_skips`: **the set the pre-tag check would otherwise get wrong.** Crates whose
    in-tree version is ALREADY published and which `release.yml` will therefore skip
    gracefully — with a `reason` per entry. This set exists because plan 07's pre-tag criterion
    demanded that EVERY intended crate's in-tree version differ from its published version,
    which conflicts with the workflow's deliberate already-published skip. Two sets, verified
    against their own expectations, is the correct model.
  - `registry_baselines`: per crate, the published version the plan-03 sweep measured and the
    baseline provenance it carried (including whether it was corroborated), plus the timestamp
    of the sweep. Plan 07 re-measures rather than trusting these — they are the record of what
    the decision was made against, so a divergence at pre-tag time is detectable as a
    divergence rather than invisible.
  - `authorised_exceptions`: the decisions that would otherwise look like defects to a later
    reader — at minimum the `mcp-tester` disposition from plan 03's checkpoint, the caret
    non-bump of `crates/mcp-tester` and `cargo-pmcp` against `pmcp` 2.19.1, and any crate the
    user chose to leave un-bumped despite a source-visible delta.
  - `generated_from` and `generated_at`.

The two crate sets must be **disjoint** and their union must account for every crate the sweep
enumerated; a crate in neither set is a crate nobody decided about, which is the failure this
whole phase exists to make visible.

Validate the file parses as JSON and commit it. It is a planning artifact, not a build input —
nothing in the build reads it — so it needs no Makefile wiring.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('.planning/phases/124-release-publish-order/124-expected-release.json')); n={c['crate'] for c in d['expected_new']}; s={c['crate'] for c in d['expected_skips']}; assert d['tag']=='v2.19.1', d['tag']; assert not (n & s), n & s; assert n, 'expected_new is empty'; print(len(n), len(s))"</automated>
  </verify>
  <acceptance_criteria>
    - The file exists, parses as JSON, and its `tag` equals `v2.19.1` — the same string as the CHANGELOG heading version with a leading `v`
    - `expected_new` is NON-EMPTY, and every entry matches a `(crate, target version)` pair on plan 03's authorised list plus Task 2's row-C1 consequence bumps — checked entry by entry and recorded in the SUMMARY
    - `expected_new` and `expected_skips` are disjoint (asserted by the verify command), and their union covers every crate the plan-03 sweep enumerated — any crate in neither set is named in the SUMMARY as an explicit gap and halts the task
    - Every `expected_skips` entry carries a non-empty `reason`
    - `authorised_exceptions` names at minimum the `mcp-tester` disposition and the caret non-bump
    - The file is COMMITTED (`git log --oneline -1 -- <path>` returns a commit), so it will be part of the release PR and readable from merged `main`
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>A single committed artifact enumerates what this release will publish, what it will deliberately skip, and which apparent anomalies were authorised — readable from merged `main` by plans 06 and 07 without depending on summaries that do not yet exist.</done>
  <reversibility rating="reversible">A planning JSON file; nothing reads it at build or publish time. Editable until the tag is pushed.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-repo version literals -> published crates.io artifacts | Whatever is in these manifests at tag time becomes permanent registry state. |
| CHANGELOG heading -> GitHub Release notes | An unmatched heading is an empty-notes release created with exit 0 — a silent failure at a boundary nobody watches. |
| pmcp-package version -> four consuming crates | A partial emitter move ships a CLI pinned to a package version it cannot read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-03 | Tampering (semantic) | `pmcp-package` version emitter set | high | mitigate | Nine-emitter one-set rule executed in a single commit; the two pin tripwires and the scaffold drift test are asserted to pass, and the compiler-invisible emitter is called out as the one most likely to be missed (Task 2) |
| T-124-04 | Elevation of Privilege | permanent version-number consumption | medium | mitigate | This plan is authorised only for the closed list recorded at plan 03's checkpoint; a diff assertion counts the changed version lines against that list (Task 1) |
| T-124-08 | Denial | CHANGELOG heading vs release.yml awk | medium | mitigate | The workflow's own awk is re-run against the file and the output asserted non-empty and reviewed before the tag exists (Task 3) |
| T-124-15 | Tampering | `crates/pmcp-openapi-server` dev-dep on `pmcp-package` | high | mitigate | CR-01 path-only constraint re-verified while pins are being edited elsewhere; `pmcp_package_dev_dep_is_path_only` asserted green (Task 2 Step D) |
| T-124-16 | Tampering | `crates/pmcp-package` dependency surface | medium | mitigate | `make no-crypto-check` asserted green after any manifest edit to that crate; the plan's prohibitions forbid adding a crypto or signing dependency (Task 2 Step E) |
| T-124-25 | Tampering (semantic) | a consumer published unbumped against a semver-incompatible `pmcp-package` | high | mitigate | The row-C1 consequence set is treated as separate from the nine emitters, its compatibility determined explicitly, and any consequence bump cross-checked against the authorised list before it is consumed (Task 2 Step C2). CLAUDE.md item 13's type-crossing inventory is why a published `cargo-pmcp` resolving two `pmcp-package` copies compiles for Cargo and fails for the type checker. |
| T-124-24 | Denial (of the release) | `mcp-tester` requirements swept along with unrelated pin edits | high | mitigate | Named prohibition listing all four before-publish pin sites by file:line; Task 2 Step F requires an explicit confirmation either way; matching named decision at plan 03's checkpoint |
| T-124-26 | Repudiation | release state carried only in post-hoc plan summaries | high | mitigate | `124-expected-release.json` is generated from measurements and COMMITTED before the PR, so plans 06 and 07 verify against an artifact that survives the merge rather than against summaries written after the events they describe (Task 4) |
</threat_model>

<verification>
- Root `pmcp` version is 2.19.1 and no unauthorised crate version moved, proven by a per-manifest keyed table rather than a diff line count
- Every emitter of a bumped `pmcp-package` version moved in ONE commit, tripwires green, with the ninth (path-only) row asserted UNCHANGED
- Row-C1 consequence bumps determined explicitly and authorised, or explicitly determined unnecessary
- `mcp-tester` requirements untouched at the four before-publish sites, or authorised with an ordering resolution
- `crates/mcp-tester` and `cargo-pmcp` NOT bumped for the `pmcp` patch move (caret compatibility)
- The openapi-server dev-dep is still path-only
- `## [2.19.1]` extracts non-empty via the workflow's own awk
- `124-expected-release.json` is committed, parses, and its two crate sets are disjoint and complete
- `RUSTFLAGS="" make quality-gate` exits 0
</verification>

<success_criteria>
- SC3 is met: `pmcp-package`'s ship version, `cargo-pmcp`'s caret pin and the pin tripwire move in the same change, so the CLI cannot ship pinned to a package version it cannot read
- The release notes are non-empty and describe the release that is actually shipping
- The version numbers about to be consumed are exactly the ones the user authorised
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-05-SUMMARY.md` when done
</output>
