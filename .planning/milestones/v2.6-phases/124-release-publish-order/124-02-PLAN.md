---
phase: 124-release-publish-order
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - Makefile
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs
  - crates/pmcp-package/tests/common/mod.rs
  - docs/design/package-portability-pmcp-run-handoff.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [PKGR-01]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "After the sync, `git merge-base --is-ancestor <the recorded MAIN_SHA> HEAD` succeeds — the release PR will be diffed against the current main, not against the pre-squash merge base."
    - "The `upstream/main` commit measured in Task 1 and the `upstream/main` commit merged in Task 2 are the SAME SHA, recorded verbatim in the SUMMARY — `upstream/main` can advance between the two tasks, and a merge of a different tip than the one whose conflicts were classified is an unmeasured merge."
    - "After the sync, `RUSTFLAGS=\"\" make quality-gate` exits 0 — including the coverage gate and the guard self-test created in plan 01, both of which live in a file the merge conflicts on."
    - "After the sync, `make check-release-coverage-guard-selftest` still exits 0 and still reports 8 fixtures — the conflict resolution did not drop plan 01's Makefile target."
    - "No version literal changed as part of the conflict resolution — neither a `[package].version` line NOR a dependency version requirement (`name = { version = \"...\" }`), both of which are release-critical and only the first of which a `^[+-]version = ` grep can see."
  artifacts:
    - "A merge commit (or equivalent) on feat/v2.6-package-portability carrying the recorded upstream/main tip"
    - "A recorded `git merge-tree` conflict transcript in the plan SUMMARY, re-measured at execution time, stamped with the MAIN_SHA it was measured against"
    - "A before/after manifest version inventory (crate name -> package version -> every dependency requirement), diffed rather than greped"
  key_links:
    - "Makefile is BOTH a measured conflict path and the file plan 01 just edited — the resolution must keep check-release-coverage-guard-selftest and its prerequisite edge"
    - "the two cargo-pmcp/src/deployment/targets/pmcp_run/graphql*.rs conflicts carry real behaviour from phase 123; they are not documentation"
    - "this sync must land before plan 05's version bumps (RESEARCH Pitfall 1): a conflict resolution that silently reverts a bumped manifest is the worst possible ordering"
    - "MAIN_SHA pins Task 1's measurement to Task 2's merge; without it the classified conflict set and the resolved conflict set can be different sets"
  prohibitions:
    - "Must not resolve a conflict by discarding one side wholesale without reading both sides — every hunk gets an explicit resolution decision recorded in the SUMMARY."
    - "Must not merge `upstream/main` by name. Merge the exact SHA recorded in Task 1, or re-run Task 1's measurement first — a symbolic ref can move between measurement and merge."
    - "Must not verify the no-version-moved property with a `^[+-]version = ` grep alone. That pattern is blind to dependency requirement lines, which are equally release-critical."
    - "Must not hard-code an environment-specific binary path (for example `/opt/homebrew/bin/git`). Resolve each evidence-producing binary ONCE via `command -v`, record the resolved absolute path in the SUMMARY, and use that variable — the repository does not require Homebrew git, and a hard-coded path silently fails on any other machine."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land the merge."
    - "Must not report the sync as complete on the strength of a clean `git status`; the post-merge full quality gate is the evidence."
---

<objective>
Sync `main` into the milestone branch (D-08) before anything in this phase touches a version
number, resolving the conflict set that the squash-merge of PR #347 created.

Purpose: `main`'s tip is a **squash** of phases 120–122 taken from this very branch, so the
merge base is the pre-squash commit and the same content arrives from two directions. Left
un-synced, GitHub computes the release PR's diff from that old base and re-presents all of
phases 120–123 as if unmerged. Done AFTER the version bumps, a conflict resolution can
silently revert a bumped manifest — the worst possible ordering.
Output: a synced branch whose full local quality gate is green, with the conflict resolution
recorded hunk by hunk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-01-SUMMARY.md
</context>

<review_dispositions>
## Cross-AI review findings dispositioned in this plan

Source: `124-REVIEWS.md`.

| Finding | Severity | Disposition |
|---|---|---|
| Merge-result provenance not pinned — `upstream/main` may advance between Task 1's `merge-tree` measurement and Task 2's merge (Codex MEDIUM) | MEDIUM | **Incorporated.** Task 1 records `MAIN_SHA`; Task 2 merges that exact SHA; a truth and a prohibition enforce it, and an acceptance criterion re-derives the SHA and halts on drift. |
| "No version literal changed" checked too narrowly — `^[+-]version = ` misses dependency requirements (Codex MEDIUM) | MEDIUM | **Incorporated.** Task 2 now builds a semantic before/after manifest inventory covering both `[package].version` and every dependency requirement, and the grep is demoted to a secondary check. |
| Hard-coded `/opt/homebrew/bin/git` is environment-specific (Codex MEDIUM) | MEDIUM | **Incorporated as a prohibition plus a Task 1 resolution step.** The underlying reason for absolute paths is real and preserved (RESEARCH Pitfall 4: the shell proxy has been measured corrupting `git diff` output), so the fix is to resolve once via `command -v` and record the path — not to drop absolute paths. |
| Declared `files_modified` becomes inaccurate as soon as `main` moves (Codex LOW) | LOW | **Accepted as inherent, mitigated by the existing halt.** Task 1 already halts when the conflict set leaves the three classified buckets. The `files_modified` list is the measured expectation, not a contract; the SUMMARY records the actual set. Noted here rather than changed, because pinning `files_modified` to a moving target is not achievable at plan time. |
| Preserve plan 01/02 artifacts before branch topology changes (Codex suggestion) | LOW | **Rejected as inapplicable.** Plan 01's outputs are committed source files, not branch-local scratch; a merge cannot lose them, and Task 2's acceptance criteria already assert the Makefile target survived. The genuine state-continuity problem is downstream (plans 06/07 summaries) and is fixed there via the committed `124-expected-release.json`. |
</review_dispositions>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 02)

This plan creates no new code symbols. It produces a git merge commit and a recorded
conflict-resolution transcript. Every file it touches already exists; the only additions it
must PRESERVE are plan 01's `check-release-coverage-guard-selftest` target and the
`check-release-coverage:` prerequisite edge in `Makefile`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the conflict set against the current main</name>
  <files>(no file writes — measurement only; output recorded in the SUMMARY)</files>
  <precondition>`git fetch upstream main` succeeds and `upstream` resolves to git@github.com:paiml/rust-mcp-sdk.git — the release PR targets paiml/rust-mcp-sdk, not the personal fork at `origin`. Halt and report if the remote is missing or the fetch fails.</precondition>
  <read_first>
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 1 — the measured 9-file conflict set, and the `git cat-file -p` evidence that main's tip is a squash with a single parent)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-08 — the PR vehicle is this branch as-is, `.planning/` commits included, no clean-branch cherry-pick of 239 commits)
  </read_first>
  <action>
RESEARCH's conflict measurement is explicitly stamped "valid ~3 days" because it was taken
against a moving `main`. Re-measure rather than trusting the recorded numbers.

**Step 0 — resolve the evidence binaries once.** Absolute paths are required because the shell
proxy has been measured corrupting `git diff` and inverse-grep output on this repo (RESEARCH
Pitfall 4) — but they must be RESOLVED, not assumed. Run `command -v git` and `command -v grep`,
record both resolved absolute paths in the SUMMARY, and use those values for every
evidence-producing command in both tasks. Do not hard-code `/opt/homebrew/bin/git`: this
repository does not require Homebrew git, and a hard-coded path that does not exist fails
differently on a different machine. If either binary does not resolve, halt.

**Step 1 — pin the target.** Fetch `upstream`, then record
`MAIN_SHA=$($GIT rev-parse upstream/main)` verbatim in the SUMMARY. Every subsequent
measurement in this task, and the merge itself in Task 2, refer to that SHA and not to the
symbolic ref. `upstream/main` is a moving target; measuring conflicts against one tip and then
merging another is an unmeasured merge, and the whole point of this task is that the merge be
measured first.

Then establish, against `$MAIN_SHA`:

  - the ahead/behind counts between `HEAD` and `$MAIN_SHA` (the ROADMAP's recorded "249
    commits divergent" is stale — it was 245 ahead / 1 behind at review time; re-measure and
    record, the 1-behind commit being what this sync brings in);
  - whether that tip is a squash — `git cat-file -p` on it, checking for a single `parent` line;
  - the merge base;
  - the conflict set, non-destructively, via `$GIT merge-tree --write-tree --messages HEAD
    $MAIN_SHA`. This requires git >= 2.38; the local git is 2.47.1.

Classify each conflicted path into one of three buckets and record the classification:
  - **behavioural** — production source whose resolution changes what the code does. The
    previously measured members were `Makefile`,
    `cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs` and
    `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs`.
  - **add/add** — the same new file created independently on both sides. Previously
    `crates/pmcp-package/tests/common/mod.rs` and
    `docs/design/package-portability-pmcp-run-handoff.md`.
  - **planning documents** — `.planning/*.md`. Resolvable by keeping this branch's content,
    which is strictly newer, but each still needs a read.

Do not perform the merge in this task. Its whole job is to produce the map the next task
resolves against, and to catch the case where `main` has moved far enough that the plan's
assumptions no longer hold. If the conflict set now includes a path outside the three buckets
above — in particular any `Cargo.toml`, `CHANGELOG.md`, `.github/workflows/release.yml`, or
`scripts/check-release-coverage.sh` — stop and report rather than proceeding: those are files
later plans in this phase own, and a conflict there changes the phase's ordering.
  </action>
  <verify>
    <automated>GIT=$(command -v git); $GIT rev-parse upstream/main > /tmp/124-main-sha.txt; $GIT merge-tree --write-tree --messages HEAD "$(cat /tmp/124-main-sha.txt)" > /tmp/124-merge-tree.txt; grep -c 'CONFLICT' /tmp/124-merge-tree.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/124-main-sha.txt` exists, holds a full 40-character SHA, and that SHA is recorded verbatim in the SUMMARY as `MAIN_SHA`
    - `/tmp/124-merge-tree.txt` exists and the conflict count is recorded in the SUMMARY as a number, alongside the full list of conflicted paths, stamped with the `MAIN_SHA` it was measured against
    - The resolved absolute paths of `git` and `grep` (from `command -v`) are recorded in the SUMMARY, and no acceptance command in this plan contains a hard-coded `/opt/homebrew/` path
    - The SUMMARY records the ahead/behind counts and states explicitly whether the `MAIN_SHA` commit has one parent (squash) or two (merge)
    - Every conflicted path is classified as behavioural, add/add, or planning document
    - `grep -c 'Cargo.toml' /tmp/124-merge-tree.txt` returns 0; if it does not, the task halts and reports instead of continuing
    - `grep -c 'check-release-coverage' /tmp/124-merge-tree.txt` returns 0; if it does not, the task halts and reports
  </acceptance_criteria>
  <done>The current conflict set is measured non-destructively, classified, and checked against the paths later plans own — with an explicit halt if the shape has changed.</done>
</task>

<task type="auto">
  <name>Task 2: Perform the sync, resolve every conflict, prove the tree with the full gate</name>
  <files>Makefile, cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs, cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs, crates/pmcp-package/tests/common/mod.rs, docs/design/package-portability-pmcp-run-handoff.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/WINDOWS.md</files>
  <read_first>
    - /tmp/124-merge-tree.txt (Task 1's measured conflict set — resolve against the current measurement, not against RESEARCH's recorded one)
    - Makefile (the current file, including plan 01's check-release-coverage-guard-selftest target and the `check-release-coverage:` prerequisite edge — both must survive)
    - cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs and graphql_contract.rs (both sides of each conflicted hunk; these carry phase 123's save/load/pull seam work)
    - crates/pmcp-package/tests/common/mod.rs (the add/add conflict — read both versions before choosing)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 1's "how to avoid" guidance and Pitfall 4's absolute-binary-path rule)
  </read_first>
  <action>
**Re-confirm the pin before merging.** Re-run `$GIT rev-parse upstream/main` and compare it to
the `MAIN_SHA` Task 1 recorded. If it has moved, `upstream/main` advanced between measurement
and merge and the classified conflict set is stale — re-run Task 1's measurement against the
new tip and re-classify before proceeding. Then merge **that exact SHA**, not the symbolic
ref, so the merge that lands is the merge that was measured.

Merge the recorded `MAIN_SHA` into `feat/v2.6-package-portability` and resolve every conflict
explicitly. This is the branch that becomes the release PR (D-08): the vehicle is this branch
as-is with `.planning/` commits included, following the v2.17 / PR #302 precedent — there is
no clean-branch cherry-pick of the ~240 commits.

Resolution rules by bucket:

  - **Planning documents** (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`,
    `.planning/STATE.md`, `.planning/WINDOWS.md`): this branch's content is strictly newer —
    `main` carries the squashed 120–122 snapshot, this branch carries 120–123 plus this
    phase. Keep this branch's version, but read each conflicted hunk first and confirm the
    incoming side introduces nothing this branch lacks. Record any hunk where the incoming
    side had unique content.
  - **add/add** (`crates/pmcp-package/tests/common/mod.rs`,
    `docs/design/package-portability-pmcp-run-handoff.md`): read both versions in full.
    These are the same file authored twice — once here, once in the squash. Keep the version
    that is a superset; if neither is, merge the content by hand and say so.
  - **Behavioural source** (`Makefile`, the two `pmcp_run/graphql*.rs` files): resolve hunk by
    hunk with both sides read. For `Makefile`, the resolution MUST retain plan 01's
    `check-release-coverage-guard-selftest` target, its fixture table comment, and the
    `check-release-coverage: check-release-coverage-guard-selftest` prerequisite line — a
    "take theirs" on this file silently deletes the gate this phase exists to build. For the
    two graphql files, the incoming side is the squashed 120–122 content and this branch's
    side additionally carries phase 123's work; keep phase 123's behaviour and fold in any
    incoming change that is genuinely new.

Do not touch any version literal during the resolution — neither a `[package].version` line
nor a dependency version requirement. If the merge presents one, stop: that means the conflict
set changed shape since Task 1 and the phase ordering needs revisiting.

**Prove that semantically, not with a single grep.** Before the merge, capture a manifest
version inventory — for every `Cargo.toml` reachable from `$GIT ls-files '*Cargo.toml'`, the
crate name, its `[package].version`, and every dependency requirement string it declares —
into a sorted file. Capture the same inventory after the merge and `diff` the two; the diff
must be empty. A `grep '^[+-]version = '` over the merge diff is kept as a secondary check but
is NOT sufficient on its own: it matches only top-level `version = ` lines and is blind to
`pmcp = { version = "..." }` dependency requirements, which are equally release-critical and
which a careless resolution of a manifest hunk can move.

Use the absolute binary paths resolved in Task 1 Step 0 (`$GIT`, `$GREP`) for any command
whose output is load-bearing evidence — the shell proxy has been measured corrupting
`git diff` and inverse greps. Do not substitute a hard-coded path.

After committing the merge, run the full local gate with `RUSTFLAGS=""` (RUSTFLAGS is
exported in CI and absent locally, which is recorded local-gate blind spot #9), and run
`rustup update stable` first per the project Pre-Flight Checklist so the local toolchain
matches CI's `dtolnay/rust-toolchain@stable`.

Record in the SUMMARY: one line per conflicted file stating which side won and why, plus the
post-merge gate transcript.
  </action>
  <verify>
    <automated>RUSTFLAGS="" make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTFLAGS="" make quality-gate` exits 0
    - `make check-release-coverage-guard-selftest` exits 0 and reports 8 fixtures (plan 01's target survived the Makefile resolution)
    - `$GREP -c 'check-release-coverage: check-release-coverage-guard-selftest' Makefile` returns 1
    - The merged tip is the measured one: `$GIT rev-parse upstream/main` equals the `MAIN_SHA` in `/tmp/124-main-sha.txt`, OR the SUMMARY records that Task 1's measurement was re-run against a new tip and re-classified before merging
    - `$GIT merge-base --is-ancestor "$(cat /tmp/124-main-sha.txt)" HEAD` exits 0
    - `$GIT status --porcelain` produces no output (no unresolved paths, no stray files)
    - **Semantic no-version-moved proof:** the pre-merge and post-merge manifest version inventories are byte-identical — `diff /tmp/124-manifest-inventory-before.txt /tmp/124-manifest-inventory-after.txt` exits 0. Both files are non-empty and their line counts are recorded in the SUMMARY (an empty inventory would make this pass vacuously).
    - Secondary check, kept but not relied upon: `$GIT diff "$(cat /tmp/124-main-sha.txt)"...HEAD -- '*Cargo.toml' > /tmp/124-manifest-diff.txt` then `$GREP -c '^[+-]version = ' /tmp/124-manifest-diff.txt` returns 0
    - `$GREP -c '<<<<<<<' Makefile` returns 0, and the same check returns 0 for both `pmcp_run/graphql*.rs` files
    - The SUMMARY carries one resolution line per conflicted file naming the winning side and the reason
  </acceptance_criteria>
  <done>The milestone branch contains `upstream/main`'s tip, every conflict is resolved with a recorded rationale, no version literal moved, and the full local quality gate is green.</done>
  <reversibility rating="costly">A squash-merge of this branch means future branches must be cut from `upstream/main` or the v2.4 squash-divergence trap repeats. The mitigation is procedural, not structural — record it in the SUMMARY so the next branch author inherits it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream/main -> working tree | Content arriving from a squash commit of this branch's own work; identical-looking hunks from two directions is exactly the shape that makes a careless resolution silently drop work. |
| conflict resolution -> release payload | Whatever survives this merge is what the tag publishes. A dropped hunk here becomes a shipped defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-05 | Tampering | `main` sync conflict resolution | high | mitigate | Non-destructive `git merge-tree` re-measure first (Task 1); per-hunk resolution with both sides read; explicit acceptance criteria that plan 01's Makefile target and prerequisite edge survive; full `RUSTFLAGS="" make quality-gate` after (Task 2) |
| T-124-10 | Tampering | version literals in manifests | high | mitigate | The sync is sequenced BEFORE plan 05's bumps, and a semantic before/after manifest version inventory — covering `[package].version` AND every dependency requirement — proves nothing moved during the resolution. The narrower `^[+-]version = ` grep is retained as a secondary check only, because it is blind to dependency requirement lines (Task 2 acceptance criteria) |
| T-124-11 | Repudiation | the sync's own evidence | medium | mitigate | Binary paths resolved once via `command -v` and recorded, so evidence commands are reproducible on any machine rather than only the measured one; the conflict transcript is written to a file and counted, not eyeballed |
| T-124-21 | Tampering | merging a different `upstream/main` tip than the one whose conflicts were classified | medium | mitigate | `MAIN_SHA` recorded in Task 1 and re-confirmed immediately before the merge in Task 2; the merge targets the SHA, not the symbolic ref; drift forces re-measurement rather than proceeding (Task 2 action, acceptance criteria) |
</threat_model>

<verification>
- Conflict set re-measured non-destructively and classified before any merge is attempted, against a recorded `MAIN_SHA`
- The merged commit is that same `MAIN_SHA`, or the measurement was re-run
- Every conflicted path resolved with a recorded rationale
- The recorded `MAIN_SHA` is an ancestor of `HEAD`
- No version literal changed — proven by an empty before/after manifest inventory diff, not by a single grep
- `RUSTFLAGS="" make quality-gate` green, and plan 01's gate + 8-fixture self-test intact
</verification>

<success_criteria>
- The release PR opened in plan 06 will diff against the current `upstream/main`, showing phase 123 + phase 124 rather than all of 120–124
- No version bump in this phase can be silently reverted by a later conflict resolution, because the resolution has already happened
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-02-SUMMARY.md` when done
</output>
