---
phase: 124-release-publish-order
plan: 07
type: execute
wave: 7
depends_on: ["124-06"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/124-release-publish-order/124-07-SUMMARY.md
autonomous: false
requirements: [PKGR-01]

estimate:
  tokens: 32000
  raw_tokens: 64000
  tasks: 5
  confidence: high

must_haves:
  truths:
    - "Immediately before the tag is pushed, the crates.io API is re-queried and every pair in `124-expected-release.json`'s `expected_new` set is confirmed NOT already present — a version already taken cannot be published and would silently skip."
    - "Crates in the `expected_skips` set are verified against their OWN expectation — already published, therefore correctly skipped — rather than being required to differ from their published version. Requiring every intended crate to differ conflicts with `release.yml`'s deliberate already-published skip and would block a legitimate release."
    - "The tag `v2.19.1` is pushed by the user, not by an agent, to `upstream`."
    - "The pushed tag is resolved explicitly as an annotated tag object AND its peeled commit, rather than by counting `git ls-remote` output lines — an annotated tag can surface both a tag ref and a `^{}` peeled ref, so a line count is not a reliable existence test."
    - "After `release.yml` completes, every expected `(crate, version)` pair is confirmed present in the crates.io API response for that crate — per crate, against the registry, not against the workflow run's overall status."
    - "A post-publish registry miss is retried with bounded backoff before being classified as a publication failure, because crates.io indexing is eventually consistent; and a malformed or unparseable response is classified as a FAILED PROBE distinct from a miss, because the two need different responses."
    - "The known-broken MCP-registry OIDC job's ACTUAL conclusion is recorded from the run rather than presupposed, and whatever it is, it does NOT block this phase's close — the phase's verdict is derived from the per-crate registry check alone."
    - "`.planning/ROADMAP.md`'s progress table, `.planning/REQUIREMENTS.md`'s PKGR-01 entry and `.planning/STATE.md` reflect the completed phase, AND those edits reach `upstream/main` through a committed, pushed, merged closeout pull request — not as a dirty local tree."
  artifacts:
    - "Git tag v2.19.1 on upstream"
    - "A GitHub Release created from the CHANGELOG section written in plan 05"
    - "A per-crate registry verification transcript recorded in the plan SUMMARY"
    - "Updated ROADMAP progress table and REQUIREMENTS entry for PKGR-01"
    - "A closeout pull request carrying the planning-record updates and this phase's post-merge summaries to `main`"
  key_links:
    - "`124-expected-release.json` (committed in plan 05, on merged main) is the source for BOTH the pre-tag check and the post-publish verification — one artifact, so the two cannot disagree"
    - "the tag name must equal `v` plus the CHANGELOG heading version, or release.yml's awk extraction yields empty release notes with exit 0"
    - "the tag must be pushed to `upstream` (paiml/rust-mcp-sdk), not to `origin` — release.yml runs on the upstream repository"
    - "`release.yml`'s publish-crates job skips already-published versions gracefully, which is exactly what makes a stale version number silently not ship — hence the pre-push re-query"
  prohibitions:
    - "Must not push the tag. The tag push is a user-fired action; an agent may prepare it, verify afterwards and report, and nothing more."
    - "Must not derive the publication verdict from the Actions run's overall status. The MCP-registry job is known-broken and would make a green-eyed reading misleading in one direction, while the already-published skip would make it misleading in the other."
    - "Must not yank, re-tag or re-push to correct a mistake without returning to the user first — a published version cannot be unpublished, only yanked, and a re-tag rewrites a published release record."
    - "Must not close the phase on a partial verification: every expected pair is checked, and any miss is reported as a miss rather than explained away."
    - "Must not classify a post-publish registry miss as a publication failure on a single immediate query. crates.io indexing is eventually consistent; retry with bounded backoff first. Symmetrically, must not classify an unparseable response as a miss — a schema-stub 200 was reproduced twice in ~8 requests during review and is a FAILED PROBE, not evidence of absence."
    - "Must not leave the phase's closeout edits as an uncommitted local tree. ROADMAP, REQUIREMENTS, STATE and this phase's summaries must reach `upstream/main` via a pushed, merged closeout PR; a green `make quality-gate` over a dirty tree proves nothing about what the repository retains."
    - "Must not require crates in the `expected_skips` set to differ from their published version. They are expected to be skipped; asserting otherwise blocks a legitimate release."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land any change in this phase."
---

<objective>
Ship it, then prove it shipped. Re-verify registry state at the last reversible moment, hand
the tag push to the user, and confirm each expected crate version live on crates.io — per
crate, against the registry API, not against the workflow run's status.

Purpose: D-07 makes the phase close only when the tag is pushed, `release.yml` has run, and
each expected crate version is confirmed live. Publishing is permanent: a version can be
yanked but never unpublished, and the number is consumed forever. Meanwhile `release.yml`'s
already-published skip means a stale version number produces a *silent* non-publication —
the failure this whole phase exists to make visible.
Output: a published v2.6 crate tree, a per-crate verification transcript, and a closed-out
phase record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-03-SUMMARY.md
@.planning/phases/124-release-publish-order/124-05-SUMMARY.md
@.planning/phases/124-release-publish-order/124-06-SUMMARY.md
@CLAUDE.md
</context>

<review_dispositions>
## Cross-AI review findings dispositioned in this plan

Source: `124-REVIEWS.md`. The review's judgement was that publication controls here are strong
but repository closeout and state continuity are incomplete; Task 5 is the structural fix.

| Finding | Severity | Disposition |
|---|---|---|
| Closeout files are never delivered to upstream — Task 4 edits ROADMAP/REQUIREMENTS/STATE after the PR merged and the tag published, with no commit, push or follow-up PR (Codex HIGH) | HIGH | **Incorporated as new Task 5**, a post-release closeout branch and PR. The `<verification>` and a must_have now require the edits to land on `upstream/main`, not merely to exist locally. |
| Plan 07 depends on summaries not present on merged main (Codex HIGH) | HIGH | **Incorporated.** The durable state moved to `124-expected-release.json`, committed in plan 05 Task 4 and therefore on merged `main`; plan 06 Task 1 additionally commits its evidence pre-merge. This plan reads the JSON, not the summaries. |
| Pre-tag criterion is too strict and partly wrong — a release may deliberately include already-published crates that skip (Codex HIGH) | HIGH | **Incorporated.** Task 1 now verifies two disjoint sets against their own expectations: `expected_new` must NOT be present on the registry, `expected_skips` must be. |
| Tag verification may return two refs for an annotated tag (Codex MEDIUM) | MEDIUM | **Incorporated.** Task 3 resolves the tag object and its peeled commit explicitly instead of counting `ls-remote` lines. |
| Registry verification lacks retry/backoff — the one concern BOTH reviewers raised (Codex MEDIUM, Gemini Risk 4) | MEDIUM | **Incorporated** in Task 4 with bounded exponential backoff, and paired with a distinct classification for a malformed body so a schema-stub 200 is never read as absence. |
| GitHub Release note "matching" is not defined (Codex MEDIUM) | MEDIUM | **Incorporated.** Task 4 Step C compares the normalised raw API body against the extracted changelog text, with the normalisation stated. |
| The plan presupposes the MCP-registry job is red (Codex LOW) | LOW | **Incorporated.** Its ACTUAL conclusion is recorded from the run; the expectation is stated to the user in advance so a red job is not misread, but the record is measured. |
| Generate a committed `expected-release.json` shared by plans 03/05/06/07 (Codex cross-plan fix 3) | HIGH | **Incorporated** — created in plan 05 Task 4, consumed here in Tasks 1 and 4 and in plan 06 Task 2. |
</review_dispositions>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 07)

No code symbols. This plan produces external artifacts — the git tag `v2.19.1` on
paiml/rust-mcp-sdk, the GitHub Release it triggers, and the published crate versions on
crates.io — plus updates to `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` and
`.planning/STATE.md`, delivered to `upstream/main` by a **closeout branch and pull request**
(`chore/124-closeout`) created in Task 5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-tag re-verification on merged main</name>
  <files>(no file writes — measurement only; output recorded in the SUMMARY)</files>
  <precondition>Network access to https://crates.io/api/v1 is available, and `upstream/main` has been fetched and carries the merge from plan 06. Halt and report if either is false: the pre-push check is the last reversible moment and cannot be skipped.</precondition>
  <read_first>
    - .planning/phases/124-release-publish-order/124-expected-release.json (READ FROM MERGED MAIN — the committed authority for `expected_new`, `expected_skips`, `registry_baselines` and `authorised_exceptions`. This is the artifact to trust; it was committed in plan 05 so it survives the merge, unlike a summary written after the merge.)
    - .planning/phases/124-release-publish-order/124-05-SUMMARY.md (supporting narrative for the version set — the JSON above is the machine-checkable form)
    - .planning/phases/124-release-publish-order/124-06-SUMMARY.md (the merge commit, style and `merged_by` — Task 1's half of it was committed pre-merge and is on main; the post-merge half may only exist locally)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (the "D-07 verification: confirm each expected version went live" code example; Pitfall 7 on the CHANGELOG extraction; Pitfall 8 on the MCP-registry job; the Runtime State Inventory's warning that the registry table ages and must be re-verified immediately before the tag push)
    - CLAUDE.md, "Release Steps" (tag form `vX.Y.Z`, annotated, pushed to upstream after the merge)
    - scripts/release-version-sweep.sh (the committed sweep — run it against merged main)
  </read_first>
  <action>
Work from a checkout of merged `upstream/main`, not from the pre-merge branch state.

Step A — re-run `make release-sweep` against merged `main`, and read
`124-expected-release.json` from that same checkout (plan 05 committed it, so it is on merged
`main`; it is the authority for what this release intends, not a plan summary). The registry
table in RESEARCH is explicitly stamped as ageing, and the sweep is the tool built precisely so
this check is one command.

**Verify TWO DISJOINT SETS against their own expectations.** A single blanket rule is wrong
here:

  - `expected_new` — every crate this release intends to publish for the first time at this
    version. Each must show an in-tree version AHEAD of its published version and must NOT
    already be present on the registry at that version. A crate whose in-tree version already
    equals its published version will be skipped silently by `release.yml`'s already-published
    guard and its changes will not ship.
  - `expected_skips` — crates whose in-tree version IS already published, which
    `release.yml` will therefore skip gracefully and deliberately. Each must be confirmed
    already present, with the recorded reason. These are a normal part of a multi-crate tag,
    and requiring them to differ from their published version — as the pre-replan criterion
    did for "every intended crate" — would block a legitimate release.

Then confirm the two sets are still disjoint and still cover every crate the sweep enumerates.
A crate in neither set is a crate nobody decided about; surface it rather than absorbing it.

Also confirm no NEW phantom delta has appeared since plan 03's checkpoint (for example, from a
commit that landed on `main` between the checkpoint and the merge). A new one is a decision,
not an execution detail — surface it to the user rather than absorbing it.

Compare the sweep's freshly measured published versions against `registry_baselines` in the
JSON. A divergence means the registry moved between the decision and now, and is worth naming
explicitly at the Task 2 checkpoint rather than silently adopting the newer reading.

Step B — confirm the CHANGELOG extraction still works on merged `main` by re-running
`release.yml`'s own awk for the version being tagged and asserting non-empty output. This is
the second run of that check (plan 05 ran the first); it is repeated here because the merge
could have altered the file and the failure mode is silent.

Step C — confirm the tag name. The tag is `v2.19.1` (D-06), matching the `pmcp` core bump, and
it must equal `v` plus the CHANGELOG heading version exactly. Confirm no tag of that name
already exists on `upstream`.

Step D — prepare, but do not push, the annotated tag command for the user, including a message
summarising the release. Also prepare the D-07 verification input: the full list of expected
`(crate, version)` pairs, one per line, taken from plan 05's summary and cross-checked against
the sweep output from Step A.

If any of Steps A–C fails, stop. Everything up to the tag push is still reversible; nothing
after it is.
  </action>
  <verify>
    <automated>make release-sweep > /tmp/124-presweep.txt; test -s /tmp/124-presweep.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/124-presweep.txt` exists, is non-empty, and is reproduced in full in the SUMMARY
    - `124-expected-release.json` is read from the merged-`main` checkout and its `expected_new` set is NON-EMPTY
    - **`expected_new`:** for every pair, the sweep line shows an in-tree version DIFFERENT from its published version, and the registry does not already hold that version — checked pair by pair and recorded
    - **`expected_skips`:** for every entry, the registry DOES already hold that version, matching the recorded reason — checked entry by entry and recorded. These are not required to differ; that is the point of the set.
    - The two sets are disjoint and their union covers every crate the sweep enumerates; any crate in neither is named in the SUMMARY and raised with the user
    - Any divergence between the sweep's fresh published versions and the JSON's `registry_baselines` is listed explicitly in the SUMMARY
    - Any crate showing a NEW phantom delta not present at plan 03's checkpoint is listed explicitly in the SUMMARY and raised with the user before the tag checkpoint
    - Re-running the release workflow's changelog awk for 2.19.1 against CHANGELOG.md produces non-empty output on merged main: `awk -v ver="## \\[2.19.1\\]" '$0 ~ ver {p=1; next} /^## \[/ && p {exit} p {print}' CHANGELOG.md > /tmp/124-notes-main.txt` then `test -s /tmp/124-notes-main.txt` exits 0
    - The tag does not already exist, resolved rather than line-counted: `git ls-remote --tags upstream 'refs/tags/v2.19.1*' > /tmp/124-tagcheck.txt` then the file contains NO line whose ref is exactly `refs/tags/v2.19.1` and none ending `refs/tags/v2.19.1^{}`. Record the raw output. (An annotated tag surfaces both a tag ref and a peeled `^{}` ref, so a bare line count is not a reliable existence test in either direction.)
    - The expected-pairs list is written to a file and its contents recorded in the SUMMARY
  </acceptance_criteria>
  <done>The last reversible moment has been used: registry state, changelog extraction and tag name are all re-verified against merged `main`, and the exact expected-pairs list is prepared.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: DECISION — authorise publication of the enumerated version set (one-way door)</name>
  <files>(no file writes — the authorisation is recorded in the SUMMARY)</files>
  <decision>Authorise, hold, or abandon publication of the exact `(crate, version)` set prepared in Task 1, by pushing tag `v2.19.1` to `upstream`.</decision>
  <context>
This is the phase's one-way door and it is worth naming as its own step rather than folding
into the mechanics of pushing a tag. Pushing a `v*` tag is what grants `release.yml` the
registry token, so the push IS the publication. A published version can be yanked but never
unpublished, and the number is consumed forever.

Task 1 has just re-verified, against merged `main` rather than against the pre-merge branch:
that every crate on the list has an in-tree version differing from its published version (so
none will be silently skipped by the already-published guard); that the release workflow's own
changelog extraction yields non-empty notes for this version; and that no tag of this name
already exists upstream. It has also checked for any NEW phantom delta that appeared between
plan 03's checkpoint and the merge.

Everything up to this point is reversible. Nothing after it is.
  </context>
  <options>
    <option id="publish">
      <name>Publish now — push tag v2.19.1 as prepared</name>
      <pros>The v2.6 milestone ships. The list was authorised at plan 03's checkpoint and re-verified against merged `main` minutes ago.</pros>
      <cons>Irreversible. Every version number on the list is consumed permanently, and any crate wrongly absent from the list stays unshipped with no repo-side reminder.</cons>
    </option>
    <option id="hold">
      <name>Hold — do not tag yet; revise the version set or the changelog first</name>
      <pros>Correct response to anything Task 1 surfaced: a new phantom delta, a crate whose version did not actually move, or release notes that read wrong. Costs only time.</pros>
      <cons>The phase stays open, and `main` may move further, requiring Task 1's checks to be re-run before a later attempt.</cons>
    </option>
    <option id="abandon">
      <name>Abandon this release cycle</name>
      <pros>Appropriate if something structural is wrong — for example a semver axis that turned out incorrect after the merge.</pros>
      <cons>The bumped version literals are already on `main` and would need a follow-up decision; the milestone does not ship.</cons>
    </option>
  </options>
  <resume-signal>Reply with the option id. `publish` proceeds to Task 3, which hands you the exact tag command; anything else stops the phase here with the reason recorded.</resume-signal>
  <read_first>
    - The Task 1 SUMMARY entry (the prepared tag command, the expected-pairs list, the sweep transcript, and any new phantom delta it surfaced)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-06 the tag name, D-07 the ownership of the ship and its reversibility note)
    - .planning/phases/124-release-publish-order/124-03-SUMMARY.md (the version set the user authorised at plan 03's checkpoint — confirm the Task 1 list still matches it)
  </read_first>
  <action>
Present the one-way door as a decision in its own right, before any command is handed over.

Show the user:
  - the complete enumerated `(crate, version)` list, exactly as Task 1 prepared it, and
    whether it still matches the set authorised at plan 03's checkpoint;
  - the Task 1 verification results, item by item: in-tree-differs-from-published per crate,
    changelog extraction non-empty, tag name free;
  - any NEW phantom delta Task 1 found since plan 03's checkpoint, called out separately — a
    new one is a decision, not an execution detail;
  - the irreversibility statement in plain terms.

Do not present the tag command at this step. This step decides whether to walk through the
door; Task 3 opens it.
  </action>
  <verify>
    <human-check>The user has selected publish, hold, or abandon, and the choice is recorded.</human-check>
    <automated>test -s /tmp/124-presweep.txt</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the selected option id verbatim, with any reason the user gave
    - The SUMMARY reproduces the enumerated `(crate, version)` list that was presented, and states whether it matched plan 03's authorised set
    - If the option was `hold` or `abandon`, the phase stops here and the SUMMARY states what must change before a retry
    - No tag exists on `upstream` at the close of this task: `/opt/homebrew/bin/git ls-remote --tags upstream v2.19.1 > /tmp/124-tagcheck2.txt` then `/usr/bin/wc -l /tmp/124-tagcheck2.txt` reports 0 lines
  </acceptance_criteria>
  <done>The user has explicitly authorised (or declined) the permanent consumption of a specific, enumerated set of version numbers, with the verification evidence in front of them.</done>
  <reversibility rating="one-way">This checkpoint is the confirmation gate for the phase's one-way door. Publishing to crates.io is permanent: a version can be yanked but never unpublished, and its number is consumed forever.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: HUMAN ACTION — push the release tag</name>
  <files>(no file writes)</files>
  <instructions>
Already done by the agent:
  - merged `main` re-swept against the crates.io API; every crate on the list confirmed to
    have an in-tree version differing from its published version
  - the release workflow's own changelog awk re-run for this version, output confirmed
    non-empty
  - confirmed no `v2.19.1` tag exists on `upstream`
  - the annotated tag command prepared verbatim, with a release message

The ONE thing requiring you:
  **Run the prepared annotated-tag command and push it to `upstream`** — not `origin`.
  `release.yml` runs on paiml/rust-mcp-sdk; a tag pushed to the personal fork triggers
  nothing.

What the push triggers: a GitHub Release built from the CHANGELOG section, the ordered
crates.io publish job with its thirty-second index waits between crates, the MCP-registry
publish job, and the cross-platform tester-binary build.

**Expect the MCP-registry job to be RED, and do not read it as a failed release.** Its OIDC
publish has been broken since v2.17.0, it is a separate job from the crates.io publish, and
fixing it is explicitly outside this phase. This is stated before the run rather than after,
so a red job in the Actions UI is not mistaken for a blocked release.

If the crates.io publish job fails partway, stop and tell the agent rather than re-running.
The already-published guard makes a re-run safe for the crates that succeeded, but whether to
re-run, skip a crate, or yank is your decision.
  </instructions>
  <verification>
After you report the push, the agent confirms the tag exists on `upstream` and that its target
commit is on `upstream/main`, identifies the triggered workflow run by id, and records every
job's name and conclusion — including the MCP-registry job, marked out of scope. The
publication verdict itself is NOT taken from that run; Task 4 derives it from the crates.io
API, per crate.
  </verification>
  <resume-signal>Reply "pushed" with the tag SHA, or describe what stopped you.</resume-signal>
  <read_first>
    - The Task 2 decision record (the authorised option and the enumerated list)
    - The Task 1 SUMMARY entry (the prepared tag command, the expected-pairs list, the sweep transcript)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-06, D-07 and its reversibility note)
    - CLAUDE.md, "What Happens Automatically (CI)" — the four jobs a `v*` tag triggers
  </read_first>
  <action>
Hand the prepared tag push to the user, the authorisation for it having been given at Task 2.
An agent must not push a `v*` tag: the tag trigger is what grants `release.yml` the registry
token, so pushing the tag IS publishing.

Give the user the exact annotated-tag command, verbatim and ready to paste, stating that it
targets `upstream` (paiml/rust-mcp-sdk) rather than `origin`, and restating the enumerated
`(crate, version)` list one final time alongside it.

After the user reports the push, watch the workflow run to completion and record, per job, its
name and conclusion. Do not derive the release verdict from the run's overall status — that is
Task 4's job, and it uses the registry, not the Actions UI. Two distinct reasons the run
status is untrustworthy here: the MCP-registry job is known-broken and would make a
green-eyed reading misleading, and the crates.io publish job's already-published guard makes
"published" and "silently skipped" indistinguishable from its exit status.

If the crates.io publish job fails partway, stop and return to the user. The already-published
guard makes a re-run safe for crates that succeeded, but the decision to re-run, to skip a
crate, or to yank is the user's.
  </action>
  <verify>
    <human-check>The user has pushed the tag v2.19.1 to upstream and confirmed it.</human-check>
    <automated>git fetch upstream --tags && git rev-parse --verify 'refs/tags/v2.19.1^{commit}'</automated>
  </verify>
  <acceptance_criteria>
    - **The tag is resolved as an object, not counted as lines.** `git fetch upstream --tags`, then `git cat-file -t refs/tags/v2.19.1` reports `tag` (an annotated tag, per CLAUDE.md's Release Steps) and `git rev-parse 'refs/tags/v2.19.1^{commit}'` resolves the peeled commit. Both the tag object SHA and the peeled commit SHA are recorded in the SUMMARY. (A `git ls-remote | wc -l` check is unreliable: an annotated tag can surface both `refs/tags/v2.19.1` and `refs/tags/v2.19.1^{}`, so the expected line count is 1 or 2 depending on the query form.)
    - The tag's PEELED commit is an ancestor of `upstream/main`: `git merge-base --is-ancestor "$(git rev-parse 'refs/tags/v2.19.1^{commit}')" upstream/main` exits 0
    - The workflow run triggered by the tag is identified by run id in the SUMMARY, with a per-job name/conclusion list read from the API
    - The MCP-registry job's ACTUAL conclusion is recorded from the run — not presupposed — and explicitly marked as out of scope rather than as a phase blocker. The expectation of red is stated to the user in Task 3's instructions so a red job is not misread; the SUMMARY records what it actually was.
    - The SUMMARY records that the push was performed by the user, with the reported time and the user's confirmation, rather than asserting the absence of an agent push from git history (which cannot show who pushed)
  </acceptance_criteria>
  <done>The release tag is live on the upstream repository, pushed by the user with the full consumed-version list in front of them, and the triggered run is recorded job by job.</done>
  <reversibility rating="one-way">Pushing a `v*` tag publishes to crates.io. A published version can be yanked but never unpublished, and its number is consumed forever. This is the phase's one-way door; Task 2's decision checkpoint is the confirmation that precedes it, and the push itself is the user's action rather than an agent's.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Verify publication per crate against the registry, and close the phase out</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <precondition>Network access to https://crates.io/api/v1 is available. The registry is the only acceptable oracle for this verification; the Actions run's status is explicitly not.</precondition>
  <read_first>
    - The Task 1 expected-pairs list and the Task 3 run record
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (the D-07 verification script shape, confirmed working for 21 crates; Pitfall 8)
    - .planning/ROADMAP.md (the Phase 124 section with SC1-SC4, and the "Progress — Current Milestone" table)
    - .planning/REQUIREMENTS.md (PKGR-01 and its measured-drift correction block at :42-49 — the correction is the requirement, not the bullet at :37)
    - .planning/STATE.md (the Current Position block and the v2.6 phase-plan table)
  </read_first>
  <action>
Step A — verify publication per crate. For every `(crate, version)` pair in `expected_new`,
query the crates.io API v1 versions endpoint for that crate with a `User-Agent` header and
confirm the expected version appears among the returned versions. Report per pair: confirmed,
or missing with the versions the registry actually holds. Do not stop at the first miss —
produce the complete picture, because a partial publish is a specific and actionable state.

**Retry before concluding, and distinguish the two failure shapes.** They need different
responses and must not be collapsed:

  - **A miss.** crates.io's index is eventually consistent and can lag a successful publish by
    tens of seconds — this is the one concern BOTH reviewers raised independently. Retry a
    missing pair with bounded exponential backoff (for example 3-5 attempts over roughly a
    minute) before classifying it as a publication failure. Record the attempt count and the
    elapsed time for any pair that needed retries, so a slow index is visible as slowness
    rather than mistaken for an outage.
  - **A malformed response.** A 200 whose body is not parseable as the versions payload is a
    FAILED PROBE, not evidence of absence. Measured during the review, twice in about eight
    requests and for both `pmcp` and `mcp-tester`: the body came back as a type descriptor
    (`{ meta: { next_page: null, total: int }, versions: [{ audit_actions: [{ action: string,
    ...`). Retry it, and if it persists report `PROBE_FAILED` for that crate explicitly. Never
    let a parse error render as "not published" — at this point in the phase that would report
    a successful release as a failure and could prompt an unnecessary re-tag.

Also confirm the `expected_skips` set behaved as expected: each already-published version is
still present and the workflow skipped it. A skip is a planned outcome here, not a defect.

The verdict for this phase comes from this check and from nothing else. In particular:
  - do not read the Actions run's overall status as the verdict. The MCP-registry job is
    known-broken and would make a green-eyed reading misleading;
  - do not read a green crates.io publish job as proof either. Its already-published guard
    means "skipped because the version was already taken" and "published successfully" look
    the same from the job's exit status. Only the registry distinguishes them, and only
    against the *expected* version.

Step B — if any pair is missing, do not improvise. Record the complete missing set, determine
from the run log whether the crate's step ran and what it reported, and return to the user
with options. Re-running the workflow is safe for the crates that succeeded, but yanking,
re-tagging or skipping a crate are all user decisions.

Step C — confirm the GitHub Release exists for the tag and that its notes are non-empty and
match the CHANGELOG section written in plan 05. **Define "match" rather than eyeballing it:**
fetch the release body via `gh api repos/paiml/rust-mcp-sdk/releases/tags/v2.19.1 --jq .body`,
normalise both it and the plan-05 extraction identically (strip trailing whitespace per line,
collapse runs of blank lines, normalise line endings), and compare the normalised texts. GitHub
renders Markdown and can differ from the source in whitespace, so an exact byte comparison
would produce false mismatches while an unspecified comparison produces false matches. Record
both the verdict and the normalisation used. Empty notes here are the Pitfall 7 failure
arriving after the fact; record the finding even though the release body can be edited
afterwards.

Step D — prepare the planning-record closeout edits. **Prepare them here; Task 5 delivers
them.** Editing these files without committing and pushing leaves the repository without the
completed state this phase claims, and a final `make quality-gate` over a dirty tree validates
nothing about what the repository retains.
  - `.planning/ROADMAP.md`: mark Phase 124 complete in the "Progress — Current Milestone"
    table with its plan count and completion date, and update the phase's Plans line. Note
    that the ROADMAP checkbox and the progress table have disagreed on this project before
    and that the table is the one that governs — update both so they agree.
  - `.planning/REQUIREMENTS.md`: mark PKGR-01 complete, referencing the correction block at
    :42-49 rather than the superseded bullet at :37, and stating which of the two residual
    gaps each piece of this phase closed.
  - `.planning/STATE.md`: advance the current position, and record the release facts a future
    reader needs: the tag, the published `(crate, version)` set, the fact that the coverage
    gate now covers workspace-excluded crates and machine-checks the `pmcp-package` cluster
    order, and the standing items this phase deliberately did not close — the
    `feat/package-172-cli` merge, the MCP-registry OIDC publish, and the four crates carrying
    an `mcp-tester` dev-dependency with both `path` and `version` that publish before it.

Step E — record the phase's own deferred items so the next releaser inherits them rather than
rediscovering them: any crate the user chose to leave un-bumped at plan 03's checkpoint (with
the note that `make release-sweep` is the only thing that will surface it again), and any new
phantom delta Task 1 found.
  </action>
  <verify>
    <automated>/usr/bin/curl -s -H "User-Agent: pmcp-release-audit (guy@mlguy.us)" "https://crates.io/api/v1/crates/pmcp/versions" > /tmp/124-pmcp-versions.json; /usr/bin/grep -c '2.19.1' /tmp/124-pmcp-versions.json</automated>
  </verify>
  <acceptance_criteria>
    - Every `(crate, version)` pair on the expected list is confirmed present in that crate's crates.io API response; the SUMMARY carries one line per pair with a confirmed-or-missing verdict and, for a miss, the versions the registry actually holds
    - `/tmp/124-pmcp-versions.json` contains the shipped `pmcp` version, and the count command returns at least 1
    - The verdict statement in the SUMMARY names the registry as its source and states explicitly that neither the Actions run status nor the publish job's exit status was used
    - The GitHub Release for the tag exists and its notes are non-empty, matching the CHANGELOG section — recorded in the SUMMARY
    - `.planning/ROADMAP.md` shows Phase 124 complete in the "Progress — Current Milestone" table AND its phase checkbox agrees with the table: `/usr/bin/grep -c '124. Release & Publish Order' .planning/ROADMAP.md` returns at least 1 and the row reads Complete
    - `.planning/REQUIREMENTS.md`'s PKGR-01 entry is marked complete and cites the correction block rather than the superseded bullet
    - `.planning/STATE.md` records the tag, the published set, and the three standing items this phase deliberately did not close
    - Every `expected_skips` entry is confirmed still present on the registry and recorded as a planned skip rather than a defect
    - Any pair that needed retries has its attempt count and elapsed time recorded; any `PROBE_FAILED` is recorded as a failed probe and NOT as a missing publication
    - The GitHub Release note comparison records the verdict AND the normalisation applied
    - `RUSTFLAGS="" make quality-gate` exits 0 on the final tree
  </acceptance_criteria>
  <done>Publication is verified crate by crate against the registry — the only oracle that can distinguish "published" from "silently skipped" — with index lag and malformed responses classified apart from genuine misses, and the planning-record edits are prepared for delivery.</done>
</task>

<task type="auto">
  <name>Task 5: Deliver the closeout to upstream/main via a pushed, merged PR</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/124-release-publish-order/124-06-SUMMARY.md, .planning/phases/124-release-publish-order/124-07-SUMMARY.md</files>
  <precondition>`gh auth status` succeeds for github.com with permission to open a pull request on paiml/rust-mcp-sdk, and Task 4's per-crate registry verification has produced a complete verdict. Halt and report if either is false — a closeout that records an unverified release is worse than an open phase.</precondition>
  <read_first>
    - The Task 4 closeout edits as prepared in the working tree (ROADMAP progress table, REQUIREMENTS PKGR-01 entry, STATE current position)
    - .planning/phases/124-release-publish-order/124-06-SUMMARY.md (its post-merge half — merge SHA, style, `merged_by` — may exist only locally; this PR is how it reaches `main`)
    - CLAUDE.md, "Release Steps" (the `gh pr create --repo paiml/rust-mcp-sdk --head <fork>:<branch> --base main` form)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-08's reversibility note — new branches must be cut from `upstream/main`, which after plan 06's squash-merge is exactly the situation this task is in)
  </read_first>
  <action>
The release PR merged in plan 06 and the tag published in Task 3. Everything written since —
the ROADMAP progress row, the REQUIREMENTS PKGR-01 closure, the STATE advance, plan 06's
post-merge facts and this plan's own summary — exists only in the local tree. Without this
task the repository never retains the completed state the phase claims, and the next reader
finds a milestone that shipped according to nobody.

**Cut the branch from `upstream/main`, not from the old milestone branch.** Plan 06's merge was
very likely a squash, which is precisely the condition under which this project's recorded
squash-divergence trap repeats. Fetch `upstream`, create `chore/124-closeout` from
`upstream/main`, and apply the closeout edits there.

Contents of the closeout:

  - `.planning/ROADMAP.md` — Phase 124 marked complete in the "Progress — Current Milestone"
    table with its plan count and completion date, and the phase's Plans line updated. The
    checkbox and the table have disagreed on this project before and the table governs; update
    both so they agree.
  - `.planning/REQUIREMENTS.md` — PKGR-01 marked complete, citing the measured-drift correction
    block at `:42-49` rather than the superseded bullet at `:37`, and stating which of the two
    residual gaps each piece of this phase closed.
  - `.planning/STATE.md` — current position advanced, plus the release facts a future reader
    needs: the tag, the published `(crate, version)` set, the fact that the coverage gate now
    covers workspace-excluded crates and machine-checks the `pmcp-package` cluster order, and
    the standing items this phase deliberately did not close (the `feat/package-172-cli` merge,
    the MCP-registry OIDC publish, and the four crates carrying an `mcp-tester` dev-dependency
    with both `path` and `version` that publish before it).
  - This phase's summaries, including plan 06's post-merge half.
  - The deferred items from Step E — any crate the user chose to leave un-bumped at plan 03's
    checkpoint, with the note that `make release-sweep` is the only thing that will surface it
    again, and any new phantom delta Task 1 found.

Run `RUSTFLAGS="" make quality-gate` on the closeout branch — this time over a COMMITTED tree,
so the result means something — then push and open the PR against `paiml/rust-mcp-sdk`'s
`main`. These are planning documents only: no manifest, no workflow, no source. Say so in the
PR body so the reviewer knows the diff should contain nothing executable.

The merge of this PR is a user action, consistent with D-07's ownership of outward-facing
steps, but it is NOT a one-way door — nothing here publishes anything. Report the PR and let
the user merge it; if the user merges immediately, confirm and record it, and if not, record
the open PR number in the SUMMARY as the phase's final outstanding item so it is not lost.
  </action>
  <verify>
    <automated>git fetch upstream main && git diff --name-only upstream/main...HEAD > /tmp/124-closeout-files.txt && test -s /tmp/124-closeout-files.txt && ! grep -qE '(Cargo\.toml|\.github/workflows/|^src/|^scripts/)' /tmp/124-closeout-files.txt && echo "closeout diff is planning-only: $(wc -l < /tmp/124-closeout-files.txt) files"</automated>
  </verify>
  <acceptance_criteria>
    - `chore/124-closeout` was branched from `upstream/main`: `git merge-base --is-ancestor upstream/main HEAD` exits 0 at branch creation time, recorded in the SUMMARY
    - The closeout diff is NON-EMPTY and contains only planning documents — no `Cargo.toml`, no `.github/workflows/`, no `src/`, no `scripts/` (asserted by the verify command, which fails on an empty diff as well as on an executable one)
    - `.planning/ROADMAP.md` shows Phase 124 complete in the "Progress — Current Milestone" table AND its phase checkbox agrees with the table
    - `.planning/REQUIREMENTS.md`'s PKGR-01 entry is marked complete and cites the correction block rather than the superseded bullet
    - `.planning/STATE.md` records the tag, the published set, and the standing items
    - `RUSTFLAGS="" make quality-gate` exits 0 on the COMMITTED closeout branch, with the transcript recorded — not over a dirty tree
    - A PR exists on paiml/rust-mcp-sdk from `chore/124-closeout` to `main`; its number and URL are recorded in the SUMMARY
    - The SUMMARY states the PR's final state: merged (with SHA), or open with the number recorded as the phase's outstanding item
  </acceptance_criteria>
  <done>The planning record of this release — what shipped, what was deliberately skipped, and what the next releaser inherits — is delivered to `upstream/main` through a reviewable PR rather than left in an uncommitted local tree.</done>
  <reversibility rating="reversible">Planning-document edits on a branch; nothing here publishes or tags. Fully revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git tag -> registry token | The `v*` tag trigger is what grants `release.yml` the `CARGO_REGISTRY_TOKEN`. Anyone who can push such a tag can publish; the human checkpoint is the control. |
| crates.io API -> phase verdict | An untrusted remote response is the sole oracle for whether this phase succeeded. It is queried per crate, per expected version. |
| Actions run status -> phase verdict | Explicitly NOT a trust path: one job is known-broken and the publish job's skip guard makes success and silent-skip indistinguishable from its exit status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-04 | Elevation of Privilege | `CARGO_REGISTRY_TOKEN` reachable via a `v*` tag push | medium | mitigate | The tag push is a blocking human checkpoint with the full consumed-version list presented; the plan's prohibitions forbid an agent-performed push and the acceptance criteria assert none occurred (Tasks 2 and 3) |
| T-124-02 | Denial (of the release) | a crate silently skipped by the already-published guard | high | mitigate | Pre-push sweep confirms every intended crate's in-tree version differs from its published version (Task 1 Step A), and post-run verification is per crate against the registry rather than against job status (Task 4 Step A) |
| T-124-07 | Spoofing | published-state oracle | medium | mitigate | crates.io API v1 with a `User-Agent`; the Actions UI and Cargo's own search are both excluded by the plan's prohibitions |
| T-124-06 | Information Disclosure | `cargo publish` payload | medium | mitigate | Publication runs only from the tag-triggered clean CI checkout, never from a local tree, and never with a dirty-tree override — a local publish would also ship untracked working state |
| T-124-17 | Denial | misreading the known-broken MCP-registry job | low | accept | Named before the run rather than after, and excluded from the verdict by construction; its ACTUAL conclusion is recorded rather than presupposed. Accepted rather than mitigated: fixing the OIDC publish is explicitly deferred out of this phase and must not gate crates.io publication. |
| T-124-28 | Repudiation | the repository never retaining the completed state this phase claims | high | mitigate | Task 5 delivers ROADMAP, REQUIREMENTS, STATE and this phase's summaries to `upstream/main` through a pushed, reviewable closeout PR, branched from `upstream/main` so the post-squash divergence trap does not repeat. The final quality gate runs over a COMMITTED tree. |
| T-124-29 | Spoofing | crates.io index lag, or a schema-stub 200, read as a failed publication | medium | mitigate | Bounded exponential backoff before classifying a miss; a parse failure classified as `PROBE_FAILED` and never as absence (Task 4 Step A). Misreading either would report a successful release as a failure and could prompt an unnecessary re-tag — which the plan's prohibitions already forbid without returning to the user. |
</threat_model>

<verification>
- Pre-push: sweep re-run on merged `main`; `expected_new` confirmed absent from the registry and `expected_skips` confirmed present, each against its own expectation; changelog extraction non-empty; tag name free (resolved, not line-counted)
- Tag pushed by the user to `upstream`, resolved as a tag object whose peeled commit is an ancestor of `main`
- Every expected `(crate, version)` pair confirmed present in the crates.io API response, with retries distinguished from misses and misses distinguished from failed probes
- The verdict derives from the registry alone; the MCP-registry job's actual state is recorded and excluded
- ROADMAP, REQUIREMENTS and STATE updated with the deferred items carried forward, AND delivered to `upstream/main` by a pushed closeout PR whose diff contains only planning documents
</verification>

<success_criteria>
- D-07 is met in full: the tag is pushed, `release.yml` has run, and each expected crate version is confirmed live via the crates.io API
- SC1-SC4 are all closed, with SC2 and SC4 permanently protected by the gate self-test rather than demonstrated once
- The v2.6 AI-Package Portability milestone has actually shipped, and the next releaser inherits a machine-checked ledger plus a written list of what this phase did not close
- The record of all of the above is ON `upstream/main`, not on a developer's disk
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-07-SUMMARY.md` when done
</output>