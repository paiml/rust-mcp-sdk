---
phase: 124-release-publish-order
plan: 04
type: execute
wave: 4
depends_on: ["124-03"]
files_modified:
  - .github/workflows/release.yml
  - CLAUDE.md
autonomous: true
requirements: [PKGR-01]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`.github/workflows/release.yml`'s comments state that `pmcp-package` publishes BEFORE `pmcp-cfn-renderer`, `pmcp-agent`, `pmcp-team-servers` and `cargo-pmcp`, and no comment in that file still claims it publishes last or that it has no consumers inside this repository (SC4, D-09)."
    - "No comment in `.github/workflows/release.yml` quotes a caret-pin value that disagrees with the manifest it describes — pin values are stated by reference to the CLAUDE.md ledger, not by value, because quoted literals in comments have no guard and have already rotted twice."
    - "CLAUDE.md states the pmcp-package-precedes-its-four-consumers constraint in exactly one place — asserted as EXACTLY ONE occurrence of a distinctive authoritative marker phrase, not as 'at least one' — and items 13, 13a, 14, 15 and 15a cross-reference that one statement using a DIFFERENT phrase instead of restating it."
    - "CLAUDE.md's nine-emitter paragraph enumerates NINE emitters, not eight. The list at CLAUDE.md:332-340 says 'Nine were measured' and then names only eight; the ninth is `crates/pmcp-openapi-server/Cargo.toml:124`, recorded in `122-08-SUMMARY.md:464-476` as row 6, UNCHANGED / path-only / CR-01 — the emitter whose correct action is to do nothing, and therefore the one most easily dropped from a list."
    - "CLAUDE.md's Version Bump Rules state the caret exception: a PATCH bump of a dependency does not require bumping crates that pin it, because a `^X.Y.Z` requirement already admits `X.Y.Z+1`. The blanket rule as written over-fires, and `pmcp` 2.19.0 -> 2.19.1 against `crates/mcp-tester/Cargo.toml:21` and `cargo-pmcp/Cargo.toml:68` is the live instance."
    - "CLAUDE.md's Pre-Flight Checklist no longer prescribes a Cargo registry-search command for checking published versions; it points at the crates.io API and at `make release-sweep`, resolving the contradiction with item 13's own Phase-122 note."
    - "`make check-release-coverage` still exits 0 after both files are edited — the comment edits do not disturb the machine-checked half."
    - "Every non-comment, non-blank line of `.github/workflows/release.yml` is byte-identical before and after this plan's edits — asserted over the whole file, not merely over lines containing `cargo publish`."
  artifacts:
    - "Reconciled ordering comment regions in .github/workflows/release.yml"
    - "A single consolidated ordering-constraint statement in CLAUDE.md's Release & Publish Workflow section"
    - "A corrected nine-emitter enumeration in CLAUDE.md's item 13"
    - "A caret-compatibility exception recorded in CLAUDE.md's Version Bump Rules"
    - "A corrected Pre-Flight Checklist step for checking published versions"
  key_links:
    - "release.yml is the AUTHORITY on publish order and CLAUDE.md is its mirror — prose is reconciled TOWARD the workflow, never the reverse"
    - "plan 01's D-10 order assertion protects the ORDER claim mechanically; the pin-VERSION claims in comments have no guard, which is why they move to by-reference form"
    - "the CLAUDE.md Pre-Flight edit points at `make release-sweep`, created in plan 03 — that target must exist before this edit lands"
    - "the nine-emitter prose fix here and plan 05's nine-emitter EXECUTION are two halves of one fact: plan 05 is already correct (its must_haves carry the path-only row); it is CLAUDE.md's prose that undercounts"
  prohibitions:
    - "Must not change any publish STEP in release.yml — this plan edits comments and prose only. The step order is already correct and is now machine-checked."
    - "Must not reconcile the workflow toward the prose. Where the two disagree, the workflow is right and the prose is wrong; that has been true every recorded time."
    - "Must not renumber CLAUDE.md's publish-order items — the numbering is deliberately dense because `item N` cross-references throughout the file are load-bearing."
    - "Must not delete the dated correction notes that record why a past entry was wrong; consolidation means adding one authoritative statement and pointing the others at it, not erasing the history."
    - "Must not 'fix' the nine-emitter inventory by reducing the stated count to eight. The count is correct and `122-08-SUMMARY.md:464-476` is the authority; it is the enumeration in CLAUDE.md's prose that is short by one. The missing row is the path-only, deliberately-UNCHANGED openapi-server dev-dep — dropping it hides the one emitter whose right move is inaction."
    - "Must not verify comment-only editing by greping the diff for `cargo publish` lines alone. That proves publish commands were unchanged; it proves nothing about other executable YAML keys, `env:` values, `if:` conditions or job wiring."
    - "Must not assert 'stated in exactly one place' with an at-least-one grep. Exactly-one means a count of exactly 1 against a distinctive marker phrase, with cross-references worded differently so they cannot inflate the count."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land any change in this phase."
---

<objective>
Reconcile both publish ledgers (D-09 / SC4) so the `pmcp-package`-precedes-its-consumers
constraint is *stated* rather than left implicit in step ordering, and so neither ledger
carries a claim that is now false.

Purpose: SC4 asks for the constraint to live in the ledger, not merely in the accident of
step order. Beyond the single stale line CONTEXT names, RESEARCH measured four stale regions
in `release.yml` — including a full prose ordering list that contradicts the executable steps
below it in the same file — plus an unreconciled contradiction inside CLAUDE.md itself, where
the Pre-Flight Checklist prescribes exactly the command item 13 forbids.
Output: two ledgers that agree with each other and with the manifests, with the order claim
mechanically protected by plan 01's assertion and the pin-version claims moved to a form that
cannot rot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-PATTERNS.md
@.planning/phases/124-release-publish-order/124-03-SUMMARY.md
@.github/workflows/release.yml
@CLAUDE.md
</context>

<review_dispositions>
## Cross-AI review findings dispositioned in this plan

Source: `124-REVIEWS.md`.

| Finding | Severity | Disposition |
|---|---|---|
| "Nine emitters" is internally inconsistent and really eight (Codex HIGH) | HIGH — **corrected** | **The reviewer's arithmetic is wrong and its stated fix is rejected; the residue is incorporated here.** `122-08-SUMMARY.md:464-476` lists exactly nine, and row 6 is `crates/pmcp-openapi-server/Cargo.toml:124` marked UNCHANGED / path-only / CR-01. Plan 05 is not defective. What IS defective is CLAUDE.md's prose at `:332-340`, which says "Nine were measured" and enumerates eight — Task 2 Edit 3 fixes exactly that. Do NOT reduce the count. |
| Plan 05 must bump `mcp-tester` and `cargo-pmcp` for the `pmcp` 2.19.1 move (Codex HIGH) | — **rejected** | **Wrong on the mechanics.** `^2.19.0` already admits 2.19.1, so no downstream pin moves. The reviewer's own fallback suggestion is the right one and is incorporated: Task 2 Edit 4 records the caret exception in CLAUDE.md's Version Bump Rules, whose blanket wording over-fires on patch bumps. |
| "Exactly one place" is not actually asserted — acceptance requires only "at least 1" (Codex MEDIUM) | MEDIUM | **Incorporated.** A distinctive authoritative marker phrase is asserted at exactly 1, and cross-references are required to use different wording. |
| The comment-only diff check is under-specified (Codex MEDIUM) | MEDIUM | **Incorporated.** Task 1 now asserts byte equality of ALL non-comment, non-blank lines in `release.yml` before and after, rather than only that no `cargo publish` line moved. |
| Fixed `sed` ranges will drift, and this plan changes the size of both inspected regions (Codex LOW; re-verified) | LOW | **Incorporated.** Both the Pre-Flight and Release Steps assertions are heading-anchored `awk` extractions. |
| The `mcp-tester` dev-dep warning slightly expands D-09 and should be recorded as inherited risk, not PKGR-01 completion (Codex LOW) | LOW | **Incorporated** — Task 2 Edit 5 labels it explicitly as inherited release risk. The enforcement half of that finding lives in plan 03's checkpoint and plan 05's prohibitions, which is where the review said it belonged. |
</review_dispositions>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 04)

No code symbols. This plan produces prose: a consolidated ordering-constraint statement in
`CLAUDE.md`'s Release & Publish Workflow section (referenced by items 13, 13a, 14, 15 and
15a), a rewritten publish-order comment block in `.github/workflows/release.yml`, and a
corrected Pre-Flight Checklist step that names `make release-sweep` (the target created in
plan 03) and the crates.io API endpoint.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile every order and pin claim in release.yml's comments</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml lines 85-105 (the `# Publish order:` prose list whose tail contradicts the steps below it) and lines 420-545 (the cluster ordering comment block and the five publish steps: pmcp-package at ~:440 via `--manifest-path`, then pmcp-cfn-renderer, pmcp-agent, pmcp-team-servers, cargo-pmcp)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 6 — the four measured stale regions with the truth beside each, and the recommendation to state pin values by reference rather than by value)
    - .planning/phases/124-release-publish-order/124-PATTERNS.md (the `.github/workflows/release.yml` section — the stale region to rewrite and the correct region to extend)
    - crates/pmcp-cfn-renderer/Cargo.toml, crates/pmcp-agent/Cargo.toml, crates/pmcp-team-servers/Cargo.toml and cargo-pmcp/Cargo.toml (the four `pmcp-package` requirement lines — read the CURRENT values rather than inheriting RESEARCH's, since plan 03's checkpoint may have authorised a move)
    - scripts/check-release-coverage.sh (as extended by plan 01 — confirm that comment edits cannot affect the matcher, which strips comments, or the order assertion, which derives ordinals from the stripped text)
  </read_first>
  <action>
Edit comments only. Do not move, add, remove or alter a single publish STEP: the step order is
already correct and is now machine-checked by plan 01's assertion, so any step edit would be
changing the authority to match the prose — precisely backwards.

Region A — the `# Publish order:` prose list near the top of the publish-crates job. Its tail
currently places `pmcp-package` at the end of the chain, after `cargo-pmcp` and `pmcp-server`.
That contradicts the executable steps in the same file, where `pmcp-package` publishes ahead
of all four of its consumers. Rewrite the tail so the listed order matches the step order.

Region B — the multi-line parenthetical attached to `pmcp-package` in that same list. It
asserts the crate has no consumers inside this repository and instructs a future maintainer to
move it earlier only once a shipped crate pins it. Both halves are false: four in-repo crates
pin it, and it was already moved earlier. Replace the parenthetical with a statement of the
real situation — a workspace-excluded crate published via `--manifest-path` rather than
`-p`, publishing ahead of its four consumers — and a pointer to the consolidated constraint
statement Task 2 creates in CLAUDE.md.

Region C — the cluster ordering comment block immediately above the `pmcp-package` publish
step (around :426-439). It already states the cluster order in prose, which is correct; the
defect is that it quotes each consumer's caret-pin as a literal value, and all four quoted
values are stale relative to their manifests. Restate them by reference — name the constraint
("each of these four pins `pmcp-package`, so it must already exist on crates.io or
`cargo publish` fails with a no-matching-package error") and point at the CLAUDE.md ledger
item for the current values, instead of quoting numbers that have no guard and have already
rotted once. Add the explicit D-09 sentence naming all four consumers, so the constraint is
*stated* rather than inferred from the sequence of steps.

Region D — the comment block above the `pmcp-cfn-renderer` publish step (around :459-460),
which quotes a stale pin value for that crate's `pmcp-package` requirement and refers to a
promotion that has long since happened. Apply the same by-reference treatment.

After editing, confirm the machine-checked half is undisturbed: the gate strips full-line
comments before matching and derives its ordinals from the stripped text, so comment edits
must not change its verdict. If they do, that is a real finding — report it rather than
adjusting the comments to satisfy the gate.
  </action>
  <verify>
    <automated>make check-release-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `make check-release-coverage` exits 0
    - `/usr/bin/sed -n '80,110p' .github/workflows/release.yml > /tmp/124-relyml-head.txt` then `/usr/bin/grep -ci 'consumers yet' /tmp/124-relyml-head.txt` returns 0
    - `/usr/bin/grep -c 'pmcp-package = "0.1"' .github/workflows/release.yml` returns 0
    - `/usr/bin/grep -c 'pmcp-cfn-renderer = "0.1"' .github/workflows/release.yml` returns 0, and the same check returns 0 for the `pmcp-agent` and `pmcp-team-servers` zero-point-one spellings
    - `/usr/bin/grep -c 'publishes BEFORE' .github/workflows/release.yml` returns at least 1, on a line naming all four consumers — confirm by `/usr/bin/grep -n 'publishes BEFORE' .github/workflows/release.yml` and reading the line
    - In the head-of-job publish-order list, `pmcp-package` now appears ahead of `cargo-pmcp`: `/usr/bin/grep -n 'pmcp-package' /tmp/124-relyml-head.txt` and `/usr/bin/grep -n 'cargo-pmcp' /tmp/124-relyml-head.txt` are compared and the pmcp-package ordinal is the smaller
    - **Semantic proof that only comments moved** — assert byte equality of every executable line, not merely of publish commands. Before editing, capture `git show HEAD:.github/workflows/release.yml | grep -vE '^[[:space:]]*(#|$)' > /tmp/124-relyml-exec-before.txt`; after editing, capture `grep -vE '^[[:space:]]*(#|$)' .github/workflows/release.yml > /tmp/124-relyml-exec-after.txt`; then `diff /tmp/124-relyml-exec-before.txt /tmp/124-relyml-exec-after.txt` exits 0. Both files must be non-empty and their line count is recorded in the SUMMARY. (The pre-replan criterion greped the diff for `cargo publish` lines only, which proves publish commands were unchanged but says nothing about `env:` values, `if:` conditions or job wiring.)
    - Secondary check, retained: `git diff .github/workflows/release.yml > /tmp/124-relyml-diff.txt` then `/usr/bin/grep -c '^[+-][^+-]*cargo publish' /tmp/124-relyml-diff.txt` returns 0
    - `git diff .github/workflows/release.yml` shows changes only on comment lines and blank lines
  </acceptance_criteria>
  <done>Every order claim and every pin claim in release.yml's comments agrees with the file's own steps and with the manifests, the cluster constraint is stated explicitly, and the publish steps themselves are byte-unchanged.</done>
  <reversibility rating="reversible">Comment-only edits to a workflow file; revertible with no external effect.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Consolidate the ordering constraint in CLAUDE.md and fix the Pre-Flight contradiction</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md lines 250-450 (the Workspace Crates publish-order list: item 9b's CR-01 path-only constraint, item 13's body including the ⚠ ORDERING CONSTRAINT FOR PHASE 124, the nine-emitter paragraph at :332-340, and the Phase-122 registry-oracle note around :371, plus items 13a, 14, 15 and 15a)
    - .planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-08-SUMMARY.md lines 464-476 (the AUTHORITATIVE nine-emitter inventory — row 6 is the openapi-server path-only entry CLAUDE.md's prose omits; read this before editing the count)
    - CLAUDE.md lines 478-530 (the Pre-Flight Checklist, the Version Bump Rules and the Release Steps)
    - crates/pmcp-openapi-server/Cargo.toml lines 112-124 (the in-tree statement of the retained-dev-dep rule, and the path-only pmcp-package entry that is the ninth emitter)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Open Question 4 — the measured contradiction between Pre-Flight step 2 and item 13's own note; and the Project Constraints table)
    - .planning/phases/124-release-publish-order/124-PATTERNS.md (the `CLAUDE.md` section — the dated-correction-note prose style, and why D-09 asks for consolidation rather than another correction note)
    - .planning/phases/124-release-publish-order/124-03-SUMMARY.md (which crates plan 03's checkpoint authorised for a bump — the constraint statement must describe the set, not a snapshot of version numbers that plan 05 is about to move)
    - scripts/release-version-sweep.sh and the `release-sweep` Make target (created in plan 03) — the Pre-Flight replacement points at them
  </read_first>
  <action>
Two edits to CLAUDE.md's Release & Publish Workflow section. Preserve the dense numbering:
`item N` cross-references are load-bearing throughout this file and are documented as such
in the file itself.

Edit 1 — consolidate the cluster ordering constraint. Today it is scattered across correction
notes under items 13, 13a and 15a, each restating a piece of it. Write ONE authoritative
statement — the natural home is item 13's ⚠ ORDERING CONSTRAINT block, which already names
this phase as its owner — saying plainly that `pmcp-package` must publish before
`pmcp-cfn-renderer`, `pmcp-agent`, `pmcp-team-servers` and `cargo-pmcp`, that all four pin it,
that `release.yml` is the authority on that order, and that the order is now machine-checked
by `scripts/check-release-coverage.sh` (so a prose-versus-workflow order bug of the kind
recorded at items 2, 12 and PR #303 is now a build failure rather than a discovery). Then
replace the restatements under items 13a, 14, 15 and 15a with a one-line cross-reference to
that statement.

Do NOT delete the existing dated correction notes that explain why a past entry was wrong.
The house style keeps the wrong text visible with the reason it was wrong, and that history is
why the constraint is trusted. Consolidation here means: one authoritative statement, plus
pointers — not erasure.

Also close out the ⚠ ORDERING CONSTRAINT block's open obligation. It explicitly tasks this
phase with checking that the three consumers publish carrying a `pmcp-package` requirement
compatible with the version being shipped. Record the outcome of that check — measured from
the manifests, and consistent with plan 03's checkpoint decision — inside the consolidated
statement, so the next releaser inherits a discharged obligation rather than an open one.

Edit 2 — fix the Pre-Flight Checklist contradiction. Step 2 currently prescribes a Cargo
registry-search invocation for checking published versions. Item 13's own Phase-122 note
forbids exactly that, because Cargo reports the in-tree path override as though it were
published state — measured on this repo. Replace step 2's command with the crates.io API v1
versions endpoint (noting the mandatory `User-Agent` header, without which the endpoint
returns an empty body and every probe looks like a fetch failure) and a pointer at
`make release-sweep`, which performs the whole three-way comparison for every publishable
crate in one run. Keep the step's intent — know what is published before bumping — and keep
its position in the checklist.

Edit 3 — correct the nine-emitter enumeration in item 13. The paragraph beginning "**Every
in-repo emitter moved in the SAME commit**" at `CLAUDE.md:332` states "Nine were measured" and
then names only eight: the crate's own `[package].version`; four consuming manifests
(`cargo-pmcp:88`, `pmcp-agent:18`, `pmcp-team-servers:24`, `pmcp-cfn-renderer:10`);
`PMCP_PACKAGE_VERSION_REQ`; and two tripwire constants. The ninth, recorded in
`122-08-SUMMARY.md:464-476` as row 6, is **`crates/pmcp-openapi-server/Cargo.toml:124`, marked
UNCHANGED — path-only, CR-01**: the emitter whose correct action is to do NOTHING.

Add it to the enumeration, and say why it is listed despite never changing — because an
emitter whose right move is inaction is precisely the one a reader drops from a list, and
dropping it is how someone later "restores a pin" there and breaks the publish order. That
summary states this outright; the prose here should too. **Do not resolve the discrepancy by
changing "Nine" to "Eight"** — the count is correct and the enumeration is short.

Edit 4 — record the caret exception in the Version Bump Rules. The rule currently reads
flatly: "Downstream crates that pin a bumped dependency must also be bumped." That over-fires
on patch bumps. A `^X.Y.Z` requirement already admits `X.Y.Z+1`, so a patch bump of a
dependency requires no change to crates that pin it. State the exception with the live
instance: `pmcp` moving 2.19.0 -> 2.19.1 in this very release requires no change to
`crates/mcp-tester/Cargo.toml:21` or `cargo-pmcp/Cargo.toml:68`, both of which pin
`pmcp = "2.19.0"`. Keep the existing rule for minor and major bumps, where it is correct and
where item 13's ⚠ ORDERING CONSTRAINT depends on it.

Edit 5 — add the `mcp-tester` inherited-risk note to the Release Steps, labelled as **inherited
release risk rather than part of PKGR-01's completion**. Measured at replan and more precise
than RESEARCH Pitfall 5's version: **six** in-repo crates carry an `mcp-tester` dev-dependency
with BOTH a `path` and a `version` key, and **four of the six publish BEFORE `mcp-tester`
itself** (`mcp-tester` at `release.yml:401`; `pmcp-server-toolkit` :263, `pmcp-sql-server`
:329, `pmcp-openapi-server` :344, `pmcp-workbook-server` :383 ahead of it; `cargo-pmcp` :525
and `pmcp-server` :543 safely after). Cargo retains a dev-dep carrying a version requirement in
the published manifest, so those four must resolve against crates.io at publish time — green
today only because 0.8.0 is already published. A future `mcp-tester` bump must therefore either
leave those four pins alone or resolve the ordering; moving them without doing so fails the
release job at `pmcp-server-toolkit`. Point the note at
`crates/pmcp-openapi-server/Cargo.toml:112-119`, which already states the general rule in-tree.
The note is prose; the ENFORCEMENT is plan 03's checkpoint decision and plan 05's prohibition.
  </action>
  <verify>
    <automated>make check-release-coverage</automated>
  </verify>
  <acceptance_criteria>
    - **Heading-anchored extraction, not fixed line windows** — this task edits the very regions a `sed -n 'A,Bp'` would address, so the windows would drift. Pre-Flight: `/usr/bin/awk '/^### Pre-Flight Checklist/{f=1; next} f && /^### /{exit} f' CLAUDE.md > /tmp/124-preflight.txt`. Release Steps: `/usr/bin/awk '/^### Release Steps/{f=1; next} f && /^### /{exit} f' CLAUDE.md > /tmp/124-relsteps.txt`. Version Bump Rules: `/usr/bin/awk '/^### Version Bump Rules/{f=1; next} f && /^### /{exit} f' CLAUDE.md > /tmp/124-bumprules.txt`. Each extraction is non-empty.
    - `/usr/bin/grep -c 'cargo search' /tmp/124-preflight.txt` returns 0
    - `/usr/bin/grep -c 'crates.io/api/v1/crates' /tmp/124-preflight.txt` returns at least 1 and `/usr/bin/grep -c 'release-sweep' /tmp/124-preflight.txt` returns at least 1
    - `/usr/bin/grep -c 'User-Agent' /tmp/124-preflight.txt` returns at least 1
    - `/usr/bin/grep -c 'cargo search' CLAUDE.md` returns at least 1 — item 13's Phase-122 note, which names the forbidden command in order to forbid it, is preserved
    - **Exactly one authoritative ordering statement.** Choose a distinctive marker phrase for it (for example `AUTHORITATIVE ORDERING CONSTRAINT`) and assert `/usr/bin/grep -c '<marker>' CLAUDE.md` returns exactly 1. Cross-references under items 13a, 14, 15 and 15a must use different wording (for example "see the authoritative statement under item 13") so they cannot inflate that count; assert each of those four items contains a cross-reference: `/usr/bin/grep -c 'see the authoritative statement' CLAUDE.md` returns at least 4.
    - The authoritative block names all four consumers: `/usr/bin/grep -c 'ORDERING CONSTRAINT' CLAUDE.md` returns at least 1, and the block names `pmcp-cfn-renderer`, `pmcp-agent`, `pmcp-team-servers` and `cargo-pmcp` on or near one line
    - `/usr/bin/grep -c 'check-release-coverage' CLAUDE.md` returns at least 1 (the ledger names the mechanism that now enforces the order)
    - **The nine-emitter enumeration lists nine.** `/usr/bin/grep -c 'Nine were measured' CLAUDE.md` returns 1 (the count is NOT reduced), and the enumerating paragraph now names the ninth: `/usr/bin/grep -c 'pmcp-openapi-server/Cargo.toml' CLAUDE.md` returns at least 1 within item 13's emitter paragraph, with `path-only` or `UNCHANGED` on or near that line. Recorded in the SUMMARY as a nine-item list quoted from the edited prose.
    - **The caret exception is stated.** `/usr/bin/grep -ci 'caret\|\^2\.19\.0' /tmp/124-bumprules.txt` returns at least 1, and the SUMMARY quotes the exception sentence and names the two pin sites it applies to.
    - The publish-order item numbers are unchanged: `/usr/bin/grep -c '^13\. ' CLAUDE.md`, `^13a\. `, `^14\. `, `^15\. `, `^15a\. ` and `^9b\. ` each return 1
    - The `mcp-tester` inherited-risk note is present in the Release Steps region and states the six/four split: `/usr/bin/grep -c 'mcp-tester' /tmp/124-relsteps.txt` returns at least 1, and the SUMMARY quotes the note showing it names four before-publish crates out of six pin sites and labels itself inherited risk rather than PKGR-01 completion
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>CLAUDE.md states the cluster ordering constraint exactly once, authoritatively, with the other items pointing at it by a distinct phrase; its nine-emitter paragraph enumerates nine; its Version Bump Rules carry the caret exception; the Pre-Flight Checklist no longer prescribes the oracle the same document forbids; and the ⚠ ORDERING CONSTRAINT block's open obligation is discharged in writing.</done>
  <reversibility rating="reversible">Documentation edits; revertible with no external effect.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose ledger -> human releaser | CLAUDE.md is read as instruction by a future releaser who may not re-derive the facts. A stale claim here is acted on. |
| workflow comments -> maintainer edits | A comment that contradicts the steps below it invites a maintainer to "fix" the steps. That is exactly the class PR #303 corrected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-03 | Tampering (semantic) | publish order as recorded in two ledgers | high | mitigate | The order claim is reconciled toward the workflow in both files AND protected mechanically by plan 01's assertion; pin values move to by-reference form because quoted literals in comments have no guard (Tasks 1 and 2) |
| T-124-13 | Repudiation | CLAUDE.md's internal contradiction on the version oracle | medium | mitigate | Pre-Flight step 2 replaced with the crates.io API plus `make release-sweep`; item 13's prohibition is preserved verbatim so the rule and its justification stay in one file (Task 2) |
| T-124-14 | Tampering | publish steps altered while editing comments | high | mitigate | Explicit diff assertion that no `cargo publish` line was added, removed or altered; the plan's prohibitions forbid step edits outright (Task 1 acceptance criteria) |
</threat_model>

<verification>
- No comment in release.yml claims pmcp-package publishes last or lacks consumers in this repo
- No stale caret-pin literal remains in release.yml's comments
- The cluster constraint is stated explicitly in both ledgers, and in CLAUDE.md exactly once
- CLAUDE.md's nine-emitter paragraph enumerates nine, including the path-only UNCHANGED row
- CLAUDE.md's Version Bump Rules carry the caret exception for patch bumps
- CLAUDE.md's Pre-Flight Checklist and item 13 no longer contradict each other
- Every non-comment, non-blank line of release.yml is byte-unchanged; `make check-release-coverage` still exits 0
</verification>

<success_criteria>
- SC4 is met in the form it asks for: the constraint is stated in the ledger, not left implicit in step ordering, and it is stated in both ledgers
- The next releaser reading either document gets a true statement, and the machine-checked half will catch them if the prose drifts again
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-04-SUMMARY.md` when done
</output>
