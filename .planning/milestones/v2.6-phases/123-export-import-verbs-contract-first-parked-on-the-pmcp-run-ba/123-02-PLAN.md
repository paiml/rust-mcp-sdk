---
phase: 123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba
plan: 02
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - contracts/pmcp-run/portability-v1.graphql
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs
  - cargo-pmcp/tests/package_portability_contract.rs
  - Makefile
autonomous: true
requirements: [PKGX-02]

estimate:
  tokens: 37000
  raw_tokens: 74000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`contracts/pmcp-run/portability-v1.graphql` exists, is itself a valid SDL, and declares `getPackageArtifact` with the three-field return shape handoff §5.1 specifies (D-02)."
    - "The SDL carries `attestation-v1.graphql`'s SDK-PROPOSED provenance header — no `Source:` line, no `Exported:` line — never `capture-v1.graphql`'s genuine-ownership header, so the difference between a platform-exported contract and an SDK-authored proposal is visible at a glance (122 D-07)."
    - "`GET_PACKAGE_ARTIFACT_QUERY` — the exact operation string the shipped client will send — validates against that SDL by an OFFLINE `apollo_compiler` test that needs no network and no backend (SC4)."
    - "The request builder and response decoder are pure and IO-free, live in the dependency-light `graphql_contract.rs` leaf, and depend on nothing heavier than `anyhow`/`serde_json`/`base64` — so the contract test reaches them through the lib target without dragging the bin-only auth/deploy tree in."
    - "The live leg is present, `#[ignore]`d AND env-gated, and PRINTS why it skipped — a silent skip is indistinguishable from a pass. Unparking is deleting a gate, not writing a test (PKGX-F1)."
    - "The four open platform questions (compression, presigned-URL auth, `payloadDigest`'s meaning, the `oci-layout` entry) are written into the SDL's argument comments as explicit questions, so the file records what the SDK is asking for rather than asserting facts about a backend that has not spoken."
    - "The blocking test's module docs state, in their own words, that both halves are SDK-written — so the test pins SDK-INTERNAL agreement today and becomes a cross-boundary drift net only when the platform implements and exports."
    - "The SDL declares no GraphQL `enum`, and every shape assertion reads a comment-stripped SDL body so the header prose about a banned word cannot satisfy the check that bans it."
    - "`test-cargo-pmcp-integration`'s `--test` selector list and `REQUIRED_TEST_BINARIES` both gain `package_portability_contract` in the SAME COMMIT that creates `cargo-pmcp/tests/package_portability_contract.rs` — the discipline the Makefile's own comment block records at `:337-339`. This plan's `make quality-gate` run therefore EXECUTES this plan's own contract test, instead of passing green while the SC4 gate sits outside it."
  prohibitions:
    - statement: "`graphql_contract.rs` must not gain a dependency on `reqwest`, `oauth2`, or `crate::commands::*` — that would drag the bin-only auth/deploy tree into the lib target and break every `tests/` consumer at once."
      status: flagged
      flagged: true
    - statement: "The new SDL must not declare a GraphQL `enum` — an enum makes any later schema-versus-schema diff show permanent drift, which is why both existing vendored SDLs record the ban."
      status: flagged
      flagged: true
    - statement: "The new SDL must not imitate `capture-v1.graphql`'s `Source:`/`Exported:`/OWNERSHIP header — it has no provenance, and imitating one would be a lie about who owns it."
      status: flagged
      flagged: true
    - statement: "No `TODO`/`FIXME`/SATD may express the parked state — parking is `#[allow(dead_code)]` plus `#[ignore = \"...\"]` plus rustdoc, as `VERIFY_ATTESTATION_QUERY` already does (C-3)."
      status: flagged
      flagged: true
    - statement: "The live leg must not run without BOTH its `#[ignore]` being overridden and its env gate being set, and it must never fall through to a silent `return` with no printed reason."
      status: flagged
      flagged: true
  artifacts:
    - contracts/pmcp-run/portability-v1.graphql
    - cargo-pmcp/tests/package_portability_contract.rs
    - "`GET_PACKAGE_ARTIFACT_QUERY` const in `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs`"
    - "`get_package_artifact_request_body` and `decode_get_package_artifact_response` in the same file"
    - "`GetPackageArtifactOutcome` struct in the same file"
    - "`package_portability_contract` appended to BOTH `test-cargo-pmcp-integration` lists in `Makefile`"
  key_links:
    - "`cargo_pmcp::pmcp_run_graphql` (`lib.rs:172-175`) -> `GET_PACKAGE_ARTIFACT_QUERY` -> `tests/package_portability_contract.rs` — the lib seam that lets an integration test validate the real runtime query with no bin-only tree"
    - "`apollo_compiler::Schema::parse_and_validate` -> `Valid<Schema>` -> `ExecutableDocument::parse_and_validate` — the only operation-vs-schema validation apollo-compiler 1.x offers, hence keeping the `Valid` wrapper"
    - "`GRAPHQL_AUTH_HEADER` (`graphql_contract.rs:94`) -> plan 05's transport — one auth shape shared by the shipped client and the parked live leg"
---

<objective>
Land the platform half of PKGX-02 contract-first: a vendored SDL for `getPackageArtifact`, the exact
operation string the client will send, its pure request/response codec, and an offline blocking test
that validates one against the other with no backend in existence (D-02, SC4).

This is the FOURTH run of a shape this repo has already executed three times — `capture-v1` (platform-
exported), `attestation-v1` (SDK-proposed), and their two `apollo_compiler` blocking tests. It is
composition, not invention, and it is why the phase can close offline.

**Wave placement, and why it moved (review finding M1 + the wave rule).** This plan has NO content
dependency on plan 01 — it neither reads nor extends anything plan 01 produces, and it could be
executed first. It sits in Wave 2 behind `depends_on: ["123-01"]` for one reason, recorded here so
nobody "optimizes" it back: adopting M1 means each creator plan registers its own test binary in the
`Makefile`, and plans 01 and 02 would then both append to the same two lines of
`test-cargo-pmcp-integration` in the same wave. GSD's wave rule is that same-wave plans must have zero
`files_modified` overlap, and a shared `Makefile` region is exactly the overlap that rule exists for.
So the dependency is ORDERING-ONLY, not content — the same "implicit dependency: `files_modified`
overlap forces a later wave" rule the planner applies everywhere else.

Plan 01 owns the local file round trip; this plan owns the remote contract. Plan 05 joins them by
wiring `pull`.

<review_feedback_incorporated>
Cross-AI review (`123-REVIEWS.md`, 2026-08-26). Codex's HIGH findings all landed on plans 01, 03 and
05; none of them touch this plan's artifacts, and its `sdl_body()` comment-stripping discipline was
called out as a strength by both reviewers and is preserved unchanged. Two changes land here:

| Finding | Severity | Where it now lives in this plan |
|---------|----------|----------------------------------|
| **M1** — deferring Makefile registration to plan 07 left Waves 1-4 falsely green | MEDIUM | Task 3 now registers `package_portability_contract` in BOTH `test-cargo-pmcp-integration` lists in the same commit that creates the binary. `Makefile` added to `files_modified`; wave moved to 2 to keep the same-wave overlap rule (see above). |
| **M5** — `depends_on` metadata under-specified across the plan set | MEDIUM | `depends_on: ["123-01"]` added and its ORDERING-ONLY nature stated explicitly above, so a later reader does not mistake it for a content dependency. |

Nothing from this review is deferred or rejected in this plan.
</review_feedback_incorporated>

Purpose: when the backend ships `getPackageArtifact`, unparking must be DELETING A GATE, not writing a
client and a schema from scratch — the discipline Phase 122 established and the reason D-04 rejects a
contract-plus-error stub.
Output: `contracts/pmcp-run/portability-v1.graphql`, a fourth operation constant plus codec in the
dependency-light contract leaf, and `cargo-pmcp/tests/package_portability_contract.rs` carrying both
the offline blocking tests and the parked live leg.

`export` is retired (D-01) and appears in no artifact here; the shipped `import` verb and its
`submitImport`/`getImportStatus` operations are untouched (D-03). The one new operation is `pull`'s.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-CONTEXT.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-RESEARCH.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-PATTERNS.md
@CLAUDE.md
</context>

<!-- planner-discipline-allow: Source: -->
<!-- planner-discipline-allow: Exported: -->
<!-- planner-discipline-allow: enum -->
<!-- planner-discipline-allow: reqwest -->
<!-- planner-discipline-allow: oauth2 -->
<!-- planner-discipline-allow: crate::commands -->

> **Why the allowances above.** This plan's acceptance criteria negative-grep for `Source:`,
> `Exported:` and `enum`, and those same literals must appear in the task actions because the actions
> are instructing the executor about exactly which words the artifact must not carry. The existing
> sibling test solved this problem structurally rather than by avoidance: `sdl_body()` strips every
> `#` comment before any shape assertion runs, precisely because *"a naive containment check would be
> satisfied by the very comment explaining the ban — a self-invalidating check"*
> (`package_attestation_contract.rs:65-82`). Every shape assertion in Task 3 runs over the stripped
> body for that reason; the provenance assertions deliberately run over the RAW file, because the
> provenance header IS comments and asserting its absence is the whole point. `reqwest`/`oauth2`/
> `crate::commands` are allowed for the same reason: the dependency-discipline instruction has to name
> the imports it forbids, and the check is line-anchored on `^use`.

<tasks>

<task type="auto">
  <name>Task 1: Vendor `portability-v1.graphql` with an honest SDK-PROPOSED header and the open questions in-line</name>
  <files>contracts/pmcp-run/portability-v1.graphql</files>
  <read_first>
    - `contracts/pmcp-run/attestation-v1.graphql` — the WHOLE file. Its header is the one to imitate: the STATUS line, THE ASK, the "no `Source:` and no `Exported:` line, because it has no provenance" sentence, the OWNERSHIP contrast paragraph, the WHAT THE BLOCKING TEST CAN AND CANNOT PROVE block, and the SCOPE section that records omissions as DECISIONS. Note also how it writes operation semantics into ARGUMENT comments
    - `contracts/pmcp-run/capture-v1.graphql` — the genuine `Source:` + `Exported:` + OWNERSHIP header, read ONLY so the difference is deliberate rather than accidental
    - `docs/design/package-portability-pmcp-run-handoff.md` §5.1 — the `getPackageArtifact` shape, the presigned-URL and audit semantics, and `pull`'s SDK acceptance line verbatim
    - `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-RESEARCH.md` § *Assumptions Log* rows A2, A3, A4, A6 — the four questions this file must carry
    - `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-CONTEXT.md` § *Claude's Discretion* first bullet — the provenance-header instruction
  </read_first>
  <action>
Create `contracts/pmcp-run/portability-v1.graphql` as an SDK-authored, not-yet-ratified SDL subset
covering exactly ONE operation (D-02): `getPackageArtifact`.

**Header — imitate `attestation-v1.graphql`, never `capture-v1.graphql`.** It must carry a
`STATUS: SDK-PROPOSED. NOT PLATFORM-EXPORTED. AWAITING RATIFICATION.` line; a THE ASK line pointing at
`docs/design/package-portability-pmcp-run-handoff.md` §5.1; the explicit statement that it deliberately
carries no provenance lines because it has none, and that imitating one would be a lie about who owns
it; and an OWNERSHIP paragraph contrasting its sibling exported from the live AppSync API on
2026-07-20, whose CONTENTS the platform owns.

Add one thing `attestation-v1.graphql` could not have said, because the platform said it after that
file was written: per their 2026-08-26 correction, a real cross-boundary drift net arrives with
IMPLEMENTATION, not with ratification — so the window in which this file pins only SDK-internal
agreement is LONGER than `capture-v1` would suggest. Write that into the WHAT THE BLOCKING TEST CAN AND
CANNOT PROVE block.

**Schema body.** A `type Query` declaring `getPackageArtifact(reference: String!)` returning a
`GetPackageArtifactReturnType` with `payloadDigest: String!`, `downloadUrl: String!` and
`expiresAt: String!`. Declare NO GraphQL `enum` anywhere and record the ban's reason in a comment the
way both siblings do: an enum makes any later schema-versus-schema diff show permanent drift.

**The four open questions go in ARGUMENT and FIELD comments, phrased as questions, not as facts.**
This file is where the SDK records what it is asking for, and the platform has already corrected one
digest-naming conflation in this exact family (`subjectPayloadDigest` became `subjectManifestDigest`,
2026-08-26), so a guessed answer costs a round trip:

- On `reference`: what the accepted reference grammar is, and whether it is the `name@version` form
  the CLI's own reference splitter produces.
- On `payloadDigest`: whether it is the OCI MANIFEST digest — the value `unpack_*` independently
  re-derives from the layout — or a digest over the transported tar bytes. State plainly that the SDK
  currently assumes the former and that getting it backwards makes the client compare two unrelated
  values. (This is also the question whose answer decides whether the tar acquires an identity of its
  own; see plan 01's assumption-delta record.)
- On `downloadUrl`: whether it is a presigned URL to be fetched with a PLAIN UNAUTHENTICATED GET, and
  confirmation that the pmcp.run `Authorization` token must NOT be attached to it — sending it would
  leak a credential to a different origin, and §5.1 already calls the URL a bearer token in its own
  right.
- On the artifact bytes themselves: whether the tar is UNCOMPRESSED (§5.1 names no compression), and
  whether the platform's reader tolerates an `oci-layout` marker entry alongside `index.json` and
  `blobs/`. Note that the SDK emits the marker and accepts-but-ignores it on read.

**SCOPE section.** State that this file covers `getPackageArtifact` ONLY, and that the omissions are
DECISIONS: there is no upload/`push` operation because `export`/`push` were retired (D-01 — `capture`
already produces packages platform-side), and there is no `import` operation here because `import` is
the platform's and is already contracted elsewhere (D-03).
  </action>
  <acceptance_criteria>
    - `contracts/pmcp-run/portability-v1.graphql` exists and parses as a valid SDL (proven by Task 3's schema fixture, which panics on an invalid vendored SDL).
    - The file contains the exact string `SDK-PROPOSED` and contains neither a line beginning `# Source:` nor a line beginning `# Exported:` — asserted in Task 3, not by eye.
    - The SDL body, with `#` comments stripped, declares `getPackageArtifact`, `reference`, `payloadDigest`, `downloadUrl` and `expiresAt`, and contains no `enum` keyword.
    - Each of the four open questions (reference grammar, `payloadDigest` meaning, presigned-URL auth, compression plus the layout-marker entry) appears as a comment attached to the field or argument it concerns, phrased as a question to the platform.
    - The SCOPE section names both deliberate omissions and cites D-01 and D-03 as their reasons.
  </acceptance_criteria>
  <verify>
    <automated>test -f contracts/pmcp-run/portability-v1.graphql &amp;&amp; grep -q 'SDK-PROPOSED' contracts/pmcp-run/portability-v1.graphql &amp;&amp; ! grep -qE '^#\s*(Source|Exported):' contracts/pmcp-run/portability-v1.graphql</automated>
  </verify>
  <done>A third vendored SDL sits beside its two siblings, its header states honestly that it has no provenance, and the four things the SDK does not know about the backend are written down as questions rather than guessed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The fourth operation — `GET_PACKAGE_ARTIFACT_QUERY` plus a pure, IO-free codec</name>
  <files>cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs</files>
  <read_first>
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` — the WHOLE file: the module header's DEPENDENCY DISCIPLINE paragraph (`:28-32`), `SUBMIT_PACKAGE_CAPTURE_QUERY` (`:42`), `GET_PACKAGE_CAPTURE_STATUS_QUERY` (`:64`), `GRAPHQL_AUTH_HEADER` and its raw-token rustdoc (`:82-94`), the parked-const pattern with its "do not hunt for a production call site" rustdoc (`:125-148`), `VERIFY_ATTESTATION_QUERY` (`:134`), the all-`String` no-enum outcome struct (`:150-167`), the refuse-empty-input request builder (`:194-227`), and the response decoder beside it
    - `cargo-pmcp/src/lib.rs:165-176` — the `#[path]` mount that makes this file reachable from `tests/`
    - `contracts/pmcp-run/portability-v1.graphql` — as Task 1 wrote it, so the operation string matches the schema it will be validated against
  </read_first>
  <behavior>
    - `get_package_artifact_request_body` on an empty reference returns `Err`, naming that it refuses to build a request for an empty reference.
    - On a non-empty reference it returns a JSON object with exactly a `query` key holding the operation string and a `variables` key holding the reference.
    - `decode_get_package_artifact_response` on a well-formed response yields a `GetPackageArtifactOutcome` carrying all three string fields.
    - On a response whose `data.getPackageArtifact` is null it returns `Err`, distinguishably from a malformed-shape error.
    - On a response carrying a GraphQL `errors` array it returns `Err` surfacing the platform's message rather than a generic parse failure.
    - On a response missing any one of the three required fields it returns `Err` naming the missing field.
  </behavior>
  <action>
Add the fourth instance of this file's established shape. Four items, all `#[allow(dead_code)]` with
the parked rustdoc, all IO-free, all reachable from the lib target.

**The operation constant.** `GET_PACKAGE_ARTIFACT_QUERY` — a `query GetPackageArtifact($reference:
String!)` selecting `payloadDigest`, `downloadUrl` and `expiresAt` from `getPackageArtifact(reference:
$reference)`. This is the exact string the shipped client will send, and Task 3 validates THIS constant
(not a copy of it) against the vendored SDL, so the test cannot drift from the client.

Its rustdoc must copy `VERIFY_ATTESTATION_QUERY`'s parking language in substance: this operation is not
sent by any shipped code path yet; its only caller until the backend exists is plan 05's transport seam
and the parked live leg; do not hunt for a production call site and do not delete it as dead code,
because removing it deletes the SDK's half of a contract it is asking the platform to ratify. Express
the parking with `#[allow(dead_code)]` and rustdoc — never with a self-admitted-technical-debt comment
(C-3).

**The outcome type.** `GetPackageArtifactOutcome { payload_digest: String, download_url: String,
expires_at: String }` — every field a `String`, mirroring the SDL's no-enum discipline for the same
reason. Its rustdoc must say that `download_url` is a BEARER CREDENTIAL in its own right (§5.1) and
therefore must never be logged, persisted, or sent with the pmcp.run `Authorization` header attached;
that instruction belongs on the type so it travels with the value into plan 05.

**The request builder.** `get_package_artifact_request_body(reference: &str) -> Result<Value>`,
following the refuse-empty-input shape at `:194-227`: bail with a message naming the refusal before
building anything, then assemble a `serde_json::Map` with a `query` key holding the operation constant
and a `variables` key holding the reference. Never interpolate the reference into the query string.

**The response decoder.** `decode_get_package_artifact_response(response: &Value) ->
Result<GetPackageArtifactOutcome>` with the four distinct failure modes from `behavior`: a GraphQL
`errors` array surfaces the platform's message; a null payload is its own error; a missing field names
which field; a wrong-typed field names it too. Four causes, four messages — a decoder that collapses
them makes every backend integration failure look identical.

Add unit tests under `#[cfg(test)]` in this file covering every `behavior` row, so they run under
`cargo test -p cargo-pmcp --lib` alongside the existing contract-leaf tests.

Do NOT add `reqwest`, `oauth2` or any `crate::commands::*` import here. The transport lives in
`graphql.rs` and plan 05 wires it; this leaf's whole reason for existing is that it stays light enough
for `tests/` to reach through the lib target. Restate that boundary in the item rustdocs so a future
reader does not "simplify" by moving the HTTP call in.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --lib` exits 0 with a nonzero passed count and its output names the new decoder/builder tests.
    - `cargo build -p cargo-pmcp --lib` exits 0, proving the leaf still compiles without the bin-only tree.
    - `grep -nE '^use (reqwest|oauth2)|^use crate::commands' cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` returns no matches.
    - `grep -c 'GET_PACKAGE_ARTIFACT_QUERY' cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` returns a value of at least 2 (the declaration plus its use in the builder).
    - Six named unit tests exist, one per `behavior` row, and each asserts on the ERROR MESSAGE for its failure mode rather than only on `is_err()`.
    - `grep -n 'allow(dead_code)' cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` shows the attribute on each of the four new items.
    - No self-admitted-technical-debt marker appears in the file — asserted by a comment-stripped scan in Task 3.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS= cargo test -p cargo-pmcp --lib > /tmp/123-02-t2.log 2>&amp;1 &amp;&amp; test "$(awk '/^test result:/ { t += $4 } END { print t+0 }' /tmp/123-02-t2.log)" -gt 0 &amp;&amp; cargo build -p cargo-pmcp --lib</automated>
  </verify>
  <done>The exact operation string the client will send exists as a constant, its pure request/response codec has six named error paths each with its own message, and the dependency-light leaf still compiles in the lib target alone.</done>
</task>

<task type="auto">
  <name>Task 3: The offline blocking contract test and its parked, self-announcing live leg</name>
  <files>cargo-pmcp/tests/package_portability_contract.rs, Makefile</files>
  <read_first>
    - `cargo-pmcp/tests/package_attestation_contract.rs` — the WHOLE file, to copy structurally: the module-doc candour block (`:24-39`), the gate note (`:13-21`), the imports and `SDL_PATH` constant (`:41-53`), the `Valid<Schema>` fixture with its "keep the wrapper" comment (`:56-63`), the comment-stripping `sdl_body()` helper and the self-invalidating-check argument (`:65-82`), the operation-validation test (`:85-98`), and the selection-set drift check
    - `crates/pmcp-openapi-server/tests/parity_replay.rs:307-346` — the double-gate live leg: `#[ignore = "..."]` plus an env check that PRINTS why it skipped, plus the runnable command in its rustdoc
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` — as Task 2 left it, so the test imports the real constant
    - `contracts/pmcp-run/portability-v1.graphql` — as Task 1 left it
    - `Makefile` lines 315-412 — the whole `test-cargo-pmcp-integration` block, and specifically the APPEND-ONLY paragraph at `:337-339`, which records that Phase 122's `package_attestation_contract` was added to BOTH lists *"in the same commit that created"* its test file. The hazard is a name added BEFORE its binary exists, not a name added WITH it — this task follows the same discipline for its own binary
  </read_first>
  <action>
Create `cargo-pmcp/tests/package_portability_contract.rs` as the third sibling of the two shipped
offline blocking contract tests, structurally copied rather than reinvented.

**Constants and fixtures.** An `SDL_PATH` built with `concat!(env!("CARGO_MANIFEST_DIR"),
"/../contracts/pmcp-run/portability-v1.graphql")`. An `EXPECTED_RESPONSE_FIELDS` array holding the
three fields the SDK reads back, kept in one place so the validation test, the drift check and the live
leg cannot disagree about them. A `schema()` fixture returning `Valid<Schema>` from
`Schema::parse_and_validate` — keep the `Valid` wrapper rather than unwrapping it, because
apollo-compiler 1.x only offers operation-vs-schema validation against an already-valid schema, and say
so in a comment as the sibling does.

**The comment-stripping helper is load-bearing — do not skip it.** Copy `sdl_body()`: it removes
everything from each line's first `#` onward before any shape assertion runs. The header prose discusses
the very words the schema bans, so a naive containment check would be satisfied by the ban's own
explanation — a self-invalidating check. Reproduce that reasoning in the helper's own doc comment.

**Four offline tests, each blocking:**
1. The operation validates: run `ExecutableDocument::parse_and_validate` over the imported
   `GET_PACKAGE_ARTIFACT_QUERY` against `schema()`, panicking with a message naming the SDL file on
   failure. This is SC4's core assertion, and it validates the CLIENT'S constant, not a copy.
2. Provenance: the SDL contains the SDK-PROPOSED status marker and carries neither a `Source:` nor an
   `Exported:` header line — the machine check behind Task 1's honesty requirement.
3. Shape: over `sdl_body()`, assert the three response fields are declared, that `reference` is a
   non-null `String` argument, and that the word `enum` does not appear.
4. Selection-set drift: assert the operation string selects exactly `EXPECTED_RESPONSE_FIELDS` — no
   more, no fewer — so a field silently added to the query without being added to the SDL fails here
   rather than at runtime against a backend nobody can run.

Also assert, in one of these, that `graphql_contract.rs` carries no self-admitted-technical-debt marker
in its non-comment-stripped body for the new items' region — a comment-stripped scan, since the plan's
own prose about the ban must not satisfy the ban.

**Module docs — state what this cannot prove, in its own words.** Both halves are SDK-written: the
schema and the query. So today this test pins SDK-INTERNAL agreement, not platform agreement, and it
cannot detect drift from a platform that has not spoken. Add the platform's 2026-08-26 correction: the
real cross-boundary drift net arrives with IMPLEMENTATION, not with ratification — so this window is
longer than `capture-v1` would suggest. Also record, as the sibling does, that the Makefile gate asserts
a NONZERO passed count for this binary BY NAME, which is what makes the word "blocking" a measured
property rather than a claim — and note that plan 07 is what performs that append.

**The live leg — parked, double-gated, and it announces its own skip.** One `#[tokio::test]` marked
`#[ignore = "..."]` whose reason string names the missing platform capability and the env var that
enables it. Inside, check the env gate FIRST and, when unset, `eprintln!` a line saying which variable
to set and return — a silent skip is indistinguishable from a pass, and that `eprintln!` is the part
people drop. Then check for credentials and skip with its own printed reason if absent.

Its body must already NAME what the backend must ship, so unparking is deleting the two gate lines
rather than writing a test: build the request with the real `get_package_artifact_request_body`, note in
comments where plan 05's transport will send it, and assert on the decoded outcome via the real
`decode_get_package_artifact_response`. Its rustdoc carries the exact runnable command, in the shape
`parity_replay.rs` uses.

**Register this binary in the gate NOW, in this same commit (review finding M1).** Append
`package_portability_contract` to BOTH `test-cargo-pmcp-integration`'s `--test` selector list AND its
`REQUIRED_TEST_BINARIES` string, in the same position in each.

The reviewed draft deferred all four appends to plan 07, reading `REQUIRED_TEST_BINARIES`' append-only
rule as forbidding an early append. The Makefile's own comment at `:337-339` says otherwise: Phase
122's `package_attestation_contract` *"was added to BOTH lists below in the same commit that created
`cargo-pmcp/tests/package_attestation_contract.rs`."* The rule's actual hazard is a name that lands
BEFORE its binary, which same-commit registration cannot produce. Deferring instead meant this plan's
own `make quality-gate` run would never execute the SC4 contract test it exists to create.

Note the ignored-test hazard while you are there and check it holds: this binary carries four
non-ignored offline tests plus one `#[ignore]`d live leg. The four non-ignored tests are what keep its
passed count nonzero; the Makefile's `0)` arm fires by design if a future change parks the last
non-ignored test. Do not relax the guard — unpark or replace the test. (The Makefile comment already
records this hazard for `package_attestation_contract`; do NOT restate it, plan 07 owns the
consolidated phase paragraph. Add one line to each list and nothing else.)

Do NOT touch the `RUSTFLAGS=` assignment on that target and do not reflow the surrounding comments.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test package_portability_contract -- --test-threads=1` exits 0 and reports a passed count of at least 4 with 1 ignored.
    - The validation test imports `cargo_pmcp::pmcp_run_graphql::GET_PACKAGE_ARTIFACT_QUERY` — the production constant — rather than declaring its own copy of the query string.
    - Negative control, run and recorded in the SUMMARY: add a field to the operation's selection set that the SDL does not declare, observe test 1 AND test 4 go red, revert, observe green. This proves the offline gate is load-bearing rather than decorative.
    - Negative control 2, run and recorded: delete the SDK-PROPOSED marker line from the SDL, observe test 2 go red, restore it.
    - The live-leg test is `#[ignore]`d, and running it with `--ignored` and the env var UNSET prints a line naming the variable and exits without failing — captured in the SUMMARY as literal output.
    - The module docs contain a paragraph stating that both halves are SDK-written and that the test cannot detect drift from a platform that has not spoken.
    - `grep -c 'eprintln' cargo-pmcp/tests/package_portability_contract.rs` returns at least 2 (one printed reason per gate).
    - **M1.** `grep -c 'package_portability_contract' Makefile` returns 2 (one occurrence per list), and `make test-cargo-pmcp-integration` exits 0 printing `✓ package_portability_contract passed <n> tests` with `n >= 4`.
    - **M1.** `git diff -- Makefile` shows exactly two changed lines and leaves the `RUSTFLAGS=` assignment untouched — asserted by a diff excerpt in the SUMMARY.
    - **M1 negative control, run and recorded in the SUMMARY:** remove `package_portability_contract` from the `--test` selector list while leaving it in `REQUIRED_TEST_BINARIES`; observe the `-1` "never RAN" verdict naming it, and a non-zero exit. Restore, observe green. This proves the append is load-bearing rather than decorative, in the same commit that makes it.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS= cargo test -p cargo-pmcp --test package_portability_contract -- --test-threads=1 > /tmp/123-02-t3.log 2>&amp;1 &amp;&amp; test "$(awk '/^test result:/ { t += $4 } END { print t+0 }' /tmp/123-02-t3.log)" -ge 4 &amp;&amp; test "$(grep -c 'package_portability_contract' Makefile)" -eq 2 &amp;&amp; make test-cargo-pmcp-integration > /tmp/123-02-t3-gate.log 2>&amp;1 &amp;&amp; grep -qE '. package_portability_contract passed [1-9][0-9]* tests' /tmp/123-02-t3-gate.log</automated>
  </verify>
  <done>A fourth offline blocking contract test validates the real client query against the vendored SDL with no network, states honestly what it cannot prove, and carries a parked live leg that announces its own skip so unparking is deleting two lines.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by THIS PLAN (new to the codebase — not drift):

- `contracts/pmcp-run/portability-v1.graphql` (new file), declaring `getPackageArtifact`, `GetPackageArtifactReturnType`, and the fields `payloadDigest` / `downloadUrl` / `expiresAt`
- `GET_PACKAGE_ARTIFACT_QUERY` (new const in `graphql_contract.rs`)
- `GetPackageArtifactOutcome` (new struct) with fields `payload_digest`, `download_url`, `expires_at`
- `get_package_artifact_request_body` (new function)
- `decode_get_package_artifact_response` (new function)
- `cargo-pmcp/tests/package_portability_contract.rs` (new test binary), with `SDL_PATH`, `EXPECTED_RESPONSE_FIELDS`, `schema()`, `sdl_body()` and the parked live-leg test
- `package_portability_contract` appended to `test-cargo-pmcp-integration`'s `--test` selector list and to `REQUIRED_TEST_BINARIES` in `Makefile`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pmcp.run GraphQL response -> `decode_get_package_artifact_response` | Untrusted remote JSON; every field is validated rather than assumed present and well-typed. |
| `GetPackageArtifactOutcome.download_url` -> anything that renders, logs or persists it | The presigned URL is itself a bearer credential (§5.1); crossing this boundary is a disclosure event. |
| vendored SDL -> the blocking test | SDK-authored on both sides today; the test's own docs record that this is SDK-internal agreement, not platform agreement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-11 | Information Disclosure | `GetPackageArtifactOutcome.download_url` | high | mitigate | The type's rustdoc states the URL is a bearer credential that must never be logged, persisted, or sent with the pmcp.run `Authorization` header; plan 05 enforces it at the call site and the SDL records the question to the platform (A3). |
| T-123-12 | Spoofing | a hostile or misconfigured GraphQL endpoint returning a crafted response | medium | mitigate | The decoder validates all three fields and surfaces a GraphQL `errors` array distinctly. Trust does not rest here: the artifact's own digests are re-verified locally by plan 01's reader (§5.1 "transport is never trusted"). |
| T-123-13 | Tampering | the query drifting from the vendored SDL without anyone noticing | medium | mitigate | The blocking test validates the PRODUCTION constant against the SDL and pins the selection set exactly; both are proven load-bearing by recorded negative controls. |
| T-123-14 | Repudiation | a contract file that looks platform-exported but is not | medium | mitigate | The SDK-PROPOSED header is machine-asserted, and the absence of `Source:`/`Exported:` lines is asserted too, so provenance cannot be quietly implied. |
| T-123-15 | Denial of Service | a live leg accidentally reaching a real backend from CI | low | mitigate | Double gate: `#[ignore]` plus an explicit env check that prints why it skipped, mirroring the pattern proven across two test files in this repo. |
</threat_model>

<verification>
- `RUSTFLAGS= cargo test -p cargo-pmcp --lib` — the codec's unit tests, nonzero passed count.
- `RUSTFLAGS= cargo test -p cargo-pmcp --test package_portability_contract -- --test-threads=1` — at least 4 passed, 1 ignored.
- `cargo build -p cargo-pmcp --lib` — the contract leaf still compiles without the bin-only tree.
- `make test-cargo-pmcp-integration` — `package_portability_contract` reported by name with a passed count of at least 4, proving this plan's own contract test is inside the gate from this plan's own commit (M1).
- Three recorded negative controls (selection-set drift; deleted provenance marker; the Makefile selector-list removal) proving both the offline gate and its wiring are load-bearing.
- One recorded live-leg skip transcript proving the skip announces itself.
- `RUSTFLAGS="" make quality-gate` exits 0.
</verification>

<success_criteria>
- A third vendored SDL exists under `contracts/pmcp-run/` with an honest SDK-PROPOSED header and the four open questions written in-line.
- The exact operation string the client will send validates against it offline, with no backend.
- The pure codec has six named failure paths and lives in the dependency-light leaf.
- The live leg is present, double-gated, and prints why it skipped.
- `RUSTFLAGS="" make quality-gate` exits 0.
</success_criteria>

<output>
Create `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-02-SUMMARY.md` when done.
</output>
