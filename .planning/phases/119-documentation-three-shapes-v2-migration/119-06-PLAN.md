---
phase: 119-documentation-three-shapes-v2-migration
plan: 06
type: execute
wave: 4
depends_on: ["119-02", "119-04"]
files_modified:
  - pmcp-course/src/part8-advanced/ch24-agents-and-teams.md
  - pmcp-course/src/part8-advanced/ch24-exercises.md
  - pmcp-course/src/SUMMARY.md
  - pmcp-course/src/quizzes/ch24-agents-and-teams.toml
autonomous: true
requirements: [DOCS-04]

estimate:
  tokens: 75000
  raw_tokens: 150000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-09: pmcp-course gains ONE new Part VIII chapter plus exercises for Agents & Teams, matching the `ch23-skills.md` depth and shape."
    - "D-09: the v2 migration gets NO course chapter — DOCS-04 names README/course, DOCS-05 does not mention the course, and that distinction is honoured literally."
    - "DOCS-04: the third shape of the three-shapes rule is delivered — README (119-02/119-09), pmcp-book (119-02/119-07), pmcp-course (this plan)."
    - "D-10: every example the course cites is named by its FULL runnable cargo invocation."
    - "The course chapter file EXISTS and is reachable — verified by an explicit file-existence assertion, because `pmcp-course/book.toml:11` sets `create-missing = true` and its build will NOT fail on a missing target; it CREATES the missing file and exits 0."
    - "Exercise 2 and Exercise 3 name the same success banners `tests/docs04_examples_run.rs` asserts — which is why this plan runs AFTER 119-04, the plan that writes the `doc_review_team` leg, rather than beside it."
  artifacts:
    - "pmcp-course/src/part8-advanced/ch24-agents-and-teams.md — new, ~440 lines"
    - "pmcp-course/src/part8-advanced/ch24-exercises.md — new, ~220 lines"
    - "pmcp-course/src/SUMMARY.md — one new Part VIII group (chapter + exercises child)"
  key_links:
    - "course SUMMARY entry ↔ chapter file: unlike the book, a missing course file does NOT fail the build — `create-missing = true` makes mdbook WRITE the file and exit 0, so a `test -f` assertion is the only real gate"
    - "each exercise's `### Verify your solution` predicate ↔ the measured stdout of the example it walks: the predicates come from real output, not from imagination"
  prohibitions:
    - "No v2 migration course chapter is written — D-09 scopes the migration to book-and-README only."
    - "No CLI verb is named beyond `cargo pmcp agent new`, `cargo pmcp agent dev` and `cargo pmcp team dev`."
    - "No new example source code is written (D-12) — the exercises direct the reader to existing examples."
    - "`pmcp-course/src/theme/exercises.css` and `exercises.js` are NOT committed as modified — the `mdbook-exercises` preprocessor rewrites them on every build."
    - "No `{{#quiz ...}}` include is added without the matching TOML file also being added — a dangling include is a build risk where an omitted include is not."
---

<objective>
Deliver the third shape. One new pmcp-course Part VIII chapter plus its exercises file, teaching
Agents & Teams at the depth `ch23-skills.md` set, leading with the `cargo pmcp` workflow and
walking the examples the book chapters cite.

Purpose: DOCS-04 names three shapes — pmcp-book chapters, runnable examples, README/course. The
book and README halves land in plans 119-02, 119-07 and 119-09; the examples are proven in 119-02
and 119-04. This plan is the course half, and without it DOCS-04 is two-thirds delivered.
Output: `ch24-agents-and-teams.md`, `ch24-exercises.md`, and their Part VIII nav group.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-CONTEXT.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-PATTERNS.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md
</context>

<artifacts_this_plan_produces>
## Artifacts this plan produces

- **New file** `pmcp-course/src/part8-advanced/ch24-agents-and-teams.md`
- **New file** `pmcp-course/src/part8-advanced/ch24-exercises.md`
- **New SUMMARY group** `- [Agents & Teams: Deploy-Anywhere Agent Loops](./part8-advanced/ch24-agents-and-teams.md)` with child `  - [Chapter 24 Exercises](./part8-advanced/ch24-exercises.md)`
- **Optional, decided in Task 3** `pmcp-course/src/quizzes/ch24-agents-and-teams.toml`

Exclude all of the above from drift verification.
</artifacts_this_plan_produces>

<review_dispositions>
## Cross-AI review dispositions (`119-REVIEWS.md`)

| Finding | Severity | Disposition |
|---|---|---|
| This plan consumes 119-04's output but ran in the SAME wave — a real race with no barrier. Task 2 reads `tests/docs04_examples_run.rs` "as written by plans 119-02 and 119-04", and its acceptance criteria require the Exercise 2/3 pass predicates to name the banners that test asserts for `s50_standalone_vs_sampled` and `doc_review_team`. `doc_review_team_runs_to_completion` is created by 119-04, which was also `wave: 3`, while this plan declared `depends_on: ["119-02"]` only | HIGH | **INCORPORATED.** `depends_on` now names `119-04`, and the wave moves from 3 to 4 to match — a plan's wave must exceed every dependency's, or `/gsd-execute-phase` would launch both in the same parallel batch and the declared dependency would be decorative. Wave 3 stays parallel for 119-03/04/05; wave 4 stays conflict-free because this plan touches only `pmcp-course/`, which 119-07 (book), 119-08 (book) and 119-09 (README/CHANGELOG) never touch. The ROADMAP Phase-119 wave listing is updated to match. |
| Both this plan and 119-02 instructed the executor to "confirm" that `pmcp-course/book.toml` carries NO `create-missing` key | MEDIUM | **INCORPORATED.** The file sets `create-missing = true` at `pmcp-course/book.toml:11` (measured). Task 3's `read_first`, `action` and the threat register now state the measured value and the real mechanism — mdbook CREATES the missing file under `pmcp-course/src/` and exits 0. The conclusion the plan drew from it is unchanged and still correct: the build cannot verify this plan's nav wiring, so `test -f` is the only real gate. |
</review_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Write the course chapter at ch23-skills depth</name>
  <files>pmcp-course/src/part8-advanced/ch24-agents-and-teams.md</files>
  <read_first>
    - `pmcp-course/src/part8-advanced/ch23-skills.md` in full (443 lines) — the depth template. Note its exact spine: H1, a one-paragraph thesis closing with a pointer to the example it walks and to its exercises file and quiz; `## Learning Objectives` (7 bullets, all opening "By the end of this chapter, you will be able to"); `## Why <Feature> Matters for Enterprise MCP`; `## The Dual-Surface Invariant` (the ONE load-bearing design property); `## Tier 1` / `## Tier 2` / `## Tier 3`; `## Cross-SDK Compatibility`; `## Chapter Contents`; `## Knowledge Check`; the quiz include; and the closing rule plus forward pointer with a `./`-prefixed link
    - `pmcp-course/src/part8-advanced/ch22-code-mode.md` (419 lines) — a second depth calibration point
    - `pmcp-book/src/ch12-15-agents-as-mcp-clients.md` as written by plan 119-02 — the book chapter this course chapter complements. The course teaches; the book references. Do not duplicate the book's `## Two Shapes` table verbatim.
    - `crates/pmcp-agent/examples/s50_standalone_vs_sampled.rs` (380 lines) and `crates/pmcp-team-servers/examples/doc_review_team.rs` (406 lines) — the two examples the tiers walk, and the banners they print
    - `examples/s49_sampling_host.rs` (225 lines) — the sampling-host example the Tier 2 material references
    - `cargo-pmcp/src/commands/agent/mod.rs:20-25` and `cargo-pmcp/src/commands/team/mod.rs:17-20` — the complete verb set
  </read_first>
  <action>
Create `pmcp-course/src/part8-advanced/ch24-agents-and-teams.md`, target ~430–460 lines, matching
`ch23-skills.md`'s structure exactly. The course has five conventions the book does NOT have;
copy all five.

**Convention 1 — `## Learning Objectives` is mandatory.** A bullet list opening "By the end of
this chapter, you will be able to:", each bullet a bolded concept plus its mechanism. Seven
bullets, matching ch23's count.

**Convention 2 — a Tier 1 / Tier 2 / Tier 3 progression**, each tier naming the in-repo example
it walks:
  - **Tier 1 — mechanical setup.** Scaffold and run an agent with the CLI:
    `cargo pmcp agent new`, then `cargo pmcp agent dev`. This is the cargo-pmcp-first lead and it
    belongs in Tier 1, before any crate API appears.
  - **Tier 2 — real-world.** The same agent loop driven by two different completion sources —
    standalone versus hosted-sampled — walking
    `cargo run -p pmcp-agent --example s50_standalone_vs_sampled`. This is where the sampling
    direction matters, so reference `s49_sampling_host` and point at the book's
    `ch17-04-sampling-hosting.md` for the server-asks-client versus client-asks-server
    disambiguation rather than re-deriving it.
  - **Tier 3 — composition with another advanced feature.** A small TEAM: several member agents
    plus the four reference servers, cooperating in one process. Walk
    `cargo pmcp team dev` first, then
    `cargo run -p pmcp-team-servers --example doc_review_team --features runtime`. Use the
    `--features runtime` form, NOT `--all-features`: plan 119-04 adopted it as the canonical
    invocation and the tests assert against it. Both work; the docs and the tests must not
    disagree.

**Convention 3 — one load-bearing design property gets its own `##` section**, the way ch23 gives
`## The Dual-Surface Invariant` its own. For this chapter that property is: **an agent is an MCP
CLIENT, and the agent loop is source-agnostic** — the same engine runs standalone against its own
completion source and hosted against MCP sampling, with no change to the loop. That is exactly
what `s50_standalone_vs_sampled` demonstrates and it is the idea a learner must not leave without.

**Convention 4 — `## Knowledge Check`** as a three-question bullet list with the answer written
inline after each bolded question.

**Convention 5 — the closing.** A `---` rule and a forward pointer to the exercises, with a
`./`-prefixed link. Course links carry `./`; book links do not. Do not mix the conventions.

Also include `## Why Agents & Teams Matter for Enterprise MCP` (ch23's second section, adapted)
and `## Cross-SDK Compatibility`, framing how the agent-as-client shape lines up with other
reference implementations. Add a `## Chapter Contents` section listing the tiers, as ch23 does.

Every example citation is a FULL runnable cargo invocation (D-10) — never a bare number. The
numbered namespace collides: `s49` appears twice inside root `examples/` and `s50` appears in
both root `examples/` and `crates/pmcp-agent/examples/`, so `-p` is load-bearing. Per the measured
feature requirements, `s50_standalone_vs_sampled` and `s49_sampling_host` need NO `--features`
flag; only `doc_review_team` does.

Do not write a v2 migration section. D-09 scopes the migration to book-and-README, tracking the
requirement wording literally: DOCS-04 names README/course, DOCS-05 does not mention the course.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch24-agents-and-teams.md && test "$(wc -l < pmcp-course/src/part8-advanced/ch24-agents-and-teams.md)" -ge 400</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` succeeds and `wc -l` is at least `400`
    - `grep -c '^## Learning Objectives' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` equals `1`
    - `grep -c '^## Tier 1' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` equals `1`; the same for `^## Tier 2` and `^## Tier 3`
    - `grep -c '^## Knowledge Check' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` equals `1`
    - `grep -c 'cargo run -p pmcp-agent --example s50_standalone_vs_sampled' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` is at least `1`
    - `grep -c 'cargo run -p pmcp-team-servers --example doc_review_team --features runtime' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` is at least `1`
    - The non-canonical team-example invocation does not appear: `grep -c 'doc_review_team --all-features' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` equals `0`
    - `grep -ohE 'cargo pmcp [a-z-]+ [a-z-]+' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md | sort -u` prints only lines within {`cargo pmcp agent new`, `cargo pmcp agent dev`, `cargo pmcp team dev`}
    - `grep -c './ch24-exercises.md' pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` is at least `1` — the course `./` link convention
    - The chapter contains no `##` section about protocol-era migration (D-09)
  </acceptance_criteria>
  <done>A ~440-line course chapter ships at ch23 depth, with Learning Objectives, a three-tier progression walking real examples, a named load-bearing design property, a Knowledge Check and a `./`-prefixed forward pointer.</done>
</task>

<task type="auto">
  <name>Task 2: Write the exercises file with pass predicates drawn from measured output</name>
  <files>pmcp-course/src/part8-advanced/ch24-exercises.md</files>
  <read_first>
    - `pmcp-course/src/part8-advanced/ch23-exercises.md` in full (222 lines) — the exact skeleton: H1 `# Chapter 23 Exercises`, a one-paragraph intro naming the Tier 1→3 ordering, then three `## Exercise N: <Title> (<tier-name>)` sections each carrying a bolded `**Difficulty:** <Level> (N min)` line, a goal paragraph, numbered `**Steps:**`, and a `### Verify your solution` subsection stating a PASS PREDICATE; then `## Prerequisites` and `## Next Steps`
    - `pmcp-course/src/part8-advanced/ch22-exercises.md` (173 lines) — a second calibration point
    - `.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md` § F-6 "Observed terminal output (excerpted)" — the REAL stdout each example prints, measured by execution. These strings are the source of the pass predicates.
    - `tests/docs04_examples_run.rs` as written by plans 119-02 and 119-04 — the same banners, asserted programmatically. The exercises' predicates and the tests' assertions must name the same strings. This file is COMPLETE when this plan runs: `depends_on` names both 119-02 and 119-04 and this plan is wave 4, so all three legs (`s50_standalone_vs_sampled`, `s49_sampling_host`, `doc_review_team`) exist to read. If any of the three is missing, STOP — the ordering guarantee has been violated and predicates written against a half-written file would be guesses
  </read_first>
  <action>
Create `pmcp-course/src/part8-advanced/ch24-exercises.md`, target ~210–240 lines, three exercises
mapped one-to-one onto the chapter's three tiers.

Each exercise carries: a title naming its tier; a bolded `**Difficulty:**` line with a level and a
minute estimate, escalating across the three; a short goal paragraph; five numbered steps; and a
`### Verify your solution` subsection.

**The pass predicates are the load-bearing part.** ch23's style is a single unambiguous sentence
stating exactly what the learner must observe — for instance, that the printed output names both
of two specific URIs. Write predicates of that shape, and source the observable strings from the
examples' MEASURED output rather than from imagination:
  - Exercise 1 (Tier 1, CLI scaffold + run): the predicate is about what
    `cargo pmcp agent new` writes and what `cargo pmcp agent dev` prints. Derive it from the
    commands' real behaviour; do not invent an output line.
  - Exercise 2 (Tier 2, `s50_standalone_vs_sampled`): the predicate names the banner the example
    actually prints on success — the line stating that the same AgentEngine ran standalone and
    hosted-sampled — and requires the learner to observe BOTH source sections in the output, not
    just the final line. That "both, not just the last" framing is the exercise's whole point: it
    is the positive control that makes the tier's claim non-vacuous.
  - Exercise 3 (Tier 3, `doc_review_team`): the predicate names the completion line the example
    prints and requires a zero exit status. Use the `--features runtime` invocation.

Finish with `## Prerequisites` (what must be installed and built first — including that examples
must be built before they can be run, since `cargo test` does not build them) and `## Next Steps`
pointing forward.

Write no exercise that requires the learner to write new example code in this repository, and add
no new files under `examples/` or `crates/*/examples/`.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch24-exercises.md && test "$(grep -c '^### Verify your solution' pmcp-course/src/part8-advanced/ch24-exercises.md)" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pmcp-course/src/part8-advanced/ch24-exercises.md` succeeds and `wc -l` is at least `180`
    - `head -1 pmcp-course/src/part8-advanced/ch24-exercises.md` equals `# Chapter 24 Exercises`
    - `grep -c '^## Exercise ' pmcp-course/src/part8-advanced/ch24-exercises.md` equals `3`
    - `grep -c '^### Verify your solution' pmcp-course/src/part8-advanced/ch24-exercises.md` equals `3`
    - `grep -c '\*\*Difficulty:\*\*' pmcp-course/src/part8-advanced/ch24-exercises.md` equals `3`
    - `grep -c '^## Prerequisites' pmcp-course/src/part8-advanced/ch24-exercises.md` equals `1` and `grep -c '^## Next Steps' pmcp-course/src/part8-advanced/ch24-exercises.md` equals `1`
    - The Exercise 2 predicate names the same success banner that `tests/docs04_examples_run.rs` asserts for `s50_standalone_vs_sampled`
    - The Exercise 3 predicate names the same completion string that `tests/docs04_examples_run.rs` asserts for `doc_review_team`
    - `grep -c 'features runtime' pmcp-course/src/part8-advanced/ch24-exercises.md` is at least `1`
    - `git status --porcelain examples/ crates/pmcp-agent/examples/ crates/pmcp-team-servers/examples/` prints nothing
  </acceptance_criteria>
  <done>Three tiered exercises ship with escalating difficulty and a pass predicate each, and every predicate names a string the corresponding example genuinely prints — the same string the run tests assert.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the Part VIII nav, decide the quiz, and prove reachability by file existence</name>
  <files>pmcp-course/src/SUMMARY.md, pmcp-course/src/quizzes/ch24-agents-and-teams.toml (only if the quiz decision ships one)</files>
  <read_first>
    - `pmcp-course/src/SUMMARY.md` lines 155–185 — the Part VIII block. Note the differences from the book's SUMMARY, all load-bearing: the part heading is a SINGLE `#`; every link carries the `./part8-advanced/` prefix; titles use a colon and never a chapter number; a BLANK LINE separates chapter groups; and the exercises child is always titled `Chapter NN Exercises`
    - `pmcp-course/src/quizzes/ch23-skills.toml` — the quiz format, and the `{{#quiz ../quizzes/ch23-skills.toml}}` include placement in `ch23-skills.md` immediately after `## Knowledge Check`
    - `.github/workflows/docs.yml` lines 45–58 — `mdbook-quiz` is installed `--locked --force --version 0.4.0`, while `mdbook-exercises` is installed with a soft-failure fallback
    - `pmcp-course/book.toml` in full — it sets `create-missing = true` EXPLICITLY at `pmcp-course/book.toml:11` (measured; an earlier draft of this plan asserted the key was absent and was wrong). That is why this task's reachability check is a `test -f` and not a build: mdbook WRITES the missing file and exits 0. Contrast `pmcp-book/book.toml:14`, `create-missing = false  # Prevent accidental file creation`
  </read_first>
  <action>
**The nav.** In `pmcp-course/src/SUMMARY.md`, insert a new group after the ch23 group — i.e.
after the `Chapter 23 Exercises` child line and before the blank line preceding the `---` that
closes Part VIII. The group is two lines: at column 0,
`- [Agents & Teams: Deploy-Anywhere Agent Loops](./part8-advanced/ch24-agents-and-teams.md)`, and
beneath it at exactly two spaces, `  - [Chapter 24 Exercises](./part8-advanced/ch24-exercises.md)`.
Preserve the blank line between chapter groups — it is the file's convention, not incidental
whitespace. Keep the `./part8-advanced/` prefix; do not import the book's no-prefix style.

**The quiz decision — make it explicitly, and record the reasoning.** ch23 carries a
`{{#quiz ../quizzes/ch23-skills.toml}}` include immediately after its `## Knowledge Check`, and
`mdbook-quiz` is pinned at 0.4.0 in CI. The asymmetry that decides this: a MISSING quiz TOML
behind a live include is a build risk, whereas an OMITTED include is not. So either ship both the
include and a real `pmcp-course/src/quizzes/ch24-agents-and-teams.toml`, or ship neither. Do not
ship a dangling include. Prefer shipping both, for consistency with ch22 and ch23 — but if the
quiz content cannot be written to the same standard as the chapter, omit the include and record
that choice in the plan SUMMARY with its reason. Either outcome is acceptable; an unrecorded
outcome is not.

**Reachability — and why it is a `test -f`, not a build.** Plan 119-02 Task 2 measured the
asymmetry between the two books: `pmcp-book/book.toml:14` sets `create-missing = false`, so a
SUMMARY entry with no file is a hard build failure there. `pmcp-course/book.toml:11` sets
`create-missing = true` explicitly, so a missing course chapter is WRITTEN OUT as a new file under
`pmcp-course/src/` — the build stays green, the learner gets a blank chapter, and the repository
gains an untracked file nobody meant to create. `mdbook build` therefore CANNOT verify this plan's
nav wiring, and `git diff` cannot see what it leaves behind. Assert file existence for both new
files explicitly, record in the plan SUMMARY that this is the reason, and after building confirm
`git status --porcelain --untracked-files=all pmcp-course/src` shows nothing beyond this plan's own
two new chapter files — a third, unexpected path there means a SUMMARY entry points at a file that
does not exist.

**The theme side effect.** Building the course book rewrites two TRACKED files,
`pmcp-course/src/theme/exercises.css` and `pmcp-course/src/theme/exercises.js`, because the
installed `mdbook-exercises` preprocessor reinstalls its assets on every run and they differ from
what is committed (measured during planning: +215 and +284/-88 lines). After building, assert the
theme directory is clean and restore it with `git checkout --` if it is not. Never commit that
diff; it is unrelated to this phase.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch24-agents-and-teams.md && test -f pmcp-course/src/part8-advanced/ch24-exercises.md && cd pmcp-course && mdbook build && cd .. && git diff --quiet -- pmcp-course/src/theme/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ch24-agents-and-teams.md' pmcp-course/src/SUMMARY.md` equals `1`
    - `grep -c 'ch24-exercises.md' pmcp-course/src/SUMMARY.md` equals `1`
    - `grep -c './part8-advanced/ch24' pmcp-course/src/SUMMARY.md` equals `2` — both links carry the course `./` prefix
    - `grep -n 'ch24-agents-and-teams.md' pmcp-course/src/SUMMARY.md` reports a line number GREATER than that of `grep -n 'ch23-exercises.md' pmcp-course/src/SUMMARY.md` and LESS than that of `grep -n '^# Appendices' pmcp-course/src/SUMMARY.md`
    - `test -f pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` and `test -f pmcp-course/src/part8-advanced/ch24-exercises.md` both succeed
    - If a `{{#quiz` include exists in the chapter, then `test -f pmcp-course/src/quizzes/ch24-agents-and-teams.toml` succeeds; if no include exists, the plan SUMMARY records the omission and its reason
    - `cd pmcp-course && mdbook build` exits 0
    - `git diff --quiet -- pmcp-course/src/theme/` succeeds
    - The course build created nothing unexpected: after `mdbook build`, `git status --porcelain --untracked-files=all pmcp-course/src` lists ONLY this plan's own deliberate changes — the two new chapter files, the modified `SUMMARY.md`, and the quiz TOML if Task 3's decision shipped one. Any OTHER path listed there is a file mdbook created on the plan's behalf, which means a SUMMARY entry points at a file that does not exist. `create-missing = true` turns that defect into a silent new file rather than a build failure, and `git diff` cannot see it — this check is the only detector
    - `cd pmcp-book && mdbook build` exits 0 — this plan does not touch the book, and this confirms it
  </acceptance_criteria>
  <done>The Part VIII nav carries the ch24 group in the course's own conventions, both files provably exist, the quiz decision is made and recorded either way, and the theme side effect is reverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| course chapter → learner's terminal | Every command in an exercise is typed verbatim by someone learning the SDK. A wrong verb or an unbuildable invocation fails the learner, not the author. |
| course SUMMARY → published course | Unlike the book, this book's builder does NOT fail on a missing target — it silently ships an empty page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-28 | Spoofing | the course teaching a `cargo pmcp` verb that does not exist | medium | mitigate | Read against `cargo-pmcp/src/commands/agent/mod.rs:20-25` and `team/mod.rs:17-20`; an acceptance criterion extracts every `cargo pmcp <verb> <sub>` string and asserts set membership in the three real verbs. |
| T-119-29 | Tampering | a silently empty published chapter from a missing file | medium | mitigate | `pmcp-course/book.toml:11` sets `create-missing = true` explicitly, so the build cannot detect it — it WRITES the missing file and exits 0. Explicit `test -f` assertions for both files, plus an untracked-file check over `pmcp-course/src` that surfaces any file mdbook created on the plan's behalf, with the reason recorded. |
| T-119-30 | Denial of service | a dangling `{{#quiz}}` include breaking the pinned mdbook-quiz 0.4.0 build | medium | mitigate | Task 3 forces an explicit both-or-neither decision and an acceptance criterion that ties the include's presence to the TOML's existence. |
| T-119-31 | Repudiation | exercise pass predicates that no example actually satisfies | medium | mitigate | Predicates are sourced from measured stdout and are cross-checked against the strings `tests/docs04_examples_run.rs` asserts, so an example that stops printing them fails CI as well as the learner. |
| T-119-32 | Repudiation | committing a 411-line unrelated theme diff | low | mitigate | An acceptance criterion asserts `git diff --quiet -- pmcp-course/src/theme/`. |
| T-119-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; this plan writes markdown only. `mdbook-quiz` and `mdbook-exercises` are pre-existing CI tooling, not new dependencies of this phase. |
</threat_model>

<verification>
1. `test -f pmcp-course/src/part8-advanced/ch24-agents-and-teams.md` — succeeds.
2. `test -f pmcp-course/src/part8-advanced/ch24-exercises.md` — succeeds.
3. `cd pmcp-course && mdbook build` — exits 0.
4. `git diff --quiet -- pmcp-course/src/theme/` — succeeds (revert the `mdbook-exercises` asset rewrite before committing).
5. `cd pmcp-book && mdbook build` — exits 0.
6. `make quality-gate` — exits 0 before the commit. This plan touches no Rust, so the expected cost is the full suite with no new compilation.
</verification>

<success_criteria>
- A ~440-line course chapter and a ~220-line exercises file ship in Part VIII at `ch23-skills.md` depth.
- The three tiers run mechanical → real-world → composition, and each names the example it walks by full runnable invocation.
- Every pass predicate names a string the example genuinely prints, matching what the run tests assert.
- Only the three real `cargo pmcp` verbs appear.
- The nav group follows the course's own conventions (single `#` part heading, `./` prefixes, blank line between groups), and the course build leaves no unexpected file behind under `pmcp-course/src`.
- The quiz decision is made explicitly and recorded either way, with no dangling include.
- No v2 migration content appears in the course (D-09).
- The course theme files are unmodified; both books build; `make quality-gate` exits 0.
</success_criteria>

<output>
Create `.planning/phases/119-documentation-three-shapes-v2-migration/119-06-SUMMARY.md` when done.
</output>
