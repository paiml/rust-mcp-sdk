---
phase: 119-documentation-three-shapes-v2-migration
plan: 07
type: execute
wave: 4
depends_on: ["119-02", "119-04", "119-05"]
files_modified:
  - pmcp-book/src/ch12-16-agent-teams.md
  - pmcp-book/src/SUMMARY.md
autonomous: true
requirements: [DOCS-04]

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-08: the second of Phase 111's two to-write chapters ships — `Agent Teams` — completing the three named chapters (two written, one relinked by plan 119-02)."
    - "D-08: all three Phase-111 chapters are now reachable from Part III: `Agents as MCP Clients`, `Agent Teams`, and `Sampling & Hosting`."
    - "D-05: the chapter opens with the era-disambiguation callout in the book's only callout form."
    - "D-10: `doc_review_team` is cited by its full runnable invocation with the `--features runtime` form the tests assert against."
    - "D-11 / cargo-pmcp-first: the chapter's first runnable command is `cargo pmcp team dev`, before any crate API appears."
  artifacts:
    - "pmcp-book/src/ch12-16-agent-teams.md — new Part III chapter, ~250 lines"
    - "pmcp-book/src/SUMMARY.md — one new top-level Part III entry, inserted between Chapter 12.15's block and Chapter 12.17"
  key_links:
    - "chapter ↔ `crates/pmcp-team-servers/examples/doc_review_team.rs`: the chapter's walkthrough must describe what the example actually does — four reference servers cooperating in ONE offline process"
    - "SUMMARY entry ↔ file: `create-missing = false` makes `mdbook build` the only detector, and it is not in `make quality-gate`"
  prohibitions:
    - "No CLI verb is named beyond `cargo pmcp agent new`, `cargo pmcp agent dev` and `cargo pmcp team dev` — `cargo pmcp team` has exactly one subcommand."
    - "The `--all-features` form of the `doc_review_team` invocation is NOT used; the canonical form adopted in plan 119-04 is `--features runtime`."
    - "No new example source code is written (D-12), and `crates/pmcp-team-servers/examples/doc_review_team.rs` is not edited."
    - "`ch17-04-sampling-hosting.md` is NOT re-parented again — plan 119-02 already moved it and it must appear exactly once in `SUMMARY.md`."
    - "No protocol-era migration content is duplicated here — that is Chapter 12.17's job; link to it."
---

<objective>
Write the second Agents & Teams book chapter — `Agent Teams` — and wire it into Part III between
the agents chapter and the migration chapter.

Purpose: Phase 111 named three chapters: "Agents as MCP Clients", "Agent Teams" and
"Sampling & Hosting". The third already existed and plan 119-02 relinked it; the first is the
tracer's chapter. This is the last of the three, and with it DOCS-04's book shape is complete.
Output: `ch12-16-agent-teams.md` and its SUMMARY entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-CONTEXT.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-PATTERNS.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md
</context>

<artifacts_this_plan_produces>
## Artifacts this plan produces

- **New file** `pmcp-book/src/ch12-16-agent-teams.md`
- **New SUMMARY entry** `- [Chapter 12.16: Agent Teams](ch12-16-agent-teams.md)`

Exclude both from drift verification.
</artifacts_this_plan_produces>

<review_dispositions>
## Cross-AI review dispositions (`119-REVIEWS.md`)

| Finding | Severity | Disposition |
|---|---|---|
| This plan consumes 119-04's output — its `doc_review_team` citation must use the `--features runtime` invocation 119-04 adopted as canonical and asserts against — but did not declare `119-04` in `depends_on` | LOW (consensus downgrade from Codex MEDIUM: not a live race, because the wave barrier already orders wave-4 after wave-3) | **INCORPORATED.** `depends_on` now names `119-04`. No wave change was needed or made: this plan is wave 4 and 119-04 is wave 3, so the declared dependency and the wave assignment already agree. The fix makes the dependency graph honest so it survives any future re-waving, which is exactly what the consensus asked for. |
</review_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Write the Agent Teams chapter</name>
  <files>pmcp-book/src/ch12-16-agent-teams.md</files>
  <read_first>
    - `pmcp-book/src/ch12-13-config-driven-workbook-servers.md` (257 lines) — the Part III spine analog: intro closing with an "After this chapter you should be able to" sentence, `## Step N:` middles, and a `## What You Built` close
    - `pmcp-book/src/ch12-15-agents-as-mcp-clients.md` as written by plan 119-02 — this chapter's sibling. Read it in full so the two do not overlap: 12.15 owns the single agent loop and the standalone-vs-hosted distinction; 12.16 owns the multi-agent composition. Cross-link rather than restate.
    - `crates/pmcp-team-servers/examples/doc_review_team.rs` (406 lines) — the walkthrough subject. Note what it actually demonstrates: four reference servers plus member agents cooperating in ONE offline process, over in-memory transports with a fixed-source mock factory and no sockets, tearing down four hosting tasks cleanly.
    - `crates/pmcp-team-servers/src/` — the four reference servers, so the chapter names them correctly
    - `crates/pmcp-team-servers/Cargo.toml:161-163` — the `required-features = ["runtime"]` declaration on the example
    - `cargo-pmcp/src/commands/team/mod.rs:17-20` — `TeamCommand` has exactly ONE variant, `Dev`. There is no `team new`, no `team run`, no `team list`.
    - `pmcp-book/src/ch01_5-quick-start-tutorial.md:5` — the book's only callout form (blockquote, bolded lead)
    - `pmcp-book/src/ch12-10-config-driven-sql-servers.md:46-60` — the `## Two Shapes` table form, if a comparison table is used
  </read_first>
  <action>
Create `pmcp-book/src/ch12-16-agent-teams.md`, target ~230–280 lines, following the measured
Part III spine.

**Structure.**
  - H1 `# Chapter 12.16: Agent Teams`.
  - Immediately after it, the D-05 era-disambiguation callout in the book's only callout form:
    protocol eras are `v1` (2025-11-25) and `v2` (2026-07-28); the crate is always written with
    its name attached, as in "pmcp 2.18", never a bare version.
  - A three-paragraph intro. Paragraph one: what the previous chapter did (a single agent loop as
    an MCP client). Paragraph two: what this chapter adds (several agents plus shared reference
    servers, cooperating). Paragraph three closes with an "After this chapter you should be able
    to …" sentence. The book folds objectives into the intro — do NOT add a `## Learning
    Objectives` heading; that is the course's convention, not the book's.
  - `## The Problem (Why a Team, Not a Bigger Agent)` — why composition beats one agent with more
    tools: separable responsibilities, independent reference servers, and the fact that a team
    member is itself just an MCP client.
  - `## The Four Reference Servers` — name each of the four servers `pmcp-team-servers` ships and
    say what each is for, in one sentence each. Read the crate to get these right; do not infer
    them from the example's output.
  - `## Step 1: Run a team` — the cargo-pmcp lead, and the FIRST runnable command in the chapter:
    `cargo pmcp team dev`, which runs an in-process small team (member agents plus the four
    reference servers). This is the ONLY `team` verb; do not name another.
  - `## Step 2: Walk the doc-review flow` — the example walkthrough. Cite it by its full runnable
    invocation `cargo run -p pmcp-team-servers --example doc_review_team --features runtime`
    (D-10). Use the `--features runtime` form, not `--all-features`: plan 119-04 adopted it as
    canonical and `tests/docs04_examples_run.rs` asserts against it. Describe what the run
    actually does and what it prints on success, and note that it is fully offline — in-memory
    transports, a mock source factory, no sockets — which is why it is safe to run anywhere.
  - `## Step 3: From the example to your own team` — drop to the crate API. Describe how a member
    agent is wired and how the hosting tasks are torn down, and cross-link Chapter 12.15 for the
    single-agent loop rather than re-deriving it.
  - `## Protocol eras and teams` — SHORT. Teams inherit the dual-version story rather than
    changing it; link Chapter 12.17 for the migration detail. Do not restate the migration
    chapter's server/client/agent tracks here.
  - `## What You Built` — a five-bullet capability list, then cross-links as bare relative
    filenames with no `./` prefix. Cross-link Chapter 12.15, Chapter 12.17 and the
    `Sampling & Hosting` page.

Use plain ```` ```bash ```` / ```` ```rust ```` / ```` ```text ```` fences. Part III does not use
mermaid. Every command shown must be one that runs; do not invent flags. Titles that carry a
subtitle use an em dash.
  </action>
  <verify>
    <automated>test -f pmcp-book/src/ch12-16-agent-teams.md && cargo build -p pmcp-team-servers --example doc_review_team --features runtime</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pmcp-book/src/ch12-16-agent-teams.md` succeeds and `wc -l` is at least `220`
    - `head -1 pmcp-book/src/ch12-16-agent-teams.md` equals `# Chapter 12.16: Agent Teams`
    - `grep -c 'cargo pmcp team dev' pmcp-book/src/ch12-16-agent-teams.md` is at least `1`
    - `grep -c 'cargo run -p pmcp-team-servers --example doc_review_team --features runtime' pmcp-book/src/ch12-16-agent-teams.md` is at least `1`
    - The non-canonical team-example invocation does not appear: `grep -c 'doc_review_team --all-features' pmcp-book/src/ch12-16-agent-teams.md` equals `0`
    - `grep -ohE 'cargo pmcp [a-z-]+ [a-z-]+' pmcp-book/src/ch12-16-agent-teams.md | sort -u` prints only lines within {`cargo pmcp agent new`, `cargo pmcp agent dev`, `cargo pmcp team dev`}
    - `grep -c 'ch12-15-agents-as-mcp-clients.md' pmcp-book/src/ch12-16-agent-teams.md` is at least `1`
    - `grep -c 'ch12-17-migrating-to-mcp-2026-07-28.md' pmcp-book/src/ch12-16-agent-teams.md` is at least `1`
    - `grep -c '^## Learning Objectives' pmcp-book/src/ch12-16-agent-teams.md` equals `0` — that heading is a course convention, not a book one
    - `grep -c '^## What You Built' pmcp-book/src/ch12-16-agent-teams.md` equals `1`
    - `cargo build -p pmcp-team-servers --example doc_review_team --features runtime` exits 0 — the cited invocation genuinely works
    - `git status --porcelain crates/pmcp-team-servers/examples/` prints nothing (D-12)
  </acceptance_criteria>
  <done>A ~250-line Agent Teams chapter ships in the Part III style, leading with `cargo pmcp team dev`, walking `doc_review_team` by its canonical invocation, and cross-linking rather than duplicating its two sibling chapters.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the SUMMARY entry and prove all three Phase-111 chapters are reachable</name>
  <files>pmcp-book/src/SUMMARY.md</files>
  <read_first>
    - `pmcp-book/src/SUMMARY.md` as it stands after plans 119-02 and 119-05 — the Part III block now carries the Chapter 12.15 entry with its indented `Sampling & Hosting` child, and Chapter 12.17 as the last entry. Read the exact current line numbers before editing; two plans have already moved them.
    - `pmcp-book/book.toml:14` — `create-missing = false`, the gate this task depends on
    - `.planning/ROADMAP.md` § `### Phase 111` — the three named chapters, so the completeness claim is checked against the record rather than from memory
  </read_first>
  <action>
Insert `- [Chapter 12.16: Agent Teams](ch12-16-agent-teams.md)` at column 0 in
`pmcp-book/src/SUMMARY.md`, positioned BETWEEN the Chapter 12.15 block (the entry plus its
two-space-indented `Sampling & Hosting` child) and the Chapter 12.17 entry. The result is a
contiguous, ascending 12.14 → 12.15 (+child) → 12.16 → 12.17 run closing Part III. Re-locate the
neighbours by their link targets rather than by line number — plans 119-02 and 119-05 have both
shifted this block since research measured it.

Do not touch the `Sampling & Hosting` child line. Plan 119-02 already re-parented it out of the
`ch17-examples.md` stub in Part V, and it must appear exactly once in the whole file. A second
occurrence would be a duplicate nav entry, not a fix.

Then prove the DOCS-04 book shape is complete by checking all three Phase-111 chapters are
reachable in ONE build: `Agents as MCP Clients` (12.15), `Agent Teams` (12.16), and
`Sampling & Hosting` (12.15's child). Run `mdbook build` from `pmcp-book/` and record its exit
status. Because `create-missing = false`, a green build IS the reachability proof for this book —
that is exactly what plan 119-02 Task 2's negative control established, and it is the reason this
task can rely on the build where plan 119-06 could not.

Also run the course build, because a wave that touches either book should sample both — and after
it, assert `pmcp-course/src/theme/` is clean. The `mdbook-exercises` preprocessor rewrites two
tracked theme files on every course build; that diff must never be committed.
  </action>
  <verify>
    <automated>cd pmcp-book && mdbook build && cd ../pmcp-course && mdbook build && cd .. && git diff --quiet -- pmcp-course/src/theme/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ch12-16-agent-teams.md' pmcp-book/src/SUMMARY.md` equals `1`
    - `grep -c 'ch17-04-sampling-hosting.md' pmcp-book/src/SUMMARY.md` equals `1` — still exactly one occurrence
    - `grep -n 'ch12-16-agent-teams.md' pmcp-book/src/SUMMARY.md` reports a line number GREATER than that of `grep -n 'ch17-04-sampling-hosting.md' pmcp-book/src/SUMMARY.md` and LESS than that of `grep -n 'ch12-17-migrating-to-mcp-2026-07-28.md' pmcp-book/src/SUMMARY.md`
    - All three Phase-111 targets are present in `SUMMARY.md`: `grep -c 'ch12-15-agents-as-mcp-clients.md' pmcp-book/src/SUMMARY.md` equals `1`, `grep -c 'ch12-16-agent-teams.md' pmcp-book/src/SUMMARY.md` equals `1`, `grep -c 'ch17-04-sampling-hosting.md' pmcp-book/src/SUMMARY.md` equals `1`
    - `cd pmcp-book && mdbook build` exits 0
    - `cd pmcp-course && mdbook build` exits 0
    - `git diff --quiet -- pmcp-course/src/theme/` succeeds
    - `git diff --numstat pmcp-book/src/SUMMARY.md` shows exactly one line added and zero removed
  </acceptance_criteria>
  <done>Chapter 12.16 sits between 12.15's block and 12.17 in Part III, all three Phase-111 chapters are reachable in a green `create-missing = false` build, and no course theme diff is left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published chapter → reader's terminal | Every command shown is typed verbatim. `cargo pmcp team` has exactly one subcommand; naming a second is a defect that reaches users. |
| `SUMMARY.md` → published book | A nav entry with no file fails the docs workflow, and `make quality-gate` does not chain a book build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-33 | Spoofing | naming a `cargo pmcp team` verb that does not exist | medium | mitigate | Read against `cargo-pmcp/src/commands/team/mod.rs:17-20` (one variant, `Dev`); an acceptance criterion extracts every `cargo pmcp <verb> <sub>` string and asserts membership in the three real verbs. |
| T-119-34 | Tampering | a duplicated `Sampling & Hosting` nav entry after the 119-02 re-parent | medium | mitigate | An acceptance criterion requires exactly one occurrence in the whole file, and a numstat criterion requires exactly one added line and zero removed. |
| T-119-35 | Denial of service | a SUMMARY entry with no file breaking the docs workflow | medium | mitigate | The file is written before the SUMMARY line; `mdbook build` is run in the plan's own `<verify>` under a config proven to fail on this exact defect (119-02 Task 2's negative control). |
| T-119-36 | Repudiation | a cited invocation that does not build | medium | mitigate | Task 1's `<verify>` builds the cited example with the exact feature set the chapter names, so a documented-but-unbuildable invocation fails the task. |
| T-119-37 | Repudiation | committing the `mdbook-exercises` theme rewrite | low | mitigate | An acceptance criterion asserts `git diff --quiet -- pmcp-course/src/theme/`. |
| T-119-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; this plan writes markdown and edits one nav file. |
</threat_model>

<verification>
1. `cargo build -p pmcp-team-servers --example doc_review_team --features runtime` — exits 0.
2. `cd pmcp-book && mdbook build` — exits 0. This is the reachability proof for all three Phase-111 chapters.
3. `cd pmcp-course && mdbook build` — exits 0, then `git diff --quiet -- pmcp-course/src/theme/` succeeds.
4. `cargo test --test docs04_examples_run` — exits 0 with `3 passed`; this plan must not disturb the run tests.
5. `make quality-gate` — exits 0 before the commit.
</verification>

<success_criteria>
- `ch12-16-agent-teams.md` ships at Part III depth and is reachable from Part III.
- All three Phase-111 chapters — 12.15, 12.16 and the relinked `Sampling & Hosting` — are reachable in one green book build, completing DOCS-04's book shape.
- The chapter's first runnable command is `cargo pmcp team dev`, and only real verbs appear.
- `doc_review_team` is cited with the `--features runtime` form, and that invocation is proven to build.
- `Sampling & Hosting` appears exactly once in `SUMMARY.md`.
- Both books build; the course theme is unmodified; `make quality-gate` exits 0.
</success_criteria>

<output>
Create `.planning/phases/119-documentation-three-shapes-v2-migration/119-07-SUMMARY.md` when done.
</output>
