---
phase: 119-documentation-three-shapes-v2-migration
plan: 08
type: execute
wave: 4
depends_on: ["119-05"]
files_modified:
  - pmcp-book/src/ch12-7-tasks.md
  - pmcp-book/src/ch10-transports.md
  - pmcp-book/src/ch10-03-streamable-http.md
autonomous: true
requirements: [DOCS-05]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-07: `ch12-7-tasks.md` gains an era delta — extension-ization, the retired v2 methods, and the capability-negotiation change — written where Tasks readers already are rather than as a parallel account."
    - "D-07: the Tasks era delta carries a PROVISIONALITY callout, because `ext-tasks` still publishes only a `draft/` schema and `114-SPEC-RECHECK.md`'s verdict is `PENDING` — measured 2026-08-18, and NOT this phase's to discharge."
    - "D-16.1: the two stale protocol-version lines become era-aware — `ch10-transports.md:572` and `ch10-03-streamable-http.md:73`."
    - "D-16.2: one `> **Era note**` callout at the head of `## Understanding Streamable HTTP Modes` states that the three documented modes are v1 BUILD-TIME modes, that v2 statelessness is PER-REQUEST and orthogonal, and that SSE resumption via `Last-Event-Id` is a v1 mechanism."
    - "D-16.3: each of the two transport chapters gains the D-05 era-disambiguation callout at its head."
    - "D-05: era vocabulary is consistent across all three amended chapters."
  artifacts:
    - "pmcp-book/src/ch12-7-tasks.md — an additive `## Era delta (v1 vs v2)` section plus the era and provisionality callouts"
    - "pmcp-book/src/ch10-transports.md — two callouts and one era-aware header line"
    - "pmcp-book/src/ch10-03-streamable-http.md — one callout and one era-aware header line"
  key_links:
    - "the Mode-1 taxonomy in `ch10-transports.md:188-206` ↔ `examples/s47_v2_stateless_mrtr.rs:19-22`: the chapter teaches statelessness as a build-time config choice, which the shipped example falsifies for v2 in its own header — that is a behavioural miss, not a cosmetic one"
    - "`ch12-7-tasks.md` era delta ↔ `114-SPEC-RECHECK.md` verdict `PENDING`: what may be stated as shipped versus what must be marked provisional"
    - "both ch10 callouts ↔ Chapter 12.17's server track: the callouts link there rather than re-deriving the v2 story"
  prohibitions:
    - "NO code block in either `ch10` chapter is modified — this is D-16's explicit acceptance criterion. The fix is additional callout prose only."
    - "The two `2025-11-25` occurrences in `ch12-7-tasks.md` at lines 156-157 are NOT era-corrected — they are JSON timestamps (`createdAt` / `lastUpdatedAt`), not protocol-version references."
    - "TASK-01..TASK-06 are NOT described as complete, and the v2 Tasks wire is NOT described as final."
    - "The `## Understanding Streamable HTTP Modes` taxonomy is NOT rewritten into a four-mode structure — option C was costed and rejected."
    - "`docs/MIGRATION.md` and `pmcp-book/src/ch21-migration.md` are NOT touched."
---

<objective>
Repair era staleness where readers already are: amend the Tasks chapter with a properly
provisional v2 delta, and add era callouts to the two transport chapters that currently teach a
v1-only story as if it were the whole story.

Purpose: `ch12-7-tasks.md` is 799 lines and mentions `2026-07-28` zero times — it reads as if v2
does not exist. `ch10-transports.md` teaches statelessness as a build-time config choice
(`session_id_generator: None`), which `examples/s47_v2_stateless_mrtr.rs` falsifies for v2 in its
own header, and it states SSE resumption via `Last-Event-Id` unqualified inside that same
taxonomy. A reader chasing v2 statelessness would disable v1 sessions unnecessarily. That is
behavioural, and it contradicts the very example this phase cites as DOCS-06's proof.
Output: three amended chapters, minimal-touch, no code-block edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-CONTEXT.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-PATTERNS.md
@.planning/phases/119-documentation-three-shapes-v2-migration/119-RESEARCH.md
</context>

<artifacts_this_plan_produces>
## Artifacts this plan produces

- **New `##` section** `## Era delta (v1 vs v2)` in `pmcp-book/src/ch12-7-tasks.md`
- **New callouts** (blockquote form): one D-05 era callout in each of the three amended chapters,
  one provisionality callout in `ch12-7-tasks.md`, one `> **Era note**` in `ch10-transports.md`

No new files. Exclude the new section heading and callouts from drift verification.
</artifacts_this_plan_produces>

<review_dispositions>
## Cross-AI review dispositions (`119-REVIEWS.md`)

| Finding | Severity | Disposition |
|---|---|---|
| Task 1's acceptance criterion contradicted itself: it invoked `cargo run --example s50_v2_tasks_server` and `cargo run --example s51_v2_tasks_agent` while claiming "build only; do not require a full run". `cargo run` builds *and runs*, and `examples/s50_v2_tasks_server.rs` is a server that binds a socket and blocks | HIGH | **INCORPORATED.** Both are now `cargo build --example ...`, with the reason stated inline so the next editor does not reintroduce `cargo run`. This aligns the criterion with the plan's own closing `<verification>` step 4, which already used `cargo build`. No other change: the citation-form claim being checked is that the two v2 Tasks examples compile under DEFAULT features, and a build proves exactly that. |
| CLAUDE.md's PMAT quality-proxy write workflow is not encoded in the plans | LOW | **NOT APPLICABLE to this plan; accepted phase-wide elsewhere.** The mandate covers code writes, and this plan writes markdown only — no `.rs`, `.sh` or `Makefile` content. The phase-wide disposition, which governs 119-02/03/04/10, is recorded in `119-02-PLAN.md`'s `<review_dispositions>`. The closing `make quality-gate` still runs here. |
</review_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Amend `ch12-7-tasks.md` with a provisional v2 era delta (D-07)</name>
  <files>pmcp-book/src/ch12-7-tasks.md</files>
  <reversibility rating="costly">Edits a large, load-bearing existing chapter rather than adding a new file.</reversibility>
  <read_first>
    - `pmcp-book/src/ch12-7-tasks.md` — its H1 at line 1 (note it uses `--`, not an em dash: this file predates the em-dash convention, so match its existing style rather than "fixing" it), `## Capability Negotiation` at line 427, `### Server Capability Advertisement` at 444, `## Client Compatibility` at 470, and `## Summary` at 780 — the last heading, before which the new section sits
    - `pmcp-book/src/ch12-7-tasks.md` lines 150–160 — the two `2025-11-25` occurrences. **They are JSON timestamps** (`"createdAt": "2025-11-25T10:30:00Z"` and `"lastUpdatedAt"`), NOT protocol-version references. There is no stale protocol-version string in this chapter. Do not "era-correct" them.
    - `src/types/capabilities.rs:301-346` — `TASKS_EXTENSION_KEY` (`io.modelcontextprotocol/tasks`), the documented v1 (`experimental.tasks`) and v2 (`extensions`) positions, and — at `:340-345` — the SDK's own provisionality note, which is the source text for the callout
    - `src/types/tasks.rs:579-585` — the `tasks/list` retirement, its `-32601` answer on a v2-negotiated request, and the SECURITY rationale: with no enumeration primitive a server cannot leak the existence of one caller's tasks to another. That sentence appears nowhere in the book today and is the strongest line in the whole delta.
    - `src/types/tools.rs:160-166` — the per-era method sets. Note that `tasks/result` is retired on v2 as well as `tasks/list`; CONTEXT.md's D-07 summary names only `tasks/list`.
    - `src/types/mrtr.rs:219` — `TASKS_UPDATE_METHOD`; `src/types/tasks.rs:511` — `CreateTaskResult`
    - `.planning/phases/114-tasks-extension-migration/114-SPEC-RECHECK.md` `## Verdict` — still `PENDING`, with the `STILL-ABSENT` landing state
    - `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md` as written by plan 119-05 — its `## Tasks on v2` pointer, which links here. The two must agree.
    - `pmcp-book/src/ch01_5-quick-start-tutorial.md:5` — the book's only callout form
  </read_first>
  <action>
The amendment is PURELY ADDITIVE. Nothing existing in this chapter is wrong; it is incomplete.

**Edit A — the era callout at the head.** Immediately after the H1 (the book places head callouts
on line 3–5), add the D-05 era-disambiguation callout in the blockquote form: eras are `v1`
(2025-11-25) and `v2` (2026-07-28); the crate is always name-attached. Link Chapter 12.17.

**Edit B — a new `## Era delta (v1 vs v2)` section**, placed immediately BEFORE the existing
`## Summary` (currently the last heading, at line 780). Its content, every claim of which is
verifiable in shipped code today and must carry its source:
  - Tasks are negotiated on v2 through the **extensions map** under the
    `io.modelcontextprotocol/tasks` key, while the v1 path stays `experimental.tasks`. Show both
    positions.
  - **`tasks/list` is retired on v2** and answers `-32601` on a v2-negotiated request. **Quote the
    security rationale** — with no enumeration primitive a server cannot leak the existence of one
    caller's tasks to another. This is the strongest sentence in the delta and it is absent from
    the book today.
  - **`tasks/result` is retired on v2 too.** State both retirements: v1 serves `tasks/get`,
    `tasks/result`, `tasks/list` and `tasks/cancel`; v2 serves `tasks/get`, `tasks/update` and
    `tasks/cancel`. A per-era method table is the clearest form.
  - `tasks/update` exists on v2.
  - v2 task-augmented results use a task result type with `CreateTaskResult`.
  - The runnable v2 Tasks examples are `s50_v2_tasks_server` and `s51_v2_tasks_agent`. Cite both
    by full runnable invocation with **NO `--features` flag** — neither carries an `[[example]]`
    block in `Cargo.toml`, so both are auto-discovered and build under DEFAULT features. Adding a
    feature flag here would be wrong, and CONTEXT.md's claim that all eight v2 examples are
    feature-gated is incorrect for these two.

**Edit C — the provisionality callout**, inside the new section and impossible to miss. The SDK
already says this to Rust readers at the definition site: the upstream repository has not
published a versioned (non-`draft`) schema directory for the Tasks extension, so every value read
from the draft is provisional, and a mismatch between the shipped constant and the eventual
published schema is a phase-reopening event rather than an advisory. Say the reader-facing half of
that: the Tasks extension schema is still `draft/` upstream, so these wire values may change.
**Drop the internal planning-directory reference from the source note** — this is published
documentation for SDK users, not an internal record, and no internal directory path belongs in it.

This callout is not optional politeness. Without it the book would assert as settled exactly what
the SDK's own source calls provisional. D-01's "no `[~]` hedging anywhere" is achievable for the
eleven HTTP/CLNT requirements plan 119-01 flipped; it is NOT achievable for TASK-01..06, and no
action available to this phase makes it achievable — `ext-tasks` was measured on 2026-08-18 with
`draft/` only, zero tags and zero releases, and it is governed by a different record with a
different trigger. The honest resolution is this callout. Do not describe TASK-01..06 as complete
and do not state that the v2 Tasks wire is final.

Change no existing sentence, no existing code block, and no existing heading in this chapter.
  </action>
  <verify>
    <automated>test "$(git show HEAD:pmcp-book/src/ch12-7-tasks.md | wc -l)" -lt "$(wc -l < pmcp-book/src/ch12-7-tasks.md)" && cd pmcp-book && mdbook build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Era delta' pmcp-book/src/ch12-7-tasks.md` equals `1`
    - `grep -c 'io.modelcontextprotocol/tasks' pmcp-book/src/ch12-7-tasks.md` is at least `1`
    - `grep -c 'tasks/update' pmcp-book/src/ch12-7-tasks.md` is at least `1`
    - `grep -c '32601' pmcp-book/src/ch12-7-tasks.md` is at least `1`
    - `grep -c 'tasks/result' pmcp-book/src/ch12-7-tasks.md` is at least `1` — the second retirement is stated, not just `tasks/list`
    - `grep -c '2026-07-28' pmcp-book/src/ch12-7-tasks.md` is at least `1` — the chapter previously mentioned it zero times
    - `grep -c 's50_v2_tasks_server' pmcp-book/src/ch12-7-tasks.md` is at least `1` and `grep -c 's51_v2_tasks_agent' pmcp-book/src/ch12-7-tasks.md` is at least `1`
    - Neither v2 Tasks example is cited with a feature flag: `grep -c 's50_v2_tasks_server --features' pmcp-book/src/ch12-7-tasks.md` equals `0` and `grep -c 's51_v2_tasks_agent --features' pmcp-book/src/ch12-7-tasks.md` equals `0`
    - The two JSON timestamps are untouched: `grep -c '2025-11-25T10:30:00Z' pmcp-book/src/ch12-7-tasks.md` equals `2`
    - No internal planning-directory path ships in the published chapter: `grep -cF '.planning/' pmcp-book/src/ch12-7-tasks.md` equals `0`
    - `grep -ciE 'TASK-0[1-6].*(complete|final)' pmcp-book/src/ch12-7-tasks.md` equals `0`
    - The provisionality callout exists and contains the word `draft`
    - `git diff --numstat pmcp-book/src/ch12-7-tasks.md` reports ZERO removed lines — the amendment is purely additive
    - `cargo build --example s50_v2_tasks_server` and `cargo build --example s51_v2_tasks_agent` both exit 0 with NO feature flag. Use `cargo build`, never `cargo run`: `cargo run` builds AND executes, and `examples/s50_v2_tasks_server.rs` is a server that binds a socket and does not return. The criterion is that the cited invocation COMPILES under default features, which is what the chapter's citation claims; nothing here requires a run
    - `cd pmcp-book && mdbook build` exits 0
  </acceptance_criteria>
  <done>`ch12-7-tasks.md` carries an additive, source-cited era delta naming both retired v2 methods and the security rationale, with a reader-facing provisionality callout, and zero existing lines removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add era callouts to the two transport chapters — no code block touched (D-16)</name>
  <files>pmcp-book/src/ch10-transports.md, pmcp-book/src/ch10-03-streamable-http.md</files>
  <read_first>
    - `pmcp-book/src/ch10-transports.md` — line 1 (H1) and line 3 (the lead paragraph) for the head callout; `## Understanding Streamable HTTP Modes` at line 184 and the taxonomy it opens (lines 184–273); the Mode-1 code block at lines 195–202 (`session_id_generator: None`, `enable_json_response: true`, `event_store: None`) — the block that must NOT be modified; line 250 (`event_store: Some(...)` with a resumption comment, inside a code block) and line 257 (`- Supports resumption via \`Last-Event-Id\` header`, inside Mode 3, in PROSE); line 572 (the `mcp-protocol-version` header bullet naming only `2025-11-25`); and line 577 (`- **\`Last-Event-Id\`**: For SSE resumption after reconnection`)
    - `pmcp-book/src/ch10-03-streamable-http.md` — line 1 and 3 for the head callout; line 73 (the `mcp-protocol-version` bullet); line 76 (`- \`Last-Event-Id\`: For SSE resumption`)
    - `examples/s47_v2_stateless_mrtr.rs` lines 19–22 — verbatim: the server is built with the STATEFUL default HTTP config and still mints sessions for v1 clients, yet no v2 response carries a session id, because that is the PER-REQUEST era gate rather than a build-time stateless switch
    - `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md` § `## For servers` as written by plan 119-05 — the link target for both callouts
    - `pmcp-book/src/ch01_5-quick-start-tutorial.md:5` and `pmcp-book/src/ch15-testing.md:771-774` — the callout form and how multi-line callouts continue the `> ` prefix on every line
  </read_first>
  <action>
Three sub-edits, and nothing more. D-16 chose the minimal-touch option deliberately over both
leaving the chapters alone and rewriting the taxonomy; the acceptance criteria below enforce that
boundary mechanically.

**Sub-edit 1 — the two protocol-version lines become era-aware.** At `ch10-transports.md:572` and
`ch10-03-streamable-http.md:73`, both under a "Headers enforced by the server" bullet list, the
`mcp-protocol-version` header bullet currently names only the v1 era string. Make each name both
eras. These are two-token edits to PROSE bullet lines, not to code blocks.

**Sub-edit 2 — one `> **Era note**` callout at the head of the modes taxonomy.** Insert it at
`ch10-transports.md` immediately after the `## Understanding Streamable HTTP Modes` heading and
before the section's lead paragraph. In the book's blockquote form, continuing the `> ` prefix on
every line, it must say three things:
  1. The three modes documented in this section are **v1 build-time** modes — configurations of
     the stateful/stateless spectrum chosen when the server is constructed.
  2. **v2 statelessness is per-request and orthogonal to all of them.** A server built with the
     STATEFUL default config still mints sessions for v1 clients and still returns no session id
     on a v2-negotiated request. That is the per-request era gate, not a build-time switch, and
     `examples/s47_v2_stateless_mrtr.rs` demonstrates exactly this. Cite the example by its full
     runnable invocation.
  3. **SSE resumption via `Last-Event-Id` is a v1 mechanism.** This clause is not optional. The
     same taxonomy states, in Mode 3, that resumption via that header is supported — unqualified —
     and that claim is v1-only. Without this clause the callout would leave a live contradiction
     in the very section it exists to fix. Say it in the callout; do NOT edit the Mode-3 bullet
     and do NOT edit the Mode-3 code block.
  Link Chapter 12.17's server track for the full v2 story rather than re-deriving it here.

**Sub-edit 3 — the D-05 head callout in each chapter.** At the top of BOTH chapters, between the
H1 and the lead paragraph, add the era-disambiguation callout: eras are `v1` (2025-11-25) and `v2`
(2026-07-28); the crate is always written name-attached. Link Chapter 12.17.

**The boundary, stated as a mechanical check.** Before editing, compute a digest over the
concatenation of every line INSIDE a fenced code block in each of the two files, from the
committed version. After editing, recompute it from the working tree. The two digests must be
identical for each file. Use an awk toggle on lines beginning with a triple backtick to select
fenced content. This is the acceptance criterion for "no code block is modified", and it is
checkable rather than a matter of judgement.

Do NOT restructure the taxonomy into a four-mode form. That option was costed: it would duplicate
Chapter 12.17's server track, re-open three further sections, and turn a documentation phase into
a chapter rewrite.
  </action>
  <verify>
    <automated>test "$(git show HEAD:pmcp-book/src/ch10-transports.md | awk '/^```/{f=!f; next} f' | shasum -a 256 | cut -d' ' -f1)" = "$(awk '/^```/{f=!f; next} f' pmcp-book/src/ch10-transports.md | shasum -a 256 | cut -d' ' -f1)"</automated>
  </verify>
  <acceptance_criteria>
    - The fenced-code digest of `pmcp-book/src/ch10-transports.md` is IDENTICAL before and after: `git show HEAD:pmcp-book/src/ch10-transports.md | awk '/^```/{f=!f; next} f' | shasum -a 256` equals `awk '/^```/{f=!f; next} f' pmcp-book/src/ch10-transports.md | shasum -a 256`
    - The same digest equality holds for `pmcp-book/src/ch10-03-streamable-http.md`
    - `grep -c 'Era note' pmcp-book/src/ch10-transports.md` equals `1`
    - `grep -c '2026-07-28' pmcp-book/src/ch10-transports.md` is at least `2` (the era-aware header line plus the era note); previously it was `0`
    - `grep -c '2026-07-28' pmcp-book/src/ch10-03-streamable-http.md` is at least `1`; previously it was `0`
    - `grep -c 'Last-Event-Id' pmcp-book/src/ch10-transports.md` is at least `4` — the three pre-existing occurrences are preserved and the era note adds at least one
    - `grep -c 's47_v2_stateless_mrtr' pmcp-book/src/ch10-transports.md` is at least `1`
    - `grep -c 'ch12-17-migrating-to-mcp-2026-07-28.md' pmcp-book/src/ch10-transports.md` is at least `1` and the same for `ch10-03-streamable-http.md`
    - `grep -c '^## ' pmcp-book/src/ch10-transports.md` equals `18` — the section count is unchanged, proving no taxonomy rewrite
    - `grep -c '^## ' pmcp-book/src/ch10-03-streamable-http.md` equals `6` — unchanged
    - `grep -c 'session_id_generator: None' pmcp-book/src/ch10-transports.md` is unchanged from its committed value
    - `git diff --numstat pmcp-book/src/ch10-transports.md` reports at most `2` removed lines (the two era-aware header bullets are line replacements) and `git diff --numstat pmcp-book/src/ch10-03-streamable-http.md` reports at most `1`
    - `cd pmcp-book && mdbook build` exits 0
  </acceptance_criteria>
  <done>Both transport chapters name both eras, carry the D-05 head callout, and `ch10-transports.md` carries one era note covering build-time modes, per-request v2 statelessness AND `Last-Event-Id` resumption — with the fenced-code digest of each file provably unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transport chapter → reader's server configuration | The modes taxonomy is followed literally when configuring a production server. Teaching that v2 statelessness requires disabling v1 sessions produces a deployment that is wrong in a way the reader cannot see. |
| Tasks chapter → downstream client implementers | Wire values read as settled are implemented against. If the upstream extension schema changes, every such implementation breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-38 | Information disclosure | docs implying v2 offers sessions or `Last-Event-Id` resumption, so a reader builds a server that leaks continuation state across callers | high | mitigate | The era note explicitly names SSE resumption via `Last-Event-Id` as a v1 mechanism and per-request statelessness as orthogonal to the build-time modes; an acceptance criterion requires the note's presence and preserves all pre-existing occurrences. |
| T-119-39 | Tampering | a code-block edit slipping in under a "minimal-touch" label | medium | mitigate | A fenced-code digest is computed from `HEAD` and from the working tree and must match exactly, for both files. This converts D-16's acceptance criterion from a judgement into a check. |
| T-119-40 | Repudiation | asserting the v2 Tasks wire is final while `ext-tasks` publishes only `draft/` | high | mitigate | The provisionality callout is mandatory and its presence is an acceptance criterion; a grep for TASK-0x-plus-complete/final phrasing must return zero. |
| T-119-41 | Tampering | "era-correcting" the two JSON timestamps in `ch12-7-tasks.md` | low | mitigate | An acceptance criterion asserts both `2025-11-25T10:30:00Z` occurrences survive; the prohibition names them explicitly as timestamps. |
| T-119-42 | Denial of service | a taxonomy rewrite expanding a documentation phase into a chapter rewrite | medium | mitigate | Acceptance criteria pin the `##` section counts of both transport chapters and cap removed lines; option C was costed and rejected. |
| T-119-43 | Information disclosure | an internal `.planning/` path shipping in published documentation | low | mitigate | An acceptance criterion requires zero `.planning/` occurrences in `ch12-7-tasks.md`. |
| T-119-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; this plan edits markdown only. |
</threat_model>

<verification>
1. Fenced-code digest equality for `pmcp-book/src/ch10-transports.md` between `HEAD` and the working tree.
2. Fenced-code digest equality for `pmcp-book/src/ch10-03-streamable-http.md`.
3. `git diff --numstat pmcp-book/src/ch12-7-tasks.md` — zero removed lines.
4. `cargo build --example s50_v2_tasks_server` and `cargo build --example s51_v2_tasks_agent` — both exit 0 with NO feature flag, confirming the citation form.
5. `cd pmcp-book && mdbook build` — exits 0.
6. `make quality-gate` — exits 0 before the commit.

Note the awk/shasum digest checks pipe only `git`, `awk`, `shasum` and `cut` — none is a
build/test invocation, so no `pipefail` wrapper is required for them. Never pipe `cargo` or `make`
without one.
</verification>

<success_criteria>
- `ch12-7-tasks.md` carries an additive era-delta section naming the extensions-map negotiation, BOTH v2 retirements, the security rationale for the `tasks/list` retirement, `tasks/update`, the task result type, and the two runnable v2 examples cited without a feature flag.
- That section carries a reader-facing provisionality callout, and no `.planning/` path ships.
- Zero lines are removed from `ch12-7-tasks.md`; both JSON timestamps survive.
- Both transport chapters name both eras and carry the D-05 head callout.
- `ch10-transports.md` carries exactly one era note covering v1 build-time modes, per-request v2 statelessness, and `Last-Event-Id` resumption as a v1 mechanism.
- The fenced-code digest of each transport chapter is byte-identical before and after.
- Section counts in both transport chapters are unchanged; the book builds; `make quality-gate` exits 0.
</success_criteria>

<output>
Create `.planning/phases/119-documentation-three-shapes-v2-migration/119-08-SUMMARY.md` when done.
</output>
