---
phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
plan: 02
type: execute
wave: 2
depends_on: [118.1-01]
files_modified:
  - tests/embedded_resource_golden.rs
  - fuzz/fuzz_targets/content_tolerant_reader.rs
  - fuzz/Cargo.toml
autonomous: true
requirements: [CONF-04]

must_haves:
  truths:
    - "A byte-literal golden pins the spec EmbeddedResource shape in the tool-result and prompt-message positions on both eras"
    - "A byte-literal golden pins the flat {uri, mimeType, blob} shape for resources/read on a binary resource"
    - "Every golden literal cites the schema.ts line it was hand-derived from"
    - "All new goldens FAIL on the unfixed tree and the failure text is captured verbatim"
    - "A fuzz target exists for the tolerant reader and is registered in fuzz/Cargo.toml"
    - "Every recorded-wire-bytes site in the repo that could flip under G-1 has been enumerated"
  artifacts:
    - path: "tests/embedded_resource_golden.rs"
      provides: "D-04/D-11 spec-derived byte-literal goldens, RED on the unfixed tree"
      min_lines: 150
    - path: "fuzz/fuzz_targets/content_tolerant_reader.rs"
      provides: "no-panic plus fixed-point invariants for the D-03 reader"
      min_lines: 20
    - path: "fuzz/Cargo.toml"
      provides: "[[bin]] registration for content_tolerant_reader"
      contains: "content_tolerant_reader"
  key_links:
    - from: "tests/embedded_resource_golden.rs"
      to: "schema/vendored/core-2026-07-28/schema.ts"
      via: "per-literal provenance comment citing a line number"
      pattern: "schema.ts:1(5[0-9][0-9]|7[0-9][0-9])"
---

<objective>
Build the CONF-04 fences BEFORE the fix and prove they are RED on the unfixed tree. D-04 makes this
non-negotiable: every golden byte literal is hand-derived from the vendored schema and must be
demonstrated failing against today's flat emitter, with the failure captured verbatim.

Purpose: `cargo semver-checks` and `cargo public-api` are structurally blind to a wire reshape, and
`tests/v1_lists_golden.rs` pins no tool result carrying an embedded resource — the blind spot sits
exactly where plan 03's fix lands. This plan closes it before the fix can hide in it.

Output: `tests/embedded_resource_golden.rs`, `fuzz/fuzz_targets/content_tolerant_reader.rs`, a
`fuzz/Cargo.toml` registration, and a recorded-bytes inventory in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-PATTERNS.md
@schema/vendored/core-2026-07-28/schema.ts
@tests/v1_lists_golden.rs
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the tree; no exploration required. -->

The spec shapes the literals derive from — `schema/vendored/core-2026-07-28/schema.ts`:
- `:1514` `interface ResourceContents { uri: string; mimeType?: string; _meta?: MetaObject; }`
- `:1535` `interface TextResourceContents extends ResourceContents { text: string; }`
- `:1548` `interface BlobResourceContents extends ResourceContents { blob: string; }`
- `:1734` `interface EmbeddedResource { type: "resource"; resource: TextResourceContents | BlobResourceContents; annotations?: Annotations; _meta?: MetaObject; }`

Harness helpers already available to an integration test:
- `tests/common/v2.rs` exports `post`, `post_raw`, `get`, `delete`, `header`, `v1_body`, `v2_body`,
  `v2_headers`, `spawn_default_config`, `spawn_stateless_config`, `teardown`,
  `default_client_capabilities`, `build_v2_server`, and the constants `V1`, `V2`, `ALLOW`,
  `REQUEST_META_KEY`, `META_PROTOCOL_VERSION`, `META_CLIENT_INFO`, `META_CLIENT_CAPABILITIES`.
- `Resp` fields: `status`, `mcp_method`, `mcp_name`, `mcp_version`, `mcp_session_id`,
  `content_type`, `allow`, `body` (SSE-unwrapped), `raw` (verbatim bytes).
- `tests/v1_lists_golden.rs` exports (within its own crate) `DynamicField`, `substitute`,
  `assert_v1_bytes`, `key_occurrences`, `v1_leak_guard`, `wire_break_message` — a Rust integration
  test is its own crate, so these must be RESTATED in the new file, not imported.

Fuzz registration form — `fuzz/Cargo.toml` currently ends each target with a four-key `[[bin]]`
block: `name`, `path`, `test = false`, `doc = false`, `bench = false`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inventory every recorded-wire-bytes site that G-1 could flip</name>
  <files>(no files edited — this task produces the SUMMARY inventory that plan 03 acts on)</files>
  <read_first>
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-RESEARCH.md — section "Runtime State Inventory" and assumption A5
    - tests/v1_lists_golden.rs — lines 1-130 (the file's own "do not re-record" rule) and the
      `RESOURCES_READ` literal near line 506
    - crates/pmcp-team-servers/tests/conformance.rs — the fixture replay driver and
      `regenerate_tools_list_fixtures`
  </read_first>
  <action>
    Enumerate, with file and line, every place in the repository that records embedded-resource
    wire bytes and could therefore flip when plan 03 changes `Content::Resource`'s emitter. Run at
    least these four searches and record their full output in the SUMMARY:

    - `git grep -n '"type":"resource"'` and `git grep -n '\\"type\\":\\"resource'` — JSON literals
      in tests and fixtures
    - `git grep -ln 'type.*resource' -- contracts/team-servers/fixtures` — the 33-fixture corpus
      across approval-mcp, mem-mcp, team-fs and team-mcp
    - `git grep -n 'Content::Resource'` restricted to `tests/`, `benches/`, `examples/` and
      `crates/` — the sites that construct or exhaustively pattern-match the variant
    - `git grep -n 'resource_with_text\|resource_with_blob'`

    Classify each hit into exactly one of: WILL FLIP (records bytes in a tool-result or
    prompt-message position), MUST NOT FLIP (records the flat `ReadResourceResult.contents` shape
    D-01 keeps — `tests/v1_lists_golden.rs` `RESOURCES_READ` is the named example), or COMPILE-ONLY
    (constructs or matches the variant, so it is affected by plan 03's field additions rather than
    by the wire change).

    State explicitly in the SUMMARY whether any `contracts/team-servers/fixtures/` fixture is in the
    WILL FLIP class. That answer determines whether plan 03 must regenerate fixtures, and RESEARCH
    assumption A5 is exactly the thing being falsified here.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'git grep -c "type.*resource" -- contracts/team-servers/fixtures | head -20; echo "inventory-search-ran"'</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a table with one row per hit, each row carrying file, line and one of
      the three classifications WILL FLIP / MUST NOT FLIP / COMPILE-ONLY
    - `tests/v1_lists_golden.rs` `RESOURCES_READ` appears in the table classified MUST NOT FLIP
    - The SUMMARY states, in one sentence, whether any `contracts/team-servers/fixtures/` fixture is
      WILL FLIP, and names them if so
    - No repository file is modified by this task (`git status --porcelain` unchanged)
  </acceptance_criteria>
  <done>Plan 03 has a complete, classified list of every byte-recording site it must either leave alone or regenerate.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/embedded_resource_golden.rs with spec-derived literals and demonstrate it RED</name>
  <files>tests/embedded_resource_golden.rs</files>
  <read_first>
    - tests/v1_lists_golden.rs — READ IN FULL: the file gate at line 61, the width-preserving
      normalization invariant (lines 97-130 and 253-283), `assert_v1_bytes`, the `v1_leak_guard`
      returns-Result technique (lines 294-332), the literal-plus-provenance-comment style at 506-512,
      the determinism constraint at 49-60 (register exactly ONE tool and ONE prompt), and the
      fixture-server plus `round_trip` shape at 440-478
    - tests/common/v2.rs — `spawn_stateless_config`, `spawn_default_config`, `post`, `teardown`, `Resp`
    - schema/vendored/core-2026-07-28/schema.ts — lines 1514-1553 and 1734-1748
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md — D-04 and D-11
  </read_first>
  <action>
    Create `tests/embedded_resource_golden.rs` modelled on `tests/v1_lists_golden.rs`, carrying the
    same file gate (`streamable-http`, `http-client`, not wasm32), the same restated harness
    (`DynamicField`, `substitute`, `key_occurrences`, width-preserving assertion, a leak guard that
    returns `Result` so a dedicated test proves it FIRES), and the same
    raw-string-not-parsed-JSON comparison.

    Pin FOUR byte literals, each preceded by a provenance comment naming the exact `schema.ts` line
    it was hand-derived from. Type them by hand from the schema — NEVER from pmcp's serializer
    output, which is the fence-restates-the-rule shape that reopened Phase 115 four times.

    1. TOOL_RESULT_EMBEDDED — a `tools/call` result whose single content item is an embedded text
       resource. Target shape, key order following schema declaration order under `preserve_order`:
       `type`, then `resource` containing `uri`, `mimeType`, `text`. Derived from schema.ts:1734-1748
       plus 1514-1539.
    2. PROMPT_MESSAGE_EMBEDDED — a `prompts/get` result whose single message content is the same
       embedded resource, same nesting. Derived from the same lines.
    3. RESOURCES_READ_BINARY — a `resources/read` result on a binary resource, FLAT (no `type` tag),
       key order `uri`, `mimeType`, `blob`. Derived from schema.ts:1514-1526 plus 1548-1553. This
       is the D-01 boundary: `ReadResourceResult.contents` stays flat and gains `blob`.
    4. TOOL_RESULT_EMBEDDED_ANNOTATED — the tool-result shape with a content-level `annotations`
       object present, asserting `annotations` sits OUTSIDE `resource`, per schema.ts:1740-1744.

    Run each literal against BOTH eras: a v1 round trip via `spawn_stateless_config` plus `v1_body`
    and a v2 round trip via `spawn_default_config` plus `v2_body` with a well-formed `_meta`. State
    in a module comment which spawn each leg uses and why, following the spawn-choice doctrine at
    `tests/v2_stateless_http.rs:9-23`.

    Add a negative-control test that proves the new leak guard FIRES: feed it synthetic bytes
    carrying a flat `uri` at content level in a tool result and assert the guard returns `Err`.
    Without it the guard is vacuous until plan 03 lands.

    Register exactly ONE tool and ONE prompt on the fixture server — `tools/list` and `prompts/list`
    iterate a `HashMap`, so more than one makes the golden non-deterministic across runs.

    Then RUN the new binary on the UNFIXED tree and capture the failure output VERBATIM into the
    SUMMARY. This is the D-04 RED evidence and it is non-negotiable. Expect the three
    embedded-resource literals to fail with a flat-versus-nested diff and RESOURCES_READ_BINARY to
    fail on a missing `blob`. If any of the four PASSES on the unfixed tree, the literal was derived
    from pmcp rather than from the schema — rewrite it.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo nextest run --features full -E "binary(embedded_resource_golden)" 2>&1 | tee target/118.1-02-red.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* (test|tests) (run|failed)" target/118.1-02-red.log || { echo "ZERO TESTS SELECTED — selector is wrong"; exit 1; }; test "$rc" -ne 0 || { echo "FENCE IS NOT RED ON THE UNFIXED TREE"; exit 1; }; echo "RED confirmed"'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/embedded_resource_golden.rs` exists and compiles
    - The nextest run selects a NON-ZERO number of tests (the log line is asserted, not assumed —
      `binary(...)`, never `test(...)`, which silently selects zero)
    - The run exits NON-ZERO on the unfixed tree and `target/118.1-02-red.log` is captured verbatim
      into the SUMMARY
    - Each of the four literals is preceded by a comment containing the string `schema.ts:` and a
      line number in the ranges 678-696, 1514-1553 or 1734-1748
    - The negative-control test for the leak guard is present and PASSES (it asserts `Err`)
    - `grep -c 'test(' tests/embedded_resource_golden.rs` is irrelevant; the plan's verify command
      uses `binary(embedded_resource_golden)` and that string appears in the SUMMARY
  </acceptance_criteria>
  <done>Four spec-derived byte literals exist, all four run, and the embedded-resource ones fail RED on the unfixed tree with the failure captured.</done>
</task>

<task type="auto">
  <name>Task 3: Add the content_tolerant_reader fuzz target and register it</name>
  <files>fuzz/fuzz_targets/content_tolerant_reader.rs, fuzz/Cargo.toml</files>
  <read_first>
    - fuzz/fuzz_targets/fuzz_peer_handle.rs — READ IN FULL (25 lines): the parse-or-return-early
      then `let _ =` each surface shape
    - fuzz/Cargo.toml — the `[[bin]]` block for `fuzz_peer_handle` near line 120
    - src/types/content.rs — lines 55-110 (the `Content` enum as it stands today) and 318-413
      (`resource_contents_serde`, including its existing tolerant `Vec<Value>` pre-pass)
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-RESEARCH.md — the Security Domain row on the tolerant reader as a new untrusted parse surface
  </read_first>
  <action>
    Create `fuzz/fuzz_targets/content_tolerant_reader.rs` in the shape of `fuzz_peer_handle.rs`:
    parse the input bytes as `serde_json::Value`, return early on parse failure, then exercise the
    `Content` deserialization surface.

    Assert three invariants beyond no-panic, the two extra ones being the reason this target exists
    rather than a generic serde fuzz:
    1. NO PANIC on arbitrary JSON — `serde_json::from_value::<Content>` may only return `Ok` or `Err`.
    2. FIXED POINT — when the input parses to a `Content`, re-serializing and re-deserializing yields
       an equal value (nested in, nested out).
    3. FLAT ACCEPTANCE — when the input is a flat legacy resource object (has `type: "resource"` and
       a top-level `uri`), it must parse; re-serializing it must produce the nested spec shape with
       a `resource` object and no top-level `uri`.

    Invariants 2 and 3 will not hold until plan 03 lands. Write them so they are ACTIVE — do not
    comment them out and do not guard them behind a feature. Note in the file header that the target
    is expected to find counterexamples on the unfixed tree and that this is the RED evidence, and
    record in the SUMMARY that the target is deliberately red until plan 03.

    Register the target in `fuzz/Cargo.toml` by copying the `fuzz_peer_handle` `[[bin]]` block
    verbatim and changing `name` and `path` to `content_tolerant_reader`.

    Verify the target BUILDS on nightly. Do not run a long campaign here — plan 03 runs the
    `-max_total_time=300` campaign after the fix, per the CLAUDE.md ALWAYS-fuzz requirement.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo +nightly fuzz build content_tolerant_reader 2>&1 | tee target/118.1-02-fuzzbuild.log; rc=${PIPESTATUS[0]}; test "$rc" -eq 0 || exit 1; grep -q "content_tolerant_reader" fuzz/Cargo.toml || { echo "target not registered"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `fuzz/fuzz_targets/content_tolerant_reader.rs` exists and `cargo +nightly fuzz build content_tolerant_reader` exits 0
    - `fuzz/Cargo.toml` contains a `[[bin]]` block with `name = "content_tolerant_reader"` and
      `path = "fuzz_targets/content_tolerant_reader.rs"` plus `test = false`, `doc = false`, `bench = false`
    - The file body contains all three invariants as executable assertions, none commented out
    - The SUMMARY states that invariants 2 and 3 are expected red until plan 03 and names them
  </acceptance_criteria>
  <done>A registered, building fuzz target encodes the tolerant reader's three invariants; the two that depend on the fix are active and known-red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arbitrary JSON to `Content::deserialize` | The D-03 reader will parse untrusted server output when pmcp acts as a client; this plan builds the fuzz harness that guards it |
| loopback HTTP to the golden fixture server | Test-only; the fixture server binds `127.0.0.1:0` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.1-02-01 | Repudiation | A golden literal derived from pmcp's own serializer would pass green on a wire break | mitigate | Every literal carries a `schema.ts:` provenance comment; the acceptance criteria require the run to be RED on the unfixed tree, which a self-derived literal cannot be |
| T-118.1-02-02 | Denial of Service | The tolerant reader is a new untrusted parse surface (memory amplification via hybrid flat/nested payloads) | mitigate | `fuzz/fuzz_targets/content_tolerant_reader.rs` asserts no-panic on arbitrary JSON; plan 03 runs the campaign at `-max_total_time=300` minimum |
| T-118.1-02-03 | Repudiation | A vacuous leak guard that never fires would read as protection | mitigate | A negative-control test feeds synthetic flat-at-content-level bytes and asserts the guard returns `Err` |
| T-118.1-02-04 | Repudiation | `nextest -E 'test(...)'` selects zero tests and exits 0 | mitigate | Every verify command uses `binary(...)` and asserts a non-zero executed-test count from the captured log |
| T-118.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan; `cargo +nightly fuzz build` compiles already-vendored dependencies from the committed `Cargo.lock` |
</threat_model>

<verification>
- `cargo nextest run --features full -E 'binary(embedded_resource_golden)'` runs a non-zero number
  of tests and exits non-zero, with the output captured
- `cargo +nightly fuzz build content_tolerant_reader` exits 0
- `cargo nextest run --features full -E 'binary(v1_lists_golden)'` still exits 0 — this plan adds
  no production code and must not disturb the existing goldens
- `make quality-gate` is NOT expected to be green at the end of this plan, because the new golden is
  deliberately red; record that explicitly rather than weakening the fence
</verification>

<success_criteria>
- Four spec-derived byte literals exist, cite their schema lines, and are demonstrated RED
- A registered fuzz target encodes the tolerant reader's three invariants
- A classified inventory of every recorded-wire-bytes site is in the SUMMARY, with an explicit
  answer on the team-servers fixture corpus
</success_criteria>

<output>
Create `.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-02-SUMMARY.md` when done
</output>
