---
phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
plan: 09
type: execute
wave: 7
depends_on: [118.1-08]
files_modified:
  - src/shared/peer.rs
  - src/server/peer_impl.rs
  - src/server/cancellation.rs
  - src/server/core.rs
  - src/server/mod.rs
  - tests/tool_output_result_http.rs
  - tests/in_tool_peer_roundtrip.rs
  - fuzz/fuzz_targets/fuzz_peer_handle.rs
autonomous: true
requirements: [CONF-07]

must_haves:
  truths:
    - "PeerHandle::elicit exists as an additive trait method with a default body, so external implementors keep compiling"
    - "DispatchPeerHandle::elicit dispatches ServerRequest::ElicitationCreate and decodes ElicitResult"
    - "set_result_meta survives the ToolOutput::Result verbatim path on BOTH dispatchers"
    - "The two rustdoc paragraphs asserting the old drop contract are updated, not left stale"
  artifacts:
    - path: "src/shared/peer.rs"
      provides: "elicit trait method with a default body and a working rustdoc example"
      contains: "async fn elicit"
    - path: "src/server/peer_impl.rs"
      provides: "DispatchPeerHandle::elicit over the single correlation authority"
      contains: "ElicitationCreate"
  key_links:
    - from: "PeerHandle::elicit"
      to: "ServerRequestDispatcher::dispatch"
      via: "ServerRequest::ElicitationCreate, no second correlation authority"
      pattern: "ElicitationCreate"
    - from: "extra.set_result_meta"
      to: "CallToolResult._meta on the verbatim path"
      via: "merge_result_meta drained before the early return, both dispatchers"
      pattern: "merge_result_meta"
---

<objective>
Land the two CONF-07 pieces that do NOT touch the HTTP transport, so plan 10's transport surgery has
a settled peer surface underneath it: `PeerHandle::elicit` (D-07) and the `set_result_meta` drain on
the `ToolOutput::Result` verbatim path (D-06).

Purpose: G-3 is the whole server-to-client back-channel, and its elicitation scenarios run straight
through the verbatim path. Fixing the transport without the drain may only relocate the failure.
`elicit` uses the same seam that makes sampling and roots work, so excluding it would leave G-3's
elicitation scenarios red while its siblings go green.

Output: an additive trait method, its dispatch-backed implementation, the drain in both dispatchers,
and the two stale rustdoc paragraphs corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-PATTERNS.md
@src/shared/peer.rs
@src/server/peer_impl.rs
</context>

<interfaces>
<!-- Contracts the executor needs. No exploration required. -->

The additive-trait-method precedent, IN THE SAME FILE — `src/shared/peer.rs:61-81`,
`async fn sample_with_tools(&self, params: CreateMessageParams) -> Result<CreateMessageResultWithTools>`
with a default body delegating to `self.sample(params)`. Its rustdoc paragraph explicitly states the
semver mechanism: "This is an ADDITIVE trait method with a default body … Existing `PeerHandle`
implementors therefore keep compiling unchanged; the dispatch-backed `DispatchPeerHandle` overrides
it". Copy that paragraph's STRUCTURE, not just the code.

`elicit` has no `sample`-shaped fallback, so its default body must return a protocol error rather
than silently succeeding. The semver mechanism (default body implies minor) is identical.

The dispatch-then-typed-decode shape — `src/server/peer_impl.rs:57-106`, `list_roots`:
dispatch a `ServerRequest`, then `serde_json::from_value::<T>(value)` mapping a decode failure to
`Error::protocol(ErrorCode::INTERNAL_ERROR, format!("Invalid … response: {e}"))`.

The request variant already exists: `ServerRequest::ElicitationCreate(Box<ElicitRequestParams>)` at
`src/types/protocol/mod.rs:564`. The result type is `ElicitResult` at `src/types/elicitation.rs:132`
with `ElicitAction` at `:143`.

The module-doc anti-pattern to honour — `src/server/peer_impl.rs:4-9`: "`DispatchPeerHandle` does
NOT own a channel. It holds an `Arc<ServerRequestDispatcher>` … The dispatcher owns the correlation
layer … This avoids the anti-pattern of ad-hoc per-site channel construction: every peer handle
shares the single correlation authority."

The unit-test harness for the new arm — `src/server/peer_impl.rs:132-141` and `:187-196`: build a
dispatcher over an `mpsc` channel with an explicit timeout via
`ServerRequestDispatcher::new_with_channel(tx).with_timeout(Duration::from_secs(2))`, drive the call
in a `tokio::spawn`, drain `(cid, req)` off the receiver, answer via
`dispatcher.handle_response(&cid, response)`.

The D-06 drop, both sites, both early returns:
- `src/server/core.rs:702-704` — `DispatchOutput::Verbatim(call_result) => { return Ok(ToolCallOutcome::Result(call_result)); }`
- `src/server/mod.rs:2187-2189` — `DispatchOutput::Verbatim(call_result) => { return Ok(serde_json::to_value(call_result)?); }`

The drain helper that already exists — `src/server/cancellation.rs:729-742`,
`pub(crate) fn merge_result_meta(result: &mut CallToolResult, meta: Map<String, Value>)` with
handler-key-wins precedence, never a whole-map replace. The handle is already captured before the
move on both sites: `let result_meta_handle = extra.result_meta_handle();` at `core.rs:657` and
`mod.rs:2107`. So the fix is "call the existing helper before the existing early return", not new
plumbing.

The two rustdoc paragraphs that assert the OLD contract and will become false:
`src/server/mod.rs:2300-2305` ("the verbatim `ToolOutput::Result` arm above returns earlier and owns
its own `_meta`") and `src/server/cancellation.rs:557-572`, which mentions the `ToolOutput::Result`
path explicitly. A stale doc here is exactly what a reviewer treats as the spec.

The existing D-06 fence model: `tests/tool_output_result_http.rs:1-27` already reads the RAW wire
`Value` off `TransportMessage::Response` — not a deserialized `CallToolResult` — to prove a
top-level `result._meta` survives dispatch on the `ToolOutput::Result` path. That is this fence, one
field different.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the set_result_meta drop on the verbatim path, then fix it in both dispatchers</name>
  <files>tests/tool_output_result_http.rs, src/server/core.rs, src/server/mod.rs, src/server/cancellation.rs</files>
  <behavior>
    - A tool handler that calls `extra.set_result_meta(key, value)` and returns `ToolOutput::Result`
      produces a wire response whose `result._meta[key]` is present
    - Handler-key-wins precedence holds: a handler key with the same name as a dispatch-built key
      overwrites it, while unrelated widget and native keys survive
    - The same is true on BOTH the `Server` dispatcher and the `ServerCore` dispatcher
    - A handler that sets NO result meta produces a response byte-identical to today's
  </behavior>
  <read_first>
    - tests/tool_output_result_http.rs — READ IN FULL (8.7K): the ephemeral-port spawn at 100-110, the
      raw-`Value`-off-`TransportMessage::Response` technique, and the doctrine comment at 22-26
    - src/server/core.rs — lines 650-720: the `result_meta_handle` capture at 657, the Verbatim arm at
      702-704 and the Payload drain at 857-866 for the shape to mirror
    - src/server/mod.rs — lines 2100-2200: the handle capture at 2107 and the Verbatim arm at 2187-2189
    - src/server/mod.rs — lines 2295-2310: the rustdoc paragraph asserting the old contract
    - src/server/cancellation.rs — lines 550-580 (the rustdoc naming the `ToolOutput::Result` path)
      and 720-745 (`merge_result_meta`)
  </read_first>
  <action>
    Extend `tests/tool_output_result_http.rs` — do not create a new file; this is the file that
    already owns the raw-wire assertion for this exact path.

    Add a tool handler that calls `extra.set_result_meta(...)` with a distinctive key and then returns
    `ToolOutput::Result`, and assert the emitted wire `Value` carries `result._meta[key]`. Add a
    second case asserting handler-key-wins against a dispatch-built key of the same name while an
    unrelated key survives. Add a `ServerCore` case so the twin is covered too. Read the RAW `Value`,
    not a deserialized `CallToolResult` — a deserialize round-trip would hide an emission bug.

    RUN it and capture the RED output verbatim before touching `src/`.

    Then fix both sites: drain the already-captured `result_meta_handle` and call the existing
    `merge_result_meta` BEFORE each early return. Nothing new is plumbed — the handle is captured at
    `core.rs:657` and `mod.rs:2107` today, it is simply never drained on this arm.

    Then update the two rustdoc paragraphs that assert the old contract, at `src/server/mod.rs:2300-2305`
    and `src/server/cancellation.rs:557-572`. They currently say the verbatim arm returns earlier and
    owns its own `_meta`; after this change that is false. Rewrite them to state the new contract —
    the verbatim arm still bypasses response middleware, the create-path gate and the text-wrap tail,
    but it now DOES drain handler-authored `_meta` with handler-key-wins precedence. Amend the D-04 /
    D-04a comment blocks in place rather than deleting them; they record a user-approved decision and
    D-06 reclassifies one clause of it, not the whole thing.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo nextest run --features full -E "binary(tool_output_result_http)" 2>&1 | tee target/118.1-09-meta.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-09-meta.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$rc" -eq 0 || exit 1; for b in structured_tool_output double_wrap_tripwire; do cargo nextest run --features full -E "binary($b)" 2>&1 | tee "target/118.1-09-$b.log"; r=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" "target/118.1-09-$b.log" || { echo "ZERO TESTS in $b"; exit 1; }; test "$r" -eq 0 || { echo "$b regressed"; exit 1; }; done; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - The RED output for the new cases is captured verbatim into the SUMMARY before the fix
    - `binary(tool_output_result_http)` runs a NON-ZERO count and exits 0 after the fix, covering the
      `Server` case, the `ServerCore` case and the handler-key-wins case
    - `binary(structured_tool_output)` and `binary(double_wrap_tripwire)` still run non-zero and exit 0
    - `git grep -n 'merge_result_meta' src/server/core.rs src/server/mod.rs` shows a call before EACH
      Verbatim early return
    - The rustdoc at `src/server/mod.rs` near 2300 and at `src/server/cancellation.rs` near 557 no
      longer asserts that the verbatim arm owns its own `_meta` exclusively; the SUMMARY quotes both
      old and new text
    - The D-04 / D-04a decision comments are amended in place, not deleted
  </acceptance_criteria>
  <done>D-06's drop is closed on both dispatchers with a raw-wire fence, and the two docs that asserted the old contract now state the new one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PeerHandle::elicit as an additive trait method and implement it on DispatchPeerHandle</name>
  <files>src/shared/peer.rs, src/server/peer_impl.rs, fuzz/fuzz_targets/fuzz_peer_handle.rs</files>
  <behavior>
    - `PeerHandle::elicit(params: ElicitRequestParams) -> Result<ElicitResult>` exists with a DEFAULT
      body that returns a protocol error, so an external implementor that does not override it keeps
      compiling and fails loudly rather than silently succeeding
    - `DispatchPeerHandle::elicit` dispatches `ServerRequest::ElicitationCreate` through the shared
      `ServerRequestDispatcher` and decodes the answer into `ElicitResult`
    - A malformed answer surfaces as `Error::protocol(ErrorCode::INTERNAL_ERROR, ...)`, matching `list_roots`
    - A dispatcher-level timeout on an unanswered elicitation returns an error rather than hanging
  </behavior>
  <read_first>
    - src/shared/peer.rs — READ IN FULL: the module doc at 1-23 (session isolation and the authz
      ordering invariant), the trait at 51-105, and `sample_with_tools`'s default body plus its
      ADDITIVE rustdoc paragraph at 61-81
    - src/server/peer_impl.rs — READ IN FULL: the module doc at 4-9 (the single-correlation-authority
      rule), `sample` and `list_roots` at 57-106, the no-op `progress_notify` at 108-123, and the
      unit-test harness at 132-141 and 187-196
    - src/types/elicitation.rs — lines 100-170: `ElicitRequestParams`, `ElicitResult`, `ElicitAction`
    - src/types/protocol/mod.rs — line 564: `ServerRequest::ElicitationCreate(Box<ElicitRequestParams>)`
  </read_first>
  <action>
    Add `elicit` to the `PeerHandle` trait in `src/shared/peer.rs`, taking `ElicitRequestParams` and
    returning `Result<ElicitResult>`. Give it a DEFAULT body — that is what makes the trait-method
    addition semver-MINOR, the same mechanism `sample_with_tools` uses. Unlike `sample_with_tools`
    there is no sensible fallback, so the default body returns a protocol error naming the missing
    capability. Silently returning a default `ElicitResult` would let a server believe a user
    approved something they never saw, which is a correctness and a security failure, not a
    convenience.

    Write the rustdoc in the shape of `sample_with_tools`'s: state that this is an ADDITIVE trait
    method with a default body, that existing implementors keep compiling, and that
    `DispatchPeerHandle` overrides it. Include a working `rust,no_run` example — `make test-doc` gates it.

    Implement it on `DispatchPeerHandle` in `src/server/peer_impl.rs` in the exact shape of
    `list_roots`: dispatch `ServerRequest::ElicitationCreate(Box::new(params))` through the SHARED
    `Arc<ServerRequestDispatcher>`, then decode with `serde_json::from_value::<ElicitResult>` mapping
    a decode failure to `Error::protocol(ErrorCode::INTERNAL_ERROR, ...)`. Do NOT build a second
    correlation registry — the module doc names that as the anti-pattern the dispatcher exists to
    prevent.

    Restate, do not quietly widen, the module-doc invariants at `src/shared/peer.rs:12-23`. Session
    isolation and "tool-level authz runs BEFORE the dispatch site wires `peer`" both apply to
    `elicit` exactly as they do to `sample`. Plan 10 puts this handle on a multi-session HTTP
    transport, where the isolation claim stops being trivially true — note that forward dependency
    explicitly in the module doc so plan 10 cannot land without confronting it.

    Add unit tests in `peer_impl.rs` using the existing harness: an answered elicitation decodes to
    the expected `ElicitAction`; a malformed answer yields `INTERNAL_ERROR`; an unanswered call hits
    the dispatcher timeout and errors rather than hanging. Every await is timeout-bounded.

    Extend `fuzz/fuzz_targets/fuzz_peer_handle.rs` to include `ElicitResult` in its `let _ = from_value`
    list — the elicitation answer is peer-supplied JSON and belongs on the same fuzzed serde boundary.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo nextest run --features full -E "binary(pmcp)" -- peer_impl 2>&1 | tee target/118.1-09-peer.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-09-peer.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$rc" -eq 0 || exit 1; cargo +nightly fuzz build fuzz_peer_handle 2>&1 | tee target/118.1-09-fuzzbuild.log; frc=${PIPESTATUS[0]}; test "$frc" -eq 0 || { echo "FUZZ TARGET BUILD FAILED"; exit 1; }; grep -q "ElicitResult" fuzz/fuzz_targets/fuzz_peer_handle.rs || { echo "ElicitResult IS NOT ON THE FUZZED SERDE BOUNDARY"; exit 1; }; make test-doc 2>&1 | tee target/118.1-09-testdoc.log; drc=${PIPESTATUS[0]}; test "$drc" -eq 0 || { echo "make test-doc FAILED"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'async fn elicit' src/shared/peer.rs` shows the method WITH a default body
    - The default body returns an error; a test asserts a bare implementor's `elicit` is `Err`, not `Ok`
    - `git grep -n 'ElicitationCreate' src/server/peer_impl.rs` shows the dispatch going through the
      shared `Arc<ServerRequestDispatcher>`; no new channel or pending map is constructed
    - Three unit tests exist: answered-decodes, malformed-yields-INTERNAL_ERROR, unanswered-times-out
    - `make test-doc` passes, covering the new `rust,no_run` example
    - `fuzz_peer_handle` builds and includes `ElicitResult`
    - The `src/shared/peer.rs` module doc notes the forward dependency on plan 10's multi-session
      HTTP transport for the session-isolation claim
    - `cargo semver-checks` shows the trait-method addition as MINOR, not major — quote the result
  </acceptance_criteria>
  <done>`elicit` exists as an additive, dispatch-backed peer method with a loud default, and the session-isolation invariant is restated with its forward dependency named.</done>
</task>

<task type="auto">
  <name>Task 3: Prove elicit end to end on the in-process loop before the HTTP surgery</name>
  <files>tests/in_tool_peer_roundtrip.rs</files>
  <read_first>
    - tests/in_tool_peer_roundtrip.rs — READ IN FULL (15.5K): `in_tool_sample_completes_on_stock_loop`
      at 155-189, `in_tool_list_roots_completes_on_stock_loop` at 195-242, the tool-that-awaits-the-peer
      at 62-82, the timeout-bounded await at 173-179, and `second_request_is_processed_while_first_handler_parks`
      at 271-352
    - src/types/elicitation.rs — `ElicitRequestParams` and `ElicitResult` construction
  </read_first>
  <action>
    Add an `elicit` case to `tests/in_tool_peer_roundtrip.rs` mirroring
    `in_tool_list_roots_completes_on_stock_loop`: a tool handler that calls `extra.peer()?.elicit(...)`,
    driven over `DuplexTransport` plus `server.run()`, with a client-side driver loop that answers the
    inbound `TransportMessage::Request` with an `ElicitResult`.

    This is deliberately the IN-PROCESS loop, not HTTP. 118-07 measured the peer handle to be PRESENT
    under `Server::run()` and ABSENT only under `StreamableHttpServer`, so proving `elicit` here
    separates "the elicit method works" from "the HTTP transport carries it". Plan 10 then has an
    unambiguous baseline: if its HTTP twin fails, the transport is the cause.

    Every await is timeout-bounded at 5 seconds with the message "call must not hang" — a hung test
    looks like a slow test in CI, and this whole cluster is about a deadlock.

    Record in the SUMMARY, in one sentence, that the in-process `elicit` is green and that the HTTP
    twin is plan 10's responsibility, so plan 12's disposition for D-07 can cite this artifact by name.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo nextest run --features full -E "binary(in_tool_peer_roundtrip)" 2>&1 | tee target/118.1-09-inproc.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-09-inproc.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$rc" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - `binary(in_tool_peer_roundtrip)` runs a NON-ZERO count and exits 0, with a new elicit case
    - Every await in the new case is bounded by `tokio::time::timeout` with an explicit message
    - The SUMMARY names the new test and states that the HTTP twin is plan 10's responsibility
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>`elicit` is proved end to end on the working in-process loop, giving plan 10 an unambiguous baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client answer to a server-issued `elicitation/create` | Peer-supplied JSON decoded into `ElicitResult`; an unanswered or forged answer changes what the server believes a user approved |
| handler-authored `_meta` to the wire | The verbatim path now merges handler keys into a result envelope the handler otherwise owns entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.1-09-01 | Spoofing | A default `elicit` body that returned a synthetic success would let a server believe a user approved something they never saw | mitigate | The default body returns a protocol error; a test asserts a bare implementor's `elicit` is `Err`, not `Ok` |
| T-118.1-09-02 | Denial of Service | An unanswered elicitation parking a handler forever | mitigate | `elicit` rides the shared `ServerRequestDispatcher`, which is already timeout-aware; a unit test asserts the unanswered path errors rather than hangs |
| T-118.1-09-03 | Tampering | A second correlation registry would let one client's answer resolve another's pending request | mitigate | `DispatchPeerHandle::elicit` dispatches through the SHARED `Arc<ServerRequestDispatcher>`; the acceptance criteria assert no new channel or pending map is constructed |
| T-118.1-09-04 | Tampering | The `_meta` drain could whole-map-replace a handler-owned envelope, dropping widget or native keys | mitigate | The existing `merge_result_meta` is reused with handler-key-wins precedence, and a test asserts an unrelated key survives |
| T-118.1-09-05 | Repudiation | The session-isolation claim in `src/shared/peer.rs` is trivially true today and stops being true on a multi-session HTTP transport | mitigate | The module doc records the forward dependency by name so plan 10 must confront it; plan 10 carries the per-session routing threat |
| T-118.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan |
</threat_model>

<verification>
- `binary(tool_output_result_http)` non-zero count, exit 0 — both dispatchers, handler-key-wins covered
- `binary(structured_tool_output)` and `binary(double_wrap_tripwire)` non-zero, exit 0
- `binary(in_tool_peer_roundtrip)` non-zero, exit 0 with the new elicit case
- `pmcp` lib tests filtered to `peer_impl` non-zero, exit 0
- `make test-doc` passes; `cargo +nightly fuzz build fuzz_peer_handle` exits 0
- `cargo semver-checks` shows the trait addition as MINOR
- `make quality-gate` exit 0
</verification>

<success_criteria>
- `elicit` exists, is additive, is dispatch-backed, and fails loudly by default
- `set_result_meta` survives the verbatim path on both dispatchers, proved on the raw wire
- The two rustdoc paragraphs asserting the old drop contract now state the new one
- The in-process elicit round trip is green, isolating plan 10's failure mode to the transport
</success_criteria>

<output>
Create `.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-09-SUMMARY.md` when done
</output>
