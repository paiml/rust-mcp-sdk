---
phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
plan: 09
subsystem: server-peer
tags: [conformance, mcp, elicitation, peer-handle, result-meta, conf-07, g-3, d-06, d-07]

# Dependency graph
requires:
  - phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
    plan: 08
    provides: "the settled `ProtocolContext` fold at both dispatch roots; this plan edits the same two dispatch arms and inherits their shape"
provides:
  - "`PeerHandle::elicit` — an ADDITIVE trait method with a default body that returns METHOD_NOT_FOUND, so external implementors keep compiling and fail loudly rather than silently reporting a user approval that never happened"
  - "`DispatchPeerHandle::elicit` — dispatch over the SHARED `Arc<ServerRequestDispatcher>`; no second correlation registry, no new channel, no new pending map"
  - "`set_result_meta` now drains onto the `ToolOutput::Result` verbatim path on BOTH native dispatchers — the D-06 contract flip"
  - "`tests/in_tool_peer_roundtrip.rs` — accept AND decline round trips proving elicit end to end on the stock loop"
  - "`fuzz/fuzz_targets/fuzz_peer_handle.rs` widened to `ElicitRequestParams` (hand-written Deserialize) and `ElicitResult`"
affects: [118.1-10, 118.1-11, 118.1-12, 118.1-13, 118.1-14]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive trait method with a LOUD default body: external implementors keep compiling, but an unimplemented `elicit` errors rather than synthesising a decision"
    - "One correlation authority: every peer method dispatches through the shared `ServerRequestDispatcher` rather than owning a per-site channel/pending map"
    - "Decode failures on peer-supplied JSON FAIL rather than degrading to a default, because a synthesised action reports a decision the user never made"
    - "Assert the pair, not the happy path: a decline is fenced as specifically as an accept, and asserted to be non-observable as an accept"

key-files:
  created: []
  modified:
    - src/shared/peer.rs
    - src/server/peer_impl.rs
    - src/server/cancellation.rs
    - src/server/core.rs
    - src/server/mod.rs
    - tests/in_tool_peer_roundtrip.rs
    - tests/tool_output_result_http.rs
    - tests/tool_with_result.rs
    - fuzz/fuzz_targets/fuzz_peer_handle.rs
    - docs/design/sep-1686-task-augmented-results.md
    - examples/s54_v2_dual_conformance.rs

key-decisions:
  - "`elicit` mirrors `list_roots` exactly — dispatch through the shared `Arc<ServerRequestDispatcher>`, then decode. The stakes make the sameness the point: a second registry could resolve one client's approval against another client's prompt (T-118.1-09-03)"
  - "The default trait body returns METHOD_NOT_FOUND rather than a synthetic success. A default that returned `Ok` would let a server believe a user approved something they never saw (T-118.1-09-01)"
  - "A malformed `ElicitResult` is an INTERNAL_ERROR, never a defaulted action — peer-supplied JSON decides whether the server believes a human approved something"
  - "The D-06 flip is scoped as a HANDLER-`_meta` concern, not a pipeline concern: the verbatim arm still bypasses response middleware, the task create-path gate and the text-wrap tail (D-04 / D-04a). What it no longer bypasses is the handler's own `_meta` slot, authored by the same handler at the same trust level as the envelope it returns"

patterns-established:
  - "Prove the METHOD on the in-process loop before the TRANSPORT surgery: plan 10 inherits a settled answer to 'does elicit work' and is left with only 'does the HTTP transport carry a peer at all'"
  - "When a fix flips a previously-asserted contract, the stale fixture is REWRITTEN with the reason recorded inline, not deleted"

requirements-completed: [CONF-07]

# Metrics
duration: 2h 4m
completed: 2026-08-11
---

# Phase 118.1 Plan 09: Peer Surface for CONF-07 Summary

**`PeerHandle::elicit` now exists and dispatches over the one correlation authority, and `set_result_meta` survives the `ToolOutput::Result` verbatim path on both native dispatchers — the two CONF-07 pieces that do not touch the HTTP transport.**

## Performance

- **Duration:** ~2h 4m (wall clock, including two executor stalls and an inline takeover)
- **Started:** 2026-08-11T19:02:36Z
- **Completed:** 2026-08-11T21:06:59Z
- **Tasks:** 3 completed
- **Files modified:** 11

## Accomplishments

- **G-3's elicitation half is now unblocked at the method level.** `elicit` rides the same seam that already makes `sample` and `list_roots` work, so plan 10's transport surgery lands on a settled peer surface instead of chasing two unknowns at once.
- **The `set_result_meta` drop on the verbatim path is fixed rather than relocated.** G-3's elicitation scenarios run straight through that arm, so a transport fix alone would have left handlers silently shipping no `_meta`.
- **The old contract was retired honestly.** Three artifacts asserted the drop was intentional; all three now record the flip and why, rather than being quietly deleted.

## Task Commits

1. **Task 1: Prove the set_result_meta drop on the verbatim path, then fix it in both dispatchers** — `b86e07ba` (test, RED) → `601df15c` (fix, GREEN)
2. **Task 2: Add PeerHandle::elicit as an additive trait method and implement it on DispatchPeerHandle** — `f4df9545` (feat, default body) → `2a86a0db` (test, RED) → `2c1ad3e1` (feat, GREEN) → `14b7831b` (test, fuzz)
3. **Task 3: Prove elicit end to end on the in-process loop** — `a858d017` (test)

**Lint fix:** `16d8fe3f` (fix) — clippy `match_wildcard_for_single_variants` in the Task 2 fence; `make lint` is `-D warnings`.

## Files Created/Modified

- `src/shared/peer.rs` — `elicit` trait method, loud default body, rustdoc example, and two `bare_implementor_elicit_is_err_*` unit tests
- `src/server/peer_impl.rs` — `DispatchPeerHandle::elicit` over the shared dispatcher, plus decode/timeout unit tests
- `src/server/core.rs`, `src/server/mod.rs`, `src/server/cancellation.rs` — the `merge_result_meta` drain before the verbatim early return, on both dispatchers
- `tests/tool_output_result_http.rs` — raw-wire fence for the drain, both dispatchers
- `tests/in_tool_peer_roundtrip.rs` — accept and decline elicit round trips on the stock loop
- `tests/tool_with_result.rs` — the FLIPPED fixture; used to assert `_meta` was absent on the Result path
- `fuzz/fuzz_targets/fuzz_peer_handle.rs` — elicitation shapes added to the serde boundary
- `docs/design/sep-1686-task-augmented-results.md`, `examples/s54_v2_dual_conformance.rs` — stale drop-contract prose corrected

## Decisions Made

See `key-decisions` above. The load-bearing one: the D-06 flip is scoped to the handler's own `_meta`, not to the response pipeline — the verbatim arm's other bypasses (middleware, task create-path gate, text-wrap tail) are unchanged.

## Deviations from Plan

### 1. Three files edited that the plan's `files_modified` did not list

- **Found during:** Task 1
- **Issue:** must_have #4 requires "the two rustdoc paragraphs asserting the old drop contract are updated, not left stale", but `files_modified` listed neither the design doc nor the fixtures that encoded the same claim. `tests/tool_with_result.rs` actively asserted the OPPOSITE of the new contract, so it would have failed.
- **Fix:** Updated `docs/design/sep-1686-task-augmented-results.md`, `examples/s54_v2_dual_conformance.rs`, and rewrote the `tests/tool_with_result.rs` fixture with the flip recorded inline.
- **Verification:** `cargo test --test tool_with_result`, `--test tool_output_result_http` (4/4)
- **Committed in:** `601df15c` (part of the Task 1 GREEN commit)

**Total deviations:** 1 (scope implied by must_haves but absent from `files_modified`)
**Impact on plan:** Necessary for correctness — a stale fixture would have failed the build. No scope creep.

## Issues Encountered

**The executor agent stalled twice and plan 09 was finished inline.**

1. First stall: `Unable to connect to API: Self-signed certificate detected` — infrastructure, mid-Task 1. The agent was resumed from its transcript with its worktree intact and completed Task 2.
2. Second stall: the 600s stream watchdog fired with no recovery. Root cause is most likely long silent `cargo … --all-features` compiles in a fresh worktree; the identical command run from the orchestrator also exceeded 600s of silent compilation. The agent was not wedged, just quiet.
3. Takeover: Task 3 was left half-written — `ElicitTool` and the module docs existed, but `ELICIT_ACCEPT_MESSAGE` was referenced and never defined, so the test binary did not compile. The unused `HostElicitationHandler` / `ElicitAction` / `ElicitResult` imports were the tell that the host handler and test cases were missing entirely. Added the constants, `CannedElicitation`, the tool registration, and both round-trip tests.

**A false-green was caught and corrected.** `cargo test … 2>&1 | tail` reports `tail`'s exit code, not cargo's — an earlier run was read as passing when the compile had in fact failed. Subsequent gates used `set -o pipefail` with an explicit `PIPESTATUS` check.

## Self-Check: PASSED

- `cargo fmt --all -- --check` — clean
- `cargo test --lib --all-features peer_impl` — 8 passed, 0 failed
- `cargo test --test tool_output_result_http --all-features` — 4 passed, 0 failed
- `cargo test --test in_tool_peer_roundtrip --all-features` — 7 passed, 0 failed
- `cargo test --doc --all-features shared::peer` — 2 passed, 0 failed
- `make lint` — ✓ No lint issues (the project gate; caught one clippy error the executor's self-check missed)
