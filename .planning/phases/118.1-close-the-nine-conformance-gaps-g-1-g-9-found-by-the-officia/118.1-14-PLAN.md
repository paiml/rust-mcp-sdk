---
phase: 118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia
plan: 14
type: execute
wave: 12
depends_on: [118.1-13]
files_modified:
  - scripts/run-conformance-suite.sh
  - tests/ci_conformance_gate_wiring.rs
  - conformance/package.json
  - conformance/package-lock.json
  - conformance/README.md
  - .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CONF-04, CONF-05, CONF-06, CONF-07, CONF-08]

must_haves:
  truths:
    - "The blocking CI gate covers exactly the surfaces that genuinely pass after re-measurement"
    - "No --expected-failures, no allowlist, no known-failure baseline anywhere in the gate"
    - "The script widening and tests/ci_conformance_gate_wiring.rs land in the SAME commit"
    - "Every check floor moved UP, never down"
    - "The suite is re-pinned to the newest release and the bump delta is reported SEPARATELY from the fix delta"
    - "UNAS-01 gets a phase assignment on evidence, or carries to v2.6 with the measurement as the reason"
  artifacts:
    - path: "scripts/run-conformance-suite.sh"
      provides: "the widened blocking surface plus raised floors"
      contains: "MIN_CHECKS_V1"
    - path: "tests/ci_conformance_gate_wiring.rs"
      provides: "structural pins kept in lockstep with the widened script"
      contains: "ZERO_CHECK_DECLARATIONS"
    - path: "conformance/package.json"
      provides: "the re-pinned suite version"
  key_links:
    - from: "scripts/run-conformance-suite.sh constants"
      to: "tests/ci_conformance_gate_wiring.rs ZERO_CHECK_DECLARATIONS"
      via: "a bijective name mapping the wiring test reads structurally"
      pattern: "MIN_CHECKS_V[12]"
---

<objective>
Widen the blocking CI gate to exactly the surfaces that genuinely pass after plan 13's
re-measurement, keeping `tests/ci_conformance_gate_wiring.rs` in lockstep; then re-pin the suite to
its newest release, re-measure once more, and report the bump's delta SEPARATELY from the fixes'
delta.

Purpose: D-09 says widen to what genuinely passes and carries D-21 forward verbatim — no
`--expected-failures`, no allowlist, no known-failure baseline. D-08 says two clean numbers, never one
confounded one. And D-13 binds UNAS-01's fate to exactly this re-pin: if the newer suite exercises
`x-mcp-header` / `Mcp-Param-{Name}`, it gets a phase assignment on evidence; if not, it carries to
v2.6 with that measurement recorded as the reason.

Output: a widened gate that lands with its wiring test, a re-pinned suite, two separately attributed
deltas, and UNAS-01 decided on measurement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md
@.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-13-SUMMARY.md
@conformance/README.md
</context>

<interfaces>
<!-- Facts the executor needs. No exploration required. -->

The gate's current data constants — `scripts/run-conformance-suite.sh`:
- `ZERO_CHECK_SCORED_SCENARIOS=()` at `:152` — measured EMPTY, and an empty array is the STRONGEST
  state while an ABSENT array cannot fail
- `ZERO_CHECK_NOT_SCORED_SCENARIOS=(...)` at `:164` — the two measured zero-check scenarios
- `MIN_CHECKS_V1=66` at `:187`, `MIN_CHECKS_V2=178` at `:188` — floors on checks EXECUTED, not passed
- `MRTR_SCENARIO_PREFIX="input-required-result-"` at `:197`, `MIN_MRTR_SCENARIOS=14` at `:198` — the
  entire current blocking surface
- `RESULTS_ROOT="target/conformance-results"` at `:204`, `MIN_NODE_MAJOR=22` at `:220`
- `rm -rf "$RESULTS_ROOT"` at `:500` is fence 10
- The floor and zero-check assertions live at `:670-740`, including `assert_set_equality` on BOTH
  zero-check arrays in BOTH directions

The structural pin that must move with it — `tests/ci_conformance_gate_wiring.rs` (60 KB):
`ZERO_CHECK_DECLARATIONS` at `:195-201` lists `"ZERO_CHECK_SCORED_SCENARIOS"`,
`"ZERO_CHECK_NOT_SCORED_SCENARIOS"`, `"MIN_CHECKS_V1"`, `"MIN_CHECKS_V2"`; `MINIMUM_GATE_NEEDS = 8` at
`:136`; `MINIMUM_JOBS = 12` at `:145`; `DEV_DEP_FREE_FENCES` at `:209-215`;
`SPEC_RECHECK_PINNED_SHA` at `:252`; plus `CONFORMANCE_FORBIDDEN_FLAGS`. This test is a BIJECTIVE
job-to-env-to-conditional-to-echo reader built by plan 118-09 — read it before editing the script,
because it pins the script's constant NAMES structurally and the two must land together.

The CI jobs it gates — `.github/workflows/ci.yml:628` (`Official MCP Conformance Suite`) and `:732`
(`Era Matrix`), both listed in the `gate` job's `needs:` array.

The floor rule, `conformance/README.md` section 7 rule 4: a floor is NEVER lowered. Closing gaps
RAISES executed-check counts, because a scenario that gets further runs more checks. Restate a floor
upward in the SAME commit as the change that raises it.

The anti-suppression rule, `conformance/README.md` section 9 and `scripts/run-conformance-suite.sh:28`:
`--expected-failures` and every other shape of known-fail are forbidden. D-21 carries this forward
verbatim and D-09 makes it doubly binding.

The re-pin mechanics: `conformance/package.json` plus a committed `conformance/package-lock.json` at
`0.2.0-alpha.11`. Also carrying pins: `tests/v2_conformance_pin.rs` (a SHA pin) and
`tests/ci_conformance_gate_wiring.rs:252` (`SPEC_RECHECK_PINNED_SHA`). The re-pin task touches all of
these together. Install with `npm ci --ignore-scripts` — never `npm install` into a dirty tree.

D-13's UNAS-01 trigger: measured during discussion, `x-mcp-header` and `Mcp-Param` return ZERO hits in
any Phase-118 measurement artifact and ZERO hits in `src/`, so the suite at alpha-11 does not exercise
it. If the newer release does, UNAS-01 gets a phase assignment THEN, on evidence.

RESEARCH validity note: every check name, probe body and expected code quoted anywhere in the 118.1
research is pinned to 0.2.0-alpha.11. RE-EXTRACT them from the new bundle at re-pin rather than
assuming they carried over.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen the blocking surface and raise the floors, in lockstep with the wiring test</name>
  <files>scripts/run-conformance-suite.sh, tests/ci_conformance_gate_wiring.rs, conformance/README.md</files>
  <read_first>
    - tests/ci_conformance_gate_wiring.rs — READ IT BEFORE EDITING THE SCRIPT: lines 130-160
      (`MINIMUM_GATE_NEEDS`, `MINIMUM_JOBS`), 190-215 (`ZERO_CHECK_DECLARATIONS`, `DEV_DEP_FREE_FENCES`),
      245-260 (`SPEC_RECHECK_PINNED_SHA`), and the `CONFORMANCE_FORBIDDEN_FLAGS` list wherever it sits
    - scripts/run-conformance-suite.sh — lines 1-60, 140-230, 490-520, 660-830
    - conformance/README.md — sections 7 and 9
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-13-SUMMARY.md — the per-leg scenario-level results and the D-21 direction agreed at its checkpoint
    - .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md — the appended disposition section
  </read_first>
  <action>
    Decide the widening MECHANISM first and state it: either an additional prefix list alongside
    `MRTR_SCENARIO_PREFIX`, or an explicit array of scenario names that must be entirely green. An
    explicit array is more legible and more brittle in the right direction; a prefix list scales but
    can silently include a future scenario. Pick one, say why, and note that
    `tests/ci_conformance_gate_wiring.rs` pins the current constant NAMES, so a new constant needs a
    corresponding entry there in the same commit.

    Widen the blocking surface to exactly the scenarios that measured entirely green in plan 13, on
    each leg. Not one more. If a scenario is green but flaky across plan 13's repeated v2 runs, leave
    it out and say so — a flaky scenario in a blocking gate is a worse outcome than a narrower gate.

    Raise `MIN_CHECKS_V1` and `MIN_CHECKS_V2` to plan 13's measured executed-check counts. These are
    floors on checks EXECUTED and closing gaps raises them. Update the provenance comment above them
    (currently citing `118-04-SUMMARY.md` and `118-05-SUMMARY.md`) to cite `118.1-13-SUMMARY.md` and
    its numbers. Never lower a floor. If plan 13 measured a count BELOW the current floor on either
    leg, STOP — that is a regression, not a floor to adjust.

    Re-derive both zero-check arrays from plan 13's results and update them if the measured sets moved.
    `assert_set_equality` runs in BOTH directions, so a stale array fails loudly in either direction —
    which is the design and must not be weakened.

    Apply the D-21 direction agreed at plan 13's checkpoint. If a pass-count assertion is now on the
    table because the v2 oscillation is gone, add it as a floor with the same never-lowered rule. If
    the surface-scoped gate stands, say so and change nothing there. Either way: NO
    `--expected-failures`, NO allowlist, NO known-failure baseline. Verify
    `CONFORMANCE_FORBIDDEN_FLAGS` still forbids them and add any new flag shape you encounter.

    Update `tests/ci_conformance_gate_wiring.rs` in the SAME commit: add any new constant name to
    `ZERO_CHECK_DECLARATIONS`, and raise `MINIMUM_GATE_NEEDS` or `MINIMUM_JOBS` if the widening adds a
    CI job. Those two are non-vacuity floors — a reader that sees almost nothing makes every wiring
    check below it vacuous.

    Update `conformance/README.md` so its stated claim matches the widened gate. The repository's claim
    must be exactly the claim it can defend; that sentence is the whole point of D-21.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'grep -q "0.2.0-alpha.11" conformance/package.json || { echo "pin moved before the widening — do the widening at the held pin"; exit 1; }; grep -rq -- "--expected-failures" scripts/ .github/workflows/ && { echo "ANTI-SUPPRESSION VIOLATION"; exit 1; }; cargo nextest run --features full -E "binary(ci_conformance_gate_wiring)" 2>&1 | tee target/118.1-14-wiring.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-14-wiring.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$rc" -eq 0 || exit 1; bash -n scripts/run-conformance-suite.sh || exit 1; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `binary(ci_conformance_gate_wiring)` runs a NON-ZERO count and exits 0 after the script edit
    - `bash -n scripts/run-conformance-suite.sh` exits 0
    - `MIN_CHECKS_V1` and `MIN_CHECKS_V2` are each greater than or equal to their previous values, and
      the provenance comment cites `118.1-13-SUMMARY.md`
    - `grep -rn -- '--expected-failures' scripts/ .github/workflows/` returns nothing
    - `git show --stat HEAD` proves `scripts/run-conformance-suite.sh` and
      `tests/ci_conformance_gate_wiring.rs` moved in the SAME commit
    - The SUMMARY states the chosen widening mechanism and why, lists every scenario added to the
      blocking surface, and names any green-but-flaky scenario deliberately excluded
    - `conformance/README.md`'s stated claim matches the widened gate
  </acceptance_criteria>
  <done>The gate blocks on exactly what passes, floors moved up only, and the structural wiring test moved with the script in one commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Suite re-pin legitimacy sign-off (D-08)</name>
  <read_first>
    - conformance/package.json and conformance/package-lock.json — the pin being replaced
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-13-SUMMARY.md — the fix delta that must already be banked
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-RESEARCH.md — the Package Legitimacy Audit row for this package
  </read_first>
  <files>(no repository files edited — this task pauses for human verification)</files>
  <action>PAUSE and hand control to the developer. Present the what-built summary and every
    numbered step in the how-to-verify block below, then WAIT for the resume signal. Do NOT
    proceed to the next task and do NOT self-approve: this gate is blocking, and
    `workflow.auto_advance` does not apply to it. Record the developer's verbatim response in
    the SUMMARY.</action>
  <what-built>
    A widened, still-anti-suppression-clean CI gate measured at the HELD pin 0.2.0-alpha.11, and a
    candidate newer release of `@modelcontextprotocol/conformance` identified for the D-08 re-pin.
  </what-built>
  <how-to-verify>
    This is the ONE point in the phase where a new external package version enters the tree. It is the
    official first-party MCP conformance package, already pinned with a committed lockfile, but the
    version is new and a version bump is a supply-chain event.

    1. Run `npm view @modelcontextprotocol/conformance versions` and confirm the target version exists
       and is the newest release.
    2. Open `https://npmjs.com/package/@modelcontextprotocol/conformance` and confirm the package is
       published by the modelcontextprotocol org, that the version's publish date is plausible, and
       that there is no sign of takeover (sudden maintainer change, an unexplained gap, a
       typosquat-adjacent name).
    3. Confirm the fix delta from plan 13 has ALREADY been reported at the old pin. D-08 requires two
       clean numbers; re-pinning before the fix delta is banked confounds them permanently.
    4. Confirm the widened gate in Task 1 was measured at the OLD pin, so the widening is attributable
       to the fixes rather than to the bump.
    5. Approve the specific target version by name.
  </how-to-verify>
  <resume-signal>Type "approved: VERSION" naming the exact version to pin, or "hold" with the reason</resume-signal>
  <verify>
    <human-check>The developer completed the numbered steps above and returned the resume signal.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer returned the resume signal and it is recorded verbatim in the SUMMARY
    - Every numbered step in the how-to-verify block above has a recorded yes/no answer
    - No step was self-approved by the agent; the gate is blocking and `workflow.auto_advance` does not apply
  </acceptance_criteria>
  <done>The developer responded with the resume signal, and that response is recorded verbatim in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin, re-measure, report the bump delta separately, and decide UNAS-01</name>
  <files>conformance/package.json, conformance/package-lock.json, scripts/run-conformance-suite.sh, tests/ci_conformance_gate_wiring.rs, .planning/phases/118-conformance-against-the-official-suite/118-CONFORMANCE-GAPS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - conformance/package.json and conformance/package-lock.json — the current pin and its lockfile
    - conformance/.npmrc — check for `engine-strict` before assuming npm would refuse Node 20
    - tests/v2_conformance_pin.rs — the SHA pin it carries
    - tests/ci_conformance_gate_wiring.rs — line 252, `SPEC_RECHECK_PINNED_SHA`, and the README
      cross-check that reads it at ~1086
    - .planning/REQUIREMENTS.md — the UNAS-01 entry and the rule that it must not be folded into a
      phase without an explicit scoping decision
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-CONTEXT.md — D-08 and D-13
  </read_first>
  <action>
    Re-pin to the version approved at the checkpoint. Update `conformance/package.json`, then install
    with `npm ci --ignore-scripts` under Node 22 and commit the regenerated
    `conformance/package-lock.json`. Never `npm install` into a dirty tree.

    Re-extract the suite's own facts from the NEW bundle rather than assuming they carried over. Every
    check name, probe body and expected code quoted in this phase's research and tests is pinned to
    0.2.0-alpha.11. At minimum re-read, and record any change to: the five-method retirement probe
    loop, the four `_meta` probes and their rpcIds, the `ServerUnsupportedVersionError` correlation
    assertion, the `completion/complete` assertion, and `HttpServerNoIndependentRequestsOnStream`. Any
    in-tree test that encoded a now-changed expectation must be updated in this commit, with each
    change listed.

    Re-measure both legs on the new pin, with the same reproducibility discipline: `rm -rf
    target/conformance-results` first, `bash -o pipefail -c '... | tee target/118.1-14-conf.log'`, exit
    code asserted separately, Node 22.

    Report the BUMP delta SEPARATELY from the fix delta. Two tables, two headings, never one combined
    number. The fix delta is plan 13's (baseline to post-fix at the old pin); the bump delta is this
    one (post-fix at the old pin to post-fix at the new pin). Attribute every moved scenario to one or
    the other. If the bump makes something WORSE, say so plainly — that is exactly the signal the
    two-number discipline exists to surface.

    Raise the floors again if the new pin executes more checks, and keep
    `tests/ci_conformance_gate_wiring.rs` in lockstep in the same commit. Update `SPEC_RECHECK_PINNED_SHA`
    and `tests/v2_conformance_pin.rs` if the new release moves them. Floors go UP only.

    Then decide UNAS-01 per D-13. Search the NEW bundle and the new requirements files for
    `x-mcp-header` and `Mcp-Param`. If the newer suite EXERCISES either, UNAS-01 gets a phase
    assignment now, on evidence — record the scenario names and the assignment in
    `.planning/REQUIREMENTS.md`. If it does not, UNAS-01 carries to v2.6 with THIS measurement recorded
    as the reason, including the exact search commands and their zero-hit output. Either way the entry
    stops being an open question with no evidence attached.

    Finally, append the bump delta and the UNAS-01 verdict to `118-CONFORMANCE-GAPS.md` as a further
    amendment. Append; do not rewrite. If any gap's disposition CHANGES under the new pin — a scenario
    that passed at alpha-11 and fails now, or vice versa — amend that gap's row with a dated note
    rather than editing the original verdict.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'grep -q "0.2.0-alpha.11" conformance/package.json && { echo "pin did not move — D-08 re-pin not done"; exit 1; }; git diff --name-only | grep -q "conformance/package-lock.json" || git diff --cached --name-only | grep -q "conformance/package-lock.json" || { echo "lockfile not regenerated"; exit 1; }; test -f target/118.1-14-conf.log || { echo "second measurement log missing"; exit 1; }; cargo nextest run --features full -E "binary(ci_conformance_gate_wiring)" 2>&1 | tee target/118.1-14-wiring2.log; rc=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-14-wiring2.log || { echo "ZERO TESTS SELECTED"; exit 1; }; test "$rc" -eq 0 || exit 1; cargo nextest run --features full -E "binary(v2_conformance_pin)" 2>&1 | tee target/118.1-14-pin.log; r2=${PIPESTATUS[0]}; grep -qE "[1-9][0-9]* tests? run" target/118.1-14-pin.log || exit 1; test "$r2" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - `conformance/package.json` names the approved version and `conformance/package-lock.json` was
      regenerated by `npm ci --ignore-scripts` and committed
    - `binary(ci_conformance_gate_wiring)` and `binary(v2_conformance_pin)` both run non-zero counts and exit 0
    - The SUMMARY contains TWO separately headed delta tables — the fix delta and the bump delta — with
      every moved scenario attributed to one of them
    - Any suite expectation that changed between pins is listed, with the in-tree test updated in this commit
    - Floors were raised, never lowered; `git show --stat HEAD` proves script and wiring test moved together
    - UNAS-01 carries either a phase assignment with named scenarios, or a carry-to-v2.6 note quoting
      the exact search commands and their zero-hit output
    - `118-CONFORMANCE-GAPS.md` gained an appended bump-delta section with zero deleted lines
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>The suite is re-pinned, both legs re-measured, the two deltas are separately attributable, and UNAS-01 is decided on evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Two-delta review and phase close</name>
  <read_first>
    - This plan's SUMMARY: both delta tables and the UNAS-01 verdict
    - .planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-13-SUMMARY.md — the fix delta
    - scripts/run-conformance-suite.sh and conformance/README.md — the widened surface and the claim it states
  </read_first>
  <files>(no repository files edited — this task pauses for human verification)</files>
  <action>PAUSE and hand control to the developer. Present the what-built summary and every
    numbered step in the how-to-verify block below, then WAIT for the resume signal. Do NOT
    proceed to the next task and do NOT self-approve: this gate is blocking, and
    `workflow.auto_advance` does not apply to it. Record the developer's verbatim response in
    the SUMMARY.</action>
  <what-built>
    A widened blocking CI gate landed in lockstep with its structural wiring test, a suite re-pinned to
    its newest release, both legs re-measured on the new pin, two separately attributed deltas, and a
    decided UNAS-01.
  </what-built>
  <how-to-verify>
    D-08 exists so a human can read two numbers and know which is which. That reading is this task.

    1. Read the FIX delta (plan 13, old pin) and the BUMP delta (plan 14, new pin) side by side.
       Confirm they are separate tables and that every moved scenario is attributed to exactly one.
    2. Confirm nothing got worse under the bump without being called out. If something did, confirm it
       has a disposition amendment on the relevant gap row.
    3. Confirm the gate blocks on exactly what passes: read the widened surface list and spot-check two
       entries against the measurement.
    4. Confirm no suppression crept in — `grep -rn -- '--expected-failures' scripts/ .github/workflows/`
       returns nothing, and neither an allowlist nor a known-failure baseline exists.
    5. Confirm every check floor moved UP or stayed, never down, and that
       `tests/ci_conformance_gate_wiring.rs` moved in the same commit as the script.
    6. Read the UNAS-01 verdict and accept the assignment or the carry-forward.
    7. Confirm `conformance/README.md`'s stated claim matches what the gate now enforces.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or name the item to redo</resume-signal>
  <verify>
    <human-check>The developer completed the numbered steps above and returned the resume signal.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer returned the resume signal and it is recorded verbatim in the SUMMARY
    - Every numbered step in the how-to-verify block above has a recorded yes/no answer
    - No step was self-approved by the agent; the gate is blocking and `workflow.auto_advance` does not apply
  </acceptance_criteria>
  <done>The developer responded with the resume signal, and that response is recorded verbatim in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to the local tree | A NEW version of an external package enters the repository — the only package-manager install in the entire phase |
| the CI gate to every future PR | What the gate blocks on becomes the claim the repository enforces on contributors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.1-14-01 | Repudiation | Confounding the fix delta with the bump delta, so neither number means anything | mitigate | The widening and the fix measurement happen at the OLD pin; the re-pin is gated behind a checkpoint that verifies the fix delta was banked first; two separately headed tables are required |
| T-118.1-14-02 | Tampering | A weakened gate — a lowered floor, an allowlist, or `--expected-failures` — silently restoring suppression | mitigate | Floors go UP only, with a STOP condition if a measured count fell below a floor; the verify command greps for `--expected-failures` across `scripts/` and `.github/workflows/` and fails on any hit; `CONFORMANCE_FORBIDDEN_FLAGS` is re-verified |
| T-118.1-14-03 | Tampering | The script and its structural wiring test landing out of step, leaving a window where the gate is unenforced or the test is vacuous | mitigate | Both files must move in the SAME commit, asserted with `git show --stat HEAD`, in both Task 1 and Task 3 |
| T-118.1-14-04 | Repudiation | Assuming the new suite's check names, probe bodies and expected codes carried over from alpha-11 | mitigate | Task 3 requires re-extracting the named facts from the new bundle and listing every changed expectation |
| T-118.1-14-05 | Denial of Service | A flaky scenario admitted to the blocking surface, turning CI red at random | mitigate | Only scenarios that measured entirely green across plan 13's repeated runs are admitted; any green-but-flaky scenario is named and excluded |
| T-118.1-SC | Tampering | npm install of a new `@modelcontextprotocol/conformance` version | mitigate | A BLOCKING human checkpoint verifies the version on npmjs.com and via `npm view` before the bump, checking publisher org, publish date and takeover indicators; install is `npm ci --ignore-scripts` into a clean tree with the regenerated lockfile committed; the version is approved by name at the checkpoint |
</threat_model>

<verification>
- `binary(ci_conformance_gate_wiring)` non-zero count, exit 0 — both before and after the re-pin
- `binary(v2_conformance_pin)` non-zero count, exit 0
- `bash -n scripts/run-conformance-suite.sh` exits 0
- `grep -rn -- '--expected-failures' scripts/ .github/workflows/` returns nothing
- Both measurement logs exist and each records its exit code separately
- `make quality-gate` exit 0
</verification>

<success_criteria>
- The gate blocks on exactly the surfaces that pass, with no suppression of any shape
- Script and wiring test moved together, floors moved up only
- The suite is re-pinned with a regenerated, committed lockfile and a human-approved version
- The fix delta and the bump delta are reported as two separate, attributable numbers
- UNAS-01 is assigned on evidence or carried forward with the measurement recorded
</success_criteria>

<output>
Create `.planning/phases/118.1-close-the-nine-conformance-gaps-g-1-g-9-found-by-the-officia/118.1-14-SUMMARY.md` when done
</output>
