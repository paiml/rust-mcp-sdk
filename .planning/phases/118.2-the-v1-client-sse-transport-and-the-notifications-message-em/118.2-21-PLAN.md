---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 21
type: execute
wave: 14
depends_on: [118.2-19, 118.2-20]
files_modified:
  - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
gap_closure: true
requirements: [CONF-09, CONF-10]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `T-118.2-15-03` entry in deferred-items.md no longer asserts the discard wait has a ceiling supplied by the awaiting caller's own request timeout; the clause is REWRITTEN in place, not merely annotated beneath, and the corrected text names the two private constants that now bound it"
    - "CONF-09's traceability row no longer asserts the same false premise in its limitation (vi), and the row additionally DISCLOSES BLOCKER 1 — which it did not mention at all — together with both Blockers' closing fences and their parsed counts"
    - "WINDOWS.md entries 12 and 13 state the severity the closure review actually found — permanent blast radius for the latch, unbounded time and whole-client scope for the discard — rather than describing either as an accepted, bounded cost; and the correction is applied to BOTH the markdown table and its JSON mirror so the two cannot disagree"
    - "WINDOWS.md entry 16, which books CR-02 as fixed, carries a cross-reference stating that CR-02's own closure code introduced the two Blockers this round closed, so no reader takes that `fixed` status as the whole story"
    - "Every finding this round does NOT take is named with its review anchor, its user-visible consequence, its reason, and an owner — WR-01's queue-send residual first among them, since an unnamed residual going unre-verified is what let the last round ship two Blockers"
    - "The round carries ONE authoritative `cargo semver-checks --baseline-rev cb5d1365 -p pmcp` verdict on BOTH the default and the `fuzzing,full` feature sets, recorded verbatim at the round's final HEAD"
    - "`make quality-gate` is green at the round's final HEAD, and every step that did not exit 0 is classified by provenance with a `git diff --name-only` citation rather than swept in"
    - "The D-16 conformance gate is re-measured and reported against 118.2-12's closing figures, with the framing that the official suite exercises pmcp only as a SERVER and therefore cannot attribute either Blocker's fix"
    - statement: "binary(log_emitter) and binary(pmcp_both_ends_logging) stay green after this round's client-transport and client-correlation changes, so the CONF-10 emitter's behaviour for zero, one and many log records is unaltered"
      verification: backstop
    - statement: "the_emitted_frame_satisfies_the_vendored_schemas_required_data_member and the rest of binary(log_emitter) stay green, so the log record's UTF-8 payload shape and its length/equality semantics are unaltered by this round"
      verification: backstop
    - statement: "binary(pmcp_both_ends_logging) stays green with pmcp on BOTH ends of a live v1 session stream, so log records still reach the client concurrently with a tools/call answered over the transport this round changed"
      verification: backstop
  prohibitions:
    - "Do NOT mint a new requirement ID. D-17 already warns that REQUIREMENTS.md carries ten REQ-IDs appearing in bodies without table rows; a new ID would widen that warning for work belonging to an existing requirement's surface."
    - "Do NOT flip CONF-09 or CONF-10's checklist checkbox or their Complete booking. The literal requirement text was and remains satisfied; what is corrected is a false claim inside the EVIDENCE."
    - "Do NOT merely append a correction beneath a false claim while leaving the false claim standing. A documentation-of-record error is fixed by removing the false clause; every correction in this plan is verified by a negative grep proving the clause is GONE, plus a positive grep proving the replacement is present."
    - "Do NOT edit WINDOWS.md's markdown table without applying the identical edit to its JSON mirror at the end of the same file. The two are one record; a divergence between them is a worse defect than the one being corrected."
    - "Do NOT weaken the D-16 gate. If the suite reds, that is a reportable finding under D-16's own tension clause, not a reason to add an allowlist, an --expected-failures flag, or a known-failure baseline. D-21 carries forward verbatim."
    - "Do NOT claim the official conformance suite validates either Blocker's fix. It exercises pmcp only as a SERVER — CONF-09's own stated limitation (i) — and this round changed client code only."
    - "Do NOT re-litigate WINDOWS.md entries 4, 5, 6, 8 or 9, and do NOT edit entry 6: the client lifecycle deadlock is a DIFFERENT defect and neither Blocker's fix touched the send-then-receive ordering."
    - "Do NOT rewrite or delete anything already recorded in deferred-items.md other than the specific false clause in T-118.2-15-03. That file is append-only by convention; this is the one exception, and the reason is that a false claim of record cannot be left standing beside its own correction."
    - "Do NOT edit any *-PLAN.md numbered 01 or 03 through 20, and do NOT edit any existing *-SUMMARY.md, including 118.2-15-SUMMARY.md, which carries the same superseded claim and is instead named as superseded from the corrected ledger entry."
    - "Do NOT use a nextest -E 'test(/.../)' selector anywhere. Every test command uses binary(...) and asserts a parsed NON-ZERO run count."
  artifacts:
    - path: ".planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md"
      provides: "the corrected T-118.2-15-03 cost, and this round's named deferrals with owners"
      contains: "T-118.2-15-03"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-09's limitation (vi) corrected and BLOCKER 1 disclosed, with both Blockers' closing fences and counts"
      contains: "CONF-09"
    - path: ".planning/WINDOWS.md"
      provides: "entries 12 and 13 restated at the severity the review found, entry 16 cross-referenced, and this round's new disclosures"
      contains: "receive()"
  key_links:
    - from: "the two Blockers this round closed and the residuals it did not"
      to: "CONF-09's traceability row and the broken-windows ledger"
      via: "an in-place correction of the false clause plus a dated amendment carrying the measured counts"
      pattern: "CONF-09"
    - from: "a reader of deferred-items.md assessing residual risk"
      to: "the actual, unbounded cost the prior round booked as bounded"
      via: "a rewritten T-118.2-15-03 clause naming the constants that now bound it"
      pattern: "T-118.2-15-03"
---

<objective>
Correct the record this phase got wrong, name what this round does not take, and produce the round's
single authoritative gate and semver verdict.

Purpose. `118.2-VERIFICATION.md` classified a documentation-of-record error as a Blocker in its own
right, and it was right to: the `T-118.2-15-03` disposition books an UNBOUNDED wait as bounded, so a
reader trusting the ledger believes a live defect is already mitigated. The same false premise
propagated into CONF-09's limitation (vi) and into `WINDOWS.md` entry 13, and `WINDOWS.md` entries 12
and 13 together describe a permanent, whole-client failure mode as an accepted, bounded cost. CONF-09
does not disclose BLOCKER 1 at all. `WINDOWS.md` entry 16 books CR-02 as `fixed` — accurate about the
original defect, silent about the two Blockers its own closure code introduced.

The second purpose is the one the last round failed at. Its WR-01 residual was recorded but never
re-verified, and its own summaries were trusted as evidence of closure. This plan therefore names
every finding this round declines, with an owner, and books nothing on a narrative: each correction
is verified by a negative grep proving the false clause is GONE and a positive grep proving the
corrected claim is present, and each measured number is quoted from a log this plan produced.

Output: three corrected documents of record, a named deferral ledger, and the closing verification.

**Locked decisions this plan preserves rather than revisits.** D-17 (mint CONF-09 and CONF-10, and do
not widen the ten-orphan REQ-ID warning) is why no new ID appears here. D-16 (hard-require the v1 leg
to exit 0 in the CI gate) is re-run rather than relaxed, and D-21 carries forward verbatim: no
allowlist, no expected-failures flag, no known-failure baseline. D-10 through D-13 (the CONF-10
logging decisions) are untouched — the three CONF-10 truths in this plan are BACKSTOPS asserting that
this round's client changes did not disturb them, not a reopening of that surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-VERIFICATION.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-REVIEW-closure.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-19-SUMMARY.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-20-SUMMARY.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-18-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs, so it does not have to re-derive them. -->

**`deferred-items.md`'s shape.** Roughly 936 lines, append-only by convention. The false clause lives
in the `## DEFERRED (118.2-15): per-id response ROUTING, and the cost of discard-on-mismatch` section,
in the paragraph headed **The stated limitation**, around line 592. The same file already carries a
`## GAP-CLOSURE ROUND (2026-08-17)` appendix ending in a `### Disposition — who should own the
remainder` section that names two owners: a client-transport hardening plan (for the first review's
WR-03/WR-04/WR-05) and a CONF-10 follow-up (for WR-06 and IN-06). Route this round's deferrals to
those SAME two owners where they fit rather than inventing new ones.

**A NAMING COLLISION that must be disambiguated, not papered over.** `118.2-REVIEW.md` (the first
review) numbered findings WR-01..WR-06 and IN-01..IN-06. `118.2-REVIEW-closure.md` (the second review,
of the closure itself) restarted at WR-01..WR-05 and IN-01..IN-04 over ENTIRELY DIFFERENT findings.
`deferred-items.md` currently indexes only the first set. Every reference this plan writes to a
closure-review finding must name the file as well as the id, or a later auditor will read the wrong
entry. State that hazard once, explicitly, in the new appendix.

**`REQUIREMENTS.md`'s CONF-09 row is ONE markdown table line** (`:1107`), so every edit is on that
same line. It already carries the D-17 minting note, eight named `binary(...)` selectors with parsed
counts, an `AMENDED 2026-08-17 by 118.2-18` block, and SIX numbered stated limitations, of which (vi)
carries the false premise. The house idiom, used three times in this file already, is a bolded dated
`AMENDED …` clause that explicitly says nothing above it is rewritten. This plan needs BOTH idioms: a
dated amendment for the new evidence AND, uniquely, an in-place correction of limitation (vi)'s false
clause — because an amendment beneath a false claim leaves the false claim readable as current.

**`WINDOWS.md` is TWO records in one file.** A markdown table (entries at lines 18 onward) and a JSON
mirror in a fenced block at the end. The false clause appears twice, once in each. The CLI —
`node <gsd-tools> windows` — offers only `status`, `append`, `waive` and `fixed`; there is no edit
subcommand, so a correction to an existing entry's TEXT is a hand edit that must be applied to both
occurrences, while NEW entries must go through `windows append --kind <kind> --phase <phase> …` so the
frontmatter counters stay consistent.

**The semver baseline, unchanged across this whole phase:**
`cargo semver-checks --baseline-rev cb5d1365 -p pmcp`. `118.2-04`, `118.2-13` and `118.2-18` all
recorded `223 checks: 223 pass, 30 skip`, `no semver update required`. `118.2-04` also recorded the
caveat to repeat: the tool is a breaking-change LINTER rather than an API-diff inventory, so there is
no "addition" line to quote for a private field and "nothing fired" is the whole of the claim.

**The conformance runner's two fail-closed guards**, both recorded in `deferred-items.md`'s closing
section: `node` must be at least v22 (the script imports `globSync` from `node:fs` at module scope,
and the shell default is v20.8.1), and `PMCP_REQUEST_STATE_KEY` must be set (the script refuses rather
than letting the example derive a per-process key, because the MRTR surface the gate blocks on would
then be measuring a different server than CI does). Both refusals exit non-zero having measured
NOTHING; neither is a conformance failure.

**The api-coverage checkpoint is already satisfied for this phase.** `COVERAGE.md` exists in the phase
directory and is a reasoned `No external API integration: …` declaration whose stated scope is
precisely this client code. This round changes nothing about that answer; confirm the file is present
and unmodified rather than producing a matrix.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two false claims of record — deferred-items.md and CONF-09's limitation (vi)</name>
  <files>.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-VERIFICATION.md (the gaps block, the missing list, and the Anti-Patterns row that classifies the ledger error as a Blocker)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-REVIEW-closure.md (CR-02 in that file — the paragraph proving no such timeout exists anywhere in Client)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md (the whole `## DEFERRED (118.2-15)` section, and the `### Disposition — who should own the remainder` section whose owners are reused)
    - .planning/REQUIREMENTS.md:1106-1108 (the CONF-07, CONF-09 and CONF-10 rows — CONF-07's and CONF-10's amendments are the house idiom to copy)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-19-SUMMARY.md and 118.2-20-SUMMARY.md (the measured fence names, parsed counts and RED measurements this correction must cite)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-18-PLAN.md (task 1 — the REQ-ID-set capture technique that proves no ID was minted)
  </read_first>
  <action>
    Two documents, two corrections, both verified by the false clause being GONE rather than by the
    file having been edited.

    **Before touching anything**, capture the sorted set of REQ-ID-shaped tokens in
    `.planning/REQUIREMENTS.md` to `target/118.2-21-reqids-before.txt`, exactly as `118.2-18` did, so
    the no-new-ID claim is measured rather than asserted.

    **deferred-items.md.** In the `## DEFERRED (118.2-15)` section's **The stated limitation**
    paragraph, REWRITE the clause that claims the discard cost has a ceiling supplied by the awaiting
    caller's own request timeout. It is false: `Client` reads no such timeout anywhere in
    `dispatch_request`, and `RequestOptions::timeout` is never consulted. Replace it with what is now
    true after plan 118.2-20: the wait is bounded by `MISMATCH_DISCARD_TIMEOUT` and capped by
    `MAX_ID_MISMATCH_DISCARDS`, both private constants in `src/client/mod.rs`; before that plan it was
    bounded by nothing at all, and the discard additionally held the transport write lock across an
    unbounded receive, wedging every other operation on the same `Client`. Keep the rest of the
    section — the two rejected alternatives, the per-id routing follow-up, and the measured half-fix
    evidence — verbatim. Add a dated correction note immediately after the rewritten clause stating
    which plan corrected it, that `118.2-15-SUMMARY.md` carries the same superseded claim and is NOT
    edited because summaries are historical records, and that the superseding statement lives here.

    **A new appendix, append-only.** Add `## SECOND GAP-CLOSURE ROUND (plans 118.2-19..21)` at the end
    of the file, containing:
    - The naming-collision warning: the closure review restarted its WR/IN numbering over different
      findings, so every reference names the FILE as well as the id.
    - What this round CLOSED: BLOCKER 1 (the latch pre-empting an in-flight SSE-answered POST
      response, permanently) and BLOCKER 2 (the lock-holding, unbounded discard), each with its plan,
      its fence names, its parsed green counts and its RED measurement.
    - What this round DECLINES, one subsection each, with review anchor, source location by SYMBOL not
      by line, user-visible consequence, reason, and OWNER:
      * `118.2-REVIEW-closure.md` WR-01 — `deliver_sse_event`'s queue send is a bare await not raced
        against shutdown, so a detached POST reader parked on a full 64-slot queue after `close()`
        still leaks a task and a socket; the leak moved one await down rather than being eliminated.
        Reason: Warning severity, and it is NOT one of the five items in the verification's `missing:`
        scope contract; its fence needs the 4 MiB backpressure shape whose drain bound is 30 seconds,
        and loading that into a round that must close two permanent-failure Blockers dilutes the
        round's own verification. OWNER: the client-transport hardening plan this file's existing
        Disposition section already names. State explicitly that this residual was recorded but NEVER
        RE-VERIFIED by the previous round, and that being unnamed and unre-verified is how it survived
        — which is why it is named here with an owner rather than left implicit.
      * `118.2-REVIEW-closure.md` WR-02 — the `biased;` in `read_next_sse_frame` starves both shutdown
        arms while the body is continuously ready, so `close()` is not observed for as long as a
        firehose peer keeps writing. Reason: the bias is deliberate and correct for the frame arm
        (D-04, never drop a burst-then-close server's last frames); adding a fairness escape is a
        tuning decision on the reader loop and belongs with WR-01's fix, which touches the same loop.
        OWNER: same client-transport hardening plan.
      * `118.2-REVIEW-closure.md` WR-03 — the `shutdown` flag is never reset, so after any `close()`
        a later `start_sse` returns `Ok(())` over a reader that immediately exits, `is_connected()`
        still answers `true`, and every clone of the transport is affected. Reason, and this is the
        NEAR MISS worth recording: plan 118.2-19 added a reset seam to `start_sse` for the TERMINAL
        LATCH and deliberately did not add one for the shutdown flag, because the review offers two
        contradictory contracts (reset the flag and support close-then-restart, or make close
        observably terminal with `is_connected()` reporting it) and picking one inside a Blocker
        closure would settle a contract by accident — the same reasoning `118.2-16` used to defer
        WR-05 of the first review. OWNER: same client-transport hardening plan, which should decide
        both reset seams together.
      * `118.2-REVIEW-closure.md` WR-04 — the test mocks re-address EVERY response once any request
        has been recorded, so a fixture deliberately carrying a foreign id is silently rewritten and a
        negative test proves nothing while still passing. Reason: test-infrastructure consolidation
        across three files, correctness-neutral for shipped code. OWNER: whichever plan next touches
        `tests/common/`.
      * `118.2-REVIEW-closure.md` IN-01 through IN-04 — the stale "already sent" comment, the dead
        `state.done` write, the reconnect floor applied per-branch rather than at the exit, and the
        write-only cursor local. Reason: hygiene, no correctness consequence, no ordering constraint.
        OWNER: ride along with whichever plan next touches the file.
    - A one-line note that `118.2-REVIEW-closure.md` WR-05 (fence 17 passing on any error) was NOT
      deferred: plan 118.2-20 tightened that arm, and the tightening is recorded there.
    - A restatement that the first review's WR-03..WR-06 and IN-01..IN-06 remain open exactly as the
      earlier appendix records them, with no change of status.

    **REQUIREMENTS.md, CONF-09's row.** Two edits on that single line.
    First, an IN-PLACE correction of limitation (vi): remove the clause asserting that the request the
    discarded frame belonged to waits out the awaiting caller's own timeout, and replace it with the
    measured truth — the wait was bounded by nothing, the discard held the transport write lock across
    an unbounded receive so a single mis-addressed frame wedged the whole `Client`, and both are now
    bounded by `MISMATCH_DISCARD_TIMEOUT` and `MAX_ID_MISMATCH_DISCARDS`. This is the one place the
    phase's append-only amendment convention is deliberately broken, and the sentence saying so must
    be in the row: a false claim of record cannot be left standing beside its own correction.
    Second, append a bolded dated amendment naming this round: that limitation (vi) as originally
    written rested on a premise the closure review refuted; that a SEVENTH limitation was missing
    entirely — the sticky latch pre-empting an in-flight, SSE-answered POST response, permanently, for
    every later call on that transport; and that BOTH are now CLOSED, with the plan, the fence names,
    the parsed green counts and the RED measurements taken from plans 19 and 20's summaries. Number
    the new disclosure (vii) so the row's list continues rather than restarting, and state plainly
    that (vii) describes a defect that was OPEN at the time the row's previous amendment was written
    and is now closed.

    After the edits, re-capture the REQ-ID set to `target/118.2-21-reqids-after.txt`.
  </action>
  <verify>
    <automated>test "$(grep -c "bounded by the caller's own timeout" .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md)" -eq 0</automated>
    <automated>test "$(grep -c "waits out its caller's own timeout" .planning/REQUIREMENTS.md)" -eq 0</automated>
    <automated>test "$(grep -c 'MISMATCH_DISCARD_TIMEOUT' .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md)" -ge 1</automated>
    <automated>test "$(grep -c 'MISMATCH_DISCARD_TIMEOUT' .planning/REQUIREMENTS.md)" -ge 1</automated>
    <automated>test "$(grep -c '^| CONF-09 |' .planning/REQUIREMENTS.md)" -eq 1</automated>
    <automated>test "$(grep -c 'SECOND GAP-CLOSURE ROUND' .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md)" -eq 1</automated>
    <automated>diff target/118.2-21-reqids-before.txt target/118.2-21-reqids-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bounded by the caller's own timeout" deferred-items.md` returns 0 AND `grep -c 'MISMATCH_DISCARD_TIMEOUT' deferred-items.md` returns at least 1 — the false clause is GONE and the corrected one is present, not merely annotated beneath.
    - `grep -c "waits out its caller's own timeout" .planning/REQUIREMENTS.md` returns 0 AND CONF-09's row names `MISMATCH_DISCARD_TIMEOUT`, `MAX_ID_MISMATCH_DISCARDS`, and both Blockers.
    - CONF-09's row contains a limitation numbered (vii) disclosing BLOCKER 1, and its earlier limitations (i) through (v) are still present verbatim.
    - `grep -c '^| CONF-09 |' .planning/REQUIREMENTS.md` returns exactly 1 — the row was amended, not duplicated — and `diff target/118.2-21-reqids-before.txt target/118.2-21-reqids-after.txt` is empty, so no requirement ID was minted.
    - CONF-09's row cites, for each Blocker, at least one `binary(...)` selector with a parsed non-zero count taken verbatim from `118.2-19-SUMMARY.md` or `118.2-20-SUMMARY.md`, and both RED measurements.
    - The new `## SECOND GAP-CLOSURE ROUND` appendix names all four declined closure-review Warnings and the four Info items, each with a review anchor that includes the FILE name, a consequence, a reason and an owner; `grep -c '118.2-REVIEW-closure.md' deferred-items.md` returns at least 6.
    - The appendix states, in words, that the previous round's WR-01 residual was recorded but never re-verified, and names the owner that now holds it.
    - `git diff --stat -- .planning/phases/118.2-*/118.2-15-SUMMARY.md` is empty — no historical summary was rewritten.
  </acceptance_criteria>
  <done>
    Neither document of record still says the discard wait is bounded by a timeout that does not
    exist, CONF-09 discloses the defect it never mentioned, and everything this round declines has a
    name, a reason and an owner.
  </done>
  <reversibility rating="reversible">
    Documentation-of-record edits; undo is a one-commit revert. The in-place clause removal is the
    deliberate exception to this phase's append-only convention and the reason is stated in the row
    itself.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Restate WINDOWS.md entries 12 and 13 at the severity found, cross-reference entry 16, and disclose this round</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - .planning/WINDOWS.md (the whole file — the frontmatter counters, the table header, entries 6, 10, 11, 12, 13, 14, 15 and 16, and the JSON mirror at the end)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-VERIFICATION.md (the regressions block, which states exactly how the severity direction worsened)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-REVIEW-closure.md (both Critical findings, for the consequence wording)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-19-SUMMARY.md and 118.2-20-SUMMARY.md (the fence names and counts each disclosure cites)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-18-PLAN.md (task 2 — how the previous round wrote its disclosures)
  </read_first>
  <action>
    `WINDOWS.md` is two records in one file: a markdown table and a JSON mirror at the end. Every text
    correction below must be applied to BOTH occurrences of the affected text, and the file must still
    parse afterwards.

    **Entry 12 — the sticky latch.** Its description frames the change as a behaviour change with a
    documented mitigation. That framing is now known to be incomplete in the direction that matters:
    the latch is `Arc`-shared across every clone, write-once, and had NO reset seam, so its blast
    radius was PERMANENT rather than one-off, and on the POST-answered-with-SSE path it pre-empted a
    caller whose own answer was still in flight. Correct the description in place to state that
    severity, and then state the resolution: plan 118.2-19 scoped the reason to the stream that raised
    it, gated the latch on live POST-response readers, and added a reset seam on a successful
    `start_sse` re-open, fenced by
    `a_latched_session_stream_does_not_pre_empt_an_sse_answered_call` and
    `a_reopened_session_stream_clears_the_terminal_latch`. Keep the parts of the entry that remain
    true — the sticky-versus-one-shot reasoning and the semver invisibility — verbatim.

    **Entry 13 — discard-on-mismatch.** Remove the clause asserting the mismatched frame's owner waits
    out the awaiting caller's own timeout; it is false, and it is the exact claim the verification
    classified as a documentation-of-record Blocker. Replace it with the measured truth: the wait was
    bounded by nothing, and the discard held the transport write lock across an unbounded receive so
    one mis-addressed frame wedged EVERY operation on the client, not only the mismatched caller.
    Then state the resolution: plan 118.2-20 bounded the wait with `MISMATCH_DISCARD_TIMEOUT`, capped
    the discards with `MAX_ID_MISMATCH_DISCARDS`, and released the transport write guard on a
    `MISMATCH_RECEIVE_SLICE`, fenced by
    `a_mismatched_frame_does_not_block_another_operation_on_the_same_client` and
    `a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever`. Keep the entry's
    correct parts — the typed-`RequestId` explanation, the per-id routing follow-up, and its explicit
    statement that entry 6 is a different defect — verbatim.

    **Entry 16 — CR-02 booked as fixed.** Do not flip its status; it is accurate about the ORIGINAL
    CR-02 defect. Append to its description a cross-reference stating that CR-02's own closure code
    introduced two Critical defects on the same client path, that they are recorded as entries 12 and
    13 as corrected above, and that both are closed by plans 118.2-19 and 118.2-20 — so no reader
    takes the `fixed` status as the whole story.

    **New entries, through the CLI.** Use `node <gsd-tools> windows append` — never a hand edit — so
    the frontmatter counters stay consistent. Append one entry per item:
    1. `--kind deviation`, file `src/shared/streamable_http.rs`: BLOCKER 1 as a Critical found by
       `118.2-REVIEW-closure.md` and independently confirmed by `118.2-VERIFICATION.md`, with its
       reachability (a spent reconnect budget, a 405 on reconnect, one truncated streaming POST), its
       permanence, and its fix, fences and parsed counts. Mark it `fixed` once recorded.
    2. `--kind deviation`, file `src/client/mod.rs`: BLOCKER 2, likewise, naming both halves — the
       lock-holding receive and the non-existent ceiling — and both fences. Mark it `fixed`.
    3. `--kind deviation`, file `src/shared/streamable_http.rs`: the consumer-observable behaviour
       change plan 118.2-19 introduces — `receive()` no longer answers immediately with a latched
       reason while a streaming POST response is in flight, and a successful `start_sse` now clears a
       latch that was previously permanent. OPEN by design, as a disclosure.
    4. `--kind deviation`, file `src/client/mod.rs`: the consumer-observable behaviour change plan
       118.2-20 introduces — a `tools/call` that has seen a mis-addressed frame now fails with a
       timeout after `MISMATCH_DISCARD_TIMEOUT` where it previously waited forever, and the transport
       write lock is released and re-taken on a slice during that discard. Name the rejected
       alternative too: plumbing the public `RequestOptions::timeout` was rejected because activating a
       public field that has never been read changes behaviour for existing callers with no version
       signal. OPEN by design, as a disclosure.
    5. `--kind unmet-truth`, file `src/shared/streamable_http.rs`: the four closure-review Warnings and
       four Info items this round declines, each with its consequence and its named owner, pointing at
       `deferred-items.md`'s new `## SECOND GAP-CLOSURE ROUND` appendix for the full record. Name the
       WR/IN numbering collision between the two review files so a reader does not follow the id into
       the wrong entry. OPEN.

    Do not touch entries 4, 5, 6, 8 or 9.

    After all edits, run `node <gsd-tools> windows status` and record its output. Verify the JSON
    mirror and the markdown table agree on every entry this task touched.
  </action>
  <verify>
    <automated>test "$(grep -c "waits out its caller's own timeout" .planning/WINDOWS.md)" -eq 0</automated>
    <automated>test "$(grep -c 'MISMATCH_DISCARD_TIMEOUT' .planning/WINDOWS.md)" -ge 2</automated>
    <automated>test "$(grep -c 'a_latched_session_stream_does_not_pre_empt_an_sse_answered_call' .planning/WINDOWS.md)" -ge 2</automated>
    <automated>test "$(grep -c 'a_mismatched_frame_does_not_block_another_operation_on_the_same_client' .planning/WINDOWS.md)" -ge 2</automated>
    <automated>bash -o pipefail -c 'node "${HOME}/.claude/gsd-core/bin/gsd-tools.cjs" windows status 2>&1 | tee target/118.2-21-windows.log'</automated>
    <automated>python3 -c "import re,json,sys; t=open('.planning/WINDOWS.md').read(); m=re.search(r'\`\`\`\`?json\s*(\[.*?\])\s*\`\`\`\`?', t, re.S); json.loads(m.group(1)); print('json mirror parses')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "waits out its caller's own timeout" .planning/WINDOWS.md` returns 0 — the false clause is gone from BOTH the table and the JSON mirror, which is why the count is 0 rather than 1.
    - Every corrected literal appears an EVEN number of at-least-2 times, once in the table and once in the mirror: `MISMATCH_DISCARD_TIMEOUT` at least 2, each of the four new fence names at least 2.
    - The JSON mirror parses as valid JSON and its entry count equals the number of table rows — asserted by the python3 check in `<verify>` plus a recorded comparison of the two counts in the summary.
    - `node <gsd-tools> windows status` exits 0, and the summary quotes `open_count`, `fixed_count` and `total_count` before and after this task, with the delta explained entry by entry.
    - Entries 12, 13 and 16 each still contain the clauses this plan's prohibitions require kept: entry 12's sticky-versus-one-shot reasoning and its semver-invisibility note, entry 13's typed-`RequestId` explanation and its "entry 6 is a different defect" sentence, entry 16's original CR-02 description.
    - Entries 4, 5, 6, 8 and 9 are byte-unchanged — verifiable by `git diff .planning/WINDOWS.md` showing no touched line inside those rows or their mirror objects.
    - The five new entries are present, the two Blocker entries are `fixed` and the three disclosure entries are `open`, and each names at least one fence and one parsed count or one named owner.
  </acceptance_criteria>
  <done>
    The broken-windows ledger states what the closure review actually found, at the severity it found
    it, in both of the records the file keeps — and nothing this round declines is missing from it.
  </done>
  <reversibility rating="reversible">
    Ledger edits; undo is a one-commit revert. New entries go through the CLI so the counters stay
    derivable rather than hand-maintained.
  </reversibility>
  <precondition>
    The `gsd-tools` shim must be resolvable and its `windows append` subcommand must accept
    `--kind`, `--phase`, `--file` and a description; probe it with no arguments first and halt with
    the tool's own usage output if the flag set differs, rather than hand-editing the JSON mirror and
    leaving the frontmatter counters stale.
  </precondition>
</task>

<task type="auto">
  <name>Task 3: The closing verification — semver on both feature sets, the gate, the CONF-10 backstops, and the D-16 regression check</name>
  <files>target/118.2-21-semver.log, target/118.2-21-semver-fuzzing.log, target/118.2-21-gate.log, target/118.2-21-conf10.log, target/118.2-21-conf.log, target/118.2-21-always.log</files>
  <read_first>
    - CLAUDE.md (the quality-gate contract, the release/semver practice, and the ALWAYS requirements)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-18-SUMMARY.md (the closing-record shape this task mirrors, including the semver caveat and the two-run conformance record)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md (the CLOSING VERIFICATION section — the pre-existing keychain `.expect` panic, the two conformance-runner fail-closed guards, and the D-16 run-1 red that was reported rather than accommodated)
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/COVERAGE.md (the reasoned no-external-API declaration that already satisfies the api-coverage seal gate)
    - scripts/run-conformance-suite.sh (the D-16 gate, its floors, and its exit-code hardening)
    - Makefile (quality-gate, validate-always, comply)
  </read_first>
  <action>
    Produce the round's single authoritative closing record. Every number in it comes from a command
    run in this task and is quoted from its log — never carried forward from a plan's expectation and
    never taken from a sibling plan's narrative. That last rule is the one the previous round broke.

    **Semver, both feature sets.** Run `cargo semver-checks --baseline-rev cb5d1365 -p pmcp` at the
    round's final HEAD and quote the output verbatim; repeat with the `fuzzing` and `full` features
    enabled, matching `118.2-04`'s and `118.2-18`'s practice of proving the seam is in the compiled
    surface on both sides. Record the caveat alongside the verdict: the tool is a breaking-change
    linter rather than an API-diff inventory, so there is no addition line to quote for a private
    constant, field, struct or method, and "nothing fired" is the whole of the claim it supports. If
    ANYTHING fires, STOP and report it — this round is required to be additive, and if some part of it
    cannot be, say so explicitly and design the additive alternative rather than accepting a break.

    **Quality gate.** Run `make quality-gate`. Classify every failing step by provenance: a step whose
    offending path is absent from this round's `git diff --name-only` against the round's base is
    pre-existing and must be quoted WITH that evidence; a step whose path IS present belongs to this
    round and must be fixed. The keychain `.expect` panic in the OAuth integration tests is the known
    environmental one recorded in this phase's closing section — if it fires, re-run and record both
    timings and the free-space figure, exactly as that record does. Then run `make validate-always`
    and `make comply` and record both.

    **The CONF-10 backstops, framed as backstops.** Run `binary(log_emitter)` together with
    `binary(pmcp_both_ends_logging)` and record their parsed counts. These discharge this plan's three
    `verification: backstop` truths: this round changed the client transport that CARRIES log records
    and the client loop that CONSUMES them, so the check that CONF-10's emitter behaviour is unaltered
    — for zero records, for one, for many, for the vendored schema's required `data` member, and for
    records arriving concurrently with a call — is that its own fences stay green. Say that framing
    explicitly rather than listing the binaries as generic regression runs.

    **The D-16 regression check.** Run `scripts/run-conformance-suite.sh` and record both legs'
    numbers and exit codes against `118.2-12`'s closing figures. Before running, satisfy the script's
    two fail-closed guards and record that you did: `node` at v22 or later on `PATH`, and an ephemeral
    non-production `PMCP_REQUEST_STATE_KEY` that is never echoed and never committed. Both refusals
    exit non-zero having measured NOTHING, so a record that reported either as a suite result would be
    reporting no referee at all. Frame the result correctly and prominently: the official suite
    exercises pmcp only as a SERVER — CONF-09's own stated limitation (i) — and this round changed
    CLIENT code only, so the suite CANNOT attribute either Blocker's fix; this is a regression check on
    the D-16 exit-code gate. If the `2025-11-25` leg no longer exits 0, report it under D-16's own
    tension clause: do not add an allowlist, an expected-failures flag, or a known-failure baseline,
    and assert that `git status --porcelain -- scripts/run-conformance-suite.sh conformance/` is empty
    to prove nothing was moved.

    **API coverage.** Confirm `COVERAGE.md` is present and unmodified, and record that this round
    changes nothing about its reasoned no-external-API declaration: the work is pmcp's own client
    transport and its own correlation loop, and the only network peer is a local recording TCP
    listener. Do not produce a matrix.

    **ALWAYS.** Discharge CLAUDE.md's four requirements for the round as a whole, one line each,
    citing the plan that owns each: unit (plan 19's `latch_gate` submodule and plan 20's
    `mismatch_budget` submodule, with their parsed counts), property (the counter-monotonicity and
    slice-bound arms those plans added), fuzz (the existing `streamable_sse_frames` target re-run
    clean, with the recorded reasoning for why no second seam was invented), and example (the reasoned
    no-new-example decision: this round adds no public API, which the semver verdict above confirms,
    so there is no new surface to demonstrate, and the changed behaviour is exercised end to end by
    fences 21 through 24 plus `binary(pmcp_both_ends_logging)`).

    **Contract-first.** Check whether `../provable-contracts/contracts/pmcp/` exists. It did not at
    `118.2-18`'s time — the sibling repository is present but carries no `pmcp` contract directory — so
    record the measurement rather than asserting compliance. If it now exists, run `pmat comply check`
    and record the verdict.

    Assemble all of the above into the summary as ONE verification table matching
    `118.2-18-SUMMARY.md`'s shape. Commit with a message naming the second gap-closure round and both
    Blockers.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo semver-checks --baseline-rev cb5d1365 -p pmcp 2>&1 | tee target/118.2-21-semver.log'</automated>
    <automated>grep -q 'no semver update required' target/118.2-21-semver.log</automated>
    <automated>bash -o pipefail -c 'cargo semver-checks --baseline-rev cb5d1365 -p pmcp --features fuzzing,full 2>&1 | tee target/118.2-21-semver-fuzzing.log'</automated>
    <automated>grep -q 'no semver update required' target/118.2-21-semver-fuzzing.log</automated>
    <automated>bash -o pipefail -c 'make quality-gate 2>&1 | tee target/118.2-21-gate.log'</automated>
    <automated>bash -o pipefail -c 'make validate-always 2>&1 | tee target/118.2-21-always.log'</automated>
    <automated>bash -o pipefail -c 'cargo nextest run -E "binary(log_emitter) or binary(pmcp_both_ends_logging)" --features full 2>&1 | tee target/118.2-21-conf10.log'</automated>
    <automated>grep -Eq 'Summary \[[^]]*\] [1-9][0-9]* tests run: [1-9][0-9]* passed' target/118.2-21-conf10.log</automated>
    <automated>bash -o pipefail -c './scripts/run-conformance-suite.sh 2>&1 | tee target/118.2-21-conf.log'</automated>
    <automated>test -z "$(git status --porcelain -- scripts/run-conformance-suite.sh conformance/)"</automated>
  </verify>
  <acceptance_criteria>
    - Both `target/118.2-21-semver.log` and `target/118.2-21-semver-fuzzing.log` contain `no semver update required`, and the summary quotes each invocation's check count verbatim alongside the breaking-change-linter caveat.
    - `target/118.2-21-conf10.log` matches `Summary \[[^]]*\] [1-9][0-9]* tests run: [1-9][0-9]* passed` with a parsed run count of at least 26 and zero failures — discharging all three `verification: backstop` truths, each named explicitly in the summary rather than folded into a generic regression line.
    - `./scripts/run-conformance-suite.sh` exits 0; the summary records both legs' passed/failed counts and exit statuses against `118.2-12`'s closing figures, states the two fail-closed guards were satisfied and how, and carries the sentence stating the suite cannot attribute either Blocker's fix because pmcp is only ever the server under test.
    - `git status --porcelain -- scripts/run-conformance-suite.sh conformance/` is empty and `git diff --stat -- scripts/ conformance/` across the whole round is empty — D-21 carried forward verbatim, nothing weakened.
    - `make validate-always` exits 0 and the summary carries one explicit line per ALWAYS requirement, each citing the plan that owns it and a parsed count where one exists.
    - `make quality-gate` exits 0, or every step that did not is named with a `git diff --name-only` citation proving the offending path is not in this round's commits.
    - The summary records whether `../provable-contracts/contracts/pmcp/` exists and, if so, the `pmat comply check` verdict; `make comply` is run and recorded either way.
    - `git diff --stat -- .planning/phases/118.2-*/COVERAGE.md` is empty, and the summary records that the reasoned no-external-API declaration still covers this round's scope.
    - `git log --oneline -1` shows a commit whose message names the second gap-closure round and both Blockers.
  </acceptance_criteria>
  <done>
    The round carries one authoritative semver verdict on both feature sets, one gate record, measured
    CONF-10 backstops, an explicit ALWAYS discharge, and a D-16 regression check whose framing does not
    overclaim what the official suite can attribute.
  </done>
  <reversibility rating="reversible">
    Measurement and record-keeping only; no source changes.
  </reversibility>
  <precondition>
    `scripts/run-conformance-suite.sh` needs `node` at v22 or later on `PATH` (the shell default is
    v20.8.1 and the script imports `globSync` from `node:fs` at module scope), a set
    `PMCP_REQUEST_STATE_KEY`, and the pinned conformance harness resolved under
    `conformance/node_modules/@modelcontextprotocol/conformance` at the held `0.2.0-alpha.11` pin.
    Assert all three before running and halt with the measured values rather than reporting a suite
    result from a different referee than the one `118.2-11` and `118.2-12` measured against.
  </precondition>
</task>

</tasks>

## Edge-probe disposition

This plan disposes CONF-10's three spec-less edge-probe items, all
`resolved (verification: backstop)` — this round changed no CONF-10 surface, so the defensible
acceptance criterion is that CONF-10's own fences stay green across a change to the transport that
carries its records and the client loop that consumes them. Plan 118.2-19 disposed CONF-09's
`boundary`, `empty`, `idempotency` and `concurrency`; plan 118.2-20 disposed CONF-09's `encoding` and
`precision`. All nine are disposed; none is dismissed.

| Requirement | Category | Disposition | Where it is verified |
|---|---|---|---|
| CONF-10 | empty | resolved — backstop | `binary(log_emitter)` and `binary(pmcp_both_ends_logging)` green at the round's final HEAD, covering zero, one and many records |
| CONF-10 | encoding | resolved — backstop | `the_emitted_frame_satisfies_the_vendored_schemas_required_data_member` and the rest of `binary(log_emitter)` green — the record's UTF-8 payload shape and its length/equality semantics unaltered |
| CONF-10 | concurrency | resolved — backstop | `binary(pmcp_both_ends_logging)` green with pmcp on both ends of a live v1 session stream — records still arrive concurrently with a call answered over the changed transport |

## Deferrals this round names, with owners

Recorded here as well as in `deferred-items.md`, because an unnamed residual going unre-verified is
exactly how the previous round shipped two Blockers.

| Finding | Anchor | Consequence if left | Reason not taken | Owner |
|---|---|---|---|---|
| Queue-send leak | `118.2-REVIEW-closure.md` WR-01 | a detached POST reader parked on a full 64-slot queue after `close()` still leaks a task and a socket | Warning severity, not in the verification's `missing:` scope contract; its fence needs the 4 MiB backpressure shape with a 30 s drain bound, which dilutes a round that must close two permanent-failure Blockers | the client-transport hardening plan already named in `deferred-items.md`'s Disposition section |
| Biased-select starvation | `118.2-REVIEW-closure.md` WR-02 | `close()` unobserved while a firehose peer keeps the body ready | the bias is deliberate and correct for the frame arm (D-04); a fairness escape is a tuning decision on the same loop WR-01's fix touches | same |
| `shutdown` never reset | `118.2-REVIEW-closure.md` WR-03 | after any `close()`, `start_sse` reports success over a reader that exits at once, and `is_connected()` still answers true, on every clone | the review offers two contradictory contracts and picking one inside a Blocker closure settles it by accident; plan 118.2-19's reset seam is for the LATCH only, and the two seams should be decided together | same |
| Mocks re-address every response | `118.2-REVIEW-closure.md` WR-04 | a negative fixture carrying a foreign id is silently rewritten, so a test proving rejection proves nothing and still passes | test-infrastructure consolidation across three files; correctness-neutral for shipped code | whichever plan next touches `tests/common/` |
| IN-01..IN-04 | `118.2-REVIEW-closure.md` | stale comment, dead write, per-branch floor, write-only local | hygiene, no correctness consequence, no ordering constraint | ride along with whichever plan next touches the file |
| First review's WR-03..WR-06, IN-01..IN-06 | `118.2-REVIEW.md`, indexed in `deferred-items.md`'s first appendix | unchanged | unchanged; no status change this round | as already recorded there |

Not deferred: `118.2-REVIEW-closure.md` WR-05 (fence 17 passing on any error) is CLOSED by plan
118.2-20's tightening of that arm.

## Artifacts this phase produces

Created by THIS plan:

| Symbol / path | Kind | Where |
|---|---|---|
| the corrected `T-118.2-15-03` cost clause | in-place documentation correction | `deferred-items.md` |
| `## SECOND GAP-CLOSURE ROUND (plans 118.2-19..21)` | new append-only appendix | `deferred-items.md` |
| CONF-09 limitation (vi) corrected, (vii) added | in-place correction plus dated amendment | `.planning/REQUIREMENTS.md` |
| entries 12 and 13 restated, entry 16 cross-referenced | in-place ledger corrections, table and JSON mirror | `.planning/WINDOWS.md` |
| five new ledger entries (2 fixed, 3 open) | `windows append` | `.planning/WINDOWS.md` |
| `target/118.2-21-reqids-{before,after}.txt` | no-new-ID evidence | `target/` |
| `target/118.2-21-{semver,semver-fuzzing,gate,always,conf10,conf,windows}.log` | evidence logs | `target/` |

Created by SIBLING plans in this round: plan 19 — `StreamKind`, `PostReaderGuard`,
`open_post_readers`, the `start_sse` reset seam, the SSE-answered-POST harness mode
(`answer_calls_with_sse`, `call_answer_body`, `serve_post_sse_answer`,
`reopen_get_streams_held_open`), `LATCH_RESET_BOUND`, fences 21 and 22, and the `latch_gate` unit
submodule. Plan 20 — `MISMATCH_DISCARD_TIMEOUT`, `MISMATCH_RECEIVE_SLICE`,
`MAX_ID_MISMATCH_DISCARDS`, `MismatchBudget`, `Client::receive_bounded`,
`RecordingServer::answers_written`, `LOCK_PROBE_BOUND`, `MISMATCH_TIMEOUT_BOUND`, fences 23 and 24,
the `mismatch_budget` unit submodule, and the corrected comment at the discard site.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a document of record → an operator's risk assessment | a ledger booking an unbounded cost as bounded causes a live risk to be treated as already mitigated |
| a `fixed` ledger status → a reader's belief about a defect | entry 16 books CR-02 fixed while that closure's own code shipped two Criticals |
| the markdown table → its JSON mirror inside one file | a correction applied to one and not the other creates two disagreeing records where there was one |
| a released crate → downstream consumers | two consumer-observable behaviour changes ship with no semver signal, because every symbol is private |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118.2-21-01 | Repudiation | `deferred-items.md` `T-118.2-15-03`, CONF-09 limitation (vi), `WINDOWS.md` entry 13 | high | mitigate | All three assert a ceiling that does not exist, so a reader auditing residual risk concludes the defect is already mitigated and stops looking. Closed by REWRITING the false clause in each rather than annotating beneath it, verified by a negative grep proving the clause is GONE plus a positive grep proving the replacement is present — the "edited the file" acceptance criterion is exactly what would let this recur. |
| T-118.2-21-02 | Repudiation | CONF-09's row, which never mentioned BLOCKER 1 | high | mitigate | A permanent, process-lifetime failure of every SSE-answered `tools/call` was absent from the requirement's stated limitations entirely, so the row read as a complete disclosure when it was not. Closed by adding limitation (vii), stating plainly that it describes a defect that was OPEN when the row's previous amendment was written. |
| T-118.2-21-03 | Tampering | `WINDOWS.md`'s table and its JSON mirror | high | mitigate | A hand edit applied to one record and not the other produces two disagreeing ledgers in one file, and the CLI offers no edit subcommand to keep them in step. Mitigated by requiring every corrected literal to appear in BOTH, asserting a count of 0 for each removed clause across the whole file, parsing the JSON mirror in `<verify>`, and routing all NEW entries through `windows append` so the frontmatter counters stay derivable. |
| T-118.2-21-04 | Information Disclosure | the ephemeral `PMCP_REQUEST_STATE_KEY` the conformance run needs | medium | mitigate | A per-run state key echoed into a log or committed would be a credential in the repository and in CI output. Mitigated by generating it ephemerally, never echoing it, never committing it, and recording only that the guard was satisfied. |
| T-118.2-21-05 | Tampering | the D-16 conformance gate | high | mitigate | The cheapest way to make a closing record look green is to move the gate. Prohibited outright, and mechanically checked: `git status --porcelain -- scripts/run-conformance-suite.sh conformance/` must be empty and the round's `git diff --stat` over those paths must be empty, so no allowlist, expected-failures flag or known-failure baseline can have been introduced. |
| T-118.2-21-06 | Spoofing | a suite green presented as validating the client fixes | medium | mitigate | The official suite exercises pmcp only as a SERVER; attributing a client-transport fix to it would invent evidence. Mitigated by requiring the disclaiming sentence in the record and by keeping the suite result framed as a regression check on the D-16 exit-code gate. |
| T-118.2-21-07 | Elevation of Privilege | two behaviour changes shipping with no semver signal | medium | accept | Every symbol this round adds is private, so `cargo semver-checks` cannot see either change and no version bump is triggered. Accepted as the correct outcome — the alternative, minting public surface to make a fix visible to a linter, is worse — and the residual risk is carried by the two OPEN `WINDOWS.md` disclosures rather than by silence. |
| T-118.2-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installed and no manifest edited by this plan — it touches only `.planning/` documents and runs already-installed tooling. The conformance harness is resolved at the HELD `0.2.0-alpha.11` pin, asserted by the task's precondition rather than re-resolved, so no new package enters the tree and the package-legitimacy audit is not triggered. |
</threat_model>

<verification>
- `grep -c "bounded by the caller's own timeout" deferred-items.md`, `grep -c "waits out its caller's own timeout" .planning/REQUIREMENTS.md` and `grep -c "waits out its caller's own timeout" .planning/WINDOWS.md` all return 0, and each file names `MISMATCH_DISCARD_TIMEOUT`.
- `diff target/118.2-21-reqids-before.txt target/118.2-21-reqids-after.txt` is empty and `grep -c '^| CONF-09 |' .planning/REQUIREMENTS.md` is exactly 1.
- `WINDOWS.md`'s JSON mirror parses and its entry count equals the table's row count; `windows status` exits 0 and its counters are quoted before and after.
- Both semver invocations report `no semver update required`, with their check counts quoted verbatim.
- `binary(log_emitter)` with `binary(pmcp_both_ends_logging)` runs at least 26 tests, all passing, discharging all three backstop truths by name.
- `./scripts/run-conformance-suite.sh` exits 0 with both legs' figures recorded against `118.2-12`'s, and `git status --porcelain -- scripts/run-conformance-suite.sh conformance/` is empty.
- `make quality-gate` and `make validate-always` both exit 0, or every non-zero step is classified by provenance with a `git diff --name-only` citation.
</verification>

<success_criteria>
No document of record in this repository still claims the id-mismatch discard wait is bounded by a
timeout that does not exist; CONF-09 discloses the defect it never mentioned; the broken-windows
ledger states both Blockers at the severity the review found and agrees with itself across both of
the records it keeps; every finding this round declines carries a name, a consequence, a reason and an
owner; and the round closes on one authoritative semver verdict, a green gate, measured CONF-10
backstops, and a conformance result whose framing does not overclaim.
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-21-SUMMARY.md` when done.
</output>
