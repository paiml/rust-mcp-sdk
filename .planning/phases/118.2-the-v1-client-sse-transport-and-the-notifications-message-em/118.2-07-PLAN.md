---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 07
type: execute
wave: 3
depends_on: [118.2-06]
files_modified:
  - src/server/streamable_http_server/v1_session.rs
  - src/server/streamable_http_server/v1_session_off.rs
  - src/server/streamable_http_server.rs
  - tests/log_emitter.rs
autonomous: true
requirements: [CONF-10]

must_haves:
  truths:
    - "v1 logging/setLevel changes the level for THAT session only — client B cannot change client A's filtering"
    - "v2 honours the io.modelcontextprotocol/logLevel request _meta key, per request, with no session"
    - "A malformed level value neither panics nor is echoed back, and the request still serves"
    - "A full-v2 build allocates nothing for the session level — the null twin is a constant answer"
    - "The default level is info when nothing set one"
  artifacts:
    - path: "src/server/streamable_http_server/v1_session.rs"
      provides: "SessionInfo.log_level plus paired session_log_level / set_session_log_level accessors"
      contains: "log_level"
    - path: "src/server/streamable_http_server/v1_session_off.rs"
      provides: "the signature-identical null twin returning a constant, declaring nothing the real module lacks"
      contains: "const fn"
    - path: "src/server/streamable_http_server.rs"
      provides: "HTTP-layer capture of the v1 level and read of the v2 _meta key, resolved onto the request"
      contains: "LOG_LEVEL_REQUEST_META_KEY"
  key_links:
    - from: "logging/setLevel request at the HTTP ingress"
      to: "v1::V1State.sessions[session_id].log_level"
      via: "a session-keyed write beside the V2_RETIRED_METHODS table"
      pattern: "set_session_log_level"
    - from: "the resolved level"
      to: "RequestHandlerExtra.log_level"
      via: "threaded onto the request so the filter read is O(1) at emit time, not a lock per record"
      pattern: "with_log_level"
---

<objective>
Give the emitter a level to filter on, on both eras: v1's `logging/setLevel` stored PER SESSION
(D-11), v2's `io.modelcontextprotocol/logLevel` request `_meta` key read per request (D-10), and `info`
as the default when nothing set one (D-12).

Purpose: `LOG_LEVEL_REQUEST_META_KEY` (`src/server/streamable_http_server.rs:1429`) exists and is
referenced **only** by the `V2_RETIRED_METHODS` table (`:1485`). v2 today advertises a replacement
mechanism that does nothing — closing that is part of D-10.

**Why the level cannot live on the server.** `ServerState.server` is one
`Arc<tokio::sync::Mutex<Server>>` shared by every session (`streamable_http_server.rs:479`). Putting
the level there would let client B's `setLevel` change client A's filtering — the same defect class as
the deferred cross-session `client_capabilities` misattribution. The per-session home already exists and
is already v1-gated, which matches `setLevel`'s life exactly since v2 retired it. On a `full-v2` build
`V1State` is a zero-sized twin, so those builds allocate nothing for a mechanism they do not have —
structural, not a runtime branch someone can forget to take.

**RESEARCH Open Question 1, resolved here:** capture at the HTTP layer next to the retirement table and
thread the RESOLVED level onto the request's `RequestHandlerExtra`, so the dispatch arm (plan 08) only
has to answer `Ok(json!({}))` and the filter read is O(1) at emit time rather than a lock per record.

Output: the storage, both capture paths, the null twin, and the isolation fence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-CONTEXT.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-06-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. -->

The struct the level joins, `src/server/streamable_http_server/v1_session.rs:124-131`:

```
/// Both fields are private to this module: every read and write goes through an
/// operation below, so the twin never has to model a shape it does not hold.
#[derive(Debug, Clone)]
struct SessionInfo { initialized: bool, protocol_version: Option<String> }
```

The accessor pattern to copy, `:236-250` — note it DELIBERATELY collapses "no such session" and
"session with no value" into one `None`, precisely so the twin can represent it:

```
pub(crate) fn session_protocol_version(state: &V1State, session_id: &str) -> Option<String> {
    state.sessions.read().get(session_id).and_then(|info| info.protocol_version.clone())
}
```
Its writer twin is `insert_session` at `:222-234` — every `SessionInfo` write goes through one
module-private helper. Return an OWNED answer, never a borrow the zero-sized twin could not produce.

The null twin to mirror, `src/server/streamable_http_server/v1_session_off.rs:98-107`:

```
pub(crate) const fn session_protocol_version(_state: &V1State, _session_id: &str) -> Option<String> { None }
```
and `:73-81` — `pub(crate) struct V1State;`, a UNIT struct, not an empty-braced one: "the absence of
fields is the whole point, and the unit form makes it impossible to add one without changing the
declaration that the tripwire reads."

The declared-but-unused key, `src/server/streamable_http_server.rs:1420-1429` — its own rustdoc admits
nothing reads it:

```
const LOG_LEVEL_REQUEST_META_KEY: &str = "io.modelcontextprotocol/logLevel";
```
and the retirement table it feeds, `:1478-1493`, matched by EXACT BYTE EQUALITY (no case folding, no
trimming — T-118.1-05-01):

```
const V2_RETIRED_METHODS: [(&str, Option<&str>); 5] = [
    ("initialize", Some(SERVER_DISCOVER_METHOD)),
    ("ping", None),
    ("logging/setLevel", Some(LOG_LEVEL_REQUEST_META_KEY)),
    ("resources/subscribe", Some(SUBSCRIPTIONS_LISTEN_METHOD)),
    ("resources/unsubscribe", Some(SUBSCRIPTIONS_LISTEN_METHOD)),
];
```

`ServerState`'s v1 field, `:477-495` — `v1: v1::V1State`, one field not three, whose type comes from the
paired module so a `full-v2` build allocates no session map at all.

**Hard constraints on the null twin**, enforced by `tests/v1_severability_tripwire.rs`:
- `const fn` returning a constant answer where possible
- IDENTICAL parameter list to its real counterpart, including the ignored ones
- contains none of `FORBIDDEN_STATE_TYPES` (`:289`): `HashMap`, `BTreeMap`, `RwLock`, `Mutex`, `DashMap`,
  `SessionInfo`, `InMemoryEventStore`, `Arc<dyn EventStore`
- contains none of `FORBIDDEN_OPERATIONS` (`:309`): `.read()`, `.write()`, `.lock()`, `.insert(`,
  `.remove(`, `.entry(`, `.get_mut(`, `.contains_key(`, `tokio::spawn`, `LAST_EVENT_ID`,
  `MCP_SESSION_ID`, `.headers()`, `headers.get`
- declares NOTHING the real module does not (`the_v1_null_twin_declares_nothing_the_real_module_does_not`, `:726`)
- `V1State` stays a UNIT struct

`RequestHandlerExtra` gained `log_level: Option<LoggingLevel>` and `with_log_level(..)` in plan 05, and
`DEFAULT_LOG_LEVEL = LoggingLevel::Info`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Per-session log level in V1State, with its signature-identical null twin</name>
  <files>src/server/streamable_http_server/v1_session.rs, src/server/streamable_http_server/v1_session_off.rs</files>
  <read_first>
    - src/server/streamable_http_server/v1_session.rs — read the whole file, especially `SessionInfo` (`:124-131`), `V1State` (`:146`), `insert_session` (`:222-234`), `session_protocol_version` (`:236-250`), `route_to_session_stream` (`:292`) and `sessions_active_for` (`:333`).
    - src/server/streamable_http_server/v1_session_off.rs — read the whole file, especially the null-twin doctrine comments, `V1State` (`:73-81`) and `session_protocol_version` (`:98-107`).
    - tests/v1_severability_tripwire.rs — read `FORBIDDEN_STATE_TYPES` (`:289`), `FORBIDDEN_OPERATIONS` (`:309`), and the four null-twin tests at `:671`, `:704`, `:726`, `:760`. Read these BEFORE writing the twin, so the constraints shape the code rather than correct it.
  </read_first>
  <action>
Add `log_level: Option<LoggingLevel>` to `SessionInfo` in `v1_session.rs`, defaulting to `None` at every
construction site (`insert_session` and anywhere else a `SessionInfo` is built). `None` means "this
session never called `logging/setLevel`"; the D-12 default is applied at resolution time, not stored.

Add two module-level operations, following `session_protocol_version`'s shape exactly:
- `pub(crate) fn session_log_level(state: &V1State, session_id: &str) -> Option<LoggingLevel>` —
  deliberately collapses "no such session" and "session with no recorded level" into one `None`, for the
  same reason the version accessor does: both callers treat them identically, and the collapse removes a
  distinction the twin cannot represent. Say that in the rustdoc.
- `pub(crate) fn set_session_log_level(state: &V1State, session_id: &str, level: LoggingLevel)` — the
  single write path. A write for an unknown session id is a NO-OP, not an insert: minting a session row
  from a `logging/setLevel` would let an unauthenticated caller grow the session map by guessing ids.
  Say that in the rustdoc; it is a DoS control, not a style choice.

Add the signature-identical twins to `v1_session_off.rs`:
- `pub(crate) const fn session_log_level(_state: &V1State, _session_id: &str) -> Option<LoggingLevel> { None }`
  with rustdoc explaining: the 2026-07-28 transport retired `logging/setLevel` and carries the level per
  request in `_meta`, so a session-scoped level would be the wrong authority even if one existed.
- `pub(crate) const fn set_session_log_level(_state: &V1State, _session_id: &str, _level: LoggingLevel) {}`
  with the parallel rustdoc.

The twin must contain no `FORBIDDEN_STATE_TYPES` and no `FORBIDDEN_OPERATIONS` token, `V1State` must
stay a unit struct, and the twin must declare nothing the real module lacks. Verify by running the
severability tripwire, not by inspection.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full" 2>&1 | tee target/118.2-07-t1.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped' &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'session_log_level' src/server/streamable_http_server/v1_session.rs` returns ≥ 2 and the same for `v1_session_off.rs`
    - `grep -c 'struct V1State;' src/server/streamable_http_server/v1_session_off.rs` returns 1 — still a unit struct
    - `grep -nE '\.read\(\)|\.write\(\)|\.lock\(\)|\.insert\(|\.entry\(|HashMap|RwLock|SessionInfo' src/server/streamable_http_server/v1_session_off.rs` returns no NEW hits introduced by this task
    - `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` reports a NON-ZERO test count with 0 failed
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
    - A write for an unknown session id inserts nothing — assert this in the next task's fence, and note the rustdoc statement here
  </acceptance_criteria>
  <done>The v1 session carries a log level through paired accessors, and the `full-v2` twin answers a constant while allocating nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the level at the HTTP ingress on both eras and resolve it onto the request</name>
  <files>src/server/streamable_http_server.rs</files>
  <read_first>
    - src/server/streamable_http_server.rs — read `LOG_LEVEL_REQUEST_META_KEY` (`:1420-1429`) and its rustdoc, `V2_RETIRED_METHODS` (`:1478-1493`) and the ingress code that applies it, `ServerState` (`:477-495`), `v2_multi_frame_eligible` (`:2580`), `attach_v2_progress_sink` (`:2600`), and the `handle_request_with_context` call at `:3765`.
    - src/server/streamable_http_server/peer_channel.rs — `attach_session_backchannel` (`:605-650`), because the level must be threaded at the same layer that already knows the session id and already augments the `ProtocolContext`.
    - src/server/cancellation.rs — the `log_level` field, `with_log_level` and `DEFAULT_LOG_LEVEL` from plan 05.
    - src/types/notifications.rs — `LoggingLevel`'s serde (`rename_all = "lowercase"`), so parsing a peer-supplied value uses the same mapping the wire uses.
  </read_first>
  <action>
Two capture paths, both at the HTTP ingress, plus one resolution step.

**(a) v1 capture — `logging/setLevel`.** At the ingress, beside the `V2_RETIRED_METHODS` handling, detect
a v1 request whose method is exactly `logging/setLevel` (exact byte equality — no case folding, no
trimming, matching the retirement table's rule) and, when a `Mcp-Session-Id` is present, parse
`params.level` and call `v1::set_session_log_level(&state.v1, session_id, level)`. The request then
continues to dispatch normally — plan 08 owns what the dispatch arm answers, and **Pitfall 8 (MEASURED)
requires it to stay a literal `{}`**.

**(b) v2 capture — the `_meta` key.** Read `params._meta["io.modelcontextprotocol/logLevel"]` using the
existing `LOG_LEVEL_REQUEST_META_KEY` constant. This is per request; v2 is session-free by construction,
so nothing is stored. This is what closes the "advertised a replacement mechanism that does nothing"
gap.

**(c) Resolution.** Thread the RESOLVED level onto the request so the emitter's filter read is O(1) at
emit time rather than a lock per record. Precedence, stated once in a comment at the resolution site:
1. v2: the request `_meta` key, when the era is V2;
2. v1: the session's stored level, when sessions are on and a session id is present;
3. otherwise: nothing — leave `RequestHandlerExtra.log_level` as `None` and let `DEFAULT_LOG_LEVEL` apply
   at emit time.
Set it with `with_log_level(..)` at the same layer that already augments the request context
(`attach_session_backchannel`'s neighbourhood is the natural home — it already has both the session id
and the carrier).

**Malformed input (ASVS V5, Claude's discretion resolved here).** A malformed
`io.modelcontextprotocol/logLevel` value, or a malformed `logging/setLevel` `params.level`:
- MUST NOT panic;
- MUST NOT be echoed into any log line or error message — the value is peer-supplied, and this crate has
  an established rule that an error derived from peer input does not echo the input
  (`collected_body_over_cap`, `listen_overflow`);
- is IGNORED, falling back to the default `info`, rather than rejected with `-32602`. Rationale to state
  in the comment: the key is advisory and per request, and rejecting a whole tool call because a
  diagnostic hint was misspelled converts a logging preference into an availability failure. The v1
  `logging/setLevel` RPC is a different case — it is a request whose only purpose is the level — but
  Pitfall 8 pins its RESPONSE to a literal `{}`, so it also stores-or-ignores and still answers `{}`.
Record this decision explicitly in the SUMMARY; CONTEXT.md left it to Claude's discretion.

**Do not gate anything on `ServerCapabilities.logging`** (RESEARCH Open Question 6) — the sink is
per-request and the capability is advisory; gating would make a correctly-plumbed server silently mute.

**Do NOT touch `crates/pmcp-team-servers/src/conformance/era_target.rs`** (Pitfall 9). Its
`observed_log_level` reads the v2 `_meta` key only, gated on `extra.era() == Some(Era::V2)`, and falls
back to `("info", "server-default")`; `era_matrix`'s `v1_capability_arm` asserts
`levelSource == "server-default"` AND `observedLevel != "debug"` AFTER calling
`client.set_logging_level(Debug)`, to make baseline row ERA-12 (`ignored` on v1 → `honored` on v2)
observable. Changing the era target without changing that arm reds it; changing both erases the ERA-12
contrast. Leave both alone in this phase and prove the v1 level with the dedicated fence in task 3.
Plan 10 owns the era_matrix edits and must not be pre-empted here.

Keep every touched function under PMAT cog 25 — the ingress is already dense, so extract the capture and
the resolution into named helpers rather than growing the existing handler.
  </action>
  <verify>
    <automated>RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full" 2>&1 | tail -3 &amp;&amp; cargo nextest run -E 'binary(v2_retired_methods)' --features "full" 2>&1 | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'LOG_LEVEL_REQUEST_META_KEY' src/server/streamable_http_server.rs` returns ≥ 3 — the declaration, the retirement table, and at least one real READER (today it is 2, with no reader)
    - `grep -c 'set_session_log_level' src/server/streamable_http_server.rs` returns ≥ 1
    - `grep -c 'with_log_level' src/server/streamable_http_server.rs` returns ≥ 1
    - `git diff --stat -- crates/pmcp-team-servers/` is empty — the era target and era_matrix are untouched by this plan
    - `cargo nextest run -E 'binary(v2_retired_methods)' --features "full"` reports a NON-ZERO test count with 0 failed — the retirement table still behaves
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` and `--no-default-features --features full-v2` both exit 0
    - No touched function exceeds PMAT cognitive complexity 25
  </acceptance_criteria>
  <done>The v2 `_meta` key has a real reader for the first time, the v1 RPC writes a per-session level, and the resolved level rides the request.</done>
</task>

<task type="auto">
  <name>Task 3: Wire fences for session isolation, the v2 key, the default and malformed input</name>
  <files>tests/log_emitter.rs</files>
  <read_first>
    - tests/log_emitter.rs — the file and fences from plans 05 and 06.
    - tests/http_peer_roundtrip.rs — read `build_server` / `build_dual_era_server` / `spawn_server` (`:302-370`) and `struct Conn` (`:372-470`). These fences need a real `StreamableHttpServer` on an ephemeral port with a raw stream-holding client, which is exactly what this harness provides. pmcp's own client cannot be used as the observer yet — plan 10 owns the joint fence.
    - src/server/streamable_http_server.rs — the capture and resolution helpers task 2 just wrote.
  </read_first>
  <action>
Add wire fences to `tests/log_emitter.rs` over a real `StreamableHttpServer` on an ephemeral port, with a
raw stream-holding client reader lifted from `tests/http_peer_roundtrip.rs`. Register a tool whose
handler calls `extra.log(..)` at several levels. Every wire await is `tokio::time::timeout(BOUND, ..)`-wrapped.

1. `a_v1_handler_log_record_reaches_the_live_session_stream` — v1 session, raw client holds the GET SSE
   stream open, call the tool, assert a `notifications/message` frame arrives on that stream inside
   `BOUND`, and assert its `params.level` and `params.message` on the JSON.

2. `a_v2_handler_log_record_rides_the_multi_frame_post_body` — v2 request with
   `Accept: text/event-stream` and `enable_json_response` off (the `v2_multi_frame_eligible` conditions),
   assert the record arrives as a notification frame on the POST response body BEFORE the result frame.

3. `the_v1_level_is_per_session_and_client_b_cannot_change_client_a` — two sessions A and B. B sends
   `logging/setLevel` with `debug`; A does not. Emit a `debug` record inside a tool call on EACH session.
   Assert B receives it and A does NOT. This is the D-11 claim and the ASVS V3 control; it is the fence
   that would have caught the deferred cross-session `client_capabilities` defect had it existed.

4. `the_v2_meta_key_sets_the_level_for_that_request_only` — two v2 requests, one carrying
   `io.modelcontextprotocol/logLevel: "debug"` in `_meta` and one not. Assert the `debug` record appears
   for the first and not the second.

5. `an_unset_level_defaults_to_info` — no `setLevel`, no `_meta` key: a `debug` record is suppressed and
   an `info` record is delivered.

6. `a_malformed_level_value_is_ignored_and_not_echoed` — send
   `io.modelcontextprotocol/logLevel: "NOT_A_LEVEL"` (and separately a `logging/setLevel` with a bogus
   `level`). Assert: the request still SERVES (HTTP 200 / a normal result, not a 4xx or a JSON-RPC
   error), the effective level is the `info` default, no panic, and the literal string `NOT_A_LEVEL` does
   NOT appear anywhere in the response body or in any emitted notification. Use a distinctive sentinel so
   the absence assertion is meaningful.

7. `a_set_level_for_an_unknown_session_id_inserts_no_session` — send `logging/setLevel` with a
   `Mcp-Session-Id` that was never issued and assert no session row is created (observe through whatever
   session-count surface exists, e.g. `sessions_active_for`, or through a follow-up request that must
   still be treated as un-initialized). This pins task 1's no-op-write DoS control.

Demonstrate RED first for the fences that can be red before tasks 1-2 land; capture to
`target/118.2-07-red.log`, then GREEN to `target/118.2-07-green.log`.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(log_emitter)' --features "full" 2>&1 | tee target/118.2-07-green.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run -E 'binary(log_emitter)' --features "full"` reports a NON-ZERO test count with 0 failed, strictly greater than plan 06's recorded count
    - All seven fence names above exist in `tests/log_emitter.rs`
    - `grep -c 'timeout(' tests/log_emitter.rs` returns at least one per wire fence
    - The malformed-input fence asserts the SENTINEL string is absent from the response and from every emitted notification
    - `target/118.2-07-red.log` and `target/118.2-07-green.log` form the RED→GREEN pair
    - `cargo nextest run -E 'binary(era_matrix)' --features "full"` (in `crates/pmcp-team-servers`) still reports a NON-ZERO count with 0 failed — ERA-12 is undisturbed
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>Both eras honour a level, the v1 level is provably per-session, malformed input is inert and unechoed, and the ERA-12 contrast is undisturbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client A session ↔ client B session | Both are served by ONE `Arc<Mutex<Server>>`; anything stored server-wide is cross-session |
| peer-supplied `level` value → server state | `logging/setLevel` params and the v2 `_meta` key are both attacker-controlled strings |
| unknown session id → session map | A write keyed by a peer-supplied id can grow server state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.2-07-01 | Tampering / Information Disclosure | per-session log level | mitigate | D-11: store in `v1::V1State.sessions` keyed by session id, NEVER on the shared `Arc<Mutex<Server>>`. Fenced by `the_v1_level_is_per_session_and_client_b_cannot_change_client_a` (ASVS V3) |
| T-118.2-07-02 | Denial of Service | session map growth via `logging/setLevel` | mitigate | `set_session_log_level` for an unknown session id is a NO-OP, never an insert, so a caller cannot grow the map by guessing ids. Fenced by `a_set_level_for_an_unknown_session_id_inserts_no_session` |
| T-118.2-07-03 | Denial of Service / Input Validation | malformed level value | mitigate | Parse-or-ignore with a fallback to the `info` default; never panic; the request still serves. A malformed advisory hint must not convert into an availability failure (ASVS V5). Fenced by `a_malformed_level_value_is_ignored_and_not_echoed` |
| T-118.2-07-04 | Information Disclosure / log injection | echoing the peer's level string | mitigate | The value is never echoed into an error message or a log line, per this crate's established `collected_body_over_cap` / `listen_overflow` rule (ASVS V7). Fenced by the sentinel-absence assertion |
| T-118.2-07-05 | Tampering | `full-v2` build carrying session state | mitigate | The null twin is a `const fn` returning a constant, `V1State` stays a unit struct, and `tests/v1_severability_tripwire.rs` enforces the forbidden-type and forbidden-operation token sets plus `the_v1_null_twin_declares_nothing_the_real_module_does_not` |
| T-118.2-07-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — no packages installed |
</threat_model>

<verification>
- `cargo nextest run -E 'binary(log_emitter)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(v2_retired_methods)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(era_matrix)' --features "full"` in `crates/pmcp-team-servers` — non-zero count, 0 failed
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`
- `git diff --stat -- crates/pmcp-team-servers/` — empty
- `make quality-gate`
</verification>

<success_criteria>
- `LOG_LEVEL_REQUEST_META_KEY` has a real production reader for the first time
- The v1 level is provably per-session, and `full-v2` allocates nothing for it
- A malformed level is inert, unechoed, and non-fatal
- ERA-12's `ignored on v1 / honored on v2` contrast in `era_matrix` is undisturbed by this plan
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-07-SUMMARY.md` when done.
Record: where the capture landed (HTTP ingress vs dispatch), the resolution precedence verbatim, the malformed-input decision (ignore-and-default, with its rationale), and confirmation that `crates/pmcp-team-servers/` is byte-unchanged.
</output>
