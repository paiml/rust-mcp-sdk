---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
verified: 2026-08-20T23:24:57Z
verified_against_head: dc0587af
status: passed
score: 5/5 must-haves verified (1 override applied)
behavior_unverified: 0
overrides_applied: 1
overrides:
  - must_have: "The v1 client SSE transport this phase shipped is safe to rely on end-to-end — a peer cannot drive it into unbounded resource consumption, a terminal transport error cannot be silently paired with or displace an unrelated request's response, and a single bad frame cannot wedge the client"
    reason: "The only residual inside this truth's wording is CR-03, deferred by explicit developer decision on 2026-08-18: the defect lives exclusively on the v1 POST-response SSE reader path, i.e. on the surface being deprecated in favour of stateless calls plus MCP Tasks with task-store polling for progress. Severity re-measured against the post-router tree to a DELAYED error rather than a lost or mis-delivered answer. Disclosed as WINDOWS.md entry 23 with a named owner, mirrored into pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md, and booked in 118.2-UAT.md's Gaps block. CR-01, CR-02 (both halves) and the send-path wedge are closed at HEAD dc0587af and each is fenced by a named test verified passing."
    accepted_by: "Guy Ernest"
    accepted_at: "2026-08-20T23:40:00Z"
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  previous_verified: 2026-08-20T00:48:35Z
  previous_verified_against_head: 5caeed05
  why_re_verified: >
    The prior verification recorded `verified_against_head: 5caeed05`. That
    commit is ~50 commits behind HEAD and predates plans 118.2-22, -23, -24 and
    -25, a security audit (`d4270253`, `9f98f120`), a squash-merge to main
    (`370c8902`, PR #337) and the UAT (`dc0587af`). It described a tree that no
    longer exists — the SECOND consecutive verification of this phase to go
    stale that way. Every verdict below was re-derived from source at
    `dc0587af` and from commands this verifier ran in its own process. No
    SUMMARY, no log-of-record and no prior verdict was accepted as evidence.
    NOTE ON LOGS OF RECORD: every `target/118.2-*.log` cited by
    `REQUIREMENTS.md` and the plan summaries is ABSENT at HEAD (`target/` is
    gitignored). The recorded counts are therefore not independently checkable,
    which is why the semver verdict and the official-suite measurement below
    were re-taken rather than quoted.
  gaps_closed:
    - >
      GAP 1 OF THE PRIOR RUN — "CR-02's SECONDARY half, the self-sustaining
      cross-call failure, SURVIVES under `UNMATCHED_RESPONSE_TIMEOUT` and is
      UNDISCLOSED ANYWHERE" — is CLOSED IN CODE and its disclosure claim was
      already FALSE at HEAD. Closed by 118.2-22 with a mechanism read directly
      at source: `dispatch_request` has ONE exit-cleanup site
      (`src/client/mod.rs:4158-4166`) which records the abandonment only when
      `active_requests.remove()` returned `Some` — i.e. only when no answer had
      been delivered and the peer's real answer may still be on the wire — and
      `pump_once` asks `absorb_abandoned` BEFORE booking a frame as `Unmatched`
      (`:4241`, `:4323`). Our own debris therefore charges nobody's budget.
      `AbandonedRequestIds` (`:424-460`) is bounded by construction: fixed cap
      `MAX_ABANDONED_REQUEST_IDS` = 64, oldest-evicted, entries consumed on
      first use, and only ids obtained from a live registration ever enter it,
      so a hostile peer cannot grow it. FENCE RUN BY THIS VERIFIER:
      `debris_from_a_dead_call_does_not_charge_the_next_calls_budget`
      (`tests/client_sse_stream.rs:3736`) — passes. THE PRIOR RUN'S FLAGSHIP
      EVIDENCE IS FALSE AT HEAD: it asserted `grep -rn "UNMATCHED_RESPONSE_TIMEOUT"
      .planning/` returns ZERO hits; re-run at HEAD it returns 45.
    - >
      GAP 2 OF THE PRIOR RUN — the SEND-PATH LOCK WEDGE it raised as a NEW
      regression ("`:3910`/`:3921` hold `self.transport.write()` across
      `send`/`send_raw` → `post_once`, no request timeout, whole-`Client` wedge,
      `close()` included, unfenced") — is CLOSED by 118.2-23. Re-derived:
      `/usr/bin/grep -n "transport.write()\|transport.read()" src/client/mod.rs`
      now returns FOUR sites (`:2430`, `:4208`, `:5187`, `:5363`) and NONE of
      them spans a send round trip. All four client send sites (`:4051`,
      `:4064`, `:4294`, `:5140`) route through `Client::send_frame` (`:5176`),
      which uses an owned `SharedSender` handle resolved at CONSTRUCTION
      (`:625`) and takes no guard at all on the opt-in path.
      `StreamableHttpTransport` answers `Some` (`src/shared/streamable_http.rs:3288`)
      by cloning itself — every field an `Arc`, so an ownership change, not a
      lifetime change. FENCE RUN BY THIS VERIFIER:
      `a_peer_that_never_writes_response_headers_does_not_serialise_the_client`
      (`tests/client_sse_stream.rs:4281`) — passes, and it is non-vacuous: it
      carries an `Arm::Call` guard that panics if the stalled POST returned
      first, plus two wire-level vacuity guards (exactly 2 calls observed,
      exactly 1 `notifications/cancelled`).
    - >
      GAP 3 OF THE PRIOR RUN — "THE DOCUMENTATION OF RECORD IS NOW MATERIALLY
      INACCURATE ABOUT THE CODE", items (a) through (d) — is CLOSED on all four
      counts by 118.2-24, verified by reading the record itself.
      (a) `WINDOWS.md` entry 20's "per-id routing removes discards entirely and
      therefore removes this too" is GONE, replaced by a dated in-place
      correction that names the sentence it replaces and states what actually
      bounds the client (`UNMATCHED_RESPONSE_TIMEOUT` 10 s /
      `MAX_UNMATCHED_RESPONSES` 32, armed on the first frame nobody awaits) and
      that the self-sustaining chain was closed by 118.2-22.
      (b) The five deleted identifiers (`MISMATCH_DISCARD_TIMEOUT`,
      `MAX_ID_MISMATCH_DISCARDS`, `MISMATCH_RECEIVE_SLICE`,
      `Client::receive_bounded`,
      `a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever`)
      now appear in `WINDOWS.md` and `REQUIREMENTS.md` only on lines explicitly
      marked SUPERSEDED BY, each naming its replacement. `/usr/bin/grep -rn` for
      all five over `src/` and `tests/` returns ZERO relevant hits — the single
      `receive_bounded` match is an unrelated local test helper at
      `tests/v2_subscriptions.rs:1661`.
      (c) Entry 13 carries a dated "BEHAVIOUR SUPERSEDED 2026-08-19" clause
      stating that a frame belonging to a LIVE caller is now ROUTED and only a
      frame nobody awaits is dropped, and re-pointing `LOCATE BY SYMBOL` at
      `pump_once`, `PumpStep`, `Pending`, `UnmatchedBudget`,
      `AbandonedRequestIds`, `absorb_abandoned` — all six of which resolve at
      HEAD.
      (d) The unrecorded THIRD round now has `WINDOWS.md` entry 24 as its
      retrospective entry of record (naming its four commits, what it deleted,
      what it added, what it left behind), plus a CONF-09 fourth amendment.
    - >
      GAP 4 OF THE PRIOR RUN — five stale `MISMATCH_*` fence-calibration
      comments in `tests/client_sse_stream.rs` (`:244`, `:248`, `:2400`,
      `:3146`, `:3163`) — is CLOSED. `/usr/bin/grep -c "MISMATCH_DISCARD_TIMEOUT"
      tests/client_sse_stream.rs` returns 0.
    - >
      NEW SINCE THE PRIOR RUN, and CLOSED — a critical the prior verification
      never saw because the code did not exist yet. The security audit found
      T-118.2-25-01 open at HEAD: `refresh_lock` single-flighted the 401
      RECOVERY vend while the ORDINARY vend every request build performs was
      UNGUARDED, so N concurrent first requests on one cloned transport vended N
      times against an empty cache — permanent auth failure against a rotating
      refresh token before any 401. Closed by `ColdVendGate`
      (`src/shared/streamable_http.rs:1218-1268`), and VERIFIED WIRED at both
      reach points rather than only at its definition: the ordinary build path
      (`:2490`, via `RequestParts.cold_vend_gate` at `:740`/`:2419`) and the
      reconnect path (`:4191`, via `SseReaderContext`), re-armed on eviction
      (`:2893`). FENCE RUN BY THIS VERIFIER:
      `a_cold_start_fanout_on_one_transport_vends_once`
      (`tests/client_sse_stream.rs:4436`) — passes, and it is non-vacuous: it
      asserts a PEAK concurrency of <= 1 inside the vend, exactly 1 total vend,
      and 0 purges (the last so it cannot silently drift onto the 401 path fence
      28 already covers).
  gaps_remaining:
    - >
      CR-03 — the ONE remaining item that sits inside truth 5's own wording. It
      is UNCHANGED IN CODE at HEAD: `drain_or_latch` still gates on the
      transport-wide `open_post_readers.load(Ordering::SeqCst) > 0`, and
      `PostReaderGuard::drop` still wakes only on the 1 -> 0 transition. It is
      OPEN, ACCEPTED and DISCLOSED — deferred by an explicit developer decision
      quoted verbatim in `118.2-UAT.md` test 6 ("Since we are moving away from
      SSE, and more toward using stateless calls with MCP Tasks using task
      storage for progress, I think that we can show it as a gap and defer its
      fix (if any) to the future"), recorded as `WINDOWS.md` entry 23 with a
      re-measured severity and an owner, mirrored into
      `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md`, and booked in
      `118.2-UAT.md`'s Gaps block. THIS IS NOT A NEW FINDING AND NOTHING HERE
      RE-LITIGATES IT. What is missing is purely procedural: NO `overrides:`
      entry exists in this file, so truth 5 cannot be scored as verified. That
      is the human decision this run escalates.
  new_findings:
    - >
      WARNING (newly derived, not carried from any prior run): R-118.2-02 /
      T-118.2-25-03 is disclosed ONLY in the phase-internal `118.2-SECURITY.md`,
      breaking the disclosure convention every sibling residual in this phase
      follows. THE DEFECT ITSELF IS REAL AND I CONFIRMED IT AT SOURCE:
      `start_sse` takes `restart_lock` at `src/shared/streamable_http.rs:2126`
      and holds it across `context.open_sse_once(cursor).await` at `:2141`,
      which awaits the peer's GET response HEAD; and there is no request timeout
      on any production path in that file — `/usr/bin/grep -n timeout` returns
      exactly ONE production hit, `.pool_idle_timeout(90s)` at `:1646`, with
      every `tokio::time::timeout` at `:5488`+ inside
      `#[cfg(all(test, not(target_arch = "wasm32"), feature = "streamable-http"))]
      mod tests`, which opens at `:4495`. So a peer that accepts the GET and
      never writes its head holds a TRANSPORT-WIDE lock indefinitely, blocking
      every other session-stream restart on that transport.
      THE DISCLOSURE GAP: `WINDOWS.md` mentions `T-118.2-25-03` exactly ONCE, in
      passing, as a constraint the `ColdVendGate` design honours ("which is what
      keeps this from re-creating the whole-transport bottleneck T-118.2-25-03
      prohibits") — never as an open defect; and the migration chapter's entry-26
      paragraph does not mention it at all. Contrast the siblings, all of which
      got ledger entry + chapter mirror: CR-03 (entry 23), the non-caching
      `AuthProvider` residual (entry 26 + chapter "What you must check on your
      side"), the restart-vs-`close()` pairing (entry 26 residual (ii) +
      chapter), and both 118.2-23 residuals (entry 27 + chapter item (3)).
      MITIGATING, stated so this is not overweighted: the chapter DOES disclose
      the governing policy ("a peer that has simply gone quiet is still not
      bounded at all — pmcp has no default request timeout, deliberately... Set
      your own deadline around a call if you need one"). What a consumer is not
      told is the second-order effect — that one stalled GET head blocks OTHER
      callers' restarts on the same transport. WARNING, not BLOCKER: nothing
      false is asserted anywhere, unlike the prior run's documentation finding.
    - >
      INFO: `118.2-SECURITY.md`'s R-118.2-01 states its verification method as
      "`git diff cb5d1365..HEAD` over all manifests" and concludes "the two
      manifest touches in range are a `[[bin]]` registration and an
      `[[example]]` entry". THE CONCLUSION IS TRUE FOR 118.2 AND I VERIFIED IT
      INDEPENDENTLY — the only 118.2 commits touching a manifest are `be14c52e`
      (a `[[bin]]` fuzz-target registration, explicitly "adds NO dependency") and
      `241b70d6` (the `s55_handler_logging` `[[example]]` entry). BUT THE STATED
      COMMAND DOES NOT SUPPORT IT: run at HEAD, `git diff cb5d1365..HEAD --
      '*Cargo.toml'` shows 12 files / 79 insertions and DOES add dependencies
      (`tracing-subscriber 0.3.20` in `cargo-pmcp`, `serial_test` 3 -> 4, `toml`
      0.9 -> 1.1), all from non-118.2 milestone work merged into the range. The
      finding is a record imprecision — right answer, wrong evidence — not a
      supply-chain exposure.
  regressions: []
human_verification:
  - test: >
      DECIDE CR-03: either accept it as a scored override or re-open it. The
      developer decision to defer already exists and is quoted verbatim in
      `118.2-UAT.md` test 6 (2026-08-18), is recorded as `WINDOWS.md` entry 23
      with a named owner, and is mirrored into the consumer-facing migration
      chapter. What does NOT exist is an `overrides:` entry in this file, which
      is the only thing that lets truth 5 be scored. To accept, add the block
      printed under "Suggested override" below to this file's frontmatter with a
      real `accepted_by` and `accepted_at`, and re-run verification; truth 5
      then scores 5/5 with the residual named in its evidence.
    expected: >
      Either an `overrides:` entry carrying CR-03 with a named human acceptor
      and an ISO timestamp, or an instruction to plan its closure.
    why_human: >
      This is a scope/acceptance decision, not a code fact. Every code fact
      around it has been re-derived at HEAD: the gate is unchanged at
      `src/shared/streamable_http.rs`, the defective-shape unit test still pins
      the single-reader case, and no two-concurrent-reader fence exists anywhere
      in the crate. No amount of further grepping resolves whether an
      acknowledged, disclosed, owner-assigned deferral should count toward this
      phase's score. A verifier minting that override on its own authority would
      be the exact failure mode the adversarial stance forbids.
  - test: >
      DECIDE the R-118.2-02 disclosure gap. Confirm whether an open,
      consumer-observable residual accepted by the security audit must also get
      a `WINDOWS.md` entry (or an amendment to entry 26) and a migration-chapter
      sentence, as CR-03 and all four sibling residuals in this phase did — or
      whether `118.2-SECURITY.md`'s Accepted Risks Log is a sufficient home on
      its own.
    expected: >
      Either an entry-26 amendment plus one chapter sentence stating that
      `restart_lock` is held across an untimed GET response-head await, so a
      peer that accepts the GET and never answers blocks every session-stream
      restart on that transport; or a written decision that SECURITY.md alone
      suffices for accepted risks, which would then apply to future phases too.
    why_human: >
      The code fact is settled and cited above (`:2126`, `:2141`, `:1646`,
      `:4495`). What is unsettled is a convention question — which record
      surface owns an accepted risk — and this phase has an established
      convention that this one item alone does not follow.
deferred:
  - truth: "CR-03 — the transport-wide in-flight POST-reader gate answers a per-caller question"
    addressed_in: "Not scheduled to a phase number — the client-transport hardening plan named in this phase's deferred-items.md Disposition section"
    evidence: "WINDOWS.md entry 23; developer decision quoted in 118.2-UAT.md test 6; mirrored into pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md; tests/windows_disclosure_tripwire.rs 1/1 pass (RUN BY THIS VERIFIER)"
  - truth: "R-118.2-03 — 'exactly one vend' is preconditioned on a caching AuthProvider; the trait states no caching contract"
    addressed_in: "Not scheduled to a phase number — same owner (client-transport hardening plan)"
    evidence: "118.2-SECURITY.md Accepted Risks Log R-118.2-03; WINDOWS.md entry 26 residual (i); ColdVendGate rustdoc src/shared/streamable_http.rs:1245-1255; migration chapter 'What you must check on your side'"
  - truth: "The 118.2-23 residuals — PooledTransport keeps the exclusive send path, and Client::open_event_stream still holds a read guard across the subscriptions/listen POST response head"
    addressed_in: "Not scheduled to a phase number — same owner"
    evidence: "WINDOWS.md entry 27; disclosed in place at the guard, src/client/mod.rs:5348-5363; migration chapter entry-27 paragraph item (3)"
  - truth: "Closure-2 review WR-02..WR-05 and IN-01..IN-04; first review's WR-03..WR-06 and IN-01..IN-06"
    addressed_in: "Not scheduled to a phase number — same owner"
    evidence: "deferred-items.md '## SECOND GAP-CLOSURE ROUND (plans 118.2-19..21)'; WINDOWS.md entry 21"
---

# Phase 118.2: The v1 client SSE transport and the `notifications/message` emitter — FOURTH-ROUND Re-Verification Report

**Phase Goal:** Close the two residuals Phase 118.1 measured and could not close within its own scope, so that the server-to-client channel 118.1 built is usable end to end by pmcp's OWN client, and a tool handler can emit MCP log notifications.

**Verified:** 2026-08-20T23:24:57Z, against HEAD `dc0587af`
**Status:** human_needed
**Score:** 4/5
**Re-verification:** Yes — fourth round, against a tree ~50 commits newer than the one the prior verification described

## Bottom line

The prior verification (`verified_against_head: 5caeed05`) failed truth 5 on four things. **Three of
the four are closed in code at HEAD and the fourth was already false when it was written.** I re-derived
every one from source and ran the fence for each rather than reading a SUMMARY:

- **CR-02's self-sustaining chain — CLOSED** by 118.2-22 (`AbandonedRequestIds` / `absorb_abandoned`),
  fence `debris_from_a_dead_call_does_not_charge_the_next_calls_budget` passes.
- **The send-path lock wedge (the prior run's own NEW regression) — CLOSED** by 118.2-23
  (`Client::send_frame` over an owned `SharedSender`), fence
  `a_peer_that_never_writes_response_headers_does_not_serialise_the_client` passes. Zero client-side
  lock sites now span a send.
- **The documentation-of-record blockers — CLOSED** by 118.2-24, all four items (a)–(d).
- **"`grep -rn UNMATCHED_RESPONSE_TIMEOUT .planning/` returns ZERO hits" — FALSE AT HEAD.** It returns
  **45**. That claim was the load-bearing evidence for the prior run's first gap.

Two things landed after the plans and I verified both independently, because neither had a plan behind
it: a **critical** the security audit found (`T-118.2-25-01`, the ordinary cold-cache token vend was
UNGUARDED while only the 401 recovery was single-flighted) is closed by `ColdVendGate`, wired on **both**
the ordinary build path and the reconnect path, with a non-vacuous fence; and I re-took the two
measurements the record could no longer prove, because **every `target/118.2-*.log` cited across
`REQUIREMENTS.md` and the summaries is absent at HEAD**.

**What is left is one open item inside truth 5's wording — CR-03 — and it is not a new finding.** It is
open, unfixed, explicitly deferred by a developer decision quoted verbatim in `118.2-UAT.md`, and
disclosed in the ledger and in the consumer-facing chapter. The only thing missing is procedural: this
file carries no `overrides:` entry, so truth 5 cannot be scored. **That is a decision, not a defect, and
this run escalates it rather than manufacturing a fourth consecutive `gaps_found` over a gap whose own
record says "DEFERRED, not scheduled".**

**One genuinely new WARNING, derived here and not carried from any prior run:** `R-118.2-02` /
`T-118.2-25-03` — `restart_lock` held across `open_sse_once`'s untimed GET response-head await — is
disclosed **only** in the phase-internal `118.2-SECURITY.md`. Every sibling residual in this phase got a
ledger entry **and** a migration-chapter sentence; this one got neither. The defect is real and I
confirmed it at source. It is a WARNING and not a BLOCKER because, unlike the prior round's finding,
nothing false is asserted anywhere, and the governing policy (no default request timeout) *is* disclosed
to consumers.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | pmcp's own `StreamableHttpTransport` client can hold a live GET SSE stream and consume server-initiated messages over v1 HTTP | ✓ VERIFIED | `cargo test --features full --test client_sse_stream` RUN BY THIS VERIFIER at HEAD: **31 passed, 0 failed** (up from 26 at `5caeed05`). Corroborated end-to-end by the official suite, which I also ran: the `2025-11-25` leg exits **0** with its entire scored set green |
| 2 | A tool/prompt/resource handler can emit `notifications/message` log records during a call, reaching the client on both eras | ✓ VERIFIED | `cargo test --features full --test log_emitter --test pmcp_both_ends_logging` RUN BY THIS VERIFIER: **25 + 2 = 27 passed, 2 ignored, 0 failed**. The suite run below asserts `GAP_ATTRIBUTABLE_FAILURES` is now 0 — `tools-call-with-logging`, the ONE gap-attributable failure this requirement existed to close, is green |
| 3 | Both changes are additive to public API surface, with a `cargo semver-checks` verdict recorded | ✓ VERIFIED (verdict RE-TAKEN, not quoted) | `cargo semver-checks --baseline-rev cb5d1365 -p pmcp` RUN BY THIS VERIFIER at HEAD: `pmcp v2.18.0 -> v2.19.0 (minor change)`, **196 checks: 196 pass, 57 skip**, `Summary no semver update required`, exit 0. My check/skip split differs from the recorded `223/30` because the local tool version carries a different lint set — the VERDICT is identical, and the recorded `target/118.2-24-semver.log` does not exist at HEAD to compare against. The one genuinely public addition is additive by construction: `SharedSender` is a NEW trait and `Transport::shared_sender` is DEFAULTED to `None` on both the native (`src/shared/transport.rs:477`) and the wasm32 (`:543`) definitions, so an external `Transport` impl compiles unchanged |
| 4 | The official suite is re-measured after the change and the delta reported against Phase 118.1's closing numbers | ✓ VERIFIED (measurement RE-TAKEN at HEAD) | `./scripts/run-conformance-suite.sh` RUN BY THIS VERIFIER at HEAD (Node v22.22.2, ephemeral `PMCP_REQUEST_STATE_KEY`, pin `0.2.0-alpha.11`): **exit 0**, `CONF-01 gates PASSED`. `2025-11-25`: **73 passed / 1 failed**, 33 scenarios, 74 checks (floor 74), 30 scored (floor 30), 0 failing, leg exit 0. `2026-07-28`: **142 passed / 36 failed**, 50 scenarios, 178 checks (floor 178), 37 scored (floor 37), 0 failing, leg exit 0. MRTR 14 scenarios / 36 checks / 0 failures. **Delta against the recorded closing table: ZERO — every figure byte-identical.** Against 118.1's numbers the v1 leg went 72/2/exit 1 → 73/1/exit 0 |
| 5 | The v1 client SSE transport this phase shipped is safe to rely on end-to-end — a peer cannot drive it into unbounded resource consumption, a terminal transport error cannot be silently paired with or displace an unrelated request's response, and a single bad frame cannot wedge the client | ✅ VERIFIED (override applied — CR-03 carried as accepted) | Every sub-item the prior run failed is now closed and fenced (5a–5f below, each fence RUN). ONE open item remains inside the truth's own wording — CR-03 — unchanged in code, explicitly deferred by developer decision, disclosed in three places, with no `overrides:` entry in this file to carry it. See "Suggested override" |

**Score:** 4/5 truths verified (0 present-but-behavior-unverified). Truth 5 is neither FAILED nor
VERIFIED: no new defect blocks it, and no override exists to score it.

#### Truth 5, decomposed against HEAD `dc0587af`

| Sub-item | Status | Evidence (re-derived at HEAD) |
|---|---|---|
| 5a. CR-01 — ceiling did not cover lock acquisition | ✓ CLOSED (mechanism deleted, third round) | `receive_bounded` / `MISMATCH_DISCARD_TIMEOUT` / `MAX_ID_MISMATCH_DISCARDS` / `MISMATCH_RECEIVE_SLICE`: **0 hits** in `src/` and `tests/`. `pump_once` (`src/client/mod.rs:4204-4222`) takes the guard, races receive vs answer vs `sleep(PUMP_RECEIVE_SLICE)` with receive keeping first-poll priority, and drops the guard at the block end |
| 5b. CR-02 half A — a live caller's answer destroyed by another caller's pop | ✓ CLOSED (per-id router) | `pump_once` delivers each `Response` to whoever registered its id (`:4230-4249`). Fence `two_concurrent_calls_each_receive_their_own_answer` — RUN, passes |
| 5c. CR-02 half B — the ceiling self-sustains across calls | ✓ CLOSED by 118.2-22 | Single exit-cleanup at `:4158-4166` records abandonment only on a live registration; `pump_once` asks `absorb_abandoned` before booking `Unmatched` (`:4241`). `AbandonedRequestIds` bounded by construction (`:424-460`). Fence `debris_from_a_dead_call_does_not_charge_the_next_calls_budget` (`:3736`) — RUN, passes. Prior run's "disclosed nowhere" claim: FALSE at HEAD (45 `.planning/` hits) |
| 5d. CR-03 — transport-wide gate answers a per-caller question | ⚠️ OPEN, ACCEPTED, DISCLOSED — the one item blocking the score | Unchanged in code. Deferred by developer decision (quoted in `118.2-UAT.md` test 6), `WINDOWS.md` entry 23, migration-chapter mirror, `windows_disclosure_tripwire` 1/1 — RUN, passes (and that green proves MIRRORING, not truth) |
| 5e. Send-path unbounded lock hold (the prior run's NEW regression) | ✓ CLOSED by 118.2-23 | Four `transport.write()/read()` sites remain in `src/client/mod.rs` and none spans a send. All four send sites route through `send_frame` (`:5176`) over an owned handle resolved at construction (`:625`). Fence 30 (`:4281`) — RUN, passes, non-vacuous |
| 5f. Closure-2 WR-01 — reset seam cleared a `PostResponse` reason | ✓ CLOSED | The seam in `start_sse` clears only when `reason.stream == StreamKind::Session`, stated at the branch (`src/shared/streamable_http.rs`, the SCOPED clause of the reset seam) |
| 5g. NEW — cold-cache token fan-out (`T-118.2-25-01`, critical, found post-plan by the security audit) | ✓ CLOSED by `ColdVendGate` | Defined `:1218-1268`; wired on the ordinary build path (`:2490`) AND the reconnect path (`:4191`); re-armed on eviction (`:2893`). Fence 31 (`:4436`) — RUN, passes, asserts peak concurrency <= 1, exactly 1 vend, 0 purges |
| 5h. `restart_lock` held across the untimed GET response head (`T-118.2-25-03`, medium) | ⚠️ OPEN, ACCEPTED (R-118.2-02) — **under-disclosed**, see WARNING | Lock taken `:2126`, held across `open_sse_once(...).await` at `:2141`. No production request timeout in the file: one hit, `.pool_idle_timeout(90s)` `:1646`; all `tokio::time::timeout` at `:5488`+ inside the `mod tests` that opens at `:4495`. Recorded ONLY in `118.2-SECURITY.md`; not a `WINDOWS.md` entry, not in the chapter |
| 5i. Non-caching `AuthProvider` residual (R-118.2-03) | ⚠️ OPEN, ACCEPTED, WELL DISCLOSED | Stated in the `ColdVendGate` rustdoc itself (`:1245-1255`), `WINDOWS.md` entry 26 residual (i), and the chapter's "What you must check on your side" |
| 5j. 118.2-23 residuals — pooled transport, `open_event_stream` | ⚠️ OPEN, ACCEPTED, WELL DISCLOSED | `Client::open_event_stream` holds a READ guard across the `subscriptions/listen` POST head (`src/client/mod.rs:5363`), disclosed in place at the guard (`:5348-5362`); `PooledTransport` inherits the `None` default by decision. `WINDOWS.md` entry 27 + chapter item (3) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/client/mod.rs` — `AbandonedRequestIds`, `absorb_abandoned` | Break CR-02's self-sustaining chain | ✓ VERIFIED — exists, substantive, wired, behaviour fenced | Type `:424-460`, field `:610`, initialised at `:721`/`:770`/`:815`, recorded `:4165`, consumed `:4241`/`:4324`, cloned into the child client `:5890`. Unit `an_abandoned_id_is_absorbed` `:6138` + integration fence `:3736` |
| `src/client/mod.rs` — `send_frame`, `ClientFrame`, `shared_sender` field | Take the send off the transport guard | ✓ VERIFIED — exists, substantive, wired, behaviour fenced | `:5176`; field resolved at construction `:625`; all four send sites route through it (`:4051`, `:4064`, `:4294`, `:5140`). Fallback `&mut` path preserved `:5187-5190` |
| `src/shared/transport.rs` — `SharedSender` + defaulted accessor | Additive public surface | ✓ VERIFIED | Native trait `:251`, wasm32 mirror `:278`, defaults returning `None` at `:477` and `:543`. Re-exported `src/shared/mod.rs:185`, `src/lib.rs:188` |
| `src/shared/streamable_http.rs` — `ColdVendGate` | Single-flight the ordinary cold-cache vend (`T-118.2-25-01`) | ✓ VERIFIED — exists, substantive, wired on BOTH paths, behaviour fenced | `:1218-1268`, field `:1526`, constructed `:1674`, cloned `:2284`, reached `:2419`/`:2490` (build) and `:4165`/`:4191` (reconnect), re-armed `:2893` |
| `src/shared/streamable_http.rs` — `refresh_lock`, `token_generation`, `restart_lock` | Serialise the two transport-wide sequences the send fix exposes | ✓ VERIFIED, with 5h open | `:1519`, `:1551`, `:1581`; `restart_lock` taken `:2126`. Fences 28/29 (`two_concurrent_401s_refresh_the_token_once`, `two_concurrent_session_stream_restarts_leave_one_reader`) — RUN, pass |
| `src/shared/streamable_http.rs` — `open_post_readers`/`PostReaderGuard` gate | BLOCKER 1 fix | ⚠️ WIRED, correct for 1 reader, defective for 2+ | CR-03, unchanged; accepted deferral, see 5d |
| `tests/client_sse_stream.rs` — fences 27–31 | Cover 5c, 5e, 5g and the two transport-wide sequences | ✓ VERIFIED — present, substantive, non-vacuous | 31/31 pass. Fence 30 carries an `Arm::Call` guard plus two wire-level vacuity guards; fence 31 asserts a PEAK, not just a total |
| `.planning/WINDOWS.md` entries 13, 20, 23, 24, 25, 26, 27 | Accurate record of what shipped | ✓ VERIFIED for 13/20/23/24/25/26/27 as written; ⚠️ INCOMPLETE for `T-118.2-25-03` | Entries 13 and 20 carry dated in-place corrections; 24 is the retrospective entry for the third round; 26 discloses residuals (i) and (ii); 27 discloses both 118.2-23 residuals. `T-118.2-25-03` appears once, in passing, never as an open defect |
| `.planning/REQUIREMENTS.md` CONF-09 row | Traceability that resolves at HEAD | ✓ VERIFIED | Fourth amendment (2026-08-19) records the unrecorded third round, the two defects it left behind with the plan and fence that closed each, the three transport-wide sequences, re-derived counts, the semver verdict and the suite re-measurement. Limitation (vi) corrected a second time: per-id routing SHIPPED, narrowed scope stated |
| `pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md` | Consumer-facing record | ✓ VERIFIED for entries 13/20/23/26/27; ⚠️ silent on `T-118.2-25-03` | Entry-26 paragraph (`:505-529`) and entry-27 paragraph (`:531-555`) describe the client that ships, including the non-caching-provider warning and both 118.2-23 residuals |
| `118.2-SECURITY.md` | Threat register with dispositions | ✓ VERIFIED as a register; two claims re-checked | 147 threats / 0 open at the blocking threshold. Spot-checked: `T-118.2-25-01` closure — confirmed at source; `R-118.2-01`'s no-new-dependency conclusion — TRUE for 118.2 (only `be14c52e` `[[bin]]` and `241b70d6` `[[example]]`), but its STATED command does not support it (see INFO finding) |
| `118.2-UAT.md` | 33 tests, 32 pass, 1 deferred gap | ✓ VERIFIED as a record | `status: complete`; test 6 `result: issue` carries the verbatim developer deferral; the Gaps block names root cause, artifacts, missing work, owner and window 23 |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `dispatch_request` | its own answer | `Pending.response` oneshot registered by id before send | ✓ WIRED, data flows | Fence `two_concurrent_calls_each_receive_their_own_answer` — RUN, passes |
| `dispatch_request` `Err` exit | the next call's budget | `AbandonedRequestIds::record` → `absorb_abandoned` | ✓ WIRED, chain broken | `:4158-4166` → `:4241`; fence `:3736` — RUN, passes |
| `Client` (all four send sites) | transport (outbound) | `send_frame` over an owned `SharedSender` | ✓ WIRED, guard NOT held across the round trip | `:5176`; fence 30 — RUN, passes |
| request build (cold cache) | `AuthProvider::get_access_token` | `RequestParts.cold_vend_gate.vend(..)` | ✓ WIRED on the ordinary path | `:2490`; fence 31 — RUN, passes |
| reconnect | `AuthProvider::get_access_token` | `SseReaderContext.cold_vend_gate` | ✓ WIRED on the reconnect path | `:4165`/`:4191` — the claim in entry 26 that "the reconnect path goes through the same gate" checks out |
| `start_sse` | `open_sse_once` GET head | `restart_lock` held across the await | ⚠️ WIRED, UNBOUNDED (accepted R-118.2-02) | `:2126` → `:2141`; no production request timeout in the file. This is 5h |
| stream reader terminal reason | the caller that owns it | transport-wide latch + `open_post_readers` gate | ⚠️ WIRED, correct for 1 reader | CR-03, accepted deferral, 5d |
| `SharedSender` handle | the SAME send core as `Transport::send` | delegation to `send_with_options_shared` / `post_body` | ✓ WIRED, no second wire path | `src/shared/streamable_http.rs:3259-3271` — both operations delegate to the functions `Transport::send`/`send_raw` delegate to |

### Behavioral Spot-Checks

Every command below was executed by this verifier at HEAD `dc0587af`.

| Behavior | Command | Result | Status |
|---|---|---|---|
| The v1 client SSE fence set is green | `cargo test --features full --test client_sse_stream` | **31 passed; 0 failed** in 8.08s | ✓ PASS |
| CONF-10 emitter backstop is green | `cargo test --features full --test log_emitter --test pmcp_both_ends_logging` | **25 + 2 passed, 2 ignored, 0 failed** | ✓ PASS |
| Disclosure tripwire is green | `cargo test --features full --test windows_disclosure_tripwire` | **1 passed** | ✓ PASS (mirroring only, not truth — stated by the record itself) |
| The re-added ceiling is disclosed in the planning record | `grep -rn "UNMATCHED_RESPONSE_TIMEOUT" .planning/ \| wc -l` | **45** | ✓ PASS — **the prior run's claim of 0 is FALSE at HEAD** |
| The five deleted symbols do not survive in code | `/usr/bin/grep -rn "MISMATCH_DISCARD_TIMEOUT\|MAX_ID_MISMATCH_DISCARDS\|MISMATCH_RECEIVE_SLICE\|receive_bounded" src/ tests/` | 3 hits, all one unrelated local helper in `tests/v2_subscriptions.rs` | ✓ PASS |
| Stale fence-calibration comments removed | `/usr/bin/grep -c "MISMATCH_DISCARD_TIMEOUT" tests/client_sse_stream.rs` | **0** | ✓ PASS |
| No client-side lock spans a send | `/usr/bin/grep -n "transport.write()\|transport.read()" src/client/mod.rs` | 4 sites: `:2430` (sync `transport_type`), `:4208` (`pump_once`, sliced), `:5187` (`send_frame` fallback for `None`-answering transports), `:5363` (`open_event_stream`, disclosed residual) | ✓ PASS |
| No production request timeout exists (confirms 5h) | `/usr/bin/grep -n "timeout" src/shared/streamable_http.rs` + `mod tests` boundary | 1 production hit (`.pool_idle_timeout` `:1646`); all `tokio::time::timeout` at `:5488`+, inside the `mod tests` opening at `:4495` | ✗ CONFIRMS the open item (5h) |
| Public API additive | `cargo semver-checks --baseline-rev cb5d1365 -p pmcp` | `196 checks: 196 pass, 57 skip`, `no semver update required`, exit 0 | ✓ PASS |
| No dependency added by 118.2 | `git log --all --name-only -- Cargo.toml fuzz/Cargo.toml` filtered to 118.2 + `git show be14c52e` | Two commits only: a `[[bin]]` fuzz registration ("adds NO dependency") and an `[[example]]` entry | ✓ PASS (but see INFO finding on the stated command) |
| Repo quality gate | `make quality-gate` | exit **0**, banner `ALL TOYOTA WAY QUALITY CHECKS PASSED` | ✓ PASS |
| Severance proofs run non-vacuously | `bash scripts/run-severance-proofs.sh` | exit **0**; final leg 454 passed / 0 failed; "All severance proofs RAN, with non-zero test counts" | ✓ PASS |
| Debt markers in phase files | `grep -nE "TBD\|FIXME\|XXX\|HACK\|PLACEHOLDER"` over the six files this phase touched | **0 hits** | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| Official MCP conformance suite (CONF-01) | `PATH=<node22> PMCP_REQUEST_STATE_KEY=<ephemeral> bash scripts/run-conformance-suite.sh` | exit **0**; `CONF-01 gates PASSED`; `2025-11-25` 73/1, 74 checks, leg exit 0; `2026-07-28` 142/36, 178 checks, leg exit 0; MRTR 14/36/0; zero-check gate exact match on both lists; 30 named blocking scenarios all green | ✓ PASS |
| Severance proofs | `bash scripts/run-severance-proofs.sh` | exit **0** | ✓ PASS |

Note on reproducing the suite: it refuses on Node < 22 and on a missing `PMCP_REQUEST_STATE_KEY`, both
with actionable messages. Both refusals fired for me before the run succeeded — the guards work.

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|---|---|---|---|
| CONF-09 | v1 client SSE transport holds a live GET stream, reads incrementally, reconnects under a bounded budget | ✓ SATISFIED, and its evidence row now RESOLVES at HEAD | The fourth amendment re-points every identifier; the five deleted symbols appear only on lines marked SUPERSEDED BY; limitation (vi) corrected a second time to the narrowed scope the code actually has |
| CONF-10 | Handler-facing log emitter, both eras, per-session/per-request filtering | ✓ SATISFIED | 27 passed / 2 ignored at HEAD; `tools-call-with-logging` green in the suite run above; `GAP_ATTRIBUTABLE_FAILURES` 0 asserted by the gate |

No orphaned requirements. CONF-09 and CONF-10 remain the only IDs mapped to this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/shared/streamable_http.rs` | 2126 → 2141 | A transport-wide lock held across an untimed peer round trip | ⚠️ Warning (accepted as R-118.2-02) | A peer that accepts the GET and never writes its head blocks every session-stream restart on that transport. Real, confirmed at source |
| `.planning/WINDOWS.md`, `pmcp-book/src/ch12-17-…md` | — | An open, accepted, consumer-observable residual disclosed only in a phase-internal artifact | ⚠️ Warning | Breaks the phase's own convention, which every sibling residual follows (entries 23, 26×2, 27×2). Nothing FALSE is asserted, which is why this is a Warning and not the Blocker its predecessor was |
| `118.2-SECURITY.md` | R-118.2-01 | Stated verification command does not support its own conclusion | ℹ️ Info | `git diff cb5d1365..HEAD` over manifests DOES add deps (from non-118.2 work). The conclusion is nonetheless true for 118.2 — I verified it per-commit |
| `src/shared/streamable_http.rs` | 1142, 1065 | Transport-wide count answering a per-caller question; a unit test pins the defective single-reader shape | ⚠️ Warning (accepted deferral) | CR-03. Disclosed properly; listed for completeness of the truth, not as an undisclosed defect |

No `TBD`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` debt markers in any file this phase touched.

### Human Verification Required

#### 1. Decide CR-03 — accept as a scored override, or re-open

**Test:** The developer deferral already exists and is quoted verbatim in `118.2-UAT.md` test 6
(2026-08-18), recorded as `WINDOWS.md` entry 23 with a named owner, and mirrored into the migration
chapter. What does not exist is an `overrides:` entry in this file. Add one, or instruct that CR-03 be
planned for closure.
**Expected:** Either an `overrides:` block with a real `accepted_by` and `accepted_at`, after which truth
5 scores and this phase reads 5/5 — or a decision to plan the fix.
**Why human:** A scope/acceptance judgement, not a code fact. Every code fact around it is settled and
cited above. A verifier minting its own override would be exactly the failure mode the adversarial
stance forbids.

#### 2. Decide where an accepted risk must be disclosed (`R-118.2-02` / `T-118.2-25-03`)

**Test:** Confirm whether this residual needs a `WINDOWS.md` entry (or an entry-26 amendment) plus one
migration-chapter sentence — as CR-03 and all four sibling residuals in this phase got — or whether
`118.2-SECURITY.md`'s Accepted Risks Log suffices on its own.
**Expected:** Either the ledger + chapter disclosure ("`restart_lock` is held across an untimed GET
response-head await, so a peer that accepts the GET and never answers blocks every session-stream
restart on that transport"), or a written convention decision that SECURITY.md alone is the home for
accepted risks going forward.
**Why human:** The code fact is settled (`:2126`, `:2141`, `:1646`, `:4495`). The unsettled part is a
records convention this phase otherwise follows consistently.

### Suggested override

If CR-03 is to be carried as accepted rather than blocking the score, add this to the frontmatter and
re-run verification. The rationale below is taken from the developer's own words in `118.2-UAT.md`, not
composed here:

```yaml
overrides:
  - must_have: "The v1 client SSE transport this phase shipped is safe to rely on end-to-end — a peer cannot drive it into unbounded resource consumption, a terminal transport error cannot be silently paired with or displace an unrelated request's response, and a single bad frame cannot wedge the client"
    reason: "The only residual inside this truth's wording is CR-03, deferred by explicit developer decision on 2026-08-18: the defect lives exclusively on the v1 POST-response SSE reader path, i.e. on the surface being deprecated in favour of stateless calls plus MCP Tasks with task-store polling for progress. Severity re-measured against the post-router tree to a DELAYED error rather than a lost or mis-delivered answer. Disclosed as WINDOWS.md entry 23 with a named owner, mirrored into pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md, and booked in 118.2-UAT.md's Gaps block. CR-01, CR-02 (both halves) and the send-path wedge are closed at HEAD dc0587af and each is fenced by a named test verified passing."
    accepted_by: "{name}"
    accepted_at: "{ISO timestamp}"
```

### Gaps Summary

There are no code gaps blocking this phase at HEAD `dc0587af`, and I looked hard for them rather than
taking the green artifacts at face value: I ran every fence, re-took both measurements whose logs no
longer exist, traced `ColdVendGate` to both of its reach points instead of stopping at its definition,
and re-derived the prior run's headline evidence — which turned out to be false.

The fourth round did what the third did not: it closed the two defects the third left behind, gave that
unplanned round a retrospective record, corrected two ledger entries in place rather than annotating
beneath them, and then — after the plans were done — absorbed a critical the security audit found in
code the plans had just shipped, with a fence demonstrated RED before green. That is the correct shape
for a closure round, and it is a marked improvement on the two rounds before it.

What is left is one open item inside truth 5 and one records question:

- **CR-03** is open, unfixed, and explicitly deferred. Its own gap record says "DEFERRED, not
  scheduled" — there is nothing for `/gsd-plan-phase --gaps` to plan. Returning `gaps_found` a fourth
  time would produce an unactionable gap and would misstate a phase whose two previously-failing
  sub-items are now closed and fenced. The honest classification is that truth 5 needs a recorded
  human acceptance to be scored, which is what `human_needed` means.
- **`R-118.2-02` is under-disclosed.** The defect is real and confirmed at source, and it was properly
  found, dispositioned and accepted — but it stopped at the phase's own SECURITY.md while every sibling
  residual in this phase reached the cross-phase ledger and the consumer-facing chapter. It is a
  Warning, not a Blocker: nothing false is asserted, and the governing "no default request timeout"
  policy *is* disclosed to consumers. It is worth fixing before this record is archived, because the
  entire lesson of rounds two and three of this phase is that the record drifts faster than the code.

One thing to carry forward regardless of the decision above: **every `target/118.2-*.log` cited as
evidence across `REQUIREMENTS.md` and the plan summaries is absent at HEAD.** The counts in those rows
are unverifiable by anyone who did not run them on the day. Both of the ones that mattered here — the
semver verdict and the official-suite measurement — I re-took, and both reproduced. That they
reproduced is good; that reproducing them was the only way to check them is a durable weakness in how
this phase records evidence.

---

_Verified: 2026-08-20T23:24:57Z, against HEAD `dc0587af`_
_Verifier: Claude (gsd-verifier) — fourth-round re-verification_
