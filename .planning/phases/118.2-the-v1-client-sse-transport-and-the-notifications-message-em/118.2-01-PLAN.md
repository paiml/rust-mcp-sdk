---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/client_sse_stream.rs
  - src/shared/streamable_http.rs
autonomous: true
requirements: [CONF-09]

must_haves:
  truths:
    - "pmcp's own client issues an HTTP GET with Accept: text/event-stream after the initialized notification is answered 202"
    - "The GET is issued for notifications/initialized ONLY, not for every outbound notification"
    - "A recording listener exists that can observe request lines and hold a chunked text/event-stream open indefinitely"
  artifacts:
    - path: "tests/client_sse_stream.rs"
      provides: "the CONF-09 client fence file plus its recording TCP listener harness"
      min_lines: 220
      contains: "tokio::time::timeout"
    - path: "src/shared/streamable_http.rs"
      provides: "a reachable 202-Accepted branch that opens the session SSE stream"
      contains: "StatusCode::ACCEPTED"
  key_links:
    - from: "StreamableHttpTransport::post_body 202 handling"
      to: "StreamableHttpTransport::start_sse"
      via: "an initialized-notification predicate evaluated OUTSIDE the !is_success() guard"
      pattern: "start_sse"
    - from: "tests/client_sse_stream.rs RecordingServer"
      to: "observed request lines"
      via: "raw TCP accept loop recording the request line of every connection"
      pattern: "GET / HTTP/1.1"
---

<objective>
Close **Defect A**: pmcp's `StreamableHttpTransport` never issues a GET at all, so there is no live
session stream for any later fix to read.

Purpose: RESEARCH MEASURED this with a live probe — a recording TCP listener plus a real
`pmcp::ClientBuilder` driven through `initialize` observed request lines
`["POST / HTTP/1.1", "POST / HTTP/1.1"]` and a **GET count of 0**. The `start_sse(None)` call that
should open the session stream sits inside `if !response.status().is_success()`, and `202 Accepted`
**is** a success status (`http-1.4.0/src/status.rs:182`: `(200..300).contains(&self.0.get())`), so
that branch is dead code. Every downstream client fence in plans 02, 03 and 04 is vacuous until this
lands — a perfect incremental reader over a stream that is never opened still reports
`no-live-stream`.

Output: `tests/client_sse_stream.rs` with a reusable recording listener harness (plans 02/03/04
extend this same file), plus the one-line-class fix that makes the 202 branch reachable and scopes
it to the *initialized* notification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-CONTEXT.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md
</context>

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required to start. -->

The defect, `src/shared/streamable_http.rs` around `:1463-1479` (grep-confirmed: `:985` and `:1466`
are the two `if !response.status().is_success()` sites; the 202 one is inside `post_body`):

```
if !response.status().is_success() {          // 202 NEVER enters this block
    if response.status() == StatusCode::ACCEPTED {
        if is_notification {
            let _ = self.start_sse(None).await;   // DEAD CODE
        }
        return Ok(());
    }
    ...
```

The reference behaviour, vendored at
`conformance/node_modules/@modelcontextprotocol/sdk/dist/esm/client/streamableHttp.js:370-377`:

```
if (response.status === 202) {
    if (isInitializedNotification(message)) {
        this._startOrAuthSse({ resumptionToken: undefined }).catch(err => this.onerror?.(err));
    }
    return;
}
```

The guard is `isInitializedNotification`, **not** "is a notification". The MCP method string is
`notifications/initialized`.

`StreamableHttpTransport::start_sse` is `pub async fn start_sse(...)` at `src/shared/streamable_http.rs:928`.
Its first act is to take and `abort()` `self.abort_handle` (`:930-936`) — that matters for plan 04's
reconnect loop, not here, but do not call it from inside a spawned reader task.

The harness analog is `tests/http_peer_roundtrip.rs`. Its `#![cfg]` header (`:47-52`):

```
#![cfg(all(feature = "streamable-http", feature = "v1-compat", not(target_arch = "wasm32")))]
```

and its `struct Conn` / `Conn::open` / `Conn::pull` raw-HTTP helper at `:372-470` shows the status-line
parse, the lowercased header vec and the `transfer-encoding: chunked` incremental read. This plan needs
the **inverse** direction: pmcp is the CLIENT, so the harness is a recording *server*.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Recording TCP listener harness plus the RED fence proving no GET is issued</name>
  <files>tests/client_sse_stream.rs</files>
  <read_first>
    - tests/http_peer_roundtrip.rs — read the module doc (`:40-52`) for the reliability doctrine and the `#![cfg]` header verbatim; read `:74-129` for the bounds-as-constants-with-reasons convention; read `:302-470` for `build_server` / `spawn_server` / `struct Conn` shapes. This file is the harness analog; do NOT weaken or edit it.
    - src/shared/streamable_http.rs — read `StreamableHttpTransportConfig` (around `:500-540`) and `StreamableHttpTransport::new` so the fence can construct a transport pointed at an ephemeral port; read `start_sse` (`:928`) and `post_body`'s 202 region (`:1460-1490`).
    - .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md — read ONLY the section `### tests/client_sse_stream.rs — NEW (test, streaming) — CONF-09 fences` and `### 7. Every wire await is tokio::time::timeout-wrapped`. Locate with `grep -n 'client_sse_stream.rs' .planning/phases/118.2-*/118.2-PATTERNS.md` and read that range.
  </read_first>
  <action>
Create `tests/client_sse_stream.rs`. It is the CONF-09 fence file and plans 02, 03 and 04 all extend
it, so the harness half must be built to be reused rather than inlined into one test.

File header, copied in shape from `tests/http_peer_roundtrip.rs:40-52`:
- `#![cfg(all(feature = "streamable-http", feature = "v1-compat", not(target_arch = "wasm32")))]`
- A module doc that states the reliability doctrine explicitly and names WHY: EPHEMERAL PORT
  (`127.0.0.1:0`, address read back from the bind), READINESS (bind before returning, never a fixed
  sleep as a synchronisation device), SHUTDOWN (sockets dropped, then `abort()`, then `await`), and
  EVERY await that crosses the wire wrapped in `tokio::time::timeout`. Record the measured reason in
  the doc: RESEARCH measured that `start_sse(None)` against a never-ending `text/event-stream` **does
  not return within 3 seconds** and a subsequent `receive()` also does not — so against the unfixed
  tree an unbounded await HANGS rather than fails, and a hung test reads as a slow test in CI rather
  than a red one, which would silently defeat the RED-first requirement.

Bounds as named constants, each carrying a one-line reason (never bare numbers):
- `BOUND: Duration = Duration::from_secs(5)` — upper bound on any single operation that crosses the wire.
- `QUIET: Duration = Duration::from_millis(600)` — how long a listener is watched to prove NOTHING
  arrives on it.

Build `struct RecordingServer` (or an equivalently named helper) with this surface:
- `async fn start() -> RecordingServer` — binds `TcpListener` on `(Ipv4Addr::LOCALHOST, 0)`, reads the
  address back, spawns the accept loop, returns before any client connects.
- `fn url(&self) -> String` — `http://127.0.0.1:<port>/`.
- `fn request_lines(&self) -> Vec<String>` — every observed HTTP request line, e.g.
  `"POST / HTTP/1.1"`, `"GET / HTTP/1.1"`, in arrival order. Back it with an
  `Arc<parking_lot::Mutex<Vec<String>>>` or `Arc<std::sync::Mutex<..>>`; the crate already depends on
  `parking_lot`.
- `async fn shutdown(self)` — drop the sockets, `abort()` the accept task, then `await` it.

The accept loop must answer, per connection, by method plus body:
- `POST` whose body JSON has `"method": "initialize"` → `HTTP/1.1 200 OK`, `content-type: application/json`,
  header `Mcp-Session-Id: s1`, and a JSON-RPC result body carrying `protocolVersion` `"2025-11-25"`,
  an empty `capabilities` object and a `serverInfo` object. Match the shape the pmcp client's
  `initialize` will accept — read `src/shared/streamable_http.rs` and, if needed,
  `src/client/mod.rs::initialize` to confirm the minimum fields.
- `POST` whose body JSON has `"method": "notifications/initialized"` → `HTTP/1.1 202 Accepted` with
  `content-length: 0`.
- Any other `POST` → `HTTP/1.1 202 Accepted` with `content-length: 0`.
- `GET` → `HTTP/1.1 200 OK`, `content-type: text/event-stream`, `transfer-encoding: chunked`, then
  HOLD the socket open without sending a terminating chunk. Expose a way for a test to push frames
  later (an `mpsc` channel of `String` per GET connection is sufficient; plans 02/03/04 need it, this
  plan only needs the hold).

Add the RED fence `client_issues_a_get_sse_stream_after_the_initialized_notification`:
build a `StreamableHttpTransport` against `server.url()`, wrap it in a `pmcp::ClientBuilder`, drive
`client.initialize(ClientCapabilities::default())` inside `tokio::time::timeout(BOUND, ..)`, then poll
`server.request_lines()` until a line starting with `GET ` appears or `BOUND` elapses. Assert the GET
is present, and on failure print the full observed `request_lines()` vector in the assertion message
so the RED evidence is the measurement itself, not a bare `assert!`.

Run it against the unfixed tree and CAPTURE THE RED. Write the run to `target/118.2-01-red.log`. This
is 118.1 D-04 carried forward by D-15: every fence is demonstrated RED before its fix lands. The
expected RED evidence is exactly two POST lines and zero GET lines.

Do not add a `[[test]]` block to `Cargo.toml` — this repo has zero `[[test]]` entries and integration
tests are auto-discovered (verified: `grep -c '^\[\[test\]\]' Cargo.toml` returns 0).
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(client_sse_stream)' --features "full" 2>&1 | tee target/118.2-01-red.log | grep -E 'Summary.*[1-9][0-9]* tests? run.*[1-9][0-9]* failed' &amp;&amp; grep -E 'GET|POST / HTTP' target/118.2-01-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `tests/client_sse_stream.rs` exists and its first non-comment line is `#![cfg(all(feature = "streamable-http", feature = "v1-compat", not(target_arch = "wasm32")))]`
    - `grep -c 'tokio::time::timeout' tests/client_sse_stream.rs` returns ≥ 2 and `grep -v '^\s*//' tests/client_sse_stream.rs | grep -c 'BOUND'` returns ≥ 2
    - `cargo nextest run -E 'binary(client_sse_stream)' --features "full"` reports a NON-ZERO test count (a `Summary` line matching `[1-9][0-9]* tests run`) — a file that gates itself out compiles and exits 0 having run nothing
    - The run is RED: the summary line reports at least 1 failed test, and the failure message contains the observed request-line vector
    - `target/118.2-01-red.log` exists and contains two `POST / HTTP/1.1` observations and zero `GET / HTTP/1.1` observations
    - `git diff --stat -- tests/http_peer_roundtrip.rs` is empty — the 12/12 server-half proof is not touched
  </acceptance_criteria>
  <done>The recording harness exists, the GET fence runs, and it is RED with the measured `2 POSTs / 0 GETs` evidence captured on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Make the 202 branch reachable and scope it to notifications/initialized</name>
  <files>src/shared/streamable_http.rs, tests/client_sse_stream.rs</files>
  <read_first>
    - src/shared/streamable_http.rs — read `post_body` in full around `:1400-1560`, including the `is_notification` computation, the `if !response.status().is_success()` guard at `:1466`, the 202 sub-branch, and the POST-response `text/event-stream` handling that follows at `:1543`. Do NOT change the POST-response read here — plan 03 owns it.
    - tests/client_sse_stream.rs — the file task 1 created, so the new test reuses the harness rather than adding a second listener.
    - conformance/node_modules/@modelcontextprotocol/sdk/dist/esm/client/streamableHttp.js — read `:360-390` for the reference 202 handler and confirm the `isInitializedNotification` predicate.
  </read_first>
  <action>
In `src/shared/streamable_http.rs::post_body`, restructure the response handling so `202 Accepted` is
handled BEFORE the `if !response.status().is_success()` guard, not inside it.

Concretely, the target state:
- A `202 Accepted` response short-circuits with `Ok(())` and, when the message that produced it was
  the `notifications/initialized` notification specifically, first calls `self.start_sse(None).await`
  and tolerates its failure (a server that answers `405 Method Not Allowed` to the GET is a valid
  StreamableHTTP server that offers no session stream; that tolerance already exists in the current
  dead branch and must be preserved).
- The predicate is `notifications/initialized` by method string, NOT the existing broad
  `is_notification`. Copying the broad predicate would tear down and re-open the session stream on
  every `notifications/cancelled` and `notifications/progress` the client sends — `start_sse`'s first
  act is to `abort()` the existing `abort_handle`, so the broad predicate is an active regression, not
  merely wasteful. Name that reason in a comment at the predicate.
- The `if !response.status().is_success()` block keeps its remaining job (non-2xx error mapping) and
  loses only the now-unreachable 202 sub-branch. Leave a comment recording WHY the branch moved: 202
  satisfies `is_success()` — `(200..300)` — so the old placement was dead code from the day it was
  written, MEASURED in Phase 118.2 research as 2 POSTs / 0 GETs.
- Derive the method string from the outbound message the transport already has in hand. Do not
  re-serialise or re-parse the body to obtain it if the method is already available on the
  `TransportMessage`; read the surrounding code and use whatever `is_notification` is computed from.

Then add a second fence to `tests/client_sse_stream.rs`:
`a_non_initialized_notification_does_not_reopen_the_stream` — after handshake, send a non-initialized
notification through the transport (e.g. `notifications/cancelled`), wait `QUIET`, and assert the
recorded request lines contain EXACTLY ONE `GET ` line. This is the anti-pattern fence for the broad
predicate and it is the reason the narrow predicate is not merely stylistic.

Both fences must now be GREEN. Record the GREEN run to `target/118.2-01-green.log`.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(client_sse_stream)' --features "full" 2>&1 | tee target/118.2-01-green.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped' &amp;&amp; ! grep -qE '[1-9][0-9]* failed' target/118.2-01-green.log</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run -E 'binary(client_sse_stream)' --features "full"` reports a NON-ZERO test count with 0 failed
    - `grep -n 'notifications/initialized' src/shared/streamable_http.rs` returns at least one hit inside `post_body`'s 202 handling
    - The `start_sse` call in `post_body` is NOT lexically inside the `if !response.status().is_success()` block — verify by reading the function; the 202 arm returns before that guard is evaluated
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` exits 0
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0 (v1 severance still builds)
    - `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` reports a NON-ZERO test count, 0 failed
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>The client issues exactly one GET after the initialized 202, issues none for other notifications, and both fences are green with the RED-then-GREEN pair recorded on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → pmcp client (HTTP response status + headers) | A peer-chosen status code selects which client branch runs |
| server → pmcp client (GET SSE body) | Opened by this plan; its bytes are parsed by plans 02/03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.2-01-01 | Denial of Service | `post_body` 202 branch | mitigate | Scope the stream-open to `notifications/initialized` only. A broad `is_notification` predicate lets a client's own notification traffic drive an abort-and-reopen storm against the server, because `start_sse` aborts the live reader as its first act. Fenced by `a_non_initialized_notification_does_not_reopen_the_stream` |
| T-118.2-01-02 | Denial of Service | GET SSE stream open | accept | This plan only OPENS the stream; the bytes it carries are unbounded until plan 02's parser bound lands. Accepted for the duration of one wave because the reader still uses the existing whole-body cap (`max_collected_body_bytes`, 16 MiB) — the stream hangs rather than growing without limit. Plan 02 replaces it with the incremental bound |
| T-118.2-01-03 | Information Disclosure | fence assertion messages | mitigate | The fence prints observed REQUEST LINES (method + path + version), never request or response bodies, so a future harness change cannot leak a token from an `Authorization` header into CI logs |
| T-118.2-01-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. RESEARCH's Package Legitimacy Audit records the whole phase as package-free; the only external artefact is the conformance suite, held at `0.2.0-alpha.11` by D-14 until plan 12 |
</threat_model>

<verification>
- `cargo nextest run -E 'binary(client_sse_stream)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(http_peer_roundtrip)' --features "full"` — expect 12 run, 12 passed (the server half must not regress)
- `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` — non-zero count, 0 failed
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`
- `make quality-gate`
</verification>

<success_criteria>
- `target/118.2-01-red.log` records the pre-fix measurement: 2 POSTs, 0 GETs
- `target/118.2-01-green.log` records the post-fix measurement: exactly one GET, issued after the initialized 202
- The recording harness in `tests/client_sse_stream.rs` exposes `request_lines()`, holds a chunked `text/event-stream` open, and can push frames on demand — plans 02/03/04 extend it without rewriting it
- `tests/http_peer_roundtrip.rs` is byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-01-SUMMARY.md` when done.
Record in it: the RED request-line vector, the GREEN request-line vector, and the exact predicate used for the stream-open guard.
</output>
