---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 04
type: execute
wave: 4
depends_on: [118.2-03]
files_modified:
  - src/shared/streamable_http.rs
  - tests/client_sse_stream.rs
  - fuzz/fuzz_targets/streamable_sse_frames.rs
  - fuzz/Cargo.toml
autonomous: true
requirements: [CONF-09]

must_haves:
  truths:
    - "A session stream dropped mid-flight is re-opened with a GET carrying the last event id, on a v1-compat build"
    - "Reconnect gives up after a bounded retry budget with a named error rather than retrying forever or going quiet"
    - "A full-v2 build names no resumption cursor at the reconnect call site and adds no second call-site cfg"
    - "The incremental reader never panics and its retained bytes stay under the parser bound for arbitrary input"
    - "The client changes carry a recorded cargo semver-checks verdict"
  artifacts:
    - path: "src/shared/streamable_http.rs"
      provides: "a bounded reconnect loop inside the single spawned reader task, driven by a paired cursor accessor"
      contains: "reconnect_cursor"
    - path: "fuzz/fuzz_targets/streamable_sse_frames.rs"
      provides: "the fuzz seam for the incremental reader, asserting bounded retention rather than a latch that cannot clear"
      min_lines: 60
  key_links:
    - from: "reader task end-of-body or IO error"
      to: "a new GET carrying the last event id"
      via: "a retry loop INSIDE the one spawned task, never a recursive start_sse call"
      pattern: "reconnect_cursor"
    - from: "fuzz/fuzz_targets/streamable_sse_frames.rs"
      to: "fuzz/Cargo.toml"
      via: "a [[bin]] registration block"
      pattern: "streamable_sse_frames"
---

<objective>
Finish the client half: bounded auto-reconnect with `Last-Event-ID` (D-03), the ALWAYS-requirement
fuzz seam for the new reader, the `full-v2` severance proof, and the `cargo semver-checks` verdict
that ROADMAP success criterion 3 requires.

Purpose: real deployments idle-drop long-lived streams routinely (ALB ~60 s, Cloudflare, API
gateways). Without reconnect, this phase's fix silently regresses to `NO_LIVE_STREAM` the first time a
proxy blinks — the exact symptom it exists to remove.

Two hard constraints the implementation must resolve rather than discover:
- **`start_sse` aborts `self.abort_handle` as its very first act** (`:930-936`), so a recursive
  `self.start_sse(..)` call from inside the reader task would abort the task making the call. The
  retry loop lives INSIDE the one spawned task.
- `apply_resumption_header` is `#[cfg(feature = "v1-compat")]` and is **the only `#[cfg]` at a call
  site in this file** — the file's own comment says so and asks that a second one not accumulate. The
  reconnect cursor therefore goes through a PAIRED ACCESSOR with a constant `full-v2` answer, not a
  second call-site `#[cfg]`.

Output: the reconnect loop, the fuzz target, the severance build, and the recorded semver verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-CONTEXT.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-03-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. -->

The in-file paired-accessor precedent, `src/shared/streamable_http.rs:709-729`: a
`#[cfg(feature = "v1-compat")] fn resumption_callback(&self) -> Option<Arc<dyn Fn(String) + Send + Sync>>`
reading the config, paired with a `#[cfg(not(feature = "v1-compat"))] #[allow(clippy::unused_self)] const fn`
of the identical signature returning the constant `None`. Its rustdoc says: do NOT "improve" the twin by
reading the config, because the field does not exist on that build, and answering here is what keeps the
call sites free of a `#[cfg]`.

The ONE existing call-site `#[cfg]` and its rule, `src/shared/streamable_http.rs:962-971`, inside
`start_sse` (`:928`): a comment stating this is the ONLY `#[cfg]` at a call site in the file, that it is
unavoidable because the argument it reads is itself gated, and that a second one must not accumulate —
followed by `#[cfg(feature = "v1-compat")] Self::apply_resumption_header(&mut request, resumption_token.as_deref())?;`

`start_sse`'s parameter keeps the same POSITION and TYPE on both feature sets; only its name differs
(`resumption_token` on v1-compat, `_ignored_cursor` on `full-v2`).

`SseParser::last_event_id(&self) -> Option<&str>` is at `src/shared/sse_parser.rs:650`. The transport
also holds `last_event_id: Arc<RwLock<Option<String>>>` (`src/shared/streamable_http.rs`, initialised at
`:607`).

**Naming constraint, enforced mechanically.** `tests/v1_severability_tripwire.rs` scans
`src/shared/streamable_http.rs` for `CLIENT_V1_TOKENS = ["session_id", "resumption_token", "LAST_EVENT_ID"]`
(`:897`) and requires every occurrence outside `#[cfg(test)]` to sit inside a `v1-compat` gate. It also
enforces `MIN_CLIENT_V1_OCCURRENCES = 20` (`:907`) and `MIN_CLIENT_STRIPPED_BYTES = 20_000` (`:910`) as
anti-vacuity floors, plus `the_last_event_id_const_and_its_reader_are_co_gated` (`:1364`).
`reconnect_cursor` is SAFE. `reconnect_resumption_token` and `sse_session_id` are NOT — they red the
tripwire on a token match, not on a behaviour.

The reference reconnect defaults, vendored at
`conformance/node_modules/@modelcontextprotocol/sdk/dist/esm/client/streamableHttp.js:8-12`:
`initialReconnectionDelay: 1000`, `maxReconnectionDelay: 30000`, `reconnectionDelayGrowFactor: 1.5`,
`maxRetries: 2`. Its `_getNextReconnectionDelay` (`:121`) lets a server-provided `retry:` win, otherwise
`min(initial * grow^attempt, max)`; `_scheduleReconnection` (`:139`) gives up with an error after
`maxRetries` attempts and resumes with the last event id.

`SseEvent` carries `pub retry: Option<u64>` (`src/shared/sse_parser.rs:11`), so honouring a server-sent
`retry:` costs nothing.

The fuzz analog is `fuzz/fuzz_targets/subscription_listen_frames.rs`, registered in `fuzz/Cargo.toml` at
`:184-187` with a three-line `[[bin]]` block giving `name` and `path`. Its header documents three
invariants under arbitrary bytes: never panics (T-113-67), never cross-delivers (T-113-66), and memory
stays bounded (T-113-79 / T-113-93) — asserting `SseParser::buffered_bytes() <= max_buffer_size` after
every chunk. Its own doc records the anti-vacuity lesson: the target previously asserted only that the
overflow latch never clears, which cannot fail for any input at any bound, so 20 000 green runs coexisted
with exactly the unbounded-growth defect it was supposed to catch.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RED fences for reconnect, resumption and the retry budget</name>
  <files>tests/client_sse_stream.rs</files>
  <read_first>
    - tests/client_sse_stream.rs — the harness and the eight fences from plans 01-03. The harness must gain the ability to CLOSE a GET stream mid-flight and to record the request headers of each subsequent GET.
    - src/shared/streamable_http.rs — read `start_sse` in full (`:928-1060`) including the abort-handle take at `:930-936`, `apply_resumption_header` (`:745`), the `resumption_callback` pair (`:709-729`), and the reader plans 02/03 built.
    - tests/v1_severability_tripwire.rs — read `CLIENT_V1_TOKENS` (`:897`), `MIN_CLIENT_V1_OCCURRENCES` (`:907`), `MIN_CLIENT_STRIPPED_BYTES` (`:910`), `the_client_transport_carries_no_ungated_session_state` (`:1281`) and `the_last_event_id_const_and_its_reader_are_co_gated` (`:1364`) BEFORE naming anything, so the new identifiers are chosen against the scan rather than corrected after it.
  </read_first>
  <action>
Extend `RecordingServer` so it can (i) close a GET stream's chunked body mid-flight on demand and
(ii) record every request header of every GET, so a fence can assert on `Last-Event-ID`.

Add three fences, all `tokio::time::timeout(BOUND, ..)`-wrapped:

1. `a_dropped_stream_is_reopened_with_the_last_event_id` — handshake, let the GET open, push a frame
   carrying `id: e7`, then close the body from the server side. Assert a SECOND `GET ` request line
   appears inside `BOUND`, and that its recorded headers contain `Last-Event-ID: e7`.

2. `reconnect_gives_up_after_the_retry_budget_with_a_named_error` — close the GET body immediately on
   every accept. Assert the observed `GET ` request-line count settles at exactly
   `1 + MAX_SSE_RECONNECT_ATTEMPTS` (the initial open plus exactly the budgeted retries, no more), and
   that the exhaustion is SURFACED — the transport ends with an error naming the exhausted budget rather
   than silently going quiet, because a silent stop is indistinguishable from a healthy idle stream.
   Poll the recorded request lines rather than using a fixed sleep as a synchronisation device. If the
   shipped backoff makes the fence slower than `BOUND`, add a crate-internal test-only knob for the
   initial delay (an inherent `with_*` method on the transport struct, never a
   `StreamableHttpTransportConfig` field) and say in the SUMMARY that the knob exists and why.

3. `a_full_v2_build_names_no_resumption_cursor_on_reconnect` — assert the reconnect GET carries NO
   `Last-Event-ID` header on a `full-v2` build. This file's `#![cfg]` header requires `v1-compat`, so
   this assertion almost certainly belongs in `tests/v1_severability_tripwire.rs` instead, as a source
   assertion that `reconnect_cursor`'s `full-v2` twin returns a constant `None` and that no new
   call-site `#[cfg]` appeared. Choose one route, implement it, and record which in the SUMMARY — do not
   skip it silently.

Capture RED to `target/118.2-04-red.log`.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(client_sse_stream)' --features "full" 2>&1 | tee target/118.2-04-red.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run' ; grep -E '[1-9][0-9]* failed' target/118.2-04-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `tests/client_sse_stream.rs` contains the reconnect fence names listed above
    - The run reports a NON-ZERO test count and at least 2 failed
    - The eight fences from plans 01-03 still pass in the same run
    - `RecordingServer` exposes recorded GET request headers, so fence 1 asserts on the actual `Last-Event-ID` wire value rather than on a side effect
    - No new identifier introduced by this task contains `session_id`, `resumption_token` or `LAST_EVENT_ID` outside a `v1-compat` gate
  </acceptance_criteria>
  <done>Reconnect fences exist, run, and are RED against a transport that does not reconnect.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded reconnect with a paired cursor accessor, inside the one spawned task</name>
  <files>src/shared/streamable_http.rs</files>
  <read_first>
    - src/shared/streamable_http.rs — `start_sse` (`:928`) in full including the abort-handle take, the call-site `#[cfg]` comment at `:962-971`, `apply_resumption_header` (`:745`), the `resumption_callback` pair (`:709-729`), `DEFAULT_MAX_COLLECTED_BODY_BYTES` (`:454`) as the bounds-with-reasons model, and the reader plans 02/03 built.
    - conformance/node_modules/@modelcontextprotocol/sdk/dist/esm/client/streamableHttp.js — read `:8-12`, `:79` (`_startOrAuthSse`), `:121` (`_getNextReconnectionDelay`) and `:139` (`_scheduleReconnection`) so the pmcp curve matches the referee's.
  </read_first>
  <action>
Add the reconnect loop to `src/shared/streamable_http.rs`.

**Constants.** Bounds in this file are named constants carrying a reason; bare numbers are not
acceptable. Mirror the reference client's own values and say in each rustdoc that the value is the
reference client's, so a pmcp client behaves the way server operators already expect:
- `INITIAL_SSE_RECONNECT_DELAY: Duration = Duration::from_millis(1000)`
- `MAX_SSE_RECONNECT_DELAY: Duration = Duration::from_millis(30_000)`
- `SSE_RECONNECT_GROWTH: f64 = 1.5`
- `MAX_SSE_RECONNECT_ATTEMPTS: u32 = 2` — deliberately small; D-03's stated purpose is surviving a proxy
  blink, not indefinite reconnection.

Keep them private. Expose a `with_*` builder ONLY if a caller in this repo needs one. Do NOT add a field
to `StreamableHttpTransportConfig` under any circumstances: it is externally constructible with all-`pub`
fields and three struct-literal rustdoc examples, so a new field trips `cargo semver-checks`'
`constructible_struct_adds_field` and forces a MAJOR bump — measured and recorded by plan 113-17. Every
knob goes on the transport struct (all-private fields) as an inherent `with_*` method.

**The paired cursor accessor.** Add `reconnect_cursor()` as a `#[cfg]` PAIR with identical signatures,
exactly like `resumption_callback`:
- `#[cfg(feature = "v1-compat")] fn reconnect_cursor(&self) -> Option<String>` returning the stored last
  event id;
- `#[cfg(not(feature = "v1-compat"))] #[allow(clippy::unused_self)] const fn reconnect_cursor(&self) -> Option<String>`
  returning `None`, with rustdoc stating that a `full-v2` build has no resumption cursor and that
  answering here is what keeps the reconnect call site free of a `#[cfg]`.

The name must contain none of `session_id`, `resumption_token`, `LAST_EVENT_ID` outside a `v1-compat`
gate. `reconnect_cursor` satisfies this; `last_event_id` (lowercase) is not a tracked token.

**The retry loop.** It lives INSIDE the single spawned reader task. Do NOT call `self.start_sse(..)`
recursively from the task — `start_sse` takes and aborts `self.abort_handle` as its first act, so a
recursive call aborts the task making it. Extract the "issue one GET and return its body" step into an
internal helper (e.g. `open_sse_once(cursor: Option<String>)`) that `start_sse` and the retry loop both
call; the retry loop then owns the attempt counter and the sleep.

Loop shape:
- on end-of-body or a transport IO error from `read_next_sse_frame`, increment the attempt counter;
- if the counter exceeds `MAX_SSE_RECONNECT_ATTEMPTS`, end the stream with a NAMED error stating the
  budget was exhausted and how many attempts were made. Do not go quiet;
- otherwise sleep `min(INITIAL_SSE_RECONNECT_DELAY * SSE_RECONNECT_GROWTH^attempt, MAX_SSE_RECONNECT_DELAY)`,
  unless the last received `SseEvent::retry` gave a server-provided value, which WINS (the reference
  client does this and `SseEvent` already carries the field);
- re-open with `self.reconnect_cursor()` as the cursor argument. The call site carries NO `#[cfg]`;
- reset the attempt counter after a successful re-open that delivers at least one frame, so a stream that
  survives for hours and then blinks again gets a fresh budget rather than inheriting an old one. State
  this in a comment — it is a deliberate choice, not an oversight;
- an overflow or parse-failure end (plans 02/03's D-02 / D-05 paths) is a CORRUPTION end, not a transport
  drop: it must NOT be retried. Retrying a stream the peer is corrupting is a reconnect storm against a
  server that is already misbehaving. Distinguish the two end reasons explicitly.

**Do NOT re-issue `initialize` on reconnect.** Reconnect re-issues the GET only. A 404 / expired-session
response ends the retry loop with a named error rather than silently re-handshaking: a silent
re-handshake would mint a new session id and orphan every in-flight correlation. Record that decision in
the loop's rustdoc (RESEARCH Open Question 2).

**Do NOT add a second call-site `#[cfg]`.** After this edit, the number of `#[cfg(feature = "v1-compat")]`
attributes appearing inside function BODIES in this file must still be exactly 1 — the existing
`apply_resumption_header` call. Item-level `#[cfg]` pairs on accessors are the sanctioned form and do not
count. Verify by reading, and record the count in the SUMMARY.

Keep every new function under PMAT cog 25; split the loop from the delay computation if needed.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(client_sse_stream)' --features "full" 2>&1 | tee target/118.2-04-green.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'reconnect_cursor' src/shared/streamable_http.rs` returns ≥ 3 (the two paired definitions plus at least one call site)
    - `grep -c 'MAX_SSE_RECONNECT_ATTEMPTS' src/shared/streamable_http.rs` returns ≥ 2
    - `cargo nextest run -E 'binary(client_sse_stream)' --features "full"` reports a NON-ZERO test count with 0 failed
    - `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` reports a NON-ZERO test count with 0 failed
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` exits 0
    - The count of `#[cfg(feature = "v1-compat")]` attributes inside function bodies in `src/shared/streamable_http.rs` is 1, recorded in the SUMMARY
  </acceptance_criteria>
  <done>A dropped stream reconnects with the cursor under a bounded budget, gives up loudly, and the `full-v2` build names nothing — with no second call-site `#[cfg]`.</done>
</task>

<task type="auto">
  <name>Task 3: Fuzz seam for the incremental reader, severance proof and the semver-checks verdict</name>
  <files>fuzz/fuzz_targets/streamable_sse_frames.rs, fuzz/Cargo.toml</files>
  <read_first>
    - fuzz/fuzz_targets/subscription_listen_frames.rs — read the whole file, especially its header's three-invariant enumeration and the anti-vacuity paragraph explaining why asserting "the latch never clears" was worthless and `buffered_bytes() <= max_buffer_size` replaced it.
    - fuzz/Cargo.toml — read the `[[bin]]` block at `:184-187` for the registration shape.
    - src/shared/streamable_http.rs — the reader helpers plans 02/03/04 built, so the fuzz target drives the real decode path rather than re-implementing it.
    - src/shared/sse_parser.rs — `feed`, `overflowed`, `max_buffer_size`, `buffered_bytes`, and `take_utf8_prefix`.
  </read_first>
  <action>
**(a) The fuzz target.** Create `fuzz/fuzz_targets/streamable_sse_frames.rs` modelled on
`subscription_listen_frames.rs`. It must drive the SAME decode path the transport now uses — the
`bytes.extend_from_slice(chunk)` → `take_utf8_prefix` → `SseParser::feed` → bound-check sequence — over
arbitrary byte chunks. `hyper::body::Incoming` cannot be constructed outside hyper, which is exactly why
plans 02/03 made `drain_sse_events` and `sse_stream_overflow` free functions over already-decoded values:
fuzz those, plus the byte-buffer/UTF-8-prefix step, not the hyper frame read.

Header rustdoc must enumerate the invariants and name the run command
(`cargo +nightly fuzz run streamable_sse_frames -- -runs=20000`; the `+nightly` is load-bearing because
`cargo fuzz run` passes `-Zsanitizer=address`, which stable rustc rejects). Assert, per chunk:
1. **Never panics** for arbitrary bytes, including split multi-byte UTF-8 across chunk boundaries;
2. **Memory stays bounded** — `SseParser::buffered_bytes() <= parser.max_buffer_size()` after EVERY
   chunk, and the residual byte-buffer tail after `take_utf8_prefix` is at most 3 bytes. Carry over the
   anti-vacuity lesson explicitly in the header: do NOT assert "the overflow latch never clears", which
   cannot fail for any input at any bound;
3. **Never cross-delivers** — an event yielded by `drain_sse_events` carries data from exactly one SSE
   event.

Register it in `fuzz/Cargo.toml` with a `[[bin]]` block naming `streamable_sse_frames` and its path,
matching the existing block's shape.

**(b) Severance proof.** Run and record:
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` — the SMPL-01
  severance set (exactly what `full` has, except `v1-compat`).
- `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` — non-zero count, 0 failed.

**(c) The `cargo semver-checks` verdict** (ROADMAP success criterion 3). `cargo-semver-checks` 0.49.0 is
installed locally and is NOT part of `make quality-gate` (there is no semver target in the Makefile), so
run it manually against the phase base revision and record the verdict verbatim in the SUMMARY:

invoke `cargo semver-checks --baseline-rev <phase-base-sha> -p pmcp`.

The claims to confirm, each of which RESEARCH flagged as an assumption to VERIFY rather than assume:
- A5: replacing `mpsc::unbounded_channel()` with `mpsc::channel(N)` is semver-invisible because both
  `sender` and `receiver` are private fields and the change is in the call shape inside the crate.
- Deleting `SseParser::feed_complete_body` is invisible because it was already `pub(crate)`.
- No field was added to `StreamableHttpTransportConfig` — confirm `constructible_struct_adds_field` did
  NOT fire.

If the verdict is anything other than "no breaking changes", STOP and report rather than proceeding: this
is the criterion the whole phase's additivity claim rests on.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full" 2>&1 | tee target/118.2-04-sever.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped' &amp;&amp; RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `fuzz/fuzz_targets/streamable_sse_frames.rs` exists and contains the string `buffered_bytes` (the non-vacuous bound assertion)
    - `grep -c 'streamable_sse_frames' fuzz/Cargo.toml` returns ≥ 1 and `cd fuzz && cargo fuzz list` includes `streamable_sse_frames`
    - `cargo +nightly fuzz run streamable_sse_frames -- -runs=20000` completes without a crash artifact (record the run in the SUMMARY; if nightly is unavailable in the environment, record that fact and the `cargo fuzz list` output instead of silently skipping)
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
    - `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` reports a NON-ZERO test count with 0 failed
    - The `cargo semver-checks` verdict is recorded VERBATIM in the SUMMARY, and it reports no breaking changes
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>The reader has a non-vacuous fuzz seam, the v1 severance still builds clean, and the additive-API claim is a recorded measurement rather than an assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → pmcp client (stream lifecycle) | The peer decides when the stream ends, and therefore drives the client's reconnect behaviour |
| pmcp client → server (reconnect GET) | Client-initiated traffic whose rate the peer can provoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118.2-04-01 | Denial of Service (self-inflicted, against the server) | reconnect loop | mitigate | Bounded retry count (`MAX_SSE_RECONNECT_ATTEMPTS = 2`) plus exponential backoff with a max delay. A server that keeps closing the stream cannot be turned into a reconnect storm target. Fenced by `reconnect_gives_up_after_the_retry_budget_with_a_named_error` |
| T-118.2-04-02 | Denial of Service | reconnect after a corrupt frame | mitigate | An overflow or parse-failure end is NOT retried — only a transport drop is. Retrying a stream the peer is actively corrupting is a storm against a peer that is already misbehaving |
| T-118.2-04-03 | Tampering | peer-influenced `Last-Event-ID` on a `full-v2` build | mitigate | `reconnect_cursor`'s `full-v2` twin is a `const fn` returning `None`, so no cursor is constructible on that build. Enforced by `the_last_event_id_const_and_its_reader_are_co_gated` and `the_client_transport_carries_no_ungated_session_state` |
| T-118.2-04-04 | Spoofing / Tampering | silent re-handshake on an expired session | mitigate | Reconnect re-issues the GET ONLY. A 404 / expired-session ends the loop with a named error; a silent re-handshake would mint a new session id and orphan every in-flight correlation, so a peer could induce correlation loss by expiring a session |
| T-118.2-04-05 | Denial of Service | arbitrary peer bytes through the reader | mitigate | The fuzz seam asserts `buffered_bytes() <= max_buffer_size` after every chunk and a ≤3-byte UTF-8 tail — a NON-VACUOUS bound assertion, unlike the latch-never-clears assertion that let 20 000 green runs coexist with an unbounded-growth defect |
| T-118.2-04-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — no packages installed. `cargo-semver-checks` 0.49.0 is already present locally and is invoked, not installed |
</threat_model>

<verification>
- `cargo nextest run -E 'binary(client_sse_stream)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(v2_bounded_reads_tripwire)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(http_peer_roundtrip)' --features "full"` — 12 run, 12 passed
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`
- `cd fuzz && cargo fuzz list` includes `streamable_sse_frames`
- `cargo semver-checks --baseline-rev <phase-base-sha> -p pmcp` — verdict recorded verbatim
- `make quality-gate`
</verification>

<success_criteria>
- CONF-09 is behaviourally complete: the client opens, holds, reads incrementally, backpressures, fails loudly on corruption, and reconnects with the cursor under a bounded budget
- `target/118.2-04-red.log` / `target/118.2-04-green.log` form the RED→GREEN pair
- The `full-v2` build and both tripwires are green, and the semver verdict is recorded verbatim
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-04-SUMMARY.md` when done.
Record: the shipped backoff constants, the in-body `#[cfg]` count (must be 1), where the `full-v2` no-cursor assertion landed, the fuzz run output, and the `cargo semver-checks` verdict verbatim.
</output>
