---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/notifications.rs
  - src/server/cancellation.rs
  - tests/log_emitter.rs
autonomous: true
requirements: [CONF-10]

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A handler can call extra.log(LoggingLevel::Info, \"msg\") and the record reaches the request's notification sink as a notifications/message"
    - "extra.log with no sink returns Ok(()) and emits nothing, so a handler stays callable outside a server"
    - "A record below the configured level is not sent; a record at or above it is"
    - "LoggingLevel compares by syslog severity, not by string"
    - "No trait surface is touched — there is no new PeerHandle method"
    - "The emitter's public surface is exactly two methods — log and log_with_data — and its rustdoc does not claim delivery acknowledgement"
  artifacts:
    - path: "src/types/notifications.rs"
      provides: "LoggingLevel with PartialOrd + Ord derived over its syslog declaration order"
      contains: "Ord"
    - path: "src/server/cancellation.rs"
      provides: "RequestHandlerExtra log_sink and log_level fields plus exactly two emitter methods, log and log_with_data"
      contains: "pub fn log"
    - path: "tests/log_emitter.rs"
      provides: "the CONF-10 fence file: no-sink Ok(()), wire shape, and the exhaustive 64-pair level-filter property"
      min_lines: 180
  key_links:
    - from: "RequestHandlerExtra::log"
      to: "ServerNotification::LogMessage(LogMessageParams)"
      via: "the request-scoped Arc<dyn Fn(Notification)> sink"
      pattern: "LogMessage"
---

<objective>
Ship the handler-facing emitter surface: `extra.log(..)` on `RequestHandlerExtra`, the level type it
filters on, and the fences that pin its no-sink behaviour and its filter ordering.

Purpose: `ServerNotification::LogMessage` (`src/types/notifications.rs:135`) is constructed ONLY in
tests today (`:278`, `src/types/subscriptions.rs:920`, `src/server/subscriptions.rs:1459`) — no
production path emits it. `PeerHandle` is exactly `{ sample, sample_with_tools, list_roots, elicit,
progress_notify }` and `RequestHandlerExtra` exposes `report_progress` / `report_percent` /
`report_count` with no logging analogue. This plan gives the wire type its first production
constructor.

**D-06 is a deliberate choice, not an oversight.** `RequestHandlerExtra` only — no `PeerHandle`
method, even though this crate has an established additive-with-default-body pattern
(`PeerHandle::sample_with_tools`, `PeerHandle::elicit` both ship that way). Criterion 3's
`cargo semver-checks` verdict is therefore a clean "no public trait surface touched" rather than
"additive with a default body", and external `PeerHandle` implementors are unaffected rather than
defaulted-around. The roadmap's criterion 3 anticipated a defaulted `PeerHandle` method; do not add
one just because the prose implies it.

**API surface REDUCED per cross-AI review (MEDIUM finding, ACCEPTED).** The reviewed draft of this plan
shipped five methods (`log`, `log_with_data`, `log_info`, `log_warn`, `log_error`). Codex's point stands:
`log(level, message)` plus one structured overload satisfies CONF-10 with less semver and maintenance
exposure, and under CLAUDE.md every public method carries doc, doctest, contract and property cost.
**Ship exactly `log` and `log_with_data`.** The three severity wrappers are pure sugar over `log`,
additive at any later time without a breaking change, and are recorded as a deferred item rather than
dropped silently.

Output: the API, the level ordering, and three fences — no plumbing yet. Plans 06 and 07 wire the
sink and the level source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-CONTEXT.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md
</context>

<interfaces>
<!-- Contracts the executor needs. -->

The wire type, `src/types/notifications.rs` — already correct, needs only the ordering derive:
- `ServerNotification::LogMessage(LogMessageParams)` at `:134-135`, under `#[serde(rename = "notifications/message")]`
- `LogMessageParams` at `:158-193`: `level`, `logger` (skipped when `None`), `message`, `data`
  (skipped when `None`), with `new` (`:178`), `with_logger` and `with_data` builders
- `LoggingLevel` at `:216-234`:
  `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)] #[serde(rename_all = "lowercase")]`
  with variants in this order: `Debug, Info, Notice, Warning, Error, Critical, Alert, Emergency`.
  **The declaration order IS syslog severity order**, so `#[derive(PartialOrd, Ord)]` produces the
  correct comparison for free.

The signature template, `src/server/cancellation.rs:656-668` — copy the `None => Ok(())` shape verbatim:

```
pub async fn report_progress(&self, progress: f64, total: Option<f64>, message: Option<String>)
    -> crate::Result<()>
{
    if let Some(rep) = &self.progress_reporter { rep.report_progress(progress, total, message).await }
    else { Ok(()) }
}
```

The struct: `RequestHandlerExtra` is `#[non_exhaustive]` (`src/server/cancellation.rs:178`), which is
why adding a field is semver-additive — external struct-literal construction is already impossible.
`pub progress_reporter: Option<Arc<dyn ProgressReporter>>` sits at `:196-197`; the new fields go beside
it. `RequestHandlerExtra::new(request_id, cancellation_token)` at `:271` initialises every field to its
inert value; `impl Default for RequestHandlerExtra` is at `:694` and its rustdoc (`:692-698`) documents
it as usable standalone "for testing and simple tool invocations" — that is the doctest hook.

The sink type, already plumbed into both dispatch roots by Phase 118.1:
`Arc<dyn Fn(Notification) + Send + Sync>`. It is SYNCHRONOUS. `ServerProgressReporter::new` takes one
today and it already carries ANY `Notification`, not just progress.

The property-test analog is `tests/notification_properties.rs` (`proptest` is already a dev-dependency;
confirm in `Cargo.toml` before writing).

The integration-fence analog is `tests/http_peer_roundtrip.rs` — `#![cfg]` header at `:47-52`,
bounds-as-constants at `:74-96`, `build_server` / `spawn_server` at `:302-370`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Derive syslog ordering on LoggingLevel and fence it exhaustively over all 64 pairs</name>
  <files>src/types/notifications.rs, tests/log_emitter.rs</files>
  <read_first>
    - src/types/notifications.rs — read `:130-240`: the `ServerNotification::LogMessage` variant and its serde rename, `LogMessageParams` and its three builders, and `LoggingLevel`'s full declaration including the doc comment on each variant.
    - tests/notification_properties.rs — read the header and the `prop_compose!` usage so the new property arm matches the house shape.
    - tests/http_peer_roundtrip.rs — read `:40-96` for the `#![cfg]` header and the bounds-as-constants-with-reasons convention that `tests/log_emitter.rs` inherits.
    - Cargo.toml — confirm `proptest` is a dev-dependency before writing a `proptest!` block.
  </read_first>
  <action>
Add `PartialOrd, Ord` to `LoggingLevel`'s derive list in `src/types/notifications.rs`. This is
semver-additive. Add one rustdoc paragraph on the enum stating WHY the derive is load-bearing: the
declaration order is syslog severity order, so `Ord` gives the correct comparison for free, and level
filtering must never compare the serialized strings — alphabetically `critical < debug`, which would
suppress `debug` records while passing `critical` ones through a `>= debug` filter, i.e. exactly
backwards.

Create `tests/log_emitter.rs` with:
- the `#![cfg(all(feature = "streamable-http", feature = "v1-compat", not(target_arch = "wasm32")))]`
  header (later tasks and plans 06/07/08 add wire fences to this same file);
- a module doc restating the reliability doctrine (ephemeral port, readiness by bind, shutdown order,
  every wire await `tokio::time::timeout`-wrapped) and naming the file's requirement (CONF-10);
- bounds as named constants with reasons (`BOUND`, `QUIET`), mirroring `tests/http_peer_roundtrip.rs:74-96`.

Add the level-ordering fence `log_levels_order_by_syslog_severity_not_by_string`: iterate the full 8×8 =
64 `(configured, emitted)` matrix EXHAUSTIVELY (cheaper and strictly stronger than sampling) and assert
`emitted >= configured` matches the intended severity relation for every pair. Include an explicit
regression assertion that `LoggingLevel::Critical > LoggingLevel::Debug` — the exact pair a string
comparison gets wrong. Add a `proptest` arm over the enum too if it reads naturally; the exhaustive loop
is the load-bearing half.

**Name the test function so `make test-property` can select it.** `make test-property` runs
`cargo test --features "full" -- --ignored property_`, so a property arm intended for that target must be
named with a `property_` prefix AND marked `#[ignore]`. Decide explicitly: either add a `property_`-prefixed
`#[ignore]` arm in addition to the always-run exhaustive fence, or record in the SUMMARY why the exhaustive
fence discharges the ALWAYS property requirement on its own. Do not leave it ambiguous — `validate-always`
is in `make quality-gate`.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(log_emitter)' --features "full" 2>&1 | tee target/118.2-05-t1.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'derive' src/types/notifications.rs` shows `PartialOrd` and `Ord` on `LoggingLevel`'s derive list
    - `tests/log_emitter.rs` exists, begins with the `#![cfg(all(...))]` header, and defines `BOUND`
    - `cargo nextest run -E 'binary(log_emitter)' --features "full"` reports a NON-ZERO test count with 0 failed
    - The ordering fence covers all 64 `(configured, emitted)` pairs — assert this structurally (a counter incremented in the loop asserted to equal 64), not by inspection
    - `cargo semver-checks` is not required for this task, but the derive addition is noted in the SUMMARY for plan 04's verdict to cover
  </acceptance_criteria>
  <done>`LoggingLevel` orders by severity, and a 64-pair exhaustive fence proves it including the `critical`/`debug` inversion a string comparison produces.</done>
</task>

<task type="auto">
  <name>Task 2: Add the log_sink and log_level fields plus the extra.log family</name>
  <files>src/server/cancellation.rs</files>
  <read_first>
    - src/server/cancellation.rs — read the module doc (`:1-40`), the `#[non_exhaustive]` struct declaration and every field (`:170-270`), `RequestHandlerExtra::new` (`:271-300`), `era()` (`:367`), `report_progress` / `report_percent` / `report_count` (`:656-690`), and `impl Default` with its rustdoc (`:692-700`).
    - src/server/progress.rs — read the `ProgressReporter` trait (`:17`) and `ServerProgressReporter` (`:71`) including its 100 ms rate limiter. It is the STRUCTURAL template minus the rate limiter (D-09) and minus the token gate (D-07); do not reuse the type.
    - src/types/notifications.rs — `LogMessageParams::new` / `with_logger` / `with_data` and the `ServerNotification::LogMessage` variant.
  </read_first>
  <action>
Add two private-by-convention fields to `RequestHandlerExtra`, beside `progress_reporter`:
- `log_sink: Option<Arc<dyn Fn(Notification) + Send + Sync>>` — the request-scoped notification sink,
  UNGATED by any progress token;
- `log_level: Option<LoggingLevel>` — the resolved level for this request. `None` means "nothing set
  one"; the effective level then falls back to the D-12 default.

Match the existing field style (the struct's fields are `pub` with `#[allow(dead_code)]` where
appropriate — read and follow whatever the neighbouring fields do). Initialise both to `None` in
`RequestHandlerExtra::new` and in `Default`. Add `with_log_sink(..)` and `with_log_level(..)` builder
methods following whatever `with_peer` / `with_protocol_context` do in this file, so plans 06 and 07 have
a construction seam.

Mint `const DEFAULT_LOG_LEVEL: LoggingLevel = LoggingLevel::Info;` (D-12) with rustdoc stating the
reason: conventional across MCP implementations, `debug` and finer are suppressed until asked for so a
chatty handler cannot flood a client that never opted in, and the conformance scenario emits at `info`
or above and therefore passes unconfigured.

Add the emitter. **Synchronous, not `async`** — the sink is a synchronous
`Arc<dyn Fn(Notification) + Send + Sync>`; `report_progress` is `async` only because the
`ProgressReporter` TRAIT is, and D-06 adds no trait, so `async` would buy nothing and would force every
logging call site into an `.await`. Return `crate::Result<()>` (not `()`) so a future "sink refused"
signal is not a breaking change.

**EXACTLY TWO methods** (cross-AI review, MEDIUM, accepted — see the objective):

- `pub fn log(&self, level: LoggingLevel, message: impl Into<String>) -> crate::Result<()>`
- `pub fn log_with_data(&self, level: LoggingLevel, message: impl Into<String>, data: serde_json::Value) -> crate::Result<()>`

Do NOT add `log_info` / `log_warn` / `log_error`. Append them to the phase's `deferred-items.md`
instead, with the note that they are pure sugar over `log`, additive at any time, and were withheld to
keep the first public surface minimal.

**`Result<()>` MUST NOT be documented as delivery acknowledgement** (cross-AI review, MEDIUM,
accepted). The sink type is `Arc<dyn Fn(Notification) + Send + Sync>` — it returns `()` and cannot
report failure — and the existing `notification_tx` fallback in `src/server/mod.rs:1296` explicitly
IGNORES its own `try_send` result. So `Ok(())` here means "the record was handed to whatever sink this
request has, or there was none", NOT "the client received it". Write that sentence in the rustdoc in
those terms, and state that the `Result` exists so a future refusal signal is additive rather than
breaking. Rustdoc that implies acknowledgement would be a false guarantee a caller could build retry
logic on.

Behaviour, in one shared private helper both methods delegate to (so the filter rule exists once):
1. Compare `level` against `self.log_level.unwrap_or(DEFAULT_LOG_LEVEL)`. If `level < effective`, return
   `Ok(())` without constructing anything.
2. Build `LogMessageParams::new(level, message)` — `logger` defaults to `None`. Do NOT synthesise a
   logger value from the tool name; that would be a guess. `data` is set only by `log_with_data`.
3. Wrap in `ServerNotification::LogMessage(..)` and hand it to `self.log_sink`.
4. **D-08: no sink → `Ok(())`, silently dropped.** Matches `report_progress` (`:663`) exactly, so a
   handler stays callable outside a server — `RequestHandlerExtra::default()` is documented for "testing
   and simple tool invocations". State the accepted cost plainly in the method rustdoc: a misplumbed
   transport looks identical to a quiet handler, and the conformance fence is what catches it, so this is
   a production-diagnostics hole rather than a false-green in the gate.

**D-09: no rate limit.** `ServerProgressReporter` rate-limits to 100 ms and that is right for progress —
dropping an intermediate value loses nothing because the next supersedes it. Log records are not
idempotent: each one is the only copy of its information, and a rate limiter silently deletes evidence.
Say this in the rustdoc so the omission reads as a decision.

**Pitfall 4, stated in the rustdoc rather than discovered later — and D-09's "no log loss" framing
corrected here** (cross-AI review, MEDIUM, accepted). On v2 the sink a record rides is
`new_v2_progress_queue()` — an `mpsc::channel(64)` with an explicit DROP-NEWEST `try_send` policy,
because the closure is synchronous and must not block. So a v2 handler emitting more than 64
notifications in one call LOSES THE EXCESS. D-09's rationale ("a rate limiter silently deletes
evidence") is about the EMITTER not adding a limiter of its own; it is not a claim that no record can
ever be dropped downstream, and the rustdoc must not read as though it were. Say plainly: the emitter
never drops and never rate-limits; the v2 progress queue's drop-newest policy is a property of that
vehicle, and a v2 handler that must not lose records should keep a call under the queue capacity.
Task 3 fence 4 PINS that behaviour with a saturation test rather than leaving it as prose.
Also note that the v2 vehicle is only attached when `v2_multi_frame_eligible` holds (era is V2 AND
`!config.enable_json_response` AND the request's `Accept` contains `text/event-stream`), so a v2 client
that asked only for JSON gets no log records at all — correct behaviour to document, not a bug to fix.
Do NOT make the emitter block; the sink signature is a synchronous `Fn(Notification)`.

**Do not gate emission on `ServerCapabilities.logging`** (`src/types/capabilities.rs:118`). The sink is
per-request and the capability is advisory; gating would make a correctly-plumbed server silently mute.
Document that servers SHOULD declare it (RESEARCH Open Question 6).

Write a rustdoc doctest on `log` that constructs `RequestHandlerExtra::default()` and calls `.log(..)`,
relying on D-08 to make it `Ok(())` with no server. Doctests are in the gate.

Check `../provable-contracts/contracts/pmcp/` for a contract covering `RequestHandlerExtra` BEFORE the
first commit — `pmat comply check` runs inside `make quality-gate` via the `comply` target, and a new
public method may need a contract update. Do this first, not after a gate failure.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'cargo test --features "full" --doc server::cancellation 2>&1 | tee target/118.2-05-t2-doc.log' &amp;&amp; bash -o pipefail -c 'RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full" 2>&1 | tee target/118.2-05-t2-build.log'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//\|^\s*///' src/server/cancellation.rs | grep -c 'pub fn log'` returns EXACTLY 2 — `log` and `log_with_data`, and no severity wrappers
    - `grep -v '^\s*//\|^\s*///' src/server/cancellation.rs | grep -c 'async fn log'` returns 0 — the emitter is synchronous
    - `grep -v '^\s*//\|^\s*///' src/server/cancellation.rs | grep -c 'DEFAULT_LOG_LEVEL'` returns ≥ 2
    - `grep -rn 'fn log' src/shared/peer.rs` returns 0 — no `PeerHandle` trait method was added
    - The `log` rustdoc states that `Ok(())` is NOT delivery acknowledgement, naming the `Fn(Notification) -> ()` sink type as the reason — verify by reading, and quote the sentence in the SUMMARY
    - The phase's `deferred-items.md` records the withheld `log_info` / `log_warn` / `log_error` wrappers
    - The `log` rustdoc contains a runnable doctest using `RequestHandlerExtra::default()`, and `cargo test --features "full" --doc` passes
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` exits 0
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` exits 0
    - No function added exceeds PMAT cognitive complexity 25
  </acceptance_criteria>
  <done>`extra.log(..)` exists, is synchronous, filters by level, is `Ok(())` with no sink, and touches no trait surface.</done>
</task>

<task type="auto">
  <name>Task 3: Fences for the no-sink contract, the wire shape and the filter</name>
  <files>tests/log_emitter.rs</files>
  <read_first>
    - src/server/cancellation.rs — the emitter task 2 just wrote, including the shared private helper and the builder seams.
    - src/types/notifications.rs — the exact serde shape of `ServerNotification::LogMessage` and `LogMessageParams`, so the wire assertion is against the SPEC shape rather than against pmcp's own serializer round-tripping itself.
    - schema/vendored — locate the `notifications/message` / `LoggingMessageNotification` definition and read its required fields, so the wire fence is derived from the spec (118.1 D-04: byte-literal fences derived from the spec, not from pmcp's own serializer).
  </read_first>
  <action>
Add three fences to `tests/log_emitter.rs`. All are in-process — no wire, no server — so they are fast
and belong in the same binary as the ordering fence.

1. `log_with_no_sink_is_ok_and_emits_nothing` — build `RequestHandlerExtra::default()`, call BOTH
   methods, assert every call returns `Ok(())`. Then build an extra WITH a capturing sink, call nothing,
   and assert the capture is empty — so "emits nothing" is asserted positively rather than inferred
   from the absence of a panic.

2. `a_log_record_serializes_as_the_spec_notifications_message_shape` — install a sink that pushes into a
   `Vec<Notification>`, call `extra.log(LoggingLevel::Warning, "hello")` and
   `extra.log_with_data(LoggingLevel::Error, "boom", json!({"k": 1}))`, then serialize each captured
   notification with `serde_json::to_value` and assert on the JSON, not on the Rust enum:
   - `method` equals the literal `"notifications/message"`;
   - `params.level` equals the literal lowercase `"warning"` / `"error"`;
   - `params.message` equals the text;
   - `params.logger` is ABSENT for both (skipped when `None`), and `params.data` is absent for the first
     and equals the object for the second.
   Assert with literal strings; do not compare against a constant imported from `src/`, because a test
   that reads its expectation out of the code under test asserts nothing.

3. `a_record_below_the_configured_level_is_not_sent` — with a capturing sink and
   `with_log_level(LoggingLevel::Warning)`, assert `log(Debug, ..)` and `log(Info, ..)` capture nothing
   and both return `Ok(())`, while `log(Warning, ..)`, `log(Error, ..)` and `log(Emergency, ..)` each
   capture exactly one record. Then repeat with NO configured level and assert the D-12 default `info`
   applies: `Debug` is suppressed, `Info` is sent.

4. `a_saturated_bounded_sink_drops_the_excess_and_the_emitter_still_returns_ok` — the loss-policy fence
   the cross-AI review asked for, replacing the plans' earlier "no log loss" framing with a pinned
   behaviour. Build a sink over an `mpsc::channel(N)` with a small `N` and a `try_send` DROP-NEWEST
   closure, modelled on `new_v2_progress_queue`'s policy (read it; do not re-derive it). Emit `N + 8`
   records. Assert: (a) every `log(..)` call returned `Ok(())` — the emitter never blocks and never
   reports downstream loss, which is precisely why its rustdoc must not claim acknowledgement; (b) the
   receiver holds exactly `N` records; (c) the records it holds are the FIRST `N`, i.e. the policy is
   drop-newest, matching the vehicle rather than silently differing from it. This fence is the reason
   the v2 limitation is a documented property rather than an assertion nobody checks.

Every one of these must be demonstrated RED against the tree before task 2 lands — since tasks 2 and 3
are in the same plan, run task 3's fences against the pre-task-2 tree (they will not compile, which is
the RED for an API that does not exist yet: record the compile error naming the missing method) OR write
them first and run them. Record which route was taken and the evidence in `target/118.2-05-red.log`.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(log_emitter)' --features "full" 2>&1 | tee target/118.2-05-green.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run -E 'binary(log_emitter)' --features "full"` reports a NON-ZERO test count with 0 failed
    - All four fence names above exist in `tests/log_emitter.rs`
    - `grep -c '"notifications/message"' tests/log_emitter.rs` returns ≥ 1 — the method name is a literal in the test, not an import from `src/`
    - The filter fence asserts BOTH directions (suppressed below, sent at or above) and BOTH the configured and the defaulted case
    - The saturation fence asserts the receiver holds exactly `N` records AND that they are the FIRST `N` — a fence that only checks the count cannot tell drop-newest from drop-oldest
    - `target/118.2-05-red.log` records the pre-fix RED evidence
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>The emitter's contract — no-sink `Ok(())`, spec wire shape, level filtering including the default — is pinned by fences that assert against literals rather than against the code under test.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files this plan CREATES (excluded from drift verification):

**New files**
- `tests/log_emitter.rs` (extended by plans 06, 07 and 08)
- `.planning/phases/118.2-.../deferred-items.md` if it does not already exist (for the withheld
  severity wrappers)

**New source items**
- `src/types/notifications.rs` — `PartialOrd, Ord` added to `LoggingLevel`'s derive list (no new type)
- `src/server/cancellation.rs` — `RequestHandlerExtra::log_sink` and `RequestHandlerExtra::log_level`
  fields; `with_log_sink` and `with_log_level` builders; `DEFAULT_LOG_LEVEL`; `pub fn log`;
  `pub fn log_with_data`; one shared private emit helper (name at executor's discretion)

**New test functions** (in `tests/log_emitter.rs`)
- `log_levels_order_by_syslog_severity_not_by_string`
- `log_with_no_sink_is_ok_and_emits_nothing`
- `a_log_record_serializes_as_the_spec_notifications_message_shape`
- `a_record_below_the_configured_level_is_not_sent`
- `a_saturated_bounded_sink_drops_the_excess_and_the_emitter_still_returns_ok`
- `BOUND`, `QUIET` constants

**Deliberately NOT produced** — `log_info`, `log_warn`, `log_error`. Withheld per cross-AI review;
recorded in `deferred-items.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler code → notification sink | Handler-supplied strings become wire payloads delivered to a client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118.2-05-01 | Denial of Service | `extra.log` on the v2 vehicle | medium | accept | The v2 sink is `new_v2_progress_queue()` — `mpsc::channel(64)`, DROP-NEWEST `try_send`, non-blocking because the closure is synchronous. A v2 handler emitting >64 notifications in one call loses the excess. Accepted, DOCUMENTED at the emitter, and now PINNED by `a_saturated_bounded_sink_drops_the_excess_and_the_emitter_still_returns_ok` rather than left as prose; the emitter must not block |
| T-118.2-05-02 | Information Disclosure | a handler flooding a client that never asked for verbose logs | medium | mitigate | D-12's `info` default suppresses `debug` and finer until requested. Fenced by the defaulted-level half of `a_record_below_the_configured_level_is_not_sent` |
| T-118.2-05-03 | Tampering | level comparison by string | high | mitigate | `Ord` derived over the syslog declaration order; the 64-pair exhaustive fence includes the `critical`/`debug` inversion that a string comparison produces, which would pass `critical` through a `>= debug` filter while suppressing `debug` — exactly backwards |
| T-118.2-05-04 | Repudiation | rate-limiting log records | medium | mitigate | D-09: no rate limit IN THE EMITTER. Log records are not idempotent — each is the only copy of its information, and a limiter silently deletes evidence. The rationale is in the rustdoc so the omission reads as a decision, and the rustdoc is explicit that downstream vehicles may still drop |
| T-118.2-05-05 | Repudiation | no-sink silence, and `Ok(())` read as acknowledgement | medium | accept | D-08: `Ok(())` with no sink means a misplumbed transport looks identical to a quiet handler, and the sink's `Fn(Notification) -> ()` type means `Ok(())` can never mean "delivered". Accepted because the conformance fence (a test asserting logs arrive) fails loudly when none do, so this is a production-diagnostics hole rather than a false-green in the gate. The rustdoc states both limits explicitly so no caller builds retry logic on the `Result`. Recorded in `deferred-items` as a candidate for a `tracing::warn!`-once variant |
| T-118.2-05-06 | Elevation of Privilege | public API surface larger than the requirement | low | mitigate | Two methods, not five. Every additional public method is permanent surface under this repo's semver policy and carries doc, doctest, contract and property obligations; the severity wrappers are additive later at zero cost |
| T-118.2-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no packages installed |
</threat_model>

<verification>
- `cargo nextest run -E 'binary(log_emitter)' --features "full"` — non-zero count, 0 failed
- `cargo test --features "full" --doc` — the `extra.log` doctest passes
- `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` and `--no-default-features --features full-v2`
- `grep -rn 'fn log' src/shared/peer.rs` — 0 hits (no trait surface touched)
- `make quality-gate`
</verification>

<success_criteria>
- `extra.log(..)` and `extra.log_with_data(..)` — and NOTHING else — exist, are synchronous, return `crate::Result<()>`, and are `Ok(())` with no sink
- The `Result<()>` rustdoc explicitly disclaims delivery acknowledgement and names the `Fn(Notification) -> ()` sink type as the reason
- `LoggingLevel` orders by syslog severity with a 64-pair exhaustive proof
- `PeerHandle` is unchanged
- The v2 drop-newest limitation is documented at the emitter AND pinned by a saturation fence, not discovered later
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-05-SUMMARY.md` when done.
Record: the final `extra.log` / `extra.log_with_data` signatures verbatim; the `Result<()>` disclaimer
sentence verbatim; the `DEFAULT_LOG_LEVEL` decision; the saturation fence's observed drop behaviour; the
`deferred-items.md` entry for the withheld severity wrappers; how the ALWAYS property requirement was
discharged (`property_`-prefixed `#[ignore]` arm vs the exhaustive fence); and the contract-check result
from `../provable-contracts/`.
</output>
