---
phase: 118.2
slug: the-v1-client-sse-transport-and-the-notifications-message-em
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-11
---

# Phase 118.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `118.2-RESEARCH.md` § Validation Architecture — read that section for full detail.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` / `#[tokio::test]` + `proptest` + `cargo-fuzz`, driven by `cargo test` / `cargo nextest` |
| **Config file** | `Cargo.toml` (`[[test]]` targets) + `Makefile` targets; no separate test-runner config |
| **Quick run command** | `cargo nextest run -E 'binary(<name>)'` — **`binary(..)`, never `test(/…/)`** (silent-zero-selection pitfall; bit Phase 114 seven times) |
| **Full suite command** | `make test-all` (invoked by `make quality-gate`) |
| **External oracle** | `bash scripts/run-conformance-suite.sh` (builds examples, runs both era legs, applies the gate) |
| **Estimated runtime** | quick: seconds per binary; full gate: minutes; conformance suite: ~20 min both legs |

Every verify block must assert a **non-zero test count** in its expected output.
Every wire-crossing await in a fence is `tokio::time::timeout`-wrapped — the unfixed client
**hangs** rather than fails, which would otherwise defeat RED-first demonstration.

---

## Sampling Rate

- **After every task commit:** run the plan's named `binary(...)` fence, plus `make quality-gate` (mandatory pre-commit per CLAUDE.md)
- **After every plan wave:** `make test-all`, plus `binary(v2_bounded_reads_tripwire)`, `binary(v1_severability_tripwire)`, `binary(http_peer_roundtrip)` (expect 12 run / 12 passed), `binary(era_matrix)`, plus the `full-v2` severance build (`RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2`)
- **Before `/gsd:verify-work`:** full `make quality-gate` green, conformance suite re-measured with D-16 hardening in place, `cargo semver-checks` verdict recorded
- **Max feedback latency:** ~120 s for in-tree fences; the ~20-min suite runs at wave boundaries at most, once at phase end mandatorily (plans 11/12 — the external oracle IS the deliverable there, matching 118.1 precedent)

---

## Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| CONF-09 | Client issues a GET after the initialized 202 (Defect A) | integration | `cargo nextest run -E 'binary(client_sse_stream)'` | ❌ Wave 0 (plan 01) |
| CONF-09 | `start_sse` returns promptly against a never-ending stream; events delivered incrementally (Defect B) | integration | `cargo nextest run -E 'binary(client_sse_stream)'` | ❌ Wave 0 |
| CONF-09 | Oversized chunk latches `overflowed`, ENDS stream with named error (D-02) | unit | `cargo nextest run -E 'binary(client_sse_stream)'` | ❌ Wave 0 |
| CONF-09 | Unparseable frame ENDS the stream with a named error (D-05) | unit | `cargo nextest run -E 'binary(client_sse_stream)'` | ❌ Wave 0 |
| CONF-09 | Reconnect re-issues GET with `Last-Event-ID`, gives up after retry budget (D-03) | integration | `cargo nextest run -E 'binary(client_sse_stream)'` | ❌ Wave 0 |
| CONF-09 | Slow consumer applies backpressure; NO frame dropped (D-04) | integration | `cargo nextest run -E 'binary(client_sse_stream)'` | ❌ Wave 0 |
| CONF-09 | Existing whole-body/severability tripwires still pass | tripwire | `binary(v2_bounded_reads_tripwire)` + `binary(v1_severability_tripwire)` | ✅ |
| CONF-09 | v1 severance still builds | build | `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` | ✅ |
| CONF-10 | `extra.log(..)` with no sink is `Ok(())`, emits nothing (D-08) | unit | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 (plan 05) |
| CONF-10 | Record from a v1 tool handler reaches a live session SSE stream | integration | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 |
| CONF-10 | Record from a v2 tool handler rides the multi-frame POST body | integration | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 |
| CONF-10 | Level filtering: `emitted >= configured` iff sent, all 64 level pairs | property | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 |
| CONF-10 | v1 `logging/setLevel` stores per-SESSION (client B cannot change client A) | integration | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 |
| CONF-10 | v1 `logging/setLevel` still answers a literal `{}` | unit | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 |
| CONF-10 | v2 honours `io.modelcontextprotocol/logLevel` in request `_meta` | integration | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 |
| CONF-10 | Both dispatch roots agree about `SetLoggingLevel` (D-13) | unit | `cargo nextest run -E 'binary(log_emitter)'` | ❌ Wave 0 |
| CONF-09+10 | **Joint fence:** pmcp client receives `notifications/message` over a live stream from a pmcp server (D-15.3) | integration | `cargo nextest run -E 'binary(pmcp_both_ends_logging)'` | ❌ Wave 0 (plan 10, wave 5) |
| both | `era_matrix`: `no-live-stream` → `completed` | integration | `cargo nextest run -E 'binary(era_matrix)'` | ✅ (assertion changes) |
| both | Server half must not regress | integration | `binary(http_peer_roundtrip)` — expect 12 run, 12 passed | ✅ |
| both | The official suite, delta vs 118.1 closing numbers | external | `bash scripts/run-conformance-suite.sh` | ✅ |

**Per-plan fence ownership:** 01–04 → `client_sse_stream`; 05–09 → `log_emitter` (+ example in 09);
10 → `pmcp_both_ends_logging` + `era_matrix` flip; 11 → suite re-measure + gate hardening;
12 → re-pin re-measure (blocking human checkpoint).

---

## Wave 0 Requirements

- [ ] `tests/client_sse_stream.rs` — client fence file with a **recording** TCP listener helper (logs request lines; holds a chunked `text/event-stream` open indefinitely; delivers frames on demand; closes mid-stream for reconnect). The research prototype was deleted — re-derive, don't assume it survives. Covers CONF-09. (plan 01)
- [ ] `tests/log_emitter.rs` — emitter fence over a real `StreamableHttpServer` on an ephemeral port with a raw stream-holding client (reuse `tests/http_peer_roundtrip.rs` reader shape). Covers CONF-10. (plan 05)
- [ ] `tests/pmcp_both_ends_logging.rs` — the D-15.3 joint fence; blocked on both waves. (plan 10)
- [ ] Property-test harness for `LoggingLevel` ordering (proptest already a dev-dependency)
- [ ] Fuzz seam for the incremental reader, or written justification for absence (`make validate-always` runs in `make quality-gate`)
- [ ] Runnable example demonstrating `extra.log(..)` (CLAUDE.md ALWAYS requirement) (plan 09)
- [ ] **Every fence demonstrated RED against the unfixed tree before its fix lands** (118.1 D-04, carried by D-15)

## CLAUDE.md ALWAYS Requirements (per feature)

- **Fuzz:** incremental SSE reader seam (or justified absence)
- **Property:** `LoggingLevel` ordering / 64-pair level-filter truth table
- **Unit:** per-fix unit tests at each seam (overflow, unparseable frame, `{}` result, D-13 twin roots)
- **Example:** runnable `extra.log(..)` demonstration (plan 09)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Final re-pin delta review (D-14) | both | Two deltas (this phase's fixes vs suite bump) must be attributed by a human reading the report | Plan 12's blocking `checkpoint:human-verify` presents both numbers side by side |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (30/30 tasks carry `<automated>` verify — planner audit, confirmed by plan-checker)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (fence files created inside the plans that need them)
- [x] No watch-mode flags
- [x] Feedback latency < 120s for in-tree fences (suite runs are wave-boundary/phase-end only, per 118.1 precedent)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-11 (plan-checker VERIFICATION PASSED; content synced from RESEARCH.md § Validation Architecture)
