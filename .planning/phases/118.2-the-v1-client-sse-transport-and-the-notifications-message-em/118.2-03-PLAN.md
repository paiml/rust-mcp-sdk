---
phase: 118.2-the-v1-client-sse-transport-and-the-notifications-message-em
plan: 03
type: execute
wave: 2
depends_on: [118.2-01]
files_modified:
  - src/shared/streamable_http.rs
  - src/shared/sse_parser.rs
  - tests/client_sse_stream.rs
  - tests/v2_bounded_reads_tripwire.rs
autonomous: true
requirements: [CONF-09]

estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A notification carried on a POST text/event-stream response reaches the client BEFORE the response frame"
    - "A server-to-client request carried on a POST text/event-stream response is parsed and answerable before the call ends"
    - "The POST SSE reader terminates when its owning transport is dropped — no orphan task survives"
    - "Response-body middleware is documented as bypassed on the POST SSE path, matching post_streaming's existing contract"
    - "SseParser::feed_complete_body has no production caller and no false rustdoc describing one"
  artifacts:
    - path: "src/shared/streamable_http.rs"
      provides: "the same incremental reader applied at the POST-response SSE site"
      contains: "read_next_sse_frame"
    - path: "src/shared/sse_parser.rs"
      provides: "feed_complete_body retired, with no rustdoc naming call sites that no longer exist"
  key_links:
    - from: "post_body text/event-stream branch"
      to: "the shared incremental reader"
      via: "the same four-function split the GET path uses"
      pattern: "read_next_sse_frame"
---

<objective>
Apply the incremental reader to the **second** whole-body collect site — the POST response that
answers `text/event-stream` — and retire `SseParser::feed_complete_body`, which loses its last
production caller in the same edit.

Purpose: D-01 widened the roadmap's scope deliberately, and this is the site that made it worth doing.
In StreamableHTTP a POST that answers `text/event-stream` stays open and can carry notifications
**and server-to-client requests** before the response frame. Collecting it whole means progress
arrives only after the call ends, and **in-tool elicitation over a POST stream deadlocks outright** —
the client cannot answer a request it has not parsed yet.

`feed_complete_body`'s disposition is a plan decision, not a build-time discovery (Pitfall 5). After
this edit it has zero non-test callers: under `-D warnings` that is a `dead_code` failure, its own
rustdoc names both call sites as the discharge of its byte-cap precondition and that prose becomes
false, and the tripwire ALLOWLIST justification that cites it goes stale. Deleting is a strict
reduction in attack surface — the rustdoc itself calls a public unbounded parser entry point "an
attractive nuisance."

Output: one reader shape, two sites; `feed_complete_body` gone with all three of its co-located
dependants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-CONTEXT.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-PATTERNS.md
@.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-01-SUMMARY.md
</context>

<!--
  PLAN NUMBERING NOTE. `118.2-02-PLAN.md` was DELETED when cross-AI review finding 1 forced the
  reachable-202 branch and the incremental GET reader into ONE atomic plan. Everything this plan
  previously attributed to "plan 02" now lives in `118.2-01-PLAN.md`. Plan numbers 03-12 are
  deliberately unchanged so every existing cross-reference stays valid; the gap at 02 is recorded,
  not a missing file.
-->

<interfaces>
<!-- Contracts the executor needs. -->

The POST-response site being replaced, `src/shared/streamable_http.rs:1666-1706` — it is the
byte-identical twin of the GET reader plan 01 already replaced:

```
} else if content_type.contains(TEXT_EVENT_STREAM) {
    let sender = self.sender.clone();
    let on_resumption = self.resumption_callback();
    let last_event_id = self.last_event_id.clone();
    tokio::spawn(async move {
        let mut sse_parser = SseParser::new();
        let body = String::from_utf8_lossy(&modified_body);
        let events = sse_parser.feed_complete_body(&body);      // :1685
        for event in events {
            if let Some(id) = &event.id { *last_event_id.write() = Some(id.clone());
                if let Some(callback) = &on_resumption { callback(id.clone()); } }
            if event.event.as_deref() == Some("message") || event.event.is_none() {
                if let Ok(msg) = crate::shared::StdioTransport::parse_message(event.data.as_bytes()) {
                    let _ = sender.send(msg);                    // :1701
                }
            }
        }
    });
}
```

Note it operates on `modified_body`, a `Vec<u8>` the caller already collected whole. The fix removes
that collect for this branch and hands the live `hyper::body::Incoming` to the shared reader instead.

The reader plan 01 built, in the same file: `SseReadState`, `read_next_sse_frame`, `drain_sse_events`,
`sse_stream_overflow`, `CLIENT_RECEIVE_QUEUE_CAPACITY`. Reuse them; do not fork a second copy.

`SseParser::feed_complete_body` is `pub(crate) fn feed_complete_body(&mut self, body: &str) -> Vec<SseEvent>`
at `src/shared/sse_parser.rs:473`. Its three co-located dependants:
1. `#[test] feed_complete_body_bypasses_the_bound` at `src/shared/sse_parser.rs:1630`
2. the ALLOWLIST justification prose in `tests/v2_bounded_reads_tripwire.rs` that cites it
3. the "both call sites" sentence in `feed_complete_body`'s own rustdoc, which names
   `StreamableHttpTransport::post_body` and `StreamableHttpTransport::start_sse`

The whole-body tripwire that must KEEP passing:
`tests/v2_bounded_reads_tripwire.rs::the_two_known_capped_whole_body_reads_are_found_and_classified_bounded`
asserts EXACTLY 2 shipped `.collect().await` sites — one in `src/shared/http.rs`, one in
`src/shared/streamable_http.rs` — both `Limited::new(`-wrapped. The count is over the
`collect_body_within_cap` HELPER BODIES, which must stay: they still serve `collect_capped_body`
(`streamable_http.rs:695`) and `jsonrpc_error_envelope` (`:1330`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RED fence — a notification and a server-to-client request arrive on a POST SSE body before the result frame</name>
  <files>tests/client_sse_stream.rs</files>
  <read_first>
    - tests/client_sse_stream.rs — the harness and the six fences from plan 01.
    - src/shared/streamable_http.rs — read `post_body` `:1560-1720` so the fence drives the exact branch (`content_type.contains(TEXT_EVENT_STREAM)` on a POST response, not the GET path).
    - tests/http_peer_roundtrip.rs — read the in-tool elicitation arm to see what a server-to-client request looks like on the wire, so the fence's frames are spec-shaped rather than invented.
  </read_first>
  <action>
Extend `tests/client_sse_stream.rs`'s `RecordingServer` so a POST can be answered with a chunked
`text/event-stream` body that the test drives frame-by-frame, in addition to the GET behaviour plan 01
uses. Keep both behaviours; the existing six fences must not change shape.

Add three fences, all fully `tokio::time::timeout(BOUND, ..)`-wrapped:

1. `a_notification_on_a_post_sse_body_arrives_before_the_result_frame` — issue a request through the
   transport; answer the POST with `content-type: text/event-stream`; push ONE
   `event: message` frame carrying a JSON-RPC notification; then, WITHOUT closing the body, assert the
   notification is observable inside `BOUND`. Only after that assertion push the result frame and
   close. Against the unfixed tree the notification cannot arrive before the body ends, so this fence
   is RED by timeout.

2. `a_server_to_client_request_on_a_post_sse_body_is_parsed_before_the_result_frame` — same shape, but
   the pushed frame carries a JSON-RPC **request** (an `id` plus a method the client answers, e.g. a
   `roots/list` or `ping` shaped server→client request). Assert the client emits its answer as a new
   POST — observable through `RecordingServer::request_lines()` plus the recorded bodies — inside
   `BOUND`, while the original POST's stream is still open. This is the deadlock D-01 names: the client
   cannot answer a request it has not parsed yet.

3. `a_post_sse_reader_terminates_when_its_owning_transport_is_dropped` — the task-leak fence the
   cross-AI review asked for. Issue a request that is answered with a still-open `text/event-stream`
   POST body, then DROP every `StreamableHttpTransport` clone (and the `Client` wrapping it). Assert,
   within `BOUND`, that the RecordingServer observes the POST connection closed from the client side
   and that no further bytes are read from it after `QUIET`. The mechanism is plan 01's task-2 rule 3:
   dropping the last transport clone drops the `Receiver`, so the reader's next
   `sender.send(..).await` returns `Err` and the reader RETURNS. There is no `Drop` impl to rely on —
   `StreamableHttpTransport` is `Clone` and shares its abort handle, so a `Drop` impl would let one
   clone's drop kill the original's stream — which is exactly why the send-failure signal is the
   contract. If the POST reader does not consult its send result, this fence stays red.

Capture RED to `target/118.2-03-red.log`.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(client_sse_stream)' --features "full" 2>&1 | tee target/118.2-03-red.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run' &amp;&amp; grep -E '[1-9][0-9]* failed' target/118.2-03-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `tests/client_sse_stream.rs` contains all three new test function names
    - The run reports a NON-ZERO test count and at least 2 failed
    - The six fences from plan 01 still pass in the same run — `target/118.2-03-red.log` shows only the new tests failing
    - `RecordingServer` records POST bodies, so the second fence asserts on the client's actual answer rather than on a side effect
    - Fence 3 asserts a SERVER-OBSERVED connection close plus a quiet period, not merely that a handle went out of scope — read the test body and confirm
  </acceptance_criteria>
  <done>Three POST-stream fences exist, run, and are RED by timeout against the whole-body collect.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the shared reader at the POST-response SSE site and retire feed_complete_body</name>
  <files>src/shared/streamable_http.rs, src/shared/sse_parser.rs, tests/v2_bounded_reads_tripwire.rs</files>
  <read_first>
    - src/shared/streamable_http.rs — read `post_body` in full (`:1440-1720`), especially where `modified_body` is produced and which branches consume it, and the reader helpers plan 01 added.
    - src/shared/sse_parser.rs — read `feed_complete_body` (`:473`) and its full rustdoc including the "both call sites" precondition sentence and the "attractive nuisance" note; read `#[test] feed_complete_body_bypasses_the_bound` (`:1630`).
    - tests/v2_bounded_reads_tripwire.rs — read every entry whose `why` mentions `feed_complete_body`, plus the entries plan 01 added for `src/shared/streamable_http.rs`, plus `the_two_known_capped_whole_body_reads_are_found_and_classified_bounded` (`:746`).
  </read_first>
  <action>
**(a) The POST-response SSE branch.** In `post_body`, stop collecting the body whole when the response
`content-type` contains `text/event-stream`. Restructure so this branch is decided BEFORE the collect
that produces `modified_body`, and hand the live `hyper::body::Incoming` to the same reader plan 01
built: `SseReadState` + `read_next_sse_frame` + `drain_sse_events` + `sse_stream_overflow`, with the
parser built at `SseParser::with_max_buffer_size(self.max_collected_body_bytes)`.

Everything the deleted task did is preserved by the shared reader and must remain observable:
- `event.id` writes `self.last_event_id` and fires the paired `resumption_callback()`;
- the message-event filter (`event` absent or `"message"`);
- D-05 applies here too — a `parse_message` `Err` ENDS the stream with the same named, truncated-echo
  error the GET path uses, delivered to `receive()` through plan 01's `Result`-carrying channel. There
  must be exactly ONE story for a corrupt frame, not two;
- delivery is `sender.send(Ok(msg)).await` from the spawned reader task (D-04), not the caller-task
  `try_send` path — this is a spawned task, so awaiting capacity is correct here — and a send failure
  RETURNS from the reader (plan 01 task-2 rule 3: a failed send means the receiver was dropped). That
  return is what fence 3 measures.

**The response-middleware contract on this path (cross-AI review finding 7), same decision as the GET
path.** `post_body` currently runs `apply_response_middleware` over the COLLECTED body at `:1554`, and
this branch no longer has one. Adopt the contract this file already established: response-BODY
middleware is explicitly BYPASSED for a streaming response, exactly as `post_streaming` (`:1444`)
already documents for `subscriptions/listen` — *"the chain processes a complete `Vec<u8>` body, and a
stream has none by construction"* (`:1440-1443`). Reproduce that reasoning in a comment at the branch,
note that `process_response_headers` still runs so header-level middleware behaviour is unchanged, and
state the contract in `post_body`'s rustdoc. The JSON and non-SSE branches keep running the middleware
chain over their collected body — nothing about them changes. Consider routing this branch through
`post_streaming` itself if the restructure makes that natural, since it already encodes both the
bypass and the header processing; if it does not fit (the 401 retry and the 202 arm sit between), say
why in the SUMMARY rather than duplicating `post_streaming`'s body.

The JSON and non-SSE branches keep using the collected body and `collect_capped_body`. Do not remove
`collect_body_within_cap` or `collect_capped_body`.

Note the `else if status_code == StatusCode::ACCEPTED` and error branches that follow at `:1707+` —
re-read them after restructuring so the 202 handling plan 01 moved is not disturbed.

**(b) Retire `feed_complete_body`.** After (a) it has zero non-test callers. DELETE it, together with
all three co-located dependants:
1. delete `#[test] feed_complete_body_bypasses_the_bound` (`src/shared/sse_parser.rs:1630`);
2. rewrite (do not merely delete) the ALLOWLIST `why` prose in `tests/v2_bounded_reads_tripwire.rs`
   that cites `feed_complete_body`, so the justification describes what is actually there now — the
   incremental reader under the parser bound. A stale justification is worse than none: it asserts a
   mechanism that no longer exists;
3. remove the "both call sites" precondition sentence from the parser's module/rustdoc surface, and add
   one sentence of recorded history where the item was: the complete-body entry point existed to serve
   two whole-body collects in `StreamableHttpTransport`, both of which became incremental reads in
   Phase 118.2, and it was deleted rather than kept because a public unbounded parser entry point with
   no caller is exactly the attractive nuisance its own doc warned about.

If any other module still calls it, do NOT keep it "just in case" — report the caller and stop, because
that caller is an unbounded read this phase has not accounted for.

**(c) Re-verify the whole-body count.** `the_two_known_capped_whole_body_reads_are_found_and_classified_bounded`
must still find EXACTLY 2 `.collect().await` sites, both `Limited::new(`-wrapped. Removing call sites is
fine; the count is over the helper bodies. If the number moved, re-read `collect_body_within_cap`,
`collect_capped_body` and `jsonrpc_error_envelope` and report — do not adjust the assertion to match.

**(d) Re-measure the ALLOWLIST counts.** The second reader site may raise the `extend_from_slice(` /
`.extend(` counts for `src/shared/streamable_http.rs`, or may not if the helpers are shared. Re-run the
comment-excluding counts and update the entries. `report_dead_entry` fails if a count is set higher than
reality, so measure, do not estimate.
  </action>
  <verify>
    <automated>cargo nextest run -E 'binary(client_sse_stream)' --features "full" 2>&1 | tee target/118.2-03-green.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped' &amp;&amp; cargo nextest run -E 'binary(v2_bounded_reads_tripwire)' --features "full" 2>&1 | tee -a target/118.2-03-green.log | grep -E 'Summary.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: feed_complete_body, if let Ok(msg) -->
    - `grep -rv '^\s*//\|^\s*///' src/ tests/ 2>/dev/null | grep -c 'feed_complete_body'` returns 0 — comment-excluding, so a history note in prose cannot red it and cannot fake it either
    - `grep -v '^\s*//\|^\s*///' src/shared/streamable_http.rs | grep -c 'if let Ok(msg)'` returns 0 — both silent-drop sites are gone
    - `grep -v '^\s*//\|^\s*///' src/shared/streamable_http.rs | grep -c 'from_utf8_lossy'` returns 0 in the SSE reading paths (verify by reading; a lossy decode elsewhere on a non-SSE path may legitimately remain, and if so, name it in the SUMMARY)
    - `post_body`'s rustdoc states the streaming response-middleware bypass and cites `post_streaming` as the precedent — verify by reading, and quote it in the SUMMARY
    - `cargo nextest run -E 'binary(client_sse_stream)' --features "full"` reports a NON-ZERO test count with 0 failed — all NINE fences green (plan 01's six plus this plan's three)
    - `cargo nextest run -E 'binary(v2_bounded_reads_tripwire)' --features "full"` reports a NON-ZERO test count with 0 failed
    - `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` reports a NON-ZERO test count with 0 failed
    - `cargo nextest run -E 'binary(http_peer_roundtrip)' --features "full"` reports 12 run, 12 passed
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp --features "full"` and `RUSTFLAGS="-D warnings" cargo build -p pmcp --no-default-features --features full-v2` both exit 0
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>Both whole-body SSE collect sites are incremental reads through one shared reader, `feed_complete_body` is gone along with its test and its now-false prose, and every tripwire is green.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files this plan CREATES or DELETES (excluded from drift verification):

**New test functions** (appended to `tests/client_sse_stream.rs`)
- `a_notification_on_a_post_sse_body_arrives_before_the_result_frame`
- `a_server_to_client_request_on_a_post_sse_body_is_parsed_before_the_result_frame`
- `a_post_sse_reader_terminates_when_its_owning_transport_is_dropped`

**New harness capability** (in `tests/client_sse_stream.rs`)
- `RecordingServer` gains a POST-answered chunked `text/event-stream` mode with per-frame push and
  recorded POST bodies

**Deleted symbols**
- `SseParser::feed_complete_body` (`src/shared/sse_parser.rs`)
- `#[test] feed_complete_body_bypasses_the_bound` (`src/shared/sse_parser.rs`)

**Reused, not created** — `SseReadState`, `read_next_sse_frame`, `drain_sse_events`,
`sse_stream_overflow` and `CLIENT_RECEIVE_QUEUE_CAPACITY` are plan 01's artifacts; this plan adds a
second consumer, not a second definition.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → pmcp client (POST response SSE body) | A peer-controlled byte source that stays open across the whole tool call and can carry server-initiated REQUESTS |
| pmcp client lifetime → spawned POST reader task | A dropped transport must not leave a task reading a peer-controlled socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118.2-03-01 | Denial of Service | POST-response SSE reader | high | mitigate | Same parser bound and latching `overflowed()` check as the GET path (D-02), reached through the same four functions — one bound, one enforcement point, no second implementation to drift |
| T-118.2-03-02 | Denial of Service | in-tool elicitation over a POST stream | high | mitigate | Incremental parse means a server-to-client request is answerable before the response frame. The whole-body collect deadlocked this outright. Fenced by `a_server_to_client_request_on_a_post_sse_body_is_parsed_before_the_result_frame` |
| T-118.2-03-03 | Tampering | `SseParser::feed_complete_body` remaining reachable | medium | mitigate | Deleted rather than left `pub(crate)` with no caller. Its own rustdoc calls a public unbounded parser entry point "an attractive nuisance"; a caller-less unbounded entry point is a latent bypass of the D-02 bound |
| T-118.2-03-04 | Repudiation | stale ALLOWLIST justification | medium | mitigate | The tripwire `why` prose citing the retired complete-body entry point is REWRITTEN to describe the incremental reader, not deleted. A justification that describes a mechanism which no longer exists makes the next reviewer trust a bound that is not there |
| T-118.2-03-05 | Denial of Service | orphaned POST SSE reader tasks | medium | mitigate | The reader RETURNS when `sender.send(..)` fails, which is what happens the moment the last transport clone drops its `Receiver`. A long-lived POST stream therefore cannot outlive its client. Fenced by `a_post_sse_reader_terminates_when_its_owning_transport_is_dropped` |
| T-118.2-03-06 | Tampering | silently skipped response middleware on a streaming body | low | accept | Response-BODY middleware is DOCUMENTED as bypassed for a streaming response, matching `post_streaming`'s existing contract; `process_response_headers` still runs. Accepted rather than mitigated because the alternatives are unenforceable (prohibiting body-transforming middleware) or new public surface (a frame-aware chain), and the behaviour is now stated in `post_body`'s rustdoc rather than discovered by a user |
| T-118.2-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no packages installed |
</threat_model>

<verification>
- `cargo nextest run -E 'binary(client_sse_stream)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(v2_bounded_reads_tripwire)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(v1_severability_tripwire)' --features "full"` — non-zero count, 0 failed
- `cargo nextest run -E 'binary(http_peer_roundtrip)' --features "full"` — 12 run, 12 passed
- `grep -rv '^\s*//\|^\s*///' src/ tests/ | grep -c 'feed_complete_body'` — 0
- `make quality-gate`
</verification>

<success_criteria>
- Both SSE read sites go through ONE reader; `grep -c 'read_next_sse_frame' src/shared/streamable_http.rs` shows a single definition with two consumers
- The retired complete-body parser entry point and all three of its co-located dependants are gone in the same commit
- The streaming response-middleware contract is stated in `post_body`'s rustdoc, matching `post_streaming`'s
- A dropped transport provably terminates its POST SSE reader
- `target/118.2-03-red.log` / `target/118.2-03-green.log` form the RED→GREEN pair
</success_criteria>

<output>
Create `.planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-03-SUMMARY.md` when done.
Record: the `feed_complete_body` disposition and every co-located edit it dragged, the re-measured ALLOWLIST counts, and confirmation that the two-whole-body-reads assertion still finds exactly 2.
</output>
