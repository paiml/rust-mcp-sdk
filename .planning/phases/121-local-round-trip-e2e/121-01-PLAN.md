---
phase: 121-local-round-trip-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-openapi-server/Cargo.toml
  - crates/pmcp-openapi-server/tests/pmcp_package_pin.rs
  - crates/pmcp-openapi-server/tests/common/mod.rs
  - crates/pmcp-openapi-server/tests/parity_replay.rs
  - Makefile
autonomous: true
requirements: [PKG-04]

estimate:
  tokens: 52000
  raw_tokens: 104000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`make test-openapi-server` runs `cargo test -p pmcp-openapi-server -- --test-threads=1`, prints the summed test count, and exits non-zero when that sum is 0 (D-13)."
    - "`make test-openapi-server` additionally fails when any name in its `REQUIRED_TEST_BINARIES` list is absent from the captured cargo output, so a nonzero SUM cannot stand in for 'the phase's own integration binaries ran' (REVIEWS LOW — the summed count proves the package ran, not that a named binary ran)."
    - "`test-all`'s prerequisite list contains `test-openapi-server`, so `make quality-gate` executes `crates/pmcp-openapi-server/tests/` (D-13, RESEARCH CF-2)."
    - "`cargo test -p pmcp-openapi-server --test parity_replay -- --test-threads=1` still reports 3 passed and 1 ignored after the D-02 helper lift."
    - "`crates/pmcp-openapi-server/tests/common/mod.rs` exposes `tfl_env_lock`, `fixtures_dir`, `examples_dir`, `assert_london_tube_config_slots`, `assert_london_tube_code_mode_surface`, `mount_london_tube`, `DUMMY_APP_KEY`, and `EnvVarGuard` as `pub` items (D-02)."
    - "`common::EnvVarGuard` restores the prior value of an environment variable in its `Drop` impl — including when an assertion panics first — so no test in this crate's binaries can leak a mutated `TFL_*` variable into a sibling test (REVIEWS HIGH, cross-plan: plan 121-03's unfilled-slot negative consumes this guard and its `files_modified` does not include `common/mod.rs`, so the guard is created HERE)."
    - "`mount_london_tube` takes an `app_key: &str` second parameter and both of its `query_param` matchers use it, so two environments can mount the same backend under different credentials (D-12, RESEARCH CF-6)."
    - "The `pmcp-package` entry lives in `crates/pmcp-openapi-server/Cargo.toml`'s `[dev-dependencies]` table with the version requirement string `0.2`, and `tests/pmcp_package_pin.rs` reads that exact table and fails on any other string (D-03)."
    - "`cargo test -p pmcp-openapi-server --no-run` succeeds, proving a `[dev-dependencies]` path dep on the workspace-excluded `crates/pmcp-package` resolves (RESEARCH A2 — unexercised in this repo before this plan)."
    - statement: "No numeric precision contract applies to this plan — the only numeric quantity is the awk-summed integer test count, compared with an integer equality against a zero floor; no float, rounding, truncation, or overflow path exists."
      verification: backstop
  artifacts:
    - crates/pmcp-openapi-server/Cargo.toml
    - crates/pmcp-openapi-server/tests/pmcp_package_pin.rs
    - crates/pmcp-openapi-server/tests/common/mod.rs
    - Makefile
  key_links:
    - "`test-all` -> `test-openapi-server`: if the target exists but is not chained, every other truth in this phase is unexecuted by any gate and the phase ships a regression net nothing runs (RESEARCH CF-2)."
    - "`parity_replay.rs` -> `mod common;` -> `common::mount_london_tube(&backend, DUMMY_APP_KEY)`: if the existing call site is not updated to pass the constant, the currently-green parity test breaks."
    - "`common::EnvVarGuard` -> plan 121-03 Task 1's `degraded_env_b_unfilled_slot_is_reported`: 121-03 declares only `roundtrip_e2e.rs` and `deviation.rs` in `files_modified`, so it cannot create a shared guard without deviating from its declared artifacts. If this plan does not ship `EnvVarGuard`, 121-03 either falls back to the trailing-restore shape a failing assertion skips, or silently widens its artifact list."
    - "`Cargo.toml [dev-dependencies].pmcp-package` -> `pmcp_package_pin.rs`'s table lookup: a tripwire copied verbatim from `cargo-pmcp` reads `dependencies`, not `dev-dependencies`, and would panic on a CORRECT manifest."
  prohibitions:
    - "MUST NOT introduce a gate target that can report success while executing zero tests — the count guard is the mechanism, not decoration."
---

<objective>
Land the regression net and the risky edit together, before any new test exists.

This plan does three things in a deliberate order forced by locked decision D-02
("do the lift as its own commit, verified independently, before `roundtrip_e2e.rs`
is written") and by RESEARCH Pitfall 5 ("land the Makefile hook in the same commit
as the lift so the regression net exists before the risky edit, not after"):

1. Add the `pmcp-package` dev-dependency (D-01) and its pin tripwire (D-03), proving
   that a `[dev-dependencies]` path dep on the workspace-excluded crate resolves at all.
2. Add the `test-openapi-server` Makefile target with a nonzero-test-count guard and
   chain it into `test-all` (D-13). Today NOTHING executes
   `crates/pmcp-openapi-server/tests/` — not `make quality-gate`, not CI's `test` job,
   not `org-gate-checks.yml`'s `workspace-test` (RESEARCH CF-2, measured three ways).
3. Perform the D-02 helper lift into `tests/common/mod.rs`, parameterizing
   `mount_london_tube` by `app_key` (RESEARCH CF-6) — the one semantic edit in an
   otherwise mechanical move, and the one that can break a currently-green test — and
   add the shared panic-safe `EnvVarGuard` that plan 121-03 consumes but cannot create
   (its `files_modified` does not reach this file).

**Ordering note for the reviewer:** TRACER_MODE is on for this phase and the phase's
`type="tracer"` slice is `121-02-PLAN.md` Task 1 (the end-to-end pack-A/unpack-B/serve
slice). It cannot lead the phase, because locked decision D-02 requires the helper lift
to be its own independently-verified commit *before* `roundtrip_e2e.rs` is written, and
the tracer consumes the lifted helpers. This plan is the enabling slice that D-02 mandates.

Purpose: PKG-04's deliverable is a regression net. A regression net that no gate runs is
not a regression net. This plan makes the crate's tests executable by `make quality-gate`
*first*, so the rest of the phase is written inside a net rather than beside one.

Output: two new files (`tests/common/mod.rs`, `tests/pmcp_package_pin.rs`), two modified
files (`Cargo.toml`, `parity_replay.rs`), one new Makefile target chained into `test-all`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-local-round-trip-e2e/121-CONTEXT.md
@.planning/phases/121-local-round-trip-e2e/121-RESEARCH.md
@.planning/phases/121-local-round-trip-e2e/121-PATTERNS.md
@.planning/phases/121-local-round-trip-e2e/121-VALIDATION.md
@CLAUDE.md
</context>

<measured_baseline>
Measured by the planner on 2026-08-23, in this working tree, before any change:

- `cargo test -p pmcp-openapi-server -- --test-threads=1` -> **32 passed, 2 ignored,
  8 suites, exit 0.** The whole crate is green today, so chaining it into `test-all`
  cannot turn `make quality-gate` red on a pre-existing failure. If the executor's own
  baseline run disagrees with this, STOP and report — a pre-existing failure is a
  discovery that changes this plan, not something to work around.
- `cargo test -p pmcp-openapi-server --test parity_replay -- --test-threads=1` ->
  **3 passed, 1 ignored** (RESEARCH Summary, re-confirmed by the 8-suite run above).
- `make lint` is `cargo clippy --features full --lib --tests` with **no `-p`**
  (`Makefile:169`), so it resolves to the root `pmcp` package only. `pmcp-openapi-server`
  is NOT clippy-gated by the repo gate. `make fmt-check` IS `cargo fmt --all` and DOES
  reach this crate. Task 3 therefore runs `cargo clippy -p pmcp-openapi-server` explicitly
  as a task-level bar that is deliberately stricter than the repo gate; it does not change
  the gate.
- `check-todos` scans `src/` only (`Makefile:988`); `check-unwraps` is a no-op
  (`Makefile:992-995`); `unused-deps` is skipped (`Makefile:220-224`). None of them
  constrain files added by this phase.

**Correction to RESEARCH's `toml` recommendation (measured):** RESEARCH §Environment
Availability recommends `toml = "0.8"`. That is `crates/pmcp-package`'s pin, and
`pmcp-package` is its own standalone workspace. `pmcp-openapi-server` is a ROOT workspace
member, and every root-side consumer pins the 1.x line: root `Cargo.toml:131` `toml = "1.0"`,
`crates/pmcp-server-toolkit/Cargo.toml:27` `toml = "1"`, `cargo-pmcp/Cargo.toml:53`
`toml = "1.0"`. Decisively, the tripwire being copied — `cargo-pmcp/tests/pmcp_package_pin.rs`
— already compiles and passes against `toml = "1.0"`, so its `toml::from_str::<toml::Value>`
+ `.get()` + `.as_str()` calls are known-good on 1.x. Use `toml = "1"`. Do not add a second
major version of `toml` to the root workspace graph.
</measured_baseline>

<tasks>

<task type="auto">
  <name>Task 1: Add the pmcp-package dev-dep (D-01) and its pin tripwire (D-03)</name>

  <read_first>
    - `crates/pmcp-openapi-server/Cargo.toml` — the file being modified. Note that every
      existing `[dev-dependencies]` entry carries a `# Why:` comment; the two new entries must too.
      Note also `exclude = [".planning/", ".pmat/", "fuzz/", "tests/"]` at line 14 — `tests/`
      never ships in the crates.io tarball, so nothing added under `tests/` affects the
      published artifact.
    - `cargo-pmcp/tests/pmcp_package_pin.rs` — the file being copied. Read it in FULL,
      including the header section that enumerates what the tripwire does NOT cover.
    - `cargo-pmcp/Cargo.toml` — confirm both the `pmcp-package` pin spelling (line 87) and
      that `toml` is pinned on the 1.x line (line 53), which is the evidence the copied
      code compiles against `toml = "1"`.
    - `crates/pmcp-agent/Cargo.toml` (line 18) — the second precedent for the path-dep spelling.
    - `crates/pmcp-package/Cargo.toml` — confirm the crate's current version is `0.2.0`.
  </read_first>

  <files>crates/pmcp-openapi-server/Cargo.toml, crates/pmcp-openapi-server/tests/pmcp_package_pin.rs</files>

  <action>
Add two entries to `crates/pmcp-openapi-server/Cargo.toml`'s `[dev-dependencies]` table,
each with a `# Why:` comment matching the file's existing convention.

Entry one (per D-01): `pmcp-package` with the table form carrying `version = "0.2"` and
`path = "../pmcp-package"`. The version requirement string must be the bare caret `0.2` —
not `=0.2.0`, not `0.2.0`. Its `# Why:` comment must state that the caret spelling is
asserted verbatim by `tests/pmcp_package_pin.rs` (D-03) so a silent 0.2 to 0.3 drift fails
loudly rather than resolving to a version the round-trip E2E was never written against.

Entry two: `toml` pinned as `"1"`. Its `# Why:` comment must state that the D-03 tripwire
parses this manifest with a real TOML parser rather than grepping its text, and that the
1.x line is the root-workspace line (root `Cargo.toml:131`, `pmcp-server-toolkit` and
`cargo-pmcp` all on 1.x) — deliberately NOT `pmcp-package`'s `0.8`, because `pmcp-package`
is its own standalone workspace and this crate is a root member.

Prove resolution before writing any test: run `cargo test -p pmcp-openapi-server --no-run`.
RESEARCH A2 records that a `[dev-dependencies]` path dep on the workspace-EXCLUDED
`crates/pmcp-package` is unexercised in this repo — both existing precedents put it in
`[dependencies]`. If resolution fails, STOP and report; do not work around it.

Then create `crates/pmcp-openapi-server/tests/pmcp_package_pin.rs` by copying
`cargo-pmcp/tests/pmcp_package_pin.rs` with exactly these changes:

- The embedded-manifest const is renamed to `OPENAPI_SERVER_CARGO_TOML`; its
  `include_str!("../Cargo.toml")` argument is UNCHANGED (it resolves relative to the test
  file, i.e. `crates/pmcp-openapi-server/Cargo.toml`).
- `dependency_version_req` gains a `table: &str` parameter instead of hardcoding the
  `dependencies` table name, and the call site passes `dev-dependencies`. This is the
  change a mechanical copy drops: a copy left reading the regular-dependencies table
  panics with "has no [dependencies].pmcp-package" — loud, but on a CORRECT manifest.
  Every panic and assert message must name the table it actually read.
- `EXPECTED_PIN` stays the string `0.2`.
- Panic/assert message text: `cargo-pmcp Cargo.toml` becomes `pmcp-openapi-server Cargo.toml`;
  the requirement tag `CLI-04 / D-04b` becomes `PKG-04 / D-03`.
- The test function's doc comment must keep the source's explanation of WHY the caret
  spelling matters (an exact-equals form refuses every later 0.2.x patch; a fully-qualified
  three-component form reads like an exact pin to a human even though Cargo treats it as a
  caret) — that reasoning is the part a mechanical copy loses.
- Copy the source's "what this does NOT cover" header section, rewritten for this crate:
  it covers ONLY `pmcp-openapi-server`'s own dev-dependencies entry, and `cargo-pmcp`'s
  regular-dependencies entry is covered by the sibling tripwire. Without that sentence a
  reader assumes one file guards both.
  </action>

  <verify>
    <automated>cargo test -p pmcp-openapi-server --test pmcp_package_pin -- --test-threads=1</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test -p pmcp-openapi-server --no-run` exits 0.
    - `cargo test -p pmcp-openapi-server --test pmcp_package_pin -- --test-threads=1` exits 0
      and its summary line reports a test count of at least 1 (not 0).
    - `grep -c 'dev-dependencies' crates/pmcp-openapi-server/tests/pmcp_package_pin.rs` is
      at least 1, and the value passed to the table-name parameter at the call site is the
      string `dev-dependencies`.
    - Mutation proof that the tripwire has teeth: temporarily change the manifest's
      `pmcp-package` version requirement to the three-component string `0.2.0`, re-run
      `cargo test -p pmcp-openapi-server --test pmcp_package_pin`, confirm it FAILS **inside
      the test** (the failure message must be the tripwire's own assert text naming the
      expected pin — not a Cargo resolution error emitted before any test binary was built),
      then revert to `0.2` and confirm it passes again. Record both outcomes in the summary,
      including the verbatim failure message that proves which layer failed.
      **Do NOT mutate to `0.3`.** The path crate is version `0.2.0`, so a `0.3` requirement
      can be rejected during Cargo dependency resolution BEFORE the integration test is
      compiled or run — that would prove the dependency is incompatible, not that
      `pmcp_package_pin.rs` detected textual drift, leaving the tripwire itself unproven
      (REVIEWS MEDIUM). `0.2.0` resolves against the path crate (0.2.0 satisfies `^0.2.0`)
      while being textually different from the expected `0.2`, so it reaches the tripwire.
      `^0.2` is an equally valid alternative mutation for the same reason.
    - `toml` appears exactly once in `crates/pmcp-openapi-server/Cargo.toml` and its version
      requirement begins with `1`, not `0.`.
    - Both new manifest lines are preceded by a `# Why:` comment.
  </acceptance_criteria>

  <done>
    The dev-dep resolves, the tripwire passes on the correct manifest, and it has been
    demonstrated to FAIL on a drifted one.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the test-openapi-server Makefile target with a nonzero-test-count guard and chain it into test-all (D-13)</name>

  <precondition>
    `cargo test -p pmcp-openapi-server -- --test-threads=1` exits 0 on the current tree.
    The planner measured 32 passed / 2 ignored / exit 0 on 2026-08-23. Assert this before
    editing the Makefile; if the crate is red, chaining it into `test-all` turns the whole
    repo gate red and this task must halt and report instead.
  </precondition>

  <read_first>
    - `Makefile` lines 236-300 — the `test-tester` and `test-cargo-pmcp` targets in full,
      including the 23-line rationale comment block above `test-tester` that explains the
      gate's reach beyond the root package and why the count assertion is not ceremony.
    - `Makefile` line 577 — the `test-all` prerequisite list, the chain point.
    - `Makefile` lines 875-887 and 906 — the `pmcp-package-gate` sibling precedent and its
      chain into `quality-gate`, for the convention of naming which blind spot a target closes.
    - `crates/pmcp-openapi-server/tests/parity_replay.rs` lines 28-31 — the module doc that
      already prescribes single-threaded execution for this crate's tests.
  </read_first>

  <files>Makefile</files>

  <action>
Add a `.PHONY: test-openapi-server` target immediately after `test-cargo-pmcp`, structurally
identical to `test-tester` (`Makefile:262-274`) and `test-cargo-pmcp` (`Makefile:283-296`).

The body must follow the precedents exactly: capture the whole cargo invocation into a shell
variable, check the exit status FIRST and propagate it, echo the captured output, then sum
field 4 of every line beginning `test result:` with awk and fail with a red message when the
total is zero, else echo a green message including the count.

The cargo invocation is `$(CARGO) test -p pmcp-openapi-server -- --test-threads=1`, wrapped
in the same `RUST_LOG=$(RUST_LOG) RUST_BACKTRACE=$(RUST_BACKTRACE)` prefix both precedents use.
Both precedents redirect with `2>&1` into the captured variable; keep that, because cargo
prints its per-binary `Running tests/<name>.rs (...)` lines on stderr and the named-binary
check below reads them.

**Second required deviation from both precedents: a named-binary assertion.** A nonzero
summed count proves the PACKAGE ran; it does not prove any particular integration binary ran.
`cargo test -p` also sums unit, binary and doctest results, so a suite that stopped being
compiled — a renamed file, a `tests/` entry that silently stopped being a target — leaves the
total comfortably nonzero. Declare a `REQUIRED_TEST_BINARIES` shell list in the target body
holding, at this plan's point in the phase, exactly `parity_replay` and `pmcp_package_pin`;
iterate it and fail with a red message naming the missing binary when the captured output does
not contain that name. Comment the list stating that it is APPEND-ONLY across this phase and
that plan 121-02 Task 1 adds `roundtrip_e2e` to it when that binary first exists — a name
added before its binary exists turns the gate red for every commit in between.

**One required deviation from both precedents:** neither passes `-- --test-threads=1`. This
target MUST, because this crate's tests mutate the process-global `TFL_BASE_URL` and
`TFL_APP_KEY` environment variables and bind ephemeral ports, and `parity_replay.rs`'s own
module doc already prescribes single-threaded execution. State the reason in the target's
header comment.

The header comment must make the same argument the `test-tester` block makes, with this
crate substituted, and must cite the three measured paths that miss this directory:
`test-integration` is `cargo test --test '*'` with no `-p` so it resolves to the root `pmcp`
package (Makefile:241-243); CI's `test` job is root-scoped (ci.yml:733); and
`org-gate-checks.yml`'s `workspace-test` uses `--lib --bins`, which excludes `tests/`
(org-gate-checks.yml:73). It must also state the count-guard rationale in the precedents'
own terms: the failure this target exists to prevent is "the gate does not reach this crate",
and a run that selects zero tests exits 0 — reproducing exactly that hole while looking green.

Then append `test-openapi-server` to the `test-all` prerequisite list at `Makefile:577`.
`test-all` is already chained into `quality-gate` at `Makefile:892`, and CI's `quality-gate`
job runs `make quality-gate` and IS in `gate.needs`, so this makes the phase's deliverable
merge-blocking without promoting `workspace-test`.
  </action>

  <verify>
    <automated>make test-openapi-server</automated>
  </verify>

  <acceptance_criteria>
    - `make test-openapi-server` exits 0 and its final line reports a test count of at least
      30 (planner-measured baseline is 32 passed + the new pin test).
    - `grep -n '^test-all:' Makefile` prints a line containing `test-openapi-server`, proving
      it is in the `test-all` prerequisite list. Deliberately a static read of the Makefile:
      piping any build or test invocation into a downstream filter reports only that filter's
      exit status, so a failing build would be reported as a pass.
    - The count guard is proven to have teeth WITHOUT breaking the tree: pipe a canned
      cargo-style output containing no `test result:` line through the same awk program the
      target uses and confirm it prints `0`; then pipe one containing
      `test result: ok. 5 passed; 0 failed` and confirm it prints `5`. Record both outputs.
    - The named-binary guard is proven to have teeth WITHOUT breaking the tree: temporarily
      add a name that no binary in this crate carries to `REQUIRED_TEST_BINARIES`, run
      `make test-openapi-server`, confirm it FAILS with a message naming that missing binary,
      then remove it and confirm the target passes again. Record both outcomes.
    - `make test-openapi-server`'s successful output contains both `parity_replay` and
      `pmcp_package_pin`, and the target body's `REQUIRED_TEST_BINARIES` list contains exactly
      those two names at the end of this plan.
    - The target body contains the literal `--test-threads=1`.
    - The target's header comment cites all three of `Makefile:241-243`, `ci.yml:733`, and
      `org-gate-checks.yml:73` as the measured paths that miss this directory.
    - `grep -v '^#' Makefile | grep -c 'test-openapi-server'` is at least 3 (the `.PHONY`
      line, the target line, and the `test-all` prerequisite).
  </acceptance_criteria>

  <done>
    `make quality-gate` now reaches `crates/pmcp-openapi-server/tests/`, and a run that
    selected zero tests would fail rather than pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lift parity_replay.rs's six shared helpers into tests/common/mod.rs, parameterizing mount_london_tube by app_key, and add the shared RAII EnvVarGuard (D-02, D-12)</name>

  <reversibility rating="costly">
    Carried through from CONTEXT.md D-02. The lift edits `parity_replay.rs`, which is green
    today (3 passed, 1 ignored, measured). A botched extraction breaks a passing parity test,
    and until Task 2 landed, nothing would have caught it. This is why the lift is its own
    commit, verified independently, before `roundtrip_e2e.rs` exists.
  </reversibility>

  <read_first>
    - `crates/pmcp-openapi-server/tests/parity_replay.rs` — the source of the lift, read in
      FULL. Locate each item BY NAME and confirm its current line span before moving it;
      PATTERNS.md's cited ranges (43-63, 65-74, 76-141, 143-165, 281-310) were measured on
      2026-08-23 and may have shifted.
    - `crates/pmcp-package/tests/common/mod.rs` lines 1-12 — the header comment and the
      `#![allow(dead_code)]` with its stated reason, both to be mirrored.
    - `crates/pmcp-server-toolkit/tests/support/mod.rs` lines 1-2 — the terser form of the
      same allow, and the ancestor of `tfl_env_lock`.
    - `tests/common/mod.rs` (root `pmcp` package) lines 1-5 — the sentence stating that files
      under `tests/common/` are NOT compiled as their own test binaries.
    - `crates/pmcp-package/tests/negative.rs` line 17 — the in-tree convention for where the
      bare `mod common;` declaration goes (after the `use` block).
    - `crates/pmcp-openapi-server/src/dispatch.rs` lines 158-196 — **the source-of-truth analog
      for the new `EnvVarGuard`.** Read the doc comment above the struct in full: it states, in
      this repo's own words, that a trailing `remove_var` line is skipped on panic and leaks the
      variable into every later test in the binary. Copy that shape (`set`, `unset`, `previous:
      Option<String>`, restore in `Drop`) rather than inventing one.
    - `crates/pmcp-server-toolkit/tests/support/mod.rs` lines 45-105 — the toolkit's own
      `EnvVarGuard` (struct at line 66, `set`/`unset` at 71, `Drop` at 95), named by the
      `dispatch.rs` comment as the twin it could not reach. Read it to confirm the field and
      method names before choosing yours; matching the in-repo spelling is the point.
    - `crates/pmcp-server-toolkit/tests/base_url_expansion.rs` lines 155-200 —
      `env_var_guard_restores_prior_state_including_on_panic`, the in-repo proof shape for a
      guard of exactly this kind (both directions, `catch_unwind`, no panic-catching attribute).
      Plan 121-03 Task 1 is required to add its twin for the guard you create here, so the
      guard's API must support both the previously-set and previously-unset directions.
  </read_first>

  <files>crates/pmcp-openapi-server/tests/common/mod.rs, crates/pmcp-openapi-server/tests/parity_replay.rs</files>

  <action>
Create `crates/pmcp-openapi-server/tests/common/mod.rs`, move six items into it from
`parity_replay.rs`, and add ONE new item that `parity_replay.rs` never had. Five of the six
moves are mechanical (the move plus a `pub` on the item); the sixth is the one semantic edit
in this plan; the seventh item is new and exists because plan 121-03 needs it and cannot
create it (see item 7).

Module header, before any item: a doc comment stating (a) that files under `tests/common/`
are not compiled as their own test binaries and are consumed with a bare `mod common;`,
(b) which binaries share these helpers and why sharing matters, and (c) `#![allow(dead_code)]`
with its reason spelled out — each test binary uses a different subset, so items this module
exposes are legitimately unused in one of them. Without the allow, the lift produces dead-code
warnings in whichever binary uses the smaller subset.

The six items, by name:

1. `TFL_ENV_LOCK` (the `static`) and `tfl_env_lock()`. Move verbatim; the function becomes
   `pub`. Its existing doc comment must SURVIVE — it documents the concrete hazard it guards,
   that whichever environment-variable write lands last wins for both servers' assembly-time
   resolution. EXTEND it (do not replace it) with one added paragraph carrying RESEARCH
   Pattern 2's reasoning: moving this into `common/` gives each test binary its OWN static,
   and that is correct rather than a bug, because cargo runs each integration-test binary as
   its own process and the process environment is per-process, so the two binaries cannot
   interfere. Without that sentence a later reader "fixes" the per-binary mutex into
   something global and wrong.
2. `fixtures_dir()` — move verbatim, add `pub`. Its `CARGO_MANIFEST_DIR` expansion is a
   compile-time crate-level value, not a file-relative path, so it resolves identically from
   the new location.
3. `examples_dir()` — same treatment; keep its comment noting that `examples/` ships in the
   crates.io tarball while `tests/` does not.
4. `assert_london_tube_config_slots(...)` — move verbatim, add `pub`. Keep its full header
   comment; it explains that asserting the three-slot shape in ONE helper is what keeps the
   vendored fixture and the pointable example from drifting apart.
5. `assert_london_tube_code_mode_surface(...)` — move verbatim, add `pub`.
6. `DUMMY_APP_KEY` and `mount_london_tube` — **the one semantic edit.** `DUMMY_APP_KEY`
   becomes a `pub const` and moves alongside. `mount_london_tube` becomes
   `pub async fn mount_london_tube(server: &MockServer, app_key: &str)`, and BOTH of its
   `query_param("app_key", ...)` matcher sites take the new parameter instead of the
   constant. The canned response bodies are unchanged. Add a comment on the signature
   stating why the parameter exists: D-12 gives environments A and B their own backend on
   their own port with a DIFFERENT credential, and a hardcoded matcher would 404 every one
   of B's backend calls, turning SC3's scenario replay red for a reason unrelated to parity
   (RESEARCH CF-6).

7. **`pub struct EnvVarGuard` — the one NEW item, not a move.** A panic-safe RAII guard that
   saves an environment variable's previous value on construction and restores it in `Drop`.
   Model it on `crates/pmcp-openapi-server/src/dispatch.rs:163-196`: a `key: &'static str`
   and a `previous: Option<String>` field, an associated `set(key, value)` and an associated
   `unset(key)` constructor, and a `Drop` impl that re-sets the saved value when there was one
   and removes the variable when there was not. Give it `pub` visibility and a doc comment
   stating the hazard verbatim from the analog: a trailing restore line is SKIPPED when an
   earlier assertion panics, and the variable then leaks into every later test in the same
   binary. `parity_replay.rs` does not use it and must not be changed to use it — its existing
   deliberate no-restore behaviour is documented and out of scope for this lift.

   **Why it is created here rather than where it is used:** plan 121-03 Task 1's unfilled-slot
   negative removes `TFL_BASE_URL` mid-binary and needs this guard, but 121-03's `files_modified`
   is only `roundtrip_e2e.rs` and `deviation.rs` — it cannot add a shared item to `common/mod.rs`
   without deviating from its declared artifacts. Creating it here is the cross-plan resolution
   (REVIEWS HIGH, cross-plan defect).

   **Do NOT add a `#[test]` function for the guard to this file.** Items under `tests/common/`
   are compiled INTO each consuming test binary, so a `#[test]` here would be collected into
   `parity_replay`'s binary and break this task's own "exactly 3 passed, 1 ignored" acceptance
   criterion. The guard's behavioural proof lives in `roundtrip_e2e.rs` and is plan 121-03
   Task 1's responsibility.

In `parity_replay.rs`: delete the six moved items, add a bare `mod common;` after the `use`
block per the in-tree convention, add a `use common::{...}` importing exactly the names that
file still consumes, and update its single `mount_london_tube` call site (currently line 332)
to pass `DUMMY_APP_KEY` as the second argument so its behaviour is bit-identical.

**Prune newly-unused imports.** After the lift, `parity_replay.rs` no longer needs
`serde_json::json` or the four `wiremock` names (`Mock`, `MockServer`, `ResponseTemplate`,
and the `matchers` group) unless something still standing in that file uses them — check
each one and remove only the genuinely dead ones. `common/mod.rs` needs
`pmcp_server_toolkit::ServerConfig`, the JSON macro, and the `wiremock` names its two moved
items use.

Do NOT touch `contoso_m365_parity.rs`. It carries its own copies of `fixtures_dir` and
`examples_dir` (lines 60 and 66). D-02 names only `parity_replay.rs`'s helpers, and widening
a costly-reversibility edit to a second currently-green file buys no PKG-04 benefit. Record
the residual duplication in the summary as a follow-up for a later simplify pass.
  </action>

  <verify>
    <automated>cargo test -p pmcp-openapi-server --test parity_replay -- --test-threads=1 && cargo clippy -p pmcp-openapi-server --all-targets -- -D warnings && cargo fmt --all -- --check</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test -p pmcp-openapi-server --test parity_replay -- --test-threads=1` exits 0
      and reports exactly `3 passed` and `1 ignored` — the same numbers as the measured
      pre-change baseline. A different count in EITHER direction is a regression to report,
      not a result to accept.
    - `cargo clippy -p pmcp-openapi-server --all-targets -- -D warnings` exits 0 (this bar is
      deliberately stricter than `make lint`, which reaches only the root package).
    - `cargo fmt --all -- --check` exits 0.
    - `make test-openapi-server` exits 0 and still reports a count of at least 30.
    - `grep -c 'pub ' crates/pmcp-openapi-server/tests/common/mod.rs` is at least 8
      (five moved functions, one const, `mount_london_tube`, and `EnvVarGuard`).
    - `grep -c 'allow(dead_code)' crates/pmcp-openapi-server/tests/common/mod.rs` is exactly 1.
    - `grep -c 'impl Drop for EnvVarGuard' crates/pmcp-openapi-server/tests/common/mod.rs` is
      exactly 1, and `grep -c 'pub struct EnvVarGuard' <same file>` is exactly 1. The guard's
      `Drop` body must contain both a set-back branch and a remove branch — read it and confirm
      both are present; a `Drop` that only removes turns a previously-SET variable into an unset
      one, which is a different leak, not a fix.
    - `crates/pmcp-openapi-server/tests/common/mod.rs` declares no test functions. Verify by
      reading it; the machine check is the parity criterion above — items in this module compile
      into every consuming binary, so a test function added here inflates `parity_replay`'s count
      away from the required 3 passed / 1 ignored and that criterion goes red.
    - The `mount_london_tube` signature in `common/mod.rs` contains a second parameter of type
      `&str`, and `grep -c 'DUMMY_APP_KEY' crates/pmcp-openapi-server/tests/common/mod.rs` is
      at least 2 (the const definition and nothing hardcoded in the matchers — the matchers
      must reference the parameter).
    - `grep -v '^\s*//' crates/pmcp-openapi-server/tests/parity_replay.rs | grep -c 'fn mount_london_tube'`
      is 0 — the definition moved rather than being duplicated.
    - `grep -c 'mod common;' crates/pmcp-openapi-server/tests/parity_replay.rs` is exactly 1.
  </acceptance_criteria>

  <done>
    The six helpers live in `tests/common/mod.rs` as `pub` items, `mount_london_tube` takes
    an `app_key` parameter, the shared panic-safe `EnvVarGuard` exists for plan 121-03 to
    consume, and `parity_replay.rs` is byte-for-byte equivalent in behaviour — proven by the
    identical 3-passed/1-ignored result and a clean clippy run.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by THIS plan (a source-grounding pass must not flag these as
drift — they do not exist before this plan runs):

**New files**
- `crates/pmcp-openapi-server/tests/common/mod.rs`
- `crates/pmcp-openapi-server/tests/pmcp_package_pin.rs`

**New public items in `tests/common/mod.rs`** (the first seven lifted from `parity_replay.rs`,
now `pub`; the last is new to this repo location)
- `pub fn tfl_env_lock()`
- `pub fn fixtures_dir()`
- `pub fn examples_dir()`
- `pub fn assert_london_tube_config_slots(...)`
- `pub fn assert_london_tube_code_mode_surface(...)`
- `pub async fn mount_london_tube(server, app_key)` — NEW second parameter
- `pub const DUMMY_APP_KEY`
- `pub struct EnvVarGuard` with `EnvVarGuard::set`, `EnvVarGuard::unset` and its `Drop` impl —
  **NEW, not a move.** Modelled on `crates/pmcp-openapi-server/src/dispatch.rs:163-196` and the
  toolkit's `tests/support::EnvVarGuard`. Created here because plan 121-03 consumes it and its
  `files_modified` does not include this file.

**New items in `tests/pmcp_package_pin.rs`**
- `const OPENAPI_SERVER_CARGO_TOML`
- `const EXPECTED_PIN`
- `fn dependency_version_req(manifest, table, name)` — NEW `table` parameter vs. the analog
- `fn pmcp_package_pin_is_the_expected_caret_line()`

**New Makefile target**
- `test-openapi-server` (plus its entry in the `test-all` prerequisite list)

**New manifest entries**
- `crates/pmcp-openapi-server/Cargo.toml` `[dev-dependencies].pmcp-package`
- `crates/pmcp-openapi-server/Cargo.toml` `[dev-dependencies].toml`

Plans 121-02 and 121-03 add further symbols; see their own sections.
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active. ASVS level: **1**. Blocking threshold: **high**.
This is a test-and-build-wiring plan: it adds no network surface, parses no untrusted input,
and changes no production code path.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository working tree -> published crates.io tarball | `crates/pmcp-openapi-server/Cargo.toml:14` excludes `tests/`, so nothing added under `tests/` crosses this boundary. `[dev-dependencies]` do not reach downstream consumers. |
| developer machine -> `make quality-gate` output | A gate that reports green is a trust claim. A target that can pass while executing nothing is a false claim crossing this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-01-01 | Tampering | `test-openapi-server` Makefile target | high | mitigate | The awk-summed nonzero-test-count guard (Task 2), proven with two canned inputs in the acceptance criteria. Without it a zero-selecting run exits 0 and the gate makes a false green claim. |
| T-121-01-02 | Tampering | `crates/pmcp-openapi-server/Cargo.toml` dev-dep pin | medium | mitigate | The D-03 tripwire (Task 1), proven to FAIL on a drifted pin by the mutation check in the acceptance criteria. |
| T-121-01-03 | Repudiation | `parity_replay.rs` after the D-02 lift | medium | mitigate | The lift is its own commit and is verified against the measured 3-passed/1-ignored baseline plus a clean `clippy -D warnings` run, so a silent behaviour change cannot be attributed to a later commit. |
| T-121-01-04 | Tampering | the process environment shared by every test in one integration-test binary | high | mitigate | Task 3 ships `common::EnvVarGuard`, a panic-safe RAII restore modelled on `dispatch.rs:163-196`. The alternative — a trailing restore line — is SKIPPED when an earlier assertion panics, leaking a mutated `TFL_*` value into every later test in the binary and producing a green run against the wrong endpoint. Plan 121-03 Task 1 both consumes the guard and carries its `catch_unwind` restoration proof in both directions. |
| T-121-01-05 | Repudiation | `test-openapi-server`'s summed test count | medium | mitigate | The `REQUIRED_TEST_BINARIES` named-binary assertion (Task 2). A nonzero SUM proves only that the package ran; a suite that stopped being compiled leaves the total comfortably nonzero while its own truths go unexecuted. Proven with a deliberately-missing name in the acceptance criteria. |
| T-121-01-SC | Tampering | package-manager installs (`toml = "1"`) | low | accept | RESEARCH's Package Legitimacy Audit records that this phase resolves no new registry name. `toml` is already a resolved dependency of the root workspace (`Cargo.toml:131`), `pmcp-server-toolkit`, and `cargo-pmcp`, and is present in `Cargo.lock` at 1.1.2 — it is a version-line reuse, not a new name, so there is no slopsquat surface. `pmcp-package` is an in-repo path dep built from source. Accepted with that rationale; no legitimacy checkpoint is warranted. |
</threat_model>

<verification>
After all three tasks:

1. `cargo test -p pmcp-openapi-server -- --test-threads=1` exits 0 with a count at least
   equal to the measured baseline of 32 plus the new pin test.
2. `make test-openapi-server` exits 0 and prints a nonzero count.
3. `cargo test -p pmcp-openapi-server --test parity_replay -- --test-threads=1` reports
   exactly 3 passed, 1 ignored.
4. `cargo fmt --all -- --check` exits 0.
5. `cargo clippy -p pmcp-openapi-server --all-targets -- -D warnings` exits 0.
6. `make pmcp-package-gate` exits 0 (unchanged by this plan; run it to confirm the new
   `[dev-dependencies]` path edge did not disturb the excluded crate's standalone workspace).
</verification>

<success_criteria>
- The `pmcp-package` dev-dep resolves and is pinned to the caret `0.2`, machine-checked.
- `make quality-gate` executes `crates/pmcp-openapi-server/tests/`, and would fail rather
  than pass if it selected zero tests.
- The six helpers are shared, `mount_london_tube` is parameterized by credential, and
  `parity_replay.rs` is behaviourally unchanged.
- `common::EnvVarGuard` exists as a `pub` panic-safe RAII restore, so plan 121-03's
  environment-mutating negative has a guard to use instead of a trailing restore line.
- The gate fails on a missing NAMED test binary, not only on a zero summed count.
</success_criteria>

<output>
Create `.planning/phases/121-local-round-trip-e2e/121-01-SUMMARY.md` when done.
Record in it: the executor's own measured pre-change baseline counts; the three mutation-proof
outcomes (the `0.2.0` drifted pin failing INSIDE the test with its verbatim message, the awk
guard printing 0 then 5, and the missing-named-binary guard firing); the final
`REQUIRED_TEST_BINARIES` list; confirmation that `common::EnvVarGuard` exists with both `Drop`
branches and carries no `#[test]` function; and the `contoso_m365_parity.rs`
residual-duplication follow-up.
</output>
