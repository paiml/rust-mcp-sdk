---
phase: 126-workflow-to-skill-projection-sequentialworkflow-as-skill
plan: 07
type: execute
wave: 5
depends_on: ["126-03", "126-06"]
files_modified:
  - examples/s56_workflow_skill_projection.rs
  - Cargo.toml
  - .planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-16 / D-16a: `examples/s56_workflow_skill_projection.rs` exists with a matching `[[example]]` stanza carrying `required-features = [\"skills\", \"full\"]`, placed after the `c10_client_skills` stanza. The number is `s56` — `s45` is taken by `s45_tool_as_task_lifecycle`."
    - "ALWAYS/EXAMPLE: `cargo run --example s56_workflow_skill_projection --features skills,full` exits 0 and ASSERTS its invariants rather than merely printing them."
    - "D-16: the example demonstrates all four things in one runnable file — a workflow, its projected skill, the opt-in prepended prompt, and the gate warning."
    - "The whole phase passes `RUSTFLAGS=\"\" make quality-gate`, plus the three things that gate cannot run: PMAT complexity, the `workflow::sequential` doctest leg, and the example."
    - "`skills` is still absent from `default`, `full` and `full-v2`, proven by the tripwire that derives all three lists from `Cargo.toml` at test time."
    - "DEFERRALS ARE WRITTEN, NOT ASSUMED (REVIEWS fable (e)): `.planning/phases/126-.../deferred-items.md` EXISTS and restates Phase 125's WR-03, the six CONTEXT `<deferred>` items, the `make test-skills` doctest-leg gap (gemini Finding 5), and — if gate B answered `narrow` — the builder-reachability gap. The original plan asked a reviewer to CONFIRM a restatement that no task wrote."
    - "The example demonstrates the opt-in through the path a real server uses: under gate B's `add-builder-path` answer it uses `ServerCoreBuilder::with_workflow_skill_prepend`, not only hand-constructed handlers."
  artifacts:
    - examples/s56_workflow_skill_projection.rs
    - Cargo.toml
    - .planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md
  key_links:
    - "`scripts/run-example-builds.sh:150` runs `cargo build --all-features --examples`, so the gate BUILDS the example; it never RUNS it — the ALWAYS `cargo run --example` requirement is a manual step by construction."
    - "`s44_server_skills` keeps its scope as hand-authored skills; `s56` is the projection sibling, and `c10_client_skills` is the assert-then-print precedent that made Phase 125's example verifiable."
    - "An `[[example]]` stanza touches no `[features]` table, so `tests/v1_severability_tripwire.rs` stays green."
  prohibitions:
    - statement: "The example must ASSERT, not merely print — `s44`'s print-only habit is explicitly not the model here; `c10_client_skills`'s assert-then-print habit is."
      status: must_hold
      verification: "cargo run --example s56_workflow_skill_projection --features skills,full"
    - statement: "The number is `s56`, not `s45` — `Cargo.toml:713-717` already registers `s45_tool_as_task_lifecycle` and `examples/s45_tool_as_task_lifecycle.rs` exists."
      status: must_hold
      verification: "RUSTFLAGS=\"\" cargo build -p pmcp --features \"skills,full\" --examples"
    - statement: "`skills` must not be added to `full` or `full-v2`; the enumerated lists and `tests/v1_severability_tripwire.rs` stay untouched."
      status: must_hold
      verification: "RUSTFLAGS=\"\" cargo test -p pmcp --features \"full\" --test v1_severability_tripwire -- --test-threads=1"
    - statement: "`make test-fuzz` / `make validate-always` are not accepted as fuzz evidence anywhere in the phase record — they exit 0 having run nothing on stable."
      status: must_hold
      verification: "RUSTFLAGS=\"\" cargo test -p pmcp --features \"skills,streamable-http,http-client,testing\" --test skills_routing -- --test-threads=1"
    - statement: "`cargo nextest -E 'test(/.../)'` appears in no verify command — it silently selects zero tests and does not fail."
      status: must_hold
      verification: "RUSTFLAGS=\"\" make test-skills"
---

<objective>
Ship the runnable demonstration CLAUDE.md's ALWAYS requirements mandate, then run the three
verifications that no automated gate leg can perform, and close the phase against all six
success criteria.

Purpose: `make quality-gate` BUILDS examples but never runs them, does not run PMAT, and
cannot reach the `workflow::sequential` doctest. Those three gaps are structural, not
oversights — this plan is where they are closed by hand with recorded output.
Output: `examples/s56_workflow_skill_projection.rs` + its `[[example]]` stanza, and a recorded
manual-verification pass covering the example, PMAT complexity, and the nightly fuzz run.
</objective>

<planning_notes>
Spec-less probe fallback: SKIPPED (no `126-SPEC.md`; v2.7 has no `REQUIREMENTS.md`, so there
were no requirement IDs to derive predicates from). **No probe-derived predicates were
generated for this phase.** `must_haves` derive from ROADMAP SC-1..SC-6 and
`126-VALIDATION.md`'s Per-Task Verification Map.

`.planning/STATE.md` / `PROJECT.md` / `ROADMAP.md` are deliberately not `@`-referenced
(156/47/59 KB).

**Phase-closure decision recorded here rather than deferred silently.** Phase 125 left three
code-review findings open, of which WR-03 — a build-time `panic!` inside a `Result`-returning
`build()` at `src/server/builder.rs:1501` — names "the next phase of the v2.7 milestone" as its
suggested owner, i.e. this phase. **Phase 126 does NOT fix it.** D-15 requires only that the
projection itself never panics, which plans 126-01/02/04 satisfy, and a projected skill cannot
trip `validate_names` because name identity holds by construction — so this phase does not
worsen WR-03. Task 3 restates the deferral explicitly so it does not silently age out.
</planning_notes>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-CONTEXT.md
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-PATTERNS.md
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-06-SUMMARY.md
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-VALIDATION.md
@CLAUDE.md
</context>

<assumption_delta_decision>
**Decision: `no-change`.** The assumption-delta detector fired `detected: true` on three
`pluralization` signals — the terms `fallback`, `alongside` and `alternative`. All three are
lexical false positives: `fallback` is the D-15 slug fallback, `alongside` is spike 011's
positioning language, and `alternative` is the closing `## Server-accelerated alternative`
section heading. **No singular -> plural identity transition occurs in this phase.** Advisory,
never blocking.

Supplementary design invariant (unchanged): D-05 locks ONE byte-identical body serving both
surfaces. **This plan makes it demonstrable:** `s56` shows both surfaces side by side in one
runnable file and ASSERTS that the prompt's message [0] and the served skill bytes are the same
string — a third tripwire beside plan 126-02's `as_prompt_text() == body` and plan 126-06's
golden.
</assumption_delta_decision>

<review_dispositions>
Findings from `126-REVIEWS.md` that this plan closes:

| Finding | Severity | Disposition |
|---------|----------|-------------|
| fable (e) — no task writes the WR-03 restatement Task 3 asks a reviewer to confirm | LOW | **Fixed in Task 2.** `deferred-items.md` is now a real artifact in `files_modified`, written before the human-verify checkpoint reads it. The alternative the reviewer offered — dropping the confirmation — was rejected: WR-03 is a live finding and silently dropping its restatement is how it ages out. |
| 2 — the example would demonstrate the flag only through hand construction (fable) | HIGH (part) | **Fixed in Task 1 demonstration 4.** Under gate B's `add-builder-path` answer the example uses the builder path a real server uses, and states in a comment why that is the interesting one. Under `narrow` it uses hand construction AND prints the narrowed claim, so a reader is not misled about reachability. |
| gemini Finding 5 — the `workflow::sequential` doctest leg sits outside `make quality-gate` | LOW | **Recorded, not fixed.** Task 2 runs it explicitly and Task 3 confirms the count; the Makefile change is written into `deferred-items.md` as a named follow-up rather than dropped. |
</review_dispositions>

<artifacts_this_phase_produces>
Phase-126 new symbols — nothing below exists at HEAD; exclude from drift verification.

**Types (public):** `SkillProjection<'w>`, `ProjectionWarning`, `ProjectionWarningKind`, and
`ProjectionOutput` **iff** plan 126-01 Task 1's gate answered `output-struct`
**Methods (public):** `SequentialWorkflow::as_skill`, `SkillProjection::{new, with_tools, build}`,
`ProjectionWarning::{kind, step, tool, message}`,
`WorkflowPromptHandler::with_projected_skill_prepend`, and — **iff** plan 126-01 Task 2's gate
answered `add-builder-path` — `ServerCoreBuilder::with_workflow_skill_prepend` and
`ServerBuilder::with_workflow_skill_prepend`
**Functions (module-private, `src/server/skills/projection.rs`):** `slugify`, `fallback_slug`,
`yaml_double_quoted`, `render_body`, `render_frontmatter`, `render_context`,
`render_prompt_content`, `render_inputs`, `render_procedure`, `render_step`,
`render_data_source`, `render_closing`, `gate_check`, `sanitize_for_log`
**Fields (private):** `WorkflowPromptHandler::projected_skill_prepend: Option<String>` — the
ALREADY-RENDERED body, cached when the setter runs and never re-rendered per request (REVIEWS
fable (b)); plus `prepend_projected_skill: bool` on `ServerCoreBuilder` / `ServerBuilder` under
gate B
**Crate-private helper:** `WorkflowPromptHandler::projected_prepend`
**New files:** `src/server/skills/mod.rs` (git mv), `src/server/skills/projection.rs`,
`tests/golden/workflow_skill_projection.md`,
`fuzz/fuzz_targets/fuzz_workflow_projection.rs`, `examples/s56_workflow_skill_projection.rs`,
`.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md`
**New manifest stanzas:** `[[example]] name = "s56_workflow_skill_projection"` (`Cargo.toml`);
`[[bin]] name = "fuzz_workflow_projection"` (`fuzz/Cargo.toml`); matrix row
`- fuzz_workflow_projection` (`.github/workflows/fuzz.yml`)
**New test fns:** `fuzz_workflow_projection_is_registered_and_scheduled`
(`tests/skills_routing.rs`), the `projection::tests::*` suite, the `skills_integration.rs`
transcript and golden tests, and the `skills_routing.rs` wire tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: `examples/s56_workflow_skill_projection.rs` and its `[[example]]` stanza</name>
  <files>examples/s56_workflow_skill_projection.rs, Cargo.toml</files>
  <precondition>The example number `s56` is free: `Cargo.toml:713-717` registers `s45_tool_as_task_lifecycle`, the highest registered example is `s55_handler_logging` at `Cargo.toml:831`, and `examples/` also holds s46-s51. Re-confirm with `grep -c 's56_' Cargo.toml` returning 0 before writing the stanza.</precondition>
  <read_first>
    - `examples/s44_server_skills.rs:1-25` — the module-docs header shape: the run line, the numbered "what this example prints" list, and the long explanatory comment convention for anything surprising (its "A SEPARATE registry holding CLONES..." block is the model for explaining why `s56` constructs two prompt handlers).
    - `examples/c10_client_skills.rs` — the assert-then-print habit. `125-VERIFICATION.md` records that asserting is what made c10 verifiable; s44 only prints, and that is explicitly NOT the model here.
    - `Cargo.toml:703-711` — the `s44_server_skills` and `c10_client_skills` stanzas verbatim; the new stanza goes immediately after `c10_client_skills` to keep the skills examples contiguous.
    - `Cargo.toml:713-717` — the `s45_tool_as_task_lifecycle` stanza that makes `s45` unavailable.
    - `126-RESEARCH.md` Q9 — the five things the smallest runnable `s56` must cover, with the `refund_flow` fixture shape from spike 009.
    - `126-PATTERNS.md` § 10 `src/server/workflow/prompt_handler.rs:1020-1075` — the in-process handler construction the example needs for the D-04a demonstration.
    - `126-RESEARCH.md` Q3's WR-03 caveat — register projected skills via `.try_skills(...)` rather than `.skills(...)` where a duplicate is plausible, because `ServerBuilder::skills` surfaces failure as a `panic!` at `src/server/builder.rs:1501`.
  </read_first>
  <action>
Write the one runnable file that demonstrates the whole phase, and register it.

**Header.** Module docs in the `s44` shape: a title naming this as the workflow-to-skill
projection example, an explicit statement that `s44` remains the hand-authored-skills example
and this one is its projection sibling, the run line
`cargo run --example s56_workflow_skill_projection --features skills,full`, and a numbered list
of what it demonstrates.

**Fixture.** Build a `SequentialWorkflow` named `refund_flow` with a description, at least one
argument, two tool steps (one of which carries `.with_guidance(...)` and is backed by a tool
annotated destructive), a resource-only step, and one workflow-level `.instruction(...)`.
Reuse the shape spike 009's `build_workflow` uses — it exercises `PromptArg`, `StepOutput` with
and without a field, guidance and bindings in one object.

**Five demonstrations, each with an assertion before the print:**

1. **The projection and slugification (SC-1, SC-2, SC-5).** `let skill = wf.as_skill();` then
   `assert_eq!(skill.name(), "refund-flow")`,
   `assert_eq!(skill.as_prompt_text(), skill.body())`, and
   `assert_eq!(wf.as_skill().body(), skill.body())` — a second derivation of the same workflow
   producing the same bytes. Print the body afterwards.
2. **Registry pass-through (SC-4, in-process).** Register via
   `Skills::new().add(skill.clone()).into_handler()?`, read `skill://refund-flow/SKILL.md`
   back, and assert byte-identity. Print the first entry's digest from the registry's entries.
   Where a duplicate is plausible, prefer `.try_skills(...)` over `.skills(...)` — the latter
   surfaces failure as a `panic!` at `src/server/builder.rs:1501`, Phase 125's still-open WR-03.
3. **The gate warning (SC-6).** `SkillProjection::new(&wf).with_tools([...])` supplying a
   `crate::types::ToolInfo` whose annotations are built with
   `ToolAnnotations::new().with_destructive(true)` — never struct-literal syntax, the type is
   `#[non_exhaustive]` — then `.build()?`. Assert exactly one warning of the gate kind naming
   the guidance-bearing step, and print all returned warnings.
4. **The opt-in prepend (D-04a) — demonstrated through the path a real server uses (REVIEWS
   finding 2).** Construct the handler twice over the same workflow, once with the prepend on
   and once without, call `PromptHandler::handle` in-process on each, and assert that the
   flagged transcript's message [0] text equals `skill.body()` while the unflagged one's
   message [0] is the user-intent message. Follow `s44`'s habit of explaining a duplication in
   a long comment rather than silently doing something surprising — say in a comment why two
   handlers are constructed.

   **Which construction to show depends on gate B's answer (read `126-05-SUMMARY.md`).** Under
   `add-builder-path`, build the ON case through
   `Server::builder() ... .with_workflow_skill_prepend(true).prompt_workflow(wf.clone())?` — the
   API a reader will actually copy — and add a comment noting that the setter must precede
   `prompt_workflow` because the flag is read at registration time. The review's specific
   objection was that demonstrating the feature only through hand-constructed
   `WorkflowPromptHandler::new(...)` teaches a path real servers do not use. Under `narrow`,
   use hand construction and print an explicit line stating that the opt-in currently requires
   direct handler construction, so the example does not imply a reachability the crate does not
   have.
5. **Print a short closing summary** naming which success criterion each assertion covered, so
   a reader running the example can see what was proven.

`main` returns `Result<(), Box<dyn std::error::Error>>` like `s44`'s. No `unwrap()` on anything
that can legitimately fail.

**Stanza.** Add to `Cargo.toml`, immediately after the `c10_client_skills` stanza:
`[[example]]` with `name = "s56_workflow_skill_projection"`,
`path = "examples/s56_workflow_skill_projection.rs"`, and
`required-features = ["skills", "full"]`. Both features are required — `skills` for the
projection and `full` for the handler and tool plumbing — matching s44 and c10.

An `[[example]]` stanza touches no `[features]` table, so `tests/v1_severability_tripwire.rs`
stays green. Do NOT add `skills` to `full` or `full-v2` to make the example build.
  </action>
  <acceptance_criteria>
    - `examples/s56_workflow_skill_projection.rs` exists and its module docs carry the run line `cargo run --example s56_workflow_skill_projection --features skills,full`.
    - `Cargo.toml` contains an `[[example]]` stanza with `name = "s56_workflow_skill_projection"` and `required-features = ["skills", "full"]`, located after the `c10_client_skills` stanza.
    - `grep -c 's45_workflow_skill_projection' Cargo.toml examples/ -r` returns 0 — the D-16a number correction was applied.
    - The example contains at least 6 `assert!` / `assert_eq!` calls covering SC-1, SC-2, SC-4, SC-5, SC-6 and D-04a.
    - Under gate B's `add-builder-path` answer, `grep -c 'with_workflow_skill_prepend' examples/s56_workflow_skill_projection.rs` returns at least 1 — the D-04a demonstration goes through the builder path a real server uses (REVIEWS finding 2). Under `narrow`, the example instead prints an explicit line stating that the opt-in requires direct handler construction.
    - `grep -c 'ToolAnnotations {' examples/s56_workflow_skill_projection.rs` returns 0.
    - `git diff Cargo.toml` shows no change to the `default`, `full` or `full-v2` feature lists.
    - `cargo run --example s56_workflow_skill_projection --features skills,full` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -o pipefail -c 'RUSTFLAGS="" cargo build -p pmcp --features "skills,full" --example s56_workflow_skill_projection 2>&amp;1 | tail -5'</automated>
    <fails_when>non-zero exit, or the output contains `no example target named` (the stanza is missing or misnamed) or any line beginning `error`</fails_when>
    <automated>RUSTFLAGS="" cargo run --example s56_workflow_skill_projection --features skills,full</automated>
    <fails_when>non-zero exit, or the output contains `assertion` and `left`/`right` (an assertion fired), or the output contains no line naming a success criterion (the closing summary is missing, meaning the example printed without proving anything)</fails_when>
    <automated>RUSTFLAGS="" cargo test -p pmcp --features "full" --test v1_severability_tripwire -- --test-threads=1</automated>
    <fails_when>non-zero exit, or the summary contains `running 0 tests` or `0 passed` — this tripwire derives `full`/`full-v2`/`default` from `Cargo.toml` at test time, so a green run with a nonzero count proves the stanza did not touch a feature list</fails_when>
  </verify>
  <done>One runnable file demonstrates the workflow, its projected skill, the registry pass-through, the gate warning and the opt-in prepended prompt, asserting each rather than printing it, registered under the correct `s56` number with the feature lists untouched.</done>
</task>

<task type="auto">
  <name>Task 2: `deferred-items.md`, then the full quality gate plus the three things it cannot run</name>
  <files>.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md, examples/s56_workflow_skill_projection.rs</files>
  <precondition>`pmat` 3.15.0, the nightly toolchain and `cargo-fuzz` are installed. `cargo-deny`, `cargo-audit` and the `unused-deps` tooling that `make quality-gate` chains were UNVERIFIED at research time — if any is absent, `purity-check` / `audit` / `unused-deps` will fail for reasons unrelated to this phase. Establish that by running `RUSTFLAGS="" make quality-gate` once and triaging any failure against `git stash`ed state before attributing it to phase 126.</precondition>
  <read_first>
    - `126-VALIDATION.md` § Manual-Only Verifications — the three-row table naming exactly what `make quality-gate` cannot run and the exact command for each.
    - `126-VALIDATION.md` § Test Infrastructure "Command traps" — the five measured traps, especially that `make test-unit` / `test-integration` / `test-property` / `test` all pin `--features "full"` and exit 0 having run zero skills tests.
    - `126-RESEARCH.md` Q11 landmine #11 — the seventeen legs `make quality-gate` chains, so a failure can be triaged to the right leg.
    - `126-RESEARCH.md` Q7 § Fuzz — the measured stable-toolchain `cargo fuzz run` failure and why `make test-fuzz`'s `|| echo` makes it worthless as evidence.
    - `.planning/phases/125-sep-2640-conformance-skills-list-skills-get/deferred-items.md` — the in-repo precedent for the file this task writes: read its heading structure, its per-item fields, and the WR-03 entry verbatim so the 126 restatement matches rather than paraphrases.
    - `126-01-SUMMARY.md` and `126-05-SUMMARY.md` — the two decision-gate answers (`build()` return shape; builder reachability), which determine what this file must record.
  </read_first>
  <action>
**0. Write `deferred-items.md` FIRST (REVIEWS fable (e)).** Task 3's human-verify checkpoint
asks a reviewer to confirm that WR-03's deferral "is restated in `deferred-items.md`" — and in
the original plan set **no task in any plan wrote that file**, so the confirmation could only
ever fail or be waved through. Write it before the gate run, at
`.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md`,
matching phase 125's file structure. It must carry:

1. **Phase 125 WR-03** — the build-time `panic!` inside a `Result`-returning `build()` at
   `src/server/builder.rs:1501`. Restate why phase 126 does not fix it (D-15 requires only that
   the projection never panics, and a projected skill cannot trip `validate_names` because name
   identity holds by construction) and that phase 126 does not worsen it. Note that `s56` uses
   `.try_skills(...)` for exactly this reason.
2. **The six CONTEXT `<deferred>` items** — the tri-surface decision-matrix docs
   (implementation-order item 25), `[[skills]]` digest pins on `AgentPackage` (item 24), making
   the D-04a prepend default-on, `.strict(true)` on the builder, a provenance frontmatter key,
   and freezing the render as a semver-observable contract.
3. **The doctest-leg gap (REVIEWS gemini Finding 5)** — `as_skill()`'s doctest lives at a
   `workflow::sequential` path, so `make test-skills` selector 2's `--doc skills` substring
   filter misses it, and `test-doc` inside `test-all` pins `--features "full"` under which the
   method does not exist. This phase runs it by hand (item 3 below); extending `make
   test-skills` or `make doc-check` to a `skills,full` doctest pass is a Makefile change for a
   later phase.
4. **The builder-reachability gap, IF AND ONLY IF gate B answered `narrow`** — copy
   `126-05-SUMMARY.md`'s narrowed-claim paragraph verbatim. If gate B answered
   `add-builder-path`, record instead that the gap was CLOSED in this phase and name the two
   setters, so a later reader does not re-open a resolved item.

An item recorded nowhere is an item that ages out; that is the whole reason this file exists
rather than a sentence in a SUMMARY.

Then run the gate and close the three structural gaps by hand, recording the output.

**1. `RUSTFLAGS="" make quality-gate`.** Prefix `RUSTFLAGS=""` — CI exports it and local shells
do not, and a recorded project memory lists that mismatch as a class of CI failure a local gate
run silently passes through. If a leg fails, triage it against the unmodified tree before
attributing it to this phase: `check-release-coverage`, `audit`, `unused-deps`, `purity-check`
and `comply` all depend on external tooling whose presence was unverified at research time.

**2. `pmat quality-gate --fail-on-violation --checks complexity`.** PMAT is CI-only by Phase 75
D-07 and is NOT in `make quality-gate`. The measured baseline is `Total violations: 0` in
`src/`, so ANY `src/` violation is attributable to this phase and blocks the PR. If one appears,
apply one of the six refactor techniques from `.planning/phases/75-fix-pmat-issues/75-RESEARCH.md`;
`render_step` and `gate_check` are the two functions most likely to be over.

**3. `RUSTFLAGS="" cargo test -p pmcp --features "skills,full" --doc sequential -- --test-threads=1`.**
No gate leg runs `as_skill()`'s doctest: selector 2 filters on the substring `skills` and this
doctest lives at a `workflow::` path, while `test-doc` inside `test-all` pins `--features
"full"` under which the method does not exist. Record the passing count.

**4. `cargo run --example s56_workflow_skill_projection --features skills,full`.**
`scripts/run-example-builds.sh:150` runs `cargo build --all-features --examples`, so the gate
BUILDS the example but never RUNS it. Record the full stdout.

**5. `cd fuzz && cargo +nightly fuzz run fuzz_workflow_projection -- -runs=200000
-max_total_time=30`.** The `+nightly` is mandatory — `cargo fuzz run` on stable fails with
`error: the option 'Z' is only accepted on the nightly compiler`, and `make test-fuzz` swallows
that with `|| echo` and exits 0. Record the executed-run count from the output. Do not cite
`make test-fuzz` or `make validate-always` as fuzz evidence anywhere in the phase record.

Paste the actual output of items 2 through 5 into the plan SUMMARY. A recorded command with no
recorded output is a claim, not evidence.
  </action>
  <acceptance_criteria>
    - `.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md` exists, is git-tracked, and contains a WR-03 entry naming `src/server/builder.rs:1501`, all six CONTEXT `<deferred>` items by name, the doctest-leg gap, and an explicit statement of gate B's builder-reachability outcome (closed or deferred).
    - `RUSTFLAGS="" make quality-gate` exits 0, with any triaged pre-existing tooling failure documented in the SUMMARY together with the evidence that it predates this phase.
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0 with no `src/` violation.
    - The `--doc sequential` command reports a nonzero passing count, recorded in the SUMMARY.
    - The example's full stdout is recorded in the SUMMARY.
    - The nightly fuzz run's output, including its executed-run count, is recorded in the SUMMARY.
    - The SUMMARY states explicitly that `make test-fuzz` was NOT used as fuzz evidence.
  </acceptance_criteria>
  <verify>
    <automated>bash -o pipefail -c 'git ls-files -- .planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md | grep -c .'</automated>
    <fails_when>output is `0`, or non-zero exit — the deferrals file must exist and be tracked before Task 3's checkpoint asks a reviewer to read it</fails_when>
    <automated>bash -o pipefail -c 'grep -c "builder.rs:1501" .planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md'</automated>
    <fails_when>output is `0`, or non-zero exit — WR-03 must be restated with its file:line, not merely alluded to</fails_when>
    <automated>RUSTFLAGS="" make quality-gate</automated>
    <fails_when>non-zero exit, or the output contains `reported 0 tests` from any `test-skills` selector guard, or any leg name is followed by a `✗` marker</fails_when>
    <automated>pmat quality-gate --fail-on-violation --checks complexity</automated>
    <fails_when>non-zero exit, or any reported violation has a path starting with `src/` — the measured baseline is `Total violations: 0`</fails_when>
    <automated>bash -o pipefail -c 'cd fuzz &amp;&amp; cargo +nightly fuzz run fuzz_workflow_projection -- -runs=200000 -max_total_time=30 2>&amp;1 | tail -20'</automated>
    <fails_when>non-zero exit, or the output contains `the option 'Z' is only accepted on the nightly compiler` (the `+nightly` prefix was dropped), or the output contains `panicked at`, or the output reports fewer than 1000 executed runs (the fuzzer never got going)</fails_when>
  </verify>
  <done>`deferred-items.md` exists and is tracked, carrying WR-03 with its file:line, all six CONTEXT deferrals and the doctest-leg gap; the full gate is green; PMAT complexity is at its zero-violation baseline; and the three structurally ungateable verifications — the `workflow::sequential` doctest, the example run, and the nightly fuzz execution — have each been performed with output recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — all six success criteria, the review resolutions, and the deferrals that must not age out</name>
  <what-built>
The complete phase-126 surface: `SequentialWorkflow::as_skill()`, the `SkillProjection`
builder with `ProjectionWarning`, the deterministic renderer in
`src/server/skills/projection.rs`, the D-14 golden at
`tests/golden/workflow_skill_projection.md`, the fuzz target
`fuzz/fuzz_targets/fuzz_workflow_projection.rs`, the opt-in prompt prepend on both handler
kinds, and the runnable example `examples/s56_workflow_skill_projection.rs`.

Task 2 has already run `make quality-gate`, PMAT complexity, the `workflow::sequential`
doctest leg and the nightly fuzz execution, with output recorded in the SUMMARY. Nothing here
requires the reviewer to run a build.
  </what-built>
  <how-to-verify>
Run the example and read its output:

```
cargo run --example s56_workflow_skill_projection --features skills,full
```

Then confirm each success criterion against the recorded evidence:

**SC-1 (slugification owned by the projection).** The example prints a skill named
`refund-flow` for a workflow named `refund_flow`, and the wire test in `tests/skills_routing.rs`
shows the final URI segment equals the frontmatter `name`.

**SC-2 (determinism).** The example asserts a second derivation is byte-equal, the property test
does 100 fresh re-derivations including template bindings, and `tests/golden/workflow_skill_projection.md`
pins the bytes against the last release.

**SC-3 (full coverage).** The projection unit suite asserts each workflow fact with its own
named assertion; the two D-11 exclusions are pinned absent over a workflow that sets both to
non-default.

**SC-4 (registry pass-through).** `tests/skills_routing.rs` shows a `skills/list` entry with
two-key verbatim frontmatter and a `sha256:` + 64-lowercase-hex manifest whose `size` matches
the served body, and `resources/read` returning those exact bytes over a loopback server.

**SC-5 (dual-surface invariant).** `as_prompt_text() == body` holds with anti-vacuity guards,
and the rendered Procedure's tool-name set EQUALS `wf.steps().filter_map(WorkflowStep::tool)`
in both directions.

**SC-6 (projection-time gate warning).** The example prints exactly one gate warning naming the
guidance-bearing destructive step, and a distinct unverifiable note appears when annotations
are missing.

**Cross-AI review resolutions to confirm landed (`126-REVIEWS.md`):**

- **Frontmatter is encoded, not concatenated (finding 1).** `tests/golden/workflow_skill_projection.md`'s
  second and third lines are `name: "refund-flow"` and
  `description: "Process a customer refund: policy #7"` — quoted and escaped. The projection
  unit suite carries `prop_frontmatter_roundtrips` plus ten named adversarial cases, and
  `tests/skills_routing.rs` proves it on the wire for a description containing a newline.
- **The `build()` return shape** matches plan 126-01 Task 1's gate answer, and the deviation
  from D-02's literal `Result<Skill>` is stated in `126-04-SUMMARY.md`, the `build()` rustdoc
  and the CHANGELOG.
- **SC-6 has ONE delivery channel** — `build()`'s structured return — and both `as_skill()`'s
  and `SkillProjection::new`'s rustdocs say so. No source text claims otherwise.
- **The prepend is reachable** from `prompt_workflow` (gate B answered `add-builder-path`), or
  the narrowed claim appears in the module docs, the CHANGELOG, `deferred-items.md` AND the
  example's own output (gate B answered `narrow`). Confirm whichever shipped is consistent
  across all four places.
- **The fuzz target chunks bytes before decoding**, so a `U+FFFD` char-boundary panic cannot be
  misreported as a projection defect.

**Deferrals to confirm explicitly rather than let age out — read
`.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md`,
which Task 2 wrote:**

1. **Phase 125 WR-03 is NOT fixed here.** The build-time `panic!` inside a `Result`-returning
   `build()` at `src/server/builder.rs:1501` remains open. D-15 requires only that the
   projection never panics, and a projected skill cannot trip `validate_names` because name
   identity holds by construction — so this phase does not worsen it. Confirm the entry is in
   that file with its file:line, not merely mentioned in a SUMMARY.
2. **Everything in CONTEXT.md's `<deferred>` list stayed out:** the tri-surface decision-matrix
   docs (item 25), `[[skills]]` digest pins on `AgentPackage` (item 24), making the D-04a
   prepend default-on, `.strict(true)` on the builder, a provenance frontmatter key, and
   freezing the render as a semver-observable contract. Confirm none of them appears in the
   diff, and that all six are listed in `deferred-items.md`.
3. **The doctest-leg gap** (no `make` leg reaches `as_skill()`'s doctest) is recorded as a
   named follow-up rather than dropped.
  </how-to-verify>
  <recommendation>Approve if the example exits 0 with all assertions passing and the six criteria are each backed by a recorded command output. Reject if any criterion rests on a command that reported zero tests, on `make test-fuzz`, or on a `--features "full"` invocation naming a skills test — all three are measured false-green shapes in this repo.</recommendation>
  <resume-signal>Type `approved`, or name the criterion whose evidence is missing or unconvincing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate output -> phase-completion claim | A green command that ran zero tests is the measured false-green shape in this repo; the completion claim rests on counts, not exit codes. |
| example -> reader | `s56` is the artefact a server author copies. What it demonstrates becomes the pattern they adopt. |
| deferred findings -> next phase | WR-03 and the six `<deferred>` items must leave the phase visibly, not by omission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-10 | Repudiation | Fuzz / example / doctest coverage claimed but never executed | medium | mitigate | Each of the three ungateable verifications requires its actual output pasted into the SUMMARY, and the fuzz `<fails_when>` rejects a run reporting fewer than 1000 executed runs. `make test-fuzz` is explicitly rejected as evidence because its `\|\| echo` exits 0 having run nothing on stable |
| T-126-18 | Spoofing | A green verify command that measured nothing | medium | mitigate | Every `<fails_when>` in the phase names the zero-tests signal (`running 0 tests` / `0 passed`) alongside the exit code, and no verify command uses `--features "full"` for a skills test or `cargo nextest -E 'test(/.../)'`, both of which are measured false-green shapes here |
| T-126-19 | Elevation of Privilege | `s56` teaching an unsafe registration pattern | low | mitigate | The example prefers `.try_skills(...)` over `.skills(...)` where a duplicate is plausible, because the latter surfaces failure as a `panic!` at `src/server/builder.rs:1501` (Phase 125 WR-03, still open). A reader copying the example copies the safe form |
| T-126-20 | Repudiation | Silent scope creep via the deferred list | low | mitigate | Task 3 enumerates all six `<deferred>` items plus WR-03 and requires confirming none appears in the diff |
| T-126-26 | Repudiation | A confirmation asking a reviewer to check an artifact nobody wrote | medium | mitigate | **REVIEWS fable (e).** The original Task 3 asked the reviewer to confirm WR-03 was "restated in `deferred-items.md`" while no task in any of the seven plans created that file — a checkpoint that can only be waved through is worse than no checkpoint. Mitigation: Task 2 writes the file, it is in `files_modified`, and two `<automated>` verifies assert it exists, is git-tracked, and names `builder.rs:1501` |
| T-126-SC | Tampering | Supply chain / package installs | low | accept | This plan installs no packages. Its only manifest edit is an `[[example]]` stanza, which declares no dependency and touches no `[features]` table. `126-RESEARCH.md` § Package Legitimacy Audit records that the whole phase adds zero dependencies, so no legitimacy checkpoint is required |

ASVS level 1; block on high. No high or critical threat: this plan adds an example and runs
verifications. All threats identified across the phase (T-126-01 through T-126-20) are
mitigated, transferred or explicitly accepted with rationale; none is open at high severity.
</threat_model>

<verification>
- `RUSTFLAGS="" make quality-gate` exits 0.
- `pmat quality-gate --fail-on-violation --checks complexity` reports zero `src/` violations.
- `RUSTFLAGS="" cargo test -p pmcp --features "skills,full" --doc sequential -- --test-threads=1` passes with a nonzero count.
- `cargo run --example s56_workflow_skill_projection --features skills,full` exits 0 with every assertion passing.
- `cd fuzz && cargo +nightly fuzz run fuzz_workflow_projection -- -runs=200000 -max_total_time=30` completes with no crash and a recorded run count.
- `RUSTFLAGS="" cargo test -p pmcp --features "full" --test v1_severability_tripwire -- --test-threads=1` passes.
</verification>

<success_criteria>
All six ROADMAP success criteria (SC-1 slugification, SC-2 determinism, SC-3 full coverage,
SC-4 registry pass-through on the wire, SC-5 dual-surface invariant, SC-6 projection-time gate
warning) are backed by a recorded command output; CLAUDE.md's four ALWAYS requirements (fuzz,
property, unit, `cargo run --example`) plus doctests are each discharged by an owning artefact;
and the seven deferred items leave the phase visibly restated rather than by omission.
</success_criteria>

<output>
Create `.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-07-SUMMARY.md` when done
</output>
