---
phase: 126-workflow-to-skill-projection-sequentialworkflow-as-skill
plan: 06
type: execute
wave: 4
depends_on: ["126-04", "126-05"]
files_modified:
  - tests/golden/workflow_skill_projection.md
  - tests/skills_integration.rs
  - CHANGELOG.md
  - src/server/skills/projection.rs
  - src/server/skills/mod.rs
  - src/server/workflow/sequential.rs
autonomous: true
requirements: [SC-2, SC-3]
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-14 / SC-2 (golden half): `tests/golden/workflow_skill_projection.md` holds the exact rendered bytes of a fixed fixture workflow, consumed via `include_str!`, and a test asserts byte equality — so no render change is accidental."
    - "D-14 (operator contract): the golden-mismatch failure message names the file to update AND states that a CHANGELOG entry is required because a digest-pinning consumer must re-pin — spike 010 makes a digest mismatch a FATAL pre-loop revocation, not a warning."
    - "D-14 (docs): the module rustdoc states plainly that the exact rendered text is NOT semver-stable and evolves on minor bumps."
    - "D-11 (docs): the module rustdoc names both deliberately excluded server-execution accessors and says why."
    - "GOLDEN COVERS THE ENCODER (REVIEWS finding 1): the golden fixture's description contains a `: ` and a `#`, so the pinned bytes include a non-trivial `yaml_double_quoted` output. A golden whose description is a plain word would pin the encoder's easy case only."
    - "DOCS RECORD THE REVIEW RESOLUTIONS: the module rustdoc and the CHANGELOG both carry (a) the D-02 `build()` return-shape deviation, (b) the D-10 narrowing — SC-6 gate warnings have ONE delivery channel — and (c) gate B's builder-reachability outcome, including the narrowed claim if `narrow` was chosen."
    - "ALWAYS/DOCTEST: every new public item carries a rustdoc with a self-gated doctest, and both doctest legs execute — the `skills::` path leg via selector 2 and the `workflow::sequential` leg via an explicit command, because no gate leg reaches the latter."
  artifacts:
    - tests/golden/workflow_skill_projection.md
    - tests/skills_integration.rs
    - CHANGELOG.md
    - src/server/skills/projection.rs
  key_links:
    - "`include_str!` registers a cargo rebuild dependency, so editing the golden re-runs the test — that IS the D-14 tripwire behaviour."
    - "The golden is the OTHER half of SC-2: re-derivation byte-equality proves the renderer is pure; the golden proves the bytes did not change since the last release."
    - "`make doc-check` runs `RUSTDOCFLAGS=\"-D warnings\"` with `skills` on, and `#![warn(missing_docs)]` is set at `src/lib.rs:24-29` — rustdoc on every public item is a build requirement, not a style preference."
  prohibitions:
    - statement: "`insta` must NOT be introduced — it is a dev-dep used nowhere, and adding a `cargo insta review` workflow that CI has no story for is scope."
      status: must_hold
      verification: "RUSTFLAGS=\"\" cargo test -p pmcp --features \"skills,streamable-http,http-client,testing\" --test skills_integration -- --test-threads=1"
    - statement: "The golden must not be re-recorded from the current output to make a red test green — a mismatch means either the render changed (update golden AND CHANGELOG) or the render regressed (fix the render)."
      status: must_hold
      verification: "RUSTFLAGS=\"\" cargo test -p pmcp --features \"skills,streamable-http,http-client,testing\" --test skills_integration -- --test-threads=1"
    - statement: "No new `tests/*.rs` file — the golden is consumed from `tests/skills_integration.rs` so no fifth `make test-skills` selector is needed."
      status: must_hold
      verification: "RUSTFLAGS=\"\" make test-skills"
    - statement: "A provenance frontmatter key is NOT added (deferred in CONTEXT.md, and the legal home is a `metadata:` map rather than a bare top-level key)."
      status: must_hold
      verification: "RUSTFLAGS=\"\" cargo test -p pmcp --features \"skills,streamable-http,http-client,testing\" --lib skills::projection -- --test-threads=1"
---

<objective>
Pin the rendered bytes with a golden file and complete the documentation surface the ALWAYS
requirements and `make doc-check` both demand.

Purpose: the digest published in `skills/list` binds pinning consumers to these exact bytes,
and spike 010's model makes a digest mismatch a FATAL pre-loop revocation rather than a
warning. D-14 therefore needs both halves — the property test proves the renderer is pure, the
golden proves the bytes have not moved since the last release — and a failure message that
tells the operator to add a CHANGELOG entry rather than silently re-record.
Output: `tests/golden/workflow_skill_projection.md`, its comparison test and named failure
message, a CHANGELOG entry, and the rustdoc surface across all new public items.
</objective>

<planning_notes>
Spec-less probe fallback: SKIPPED (no `126-SPEC.md`; v2.7 has no `REQUIREMENTS.md`, so there
were no requirement IDs to derive predicates from). **No probe-derived predicates were
generated for this phase.** `must_haves` derive from ROADMAP SC-1..SC-6 and
`126-VALIDATION.md`'s Per-Task Verification Map.

`.planning/STATE.md` / `PROJECT.md` / `ROADMAP.md` are deliberately not `@`-referenced
(156/47/59 KB).

This plan sits in wave 4, after both plan 126-04 (builder) and plan 126-05 (prepend), so the
golden is recorded once against a render that nothing later in the phase changes. Recording it
earlier would guarantee churn.
</planning_notes>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-CONTEXT.md
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-PATTERNS.md
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-04-SUMMARY.md
@.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-05-SUMMARY.md
@CLAUDE.md
</context>

<assumption_delta_decision>
**Decision: `no-change`.** The assumption-delta detector fired `detected: true` on three
`pluralization` signals — the terms `fallback`, `alongside` and `alternative`. All three are
lexical false positives: `fallback` is the D-15 slug fallback, `alongside` is spike 011's
positioning language, and `alternative` is the closing `## Server-accelerated alternative`
section heading. **No singular -> plural identity transition occurs in this phase.** Advisory,
never blocking.

Supplementary design invariant (unchanged): D-05 locks ONE byte-identical body serving both
surfaces. **This plan owns the strongest tripwire for it** — the D-14 golden, whose failure
message forces the operator to confront the re-pin consequence rather than silently accept new
bytes.
</assumption_delta_decision>

<review_dispositions>
Findings from `126-REVIEWS.md` that this plan closes:

| Finding | Severity | Disposition |
|---------|----------|-------------|
| 1 — ORDERING consequence (codex + fable) | HIGH | The frontmatter encoder is fixed in plan 126-01 Task 4 (**wave 1**), three waves before this plan records the golden — so the golden is taken over already-correct bytes and needs no re-record. **The wave graph did not need to change.** This plan additionally makes the golden fixture's description carry a `: ` and a `#` so the pinned bytes exercise the encoder rather than its trivial case. |
| codex LOW — doctests on every trivial warning accessor are excessive | LOW | **Relaxed in Task 3.** Doctests are required on the four entry points and once on `ProjectionWarning` as a type. `kind()` / `step()` / `tool()` / `message()` still need rustdoc (`#![warn(missing_docs)]` makes that a build requirement) but not individual doctests. |
| 4 + the D-07/D-10 resolution + gate B | MEDIUM/HIGH | **Documented here.** Task 2 requires all three outcomes in the module rustdoc AND the CHANGELOG: the D-02 return-shape deviation, the D-10 narrowing, and gate B's builder-reachability answer. A resolution recorded only in a plan file is invisible to a reader of the shipped crate. |
| gemini Finding 5 — the `workflow::sequential` doctest leg is outside `make quality-gate` | LOW | **Unchanged and still correct:** Task 3 runs it explicitly and records the count. Extending `make test-skills` to cover it is a Makefile change scoped to a future phase; recorded in `deferred-items.md` by plan 126-07 rather than silently dropped. |
</review_dispositions>

<artifacts_this_phase_produces>
Phase-126 new symbols — nothing below exists at HEAD; exclude from drift verification.

**Types (public):** `SkillProjection<'w>`, `ProjectionWarning`, `ProjectionWarningKind`, and
`ProjectionOutput` **iff** plan 126-01 Task 1's gate answered `output-struct`
**Methods (public):** `SequentialWorkflow::as_skill`, `SkillProjection::{new, with_tools, build}`,
`ProjectionWarning::{kind, step, tool, message}`,
`WorkflowPromptHandler::with_projected_skill_prepend`, and — **iff** plan 126-01 Task 2's gate
answered `add-builder-path` — `ServerCoreBuilder::with_workflow_skill_prepend` and
`ServerBuilder::with_workflow_skill_prepend`
**Functions (module-private, `src/server/skills/projection.rs`):** `slugify`, `fallback_slug`,
`yaml_double_quoted`, `render_body`, `render_frontmatter`, `render_context`,
`render_prompt_content`, `render_inputs`, `render_procedure`, `render_step`,
`render_data_source`, `render_closing`, `gate_check`, `sanitize_for_log`
**Fields (private):** `WorkflowPromptHandler::projected_skill_prepend: Option<String>` — the
ALREADY-RENDERED body, cached when the setter runs and never re-rendered per request (REVIEWS
fable (b)); plus `prepend_projected_skill: bool` on `ServerCoreBuilder` / `ServerBuilder` under
gate B
**Crate-private helper:** `WorkflowPromptHandler::projected_prepend`
**New files:** `src/server/skills/mod.rs` (git mv), `src/server/skills/projection.rs`,
`tests/golden/workflow_skill_projection.md`,
`fuzz/fuzz_targets/fuzz_workflow_projection.rs`, `examples/s56_workflow_skill_projection.rs`,
`.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/deferred-items.md`
**New manifest stanzas:** `[[example]] name = "s56_workflow_skill_projection"` (`Cargo.toml`);
`[[bin]] name = "fuzz_workflow_projection"` (`fuzz/Cargo.toml`); matrix row
`- fuzz_workflow_projection` (`.github/workflows/fuzz.yml`)
**New test fns:** `fuzz_workflow_projection_is_registered_and_scheduled`
(`tests/skills_routing.rs`), `golden_render_is_byte_equal` and its message helper
(`tests/skills_integration.rs`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The D-14 golden file and its byte comparison</name>
  <files>tests/golden/workflow_skill_projection.md, tests/skills_integration.rs</files>
  <precondition>`tests/` currently contains only the `common/` and `integration/` subdirectories; adding `tests/golden/` must not be picked up by cargo's integration-test autodiscovery, which compiles `tests/*.rs` but not files in subdirectories. Confirm with `cargo test --features "skills,streamable-http,http-client,testing" --no-run 2>&1 | grep -c golden` returning 0 before relying on it.</precondition>
  <read_first>
    - `126-PATTERNS.md` § 7 — `insta` is a dev-dep at `Cargo.toml:253` used NOWHERE; the in-repo mechanism is a vendored file plus `include_str!` (`tests/v2_tasks_shapes.rs:65-66`), and the counter-precedent at `tests/v2_tasks_tripwires.rs:97-99` applies to file-presence tests, not golden comparison.
    - `tests/embedded_resource_golden.rs:360-374` — `wire_break_message(raw: &str) -> String`, the failure-message style to copy almost literally: a NAMED fn returning `String` with a rustdoc saying why it is factored out, naming the file the operator must edit, and stating what the operator must NOT do.
    - `tests/skills_integration.rs:31` and `:89-95` — the `#![cfg]` header and the `ensure_trailing_newline` helper the existing tests use.
    - `src/server/skills/projection.rs` — the complete renderer, to build the fixture that the golden pins.
    - `126-CONTEXT.md` D-14 — the full decision including why the render is deliberately NOT semver-stable.
  </read_first>
  <action>
Record the render once, against a fixture nothing later in the phase changes.

**1. The fixture.** Add a `fn golden_workflow() -> SequentialWorkflow` to
`tests/skills_integration.rs` exercising the full D-11 universe: a name requiring
slugification (`refund_flow`), a description, two arguments (one required with a type, one
optional), one workflow-level instruction, and three steps — a tool step with two argument
bindings and a result binding, a tool step with guidance and two template bindings whose keys
sort differently from their insertion order, and a resource-only step with one attached
resource. The template-binding ordering matters: if the golden fixture has no template
bindings, the golden cannot catch a `HashMap`-ordering regression and half of D-14's value is
gone.

**The description must exercise the encoder (REVIEWS finding 1).** Make it
`Process a customer refund: policy #7` — it carries a `: ` mapping indicator and a `#` comment
indicator, so the golden's `description:` line pins a NON-TRIVIAL `yaml_double_quoted` output
rather than the encoder's easy case. A golden whose description is a plain word would stay
green through a regression that dropped the escaping, which is precisely the silent-failure
class this phase's review surfaced. The golden's second line is therefore
`name: "refund-flow"` and its third is
`description: "Process a customer refund: policy #7"` — quotes included.

**2. Generate the golden.** Run the projection over the fixture once, write the exact bytes to
`tests/golden/workflow_skill_projection.md`, and inspect it by eye before committing. This is
the ONE time the file is written from program output; every later change to it must be a
deliberate edit accompanied by a CHANGELOG entry.

**3. The comparison test.** In `tests/skills_integration.rs`:
`const GOLDEN: &str = include_str!("golden/workflow_skill_projection.md");` then
`golden_render_is_byte_equal` asserting `golden_workflow().as_skill().body() == GOLDEN`.
Use `include_str!`, NOT `insta`: `insta` is a dev-dep used nowhere in this tree, and
introducing `cargo insta review` into a gate with no story for it is scope. `include_str!`
also registers a cargo rebuild dependency, so editing the golden re-runs the test — which is
exactly the tripwire behaviour D-14 wants.

**4. The failure message, as a named function.** Copy `wire_break_message`'s three properties
from `tests/embedded_resource_golden.rs:360-374`: it is a named `fn ... -> String` (not an
inline literal) with a rustdoc explaining why it is factored out; it names the file the
operator must edit; and it states what the operator must NOT do. For D-14 the third clause is:
if the change is intentional, update `tests/golden/workflow_skill_projection.md` AND add a
CHANGELOG entry, because a consumer pinning this skill by digest must re-pin — spike 010 makes
a digest mismatch a fatal pre-loop revocation, not a warning. Do NOT re-record the golden to
turn a red test green without deciding which of the two cases it is.

**5. Anti-vacuity.** Before the equality, assert `GOLDEN.len() > 400`,
`GOLDEN.starts_with("---\nname: \"refund-flow\"\n")` (quotes included — plan 126-01 Task 4
emits both frontmatter values as encoded YAML double-quoted scalars) and
`GOLDEN.ends_with('\n')`. Add one more: `GOLDEN.contains("description: \"Process a customer refund: policy #7\"")`,
so the golden provably pins the ESCAPED form and a regression to raw concatenation cannot pass
by producing a different-but-plausible line. An empty or truncated golden must not pass by
matching an empty render.

**6. Confirm no autodiscovery surprise.** `tests/golden/` is a new directory. Cargo compiles
`tests/*.rs` as integration targets but not files inside subdirectories, and the file is `.md`
rather than `.rs` — but confirm it rather than assuming, per the precondition.

Place the test in `tests/skills_integration.rs`, not a new file: `make test-skills` selector 3
reaches this file, and a new `tests/skills_projection.rs` would match none of the four
selectors.
  </action>
  <acceptance_criteria>
    - `tests/golden/workflow_skill_projection.md` exists, is git-tracked, is longer than 400 bytes, starts with `---\nname: "refund-flow"\n` and ends with a single newline.
    - The golden's third line is `description: "Process a customer refund: policy #7"` — the escaped form, proving the pinned bytes exercise `yaml_double_quoted` rather than its trivial case.
    - The golden's body contains at least two template-binding keys rendered in sorted rather than insertion order.
    - `tests/skills_integration.rs` contains `include_str!("golden/workflow_skill_projection.md")`.
    - The failure message is produced by a named `fn` with a rustdoc, names `tests/golden/workflow_skill_projection.md`, and contains the words CHANGELOG and re-pin.
    - `grep -c 'insta::' tests/skills_integration.rs` returns 0.
    - `RUSTFLAGS="" cargo test --features "skills,streamable-http,http-client,testing" --no-run > /tmp/norun.log 2>&1; grep -c 'golden/workflow_skill_projection' /tmp/norun.log` returns 0 — the golden is not compiled as a test target (captured-then-asserted, so no pipeline can mask the build status).
    - A deliberate one-byte edit to the golden makes `golden_render_is_byte_equal` fail (verify by hand, then revert).
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS="" cargo test -p pmcp --features "skills,streamable-http,http-client,testing" --test skills_integration -- --test-threads=1</automated>
    <fails_when>non-zero exit, or the summary contains `running 0 tests` or `0 passed`, or the passed count did not increase over plan 126-05's recorded count</fails_when>
    <automated>bash -o pipefail -c 'git ls-files -- tests/golden/workflow_skill_projection.md | grep -c .'</automated>
    <fails_when>output is `0`, or non-zero exit — the golden must be git-tracked or CI has nothing to compare against</fails_when>
    <automated>RUSTFLAGS="" make test-skills</automated>
    <fails_when>non-zero exit, or the output contains `reported 0 tests` from any of the four selector guards</fails_when>
  </verify>
  <done>The rendered bytes are pinned by a tracked golden consumed through `include_str!`, the comparison test is red on any render change, and its failure message tells the operator to update the golden AND add a CHANGELOG entry rather than silently re-record.</done>
  <reversibility rating="costly">D-14 — tightening to a frozen byte-level semver contract later is possible; loosening after consumers have pinned is not.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The CHANGELOG entry and the D-14 / D-11 documentation contract</name>
  <files>CHANGELOG.md, src/server/skills/projection.rs, src/server/skills/mod.rs</files>
  <read_first>
    - `CHANGELOG.md` — read the top ~40 lines to match the existing heading level, version-section convention and bullet style. Do not invent a format.
    - `src/server/skills/projection.rs` — the module rustdoc written in plan 126-01 Task 2, which this task completes.
    - `126-CONTEXT.md` D-11, D-12, D-13, D-14 — the four coverage/contract decisions the module docs must state.
    - `126-RESEARCH.md` Q11 landmine #14 — `SequentialWorkflow::instruction()` is a shipped public builder method whose value every served surface silently drops, so the projection's Context section is the FIRST place that text becomes observable. Worth one sentence in the module docs.
    - `126-PATTERNS.md` § 11 — the `SkillDiagnostic` crate-privacy rationale that `ProjectionWarning`'s rustdoc must cite as a deliberate departure.
  </read_first>
  <action>
**CHANGELOG.** Add an entry under the appropriate unreleased/next-version heading matching the
file's existing style. It must record: the new `SequentialWorkflow::as_skill()` and
`SkillProjection` builder; that the rendered markdown is NOT semver-stable and every future
render change will be a CHANGELOG entry because a digest-pinning consumer must re-pin (D-14);
the new opt-in `WorkflowPromptHandler::with_projected_skill_prepend`, default off, with its
resulting transcript shape; and the SC-6 projection-time gate warning. Match the file's
existing heading level and bullet style exactly — read it first.

**Three cross-AI review resolutions MUST also appear in the CHANGELOG**, because a resolution
recorded only in a planning file is invisible to a reader of the shipped crate:
1. **`build()`'s return shape**, named as the shape actually shipped. Read
   `126-04-SUMMARY.md`'s deviation section and carry its wording; do not re-derive it.
2. **SC-6 warnings are delivered through `SkillProjection::build()` only** — a bare
   `as_skill()` holds no tool map and cannot produce them. One sentence, phrased as a
   capability boundary rather than a limitation apology.
3. **Whether the prepend is reachable from `prompt_workflow`.** Under gate B's
   `add-builder-path` answer, name `with_workflow_skill_prepend` on both builders and state
   that it applies to workflows registered after it. Under `narrow`, state plainly that the
   opt-in requires direct `WorkflowPromptHandler` construction. Read `126-05-SUMMARY.md` for
   which one shipped.

**Module rustdoc for `src/server/skills/projection.rs`** — complete the block started in plan
126-01. It must state, in prose a future maintainer will read before changing anything:

- **D-14, the body contract.** The exact rendered text is NOT semver-stable and evolves on
  minor bumps. Every render change is a CHANGELOG entry because a consumer pinning the skill
  by digest must re-pin, and spike 010's model makes a digest mismatch a fatal pre-loop
  revocation rather than a warning. Name `tests/golden/workflow_skill_projection.md` as the
  tripwire.
- **D-11, the deliberate exclusions.** Name both excluded server-execution accessors and state
  that they have no manual analogue, so SC-3's "every workflow fact" is a defined universe
  rather than an open-ended one. Point at the unit test that pins their absence.
  <!-- planner-discipline-allow: is_retryable --> <!-- planner-discipline-allow: has_task_support -->
- **D-12, why no tool descriptions or input schemas.** The client is already connected to this
  server and has `tools/list`, so that information is one call away and always current;
  nothing in the digested body can drift from the live tool surface.
- **D-13, why frontmatter is `name` + `description` only.** Hosts verify frontmatter
  field-by-field against the fetched file, so a smaller surface is a smaller conformance risk.
- **The `instruction()` observation.** `SequentialWorkflow::instruction()` (`sequential.rs:168`)
  is a shipped public builder method whose value every served surface currently drops; the
  projection's Context section is the first place that text becomes observable. One sentence —
  do not fix the underlying defect here, CONTEXT.md puts it out of scope.
- **The `#[non_exhaustive]` catch-all rule.** Every `_ =>` arm emits a constant literal, never
  a Debug format, so a future upstream variant cannot silently change the golden and every
  pinned digest.
- **The frontmatter encoding contract (REVIEWS finding 1).** Both frontmatter values are
  emitted as escaped YAML double-quoted scalars by `yaml_double_quoted`, unconditionally.
  State the two reasons: a slug drawn from `[a-z0-9-]` can still be a YAML type-alike
  (`true`, `123`), which unquoted would make `validate_names`' `as_str()` guard skip the skill
  and bypass SC-1's identity check; and conditional quoting would make the digested bytes
  depend on a predicate over author text. Name the round-trip proptest as the tripwire, and
  state that `parse_frontmatter_value` is the oracle — encoder hand-written, decoder
  library-verified.
- **Description legality.** Empty descriptions get a deterministic substitute on `as_skill()`
  and an `Err` from `build()`. **No length bound is enforced, because the current SEP-2640 /
  agentskills material states none** — say that explicitly, so a later reader does not assume
  the omission was an oversight and invent a limit.
- **The D-10 narrowing (REVIEWS codex).** SC-6 gate warnings are delivered through `build()`'s
  structured return only; `as_skill()` cannot observe them because annotations live on
  `crate::types::ToolInfo` and no workflow type carries them. `build()` emits both channels for
  what it can see. Do not describe SC-6 as having two channels.
- **Gate B's outcome on builder reachability.** Under `add-builder-path`, point at
  `with_workflow_skill_prepend` and note the registration-order semantics. Under `narrow`,
  carry `126-05-SUMMARY.md`'s narrowed-claim paragraph verbatim: the anti-drift guarantee holds
  per workflow VALUE, not per server, until a single-call registration path exists.

**`ProjectionWarning`'s rustdoc** must state why the type is public where the module's own
`SkillDiagnostic` is crate-private on purpose: D-10 requires `build()` to RETURN warnings so
tests assert on data instead of installing a subscriber, and callers can act on them. Cite
`SkillDiagnostic` by name. Without that note a future reader "fixes" the asymmetry and breaks
`build()`'s signature.

**`as_skill()`'s rustdoc** must state the D-15 divergence from `Skill::with_reference`'s
`Err(e) => panic!` arm — this method warns and falls back instead — or a future reader restores
the panic and reintroduces Phase 125's still-open WR-03 shape (`src/server/builder.rs:1501`).

No SATD markers anywhere: `make check-todos` greps `src/` literally for the four markers and
zero SATD is a gate.
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` gains an entry naming `as_skill`, `SkillProjection`, `with_projected_skill_prepend` and the non-semver-stable render policy, in the file's existing heading and bullet style.
    - That CHANGELOG entry ALSO names the shipped `build()` return shape, states that SC-6 warnings come only from `build()`, and states whether the prepend is reachable from `prompt_workflow`.
    - `src/server/skills/projection.rs`'s module rustdoc covers all TEN points listed in the action, including naming `tests/golden/workflow_skill_projection.md`, the frontmatter encoding contract, the absent description length bound, the D-10 narrowing, and gate B's outcome.
    - `grep -c 'yaml_double_quoted' src/server/skills/projection.rs` returns at least 2 (the function plus its mention in the module rustdoc).
    - `ProjectionWarning`'s rustdoc names `SkillDiagnostic` and explains the deliberate publicity departure.
    - `as_skill()`'s rustdoc names the D-15 divergence from the `with_reference` panic arm.
    - `RUSTFLAGS="" make doc-check` passes under `RUSTDOCFLAGS="-D warnings"` with `skills` on.
    - `grep -rn "TODO\|FIXME\|HACK\|XXX" src/server/skills/ src/server/workflow/` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS="" make doc-check</automated>
    <fails_when>non-zero exit, or the output contains `warning: missing documentation` or any line beginning `error:` — `RUSTDOCFLAGS="-D warnings"` turns a missing rustdoc into a hard failure</fails_when>
    <automated>bash -o pipefail -c 'test -d src/server/skills && test -d src/server/workflow && ! grep -rn "TODO\|FIXME\|HACK\|XXX" src/server/skills/ src/server/workflow/'</automated>
    <fails_when>non-zero exit — either a SATD marker is present in the two touched module trees (zero SATD is a gate) or one of the two directories is missing, which would otherwise make a bare negated grep pass vacuously</fails_when>
    <automated>RUSTFLAGS="" make check-todos</automated>
    <fails_when>non-zero exit — this is the gate's own SATD check running over all of `src/`</fails_when>
  </verify>
  <done>The CHANGELOG records the new surface and the non-semver-stable render policy, and every contract decision a future maintainer would otherwise reverse (D-11, D-12, D-13, D-14, D-15's no-panic divergence, `ProjectionWarning`'s publicity) is written into the rustdoc that `make doc-check` enforces.</done>
</task>

<task type="auto">
  <name>Task 3: ALWAYS/DOCTEST — both doctest legs, including the one no gate leg reaches</name>
  <files>src/server/workflow/sequential.rs, src/server/skills/projection.rs</files>
  <read_first>
    - `src/server/workflow/sequential.rs` — the `as_skill()` rustdoc added in plan 126-01 Task 3.
    - `126-PATTERNS.md` § 5 `src/server/mod.rs:4667-4687` — the verbatim self-gating doctest template (`/// # #[cfg(feature = "skills")] { ... # }`) plus the `# Panics` rustdoc-section convention from `:4704`.
    - `126-RESEARCH.md` Q8 § Doctest gating and § Common Pitfalls, Pitfall 3 "A doctest that never runs" — the substring filter and the feature set disagree about where "skills code" lives, so `as_skill()`'s doctest is reached by no gate leg.
    - `src/server/skills.rs:528` and `:970` (now in `skills/mod.rs`) — two more self-gated doctest precedents in this module.
  </read_first>
  <action>
Discharge CLAUDE.md's doctest requirement for every new public item, and make sure both legs
actually execute.

Every new public item needs **rustdoc** — `#![warn(missing_docs)]` is on at `src/lib.rs:24-29`
and `make doc-check` runs with `RUSTDOCFLAGS="-D warnings"`, so that much is a build
requirement, not a preference.

**Runnable doctests are required on the ENTRY POINTS, not on every accessor (REVIEWS codex
LOW).** The original requirement — a separate doctest on each of `kind()`, `step()`, `tool()`
and `message()` — buys no behaviour coverage and adds maintenance churn to items whose bodies
are a field read, in a module whose docs are adjacent to a pinned digest. The required set is:
`SequentialWorkflow::as_skill`, `SkillProjection::new`, `SkillProjection::with_tools`,
`SkillProjection::build`, `WorkflowPromptHandler::with_projected_skill_prepend`, ONE doctest on
`ProjectionWarning` as a type showing how a caller reads a returned warning, and — if gate B
answered `add-builder-path` — one on `with_workflow_skill_prepend` showing the correct
registration order. The four accessors get rustdoc prose and no doctest.

Every doctest must self-gate in the `/// # #[cfg(feature = "skills")] { ... # }` form —
otherwise it fails to compile under feature sets where the item does not exist. The template is
at `src/server/mod.rs:4667-4682`.

Each doctest must ASSERT, not merely construct: `as_skill()`'s asserts
`wf.as_skill().name() == "refund-flow"`; `build()`'s asserts `is_ok()` and, per the
fallible-pair convention at `skills.rs:393`, a second doctest or a second statement asserting
`is_err()` for a name that normalizes to nothing; `with_tools`' asserts the returned warning
vector's length for a fixture that trips the gate. `ProjectionWarning`'s single doctest asserts
on a `kind()` and a `step()` read from a real `build()` result, which covers all four accessors
in one place.

**The reachability trap this task exists to close.** `as_skill()`'s doctest lives at
`pmcp::server::workflow::sequential`, whose path does NOT contain the substring `skills`, so
`make test-skills` selector 2 (`--doc skills`) filters it out. And `test-doc` inside `test-all`
pins `--features "full"`, under which the method does not exist at all. That doctest is
therefore dark in the entire quality gate unless run explicitly. Run
`RUSTFLAGS="" cargo test -p pmcp --features "skills,full" --doc sequential -- --test-threads=1`
and record its passing count in the SUMMARY. The `skills::` doctests ARE reached by selector 2;
verify them with the second command below.
  </action>
  <acceptance_criteria>
    - Every item in the required doctest set listed in the action carries a rustdoc doctest; the four `ProjectionWarning` accessors carry rustdoc prose and are NOT required to carry individual doctests.
    - Every new doctest uses the `# #[cfg(feature = "skills")] {` self-gating form.
    - Every new doctest contains at least one `assert!` / `assert_eq!` — none is construction-only.
    - `build()`'s rustdoc has an `# Errors` section naming BOTH failure conditions (illegal name, empty description) and its doctests assert both `is_ok()` and `is_err()`.
    - The SUMMARY records the passing doctest counts from BOTH commands below, separately.
    - `RUSTFLAGS="" make doc-check` passes.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS="" cargo test -p pmcp --features "skills,full" --doc sequential -- --test-threads=1</automated>
    <fails_when>non-zero exit, or the summary reports `0 passed` — no gate leg runs this doctest, so a zero count means `as_skill()`'s doctest never executed and the ALWAYS/DOCTEST requirement is undischarged for it</fails_when>
    <automated>RUSTFLAGS="" cargo test -p pmcp --features "skills,streamable-http,http-client,testing" --doc skills -- --test-threads=1</automated>
    <fails_when>non-zero exit, or the summary reports `0 passed`, or the count did not increase over plan 126-04's recorded count</fails_when>
    <automated>RUSTFLAGS="" make doc-check</automated>
    <fails_when>non-zero exit, or the output contains `warning: missing documentation` or any line beginning `error:`</fails_when>
  </verify>
  <done>All six new public items carry asserting, self-gated doctests; both doctest legs are proven to have executed with nonzero counts, including the `workflow::sequential` leg that no gate leg reaches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered bytes -> pinning consumer's digest | The bytes the golden pins are the bytes a consumer's `[[skills]]` digest covers; a silent change is a revocation event on their side. |
| maintainer -> the golden file | A red golden invites the wrong fix (re-record) unless the failure message forbids it. |
| public API surface -> future maintainer | Undocumented rationale is how a deliberate decision gets reverted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-06 | Tampering | The rendered bytes as a supply-chain pin | medium | mitigate | The golden makes any render change loud, and its named failure message states that the operator must update the golden AND add a CHANGELOG entry because a digest-pinning consumer must re-pin — spike 010 makes a digest mismatch a FATAL pre-loop revocation. The module rustdoc states the same policy where a maintainer will read it before changing the render |
| T-126-16 | Repudiation | Re-recording a red golden from current output | medium | mitigate | The failure message explicitly forbids it and forces the operator to decide which of two cases it is: an intentional render change (update + CHANGELOG) or a regression (fix the render). Copied from `tests/embedded_resource_golden.rs:360-374`, which uses the same device |
| T-126-09 | Tampering | `#[non_exhaustive]` catch-all arms silently changing the golden | medium | mitigate | The constant-literal rule is enforced in code by plan 126-02 and DOCUMENTED here, so a future maintainer adding a variant arm knows the digest consequence before writing it |
| T-126-17 | Information Disclosure | Golden fixture content | low | accept | The fixture is a synthetic `refund_flow` workflow written for this test; it carries no credential, no real endpoint and no customer data |
| T-126-SC | Tampering | Supply chain / package installs | low | accept | This plan installs no packages and adds no dependency; `insta` is explicitly NOT introduced despite already being a dev-dep. `126-RESEARCH.md` § Package Legitimacy Audit records that the whole phase adds zero dependencies, so no legitimacy checkpoint is required |

ASVS level 1; block on high. No high or critical threat: this plan adds a test fixture, a
CHANGELOG entry and documentation. Its security relevance is entirely about keeping the
supply-chain pin honest, which is a medium-severity integrity concern, fully mitigated.
</threat_model>

<verification>
- `RUSTFLAGS="" cargo test -p pmcp --features "skills,streamable-http,http-client,testing" --test skills_integration -- --test-threads=1` passes with a count strictly greater than plan 126-05's.
- `RUSTFLAGS="" cargo test -p pmcp --features "skills,full" --doc sequential -- --test-threads=1` passes with a nonzero count.
- `RUSTFLAGS="" cargo test -p pmcp --features "skills,streamable-http,http-client,testing" --doc skills -- --test-threads=1` passes with a nonzero count.
- `RUSTFLAGS="" make doc-check` and `RUSTFLAGS="" make check-todos` both pass.
- `RUSTFLAGS="" make test-skills` passes all four selectors.
</verification>

<success_criteria>
SC-2's golden half holds — the rendered bytes are pinned by a tracked file whose mismatch
message forces a CHANGELOG entry — and the ALWAYS/DOCTEST requirement is discharged on both
doctest legs, including the one no gate leg reaches.
</success_criteria>

<output>
Create `.planning/phases/126-workflow-to-skill-projection-sequentialworkflow-as-skill/126-06-SUMMARY.md` when done
</output>
