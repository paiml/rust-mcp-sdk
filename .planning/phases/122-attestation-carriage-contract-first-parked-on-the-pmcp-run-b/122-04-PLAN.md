---
phase: 122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b
plan: 04
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - contracts/pmcp-run/attestation-v1.graphql
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs
  - cargo-pmcp/tests/package_attestation_contract.rs
  - Makefile
  - docs/platform-requests/package-portability-alignment.md
autonomous: true
requirements: [PKGX-01]

estimate:
  tokens: 42000
  raw_tokens: 84000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`contracts/pmcp-run/attestation-v1.graphql` exists as a sibling of `capture-v1.graphql`, and the two files are distinguishable at a glance: the capture file records a real AppSync introspection export and platform ownership, the attestation file states plainly that it is SDK-proposed, not platform-exported, and awaiting ratification."
    - "The `verifyAttestation` operation constant the CLI would send validates against the vendored SDL with `apollo_compiler`, offline, with no network access and no pmcp.run credentials — and that test binary is executed by `make quality-gate` with a nonzero passed count asserted by name."
    - "The live verification leg exists as an `#[ignore]`d, env-gated test whose body already contains an EXECUTABLE request path — it builds the request payload with the production helper, sends `VERIFY_ATTESTATION_QUERY` over HTTP with an `Authorization` header, and asserts on the DECODED response through the production decoder. Promoting this phase from parked to blocking is deleting the `#[ignore]` and the gate blocks, never writing a request path that did not exist."
    - "The live leg authenticates through the pmcp.run credential sources `get_credentials()` already reads (`cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs:518-540`) and sends the header spelling `execute_graphql_at` already uses — there is exactly ONE pmcp.run API path in the tree, and this test does not open a second one."
    - "The contract test's own module docs state what it CANNOT prove: it validates SDK-written queries against an SDK-written schema, so it pins SDK-internal agreement today and becomes a real drift net only once the platform exports."
  prohibitions:
    - statement: "The vendored attestation SDL must NOT imitate `capture-v1.graphql`'s provenance header — it has no `Source:` and no `Exported:` line, because it was written by the SDK and the platform has not spoken."
      status: flagged
      flagged: true
    - statement: "The proposed SDL names `verifyAttestation` only. `getAttestation` and `issueAttestation` are deferred by D-11 and must not appear."
      status: flagged
      flagged: true
    - statement: "No test in this plan may perform a network call or read pmcp.run credentials on a default `cargo test` path; the live leg reaches the network only when ALL THREE gate variables (`PMCP_ATTESTATION_LIVE_TEST`, `PMCP_API_URL`, `PMCP_ACCESS_TOKEN`) are set explicitly AND `--ignored` is passed."
      status: flagged
      flagged: true
    - statement: "This plan must not open a second pmcp.run API path. The endpoint variable, the credential source and the `Authorization` header spelling are all reused from the existing seam; `graphql_contract.rs` must not gain a `reqwest` or `oauth2` import (Phase 123 SC3)."
      status: flagged
      flagged: true
  artifacts:
    - contracts/pmcp-run/attestation-v1.graphql
    - "`VERIFY_ATTESTATION_QUERY` in cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs"
    - "`verify_attestation_request_body` and `decode_verify_attestation_response` (pure, IO-free) in the same file — the executable half of the parked live leg"
    - "`GRAPHQL_AUTH_HEADER` in the same file, consumed by both `execute_graphql_at` and the live leg"
    - cargo-pmcp/tests/package_attestation_contract.rs
    - ".planning/phases/122-.../COVERAGE.md (already written at plan time; verified present by this plan)"
    - "A numbered ratification ask in docs/platform-requests/package-portability-alignment.md"
  key_links:
    - "`cargo-pmcp/src/lib.rs:174-177` `#[path]` mount -> `cargo_pmcp::pmcp_run_graphql::VERIFY_ATTESTATION_QUERY` -> `ExecutableDocument::parse_and_validate` against the vendored SDL (the test reaches the REAL runtime constant without pulling the reqwest/auth tree)"
    - "`cargo_pmcp::pmcp_run_graphql::verify_attestation_request_body` -> `reqwest` POST to `PMCP_API_URL` with `GRAPHQL_AUTH_HEADER` -> `decode_verify_attestation_response` (the live leg's executable request path; both ends are production code, only the transport lives in the test)"
    - "`Makefile` `REQUIRED_TEST_BINARIES` -> `package_attestation_contract` -> `scripts/named-test-binary-count.awk` (what makes SC1's word `blocking` true)"
---

<objective>
Land the contract-first half of PKGX-01: a vendored SDL for the one operation the SDK actually needs, the exact runtime operation constant, an offline blocking test that validates one against the other, and the parked live leg that names what the backend must ship.

D-07: the SDL is SDK-authored and marked unratified, sitting beside `capture-v1.graphql`'s genuine ownership rule so the difference is visible at a glance, paired with a concrete ask in the platform-requests doc.
D-11: the proposal names `verifyAttestation` only — the attestation arrives inside the package, so the CLI never fetches one, and issuance is entirely the platform's to design. Propose the minimum for a contract we do not own.

Purpose: make the parked boundary explicit and reversible in exactly one step, and make "blocking" a measured property rather than a claim.
Output: one vendored SDL, one operation constant, one contract-test binary carrying the parked live leg, one Makefile append, and one numbered platform ask.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-CONTEXT.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-RESEARCH.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-PATTERNS.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/COVERAGE.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the SDK-proposed attestation SDL, the runtime operation constant, and the pure request/response seam</name>
  <files>contracts/pmcp-run/attestation-v1.graphql, cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs, cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs</files>
  <read_first>
    - `contracts/pmcp-run/capture-v1.graphql` — the full file: the header shape to copy structurally, the provenance lines that must NOT be copied, the ownership paragraph, the `String`-not-enum discipline note, and the SDL body shape (root `type Mutation` / `type Query` blocks plus named return object types)
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` lines 1-60 — the module docs explaining why this narrow leaf exists, why it may depend on `anyhow` and `serde_json` but NOT on `reqwest`/oauth2, and `SUBMIT_PACKAGE_CAPTURE_QUERY` / `GET_PACKAGE_CAPTURE_STATUS_QUERY`'s raw-string shape
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs` lines 438-500 — `execute_graphql` / `execute_graphql_at`: the `{"query": ..., "variables": ...}` body shape, the shared `reqwest::Client` `OnceLock`, and SPECIFICALLY line 491's `.header("Authorization", access_token)` — note it sends the RAW token with no `Bearer ` prefix, which the live leg must match exactly
    - `cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs` lines 484-545 — the `Credentials` struct and `get_credentials()`'s three documented sources (`PMCP_CLIENT_ID`+`PMCP_CLIENT_SECRET`, `PMCP_ACCESS_TOKEN`, then `~/.pmcp/credentials.toml`), and lines 100-120 where `PMCP_API_URL` takes precedence for the endpoint
    - `cargo-pmcp/src/lib.rs` lines 1-60 and around line 174 — the `pub mod deployment { ... }` narrow mount (which deliberately exposes ONLY `config`, `iam`, `widgets`, `post_deploy_tests` and `google_cloud_run`) and the `#[doc(hidden)] #[path = ...] pub mod pmcp_run_graphql;` mount that makes these constants reachable from `tests/`. Confirm for yourself that `auth.rs` is NOT reachable from the lib target — it references `crate::commands::configure::*` at `auth.rs:150-153`, which is bin-only
    - `docs/design/package-portability-pmcp-run-handoff.md` §5.3 — the no-crypto rule, the opaque-layer rule, and the parked-boundary discipline
    - `docs/design/package-portability-and-audit.md` §4 — the reference payload shape (`schemaVersion` / `subject.payloadDigest` / `producedBy` / `components` / `verdict`), which informs what `verifyAttestation` plausibly takes and returns
    - `.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-RESEARCH.md` Pattern 5 and its "Vendored SDL header" code example
  </read_first>
  <action>
Create `contracts/pmcp-run/attestation-v1.graphql`.

Header — copy `capture-v1.graphql`'s STRUCTURE (a `# <file> — <purpose> (SDL subset)` first line, a `Version:` line, an explicit ownership paragraph) and INVERT its content. Do NOT write `Source:` or `Exported:` lines: this file has no provenance and imitating one would be a lie about who owns it. Instead lead with an unmissable status line stating that the file is SDK-proposed, not platform-exported, and awaiting ratification, and say in the ownership paragraph that its sibling `capture-v1.graphql` WAS exported from the live AppSync API on 2026-07-20 and that the platform owns that file's contents, whereas this one is a proposal the platform has not yet responded to and nothing in it is authoritative until it does. Point at the ask this plan adds in `docs/platform-requests/package-portability-alignment.md`.

Add a SCOPE paragraph recording D-11 and its reason: `verifyAttestation` ONLY. The attestation arrives inside the package — that is what carriage means — so the CLI never fetches one; issuance is entirely the platform's to design; proposing the minimum for a contract we do not own means the platform ratifies one operation, not three. Name `getAttestation` and `issueAttestation` as deliberately deferred so a reader knows their absence is a decision rather than an oversight.

Copy the `String`-not-enum discipline from the sibling and state its reason verbatim in substance: an enum makes any later schema-versus-schema diff show permanent drift.

SDL body — a `type Query` (or `type Mutation`, whichever the operation's semantics warrant; verification is a read, so `Query` is the natural choice — state the choice in a comment) declaring `verifyAttestation` with the arguments the SDK can actually supply from what carriage gives it, plus a named return object type. Ground the field set in `docs/design/package-portability-and-audit.md` §4's report shape rather than inventing one: the caller can supply the attestation payload bytes it carries and the subject digest it re-derived locally; the platform answers with a verdict plus the identity it verified against and a timestamp. Type every status/verdict-shaped field as `String`, never an enum.

Then add `VERIFY_ATTESTATION_QUERY` to `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs`, beside the two capture constants, as a `pub const &str` raw string with an indented operation body in exactly the style of `SUBMIT_PACKAGE_CAPTURE_QUERY`. Its rustdoc names the test that consumes it and restates why the constant lives in this narrow leaf: so the offline blocking contract test can validate the REAL runtime query against the vendored SDL without pulling `pmcp_run`'s auth/deploy/reqwest tree into the `cargo-pmcp` lib target.

One extra sentence in the constant's rustdoc that the capture constants do not need: this operation is NOT sent by any shipped code path in this phase. It exists so the contract is written down and validated now; the call site is the parked live leg. Say so, so nobody hunts for a caller that does not exist and nobody deletes the constant as dead.

**The pure request/response seam the parked live leg executes against.** SC5 promises unparking is "removing a gate, not writing a new test", and cross-AI review found that the previous acceptance criterion ("body OR rustdoc") let a rustdoc-only stub satisfy it. Close that by putting the request-building and response-decoding halves in PRODUCTION code here, so Task 2's ignored test has something real to call. All three additions go in `graphql_contract.rs`, which is already lib-mounted and already depends on `anyhow` + `serde_json` and nothing heavier — do NOT add `reqwest` to it.

- `pub const GRAPHQL_AUTH_HEADER: &str = "Authorization";` — the header NAME, extracted so there is one spelling. Then change `execute_graphql_at` in `graphql.rs:491` to use it (`graphql.rs` already re-exports `super::graphql_contract::*`, so the import costs nothing). This is a two-line change whose whole purpose is that the live leg and the shipped client cannot drift into two different auth shapes. Its rustdoc records the measured fact that the value sent is the RAW access token with no `Bearer ` prefix, and that this is what the platform's AppSync endpoint expects — do not "fix" it into `Bearer <token>`.
- `pub fn verify_attestation_request_body(attestation_payload: &[u8], subject_digest: &str) -> Result<serde_json::Value>` — builds the exact JSON-RPC-over-HTTP body the CLI would POST: `{"query": VERIFY_ATTESTATION_QUERY, "variables": { ... }}`, with the variable names matching the SDL's argument names character for character. Base64-encode the payload bytes for transport if the SDL types that argument as `String` (state the encoding in both the SDL comment and this function's rustdoc so the platform is not left guessing). Pure and IO-free.
- `pub fn decode_verify_attestation_response(body: &serde_json::Value) -> Result<VerifyAttestationOutcome>` plus a `pub struct VerifyAttestationOutcome` carrying the fields the SDL's return type declares — the verdict string, the verified identity, and the timestamp — all typed `String` per the no-enum discipline. It must surface a GraphQL `errors` array as an `Err` naming the first error's message rather than silently returning a default. Pure and IO-free.

Give both functions unit tests in `graphql_contract.rs`'s own `#[cfg(test)]` module, over hand-written `serde_json::json!` fixtures — those run offline under the default `cargo test -p cargo-pmcp --lib` and are what let the live leg be a thin transport wrapper rather than a place where new logic gets written at unpark time.
  </action>
  <acceptance_criteria>
    - `cargo build -p cargo-pmcp` succeeds and `cargo clippy -p cargo-pmcp --all-targets -- -D warnings` is clean (an unused `pub const` in a `pub mod` is not dead code, but confirm no warning appears).
    - `contracts/pmcp-run/attestation-v1.graphql` contains no line beginning with `# Source:` and no line beginning with `# Exported:`.
    - The file's first twenty lines contain an explicit statement that it is SDK-proposed and awaiting ratification, and a pointer to `docs/platform-requests/package-portability-alignment.md`.
    - The SDL declares exactly one operation field. `grep -c 'verifyAttestation' contracts/pmcp-run/attestation-v1.graphql` is at least 1, and the file contains neither `getAttestation` nor `issueAttestation` as a declared field (their names may appear only inside `#` comment lines explaining the deferral).
    - No `enum` keyword appears in the SDL body.
    - `VERIFY_ATTESTATION_QUERY` is reachable as `cargo_pmcp::pmcp_run_graphql::VERIFY_ATTESTATION_QUERY` from an integration test — proven by Task 2 compiling against it.
    - `verify_attestation_request_body`, `decode_verify_attestation_response`, `VerifyAttestationOutcome` and `GRAPHQL_AUTH_HEADER` are all reachable as `cargo_pmcp::pmcp_run_graphql::*` from an integration test.
    - `graphql_contract.rs` still imports nothing beyond `anyhow` and `serde_json` — `grep -n '^use ' cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` shows no `reqwest`, no `oauth2`, no `crate::commands`.
    - `execute_graphql_at` in `graphql.rs` uses `GRAPHQL_AUTH_HEADER` rather than an inline `"Authorization"` literal — `grep -n '"Authorization"' cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs` returns no match.
    - `cargo test -p cargo-pmcp --lib` passes including the new unit tests for the request builder and the response decoder, and the decoder test asserts that a body carrying a GraphQL `errors` array returns `Err` with the first error's message in the `Display` string.
    - The variable names in `verify_attestation_request_body`'s emitted body match the SDL's declared argument names exactly — asserted by a unit test that reads the variable keys out of the produced `serde_json::Value`.
  </acceptance_criteria>
  <verify>
    <automated>cargo clippy -p cargo-pmcp --all-targets -- -D warnings</automated>
  </verify>
  <done>The contract the SDK proposes is written down, honestly labelled as unratified, scoped to one operation, and paired with the exact operation string a future client would send.</done>
</task>

<task type="auto">
  <name>Task 2: The offline blocking contract test, its parked live leg, and the gate append that makes "blocking" true</name>
  <files>cargo-pmcp/tests/package_attestation_contract.rs, Makefile</files>
  <read_first>
    - `cargo-pmcp/tests/package_capture_contract.rs` — the complete model: the module docs (as plan 122-01 corrected them), the `SDL_PATH` `concat!(env!("CARGO_MANIFEST_DIR"), ...)` idiom, the `schema()` loader and its comment about keeping the `Valid<Schema>` wrapper, and all three tests (`capture_ops_validate_against_contract`, `status_field_is_string_not_enum`, `response_structs_match_selection_sets` INCLUDING its honest limitation note at lines 67-75)
    - `crates/pmcp-openapi-server/tests/parity_replay.rs` lines 300-345 — the `#[ignore = "..."]` plus explicit `std::env::var(..) != Some("1")` early return that prints why it skipped
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` as Task 1 of this plan left it — `VERIFY_ATTESTATION_QUERY`, `GRAPHQL_AUTH_HEADER`, `verify_attestation_request_body`, `decode_verify_attestation_response` and `VerifyAttestationOutcome`, all of which the live leg calls rather than reimplements
    - `cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs` lines 518-545 — `get_credentials()`'s three credential sources, and lines 150-153 where it imports `crate::commands::configure::*`, the reference that makes the whole module bin-only and unreachable from `tests/`
    - `cargo-pmcp/Cargo.toml` lines 47-115 and 158-165 — confirm `tokio` (feature `full`), `reqwest` and `serde_json` are `[dependencies]` and `assert_cmd` / `apollo-compiler` are `[dev-dependencies]`; all of them are available to an integration test in `tests/`
    - `Makefile` — the `test-cargo-pmcp-integration` target plan 122-01 added, including its `REQUIRED_TEST_BINARIES` list and the append-only discipline note
    - `.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-RESEARCH.md` Pattern 6 and the "An all-`#[ignore]`d new test binary" anti-pattern
  </read_first>
  <action>
Create `cargo-pmcp/tests/package_attestation_contract.rs` as a sibling of `package_capture_contract.rs`, mirroring its three-test structure and its module-doc candour.

Module docs: state that this is a pure offline/static check with no network access and no pmcp.run credentials, and name the mechanism that makes it blocking — the `test-cargo-pmcp-integration` Makefile target, chained into `test-all` and therefore into `make quality-gate`, which asserts a nonzero passed count for this binary by name via `scripts/named-test-binary-count.awk`. Do not repeat the claim plan 122-01 had to correct in the sibling.

Then add the D-07-specific limitation the sibling cannot have, prominently, because a reader must meet it here rather than discover it: this file validates SDK-written queries against an SDK-written schema. It pins SDK-INTERNAL agreement today — that the operation string and the proposed SDL cannot drift apart — and it becomes a real drift net the moment the platform exports its own schema. It cannot detect drift from a platform that has not spoken.

Test 1: load the vendored SDL with `Schema::parse_and_validate`, then `ExecutableDocument::parse_and_validate` the `VERIFY_ATTESTATION_QUERY` constant against it, panicking with a message naming the operation on failure — the sibling's `unwrap_or_else(|e| panic!(...))` shape.

Test 2: the shape pin — assert the SDL types its verdict/status-shaped field as `String` and contains no `enum` declaration, the sibling's `status_field_is_string_not_enum` shape.

Test 3: the selection-set drift check — assert each field the SDK expects to read back appears in the operation string, and copy the sibling's honest NOTE explaining that this greps field-name lists against the query STRING rather than parsing any Rust response struct, so it is a drift sanity check on the selection set and not a full struct-versus-schema proof.

Test 4 — the SC5 parked live leg. Make it `#[tokio::test]` (`tokio` with the `full` feature is a `cargo-pmcp` dependency and is therefore available to `tests/`) carrying `#[ignore = "..."]` whose reason string names all three gate variables. It must contain an EXECUTABLE REQUEST PATH, not a description of one. Cross-AI review found the previous formulation ("body OR rustdoc names the operation...") satisfiable by rustdoc alone, which would leave unparking as "write the client", contradicting SC5's promise that unparking is removing a gate.

Structure it as three early-return gates followed by a real request:

- Gate 1 — return early with an explanatory `eprintln!` unless `PMCP_ATTESTATION_LIVE_TEST` is exactly `"1"`, exactly as `parity_replay.rs:326-336` does it.
- Gate 2 — return early with a second explanatory `eprintln!` unless `PMCP_API_URL` is set and non-empty. Reuse that existing variable deliberately rather than inventing a second base-URL variable, and say why in a comment: Phase 123's SC3 forbids a second API path, and inventing one here would pre-break it. `PMCP_API_URL` is already the highest-precedence endpoint source for the shipped client (`auth.rs:100-120`).
- Gate 3 — the AUTHENTICATION gate, which review correctly identified as missing: `PMCP_API_URL` supplies an endpoint but no credential. Return early with a third explanatory `eprintln!` unless `PMCP_ACCESS_TOKEN` is set and non-empty.

  Name in a comment exactly why this is a reuse and not a new auth path: `PMCP_ACCESS_TOKEN` is one of the three credential sources `get_credentials()` itself reads (`auth.rs:527-540`, the documented CI/CD branch), so a live run authenticates through a source the shipped client already honours. Then record the measured constraint that forces the test to read the variable directly instead of calling `get_credentials()`: `auth.rs` references `crate::commands::configure::*` at lines 150-153, so it lives in the BIN target only and is not mounted into the lib (`cargo-pmcp/src/lib.rs`'s `pub mod deployment` exposes only `config`, `iam`, `widgets`, `post_deploy_tests`, `google_cloud_run`). An integration test cannot reach it. Mounting the oauth2/browser-flow tree into the lib target to make one ignored test tidier is the wrong trade; the honest move is to read the one variable and say so. State both facts in the test's rustdoc, and state what this costs: a live run cannot use the interactive `~/.pmcp/credentials.toml` file or the client-credentials flow, so the operator exports `PMCP_ACCESS_TOKEN` first.

Then the executable path, using ONLY production helpers for everything except the transport:

1. Build a fixture attestation payload and derive its subject digest locally — reuse the same fixture-and-digest shape the offline tests use, so the request carries a real subject rather than a placeholder.
2. Build the request body with `cargo_pmcp::pmcp_run_graphql::verify_attestation_request_body(..)` — the production builder from Task 1, never a hand-written JSON literal in the test.
3. POST it: `reqwest::Client::new().post(api_url).header(GRAPHQL_AUTH_HEADER, token).json(&body).send().await`, using the production header constant. `reqwest` is a `cargo-pmcp` dependency and is available to `tests/`.
4. Decode the response with `cargo_pmcp::pmcp_run_graphql::decode_verify_attestation_response(..)` — the production decoder — and assert on the decoded `VerifyAttestationOutcome`: that the verdict field is non-empty, and that the verified-identity field is non-empty. Those two assertions hold regardless of what the backend answers, so they are executable today.

Leave in rustdoc ONLY what is genuinely unknowable until the platform speaks: the exact verdict vocabulary, the exact identity string, and whether a mismatched subject produces a `verdict` value or a GraphQL error. Name each of those as an open question the first live run answers, and say that answering them means TIGHTENING these assertions, not writing new ones.

Add a rustdoc paragraph stating the unparking procedure in one sentence: delete the `#[ignore]` attribute and the three early-return gate blocks; the request path below them already runs.

The binary carries three non-ignored tests alongside the ignored one, which is required: a binary whose every test is ignored reports `0 passed`, and `scripts/named-test-binary-count.awk` treats `0` as a gate failure.

Then append `package_attestation_contract` to `REQUIRED_TEST_BINARIES` in the `test-cargo-pmcp-integration` target and add a matching `--test package_attestation_contract` selector to that target's cargo invocation. This is the append plan 122-01's comment reserved for this plan — the name is added now, in the same commit that creates the binary, never before.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test package_attestation_contract` reports 3 passed and 1 ignored.
    - `make test-cargo-pmcp-integration` exits 0 and prints `✓ package_attestation_contract passed 3 tests`.
    - Negative control on the contract itself, recorded in the SUMMARY: temporarily rename one field inside `VERIFY_ATTESTATION_QUERY`'s selection set, run the test binary, observe Test 1 FAIL with the apollo-compiler validation error; restore and observe green.
    - Negative control on the gate, recorded in the SUMMARY: temporarily add `#[ignore]` to the three passing tests, run `make test-cargo-pmcp-integration`, observe it fail with the "RAN but passed ZERO tests" verdict; restore.
    - `cargo test -p cargo-pmcp --test package_attestation_contract -- --ignored` prints the skip explanation naming `PMCP_ATTESTATION_LIVE_TEST` and exits 0 without any network activity.
    - The live-leg test BODY contains an executable request path, not a description of one. Verified by reading and by three source assertions: it calls `verify_attestation_request_body`, it performs a `reqwest` POST whose header name is `GRAPHQL_AUTH_HEADER`, and it calls `decode_verify_attestation_response` and asserts on at least two fields of the returned `VerifyAttestationOutcome`. A test satisfying this cannot be satisfied by rustdoc.
    - The test contains no hand-written GraphQL JSON body and no inline `"Authorization"` string literal — both come from `cargo_pmcp::pmcp_run_graphql`.
    - Three gate blocks are present and each prints its own explanatory skip message naming its own variable: `PMCP_ATTESTATION_LIVE_TEST`, `PMCP_API_URL`, `PMCP_ACCESS_TOKEN`.
    - The unpark procedure is stated in the test's rustdoc as deleting the `#[ignore]` and the three gate blocks, with nothing else to write.
    - The rustdoc records that `auth.rs` is bin-only (`crate::commands::configure::*` at `auth.rs:150-153`) and therefore unreachable from `tests/`, names `PMCP_ACCESS_TOKEN` as one of `get_credentials()`'s own three documented sources, and states the cost (a live run cannot use `~/.pmcp/credentials.toml` or the client-credentials flow).
    - No file in this plan introduces a new base-URL environment variable and none introduces a new credential source; `PMCP_API_URL` and `PMCP_ACCESS_TOKEN` are both reused from the existing pmcp.run seam.
    - Negative control on the auth gate, recorded in the SUMMARY: with `PMCP_ATTESTATION_LIVE_TEST=1 PMCP_API_URL=https://example.invalid` set but `PMCP_ACCESS_TOKEN` unset, run `-- --ignored` and observe the third skip message and a zero exit with no network activity — proving the auth gate is real and ordered before the request.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cargo-pmcp --test package_attestation_contract</automated>
  </verify>
  <done>The proposed contract is validated offline in a binary the gate provably executes, and the live leg is one `#[ignore]` and two `if` blocks away from running.</done>
  <precondition>`cargo tree -p cargo-pmcp -i apollo-compiler` reports version 1.32.0 under `[dev-dependencies]`; the test uses `Schema::parse_and_validate` and `ExecutableDocument::parse_and_validate`, which are apollo-compiler 1.x APIs. `cargo tree -p cargo-pmcp -i tokio` and `-i reqwest` both resolve, since the live leg is a `#[tokio::test]` that performs a `reqwest` POST — both are `cargo-pmcp` `[dependencies]` and are therefore linkable from `tests/`.</precondition>
</task>

<task type="auto">
  <name>Task 3: The ratification ask, promoted out of the FYI section</name>
  <files>docs/platform-requests/package-portability-alignment.md</files>
  <read_first>
    - `docs/platform-requests/package-portability-alignment.md` — the whole file: the `## The asks, in priority order` section and its four numbered subsections, the `### 5. FYI / later — attestations & admission (Phase E)` subsection at line 102 whose own text says "Nothing to build now", and the `## What we're building meanwhile (no platform dependency)` section that follows
    - `contracts/pmcp-run/attestation-v1.graphql` as Task 1 wrote it — the ask must point at the concrete file and operation
    - `docs/design/package-portability-and-audit.md` §2 — the boundary table assigning attestation storage and admission to the platform and the attestation FORMAT to a shared versioned contract
  </read_first>
  <action>
D-07 requires a CONCRETE ask, and the section that currently covers attestations is explicitly a no-ask FYI ("Nothing to build now"). Burying a ratification request inside it would leave the ask unfindable.

Insert a new numbered subsection into `## The asks, in priority order`, positioned after the existing `### 4. Joint format extension` and before the attestations FYI, and renumber the FYI subsection from `5` to `6` so the numbering stays contiguous. Rewrite no other content in the FYI beyond its number.

The new ask states, concretely:
- What we did: vendored an SDK-PROPOSED SDL at `contracts/pmcp-run/attestation-v1.graphql` naming `verifyAttestation` only, and wired an offline blocking contract test in `cargo-pmcp/tests/package_attestation_contract.rs` that fails the SDK build if the CLI's operation drifts from that SDL.
- What we are asking for: ratification (or a counter-proposal) of that one operation — its name, its arguments, its return shape — and, once ratified, an export of the platform's own SDL to replace the proposal, at which point the blocking test becomes a real cross-boundary drift net instead of an internal consistency check.
- Why only one operation (D-11): the attestation arrives inside the package, so the CLI never fetches one; `getAttestation` is speculative until import semantics settle; issuance is entirely the platform's to design. One operation to ratify, not three.
- The boundary, restated so the ask cannot be misread as scope creep: the SDK does carriage and subject-digest comparison only. It holds no keys, adds no crypto dependency, and cannot verify a signature offline — which is exactly why this one call has to exist on the platform side.
- The FORMAT note this milestone adds, per `122-CONTEXT.md`'s roadmap correction: the package format now records a component's declared range alongside its resolution, and a pack carrying an attestation refuses any unresolved component reference. Flag it as an FYI on the format the platform's capture also writes, so the two sides do not diverge silently.

Add a one-line pointer to the same ask from wherever the file's opening section summarizes what is outstanding, if such a summary exists, so the ask is reachable without reading the whole document.
  </action>
  <acceptance_criteria>
    - `grep -n '^### ' docs/platform-requests/package-portability-alignment.md` shows six contiguously numbered subsections under `## The asks, in priority order`, with no duplicate and no skipped number.
    - The new subsection names `contracts/pmcp-run/attestation-v1.graphql`, `verifyAttestation`, and `cargo-pmcp/tests/package_attestation_contract.rs` by their exact paths and spelling.
    - The new subsection states that the SDK cannot verify a signature offline and gives that as the reason the platform-side call is needed.
    - The former `### 5. FYI / later` subsection is present as `### 6.` with its body text otherwise unchanged — confirmed by `git diff` showing only the heading line altered in that block.
    - The format FYI naming the declared-range/resolution addition and the unresolved-reference refusal is present.
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^### [0-9]' docs/platform-requests/package-portability-alignment.md</automated>
  </verify>
  <done>The platform has a findable, concrete ratification ask for exactly one operation, with the SDK's boundary and the format addition stated alongside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK <-> pmcp.run GraphQL API | A contract neither side has agreed yet; the SDL is a proposal, and the test can only pin the SDK's own half |
| default `cargo test` run <-> the network | A contract test that reaches the network on a default path would leak credentials into CI and break SC1's offline requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-04 | Repudiation | a "blocking" contract test that runs in no gate | high | mitigate | `package_attestation_contract` appended to `REQUIRED_TEST_BINARIES` in the same commit that creates it; proven by an all-`#[ignore]` negative control that turns the gate red |
| T-122-16 | Repudiation | a vendored SDL that implies platform ownership it does not have | high | mitigate | The header states SDK-PROPOSED / NOT PLATFORM-EXPORTED / AWAITING RATIFICATION, carries no `Source:` or `Exported:` line, and the module docs of the test state what it cannot prove |
| T-122-17 | Information Disclosure | the live leg reaching pmcp.run on a default test run | high | mitigate | `#[ignore]` plus a TRIPLE env gate (`PMCP_ATTESTATION_LIVE_TEST`, `PMCP_API_URL`, `PMCP_ACCESS_TOKEN`), each with its own explanatory skip message; asserted by running `-- --ignored` and observing a no-network skip, plus a control run that sets the first two and confirms the third still blocks |
| T-122-18 | Tampering | a second API base-URL variable or a second credential source creating a parallel unauthenticated path | high | mitigate | The existing `PMCP_API_URL` precedence and the existing `PMCP_ACCESS_TOKEN` credential source (`auth.rs:527-540`, one of `get_credentials()`'s own three) are both reused; the header NAME is extracted into `GRAPHQL_AUTH_HEADER` and consumed by both `execute_graphql_at` and the live leg, so the two cannot drift into different auth shapes. No new base-URL or credential variable is introduced (also protects Phase 123 SC3) |
| T-122-35 | Repudiation | a "parked" leg that is a description rather than a test, so unparking silently means writing the client | high | mitigate | The request builder and the response decoder are PRODUCTION functions in `graphql_contract.rs` with their own offline unit tests; the ignored test is a thin transport wrapper that must call both, and three source assertions in the acceptance criteria make a rustdoc-only stub non-conforming |
| T-122-36 | Information Disclosure | an access token echoed into CI logs by a failing live-leg assertion | medium | mitigate | The decoder returns a typed `VerifyAttestationOutcome`; assertions read its fields, never the request body or the header value. The skip messages name VARIABLE NAMES only, never values |
| T-122-08 | Spoofing | the ask implying the SDK verifies identity | medium | mitigate | The ask states plainly that the SDK holds no keys and cannot verify a signature offline (D-11) |
| T-122-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new external packages; `apollo-compiler 1.32.0` is already a `cargo-pmcp` dev-dependency (`122-RESEARCH.md` Package Legitimacy Audit) |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --test package_attestation_contract` reports 3 passed / 1 ignored.
- `cargo test -p cargo-pmcp --lib` green, including the new `graphql_contract.rs` unit tests for the request builder and the response decoder.
- `make test-cargo-pmcp-integration` green with four named binaries each at a nonzero count.
- `cargo test -p cargo-pmcp --test package_attestation_contract -- --ignored` skips loudly with no network activity.
- All three recorded negative controls (renamed selection-set field; all-ignored sweep; the auth-gate control with `PMCP_ACCESS_TOKEN` unset) reproduced.
- `make quality-gate` green before commit.
- `.planning/phases/122-.../COVERAGE.md` is present and its matrix matches D-11 (verify, do not rewrite — it was authored at plan time).
</verification>

<success_criteria>
- SC1 is satisfied and measured: the vendored contract exists, the CLI's operation validates against it offline with `apollo_compiler`, and the binary runs in a gate that asserts it passed a nonzero number of tests.
- SC5 is satisfied: the live leg is `#[ignore]`d, triple-env-gated, carries an executable request path built from production helpers, names what the backend must ship, and unparking removes gates rather than adding tests.
- D-07's ratification ask is a numbered, findable ask rather than an FYI.
</success_criteria>

## Artifacts this phase produces

Symbols created or first introduced by THIS plan (excluded from drift verification — they do not exist on `main`):

- File `contracts/pmcp-run/attestation-v1.graphql`
- `VERIFY_ATTESTATION_QUERY` (pub const, `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs`)
- `GRAPHQL_AUTH_HEADER` (pub const, same file) — and `execute_graphql_at` in `graphql.rs` re-pointed at it
- `verify_attestation_request_body` (pub fn, same file) — pure, IO-free
- `decode_verify_attestation_response` (pub fn, same file) — pure, IO-free
- `VerifyAttestationOutcome` (pub struct, same file)
- GraphQL field name `verifyAttestation` and its proposed return object type
- File `cargo-pmcp/tests/package_attestation_contract.rs` and its four test functions (three passing, one `#[ignore]`d live leg with an executable request path)
- Environment variable `PMCP_ATTESTATION_LIVE_TEST` (test-only gate; read by no production path)
- The live leg's REUSED (not created) environment variables: `PMCP_API_URL` and `PMCP_ACCESS_TOKEN`, both existing pmcp.run seam variables
- `REQUIRED_TEST_BINARIES` entry `package_attestation_contract` and the matching `--test` selector in `test-cargo-pmcp-integration`
- A new numbered subsection in `docs/platform-requests/package-portability-alignment.md`, and the renumbering of the former `### 5.` FYI to `### 6.`
- File `.planning/phases/122-.../COVERAGE.md` (authored at plan time; this plan verifies rather than creates it)

Symbols created elsewhere in Phase 122 and referenced here only by name: `test-cargo-pmcp-integration` and `no-crypto-check` (122-01); `MT_ATTESTATION` (the KIND-NEUTRAL constant — see the decision record at the end of 122-02) and `AttestationFile` (122-02); the subject verdict (122-03).

## Review findings incorporated

- **HIGH (Consensus verdict: confirmed, blocking before this plan executes)** — *"the parked live leg is not guaranteed to be an actual live verification test ... `PMCP_API_URL` only supplies an endpoint, not authentication."* **Accepted in full.** Task 1 now lands three production helpers (`verify_attestation_request_body`, `decode_verify_attestation_response`, `GRAPHQL_AUTH_HEADER`) with their own offline unit tests, and Task 2's live leg is required to CALL them over a real `reqwest` POST and assert on the decoded outcome. The "body or rustdoc" acceptance criterion is deleted and replaced with three source assertions a rustdoc-only stub cannot satisfy. The auth gap is closed with a third gate on `PMCP_ACCESS_TOKEN` — one of `get_credentials()`'s own three documented sources — plus a recorded explanation of why `get_credentials()` itself is unreachable from `tests/`.

## Review findings rejected

- **HIGH as filed by one reviewer; Consensus Summary DOWNGRADED to LOW** — *"the mandatory provable-contract-first workflow is missing: no plan owns a `../provable-contracts/contracts/<crate>/` YAML or the pre/post `pmat comply check` runs."* **Rejected, and deliberately not actioned.** The cited sibling directory does not exist — that repository contains only a `README.md`, with no `contracts/` tree at all, so there is no YAML for a plan to own and no schema to write one against. The repo's live, ENFORCED contract mechanism is the in-repo `contracts/` tree checked by `comply-bindings-check` (`Makefile:1270-1276`, resolving `contracts/team-servers/binding.yaml`), while `make comply` treats `pmat comply check` output as informational (`Makefile:1296`, a trailing `|| echo`). Phase 122 already follows that live convention: this plan vendors `contracts/pmcp-run/attestation-v1.graphql` beside the existing `contracts/pmcp-run/capture-v1.graphql` and puts a BLOCKING `apollo_compiler` test on it. The reviewer cited the instruction without checking whether it maps to a running gate. If the provable-contracts repo is ever populated, adding a YAML for `pmcp-package` is its own task in its own phase — it is not a Phase 122 gap.

<output>
Create `.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-04-SUMMARY.md` when done. Record the final SDL operation signature and its argument names, the payload encoding chosen for transport, the three env-gate variable names and which of them are reused versus created, the signatures of `verify_attestation_request_body` / `decode_verify_attestation_response` / `VerifyAttestationOutcome`, the list of open questions left in the live leg's rustdoc for the first live run to answer, and all three negative-control runs with their observed failures.
</output>
