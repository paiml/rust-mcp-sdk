---
status: complete
phase: 125-sep-2640-conformance-skills-list-skills-get
source: [125-VERIFICATION.md]
started: 2026-09-02T09:40:00Z
updated: 2026-09-02T18:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. CR-01 — stdio session teardown on an advertised capability
expected: A recorded human decision — accept as D-01-scoped residual risk (via VERIFICATION override), or open a follow-up plan. Not a silent pass.
why_human: Product-risk acceptance judgment, not a fact a grep can settle. The code is present, correct, and exactly as documented; the question is whether the documented risk is tolerable to ship.
result: pass
decision: ACCEPTED as a D-01-scoped residual risk. No follow-up plan opened.
decided_by: human (UAT, 2026-09-02)
rationale: |
  The SDK's direction is remote MCP servers over streamable HTTP; stdio is actively
  discouraged, and even local testing runs against an HTTP server. Skills are strictly
  opt-in, so the affected population is servers that deliberately combine an opt-in
  feature with a discouraged transport.
grounds_corrected_at_decision_time: |
  The acceptance was first offered on the basis that the session "tears down with the
  error message". That premise was checked and is FALSE — recorded here so the accepted
  risk is the real one, not a milder one:

    - The CLIENT receives no JSON-RPC error. The actor `break`s the receive loop
      (`src/server/mod.rs:1481-1484`) without constructing a response; the transport drops
      and the host observes EOF / broken pipe, not -32601.
    - The OPERATOR receives nothing either. `Self::log_error` delegates to `crate::log`,
      which is a documented no-op stub (`src/lib.rs:373-386`, all parameters `_`-prefixed,
      "placeholder for examples"). It is the only `crate::log` in the tree. The formatted
      "Transport receive error: {e}" string is built and discarded.

  The accepted behavior is therefore a session that dies mid-conversation with NO
  diagnostic on either end.

  The acceptance was re-affirmed by the user on these corrected grounds.
opt_in_strength_verified: |
  Stronger than assumed at decision time. `skills` is absent from BOTH `default`
  (= ["logging", "v1-compat"]) and `full` — it is a standalone feature
  (`Cargo.toml:41`, `skills = ["dep:serde_yaml"]`). `set_skills_capabilities` has exactly
  four call sites, all `#[cfg(feature = "skills")]`: `.skills()`/`.try_skills()` on
  `ServerBuilder` (`mod.rs:4712,4736`) and on `ServerCoreBuilder` (`builder.rs:494,518`).
  A server must both compile with `features = ["skills"]` AND call `.skills(...)` to
  declare the extension. Servers doing neither are unaffected — the "won't break all MCP
  servers over stdio" condition holds.
scope_note: |
  The defect is NOT skills-specific and was not introduced by this phase. `parse_request`
  returns `method_not_found` for any `IngressRequest::Internal(_)` and for any entirely
  unknown method (`protocol_helpers.rs:100,114`); `parse_method_message` wraps both as
  `TransportError::InvalidMessage` (`transport.rs:139`); the actor breaks on either. Any
  unroutable method frame has always killed a stdio session, `server/discover` included.
  Phase 125's contribution is making it reachable by a CORRECTLY behaving host acting on a
  declaration this SDK emits. Accepting it here does not accept it as a permanent
  property of the stdio actor — the general receive-arm fix (125-REVIEW.md CR-01) and the
  `crate::log` stub remain open, unscheduled, and out of this phase's scope.
result_note: This is the recorded decision the test demanded — not a silent pass.

### 2. WR-06 — undeclared extension still answers `skills/list`
expected: A recorded human decision on whether a server that never declared `io.modelcontextprotocol/skills` returning HTTP 200 `{"skills": []}` needs a capability guard before shipping, or is acceptable JSON-RPC behavior. It breaks the "probe by calling and treat -32601 as unsupported" idiom the codebase itself uses for `server/discover`, but is not covered by any ROADMAP Success Criterion, so it is not scored as a gap.
why_human: Spec-conformance judgment call outside the phase's literal must-haves.
premise_drift: |
  STALE as written. This checkpoint was authored at 2026-09-02T09:33Z against commit
  6b4a417a. Commit 1b10e3fb (2026-09-02 08:06 -0700) landed the capability guard it asks
  about: both delegates now gate on the declared `io.modelcontextprotocol/skills`
  extension — declared-but-empty answers `{"skills":[]}`, undeclared answers -32601 — with
  a regression test for the previously untested undeclared population. The decision this
  checkpoint solicits has already been taken in code; what remains is confirming it.
result: pass
decision: SHIP AS-IS. No further work required — the capability guard already landed in 1b10e3fb and is retained.
decided_by: human (UAT, 2026-09-02)
rationale_as_given: "Since skills are opt-in we don't need any special capability guard before shipping."
rationale_correction: |
  The CONCLUSION is correct — nothing further to do — but the stated reason does not
  support it, and is recorded corrected here so a later reader does not DELETE the
  shipped guard citing "skills are opt-in anyway".

  Opt-in-ness applies to DECLARATION, not to ROUTING, and the two are independent:

    - Declaration is opt-in: `set_skills_capabilities` has four call sites, all
      `#[cfg(feature = "skills")]` (`mod.rs:4712,4736`, `builder.rs:494,518`).
    - Routing is NOT: `classify_internal_method`, `SKILLS_LIST_METHOD` and
      `SKILLS_GET_METHOD` (`src/types/protocol/mod.rs:946,961,990,1001`) carry no
      `cfg` at all and compile into EVERY build.

  So before 1b10e3fb every streamable-HTTP pmcp server answered `skills/list` with
  `200 {"skills": []}` — including servers that never called `.skills(..)` and servers
  compiled entirely without `feature = "skills"`. The feature's opt-in-ness did not
  propagate to the wire. The guard is precisely what makes observable behavior match the
  opt-in intent this decision relies on; it is load-bearing, not a "special" extra.
guard_verified: |
  `skills_method_not_found` (`src/server/mod.rs:1788-1812`) reads
  `capabilities.extensions` for `SKILLS_EXTENSION_KEY`, hard-codes `declared = false`
  under `#[cfg(not(feature = "skills"))]`, and returns -32601 naming the extension.
  Both `handle_skills_list` and `handle_skills_get` call it as their first statement.
  Regression test `a_server_that_declares_no_skills_extension_refuses_both_methods`
  (`tests/skills_routing.rs:459-489`) asserts -32601 on BOTH methods over a real
  loopback socket. MEASURED 2026-09-02: `cargo test -p pmcp --features
  "skills,streamable-http,http-client,testing" --test skills_routing
  a_server_that_declares_no_skills_extension_refuses_both_methods` → `1 passed;
  0 failed; 19 filtered out`. Removing the guard now fails this test.
selector_warning: |
  The same command run with the feature set `skills,streamable-http` (omitting
  `http-client,testing`) exits 0 reporting `running 0 tests` — the file-level
  `#![cfg(all(...))]` at `tests/skills_routing.rs:62-67` configures the whole binary
  out. Use the Makefile's canonical `SKILLS_FEATURES` (`Makefile:954`) when running
  these by hand; a bare exit-0 here is not evidence of a pass.

### 3. Disposition of 125-REVIEW.md's unremediated findings
expected: Confirm the 2 CRITICAL + 8 WARNING findings are intentionally carried forward into a follow-up phase/plan, rather than silently left open — either a follow-up reference, or an explicit VERIFICATION override accepting them as-is.
why_human: Verified via `git diff --stat` that zero source changes landed after the review. The review is dated the same day as phase completion and its findings are unremediated at the point of verification.
premise_drift: |
  STALE as written. The `why_human` claim "zero source changes landed after the review" was
  true at 2026-09-02T09:33Z and is now false. Two commits have since landed against the
  review: 6af1c120 (single build pass, derived route predicate, one catalog shape) and
  1b10e3fb (`/code-review max --fix` over the phase diff — capability gate, ComposedResources
  pagination, cross-map URI collisions, control-char neutralization, one-time catalog
  serialization). The surviving question is narrower than the original: which findings are
  still open, and are those carried forward deliberately.
result: pass
decision: CARRY FORWARD. The three still-open findings (WR-03, WR-04-partial, WR-05) are deliberately deferred, not silently dropped.
decided_by: human (UAT, 2026-09-02)
recorded_in: deferred-items.md — "Three 125-REVIEW.md findings carried forward unfixed (UAT decision, 2026-09-02)"
disposition_measured_at: HEAD 1b10e3fb
disposition: |
  Re-measured against source at UAT time rather than read from commit messages.
  6 FIXED / 1 ACCEPTED / 3 OPEN.

    FIXED    CR-02  user result is now the base; skills prepended only when cursor.is_none()
    FIXED    WR-01  `make lint-skills` (full,skills) chained into quality-gate; found 5 real violations
    FIXED    WR-02  catalog serialized once in build() as Arc<IndexMap<String, Value>>
    FIXED    WR-06  capability gate + regression test; MEASURED `1 passed` by this UAT
    FIXED    WR-07  book `size` corrected to 172
    FIXED    WR-08  scan now split_once("target:") + take_while block scoping + anti-vacuity
                    check (tests/skills_routing.rs:1517-1528)
    ACCEPTED CR-01  this UAT, test 1 — D-01-scoped residual risk
    OPEN     WR-03  panic! still at src/server/builder.rs:1501
    OPEN     WR-04  control chars neutralized; still zero direct tests (2 refs, both core.rs)
    OPEN     WR-05  4 refs, all in streamable_http_server.rs; no test reaches either assembler

  Not re-measured: IN-01..IN-04 (informational tier, never in scope for this checkpoint).
why_this_is_not_a_silent_drop: |
  The carry-forward is written to deferred-items.md, which `/gsd-audit-uat` reads
  cross-phase. A decision recorded only in this UAT file would stop surfacing the moment
  the file's status flips to `complete`.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all three checkpoints were human decisions, each resolved by a recorded decision
rather than by reporting a defect. The carried-forward review findings are tracked in
deferred-items.md, not here: they were raised by code review, not by a failed UAT test.]
