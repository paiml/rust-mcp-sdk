---
phase: 125-sep-2640-conformance-skills-list-skills-get
plan: 02
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - src/types/protocol/mod.rs
  - src/server/streamable_http_server.rs
  - src/server/core.rs
  - src/server/mod.rs
  - src/server/builder.rs
  - tests/skills_routing.rs
autonomous: true
requirements: [D-06, D-07]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A live server answers a `skills/get` POST whose `params.uri` names a registered SKILL.md with a single entry identical in shape to a `skills/list` entry, carrying `resultType: \"complete\"` (D-07)."
    - "`skills/get` on a URI the server does not serve returns JSON-RPC error -32602 (Invalid params), per the current draft — NOT the -32601 the shipped `SkillsHandler::read` returns for `resources/read` (D-06)."
    - "`skills/get` with malformed or absent params returns -32602 from the served branch, after the header and auth pipeline has run, never as a classification-time parse error."
    - "The `skills/get` lookup is an exact-match against the entry map keyed by SKILL.md URI; the caller's URI is never joined, normalized, or manipulated into a path (ASVS V5)."
    - "The header and auth pipeline runs BEFORE the -32602 params error: a `skills/get` with malformed params sent without credentials to a server carrying an auth provider receives the authentication refusal, not a params error. Preserving raw params in the classifier is the mechanism; this is the test that proves the ordering."
    - "The two skills methods reach ONLY the high-level `Server` HTTP dispatch path. A `ServerCoreBuilder`-built `ServerCore` still serves every skill through `resources/list` and `resources/read` — the 125-01 tuple change did not break that path — but answers no skills METHOD, because `ProtocolHandler::handle_request` accepts only the typed public `Request` and the phase deliberately adds no variant to it. That limit is recorded as a deferral, never left silently true."
    - "`ServerCore` carries no skills field and no skills delegate: a source-scan test fails if one is added, because such a delegate would be unreachable dead code — exactly what Phase 112 deleted when it consolidated `server/discover` into a shared free function."
    - "Both projection free functions are unit-tested directly in `src/server/core.rs`'s own test module, beside the `build_discover_response` tests that already live there, so the projections have coverage that does not depend on a transport."
    - "`request_is_cacheable` gains no row for either method; cacheability is named at each projection call site (D-07), and the `skills/get` v2 response carries NEITHER `ttlMs` NOR `cacheScope` — asserted, not merely omitted from the source."
  artifacts:
    - src/types/protocol/mod.rs
    - src/server/streamable_http_server.rs
    - src/server/core.rs
    - src/server/mod.rs
    - src/server/builder.rs
    - tests/skills_routing.rs
  key_links:
    - "`classify_http_ingress` inner `match` -> `HttpIngress::SkillsGet` -> `Server::handle_skills_get` -> `build_skills_get_response`: the same five-site chain the tracer proved for `skills/list`, with the params now actually consumed."
    - "`finalize_skills_resources` -> `Server.skill_entries` (125-01) is the ONLY entries consumer. Its `ServerCoreBuilder::build` call site (`src/server/builder.rs:1358`) must destructure the 125-01 tuple and DISCARD the entries, because a `ServerCore` has no route by which they could be answered."
    - "`ProtocolHandler::handle_request` (`src/server/core.rs:80`, `ServerCore` impl at `:3764`) accepts a typed public `Request`. That signature is the reason a `ServerCore` skills delegate would be dead code, and the reason this plan tests projections rather than claiming builder wire parity."
    - "`build_skills_get_response` error path -> `error_codes::INVALID_PARAMS`, deliberately diverging from the `METHOD_NOT_FOUND` its `build_discover_response` analog uses."
---

<objective>
Expand the tracer's proven path sideways: add `skills/get` on the same classifier
route, and settle the `ServerCore` question honestly — measure the reach, prove the
125-01 tuple change did not break the `ServerCoreBuilder` path, cover both
projection functions directly, and record the limit.

Purpose: `skills/get` is the second of the two methods the draft makes MANDATORY
for any server declaring the extension. A server answering only `skills/list` is
still non-conformant.

The second half of this plan CHANGED during the cross-AI replan. It previously
claimed `ServerCoreBuilder` wire parity, which the repository's type architecture
cannot deliver — see the disposition of R-13 below. What replaces it is not a
smaller version of the same claim; it is the true statement, tested, plus a
structural guard against someone re-adding the dead code the false claim would
have produced.

Output: `skills/get` over HTTP with draft-correct `-32602` semantics, a proven
gate ordering, direct coverage on both projections, and a measured, recorded
`ServerCore` boundary.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-sep-2640-conformance-skills-list-skills-get/125-CONTEXT.md
@.planning/phases/125-sep-2640-conformance-skills-list-skills-get/125-RESEARCH.md
@.planning/phases/125-sep-2640-conformance-skills-list-skills-get/125-PATTERNS.md
@.planning/phases/125-sep-2640-conformance-skills-list-skills-get/125-01-SUMMARY.md
</context>

## Artifacts this phase produces

Created in **this plan** (125-02). See 125-01-PLAN.md for the phase-wide table.

| Symbol / artifact | Kind | Location | Visibility |
|---|---|---|---|
| `InternalClientRequest::SkillsGet` | enum variant `{ params: serde_json::Value }` | `src/types/protocol/mod.rs` | `pub(crate)` |
| `HttpIngress::SkillsGet` | enum variant `{ id, params }` | `src/server/streamable_http_server.rs` | private |
| `assemble_skills_get_fast` / `assemble_skills_get_with_middleware` | HTTP response assemblers | `src/server/streamable_http_server.rs` | private |
| `build_skills_get_response` | shared projection free fn | `src/server/core.rs` | `pub(crate)` |
| `Server::handle_skills_get` | thin delegate | `src/server/mod.rs` | `pub(crate)` |

**Deliberately NOT created (changed by the cross-AI replan):** `ServerCore.skill_entries`,
`ServerCore::handle_skills_list` and `ServerCore::handle_skills_get`. An earlier
revision of this plan listed all three. They would be unreachable dead code — see
R-13 below — and Task 2 now adds a source-scan guard that FAILS if any of them
reappears.

## Cross-AI review disposition (125-REVIEWS.md) — findings owned by 125-02

| ID | Source | Severity | Finding | Disposition |
|---|---|---|---|---|
| R-13 | Codex (orchestrator-corroborated) | HIGH | `ServerCore` cannot receive either skills request through its public dispatch seam, so `ServerCore::handle_skills_list/get` would be private helpers with no ingress route; the named integration harnesses cannot drive them; and carrying entries on `ServerCore` does not make `ServerCoreBuilder` conformant. The plan conflated data parity with transport parity. | **INCORPORATED — Task 2 rewritten.** Re-verified independently during the replan: `ProtocolHandler::handle_request` takes a typed public `Request` (`src/server/core.rs:80`), the `ServerCore` impl uses exactly that signature (`:3764`), `grep -c 'ServerCore' src/server/streamable_http_server.rs` returns **0**, and the duplex harness `raw_via_core` (`tests/common/duplex.rs:334`) also takes `Arc<dyn ProtocolHandler>` plus a typed `Request`. **The replan also found the decisive in-repo precedent neither reviewer cited:** Phase 112 hit this exact wall for `server/discover` and DELETED the `ServerCore` wrappers — today `grep -n 'handle_discover' src/server/core.rs` returns only two rustdoc mentions and no function, `build_discover_response` is a free fn called by `Server::handle_discover` and by `core.rs`'s own unit tests, and `handle_tasks_update` likewise exists only on `Server`. This plan follows that precedent rather than re-litigating it. |
| R-14 | Codex | HIGH | The proposed integration parity test cannot use the named harness — `spawn_default_config` accepts `Server`, not `ServerCore` | **INCORPORATED** — the parity test is removed. What replaces it: a `ServerCoreBuilder` resource-surface test driven through the seam that DOES accept a `ServerCore` (`raw_via_core` with a typed `resources/list` / `resources/read` request), plus direct unit tests on both projection free functions in `core.rs`. |
| R-15 | Codex | MEDIUM | If only projection parity is desired, it should be named "projection parity" and tested as free functions in unit tests, not claimed as builder wire parity | **INCORPORATED** — Task 2 is renamed and its must-have truths restate the boundary in the true direction. |
| R-16 | Codex | MEDIUM | No test proves auth/header gates execute BEFORE malformed params return -32602; preserving raw params in the classifier does not by itself prove the order | **INCORPORATED** — Task 1 gains the gate-ordering test as a `<behavior>` row and an acceptance criterion. |
| R-17 | Codex | LOW | `skills/get` cache behavior needs a direct envelope assertion — "do not name `Cacheable::Yes`" should be tested by asserting the absence of the caching keys on both eras | **INCORPORATED** — Task 1 asserts `ttlMs` and `cacheScope` are absent from the `skills/get` result on BOTH v1 and v2, pairing with 125-01's positive assertion for `skills/list`. |
| R-18 | Fable | LOW | `grep -c 'skills/list\|skills/get' src/server/core.rs` "shows the method strings are referenced only through constants" — a count cannot show that | **INCORPORATED** — replaced with a structural criterion that names what would actually be wrong. |
| R-19 | Codex + Fable | MEDIUM/LOW | Wave 2 parallelism: 125-02 and 125-03 have disjoint `files_modified` (verified) but 125-03's new `validate_names` hard-reject changes `into_handler` behavior that this plan's fixtures build servers through | **INCORPORATED** — the fixture frontmatter-name rule is restated in Task 1, and the merge-sequencing note is added to `<verification>`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: skills/get over HTTP with draft-correct -32602 semantics</name>

  <files>src/types/protocol/mod.rs, src/server/streamable_http_server.rs, src/server/core.rs, src/server/mod.rs, tests/skills_routing.rs</files>

  <read_first>
    - .planning/phases/125-sep-2640-conformance-skills-list-skills-get/125-01-SUMMARY.md — what the tracer actually landed, including the exact names of the entry map field and the projection fn.
    - src/types/protocol/mod.rs — the `SKILLS_GET_METHOD` constant and `InternalClientRequest::SkillsList` variant the tracer added, plus the `TasksUpdate` variant's raw-params rustdoc at ~:798-806.
    - src/server/streamable_http_server.rs — the five `HttpIngress::SkillsList` sites the tracer wrote; each needs a `SkillsGet` sibling. Also `assemble_tasks_update_fast` (:3888) and `assemble_tasks_update_with_middleware` (:3942), the params-consuming analog pair, and the `TasksUpdateCall` struct (:3831) showing the argument-bundling convention that avoids `clippy::too_many_arguments`.
    - src/server/core.rs — `build_skills_list_response` as the tracer wrote it, `build_discover_response` (~:2380) for the envelope shape, and the `request_is_cacheable` rustdoc (~:2153-2200) explaining why neither method gets a row.
    - src/server/skills.rs:556-575 — `SkillsHandler::read`, which returns `ErrorCode::METHOD_NOT_FOUND` for an unknown URI. That is the -32601 divergence D-06 records as an out-of-scope observation; do NOT copy it, and do NOT change it.
    - src/server/skills.rs:280-292 — `resolved_path` / `skill_md_uri`, and :321-357 `validate_reference_path`, the registration-time rules the lookup side must not re-open.
    - tests/skills_integration.rs:253 — `resources_read_unknown_uri_method_not_found`, the test that pins the pre-existing -32601 behavior this plan must leave passing.
    - tests/skills_routing.rs — the file to extend.
  </read_first>

  <behavior>
    - `classify_internal_method("skills/get", &garbage_params)` returns `Some(InternalClientRequest::SkillsGet { params })` with `params` byte-identical to the input, including when the input is a non-object or `Value::Null`. The classifier never rejects a body.
    - `classify_internal_method("skills/gets", ...)` returns `None`.
    - A `skills/get` POST whose `params.uri` equals a registered SKILL.md URI returns `result.skill` — a single object with the same `uri`, `frontmatter` and `resources` keys a `skills/list` entry carries — plus `result.resultType == "complete"`.
    - A `skills/get` POST whose `params.uri` names an unregistered URI returns error code -32602.
    - A `skills/get` POST whose `params.uri` names a registered skill's REFERENCE file (not its SKILL.md) returns -32602: the draft says the `uri` MUST be a skill's SKILL.md.
    - A `skills/get` POST with `params` absent, or with `params.uri` a non-string, returns -32602 and does not panic.
    - A `skills/get` POST whose `params.uri` contains `..` path segments or a trailing path suffix appended to a registered URI returns -32602 — the map lookup is exact-match, so no traversal reaches a file.
    - The `skills/get` result carries no pagination cursor key.
    - The `skills/get` result carries NEITHER a `ttlMs` key NOR a `cacheScope` key, on a v1 framing AND on a v2 framing. The draft leaves the get-caching question open, so absence must be asserted on the wire rather than inferred from the fact that the source does not name `Cacheable::Yes` (R-17).
    - **Gate ordering (R-16):** against a server carrying an auth provider, a `skills/get` POST with deliberately malformed params and NO credentials receives the authentication refusal, not -32602. The same POST WITH valid credentials receives -32602. Both halves are required: the first alone would also pass on a server that refuses everything, and the second alone proves nothing about ordering.
  </behavior>

  <action>
Add the second method on the route the tracer proved, consuming its params for the
first time in this phase.

1. `src/types/protocol/mod.rs`: add `InternalClientRequest::SkillsGet { params: serde_json::Value }`
   and its classifier arm keyed on `SKILLS_GET_METHOD`, cloning `params` and
   deserializing nothing. Copy the `TasksUpdate` variant's raw-params rustdoc
   reasoning: a classifier that deserialized would hand an unauthenticated caller a
   params error instead of an auth refusal, inverting the gate ordering. Extend the
   in-module classifier test with `skills/gets` as the near-miss control and a
   non-object params value proving pass-through.

2. `src/server/core.rs`: add `pub(crate) fn build_skills_get_response`, taking the
   entry map, the raw params, the id and the protocol context. Deserialize
   `params.uri` HERE, in the served branch. Four deltas from the
   `build_skills_list_response` sibling the tracer wrote.
   (a) On absent params, a non-object params, a missing `uri` key, or a non-string
   `uri`, return `ServerCore::error_response` with `error_codes::INVALID_PARAMS`
   and a message that does not echo the caller's raw URI unbounded (ASVS V7) —
   truncate or omit it.
   (b) Look the URI up by exact key in the entry map. Never join, normalize,
   percent-decode or otherwise manipulate the caller's string into a path (ASVS
   V5); registration-time `validate_reference_path` already closed the traversal
   surface and the lookup side must not re-open it.
   (c) On a miss, return `error_codes::INVALID_PARAMS` — that is -32602 per the
   draft (D-06). Do NOT use `error_codes::METHOD_NOT_FOUND`, which is what
   `build_discover_response` uses and what the shipped `SkillsHandler::read` returns
   for `resources/read`. Add a rustdoc paragraph recording that the `resources/read`
   -32601 divergence is a known, separately-tracked observation this function
   deliberately does not copy and does not fix.
   (d) On a hit, emit `{"skill": <entry>}` with `ResponseDisposition::Complete` so
   the result carries `resultType: "complete"`. Do NOT name `Cacheable::Yes`: the
   draft explicitly leaves the caching question open for `skills/get` (SEP line
   359). Name the non-cacheable claim at this call site and say in the rustdoc that
   the draft leaves it open, so a later phase can change it with a stated reason.
   Add no row to `request_is_cacheable` for either method — its `match` has no
   wildcard arm and its rustdoc calls such a row a lie about where the claim is
   made.

3. `src/server/mod.rs`: add `pub(crate) fn handle_skills_get` as a thin delegate
   over the shared projection, identical in shape to the `handle_skills_list` the
   tracer wrote. Zero logic; all gates live in `core.rs`.

4. `src/server/streamable_http_server.rs`: add `HttpIngress::SkillsGet { id, params }`
   at all five sites — variant declaration, the `is_initialize` `false` alternation,
   the `classify_http_ingress` fast-reject condition plus inner match arm, the v2
   header-gate alternation with no `method_override`, and the TWO per-path
   response-assembly arms via `assemble_skills_get_fast` and
   `assemble_skills_get_with_middleware`. If either assembler's argument list
   reaches eight, bundle the router inputs into a call struct as `TasksUpdateCall`
   does — that bundling exists because the count was measured against
   `clippy::too_many_arguments`, not anticipated.

5. `tests/skills_routing.rs`: add live-wire tests for every row of this task's
   `<behavior>` block. The unknown-URI, reference-URI, malformed-params and
   traversal-shaped-URI cases must each assert the numeric error code -32602
   explicitly, not merely that an error was returned. Add a control assertion in
   the same file that `resources/read` on an unknown URI still returns its
   pre-existing -32601 — the divergence is deliberate and must be visible as such
   rather than looking like an inconsistency someone should "fix".

   Reuse the 125-01 fixture builder, which carries the explicit `[V1, V2]` accept
   list; a fixture without it can be refused by the version gate before skills
   dispatch. Every fixture skill authored here uses frontmatter whose `name` equals
   its URI's final segment: 125-03 lands a build-time rejection for that mismatch in
   the SAME wave, and a violating fixture here would surface as a failure inside a
   plan that did not author it (R-19).

6. Add the gate-ordering test (R-16). Build a second server that carries an auth
   provider, POST a malformed-params `skills/get` with no credentials, and assert the
   response is the authentication refusal rather than -32602; then repeat WITH
   credentials and assert -32602. The classifier's raw-params discipline is the
   mechanism that makes this true, but the mechanism is not the proof — a future
   refactor that deserialized in the classifier would leak a params error to an
   unauthenticated caller while every existing test still passed.

7. Add the cache-attribute absence test (R-17): assert the `skills/get` result
   carries neither `ttlMs` nor `cacheScope`, on a v1 framing and on a v2 framing.
   `src/types/caching.rs:196-214` is the single writer of both keys, and it is total
   — every input either ensures both or removes both — so asserting absence on the
   v2 framing is a real check on the disposition passed at the projection call site,
   not a tautology.
  </action>

  <verify>
    <automated>cargo test --all-features --test skills_routing -- --test-threads=1</automated>
    <fails_when>non-zero exit, or "running 0 tests" / "0 passed" in the output</fails_when>
    <automated>bash -o pipefail -c "cargo test --all-features --test skills_routing -- --test-threads=1 2>&amp;1 | grep -c 'auth\|cache'"</automated>
    <fails_when>the printed count is 0 — the gate-ordering test and the cache-attribute absence test must both appear by name in the reported set (R-16, R-17)</fails_when>
    <automated>cargo test --all-features --test skills_integration -- --test-threads=1</automated>
    <fails_when>non-zero exit, or "running 0 tests" — in particular `resources_read_unknown_uri_method_not_found` must still pass unchanged</fails_when>
    <automated>cargo test -p pmcp --all-features --lib classify_internal_method -- --test-threads=1</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>bash -o pipefail -c "cargo clippy --all-targets --all-features -- -D warnings 2>&amp;1 | tail -5"</automated>
    <fails_when>non-zero exit, or any line containing "error:" or "warning:" attributable to src/server/streamable_http_server.rs or src/server/core.rs</fails_when>
  </verify>

  <acceptance_criteria>
    - A test asserts a `skills/get` response for an unregistered URI carries error code exactly -32602.
    - A test asserts a `skills/get` response for a registered skill's reference-file URI carries error code exactly -32602.
    - A test asserts `resources/read` on an unknown URI still carries error code exactly -32601, documenting the divergence as intentional.
    - A test asserts the `skills/get` result carries neither a `ttlMs` nor a `cacheScope` key, on both a v1 and a v2 framing (R-17).
    - A test asserts an uncredentialed malformed-params `skills/get` against an auth-carrying server returns the authentication refusal, AND that the same request with credentials returns -32602 (R-16). Both halves must be in the reported set.
    - Method strings are single-sourced structurally, not by occurrence count (R-18): every dispatch-relevant use of either method string in `src/server/core.rs` resolves through the `pub(crate)` constants introduced in 125-01. Check the failure this defends against directly — `grep -n '"skills/list"\|"skills/get"' src/server/core.rs` returns only lines inside rustdoc or `#[cfg(test)]` blocks, and no line inside a `match` guard or an `if` condition in production code. A whole-file count is rejected as the check: it cannot distinguish a rustdoc mention from a re-typed dispatch literal, which is the only thing that would actually be wrong.
    - `src/server/core.rs`'s `request_is_cacheable` function body is unchanged: `git diff --stat src/server/core.rs` shows additions, and the `request_is_cacheable` match arms are byte-identical to the pre-plan version.
    - `HttpIngress::SkillsGet` appears at five distinct sites in `src/server/streamable_http_server.rs`: `grep -c 'HttpIngress::SkillsGet' src/server/streamable_http_server.rs` returns at least 5.
    - `cargo build --all-features` exits 0 — the deliberately-exhaustive inner `match` in `classify_http_ingress` compiled, proving no site was skipped.
  </acceptance_criteria>

  <done>
A live server answers `skills/get` with a conforming single entry, and returns
-32602 for an unknown URI, a reference URI, malformed params and a traversal-shaped
URI — while the pre-existing `resources/read` -32601 behavior is untouched and
pinned. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Projection coverage, the ServerCore boundary, and a guard against re-adding dead code</name>

  <files>src/server/core.rs, src/server/builder.rs, tests/skills_routing.rs</files>

  <read_first>
    - src/server/core.rs:70-101 — the `ProtocolHandler` trait's `handle_request(&self, id, request: Request, auth_context)` signature. This is the ONLY request ingress a `ServerCore` has, and `Request` is the public exhaustive enum the phase deliberately adds no variant to. Read this BEFORE writing anything in this task; it is the fact the whole task turns on.
    - src/server/core.rs:3763-3790 — the `impl ProtocolHandler for ServerCore` block using exactly that signature.
    - src/server/core.rs:2355-2440 — `build_discover_response`, its rustdoc naming `Server::handle_discover` -> `HttpIngress::Discover` as the route, and the twin-site parity note. Observe what is NOT there: there is no `ServerCore::handle_discover`. Phase 112 removed it.
    - src/server/core.rs:5280-5380 — the existing in-module unit tests that call `build_discover_response` directly with a constructed argument set. This is the shape the two skills projection tests copy.
    - src/server/builder.rs:1426-1455 — `finalize_skills_resources` as 125-01 changed it to return a tuple, and its `ServerCoreBuilder::build` call site at :1356-1360 including the `#[cfg]` / `#[cfg(not)]` pair.
    - src/server/builder.rs:433-525 — `ServerCoreBuilder::skills` and `try_skills`, which DO exist and DO register skills as resources. That surface is real and must keep working; only the METHOD reach is absent.
    - tests/common/duplex.rs:325-350 — `raw_via_core(core: Arc<dyn ProtocolHandler>, request: Request)`, the one harness that accepts a `ServerCore`. Note it also takes a typed `Request`, which is why it can drive `resources/list` but can never drive `skills/list`.
    - 125-RESEARCH.md `### Pitfall 6: Two build paths, two places to thread the entries` — read it, then read the R-13 disposition above. Pitfall 6's PREMISE (both build paths need entries) is superseded by measurement: only one build path has a route by which entries could be answered. Preserve its cfg-asymmetry warning, which remains correct.
  </read_first>

  <behavior>
    - A `ServerCoreBuilder`-built `ServerCore` carrying a skills registry answers a typed `resources/list` request with the registry's SKILL.md URIs, and a typed `resources/read` on one of them with that skill's body — proving the 125-01 tuple change did not break the `ServerCoreBuilder` skills path.
    - `build_skills_list_response`, called directly with a populated entry map, produces a result carrying `resultType: "complete"`, one element per entry, and no cursor key.
    - `build_skills_get_response`, called directly, produces the single-entry result on a hit and the -32602 error envelope on a miss.
    - A source-scan test FAILS if `src/server/core.rs` ever declares a `skill_entries` field on `ServerCore` or a `ServerCore` method named for either skills method.
    - `cargo build --no-default-features` succeeds, proving the `#[cfg(not(...))]` arm of the `ServerCoreBuilder` call site was updated alongside the 125-01 tuple.
  </behavior>

  <action>
Settle the `ServerCore` question with the true statement rather than the convenient
one, and leave a guard so the convenient one cannot come back.

**Why this task no longer adds `ServerCore` delegates.** A `ServerCore`'s only
request ingress is `ProtocolHandler::handle_request`, which accepts the typed public
`Request` enum (`src/server/core.rs:80`; `ServerCore` impl at `:3764`). This phase
deliberately adds no skills variant to that enum — that is ROADMAP success criterion
1 and the whole reason the classifier route exists. The streamable-HTTP transport,
which is where the classifier route lands, contains **zero** references to
`ServerCore` (`grep -c 'ServerCore' src/server/streamable_http_server.rs` returns 0);
it dispatches through the high-level `Server`. So a `ServerCore::handle_skills_list`
would be a `pub(crate)` function that nothing could ever call, and the entries field
feeding it would be a field nothing could ever read. Phase 112 reached this
conclusion for `server/discover` and acted on it: the `ServerCore` discover wrappers
were deleted, the projection was consolidated into one free function, and no
`#[allow(dead_code)]` was left behind. `handle_tasks_update` followed the same shape.
This task follows that precedent.

1. `src/server/builder.rs`: at the `ServerCoreBuilder::build` call site (:1356-1360),
   destructure the tuple `finalize_skills_resources` now returns and bind the entries
   half to a discarding pattern. Do NOT store them. Write a comment at that site — in
   the file's existing explanatory style, not a marker — stating that a `ServerCore`
   has no ingress by which a skills method could arrive, naming the
   `ProtocolHandler::handle_request` signature as the reason and the Phase-112
   `server/discover` consolidation as the precedent, so the next reader understands
   the discard is a decision rather than an oversight. Update the `#[cfg(not(...))]`
   arm to match the new tuple shape so the non-skills build keeps compiling.

2. `src/server/core.rs`: add NOTHING to the `ServerCore` struct and NO `ServerCore`
   method. Instead add a rustdoc paragraph to `build_skills_list_response` (which
   125-01 created here) mirroring `build_discover_response`'s route sentence: name
   `Server::handle_skills_list` -> `HttpIngress::SkillsList` as the route, and state
   explicitly that there is no `ServerCore` route and why.

3. `src/server/core.rs` in-module tests: add direct unit tests for
   `build_skills_list_response` and `build_skills_get_response`, constructed the way
   the existing `build_discover_response` tests at :5280-5380 are. Cover the list
   shape (complete disposition, one element per entry, no cursor), the get hit, and
   the get miss returning -32602. These give both projections coverage that does not
   depend on any transport — which is what "projection parity" honestly means, as
   distinct from the wire parity the earlier revision of this plan claimed.

4. `tests/skills_routing.rs`: add the `ServerCoreBuilder` resource-surface test.
   Build a `ServerCore` through `ServerCoreBuilder` with the same registry the
   `Server` fixtures use, and drive it through `common::duplex::raw_via_core` with a
   typed `resources/list` request, asserting the SKILL.md URIs come back; then a
   typed `resources/read` asserting the body. This is the reachable half of the old
   parity claim and the one that actually regresses if the tuple change is done
   wrong. Write the test's rustdoc to state what it does NOT prove: a `ServerCore`
   answers no skills METHOD, and that limit is recorded in 125-05's deferral list.

5. `tests/skills_routing.rs`: add the dead-code guard. Read `src/server/core.rs` from
   disk and assert it declares neither a `skill_entries` field on `ServerCore` nor a
   `ServerCore` method named for either skills method. A future contributor reading
   the phase summary could reasonably conclude the delegates were forgotten rather
   than deliberately omitted; this test is the record that they were omitted on
   purpose, and it fails loudly if someone re-adds them. Its rustdoc names the
   `ProtocolHandler::handle_request` signature as the fact that would have to change
   first, so the test tells a future widener exactly what to do rather than merely
   blocking them.
  </action>

  <verify>
    <automated>cargo test --all-features --test skills_routing -- --test-threads=1</automated>
    <fails_when>non-zero exit, or "running 0 tests" / "0 passed" in the summary line</fails_when>
    <automated>bash -o pipefail -c "cargo test --all-features --test skills_routing -- --test-threads=1 2>&amp;1 | grep -c 'core'"</automated>
    <fails_when>the printed count is 0 — the `ServerCoreBuilder` resource-surface test and the dead-code guard must both appear by name in the reported set</fails_when>
    <automated>cargo test -p pmcp --all-features --lib build_skills -- --test-threads=1</automated>
    <fails_when>non-zero exit, or "running 0 tests" — the two direct projection unit tests in core.rs must run</fails_when>
    <automated>cargo test -p pmcp --all-features --lib server::builder -- --test-threads=1</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>bash -o pipefail -c "cargo build --no-default-features 2>&amp;1 | tail -5"</automated>
    <fails_when>non-zero exit — proves the `#[cfg(not(...))]` arm of the `ServerCoreBuilder` call site was updated alongside the tuple</fails_when>
    <automated>bash -o pipefail -c "cargo build --all-features 2>&amp;1 | tail -5"</automated>
    <fails_when>non-zero exit, or any line containing "error[" — in particular an `unused variable` or `dead_code` denial would mean the entries discard was written as a binding rather than a discarding pattern</fails_when>
    <automated>cargo test --all-features -- --test-threads=1</automated>
    <fails_when>non-zero exit, or any line beginning with "failures:" — this plan's single full CI-matching run, on its last task per R-11</fails_when>
  </verify>

  <acceptance_criteria>
    - <!-- planner-discipline-allow: skill_entries --> `src/server/core.rs` declares no `skill_entries` field and no `ServerCore` method named for either skills method, checked over CODE lines only: `sed -n '/impl ServerCore/,$p' src/server/core.rs | grep -v '^\s*//' | grep -c 'fn handle_skills'` returns 0, and `grep -v '^\s*//' src/server/core.rs | grep -c 'skill_entries'` returns 0. The comment filter is load-bearing here rather than defensive: this task's own `<action>` directs an explanatory comment at the `ServerCoreBuilder::build` discard site, and the natural way to write it names the field that is deliberately absent. An unfiltered count would turn the plan's own documentation instruction into a gate failure.
    - A `#[test]` in `tests/skills_routing.rs` reads `src/server/core.rs` from disk and fails if either of those appears, and its rustdoc names `ProtocolHandler::handle_request`'s typed-`Request` signature as the precondition a future widener must change first.
    - A test drives a `ServerCoreBuilder`-built core through `raw_via_core` with a typed `resources/list` and a typed `resources/read`, asserting the registry's SKILL.md URIs and one body — the reachable half of the retired parity claim.
    - Direct unit tests for `build_skills_list_response` and `build_skills_get_response` exist in `src/server/core.rs`'s own test module and cover the list shape, a get hit, and a get miss returning -32602.
    - Each projection fn is DEFINED exactly once: `grep -c 'fn build_skills_list_response' src/server/core.rs` returns 1 and `grep -c 'fn build_skills_get_response' src/server/core.rs` returns 1.
    - The `ServerCoreBuilder::build` entries discard carries an explanatory comment naming the ingress reason and the Phase-112 precedent, and contains none of the tokens TODO, FIXME, HACK or XXX.
    - `cargo build --no-default-features` exits 0.
    - `cargo test --all-features -- --test-threads=1` exits 0 with no newly failing test relative to the 125-01 baseline.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Omitting the `ServerCore` delegates removes nothing that exists and adds no public
    surface. Widening the reach later — the path the dead-code guard's rustdoc points
    at — is the additive change the `protocol_helpers.rs` seam rustdoc already
    describes as non-semver-breaking. Reversal is adding code, not removing it.
  </reversibility>

  <done>
Both projections have direct unit coverage; a `ServerCoreBuilder`-built core is
proven to still serve its skills as resources; the `ServerCore` method boundary is
measured, guarded against silent re-addition, and routed to 125-05's deferral
record. Committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> `skills/get` `params.uri` | Fully attacker-controlled string used as a lookup key. This is the phase's only caller-supplied identifier. |
| HTTP client -> `skills/get` `params` body | Arbitrary JSON, including non-objects and absent params. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-125-06 | Tampering / Information disclosure | `build_skills_get_response` URI lookup | high | mitigate | Exact-match lookup in the entry `IndexMap` keyed by SKILL.md URI. No path join, no normalization, no percent-decode. Registration-time `validate_reference_path` already rejects `..`, leading `/`, `://` and null bytes; the lookup side does not re-open that. Asserted by the traversal-shaped-URI test returning -32602. |
| T-125-07 | Elevation of privilege | Gate-ordering inversion in the classifier | high | mitigate | `classify_internal_method` clones `params` and deserializes nothing, so a malformed `skills/get` body becomes a -32602 in the served branch AFTER the header/auth pipeline, never a parse error that would leak a params error to an unauthenticated caller. Asserted by the non-object-params classifier test. |
| T-125-08 | Information disclosure | Error message echoing the caller's URI | medium | mitigate | The -32602 message must not echo the raw caller-supplied URI unbounded (ASVS V7); truncate or omit it. |
| T-125-09 | Denial of service | Unbounded `skills/get` params | low | accept | Params are cloned once and read for a single string key; the frame size bound is the transport's existing body limit, unchanged by this plan. |
| T-125-10 | Spoofing / Session fixation | `HttpIngress::SkillsGet` minting a session | high | mitigate | The new variant joins the `is_initialize` `false` alternation, same as its `SkillsList` sibling (ASVS V3). |
</threat_model>

<verification>
- `cargo test --all-features -- --test-threads=1` exits 0 (matches CI). Run once, on Task 2.
- `cargo clippy --all-targets --all-features -- -D warnings` exits 0.
- `cargo build --no-default-features` exits 0.
- `cargo test --all-features --test skills_integration -- --test-threads=1` exits 0 with `resources_read_unknown_uri_method_not_found` still passing — this plan changes no existing error code.

**Wave 2 merge sequencing (R-19).** This plan and 125-03 run in parallel and share
no `files_modified` (verified: this plan touches `protocol/mod.rs`,
`streamable_http_server.rs`, `core.rs`, `mod.rs`, `builder.rs`, `tests/skills_routing.rs`;
125-03 touches `src/server/skills.rs` and `tests/skills_integration.rs`). They are
NOT independent in behavior: 125-03 makes a frontmatter-name/URI mismatch a
build-time `Err` from `into_handler`, and every server fixture in this plan is built
through `into_handler`. Two consequences the executor must hold:

1. Every skill fixture authored here uses frontmatter whose `name` equals the final
   `/` segment of its resolved URI. A violating fixture compiles and passes while
   125-03 is unmerged, then fails afterwards inside a test this plan owns — a
   failure signal that points at the wrong plan.
2. Merge 125-03 before or with this plan, then re-run
   `cargo test --all-features --test skills_routing -- --test-threads=1` before
   declaring Wave 2 green. A green run of this plan's suite taken before 125-03
   lands does not carry over.
</verification>

<success_criteria>
- Both mandatory methods are answered over streamable HTTP.
- `skills/get` uses -32602 for every unresolvable-URI and malformed-params case, and the auth gate is proven to run first.
- The `skills/get` result carries neither caching key, on both eras.
- The `ServerCore` boundary is measured, tested in its reachable half, guarded against dead-code re-addition, and recorded as a deferral — not claimed as parity it cannot have.
- No row is added to `request_is_cacheable`.
</success_criteria>

<output>
Create `.planning/phases/125-sep-2640-conformance-skills-list-skills-get/125-02-SUMMARY.md` when done.
</output>
