---
phase: 118-conformance-against-the-official-suite
plan: 09
type: execute
wave: 4
depends_on: ["118-07", "118-08"]
files_modified:
  - .github/workflows/ci.yml
  - tests/ci_conformance_gate_wiring.rs
  - tests/ci_severance_gate_wiring.rs
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]

must_haves:
  truths:
    - "A failing official-suite run turns the org-required `gate` check RED"
    - "A failing era-matrix run turns the org-required `gate` check RED"
    - "Each new job is wired into `gate` in all FOUR places: needs, the env binding, the if chain, and the failure echo"
    - "The wiring is proved by PARSING .github/workflows/ci.yml with serde_yaml, never by string-matching it"
    - "The `feature-flags` job remains absent from gate.needs, keeping the file's live negative control honest"
    - "The suite runs on Node 22 provisioned by actions/setup-node — the first setup-node in this repo"
    - "The -o results/ artifact is uploaded on GREEN runs too, so not-scored results stay reviewable at re-pin time"
    - "The Phase-109 team-servers harness is EXECUTED by a blocking CI job — today it runs in none"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "two new blocking jobs plus the four gate edits for each"
      contains: "setup-node"
    - path: "tests/ci_conformance_gate_wiring.rs"
      provides: "structural proof that both jobs block merge and that the scripts carry their fences"
      contains: "MINIMUM_GATE_NEEDS"
      min_lines: 300
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/run-conformance-suite.sh"
      via: "a one-line run: step, so the commands stay pinnable as data"
      pattern: "run-conformance-suite.sh"
    - from: "tests/ci_conformance_gate_wiring.rs"
      to: ".github/workflows/ci.yml"
      via: "serde_yaml parse, then env-binding / if-chain mutual-consistency assertions"
      pattern: "serde_yaml"
---

<objective>
Make the conformance claim capable of failing. Add two BLOCKING CI jobs — one for the official suite
(CONF-01), one for the era matrix (CONF-02/CONF-03) — wire each into the `gate` aggregate in all
four required places (D-02), and prove that wiring STRUCTURALLY from the workflow file rather than
asserting it.

Purpose: *a conformance claim that cannot fail is not a claim.* Two measured facts make this the
riskiest plan in the phase. First, `gate.needs` does NOT list `security_audit` or `workspace-test`;
both were RED on PR #319 while `gate` was GREEN, so adding a job to `ci.yml` does NOT make it block.
Second, the Phase-109 team-servers harness runs in **zero** CI jobs today: `ci.yml`'s `test` job is
scoped to the root `pmcp` package, `org-gate-checks.yml`'s `workspace-test` is `--lib --bins` (which
excludes `tests/`) and is itself absent from `gate.needs`, and `make quality-gate` never reaches
`crates/pmcp-team-servers/tests/`. D-15 puts CONF-02's execution in the NEW job for exactly that
reason, keeping the deferred `gate.needs` widening genuinely deferred.

Output: two blocking jobs, eight gate edits, a structural wiring tripwire, and two raised floors in
the existing severance wiring test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-02-SUMMARY.md
@.planning/phases/118-conformance-against-the-official-suite/118-08-SUMMARY.md

<interfaces>
<!-- Current state of ci.yml, measured. Do NOT re-derive. -->

Jobs present today (10): test, feature-flags, quality-gate, benchmarks, msrv, purity-check,
v1-severance, pmcp-agent-targets, wasm32-purity, gate.

The gate job's current shape:

  gate:
    runs-on: ubuntu-latest
    needs: [test, quality-gate, purity-check, pmcp-agent-targets, wasm32-purity, v1-severance]
    if: always()
    steps:
      - name: Evaluate required checks
        env:
          TEST_RESULT / QG_RESULT / PURITY_RESULT / AGENT_TARGETS_RESULT / WASM32_RESULT /
          SEVERANCE_RESULT, each bound to a `needs.<job>.result` expression
        run: |
          a shell `if` chain comparing every bound variable to "success", an echo naming every job
          and its result, then `exit 1`; a trailing "All required checks passed." echo

FOUR edits per new job — the echo is the fourth, and ci_severance_gate_wiring.rs:588-596 asserts it:
  1. append the job name to `needs:`
  2. add a `<NAME>_RESULT` binding to `env:` bound to that job's result expression
  3. add a `[[ "$<NAME>_RESULT" != "success" ]]` clause to the `if` chain
  4. add `<job>=$<NAME>_RESULT` to the `Required checks failed: ...` echo

The blocking-job template (`v1-severance`, ci.yml:395-433): actions/checkout@v7,
dtolnay/rust-toolchain@stable, actions/cache@v6 with a JOB-PRIVATE cache key plus restore-keys, a
50-line numbered rationale comment above a 39-line job body, and a one-line `run:` that calls the
script.

Existing constants in tests/ci_severance_gate_wiring.rs that must be RAISED:
  const MINIMUM_GATE_NEEDS: usize = 6;   ->  8  (two new jobs)
  const MINIMUM_JOBS: usize = 8;         -> 10
  const NON_BLOCKING_JOB: &str = "feature-flags";   // the LIVE negative control — keep it OUT

Reusable elements of tests/ci_severance_gate_wiring.rs (662 lines) to transfer:
  - the workflow parsed with `serde_yaml`, NEVER string-matched (a comment would "find" a job name)
  - `step_script_containing(job, needle)` asserting EXACTLY ONE matching `run:` step — fences are
    properties of a COMMAND, not of a job
  - comment-stripping before forbidden-flag checks
  - the `raw` vs `floored` reader split (`gate_needs_raw` / `gate_needs`)
  - the mutual-consistency test: the env var that is BOUND must be the one that is READ, and the
    failure echo must name the job
  - the general invariant `env.len() >= needs.len()`, measured against the ACTUAL length
  - the live negative control asserting `feature-flags` stays out of `gate.needs`

`serde_yaml = "0.9"` is a root dev-dependency (Cargo.toml:194). A shelled-out interpreter (PyYAML)
is REJECTED — the rejection is written out in ci_severance_gate_wiring.rs:44-59. Do not use a
`python3 -c "import yaml"` one-liner anywhere, including in a verification command.

Artifact upload: `ci.yml` has NO `upload-artifact` step at all. The nearest in-repo shape is
.github/workflows/fuzz.yml:69-81, which uses `if: failure()` — the WRONG condition for D-14, which
needs the not-scored `extension`/`pending` results visible on GREEN runs too.

No `actions/setup-node` exists anywhere in `.github/workflows/` — this is the first.

Packaging disposition, already decided and landed by plan 118-02: `conformance/` and
`tests/ci_conformance_gate_wiring.rs` are BOTH in the root Cargo.toml `exclude` array
("ship both or exclude both"). This plan VERIFIES that and must not change it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two blocking CI jobs, each with a numbered rationale block</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — read lines 311-440 (the `purity-check` job and the whole
      `v1-severance` job INCLUDING its 50-line rationale comment) and lines 525-560 (the `gate`
      job). You are editing this file; read its current state rather than assuming it.
    - .github/workflows/fuzz.yml — lines 60-85, the only `upload-artifact` shape in the repo, and
      the `if: failure()` condition that is WRONG to copy here.
    - scripts/run-conformance-suite.sh and scripts/run-era-matrix.sh — the two scripts from plan
      118-08, so the `run:` steps invoke them and nothing else.
    - conformance/package.json — the `engines.node` floor the job must satisfy.
    - .planning/phases/118-conformance-against-the-official-suite/118-08-SUMMARY.md — the scripts'
      environment requirements and the port they bind.
  </read_first>
  <behavior>
    Job `conformance-suite`:
      checkout -> actions/setup-node@v4 (node-version 22) -> install Rust -> cache cargo with a
      job-private key -> `./scripts/run-conformance-suite.sh` as a ONE-LINE run step -> upload the
      results directory with `if: always()`. `PMCP_REQUEST_STATE_KEY` supplied as job-level `env:`
      from a fixed non-production value or a repository secret, never echoed.

    Job `era-matrix`:
      checkout -> install Rust -> cache cargo with its own job-private key ->
      `./scripts/run-era-matrix.sh` as a ONE-LINE run step.

    Neither job carries `continue-on-error`, `|| true`, or any conditional that could let it pass
    without running.
  </behavior>
  <action>
Add two jobs to `.github/workflows/ci.yml`, placed after `v1-severance` and before `gate`, each
preceded by a numbered rationale comment block in the style of `ci.yml:344-394`.

`conformance-suite` rationale block must number and name at least these fences and their
false-greens:
  1. `actions/setup-node@v4` with `node-version: 22` — the first `setup-node` in this repo. The
     suite imports `globSync` from `node:fs` at module scope and the published package declares NO
     `engines`, so a Node-20 runner passes `npm ci` and dies at load with a `SyntaxError`. Do not
     rely on the runner's default Node.
  2. `npm ci`, not `npm install` — the committed `conformance/package-lock.json` and its integrity
     hashes are the reproducibility mechanism (D-01).
  3. The commands live in `scripts/run-conformance-suite.sh`, not inline, so
     `tests/ci_conformance_gate_wiring.rs` can pin them AS DATA — including the vacuity guards,
     which cannot live inside a `#![cfg]`-selected test file.
  4. `--requirements`, not `--spec-version`, and no `--expected-failures` — D-03 admits no
     known-fail allowlist; D-14 fixes the boundary at the suite's SCORED set.
  5. ONE server process for both runs (D-06), because two processes would prove "pmcp can serve v1"
     and "pmcp can serve v2" separately rather than that one binary does both.
  6. `if: always()` on the artifact upload, NOT `if: failure()` — D-14 needs the not-scored
     `extension`/`pending` results reviewable on GREEN runs, which is precisely when a
     failure-conditioned upload would skip.
  7. This job is in `gate.needs` and therefore BLOCKS merge; it must never be made advisory.

`era-matrix` rationale block must number and name at least:
  1. Why this job exists at all: **no CI job runs `crates/pmcp-team-servers/tests/` today.** The
     `test` job is scoped to the root `pmcp` package; `org-gate-checks.yml`'s `workspace-test` is
     `--lib --bins`, which excludes `tests/`, and is itself absent from `gate.needs`;
     `make quality-gate` never reaches that crate. Adding fixtures to a harness nothing executes
     gates nothing.
  2. Why it is NOT wired by widening `workspace-test`: that job's absence from `gate.needs` is an
     explicitly DEFERRED item, and D-15 keeps it deferred by giving CONF-02 a gate it owns.
  3. The dev-dependency-free build fence and the nonzero-test-count guard live in the script,
     outside the compilation unit, because a `cfg!`-based guard inside a `#![cfg]`-selected file
     expands to `!false` and cannot fail.

Copy from the `v1-severance` template exactly: `actions/checkout@v7`,
`dtolnay/rust-toolchain@stable`, `actions/cache@v6` with a JOB-PRIVATE cache key
(`${{ runner.os }}-cargo-conformance-` and `${{ runner.os }}-cargo-era-matrix-`) plus
`restore-keys`, and a one-line `run:` calling the script.

For the artifact upload use `actions/upload-artifact@v7` with `if: always()`, a name including the
run identifier, and the results path the script writes.

Supply `PMCP_REQUEST_STATE_KEY` as a job-level `env:` mapping from a fixed non-production
64-character test value or a repository secret. Never `echo` it and never place it on a command line.

Do NOT add `security_audit` or `workspace-test` to anything — both are explicitly deferred and out
of scope for this phase.
  </action>
  <verify>
    <automated>grep -c '^  conformance-suite:' .github/workflows/ci.yml; grep -c '^  era-matrix:' .github/workflows/ci.yml; grep -c 'actions/setup-node' .github/workflows/ci.yml; cargo test --features full --test ci_severance_gate_wiring 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  conformance-suite:' .github/workflows/ci.yml` returns 1 and
      `grep -c '^  era-matrix:' .github/workflows/ci.yml` returns 1.
    - `grep -c 'actions/setup-node@v4' .github/workflows/ci.yml` is >= 1 and the adjacent
      `node-version` value is `22` or higher.
    - `grep -c 'actions/upload-artifact' .github/workflows/ci.yml` is >= 1, and the same step
      carries `if: always()` — NOT `if: failure()`. State the line numbers in the SUMMARY.
    - `grep -cE 'continue-on-error|\|\| true' .github/workflows/ci.yml` does not increase relative to
      `git show HEAD:.github/workflows/ci.yml | grep -cE 'continue-on-error|\|\| true'`; record both
      numbers.
    - Each new job's `run:` steps invoke exactly the two scripts and nothing else:
      `grep -c 'run-conformance-suite.sh' .github/workflows/ci.yml` is >= 1 and
      `grep -c 'run-era-matrix.sh' .github/workflows/ci.yml` is >= 1.
    - The cache keys are job-private: `grep -c 'cargo-conformance-' .github/workflows/ci.yml` is
      >= 1 and `grep -c 'cargo-era-matrix-' .github/workflows/ci.yml` is >= 1.
    - `PMCP_REQUEST_STATE_KEY`'s value is never echoed: no line in `ci.yml` both names the variable
      and contains `echo`. State the audited lines in the SUMMARY.
    - `git diff .github/workflows/ci.yml | grep -cE '^\+.*(security_audit|workspace-test)'` returns 0.
    - The workflow still parses: `cargo test --features full --test ci_severance_gate_wiring` exits
      0 — its `serde_yaml` reader is the parse check. Do NOT introduce a PyYAML one-liner.
  </acceptance_criteria>
  <done>Both jobs exist with numbered rationale blocks, provision Node 22, invoke only the two scripts, upload results on every run, and carry no advisory escape hatch.</done>
</task>

<task type="auto">
  <name>Task 2: The eight gate edits — needs, env, if chain, and failure echo, twice</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — the `gate` job as it stands after Task 1, lines ~525-560.
    - tests/ci_severance_gate_wiring.rs — lines 551-597, the test that asserts wirings 2 and 3 for
      MUTUAL CONSISTENCY (the env var that is BOUND must be the one that is READ) and asserts the
      failure echo names the job. That test defines what "correctly wired" means; read it before
      editing so the edits satisfy it by construction.
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `.github/workflows/ci.yml — the THREE gate edits`, which states that it is actually FOUR and
      warns that `MINIMUM_GATE_NEEDS` and `MINIMUM_JOBS` ripple into the existing severance test.
  </read_first>
  <behavior>
    After this task, `gate.needs` has 8 entries; `gate`'s evaluation step has 8 env bindings, an
    8-clause `if` chain, and a failure echo naming all 8 jobs. Every bound variable is read, and
    every read variable is bound.
  </behavior>
  <action>
Apply all FOUR edits for EACH of the two new jobs to the `gate` job.

1. `needs:` — append `conformance-suite` and `era-matrix`, taking the list from 6 entries to 8.
2. `env:` — add a `CONFORMANCE_RESULT` binding to the `conformance-suite` job's result expression
   and an `ERA_MATRIX_RESULT` binding to the `era-matrix` job's result expression, following the
   exact form the existing six bindings use.
3. The shell `if` chain — add `[[ "$CONFORMANCE_RESULT" != "success" ]]` and
   `[[ "$ERA_MATRIX_RESULT" != "success" ]]` clauses, preserving the existing line-continuation
   style so the final clause remains the one without a trailing continuation.
4. The `Required checks failed: ...` echo — append `conformance-suite=$CONFORMANCE_RESULT` and
   `era-matrix=$ERA_MATRIX_RESULT` so a failing gate names the cause.

Adding to `needs:` alone produces an *awaited but unchecked* job, which is strictly worse than not
adding it at all: the gate waits for the job and then ignores its result. All four edits or none.

Do not reorder or rename the existing six entries, and do not touch `if: always()` on the gate.
  </action>
  <verify>
    <automated>grep -A 32 '^  gate:' .github/workflows/ci.yml | grep -cE 'conformance-suite|era-matrix|CONFORMANCE_RESULT|ERA_MATRIX_RESULT'</automated>
  </verify>
  <acceptance_criteria>
    - The `gate` job's `needs:` list contains exactly 8 entries including `conformance-suite` and
      `era-matrix`; quote the line in the SUMMARY.
    - `grep -c 'CONFORMANCE_RESULT' .github/workflows/ci.yml` is >= 3 (env binding, `if` clause,
      echo) and the same for `ERA_MATRIX_RESULT`.
    - The `if` chain's clause count equals the `env` binding count equals 8 — state all three counts
      in the SUMMARY.
    - The failure echo names both new jobs by their workflow job name (`conformance-suite=` and
      `era-matrix=`).
    - `feature-flags` is still ABSENT from `gate.needs`:
      `grep -A 3 '^  gate:' .github/workflows/ci.yml | grep -c 'feature-flags'` returns 0.
    - `security_audit` and `workspace-test` are still absent from `gate.needs` — the deferred item
      stays deferred.
  </acceptance_criteria>
  <done>Both jobs are awaited, bound, evaluated and named in the failure echo, with the existing six wirings and the live negative control untouched.</done>
</task>

<task type="auto">
  <name>Task 3: tests/ci_conformance_gate_wiring.rs, and the two raised floors in the severance test</name>
  <files>tests/ci_conformance_gate_wiring.rs, tests/ci_severance_gate_wiring.rs</files>
  <read_first>
    - tests/ci_severance_gate_wiring.rs — read the WHOLE file (662 lines). It is the single
      highest-value file to copy in this phase. Take: the module doc's headings
      (`CORRECTION-116-DOC`, the live counter-example, why THREE wirings not one, why the workflow
      is PARSED not string-matched, and why the interpreter route was REJECTED); the named constants
      and their non-vacuity floors; `parse_workflow` with its actionable panic;
      `step_script_containing` and why it exists; `proof_script_commands`'s comment-stripping; the
      `gate_needs_raw` / `gate_needs` split; the mutual-consistency test at 551-597; the live
      negative control at 603-625; and the `env.len() >= needs.len()` invariant at 631-662.
    - .github/workflows/ci.yml — post-Task-2 state.
    - scripts/run-conformance-suite.sh and scripts/run-era-matrix.sh — the arrays this test pins as
      data (`REQUIREMENT_SETS`, `MATRIX_TESTS`) and the guards it asserts are present.
    - Cargo.toml — confirm `tests/ci_conformance_gate_wiring.rs` is already in the `exclude` array
      (plan 118-02 added it). Do not change that disposition.
  </read_first>
  <behavior>
    `tests/ci_conformance_gate_wiring.rs` must FAIL when:
      - either new job is missing from `ci.yml`
      - either new job is missing from `gate.needs`
      - a job is in `gate.needs` but its result is not bound in `env:`
      - a bound env var is not read in the `if` chain
      - the failure echo does not name a new job
      - `gate.needs` shrinks below `MINIMUM_GATE_NEEDS`
      - the parsed job count drops below `MINIMUM_JOBS`
      - either job stops invoking its script, or invokes it from more than one step
      - `scripts/run-conformance-suite.sh` stops declaring BOTH requirement-set names
      - `scripts/run-era-matrix.sh` stops declaring BOTH matrix test targets
      - a forbidden flag appears in either script's COMMANDS (after comment-stripping)
      - `feature-flags` appears in `gate.needs` (the live negative control)
  </behavior>
  <action>
Create `tests/ci_conformance_gate_wiring.rs`, transferring `tests/ci_severance_gate_wiring.rs`'s
structure section by section, and raise the two floors in that existing file.

Named constants for the new test:
  - `WORKFLOW` / `WORKFLOW_REL` — as in the analog, `CARGO_MANIFEST_DIR`-anchored
  - `CONFORMANCE_JOB: &str = "conformance-suite"`
  - `ERA_MATRIX_JOB: &str = "era-matrix"`
  - `GATE_JOB: &str = "gate"`
  - `NON_BLOCKING_JOB: &str = "feature-flags"` — reuse the SAME live negative control
  - `MINIMUM_GATE_NEEDS: usize = 8`
  - `MINIMUM_JOBS: usize = 10`
  - `CONFORMANCE_SCRIPT_REL: &str = "scripts/run-conformance-suite.sh"` plus its
    `CARGO_MANIFEST_DIR`-anchored absolute twin
  - `ERA_MATRIX_SCRIPT_REL: &str = "scripts/run-era-matrix.sh"` plus its absolute twin
  - `REQUIREMENT_SETS: &[&str] = &["2025-11-25", "2026-07-28"]` — D-04 as DATA; assert both members
    appear in the conformance script
  - `MATRIX_TESTS: &[&str] = &["conformance", "era_baseline"]` — assert both appear in the era script
  - `CONFORMANCE_FORBIDDEN_FLAGS: &[&str] = &["--expected-failures", "--spec-version", "--suite",
    "--all-features"]` — D-03's no-allowlist rule enforced by the analog's forbidden-flag loop
  - `ERA_MATRIX_FORBIDDEN_FLAGS: &[&str] = &["|| true", "continue-on-error"]`
  - `DEV_DEP_FREE_FENCES: &[&str] = &["-p pmcp-team-servers", "--all-features", "cargo build"]` —
    assert the era script carries the dev-dependency-free existence fence (D-09)

Parse the workflow with `serde_yaml` through a `LazyLock` reader with an actionable panic. NEVER
string-match the workflow for a job name: a comment would "find" it.

Transfer these tests, one per new job where applicable:
  - both jobs exist in the parsed `jobs` map
  - both jobs appear in `gate.needs`
  - the mutual-consistency test for each: the env var BOUND to that job's result expression is the
    same variable READ in the `if` chain, and the failure echo names the job. Copy the analog's
    failure-message wording about a job being "AWAITED but NEVER CHECKED"
  - the general invariant `env.len() >= needs.len()`, measured against the ACTUAL length, not the
    const
  - `step_script_containing(job, script_rel)` asserting EXACTLY ONE `run:` step per job invokes its
    script — zero means the command was renamed and every fence below would assert against nothing;
    more than one means a fence could be satisfied by a DIFFERENT command than the one it names
  - the scripts' arrays pinned as data: every member of `REQUIREMENT_SETS` and `MATRIX_TESTS`
    appears in the corresponding script source
  - the forbidden-flag loops, run over COMMENT-STRIPPED script text (both scripts' headers explain
    at length why these flags are forbidden, so an unstripped grep would find the words in the prose
    and self-invalidate)
  - the era script declares the nonzero-test-count guard: assert the literal `running [1-9]` pattern
    appears in its source
  - the live negative control, copied unchanged: `feature-flags` still exists as a job AND is still
    absent from `gate.needs`. Copy the analog's explanation — with every job wired, a reader that
    answered "yes, it's wired" to everything would pass all the tests in the file

Module doc must restate, in the analog's voice: blocking status is proved from the WORKFLOW, never
from the Makefile (`CORRECTION-116-DOC`); the live counter-example and why it is kept; why FOUR
wirings and not one; why the workflow is PARSED and not string-matched; and why the interpreter
route (PyYAML) was rejected — `serde_yaml` is already a root dev-dependency, costing zero new
packages, while PyYAML is an undeclared, unversioned, out-of-band interpreter package.

Then edit `tests/ci_severance_gate_wiring.rs`: raise `MINIMUM_GATE_NEEDS` from 6 to 8 and
`MINIMUM_JOBS` from 8 to 10, updating each doc comment to record that Phase 118 added two jobs and
restating the standing rule — if the floor fires, FIX THE READER or restore the workflow; never
lower the floor. Change nothing else in that file; its `feature-flags` negative control and its
`env.len() >= needs.len()` invariant must keep passing untouched.
  </action>
  <verify>
    <automated>cargo test --features full --test ci_conformance_gate_wiring 2>&1 | tee /tmp/118-09-t3.log | tail -25; grep -qE '^running [1-9][0-9]* tests?$' /tmp/118-09-t3.log; cargo test --features full --test ci_severance_gate_wiring 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features full --test ci_conformance_gate_wiring` exits 0 AND its output contains
      a line matching `^running [1-9][0-9]* tests?$`.
    - `cargo test --features full --test ci_severance_gate_wiring` still exits 0 after the two floors
      are raised.
    - `grep -c 'serde_yaml' tests/ci_conformance_gate_wiring.rs` is >= 1 and
      `grep -ciE 'python|pyyaml|yq ' tests/ci_conformance_gate_wiring.rs` returns 0.
    - `grep -c 'MINIMUM_GATE_NEEDS: usize = 8' tests/ci_conformance_gate_wiring.rs` returns 1 and
      `grep -c 'MINIMUM_GATE_NEEDS: usize = 8' tests/ci_severance_gate_wiring.rs` returns 1.
    - `grep -c 'MINIMUM_JOBS: usize = 10' tests/ci_severance_gate_wiring.rs` returns 1.
    - `grep -c 'feature-flags' tests/ci_conformance_gate_wiring.rs` is >= 1 — the live negative
      control is reused, not dropped.
    - FOUR negative controls, each executed by hand and recorded verbatim in the SUMMARY, then
      reverted: (a) remove `conformance-suite` from `gate.needs` -> the wiring test FAILS naming it;
      (b) remove the `ERA_MATRIX_RESULT` binding from `env:` while leaving the job in `needs:` ->
      the mutual-consistency test FAILS with the "AWAITED but NEVER CHECKED" message; (c) delete
      `2026-07-28` from the script's `REQUIREMENT_SETS` array -> the data pin FAILS; (d) add
      `--expected-failures` to a command line in `run-conformance-suite.sh` -> the forbidden-flag
      loop FAILS. A wiring test that cannot fail is not a wiring test.
    - Packaging disposition unchanged and verified:
      `cargo package -p pmcp --list --allow-dirty | grep -cE 'conformance/|ci_conformance_gate_wiring'`
      returns 0.
    - `make quality-gate` exits 0.
  </acceptance_criteria>
  <done>Both jobs are proved to block merge by a serde_yaml-parsing structural test, all four negative controls were executed and reverted, the severance test's floors are raised, and neither new artifact ships in the crate tarball.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pull request -> `gate` required status check | The gate is the sole org-required check; whatever it does not evaluate cannot block a merge |
| CI job -> npm registry and a spawned server | The conformance job installs and executes third-party JavaScript and binds a port |
| Workflow file -> wiring test | The test's verdict is only as good as its reader; a string-matched read can be satisfied by a comment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-45 | Repudiation | A job added to `ci.yml` that does not actually block merge — the measured PR #319 failure mode | mitigate | All FOUR wirings per job, asserted structurally by a `serde_yaml` parse with mutual-consistency checks (bound var == read var) and by the `env.len() >= needs.len()` invariant; negative control (a) proves the test fires |
| T-118-46 | Repudiation | A job that is AWAITED but never CHECKED — strictly worse than not adding it | mitigate | The mutual-consistency test copied from ci_severance_gate_wiring.rs:551-597; negative control (b) proves it fires |
| T-118-47 | Tampering | A wiring test satisfied by a comment mentioning the job name | mitigate | The workflow is PARSED with `serde_yaml`, never string-matched; `step_script_containing` asserts EXACTLY ONE matching `run:` step so a fence cannot be satisfied by a different command |
| T-118-48 | Tampering | A known-fail allowlist reintroduced in the script to turn a red job green | mitigate | `CONFORMANCE_FORBIDDEN_FLAGS` loop over comment-stripped script text; negative control (d) proves it fires |
| T-118-49 | Elevation of Privilege | An over-permissive CI job token, or a secret exposed to third-party JavaScript | mitigate | Neither new job requests elevated `permissions:`; `PMCP_REQUEST_STATE_KEY` is a fixed non-production test value or a repository secret, is never echoed, and is never placed on a command line; the audited lines are recorded |
| T-118-50 | Tampering | Executing third-party JavaScript in CI from an unpinned source | mitigate | `npm ci` against the committed lockfile pinned and slopcheck-cleared in plan 118-02 (empty postinstall, OIDC-published, `[OK]`); the job never runs `npm install` |
| T-118-51 | Information Disclosure | The uploaded `results/` artifact carrying secrets | mitigate | The artifact path is the suite's own results directory only; the SUMMARY records an inspection of one uploaded artifact confirming it contains no environment dump |
| T-118-52 | Repudiation | The live negative control being lost, so every "is it wired?" answer becomes yes | mitigate | `feature-flags` is reused as the control in the NEW test and asserted to remain both present as a job and absent from `gate.needs` |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | The only install is the plan-118-02 lockfile via `npm ci`; PyYAML is explicitly rejected in favour of the already-resolved `serde_yaml` dev-dependency, so no out-of-band interpreter package is introduced |
</threat_model>

<verification>
- `cargo test --features full --test ci_conformance_gate_wiring` exits 0 with a nonzero test count.
- `cargo test --features full --test ci_severance_gate_wiring` still exits 0 with the raised floors.
- All FOUR negative controls were executed, failed as intended, and were reverted.
- `cargo package -p pmcp --list --allow-dirty` lists neither `conformance/` nor
  `tests/ci_conformance_gate_wiring.rs`.
- `make quality-gate` exits 0.
- On the PR: both new jobs appear as checks, and `gate` is red when either is red — confirm from
  `gh api` check-runs rather than from a piped `gh pr checks`, which is known to corrupt under the
  rtk proxy.
</verification>

<success_criteria>
- The official-suite job and the era-matrix job both BLOCK merge through the org-required `gate`
  check, wired in all four places each.
- The wiring is proved by parsing the workflow, with a live negative control that keeps the proof
  honest, and with four executed failure demonstrations.
- The Phase-109 team-servers harness is executed by CI for the first time.
- The `-o results/` artifact is uploaded on green runs, keeping the not-scored results reviewable at
  re-pin time.
- `security_audit` and `workspace-test` remain untouched — the deferred item stays deferred.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-09-SUMMARY.md` when done.
Record: the final `gate.needs` line verbatim, the three counts (needs / env bindings / if clauses),
all four negative-control transcripts, the `cargo package --list` verification, the audited
`PMCP_REQUEST_STATE_KEY` lines, and the observed check-run states from the PR.
</output>
