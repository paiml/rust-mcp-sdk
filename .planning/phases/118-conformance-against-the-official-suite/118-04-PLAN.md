---
phase: 118-conformance-against-the-official-suite
plan: 04
type: execute
wave: 2
depends_on: ["118-02"]
files_modified:
  - examples/s54_v2_dual_conformance.rs
  - Cargo.toml
autonomous: true
requirements: [CONF-01]

must_haves:
  truths:
    - "One process, started once, serves both MCP 2025-11-25 and 2026-07-28 — the era is decided per request, not per process"
    - "`cargo run --example s54_v2_dual_conformance --features full` starts a server that binds 127.0.0.1:8149 and prints copy-pasteable curl lines for both eras"
    - "The example is registered as a [[example]] with required-features, so a default-feature build does not try — and fail — to compile it"
    - "The bind address can be overridden by argv[1], and the driver fails with an actionable message if the port is already held"
    - "The server starts with NO startup WARNING when PMCP_REQUEST_STATE_KEY is set in the environment"
    - "The official suite's 2025-11-25 scored scenarios that depend on tools, resources and prompts find every fixture they name"
    - "Session machinery is live (StreamableHttpServerConfig::default, not ::stateless), so the v1 session-lifecycle scenario has a real session path to exercise"
    - "All suite output is written under target/conformance-results/, which .gitignore already covers — the worktree stays clean"
  artifacts:
    - path: "examples/s54_v2_dual_conformance.rs"
      provides: "the dual-version conformance target server: accept-list, fixture tools, resources, prompts"
      contains: "PROTOCOL_VERSION_2026_07_28"
    - path: "Cargo.toml"
      provides: "the [[example]] registration with required-features"
      contains: "s54_v2_dual_conformance"
  key_links:
    - from: "examples/s54_v2_dual_conformance.rs"
      to: "pmcp::ServerBuilder::with_supported_protocol_versions"
      via: "the dual accept-list — the single call that opts the process into v2"
      pattern: "with_supported_protocol_versions"
    - from: "examples/s54_v2_dual_conformance.rs"
      to: "pmcp::types::protocol::PROTOCOL_VERSION_2026_07_28"
      via: "version constants sourced from the SDK, never as literals"
      pattern: "PROTOCOL_VERSION_2026_07_28"
    - from: "Cargo.toml"
      to: "examples/s54_v2_dual_conformance.rs"
      via: "[[example]] + required-features, the s47_v2_stateless_mrtr precedent"
      pattern: "s54_v2_dual_conformance"
---

<objective>
Build the purpose-built dual-version example the official conformance suite runs against (D-05),
and give it the tool / resource / prompt surface the **2025-11-25** scored set requires.

Purpose: D-05 chose a NEW example over reusing `t05_streamable_http_stateless` because an
overloaded example lets a change made for a docs or transport reason silently alter what
conformance measures — and a stateless-only server does not exercise the v1 session path D-04's
first run needs. RESEARCH measured that against the existing `s47_v2_stateless_mrtr`, the v1 leg's
protocol machinery is ALREADY conformant: every one of its 23 v1 failures was a *missing fixture*,
never a protocol defect. This plan supplies those fixtures.

Deliberately dual-purpose: this is also the runnable v2 example Phase 119's DOCS-06 needs. Build it
so Phase 119 can cite it rather than write a second one.

Output: `examples/s54_v2_dual_conformance.rs` with the dual accept-list, the v1 fixture tools, all
`test://` resources and all named prompts, plus its `[[example]]` registration. Plan 118-05 adds the
v2 MRTR / input-required surface and runs both measurements.

## Review corrections applied (D-20)

- **`[[example]]` registration is REQUIRED, not forbidden.** The pre-review plan said "examples are
  auto-discovered — do NOT add a `[[example]]` entry", and its run commands omitted `--features`.
  MEASURED: `s47_v2_stateless_mrtr` IS registered (`Cargo.toml:671-674`) with
  `required-features = ["streamable-http", "testing"]`, and a bare
  `cargo run --example s54_v2_dual_conformance` under default features would not compile. Task 1
  adds the entry, and every run command in this plan and its successors carries the feature set.
  This is why `Cargo.toml` is now in `files_modified`.
- **Suite output moves to `target/conformance-results/`.** `.gitignore:38` ignores `test-results/`,
  NOT `results/`, and no plan owns `.gitignore`. `target/` is already ignored (`.gitignore:2`), so
  the generated artifacts cannot dirty the worktree.
- **The zero-check criterion is replaced by the canonical policy.** "No scored scenario may report
  `0 passed, 0 failed`" is UNSATISFIABLE against the pinned suite (`server-sse-polling` is measured
  to do exactly that). This plan applies `conformance/README.md` § 7 — see Task 3.
- **`npm ci --ignore-scripts`** everywhere, matching plan 118-02.
- **D-19**: no verification command masks the exit status of what it verifies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md
@.planning/phases/118-conformance-against-the-official-suite/118-02-SUMMARY.md

<interfaces>
<!-- Contracts and exact target values. Do NOT re-derive by exploring. -->

The load-bearing opt-in. Without it the server is v1-ONLY: PROTOCOL_VERSION_2026_07_28 is
deliberately absent from SUPPORTED_PROTOCOL_VERSIONS and is never returned by
negotiate_protocol_version.

  pmcp::ServerBuilder::with_supported_protocol_versions(impl IntoIterator<Item = ProtocolVersion>)
      // src/server/mod.rs:3087 — the high-level builder's passthrough to
      // ServerCoreBuilder::with_supported_protocol_versions (src/server/builder.rs:802).
      // An empty accept-list falls back to the v1-only default; it never all-rejects.

Transport construction (examples/s47_v2_stateless_mrtr.rs:193-215):

  let http = StreamableHttpServer::with_config(requested, Arc::new(Mutex::new(server)),
                                               StreamableHttpServerConfig::default());
  let (addr, server_handle) = http.start().await?;    // returns AFTER the socket is bound

The registration precedent, verbatim (Cargo.toml:671-674):

  [[example]]
  name = "s47_v2_stateless_mrtr"
  path = "examples/s47_v2_stateless_mrtr.rs"
  required-features = ["streamable-http", "testing"]

  Numbering: s50/s51/s52/s53 exist; `s54` is the next free slot. Cargo example NAMES are unique, and
  the file carries a comment block above the s47 entry explaining the numbering bijection — extend
  it rather than starting a new convention.

The 2025-11-25 fixture surface the suite names (RESEARCH Code Example 5). Tools:

  test_simple_text        test_image_content       test_audio_content
  test_embedded_resource  test_multiple_content_types
  test_error_handling     test_tool_with_progress  test_tool_with_logging
  test_complete           test_missing_capability
  test_headers            test_custom_headers
  test_sampling           test_elicitation         test_logging_tool

Resource URIs:

  test://static-text  test://static-binary  test://example-resource
  test://embedded-resource  test://mixed-content-resource
  test://template/{id}/data  test://watched-resource

Prompts:

  test_prompt  test_simple_prompt  test_prompt_with_arguments
  test_prompt_with_embedded_resource  test_prompt_with_image

Exact expected payloads are carried by each scenario's `**Server Implementation Requirements:**`
block, printed on failure. Example, verbatim: implement tool `test_simple_text` with no arguments
returning `{"content":[{"type":"text","text":"This is a simple text response for testing."}]}`.
Retrieve any scenario's full spec with:
  conformance server --url <url> --scenario <name> --spec-version 2025-11-25

OUT OF SCOPE for this example (D-14): the Tasks extension. `test_tool_with_task` and all 10 Tasks
scenarios are `not_scored` at both revisions and must NOT be implemented.

Port hygiene (RESEARCH Pitfall 7): t04/t05 use 8080/8081 in scripts/test_examples_with_tester.sh;
s47_v2_stateless_mrtr defaults to 8147. This example uses 8149.

Output path: `target/conformance-results/<requirement-set>/`. `.gitignore:2` already ignores
`target/`; `results/` is NOT ignored and must not be used (D-20).

PRECONDITION supplied by plan 118-02 (this plan's declared dependency): the committed
`conformance/{package.json,package-lock.json,.npmrc}`. Materialise the suite binary with
`npm ci --prefix conformance --ignore-scripts` under Node 22 before the first invocation — that
command is idempotent, so running it here as well as in plan 118-08's script is harmless. If
`conformance/package-lock.json` is absent, plan 118-02 has not landed: STOP rather than running
`npm install`, which would create an unpinned tree and silently defeat D-01.

THE ZERO-CHECK POLICY is stated once, normatively, in `conformance/README.md` § 7 (written by plan
118-02 Task 3). Read it before Task 2 and apply it verbatim. Summary of the part this plan owns: a
scored scenario reporting `0 passed, 0 failed` is recorded as a FINDING in this plan's SUMMARY,
with its requirement set and scenario name, UNLESS it is one of the suite's known zero-check
scenarios (`server-sse-polling`, `tasks-status-notifications` as measured). The complete list is
AUTHORED in plan 118-08 from the measurements this plan and 118-05 record — so record every one,
including the known ones, with the observed check counts.

**Why this plan is Wave 2 and not Wave 1.** Tasks 2 and 3 obtain each fixture's expected payload by
RUNNING the pinned suite binary at `conformance/node_modules/.bin/conformance`. That binary exists
only after `npm ci --prefix conformance --ignore-scripts`, which in turn requires the committed
`conformance/package-lock.json` that plan **118-02 creates**. Neither the binary (generated,
gitignored) nor the lockfile-before-commit is visible to the `files_modified` overlap check, so a
same-wave placement would have been an INVISIBLE race. Hence the declared dependency.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The dual-version server skeleton — registration, accept-list, bind, banner, no startup warning</name>
  <files>examples/s54_v2_dual_conformance.rs, Cargo.toml</files>
  <read_first>
    - examples/s47_v2_stateless_mrtr.rs — read the whole file (296 lines). It is the example
      RESEARCH actually ran the official suite against. Copy: the header-doc convention (the "what
      this demonstrates" bullets), the `DEFAULT_ADDR` constant plus argv[1] override, the
      `with_supported_protocol_versions` accept-list, the `StreamableHttpServer::with_config` +
      `start()` shape, and the `print_instructions` runtime banner that builds copy-pasteable curl
      lines from pmcp's OWN header constants rather than retyped strings.
    - Cargo.toml lines 660-680 — the `s47_v2_stateless_mrtr` / `s48_v2_mrtr_client` `[[example]]`
      block and the numbering-bijection comment above it. You are adding a sibling entry and
      extending that comment.
    - tests/common/v2.rs — read lines 60-90 (the `V1`/`V2` constants sourced from pmcp) and lines
      290-390 (`build_v2_server_with` registering all three primitive kinds, and the comment at
      :371-385 explaining why `stateless()` is the WRONG config for a per-request era proof).
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `examples/sNN_v2_dual_conformance.rs`, which lists what to copy exactly and the one thing to
      CHANGE (the `PMCP_REQUEST_STATE_KEY` warning).
    - .planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md — the verified
      MEDIUM on the missing `[[example]]` registration.
  </read_first>
  <action>
Create `examples/s54_v2_dual_conformance.rs` AND register it in the root `Cargo.toml`.

**Registration first.** Add a `[[example]]` entry immediately after the `s48_v2_mrtr_client` block,
following the `s47_v2_stateless_mrtr` shape exactly:
  - `name = "s54_v2_dual_conformance"`, `path = "examples/s54_v2_dual_conformance.rs"`,
  - `required-features = ["streamable-http", "testing"]` — the same pair `s47` needs, because this
    example uses the same transport and reads the same reserved `_meta` key spellings. If the
    implementation turns out to need more, ADD to the list rather than dropping the entry.
  - a comment above the entry naming Phase 118 CONF-01, stating that this is the conformance TARGET
    the official suite runs against, that Phase 119 DOCS-06 cites it, and extending the existing
    numbering-bijection note with `s54`.
Without this entry the example has no required-feature guard and a default-feature
`cargo run --example` fails to compile — the exact MEDIUM the review raised.

**Skeleton target state:**

1. **Header doc.** State: this is the conformance TARGET for CONF-01; the official
   `@modelcontextprotocol/conformance` suite is run against it twice from one process (D-04/D-06);
   its tool/resource/prompt names are dictated by that suite and must not be renamed for taste;
   the Tasks extension is deliberately absent (D-14); Phase 119 DOCS-06 cites this example; and the
   required feature set, so a reader knows the run command is
   `cargo run --example s54_v2_dual_conformance --features full`. Include the two
   `conformance server --requirements ...` command lines a reader can run, with the
   `-o target/conformance-results/...` output path.

2. **`const DEFAULT_ADDR: &str = "127.0.0.1:8149";`** with a doc comment naming the collision it
   avoids: `t04`/`t05` bind 8080/8081 in `scripts/test_examples_with_tester.sh` and
   `s47_v2_stateless_mrtr` defaults to 8147. Parse the bind address from `std::env::args().nth(1)`
   falling back to `DEFAULT_ADDR`, so CI can pass a free port. On a bind failure, print an
   actionable message naming the address and the likely cause (a stale process from a previous run)
   and exit non-zero — never a bare `unwrap`.

3. **The dual accept-list**, using pmcp's own constants — `LATEST_PROTOCOL_VERSION` and
   `PROTOCOL_VERSION_2026_07_28` — never string literals. Add the comment explaining that listing
   both is what makes ONE binary serve both, because the era is negotiated PER REQUEST.

4. **`StreamableHttpServerConfig::default()`, NOT `::stateless()`.** Add the comment stating the
   rule from `tests/common/v2.rs:371-385`: `stateless()` is a BUILD-TIME config that removes the
   session machinery before a request is ever seen, so it could never prove the per-request era gate
   suppresses sessions on v2 — and the v1 `server-session-lifecycle` scenario is SCORED at
   2025-11-25 and needs a live session path.

5. **`PMCP_REQUEST_STATE_KEY` handling — the one deliberate divergence from s47.** `s47` leaves it
   unset to demo the warning. A conformance target must not emit a startup WARNING as part of its
   contract: read the key from the environment and, when absent, print a single explicit line
   telling the operator to set it (and that CI does), without going through the SDK's warn path.
   Never echo the key's value. Document this divergence in the header doc.

6. **Capabilities.** Declare tools, resources, prompts, logging and completions — the 2025-11-25
   scored set includes `logging-set-level`, `resources-subscribe`, `resources-unsubscribe` and
   `completion/complete`, and a capability the server does not advertise makes those scenarios
   unreachable rather than merely failing.

7. **`print_instructions` banner** modelled on `s47:226-275`: the bound address, both
   `conformance server --requirements ... -o target/conformance-results/...` invocations, and
   copy-pasteable `curl` lines for a v1 POST and a v2 POST, with header names taken from pmcp's own
   header constants.

Register no tools yet — Tasks 2 and 3 add them. The skeleton must compile and start.
  </action>
  <verify>
    <automated>RUSTFLAGS="-D warnings" cargo build --example s54_v2_dual_conformance --features full && cargo metadata --no-deps --format-version 1 > target/118-04-meta.json && grep -q 's54_v2_dual_conformance' target/118-04-meta.json</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTFLAGS="-D warnings" cargo build --example s54_v2_dual_conformance --features full`
      exits 0.
    - The example is REGISTERED, not merely auto-discovered:
      `grep -A3 'name = "s54_v2_dual_conformance"' Cargo.toml | grep -c 'required-features'`
      returns 1, and the required-features list contains `streamable-http`.
    - Under DEFAULT features the example is guarded, not broken:
      `cargo build --example s54_v2_dual_conformance` (no `--features`) either does nothing or
      reports the missing required features — it must NOT emit a compile error from the example's
      own source. Record the exact output in the SUMMARY.
    - `grep -c 'with_supported_protocol_versions' examples/s54_v2_dual_conformance.rs` is >= 1.
    - `grep -c '"2026-07-28"' examples/s54_v2_dual_conformance.rs` returns 0 — the version comes
      from `PROTOCOL_VERSION_2026_07_28`.
    - `grep -c 'StreamableHttpServerConfig::stateless' examples/s54_v2_dual_conformance.rs`
      returns 0.
    - `grep -c '8149' examples/s54_v2_dual_conformance.rs` is >= 1.
    - No bare `results/` path anywhere:
      `grep -cE '(^|[^-/])results/' examples/s54_v2_dual_conformance.rs` counts only
      `target/conformance-results` occurrences; state the audited lines in the SUMMARY.
    - Started with `PMCP_REQUEST_STATE_KEY` set to a 64-character test value, the process prints no
      line containing `WARN`: capture stdout+stderr for 3 seconds and assert `grep -c WARN` returns
      0. Record the captured banner verbatim in the SUMMARY.
    - Started on an explicit argv port (e.g. `127.0.0.1:8155`), the banner reports that address —
      proving argv[1] is honoured.
    - Started twice on the same port, the SECOND process exits non-zero with the actionable bind
      message and does not panic. Record the message.
  </acceptance_criteria>
  <done>The example is registered with required-features, builds warning-free, binds 127.0.0.1:8149 by default and an argv-supplied address on request, advertises both eras, keeps live session machinery, fails legibly on a held port, and starts silently when the request-state key is present.</done>
</task>

<task type="auto">
  <name>Task 2: The test:// resource surface and the named prompt surface</name>
  <files>examples/s54_v2_dual_conformance.rs</files>
  <read_first>
    - examples/s54_v2_dual_conformance.rs — the skeleton from Task 1.
    - examples/s03_server_resources.rs and examples/s04_server_resources_collection.rs — the
      in-repo shape for registering resources, including a URI-template resource.
    - examples/s05_server_prompts.rs — the in-repo shape for registering prompts with arguments.
    - tests/common/v2.rs lines 290-310 — `build_v2_server_with` registering `.tool()`, `.prompt()`
      and `.resources()` on one builder, so every MRTR-eligible method has a real dispatch target.
    - conformance/README.md — the pinned suite version, the Node 22 floor, the `--ignore-scripts`
      rule, and § 7 THE ZERO-CHECK POLICY. Read § 7 before running anything; it is normative for
      this task's reporting.
  </read_first>
  <behavior>
    Each URI and prompt name below is dictated by the official suite. For each, the expected payload
    is the one printed in that scenario's `**Server Implementation Requirements:**` block — obtain it
    by running the suite in single-scenario mode, do not invent it.

    Resources:
      test://static-text            -> a text resource
      test://static-binary          -> a base64 blob resource with an explicit mimeType
      test://example-resource       -> the resource `resources-read` reads
      test://embedded-resource      -> the resource `test_embedded_resource` embeds
      test://mixed-content-resource -> a resource whose contents mix text and binary
      test://template/{id}/data     -> a URI-TEMPLATE resource, listed under resources/templates/list
      test://watched-resource       -> the resource resources/subscribe and /unsubscribe target

    Prompts:
      test_prompt, test_simple_prompt, test_prompt_with_arguments (declares its arguments),
      test_prompt_with_embedded_resource (returns an embedded resource content block),
      test_prompt_with_image (returns an image content block)
  </behavior>
  <action>
Register the seven `test://` resources and the five named prompts on the builder from Task 1.

First materialise the pinned suite: run `npm ci --prefix conformance --ignore-scripts` under Node 22
(locally `PATH=~/.nvm/versions/node/v22.22.2/bin:$PATH`). This consumes the lockfile plan 118-02
committed — this plan's declared dependency. If that lockfile is absent, STOP; never fall back to
`npm install`.

Obtain each expected payload empirically rather than guessing: start the Task-1 binary with
`cargo run --example s54_v2_dual_conformance --features full`, then for each relevant scenario run
`conformance server --url http://127.0.0.1:8149/ --scenario <name> --spec-version 2025-11-25`
under Node 22, using the suite installed at `conformance/node_modules/.bin/conformance`. The failure
output prints the scenario's `**Server Implementation Requirements:**` block with the exact expected
payload. Encode that payload; do not paraphrase it.

Requirements on the implementation:
- `test://template/{id}/data` must be registered as a URI TEMPLATE so it appears under
  `resources/templates/list` and not under `resources/list` — the suite distinguishes them.
- `test://watched-resource` must be subscribable: `resources-subscribe` and `resources-unsubscribe`
  are SCORED at 2025-11-25 (both carry `removedIn: 2026-07-28`, so they are correctly absent from
  the v2 set; do not try to keep them alive under v2).
- `test://static-binary` must return a `blob` with a `mimeType`, not text.
- Every resource declares a `name` and a `mimeType`; the `resources-list` scenario validates
  structure, and RESEARCH measured `tools-list` failing against `s47` purely for a missing
  `description` — the same class of structural gap applies to resources and prompts.
- `test_prompt_with_arguments` must DECLARE its arguments in `prompts/list`, and honour them in
  `prompts/get`.

Add a module-level comment listing every URI and prompt name with the scenario that consumes it, so
a future reader can tell which entries are load-bearing and which scenario breaks if one is renamed.

**Reporting, per `conformance/README.md` § 7.** For every scenario you run, record in the SUMMARY:
the scenario name, the requirement set, the verdict, and the OBSERVED CHECK COUNT. A scored scenario
reporting `0 passed, 0 failed` is recorded as a zero-check observation with its name — not silently
counted as a pass, and not treated as an automatic failure either. Plan 118-08 authors the closed
`ZERO_CHECK_SCENARIOS` list from these records, so an unrecorded zero-check scenario is a gap in the
phase's evidence.
  </action>
  <verify>
    <automated>RUSTFLAGS="-D warnings" cargo build --example s54_v2_dual_conformance --features full</automated>
  </verify>
  <acceptance_criteria>
    - All seven `test://` URIs appear in the source:
      `for u in static-text static-binary example-resource embedded-resource mixed-content-resource 'template/{id}/data' watched-resource; do grep -q "test://$u" examples/s54_v2_dual_conformance.rs || echo "MISSING $u"; done`
      prints nothing.
    - All five prompt names appear:
      `for p in test_prompt test_simple_prompt test_prompt_with_arguments test_prompt_with_embedded_resource test_prompt_with_image; do grep -q "\"$p\"" examples/s54_v2_dual_conformance.rs || echo "MISSING $p"; done`
      prints nothing.
    - With the server running, these scenarios pass at `--spec-version 2025-11-25`:
      `resources-list`, `resources-read`, `resources-templates-list`, `resources-subscribe`,
      `resources-unsubscribe`, `prompts-list`, `prompts-get`. Record each verdict AND its observed
      check count in the SUMMARY as a three-column table.
    - Any scenario in that list reporting `0 passed, 0 failed` is recorded by name in a
      `## Zero-check observations` section of the SUMMARY, with the requirement set. Per
      `conformance/README.md` § 7 this is an observation to be carried into 118-08's
      `ZERO_CHECK_SCENARIOS`, not an automatic failure and not a pass.
    - `RUSTFLAGS="-D warnings" cargo build --example s54_v2_dual_conformance --features full`
      exits 0.
  </acceptance_criteria>
  <done>Every `test://` resource and named prompt the 2025-11-25 scored set requires is served with the suite's exact expected payload, each consuming scenario's verdict and check count is recorded, and any zero-check scenario is named.</done>
</task>

<task type="auto">
  <name>Task 3: The 2025-11-25 fixture tool surface, and the v1 leg measured green</name>
  <files>examples/s54_v2_dual_conformance.rs</files>
  <read_first>
    - examples/s54_v2_dual_conformance.rs — state after Task 2.
    - examples/s11_error_handling.rs — the in-repo shape for a tool that returns a protocol error
      versus a tool result with `isError: true`; `test_error_handling` distinguishes them.
    - examples/s08_progress_notifications.rs — the in-repo progress-notification shape for
      `test_tool_with_progress`.
    - examples/s07_logging.rs — the in-repo logging shape for `test_tool_with_logging` and
      `test_logging_tool`.
    - examples/s49_sampling_host.rs — the in-repo server->client sampling shape for `test_sampling`.
    - conformance/README.md § 7 and § 8 — the zero-check policy and the scored-set boundary.
  </read_first>
  <behavior>
    Implement each tool to the payload its scenario prints. The named surface, with what each one
    is for:

      test_simple_text            — no arguments; returns one text content block
      test_image_content          — returns an image content block with a mimeType
      test_audio_content          — returns an audio content block with a mimeType
      test_embedded_resource      — returns an embedded-resource content block referencing
                                    test://embedded-resource
      test_multiple_content_types — returns several content blocks of different types in one result
      test_error_handling         — exercises both error shapes the suite checks
      test_tool_with_progress     — emits progress notifications against the request's progress token
      test_tool_with_logging      — emits notifications/message during the call
      test_logging_tool           — the logging-focused tool the logging scenarios drive
      test_complete               — backs completion/complete
      test_missing_capability     — the negative case for a capability the server does not advertise
      test_headers                — echoes the request's standard headers back
      test_custom_headers         — echoes custom request headers back
      test_sampling               — issues a server->client sampling/createMessage (v1 shape)
      test_elicitation            — issues a server->client elicitation request (v1 shape)

    NOT implemented, deliberately: test_tool_with_task and every Tasks-extension tool (D-14).
  </behavior>
  <action>
Implement the fifteen tools above and register them on the builder.

Obtain each expected payload empirically, exactly as in Task 2 (the
`npm ci --prefix conformance --ignore-scripts` step has already materialised the pinned binary;
re-running it is idempotent): run the suite in single-scenario mode against the live server and read
the `**Server Implementation Requirements:**` block it prints on failure. Encode the payload
verbatim. Where a scenario names a literal string (for instance `test_simple_text`'s "This is a
simple text response for testing."), that string is part of the contract — reproduce it character
for character.

Every tool must declare a non-empty `description` and a well-formed input schema: RESEARCH measured
`tools-list` failing against `s47` with `Tool 0: missing description`, and `tools-list` is SCORED at
BOTH revisions.

`test_headers` and `test_custom_headers` need access to the inbound HTTP headers. Reach them through
the request-handler extras the SDK already provides; do not add a bespoke transport hook and do not
change any SDK source — this task touches exactly one file.

Then measure the v1 leg end to end. Start ONE process with
`cargo run --example s54_v2_dual_conformance --features full` on 127.0.0.1:8149 with
`PMCP_REQUEST_STATE_KEY` set, and run
`conformance server --url http://127.0.0.1:8149/ --requirements 2025-11-25 -o target/conformance-results/2025-11-25`.
Report the full pass/fail line and the per-scenario list in the SUMMARY.

D-03 admits no known-fail allowlist. If a scored 2025-11-25 scenario cannot be made to pass, do NOT
descope it, do NOT reach for `--expected-failures`, and do NOT weaken the example. Record the
blocker with the scenario name and the suite's verbatim failure text and stop — that is a finding
for the phase, not something to work around.

Apply `conformance/README.md` § 7 to the full run exactly as Task 2 applied it per scenario: build a
`## Zero-check observations` table naming every scored scenario that reported `0 passed, 0 failed`,
with its requirement set. Also record the TOTAL executed check count for the run — plan 118-08 turns
that number into the hard-coded `MIN_CHECKS_V1` floor, so it must be measured, not estimated.
  </action>
  <verify>
    <automated>RUSTFLAGS="-D warnings" cargo build --example s54_v2_dual_conformance --features full && make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - All fifteen tool names appear in the source:
      `for t in test_simple_text test_image_content test_audio_content test_embedded_resource test_multiple_content_types test_error_handling test_tool_with_progress test_tool_with_logging test_logging_tool test_complete test_missing_capability test_headers test_custom_headers test_sampling test_elicitation; do grep -q "\"$t\"" examples/s54_v2_dual_conformance.rs || echo "MISSING $t"; done`
      prints nothing.
    - `grep -c 'test_tool_with_task' examples/s54_v2_dual_conformance.rs` returns 0 — the Tasks
      extension is deliberately absent (D-14).
    - The `tools-list` scenario passes at `--spec-version 2025-11-25` with a NONZERO check count;
      record the count.
    - `conformance server --url http://127.0.0.1:8149/ --requirements 2025-11-25 -o target/conformance-results/2025-11-25`
      exits 0. The full stdout is recorded verbatim in the SUMMARY, including the
      `Total: N passed, M failed` line.
    - The SUMMARY carries a `## Zero-check observations` table for this run (possibly empty, but
      present and explicitly stating "none observed" if so) and a `## Total executed checks` line
      with the measured number — both are inputs plan 118-08 consumes.
    - `target/conformance-results/2025-11-25/**/checks.json` exists, and the worktree stays clean:
      `git status --porcelain` shows no untracked `results/` directory. State the verdict.
    - `RUSTFLAGS="-D warnings" cargo build --example s54_v2_dual_conformance --features full` and
      `make quality-gate` both exit 0.
    - `cargo run --example s54_v2_dual_conformance --features full` starts and prints its banner
      (the CLAUDE.md ALWAYS "runnable example" arm — note the required `--features`).
  </acceptance_criteria>
  <done>The 2025-11-25 requirement set exits 0 against one running instance of the example, with the full run output, the zero-check observations and the total check count all recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Conformance client -> example HTTP server | The suite drives arbitrary MCP requests, including deliberately malformed ones, across this boundary |
| Environment -> process | `PMCP_REQUEST_STATE_KEY` is a secret consumed from the environment |
| Localhost network -> bound socket | The example binds a TCP port on a developer machine and on CI runners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-15 | Information Disclosure | `PMCP_REQUEST_STATE_KEY` leaking into logs or the startup banner | mitigate | The key is read from the environment and NEVER echoed; the banner prints the variable NAME only. An acceptance criterion asserts no `WARN` line and the captured banner is reviewed |
| T-118-16 | Spoofing | DNS rebinding against a locally bound example | mitigate | Bind loopback only (`127.0.0.1`), never `0.0.0.0`. `dns-rebinding-protection` is a SCORED scenario in BOTH requirement sets and must pass — it already passed at v1 against `s47` |
| T-118-17 | Tampering | Port hijack or a stale process from a previous run answering the suite | mitigate | A distinct port (8149) away from `t04`/`t05` (8080/8081) and `s47` (8147); argv[1] override so CI can pick a free port; an actionable non-panicking bind failure so a held port is diagnosed rather than mistaken for a conformance result; teardown is owned by plan 118-08's script trap |
| T-118-18 | Denial of Service | An example that keeps live session state serving untrusted requests | accept | The example is a test target bound to loopback, started and killed by CI. Session machinery is REQUIRED (D-04's v1 leg needs `server-session-lifecycle`); `::stateless()` would remove the very thing under test |
| T-118-19 | Repudiation | A scenario reporting a green tick on zero checks, so "conformant" means nothing | mitigate | Every scenario's OBSERVED CHECK COUNT is recorded, and every zero-check scored scenario is named in a dedicated SUMMARY section per `conformance/README.md` § 7. Plan 118-08 turns those records into an exact-set-equality gate plus a hard-coded total-check floor |
| T-118-62 | Repudiation | Suite output dirtying the worktree, so a reviewer cannot tell a real change from a run artifact | mitigate | All output under `target/conformance-results/`, which `.gitignore:2` already covers; an acceptance criterion asserts `git status --porcelain` shows no untracked results directory |
| T-118-63 | Denial of Service | An unregistered example failing to compile under default features, so a downstream `cargo build --examples` breaks | mitigate | The `[[example]]` entry with `required-features` (the `s47_v2_stateless_mrtr` precedent), asserted by an acceptance criterion that runs a DEFAULT-feature build and requires it not to error from the example's own source |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing new; it consumes the suite already pinned and slopcheck-cleared by plan 118-02, via `npm ci --ignore-scripts` from `conformance/node_modules` |
</threat_model>

<verification>
- `RUSTFLAGS="-D warnings" cargo build --example s54_v2_dual_conformance --features full` exits 0.
- `cargo run --example s54_v2_dual_conformance --features full` starts, binds, and prints the banner.
- `conformance server --url http://127.0.0.1:8149/ --requirements 2025-11-25 -o target/conformance-results/2025-11-25`
  exits 0, with the full output recorded in the SUMMARY.
- The SUMMARY carries the per-scenario check counts, the zero-check observations, and the total
  executed check count.
- `make quality-gate` exits 0.

**Latency expectation (not a hang).** `npm ci --prefix conformance --ignore-scripts`, a cold
`cargo build --example … --features full`, the 33-scenario suite run and `make quality-gate` are
each multi-minute — budget **20-40 minutes** for the plan's verification passes. Let them finish; do
not interrupt and retry, and do not add a watch-mode or `--no-fail-fast`-style flag.
</verification>

<success_criteria>
- One process serves both eras by accept-list, with live session machinery and no startup warning.
- The example is registered with `required-features`, so its run command is stable and a
  default-feature build is guarded rather than broken.
- Every `test://` resource, named prompt and 2025-11-25 fixture tool the suite requires is served
  with the suite's own expected payload.
- The 2025-11-25 requirement set exits 0, with per-scenario check counts, the zero-check
  observations and the total check count all recorded for plan 118-08 to consume.
- The Tasks extension is absent by decision, stated in the header doc.
- No suite output lands outside `target/`.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-04-SUMMARY.md` when done.
Record: the verbatim v1 run output; the per-scenario verdict + check-count table; the
`## Zero-check observations` table; the `## Total executed checks` line; the captured startup
banner; the default-feature build output; the held-port failure message; and the `git status`
verdict on the results directory.
</output>
