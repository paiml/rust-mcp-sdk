---
phase: 118-conformance-against-the-official-suite
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-team-servers/src/conformance/runner.rs
  - crates/pmcp-team-servers/tests/conformance.rs
  - scripts/lint-plan-verify-commands.sh
  - Makefile
autonomous: true
requirements: [CONF-02]

must_haves:
  truths:
    - "The word `v2` in the runner's prose means the MCP era and nothing else; the fixture FORMAT revision is named differently"
    - "The on-disk fixture data is untouched — all 33 files still carry `\"schema_version\": \"2\"` and the runner still requires it"
    - "The v1 regression guard asserts EXACT counts: failed == 0, exactly 33 cases in total, and the exact per-directory case count for each of the four servers"
    - "Deleting a fixture file fails a test rather than producing a smaller green run"
    - "A GSD verification command that pipes a build/test invocation into another command without pipefail FAILS a committed lint"
  artifacts:
    - path: "crates/pmcp-team-servers/src/conformance/runner.rs"
      provides: "the D-08 prose rename — three sites, zero data change"
      contains: "format rev 2"
    - path: "crates/pmcp-team-servers/tests/conformance.rs"
      provides: "the v1-only REGRESSION GUARD with exact counts and a corpus-size fence"
      contains: "EXPECTED_CASES"
    - path: "scripts/lint-plan-verify-commands.sh"
      provides: "the D-19 plan-lint: a piped build/test invocation without pipefail is a hard failure"
      contains: "pipefail"
  key_links:
    - from: "crates/pmcp-team-servers/tests/conformance.rs"
      to: "contracts/team-servers/fixtures/"
      via: "per-directory exact case counts plus an on-disk file-count fence"
      pattern: "EXPECTED_CASES"
    - from: "Makefile"
      to: "scripts/lint-plan-verify-commands.sh"
      via: "the lint-plans target, chained into quality-gate"
      pattern: "lint-plan-verify-commands"
---

<objective>
Three small, independent pieces of hygiene that the rest of the phase depends on:

1. **D-08's prose rename.** `runner.rs:43` reads `Fixture schema v2` and `FixtureKind`'s rustdoc
   says "a v2 fixture case" — that is format revision 2, NOT MCP era v2. Rename the prose and
   reserve `v2` for the era throughout, before nine plans' worth of new era prose lands on top of
   the ambiguity.
2. **The v1 regression guard, tightened to EXACT counts.** Under D-16 the 33 in-process fixtures are
   no longer the era-comparison surface; they are the guard that says *the v1 corpus stayed green
   while the era work happened*. A guard whose assertions are floors (`>= 11`, `>= 6`, `>= 5`,
   `>= 7`) cannot notice a shrinking corpus, and the plans claimed exactness while asserting floors
   (118-REVIEWS.md, verified MEDIUM). Assert `failed == 0`, exact per-directory counts, an exact
   total of 33, and an on-disk file-count fence.
3. **The D-19 plan-lint.** The cross-AI review found ten `<automated>` blocks across this phase
   whose exit status came from `tail` or `grep` rather than from the command under test — and one
   that reported PASS when `cargo package` FAILED. Every one is corrected in the revised plans; this
   task ships the lint that stops the class from recurring.

Purpose: keep the "v1 fixtures stay green" half of CONF-02 honest and mechanical, remove a
terminology trap that would compound across the milestone, and make the verification-masking defect
class detectable rather than reviewer-dependent.

## Wave and ownership notes

This plan is Wave 1 and shares the wave with 118-01, 118-02 and 118-03. It is the ONLY wave-1 plan
that touches `crates/pmcp-team-servers/src/conformance/runner.rs`,
`crates/pmcp-team-servers/tests/conformance.rs`, `scripts/` or `Makefile` — 118-03 owns
`src/conformance/mod.rs`, the two new `era_*` modules, `baselines/`, `tests/era_baseline.rs` and
`fuzz/`, and there is no overlap. Do not create or edit any `era_*` file here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md

<interfaces>
<!-- Measured state. Do NOT re-derive by exploring. -->

The THREE D-08 rename sites in crates/pmcp-team-servers/src/conformance/runner.rs:
  :1-7      module doc       "fixture schema v2"  -> "fixture format rev 2"
  :44-56    section banner   "Fixture schema v2"  -> "Fixture format rev 2"
            FixtureKind doc  "a v2 fixture case"  -> "a fixture case (format rev 2)"
  :132-136  Fixture rustdoc  "(schema v2)"        -> "(format rev 2)"
UNCHANGED: the on-disk `"schema_version": "2"` field name and value in all 33 fixtures; the
`schema_version != "2"` runtime check at :463-468; the `#[ignore]`d regenerator's emission at
tests/conformance.rs:434.

The fixture corpus, counted on disk 2026-08-09:
  contracts/team-servers/fixtures/team-fs        12 *.json
  contracts/team-servers/fixtures/mem-mcp         7 *.json
  contracts/team-servers/fixtures/approval-mcp    6 *.json
  contracts/team-servers/fixtures/team-mcp        8 *.json
  TOTAL                                          33
One fixture FILE is one CASE: `run_fixtures` (runner.rs:392-441) records exactly one
`report.record(fx.case_id, …)` per loaded `Fixture`, whether independent or part of an ordered
scenario group. So on an all-green run `report.passed` EQUALS the directory's file count. Confirm
this by measurement before hard-coding — the numbers above are the file counts, not a measured
`report.passed`, and the existing floors (11/6/5/7) are each exactly one BELOW the file count,
which is unexplained and must be resolved by running the suite rather than by arithmetic.

The four existing tests to tighten (crates/pmcp-team-servers/tests/conformance.rs:241-277):
  team_fs_is_conformant        assert!(report.passed >= 11)
  mem_mcp_is_conformant        assert!(report.passed >= 6)
  approval_mcp_is_conformant   assert!(report.passed >= 5)
  team_mcp_is_conformant       assert!(report.passed >= 7)
`assert_conformant(&report)` (runner.rs) already panics on any failure; keep it, and ADD the exact
`report.failed == 0` assertion anyway so the guarantee is legible at the call site.

The Makefile analog for a standalone target: `test-severance` at Makefile:309-323 — a `.PHONY`
target that shells to a script, with a comment block stating what it proves and where the blocking
enforcement lives.

The masked-verification sites the lint must catch, quoted from 118-REVIEWS.md (all now corrected in
the revised plans, so the lint must pass on phase 118):
  `cargo test … 2>&1 | tee /tmp/x.log | tail -20`            -> status is `tail`'s
  `cargo test … | tee … ; grep -q … `                        -> status is `grep`'s
  `cargo package … | grep -E … ; test $? -eq 1`              -> reports PASS when cargo FAILS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-08 prose rename — three sites, zero data change</name>
  <files>crates/pmcp-team-servers/src/conformance/runner.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/conformance/runner.rs — the module doc at :1-31, the
      `Fixture schema v2` section banner and `FixtureKind` rustdoc at :44-56, the `Fixture` rustdoc
      and `schema_version` field doc at :132-136, and the `schema_version != "2"` runtime check at
      :463-468. You are editing prose in this file; read the current state.
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-08 verbatim.
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `crates/pmcp-team-servers/src/conformance/runner.rs`, which quotes all three rename sites
      verbatim and states the "do NOT change the on-disk field or value" rule.
  </read_first>
  <action>
Perform the D-08 rename at exactly the three prose sites named in `<interfaces>`, and nowhere else.

Add one sentence to the module doc making the distinction explicit and permanent: *"`rev 2` is the
FIXTURE FORMAT revision. It has nothing to do with MCP era v2 (`2026-07-28`); throughout this crate
a bare `v2` means the ERA. The on-disk field is still spelled `schema_version: "2"` and is
deliberately unchanged — renaming it would be a 33-file data diff for a naming win (Phase 118
D-08)."*

Do NOT touch `contracts/team-servers/fixtures/**`, the `schema_version` field name, its `"2"` value,
the runtime check, or the `#[ignore]`d regenerator's emission at `tests/conformance.rs:434`. A
0-file data diff beats a 33-file one.
  </action>
  <verify>
    <automated>mkdir -p target && bash -o pipefail -c 'cargo build -p pmcp-team-servers --all-features 2>&1 | tee target/118-10-t1.log' && test -z "$(git diff --name-only contracts/team-servers/fixtures/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'schema v2' crates/pmcp-team-servers/src/conformance/runner.rs` returns 0.
    - `grep -c 'v2 fixture case' crates/pmcp-team-servers/src/conformance/runner.rs` returns 0.
    - `grep -c 'format rev 2' crates/pmcp-team-servers/src/conformance/runner.rs` is >= 3.
    - The module doc carries the disambiguation sentence:
      `grep -c 'a bare `v2` means the ERA' crates/pmcp-team-servers/src/conformance/runner.rs`
      is >= 1 (adjust for exact quoting; quote the committed sentence in the SUMMARY).
    - `git diff --name-only contracts/team-servers/fixtures/` prints nothing — ZERO data files
      changed.
    - `grep -rc '"schema_version": "2"' contracts/team-servers/fixtures/ | awk -F: '{s+=$2} END {print s}'`
      returns the same total as before the change; record both numbers in the SUMMARY.
    - The runtime check is intact:
      `grep -c 'schema_version != "2"' crates/pmcp-team-servers/src/conformance/runner.rs` is >= 1.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features` exits 0.
    - `make comply-bindings-check` exits 0 — the rename must not disturb the team-servers contract
      bindings (CLAUDE.md contract-first rule).
  </acceptance_criteria>
  <done>The three prose sites read "format rev 2", the module doc states the era-vs-format distinction, and `git diff` over the fixture corpus is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the v1 regression guard to exact counts and add a corpus-size fence</name>
  <files>crates/pmcp-team-servers/tests/conformance.rs</files>
  <read_first>
    - crates/pmcp-team-servers/tests/conformance.rs — read the whole file (448 lines). Specifically:
      the module doc at :1-19, `fixtures_root()` at :69, the per-target factories at :102-235, the
      four `*_is_conformant` tests with their floors at :241-277, the `load_dir` / `exercised_tools`
      / `advertised_surface` helpers at :284-318, the coverage tests at :320-407, and the
      `#[ignore]`d regenerator at :409-448.
    - crates/pmcp-team-servers/src/conformance/runner.rs — `ConformanceReport` (`passed` / `failed` /
      `cases`), `assert_conformant`, and the `run_fixtures` recording loop at :392-441, which is the
      evidence that one fixture file is one recorded case.
    - .planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md — the verified
      MEDIUM "118-06's v1 guarantee uses floors, not the claimed exact corpus".
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — Shared Pattern 2
      (named non-vacuity floors whose doc comment forbids lowering them) and the `115-REVIEW.md`
      WR-01 rule that an anti-vacuity count is hard-coded, never length-derived.
  </read_first>
  <behavior>
    After this task, the v1 regression guard FAILS when:
      - any case fails (`report.failed != 0`)
      - a server's case count is not EXACTLY its recorded value
      - the four counts do not sum to EXACTLY `EXPECTED_TOTAL_CASES`
      - the on-disk `*.json` file count for a server directory is not EXACTLY its recorded value
      - the on-disk total is not EXACTLY 33

    A deleted fixture therefore fails TWO independent assertions (the case count and the file
    count), and an ADDED fixture fails them too — which is correct: growing the corpus is a
    deliberate act that updates the constants in the same commit.

    The module doc must state the new role: under Phase 118 D-16 these 33 in-process cases are the
    v1-only REGRESSION GUARD. They are no longer the era-comparison surface; that lives in
    `crates/pmcp-team-servers/tests/era_matrix.rs` over real streamable HTTP (plan 118-07).
  </behavior>
  <action>
Replace the four floors with exact counts, add a corpus-size fence, and record the new role.

1. **Measure first, hard-code second.** Run `cargo test -p pmcp-team-servers --test conformance --
   --nocapture` and read the actual `report.passed` for each of the four servers. Do NOT assume it
   equals the file count and do NOT assume it equals the existing floor plus one. Record all four
   measured numbers, and the file counts, in the SUMMARY as a two-column table, with a sentence
   explaining any discrepancy. If a measured count differs from its directory's file count, that
   discrepancy is itself a finding: state the cause (a fixture the loader rejects, two fixtures in
   one file, a scenario group) before hard-coding anything.

2. **Named constants**, each with the remedy in its doc comment, hard-coded and never
   length-derived:
   - `EXPECTED_CASES_TEAM_FS`, `EXPECTED_CASES_MEM_MCP`, `EXPECTED_CASES_APPROVAL_MCP`,
     `EXPECTED_CASES_TEAM_MCP` — the four measured values.
   - `EXPECTED_TOTAL_CASES` — their sum, written as a LITERAL, not as an expression over the four
     constants. A sum expression cannot catch a coordinated edit; a literal can.
   - `EXPECTED_FIXTURE_FILES_*` and `EXPECTED_TOTAL_FIXTURE_FILES = 33` — the on-disk counts.
   Every doc comment states: the remedy for a failure is NEVER to adjust the number to match; either
   restore the fixture or, if the corpus legitimately changed, change the constant IN THE SAME
   COMMIT as the fixture and say why in the message.

3. **Rewrite the four `*_is_conformant` tests** to keep `assert_conformant(&report)` AND add
   `assert_eq!(report.failed, 0, …)` and `assert_eq!(report.passed, EXPECTED_CASES_…, …)`. Use the
   three-part `FAILURE MODE:` / `CONSEQUENCE:` / `WHAT TO DO:` convention and echo both the expected
   and the observed count, so a reader does not have to re-run to learn the delta.

4. **Add `fixture_corpus_is_exactly_thirty_three`** — a plain `#[test]` that counts `*.json` files
   per directory under `fixtures_root()` and asserts each `EXPECTED_FIXTURE_FILES_*` and the total.
   Doc comment: this is the second, independent arm. The case-count assertions could all be
   satisfied by a corpus that lost one file and gained another; this one cannot.

5. **Update the module doc** with the new role statement from `<behavior>`, naming D-16, naming
   `crates/pmcp-team-servers/tests/era_matrix.rs` as where the era comparison now lives, and stating
   why: `DuplexTransport` (`crates/pmcp-team-servers/src/transport.rs:47`) never overrides
   `supports_negotiated_protocol_version` (trait default `false`, `src/shared/transport.rs:351`), so
   an in-process v2 arm is INERT — `ClientBuilder::build` (`src/client/mod.rs:5213`) says so in a
   `tracing::warn!`. A matrix built here would have compared v1 against v1 and reported green.

Do NOT weaken or delete the four coverage tests at :320-407, do NOT touch the `#[ignore]`d
regenerator, and do NOT add any era dimension to this file.
  </action>
  <verify>
    <automated>mkdir -p target && bash -o pipefail -c 'cargo test -p pmcp-team-servers --test conformance 2>&1 | tee target/118-10-t2.log' && grep -qE '^running [1-9][0-9]* tests?$' target/118-10-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-team-servers --test conformance` exits 0 with a nonzero test count, and the
      reported test count is strictly greater than the 9 the harness reported before this plan
      (record both counts).
    - No floors survive:
      `grep -cE 'passed >= (11|6|5|7)' crates/pmcp-team-servers/tests/conformance.rs` returns 0.
    - `grep -c 'EXPECTED_TOTAL_CASES' crates/pmcp-team-servers/tests/conformance.rs` is >= 2, and its
      declaration is a literal integer — `grep -A1 'EXPECTED_TOTAL_CASES: usize =' crates/pmcp-team-servers/tests/conformance.rs`
      shows no `+` and no `len()`.
    - `grep -c 'EXPECTED_TOTAL_FIXTURE_FILES' crates/pmcp-team-servers/tests/conformance.rs` is >= 2
      and its value is `33`.
    - `grep -c 'assert_eq!(report.failed, 0' crates/pmcp-team-servers/tests/conformance.rs` returns 4.
    - The module doc names the new role: `grep -c 'D-16' crates/pmcp-team-servers/tests/conformance.rs`
      is >= 1 and `grep -c 'era_matrix' crates/pmcp-team-servers/tests/conformance.rs` is >= 1.
    - If nextest is used anywhere, the selector is `binary(conformance)`, never
      `test(/conformance/)`: `grep -rn "test(/" crates/pmcp-team-servers/ scripts/ 2>/dev/null`
      returns nothing. (`-E 'test(/foo/)'` silently selects ZERO tests and exits 0; it bit Phase 114
      seven times.)
    - Negative control, executed by hand once and reported verbatim, then reverted: move one
      `contracts/team-servers/fixtures/mem-mcp/*.json` file aside and confirm BOTH
      `mem_mcp_is_conformant` and `fixture_corpus_is_exactly_thirty_three` FAIL, each naming the
      expected and observed numbers. Restore the file.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features` exits 0.
  </acceptance_criteria>
  <done>The v1 regression guard asserts zero failures, exact per-server case counts, an exact total, and an independent on-disk file-count fence — and one deleted fixture fails two of them, proved by execution.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/lint-plan-verify-commands.sh — the D-19 lint that stops masked verifications recurring</name>
  <files>scripts/lint-plan-verify-commands.sh, Makefile</files>
  <read_first>
    - scripts/run-severance-proofs.sh — read the whole file (129 lines). Copy its STRUCTURE: the
      `set -euo pipefail` line, the `fail()` helper, the three-part
      `FAILURE MODE:` / `CONSEQUENCE:` / `WHAT TO DO:` message convention, and the numbered header
      comment that names each fence and the false-green it closes.
    - Makefile lines 300-345 — the `test-severance` target and its comment block, the template for a
      `.PHONY` target that shells to a script.
    - Makefile — the `quality-gate` target, so the new `lint-plans` target can be chained into it in
      the right position (before the slow steps, so a plan defect fails fast).
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-19 verbatim.
    - .planning/phases/118-conformance-against-the-official-suite/118-REVIEWS.md — Consensus item 5
      and the `118-02:222` `test $? -eq 1` example, which is the worst shape and must be caught.
  </read_first>
  <behavior>
    `scripts/lint-plan-verify-commands.sh` scans the `*-PLAN.md` files under a committed list of
    phase directories and FAILS, naming file, line and the offending text.

    SCOPE — the rules apply ONLY to lines inside a `<verify>` … `</verify>` element or inside an
    `<acceptance_criteria>` … `</acceptance_criteria>` element. Prose elsewhere in a plan
    legitimately QUOTES the anti-patterns in order to forbid them (this very plan does, in its
    `<interfaces>` and `<action>` blocks), and a lint that fired on its own rationale would be
    unusable. Match the opening and closing tags EXACTLY at the start of a line — a prefix match on
    `<verify` would also match the `<verification>` section, which is prose.

    Within that scope it FAILS when it finds:

      RULE 1 — a line that pipes a build/test invocation into another command without `pipefail`.
        The invocation matches any of: `cargo test`, `cargo build`, `cargo run`, `cargo package`,
        `cargo clippy`, `cargo fmt`, `cargo doc`, `cargo bench`, `npm ci`, `npm install`, `make `,
        `./scripts/`. The line contains a `|` that is not `||`. The line does NOT contain
        `pipefail`.

        QUOTE-AWARENESS IS MANDATORY, and it is not an optimisation — it is what makes the rule
        usable at all. Both the invocation match and the pipe match MUST ignore anything inside
        single or double quotes. Two measured false positives from a naive detector, both in this
        phase's own plans:
          * `grep -qE 'conformance/|ci_conformance_gate_wiring' <file>` — the `|` is regex
            alternation inside quotes, and there is no pipeline on the line at all;
          * `grep -v '^\s*#' <script> | grep -c 'cargo run'` — the pipeline is grep-into-grep and
            the "build command" is a quoted SEARCH STRING, not an invocation.
        A detector that flagged either would be turned off within a week, which is the real failure
        mode for a lint. Strip quoted spans before applying the rule, and record in the SUMMARY that
        both of the above shapes were tested and pass.
      RULE 2 — a line containing `test $? -eq` or `test "$?" -eq`. Consulting `$?` after a pipeline
        reads the LAST stage's status, and the `-eq 1` spelling actively converts a build failure
        into a reported pass.
      RULE 3 — a line containing `|| true`.

    It SKIPS cleanly (exit 0, with a printed note) when `.planning/` is absent — the same guard
    semantics `tests/v2_conformance_pin.rs:100-111` uses, so the target does not break outside a
    full checkout.

    The linted set is a committed, hard-coded array, seeded with THIS phase only. Its doc comment
    states the growth rule: phases are ADDED as they are swept and NEVER removed. This is what lets
    the lint be chained into `quality-gate` on day one without detonating on the historical plan
    corpus.
  </behavior>
  <action>
Create `scripts/lint-plan-verify-commands.sh`, executable, modelled structurally on
`scripts/run-severance-proofs.sh`.

Header comment, numbered, naming each fence and the false-green it closes:
  1. Why this lint exists at all: the Phase-118 cross-AI review found TEN verification commands
     whose reported status came from `tail` or `grep` rather than from the command under test, and
     one (`cargo package … | grep …; test $? -eq 1`) that reported PASS precisely when
     `cargo package` FAILED. A reviewer caught them; a lint is what makes the next one impossible to
     merge. Cite D-19.
  2. Why the linted set is an explicit ALLOW-GROWTH array rather than "everything under
     `.planning/`": the historical plan corpus predates the rule, so a repo-wide sweep would make
     `make quality-gate` red for reasons unrelated to the change under test. The array only grows.
  3. Why it skips on a missing `.planning/`: `.planning/` is excluded from the published crate
     (`Cargo.toml` `exclude`), and a gate that panics outside a full checkout is a gate people
     disable.
  4. Why the RULE-1 detector requires `pipefail` on the SAME LINE rather than anywhere in the file:
     `set -o pipefail` in one `<verify>` block does not apply to a sibling block, and a plan's prose
     frequently explains pipefail at length — a whole-file grep would self-satisfy.
  5. Why the rules are scoped to `<verify>` and `<acceptance_criteria>` elements only: a plan that
     forbids an anti-pattern must be able to QUOTE it. Scoping to the executable blocks is what lets
     the rationale and the rule coexist in one file.

Behaviour:
  1. `set -euo pipefail`; a `fail()` helper that increments a counter and prints the three-part
     message with `file:line` and the offending text; a final non-zero exit if the counter is
     non-zero.
  2. `LINTED_PHASES=( "118-conformance-against-the-official-suite" )` with the growth-rule doc
     comment. Accept an optional argv override for one-off use, and say in the header that the
     override is for local exploration only — CI and `quality-gate` always use the array.
  3. If `.planning/phases` does not exist, print the skip note and exit 0.
  4. For each linted phase, for each `*-PLAN.md` in it, track whether the current line is inside a
     `<verify>` or an `<acceptance_criteria>` element (exact line-anchored tag match, so
     `<verification>` is NOT treated as `<verify>`), and apply RULES 1-3 from `<behavior>` only to
     lines in that scope. If a false positive appears in practice, the remedy is to make the RULE
     narrower, never to add a per-file skip.
  5. On success print the number of plan files scanned and the number of verification lines
     inspected, so a run that scanned NOTHING is visibly different from a clean run. Fail if the
     scanned-file count is zero while the phase directory exists — a lint that inspected nothing is
     the vacuity defect it exists to prevent.

Then add a `.PHONY: lint-plans` target to the `Makefile`, modelled on `test-severance`, that shells
to the script. Its comment must state: what it proves (D-19 — no GSD verification command may mask
the exit status of the thing it verifies); that the linted set only grows; and that it IS chained
into `quality-gate`, unlike `test-severance`, because it is fast and has no external prerequisite.
Chain `lint-plans` into `quality-gate` EARLY, before the multi-minute steps.
  </action>
  <verify>
    <automated>bash -n scripts/lint-plan-verify-commands.sh && test -x scripts/lint-plan-verify-commands.sh && ./scripts/lint-plan-verify-commands.sh && make -n lint-plans >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/lint-plan-verify-commands.sh` exits 0 and the file is executable.
    - `./scripts/lint-plan-verify-commands.sh` exits 0 against the revised phase-118 plans, and its
      output reports a NONZERO scanned-file count. Record the count and the inspected-line count in
      the SUMMARY — a run reporting `0 files scanned` must fail, and that behaviour is asserted by
      negative control (c) below.
    - `grep -c 'pipefail' scripts/lint-plan-verify-commands.sh` is >= 2 (the script's own
      `set -euo pipefail` plus the RULE-1 detector).
    - `grep -c 'LINTED_PHASES' scripts/lint-plan-verify-commands.sh` is >= 2, and the array literal
      contains `118-conformance-against-the-official-suite`.
    - `make -n lint-plans` prints an invocation of `./scripts/lint-plan-verify-commands.sh`, and
      the target is REACHED by the gate, not merely present — assert with
      `make -n quality-gate > target/118-10-qg.txt && grep -q 'lint-plan-verify-commands' target/118-10-qg.txt`
      (capture-then-assert, so `make`'s exit status is not masked by the grep).
    - Three negative controls, each executed by hand once, recorded verbatim, then reverted:
      (a) in a scratch
          `.planning/phases/118-conformance-against-the-official-suite/118-99-PLAN.md`, put a
          `cargo test`-into-`tee`-into-`tail` pipeline with no `pipefail` INSIDE a `<verify>`
          element, confirm RULE 1 FIRES naming file and line, then delete the scratch file;
      (b) in the same scratch file, inside a `<verify>` element, put a `cargo package --list`
          piped into `grep` followed by an exit-status comparison of the PIPELINE'S LAST STAGE
          against 1 (the `118-02:222` shape the review found, which reported PASS precisely when
          `cargo package` failed) — confirm RULE 2 FIRES. The literal spelling is given in this
          plan's `<behavior>` block; it is deliberately not repeated inside this criterion, because
          the lint's scope includes `<acceptance_criteria>` and a criterion that quoted the
          anti-pattern would make the lint fire on this plan;
      (b2) move the SAME two lines out of the `<verify>` element into the plan's `<action>` prose
          and confirm the lint now PASSES — this is what proves the scoping works and that a plan
          may quote what it forbids;
      (c) point the argv override at an empty directory, confirm the script FAILS with the
          "inspected nothing" message rather than exiting 0.
    - The lint does NOT fire on a correctly written command: confirm that
      a `bash -o pipefail -c` wrapped `cargo test` teed to a log, followed by a separate `grep -q`
      against that log, passes RULE 1. Quote the exact line it was tested against in the SUMMARY.
    - The lint does NOT fire on either measured false-positive shape: a `grep -qE` whose pattern
      contains a quoted `|` alternation, and a grep-into-grep whose search string happens to contain
      a build-command name. Both were run through the lint and both passed; quote both in the
      SUMMARY. If either fires, the remedy is to fix the quote-stripping, never to add a skip.
    - No `justfile` was created: `test -f justfile` fails. This repo is Makefile-driven and a
      conformance phase is not the place to migrate.
    - `make quality-gate` exits 0 — including the newly chained `lint-plans` step.
  </acceptance_criteria>
  <done>A committed lint fails on a piped build/test invocation without pipefail, on the `test $? -eq` shape, and on a vacuous scan; it passes on this phase's plans; and `make quality-gate` reaches it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture corpus on disk -> the v1 regression guard | Files can be deleted, added or edited between runs; the guard's counts are what notice |
| Plan text -> the executor's verification | A masked verification command makes a red build report green; the whole phase's evidence rests on these commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-30 | Denial of Service | The D-08 rename touching on-disk fixture data and breaking 33 files plus the contract bindings | mitigate | Rename is PROSE-ONLY at three named sites; acceptance criteria assert `git diff --name-only contracts/team-servers/fixtures/` is EMPTY and `make comply-bindings-check` exits 0 |
| T-118-58 | Repudiation | A shrinking fixture corpus producing a smaller GREEN run, so "the v1 corpus stayed green" means less each time | mitigate | Exact per-server case counts, an exact literal total, and an INDEPENDENT on-disk file-count fence; one deleted fixture fails two assertions, proved by an executed negative control |
| T-118-59 | Tampering | A coordinated edit that removes one fixture and adds another, keeping the total intact | mitigate | The per-directory counts are asserted individually, and `EXPECTED_TOTAL_CASES` is a LITERAL rather than a sum expression, so a compensating edit still fails at least one assertion |
| T-118-60 | Repudiation | A verification command whose reported status comes from the last pipe stage, so a failing build reports success — the defect class the cross-AI review found ten times in this phase | mitigate | `scripts/lint-plan-verify-commands.sh` RULES 1-3, chained into `make quality-gate`, with three executed negative controls including the vacuous-scan case |
| T-118-61 | Repudiation | The lint itself scanning nothing and exiting 0 | mitigate | A zero-scanned-file run is a hard failure when the phase directory exists; negative control (c) proves it |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds no dependency and does not touch `fuzz/Cargo.toml` or any `Cargo.toml` |
</threat_model>

<verification>
- `cargo test -p pmcp-team-servers --test conformance` exits 0 with a nonzero test count strictly
  greater than the pre-plan 9.
- `git diff --name-only contracts/team-servers/fixtures/` is empty.
- `make comply-bindings-check` exits 0.
- `./scripts/lint-plan-verify-commands.sh` exits 0 with a nonzero scanned-file count.
- `make -n quality-gate` reaches `lint-plan-verify-commands`.
- All four negative controls (one fixture removed; RULE 1; RULE 2; vacuous scan) were executed,
  recorded verbatim, and reverted.
- `make quality-gate` exits 0.

**Latency expectation (not a hang).** `cargo test -p pmcp-team-servers --test conformance` is ~25 s;
`make quality-gate` is **8-20 minutes**. Let them finish; do not interrupt and retry, and do not add
a watch-mode or `--no-fail-fast`-style flag to make them feel faster.
</verification>

<success_criteria>
- Bare "v2" in the runner's prose means the MCP era; the fixture format revision has its own name;
  the on-disk data is byte-identical.
- The v1 regression guard is exact in both dimensions (cases and files) and its module doc records
  why it is no longer the era-comparison surface.
- A committed, gate-reached lint makes the masked-verification defect class fail rather than depend
  on a reviewer noticing.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-10-SUMMARY.md` when done.
Record: the measured per-server case counts beside the on-disk file counts with any discrepancy
explained; the pre/post fixture `schema_version` totals; the committed disambiguation sentence; the
four negative-control transcripts; and the lint's scanned-file and inspected-line counts.
</output>
