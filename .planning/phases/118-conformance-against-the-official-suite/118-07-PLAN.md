---
phase: 118-conformance-against-the-official-suite
plan: 07
type: execute
wave: 3
depends_on: ["118-06"]
files_modified:
  - crates/pmcp-team-servers/src/conformance/deprecated_caps.rs
  - crates/pmcp-team-servers/src/conformance/mod.rs
  - crates/pmcp-team-servers/tests/conformance.rs
  - crates/pmcp-team-servers/baselines/era-deltas.yaml
  - crates/pmcp-team-servers/tests/era_baseline.rs
  - contracts/team-servers/fixtures/deprecated-caps
  - docs/v1-sunset-policy.md
autonomous: true
requirements: [CONF-03]

must_haves:
  truths:
    - "Under v1 negotiation, Logging is reachable via the logging/setLevel RPC and a fixture proves it"
    - "Under v1 negotiation, Sampling and Roots are reachable via server->client requests mid-call and fixtures prove it"
    - "Under v2 negotiation, Logging is reachable via the _meta key io.modelcontextprotocol/logLevel and a fixture proves it"
    - "Under v2 negotiation, Sampling and Roots are reachable via InputRequiredResult and fixtures prove it"
    - "Each of the three capabilities' era difference is an EXPECTED entry in the baseline, not a finding"
    - "Deleting any deprecated-capability fixture fails a coverage test rather than silently shrinking coverage"
    - "No runtime warning, no #[deprecated] attribute and no behavioural change is introduced by this deprecation"
    - "docs/v1-sunset-policy.md states the 12-month advisory window WITHOUT contradicting its existing 'no date and no committed window' clause for v1 removal"
  artifacts:
    - path: "crates/pmcp-team-servers/src/conformance/deprecated_caps.rs"
      provides: "a conformance-feature-gated target server exercising Roots, Sampling and Logging under both eras"
      min_lines: 150
    - path: "contracts/team-servers/fixtures/deprecated-caps"
      provides: "the CONF-03 fixture cases replayed by the D-07 matrix"
      contains: "schema_version"
    - path: "docs/v1-sunset-policy.md"
      provides: "the 12-month advisory deprecation window for Roots/Sampling/Logging"
      contains: "12-month"
  key_links:
    - from: "contracts/team-servers/fixtures/deprecated-caps"
      to: "crates/pmcp-team-servers/tests/conformance.rs"
      via: "the era_matrix_is_conformant driver replaying them under both eras"
      pattern: "deprecated-caps"
    - from: "crates/pmcp-team-servers/baselines/era-deltas.yaml"
      to: "the three capabilities' era differences"
      via: "observation_id rows method.logging_set_level / meta.log_level / result.input_required"
      pattern: "logging_set_level"
---

<objective>
Discharge CONF-03 through the SAME mechanism as CONF-02 (D-10): Roots, Sampling and Logging get
fixtures IN the era matrix, so D-07's replay-under-both-eras machinery proves they remain fully
functional under v2 negotiation. Then state the advisory deprecation in prose (D-11) — documented,
never emitted.

Purpose: the CONTEXT through-line is explicit — one mechanism discharges CONF-02 and CONF-03
together; do not build a second parallel mechanism. D-10 chose fixtures in the matrix over dedicated
Rust tests (a second mechanism that rots independently) and over relying on the official suite
(coverage we do not control — a re-pin could drop it and CONF-03 would silently stop being proven).
RESEARCH measured that v2 has NO direct Roots/Sampling server scenario at all, which validates that
choice.

D-12 settles what "fully functional" means: the capability is reachable via its **v2 mechanism**.
Under v2, Logging is exercised through the `_meta` key `io.modelcontextprotocol/logLevel`, and
Sampling and Roots through `InputRequiredResult` (SEP-2322). The v1 RPC shapes stay green under
**v1 negotiation only**; under v2 the official suite's expectation (removed methods answer 404 +
-32601) stands.

Output: a purpose-built deprecated-capability target, its fixtures, matrix and coverage wiring, and
the sunset-policy section that records the window without contradicting the document it lives in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-06-SUMMARY.md

<interfaces>
<!-- What v2 actually does to the three capabilities. Measured, RESEARCH § State of the Art. -->

  Logging   v1: logging/setLevel RPC
            v2: per-request _meta key "io.modelcontextprotocol/logLevel".
                The v2 schema says verbatim: "Replaces the former `logging/setLevel` RPC."
                notifications/message survives, gated on the per-request key.

  Sampling  v1: server->client sampling/createMessage mid-request
            v2: InputRequiredResult — resultType: "input_required" + inputRequests (SEP-2322)

  Roots     v1: server->client roots/list mid-request
            v2: InputRequiredResult — same mechanism as Sampling

  The official v2 suite carries input-required-result-basic-sampling and -basic-list-roots and
  NOTHING else for these capabilities. `logging-set-level`, `tools-call-sampling` and
  `tools-call-elicitation` all carry `source.removedIn: 2026-07-28`.

Fixture format (crates/pmcp-team-servers/src/conformance/runner.rs, format rev 2 after plan 118-06):
  kinds: tools_list, tool_call
  match modes (:68-80): exact (deep equality, ignoring `_note*` keys), subset (default),
                        predicate (supports "*", "@nonempty", "@string", "@number", "@bool",
                        "@contains:")
  `_note*` keys are IGNORED by matching, so they are the sanctioned place to explain a fixture inline.

Fixture naming conventions observed across all 33 existing files:
  <scenario>.<NN>-<step>.json   for ordered sequences
  <tool>.success.json / <guard>.error.json   for independent cases
  exactly one tools_list.json per server directory
  case_id is <server>.<tool-or-family>.<positive|negative-<guard>>

Coverage-test pattern to copy (tests/conformance.rs:320-407): load every fixture in a directory,
collect the `case_id`s, and assert each named guard appears — so a deleted fixture fails a test
rather than silently shrinking coverage.

docs/v1-sunset-policy.md structure:
  ~:68-84   "What is deliberately NOT severed" — a 3-column table, one prose paragraph per cell
  ~:120-130 "The removal condition" — contains the clause the new section must reconcile with:
            "**Not scheduled.** There is **no date and no committed window** in this policy,
            deliberately."
  ~:137-213 the runnable-verification section shape: every claim ends in a copy-pasteable command
            plus a "three things that can never prove this, and why" list
  ~:215-234 "Explicit non-commitments" — already forbids #[deprecated] attributes and a runtime
            warning on v1 negotiation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A deprecated-capability conformance target exercising Roots, Sampling and Logging</name>
  <files>crates/pmcp-team-servers/src/conformance/deprecated_caps.rs, crates/pmcp-team-servers/src/conformance/mod.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/conformance/runner.rs — the `EchoTool` in-crate test server near
      the end of the file (the precedent for a conformance-owned server), the `ConformanceTarget`
      trait, the `Era` enum and the era-pinned constructors plan 118-06 added.
    - crates/pmcp-team-servers/src/conformance/mod.rs — the module declarations; you are adding one.
    - crates/pmcp-team-servers/tests/conformance.rs — the `SamplingHandler`-style `create_message`
      implementation at :158-175, which is the in-repo shape for a client-side sampling handler the
      target needs on the v1 arm.
    - examples/s49_sampling_host.rs and examples/s07_logging.rs — the in-repo server-side shapes for
      issuing a sampling request and for emitting log notifications.
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-10, D-11, D-12.
  </read_first>
  <behavior>
    A server exposing exactly three tools, each of which reaches its capability through whichever
    mechanism the negotiated era provides:

      dep__log_emit      — emits a log message. Under v1 the level is set by a preceding
                           logging/setLevel; under v2 it comes from the per-request `_meta` key
                           io.modelcontextprotocol/logLevel. The tool result reports the level it
                           actually observed, so a fixture can assert the capability is REACHABLE
                           rather than merely present.
      dep__request_sampling — needs a model completion. Under v1 it issues a server->client
                           sampling/createMessage; under v2 it returns an InputRequiredResult and
                           completes on the resend.
      dep__list_roots    — needs the client's roots. Under v1 it issues a server->client roots/list;
                           under v2 it returns an InputRequiredResult and completes on the resend.

    Plus a paired target factory per era, so the matrix can drive the same three tools twice.

    Deliberately absent (D-11): no #[deprecated] attribute, no tracing::warn, no new log line, no
    behavioural difference of any kind attributable to deprecation. The capabilities keep working
    and say nothing.
  </behavior>
  <action>
Create `crates/pmcp-team-servers/src/conformance/deprecated_caps.rs`, gated behind the crate's
existing `conformance` feature (which is in `default`), and declare it from
`src/conformance/mod.rs`.

Build:
1. A `pmcp::Server` exposing the three tools named in `<behavior>`, each with a non-empty
   description and a well-formed input schema.
2. Deterministic behaviour — no clocks, no randomness, no network. The fixtures assert on exact or
   predicate matches, so a nondeterministic field makes the corpus flaky.
3. A client-side sampling handler and a roots provider for the V1 arm, following the shape already
   used in `tests/conformance.rs:158-175`, returning fixed deterministic values.
4. Era-paired target factories built on the constructors plan 118-06 added — one returning a
   V1-pinned target, one a V2-pinned target, each wrapping the same server construction so the ONLY
   difference between the two arms is the negotiated era.
5. If the reference servers needed the dual accept-list in plan 118-06, apply the same
   `with_supported_protocol_versions([LATEST_PROTOCOL_VERSION, PROTOCOL_VERSION_2026_07_28])` here,
   citing plan 118-06's measurement. Read 118-06-SUMMARY before deciding; do not re-derive.

Module doc must state: this server exists ONLY to prove CONF-03; its three tools map one-to-one onto
Roots, Sampling and Logging; D-12's reading is that the capability must be reachable via its v2
mechanism, NOT that the v1 RPC shapes answer under v2; and D-11 forbids any runtime signal, so
nothing here emits a deprecation warning.

Rustdoc every public item with a working example (C-5). Keep every function under cognitive
complexity 25 (C-3).
  </action>
  <verify>
    <automated>RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features && cargo test -p pmcp-team-servers --lib conformance::deprecated_caps 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three tool names appear in the source:
      `for t in dep__log_emit dep__request_sampling dep__list_roots; do grep -q "\"$t\"" crates/pmcp-team-servers/src/conformance/deprecated_caps.rs || echo "MISSING $t"; done`
      prints nothing.
    - D-11 is honoured: `grep -cE '#\[deprecated|tracing::warn|warn!' crates/pmcp-team-servers/src/conformance/deprecated_caps.rs`
      returns 0.
    - Determinism: `grep -cE 'SystemTime|Instant::now|rand::|uuid::Uuid::new_v4' crates/pmcp-team-servers/src/conformance/deprecated_caps.rs`
      returns 0.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features` exits 0 (the
      dev-dependency-free build).
    - `cargo doc -p pmcp-team-servers --no-deps` exits 0 with no missing-docs warning for the new
      module.
    - `pmat analyze complexity --format json --max-cognitive 25 | jq '.violations[] | select(.path | contains("deprecated_caps"))'`
      emits nothing.
  </acceptance_criteria>
  <done>A deterministic, warning-free target server exposes the three deprecated capabilities and can be driven under either era through the plan-118-06 constructors.</done>
</task>

<task type="auto">
  <name>Task 2: The CONF-03 fixtures, their matrix wiring, and a coverage test that fails on deletion</name>
  <files>contracts/team-servers/fixtures/deprecated-caps, crates/pmcp-team-servers/tests/conformance.rs, crates/pmcp-team-servers/baselines/era-deltas.yaml, crates/pmcp-team-servers/tests/era_baseline.rs</files>
  <read_first>
    - contracts/team-servers/fixtures/mem-mcp/mem-lifecycle.01-add.json — a positive `tool_call`
      with `scenario`, `order`, `capture` and a `predicate` match.
    - contracts/team-servers/fixtures/team-mcp/team_mcp__self-call.error.json — a negative case with
      `_meta` guard state and a `_note` annotation.
    - contracts/team-servers/fixtures/team-mcp/tools_list.json — the one-per-directory tools_list
      fixture shape.
    - crates/pmcp-team-servers/tests/conformance.rs — the state plan 118-06 left it in: the
      `era_matrix_is_conformant` driver, `MINIMUM_MATRIX_CASES`, `MINIMUM_ERA_OBSERVATIONS`, the
      `load_dir` / `exercised_tools` helpers at :284-320, and the coverage tests at :320-407.
    - crates/pmcp-team-servers/baselines/era-deltas.yaml — the reconciled, non-provisional baseline
      after plan 118-06 Task 3, including the three D-12 rows.
    - crates/pmcp-team-servers/src/conformance/runner.rs — the match modes at :68-80 and the
      `schema_version != "2"` check, so the new fixtures use the format the runner actually accepts.
  </read_first>
  <behavior>
    A new fixture directory `contracts/team-servers/fixtures/deprecated-caps/` whose cases, replayed
    under both eras by the plan-118-06 matrix, produce exactly these outcomes:

      logging  — v1: the level set by logging/setLevel is observed by dep__log_emit
                 v2: the level supplied in _meta io.modelcontextprotocol/logLevel is observed
                 the era difference is EXPECTED, matching baseline rows method.logging_set_level
                 and meta.log_level

      sampling — v1: dep__request_sampling completes via a server->client sampling/createMessage
                 v2: it completes via InputRequiredResult on the resend
                 the era difference is EXPECTED, matching baseline row result.input_required

      roots    — v1: dep__list_roots completes via a server->client roots/list
                 v2: it completes via InputRequiredResult on the resend
                 the era difference is EXPECTED, matching baseline row result.input_required

    In BOTH eras every capability COMPLETES. CONF-03's claim is reachability, not sameness — the
    mechanism differs and that difference is the baselined expectation, never a failure.
  </behavior>
  <action>
Create `contracts/team-servers/fixtures/deprecated-caps/` following the conventions the existing 33
files already use — `"schema_version": "2"` (unchanged by D-08, which was prose-only), one
`tools_list.json` for the directory, `<scenario>.<NN>-<step>.json` for ordered sequences,
`<tool>.success.json` for independent cases, and `case_id` shaped
`deprecated-caps.<tool>.<positive|negative-<guard>>`.

Write one ordered scenario per capability, plus at least one negative case per capability (for
example: a log level the server should not emit below; a sampling resend carrying no supplied
content; a roots resend supplying an empty list). Use `_note` keys to explain inline WHY a case
exists and which of D-12's readings it encodes — `_note*` keys are ignored by matching, so they are
the sanctioned place for that.

Wire the directory into the matrix in `crates/pmcp-team-servers/tests/conformance.rs`:
- add a `deprecated_caps_is_conformant` test mirroring the four existing `*_is_conformant` tests,
  with its own hard-coded pass floor (not derived from the corpus length);
- extend `era_matrix_is_conformant` to replay the new directory under both eras alongside the four
  reference servers, and RAISE `MINIMUM_MATRIX_CASES` by the number of new cases. Never lower it.
- add a coverage test in the shape of `team_mcp_covers_every_guard` (:320-407): load the directory,
  collect the `case_id`s, and assert each of the three capability families and each named guard
  appears. Deleting a fixture must fail this test rather than silently shrinking coverage.

Reconcile the baseline: run the matrix, and for any era difference the new fixtures produce that has
no matching `observation_id`, ADD a row with a checkable `source` (a spec section or SEP number is
sufficient; `MIN_SOURCE_CHARS = 10` enforces that a bare label is not). Then RAISE `MINIMUM_DELTAS`
in `crates/pmcp-team-servers/tests/era_baseline.rs` to the new count. If a new fixture produces an
UNEXPECTED difference you cannot cite a spec source for, that is a finding — record it and stop
rather than baselining it to make the test pass.
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers --test conformance 2>&1 | tee /tmp/118-07-t2.log | tail -30; grep -qE '^running [1-9][0-9]* tests?$' /tmp/118-07-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `ls contracts/team-servers/fixtures/deprecated-caps/*.json | wc -l` is >= 7 (one tools_list,
      three positive scenarios, at least three negatives).
    - Every new fixture carries the unchanged format marker:
      `grep -L '"schema_version": "2"' contracts/team-servers/fixtures/deprecated-caps/*.json`
      prints nothing.
    - `cargo test -p pmcp-team-servers --test conformance` exits 0 with a nonzero test count, and
      the count is strictly greater than the count plan 118-06 recorded.
    - The three capabilities are covered by name:
      `for c in log_emit request_sampling list_roots; do grep -rq "dep__$c" contracts/team-servers/fixtures/deprecated-caps/ || echo "MISSING $c"; done`
      prints nothing.
    - Coverage negative control, executed and reverted: delete one deprecated-caps fixture, confirm
      the coverage test FAILS naming the missing guard, restore it. Record the failure message.
    - `MINIMUM_MATRIX_CASES` and `MINIMUM_DELTAS` were both RAISED; record the before and after
      values for each.
    - `grep -c 'provisional: true' crates/pmcp-team-servers/baselines/era-deltas.yaml` still
      returns 0.
    - `cargo test -p pmcp-team-servers --test era_baseline` exits 0 with a nonzero test count.
    - `RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features` exits 0.
  </acceptance_criteria>
  <done>Roots, Sampling and Logging each complete under BOTH eras through their era-appropriate mechanism, their differences are baselined as expected with citable sources, and deleting any fixture fails a coverage test.</done>
</task>

<task type="auto">
  <name>Task 3: State the 12-month advisory window in docs/v1-sunset-policy.md without contradicting it</name>
  <files>docs/v1-sunset-policy.md</files>
  <read_first>
    - docs/v1-sunset-policy.md — read the whole document. In particular: the "What is deliberately
      NOT severed" table at ~:68-84 (3 columns, one prose paragraph per cell); "The removal
      condition" at ~:120-130, which contains the clause you must reconcile with — "**Not
      scheduled.** There is **no date and no committed window** in this policy, deliberately."; the
      runnable-verification section shape at ~:137-213; and "Explicit non-commitments" at ~:215-234,
      which already forbids `#[deprecated]` attributes and a runtime warning on v1 negotiation.
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-11 verbatim,
      and the `<deferred>` list (SMPL-F1 remains unscheduled — do not accidentally schedule it).
    - .planning/REQUIREMENTS.md line ~991 — the "Deprecated, not removed — 12-month advisory"
      decision behind CONF-03.
    - .planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md — assumption A8:
      the window is anchored to the 2026-07-28 final-spec date unless stated otherwise.
  </read_first>
  <action>
Add a new section to `docs/v1-sunset-policy.md` recording the CONF-03 deprecation, and resolve the
tension the document currently carries.

The tension, stated plainly so the new text can address it: the document says there is **no date and
no committed window** — that clause is about **removing v1 support entirely** (SMPL-F1, a future
pmcp 3.0, adoption-gated). D-11 introduces a **12-month advisory window** for **three named
capabilities** whose v2 mechanisms have replaced their v1 RPC shapes. These are different, narrower
claims. The new section MUST say so explicitly and MUST NOT edit the removal-condition clause into
something weaker — instead, add a cross-reference from the new section to it, naming the distinction:
capability deprecation is not v1 removal.

Content required:

1. A section titled for the three capabilities, listing Roots, Sampling and Logging with, for each:
   its v1 mechanism, its v2 mechanism (Logging: the `_meta` key
   `io.modelcontextprotocol/logLevel`, which the v2 schema says verbatim "Replaces the former
   `logging/setLevel` RPC"; Sampling and Roots: `InputRequiredResult`, SEP-2322), and the statement
   that the capability remains fully functional under v2 through that mechanism.
2. The window: 12 months, advisory only, anchored to the 2026-07-28 final-spec date. State the
   resulting end date explicitly so a reader does not have to compute it, and state that advisory
   means nothing changes at the end of the window without a separate decision.
3. The explicit reconciliation paragraph described above.
4. What "deprecated" does NOT mean here, extending the existing "Explicit non-commitments" list with
   a fourth bullet in the same voice: **no runtime warning on a still-supported capability.** Give
   the reason D-11 gives — a warn on a still-supported capability trains users to ignore warnings
   and would fire for a year — and add the supporting evidence that warn-capture assertions carry
   their own maintenance burden, citing the `oauth_store_wiring` flake.
5. A runnable-verification block in the shape of the section at ~:137-213: name the fixture
   directory `contracts/team-servers/fixtures/deprecated-caps/`, give the exact command
   `cargo test -p pmcp-team-servers --test conformance`, and add the document's characteristic
   "things that can never prove this, and why" list — for this claim, at minimum: a passing run with
   a zero test count; a matrix that observed no era difference at all; and the official suite's v2
   set, which contains no direct Roots or Sampling server scenario and therefore cannot prove
   CONF-03 even in principle.
  </action>
  <verify>
    <automated>grep -q '12-month' docs/v1-sunset-policy.md && grep -q 'deprecated-caps' docs/v1-sunset-policy.md && grep -q 'io.modelcontextprotocol/logLevel' docs/v1-sunset-policy.md && make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - `docs/v1-sunset-policy.md` contains the literal strings `12-month`,
      `io.modelcontextprotocol/logLevel`, `InputRequiredResult`, `SEP-2322`,
      `contracts/team-servers/fixtures/deprecated-caps/`, and
      `cargo test -p pmcp-team-servers --test conformance`.
    - The existing removal-condition clause is UNCHANGED:
      `git diff docs/v1-sunset-policy.md | grep -c '^-.*no date and no committed window'` returns 0.
    - The new section explicitly distinguishes capability deprecation from v1 removal — it contains
      a sentence naming both and stating they are different claims. Quote it in the SUMMARY.
    - The computed window end date appears as a literal date, and matches 12 months after
      2026-07-28; state the arithmetic in the SUMMARY.
    - The fourth non-commitment bullet exists and names the no-runtime-warning rule.
    - No runtime signal was added anywhere by this plan:
      `git diff --stat src/ crates/pmcp-team-servers/src/` shows changes only in
      `crates/pmcp-team-servers/src/conformance/`, and
      `git diff src/ | grep -cE '^\+.*warn!'` returns 0.
    - `make quality-gate` exits 0 (this also runs `make check-todos`, so the new prose must carry no
      SATD marker).
  </acceptance_criteria>
  <done>The policy records the three capabilities, their v2 mechanisms, the 12-month advisory window with its end date, the explicit distinction from v1 removal, the no-runtime-warning non-commitment, and a runnable verification block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture JSON -> runner | New checked-in fixture files are parsed and replayed under both eras |
| Baseline YAML -> conformance verdict | A new baseline row can convert a real regression into an "expected difference" |
| Documentation -> operator expectation | The sunset policy is what downstream users plan their migrations against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-32 | Repudiation | A CONF-03 claim proved by fixtures that were silently deleted | mitigate | A coverage test in the `team_mcp_covers_every_guard` shape asserts each capability family and each named guard has a fixture; the deletion negative control is executed and recorded |
| T-118-33 | Tampering | An unexplained era difference baselined just to make the matrix green | mitigate | Every new baseline row needs a citable `source` (>= `MIN_SOURCE_CHARS`); an uncitable difference is a FINDING that halts the task rather than a row to add |
| T-118-34 | Repudiation | "Fully functional under v2" asserted without the capability ever completing | mitigate | Fixtures assert COMPLETION through the v2 mechanism (`_meta` logLevel observed; `InputRequiredResult` resolved on the resend), not mere presence in a capability list |
| T-118-35 | Information Disclosure | A deprecation warning leaking internal state or training users to ignore warnings | mitigate | D-11 forbids any runtime signal; asserted by `grep` for `#[deprecated]`, `warn!` and `tracing::warn` returning 0 in the new module and by a zero-diff check over root `src/` |
| T-118-36 | Repudiation | A policy document that contradicts itself, so neither claim is trustworthy | mitigate | The removal-condition clause is asserted UNCHANGED by `git diff`, and the new section must contain an explicit sentence distinguishing capability deprecation from v1 removal, quoted into the SUMMARY |
| T-118-37 | Denial of Service | A nondeterministic fixture server making the corpus flaky and the gate ignorable | mitigate | The target forbids clocks, randomness and network by `grep`-checked acceptance criterion |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds no dependency |
</threat_model>

<verification>
- `cargo test -p pmcp-team-servers --test conformance` exits 0 with a test count greater than plan
  118-06's.
- `cargo test -p pmcp-team-servers --test era_baseline` exits 0 with a nonzero test count.
- `RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features` exits 0.
- The coverage negative control (delete a fixture -> coverage test FAILS) was executed and reverted.
- `git diff src/ | grep -cE '^\+.*warn!'` returns 0.
- `make quality-gate` exits 0.

**Latency expectation (not a hang).** `make quality-gate` is **8-20 minutes**; the matrix run grows with the new fixtures. Let them finish; do not interrupt and retry, and do not add a watch-mode or `--no-fail-fast`-style flag to make them feel faster.
</verification>

<success_criteria>
- Roots, Sampling and Logging each complete under v1 through their v1 mechanism and under v2 through
  their v2 mechanism, proved by fixtures replayed by the SAME matrix that discharges CONF-02.
- Each capability's era difference is an expected, cited baseline row rather than a finding.
- Deleting any deprecated-capability fixture fails a coverage test.
- The 12-month advisory window is documented, distinguished from v1 removal, and carries a runnable
  verification block.
- No runtime signal of any kind was added.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-07-SUMMARY.md` when done.
Record: the fixture inventory with each case's purpose, the before/after `MINIMUM_MATRIX_CASES` and
`MINIMUM_DELTAS`, the coverage negative-control transcript, the quoted reconciliation sentence from
the policy, and the window end-date arithmetic.
</output>
