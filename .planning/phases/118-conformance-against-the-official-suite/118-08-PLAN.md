---
phase: 118-conformance-against-the-official-suite
plan: 08
type: execute
wave: 3
depends_on: ["118-02", "118-05", "118-06"]
files_modified:
  - scripts/run-conformance-suite.sh
  - scripts/run-era-matrix.sh
  - Makefile
autonomous: true
requirements: [CONF-01, CONF-02]

must_haves:
  truths:
    - "`./scripts/run-conformance-suite.sh` starts ONE server process, runs both requirement sets against it, and tears the process down even on the error path"
    - "The script fails when either requirement set exits non-zero"
    - "The script fails when a run reports zero scenarios — a vacuous conformance run cannot pass"
    - "The script fails when the runner's Node major version is below 22"
    - "`./scripts/run-era-matrix.sh` fails when the harness reports `running 0 tests`"
    - "`./scripts/run-era-matrix.sh` proves the team-servers crate builds dev-dependency-free before running anything"
    - "Both requirement-set names live in ONE array in the script, so a wiring test can pin them AS DATA"
    - "Neither script contains `--expected-failures`, `--all-features` on the suite leg, `|| true`, or a bare readiness `sleep`"
  artifacts:
    - path: "scripts/run-conformance-suite.sh"
      provides: "the CONF-01 driver: one process, two runs, readiness poll, trap teardown, artifact dir"
      contains: "REQUIREMENT_SETS"
      min_lines: 120
    - path: "scripts/run-era-matrix.sh"
      provides: "the CONF-02 driver: dev-dependency-free build fence + harness run + nonzero-count guard"
      contains: "assert_nonzero_test_count"
      min_lines: 60
    - path: "Makefile"
      provides: "test-conformance and test-era-matrix targets, deliberately not chained into quality-gate"
      contains: "test-conformance"
  key_links:
    - from: "scripts/run-conformance-suite.sh"
      to: "conformance/node_modules/.bin/conformance"
      via: "npm ci --prefix conformance then invoke the pinned binary"
      pattern: "npm ci"
    - from: "scripts/run-era-matrix.sh"
      to: "cargo test -p pmcp-team-servers --test conformance"
      via: "the harness run whose test count is guarded"
      pattern: "pmcp-team-servers"
---

<objective>
Put the conformance commands in scripts, so plan 118-09's structural wiring test can pin them AS
DATA rather than restating them, and so the local spelling and the CI spelling cannot diverge.

Purpose: `scripts/run-severance-proofs.sh` established this pattern in Phase 117, and
`ci.yml:428-433` states the rule in a comment: the commands live in the script, not inline, so
`tests/ci_severance_gate_wiring.rs` can pin them — including the zero-count guard, which CANNOT live
inside a `#![cfg]`-selected test file (Shared Pattern 6: a guard that polices compilation must live
OUTSIDE the compilation unit).

RESEARCH found there is **no working launch-and-probe CI pattern in this repo to copy**:
`mcp-tester-validation.yml` sets `MCP_TESTER_BIN=echo`, `Makefile:280` invokes
`scripts/test_examples_with_tester.sh` with `|| true`, and that script uses a bare `sleep 2`. This
plan builds the missing pattern.

Output: two scripts and two Makefile targets. Plan 118-09 wires them into blocking CI jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md
@.planning/phases/118-conformance-against-the-official-suite/118-02-SUMMARY.md
@.planning/phases/118-conformance-against-the-official-suite/118-05-SUMMARY.md
@.planning/phases/118-conformance-against-the-official-suite/118-06-SUMMARY.md

<interfaces>
<!-- Exact values and commands. Do NOT re-derive. -->

The two runs (D-04), against ONE process (D-06):
  conformance server --url http://127.0.0.1:<port>/ --requirements 2025-11-25 -o results/v1
  conformance server --url http://127.0.0.1:<port>/ --requirements 2026-07-28 -o results/v2

Exit-code semantics with --requirements, read from the suite bundle's Bc():
  exit 1 iff any SCORED scenario has a FAILURE check. `not_scored` entries (reasons `extension`,
  `pending`, `added-after-release`) run, are reported, and CANNOT fail the job. That scored set is
  D-03's boundary per D-14.

Mutually exclusive with --requirements — the CLI hard-exits if combined:
  --suite, --scenario, --spec-version

FORBIDDEN (D-03): --expected-failures. There is no known-fail allowlist.

Scored-set sizes measured on the pinned suite: 33 scenarios at 2025-11-25, 50 at 2026-07-28.

The target: examples/s54_v2_dual_conformance, default bind 127.0.0.1:8149, bind address overridable
by argv[1]. Needs PMCP_REQUEST_STATE_KEY in the environment; never echo its value.
Port hygiene: t04/t05 use 8080/8081, s47 uses 8147.

The CONF-02 harness run:
  cargo test -p pmcp-team-servers --test conformance
The dev-dependency-free existence fence (D-09):
  RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features
  (`cargo test` sees this crate's dev-dep `pmcp = { features = ["full"] }` and unifies the feature
   back on; `cargo build` does not. That is why the build fence and the test run are separate steps.)

The zero-count guard to copy in SHAPE (scripts/run-severance-proofs.sh:82-106):
  grep -qE '^running [1-9][0-9]* tests?$' "$log"   -> otherwise `fail`, with a FAILURE MODE /
  CONSEQUENCE / WHAT TO DO message that says explicitly "do NOT delete the proof or relax this guard".

nextest selector rule: `binary(conformance)`, NEVER `test(/conformance/)`. The latter silently
selects ZERO tests and exits 0; it bit Phase 114 seven times.

Makefile analog (Makefile:309-323) — the `test-severance` target: a `.PHONY` target that shells to
the script, deliberately NOT chained into `quality-gate`, with a comment stating where the blocking
enforcement actually lives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/run-conformance-suite.sh — one process, two runs, readiness poll, trap teardown</name>
  <files>scripts/run-conformance-suite.sh</files>
  <read_first>
    - scripts/run-severance-proofs.sh — read the whole file (129 lines). Copy its STRUCTURE
      verbatim and swap the payload: the `set -euo pipefail` line, the commands-as-data arrays with
      the comment tying them to the wiring test, the `mktemp -d` + `trap ... EXIT` pair, the
      `fail()` helper, `assert_nonzero_test_count`'s three-part failure message, and the
      `cmd | tee "$log"` loop (`pipefail` is what makes a teed command still fail the script).
    - scripts/test_examples_with_tester.sh — read line ~45. Its bare `sleep 2` is the ANTI-pattern
      this script must not copy, and its ports 8080/8081 are the collision to avoid.
    - conformance/README.md — the two-run semantics and the forbidden flags, written by plan 118-02.
    - .planning/phases/118-conformance-against-the-official-suite/118-05-SUMMARY.md — the measured
      pass counts and the exact invocation that produced them.
  </read_first>
  <behavior>
    The script must FAIL (non-zero exit, with a named cause) when:
      - the runner's Node major version is below 22
      - `npm ci --prefix conformance` fails
      - the example fails to build
      - the server does not become ready within the readiness budget
      - either requirement-set run exits non-zero
      - either run reports zero scenarios (a vacuous run)
      - the server process died between the two runs (the D-06 one-process claim)

    The script must SUCCEED, and leave `results/v1/` and `results/v2/` populated, when both scored
    sets are green.

    The server process must be killed on EVERY exit path, including the error paths.
  </behavior>
  <action>
Create `scripts/run-conformance-suite.sh`, executable, modelled structurally on
`scripts/run-severance-proofs.sh`.

Header comment — follow the analog's convention of numbering each fence and naming the false-green
it closes. At minimum: (1) the Node 22 assertion, and that `npm ci` succeeds on Node 20 while the
suite dies at load with `SyntaxError: The requested module 'fs' does not provide an export named
'globSync'`; (2) `npm ci` not `npm install`, so the committed lockfile is authoritative;
(3) `--requirements` not `--spec-version`, because a requirement set is FROZEN at revision ship and
gives a stable scored set plus a revision-aware exit code; (4) no `--expected-failures`, because
D-03 admits no known-fail allowlist; (5) ONE process for both runs (D-06), because two processes
prove "pmcp can serve v1" and "pmcp can serve v2" separately rather than that one binary does both,
and a single process also catches cross-era state bleed; (6) a readiness POLL rather than a `sleep`;
(7) a distinct port away from 8080/8081/8147.

Commands as DATA — the single source plan 118-09's wiring test pins:
  - `REQUIREMENT_SETS=(2025-11-25 2026-07-28)` with a comment stating that plan 118-09's
    `tests/ci_conformance_gate_wiring.rs` asserts both members appear, so the array is the one place
    D-04's two runs are declared;
  - a named constant for the example (`s54_v2_dual_conformance`), the port, and the results
    directory.

Behaviour:
  1. `set -euo pipefail`.
  2. Assert the Node major: read `node -v`, parse the major, and `fail` with an actionable message
     if it is below 22.
  3. `npm ci --prefix conformance`.
  4. `cargo build --example s54_v2_dual_conformance --features full`.
  5. Start ONE server on the chosen port with `PMCP_REQUEST_STATE_KEY` taken from the environment
     (fail with a clear message if it is unset; NEVER echo its value), record its PID.
  6. `trap` on EXIT: kill the server PID (tolerating an already-dead process) and remove the temp log
     dir. The trap fires on the error path too, which a trailing `kill` does not.
  7. Readiness POLL: repeatedly POST a minimal JSON-RPC request until the endpoint answers, with a
     bounded attempt count and a short sleep BETWEEN attempts (that is a backoff, not a readiness
     wait). `fail` with the attempt count and the last observed error if the budget is exhausted.
  8. For each member of `REQUIREMENT_SETS`, run the pinned suite binary at
     `conformance/node_modules/.bin/conformance` with `server --url http://127.0.0.1:<port>/
     --requirements <rev> -o <results>/<rev>`, teeing to a log. After each run:
     - assert the server PID is still alive (the D-06 one-process claim, checked BETWEEN runs, not
       only at the end);
     - assert the log reports a nonzero scenario count. Write this guard in the shape of
       `assert_nonzero_test_count`, with its own three-part failure message explaining that a run
       reporting zero scenarios and exiting 0 makes "ran and passed" indistinguishable from "never
       ran", and that the remedy is never to delete the guard. Derive the expected shape from the
       suite's own `Running requirements <rev> (N scenarios)` line; do NOT hard-code N — the scored
       set legitimately changes at re-pin, and D-115-AI(4) forbids deriving a fence's expected value
       from the artifact under test, which a hard-coded 33/50 would invert into brittleness. Guard
       on N >= 1 only.
  9. Print a final summary naming both runs and the results directory.

Do NOT add `|| true`, `continue-on-error`, `--expected-failures`, `--suite`, `--scenario`,
`--spec-version`, or `--all-features` anywhere in this script.
  </action>
  <verify>
    <automated>bash -n scripts/run-conformance-suite.sh && shellcheck scripts/run-conformance-suite.sh 2>/dev/null; test -x scripts/run-conformance-suite.sh && PMCP_REQUEST_STATE_KEY=$(head -c 32 /dev/zero | od -An -tx1 | tr -d ' \n') ./scripts/run-conformance-suite.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/run-conformance-suite.sh` exits 0 and the file is executable.
    - `./scripts/run-conformance-suite.sh` exits 0 end to end under Node 22, and leaves
      `results/2025-11-25/` and `results/2026-07-28/` (or the chosen naming) populated with
      `checks.json`.
    - `grep -c 'REQUIREMENT_SETS' scripts/run-conformance-suite.sh` is >= 2, and the array literal
      contains both `2025-11-25` and `2026-07-28`.
    - Forbidden constructs absent from the COMMANDS (comments stripped first, since the header
      explains at length why each is forbidden):
      `grep -v '^\s*#' scripts/run-conformance-suite.sh | grep -cE '\-\-expected-failures|\|\| true|--all-features|--spec-version|--suite |--scenario '`
      returns 0.
    - No bare readiness sleep: the script contains a polling loop, and
      `grep -v '^\s*#' scripts/run-conformance-suite.sh | grep -c 'sleep' ` matches only lines
      inside the poll's backoff — state the line numbers in the SUMMARY.
    - `grep -c 'set -euo pipefail' scripts/run-conformance-suite.sh` is >= 1 and
      `grep -c 'trap ' scripts/run-conformance-suite.sh` is >= 1.
    - The key is never echoed: `grep -c 'echo.*PMCP_REQUEST_STATE_KEY\|\$PMCP_REQUEST_STATE_KEY"' scripts/run-conformance-suite.sh`
      shows no line that prints its value; state the audited lines in the SUMMARY.
    - Negative controls, each executed and recorded: (a) run with Node 20 on PATH and confirm the
      script fails at the version assertion with an actionable message; (b) kill the server between
      the two runs and confirm the liveness check fails naming the PID; (c) point the URL at a dead
      port and confirm the readiness poll fails with its attempt count. Restore after each.
    - After the script exits, no orphaned process holds the port:
      `lsof -ti :<port>` returns nothing. Verified after both a successful run and a forced failure.
  </acceptance_criteria>
  <done>One invocation builds, launches, polls, runs both requirement sets against one live process, tears it down on every path, and fails loudly on each of the three executed negative controls.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/run-era-matrix.sh — the dev-dependency-free fence and the nonzero-count guard</name>
  <files>scripts/run-era-matrix.sh</files>
  <read_first>
    - scripts/run-severance-proofs.sh — lines 22-42 (why a guard that polices compilation must live
      OUTSIDE the compilation unit) and lines 82-106 (`assert_nonzero_test_count` verbatim). Copy
      both, including the reasoning comments.
    - crates/pmcp-team-servers/Cargo.toml — the `[features]` block and the `[dev-dependencies]`
      `pmcp = { features = ["full"] }` line that makes `cargo test` unify the feature back on.
    - crates/pmcp-team-servers/tests/conformance.rs — the `#![cfg(feature = "conformance")]` guard
      at :21 and the test names, so the guard has the right thing to grep for.
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-09 verbatim.
    - .planning/phases/118-conformance-against-the-official-suite/118-06-SUMMARY.md — the test count
      the matrix reports, so the SUMMARY can compare.
  </read_first>
  <behavior>
    The script must FAIL when:
      - `RUSTFLAGS="-D warnings" cargo build -p pmcp-team-servers --all-features` fails or warns
      - the harness run reports `running 0 tests`
      - the harness run itself fails
      - `cargo test -p pmcp-team-servers --test era_baseline` reports zero tests or fails

    It must SUCCEED only when both runs report a NONZERO test count and pass.
  </behavior>
  <action>
Create `scripts/run-era-matrix.sh`, executable, in the same structural shape as
`scripts/run-severance-proofs.sh`.

Header comment, numbering the fences and naming each false-green:
  1. `-p pmcp-team-servers` — the workspace root is the `pmcp` package; a workspace-wide command
     tests something else entirely.
  2. `cargo build`, not `cargo test`, for the EXISTENCE claim — `cargo test` sees this crate's
     dev-dependency `pmcp = { features = ["full"] }` and unifies the feature back on, so a missing
     feature declaration in `[dependencies]` is invisible to it. This is the exact mechanism
     recorded in `project_cargo_feature_severance_false_greens`, and the same mechanism that made
     Phase 117's proofs report `running 0 tests` and exit 0.
  3. `RUSTFLAGS="-D warnings"` — `make lint` passes `-D clippy::all` but not a bare `-D warnings`,
     so a stranded helper would emit a rustc `dead_code` lint and still build green.
  4. The nonzero-test-count guard lives HERE, in the shell, not in the test file:
     `assert!(!cfg!(feature = "x"))` inside a `#![cfg]`-selected file expands to `!false` and cannot
     fail — and on the build where it would be false, the file does not compile.
  5. Why this is not folded into `make quality-gate`: the gate is scoped to the root `pmcp` package
     and does not reach `crates/pmcp-team-servers/tests/` at all. Name that gap explicitly — it is
     the reason CONF-02 needs its own execution wiring (RESEARCH Pitfall 4).

Commands as DATA, so plan 118-09's wiring test can pin them:
  - `MATRIX_TESTS=(conformance era_baseline)` — the test-target names, with a comment stating the
    wiring test asserts both appear;
  - a named constant for the dev-dependency-free build fence flags.

Behaviour:
  1. `set -euo pipefail`; `mktemp -d` log dir with a `trap ... EXIT` cleanup; the `fail()` helper.
  2. Run the dev-dependency-free build fence FIRST and fail loudly if it does not exit 0.
  3. For each member of `MATRIX_TESTS`: `cargo test -p pmcp-team-servers --test "$t" | tee "$log"`,
     then `assert_nonzero_test_count "$t" "$log"` copied in shape from the analog, with its
     FAILURE MODE / CONSEQUENCE / WHAT TO DO message adapted — the consequence being that "ran and
     passed" and "never compiled" become indistinguishable, and the remedy never being to delete the
     proof or relax the guard.
  4. Print a summary naming both test targets and their reported counts.

If you use `cargo nextest` anywhere, the selector is `binary(<name>)`. `test(/<name>/)` is FORBIDDEN
— it silently selects ZERO tests and exits 0, and it bit Phase 114 seven times. Do not add
`|| true` or `continue-on-error`.
  </action>
  <verify>
    <automated>bash -n scripts/run-era-matrix.sh && test -x scripts/run-era-matrix.sh && ./scripts/run-era-matrix.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/run-era-matrix.sh` exits 0 and the file is executable.
    - `./scripts/run-era-matrix.sh` exits 0 and its output shows a nonzero test count for BOTH
      `conformance` and `era_baseline`; record both counts in the SUMMARY.
    - `grep -c 'assert_nonzero_test_count' scripts/run-era-matrix.sh` is >= 2 (definition plus at
      least one call).
    - `grep -c 'running \[1-9\]' scripts/run-era-matrix.sh` is >= 1 — the guard greps for a nonzero
      count, not merely for the word `running`.
    - `grep -v '^\s*#' scripts/run-era-matrix.sh | grep -cE 'test\(/|\|\| true|continue-on-error'`
      returns 0.
    - `grep -c 'cargo build' scripts/run-era-matrix.sh` is >= 1 AND that line carries
      `--all-features` and `RUSTFLAGS="-D warnings"` — the dev-dependency-free existence fence.
    - Negative control, executed and recorded: temporarily rename the matrix test so the run selects
      nothing, confirm the script FAILS at the zero-count guard rather than passing green, then
      revert. Quote the failure message.
  </acceptance_criteria>
  <done>The script proves the crate builds dev-dependency-free, runs both test targets, and fails on a zero test count — proved by an executed negative control.</done>
</task>

<task type="auto">
  <name>Task 3: Makefile targets — the local spelling, deliberately outside quality-gate</name>
  <files>Makefile</files>
  <read_first>
    - Makefile — read lines 300-345: the `test-integration` target above and the `test-severance`
      target (309-323) with its full comment block. That comment is the template: it explains why
      the target is NOT chained into `quality-gate`, where the blocking enforcement actually lives,
      and which test pins the script.
    - scripts/run-conformance-suite.sh and scripts/run-era-matrix.sh — the two scripts from Tasks 1
      and 2.
    - CLAUDE.md — the repo's Makefile convention. Note the standing global preference for a
      `justfile`: this repo has no `justfile` and every gate is Makefile-driven, so follow the repo.
      Do NOT introduce a `justfile` in a conformance phase.
  </read_first>
  <action>
Add two `.PHONY` targets to the `Makefile`, each modelled on `test-severance` (309-323):

`test-conformance` — shells to `./scripts/run-conformance-suite.sh`. Comment must state:
  - what it proves (CONF-01: the official suite's scored sets at both revisions, against one
    dual-version example process);
  - why it is deliberately NOT chained into `quality-gate` (it needs Node 22, an `npm ci`, and a
    live server on a bound port — the dev loop should not require any of those);
  - where the blocking enforcement actually lives: `.github/workflows/ci.yml`'s conformance job,
    which plan 118-09 wires into `gate.needs`, so a failure is a red required check;
  - that `tests/ci_conformance_gate_wiring.rs` pins both the script's contents and its wiring into
    the blocking gate;
  - that `PMCP_REQUEST_STATE_KEY` must be set in the environment.

`test-era-matrix` — shells to `./scripts/run-era-matrix.sh`. Comment must state:
  - what it proves (CONF-02/CONF-03: every fixture replayed under both eras, joined against the
    expected-difference baseline, on a dev-dependency-free build with a nonzero test count);
  - why it is deliberately NOT chained into `quality-gate`, and — the load-bearing part — that
    `quality-gate` is scoped to the root `pmcp` package and does not reach
    `crates/pmcp-team-servers/tests/` AT ALL, which is precisely why this target and its CI job
    exist (RESEARCH Pitfall 4);
  - where the blocking enforcement lives.

Do NOT add either target to `quality-gate`, `test-all`, or `test-integration`. Do not create a
`justfile`.
  </action>
  <verify>
    <automated>make -n test-conformance >/dev/null && make -n test-era-matrix >/dev/null && grep -c 'test-conformance\|test-era-matrix' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `make -n test-conformance` prints an invocation of `./scripts/run-conformance-suite.sh`.
    - `make -n test-era-matrix` prints an invocation of `./scripts/run-era-matrix.sh`.
    - Both targets are declared `.PHONY`.
    - Neither target is reachable from `quality-gate`:
      `make -n quality-gate | grep -cE 'run-conformance-suite|run-era-matrix'` returns 0.
    - No `justfile` was created: `test -f justfile` fails.
    - Each target's comment names the CI job that provides the blocking enforcement and names
      `tests/ci_conformance_gate_wiring.rs` as the pin.
    - `make quality-gate` exits 0.
  </acceptance_criteria>
  <done>Both targets exist as the local spelling of the CI commands, are absent from `quality-gate`, and their comments name where blocking enforcement actually lives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> third-party Node CLI | The script downloads (via `npm ci`) and executes third-party JavaScript |
| Script -> spawned server process | A long-lived child process holds a TCP port for the duration of two runs |
| Environment -> script | `PMCP_REQUEST_STATE_KEY` is a secret consumed from the environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-38 | Tampering | Executing an unpinned or drifted suite version | mitigate | `npm ci --prefix conformance` against the committed lockfile from plan 118-02, never `npm install`; forbidden-flag check asserts no escape hatch was added |
| T-118-39 | Repudiation | A conformance run that reports zero scenarios and exits 0, making "conformant" meaningless | mitigate | A nonzero-scenario guard per run, and a nonzero-test-count guard per harness target, both living in the shell OUTSIDE the compilation unit; both proved by executed negative controls |
| T-118-40 | Repudiation | A run that appears to prove dual-version but used two processes | mitigate | The server PID is checked for liveness BETWEEN the two runs, not only at the end (D-06) |
| T-118-41 | Denial of Service | A leaked server process holding the port and answering the NEXT run | mitigate | `trap ... EXIT` teardown that fires on the error path too; an acceptance criterion asserts `lsof -ti :<port>` is empty after both a success and a forced failure; a distinct port away from 8080/8081/8147 |
| T-118-42 | Information Disclosure | `PMCP_REQUEST_STATE_KEY` appearing in CI logs | mitigate | The value is never echoed; the script fails with the variable NAME only when it is unset; the audited lines are recorded in the SUMMARY |
| T-118-43 | Tampering | A known-fail allowlist quietly reintroduced to make a red job green | mitigate | `--expected-failures` is absent from the commands, asserted after stripping comments (the header explains at length why it is forbidden, so an unstripped grep would self-invalidate) |
| T-118-44 | Repudiation | A test selector that silently selects zero tests | mitigate | `binary(<name>)` selectors only; `test(/.../)` forbidden by an acceptance-criterion grep |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | The only install is `npm ci` against the lockfile plan 118-02 pinned and slopcheck-cleared (`[OK]`, empty postinstall, OIDC-published). No new package is introduced here |
</threat_model>

<verification>
- `./scripts/run-conformance-suite.sh` exits 0 end to end and leaves both results directories
  populated, with no orphaned process on the port afterwards.
- `./scripts/run-era-matrix.sh` exits 0 with a nonzero test count for both test targets.
- All four negative controls (Node 20; server killed mid-run; dead port; renamed matrix test) were
  executed, failed as intended, and are recorded verbatim.
- `make -n quality-gate` does not reach either script.
- `make quality-gate` exits 0.
</verification>

<success_criteria>
- Both conformance commands live in scripts, with the requirement-set names and test-target names in
  arrays a wiring test can pin as data.
- The launch-and-probe pattern the repo lacked now exists: readiness poll, PID liveness between
  runs, trap teardown on every path.
- Every vacuity guard lives outside the compilation unit and is proved by an executed negative
  control.
- No forbidden flag, no `|| true`, no known-fail allowlist, no `justfile`.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-08-SUMMARY.md` when done.
Record: the full transcript of a successful `run-conformance-suite.sh`, the reported test counts
from `run-era-matrix.sh`, all four negative-control transcripts, and the audited lines that touch
`PMCP_REQUEST_STATE_KEY`.
</output>
