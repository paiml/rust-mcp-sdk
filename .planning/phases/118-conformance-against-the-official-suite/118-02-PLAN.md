---
phase: 118-conformance-against-the-official-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - conformance/package.json
  - conformance/package-lock.json
  - conformance/README.md
  - Cargo.toml
autonomous: true
requirements: [CONF-01]

must_haves:
  truths:
    - "`npm ci --prefix conformance` installs exactly one pinned version of @modelcontextprotocol/conformance, reproducibly, from a committed lockfile with integrity hashes"
    - "The pinned version supports `--requirements <revision>` (it is 0.2.0-alpha.11 or later, never the `latest` 0.1.16 line)"
    - "`conformance/package.json` refuses to install on Node < 22"
    - "`cargo package -p pmcp --list --allow-dirty` lists NO path under conformance/ and NO tests/ci_conformance_gate_wiring.rs"
    - "`make purity-check` still exits 0 with the Node manifest in the tree"
    - "A reader can learn from conformance/README.md why the pin exists, how to re-pin, and what the two runs prove"
  artifacts:
    - path: "conformance/package.json"
      provides: "exact-pinned suite dependency + Node 22 engine floor + private marker"
      contains: "@modelcontextprotocol/conformance"
    - path: "conformance/package-lock.json"
      provides: "integrity-hash reproducibility (the mechanism D-01 is buying)"
      contains: "integrity"
    - path: "conformance/README.md"
      provides: "the pin rationale, the re-pin procedure, and what the two runs prove"
      min_lines: 40
    - path: "Cargo.toml"
      provides: "exclude entries keeping the Node manifest and its reader out of the crates.io tarball"
      contains: "conformance/"
  key_links:
    - from: "conformance/package-lock.json"
      to: "registry.npmjs.org/@modelcontextprotocol/conformance"
      via: "resolved + integrity"
      pattern: "modelcontextprotocol/conformance"
    - from: "Cargo.toml"
      to: "conformance/"
      via: "exclude array"
      pattern: "conformance/"
---

<objective>
Land the pinned Node manifest for the official `@modelcontextprotocol/conformance` suite (D-01),
and settle its packaging disposition so the first Rust-repo-adjacent Node manifest cannot ship in
the `pmcp` crates.io tarball.

Purpose: CONF-01's referee is a Node CLI. D-01 chose a committed `package.json` +
`package-lock.json` run with `npm ci` over `npx` (a yanked or re-published version silently changes
the referee) and over a submodule or container (cost with no added reproducibility). The ledger
entry `project_ci_purity_gate_unpinned_tooling_drift` records the Purity Gate bit-rotting from
exactly this failure mode. Pin it or it rots.

Output: `conformance/{package.json,package-lock.json,README.md}` and two `exclude` entries in the
root `Cargo.toml` with the reasoning stated inline, verified the same way `115-REVIEW.md` CR-01 was
caught.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md
@.planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md
@.planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md

<interfaces>
<!-- Values the executor needs verbatim. -->

Package legitimacy (RESEARCH § Package Legitimacy Audit, disposition **Approved**, no checkpoint
required): scoped `@modelcontextprotocol/*` name; source repo
`git+https://github.com/modelcontextprotocol/conformance.git`; 27,587 weekly downloads;
`scripts.postinstall` empty (no install-time script); published via GitHub Actions OIDC trusted
publishing; `slopcheck install -e npm @modelcontextprotocol/conformance` -> `1 OK`.
NOTE: slopcheck MUST be passed `-e npm` in this repo — auto-detection picks crates.io and reports a
spurious `[ERR] registry unreachable`.

Version facts measured 2026-08-09:
  dist-tags: latest = 0.1.16 (2026-03-30), alpha = 0.2.0-alpha.11 (2026-08-07)
  0.1.16 ships NO requirements/ directory and NO --requirements flag -> unusable for D-04.
  Requirement sets available on the alpha line: 2025-11-25, 2026-07-28.

The in-repo analog manifest to copy the SHAPE of:
  tests/integration/typescript-interop/package.json  (name/version/description/type/scripts/
  dependencies/engines) and its package-lock.json (lockfileVersion 3, per-package
  resolved + integrity + license).

`.gitignore:46` already contains `**/node_modules/` — no .gitignore edit is needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the suite with an exact version, a Node 22 floor, and a committed lockfile</name>
  <files>conformance/package.json, conformance/package-lock.json</files>
  <read_first>
    - tests/integration/typescript-interop/package.json — the in-repo manifest shape to copy.
    - tests/integration/typescript-interop/package-lock.json — the lockfile shape
      (`lockfileVersion: 3`, per-package `resolved` + `integrity` + `license`).
    - .gitignore — confirm `**/node_modules/` at :46 so no ignore edit is needed.
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `conformance/package.json + conformance/package-lock.json`, which enumerates the THREE
      deltas from the analog (exact pin, engines >=22, private: true).
  </read_first>
  <action>
Create the directory `conformance/` at the repo root and pin the suite there.

Step 1 — re-check the pin before using it. RESEARCH is valid until 2026-08-23 for this specific
value. Run `npm view @modelcontextprotocol/conformance dist-tags --json` and
`npm view @modelcontextprotocol/conformance time --json | tail`. Choose the newest
`0.2.0-alpha.*` (or newer stable if the alpha line has graduated). Requirements:
  - it MUST be `0.2.0-alpha.11` or later — the `latest` 0.1.16 line has no `--requirements` flag
    and no `requirements/` directory, so D-04 is impossible on it;
  - confirm `conformance list` on the chosen version reports both requirement sets `2025-11-25`
    and `2026-07-28`;
  - re-run `slopcheck install -e npm @modelcontextprotocol/conformance` (the `-e npm` is
    mandatory) and confirm `1 OK`;
  - confirm `npm view @modelcontextprotocol/conformance@<pin> scripts.postinstall` is empty.
Record all four results verbatim in the SUMMARY. If the chosen version differs from
`0.2.0-alpha.11`, say so and say why.

Step 2 — install with an exact pin:
`npm install --save-exact @modelcontextprotocol/conformance@<pin>` inside `conformance/`, run
under Node 22+ (locally: `PATH=~/.nvm/versions/node/v22.22.2/bin:$PATH`).

Step 3 — hand-edit `conformance/package.json` to its final target state:
  - `"name": "pmcp-conformance-suite"`, `"version": "0.0.0"`, `"private": true`
  - `"description"` naming CONF-01 and the fact that this manifest exists ONLY to pin the referee
  - `"type": "module"`
  - `"engines": { "node": ">=22" }` — load-bearing: the suite bundle imports `globSync` from
    `node:fs` (Node 22+) at module scope and the published package declares NO `engines`, so
    without this floor `npm ci` succeeds on Node 20 and the first invocation dies with
    `SyntaxError: The requested module 'fs' does not provide an export named 'globSync'`
  - `"dependencies": { "@modelcontextprotocol/conformance": "<pin>" }` — exact, NO caret, NO
    tilde. The analog uses `^1.17.2`; D-01's reproducibility argument forbids a range here.
  - no `scripts` block that could hide the real invocation — the commands live in
    `scripts/run-conformance-suite.sh` (plan 118-08) so a wiring test can pin them AS DATA.

Step 4 — commit BOTH `package.json` and `package-lock.json`. Verify the lockfile carries a
`resolved` URL and an `integrity` `sha512-...` hash for `@modelcontextprotocol/conformance` — that
hash IS the reproducibility mechanism D-01 is buying. Do NOT commit `node_modules/`.

Step 5 — prove `npm ci` reproduces the pin from a clean state: remove `conformance/node_modules`,
run `npm ci --prefix conformance`, and confirm
`npm ls --prefix conformance @modelcontextprotocol/conformance` reports the pinned version.
  </action>
  <verify>
    <automated>rm -rf conformance/node_modules && npm ci --prefix conformance && npm ls --prefix conformance --depth=0 2>&1 | grep -F "@modelcontextprotocol/conformance@"</automated>
  </verify>
  <acceptance_criteria>
    - `conformance/package.json` and `conformance/package-lock.json` are tracked by git
      (`git ls-files conformance/` lists exactly the three files this plan creates).
    - `jq -r '.dependencies["@modelcontextprotocol/conformance"]' conformance/package.json` prints
      a bare version with no `^`, `~`, `>` or `*` character.
    - The pinned version is >= `0.2.0-alpha.11`, and
      `node conformance/node_modules/.bin/conformance list 2>&1 | grep -F "2026-07-28"` matches
      (the requirement sets exist on the pinned build).
    - `jq -r '.engines.node' conformance/package.json` prints a constraint containing `22`.
    - `jq -r '.private' conformance/package.json` prints `true`.
    - `jq -r '.packages["node_modules/@modelcontextprotocol/conformance"].integrity' conformance/package-lock.json`
      prints a value starting with `sha512-`.
    - `git status --porcelain conformance/node_modules` prints nothing (never committed).
    - SUMMARY records: the chosen pin, the dist-tags output, the slopcheck `-e npm` verdict, and
      the empty `scripts.postinstall`.
  </acceptance_criteria>
  <done>`npm ci --prefix conformance` reproducibly installs the exact pinned suite version from a committed lockfile, and the manifest refuses Node < 22.</done>
</task>

<task type="auto">
  <name>Task 2: Settle the packaging disposition — exclude the manifest AND its reader</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml — the `exclude` array (lines ~15-59). It already encodes the exact decision
      procedure this task needs, with the reasoning in comments on three entries
      (`tests/team_contracts_conformance.rs`, `tests/phase115_contract_bindings.rs`,
      `tests/keyword_list_mirrors.rs`).
    - .planning/phases/118-conformance-against-the-official-suite/118-PATTERNS.md — section
      `Cargo.toml exclude disposition`, which states the rule: *a test that reads a path at runtime
      must have the SAME disposition as the path it reads*, and the measured counter-example
      (ci.yml + run-severance-proofs.sh + ci_severance_gate_wiring.rs ALL ship).
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-01 trap (a):
      the Node manifest MUST be excluded from the published crate.
  </read_first>
  <action>
Add exactly TWO entries to the root `Cargo.toml` `exclude` array, each with a comment stating the
rule in the same voice as the three existing commented entries.

Entry 1: `"conformance/"` — the Node manifest and lockfile are CI tooling, never crate content.
D-01 trap (a) locks this. `115-REVIEW.md` CR-01 is the precedent for what happens when a
CI-only artifact ships.

Entry 2: `"tests/ci_conformance_gate_wiring.rs"` — plan 118-09 creates this test and it reads
`conformance/package-lock.json` at runtime to pin the suite version structurally. Excluding the
path it reads while shipping the reader would make `cargo test` panic on the published crate,
which is exactly the `tests/team_contracts_conformance.rs` disposition. The two entries must NEVER
be split: ship both or exclude both. Write that sentence into the comment.

Note explicitly in the comment why this differs from `tests/ci_severance_gate_wiring.rs`, which
SHIPS: everything that test reads (`.github/workflows/ci.yml`, `scripts/run-severance-proofs.sh`)
also ships, so its disposition is consistent. Consistency is the property, not exclusion.

Then verify the tarball, exactly the way `115-REVIEW.md` CR-01 was caught, and confirm the two
tree-inspecting CI jobs are unaffected:
  - `cargo package -p pmcp --list --allow-dirty | grep -E 'conformance/|ci_conformance_gate_wiring'`
    must produce NO output;
  - `make purity-check` must still exit 0 with the Node manifest in the tree (the Purity Gate is
    per-crate/per-feature over Cargo metadata, but confirm by running it rather than reasoning);
  - the `package-content` job in `.github/workflows/course.yml` tars only `pmcp-course/src/` and
    `pmcp-course/metadata.toml` — read it and confirm the new directory is outside its globs.
  </action>
  <verify>
    <automated>cargo package -p pmcp --list --allow-dirty 2>/dev/null | grep -E 'conformance/|ci_conformance_gate_wiring' ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo package -p pmcp --list --allow-dirty | grep -cE '^conformance/'` returns 0.
    - `cargo package -p pmcp --list --allow-dirty | grep -c 'ci_conformance_gate_wiring'` returns 0.
    - `grep -c 'conformance/' Cargo.toml` is >= 1 and
      `grep -c 'tests/ci_conformance_gate_wiring.rs' Cargo.toml` is >= 1.
    - The comment above the two entries contains the literal phrase "ship both or exclude both".
    - `make purity-check` exits 0.
    - `grep -n 'pmcp-course' .github/workflows/course.yml` confirms the `package-content` tar globs
      are limited to `pmcp-course/`; record the verdict in the SUMMARY.
    - `cargo package -p pmcp --list --allow-dirty | wc -l` is recorded in the SUMMARY alongside the
      pre-change count from `git stash`-free comparison, so a surprise mass-exclusion is visible.
  </acceptance_criteria>
  <done>Neither the Node manifest nor its future reader appears in `cargo package --list`, the reasoning is committed inline, and `make purity-check` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Write conformance/README.md — the pin rationale and the re-pin procedure</name>
  <files>conformance/README.md</files>
  <read_first>
    - crates/mcp-tester/baselines/era-deltas.yaml — its header block (lines 1-27) is the in-repo
      role model for "a data/config artifact that explains itself to a reviewer, not to a machine".
    - .planning/phases/118-conformance-against-the-official-suite/118-RESEARCH.md — § Standard
      Stack (the version facts), § Pattern 1 (the two invocations and their exit-code semantics),
      Pitfall 6 (`--requirements` freezes MEMBERSHIP, not IMPLEMENTATION — which is why the
      lockfile and the requirement set are complementary, not redundant).
    - .planning/phases/118-conformance-against-the-official-suite/118-CONTEXT.md — D-03, D-04,
      D-14 (the scored-set boundary and the no-allowlist rule).
  </read_first>
  <action>
Write `conformance/README.md` for a human reviewer arriving cold at a Node manifest in a Rust repo.
It must answer, in this order and with concrete values:

1. **What this directory is.** It pins the official `@modelcontextprotocol/conformance` suite, the
   external referee for CONF-01. It contains no source code and ships in no crate.
2. **Why a lockfile and not `npx`.** State D-01's argument: integrity hashes give commit-level
   reproducibility and survive a yanked or re-published version. Cite the in-repo precedent that
   an unpinned CLI plus a gitignored lock rotted the Purity Gate.
3. **Why this exact version and not `latest`.** `latest` is the 0.1.16 line, which has no
   `--requirements` flag and no `requirements/` directory, so the two-run shape D-04 requires is
   impossible on it. Name the pinned version.
4. **Why Node >= 22.** The suite imports `globSync` from `node:fs` at module scope and declares no
   `engines` of its own, so `npm ci` succeeds on Node 20 and the first invocation dies at load
   time. Quote the exact `SyntaxError`.
5. **What the two runs prove.** One server process, two negotiations: `--requirements 2025-11-25`
   and `--requirements 2026-07-28`. Explain, in the suite's own words, that a scenario applying to
   both must be run once under each and one run does not cover the other; and that D-06's single
   process is what makes it a claim about ONE binary rather than two.
6. **What can and cannot fail the job.** With `--requirements`, exit 1 iff a SCORED scenario has a
   FAILURE check; `not_scored` entries (reasons `extension`, `pending`, `added-after-release`) run,
   report, and cannot fail. That scored set is D-03's boundary per D-14. The Tasks extension is NOT
   implemented in the target example. The `-o results/` artifact is uploaded so not-scored results
   stay reviewable at re-pin time.
7. **What is FORBIDDEN.** No `--expected-failures` baseline and no known-fail allowlist (D-03). If
   a scenario genuinely does not apply, that is a conversation at re-pin time, not a standing
   exemption.
8. **How to re-pin.** The exact command sequence: check `npm view ... dist-tags`, run
   `npm install --save-exact @modelcontextprotocol/conformance@<new>`, re-run
   `slopcheck install -e npm @modelcontextprotocol/conformance`, confirm
   `scripts.postinstall` is empty, run `make test-conformance` locally, and review the diff in
   `results/`. Note that `--requirements` freezes which scenarios are SCORED but not their
   implementations, so a version bump can still change verdicts — that is precisely why both
   mechanisms exist.
9. **How to run it locally.** Point at `make test-conformance` (created by plan 118-08) as the
   single local spelling, and state that the blocking enforcement lives in `.github/workflows/ci.yml`,
   not in the Makefile.
  </action>
  <verify>
    <automated>test -s conformance/README.md && grep -q -- '--requirements' conformance/README.md && grep -q 'globSync' conformance/README.md && grep -q 'expected-failures' conformance/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `conformance/README.md` exists and is at least 40 lines.
    - It contains the literal strings `--requirements 2025-11-25`, `--requirements 2026-07-28`,
      `globSync`, `--expected-failures`, `slopcheck install -e npm`, and the pinned version.
    - The pinned version string in the README matches
      `jq -r '.dependencies["@modelcontextprotocol/conformance"]' conformance/package.json`
      exactly — verified by command, not by eye, and the command recorded in the SUMMARY.
    - `make quality-gate` exits 0 (confirms `make check-todos` sees no SATD in the new prose).
  </acceptance_criteria>
  <done>A reviewer arriving cold can learn the pin's rationale, the two-run semantics, the scored-set boundary, and the exact re-pin procedure without reading any plan document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> CI runner | Third-party JavaScript is downloaded and executed by a CI job |
| Repo tree -> crates.io tarball | Anything not excluded ships to every downstream consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Spoofing | Dependency confusion / typosquat on `@modelcontextprotocol/conformance` | mitigate | Scoped `@modelcontextprotocol/*` name; `--save-exact` pin; committed lockfile with `sha512-` integrity hashes; `slopcheck install -e npm` re-run at pin time with `1 OK` required |
| T-118-06 | Tampering | Malicious or drifting `postinstall` in the suite or a transitive dep | mitigate | `npm ci` (lockfile-respecting, never `npm install` in CI); `scripts.postinstall` verified empty at pin time and re-verified at every re-pin per the README procedure |
| T-118-07 | Tampering | Supply-chain drift from an unpinned CLI — the exact failure `project_ci_purity_gate_unpinned_tooling_drift` records | mitigate | Exact version pin (no caret/tilde, enforced by an acceptance criterion) + committed `package-lock.json`; re-pin is a reviewable commit |
| T-118-08 | Information Disclosure | CI tooling shipping to crates.io consumers | mitigate | `conformance/` excluded from the tarball, verified by `cargo package -p pmcp --list --allow-dirty` — the same command that caught `115-REVIEW.md` CR-01 |
| T-118-09 | Denial of Service | A published crate whose `cargo test` panics on a missing path | mitigate | The reader `tests/ci_conformance_gate_wiring.rs` is excluded together with the path it reads; "ship both or exclude both" is written into the `Cargo.toml` comment |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH § Package Legitimacy Audit disposition is **Approved** / `[OK]` (OIDC trusted publishing, MCP-org maintainers, 27.5k weekly downloads, empty postinstall). No `[ASSUMED]`/`[SUS]` package is installed, so no blocking human checkpoint is required; the slopcheck re-run at pin time is the standing control |
</threat_model>

<verification>
- `npm ci --prefix conformance` from a clean `node_modules` installs exactly the pinned version.
- `cargo package -p pmcp --list --allow-dirty` lists neither `conformance/` nor
  `tests/ci_conformance_gate_wiring.rs`.
- `make purity-check` exits 0.
- `make quality-gate` exits 0.

**Latency expectation (not a hang).** `npm ci --prefix conformance`, `make purity-check` and `make quality-gate` are all multi-minute — expect **10-25 minutes** for the trio. Let them finish; do not interrupt and retry, and do not add a watch-mode or `--no-fail-fast`-style flag to make them feel faster.
</verification>

<success_criteria>
- The suite is pinned to a `--requirements`-capable version with a committed lockfile.
- The Node 22 floor is declared, so a wrong-Node install fails at install time rather than at load
  time.
- The Node manifest and its future reader are both out of the published tarball.
- `conformance/README.md` documents the pin, the two-run semantics, the scored-set boundary, the
  no-allowlist rule, and the re-pin procedure.
</success_criteria>

<output>
Create `.planning/phases/118-conformance-against-the-official-suite/118-02-SUMMARY.md` when done.
Record: the chosen pin and why, the four legitimacy re-checks verbatim, the `cargo package --list`
entry count before and after, and the `package-content` / Purity Gate verdicts.
</output>
