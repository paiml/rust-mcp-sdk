---
schema_version: 1
open_count: 21
waived_count: 0
fixed_count: 6
total_count: 27
last_updated: 2026-08-20T05:17:48.299Z
---

# Broken Windows Ledger

> Cross-phase defect register. With `workflow.windows_enforce` enabled, `/gsd-ship` blocks while `open_count > 0`.
> Waive with `gsd-tools windows waive <id> "<reason>"` (reason required).
> Mark fixed with `gsd-tools windows fixed <id>`.

| id | phase | kind | file | line | description | status | reason | recorded_at | resolved_at |
|----|-------|------|------|------|-------------|--------|--------|-------------|-------------|
| 1 | 118.2 | deviation | src/shared/streamable_http.rs |  | 118.2-03 rewrote two collected_body_cap unit tests that measured a whole-body cap the POST SSE path no longer has (parser bound + receive() refusal is the new contract) | open |  | 2026-08-17T08:07:00.553Z |  |
| 2 | 118.2 | deviation | src/shared/streamable_http.rs |  | decode_sse_chunks_for_fuzz carries an annotated #[allow(clippy::type_complexity)] on its four-tuple return; a pub type alias was rejected to keep this plan's public-API addition at exactly one item | open |  | 2026-08-17T09:51:32.637Z |  |
| 3 | 118.2 | deviation | src/server/streamable_http_server.rs |  | A malformed v1 logging/setLevel is rejected 400/-32601 by the pre-existing typed parse rather than answered {} as plan 118.2-07 predicted; the v2 _meta half of the claim holds | open |  | 2026-08-17T10:29:37.425Z |  |
| 4 | 118.2 | deviation | src/types/notifications.rs | 161 | LogMessageParams diverges from the 2026-07-28 schema: pmcp emits a required message and no data, the schema requires data and defines no message. 118.2-08 VERDICT: declared not fixed (breaking public-type change; the pinned suite validates no emitted notifications/message params). Mechanized by the_vendored_schema_requires_data_where_pmcp_emits_message. | fixed |  | 2026-08-17T11:17:30.173Z | 2026-08-17T13:35:18.978Z |
| 5 | 118.2 | unmet-truth | src/server/streamable_http_server.rs | 1677 | SEP-2575: on v2 a request with no _meta logLevel still gets notifications/message, because resolve_request_log_level returns None and DEFAULT_LOG_LEVEL (info) applies. Measured 118.2-09 RED mutation 2. Fixture guards; SDK does not. | open |  | 2026-08-17T11:41:50.564Z |  |
| 6 | 118.2 | unmet-truth | crates/pmcp-team-servers/tests/era_matrix.rs | 776 | deprecated_capabilities_complete_under_both_eras still asserts no-live-stream, not completed: phase 118.2 fixed DELIVERY of the server-to-client request but a pmcp client cannot ANSWER one issued during its own call (parked inside transport.send while the server holds the tools/call POST). Detail moved from 'Dispatch oneshot channel closed' (0.18s) to 'Server request dispatch-1 timed out' (~30s). | open |  | 2026-08-17T12:11:22.433Z |  |
| 7 | 118.2 | deviation | .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md |  | 118.2-10 deferred the Client-level notification observation API: Client::notification_tx is None at src/client/mod.rs:406/:453/:496, ClientBuilder has no setter, the forwarding branch at :3764 is dead. A pmcp::Client consumer still cannot see notifications/message without dropping to Transport::receive(). | open |  | 2026-08-17T12:11:22.529Z |  |
| 8 | 118.2 | unmet-truth | src/types/notifications.rs | 168 | LogMessageParams emits required 'message' and omits optional 'data'; the spec requires 'data' and has no 'message'. Measured 118.2-11: official suite 2025-11-25:tools-call-with-logging went 1/1 -> 0/2 (WireSchemaValid rejects all 3 frames; the reference zod client drops them so logCount=0). GAP_ATTRIBUTABLE_FAILURES 1 -> 2. Refutes 118.2-08's 'no scenario validates emitted params'. | fixed |  | 2026-08-17T12:28:31.523Z | 2026-08-17T13:44:06.553Z |
| 9 | 118.2 | unmet-truth | src/client/mod.rs |  | MEASURED FLAKE: official suite 2025-11-25:tools-call-elicitation failed 1 of 9 fresh runs (118.2-11 re-measurement, held pin 0.2.0-alpha.11) with 'MCP error -32603: Dispatch oneshot channel closed' -- the same server-to-client request-lifecycle race as entry 6, here against the reference client. Already gate-fatal via BLOCKING_GREEN_SCENARIOS at pre-hardening settings, so the D-16 widening adds no new exposure. Nothing softened to accommodate it. | open |  | 2026-08-17T13:44:13.623Z |  |
| 10 | 118.2 | deviation | .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-15-PLAN.md | 391 | pmat verify line uses .violations[]/.path, which does not exist in pmat 3.15.0 (correct: .summary.violations[]/.file with a ./ prefix); the command errors instead of measuring | open |  | 2026-08-17T20:14:57.119Z |  |
| 11 | 118.2 | deviation | .planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-17-PLAN.md | 341 | same broken pmat jq path as 118.2-15-PLAN.md:391 | open |  | 2026-08-17T20:14:57.208Z |  |
| 12 | 118.2 | deviation | src/shared/streamable_http.rs |  | [CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-15, CR-02 half 1): Transport::receive()'s terminal reason is now STICKY. OLD BEHAVIOUR: a terminal stream reason was pushed once onto the response mpsc, so it reached exactly ONE caller and every later caller blocked forever. NEW BEHAVIOUR: the reason is latched write-once on the transport and returned by EVERY subsequent receive() call, immediately. CONSUMER CONSEQUENCE: a consumer that loops on receive() and merely LOGS errors will now SPIN rather than hang. SEVERITY CORRECTED IN PLACE 2026-08-17 BY 118.2-21 - the framing this replaces ('a behaviour change with a documented mitigation') was INCOMPLETE IN THE DIRECTION THAT MATTERS, and 118.2-VERIFICATION.md recorded the severity direction as having WORSENED on this axis: the latch is Arc-shared across every clone, write-once, and as 118.2-15 shipped it had NO RESET SEAM in any constructor, in start_sse or in close(). Its blast radius was therefore PERMANENT, not one-off. Pre-closure a bad terminal reason could poison AT MOST ONE call (a single FIFO entry); post-closure the FIRST trip failed EVERY LATER tools/call against an SSE-answering server FOR THE LIFE OF THE PROCESS. And on the POST-answered-with-text/event-stream path it PRE-EMPTED A CALLER WHOSE OWN ANSWER WAS STILL IN FLIGHT, because drain_or_latch surfaced the latch as soon as try_recv() reported Empty while post_body returns Ok(()) before the SSE-delivered answer lands on the queue - so the queue was legitimately and transiently empty when the latch won. Reachability is ORDINARY, not adversarial: a spent MAX_SSE_RECONNECT_ATTEMPTS=2 budget, a 405 on reconnect, or one earlier truncated streaming-POST response. RESOLUTION: 118.2-19 scoped the terminal reason to the STREAM that raised it (StreamKind stamped on every TerminalReason), gated the latch on live POST-response readers (open_post_readers plus the RAII PostReaderGuard, acquired synchronously before tokio::spawn, with a wake on the last reader out), and added a RESET SEAM on a successful start_sse re-open. Fenced by a_latched_session_stream_does_not_pre_empt_an_sse_answered_call and a_reopened_session_stream_clears_the_terminal_latch, binary(client_sse_stream) 22 run / 22 passed. STICKY IS THEREFORE NO LONGER PERMANENT: a recovered transport is usable again, and a peer can now cause more than one latched reason over a transport's life where before it could cause exactly one (T-118.2-19-05, bounded by MIN_SSE_RECONNECT_DELAY and the reconnect budget). Full record as entry 17 of this ledger. Everything below this sentence stands as originally written and remains true. Mitigation already in the tree: Transport::receive's rustdoc carries the heading 'The terminal reason is STICKY - stop on it, do not loop', stating that the contract is to STOP on a terminal error. Sticky was chosen over one-shot deliberately - a one-shot reason restores the exact CR-02 hazard the latch exists to remove, where the error is consumed by whichever caller happened to be next and every caller after that gets an unexplained hang. T-118.2-15-04. Invisible to cargo semver-checks: the latch is a private field, so 223/223 checks pass and this disclosure is the only place a consumer learns of it. | open |  | 2026-08-17T22:19:42.169Z |  |
| 13 | 118.2 | deviation | src/client/mod.rs |  | [CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE (118.2-15, CR-02 half 2): Client::dispatch_request now compares response.id against the request_id it is awaiting and keeps looping on a mismatch; the mismatched frame is DISCARDED, because a Transport consumer holds no producer handle to re-queue it and an orphan holding pen would add its own unbounded-growth question. CONSUMER CONSEQUENCE, RESTATED 2026-08-17 BY 118.2-21 AT THE SEVERITY THE CLOSURE REVIEW ACTUALLY FOUND - the text this replaces described an ACCEPTED, BOUNDED cost and was WRONG IN THE DIRECTION THAT MATTERS, and it was rewritten in place rather than annotated beneath because a false claim of record cannot be left standing beside its own correction: as 118.2-15 shipped it the discard wait was BOUNDED BY NOTHING AT ALL. dispatch_request applies no tokio::time::timeout anywhere and never reads the public RequestOptions::timeout field, so the ceiling the superseded text named DOES NOT EXIST in pmcp::Client; 118.2-VERIFICATION.md classified that claim as a documentation-of-record BLOCKER in its own right. And the cost was never confined to the mismatched caller: the discard held the transport WRITE lock across that unbounded receive().await and re-took it on every continue, so ONE mis-addressed frame WEDGED EVERY OPERATION ON THE CLIENT - send, call_tool and close alike - with no error, no log after the first warn!, and no recovery. Measured on the RED tree: the second operation's notifications/cancelled NEVER REACHED THE WIRE inside LOCK_PROBE_BOUND (5 s), and the awaiting call was still parked at 20 s. It is still strictly better than one caller silently receiving another caller's tool result (a cross-request data leak inside one process), and it remains a real cost accepted as T-118.2-15-03. RESOLUTION AS OF 118.2-20, NOW ITSELF SUPERSEDED - see the dated clause at the end of this entry: 118.2-20 bounded the wait with MISMATCH_DISCARD_TIMEOUT (10 s, armed on the FIRST mismatch only), SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; capped the discards with MAX_ID_MISMATCH_DISCARDS (32, failing loudly and naming the count and both typed ids), SUPERSEDED BY MAX_UNMATCHED_RESPONSES; and released the transport write guard on a MISMATCH_RECEIVE_SLICE (250 ms), SUPERSEDED BY PUMP_RECEIVE_SLICE, so another operation waits at most one slice. Fenced at the time by a_mismatched_frame_does_not_block_another_operation_on_the_same_client and a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever, SUPERSEDED BY the count-and-clock bounds now exercised inside binary(client_sse_stream); binary(client_sse_stream) 24 run / 24 passed AT THAT TIME. Full record as entry 18 of this ledger. A LENIENT SERVER that re-types the id - a JSON string where the client sent a number - now gets a wait too, because RequestId equality is TYPED and structural and JSON-RPC 2.0 requires the response id to be the same VALUE as the request's. FOLLOW-UP that removes the cost: per-id response ROUTING (active_requests carrying a response channel per id), recorded in this phase's deferred-items.md under '## DEFERRED (118.2-15): per-id response ROUTING, and the cost of discard-on-mismatch' - IT SHIPPED, see the next clause. BEHAVIOUR SUPERSEDED 2026-08-19, RECORDED BY 118.2-24: EVERYTHING ABOVE IS HISTORY AND IS NOT HOW THE CLIENT BEHAVES AT HEAD. The THIRD gap-closure round (entry 24 of this ledger, commits e104dea6 / d01b87e2 / 2d385d60 / 26447f94) rewrote Client::dispatch_request into a per-id ROUTER, and 118.2-22 (entry 25) then split the classification of the frames the router does not own. AT THIS HEAD: a response frame whose id belongs to a LIVE caller is ROUTED to that caller, not discarded, so no live caller loses its answer and no live caller pays for another's frame; only a frame NOBODY awaits is dropped; and of those, one that is the LATE ANSWER of a call this client already stopped waiting for is ABSORBED without charging anyone, while a frame addressed to an id this client never minted still spends budget and still fails the awaiting call. The ceiling was NOT removed - it exists under UNMATCHED_RESPONSE_TIMEOUT (10 s) and MAX_UNMATCHED_RESPONSES (32); see entry 20's own in-place correction, which repairs the opposite error. LOCATE BY SYMBOL, not by line, and only by these: Client::pump_once, PumpStep, Pending, UnmatchedBudget, AbandonedRequestIds and Client::absorb_abandoned in src/client/mod.rs. SUPERSEDED SUMMARIES, NAMED RATHER THAN REWRITTEN (none of them was edited, and none may be): 118.2-15-SUMMARY.md and 118.2-20-SUMMARY.md describe the discard-and-loop mechanism this clause supersedes; 118.2-22-SUMMARY.md and 118.2-23-SUMMARY.md describe the client that ships. NOT the client lifecycle deadlock: entry 6 is a DIFFERENT defect (dispatch_request awaits transport.send before entering its receive loop while the server holds the POST open) and this edit deliberately did not touch that send-then-receive ordering - entry 6 stands unchanged and remains open. | open |  | 2026-08-17T22:19:53.731Z |  |
| 14 | 118.2 | unmet-truth | src/shared/streamable_http.rs |  | The gap-closure round (118.2-14..18) fixed CR-01, CR-02, WR-01 and WR-02 and DECLINES four review Warnings; disclosed here with their consequences so a reader scanning this ledger does not have to infer them from a plan summary. WR-03: start_sse's TOCTOU on abort_handle can leave a second, untracked GET session reader, so a client sees each server-initiated message TWICE - narrowed by 118.2-17, since the two readers can no longer poison each other's reconnect cursor (each owns a local) and both are now stoppable, so it is duplicate delivery only. WR-04: a reconnect GET gets no 401 refresh, so when a bearer token expires and the gateway drops the idle session stream, the reissued GET is answered 401, the reconnect loop treats it as terminal, and the session stream is lost PERMANENTLY - sampling/roots/elicitation dead with no recovery short of rebuilding the client - while POSTs on the same transport keep working. WR-05: an ordinary EOF on the session stream escalates to an application-visible Err, contradicting the receive() taxonomy row that promises the reader 'exits silently'; 118.2-15 rewrote how a terminal reason is DELIVERED while leaving that row exactly as it stood, so the divergence is untouched rather than half-fixed. WR-06: logging/setLevel reports success while silently discarding the level on stateless v1 deployments and inside a JSON-RPC batch, so a client that asked for 'error' keeps receiving 'info' chatter it explicitly declined; explicitly OUT OF SCOPE for this closure because fixing it reopens CONF-10 territory that plans 118.2-07, 118.2-08 and 118.2-13 argued to a booked conclusion on locked decisions D-10..D-13. FULL RECORD, with each finding's 118.2-REVIEW.md anchor, source span, consequence and reason (and IN-01..IN-06 besides): this phase's deferred-items.md, section '## GAP-CLOSURE ROUND (2026-08-17)'. The recorded source spans predate this closure's edits to the same file - locate by symbol, not by line. | open |  | 2026-08-17T22:20:07.236Z |  |
| 15 | 118.2 | deviation | src/shared/streamable_http.rs |  | CR-01 (Critical; raised by 118.2-REVIEW.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): a peer-supplied SSE 'retry: 0' was bounded only from ABOVE and the reconnect budget was refunded by any single delivered frame (if delivered { attempt = 0; }), so one frame per body drove pmcp's OWN client into an unbounded zero-delay reconnect loop - a remote-triggerable client-side DoS that also fetched a fresh auth_provider access token per iteration. FIXED by 118.2-14 with a two-sided delay bound (MIN_SSE_RECONNECT_DELAY, 500 ms) plus an uptime-gated budget (budget_reset_earned / RECONNECT_BUDGET_RESET_UPTIME, 30 s); both bounds are separately load-bearing. Fence: reconnect_with_one_delivered_frame_and_zero_retry_stays_bounded in binary(client_sse_stream), 20 run / 20 passed (target/118.2-17-green.log). RED at 65 'GET / HTTP/1.1' lines in 7.9 s against a 3-GET budget (target/118.2-14-red.log) - a measurement that cannot be retaken. | fixed |  | 2026-08-17T22:20:25.030Z | 2026-08-17T22:20:39.888Z |
| 16 | 118.2 | deviation | src/shared/streamable_http.rs |  | CR-02 (Critical; raised by 118.2-REVIEW.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): every terminal stream reason rode the SAME mpsc<Result<TransportMessage>> the responses ride, so a reason raised while the application was idle failed the next, unrelated request; and src/client/mod.rs's dispatch_request returned on the first Response frame it popped with NO comparison of response.id, so one out-of-band queue entry desynchronised the FIFO permanently and call n+1 silently received call n's result. FIXED by 118.2-15, both halves: a write-once terminal-reason latch consulted only after the queue is drained, plus response-id correlation that keeps looping on a mismatch. Fences: an_idle_terminal_error_does_not_fail_the_next_unrelated_call and a_response_whose_id_does_not_match_is_not_returned_as_this_calls_answer in binary(client_sse_stream), 20 run / 20 passed. RED at a tools/call that SUCCEEDED on the wire yet reported the stale session-stream reconnect error, and at call 2 receiving the marker 'call-answer-1' (target/118.2-15-red.log); the latch alone is a HALF fix, measured at 17 run / 16 passed / 1 failed (target/118.2-15-latch-only.log). The two consumer-observable COSTS of this fix are disclosed as separate OPEN entries in this ledger: the sticky receive() reason, and discard-on-mismatch. CROSS-REFERENCE ADDED 2026-08-17 BY 118.2-21 - THIS 'fixed' STATUS IS ACCURATE ABOUT THE ORIGINAL CR-02 DEFECT AND IS NOT THE WHOLE STORY, AND IT IS NOT FLIPPED: CR-02's OWN CLOSURE CODE INTRODUCED TWO CRITICAL DEFECTS ON THE SAME CLIENT PATH, each independently confirmed against the merged source by 118.2-VERIFICATION.md and each raised by 118.2-REVIEW-closure.md (whose WR/IN numbering restarts over DIFFERENT findings from 118.2-REVIEW.md's - see entry 21). BLOCKER 1: the sticky latch pre-empted an in-flight, SSE-answered POST response, PERMANENTLY, because it had no reset seam - recorded as entry 17 and as the corrected entry 12. BLOCKER 2: the id-mismatch discard held the transport write lock across an UNBOUNDED receive().await, wedging the whole Client, and the ceiling it was booked against did not exist - recorded as entry 18 and as the corrected entry 13. BOTH ARE CLOSED, by plans 118.2-19 and 118.2-20 respectively, with fences and parsed counts on those entries. A reader must not take this row's 'fixed' as meaning the client path was safe after 118.2-15; it was not, and the second gap-closure round is what made it so. | fixed |  | 2026-08-17T22:20:25.106Z | 2026-08-17T22:20:39.960Z |
| 17 | 118.2 | deviation | src/shared/streamable_http.rs |  | BLOCKER 1 (Critical; raised as CR-01 in 118.2-REVIEW-closure.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): the write-once terminal-reason latch that 118.2-15 introduced to close CR-02 was Arc-shared across every clone, had NO reset seam in any constructor, in start_sse or in close(), and drain_or_latch surfaced it as soon as try_recv() reported Empty. On the POST-answered-with-text/event-stream path this phase's own D-01 work added, post_body spawns a detached SSE reader and returns Ok(()) BEFORE the answer lands on the queue, so the queue is legitimately and transiently empty while a real answer is on the wire - and the latch won instantly with a stale, unrelated reason. REACHABILITY IS ORDINARY, NOT ADVERSARIAL: a spent MAX_SSE_RECONNECT_ATTEMPTS=2 budget, a 405 on reconnect, or one earlier truncated streaming-POST response. PERMANENCE: because the latch never reset, the FIRST trip failed every later tools/call against an SSE-answering server for the LIFE OF THE PROCESS - a severity direction strictly worse than the one-call poisoning it replaced. Fence 16 could not catch it: its harness answers content-type application/json, so the response lands on the queue synchronously and the queue-wins-over-latch rule never gets exercised against the SSE-answered shape. FIXED by 118.2-19: StreamKind stamped on every TerminalReason and rendered by to_error, an in-flight POST-reader gate (open_post_readers plus the RAII PostReaderGuard, acquired synchronously BEFORE tokio::spawn), a wake on the last reader out, and a reset seam on a successful start_sse re-open. Fences: a_latched_session_stream_does_not_pre_empt_an_sse_answered_call and a_reopened_session_stream_clears_the_terminal_latch in binary(client_sse_stream), 22 run / 22 passed / 0 skipped (target/118.2-19-green.log), plus binary(pmcp) and test(latch_gate) 9 run / 9 passed (target/118.2-19-latchgate.log). RED at target/118.2-19-red.log - 22 run / 20 passed / 2 failed, exactly the two new fences; call 1 reported the GET session stream's reconnect-budget error although it had SUCCEEDED on the wire, and fence 22 measured receive() answering the latch 6.625 microseconds after a SUCCESSFUL re-open, which is what proves the failure was permanent rather than delayed. Neither measurement is retakeable. Invisible to cargo semver-checks: every symbol is private. | fixed |  | 2026-08-18T03:55:33.713Z | 2026-08-18T03:56:37.143Z |
| 18 | 118.2 | deviation | src/client/mod.rs |  | BLOCKER 2 (Critical; raised as CR-02 in 118.2-REVIEW-closure.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): the id-mismatch discard loop that 118.2-15 introduced to close CR-02 was defective in BOTH halves. HALF A, THE LOCK: 'let response_message = self.transport.write().await.receive().await?;' held the RwLockWriteGuard TEMPORARY across the await because the guard lives to the end of the let statement, and the discard loop continue-d on a mismatch, re-taking and re-holding it - so ONE mis-addressed frame blocked every other send / call_tool / close on that Client, not only the mismatched caller. HALF B, THE NON-EXISTENT CEILING: the wait was bounded by NOTHING. dispatch_request applies no tokio::time::timeout anywhere and never reads the public RequestOptions::timeout field, so the comment and the deferred-items entry that booked the cost as bounded by that request's own timeout named a ceiling that does not exist in pmcp::Client. One frame with a re-typed id (Number(1) where the client sent String("1")), from a hostile peer or a merely lenient one, wedged the client with no error, no further log after the first warn!, and no recovery. FIXED by 118.2-20: MISMATCH_DISCARD_TIMEOUT (10 s, armed on the FIRST mismatch only so a dripping peer cannot extend the ceiling), MAX_ID_MISMATCH_DISCARDS (32, failing loudly with a TransportError::InvalidMessage naming the count and both typed ids), and Client::receive_bounded taking the guard in an INNER SCOPE under a MISMATCH_RECEIVE_SLICE (250 ms) so the lock is released and re-taken each slice. Fences: a_mismatched_frame_does_not_block_another_operation_on_the_same_client and a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever in binary(client_sse_stream), 24 run / 24 passed / 0 skipped (target/118.2-20-green.log), plus binary(pmcp) and test(mismatch_budget) 6 run / 6 passed (target/118.2-20-unit.log). RED at target/118.2-20-red.log - 24 run / 22 passed / 2 failed; fence 23's lock probe NEVER RETURNED inside LOCK_PROBE_BOUND (5 s) and post_bodies() at the elapse held initialize, notifications/initialized and tools/call but NO notifications/cancelled at all - that absence IS the measurement, because cancel_request never left the process, parked on the same transport.write() the discard loop held; fence 24's call was STILL PARKED at 20 s (MISMATCH_TIMEOUT_BOUND), neither Ok nor Err. Neither measurement is retakeable. Invisible to cargo semver-checks: all three constants are private. HISTORY, NOT CURRENT GUIDANCE - noted in place 2026-08-19 by 118.2-24: the mechanism this entry names was DELETED on 2026-08-18 by the THIRD gap-closure round (entry 24 of this ledger). MISMATCH_DISCARD_TIMEOUT, SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; MAX_ID_MISMATCH_DISCARDS, SUPERSEDED BY MAX_UNMATCHED_RESPONSES; MISMATCH_RECEIVE_SLICE, SUPERSEDED BY PUMP_RECEIVE_SLICE; Client::receive_bounded, SUPERSEDED BY Client::pump_once; and the fence a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever, SUPERSEDED BY the count-and-clock bounds now exercised inside binary(client_sse_stream). This entry stays as the record of what BLOCKER 2 was and how it was closed AT THE TIME; do not locate anything in the tree by the symbols above. | fixed |  | 2026-08-18T03:55:49.069Z | 2026-08-18T03:56:37.213Z |
| 19 | 118.2 | deviation | src/shared/streamable_http.rs |  | [CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design and OPEN as a disclosure (118.2-19, closing BLOCKER 1): THREE consumer-observable changes ship with NO semver signal, because every symbol involved is private and cargo semver-checks therefore reports no semver update required - this entry is the only place a consumer learns of them. (1) Transport::receive() no longer answers IMMEDIATELY with a latched terminal reason while a streaming POST response is in flight; it WAITS while open_post_readers is non-zero. A consumer that relied on the immediate (stale) answer now sees a wait instead. This is a strict improvement - the stale answer was BLOCKER 1 - but it is a timing change a downstream consumer can observe. (2) A successful start_sse re-open now CLEARS a latch that was previously permanent, so a peer can cause MORE THAN ONE latched reason over a transport's life where before it could cause exactly one; MIN_SSE_RECONNECT_DELAY and the reconnect budget bound how fast that cycle can run, and one diagnosis per genuine recovery cycle is the correct amount (T-118.2-19-05). Entry 12's 'sticky' wording remains accurate; sticky is no longer PERMANENT. (3) The rendered TEXT of every terminal reason now gains a stream-name prefix - 'the GET session stream ended: ...' or 'this call's own POST response stream ended: ...'. Every existing message BODY is preserved verbatim (pinned by to_error_names_the_stream_and_preserves_the_message_body), so a consumer matching on a substring of the body still matches, but a consumer matching on the WHOLE string, or logging it, sees different text. RESIDUAL under peer control, named rather than buried: a peer that holds a POST response body open forever keeps the gate closed for that transport - this is D-01's existing shape, not new, and close() and last-clone-drop both stop such a reader (fences 18 and 19 pass unchanged), with the reader's exit now waking any parked consumer. | open |  | 2026-08-18T03:56:13.311Z |  |
| 20 | 118.2 | deviation | src/client/mod.rs |  | [CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design and OPEN as a disclosure (118.2-20, closing BLOCKER 2): TWO consumer-observable changes, both with NO semver signal because all three constants are private. (1) A dispatch_request that has seen a mis-addressed frame now FAILS with a timeout after MISMATCH_DISCARD_TIMEOUT (10 s), or with a named TransportError::InvalidMessage after MAX_ID_MISMATCH_DISCARDS (32), where it previously waited FOREVER. THE BLAST RADIUS IS WIDER THAN 'wedged becomes bounded' AND MUST NOT BE SOFTENED TO THAT: the deadline is armed on the FIRST mismatch and STAYS ARMED FOR THE LIFE OF THAT CALL, so a peer that emits ONE stray frame at t=0 and then answers CORRECTLY at t=15 s previously SUCCEEDED and now FAILS at 10 s. That is a NEW FAILURE MODE for a previously-working slow-but-correct peer, not merely a bound on a hang. It is the mechanism the design requires - arming on the first mismatch is what makes the ceiling un-extendable by a peer emitting a steady drip of wrong ids, and the re-arming alternative is strictly worse - but a downstream consumer with a slow and slightly lenient peer WILL observe it. CORRECTED IN PLACE 2026-08-19 BY 118.2-24, in place rather than appended beneath, because a false claim of record cannot be left standing beside its own correction - the house rule 118.2-21 set for exactly this file. THE SENTENCE THAT STOOD HERE promised that per-id response routing removes discards entirely and therefore removes this ceiling too. THAT IS FALSE AT HEAD, and 118.2-VERIFICATION.md classified it as a documentation-of-record BLOCKER in its own right. The THIRD gap-closure round (entry 24 of this ledger) removed the discarding of frames that DO match a LIVE caller's id; a frame matching NO id is still dropped, and the ceiling came back under two RENAMED PRIVATE CONSTANTS. WHAT ACTUALLY BOUNDS THE CLIENT AT THIS HEAD, stated here so a reader needs no other entry: UNMATCHED_RESPONSE_TIMEOUT (10 s) and MAX_UNMATCHED_RESPONSES (32) in src/client/mod.rs, either bound alone sufficient, armed by UnmatchedBudget::record on the FIRST frame nobody awaits and NEVER re-armed. It fires ONLY once a frame addressed to an id THIS CLIENT NEVER MINTED has actually been seen - a re-typed id, or a peer spraying ids nobody here ever asked for - so the slow-and-lenient-peer blast radius stated above is UNCHANGED and still real; and a peer that has simply gone QUIET is bounded by nothing at all, still deliberately, because pmcp has no default request timeout and a blanket deadline would break long-running tools/call handlers. THE SELF-SUSTAINING CHAIN that arming-on-the-first-frame created - a call that already died leaving its own real answer un-owned for the NEXT call to book as the peer's misbehaviour and arm ITS deadline on frame one, failing every later call on that Client permanently - was closed by 118.2-22 (entry 25) with AbandonedRequestIds and Client::absorb_abandoned, which classify our own debris apart from the peer's misbehaviour and absorb it exactly once. The two constants item (1) names above are HISTORY, not current guidance: MISMATCH_DISCARD_TIMEOUT, SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; MAX_ID_MISMATCH_DISCARDS, SUPERSEDED BY MAX_UNMATCHED_RESPONSES. (2) The transport write lock is now RELEASED and RE-TAKEN on a MISMATCH_RECEIVE_SLICE (250 ms, SUPERSEDED BY PUMP_RECEIVE_SLICE) during a discard, so another operation on the same Client waits at most one slice instead of indefinitely; tokio's RwLock is fair (FIFO semaphore), so a waiter queued while the guard was held gets it before the discard loop re-takes it. CORRECTED IN PLACE 2026-08-19 BY 118.2-24: item (2)'s claim that another operation waits at most one slice instead of indefinitely was TRUE OF THE RECEIVE PATH AND FALSE OF THE SEND PATH, and it stayed half wrong for TWO ROUNDS rather than being caught. Client::dispatch_request took the SAME transport write guard and awaited the SEND inside it; that send reaches StreamableHttpTransport::post_once, which awaits the peer's response HEADERS, and no request timeout exists on any path in src/shared/streamable_http.rs - so a peer that accepted the POST and never wrote its response head held the one guard indefinitely and every other operation on that Client, close() included, blocked at acquisition. Neither code review saw it and neither earlier verification did either; it was found by 118.2-VERIFICATION.md's THIRD round and CLOSED by 118.2-23 (entry 27), which routes every client-side send through Client::send_frame over an OWNED handle taken under a momentary guard that is dropped BEFORE the round trip is awaited. The claim is true on BOTH paths only from 118.2-23 onward. TWO EXCEPTIONS a reader must not infer away, both named in entry 27: a POOLED StreamableHttpTransport keeps the exclusive path, and Client::open_event_stream still holds a read guard across the subscriptions/listen POST's response head. REJECTED ALTERNATIVE, recorded because it is the obvious one: plumbing the PUBLIC RequestOptions::timeout in as the ceiling was rejected because activating a public field that has NEVER been read silently changes behaviour for every existing caller who already sets it, with no version signal and no way to walk it back except another behaviour change. A private ceiling is the smaller and reversible choice. NOTE FOR A FUTURE READER: 118.2-20 made grep -c 'self.transport.write().await.receive()' src/client/mod.rs return 0 for code, but a guard IS still held across receive() on the ORDINARY (no-mismatch) path - bound to a NAMED LOCAL rather than left an invisible temporary. That non-change is deliberate and is stated at the branch; do not conclude from that grep alone that no guard is ever held across a receive. | open |  | 2026-08-18T03:56:13.382Z |  |
| 21 | 118.2 | unmet-truth | src/shared/streamable_http.rs |  | The SECOND gap-closure round (plans 118.2-19..21) closed BLOCKER 1 and BLOCKER 2 (entries 17 and 18) and DECLINES four Warnings and four Info items from 118.2-REVIEW-closure.md; disclosed here with consequences and named owners so a reader scanning this ledger does not have to infer them from a plan summary. READ THIS FIRST - AN ID COLLISION: 118.2-REVIEW.md (the FIRST review) numbered its findings WR-01..WR-06 and IN-01..IN-06, and 118.2-REVIEW-closure.md (the SECOND review, of the closure itself) RESTARTED at WR-01..WR-05 and IN-01..IN-04 over ENTIRELY DIFFERENT findings. Entry 14 of this ledger indexes the FIRST set; this entry indexes the SECOND. Every id below is prefixed with its file; a bare id will lead a later auditor to the wrong entry. CLOSURE-REVIEW WR-01: deliver_sse_event's delivery.sender.send(Ok(message)).await is a BARE await not raced against shutdown (only the body read is), so after close() a peer that keeps writing fills the 64-slot queue and the detached POST reader parks in send() forever - close() does not drop the Receiver and its abort reaches only the GET reader, so the TASK AND ITS TCP SOCKET LEAK. The first round's WR-01 fix moved the leak ONE AWAIT DOWN rather than eliminating it. Fence 19 misses it because its POST stream is idle. THIS RESIDUAL WAS RECORDED BY THE PREVIOUS ROUND AND NEVER RE-VERIFIED (118.2-VERIFICATION.md marks it PARTIAL and says so); being unnamed and unre-verified is how it survived a whole closure round, which is why it is named here with an owner. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section. CLOSURE-REVIEW WR-02: the biased; in read_next_sse_frame polls the body first and returns on the first Ready, so both shutdown arms are never polled while a firehose peer keeps the body ready - close() is not observed for as long as the peer keeps writing. The bias is deliberate and correct for the frame arm (D-04, never drop a burst-then-close server's last frames); a fairness escape is a tuning decision on the same loop WR-01's fix touches. OWNER: same. CLOSURE-REVIEW WR-03: the shutdown flag is never reset, so after any close() a later start_sse returns Ok(()) over a reader that exits immediately, every streaming-POST reader dies the same way and its dispatch_request hangs, is_connected() still answers true, and because shutdown is Arc-shared and the transport is Clone, one component closing ITS clone permanently disables SSE on EVERY clone. NEAR MISS worth recording: 118.2-19 added a reset seam for the TERMINAL LATCH and deliberately did NOT add one for the shutdown flag, because the review offers two CONTRADICTORY contracts (reset the flag and support close-then-restart, or make close observably terminal with is_connected() reporting it) and picking one inside a Blocker closure would settle a public contract by accident - the same reasoning 118.2-16 used to defer the first review's WR-05. OWNER: same plan, which should decide BOTH reset seams together. CLOSURE-REVIEW WR-04: the test mocks re-address EVERY response once any request has been recorded, so a fixture deliberately carrying a foreign id is silently rewritten and a NEGATIVE TEST PROVES NOTHING WHILE STILL PASSING - a false green in exactly the surface BLOCKER 2 lived in. OWNER: whichever plan next touches tests/common/. CLOSURE-REVIEW IN-01..IN-04: a stale 'already sent' comment that describes the behaviour CR-02 removed, a dead state.done write, the reconnect floor applied per-branch rather than at the exit, and a write-only cursor local kept deliberately. Hygiene, no correctness consequence, no ordering constraint. OWNER: ride along with whichever plan next touches the file. NOT DEFERRED: closure-review WR-05 (fence 17 passing on ANY error, including ones that never reach the correlation check) is CLOSED - 118.2-20 tightened that arm. UNCHANGED: the FIRST review's WR-03..WR-06 and IN-01..IN-06 remain open exactly as entry 14 and deferred-items.md's first appendix record them; this round changed no status among them. FULL RECORD with anchors, consequences, reasons and owners: this phase's deferred-items.md, section '## SECOND GAP-CLOSURE ROUND (plans 118.2-19..21)'. Locate source by SYMBOL, not by line - this round edited both files. | open |  | 2026-08-18T03:56:37.073Z |  |
| 22 | 118.2 | deviation | Makefile | 247 | FALSE GREEN, measured by 118.2-21's closing gate run and NOT this round's defect: 'make validate-always' step 1 (FUZZ) reports success while fuzzing NOTHING. test-fuzz at Makefile:247 runs 'timeout 30s $(CARGO) fuzz run $$target \|\| echo "Fuzz target $$target completed"'. $(CARGO) is STABLE cargo, and cargo-fuzz builds with -Zsanitizer=address, which stable rejects with 'error: the option Z is only accepted on the nightly compiler'. EVERY fuzz target therefore fails to BUILD, the \|\| arm prints 'Fuzz target X completed' in yellow, the loop continues, and the target exits 0 - so 'ALL ALWAYS requirements validated' and 'ALL TOYOTA WAY QUALITY CHECKS PASSED' are both printed having executed ZERO fuzz iterations. Measured verbatim at target/118.2-21-gate.log and target/118.2-21-always.log: the build error appears once per target and no target reports a run count. CONSEQUENCE: CLAUDE.md's ALWAYS-fuzz requirement cannot be discharged by the gate; a plan that cites 'make validate-always exit 0' as its fuzz evidence has cited nothing. This is the same class of defect as WINDOWS entries 10 and 11 (a verify command that exits without measuring), here in the Makefile rather than in a plan file. PROVENANCE - NOT THIS ROUND'S: git diff --name-only 45929873..HEAD -- Makefile fuzz/ returns 0 lines; neither file was touched by plans 118.2-19, -20 or -21. THE REAL FUZZ DISCHARGE for this round is plan 118.2-19's explicit nightly invocation, 'cargo +nightly fuzz run streamable_sse_frames -- -runs=20000' - 20000 runs, exit 0, fuzz/artifacts/streamable_sse_frames/ empty before and after. NOT FIXED HERE: pre-existing, off this round's path, and the fix is a build-tooling decision (pin +nightly in the recipe, or fail closed on a non-nightly toolchain instead of swallowing the error) that belongs with whoever owns the Makefile's test targets. OWNER: whichever plan next touches the Makefile's ALWAYS targets. | open |  | 2026-08-18T04:16:03.883Z |  |
| 23 | 118.2 | unmet-truth | src/shared/streamable_http.rs |  | [CONSUMER-OBSERVABLE] CR-03, OPEN AND DEFERRED BY DEVELOPER DECISION (2026-08-18), recorded at the severity re-measured against HEAD 26447f94 rather than the severity 118.2-VERIFICATION.md assigned. THE DEFECT: drain_or_latch (src/shared/streamable_http.rs, the gate reading open_post_readers.load(Ordering::SeqCst) > 0) answers a PER-CALLER question ('is MY answer still on the wire') with a TRANSPORT-WIDE count, and PostReaderGuard::drop wakes only on the 1->0 transition. With two or more concurrent SSE-answered POST responses live on one transport, a caller whose OWN stream has already latched its OWN correct terminal reason cannot see that reason while an UNRELATED reader is still open. TRIGGER, measured precisely: concurrency >= 2 SSE-answered POST responses AND a terminal failure (connection drop, oversized chunk, unparseable frame) on one of them. NOT a load-dependent defect - two concurrent streaming tool calls suffice, which is ordinary for an agent doing parallel tool invocation; the rare half is the transport failure, not the concurrency. The always-open v1 session GET stream does NOT gate: PostReaderGuard::acquire appears only in spawn_sse_reader (the POST-response path), so the single-caller case is clean. CONSEQUENCE, RE-MEASURED AND MILDER THAN THE VERIFICATION'S FRAMING: 118.2-VERIFICATION.md describes an 'unbounded, silent hang' for 'the process lifetime'. That was accurate against the code as it stood at 31a80a75, and is NO LONGER accurate after the per-id response router landed in d01b87e2. Today the failed call receives a DELAYED ERROR bounded by the other reader's lifetime - the last guard's drop fires the wake and the latch then surfaces. No answer is lost, none is mis-delivered to the wrong caller, and nothing is starved (pump_once slices the transport write lock every PUMP_RECEIVE_SLICE = 250ms rather than parking on it). Re-confirmed by direct source read that ReaderDelivery carries the SHARED sender and SHARED terminal latch, so reader ERRORS are not per-caller routed even though successful RESPONSES now are. WHY DEFERRED, developer's reasoning verbatim in intent: the architecture is moving away from SSE toward stateless calls with MCP Tasks and task-store polling for progress. This defect lives exclusively in the POST-response SSE reader path, i.e. on the v1 surface being deprecated; the v2 stateless/Tasks direction does not create concurrent POST readers at all, so the population of affected deployments shrinks rather than grows. Deferred as a Warning, NOT closed and NOT waived - no fix is scheduled and none may be implied. SIBLING DEFECT WORTH FIXING WITH IT: the unit test a_post_reader_in_flight_gates_a_reason_from_either_stream ASSERTS THE DEFECTIVE SHAPE IS CORRECT and pins the single-reader case only. A future fixer will read it as a deliberate invariant and mistake their own fix for a regression. There is NO two-concurrent-reader fence anywhere in the crate - searched at HEAD. THE DURABLE FIX (for whoever picks this up): scope the gate to the stream that owns the question - let a StreamKind::PostResponse reason surface immediately regardless of open_post_readers, reserving the transport-wide gate for StreamKind::Session reasons; the fully durable form is a reason per reader delivered to the caller that owns it, the same per-id routing d01b87e2 already applied to responses. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section - the same owner as closure-review WR-01/WR-02/WR-03 (entry 21), which touch the same loop and should be decided together. LOCATE BY SYMBOL, NOT BY LINE. | open |  | 2026-08-19T00:25:15.934Z |  |
| 24 | 118.2 | deviation | src/client/mod.rs |  | [CONSUMER-OBSERVABLE] RETROSPECTIVE ENTRY OF RECORD, written 2026-08-19 by 118.2-24 for a round that shipped with NO plan, NO summary, NO ledger entry, NO requirement amendment and NO semver verdict of its own: the THIRD gap-closure round of phase 118.2, commits e104dea6, d01b87e2 ("route JSON-RPC responses by id instead of discarding them"), 2d385d60 and 26447f94. It is the LARGEST consumer-observable change in the phase - a 378-line rewrite of Client::dispatch_request into a per-id response ROUTER - and it was found only because 118.2-VERIFICATION.md re-verified against a tree four commits newer than the one the previous verification described. THIS ENTRY IS RETROSPECTIVE: it is written after the fact from the diff and from the source at HEAD, not from a plan the round followed, and the fact that it had to be reconstructed at all is the defect it records. WHAT IT DELETED - every one of these is a symbol some earlier entry or requirement row still instructed a reader to LOCATE BY, which is why each surviving citation now carries the marker: Client::receive_bounded, SUPERSEDED BY Client::pump_once; MismatchBudget, SUPERSEDED BY UnmatchedBudget; MISMATCH_DISCARD_TIMEOUT, SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; MAX_ID_MISMATCH_DISCARDS, SUPERSEDED BY MAX_UNMATCHED_RESPONSES; MISMATCH_RECEIVE_SLICE, SUPERSEDED BY PUMP_RECEIVE_SLICE; and the fence a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever, SUPERSEDED BY the count-and-clock bounds now exercised inside binary(client_sse_stream). WHAT IT ADDED: Pending, the per-id answer channel registered in Client::active_requests BEFORE the frame goes on the wire; Client::pump_once and PumpStep; PUMP_RECEIVE_SLICE (250 ms), the slice on which the transport guard is released so a second operation can make progress; UnmatchedBudget with UNMATCHED_RESPONSE_TIMEOUT (10 s) and MAX_UNMATCHED_RESPONSES (32); echoed_request_id bounded by MAX_ECHOED_REQUEST_ID (128) so a hostile peer cannot push an unbounded id into a client log; and fences 25 and 26. CONSUMER CONSEQUENCE: a response frame whose id belongs to a DIFFERENT LIVE caller is now DELIVERED to that caller instead of being discarded, so the cross-caller answer destruction entry 13 discloses no longer happens to a live caller. Only a frame NOBODY awaits is dropped, and only such a frame spends budget. THE CEILING WAS NOT REMOVED - it was re-added under the two renamed private constants above, which is exactly the claim entry 20 got wrong and 118.2-24 corrected in place. WHAT IT LEFT BEHIND, both closed later in this phase rather than by the round itself: the self-sustaining chain, where a call that died at its ceiling left its own real answer un-owned for the NEXT call to book as the peer's misbehaviour, closed by 118.2-22 and recorded as entry 25 of this ledger; and the SEND-path wedge, where the transport write guard was held across a POST whose response head never arrives, closed by 118.2-23 and recorded as entry 27. NO SEMVER SIGNAL: every symbol above is private. Two of the four commits also touched SERVER code (src/server/core.rs, src/server/streamable_http_server.rs, tests/log_emitter.rs), which is why 118.2-24 RE-RAN the official conformance suite at this HEAD rather than scoping it out as client-only - the suite exercises pmcp as a SERVER, so it cannot attribute the client rewrite but it can see those edits. The semver verdict of record for this round is taken by 118.2-24 at this HEAD and quoted in 118.2-24-SUMMARY.md. LOCATE BY SYMBOL, not by line: Client::pump_once, PumpStep, Pending, UnmatchedBudget, PUMP_RECEIVE_SLICE, UNMATCHED_RESPONSE_TIMEOUT and MAX_UNMATCHED_RESPONSES in src/client/mod.rs. | open |  | 2026-08-20T05:16:26.155Z |  |
| 25 | 118.2 | deviation | src/client/mod.rs |  | [CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-22, closing CR-02's SURVIVING half - the self-sustaining chain the THIRD gap-closure round left behind, recorded as entry 24 of this ledger, and disclosed nowhere until now). WHAT A CONSUMER OBSERVED BEFORE: one abandoned call plus a peer whose honest round trip outlasts UNMATCHED_RESPONSE_TIMEOUT (10 s) failed EVERY SUBSEQUENT CALL on that Client, silently and for the life of the process. The mechanism was a MISCLASSIFICATION, not a bound that was too tight: Client::dispatch_request removes its registration on every Err exit and drains nothing, so a call that dies at its ceiling leaves its own real answer un-owned on the shared receive queue; the NEXT call pops that answer, finds no owner, books it as the peer's misbehaviour and arms ITS OWN ten-second deadline on frame one. AFTER: a response frame that is the late answer of a request this client already stopped waiting for is classified as our OWN debris and absorbed exactly once, charging nobody. THE BOUND THAT REMAINS, unchanged in value and still firing for the peer misbehaviour it was written for: a frame addressed to an id THIS CLIENT NEVER MINTED still spends budget and still fails the awaiting call at the same two values - MAX_UNMATCHED_RESPONSES (32) and UNMATCHED_RESPONSE_TIMEOUT (10 s) - with the same named, counted error naming both typed ids. THE BOUND THAT NEVER EXISTED, and still does not: a peer that has simply GONE QUIET is not bounded at all. pmcp has no default request timeout, deliberately, because a blanket deadline would break long-running tools/call handlers; that is a product decision recorded as such, not a review gap left open by accident. THE MEMORY THIS FIX ADDS, and why a peer cannot grow it: AbandonedRequestIds is a VecDeque<RequestId> at a fixed PRIVATE cap MAX_ABANDONED_REQUEST_IDS = 64, oldest-evicted, with each entry consumed on its first take, and it can only ever hold ids that came back from an active_requests::remove - i.e. ids this client itself minted - so a hostile peer cannot put a single entry into it at any rate. No public symbol changed, no bound was loosened, and nothing that succeeded before now fails; the victim set shrinks from "every later call on this Client" to "the call whose peer is actually misbehaving", with no new victim class created. NO SEMVER SIGNAL: AbandonedRequestIds, MAX_ABANDONED_REQUEST_IDS and Client::absorb_abandoned are all private. Fenced by debris_from_a_dead_call_does_not_charge_the_next_calls_budget - deterministic by COUNT with no wall clock in it, three sequential calls sprayed 32 / 31 / 0 un-owned frames so call two can only reach the bound by being charged for call one's leftover answer - plus five unit fences covering absorption, single-use, the never-minted case, cap eviction and stale-debris clearing on re-registration. LOCATE BY SYMBOL, not by line: AbandonedRequestIds, MAX_ABANDONED_REQUEST_IDS, Client::absorb_abandoned and the no-owner branch of Client::pump_once in src/client/mod.rs. Full record: 118.2-22-SUMMARY.md. | open |  | 2026-08-20T05:16:49.798Z |  |
| 26 | 118.2 | deviation | src/shared/streamable_http.rs |  | [CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-25). TWO transport-wide sequences on StreamableHttpTransport that were serialised only by the pmcp Client's outer transport write guard - i.e. by accident - are now serialised IN THEIR OWN RIGHT. This landed BEFORE 118.2-23 removed that guard, precisely so that no commit in this round ever carried the hazard: had the order been reversed, the window between the two plans would have shipped a transport whose 401 recovery and session-stream restart were unprotected. (1) THE 401 TOKEN RECOVERY IS NOW SINGLE-FLIGHT, and the guarded region runs from the purge THROUGH the retry request's get_access_token - purge inside, retry BUILD inside, retry SEND outside - so two concurrent 401s on one transport produce exactly ONE AuthProvider::on_unauthorized call and exactly ONE token vend. BEFORE, a consumer holding two clones of one transport (the durable-agent shape: one transport shared across concurrent tasks) had both clones call on_unauthorized and, one step later, both reach get_access_token concurrently against the cache the first had just emptied. Against a ROTATING refresh token that is two presentations of the same refresh token: the identity provider accepts one and rejects the other, and the rejected caller's purge destroys the token the winner had just cached, so the transport's auth fails PERMANENTLY until an out-of-band re-authentication. This is the recorded SDK blocker for the durable-agent deployment shape, not a thought experiment. The retry POST is deliberately NOT inside the lock, so the transport gains no whole-transport bottleneck. (2) THE SESSION-STREAM RESTART IS NOW ATOMIC across its take-abort / open / reset / respawn sequence. BEFORE, two concurrent restarts each took a None abort handle, each opened a GET, and the second's store orphaned the first's reader; because close() aborts exactly one JoinHandle, that orphan outlived the transport holding a live peer-controlled socket that nothing could reach. A caller that uses a transport one call at a time sees byte-identical behaviour in both cases: the same single 401 retry, the same single restart, the same errors. NO public signature changed, the AuthProvider trait is UNTOUCHED, no dependency was added, and no bound, deadline or timeout was introduced anywhere. NO SEMVER SIGNAL: refresh_lock, token_generation and restart_lock are private fields. Fenced by two_concurrent_401s_refresh_the_token_once and two_concurrent_session_stream_restarts_leave_one_reader, plus five unit fences; the auth fence was validated by a COUNTERFACTUAL - with only the purge single-flighted it is STILL red on the vend peak - so it demonstrably measures the vend rather than the operation adjacent to it. NAMED RESIDUAL WITH AN OWNER, disclosed here rather than left implicit, because this phase's entire failure history is undisclosed residuals: "exactly one vend" is PRECONDITIONED on the AuthProvider implementation CACHING what get_access_token vends before returning, and the trait imposes no such contract - on_unauthorized's default body is a no-op and get_access_token is unconstrained. Against a NON-CACHING provider the generation check is INERT: the loser skips the purge but vends anyway, so all the lock buys is that the two vends are SERIALISED rather than simultaneous, which a rotating refresh token still rejects. REACHABILITY, stated honestly: the shape is a durable agent holding one transport across concurrent tasks - the same shape part (1) exists for. WHY IT IS UNFENCEABLE IN-REPO rather than merely unfenced: there is NO in-tree client-side AuthProvider implementation at all. grep over src/ and tests/ finds the UNRELATED server-side trait of the same name (src/server/auth/traits.rs) and, for the client-side trait at src/shared/streamable_http.rs, only test doubles - CountingProvider, MinimalProvider, DualTokenProvider and CachingProbe under cfg(test), plus CountingProvider, TestAuthProvider, MockAuthProvider and CachingAuthProvider in tests/. Every real implementation is downstream, and no in-tree implementation demonstrates the behaviour either way. WHY THE ASSUMPTION IS NONETHELESS WELL-FOUNDED: the trait's own rustdoc tells implementors to "evict any cached access token here so that the subsequent get_access_token() call returns a freshly-vended token rather than the now-invalid cached one" - wording that only makes sense if get_access_token normally serves FROM CACHE. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section - the same owner as entry 23. THE DURABLE FIX, recorded for whoever picks it up: state a caching contract on the trait. That is a decision with an external-implementor cost, not a gap-closure edit, which is why it was not taken here. AMENDED 2026-08-20 BY CODE REVIEW OF THIS ROUND'S OWN DIFF, in place per the house rule 118.2-21 set for this file, because the mirror in pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md was updated with BOTH facts below while THIS entry - the source of record - carried neither, leaving the mirror saying more than its source. The disclosure tripwire cannot see that: it checks that a CONSUMER-OBSERVABLE entry is CITED in the chapter, not that the chapter's claims are backed by the ledger. (i) THE SINGLE-FLIGHT COVERS THE 401 RECOVERY ONLY. AuthProvider::get_access_token runs OUTSIDE refresh_lock on every request build (build_request_from_parts), so a COLD-START FAN-OUT - one cloned transport, N concurrent first requests against an empty provider cache - still reaches the provider N times concurrently. Against a rotating refresh token that is the SAME permanent auth failure this entry's single-flight exists to prevent, reached on the path every deployment hits BEFORE any 401 has ever occurred. CLOSED 2026-08-20, same day, by ColdVendGate in src/shared/streamable_http.rs: a SECOND gate rather than a wider refresh_lock, because the retry request is built INSIDE refresh_lock and a build path taking that same mutex would self-deadlock on the first 401. It single-flights the vend ONLY while the provider cache is believed cold - an AtomicBool fast path means a warm transport takes no lock per request, which is what keeps this from re-creating the whole-transport bottleneck T-118.2-25-03 prohibits - and it is RE-ARMED after on_unauthorized evicts, or it would have guarded the startup window only and let the identical fan-out recur one eviction later. Lock order is always refresh_lock -> ColdVendGate and nothing takes refresh_lock while holding the gate, so no cycle exists. The reconnect path goes through the same gate, carried on RequestParts, because a reconnect storm against a cold cache is the same fan-out. FENCED by fence 31, a_cold_start_fanout_on_one_transport_vends_once in tests/client_sse_stream.rs, which was demonstrated RED on the ungated tree - peak 4 of 4 callers inside the vend concurrently - before it went green, so it is not a fence that cannot fail. THE RESIDUAL IS UNCHANGED AND IS NOT CLOSED BY THIS GATE: against a NON-caching AuthProvider every caller vends regardless, because the gate holds no token and asking the provider again is what reads the winner's cached value. That is the same residual token_generation already carried and it still awaits the trait-level caching contract named above. (ii) THE ATOMIC RESTART IS RESTART-VS-RESTART ONLY. Transport::close() does not take restart_lock, so a start_sse holding the lock and awaiting its GET can store a reader handle AFTER close() already took - and found None in - abort_handle. That reader is stopped by run_session_stream's separate shutdown watch, NOT by close()'s abort: the invariant survives on a second mechanism, which a future reader must know before removing that watch. 118.2-23 made this pairing reachable by removing the client guard that had made send and close mutually exclusive. (iii) AMENDED 2026-08-20 by 118.2 re-verification at HEAD dc0587af, which rated this a WARNING: the residual below was disclosed ONLY in the phase-internal 118.2-SECURITY.md (accepted risk R-118.2-02), while every sibling residual in this phase - CR-03 as entry 23, and (i)/(ii) above - got a ledger entry AND a migration-chapter mirror. Stated here so a consumer meets it in the record rather than in a security file they will never read. THE RESIDUAL: start_sse takes restart_lock and HOLDS IT ACROSS open_sse_once's await of the peer's GET RESPONSE HEAD. There is no request timeout on any production path in src/shared/streamable_http.rs - the only production timeout in the file is .pool_idle_timeout(90s), and every tokio::time::timeout in it sits inside the cfg(test) mod tests. So a peer that ACCEPTS the session-stream GET and then never writes its response head holds a TRANSPORT-WIDE lock indefinitely, blocking every later session-stream restart on that transport - the resumption-cursor send path and the 202 notifications/initialized path both park at that lock with nothing to bound them. Severity MEDIUM, below the security gate's `high` block threshold, which is why it was accepted rather than fixed. NOT A CONTRADICTION of item 3 above: that item's claim is that the restart SEQUENCE is indivisible, and it is; this is the separate cost of the lock's DURATION. The durable fix is a request timeout, which entry 20 records as DELIBERATELY REJECTED in its RequestOptions::timeout form - so closing this needs the bounded-request decision reopened, not a local edit. Owner: the client-transport hardening plan named in deferred-items.md, the same owner as entry 23. LOCATE BY SYMBOL, not by line: refresh_lock, token_generation and restart_lock and their rustdoc in src/shared/streamable_http.rs. Full record: 118.2-25-SUMMARY.md. | open |  | 2026-08-20T05:17:16.650Z |  |
| 27 | 118.2 | deviation | src/client/mod.rs |  | [CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-23, closing the SEND-path wedge the THIRD gap-closure round left behind - entry 24 of this ledger - and which neither code review nor the previous verification had seen). FOUR things a consumer can observe, and all four are stated here rather than only the flattering one. (1) A STALLED PEER NO LONGER SERIALISES THE WHOLE CLIENT. Before, Client::dispatch_request took the transport WRITE guard and awaited the send inside it; that send reaches StreamableHttpTransport::post_once, which awaits the peer's response HEADERS, and no request timeout exists on any path in src/shared/streamable_http.rs. A peer that accepted the POST and never wrote its response head therefore held the one transport guard for as long as it liked, and every other operation on that Client - a second call, a notification, a cancellation, Transport::close - blocked at acquisition with nothing to bound it. Now every client-side send goes through Client::send_frame, which takes an OWNED handle under a momentary READ guard and DROPS that guard before the round trip is awaited. Measured, not argued: fence 30 turned six seconds of wedge into one second of ordinary traffic with the peer still silent. NOTE what did NOT change: the stalled call itself still waits indefinitely. No timeout, no response-header deadline and no clock of any kind was added, deliberately - a JSON-answering or Lambda-hosted server writes its response head only when the handler finishes, so any finite bound would break a legitimate long tools/call that works today. (2) TWO POSTs CAN NOW BE IN FLIGHT ON ONE TRANSPORT AT ONCE, where the pmcp client previously imposed a total order. Three things a POST touches beyond its own call were checked BY NAME - the per-caller POST-reader accounting (an atomic), the transport-wide AuthProvider (single-flighted by 118.2-25's refresh_lock from the purge through the vend, entry 26) and start_sse reached from post_body's 202 branch (made indivisible by 118.2-25's restart_lock) - and nothing beyond those three is asserted. (3) OUTBOUND FRAME ORDERING ACROSS SEPARATE POSTs IS NO LONGER IMPOSED BY THE CLIENT. HTTP never guaranteed one, and transports on the exclusive path keep their total order, but a consumer who was relying on the client's incidental ordering will not get it. (4) A POOLED TRANSPORT KEEPS THE WEDGE. PooledTransport<T> is the ONE wrapping impl Transport for ... in the tree, and it inherits the new accessor's default None BY EXPLICIT DECISION with a Why comment at the impl, not by omission: ConnectionPool::send_message chooses a connection PER SEND and a per-connection worker task owning its own clone performs the I/O, so no inner transport's handle would be correct for a FUTURE send and forwarding one would silently pin every shared send to a single connection. NAMED, OWNED RESIDUAL: a consumer who pools a StreamableHttpTransport keeps the exclusive &mut path. It is materially narrower than the wedge (1) closes - send_to_connection awaits an mpsc push, not the peer's response head - but it is not zero: with the connection's channel full (capacity 100) the push blocks until the worker drains, and the worker is itself blocked on the stalled peer, so sustained traffic against a silent peer can still serialise a client over a pooled transport. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section, the same owner as entries 23 and 26. A SECOND NAMED, OWNED RESIDUAL, found DURING 118.2-23 rather than before it, and recorded here so it is booked rather than lost: Client::open_event_stream still holds a READ guard across the subscriptions/listen POST's response HEAD. A read guard is no better than a write guard here - tokio's RwLock is fair, so a writer arriving parks and every later reader parks behind it - so a peer that accepts that POST and never answers still serialises the client, exactly as dispatch_request did. It is NOT closed because the owned-handle seam (Transport::shared_sender) does not exist on EventStreamTransport, whose open_event_stream is reached through the generic T with no way to own one; adding it is a PUBLIC-TRAIT change and belongs to a decision, not to a gap-closure round. Blast radius: callers of subscriptions/listen only; every other client-side send is off the guard. The residual is also recorded IN PLACE at the guard in src/client/mod.rs under the marker "KNOWN RESIDUAL (Phase 118.2, plan 23)". OWNER: as above. THE PUBLIC-API CHANGE, which unlike everything else in this ledger DOES have a semver surface: a new public trait SharedSender and a new method Transport::shared_sender(&self) -> Option<Arc<dyn SharedSender>> DEFAULTED to None on BOTH the native and the wasm32 trait definitions. Every external implementor compiles unchanged AND behaves unchanged, and every transport in this tree except StreamableHttpTransport keeps the default, so transports without a shared-send path are byte-for-byte unchanged on the wire. cargo semver-checks reports no update required - with the caveat this phase has now recorded five times, that it is a breaking-change LINTER and not an API-diff inventory, so for an ADDITION "nothing fired" is the whole of the claim; the additive shape is evidenced separately by a_transport_that_does_not_opt_in_answers_none, which implements only the pre-118.2-23 trait surface and still compiles and still answers None. Fenced by a_peer_that_never_writes_response_headers_does_not_serialise_the_client, plus stdio_offers_no_shared_send_path and the_shared_handle_writes_the_same_request_as_the_exclusive_path. LOCATE BY SYMBOL, not by line: SharedSender and Transport::shared_sender in src/shared/transport.rs; StreamableHttpTransport::send_with_options_shared in src/shared/streamable_http.rs; Client::send_frame and ClientFrame in src/client/mod.rs; the Why comment at impl Transport for PooledTransport in src/shared/connection_pool.rs. Full record: 118.2-23-SUMMARY.md. | open |  | 2026-08-20T05:17:48.299Z |  |

````json
[
  {
    "id": 1,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "118.2-03 rewrote two collected_body_cap unit tests that measured a whole-body cap the POST SSE path no longer has (parser bound + receive() refusal is the new contract)",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T08:07:00.553Z",
    "resolved_at": null
  },
  {
    "id": 2,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "decode_sse_chunks_for_fuzz carries an annotated #[allow(clippy::type_complexity)] on its four-tuple return; a pub type alias was rejected to keep this plan's public-API addition at exactly one item",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T09:51:32.637Z",
    "resolved_at": null
  },
  {
    "id": 3,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/server/streamable_http_server.rs",
    "line": null,
    "description": "A malformed v1 logging/setLevel is rejected 400/-32601 by the pre-existing typed parse rather than answered {} as plan 118.2-07 predicted; the v2 _meta half of the claim holds",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T10:29:37.425Z",
    "resolved_at": null
  },
  {
    "id": 4,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/types/notifications.rs",
    "line": 161,
    "description": "LogMessageParams diverges from the 2026-07-28 schema: pmcp emits a required message and no data, the schema requires data and defines no message. 118.2-08 VERDICT: declared not fixed (breaking public-type change; the pinned suite validates no emitted notifications/message params). Mechanized by the_vendored_schema_requires_data_where_pmcp_emits_message.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-17T11:17:30.173Z",
    "resolved_at": "2026-08-17T13:35:18.978Z"
  },
  {
    "id": 5,
    "kind": "unmet-truth",
    "phase": "118.2",
    "file": "src/server/streamable_http_server.rs",
    "line": 1677,
    "description": "SEP-2575: on v2 a request with no _meta logLevel still gets notifications/message, because resolve_request_log_level returns None and DEFAULT_LOG_LEVEL (info) applies. Measured 118.2-09 RED mutation 2. Fixture guards; SDK does not.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T11:41:50.564Z",
    "resolved_at": null
  },
  {
    "id": 6,
    "kind": "unmet-truth",
    "phase": "118.2",
    "file": "crates/pmcp-team-servers/tests/era_matrix.rs",
    "line": 776,
    "description": "deprecated_capabilities_complete_under_both_eras still asserts no-live-stream, not completed: phase 118.2 fixed DELIVERY of the server-to-client request but a pmcp client cannot ANSWER one issued during its own call (parked inside transport.send while the server holds the tools/call POST). Detail moved from 'Dispatch oneshot channel closed' (0.18s) to 'Server request dispatch-1 timed out' (~30s).",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T12:11:22.433Z",
    "resolved_at": null
  },
  {
    "id": 7,
    "kind": "deviation",
    "phase": "118.2",
    "file": ".planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/deferred-items.md",
    "line": null,
    "description": "118.2-10 deferred the Client-level notification observation API: Client::notification_tx is None at src/client/mod.rs:406/:453/:496, ClientBuilder has no setter, the forwarding branch at :3764 is dead. A pmcp::Client consumer still cannot see notifications/message without dropping to Transport::receive().",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T12:11:22.529Z",
    "resolved_at": null
  },
  {
    "id": 8,
    "kind": "unmet-truth",
    "phase": "118.2",
    "file": "src/types/notifications.rs",
    "line": 168,
    "description": "LogMessageParams emits required 'message' and omits optional 'data'; the spec requires 'data' and has no 'message'. Measured 118.2-11: official suite 2025-11-25:tools-call-with-logging went 1/1 -> 0/2 (WireSchemaValid rejects all 3 frames; the reference zod client drops them so logCount=0). GAP_ATTRIBUTABLE_FAILURES 1 -> 2. Refutes 118.2-08's 'no scenario validates emitted params'.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-17T12:28:31.523Z",
    "resolved_at": "2026-08-17T13:44:06.553Z"
  },
  {
    "id": 9,
    "kind": "unmet-truth",
    "phase": "118.2",
    "file": "src/client/mod.rs",
    "line": null,
    "description": "MEASURED FLAKE: official suite 2025-11-25:tools-call-elicitation failed 1 of 9 fresh runs (118.2-11 re-measurement, held pin 0.2.0-alpha.11) with 'MCP error -32603: Dispatch oneshot channel closed' -- the same server-to-client request-lifecycle race as entry 6, here against the reference client. Already gate-fatal via BLOCKING_GREEN_SCENARIOS at pre-hardening settings, so the D-16 widening adds no new exposure. Nothing softened to accommodate it.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T13:44:13.623Z",
    "resolved_at": null
  },
  {
    "id": 10,
    "kind": "deviation",
    "phase": "118.2",
    "file": ".planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-15-PLAN.md",
    "line": 391,
    "description": "pmat verify line uses .violations[]/.path, which does not exist in pmat 3.15.0 (correct: .summary.violations[]/.file with a ./ prefix); the command errors instead of measuring",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T20:14:57.119Z",
    "resolved_at": null
  },
  {
    "id": 11,
    "kind": "deviation",
    "phase": "118.2",
    "file": ".planning/phases/118.2-the-v1-client-sse-transport-and-the-notifications-message-em/118.2-17-PLAN.md",
    "line": 341,
    "description": "same broken pmat jq path as 118.2-15-PLAN.md:391",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T20:14:57.208Z",
    "resolved_at": null
  },
  {
    "id": 12,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-15, CR-02 half 1): Transport::receive()'s terminal reason is now STICKY. OLD BEHAVIOUR: a terminal stream reason was pushed once onto the response mpsc, so it reached exactly ONE caller and every later caller blocked forever. NEW BEHAVIOUR: the reason is latched write-once on the transport and returned by EVERY subsequent receive() call, immediately. CONSUMER CONSEQUENCE: a consumer that loops on receive() and merely LOGS errors will now SPIN rather than hang. SEVERITY CORRECTED IN PLACE 2026-08-17 BY 118.2-21 - the framing this replaces ('a behaviour change with a documented mitigation') was INCOMPLETE IN THE DIRECTION THAT MATTERS, and 118.2-VERIFICATION.md recorded the severity direction as having WORSENED on this axis: the latch is Arc-shared across every clone, write-once, and as 118.2-15 shipped it had NO RESET SEAM in any constructor, in start_sse or in close(). Its blast radius was therefore PERMANENT, not one-off. Pre-closure a bad terminal reason could poison AT MOST ONE call (a single FIFO entry); post-closure the FIRST trip failed EVERY LATER tools/call against an SSE-answering server FOR THE LIFE OF THE PROCESS. And on the POST-answered-with-text/event-stream path it PRE-EMPTED A CALLER WHOSE OWN ANSWER WAS STILL IN FLIGHT, because drain_or_latch surfaced the latch as soon as try_recv() reported Empty while post_body returns Ok(()) before the SSE-delivered answer lands on the queue - so the queue was legitimately and transiently empty when the latch won. Reachability is ORDINARY, not adversarial: a spent MAX_SSE_RECONNECT_ATTEMPTS=2 budget, a 405 on reconnect, or one earlier truncated streaming-POST response. RESOLUTION: 118.2-19 scoped the terminal reason to the STREAM that raised it (StreamKind stamped on every TerminalReason), gated the latch on live POST-response readers (open_post_readers plus the RAII PostReaderGuard, acquired synchronously before tokio::spawn, with a wake on the last reader out), and added a RESET SEAM on a successful start_sse re-open. Fenced by a_latched_session_stream_does_not_pre_empt_an_sse_answered_call and a_reopened_session_stream_clears_the_terminal_latch, binary(client_sse_stream) 22 run / 22 passed. STICKY IS THEREFORE NO LONGER PERMANENT: a recovered transport is usable again, and a peer can now cause more than one latched reason over a transport's life where before it could cause exactly one (T-118.2-19-05, bounded by MIN_SSE_RECONNECT_DELAY and the reconnect budget). Full record as entry 17 of this ledger. Everything below this sentence stands as originally written and remains true. Mitigation already in the tree: Transport::receive's rustdoc carries the heading 'The terminal reason is STICKY - stop on it, do not loop', stating that the contract is to STOP on a terminal error. Sticky was chosen over one-shot deliberately - a one-shot reason restores the exact CR-02 hazard the latch exists to remove, where the error is consumed by whichever caller happened to be next and every caller after that gets an unexplained hang. T-118.2-15-04. Invisible to cargo semver-checks: the latch is a private field, so 223/223 checks pass and this disclosure is the only place a consumer learns of it.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T22:19:42.169Z",
    "resolved_at": null
  },
  {
    "id": 13,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/client/mod.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE (118.2-15, CR-02 half 2): Client::dispatch_request now compares response.id against the request_id it is awaiting and keeps looping on a mismatch; the mismatched frame is DISCARDED, because a Transport consumer holds no producer handle to re-queue it and an orphan holding pen would add its own unbounded-growth question. CONSUMER CONSEQUENCE, RESTATED 2026-08-17 BY 118.2-21 AT THE SEVERITY THE CLOSURE REVIEW ACTUALLY FOUND - the text this replaces described an ACCEPTED, BOUNDED cost and was WRONG IN THE DIRECTION THAT MATTERS, and it was rewritten in place rather than annotated beneath because a false claim of record cannot be left standing beside its own correction: as 118.2-15 shipped it the discard wait was BOUNDED BY NOTHING AT ALL. dispatch_request applies no tokio::time::timeout anywhere and never reads the public RequestOptions::timeout field, so the ceiling the superseded text named DOES NOT EXIST in pmcp::Client; 118.2-VERIFICATION.md classified that claim as a documentation-of-record BLOCKER in its own right. And the cost was never confined to the mismatched caller: the discard held the transport WRITE lock across that unbounded receive().await and re-took it on every continue, so ONE mis-addressed frame WEDGED EVERY OPERATION ON THE CLIENT - send, call_tool and close alike - with no error, no log after the first warn!, and no recovery. Measured on the RED tree: the second operation's notifications/cancelled NEVER REACHED THE WIRE inside LOCK_PROBE_BOUND (5 s), and the awaiting call was still parked at 20 s. It is still strictly better than one caller silently receiving another caller's tool result (a cross-request data leak inside one process), and it remains a real cost accepted as T-118.2-15-03. RESOLUTION AS OF 118.2-20, NOW ITSELF SUPERSEDED - see the dated clause at the end of this entry: 118.2-20 bounded the wait with MISMATCH_DISCARD_TIMEOUT (10 s, armed on the FIRST mismatch only), SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; capped the discards with MAX_ID_MISMATCH_DISCARDS (32, failing loudly and naming the count and both typed ids), SUPERSEDED BY MAX_UNMATCHED_RESPONSES; and released the transport write guard on a MISMATCH_RECEIVE_SLICE (250 ms), SUPERSEDED BY PUMP_RECEIVE_SLICE, so another operation waits at most one slice. Fenced at the time by a_mismatched_frame_does_not_block_another_operation_on_the_same_client and a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever, SUPERSEDED BY the count-and-clock bounds now exercised inside binary(client_sse_stream); binary(client_sse_stream) 24 run / 24 passed AT THAT TIME. Full record as entry 18 of this ledger. A LENIENT SERVER that re-types the id - a JSON string where the client sent a number - now gets a wait too, because RequestId equality is TYPED and structural and JSON-RPC 2.0 requires the response id to be the same VALUE as the request's. FOLLOW-UP that removes the cost: per-id response ROUTING (active_requests carrying a response channel per id), recorded in this phase's deferred-items.md under '## DEFERRED (118.2-15): per-id response ROUTING, and the cost of discard-on-mismatch' - IT SHIPPED, see the next clause. BEHAVIOUR SUPERSEDED 2026-08-19, RECORDED BY 118.2-24: EVERYTHING ABOVE IS HISTORY AND IS NOT HOW THE CLIENT BEHAVES AT HEAD. The THIRD gap-closure round (entry 24 of this ledger, commits e104dea6 / d01b87e2 / 2d385d60 / 26447f94) rewrote Client::dispatch_request into a per-id ROUTER, and 118.2-22 (entry 25) then split the classification of the frames the router does not own. AT THIS HEAD: a response frame whose id belongs to a LIVE caller is ROUTED to that caller, not discarded, so no live caller loses its answer and no live caller pays for another's frame; only a frame NOBODY awaits is dropped; and of those, one that is the LATE ANSWER of a call this client already stopped waiting for is ABSORBED without charging anyone, while a frame addressed to an id this client never minted still spends budget and still fails the awaiting call. The ceiling was NOT removed - it exists under UNMATCHED_RESPONSE_TIMEOUT (10 s) and MAX_UNMATCHED_RESPONSES (32); see entry 20's own in-place correction, which repairs the opposite error. LOCATE BY SYMBOL, not by line, and only by these: Client::pump_once, PumpStep, Pending, UnmatchedBudget, AbandonedRequestIds and Client::absorb_abandoned in src/client/mod.rs. SUPERSEDED SUMMARIES, NAMED RATHER THAN REWRITTEN (none of them was edited, and none may be): 118.2-15-SUMMARY.md and 118.2-20-SUMMARY.md describe the discard-and-loop mechanism this clause supersedes; 118.2-22-SUMMARY.md and 118.2-23-SUMMARY.md describe the client that ships. NOT the client lifecycle deadlock: entry 6 is a DIFFERENT defect (dispatch_request awaits transport.send before entering its receive loop while the server holds the POST open) and this edit deliberately did not touch that send-then-receive ordering - entry 6 stands unchanged and remains open.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T22:19:53.731Z",
    "resolved_at": null
  },
  {
    "id": 14,
    "kind": "unmet-truth",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "The gap-closure round (118.2-14..18) fixed CR-01, CR-02, WR-01 and WR-02 and DECLINES four review Warnings; disclosed here with their consequences so a reader scanning this ledger does not have to infer them from a plan summary. WR-03: start_sse's TOCTOU on abort_handle can leave a second, untracked GET session reader, so a client sees each server-initiated message TWICE - narrowed by 118.2-17, since the two readers can no longer poison each other's reconnect cursor (each owns a local) and both are now stoppable, so it is duplicate delivery only. WR-04: a reconnect GET gets no 401 refresh, so when a bearer token expires and the gateway drops the idle session stream, the reissued GET is answered 401, the reconnect loop treats it as terminal, and the session stream is lost PERMANENTLY - sampling/roots/elicitation dead with no recovery short of rebuilding the client - while POSTs on the same transport keep working. WR-05: an ordinary EOF on the session stream escalates to an application-visible Err, contradicting the receive() taxonomy row that promises the reader 'exits silently'; 118.2-15 rewrote how a terminal reason is DELIVERED while leaving that row exactly as it stood, so the divergence is untouched rather than half-fixed. WR-06: logging/setLevel reports success while silently discarding the level on stateless v1 deployments and inside a JSON-RPC batch, so a client that asked for 'error' keeps receiving 'info' chatter it explicitly declined; explicitly OUT OF SCOPE for this closure because fixing it reopens CONF-10 territory that plans 118.2-07, 118.2-08 and 118.2-13 argued to a booked conclusion on locked decisions D-10..D-13. FULL RECORD, with each finding's 118.2-REVIEW.md anchor, source span, consequence and reason (and IN-01..IN-06 besides): this phase's deferred-items.md, section '## GAP-CLOSURE ROUND (2026-08-17)'. The recorded source spans predate this closure's edits to the same file - locate by symbol, not by line.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-17T22:20:07.236Z",
    "resolved_at": null
  },
  {
    "id": 15,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "CR-01 (Critical; raised by 118.2-REVIEW.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): a peer-supplied SSE 'retry: 0' was bounded only from ABOVE and the reconnect budget was refunded by any single delivered frame (if delivered { attempt = 0; }), so one frame per body drove pmcp's OWN client into an unbounded zero-delay reconnect loop - a remote-triggerable client-side DoS that also fetched a fresh auth_provider access token per iteration. FIXED by 118.2-14 with a two-sided delay bound (MIN_SSE_RECONNECT_DELAY, 500 ms) plus an uptime-gated budget (budget_reset_earned / RECONNECT_BUDGET_RESET_UPTIME, 30 s); both bounds are separately load-bearing. Fence: reconnect_with_one_delivered_frame_and_zero_retry_stays_bounded in binary(client_sse_stream), 20 run / 20 passed (target/118.2-17-green.log). RED at 65 'GET / HTTP/1.1' lines in 7.9 s against a 3-GET budget (target/118.2-14-red.log) - a measurement that cannot be retaken.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-17T22:20:25.030Z",
    "resolved_at": "2026-08-17T22:20:39.888Z"
  },
  {
    "id": 16,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "CR-02 (Critical; raised by 118.2-REVIEW.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): every terminal stream reason rode the SAME mpsc<Result<TransportMessage>> the responses ride, so a reason raised while the application was idle failed the next, unrelated request; and src/client/mod.rs's dispatch_request returned on the first Response frame it popped with NO comparison of response.id, so one out-of-band queue entry desynchronised the FIFO permanently and call n+1 silently received call n's result. FIXED by 118.2-15, both halves: a write-once terminal-reason latch consulted only after the queue is drained, plus response-id correlation that keeps looping on a mismatch. Fences: an_idle_terminal_error_does_not_fail_the_next_unrelated_call and a_response_whose_id_does_not_match_is_not_returned_as_this_calls_answer in binary(client_sse_stream), 20 run / 20 passed. RED at a tools/call that SUCCEEDED on the wire yet reported the stale session-stream reconnect error, and at call 2 receiving the marker 'call-answer-1' (target/118.2-15-red.log); the latch alone is a HALF fix, measured at 17 run / 16 passed / 1 failed (target/118.2-15-latch-only.log). The two consumer-observable COSTS of this fix are disclosed as separate OPEN entries in this ledger: the sticky receive() reason, and discard-on-mismatch. CROSS-REFERENCE ADDED 2026-08-17 BY 118.2-21 - THIS 'fixed' STATUS IS ACCURATE ABOUT THE ORIGINAL CR-02 DEFECT AND IS NOT THE WHOLE STORY, AND IT IS NOT FLIPPED: CR-02's OWN CLOSURE CODE INTRODUCED TWO CRITICAL DEFECTS ON THE SAME CLIENT PATH, each independently confirmed against the merged source by 118.2-VERIFICATION.md and each raised by 118.2-REVIEW-closure.md (whose WR/IN numbering restarts over DIFFERENT findings from 118.2-REVIEW.md's - see entry 21). BLOCKER 1: the sticky latch pre-empted an in-flight, SSE-answered POST response, PERMANENTLY, because it had no reset seam - recorded as entry 17 and as the corrected entry 12. BLOCKER 2: the id-mismatch discard held the transport write lock across an UNBOUNDED receive().await, wedging the whole Client, and the ceiling it was booked against did not exist - recorded as entry 18 and as the corrected entry 13. BOTH ARE CLOSED, by plans 118.2-19 and 118.2-20 respectively, with fences and parsed counts on those entries. A reader must not take this row's 'fixed' as meaning the client path was safe after 118.2-15; it was not, and the second gap-closure round is what made it so.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-17T22:20:25.106Z",
    "resolved_at": "2026-08-17T22:20:39.960Z"
  },
  {
    "id": 17,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "BLOCKER 1 (Critical; raised as CR-01 in 118.2-REVIEW-closure.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): the write-once terminal-reason latch that 118.2-15 introduced to close CR-02 was Arc-shared across every clone, had NO reset seam in any constructor, in start_sse or in close(), and drain_or_latch surfaced it as soon as try_recv() reported Empty. On the POST-answered-with-text/event-stream path this phase's own D-01 work added, post_body spawns a detached SSE reader and returns Ok(()) BEFORE the answer lands on the queue, so the queue is legitimately and transiently empty while a real answer is on the wire - and the latch won instantly with a stale, unrelated reason. REACHABILITY IS ORDINARY, NOT ADVERSARIAL: a spent MAX_SSE_RECONNECT_ATTEMPTS=2 budget, a 405 on reconnect, or one earlier truncated streaming-POST response. PERMANENCE: because the latch never reset, the FIRST trip failed every later tools/call against an SSE-answering server for the LIFE OF THE PROCESS - a severity direction strictly worse than the one-call poisoning it replaced. Fence 16 could not catch it: its harness answers content-type application/json, so the response lands on the queue synchronously and the queue-wins-over-latch rule never gets exercised against the SSE-answered shape. FIXED by 118.2-19: StreamKind stamped on every TerminalReason and rendered by to_error, an in-flight POST-reader gate (open_post_readers plus the RAII PostReaderGuard, acquired synchronously BEFORE tokio::spawn), a wake on the last reader out, and a reset seam on a successful start_sse re-open. Fences: a_latched_session_stream_does_not_pre_empt_an_sse_answered_call and a_reopened_session_stream_clears_the_terminal_latch in binary(client_sse_stream), 22 run / 22 passed / 0 skipped (target/118.2-19-green.log), plus binary(pmcp) and test(latch_gate) 9 run / 9 passed (target/118.2-19-latchgate.log). RED at target/118.2-19-red.log - 22 run / 20 passed / 2 failed, exactly the two new fences; call 1 reported the GET session stream's reconnect-budget error although it had SUCCEEDED on the wire, and fence 22 measured receive() answering the latch 6.625 microseconds after a SUCCESSFUL re-open, which is what proves the failure was permanent rather than delayed. Neither measurement is retakeable. Invisible to cargo semver-checks: every symbol is private.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-18T03:55:33.713Z",
    "resolved_at": "2026-08-18T03:56:37.143Z"
  },
  {
    "id": 18,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/client/mod.rs",
    "line": null,
    "description": "BLOCKER 2 (Critical; raised as CR-02 in 118.2-REVIEW-closure.md, independently confirmed against the merged source by 118.2-VERIFICATION.md): the id-mismatch discard loop that 118.2-15 introduced to close CR-02 was defective in BOTH halves. HALF A, THE LOCK: 'let response_message = self.transport.write().await.receive().await?;' held the RwLockWriteGuard TEMPORARY across the await because the guard lives to the end of the let statement, and the discard loop continue-d on a mismatch, re-taking and re-holding it - so ONE mis-addressed frame blocked every other send / call_tool / close on that Client, not only the mismatched caller. HALF B, THE NON-EXISTENT CEILING: the wait was bounded by NOTHING. dispatch_request applies no tokio::time::timeout anywhere and never reads the public RequestOptions::timeout field, so the comment and the deferred-items entry that booked the cost as bounded by that request's own timeout named a ceiling that does not exist in pmcp::Client. One frame with a re-typed id (Number(1) where the client sent String(\"1\")), from a hostile peer or a merely lenient one, wedged the client with no error, no further log after the first warn!, and no recovery. FIXED by 118.2-20: MISMATCH_DISCARD_TIMEOUT (10 s, armed on the FIRST mismatch only so a dripping peer cannot extend the ceiling), MAX_ID_MISMATCH_DISCARDS (32, failing loudly with a TransportError::InvalidMessage naming the count and both typed ids), and Client::receive_bounded taking the guard in an INNER SCOPE under a MISMATCH_RECEIVE_SLICE (250 ms) so the lock is released and re-taken each slice. Fences: a_mismatched_frame_does_not_block_another_operation_on_the_same_client and a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever in binary(client_sse_stream), 24 run / 24 passed / 0 skipped (target/118.2-20-green.log), plus binary(pmcp) and test(mismatch_budget) 6 run / 6 passed (target/118.2-20-unit.log). RED at target/118.2-20-red.log - 24 run / 22 passed / 2 failed; fence 23's lock probe NEVER RETURNED inside LOCK_PROBE_BOUND (5 s) and post_bodies() at the elapse held initialize, notifications/initialized and tools/call but NO notifications/cancelled at all - that absence IS the measurement, because cancel_request never left the process, parked on the same transport.write() the discard loop held; fence 24's call was STILL PARKED at 20 s (MISMATCH_TIMEOUT_BOUND), neither Ok nor Err. Neither measurement is retakeable. Invisible to cargo semver-checks: all three constants are private. HISTORY, NOT CURRENT GUIDANCE - noted in place 2026-08-19 by 118.2-24: the mechanism this entry names was DELETED on 2026-08-18 by the THIRD gap-closure round (entry 24 of this ledger). MISMATCH_DISCARD_TIMEOUT, SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; MAX_ID_MISMATCH_DISCARDS, SUPERSEDED BY MAX_UNMATCHED_RESPONSES; MISMATCH_RECEIVE_SLICE, SUPERSEDED BY PUMP_RECEIVE_SLICE; Client::receive_bounded, SUPERSEDED BY Client::pump_once; and the fence a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever, SUPERSEDED BY the count-and-clock bounds now exercised inside binary(client_sse_stream). This entry stays as the record of what BLOCKER 2 was and how it was closed AT THE TIME; do not locate anything in the tree by the symbols above.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-18T03:55:49.069Z",
    "resolved_at": "2026-08-18T03:56:37.213Z"
  },
  {
    "id": 19,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design and OPEN as a disclosure (118.2-19, closing BLOCKER 1): THREE consumer-observable changes ship with NO semver signal, because every symbol involved is private and cargo semver-checks therefore reports no semver update required - this entry is the only place a consumer learns of them. (1) Transport::receive() no longer answers IMMEDIATELY with a latched terminal reason while a streaming POST response is in flight; it WAITS while open_post_readers is non-zero. A consumer that relied on the immediate (stale) answer now sees a wait instead. This is a strict improvement - the stale answer was BLOCKER 1 - but it is a timing change a downstream consumer can observe. (2) A successful start_sse re-open now CLEARS a latch that was previously permanent, so a peer can cause MORE THAN ONE latched reason over a transport's life where before it could cause exactly one; MIN_SSE_RECONNECT_DELAY and the reconnect budget bound how fast that cycle can run, and one diagnosis per genuine recovery cycle is the correct amount (T-118.2-19-05). Entry 12's 'sticky' wording remains accurate; sticky is no longer PERMANENT. (3) The rendered TEXT of every terminal reason now gains a stream-name prefix - 'the GET session stream ended: ...' or 'this call's own POST response stream ended: ...'. Every existing message BODY is preserved verbatim (pinned by to_error_names_the_stream_and_preserves_the_message_body), so a consumer matching on a substring of the body still matches, but a consumer matching on the WHOLE string, or logging it, sees different text. RESIDUAL under peer control, named rather than buried: a peer that holds a POST response body open forever keeps the gate closed for that transport - this is D-01's existing shape, not new, and close() and last-clone-drop both stop such a reader (fences 18 and 19 pass unchanged), with the reader's exit now waking any parked consumer.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-18T03:56:13.311Z",
    "resolved_at": null
  },
  {
    "id": 20,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/client/mod.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design and OPEN as a disclosure (118.2-20, closing BLOCKER 2): TWO consumer-observable changes, both with NO semver signal because all three constants are private. (1) A dispatch_request that has seen a mis-addressed frame now FAILS with a timeout after MISMATCH_DISCARD_TIMEOUT (10 s), or with a named TransportError::InvalidMessage after MAX_ID_MISMATCH_DISCARDS (32), where it previously waited FOREVER. THE BLAST RADIUS IS WIDER THAN 'wedged becomes bounded' AND MUST NOT BE SOFTENED TO THAT: the deadline is armed on the FIRST mismatch and STAYS ARMED FOR THE LIFE OF THAT CALL, so a peer that emits ONE stray frame at t=0 and then answers CORRECTLY at t=15 s previously SUCCEEDED and now FAILS at 10 s. That is a NEW FAILURE MODE for a previously-working slow-but-correct peer, not merely a bound on a hang. It is the mechanism the design requires - arming on the first mismatch is what makes the ceiling un-extendable by a peer emitting a steady drip of wrong ids, and the re-arming alternative is strictly worse - but a downstream consumer with a slow and slightly lenient peer WILL observe it. CORRECTED IN PLACE 2026-08-19 BY 118.2-24, in place rather than appended beneath, because a false claim of record cannot be left standing beside its own correction - the house rule 118.2-21 set for exactly this file. THE SENTENCE THAT STOOD HERE promised that per-id response routing removes discards entirely and therefore removes this ceiling too. THAT IS FALSE AT HEAD, and 118.2-VERIFICATION.md classified it as a documentation-of-record BLOCKER in its own right. The THIRD gap-closure round (entry 24 of this ledger) removed the discarding of frames that DO match a LIVE caller's id; a frame matching NO id is still dropped, and the ceiling came back under two RENAMED PRIVATE CONSTANTS. WHAT ACTUALLY BOUNDS THE CLIENT AT THIS HEAD, stated here so a reader needs no other entry: UNMATCHED_RESPONSE_TIMEOUT (10 s) and MAX_UNMATCHED_RESPONSES (32) in src/client/mod.rs, either bound alone sufficient, armed by UnmatchedBudget::record on the FIRST frame nobody awaits and NEVER re-armed. It fires ONLY once a frame addressed to an id THIS CLIENT NEVER MINTED has actually been seen - a re-typed id, or a peer spraying ids nobody here ever asked for - so the slow-and-lenient-peer blast radius stated above is UNCHANGED and still real; and a peer that has simply gone QUIET is bounded by nothing at all, still deliberately, because pmcp has no default request timeout and a blanket deadline would break long-running tools/call handlers. THE SELF-SUSTAINING CHAIN that arming-on-the-first-frame created - a call that already died leaving its own real answer un-owned for the NEXT call to book as the peer's misbehaviour and arm ITS deadline on frame one, failing every later call on that Client permanently - was closed by 118.2-22 (entry 25) with AbandonedRequestIds and Client::absorb_abandoned, which classify our own debris apart from the peer's misbehaviour and absorb it exactly once. The two constants item (1) names above are HISTORY, not current guidance: MISMATCH_DISCARD_TIMEOUT, SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; MAX_ID_MISMATCH_DISCARDS, SUPERSEDED BY MAX_UNMATCHED_RESPONSES. (2) The transport write lock is now RELEASED and RE-TAKEN on a MISMATCH_RECEIVE_SLICE (250 ms, SUPERSEDED BY PUMP_RECEIVE_SLICE) during a discard, so another operation on the same Client waits at most one slice instead of indefinitely; tokio's RwLock is fair (FIFO semaphore), so a waiter queued while the guard was held gets it before the discard loop re-takes it. CORRECTED IN PLACE 2026-08-19 BY 118.2-24: item (2)'s claim that another operation waits at most one slice instead of indefinitely was TRUE OF THE RECEIVE PATH AND FALSE OF THE SEND PATH, and it stayed half wrong for TWO ROUNDS rather than being caught. Client::dispatch_request took the SAME transport write guard and awaited the SEND inside it; that send reaches StreamableHttpTransport::post_once, which awaits the peer's response HEADERS, and no request timeout exists on any path in src/shared/streamable_http.rs - so a peer that accepted the POST and never wrote its response head held the one guard indefinitely and every other operation on that Client, close() included, blocked at acquisition. Neither code review saw it and neither earlier verification did either; it was found by 118.2-VERIFICATION.md's THIRD round and CLOSED by 118.2-23 (entry 27), which routes every client-side send through Client::send_frame over an OWNED handle taken under a momentary guard that is dropped BEFORE the round trip is awaited. The claim is true on BOTH paths only from 118.2-23 onward. TWO EXCEPTIONS a reader must not infer away, both named in entry 27: a POOLED StreamableHttpTransport keeps the exclusive path, and Client::open_event_stream still holds a read guard across the subscriptions/listen POST's response head. REJECTED ALTERNATIVE, recorded because it is the obvious one: plumbing the PUBLIC RequestOptions::timeout in as the ceiling was rejected because activating a public field that has NEVER been read silently changes behaviour for every existing caller who already sets it, with no version signal and no way to walk it back except another behaviour change. A private ceiling is the smaller and reversible choice. NOTE FOR A FUTURE READER: 118.2-20 made grep -c 'self.transport.write().await.receive()' src/client/mod.rs return 0 for code, but a guard IS still held across receive() on the ORDINARY (no-mismatch) path - bound to a NAMED LOCAL rather than left an invisible temporary. That non-change is deliberate and is stated at the branch; do not conclude from that grep alone that no guard is ever held across a receive.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-18T03:56:13.382Z",
    "resolved_at": null
  },
  {
    "id": 21,
    "kind": "unmet-truth",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "The SECOND gap-closure round (plans 118.2-19..21) closed BLOCKER 1 and BLOCKER 2 (entries 17 and 18) and DECLINES four Warnings and four Info items from 118.2-REVIEW-closure.md; disclosed here with consequences and named owners so a reader scanning this ledger does not have to infer them from a plan summary. READ THIS FIRST - AN ID COLLISION: 118.2-REVIEW.md (the FIRST review) numbered its findings WR-01..WR-06 and IN-01..IN-06, and 118.2-REVIEW-closure.md (the SECOND review, of the closure itself) RESTARTED at WR-01..WR-05 and IN-01..IN-04 over ENTIRELY DIFFERENT findings. Entry 14 of this ledger indexes the FIRST set; this entry indexes the SECOND. Every id below is prefixed with its file; a bare id will lead a later auditor to the wrong entry. CLOSURE-REVIEW WR-01: deliver_sse_event's delivery.sender.send(Ok(message)).await is a BARE await not raced against shutdown (only the body read is), so after close() a peer that keeps writing fills the 64-slot queue and the detached POST reader parks in send() forever - close() does not drop the Receiver and its abort reaches only the GET reader, so the TASK AND ITS TCP SOCKET LEAK. The first round's WR-01 fix moved the leak ONE AWAIT DOWN rather than eliminating it. Fence 19 misses it because its POST stream is idle. THIS RESIDUAL WAS RECORDED BY THE PREVIOUS ROUND AND NEVER RE-VERIFIED (118.2-VERIFICATION.md marks it PARTIAL and says so); being unnamed and unre-verified is how it survived a whole closure round, which is why it is named here with an owner. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section. CLOSURE-REVIEW WR-02: the biased; in read_next_sse_frame polls the body first and returns on the first Ready, so both shutdown arms are never polled while a firehose peer keeps the body ready - close() is not observed for as long as the peer keeps writing. The bias is deliberate and correct for the frame arm (D-04, never drop a burst-then-close server's last frames); a fairness escape is a tuning decision on the same loop WR-01's fix touches. OWNER: same. CLOSURE-REVIEW WR-03: the shutdown flag is never reset, so after any close() a later start_sse returns Ok(()) over a reader that exits immediately, every streaming-POST reader dies the same way and its dispatch_request hangs, is_connected() still answers true, and because shutdown is Arc-shared and the transport is Clone, one component closing ITS clone permanently disables SSE on EVERY clone. NEAR MISS worth recording: 118.2-19 added a reset seam for the TERMINAL LATCH and deliberately did NOT add one for the shutdown flag, because the review offers two CONTRADICTORY contracts (reset the flag and support close-then-restart, or make close observably terminal with is_connected() reporting it) and picking one inside a Blocker closure would settle a public contract by accident - the same reasoning 118.2-16 used to defer the first review's WR-05. OWNER: same plan, which should decide BOTH reset seams together. CLOSURE-REVIEW WR-04: the test mocks re-address EVERY response once any request has been recorded, so a fixture deliberately carrying a foreign id is silently rewritten and a NEGATIVE TEST PROVES NOTHING WHILE STILL PASSING - a false green in exactly the surface BLOCKER 2 lived in. OWNER: whichever plan next touches tests/common/. CLOSURE-REVIEW IN-01..IN-04: a stale 'already sent' comment that describes the behaviour CR-02 removed, a dead state.done write, the reconnect floor applied per-branch rather than at the exit, and a write-only cursor local kept deliberately. Hygiene, no correctness consequence, no ordering constraint. OWNER: ride along with whichever plan next touches the file. NOT DEFERRED: closure-review WR-05 (fence 17 passing on ANY error, including ones that never reach the correlation check) is CLOSED - 118.2-20 tightened that arm. UNCHANGED: the FIRST review's WR-03..WR-06 and IN-01..IN-06 remain open exactly as entry 14 and deferred-items.md's first appendix record them; this round changed no status among them. FULL RECORD with anchors, consequences, reasons and owners: this phase's deferred-items.md, section '## SECOND GAP-CLOSURE ROUND (plans 118.2-19..21)'. Locate source by SYMBOL, not by line - this round edited both files.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-18T03:56:37.073Z",
    "resolved_at": null
  },
  {
    "id": 22,
    "kind": "deviation",
    "phase": "118.2",
    "file": "Makefile",
    "line": 247,
    "description": "FALSE GREEN, measured by 118.2-21's closing gate run and NOT this round's defect: 'make validate-always' step 1 (FUZZ) reports success while fuzzing NOTHING. test-fuzz at Makefile:247 runs 'timeout 30s $(CARGO) fuzz run $$target || echo \"Fuzz target $$target completed\"'. $(CARGO) is STABLE cargo, and cargo-fuzz builds with -Zsanitizer=address, which stable rejects with 'error: the option Z is only accepted on the nightly compiler'. EVERY fuzz target therefore fails to BUILD, the || arm prints 'Fuzz target X completed' in yellow, the loop continues, and the target exits 0 - so 'ALL ALWAYS requirements validated' and 'ALL TOYOTA WAY QUALITY CHECKS PASSED' are both printed having executed ZERO fuzz iterations. Measured verbatim at target/118.2-21-gate.log and target/118.2-21-always.log: the build error appears once per target and no target reports a run count. CONSEQUENCE: CLAUDE.md's ALWAYS-fuzz requirement cannot be discharged by the gate; a plan that cites 'make validate-always exit 0' as its fuzz evidence has cited nothing. This is the same class of defect as WINDOWS entries 10 and 11 (a verify command that exits without measuring), here in the Makefile rather than in a plan file. PROVENANCE - NOT THIS ROUND'S: git diff --name-only 45929873..HEAD -- Makefile fuzz/ returns 0 lines; neither file was touched by plans 118.2-19, -20 or -21. THE REAL FUZZ DISCHARGE for this round is plan 118.2-19's explicit nightly invocation, 'cargo +nightly fuzz run streamable_sse_frames -- -runs=20000' - 20000 runs, exit 0, fuzz/artifacts/streamable_sse_frames/ empty before and after. NOT FIXED HERE: pre-existing, off this round's path, and the fix is a build-tooling decision (pin +nightly in the recipe, or fail closed on a non-nightly toolchain instead of swallowing the error) that belongs with whoever owns the Makefile's test targets. OWNER: whichever plan next touches the Makefile's ALWAYS targets.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-18T04:16:03.883Z",
    "resolved_at": null
  },
  {
    "id": 23,
    "kind": "unmet-truth",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] CR-03, OPEN AND DEFERRED BY DEVELOPER DECISION (2026-08-18), recorded at the severity re-measured against HEAD 26447f94 rather than the severity 118.2-VERIFICATION.md assigned. THE DEFECT: drain_or_latch (src/shared/streamable_http.rs, the gate reading open_post_readers.load(Ordering::SeqCst) > 0) answers a PER-CALLER question ('is MY answer still on the wire') with a TRANSPORT-WIDE count, and PostReaderGuard::drop wakes only on the 1->0 transition. With two or more concurrent SSE-answered POST responses live on one transport, a caller whose OWN stream has already latched its OWN correct terminal reason cannot see that reason while an UNRELATED reader is still open. TRIGGER, measured precisely: concurrency >= 2 SSE-answered POST responses AND a terminal failure (connection drop, oversized chunk, unparseable frame) on one of them. NOT a load-dependent defect - two concurrent streaming tool calls suffice, which is ordinary for an agent doing parallel tool invocation; the rare half is the transport failure, not the concurrency. The always-open v1 session GET stream does NOT gate: PostReaderGuard::acquire appears only in spawn_sse_reader (the POST-response path), so the single-caller case is clean. CONSEQUENCE, RE-MEASURED AND MILDER THAN THE VERIFICATION'S FRAMING: 118.2-VERIFICATION.md describes an 'unbounded, silent hang' for 'the process lifetime'. That was accurate against the code as it stood at 31a80a75, and is NO LONGER accurate after the per-id response router landed in d01b87e2. Today the failed call receives a DELAYED ERROR bounded by the other reader's lifetime - the last guard's drop fires the wake and the latch then surfaces. No answer is lost, none is mis-delivered to the wrong caller, and nothing is starved (pump_once slices the transport write lock every PUMP_RECEIVE_SLICE = 250ms rather than parking on it). Re-confirmed by direct source read that ReaderDelivery carries the SHARED sender and SHARED terminal latch, so reader ERRORS are not per-caller routed even though successful RESPONSES now are. WHY DEFERRED, developer's reasoning verbatim in intent: the architecture is moving away from SSE toward stateless calls with MCP Tasks and task-store polling for progress. This defect lives exclusively in the POST-response SSE reader path, i.e. on the v1 surface being deprecated; the v2 stateless/Tasks direction does not create concurrent POST readers at all, so the population of affected deployments shrinks rather than grows. Deferred as a Warning, NOT closed and NOT waived - no fix is scheduled and none may be implied. SIBLING DEFECT WORTH FIXING WITH IT: the unit test a_post_reader_in_flight_gates_a_reason_from_either_stream ASSERTS THE DEFECTIVE SHAPE IS CORRECT and pins the single-reader case only. A future fixer will read it as a deliberate invariant and mistake their own fix for a regression. There is NO two-concurrent-reader fence anywhere in the crate - searched at HEAD. THE DURABLE FIX (for whoever picks this up): scope the gate to the stream that owns the question - let a StreamKind::PostResponse reason surface immediately regardless of open_post_readers, reserving the transport-wide gate for StreamKind::Session reasons; the fully durable form is a reason per reader delivered to the caller that owns it, the same per-id routing d01b87e2 already applied to responses. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section - the same owner as closure-review WR-01/WR-02/WR-03 (entry 21), which touch the same loop and should be decided together. LOCATE BY SYMBOL, NOT BY LINE.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-19T00:25:15.934Z",
    "resolved_at": null
  },
  {
    "id": 24,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/client/mod.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] RETROSPECTIVE ENTRY OF RECORD, written 2026-08-19 by 118.2-24 for a round that shipped with NO plan, NO summary, NO ledger entry, NO requirement amendment and NO semver verdict of its own: the THIRD gap-closure round of phase 118.2, commits e104dea6, d01b87e2 (\"route JSON-RPC responses by id instead of discarding them\"), 2d385d60 and 26447f94. It is the LARGEST consumer-observable change in the phase - a 378-line rewrite of Client::dispatch_request into a per-id response ROUTER - and it was found only because 118.2-VERIFICATION.md re-verified against a tree four commits newer than the one the previous verification described. THIS ENTRY IS RETROSPECTIVE: it is written after the fact from the diff and from the source at HEAD, not from a plan the round followed, and the fact that it had to be reconstructed at all is the defect it records. WHAT IT DELETED - every one of these is a symbol some earlier entry or requirement row still instructed a reader to LOCATE BY, which is why each surviving citation now carries the marker: Client::receive_bounded, SUPERSEDED BY Client::pump_once; MismatchBudget, SUPERSEDED BY UnmatchedBudget; MISMATCH_DISCARD_TIMEOUT, SUPERSEDED BY UNMATCHED_RESPONSE_TIMEOUT; MAX_ID_MISMATCH_DISCARDS, SUPERSEDED BY MAX_UNMATCHED_RESPONSES; MISMATCH_RECEIVE_SLICE, SUPERSEDED BY PUMP_RECEIVE_SLICE; and the fence a_mismatched_frame_fails_this_call_within_a_bound_instead_of_waiting_forever, SUPERSEDED BY the count-and-clock bounds now exercised inside binary(client_sse_stream). WHAT IT ADDED: Pending, the per-id answer channel registered in Client::active_requests BEFORE the frame goes on the wire; Client::pump_once and PumpStep; PUMP_RECEIVE_SLICE (250 ms), the slice on which the transport guard is released so a second operation can make progress; UnmatchedBudget with UNMATCHED_RESPONSE_TIMEOUT (10 s) and MAX_UNMATCHED_RESPONSES (32); echoed_request_id bounded by MAX_ECHOED_REQUEST_ID (128) so a hostile peer cannot push an unbounded id into a client log; and fences 25 and 26. CONSUMER CONSEQUENCE: a response frame whose id belongs to a DIFFERENT LIVE caller is now DELIVERED to that caller instead of being discarded, so the cross-caller answer destruction entry 13 discloses no longer happens to a live caller. Only a frame NOBODY awaits is dropped, and only such a frame spends budget. THE CEILING WAS NOT REMOVED - it was re-added under the two renamed private constants above, which is exactly the claim entry 20 got wrong and 118.2-24 corrected in place. WHAT IT LEFT BEHIND, both closed later in this phase rather than by the round itself: the self-sustaining chain, where a call that died at its ceiling left its own real answer un-owned for the NEXT call to book as the peer's misbehaviour, closed by 118.2-22 and recorded as entry 25 of this ledger; and the SEND-path wedge, where the transport write guard was held across a POST whose response head never arrives, closed by 118.2-23 and recorded as entry 27. NO SEMVER SIGNAL: every symbol above is private. Two of the four commits also touched SERVER code (src/server/core.rs, src/server/streamable_http_server.rs, tests/log_emitter.rs), which is why 118.2-24 RE-RAN the official conformance suite at this HEAD rather than scoping it out as client-only - the suite exercises pmcp as a SERVER, so it cannot attribute the client rewrite but it can see those edits. The semver verdict of record for this round is taken by 118.2-24 at this HEAD and quoted in 118.2-24-SUMMARY.md. LOCATE BY SYMBOL, not by line: Client::pump_once, PumpStep, Pending, UnmatchedBudget, PUMP_RECEIVE_SLICE, UNMATCHED_RESPONSE_TIMEOUT and MAX_UNMATCHED_RESPONSES in src/client/mod.rs.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-20T05:16:26.155Z",
    "resolved_at": null
  },
  {
    "id": 25,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/client/mod.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-22, closing CR-02's SURVIVING half - the self-sustaining chain the THIRD gap-closure round left behind, recorded as entry 24 of this ledger, and disclosed nowhere until now). WHAT A CONSUMER OBSERVED BEFORE: one abandoned call plus a peer whose honest round trip outlasts UNMATCHED_RESPONSE_TIMEOUT (10 s) failed EVERY SUBSEQUENT CALL on that Client, silently and for the life of the process. The mechanism was a MISCLASSIFICATION, not a bound that was too tight: Client::dispatch_request removes its registration on every Err exit and drains nothing, so a call that dies at its ceiling leaves its own real answer un-owned on the shared receive queue; the NEXT call pops that answer, finds no owner, books it as the peer's misbehaviour and arms ITS OWN ten-second deadline on frame one. AFTER: a response frame that is the late answer of a request this client already stopped waiting for is classified as our OWN debris and absorbed exactly once, charging nobody. THE BOUND THAT REMAINS, unchanged in value and still firing for the peer misbehaviour it was written for: a frame addressed to an id THIS CLIENT NEVER MINTED still spends budget and still fails the awaiting call at the same two values - MAX_UNMATCHED_RESPONSES (32) and UNMATCHED_RESPONSE_TIMEOUT (10 s) - with the same named, counted error naming both typed ids. THE BOUND THAT NEVER EXISTED, and still does not: a peer that has simply GONE QUIET is not bounded at all. pmcp has no default request timeout, deliberately, because a blanket deadline would break long-running tools/call handlers; that is a product decision recorded as such, not a review gap left open by accident. THE MEMORY THIS FIX ADDS, and why a peer cannot grow it: AbandonedRequestIds is a VecDeque<RequestId> at a fixed PRIVATE cap MAX_ABANDONED_REQUEST_IDS = 64, oldest-evicted, with each entry consumed on its first take, and it can only ever hold ids that came back from an active_requests::remove - i.e. ids this client itself minted - so a hostile peer cannot put a single entry into it at any rate. No public symbol changed, no bound was loosened, and nothing that succeeded before now fails; the victim set shrinks from \"every later call on this Client\" to \"the call whose peer is actually misbehaving\", with no new victim class created. NO SEMVER SIGNAL: AbandonedRequestIds, MAX_ABANDONED_REQUEST_IDS and Client::absorb_abandoned are all private. Fenced by debris_from_a_dead_call_does_not_charge_the_next_calls_budget - deterministic by COUNT with no wall clock in it, three sequential calls sprayed 32 / 31 / 0 un-owned frames so call two can only reach the bound by being charged for call one's leftover answer - plus five unit fences covering absorption, single-use, the never-minted case, cap eviction and stale-debris clearing on re-registration. LOCATE BY SYMBOL, not by line: AbandonedRequestIds, MAX_ABANDONED_REQUEST_IDS, Client::absorb_abandoned and the no-owner branch of Client::pump_once in src/client/mod.rs. Full record: 118.2-22-SUMMARY.md.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-20T05:16:49.798Z",
    "resolved_at": null
  },
  {
    "id": 26,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/shared/streamable_http.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-25). TWO transport-wide sequences on StreamableHttpTransport that were serialised only by the pmcp Client's outer transport write guard - i.e. by accident - are now serialised IN THEIR OWN RIGHT. This landed BEFORE 118.2-23 removed that guard, precisely so that no commit in this round ever carried the hazard: had the order been reversed, the window between the two plans would have shipped a transport whose 401 recovery and session-stream restart were unprotected. (1) THE 401 TOKEN RECOVERY IS NOW SINGLE-FLIGHT, and the guarded region runs from the purge THROUGH the retry request's get_access_token - purge inside, retry BUILD inside, retry SEND outside - so two concurrent 401s on one transport produce exactly ONE AuthProvider::on_unauthorized call and exactly ONE token vend. BEFORE, a consumer holding two clones of one transport (the durable-agent shape: one transport shared across concurrent tasks) had both clones call on_unauthorized and, one step later, both reach get_access_token concurrently against the cache the first had just emptied. Against a ROTATING refresh token that is two presentations of the same refresh token: the identity provider accepts one and rejects the other, and the rejected caller's purge destroys the token the winner had just cached, so the transport's auth fails PERMANENTLY until an out-of-band re-authentication. This is the recorded SDK blocker for the durable-agent deployment shape, not a thought experiment. The retry POST is deliberately NOT inside the lock, so the transport gains no whole-transport bottleneck. (2) THE SESSION-STREAM RESTART IS NOW ATOMIC across its take-abort / open / reset / respawn sequence. BEFORE, two concurrent restarts each took a None abort handle, each opened a GET, and the second's store orphaned the first's reader; because close() aborts exactly one JoinHandle, that orphan outlived the transport holding a live peer-controlled socket that nothing could reach. A caller that uses a transport one call at a time sees byte-identical behaviour in both cases: the same single 401 retry, the same single restart, the same errors. NO public signature changed, the AuthProvider trait is UNTOUCHED, no dependency was added, and no bound, deadline or timeout was introduced anywhere. NO SEMVER SIGNAL: refresh_lock, token_generation and restart_lock are private fields. Fenced by two_concurrent_401s_refresh_the_token_once and two_concurrent_session_stream_restarts_leave_one_reader, plus five unit fences; the auth fence was validated by a COUNTERFACTUAL - with only the purge single-flighted it is STILL red on the vend peak - so it demonstrably measures the vend rather than the operation adjacent to it. NAMED RESIDUAL WITH AN OWNER, disclosed here rather than left implicit, because this phase's entire failure history is undisclosed residuals: \"exactly one vend\" is PRECONDITIONED on the AuthProvider implementation CACHING what get_access_token vends before returning, and the trait imposes no such contract - on_unauthorized's default body is a no-op and get_access_token is unconstrained. Against a NON-CACHING provider the generation check is INERT: the loser skips the purge but vends anyway, so all the lock buys is that the two vends are SERIALISED rather than simultaneous, which a rotating refresh token still rejects. REACHABILITY, stated honestly: the shape is a durable agent holding one transport across concurrent tasks - the same shape part (1) exists for. WHY IT IS UNFENCEABLE IN-REPO rather than merely unfenced: there is NO in-tree client-side AuthProvider implementation at all. grep over src/ and tests/ finds the UNRELATED server-side trait of the same name (src/server/auth/traits.rs) and, for the client-side trait at src/shared/streamable_http.rs, only test doubles - CountingProvider, MinimalProvider, DualTokenProvider and CachingProbe under cfg(test), plus CountingProvider, TestAuthProvider, MockAuthProvider and CachingAuthProvider in tests/. Every real implementation is downstream, and no in-tree implementation demonstrates the behaviour either way. WHY THE ASSUMPTION IS NONETHELESS WELL-FOUNDED: the trait's own rustdoc tells implementors to \"evict any cached access token here so that the subsequent get_access_token() call returns a freshly-vended token rather than the now-invalid cached one\" - wording that only makes sense if get_access_token normally serves FROM CACHE. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section - the same owner as entry 23. THE DURABLE FIX, recorded for whoever picks it up: state a caching contract on the trait. That is a decision with an external-implementor cost, not a gap-closure edit, which is why it was not taken here. AMENDED 2026-08-20 BY CODE REVIEW OF THIS ROUND'S OWN DIFF, in place per the house rule 118.2-21 set for this file, because the mirror in pmcp-book/src/ch12-17-migrating-to-mcp-2026-07-28.md was updated with BOTH facts below while THIS entry - the source of record - carried neither, leaving the mirror saying more than its source. The disclosure tripwire cannot see that: it checks that a CONSUMER-OBSERVABLE entry is CITED in the chapter, not that the chapter's claims are backed by the ledger. (i) THE SINGLE-FLIGHT COVERS THE 401 RECOVERY ONLY. AuthProvider::get_access_token runs OUTSIDE refresh_lock on every request build (build_request_from_parts), so a COLD-START FAN-OUT - one cloned transport, N concurrent first requests against an empty provider cache - still reaches the provider N times concurrently. Against a rotating refresh token that is the SAME permanent auth failure this entry's single-flight exists to prevent, reached on the path every deployment hits BEFORE any 401 has ever occurred. CLOSED 2026-08-20, same day, by ColdVendGate in src/shared/streamable_http.rs: a SECOND gate rather than a wider refresh_lock, because the retry request is built INSIDE refresh_lock and a build path taking that same mutex would self-deadlock on the first 401. It single-flights the vend ONLY while the provider cache is believed cold - an AtomicBool fast path means a warm transport takes no lock per request, which is what keeps this from re-creating the whole-transport bottleneck T-118.2-25-03 prohibits - and it is RE-ARMED after on_unauthorized evicts, or it would have guarded the startup window only and let the identical fan-out recur one eviction later. Lock order is always refresh_lock -> ColdVendGate and nothing takes refresh_lock while holding the gate, so no cycle exists. The reconnect path goes through the same gate, carried on RequestParts, because a reconnect storm against a cold cache is the same fan-out. FENCED by fence 31, a_cold_start_fanout_on_one_transport_vends_once in tests/client_sse_stream.rs, which was demonstrated RED on the ungated tree - peak 4 of 4 callers inside the vend concurrently - before it went green, so it is not a fence that cannot fail. THE RESIDUAL IS UNCHANGED AND IS NOT CLOSED BY THIS GATE: against a NON-caching AuthProvider every caller vends regardless, because the gate holds no token and asking the provider again is what reads the winner's cached value. That is the same residual token_generation already carried and it still awaits the trait-level caching contract named above. (ii) THE ATOMIC RESTART IS RESTART-VS-RESTART ONLY. Transport::close() does not take restart_lock, so a start_sse holding the lock and awaiting its GET can store a reader handle AFTER close() already took - and found None in - abort_handle. That reader is stopped by run_session_stream's separate shutdown watch, NOT by close()'s abort: the invariant survives on a second mechanism, which a future reader must know before removing that watch. 118.2-23 made this pairing reachable by removing the client guard that had made send and close mutually exclusive. (iii) AMENDED 2026-08-20 by 118.2 re-verification at HEAD dc0587af, which rated this a WARNING: the residual below was disclosed ONLY in the phase-internal 118.2-SECURITY.md (accepted risk R-118.2-02), while every sibling residual in this phase - CR-03 as entry 23, and (i)/(ii) above - got a ledger entry AND a migration-chapter mirror. Stated here so a consumer meets it in the record rather than in a security file they will never read. THE RESIDUAL: start_sse takes restart_lock and HOLDS IT ACROSS open_sse_once's await of the peer's GET RESPONSE HEAD. There is no request timeout on any production path in src/shared/streamable_http.rs - the only production timeout in the file is .pool_idle_timeout(90s), and every tokio::time::timeout in it sits inside the cfg(test) mod tests. So a peer that ACCEPTS the session-stream GET and then never writes its response head holds a TRANSPORT-WIDE lock indefinitely, blocking every later session-stream restart on that transport - the resumption-cursor send path and the 202 notifications/initialized path both park at that lock with nothing to bound them. Severity MEDIUM, below the security gate's `high` block threshold, which is why it was accepted rather than fixed. NOT A CONTRADICTION of item 3 above: that item's claim is that the restart SEQUENCE is indivisible, and it is; this is the separate cost of the lock's DURATION. The durable fix is a request timeout, which entry 20 records as DELIBERATELY REJECTED in its RequestOptions::timeout form - so closing this needs the bounded-request decision reopened, not a local edit. Owner: the client-transport hardening plan named in deferred-items.md, the same owner as entry 23. LOCATE BY SYMBOL, not by line: refresh_lock, token_generation and restart_lock and their rustdoc in src/shared/streamable_http.rs. Full record: 118.2-25-SUMMARY.md.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-20T05:17:16.650Z",
    "resolved_at": null
  },
  {
    "id": 27,
    "kind": "deviation",
    "phase": "118.2",
    "file": "src/client/mod.rs",
    "line": null,
    "description": "[CONSUMER-OBSERVABLE] BEHAVIOUR CHANGE, by design (118.2-23, closing the SEND-path wedge the THIRD gap-closure round left behind - entry 24 of this ledger - and which neither code review nor the previous verification had seen). FOUR things a consumer can observe, and all four are stated here rather than only the flattering one. (1) A STALLED PEER NO LONGER SERIALISES THE WHOLE CLIENT. Before, Client::dispatch_request took the transport WRITE guard and awaited the send inside it; that send reaches StreamableHttpTransport::post_once, which awaits the peer's response HEADERS, and no request timeout exists on any path in src/shared/streamable_http.rs. A peer that accepted the POST and never wrote its response head therefore held the one transport guard for as long as it liked, and every other operation on that Client - a second call, a notification, a cancellation, Transport::close - blocked at acquisition with nothing to bound it. Now every client-side send goes through Client::send_frame, which takes an OWNED handle under a momentary READ guard and DROPS that guard before the round trip is awaited. Measured, not argued: fence 30 turned six seconds of wedge into one second of ordinary traffic with the peer still silent. NOTE what did NOT change: the stalled call itself still waits indefinitely. No timeout, no response-header deadline and no clock of any kind was added, deliberately - a JSON-answering or Lambda-hosted server writes its response head only when the handler finishes, so any finite bound would break a legitimate long tools/call that works today. (2) TWO POSTs CAN NOW BE IN FLIGHT ON ONE TRANSPORT AT ONCE, where the pmcp client previously imposed a total order. Three things a POST touches beyond its own call were checked BY NAME - the per-caller POST-reader accounting (an atomic), the transport-wide AuthProvider (single-flighted by 118.2-25's refresh_lock from the purge through the vend, entry 26) and start_sse reached from post_body's 202 branch (made indivisible by 118.2-25's restart_lock) - and nothing beyond those three is asserted. (3) OUTBOUND FRAME ORDERING ACROSS SEPARATE POSTs IS NO LONGER IMPOSED BY THE CLIENT. HTTP never guaranteed one, and transports on the exclusive path keep their total order, but a consumer who was relying on the client's incidental ordering will not get it. (4) A POOLED TRANSPORT KEEPS THE WEDGE. PooledTransport<T> is the ONE wrapping impl Transport for ... in the tree, and it inherits the new accessor's default None BY EXPLICIT DECISION with a Why comment at the impl, not by omission: ConnectionPool::send_message chooses a connection PER SEND and a per-connection worker task owning its own clone performs the I/O, so no inner transport's handle would be correct for a FUTURE send and forwarding one would silently pin every shared send to a single connection. NAMED, OWNED RESIDUAL: a consumer who pools a StreamableHttpTransport keeps the exclusive &mut path. It is materially narrower than the wedge (1) closes - send_to_connection awaits an mpsc push, not the peer's response head - but it is not zero: with the connection's channel full (capacity 100) the push blocks until the worker drains, and the worker is itself blocked on the stalled peer, so sustained traffic against a silent peer can still serialise a client over a pooled transport. OWNER: the client-transport hardening plan named in this phase's deferred-items.md Disposition section, the same owner as entries 23 and 26. A SECOND NAMED, OWNED RESIDUAL, found DURING 118.2-23 rather than before it, and recorded here so it is booked rather than lost: Client::open_event_stream still holds a READ guard across the subscriptions/listen POST's response HEAD. A read guard is no better than a write guard here - tokio's RwLock is fair, so a writer arriving parks and every later reader parks behind it - so a peer that accepts that POST and never answers still serialises the client, exactly as dispatch_request did. It is NOT closed because the owned-handle seam (Transport::shared_sender) does not exist on EventStreamTransport, whose open_event_stream is reached through the generic T with no way to own one; adding it is a PUBLIC-TRAIT change and belongs to a decision, not to a gap-closure round. Blast radius: callers of subscriptions/listen only; every other client-side send is off the guard. The residual is also recorded IN PLACE at the guard in src/client/mod.rs under the marker \"KNOWN RESIDUAL (Phase 118.2, plan 23)\". OWNER: as above. THE PUBLIC-API CHANGE, which unlike everything else in this ledger DOES have a semver surface: a new public trait SharedSender and a new method Transport::shared_sender(&self) -> Option<Arc<dyn SharedSender>> DEFAULTED to None on BOTH the native and the wasm32 trait definitions. Every external implementor compiles unchanged AND behaves unchanged, and every transport in this tree except StreamableHttpTransport keeps the default, so transports without a shared-send path are byte-for-byte unchanged on the wire. cargo semver-checks reports no update required - with the caveat this phase has now recorded five times, that it is a breaking-change LINTER and not an API-diff inventory, so for an ADDITION \"nothing fired\" is the whole of the claim; the additive shape is evidenced separately by a_transport_that_does_not_opt_in_answers_none, which implements only the pre-118.2-23 trait surface and still compiles and still answers None. Fenced by a_peer_that_never_writes_response_headers_does_not_serialise_the_client, plus stdio_offers_no_shared_send_path and the_shared_handle_writes_the_same_request_as_the_exclusive_path. LOCATE BY SYMBOL, not by line: SharedSender and Transport::shared_sender in src/shared/transport.rs; StreamableHttpTransport::send_with_options_shared in src/shared/streamable_http.rs; Client::send_frame and ClientFrame in src/client/mod.rs; the Why comment at impl Transport for PooledTransport in src/shared/connection_pool.rs. Full record: 118.2-23-SUMMARY.md.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-20T05:17:48.299Z",
    "resolved_at": null
  }
]
````
